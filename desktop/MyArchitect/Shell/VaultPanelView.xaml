<UserControl x:Class="MyArchitect.Shell.VaultPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#1E1E1E">
    <UserControl.Resources>
        <Style x:Key="VaultInput" TargetType="Control">
            <Setter Property="Background" Value="#252526" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#3C3C3C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="VaultButton" TargetType="Button">
            <Setter Property="Background" Value="#0E639C" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="VaultChipButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="#CCCCCC" />
            <Setter Property="BorderBrush" Value="#3C3C3C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="VaultLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#858585" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="0">
        <!-- ============ CREATE (first run) ============ -->
        <Grid x:Name="CreatePanel" Visibility="Collapsed" MaxWidth="440" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Credential Vault" Foreground="#FFFFFF" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock TextWrapping="Wrap" Foreground="#B0B0B0" FontSize="12" Margin="0,0,0,16"
                           Text="Set a master password to protect this vault. It encrypts every stored credential and is required once each time you open MyArchitect. There is no recovery — if you forget it, the stored credentials cannot be decrypted." />
                <TextBlock Style="{StaticResource VaultLabel}" Text="Master password" />
                <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource VaultInput}" />
                <TextBlock Style="{StaticResource VaultLabel}" Text="Confirm master password" />
                <PasswordBox x:Name="ConfirmPasswordBox" Style="{StaticResource VaultInput}" />
                <TextBlock x:Name="CreateErrorText" Foreground="#F48771" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8" />
                <Button x:Name="CreateButton" Style="{StaticResource VaultButton}" Content="Create Vault" HorizontalAlignment="Left" Click="CreateButton_Click" />
            </StackPanel>
        </Grid>

        <!-- ============ UNLOCK ============ -->
        <Grid x:Name="UnlockPanel" Visibility="Collapsed" MaxWidth="440" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Credential Vault" Foreground="#FFFFFF" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock Text="Locked. Enter your master password to unlock this session." Foreground="#B0B0B0" FontSize="12" Margin="0,0,0,16" TextWrapping="Wrap" />
                <TextBlock Style="{StaticResource VaultLabel}" Text="Master password" />
                <PasswordBox x:Name="UnlockPasswordBox" Style="{StaticResource VaultInput}" KeyDown="UnlockPasswordBox_KeyDown" />
                <TextBlock x:Name="UnlockErrorText" Foreground="#F48771" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8" />
                <Button x:Name="UnlockButton" Style="{StaticResource VaultButton}" Content="Unlock" HorizontalAlignment="Left" Click="UnlockButton_Click" />
            </StackPanel>
        </Grid>

        <!-- ============ UNLOCKED ============ -->
        <Grid x:Name="UnlockedPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Background="#181818" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,10" VerticalAlignment="Center">
                    <TextBlock Text="Credential Vault" Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <TextBlock x:Name="TenantHeaderText" Foreground="#858585" FontSize="13" VerticalAlignment="Center" Margin="8,0,0,0" />
                </StackPanel>
                <Button Grid.Column="1" x:Name="LockButton" Style="{StaticResource VaultChipButton}" Content="Lock" Margin="0,0,16,0" VerticalAlignment="Center" Click="LockButton_Click" />
            </Grid>

            <!-- Add / Edit form -->
            <Border Grid.Row="1" Background="#232323" BorderBrush="#2B2B2B" BorderThickness="0,1,0,1" Padding="16,12">
                <StackPanel>
                    <TextBlock x:Name="FormHeaderText" Text="Add credential" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource VaultLabel}" Text="Label" />
                            <TextBox x:Name="FormLabelBox" Style="{StaticResource VaultInput}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource VaultLabel}" Text="Username" />
                            <TextBox x:Name="FormUsernameBox" Style="{StaticResource VaultInput}" />
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock x:Name="FormPasswordLabel" Style="{StaticResource VaultLabel}" Text="Password" />
                            <PasswordBox x:Name="FormPasswordBox" Style="{StaticResource VaultInput}" />
                        </StackPanel>
                    </Grid>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="SaveButton" Style="{StaticResource VaultButton}" Content="Add Credential" Click="SaveButton_Click" />
                        <Button x:Name="CancelEditButton" Style="{StaticResource VaultChipButton}" Content="Cancel" Margin="8,0,0,0" Visibility="Collapsed" Click="CancelEditButton_Click" />
                        <TextBlock x:Name="FormErrorText" Foreground="#F48771" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0" Visibility="Collapsed" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Entry list -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="16,10">
                <StackPanel x:Name="EntriesHost" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
