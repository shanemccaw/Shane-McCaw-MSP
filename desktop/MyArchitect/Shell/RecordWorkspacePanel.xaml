<UserControl x:Class="MyArchitect.Shell.RecordWorkspacePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#181818"
             d:DesignWidth="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header: eyebrow / title / sub -->
        <Border Grid.Row="0" Padding="14,12" BorderBrush="#2B2B2B" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock x:Name="EyebrowText" FontSize="10" FontWeight="Bold" Foreground="#389BFF" Text="RECORD" />
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Margin="0,4,0,0" TextWrapping="Wrap" />
                <TextBlock x:Name="SubText" FontSize="11" Foreground="#858585" Margin="0,2,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="BodyPanel" Margin="14,12" />
        </ScrollViewer>

        <!-- Empty state -->
        <StackPanel x:Name="EmptyState" Grid.Row="0" Grid.RowSpan="2"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <TextBlock Text="No record open" Foreground="#858585" FontSize="12" HorizontalAlignment="Center" />
            <TextBlock Text="Select a row from a gallery or contextual tab to work it here."
                       Foreground="#5A5A5A" FontSize="11" TextWrapping="Wrap" TextAlignment="Center"
                       MaxWidth="240" Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
