<Window x:Class="BuildConsole.NewIssueDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Issue" Height="480" Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BaseBrush}"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Title"
                   FontSize="11"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource Subtext1Brush}"
                   Margin="0,0,0,4"/>

        <TextBox x:Name="TitleBox"
                 Grid.Row="1"
                 Height="26"
                 FontSize="13"
                 Padding="6,2"
                 Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2"
                   Text="Body"
                   FontSize="11"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource Subtext1Brush}"
                   Margin="0,0,0,4"/>

        <TextBox x:Name="BodyBox"
                 Grid.Row="3"
                 Style="{StaticResource EditorTextBox}"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <DockPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="Epic:"
                       FontSize="12"
                       Foreground="{StaticResource Subtext1Brush}"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>
            <Button x:Name="BtnClearEpic"
                    DockPanel.Dock="Right"
                    Content="Clear"
                    Padding="8,4"
                    Margin="6,0,0,0"
                    Visibility="Collapsed"
                    Click="BtnClearEpic_Click"/>
            <Button x:Name="BtnPickEpic"
                    DockPanel.Dock="Right"
                    Content="Choose..."
                    Padding="10,4"
                    Click="BtnPickEpic_Click"/>
            <TextBlock x:Name="SelectedEpicText"
                       Text="(none)"
                       FontSize="12"
                       Foreground="{StaticResource TextBrush}"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"/>
        </DockPanel>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Content="Cancel"
                    Padding="14,6"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Create Issue"
                    Style="{StaticResource PrimaryButton}"
                    Padding="14,6"
                    Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
