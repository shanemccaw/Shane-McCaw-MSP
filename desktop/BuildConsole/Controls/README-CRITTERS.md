using System;
using System.Windows;

// Wire the spawner into FocusCharacterLayer by creating an instance and calling it from CelebrateBuildFinished.
// This file modifies FocusCharacterLayer.xaml.cs to instantiate CritterSpawner and invoke it on build celebrations.
