<UserControl x:Class="BuildConsole.Controls.BuildLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource CrustBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>  <!-- context header -->
            <RowDefinition Height="*"/>   <!-- log stream   -->
        </Grid.RowDefinitions>

        <!-- ── Context header ── -->
        <Border Grid.Row="0"
                Background="{StaticResource MantleBrush}"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="0,0,0,1"
                Padding="10,0">
            <DockPanel VerticalAlignment="Center">

                <!-- Status dot -->
                <Ellipse x:Name="HeaderDot"
                         Width="8" Height="8"
                         Fill="{StaticResource Surface2Brush}"
                         Margin="0,0,10,0"
                         VerticalAlignment="Center"/>

                <!-- BUILDING label -->
                <TextBlock x:Name="HeaderVerb"
                           Text="SELECT A TASK"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Subtext1Brush}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <!-- Epic breadcrumb -->
                <TextBlock x:Name="EpicLabel"
                           Text=""
                           FontSize="12"
                           Foreground="{StaticResource Subtext0Brush}"
                           VerticalAlignment="Center"/>

                <TextBlock Text=" › "
                           FontSize="12"
                           Foreground="{StaticResource Subtext1Brush}"
                           VerticalAlignment="Center"/>

                <!-- Task name -->
                <TextBlock x:Name="TaskLabel"
                           Text=""
                           FontSize="12"
                           Foreground="{StaticResource TextBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,14,0"/>

                <!-- Git #944 - Shane: "In the open space can you add pills for
                     every running build so I can easily switch between them...
                     check actual running claude instances, not what the queue
                     says." Ground truth is the real claude CLI agent list (real
                     OS processes), NOT queue status - #943 just proved queue
                     status can be wrong while the real process keeps going.
                     Sits in the open space between the task name and the
                     right-docked status badge/elapsed time. -->
                <ItemsControl x:Name="RunningPillsPanel" VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <!-- Status badge -->
                <Border x:Name="StatusBadge"
                        DockPanel.Dock="Right"
                        Background="{StaticResource Surface0Brush}"
                        CornerRadius="3"
                        Padding="6,2"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">
                    <TextBlock x:Name="StatusBadgeText"
                               Text=""
                               FontSize="11"
                               Foreground="{StaticResource Subtext1Brush}"/>
                </Border>

                <!-- Elapsed time -->
                <TextBlock x:Name="ElapsedLabel"
                           DockPanel.Dock="Right"
                           Text=""
                           FontSize="11"
                           Foreground="{StaticResource Subtext1Brush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0,0,12,0"/>

            </DockPanel>
        </Border>

        <!-- ── Real log stream (Git #819 — was 100% fabricated demo entries; now the actual per-item file scripts/build-queue-watcher.ps1 / QueueWatcherService writes, same one the chat-tab split pane tails) ── -->
        <TextBox x:Name="RawLogBox"
                 Grid.Row="1"
                 Style="{StaticResource TerminalOutputBox}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"/>

        <!-- Empty state overlay -->
        <StackPanel x:Name="EmptyState"
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Opacity="0.4">
            <TextBlock Text="&#xE7C4;"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="32"
                       Foreground="{StaticResource Surface2Brush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Click a task in the Build Queue to see its real log"
                       FontSize="12"
                       Foreground="{StaticResource Subtext1Brush}"
                       HorizontalAlignment="Center"/>
        </StackPanel>

    </Grid>
</UserControl>
