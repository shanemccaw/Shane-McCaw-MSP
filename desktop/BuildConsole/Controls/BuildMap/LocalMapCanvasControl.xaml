<UserControl x:Class="BuildConsole.Controls.BuildMap.LocalMapCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True">

    <!--
        Git #2806 (Local Map, item #2 of #2804's structured index) — the schema-graph canvas,
        reusing BuildChainMapWindow's real rendering/interaction pattern
        (Controls/BuildMap/ChainCanvasControl.xaml): Scroller + Stage + LayoutTransform-based
        uniform zoom, edge layer drawn under the node layer. Geometry comes from
        Services/BuildMap/LocalMapLayout.cs — no positioning math lives in this control, same
        discipline as ChainCanvasControl.
    -->

    <ScrollViewer x:Name="Scroller"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  Focusable="False">
        <ScrollViewer.Background>
            <!-- Same #0a0d12 + 22px dot grid as ChainCanvasControl, for the same "real map surface"
                 feel across both the Git Map and the Local Map. -->
            <DrawingBrush TileMode="Tile" Viewport="0,0,22,22" ViewportUnits="Absolute" Stretch="None">
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#0a0d12">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,22,22"/>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing Brush="#141a22">
                            <GeometryDrawing.Geometry>
                                <EllipseGeometry Center="1,1" RadiusX="1" RadiusY="1"/>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </ScrollViewer.Background>

        <Canvas x:Name="Stage" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Transparent">
            <Canvas.LayoutTransform>
                <ScaleTransform x:Name="StageScale" ScaleX="1" ScaleY="1"/>
            </Canvas.LayoutTransform>

            <Canvas x:Name="EdgeLayerHost" Canvas.Left="0" Canvas.Top="0"/>
            <Canvas x:Name="NodeLayer" Canvas.Left="0" Canvas.Top="0"/>
        </Canvas>
    </ScrollViewer>
</UserControl>
