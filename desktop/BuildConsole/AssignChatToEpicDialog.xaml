<Window x:Class="BuildConsole.AssignChatToEpicDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Chat to Epic" Height="240" Width="460"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BaseBrush}"
        ResizeMode="NoResize">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Text="Assign chat to epic:"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="Chat URL"
                   FontSize="11"
                   Foreground="{StaticResource Subtext1Brush}"
                   Margin="0,0,0,4"/>

        <TextBox x:Name="ChatUrlBox"
                 Grid.Row="2"
                 Background="{StaticResource MantleBrush}"
                 BorderBrush="{StaticResource Surface0Brush}"
                 BorderThickness="1"
                 Foreground="{StaticResource TextBrush}"
                 Padding="6,5"
                 FontSize="12"/>

        <Button x:Name="UseCurrentChatButton"
                Grid.Row="3"
                Content="Assign current chat"
                HorizontalAlignment="Left"
                Padding="10,5"
                Margin="0,8,0,0"
                Click="UseCurrentChat_Click"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   FontSize="11"
                   Foreground="{StaticResource RedBrush}"
                   TextWrapping="Wrap"
                   VerticalAlignment="Top"
                   Margin="0,8,0,0"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Cancel"
                    Padding="14,6"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Assign"
                    Style="{StaticResource PrimaryButton}"
                    Padding="14,6"
                    Click="Assign_Click"/>
        </StackPanel>
    </Grid>
</Window>
