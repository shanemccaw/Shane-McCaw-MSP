# BuildConsole Build Log

Append-only log of work sessions whose work is entirely within `desktop/BuildConsole/` (this WPF app). Each session adds exactly one row, in two edits:

1. **At session start** (before any real work): append a row with status `⏳ IN FLIGHT — {step name}`, commit it immediately as its own standalone commit.
2. **At session end** (after the real work is done, tested, and typechecked): update that same row to `✅ DONE — {step name}`, filling in the real commit hash of the work, then commit the update (either folded into the final commit or immediately after it).

If a session dies, crashes, or is abandoned mid-way, its row stays `⏳ IN FLIGHT`, which is itself the record that an attempt was made and didn't finish.

See [CLAUDE.md](../../CLAUDE.md) for the exact instructions, including which sessions bookend here versus in the root [PLATFORM_BUILD.md](../../PLATFORM_BUILD.md).

| Date | Status | Step | Commit |
|------|--------|------|--------|
| 2026-08-23 | ✅ DONE — Replaced flakey WebView2 DOM usage-meter scraping with direct CLI-first claude -p /usage query (runs in ~2s, 100% reliable): (1) ClaudeUsageMeterService spawns claude.exe -p --no-session-persistence "/usage" with closed stdin to fetch direct subscription usage; (2) Targeted extraction specifically for the "All Models" progress bar (Current week (all models): X% used), ignoring "Current session" and "Fable"; (3) Timezone-aware reset parsing converting IANA / Windows timezone strings (e.g. Aug 23, 9pm (America/New_York)) into local DateTime targets; (4) Retained WebView2 probe as a secondary fallback. Build clean (dotnet build -c Release 0 errors). | Claude usage meter CLI-first All Models | `1160d4a8` |
| 2026-08-23 | ⏳ IN FLIGHT — Manual "Deploy to Staging" button (local, Shane-only): one deliberate action doing the full real chain — SSH connect + git pull (ReplitSshService.DeployAsync) -> apply any pending CREATE/ALTER/INSERT migration SQL + restart via #911 build-complete (schema-SQL-apply, the existing migration mechanism, targeted at the Staging/Replit tier via BuildTrackerConfig.ForEnvironment(Staging)) -> confirm restart via #805 deploy-status poll-until-flip -> non-blocking ToastEngine (#24) notification with real per-step success/failure detail. No automatic test-triggering (Shane switches the env selector to Staging and runs tests himself afterward). Also pausing the earlier auto-Replit automation per the new local-first scope (AutoDeployOnBuildComplete / AutoRunTestsOnBuildComplete defaults -> false; ReplitWatcher + scheduled regression already default-off — flagged). | Deploy to Staging button | `pending` |
| 2026-08-23 | ✅ DONE — Three-tier Target Environment architecture (Dev local / Staging Replit / Production live) + protocol-layer Dev-only hard safety boundary: (1) Added TargetEnvironment enum (Dev, Staging, Production) and per-tier URL configuration (devBaseUrl, stagingBaseUrl, productionBaseUrl) in BuildTrackerConfig.cs and scripts/build-queue-watcher.config.json(.example) with ForEnvironment(TargetEnvironment env) resolution; (2) Hard-enforced TargetEnvironment.Dev for shaneapp://runTest, shaneapp://runScan, shaneapp://executeScan, and shaneapp://executeSql protocol executions with explicit security log notices; (3) Added manual Target Environment selector ComboBox in LeftSidebar's Automation view with prominent visual warning badges for Staging (amber) and Production (red), auto-resetting back to Dev after non-Dev runs, and updated TestRunnerWindow with a target environment badge in the status header and environment tracking on RetryRequested; (4) Updated SettingsTabView to display all three environment URLs; (5) Updated stripe.ts, env.ts, admin-testbed.ts, and sync-stripe-webhooks.ts with isDevDomain / isReplitDevEnvironment to recognize local development origins (localhost, 127.0.0.1, [::1], *.local, APP_ENV=dev) while maintaining strict production barriers. Built clean (dotnet build -c Release 0 errors, 12/12 stripe.test.ts unit tests passed). | Target Environment & Dev safety boundary | `e2171c6f` |
| 2026-08-19 | ✅ DONE — Fixed trigger-deploy-and-wait.ps1 falsely reporting TIMED OUT over a deploy that had genuinely succeeded: the flat 300s cap expired during a retheme-sized restart that really took ~10 minutes (kill 1 reboots the whole Repl container, which re-runs install + a FULL rebuild before the server listens again). Raised the hard ceiling 300s -> 1800s AND, per the ask's better option, made the wait end on the server's OBSERVED state rather than the clock: unreachable = restart/rebuild still in progress, waited out with a heartbeat carrying real elapsed time (new -HeartbeatSeconds, default 15, so a 3s poll does not emit hundreds of identical lines); commit hash flipped = success; reachable and still on the OLD commit for a new -StallSeconds (default 180) straight = not deploying, stops EARLY naming which case it actually is - RESTARTED ON THE OLD COMMIT (the restart worked, there was simply nothing new on origin/main to pull) vs RESTART NEVER TOOK EFFECT (the server never went down) vs HARD CEILING HIT (still rebuilding past -TimeoutSeconds; re-run with a larger one, it is a backstop not a verdict). Any unreachable poll resets the stall window, so a rebuild that bounces the server can never trip it. Net effect: a genuinely failed deploy is now reported FASTER than before (~3 min rather than 5) while a genuinely slow one is allowed to finish instead of being called a failure. Also: parse the commit the server's own git pull landed the CHECKOUT on (both shapes - "Updating x..y" and the reset --hard fallback's "HEAD is now at y") and warn if the live hash ends up being a different one; compare hashes on the shorter abbreviation since rev-parse --short is adaptive; build-complete POST timeout 120s -> 240s (911 can spend up to 120s on the schema-SQL transaction plus 60s on the pull before it ever replies); and say up front when deploy-status reports "unknown", where a flip can never be observed at all. Deliberately does NOT fast-exit on "Already up to date" - after the SSH pull pre-step (82's hybrid) the checkout is already advanced while the running process is still on old code, so the restart genuinely will flip the hash. VERIFIED end to end against a throwaway local stub api-server (HttpListener, written outside the repo): all four outcomes produce the right banner and exit code - flip-after-downtime exit 0 in 12.3s, up-and-unchanged stall exit 1 in 12.3s, down-then-back-on-the-old-commit stall exit 1 in 18.3s (stall window correctly restarted from when it came back, not from the trigger), hard ceiling exit 1 - plus unit checks of the pull-output parse (both shapes, null on "Already up to date"/empty) and the hash comparison (7 vs 8 chars both directions, case-insensitive, null-safe). Script parses clean through the PowerShell AST parser and its comment-based help still binds. Exit-code contract unchanged (0 = confirmed live, 1 = not confirmed), so nothing calling it breaks. AGENT_PROTOCOLS.md section 4 updated to match (progress-aware table, new params, the three distinct red outcomes). FLAGGED BUT NOT CHANGED: PostBuildDeployPipeline.cs's DeployTimeoutSeconds = 600 is the same flat-cap defect in the C# mirror of this exact poll loop, and that is the default-ON automatic path - a ~10 minute restart lands right on that boundary. Left for Shane to decide, since changing it means rebuilding BuildConsole. No DB work. WPF-internal tooling, so out of the test-manifest coverage scope (msp-portal / shane-mccaw-consulting only). No Git # - not filed as an issue, so no label sync. | Progress-aware deploy restart wait | `f5b976eb` |
| 2026-08-17 | ✅ DONE — #82 build→deploy→test chain: swap deploy to SSH where the concurrent SSH refactor genuinely replaces #911/#805. Read #82's real landed `PostBuildDeployPipeline.cs` in full + the concurrent SSH refactor (committed `b3958821` — `ReplitSshService.cs`/settings/`Watch-Deploy.ps1` — plus that agent's LIVE uncommitted edits, left untouched). **Confirmed exactly what SSH genuinely provides:** git PULL+build ✅ (`DeployAsync` = `git fetch origin main && git reset --hard origin/main && npm run build`); a real RESTART ❌ (no `kill 1` anywhere — and per `admin-deploy-console.ts` a pull only rewrites files, the old code keeps running until PID 1 is killed, and Replit does NOT auto-restart); a genuine live-CONFIRM ❌ (`GetRemoteCommitHashAsync` = `git rev-parse HEAD` reads the CHECKOUT, which advances the instant `reset --hard` runs — NOT the running process; only #805's server-STARTUP commit is real restart proof). The landed SSH branch (`b3958821`) was therefore unsafe: it "confirmed live" via checkout-HEAD with no restart, running tests against possibly-stale in-memory code — the exact failure #82's ancestry/flip machinery exists to prevent. **Fix = documented HYBRID:** SSH now does the git PULL+build pre-step (the one piece genuinely replaced); the RESTART + genuine live-CONFIRM stay on HTTP #911 (`kill 1`) / #805 (startup-commit flip), since SSH hasn't replaced them yet (#911's own ff-only pull is a harmless no-op after the SSH reset). Removed the unsafe `GetRemoteCommitHashAsync` confirm; reused the existing `PollUntilDeployedAsync` #805 flip. #82's downstream is UNCHANGED — tests fire via `shaneapp://runTest` only once #805 confirms live, results surface via the existing toast (#24) + Build Queue "Needs Attention" (#54). **SSH-based:** git pull+build. **Still HTTP (until the SSH refactor adds a real `kill 1` + a startup-commit confirm):** restart, live-confirm. Scope confined to `PostBuildDeployPipeline.cs`; did NOT touch the SSH agent's concurrently-dirty `ReplitSshService.cs`/`BuildConsoleSettings.cs`/`Watch-Deploy.ps1`. `dotnet build --nologo` clean: 0 errors, only pre-existing CS8602/CS0067/CS8604 warnings in unrelated files. WPF internal tool → no test manifest (test-coverage scope is msp-portal/shane-mccaw-consulting only); no DB work. Local #82 (BuildConsole issue), not GitHub #82 (Zoho) → no GitHub label sync. FYI for the SSH agent: `DeployAsync` uses `npm run build` but this is a pnpm workspace monorepo — likely should be `pnpm run build`. | #82 deploy chain SSH pull hybrid | `65eb97c1` |
| 2026-08-14 | ✅ DONE — shaneapp://executeSql design-deviation fix (confirmed real by a live session hitting "no local Postgres connection string is configured"). Read the real current LocalSqlExecutor.cs and SqlRunnerView.xaml.cs's actual execution mechanism in full first, then traced the SQL Runner's real pipe precisely: SqlRunnerView.Execute_Click → `_api.ExecuteSqlAsync(toRun)` where `_api` is the BuildTrackerApiClient → `POST api/simulator/sql/execute` against the configured dev api-server (apiBaseUrl) → List<SqlStatementResult>. The bug: LocalSqlExecutor had been built to open its OWN direct Npgsql connection, resolved from a `databaseUrl` config field / `DATABASE_URL` env var that was never configured — so every executeSql call failed. Deviated from Shane's real intent: "you send the SQL through the pipe like the SQL Runner, no postgres needed." Fix: rewrote LocalSqlExecutor.cs to a thin adapter that delegates to `BuildTrackerApiClient.ExecuteSqlAsync` (the SQL Runner's exact pipe) — removed ALL Npgsql/connection-string machinery (TryResolveConnectionString, the postgres:// URI parser, per-statement Npgsql reader); its ExecuteAsync now takes the BuildTrackerApiClient and throws a clear "api-server not configured" if IsConfigured is false. Updated the shaneapp://executeSql handler in MainWindow.xaml.cs to drop the connection-string resolution and call `LocalSqlExecutor.ExecuteAsync(_buildTrackerApi, sql)` — `_buildTrackerApi` is the SAME instance `SqlRunnerView.Initialize(_buildTrackerApi)` receives (MainWindow:305), so an executeSql request now hits the identical client + endpoint a manual SQL Runner query already succeeds through. Kept `BuildTrackerConfig.DatabaseUrl` for back-compat (old config keys still deserialize) but nothing reads it; corrected its doc comment + the two MainWindow "direct local Postgres" comments + AGENT_PROTOCOLS.md §1 (and the §6 "why HTTP" aside) which all still claimed a direct-Postgres design. `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings (the concurrent-session LeftSidebar edit in the tree compiled fine and added none of its own). VERIFICATION per the task: by TRACING the call chain end-to-end (not a direct DB query — that's exactly the thing this fix removes) — executeSql now routes through the same ExecuteSqlAsync → POST api/simulator/sql/execute the SQL Runner uses, so it succeeds the moment the app is configured with apiBaseUrl+ingestToken, no connection string. NOT live-triggered here: shaneapp://executeSql runs Shane's DEPLOYED ShanesBuild, which still has the old broken direct-Postgres path, so a live trigger would only re-confirm the OLD failure, not this fix — it verifies once Shane deploys. Committed only my 4 files by pathspec (concurrent session's admin-build-tracker.ts + LeftSidebar.xaml.cs left untouched). No DB work (that's the point). No Git # (design correction, not a filed issue — no label sync). | shaneapp://executeSql via SQL Runner pipe | `677dba7e` |
| 2026-08-14 | ✅ DONE — Per-build Task Checklist column (local #42/#56 follow-on): moved each concurrent build's checklist OUT of the single shared global "Task Checklist" side panel (which merged every build's items into one flat list, told apart only by a small per-item "GH #-59"/"GH #-61" tag) and INTO that build's own Build Watch square — a slim right-hand column inside each ChatSessionPane, scoped to just that build, matching Shane's original #42 ask ("pull just those steps... and display them down a column"). ChatSessionPane gained a Grid.Column checklist panel (hidden until its build reports a step) whose rows are built in code-behind (RefreshChecklist/BuildChecklistRow) from the SAME real shared TaskChecklistViewModel/ChecklistExtractor detection — structured TaskCreate/TaskUpdate (#56) plus #28's free-form ☐ / "- [ ]" text fallback — filtered to the pane's queue id; it subscribes to the shared tracker on Loaded and unsubscribes on Unloaded (the FocusModeBar pattern, so a closed window's panes don't linger on the app-wide singleton). BuildWatchWindow points each pane's column at its build in OccupySlot and clears it in ClearSlot; the shared global ChecklistPanel + its GridSplitter + the 270px column were removed (the slot grid now fills the whole window). Detection is 100% unchanged — only WHERE items render moved; Focus Mode's band still reads the same shared model. `dotnet build -c Release --no-incremental` clean, 0 errors (only the 4 pre-existing CS0067 LeftSidebar warnings), verified in an isolated worktree off origin/main (the shared tree was mid-flight with several concurrent sessions). NOT GUI-verified here (WPF, no headless run) — Shane should confirm each running build's square shows only its own ☐/green-✔ steps down its right edge and that the old merged global panel is gone. No Git # (local BuildConsole tooling — no label sync). | Per-build Task Checklist column | `4d857571` |
| 2026-08-14 | ✅ DONE — Immersive Focus left-panel epic drill-down (local #47 refinement). Read #47's real landed immersive impl in full first (FocusImmersiveView + FocusModeService.ActiveChildIssues + FocusCharacterLayer + MainWindow.FocusImmersive.cs) — this works well per Shane; this is a scoped refinement to the left CHILDREN panel only, not a rebuild. Shane's feedback (with screenshot): the panel dumped every open issue across ALL epics under the active milestone in one flat list, with no way to narrow to a single epic. Added a second level of filtering IN THE SAME panel: clicking an Epic child (a row with real sub-issues — now shown with a › drill affordance and a "Click to see only this epic's children" tooltip) narrows the list to ONLY that epic's own real sub-issues. Since there is no client-side epic→child linkage (SubIssueCount is a count only), the real children are fetched live via GitHubApiClient.GetSubIssuesAsync on the click — a user-initiated MANUAL action (the exact precedent BuildQueuePanel.SetActiveChatEpic set), so it does NOT reintroduce the automatic GitHub polling the manual-refresh-only work removed; the fetch is guarded by a request-sequence token so a stale response can't clobber a newer click/back, and has loading / empty / error / no-PAT states. A breadcrumb bar (new DrillBreadcrumb in the XAML, collapsed until drilled) provides a "← Back to all children" button to restore the full milestone-wide view plus an "open ▸" button that opens the epic itself (its live build or linked chat) so drilling never removes epic chat/build access. Drill state is dropped automatically if the focused milestone changes and reset in Deactivate so immersive reopens on the full list. Non-epic and closed rows keep their existing click behaviour (focus live build / load linked chat in place) unchanged. Files: Controls/FocusImmersiveView.xaml + FocusImmersiveView.xaml.cs only. No new logging channel (pure UI/filter refinement, as specified), no DB work. Out of the test-manifest coverage scope (BuildConsole is Shane's own WPF tooling, not customer-facing product). Built in an isolated worktree off origin/main (shared working tree held ~4 concurrent sessions' in-flight files — executeScan/runScan/MonitorScanClient — so an in-place build wouldn't be a clean signal): `dotnet build -c Release --no-incremental` clean, 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. Landed via isolated commit-tree onto origin/main (my 2 files only, never the polluted index). NOT GUI-verified here (WPF, no headless run) — Shane should confirm on a real run that clicking an epic in the immersive left panel narrows it to just that epic's children, Back returns to the full list, and "open" still opens the epic itself. Local #47 (not a filed GitHub issue — no label sync). | Immersive Focus epic drill-down | `96b78934` |
| 2026-08-14 | ✅ DONE — Build Watch transcript copy/select: added right-click Copy context menus (matching #945's pattern) to ChatSessionPane transcript blocks (user message, assistant paragraph, tool-call detail lines), plus a "Copy Tool Call" option that copies a whole tool-call detail (name/command/diff/output) at once. Kept the #1026 visual redesign untouched — TextBlocks stayed TextBlocks; the fenced-diff DiffTemplate already used AvalonEdit's TextEditor, which was already natively selectable/copyable. `dotnet build -c Release --no-incremental` clean, 0 errors (only the 4 pre-existing CS0067 warnings). No DB work, no logging channel needed (pure UI convenience). NOT GUI-verified here (WPF, no headless run) — Shane should confirm right-click "Copy" on a narrative line, a tool-call detail line, and "Copy Tool Call" on a tool-call block all paste the expected text. No Git # (not filed as an issue). | Build Watch transcript copy/select | `4f8ced4b` |
| 2026-08-14 | ✅ DONE — shaneapp://executeScan local protocol action (local #60 companion to #59): a new sibling to shaneapp://executeSql / runTest / runScan that triggers exactly ONE individual monitor check by its real `monitor_checks` key against a specified testbed tenant — genuinely distinct from #59's runScan (which triggers the full aggregate scan). Reuses Simulator Studio's OWN real per-check execution endpoint (`POST /api/admin/monitor-checks/:key/run` + `GET .../monitor-check-runs/:runId`[`/items`]) via a new dedicated `Services/MonitorScanClient.cs` HTTP client (bearer ingest-token auth, not BuildTrackerApiClient — those routes live outside the build-tracker surface), enforces the same #965 isTestbed-only gate every tenant-touching action already requires (new `TestbedGate.ResolveTestbedTargetAsync`, accepting either the tenant GUID or numeric customer id) so it can never hit a real customer tenant, and writes a structured result envelope (ok/error/action/source/ranAtUtc + the real check's actual observed output — extractedProperties/item counts/severity/raw items — enough per-check detail for an agent to diff against an independent PowerShell ground-truth read, per local #60's dual-verification framing) to a resultRef path, guaranteed on every path including a top-level catch. Invocation: shaneapp://executeScan?scan=<monitor_checks key>&tenantId=<testbed tenant>&resultRef=<optional out>&src=<optional tag>. Server-side, the three monitor-check-run routes (`admin-monitor-check-runs.ts`) now accept `requireAdminOrIngestToken` (not just `requireAdmin`) so BuildConsole's ingest token can reach them — executeScan's own testbed gate is what actually prevents it from ever pointing at a live tenant, not the route auth. Logged on the sql-runner.protocol ActivityLog channel (real scan key, real tenant, real outcome). Documented in AGENT_PROTOCOLS.md §7 alongside the other three actions. `dotnet build --no-incremental` clean (0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings); `pnpm run typecheck` clean on the touched api-server file. No DB work. NOT end-to-end verified against a live tenant here (needs a running BuildConsole + a real testbed tenant GUID) — Shane should run one shaneapp://executeScan and confirm the envelope carries the real per-check observed output. This work was found sitting complete-but-uncommitted in the primary checkout (other sessions had pushed newer work via isolated worktrees without this checkout ever syncing back up, leaving local main ~19 files stale behind origin/main) — reset local main to origin/main and reapplied only this genuinely real content on top. | shaneapp://executeScan protocol action | `f8f33341` |
| 2026-08-14 | ✅ DONE — shaneapp://runScan local protocol action: a third sibling to shaneapp://executeSql / runTest that triggers the platform's REAL Copilot Readiness scan/assessment engine (runDiagnostics — real per-tenant Microsoft Graph reads → real findings/scores/pillars) against a testbed tenant and writes ONE settled structured result envelope, so a build session can debug/improve the scanning engine without walking the full quiz→consent→scan→verdict UI. Traced the real flow first (two Explore passes): the ONLY HTTP scan trigger the UI calls is POST /api/portal/assessment/debug-trigger-scan (portal-assessment.ts), guarded by requireRole('Assessment') and hard-gated server-side to tenants.is_testbed=true (#965); it reads no body (identity from the JWT customerId) and is fire-and-forget (202 {runId} → runDiagnostics async). CRUCIAL auth finding: BuildConsole's BUILD_TRACKER_INGEST_TOKEN is NOT a JWT and is rejected (401) on that surface, and NO ingest-token endpoint scans a chosen tenant — so runScan does what the test manifests do: logs in as the pre-provisioned testbed Assessment account (TEST_PORTAL_EMAIL/PASSWORD from Settings→Test Environment Variables) via POST /api/auth/login to mint an Assessment JWT, then triggers + reads with it. As shipped: (1) NEW Services/ScanRunnerClient.cs — the HTTP client (login incl. JWT-customerId decode, trigger, poll GET /api/portal/diagnostics/runs/:runId to a terminal status completed/partial/failed with a timeout backstop, and read GET /api/portal/assessment/status for the computed radar/pillars+copilotGate breakdown); (2) NEW MainWindow.ShaneAppRunScan.cs — the handler: parses ?tenantId= (Entra GUID) via the existing GetShaneAppQueryParam (no ShaneAppProtocol.cs edit, matching runTest's deliberate precedent), enforces the #965 gate by REUSING the existing Services/TestbedGate.VerifyTenantIsTestbedAsync (the same gate graphTests/powerShellVerify use), reads creds, logs in, and as a correctness belt confirms the login's customerId matches the requested tenant's testbed customer (never scans a different tenant than asked — surfaced as tenantCorrespondenceConfirmed), then triggers/polls/reads and writes the envelope; (3) TestbedGate.cs — added a small additive FindTestbedCustomerAsync (numeric-id resolve reusing the 30s-cached authoritative testbed list) for that correspondence check; (4) MainWindow.xaml.cs — dispatch: routes runScan ahead of the executeSql gate + adds it to the unsupported-action message. Envelope matches the executeSql/runTest convention (ok/error/action/source/ranAtUtc) plus tenantId/testbedCustomer/runId/scanStatus/tenantCorrespondenceConfirmed/checks*/findingsCount/copilotGate + the FULL real run row, the FULL findings[] (each with extractedProperties — satisfying the concurrent #59 §5b dual-verification per-finding requirement), and the full assessmentStatus breakdown. Double testbed protection: BuildConsole's own #965 gate BEFORE triggering AND the server-side isTestbed gate at debug-trigger-scan. Logged end-to-end on the shared sql-runner.protocol channel (gate/login/trigger+runId/poll/settle/counts/result path) plus TestbedGate's testing.testbed-gate channel; guarantees a result envelope on every path (top-level backstop) so an agent's poll never hangs; pure HTTP, no _testTriggerBusy latch needed. Documented as AGENT_PROTOCOLS.md §6 (invocation contract, PowerShell example, envelope, gate, logging, one-time setup — matching the executeSql/runTest style), reconciled factually with the concurrent local-#59 dual-verification §5b (which had assumed the MSP-operator route /api/msp/customers/:id/diagnostics/run — not reachable by the ingest token; the shipped path uses the UI's exact debug-trigger-scan + testbed Assessment login, and the findings[] passthrough carries the extractedProperties §5b requires). `dotnet build -c Release --no-incremental` clean in an isolated worktree off origin/main: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. No DB work (existing endpoints; no schema change). Landed via isolated worktree + private-index commit-tree onto origin/main (shared checkout was a warzone of concurrent sessions incl. the #59 docs session on AGENT_PROTOCOLS.md and a ChatSessionPane session — committed ONLY my 5 files, theirs untouched). NOT end-to-end verified against a live tenant here — that needs a running BuildConsole, a real testbed tenant GUID, and non-MFA TEST_PORTAL_* creds in Settings; Shane should run one shaneapp://runScan against a testbed tenant and confirm the envelope carries the real run/findings/pillar breakdown. Local #59. | shaneapp://runScan protocol action | `25ecc81d` |
| 2026-08-14 | ✅ DONE — Dual-verification protocol (local #59 framing + #901 generalization). Docs-only: read #901's real landed manifest (`test-manifests/admin/baseline-actions-powershell-verify.json`) and #900's `PowerShellTestExecutor.cs` delegated-auth path, the real scan engine (`msp-diagnostics.ts`: `POST …/diagnostics/run` is 202 fire-and-forget → settled per-finding read at `GET …/diagnostics/runs/:runId` = `{ run, findings }`), and the real `msp_diagnostic_runs`/`msp_diagnostic_findings` columns first. Confirmed `shaneapp://runScan` does NOT exist in the tree yet (#59 not landed), so this is the durable DESIGN CONTRACT #59 is built to, not a mechanism build (avoids colliding with the queued #59 build under the single-executor rule). Added a new AGENT_PROTOCOLS.md §5 "Dual-verification — engine observation vs PowerShell ground truth": the principle (Shane's own words — PowerShell is independent ground truth; the engine finding / write effect is checked AGAINST it, not just for "did it run") + a general 3-step model; §5a frames the LIVE write side (#900/#901) as the canonical template every future write-action test copies (trigger → app-reported → `powerShellVerify` diff, never stop at `result.success`); §5b specifies runScan's REQUIRED per-finding result envelope (must carry each finding's `extractedProperties` observed values + real `checks_total/ok/error/license_gap` rollup, not just a `compositeScore`) and a concrete scan→PowerShell comparison workflow using a REAL `exchange:distribution-list-count` finding (observed `distributionListCount:0`, the only `ok` amid 1869 `error` findings in `docs/check_key.json` — exactly the false-clean a ground-truth diff catches). Also added a runScan row to the trigger-surfaces table (marked "landing under #59"). Honest boundaries: no code/mechanism shipped (runScan handler + envelope are #59's to build to this contract); the §5b PowerShell example targets a not-yet-registered `shaneapp://runScan` and is marked "do not invoke until #59 ships". No DB work, no build/typecheck needed (Markdown), out of the test-manifest coverage scope (BuildConsole tooling). No Git # (local #59 is a BuildConsole-local tracker item, not a GitHub issue — no label sync). | Dual-verification protocol (runScan #59 / #901 generalization) | `97dc0a2d` |
| 2026-08-14 | ✅ DONE — Unassign Chat from Epic: right-click "Unassign from Epic" on an epic-assigned chat leaf in the Chats tree (`LeftSidebar.BuildChatLeaf`, shown only when `chat.EpicId.HasValue`), calling new `BuildTrackerApiClient.UnlinkChatFromEpicAsync` → new server-side `POST /admin/build-tracker/chats/unassign-epic` (added since #828/#781's landed `/chats/ingest` can't clear an epicId, only backfill a non-null one) which clears the chat's own `epicId` column, looked up by `conversation_id`. On success clears `_lastBoardSignature` + calls `PopulateChatsTree()` (same full-rebuild pattern the existing "Assign to Epic" flow uses), so the chat lands in the tree's existing "Unlinked" group for free — no new grouping code. Logged on `git-board.assign-chat` (reused from the existing assign flow) with the former epic recorded. `dotnet build -c Release --no-incremental` clean, 0 errors (only the 4 pre-existing CS0067 warnings). No DB work. NOT GUI-verified here — Shane should confirm the menu item shows only on assigned chats and clicking it moves the chat under Unlinked immediately. No Git # (not filed as an issue). | Unassign Chat from Epic | `75383b70` |
| 2026-08-14 | ✅ DONE — Focus Mode hard-filter live bug (local #40/#41): clicking "Focus this" correctly activated the state (header showed real "N hidden" + "7/34 · 20%" progress, "Exit focus" appeared) but the Chats tree and Build Queue panel showed byte-for-byte identical content before/after — nothing was actually hidden. Root cause: the filter STATE and per-panel predicates (`IsChatInFocus`/`IsIssueInFocus`/`IsMilestoneInFocus`) were real and wired into the render methods, but nothing forced those panels to genuinely RE-RENDER filtered on a focus toggle. Per panel, verified individually: (Chats) `LeftSidebar.ReapplyFocusFilter` called `PopulateChatsTree`, which re-fetches the board and early-returns at its unchanged-data signature guard BEFORE `RenderChatsTree`/`IsChatInFocus` ever runs → fixed by calling `RenderChatsTree()` directly from the cached board. (Build Queue) the Queue re-render was skipped by `RefreshAsync`'s signature guard, and the In-Flight/To-Do/Completed sub-lists had NO focus filter at all → added a shared `ApplyIssueFocusFilter` to the In-Flight/To-Do/Completed renders, cached the last-fetched In-Flight/To-Do issues, and added a cache-only `BuildQueuePanel.ReapplyFocusFilter()` (no API/`gh` call) wired into `MainWindow.OnFocusFilterChanged` in place of `RefreshAsync`. (Home) "Where you left off" filter existed but wasn't in the focus fan-out → now re-renders `RenderLeftOff` on focus change. (Git Board) already correct — `RenderIssuesTree` renders from the milestone cache with `IsMilestoneInFocus`. Sessions left unfiltered — a running claude.exe carries no issue/milestone linkage to filter on (documented + logged honestly, not faked). Per-panel shown/hidden counts logged on the `focus-mode` channel so a future regression is instantly diagnosable by comparing logged vs rendered. `dotnet build -c Release` clean (0 errors; only the 4 pre-existing CS0067 LeftSidebar warnings), verified in an isolated worktree off origin/main so it compiles against the concurrent #46/#922 LeftSidebar changes. No DB work. NOT GUI-verified here (WPF, no headless run) — Shane should confirm "Focus this" now genuinely shrinks the Chats tree to only the active milestone's epics/chats and the Build Queue's Queue/In-Flight/To-Do/Completed to only on-milestone items. Landed via isolated worktree + cherry-pick onto origin/main (local main had diverged with duplicate copies of #46/#922 that must not be re-pushed). | Focus Mode hard-filter fix (local #40/#41) | `85404b29` |
| 2026-08-14 | ✅ DONE — Pause/Resume toggle for live log panels: added a shared `Services/PausableTextBoxLog.cs` helper wrapping a live TextBox — while paused it buffers incoming appended text instead of touching the box (so the underlying stream keeps capturing everything, nothing lost) and stops auto-scroll/visual updates so Shane can select and copy text without it jumping; Resume flushes the buffer and scrolls back to live. Wired into all three continuously auto-scrolling TextBox panels that had the "forced `ScrollToEnd()` on every append regardless of user scroll position" pattern: Activity Log (`MainWindow.xaml` Output tab / `AppendOutputLog`), Build Log (`Controls/BuildLogView.xaml` `RawLogBox` / `LoadNow`, reset on switching queue items), and Terminal (`Controls/TerminalView.xaml` `OutputBox` / `AppendLine`). Each got a small "⏸ Pause" / "▶ Resume" `ChipButton`-style toggle in its header/overlay. `ChatSessionPane`'s Build Watch transcript (the fourth live-streaming view found during research) was left alone — it already has its own distance-from-bottom "stick to bottom" auto-scroll guard rather than the unconditional-`ScrollToEnd()` pattern this task targeted. `dotnet build` clean, 0 errors (only the 4 pre-existing CS0067 warnings). No DB work, no logging channel needed (pure UI convenience per the task). NOT GUI-verified here (WPF, no headless run) — Shane should confirm pausing freezes the panel and lets text be selected, and resuming catches back up with everything that streamed in while paused. No Git # (not filed as an issue). | Pause/Resume toggle for live log panels | `d4d832c1` |
| 2026-08-14 | ✅ DONE — Focus Mode immersive full-screen redesign (vision-level, follow-up to local #40/#41 Focus Mode + #46 live checklist column). Read #40/#41's real landed impl (FocusModeService singleton + FocusModeBar strip + MainWindow.FocusMode.cs context save/restore + game layer) and #46's checklist band in full first. Shane's vision: not "same layout, some rows hidden" but a genuinely dedicated, immersive FULL-SCREEN view — a zoom-in of only the Epic being worked, its child issues, the builds running on it, and the live Claude chat driving it, front and centre, with playful Microsoft-Clarity-style ambient animal-companion animations around the borders on real milestone events (build done / issue closed), a unified game layer, and gentle distraction-recovery. Reconciliation (Focus Mode is milestone-keyed; there's NO client-side epic→child-issue linkage — SubIssueCount is a count only): treat the ACTIVE FOCUS MILESTONE as "the Epic", its child issues (real GitHub epics highlighted) as its children — the only fully-wired grouping. Plan: NEW Controls/FocusImmersiveView (full-window overlay added as a direct child of MainWindow's root Grid, high ZIndex, opaque takeover) with a purpose-built layout — epic header w/ progress+game layer, child-issues column, centre live-build "Claude chat" transcript (reusing QueueWatcherService.CopyEventsSince/OwnsInteractive/SendInput — genuinely Claude running the epic) + Send box, and a progress/achievements/queued rail; NEW Controls/FocusCharacterLayer (Canvas of cute emoji companions strolling the borders + confetti/trophy celebrations on QueueWatcherService.BuildFinished and a new FocusModeService.MilestoneIssuesClosed signal, matching the Reticulating-Splines energy); FocusModeService gains ImmersiveActive state (persisted) + Enter/ExitImmersive + ImmersiveChanged + ActiveChildIssues accessor + MilestoneIssuesClosed event; NEW MainWindow.FocusImmersive.cs partial hosts/toggles the overlay, feeds builds+transcript, a Ctrl+Shift+F hotkey, and gentle distraction-recovery (exit-while-still-focused → one soft ToastEngine nudge to jump back, reusing the existing context snapshot); FocusModeBar gains an "⛶ Immersive" enter button. Logging: ActivityLog.Log("focus-mode", ...) on enter/exit immersive + each animation trigger. No DB work. Built in an isolated worktree off origin/main (which already carried the #46 checklist column). DONE — all pieces landed and compile: `dotnet build -c Release --no-incremental` clean (0 errors; only the 4 pre-existing CS0067 LeftSidebar warnings). Shipped: the purpose-built full-screen layout (epic header + progress/game layer, child-issues column, centre live-build "Claude chat" transcript reusing the interactive event stream + a Send box wired to SendInput, unified game/achievements rail), the Clarity-style companion + confetti/trophy celebration animation layer, Ctrl+Shift+F / ⛶ toggle + Esc exit, persisted immersive state (restores on launch), and the gentle exit-while-focused ToastEngine nudge. Engaging immersive collapses the frame DockPanel — BOTH the "full-screen, only my work" vision AND the robust fix for WebView2 (HwndHost) airspace that would otherwise bleed live chat panes over the WPF overlay. HONEST boundaries (WPF, no headless GUI here — Shane should eyeball on a real running app): (1) the live-build transcript/Send round-trip + build-done celebration need a real queued interactive build tied to the focused milestone to fully exercise; (2) the "issue closed" celebration is milestone-aggregate-closed-delta driven (no per-issue closed signal exists in the app) so it only fires when GitHub milestone data is manually refreshed; (3) confirm the frame-collapse fully hides any open chat WebView and that the companion animations read as tasteful, not busy. | Focus Mode immersive full-screen redesign | `ffeae9d7` |
| 2026-08-14 | ✅ DONE — Focus Mode live checklist column (reuse #28's real detection in Focus Mode). Follow-up to local #40/#41 (Focus Mode) + local #28 (Build Watch's Task Checklist side panel). Read both first: #28's detection is `ChecklistExtractor` (narrow ☐/☑/☒/✓/✔ + `- [ ]`/`- [x]` markers only, never plain bullets) feeding `TaskChecklistViewModel` (per-build tracking, dedup + similarity-matched done-flips), fed from `BuildWatchWindow.ScanForChecklist` on the poll tick into a private per-window instance; #40/#41's Focus Mode is the `FocusModeService` singleton spine + `FocusModeBar` strip (active-milestone hard filter, saturation downtime, context save/restore, game layer). Surfaced those SAME real items inside Focus Mode as a live per-build progress band — reusing #28's detection/tracking DIRECTLY, no second parser: (1) promoted `TaskChecklistViewModel` to an app-wide `Shared` singleton — Build Watch now feeds `Shared` (was `new()`) and `Clear()`s it on window close, preserving #28's fresh-per-session behaviour and bounding cross-session accumulation; (2) threaded the build's GitHub issue number onto each `ChecklistItemViewModel` (via a new `int? githubNumber` arg on `Ingest`) so Focus Mode can map a build→milestone; (3) new strict `FocusModeService.IsIssueInActiveMilestone(int?)` predicate — positive warm-map milestone match only (unlike `IsIssueInFocus`, which errs toward SHOWING null/local/cold-map so the running build is never hidden), so a local/off-milestone build's checklist stays hidden per Focus Mode's hard-filter spirit; (4) `FocusModeBar` renders one compact column per on-milestone build that actually reported items (grouped over the shared tracker's rows — never an empty/placeholder column), mirroring #28's ☐ / green-✔ / strikethrough visuals, re-rendered on the shared tracker's `PropertyChanged` (add / done-flip / remove / clear all bubble up via Summary/HasItems) + Focus `StateChanged`, with a signature guard to skip no-op rebuilds. Band hidden entirely when focus is off or nothing on-milestone has been detected. No new logging (beyond #28's existing `build-watch` logs), as specified. Files: `Controls/TaskChecklistModel.cs`, `Controls/FocusModeBar.xaml`(.cs), `Services/FocusModeService.cs`, `BuildWatchWindow.xaml.cs`. `dotnet build -c Release` clean — 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. No DB work. Landed via an isolated `--no-checkout` worktree off origin/main + pathspec commits (shared working tree was a warzone of ~5 concurrent sessions mid-rebase; my 5 code files were unchanged on origin so a clean blob-checkout equalled my diff exactly). Not GUI-verified here — Shane should confirm that, while focused on a milestone with a running on-milestone build that prints a checklist, the Focus strip shows that build's steps ticking to green, and shows nothing for builds with no detected items. | Focus Mode live checklist column | `7e000789` |
| 2026-08-14 | ✅ DONE — New Epic chat association gap (#922 follow-up, tested live on #704). Shane: right-click #704 → New Epic chat prefilled the "Epic #N" message fine but never assigned the chat to the epic / put it in the Chats panel. Read both real code paths first. CORRECTION to the premise: the Chrome extension has NO "watch for a sent Epic #N message" parser — its association is the explicit panel-click `linkTo({epicId, force:true})` → `POST /admin/build-tracker/chats/ingest` (content.js/background.js), passing the epic's numeric bt_epics id; the "Epic #N" string is only ever prefill/title text. #922 ported the extension's composer prefill (EpicChatPrefillPollScript) into BuildConsole's WebView2 but stopped there — no association write. This adds the missing half faithfully: BuildConsole already knows which epic was right-clicked, so `EpicChatRequested` now carries the epic's GitHub number; `OpenWebTab` injects a URL watcher (EpicChatAssociationWatcherScript) that reads `location.pathname` — a fresh claude.ai chat only gets a real `/chat/<uuid>` conversation once Shane actually SENDS the first message (the SPA route change), which is exactly the "once the prefilled message is sent" trigger — and posts that uuid back the instant it appears (once; self-polls across pushState; ignores any uuid already in the initial URL). `AssociateEpicChatAsync` then resolves the epic's internal bt_epics id from its GitHub number via a live board fetch and calls `BuildTrackerApiClient.LinkChatToEpicAsync` (the SAME `force:true` `/chats/ingest` the existing "Assign to Epic…" path uses), then refreshes the Chats tree so the new BoardChat lands under that epic. Logged on `git-board.epic-chat` at every branch (configured/not, epic-not-in-bt_epics, 2xx/non-2xx, exception) so a missed association is diagnosable, not silent. `dotnet build -c Release` clean (0 errors; only the 4 pre-existing CS0067 LeftSidebar warnings). No DB work (uses existing endpoints). NOT GUI-verified here (WPF, no headless run) — Shane should confirm on a live run that sending the prefilled message makes the chat appear under the epic in the Chats panel. Landed via isolated commit-tree onto origin/main: the shared checkout repeatedly clobbered my LeftSidebar/MainWindow edits mid-session (a concurrent api-server session's working-tree revert) and left a foreign `.git/rebase-apply`, so I re-applied, staged, built clean, then committed only my 2 files and pushed via commit-tree to avoid disturbing the other session. | New Epic chat epic-association | `cb37734f` |
| 2026-08-14 | ✅ DONE — Focus Mode downtime-trigger correction (local #40 follow-up). Read #40's real landed impl first: its downtime trigger was `buildRunning && IdleSeconds() >= IdleThresholdSeconds` (a build running via `_queueWatcher.RunningCount`/queue snapshot, AND Shane idle ≥60s via Win32 `GetLastInputInfo`) — an approximate condition. Shane's correction: he never lets builds sit (stacks 10-20 blocked builds), so idle was the wrong signal; genuine downtime is queue **saturation** — all 8 Build Watch slots actively occupied by a running build AND the queue still has items waiting behind them. Replaced the trigger: new `FocusBuildSaturation` struct (RunningCount/QueuedCount/SlotCount, `IsSaturated = SlotCount>0 && Running>=SlotCount && Queued>0`); `FocusModeService` downtime probe changed from `Func<bool> isBuildRunning` to `Func<FocusBuildSaturation>`; `Tick()` keys off `IsSaturated` (no idle timer). Downtime begins the instant saturation becomes true and ends the instant it breaks (a slot frees → running<slots, or the queue drains → queued==0); restore still fires on the genuine `QueueWatcher.BuildFinished` signal (unchanged). The shell probe `GetBuildSaturation` reads the shared server queue snapshot `BuildQueuePanel.CurrentQueueItems` (covers foreign / standalone-watcher builds too — same source Build Watch fills its slots from), counting `Status=="running"` vs `"queued"`, slot capacity from `BuildWatchWindow.SlotCount` (made public). Removed the now-dead Win32 idle detection (`GetLastInputInfo`/`LASTINPUTINFO`/`IdleSeconds`/`IdleThresholdSeconds`) — only Focus Mode used it. `ActivityLog.Log("focus-mode", ...)` on both edges now reports the real slot-occupancy + queue-depth counts (`all N/8 slots occupied with M queued behind them` / `a slot freed (N/8 occupied, M queued)` / `queue drained`), so the trigger is verifiable. Everything else #40 built (active-milestone declaration + hard filter, quick-task suggestions, context save/restore/refocus, game layer) untouched. Built in an isolated worktree off origin/main (local `main` is a stale diverged "lost commits" branch that never got #40; origin/main has it). origin/main is itself pre-broken by an unrelated concurrent session — `MainWindow.xaml` references `Controls.StartupLoadingView` whose files aren't committed — so build-verified with a temporary code-only stub for that type (NOT committed): `dotnet build -c Release` clean, 0 errors, only the pre-existing CS0067 LeftSidebar warnings. No DB work. Not GUI-verified here — Shane can confirm the bar's downtime quick-tasks now appear only at genuine 8/8+queued saturation. | Focus Mode downtime trigger correction | `2cbbe10b` |
| 2026-08-14 | ✅ DONE — Global Queue Pause/Resume toggle. Shane wanted a single global "Queue Paused / Running" switch, separate from any individual build's Stop: while paused he can still queue items normally, but the processor must not pick up and START any new item until he presses Play/Resume; already-running builds keep running to completion. Root of the pickup logic is `QueueWatcherService.TickAsync` — it reaps finished builds, then computes free slots and calls `GetNextQueueItemsAsync` (a server-side CLAIM) + `LaunchItem`. Added a per-instance in-memory `_paused` flag + `IsPaused`/`SetPaused(bool)` (+ `PausedStateChanged` event); the gate sits AFTER the reap loop and BEFORE the claim, so completions/slot-frees still fire while paused but no new item is ever claimed-but-not-run (items stay `queued`). "Run Now" (ForceLaunch) is left working as a deliberate per-item manual override. UI: an always-visible Pause/Play toggle in the Build Queue panel header (next to the pin/refresh icons) — glyph+label+colour flip (⏸ "Running" quiet → ▶ "Paused" peach accent) with a clear tooltip; handler in a NEW `BuildQueuePanel.QueuePause.cs` partial to stay clear of the heavily co-edited main code-behind (no `Initialize()` hook needed — pause defaults to off, so the initial "Running" visual is always correct). State changes logged: `watcher` channel (PAUSED/RESUMED) + `build-queue-panel` channel (header toggle). Built in an isolated worktree off origin/main (the shared working tree was polluted by ~5 concurrent sessions, incl. one reverting the #11d137a3 GitHub refresh button): `dotnet build -c Release` clean, 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. NOTE: origin/main is itself currently broken by an unrelated concurrent session (MainWindow.xaml references `Controls.StartupLoadingView`, whose files aren't committed yet) — verified my change compiles by temporarily copying in those untracked files for the build only; not committed. No DB work. Not GUI-verified here — Shane should confirm the header toggle flips state and that a paused queue stops starting new items while letting running ones finish. | Global Queue Pause/Resume toggle | `4a2e1172` |
| 2026-08-14 | ✅ DONE — Eliminate ALL automatic/background GitHub API traffic → manual-refresh-only. Shane: "this app is killing my git connections... turn git into a manual refresh. I hit it when I know things have changed instead of this automatic stuff that's causing me to get rate limited hard." Full audit (two parallel sweep agents + hand-verification) found **7 automatic GitHub-API sources**: (1) LeftSidebar Git Board poll — GraphQL issues + REST milestones, PAT; (2) its per-open-issue `blocked_by` REST burst (`EnrichBlockedStatusAsync`, up to 40-80 REST calls, the documented #876 burster); (3-5) BuildQueuePanel In-Flight / To-Do / Completed tiles — `gh` CLI, 60s each; (6) BuildWatchWindow issue-closure check (`CheckIssueClosuresAsync`) — `gh`, ~30s; (7) MainWindow Home "Done, waiting for you" rollup — `gh`, ~60s. Disabling each automatic firing; keeping/adding a prominent manual Refresh per data source (reusing the #863 Git-Board refresh-icon pattern) + logging every manual refresh (source + real outcome) on a `github.manual-refresh` ActivityLog channel so ALL future GitHub traffic from this app is attributable/verifiable. #942's chat-button In-Progress→Done/Failed status poll investigated and DELIBERATELY LEFT automatic — it hits the local dev server (GET /extension/queue), NOT GitHub, so it burns zero rate limit and disabling it would break that feature for no benefit. Contended files (LeftSidebar.xaml.cs, BuildWatchWindow.xaml.cs, MainWindow.xaml.cs) edited ONLY in concurrent-edit-free regions and committed as isolated non-overlapping hunks; #7's in-place Home refresh button deferred (HomeView is owned by the concurrent Home-ADHD-redesign session — its gh fetch is gated to force-only, re-syncing on Home-tab reopen for now). Unrelated concurrent-session dirty files left untouched. Build: `dotnet build -c Release` clean in an isolated worktree (0 errors; only the 4 pre-existing CS0067 warnings); all 7 automatic GitHub sources handled. Not GUI-verified here — Shane should confirm the two new manual Refresh icons (Build Queue panel header + existing Git Board) and that GitHub traffic now moves ONLY on a click. | Manual-only GitHub refresh (eliminate background polling) | `11d137a3` |
| 2026-08-14 | ✅ DONE — Manual refresh icon for Claude usage status bar. Shane's screenshot showed "Claude: --" (muted failure state) and the automatic ~10min poll only succeeds ~25% of the time; wanted a manual retry instead of waiting for the next cycle. Added `ClaudeUsageMeterService.ManualRefreshAsync()` — reuses the exact existing `TickAsync` poll logic (navigate hidden WebView2 → wait for SPA hydration → read `[role="meter"]` → publish), just invoked on demand instead of waiting on the `DispatcherTimer`. A small ↻ `IconButton` next to the "Claude: N% used" status bar text (`BtnUsageRefresh` in MainWindow.xaml, same glyph/style as the Git Board refresh icon) calls it; the service's own Polling-state emit (dot turns amber, "Checking claude.ai…" tooltip) is the in-progress feedback so a click isn't silent, and the button disables for the duration. Manual triggers logged distinctly on the existing `usage-meter` ActivityLog channel ("Manual refresh requested (status bar refresh icon)."); a manual click while a poll is already in flight logs and no-ops rather than stacking. Isolated the change from unrelated concurrent-session dirty content already present in MainWindow.xaml.cs (staged GitHub-manual-refresh revert + unstaged in-progress StartupOverlay work) via a private-index commit-tree build containing only my hunk — left both untouched in the working tree. Build clean (`dotnet build`, 0 errors, only the pre-existing CS0067 warnings). No DB work. Not GUI-verified here — Shane should confirm clicking ↻ shows the amber dot briefly then a real percentage (or "--" again if the poll genuinely fails). | Manual refresh icon for Claude usage status bar | `d072bf77` |
| 2026-08-14 | ✅ DONE — Home screen ADHD redesign (collapse What's New + stale Running-Now detection). Shane: "it's a very long scroll list. NOT ADHD friendly." The Home tab currently dumps the full What's New bullet list (14+ raw commit subjects) always-visible above Where-You-Left-Off/Running-Now/Done-Waiting, forcing a long vertical scroll before reaching the actionable sections. Redesign using #874's EXACT proven Build-Queue-panel visual language (not a new design): (1) What's New collapses to one quiet count-badge QuietTile ("N changes since you last looked", collapsed by default, click-to-expand the real full list into an attached content Border — same ToggleButton/CornerRadius 0,0,6,6 shape as the panel's In-Flight/Completed tiles); (2) Where-You-Left-Off / Running-Now / Done-Waiting stay visible and prominent with count badges + real hairline visual separation and generous spacing, so the common case is scannable in seconds without scrolling. Also fixes a real bug in the same screenshot: Running-Now shows stale/orphaned entries (builds queued during last night's network failure, never completed, still "running" hours later). Add a real staleness check — a running item whose UpdatedAt is ≥60 min old (or absent) is flagged with a ⚠ amber treatment + "no update in Xh — likely stuck" subtitle instead of rendering identically to a live build, the header count noting "· N stuck", and a per-row ✕ clear that cancels the orphaned queue item (DELETE queue/{id} via the existing CancelQueueItemAsync, same action as the panel's right-click Cancel) then refreshes. Staleness detection logged on the existing home-screen ActivityLog channel. Files: Controls/HomeView.xaml + HomeView.xaml.cs (self-contained); one clean inline event subscription in MainWindow.OpenHomeTab. Unrelated concurrent-session dirty files (BuildWatchWindow.*, MainWindow.PendingUpdateQueue/VersionUpdate, LeftSidebar, StartupLoadingView.*, ChecklistExtractor, etc.) left untouched. Picked up from an uncommitted working tree and committed alongside the Startup loading overlay (same commit, both wired through MainWindow together). `dotnet build --no-incremental` clean, 0 errors, verified after a full rebase onto origin/main (including the separately-shipped Focus Mode + Global Queue Pause/Resume work). Not live-verified in a running GUI here — Shane should confirm the What's New tile collapses/expands and a stale Running-Now row shows the amber "likely stuck" treatment with a working clear button. | Home screen ADHD redesign | `94c3d211` |
| 2026-08-14 | ✅ DONE — Build Watch live-render freeze fix. Shane: "they are not staying live and hanging the main app when they do catch up." Investigating the real cause of Build Watch slots stalling then freezing the whole app on catch-up (6 queued tasks all showed "running" with the identical timestamp = the single UI Dispatcher thread seized). Confirmed candidate: `BuildWatchWindow.DrainInteractiveOutput` renders the ENTIRE per-poll backlog (`CopyEventsSince`, up to thousands of events, across every occupied slot) in one synchronous UI-thread `foreach` into a NON-virtualized `ItemsControl` (ChatSessionPane transcript) with a per-add `ScrollToEnd()` — O(n²) work that blocks every window until it finishes. Fix: bound per-tick render work, drain in small chunks yielding via `Dispatcher.BeginInvoke(Background)` between them, coalesce a runaway backlog (drop the surplus that would only be trimmed off the ≤MaxCardsPerSlot transcript anyway, with one honest "skipped N" marker), and log real timing around the flush. QueueWatcherService (background output/buffer side, already thread-safe) untouched; unrelated concurrent-session dirty files (MainWindow.xaml + StartupLoadingView.*, the row below) left alone. Picked up from an uncommitted working tree and committed alongside the Task Checklist side panel (same file, same commit). `dotnet build --no-incremental` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm Build Watch stays responsive on a large catch-up backlog instead of freezing. | Build Watch live-render freeze fix | `21bceb6f` |
| 2026-08-14 | ✅ DONE — Animated startup loading screen (resumes the abandoned 2026-08-13 attempt below): build the missing backing `StartupConnectivityService` + wire it into MainWindow so the already-authored `StartupLoadingView` (original running-character animation + honest per-connection progress list) actually runs. Real per-connection probes against the SAME endpoints the app connects to at launch — Build Tracker board (`GetBoardAsync`), in-flight issues (`GetInProgressAsync`), Build queue (`GetQueueAsync`), Deploy status (`GetDeployStatusAsync`) — plus the real Claude usage meter observed via its existing `StatusChanged`. Each connection's timing/success/failure logged on the `startup` ActivityLog channel; per-connection timeout + a global cap so one slow/failed connection never blocks launch; overlay fades to Home once all settle. NOTE: the prior attempt left MainWindow.xaml in a stale state that reverts HEAD's universal-search-bar work — I discard that revert and re-apply only the overlay onto current HEAD; unrelated concurrent-session dirty files (BuildWatchWindow.xaml.cs, ChecklistExtractor.cs, TaskChecklistModel.cs) left untouched. Picked up from an uncommitted working tree and committed alongside the Home screen ADHD redesign (same commit, both wired through MainWindow together). `dotnet build --no-incremental` clean, 0 errors, verified after a full rebase onto origin/main. Not live-verified in a running GUI here — Shane should confirm the startup overlay shows real per-connection progress and fades to Home once everything settles. | Animated startup loading screen | `94c3d211` |
| 2026-08-13 | ✅ DONE — Build Watch tool-call bubbles show real content. Root cause CONFIRMED (live stream-json capture, not assumed): the `--output-format stream-json` events genuinely carry the full payload — a `tool_use` block has `id`+`name`+`input` (the real command/args) and each tool result arrives as a separate `type:"user"` event whose `message.content[]` holds `tool_result` blocks (`tool_use_id`, `content`, `is_error`). `SummarizeStreamJsonLine` extracted ONLY the tool `name` (`[tool: X]`) and dropped `input` entirely, and the whole `user`/tool_result event fell through its `default` case as "noise" — so both the log line and the buffer only ever carried the bare name, which the display then regex-reconstructed. Pure parsing gap, no source suppression. Fix: a new structured interactive event stream (`InteractiveEvent`: AssistantText / ToolCall(name,id,command-preview,input) / ToolResult(id,output,is_error) / TurnResult) parsed from each line and pulled by the interactive Build Watch render, replacing the old flattened string buffer; the human-readable per-item log FILE (#802 + foreign file-tail) is still fed the old summary text unchanged. The collapsed #1012 chip now shows, on expand, the real command/params + real tool output, and a real inline colored diff for Edit/Write/MultiEdit file edits (self-contained; does not depend on the concurrent code-diff DiffView row below). One raw `tool_use` + one raw `tool_result` sample logged once per run on a `stream-json` ActivityLog channel for future diagnosis. Verified: the real event shapes were captured LIVE (`claude.exe --print --output-format stream-json --verbose` forcing a Bash + Read tool call — the output-event shapes are identical to the interactive stdin path, which only differs in input delivery) and every captured line traced through the parser; `dotnet build -c Release` clean (0 errors; only the 4 pre-existing CS0067 LeftSidebar warnings), including the combined tree after rebasing onto the concurrent universal-search commits that landed mid-session. HONEST boundary: the end-to-end GUI render of a live interactive queued build wasn't exercised here (same environment constraint as the #29c2cdca interactive-build row) — Shane should confirm on a real queued build that a tool bubble expands to show the real command/output and, for a file edit, the diff. | Build Watch tool-call detail + file-edit diff from stream-json | `4c0adab3` |
| 2026-08-13 | ✅ DONE — Build Watch event stream now renders genuine code diffs. A run of consecutive unified-diff lines (`@@ … @@` hunks, `+`/`-`/context lines, or a fenced ```` ```diff ```` block) is detected in the tail/interactive line stream and folded into one DiffTurn — analogous to how consecutive `[tool:]` markers fold into a ToolGroupTurn — then rendered by a new AvalonEdit-based DiffView: per-line green (added) / red (removed) backgrounds, a diff-aware line-number gutter (real old/new file numbers from the hunk header), and underlying-language syntax highlighting (reusing #939's SqlSyntax.xshd + JsonSyntax.xshd + AvalonEdit's built-in definitions). Visually distinct from the grey narrative/tool-chip bubbles but consistent with the pane's dark palette; plain prose + collapsed tool chips render exactly as before. Pure display feature (no logging). QueueWatcherService untouched. Landed as an additive union merge (620 insertions, 0 deletions — the other session's work fully preserved, its markers unchanged) on top of the concurrent, complementary `4c0adab3` "tool-call detail + file-edit diff from stream-json" feature already on main: theirs renders Edit/Write/MultiEdit tool-input diffs inside the expanded tool chip, this renders diffs the agent writes in its prose text stream as a standalone DiffView (different triggers, different surfaces — no collision, verified by their own "does not depend on the code-diff DiffView row" note). Combined tree builds clean (0 errors). HONEST boundary: the live GUI render of a real diff wasn't exercised (WPF, no headless GUI) — Shane should eyeball one on a real build. | Build Watch code-diff rendering | `c37b3639` |
| 2026-08-13 | ⏳ IN FLIGHT — Animated startup loading screen: replace the blank window shown while real startup connections (GitHub board, Build Queue, Deploy status, Claude usage meter) complete with an original animated running character + honest per-connection progress list, per-connection timing logged, transitioning smoothly to Home once all connections resolve (or per-connection timeout, never blocking on one slow/failed connection). | Animated startup loading screen | `TBD` |
| 2026-08-13 | ✅ DONE — powerShellVerify delegated device-code token now persists/reuses across BuildConsole-spawned pwsh runs. Root cause CONFIRMED (on disk + against the Graph SDK source at 2.29.1…main, not assumed): cross-process SILENT reuse of a `-UseDeviceCode` token requires the Azure.Identity `AuthenticationRecord` at `%USERPROFILE%\.mg\mg.authrecord.json`. On Shane's box that file was ABSENT while the DPAPI MSAL token cache (`%LOCALAPPDATA%\.IdentityService\mg.msal.cache.cae/.nocae`) persisted fine and was fresh — so the SDK's device-code path, which gates reuse on `if (!File.Exists(AuthRecordPath))`, fell through to a fresh device-code prompt every run despite a perfectly valid cached token. Ruled OUT by live testing on the machine (not guesswork): `-NoProfile` (irrelevant to the cache path); env/user-context mismatch (a spawned `-NoProfile -NonInteractive -EncodedCommand` child's USERPROFILE/LOCALAPPDATA/MyDocuments/PSModulePath are byte-identical to an interactive shell — the child inherits BuildConsole's env verbatim under UseShellExecute=false); UseShellExecute/env-inheritance; and ContextScope defaulting to Process (the delegated device-code path defaults to CurrentUser on Windows per the SDK source — only WSL forces Process). Also found: the SDK hardcodes a 120s device-code auth window (`MaxAuthenticationTimeOutInSeconds`, NOT extendable via `-ClientTimeout`, which is only the HTTP-client timeout) — if a sign-in exceeds 2 min the record is never written. FIX (`830dcd63`, `Services/PowerShellTestExecutor.cs`): (1) import the NEWEST installed `Microsoft.Graph.Authentication` — a bare `Import-Module` was silently loading a stale **2.29.1** (`D:\Shane\Documents\PowerShell\Modules`, first on PSModulePath) that shadowed **2.38.0** (Program Files); newer builds have more robust keyed/legacy auth-record resolution and share the same on-disk cache, so nothing is orphaned; (2) pass `-ContextScope CurrentUser` explicitly so the on-disk (not in-process) cache is used unambiguously; (3) log the resolved cache-relevant paths/state at process launch (C#) and pre/post-connect (pwsh `__PSVERIFY_DIAG__`): loaded module version+base, USERPROFILE/LOCALAPPDATA, the `.mg` AuthenticationRecord path + whether it exists BEFORE and AFTER connect, and the MSAL cache files — so any recurrence is diagnosable from the `testing.powershell-verify` ActivityLog channel alone. `dotnet build -c Release` clean in an isolated worktree (0 errors; only the pre-existing unrelated CS0067 LeftSidebar warnings); the injected pwsh parses clean and the read-only diag block was run live — it now loads 2.38.0 and reports `authRecordExists=False` + `msalCache=[mg.msal.cache.cae,mg.msal.cache.nocae]`, i.e. it makes the exact root cause visible. HONEST boundary: the end-to-end proof — that one completed interactive device-code sign-in now writes `mg.authrecord.json` (`authRecordExistsNow=True` in the post-connect log) and that the NEXT run then reconnects silently with no prompt — needs Shane to complete ONE real interactive sign-in (device-code + browser + MFA, promptly given the SDK's 120s window); it cannot be exercised headlessly here. Shane To-Do: run one Play-Test powerShellVerify, complete the device-code sign-in, watch the `testing.powershell-verify` channel for `authRecordExistsNow=True`, then run a second time and confirm it connects silently. | powerShellVerify device-code token persistence | `830dcd63` |
| 2026-08-13 | ✅ DONE — Usage meter: projected "time until 100%" from real observed growth rate, shown alongside the existing percentage (`Claude: 72% used · ~1d 4h to 100%`; independent of the ≥85% reset-countdown threshold). New `UsageHistoryStore` appends each successful poll's `(timestamp, percentage, reset-target)` as one line to `%AppData%\BuildConsole\usage-history.jsonl` — deliberately the SAME append-only-JSONL shape/next-to-settings.json location as `TestHistoryStore`, not a new mechanism (crash-safe, malformed trailing line skipped on read, light self-compaction past 8000 lines). New pure/testable `UsageProjection` scopes the growth rate to the CURRENT reset window ONLY: `CurrentWindow` cuts at the most recent reset boundary, detected two ways — the parsed reset-target jumping forward (this week's "Resets Sun 9pm" rolled to next week) OR the percentage dropping >5pts (a window can only climb) — then fits %/hr via ordinary least-squares across ALL current-window points (not an unstable two-point extrapolation). Projection is WITHHELD (logged reason) until the window holds ≥3 points spanning ≥20min with a positive rate, so a wild estimate right after a reset is never shown. The estimate is anchored as an absolute target datetime so the existing display timer ticks it down live between polls exactly like the reset countdown (`OnDisplayTick` now re-emits when a projection is active too); tooltip gains a "Projected 100%: … (~N%/hr)" line. Each stored point AND each computed projection (or withheld-reason) is logged on the existing "usage-meter" ActivityLog channel. MainWindow needed no change — the service still computes DisplayText/ToolTip in full. Verified: full current-working-tree state + these files `dotnet build -c Release` clean in an isolated worktree (0 errors; only the 4 pre-existing CS0067 warnings) — note HEAD itself was momentarily non-compiling from a concurrent session's committed call-site whose definition was still uncommitted, unrelated to this work and since resolved by `842680a5`. Not live-verified in a running GUI here — Shane should watch the "usage-meter" channel and confirm the "~… to 100%" text appears once a few real polls accumulate in a window. | Usage meter time-to-100% projection | `69386734` |
| 2026-08-14 | ✅ DONE — Immersive Focus view: clicking a left-panel child that has an associated Claude chat used to EXIT immersive mode and drop back to the normal tab layout. Read #47's real landed impl in full first: the child rows are built in `FocusImmersiveView.BuildChildRow`, each wired to `OnChildClicked(GitBoardIssue)`; when no build was running for the child it called `_openIssue?.Invoke(k.Number)`, wired in `MainWindow.FocusImmersive.cs` to `n => { FocusModeService.Instance.ExitImmersive(); OpenChatForIssue(n); }` — so the associated chat opened in the MAIN window tab bar (`OpenChatForIssue`→`OpenChatTab`, a fresh WebView2 tab) and immersive mode collapsed as a side effect, exactly the reported symptom. Fix (stays immersive, loads the chat into the immersive view's OWN centre panel): (1) extracted the real chat-WebView2 setup out of `OpenChatTab` into a shared `MainWindow.BuildChatWebView(BoardChat)` — nav handlers + `InjectBuilderButtonsAsync` + the Git #852 navigate-once-on-first-Loaded guard, unchanged — so both the normal tabs and the immersive path build the SAME real chat session; (2) new `MainWindow.BuildImmersiveChatView(int githubNumber)` resolves the linked chat via `LeftSidebar.FindChatForIssue` and returns a ready WebView2 (or null + a toast when no chat is linked yet) WITHOUT touching the tab bar or immersive state; (3) `FocusImmersiveView.Init`'s `openIssue` callback replaced with `buildChatView: BuildImmersiveChatView`; `OnChildClicked`'s no-running-build branch now calls `LoadChatForChild`, which swaps the returned WebView2 into a NEW `ChatHost` Border in the centre panel (disposing any prior one) and enters a `_chatMode` that parks the transcript pump (`_selectedBuildId=null`) and guards `RefreshBuilds` so the 3s auto-follow can't yank the centre back to a transcript/empty state; selecting a running build chip (`SelectBuild`) leaves chat mode and shows the transcript, and `Deactivate` disposes the hosted chat WebView2 on leaving immersive. Immersive mode stays active and visible throughout — no exit, no return to the normal layout. Pure navigation/host fix, no new logging channel (reuses the existing `focus-mode` ActivityLog lines). `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. No DB work. NOT GUI-verified here (WPF, no headless run) — Shane should confirm on a real run that clicking a child with a linked chat loads that chat in the immersive centre panel and stays immersive, and that a child with no linked chat toasts "No chat linked" without exiting. Local #47 (not a filed GitHub issue). | Immersive Focus in-place chat load | `508ccb16` |
| 2026-08-13 | ✅ DONE — Build Queue panel: added a distinct "🔄 Queued for Restart" tree grouping (MauveBrush, non-selectable header + one row per item) at the top of QueueTree, showing items MainWindow.PendingUpdateQueue intercepted-and-persisted to the local `%AppData%\BuildConsole\pending-update-queue.json` spillover file while a version update sat waiting — so they now read as visually distinct from the real active/pending/running queue rows instead of being indistinguishable from them. New `MainWindow.GetPersistedQueueDisplayItems()` (public static) exposes a read-only `Title`/`GithubNumber`-only DTO off the existing private spillover-file loader for the panel to read directly (no live-DB access, purely local file). Group renders regardless of which filter chip is active (same as the existing offline banner), read fresh from disk on every `RenderQueue` call since the file only changes on a rare Queue-click-during-pending-update or the post-restart replay. Pure display grouping — no new logging wired (none needed). Verified: `dotnet build BuildConsole.csproj` clean (0 errors, only the 4 pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should trigger the existing pending-update-queue interception (Queue click while an update is pending) and confirm the new group renders. | Build Queue panel Queued-for-Restart grouping | `4617da1e` |
| 2026-08-13 | ✅ DONE — Interactive queue-managed builds. QueueWatcherService now launches queue builds (when the new `BuildConsoleSettings.InteractiveBuilds` is on, the default) with BuildConsole owning redirected stdin/stdout: `claude.exe --print --input-format stream-json --output-format stream-json --verbose` (all confirmed-real flags via `claude --help`). The prompt + every Build Watch chat message are delivered as stream-json user messages over stdin, so `@paths` / any chars pass through byte-for-byte (a JSON value, never a shell/commander arg — the whole #767/#820 mangling class can't apply). Live output is kept in a BuildConsole-owned in-memory buffer the Build Watch window pulls directly (replacing the file-tail for these) while STILL teeing the per-item log file so BuildLogView (#802) is unaffected. Each active slot for an owned interactive build gets a real chat Send box → stdin, a Stop (soft stream-json control_request interrupt escalating to `Kill(entireProcessTree)` if unresponsive / on a repeat press — same hard-kill as #1001's Close), a clear three-state indicator (working = blue spinner / paused = muted / waiting-for-input = peach "needs attention" + badge pulse), and lightweight @path autocomplete. Waiting-for-input is detected from a turn's stream-json `result` while the process is still alive. Git #800 auto-completion preserved: an idle build closes its own stdin after `InteractiveIdleFinalizeSeconds` (default 15) so it still exits with a real code; each sent message resets that window. Slot cap stays 8. Send-to-Builder session tracking (#1001, ClaudeAgentsService) untouched; legacy non-interactive launch kept as a settings safety valve. Logs on the "interactive-build" ActivityLog channel: launch, input sent, stop requested, detected waiting-for-input. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). NOT live-verified in a running GUI here — the interactive stdin round-trip / soft-interrupt / waiting-detect need a real queued build + configured Build Tracker API + streaming claude.exe to exercise; the `--input-format stream-json` contract and the CLI's post-`result` linger behaviour are implemented to the documented SDK shape but not exercised in this environment. Shane should confirm on a real queued build. | Interactive Build Watch (stdin ownership + chat input + Stop + waiting detect) | `29c2cdca` |
| 2026-08-13 | ✅ DONE — Build Watch slot content redesign (chat-bubble event cards, color coding, animated thinking spinner, "Reticulating splines" easter egg) — #980/#1000 display layer only | Build Watch slot event-card display redesign | `8ff33eca` |
| 2026-08-13 | ✅ DONE | Git #1003 (bookend restoration) — real commit `3a4ff8a7cd` confirmed: UiTestExecutor.StepCompleted event and its BuildStepCompletedResult are real, and TestRunnerWindow.xaml.cs genuinely subscribes/unsubscribes alongside the other four executors. Bookend was left at IN FLIGHT/TBD. | `3a4ff8a7cd` |
| 2026-08-13 | ✅ DONE — Build completion sound (bundled from real Antigravity IDE install, taskCompleted.mp3, confirmed by ear against antigravityCascadeDone.mp3), mute toggle in top menu bar + configurable sound path in Settings. Wired to QueueWatcherService's new BuildFinished event. `dotnet build --configuration Release` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm the sound plays on a real queue completion and the mute toggle/Settings path picker work. | `976abf8e` |
| 2026-08-13 | ✅ DONE | Git #982 (bookend restoration) — real commit `17e9ea501a` ("Fix collapsed pinned WebView2 covering the editor panes") confirmed - internally labeled "Git #972" in its own commit message (referencing the original pinned-tabs feature issue rather than this specific bug-tracking issue), same real fix. Shane confirmed live: works. | `17e9ea501a` |
| 2026-08-13 | ✅ DONE | Git #1006 — custom title bar (Git #894 style) applied to BuildWatchWindow, TestRunnerWindow, ScreenshotGalleryWindow, ScreenshotReviewWindow, TestHistoryWindow (WindowStyle=None + WindowChrome + dark caption buttons + DWM dark title bar + maximize-respects-taskbar clamp). Shared plumbing extracted to `Services/WindowChromeHelper.cs`; `CaptionButton`/`CaptionButtonClose` styles moved to `Themes/DarkTheme.xaml` as app-level resources; MainWindow refactored to use both (net removed its local duplicate P/Invoke block). LinkedInComposerWindow/StickyNotesWindow deliberately left alone — already WindowStyle=None + AllowsTransparency + Topmost floaties with their own rounded-corner drag-strip + close button, no maximize concept fits. `dotnet build` clean, 0 errors (only pre-existing unrelated CS0067 warnings), rebuilt after every file. Not live-verified in a running GUI here — Shane should confirm each window opens with the dark caption bar and min/max/close all work. | `d66c8258` |
| 2026-08-13 | ✅ DONE — ClaudeUsageMeterService: (1) `UsageUrl` → `https://claude.ai/new#settings/usage` (Shane confirmed this real URL by navigating himself). (2) Replaced the single fixed 1500ms post-nav delay with a real hydration wait (`WaitForHydrationAsync`, polls the DOM every 400ms up to 12s until `[role="meter"]` renders or an auth wall is detected) — the SPA's NavigationCompleted fires long before React paints the meter, which was the root cause of spurious "meter not found". (3) Rich phase-distinguishing ActivityLog diagnostics on the "usage-meter" channel — every failure now logs WHICH phase failed: `FAIL[navigation]` (never loaded), `FAIL[auth-wall]` (login/password page, not usage — logs url/readyState/passwordField/loginText), `FAIL[meter-not-found]` (loaded, no auth wall, meter never appeared — logs url/readyState/bodyLen/resetLabelSeen), `FAIL[meter-malformed]` (meter element present but aria-valuenow/valuetext missing/unparseable — dumps every raw meter's attrs), plus `OK[navigation]`/`OK[read]`. `dotnet build` clean (0 errors, 4 pre-existing warnings). Not live-verified in a running GUI here — Shane can watch the "usage-meter" channel in ActivityLog to see the exact real failure reason on the next poll. | Claude usage meter diagnostics | `33a7becb` |
| 2026-08-13 | ✅ DONE — Double-clicking a leaf in ManifestFilesTree loads it through the exact same path a single click already uses (`LoadManifestLeaf`, factored out of `ManifestFilesTree_SelectedItemChanged`), then immediately fires the same real run path `BtnPlayTest_Click` uses via `PlayTestRequested`. Walks up from the double-click's real hit-test target (`FindAncestorTreeViewItem`) rather than trusting `SelectedItem`, so an area-group header (no `Tag`) just expands/collapses on double-click and never triggers a run. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm double-clicking a leaf both opens the steps flyout and kicks off the real run. | Manifest tree double-click to run | `682d8c70` |
| 2026-08-13 | ✅ DONE — Version status bar + auto-Update button. Scheme v{Major}.{Minor}.{build}: Major.Minor hardcoded (v1.0 in `Services/VersionInfo.cs`), {build} = `git rev-list --count HEAD -- desktop/BuildConsole`. The running build is EMBEDDED at compile time — a new `GenerateBuildInfo` MSBuild target in `BuildConsole.csproj` runs that git command from the repo root before CoreCompile, writes `GeneratedBuildInfo.g.cs` into obj/ and adds it to @(Compile), so `VersionInfo.RunningBuild` knows exactly which build this instance is; `VersionInfo.GetCurrentBuild()` re-runs the same command live off the UI thread. Status bar's Deploy item now shows "Current: v1.0.N" alongside the deploy serial, revealing the running-vs-current diff + an `⬆ Update` button (PrimaryButton) ONLY when behind. Click (`MainWindow.VersionUpdate.cs` → `BtnUpdate_Click`) freshens `BuildQueuePanel.RefreshAsync()` then checks the real queue via the new `BuildQueuePanel.HasActiveQueueItems` (status queued/running): clear ⇒ `Process.Start(deploy-shanesbuild.cmd)` immediately; active ⇒ sets pending flag, shows a loud full-width peach banner, polls every 5s and auto-fires the script the instant the queue drains (no second click). Script's own kill+relaunch replaces the running process, so no post-script handling. Logs to the "version-update" ActivityLog channel on check/request/queue-block engaged+cleared/invocation. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings); generated build number verified embedded (159). NOT run/live-verified in a GUI here (did not execute the deploy script — Shane controls ShanesBuild deploys). | BuildConsole version/update indicator | `e0ed54ac` |
| 2026-08-13 | ✅ DONE | Git #1005 (bookend restoration) — real commit `8ff33ecaf3` ("Build Watch slot event-card display redesign") confirmed: `AddEventBubble` real, tool-call chips (mauve, gear glyph) confirmed in code, chat-bubble card presentation matches Shane's real screenshot. | `8ff33ecaf3` |
| 2026-08-13 | ✅ DONE | Git #1013 — fix CS0104 `Path` ambiguity (System.Windows.Shapes.Path vs System.IO.Path) build break in `BuildWatchWindow.xaml.cs`'s `MatchPaths()`, left by the concurrent Build Watch slot content redesign session, unrelated to my Git #1006 title bar work in the same file. Fully-qualified the two `System.IO.Path` calls rather than removing the `System.Windows.Shapes` using (`Ellipse` from that namespace is used elsewhere in the file). `dotnet build` clean, 0 errors. | `95a2235b` |
| 2026-08-13 | ✅ DONE | TestRunnerWindow Retry button — re-run last manifest in-window without re-selection. | `c6e7f3d1` |
| 2026-08-13 | ✅ DONE | Git #1014 (bookend restoration) — real commit `55428a6c59` confirmed: GetContentAsync() now kicked off immediately inside WebResourceResponseReceived, real elapsed-time diagnostics added, real WebView2Feedback issue references (#3283/#4298) cited, honestly labeled as a diagnostic change rather than a guaranteed documented-constraint fix. Shane confirmed rebuilt. | `55428a6c59` |
| 2026-08-13 | ✅ DONE | Git #1009 (bookend restoration) — real commit `29c2cdcabe` confirmed, thorough: owned stdin/stdout via claude.exe --print --input-format stream-json, chat Send box, Stop (soft interrupt -> hard Kill), three-state indicator, @path passthrough byte-for-byte, slot cap correctly kept at 8 per Shane's correction, #1001 Send-to-Builder explicitly confirmed untouched. | `29c2cdcabe` |
| 2026-08-13 | ✅ DONE | Git #992 (bookend restoration) — real commit `806a0d55e8` ("BuildConsole Home screen: native closable Home tab, session persistence, real-data roll-up") confirmed. Shane confirmed live: all three sections (Where you left off / Running now / Done waiting for you) populated with genuinely correct, real data. | `806a0d55e8` |
| 2026-08-13 | ✅ DONE — Build Watch event-stream visual pass. Genuine visual refinement of the #1004/#1005 chat-card stream, grounded in real Claude.ai chat reference values (assistant line-height ~1.65, rounded-2xl bubbles, flat secondary tool blocks). Prose/error/done cards: line-height 19→23 (~1.6×) for a calmer reading rhythm, corner radius (4,10,10,10)→(5,14,14,14), padding 10,7→13,9, inter-card gap 6→9, plus a very subtle drop shadow (BlurRadius 7 / Depth 1 / Opacity 0.25 — lighter than the app's own 0.45 speech-bubble shadow) to lift each card off the Base background. Collapsed "Ran N tools" chips now read as clearly secondary: transparent fill + thin 1px Surface1 outline pill (flat, no shadow, so they recede against the shadowed filled prose cards — matching how Claude.ai renders collapsed tool blocks) instead of the same Surface0 fill as prose; summary de-bolded (SemiBold→Normal) and chevron dimmed to Overlay. Whole-stream edge margin 8→10. All values from the existing Catppuccin palette; `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Display layer only — not live-verified in a running GUI here; Shane should eyeball a real streaming build. | Build Watch event-stream visual refinement | `6066659e` |
| 2026-08-13 | ✅ DONE | ScreenshotReviewWindow: right-click "Copy Image" context menu (Git #1017) on both BaselineImage and CurrentImage, copying the real rendered BitmapSource to the clipboard via `Clipboard.SetImage()` — same real right-click pattern established in Git #945. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). | `b6ec72fe` |
| 2026-08-13 | ✅ DONE — expect-step DOM retry/polling. UiTestExecutor's `expect` action checked the DOM exactly once (#1016), losing async render/hydration timing races when the prior step's DOM hadn't settled the instant that step returned. `EvaluateExpectAsync` is now a polling wrapper over the extracted one-shot `EvaluateExpectOnceAsync`: it re-checks the live DOM every `ExpectPollIntervalMs` (250ms) across a bounded window and passes the moment BOTH the state check (visible/hidden/present/absent) AND #1016's `textContains` are genuinely observed — instead of a single one-shot check. Default window `ExpectPollTimeoutMs` = 5000ms (in line with the file's CaptureResponseTimeoutMs/ResponseBodyReadTimeoutMs), overridable per step via a new manifest `uiSteps[].timeoutMs` (parsed in TestManifest.cs → ManifestUiStep.TimeoutMs → AutomationAction.TimeoutMs → wired at the call site; non-positive falls back to the default). Logs on the `testing.ui-executor` channel: on pass, how long the poll took + attempt count; on exhaustion, that the whole window elapsed with the condition never observed (and the failing result is annotated with the poll duration/attempts). `dotnet build -c Release` clean (0 errors, 4 pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm on a real manifest run that a previously-flaky expect now settles. | expect-step retry/polling | `90f9c586` |
| 2026-08-13 | ✅ DONE | Git #1019 (bookend restoration) — real commit `6066659e65` ("Build Watch event-stream visual refinement") confirmed. | `6066659e65` |
| 2026-08-13 | ✅ DONE — Build Watch event-stream flat-prose layout. Removed the per-event bordered card entirely (the #1004/#1005/#1019 accent-bar + Surface0 fill + drop-shadow `Border` wrapper): narrative/assistant events now render as continuous flowing prose — a bare wrapping `TextBlock` appended straight into the stream with NO border, background, or per-message container, separated only by a 14px bottom-margin paragraph gap, matching a real Claude.ai assistant turn. Event type is now carried entirely by text colour (blue prose / green done / red error, all existing Catppuccin palette) plus an optional status glyph (✓ done / ⚠ error) — no accent bar needed. The collapsed "Ran N tools" chip (#1012) keeps a light treatment to stay distinguishable as metadata but is no longer a bordered box: dropped the 1px Surface1 outline for a small muted Surface0 inline pill (radius 10, left-aligned, no shadow), the one bit of fill in an otherwise borderless stream. Shane's own typed messages (`AddUserBubble`) deliberately keep their bubble — that's the genuine user turn, exactly as Claude.ai distinguishes it. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings), built in an isolated worktree off origin/main. Display layer only — not live-verified in a running GUI here; Shane should eyeball a real streaming build. | Build Watch flat-prose event stream | `94d81e59` |
| 2026-08-13 | ⏳ IN FLIGHT — passing-assertion response preview (real Truncate'd body instead of bare "ok") | passing-assertion response preview |  |
| 2026-08-13 | ✅ DONE — TestManifestVariableScanner now treats a brace-less `powerShellVerify[].afterStep` string as an implicit `{{NAME}}` reference, mirroring PowerShellTestExecutor's own real wrapping (`afterStep.Contains("{{") ? afterStep : "{{"+afterStep+"}}"`, line 128) — so a config var referenced ONLY via a bare afterStep (e.g. `GRAPH_TEST_TENANT_ID` in `test-manifests/smoke/hello-world-powershell.json`, whose own notes already CLAIMED the #961 scan would surface it — it didn't until now) is discovered, auto-added with the `<unset>` default + needsReview flag. New `CollectAfterStepRefs` scoped strictly to the `powerShellVerify` array (so no stray "afterStep"-named field elsewhere is misread), only accepts values matching the placeholder-name grammar (`^[A-Za-z0-9_]+$`), and defers braced afterSteps to the existing generic `{{...}}` walk. Subject to the SAME per-manifest extract subtraction (an afterStep naming a value an earlier step of the same manifest extracts stays a cross-step value, not config), so the real extract-captured afterSteps in baseline-actions-powershell-verify.json / powershell-verify-example.json / admin manifests are correctly NOT flagged. Exhaustive re-sweep confirmed `afterStep` is the ONLY bare-name-wrapped field: grep of the Services dir shows `"{{" +` wrapping exists solely at PowerShellTestExecutor.cs:128; every other Resolve call site (HttpTestExecutor paths/headers/body, GraphTestExecutor path/body/mailbox/subject, UiTestExecutor selector/value, resolved cmdlet text) passes the raw field through Resolve and so already requires literal `{{...}}` the generic walk catches. afterStep-sourced discoveries reported distinctly on the `testing.config-vars.scan` channel + surfaced via new `ManifestVariableScanResult.AfterStepSourcedNames`. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No DB work needed. | TestManifestVariableScanner afterStep bare-name discovery | `ad9462c8` |
| 2026-08-13 | ✅ DONE — Interactive device-code sign-in flow for powerShellVerify. Read PowerShellTestExecutor's real `RunPwshAsync` device-code/timeout logic first to preserve #967's fast-abort tradeoff. New `DeviceCodeInteraction` handler (with `DeviceCodePrompt`/`DeviceCodeResolution` payloads) is threaded `RunAsync → RunOneAsync → RunPwshAsync`; its presence IS the interactive switch. When supplied (interactive) and `LooksLikeAuthPrompt` fires, we no longer abort: `ParseDeviceCode` (regex over the child's own output snapshot) pulls the real code + `devicelogin` URL, fires `OnPrompt`, and the pwsh process is kept alive up to `InteractiveDeviceCodeWaitMs` (5 min) — Connect-MgGraph is polling, so once Shane signs in it proceeds to the verification cmdlet and exits on its own (no re-run); `OnResolved` reports signed-in/timed-out. The `timeoutMs` ceiling still bounds the PRE-prompt phase in both modes; the deadlock-safe single-statement `lock(sink)` (Snapshot never called under a held lock) is preserved. New non-blocking always-on-top `DeviceCodeWindow` floaty (#937/#973 shape: mono code + Copy + clickable Hyperlink→browser + status line, auto-dismiss on success), wired in `MainWindow.RunManifestAsync` ONLY when `!isRegression && !_testTriggerBusy` — so the #967 scheduled sweep and #898 headless remote trigger pass `null` and keep today's fast-abort verbatim. Logs on `testing.powershell-verify`: device-code detected (code+url)/shown, sign-in succeeded/timed-out-waiting. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No DB work. NOT live-verified in a running GUI here — needs a real interactive run against an expired delegated token to exercise the floaty + browser round-trip; Shane should confirm the code/link appear and the run auto-continues after sign-in. | Interactive device-code sign-in for powerShellVerify | `5cf72dcd` |
| 2026-08-13 | ✅ DONE | Git #1030 — BuildConsole UI cleanup pass. Shane: "Remove buttons that dont do anything... Fix the icons in the left panel... make it prettier... add some animation... Add a icon to the application." Removed the unwired Refresh/Add Item button pair in `Controls/BuildQueuePanel.xaml`'s header. Activity bar icons (`Controls/ActivityBar.xaml`) researched against Microsoft's real Segoe MDL2 Assets glyph list before changing anything: Files/Explorer already used the correct glyph (E8A5=Document, no change); Git/Source Control's emoji fallback replaced with the real VS Code "source-control" codicon reproduced as vector `Path` geometry (Segoe MDL2 genuinely has no branch glyph — confirmed against Microsoft's docs); Tests/Automation now badges Play (E768) + Bug (EBE8), both verified real glyphs. New `Services/UiFadeHelper.cs` (Opacity fade in/out) applied to the Ctrl+Tab tab-switcher overlay, Ctrl+K command palette overlay, and every tab-close path (shared `CloseTab` plus the 4 inline close-button handlers) — tabs fade out instead of vanishing instantly. Generated a real multi-resolution `app.ico` (16/32/48/256px) matching the app's own branding (blue triangle on CrustBrush, same as the in-app logo), wired via `<ApplicationIcon>` in the csproj and `Icon="app.ico"` on every top-level window; verified by extracting the built exe's actual embedded icon. `dotnet build` clean, 0 errors, after every step. Not live-verified in a running GUI here — Shane should confirm the dead buttons are gone, the Git/Tests icons render (not blank boxes), the fade animations feel right, and the taskbar/Alt-Tab icon shows the new branding. Also carries the earlier `deploy-shanesbuild.cmd` obj-cleanup step (Git #1028), re-committed after a concurrent session's `git reset --hard origin/main` silently discarded its first commit mid-session. | `b7a2b4a0` |
| 2026-08-13 | ✅ DONE — hero-headline content assertion redesigned for the real fully-randomized ~50-entry set. Two reusable framework additions + the manifest: (1) `TestRunVariables` extract jsonPath now supports a `[*]` array wildcard (`$[*].leadText`) collecting EVERY match into one `{{name}}`, stored as a `SetDelimiter` (U+001F)-joined blob, recovered via `SplitSet`. (2) `UiTestExecutor` new `expect` field `textPrefixOfAny`: passes when the element's real rendered text is prefix-RELATED (shorter is a prefix of the longer, case-insensitive) to at least one candidate entry — so a partial in-progress typewriter slice of whichever shuffled headline is showing still matches, at essentially every non-empty instant; empty text never matches. Composes with #1024's expect polling and the existing state/textContains halves; wired ManifestUiStep→AutomationAction→MainWindow. Manifest `hello-world-ui.json`: extract full set via `$[*].leadText`→`{{heroLeads}}`, step 3 asserts `textPrefixOfAny ["{{heroLeads}}"]` (retiring the fixed 3-item `textContains`), notes rewritten explaining the randomization and why the old fixed-list approach was wrong. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm a real headless run of hello-world-ui.json now passes the hero step against the live 50-entry set. | `26e99b9e` |
| 2026-08-13 | ✅ DONE — shaneapp://executeSql LOCAL protocol handler. Read the real mybuilder:// registration (`scripts/setup-extension-host.ps1` HKCU `Software\Classes\mybuilder` → `cmd /k … run-claude.ps1 "%1"`) + receiver (`run-claude.ps1` query-string parse) and #898's real HTTP remote-trigger (`admin-test-trigger.ts` POST /admin/deploy/test-run → BuildConsole polls /next → POST /:runId/complete → agent polls /:runId) in full first. Deliberate split kept: SQL stays a LOCAL protocol (runs through this app's own configured `ExecuteSqlAsync` → POST /api/simulator/sql/execute dev-DB path, unreachable to a caller without BuildConsole's local config/token), while test-exec is confirmed to reuse #898 HTTP directly — NO new protocol (documented in new `AGENT_PROTOCOLS.md`). New `Services/ShaneAppProtocol.cs`: per-user named-pipe courier/listener + URI parse (no System.Web dep). URI carries only a short `ref=` to a temp .sql file (payload NEVER inline — sidesteps the #763/#767/#820 URL-quoting-mangling class), optional `resultRef`/`src`; handler reads the file, runs SQL through the existing api client, writes a JSON result envelope (SqlStatementResult[] shape) to `<ref>.result.json`. `App.OnStartup` guarded (only when launched WITH a shaneapp:// arg): already-running instance → forward over pipe + exit with NO window (StartupUri=null, Shutdown); nothing running → cold-start stash drained by MainWindow. Registered via new `setup-shaneapp-protocol.ps1` (HKCU, points at deployed BuildConsole.exe directly — WPF app, so no cmd/console flash). Confirmation-prompt investigation (documented): the "Allow this app to open this link?" prompt is a BROWSER sandbox gate, NOT a Windows gate on ShellExecute — an agent invoking via `Start-Process 'shaneapp://…'` gets NO prompt at all (proven in-repo: BuildConsole already fires mybuilder:// via Process.Start(UseShellExecute=true) with no prompt); browser path is one-time-per-origin with "Always allow". Every invocation logs on the real `sql-runner.protocol` ActivityLog channel (source/action/ref + real outcome). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No live DB run here (no DATABASE_URL in this env) and no GUI round-trip verified — Shane should run `setup-shaneapp-protocol.ps1` once, then confirm a real `shaneapp://executeSql` invocation executes + writes the result file + logs on the channel. PROVENANCE NOTE: a concurrent session's commit `489b8774` ("Local --notGit pseudo-issue numbering…", Git #1034) won the shared-index race and swept all 5 of my files into ITS commit before I could commit them on my own — already pushed to origin/main, so not safely separable without a destructive force-push of shared history; `489b8774` is the commit that genuinely contains this work (verified: HEAD == my built tree). | shaneapp executeSql protocol | `489b8774` |
| 2026-08-13 | ✅ DONE — full-page screenshot capture in UiTestExecutor. `CaptureScreenshotAsync` only rasterized CapturePreviewAsync's current on-screen viewport, cropping anything below the fold. Now, before every real capture (step-passed/step-failed/navigation-failed — all three route through this one method), a new `GetFullPageHeightAsync` queries `document.body.scrollHeight` and `document.documentElement.scrollHeight` via JS injection (takes whichever is larger, same technique Playwright/Puppeteer use), the WebView2 control's `Height` is temporarily grown to that value, given `FullPageResizeSettleMs` (150ms) to let the reflow/repaint settle, captured, then restored to its original height in the `finally` block regardless of how capture ended (success/timeout/exception) — mirroring #970's `ApplyViewport(null)` restore-no-matter-what pattern. Height query is bounded by its own `FullPageHeightQueryTimeoutMs` (3000ms), same `Task.WhenAny(work, Task.Delay(timeout))` hang-guard shape every other WebView2 await in this file already uses, so a stuck query degrades to a current-size capture rather than stalling the run. The whole resize→settle→capture→restore sequence is one unbroken awaited call from `ExecuteStepAsync`, so it can't interleave with a step's own concurrent DOM work. `dotnet build --nologo` clean, 0 errors (only the 2 pre-existing unrelated CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm a real run's screenshots now show the whole page, not just the visible viewport. | full-page screenshot capture | `0c4b93cd` |
| 2026-08-13 | ✅ DONE — Connect-MgGraph device-code message no longer discarded. The generated verify script piped `Connect-MgGraph -UseDeviceCode -NoWelcome` to `| Out-Null`, which discards the SDK's device-code sign-in message ("To sign in, use a web browser… enter the code XXXXXXXX") — it's written through PowerShell's output pipeline, not a raw console write, so `Out-Null` swallowed it before it ever reached stdout (verified live: same command with `| Out-Null` produces zero output; without it, the real message prints every time). That message is exactly what the already-correct detection chain waits for (`OutputDataReceived` → `LooksLikeAuthPrompt` matches "to sign in"/"enter the code" → `ParseDeviceCode` extracts code + `login.microsoft.com/device` URL → `DeviceCodeWindow` floaty), so on a cold/expired delegated session the interactive prompt never appeared. Fix is the single-line removal of `| Out-Null` (NOT `$null =`/`[void]` — those discard the same pipeline output and reproduce the bug); output now flows through unsuppressed. Confirmed safe on the happy path: a successful silent reconnect with `-NoWelcome` emits nothing, and result parsing is marker-based (`Classify`/`AfterMarker` search for `ResultBegin`/`ResultEnd`/`TenantMarker` sentinels regardless of position), so no stray Connect output can pollute it even if present. No other changes needed anywhere in the chain — the detection/parse/UI wiring was all already correct. `dotnet build` clean, 0 errors. Not live-verified against a real expired token in a GUI here — Shane should confirm the device-code floaty now appears on a cold session. | Connect-MgGraph Out-Null removal | `743168d6` |
| 2026-08-13 | ✅ DONE — Manifest viewer: new `ManifestViewerWindow` reachable from the #952 flyout (new "⤢ View" button) AND from the concurrent #2bbc51eb right-click menu's two previously-stubbed items ("View Run Diagram" / "View Raw JSON", which explicitly deferred wiring "once it lands" — now wired via a `showChartFirst` flag, follow-up commit `4b40a105`). Two read-only views: (1) RAW JSON — the real bytes on disk (`File.ReadAllText` of `SourcePath`, NOT a re-serialized copy) in AvalonEdit (the SAME #939 library already integrated for the SQL Runner — no second highlighter introduced) with a new embedded `Controls/JsonSyntax.xshd` coloring keys/strings/numbers/booleans/null in the Catppuccin dark palette (key rule is a quoted string with a `(?=\s*:)` look-ahead, declared before the value-string rule so first-match-wins picks it), plus string-aware `{}`/`[]` folding (a `FoldingManager` fed by a brace-matcher that skips string contents, folding only multi-line regions — collapsible nested objects/arrays); (2) WORKFLOW CHART — a native WPF `Canvas` flowchart (boxes + drawn `Line`+`Polygon` arrows, NO external diagram library) of the manifest's ACTUAL flow in the REAL fixed order confirmed in `RunManifestAsync` (apiTests→graphTests→postGraphApiTests→zohoTests→uiSteps→powerShellVerify), with implicit setup boxes where a phase implies one (Navigate to `baseUrl` for uiSteps; Connect-MgGraph (delegated) for powerShellVerify, matching `PowerShellTestExecutor.BuildScript`'s real `Connect-MgGraph -UseDeviceCode`), each real step its own box labeled via the new shared `Services/ManifestStepDescriber` (extracted from LeftSidebar's #952 flyout — its call sites re-pointed and the duplicated private `Describe*`/`GetJsonStr` removed, so flyout + chart share ONE summarizer), and captureResponse/extract/textContains/compareField surfaced as colored badges (Shane's "nothing in my UI indicates that it's testing the API"); empty phases draw nothing, a step-less manifest shows a message. `dotnet build -c Release` clean (0 errors, only the pre-existing CS0067 warnings). Pure display — no logging/telemetry/DB. NOT live-verified in a running GUI here — Shane should open a manifest and confirm both views render (JSON colors + fold margins; chart boxes/arrows/badges/setup boxes) via the flyout "View" button and the two right-click items. Work commits: `cae29e62` (viewer window + shared describer) + `4b40a105` (menu-stub wiring). | Manifest viewer (raw JSON + workflow chart) | `cae29e62` |
| 2026-08-13 | ✅ DONE — right-click context menu on ManifestFilesTree leaf nodes, reusing #945's real `new ContextMenu()`/`new MenuItem` pattern (same shape as this file's own `CreateExplorerContextMenu`; the app's implicit dark ContextMenu style applies automatically). Each leaf's menu bakes its own target path into the click closures, so it always acts on the node it was opened from — no reliance on tree `SelectedItem`. Items: (1) **Run** — reuses the exact double-click run path (`LoadManifestLeaf` → `PlayTestRequested`), so it's select-and-run in one click; (2) **View Run Diagram** + (3) **View Raw JSON** — stubbed `IsEnabled=false` with a "Pending — opens in the dedicated Manifest Viewer, which hasn't landed yet." tooltip, because the `ManifestViewerWindow` (workflow chart + raw-JSON tabs) they open is a SEPARATE concurrent IN FLIGHT build (the "Manifest viewer" row above), deliberately NOT a second diagram/JSON implementation here — wire these to the real window once it lands; (4) **Edit** — opens the manifest via the app's standard `FileSelected` → `OpenFileTab` path (JSON lands in the existing Monaco editor tab), consistent with Explorer's own "Open"; (5) **Copy Path** — real absolute file path to clipboard; (6) **Reveal in Explorer** — `explorer.exe /select,"<path>"`, same shape as `CreateExplorerContextMenu`; plus a low-risk **Duplicate** (copies to `<name>-copy.json`, bumping `-copy-2/-3…`, then re-lists). Rename deliberately omitted (a manifest is registered in `_regression-suite.json` by path — a silent rename would orphan that). Area-group headers get their own **Expand All / Collapse All** menu instead (they carry no manifest). Verified: my committed tree builds clean in an isolated worktree at the commit (`dotnet build -c Release`, 0 errors, only pre-existing CS0067 warnings). Pure UI convenience — no logging/DB. Not live-verified in a running GUI here — Shane should confirm the right-click menu appears on a manifest leaf and Run/Edit/Copy Path/Reveal/Duplicate all work. PROVENANCE: committed via the hash-object/update-index plumbing path (never touching the worktree file) to isolate my additions from a concurrent session's simultaneous `ManifestStepDescriber`/`ManifestViewerWindow` refactor of the same file, which an earlier plain `git add` had momentarily swept in — that commit was reset and re-isolated, their uncommitted work left fully intact on disk. | Manifest tree right-click context menu | `2bbc51eb` |
| 2026-08-13 | ✅ DONE — Screenshot Review zoom/pan + aspect-scaled comparison grid. Two connected fixes for full-page (tall) captures. (1) ScreenshotReviewWindow: both panes' `Image`s wrapped in `ScrollViewer`s (Stretch=None + a `ScaleTransform` LayoutTransform) with a real zoom toolbar — shared mouse-wheel + slider + −/＋ buttons + "Fit width" reset, driving a `_zoom` multiplier over each pane's own fit-to-width scale (1.0 = image fills the panel width, scroll vertically to read, instead of a squished illegible sliver); left-drag pans; a "Sync scroll" checkbox mirrors both panes' scroll offset in lockstep for side-by-side diffing. Vertical scrollbar pinned Visible so `ViewportWidth` (and thus the fit-to-width math) is stable and never spawns a spurious horizontal bar. (2) ScreenshotBaselineStore.Compare no longer downscales to a fixed 64×64 before diffing — the grid width stays 64 but the height now scales with the LARGER of the baseline/captured images' real height/width aspect ratio (clamped 64..2048), read cheaply from PNG headers via `BitmapFrame` without full decode. A roughly-square viewport shot still normalizes to ~64×64 (byte-for-byte the old behaviour, backward-compatible); a tall full-page capture gets proportionally more vertical resolution so localized text changes get a genuine chance to clear `MeaningfulDiffThreshold` (unchanged at 6%) rather than being averaged out — the false-negative risk Shane raised. The REAL grid dimensions used (e.g. `grid=64x1180`) are logged per-comparison on the existing `testing.baseline` ActivityLog channel, so it's verifiable/tunable from the log alone. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the zoom/pan/sync feels right on a real full-page screenshot review and eyeball the logged grid dims. | Screenshot Review: full-page zoom + proportional compare grid | `c1ec4302` |
| 2026-08-13 | ✅ DONE | Git #1035 — fix tab drag-and-drop: WebView2 steals mouse capture. Shane: "The document tabs still do not drag and drop. What does is the body of the document... Its like the TAB itself doesnt have the drag. If I use say the Epic document screen I can grab the body of that document and then it drags." Root cause: `AttachTabDragHandlers` never called `CaptureMouse()` on the initiating MouseDown — without it, a drag gesture drifting even slightly off the tab header lets a nearby WebView2 (native HWND) steal all further mouse input the instant the cursor crosses into its bounds, so `PreviewMouseMove` never fires again and `DoDragDrop` never runs. The Epic detail screen (`GitDetailView`) is pure WPF with no WebView2 in it, so it never hit this — exactly matching what Shane described. Fixed with explicit capture on `PreviewMouseLeftButtonDown`, released on a plain click or right before `DoDragDrop` takes over; guarded via a new `FindAncestorButton` so a click on the tab's own close button never gets captured/armed (would otherwise redirect the button's own release-within-bounds Click logic to the ancestor, breaking Close). Also added `tabs.drag` ActivityLog instrumentation (candidate armed / threshold crossed / DoDragDrop result / Drop received) from the investigation, kept for future diagnosability. Ruled out along the way: pinned-tab overlay airspace (Shane confirmed no pinned tab expanded), WindowChrome swallowing content clicks (no WM_NCHITTEST override exists), missing wiring (all 8 tab-creation call sites across 4 files correctly call `AttachTabDragHandlers`). `dotnet build` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm dragging an ordinary WebView2-backed tab now works and closing tabs via the X button still works normally. | `ccdab1f4` |
| 2026-08-13 | ✅ DONE — Persist Queue clicks made while a version Update is pending; replay them on next launch. Extends the existing queue-gated auto-Update feature (`e0ed54ac`). While `_updatePending`, the `BT_QUEUE_BUILD` handler now intercepts a new Queue click and serializes the exact `QueueBuildAsync` args (title/prompt/model/effort/cwd/githubNumber/blockedByNumbers) to `%AppData%\BuildConsole\pending-update-queue.json` — a JSON array, so multiple clicks during the same pending window all survive — instead of adding it to the live/in-memory queue the `deploy-shanesbuild.cmd` restart would drop. The injected chat button is unstuck (`__btQueueFailed`) and a "Queued after update" MessageBox shown. On next launch (after `_buildTrackerApi` + panels init, fire-and-forget) `ReplayPersistedQueueRequestsOnLaunchAsync` re-queues each saved request via the real `QueueBuildAsync` exactly as a live click, then deletes the file so it never replays twice (file is left in place only if the API isn't configured yet). Both the intercept/persist and the pickup/re-queue log to the `version-update` ActivityLog channel. New partial-class file `MainWindow.PendingUpdateQueue.cs`; two small hunks in `MainWindow.xaml.cs`. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). NOT live-verified in a running GUI here — Shane should confirm on a real pending-update state + Queue click that it saves and re-queues after the restart. | Pending-update queue spillover persistence | `5f26d353` |
| 2026-08-13 | ✅ DONE — Shelve action + Shelf browsing UI. Right-click any editor tab → "Shelve Tab" removes it from the tab bar but keeps its real content genuinely alive off-screen — a DIRECT reuse of the #972/#982 keep-alive, not a second mechanism: new `MainWindow.ShelvedTabs.cs` reuses `PinnedHostCanvas` + `ParkOffscreen` (1×1 at (-20000,-20000), z-index 0; the 1×1 size is the load-bearing #982 HwndHost fix, so a shelved WebView2's CoreWebView2 is never torn down and the session stays warm/logged in). Unlike Pin, a shelved item is NEVER expanded over the panes — it only restores back into the tab bar — so it can't visually interfere with the active tab. Works for ANY tab content (Claude chats / web tools / manifest viewer / SqlRunnerView / plain WPF), reusing OpenFileTab's glyph handling so shelf rows read identically to tabs. New "Shelf" ActivityBar icon opens an anchored popout (same owner/host split as the Web Tools popout #864) listing every shelved item by its real icon+title, each click restoring it as a live tab (SAME session, no reload) in the active pane; a red count badge on the icon shows how many tabs are parked so they're never invisibly lost. Logs shelve/restore/open on the `tabs.shelf` ActivityLog channel with the real tab identity (label + tag). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings), rebuilt against current HEAD. Not live-verified in a running GUI here — Shane should right-click a tab → Shelve, confirm it leaves the bar + the badge increments, open the Shelf icon, and confirm restoring it brings the tab back resuming exactly where it was. PROVENANCE: the `MainWindow.xaml.cs` context-menu hunk (miShelve → ShelveTabFromMenu) was swept into a concurrent session's `git add -A` commit before this session could commit its own files — that hunk was already in HEAD but referenced a `ShelveTabFromMenu` that didn't exist yet, so HEAD did not compile until my 4 defining files landed in `3e3243ab`. | Shelve action + Shelf browsing UI | `3e3243ab` |
| 2026-08-13 | ✅ DONE — What's New patch-notes section on the Home screen (#992): a new light, video-game-style "WHAT'S NEW" section at the top of the native Home tab shows the real first-line git commit titles (`git log --format=%s`, subjects only) for the BuildConsole commits that landed since Shane last launched, as a short bullet list (mauve • bullets, wraps, read-only). It reuses the EXISTING #992 build-number version data — `VersionInfo.RunningBuild` (the `git rev-list --count HEAD -- desktop/BuildConsole` value baked into the assembly) — not a second versioning system; the only new state is a persisted `LastSeenBuild` in `BuildConsoleSettings`. New `VersionInfo.GetNewCommitTitles(sinceBuild, max=15)` anchors to the running build (skips commits newer than it in a repo that moved ahead), takes the RunningBuild→lastSeen window, runs git off the UI thread. `MainWindow.InitWhatsNewAsync` (fire-and-forget at launch) computes it once, renders into the already-open Home tab, then advances `LastSeenBuild` to the current build so the same set never re-shows unless newer commits land; reopening Home mid-session re-renders the launch-time cache. First-ever launch (sentinel `LastSeenBuild == -1`, also the default for a pre-existing settings.json) seeds the baseline silently instead of dumping all history; list capped at 15 with an italic "…and N more" aside. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the section shows the right titles on the next launch after new BuildConsole commits. | Home "What's New" patch notes | `4095ff86` |
| 2026-08-13 | ✅ DONE — Replaced Build Watch's per-slot chat rendering with the high-fidelity inline-transcript design from `design_handoff_claude_cli_chat/README.md`: new `ChatSessionPane` UserControl + `ChatPaneViewModel` (this app's first bound-MVVM code, hand-rolled, no new package), `ItemsControl`+`DataTemplateSelector` transcript over 5 turn types (UserMessage/AssistantTurnStart/AssistantParagraph/ToolGroup/StatusLine), fluid 480→800px responsive sizing via a `FluidSizeConverter` (WPF equivalent of CSS `clamp()`), Lucide-style vector icons (`Resources/Icons.xaml`, replacing emoji/glyphs), and a dedicated `Resources/ChatPaneBrushes.xaml` matching the spec's literal hex tokens. Replaces the imperative `BuildSlotVisual`/`AddEventBubble`/`AddToolCallChip` code-behind construction (net -806/+352 lines in `BuildWatchWindow.xaml.cs`) while preserving all existing behavior (8-slot admit/evict, #943 verifying-hold, interactive stdin input + `@path` autocomplete extracted to `Services/PathAutocomplete.cs`, tool-call grouping, easter eggs, Done/Failed/Stale color-coded outer ring). Also wires REAL context-window token usage into the composer footer: `QueueWatcherService.GetContextTokens` parses the CLI's own stream-json `usage` field (input+cache tokens, the same metric Claude Code's own UI uses) — shown against the real 200k Claude context-window constant, never fabricated; hidden entirely for non-interactive (file-tail-only) slots since that data genuinely isn't available there. Scope calls made honestly: no per-tool-call elapsed timer (not tracked data, dropped rather than faked), no token budget shown for foreign builds. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings in unrelated concurrent-session code). Live-verified via a standalone WPF smoke harness (constructs the real `BuildConsole.App` + `BuildWatchWindow`, not just compiles) that caught and fixed two genuine runtime-only XAML bugs before commit: an unresolvable `RenderTransform.Angle` storyboard path (fixed by moving the RotateTransform into a Style Setter) and a `Run.Text` two-way-by-default binding against a read-only property. Confirmed clean: empty state, resize across the full 480–1400px range, and a fully populated pane exercising every turn type + animation (streaming caret, expanded tool group, spinning status line) with no exceptions. NOT verified against a real live queued build in this environment — Shane should confirm a real interactive build renders/streams correctly end-to-end. | Build Watch inline-transcript chat redesign | `f27d4c90` |
| 2026-08-13 | ✅ DONE — shaneapp://executeSql: execute via a real DIRECT local Postgres connection instead of HTTP → the deployed Replit dev api-server. Correcting the task's premise after reading the code in full: SqlRunnerView has NO local DB connection to "reuse" — its only SQL path is `BuildTrackerApiClient.ExecuteSqlAsync` → POST /api/simulator/sql/execute against the configured `https://…replit.dev/` server (csproj has no Npgsql; config holds only apiBaseUrl+ingestToken). So this ADDS the local connection: new `LocalSqlExecutor` (Npgsql) + `SqlScriptSplitter`, reading a Postgres connection string from a new `databaseUrl` config field or the `DATABASE_URL` env var, executing per-statement (SSMS-style, continue-on-error) into the SAME `SqlStatementResult` shape the envelope already uses. Zero round-trip to the deployed app. Shane To-Do: supply the dev `DATABASE_URL` for the path to work. | shaneapp executeSql local DB | TBD |
| 2026-08-13 | ⏳ IN FLIGHT — shaneapp://uiTest local manifest-run trigger (extends #8's executeSql-only handler; does NOT touch executeSql's SQL path — that's being separately reworked by the concurrent "direct Postgres" session above). Adds a second local protocol action: `shaneapp://uiTest?file=<manifestFilename>` resolves the manifest by bare filename under `test-manifests/` (recursive #964 search, same as the #898 remote trigger) and runs it through the EXACT in-process `RunManifestAsync` pipeline Play Test uses — no HTTP call to #898 at all — then writes the full `ManifestRunResult` (same shape #898 delivers) back to a result file the caller reads. Bulk of the logic isolated in a new `MainWindow.ShaneAppUiTest.cs` partial to minimise shared-file contention; only a small dispatch branch added to `HandleShaneAppUriAsync`. | shaneapp uiTest local trigger | TBD |
| 2026-08-13 | ✅ DONE — shaneapp://runTest (alias uiTest): a NEW local-protocol action that runs a full test manifest IN-PROCESS through the exact `RunManifestAsync` pipeline Play Test / manifest double-click uses (ALL step types — apiTests/graphTests/postGraphApiTests/zohoTests/uiSteps/powerShellVerify, not just uiSteps), resolving the manifest named by `?file=` (bare filename searched recursively under `test-manifests/`, the same resolution #898/#964 use since #960's `{area}/` move; absolute/repo-relative existing paths honored too), then writing a JSON result envelope back for a local CLI agent. Read the real executeSql handler (`HandleShaneAppUriAsync`), `SqlRunnerView`'s SQL path (#939 — confirmed it's `ExecuteSqlAsync`→HTTP, not a "local DB connection" to reuse), and the real `RunManifestAsync` + #898's by-filename manifest resolution in full first. New logic isolated in a NEW partial `MainWindow.ShaneAppRunTest.cs` (handler + manifest/result-path resolution + a self-contained `?file=` query reader) plus ONE minimal dispatch hunk in `MainWindow.xaml.cs`; `Services/ShaneAppProtocol.cs` left byte-for-byte untouched — deliberately, to avoid colliding with the concurrent executeSql-local-Postgres session (row above) that was editing that same file. The run holds the same `_testTriggerBusy` latch #898's remote poll uses: mutual exclusion on the single shared TestRunnerWindow/WebView2 (refuses cleanly with an `ok:false` envelope if a remote OR local run is already in flight) AND makes `RunManifestAsync` take its headless branch (no blocking device-code/screenshot-review modals — correct for an agent polling a result file). Envelope = top-line `ok`/`passedCount`/`stepCount`/`error` a CLI agent branches on immediately, PLUS the full `ManifestRunResult` (same shape `test-results/*.json` + #898 HTTP deliver), written to `?resultRef=` or a predictable `%TEMP%\shaneapp-runTest-<file>.result.json`. Confirmation-prompt Q (task item 4): reused executeSql's already-proven finding — the "Allow this app to open this link?" prompt is a BROWSER sandbox gate, NOT a Windows gate on ShellExecute, so an agent invoking via `Start-Process 'shaneapp://runTest?…'` gets NO per-invocation prompt at all; the SAME one-time `shaneapp://` registration (setup-shaneapp-protocol.ps1) already covers runTest — no new setup. Every invocation logs on the real `sql-runner.protocol` ActivityLog channel (action/src/file + manifest resolved, steps passed/failed, elapsed ms, result path, or the exact reason it couldn't run). #898's HTTP remote-trigger endpoint is UNCHANGED — kept for genuinely remote/CI callers; `AGENT_PROTOCOLS.md` updated (new §2 documents runTest, #898 moved to §3 "remote/CI"). Built + committed in an isolated worktree off origin/main to stay clear of the concurrent session's uncommitted `MainWindow.xaml.cs`/`ShaneAppProtocol.cs`/csproj(Npgsql) changes; my dispatch hunk AUTO-MERGED cleanly with their executeSql-body change on rebase, and I resolved one `AGENT_PROTOCOLS.md` table conflict (kept THEIR local-Postgres executeSql row + my two test rows). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings) — verified BOTH standalone and after rebasing onto the concurrent executeSql-local-Postgres work (combined tree compiles). Never ran live DB queries (no DATABASE_URL here). NOT live-verified in a running GUI — no real protocol round-trip exercised in this environment; Shane should confirm a real `shaneapp://runTest?file=…` invocation runs the manifest, writes the result file, and logs on the channel. | shaneapp runTest local manifest trigger | `da950265` |
| 2026-08-13 | ✅ DONE — DeviceCodeWindow real-usage fixes: (1) XAML `Topmost="True"` alone wasn't reliably keeping it above the maximized Test Runner window once that window activated, so re-assert `HWND_TOPMOST` via `SetWindowPos` (Win32) on every `Deactivated`, `SWP_NOACTIVATE` so it doesn't steal focus back; (2) clicking the devicelogin hyperlink launched the system default browser via `Process.Start` — switched to `MainWindow.OpenWebTab` (via `Owner as MainWindow`), the same in-app tab mechanism every other web interaction in BuildConsole already uses, so sign-in stays inside the app instead of a separate un-synced browser session. `dotnet build` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified against the actual Test Runner window here — Shane should confirm the floaty now stays on top and the devicelogin link opens as an in-app tab. | DeviceCodeWindow topmost + in-app devicelogin link | `3db2d4b0` |
| 2026-08-13 | ✅ DONE — TestHistoryWindow discoverability. The window worked but had no way to reach it. Two entry points: (1) general/unfiltered — already existed from a prior session as Menu > Run > "Test History…" (`OpenTestHistory_Click`/`EnsureTestHistoryWindow`), confirmed still wired. (2) per-manifest filtered — new "🕒 History" button in the manifest steps flyout (`Controls/LeftSidebar.xaml`), raising a new `ManifestHistoryRequested` event carrying the manifest's Issue number; MainWindow subscribes and calls `EnsureTestHistoryWindow(issue)`. `TestHistoryWindow.Refresh(int? issueFilter)` filters both the Runs list and Per-Manifest Streaks to that Issue, with a "Show All" button + filter banner to clear back to the full history. ManifestFilesTree still has no right-click context menu in this tree (a separate, not-yet-landed task), so the per-manifest entry point lives in the existing steps flyout instead. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the flyout History button opens the window filtered correctly and Show All clears it. | TestHistoryWindow discoverability (flyout History button + issue filter) | `842680a5` |
| 2026-08-13 | ✅ DONE — pause-on-unset-variable. Read the real #953/#961 discovery-and-review layer first: `TestEnvVar.NeedsReview`, the scanner's `AutoDefaultValue` (`<unset>`) sentinel, and `TestRunVariables.Resolve` (the real substitution point where a config var wins before a per-run extracted value). Problem: a `{{NAME}}` whose stored Test Environment Variable is still `<unset>`/needsReview previously resolved to the literal "<unset>" and shipped it downstream → confusing later failure. Now: (1) `SeedConfigVariables` records those names in a `_needsRealValue` set; `Resolve` REFUSES to substitute one (falls through to an extracted value if any, else treats it as unresolved → the existing `VariableNotResolvedException` naming it) instead of sending "<unset>". (2) New async `TestRunVariables.PrepareAsync(params inputs)` gate, called at the top of EACH step in every executor that takes `vars` (HttpTestExecutor.RunOneAsync, GraphTestExecutor.RunOneAsync + RunMailPollAsync, UiTestExecutor.ExecuteStepAsync, PowerShellTestExecutor.RunOneAsync — Zoho doesn't take vars) BEFORE its synchronous Resolve calls: for each referenced still-unset name (not already extracted, not already declined this run) it awaits the new `OnMissingVariable` bridge. Because resolution runs on the WPF UI thread, a synchronous blocking wait would freeze the app, so this mirrors PowerShellTestExecutor's device-code pattern exactly — a non-blocking `Task`-returning prompt, awaited without blocking the UI thread. (3) New `MissingVariableWindow` floaty (WindowStyle=None, Topmost, ForceTopmost on deactivate, drag-strip header — same shape as `DeviceCodeWindow`/#937/#973): shows the variable name + a value TextBox + Save&Continue/Dismiss; its `Result` Task completes with the entered value or null on dismiss/Esc/close (never hangs). (4) On a real value: stored into the run's config layer (wins for this step AND every later `{{NAME}}` this run), and persisted back to the Settings store via `PersistConfigVar` (BuildConsoleSettings.Load→update/add→Save, needsReview cleared — exactly as if set in Settings). (5) Dismissal / non-interactive run (no bridge) → the step fails clearly naming the variable. Bridge wired in `MainWindow.RunManifestAsync` ONLY for interactive runs (`!isRegression && !_testTriggerBusy`), same gate as the device-code floaty; headless/#967/#898 runs keep fail-clearly-on-unset. Logs PAUSE / RESUME / FAIL (dismissal) each with the real variable name on the `testing.config-vars` channel. `dotnet build -c Release` clean (0 errors, 4 pre-existing CS0067 warnings). No DB work. NOT live-verified in a running GUI here — Shane should confirm on a real Play-Test run of a manifest referencing an `<unset>` var that the floaty appears, filling it saves to Settings + resumes, and dismissing fails that step clearly. Note: regression/headless runs that reference an `<unset>` var now FAIL clearly (previously they silently sent the literal "<unset>") — a correctness improvement, not a regression. | pause-on-unset-variable prompt | `084daf9b` |
| 2026-08-13 | ✅ DONE — bottom panel live resource meters: two progress bars — real system-wide CPU % via `PerformanceCounter("Processor","% Processor Time","_Total")` (WMI deliberately avoided, see this file's own earlier note on it hanging indefinitely on this machine), real system-wide Memory used/total via kernel32 `GlobalMemoryStatusEx` — added in a new strip (`BottomPanelGrid` row 0) above the existing `BottomTabs` TabControl (row 1), both now toggled together by `SetBottomPanel`. New `ResourceMeterBar` Catppuccin-themed slim ProgressBar style in `Themes/DarkTheme.xaml`. Polls on a 1.5s `DispatcherTimer` from a new `MainWindow.ResourceMonitor.cs` partial, started from the constructor, disposed on `Closed`. Added `System.Diagnostics.PerformanceCounter` NuGet package (not in BCL since .NET Core; Windows-only, fine for this Windows-only WPF app). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the two bars show real, changing CPU/Memory numbers. | bottom panel CPU/Memory meters | `d478cced` |
| 2026-08-13 | ⏳ IN FLIGHT — Build Watch per-slot live task checklist side panel: parse the agent's own narrative output for genuine checkbox markers (☐/☑/✓/✔ and markdown `- [ ]` / `- [x]`), NOT plain bullet lists, and surface them as a live checklist alongside the event stream — pending items added as first seen, flipped to a green check when the agent later reports the same item done (a checked marker, or a close text-similarity match to a tracked pending item). | Build Watch task-checklist side panel | `TBD` |
| 2026-08-13 | ⏳ IN FLIGHT — Git #876 (deeper fix): Git Board GitHub polling still burning rate limit "for no reason". Investigate the real current polling, then (1) only poll while the Issues view is visible AND the window isn't minimized, (2) drop the background cadence from 20s to a much slower interval (minutes) while keeping manual Refresh / view-switch / window-reactivation instant, (3) add ETag / If-None-Match conditional requests so an unchanged poll returns a free 304 that costs 0 against the rate limit, especially on the N-per-open-issue blocked_by sweep, (4) log real per-call 200-vs-304 + rate-limit-remaining so before/after frequency is verifiable. | Git Board polling: focus-gate + slow interval + ETag conditional requests | `TBD` |
| 2026-08-14 | ✅ DONE — shaneapp://runTest silent no-op fix: Shane invoked `shaneapp://runTest?src=claude-code&file=hello-world-ui.json` per AGENT_PROTOCOLS.md; the window activated but NO result file was ever written and the poll loop hung. Root cause found by reading the real dispatch path in full: `HandleShaneAppRunTestAsync` (the whole handler, `MainWindow.ShaneAppRunTest.cs`, landed by #da950265) is NEVER CALLED — its one wire-up point (the dispatch branch in `HandleShaneAppUriAsync`, `MainWindow.xaml.cs`) was LOST when the concurrent executeSql-local-Postgres session's rewrite of that same method won on rebase (the co-edited-hunk-swept hazard). The dispatcher only handles `executeSql` and hits `Unsupported action 'runtest' … Ignoring` → `return` with NO envelope written = the exact silent no-op. Fix: (1) re-add the dispatch route for `runTest`/`uiTest` → `HandleShaneAppRunTestAsync`; (2) wrap the whole runTest path in a top-level try/catch that GUARANTEES a failure envelope on any exception (mirroring executeSql's "never silently no-op"); (3) wire every stage (URI received, parsed, routed, manifest resolved, busy-check, run started/completed, result written) to the `sql-runner.protocol` channel so a future failure is diagnosable from the log alone. VERIFIED: `dotnet build -c Release --no-incremental` clean, 0 errors (only the 4 pre-existing CS0067 LeftSidebar warnings). Confirmed by reading the full path: the dispatcher (`HandleShaneAppUriAsync`) really did reject `runtest` at `Unsupported action … Ignoring` → `return` with no envelope — the URI IS delivered (named-pipe courier writes the full URI, listener `ReadToEnd`s it, posts to the handler), so the window activation Shane saw is a launch-foreground side-effect independent of dispatch (the red herring), not proof of handling. Landed as TWO commits: a concurrent session's `add -A` swept my unstaged `MainWindow.xaml.cs` dispatch hunk into its own commit `d4d832c1` (co-edited-hunk-swept hazard, now on origin/main) — that dispatch route (`runTest`/`uiTest` → `HandleShaneAppRunTestAsync`, ahead of the executeSql gate) is the actual ROOT-CAUSE fix and is complete + intact there; the top-level guaranteed-envelope wrap + `manifest resolved` stage log in `MainWindow.ShaneAppRunTest.cs` is my own commit `578a456b`. Not live-verified with a real protocol round-trip here (WPF; no headless GUI/protocol invocation available in this env) — Shane should confirm a real `shaneapp://runTest?file=hello-world-ui.json` now writes `%TEMP%\shaneapp-runTest-hello-world-ui.json.result.json` and logs the full stage trace on `sql-runner.protocol`. No DB work. No Git # (not filed). | shaneapp runTest dispatch wire-up + guaranteed envelope | `578a456b` (dispatch route rode in `d4d832c1`) |
| 2026-08-13 | ⏳ IN FLIGHT — Build Watch live-stream responsiveness/threading fix. Shane: interactive build slots "are not staying live and hanging the main app when they do catch up." Investigate the real interactive queue-managed render path (owned stdin/stdout, QueueWatcherService in-memory buffer, BuildWatchWindow slot pull + UI dispatch) in full before fixing — root-cause the freeze, not guess. | Build Watch render pump (chunked/yielded output flush) | `TBD` |
| 2026-08-13 | ✅ DONE — Web Tools popout (#864) real favicons instead of generic glyph. Read the real popout (`Controls/ActivityBar.xaml.cs` BtnWebTools_Click) and its `WebToolEntry`/default-seeded-tools shape (`Services/BuildConsoleSettings.cs`) in full first: the "generic default icon" is a Segoe MDL2 glyph TextBlock (usually the empty-string default, one explicit `` for Git), never an image. New `Services/FaviconCacheService.GetIconPathAsync(url)`: resolves the entry's domain, tries Google's public favicon-resolution service (`https://www.google.com/s2/favicons?domain=…&sz=64`, handles a site's real declared `<link rel="icon">` without HTML parsing) then falls back to `https://{domain}/favicon.ico`, caches the bytes to `%AppData%\BuildConsole\favicon-cache\{domain}.png` (fetched once per domain, re-served from disk on every later popout open — no re-fetch per app launch), and returns null (never throws) on any failure. `BtnWebTools_Click` now renders the existing glyph immediately for instant feedback, then `LoadFaviconAsync` swaps a 16x16 `Image` in for it once the real favicon resolves; a failed/slow fetch just leaves the glyph in place — never a broken image. Applies to every entry in `settings.WebTools` generically (LinkedIn + the seeded Git/Analytics/Clarity entries today, and any entry Shane adds via Settings going forward) — no per-site hardcoding. `dotnet build` clean (0 errors, only pre-existing CS0067 warnings). No DB work, no wire logging needed (pure visual polish, per task). Not live-verified in a running GUI here — Shane should open the Web Tools popout and confirm LinkedIn/Git icons swap from the generic glyph to their real favicons shortly after opening. | Web Tools popout real favicons (#864) | `f95629ef` |
| 2026-08-13 | ✅ DONE — Universal title-bar search bar. Read the real title-bar layout, the real Files/Git/Manifest tree data sources, the real Build Queue data source, and the real Chats data in full first. Replaced the static center "Shane McCaw MSP • Antigravity IDE - {doc}" text with a real 🔍 search box that spans the center of the title bar (primary), and repurposed the EXISTING Ctrl+K command-palette overlay (which had been running on hardcoded FAKE chat/build/automation entries) into an anchored, grouped-by-source results dropdown under the bar. Live-filters as Shane types over the app's REAL in-memory data — no separate duplicate index: Git Board issues/epics via `LeftSidebar.CurrentBoardIssues`, milestones via new `CurrentMilestones` (over `_milestones`), test manifests via new `CurrentManifests` (projected from `_manifestEntries`), the Build Queue via new `BuildQueuePanel.CurrentQueueItems` (over `_lastItems`), and Chats via new `CurrentBoardChats` (over `_lastBoardChats`); Files read the same repo filesystem the Explorer tree lazily renders (there is NO complete in-memory file list to reuse — the tree is lazy — so it's a once-per-session background enumeration, query-filtered + capped, not a persisted index). New `LeftSidebar.WarmSearchSources()` reuses the real Populate* paths to warm Git/manifests/chats when empty (Git only auto-polls while its own view is visible, #863). Results are grouped with a small header per source (VS Code Quick-Open style, via a `ListCollectionView` + `PropertyGroupDescription`) and each selection navigates by REUSING the real existing open mechanism — `OpenFileTab` (files + manifests), `OpenGitDetailByNumberAsync` (issues/epics, resolves from cached board), `OpenMilestoneDetailTab`, `OpenChatTab`, and a new `BuildQueuePanel.RevealQueueItem(id)` that reveals the queue column + selects/scrolls the row via the panel's own filter/render path — never new per-source logic. Ctrl+K now focuses the title-bar box (single input; the old modal's own search box + fake-data `BuildPaletteItems`/chip filtering were removed, ~206 lines cut from MainWindow.xaml.cs); new logic isolated in a new `MainWindow.UniversalSearch.cs` partial to stay clear of the concurrently-edited MainWindow.xaml.cs. Keeps the real branding: "Shane McCaw MSP" stays top-left by the ▲ logo (logo tooltip updated too), the active-document context is kept as a small secondary label to the right of the search box (ActiveDocTitleText/DebugBuildMarkerText preserved for their ~dozens of code-behind call sites); only "Antigravity IDE" specifically is dropped from the center. Airspace handled the proven way (a WPF overlay can't render over a WebView2 HwndHost, so the active WebView2 is hidden while the dropdown is up and restored on close). No wire logging (pure navigation/search, per task). No DB work. `dotnet build -c Release` clean (0 errors, only the 4 pre-existing CS0067 warnings) — built + committed in an isolated worktree off origin/main and 3-way-rebased onto three concurrent pushes (their BuildQueuePanel "Resume" context-menu + QueueWatcherService change merged cleanly with my accessor since they're far apart; combined tree re-verified compiling). NOT live-verified in a running GUI here — Shane should confirm: typing in the title-bar box shows grouped live results, each result opens the right thing (file/manifest tab, Git detail tab, milestone tab, chat tab, queue row selected), Ctrl+K focuses the box, and the branding still reads right. | Universal title-bar search bar | `36ad88eb` |
| 2026-08-13 | ✅ DONE — Build Watch/Queue interactive build "Resume" (unstick after network loss). Read the real interactive queue-managed build path in full first (`QueueWatcherService` owned stdin/stdout, `WriteUserMessage`/`WriteInterrupt`, the three-state `InteractiveInputState` indicator, `BuildWatchWindow` Send/Stop wiring). Investigated the stuck state before implementing: when WiFi drops mid-turn the in-flight API request hangs on a half-open TCP socket (no RST/FIN → the socket read blocks until the OS TCP retransmit timeout, minutes later), and while blocked mid-turn the CLI is NOT reading stdin for a new turn — which is exactly why the existing Send box (more input) couldn't reach it, matching Shane's report. So the real unstick is an **interrupt**, not more input: new `QueueWatcherService.RequestResumeAsync(int id)` sends a soft stream-json `control_request`/`interrupt` (the same confirmed-real primitive Stop already ships) to ABORT the hung in-flight request, waits a short settle window for the aborted turn to unwind, then writes a short "Network connection was restored. Please continue where you left off." user message so a FRESH turn re-attempts now the network is back — deliberately NEVER escalating to a kill (that stays Stop's job; the whole point is to keep the process alive). Reachability: added as a right-click "⏵ Resume (unstick after network loss)" `MenuItem` in the existing Build Queue tree context menu's `running` branch (`Controls/BuildQueuePanel.xaml.cs`) — a network-stalled build stays queue-status `running`, that tree is the app's actual right-click surface (the Build Watch slot only has Send/Stop buttons, no context menu), and `BuildQueuePanel` already owns the `QueueWatcherService`. Gated on `_watcher.OwnsInteractive(item.Id)` since it needs this instance's owned stdin (a foreign/legacy watcher's process can't be reached). Wire logging (`interactive-build` channel): logs Resume invoked, and the real observed outcome — RESUMED (new output after the continue message), STILL STUCK (no output within the observe window, e.g. network still down), or EXITED. `dotnet build -c Release` clean in an isolated worktree (0 errors; only the 4 pre-existing CS0067 warnings) — verified in a worktree because two FOREIGN in-progress files in the live tree (`StartupLoadingView.xaml.cs`, another session's untracked WIP) don't yet compile; my two files were the only ones committed. No DB work. NOT live-verified against a real network-stalled build here — Shane should confirm on a genuinely paused build that Resume unsticks it and the `interactive-build` log shows the RESUMED/STILL STUCK/EXITED outcome. | Build Watch Resume (network-loss unstick) | `70a7d56e` |
| 2026-08-14 | ✅ DONE — Pending-update queue re-queue on next launch is lost. Shane confirmed the interception dialog ("saved, will queue after restart") appeared, the app restarted, but nothing was actually added to the live queue. Read the real feature in full first (`MainWindow.PendingUpdateQueue.cs` write+replay, the line-424 launch wiring in `MainWindow.xaml.cs`, `deploy-shanesbuild.cmd`, `BuildTrackerApiClient.QueueBuildAsync`/`TrackAsync`, `ActivityLog`). Ruled out the two simple hypotheses with evidence: the read side IS wired (`_ = ReplayPersistedQueueRequestsOnLaunchAsync()` runs on every launch, whole feature landed in one commit `5f26d353` so it was never "unwired"), and there is NO path mismatch (both sides use `%AppData%\Roaming\BuildConsole\pending-update-queue.json`; the file wasn't on disk at investigation time, consistent with the replay having run and deleted it). Real root cause = cause #3, silent-swallow: the replay POSTs each persisted request to the dev api-server in the first moments of relaunch (the dev server auto-sleeps ~every 15 min and is routinely cold exactly then), then DELETED the spillover file UNCONDITIONALLY right after the attempt (old line 200) — so a POST that failed against a cold/asleep server silently discarded the build forever, which is precisely the reported symptom. And `ActivityLog` is in-memory only (raised to a panel, wiped on every restart, and drops lines when no subscriber is attached), so a past failure left zero forensic trail — why it needed another live bug report. Fix (`23b71230`): (1) a request is removed from the file ONLY once it genuinely queues (a real 2xx) — anything that didn't queue (thrown/cold-server or a non-2xx) is KEPT and the file rewritten with just the remainder (`SavePersistedQueueRequests`), so the next launch retries it automatically and nothing is ever silently dropped; if the trimmed-file rewrite itself fails, the ORIGINAL file is left intact (a harmless duplicate next launch beats a lost build). (2) A small bounded per-item retry (`TryReplayOneAsync`, 3 attempts 4s apart) gives a cold-starting server a chance to wake on this very launch. (3) Durable, append-only diagnostic log `pending-update-queue.log` (next to the JSON under `%AppData%\BuildConsole\`, via `PendingUpdateQueueDiag` — best-effort, lock-guarded, never throws, still mirrors to the live `version-update` panel): logs whether the spillover file was found on launch, its RAW contents (captured before parse so a corrupt file is still visible), the parsed count, each item's real per-attempt HTTP status/body or exception, and the final keep-or-delete decision — so a recurrence is diagnosable straight from the file. Also logged: the write-side interception, the previously-silent count==0/corrupt-file delete, and the not-configured leave-in-place path. Verified in an isolated worktree off HEAD (`dotnet build -c Release` — 0 errors, only the 4 pre-existing CS0067 warnings) because two FOREIGN untracked in-progress files in the live tree (`Controls/StartupLoadingView.xaml*`, another session's WIP) don't yet compile; committed my one file only via explicit pathspec, foreign `MainWindow.xaml`/StartupLoadingView left untouched. No DB work. NOT live-verified through a real pending-update deploy restart here — Shane should confirm on a genuine Queue-during-pending-update that after the restart the build appears in the live queue, and that `%AppData%\BuildConsole\pending-update-queue.log` shows the found/contents/re-queue-outcome trail. | Pending-update queue re-queue durability + durable diag log | `23b71230` |
| 2026-08-14 | ✅ DONE — Build Watch live Task Checklist side panel. Read the real event-stream render (`BuildWatchWindow` AppendEventCard / ApplyInteractiveEvent) in full first, then add a side panel that extracts a live task checklist from the agent's own reported progress: detect real checklist markers (☐/☑/✓/✔ or markdown `- [ ]`/`- [x]`), add each newly-seen unchecked item as pending, mark it done with a green ✓ when the agent later reports the same item complete, matched by straightforward text-similarity with honest notes on where it can misfire. Picked up from an uncommitted working tree (new `Controls/TaskChecklistModel.cs` + `Services/ChecklistExtractor.cs`) and committed alongside the Build Watch live-render freeze fix (same file, same commit). `dotnet build --no-incremental` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm the checklist panel populates and flips items to a green check as the agent reports them done. | Build Watch Task Checklist side panel | `21bceb6f` |
| 2026-08-14 | ✅ DONE — Git Board GitHub traffic reduction (#876 reopened). Shane: "heavy GitHub API traffic for no reason", repeatedly crossing the 5,000/hr REST limit. Read the real polling paths in full first. Findings: (1) the Git Board (LeftSidebar Issues view) 20s `_pollTimer` was gated on the Issues view being visible (#863) but NOT on window visibility, so it hit GitHub every 20s all day even while BuildConsole was minimized/hidden — per cycle: `ListBoardIssuesAsync` (GraphQL, separate points budget), `GetMilestones` (REST, already 5-min cached #876), and `EnrichBlockedStatusAsync` (REST, one call PER open issue, already 3-min throttled #876); (2) BuildQueuePanel fired three `gh`-CLI GitHub reads (in-flight/To-Do/completed) unconditionally every 60s regardless of visibility (~180 REST calls/hr all day); (3) no conditional requests anywhere — every REST poll re-fetched full bodies and counted against the limit even when nothing changed. Fix: only poll while the host window is genuinely on screen (`Window.GetWindow(this)` present + IsVisible + not Minimized) — applied to the Git Board timer AND the three BuildQueuePanel gh timers (initial immediate paint on Initialize kept); Git Board background GitHub refresh slowed from 20s to a real 2-min cadence (`GitBoardBackgroundPollInterval`) decoupled from the 20s chat tick, with tab-switch (SwitchView) and the manual Refresh button still repainting immediately; ETag conditional requests added to every REST GET read (milestones, per-issue blocked_by sweep, sub-issues, single issue, comments, numeric search) via a new static ETag cache + `GetConditionalAsync<T>` in GitHubApiClient (static because a fresh `new GitHubApiClient(pat)` is built per poll) — an unchanged resource now returns 304, which GitHub does not count against the rate limit, so a steady-state poll is largely free; GraphQL board fetch left as-is (POST, cannot use ETags, separate points budget). Logging: GitHubApiClient tracks REST GETs sent vs. 304-not-modified and logs a before/after summary on the `git-board.traffic` channel each background poll, plus a one-time startup line stating old (20s, even minimized) vs. new (2-min, visible-only, ETag) cadence. Verified: built clean in an ISOLATED worktree at HEAD overlaid with only my 3 changed files (the shared tree had a concurrent session's in-progress StartupLoadingView that does not compile) — `dotnet build -c Release` 0 errors, only the 4 pre-existing unrelated CS0067 warnings. HONEST boundary: not exercised in a live running GUI here (WPF, no headless GUI, and the shared tree wouldn't build) — Shane should confirm on a real run that the git-board.traffic log shows the drop and the board still updates promptly when the Issues tab is focused. | Git Board rate-limit reduction | `74b467ee` |
| 2026-08-14 | ✅ DONE — Non-blocking dialog/notification engine (ToastEngine): one reusable, dark-themed, non-blocking toast/notification system generalized from DeviceCodeWindow's proven floaty pattern (borderless always-on-top window, center mid-top of the work area, never ShowDialog / never steals focus / no system alert sound, multiple stacking toasts with a soft cap), a simple static API (ToastEngine.Show(title, message, kind)), and migration of the scattered genuinely-informational MessageBox.Show() calls to it (leaving the crash-handler and in-modal form-validation MessageBoxes as intentional blocking exceptions). | Non-blocking toast/notification engine | `20a82da0` |
| 2026-08-14 | ✅ DONE — What's New: filter out session-bookend commits. Read the real What's New implementation (VersionInfo.GetNewCommitTitles + MainWindow.InitWhatsNewAsync + HomeView.RenderWhatsNew) in full first. Real problem: raw bookend/tracking commits (BUILD_LOG:/PLATFORM_BUILD: IN FLIGHT/DONE rows, and the looser Bookend:/bare IN FLIGHT: phrasings seen in real history) were showing up alongside the real feature commit for the same change, creating duplicate noise for a single real change. Fix: new VersionInfo.IsBookendCommitTitle (regex covering all observed bookend title shapes) filters these out inside GetNewCommitTitles before the list reaches the UI; MainWindow's more-count calc reworked to derive from totalNew/max-cap directly instead of post-filter titles.Count, so filtered-out bookends no longer inflate the "+N more" indicator. Pure filtering logic, no wire logging needed. Build clean (0 errors, only the pre-existing unrelated CS0067 warnings). No DB work. NOT live-verified in a running GUI here — Shane should confirm the Home screen What's New list shows only real feature titles next launch. | What's New bookend-commit filtering | `54da9707` |
| 2026-08-14 | ✅ DONE — Focus Mode: a real behavioral-design system sitting on top of every panel (not an isolated feature). Shane: "I have down time waiting for the builds... side quests end up 'more fun' and I lose track of time... I need a real focus mode... NOT another window... hide things, only show me what I'm working on... during downtime give me something quick/easy then refocus me back... keeping track of what I was doing so I can pick it right back up." Five pieces shipped as one system (new-file-heavy: FocusModeService + FocusModels + FocusModeBar + MainWindow.FocusMode partial; surgical panel hooks): (1) **Active Milestone declaration** — reuses the real Git-Board GitHub milestones (LeftSidebar.CurrentMilestones), one active at a time, persisted in its own focus-mode.json; (2) **Hard global single-window filter** — while active, RenderIssuesTree (Git Board), PopulateChatsTree (Chats), RenderQueue (Build Queue) and HomeView's Running/Done/Left-off all hard-hide anything not under the active milestone (resolved via GitBoardIssue.MilestoneNumber; chat/queue join by GitHub #), genuinely gone, no new window, with an honest "N hidden" affordance; (3) **Downtime detection + quick tasks** — a queue-managed build actively running (QueueWatcher.RunningCount / running snapshot) + Shane idle (new Win32 GetLastInputInfo, ≥60s) surfaces a short bounded list of quick ON-milestone tasks only (Shane-To-Do issues + small non-epic issues), never off-milestone drift; (4) **Auto context save/restore** — snapshots the active tab/pane on downtime start, snaps back on the real QueueWatcher.BuildFinished signal OR a taken/dismissed quick task (re-selects the pre-detour tab, reopening it if closed); (5) **Game layer over REAL numbers** — honest closed/total from GitHub's own milestone counts, ETA via the SAME least-squares math as the Claude usage-meter (UsageProjection.LeastSquaresSlopePerHour), points + tasteful earned achievements (first close / halfway / home stretch / cleared / To-Do inbox-zero), Reticulating-Splines spirit. Logged throughout on the new `focus-mode` ActivityLog channel (activate/deactivate, downtime detected/ended, context saved/restored, suggestion shown/taken, points/achievements). Home refresh path uses force:false so the filter re-render never triggers a gh/GitHub call (respects the manual-refresh-only rule). Compile-verified in an isolated worktree (0 errors); committed isolated via commit-tree so no concurrent session's work was reverted (concurrent MainWindow.xaml.cs BtnUsageRefresh change preserved). NOTE: my earlier ⏳ IN FLIGHT bookend row (commit 2c53e3a8) was clobbered off BUILD_LOG.md by a concurrent session's stale-base rewrite (2ec593b1) — this DONE row is re-appended in its place. | Focus Mode (active-milestone global filter + downtime + game layer) | `c5600997` |
| 2026-08-14 | ✅ DONE — Assign Chat to Epic. Read the real BoardChat data model (ClaudeUrl/IssueGithubNumber/EpicId) and the real Git Board epic right-click menu in full first. Shane has real chats he can't currently associate to Epics through any manual path. Right-click an epic in the Git Board -> "Assign Chat to Epic...", dialog (new AssignChatToEpicDialog) offers pasting a chat URL directly or an "Assign current chat" button that pulls the URL off whatever chat tab is actually focused — a new LeftSidebar.GetActiveChatUrl callback MainWindow wires from EditorTabs.SelectedItem, keyed off the tab's Tag being a BoardChat (plain web tabs use a bare string). Submit extracts the /chat/<uuid> conversation id, resolves the epic's real bt_epics row by GitHub number, and posts through the same real BuildTrackerApiClient.LinkChatToEpicAsync (POST /chats/ingest force:true) every other chat-epic assignment already goes through, so the Chats panel and every other epic-aware view pick it up immediately — no separate sync step. Logs the real association (chat URL, conversation id, epic number, bt_epics id) on the git-board.assign-chat channel, including abort/failure paths (no recognizable URL, epic not registered, API failure). No DB schema change, no SQL needed. `dotnet build` clean, 0 errors, only the 4 pre-existing unrelated CS0067 warnings. NOT live-verified in a running GUI here — Shane should confirm the right-click menu item appears on epics, both the paste and "Assign current chat" paths populate the field correctly, and a successful assign shows up immediately in the Chats panel under the target epic. | Assign Chat to Epic | `9eb007e5` |
| 2026-08-14 | ✅ DONE — TestRunnerWindow non-intrusive run lifecycle: stop opening the Test Runner maximized/focus-stealing for a routine run; run it hidden (parked off-screen in Normal state, never activated — NOT minimized, because a minimized/occluded WebView2 can't be screenshotted per UiTestExecutor's ScreenshotTimeoutMs) so uiStep captures still work; on a genuinely clean run (no failed steps AND #975's exact `items.Any(i => i.NeedsReview)` condition false) auto-close the window; on a run that needs attention (a real step failure OR #975's needsReview firing) keep the window and surface a non-blocking clickable ToastEngine (#24) toast that restores the window / opens the deferred #975 review dialog. Applies to every trigger (Play Test, double-click, shaneapp://runTest, #898 remote) since it's the window's own default. Log the outcome (auto-closed clean / surfaced toast) per run on testing.results-panel. | TestRunnerWindow non-intrusive run lifecycle | `c962ee7d` |
| 2026-08-14 | ✅ DONE — build-complete deploy trigger PowerShell helper. Read #911's real landed `POST /api/admin/deploy/build-complete` (auth `requireAdminOrIngestToken` = Bearer BUILD_TRACKER_INGEST_TOKEN; body `{schemaSql?}`; returns `{ok,steps,restarting,pollUrl,note}` and — per its own comments — DEFERS/DETACHES the restart (kills PID 1 behind a `sleep 2` so the HTTP response flushes first) so the connection drops and it never waits) and #805's `GET /api/internal/deploy-status` (unauth, returns `{commitHash,timestamp}` from the module-level versionInfo recomputed at each process start) in full first. Both mount under `/api` (routes/index.ts → app.ts `app.use("/api", router)`). New `desktop/BuildConsole/trigger-deploy-and-wait.ps1`: (1) GET deploy-status to capture the OLD short commit hash BEFORE triggering; (2) POST build-complete with the Bearer token (apiBaseUrl+ingestToken default from the same `scripts/build-queue-watcher.config.json` BuildConsole reads, token also falls back to `$env:BUILD_TRACKER_INGEST_TOKEN`; optional -SchemaSqlFile/-SchemaSql passthrough for Shane's CREATE/ALTER/INSERT SQL, which #911 hard-filters), tolerating the documented connection drop; (3) poll deploy-status every 3s (unreachable=restarting=keep waiting) until commitHash CHANGES from the captured value = real proof the new code is live, not just accepted, or a 300s timeout; (4) print a green DEPLOY CONFIRMED block (old hash → new hash → real elapsed) on success, or a red exit-1 block that says plainly the server is NOT confirmed on new code (nothing to pull, or restart didn't complete) so tests aren't run against stale code. Also exits 1 on a real build-complete failure (git pull diverged / schema SQL rolled back → no restart scheduled). AGENT_PROTOCOLS.md gains a section 4 documenting all of the above (matching the existing shaneapp:// section style + endpoint/source table). No server logging added — #911/#805 already log on `admin.deploy`/`testing.deploy-poll`. No DB work. PowerShell parse-verified (`[Parser]::ParseFile` clean). NOT run end-to-end here (needs a live dev server + a real pending push) — Shane runs it after a build pushes to confirm the pull+restart genuinely lands. No Git # (not filed as an issue). | build-complete deploy trigger script | `76f066a9` |
| 2026-08-14 | ✅ DONE — Build Watch checklist from structured Task tool events. Read #28's real checkbox-text detection (`ChecklistExtractor` markers → `TaskChecklistViewModel.Shared` similarity-matched rows) and #1009's real stream-json parsing (`QueueWatcherService.ParseInteractiveEvents` → `InteractiveEvent` {ToolName/ToolUseId/InputJson/…} → `BuildWatchWindow.ApplyInteractiveEvent`/`AddInteractiveToolCall`/`FillToolResult`) in full first. Premise confirmed: TodoWrite is a disabled legacy path in current Claude Code; the live mechanism is the structured Task tools, whose tool-call events carry real task data (subject/description, an explicit id, a status) as JSON `input` fields — not text to pattern-match. Extended the EXISTING #1009 event pipeline (no second parser): in `AddInteractiveToolCall`, a `TaskCreate` tool-call now ALSO adds a pending row to the #28 side panel via new `TaskChecklistViewModel.AddStructuredTask` using the task's own `subject` (fallback `description`→`activeForm`), and is remembered by its tool_use id in a new per-slot `PendingTaskCreatesByUseId`; when that call's `tool_result` lands (`ApplyInteractiveEvent`→new `TryResolveStructuredTaskResult`), the assigned "Task #N" id is parsed from the result text and bound onto the row (new `ChecklistItemViewModel.TaskId`/`AssignTaskId`); a later `TaskUpdate` (whose `input` carries `taskId`+`status`) flips that EXACT row to a green ✔ on `completed`/`done` (or drops it on `deleted`) via new `MarkStructuredStatus`+`FindByTaskId` — an explicit real-id match, not #28's text-similarity guess, so it's materially more reliable. The Task tool call still renders as an ordinary tool chip too (additive). #28's free-form ☐/☑/✓/✔/`- [ ]`/`- [x]` `ScanForChecklist` detection stays fully wired on `AssistantText`/`TurnResult` as the fallback for agents that still print text checklists without the Task tools, so nothing regresses. New per-slot dict reset alongside `ToolCallsById` on both `OccupySlot` and `ClearSlot`. No new logging (reuses #28's existing `build-watch` channel), as specified. No DB work. `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. Files: `Controls/TaskChecklistModel.cs`, `BuildWatchWindow.xaml.cs`. Committed only my 2 files (foreign concurrent-session dirty files — .claude/settings.json, CLAUDE.md, desktop/test-tester.ps1 — left untouched). NOT GUI-verified here (WPF, no headless run) — Shane should confirm on a real queued interactive build that uses the Task tools: creating tasks populates the checklist panel with their subjects as pending rows, and marking a task completed flips its row to a green check. Local task (not a filed GitHub issue). | Build Watch structured Task-tool checklist | `1f752d3e` |
| 2026-08-14 | ✅ DONE — Critical-step abort for uiSteps: optional per-step `"critical": true`. Read the real step-execution loop first (`UiTestExecutor.RunAsync`'s `for` over `steps` → `ExecuteStepAsync`, whose failure only Emits a `STEP N WARN` and falls through to the next step). Real motivating log: login failed at step 2 (`#email` — Element not found in DOM) and step 3 (`#password`), so login never succeeded, yet steps 5–13 (nine `expect` steps) each still ran and burned their full 5s `ExpectPollTimeoutMs` waiting for content that could only appear post-login — ~45s of pure waste, nine downstream failures that are all really the one root cause. Fix: a uiStep may now set `"critical": true` (same optional-field convention as maxDurationMs/extract/textContains — `ManifestUiStep.Critical` + JSON parse in `TestManifest.cs`, mirrored onto `AutomationAction` in `LeftSidebar.xaml.cs`, carried through `RunManifestAsync`'s uiStep→AutomationAction map). When a critical step fails, `RunAsync` HALTS the whole run immediately — sets `UiTestRunResult.Aborted`/`AbortReason`/`StatusText` naming which step failed and why, emits a distinct `RUN ABORTED` (ERROR) telemetry level + a `testing.ui-executor` "RUN ABORTED" log line (separate from an ordinary per-step WARN), and returns without executing the remaining steps (the `finally` still restores the viewport). Steps NOT marked critical keep today's exact log-the-WARN-and-continue behaviour — opt-in per step, not a global change, since multiple independent failures in one pass is sometimes useful. `RunManifestAsync`'s uiSteps summary log now appends the abort reason when a run aborted. `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing unrelated CS0067 LeftSidebar warnings. Committed only my own files via an isolated worktree on origin/main (the main working tree was polluted with a concurrent session's dirty MainWindow.xaml.cs/BUILD_LOG.md/etc — left untouched). HONEST VERIFICATION BOUNDARY: the abort is BuildConsole (WPF) code and `shaneapp://runTest` is couriered to Shane's RUNNING deployed ShanesBuild, which does NOT yet contain this change — so the abort behaviour cannot be live-exercised from here until Shane deploys (Shane controls deploys). Verified instead by compile-clean build + code walkthrough; the retrofit manifests remain valid JSON and the currently-deployed build safely IGNORES the unknown `critical` field (defaults false), so nothing regresses pre-deploy. Manifest retrofit tracked in the paired PLATFORM_BUILD.md row. No DB work. No Git # (not filed as an issue). | Critical-step abort (uiSteps) | `602341fb` |
| 2026-08-14 | ✅ DONE — Queue Build dev-server-nap HTML error dialog: the injected chat "Queue" button's failure path (`MainWindow.ChatWv_WebMessageReceived`'s `BT_QUEUE_BUILD` handler) was dumping the raw HTML body of Replit's own "wake up the app" placeholder page straight into the `ToastEngine.Error` dialog whenever the dev api-server was asleep (#931) and returned that page instead of JSON. Added `MainWindow.IsLikelyDevServerNapHtml` (Content-Type + `<!DOCTYPE`/`<html` body sniff) to `BT_QUEUE_BUILD`'s failure path: on a detected nap-HTML response, logs the real status/content-type on the `api` channel (distinct from a genuine API error), waits 8s, and retries the POST once — falls through to the normal success handling if the retry lands 2xx, or shows a short human toast ("Server unreachable, it may be asleep — try again in a moment.") instead of raw markup if the retry also comes back HTML. A genuine non-HTML API error still shows its real body unchanged. `dotnet build -c Release --no-incremental` clean, 0 errors (only the 4 pre-existing CS0067 warnings), verified in an isolated worktree off origin/main (shared checkout was polluted by concurrent sessions' uncommitted MainWindow.xaml.cs edits elsewhere in the same file). No DB work, no test-manifest scope (BuildConsole tooling). NOT GUI-verified here (WPF, no headless run, and no way to force the real dev server to nap on demand) — Shane should confirm next time the dev server naps that clicking "Queue" in a chat shows the short human message (or silently retries through) instead of a wall of raw HTML. No Git # (not filed as an issue). | Queue Build dev-server-nap error handling | `9bc4a413`
| 2026-08-14 | ✅ DONE — Build Watch original-instructions reveal + Build Queue "Needs Attention" section. Part 1: a small tucked-away FileText-icon button in each Build Watch slot's ChatSessionPane header (hidden until there are instructions via new `HasInstructions`, collapsed by default — same spirit as the #1012 collapsed tool chips and the #42/#56 per-build checklist column) pops the build's full original prompt (`QueueItem.Prompt`) in a scrollable, selectable read-only panel. New `ChatPaneViewModel.OriginalInstructions`/`HasInstructions`; wired in `BuildWatchWindow` OccupySlot (set from `item.Prompt`) / ClearSlot (clear). Part 2: durable fallback for #54's needs-attention test toast (which auto-dismisses after a few seconds and gets missed) — new "Needs Attention" collapsible tile in the Build Queue panel alongside the #874 To-Do tile (same `QuietTile` collapsed-by-default + `PeachBrush` "announce itself" treatment; mutually-exclusive expand with To-Do/Completed). `MainWindow.ApplyRunOutcomeToRunnerWindow` now ALSO calls `BuildQueuePanel.AddNeedsAttention` at the same moment it fires the toast, for every real step-failure OR #975 screenshot-review-needed outcome; the row persists until Shane addresses it — clicking the toast OR the section row both run the same open action (`RestoreToForeground` + the deferred #975 review dialog) AND clear the item. Keyed by issue+feature so a re-run updates its row instead of duplicating. Logged on a new `testing.needs-attention` ActivityLog channel on add and on clear/address. Built in an isolated worktree off origin/main (local main was 1-ahead/5-behind with a polluted working tree, and origin/main had already moved the checklist into `ChatSessionPane` per #42/#56, so the local working-tree reads were stale — re-read + edited the real origin/main versions). `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings. Committed only my 6 files (foreign concurrent dirty files left untouched). No DB work. NOT GUI-verified here (WPF, no headless run) — Shane should confirm: the header document icon reveals a build's real prompt; a failing/review test leaves a persistent Needs Attention row that clears when opened from either the toast or the row. Out of the msp-portal/shane-mccaw-consulting test-manifest scope (BuildConsole tooling). No Git # (not filed as an issue). | Build Watch instructions reveal + Needs-Attention section | `95767822` |
| 2026-08-14 | ✅ DONE — "Queued for Restart" list refresh-lag fix (local #6 spillover persistence + local #25 grouping-node UI). Read both in full first: the write side (`MainWindow.PendingUpdateQueue.cs`) persists each intercepted Queue click to `%AppData%\BuildConsole\pending-update-queue.json` PROMPTLY (synchronous `File.WriteAllText`, no batching), and the UI side (`BuildQueuePanel.RenderQueuedForRestartGroup`) reads that file FRESH off disk on every render — so neither a stale in-memory copy nor a slow write was the cause. Real root cause = the render TRIGGER: the "🔄 Queued for Restart" group is drawn only as a side-effect of `RenderQueue`, and `RefreshAsync`'s #821 anti-flicker signature guard SKIPS `RenderQueue` whenever the LIVE queue snapshot (`_lastItems`) is unchanged. An intercepted Queue click during a pending update changes ONLY the spillover file, never `_lastItems` — so the group never repainted until some unrelated live-queue change happened to trip the guard (Shane's "takes a really long time to update"). On top of that, the intercept path in MainWindow's `BT_QUEUE_BUILD` handler never called `RefreshAsync` at all (only the normal, non-pending queue path did). Same bug class as the #40/#41 Focus-Mode signature-guard miss. Fix (3 parts): (1) fold `GetPersistedQueueDisplayItems()` into `RefreshAsync`'s render signature so a spillover-file change forces `RenderQueue` even with an unchanged live queue — the 15s poll now reflects spillover changes; (2) call `BuildQueuePanel.RefreshAsync()` immediately after a successful intercepted persist so a just-queued item shows at once, not on a later unrelated poll; (3) durable lag logging — new `MainWindow.LogQueuedForRestartRender` writes a real UI-refresh timestamp to the SAME `pending-update-queue.log` the file-writes already timestamp, guarded by a per-set signature (`_lastRestartGroupSignature`) incl. the transition-to-empty, so write→display lag is computable from the log alone (ActivityLog is in-memory-only and useless across the restart this feature spans). Verified compile-clean: `dotnet build -c Release --no-incremental`, 0 errors (only the 4 pre-existing CS0067 LeftSidebar warnings), in an isolated worktree off origin/main (local main had diverged; a concurrent session had just landed the Needs-Attention section on the same file). NOT GUI-verified here (WPF, no headless run; `shaneapp://runTest` hits the deployed ShanesBuild, not this worktree) — Shane should confirm on a real run that clicking Queue during a pending update makes the item appear under "Queued for Restart" within ~1s, and that the group clears promptly after the post-restart replay. No Git # (local #6/#25 are BuildConsole-local tracker items, not GitHub issues — no label sync). | Queued-for-Restart list refresh-lag fix | `94ba6ae6` |
| 2026-08-14 | ✅ DONE - Chats panel now hides CLOSED epics (and their nested chats), matching the Git Board's #839 real-GitHub-state convention ("done done get out of my view"). Read the full Chats tree (`RenderChatsTree`/`BuildChatEpicHeader`/`BuildChatLeaf` in `Controls/LeftSidebar.xaml.cs`) and the server's `GET /extension/board` first. Root cause of the leak (Shane's example: closed EPIC "Copilot Readiness Scroll..." still showing): the server already filters `epics` to `bt_epics.status != "closed"`, but that LOCAL status lags real GitHub state, so a GitHub-closed epic whose local row still reads open passes the filter and renders with its real title. Fix in `RenderChatsTree`: an epic group is hidden entirely (nested chats too) on EITHER of two reliable signals - (A) its EpicId resolves to no server-returned epic (server already dropped it as closed; these used to show as orphan "Epic #<id>" rows), or (B) the epic IS returned but the SAME live open-issue set the Git Board itself fetched (`_lastBoardIssues`, OPEN-only) shows its GitHub issue is no longer open (catches the stale-local-status case, e.g. #343). Fail-open when no live board data is loaded yet (`_lastBoardIssues` empty) so a chat is never wrongly hidden for lack of knowledge; the Chats tree also re-renders on each Git Board refresh so a just-closed epic drops immediately instead of lingering to the next Chats poll. Logs the hidden count on a new `git-board.chats` ActivityLog channel. Build-verified compile-clean in an isolated worktree off HEAD (`dotnet build -c Debug`): 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings - the shared checkout's main working tree was polluted by a concurrent session's in-progress MainWindow.xaml.cs/LocalSqlExecutor.cs/BuildTrackerConfig.cs/AGENT_PROTOCOLS.md edits (their executeSql connection-string work), left untouched; committed only my own file. NOT GUI-verified here (WPF, no headless run; `shaneapp://runTest` hits the deployed ShanesBuild, not this change) and `shaneapp://executeSql` could not confirm #343's DB row this session (BuildConsole reports no local Postgres connection string configured - the very thing the concurrent session is mid-fixing) - Shane should confirm on a live board that a closed epic disappears from Chats. No DB work. Local #-tracked (no GitHub issue). | Chats panel closed-epic filter | `d8ff6475` |
| 2026-08-14 | ✅ DONE — Focus Mode Build Queue over-filtering fix (local #46 follow-on). Read #46's real landed active-milestone filter (`FocusModeService.IsIssueInFocus` + `BuildQueuePanel.RenderQueue`'s focus block) and the full milestone/epic/issue data model first. Symptom (with screenshot): after #46, the Build Queue's Queued/Running list shows "Nothing queued or running" even when real builds tied to the active milestone are running — over-filtering (the opposite of #46's pre-fix no-filter over-showing). Root cause CONFIRMED against live GitHub GraphQL (queried `parent { … milestone { number } }` on the recent open issues): Shane milestones the EPIC, not each sub-issue — e.g. sub-issues #1031/#1029/#1027/#1023 all carry `milestone: null` while their parent epic (#803/#530) carries the real milestone (`{16, "F. Internal Tooling & Ops"}`). #46 filtered on the sub-issue's OWN (null) milestone, so a queued build's `IsIssueInFocus` computed `null == activeMilestone` → false → hidden, excluding every genuine on-milestone build. The association chain build → GithubNumber → parent epic → epic's milestone was never traced. Fix: (1) `GitHubApiClient` — fetch `parent { number milestone { number } }` in the board GraphQL (same single query, zero new API calls, within the manual-refresh-only rule) and carry `ParentNumber`/`ParentMilestoneNumber` on `GitBoardIssue`; (2) `FocusModeService` — new `DescribeIssueFocus` is the single source of truth that traces issue → its own milestone, else → parent epic → epic's milestone, returning (shown, resolvedMilestone, human path); `IsIssueInFocus` and `IsIssueInActiveMilestone` delegate to it, so the whole queue-derived surface (Queue, In-Flight, To-Do, Completed tiles, Home rollup, immersive band) now correctly recognises sub-issue builds. Still errs toward SHOWING for genuinely unattributable items (null/local number, an epic with no milestone of its own, or a cold board map) so an actively-running build is never hidden; only a positive off-milestone resolution hides. (3) `BuildQueuePanel.RenderQueue` — per-queue-item focus-mode log (item → resolved milestone path → SHOWN/HIDDEN vs active milestone), gated to focus-active renders (RenderQueue only fires on a real data/filter/focus change, not the 15s poll) so a future over/under-filtering bug is diagnosable straight from the feed. (4) `LeftSidebar` — folded parent fields into the board-refresh signature so a re-parent / epic-milestone change (which leaves a sub-issue's own null milestone untouched) still trips the guard and re-feeds the focus map via `UpdateBoardSnapshot`. `dotnet build -c Release --no-incremental` clean: 0 errors, only the 4 pre-existing CS0067 LeftSidebar warnings (StartRecordingRequested/StopRecordingRequested, unrelated). Verified the ROOT CAUSE and fix logic against real GitHub data (not a live UI run): a build for #1031 (parent #803, epic milestone #16) resolves via the epic to #16 == active #16 → SHOWN, where before it was `null == 16` → HIDDEN. NOT live GUI-verified (WPF, no headless run; a new BuildConsole logic path runs Shane's deployed ShanesBuild via runTest, not this worktree) — Shane should confirm on a live run with a milestone focused that sub-issue builds under it now appear in the Queue. No DB work (GitHub GraphQL, no SQL). Out of test-manifest scope (BuildConsole is Shane's own WPF tooling, not customer-facing product). Local #46 (not a filed GitHub issue — no label sync). | Focus Mode Build Queue over-filter fix | `3f93fbaf` |
| 2026-08-14 | ✅ DONE — Live checklist mark-done detection + floating-band placement fix. Read #28's ChecklistExtractor (narrow ☐/☑/✓/✔ + "- [ ]"/"- [x]" markers → TaskChecklistViewModel per-build tracking with similarity-matched done-flips), #42/#56's per-build ChatSessionPane checklist column, and #46's (commit 7e000789) floating FocusModeBar "Live checklist" band in full first. TWO real bugs fixed. (1) MARK-DONE NEVER FIRED ("GH #753 0/10"): 10 pending "- [ ]" rows were detected but none ticked over. Root cause — the done-glyph classes in ChecklistExtractor were only [✓✔] (U+2713/2714) and the markdown box [ xX]; they OMITTED ✅ (U+2705, the green-check EMOJI agents overwhelmingly use when they re-render a completed step, e.g. "- ✅ wire up auth" / "✅ wire up auth"). ✅ is not ✓/✔, so a completion re-render fell through as Marker.None and IngestDone was never even invoked → the pending row stayed pending forever (exactly the "only ever adds pending, never updates state" symptom). Fix: added ✅ to the leading-mark class and the markdown box class (so "- [✅]"/"- [✓]" also read done), tolerating a trailing U+FE0F variation selector, and taught CleanLabel to strip a trailing ✅/VS — so "- [ ] wire up auth" and "- ✅ wire up auth" normalize to the SAME label (Similarity 1.0 → confident flip). Verified by exercising the exact three regexes against real sample lines via .NET: "- [ ] x"→Pending, "- ✅ x"/"✅ x"/"- [✅] x"→Done with identical cleaned text, "  - ✓ nested"→Done, plain prose/plain bullets→None (no false positives). Also rewrote IngestDone as a SINGLE logged pass (decision semantics byte-identical: flip best pending ≥0.60, else idempotent already-done ≥0.82, else new done row) that logs EVERY real match attempt — incoming done-text vs each existing row, the score, both thresholds, and the outcome — plus FLIP/NOOP/NEW decisions, on a new build-watch.checklist ActivityLog channel; MarkStructuredStatus (the TaskCreate/TaskUpdate path) now logs its id hits/misses too. A future "why did an item never tick over?" is now answerable straight from the log instead of guessed at. (2) WRONG PLACEMENT: the "Live checklist" element floating at the top of the window (above the Chats sidebar's own header) was FocusModeBar's ChecklistBand — a full-width aggregate that duplicated AND misplaced the per-build checklist #42/#56 already renders inside each build's OWN Build Watch square (ChatSessionPane's right-hand column; the screenshot's "GH #753  0/10" is exactly that band's BuildChecklistColumn header). Removed the band from FocusModeBar.xaml + all its render code-behind (RefreshChecklist/BuildChecklistColumn/BuildChecklistRow, the shared-tracker subscription, the now-unused System.ComponentModel using) so a build's steps appear ONLY in that build's own pane, never floating in the Focus strip. Detection/tracking is 100% unchanged — only the stray render was dropped; the immersive full-screen checklist (a separate surface) is untouched. Files: Services/ChecklistExtractor.cs, Controls/TaskChecklistModel.cs, Controls/FocusModeBar.xaml(.cs). `dotnet build -c Release --no-incremental` clean: 0 errors, only the pre-existing CS0067 LeftSidebar warnings (none from my files). NOT live-GUI-verified here (WPF, no headless run) — and per the shaneapp://runTest boundary, a NEW BuildConsole detection change can't be live-triggered anyway (runTest exercises Shane's DEPLOYED ShanesBuild, which still has the old glyph classes), so this verifies on rebuild+deploy. Shane should confirm on a real running build that an agent's ✅-completed steps now tick to green in that build's own pane and that no "Live checklist" panel floats above the Chats sidebar. Committed only my 4 files by pathspec; concurrent sessions' dirty files (api-server monitor-executor, msp-portal remediationLiveGuide, the onedrive-sync-errors-753 SQL migration) left untouched. No DB work. No Git # (BuildConsole-local tooling — "GH #753" was an example build label, not a filed issue — no label sync). | Live checklist mark-done + placement fix | `bd3917bc` |
| 2026-08-14 | ✅ DONE — Chats tree epic-row: show real GitHub issue number alongside title. Read `RenderChatsTree`/`BuildChatEpicHeader`/`BuildChatLeaf` in `Controls/LeftSidebar.xaml.cs` in full first. The epic header already truncates long titles via `TextTrimming.CharacterEllipsis` and appends a never-trimmed " (N)" chat-count `TextBlock` after the title block; the real epic GitHub issue number just wasn't threaded through at all. Fix: `RenderChatsTree`'s `groups` list now carries `GithubNumber` (from `epicById[...].GithubNumber`, null for the synthetic "Unlinked" bucket) alongside `Title`/`Chats`; `BuildChatEpicHeader` takes a new `int? githubNumber` param and, when present, inserts a " #NNN" `TextBlock` (same `Subtext0Brush`/11px styling as the count suffix) between the trimmed title and the count — matching the same "#NNN" real-issue-number convention already used by Git Board rows and Build Watch slot headers, and staying visible even when the title itself is ellipsis-trimmed. Pure display change, no wiring/logging needed. `dotnet build --nologo` clean: 0 errors, only pre-existing CS8602/CS0067 warnings unrelated to this change. NOT live-GUI-verified here (WPF, no headless run) — Shane should confirm visually that Chats panel epic rows now show "EPIC title... #NNN". Committed only my 1 file; concurrent sessions' dirty files (api-server/msp-portal auth+PDF work, lib/db schema) left untouched. No DB work. No Git # given for this task — no label sync. | Chats epic-row real issue number | `411183f9` |
| 2026-08-14 | ✅ DONE — Manifest Viewer Raw JSON view had no working save: `IsReadOnly="True"` on the AvalonEdit control and no save code anywhere, reachable via the tree's "Edit" action which actually opened the generic Monaco/WebView2 tab (no disk-save bridge for any file). Read `ManifestViewerWindow.xaml(.cs)` and `LeftSidebar.xaml.cs`'s `BuildManifestLeafContextMenu` in full first. Fix: made `JsonEditor` editable; Ctrl+S (window-level `PreviewKeyDown`, scoped to when the JSON panel is visible) and a new header "💾 Save" button (visible only in JSON view) both call `SaveJsonToDisk()`, which validates via `JsonDocument.Parse` before writing — invalid JSON is rejected with a `ToastEngine.Error` and never written; a successful write shows `ToastEngine.Success` (the app's standard non-blocking feedback pattern). Every attempt (validation-blocked, write-failed, or saved) logs on a new `testing.manifest-edit` ActivityLog channel. Repointed the manifest leaf context menu's "Edit" item from `FileSelected` (Monaco dead end) to `OpenManifestViewerFromPath` so Edit now lands on this same editable view. `dotnet build -c Release --no-incremental` clean: 0 errors, only pre-existing CS8602/CS0067 warnings unrelated to this change. NOT GUI-verified here (WPF, no headless run) — Shane should confirm typing in Raw JSON, Ctrl+S / the Save button writes the file, a deliberately broken brace shows the invalid-JSON toast and does NOT save, and Edit from the manifest tree opens this view. Out of test-manifest scope (BuildConsole is Shane's own WPF tooling). No DB work. No Git # given for this task — no label sync. | Manifest Viewer editable save | `de43252a` |
| 2026-08-15 | ✅ DONE — Manual Git refresh now updates Focus Mode's milestone progress bar. Read the real Git Board manual-refresh (local #37 — `BtnRefreshGitBoard_Click` → `PopulateGitTrackerBoard(forceFresh:true)`) and Focus Mode's real progress calc (`FocusModeService.RecomputeGame`/`BuildProgress`, fed by `UpdateBoardSnapshot(milestoneInfos)`) in full first. Symptom (Shane): closed 4-5 issues under the active milestone, clicked the Git Refresh button, progress bar stayed stale ("20/35"). Root cause traced end-to-end: Focus `Progress.Closed/Total` is a pure function of `milestoneInfos` (GitHub's own milestone `open_issues`/`closed_issues`), and so is the Git Board milestone-node % (`BuildBoardFromGitHub`, same `milestoneInfoByNumber`) — they CANNOT diverge, so "tree updated but Focus didn't" really meant the tree's OPEN-issue LIST (GraphQL, always fresh) refreshed while the milestone COUNTS (which drive both the tree node % and the Focus bar) stayed stale. Why stale after a forceFresh: `forceFresh` bypassed the LeftSidebar TTL milestone cache (`GetMilestonesThrottledAsync`) AND the repaint-signature guard, but NOT the deeper STATIC ETag `_conditionalCache` in `GitHubApiClient.GetConditionalAsync` — GitHub's milestones-list ETag lags the actual open/closed counters, so a conditional re-GET replayed `If-None-Match` and got `304 Not Modified`, returning the OLD cached counts. That's the "separate cached data source not triggered by the same refresh." Fix: threaded a `bypassCache` flag from `forceFresh` → `GetMilestonesAsync(bypassCache)` → `GetConditionalAsync(path, bypassCache)`, which then OMITS `If-None-Match` so GitHub must return a fresh 200 body (the fresh ETag still repopulates the cache, preserving the next background poll's rate-limit-free 304 — cost is one rate-limited GET per rare manual click). Both dependent views update from the same fresh `milestoneInfos`; the bar (`FocusModeBar.Refresh` on `StateChanged`) and the immersive header (`RefreshHeader`, also a 3s self-poll) both re-read `svc.Progress`. Logging (as required): `RecomputeGame` now takes a `trigger` tag threaded through `UpdateBoardSnapshot` (`"manual Git refresh"` vs `"board update"` vs `"milestone #N activated"`) and logs each recompute with the real old→new closed/total on the `focus-mode` channel — a CHANGED line proves fresh counts landed; an unchanged line under a manual-refresh trigger points straight at an upstream cache, so a future "bar didn't move" is answerable from the feed alone. Files: `Services/GitHubApiClient.cs`, `Controls/LeftSidebar.xaml.cs`, `Services/FocusModeService.cs`. `dotnet build -c Release --no-incremental` clean: 0 errors (only the pre-existing CS0067/CS8602 LeftSidebar warnings, none from my files). NOT live-GUI-verified here: this is a NEW BuildConsole code path — `shaneapp://runTest` exercises Shane's DEPLOYED ShanesBuild (not this worktree), and BuildConsole is out of the test-manifest scope, so it verifies on Shane's next deploy; it's GitHub REST data (no SQL), so `shaneapp://executeSql` doesn't apply. Shane should confirm on a real run: focus a milestone, close an issue on GitHub, click Git Board Refresh — the bar's count and % move immediately. Committed only my 3 files by pathspec (a concurrent session's `test-manifests/copilot-readiness/live-document-pdf-export.json` left untouched). No DB work. Local #37 (BuildConsole-local tracker item, not a filed GitHub issue — no label sync). | Focus Mode progress stale on manual refresh | `c30cdd92` |
| 2026-08-16 | ✅ DONE — Assign Chat to Epic: auto-register unrecognized epic via sync-epic. Shane hit "Epic #1085 isn't registered in the Build Tracker yet." in the WPF app's "Assign Chat to Epic..." flow (`LeftSidebar.xaml.cs`), and specifically wanted the fix inside BuildConsole itself, not a redirect to the web admin panel. Traced the real gap first: `bt_epics` rows are only ever created by an explicit sync action (`POST /admin/build-tracker/extension/sync-epic` or the full `/github-sync`, both in `admin-build-tracker.ts`) — no webhook, no timer — and BuildConsole had zero callers of either endpoint anywhere in the WPF tree (confirmed by grepping every `BuildTrackerApiClient` method and every WPF file for both route names). The only existing trigger was the admin panel's "Sync GitHub" button. Fix: new `BuildTrackerApiClient.SyncEpicAsync(int epicNumber)` (same `POST extension/sync-epic` body shape the admin panel's `syncFromGitHub()` uses, scoped to one issue). The "Assign Chat to Epic..." click handler's not-found branch now calls it automatically instead of just warning: on success, re-fetches the board (`GetBoardAsync`) to refresh `_chatEpicById` and retries the lookup — `upsertEpicRow` on the sync-epic route always upserts the TARGET issue's own row regardless of whether it has sub-issues, so this registers plain leaf issues too, not just issues with children. Only genuine failures now surface a warning/error toast (sync-epic HTTP failure, exception, or the rare case the issue still isn't found after a successful sync, e.g. a bogus number). Logged on the existing `git-board.assign-chat` channel at each branch (auto-syncing / sync failed / still-missing-after-sync / auto-registered) so a future miss is diagnosable. `dotnet build -c Release --no-incremental` clean: 0 errors, only the pre-existing CS0067/CS8602/CS8600/CS8604/CS1998 warnings already present before this change (none from the 2 touched files). No DB work (existing endpoint, no schema change). NOT GUI-verified here (WPF, no headless run) — Shane should confirm that right-clicking an issue whose epic isn't yet in the Build Tracker and choosing "Assign Chat to Epic..." now auto-registers it and completes the assignment in one action instead of erroring out. No Git # (not filed as an issue — no label sync). | Assign Chat to Epic auto-register | `033543b0` |
| 2026-08-15 | ✅ DONE — Auto deploy+verify+test on queue-managed build completion (Epic #803 wiring; the missing automation between "a build finished" and "its code is live and tested"). On `QueueWatcherService.BuildFinished` (success only), automatically: (1) POST `/api/admin/deploy/build-complete` (#911 — real `git pull --ff-only` + deferred/detached `kill 1` restart), reusing the same endpoint `trigger-deploy-and-wait.ps1` drives manually; (2) poll `GET /api/internal/deploy-status` (#805) until the live commit hash flips, returning the REAL new live short hash (not just a boolean) plus an advanced-to-own-commit ancestry check vs local HEAD, so the result is independently verifiable; (3) once hash-confirmed, run the full regression suite (`_regression-suite.json`) via the in-process `RunManifestAsync` pipeline (scoping a single manifest off a queue item isn't reliable — issue# isn't a manifest key — so the documented full-sweep fallback); (4) surface deploy-hash + test pass/fail through `ToastEngine` (#24) + the Build Queue "Needs Attention" section (#54) rather than completing silently. New `Services/PostBuildDeployPipeline.cs` (injected-delegate pattern like RegressionScheduleService), new `BuildTrackerApiClient.PostBuildCompleteAsync` (#911 client), a settings kill-switch `AutoDeployOnBuildComplete` (default on), full stage logging on a `testing.post-build-deploy` channel (build done → deploy triggered → deploy confirmed live w/ real hash → tests triggered → tests done). VERIFICATION: `dotnet build -c Release --no-incremental` clean — 0 errors, only the 4 pre-existing CS0067/CS8602 LeftSidebar warnings (my code adds none; fixed the two nullability warnings my first pass introduced — coalesced FindRepoRoot()'s string? and pinned TrackAsync<BuildCompleteResult?>). NOT live-exercised here: this is a NEW BuildConsole WPF code path and `shaneapp://runTest` exercises Shane's DEPLOYED ShanesBuild, not this worktree — so the auto-trigger runs live only after Shane redeploys BuildConsole; the product endpoints it drives (#911 build-complete, #805 deploy-status) and the regression sweep are the SAME ones already proven by trigger-deploy-and-wait.ps1 + the #967 scheduler, so nothing new server-side. Design: single-flight (a completion landing mid-pipeline coalesces into ONE follow-up run), gated on exit==0 + AutoDeployOnBuildComplete (default on, off-switch in settings.json), and skips the whole pipeline + needless restart when the server is already live on our commit. Naming note: the prompt's "#55/#67" partly mismatched the tree — the real git-pull+kill 1+restart+poll pipeline is Epic #803 = #911 + #805 (issue #55 is an unrelated assessment-wizard feature), the "Needs Attention" section is #54 (not #67), the toast is #24 (matches). No DB work (no schema change; uses existing endpoints). Out of test-manifest coverage scope (BuildConsole is Shane's own WPF tooling, not customer-facing product). Committed only my 4 files by pathspec (a concurrent session's `test-manifests/copilot-readiness/live-document-pdf-export.json` left untouched). No Git # (BuildConsole-local Epic #803 wiring, not a filed issue — no label sync). Shane should confirm on a real run (post-BuildConsole-redeploy) that finishing a queue build auto-deploys, the log shows the real old→new commit hash flip on the `testing.post-build-deploy` channel, and a toast/Needs-Attention row reports the deploy hash + regression pass/fail. | Auto deploy+verify+test on build complete | `86506d0f` |
| 2026-08-17 | ✅ DONE — "Issues in Epic" stale until app restart after Assign Chat to Epic. Shane: assigned a chat to an epic successfully (the auto-register fix from earlier this session), but the Build Queue panel's "Issues in Epic" section stayed empty/stale on the already-open chat tab until he closed and relaunched BuildConsole. Traced end-to-end: `BuildQueuePanel.SetActiveChatEpic` only runs from `MainWindow.EditorTabs_SelectionChanged` (a tab-switch event), reading the epic id off `((TabItem)EditorTabs.SelectedItem).Tag` — a `BoardChat` SNAPSHOT captured once when `OpenChatTab` first created that tab. Assigning an epic afterward updates the server + `LeftSidebar`'s own `_chatEpicById`/tree via `PopulateChatsTree()`, but never touches that already-created tab's frozen `Tag`, so even switching away and back kept reading the same stale `EpicId: null` — only a full restart rebuilds every tab (and its Tag) from a fresh board fetch. Fix: new `LeftSidebar.ChatEpicAssigned` event, raised right after a successful `LinkChatToEpicAsync` in the "Assign Chat to Epic..." handler (both the pre-existing-epic path and the auto-register path added earlier this session share one call site, so one `Invoke` covers both); `MainWindow` subscribes, finds the open tab (if any) whose `Tag.ConversationId` matches, patches that `BoardChat.EpicId` in place, and — if it's the currently active tab — calls `BuildQueuePanel.SetActiveChatEpic(..., force: true)` immediately instead of waiting for the next tab switch. While investigating found and fixed a SEPARATE real bug already sitting uncommitted in the working tree (not mine originally, but broken and needed fixing regardless): `admin-build-tracker.ts`'s `/extension/sync-epic` route had a duplicated `router.post(...)` line (two identical registrations stacked, from an in-progress local-data-fallback change) that broke the whole file's brace matching — confirmed via `tsc`: `error TS1005: '}' expected` at the end of the file. Removed the duplicate line only, keeping the intended fallback logic (when a live GitHub fetch fails — e.g. the server's own `GITHUB_TOKEN` problem diagnosed earlier this session — sync-epic now falls back to a client-supplied `title`/`description`/`status`, and `BuildTrackerApiClient.SyncEpicAsync`/the "Assign Chat to Epic..." call site were already extended to pass the right-clicked issue's own already-known `RawTitle`/`Body`/`Status` from the Git Board tree data, so registration can still succeed without a live GitHub round-trip). `dotnet build -c Release --no-incremental` clean: 0 errors, same pre-existing warning count/pattern. `pnpm run typecheck` in `artifacts/api-server`: back to the known ~73-error pre-existing baseline pattern (confirmed the duplicate-route line was the ONLY syntax break; no new errors from the fallback logic itself). No DB work. Committed only the 4 files this work actually touched (`admin-build-tracker.ts`, `LeftSidebar.xaml.cs`, `MainWindow.xaml.cs`, `BuildTrackerApiClient.cs`) — a concurrent session's staged-but-uncommitted `artifacts/shane-mccaw-consulting/` page-split work (`App.tsx`/`Home.tsx`/new `CopilotAssessmentLanding.tsx`) was left completely untouched and flagged to Shane rather than swept in. NOT GUI-verified here (WPF, no headless run) — Shane should confirm that assigning a chat to an epic now updates the open tab's "Issues in Epic" section immediately, without needing a restart. No Git # (not filed as an issue — no label sync). | Issues-in-Epic live refresh + sync-epic fallback fix | `99d0c28d` |
| 2026-08-17 | ✅ DONE — Git Board: active-epic highlight + collapsed-by-default tree. Two asks. (1) Shane: "when I am in focus mode onto a Milestone with a lot of Epics... I need a way for the Epic I'm working to be more visually ahead... I should be able to look at the Git Board Tree View and know exactly what Epic I'm working." New `LeftSidebar.SetActiveEpicGithubNumber(int?)`, fed by `MainWindow` at the SAME two signals that already drive `BuildQueuePanel.SetActiveChatEpic` (`EditorTabs_SelectionChanged` — whichever chat tab is on screen — and the earlier-this-session `ChatEpicAssigned` event, so a fresh assignment reflects immediately too). `RenderIssuesTree`/`CreateIssueHeader` compare it against each epic issue's real GitHub number: the matching epic gets a green left-bar + tinted background wrap, a bold green title, and a "🎯 WORKING" badge (same Border-badge convention as the existing Blocked/No-Epic badges) — reads ahead of every sibling epic at a glance, not just on close inspection — and its own node PLUS its ancestor milestone/epic-bucket nodes force-expand regardless of collapsed state, so it's never hidden inside a collapsed branch. (2) Shane: "make all Tree Nodes in the Git Board retracted by default." The #873 persistence set (`_collapsedNodeKeys`, tracking manually-COLLAPSED nodes against a default-EXPANDED tree) had its polarity flipped to `_expandedNodeKeys` (tracking manually-EXPANDED nodes against a default-COLLAPSED tree) at all three Git Board node levels (milestone/epic-bucket/issue) — confirmed via grep this set is exclusively used by the Git Board's `IssuesTree` (the Chats tree/Manifests tree/Explorer tree/local Git Changes tree each have their own separate, untouched expand logic elsewhere in the same file). The #873 fix's actual goal — collapse state survives a full tree rebuild/refresh — is unchanged; only which state is the persisted opt-in changed. `dotnet build -c Release --no-incremental` clean: 0 errors, same pre-existing warning count/pattern (none from the 2 touched files). No DB work, no schema change, no new endpoints — pure client-side render logic. Committed only the 2 files this touched (`LeftSidebar.xaml.cs`, `MainWindow.xaml.cs`) — a concurrent session's own new row in `PLATFORM_BUILD.md` was sitting uncommitted at the same time and was left completely untouched. NOT GUI-verified here (WPF, no headless run) — Shane should confirm on a real focused milestone with an active chat that its epic shows the green "🎯 WORKING" treatment and auto-expands, and that every Git Board node (milestone/epic/issue) now opens collapsed on a fresh load/refresh until manually expanded. No Git # (not filed as an issue — no label sync). | Git Board active-epic highlight + collapsed-by-default | `9ff7c0f2` |
| 2026-08-17 | ✅ DONE — Test manifest tree: bold/badge new manifests independent of run status. Shane: "anytime a new test manifest is added, even if it's been ran by the agent... I need it bold or something so I can easily find it." Root design point: "newness" had to be independent of `ManifestRunStatus` (already tracked from `TestHistoryStore` for the pass/fail ✓/✕ icon) — a manifest an agent adds AND immediately runs still had to read as new until Shane himself looks at it, not the moment it gets a run result. New `ManifestEntry.IsNew`, computed in `PopulateManifestsList` against a persisted `BuildConsoleSettings.SeenManifestPaths` set (same local `settings.json` round-trip pattern as `GitHubPat`/`LastSeenBuild` etc.) — a manifest's relative path not yet in that set is new. Bootstrap sentinel `ManifestTrackingBootstrapped` (same shape as `LastSeenBuild`'s -1 sentinel for the Home "What's New" feature): on the very first scan after this shipped, the ENTIRE current corpus is silently seeded into `SeenManifestPaths` without anything flagged new, so an existing install's whole `test-manifests/` tree doesn't suddenly light up bold. From then on: `RenderManifestTree`/`MakeLeafHeader` give a new leaf a bold green filename + a "NEW" badge (Border-badge convention matching the existing issue-number badge), and `MakeAreaHeader` gets an "N NEW" badge on the area row itself plus auto-expand (`IsExpanded = searching || newCount > 0`) so a new manifest sitting inside a collapsed area is never hidden from the badge/highlight — same auto-expand-the-hidden-thing pattern as the Git Board active-epic highlight shipped earlier this session. `LoadManifestLeaf` (the single shared path both single-click-select and #1017's double-click-to-run already go through) clears `IsNew` and persists the path into `SeenManifestPaths` the MOMENT SHANE OPENS the manifest — not when an agent runs it, matching "even if it's been ran" precisely. Fixed a real DockPanel layout bug found while wiring the leaf badge: the badge must be added BEFORE the filename TextBlock, not after — DockPanel's `LastChildFill=true` (default) makes whichever child is literally LAST in the `Children` collection fill the remaining width regardless of its own `Dock` property, so a badge added after the filename would have stolen the fill slot and stretched full-width instead of the filename ellipsis-trimming correctly; the area header's badge needed no such reordering since `MakeAreaHeader` uses a plain `StackPanel` (no fill/last-child concept). `dotnet build -c Release --no-incremental` clean: 0 errors, same pre-existing warning count/pattern (none from the 2 touched files). No DB work — pure local `settings.json` + client-side render logic. Committed only the 2 files this touched (`LeftSidebar.xaml.cs`, `BuildConsoleSettings.cs`) — a concurrent session's `test-manifests/_regression-suite.json` edit + new untracked `test-manifests/marketing/` folder were left completely untouched. NOT GUI-verified here (WPF, no headless run) — Shane should confirm that adding a new manifest file to `test-manifests/` shows it bold-green with a "NEW" badge (and its area auto-expands even if it was collapsed) even after an agent runs it, and that opening it once in the tree clears the bold/badge permanently. No Git # (not filed as an issue — no label sync). | Test manifest tree new-manifest highlight | `ca789850` |
| 2026-08-17 | ✅ DONE — Git issue detail: fix "NEVER RAN" for feature-slug manifests with real passing history. Shane, with screenshot: "Test Manifests is showing as never ran but I've ran it multiple times with pass" — `test-manifests/copilot-readiness/copilot-assessment-landing-route.json` showed the "⚠️ NEVER RAN" peach badge on its Git issue detail card despite real passing runs. Root cause: `GitDetailView.GetOrFetchTestHistoryAsync` built its run-history lookup keyed EXCLUSIVELY by `TestHistoryEntry.Issue` (`history.GroupBy(e => e.Issue)`), and both places that render a manifest's status (`CreateTestManifestActionCard` — the card in the screenshot — and an inline `[NEVER RAN]`/`[PASSED]`/`[FAILED]` text-run decoration on manifest-path mentions inside issue body/comment markdown) only ever checked `manifestIssue` (the manifest's numeric `issue` field). A route-registration manifest under the newer area/feature-slug convention (CLAUDE.md's discover-before-create rule) has no numeric `issue` — it's identified by its `feature` slug instead — so `manifestIssue` was always null and both call sites fell straight to the "never ran" branch no matter how many real runs existed. `LeftSidebar.PopulateManifestsList` (the Automation sidebar's manifest tree — separate code, separate bug surface) already did this lookup correctly (issue-or-feature), so the same manifest showed its real pass/fail status THERE while showing false "never ran" on the issue detail tab — same underlying data, two independently-built read paths, only one of them right. Fix: new `Services.TestHistoryLookup` (`ByIssue`/`ByFeature` dictionaries + `TryGetForManifest(issue, feature)`, issue tried first, feature as fallback — same precedence LeftSidebar already used) replaces the bare `Dictionary<int, TestHistoryEntry>` threaded through `GitDetailView`'s ~8 signatures (`GetOrFetchTestHistoryAsync`/`RenderMarkdownBody`/`AddBody`/`CommentCard`/`CreateLinkedTextBlock`/`RenderActionsColumn`/`CreateTestManifestActionCard`) — mechanical type swap via `Edit ... replace_all`, then both real lookup sites also now capture `m.Feature` alongside `m.Issue` from the already-loaded `TestManifest` and call `TryGetForManifest` instead of a bare issue-only `TryGetValue`. Checked `IssueDetailView.xaml.cs` too — it has the IDENTICAL bug pattern in its own independent copy of this code, but confirmed via `MainWindow.xaml.cs`'s own comment ("The bottom-panel IssueDetailView is no longer driven by issue...") and zero `new IssueDetailView(` construction sites anywhere in the tree that it's dead/unwired code (GitDetailView is the live path, instantiated 3× in `MainWindow.GitDetailTabs.cs`) — left it untouched rather than fixing dead code. `dotnet build -c Release --no-incremental` clean: 0 errors, same pre-existing warning count/pattern (none from the 2 touched files). No DB work — pure local `test-results/_history.jsonl` read + client-side render logic, no schema/endpoint change. Committed only the 2 files this touched (`GitDetailView.cs`, `TestHistoryStore.cs`) — an untracked `desktop/Watch-Deploy.ps1` sitting in the working tree from a concurrent session was left completely untouched. NOT GUI-verified here (WPF, no headless run) — Shane should confirm that `copilot-assessment-landing-route.json`'s card on its Git issue detail tab now shows its real ✅ PASSED status instead of ⚠️ NEVER RAN. No Git # (not filed as an issue — no label sync). | Fix NEVER RAN for feature-slug manifests | `f8e6ee1a` |
| 2026-08-17 | ✅ DONE — Auto deploy+test chain never fired a real git-pull+restart when the dev server was napping (Epic #803/#911; Shane's "#82"). Symptom: builds finish, tests run automatically, but the deploy's git pull never happens — yet Shane's manual `Watch-Deploy.ps1` against the SAME endpoint works. Traced the whole real chain first (`QueueWatcherService.BuildFinished` → `MainWindow.QueueWatcher_BuildFinished` → `PostBuildDeployPipeline.OnBuildFinishedAsync` → `BuildTrackerApiClient.PostBuildCompleteAsync` → `POST /api/admin/deploy/build-complete` → poll `GET /api/internal/deploy-status` → `RunScopedManifestsAsync`). Verified each of the three suspects: (Q1) listener firing — **NO, not reliably**; (Q2) HTTP call — URL/base/Bearer-ingest-token all correct, endpoint genuinely works (`admin-deploy-console.ts` + `Watch-Deploy.ps1` prove it), so NOT the cause; (Q3) separate ungated test path — **yes**: build sessions run their own `shaneapp://runTest` in-process (`MainWindow.ShaneAppRunTest.cs`), ungated on the deploy. ROOT CAUSE (Q1): `QueueWatcherService.TickAsync` had `await _api.MarkQueueItemCompleteAsync(...)` and `BuildFinished?.Invoke(...)` **inside one try/catch**; `MarkQueueItemCompleteAsync` is a remote HTTP POST to the Replit dev server, which naps after ~15 min — a build finishing while it's asleep makes that POST throw, so `BuildFinished` never fires and the entire `PostBuildDeployPipeline` (the git-pull+restart) is silently skipped, while the build's own `shaneapp://runTest` still runs against stale deployed code. Manual `Watch-Deploy.ps1` always worked because Shane's own run has the server awake. FIX (all in `desktop/BuildConsole/Services/`): (1) `QueueWatcherService.cs` — decoupled the local `BuildFinished` fan-out from the remote completion report into two independent try/catches, so it fires REGARDLESS of the report outcome (the pipeline's own build-complete POST is what wakes the server + pulls); (2) `ActivityLog.cs` — added a durable per-day file sink under `%AppData%\BuildConsole\logs\activity-YYYY-MM-DD.log` (the in-memory event dropped lines logged before MainWindow subscribed and left nothing after a crash — the exact "which stage silently didn't happen" gap), best-effort/never-throws; (3) `PostBuildDeployPipeline.cs` — loud stage logging: an unconditional `[fired]` marker proving the listener ran, a `[2/5] build-complete RESPONSE received` line dumping the real `ok`/`restarting`/per-step git-pull result, plus a new gate that skips tests when the confirmed-live commit provably does NOT contain this build's commit (`AdvancedToOwnCommit == false`) so tests never run against stale code. `dotnet build -c Release --no-incremental` clean: 0 errors, only the pre-existing warnings (none from the 3 touched files). Committed ONLY my 3 files — a concurrent session's dirty `IssueChompAnimation.cs` and Shane's untracked `Watch-Deploy.ps1` were left untouched. NOT live-verifiable from here (WPF change to BuildConsole itself — takes effect only once Shane deploys his ShanesBuild copy; no headless WPF run, and `shaneapp://runTest` targets the DEPLOYED build); the fix is code-traced + compile-verified. Shane should confirm on a real run that a build finishing while the dev server is asleep now still shows `[fired]` → `[2/5] … RESPONSE received` → `[3/5] Deploy CONFIRMED LIVE` in `logs\activity-*.log` and the server actually advances to the new commit before tests run. Out of test-manifest scope (BuildConsole tooling). No Git # (Shane's "#82" is not the GitHub issue #82 "Zoho Integration Foundation"; this is Epic #803/#911 work — no label sync). | Auto deploy+test chain napping-server fix | `91efbdd7` |
| 2026-08-17 | ✅ DONE — Mo Work grump critter (new issues) + in-flight airplane icon. Two asks. (1) Shane: "More work is added with new Issues created... This little guy should be grumpy, sad... Mo Work! Think Warcraft (before World of Warcraft) when you would click on Ogre and he'd say 'Mo Work!'" New `IssueChompAnimation.PlayMoWork` — the deliberate ANTI-celebration critter: every existing critter (`Play`/`PlayEpic`/`PlayWhammy`/`PlayMilestoneClosedParty`) sprints or charges in fast and dashes off triumphant; this one shuffles in slow (1400ms vs 700-800ms) and heavy, low-amplitude hop, groans a random `MoWorkPhrases` line ("MO' WORK!", "UGH... MORE?", etc.) in a muted-gray speech bubble instead of a bright celebration color, slumps with a resigned puff of gray dust (new `SpawnGrumpDust`, a low downward fan — not a burst) instead of confetti, then trudges off with a slow fade instead of a victory dash. New `BuildGrumpMascot` — an olive-green droopy-eared grump with small tusks, furrowed down-angled brows, half-lidded tired eyes, and a frown, distinct from the existing 5 peppy chomp mascots. Wired in `LeftSidebar.PopulateGitTrackerBoard`'s board-diff (the exact mirror of the existing "newly closed issues -> critter chomp" check that already runs there): a new issue number present in the fresh OPEN-only fetch but absent from `_lastBoardIssues` fires `PlayMoWork`, staggered 500ms apart, same first-load guard as the closed-issue check (skipped when `_lastBoardIssues` is still empty, so the very first board load never floods a wall of grumps for every pre-existing issue). (2) Shane: "In the Git Board when an Issue has the label 'In-Flight' you should add an airplane icon to the node." New `GitIssue.IsInFlight`, populated straight from the ALREADY-EXISTING `GitBoardIssue.HasInFlightLabel` (the same real `in-flight` GitHub label CLAUDE.md's session bookends and this session's earlier queue-time auto-sync already apply — not a second concept) in `BuildBoardFromGitHub`'s `MapBucket`; `CreateIssueHeader` renders a small ✈️ before the title on any open issue carrying it. SHARED-CHECKOUT HAZARD hit and worked around: `IssueChompAnimation.cs` was being actively live-edited by Shane in another editor DURING this session (his own commits `4407d20a`/`83dbd2a2`/`9320753d` all landed on this exact file within the same ~15-minute window) — my insertions into that file kept vanishing within moments of being written (confirmed by re-grepping immediately after each edit and finding 0 matches, twice), which is a live-save-clobber, not a one-time stale read. Fixed by NOT fighting for that file: flipped `IssueChompAnimation` to a `partial class` (the only change landed in that file — a single word, low collision surface) and moved `PlayMoWork`/`SpawnGrumpDust`/`BuildGrumpMascot`/`MoWorkPhrases` into a brand new `IssueChompAnimation.MoWork.cs` partial-class file nothing else touches — same "new-partial-class-file avoids shared-file contention" precedent used for Git Board detail tabs previously. `dotnet build -c Release --no-incremental` clean: 0 errors, only the pre-existing warning pattern (none from the 3 touched/added files) — confirmed on a build run immediately after staging, to catch the file in a non-reverted moment. Committed only my 3 files (`IssueChompAnimation.cs`, new `IssueChompAnimation.MoWork.cs`, `LeftSidebar.xaml.cs`) — concurrent dirty `admin-build-tracker.ts` and `assessmentZones.ts` left completely untouched. NOT GUI-verified here (WPF, no headless run) — Shane should confirm that a new GitHub issue appearing on the next Git Board refresh sends in the sad grump instead of (or alongside) the celebratory critters, and that an issue with the real `in-flight` label shows a ✈️ on its tree node. No Git # (not filed as an issue — no label sync). | Mo Work critter + in-flight airplane icon | `795e01cd` |
| 2026-08-17 | ✅ DONE — Terminal panel: wire to the real Replit deploy-console endpoint, not a local shell. Shane: "The Terminal panel in the bottom panel... that terminal is supposed to send the commands to the Replit server." Confirmed: `TerminalView.xaml.cs` spawned a LOCAL `powershell.exe` process on Shane's own machine (`RedirectStandard{Input,Output,Error}`), never touching Replit at all — genuinely wrong, not a misunderstanding. Asked where the intended target actually was rather than guessing between the several plausible approaches (browser-automate Replit's own web Shell, real SSH, or a new server-side exec endpoint); Shane: "There is already an Endpoint in the API server that allows for this." Found it — `POST /admin/simulator/deploy/console` (`admin-deploy-console.ts`), the SAME free-text deploy-console route the admin panel's own floating console already uses: runs the command verbatim via `child_process.exec` on the dev server itself (cwd = real repo root via `git rev-parse --show-toplevel`), gated by the same `requireAdminOrIngestToken()` Bearer pattern every other BuildConsole endpoint already uses, returns real `{ ok, command, output }`. Rewired `TerminalView` end to end: removed the local `Process`/PowerShell plumbing entirely; new `BuildTrackerApiClient.RunDeployConsoleCommandAsync(command)` POSTs `{ command }` to `api/admin/simulator/deploy/console` and returns a new `DeployConsoleResult { Ok, Command, Output }`; `TerminalView` gained a real `Initialize(BuildTrackerApiClient api)` (wired in `MainWindow` alongside every other panel's `.Initialize(_buildTrackerApi)` fan-out) instead of constructing its own process in the constructor; `Send_Click` is now async, disables the Send button while a command is in flight (one exec per round-trip — the endpoint has no persistent shell/session state server-side, so this correctly reflects that contract rather than implying a stateful shell), and reports a clear "[Build Tracker API isn't configured]" message instead of silently failing when unconfigured. Also fixed the "dotnet build --configuration Release" quick-command chip, which would have always failed on a Node/pnpm Replit box with no .NET SDK — swapped for "pnpm install"/"pnpm build" chips that actually make sense for that server; the git chips (status/log/pull/push/fetch/stash/diff/branch) stay valid since Replit is a real git checkout too. Updated the placeholder text and Send button tooltip to stop implying a local PowerShell prompt. `dotnet build -c Release --no-incremental` clean: 0 errors, only the pre-existing warning pattern (none from the 4 touched files). No DB work — reuses an existing endpoint, no schema/server change at all. Committed cleanly — no shared-checkout contention on these 4 files this time (unlike `IssueChompAnimation.cs` earlier this session). NOT GUI-verified here (WPF, no headless run) — Shane should confirm that typing a command (or clicking a chip) in the Terminal panel now shows real output FROM the Replit server (e.g. `git status` should reflect the server's own checkout state, not this machine's), and that the Send button disables while a command is running. No Git # (not filed as an issue — no label sync). | Terminal panel wired to Replit deploy-console | `0075f45d` |
| 2026-08-17 | ✅ DONE — Whole-app aesthetic polish pass: subtle dark DropShadow glows + refined low-opacity depth shadows + slow hover/state micro-transitions + dark-to-dark gradient accents across real panels (Build Watch, Home, Chats tree, Build Queue, Focus immersive) using existing Catppuccin Mocha muted-accent brushes ONLY — no bright/pink/harsh (ADHD constraint). Plus reuse/extend the existing critter system (IssueChompAnimation/CritterLoungeControl/EncouragementService) into achievement moments where genuinely missing, same dark/low-intensity constraint. | ae731660 |
| 2026-08-21 | ✅ DONE — Made the UI-step DOM poll timeout + interval tunable via settings.json (BuildConsoleSettings) instead of hardcoded UiTestExecutor consts, so Shane can adjust them without a rebuild. Two new settings: `UiStepPollTimeoutMs` (default 30000) + `UiStepPollIntervalMs` (default 250), same field-initializer-as-default convention as the rest of the store (a pre-existing settings.json with no `uiStepPoll*` keys deserializes to the same 30000/250 as before — behaviour unchanged). UiTestExecutor now reads both in its constructor via `BuildConsoleSettings.Load()`, replacing the former `private const int ExpectPollTimeoutMs/ExpectPollIntervalMs` (kept as `DefaultExpectPollTimeoutMs`/`DefaultExpectPollIntervalMs` fallbacks); a missing or non-positive settings value falls back to the default so the window/cadence can never degenerate to <= 0. These govern BOTH the `expect` and click/input step poll loops. `dotnet build -c Release` clean in an isolated origin/main worktree (0 errors; 24 pre-existing warnings, none in the changed code). Not live-verified in a running GUI here — Shane can set `uiStepPollTimeoutMs`/`uiStepPollIntervalMs` in %AppData%\\BuildConsole\\settings.json and confirm a run honours them. No Git # — not filed as an issue, so no label sync. | UI step poll timeout/interval as settings | `226adfb3` |
| 2026-08-23 | ✅ DONE — Build Queue critical-path visibility: inline "⛓ blocks N" downstream count per queue item. Shane regularly stacks 10-20 queued builds with real blocked-by chains, but the flat/nested Queue list never showed which items are actually on the critical path (gating the most downstream work) vs. independent/low-impact if delayed. Read the whole real Build Queue panel first — `QueueItem.BlockedByNumbers` (the real list field, `BlockedByNumber` singular kept for back-compat) is the blocked-by relationship; `RenderQueue` already nests a blocked item under its blocker by matching those numbers, and `BuildQueueTreeItem` builds each card's top row (status pill + issue-# badge). Chose the inline per-item count (over a separate Canvas graph window) as the lowest-risk, highest-glance-value option that reads the exact data already tracked and rendered. New `ComputeDownstreamBlockCounts(items)` builds the reverse (dependents) graph — for each item, BFS the transitive set of OTHER items that depend on it (X depends on B when B ∈ X.BlockedByNumbers), so a chain A→B→C credits A with 2 downstream (surfaces the real head-of-chain bottleneck, not just its immediate child). Keyed by `QueueItem.Id` (rows can share a GithubNumber; un-numbered rows block nothing), cycle-guarded via a visited-Id set + expanded-number set. Computed once per `RenderQueue` over the exact post-search-filtered set being rendered (so counts always match what's shown), stored in `_downstreamBlockCounts` + `_maxDownstreamBlockCount`. `BuildQueueTreeItem` reads it: any item with count > 0 gets a "⛓ blocks N" badge in its top row (same Border+colored-TextBlock badge idiom as the existing status pill / #-badge); the queue's single worst bottleneck (count == max, max > 1) gets a louder filled peach/red bold badge so the highest-leverage item to prioritize jumps out, lesser blockers get the quiet neutral outline, items that block nothing get no badge at all (the badge itself IS the "on the critical path" signal). Tooltip explains the count is direct-or-transitive. Pure display — no new data source, no server/DB/schema change, no logging (none needed per the task). `dotnet build -c Release --no-incremental` clean: 0 errors, 24 pre-existing warnings (GitDetailView/LeftSidebar/MainWindow/UiTestExecutor/TestRunnerWindow — none from the one touched file BuildQueuePanel.xaml.cs). Committed ONLY my one file — a concurrent session's uncommitted `LeftSidebar.xaml`/`LeftSidebar.xaml.cs` edits in the working tree were left completely untouched. NOT GUI-verified here (WPF, no headless run) — Shane should confirm that with several blocked-by-chained builds queued, the head-of-chain build shows the loud "⛓ blocks N" badge and the count matches how many downstream builds actually depend on it. Out of test-manifest scope (BuildConsole tooling). No Git # (not filed as an issue — no label sync). | Build Queue blocks-N downstream count | `9036ea83` |
