# BuildConsole Build Log

Append-only log of work sessions whose work is entirely within `desktop/BuildConsole/` (this WPF app). Each session adds exactly one row, in two edits:

1. **At session start** (before any real work): append a row with status `⏳ IN FLIGHT — {step name}`, commit it immediately as its own standalone commit.
2. **At session end** (after the real work is done, tested, and typechecked): update that same row to `✅ DONE — {step name}`, filling in the real commit hash of the work, then commit the update (either folded into the final commit or immediately after it).

If a session dies, crashes, or is abandoned mid-way, its row stays `⏳ IN FLIGHT`, which is itself the record that an attempt was made and didn't finish.

See [CLAUDE.md](../../CLAUDE.md) for the exact instructions, including which sessions bookend here versus in the root [PLATFORM_BUILD.md](../../PLATFORM_BUILD.md).

| Date | Status | Step | Commit |
|------|--------|------|--------|
| 2026-08-14 | ⏳ IN FLIGHT — Build Watch live-render freeze fix. Shane: "they are not staying live and hanging the main app when they do catch up." Investigating the real cause of Build Watch slots stalling then freezing the whole app on catch-up (6 queued tasks all showed "running" with the identical timestamp = the single UI Dispatcher thread seized). Confirmed candidate: `BuildWatchWindow.DrainInteractiveOutput` renders the ENTIRE per-poll backlog (`CopyEventsSince`, up to thousands of events, across every occupied slot) in one synchronous UI-thread `foreach` into a NON-virtualized `ItemsControl` (ChatSessionPane transcript) with a per-add `ScrollToEnd()` — O(n²) work that blocks every window until it finishes. Fix: bound per-tick render work, drain in small chunks yielding via `Dispatcher.BeginInvoke(Background)` between them, coalesce a runaway backlog (drop the surplus that would only be trimmed off the ≤MaxCardsPerSlot transcript anyway, with one honest "skipped N" marker), and log real timing around the flush. QueueWatcherService (background output/buffer side, already thread-safe) untouched; unrelated concurrent-session dirty files (MainWindow.xaml + StartupLoadingView.*, the row below) left alone. | Build Watch live-render freeze fix | `TBD` |
| 2026-08-14 | ⏳ IN FLIGHT — Animated startup loading screen (resumes the abandoned 2026-08-13 attempt below): build the missing backing `StartupConnectivityService` + wire it into MainWindow so the already-authored `StartupLoadingView` (original running-character animation + honest per-connection progress list) actually runs. Real per-connection probes against the SAME endpoints the app connects to at launch — Build Tracker board (`GetBoardAsync`), in-flight issues (`GetInProgressAsync`), Build queue (`GetQueueAsync`), Deploy status (`GetDeployStatusAsync`) — plus the real Claude usage meter observed via its existing `StatusChanged`. Each connection's timing/success/failure logged on the `startup` ActivityLog channel; per-connection timeout + a global cap so one slow/failed connection never blocks launch; overlay fades to Home once all settle. NOTE: the prior attempt left MainWindow.xaml in a stale state that reverts HEAD's universal-search-bar work — I discard that revert and re-apply only the overlay onto current HEAD; unrelated concurrent-session dirty files (BuildWatchWindow.xaml.cs, ChecklistExtractor.cs, TaskChecklistModel.cs) left untouched. | Animated startup loading screen | `TBD` |
| 2026-08-13 | ✅ DONE — Build Watch tool-call bubbles show real content. Root cause CONFIRMED (live stream-json capture, not assumed): the `--output-format stream-json` events genuinely carry the full payload — a `tool_use` block has `id`+`name`+`input` (the real command/args) and each tool result arrives as a separate `type:"user"` event whose `message.content[]` holds `tool_result` blocks (`tool_use_id`, `content`, `is_error`). `SummarizeStreamJsonLine` extracted ONLY the tool `name` (`[tool: X]`) and dropped `input` entirely, and the whole `user`/tool_result event fell through its `default` case as "noise" — so both the log line and the buffer only ever carried the bare name, which the display then regex-reconstructed. Pure parsing gap, no source suppression. Fix: a new structured interactive event stream (`InteractiveEvent`: AssistantText / ToolCall(name,id,command-preview,input) / ToolResult(id,output,is_error) / TurnResult) parsed from each line and pulled by the interactive Build Watch render, replacing the old flattened string buffer; the human-readable per-item log FILE (#802 + foreign file-tail) is still fed the old summary text unchanged. The collapsed #1012 chip now shows, on expand, the real command/params + real tool output, and a real inline colored diff for Edit/Write/MultiEdit file edits (self-contained; does not depend on the concurrent code-diff DiffView row below). One raw `tool_use` + one raw `tool_result` sample logged once per run on a `stream-json` ActivityLog channel for future diagnosis. Verified: the real event shapes were captured LIVE (`claude.exe --print --output-format stream-json --verbose` forcing a Bash + Read tool call — the output-event shapes are identical to the interactive stdin path, which only differs in input delivery) and every captured line traced through the parser; `dotnet build -c Release` clean (0 errors; only the 4 pre-existing CS0067 LeftSidebar warnings), including the combined tree after rebasing onto the concurrent universal-search commits that landed mid-session. HONEST boundary: the end-to-end GUI render of a live interactive queued build wasn't exercised here (same environment constraint as the #29c2cdca interactive-build row) — Shane should confirm on a real queued build that a tool bubble expands to show the real command/output and, for a file edit, the diff. | Build Watch tool-call detail + file-edit diff from stream-json | `4c0adab3` |
| 2026-08-13 | ✅ DONE — Build Watch event stream now renders genuine code diffs. A run of consecutive unified-diff lines (`@@ … @@` hunks, `+`/`-`/context lines, or a fenced ```` ```diff ```` block) is detected in the tail/interactive line stream and folded into one DiffTurn — analogous to how consecutive `[tool:]` markers fold into a ToolGroupTurn — then rendered by a new AvalonEdit-based DiffView: per-line green (added) / red (removed) backgrounds, a diff-aware line-number gutter (real old/new file numbers from the hunk header), and underlying-language syntax highlighting (reusing #939's SqlSyntax.xshd + JsonSyntax.xshd + AvalonEdit's built-in definitions). Visually distinct from the grey narrative/tool-chip bubbles but consistent with the pane's dark palette; plain prose + collapsed tool chips render exactly as before. Pure display feature (no logging). QueueWatcherService untouched. Landed as an additive union merge (620 insertions, 0 deletions — the other session's work fully preserved, its markers unchanged) on top of the concurrent, complementary `4c0adab3` "tool-call detail + file-edit diff from stream-json" feature already on main: theirs renders Edit/Write/MultiEdit tool-input diffs inside the expanded tool chip, this renders diffs the agent writes in its prose text stream as a standalone DiffView (different triggers, different surfaces — no collision, verified by their own "does not depend on the code-diff DiffView row" note). Combined tree builds clean (0 errors). HONEST boundary: the live GUI render of a real diff wasn't exercised (WPF, no headless GUI) — Shane should eyeball one on a real build. | Build Watch code-diff rendering | `c37b3639` |
| 2026-08-13 | ⏳ IN FLIGHT — Animated startup loading screen: replace the blank window shown while real startup connections (GitHub board, Build Queue, Deploy status, Claude usage meter) complete with an original animated running character + honest per-connection progress list, per-connection timing logged, transitioning smoothly to Home once all connections resolve (or per-connection timeout, never blocking on one slow/failed connection). | Animated startup loading screen | `TBD` |
| 2026-08-13 | ✅ DONE — powerShellVerify delegated device-code token now persists/reuses across BuildConsole-spawned pwsh runs. Root cause CONFIRMED (on disk + against the Graph SDK source at 2.29.1…main, not assumed): cross-process SILENT reuse of a `-UseDeviceCode` token requires the Azure.Identity `AuthenticationRecord` at `%USERPROFILE%\.mg\mg.authrecord.json`. On Shane's box that file was ABSENT while the DPAPI MSAL token cache (`%LOCALAPPDATA%\.IdentityService\mg.msal.cache.cae/.nocae`) persisted fine and was fresh — so the SDK's device-code path, which gates reuse on `if (!File.Exists(AuthRecordPath))`, fell through to a fresh device-code prompt every run despite a perfectly valid cached token. Ruled OUT by live testing on the machine (not guesswork): `-NoProfile` (irrelevant to the cache path); env/user-context mismatch (a spawned `-NoProfile -NonInteractive -EncodedCommand` child's USERPROFILE/LOCALAPPDATA/MyDocuments/PSModulePath are byte-identical to an interactive shell — the child inherits BuildConsole's env verbatim under UseShellExecute=false); UseShellExecute/env-inheritance; and ContextScope defaulting to Process (the delegated device-code path defaults to CurrentUser on Windows per the SDK source — only WSL forces Process). Also found: the SDK hardcodes a 120s device-code auth window (`MaxAuthenticationTimeOutInSeconds`, NOT extendable via `-ClientTimeout`, which is only the HTTP-client timeout) — if a sign-in exceeds 2 min the record is never written. FIX (`830dcd63`, `Services/PowerShellTestExecutor.cs`): (1) import the NEWEST installed `Microsoft.Graph.Authentication` — a bare `Import-Module` was silently loading a stale **2.29.1** (`D:\Shane\Documents\PowerShell\Modules`, first on PSModulePath) that shadowed **2.38.0** (Program Files); newer builds have more robust keyed/legacy auth-record resolution and share the same on-disk cache, so nothing is orphaned; (2) pass `-ContextScope CurrentUser` explicitly so the on-disk (not in-process) cache is used unambiguously; (3) log the resolved cache-relevant paths/state at process launch (C#) and pre/post-connect (pwsh `__PSVERIFY_DIAG__`): loaded module version+base, USERPROFILE/LOCALAPPDATA, the `.mg` AuthenticationRecord path + whether it exists BEFORE and AFTER connect, and the MSAL cache files — so any recurrence is diagnosable from the `testing.powershell-verify` ActivityLog channel alone. `dotnet build -c Release` clean in an isolated worktree (0 errors; only the pre-existing unrelated CS0067 LeftSidebar warnings); the injected pwsh parses clean and the read-only diag block was run live — it now loads 2.38.0 and reports `authRecordExists=False` + `msalCache=[mg.msal.cache.cae,mg.msal.cache.nocae]`, i.e. it makes the exact root cause visible. HONEST boundary: the end-to-end proof — that one completed interactive device-code sign-in now writes `mg.authrecord.json` (`authRecordExistsNow=True` in the post-connect log) and that the NEXT run then reconnects silently with no prompt — needs Shane to complete ONE real interactive sign-in (device-code + browser + MFA, promptly given the SDK's 120s window); it cannot be exercised headlessly here. Shane To-Do: run one Play-Test powerShellVerify, complete the device-code sign-in, watch the `testing.powershell-verify` channel for `authRecordExistsNow=True`, then run a second time and confirm it connects silently. | powerShellVerify device-code token persistence | `830dcd63` |
| 2026-08-13 | ✅ DONE — Usage meter: projected "time until 100%" from real observed growth rate, shown alongside the existing percentage (`Claude: 72% used · ~1d 4h to 100%`; independent of the ≥85% reset-countdown threshold). New `UsageHistoryStore` appends each successful poll's `(timestamp, percentage, reset-target)` as one line to `%AppData%\BuildConsole\usage-history.jsonl` — deliberately the SAME append-only-JSONL shape/next-to-settings.json location as `TestHistoryStore`, not a new mechanism (crash-safe, malformed trailing line skipped on read, light self-compaction past 8000 lines). New pure/testable `UsageProjection` scopes the growth rate to the CURRENT reset window ONLY: `CurrentWindow` cuts at the most recent reset boundary, detected two ways — the parsed reset-target jumping forward (this week's "Resets Sun 9pm" rolled to next week) OR the percentage dropping >5pts (a window can only climb) — then fits %/hr via ordinary least-squares across ALL current-window points (not an unstable two-point extrapolation). Projection is WITHHELD (logged reason) until the window holds ≥3 points spanning ≥20min with a positive rate, so a wild estimate right after a reset is never shown. The estimate is anchored as an absolute target datetime so the existing display timer ticks it down live between polls exactly like the reset countdown (`OnDisplayTick` now re-emits when a projection is active too); tooltip gains a "Projected 100%: … (~N%/hr)" line. Each stored point AND each computed projection (or withheld-reason) is logged on the existing "usage-meter" ActivityLog channel. MainWindow needed no change — the service still computes DisplayText/ToolTip in full. Verified: full current-working-tree state + these files `dotnet build -c Release` clean in an isolated worktree (0 errors; only the 4 pre-existing CS0067 warnings) — note HEAD itself was momentarily non-compiling from a concurrent session's committed call-site whose definition was still uncommitted, unrelated to this work and since resolved by `842680a5`. Not live-verified in a running GUI here — Shane should watch the "usage-meter" channel and confirm the "~… to 100%" text appears once a few real polls accumulate in a window. | Usage meter time-to-100% projection | `69386734` |
| 2026-08-13 | ✅ DONE — Build Queue panel: added a distinct "🔄 Queued for Restart" tree grouping (MauveBrush, non-selectable header + one row per item) at the top of QueueTree, showing items MainWindow.PendingUpdateQueue intercepted-and-persisted to the local `%AppData%\BuildConsole\pending-update-queue.json` spillover file while a version update sat waiting — so they now read as visually distinct from the real active/pending/running queue rows instead of being indistinguishable from them. New `MainWindow.GetPersistedQueueDisplayItems()` (public static) exposes a read-only `Title`/`GithubNumber`-only DTO off the existing private spillover-file loader for the panel to read directly (no live-DB access, purely local file). Group renders regardless of which filter chip is active (same as the existing offline banner), read fresh from disk on every `RenderQueue` call since the file only changes on a rare Queue-click-during-pending-update or the post-restart replay. Pure display grouping — no new logging wired (none needed). Verified: `dotnet build BuildConsole.csproj` clean (0 errors, only the 4 pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should trigger the existing pending-update-queue interception (Queue click while an update is pending) and confirm the new group renders. | Build Queue panel Queued-for-Restart grouping | `4617da1e` |
| 2026-08-13 | ✅ DONE — Interactive queue-managed builds. QueueWatcherService now launches queue builds (when the new `BuildConsoleSettings.InteractiveBuilds` is on, the default) with BuildConsole owning redirected stdin/stdout: `claude.exe --print --input-format stream-json --output-format stream-json --verbose` (all confirmed-real flags via `claude --help`). The prompt + every Build Watch chat message are delivered as stream-json user messages over stdin, so `@paths` / any chars pass through byte-for-byte (a JSON value, never a shell/commander arg — the whole #767/#820 mangling class can't apply). Live output is kept in a BuildConsole-owned in-memory buffer the Build Watch window pulls directly (replacing the file-tail for these) while STILL teeing the per-item log file so BuildLogView (#802) is unaffected. Each active slot for an owned interactive build gets a real chat Send box → stdin, a Stop (soft stream-json control_request interrupt escalating to `Kill(entireProcessTree)` if unresponsive / on a repeat press — same hard-kill as #1001's Close), a clear three-state indicator (working = blue spinner / paused = muted / waiting-for-input = peach "needs attention" + badge pulse), and lightweight @path autocomplete. Waiting-for-input is detected from a turn's stream-json `result` while the process is still alive. Git #800 auto-completion preserved: an idle build closes its own stdin after `InteractiveIdleFinalizeSeconds` (default 15) so it still exits with a real code; each sent message resets that window. Slot cap stays 8. Send-to-Builder session tracking (#1001, ClaudeAgentsService) untouched; legacy non-interactive launch kept as a settings safety valve. Logs on the "interactive-build" ActivityLog channel: launch, input sent, stop requested, detected waiting-for-input. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). NOT live-verified in a running GUI here — the interactive stdin round-trip / soft-interrupt / waiting-detect need a real queued build + configured Build Tracker API + streaming claude.exe to exercise; the `--input-format stream-json` contract and the CLI's post-`result` linger behaviour are implemented to the documented SDK shape but not exercised in this environment. Shane should confirm on a real queued build. | Interactive Build Watch (stdin ownership + chat input + Stop + waiting detect) | `29c2cdca` |
| 2026-08-13 | ✅ DONE — Build Watch slot content redesign (chat-bubble event cards, color coding, animated thinking spinner, "Reticulating splines" easter egg) — #980/#1000 display layer only | Build Watch slot event-card display redesign | `8ff33eca` |
| 2026-08-13 | ✅ DONE | Git #1003 (bookend restoration) — real commit `3a4ff8a7cd` confirmed: UiTestExecutor.StepCompleted event and its BuildStepCompletedResult are real, and TestRunnerWindow.xaml.cs genuinely subscribes/unsubscribes alongside the other four executors. Bookend was left at IN FLIGHT/TBD. | `3a4ff8a7cd` |
| 2026-08-13 | ✅ DONE — Build completion sound (bundled from real Antigravity IDE install, taskCompleted.mp3, confirmed by ear against antigravityCascadeDone.mp3), mute toggle in top menu bar + configurable sound path in Settings. Wired to QueueWatcherService's new BuildFinished event. `dotnet build --configuration Release` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm the sound plays on a real queue completion and the mute toggle/Settings path picker work. | `976abf8e` |
| 2026-08-13 | ✅ DONE | Git #982 (bookend restoration) — real commit `17e9ea501a` ("Fix collapsed pinned WebView2 covering the editor panes") confirmed - internally labeled "Git #972" in its own commit message (referencing the original pinned-tabs feature issue rather than this specific bug-tracking issue), same real fix. Shane confirmed live: works. | `17e9ea501a` |
| 2026-08-13 | ✅ DONE | Git #1006 — custom title bar (Git #894 style) applied to BuildWatchWindow, TestRunnerWindow, ScreenshotGalleryWindow, ScreenshotReviewWindow, TestHistoryWindow (WindowStyle=None + WindowChrome + dark caption buttons + DWM dark title bar + maximize-respects-taskbar clamp). Shared plumbing extracted to `Services/WindowChromeHelper.cs`; `CaptionButton`/`CaptionButtonClose` styles moved to `Themes/DarkTheme.xaml` as app-level resources; MainWindow refactored to use both (net removed its local duplicate P/Invoke block). LinkedInComposerWindow/StickyNotesWindow deliberately left alone — already WindowStyle=None + AllowsTransparency + Topmost floaties with their own rounded-corner drag-strip + close button, no maximize concept fits. `dotnet build` clean, 0 errors (only pre-existing unrelated CS0067 warnings), rebuilt after every file. Not live-verified in a running GUI here — Shane should confirm each window opens with the dark caption bar and min/max/close all work. | `d66c8258` |
| 2026-08-13 | ✅ DONE — ClaudeUsageMeterService: (1) `UsageUrl` → `https://claude.ai/new#settings/usage` (Shane confirmed this real URL by navigating himself). (2) Replaced the single fixed 1500ms post-nav delay with a real hydration wait (`WaitForHydrationAsync`, polls the DOM every 400ms up to 12s until `[role="meter"]` renders or an auth wall is detected) — the SPA's NavigationCompleted fires long before React paints the meter, which was the root cause of spurious "meter not found". (3) Rich phase-distinguishing ActivityLog diagnostics on the "usage-meter" channel — every failure now logs WHICH phase failed: `FAIL[navigation]` (never loaded), `FAIL[auth-wall]` (login/password page, not usage — logs url/readyState/passwordField/loginText), `FAIL[meter-not-found]` (loaded, no auth wall, meter never appeared — logs url/readyState/bodyLen/resetLabelSeen), `FAIL[meter-malformed]` (meter element present but aria-valuenow/valuetext missing/unparseable — dumps every raw meter's attrs), plus `OK[navigation]`/`OK[read]`. `dotnet build` clean (0 errors, 4 pre-existing warnings). Not live-verified in a running GUI here — Shane can watch the "usage-meter" channel in ActivityLog to see the exact real failure reason on the next poll. | Claude usage meter diagnostics | `33a7becb` |
| 2026-08-13 | ✅ DONE — Double-clicking a leaf in ManifestFilesTree loads it through the exact same path a single click already uses (`LoadManifestLeaf`, factored out of `ManifestFilesTree_SelectedItemChanged`), then immediately fires the same real run path `BtnPlayTest_Click` uses via `PlayTestRequested`. Walks up from the double-click's real hit-test target (`FindAncestorTreeViewItem`) rather than trusting `SelectedItem`, so an area-group header (no `Tag`) just expands/collapses on double-click and never triggers a run. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm double-clicking a leaf both opens the steps flyout and kicks off the real run. | Manifest tree double-click to run | `682d8c70` |
| 2026-08-13 | ✅ DONE — Version status bar + auto-Update button. Scheme v{Major}.{Minor}.{build}: Major.Minor hardcoded (v1.0 in `Services/VersionInfo.cs`), {build} = `git rev-list --count HEAD -- desktop/BuildConsole`. The running build is EMBEDDED at compile time — a new `GenerateBuildInfo` MSBuild target in `BuildConsole.csproj` runs that git command from the repo root before CoreCompile, writes `GeneratedBuildInfo.g.cs` into obj/ and adds it to @(Compile), so `VersionInfo.RunningBuild` knows exactly which build this instance is; `VersionInfo.GetCurrentBuild()` re-runs the same command live off the UI thread. Status bar's Deploy item now shows "Current: v1.0.N" alongside the deploy serial, revealing the running-vs-current diff + an `⬆ Update` button (PrimaryButton) ONLY when behind. Click (`MainWindow.VersionUpdate.cs` → `BtnUpdate_Click`) freshens `BuildQueuePanel.RefreshAsync()` then checks the real queue via the new `BuildQueuePanel.HasActiveQueueItems` (status queued/running): clear ⇒ `Process.Start(deploy-shanesbuild.cmd)` immediately; active ⇒ sets pending flag, shows a loud full-width peach banner, polls every 5s and auto-fires the script the instant the queue drains (no second click). Script's own kill+relaunch replaces the running process, so no post-script handling. Logs to the "version-update" ActivityLog channel on check/request/queue-block engaged+cleared/invocation. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings); generated build number verified embedded (159). NOT run/live-verified in a GUI here (did not execute the deploy script — Shane controls ShanesBuild deploys). | BuildConsole version/update indicator | `e0ed54ac` |
| 2026-08-13 | ✅ DONE | Git #1005 (bookend restoration) — real commit `8ff33ecaf3` ("Build Watch slot event-card display redesign") confirmed: `AddEventBubble` real, tool-call chips (mauve, gear glyph) confirmed in code, chat-bubble card presentation matches Shane's real screenshot. | `8ff33ecaf3` |
| 2026-08-13 | ✅ DONE | Git #1013 — fix CS0104 `Path` ambiguity (System.Windows.Shapes.Path vs System.IO.Path) build break in `BuildWatchWindow.xaml.cs`'s `MatchPaths()`, left by the concurrent Build Watch slot content redesign session, unrelated to my Git #1006 title bar work in the same file. Fully-qualified the two `System.IO.Path` calls rather than removing the `System.Windows.Shapes` using (`Ellipse` from that namespace is used elsewhere in the file). `dotnet build` clean, 0 errors. | `95a2235b` |
| 2026-08-13 | ✅ DONE | TestRunnerWindow Retry button — re-run last manifest in-window without re-selection. | `c6e7f3d1` |
| 2026-08-13 | ✅ DONE | Git #1014 (bookend restoration) — real commit `55428a6c59` confirmed: GetContentAsync() now kicked off immediately inside WebResourceResponseReceived, real elapsed-time diagnostics added, real WebView2Feedback issue references (#3283/#4298) cited, honestly labeled as a diagnostic change rather than a guaranteed documented-constraint fix. Shane confirmed rebuilt. | `55428a6c59` |
| 2026-08-13 | ✅ DONE | Git #1009 (bookend restoration) — real commit `29c2cdcabe` confirmed, thorough: owned stdin/stdout via claude.exe --print --input-format stream-json, chat Send box, Stop (soft interrupt -> hard Kill), three-state indicator, @path passthrough byte-for-byte, slot cap correctly kept at 8 per Shane's correction, #1001 Send-to-Builder explicitly confirmed untouched. | `29c2cdcabe` |
| 2026-08-13 | ✅ DONE | Git #992 (bookend restoration) — real commit `806a0d55e8` ("BuildConsole Home screen: native closable Home tab, session persistence, real-data roll-up") confirmed. Shane confirmed live: all three sections (Where you left off / Running now / Done waiting for you) populated with genuinely correct, real data. | `806a0d55e8` |
| 2026-08-13 | ✅ DONE — Build Watch event-stream visual pass. Genuine visual refinement of the #1004/#1005 chat-card stream, grounded in real Claude.ai chat reference values (assistant line-height ~1.65, rounded-2xl bubbles, flat secondary tool blocks). Prose/error/done cards: line-height 19→23 (~1.6×) for a calmer reading rhythm, corner radius (4,10,10,10)→(5,14,14,14), padding 10,7→13,9, inter-card gap 6→9, plus a very subtle drop shadow (BlurRadius 7 / Depth 1 / Opacity 0.25 — lighter than the app's own 0.45 speech-bubble shadow) to lift each card off the Base background. Collapsed "Ran N tools" chips now read as clearly secondary: transparent fill + thin 1px Surface1 outline pill (flat, no shadow, so they recede against the shadowed filled prose cards — matching how Claude.ai renders collapsed tool blocks) instead of the same Surface0 fill as prose; summary de-bolded (SemiBold→Normal) and chevron dimmed to Overlay. Whole-stream edge margin 8→10. All values from the existing Catppuccin palette; `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Display layer only — not live-verified in a running GUI here; Shane should eyeball a real streaming build. | Build Watch event-stream visual refinement | `6066659e` |
| 2026-08-13 | ✅ DONE | ScreenshotReviewWindow: right-click "Copy Image" context menu (Git #1017) on both BaselineImage and CurrentImage, copying the real rendered BitmapSource to the clipboard via `Clipboard.SetImage()` — same real right-click pattern established in Git #945. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). | `b6ec72fe` |
| 2026-08-13 | ✅ DONE — expect-step DOM retry/polling. UiTestExecutor's `expect` action checked the DOM exactly once (#1016), losing async render/hydration timing races when the prior step's DOM hadn't settled the instant that step returned. `EvaluateExpectAsync` is now a polling wrapper over the extracted one-shot `EvaluateExpectOnceAsync`: it re-checks the live DOM every `ExpectPollIntervalMs` (250ms) across a bounded window and passes the moment BOTH the state check (visible/hidden/present/absent) AND #1016's `textContains` are genuinely observed — instead of a single one-shot check. Default window `ExpectPollTimeoutMs` = 5000ms (in line with the file's CaptureResponseTimeoutMs/ResponseBodyReadTimeoutMs), overridable per step via a new manifest `uiSteps[].timeoutMs` (parsed in TestManifest.cs → ManifestUiStep.TimeoutMs → AutomationAction.TimeoutMs → wired at the call site; non-positive falls back to the default). Logs on the `testing.ui-executor` channel: on pass, how long the poll took + attempt count; on exhaustion, that the whole window elapsed with the condition never observed (and the failing result is annotated with the poll duration/attempts). `dotnet build -c Release` clean (0 errors, 4 pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm on a real manifest run that a previously-flaky expect now settles. | expect-step retry/polling | `90f9c586` |
| 2026-08-13 | ✅ DONE | Git #1019 (bookend restoration) — real commit `6066659e65` ("Build Watch event-stream visual refinement") confirmed. | `6066659e65` |
| 2026-08-13 | ✅ DONE — Build Watch event-stream flat-prose layout. Removed the per-event bordered card entirely (the #1004/#1005/#1019 accent-bar + Surface0 fill + drop-shadow `Border` wrapper): narrative/assistant events now render as continuous flowing prose — a bare wrapping `TextBlock` appended straight into the stream with NO border, background, or per-message container, separated only by a 14px bottom-margin paragraph gap, matching a real Claude.ai assistant turn. Event type is now carried entirely by text colour (blue prose / green done / red error, all existing Catppuccin palette) plus an optional status glyph (✓ done / ⚠ error) — no accent bar needed. The collapsed "Ran N tools" chip (#1012) keeps a light treatment to stay distinguishable as metadata but is no longer a bordered box: dropped the 1px Surface1 outline for a small muted Surface0 inline pill (radius 10, left-aligned, no shadow), the one bit of fill in an otherwise borderless stream. Shane's own typed messages (`AddUserBubble`) deliberately keep their bubble — that's the genuine user turn, exactly as Claude.ai distinguishes it. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings), built in an isolated worktree off origin/main. Display layer only — not live-verified in a running GUI here; Shane should eyeball a real streaming build. | Build Watch flat-prose event stream | `94d81e59` |
| 2026-08-13 | ⏳ IN FLIGHT — passing-assertion response preview (real Truncate'd body instead of bare "ok") | passing-assertion response preview |  |
| 2026-08-13 | ✅ DONE — TestManifestVariableScanner now treats a brace-less `powerShellVerify[].afterStep` string as an implicit `{{NAME}}` reference, mirroring PowerShellTestExecutor's own real wrapping (`afterStep.Contains("{{") ? afterStep : "{{"+afterStep+"}}"`, line 128) — so a config var referenced ONLY via a bare afterStep (e.g. `GRAPH_TEST_TENANT_ID` in `test-manifests/smoke/hello-world-powershell.json`, whose own notes already CLAIMED the #961 scan would surface it — it didn't until now) is discovered, auto-added with the `<unset>` default + needsReview flag. New `CollectAfterStepRefs` scoped strictly to the `powerShellVerify` array (so no stray "afterStep"-named field elsewhere is misread), only accepts values matching the placeholder-name grammar (`^[A-Za-z0-9_]+$`), and defers braced afterSteps to the existing generic `{{...}}` walk. Subject to the SAME per-manifest extract subtraction (an afterStep naming a value an earlier step of the same manifest extracts stays a cross-step value, not config), so the real extract-captured afterSteps in baseline-actions-powershell-verify.json / powershell-verify-example.json / admin manifests are correctly NOT flagged. Exhaustive re-sweep confirmed `afterStep` is the ONLY bare-name-wrapped field: grep of the Services dir shows `"{{" +` wrapping exists solely at PowerShellTestExecutor.cs:128; every other Resolve call site (HttpTestExecutor paths/headers/body, GraphTestExecutor path/body/mailbox/subject, UiTestExecutor selector/value, resolved cmdlet text) passes the raw field through Resolve and so already requires literal `{{...}}` the generic walk catches. afterStep-sourced discoveries reported distinctly on the `testing.config-vars.scan` channel + surfaced via new `ManifestVariableScanResult.AfterStepSourcedNames`. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No DB work needed. | TestManifestVariableScanner afterStep bare-name discovery | `ad9462c8` |
| 2026-08-13 | ✅ DONE — Interactive device-code sign-in flow for powerShellVerify. Read PowerShellTestExecutor's real `RunPwshAsync` device-code/timeout logic first to preserve #967's fast-abort tradeoff. New `DeviceCodeInteraction` handler (with `DeviceCodePrompt`/`DeviceCodeResolution` payloads) is threaded `RunAsync → RunOneAsync → RunPwshAsync`; its presence IS the interactive switch. When supplied (interactive) and `LooksLikeAuthPrompt` fires, we no longer abort: `ParseDeviceCode` (regex over the child's own output snapshot) pulls the real code + `devicelogin` URL, fires `OnPrompt`, and the pwsh process is kept alive up to `InteractiveDeviceCodeWaitMs` (5 min) — Connect-MgGraph is polling, so once Shane signs in it proceeds to the verification cmdlet and exits on its own (no re-run); `OnResolved` reports signed-in/timed-out. The `timeoutMs` ceiling still bounds the PRE-prompt phase in both modes; the deadlock-safe single-statement `lock(sink)` (Snapshot never called under a held lock) is preserved. New non-blocking always-on-top `DeviceCodeWindow` floaty (#937/#973 shape: mono code + Copy + clickable Hyperlink→browser + status line, auto-dismiss on success), wired in `MainWindow.RunManifestAsync` ONLY when `!isRegression && !_testTriggerBusy` — so the #967 scheduled sweep and #898 headless remote trigger pass `null` and keep today's fast-abort verbatim. Logs on `testing.powershell-verify`: device-code detected (code+url)/shown, sign-in succeeded/timed-out-waiting. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No DB work. NOT live-verified in a running GUI here — needs a real interactive run against an expired delegated token to exercise the floaty + browser round-trip; Shane should confirm the code/link appear and the run auto-continues after sign-in. | Interactive device-code sign-in for powerShellVerify | `5cf72dcd` |
| 2026-08-13 | ✅ DONE | Git #1030 — BuildConsole UI cleanup pass. Shane: "Remove buttons that dont do anything... Fix the icons in the left panel... make it prettier... add some animation... Add a icon to the application." Removed the unwired Refresh/Add Item button pair in `Controls/BuildQueuePanel.xaml`'s header. Activity bar icons (`Controls/ActivityBar.xaml`) researched against Microsoft's real Segoe MDL2 Assets glyph list before changing anything: Files/Explorer already used the correct glyph (E8A5=Document, no change); Git/Source Control's emoji fallback replaced with the real VS Code "source-control" codicon reproduced as vector `Path` geometry (Segoe MDL2 genuinely has no branch glyph — confirmed against Microsoft's docs); Tests/Automation now badges Play (E768) + Bug (EBE8), both verified real glyphs. New `Services/UiFadeHelper.cs` (Opacity fade in/out) applied to the Ctrl+Tab tab-switcher overlay, Ctrl+K command palette overlay, and every tab-close path (shared `CloseTab` plus the 4 inline close-button handlers) — tabs fade out instead of vanishing instantly. Generated a real multi-resolution `app.ico` (16/32/48/256px) matching the app's own branding (blue triangle on CrustBrush, same as the in-app logo), wired via `<ApplicationIcon>` in the csproj and `Icon="app.ico"` on every top-level window; verified by extracting the built exe's actual embedded icon. `dotnet build` clean, 0 errors, after every step. Not live-verified in a running GUI here — Shane should confirm the dead buttons are gone, the Git/Tests icons render (not blank boxes), the fade animations feel right, and the taskbar/Alt-Tab icon shows the new branding. Also carries the earlier `deploy-shanesbuild.cmd` obj-cleanup step (Git #1028), re-committed after a concurrent session's `git reset --hard origin/main` silently discarded its first commit mid-session. | `b7a2b4a0` |
| 2026-08-13 | ✅ DONE — hero-headline content assertion redesigned for the real fully-randomized ~50-entry set. Two reusable framework additions + the manifest: (1) `TestRunVariables` extract jsonPath now supports a `[*]` array wildcard (`$[*].leadText`) collecting EVERY match into one `{{name}}`, stored as a `SetDelimiter` (U+001F)-joined blob, recovered via `SplitSet`. (2) `UiTestExecutor` new `expect` field `textPrefixOfAny`: passes when the element's real rendered text is prefix-RELATED (shorter is a prefix of the longer, case-insensitive) to at least one candidate entry — so a partial in-progress typewriter slice of whichever shuffled headline is showing still matches, at essentially every non-empty instant; empty text never matches. Composes with #1024's expect polling and the existing state/textContains halves; wired ManifestUiStep→AutomationAction→MainWindow. Manifest `hello-world-ui.json`: extract full set via `$[*].leadText`→`{{heroLeads}}`, step 3 asserts `textPrefixOfAny ["{{heroLeads}}"]` (retiring the fixed 3-item `textContains`), notes rewritten explaining the randomization and why the old fixed-list approach was wrong. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm a real headless run of hello-world-ui.json now passes the hero step against the live 50-entry set. | `26e99b9e` |
| 2026-08-13 | ✅ DONE — shaneapp://executeSql LOCAL protocol handler. Read the real mybuilder:// registration (`scripts/setup-extension-host.ps1` HKCU `Software\Classes\mybuilder` → `cmd /k … run-claude.ps1 "%1"`) + receiver (`run-claude.ps1` query-string parse) and #898's real HTTP remote-trigger (`admin-test-trigger.ts` POST /admin/deploy/test-run → BuildConsole polls /next → POST /:runId/complete → agent polls /:runId) in full first. Deliberate split kept: SQL stays a LOCAL protocol (runs through this app's own configured `ExecuteSqlAsync` → POST /api/simulator/sql/execute dev-DB path, unreachable to a caller without BuildConsole's local config/token), while test-exec is confirmed to reuse #898 HTTP directly — NO new protocol (documented in new `AGENT_PROTOCOLS.md`). New `Services/ShaneAppProtocol.cs`: per-user named-pipe courier/listener + URI parse (no System.Web dep). URI carries only a short `ref=` to a temp .sql file (payload NEVER inline — sidesteps the #763/#767/#820 URL-quoting-mangling class), optional `resultRef`/`src`; handler reads the file, runs SQL through the existing api client, writes a JSON result envelope (SqlStatementResult[] shape) to `<ref>.result.json`. `App.OnStartup` guarded (only when launched WITH a shaneapp:// arg): already-running instance → forward over pipe + exit with NO window (StartupUri=null, Shutdown); nothing running → cold-start stash drained by MainWindow. Registered via new `setup-shaneapp-protocol.ps1` (HKCU, points at deployed BuildConsole.exe directly — WPF app, so no cmd/console flash). Confirmation-prompt investigation (documented): the "Allow this app to open this link?" prompt is a BROWSER sandbox gate, NOT a Windows gate on ShellExecute — an agent invoking via `Start-Process 'shaneapp://…'` gets NO prompt at all (proven in-repo: BuildConsole already fires mybuilder:// via Process.Start(UseShellExecute=true) with no prompt); browser path is one-time-per-origin with "Always allow". Every invocation logs on the real `sql-runner.protocol` ActivityLog channel (source/action/ref + real outcome). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). No live DB run here (no DATABASE_URL in this env) and no GUI round-trip verified — Shane should run `setup-shaneapp-protocol.ps1` once, then confirm a real `shaneapp://executeSql` invocation executes + writes the result file + logs on the channel. PROVENANCE NOTE: a concurrent session's commit `489b8774` ("Local --notGit pseudo-issue numbering…", Git #1034) won the shared-index race and swept all 5 of my files into ITS commit before I could commit them on my own — already pushed to origin/main, so not safely separable without a destructive force-push of shared history; `489b8774` is the commit that genuinely contains this work (verified: HEAD == my built tree). | shaneapp executeSql protocol | `489b8774` |
| 2026-08-13 | ✅ DONE — full-page screenshot capture in UiTestExecutor. `CaptureScreenshotAsync` only rasterized CapturePreviewAsync's current on-screen viewport, cropping anything below the fold. Now, before every real capture (step-passed/step-failed/navigation-failed — all three route through this one method), a new `GetFullPageHeightAsync` queries `document.body.scrollHeight` and `document.documentElement.scrollHeight` via JS injection (takes whichever is larger, same technique Playwright/Puppeteer use), the WebView2 control's `Height` is temporarily grown to that value, given `FullPageResizeSettleMs` (150ms) to let the reflow/repaint settle, captured, then restored to its original height in the `finally` block regardless of how capture ended (success/timeout/exception) — mirroring #970's `ApplyViewport(null)` restore-no-matter-what pattern. Height query is bounded by its own `FullPageHeightQueryTimeoutMs` (3000ms), same `Task.WhenAny(work, Task.Delay(timeout))` hang-guard shape every other WebView2 await in this file already uses, so a stuck query degrades to a current-size capture rather than stalling the run. The whole resize→settle→capture→restore sequence is one unbroken awaited call from `ExecuteStepAsync`, so it can't interleave with a step's own concurrent DOM work. `dotnet build --nologo` clean, 0 errors (only the 2 pre-existing unrelated CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm a real run's screenshots now show the whole page, not just the visible viewport. | full-page screenshot capture | `0c4b93cd` |
| 2026-08-13 | ✅ DONE — Connect-MgGraph device-code message no longer discarded. The generated verify script piped `Connect-MgGraph -UseDeviceCode -NoWelcome` to `| Out-Null`, which discards the SDK's device-code sign-in message ("To sign in, use a web browser… enter the code XXXXXXXX") — it's written through PowerShell's output pipeline, not a raw console write, so `Out-Null` swallowed it before it ever reached stdout (verified live: same command with `| Out-Null` produces zero output; without it, the real message prints every time). That message is exactly what the already-correct detection chain waits for (`OutputDataReceived` → `LooksLikeAuthPrompt` matches "to sign in"/"enter the code" → `ParseDeviceCode` extracts code + `login.microsoft.com/device` URL → `DeviceCodeWindow` floaty), so on a cold/expired delegated session the interactive prompt never appeared. Fix is the single-line removal of `| Out-Null` (NOT `$null =`/`[void]` — those discard the same pipeline output and reproduce the bug); output now flows through unsuppressed. Confirmed safe on the happy path: a successful silent reconnect with `-NoWelcome` emits nothing, and result parsing is marker-based (`Classify`/`AfterMarker` search for `ResultBegin`/`ResultEnd`/`TenantMarker` sentinels regardless of position), so no stray Connect output can pollute it even if present. No other changes needed anywhere in the chain — the detection/parse/UI wiring was all already correct. `dotnet build` clean, 0 errors. Not live-verified against a real expired token in a GUI here — Shane should confirm the device-code floaty now appears on a cold session. | Connect-MgGraph Out-Null removal | `743168d6` |
| 2026-08-13 | ✅ DONE — Manifest viewer: new `ManifestViewerWindow` reachable from the #952 flyout (new "⤢ View" button) AND from the concurrent #2bbc51eb right-click menu's two previously-stubbed items ("View Run Diagram" / "View Raw JSON", which explicitly deferred wiring "once it lands" — now wired via a `showChartFirst` flag, follow-up commit `4b40a105`). Two read-only views: (1) RAW JSON — the real bytes on disk (`File.ReadAllText` of `SourcePath`, NOT a re-serialized copy) in AvalonEdit (the SAME #939 library already integrated for the SQL Runner — no second highlighter introduced) with a new embedded `Controls/JsonSyntax.xshd` coloring keys/strings/numbers/booleans/null in the Catppuccin dark palette (key rule is a quoted string with a `(?=\s*:)` look-ahead, declared before the value-string rule so first-match-wins picks it), plus string-aware `{}`/`[]` folding (a `FoldingManager` fed by a brace-matcher that skips string contents, folding only multi-line regions — collapsible nested objects/arrays); (2) WORKFLOW CHART — a native WPF `Canvas` flowchart (boxes + drawn `Line`+`Polygon` arrows, NO external diagram library) of the manifest's ACTUAL flow in the REAL fixed order confirmed in `RunManifestAsync` (apiTests→graphTests→postGraphApiTests→zohoTests→uiSteps→powerShellVerify), with implicit setup boxes where a phase implies one (Navigate to `baseUrl` for uiSteps; Connect-MgGraph (delegated) for powerShellVerify, matching `PowerShellTestExecutor.BuildScript`'s real `Connect-MgGraph -UseDeviceCode`), each real step its own box labeled via the new shared `Services/ManifestStepDescriber` (extracted from LeftSidebar's #952 flyout — its call sites re-pointed and the duplicated private `Describe*`/`GetJsonStr` removed, so flyout + chart share ONE summarizer), and captureResponse/extract/textContains/compareField surfaced as colored badges (Shane's "nothing in my UI indicates that it's testing the API"); empty phases draw nothing, a step-less manifest shows a message. `dotnet build -c Release` clean (0 errors, only the pre-existing CS0067 warnings). Pure display — no logging/telemetry/DB. NOT live-verified in a running GUI here — Shane should open a manifest and confirm both views render (JSON colors + fold margins; chart boxes/arrows/badges/setup boxes) via the flyout "View" button and the two right-click items. Work commits: `cae29e62` (viewer window + shared describer) + `4b40a105` (menu-stub wiring). | Manifest viewer (raw JSON + workflow chart) | `cae29e62` |
| 2026-08-13 | ✅ DONE — right-click context menu on ManifestFilesTree leaf nodes, reusing #945's real `new ContextMenu()`/`new MenuItem` pattern (same shape as this file's own `CreateExplorerContextMenu`; the app's implicit dark ContextMenu style applies automatically). Each leaf's menu bakes its own target path into the click closures, so it always acts on the node it was opened from — no reliance on tree `SelectedItem`. Items: (1) **Run** — reuses the exact double-click run path (`LoadManifestLeaf` → `PlayTestRequested`), so it's select-and-run in one click; (2) **View Run Diagram** + (3) **View Raw JSON** — stubbed `IsEnabled=false` with a "Pending — opens in the dedicated Manifest Viewer, which hasn't landed yet." tooltip, because the `ManifestViewerWindow` (workflow chart + raw-JSON tabs) they open is a SEPARATE concurrent IN FLIGHT build (the "Manifest viewer" row above), deliberately NOT a second diagram/JSON implementation here — wire these to the real window once it lands; (4) **Edit** — opens the manifest via the app's standard `FileSelected` → `OpenFileTab` path (JSON lands in the existing Monaco editor tab), consistent with Explorer's own "Open"; (5) **Copy Path** — real absolute file path to clipboard; (6) **Reveal in Explorer** — `explorer.exe /select,"<path>"`, same shape as `CreateExplorerContextMenu`; plus a low-risk **Duplicate** (copies to `<name>-copy.json`, bumping `-copy-2/-3…`, then re-lists). Rename deliberately omitted (a manifest is registered in `_regression-suite.json` by path — a silent rename would orphan that). Area-group headers get their own **Expand All / Collapse All** menu instead (they carry no manifest). Verified: my committed tree builds clean in an isolated worktree at the commit (`dotnet build -c Release`, 0 errors, only pre-existing CS0067 warnings). Pure UI convenience — no logging/DB. Not live-verified in a running GUI here — Shane should confirm the right-click menu appears on a manifest leaf and Run/Edit/Copy Path/Reveal/Duplicate all work. PROVENANCE: committed via the hash-object/update-index plumbing path (never touching the worktree file) to isolate my additions from a concurrent session's simultaneous `ManifestStepDescriber`/`ManifestViewerWindow` refactor of the same file, which an earlier plain `git add` had momentarily swept in — that commit was reset and re-isolated, their uncommitted work left fully intact on disk. | Manifest tree right-click context menu | `2bbc51eb` |
| 2026-08-13 | ✅ DONE — Screenshot Review zoom/pan + aspect-scaled comparison grid. Two connected fixes for full-page (tall) captures. (1) ScreenshotReviewWindow: both panes' `Image`s wrapped in `ScrollViewer`s (Stretch=None + a `ScaleTransform` LayoutTransform) with a real zoom toolbar — shared mouse-wheel + slider + −/＋ buttons + "Fit width" reset, driving a `_zoom` multiplier over each pane's own fit-to-width scale (1.0 = image fills the panel width, scroll vertically to read, instead of a squished illegible sliver); left-drag pans; a "Sync scroll" checkbox mirrors both panes' scroll offset in lockstep for side-by-side diffing. Vertical scrollbar pinned Visible so `ViewportWidth` (and thus the fit-to-width math) is stable and never spawns a spurious horizontal bar. (2) ScreenshotBaselineStore.Compare no longer downscales to a fixed 64×64 before diffing — the grid width stays 64 but the height now scales with the LARGER of the baseline/captured images' real height/width aspect ratio (clamped 64..2048), read cheaply from PNG headers via `BitmapFrame` without full decode. A roughly-square viewport shot still normalizes to ~64×64 (byte-for-byte the old behaviour, backward-compatible); a tall full-page capture gets proportionally more vertical resolution so localized text changes get a genuine chance to clear `MeaningfulDiffThreshold` (unchanged at 6%) rather than being averaged out — the false-negative risk Shane raised. The REAL grid dimensions used (e.g. `grid=64x1180`) are logged per-comparison on the existing `testing.baseline` ActivityLog channel, so it's verifiable/tunable from the log alone. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the zoom/pan/sync feels right on a real full-page screenshot review and eyeball the logged grid dims. | Screenshot Review: full-page zoom + proportional compare grid | `c1ec4302` |
| 2026-08-13 | ✅ DONE | Git #1035 — fix tab drag-and-drop: WebView2 steals mouse capture. Shane: "The document tabs still do not drag and drop. What does is the body of the document... Its like the TAB itself doesnt have the drag. If I use say the Epic document screen I can grab the body of that document and then it drags." Root cause: `AttachTabDragHandlers` never called `CaptureMouse()` on the initiating MouseDown — without it, a drag gesture drifting even slightly off the tab header lets a nearby WebView2 (native HWND) steal all further mouse input the instant the cursor crosses into its bounds, so `PreviewMouseMove` never fires again and `DoDragDrop` never runs. The Epic detail screen (`GitDetailView`) is pure WPF with no WebView2 in it, so it never hit this — exactly matching what Shane described. Fixed with explicit capture on `PreviewMouseLeftButtonDown`, released on a plain click or right before `DoDragDrop` takes over; guarded via a new `FindAncestorButton` so a click on the tab's own close button never gets captured/armed (would otherwise redirect the button's own release-within-bounds Click logic to the ancestor, breaking Close). Also added `tabs.drag` ActivityLog instrumentation (candidate armed / threshold crossed / DoDragDrop result / Drop received) from the investigation, kept for future diagnosability. Ruled out along the way: pinned-tab overlay airspace (Shane confirmed no pinned tab expanded), WindowChrome swallowing content clicks (no WM_NCHITTEST override exists), missing wiring (all 8 tab-creation call sites across 4 files correctly call `AttachTabDragHandlers`). `dotnet build` clean, 0 errors. Not live-verified in a running GUI here — Shane should confirm dragging an ordinary WebView2-backed tab now works and closing tabs via the X button still works normally. | `ccdab1f4` |
| 2026-08-13 | ✅ DONE — Persist Queue clicks made while a version Update is pending; replay them on next launch. Extends the existing queue-gated auto-Update feature (`e0ed54ac`). While `_updatePending`, the `BT_QUEUE_BUILD` handler now intercepts a new Queue click and serializes the exact `QueueBuildAsync` args (title/prompt/model/effort/cwd/githubNumber/blockedByNumbers) to `%AppData%\BuildConsole\pending-update-queue.json` — a JSON array, so multiple clicks during the same pending window all survive — instead of adding it to the live/in-memory queue the `deploy-shanesbuild.cmd` restart would drop. The injected chat button is unstuck (`__btQueueFailed`) and a "Queued after update" MessageBox shown. On next launch (after `_buildTrackerApi` + panels init, fire-and-forget) `ReplayPersistedQueueRequestsOnLaunchAsync` re-queues each saved request via the real `QueueBuildAsync` exactly as a live click, then deletes the file so it never replays twice (file is left in place only if the API isn't configured yet). Both the intercept/persist and the pickup/re-queue log to the `version-update` ActivityLog channel. New partial-class file `MainWindow.PendingUpdateQueue.cs`; two small hunks in `MainWindow.xaml.cs`. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). NOT live-verified in a running GUI here — Shane should confirm on a real pending-update state + Queue click that it saves and re-queues after the restart. | Pending-update queue spillover persistence | `5f26d353` |
| 2026-08-13 | ✅ DONE — Shelve action + Shelf browsing UI. Right-click any editor tab → "Shelve Tab" removes it from the tab bar but keeps its real content genuinely alive off-screen — a DIRECT reuse of the #972/#982 keep-alive, not a second mechanism: new `MainWindow.ShelvedTabs.cs` reuses `PinnedHostCanvas` + `ParkOffscreen` (1×1 at (-20000,-20000), z-index 0; the 1×1 size is the load-bearing #982 HwndHost fix, so a shelved WebView2's CoreWebView2 is never torn down and the session stays warm/logged in). Unlike Pin, a shelved item is NEVER expanded over the panes — it only restores back into the tab bar — so it can't visually interfere with the active tab. Works for ANY tab content (Claude chats / web tools / manifest viewer / SqlRunnerView / plain WPF), reusing OpenFileTab's glyph handling so shelf rows read identically to tabs. New "Shelf" ActivityBar icon opens an anchored popout (same owner/host split as the Web Tools popout #864) listing every shelved item by its real icon+title, each click restoring it as a live tab (SAME session, no reload) in the active pane; a red count badge on the icon shows how many tabs are parked so they're never invisibly lost. Logs shelve/restore/open on the `tabs.shelf` ActivityLog channel with the real tab identity (label + tag). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings), rebuilt against current HEAD. Not live-verified in a running GUI here — Shane should right-click a tab → Shelve, confirm it leaves the bar + the badge increments, open the Shelf icon, and confirm restoring it brings the tab back resuming exactly where it was. PROVENANCE: the `MainWindow.xaml.cs` context-menu hunk (miShelve → ShelveTabFromMenu) was swept into a concurrent session's `git add -A` commit before this session could commit its own files — that hunk was already in HEAD but referenced a `ShelveTabFromMenu` that didn't exist yet, so HEAD did not compile until my 4 defining files landed in `3e3243ab`. | Shelve action + Shelf browsing UI | `3e3243ab` |
| 2026-08-13 | ✅ DONE — What's New patch-notes section on the Home screen (#992): a new light, video-game-style "WHAT'S NEW" section at the top of the native Home tab shows the real first-line git commit titles (`git log --format=%s`, subjects only) for the BuildConsole commits that landed since Shane last launched, as a short bullet list (mauve • bullets, wraps, read-only). It reuses the EXISTING #992 build-number version data — `VersionInfo.RunningBuild` (the `git rev-list --count HEAD -- desktop/BuildConsole` value baked into the assembly) — not a second versioning system; the only new state is a persisted `LastSeenBuild` in `BuildConsoleSettings`. New `VersionInfo.GetNewCommitTitles(sinceBuild, max=15)` anchors to the running build (skips commits newer than it in a repo that moved ahead), takes the RunningBuild→lastSeen window, runs git off the UI thread. `MainWindow.InitWhatsNewAsync` (fire-and-forget at launch) computes it once, renders into the already-open Home tab, then advances `LastSeenBuild` to the current build so the same set never re-shows unless newer commits land; reopening Home mid-session re-renders the launch-time cache. First-ever launch (sentinel `LastSeenBuild == -1`, also the default for a pre-existing settings.json) seeds the baseline silently instead of dumping all history; list capped at 15 with an italic "…and N more" aside. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the section shows the right titles on the next launch after new BuildConsole commits. | Home "What's New" patch notes | `4095ff86` |
| 2026-08-13 | ✅ DONE — Replaced Build Watch's per-slot chat rendering with the high-fidelity inline-transcript design from `design_handoff_claude_cli_chat/README.md`: new `ChatSessionPane` UserControl + `ChatPaneViewModel` (this app's first bound-MVVM code, hand-rolled, no new package), `ItemsControl`+`DataTemplateSelector` transcript over 5 turn types (UserMessage/AssistantTurnStart/AssistantParagraph/ToolGroup/StatusLine), fluid 480→800px responsive sizing via a `FluidSizeConverter` (WPF equivalent of CSS `clamp()`), Lucide-style vector icons (`Resources/Icons.xaml`, replacing emoji/glyphs), and a dedicated `Resources/ChatPaneBrushes.xaml` matching the spec's literal hex tokens. Replaces the imperative `BuildSlotVisual`/`AddEventBubble`/`AddToolCallChip` code-behind construction (net -806/+352 lines in `BuildWatchWindow.xaml.cs`) while preserving all existing behavior (8-slot admit/evict, #943 verifying-hold, interactive stdin input + `@path` autocomplete extracted to `Services/PathAutocomplete.cs`, tool-call grouping, easter eggs, Done/Failed/Stale color-coded outer ring). Also wires REAL context-window token usage into the composer footer: `QueueWatcherService.GetContextTokens` parses the CLI's own stream-json `usage` field (input+cache tokens, the same metric Claude Code's own UI uses) — shown against the real 200k Claude context-window constant, never fabricated; hidden entirely for non-interactive (file-tail-only) slots since that data genuinely isn't available there. Scope calls made honestly: no per-tool-call elapsed timer (not tracked data, dropped rather than faked), no token budget shown for foreign builds. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings in unrelated concurrent-session code). Live-verified via a standalone WPF smoke harness (constructs the real `BuildConsole.App` + `BuildWatchWindow`, not just compiles) that caught and fixed two genuine runtime-only XAML bugs before commit: an unresolvable `RenderTransform.Angle` storyboard path (fixed by moving the RotateTransform into a Style Setter) and a `Run.Text` two-way-by-default binding against a read-only property. Confirmed clean: empty state, resize across the full 480–1400px range, and a fully populated pane exercising every turn type + animation (streaming caret, expanded tool group, spinning status line) with no exceptions. NOT verified against a real live queued build in this environment — Shane should confirm a real interactive build renders/streams correctly end-to-end. | Build Watch inline-transcript chat redesign | `f27d4c90` |
| 2026-08-13 | ✅ DONE — shaneapp://executeSql: execute via a real DIRECT local Postgres connection instead of HTTP → the deployed Replit dev api-server. Correcting the task's premise after reading the code in full: SqlRunnerView has NO local DB connection to "reuse" — its only SQL path is `BuildTrackerApiClient.ExecuteSqlAsync` → POST /api/simulator/sql/execute against the configured `https://…replit.dev/` server (csproj has no Npgsql; config holds only apiBaseUrl+ingestToken). So this ADDS the local connection: new `LocalSqlExecutor` (Npgsql) + `SqlScriptSplitter`, reading a Postgres connection string from a new `databaseUrl` config field or the `DATABASE_URL` env var, executing per-statement (SSMS-style, continue-on-error) into the SAME `SqlStatementResult` shape the envelope already uses. Zero round-trip to the deployed app. Shane To-Do: supply the dev `DATABASE_URL` for the path to work. | shaneapp executeSql local DB | TBD |
| 2026-08-13 | ⏳ IN FLIGHT — shaneapp://uiTest local manifest-run trigger (extends #8's executeSql-only handler; does NOT touch executeSql's SQL path — that's being separately reworked by the concurrent "direct Postgres" session above). Adds a second local protocol action: `shaneapp://uiTest?file=<manifestFilename>` resolves the manifest by bare filename under `test-manifests/` (recursive #964 search, same as the #898 remote trigger) and runs it through the EXACT in-process `RunManifestAsync` pipeline Play Test uses — no HTTP call to #898 at all — then writes the full `ManifestRunResult` (same shape #898 delivers) back to a result file the caller reads. Bulk of the logic isolated in a new `MainWindow.ShaneAppUiTest.cs` partial to minimise shared-file contention; only a small dispatch branch added to `HandleShaneAppUriAsync`. | shaneapp uiTest local trigger | TBD |
| 2026-08-13 | ✅ DONE — shaneapp://runTest (alias uiTest): a NEW local-protocol action that runs a full test manifest IN-PROCESS through the exact `RunManifestAsync` pipeline Play Test / manifest double-click uses (ALL step types — apiTests/graphTests/postGraphApiTests/zohoTests/uiSteps/powerShellVerify, not just uiSteps), resolving the manifest named by `?file=` (bare filename searched recursively under `test-manifests/`, the same resolution #898/#964 use since #960's `{area}/` move; absolute/repo-relative existing paths honored too), then writing a JSON result envelope back for a local CLI agent. Read the real executeSql handler (`HandleShaneAppUriAsync`), `SqlRunnerView`'s SQL path (#939 — confirmed it's `ExecuteSqlAsync`→HTTP, not a "local DB connection" to reuse), and the real `RunManifestAsync` + #898's by-filename manifest resolution in full first. New logic isolated in a NEW partial `MainWindow.ShaneAppRunTest.cs` (handler + manifest/result-path resolution + a self-contained `?file=` query reader) plus ONE minimal dispatch hunk in `MainWindow.xaml.cs`; `Services/ShaneAppProtocol.cs` left byte-for-byte untouched — deliberately, to avoid colliding with the concurrent executeSql-local-Postgres session (row above) that was editing that same file. The run holds the same `_testTriggerBusy` latch #898's remote poll uses: mutual exclusion on the single shared TestRunnerWindow/WebView2 (refuses cleanly with an `ok:false` envelope if a remote OR local run is already in flight) AND makes `RunManifestAsync` take its headless branch (no blocking device-code/screenshot-review modals — correct for an agent polling a result file). Envelope = top-line `ok`/`passedCount`/`stepCount`/`error` a CLI agent branches on immediately, PLUS the full `ManifestRunResult` (same shape `test-results/*.json` + #898 HTTP deliver), written to `?resultRef=` or a predictable `%TEMP%\shaneapp-runTest-<file>.result.json`. Confirmation-prompt Q (task item 4): reused executeSql's already-proven finding — the "Allow this app to open this link?" prompt is a BROWSER sandbox gate, NOT a Windows gate on ShellExecute, so an agent invoking via `Start-Process 'shaneapp://runTest?…'` gets NO per-invocation prompt at all; the SAME one-time `shaneapp://` registration (setup-shaneapp-protocol.ps1) already covers runTest — no new setup. Every invocation logs on the real `sql-runner.protocol` ActivityLog channel (action/src/file + manifest resolved, steps passed/failed, elapsed ms, result path, or the exact reason it couldn't run). #898's HTTP remote-trigger endpoint is UNCHANGED — kept for genuinely remote/CI callers; `AGENT_PROTOCOLS.md` updated (new §2 documents runTest, #898 moved to §3 "remote/CI"). Built + committed in an isolated worktree off origin/main to stay clear of the concurrent session's uncommitted `MainWindow.xaml.cs`/`ShaneAppProtocol.cs`/csproj(Npgsql) changes; my dispatch hunk AUTO-MERGED cleanly with their executeSql-body change on rebase, and I resolved one `AGENT_PROTOCOLS.md` table conflict (kept THEIR local-Postgres executeSql row + my two test rows). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings) — verified BOTH standalone and after rebasing onto the concurrent executeSql-local-Postgres work (combined tree compiles). Never ran live DB queries (no DATABASE_URL here). NOT live-verified in a running GUI — no real protocol round-trip exercised in this environment; Shane should confirm a real `shaneapp://runTest?file=…` invocation runs the manifest, writes the result file, and logs on the channel. | shaneapp runTest local manifest trigger | `da950265` |
| 2026-08-13 | ✅ DONE — DeviceCodeWindow real-usage fixes: (1) XAML `Topmost="True"` alone wasn't reliably keeping it above the maximized Test Runner window once that window activated, so re-assert `HWND_TOPMOST` via `SetWindowPos` (Win32) on every `Deactivated`, `SWP_NOACTIVATE` so it doesn't steal focus back; (2) clicking the devicelogin hyperlink launched the system default browser via `Process.Start` — switched to `MainWindow.OpenWebTab` (via `Owner as MainWindow`), the same in-app tab mechanism every other web interaction in BuildConsole already uses, so sign-in stays inside the app instead of a separate un-synced browser session. `dotnet build` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified against the actual Test Runner window here — Shane should confirm the floaty now stays on top and the devicelogin link opens as an in-app tab. | DeviceCodeWindow topmost + in-app devicelogin link | `3db2d4b0` |
| 2026-08-13 | ✅ DONE — TestHistoryWindow discoverability. The window worked but had no way to reach it. Two entry points: (1) general/unfiltered — already existed from a prior session as Menu > Run > "Test History…" (`OpenTestHistory_Click`/`EnsureTestHistoryWindow`), confirmed still wired. (2) per-manifest filtered — new "🕒 History" button in the manifest steps flyout (`Controls/LeftSidebar.xaml`), raising a new `ManifestHistoryRequested` event carrying the manifest's Issue number; MainWindow subscribes and calls `EnsureTestHistoryWindow(issue)`. `TestHistoryWindow.Refresh(int? issueFilter)` filters both the Runs list and Per-Manifest Streaks to that Issue, with a "Show All" button + filter banner to clear back to the full history. ManifestFilesTree still has no right-click context menu in this tree (a separate, not-yet-landed task), so the per-manifest entry point lives in the existing steps flyout instead. `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the flyout History button opens the window filtered correctly and Show All clears it. | TestHistoryWindow discoverability (flyout History button + issue filter) | `842680a5` |
| 2026-08-13 | ✅ DONE — pause-on-unset-variable. Read the real #953/#961 discovery-and-review layer first: `TestEnvVar.NeedsReview`, the scanner's `AutoDefaultValue` (`<unset>`) sentinel, and `TestRunVariables.Resolve` (the real substitution point where a config var wins before a per-run extracted value). Problem: a `{{NAME}}` whose stored Test Environment Variable is still `<unset>`/needsReview previously resolved to the literal "<unset>" and shipped it downstream → confusing later failure. Now: (1) `SeedConfigVariables` records those names in a `_needsRealValue` set; `Resolve` REFUSES to substitute one (falls through to an extracted value if any, else treats it as unresolved → the existing `VariableNotResolvedException` naming it) instead of sending "<unset>". (2) New async `TestRunVariables.PrepareAsync(params inputs)` gate, called at the top of EACH step in every executor that takes `vars` (HttpTestExecutor.RunOneAsync, GraphTestExecutor.RunOneAsync + RunMailPollAsync, UiTestExecutor.ExecuteStepAsync, PowerShellTestExecutor.RunOneAsync — Zoho doesn't take vars) BEFORE its synchronous Resolve calls: for each referenced still-unset name (not already extracted, not already declined this run) it awaits the new `OnMissingVariable` bridge. Because resolution runs on the WPF UI thread, a synchronous blocking wait would freeze the app, so this mirrors PowerShellTestExecutor's device-code pattern exactly — a non-blocking `Task`-returning prompt, awaited without blocking the UI thread. (3) New `MissingVariableWindow` floaty (WindowStyle=None, Topmost, ForceTopmost on deactivate, drag-strip header — same shape as `DeviceCodeWindow`/#937/#973): shows the variable name + a value TextBox + Save&Continue/Dismiss; its `Result` Task completes with the entered value or null on dismiss/Esc/close (never hangs). (4) On a real value: stored into the run's config layer (wins for this step AND every later `{{NAME}}` this run), and persisted back to the Settings store via `PersistConfigVar` (BuildConsoleSettings.Load→update/add→Save, needsReview cleared — exactly as if set in Settings). (5) Dismissal / non-interactive run (no bridge) → the step fails clearly naming the variable. Bridge wired in `MainWindow.RunManifestAsync` ONLY for interactive runs (`!isRegression && !_testTriggerBusy`), same gate as the device-code floaty; headless/#967/#898 runs keep fail-clearly-on-unset. Logs PAUSE / RESUME / FAIL (dismissal) each with the real variable name on the `testing.config-vars` channel. `dotnet build -c Release` clean (0 errors, 4 pre-existing CS0067 warnings). No DB work. NOT live-verified in a running GUI here — Shane should confirm on a real Play-Test run of a manifest referencing an `<unset>` var that the floaty appears, filling it saves to Settings + resumes, and dismissing fails that step clearly. Note: regression/headless runs that reference an `<unset>` var now FAIL clearly (previously they silently sent the literal "<unset>") — a correctness improvement, not a regression. | pause-on-unset-variable prompt | `084daf9b` |
| 2026-08-13 | ✅ DONE — bottom panel live resource meters: two progress bars — real system-wide CPU % via `PerformanceCounter("Processor","% Processor Time","_Total")` (WMI deliberately avoided, see this file's own earlier note on it hanging indefinitely on this machine), real system-wide Memory used/total via kernel32 `GlobalMemoryStatusEx` — added in a new strip (`BottomPanelGrid` row 0) above the existing `BottomTabs` TabControl (row 1), both now toggled together by `SetBottomPanel`. New `ResourceMeterBar` Catppuccin-themed slim ProgressBar style in `Themes/DarkTheme.xaml`. Polls on a 1.5s `DispatcherTimer` from a new `MainWindow.ResourceMonitor.cs` partial, started from the constructor, disposed on `Closed`. Added `System.Diagnostics.PerformanceCounter` NuGet package (not in BCL since .NET Core; Windows-only, fine for this Windows-only WPF app). `dotnet build -c Release` clean (0 errors, only pre-existing CS0067 warnings). Not live-verified in a running GUI here — Shane should confirm the two bars show real, changing CPU/Memory numbers. | bottom panel CPU/Memory meters | `d478cced` |
| 2026-08-13 | ⏳ IN FLIGHT — Build Watch per-slot live task checklist side panel: parse the agent's own narrative output for genuine checkbox markers (☐/☑/✓/✔ and markdown `- [ ]` / `- [x]`), NOT plain bullet lists, and surface them as a live checklist alongside the event stream — pending items added as first seen, flipped to a green check when the agent later reports the same item done (a checked marker, or a close text-similarity match to a tracked pending item). | Build Watch task-checklist side panel | `TBD` |
| 2026-08-13 | ⏳ IN FLIGHT — Git #876 (deeper fix): Git Board GitHub polling still burning rate limit "for no reason". Investigate the real current polling, then (1) only poll while the Issues view is visible AND the window isn't minimized, (2) drop the background cadence from 20s to a much slower interval (minutes) while keeping manual Refresh / view-switch / window-reactivation instant, (3) add ETag / If-None-Match conditional requests so an unchanged poll returns a free 304 that costs 0 against the rate limit, especially on the N-per-open-issue blocked_by sweep, (4) log real per-call 200-vs-304 + rate-limit-remaining so before/after frequency is verifiable. | Git Board polling: focus-gate + slow interval + ETag conditional requests | `TBD` |
| 2026-08-13 | ⏳ IN FLIGHT — Build Watch live-stream responsiveness/threading fix. Shane: interactive build slots "are not staying live and hanging the main app when they do catch up." Investigate the real interactive queue-managed render path (owned stdin/stdout, QueueWatcherService in-memory buffer, BuildWatchWindow slot pull + UI dispatch) in full before fixing — root-cause the freeze, not guess. | Build Watch render pump (chunked/yielded output flush) | `TBD` |
| 2026-08-13 | ✅ DONE — Web Tools popout (#864) real favicons instead of generic glyph. Read the real popout (`Controls/ActivityBar.xaml.cs` BtnWebTools_Click) and its `WebToolEntry`/default-seeded-tools shape (`Services/BuildConsoleSettings.cs`) in full first: the "generic default icon" is a Segoe MDL2 glyph TextBlock (usually the empty-string default, one explicit `` for Git), never an image. New `Services/FaviconCacheService.GetIconPathAsync(url)`: resolves the entry's domain, tries Google's public favicon-resolution service (`https://www.google.com/s2/favicons?domain=…&sz=64`, handles a site's real declared `<link rel="icon">` without HTML parsing) then falls back to `https://{domain}/favicon.ico`, caches the bytes to `%AppData%\BuildConsole\favicon-cache\{domain}.png` (fetched once per domain, re-served from disk on every later popout open — no re-fetch per app launch), and returns null (never throws) on any failure. `BtnWebTools_Click` now renders the existing glyph immediately for instant feedback, then `LoadFaviconAsync` swaps a 16x16 `Image` in for it once the real favicon resolves; a failed/slow fetch just leaves the glyph in place — never a broken image. Applies to every entry in `settings.WebTools` generically (LinkedIn + the seeded Git/Analytics/Clarity entries today, and any entry Shane adds via Settings going forward) — no per-site hardcoding. `dotnet build` clean (0 errors, only pre-existing CS0067 warnings). No DB work, no wire logging needed (pure visual polish, per task). Not live-verified in a running GUI here — Shane should open the Web Tools popout and confirm LinkedIn/Git icons swap from the generic glyph to their real favicons shortly after opening. | Web Tools popout real favicons (#864) | `f95629ef` |
| 2026-08-13 | ✅ DONE — Universal title-bar search bar. Read the real title-bar layout, the real Files/Git/Manifest tree data sources, the real Build Queue data source, and the real Chats data in full first. Replaced the static center "Shane McCaw MSP • Antigravity IDE - {doc}" text with a real 🔍 search box that spans the center of the title bar (primary), and repurposed the EXISTING Ctrl+K command-palette overlay (which had been running on hardcoded FAKE chat/build/automation entries) into an anchored, grouped-by-source results dropdown under the bar. Live-filters as Shane types over the app's REAL in-memory data — no separate duplicate index: Git Board issues/epics via `LeftSidebar.CurrentBoardIssues`, milestones via new `CurrentMilestones` (over `_milestones`), test manifests via new `CurrentManifests` (projected from `_manifestEntries`), the Build Queue via new `BuildQueuePanel.CurrentQueueItems` (over `_lastItems`), and Chats via new `CurrentBoardChats` (over `_lastBoardChats`); Files read the same repo filesystem the Explorer tree lazily renders (there is NO complete in-memory file list to reuse — the tree is lazy — so it's a once-per-session background enumeration, query-filtered + capped, not a persisted index). New `LeftSidebar.WarmSearchSources()` reuses the real Populate* paths to warm Git/manifests/chats when empty (Git only auto-polls while its own view is visible, #863). Results are grouped with a small header per source (VS Code Quick-Open style, via a `ListCollectionView` + `PropertyGroupDescription`) and each selection navigates by REUSING the real existing open mechanism — `OpenFileTab` (files + manifests), `OpenGitDetailByNumberAsync` (issues/epics, resolves from cached board), `OpenMilestoneDetailTab`, `OpenChatTab`, and a new `BuildQueuePanel.RevealQueueItem(id)` that reveals the queue column + selects/scrolls the row via the panel's own filter/render path — never new per-source logic. Ctrl+K now focuses the title-bar box (single input; the old modal's own search box + fake-data `BuildPaletteItems`/chip filtering were removed, ~206 lines cut from MainWindow.xaml.cs); new logic isolated in a new `MainWindow.UniversalSearch.cs` partial to stay clear of the concurrently-edited MainWindow.xaml.cs. Keeps the real branding: "Shane McCaw MSP" stays top-left by the ▲ logo (logo tooltip updated too), the active-document context is kept as a small secondary label to the right of the search box (ActiveDocTitleText/DebugBuildMarkerText preserved for their ~dozens of code-behind call sites); only "Antigravity IDE" specifically is dropped from the center. Airspace handled the proven way (a WPF overlay can't render over a WebView2 HwndHost, so the active WebView2 is hidden while the dropdown is up and restored on close). No wire logging (pure navigation/search, per task). No DB work. `dotnet build -c Release` clean (0 errors, only the 4 pre-existing CS0067 warnings) — built + committed in an isolated worktree off origin/main and 3-way-rebased onto three concurrent pushes (their BuildQueuePanel "Resume" context-menu + QueueWatcherService change merged cleanly with my accessor since they're far apart; combined tree re-verified compiling). NOT live-verified in a running GUI here — Shane should confirm: typing in the title-bar box shows grouped live results, each result opens the right thing (file/manifest tab, Git detail tab, milestone tab, chat tab, queue row selected), Ctrl+K focuses the box, and the branding still reads right. | Universal title-bar search bar | `36ad88eb` |
| 2026-08-13 | ✅ DONE — Build Watch/Queue interactive build "Resume" (unstick after network loss). Read the real interactive queue-managed build path in full first (`QueueWatcherService` owned stdin/stdout, `WriteUserMessage`/`WriteInterrupt`, the three-state `InteractiveInputState` indicator, `BuildWatchWindow` Send/Stop wiring). Investigated the stuck state before implementing: when WiFi drops mid-turn the in-flight API request hangs on a half-open TCP socket (no RST/FIN → the socket read blocks until the OS TCP retransmit timeout, minutes later), and while blocked mid-turn the CLI is NOT reading stdin for a new turn — which is exactly why the existing Send box (more input) couldn't reach it, matching Shane's report. So the real unstick is an **interrupt**, not more input: new `QueueWatcherService.RequestResumeAsync(int id)` sends a soft stream-json `control_request`/`interrupt` (the same confirmed-real primitive Stop already ships) to ABORT the hung in-flight request, waits a short settle window for the aborted turn to unwind, then writes a short "Network connection was restored. Please continue where you left off." user message so a FRESH turn re-attempts now the network is back — deliberately NEVER escalating to a kill (that stays Stop's job; the whole point is to keep the process alive). Reachability: added as a right-click "⏵ Resume (unstick after network loss)" `MenuItem` in the existing Build Queue tree context menu's `running` branch (`Controls/BuildQueuePanel.xaml.cs`) — a network-stalled build stays queue-status `running`, that tree is the app's actual right-click surface (the Build Watch slot only has Send/Stop buttons, no context menu), and `BuildQueuePanel` already owns the `QueueWatcherService`. Gated on `_watcher.OwnsInteractive(item.Id)` since it needs this instance's owned stdin (a foreign/legacy watcher's process can't be reached). Wire logging (`interactive-build` channel): logs Resume invoked, and the real observed outcome — RESUMED (new output after the continue message), STILL STUCK (no output within the observe window, e.g. network still down), or EXITED. `dotnet build -c Release` clean in an isolated worktree (0 errors; only the 4 pre-existing CS0067 warnings) — verified in a worktree because two FOREIGN in-progress files in the live tree (`StartupLoadingView.xaml.cs`, another session's untracked WIP) don't yet compile; my two files were the only ones committed. No DB work. NOT live-verified against a real network-stalled build here — Shane should confirm on a genuinely paused build that Resume unsticks it and the `interactive-build` log shows the RESUMED/STILL STUCK/EXITED outcome. | Build Watch Resume (network-loss unstick) | `70a7d56e` |
| 2026-08-14 | ✅ DONE — Pending-update queue re-queue on next launch is lost. Shane confirmed the interception dialog ("saved, will queue after restart") appeared, the app restarted, but nothing was actually added to the live queue. Read the real feature in full first (`MainWindow.PendingUpdateQueue.cs` write+replay, the line-424 launch wiring in `MainWindow.xaml.cs`, `deploy-shanesbuild.cmd`, `BuildTrackerApiClient.QueueBuildAsync`/`TrackAsync`, `ActivityLog`). Ruled out the two simple hypotheses with evidence: the read side IS wired (`_ = ReplayPersistedQueueRequestsOnLaunchAsync()` runs on every launch, whole feature landed in one commit `5f26d353` so it was never "unwired"), and there is NO path mismatch (both sides use `%AppData%\Roaming\BuildConsole\pending-update-queue.json`; the file wasn't on disk at investigation time, consistent with the replay having run and deleted it). Real root cause = cause #3, silent-swallow: the replay POSTs each persisted request to the dev api-server in the first moments of relaunch (the dev server auto-sleeps ~every 15 min and is routinely cold exactly then), then DELETED the spillover file UNCONDITIONALLY right after the attempt (old line 200) — so a POST that failed against a cold/asleep server silently discarded the build forever, which is precisely the reported symptom. And `ActivityLog` is in-memory only (raised to a panel, wiped on every restart, and drops lines when no subscriber is attached), so a past failure left zero forensic trail — why it needed another live bug report. Fix (`23b71230`): (1) a request is removed from the file ONLY once it genuinely queues (a real 2xx) — anything that didn't queue (thrown/cold-server or a non-2xx) is KEPT and the file rewritten with just the remainder (`SavePersistedQueueRequests`), so the next launch retries it automatically and nothing is ever silently dropped; if the trimmed-file rewrite itself fails, the ORIGINAL file is left intact (a harmless duplicate next launch beats a lost build). (2) A small bounded per-item retry (`TryReplayOneAsync`, 3 attempts 4s apart) gives a cold-starting server a chance to wake on this very launch. (3) Durable, append-only diagnostic log `pending-update-queue.log` (next to the JSON under `%AppData%\BuildConsole\`, via `PendingUpdateQueueDiag` — best-effort, lock-guarded, never throws, still mirrors to the live `version-update` panel): logs whether the spillover file was found on launch, its RAW contents (captured before parse so a corrupt file is still visible), the parsed count, each item's real per-attempt HTTP status/body or exception, and the final keep-or-delete decision — so a recurrence is diagnosable straight from the file. Also logged: the write-side interception, the previously-silent count==0/corrupt-file delete, and the not-configured leave-in-place path. Verified in an isolated worktree off HEAD (`dotnet build -c Release` — 0 errors, only the 4 pre-existing CS0067 warnings) because two FOREIGN untracked in-progress files in the live tree (`Controls/StartupLoadingView.xaml*`, another session's WIP) don't yet compile; committed my one file only via explicit pathspec, foreign `MainWindow.xaml`/StartupLoadingView left untouched. No DB work. NOT live-verified through a real pending-update deploy restart here — Shane should confirm on a genuine Queue-during-pending-update that after the restart the build appears in the live queue, and that `%AppData%\BuildConsole\pending-update-queue.log` shows the found/contents/re-queue-outcome trail. | Pending-update queue re-queue durability + durable diag log | `23b71230` |
| 2026-08-14 | ⏳ IN FLIGHT — Build Watch live Task Checklist side panel. Read the real event-stream render (`BuildWatchWindow` AppendEventCard / ApplyInteractiveEvent) in full first, then add a side panel that extracts a live task checklist from the agent's own reported progress: detect real checklist markers (☐/☑/✓/✔ or markdown `- [ ]`/`- [x]`), add each newly-seen unchecked item as pending, mark it done with a green ✓ when the agent later reports the same item complete, matched by straightforward text-similarity with honest notes on where it can misfire. | Build Watch Task Checklist side panel | `TBD` |
| 2026-08-14 | ✅ DONE — Git Board GitHub traffic reduction (#876 reopened). Shane: "heavy GitHub API traffic for no reason", repeatedly crossing the 5,000/hr REST limit. Read the real polling paths in full first. Findings: (1) the Git Board (LeftSidebar Issues view) 20s `_pollTimer` was gated on the Issues view being visible (#863) but NOT on window visibility, so it hit GitHub every 20s all day even while BuildConsole was minimized/hidden — per cycle: `ListBoardIssuesAsync` (GraphQL, separate points budget), `GetMilestones` (REST, already 5-min cached #876), and `EnrichBlockedStatusAsync` (REST, one call PER open issue, already 3-min throttled #876); (2) BuildQueuePanel fired three `gh`-CLI GitHub reads (in-flight/To-Do/completed) unconditionally every 60s regardless of visibility (~180 REST calls/hr all day); (3) no conditional requests anywhere — every REST poll re-fetched full bodies and counted against the limit even when nothing changed. Fix: only poll while the host window is genuinely on screen (`Window.GetWindow(this)` present + IsVisible + not Minimized) — applied to the Git Board timer AND the three BuildQueuePanel gh timers (initial immediate paint on Initialize kept); Git Board background GitHub refresh slowed from 20s to a real 2-min cadence (`GitBoardBackgroundPollInterval`) decoupled from the 20s chat tick, with tab-switch (SwitchView) and the manual Refresh button still repainting immediately; ETag conditional requests added to every REST GET read (milestones, per-issue blocked_by sweep, sub-issues, single issue, comments, numeric search) via a new static ETag cache + `GetConditionalAsync<T>` in GitHubApiClient (static because a fresh `new GitHubApiClient(pat)` is built per poll) — an unchanged resource now returns 304, which GitHub does not count against the rate limit, so a steady-state poll is largely free; GraphQL board fetch left as-is (POST, cannot use ETags, separate points budget). Logging: GitHubApiClient tracks REST GETs sent vs. 304-not-modified and logs a before/after summary on the `git-board.traffic` channel each background poll, plus a one-time startup line stating old (20s, even minimized) vs. new (2-min, visible-only, ETag) cadence. Verified: built clean in an ISOLATED worktree at HEAD overlaid with only my 3 changed files (the shared tree had a concurrent session's in-progress StartupLoadingView that does not compile) — `dotnet build -c Release` 0 errors, only the 4 pre-existing unrelated CS0067 warnings. HONEST boundary: not exercised in a live running GUI here (WPF, no headless GUI, and the shared tree wouldn't build) — Shane should confirm on a real run that the git-board.traffic log shows the drop and the board still updates promptly when the Issues tab is focused. | Git Board rate-limit reduction | `74b467ee` |
| 2026-08-14 | ⏳ IN FLIGHT — Non-blocking dialog/notification engine (ToastEngine): one reusable, dark-themed, non-blocking toast/notification system generalized from DeviceCodeWindow's proven floaty pattern (borderless always-on-top window, center mid-top of the work area, never ShowDialog / never steals focus / no system alert sound, multiple stacking toasts with a soft cap), a simple static API (ToastEngine.Show(title, message, kind)), and migration of the scattered genuinely-informational MessageBox.Show() calls to it (leaving the crash-handler and in-modal form-validation MessageBoxes as intentional blocking exceptions). | Non-blocking toast/notification engine | TBD |
| 2026-08-14 | ✅ DONE — What's New: filter out session-bookend commits. Read the real What's New implementation (VersionInfo.GetNewCommitTitles + MainWindow.InitWhatsNewAsync + HomeView.RenderWhatsNew) in full first. Real problem: raw bookend/tracking commits (BUILD_LOG:/PLATFORM_BUILD: IN FLIGHT/DONE rows, and the looser Bookend:/bare IN FLIGHT: phrasings seen in real history) were showing up alongside the real feature commit for the same change, creating duplicate noise for a single real change. Fix: new VersionInfo.IsBookendCommitTitle (regex covering all observed bookend title shapes) filters these out inside GetNewCommitTitles before the list reaches the UI; MainWindow's more-count calc reworked to derive from totalNew/max-cap directly instead of post-filter titles.Count, so filtered-out bookends no longer inflate the "+N more" indicator. Pure filtering logic, no wire logging needed. Build clean (0 errors, only the pre-existing unrelated CS0067 warnings). No DB work. NOT live-verified in a running GUI here — Shane should confirm the Home screen What's New list shows only real feature titles next launch. | What's New bookend-commit filtering | `54da9707` |
