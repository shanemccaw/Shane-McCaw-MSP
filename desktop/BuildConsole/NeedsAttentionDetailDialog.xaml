<Window x:Class="BuildConsole.NeedsAttentionDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Needs Attention Details" Height="520" Width="640"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BaseBrush}"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource MantleBrush}"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="IconText"
                           Grid.Column="0"
                           Text="🔴"
                           FontSize="24"
                           Margin="0,0,12,0"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="HeadingText"
                               Text="Needs Attention Details"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"/>
                    <TextBlock x:Name="TimeText"
                               Text="Recorded today at 9:00 AM"
                               FontSize="11"
                               Foreground="{StaticResource Subtext1Brush}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="1"
                Background="{StaticResource MantleBrush}"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="SUMMARY"
                           FontSize="10"
                           FontWeight="Bold"
                           Foreground="{StaticResource Subtext1Brush}"
                           Margin="0,0,0,4"/>
                <TextBlock x:Name="SummaryText"
                           Text="Description of failure..."
                           FontSize="12"
                           Foreground="{StaticResource TextBrush}"
                           TextWrapping="Wrap"
                           LineHeight="18"/>
            </StackPanel>
        </Border>

        <!-- Diagnostics / Error Details -->
        <Border Grid.Row="2"
                Background="{StaticResource CrustBrush}"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="2,0,2,6">
                    <TextBlock Text="DIAGNOSTIC DETAILS &amp; LOGS"
                               FontSize="10"
                               FontWeight="Bold"
                               Foreground="{StaticResource Subtext1Brush}"
                               VerticalAlignment="Center"/>
                    <Button x:Name="CopyBtn"
                            Content="📋 Copy Logs"
                            HorizontalAlignment="Right"
                            Padding="8,2"
                            FontSize="10"
                            Click="CopyBtn_Click"/>
                </DockPanel>

                <TextBox x:Name="DetailsBox"
                         Grid.Row="1"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource TextBrush}"
                         FontFamily="Cascadia Code, Consolas, Courier New"
                         FontSize="11"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <!-- Footer Actions -->
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Button x:Name="DismissBtn"
                    Content="✓ Dismiss from List"
                    HorizontalAlignment="Left"
                    Padding="12,6"
                    Click="DismissBtn_Click"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="OpenBtn"
                        Content="🔍 Open in Runner / Log"
                        Style="{StaticResource PrimaryButton}"
                        Padding="14,6"
                        Margin="0,0,8,0"
                        Click="OpenBtn_Click"/>
                <Button Content="Close"
                        Padding="14,6"
                        Click="CloseBtn_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
