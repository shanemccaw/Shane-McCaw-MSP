Category identity glyph — never a plain colored dot for a named category (dots are reserved for severity/legend meanings).

```jsx
<PillarIdentity color={PILLAR_COLORS.Security} icon={PILLAR_GLYPHS.Security} label="Security" variant="chip" active />
```
