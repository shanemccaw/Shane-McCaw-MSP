repo: shanemccaw/Shane-McCaw-MSP
branch: main

## Last sync
date: 2026-09-14T12:58:00Z
tree: 434a30b929b0

### Updated in this project
- `Team Management.dc.html` gained the Billing / Customer Admin role surface (#4013 / #4031 — a Design gap, not a code fix): the roster read on `main` already returns `isCustomerAdmin` / `hasBillingRole` per member (#3647) and `PATCH /portal/team/:userId/role` takes `{ role: "customer-admin" | "billing", granted }`. Drawn: role chips on roster rows, a ROLES block on the member panel with Held / Not held per role, what each grants (Customer Admin = billing.view + billing.manage + team.manage + changes.approve + marketplace.browse-full; Billing = the two billing capabilities only, per the #3629 migration), grant/remove confirm modals naming the audit action and role key, the self-revoke block with the server's own reason, and the #3629 trigger fact that Billing is auto-granted to whoever a bill is addressed to (a removal stands until the next bill). Read-only in the viewer scene. Ledger: only the two platform keys are offered (400 otherwise), gate is the live team-management grant, no last-admin floor added, 503 drawn as unavailable not denied; the stale "roster does not carry Reports to" line corrected (#3996 projects `managerUserId`)
- Projects board: five fixed columns that scroll sideways inside the card (was auto-fit, which orphaned COMPLETED onto a second row under 810px); template preview items moved out of Backlog into their own "Coming up" strip so no column towers
- Built `Projects.dc.html` from the projects pack (#1737 for #1570) on the one customer route, `GET /api/portal/projects/:id` (+ its kanban-events SSE): project header (type / status / free-text phase / MSP-computed progress), signed contract, SharePoint folder, coupon line drawn as earliest-invoice-only (the pack's sum-vs-single finding), four workflow steps on the real enum incl. `blocked`, five-column board incl. `review` (DB enum, missing from the admin TS cast) with `waiting_on_customer` highlighted, `previewTasks` as dashed template projections in Backlog, updates on the 4-kind enum, documents, sent-only retainer status reports with the `pendingStatusReport` review flag pointing to My Architect. The closure request (#4025) is drawn as an honest dead end — real request, real email link, no customer sign-off route — not a fake form. Scenes: live / review-pending / closure-requested / not-found (404 — today's answer for every id, `projects` = 0 rows; also states there is no list route) / failed
- Earlier this sync: `github_compare b368710e0dd6...main` under docs/portal/: 3 files added, nothing else changed. Built `POAMs.dc.html` (POA&Ms #4021 for #1935; pack §7.1 stale — `authorizedBy` now replayed on read in `portal-poams.ts`) and `Status Reports.dc.html` (#3889 for #3435). All three wired into `Shell.dc.html` (POA&Ms after Risk Register, Status reports after My Architect, Projects after Offers)
- Correction from Shane: #1570 is NOT deferred — the pack's own v1.2 framing is out of date. Standing rule from here: every pack under docs/portal/ gets a screen regardless of milestone
- 37 packs held flat under docs/; every one now has a screen
- Built `POAMs.dc.html` from the POA&Ms pack + the live `portal-poams.ts` (6 routes): newest-first list on the real `WirePoam` shape, all six status words drawn as the server's own (incl. `converted_to_risk_acceptance` and `cancelled` as MSP-side finals, `completed` explained as never-written), derived overdue only on active plans / pending milestones, live target vs write-once original date, current Accountable holder beside the point-in-time "backed at signing" block, read-only milestones, the typed-name + literal-true checkbox signature ceremony with the #1511 role gate (holder / 403 not-holder / 409 no-holder, tweak `signerRole`), soft delete with the reason-always rule and a UI-only warn→type-the-code ladder (stated as UI-only, #1696/#1704), request-acceleration as a queued operator review, and the customer create form (server-derived scope, server-assigned code, always `pending_signature`). Scenes: live / empty / tier-gated (402 — the CURRENT state for every real tenant, drawn with the honest "raise anyway, can't read it back" path) / failed
- POA&Ms pack §7.1 is stale against `main`: the filed `signed.authorizedBy: null` gap is fixed in the live route (list uses `resolveRiskAuthoritiesBatch`, get-one uses `resolveAuthorizedByAsOf`; `toWirePoam` takes a 4th `authorizedBy` arg). Screen draws the replayed point-in-time holders on every read and names the pack as behind the code in its ledger. §7.2 (inline scoped lookup on GET one) still stands, cosmetic
- Built `Status Reports.dc.html` from the status-reports portal pack + `portal-status-reports.ts` (4 routes, matches the pack): published-only list newest-period-first (a late-entered report drawn sorting by its period), report detail on the narrowed customer `reportToWire` (no state/customerId/authoredByUserId), `authoredByName: null` drawn as "Unknown operator", two-sided comment thread oldest-first with `authorType` chips, composer on the real 1–10,000 body rule, 201 append, the "Failed to add comment" 500 as its own scene (tweak `commentOutcome`), 404 drawn as one indistinguishable answer for foreign/draft/unknown, honest empty. Ledger states the §0 disambiguation (not the My Architect retainer status-reports card) and the §1.4 unverified customer→operator notification
- `Shell.dc.html`: sidebar gained POA&Ms (after Risk Register), Status reports (after My Architect) and Projects (after Offers); header titles, page flags and mounts added

## Previous sync (2026-09-14T02:40:47Z)
tree: b368710e0dd6
- Packaged `design_handoff_portal_modules_and_mobile/` for Claude Code: the 16 module screens, updated Shell, Mobile Preview, 19 screenshots, the 16 packs, this file as sync-record.md, and a README with per-screen honesty notes and the mobile behaviour spec (760px shell breakpoint, drawer, fluid rules). `design_handoff_full_site/README.md` update log points at it
- `github_compare b6cb2221d593...main` under docs/: no changes. All 33 `docs/portal/*-contract-pack.md` files re-pulled and byte-compared to the held copies — identical; no new pack has landed. `alert_preferences.md` is now also held under docs/ (it was the only portal file not mirrored). Nothing rebuilt
- Non-portal packs upstream (not customer-portal surfaces, not built here): 38 under docs/msp-console/, 8 under docs/buildconsole/, docs/email-templates-contract-pack.md, docs/marketing/{website,marketing-command-center}.md, docs/admin-panel/admin-panel.md

## Previous sync (2026-09-14T02:36:36Z)
tree: e04c6e182dd7
- Read-only check of upstream `docs/` (this turn): confirmed the 16 new screens were built from `docs/portal/*-contract-pack.md`, not the repo's root `docs/` (system docs, SQL, pillarmapping, one MSP `email-templates` pack). `docs/portal/` has since grown to 34 files at this tree — not yet diffed or pulled; next sync
- Mobile pass, no upstream content involved: Shell measures its own width (<760px → sidebar becomes a drawer behind a header menu button, settings stack, popovers clamp to the viewport); 30 screens made fluid (container-relative page padding, master/detail grids stack, auto-fit minimums capped at 100%, wide tables scroll inside their card, stat strips wrap); Resources Page gained a mobile menu (<860px); `Mobile Preview.dc.html` shows the Shell / Resources / Sign-in at 390×844

## Previous sync (2026-09-14T01:36:00Z)
tree: b6cb2221d593
- Built the 16 customer-portal modules that had a contract pack under docs/portal/ but no screen: Break-glass Access, Consent and Onboarding, Data Rights and Privacy, Diagnostics and Scripts, Documents, Email Authentication Setup, My Architect, Offboarding, Offers and SOW Acceptance, Public Share Pages, Requests and Support Chat, Resources Page, Runbooks, Scope and SLA, Signup Agreement and Invite, Team Management. Each is a single DC on the portal's dark chrome with a `scene` tweak over the real response states its pack documents, and a "what this page deliberately does not do" ledger citing pack sections
- Honesty carried onto screens from the packs' own findings: the live-rendered PDF path that 502s (#2507, Documents); the structurally unreachable script download (#2506, Diagnostics); the SLA reading that evaluates an empty set (#2513, Scope and SLA — drawn with a visible caveat, not an all-clear); the add-on misclassified as a signup tier (#2509, filtered out and named); `effectivePriceCents` being dollars (#2511, Offers); the list/detail Zoho-outage mismatch (#2512, Requests); the platform agreement being MSP↔platform, not a customer terms record (§9b, Offers — no fake checkbox gate); no requester confirmation email or status for deletion requests (Data Rights); the {pending:false} three-state collapse (Break-glass)
- Resources Page is the marketing-site page (Epic #1093), built from the live Resources.tsx copy verbatim and the 7 real article frontmatters in src/content/articles/, on the new tokens (blue→teal gradient, dark canvas) — copy untouched per the pack's tone note
- Shell.dc.html: sidebar gained Diagnostics, Documents, Offers, My Architect, Service & scope, Requests; the Runbooks nav item now mounts the new Runbooks DC (cycle model, run history, hold-window decisions) instead of SOPs' runbooks view; user menu gained Team, Break-glass access, Leaving; Settings → "Privacy and your data" now mounts the real Data Rights DC (replacing the hand-drawn export/delete cards) and a new "Email authentication" tab mounts that DC
- The 16 packs are now held flat under docs/ alongside the original 17 (33 packs held)

## Previous sync (2026-09-14, earlier)
- No screen rebuilt. Of the 17 packs held, only billing changed, and it's a documentation-only correction (#3782, explicitly "read-only, no product/schema/UI changed"): the pack originally recorded `invoicesTable.amount` as a decimal-dollar exception to the platform's cents rule, then found that #1610 migrated it to integer cents ~1.5h after the pack closed (2026-09-03), so the exception never applied to the live code Billing.dc.html was built against. Also corrected: one non-synthetic-looking but still testbed `client_services` row exists (not zero) — has no linked Stripe subscription, so §1.6's "nothing to wire the tier cards to" conclusion is unchanged. Billing.dc.html already renders amounts as pre-formatted strings and derives paid/pending from a boolean, matching the corrected pack exactly; no code in the screen assumed decimal-dollar math

## Previous sync (2026-09-11)
- Upstream reorganised the contract packs into module folders — every pack this project uses now lives at `docs/portal/<name>-contract-pack.md`, with MSP-console siblings under `docs/msp-console/` and buildconsole packs under `docs/buildconsole/`. All path references in this file's Screen map are updated; the copies held here keep their flat `docs/` names
- Only one pack changed in substance: webhooks. #3525 landed as `DECIDED` — replay has no backend and none will be built, and the pack now cites this project's own Webhooks.dc.html as the resolution (the `d.terminal` branch's "Terminal after 3 attempts. There is no replay — this event is gone." and the matching ledger line). Section 9 was retitled CURRENT vs. open vs. DECIDED. Screen needed no rebuild — it already draws exactly what the pack now records; the ledger line gained "(#3525, decided)" so the page states the gap is settled rather than merely unfiled
- The other six changed packs (auth-core, change-control, notification-preferences, policy-decisions, remediation-tracking, shanebot) differ from the held copies only in cross-reference paths rewritten for the new folder layout. The remaining ten are byte-identical. Previous copies under docs/_prev/

## Previous sync detail (2026-09-09)
- No screen rebuilt: all 17 contract packs this project holds are byte-identical to upstream at this tree, so no contract of record moved
- Source-level check found one real change against a built screen — `portal-security-plan-document.ts` gained `GET /api/portal/security-plan/drift` at #3027, the customer-scoped reuse of the same `computeSecurityPlanDrift` the MSP console already served. Security Plan.dc.html's section 7 ("Changes since signing") already drew exactly that comparison, and drew `hasLastSignedVersion: false` as its own never-signed state, so the route makes the section genuinely customer-readable rather than changing what it says. The security-plan pack is stale on #3027 (its route table lists only the MSP-side `/drift`); noted, not built around
- Also audited and found unchanged in effect: `msp-standing-policies.ts` is still MSP-only (no customer read of standing policies — Policy Decisions.dc.html's "separate and not shown here" note stands), and the new `msp-policy-decisions.ts` is an MSP-console surface whose header settles under #3035 that no MSP-side create for `policy_decisions` will be built — which is what the customer screen already draws (create is the customer's own signed action). The tenant-authored obligation gap the page states ("no route exists yet, for you or your MSP") is still real

## Sync history
- 2026-09-14T12:58:00Z — Built Projects.dc.html after Shane's correction that #1570 is not deferred; wired into Shell. Rule recorded: every docs/portal pack gets a screen regardless of milestone
- 2026-09-14T12:42:00Z — 3 new portal packs (POA&Ms #4021, Projects #1737, Status Reports #3889). Built POAMs.dc.html + Status Reports.dc.html, wired into Shell. POA&Ms pack §7.1 found fixed on main (authorizedBy now replayed on read)
- 2026-09-14T02:40:47Z — Checked for new packs: none under docs/ since b6cb2221d593; portal copies identical; alert_preferences.md filed under docs/
- 2026-09-14T02:36:36Z — Verified pack provenance (docs/portal, not root docs); upstream now e04c6e182dd7 with 34 portal packs (not pulled). Mobile-friendly pass across Shell + 30 screens + Resources; Mobile Preview added
- 2026-09-14T01:36:00Z — Built all 16 remaining docs/portal packs as screens (Break-glass, Consent & Onboarding, Data Rights, Diagnostics, Documents, Email Auth, My Architect, Offboarding, Offers/SOW, Public Share Pages, Requests, Resources, Runbooks, Scope & SLA, Signup/Agreement/Invite, Team); wired into Shell; 16 packs added under docs/
- 2026-09-14T01:03:10Z — Checked all 17 held packs; only billing changed, a doc-only correction (#3782: amount is integer cents, was never decimal-dollar in the live code; one non-zero but Stripe-less client_services row). No screen changed
- 2026-09-11T03:10:15Z — Docs reorganised upstream into docs/portal/ + docs/msp-console/; 7 packs refreshed, 6 of them path-only. Webhooks #3525 DECIDED (no replay backend, ever) — screen already correct, ledger line updated to name the decision
- 2026-09-09T19:08:20Z — Verified all 17 packs byte-identical upstream; no rebuild. #3027 customer drift route found (Security Plan section 7 now backed by a customer route); pack stale on it. #3035 confirms no MSP-side policy-decision create
- 2026-09-08T15:20:14Z — Rebuilt Change Control against the returned #2989 pack; reconciled Policy Decisions with #1722; #2827/#2869 on Remediation Tracking; #1168 tier gates on Ownership RACI and Policy Decisions; #3049 on Overview
- 2026-09-06T15:10:00Z — Rebuilt Configuration State against the #2899 regeneration (fresh snapshot 110 numbers, real 10-workload roll-up, the new #2759 change-attribution verdict roll-up)
- 2026-09-06T14:49:00Z — Built the #2949 customer sign action into Security Plan; refreshed 3 stale contract packs (security-plan, configuration-state, account-security); re-verified the other 13 packs already match upstream
- 2026-09-05T01:13:00Z — Redesigned Overview against the #2922-regenerated pack (overviewCounts, six cross-Feature roll-ups)
- 2026-09-04T22:47:00Z — Built Overview.dc.html from the #2446 pack + live routes (pack stale on #2500 and §4)
- 2026-09-04T19:40:00Z — Rebuilt Remediation Tracking against the regenerated #1719 pack (15 routes); updated Ownership RACI's honest-empty contract; refreshed 15 contract packs
- 2026-09-03T22:10:00Z — Built the six Auth Core pre-auth screens from the #2441 contract pack
- 2026-09-03T20:05:00Z — Built ShaneBot.dc.html from #1740; docked it as the shell's chat widget
- 2026-09-03T19:10:00Z — Built Billing.dc.html from the #1598 pack; wired Shell user menu
- 2026-09-03T18:05:00Z — Updated Account Security.dc.html with the tenant-wide Graph readings
- 2026-09-03T17:20:00Z — Built Security Plan.dc.html from the #1731 pack; wired Shell sidebar
- 2026-09-03T16:35:00Z — Updated SOPs.dc.html against the regenerated #1493/#1488 pack
- 2026-09-03T15:40:00Z — Built Risk Register.dc.html from the #1487/#1712 pack; wired Shell sidebar
- 2026-09-03T14:12:00Z — Built Webhooks.dc.html from #1597 pack; wired Shell user-menu item
- 2026-09-03T11:47:00Z — Rebuilt Change Control, Policy Decisions, Ownership RACI against upstream
- 2026-09-01T21:05:00Z — Read docs/portal/configuration-state-contract-pack.md (#1844) + Settings-as-container addendum (#1596/#1933); built Configuration State.dc.html; reworked Shell Settings into a module-grouped container
- 2026-09-01T20:40:00Z — Read docs/portal/sops-contract-pack.md; built SOPs.dc.html (SOP hub + Active Runbooks) and wired both Shell nav items
- 2026-09-01T18:47:07Z — Read docs/portal/account-security-contract-pack.md (#1595); built Account Security.dc.html
- 2026-08-30T04:48:53Z — Built Portal Alerts.dc.html — alerts dropdown (#1821) + alert-preferences page
- 2026-08-30T00:09:00Z — Built Pillar Pages.dc.html from docs/pillarmapping.md (#1481)
- 2026-08-30T00:03:03Z (commit 176fbd7d8d79) — Checked journeyTokens.ts / journeyMotion.ts; no functional changes, no screens rebuilt
- 2026-08-29T21:39:58Z — Read design tokens to ground the shell design system; built 3 shell nav-structure variations

## Previous sync detail (2026-09-08)
- Change Control.dc.html rebuilt against the regenerated pack (#2989, 2026-09-06 — the pack deleted on 2026-09-03 is back, re-extracted, 2 → 14 customer routes + a 3-route settings surface): maintenance windows with the raise-time containment check, collision refusal and blocked-by / blocks dependencies (#1504, drawn on a new CR-2026-116 → CR-2026-118 edge), attachments with the four real kinds (#1503), change metrics under the unavailable-never-zero rule (#1506), the customer settings surface (policy switches, live-computed approver list, seven fixed notification rules), the fail-closed split (register GETs answer empty, actions 409) and risk discharge at capture time. The three gaps the pack filed are drawn where they bite: #3044 (catalogue execute skips freeze/window/collision checks), #3045 (metrics ignore PIR close codes), #3046 (SLA breach computed, never escalated). Rollback and CAB/PIR/execution stated as MSP-side only; moving a booked change stated as having no route
- Policy Decisions.dc.html reconciled with the #1722 regeneration (replaces the 2026-08-29 pack wholesale): #1168 tier-gated read state added (create stays ungated, drawn as such), unresolvable tenant drawn as a true empty rather than a failure, catalogue corrected to the real 8 seeded rows (PCI DSS and ISO 27001 A.5.18 as certifications, ISO added), the customer's own authorities (insurance schedule, records schedule) demoted to text citations because no route anywhere can create a tenant-authored catalogue row (the pack's own finding), and the pack's §9 open question answered on the page: risk-derived positions stay on the Risk Register, this page holds freestanding positions only, nothing merged
- Remediation Tracking.dc.html: #2827 closed upstream (checklist PUT now refuses accepted_risk) and #2869 built — the findings checklist gained its own signed decline-to-risk action (mirrors the programme's, 409 on a repeat), ledger corrected to one cosmetic rough edge (export label). Ownership RACI.dc.html: #1168 tier-not-included state (Premier-only visibility, writes untouched, nothing removed) and the #1518/#1524 delegation decision (keep as-is) stated in place. Overview.dc.html: overviewCounts gained raciPendingAcceptance (#3049) — the RACI count is now a real field, scoped by customerId, so it still reads when tenantScope is null; the no-scope state now zeroes six of seven
- Packs refreshed from upstream: change-control, policy-decisions, remediation-tracking, ownership-raci, configuration-state (#2819 attribution wording only — no screen change), security-plan (legacy portal_security_plan* tables now dropped — no screen change; the screen never claimed otherwise), customer-home-and-timeline (now held locally for the first time). The other 10 packs this project uses are byte-identical to the copies already here. Previous copies of the six replaced packs kept under docs/_prev/

## Previous sync detail (2026-09-04 → 2026-09-06)
- Security Plan.dc.html: the #2949 customer review-and-sign action (`POST /api/portal/security-plan/versions/:versionUid/sign`) — a sealed-but-unsigned version shows a typed full-name (+ optional title) panel; signing attaches a signature to the sealed version and never edits plan content; a version is signed once, by whichever side lands first
- Configuration State.dc.html rebuilt against the #2899 regeneration: live snapshot 110 (1,092 targeted, 151 answered, 50,464 objects, 13.8% readable), the real 10-workload roll-up (PublicDNS new, Teams mostly-failed as the honest signal), and the #2759 attribution verdict roll-up (attributed / accepted risk / contested / unattributed / ignored) with the 100%-unattributed state explained as zero CRs or accepted risks for the tenant, not a broken engine
- Overview.dc.html redesigned against the #2922 pack: `overviewCounts` (rbdWaiting, rbdActive, microsoftChangesThisWeek, changeScheduleThisWeek gated on the change_control add-on, remediationInProgress via remediationTerminalState, policiesExpiringSoon via reviewState) as linked tiles; `no_tenant_scope` and unentitled change_control as first-class real-0 states; page restructured onto eyebrow-labelled bands; no score bars, stated in place; priority items on the built #2500 query
- Remediation Tracking.dc.html rebuilt against the regenerated #1719 pack (6 → 15 routes, eight of nine DECIDED threads built): findings-derived checklist keyed by check with the #1539 fix-route dimension and the write-consent ceiling; the 28-step programme with accepted_risk, derived terminalState, READY_STATUSES counting a signed acceptance, pointed re-verification (#1540), published-only guide, #1542 decline-to-risk sheet; #1543 bypass correlation as an observational read; #1541 reveal gate fail-closed with four outcomes; coverage gap corrected to three steps
- Ownership RACI.dc.html: real tenantScoped:false contract (workloads, Microsoft changes, change requests absent for a named reason while purchased services, hold windows and people still serve), plus three honesty entries (decline stops at the assigner #2527, accepting a gap is session-only, fixed reason on assignment writes); surfaces C/D not drawn
- Packs: 14 existing + microsoft-changes refreshed; ten changed only in provenance notes; docs/portal/change-control-contract-pack.md was deleted upstream at that point and Change Control was left untouched until a regenerated pack existed (it now does — see Last sync)

## Previous sync detail (2026-09-03)
- Auth Core built from uploads/auth-core-contract-pack.md (#2441, part of #1648/#1485) as six separate pre-auth DCs, one per screen — Auth Sign-In, Auth MFA Challenge, Auth Forgot Password, Auth Reset Password, Auth Account Setup, Auth Sign-In Help. Each carries a real-response-state switcher over the branches the routes actually return, not a happy path only: sign-in's 401 (credentials and no-passwordHash sharing one message), 423 lockout with lockedUntil, and the 200 mfaRequired handoff that issues no session; the MFA screen as TOTP + bypass with the single-use/atomic/audited distinction stated and the sms-only and passkey-only accounts drawn as an honest not-wired dead end (backend real, no caller); forgot-password's unconditional 200 explained as enumeration resistance with the two invisible branches (1h reset vs 72h setup, entitlement-gated) shown separately from the response; reset-password issuing no session and collapsing invalid/used/expired into one message; account setup on the real setup-context shape (greeting, role-derived destination, up to 4 real purchases) with the double entitlement gate as its own 409 state and the distinct 404 account-not-found case; sign-in help with the four server-authoritative issueKeys carrying their fixed priority and routing note, the honest attach contract (no per-attempt log exists — counter + lock state + last ten successful sign-ins), and the full /status draw the old page never made: derived platform status, 90-day incident strip, incidents list, and both Graph discriminated unions rendered as cannot-read rather than clean results
- No white-label branding variant and no "does not do" ledger on these six, per the build brief
- Notification Preferences.dc.html built from docs/portal/notification-preferences-contract-pack.md (#2552): the real 15-category vocabulary with CATEGORY_INFO's label/description copy and the delivery-not-detection banner read from customer-notifications.tsx at portal-archive-2026-08-29 and carried forward verbatim, in-app and email as the only two channels with email structurally dependent on in-app (both the UI constraint and the backend's own), the §5/§6 gating truth stated plainly — muting a category suppresses the bell entry, the email AND the webhook fan-out, and no record of the suppression exists anywhere — silent email failure named, the always-15-rows shape shown as an all-default state rather than a blank form, and the §8 boundary with customer_alert_preferences stated with a link instead of merging the two taxonomies
- Shell.dc.html: added it as a second Settings tab beside Alert preferences (the two registers stay separate)
- ShaneBot.dc.html built from docs/portal/shanebot-contract-pack.md (#1740, regenerating the pre-#2519 pack): full-page chat surface with the four real Active Card renderers (invoice / subscription / score / data-answer) as the built #2519 library renders them, specific-card-first behaviour per #1624 with data-answer named as the fallback, an authorized-but-empty card answered in prose instead of an empty table, unrecognised card types rendering nothing, escalation as a one-way handoff with no ticket read-back, and the ephemerality contract stated in the surface itself — no persistence on either side, reload starts empty; the five unbuilt card types are listed as not-yet-offered rather than mocked
- Shell.dc.html: the bottom-right ShaneBot dock now mounts the real ShaneBot component (compact mode) instead of its hand-mocked transcript; Billing user-menu entry wired; removed an orphaned menu div
- Billing.dc.html built from docs/portal/billing-contract-pack.md (#1598): receipts list on the real WireInvoice shape (date/what/ref/amount/status/downloadable, no-PDF rows explained rather than linked), the #1463 three-state honesty contract with zero-invoices as a real live answer distinct from a failed read (dataState carried on a source marker attr), Stripe billing-portal handoff as the one live plan-state action, monitoring tiers kept explicitly illustrative with no prices — per-seat metering and the unresolved Premier rate stated as the reason — and no hypothetical repricing controls; retainer money kept off the page per the §7 boundary
- Account Security.dc.html updated against the regenerated docs/portal/account-security-contract-pack.md (#1595 + #1593's build): real per-user failed-portal-sign-in state added beside the password card (the user's own row, always answerable, with lock status), plus a new "Your Microsoft 365 tenant" section carrying the three tenant-wide Graph readings as honest discriminated states — password age read (18 of 24 accounts stale, oldest 2014), tenant failed sign-ins and device compliance both shown as cannot-read with the licensing reason named, never as clean results; the tenant-wide vs your-own-login boundary is stated in copy, and sessions still carry no compliance label
- Security Plan.dc.html built from docs/portal/security-plan-contract-pack.md (#1731, superseding #1495): designed on the assembled/versioned/signed model rather than the stale hand-typed table — a sealed snapshot with its scope statement carried beside the signature, the four authored prose sections marked rewritten vs carried forward, the seven source modules shown in each module's own status vocabulary with no rolled-up score anywhere, drift against the last signed version (status and detail only, prose explicitly not compared), version history with one current version at a time, honest never-sealed state as its own first-class case, and a ledger covering the no-outcome-narrowing and all-or-nothing business-unit limits
- SOPs.dc.html updated against the regenerated docs/portal/sops-contract-pack.md (#1493/#1488): library rebuilt on the real 15-row seed set incl. the four #1560 lifecycle procedures (IAM-04–07, with the honest note that nothing fires them automatically yet), per-tenant custom-step overlay (#1558) shown as appended "yours" steps frozen against a base version and never affecting runnability, run provenance (#1556 origin: policy / lifecycle / remediation / by hand) and the frozen sop_version on every queue row, and run history reframed to the real state — zero runs, so History shows only version publications and the queue explains what would start one
- Risk Register.dc.html built from docs/portal/risk-register-contract-pack.md (#1487, regenerated #1712): the three lifecycles (risk status / acceptance / review) kept as three separate columns, likelihood-impact heat map with the unplottable row named, per-line accept with the #1511 role-based authority gate (no Accountable holder = blocked with a route to Ownership, non-holder = named holders), write-once permanent signature with no expiry and no renewal, point-in-time authorizedBy on signed lines, #1512 document version chain (needs-signing on scope expansion vs signature carried forward), risk-derived decisions shown as their own section explicitly separate from the policy_decisions register (§0.1), and a 12-row does-not-do ledger
- Webhooks.dc.html built from docs/portal/webhooks-contract-pack.md (#1597): honest-empty as the default real state, one-time secret moment (create + rotate) with no reveal afterwards, real delivery log (status/attempt/code/snippet/next retry) with the 200-row ceiling and unservable payload stated, client-computed health relabelled Untested for zero-delivery endpoints, wired rotate/edit/delete/pause (real endpoints), replay dropped as unbuilt, subscribable event list replaced with the route-declared 13 + named bus events (signal.fired flagged as never dispatched), alert-taxonomy claim corrected, and a "what this page does not do" ledger of the 11 open gaps
- Shell.dc.html: user-menu Webhooks item wired to the new page
- Change Control.dc.html rebuilt against the now-built approval model: real approve/reject/decline, per-stage ledger with SLA + breach + separation of duties, tenant signature floor, live freeze calendar, standard-change catalogue, per-CR timeline and comments, booked-window instants (date-ordered "next window" when present); rollback and rescheduling remain the only unbuilt actions, CAB noted MSP-side only
- Policy Decisions.dc.html rebuilt on the own-table policy register (GET/POST /portal/policy-register): signed at creation with no draft state, five fixed review cadences with real due dates, dependency-clearance decisions (licence-SKU observable vs manual mark-resolved), typed authorities (regulation/certification/contract/insurance/internal_schedule) as first-class citations; Policy Engine tab reframed from "nothing exists" to built-MSP-side
- Ownership RACI.dc.html rebuilt: multiple holders per role (A ordered, informational), decline as a recorded resting state, append-only per-cell history, per-customer acceptance gate (loose/strict, symmetric across the MSP boundary), real workload rows derived from enabled service plans with the tracking toggle and its untracked-while-enabled finding

## Screen map
| Screen | Repo files |
|---|---|
| Shell Explorations.dc.html | lib/copilot-scan-scene/src/journeyTokens.ts, lib/copilot-scan-scene/src/journeyMotion.ts (+ uploads/design-system-brief.md) |
| Shell.dc.html | lib/copilot-scan-scene/src/journeyTokens.ts, lib/copilot-scan-scene/src/journeyMotion.ts (+ uploads/design-system-brief.md) |
| Microsoft Changes.dc.html | uploads/design-system-brief.md + Shell.dc.html chrome (page brief pasted in chat) |
| Pillar Pages.dc.html | docs/pillarmapping.md (§1–§9.1), docs/design-system.md + Shell.dc.html chrome |
| Security Plan.dc.html | docs/portal/security-plan-contract-pack.md (#1731/#1495, #2949; legacy-drop correction 2026-09-06; pack stale on #3027) + artifacts/api-server/src/routes/portal-security-plan-document.ts (#2949 sign, #3027 customer drift) + artifacts/api-server/src/routes/msp-security-plan.ts, portal-security-plan.ts, src/lib/security-plan-assembly.ts, security-plan-versioning.ts, security-plan-drift.ts, security-plan-prose.ts, security-plan-draft.ts, lib/db/src/schema/msp.ts (msp_security_plan_versions, msp_security_plan_drafts) |
| Notification Preferences.dc.html | docs/portal/notification-preferences-contract-pack.md (#2552) + artifacts/api-server/src/routes/notification-preferences.ts, routes/notifications.ts (CATEGORY_STYLES), src/lib/notification-center.ts, lib/db/src/schema/index.ts:826-839 (customerNotificationPreferencesTable), lib/db/src/schema/msp.ts:8404-8436 |
| ShaneBot.dc.html | docs/portal/shanebot-contract-pack.md (#1740/#1616) + artifacts/api-server/src/lib/shanebot-engine.ts, shanebot-persona.ts, chat-content-blocks.ts, routes/support-chat.ts, lib/zoho-desk.ts, artifacts/portal/src/pages/support.tsx, components/support-chat/cards/* (ActiveCard, Invoice, Subscription, Score, DataAnswer, types, card-status), lib/chat-content-blocks.ts |
| Billing.dc.html | docs/portal/billing-contract-pack.md (#1598) + artifacts/api-server/src/routes/portal-billing.ts, src/lib/stripe.ts, msp-portal/src/components/portal-v2/billingWire.ts, billingLive.ts, billingData.ts, receiptWire.ts, receiptLive.ts, lib/db/src/schema/index.ts (invoicesTable, clientServicesTable, servicesTable) |
| Risk Register.dc.html | docs/portal/risk-register-contract-pack.md (#1487/#1712) + artifacts/api-server/src/routes/portal-risk-register.ts, portal-rbd-document.ts, public-rbd-document.ts, src/lib/risk-authority.ts, rbd-versioning.ts, tenant-workloads.ts, alert-engine.ts, lib/db/src/schema/msp.ts (msp_risk_decisions, risk_instances, msp_rbd_versions) |
| Webhooks.dc.html | docs/portal/webhooks-contract-pack.md (#1597) + artifacts/api-server/src/routes/webhooks.ts, src/lib/webhook-delivery.ts, src/lib/event-bus.ts, lib/db/src/schema/msp.ts (outbound_webhooks, outbound_webhook_deliveries) |
| Configuration State.dc.html | docs/portal/configuration-state-contract-pack.md (#1844, regenerated #2899; #2819 attribution note 2026-09-06) + Shell.dc.html Settings container |
| SOPs.dc.html | docs/portal/sops-contract-pack.md (#1493/#1488, regenerated 2026-09-03) + artifacts/api-server/src/routes/portal-sops.ts, portal-runbooks.ts, msp-sops.ts, src/lib/sop-execution.ts, portal-sops.ts, portal-hold-windows.ts, portal-runbook-cycles.ts, lib/db/src/schema/msp.ts (msp_sops, msp_sop_runs, portal_sop_custom_steps, portal_runbook_*) |
| Account Security.dc.html | docs/portal/account-security-contract-pack.md (#1595, regenerated with #1593; #2840 addendum) + artifacts/api-server/src/routes/portal-account-security-graph.ts, routes/mfa.ts, routes/auth.ts, src/lib/account-security-graph.ts, session-tracking.ts, lib/db/src/schema/msp.ts (user_sessions, mfa_enrollments) |
| Auth Sign-In.dc.html / Auth MFA Challenge.dc.html / Auth Forgot Password.dc.html / Auth Reset Password.dc.html / Auth Account Setup.dc.html / Auth Sign-In Help.dc.html | uploads/auth-core-contract-pack.md (#2441) + artifacts/api-server/src/routes/auth.ts, routes/mfa.ts, routes/portal-sign-in-help.ts, routes/public-status.ts, routes/msp-custom-domain.ts, src/lib/zoho-desk.ts, session-tracking.ts, lib/db/src/schema/index.ts, schema/msp.ts (mfa_enrollments, mfa_bypass_codes, account_setup_tokens, password_reset_tokens, msps, users) |
| Overview.dc.html | docs/portal/customer-home-and-timeline-contract-pack.md (#2446/#1655, regenerated #2922 and #3049) + artifacts/api-server/src/routes/portal-customer-engines.ts (GET /portal/dashboard), portal-customer-timeline.ts, src/lib/doc-gate-coverage.ts, engine-registry.ts (ENGINE_DEFS), tenant-signals.ts (resolveCustomerUserIds), portal-customer-scope.ts, portal-addon-entitlements.ts, portal-change-maintenance.ts, portal-message-center.ts, remediation-tracker-terminal-state.ts, portal-ownership.ts (personIdForUser), lib/db/src/schema/msp.ts (msp_diagnostic_runs, msp_diagnostic_findings, msp_risk_decisions, policy_decisions, portal_ownership_assignments), schema/index.ts (tenant_engine_snapshots, insights_generated_documents, sales_offers, projects, kanban_tasks, client_services, invoices, reports, notifications, messages) |
| Portal Alerts.dc.html | docs/design-system.md, docs/alert_preferences.md brief, lib/db seed 2026-08-25-customer-tenant-alert-rules-1278.sql |
| Remediation Tracking.dc.html | docs/portal/remediation-tracking-contract-pack.md (#1719, regenerated 2026-09-04; #2827/#2869 revision 2026-09-07) + artifacts/api-server/src/routes/portal-remediation-tracker.ts, portal-remediation-checklist.ts (incl. decline-to-risk :281-406), portal-remediation-fix-routes.ts, portal-remediation-reveal.ts, portal-remediation-bypass-resolutions.ts, portal-remediation-tracker-scores.ts, portal-remediation-tracker-export.ts, src/lib/remediation-checklist.ts, remediation-fix-route.ts, remediation-reveal-gate.ts, remediation-bypass-resolutions.ts, remediation-tracker-risk-decline.ts, remediation-raise-change.ts, remediation-tracker-catalogue.ts, remediation-tracker-verification.ts, remediation-tracker-pricing.ts, portal-change-control-raise.ts, lib/db/src/schema/msp.ts (remediation_tracker_steps, remediation_knowledge_base, config_pack_templates, msp_change_requests, msp_risk_decisions, drift_events) |
| Change Control.dc.html | docs/portal/change-control-contract-pack.md (#2989, regenerated 2026-09-06) + artifacts/api-server/src/routes/portal-change-control.ts (11 routes), portal-change-catalog.ts, portal-settings-change-control.ts, portal-remediation-checklist.ts (raise-change), src/lib/portal-change-control.ts, portal-change-control-raise.ts, portal-change-approvals.ts, portal-change-approvals-store.ts, portal-change-rejection.ts, change-control-write-gate.ts, change-request-risk-discharge.ts, portal-change-freeze*.ts, portal-change-maintenance*.ts, portal-change-dependencies-store.ts, portal-change-timeline-store.ts, portal-change-metrics.ts, portal-settings-change-control.ts, lib/db/src/schema/msp.ts (msp_change_requests, change_freeze_windows, change_maintenance_windows, change_request_dependencies, cr_approvals, change_catalog_items, cr_events, cr_comments, cr_attachments, portal_change_control_policy, portal_change_control_notifications) |
| Ownership RACI.dc.html | docs/portal/ownership-raci-contract-pack.md (regenerated 2026-09-04; #1168 gate + #1518/#1524 decision audited 2026-09-07 — surfaces A/B only) + artifacts/api-server/src/routes/portal-ownership.ts, portal-settings-ownership.ts, src/lib/portal-ownership.ts, portal-ownership-policy.ts, ownership-workload-membership.ts, tenant-workloads.ts, portal-customer-scope.ts, portal-tier-features.ts, lib/db/src/schema/msp.ts (portal_ownership_* tables) |
| Break-glass Access.dc.html | docs/portal/break-glass-access-contract-pack.md (#2628, regenerating #2443) + artifacts/api-server/src/routes/break-glass-verification.ts (portal + public routes, performBreakGlassAdminOverride), msp-break-glass.ts (MSP side, not drawn), src/lib/workflow-executor.ts (break_glass_verification_gate), lib/db/src/schema/msp.ts (break_glass_pending_secrets, break_glass_verification_attempts, break_glass_override_audit) |
| Consent and Onboarding.dc.html | docs/portal/consent-and-onboarding-contract-pack.md (#2758) + artifacts/api-server/src/routes/consent.ts (GET /consent/callback portal-origin redirects), portal-onboarding.ts, portal-checkout-free.ts (provisionFreeOnboarding, 409 consent-first), msp-onboarding.ts (GET /public/onboarding/link/:token), public-services.ts |
| Data Rights and Privacy.dc.html | docs/portal/data-rights-and-privacy-contract-pack.md (#2549) + artifacts/api-server/src/routes/portal-privacy.ts (GET /portal/data-export, POST /portal/deletion-request), src/lib/data-rights.ts, msp-data-rights.ts (overlap check) |
| Diagnostics and Scripts.dc.html | docs/portal/diagnostics-and-scripts-contract-pack.md (#2451; #2506 filed) + artifacts/api-server/src/routes/msp-diagnostics.ts (/portal/diagnostics/latest, /runs/:runId, /scripts/:checkKey/download, /health-benchmark), portal-assessment.ts (scan fragment), lib/db/src/schema/msp.ts (msp_diagnostic_runs, msp_diagnostic_findings) |
| Documents.dc.html | docs/portal/documents-contract-pack.md (#2449; #2507 filed) + artifacts/api-server/src/routes/portal-documents.ts (all 8 routes), src/lib/insight-pdf.ts, portal-url.ts, sow-pricing.ts, lib/db/src/schema/index.ts (reports, insights_generated_documents, quick_win_result_shares, presentation_doc_views, document_types) |
| Email Authentication Setup.dc.html | docs/portal/email-authentication-setup-contract-pack.md (#2445) + artifacts/api-server/src/routes/portal-email-auth-status.ts, src/lib/monitor-executor.ts (runDnsCheck, DKIM_DEFAULT_SELECTORS), drift-check-specs.ts |
| My Architect.dc.html | docs/portal/my-architect-contract-pack.md (#1581) + artifacts/api-server/src/routes/portal-retainer.ts, admin-retainer.ts (entryToWire/bucketToWire), src/lib/retainer-hours.ts, retainer-work-logger.ts, lib/db/src/schema/msp.ts (retainer_settings, retainer_work_log), schema/index.ts (status_reports) |
| Offboarding.dc.html | docs/portal/offboarding-contract-pack.md (#2444; #2510 filed) + artifacts/api-server/src/routes/portal-customer-engines.ts (POST /portal/customer/offboard, GET /portal/customer/export), msp-portal.ts (MSP 3-step state machine, not drawn) |
| Offers and SOW Acceptance.dc.html | docs/portal/offers-and-sow-acceptance-contract-pack.md (#2448; #2511 filed) + artifacts/api-server/src/routes/portal-offers.ts (5 routes incl. SSE), portal-presentations.ts (:id, sow-document, sign, latest; deriveEffectiveSowData, resolveScopeAwarePrice), platform-agreements.ts (§9b), src/lib/sales-offer-engine.ts, sow-pricing.ts, project-sow-fulfillment.ts |
| Public Share Pages.dc.html | docs/portal/public-share-pages-contract-pack.md (#2453) + artifacts/api-server/src/routes/live-document-shares.ts (GET /public/live-document-shares/:token), msp-sow.ts (GET /public/sows/:shareToken, POST .../sign), portal-documents.ts (public document viewer — drawn on Documents.dc.html) |
| Requests and Support Chat.dc.html | docs/portal/requests-and-support-chat-contract-pack.md (#2450, §16-19 addendum #2648) + artifacts/api-server/src/routes/portal-customer-requests.ts (4 routes), support-chat.ts (chat, escalate, actions/*), portal-mission-control.ts (remediate, overview, listRemediableOffers), src/lib/zoho-desk.ts; chat surface itself is ShaneBot.dc.html |
| Resources Page.dc.html | docs/portal/resources-page-contract-pack.md (Epic #1093) + artifacts/shane-mccaw-consulting/src/pages/Resources.tsx (copy verbatim), src/content/articles/*.md (7 articles + README), routes/leads.ts, shares.ts, downloads.ts |
| Runbooks.dc.html | docs/portal/runbooks-contract-pack.md (#1488, regenerated for #1557 cycles + #1940 cycle affinity) + artifacts/api-server/src/routes/portal-runbooks.ts (8 routes), src/lib/portal-hold-windows.ts, portal-runbook-cycles.ts, portal-oversharing-sites.ts (producer), lib/db/src/schema/msp.ts (portal_runbooks, portal_runbook_runs, portal_runbook_steps, portal_hold_windows, portal_hold_window_events) |
| Scope and SLA.dc.html | docs/portal/scope-and-sla-contract-pack.md (#2452; #2513 filed — drawn as a visible caveat) + artifacts/api-server/src/routes/portal-customer-engines.ts (sla-status, scope-status), src/lib/sla-engine.ts, scope-creep-engine.ts |
| Signup Agreement and Invite.dc.html | docs/portal/signup-agreement-and-invite-contract-pack.md (#2442; #2509 filed) + artifacts/api-server/src/routes/msp-signup.ts (tiers, start, success), platform-agreements.ts (current, acceptance-status, accept), msp-onboarding.ts (GET/POST /public/msp-invite/:token), msp-settings.ts (invites write side) |
| Team Management.dc.html | docs/portal/team-management-and-invitations-contract-pack.md (#2895, regenerating #2447; #2527 manager route; pack predates #3647/#3996) + artifacts/api-server/src/routes/portal-team.ts (12 routes incl. PATCH /portal/team/:userId/role — read in full on main 2026-09-14), src/middlewares/requireAuth.ts (assertCustomerAccess), rbac-capability.ts (customer:team.manage, setGrantRole, usersHoldingGrantRole), lib/db/src/rbac/legacy-ladder.ts (CUSTOMER_PLATFORM_ROLE_KEYS), lib/db/migrations/manual/2026-09-11-rbac-customer-admin-billing-roles-3629.sql (what each role grants; billed-party trigger), session-tracking.ts, lib/db/src/schema/index.ts (users incl. managerUserId, mfa_enrollments, webauthn_credentials, mfa_bypass_codes) |
| Policy Decisions.dc.html | docs/portal/policy-decisions-contract-pack.md (#1722, regenerated 2026-09-06 — replaces the 2026-08-29 pack) + artifacts/api-server/src/routes/portal-policy-decisions.ts, routes/portal-risk-register.ts (GET /portal/policy-decisions), routes/portal-compliance-obligations.ts, src/lib/portal-tier-features.ts, alert-engine.ts (advancePolicyReviewClock, advancePolicyClearances), lib/db/src/schema/msp.ts (policy_decisions, compliance_frameworks AUTHORITY_TYPES, compliance_obligations, tenant_compliance_scope) |
| POAMs.dc.html | docs/portal/poams-contract-pack.md (#4021 for #1935; §7.1 stale — fixed on main) + artifacts/api-server/src/routes/portal-poams.ts (all 6 routes, read in full on main), src/lib/risk-authority.ts (resolveRiskAuthority, resolveAuthorizedByAsOf, resolveRiskAuthoritiesBatch, currentAHolderPersonIds), poam-ref.ts, portal-tier-features.ts (PORTAL_TIER_MODULE_KEYS.poams), retention/lifecycle.ts (softDelete, requestAcceleration, findOpenDeletion), lib/db/src/schema/msp.ts (msp_poams, msp_poam_milestones, POAM_STATUSES ×6); MSP-side msp-poams.ts (cancel, convert, milestones) not drawn |
| Status Reports.dc.html | docs/portal/status-reports-portal-contract-pack.md (#3889 for #3435 phase 2; Design phase #3890) + artifacts/api-server/src/routes/portal-status-reports.ts (all 4 routes, read in full on main), msp-status-reports.ts (comment routes :317-421, MSP side not drawn), src/lib/notification-center.ts (createNotification msp_user recipient), lib/db/src/schema/msp.ts (msp_status_reports, msp_status_report_comments); docs/msp-console/status-reports-msp-console-contract-pack.md (#3763) read for the parent object's state enum and wire shape |
| Projects.dc.html | docs/portal/projects-contract-pack.md (#1737 for #1570; #4024 orphaned customer route, #4025 no customer closure sign-off — both drawn as stated limits) + artifacts/api-server/src/routes/portal-projects.ts (GET /portal/projects/:id, kanban-events SSE), admin-projects.ts (syncProjectProgress, seedKanbanCardsForPhase, closure-request — MSP side, not drawn), src/lib/project-sow-fulfillment.ts (the only projects-row writer), lib/db/src/schema/index.ts (projects, workflow_steps, kanban_tasks ×5 columns, project_updates, project_closures, status_reports, contracts, invoices) |

## Notes
- Team Management: the held pack (#2895) predates the role route. On `main`, `portal-team.ts` has 12 routes: the 11 packed plus `PATCH /portal/team/:userId/role` (#3647), and the roster read projects `isCustomerAdmin`, `hasBillingRole` (#3647) and `managerUserId` (#3996). The screen follows the code. Role keys are the platform `customer_roles.key` values `customer-admin` and `billing` (`CUSTOMER_PLATFORM_ROLE_KEYS`); custom org roles are refused by the route. Nobody is made Customer Admin automatically (#3629) — this screen is the portal-side grant surface the migration header said did not yet exist.
- POA&Ms: the pack's §7.1 finding (`WirePoam.signed.authorizedBy` always null on read) is fixed on `main` — `portal-poams.ts` now passes `resolveRiskAuthoritiesBatch(...).authorizedBy[i]` (list) / `resolveAuthorizedByAsOf(...)` (get one) into a 4-arg `toWirePoam`. Treat the pack as behind the code on that point; the screen draws the replayed holders. Still true from the pack: both READ routes 402 for every real tenant (no tier includes `poams`), writes are not tier-gated, delete requires a reason unconditionally with no role restriction beyond the Customer floor, `msp-poams.ts` list/get do not filter soft-deleted rows (#1571 filing), and no route anywhere writes `completed`.
- Status Reports: two unrelated "status reports" exist — `msp_status_reports` (this screen, MSP-console-authored, #3434/#3435) and `status_reports` (retainer reports from AdminV2, read by `GET /api/portal/retainer` and drawn on My Architect.dc.html). Not merged; each screen's ledger names the other. The customer→operator comment notification (§1.4) is unverified live — the screen says the comment is recorded, not that anyone was told.
- Projects: built on the single customer read (`GET /api/portal/projects/:id`) — there is no customer list route, so the screen is a by-id page reached from a link and says so. `previewTasks` is a synthetic read-only projection of template tasks for unseeded steps; `progress` is admin-side arithmetic (completed/total tasks); `phase`/`priority` are free text; `kanban_tasks.column` has five DB values incl. `review` (the admin route's TS cast omits it). The closure loop has no customer half (#4025) and the route has no caller (#4024) — both drawn as limits. The pack's "deferred to v1.2" framing is superseded by Shane's instruction on 2026-09-14: build every portal pack regardless of milestone.
- Security Plan: `GET /api/portal/security-plan/drift` (#3027) is real upstream but absent from the contract pack. Section 7 of the screen was already drawn on the MSP-side computation and needs no change; treat the pack as behind the code here.
- No commit sha is recorded: github_get_tree resolves a tree hash (f6a1182ddee5 this sync), not a commit, and a compare from the last recorded commit (176fbd7d8d79) truncates server-side. Syncs work by refreshing every contract pack this project uses, diffing byte sizes and content against the copies held here (previous copies under docs/_prev/), then re-reading the packs whose content genuinely changed.
- Change Control: the policy panel omits the per-gate switches (`gated: Record<gateKey, boolean>`) because the gate-key catalogue is not in the pack. Everything else on the settings surface (policy booleans, requiredSignatures floor, live-computed approvers, 7 notification event keys) is drawn.
- Policy Decisions: the pack's §9 question (merge / two tabs / pick one for the own-table register vs the risk-derived `GET /portal/policy-decisions` view) is answered in this project as pick-one — this page draws the own-table register; risk-derived positions are drawn on Risk Register.dc.html's own section. #1724 should treat that as this design's proposal, not a settled product decision.
- Contract-pack claims now built upstream: #1496 approvals, #1497 write gate, #1499 execution record (MSP-side), #1500 freeze windows, #1503 timeline/comments/attachments, #1504 maintenance windows + collisions + dependencies, #1506 metrics, #1515 multi-holder, #1518/#1519 decline + gate mode, #1522 cell event log, #1525 typed authorities, #1526 dependency clearance, #1528 own-table policy register, #1547–#1553 policy engine (MSP-side). Still absent on the customer portal: a reschedule route for a booked change, any rollback path (#1499 is MSP-console only), customer-facing CAB/PIR (#1501/#1502 MSP-only), renew/withdraw on a policy decision, a customer read of standing policies (#1685).
- Open gaps carried on screens as known limits: #3044 (catalogue execute bypasses freeze/window/collision checks), #3045 (metrics ignore PIR close codes), #3046 (approval SLA breach computed, never escalated) on Change Control; no create route for tenant-authored compliance obligations (Policy Decisions, pack finding under #1490); the export status-label gap on Remediation Tracking (cosmetic, not filed); #2527 (decline cannot climb a management chain) on Ownership RACI. #2827 is closed and no longer drawn as a gap.
- Every pack under docs/portal/ (37 files incl. alert_preferences.md) now has a screen in this project. Upstream still carries packs under docs/msp-console/ (39) and docs/buildconsole/ (8) (MSP-operator and build-console surfaces: buildconsole, email templates, msp executive, launch control, plan self-service, staff roles & onboarding, inbound webhooks, plus MSP-console mirrors of customer modules) — none of those is a customer-portal screen and none is built here.
- Packs whose MSP-side half is deliberately not drawn on the customer screen: break-glass (operator watchlist/audit §3), offboarding (request → export → archive), requests (msp-support.ts operator queue §16-19), consent (generate-link, invites list). Public/unauthenticated surfaces are drawn as their own scenes (Consent outcomes, Public Share Pages, invite landing, signup).
