# #4394 — promoteMspUserToCustomer has had no caller since the portal.ts decommission (assessment paid flow)

- **Status:** ✅ DONE 2026-09-16T19:28:34Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 13ee411d4803ce3fef9a761808ce27a3272cc595

## Log
- 2026-09-16T19:16:11Z ⏳ IN FLIGHT — Tracing the paid assessment flow from copilot-assessment-checkout.ts through the Stripe webhook to the portal landing to determine honestly whether a paying assessment customer is stranded below Customer, or reaches the portal via a role-independent gate (hasRealEntitlement + client_services).
- 2026-09-16T19:28:34Z ✅ DONE — Real trace found `copilot-assessment-checkout.ts` is an unrelated, unwired SOW add-on route (no frontend caller, no webhook consumer for its `metadata.type`) — not the assessment funnel's actual paid path. The real funnel is: marketing-site consent → `public-assessment-payment.ts`'s `/payment-confirmed` (marks the checkout session `paid`; does NOT prove the mailbox) → `public-assessment-account.ts`'s `/verify-code` then `/set-password` (the real paid + email-proven point; its own docstring already named role promotion as an undone gap) → a `signupExchangeTokensTable` token → portal. Wired `promoteMspUserToCustomer(user.id)` into `/set-password`, same placement/guard #4392 used in `purchase-account-flow.ts`'s `attachPasswordToAccount`. Also traced the portal's real landing gate: it is `App.tsx`'s `RequireAuth`, not `hasRealEntitlement`+`client_services` (that pair only gates `auth.ts`'s password-setup/forgot-password issuance) — any non-Customer role, including `Free`, gets a client-side "Continue into the customer portal" click-through on `portal-identity-interstitial.tsx`, which mislabels `Free` as a staff role. Filed two follow-up findings under Feature #4370: #4434 (the base assessment purchase never creates a `client_services` entitlement row — only the optional rescan add-on does) and #4435 (the interstitial's staff-account copy/routing is wrong for `Free`). `tsc --noEmit` clean on the changed file; `direct-tenant-provisioning.test.ts` (21/21) and `free-scan-prospect.test.ts` (3/3, live-db) both pass unchanged. Branch verified merged into origin/main (`verify-branch-merged.mjs`); working tree clean.
