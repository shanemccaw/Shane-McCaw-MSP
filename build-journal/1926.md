# #1926 — Literal NUL bytes in portal-risk-register.ts / portal-sops.ts hash separators

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 4766fd31fb420bf5490ea855904b1ffe614516ea

## Log
- 2026-08-31 ⏳ IN FLIGHT — Confirmed literal NUL at offset 20820 in artifacts/api-server/src/routes/portal-risk-register.ts and offset 11557 in artifacts/api-server/src/lib/portal-sops.ts, both inside `.join("\x00")` hash separators. Replacing with the `\x00` escape (byte-identical), verifying hashes unchanged, re-running greps now that files are visible, adding a repo guard against NUL bytes in source.
- 2026-08-31 ✅ DONE — Replaced the literal NUL with the `\x00` escape in both files; proved byte-identical hash output before/after over fixed test inputs (matching SHA-256 digests). Confirmed both files no longer read as binary: `grep -c ''` succeeds (547 / 350 lines) and `git grep` matches inside them. Re-ran route-consumer/wire-contract/endpoint-prefix checks against both now-visible files against `docs/risk-register-contract-pack.md` and `docs/sops-contract-pack.md` — both packs already fully documented these files (extracted via Read, not grep, on 2026-08-29) and list no orphaned endpoint beyond what's already tracked on #1712/#1728 (contract-pack regeneration post portal-v2 retirement); no new gap found. Added `artifacts/api-server/src/lib/repo-source-nul-byte-guard.test.ts`, wired into `vitest.config.ts`'s explicit `include` list, which walks every `.ts/.tsx/.js/.jsx/.mjs/.json/.md/.sql/.css/.html` file under the repo root for a literal NUL byte. Running it found a third, previously-unknown live instance: `artifacts/api-server/src/lib/config-snapshot-differ.ts`'s `fingerprintRuleset` (from the concurrent #1798 work, landed after #1926's own "only two files" scan) — 3 literal-NUL separator positions in one template literal. Fixed with the identical proven-safe method (byte-identical hash confirmed) since it's a "gap fixed in this build," not a separate finding. All touched files pass their existing test suites (90 tests across portal-sops.test.ts, portal-risk-register.test.ts, config-snapshot-differ.test.ts, and the new guard) and `tsc --noEmit` shows zero errors in any of the 4 touched files (116 pre-existing unrelated errors elsewhere, already tracked per #1765/#1772/#1774/#1777/#1778/#1781, not introduced here). Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
