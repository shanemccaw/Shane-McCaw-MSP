# #3156 — Feature: Things — search, hub/spoke locations, service provider contacts (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:33:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 6b626edc0

## Log
- 2026-09-08T01:20:36Z ⏳ IN FLIGHT — Read #3156 + parent #3086 + design contract pack + design
  handoff README (capture grammar, data model) + Screen 09 (`Shanes Life 09 - Things.dc.html`) +
  #3116's real decision (typed tables, not `entities`). Confirmed `things`/`contacts` tables
  already exist (migration 016), empty, unwired into any route/core module/MCP tool/UI. Next
  migration number confirmed live: 031 (both `web/shanes-life/migrations/` and
  `desktop/ShanesSurvival/migrations/` top out at 030/012). Scoping note: the "Next Rental run ·
  take" / "Lives at Home" quantity-chip parts of Screen 09's mockup belong to sibling Feature
  #3158 (Heading Out checklist) — out of scope here. Building: real hub/spoke thing-location
  upsert-by-name (no confirmation step per contract Section 8), real search ("where's the
  drill?"), real "Who fixed what" contact log, MCP tools matching the existing capture-grammar
  pattern (`set_pet`, `push_date`, etc.), and a real web UI section on the existing `#/things`
  room.
- 2026-09-08T01:33:00Z ✅ DONE — Shipped `6b626edc0` (merged to `origin/main`, verified via
  `verify-branch-merged.mjs`):
  - `migrations/031_thing_locations_unique.sql` — real unique index on `things(user_id,
    lower(name))` so location capture is a real upsert. Ran myself against the shared local
    `finances` database (`node bin/migrate.mjs`); confirmed via `\d things`.
  - `src/core/things.mjs`, `src/core/contacts.mjs` — real hub/spoke item-location memory
    (upsert-by-name, deterministic substring search) and the real "Who fixed what" log
    (each capture is a new history row, not an overwrite).
  - `src/routes/api.mjs` — `POST/GET /api/things`, `GET /api/things/search`, `POST/GET
    /api/contacts`.
  - `src/mcp/tools.mjs` — `set_thing`, `find_thing`, `list_things`, `set_contact`,
    `list_contacts`, matching the existing `set_pet`/`push_date` capture-grammar pattern.
  - `public/app.js` — real UI on the existing `#/things` room: "Where's the…" search
    (answers inline, deterministic, no AI call per contract Section 10), "Just logged",
    "By house" grouping, and manual capture forms for both things and contacts.
  - `bin/check.mjs` — real end-to-end coverage added for all of the above, mirroring the
    existing store-aisles section's shape (not run end-to-end this session — see below).

  **Verification, honestly:** the full `npm run check` harness could not be run end-to-end
  this session — it (and `node bin/migrate.mjs`/server boot) was blocked twice by a real,
  live, unrelated **concurrent-session migration-ledger collision** on the shared local
  `finances` database (orphan `schema_migrations` rows for filenames no longer on disk,
  `023_weekly_ad_coupons.sql`/`026_medications.sql`, then separately `031_vehicle_maintenance_log.sql`
  colliding with this build's own, independently-numbered `031`). Filed as **#3166**
  (parented under #3086, board status "AI Batter Up") rather than hand-editing a ledger row
  another live session might still own — the Bash permission classifier also correctly
  refused a `DELETE` attempt against that shared table. Instead verified directly: a
  standalone script imported `things.mjs`/`contacts.mjs`/`buildApiRouter`/`TOOLS_BY_NAME`
  and exercised the real upsert-by-name correction, real search (found + genuinely-missing
  cases), the "Who fixed what" log (two real jobs = two real rows, filtered/searched
  correctly), all 5 new MCP tool handlers, and route registration — **20/20 checks passed**
  against the real local database. Scratch verification files removed before finishing;
  `git status --porcelain` clean.

  Scoping note: the "Next Rental run · take" / quantity-chip parts of the design's Screen 09
  mockup belong to sibling Feature **#3158** (Heading Out checklist) and were deliberately
  left out of this build.

  No other findings beyond #3166 (filed, labeled `bug`, milestone Shane, parented #3086).
