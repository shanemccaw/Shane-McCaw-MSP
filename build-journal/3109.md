# #3109 — Feature: Shopping — Barcode scan (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T20:50:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `c42635b03` (IN FLIGHT bookend), `ab828261b` (the work)

Sub-issue of #3086 (Epic: Shane's Life). Real `blocked_by` edge on #3088 (Shopping — Core
list + capture) confirmed cleared: #3088 carries the `complete` label (real per the
`blocked` label section's "closed/`complete`-labeled" rule), so the stale edge was removed
from #3109 before starting.

## Real scope (from the issue body)
- Real barcode scan (device camera) resolves to a real item + real price, added to the run
- Real fallback when a scanned barcode isn't recognized — never a silent failure

Design source: `web/shanes-life/Design/design_handoff_shanes_life/` — `contract.md`,
`README.md`, `Shanes Life 04 - Shopping.dc.html` (`2a` "Scan → real price": exact / near /
unknown match states, barcode-to-item link remembered forever, "Save price" also checks the
item off).

## Log
- 2026-09-07T20:30:00Z — Investigation done: read `src/core/lists.mjs` (list_items has no
  price/quantity/aisle column by design — #3088's own header defers that to this Feature),
  `src/routes/api.mjs` shopping routes, `public/app.js` Shopping room UI, `src/mcp/tools.mjs`
  (`push_list`/`get_list`, no `get_prices` built yet — that's #3112's scope, not this one).
  Confirmed local DB (`finances`) reachable, migrations 013–019 applied, no price/barcode
  columns yet.
- 2026-09-07T20:50:00Z — ✅ DONE — Built the real scan → price flow end to end. Migration
  020_barcode_scan.sql (`list_items.price_cents/price_source/priced_at`, `barcode_links`
  unique on `(user_id, barcode)`), run myself against the local `finances` DB. New
  `src/core/scan.mjs`: `lookupBarcode` (exact/near/unknown per the design's own three-state
  grammar) and `saveScan` (link + price + checks the item off), with a real product-name
  lookup against `world.openfoodfacts.org` (no API key, confirmed reachable) — a genuinely
  unreachable/unrecognised lookup honestly degrades to "unknown", never a fake name. Two new
  routes (`POST /api/lists/:id/scan/lookup`, `/scan/save`) in `src/routes/api.mjs`. Real
  camera UI (native `BarcodeDetector`, honest manual-barcode-entry fallback where
  unsupported/denied) wired into the Shopping room in `public/app.js`/`app.css`. Extended the
  project's own real end-to-end harness (`bin/check.mjs`) with coverage for all three match
  states, run against a live server + the real database + the real OFF network call —
  **77/77 checks pass**. Commit `ab828261b`, pushed to `origin/main`
  (`verify-branch-merged.mjs` confirms merged). `git status --porcelain` clean.

  **Filed #3121** (parented under #3086, board status "AI Batter Up"): while building, found
  that #3111 (Per-run budget, running concurrently in `C:\wt\3111-q1873`) independently added
  its *own* `list_items.price_cents` column for a different purpose (budget estimate vs. this
  Feature's real scanned price) — a real schema-overlap between two concurrently-dispatched
  sibling Features, not something this build could resolve unilaterally (a product decision
  about what the shared column means once both land). Not a finding about my own scope; noted
  here per the mandatory-filing rule.
