# #1836 — Build Queue refresh button skips loading-strip feedback; strip clips critters

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 89075e967 (bookend), 0e091da4ed9c7767ac9137d5009f8509fec70fd5 (real fix)

## Log
- 2026-08-30 ⏳ IN FLIGHT — read BuildQueuePanel.xaml.cs BtnRefreshGitHubTiles_Click, MainWindow.xaml.cs FullGitRefreshRequested handler, LeftSidebar.xaml GitRefreshLoadingStrip, IssueChompAnimation.cs Start/StopRefreshLoadingStrip + critter dims; then fix strip height and expose shared disable+strip implementation for both trigger paths.
- 2026-08-30 ✅ DONE — Root-caused the clipping: StartRefreshLoadingStrip scales the critter
  0.34x about its own center, landing its visible band ~16-36px down from the strip's top
  against a 22px-tall Border, clipping everything below the head. Fixed `GitRefreshLoadingStrip`
  Height 22 -> 56 (matches the largest native critter canvas, 60x56). Extracted the disable-button
  + critter-strip start/stop + fetch logic out of `LeftSidebar.BtnRefreshGitBoard_Click` into a new
  public `LeftSidebar.RefreshGitBoardWithLoadingFeedbackAsync()`. `MainWindow`'s
  `FullGitRefreshRequested` handler (fired by `BuildQueuePanel.BtnRefreshGitHubTiles_Click`) now
  awaits that same method instead of calling `LeftSidebar.PopulateGitTrackerBoard(forceFresh:
  true)` directly, so both trigger paths show identical disable+strip feedback for the identical
  real duration. Added `BuildQueuePanel.SetGitHubTilesRefreshInProgress(bool)` so the Build Queue
  panel's own refresh button is disabled/re-enabled around that same awaited span, matching Git
  Board's own button-disable treatment (issue's requirement #3). `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors). Rebased onto
  origin/main and pushed directly (`0e091da4e`); `git merge-base --is-ancestor` confirms it's a
  real ancestor of origin/main. `git status --porcelain` clean.
