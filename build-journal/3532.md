# #3532 — shanes-life has no persisted write path for account role/target_amount/is_gate/due_day/last_paid_date after WPF's AccountRoleWindow removal (#3296)

- **Status:** ✅ DONE 2026-09-10T21:55:00Z
- **Scope:** platform (personal — Shane milestone, web/shanes-life)
- **Started:** 2026-09-10
- **Commit(s):** 4fe412909 (rebased/pushed to main as 60f7662af)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — investigating web/shanes-life/src/routes/api.mjs and core/money.mjs to build a real PATCH /api/money/accounts/:id write path + Money room UI, per #3532.
- 2026-09-10T21:55:00Z ✅ DONE — Built `PATCH /api/money/accounts/:id` (money.updateAccount:
  partial patch of role/target_amount/is_gate/due_day/last_paid_date/bill_category, same idiom
  as updateDebt), wired it into a new "Assign Account Roles…" sheet on the Accounts tab
  (public/app.js) -- the real successor to WPF's removed AccountRoleWindow, one row per real
  account, partial-PATCH per row. Updated the stale header comments in money.mjs (lines
  formerly 70-71/976) and api.mjs (formerly 2358-2359) that wrongly attributed these mutations
  to ShanesSurvival's FinanceTools.cs (that tool was always read-only; the real prior write path
  was WPF's own AccountRoleWindow -> AccountRepository, removed by #3296). No schema change
  needed -- accounts.role/target_amount/is_gate/due_day/last_paid_date/bill_category already
  existed (confirmed via `\d accounts` against the real local shanes-life DB).
  Verified: (1) direct round trip against the real DB -- imported `updateAccount`, patched a
  real account (role/target/isGate/dueDay/lastPaidDate/billCategory in one call), confirmed the
  real DB row via psql, confirmed an invalid role throws badRequest, then restored the account
  to its exact original values (confirmed identical) -- no residue left in real data; (2) booted
  the real server against the real DB and confirmed `PATCH /api/money/accounts/:id` is
  registered and auth-gated (401 "Not signed in" unauthenticated), matching the existing route
  pattern. `node --check` clean on all three touched files. `web/shanes-life` is standalone (not
  part of the pnpm workspace) and out of the msp-portal/marketing test-manifest scope, so no
  manifest applies here. Working tree clean; branch verified merged into origin/main
  (`verify-branch-merged.mjs` exit 0).
