# #2743 — Chats panel EpicProgress + GATE fraction still native one-level rollup

- **Status:** ⏳ IN FLIGHT 2026-09-03T23:10:09Z
- **Scope:** buildconsole

## Log
- 2026-09-03T23:10:09Z — Pulled #2743 body (no comments). Fixing the two real consumers it names
  that #2739 didn't cover: `LeftSidebar.xaml.cs` `EpicProgress` (Chats panel Epic progress bar) and
  `RenderGateCards` (GATE release-card fraction), both still reading `GitBoardIssue.SubIssueCount`/
  `SubIssueCompleted` (GitHub's native one-level `subIssuesSummary`) directly instead of the shared
  `GitBoardIssueFilters.ComputeTransitiveLeafRollup` #2739 built. Found #2739 already added a local
  `ResolveRollup(GitBoardIssue)` helper (wraps `ComputeTransitiveLeafRollup` when the ALL-states set
  is loaded and the node is a placeholder, else falls back to native fields) — reusing that same
  helper in both spots rather than re-deriving.
