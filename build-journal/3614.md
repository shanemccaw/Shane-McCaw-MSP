# #3614 — Status bar cleanup (pass 1): remove Ready/URL/Zoom/Sync/Replit, Postgres becomes an alert

- **Status:** ✅ DONE 2026-09-11T02:03:49Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** bcb54777e (rebased to 0cf14411c on origin/main)

## Log
- 2026-09-11T01:52:22Z ⏳ IN FLIGHT — starting: remove StatusBar columns 0 (Ready), 2 (URL), 4 (Zoom), 6 (Sync), 12 (Replit) from MainWindow.xaml; remove visible Postgres segment (column 8) but keep the underlying poll, replacing the always-on line with a real up→down toast alert with a working Start action reusing BtnPostgresStart_Click's logic. Deploy/Version (column 10) and everything right of Replit stay untouched.
- 2026-09-11T02:03:49Z ✅ DONE — Removed Ready/URL/Zoom/Sync/Replit StatusBarItems + Separators from MainWindow.xaml, renumbered the remaining grid columns (spring, Deploy+Version, Claude usage/weekly meters, Claude online status, clock — all byte-identical content, just renumbered). Removed the now-orphaned code-behind: WebView_NavigationStarting/WebView_SourceChanged (pure UI writes to removed elements, no other logic) and their 3 wiring sites; simplified WebView_NavigationCompleted to keep only its real UpdateVisualTestTrackerForNavigatedTab call; removed UpdateZoomDisplay() and its call sites (ZoomIn/Out/Reset menu actions themselves are untouched — only the status-bar "100%" readout is gone); simplified ReportSyncStatus to keep its real logging + Replit-wake-on-error logic, dropping only the QueueDot/QueueStatusText writes; simplified ReplitWatcher_StatusChanged to log to ActivityLog instead of writing to removed UI (the underlying ReplitWatcherService itself is untouched — still polls via its own hidden WebView2, Git #902/#1637). Postgres (Git #1417): RefreshPostgresStatusAsync keeps polling PostgresServiceMonitor on the same 15s timer, now tracks the last-seen PostgresServiceState and fires ToastEngine.ShowPersistent(Error) ONLY on a genuine Running→{Stopped,NotFound,Unknown} transition (never on the app's own initial/unknown startup state) — the toast's onClick invokes a new StartPostgresServiceAsync() (refactored from the old BtnPostgresStart_Click body, same PostgresServiceMonitor.StartAsync + re-check-before-reporting-success logic), which now also toasts its own success/failure result. `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 errors (6 pre-existing unrelated FocusModeBar CS0067 warnings only). Deploy/Version column and everything from the Claude usage meters rightward verified byte-identical in the diff — untouched. Commit bcb54777e, rebased onto origin/main and pushed as 0cf14411c; `verify-branch-merged.mjs` confirms ancestor of origin/main; `git status --porcelain` clean.
