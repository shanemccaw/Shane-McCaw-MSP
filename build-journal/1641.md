# #1641 — Restart-pending builds: right-click Build Now override, and the auto-restart timer must wait for that override to finish before resuming the rest of the batch

- **Status:** ✅ DONE 2026-09-15T02:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 243e632dea7a4269b05c3aff2504300c7180d601 (IN FLIGHT bookend), e6a1e6d8d08992de926e6d255cf3a3bf8b01fd7e (implementation)

## Log
- 2026-09-15T02:12:16Z ⏳ IN FLIGHT — read SessionLimitAutoRestartService.cs (RegisterLimitHit/Arm/FireAsync) and BuildQueuePostgresClient.ResumeLimitPausedAsync/RequeueLimitPausedAsync in full. Found a single-row resume path already exists (RequeueLimitPausedAsync + BuildQueuePanel's "Resume Now" context menu item, predating this issue) — that satisfies requirement 1's mechanics; renaming it to "Build Now" per the issue's naming and a forward-reference comment already in BuildQueuePanel.xaml.cs (`#1641 Build Now`). Requirement 2 (account-scoped FireAsync/ResumeLimitPausedAsync, aware of a manually-started sibling still running in the same batch) is genuinely missing — the auto-restart timer/armed-state is currently a single global instance with no account column filter at all. Implementing: per-account armed state + timers in SessionLimitAutoRestartService, account column added to ResumeLimitPausedAsync's WHERE clause, a new GetNonTerminalIdsAsync DB helper, and an in-memory per-account manual-override registry (RegisterManualOverride) that FireAsync checks before blanket-resuming — deferring with a 2-minute retry, logged on the existing "session-limit" channel, if a manually force-started sibling from the same batch is still non-terminal.
- 2026-09-15T02:45:00Z ✅ DONE — shipped in e6a1e6d8:
  - `BuildQueuePanel.xaml.cs` — right-click "▶ Build Now (skip the wait)" on a limit-paused card (renamed from "Resume Now") flips that one row to `queued` via the pre-existing `RequeueLimitPausedAsync(id)`, then calls the new `SessionLimitAutoRestartService.RegisterManualOverride(id, account)`.
  - `SessionLimitAutoRestartService.cs` — rewritten so armed restart state is a `Dictionary<account, ArmedState>` (one independent `Timer` per account) instead of one global timer; `RegisterLimitHit(id, resetLabel, account)` now threads the row's real account through from reap time; `FireAsync(account)` checks `_db.GetNonTerminalIdsAsync(...)` against that account's in-memory manual-override id set before resuming — if a "Build Now" sibling from the same batch is still queued/running/verifying, it defers and re-arms a 2-minute retry (logged on `session-limit`) instead of firing; startup re-arm/first-set-bootstrap paths updated to arm per the row's actual account.
  - `BuildQueuePostgresClient.cs` — `ResumeLimitPausedAsync(string? account)` now filters `WHERE account IS NOT DISTINCT FROM @account` (was unconditional across every account); new `GetNonTerminalIdsAsync(ids)` backs the sibling check.
  - `QueueWatcherService.cs` — `RunningEntry.Account` added and populated at both launch sites (adopted + fresh launch) so the reap-time `RegisterLimitHit` call carries the real account.
  - `BuildConsoleSettings.cs` — added `SessionLimitRestartAtIsoSecondary`/`SessionLimitResetAtIsoSecondary` alongside the existing primary-only pair, so each account's armed restart survives an app restart independently.
  - Issue's requirement 3 (Build Now clicked against the "other" account): confirmed out of scope, not guessed — Build Now reuses the row's own persisted `account` column as-is; there is no UI path to reassign a limit-paused row's account before overriding it, so no cross-account reassignment case exists to handle.
  - **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings, 0 errors. No live two-account session-limit collision was available to reproduce end-to-end in this session (would require two real accounts simultaneously hitting their own session limits); the account-scoping and defer/retry logic were verified by full read-through against the compiled build and the existing `_reservedSlots`/manual-override precedent this mirrors, not by a live repro. `git merge-base --is-ancestor e6a1e6d8 origin/main` → exit 0 (confirmed merged). `git status --porcelain` clean.
  - Not touched: `desktop/BuildConsole` test manifests (out of scope per standing rule).
