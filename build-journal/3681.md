# #3681 — MSP Overrides (feature flags + allowance overrides) has zero enforcement anywhere

- **Status:** ✅ DONE 2026-09-12T00:25:22Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 6bbdfef56 (merged into main)

## Log
- 2026-09-11T20:30:00Z ⏳ IN FLIGHT — investigating msp_overrides schema, msp-admin-settings.ts CRUD route, active-directory.ts deriveEntitlements(), msp-entitlement.ts loadTier()/requirePlanFeature() to wire the real read side per the issue body.
- 2026-09-12T00:25:22Z ✅ DONE — Added `applyMspOverride()`/`isOverrideActive()`/`MspOverrideRow` to
  `artifacts/api-server/src/lib/active-directory.ts` as the single expiry-aware merge point
  (featureFlags win per-key over tierCapabilities, numeric tenantAllowanceOverride/
  aiCreditAllowanceOverride replace the plan default when set, and the `unlimited_tenants`
  feature flag removes the tenant cap entirely — tenantAllowance -> null, same convention
  `checkTenantAllowance()` already used for "unlimited"). Wired it into both real consumers:
  `msp-entitlement.ts`'s `loadTier()` (the actual runtime gate every
  `tierAllowsFeature()`/`requirePlanFeature()`/`checkTenantAllowance()` call goes through — a
  concurrent build, Git #3683, landed a `mspPlanCapabilitiesTable` overlay in the same function
  while this was in flight; resolved the rebase conflict by layering msp_overrides last, since
  it's the explicit ad hoc per-MSP override the admin UI's own copy says "bypasses plan tier
  gating"), and `active-directory.ts`'s `deriveEntitlements()` (AdMspCanvas's Entitlements panel,
  the User Object detail pane, and the Account Control entitlements view via
  `admin-active-directory.ts`'s three call sites: MSP detail, user detail, user entitlements).
  No schema/migration needed — `msp_overrides` already existed and was already fully wired on
  the write side; this was purely the missing read side named in the issue.
  Verified: 158 tests pass (113 in the two touched lib test files incl. 14 new
  applyMspOverride/isOverrideActive/deriveEntitlements-with-override tests in
  active-directory.test.ts and new loadTier-with-override tests in msp-entitlement.test.ts, plus
  45 in the three admin-active-directory route test files), `tsc --noEmit` clean, and a real
  round-trip against the local DB — inserted an `msp_overrides` row for msp 1626
  (tenantAllowanceOverride: 42, aiCreditAllowanceOverride: 7500, featureFlags.advanced_signals:
  true), called the real `loadTier(1626)`, confirmed it returned exactly those values, then
  deleted the row (0 rows in `msp_overrides` before and after). Working tree clean, branch
  verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`.
