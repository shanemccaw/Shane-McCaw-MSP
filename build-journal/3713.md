# #3713 — Build Queue panel: collapse into an icon rail with running builds as circular progress icons

- **Status:** ✅ DONE 2026-09-12T00:52:52Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** ac79a33e2 (implementation, merged into `origin/main`)

## Log
- 2026-09-12T00:24:14Z ⏳ IN FLIGHT — dispatched, blocked-by #3698 (confirmed landed on `origin/main` at `e80434a59` before starting). Plan: add a real third `ColQueue` collapse width (300 expanded / ~44-48px icon rail, matching `ActivityBar`), a new icon-rail view in `BuildQueuePanel` showing one circular status icon per notable queue item (Running/Blocked/Waiting/Ask-Shane), a real progress ring for Running items sourced from the same `BuildProgressTracker.GetProgress` data `BuildProgressBubbleRow` (#2692) already reads (not indeterminate, per the issue's correction comment — indeterminate only as a per-item fallback when a build has no real progress data yet), a hover popout reusing `ChatSessionPane.RefreshProgress`'s phase-list rendering plus Feature/Epic chips, click-through to expand + navigate (reusing #3599's cross-filter nav), and overflow handling. Applying #3689/#3698's animation-lifecycle lessons: any ring/spinner animation started must be explicitly stopped when its item leaves the rail.
- 2026-09-12T00:52:52Z ✅ DONE — Shipped (`ac79a33e2`).

  **What shipped:**
  - `BuildQueuePanel.CollapsedRailWidth` (44px) — `MainWindow.xaml.cs`'s `PinToggled` handler now
    resizes `ColQueue` to this instead of `0` when the panel is pinned closed.
  - `BuildQueuePanel.xaml` gained a second root view, `IconRailDock`, swapped in for the existing
    `MainContentDock` (renamed from the previously-unnamed root `DockPanel`) by `TogglePin()`, plus
    a shared `RailHoverPopup`.
  - `RenderIconRail(List<QueueItem> rawItems)` (new, `BuildQueuePanel.xaml.cs`) — called at the top
    of every `RenderQueue` pass, deliberately off the **raw, unfiltered** queue rather than the
    status-filter/build-set/search-scoped `_currentGraphNodes`, so the rail always reflects the
    whole real queue regardless of what tab the expanded view happens to be on. Reuses
    `BuildItemNode` for the exact same `IsBlocked`/`IsWaitingForInput`/`IsAskingShane` computation
    the cards use, and `GhostStatusLabel` for the exact same status/color/priority vocabulary.
    Notable-item set: Running, Verifying, Blocked, Waiting-for-input, Ask-Shane, paused, capped,
    limit-paused, self-blocked-waiting — matches the issue's minimum ask plus the file's existing
    "something's actively different" vocabulary. Capped at 10 icons with a real "+N more" overflow
    tile; a dedicated `_railIcons` `KeyedCardPool` keeps a running item's ring element stable
    across renders when nothing about it changed (cache key = status/blocked/waiting/asking-
    shane/paused/hasProgress/percent-bucket).
  - `BuildRailIcon` — a real progress ring (`Path`/`ArcSegment`) for a Running item with real
    `BuildProgressTracker.GetProgress` data (a real percentage arc, reusing the exact data
    `BuildProgressBubbleRow` #2692 already reads), or a genuinely indeterminate spinning arc
    (never a fabricated percentage) for one with none yet — the spinner's `RotateTransform.Angle`
    Forever animation is registered via `KeyedSlotCardHost.BeginOwnedAnimation` on the pooled
    container, so `KeyedCardPool` stops it the instant the icon is retired, applying #3689/#3698's
    own animation-leak lesson to this rail's new animated element.
  - `ShowRailHoverPopup` — real hover popout: status pill, title, `BuildEpicChipRow`'s Feature/Epic
    chip, and — for a Running item with real progress data — the same phase-list rendering
    `ChatSessionPane.RefreshProgress` uses in Build Watch, reusing its `BuildProgressStepRow`
    (promoted `private` → `internal static` for this cross-panel reuse, matching the
    `BuildStatusPill` precedent). An item with no progress history yet says so plainly.
  - Click-through re-expands the panel (`TogglePin()`) and calls the existing `RevealQueueItem`
    (#3599) to switch to the correct filter tab, select and scroll to the real item.
  - `scripts/probes/queue-icon-rail-leak/` (new, kept diagnostic per the `queue-card-leak`
    precedent) — standalone probe compiling the real `KeyedCardPool`/`KeyedSlotCardHost`,
    replicating the rail's one new animated element (the indeterminate spinner) across a churn
    schedule (progress arriving, items finishing, new running items joining, a full-clear event).
    **Measured (90s @ 20ms ticks):** `estLiveSpinnerClocks` tracked `expectedSpinners` throughout,
    and reads exactly 0 on every dedicated post-full-clear quiet-window sample
    (`zeroClockOnEmptyRail: true` on all 6 such samples) — `anyBadEmpty=False spinnerMismatch=False`,
    exit code 0. Arc-geometry sanity check passed at sweep 0, 0.1, 50, 80, 180, 270, 359.9, 360,
    720, -5 (no degenerate geometry).
  - `dotnet build` of BuildConsole (post-rebase onto `origin/main`): 0 errors; only pre-existing
    unrelated warnings (`FocusModeBar` unused events, `BuildQueuePanel` CS0162 at the
    already-known #3699 `BuildMatrixDisabled` branch).

  **Honest gaps:**
  - No live click-through in the running app: per the standing rule a session must not launch
    BuildConsole.exe to verify (its single-instance mutex can courier into Shane's live instance).
    Visual confirmation of layout/spacing/hover placement in the real app is Shane's.
  - The right/queue `GridSplitter` drag and the View-menu "Show Build Queue"/"Reset Layout"
    actions can still desync `ColQueue.Width` from `_isPinned`/which root view renders — a
    pre-existing gap (the same splitter had no drag-resync before #3713 either), now more visibly
    consequential because the collapsed state is a real, grabbable 44px rail instead of an
    invisible 0. **Filed as #3741**, parented under #3706 (Feature: Build Queue Panel — Part 2),
    labeled `bug`, board status AI Batter Up — not fixed here (out of scope for #3713 itself).

  Nothing was dirty in the worktree on arrival; `git status --porcelain` is clean of everything I
  touched, and the branch is confirmed merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs`).
