# #3997 — Offers and SOW Acceptance: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T13:52:44Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** aa517e1d3 (merged into origin/main)

## Log
- 2026-09-14T13:46:34Z ⏳ IN FLIGHT — re-dispatch (original dispatch on this issue was never claimed). Pulled #3997's full body, the contract pack (Design/portal/design_handoff_full_site/docs/offers-and-sow-acceptance-contract-pack.md), the real .dc.html design export, and the archived portal-v2 customer-offers.tsx/customer-sow.tsx for error/validation/redirect logic to carry forward. Building the real page against the 7 live endpoints in portal-offers.ts / portal-presentations.ts.
- 2026-09-14T13:52:44Z ✅ DONE — Built `artifacts/portal/src/pages/customer-offers.tsx` (route `/offers`, nav entry "Offers") wired against the 7 real endpoints: `GET /api/portal/offers[/:id][/sse]`, `POST /api/portal/offers/:id/[accept|reject]`, `GET /api/portal/presentations/[latest|:id]`, `POST /api/portal/presentations/:id/sign`. `GET /api/platform/agreement/current` deliberately not wired per the contract pack's own §9b finding (not a customer offer-acceptance concept, no customer terms-acceptance endpoint exists to fake). Confirmed #2511 (`effectivePriceCents` held dollars) is already fixed on main (field renamed to `effectivePrice`, commit 251f7ebab) and stays that way; this page binds to the total already shown on the page rather than a second price from the sign response either way. Added `lib/offers-sow-types.ts`, `lib/offers-sow-api.ts` (react-query hooks + SSE live-update with 30s fallback poll), signature-canvas sign flow rebuilt against this repo's Tailwind/shadcn patterns. Documented deliberate divergences from the design mock in the page's own header comment + its "what this page deliberately does not do" card (the customer-safe offer projection strips `service_class`, so a project vs. non-project accepted-offer note can't be distinguished). Added and registered `test-manifests/portal/offers-and-sow-acceptance.json`. Verified: `tsc --noEmit` (0 errors), `vite build` (succeeds) both before and after two rebases onto a fast-moving `origin/main`. Attempted `shaneapp://runTest?file=offers-and-sow-acceptance.json&onlyTag=offers-sow-3997` for real — failed with `manifest not found or unparseable`, the same documented worktree-isolation gap (Git #1371/#1372) `test-manifests/portal/account-security-change-password.json`'s own notes hit on its own build; re-run once BuildConsole's own end-of-build merge lands this worktree's commits. Resolved one real merge conflict in `moduleNav.ts` against a concurrent build (#4038, Status reports) that landed the same nav slot — kept both rows. `git push origin agent/3997-q2725:main` succeeded on the 2nd fetch/rebase/push retry (`aa517e1d3`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
