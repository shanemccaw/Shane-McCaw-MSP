# #1519 — Ownership / RACI: Decline as a resting state, with a reason field

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Foundation (#1515, unique index) is DONE and merged. Building the
  decline resting-state: `portal_ownership_assignments.acceptance` gains a `declined` value
  alongside `""` / `pending` / `accepted`, plus a separate `declineReason`/`declinedBy`/`declinedAt`
  provenance triple (distinct from the assigner's `setBy`/`setAt`/`setWhy`, per the issue's
  "Settled" note). New `POST /portal/ownership/decline` route, scoped like the existing
  `/accept`; MSP-side decline requires a non-empty reason server-side, customer-side does not
  (customer decline escalates internally rather than recording an agreement). Re-assigning a
  holder clears a stale decline so the new assignment cycle starts clean. SCOPE STOP: wire
  contract only, no `artifacts/portal` page exists for this module yet.
