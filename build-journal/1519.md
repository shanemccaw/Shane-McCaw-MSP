# #1519 — Ownership / RACI: Decline as a resting state, with a reason field

- **Status:** 🛑 BLOCKED (on #1518)
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 395bd281d (bookend IN FLIGHT) — no other commits; all work reverted, see below.

## Log
- 2026-08-31 ⏳ IN FLIGHT — Foundation (#1515, unique index) confirmed closed/merged. Started
  building the decline resting-state: `portal_ownership_assignments.acceptance` gaining a
  `declined` value, plus a separate `declineReason`/`declinedBy`/`declinedAt` provenance triple,
  and a new `POST /portal/ownership/decline` route.
- 2026-08-31 🛑 BLOCKED — before committing any of it, found sibling worktree `C:/wt/1518-q936`
  (branch `agent/1518-q936`, issue **#1518** "Acceptance gate — universal, symmetric,
  cross-boundary") actively mid-build on the exact same schema surface, against the same shared
  local Postgres. Their uncommitted migration `2026-08-31-ownership-raci-acceptance-gate-1518.sql`
  already adds `decline_reason` (identical column, identical purpose) plus `responded_by`/
  `responded_at` (a generic responder pair that subsumes my planned `declined_by`/`declined_at`),
  and their in-progress diff touches the same three files (`lib/db/src/schema/msp.ts`,
  `artifacts/api-server/src/routes/portal-ownership.ts`, `artifacts/api-server/src/lib/portal-ownership.ts`)
  plus a new `portal-ownership-actions.ts`. #1518's own issue body lists "Nothing recorded a REASON
  for a decline" as part of its own scope. Continuing in parallel guaranteed a duplicated/
  conflicting column and a merge conflict on every file both issues touch.
  **Reverted everything:** dropped the two DB columns (`declined_by`, `declined_at`) I had added
  to local Postgres, deleted my migration file, `git checkout`'d the schema.ts edit, removed the
  self-marking `simulator_migration_runs` row I had inserted. `git status --porcelain` clean.
  Wired a real `blocked_by` → #1518 edge (`gh api ... issues/1519/dependencies/blocked_by`).
  Removed the stale `blocked_by` → #1515 edge (that issue is closed; the edge was dead weight
  before I found the real blocker). Labels: removed `in-flight`, added `blocked`. Posted a full
  writeup on the issue (https://github.com/shanemccaw/Shane-McCaw-MSP/issues/1519#issuecomment-5477224288)
  covering what was found, what was reverted, and the narrower scope remaining for #1519 once
  #1518 lands (the acceptance-gate + decline outcome + `decline_reason` column) — specifically the
  by-side asymmetric reason-required rule this issue settles that #1518's own migration comment
  does not distinguish. No findings filed — this is a scheduling collision between two
  concurrently-dispatched sibling issues in the same epic (#1491), not a code defect.
