# #1519 — Ownership / RACI: Decline as a resting state, with a reason field

- **Status:** 🛑 BLOCKED (on #1518)
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** none — no schema/route work committed. Prior session's work was reverted before
  commit; this file itself is being recovered onto `main` (see log below).

## Log
- 2026-08-31 ⏳ IN FLIGHT — Foundation (#1515, unique index) confirmed closed/merged. Started
  building the decline resting-state: `portal_ownership_assignments.acceptance` gaining a
  `declined` value, plus a separate `declineReason`/`declinedBy`/`declinedAt` provenance triple,
  and a new `POST /portal/ownership/decline` route.
- 2026-08-31 🛑 BLOCKED — before committing any of it, found sibling worktree `C:/wt/1518-q936`
  (branch `agent/1518-q936`, issue **#1518** "Acceptance gate — universal, symmetric,
  cross-boundary") actively mid-build on the exact same schema surface, against the same shared
  local Postgres. Their uncommitted migration `2026-08-31-ownership-raci-acceptance-gate-1518.sql`
  already adds `decline_reason` (identical column, identical purpose) plus `responded_by`/
  `responded_at` (a generic responder pair that subsumes the planned `declined_by`/`declined_at`),
  and their in-progress diff touched the same three files (`lib/db/src/schema/msp.ts`,
  `artifacts/api-server/src/routes/portal-ownership.ts`, `artifacts/api-server/src/lib/portal-ownership.ts`)
  plus a new `portal-ownership-actions.ts`. #1518's own issue body lists "Nothing recorded a REASON
  for a decline" as part of its own scope. Continuing in parallel guaranteed a duplicated/
  conflicting column and a merge conflict on every file both issues touch.
  Reverted everything: dropped the two DB columns (`declined_by`, `declined_at`) that had been
  added to local Postgres, deleted the migration file, `git checkout`'d the schema.ts edit, removed
  the self-marking `simulator_migration_runs` row. Wired a real `blocked_by` → #1518 edge and
  removed a stale `blocked_by` → #1515 edge (#1515 is closed). Labels: removed `in-flight`, added
  `blocked`. Posted a full writeup on the issue covering what was found, what was reverted, and the
  narrower scope remaining for #1519 once #1518 lands — specifically the by-side asymmetric
  reason-required rule this issue settles (MSP-side decline requires a reason; customer-side
  decline escalates internally with no stated-reason requirement) that #1518's own migration
  comment does not distinguish. No findings filed — this was a scheduling collision between two
  concurrently-dispatched sibling issues in the same epic (#1491), not a code defect.
  *(That IN FLIGHT/BLOCKED bookend pair was committed on this worktree's branch but never reached
  `origin/main` before the worktree was recycled — the commits survive only on
  `origin/agent/1519-q937` and a local `rescued/1519-q937-*` branch. This paragraph recovers that
  record onto `main`.)*
- 2026-08-31 🛑 STILL BLOCKED — resumed session, re-checked per the standing "picking this back up"
  protocol before doing anything else: `gh api .../issues/1519/dependencies/blocked_by` still
  lists only #1518, and #1518 is still `state: open` with label `in-flight` (not `complete`).
  The blocker has not cleared — schema for #1518 landed (`eb9c3f8d2`, adding `responded_by`/
  `responded_at`/`decline_reason` to `portal_ownership_assignments`, confirmed present in
  `lib/db/src/schema/msp.ts` on `main`), but #1518's own route work (actor-must-equal-owner gate,
  symmetric MSP-side accept/decline routes, notification wiring) has not landed — grepped
  `artifacts/api-server/src/routes/portal-ownership.ts` and `.../lib/portal-ownership.ts` for
  `decline_reason`/`responded_by`/`declined_by` and found no references. #1519's remaining scope
  (documenting decline as a non-resettable resting state, and applying the by-side asymmetric
  reason rule) has nothing to attach to until that handler exists. `blocked_by` edge, `blocked`
  label, and this bookend confirmed/left in place; no schema, route or UI work performed this
  session. Working tree left clean.
