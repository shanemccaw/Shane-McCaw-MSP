# #1519 — Ownership / RACI: Decline as a resting state, with a reason field

- **Status:** ✅ DONE 2026-09-03T12:30:00Z
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** `20ccfc2bb` (bookend IN FLIGHT), `df93d525a` (real work — schema +
  migration + routes + wire contract + escalation notification), both confirmed
  present on `origin/main` post-push (`verify-branch-merged.mjs` → OK).

## Log
- 2026-08-31 ⏳ IN FLIGHT — Foundation (#1515, unique index) confirmed closed/merged. Started
  building the decline resting-state: `portal_ownership_assignments.acceptance` gaining a
  `declined` value, plus a separate `declineReason`/`declinedBy`/`declinedAt` provenance triple,
  and a new `POST /portal/ownership/decline` route.
- 2026-08-31 🛑 BLOCKED — before committing any of it, found sibling worktree `C:/wt/1518-q936`
  (branch `agent/1518-q936`, issue **#1518** "Acceptance gate — universal, symmetric,
  cross-boundary") actively mid-build on the exact same schema surface, against the same shared
  local Postgres. Their uncommitted migration `2026-08-31-ownership-raci-acceptance-gate-1518.sql`
  already adds `decline_reason` (identical column, identical purpose) plus `responded_by`/
  `responded_at` (a generic responder pair that subsumes the planned `declined_by`/`declined_at`),
  and their in-progress diff touched the same three files (`lib/db/src/schema/msp.ts`,
  `artifacts/api-server/src/routes/portal-ownership.ts`, `artifacts/api-server/src/lib/portal-ownership.ts`)
  plus a new `portal-ownership-actions.ts`. #1518's own issue body lists "Nothing recorded a REASON
  for a decline" as part of its own scope. Continuing in parallel guaranteed a duplicated/
  conflicting column and a merge conflict on every file both issues touch.
  Reverted everything: dropped the two DB columns (`declined_by`, `declined_at`) that had been
  added to local Postgres, deleted the migration file, `git checkout`'d the schema.ts edit, removed
  the self-marking `simulator_migration_runs` row. Wired a real `blocked_by` → #1518 edge and
  removed a stale `blocked_by` → #1515 edge (#1515 is closed). Labels: removed `in-flight`, added
  `blocked`. Posted a full writeup on the issue covering what was found, what was reverted, and the
  narrower scope remaining for #1519 once #1518 lands — specifically the by-side asymmetric
  reason-required rule this issue settles (MSP-side decline requires a reason; customer-side
  decline escalates internally with no stated-reason requirement) that #1518's own migration
  comment does not distinguish. No findings filed — this was a scheduling collision between two
  concurrently-dispatched sibling issues in the same epic (#1491), not a code defect.
  *(That IN FLIGHT/BLOCKED bookend pair was committed on this worktree's branch but never reached
  `origin/main` before the worktree was recycled — the commits survive only on
  `origin/agent/1519-q937` and a local `rescued/1519-q937-*` branch. This paragraph recovers that
  record onto `main`.)*
- 2026-08-31 🛑 STILL BLOCKED — resumed session, re-checked per the standing "picking this back up"
  protocol before doing anything else: `gh api .../issues/1519/dependencies/blocked_by` still
  lists only #1518, and #1518 is still `state: open` with label `in-flight` (not `complete`).
  The blocker has not cleared — schema for #1518 landed (`eb9c3f8d2`, adding `responded_by`/
  `responded_at`/`decline_reason` to `portal_ownership_assignments`, confirmed present in
  `lib/db/src/schema/msp.ts` on `main`), but #1518's own route work (actor-must-equal-owner gate,
  symmetric MSP-side accept/decline routes, notification wiring) has not landed — grepped
  `artifacts/api-server/src/routes/portal-ownership.ts` and `.../lib/portal-ownership.ts` for
  `decline_reason`/`responded_by`/`declined_by` and found no references. #1519's remaining scope
  (documenting decline as a non-resettable resting state, and applying the by-side asymmetric
  reason rule) has nothing to attach to until that handler exists. `blocked_by` edge, `blocked`
  label, and this bookend confirmed/left in place; no schema, route or UI work performed this
  session. Working tree left clean.
- 2026-09-03 ⏳ IN FLIGHT — resumed session, checked `blocked_by` before anything else per the
  standing protocol. #1518 had been closed `NOT_PLANNED` and redone as **#2162** ("acceptance
  gate — configurable strict/loose, customer setting"), and a still-earlier session had already
  re-pointed the `blocked_by` edge from #1518 to #2162 (found on lookup, not something this
  session did). #2162 is now `state: closed`, `state_reason: completed`, with its real commits
  confirmed on `main` (`066c2fbe4` schema, `4c90ccb87` routes, `ea53294f5` bookend DONE) — grepped
  `decline_reason`/`responded_by`/`respondedAt`/`gateMode` in both `portal-ownership.ts` and
  `msp-ownership.ts` and confirmed the symmetric accept/decline routes, the strict/loose Settings
  toggle, and the actor-must-equal-owner gate are all real and present. **Unblocked**: removed the
  stale `blocked_by`→#2162 edge (closed, nothing left to wait on) and swapped the `blocked` label
  back to `in-flight`.

  Re-verified #1519's own remaining scope against the real code (not re-architecting — this is
  reading what's actually there):
  1. **"Decline is a resting state, not reset by a later read"** — already true. `GET
     /portal/ownership` only ever `SELECT`s `acceptance`; no code path writes it during a read.
     Confirmed by inspection, no fix needed.
  2. **Asymmetric reason rule was NOT built by #2162** — #2162 gave both the customer-side
     (`POST /portal/ownership/decline`) and MSP-side (`POST /msp/ownership/:customerId/decline`)
     routes the *same* optional, unenforced `reason` field. #1519's own "Settled" section requires
     MSP-side reason to be **required** ("Reason box required") and customer-side to stay optional
     but **escalate internally to the assigner**. Neither half of that split existed.
  3. **The wire contract never surfaced the decline record at all** — `WireOwnAssignment` /
     `toWireAssignment` (customer GET) and `WireMspOwnHolding` (MSP cross-customer GET) carry
     `acceptance` but not `declineReason` / `respondedBy` / `respondedAt`. A declined cell's own
     "why" was stored and immediately unreadable by any client. Per the wire-contract-first /
     BUILD-IT rule, extending this is in scope even though there is no page to wire it into yet.
  4. **No stable identity for "the assigner" to escalate to** — `setBy` is free-text
     (`actingName`/`actingMspName`: display name, falling back to email), not a resolvable person
     id. Escalation needs a real recipient, so this session adds `setByPersonId` (same `u{id}`
     wire-person-id scheme as `ownerPersonId`), captured at assign time on both assign routes,
     mirroring `personIdForUser`.

  Building all four in this session (schema first, then routes, then a best-effort
  `notifyOwnershipDeclined` escalation on the customer-side decline route only — MSP-side decline
  intentionally gets no escalation notification, per the issue's own by-side split). "Escalates …
  or up their chain" — the "chain" (a management hierarchy above the assigner) has no backing data
  anywhere in this schema (`users` has no manager/reports-to column); implementing only "notify the
  assigner" and filing the chain-escalation gap as its own finding rather than inventing a
  hierarchy that doesn't exist.
- 2026-09-03 ✅ DONE — built all four pieces:
  1. **Schema + migration**: `portal_ownership_assignments.set_by_person_id` (text, `""` default,
     same `u{id}` scheme as `owner_person_id`) —
     `lib/db/migrations/manual/2026-09-03-ownership-raci-decline-escalation-1519.sql`, run against
     local `DATABASE_URL` and confirmed via `psql \d portal_ownership_assignments` and a row-count
     query (table is empty — `0` real assignments exist yet, honestly reported; #1727 hasn't wired
     the UI that would create them).
  2. **By-side reason rule**: MSP-side `POST /msp/ownership/:customerId/decline`
     (`routes/msp-ownership.ts`) now 400s on a blank `reason`. Customer-side
     `POST /portal/ownership/decline` (`routes/portal-ownership.ts`) stays optional, unchanged.
  3. **Real escalation**: `setByPersonId` captured at assign time on both assign routes
     (`routes/portal-ownership.ts`, `routes/msp-ownership.ts`); a new
     `notifyOwnershipDeclined` (`lib/notification-center.ts`) fires from the customer-side decline
     route only, notifying whoever assigned the cell — best-effort, skips self-declines and rows
     with no assigner id on file (pre-migration rows).
  4. **Wire contract**: `WireOwnAssignment` (customer GET) and `WireMspOwnHolding` (MSP
     cross-customer GET) now carry `declineReason`/`respondedBy`/`respondedAt` — previously stored
     since #2162 but unreachable by any client. No page exists yet to consume this (#1727,
     unbuilt) — per CLAUDE.md's scope-stop rule this session ends at the wire contract and does
     not touch `artifacts/portal`.

  **Findings filed**: #2527 ("Ownership / RACI: customer-side decline escalation stops at the
  assigner, no reports-to chain to climb") — parented to #1491 (the Feature-tier parent, per the
  Feature-first rule), milestone 5, labeled `bug`, board status set to AI Batter Up. Also
  reproduced and corroborated (comment, not a new duplicate) an already-filed, unrelated,
  repo-wide finding — #2522/#2503/#2515/#2192 all already describe the shared local pnpm store
  having empty-extracted package directories; this session's own instance of it was
  `drizzle-orm@0.45.2_...` being a 0-entry empty dir, which blocked a clean `tsc` run for this
  build's own diff. Did not run `pnpm install` per the standing rule — verified the real DB state
  directly instead (migration ran, column exists, table read back cleanly) and reviewed the diff
  by hand.

  Branch verified merged (`verify-branch-merged.mjs` → OK, `df93d525a` on `origin/main`).
  `git status --porcelain` clean. No test manifest: scope-stop means there is no
  `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting` UI change in this build, so the
  Test Coverage section's manifest requirement does not apply.
