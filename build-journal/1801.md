# #1801 — Build Watch slot card: long issue titles wrap, no truncation

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** de15df670e80424b24c3efadb2616308a5da3241 (merged to main as 9d7b94a73e656d9bacb55d50c0089498963f299b)

## Log
- 2026-08-29 ⏳ IN FLIGHT — reading SafeTitle / title TextBlock, then adding max-length truncation with ellipsis + tooltip; also a bounded check of BatterUpQueueService for blank-title cases (#1785).
- 2026-08-29 ✅ DONE — Read `BuildWatchWindow.xaml.cs` `SafeTitle` (~1866, only handles the blank-title
  fallback, not truncation) and traced the real slot-card title display through
  `slot.Title = SafeTitle(item)` → `vm.SessionLabel = slot.Title` → `ChatSessionPane.xaml`'s header
  `TextBlock` (line ~305). That TextBlock already had `TextTrimming="CharacterEllipsis" MaxWidth="140"`
  (landed 2026-08-13, f27d4c90c9, predating #1801) — so on current code a long title is a single-line
  ellipsis, not a 3-line wrap; the screenshot's wrap likely predates that commit. What was genuinely
  missing was the "full text reachable via tooltip" half of #1801's ask — added
  `ToolTip="{Binding SessionLabel}"` to that TextBlock so hovering a truncated title now shows the
  full real title.
  - Bounded check of the Batter-Up-auto-queue path for #1785's blank-title symptom: `gh issue view 1785`
    confirms #1785 is a normal closed issue with a real, non-empty title — so the auto-queue path's
    `item.Title` (`BatterUpQueueService.cs`, straight off `GetBatterUpIssuesAsync`) wasn't handed an
    empty string for this issue at the GitHub-API level; #1785's blank display is most plausibly a
    stale/edited queue row, as #1801 itself already guessed — **inconclusive**, not chased further per
    the bounded-effort instruction.
  - While checking that path, found and filed a **separate, real, reproducible** gap: `GitHubApiClient
    .ScanProjectItemsForStatusAsync`'s GraphQL query only fragments `content { ... on Issue { ... } }`;
    any project item whose content is a Draft Issue or Pull Request comes back with `Number=0` and
    `Title=null`, which flows straight into `BatterUpRow`/auto-queue with no error. Filed as #1807,
    parented to #1202, board status set to "AI Batter Up".
  - Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
    warnings, 0 errors. No desktop/BuildConsole test manifests added (explicitly out of scope).
  - Branch `agent/1801-q864` merged into `origin/main` as `9d7b94a73e656d9bacb55d50c0089498963f299b`,
    verified via `verify-branch-merged.mjs`. `git status --porcelain` clean.
