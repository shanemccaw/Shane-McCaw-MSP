# #2606 — Settings: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-15T13:52:46Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 45ce76a2e (real work), 8f838748c (IN FLIGHT bookend)

## Log
- 2026-09-15T13:49:21Z ⏳ IN FLIGHT — building the Settings screen directly (no `.dc.html` export exists for #2605; Shane authorized building without a design pass, per the dispatch comment on this issue). Wiring the 24 previously-orphaned routes from `msp-settings.ts` Groups A/B/C/D/H/I/J/L (organization profile, connector + Exchange Online, outbound mailbox, service accounts, billing, email templates, customer agreement template, own notification preferences). Groups E/F/G/K are already wired by Staff Roster (`/ops/staff`) and Account Security (`/ops/acctsec`) — not duplicated here.
- 2026-09-15T13:52:46Z ✅ DONE — Shipped `artifacts/msp-console/src/api/msp-settings-api.ts` (data seam, 24 hooks) and `artifacts/msp-console/src/console/modules/Settings.tsx` (8-tab UI: Organization, Connector, Outbound mailbox, Service accounts, Billing, Email templates, Agreement template, Notifications), wired into `ConsoleShell.tsx`/`treeModel.ts` at `/ops/settings`. Carries the required "Agent-built UI — pending design review" banner. Verified: `tsc --noEmit` clean, `vite build` clean (1891 modules), and both new files + the edited shell/treeModel files transform cleanly through an isolated dev-mode Vite server (200s, no compile errors) — no Playwright/shaneapp runTest tool was reachable from this session, so no rendered-browser screenshot was taken; documented honestly rather than claimed. Filed **#4249** (parented under Feature #1690, board status AI Batter Up) for a real backend bug found while building: `PUT /api/msp/settings/connector` unconditionally nulls `customerAgreementTemplate` on every save. This screen's own Connector tab works around it by round-tripping the current template value through the same call. Rebased twice onto a fast-moving `origin/main` (concurrent builds #2609/#4242/etc. landing throughout the session) and re-verified clean each time; confirmed merged via `verify-branch-merged.mjs` before writing DONE. Working tree clean.
