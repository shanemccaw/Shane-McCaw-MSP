# Build Journal — Git #3694

- **Status:** ✅ DONE 2026-09-11T20:23:12Z
- **Scope:** buildconsole

## Log

- 2026-09-11T20:20:16Z — IN FLIGHT. Task: replace `Process.Start`/`UseShellExecute` in the
  Design pill click handler (`Controls/LeftSidebar.xaml.cs` ~line 4043) with
  `mWindow.OpenWebTab(designUrl, ...)`, the same real internal-tab mechanism Web Tools
  already uses (`MainWindow.xaml.cs:2493`), per #3694 and follow-up to #3692.
- 2026-09-11T20:23:12Z — ✅ DONE. Replaced `Process.Start`/`UseShellExecute` at
  `Controls/LeftSidebar.xaml.cs` (Design pill `MouseLeftButtonUp` handler) with
  `mWindow.OpenWebTab(designUrl, epicTitle + " — Design", "🎨")`, resolving `mWindow` via
  `Application.Current.MainWindow is MainWindow mWindow` (matching `IssueDetailView`/
  `GitDetailView`'s existing pattern). Verified with `dotnet build -c Debug` — Build
  succeeded, 0 errors, only pre-existing unrelated warnings (FocusModeBar CS0067).
  Commits: `0ca0962fc` (code, rebased onto main as `bf8532acb`). `git status --porcelain`
  clean. Merged-into-main verified via `verify-branch-merged.mjs`.
