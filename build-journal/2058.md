# Git #2058 — Build Queue: filter-active warning on dispatch/search

- **Status:** ✅ DONE
- **Scope:** buildconsole

## Log
- 2026-08-31 — Session started. Scope: add non-reflowing yellow warning when a build is dispatched or a search performed while a Build Set filter is active. Decision from issue comments: option 2 (visible warning), not auto-unselect.
- 2026-08-31 — ✅ DONE — Added `BuildSetFilterWarningPopup` (WPF `Popup`, overlay layer — genuinely cannot reflow the panel) to `desktop/BuildConsole/Controls/BuildQueuePanel.xaml`. Wired into both decided trigger points via shared `ShowBuildSetFilterWarning`: (1) `QueueSearchBox_KeyDown` (Enter-to-search) when `_buildSetFilter != null`, and (2) new public `BuildQueuePanel.NotifyBuildDispatched()`, called from `MainWindow.xaml.cs`'s `DispatchPanel.Dispatched` handler (fires only on a real successful dispatch). Auto-hides after 8s. Auto-unselect was explicitly rejected per the issue's 2026-08-31 decision comment — not implemented. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/errors. Commits: 9637e149d (bookend IN FLIGHT), 500917e30 (implementation), rebased+pushed to `main` as 5aa8d0a72. `verify-branch-merged.mjs` confirms merged. Working tree clean.
