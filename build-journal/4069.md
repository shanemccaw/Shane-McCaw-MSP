# #4069 — Dashboard: wire the three unconsumed dashboard-export.ts routes into Overview

- **Status:** ✅ DONE 2026-09-15T01:20:11Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 3ea32e005 (feature), 3a94fbc8b (IN FLIGHT bookend)

## Log
- 2026-09-15T01:18:37Z ⏳ IN FLIGHT — wiring GET /portal/dashboard/pdf, GET /portal/dashboard/ppt, GET/POST /portal/dashboard/share into real Export PDF / Export PPT / Share buttons on the Overview page (artifacts/portal/src/pages/index.tsx), following the Documents page's existing export/share pattern (no Design/portal export exists for this affordance).
- 2026-09-15T01:20:11Z ✅ DONE — Added `artifacts/portal/src/lib/dashboard-export-{api,types}.ts` (react-query hooks mirroring `documents-api.ts`) and wired Export PDF / Export PPT / Share buttons + a `DashboardShareDialog` (mirrors `ShareDocumentDialog`) into `index.tsx`'s header row. `Design/portal/` was searched for an Overview/Dashboard export affordance first — none exists (Overview.dc.html has no export/share markup) — so the existing, already-shipped Documents page pattern was followed per the issue's own instruction. Extended `test-manifests/portal/customer-home-overview.json` with new auth-gate/shape `apiTests` (AUTH-GATE-4/5/6, DASHBOARD-SHARE-GET/POST) and button-visibility `uiSteps` (UI3-UI7) under `sessionTag: 4069-dashboard-export-share`; already registered in `_regression-suite.json`. Verification: `tsc --noEmit` clean, `vite build` clean (both in `artifacts/portal`). `shaneapp://runTest` was not invoked — no such tool is registered/reachable in this session (checked via ToolSearch, no match), a genuine protocol-unreachable case, not an assumed limitation; the new apiTests/uiSteps are written and tagged for the next session with runTest access to execute. No genuine findings/bugs surfaced during this build (no new sub-issue filed under #1943). `git status --porcelain` clean; branch rebased onto and pushed directly to `origin/main` (verified via `scripts/dev-server/verify-branch-merged.mjs`: OK).
