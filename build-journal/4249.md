# #4249 — PUT /connector silently nulls customerAgreementTemplate

- **Status:** ✅ DONE 2026-09-15T14:35:00Z
- **Scope:** platform

## Log
- 2026-09-15T14:14:24Z — Starting fix: remove `customerAgreementTemplate` from the `onConflictDoUpdate` `set` clause in `PUT /api/msp/settings/connector` (msp-settings.ts:377-411), keeping it only in the initial `values` insert. Will live-verify the issue's repro via psql/direct route testing.
- ✅ DONE 2026-09-15T14:35:00Z — Fixed `artifacts/api-server/src/routes/msp-settings.ts`: `onConflictDoUpdate`'s `set` clause now destructures `customerAgreementTemplate` out of `values` before use, so the update path never touches that column; the insert path is unchanged. Live-verified against the local Postgres `msp_connector_configs` table (msp_id=1) by simulating the exact old vs. new SQL: old `set` clause nulled an existing template on an unrelated connector-mode-only update, new clause preserved it. `tsc --noEmit` clean for the changed file. Commit `1116260b0` (fix), rebased and pushed to `origin/main` as `c7f399360`; `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
  - Note for #2606's `ConnectorTab` workaround: now unnecessary since the route no longer clobbers the template, but simplifying that call site is Settings' own scope — not done here.
