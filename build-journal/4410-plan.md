# #4410 — pre-work plan

Read #4410's full body and #4306's full body/comments. #4306 already confirmed the
Rainbow-at-100% fill rule itself is intentional (Git #3789) and traced the real rollup
via `ComputeTransitiveLeafRollup`'s leaf filter (root excluded, nested Epics excluded,
`Feature:`-titled placeholders excluded), replicated as a recursive SQL walk against
`bt_issue_mirror`.

Plan:
1. Read `BuildEpicSection()` / the % text formatting code near `LeftSidebar.xaml.cs:3901-4117`
   and diff it against the `frac` value feeding the fill-brush conditional.
2. Recompute real ground truth for Epic #1096 and Issue #77 via the same SQL method #4306
   used (counts may have moved since #4306's session ~10h earlier).
3. Identify the real divergence point (or confirm none exists in the live code).
4. Fix so the text is always derived from the same real fraction as the fill — round only
   for display, never let rounding claim 100% when the fill's own threshold says otherwise.
5. Build verify (`dotnet build BuildConsole.csproj -c Debug`), verify against real #1096/#77
   data, write the bookend, commit, push.
