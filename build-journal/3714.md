# Issue #3714 — Batter Up / AI Batter Up mascot clock leak on Clear()

- **Status:** ✅ DONE 2026-09-11T00:30:00Z
- **Scope:** buildconsole

## Log
- 2026-09-11T00:00:00Z — Starting. Adding `KeyedSlotCardHost.StopOwnedAnimations(RowsList)` before each `RowsList.Children.Clear()` clear site in `BatterUpPanel.xaml.cs` and `AiBatterUpPanel.xaml.cs`, per #3714's issue body (sibling of #3698, same helper).
- 2026-09-11T00:30:00Z — ✅ DONE. Added the one-line call at all 6 clear sites: `BatterUpPanel.xaml.cs:240,441,511` and `AiBatterUpPanel.xaml.cs:203,245,293`. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors. Commit `8c5e019fe` on `agent/3714-q2379`, fast-forward-merged into `origin/main` (verified via `verify-branch-merged.mjs`, exit 0). Working tree clean. No findings beyond what #3714 already specified — no new issue filed.
