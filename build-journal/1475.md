# #1475 — Change Control freeze banner: inverted honest-state gate

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** c5661b7355589fdf0cc02d454939ead8a27177de

## Log
- 2026-08-28 ⏳ IN FLIGHT — fixing `portal-v2-change-control.tsx`'s inverted `isLive` gate (3 declarations + FROZEN timeline strip band) so real data always means "no freeze declared", no-scan-data shows honest empty state, and fictional FZ/CC_FREEZE content is never rendered as fact in any state.
- 2026-08-28 ✅ DONE — Fixed all 4 spots per the issue: the StatsBand freeze stat card, the BriefingView freeze banner + a second, previously-unconditional "FROZEN · {FZ.label}" timeline grid band that rendered regardless of `isLive`, and the SettingsView Active-freeze field. All four now read "No freeze declared for your tenant." / "None declared" in EVERY data state instead of splitting on `isLive`, since no freeze-window table exists at all; the unscanned/fixture path shows the platform's `NoScanDataState` ("No scan data available") instead of `CC_FREEZE`'s fictional Halden banner. Removed now-dead fixture-only freeze-clash computations (`freezeSet`/`freezeClash`/`fzClashes`/`freezeStrip`/`freezeClashLabel`/`freezeClashPill`) that only fed the removed banner branch. `tsc --noEmit` clean for this file (2 pre-existing unrelated errors elsewhere, confirmed not touched by this diff). Updated `test-manifests/portal/change-control.json`'s notes with a new GIT #1475 entry; existing CC10/CC11/CC13c/CC47c assertions (live-tenant path) still hold — ran them via `shaneapp://runTest onlyTag=cc1444` but it hit a navigation error because the local dev server (`C:\dev-server`) can't currently advance past a REAL, PRE-EXISTING merge conflict unrelated to this issue (`test-manifests/portal/security-drilldowns.json`, `build-journal/1430.md`, `artifacts/msp-portal/src/pages/portal-v2-security-evidence.tsx`, `artifacts/msp-portal/src/components/portal-v2/secEvidenceData.ts` — status.mjs shows several prior cycles failing the same way, `merged=0 conflicts=1`, before this session even started). Did not attempt to resolve someone else's in-flight conflict. Commit `c5661b73` verified merged into `origin/main` via `verify-branch-merged.mjs` and `git merge-base --is-ancestor`.
