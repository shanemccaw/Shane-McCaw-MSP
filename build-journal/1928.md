# #1928 — admin-projects.ts POST missing clientUserId validation

- **Status:** ✅ DONE 2026-09-06T00:10:00Z
- **Scope:** platform

## Log
- 2026-09-06T00:00:00Z — Session started. Fix: require clientUserId in POST /admin/projects, same as title, per issue body.
- 2026-09-06T00:10:00Z — ✅ DONE — Added `if (!clientUserId) { res.status(400)... }` validation to `POST /admin/projects` in `artifacts/api-server/src/routes/admin-projects.ts`, matching the existing `title` check, and dropped the now-redundant `?? null` fallback on the insert. tsc --noEmit clean for the file. Commit `36e743388c6ee214aae9b7fbd52e2526b5a2f896` pushed directly to `main` (fast-forwarded from worktree branch `agent/1928-q1741`), verified ancestor of origin/main.
