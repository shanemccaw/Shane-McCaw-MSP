# #2872 — 34 powershell-transport rows + Get-CompareParameters/Get-M365TenantId fake-cmdlet pollution

- **Status:** ✅ DONE 2026-09-05T00:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 12214190b

## Log
- 2026-09-04T23:39:58Z ⏳ IN FLIGHT — starting. Same method as #1896/#2839: for each
  affected `config_resources` row, read the resource's real Microsoft365DSC
  `Get-TargetResource`/`Test-TargetResource` source at the catalog's cited `source_ref`
  commit, trace past `Get-MSCloudLoginConnectionProfile` / `Get-CompareParameters` /
  `Get-M365TenantId` to the real read cmdlet(s), correct `read_cmdlets` via a manual
  migration run against local `DATABASE_URL`. Two classes in scope: (1) 34
  `powershell`-transport rows citing `Get-MSCloudLoginConnectionProfile` (byproduct of
  #1960's graph->powershell reclassification, confirmed distinct from #1896's 12 rows);
  (2) `Get-CompareParameters`/`Get-M365TenantId` citations across all transports.
- 2026-09-05T00:05:00Z ✅ DONE — Traced both classes against real Microsoft365DSC source at the
  catalog's own cited commit (`Microsoft365DSC/Microsoft365DSC@f79f297`). (1) All 34
  `powershell`-transport rows citing `Get-MSCloudLoginConnectionProfile` already carried a real
  cmdlet elsewhere in their array (verified individually), so per #1896/#2839's precedent only the
  fake entry was dropped, no row needed a full rewrite. (2) `Get-CompareParameters` (123 rows:
  azure-rm 7, graph 25, powershell 85, sharepoint-admin 6) is confirmed the same shape of
  pollution as the connection helper -- a locally-defined, `Export-ModuleMember`'d comparison
  helper called only from `Test-TargetResource`, never `Get-TargetResource` -- generically true
  across every resource that defines it, spot-verified against several distinct resources' real
  source. 118 of those rows already had a real cmdlet elsewhere and just had the fake entry
  dropped; 5 rows (`AzureBillingAccountsAssociatedTenant`, `IntuneDeviceControlPolicySetting`,
  `IntuneEpmCertificatePolicySetting`, `IntuneFirewallPolicySetting`,
  `DefenderDeviceAuthenticatedScanDefinition`) had it as their ONLY entry and were individually
  traced to their real `Get-TargetResource` read call: `Invoke-AzRestMethod` (via
  `Get-M365DSCAzureBillingAccount*` in `M365DSCAzureHelper.psm1`, same helper #2839 already
  traced for sibling AzureBillingAccount* resources), `Invoke-MgGraphRequest` x3 (direct GET
  calls), and `Invoke-WebRequest` (via `Invoke-M365DSCDefenderREST` in
  `M365DSCDefenderHelper.psm1`, which wraps `Invoke-WebRequest` directly). `Get-M365TenantId`
  (1 row, `PPTenantIsolationSettings`) traced to a locally-defined helper in that resource's own
  `.psm1`, called inside `Get-TargetResource` only to translate a tenant name string to a GUID
  for result formatting -- not a read call -- confirmed the same pollution shape; dropped, leaving
  the row's existing `Get-MgContext` entry.

  Wrote and ran `lib/db/migrations/manual/2026-09-04-fix-cmdlet-pollution-2872.sql` locally
  against `DATABASE_URL` (34 + 118 + 1 + 5x1 row `UPDATE`s in one transaction, using the jsonb `-`
  operator for the bulk drops rather than retyping ~150 arrays by hand, self-marking `INSERT` into
  `simulator_migration_runs`). Verified post-migration: 0 rows remain citing
  `Get-MSCloudLoginConnectionProfile` (powershell-transport), `Get-CompareParameters`, or
  `Get-M365TenantId` anywhere in `read_cmdlets`.

  Also extended `scripts/config-state/parse-m365dsc.mjs`'s `CMDLET_NOISE` exclusion regex to
  include `CompareParameters` and `M365TenantId`, so a future re-run of the `.psm1`-body fallback
  extraction won't reintroduce either as a fake read-cmdlet citation.

  No new findings discovered mid-trace this session (both classes named in #2872's own issue body
  were resolved in full; nothing additional surfaced).

  Committed `12214190b` directly to `origin/main` (rebased onto main, which had moved to
  `20b1bcc40` since session start). `node scripts/dev-server/verify-branch-merged.mjs` confirms
  merged. `git status --porcelain` clean.
