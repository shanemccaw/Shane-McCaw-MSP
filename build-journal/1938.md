# #1938 — msp_sop_runs: manual POST /api/msp/sop-runs write path bypasses the #1559 CR-gated execution hook

- **Status:** ✅ DONE 2026-09-03T02:35:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 65909f2a6 (rebased/pushed to main as 57f187873)

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — narrowing `POST /api/msp/sop-runs` (`artifacts/api-server/src/routes/msp-sops.ts`) to genuinely manual-only runs per the issue's suggested fix: force `origin: "manual"`, refuse a `status` other than `"In Progress"` at creation. Confirmed no real caller (frontend or MCP tool) POSTs to this route today — only `GET /msp/sop-runs` is used outside this file — and confirmed `wf_run_id`/`automated_step_map` were already unreachable from this route's schema (not in `createSopRunSchema`).
- 2026-09-03T02:35:00Z ✅ DONE — `createSopRunSchema`'s `origin`/`standingPolicyId` fields removed (a policy-enacted run only ever comes from `runSopForCustomer`, never this hand-entry route); `status` narrowed to `z.literal("In Progress")` so a run can no longer be born already "Completed"/"Blocked"/"Failed"; the insert now hardcodes `origin: "manual"` server-side regardless of caller input; the now-dead `standingPolicyId` coherence-check block removed along with the newly-unused `standingPoliciesTable` import. `wf_run_id`/`automated_step_map` were already absent from this route's schema — confirmed, not newly fixed. Merged onto `main` at `57f187873` (rebase + push, `verify-branch-merged.mjs` confirms ancestor). Working tree clean. **Verification limitation, honestly reported:** could not run `tsc --noEmit` or vitest for this package — the shared main checkout's `artifacts/api-server/node_modules` hoisted layer is broken (express/drizzle-orm/zod/@types-node all unresolvable, confirmed pre-existing in the main checkout itself, not this worktree), already tracked as #2192 (filed 2026-09-02, before this session). Verified instead by manual review of the diff and confirming no other file references the removed schema fields (`origin`/`standingPolicyId` on `createSopRunSchema`) or the removed `standingPoliciesTable` import.
