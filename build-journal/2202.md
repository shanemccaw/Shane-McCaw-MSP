# #2202 — Feature: API Explorers + Autofill Lock (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T16:14:40Z
- **Scope:** buildconsole  <!-- ShaneBuilder WPF app (desktop/ShaneBuilder), the new isolated project per #2178 -->
- **Started:** 2026-09-02
- **Commit(s):** `0c8475f9d` (feature; landed on origin/main). Bookend IN FLIGHT `8aad5ec12`→`1679ed74e`.

## Log
- 2026-09-02T15:59:49Z ⏳ IN FLIGHT — Completing readme-phase2.md Step 12 (API Explorers + Autofill Lock) in ShaneBuilder.
  Blocker #2204 (Settings / Accounts & Tiers store) confirmed CLOSED + landed on main at `e17b4620e` —
  `SettingsStoreService.GetProfiles/AddProfile/RemoveProfile` is the real profile store to read from.
  Groundwork from #2220 exists (`Services/ApiExplorerService.cs` + MainWindow mini-panel: three modes,
  DRY-RUN default, client_credentials + local password login). Remaining #2202 scope:
  (1) wire the Autofill Gated Profile lock to #2204's real store (pick-fills-both, +Add-and-use, tier badges);
  (2) formalize the ITokenBroker/GatedProfile/GraphTenant contract from the readme;
  (3) permission/scope gate — missing scope shows amber banner + consent URL and a real Graph 403 on LIVE;
  (4) Raw Response vs JSON Output; (5) third entry point (Ctrl+K palette). Security surface — DRY-RUN
  default and write-path guardrails handled with care, no shortcuts.
- 2026-09-02T16:14:40Z ✅ DONE — Shipped on top of the #2220 mini-panel, in `desktop/ShaneBuilder`:
  - **Services/ApiExplorerService.cs** — formalized the readme Step 12 contract as REAL, USED code:
    `GraphTenant`, `GatedProfile` (FromAccount maps #2204's `AccountProfile` — no parallel store),
    `Token`, and `ITokenBroker`/`TokenBroker` (`ClientCredentials` + `PasswordLogin`, one HttpClient
    POST each, no MSAL/NuGet). `ParseGrantedScopes` reads the token's own `roles`/`scp` claim;
    `HasPermission` + `BuildAdminConsentUrl` back the permission gate. `ExecuteAsync` now captures the
    real diagnostic headers (content-type, request-id, client-request-id, x-ms-ags-diagnostic, date)
    into a `RawBody` so Raw Response genuinely differs from JSON Output.
  - **MainWindow.xaml.cs** — Autofill Gated Profile lock (droop animation, tier badges) wired to the
    real `SettingsStore.GetProfiles()/AddProfile()`; pick fills both fields + marks "filled from the
    gated profile", "+ Add an account" does add-it-and-use-it (same list as Settings → Accounts &
    Tiers). Token panel shows for/flow/TTL. Permission gate: a Graph endpoint whose scope isn't in the
    token's granted scopes drops an amber banner + copyable admin-consent URL; LIVE still sends so the
    real Graph 403 surfaces. Raw/JSON response toggle. Ctrl+K palette gained the three explorers (third
    entry point, alongside the topbar wrench menu and the in-chat panel). LIVE Graph writes are audit-
    logged. DRY RUN stays the default on every panel open (reset in OpenApiExplorer, never persisted).
  - **Concurrency:** merge-conflicted with concurrently-landed #2205 (Graph App Registration picker/
    editor + `ResolveGraphCredentials(root, store)` signature). Resolved by KEEPING #2205's picker/gear/
    editor intact and appending #2202's granted-scope readout; updated my callers to pass `SettingsStore`.
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors (twice: before
    and after the #2205 merge). Branch merged into origin/main confirmed via verify-branch-merged.mjs;
    `git status --porcelain` clean. No live WPF UI click-through (ShaneBuilder is internal tooling — no
    test manifest per CLAUDE.md scope; never launch the exe to drive its UI per #2156). Logic verified
    by review. No out-of-scope findings discovered.
