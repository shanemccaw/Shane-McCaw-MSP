# Git #1533 — M365 Changes resolution layer

- **Status:** ✅ DONE
- **Scope:** platform

## Log

- ⏳ IN FLIGHT — session opened (BuildConsole build 765, worktree C:\wt\1533-q765). Reading #1494 and #1533 threads, then building the resolution layer that runs interpretations against a real tenant (reusing monitor_checks + ca-ps-execution, license_assignment_snapshots for SKU cases; "not measured" where no probe exists).
- ✅ DONE — resolution layer shipped as `b85ba185c` on main (verify-branch-merged: OK). New `m365_change_resolutions` table (one current answer per interpretation × customer) + manual migration `lib/db/migrations/manual/2026-08-28-m365-change-resolutions-1533.sql` (Shane must run it). Resolver lib `m365-change-resolver.ts` reuses executeMonitorCheck (Graph / ca-ps-execution PowerShell / sharepoint-admin / dns by the check's own executorType, stored profile preferred, live only on explicit resolve) and license_assignment_snapshots (touches.skus matched to the tenant's stored /subscribedSkus catalog; unmapped names → not_measured/sku_not_mapped, never zero). Admin endpoints: POST /admin/m365/interpretations/:id/resolve (confirmed-only, 409 otherwise) + GET .../resolutions. Daily stored-data-only sweep wired in index.ts. Portal /portal/message-center now serves per-post `analysis` (confirmed interpretation + measured count, `noise: true` on measured zero); "your tenant has not been read against this notice" wording preserved for everything unmeasured. Verification: api-server typecheck clean for all touched files (remaining errors are pre-existing in untouched files: ai-billing, admin-build-tracker, old tests); vitest 8/8 new + 71/71 neighboring (license-waste*, portal-message-center) pass.
