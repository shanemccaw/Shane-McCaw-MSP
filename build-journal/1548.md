# #1548 — Policy Engine: Policy is enacted by an SOP — the engine does not execute

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `1a0385b53` (schema + migration), `db3b02a1c` (routes/wire, real post-push hash)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1548 body + comments, and the settled architecture on
  #1547 (done, `standing_policies` table exists), #1550 (in-flight parallel build),
  #1553 (in-flight parallel build), #1493/#1556 (done — `msp_sop_runs.origin` +
  unification). #1548's own scope, given those: "names the procedure" (a `sopId` on
  `standing_policies`) and "msp_sops/msp_sop_runs become the enactment record for
  policy" (a `standingPolicyId` trace-back on `msp_sop_runs`) — both additive links
  onto tables #1547/#1556 already built, consumed by the EXISTING execution path
  (`POST /api/msp/sops/:sopId/run` → `runSopForCustomer`, #1559) per the issue's own
  "no new execution path is created" constraint. Building schema + migration + routes now.
- 2026-08-30 ✅ DONE — Shipped both halves of the settled sentence: "Policy defines a target
  state and names the procedure that achieves it" (`standing_policies.sop_id`, nullable text
  naming `msp_sops.sop_id`, validated to belong to the same MSP on author) and "msp_sops/
  msp_sop_runs become the enactment record for policy" (`msp_sop_runs.standing_policy_id`,
  nullable int FK -> `standing_policies.id`). No new execution path: both existing SOP-run
  writers (`POST /api/msp/sops/:sopId/run` -> `runSopForCustomer`, and the hand-entered
  `POST /api/msp/sop-runs`) now accept an optional `standingPolicyId`, verified before the
  row is written — must belong to this MSP, must be `isActive`, must name exactly this
  `sopId`, and forces `origin` to `"policy"`.
  - Migration `lib/db/migrations/manual/2026-08-30-policy-sop-enactment-1548.sql` — run
    against local Postgres in-session (both columns + indexes + FK verified via
    `information_schema`; logged on #1630, not `Shane To-Do`).
  - Code: `lib/db/src/schema/msp.ts` (both columns), `artifacts/api-server/src/lib/
    standing-policies.ts` + `.test.ts` (wire contract + 7 passing unit tests, one new),
    `artifacts/api-server/src/routes/msp-standing-policies.ts` (author-time `sopId`
    validation), `artifacts/api-server/src/lib/sop-execution.ts` +
    `artifacts/api-server/src/routes/msp-sops.ts` (enactment binding + validation on
    both run-creation paths).
  - **Verification:** `lib/db` typechecks clean standalone (`tsc --noEmit`).
    `standing-policies.test.ts` observed green (7/7, including the new test) via
    `vitest run` on this exact code before a mid-session worktree/node_modules reset (see
    below) made a second live re-run of that same file fail on `@workspace/db` module
    resolution alone (the shared node_modules junction for this worktree was repointed by
    concurrent build provisioning, observed pointing at two DIFFERENT other agents'
    worktrees in succession) — not a code failure, and I did not re-claim a clean run I
    didn't observe. In its place: a real transactional (rolled-back) DB integration test
    against local Postgres proved the actual runtime behavior — inserting a policy naming
    a real `msp_sops.sop_id`, inserting a run with `standing_policy_id` + `origin='policy'`
    referencing it, and confirming the FK rejects a bogus `standing_policy_id` — then
    rolled back with 0 rows left behind (confirmed via count queries after).
  - **Session note:** partway through, this worktree's git branch pointer and the
    `node_modules/@workspace/db` junction were both reset by concurrent build/provisioning
    activity outside this session's control (the branch tip briefly reverted to an
    unrelated commit, discarding my then-uncommitted route/lib edits). The already-pushed
    schema+migration commit (`1a0385b53`) was unaffected on `origin/main`; the lost
    route/lib edits were reapplied byte-for-byte from this session's own record and
    recommitted as `db3b02a1c`, then re-verified against the DB directly. No findings
    filed for this — it's the documented worktree-junction sweep hazard
    ([[provision-worktree-owner-pid-sweep-hazard]]), not a product defect.
  - **Findings:** none new. `active_directory_ous` and `standing_policies` remain at 0 rows
    in the local DB (matches #1547's own note — OU seeding is out of scope, create-on-demand).
