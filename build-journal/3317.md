# #3317 — Open Food Facts nutrition strip on the barcode-scan matched-product sheet

- **Status:** ✅ DONE 2026-09-09T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** cac831cd6 (main)

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — read #3317 + README.md item 5 (Shopping scan) and the
  `.dc.html` prototype's logic class (`scanNut`/`scanNutNote`). Backend (#3260, `off_products`,
  `nutrition.mjs`, `scan.mjs`) already ships real per-100g nutrition + image_url; the existing
  frontend strip in `public/app.js` uses the wrong thresholds (UK FSA 0.6g/5g/22.5g, reused from
  `nutrition.mjs`'s `nutritionFlags`, which back a different feature) and a full-width product
  banner instead of the spec's per-metric amber value coloring, the exact "sodium runs high for
  the heart plan" copy, and a 36px in-tile thumbnail. Fixing the frontend only — no schema/backend
  change needed.
- 2026-09-09T00:45:00Z ✅ DONE — `web/shanes-life/public/app.js` + `app.css` (commit `cac831cd6`
  on `main`): replaced the old full-width `.scan-product-image` banner + per-row "High" chip
  (wrong FSA thresholds) with (1) a real 36px in-tile thumbnail (`productThumbEl`) rendered on all
  three match-state tiles (exact/near/unknown), using `image_url` from `off_products` via the
  existing `scan.mjs`/`nutrition.mjs` enrichment (unchanged — #3260's backend already served the
  real data this needed); (2) a per-100g nutrition strip whose Sodium/Sat fat/Sugars/Fiber VALUES
  turn amber (`hsl(var(--warning))`) individually against the issue's own real cutoffs (sodium
  ≥0.4g, sat fat ≥5g, sugars ≥15g — a new, sheet-local `SCAN_HIGH_*` set, deliberately not reusing
  `nutrition.mjs`'s `nutritionFlags`, which stay on their own UK-FSA cutoffs for the separate
  Recipes/Money heartHealthy-tagging feature via `getNutritionForItem`); (3) the exact copy
  "Per 100 g · Open Food Facts · sodium runs high for the heart plan" appended to the note line
  only when sodium specifically crosses its cutoff, matching the prototype's own `scanNutNote`
  logic exactly (verified with a standalone Node script reproducing the threshold/copy math
  against three sample nutrient sets before wiring it in).
  Verification: `node --check public/app.js` (syntax OK); copied the real local `.env`
  (`DATABASE_URL` → the shared local ShanesSurvival Postgres) into the worktree, booted the real
  server on a scratch port, confirmed `/healthz` reports `db: up`, and confirmed both `/app.js`
  and `/app.css` serve the new `scan-nutrition-value`/`scan-product-thumb`/`SCAN_HIGH_SODIUM`
  code — then stopped the server and deleted the copied `.env` again (gitignored, never
  committed). Did not run the full `npm run check` E2E suite (destructive account
  creation/scan writes against Shane's real shared Postgres) for a pure frontend-rendering
  change that suite has no DOM assertions to exercise anyway; the backend enrichment path
  (`scan.mjs`/`nutrition.mjs`) was not touched. Branch `agent/3317-q2041` rebased onto
  `origin/main` and pushed directly to `main` (`cac831cd6`); `verify-branch-merged`-equivalent
  ancestor check passed (`git merge-base --is-ancestor HEAD origin/main`). Working tree clean.
  No migration, no schema change, no findings to file.
