# #3401 — Feature: Remove leftover legacy site-analytics remnants from Admin Panel

- **Status:** ✅ DONE 2026-09-11T01:42:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none — investigation-only conclusion, no code changes to #3401 itself; filed #3610 as a finding

## Log
- 2026-09-10T02:07:54Z ⏳ IN FLIGHT — pulled issue body + comments, then checked the
  build header's `--blocked-by 3441` before touching any code.
- 2026-09-10T02:07:54Z 🛑 BLOCKED — confirmed via `gh api
  repos/shanemccaw/Shane-McCaw-MSP/issues/3401/dependencies/blocked_by` that a real
  `blocked_by` edge exists pointing at #3441, and #3441 is `state: open`, labeled
  `in-flight` (not `complete`/closed). Per CLAUDE.md's "picking this back up later"
  rule, a still-open, non-complete blocker means stop rather than guess forward. No
  investigation of Analytics.tsx reachability or any deletion work was started — the
  session stopped at the blocked-by check itself, per the build header's own
  `--blocked-by 3441`.
- 2026-09-10T17:45:00Z 🛑 BLOCKED — resumed session, re-checked before doing anything
  else. `gh api .../issues/3401/dependencies/blocked_by` still shows the edge to
  #3441; #3441 is still `state: open`, still labeled `in-flight`/`blocked`, not
  `complete`/closed. Went one level further this time: #3441 itself has a real
  `blocked_by` edge to #3429, which is also still `state: open`. So the whole chain
  (#3401 → #3441 → #3429) is still unresolved. Per the standing "picking this back up
  later" rule, still genuinely blocked — stopped again without touching
  `Analytics.tsx` or any code. No status/label changes needed since nothing changed
  since the last check.
- 2026-09-11T01:38:00Z — Resumed again, re-checked the chain first. #3441 is now
  `state: closed`, labeled `complete`; its own blocker #3429 is also `state: closed`,
  labeled `complete`. Found myself unblocked: removed the stale `blocked_by` edge
  #3401→#3441 (`DELETE .../dependencies/blocked_by/5406650021`), flipped labels
  `blocked`→`in-flight`, rebased worktree onto current `origin/main`, and proceeded
  with the real investigation.
- 2026-09-11T01:42:00Z ✅ DONE — Investigated Analytics.tsx reachability per the
  issue's own scope, branch 3 applies: **it is still serving something real — did
  not delete anything.** Real evidence:
  - `artifacts/admin-panel/src/components/shell/workspaceNav.tsx:241` — live nav
    entry `cmd-analytics` under the "Command" workspace's "Overview" section,
    path `/command/analytics`.
  - `artifacts/admin-panel/src/App.tsx:173-176` — `/command/:section` routes into
    `CommandWorkspace`; `App.tsx:330` even has a legacy `/analytics` →
    `/command/analytics` redirect.
  - `artifacts/admin-panel/src/pages/workspaces/CommandWorkspace.tsx:12` — `case
    "analytics"` renders `<AnalyticsPage />` (`@/pages/Analytics`).
  - `artifacts/admin-panel/src/pages/Analytics.tsx` — a real, fully-wired dashboard
    (KPIs, pageviews chart, top pages/events/referrers/campaigns, AI revenue
    forecasting, Portal Engagement card-click tracking) hitting real backend
    endpoints under `/api/admin/analytics/*`
    (`artifacts/api-server/src/routes/analytics.ts`), which is genuinely GA4
    Data-API-backed (`BetaAnalyticsDataClient`), not a stub.
  - `git log --follow -- .../pages/Analytics.tsx` — commit `e77ccab67 GA4-source the
    Admin Panel Analytics dashboard` shows this exact file was rewritten in place to
    become #118's ("Admin Panel GA4 Insights view", closed) deliverable — it IS
    #118, not something #118 superseded. #123's own closed body explicitly scopes
    only `AnalyticsHeatmap.tsx`, `SessionReplay.tsx`, and `Attribution.tsx` for
    deletion — never `Analytics.tsx`. The premise in #3401's issue body (that
    Analytics.tsx is leftover pre-GA4 debris) does not hold.
  - Filed **#3610** as a separate, real, verified finding from the same
    investigation: #123's own stated scope to drop `analytics_site_events`,
    `analytics_pageviews`, `analytics_sessions` via manual SQL was apparently never
    executed — confirmed via `psql "$DATABASE_URL" -c "\dt"` against the real local
    dev DB; all three tables still exist. Parented under #1095, labeled `bug`,
    milestone v1.1, board status set to AI Batter Up.
  - Posted the real conclusion as a comment on #3401 itself (no NOT_PLANNED closure
    — that's Shane's call).
  - `git status --porcelain` clean (only the bookend file itself was touched this
    session, already committed/pushed). No repo code changes were needed or made.
