# #1372 — Git worktrees share a single node_modules, not per-worktree installs

- **Status:** ✅ DONE
- **Scope:** platform (scripts/dev-server + desktop/BuildConsole)
- **Started:** 2026-08-26
- **Commit(s):** `05cba6a0e` (provisioner passes/documents `--link`) · `bbbc67705` (BuildConsole auto-provisions with `link: true`)

Follow-up to [[1371]]. Constraint (Shane): a limited Verizon wifi connection means
8 builds each downloading node_modules would eat the connection and take forever.

## Log
- 2026-08-26 ⏳ IN FLIGHT — This repo is a pnpm workspace, and
  `scripts/dev-server/link-deps.mjs` already junctions `node_modules` + `lib/*/dist`
  from the main repo into each worktree via the `--link` flag (Windows `mklink /J`) —
  a share-not-install approach, zero re-download. Work: ensure #1371's automatic
  provisioning always passes `--link`, and prove with a live 2-worktree test that no
  second dependency download happens.
- 2026-08-26 ✅ DONE — Confirmed + proven. This repo is pnpm; `link-deps.mjs` junctions
  a SHARED node_modules rather than installing per-worktree. #1371's automatic
  provisioning passes `--link` (BuildConsole `WorktreeProvisionService.ProvisionWorktreeAsync
  (link: true)`), and the CLAUDE.md worktree convention documents it. **Live proof:**
  provisioned two worktrees off origin/main; the linked one got **29 junctions**, and its
  `node_modules` is a Windows **Junction → `C:\Source\…\node_modules`** (the main repo's
  single copy) — i.e. ZERO dependency downloads for a new worktree, not one, not eight.
  Cleanup unlinks the 29 junctions before removing the worktree (the "delete THROUGH
  junctions" guard), so the shared store is never touched. No new install code was needed —
  the share-not-install mechanism already existed; #1371 just always uses it.
