# #1534 — Microsoft Changes: Automatic routing to Change Control and Risk

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** ab2368df8 (schema+migration), cbbd6b4c5 (routing engine + workflow node + wire + decline endpoint), 59faa543d (unit tests), a9186923c (vitest include)
- **Migration:** `lib/db/migrations/manual/2026-08-29-m365-change-routing-1534.sql` (run against local DATABASE_URL)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Routing resolved Microsoft changes into Change Control (auto-create CR, Microsoft as implementer) and Risk (declined → accepted risk via #1514). Investigating existing change-control / message-center / risk schema and routes.
- 2026-08-29 ✅ DONE — Built the M365 Changes ROUTING layer (third stage after #1532 interpretation / #1533 resolution).
  - **Schema (ab2368df8):** `msp_change_requests` gained `intake` (informed/approval/advisory), `implementer` (microsoft/customer/msp), and the link back to the announcement (`source_kind`, `source_graph_message_id`, `source_interpretation_id`, `source_resolution_id`); `msp_risk_decisions` gained `spawned_by_change_request_id` / `discharged_by_change_request_id` (#1514 pointers); new `m365_change_routings` ledger table (one decision per interpretation×customer). Migration run against local Postgres 18.
  - **Engine (203dbbf6d):** `artifacts/api-server/src/lib/m365-change-router.ts` — `decideRouting` gate (measured+dated+non-zero → auto-create a CR with Microsoft as implementer; undated/no-announcement/zero-affected → propose only; not-measured → none), intake/implementer derivation from `who_acts`/`controllable`, idempotent sweep (`runM365ChangeRoutingSweep`), and `declineRoutedChangeToRisk` (customer decline → accepted risk with the back-pointer; MSP decline → no risk, per #1514).
  - **Visible Workflow Engine node:** `m365_route_changes` registered in `workflow-executor.ts` + `node-type-registry.ts`, seeded as `__system__: M365 Changes Routing` (daily 04:00 UTC, after the resolution sweep). No bare scheduler.
  - **Wire:** implementer/intake/sourceGraphMessageId added to the change-control GET; portal decline endpoint `POST /portal/change-control/:code/decline`.
  - **Verification:** `tsc -p tsconfig.json --noEmit` exit 0; router unit tests 14/14; portal-change-control + node-type-registry + workflow-node-output-samples + ai-usage-metering all green. End-to-end against the real local DB (temp spec, since api-server has no test-manifest scope): seeded a confirmed interpretation + dated MC post + measured 412-count resolution → sweep auto-created a CR (implementer=microsoft, intake=informed, auto-approved), idempotent on re-run, and a customer decline produced an accepted risk linked back to the CR. Test rows cleaned up; DB confirmed empty. Branch verified merged into origin/main.
