# #3683 — Plan Capability Rules admin UI (msp_plan_capabilities) writes a table requirePlanFeature() never reads

- **Status:** ⏳ IN FLIGHT 2026-09-11T20:31:51Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T20:31:51Z ⏳ IN FLIGHT — wiring `loadTier()` in `artifacts/api-server/src/lib/msp-entitlement.ts`
  to overlay `mspPlanCapabilitiesTable` rows (keyed by `serviceId`) on top of the existing
  `typeAttributes.tierCapabilities` map, so `requirePlanFeature()` / `tierAllowsFeature()` /
  `hasPlanFeature()` (and `msp-launch-control.ts`, which also consumes `loadTier()` directly)
  all actually honor capability rules a PlatformAdmin sets via `/msp/plans`. A rule row's
  `enabled` wins over typeAttributes when present; a missing row falls back to the prior
  typeAttributes-only behavior — no regression for services with no capability rules.
