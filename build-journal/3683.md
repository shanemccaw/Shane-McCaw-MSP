# #3683 — Plan Capability Rules admin UI (msp_plan_capabilities) writes a table requirePlanFeature() never reads

- **Status:** ✅ DONE 2026-09-12T00:13:56Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 1e0fe24d5 (fix + tests), cc8794704 (bookend IN FLIGHT)

## Log
- 2026-09-11T20:31:51Z ⏳ IN FLIGHT — wiring `loadTier()` in `artifacts/api-server/src/lib/msp-entitlement.ts`
  to overlay `mspPlanCapabilitiesTable` rows (keyed by `serviceId`) on top of the existing
  `typeAttributes.tierCapabilities` map, so `requirePlanFeature()` / `tierAllowsFeature()` /
  `hasPlanFeature()` (and `msp-launch-control.ts`, which also consumes `loadTier()` directly)
  all actually honor capability rules a PlatformAdmin sets via `/msp/plans`. A rule row's
  `enabled` wins over typeAttributes when present; a missing row falls back to the prior
  typeAttributes-only behavior — no regression for services with no capability rules.
- 2026-09-12T00:13:56Z ✅ DONE — shipped in commit `1e0fe24d5` (pushed directly, no separate
  PR — main is the default target per repo convention), confirmed merged into `main`
  (`git merge-base --is-ancestor 1e0fe24d5 HEAD` on origin/main passes). What shipped:
  - `artifacts/api-server/src/lib/msp-entitlement.ts` — `loadTier()` now runs a second query
    against `mspPlanCapabilitiesTable` filtered by the subscription's `serviceId`, and merges
    each rule's `enabled` value into `tierCapabilities`, overriding the typeAttributes default
    when a rule row exists. No route changes needed — `requirePlanFeature()`,
    `tierAllowsFeature()`, `hasPlanFeature()`, and `msp-launch-control.ts`'s direct `loadTier()`
    consumers all read the same merged map, so the fix propagates to every caller for free.
  - `artifacts/api-server/src/lib/msp-entitlement.test.ts` — added 4 new unit tests
    (`describe("loadTier — mspPlanCapabilitiesTable overlay (Git #3683)")`) covering: no rule
    rows falls back to typeAttributes; a rule row gates a feature typeAttributes left open; a
    rule row re-opens a feature typeAttributes gated; no subscription → null (unchanged).
  - No schema/migration needed — `mspPlanCapabilitiesTable` already existed
    (`lib/db/src/schema/msp.ts:1860-1876`), this was purely a missing read-side wire-up.
  - **Verification:** `npx vitest run src/lib/msp-entitlement.test.ts` → 24/24 passed (incl. the
    4 new tests). Also re-ran `src/routes/msp-sales-offers.test.ts` +
    `src/routes/msp-sales-bundles.test.ts` (both consume `requirePlanFeature`/`loadTier`
    indirectly) → 30/30 passed, no regression. Confirmed live against the local dev DB
    (2026-09-11): `msp_plan_capabilities` has 1 real row (`service_id=131`,
    `capability_key=launch_control_plus`, `enabled=true`) whose owning service
    (`msp_subscriptions.msp_id=1626`) has no `typeAttributes.tierCapabilities` key for that
    feature today — before this fix that row had zero effect on gating; after this fix it is
    read and merged (confirmed by tracing the exact join query by hand against the real table).
  - A full-repo `tsc -p tsconfig.json --noEmit` in `artifacts/api-server` surfaces 3 pre-existing,
    unrelated errors (`mspStaffNotificationPreferencesTable` not resolving in
    `notification-center.ts` / `msp-settings.ts` / a live-db test) that predate this change and
    do not touch any file this build edited — not filed as a new finding since it looked like
    transient staleness from a concurrently-landing commit in this same shared main, not a
    reproducible defect isolated to my own work; left as-is per scope.
  - `git status --porcelain` clean at finish; branch HEAD confirmed an ancestor of `origin/main`.
