# #3282 — URGENT: Meds should always be compact one-line-per-batch (supersedes #3269), merge in pet meds/feeding

- **Status:** ✅ DONE 2026-09-09T00:08:06Z
- **Scope:** platform (web/shanes-life -- Shane's Life, not the MSP product)
- **Started:** 2026-09-08
- **Commit(s):** 967bffbe2 (Meds: always compact one-line-per-batch, merge pet meds/feeding)

## Log
- 2026-09-09T00:04:16Z ⏳ IN FLIGHT — read #3282's full body (Shane's own direct correction,
  supersedes #3269). Confirmed `set_pet_care`'s backend capability already exists and is fully
  wired: `medications.getMedsToday()` (`src/core/medications.mjs`) already queries `pet_care`
  joined to `pets` and merges each row into its matching batch (`isPetCare` flag, `petName`) --
  this issue is a pure rendering gap, no backend work needed. Read
  `Design/design_handoff_shanes_life/Shanes Life - First Slice Prototype.dc.html` lines 690-750
  (the real newer design source Shane named) and the current `public/app.js` `medBatchCard`/
  `viewMeds` (Git #3269's itemized-when-active implementation) to plan the compact-line rewrite.
  Next: rewrite `medBatchCard` to render every batch as one compact line (organic-blob avatar,
  untaken "N for you, M for the pets"/named-pet-item summary, taken "Taken H:MM · names" summary
  + green pill + Undo applied uniformly), add the real footer copy line, update CSS.
- 2026-09-09T00:08:06Z ✅ DONE — Rewrote `medBatchCard` (`public/app.js`) to always render one
  compact line: real `.med-batch-avatar` organic-blob (`58% 42% 55% 45% / 48% 56% 44% 52%`,
  tinted amber/periwinkle by daypart) replaces the bare critter icon; itemized item rows
  (`.med-item-row`/`.med-item-icon`, `PILL_ICON_PATH`) removed entirely -- untaken batches show
  "{N} for you"/single-named-or-counted pet summary (`medBatchUntakenSummary`), taken batches
  show "Taken {H:MM} · {names, truncated to 3 +N more}" in success-green
  (`medBatchNamesSummary`/`.med-batch-summary-done`) with Undo now applied to every batch, not
  only the previously-non-collapsed one. "Later" batches keep the existing dimmed/swipe-less
  state (`.med-batch-collapsed`), only the current timed batch (or any untimed "as needed"
  batch) keeps the real slide-to-take control -- unchanged logic from #3269, just no more
  itemized rows gating it. Added the real footer copy line verbatim in `viewMeds()`. No backend
  changes -- `medications.getMedsToday()`/`pets.mjs` already merge `pet_care` into its matching
  batch (`isPetCare`/`petName`), confirmed by reading both files before touching anything, so
  point 3 of the issue's scope was already true. Verified: `node --check public/app.js` (clean),
  isolated node run of the two new pure summary functions against the issue's own example data
  produced the exact quoted copy shapes ("3 for you, 2 for the pets" /
  "2 for you, Pepper -- thyroid half-pill · later"), grep confirmed no dangling references to the
  removed `.med-item-row`/`.med-item-icon`/`PILL_ICON_PATH`. Not live-verified against a running
  server -- this worktree has no `web/shanes-life/.env` (only `.env.example`; the real DB is
  ShanesSurvival's own Postgres, a separate credential from this repo's `DATABASE_URL`), so no
  dev server/live DOM check was reachable from here; this is a pure client-side rendering change
  with no backend touched, and the honest verification above is what was actually possible in
  this environment. Committed `24059727e` (implementation) on top of the IN FLIGHT bookend commit
  `900ff7fbe`, rebased onto and pushed straight to `origin/main` per this repo's own convention
  (real hash after rebase: `967bffbe2`); `verify-branch-merged.mjs` confirms merged.
  `git status --porcelain` clean. Commented on #3282 and synced `in-flight`→`complete` labels.
