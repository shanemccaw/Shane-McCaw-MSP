# #3112 — Feature: Shopping — Per-store price history (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T20:50:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-07
- **Commit(s):** 41f2da6e7 (feature), 851693cd4 (merge with concurrent #3109), both pushed to `main`

## Log
- 2026-09-07T20:40:00Z ⏳ IN FLIGHT — blocked_by #3088 confirmed cleared (labeled `complete`).
  Read contract.md + README.md (Screens #3/#12, capture grammar, "Prices are stored per store and
  per date, and Claude reads them over MCP (get_prices)"). Read #3088's real `lists`/`list_items`
  shape (Git #3116 typed-tables decision) since price history hangs off item text, not a
  barcode-linked row (barcode itself is #3109, separate, not built here). Building: `stores` +
  `item_prices` tables (migration 020), core/prices.mjs, GET/POST /api/stores, GET/POST
  /api/prices, a "last price" hint + inline log-a-price form on Shopping list rows, MCP
  `get_prices`, bin/check.mjs coverage, README update.
- 2026-09-07T20:50:00Z ✅ DONE — `stores` + `item_prices` (real per-store, dated price history),
  `src/core/prices.mjs` CRUD, `GET/POST /api/stores`, `GET/POST /api/prices`, `GET /api/shopping`
  now attaches each item's real `lastPrice`, MCP `get_prices`, and a "Log price"/"History"
  affordance on every Shopping row in `public/app.js`. Migration renumbered 020 -> 021 in-session
  after discovering concurrent sibling builds (#3109 barcode scan, #3111 budget, aisle memory)
  had already claimed 020_barcode_scan.sql / 020_shopping_budget.sql / 020_store_aisle_memory.sql
  against the same shared `finances` database -- renamed the file and the already-applied
  schema_migrations ledger row to keep the leading-number space collision-free (#3118's own
  discipline). Merged against #3109 (already on `main` by the time this session finished) --
  real conflicts in `public/app.js`/`src/routes/api.mjs`, resolved by keeping both features'
  routes/UI side by side (a scanned item shows both its own scanned price and the item's
  cross-store `lastPrice` hint). Verified: `npm run check` 86/86 PASS against a real local
  server + the real shared `finances` database (79 before the #3109 merge, +7 real barcode-scan
  checks after), including the 9 new price-history checks (store find-or-create, case-insensitive
  reuse, a different store getting its own row, `GET /api/prices?item=` returning all 3 most-
  recent-first, the Shopping list's `lastPrice` hint, `get_prices` over MCP, a real `item_prices`
  row count). Migration applied by hand against the real local `DATABASE_URL` (`finances`).
  Filed #3122 (parented to #3086) for a real gap found along the way: #3109's scan-saved price
  writes only `list_items.price_cents` / `barcode_links.last_price_cents`, never reaching
  #3112's real per-store history -- a real product question (what store was the scan at?), not a
  code-only fix, so left as a filed finding rather than rewritten in this session. Pushed
  directly to `main` (fast-forward); `verify-branch-merged.mjs` confirms OK. `git status
  --porcelain` clean.
