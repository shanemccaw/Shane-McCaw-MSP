# #2059 — Floating Chat Window Phase 1: single-chat AIM-style always-on-top window (WebView2 DOM bridge reuse)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** f8c097ca1 (IN FLIGHT bookend), c4f343ac6 (feature) — both verified ancestors of origin/main

## Log
- 2026-08-31 ⏳ IN FLIGHT — Starting Phase 1 of #2035 (Global Chat Drawer epic). Scope is deliberately
  minimal: a real WPF Window owned/launched by BuildConsole, floating + always-on-top (Topmost), opening
  ONE chat at a time, sending/receiving through the EXISTING WebView2 → DOM injection chat bridge already
  in the app (auditing it first — reuse, do not reinvent), output rendered as RichText. Explicitly OUT of
  scope for this phase and NOT to be built now: tabs, side dock, progress/checklist extraction, screenshot
  paste, OCR, self-purging gallery (all deferred to later phases on #2035). Auditing the existing WebView2
  bridge / chat document view / window-ownership / logging patterns before writing any code.
- 2026-08-31 ✅ DONE — Shipped Phase 1: a real always-on-top WPF `FloatingChatWindow` owned/launched by
  BuildConsole, opening ONE chat at a time. Audit first confirmed the existing bridge is embedded in
  `MainWindow` (no standalone bridge class): SEND = `StickyNotesComposerInsertScript` /
  `SendTextToActiveClaudeChatAsync` (largest-visible `div[contenteditable]` + `execCommand('insertText')`
  + `InputEvent` fallback, #937/#940); the only DOM read-out today is `ChatContextMeterScript`'s char-count
  scrape (no full-response scraper existed). Reused both: new `Services/FloatingChatBridgeScript.cs` sends
  via the identical insert technique then SUBMITS (clicks the real Send button, Enter fallback — the floaty's
  whole point is to fire without switching tabs), and RECEIVES by locating the last assistant turn with the
  context meter's own selectors (`.font-claude-message`, `[data-testid="assistant-message"]`,
  `[role="article"][aria-posinset]`) and walking it into markdown, rendered as RichText via the existing
  `MarkdownRenderer` (the epic's "webpage equivalent of stdout … rendered as RichText"). The window hosts a
  real `ChatSafeWebView2` on the shared WebView2 environment (`MainWindow.EnsureWebViewInitializedAsync` →
  shared login), so it is the same bridge, not a new mechanism. Topmost is enforced with the
  `DeviceCodeWindow` `SetWindowPos(HWND_TOPMOST)`-on-Deactivated pattern; window shape/persistence mirror the
  `LinkedInComposerWindow` floaty. Launched from the Chats-panel card context menu ("🪟 Open as Floating
  Window") WITHOUT opening a tab. Logging via `ActivityLog.Log("chat.floating", …)` (the C# desktop app uses
  the free-form-string `ActivityLog` channel convention, not the Node logger taxonomy). Explicitly OUT of
  scope and NOT built: tabs, side dock, progress/checklist extraction, screenshot paste, OCR, self-purging
  gallery (all deferred on #2035). Verification: `dotnet build BuildConsole.csproj` clean — 0 warnings,
  0 errors. Test manifests N/A (`desktop/BuildConsole` is explicitly out of manifest scope per CLAUDE.md).
  Files: `FloatingChatWindow.xaml`/`.xaml.cs`, `MainWindow.FloatingChat.cs`, `Services/FloatingChatBridgeScript.cs`,
  plus additive hunks in `Controls/LeftSidebar.xaml.cs` (context-menu item) and `Services/BuildConsoleSettings.cs`
  (position/size persistence). Honest Phase-1 limitation (NOT a separate finding — it's inherent to the
  epic's later "engine not shown" vision, already carried by #2035): WebView2 throttles a hidden control, so
  the live "bridge" strip is kept VISIBLE by default rather than a fully-hidden background engine; capture
  pauses if the user collapses it (tooltip states this). No new findings filed — no broken tests, unused
  endpoints, or stale docs were hit; the injection-technique duplication the audit noted
  (`StickyNotesComposerInsertScript` vs `EpicChatPrefillPollScript`) is a pre-existing refactor opportunity,
  not a defect.
