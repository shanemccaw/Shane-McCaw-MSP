# #2708 — Top bar: replace Points/Achievement chips with "Open Last Tabs" when unrestored tabs exist

- **Status:** ✅ DONE 2026-09-03T19:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 6739d07f (Top bar: swap Points/Achievement chips for Open Last Tabs when unrestored), aa6db6af1 (bookend IN FLIGHT)

## Log
- 2026-09-03T18:56:26Z ⏳ IN FLIGHT — read issue #2708 body + comment (BUILD dispatch, Posted 2026-09-03T18:30:19Z). Confirmed #2707 (real dependency) is closed/complete and merged to `main` at `28f530639` (verified via `git log`); pulled its real landed diff for the exact reopen-loop shape: `MainWindow.Home_ReopenAllRequested(object? sender, IReadOnlyList<PersistedChatTab> tabs)` loops `Home_ResumeChatRequested(sender, p)` per tab (same `OpenChatTab` call, same honest skip/toast for no-`ClaudeUrl` entries).
  Real audit confirmed: `Controls/FocusModeBar.xaml(.cs)` — `PointsChip`/`PointsText` and `AchvChip`/`AchvText` live in `MiddlePanel`, only rendered inside `Refresh()`'s `if (active)` branch (Focus Mode must be active for either chip to show — that's the real existing gating, so the new chip inherits the same gate). `MainWindow._chatTabsAtLaunch` (set at `MainWindow.xaml.cs:999`, before `InitFocusMode()` at line 1084) is the real remembered-tab list, same data #2707's Reopen All consumes.
  Plan: (1) add `OpenLastTabsChip`/`OpenLastTabsText` to `FocusModeBar.xaml`'s `MiddlePanel`, same chip convention as Points/Achv; (2) `FocusModeBar` gets `OpenLastTabsRequested` event + public `SetUnrestoredTabCount(int)`, swaps it in for Points/Achv (not stacked) inside `Refresh()`; (3) `MainWindow.FocusMode.cs` wires the event to a new handler that calls the SAME `Home_ReopenAllRequested(this, _chatTabsAtLaunch)` #2707 built, then clears the count; (4) give-up point: hook the top of `OpenChatTab` (the single funnel every chat-tab open goes through) to clear the unrestored count the moment ANY chat tab is opened — covers "ignores the button and starts working normally" per the issue's point 4, and is idempotent/no-op once already cleared.
- 2026-09-03T19:05:00Z ✅ DONE — Shipped at `6739d07f` (`desktop/BuildConsole/Controls/FocusModeBar.xaml(.cs)`, `MainWindow.FocusMode.cs`, `MainWindow.xaml.cs`):
  1. `FocusModeBar.xaml`: new `OpenLastTabsChip`/`OpenLastTabsText` in `MiddlePanel`, same chip visual convention as `PointsChip`/`AchvChip` (blue-accented, glow effect to draw the eye since it's an action, not a display).
  2. `FocusModeBar.xaml.cs`: new `OpenLastTabsRequested` event + public `SetUnrestoredTabCount(int)` (no-op on unchanged value). Inside `Refresh()` (still gated by Focus Mode `active`, the SAME existing gate Points/Achv already live under — those chips only ever render there, so the new chip inherits that real behavior rather than inventing a new always-visible surface): `showOpenLastTabs = _unrestoredTabCount > 0` — when true, the chip shows and PointsChip/AchvChip are forced `Collapsed` regardless of their own real conditions (a genuine swap, not z-order/stacking).
  3. `MainWindow.FocusMode.cs` (`InitFocusMode`): wires `OpenLastTabsRequested` to `OnFocusOpenLastTabsRequested`, which calls the exact same real reopen logic #2707 landed — `Home_ReopenAllRequested(this, _chatTabsAtLaunch)` (loops `Home_ResumeChatRequested` → `OpenChatTab`, same honest skip/toast for any entry with no `ClaudeUrl`) — no second reopen implementation written. Seeds the bar's count right after wiring, from `_chatTabsAtLaunch.Count(t => !string.IsNullOrWhiteSpace(t.ClaudeUrl))` (only entries that will actually reopen count, so the displayed N matches reality).
  4. Give-up point (issue's point 4, real judgement call): hooked the very top of `MainWindow.OpenChatTab` — the single funnel every chat-tab open goes through (manual click, a remembered-row click, the #1887 background reopen preload, or Open Last Tabs itself) — to call `_focusBar?.SetUnrestoredTabCount(0)` unconditionally. The moment ANY chat tab opens for any reason, the button's job is considered done and Points/Achievement come back; this also self-satisfies the click path itself (the reopen loop's own `OpenChatTab` calls already clear it), the explicit clear in `OnFocusOpenLastTabsRequested` only covers the edge case where every remembered entry lacked a `ClaudeUrl` and none of them called `OpenChatTab`.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. No live WebView2 click-through (no real prior-session `OpenChatTabs` state with unrestored entries to exercise in this environment) — the code reuses #2707's already-verified reopen path directly, so a clean build plus tracing the real call chain was the meaningful verification available here.
  - Merge: rebased onto a concurrently-landed commit (`origin/main` moved under `#2716` mid-session) and pushed `agent/2708-q1452` directly onto `origin/main` (fast-forward to `6739d07f`); `verify-branch-merged.mjs` confirms merged.
  - `git status --porcelain` clean after final commit.
