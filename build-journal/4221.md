# #4221 — CritterLoungeControl.xaml: 11 raw hex colors have no matching brush

- **Status:** ✅ DONE 2026-09-15T05:06:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** b7bcfa682 (real change), 249440181 (IN FLIGHT bookend)

## Log
- 2026-09-15T04:57:00Z ⏳ IN FLIGHT — pulled #4221's real evidence (11 raw hex literals: 5 velvet gradient stops, 2 gold gradient stops, 2×`#CA8A04` solid borders, 2×`#20000000` alpha-black shadow fills, all in `desktop/BuildConsole/Controls/CritterLoungeControl.xaml`). Chose option (a) from the issue: promote real named brushes into `Themes/DarkTheme.xaml` rather than substitute an inexact existing hue.
- 2026-09-15T05:06:00Z ✅ DONE — added `CurtainVelvetBrush`/`GoldFringeBrush` (LinearGradientBrush, values verbatim), `GoldTrimBrush` (solid `#CA8A04`), `CurtainPleatShadowBrush` (solid `#20000000`) to `Themes/DarkTheme.xaml`; removed the local `UserControl.Resources` block from `CritterLoungeControl.xaml` and wired all 11 sites to `StaticResource`. `grep -n '#[0-9A-Fa-f]\{6,8\}' CritterLoungeControl.xaml` now returns nothing. Verified via a temporary, uncommitted local fix of the unrelated pre-existing broken `GitModeContainer.xaml` (duplicate closing tags — filed as #4233, parented under #1202) so `dotnet build BuildConsole.csproj -c Debug` could actually run: **Build succeeded, 0 Error(s)**. Reverted that temp fix (`git checkout --`) before committing — confirmed `git status --porcelain` showed only my two intended files. Real commit `b7bcfa682` pushed to `origin/main`; `verify-branch-merged` confirmed ancestor. Working tree clean at session end.
