# #4028 — msp-poams.ts soft-delete filter gap

- **Status:** ✅ DONE 2026-09-14T13:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 923bfb773

## Log
- 2026-09-14T12:59:21Z ⏳ IN FLIGHT — add `isNull(mspPoamsTable.deletedAt)` to `loadOwnScoped` (shared by GET one, PATCH, cancel, milestones, DELETE) in `artifacts/api-server/src/routes/msp-poams.ts`; list route already had it. Verifying against a live soft-deleted row next.
- 2026-09-14T13:10:00Z ✅ DONE — On inspection, `GET /api/msp/poams` (list) already had `isNull(mspPoamsTable.deletedAt)` (matching `portal-poams.ts:245`); the real gap was `loadOwnScoped`, the shared lookup `GET /msp/poams/:poamId` (and PATCH/cancel/milestone/DELETE) use — added the same `isNull` filter there. Live-verified against `DATABASE_URL`: inserted `msp_poams` row id 150 (`poam_id='ZZ-TEST-4028'`, msp_id=1), set `deleted_at`, then re-ran the exact WHERE clauses both routes now execute — both returned 0 rows for the soft-deleted poam; before the fix `loadOwnScoped`'s clause had no `deleted_at` filter and would have returned it. Test row deleted after verification. `tsc --noEmit` clean on msp-poams.ts. `verify-branch-merged.mjs` confirms 94c07e68 is an ancestor of origin/main; `git status --porcelain` clean.
