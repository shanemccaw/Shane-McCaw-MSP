# #4200 — Colors.xaml/DarkTheme.xaml has no StatusError/StatusWarning wash brush

- **Status:** ⏳ IN FLIGHT 2026-09-15T04:07:45Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-15T04:07:45Z ⏳ IN FLIGHT — Added `StatusErrorWashLightBrush`/`StatusErrorWashMediumBrush` and
  `StatusWarningWashLightBrush`/`StatusWarningWashMediumBrush` to `Themes/Colors.xaml`, exact 10%/20%
  opacity wash of `StatusErrorColor` (#DC8A8A) / `StatusWarningColor` (#D4A56C), matching the
  `StatusSuccessWashLightBrush`/`StatusSuccessWashMediumBrush` pattern exactly. Wired the 5 raw hex
  literals in `WpfTesterWindow.xaml` (lines 273, 321, 324, 343, 346) to the new brushes: the two Event
  Log row backgrounds (Error/Warning) and the PnlError render-error box background use the Light tier;
  the two Event Log level-badge backgrounds use the Medium tier, matching how the Success pair is
  already applied at the same sites. Next: repo-wide grep for the same unmapped pattern, then build
  verify, then DONE.
  Note for future readers: #4201 is real-blocked on this issue and will reuse
  `StatusErrorWashLightBrush`/`StatusErrorWashMediumBrush` /
  `StatusWarningWashLightBrush`/`StatusWarningWashMediumBrush` by these exact names — do not rename.
