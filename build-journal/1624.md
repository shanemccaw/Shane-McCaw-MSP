# #1624 — ShaneBot: persona is a draft and cardsBlock has no card-preference ordering

- **Status:** ✅ DONE 2026-09-03T02:20:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** e24b89aba (code), 1741745fd→rebased onto e24b89aba

## Log
- 2026-09-03T01:50:00Z ⏳ IN FLIGHT — read #1624 body/comments + `docs/shanebot-contract-pack.md`
  §4.4/§7/§9. Scope 2 (rewrite `cardsBlock` to encode specific-card-first ordering, `data-answer`
  as fallback of last resort) is agent work. Scope 1 (rewrite `SHANEBOT_PERSONA_PORTAL` to final
  voice) is explicitly Shane-authored per the issue's own body — not something this session can
  invent. Proceeding with scope 2 + keeping the one-card-per-reply constraint intact.
- 2026-09-03T02:20:00Z ✅ DONE — Rewrote `cardsBlock` in
  `artifacts/api-server/src/routes/support-chat.ts` (§4.4/§9 DECIDED): the three specific cards
  (`invoice`, `subscription`, `score`) are now listed first with an explicit "prefer the specific
  card" instruction, and `data-answer` is named the fallback of last resort — only offered when
  none of the three specific cards fit. One-card-per-reply and the "no real data → no card, never
  claim one is showing" rules are unchanged/preserved verbatim. `resolveCardData`'s null-coalesce
  behavior (`support-chat.ts:189-206`) was not touched — still no fixture-as-fallback.
  Scope item 1 (persona rewrite to final voice) is NOT done — the issue's own body says that's
  Shane-authored input, not an agent decision; `shanebot-persona.ts` remains the DRAFT header it
  already carries. This is a real product-decision stop, not a skipped task.
  Verification: `tsc --noEmit` on api-server shows zero errors touching `support-chat.ts` (the
  errors present are pre-existing, unrelated `lib/db`/test-tooling module-resolution gaps in this
  worktree — matches memory `api-server-tsc-needs-libdb-decls`). Reviewed
  `support-chat.test.ts`'s existing `[SHOW_CARD:x]` tests (`:425-490`) — they mock the AI reply
  and assert routing/stripping behavior, not the literal `cardsBlock` prompt text, so they remain
  valid unchanged. Could not run the suite live: `vitest` resolves through a symlink chain
  (worktree → main-repo relative symlink → main repo's `.pnpm` store) whose final hop is
  genuinely missing on disk in this environment (`node_modules/.pnpm/vitest@4.1.9.../node_modules/vitest`
  does not exist) — the same worktree dependency-isolation class already tracked at #1988; not a
  new finding, not filed again. Per #1624's own body, a card can't be forced/seeded anyway — real
  verification is Shane asking real questions against the drawn surface, same as the issue states.
  No test-manifest update: this is a prompt-text change to an existing route with no new
  testable behavior (per Test Coverage rules, "opt-in when the feature has meaningful new API/UI
  surface" — none added here), and `test-manifests/chat/support-chat-active-cards.json` already
  documents card-presence as non-deterministic/non-assertable.
  No fixture data introduced. Branch verified ancestor of `main` via
  `git merge-base --is-ancestor HEAD origin/main` (exit 0). `git status --porcelain` clean.
