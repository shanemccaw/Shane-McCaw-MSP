# Build Journal — #3559

- **Status:** ✅ DONE 2026-09-11T00:05:00Z
- **Scope:** platform (docs — CLAUDE.md only)

## Log
- 2026-09-10T23:56:07Z — Started. Adding "GitHub access from an interactive chat session —
  use the shanes-git MCP, not a raw PAT" section to CLAUDE.md, placed right before "Mandatory:
  file every finding as its own GitHub issue." Leaving the existing "### How" gh-CLI subsection
  untouched per issue instructions.
- 2026-09-11T00:05:00Z — ✅ DONE — Added the new "## GitHub access from an interactive chat
  session — use the shanes-git MCP, not a raw PAT (Git #3556)" section to CLAUDE.md, verbatim
  substance from the issue body, immediately before "## Mandatory: file every finding as its
  own GitHub issue." The existing "### How" gh-CLI subsection under that section was left
  completely untouched. Cross-referenced `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md`
  and confirmed it exists and documents `shanes-git` at `https://shanes-git.replit.app`
  (matches, real file). Commit `ed6f98165749c12c074c56da4acfc76bc62cf920`, pushed to
  `origin/main` and verified merged via `verify-branch-merged.mjs`. `git status --porcelain`
  clean. No findings filed — no bugs discovered mid-build.
