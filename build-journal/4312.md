# #4312 — Launch Control execute never records catalogItemId on the CR row

- **Status:** ✅ DONE 2026-09-16T01:25:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** c440340b44cdceb503fb9de1a0f1210a9a897631

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — pass catalogActionId through into raiseChangeRequestForLaunchControlExecution's call site so msp_change_requests.catalogItemId gets populated for Launch Control writes.
- 2026-09-16T01:25:00Z ✅ DONE — `catalogRow` (msp-launch-control.ts execute route) now carries the real `write_action_catalog.id`; `raiseChangeRequestForLaunchControlExecution` writes it into `msp_change_requests.catalogItemId` on insert. Real gap found mid-build: that column had a live FK to `change_catalog_items(id)`, a DIFFERENT table than `write_action_catalog` — inserting the catalog-action id would have thrown a FK violation, not silently succeeded. Dropped the FK (non-destructive per the migration gate — `DROP CONSTRAINT` — ran `lib/db/migrations/manual/2026-09-16-drop-catalog-item-id-fk-4312.sql` myself against local `DATABASE_URL`) and documented the column's now dual-source (change-catalog vs write-action-catalog) convention directly on the Drizzle schema column. All 4 tests in `launch-control-change-request.test.ts` pass against the real local DB, including a new assertion that `catalogItemId` round-trips through the insert (`expect(row!.catalogItemId).toBe(95)`). `GET /api/msp/change-requests` already does a bare `select()`, so the field needed no route change to surface. Verified branch is an ancestor of `origin/main` via `verify-branch-merged.mjs`; working tree clean.
