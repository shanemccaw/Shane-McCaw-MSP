# Git #3757 — Focus dropdown shows closed milestones

- **Status:** ✅ DONE 2026-09-12T02:55:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T02:40:00Z — Confirmed root cause: `FocusMilestone` had no closed/state field; `PopulatePicker` couldn't filter on it. `GitHubMilestoneInfo` already carries `IsClosed` from GitHub's `state=all` fetch, just never propagated. Added `FocusMilestone.IsClosed`, populated it in both `FocusModeService` construction branches (real-counts and fallback), and excluded `IsClosed` milestones in `FocusModeBar.PopulatePicker`.
- 2026-09-12T02:55:00Z — ✅ DONE — Commit `9f61d370` (Focus dropdown excludes closed milestones, Git #3757) pushed directly to `origin/main`. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` succeeded, 0 errors (10 pre-existing warnings, none introduced by this change). `verify-branch-merged.mjs` confirmed `agent/3757-q2394` is an ancestor of `origin/main`. `git status --porcelain` clean.
