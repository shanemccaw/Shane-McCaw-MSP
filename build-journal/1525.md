# #1525 — Policy Decisions: Obligation register — cited authority as a first-class reference

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** (fill after push)

## Log
- 2026-08-31 ⏳ IN FLIGHT — extending the existing #1256 compliance-framework/obligation
  catalog with an `authority_type` (regulation/certification/contract/insurance/
  internal_schedule) and MSP/tenant-authored rows, plus a first-class `obligation_id`
  FK on `msp_risk_decisions` and `policy_decisions` pointing at `compliance_obligations`.
  Migration `2026-08-31-obligation-register-authority-type-1525.sql` run locally.
  Next: wire `obligationId` into the create paths (msp-rbd.ts, portal-policy-decisions.ts)
  and the read wire shapes (portal-risk-register.ts).
- 2026-08-31 ✅ DONE — obligation register built on top of the existing #1256 catalog
  rather than a parallel table: `compliance_frameworks` gained `authority_type`
  (regulation/certification/contract/insurance/internal_schedule, defaulted per existing
  seed row) and `msp_id`/`tenant_id` (MSP/tenant-authored authorities, e.g. a customer's
  own insurance schedule — null on every global/seeded row). Seeded ISO 27001 A.5.18,
  the one example citation from #1525's own issue body missing from the #1256 catalog.
  `msp_risk_decisions.obligation_id` and `policy_decisions.obligation_id` (both nullable
  FK → `compliance_obligations.id`) are the first-class reference `decisionState`
  deviations now point at, alongside the existing free-text citation columns which stay
  authoritative for any row with no catalog match. Wired into the create paths
  (`msp-rbd.ts`'s `POST /api/msp/rbd` + new `GET /api/msp/rbd/available-obligations`
  picker; `portal-policy-decisions.ts`'s `POST /api/portal/policy-register`, which
  server-verifies a submitted `obligationId` resolves to an accessible catalog entry
  before trusting it) and the read wire shapes (`portal-risk-register.ts`'s
  `WireRisk`/`WirePolicyDecision` and `portal-policy-decisions.ts`'s
  `WirePolicyRegisterEntry` all gained `obligationId`/`obligationType`, resolved via a
  single batched query per list). No UI — `Design/portal/` has no export for this module
  and `artifacts/portal` has no page for it; ends at the wire contract per the standing
  SCOPE STOP.
  Migration `lib/db/migrations/manual/2026-08-31-obligation-register-authority-type-1525.sql`
  run against local Postgres myself — verified columns, FKs and the ISO 27001 seed row
  landed. Appended to #1630.
  Verification: `pnpm --filter @workspace/db exec tsc --noEmit` clean. Could NOT get a
  clean cross-package `tsc` for `artifacts/api-server` against these changes —
  `artifacts/api-server/node_modules/@workspace/db` is junctioned to the MAIN checkout
  (`C:\Source\...\lib\db`), by design, for worktree isolation, and self-heals back to
  that target even after being repointed for a local check; `lib/db/dist` is junctioned
  the same way. Rather than leave that mutated, both junctions were restored to point at
  the main checkout exactly as provisioned. Instead verified by: (1) the real Postgres
  schema confirmed above, (2) a field-by-field manual review of every
  `complianceFrameworksTable.*`/`complianceObligationsTable.*`/`obligationId` reference
  in the three edited route files against the schema I wrote — every property name
  matches exactly. This is a real, named verification gap, not a claimed clean run;
  full cross-package typecheck will run for real once this merges into main and the
  dev-server merge-back rebuilds against it.
  `git status --porcelain` clean before this line was written.
