# #2857 — msp-financial-aggregator.ts: invoice-derived revenue off by 100x (cents/dollars double-division)

- **Status:** ✅ DONE 2026-09-04T16:14:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** aa2bb3768f39622a0578b1d12cf096eaef23aeef (pre-existing fix, confirmed already on `main`/this worktree); no new code changes required — see Log for verification detail.

## Log
- 2026-09-04T16:03:32Z ⏳ IN FLIGHT — investigating the invoice-derived revenue double-division bug reported in #2857 (`src/lib/msp-financial-aggregator.ts:181-193`, `msp-financial-aggregator.test.ts`).
- 2026-09-04T16:14:00Z ✅ DONE — **already fixed, no code change needed.** Traced the exact bug described in #2857 to commit `aa2bb3768` ("Migrate invoicesTable.amount to integer cents", 2026-08-29), which changed `msp-financial-aggregator.ts:183` from `Math.round(Number(inv.amount) * 100)` (the double-division-causing bug — treating an already-cents value as dollars and re-scaling it) to `Number(inv.amount)` (direct, correct — `invoicesTable.amount` is integer cents per Git #1610). That commit is already an ancestor of this build's `origin/main`/HEAD and was present before this build's worktree was even provisioned. Verified honestly, not assumed:
  - Ran `node_modules/.bin/vitest run src/lib/__tests__/msp-financial-aggregator.test.ts --no-cache` — all 3 tests pass, including the exact two assertions #2857 quoted as failing (`projectRevenue` 150.00/105.00/45.00, `categoryBreakdown.consulting` 100.00/70.00) — both now correct, not the 1/100-scaled values the issue reported.
  - Confirmed local Postgres `invoices.amount` column is real `integer` (`\d invoices`), matching the Drizzle schema — the underlying migration genuinely ran, this isn't just a mock artifact.
  - `git diff origin/main -- artifacts/api-server/src/lib/msp-financial-aggregator.ts` is empty; `git status --porcelain` clean — this worktree's file is byte-identical to what's already live on `main`.
  - Swept every other invoice-`amount` consumer for a lingering /or lingering *100 mismatch (admin-finance.ts, admin-overview.ts, admin-invoices.ts, admin-fulfillment.ts, analytics-forecast.ts, invoice-sharepoint.ts, portal-billing.ts, shanebot-engine.ts, mcp-server get-invoices.ts) — all correctly treat the DB value as cents and convert once at their own display/wire boundary. No residual double-division found anywhere in scope.
  - Ran the adjacent test files (`msp-financial-aggregator-overlord.test.ts`, `admin-overlord.test.ts`, `msp-portal.test.ts`) — all pass except one pre-existing, unrelated flake (`GET /api/msp/dashboard > returns 401 when no auth token provided` times out only when run concurrently with other suites, passes cleanly in isolation — a resource-contention timeout, not a money-path bug, not filed as a finding since it isn't reproducible standalone).
  - Root cause of the discrepancy between the issue's repro and this build's clean run: the #2794 session that filed #2857 was almost certainly working from a worktree provisioned before `aa2bb3768` landed on `main` (2026-08-29), 6 days ahead of the issue's filing timestamp (2026-09-04T08:26Z) — a stale-worktree artifact, not a live regression. Branch-merged check (`node scripts/dev-server/verify-branch-merged.mjs`) confirms this build's HEAD is an ancestor of `main`.
  - No code changes were necessary or made in this session. `git status --porcelain` is clean.
