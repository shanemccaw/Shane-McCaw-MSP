# #2487 — ShaneBuilder.exe survives after MainWindow closes (TestPadPillWindow keep-alive)

- **Status:** ✅ DONE 2026-09-03T00:15:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 19362a8cd (fix, on agent/2487-q1322), acbfa8622 (merge to main)

## Log
- 2026-09-02T23:56:00Z ⏳ IN FLIGHT — Investigated root cause: `App.xaml` has no `ShutdownMode`
  (default `OnLastWindowClose`); `MainWindow` creates `_testPadPillWindow` and `_testPadWindow`
  as independent top-level windows at startup (`MainWindow.xaml.cs:62-64`) but its existing
  `OnClosed` override (added for #2210/#2216 teardown) only closed `_alertStackWindow`,
  `_critterOverlayWindow`, and `_alertLabWindow` — never the Test Pad windows. Fix: added
  `_testPadPillWindow?.Close(); _testPadWindow?.Close();` to that same `OnClosed` block,
  matching the existing pattern rather than introducing an app-wide `ShutdownMode` change.
  Next: build and verify via real launch + tasklist check.
- 2026-09-03T00:15:00Z ✅ DONE — `dotnet build` clean, 0 warnings/errors. Real verification:
  launched `bin\Debug\net8.0-windows\ShaneBuilder.exe` standalone (PID 31836), confirmed via
  `tasklist` both it and its pill/pad windows were up, then closed ONLY MainWindow via a
  `WM_CLOSE` PostMessage to its `MainWindowHandle` (not a taskkill/PID-based kill — a genuine
  graceful window close). Re-ran `tasklist /FI "IMAGENAME eq ShaneBuilder.exe"` — PID 31836 was
  gone, zero orphaned instances from my launch. Confirms the fix: closing MainWindow now
  guarantees full process exit.
  Note: a *pre-existing* orphaned `ShaneBuilder.exe` (PID 30380, running from the main checkout
  at `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\...`, started ~4h before this session, no
  window title) was already running when I started — not created by me, left untouched per
  "don't touch what isn't yours." This is itself live evidence of the bug the issue describes;
  Shane should `taskkill /F /IM ShaneBuilder.exe` or check Task Manager per the issue's own
  "Immediate action" note.
  Merged `origin/main` into `agent/2487-q1322` (auto-merge, my `OnClosed` hunk untouched — only
  unrelated `build-journal/2314.md`, `build-journal/2477.md`, `GitMapService.cs` came in from
  main), rebuilt clean post-merge, pushed to `main` as `acbfa8622`.
  `verify-branch-merged.mjs` → OK, merged into origin/main. `git status --porcelain` → clean.
