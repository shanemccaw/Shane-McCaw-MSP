# #2487 — ShaneBuilder.exe survives after MainWindow closes (TestPadPillWindow keep-alive)

- **Status:** ⏳ IN FLIGHT 2026-09-02T23:56:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T23:56:00Z ⏳ IN FLIGHT — Investigated root cause: `App.xaml` has no `ShutdownMode`
  (default `OnLastWindowClose`); `MainWindow` creates `_testPadPillWindow` and `_testPadWindow`
  as independent top-level windows at startup (`MainWindow.xaml.cs:62-64`) but its existing
  `OnClosed` override (added for #2210/#2216 teardown) only closed `_alertStackWindow`,
  `_critterOverlayWindow`, and `_alertLabWindow` — never the Test Pad windows. Fix: added
  `_testPadPillWindow?.Close(); _testPadWindow?.Close();` to that same `OnClosed` block,
  matching the existing pattern rather than introducing an app-wide `ShutdownMode` change.
  Next: build and verify via real launch + tasklist check.
