# #3506 — MyArchitect: Log viewer (Loaded/Live modes) — UI_RULES.md §1

- **Status:** ⏳ IN FLIGHT 2026-09-12T08:15:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T08:15:00Z ⏳ IN FLIGHT — Investigated existing `IConsoleHistoryService`/`ConsoleHistoryService`
  (Loaded half), `IPowerShellConsoleService.HostOutputReceived` (local console stdout), and the real
  `GET /api/admin/live-stream` SSE endpoint (`artifacts/api-server/src/routes/admin-live-stream.ts`,
  same feed `artifacts/admin-panel/src/hooks/useLiveStream.ts` consumes) as the Live half's backend.
  Building a title-bar `LogViewerOverlay` (modeled on the existing `CommandPaletteOverlay`) with a
  new `ILogStreamService`/`LogStreamService` SSE client, always scoped to the operator's own mspId
  (never the route's `channel=*` cross-tenant firehose tier).
