# #3506 — MyArchitect: Log viewer (Loaded/Live modes) — UI_RULES.md §1

- **Status:** ✅ DONE 2026-09-12T22:55:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 5c930688c (main)

## Log
- 2026-09-12T08:15:00Z ⏳ IN FLIGHT — Investigated existing `IConsoleHistoryService`/`ConsoleHistoryService`
  (Loaded half), `IPowerShellConsoleService.HostOutputReceived` (local console stdout), and the real
  `GET /api/admin/live-stream` SSE endpoint (`artifacts/api-server/src/routes/admin-live-stream.ts`,
  same feed `artifacts/admin-panel/src/hooks/useLiveStream.ts` consumes) as the Live half's backend.
  Building a title-bar `LogViewerOverlay` (modeled on the existing `CommandPaletteOverlay`) with a
  new `ILogStreamService`/`LogStreamService` SSE client, always scoped to the operator's own mspId
  (never the route's `channel=*` cross-tenant firehose tier).
- 2026-09-12T22:55:00Z ✅ DONE — Shipped `LogViewerOverlay` (Loaded: static render of
  `IConsoleHistoryService`; Live: explicit-only Play button subscribing to `ILogStreamService`'s
  real SSE feed + `IPowerShellConsoleService.HostOutputReceived`, no auto-connect) and the new
  title-bar button, wired in `MainWindow.xaml`/`.cs`. Commit `5c930688c`, pushed to `origin/main`
  after resolving one real rebase conflict against a concurrently-landed Undo feature (kept both
  changes). `dotnet build` — 0 warnings/errors. Smoke-launched `MyArchitect.exe` (confirmed no
  live instance running first): stayed up 6s with no crash, cleanly stopped. No test manifest —
  `desktop/MyArchitect` is a WPF desktop app, same test-manifest-scope exemption as
  `desktop/BuildConsole`. Filed finding #3868 (`bug`+`security`, sibling sub-issue of #3493): the
  live-stream route's `channel="*"` tier ignores `mspId` even when passed, so there's no
  MSP-scoped firehose tier — `LogStreamService` was built to never use that tier for exactly this
  reason. #3868's board status couldn't be set to "AI Batter Up" — GraphQL genuinely
  rate-limited/circuit-open (3 real attempts, all `RATE_LIMIT`; REST core quota was 5000/5000),
  noted honestly on #3868 itself. `verify-branch-merged.mjs` — OK. `git status --porcelain` —
  clean. Posted completion comment on #3506; labels synced (`in-flight` was never applied at
  session start — missed step, no functional impact — `complete` added directly).
