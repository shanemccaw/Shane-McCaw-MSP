# #3417 — Remove orphaned Insights Engine code from Admin Panel

- **Status:** ✅ DONE 2026-09-10T04:07:51Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** c202272b1aa931d89cdaf8565f9f0d6796119ba3 (merged to main)

## Log
- 2026-09-10T04:01:09Z ⏳ IN FLIGHT — Investigated the 4 files named in the issue.
  Confirmed `InsightsPayloadDialog.tsx` / `InsightsOutputs.tsx` (admin-panel) are
  genuinely dead (only referenced by each other + `CommandWorkspace.tsx`'s
  unreachable `case "insights"`, no nav entry routes to it). BUT
  `admin-insights.ts` and `insight-pdf.ts` are NOT safely deletable as the issue
  instructs — the code itself documents why (see finding filed below). Proceeding
  with the safe half of the deletion; the other half needs a real rehome first.
- 2026-09-10T04:07:51Z ✅ DONE — Deleted `InsightsPayloadDialog.tsx` and
  `InsightsOutputs.tsx`, removed the dead import/case in `CommandWorkspace.tsx`,
  updated the stale comment in `DocumentGeneratorIde.tsx`. `admin-panel` `tsc
  --noEmit` passed clean (exit 0) after the change. Did NOT delete
  `admin-insights.ts`, `insight-pdf.ts`, or the generated
  `lib/api-zod/src/generated/types/insights*.ts` files — both source files carry
  real, live, non-insights consumers (admin-insights.ts's three GET routes back
  live document downloads/pickers; insight-pdf.ts's PDF helpers are used by
  msp/portal-remediation-tracker-export.ts and live-document-pdf.ts). Filed
  #3478 (sibling under #1095, board status AI Batter Up) with the real evidence
  and the concrete rehome steps needed before those two can go.
  Did NOT delete the drizzle migration files (per the issue's own instruction) —
  the underlying-table-drop decision is a separate, still-open DB decision, not
  made here.
  Merged `agent/3417-q2126` into `origin/main` (fast-forward merge,
  `verify-branch-merged.mjs` confirms ancestor). Working tree clean
  (`git status --porcelain` empty).
