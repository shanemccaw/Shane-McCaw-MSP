# #4147 — MSP Console: strip dev-facing UI chrome from all contract-pack screens (2026-09-15 design refresh)

- **Status:** ✅ DONE 2026-09-15T00:31:55Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 7b38631e4 — "MSP Console: strip dev-facing UI chrome from contract-pack screens"

## Log
- 2026-09-15T00:10:29Z ⏳ IN FLIGHT — starting systematic pass across 19 contract-pack screens: read each `.dc.html` in `Design/MSP_Console/design_handoff_msp_console/`, then strip footer/CONTRACT NOTES panels, raw HTTP status-code labels, and inline route mentions from the matching real component, leaving all guards/disabled-states/empty-states untouched.
- 2026-09-15T00:31:55Z ✅ DONE — all 19 screens reconciled (one, no change needed: AD OU Assignment was already clean). `pnpm run typecheck` and `pnpm run build` both pass clean in `artifacts/msp-console`. Two design-file misses found and worked around per the issue's explicit instruction (stripped anyway): `Authentication.dc.html` and `SOWs.dc.html` still bind their raw status-code fields in template, unlike every other refreshed screen where the field is computed but unused — not filed as a finding since it's a design-source gap, not a product/backend gap, and the issue's instruction already covers what to do about it. Working tree confirmed clean before this bookend's own commit.

## Per-screen results (filled in as each is reconciled)
- Authentication (`artifacts/msp-console/src/auth/*`) — DONE. `InlineMessage` (`AuthLayout.tsx`) dropped its `code` prop; `SignInPage.tsx`/`TwoFactorPage.tsx`/`ForgotPasswordPage.tsx` stopped constructing/passing raw `"401 · ..."`/`"423 · ..."` status-code labels, keeping the tone/message distinguishing logic (423 lockout, distinguishable no-password-set 401, generic 401) unchanged. Note: `Authentication.dc.html`'s own template (`loginErrCode`/`chCode` bindings) still renders the raw code, unlike sibling contract-pack screens where the code is computed but unused in the template — an apparent miss in the design refresh for this one file. Followed the issue's explicit, unconditional instruction to strip it from the real UI regardless.
- Account Security (`console/modules/AccountSecurity.tsx`) — DONE. Removed the `NOTES` "what this surface can and cannot do" footer panel, the `wire`/`route` inline endpoint-path spans (StatePanel's `wire` prop, each action's `a.route` line), and `lastResult.code` raw status label. Confirmed against `Account Security.dc.html`: `resCode`/`notes` are computed in `renderVals()` but never bound in the current template — matches removal.
- Status Reports (`console/modules/StatusReports.tsx`) — DONE. Removed `ResultBanner.code`/`errorBanner` raw status-code strings (`"400 · ..."`, `"201 · report created"`, etc.) and the `StatePanel`'s `wire` (route+status) span. Confirmed against `Status Reports.dc.html`: `resCode` computed in `renderVals()` but unused in template.
- POA&Ms (`pages/poams/*`) — DONE. Removed the "What these eight routes do and don't give this screen" `NOTES` footer panel, and stripped the raw `POST /api/msp/poams/.../convert-to-risk-acceptance` route mention from the Convert drawer's eyebrow (now just "CONVERT"). `POA&Ms.dc.html`'s current template has no notes panel, no route mention and no delete/convert UI at all (the real module's delete/convert/admin-gating is documented in its own file header as a deliberate build-beyond-the-mock).
- Reports (`console/modules/Reports.tsx`) — DONE. Removed `ResultBanner.code`, `ErrorPanel`'s `wire` prop (4 usages: definitions/runs/canvases/schedules/waste), and the "What these nineteen routes do and don't give this screen" notes panel on the Definitions tab.
- DLQ (`console/modules/Dlq.tsx`) — DONE. Removed `LastResult.code`, the top-level error state's `GET /api/msp/dlq · {status}` route+code line, and the "What these four routes do and don't give this screen" `NOTES` panel.
- Retention Queue (`modules/retention/*`) — DONE. Removed the raw status label from the live-view error banner (`decide.error`/`discuss.error` status), the `[code, text, tone]` shape rendering `shapeResult[0]` in Menlo font on the "shape" (illustrative) view (now `[text, tone]`), and the "What these three routes do and don't give this screen" `notes` panel (+ now-unused `useMemo` import).
- Offboarding (`pages/Offboarding.tsx`) — DONE. Removed `ResultBanner.code` (raw `"200 · cancellation_requested"` etc. strings) and the "What these routes do and don't give this screen" `NOTES` panel.
- AD OU Assignment (`pages/ad-ou-assignment/*`) — DONE, no change needed. Already clean: no notes panel, no raw status codes, no inline route mentions anywhere in the file.
- Marketplace Purchase (`console/modules/MarketplacePurchase.tsx`) — DONE. Removed `OutcomeCard`'s/`ErrorOutcomeCard`'s raw `"201 · free_activated"`/`"{status} · request failed"` code labels and the "What this screen does and doesn't give you" `NOTES` panel. Confirmed against design: `outCode` computed but unused in template.
- Partner Revenue (`console/modules/PartnerRevenue.tsx`) — DONE. Removed the "What this one route does and doesn't give this screen" `NOTES` panel (no raw status codes were present on this screen to begin with).
- Plan Self-Service (`console/modules/PlanSelfService.tsx`) — DONE. Removed `LogEntry.code`/`.path` (raw `200`/`POST /api/msp/plan/change` labels next to each log entry), `StatePanel`'s `wire` prop (2 usages), and the "STATE OF THE BACKEND" footer panel (matches the issue's own named example verbatim).
- Policy Engine (`console/modules/PolicyEngine.tsx`) — DONE. No raw status-code chrome or notes panel present, but four inline route-path mentions in visible caveat copy (`GET /api/msp/policy-decisions/:customerId`, `PATCH .../clearance/resolve`, `GET /api/msp/rbd/available-obligations` as a section label, `/api/msp/active-directory/ou/:id/assignments`) were stripped, keeping the underlying explanations.
- Audit Log (`console/modules/AuditLog.tsx`, both mounts) — DONE. Removed the "What this one route does and doesn't give this screen" notes panel (same pattern under both the per-tenant and Operations mounts, since it's one component). `BusinessActionLog.tsx` (Business Actions tab) checked separately — already clean.
- Consent and Onboarding (`console/modules/ConsentOnboarding.tsx`) — DONE. Removed the `NOTES` "What these routes do and don't give this screen" panel, `emptyState`'s `wire` param (2 call sites: `GET /api/msp/consent → []`, `GET /api/msp/onboarding/links → []`), and `result.code` (raw `"200 · read-consent invite minted"` etc.) from the invite/revoke result banner.
- Staff Roster (`console/modules/StaffRoster.tsx`) — DONE. Removed `LastResult.code`/`ResultPanel`'s Menlo code line/`errorResult`'s status string (7 call sites), the top-level roster-load error's `GET /api/msp/settings/users · {status}` line, and the invites-empty-state's `GET /api/msp/settings/invites → []` route mention.
- Offers & SOWs (`console/modules/OffersAndSows.tsx`) — DONE. Removed `ResultCard`'s raw `code` line and stripped `code` from all 18 `setResult(...)` call sites across the SOWs/Offers/Clickwrap tabs (sign, charge, expire, open-doc, accept-offer, record-clickwrap). Note: `SOWs.dc.html`'s own template still binds `resCode`/`oresCode` (same Authentication-style miss in the design refresh) — followed the issue's explicit instruction to strip regardless.
- Projects (`console/modules/Projects.tsx`) — DONE. Removed two inline route mentions: the footer note citing `GET /api/msp/customers/:customerId/kanban/buckets` and `AdvisoryPanel`'s error text citing the same route + raw status.
- Retainer Hours (`modules/retainer/*`) — DONE. Removed the "What this router does and doesn't give this screen" `notes` panel (+ now-unused `useMemo` import). `SeatPricing.tsx` and `RetainerIntervalSwitch.tsx` checked — already clean, no chrome present.
