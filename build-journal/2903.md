# #2903 — Feature: MCP debt tracking — record_debt/get_debts (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T21:45:00Z
- **Scope:** other (desktop/ShanesSurvival — personal project, milestone "Shane")
- **Started:** 2026-09-04
- **Commit(s):** 5584f1973dacd6c43f3035d008bfcdf06aadd01 (merged to main)

## Log
- 2026-09-04T21:00:00Z ⏳ IN FLIGHT — DebtRepository (upsert by creditor_name) + record_debt/get_debts MCP tools on ShanesSurvival.Mcp, following the AccountRepository/set_bill_target convention.
- 2026-09-04T21:45:00Z ✅ DONE — Added `ShanesSurvival.Core/Debts/DebtRepository.cs` (upsert-by-creditor-name, case-insensitive; partial-update semantics — an unset optional field on an update leaves that column unchanged) and `ShanesSurvival.Mcp/Tools/DebtTools.cs` with `record_debt`/`get_debts`, registered in `Program.cs`. No linkage to target_amount/GATE math added — intentionally out of scope per the issue. `dotnet build ShanesSurvival.sln` clean. Real end-to-end MCP stdio verification against the real local `finances` Postgres database: spawned the built `ShanesSurvival.Mcp.exe`, did a real JSON-RPC initialize handshake, called `record_debt` for a new creditor (confirmed created via `get_debts`, count 1), called `record_debt` again on the same creditor with different casing (confirmed update-not-duplicate via `get_debts`, still count 1, balance updated, prior notes replaced as expected, minimum_payment preserved via COALESCE since it was left unset on the update call), then deleted the test row directly via psql to leave the real table as found (empty). Rebased twice onto concurrently-landed `main` commits (#2907 Emergency Fund, then another) — one real conflict in `Program.cs` (two features adding a `using`/DI line in the same place, resolved by keeping both), rebuilt clean after each rebase. Pushed directly to `main` (`verify-branch-merged.mjs` confirms merged). Working tree clean.
