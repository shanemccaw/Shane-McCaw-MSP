# #2350 — Import: header reports chars, notes, sections, and matched count, plus a type tally

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 32e45c16c

## Log
- 2026-09-02T19:44:00Z ⏳ IN FLIGHT — Investigated existing Test Pad import code
  (`TestPadImportParser.cs`, `TestPadImportWindow.xaml(.cs)`). Sibling sub-issues
  #2344 (Section detection) and #2349 (feature auto-match) are still open, so the
  underlying Section/matched-feature concepts don't exist yet. Scoping this build to
  what #2350 actually owns: extend the candidate model with the fields those stats
  need (`IsSection`, `MatchedFeature`) and wire the header line to report
  chars/notes/sections/matched + a type tally off real candidate data — honestly 0
  for sections/matched today since nothing sets those flags yet, and correct
  automatically once #2344/#2349 land.
- 2026-09-02T19:58:00Z ✅ DONE — Rebased onto origin/main (picked up #2344's real Section
  detection, already landed mid-session). Added `TestPadImportCandidate.MatchedFeature`
  (nullable string, forward-compatible field for #2349's matcher — honestly null/unset
  today, same pattern #2344 used for `Section`). Wired `TestPadImportWindow`'s
  `PreviewHeaderText` to a real `BuildHeaderStatsText()`: chars pasted, note count,
  distinct-section count (from `Candidate.Section`), matched count (from
  `Candidate.MatchedFeature`), and a per-`NoteType` tally — all computed off the real
  parsed candidate list, no fixture/hardcoded numbers. `dotnet build ShaneBuilder.csproj`
  succeeded, 0 warnings/0 errors, both before and after the final rebase. Commit
  32e45c16c pushed straight to `origin/main` (no PR, per repo convention); rebased
  cleanly onto main twice for concurrent landings. `verify-branch-merged.mjs` confirms
  merged; `git status --porcelain` clean. No findings filed — no genuine gap discovered
  outside this issue's own scope.
