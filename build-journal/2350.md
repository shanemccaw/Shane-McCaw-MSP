# #2350 — Import: header reports chars, notes, sections, and matched count, plus a type tally

- **Status:** ⏳ IN FLIGHT 2026-09-02T19:44:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T19:44:00Z ⏳ IN FLIGHT — Investigated existing Test Pad import code
  (`TestPadImportParser.cs`, `TestPadImportWindow.xaml(.cs)`). Sibling sub-issues
  #2344 (Section detection) and #2349 (feature auto-match) are still open, so the
  underlying Section/matched-feature concepts don't exist yet. Scoping this build to
  what #2350 actually owns: extend the candidate model with the fields those stats
  need (`IsSection`, `MatchedFeature`) and wire the header line to report
  chars/notes/sections/matched + a type tally off real candidate data — honestly 0
  for sections/matched today since nothing sets those flags yet, and correct
  automatically once #2344/#2349 land.
