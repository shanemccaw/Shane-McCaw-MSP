# #2201 — Feature: Alerts and Critters (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T16:05:00Z
- **Scope:** buildconsole (ShaneBuilder — desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** b687e1225

## Log
- 2026-09-02T15:14:00Z ⏳ IN FLIGHT — readme-phase2.md Step 11 (Alerts and Critters). Contract:
  `Alert`/`AlertAction`/`Celebration` records, Channel 1 alert stack (bottom-right, max 2 visible +
  overflow), Channel 2 full-screen critter overlay (5 tiers), bell + Alert Lab QA harness. #2176
  confirmed landed and merged (real GitHub/Postgres data layer). Extracted the real logic-class spec
  (ALERT_SEED, PARTY_EVENTS, tier counts/animation timings) from `Shell Skeleton v2.dc.html`'s
  `computeAlerts`/`computeParties`/`celebrate`/`pushAlert`. Wiring real emitters against: GitDoctorService
  (worktree dirty/diverged), bt_build_queue (build failed / blocked_by cleared), bt_epics/bt_issues
  (epic/issue closed+opened), deployed_version_stamp (deploy succeeded), chat_pinned_questions (Claude
  waiting on you), GitHub blocked-label search (issue blocked).
- 2026-09-02T16:05:00Z ✅ DONE — commit `b687e1225`. Real contract (Alert/AlertAction/Celebration) in
  `Notifications/AlertModels.cs` + `AlertCenter.cs`. Channel 1: `AlertStackWindow`/`AlertCard.xaml`
  (bottom-right, max 2 visible + overflow pill). Channel 2: `CritterOverlayWindow.xaml.cs` (5 tiers,
  confetti/disco/red-wash/banner/chip/stamp, built on the existing `CritterRegistry` art pool from
  #2180). Bell + Alert Lab QA harness in `AlertLabWindow.xaml`. Real emitters in `AlertWatchers.cs`,
  wired against live data: `bt_build_queue` status transitions, `GitDoctorService.RunChecksAsync()`,
  `bt_epics`/`bt_issues` status transitions, `deployed_version_stamp` new rows, GitHub's
  `label:blocked` search, and `chat_pinned_questions` (new `ChatReadClient.GetAllOpenPinnedQuestionsAsync`).
  New read methods: `QueueReadClient.GetRecentAsync`, `GitHubReadClient.GetBlockedOpenIssuesAsync`,
  new `BoardReadClient` for the board tables. Verification: `dotnet build` clean (0 warnings/errors);
  live-launched the built exe multiple times — first launch crashed on a `Brush.Text.Body` resource-key
  typo (real key is `Brush.Text.Primary`), fixed, then confirmed stable across 15s/20s runs covering
  every watcher's first poll cycle, plus a UI-Automation-driven click on the bell button opening the
  Alert Lab without crashing. Filed #2235 (parented under #2198, milestone v1.1, board status "AI Batter
  Up") for the one honest gap: tier 4/5 (milestone-closed/release-shipped) celebrations have no real
  local data source to watch, so they're Alert-Lab-fireable only, not live-wired. Branch verified merged
  into origin/main via `verify-branch-merged.mjs`; working tree clean.
