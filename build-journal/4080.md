# #4080 — Workflows + Agents (MSP Console): reusable placeholder module, real nav slots

- **Status:** ✅ DONE 2026-09-14T15:50:29Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 93f8c6bae (post-rebase result of d1b36669a / 60960aea0)

## Log
- 2026-09-14T15:45:16Z ⏳ IN FLIGHT — investigated `artifacts/msp-console/src/console/nav.ts`, `ConsoleShell.tsx`, `ScreenSlot.tsx`; confirmed no existing generic stub module. `ScreenSlot`'s dashed fallback is the shell's own "module not yet dispatched" default, not the per-page placeholder this issue wants. Real modules (`ScopeSla`, `Sales`, `Sops`, `change-control/shared.tsx`) each define their own local `EmptyState`-shaped card using `console/tokens` + `Icon` — building a reusable `PlaceholderModule` in that same visual language, then wiring real `workflows`/`agents` MSP_PAGES nav entries under Operations to it.
- 2026-09-14T15:50:29Z ✅ DONE — added `artifacts/msp-console/src/console/modules/PlaceholderModule.tsx` (title/description/icon props, reuses the `border`/`surface`/`signal`/`text` token EmptyState card shape every real module already uses); added `workflows` (icon `workflow`) and `agents` (icon `sparkles`, both pre-existing icons in `console/icons.tsx`) to `MSP_PAGES` in `nav.ts`; mounted both in `ConsoleShell.tsx`'s `moduleFor` with honest "not yet scoped" copy, no fabricated data. Rebased onto a concurrent `main` landing (Projects/Kanban #2621) mid-push — trivial conflict in `ConsoleShell.tsx` (adjacent `if` blocks), resolved by keeping both sides, re-typechecked clean. Verified: `tsc --noEmit` clean, `pnpm --filter @workspace/msp-console run build` succeeds. No live UI click-through (no dev server running this session) — routes are the same nav pattern every other Operations page already uses (`/ops/workflows`, `/ops/agents`), driven generically off `MSP_PAGES`/`mspPageMeta`/`moduleFor`, same mechanism as every already-shipped Operations page. `verify-branch-merged.mjs` OK, `git status --porcelain` clean. No findings to file this session.
