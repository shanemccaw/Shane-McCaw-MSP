# #4080 — Workflows + Agents (MSP Console): reusable placeholder module, real nav slots

- **Status:** ⏳ IN FLIGHT 2026-09-14T15:45:16Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-14T15:45:16Z ⏳ IN FLIGHT — investigated `artifacts/msp-console/src/console/nav.ts`, `ConsoleShell.tsx`, `ScreenSlot.tsx`; confirmed no existing generic stub module. `ScreenSlot`'s dashed fallback is the shell's own "module not yet dispatched" default, not the per-page placeholder this issue wants. Real modules (`ScopeSla`, `Sales`, `Sops`, `change-control/shared.tsx`) each define their own local `EmptyState`-shaped card using `console/tokens` + `Icon` — building a reusable `PlaceholderModule` in that same visual language, then wiring real `workflows`/`agents` MSP_PAGES nav entries under Operations to it.
