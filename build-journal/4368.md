# #4368 — Chain summary card: "needs attention" badge + dispatch icon clipped off-screen

- **Status:** ✅ DONE 2026-09-16T15:33:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** 631d12c96 (fix), 30ba89201 (pre-rebase same fix), 5088dad43 (IN FLIGHT bookend)

## Log
- 2026-09-16T15:18:12Z ⏳ IN FLIGHT — confirmed real source is `BuildRollupRow` in
  `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` (not any of the files the
  issue's note-for-whoever-picks-this-up had already ruled out). Wrote
  `build-journal/4368-plan.md`. About to restructure `headerGrid` from one row to two
  so the badge + dispatch icon never share horizontal space with the title/counts line.
- 2026-09-16T15:33:00Z ✅ DONE — restructured `headerGrid` in `BuildRollupRow` from a
  single 4-column row to two `Auto` rows: row 0 is the `summaryRow` (accent dot + title
  + counts, unchanged content, now full-width and free to wrap), row 1 is a new
  right-aligned `badgeRow` `StackPanel` holding `sendButton`, `needsAttentionPill`, and
  `chevron` in the same order as before. No event-wiring changes (`MouseLeftButtonDown`,
  `chevron.Click`, `sendButton.Click`, context menu) — same element instances, just
  reparented. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build
  succeeded, 0 Warning(s), 0 Error(s). `git status --porcelain` clean pre- and
  post-commit. `verify-branch-merged.mjs` confirms `agent/4368-q3031` (631d12c9) is an
  ancestor of `origin/main`.

## Changes Made
- `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` — `BuildRollupRow`: two-row
  `Grid` layout (title/counts line, then a right-aligned badge/icon strip on its own
  row) so the "needs attention" badge and dispatch icon are always reachable regardless
  of chain-title length.

## Verification Results
- `dotnet build` (Debug): succeeded, 0 warnings, 0 errors.
- Code-level confirmation: `summaryRow` (title+counts) is the sole occupant of
  `headerGrid` row 0 with no competing Auto columns; `sendButton`/`needsAttentionPill`/
  `chevron` are children of `badgeRow` (row 1, `HorizontalAlignment.Right`); all
  existing click handlers (`headerBorder.MouseLeftButtonDown`, `chevron.Click`,
  `sendButton.Click`, `headerBorder.ContextMenu`) reference the same element instances
  post-refactor — unchanged behavior, only repositioned.
- BuildConsole/WPF UI is out of scope for test manifests per CLAUDE.md; this is a
  layout-only fix, no live-instance visual verification performed (no BuildConsole
  process launched from this session — see the "NEVER launch exe for verification"
  memory).
