# #3969 — MSP Console: Azure credential management — test + complete

- **Status:** 🛑 BLOCKED 2026-09-14T15:40:00Z
- **Scope:** platform
- **Started:** 2026-09-14

## Log
- 2026-09-14T15:10:00Z ⏳ IN FLIGHT — #3968 confirmed DONE/merged (commit 2534a756d). Starting real end-to-end verification of MSP Console Azure credential management: create/edit/rotate/delete through the UI's routes against #3967's real `/api/msp/:mspId/clients/:clientUserId/azure-credential` endpoints, confirming Key Vault writes and client-scoping (an MSP genuinely cannot see another MSP's client's credential) with two distinct MSP-scoped sessions.
- 2026-09-14T15:40:00Z 🛑 BLOCKED — Ran the live verification against the real dev api-server (serving merged #3968/#3967 code) using two genuine, distinct MSP-scoped sessions obtained via real `/api/auth/login` (freshly provisioned `zz-test-3969-operator-a@scratch.invalid` under MSP 1, `zz-test-3969-operator-b@scratch.invalid` under a fresh secondary test MSP; both users + the test MSP deleted from the DB after the run — no scratch rows left behind).
  - **Client-scoping: CONFIRMED live, both directions.** Operator B → GET/POST client A's credential under B's own mspId → `404 Client not found for this MSP`; Operator B → GET under mspId=1 (not B's own) → `403 Access to this MSP is not permitted` (requireMspScope blocks before the ownership check runs); Operator A → GET client B's credential under A's own mspId → `404`. An MSP genuinely cannot see or write another MSP's client's credential — both real gates (`requireMspScope` + `clientBelongsToMsp`) verified live.
  - **Key Vault writes: CANNOT be verified.** Every POST create fails at the Key Vault step with `RestError: The subscription associated with this vault has been disabled. Vault: ShaneMcCawConsulting;location=eastus` (confirmed in the live `api-server.log`, `azure-keyvault.ts:setSecretValue` → 403 Forbidden from Azure) — the exact same outage already tracked on #3530 (`Shane To-Do`). `azure_tenant_credentials` has zero rows in the local DB; no credential has ever been successfully written through either surface while the vault is down. The route code itself is correctly wired (right params, right error surfaced as 502) — this is #3530's outage, not a bug in #3967/#3968.
  - Wired `blocked_by` edge #3969 → #3530 (replacing a stale edge that had pointed at the now-resolved #3968), posted full findings as a comment on #3969, labeled `blocked` (removed `in-flight`). No code changes this session — verification-only. The scoping half is fully done and will not need to be repeated once #3530 clears; only the write/rotate/delete-persistence half remains.
