# #3517 — Free-flow never re-queues a supervisorily-canceled (exit 0) blocked item

- **Status:** ✅ DONE 2026-09-10T14:26:56Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 6f9e304cc (code), a17fa28fe (IN FLIGHT bookend)

## Log
- 2026-09-10T14:23:12Z ⏳ IN FLIGHT — Implement the loop-safe fix per #3517: let free-flow auto-requeue a supervisory cancel (status 'canceled' + exit_code 0, set by the false-done/board reconcile) IFF the row is still genuinely blocked, while keeping the #1997 anti-loop guard for genuine failures. #3513 (false-done reconcile root cause) already landed (commit cba9399d7), so building forward is safe.
- 2026-09-10T14:26:56Z ✅ DONE — Shipped in `BatterUpQueueService.QueueRowAsync` (commit 6f9e304cc, verified ancestor of origin/main). Free-flow now re-queues a `canceled` + `exit_code==0` dedup row IFF `row.IsBlocked` (live GitHub blocker open AND not bookend-satisfied), reusing the exact dead row (reuseRowId) so no duplicate piles up. Loop-safe: re-queued row is written `queued`/exit NULL and held by the #1600 launch gate until the blocker clears, then launches once; next free-flow pass sees `queued` (non-terminal) and no longer matches; a relaunch that re-cancels is by then unblocked so free-flow won't touch it (manual-only). Genuine `failed` rows, user-cancelled queued rows (exit_code NULL, never ran), and unblocked canceled rows keep the #1997 manual-only guard untouched. Verification: `dotnet build BuildConsole.csproj` clean (0 warnings, 0 errors); live `bt_build_queue` confirms the exit_code distinction is real (69 canceled/exit 0 vs 55 canceled/NULL). desktop/BuildConsole (WPF) is out of scope for test manifests per CLAUDE.md, so verification is the clean build + live-DB confirmation. No findings filed (no new gap discovered). Working tree clean.
