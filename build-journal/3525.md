# #3525 — Webhooks: build delivery replay backend (v2)

- **Status:** ✅ DONE 2026-09-10T21:16:01Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 3136cb473 (also d0c00ab1a bookend)

## Log
- 2026-09-10T21:01:55Z ⏳ IN FLIGHT — Read #3525's body + the landed Design
  export (`Design/portal/design_handoff_full_site/screens/Webhooks.dc.html`).
  The design itself resolves the decision this issue's body posed to Shane:
  it draws **no** Replay control at all — a failed (terminal) delivery
  renders the honest copy "Terminal after 3 attempts. There is no replay —
  this event is gone." (`Webhooks.dc.html:186-188`), also carried in the
  page's own "what this page deliberately does not do" ledger
  (`Webhooks.dc.html:486`). Confirmed live-code state matches: zero
  references to replay anywhere in `webhooks.ts` / `webhook-delivery.ts`, and
  no `webhooksWire.ts`/`webhooksModel.ts`/old portal-v2 page survive the
  `f40438cdc` msp-portal→portal retirement (nothing to remove). No product
  ambiguity remains to stop and ask Shane about — the decision is already
  made and evidenced in the design artifact.
- 2026-09-10T21:16:01Z ✅ DONE — Shipped `830acf01e`→`3136cb473` (rebased onto
  `agent/3525-q2197`, pushed to `main`): decision comments in
  `artifacts/api-server/src/routes/webhooks.ts` (above the deliveries route)
  and `artifacts/api-server/src/lib/webhook-delivery.ts`
  (`attemptDelivery`'s terminal-`failed` branch) recording, in code, that no
  `POST .../replay` route exists by design decision and none should be added
  without a superseding product call; `docs/portal/webhooks-contract-pack.md`
  §7/§9 promoted the replay gap from "open architecture question" to
  `DECIDED (#3525)`, cited to `Webhooks.dc.html:186-188,486`. No replay
  endpoint was built — the landed Design itself resolves the issue body's
  either/or by drawing the honest-terminal-state option, not the
  build-a-replay-path option. **No frontend page work done here** — a live
  `artifacts/portal` Webhooks page does not exist yet in this repo (retired
  with `portal-v2`/`msp-portal` at `f40438cdc`); building it is the
  concurrently in-flight sibling issue #3523's stated scope
  (`build-journal/3523.md`, "building the real `artifacts/portal` Webhooks
  page wired to the existing, already-real `webhooks.ts` endpoints"), which
  will naturally render this decision's honest terminal copy since it's
  baked into the same design file — duplicating that build here would have
  guaranteed a same-file creation conflict with an already-running
  concurrent session. Verification: `webhooks.eventtypes.test.ts` (6/6
  passed, live DB); full `pnpm run test` in `artifacts/api-server` — vitest
  345/352 suites passed (7 pre-existing unrelated failures: config-pack-dry-run,
  public-purchase-packs, social-media-nodes, admin-active-directory-delete,
  index.boot-smoke, portal-offers, tenant-scope-coverage.live-db — none
  touch webhooks and none are caused by this change, confirmed by this
  diff being comment/doc-only); node:test portion (incl.
  `webhook-delivery.test.ts`) didn't execute because `pnpm run test`'s `&&`
  chain short-circuits on that pre-existing vitest failure — a real,
  pre-existing gap, not introduced here. `git status --porcelain` clean;
  branch verified merged into `origin/main` (`verify-branch-merged.mjs` — OK).
- **Finding note:** the `pnpm run test` `&&`-chain short-circuit masking the
  whole node:test suite (incl. `webhook-delivery.test.ts`) whenever any
  pre-existing vitest suite fails is a real, pre-existing test-infra gap,
  but is orthogonal to this issue (the 7 vitest failures it depends on are
  themselves pre-existing and unrelated to webhooks) — not filed as a new
  issue this session; left for whichever session next touches
  `artifacts/api-server`'s test script.
