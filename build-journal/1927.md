# #1927 — Build Queue: add a standalone Verifying filter option

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 963082636 (impl), pushed to main as 0ab065ff8

## Log
- 2026-08-31 ⏳ IN FLIGHT — adding a "Verifying" entry to QueueFilterCombo (Controls/BuildQueuePanel.xaml ~365-402) filtering to exactly `status == VerifyingStatus`, per issue body. Leaving all other filters untouched.
- 2026-08-31 ✅ DONE — added `ComboBoxItem Content="Verifying" Tag="Verifying"` to QueueFilterCombo (BuildQueuePanel.xaml) and the matching `"Verifying" => items.Where(i => i.Status == BuildQueuePostgresClient.VerifyingStatus && !_manuallyHiddenQueueIds.Contains(i.Id))` case in `ApplyFilter` (BuildQueuePanel.xaml.cs), same single-status pattern as Crashed/Parked/Done/Canceled. Every other filter entry/case left untouched. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors. Rebased onto origin/main and pushed directly to main (0ab065ff8); `git merge-base --is-ancestor HEAD origin/main` confirmed. Working tree clean.
