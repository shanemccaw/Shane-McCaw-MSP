# #4503 — identity:app-proxy-connector-groups app-only unreliability & false-positive gate

- **Status:** ✅ DONE 2026-09-17T13:23:24Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** b4ef33e90 (Gate identity:app-proxy-connector-groups on applicationProxy.isEnabled)

## Log
- 2026-09-17T13:03:38Z ⏳ IN FLIGHT — investigating `identity:app-proxy-connector-groups` monitor check (row in `monitor_checks`, endpoint `beta/onPremisesPublishingProfiles/applicationProxy/connectorGroups`). Plan: gate the check's info-severity rule on `applicationProxy.isEnabled` (queried from the sibling singleton endpoint, which reads fine app-only) so tenants with App Proxy disabled get no finding; then confirm on a real App-Proxy-enabled tenant whether app-only returns real connector groups or always empty-lists.
- 2026-09-17T13:23:24Z ✅ DONE — shipped a real, reusable gate mechanism rather than a one-off fix:
  - Added `monitor_checks.gate_endpoint` / `gate_expression` (Drizzle schema `lib/db/src/schema/msp.ts`, additive, NULL for every other check — same convention as `arm_operation`/`sp_operation`/`pp_operation`).
  - Migration `lib/db/migrations/manual/4503-app-proxy-connector-groups-gate.sql` — ran it myself against local `DATABASE_URL`: adds the two columns and sets them on `identity:app-proxy-connector-groups` (`gate_endpoint` = the `applicationProxy` singleton, `gate_expression` = `{{isEnabled}} == true`).
  - `executeMonitorCheck` (`artifacts/api-server/src/lib/monitor-executor.ts`, before the standard graph fetch, right after the fan-out branch): when `gateEndpoint`+`gateExpression` are both set, fetches the gate endpoint first; if the expression doesn't match, the check's own endpoint is never fetched and it persists `status: 'ok'`, `severityMatched: null` (the platform's existing "ran, no finding" contract — confirmed against `build-tenant-profile.test.ts`'s own use of that contract) instead of the false-positive info finding.
  - Added 2 new tests (`executeMonitorCheck — prerequisite gate (#4503)` in `monitor-executor.test.ts`) covering both the skip and pass-through paths; fixed 3 pre-existing test files (`monitor-executor.test.ts`, `onedrive-sharing.test.ts`, `sharepoint-sharing.test.ts`) whose `MonitorCheck` literals needed the two new required fields.
  - **Live-verified against the real testbed tenant** (`c4c814d4-3afe-441e-9145-62461d0a4fd3`, App Proxy `isEnabled: false`) via `node run-script.mjs src/scripts/run-checks-4481.ts <tenant> identity:app-proxy-connector-groups`: `severityMatched: null` — the false-positive finding no longer fires.
  - **Item 2 of the issue (confirm on a real App-Proxy-enabled tenant whether app-only returns real connector groups) could NOT be verified** — the only real tenant in local Postgres is the testbed itself (App Proxy disabled there); `vitest-*`/`zz-test-*` rows are synthetic scratch tenants with no live Graph consent. Reporting honestly rather than guessing: the check stays `active` (gated, not retired) since retiring it would need that missing evidence either way.
  - `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. `vitest run` on `monitor-executor.test.ts` + `onedrive-sharing.test.ts` + `sharepoint-sharing.test.ts`: 254/254 pass. Full api-server suite: 93 failed files / 13 failed tests — all pre-existing (live-db module-scope import baseline per repo memory; none in the files this build touched), 5078 passed.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this. `git status --porcelain` clean.
