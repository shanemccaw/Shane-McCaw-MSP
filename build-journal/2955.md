# Build Journal — Issue #2955

New Site v2 — Work With Me (new home page, real dev: retainer tiers + embedded Stripe checkout)

- **Status:** ✅ DONE 2026-09-05T23:50:18Z
- **Scope:** platform (marketing site `artifacts/shane-mccaw-consulting` + `artifacts/api-server`)
- **Branch:** `production-base-2026-09-05` (NOT main — per dispatch & #2954)
- **Feature parent:** #2953
- **Blocked-by:** #2954 (Shared Chrome) — landed `4ad0df12a`, edge cleared

## Commits (all on origin/production-base-2026-09-05)
- `d009e60ed` — Bookend IN FLIGHT
- `7a6a2e403` — Work With Me home page: retainer tiers + embedded Stripe subscription checkout
- `b61802bdf` — Retainer payment route: SDK 22 confirmation_secret + self-serve tier allowlist
- `3c7ea454e` — Test manifest: Work With Me retainer tiers data layer

## What shipped
- **New home (`/`) = Work With Me** (`src/pages/WorkWithMe.tsx`), built fresh per `Design/fractional_architecture/README.md` §1 + `Work With Me.dc.html`. Uses the shared #2954 chrome (`Layout`/`Header`/`Footer`). Sections: hero (violet stat panel), credibility strip, four-tier accordion, scoped retainers (feature-flagged hidden), Assessment teaser → /assessment, close+contact panel (posts /api/leads).
- **Four-tier accordion** — one open at a time, Advisory open on load, click expands an inline checkout panel with idle → processing → done states. Tier names/hours/prices come from the live catalog via `GET /api/services?type=retainer` (no hardcoded pricing); design "fit"/tag copy mapped by slug in fixed order.
- **Real embedded Stripe checkout** (`src/pages/work-with-me/RetainerCheckout.tsx`) — ports the never-redirects Payment Element pattern from `home/AssessmentFlow.tsx`, adapted to a recurring subscription. Work email → creates the subscription → mounts the Payment Element → confirms in-page (`redirect: "if_required"`).
- **New retainer-scoped payment route** (`artifacts/api-server/src/routes/public-retainer-payment.ts`, registered in `routes/index.ts`, `logger.child({ channel: "billing" })`): `POST /api/public/retainers/payment-intent` creates a `default_incomplete` Subscription and returns `latest_invoice.confirmation_secret.client_secret` (Stripe SDK 22 removed `invoice.payment_intent`); `POST /api/public/retainers/payment-confirmed` re-reads the subscription from Stripe and requires active + order-key match before recording. Amounts resolved server-side from the catalog (`price_cents`); self-serve limited to the four fixed tiers via an allowlist (scoped/discovery-call retainers refused). Not reused from `public-assessment-payment.ts` (that route is woven into consent/tenant/rescan fields that don't apply).
- **Routing:** `/` → Work With Me; the former home content (Assessment scroll experience) relocated to `/assessment` unchanged (content move only; visual redesign is #2956, blocked on this).

## Verification (honest)
- `tsc --noEmit` clean for the marketing app and for my new api-server file (`public-retainer-payment.ts`).
- `vite build` (marketing) and `node build.mjs` (api-server) both succeed.
- **Live against Stripe TEST mode + the real local DB** (one-off harness, deleted after): 12/12 checks —
  Advisory payment-intent creates a real `sub_...` returning a `confirmation_secret` client secret (embedded, no redirect), `amountCents` 90000 from the catalog, `pk_test_` publishable key, `hoursPerMonth` 5; payment-confirmed refuses an unpaid subscription (409) and an order-key mismatch (403); a non-self-serve slug (scoped retainer / assessment) is refused (409); an invalid email is refused (400).
- **Not run:** `shaneapp://runTest` UI/route execution — the runner targets the main-based deployed dev server, and this route + page ship only on `production-base-2026-09-05` (not deployed there). The manifest's apiTest targets the pre-existing deployed `/api/services` data layer; the embedded-checkout evidence is the live harness above (documented in the manifest notes).

## Notes for the next session / Shane
- The two "scoped" retainers (vCISO / Copilot Governance) carry a real `price_cents` in the catalog but are intentionally excluded from self-serve checkout (allowlist) — they route to Contact ("Request scoping"). Scoped-retainers block is feature-flagged off by default (`VITE_SHOW_SCOPED_RETAINERS=true` to show).
- Pre-existing `tsc` errors across ~24 unrelated api-server files are a **worktree junction artifact** (`@workspace/db` is junctioned from the main checkout, so production-base api-server code typechecks against main's newer schema — memory #2121), **not** a real defect and not introduced by this build. The esbuild build passes; my code typechecks clean.
- No genuine bugs discovered → no findings filed.

## Log
- ⏳ IN FLIGHT 2026-09-05T23:13:42Z — Opened. Confirmed #2954 complete. Verified all four retainer tier prices against the live `services` table.
- ✅ DONE 2026-09-05T23:50:18Z — Shipped Work With Me home + embedded retainer subscription checkout + routing move + billing-channel payment route + test manifest. Verified live against Stripe test mode (12/12) and clean builds/typechecks. All four commits on origin/production-base-2026-09-05; HEAD confirmed ancestor of origin/production-base-2026-09-05; working tree clean.
