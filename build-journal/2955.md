# #2955 — New Site v2 — Work With Me (new home page: retainer tiers + embedded Stripe checkout)

- **Status:** ✅ DONE 2026-09-05T23:37:51Z (shipped by a concurrent duplicate build — see below)
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`; new retainer payment route in `artifacts/api-server`)
- **Started:** 2026-09-05
- **Commit(s):** `7a6a2e403` on `production-base-2026-09-05` — the landed #2955 deliverable, authored by the **concurrent duplicate build `agent/2955-q1706`** which finished first. **My own equivalent implementation was NOT pushed** (deliberately, to avoid clobbering the winner). This `main`-based history carries only this bookend file.

## Log
- 2026-09-05T23:11:26Z ⏳ IN FLIGHT — Read #2955's issue body + dispatch comment (in order) and `Design/fractional_architecture/README.md` §1 (Work With Me) + `Work With Me.dc.html` (both only on `main`). Confirmed target branch is `production-base-2026-09-05` (a much older base than `main`: no `build-journal/`, no `Design/fractional_architecture/` there). Blocker #2954 is `complete`-labeled and landed at `4ad0df12a` on `production-base-2026-09-05`; its `blocked_by` edge on this issue was cleared. Plan, matching #2954's split: bookend + progress reporting on this `main`-based branch; do the real page build on `agent/2955-prodbase` (checked out from `origin/production-base-2026-09-05`, deps junctioned via `link-deps.mjs`), push directly to `origin/production-base-2026-09-05`. Work items: (1) query live `services` table for all four retainer tier prices before hardcoding; (2) new retainer-scoped embedded-Stripe route (payment-intent + confirm) ported from `public-assessment-payment.ts` but stripped of assessment-specific fields, with a `logger.child({ channel: "billing" })` binding; (3) fresh four-tier accordion Work With Me page (Advisory open on load, one open at a time, idle→processing→done inline checkout); (4) scoped-retainers block (feature-flagged, hidden), Assessment teaser → `/assessment`, contact/close panel; (5) routing: Work With Me → `/`, relocate current Assessment-scroll home content to `/assessment` (content move only — restyle is #2956, blocked on this).
- 2026-09-05T23:37:51Z ✅ DONE (superseded) — Built the full deliverable independently and verified it: queried the live `services` table (all four tier prices already real in `price_cents`: Advisory 90000 / Essentials 150000 / Growth 300000 / Enterprise 550000 — no migration needed, no hardcoding); wrote a retainer-scoped embedded-Stripe route (`public-retainer-payment.ts`, `billing` channel, first-month PaymentIntent + `setup_future_usage` then a recurring monthly subscription with a +1-month billing anchor; narrow chargeable guard on `serviceType=retainer` + `recurring_monthly` + `hours_per_month`); built `WorkWithMe.tsx` (four-tier accordion, Advisory open on load, inline idle→processing→done checkout, scoped block feature-flagged off, assessment teaser → `/assessment`, contact panel → `/api/leads`); moved routing (`/` → WorkWithMe, `/assessment` → Home). `tsc` clean on both apps (my files), `vite` + api-server builds green, and the retainer endpoints exercised **live against real Stripe TEST mode** (real PaymentIntent + $900 resolved from DB; guards for scoped/project/unknown slug, invalid email, and unsucceeded-intent confirm all correct). **On going to push, discovered a concurrent duplicate build of the SAME issue — `agent/2955-q1706` — had already landed `7a6a2e403` on `production-base-2026-09-05`** (a complete, coherent implementation: `/` → WorkWithMe (663 lines) + `work-with-me/RetainerCheckout.tsx`, `/assessment` → Home, `public-retainer-payment.ts` using a `default_incomplete` subscription, plus `portal-retainer-billing`). Per the repo's "check origin first / never clobber concurrent work" rule I **aborted my rebase, did NOT push my duplicate, and `git reset --hard` back to their landed commit** — the issue is genuinely done in code, just not by my hand. My local `agent/2955-prodbase` commit `ba85b8d1a` is unpushed/local-only and left behind (harmless). Working tree clean (only the pre-existing, un-ignored root `.env.local` remains, which is not mine — I never staged it). Process note: #2955 was double-dispatched (q1705 + q1706) and the q1706 session left the issue label at `in-flight` with no bookend — I finalize the label + bookend here.
