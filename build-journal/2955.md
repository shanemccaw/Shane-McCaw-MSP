# #2955 — New Site v2 — Work With Me (new home page: retainer tiers + embedded Stripe checkout)

- **Status:** ⏳ IN FLIGHT 2026-09-05T23:11:26Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`; new retainer payment route in `artifacts/api-server`)
- **Started:** 2026-09-05
- **Commit(s):** TBD on `production-base-2026-09-05` (real target branch per issue body — NOT `main`). This `main`-based history carries only this bookend file.

## Log
- 2026-09-05T23:11:26Z ⏳ IN FLIGHT — Read #2955's issue body + dispatch comment (in order) and `Design/fractional_architecture/README.md` §1 (Work With Me) + `Work With Me.dc.html` (both only on `main`). Confirmed target branch is `production-base-2026-09-05` (a much older base than `main`: no `build-journal/`, no `Design/fractional_architecture/` there). Blocker #2954 is `complete`-labeled and landed at `4ad0df12a` on `production-base-2026-09-05`; its `blocked_by` edge on this issue was cleared. Plan, matching #2954's split: bookend + progress reporting on this `main`-based branch; do the real page build on `agent/2955-prodbase` (checked out from `origin/production-base-2026-09-05`, deps junctioned via `link-deps.mjs`), push directly to `origin/production-base-2026-09-05`. Work items: (1) query live `services` table for all four retainer tier prices before hardcoding; (2) new retainer-scoped embedded-Stripe route (payment-intent + confirm) ported from `public-assessment-payment.ts` but stripped of assessment-specific fields, with a `logger.child({ channel: "billing" })` binding; (3) fresh four-tier accordion Work With Me page (Advisory open on load, one open at a time, idle→processing→done inline checkout); (4) scoped-retainers block (feature-flagged, hidden), Assessment teaser → `/assessment`, contact/close panel; (5) routing: Work With Me → `/`, relocate current Assessment-scroll home content to `/assessment` (content move only — restyle is #2956, blocked on this).
