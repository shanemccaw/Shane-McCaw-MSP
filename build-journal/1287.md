# #1287 — Drift engine: extend real drift tracking to ALL checks/domains, not just Conditional Access

- **Status:** ✅ DONE
- **Scope:** platform (api-server drift engine)
- **Started:** 2026-08-26
- **Commit(s):** 20eb3558a (schema+migration), cc0dc05b7 (spec registry + collector), 4fbce4260 (wire all 5 executor paths + resolver + registry)

## Log
- 2026-08-26 ⏳ IN FLIGHT — Generalize the #1283 CA-only drift hook into a universal, per-check drift-spec registry wired into ALL five executor paths (graph / powershell / sharepoint-admin / dns / fan-out). Add real drift specs starting with `compliance:eeeu-site-sharing` (fan-out) and `governance:public-teams-discoverable` (graph) that #1333's External Sharing Drift card is blocked on, plus honest `not_comparable` fallback (with a specific persisted reason) where a given executor/check genuinely can't produce a stable before/after diff (e.g. incomplete fan-out coverage).
- 2026-08-26 ✅ DONE — New `drift-check-specs.ts`: an executor-agnostic per-check registry. Each spec turns a completed scan's `items`/`extracted`/`status` into a STABLE, id-keyed comparable config (avoiding `detectDrift`'s positional-array footgun — a new site is an `add` at `/sites/<id>`, not a whole-array `replace`) or returns `{ comparable:false, reason }`. `maybeCollectDriftForCheck` (drift-collector.ts) drives it and upserts the new `drift_collection_status` table (tracked / baseline_captured / not_comparable / error + specific reason). `collectDriftForCompletedCheck` in monitor-executor.ts replaces #1283's CA-only inline hook and now runs on ALL five executor paths. Real specs span four executor types: `ca-policy` + `public-teams-discoverable` (graph), `eeeu-site-sharing` (fan-out, coverage-guarded — a truncated scan is refused with a specific reason rather than fabricating removed shares), `tenant-sharing-capability` (sharepoint-admin), `email-authentication` (dns). PowerShell path is wired (hook present) but has no spec yet, deliberately: its checks split between deterministic policy config and non-deterministic operational counts, and a guessed diff would manufacture false churn — a one-line registry entry adds one once a cmdlet's output is confirmed stable. `resolveDriftEvents` now surfaces a not_comparable/error scan's specific reason instead of the generic "never scanned". Four new `drift:*` timeline metrics registered so the domains are consumable (External Sharing Drift #1333 reads `drift:eeeu-site-sharing`). Tests: 17 pure spec tests + 5 new DB-backed orchestration tests (incl. the truncated-fan-out honest refusal), all green alongside the existing 23 drift-collector tests (45 total). Manual SQL `lib/db/migrations/manual/2026-08-26-drift-collection-status-1287.sql` run locally; Shane must run it on staging/prod (Shane To-Do).
