# #1287 — Drift engine: extend real drift tracking to ALL checks/domains, not just Conditional Access

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform (api-server drift engine)
- **Started:** 2026-08-26
- **Commit(s):** _pending_

## Log
- 2026-08-26 ⏳ IN FLIGHT — Generalize the #1283 CA-only drift hook into a universal, per-check drift-spec registry wired into ALL five executor paths (graph / powershell / sharepoint-admin / dns / fan-out). Add real drift specs starting with `compliance:eeeu-site-sharing` (fan-out) and `governance:public-teams-discoverable` (graph) that #1333's External Sharing Drift card is blocked on, plus honest `not_comparable` fallback (with a specific persisted reason) where a given executor/check genuinely can't produce a stable before/after diff (e.g. incomplete fan-out coverage).
