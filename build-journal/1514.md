# #1514 — Risk Register: Rejection-to-risk path from Change Control

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 279a85fbb (implementation), f6c151e3f (merge to main, pushed)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Investigation found the "spawn" half of #1514 already
  shipped under #1496's commit (ab2368df8c, and portal-change-rejection.ts /
  m365-change-router.ts): `spawnedByChangeRequestId` is written on every
  customer-declined risk. `dischargedByChangeRequestId` exists as a column
  (migrated 2026-08-29-m365-change-routing-1534.sql) but has no writer anywhere
  in the codebase — that's the real remaining gap. Also found
  `createAssignedRiskFromRejection` never carries `msp_change_requests.remediation_check_key`
  forward onto the risk's own `checkKey`, which would make a discharge lookup by
  checkKey unreachable for CR-declined risks. Building: the checkKey carry-forward
  fix, a discharge helper wired into the CR-creation route (a fresh CR raised
  against the same checkKey as an active accepted risk discharges it), and the
  wire-contract extension on the customer risk register GET route.
- 2026-08-31 ✅ DONE — Shipped: (1) `portal-change-rejection.ts` now carries
  `remediation_check_key` forward as the risk's `check_key` when a customer
  rejects a non-routed CR. (2) New `change-request-risk-discharge.ts` —
  `dischargeRisksForNewChangeRequest()`, wired non-fatally into
  `POST /portal/change-control`: a fresh CR raised with the same
  `remediationCheckKey` as an active, undischarged accepted risk (same
  mspId+tenantId scope #1279's suppression query already uses) sets
  `discharged_by_change_request_id` and flips `riskStatus` to `Closed`. The
  acceptance `status` itself is deliberately left untouched (#1507: an
  acceptance is a permanent signed fact; `revoked` stays the MSP-operator's own
  act, not a side effect of a new CR existing) — flagged in the issue comment
  as a design call Shane may want to revisit, since it means #1279's
  suppression on the ORIGINAL finding keeps holding until the discharging CR
  actually resolves it, not the moment it's raised. (3) `portal-risk-register.ts`'s
  `WireRisk` now surfaces `spawnedByChangeRequestCode`/`dischargedByChangeRequestCode`
  as display codes — no UI, per the issue's explicit SCOPE STOP.
  No new migration needed — both FK columns were already migrated on
  2026-08-29. Verified the discharge UPDATE predicate directly against local
  Postgres (fires once, `IS NULL` guard blocks a repeat). New tests:
  `portal-change-rejection.test.ts` (2), `change-request-risk-discharge.test.ts`
  (2) — both registered in `vitest.config.ts`'s include list, all passing.
  `portal-cab-store.test.ts` (pre-existing, exercises the same rejection path)
  still green. `tsc --noEmit` clean on a warm run (the first cold run hit the
  known #1778 module-resolution artifact, not a real error). 8 pre-existing
  failures in `portal-risk-register.test.ts` confirmed unrelated — reproduced
  identically against the pre-change file, already covered by #1772. Branch
  merged origin/main (one trivial additive conflict in `vitest.config.ts`'s
  include list, resolved by keeping both new entries) and pushed straight to
  main as a fast-forward; `verify-branch-merged.mjs` confirms `f6c151e3f` is an
  ancestor of `origin/main`. `git status --porcelain` clean.
