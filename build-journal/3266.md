# #3266 — Standalone mileage capture for Cars (log_car_maintenance requires description+amount)

- **Status:** ✅ DONE 2026-09-13T07:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 9e9f9f7c2 (rebased/pushed as 85788e115 on origin/main)

## Log
- 2026-09-13T06:50:55Z ⏳ IN FLIGHT — Investigated `log_car_maintenance` (src/mcp/tools.mjs:1849), `vehicles.logMaintenance` (src/core/vehicles.mjs:387) and the capture-grammar `vehicle_maintenance_log` rule (src/core/capture-grammar.mjs:832). Plan: relax `logMaintenance`'s description/amount requirement to allow a mileage-only odometer reading (defaults description to "Odometer reading", amount to 0 — a real, honest label for what happened, not fabricated data), update the MCP tool schema/description to match, and add a capture-grammar rule matching "Tesla's at 40,900 miles" so the onboarding example actually resolves.
- 2026-09-13T07:20:00Z ✅ DONE — `vehicles.logMaintenance` now allows mileage alone (writes a real $0 "Odometer reading" row); `log_car_maintenance`'s MCP schema relaxed to `required: ["vehicle"]` with an updated description; added the `vehicle_mileage_update` capture-grammar rule (`web/shanes-life/src/core/capture-grammar.mjs`) matching the onboarding card's own literal example, plus a unit test. Verified: `node src/core/capture-grammar.test.mjs` — 72/72 passed (against local `finances` Postgres db). Live end-to-end check against that same real local DB: created a scratch `ZZ-TEST-3266` vehicle, called `logMaintenance(userId, id, { mileage: 40900 })`, confirmed the real row (`description: "Odometer reading", amount: 0, mileage: 40900`) via `getVehicle`, then deleted the scratch vehicle. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. `git status --porcelain` clean.
