# #1980 — Shared checkout node_modules symlinks dangling into swept worktree

- **Status:** ⏳ IN FLIGHT 2026-09-06T15:20:00Z
- **Scope:** platform (dev-server tooling, scripts/dev-server)
- **Started:** 2026-09-06
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-06T15:20:00Z ⏳ IN FLIGHT — confirmed real current corruption: main checkout's
  root `node_modules` had 670 links foreign/pointing into `C:\wt\2936-q1730` (swept
  earlier today; per `.logs/dev-server/cleanups.log` the poisoning first appears at
  the 2026-09-06T14:36:52Z removal of `2950-q1726` and survived unrepaired through
  9 further sweep removals up to 15:07:05Z, ~30 min, because `removeWorktreeSafe`'s
  post-removal canary only ever logged a `console.warn` — nothing acted on it).
  Ran `node scripts/dev-server/store-doctor.mjs --repair` to relink all 670 entries
  from the intact main `.pnpm` store (0 unrepairable). Now fixing the underlying gap:
  wiring `repairSharedStore()` (already implemented for the CLI `--repair` flag) into
  both `provision-worktree.mjs`'s pre-junction scan and
  `worktree-lifecycle.mjs`'s post-removal canary, so poisoning is auto-repaired the
  moment either scan detects it, instead of sitting logged-but-unfixed.
