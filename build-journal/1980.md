# #1980 — Shared checkout node_modules symlinks dangling into swept worktree

- **Status:** ✅ DONE 2026-09-06T15:35:00Z
- **Scope:** platform (dev-server tooling, scripts/dev-server)
- **Started:** 2026-09-06
- **Commit(s):** d1d962866 (bookend IN FLIGHT), f59d77f76 (real fix, merged to main as f71333aec)

## Log
- 2026-09-06T15:20:00Z ⏳ IN FLIGHT — confirmed real current corruption: main checkout's
  root `node_modules` had 670 links foreign/pointing into `C:\wt\2936-q1730` (swept
  earlier today; per `.logs/dev-server/cleanups.log` the poisoning first appears at
  the 2026-09-06T14:36:52Z removal of `2950-q1726` and survived unrepaired through
  9 further sweep removals up to 15:07:05Z, ~30 min, because `removeWorktreeSafe`'s
  post-removal canary only ever logged a `console.warn` — nothing acted on it).
  Ran `node scripts/dev-server/store-doctor.mjs --repair` to relink all 670 entries
  from the intact main `.pnpm` store (0 unrepairable). Now fixing the underlying gap:
  wiring `repairSharedStore()` (already implemented for the CLI `--repair` flag) into
  both `provision-worktree.mjs`'s pre-junction scan and
  `worktree-lifecycle.mjs`'s post-removal canary, so poisoning is auto-repaired the
  moment either scan detects it, instead of sitting logged-but-unfixed.
- 2026-09-06T15:35:00Z ✅ DONE — shipped commit f59d77f76 (merged to main as
  f71333aec, verified via `verify-branch-merged.mjs`). Both callers of
  `scanSharedStore` now call `repairSharedStore()` themselves the instant their scan
  is not clean, and both still log detection + repair outcome (`autoRepair` on
  `storeHealth`/`storeAfterRemoval`) — nothing hidden. `removeWorktreeSafe`'s return
  value now also carries `storeAfterRemoval` (was log-only before), so
  `sweepWorktrees`'s `--json` output surfaces it per removed worktree too. Added
  `scripts/dev-server/store-doctor-autorepair.selftest.mjs` (isolated throwaway
  repo; plants a real foreign junction, removes an unrelated worktree, asserts the
  canary auto-repairs it and the link resolves through to real content) — 8/8
  assertions pass. Pre-existing `worktree-sweep.selftest.mjs` still passes
  unchanged (6/6). Real shared checkout re-scans CLEAN (`store-doctor.mjs`, no
  args) after the manual `--repair` pass. `git status --porcelain` clean.
  Filed no new GitHub issue — this session's own real, current-state finding (the
  670-link poisoning and its 30+ min unrepaired window) is documented in this
  commit/bookend and on #1980's own issue comment rather than a separate issue,
  since it is direct evidence for the exact gap #1980 was already filed to fix, not
  a new/different problem.
