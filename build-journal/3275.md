# #3275 — Capture bar redesign: multi-line textarea, Enter=newline, per-line filing (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T22:15:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** aecde0785, 8ba458597

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3275 + Design/design_handoff_shanes_life/README.md
  ("Capture bar redrawn" section); confirmed current state (`#capture-text` is already a
  `rows="1"` textarea in `web/shanes-life/public/index.html`, with a keydown handler that
  currently makes Enter submit the form). Implementing the 5 real-scope items: auto-grow to
  5 lines, Enter=newline/⌘-Enter or Send submits, per-line filing with a summary toast, the
  26px pill + three 40px round buttons, and the translucent Today fox-bubble.
- 2026-09-08T22:15:00Z ✅ DONE — shipped all 5 real-scope items in
  `web/shanes-life/public/{index.html,app.css,app.js}`:
  1. Auto-grow: `field-sizing: content` on the textarea, with a `CSS.supports` JS-height
     fallback for browsers without it, capped at 122px (~5 lines at 15px/1.35) either way.
  2. Enter now inserts a newline (removed the `preventDefault`+`requestSubmit` on bare Enter);
     only Send or ⌘/Ctrl+Enter submits.
  3. Per-line filing: submit splits on `\n`, trims/drops blank lines, files each line through
     the same per-route logic (shopping grammar vs `/api/captures`) as before, attachment (if
     any) riding only the first line, then shows "N lines, each filed on its own." when >1
     line, else the existing "Got it." toast.
  4. Pill restyle: `.capture-row` is now the 26px-radius pill (`padding:5px 5px 5px 16px`,
     card bg/border/shadow); the textarea itself is transparent/borderless inside it; Camera/
     Mic/Send are three 40px round `.icon-btn`s — Send toggles `.has-text` (accent vs faint
     `rgba(255,255,255,.08)`), Mic gets an accent-fill + 4px ring while recording instead of
     the generic destructive-red `aria-pressed` rule.
  5. Today fox-bubble: `rgba(10,26,48,.38)` bg + `backdrop-filter: blur(14px) saturate(1.2)`,
     `rgba(255,255,255,.14)` border, 600-weight `#F8FAFC` text with the spec'd text-shadow.
  Verification: `node bin/check.mjs` run against local Postgres (copied `.env` from the main
  checkout for this standalone app, gitignored, not committed) — 217/221 passed; the 4
  failures are all pre-existing `SL_VAULT_KEY not set` gaps in the Vault room, unrelated to
  this issue's scope. Manually confirmed the new markup/CSS serve correctly by running the
  server on a scratch port (5091, since the shared dev-server already owns 5000) and curling
  `/` and `/app.css` — new pill/button markup and CSS rules present as written. No
  test-manifest needed (Test Coverage section scope is `artifacts/msp-portal/` and
  `artifacts/shane-mccaw-consulting/` only; this is `web/shanes-life/`). `git status
  --porcelain` clean; branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`.
