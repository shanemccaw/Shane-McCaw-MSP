# Issue #4262 — Run Script pipeline (POST /api/admin/run-script) is dead — migrate off retired azure-automation.ts stub to ps-execution-client

- **Status:** ✅ DONE 2026-09-15T16:58:40Z (code landed; live container run not verified: the container environment is suspended, see #4265)
- **Scope:** platform
- **Commits:** 0a4ef652d (work), 54448de01 (IN FLIGHT bookend)
- **Migration:** `lib/db/migrations/manual/4262-library-script-ps-cmdlet-binding.sql` (run locally; appended to #1630)
- **Findings filed:** #4265, #4266, #4267

## Log
- 2026-09-15T16:40:09Z — Starting. Migrating `POST /admin/run-script` and `GET /admin/run-script/:jobRef/status` in `admin-m365-run.ts` from the retired `azure-automation.ts` stub to `ps-execution-client.ts`, preserving the `{ status, outputLines, findings, recommendations, scoreImpact }` client contract.
- ✅ DONE 2026-09-15T16:58:40Z — Commit 0a4ef652d. Both `POST /admin/run-script` and `GET /admin/run-script/:jobRef/status` now run through `ps-execution-client.ts` instead of the retired `azure-automation.ts` stub. Neither route calls `createScriptJob`/`getJobStatus`/`getJobOutput` any more, and `getSecretValue` is gone from the route. The container only runs code-owned catalog entries (#209), so `powershell_scripts` and `script_modules` got a nullable `ps_cmdlet_key` + `ps_params` binding, the same contract as `monitor_checks`. An unbound script now gets a 422 before any row is written. `Organization` is resolved server-side from the scoped tenant and overrides any stored value. The `jobRef` is a server-minted UUID. The status response shape `{ status, outputLines, findings, recommendations, scoreImpact }` is unchanged. Runs are capped at 10 minutes, and a run still `running` 15 minutes after it started is reported failed. AI analysis now runs on completed runs only.
  Verification: `tsc --noEmit` is clean for api-server and lib/db. `admin-m365-run.test.ts` passes 6/6 (node --test), including new POST tests for the 422 refusal and for the bound run, which checks the cmdletKey, the server-resolved Organization and that no ClientSecret is sent. Live check on an isolated api-server (worktree build, :8099) as a synthetic `zz-test-*` PlatformAdmin against testbed tenant c4c814d4: an unbound real script returned HTTP 422 as designed. A synthetic script bound to `get-connection-info` returned HTTP 200 `{jobRef, status: running}`. Polling then returned `status: failed` with `ps-execution unreachable: fetch failed`, and the run row, kanban-free path and audit entries were all written correctly. The container call failed because the Container Apps environment `cae-smccaw-2184` is `ManagedClusterSuspended` (subscription disabled). That is out of agent reach, filed as #4265 (Shane To-Do). A real container execution is still NOT verified. Synthetic user, script and run rows were deleted afterwards; the audit rows were left.
  Findings: #4265 (container env suspended, Shane To-Do), #4266 (Ask Shane: no script is bound and nothing sets a binding; the #209 execution-model decision), #4267 (azure-automation stub still wired into workflow-executor.ts / client-script-sequence.ts). `git status --porcelain` was clean before this bookend; nothing pre-existing was dirty.
