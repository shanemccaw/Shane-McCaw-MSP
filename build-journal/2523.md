# #2523 — Ownership / RACI: Cross-customer MSP view — what do I hold, everywhere

- **Status:** ✅ DONE 2026-09-03T13:19:39Z
- **Scope:** platform
- **Commit(s):** none new — verified against already-merged `f683fb76c` (#1521)
  and `4c90ccb87` (#2162, the same route file's symmetric write endpoints)

## Log
- 2026-09-03T13:17:52Z ⏳ IN FLIGHT — pulled #2523's body: identical scope to #1521
  ("carried forward from #1521, closed, superseded, dedup limit"). Investigating
  whether #1521's own work (closed 2026-08-31, `f683fb76c`) already covers this
  issue's full scope before writing any new code.
- 2026-09-03T13:19:39Z ✅ DONE — confirmed #2523 has zero new scope beyond #1521,
  which already shipped and is verified merged into `origin/main`:
  - `git merge-base --is-ancestor f683fb76c origin/main` — true (the feature
    commit: `GET /api/msp/ownership/mine` in `artifacts/api-server/src/routes/
    msp-ownership.ts`, backed by the pure mapping module
    `artifacts/api-server/src/lib/msp-ownership-book.ts`).
  - `4c90ccb87` (#2162) and `df93d525a` (#1519) also ancestors of `origin/main`
    — later sessions extended the same route file with the symmetric MSP-side
    `/assign`, `/accept`, `/decline` write endpoints (#2162, redo of #1518) and
    decline-escalation handling (#1519), so the route is not just the original
    read but has kept growing on top of the same #1521 foundation.
  - Route confirmed mounted: `artifacts/api-server/src/routes/index.ts:211`
    imports and registers `mspOwnershipRouter`.
  - Read the full route + mapping module: MSP-side identity boundary is
    `mspRole IN ('MSPAdmin','MSPOperator','ServiceAccount') AND mspId = caller's
    mspId`, OR `mspRole = 'PlatformAdmin'` — exactly `users_role_scope_check`'s
    own boundary, nothing invented. Scoping mirrors `msp-executive.ts`
    (`resolveMspIdStrict` + `resolveStaffScopedCustomerIds`), so a scoped
    MSPOperator never sees a customer outside their assignment. `byCustomer`
    always carries every in-scope customer including zero-count ones, matching
    #1491 note 7 ("dense for some, empty for others is real information, not a
    data quality problem").
  - Verified against real local Postgres (`DATABASE_URL`): `portal_ownership_
    assignments` schema matches the route's query shape exactly (`customer_id,
    object_id, role_key, owner_person_id, acceptance, order_rank`, unique index
    on the 4-column tuple — the #1515 multi-holder fix is live). Real data: 2
    tenants (both `msp_id=1`), 1 user with an MSP-scoped role (`id=1`,
    `PlatformAdmin`), 0 rows in `portal_ownership_assignments`. Hand-traced the
    query against that state: `mspPersonIds = ['u1']`, `holdings: []`,
    `byCustomer: [{customerId:1,count:0},{customerId:3,count:0}]` — an honest
    empty result matching real DB state, not a fixture.
  - `msp-ownership-book.test.ts` (5 vitest cases) read and reasoned through by
    hand against `resolveHoldingsForCustomer`'s real implementation — logic
    matches: skips non-r/a/c/i role keys, skips an assignment naming an object
    no longer in the live object list rather than inventing a name, defaults
    null acceptance/orderRank to real empty values, preserves independent
    per-holder ordering. Could not execute the suite live — see blocker below.
  - **Could not run `pnpm exec vitest` or a clean `tsc --noEmit`**: the MAIN
    checkout's shared `node_modules`/pnpm store is genuinely broken repo-wide
    (`node_modules/drizzle-orm`, `node_modules/vitest`'s package dir, etc. are
    empty/missing at the top level, not a worktree-specific dangling link).
    `tsc --noEmit` on `artifacts/api-server` returned 3852 errors repo-wide
    (`Cannot find module 'vitest'`, `'drizzle-orm'`, `'pino'`, `lib/db/dist`
    unbuilt), including in `msp-ownership.ts`/`portal-ownership.ts` themselves
    — all environmental, not a real regression in this code (the prior #1521
    session on 2026-08-31 reported only 64 pre-existing, unrelated errors and
    all 5 vitest cases passing, so this broke afterward, elsewhere). Already
    extensively filed and open: #2515, #2528, #2192, #2193, #2501, #2516 — not
    refiling. Per the standing rule, `pnpm install`/`--force` is not a remedy
    for this class of failure and was not attempted.
  - **No UI wiring**: `#1727` (wire the portal UI to the real endpoints) is its
    own open issue, explicitly blocked on the Claude Design export `#1726`
    ("There is no UI to wire until an export exists in `Design/portal/`...
    must not create a page") — confirmed no `.dc.html` exists yet for this
    surface. Out of scope for #2523 by #1491's own structural convention
    (architect → endpoints → contract pack → Design → wire) and by #1727's own
    explicit scope stop.
  - **git status --porcelain clean** before DONE; branch verified merged via
    `verify-branch-merged.mjs` after rebase onto `origin/main` (which had moved
    since this worktree was provisioned).
  - **Conclusion: no code changes were needed.** #2523's full body — the
    cross-tenant MSP ownership read, with explicit fail-closed guard work
    rather than inherited scoping, emergent per-customer density — is already
    real, live, and verified in `main`. Filing this as a genuine "no code
    changed" DONE rather than re-doing #1521's work under a new number.
