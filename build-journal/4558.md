# #4558 — Opening a large file crashes BuildConsole (NavigateToString 2,097,152-char limit)

- **Status:** ✅ DONE 2026-09-17T19:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 5b79582a2

## Log
- 2026-09-17T19:35:00Z ⏳ IN FLIGHT — confirmed MainWindow.xaml.cs:6730 still calls `wv.CoreWebView2.NavigateToString(htmlContent)` with no length check. About to add a length guard, a temp-file `Navigate` fallback for HTML over WebView2's 2,097,152-char ceiling, an honest in-tab notice, and a try/catch around the whole call.
- 2026-09-17T19:52:00Z ✅ DONE — added `NavigateViewerContent` helper (MainWindow.xaml.cs) that: (1) uses `NavigateToString` unchanged when `htmlContent.Length < 2,097,152`; (2) otherwise writes `htmlContent` to a real temp `.html` file under `%TEMP%\BuildConsole_LargeFileViewer\` and calls `CoreWebView2.Navigate` on its file URI, preserving the full rich Monaco/Markdown viewer for large files with no in-memory string ceiling; (3) if the temp-file write/navigate itself throws, falls back to an honest plain-text `NavigateToString` view (truncated safely under the same limit) stating the file is too large and why the fallback failed; (4) wraps the whole path in try/catch so any other unexpected failure degrades to a visible in-tab error message instead of the unhandled "UI Thread Error" dialog. Verified via `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Not launched/click-tested against a real oversized file per the "never launch the exe for verification" rule; verification here is a clean build + code-path review against the exact reproduction described in the issue (large `.json` generating >2,097,152-char HTML). Commit 5b79582a2 pushed and confirmed merged into origin/main via `verify-branch-merged.mjs`. Working tree clean.
