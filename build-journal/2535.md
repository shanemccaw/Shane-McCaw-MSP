# #2535 — Git panel RunGitCommand silently swallows stdout/stderr; add Stage All

- **Status:** ✅ DONE 2026-09-03T13:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 4909f7ef9 (implementation), ff417706c (IN FLIGHT bookend) — both on origin/main

## Log
- 2026-09-03T12:57:48Z ⏳ IN FLIGHT — BuildConsole launched into an empty/unprovisioned worktree (#2118 shape); manually created worktree at C:\wt\2535-q1371 off origin/main, registered with $CLAUDE_PID, linked deps. Audited `RunGitCommand` in `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs` (~7143): stdout/stderr redirected but never read, exit code never checked, wrapped in empty `catch {}`. About to: capture+surface real stdout/stderr+exit code, add success confirmation, add Stage All bulk action, audit same pattern elsewhere.
- 2026-09-03T13:15:00Z ✅ DONE — Fixed `RunGitCommand` (now `Task<bool>`): drains stdout AND stderr (before WaitForExit, to avoid pipe deadlock), checks ExitCode, and surfaces git's own message in `GitStatusSummaryText` — `✓ <line>` on exit 0, `✗ git <args> failed — <reason>` on non-zero, full raw output on the ToolTip. `BestGitLine` picks the informative line (fatal/error/rejected/nothing-to-commit prioritized; warning:/hint: noise skipped). Success/failure distinct and logged to ActivityLog (git-panel.ok / git-panel.error). Commit box now only clears on a genuine zero-exit commit, preserving the typed message on failure. Added "Stage All" button (git add -A) to the CHANGES header. `RefreshGitStatus` split into awaitable `RefreshGitStatusAsync` so the result message wins over the refresh summary. Fire-and-forget call sites discarded via `_ =` (no CS4014). **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` = 0 warnings, 0 errors (before AND after rebase onto origin/main). Cannot drive the WPF UI directly (never launch BuildConsole.exe — single-instance mutex could courier into Shane's live session), so verified the real git signals the branches depend on in a throwaway repo: nothing-staged commit → exit 1 + "nothing to commit"; staged commit → exit 0 + "[branch hash] msg"; add -A → exit 0 stages all; push w/o remote → exit 128 + "fatal:". Branch merged to origin/main (verify-branch-merged.mjs OK, 4909f7ef). Audit follow-up: the 3 read-only git query sites (status/branch/log) share the same swallow pattern but are lower severity (reads, not mutations) — filed as **#2536** (sibling of #1791, board "AI Batter Up"), not fixed here per #2535's "fix trivially or file separately". Working tree clean. Pre-existing dirty-not-mine: none.
