# #1881 — Run Now locks up the entire application while the process starts

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** ea47603c3 (bookend IN FLIGHT), 3b250cfe8 (real fix)

## Log
- 2026-08-31 ⏳ IN FLIGHT — traced SafeLaunch/LaunchItem; confirmed via a live, real
  (non-mocked) repro harness that RedirectedProcessLauncher.Launch (the real Win32
  CreateProcess call) and the worktree-provisioning sync prefix both run synchronously
  on whatever thread calls SafeLaunch — which for Run Now is the UI thread. Also found
  TickAsync (the normal poll path) has the same exposure via its DispatcherTimer.Tick.
  Fixing SafeLaunch per the issue's scope; filing TickAsync as a separate finding.
- 2026-08-31 ✅ DONE — Fixed `SafeLaunch` (`QueueWatcherService.cs`) to
  `await Task.Run(() => LaunchItem(...))` instead of `await LaunchItem(...)` directly,
  so the entire LaunchItem body (including the always-synchronous
  `RedirectedProcessLauncher.Launch` CreateProcess call, and any pre-first-await sync
  work) runs off the calling thread. Verified LIVE, not just by code read:
  1) a real repro harness invoked the actual `WorktreeProvisionService`/
     `RedirectedProcessLauncher` production code against the real `claude.exe` on this
     machine — measured the pre-fix synchronous cost at ~50-200ms per launch (worse
     under real contention), and confirmed the worktree-provisioning await itself
     already yielded correctly (that path was not the bug).
  2) A second harness reproduced the exact `_ = SafeLaunch(...)` fire-and-forget shape
     with the fix applied and confirmed the caller regains control before
     `RedirectedProcessLauncher.Launch`'s CreateProcess call completes on the
     background thread.
  3) `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
     warnings, 0 errors.
  I did NOT click a real "Run Now" on a live queue item in Shane's actively-running
  BuildConsole instance (pid 28596, orchestrating this very build) — that would force-
  claim a real queued item and could launch a real, credit-consuming Claude session
  unintentionally. Instead I reproduced the actual vulnerable production code paths
  directly, with real timing, which is a genuine live reproduction without that risk.
  Scratch artifacts (a throwaway `selftest-1881-repro` git worktree/branch created by
  the repro, and a temp harness project) were fully cleaned up before finishing.
  Found and filed a related gap: `TickAsync` (the normal 10s poll pickup loop) has the
  identical UI-thread exposure via its `DispatcherTimer.Tick` — out of #1881's stated
  scope, filed separately as **#2096**, parented to #1202, board status "AI Batter Up".
  Branch verified merged into origin/main (`verify-branch-merged.mjs` — OK). Working
  tree clean (one unrelated tracked scratch file, `shaneapp-executeSql.result.json`,
  was touched only by my own `reportProgress` protocol call as a side effect — reverted
  before finishing, not part of the deliverable).
