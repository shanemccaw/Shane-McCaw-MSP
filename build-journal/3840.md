# Build Journal — Git #3840

- **Status:** ✅ DONE 2026-09-12T21:15:03Z
- **Scope:** buildconsole

## Log
- 2026-09-12T21:05:00Z — ⏳ IN FLIGHT — Title bar search pill: blend color, widen, verify height fit in 36px caption.
- 2026-09-12T21:15:03Z — ✅ DONE — `desktop/BuildConsole/MainWindow.xaml`: `SearchBorder.Background` changed from `Surface0Brush` (`#313244`, distinctly lighter than the window's root `BaseBrush`) to `MantleBrush` (`#CC161B22`), same dark-charcoal family as the root background so the pill blends instead of reading as a cutout rectangle; `Surface1Brush` border kept so it still reads as clickable. `Width` widened `240` → `320`. Height: verified, not changed — `SizeSearchBoxToCaptionHeight()` computes `Math.Max(24, captionHeight-8)` = 28px inside a title-bar container whose own `Height="36"` (MainWindow.xaml:84) exactly matches `WindowChrome.CaptionHeight`; with `VerticalAlignment="Center"` that centers with 4px above/below and no clipping — real math, no code change needed there. `dotnet build BuildConsole.csproj -c Debug` — 0 Warning(s), 0 Error(s).
