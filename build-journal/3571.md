# #3571 — legacy-ladder.ts LEGACY_CAPABILITY_RULES stale source citations

- **Status:** ✅ DONE 2026-09-11T04:52:54Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 65f7156da (fix), 97006276c (IN FLIGHT bookend)

## Log
- 2026-09-11T04:48:52Z ⏳ IN FLIGHT — pinning the two stale `source` citations
  (`msp:ladder.*` and `msp:purchases.approve`) in
  `lib/db/src/rbac/legacy-ladder.ts` to the real pre-#2460 commit/line ranges that
  actually held the transcribed rule, same form as #3360's `portal-team.ts` fix.
- 2026-09-11T04:52:54Z ✅ DONE — verified in git history: `3dddd4b26` is the commit
  that actually deleted `ROLE_ORDER`/`roleIndex` from `requireAuth.ts` (retired
  the "80-93" citation); `2868efa79` is the commit that moved the
  `purchases.approve` decision block out of `msp-v1.ts`. Fixed both `source`
  fields to cite the real pre-#2460 code at `3dddd4b26^` and `2868efa79^`
  respectively, with real verified line numbers. `legacy-ladder.test.ts`'s
  citation-format regex still matches; ran the file directly —
  `npx vitest run src/rbac/legacy-ladder.test.ts` — 30/30 pass. Also found
  `ladderCapabilityDescription()` carries the same stale `requireAuth.ts:80-93`
  citation and that string is baked into seeded `rbac_capabilities` DB rows via
  `2026-09-09-rbac-seed-current-model-2457.sql` — out of #3571's stated scope
  (title/suggested-fix table covered only the two `LEGACY_CAPABILITY_RULES[].source`
  fields), filed as #3638, parented under #1696, board status set to AI Batter Up.
  Branch verified merged into origin/main via `verify-branch-merged.mjs`; working
  tree clean.
