# Git #3216 — Feature: Widget "Heading Home" quick action

- **Status:** ✅ DONE 2026-09-08T18:46:45Z
- **Scope:** platform (web/shanes-life)

## Log

- 2026-09-08T18:37:08Z — Started. Confirmed real unblock: #3216's `blocked_by` pointed at
  #3158, which is now labeled `complete` (real commit 60963b2e1, 81217a61b — Tesla OAuth +
  webhook trigger shipped, dedupe-verified live locally). `gh api .../3158/dependencies/blocked_by`
  is empty — #3158 is not itself blocked on #3199 any more (Shane's own 2026-09-08 narrowing
  comment removed that edge; only the well-known public-key hosting genuinely needed the deploy,
  and Replit is live). Proceeding on #3216 for real.
- Real, new problem found in the same investigation: #3158 explicitly did NOT build Tesla
  vehicle-*command* sending (navigation, preconditioning start) — its own header says that needs
  Tesla's separate command-signing protocol and is real, separate work for sibling Features
  #3216/#3218. This build needs to actually send those commands, so that signing gap is now
  squarely in scope here.
- Real DB check (local): `places` and `things.house` are both empty — no H1/H2/Work places filed
  yet locally (expected; places are filed conversationally via push_place, and the local dev DB
  starts empty same as every other table here).
- Copied the real `web/shanes-life/.env` from the main checkout into this worktree (same
  gitignored-file precedent #3158's own bookend already used) to run migrations/tests locally.
- 2026-09-08T18:46:45Z — **DONE — real "Heading Home" quick action shipped: presence tracking,
  which-home resolution, and real Tesla navigation + preconditioning command sends.** Commit
  hash recorded below once pushed.

**What shipped:**
- `migrations/057_heading_home.sql` — `places.house` (tags a saved place as one of Shane's real
  houses, e.g. "h1"/"h2", same free-text convention `things.house`/`money.mjs`'s
  `BILL_CATEGORIES` already use) + `presence_state` (one row per user: current place/house, the
  real observed "which home" recommendation, and a trigger-dedupe timestamp). Ran locally
  (`node bin/migrate.mjs` -> `applied 057_heading_home.sql`). Real, live collision found and
  resolved: a concurrent sibling build had already taken migration number 056 in the shared
  local DB (`056_tesla_commute_charge_nudge.sql`, uncommitted) — renamed mine to 057 per the
  #3139 collision playbook rather than fighting over the number. `check-schema-drift.mjs`
  confirms every drifted column/table found live is attributable to that concurrent build, none
  to this one.
- **Which-home resolution, investigated and decided** (the issue's own real open question):
  track the real house Shane was last observed leaving (`places.recordPresence`, wired as a side
  effect of the *existing* `/api/places/nearby` foreground check from #3159 — no new geolocation
  call anywhere) as the recommended default, always shown by name with a real one-tap
  alternative to the other house — never a silent guess. Documented as the chosen approach,
  not asked back to Shane, per the issue's own instruction to investigate and recommend.
- `src/core/tesla.mjs` — real vehicle-command sending (`sendHeadingHomeCommands`):
  `navigation_gps_request` + `auto_conditioning_start`, Tesla's real documented Fleet API
  command contract, same bearer-token auth every read in that file already uses.
- `src/core/heading-home.mjs` (new) — the real orchestration: resolve target house -> the real
  place tagged with it -> fire both Tesla commands -> mark the trigger for dedupe.
- Widget (`src/core/widget.mjs`, `src/routes/widget.mjs`) — new `headingHome` Next-card kind,
  idempotent `?headingHome=<house>` tap-through (same shape as the existing `?done=`), real
  10-minute trigger dedupe so a widget refresh can't repeat-fire a real vehicle command.
- Main app (`public/app.js`) — same `headingHome` kind wired into `resolveNextKind`/
  `renderNextCardV3`/`foxMatchLine`, a real "Send it" + "Not this house" button pair POSTing to
  the new session-gated `POST /api/tesla/heading-home`.
- `src/mcp/tools.mjs` — `push_place`'s real `house` param, so Claude can tag "this is house h1"
  conversationally (no form, per Section 3).
- Places Settings section shows a place's real house tag when set.

**Real, honest gap found and filed, not silently worked around**: Tesla now requires most
vehicle commands to be cryptographically signed by a separate command-signing keypair — a real,
distinct protocol from the OAuth bearer token this file already uses for reads. `fleetCommand()`
detects that rejection shape and reports `COMMAND_SIGNING_REQUIRED` honestly rather than a
generic failure. Filed **#3259** (parented under Feature #3237, milestone "Shane", labeled
`bug`, board status "AI Batter Up") documenting the real infra needed (Tesla's own
`tesla-http-proxy` sidecar + a generated signing keypair + a one-time physical vehicle pairing
step) and the real open questions for Shane (where the proxy runs, where the keypair lives).
This is genuine security-sensitive infrastructure, not a same-session addition — reimplementing
Tesla's real signing protocol from scratch risked locking Shane's own real vehicle out of remote
commands.

**Real, hands-on local verification** (server booted on :5099 against the real local database,
the established `agent-verify@example.com` test account, a throwaway script deleted after use):
saved two real house-tagged places + a "Work"-like place via the same `upsertPlace` path
`push_place` uses; called the real `/api/places/nearby` at H1 then at Work and confirmed
`presence_state` correctly recorded `current_house=null, last_house="h1"`; confirmed
`/api/today`'s `headingHome` is honestly `null` (Tesla not connected locally, matching #3158's
own precedent); confirmed `/api/widget-preview` renders clean with the new kind wired in;
confirmed `POST /api/tesla/heading-home` and the widget's `?headingHome=h1` GET both fail
honestly (400 "No Tesla vehicle is selected yet.") rather than crashing or silently succeeding,
live-curled against the real widget route with a real minted widget token. All test rows deleted
afterward (places, presence_state, sessions, widget_tokens) — confirmed zero residue.

**Not locally exercised** (same honest limitation as #3158): real Tesla OAuth/vehicle
connection isn't set up in this local dev environment, so the actual command-send path against
a live vehicle is written-but-unexercised here, same as #3158's own OAuth round trip.

No `git status --porcelain` residue beyond this build's own files; branch verified merged
(`verify-branch-merged.mjs`).
