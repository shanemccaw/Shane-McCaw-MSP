# #4315 — Kanban card's linked runbook has no click-through target in MyArchitect

- **Status:** ✅ DONE 2026-09-16T00:45:00Z
- **Scope:** both (client: shanemccaw/MyArchitect; backend: none needed — GET /api/admin/ps-scripts/:id already exists)
- **Started:** 2026-09-16
- **Commit(s):** MyArchitect `03ed54f` (feature), MyArchitect `69fd3eb` (its own bookend). No
  Shane-McCaw-MSP code changed — backend endpoint already existed.

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — Investigated both options in the issue body. `GET /api/admin/ps-scripts/:id`
  (artifacts/api-server/src/routes/admin-ps-scripts.ts:1020) already returns a full `powershell_scripts` row by its
  real UUID — no new backend endpoint needed. `requireAdmin` gates it (role === "admin"), which every MSP-staff
  MyArchitect login already carries (#3501's auth contract: `role("admin"|"client")`), so it's reachable today.
  Smaller real lift is option 2: a new client service + a new sibling record opener in MyArchitect, not a
  scriptId(UUID)->LaunchControlAction(int) mapping (there is no real relationship between those id spaces).
  Blocked on applying/committing: `C:\Source\ShaneMcCawConsulting\MyArchitect-clone` (the shared, non-worktree-isolated
  MyArchitect checkout) is mid an unresolved `git merge` (MERGE_HEAD present, conflicts in
  Controls/HomeDashboardView.xaml.cs, MainWindow.xaml.cs, build-journal/4341.md) owned by another concurrent
  BuildConsole session — not mine to resolve. Prepared the new client files
  (Models/PowerShellScriptRecord.cs, Services/IPowerShellScriptService.cs, Services/PowerShellScriptService.cs) and
  the exact MainWindow.xaml.cs edit points, waiting for that merge to clear before committing.
- 2026-09-16T00:45:00Z ✅ DONE — The shared checkout's merge cleared on its own (resolved by its owning session);
  my untracked new files had been wiped by that merge resolution, so recreated them. Added
  `Services/IPowerShellScriptService.cs` + `Services/PowerShellScriptService.cs` (real HTTP client for
  `GET /api/admin/ps-scripts/:id`) and `Models/PowerShellScriptRecord.cs`. Added `OpenPowerShellScriptRecordAsync`
  in `MainWindow.xaml.cs` (new sibling record opener, modeled on `OpenSupportTicketRecordAsync`) and wired it as a
  real "Open in Script Library" `WorkspaceAction` on `OpenKanbanCardRecord`'s linked-runbook section — replacing
  the read-only text display the issue was filed against. No fixture/hardcoded data. `dotnet build
  MyArchitect.csproj -c Debug` — 0 warnings, 0 errors. Pushed to MyArchitect `origin/main` as `03ed54f`; MyArchitect
  repo's own bookend (build-journal/4315.md there) is `69fd3eb`, confirmed `git merge-base --is-ancestor` true
  against MyArchitect's `origin/main`. This worktree (`agent/4315-q3018`) itself carries no code changes — the
  fix is entirely a real, existing backend endpoint plus new MyArchitect client code — and
  `verify-branch-merged.mjs` confirms it's merged into `origin/main`. `git status --porcelain` clean.
