# #2331 — Every note stamps screen, feature, and the build number running at that moment

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 3537b8326

## Log
- 2026-09-02T19:42:30Z ⏳ IN FLIGHT — Sub-issue of #2326 (Feature: Test Pad), item 5
  of 28. Confirmed Test Pad is still greenfield in this worktree: only
  `Services/TestPad/NoteType.cs` and `NoteMarkerParser.cs` exist (from #2329). Siblings
  #2327 (pill/pad shell + `TestPadService`/`TestPadNote`) and #2328 (composer, blocked
  on #2327) are/were concurrent on the same new scaffold files — building the actual
  note-filing pipeline here would collide. Following the same decoupled-piece pattern
  #2329 used: adding a pure, self-contained `NoteStamp` value + `NoteContextStamper`
  service under `Services/TestPad/` that captures screen/feature/running-build-number
  via injected resolver delegates (no dependency on `MainWindow`'s private `TabDef`/
  `_activeTabId` or on `QueueReadClient` internals), for the Composer/pill to wire real
  resolvers into once it lands.
- 2026-09-02T20:05:00Z ✅ DONE — While in progress, #2327 (pill/pad shell) landed on
  `main` and its `TestPadNote` model (`Services/TestPadModels.cs`) already scaffolded
  nullable `Screen`/`Feature`/`BuildNumber` fields explicitly for this issue to
  populate, and `TestPadService.AddNote` (`Services/TestPadService.cs`) is the one real
  funnel every note (composer, import, ...) will pass through. Built
  `Services/TestPad/NoteStamp.cs` (immutable stamp record + single-line `Format()`)
  and `Services/TestPad/NoteContextStamper.cs` (resolver-delegate-based capture, with a
  static `Current`/`Configure(...)` so the Composer/pill can wire in the real
  screen/feature/running-build lookups once they exist — kept decoupled from
  `MainWindow`'s private `TabDef`/`_activeTabId` and from `QueueReadClient` to avoid
  colliding with concurrent Test Pad sibling builds touching those). Wired
  `TestPadService.AddNote` to call `StampIfUnset` so every note that ever goes through
  the real funnel is auto-stamped at the moment it's filed, without the caller having
  to remember to — a caller that pre-sets a field (e.g. import re-filing an edited
  note) keeps its own value rather than being overwritten. Until a resolver is
  configured, notes stamp with `null`s (documented, no-op default) rather than
  throwing. `dotnet build ShaneBuilder.csproj` succeeds, 0 warnings/0 errors, both
  before and after wiring into `TestPadService`. Commit 3537b8326, pushed to `main`,
  verified merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
