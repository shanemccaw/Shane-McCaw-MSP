# #1580 — My Architect: honest-read audit — current wiring, status_reports vs work_log, documents backing store

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** d966b012a (bookend IN FLIGHT), da6f31001 (merge to main)

## Log
- 2026-08-28 ⏳ IN FLIGHT — READ-ONLY audit per #1580 (part of #1569). Answering all 5
  numbered items against live code (`portal-retainer.ts`, `useRetainerLive.ts`,
  `portal-v2-retainer.tsx`, `retainerModel.ts`, `retainerData.ts`, `admin-retainer.ts`,
  `retainer-work-logger.ts`, `msp.ts`/`index.ts` schema) and a direct query against
  local Postgres (`retainer_settings`, `retainer_work_log`, `status_reports`,
  `client_documents`). No code changes — findings posted as a comment on #1580.
- 2026-08-28 ✅ DONE — Audit complete, no code changes (read-only per scope). Real,
  live-queried findings: all four relevant tables (`retainer_settings`,
  `retainer_work_log`, `status_reports`, `client_documents`) exist in local Postgres
  but every one is genuinely empty (0 rows) — so in local dev every customer is
  "unconfigured," never "empty," and no test-manifest step has ever exercised the
  live/empty branches. `status_reports` has no FK to `retainer_work_log` — it is a
  fully independent authored artifact, not a view over the log. `client_documents`
  is a real, separate document backing store, but it is wired only into the admin
  Document Hub and the customer privacy/export route — never into My Architect's
  Documents rail, which derives synthetic document entries from sent
  `status_reports` rows only. Two live fixture leaks found beyond what #1401/#1407
  already fixed: the two SmallCards (`RET_COPY.findingsClosedValue/Sub`,
  `nextScheduledValue/Sub`, `portal-v2-retainer.tsx:388-389,394-395`, already known
  from #1398's follow-up) and a NEW one — `retainerModel.ts:168`'s `askWho()`
  hardcodes "Priya Raman" as the "them" sender label in a real live report's
  Q&A thread, regardless of the customer's actual `retainer_settings.architectName`.
  Full findings posted as a comment on #1580. Branch merged to `origin/main` as
  `da6f31001`, confirmed via `verify-branch-merged.mjs` before writing DONE.
