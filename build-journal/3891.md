# #3891 — Testimonials: standing customer submission surface — table + endpoint (phase 1 of 4 — API)

- **Status:** ✅ DONE 2026-09-13T01:45:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 2389eff7a

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — new `customer_testimonials` table + manual migration, `POST/GET /api/portal/testimonials`, extend `admin-testimonials.ts` to list both sources.
- 2026-09-13T01:45:00Z ✅ DONE — Added `customerTestimonialsTable` (`lib/db/src/schema/index.ts`), ran
  `lib/db/migrations/manual/2026-09-13-customer-testimonials-3891.sql` against local Postgres (real
  `CREATE TABLE`/`CREATE INDEX`, verified with a live insert/select/delete round-trip). Added
  `POST /api/portal/testimonials` and `GET /api/portal/testimonials` (own history) in
  `artifacts/api-server/src/routes/portal-testimonials.ts`, gated by `requireCapability("ladder.customer-user")`,
  registered in `routes/index.ts`. Extended `admin-testimonials.ts` with
  `GET /api/admin/testimonials/all`, merging `project_closures` (existing, unchanged) and
  `customer_testimonials` (new) into one admin view, tagged by `source`. `tsc --noEmit` clean for
  all touched files (one pre-existing unrelated error in `auth-impersonation.test.ts`, confirmed via
  `git show` predates this session). Branch verified an ancestor of `origin/main` via
  `verify-branch-merged.mjs`/`git merge-base --is-ancestor`; `git status --porcelain` clean.
  Commit `2389eff7a`.
