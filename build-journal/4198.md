# #4198 — WPF native Element Picker (Ctrl+Shift+W)

- **Status:** ✅ DONE 2026-09-15T01:38:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** f7cd037c1 (5cdb2ba20 after rebase onto origin/main)

## Log
- 2026-09-15T01:35:11Z ⏳ IN FLIGHT — audited the real Ctrl+Shift+T hotkey mechanism
  (`MainWindow.xaml.cs` `Window_PreviewKeyDown`) and `WpfTesterWindow`'s
  `TypeName [x:Name]` naming convention (`BuildTreeItem`/`InspectElement`); implementing
  Ctrl+Shift+W element picker mode with a non-modal `VisualTreeHelper.HitTest`-driven
  highlight overlay, mirroring both patterns.
- 2026-09-15T01:38:00Z ✅ DONE — shipped `Controls/ElementPickerLayer.xaml(.cs)` (a
  non-interactive, `IsHitTestVisible="False"` Canvas overlay modeled directly on
  `SailorDuckMascotLayer`'s existing overlay pattern, drawing a highlight border +
  identifier label) and `MainWindow.ElementPicker.cs` (toggle/enter/exit state,
  `VisualTreeHelper.HitTest`-driven hover tracking via new `Window_PreviewMouseMove`,
  click-to-copy via new `Window_PreviewMouseLeftButtonDown_ElementPicker`, and
  `GetElementIdentifier` reusing WpfTesterWindow's exact `TypeName [x:Name]` convention
  with a short ancestor-path fallback, e.g. `Grid > StackPanel > TextBlock`, for unnamed
  elements). Wired Ctrl+Shift+W into `Window_PreviewKeyDown` using the same chord-check
  pattern as the existing Ctrl+Shift+T block, plus an Esc-cancels-picker-mode check
  checked first (ahead of all other Escape handling) so it always wins while picker
  mode is active. Added a "View → Element Picker (Ctrl+Shift+W)" menu item next to the
  existing "Toggle Test Mode" entry for discoverability, matching the same
  `InputGestureText` pattern. Scoped to the active Window (v1), per the issue's stated
  scope — no low-level mouse hook / `Application.Current.Windows` extension attempted,
  since MainWindow already covers the overwhelming majority of real UI (all docked
  panels + document tabs) and the issue explicitly says not to let further scope block
  landing this case.
  **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`
  — Build succeeded, 0 Warning(s), 0 Error(s) (run both before and after the
  origin/main rebase, which applied with no conflicts, so the built output is
  unchanged). No live/manual interactive run of the built exe was performed in this
  session — verification is the clean Release build plus a full code-level re-read of
  every changed file; interactive hover/click/Esc behavior should be spot-checked by
  Shane on a real running instance per the issue's "Real verification" checklist.
  `node scripts/dev-server/verify-branch-merged.mjs` confirmed `agent/4198-q2842`
  (5cdb2ba2) is a real ancestor of `origin/main`. `git status --porcelain` clean at
  finish. No findings filed — no genuine bug/gap surfaced during this build.
