# #4301 — MyArchitect: ribbon dropdown lists (breaches/escalations/compliance/etc.) → right panel

- **Status:** ✅ DONE 2026-09-15T23:42:58Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** `f009c3f` on `shanemccaw/MyArchitect` main (`Watch tab list surfaces open in right panel, not ribbon dropdown popups`)

## Log
- 2026-09-15T23:36:40Z ⏳ IN FLIGHT — Blockers #4278/#4284 both cleared (#4278 closed not_planned, superseded by #4305 which landed complete; #4284 closed complete after #4305). Confirmed via `gh api .../4301/dependencies/blocked_by`. Cloned `shanemccaw/MyArchitect` (private, confirmed). Read UI_RULES.md — "Compliance" resolves unambiguously to the Watch tab's SLA group "Compliance" gallery (GET /api/msp/sla/compliance), sitting right next to "Breaches"/"Escalations" — an exact match to Shane's named examples, no ambiguity to ask about. Found existing precedent in the same codebase (`OpenSupportTicketsListAsync`, `OpenBreakGlassPendingListAsync`) for the exact right-panel-list pattern this issue wants applied to the Watch tab's gallery-dropdown commands. Converting SLA Breaches/Escalations/Compliance/Policies/M365 Uptime, Task Queue, and Alerts from `Gallery` dropdown popups to `OnSelect` opening `RecordWorkspaceSpec.List` in the right panel.
- 2026-09-15T23:42:58Z ✅ DONE — Converted all 7 Watch-tab gallery-dropdown commands (SLA Breaches/Escalations/Compliance/Policies/M365 Uptime, Task Queue's Open Tasks, Alerts) in `MainWindow.xaml.cs` from `RibbonCommandSpec.Gallery` (Fluent.Ribbon `DropDownButton` popup) to `OnSelect` opening a `RecordWorkspaceSpec.List` in the shell's right panel (UI_RULES.md §3), matching the already-proven `OpenSupportTicketsListAsync`/`OpenBreakGlassPendingListAsync` pattern in the same file. Same real data (unchanged service calls), same click-through-to-record behavior on each row, just full right-panel space instead of a cramped dropdown. Left the other tabs' galleries (Script Library, Runbooks, Document Hub, the Remediation Plan browser) untouched — UI_RULES.md §4 explicitly keeps those as galleries, and the issue named only the Watch-tab list surfaces. Updated two stale doc-comments (`RegisterWatchTab` header, the Alerts section banner) that described the old gallery mechanism. `dotnet build` clean (0 warnings/0 errors), rebuilt again after two concurrent-session rebases onto `origin/main`. Live-verify: launched `MyArchitect.exe` — no startup crash, ran clean for 6s before being stopped. Did not click through the Watch tab dropdowns live (requires an authenticated tenant session this build session doesn't have) — that's Shane's own visual/interaction confirmation to make, same honest limit #4284 stated for its own final glare/readability judgment. No new findings — nothing broken discovered mid-build. Pushed as `f009c3f` to `shanemccaw/MyArchitect` main after rebasing twice past concurrent sessions' pushes (`2e6c29d`, `103524a`).
