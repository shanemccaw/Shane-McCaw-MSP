# Git #2905 — Feature: Income sources + pay history tracking (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T00:45:00Z
- **Scope:** buildconsole (desktop/ShanesSurvival)

## Log
- 2026-09-04T00:00:00Z — Session started. Sub-issue of #2880. Adding income_sources +
  income_entries tables, PayHistory Core repository, and 3 new MCP tools
  (set_income_source, record_income, get_income_history) on ShanesSurvival.Mcp.
- 2026-09-04T00:45:00Z — ✅ DONE — Added `migrations/005_income_tracking.sql`
  (income_sources, income_entries), `ShanesSurvival.Core/Income/{IncomeModels,IncomeRepository}.cs`,
  `ShanesSurvival.Mcp/Tools/IncomeTools.cs` (set_income_source, record_income,
  get_income_history), and registered `IncomeRepository` in `ShanesSurvival.Mcp/Program.cs`.
  Ran the migration myself against the local `finances` Postgres DB
  (`Host=localhost;Port=5432;Database=finances`) and recorded it in `schema_migrations`.
  Real E2E verification via a scratch console harness against `IncomeRepository` directly
  (real DB, not mocked): upserted "NASA Salary" (biweekly, $4,343.63/cycle) and "Ronnie Uber"
  (irregular), recorded 3 real entries, confirmed `next_pay_date` correctly advanced by
  `pay_frequency_days` for the biweekly source and was left untouched for the irregular one,
  confirmed `get_income_history` returns most-recent-first both unfiltered and filtered by
  source name, confirmed a null-argument upsert preserves existing field values rather than
  clearing them, confirmed an unmatched source name fails `record_income` cleanly. Found and
  fixed 2 real bugs during that verification: (1) `RETURNING created_at` via ExecuteScalarAsync
  threw a DateTime→DateTimeOffset cast error — fixed by reading via ExecuteReaderAsync +
  GetFieldValue<DateTimeOffset> like the rest of the codebase; (2) a null `sourceName` filter
  parameter passed via AddWithValue(DBNull.Value) hit Postgres error 42P08 ("could not
  determine data type of parameter") — fixed by adding it as an explicit NpgsqlDbType.Text
  parameter. Test data cleared from the DB after verification; scratch harness deleted, not
  committed. `dotnet build ShanesSurvival.sln` clean, 0 warnings/errors.
  Commit: f4cdcac3a0a0a00a9abbdc8706af168b9ae7ec28.
  Explicitly out of scope (per issue #2905): wiring #2904's `pay_period_due_status` to pull
  `nextPayDate` automatically from a real income source — noted, not built.
