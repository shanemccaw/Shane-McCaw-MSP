# #3583 — Multi-repo: new per-repo pause control, independent of the existing global Active/Pause toggle

- **Status:** 🛑 BLOCKED 2026-09-11T00:50:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** none — no code changed

## Log
- 2026-09-11T00:50:00Z ⏳ IN FLIGHT — pulled #3583's full body + comments, re-checked its stated blockers #3579/#3581 per dispatch instructions before starting.
- 2026-09-11T00:50:00Z 🛑 BLOCKED — #3579 (real `(repo_owner, repo_name)` dimension on `bt_issue_mirror`/`bt_build_queue`) and #3581 (real Settings repo registry) are both still **OPEN** and labeled `in-flight` (not `complete`). Confirmed no code has landed for either: no `repo_owner`/`repo_name` column in any file under `lib/db/migrations/manual/`, and no `RepoRegistry`/`RepoTier`/repo-registry Settings UI anywhere under `desktop/BuildConsole`. #3583's own per-repo pause logic has nothing real to key off yet (no repo dimension on the queue rows, no known repo list/tier to build a toggle against). The `blocked_by` edges to #3579 and #3581 already existed on #3583 (verified via `gh api .../issues/3583/dependencies/blocked_by`) — left in place since they reflect real, current blockers, not stale ones. Added `blocked` label, removed `in-flight`. No code changes made; nothing to typecheck/build/test. Stopping per the blocked_by-edge discipline rather than guessing forward against schema/UI that doesn't exist yet.
