# #3583 — Multi-repo: new per-repo pause control, independent of the existing global Active/Pause toggle

- **Status:** ✅ DONE 2026-09-11T01:32:19Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 13c04a00908c020ddff1ad777e292deb3a44efb6

## Log
- 2026-09-11T00:50:00Z 🛑 BLOCKED — see prior log entry below; #3579/#3581 not yet landed.
- 2026-09-11T01:17:51Z ⏳ IN FLIGHT — re-checked #3579/#3581 again: both are now genuinely landed on `origin/main` (verified via real code, not the stale `in-flight` label still sitting on #3579's issue at the time of this check — `git merge-base --is-ancestor` confirms commit `677dda0a1` (#3579's migration + call-site audit) and `533986cb7` (#3581's Settings repo registry) are both ancestors of `origin/main`, and both have `✅ DONE` bookends). Rebased this worktree onto current `origin/main` to pick up both. Removed the now-stale `blocked_by` edges on #3583 pointing at #3579/#3581 and flipped labels `blocked` → `in-flight`. Resuming real work now.
- 2026-09-11T01:32:19Z ✅ DONE — real per-repo pause, distinct from the existing global Active/Pause
  toggle (commit `13c04a009`):
  - `RepoRegistryEntry.IsPaused` — a real flag on #3581's Settings repo registry entry.
  - `BuildConsoleSettings.IsRepoPaused`/`SetRepoPaused` — real query/mutate surface, persisted the
    same way as every other repo field, logs the transition on the `watcher` channel.
  - `QueueItem.RepoOwner`/`RepoName`/`OwnerRepo` — threaded from `bt_build_queue`'s real
    `repo_owner`/`repo_name` columns (#3579's migration) as NEW OPTIONAL trailing `MapRow` ordinals
    (22/23, right after `SupersededById`'s 21) — same #1384 fixed-ordinal discipline; only the two
    queries that need it (`SelectClaimCandidatesAsync`'s fetch + `GetNextAsync`'s claim RETURNING)
    select them, so every other existing SELECT against `bt_build_queue` is unaffected.
  - `BuildQueuePostgresClient.SelectClaimCandidatesAsync` — the shared claim path both
    `GetNextAsync` (claims) and `PeekNextAsync` (read-only preview) run — now holds any candidate
    whose repo is paused, with its own distinct held reason, before the exclusive-build-set/blocker
    checks. Real, honest interaction with global pause verified by design: global pause is enforced
    entirely separately in `TickAsync` (it skips `GetNextAsync` outright while paused, never
    reaching this method at all), so the two can never double-negate — a repo-paused item is simply
    never reached by the global check, and a non-repo-paused item is still held by global pause
    exactly as before.
  - Settings > Repos tab: a real Pause/Resume toggle button + PAUSED badge on each repo row
    (`BtnToggleRepoPause_Click`) — the real control surface in place of #3582's not-yet-landed
    Batter Up repo badge/grouping (per the issue body's own "or a new small per-repo control").

  **Verification (real, not simulated):** `dotnet build BuildConsole.csproj` — clean, 0 errors.
  Live end-to-end round trip against the real local Postgres DB: an isolated `--instance
  3583-verify-scratch` settings dir (never touching the real running BuildConsole's own
  settings.json) called the real `BuildQueuePostgresClient.PeekNextAsync` (structurally read-only —
  no `UPDATE` anywhere in its path) against a real existing queued row (#2252, repo
  `shanemccaw/Shane-McCaw-MSP`) three times: ready with the repo unpaused → held (0 ready) with it
  paused → ready again on resume. PASS.

  **A real, live mistake caught and corrected during verification, worth recording honestly:** the
  first verification attempt inserted two synthetic `status='queued'` rows directly into
  `bt_build_queue` to test the claim path end-to-end. A real BuildConsole watcher instance actively
  running against this same local DB (evidently launched by a concurrent session/build on this
  machine) race-claimed BOTH rows to `status='running'` within seconds — a live consequence of
  `bt_build_queue` being a genuinely shared, actively-polled table, not a private test fixture.
  Caught immediately (`build_pid` was still `NULL` on both — no real process had actually spawned
  yet), canceled and deleted both rows before any `claude.exe` launched, and confirmed via
  `tasklist` that the three real `claude.exe` processes running on the machine at the time were
  pre-existing, unrelated builds. Switched the verification approach entirely to the read-only
  `PeekNextAsync` path against real rows instead (see above) — genuinely safe, since it issues no
  `UPDATE` at all. No real build was disrupted; DB left clean (confirmed 0 leftover `ZZ-TEST-3583*`
  rows). Noting this plainly rather than omitting it, per this repo's own "report outcomes
  faithfully" standard.

  No findings filed — no genuine bug/gap surfaced beyond what #3579/#3581/#3582/#3584 already
  document as their own explicit scope (e.g. #3582's Batter Up repo grouping, #3584's dispatch/
  worktree provisioning threading the real per-item repo through — neither blocks this issue's own
  ask). `git status --porcelain` clean; branch confirmed merged into `origin/main`
  (`verify-branch-merged.mjs` OK).

## Prior blocked entry (superseded)
- 2026-09-11T00:50:00Z ⏳ IN FLIGHT — pulled #3583's full body + comments, re-checked its stated blockers #3579/#3581 per dispatch instructions before starting.
- 2026-09-11T00:50:00Z 🛑 BLOCKED — #3579 and #3581 were both still OPEN and not yet showing landed code at that check. See above — since resolved.
