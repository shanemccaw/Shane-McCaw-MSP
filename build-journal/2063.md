# #2063 — Build Queue Panel: route no-BUILD-comment dispatch to active chat

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** `503dc3cdb` (feature) + `715590efb` (IN FLIGHT bookend) — both verified ancestors of `origin/main`

## Log
- 2026-08-31 ⏳ IN FLIGHT — Audited #2059 (FloatingChatBridgeScript send+submit), #2061 (Git Board
  hover popover, confirmed `build == null` case shows nothing today), and #2072 (still OPEN,
  no bookend, no landed commit — real unresolved trust issue in the RECEIVE/capture side of the
  #2059 bridge). Found the literal "No build prompt found on #N yet" dead-end at
  `Controls/DispatchPanel.xaml.cs:184`. Proceeding to wire it (and the Git Board hover popover's
  equivalent silent dead-end) to ask the currently-active chat to write and post the `BUILD:`
  comment, reusing #2059's existing send+submit scripts — not the receive/capture path #2072
  concerns, see reasoning below.

- 2026-08-31 ✅ DONE — commit `503dc3cdb` on `main`.

  **#2072's effect on this build (explicit, as required):** re-checked #2072 immediately before
  writing this DONE — still `OPEN`, still labeled `in-flight`, no bookend file, no commit on
  `main` or on `origin/agent/2072-q1063` beyond its own IN FLIGHT bookend. It has **not** landed.
  Proceeded anyway, deliberately, because #2072's diagnosed bug is in the RECEIVE/capture half of
  the #2059 bridge (`FloatingChatBridgeScript.CaptureScript` / `OnBridgeMessage` — polling
  `.font-claude-message` / `[data-testid="assistant-message"]` / `[aria-posinset]` to scrape the
  *latest assistant reply* back into the floaty's response pane) — a real DOM-scrape bug where a
  stale prior turn can render instead of the fresh one. This build never reads or trusts that
  scraped reply at all. It only needs the SEND+SUBMIT half (`BuildInsertScript` /
  `SubmitScript`), which is a different mechanism with its own independent DOM-based
  confirmation: `SubmitScript` compares the composer's text before/after clicking Send (or
  dispatching Enter) and returns `'sent'` only when the composer actually emptied, `'inserted-no-send'`
  when it didn't. That status is what both new call sites report back to Shane verbatim (via
  `ActiveChatBuildRequestHelper.DescribeStatus`) — never a fabricated "success". So this
  proceeded as the "lower-trust fire-and-hope" option the issue offered, but it is honestly
  fire-and-hope only on the pre-existing risk the #2059 SEND path already carries (shared with
  Sticky Notes/SQL Runner's insert, now also auto-submitting) — not on the specific bug #2072 is
  chasing, which lives in a code path this feature doesn't touch.

  **What shipped:**
  - `FloatingChatWindow.xaml.cs` — refactored `SendAsync`'s insert+submit into a shared private
    `InsertAndSubmitAsync(tab, text)`, and added a public `SendToActiveTabAsync(text)` for a
    programmatic (non-user-typed) send+submit into whatever tab is currently active.
  - `MainWindow.FloatingChat.cs` — new `SendToActiveChatAsync(text)`: floaty's active tab if the
    floaty window is open (`_floatingChatWindow != null && IsLoaded`, the one existing "active
    chat" tracker in the app), else falls back to the main editor area's active Claude.ai tab via
    the existing `GetActiveEditorTabWebView()` + the same #2059 scripts. No new active-chat
    tracking mechanism invented — reused what already exists, per the issue's own instruction.
  - `Services/ActiveChatBuildRequestHelper.cs` — new, small: the one ask-message prompt text and
    the one status→human-message mapping, shared by both call sites below so they report the
    same real outcome instead of drifting into two wordings.
  - `Controls/DispatchPanel.xaml.cs` — the literal "No build prompt found on #N yet" dead-end
    (was line 184) now asks the active chat instead of just erroring, and reports the real
    send/submit status back in the status bar.
  - `Controls/LeftSidebar.xaml.cs` — the Git Board hover popover's parallel dead-end (`build ==
    null` in `BuildIssueHoverPopupContent` used to render literally nothing beyond the
    title/snippet) now offers a "⚡ Dispatch (asks active chat if no BUILD: yet)" quick action:
    `DispatchOrAskActiveChatAsync` mirrors `DispatchPanel.DispatchAsync`'s own
    fetch-comment→dedupe→blocked-by→`QueueBuildAsync` path (not a second parser/queue
    implementation) when a `BUILD:` comment already exists, and falls to the same
    ask-active-chat path when it doesn't. Result reported via `ToastEngine`, matching this
    popover's existing Dispatch-Now/Cancel/Retry/Reply convention (#2061).

  **Verification:** `dotnet build BuildConsole.csproj` clean (0 warnings/errors), twice — once
  before rebasing onto `origin/main` (which picked up #2074's unrelated `MainWindow.FloatingChat.cs`
  edit removing `Owner = this`, applied cleanly with no conflict) and once after. Did not launch a
  live BuildConsole.exe to click through the actual UI flow (would connect to Shane's real GitHub
  PAT, local Postgres, and active queue watcher, and could interfere with his own running
  instance); `desktop/BuildConsole` is explicitly out of scope for test manifests per CLAUDE.md,
  so none was written.

  No findings filed — no stale docs, no broken tests, no unused endpoints hit during this audit.

  Working tree clean (`git status --porcelain` empty) after the final push. Branch verified
  merged via `verify-branch-merged.mjs`.
