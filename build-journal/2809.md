# #2809 — Git Doctor should be a full-width Editor tab, not a narrow ActivityBar sidebar panel

- **Status:** ✅ DONE 2026-09-04T04:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 4650cceeb (real work), 7e57af1e (merge into main)

## Log
- 2026-09-04T04:15:00Z ⏳ IN FLIGHT — rehost GitDoctorView from LeftSidebar ActivityBar panel (#2798) into a full-width EditorTabs tab, mirroring `OpenLogViewerTab()`/`OpenSqlRunnerTab()`; rewire `BtnGitDoctor` to a plain button that opens the tab, remove the ActivityBar/SwitchView wiring.
- 2026-09-04T04:40:00Z ✅ DONE — Added `MainWindow.OpenGitDoctorTab()` mirroring `OpenLogViewerTab()`'s exact pattern (dedup by `Tag == "git_doctor"`, header icon+title+close, `AttachTabContextMenu`/`AttachTabDragHandlers`, `EditorTabs.Items.Add`, `EnsureLoaded()` on first open) hosting the same real `GitDoctorView` control from #2798 with no content changes. Changed `BtnGitDoctor` from a `RadioButton` in the `ActivityBar` workspace group to a plain `Button` (same shape as `BtnStickyNotes`), raising a new `GitDoctorRequested` event MainWindow wires to `OpenGitDoctorTab()`. Removed the LeftSidebar `<controls:GitDoctorView>` hosting and all `SwitchView("GitDoctor")` branches #2798 added (visibility toggle, header title, New-button tooltip, `EnsureLoaded()` call) — GitDoctorView now lives only in the tab. Removed `ActivityBar.SelectGitDoctor()` (dead now that there's no RadioButton to check) and `MainWindow.OpenGitDoctorView()`; updated the chat tool-rail mini panel's Maximize handler (#2799, `ChatDocumentContainer.RailMaximize_Click`) to call `_owner.OpenGitDoctorTab()` directly, same as the "logs" case right above it. Verification: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 Warnings, 0 Errors. Diff matches the issue's 4-point plan exactly (6 files: ActivityBar.xaml/.cs, ChatDocumentContainer.xaml.cs, LeftSidebar.xaml/.cs, MainWindow.xaml.cs). Merged onto `main` at 7e57af1e (merge origin/main → agent/2809-q1591, then pushed HEAD:main), confirmed via `verify-branch-merged.mjs` — OK. Working tree clean.
