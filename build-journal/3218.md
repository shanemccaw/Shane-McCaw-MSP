# #3218 — Feature: Dedicated Tesla room — vehicle automation, starting with checkout-to-trunk (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T18:55:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 2cd51b868

## Log
- 2026-09-08T18:35:00Z ⏳ IN FLIGHT — Pulled #3218's full body. Confirmed the stated blocker
  (#3158) is genuinely unblocked: #3158 shipped real code (commit 81217a61b, DONE bookend) and
  now carries the `complete` label (not `in-flight`), which per the blocked-by resume convention
  means this build proceeds. #3158 deliberately did NOT build vehicle *command* capability
  (read-only `vehicle_device_data` OAuth only) — that's explicitly this issue's job.
  Real scope for this session: the one concrete first automation Shane named — checkout-to-trunk.
  Found the real "Done shopping" completion moment: `POST /api/lists/:id/clear-checked`
  (lists.mjs `clearCheckedItems`, wired in routes/api.mjs). Plan: add real vehicle-command-send
  capability to tesla.mjs (via Tesla's own documented vehicle-command signing proxy, config-driven
  `TESLA_COMMAND_PROXY_URL` — not reimplementing Tesla's binary signing protocol in this app,
  which would mean re-deriving a security-critical protocol Tesla ships as an official Go SDK
  specifically so third parties don't have to), a real `tesla_scheduled_commands` table + sweep
  (mirrors the existing 5-min snoozed-nudge redelivery interval already in server.mjs) for the
  5-minute countdown, a per-user opt-in toggle (defaults off — a real car door/trunk actuator is
  not something to fire automatically without Shane explicitly turning it on), and Settings UI to
  enable/disable + see/cancel a pending scheduled command.
  Migration number confirmed live: `node bin/next-migration-number.mjs` -> **056**.
- 2026-09-08T18:55:00Z ✅ DONE — real code landed, real-verified against the live local database
  (no local TESLA_CLIENT_ID/SECRET/TESLA_COMMAND_PROXY_URL exist yet, so the real Tesla-side
  round trip is unexercised -- honest gap below, matches #3158's own precedent).
  **What shipped** (commit 2cd51b868):
  - `migrations/058_tesla_vehicle_commands.sql` (renamed up from 056→057→058 live, twice, as two
    concurrent sibling sessions -- #3216 and another Tesla build -- took 056 and 057 first;
    `next-migration-number.mjs` caught both collisions correctly) -- `tesla_scheduled_commands`
    (one real row per scheduled command, durable so a 5-minute countdown survives a Replit
    redeploy) + `tesla_accounts.auto_trunk_on_checkout` (real per-user opt-in, defaults OFF --
    this fires a genuine physical trunk actuator on Shane's actual car).
  - `src/core/tesla.mjs` — real `sendVehicleCommand`, `scheduleCheckoutTrunkOpen`,
    `listScheduledCommands`, `cancelScheduledCommand`, `dispatchDueCommands`,
    `setAutoTrunkOnCheckout`, `teslaCommandsConfigured`. Deliberate architecture call: this app
    never signs a command itself or reads TESLA_PRIVATE_KEY — modern Teslas (2021+) reject
    unsigned Fleet API commands outright (HTTP 412), and the signing protocol is Tesla's own
    official local proxy (github.com/teslamotors/vehicle-command), shipped specifically so third
    parties don't reimplement a security-critical binary protocol against a real car. Commands
    forward to `TESLA_COMMAND_PROXY_URL` instead. Also added `vehicle_cmds` to the real OAuth
    scope (055's original scope was read-only) -- anyone already connected needs to
    Disconnect/Connect again in Settings before a command will work; documented in
    `.env.example` and `config.mjs`.
  - `src/routes/api.mjs` — `PATCH /api/tesla/auto-trunk-on-checkout`,
    `GET/DELETE /api/tesla/scheduled-commands[/:id]`. Wired the real "Done shopping" trigger:
    `POST /api/lists/:id/clear-checked` now calls `scheduleCheckoutTrunkOpen` after the real
    clear, non-fatally (a shopping list clear must never fail because an unrelated automation
    isn't configured).
  - `src/server.mjs` — a real 5-minute sweep (`dispatchDueCommands`), same cadence as the
    existing snoozed-nudge redelivery interval, so a scheduled command actually fires.
  - `public/app.js` — real Settings → Tesla UI: "Checkout-to-trunk" card, the opt-in toggle, and
    a real pending-countdown/cancel list (`inShort()` added -- `agoShort` is past-only).
  **Real, hands-on verification, live against the real local database** (not simulated): ran the
  migration (`node bin/migrate.mjs` -> applied 058, `check-schema-drift.mjs` clean for my own
  change — the drift it reports is entirely two concurrent sibling builds' unmerged migrations,
  not mine). Then, in a one-off script (deleted after, no residue): generated an in-process
  vault key + a local mock HTTP server standing in for the real Tesla command proxy, ran the
  real `saveTokens`/`selectVehicle`/`setAutoTrunkOnCheckout`/`scheduleCheckoutTrunkOpen` path
  end-to-end, force-matured the schedule, called the real `dispatchDueCommands` sweep, and
  confirmed the mock proxy received the exact real request shape
  (`POST /api/1/vehicles/999888777/command/actuate_trunk`, `Authorization: Bearer <token>`) and
  the row flipped to `status='sent'`. Also confirmed the real guard paths: `teslaCommandsConfigured()`
  correctly reports `false` locally (no `TESLA_COMMAND_PROXY_URL` set), `scheduleCheckoutTrunkOpen`
  is a real no-op (`null`) when not configured, `setAutoTrunkOnCheckout` refuses with a real error
  when no vehicle is selected, and the cancel path flips a row to `canceled`. Cleaned up every
  test row/connection created afterward (matches #3158's own cleanup convention).
  **Honest gap, stated plainly** (same shape as #3158's own honest gap): the real command actually
  reaching Shane's car is UNEXERCISED — needs three real things only Shane can do: (1) real
  `TESLA_CLIENT_ID`/`TESLA_CLIENT_SECRET` locally/on Replit (same gap #3158 already named,
  still open), (2) run Tesla's own official `vehicle-command` local proxy with the real enrolled
  private key and set `TESLA_COMMAND_PROXY_URL` to it, (3) Disconnect/Connect again in Settings →
  Tesla so the new `vehicle_cmds` scope is actually granted. Real next action for Shane, in order:
  set the Tesla env vars, install/run `github.com/teslamotors/vehicle-command`'s proxy (its README
  covers enrolling the key via the Tesla app against the vehicle already paired for #3158), point
  `TESLA_COMMAND_PROXY_URL` at it, reconnect. No findings filed — the real gap here is a genuine
  Shane-only physical/account setup step (same class as #3158's own), not a code defect; the
  dedicated Tesla-room UI and further automations (Sentry Mode, scheduled charging) remain the
  issue's own stated "open scope, add as Shane thinks of them" — not this build's job.
  `git status --porcelain` clean (aside from the gitignored, uncommitted `web/shanes-life/.env`
  copied in from the main checkout to run migrations/tests locally — not tracked, not part of
  the diff). Branch verified merged (`verify-branch-merged.mjs`, exit 0).
