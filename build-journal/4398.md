# Build Journal — Issue #4398

- **Status:** ✅ DONE 2026-09-16T18:55:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-16T18:39:32Z — Started. Normalise email (.toLowerCase().trim()) before the returning-customer lookup in POST /api/public/checkout-session (artifacts/api-server/src/routes/public-services.ts), and before storing on checkout_sessions.
  - 2026-09-16T18:55:00Z — ✅ DONE — Fixed in commit `e7bf3aa23` (pushed to main as `9d202b90d`). Live-verified by mounting the real `public-services.ts` router in-process (tsx) against the local Postgres DB: POSTing `checkout-session` with `TEST.Customer.0914-1302@Example.com` (mixed-case of an existing account's stored `test.customer.0914-1302@example.com`) now correctly returns 409 `already_has_account` instead of creating a session. No fixture/hardcoded data used. Working tree clean; branch confirmed merged into origin/main via verify-branch-merged.mjs.
