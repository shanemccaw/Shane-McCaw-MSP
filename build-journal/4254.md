# #4254 — TypedCardContent.tsx dead runbook-jobs polling paths

- **Status:** ✅ DONE 2026-09-15T15:46:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** e091f6794 (bookend), fabdec238 (fix)

## Log
- 2026-09-15T15:41:00Z ⏳ IN FLIGHT — investigated dead `/api/admin/runbook-jobs` +
  `/api/admin/runbook-jobs/output` calls in `TypedCardContent.tsx`'s inline
  `ScriptModalBody.handleRun`/`poll`. Read `admin-m365-run.ts` (the real
  `POST /api/admin/run-script` + `GET /api/admin/run-script/:jobRef/status` pair),
  `scriptPoller.ts`, and `RunLibraryScriptDialog.tsx` (the already-working
  `KanbanCardModal.tsx` flow) as reference. Per Shane's clarifying comment on the
  issue, rewiring in place rather than folding into `scriptPoller.ts` or deleting.
- 2026-09-15T15:46:00Z ✅ DONE — rewired `handleRun`/`poll` in `TypedCardContent.tsx`
  to `POST /api/admin/run-script` (`{ libraryScriptId: sm.scriptId, credentialId:
  sm.credentialId, kanbanTaskId }`) and `GET /api/admin/run-script/:jobRef/status`
  (`{ status, outputLines, findings, recommendations, scoreImpact }` — full
  cumulative `outputLines` each poll, not the old delta/`since` shape). Dropped
  `governanceAreas` from the POST body — the real schema has no such field and the
  card already persists it via the prior PATCH to `/api/admin/kanban-tasks/:id`.
  `npx tsc --noEmit` in `artifacts/msp-console` passes clean; no other reference to
  the dead paths remained (`grep -n "runbook-jobs"` in the file: no matches).
  Mid-build finding: `POST /api/admin/run-script`'s job creation still calls the
  retired `azure-automation.ts` stub, which unconditionally throws — the whole Run
  Script pipeline (this card's flow and `KanbanCardModal`'s) 502s regardless of
  wiring, because it was never migrated to the real `ps-execution-client.ts` path
  other engine code already uses. Filed as #4262, parented under #3433 (sibling
  sub-issue, per this build's standing rule), labeled `bug`, board status set to
  AI Batter Up. Branch verified merged into `origin/main` via
  `verify-branch-merged.mjs`; working tree clean.
