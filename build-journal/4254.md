# #4254 — TypedCardContent.tsx dead runbook-jobs polling paths

- **Status:** ⏳ IN FLIGHT 2026-09-15T15:41:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-15T15:41:00Z ⏳ IN FLIGHT — investigated dead `/api/admin/runbook-jobs` +
  `/api/admin/runbook-jobs/output` calls in `TypedCardContent.tsx`'s inline
  `ScriptModalBody.handleRun`/`poll`. Read `admin-m365-run.ts` (the real
  `POST /api/admin/run-script` + `GET /api/admin/run-script/:jobRef/status` pair),
  `scriptPoller.ts`, and `RunLibraryScriptDialog.tsx` (the already-working
  `KanbanCardModal.tsx` flow) as reference. Per Shane's clarifying comment on the
  issue, rewiring in place rather than folding into `scriptPoller.ts` or deleting.
