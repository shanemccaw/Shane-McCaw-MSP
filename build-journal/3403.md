# #3403 — msp-marketplace-purchase.ts: retainer catalog items charged once instead of as a recurring Stripe subscription

- **Status:** ✅ DONE 2026-09-11T04:44:42Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 24b36fca04eaf9e5a3c51efd657a80be4af10039 (on main; originally committed as a1ca4c4cb, rebased twice due to concurrent landings)

## Log
- 2026-09-11T04:21:07Z ⏳ IN FLIGHT — read issue #3403 + BUILD comment; about to investigate `artifacts/api-server/src/routes/msp-marketplace-purchase.ts` paid checkout path (`:266-446`) and the working pattern in `portal-checkout-direct.ts:164`.
- 2026-09-11T04:44:42Z ✅ DONE — Widened the paid-checkout branch to `serviceClass === "subscription" || svc.billingType === "recurring_monthly"`, matching `portal-checkout-direct.ts:164`'s proven pattern. `recordTenantSubscription()` was already wired inside that branch (#2847), so the 6 live retainer catalog rows (ids 115/116/117/168/118/119 — confirmed live in local DB: `service_class='retainer'`, `billing_type='recurring_monthly'`) now create a real Stripe Subscription + `tenant_subscriptions` row instead of a single one-time PaymentIntent charge.
  A concurrent commit landed mid-rebase that moved `recordTenantSubscription()`'s call behind a second, separately-re-derived `serviceClass === "subscription"` gate — which would have silently undone this fix. Caught via my own regression test failing post-merge; fixed by gating on the real Stripe outcome (`subscriptionId && stripeSubStatus && stripeSubPeriod`) instead of re-deriving `serviceClass`.
  Added a regression test (retainer/recurring_monthly purchase → Stripe Subscription created, not PaymentIntent; `recordTenantSubscription` called with correct ids).
  Verified: `pnpm --filter @workspace/api-server run typecheck` (pretypecheck rebuilds `lib/db` decls via `tsc -b`, then `tsc -p tsconfig.json --noEmit`) — clean. `vitest run src/routes/msp-marketplace-purchase.test.ts` — 17/17 passing.
  Filed finding #3634 (portal-checkout.ts:655 + msp-sow.ts:347 carry the identical narrow-check bug for the same 6 retainer rows) — parented under #1571, board status "AI Batter Up".
  Posted completion comment on #3403; flipped its labels `in-flight` → `complete`. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. Working tree clean.
