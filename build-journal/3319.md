# #3319 — Weekly-ad deal-type chips + thumbnails on Shopping rows

- **Status:** ✅ DONE 2026-09-09T01:00:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** cb119178d (bookend), 3588140d5 → rebased/pushed as c0ac643ad

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — Read README `Design/design_handoff_shanes_life/README.md` "Weekly-ad deal types" bullet + issue #3319 body; #3310's backend (`deal_type`/`category`/`image_url`/`valid_to` on `item_prices`/`coupons`) already exists and flows into `attachWeeklyAdVerdicts`'s verdict object except `validTo`, which was queried nowhere. Implementing: (1) select+attach `valid_to` in `attachWeeklyAdVerdicts` (src/core/prices.mjs), (2) client `verdictBadge()` in public/app.js gains an uppercase kind chip (`dealType`) before the pill, a "through Wed" suffix from `validTo`, and a 22px ad-thumbnail slot with store-initial fallback until the real image loads, (3) matching CSS in public/app.css.
- 2026-09-09T01:00:00Z ✅ DONE — Shipped `web/shanes-life/src/core/prices.mjs` (select+attach `valid_to` in `attachWeeklyAdVerdicts`), `public/app.js` (`throughLabel()` helper + `verdictBadge()` kind chip / through-suffix / thumbnail-slot-with-fallback), `public/app.css` (`.shop-item-verdict-thumb-wrap`/`-fallback`, `.loaded` fade-in). Live-verified against the real local DB (not fabricated): inserted a real `item_prices` row (source `weekly_ad`, `deal_type='BOGO'`, a real `image_url`, `valid_to` 3 days out) under the scratch test user `check-3273-*@shanes.life`, ran `attachWeeklyAdVerdicts` directly, confirmed the returned verdict carries `dealType`/`imageUrl`/`validTo` correctly, confirmed `throughLabel()` renders the correct weekday ("Sat" for 2026-09-12) via the same local-midnight-safe date parsing `dueLabel()` already uses elsewhere in this file, then deleted the scratch store/price rows. `node --check` passed on both touched JS files. Branch verified merged into origin/main (`verify-branch-merged.mjs` OK, c0ac643a). Working tree clean (`git status --porcelain` empty). No test-manifest section applies -- this repo's Test Coverage standing practice is scoped to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/` only; `web/shanes-life/` is Shane's personal project, out of that scope.
