# #3200 — Shane's Life: orphan schema_migrations ledger rows (041/043) block migrate.mjs

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** (investigation-only, premise resolved — no code change)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — investigating orphan `schema_migrations` ledger rows (`041_plaid_item_health.sql`, `043_account_mask.sql`) reported missing from disk in #3200.
- 2026-09-08T00:05:00Z ✅ DONE — Re-checked against current `origin/main` (this worktree's HEAD `a41ce8a0a` is an ancestor of `origin/main`). Both files now exist and are tracked in `web/shanes-life/migrations/`: `041_plaid_item_health.sql` landed via commit `995d7a389` ("Plaid webhooks + reconnect/update-mode flow"), `043_account_mask.sql` via `0d622429d` ("Feature: Money — Accounts view (Shane's Life) — Git #3170"). Neither was ever actually lost — the issue was filed while a sibling worktree held those files uncommitted (the exact race #3175 describes), and the concurrent builds have since landed on main. Verified `SELECT filename FROM schema_migrations` against the real local `finances` DB (46 rows) matches the 46 files on disk in `web/shanes-life/migrations/` exactly, and ran `runMigrations()` from `web/shanes-life/src/migrate.mjs` directly against the real local DB: `[migrate] up to date (33 already applied)` — no orphan-ledger error, `check-migration-numbers.mjs` also reports OK. No `.sql` recovery or ledger `UPDATE`/`DELETE` was needed; the premise (files missing) is false as of this session. No code changed.
