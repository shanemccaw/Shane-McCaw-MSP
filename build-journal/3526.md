# #3526 — Webhooks: delivery log depth and paging (v2)

- **Status:** ✅ DONE 2026-09-10T21:16:09Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** e508f3e80 (implementation), c11aaf2b1 (IN FLIGHT bookend)

## Log
- 2026-09-10T20:59:41Z ⏳ IN FLIGHT — Backend-only fix for the two real gaps on `GET
  /api/portal/webhooks/:webhookId/deliveries` (and its admin + msp-console siblings,
  all sharing `getDeliveryLog`): (1) serve the already-stored `requestBodySnapshot`
  JSONB column, never selected before; (2) add real keyset pagination (`cursor`/
  `nextCursor` on the internal serial `id`) so a webhook with >200 deliveries can be
  paged past the previous hard 200-row ceiling. No schema change needed — the column
  already exists (`msp.ts:1165`). Scope confirmed backend-only: wiring lives on the
  separate sibling issue #3527.
- 2026-09-10T21:16:09Z ✅ DONE — Both gaps fixed in `webhook-delivery.ts` /
  `webhooks.ts` / `msp-console-webhooks.ts` (e508f3e80): `getDeliveryLog` now
  selects and returns `requestBodySnapshot`, and does keyset pagination on the
  internal serial `id` column — all three route callers (portal, admin,
  msp-console) accept `?cursor=` and return `nextCursor`. Also fixed a
  pre-existing bug in `webhook-delivery.test.ts`'s logger mock (missing
  `.child()`, predates this session, confirmed via `git show HEAD`) so the
  file's own tests actually run; added 3 new tests. Verified: `node --test`
  16/16 pass, `tsc --noEmit` clean. Rebased onto and pushed to `origin/main`
  (e508f3e80) myself — `verify-branch-merged.mjs` confirms OK. Working tree
  clean. No new findings filed (the only irregularity found — the broken
  logger mock — was fixed in this same build, not merely discovered).
