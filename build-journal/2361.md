# #2361 — Batter Up item: NEEDS YOU badge, amber tint, blocking reason (Ask Shane lane)

- **Status:** ✅ DONE 2026-09-03T01:19:58Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-03T01:07:15Z
- **Commit(s):** `e432e4c4e` on `main` (rebased twice onto `origin/main` under concurrent
  #2358/#2360/#2364 builds landing mid-session; both rebases resolved manually — see log)

## Log
- 2026-09-03T01:07:15Z ⏳ IN FLIGHT — confirmed #2359 genuinely DONE on `main`
  (bookend `build-journal/2359.md` status ✅ DONE, commit `e592d495c`/`d8406701a`, both
  present in this worktree's `main` history). Starting item 6 of 11 under Feature #2355:
  items in the real "Ask Shane" lane (board Status option id `404998bb` — NOT "AI For
  Shane", per dispatch correction) render a NEEDS YOU badge, amber tint reusing
  `Brush.Toast.Warning` (#E2984A, same token as #2311's amber banner), and the real reason
  it's blocking pulled from the issue's own body/latest comment.
- 2026-09-03T01:19:58Z ✅ DONE — implemented in `ChatGitHubFilter.cs`
  (`BatterUpItemRef.BlockingReason` + new `ExtractBlockingReason` extraction) and
  `MainWindow.BatterUpPanel.cs` (NEEDS YOU pill, amber row tint reusing
  `Brush.Toast.Warning`, blocking-reason line replacing the generic "why it is here" line
  for Ask Shane items only — checked by real item number against `counts.AskShaneItems`,
  so it renders correctly on the "All" tab too, not just the "Ask Shane" tab). `dotnet
  build ShaneBuilder.csproj -c Debug` clean (0/0) after implementation and after both
  rebases. Live-verified against the real board: queried the actual 4 open Ask Shane items
  (#2399, #2404, #2405, #2406) via `gh api graphql` and independently re-implemented
  `ExtractBlockingReason`'s exact algorithm in a throwaway Node script fed the real
  body/comment text — #2399's body genuinely states a real open question and extracts
  correctly; #2404/#2405/#2406 have zero comments and a body that's pure "Sub-issue of
  #N..." navigation boilerplate with the real reason sitting only in the issue title, so
  `ExtractBlockingReason` correctly returns null (never invents the boilerplate as a fake
  reason) and the panel shows an honest "No open question stated yet." for those 3 — see
  finding #2497 below. Two rebases onto `origin/main` were needed as #2358/#2360 landed
  concurrently (both touched the same per-item render loop and class doc comment); both
  resolved by hand, keeping both features' content (state pill/Dispatch all/Hold-Release
  from #2358/#2360 alongside this build's NEEDS YOU badge/blocking-reason line), verified
  clean via a rebuild after each rebase.
- **Finding filed:** #2497 "Some Ask Shane items state their real blocking reason only in
  the title, not body/comment" — real live gap found during verification (see above).
  Parented as a direct sibling sub-issue of #2355 (not the Epic), labeled `bug` +
  `agent-finding`, board status set to "AI Batter Up" per the standing rule.
- `verify-branch-merged.mjs` — OK, `agent/2361-q1201` (`e432e4c4e`) merged into
  `origin/main`. `git status --porcelain` clean.
