# #2912 — mark_bill_paid — track already-paid-this-cycle (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 1e08715af (mark_bill_paid — track already-paid-this-cycle (ShanesSurvival) #2912)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — migration (accounts.last_paid_date), mark_bill_paid MCP tool, pay_period_due_status already-paid exclusion, bill_status last_paid_date surfacing.
- 2026-09-04T00:45:00Z ✅ DONE — Real migration `migrations/011_bill_last_paid_date.sql` (renamed from
  010 after rebase found #2910 had already claimed 010 on main) adds `accounts.last_paid_date DATE`,
  self-applied by MigrationRunner on next WPF app launch against Shane's own ShanesSurvival Postgres
  (no manual psql step — same convention as migrations 007/008). New MCP write tool
  `mark_bill_paid(accountName, paidDate?)` sets it (Bill-role validation, same pattern as
  set_bill_target/set_bill_due_day). `pay_period_due_status` now excludes a bill whose
  `last_paid_date` is on or after the most recent real due-day occurrence at or before today
  (new `FindMostRecentDueDateOnOrBefore` helper in `PayPeriodDueService`), handling real month
  wraparound the same way `FindDueDateInWindow` already does. `bill_status` surfaces
  `last_paid_date` per bill (informational only — doesn't change its own shortfall math).
  Verified the exact repro logic standalone (dueDay=5, today=Sep4, paidDate=Sep3 → excluded;
  same bill at today=Sep10 with no fresh mark → reappears; Jan wraparound dueDay=31 → correct
  Dec31 anchor). `dotnet build ShanesSurvival.sln` clean, 0 warnings/errors. No test project
  exists for ShanesSurvival (personal desktop app, out of the msp-portal/marketing test-manifest
  scope). Rebased onto origin/main (picked up #2910's unrelated commits) and pushed straight to
  `main` per this repo's convention; `verify-branch-merged.mjs` confirms ancestor. Working tree
  clean.
