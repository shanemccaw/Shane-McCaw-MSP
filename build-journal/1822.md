# #1822 — Portal Shell: SOP dropdown / tray

- **Status:** ✅ DONE 2026-09-03T16:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** c0ec26204 (implementation), 7af600766 + c388645d9 (merges into main)

## Log
- 2026-09-03T15:20:00Z ⏳ IN FLIGHT — Read #1822/#1819/#1817/#1493 body+comments. Blocker #1819
  (navigation) is CLOSED/complete, so this build is unblocked. Note: the dispatch's premise
  ("SOPs #1493 is 0 of 6 backend children done") is stale — #1556/#1557/#1558/#1559/#1560/#1620
  and the contract-pack #1728 are all now CLOSED; `GET /api/portal/sop-runs`
  (artifacts/api-server/src/routes/portal-sops.ts:510-663) is a real, fully-wired endpoint
  reading `msp_sop_runs`/`msp_sops`, not an empty stub. Only #1729 (SOPs page design export)
  and #1730 (wire the full SOPs page) remain open — neither blocks the shell tray, which reads
  the same endpoint the full page will use. Building the tray against real data per the
  Design/portal/design_handoff_ui_shell/ export (`TopBar.tsx`'s `topbar-sop-trigger` is the
  existing unwired mount point, comment cites #1822 by name).
- 2026-09-03T16:05:00Z ✅ DONE — Built `SopTray.tsx` (the popover, both empty/full states from
  `Design/portal/design_handoff_ui_shell/Shell.dc.html:624-674`) and `useSopRuns.ts` (client-side
  mirror of `WireSopQueueItem`, mirrors `usePillarSummary.ts`'s fetch pattern against the real
  `GET /api/portal/sop-runs`, `artifacts/api-server/src/routes/portal-sops.ts:510-663`). Wired the
  existing `topbar-sop-trigger` mount point to it plus a real running+queued count badge.
  Mid-build, `#1820` (account dropdown) landed on `origin/main` concurrently and established a
  shared `openPopover` state + single overlay pattern in `TopBar.tsx` (mutually exclusive with
  the user menu) that my independent local-state implementation didn't know about — merged
  `origin/main`, resolved the real conflict by adopting that pattern (widened `openPopover` to
  include `"sop"`, dropped my own overlay, repositioned `SopTray` `absolute right-0` off its own
  `relative` trigger wrapper the same way `UserMenu` does, instead of the design's raw
  viewport-offset pixels).
  Footer links ("View SOP library" / "Open Runbooks") route through the existing honest
  `/coming-soon` state via `wouter`'s `Link` (both module pages are `builtPath: null` in
  `moduleNav.ts`).
  Findings check: this session's own reading of #1493 found its dispatch premise ("0 of 6 backend
  children done") stale — #1556/#1557/#1558/#1559/#1560/#1620 and contract-pack #1728 are all
  closed; `sop-runs` is a real wired read, not a stub. No new finding filed for that (not a bug,
  just resolved before this build started). The pre-existing `class-variance-authority` worktree
  dependency-link failure that blocks a full `tsc`/`vite build` for this whole app was already
  filed by #1819's own session as #2546 — not duplicated here. The stale `artifacts/msp-portal`
  path named in CLAUDE.md's Test Coverage section is already tracked by #1921 — not duplicated
  either. No test manifest added, following #1819's own precedent (no manifest infra yet for this
  new `artifacts/portal` app).
  Verification: `npx tsc -p tsconfig.json --noEmit` clean for every touched/added file, both
  before and after the merge conflict resolution (grep for `SopTray|useSopRuns|TopBar|UserMenu`
  against the tsc output returns nothing both times; all remaining errors are the pre-existing
  #2546 class, untouched by this build).
  Merged `agent/1822-q1386` → `origin/main` directly (commits `7af600766`, then `c388645d9` after
  a second concurrent-commit rebase), confirmed via `verify-branch-merged.mjs` ("Safe to write
  DONE"). `git status --porcelain` clean.
