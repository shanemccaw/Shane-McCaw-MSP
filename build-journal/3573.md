# #3573 — Session-limit recovery should be fully automatic (periodic log sweep)

- **Status:** ✅ DONE 2026-09-11T00:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** fa80c5145 (rebased/merged into main; original commit was ef655274a)

## Log
- 2026-09-11T00:28:08Z ⏳ IN FLIGHT — implementing per issue body: schedule `SessionLimitAutoRestartService.ManualRecoverFromLogsAsync` on a periodic timer (5-minute cadence, 1-hour log window, matching this app's existing "every 5 minutes" background-poll convention), reusing the method verbatim; leaving `DetectSessionLimit`/`limit-paused`/reset-timer path untouched; then removing `BtnRecoverSessionLimit` (XAML button + click handler) and its now-dead doc references.
- 2026-09-11T00:52:00Z ✅ DONE — `SessionLimitAutoRestartService` now has `StartPeriodicSweep()`/`RunPeriodicSweepAsync()`: a `System.Threading.Timer` armed from `StartAsync()` (unconditionally, before any early-return bootstrap branch) that calls the exact same `ManualRecoverFromLogsAsync(TimeSpan.FromHours(1))` every 5 minutes, with an `Interlocked` guard so a slow scan can't overlap the next tick. Live `TryDetectLimitMessage`/`RegisterLimitHit`/`Arm`/`FireAsync` (the reset-timer restart path) is byte-for-byte untouched. Removed `BtnRecoverSessionLimit` from `BuildQueuePanel.xaml` and its click handler + orphaned XML-doc comment from `BuildQueuePanel.xaml.cs`; updated stale doc comments in `BuildQueuePostgresClient.cs` referencing the old button. Confirmed no remaining references to `BtnRecoverSessionLimit`/`RecoverSessionLimit` anywhere in `desktop/BuildConsole` (grep). `dotnet build BuildConsole.csproj -c Debug` — 0 errors (8 pre-existing unrelated warnings). Hit a real concurrent-session conflict on push (Git #3575 removed the neighboring `BtnBoardReconcile` button/handler in the same file region while this session worked) — resolved by rebase, keeping both removals; rebuilt clean after resolving. `verify-branch-merged.mjs` confirms `fa80c5145` is an ancestor of `origin/main`. `git status --porcelain` clean.
