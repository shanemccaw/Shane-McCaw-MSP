# #3450 — GraphProbeModal fabricates fake probe responses; dead endpoint preset

- **Status:** ✅ DONE 2026-09-10T23:05:00Z
- **Scope:** platform

## Log
- 2026-09-10T22:50:00Z — Started. Scope for this pass: fix dead `/api/msp/audit-logs` preset in `artifacts/admin-panel/src/components/GraphProbeModal.tsx:20` to `/api/msp/audit`. The larger "whole modal fabricates responses" issue is a product decision per the issue body — filed as a sibling finding under #1095, not fixed in this pass.
- 2026-09-10T23:05:00Z — ✅ DONE — Changed `GraphProbeModal.tsx:20` preset URL from `/api/msp/audit-logs` to the real, only-ever-mounted `/api/msp/audit` (confirmed via `grep -rn "audit-logs"` there's no collision — `/api/audit-logs` is a separate real platform-wide route unrelated to this preset). Commit `6e1c6aa00` (rebased onto and pushed directly to `origin/main`). Filed sibling finding #3560 ("GraphProbeModal fabricates every probe response client-side instead of calling the real API") under #1095, board status "Ask Shane" (needs Shane's real/mock decision), labeled `bug`. Working tree clean; `verify-branch-merged.mjs` confirms ancestor of main.
