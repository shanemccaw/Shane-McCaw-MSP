# #1827 — All 23 alert rules deep-link to /portal-v2/* — the deleted artifact

- **Status:** ✅ DONE 2026-09-03T02:35:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `0464f6a27e35cc2ddbc8260dc28fff41023bad26`

## Log
- 2026-09-03T01:47:00Z ⏳ IN FLIGHT — investigating real notifications.link_path state, deep_link_path values, and Design/portal/ pages under #1485 to establish the destination mapping.
- 2026-09-03T02:35:00Z ✅ DONE — investigation + fix, per the issue's own 3-part ask:

  **1. Real state (verified live against local Postgres, 2026-09-03):**
  - `notifications.link_path`: 84 non-null rows, distinct values `/customer-offers` (80),
    `/admin-panel/content/articles?tab=drafts` (4) — **no `/portal-v2/*` rows currently in
    the local dataset.** No live surface currently writes a notifications row from a
    customer-tenant-alert firing (`customer-alert-delivery.ts` only emails; nothing calls
    `notification-center.ts`'s `createNotification` from that path).
  - `customer_tenant_alert_rules.deep_link_path`: all 23 rows carry one of 8 distinct
    `/portal-v2/*` paths, exactly as the issue states (health, billing, projects, support,
    risk-register, governance, governance/oversharing, compliance-obligations).

  **2. Destination mapping (Design/portal/ + artifacts/portal read live):** `Design/portal/`
  has **zero `.dc.html` exports**; `artifacts/portal/src/pages/` has only `index.tsx`
  (placeholder) and `not-found.tsx`. **All 8 destinations have no page yet** — none can be
  find-and-replaced to a real path today. `artifacts/portal/src/App.tsx` routes only `/`.

  **3. Honest behaviour, built (not deferred):** the two live surfaces that actually turn a
  `deep_link_path` into a customer-facing href today are the immediate customer-alert email
  (`customer-alert-delivery.ts`) and the digest-drain email (`customer-alert-digest.ts`) —
  both were embedding the raw dead `/portal-v2/*` path directly into an emailed `<a href>`.
  Fixed by adding one shared resolver, `artifacts/api-server/src/lib/portal-deep-links.ts`
  (`resolvePortalDeepLink`), that every such surface now goes through:
  - Unknown/not-yet-shipped destinations resolve to `/coming-soon?feature=<label>` — a new,
    real portal page (`artifacts/portal/src/pages/coming-soon.tsx`, routed in `App.tsx`)
    that honestly states the destination isn't built yet, rather than a dead link or a
    hidden notification.
  - As each of the 8 destinations ships a real page under #1485, its `livePath` flips in
    `portal-deep-links.ts` — nothing else (the 23 DB rows, the email templates, the future
    #1821 dropdown) needs to change. The 23 `deep_link_path` DB rows were deliberately
    **not** rewritten — rewriting them to a guessed new path would just trade one 404 for
    another, exactly the trap the issue's own body calls out.
  - Added `portal-deep-links.test.ts`, registered in `vitest.config.ts`'s include list, but
    **could not execute it** — see Verification below.

  **Verification:** `tsc --noEmit` clean on both touched packages (api-server: no new
  errors beyond the pre-existing `lib/db/dist` not-built baseline; portal: exit 0, fully
  clean). `resolvePortalDeepLink` logic hand-verified via
  `node --experimental-strip-types` (all 8 seeded paths → `/coming-soon?feature=...`,
  `available: false`; unknown path → same honest fallback, never a raw `/portal-v2/*` href;
  `null` → portal home). **Could not run the new vitest file itself**, or any existing
  api-server vitest test — `artifacts/api-server`'s pnpm-store copy of `vitest@4.1.9` is
  genuinely empty (`node_modules/vitest` resolves, the store package it points at has no
  `package.json`/`dist`), confirmed against the **main checkout**, not a worktree artifact,
  and confirmed by `store-doctor.mjs` as its own "unrepairable — store copy itself missing"
  class. Attempted the established hand-patch recipe (`npm pack` + copy into the store,
  precedented in the `api-server-vitest-now-runnable` memory) — denied by this session's
  permission classifier for writing outside the worktree. Filed as **#2516** (`bug`,
  `area:api-server`, parented under #1621 per the Feature-first rule, board status "AI
  Batter Up", assigned Shane) with the full evidence and the ask: a deliberate `pnpm
  install`/scoped vitest refetch, Shane's call per the bandwidth rule.
  Also confirmed `vite build` on `artifacts/portal` fails on a separate, pre-existing,
  already-documented gap (`windows-native-binaries-missing-pnpm-store` memory —
  missing win32 esbuild native in this same store) — not filed again, already tracked.

  `git status --porcelain` clean at finish. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
