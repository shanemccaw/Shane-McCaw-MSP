# #3811 — Remove GitDoctorView's local duplicated Catppuccin palette — enforce real DarkTheme.xaml resources

- **Status:** ✅ DONE 2026-09-12T19:29:35Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 5fbad4793 (rebased/pushed to main; original pre-rebase commit was ab1776104)

## Log
- 2026-09-12T19:26:13Z ⏳ IN FLIGHT — replacing every local `Brush.*` key in
  `GitDoctorView.xaml.cs` with its real, exact-hex `DarkTheme.xaml` equivalent per
  the issue's confirmed mapping table; the two real gap cases (`Bg.Card`,
  `Bg.Window`) stay local but honestly renamed/commented rather than disguised as
  ported ShaneBuilder semantic keys.
- 2026-09-12T19:29:35Z ✅ DONE — Replaced every local `Brush.*` key with its real
  `DarkTheme.xaml` equivalent (exact hex matches, per the issue's table):
  `Brush.Bg.Chip`/`Brush.Border.Card` → `Surface0Brush`; `Brush.Border.Strong` →
  `Surface1Brush`; `Brush.Claude.Accent` → `PeachBrush`; `Brush.Epic.AppCore` →
  `BlueBrush`; `Brush.Epic.Gate` → `RedBrush`; `Brush.NextUp.NoBuild.Fg`/`Brush.Text.Muted`
  → `Subtext1Brush`; `Brush.Status.Running` → `GreenBrush`; `Brush.Text.Dim` →
  `OverlayBrush`; `Brush.Text.Heading`/`Brush.Text.Primary` → `TextBrush`. Also found
  and removed the dead, never-referenced `Brush.LogSource.Console` registration
  (mapped to `TealBrush` conceptually, but had zero call sites — deleted rather
  than "resolved"). A concurrent commit (#3809, landed mid-session) had added
  `Brush.Danger.Soft` (`#EBA0AC`) for the branch-delete button's danger tone —
  resolved the rebase conflict by mapping it to its own exact-hex DarkTheme
  equivalent, `MaroonBrush`, consistent with the rest of this cleanup.
  The two real gaps with no opaque DarkTheme.xaml equivalent (`Bg.Card` #1C2128,
  `Bg.Window` #0D1117 — DarkTheme's BaseBrush/CrustBrush share the same RGB but
  carry baked-in alpha for other call sites, which would visibly shift these as
  opaque backgrounds) were kept as local resources, renamed `Brush.Local.Bg.Card`
  / `Brush.Local.Bg.Window` and commented honestly as intentionally local rather
  than disguised ShaneBuilder-port keys. FontFamily.*/FontSize.*/FontWeight.*
  registrations untouched, out of scope per the issue's explicit "colors" ask.
  Verified: grep confirms zero remaining references to any of the old local
  Brush keys anywhere in the file; clean `dotnet build` (0 errors, only
  pre-existing unrelated warnings). No live BuildConsole instance was launched
  for a visual screenshot (per standing rule against launching BuildConsole.exe
  for self-verification) — every replaced key is an exact hex match per the
  issue's own confirmed table, and the two real gap cases were kept exactly as
  before (same hex, same opacity), so there is no code path that changes any
  rendered pixel. Pushed to `main` at `5fbad4793`; `verify-branch-merged.mjs`
  confirms ancestor; `git status --porcelain` clean.
