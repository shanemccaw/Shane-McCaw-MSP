# #1855 — dev-server: coordinator restarts the api-server mid-flight with no signal to long-running agent work

- **Status:** ✅ DONE 2026-09-06T15:58:24Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 01e128c0f — "dev-server: real restart signal + bounded advisory holds for long-running agent work (#1855)"

## Log
- 2026-09-06T15:43:10Z ⏳ IN FLIGHT — read #1855's body + the #1793 capability-survey evidence
  (`scripts/src/ps-capability-survey.ts`'s `ApiServerUnreachableError`/`callCmdletWaitingOutRestarts`
  is the caller-side workaround already shipped for that one script). Read the coordinator
  (`scripts/dev-server/{coordinator,config,lock,server-process,refresh-main-server,request-restart,
  status,buildset}.mjs`). Plan: (1) a generalized, reusable helper any caller (JS or TS) can use to
  distinguish "coordinator is mid-restart" from "genuinely down" using the coordinator's own real
  state files (`current-cycle.json` phase, `server.json` pid liveness) instead of blind retries; (2)
  a bounded advisory "restart hold" a long-running agent task can take so the coordinator defers its
  next restart briefly around it, mirroring the existing `--buildSet` deferral mechanism but for
  ad-hoc agent work rather than a declared build stack; (3) wire `ps-capability-survey.ts` to use
  both instead of its own bespoke retry loop; (4) document the drop window + the new tools in
  `scripts/dev-server/README.md`.
- 2026-09-06T15:58:24Z ✅ DONE — commit 01e128c0f (pushed, `agent/1855-q1770` verified an
  ancestor of `origin/main` via `verify-branch-merged.mjs`). Shipped:
  - `scripts/dev-server/api-fetch.mjs` (+ `.d.mts`) — `fetchResilient()` retries ONLY a
    genuine transport failure and, instead of guessing, reads the coordinator's real state
    (`current-cycle.json` phase, lock owner) via `describeRestartState()` to say WHY it's
    retrying and, if it gives up, to report the real reason in `ApiServerUnreachableError`.
  - `scripts/dev-server/restart-hold.mjs` (+ `.d.mts`) — bounded, advisory restart holds:
    `acquireHold`/`renewHold`/`releaseHold`/`activeHolds` + CLI. A hold whose heartbeat goes
    stale (`restartHoldDefaultTtlMs`, default 10 min) is ignored, same discipline as
    `lock.mjs`'s stale-lock recovery — never wedges the fleet.
  - `coordinator.mjs` — `runCycle()` and `maybeFireSetRestart()` both wait, bounded by
    `restartHoldMaxWaitMs` (default 2 min), for active holds to clear before firing
    `deps.restart()`; the wait outcome is recorded on the cycle/set-restart records.
  - `config.mjs` — new `restartHoldsDir`, `restartHoldDefaultTtlMs`, `restartHoldMaxWaitMs`
    knobs, all env-overridable. `config.d.mts` added so TS callers get real types.
  - `scripts/src/ps-capability-survey.ts` (the actual #1793 driver this was filed from) now
    acquires/renews a `ps-capability-survey` restart hold for the run's duration and reports
    the coordinator's real state in its own retry loop instead of a generic guess.
  - `scripts/dev-server/README.md` — new section documenting the drop window + both tools;
    Files table and Config knobs updated.
  - `selftest.mjs` Scenario 11 (3 new checks group): active hold genuinely defers the
    restart; releasing it early lets the restart proceed sooner; an unreleased hold does
    NOT block forever (bounded wait times out, restart still fires). **Verification:**
    `pnpm --filter @workspace/scripts run typecheck` clean; `node
    scripts/dev-server/selftest.mjs` 45/45 passing (env-isolated from this session's own
    `DEV_BUILD_SET*` vars, which otherwise leak into the spawned child processes and give a
    false read — see finding below). Manual smoke test of `restart-hold.mjs` CLI
    (acquire/status/release) and `fetchResilient()` against a genuinely-unreachable port,
    both behaved as designed.
  - **Finding filed:** #3020 (parented under #1789, the Feature this issue itself sits
    under) — `selftest.mjs` Scenarios 3/9 (real cross-process concurrency) intermittently
    fail on genuine git-level races (`index.lock` contention, a HEAD ref-update race) on
    this machine, roughly 1-in-3 runs. Confirmed pre-existing and unrelated to this build's
    changes (Scenario 11, which #1855 added, passed 3/3 runs while 3/9 flaked twice in the
    same three runs). Board status set to AI Batter Up.
  - Working tree left clean (`git status --porcelain` empty) after the final push.
