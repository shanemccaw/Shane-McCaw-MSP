# #1865 — Config state: re-run resource model extraction against #1793's PowerShell survey results

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** a3ab01ca5

## Log
- 2026-08-30 ⏳ IN FLIGHT — verifying `ps_capability_survey_results` is populated, then re-running `node scripts/config-state/build-resource-model.mjs` to reconcile the 211 powershell-transport resources against #1793's real survey data.
- 2026-08-30 ✅ DONE — verified `ps_capability_survey_results` populated (run 4, 1,166 rows, matches #1793's own reported breakdown exactly). Found #1794's claim that re-running `build-resource-model.mjs` would pick up the survey "automatically" was FALSE — no reference to the survey table existed in the script. Built `scripts/config-state/reconcile-ps-survey.mjs`, wired into the pipeline, no schema changes. Fixed a real Windows/MSYS `tar` bug in `fetch-sources.mjs` blocking the re-run. Real before/after delta (211 powershell resources): 60 upgraded to `available_now`, 105 confirmed, 0 downgraded, 14 inconclusive, 32 unreconciled. Overall `available_now` 594→658, readable-today-and-uncovered 554→612. Recorded as a new section in `docs/graph-resource-model.md` (original numbers left untouched). Found and filed a real regression: re-running `build-resource-model.mjs` wipes `config_resource_samples`, silently discarding #1794's live `needs_license` evidence on 4 resources — not fixed (fixing it means re-deriving the Graph half, out of scope) — filed as #1895. Also filed #1896 (12 resources cite a DSC connection-helper as their read cmdlet, not a real cmdlet). Both parented to #1850, board status AI Batter Up. `verify-branch-merged.mjs` confirmed merged into origin/main; working tree clean.
