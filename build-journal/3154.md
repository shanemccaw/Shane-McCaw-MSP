# #3154 — Feature: Smoking tracker (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:58:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 9f02df3bc (real work), c4bea57dd (IN FLIGHT bookend)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — #3137 (blocker) is CLOSED/complete, unblocked. Real
  scope per issue body + design contract pack Section 3 + prototype logic
  (`Shanes Life - First Slice Prototype.dc.html`): one-box capture ("smoked" / "bought a
  pack") logs to the real `smoke_log` table (migration 017, already exists, 0 rows in the
  real `finances` DB), running real per-pay-cycle spend total, and the real
  financial-confrontation line on the Money "Now" tab shown only while a real shortfall
  exists. `money_habits` (migration 027) already models the recurring habit cost but no
  code path writes to `smoke_log` or surfaces cycle totals yet — building that now:
  `logSmoke`/cycle-window helper in `src/core/money.mjs`, `POST`/`GET /api/money/smoke`,
  and an `log_smoke` MCP tool, wired into `getGateStatus`'s existing response.
- 2026-09-08T01:58:00Z ✅ DONE — Real, verified end to end against the local `finances`
  Postgres (not `shanemccawmsp` -- see `.env.example`'s own note about which DB name is
  real). Shipped: `money.logSmoke`/`money.getSmokeSummary`/`resolvePayCycleWindow` in
  `src/core/money.mjs`; `POST`/`GET /api/money/smoke`; MCP tool `log_smoke`; a `smoking`
  field added to `getGateStatus`'s existing response carrying the real this-cycle/
  last-cycle totals and the design's own real financial-confrontation line ("Still $X to
  fund this cycle. Last cycle, $Y went to cigarettes...") shown only while
  `totalShortfall > 0`. No new migration needed -- `smoke_log` (017) and `money_habits`
  (027) already existed with 0 real rows; logSmoke prices itself from
  `money_habits.unit_cost` where `log_source='smoke_log'` (never a hardcoded $8.40) and
  logs at $0 with an honest warning when no such habit is configured yet. Verified
  directly against `money.mjs` with real writes/reads against the real DB (real user
  `shanemccaw@gmail.com`): a `logSmoke` call before any habit exists logged at $0 with the
  warning; setting the real "Cigarettes" habit ($148/cycle, $8.40/pack, matching the
  design's own stated values) via `setHabit` then priced a second `logSmoke` call
  correctly; `getGateStatus().smoking` showed the right cycle window
  (2026-08-28..2026-09-11, derived from the real `income_sources` row), correct running
  totals, and the correct confrontation-line text given the real live shortfall
  ($7,933.70). Test smoke_log rows created during verification were deleted afterward;
  the real "Cigarettes" habit row (a legitimate real configuration matching the design's
  own values, not fixture data) was left in place. Rebased twice onto `origin/main` to
  pick up concurrent #3150 (Vault) and #3152 (Budget Day enrichment) work landing on
  `money.mjs`/`api.mjs` at the same time -- both were additive, non-overlapping hunks,
  resolved by keeping both sides; re-verified after each rebase. Pushed directly to
  `main` (9f02df3bc), confirmed merged via `verify-branch-merged.mjs`. Working tree
  clean. No findings filed -- nothing broken or out-of-scope was discovered.
