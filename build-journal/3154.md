# #3154 — Feature: Smoking tracker (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** TBD at DONE

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — #3137 (blocker) is CLOSED/complete, unblocked. Real
  scope per issue body + design contract pack Section 3 + prototype logic
  (`Shanes Life - First Slice Prototype.dc.html`): one-box capture ("smoked" / "bought a
  pack") logs to the real `smoke_log` table (migration 017, already exists, 0 rows in the
  real `finances` DB), running real per-pay-cycle spend total, and the real
  financial-confrontation line on the Money "Now" tab shown only while a real shortfall
  exists. `money_habits` (migration 027) already models the recurring habit cost but no
  code path writes to `smoke_log` or surfaces cycle totals yet — building that now:
  `logSmoke`/cycle-window helper in `src/core/money.mjs`, `POST`/`GET /api/money/smoke`,
  and an `log_smoke` MCP tool, wired into `getGateStatus`'s existing response.
