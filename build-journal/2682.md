# #2682 — Detected panel: open in-app not browser, add Dispatch/Dismiss/Send-to-discuss per item

- **Status:** ✅ DONE 2026-09-03T16:39:09Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 8da3fedad34bb9a707c2aa42e4902519a395b8f9 (real work), 8bff529d8 (bookend IN FLIGHT)

## Log
- 2026-09-03T16:32:25Z ⏳ IN FLIGHT — read issue + comment, starting on the four real gaps: in-app issue open (OpenGitDetailByNumberAsync), Dispatch button (reuse DispatchPanel's dispatch path), Dismiss button (wire the existing-but-unwired `_dismissed` field), Send-to-discuss button (SendToChatAsync).
- 2026-09-03T16:39:09Z ✅ DONE — all four landed in `8da3fedad`:
  1. `ChatDocumentContainer.RenderDock`'s `onOpenIssue` now calls `_owner.OpenGitDetailByNumberAsync(n)` instead of `Process.Start` against a hardcoded github.com URL. Same fix applied to `FloatingChatWindow`'s own side dock (`ChatDock.Render`), the other real caller of `ChatDockPanel.Render` — it had the identical browser-open bug and would not have compiled against the new `Render` signature otherwise.
  2. Extracted the real fetch/build-comment/dedup/queue mechanics out of `DispatchPanel.DispatchAsync` (#1779) into a new `Services/IssueDispatchService.DispatchAsync` — `DispatchPanel` itself now calls it (behavior-preserving refactor, force-redispatch #1966 flow untouched since it has no second caller). A new "Dispatch" button per `ChatDockPanel` item card calls the same service.
  3. Wired the existing-but-unwired `_dismissed` `HashSet<string>` field: `ChatDockPanel.Render` now takes a `dismissed` set and filters `data.Items` against it before rendering; a new "Dismiss" button per card adds the item's number and re-renders (cached `_lastDockData`, no re-fetch); `_hasUndismissed`/glyph dot recomputed on dismiss. `FloatingChatWindow` got its own per-tab `Dismissed` set for the same fix on its dock.
  4. New "Send to discuss" button per card calls `_owner.SendToChatAsync(_chat, "Let's discuss #{n} — {title}")` — lands in the chat's own composer as a draft, never auto-sent, same convention `onResolvePin` already uses.
  - Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
  - `verify-branch-merged.mjs` confirmed merged into `origin/main`; pushed `agent/2682-q1432` HEAD directly onto `main` (fast-forward), then deleted the now-redundant remote branch.
  - `git status --porcelain` clean.
