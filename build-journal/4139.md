# #4139 — Break-glass Access: 'What is waiting' panel ACCOUNT identity field (customer read)

- **Status:** ✅ DONE 2026-09-15T00:02:55Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 39efd583d (work, on origin/main); IN FLIGHT bookend c6822af7a

## Log
- 2026-09-14T23:53:10Z ⏳ IN FLIGHT — serve the break-glass account identity on the customer-scoped
  by-run read (extending #4030's MSP-side work), render the ACCOUNT cell on
  `artifacts/portal/src/pages/break-glass-status.tsx`, live-verify tenant scoping.
- 2026-09-15T00:02:55Z ✅ DONE — `GET /api/portal/break-glass/by-run/:runId` now serves
  `breakGlassAccountId` (`secret.breakGlassAccountId`, bound to the secret row
  since #4015) on the pending shape. Added a second access gate: the route
  authorized on `run.payload.customerId` but served the secret row picked by
  `runId` alone, whose own `customerId` comes from the gate's configurable
  `customerIdField`. The caller must now also pass `assertCustomerAccess` on
  `secret.customerId`, else 404, the same gate the invite route uses.
  Portal: `BreakGlassRunStatus` type + ACCOUNT cell on "What is waiting"
  (design lines 63-79, copy verbatim, "—" for pre-#4015 rows, testid
  `break-glass-account`); header comment updated. Contract pack §2.2 row added
  (`docs/portal/break-glass-access-contract-pack.md`).
  Typecheck: api-server and portal `tsc --noEmit` both 0 errors.
  Live verify, isolated :8099 built from this worktree, local DB, synthetic
  `zz-test-4139-*` Customer users on tenant 1 / tenant 3, secrets carrying
  `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com`: A→own run 200 with the
  identity; B→A's run 404; A and B→payload/secret-customer mismatch run 404;
  no token 401. For comparison, pre-change :8080 served the mismatch row
  (tenant 3's secret 59) to tenant-1 user A with 200, which is the leak the new
  gate closes, so it is fixed here, not filed. All synthetic rows (users, sessions,
  role rows, login audit rows, runs, secrets) deleted; 0 remain.
  No migration (column already exists since #4015). No test manifest:
  `artifacts/portal` is outside the manifest scope (msp-portal /
  shane-mccaw-consulting). No findings filed. Tree clean.
