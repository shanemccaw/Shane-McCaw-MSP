# #2633 — Data Rights and Privacy: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T05:40:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 1c575a7e9

## Log
- 2026-09-12T05:05:00Z ⏳ IN FLIGHT — Design export (`Data Rights.dc.html`, README screen 16) and contract pack
  (`docs/msp-console/data-rights-and-privacy-msp-console-contract-pack.md`) confirmed real; blockers #2632
  (design) and #2668 (scaffolding) both closed. Backend `msp-data-rights.ts` (3 routes, all orphaned per the
  pack) is finished/tested. Building the per-tenant Data Rights module page (`/tenants/:id/dr`, nav id already
  registered) mirroring the Webhooks/Break-glass module pattern: Activity tab (MSP-wide feed filtered
  client-side to this tenant) + People tab (customer's linked users) + a deletion-request drawer wired to the
  real POST endpoint. No fixture data.
- 2026-09-12T05:40:00Z ✅ DONE — Shipped `artifacts/msp-console/src/api/data-rights-api.ts` (real hooks against
  `GET /api/msp/data-rights`, `GET .../customers/:id/users`, `POST .../deletion-request`) and
  `src/console/modules/DataRights.tsx` (Activity + People tabs, deletion-request drawer, filed confirmation,
  real 403/404/loading/empty states), wired into `ConsoleShell.tsx`'s `moduleFor` for `sel.page === "dr"`, plus
  the `user-x` icon. No backend changes — `msp-data-rights.ts` was already finished/tested. Two honest,
  documented departures from the design's fixture mock (in the module's own header comment): the real activity
  feed never returns the target user's identity, so People has no per-person request count; and there is no
  endpoint to preview a user's data footprint before filing, so the picker drawer's pre-submission schema
  preview was dropped in favour of showing the real `CurrentSchemaSummary` in the post-submission confirmation,
  where that data genuinely exists. Verified: `pnpm typecheck` (clean), `pnpm build` (clean, `PORT` env
  required — pre-existing, unrelated to this change), `pnpm test` (16/16 existing nav/tree tests pass,
  unaffected). Commit `1c575a7e9`, pushed to `main` and confirmed merged
  (`node scripts/dev-server/verify-branch-merged.mjs`). Working tree clean. No findings filed — nothing
  discovered outside this issue's own scope. No Shane To-Do action.
