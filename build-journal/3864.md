# Bookend — Git #3864

- **Status:** ✅ DONE 2026-09-12T22:40:04Z
- **Scope:** buildconsole

## Log
- 2026-09-12T22:35:30Z — Session started. Mirror the real queue counts
  (Queue/Up Next/Active/Blocked/Verifying/Total) from
  `BuildQueuePanel.UpdateQueueStatusCounts()` into a new segment on
  `MainWindow.xaml`'s bottom status bar. Plan: add a
  `QueueStatusCountsChanged` event on `BuildQueuePanel` (same idiom as
  `CappedCountChanged`), raise it alongside the existing
  `QueueStatusCountsText.Text` assignment, and subscribe from
  `MainWindow.xaml.cs` to set a new `GlobalQueueStatusText` TextBlock added
  to the StatusBar, positioned right after the Deploy/Version segment.
- 2026-09-12T22:40:04Z — ✅ DONE — commit `91c026680` ("Global status bar:
  mirror real queue counts (Git #3864)"). Added
  `BuildQueuePanel.QueueStatusCountsChanged` event (same idiom as
  `CappedCountChanged`), raised at the end of `UpdateQueueStatusCounts()`
  with the exact rendered `Text`/`Provisional`/`ToolTip` already applied
  to the panel's own `QueueStatusCountsText` — no second computation.
  `MainWindow.xaml` gained a new `GlobalQueueStatusText` StatusBarItem
  right after the Deploy/Version segment (existing `ColumnDefinition`s
  renumbered accordingly); `MainWindow.xaml.cs` subscribes and mirrors
  foreground/tooltip identically for the Provisional/asterisk state.
  Click reuses the existing next-to-run popup via a new public
  `BuildQueuePanel.ToggleQueueStatusPopupAsync()` (factored out of
  `QueueStatusBorder_Click`, no duplicated popup logic).
  Verified: `dotnet build BuildConsole.csproj -c Debug` — 0 warnings,
  0 errors. Pushed to origin/main as `41e181648`; branch confirmed an
  ancestor of `origin/main` via `verify-branch-merged.mjs`. Working
  tree clean at finish. No findings filed — this was a straightforward
  mirror of already-computed data, exactly as scoped.
