# #2339 — Copy-as-markdown for the selection

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (ShaneBuilder, desktop/ShaneBuilder/TestPad)
- **Started:** 2026-09-02
- **Commit(s):** 049b0390b (merged to main; rebased from 3bf9a0b79 on agent/2339-q1278)

## Log
- 2026-09-02T19:44:00Z ⏳ IN FLIGHT — Test Pad issue 13/28 (`ShaneBuilder-FullFeature.md`).
  Adding a "Copy as markdown" action next to Send to Claude that formats the
  currently-checked (`TestPadNote.IsSelected`) notes via `TestPadSendFormatter` and
  puts them on the clipboard, distinct from Send to Claude's unsent-only/fallback-to-all
  behavior — this one operates strictly on the selection, per the issue title.
- 2026-09-02T19:58:00Z ✅ DONE — Added a header action, `BtnCopyMarkdown`
  (`TestPadWindow.xaml`), collapsed unless at least one note is checked, labeled
  "Copy as markdown (N)". Click handler (`BtnCopyMarkdown_Click`,
  `TestPadWindow.xaml.cs`) reads exactly `TestPadService.Notes.Where(n => n.IsSelected)`
  (any mix of sent/unsent — no fallback to "every unsent" like Send to Claude has),
  reuses the existing `TestPadSendFormatter.Format` for the same stamped markdown
  block shape #2337 already produces, and puts it on the clipboard via
  `System.Windows.Clipboard.SetText`, with a success/warning toast. `dotnet build`
  of `desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors. Rebased onto
  `origin/main` and pushed straight to `main` (049b0390b);
  `verify-branch-merged.mjs` confirms ancestry. `git status --porcelain` clean.
