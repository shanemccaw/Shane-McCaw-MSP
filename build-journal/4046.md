# #4046 — Privileged/cross-boundary read auditing (break-glass, cross-tenant, exports)

- **Status:** ✅ DONE 2026-09-14T15:15:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8345e3d8f

## Log
- 2026-09-14T14:05:00Z ⏳ IN FLIGHT — pulled #4046 + #1946 architecture thread + #4044's schema/audit.ts; locating real routes in the four categories (break-glass #1651, MSP operator reading a customer's data, data export/download, other tenant-boundary-crossing reads) before wiring `createAuditLog` into each.
- 2026-09-14T15:15:00Z ✅ DONE — Added `resolveAuditActorRole`/`auditPrivilegedRead` to `artifacts/api-server/src/lib/audit.ts` (built against `@workspace/db/rbac`'s `effectiveLegacyRole` directly, not `requireAuth.ts`'s wrapper, so it survives the many existing tests that mock `requireAuth.ts` down to just `requireAuth`/`requireCapability`). Wired audit calls into: the real break-glass secret-reveal moment (`break-glass-verification.ts`'s public OAuth callback, attributed to the verified Entra UPN — no `req.user` exists there); ~35 MSP-operator-reads-a-specific-customer's-data GET routes across ~28 `msp-*.ts` files (incl. the live diagnostics SSE stream open, audited once per connection, not per event); ~14 data export/download routes (`portal-customer-engines.ts`, both remediation-tracker-export files, `dashboard-export.ts`, `live-document-pdf.ts`, `admin-document-downloads.ts`, `msp-documents-hub.ts`); and 8 PlatformAdmin cross-tenant reads in `admin-active-directory.ts` + `admin-document-generator.ts`. Grep-verified `audit_logs` stays append-only (no write/update/delete route anywhere). Used 4 parallel background agents for the bulk of the MSP-operator batch, reviewed their diffs, then did category 1/3/4 + the SSE route myself. Real regression found and fixed during verification: two existing tests (`msp-active-directory.test.ts`, `msp-team-invite.test.ts`) had partial `lib/audit.ts` mocks (only `createAuditLog` stubbed) that broke once the new routes called `auditPrivilegedRead`/`resolveAuditActorRole` — extended both mocks. `tsc --noEmit` clean; both fixed suites green; the rest of a full `vitest run` showed only pre-existing DB-connectivity/environment noise unrelated to this change (confirmed via isolated re-runs of the touched-route test files). Pushed to `origin/main` at 8345e3d8f after two real rebase conflicts against concurrent sessions that also touched `break-glass-verification.ts`, `msp-break-glass.ts` and `msp-azure-credentials.ts` — resolved by merging both sides' `lib/audit.ts` imports (each side needed different named exports from the same module). Filed #4072 (parented to #1946) for the one honest gap: category 4 (PlatformAdmin cross-tenant reads) was only checked against 2 of ~140 `admin-*.ts` files; also documented two judgment calls (a public unauthenticated document-share consumption route, and one ambiguous customer-vs-operator clickwrap-status route) left unaudited pending a real decision.
