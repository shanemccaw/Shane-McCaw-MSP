# Issue #2864 — vi.clearAllMocks() queued-mock reset footgun

- **Status:** ✅ DONE 2026-09-04T16:35:00Z
- **Scope:** platform

## Log
- 2026-09-04T14:25:00Z — Started. portal-checkout.test.ts already carries the
  `.mockReset()` fix + explanatory header comment (landed under #1772,
  70e89493a). Confirmed via git log. Remaining ask per issue body: grep the
  rest of the suite for the same `vi.clearAllMocks()`-only-with-chained-once-mocks
  footgun and confirm/fix.
- 2026-09-04T16:00:00Z — Ran a 3-way parallel audit of every `*.test.ts` file
  using `vi.clearAllMocks()`. One genuine real-risk file found:
  `artifacts/api-server/src/routes/support-chat.test.ts` — `mockDbAny["limit"]`
  gets 8 chained `.mockResolvedValueOnce()` calls in one test ("surfaces a
  data card…"), with no `.mockReset()` anywhere in the file across 4
  `beforeEach` blocks. Fixed by adding `.mockReset()` before each block's
  default `.mockResolvedValue()`, same pattern as the #1772 fix. Verified:
  `vitest run src/routes/support-chat.test.ts` → 31/31 passing.
  Two borderline files (`msp-custom-domain.test.ts`,
  `msp-customers-bulk.test.ts`) reviewed and found currently balanced
  (once-queues always fully drained per test) — not touched.
- 2026-09-04T16:15:00Z — While re-verifying, found `portal-checkout.test.ts`
  (the file cited in #2864's issue body as the reference "already fixed"
  example) now fails 8/10 tests on unmodified `main` — pre-existing,
  unrelated to the mock-reset-gap pattern this issue is about. Confirmed via
  `git stash` isolation that I made zero changes to that file. Filed as
  #2876, parented under #2491 (Feature: CI/Test Infra Health), board status
  set to "AI Batter Up".
- 2026-09-04T16:35:00Z — Merged to `origin/main` via rebase+push:
  `709e547da` (support-chat.test.ts fix, commit `0793f5109` rebased on top
  of main). `verify-branch-merged.mjs` confirms merged. `git status
  --porcelain` clean.
