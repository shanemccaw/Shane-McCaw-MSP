# #4837 — resolveDriftEvents reports a not_comparable drift domain as clean when an older baseline exists

- **Status:** ✅ DONE 2026-09-19T05:29:32Z
- **Scope:** platform
- **Started:** 2026-09-19
- **Commit(s):** 5274c261e (fix + tests); plan d492dfd2a; IN FLIGHT 4e7ab261f

Dispatch: `BUILD:` comment Posted 2026-09-19T00:40:00Z (build queue id 3406). Plan: [4837-plan.md](4837-plan.md).

## Changes Made
- `artifacts/api-server/src/lib/dashboard-resolvers.ts` — `resolveDriftEvents` now reads `drift_collection_status` (unique per tenant+domain, upserted every run = the latest verdict) **first**. If `driftStateFromStatus` (shared with #4578's `pillar-drift.ts`) is not `tracked`, it returns `not_available("not_comparable" | "collection_error")` with the collector's real reason, regardless of an older baseline. The baseline / no_data / events logic below is unchanged. Doc comment updated.
- `artifacts/api-server/src/lib/dashboard-resolvers-drift.test.ts` (new) — 9 tests: baseline+not_comparable, baseline+error, missing reason, no-baseline+not_comparable, no-baseline+no-status (no_data), tracked+clean, baseline_captured+events, baseline without a status row.
- No schema change, no migration, no fixture data. Manual-SQL migration: none.

## Verification Results
- Vitest (`dashboard-resolvers-drift`, `dashboard-resolvers`, `pillar-drift`): 36/36 pass, re-run after the final rebase. Against the pre-fix resolver the new baseline+not_comparable / baseline+error tests fail (confirmed).
- `tsc --noEmit` (api-server): no errors in `dashboard-resolvers.ts` / `pillar-drift.ts`.
- **Live, real local DB, tenant c4c814d4 (customer 2080)** via `resolveMetric`: `drift.caPolicyDriftCount` → `not_available / not_comparable / gate_not_satisfied` (was `ok`, zeroRows) even though a 2026-09-16 baseline is on file. The four tracked domains (email-authentication, eeeu-site-sharing, public-teams-discoverable, tenant-sharing-capability) still → `ok`, `hasBaseline: true`. The four other registry drift metrics with no baseline still → `no_data`.
- Other consumers: the only caller of `resolveDriftEvents` is `resolveMonitorProfile` (`drift:*`); `not_available` with these reasons was already a possible return (the no-baseline branch), and the portal `useHltDriftLive.ts` maps `not_available` explicitly (L166–L194). No consumer needed a change.
- Not run: `shaneapp://runTest` — the change is api-server-only (nothing under `artifacts/portal/` or the marketing site), so no manifest applies.
- Findings filed: none. `git status --porcelain` clean; branch merged into origin/main (`verify-branch-merged.mjs` exit 0).

## Log
- 2026-09-19T05:16:30Z ⏳ IN FLIGHT — read issue body + the one comment; about to write 4837-plan.md, then fix `resolveDriftEvents`.
- 2026-09-19T05:29:32Z ✅ DONE — status-first `resolveDriftEvents` shipped in 5274c261e; live-verified on tenant c4c814d4; 36/36 tests.
