# Build Journal — Issue #3898

- **Status:** ✅ DONE 2026-09-13T00:10:00Z
- **Scope:** platform
- **Log:** Fixing TS2352 cast error in auth-impersonation.test.ts:53 (Promise<never[]> to intersection type).
- **Log:** ✅ DONE 2026-09-13T00:10:00Z — Fixed by typing the `Promise.resolve` call explicitly
  (`Promise.resolve<unknown[]>([])`) instead of relying on `Promise.resolve([] as unknown[])`,
  which cleared the tsc overlap-check error on the widening cast. `npx tsc -p tsconfig.json
  --noEmit` in `artifacts/api-server` now runs clean for this file. Commit fecd0f55 (rebased
  and merged onto origin/main); verified merged via
  `node scripts/dev-server/verify-branch-merged.mjs`.
