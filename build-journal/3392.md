# #3392 — MCP tools: sub-issue hierarchy + blocked_by dependencies

- **Status:** ✅ DONE 2026-09-10T17:44:00Z
- **Scope:** platform (artifacts/github-mcp-server)
- **Started:** 2026-09-10
- **Commit(s):** a2509a1d4 (rebased/pushed as 26d9790da on origin/main)

## Log
- 2026-09-10T17:32:00Z ⏳ IN FLIGHT — #3390 (server scaffold) confirmed landed and closed `complete`
  (PR/commits merged, `artifacts/github-mcp-server` scaffold present with `server_status`,
  `github_whoami`, `get_recent_activity` tools). Unblocked #3392 (removed `blocked` label, no
  `blocked_by` edges remain). Implementing `add_sub_issue`, `remove_sub_issue`, `list_sub_issues`,
  `set_blocked_by`, `list_blocked_by` as new `ToolDef`s per the established scaffold pattern.
- 2026-09-10T17:44:00Z ✅ DONE — Shipped all five tools:
  - `add_sub_issue(parent_number, child_number)` / `remove_sub_issue(parent_number, child_number)`
    / `list_sub_issues(number)` — resolve the real internal `id` server-side, caller only ever
    passes issue numbers.
  - `set_blocked_by(number, blocker_numbers[])` — a true reconcile (adds missing edges, removes
    stale ones not in the list), not just an append, per the CLAUDE.md Git #1987 rule that a stale
    `blocked_by` edge silently reads as "clear."
  - `list_blocked_by(number)` — real current blockers + live state.
  `src/github.ts` gained repo-scoped helpers (`getIssueSummary`, `listSubIssues`, `addSubIssue`,
  `removeSubIssue`, `listBlockedBy`, `addBlockedBy`, `removeBlockedBy`) built on the existing
  `githubRequest()`, using the shared `githubRepo()` env helper (landed concurrently by #3394's
  session — resolved three separate rebase conflicts against #3394/close_issue and #3393/
  post_comment+list_comments landing in the same window; merged cleanly, all five tool sets now
  coexist in `ALL_TOOLS`).
  **Real verification:** `npm run typecheck` clean; `npm run smoke` (scaffold suite) passed;
  wrote a disposable end-to-end script that called `addSubIssue`/`listSubIssues`/`removeSubIssue`/
  `addBlockedBy`/`listBlockedBy`/`removeBlockedBy` directly against two real scratch issues
  (#3536 parent/blocker, #3537 child/blocked) created for this purpose — all 5 assertions passed
  against live GitHub (sub-issue add/list/remove, blocked_by add/list/remove-to-empty). Scratch
  script deleted after running; both scratch issues closed `not_planned` with an explanatory
  comment in the same session (real verification artifacts, not product issues).
  `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/3392-q2111` (26d9790d)
  is an ancestor of `origin/main`. No new findings to file — the scaffold and repo helpers were
  solid; only the sub-issue/dependency surface was missing, and it's now built.
