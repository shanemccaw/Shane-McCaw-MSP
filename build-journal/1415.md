# #1415 — Compliance sub-pages: audit all 4 routes for unconditional fixture fallback

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** (this file's own commits; investigation-only, no product code changed)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Auditing /portal-v2/compliance/open-gaps, /decisions,
  /obligations, and /:area (PortalV2ComplianceAreaPage) for `?? FIXTURE.field`
  fallback patterns and elements rendering outside/after a dataState conditional,
  same methodology as #1413/#1414.
- 2026-08-27 ✅ DONE — Investigation-only, no code changed. Audited all 4 routes.
  `open-gaps` and `decisions` are clean (real dataState-gated live rows, honest
  empty states, no unconditional fixture). `obligations` needs a fix (HIGH): the
  entire `CMP_OBLIGATIONS` register renders 100% unconditionally with zero
  dataState gating — a real, unmasked fixture leak. `:area`
  (`PortalV2ComplianceAreaPage`) needs a fix (HIGH): the 6 finding-backed cards'
  `FindingBlock` narrative body is always `CMP_FINDINGS` fixture, unconditionally,
  even for the 3 cards `cmpAreaWiring.ts` classifies as `nodata` — so the page
  shows an honest "Not measured" status pill directly above a fully fabricated
  finding body (specific invented mailbox/item/document counts), a direct
  on-page contradiction. Full findings posted to the GitHub issue comment.
