# #1454 — Governance oversharing bands (already fixed by #1426)

- **Status:** ✅ DONE
- **Scope:** platform
- **Commit(s) verified against:** c11e60b90 (pre-existing fix, from Git #1426)

## Log
- Pulled #1454's full issue body: it re-states the exact same real finding from
  #1413's investigation-only session ("blank visible content" call correctly
  deferred to Shane) — Stat cards band and Top Risks band need honest no-data
  (no backend exists for either), and the Anonymous Links panel needs a real
  schema/mapping decision proposed before wiring against the live #1275
  `useOversharingItemsLive` endpoint (no expiry/active-vs-expired field exists
  on `portal-oversharing-items.ts`'s wire row today).
- Read `artifacts/msp-portal/src/pages/portal-v2-gov-oversharing.tsx` in full.
  All three asks are already done, landed in commit `c11e60b90` ("Governance
  oversharing: honest no-data for stat cards, Top Risks, Anonymous Links (Git
  #1426)"), which predates this session:
  - Stat cards band (`pv2-ovr-stats`) renders `NoScanDataState` — "No live
    sharing-posture data available."
  - Top Risks band (`pv2-ovr-top-risks`) renders `NoScanDataState` — "No live
    top-risks data available."
  - Anonymous Links panel (`pv2-ovr-links`) renders `NoScanDataState` — "This
    panel needs an expiry/active-status mapping decision before it can wire to
    the real oversharing-items feed."
  - The schema-mapping proposal #1454 asks for is already written into the
    code comment directly above the panel (lines 690-701): the real endpoint
    exists (#1275, already powers `-all.tsx`'s paginated read) but its wire row
    has no expiry/`active`-vs-`expired` field, and "Edit vs View" would have to
    be inferred from `grant.roles` rather than a direct field — flagged as a
    genuine schema/product decision, not a mechanical rewire.
- Confirmed `c11e60b90` is an ancestor of `origin/main` (`git merge-base
  --is-ancestor c11e60b90 origin/main` → 0), and confirmed #1426 (the issue
  that fixed this) is already closed with the `complete` label.
- No code changes made in this session — #1454 is a duplicate of already-shipped
  work. Restating the proposed Anonymous Links mapping decision (from the
  existing code comment) in the issue comment for visibility, since #1454 asked
  for it explicitly.
- ✅ DONE — no code changes required; issue was already resolved by prior work
  under Git #1426, merged to `main` before this session started.
