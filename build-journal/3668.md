# #3668 — MSP Console: no contract pack exists for the Break-glass module (real backend, undocumented)

- **Status:** ⏳ IN FLIGHT 2026-09-11T18:33:25Z
- **Scope:** platform
- **Started:** 2026-09-11

Leaf issue found during the #3665 design freshness audit (row 2 and row 17 of
`docs/msp-console-design-audit-3665.md`), under Feature #2564 ("Feature: Break-glass
Access (MSP Console)"), Epic #1571 (Portal Admin). Real backend confirmed already built
and correct — `artifacts/api-server/src/routes/msp-break-glass.ts` (5 operator routes)
and `break-glass-verification.ts` (shared `resolvePendingContext`/
`performBreakGlassAdminOverride`). Task: extract the real contract pack, matching the
#1642 pattern (regenerate/extract fresh from current code, cite file:line, no invented
data).

## Log
- 2026-09-11 ⏳ IN FLIGHT — Pulled #3668 body + BUILD comment. Found a real prior-art
  wrinkle worth recording: `docs/portal/break-glass-access-contract-pack.md` (#2628,
  under this same Feature #2564) already documents BOTH the customer/portal side
  (§2) AND the MSP-console/operator side (§3, all 5 routes in `msp-break-glass.ts`) —
  read in full, confirmed current against the live route file (line numbers match
  exactly). So the *backend* is not undocumented anywhere; the gap is narrower than
  the issue title implies: no pack exists **specifically under `docs/msp-console/`**,
  which is the one-pack-per-MSP-Console-module directory the design audit (#3665 rows
  2 and 17) checks against. Proceeding to write a focused
  `docs/msp-console/break-glass-msp-console-contract-pack.md` — MSP-console surface
  only, citing the existing portal pack for the shared backend pieces
  (`resolvePendingContext`, `performBreakGlassAdminOverride`, the gate node) rather
  than re-deriving/duplicating them, same discipline the tenant-scores pack used
  against the customer dashboard route. Also read the design's own Break-glass screen
  (`dc.html:2838-2898`) and the Tenant Overview `bgSecrets` tile/row
  (`dc.html:3151,3167`) to confirm the design's `wire:` claims against the real code.
  Read-only on product code; only the new pack + this bookend change.
