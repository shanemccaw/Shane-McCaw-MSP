# #4693 — BuildConsole: repoint Git Board IsInFlight/IsComplete reads off labels

- **Status:** ✅ DONE 2026-09-18T17:44:31Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** c9fdc60aab314d4ecd7f8c3aa0653b3cd1b133c6 (code); 5a0564bb7 (IN FLIGHT bookend); 0ad2b4c75 (plan, `build-journal/4693-plan.md`)

## Log
- 2026-09-18T17:32:43Z ⏳ IN FLIGHT — reading #4693 + #4686 audit, re-verifying call sites, then writing build-journal/4693-plan.md before any code change.
- 2026-09-18T17:44:31Z ✅ DONE — Git Board in-flight now read from local queued/running `bt_build_queue` rows, done from the issue's real closed state; In-Flight tile is local-only. Build 0 warnings / 0 errors (twice, incl. post-rebase). Live UI click-through NOT done (never-launch-BuildConsole rule) — see Verification Results. Filed issues: none (no new genuine finding; the Mark Complete menu item interaction is already #4694's explicit scope).

## Changes Made

- **New** `desktop/BuildConsole/Services/LocalQueueActivity.cs` — process-wide snapshot of issue numbers with a `queued`/`running` local build (repo-scoped via `QueueItem.OwnerRepo`); `Update()` raises `Changed` only when the set changes.
- `Services/GitHubApiClient.cs` — `GitBoardIssue.IsComplete => IsClosed`; deleted both `HasInFlightLabel` properties.
- `Controls/BuildQueuePanel.xaml.cs` — `RefreshAsync` feeds `LocalQueueActivity.Update(_lastItems)`; `RefreshInFlightIssuesAsync` rebuilt from local queue rows + `bt_issue_mirror` (title / state / immediate parent) — the `gh issue list --label in-flight` call is gone; tile re-renders on every real queue change.
- `Controls/LeftSidebar.xaml.cs` — all `HasInFlightLabel` / inline `"complete"` reads repointed (`LocalQueueActivity.IsInFlight` / `IsClosed`), including the search-result "In Flight" badge (`:8000`, not in the issue's list — found on re-verify); subscribes to `LocalQueueActivity.Changed` to re-stamp cached nodes and re-render, so the board tracks a launching/finishing build and catches up if built before the first queue load.
- `Controls/GitDetailView.cs` — dropped the `in-flight` / `complete` chip colour arms (real OPEN/CLOSED `StatePill` already exists).
- `GitBoardPanel.xaml.cs` — no edit needed (reads the `GitIssue.IsInFlight` flag, whose source changed).
- Untouched by design: `blocked` / `Shane To-Do` reads; the label **writers** and the broken "Mark Complete" menu item (#4694); dead code (#4696). No migration, no schema change, no fixture data.

## Verification Results

- `dotnet build desktop/BuildConsole/BuildConsole.csproj`: **Build succeeded, 0 Warning(s), 0 Error(s)** — run after the edits, again after the final comment fix, and again after rebasing onto `origin/main` (which had picked up #4695/#4696).
- Live data cross-check against the real local `BuildConsole` db (`bt_build_queue`, the exact predicate the app uses): `status IN ('queued','running') AND github_number IS NOT NULL` returned **4 rows — #4693 running (this very build, queue id 3293), #4697 running, #4694 queued, #4698 queued**. The OLD source, `gh issue list --state open --label in-flight`, returned **36 issues** (e.g. #1494, #1641, #2243–#2262, #3375 …) — the label was badly stale, which is the case for this change.
- `bt_issue_mirror` rows for the tile lookup: #4693 open, parent #4692; #4694 open, parent #4692 (immediate-parent grouping data present).
- Closed-issue source: `gh issue view 4686` → `CLOSED`, `#4693` → `OPEN` (what `IsClosed`/`IsComplete` now read).
- Residual label grep over `Controls/*.cs` + `GitHubApiClient.cs`: only comments and the Mark Complete handler (`LeftSidebar.xaml.cs` `ToggleLabelAsync(... "complete" ...)`, #4694's deletion target).
- `verify-branch-merged.mjs`: OK — `agent/4693-q3293` (c9fdc60a) is merged into `origin/main`.
- **Not verified:** a live click-through of the Git Board / In-Flight tile in the running app. Per the never-launch-BuildConsole rule (Git #2156) I did not launch the exe or drive its UI. The FOCUS/ACTIVE/ancestor-marker path was verified by reading the wiring end to end (`BuildBoardFromGitHub` and `OnLocalQueueActivityChanged` → `GitIssue.IsInFlight` → `RenderIssuesTreeAsync` ancestor walk → `GitBoardPanel.SetLiveMilestones`), plus the DB cross-check above. Shane: please eyeball the board once — running builds #4693/#4697 should show the amber dot, queued #4694/#4698 too, and the stale 2243–2262 issues should no longer.
- Known transient: until #4694 lands, the (already non-functional, HTTP 410) "Mark Complete" menu item reads "Remove 'complete' label" on a closed issue because `GitIssue.IsComplete` is now "closed".
