# #3663 — Chats panel epic progress bar: read transitive leaf-issue rollup from local bt_issue_mirror, never live GitHub

- **Status:** ✅ DONE 2026-09-11T17:17:02Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** a3025f4c0

## Log
- 2026-09-11T17:10:57Z ⏳ IN FLIGHT — investigated `LeftSidebar.xaml.cs`'s `EpicProgress`/`ResolveRollup`/
  `ComputeTransitiveLeafRollup`/`_lastAllIssuesForRollups`, confirmed `bt_issue_mirror`'s schema carries
  `parent_number`/`milestone_number`/`state`/`child_issue_numbers` (structurally sufficient). Queried the
  local DB directly and confirmed a real, separate data-completeness gap: `bt_issue_mirror`'s closed-issue
  history is structurally incomplete (Git #3359 deliberately skipped a full ALL-states historical backfill
  to avoid GitHub rate limits) — #1096's own mirrored descendant tree is 68 rows / 27 closed vs. GitHub's
  live "58/77". Implementing the local-mirror-only rollup anyway (it's still strictly better than the
  fallback it replaces — never mistakes a Feature-container for "done", never fabricates, only undercounts)
  and filing the backfill gap as its own sibling finding under #1202.
- 2026-09-11T17:17:02Z ✅ DONE — replaced `EpicProgress`'s data source entirely with a local
  `bt_issue_mirror`-only ALL-states snapshot (`GitHubIssueMirror.TryGetBoardIssuesAsync(openOnly:false)`,
  cached in a new `_epicProgressMirrorIssues` field, loaded once at construction and refreshed on
  `GitHubIssueMirror.SyncCompleted`), feeding the existing `GitBoardIssueFilters.ComputeTransitiveLeafRollup`
  (#2739) — zero live GitHub calls, renders correctly/instantly on cold start, honest `(0,0)` fail-closed
  (hides the bar) when the mirror hasn't loaded or the epic isn't present in it. `ResolveRollup` /
  `_lastAllIssuesForRollups` (Git Board tree pills / GATE fraction / detail tabs) left untouched —
  out of #3663's scope, per the issue's own #4.
  **Real, honest verification result:** `dotnet build BuildConsole.csproj -c Debug` succeeds (0 errors)
  against the final pushed HEAD. Per CLAUDE.md's "never launch BuildConsole.exe to verify" rule and this
  WPF app being out of test-manifest scope, correctness was verified by directly re-running the exact same
  filter/walk logic (`CountsAsRealWork`/placeholder-exclusion/transitive descendant walk) as raw SQL against
  the real local Postgres `bt_issue_mirror` table: Epic #1096 → **27/46**, Epic #1485 (Portal) → **22/52** —
  both sane, non-degenerate real numbers, neither a Feature-container miscount.
  **Real, confirmed gap found and filed separately:** `bt_issue_mirror`'s closed-issue history is
  structurally incomplete by design (Git #3359's no-op backfill marker) — confirmed #1096 reads **27/46**
  locally vs. GitHub's own live-authoritative **58/77** the issue cites as "known-correct". This means the
  literal "spot-check #1096 == 58/77" verification bullet in #3663's own body cannot be met with a
  zero-live-call implementation until that backfill gap closes — filed as **#3678** (bug, milestone v1.1,
  board status AI Batter Up). Could NOT be linked as a sub-issue of #1202 via the GitHub API — #1202 is
  already at GitHub's hard 100-sub-issue cap (`subIssuesSummary`: 100 total / 77 completed) — noted via
  comment on both #3678 and #1202 instead.
  Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs`
  (OK, a3025f4c0). `git status --porcelain` clean.
