# #3499 — pcc.ts (/api/pcc/*) mounts with NO auth middleware at all

- **Status:** ✅ DONE 2026-09-10T22:45:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 6cd6c3a42

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — pulled #3499 body + comment. Confirmed live:
  every route in `artifacts/api-server/src/routes/pcc.ts` (catalog, state,
  environment, run, inject, replay/config, stream) has zero auth middleware.
  Found the real precedent: sibling admin route files gate on `requireAdmin`
  (`../middlewares/requireAuth.ts`); SSE routes that can't carry an
  Authorization header (EventSource) instead do a manual `jwt.verify` on a
  `?token=` query param + `role !== "admin"` check — see
  `admin-live-stream.ts` and `admin-projects.ts`'s `kanban-events` route.
  Applying both patterns to `pcc.ts` (requireAdmin on the 6 JSON routes,
  query-token verify on `/stream`), then wiring `PccDashboard.tsx` to
  `fetchWithAuth()` / `?token=` so the frontend actually sends credentials.
- 2026-09-10T22:45:00Z ✅ DONE — Shipped in `6cd6c3a42`. Added `requireAdmin`
  to `/catalog`, `/state`, `/environment`, `/run`, `/inject`,
  `/replay/config` in `pcc.ts`; added query-token `jwt.verify` +
  `role === "admin"` check to `/stream` (mirrors `admin-live-stream.ts` /
  `admin-projects.ts`'s `kanban-events`). Wired `PccDashboard.tsx`'s 8
  fetch calls to `fetchWithAuth()` and the SSE `EventSource` URL to
  `?token=<accessToken>`, gated the mount effect on `accessToken` being
  present. Extended `pcc.test.ts` with 10 new supertest cases (real HTTP
  requests through the actual router, not a mocked check): every route
  401s with no Authorization header, 403s for a non-admin (`role: client`)
  token, and 200s for a valid admin token; `/stream` specifically checked
  for missing-token and garbage-token 401s. Full suite: 19/19 passing.
  `tsc --noEmit` clean on both `api-server` and `admin-panel`. Branch
  verified merged into `origin/main` via `verify-branch-merged.mjs`.
  `git status --porcelain` clean. Filed #3551 (hardcoded `DEFAULT_TESTS`
  catalog, non-blocking, needs Shane's call — routed to "Ask Shane", not
  "AI Batter Up") as a sibling under #1095, per #3499's own "related
  observation" note.
