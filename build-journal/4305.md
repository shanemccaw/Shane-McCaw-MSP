# #4305 — Extract MyArchitect into shanemccaw/MyArchitect (simple file copy, NO git history surgery)

- **Status:** ✅ DONE 2026-09-15T21:34:25Z
- **Scope:** both
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect copy = `3e4663fb0f3dc5d990ae9e3dbf673eddb30b7e70` · Shane-McCaw-MSP removal = `bed1d630e709bcbd57f318765e1acf802e718a3a`

## Log
- 2026-09-15T21:30:16Z ⏳ IN FLIGHT — Replaces #4278 (its `git subtree split` walked 15,858 commits and crashed Shane's laptop; killed, not relaunching). Approach here is a PLAIN FILE COPY only: clone private `shanemccaw/MyArchitect`, copy current `desktop/MyArchitect/` files into it, carry over needed bits of `desktop/Directory.Build.targets` (metered NuGet gate), one normal commit, confirm standalone `dotnet build`, push, then remove `desktop/MyArchitect/` from Shane-McCaw-MSP in its own commit. NO `git subtree split`, NO `--allow-unrelated-histories`, NO full-history walk under any circumstances. Pre-flight: `shanemccaw/MyArchitect` confirmed PRIVATE.
- 2026-09-15T21:34:25Z ✅ DONE — All steps complete, verified real:
  - `shanemccaw/MyArchitect` PRIVATE confirmed **before** and **after** the move.
  - Copied via `git archive HEAD desktop/MyArchitect` (tracked files only — 154 app files, no bin/obj) into the clone; existing docs + `build-journal/` in that repo left untouched.
  - Carried the metered-NuGet-restore gate over as `Directory.Build.targets` at the repo root. Fixed the build-number git count (csproj `GenerateBuildInfo` target + runtime `Services/VersionInfo.cs`) to count this repo's own HEAD instead of the retired `desktop/MyArchitect` pathspec, keeping build-time and runtime numbers comparable standalone.
  - Standalone `dotnet build -c Debug`: **Build succeeded, 0 Warnings, 0 Errors.** Generated `BuildNumber = 2` = `git rev-list --count HEAD` — build-number wiring works standalone.
  - MyArchitect copy commit `3e4663f` pushed to `shanemccaw/MyArchitect` main (7d793e8..3e4663f). App source (`MyArchitect.csproj`, `MainWindow.xaml.cs`) confirmed present on remote main.
  - No external references to the `desktop/MyArchitect` path remained in Shane-McCaw-MSP (checked .sln/.json/.mjs/.cs/.ps1/.cmd). Removed the directory in its own commit `bed1d63`, pushed to main.
  - Multi-repo dispatch (#3578) accessibility check: `shanemccaw/MyArchitect` is reachable via the same authenticated GitHub credentials the dispatch path uses (`git clone https://github.com/shanemccaw/MyArchitect.git` succeeded; `gh api .../contents/*` returns the app source) and is PRIVATE at default branch `main`.
  - Working tree clean; branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0). No findings filed — nothing broken discovered.
