# Build Journal — #3687

- **Status:** ✅ DONE 2026-09-12T21:38:55Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Session started. Investigating 18 pre-existing adminv2 vitest failures across Shell.test.tsx, NoScreen.test.tsx, workflow.test.tsx.
- 2026-09-12T21:38:55Z — ✅ DONE — Reproduced the real failure set with
  `--pool=forks --fileParallelism=false` (full default parallelism hits an
  unrelated vitest-pool worker-spawn timeout on this worktree/machine, not a
  code issue): 8 real failures out of 525 tests, matching the issue's list.
  Root-caused all three clusters as stale tests, not shared-registry/global
  state pollution as the issue speculated:
  - `Shell.test.tsx` (3): `ShellContext` already auto-opens a `screen:<id>`
    doc tab for the current route on mount (a real, documented existing
    feature — see the file's own "falls back to the quick-nav Explorer..."
    test). Three tests never accounted for that tab colliding with an
    identically-labeled palette row. Fixed by scoping palette queries with
    `within(dialog)` and rewriting the "has no doc strip" test against a
    route with no registered screen.
  - `NoScreen.test.tsx` (4): `NoScreen` was rewritten into the real "ADHD
    Command Center" rollup (trail-backed Recent Work + SQL
    migrations/Inbox/Marketing cards); the test file still tested the old
    component's copy and its old "Search everything" button /
    generic-action-command quick starts, both of which moved to
    `StartSomethingExplorer` (covered by `Shell.test.tsx`). Rewrote the file
    to test the real, current component.
  - `workflow.test.tsx` (1): `runNow()` now explicitly sends
    `{ mode: "test" }` (server added real live-fire fan-out support,
    `POST /api/admin/workflows/definitions/:id/run` documents `mode`
    `"live"|"test"`, default `"test"`) instead of an empty body. Updated the
    assertion.
  - Commit: `ea68f9baf` (rebased onto `main` as `c9020417` when pushed).
  - Verified: full `src/adminv2` suite — 524/524 passing (was 525; two
    obsolete `NoScreen.test.tsx` assertions were consolidated into one
    accurate one) via
    `node ../../node_modules/vitest/vitest.mjs run --config vitest.config.ts src/adminv2 --pool=forks --fileParallelism=false`.
  - Branch merged into `origin/main` — confirmed via
    `node scripts/dev-server/verify-branch-merged.mjs` (OK, `c9020417`).
  - `git status --porcelain` clean.
  - No new findings filed — all three clusters were real, explainable stale
    tests with a clear real-code root cause; nothing left broken or unowned.
