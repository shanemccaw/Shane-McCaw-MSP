# #2327 — Pill bottom-right with unsent-count badge; expands to the pad

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 8d0437765

## Log
- 2026-09-02T19:42:30Z ⏳ IN FLIGHT — Sub-issue of #2326 (Feature: Test Pad), item 1 of 28.
  Building the Test Pad data model (TestPadService/TestPadNote), the floating
  bottom-right pill (always-on-top, unsent-count badge), and the expandable pad
  window shell. Composer/notes-list/import etc. are separate sibling issues
  (#2328-#2354) and out of scope here — this issue is the pill + expand mechanic only.
- 2026-09-02T20:05:00Z ✅ DONE — Shipped `desktop/ShaneBuilder/Services/TestPadService.cs`
  (static, thread-safe, event-driven note store mirroring AlertCenter's shape),
  `TestPadModels.cs` (`TestPadNote`), and `TestPad/TestPadPillWindow.xaml(.cs)` +
  `TestPad/TestPadWindow.xaml(.cs)` — an always-on-top, non-activating bottom-right pill
  (mirrors AlertStackWindow's positioning/topmost mechanics) showing a live unsent-count
  badge that hides at zero, click toggles the pad open/closed. The pad shell renders a
  plain per-note line (no select/delete/edit — that's #2333/#2335/#2336) and an honest
  "No notes yet." empty state. Wired into `MainWindow.xaml.cs` via `InitializeTestPad()`
  alongside the existing `InitializeAlertsAndCritters()` pattern.
  Reconciled with a concurrent sibling build: #2329 landed
  `Services/TestPad/NoteType.cs` + `NoteMarkerParser.cs` on `main` mid-session — rebased
  onto it and switched `TestPadNote.Type` to reuse that canonical `NoteType` enum instead
  of a second, drifting one I'd started with.
  Verification: `dotnet build` clean (0 warnings, 0 errors) after every rebase against
  concurrent pushes to `main` (#2329, then #2328's follow-on commits). No test manifest —
  desktop WPF apps are out of scope for those per CLAUDE.md.
  `verify-branch-merged.mjs` confirms merged into `origin/main`; `git status --porcelain`
  clean.
