# #1919 — accept-language: * trap in 3 modules beyond graph.ts

- **Status:** ⏳ IN FLIGHT 2026-09-04T07:03:18Z
- **Scope:** platform

## Log

- ⏳ IN FLIGHT 2026-09-04T07:03:18Z — Pulled #1919 body + comments (Feature parent #2489,
  buildId 1557). Target: 3 modules making raw Microsoft API `fetch` calls with no
  `Accept-Language` header, matching the fix already in `graph.ts:805` and
  `azure-rm.ts:233` per #393/#488:
  - `artifacts/api-server/src/lib/defender-endpoint.ts`
  - `artifacts/api-server/src/lib/sharepoint-connector.ts`
  - `artifacts/api-server/src/lib/power-platform-admin.ts`

  Scope note: matching `graph.ts`'s own precedent exactly — the header goes on the
  real data-API calls (Defender device actions, SharePoint Graph calls, BAP admin
  calls), not on the `login.microsoftonline.com` OAuth token-endpoint fetches;
  `graph.ts`'s own token fetch (`getAccessTokenForTenant`) does not set it either,
  so token calls were left untouched for consistency with the established pattern.
