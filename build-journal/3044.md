# #3044 — Change Control: standard-catalog execute skips freeze/maintenance/collision enforcement

- **Status:** ✅ DONE 2026-09-07T15:29:08Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** cd2304e9c (main)

## Log
- 2026-09-07T15:15:51Z ⏳ IN FLIGHT — read #3044's body, `portal-change-control-raise.ts`'s
  freeze/maintenance/collision gates, and `portal-change-catalog.ts`'s `execute` handler.
  About to wire the same freeze/maintenance gates into the catalog execute path,
  evaluated against "now" (immediate execute, no booked span) rather than a scheduled
  window; collision is skipped per the issue's own guidance since it only ever
  evaluates against a real booked span, which a catalog execute never has. A blocked
  execute 409s outright — no freeze-exception affordance, since a Standard catalog
  item has zero human approval stages by design and adding one would reintroduce the
  gate #1498 built this path to skip.
- 2026-09-07T15:29:08Z ✅ DONE — `POST /api/portal/change-catalog/:id/execute` now loads
  `portal_change_control_policy`, and when `enforceFreezeCalendar`/`enforceMaintenanceWindows`
  is on, calls `activeFreezeForSubmit` / `maintenanceCoverageForBookedSpan(ctx, now, null)`
  before the insert, 409ing with zero rows written if blocked. `tsc --noEmit` clean.
  New live-Postgres regression test
  `artifacts/api-server/src/routes/portal-change-catalog.live-db.test.ts` (5 cases: no-policy
  passthrough, active-freeze 409 with a verified zero-insert, expired-freeze passthrough,
  maintenance-gap 409, covered-maintenance-window passthrough) — all 5 passed against local
  Postgres; confirmed no orphaned synthetic rows left behind afterward. Related suites
  (`portal-change-control.test.ts`, `portal-change-freeze.test.ts`) still pass (57/57).
  Real commit cd2304e9c rebased onto and pushed directly to `origin/main`
  (`agent/3044-q1833` → `main`); `verify-branch-merged.mjs` confirms ancestry. Working
  tree clean. No findings filed — this build fixed the one gap #3044 described; no
  further gap discovered mid-build. Commented on #3044 with this summary.
