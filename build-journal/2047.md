# #2047 — Remediation KB: author the appgov: domain (9 active checks)

- **Status:** ✅ DONE 2026-09-02T02:47:13Z
- **Scope:** platform
- **Started:** 2026-09-02
- **Commit(s):** d8e9900793ba04de942aec71f8ab11f7c2cb195b (migration), plus bookend commits on `main`

## Log
- 2026-09-02T02:39:49Z ⏳ IN FLIGHT — pulled #2047 + #1924 (authoring standard) + reference migration `2026-08-31-remediation-kb-identity-domain-1924.sql`. Confirmed live state: 9 active `appgov:` checks in `monitor_checks`, 0 existing `remediation_knowledge_base` rows for them (81 published rows total across other domains already landed). Starting real Microsoft Learn doc verification per check before authoring rows.
- 2026-09-02T02:47:13Z ✅ DONE — Authored and published 8 of the 9 active `appgov:` checks in `remediation_knowledge_base`, every row verified against real Microsoft Learn / Microsoft Graph docs fetched this session (security-best-practices-for-app-registration, recommendation-renew-expiring-application-credential, recommendation-remove-unused-apps, configure-user-consent, configure-admin-consent-workflow, manage-application-permissions, concept-workload-identity-risk, riskyServicePrincipal API/resource docs, application-addPassword/removePassword, what-is-application-management, delete-application-portal). `appgov:enterprise-app-registration-list` (9th check) deliberately excluded — live query confirmed `filter_params: displayName eq 'ShaneMcCawConsulting'`, i.e. it's a platform self-test for our own MT app registration's presence in the customer's tenant, matching the #1134 migration's own "INTERNAL / DIAGNOSTIC (never in any customer tier)" classification (same category as `diagnostics:ps-execution-test`, which #1924 also excluded from its fan-out for the identical reason). Filed as its own finding (see issue comment).
  - Migration: `lib/db/migrations/manual/2026-09-02-remediation-kb-appgov-domain-2047.sql`, run against local `DATABASE_URL`. Before: 0 `appgov:` rows. After: 8 rows, all `published`, all `you_must_run` or `admin_center_only` (0 `we_can_run`). Re-run confirmed idempotent (`ON CONFLICT DO UPDATE`, same row count). Quality checks: 0 rows with a human name in `verified_by`, 0 empty `source_urls`, 0 empty `remediation_steps`, 100% of source URLs on microsoft.com.
  - Logged on #1630. Pushed directly to `main` (commit `d8e9900793ba04de942aec71f8ab11f7c2cb195b`), verified merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
  - Scope honored: content + seed migration only. No schema change (#1539 already built the columns), no route change, no UI, no `config_pack_templates` touch (#1925).
