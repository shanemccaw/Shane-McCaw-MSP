# #4493 — AdminV2 AD: Customer canvas's "Recent scans" has no drill-down, search, classifier, remove-from-package, or refresh

- **Status:** ⏳ IN FLIGHT 2026-09-17T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — read legacy `ActiveDirectoryCustomerPane.tsx` (#371/#374/#376/#378/#379) in full plus `AdCustomerCanvas.tsx` and `adKit.tsx`; confirmed every backend route the legacy pane calls (`GET .../diagnostics/runs`, `GET /msp/customers/:id/diagnostics/runs/:runId`, `GET/PUT /admin/monitoring-packages/:key/checks`, `GET /admin/simulator/assessments`) is already live and unchanged. Porting the reused business logic into `AdCustomerCanvas.tsx` with `adKit.tsx` primitives, in issue order: (1) refresh, (2) expand+findings, (3) search, (4) classifier+deep-link, (5) remove-from-package.
