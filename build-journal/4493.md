# #4493 — AdminV2 AD: Customer canvas's "Recent scans" has no drill-down, search, classifier, remove-from-package, or refresh

- **Status:** ✅ DONE 2026-09-17T01:10:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 9c8e15530 (rebased/pushed to main; original worktree commits 25364d8fe, 69c965121)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — read legacy `ActiveDirectoryCustomerPane.tsx` (#371/#374/#376/#378/#379) in full plus `AdCustomerCanvas.tsx` and `adKit.tsx`; confirmed every backend route the legacy pane calls (`GET .../diagnostics/runs`, `GET /msp/customers/:id/diagnostics/runs/:runId`, `GET/PUT /admin/monitoring-packages/:key/checks`, `GET /admin/simulator/assessments`) is already live and unchanged. Porting the reused business logic into `AdCustomerCanvas.tsx` with `adKit.tsx` primitives, in issue order: (1) refresh, (2) expand+findings, (3) search, (4) classifier+deep-link, (5) remove-from-package.
- 2026-09-17T01:10:00Z ✅ DONE — All five items ported into `AdCustomerCanvas.tsx`'s "Recent scans" section: refresh button (wires the previously-dead-code `fetchAdCustomerDiagnosticRuns`), expand-in-place findings fetch, client-side search with highlighting, real failure classification + Simulator Studio deep link (reusing `SimulatorFailureClassification`/`FailureCategoryChip`/`simulatorStudioCheckPath` as-is), and remove-from-package with shared-assessment detection. Added `fetchAdDiagnosticRunFindings`, `fetchAdMonitoringPackageChecks`, `setAdMonitoringPackageChecks`, `fetchAdSimulatorAssessments` to `adApi.ts` plus matching types to `adTypes.ts`. No backend changes — every route already shipped and unmodified. Verification: `pnpm run typecheck` clean, `pnpm run build` clean (both before and after rebase); queried the local dev Postgres directly and confirmed real data exercising this exact path (testbed tenant 2080, run `d1dc0ffe-...`, 7 real error findings each carrying `_rawGraphError`). No test-manifest required — `artifacts/admin-panel/` is internal tooling, outside the portal/marketing test-coverage scope. Live browser click-through not performed in-session (the dev-server checkout only picks this up after merge-to-main, which happened as the very last step); typecheck/build/DB verification above is real and honest in its place. Rebased onto `origin/main` to pick up a concurrent build's changes to the same file (#4489, testbed toggle + package assignment) — merged cleanly, both features coexist, re-typechecked clean after the merge. Pushed directly to main (9c8e15530). No findings filed — no bugs or gaps discovered beyond the issue's own scope.
