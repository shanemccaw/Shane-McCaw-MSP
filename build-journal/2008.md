# Git #2008 — Ownership/RACI: workload derivation from real M365 service-plan estate

- **Status:** ✅ DONE
- **Scope:** platform
- **Epic:** #1491 Ownership/RACI
- **Commits:** 05052b1e9 (schema + derivation + wiring), 1730baf52 (tests + manifest fix), 1acdd8fe6 (manifest lastVerifiedAgainstCommit)

## Log
- 2026-08-31 — Session started. Investigated #2008 body + comment. Confirmed
  `/subscribedSkus` full pages (including `servicePlans[]` with
  `servicePlanId`/`servicePlanName`/`servicePlanType`/`provisioningStatus`) are
  already stored in `tenant_monitor_profiles.raw_response` by 9 active
  `cost:*`/`copilot:*`/`license:*` checks — same source `license-waste-source.ts`
  already reads ad hoc. Plan: persist the enabled (provisioningStatus=Success)
  subset into a new `tenant_service_plans` table keyed by (msp_id, tenant_id),
  derive coarse workload buckets (Exchange/SharePoint & OneDrive/Teams/
  Security/Identity/Endpoint) from real `servicePlanName` values at read time
  (not stored — a pure derivation, same discipline as
  `FULL_INTUNE_SERVICE_PLAN`), wire a `workload` object type into
  `portal-ownership.ts`, and sync after any monitor package run that included
  a `/subscribedSkus` check.
- 2026-08-31 — ✅ DONE. Built:
  1. `tenant_service_plans` table (Drizzle: `lib/db/src/schema/msp.ts`; manual
     migration `lib/db/migrations/manual/2026-08-31-tenant-service-plans-workload-estate-2008.sql`,
     run against local Postgres, confirmed via `information_schema`/`\d`).
  2. `artifacts/api-server/src/lib/tenant-workloads.ts` — parses the
     already-stored `/subscribedSkus` page (reuses
     `license-waste-source.ts`'s `subscribedSkusCheckKeys` discovery, now
     exported), keeps only `provisioningStatus === "Success"` rows, and maps
     real `servicePlanName` identifiers to six coarse workload buckets
     (Exchange, SharePoint & OneDrive, Teams, Security, Identity/ICAM,
     Endpoint/Intune) — the same discipline `FULL_INTUNE_SERVICE_PLAN` already
     uses, not an invented vocabulary. `syncTenantServicePlans(tenantId)` does
     a full REPLACE per (mspId, tenantId).
  3. Wired into `executeMonitoringPackage` (`monitor-executor.ts`) — fires
     after any package run whose checks included a `/subscribedSkus` endpoint,
     non-fatal on failure.
  4. New `workload` object type in `lib/portal-ownership.ts` /
     `routes/portal-ownership.ts`, scoped via the existing `resolveTenantScope`
     (mspId, tenantId) pair, ungated by purchase per #1523's settled rule.
  5. Tests: `tenant-workloads.test.ts` (8 unit tests on the pure parse/group/
     resolve functions, registered in `vitest.config.ts`'s explicit include
     list) — all pass. `tenant-workloads-2008.live-verify.ts` — ran against
     the real local Postgres/testbed tenant
     (`c4c814d4-3afe-441e-9145-62461d0a4fd3`): synced 51 real enabled service
     plans, grouped into 4 real workloads (Exchange, SharePoint & OneDrive,
     Teams, Security) — confirmed via direct `psql` query too. Fixed
     `portal-ownership.test.ts`'s `buildSources()` count test for the new
     source. Fixed `test-manifests/portal/portal-v2-ownership.json`'s API6
     assertion (`$.sources[4]` → `$.sources[5]`, since `workload` is now
     `sources[0]`) and bumped its `lastVerifiedAgainstCommit`; could not
     execute it via `shaneapp://runTest` — that tool is not registered/
     reachable in this session (ToolSearch found nothing), stated honestly
     rather than fabricated. `pnpm typecheck` in `artifacts/api-server` is
     clean of every error introduced by this change (confirmed by comparing
     before/after with a correctly-pointed worktree — see finding below).
  6. Filed #2116 (parented to #1096, board status "AI Batter Up") — `pnpm
     typecheck` is broken on `main` itself, ~50 pre-existing errors unrelated
     to this build (compliance_frameworks/policy_decisions/risk_instances
     schema drift, missing `@workspace/db` exports, a shared error-code
     Record missing a member, and some smaller admin-build-tracker.ts issues).
     Discovered while diagnosing a worktree cross-contamination gap: this
     worktree's `node_modules/@workspace/db` and `lib/db/dist` junctions
     pointed at the MAIN CHECKOUT (a different, unrelated concurrent
     session's branch), not this worktree's own `lib/db` — repointed both
     locally (never wrote to the main checkout) to get an accurate
     typecheck baseline. Also filed #2117 (parented to #1202, board status
     "AI Batter Up") on the underlying junction-targeting gap itself, since
     it's structural to `link-deps.mjs`, not a one-off.
  7. `git status --porcelain` clean. `verify-branch-merged.mjs` confirms
     `1acdd8fe6` is an ancestor of `origin/main`.
