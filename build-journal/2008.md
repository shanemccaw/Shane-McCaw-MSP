# Git #2008 — Ownership/RACI: workload derivation from real M365 service-plan estate

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Epic:** #1491 Ownership/RACI

## Log
- 2026-08-31 — Session started. Investigated #2008 body + comment. Confirmed
  `/subscribedSkus` full pages (including `servicePlans[]` with
  `servicePlanId`/`servicePlanName`/`servicePlanType`/`provisioningStatus`) are
  already stored in `tenant_monitor_profiles.raw_response` by 9 active
  `cost:*`/`copilot:*`/`license:*` checks — same source `license-waste-source.ts`
  already reads ad hoc. Plan: persist the enabled (provisioningStatus=Success)
  subset into a new `tenant_service_plans` table keyed by (msp_id, tenant_id),
  derive coarse workload buckets (Exchange/SharePoint & OneDrive/Teams/
  Security/Identity/Endpoint) from real `servicePlanName` values at read time
  (not stored — a pure derivation, same discipline as
  `FULL_INTUNE_SERVICE_PLAN`), wire a `workload` object type into
  `portal-ownership.ts`, and sync after any monitor package run that included
  a `/subscribedSkus` check.
