# #1960 — Config snapshot registry: 197 rows are is_collectable with no graph_path

- **Status:** ✅ DONE 2026-09-04T08:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** f243d1e13 (fix, worktree branch) → merged to main as 9c79b3174

## Log
- 2026-09-04T00:30:00Z ⏳ IN FLIGHT — investigating build-snapshot-registry.mjs and the 197 m365dsc:* rows with read_transport='graph' and no graph_path; will resolve real graph_path per-resource where one exists, else flip is_collectable=false with a real reason, and fix the root cause in the registry builder.
- 2026-09-04T08:05:00Z ✅ DONE — Root cause: `readTransportFor()` in `build-resource-model.mjs`
  labelled a DSC resource `read_transport='graph'` off a `Get-Mg*` cmdlet or a bare
  `graph` permission workload, without checking whether a real REST path had actually
  been extracted (`dsc.graphPaths`). No literal URI exists in DSC source for an SDK
  cmdlet, so `graph_path` was permanently null for these rows. Fixed the classifier to
  route a cmdlet-only DSC resource to `'powershell'` (the real, callable surface)
  instead, falling to `'unknown'` only when there is no cmdlet at all. Applied the same
  correction to the 195 currently-affected `config_resources` rows via
  `lib/db/migrations/manual/2026-09-04-config-resource-graph-transport-1960.sql` (ran
  against local Postgres: 193 → `powershell`, 2 no-cmdlet DSC meta-resources →
  `unknown`), then re-ran `build-snapshot-registry.mjs` (local DB only, no network — no
  `.cache/config-state` present, so the full fetch pipeline was not re-run) to re-derive
  `config_snapshot_resource_types`. Verified 0 rows remain `is_collectable=true` with
  `read_transport='graph'` and no `graph_path`. Filed #2841 (parented under #1850, board
  status AI Batter Up) for the same shape at larger scale: 398 of 404 `powershell`-
  transport `is_collectable=true` rows have no matching ps-execution catalog entry.
  Commented real results on #1960. `verify-branch-merged.mjs` OK (9c79b3174 merged into
  origin/main). `git status --porcelain` clean.
