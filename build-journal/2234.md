# #2234 — Finding-filing section: Feature-first parenting

- **Status:** ✅ DONE 2026-09-02T15:35:00Z
- **Scope:** platform (docs-only, CLAUDE.md)
- **Started:** 2026-09-02
- **Commit(s):** f83c45f4d — "Feature-first finding-parent rule in CLAUDE.md (#2234)" (merged to main via fast-forward push, origin/main now at f83c45f4d)

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — editing CLAUDE.md's "Mandatory: file every finding as its own GitHub issue" section per #2234's 5-point scope (Feature-first parent rule, bug/security labels, URGENT prefix guidance, updated example block).
- 2026-09-02T15:35:00Z ✅ DONE — made all 5 edits to CLAUDE.md's finding-filing section, no other section touched:
  1. Renamed "### Where it goes — area epic routing" to "### Where it goes — Feature-first, area epic as fallback"; added a paragraph before the table instructing agents to check `gh issue view <your-issue-number>` for a `Feature: <name>`-titled parent and, if found, parent the new finding there as a direct sibling sub-issue instead of the Epic — citing the real #2207/#2208-under-#2198-instead-of-#2200 miss as precedent. The area-epic table is now explicitly framed as the fallback for "when your own issue has no Feature-tier parent."
  2. Added new "### Labels and title guidance" section: label every finding `bug`; add `security` too when security-relevant (separate cross-repo-filterable label).
  3. Same section adds URGENT guidance: prefix title `URGENT: ` only if it genuinely can't wait (cites #2130 precedent), stated as a title signal not a label.
  4. Updated the `gh issue create`/`sub_issues` example block to show `--label bug` (+ optional `--label security`, optional `URGENT: ` prefix) and comments clarifying `<parent>` is the Feature-tier issue if one exists, else the area-epic fallback.
  5. No new nesting level introduced — findings still land one level under whatever they're parented to (Feature or, as fallback, Epic), same as before.
  - Also updated "Why you cannot file it under your own issue" to say "parented per the Feature-first rule below" instead of "parented to the correct area epic," since that phrase preceded the actual rule and was now inaccurate.
  - Verified by re-reading lines 155-234 of the updated CLAUDE.md: the section explicitly instructs checking for a Feature-tier parent before falling back to the area-epic table — the concrete test this issue specifies.
  - Committed on branch `agent/2234-q1183`, then fast-forward-pushed that branch directly onto `origin/main` (`git push origin agent/2234-q1183:main`) since main hadn't moved since the worktree was provisioned. `verify-branch-merged.mjs` confirms OK.
  - `git status --porcelain` clean at finish.
