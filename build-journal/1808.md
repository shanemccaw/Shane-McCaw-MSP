# #1808 — Batter Up: stop showing already-tracked/queued items forever

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 9456c5229

## Log
- 2026-08-29 ⏳ IN FLIGHT — filter AlreadyTracked/JustAutoQueued rows out of
  BatterUpQueueService.RefreshAndAutoQueueAsync's returned display list; checked
  AI Batter Up for an equivalent reachable case (none — promotion moves the item off
  the "AI Batter Up" board status itself, so it drops out of that panel's own next
  refresh before #1709's queue/dedup logic ever runs).
- 2026-08-29 ✅ DONE — `RefreshAndAutoQueueAsync` now `continue`s past any row that's
  `alreadyTracked` or `justQueued` instead of adding it to the returned list, so those
  rows no longer appear in the Batter Up panel at all (the underlying dedup/auto-queue
  logic itself is untouched). Return type changed to `(List<BatterUpRow> Rows, int
  JustQueuedCount)` so the panel's "tell BuildQueuePanel to repaint" trigger still works
  even though the just-queued rows are filtered out of `Rows`; updated the one caller
  in `BatterUpPanel.xaml.cs` (which itself had picked up an unrelated concurrent #1803
  card-UI rewrite — rebased cleanly on top of it). AI Batter Up left as-is with a doc
  comment explaining why: promoting an item moves its real board Status off "AI Batter
  Up" immediately, so it drops out of that panel's own board query on its own next
  refresh — no equivalent stale-row case is reachable there.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` —
  clean, 0 warnings/errors. Branch confirmed merged: `git merge-base --is-ancestor HEAD
  origin/main` passed. `git status --porcelain` clean.
