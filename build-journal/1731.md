# #1731 — Security Plan: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `c31e4b5ae` (bookend IN FLIGHT), `6b7c2f72c` (contract pack, merged to `origin/main`)

## Log
- 2026-09-03T00:00:00Z ⏳ IN FLIGHT — Step 3 of #1578 for Security Plan, re-run per this
  issue (not #1495, which already has a DONE bookend against an earlier, structurally-wrong
  build — see the correction note in `build-journal/1495.md`). Confirmed via #2085 (closed)
  and #1561–#1568 (all closed) that the settled "assembled view over the other eight modules
  + authored prose, sealed and versioned" architecture has actually landed in code since the
  prior pack was written 2026-08-29. Read `docs/security-plan-contract-pack.md` (prior art,
  4aea9dd66 + 3 follow-ups) — its §2 explicitly documented the OLD hand-typed-only build as
  not implementing that architecture. Reading the real, current backend
  (`security-plan-assembly.ts`, `security-plan-versioning.ts`, `security-plan-drift.ts`,
  `security-plan-prose.ts`, `security-plan-cross-tenant.ts`, `security-plan-draft.ts`,
  `routes/portal-security-plan.ts`, `routes/msp-security-plan.ts`) plus schema and portal
  wire/model/live/page files next, to regenerate the pack wholesale against what is actually
  built now. Read-only: no product code, schema or UI changes.
- 2026-09-03T00:00:00Z ✅ DONE — Regenerated `docs/security-plan-contract-pack.md`
  wholesale (commit `6b7c2f72c`, merged to `origin/main`). Confirmed the settled
  #1561 "assembled view over eight modules, sealed and versioned" architecture is
  now real, tested code on the MSP side (`security-plan-assembly.ts` reads 7 real
  source tables; `security-plan-versioning.ts` implements the RBD-pattern
  supersede/seal/sign chain; `security-plan-drift.ts` diffs live vs last-signed,
  structurally never touching `.prose`; `security-plan-prose.ts`/`-draft.ts`
  implement the fixed freeze→author→seal sequence) and that #2085 landed a real
  `tenants.business_unit` column + AdminV2 editor, closing the last scope-dimension
  gap. Also discovered — genuinely new since the #1495 pack — that
  `artifacts/msp-portal` was retired and replaced by `artifacts/portal`
  (`f40438cdc`), so the customer-facing page that used to consume
  `GET /api/portal/security-plan` no longer exists anywhere in the repo (only a
  `builtPath: null` nav stub remains, `moduleNav.ts:52`), and the original
  admin-authored `portal_security_plans` model is now completely disconnected from
  the new assembled/versioned MSP-side pipeline — nothing bridges them. Filed
  **#2576** (`bug`, parented to #1495, the module's Feature-tier parent; board
  status set to "AI Batter Up") documenting that split, since no existing issue
  scheduled reconciling it. Did not file separate sub-issues for the nine
  MSP-side/#1568 cross-tenant "orphaned" endpoints — each is a documented,
  deliberate scope-stop in its own header (`msp-security-plan.ts:23-27`,
  `security-plan-cross-tenant.ts:13-16`, both explicitly naming v1.2/Design as the
  next gate), not an undocumented gap. Verified locally: all six
  `*security_plan*` tables exist but report 0 rows on this DB (noted honestly in
  the pack, §1.1/§2.9) — not re-seeded, since this is a read-only pass. Branch
  verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree
  clean (`git status --porcelain` empty). Posted a completion comment on #1731 and
  synced its `in-flight`/`complete` labels.
