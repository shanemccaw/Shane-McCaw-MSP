# #2032 — config-diffs cache-key collision (resourceKeys not part of identity)

- **Status:** ✅ DONE 2026-09-04T07:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 1d36eca4a (bookend IN FLIGHT), 501c9139e (real fix), rebased/pushed to `main` as `362c7762b..501c9139e`

## Log
- 2026-09-04T06:57:35Z ⏳ IN FLIGHT — confirmed the collision live: `sameKey` /
  `config_diffs_pair_uidx` matched only (baseSnapshotRowId, headSnapshotRowId, mode,
  rulesetFingerprint); `resourceKeys` was never part of identity, so a scoped
  recompute overwrote the full-tenant cached diff for the same pair. Fixing with a
  real `resource_keys_fingerprint` schema column + unique-index/migration, not just
  the in-memory key.
- 2026-09-04T07:05:00Z ✅ DONE — real fix landed:
  - `lib/db/src/schema/config-diffs.ts`: new `configDiffsTable.resourceKeysFingerprint`
    column (`'*'` sentinel for the unscoped/full-tenant case, NOT NULL so the unique
    index still dedupes it — a nullable column would relocate rather than close the
    bug, since a unique index does not treat two NULLs as equal), widened
    `config_diffs_pair_uidx` to include it.
  - `lib/db/migrations/manual/2026-09-04-config-diffs-resource-keys-fingerprint-2032.sql`:
    additive DDL (`ALTER TABLE ... ADD COLUMN`, index rebuild, immutability-trigger
    update to guard the new identity column). Ran it myself against local
    `DATABASE_URL` — `BEGIN / ALTER TABLE / DROP INDEX / CREATE INDEX / CREATE
    FUNCTION / INSERT 0 1 / COMMIT`, confirmed column + widened index present via
    `psql \d config_diffs`.
  - `artifacts/api-server/src/lib/config-snapshot-differ.ts`: new
    `fingerprintResourceKeys()` (mirrors `fingerprintRuleset()` — `'*'` when
    `resourceKeys` is omitted, else sha256 over the sorted, deduplicated scope);
    `diffSnapshots()`'s `sameKey` and its insert now both include it.
  - New live-db test `config-snapshot-differ.live-db.test.ts` (registered in
    `vitest.config.ts`'s include allowlist) proves the fix against real sealed
    snapshot rows: a full-tenant diff and a resource-scoped diff of the SAME pair now
    land in two distinct `config_diffs` rows (verified real diffRowIds 63 vs 64 in a
    live run), and re-requesting the full-tenant diff afterward still serves the
    original, untouched row from cache. 49/49 tests pass
    (`config-snapshot-differ.test.ts` + the new `.live-db.test.ts`).
  - `lib/db` and `artifacts/api-server` both typecheck clean (`tsc --build` /
    `tsc --noEmit`).
  - Branch rebased onto `origin/main` and pushed (`362c7762b..501c9139e`);
    `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
  - No findings filed — this session's own scope was exactly the reported bug, fixed
    at its root (schema + migration + code), not worked around.
