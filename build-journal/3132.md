# #3132 — Food preferences: dislikes (soft) and allergies (hard)

- **Status:** ✅ DONE 2026-09-08T00:10:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 33fe7ed4d (feature commit), rebased+pushed to main as d62f620af

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — starting: real `food_preferences` table (dislikes/allergies
  arrays, one row per user), MCP `get_food_preferences`/`set_food_preferences` (additive,
  matching `set_income_source`'s unset-keeps-current pattern), capture-grammar integration via
  tool description only (no separate settings form — architecture is Claude-conversation-driven
  per contract Section 10). Enforcement in `push_recipes` is out of scope here (that tool does
  not exist yet — #3127 is blocked_by this issue, not the other way around).
- 2026-09-08T00:10:00Z ✅ DONE — Real `food_preferences` table added
  (`web/shanes-life/migrations/024_food_preferences.sql`, run myself against the local
  `finances` Postgres DB — `npm run migrate` applied it cleanly, `\d food_preferences`
  confirmed). `src/core/food-preferences.mjs` (getFoodPreferences/setFoodPreferences) —
  additive merge, case-insensitive dedupe (existing casing wins), an unset field is left
  completely untouched (matches `set_income_source`'s pattern). MCP tools
  `get_food_preferences`/`set_food_preferences` wired into `src/mcp/tools.mjs`; the
  capture-grammar integration is the tool description itself, the same mechanism every other
  tool in this file uses — no separate settings form was built, matching contract Section 10's
  own architecture (AI step in conversation, app just stores). Verified end-to-end against the
  real local `finances` DB with a scratch user row (cleaned up after): set an allergy, set a
  dislike (confirmed allergy untouched), re-added a duplicate allergy in different casing
  (deduped, existing casing kept, confirming additive-not-destructive), read back via
  `get_food_preferences`. Updated `README.md` and the contract pack doc's stale "once #3132
  lands" phrasing. Real, deliberate scope line: enforcement inside `push_recipes` (#3127) and
  the "softer touch" on Shopping's own conversational check are NOT built here — `push_recipes`
  does not exist in this codebase yet (#3127 is blocked_by #3132, not the reverse); this issue's
  real job was the foundation those consume once built. Rebased branch onto `origin/main` and
  pushed directly to `main` (d62f620af) — `verify-branch-merged.mjs` confirms ancestor. Working
  tree clean.
