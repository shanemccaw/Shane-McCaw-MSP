# #1934 — Update button should no longer block until the Build Queue clears (reverses #1370, made safe by #1804)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** `55b4e1cbd` (code) · `a144eaebc` (IN FLIGHT bookend)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1934 body + comments and #1804's landed diff. About to remove the block-and-wait from `TriggerUpdateAsync`, evaluate the pending-update spillover/cancel machinery, and verify the deploy-restart path keeps in-flight builds alive per #1804.
- 2026-08-30 ✅ DONE — `55b4e1cbd`. `TriggerUpdateAsync` now shells `deploy-shanesbuild.cmd` immediately regardless of `BuildQueuePanel.HasActiveQueueItems` — the #1370 block-and-wait is gone.
  - **Spillover/cancel machinery:** removed everything that existed only to support the wait and is now dead — the pending-deploy poll timer (`_pendingDeployPollTimer`, `PendingDeployPollAsync`, `CreatePendingDeployPollTimer`, `_pendingPollInFlight`), the pending banner + Cancel button (XAML + `BtnCancelPendingUpdate_Click` + `ReplaySpilloverIntoLiveQueueAfterCancelAsync`), the `_updatePending` flag and its two `BT_QUEUE_BUILD` interception branches, and the spillover WRITE path (`PersistQueueRequestDuringPendingUpdate`). **Kept** the launch-time replay (`ReplayPersistedQueueRequestsOnLaunchAsync` + its Load/Save/TryReplayOne helpers + the BuildQueuePanel "Queued for Restart" display group) as a one-way transitional safety net: it drains any request a PRIOR blocking build persisted before this shipped, so nothing is silently lost across the reversal. In steady state no file is ever written and it's a cheap no-op.
  - **Restart-path verification (concrete, not assumed):** `deploy-shanesbuild.cmd` stops only the ShanesBuild UI process via `Stop-Process -Force` (TerminateProcess — no child cascade); grep confirmed **no job object anywhere in BuildConsole**, so children aren't tied to the parent's lifetime; #1804 gives each build its own durable stdout/stderr file handles (no EPIPE on parent death); dec5ee4ac (on origin/main) re-adopts live builds by pid+creation-time on relaunch. Live DB check: `bt_build_queue.build_pid` / `build_pid_started_at` columns exist, and all 8 currently-running builds (incl. this session id 1018, pid 47968) have a stamped pid — the adoption path has real data to survive on.
  - **Build:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` → **0 Warnings, 0 Errors**.
  - **Not done by me (Shane-controlled):** did not physically click Update + run the deploy — `deploy-shanesbuild.cmd` rebuilds from origin/main and restarts Shane's live ShanesBuild instance, which is his call to trigger. Every link of the restart-survival chain was instead verified concretely against live code + DB state.
  - Branch verified merged: `git merge-base --is-ancestor 55b4e1cbd origin/main` → 0. `git status --porcelain` clean.
