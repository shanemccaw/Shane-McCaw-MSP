# Build Journal — #4136

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** platform

## Log

- 2026-09-14T00:00:00Z — Started. Pointing "Open monitoring alert preferences" link in
  `artifacts/portal/src/pages/notification-preferences.tsx` at the real Settings → Alert
  Preferences destination (`/settings?tab=alerts`, which renders `AlertPreferencesContent`)
  instead of the `comingSoonHref("Portal Alerts", "module")` stub.
- 2026-09-14T00:20:00Z — ✅ DONE — Changed the `href` on the "Open monitoring alert
  preferences" link (notification-preferences.tsx:407) from
  `comingSoonHref("Portal Alerts", "module")` to `/settings?tab=alerts`, which is the real
  route that renders `AlertPreferencesContent` (wired via `settingsAlertsWire.ts`/
  `settingsAlertsLive.ts`) per `artifacts/portal/src/pages/settings.tsx`. Removed the now-unused
  `comingSoonHref` import. `tsc --noEmit` on `artifacts/portal` passes clean. Commit
  5555ca919, pushed directly to origin/main (fast-forward, no rebase needed). Working tree
  clean; branch HEAD confirmed ancestor of origin/main.
