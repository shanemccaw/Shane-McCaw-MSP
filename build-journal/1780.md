# #1780 — dev-server refresh-main-server.mjs: --dry-run falsely reports wouldPull:true even when a dirty tracked file would block the real fast-forward

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** a63881ecb (fix, rebased/pushed to main as 56d03577f)

## Log
- 2026-08-29 ⏳ IN FLIGHT — read fastForwardMainCheckout (~67) and previewFastForward (~285) in full; implementing a real dirty-tracked-file-blocks-ff detection in previewFastForward via `git status --porcelain` ∩ `git diff --name-only before..target`, read-only, no mutation. fastForwardMainCheckout's real merge path untouched.
- 2026-08-29 ✅ DONE — `previewFastForward` now intersects dirty tracked files (`git status --porcelain`) against the incoming ff's real footprint (`git diff --name-only before..target`); if a tracked file is both dirty and touched by the incoming range, reports `wouldPull:false` with `reason` + `blockingFiles`, matching the real `fastForwardMainCheckout`'s `git merge --ff-only` refusal. Chose status+diff intersection over an actual `git merge --ff-only --no-commit` probe (stated in the code comment) because the probe mutates the index/working tree and risks leaving a shared checkout half-merged if interrupted; the intersection approach is 100% read-only. `fastForwardMainCheckout` itself untouched (confirmed via diff — only `previewFastForward` and the new `dirtyTrackedFiles` helper changed, plus one new import).
  Verified against genuine sandbox repos (isolated temp origin+checkout pairs, never touched the shared main checkout):
  - Dirty file that the incoming ff would touch → pre-fix code falsely reported `wouldPull:true`; post-fix reports `wouldPull:false` with `blockingFiles:["f.txt"]`, matching the real merge's actual refusal (`error: Your local changes ... would be overwritten by merge`) byte-for-byte in cause.
  - Same dirty file, clean-repo case (no dirty files) → `wouldPull:true`, real ff succeeds — no regression to the common path.
  - Dirty file unrelated to the incoming diff → still `wouldPull:true`/real ff succeeds — confirms the check is precise (doesn't over-block on any dirty file, only ones the ff would actually touch).
  Commit `a63881ecb` rebased cleanly onto `origin/main` (no conflicts) and pushed as a fast-forward to `origin/main` (`56d03577f`). `git merge-base --is-ancestor HEAD origin/main` confirms merged. `git status --porcelain` clean.
