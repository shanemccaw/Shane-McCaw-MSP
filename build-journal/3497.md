# Issue #3497 — Batter Up RefreshAsync order: BUILD-comment resolve before closed-sweep

- **Status:** ✅ DONE 2026-09-10T22:40:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T22:30:00Z — IN FLIGHT. Reorder `BatterUpQueueService.RefreshAsync` so
  `ResolveBuildCommentsAsync` runs before `SweepClosedIssuesAsync`, per issue body, so
  scarce #2815 circuit-closed windows spend GitHub budget on real dispatch first, not the
  cosmetic closed-sweep.
- 2026-09-10T22:40:00Z — ✅ DONE — commit `f206a1046` (rebased/pushed to `main` as
  `969f7743e`) reorders the two calls in `RefreshAsync` in
  `desktop/BuildConsole/Services/BatterUpQueueService.cs`: `ResolveBuildCommentsAsync`
  now runs first, `SweepClosedIssuesAsync` runs after. Comments at both call sites updated
  to reflect the new order and the #3497 rationale/tradeoff (a just-closed item can now
  flash into one extra refresh's list before being swept, accepted per the issue's
  "cosmetic" framing). No behavior change to either step itself, single-method reorder as
  requested. Out of scope for a WPF test manifest (BuildConsole is excluded per CLAUDE.md
  Test Coverage section); verified by re-reading the reordered method and confirming
  `ResolveBuildCommentsAsync`'s only dependency (`boardItems`, fetched via
  `GetBatterUpBoardItemsAsync`, itself unmoved) is still available before it runs.
  `verify-branch-merged.mjs` confirms `969f7743e` is an ancestor of `origin/main`.
  `git status --porcelain` clean. No findings filed — no bug/gap surfaced during this
  build.
