# #3847 — Build Watch: card-click highlight should persist until another card is clicked

- **Status:** ✅ DONE 2026-09-12T21:42:01Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** e73701f84 (Build Watch: persistent selection tracking replacing timed flash)

## Log
- 2026-09-12T21:37:48Z ⏳ IN FLIGHT — implementing persistent selection tracking in
  BuildWatchWindow.xaml.cs, replacing the timed FlashSlotSelection with a real
  `_selectedSlot` field that composes correctly with ongoing state-driven redraws
  (SetSlotState/ApplySlotGlow) and clears on slot reassignment/dismissal (#3837 edge case).
- 2026-09-12T21:42:01Z ✅ DONE — replaced `FlashSlotSelection`/`SelectionFlashTimer`/
  `SelectionFlashDuration` with a persistent `_selectedSlot` field + `SelectSlot`/
  `ClearSelectionIfSlot`/`ApplySelectionVisual`/`RestoreNormalAppearance`. `SetSlotState`
  now reapplies the selection overlay on top of any real state transition (item 3 —
  a selected Running slot going Done still reads selected). `OccupySlot`/`ClearSlot`
  clear the selection when a selected slot is reassigned or dismissed (item 4, the
  #3837 edge case) — the safer default per the issue body. `dotnet build
  BuildConsole.csproj -c Debug` succeeded, 0 warnings/0 errors. Not live-clicked in a
  running BuildConsole instance this session (no queue builds were live to click during
  verification) — logic was verified by full read-through of every call site
  (SetSlotState, OccupySlot, ClearSlot, SelectSlotForQueueItem) plus the successful
  build; the real interaction contract (compose over state redraws, clear on
  reassignment) matches the issue's item 3/4 requirements exactly. Branch verified
  merged into origin/main via `verify-branch-merged.mjs`. Working tree clean.
