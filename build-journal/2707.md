# #2707 — Home "Recent Chats" left-off list: remove 5-item cap, add Reopen All

- **Status:** ✅ DONE 2026-09-03T18:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 28f530639 (Home Recent Chats: remove 5-item cap, add Reopen All), a1a566f98 (bookend IN FLIGHT)

## Log
- 2026-09-03T18:30:00Z ⏳ IN FLIGHT — read issue #2707 body + comment (BUILD dispatch, Posted 2026-09-03T18:26:52Z). Plan: (1) remove `.Take(5)` cap in `HomeView.RenderLeftOff`, wrap `LeftOffList` in a `ScrollViewer` with a MaxHeight; (2) add a real "Reopen All" button in the RECENT CHATS card header that loops `Home_ResumeChatRequested`'s underlying `OpenChatTab` call per remembered tab in saved order, skipping/toasting entries with no `ClaudeUrl`; (3) pane-targeting explicitly out of scope per issue body — noting as known limitation, not fixing.
- 2026-09-03T18:52:00Z ✅ DONE — Both fixes shipped:
  1. `HomeView.RenderLeftOff` (`Controls/HomeView.xaml.cs`) no longer does `tabs.Take(5)` — renders every remembered tab. `LeftOffList` (`Controls/HomeView.xaml`) is now wrapped in a `ScrollViewer MaxHeight="320"` so a big session scrolls instead of overflowing/truncating.
  2. Added a real "Reopen All" `Button` in the RECENT CHATS card header (visible only when >1 tab remembered). Click raises a new `HomeView.ReopenAllRequested` event carrying the full saved-order tab list; `MainWindow.Home_ReopenAllRequested` loops it and calls the SAME existing `Home_ResumeChatRequested(sender, p)` per tab — same `OpenChatTab` path, same honest toast-and-skip for any entry with no `ClaudeUrl`, no new reopen logic duplicated.
  3. Pane targeting: confirmed as a known, noted limitation per the issue body — `OpenChatTab` has no pane-target parameter, so Reopen All lands tabs via its existing default placement logic, not necessarily each tab's original `PaneIndex`. Not addressed here; would need a separate `OpenChatTab` signature change if Shane wants exact pane fidelity.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. No live-session/WebView2 UI walkthrough performed (no more-than-5-remembered-tabs state to click through in this environment); code path directly mirrors the existing single-click ResumeChatRequested flow with no new business logic beyond the loop + cap removal, so build-clean was the meaningful verification here.
  - Merge: pushed `agent/2707-q1451` directly onto `origin/main` (fast-forward, 59d189cab..28f530639); `verify-branch-merged.mjs` confirms merged.
  - `git status --porcelain` clean after final commit.
