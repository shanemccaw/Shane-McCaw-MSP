# #1300 — Portal-v2 landing (Scene 0): manual scan trigger + temporary QA skip affordance

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** `3d48fe75d`

## Log
- 2026-08-27 ⏳ IN FLIGHT — Removing PortalV2ScanLanding's auto-fire-on-mount scan trigger, replacing with a manual "Start my scan" button; posted the QA-skip-affordance gating proposal on the issue before building (kept the existing isTestbed-gated `[DEBUG] Skip to Overview` button #1298/Shane already added, per the proposal).
- 2026-08-27 ✅ DONE — `PortalV2ScanLanding.tsx`'s auto-trigger effect removed; `decideAutoScan`'s decision is now read-only, only a "Start my scan" click on `RevealNoScanGate` calls `debug-trigger-scan`. Added optional `ctaLabel` prop to the shared `RevealNoScanGate` (lib/copilot-scan-scene) to support the new button copy without touching `copilot-readiness.tsx`. Removed the now-redundant `[DEBUG] Pause Auto-Scan` button. Kept the existing isTestbed-gated `[DEBUG] Skip to Overview` as the QA skip affordance (gating proposal posted to issue #1300 before building). Verification: `revealAutoScan.test.ts` + `neverScannedGate.test.ts` 11/11 green (`node --experimental-strip-types --test`, unchanged pure logic these hooks call). Full `tsc --noEmit` for msp-portal/lib/copilot-scan-scene could NOT be run — this session's shared checkout had a pre-existing, unrelated broken `node_modules` (missing `@types/node`/`tsx`, `pnpm install` failing with EPERM on a native binary locked by another running dev-server process) that predates this session's changes; landed via an isolated worktree instead (see commit body). Manually reviewed the diff for type correctness. Honest gap: no live UI test possible for the new button — the only reachable test tenant (shane-mccaw-consulting) is already scanned, so `PortalV2ScanLanding` never mounts for it (documented in `test-manifests/portal/portal-v2-overview.json`'s notes, updated this session).
