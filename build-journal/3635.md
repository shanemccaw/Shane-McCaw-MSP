# #3635 — Generic POA&M PATCH lets MSPOperator fake converted_to_risk_acceptance, orphaning the status

- **Status:** ⏳ IN FLIGHT 2026-09-12T23:03:30Z      <!-- flip to: ✅ DONE <timestamp> -->
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** <real hash(es) — fill at DONE>

## Log
- 2026-09-12T23:03:30Z ⏳ IN FLIGHT — mirror the #3452 fix pattern in
  `artifacts/api-server/src/routes/msp-poams.ts`. The generic
  `PATCH /api/msp/poams/:poamId` (gated only `ladder.msp-operator`) validates
  `status` against the full `POAM_STATUSES` enum, so an `MSPOperator` can PATCH
  `status: "converted_to_risk_acceptance"` directly — a terminal state that should
  be reachable ONLY via the `ladder.msp-admin`-gated
  `POST /api/msp/poams/:poamId/convert-to-risk-acceptance` route (the only place
  that creates the linked `msp_risk_decisions` row + `convertedToRiskDecisionId`).
  Setting it via the generic PATCH orphans the status. Fix: (1) reject incoming
  `status: "converted_to_risk_acceptance"` on the generic PATCH (mirror the
  `"cancelled"` guard #3452 added), redirecting to the dedicated conversion route;
  (2) add `"converted_to_risk_acceptance"` to the existing-status terminal/uneditable
  check. Extend `msp-poams-role-floor.live-db.test.ts` with real-row coverage and
  run it against local `DATABASE_URL`.
