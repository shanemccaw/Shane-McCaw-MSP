# #3635 — Generic POA&M PATCH lets MSPOperator fake converted_to_risk_acceptance, orphaning the status

- **Status:** ✅ DONE 2026-09-12T23:15:41Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** be087416a (fix + extended live-db test), on `main`

## Log
- 2026-09-12T23:03:30Z ⏳ IN FLIGHT — mirror the #3452 fix pattern in
  `artifacts/api-server/src/routes/msp-poams.ts`. The generic
  `PATCH /api/msp/poams/:poamId` (gated only `ladder.msp-operator`) validates
  `status` against the full `POAM_STATUSES` enum, so an `MSPOperator` can PATCH
  `status: "converted_to_risk_acceptance"` directly — a terminal state that should
  be reachable ONLY via the `ladder.msp-admin`-gated
  `POST /api/msp/poams/:poamId/convert-to-risk-acceptance` route (the only place
  that creates the linked `msp_risk_decisions` row + `convertedToRiskDecisionId`).
  Setting it via the generic PATCH orphans the status. Fix: (1) reject incoming
  `status: "converted_to_risk_acceptance"` on the generic PATCH (mirror the
  `"cancelled"` guard #3452 added), redirecting to the dedicated conversion route;
  (2) add `"converted_to_risk_acceptance"` to the existing-status terminal/uneditable
  check. Extend `msp-poams-role-floor.live-db.test.ts` with real-row coverage and
  run it against local `DATABASE_URL`.
- 2026-09-12T23:15:41Z ✅ DONE — added two guards on the generic
  `PATCH /api/msp/poams/:poamId` (`msp-poams.ts:270-282`): (1) reject an
  incoming `status: "converted_to_risk_acceptance"` with 409, redirecting to
  the `MSPAdmin`-gated `POST /api/msp/poams/:poamId/convert-to-risk-acceptance`
  route (mirrors the `"cancelled"` guard #3452 added just above it); (2) added
  `"converted_to_risk_acceptance"` to the existing-status terminal/uneditable
  check alongside `cancelled`/`completed`. Extended
  `msp-poams-role-floor.live-db.test.ts` with two #3635 tests (live Postgres,
  real synthetic/suffixed rows, cleaned in `afterAll`): an `MSPOperator`'s
  PATCH with `status: "converted_to_risk_acceptance"` now 409s with the row
  left `active`; a row already `converted_to_risk_acceptance` refuses further
  generic PATCH edits (409, narrative unchanged). Verification: `tsc --noEmit`
  on `@workspace/api-server` clean (exit 0); role-floor test 5/5 passed against
  local `DATABASE_URL` (the two pre-existing tests timed out only on the first
  cold-start run under concurrent tsc load — a re-run at `--testTimeout=60000`
  passed all 5); sibling `msp-poam-rbd-conversion.live-db.test.ts` re-run 4/4
  passed, confirming the dedicated conversion route (the legitimate path that
  sets this status) still works with no regression. Commit `be087416a` rebased
  onto and pushed straight to `origin/main` (verified via
  `verify-branch-merged.mjs`, exit 0). Working tree clean
  (`git status --porcelain` empty). No fixture/hardcoded data introduced. No
  new findings filed — the fix is fully within scope.
