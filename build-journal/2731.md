# #2731 — msp-sow.ts getMspIdFromRequest bypasses resolveMspIdStrict contract

- **Status:** ✅ DONE 2026-09-04T04:46:27Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** caff790d9 (on main)

## Log
- 2026-09-04T04:42:43Z ⏳ IN FLIGHT — swapping msp-sow.ts's local `getMspIdFromRequest` (allows admin `?mspId=` override) for the canonical `resolveMspIdStrict()` on the 5 session-scoped routes named in the issue body, deleting the local duplicate.
- 2026-09-04T04:46:27Z ✅ DONE — Replaced `getMspIdFromRequest` with `resolveMspIdStrict(req)` on the 5 routes named in the issue (`GET /api/msp/sows`, `GET /api/msp/sows/:sowId`, `POST /api/msp/sows/:sowId/charge`, `POST /api/msp/sows/:sowId/expire`, `POST /api/msp/offers/:offerId/accept`) — no more `?mspId=` override on these session-scoped routes. Deleted the local duplicate function. The 3 other call sites in the same file that were NOT in the issue's fix list (`POST /msp/sows` standalone-SOW-create, and the two `/msp/customers/:customerId/clickwrap` routes) were left functionally equivalent by switching them to the canonical async `resolveMspId(req)` instead, since those explicitly need the override for admin cross-MSP creation/lookup and the issue did not flag them as wrong. Imported both helpers from `../lib/resolve-msp-id.ts`.
  Verification: `npx tsc --noEmit -p artifacts/api-server` shows zero errors referencing `msp-sow.ts` (all remaining tsc errors are pre-existing, unrelated to this file — `monitor-executor.test.ts` schema drift). Diff reviewed in full — matches the issue's specified fix exactly.
  Rebased onto `origin/main` and pushed directly to `main` (`caff790d9`); `git merge-base --is-ancestor HEAD origin/main` confirms merged. `git status --porcelain` clean.
