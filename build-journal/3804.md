# #3804 — Change-driven trigger for mention-color repaint (replace unconditional 5s clock)

- **Status:** ✅ DONE 2026-09-13T06:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** ff0a0b4eb (on main; bookend IN FLIGHT was d0d7dc99d)

## Log
- 2026-09-13T06:05:00Z ⏳ IN FLIGHT — Investigated #3804: `_localQueuePollTimer` tick (`BuildQueuePanel.xaml.cs` ~477) unconditionally fires `QueueRefreshed` → `PushLiveMentionColorsAsync` (WebView2 broadcast) and `UpdateIssueMirrorSyncStatusAsync` (2 local Postgres reads) every 5s regardless of change. Starting implementation: add a real change-driven signal so the color re-push only fires when something genuinely changed, fix 3 stale "~15s poll" comments, and do a broader pass on the same tick's other unconditional work.
- 2026-09-13T06:35:00Z ✅ DONE — Added `BuildQueuePanel.QueueDataChanged` event, fired only from inside the `signature != _lastQueueSignature` branch (a genuine re-render), and rewired `MainWindow`'s `PushLiveMentionColorsAsync` subscription from `QueueRefreshed` to it — the WebView2 mention-color broadcast no longer fires on every 5s tick, only on a real queue change. Confirmed via grep that `QueueRefreshed` now has zero subscribers left anywhere in the codebase, so its cascade carries no other unconditional per-tick cost (the broader pass the issue asked for). Also found and fixed the second named item: `UpdateIssueMirrorSyncStatusAsync`'s two local Postgres reads (`GetSyncStateAsync`/`GetIncrementalSyncStateAsync`) now cache their result, invalidated only by the existing `GitHubIssueMirror.SyncCompleted` event (a real completion signal) instead of re-querying every 5s tick; the on-screen countdown text still recomputes every tick from the cached timestamps + `DateTime.UtcNow`, so the live countdown display is unaffected. Fixed the 3 stale "~15s poll" comments named in the issue (`MainWindow.xaml.cs`, `IssueMentionInjector.cs`, `LiveMentionNumberRegistry.cs`) to reference the real 5s tick and `QueueDataChanged`. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 Warnings, 0 Errors. Rebased onto latest `origin/main` and pushed directly (`ff0a0b4eb`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean. Not independently live-verified against Shane's reported chat-input lag symptom (would require a running BuildConsole session with real WebView2 chat tabs and a live queue under load) — the fix removes the exact unconditional WebView2 broadcast + DB-read cost the issue's own evidence names as the likely contributor; worth Shane confirming the felt lag improves in real use.
