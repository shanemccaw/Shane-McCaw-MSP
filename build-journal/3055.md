# #3055 — dev-server coordinator: auto-resolve add/add conflicts on build-journal/*.md

- **Status:** ✅ DONE 2026-09-06T21:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** `02e54e527` (fix), `5e0b34391` (IN FLIGHT bookend)

## Log
- 2026-09-06T21:10:00Z ⏳ IN FLIGHT — Investigating the recurring add/add merge conflict on `build-journal/*.md` in the dev-server coordinator's merge-back path. #2830 was a manual git-history repair of two specific files (owner: "No source-code changes were needed"), so the mechanism that produces add/add conflicts is untouched and recurs for every new colliding pair. Designing a real, general auto-resolve merge strategy scoped to `build-journal/*.md` (append-only, one-writer-per-file logs whose content is throwaway in the dev-server checkout).
- 2026-09-06T21:45:00Z ✅ DONE — Shipped a two-layer, general fix scoped strictly to `build-journal/*.md`, in commit `02e54e527` (pushed to `origin/main`; branch verified merged via `verify-branch-merged.mjs`, exit 0):
  1. **`.gitattributes`**: `build-journal/*.md merge=union` — git resolves add/add AND modify/modify on these paths by union, declaratively, in any checkout carrying the attribute. Empirically verified: without it a two-branch add of the same bookend path gives `CONFLICT (add/add)` (the #3055 failure); with it the merge auto-unions and succeeds, tree clean.
  2. **`scripts/dev-server/git.mjs` `mergeNoEdit`**: code-level backstop. On a failed merge whose conflicted paths are confined ENTIRELY to `build-journal/*.md`, it resolves each (`--theirs` — the incoming commit is the change being landed; the dev-server copy is non-authoritative doc) and completes the merge; a conflict touching ANY other path still aborts + reports honestly (unchanged). This covers the pre-existing divergence already sitting in `C:\dev-server` before its dev-server branch picks up the new attribute. `autoResolved` is surfaced in coordinator per-request outcomes and the build-set event log.
  - **Verification:** extended `scripts/dev-server/selftest.mjs` with Scenario 12 (two sessions both ADD `build-journal/2830.md` divergently → BOTH land, add/add auto-resolved, both real code changes survive, checkout clean) and Scenario 13 (mixed build-journal + real-code conflict → STILL aborts honestly, not swallowed). Both pass deterministically every run.
  - **Incidental fix (test isolation):** the selftest's cross-process scenarios spawned children with `env:{...process.env}`, inheriting this build session's ambient `DEV_BUILD_SET*` vars and routing ungrouped children into the wrong set — Scenarios 3 & 9 failed deterministically on the pristine pre-change code for that reason (proven by running the committed versions in isolation). Added a `childEnv()` scrub; the suite now runs green (`53 passed, 0 failed`) or with only the residual concurrency flake below.
  - **Finding filed:** #3068 (parented under #1202, `bug`, board "AI Batter Up") — residual, genuine cross-process concurrency flake in selftest Scenarios 3/9 that remains after the env-isolation fix (intermittent `every concurrent request landed`); a single child always lands, so it's an N-way race, pre-existing and separate from #3055.
  - **Working tree:** clean; only my own commits pushed.
