# #3188 — Feature: Lightweight /widget page for third-party iOS Home Screen widget apps (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T03:17:22Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 04248666c (feature), ecd3663e5 (merge origin/main)

## Log
- 2026-09-08T03:02:45Z ⏳ IN FLIGHT — investigating the real, explicit stop-condition question first: does the widget app's WKWebView share Safari's session, or does this need its own auth mechanism (e.g. long-lived per-user token in the URL)? Then building the minimal `/widget` route sourced from the Today tray's "Next" card content, per the issue's real scope.
- 2026-09-08T03:17:22Z ✅ DONE — **Auth question resolved, not assumed:** confirmed directly from the real
  Villy21/JsWidget README that (a) `<a>` taps are routed by URL fragment/query so the widget page
  reloads normally, never sharing Safari's cookie jar, and (b) "cookies are shared between all
  JS-Widget instances and the main app" but explicitly does NOT include Safari — and passkeys
  cannot run in a third-party WKWebView at all. Codebase already had the exact right precedent
  (`mcp_tokens`, `share_links` — a long-lived, revocable, SHA-256-hashed bearer token, scoped to
  one user, carried in the URL path). Built `widget_tokens` on the same shape rather than guessing
  at a new mechanism.

  **What shipped:** `web/shanes-life/migrations/046_widget_tokens.sql` (ran it myself against the
  local `finances` Postgres — 046, not 044, because two peer sessions had already taken 044/045 on
  origin/main; caught via the shared-DB ledger before naming collided); `src/core/widget-tokens.mjs`
  (issue/resolve/list/revoke, mirrors `mcp-tokens.mjs`); `src/core/widget.mjs` (server-side
  `computeNextCard()` — the same real Next-card priority rule as `app.js`'s `resolveNextKind`,
  minus the client-only "dinner" case a stateless server render can't honestly know; renders a
  small inline-CSS HTML page, no app.js/app.css, real 30MB-ceiling constraint honored); a real
  tap-through action (`?done=<entityId>` → `updateEntity(status:'done')`, audited as actor
  `widget`); `src/routes/widget.mjs` + `GET /widget/t/:token` wired into `server.mjs` above the
  session-cookie branch, same tier as `/mcp` and the Plaid webhook; session-authed
  `GET/POST /api/widget-tokens` + `DELETE /api/widget-tokens/:id` in `api.mjs`; a Settings screen
  section in `app.js` (mint/revoke, mirrors the existing MCP-token UI); `bin/issue-widget-token.mjs`
  CLI for minting one before there's a browser session.

  **Verification (real, live, against local Postgres `finances` — the same DB ShanesSurvival
  uses):** ran the new migration for real; booted the server for real; minted a real token for
  Shane's real account via the CLI; `GET /widget/t/<token>` returned real live data (his real
  31-item open Shopping list, "home" kind — correctly outranking a disposable test "generic"
  entity per the real priority rule); fired the real `?done=<id>` action against a disposable test
  entity and confirmed both the DB row (`status='done'`) and a real `activity_log` row (actor
  `widget`) — then deleted the test entity/token/audit row, leaving zero residue on Shane's real
  data; confirmed a revoked token and a garbage token both 404; confirmed `/api/widget-tokens`
  refuses an unauthenticated GET (401); ran the full existing `npm run check` harness afterward —
  **235/235 passed**, no regressions from this change or the two peer features (`Income Rules`,
  `Money — Home-tab decision tools`) merged in along the way. Not live-tested from an actual
  Widget Web app on Shane's iPhone yet — that's the issue's own stated final verification step and
  needs a real deploy + Shane's real device; everything server-side that can be verified without
  that has been.

  Working tree left clean; `git status --porcelain` empty. No findings filed — this build hit no
  gap it didn't build, and no product decision it couldn't resolve itself.
