# #4576 — identity:ca-policy-count gate-skipped path has no scalar for the Security pillar CA tile

- **Status:** ✅ DONE 2026-09-18T04:16:01Z
- **Scope:** platform
- **Log:**
  - 2026-09-18T04:02:11Z — Session started. Pulled #4576 body + comments (one `BUILD:` comment, dispatch confirms Shane's decision: stamp `caPolicyCount: 0` + `securityDefaultsEnabled: true` on the gate-skipped path).
  - 2026-09-18T04:16:01Z — ✅ DONE — implemented, tested, and live-verified against the real McCawSoft testbed tenant (`tenants.id` 2080).

## Changes Made

- **`artifacts/api-server/src/lib/monitor-executor.ts`** (commit `8c491de2a`) — the
  Security Defaults gate-skipped branch (shared by four checks:
  `identity:app-proxy-connector-groups`, `identity:ca-mfa-coverage`,
  `identity:ca-legacy-auth-block`, `identity:ca-policy-count`) now stamps
  `{ caPolicyCount: 0, securityDefaultsEnabled: true }` into
  `extractedProperties`, scoped strictly to `check.key === "identity:ca-policy-count"`
  — the other three checks sharing this gate are unaffected.
- **`artifacts/api-server/src/lib/pillar-summary-stats.ts`** (commit `8c491de2a`) —
  `statFromCheckObservation` now reads `observation.props.securityDefaultsEnabled`
  and, when true, sets the tile's `sub` caption to "This tenant uses Security
  Defaults instead of Conditional Access" (overriding any template-driven sub,
  which a gate-skipped row never carries a denominator for anyway). Updated the
  `security.caPolicies` spec's stale comment (it no longer "reads unavailable").
- **Tests** (commit `3a68a9a3a`):
  - `monitor-executor.test.ts` — new case asserting `identity:ca-policy-count`'s
    gate-skipped row carries the two new fields; existing `identity:ca-mfa-coverage`
    gate test extended to assert it does NOT gain them. Also fixed a pre-existing
    test-mock gap: the `@workspace/db` mock had no `driftCollectionStatusTable`
    export, which only surfaced because this is the first test exercising a
    drift-tracked check through the gate-skipped branch with `persistProfile`
    defaulted true.
  - `pillar-summary-stats.test.ts` — new case asserting the tile resolves to a
    real `value: 0` with the Security Defaults sub-caption, not `unavailableReason`.
- **Live-verification script** (commit `543c88ec3`):
  `artifacts/api-server/src/scripts/verify-ca-policy-count-gate-scalar-4576.ts`
  — read-only (`persistProfile: false`), runs the real `executeMonitorCheck` +
  `statFromCheckObservation` chain against a real tenant.

No schema change, no migration — `extracted_properties` is JSONB; this only adds
keys to an existing row shape.

## Verification Results

1. `pnpm exec vitest run src/lib/__tests__/monitor-executor.test.ts` — 230/230 passed.
2. `pnpm exec vitest run src/lib/pillar-summary-stats.test.ts` — 44/44 passed.
3. **Live-verified** against the real McCawSoft testbed tenant (`tenants.id` 2080,
   `tenant_id c4c814d4-3afe-441e-9145-62461d0a4fd3`) via
   `node run-script.mjs src/scripts/verify-ca-policy-count-gate-scalar-4576.ts 2080`:
   - `executeMonitorCheck` result: `status=ok`, `extractedProperties.caPolicyCount = 0`,
     `extractedProperties.securityDefaultsEnabled = true` (alongside the existing
     `_gateSkipped: true` and raw Security Defaults policy fields).
   - `statFromCheckObservation` on the `security.caPolicies` tile spec resolved to
     `value: 0`, `sub: "This tenant uses Security Defaults instead of Conditional Access"`
     — no `unavailableReason`. Confirms the tile now renders a real "0 — Security
     Defaults" state instead of "—".

Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0).
Working tree clean (`git status --porcelain` empty) before this bookend commit.

## Commits

- `da4cb112f` Bookend: #4576 IN FLIGHT
- `7c335fab8` Plan: #4576 gate-skipped identity:ca-policy-count scalar
- `8c491de2a` Stamp explicit caPolicyCount/securityDefaultsEnabled on gate-skipped CA check
- `3a68a9a3a` Add test coverage for gate-skipped identity:ca-policy-count scalar (#4576)
- `543c88ec3` Add live-verification script for #4576 gate-skipped CA policy scalar
