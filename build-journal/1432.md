# #1432 — Compliance: obligations page — replace 100% fabricated register with honest no-data state

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Build:** 714
- **Started:** 2026-08-27
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Pulled #1432's body + #1415's investigation comment. #1415 asserted "no register endpoint has ever been wired for this page" and the issue's standing rule (strip the register, show honest no-data). Investigation before touching code found that assertion is stale: Git #1223/#1333 (commit `8f0d71788`, landed 2026-08-26, one day before #1415's audit) already built a REAL endpoint (`GET /api/portal/compliance-obligations`, joining `compliance_frameworks`/`compliance_obligations`/`tenant_compliance_scope`/`msp_risk_decisions`), a real client hook (`useComplianceObligationsLive`), and a wire normalizer — but the route was never mounted into `routes/index.ts` and the page (`portal-v2-compliance-obligations.tsx`) never called the hook, so the endpoint was fully dead code and the page kept rendering the raw `CMP_OBLIGATIONS` fixture unconditionally. `test-manifests/portal/compliance-drilldowns.json` already has a `CMP-LIVE4`/`C-CMP-OBL` step asserting a `pv2-cmpobl-rows-source` "live" marker that does not exist in the page — a pre-written PASS claim for functionality that was never actually wired. Given real backing genuinely exists, completing the wiring (not permanently deleting it for a dead-end message) is the correct honest fix — plan: mount the orphaned route, wire the page to the real hook with honest loading/error/empty states (never rendering the fixture as real), add the missing `pv2-cmpobl-rows-source` marker, verify against the manifest's own pre-written assertions.
