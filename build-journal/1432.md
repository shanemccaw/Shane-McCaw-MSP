# #1432 — Compliance: obligations page — replace 100% fabricated register with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Build:** 714
- **Started:** 2026-08-27
- **Commit(s):** `45bfef9fe`, `30714c9c5`

## Log
- 2026-08-27 ⏳ IN FLIGHT — Pulled #1432's body + #1415's investigation comment. #1415 asserted "no register endpoint has ever been wired for this page" and the issue's standing rule (strip the register, show honest no-data). Investigation before touching code found that assertion is stale: Git #1223/#1333 (commit `8f0d71788`, landed 2026-08-26, one day before #1415's audit) already built a REAL endpoint (`GET /api/portal/compliance-obligations`, joining `compliance_frameworks`/`compliance_obligations`/`tenant_compliance_scope`/`msp_risk_decisions`), a real client hook (`useComplianceObligationsLive`), and a wire normalizer — but the route was never mounted into `routes/index.ts` and the page (`portal-v2-compliance-obligations.tsx`) never called the hook, so the endpoint was fully dead code and the page kept rendering the raw `CMP_OBLIGATIONS` fixture unconditionally. `test-manifests/portal/compliance-drilldowns.json` already has a `CMP-LIVE4`/`C-CMP-OBL` step asserting a `pv2-cmpobl-rows-source` "live" marker that does not exist in the page — a pre-written PASS claim for functionality that was never actually wired. Given real backing genuinely exists, completing the wiring (not permanently deleting it for a dead-end message) is the correct honest fix — plan: mount the orphaned route, wire the page to the real hook with honest loading/error/empty states (never rendering the fixture as real), add the missing `pv2-cmpobl-rows-source` marker, verify against the manifest's own pre-written assertions.
- 2026-08-27 ✅ DONE — Mounted `portalComplianceObligationsRouter` in `artifacts/api-server/src/routes/index.ts` (the one-line gap that left the real Git #1223 endpoint dead). Rewrote `portal-v2-compliance-obligations.tsx` to read `useComplianceObligationsLive()` instead of the static `CMP_OBLIGATIONS` fixture — `rows` is now strictly the real live catalog; the fixture is never rendered under any state. Added an honest `pv2-cmpobl-status` banner for loading ("Loading your obligation register…") and for a failed/never-resolved fetch ("Your obligation register could not be loaded — no live data available."), an honest `pv2-cmpobl-empty` state for a genuinely empty live catalog, and a second `pv2-cmpobl-rows-source` live/fixture marker for the register itself (separate from the pre-existing `pv2-cmpobl-source` hero marker) — this is the exact marker `compliance-drilldowns.json`'s pre-existing CMP-LIVE4 step was already asserting before it existed. Updated that manifest's notes documenting the wiring-gap closure and the two new honest-state markers (not independently exercised — same reasoning the decisions page's untested error branch already established: forcing a fetch failure isn't practical against a live deployed backend); bumped `lastVerifiedAgainstCommit` to `45bfef9fe`.
  - **Verification:** `tsc -p tsconfig.json --noEmit` clean (0 errors) on both `artifacts/msp-portal` and `artifacts/api-server`. Full `msp-portal` suite: 1795/1795 green (`npm run test`), including the untouched `cmpDashboardData.test.ts`/`cmpAreaWiring.test.ts` suites (26/26). Did **not** run the manifest live via `shaneapp://runTest` — the `shane-msp` MCP server failed to connect this session (`CONNECTION_CLOSED`), a genuine tool-availability gap stated honestly, not an assumed default limitation. Once a session with that server reachable runs it, `CMP8`/`CMP9`/`CMP-LIVE3`/`CMP-LIVE4`/`C-CMP-OBL` in `test-manifests/portal/compliance-drilldowns.json` should now genuinely pass against the mounted endpoint (previously they were asserting functionality that plain didn't exist).
  - **No new logger channel needed:** the route (`portal-compliance-obligations.ts`) already carries `logger.child({ channel: "tenant.portal" })` from its original #1223/#1333 commit, matching the locked taxonomy; mounting it added no new logging surface. The page itself is a UI-only React component, consistent with every sibling drill-down page in this family (none log client-side).
  - Commits: `45bfef9fe` (the real wiring + honest states) and `30714c9c5` (manifest `lastVerifiedAgainstCommit` pin, hash corrected post-rebase).
