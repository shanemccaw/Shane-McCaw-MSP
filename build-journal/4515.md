# #4515 — generated-secret-store must not fall back to AZURE_KEY_VAULT_URL (prod vault)

- **Status:** ✅ DONE 2026-09-17T14:07:21Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** df2683dcb (fix + unit test), 3a96fde43 (IN FLIGHT bookend)

## Log
- 2026-09-17T13:58:16Z ⏳ IN FLIGHT — dropping the AZURE_KEY_VAULT_URL fallback in generated-secret-store vaultUrl() so an unset GENERATED_SECRET_VAULT_URL fails closed (generated_secret_store_unavailable).
- 2026-09-17T14:07:21Z ✅ DONE — df2683dcb: `vaultUrl()` in `artifacts/api-server/src/lib/generated-secret-store.ts` now reads only `GENERATED_SECRET_VAULT_URL` (trimmed; unset/blank = unconfigured). No fallback to `AZURE_KEY_VAULT_URL`. Error text in the store and in `config-pack-orchestrator.ts` updated to say so; stale fallback note in `break-glass-admin-override.live-verify.ts` header corrected. New unit test `generated-secret-store-vault-fallback-4515.test.ts`: 4/4 pass. api-server `tsc --noEmit`: 0 errors. Live check (scratch harness, deleted after): real `.env.local` loaded with `AZURE_KEY_VAULT_URL` + Azure creds present, `GENERATED_SECRET_VAULT_URL` unset. Real `persistGeneratedSecretsForRun` (the step a gated pack run hits before any workflow/run row or tenant write) threw `ConfigPackError` `generated_secret_store_unavailable`, stored 0 secrets, and constructed 0 `SecretClient`s (counted through a wrapped constructor), so no vault was contacted. Not run end to end through the HTTP route (`admin-config-pack-run.ts` maps this code to 503). **Still needed (Shane/#1281):** provision a separate dev vault and set `GENERATED_SECRET_VAULT_URL` locally, then set it explicitly on Staging/Prod. Until then, gated packs that mint a credential (e.g. quickstart-v1 create-break-glass-account) and the break-glass admin override refuse with 503 in any env without it, and the orphan-secret sweep skips. No findings filed. No pre-existing dirty files.
