# #1529 — Policy Decisions: Correct this module's table list in tooling and docs

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 2b222abe2 (IN FLIGHT, real post-push hash), 23706c118 (this DONE
  update, real post-push hash — no product-code commit was needed, see log)

## Log
- 2026-08-30 ⏳ IN FLIGHT — sweeping for the same `policy_rules`/`policy_rule_*` ↔
  Policy Decisions mistaken association across MCP tool descriptions, `CLAUDE.md`,
  prior issue bodies, and #1481's pillar/data mapping doc.
- 2026-08-30 ✅ DONE — investigation-only, no product code touched. Swept the whole
  repo (`grep -rn "policy_rule"`, plus `gh search issues "policy_rule" in:body`) for
  every place the alert-engine's `policy_rules`/`policy_rule_firings`/
  `policy_rule_audit_log`/`policy_rule_suppressions`/`policy_rule_incidents` tables
  could be mistakenly attributed to the Policy Decisions module. Found:
  - `CLAUDE.md` — clean, no reference.
  - `artifacts/mcp-server/src/tools/*` (MCP tool descriptions) — two unrelated `policy`
    hits (`policyId` template variable in `execute-action.ts`, `ca-policy` Conditional
    Access drift domain in `get-microsoft-drift.ts`), neither the alert-engine tables.
  - `docs/pillarmapping.md` (#1481's deliverable) — clean, no reference.
  - Every other Policy Decisions/Policy Engine issue body (#1525-#1528, #1547-#1553) —
    clean via `gh issue view --json body`.
  - GitHub-wide issue search for `policy_rule`/`policy_rules` returns exactly three
    issues: #1490 (the original error), #1529 (this one, which quotes it as the
    thing being corrected), and #1041 (SPF/DKIM/DMARC — false-positive token match,
    contains no literal `policy_rule` substring on inspection).
  - `docs/policy-decisions-contract-pack.md` §0 (committed `d5e12127e`, prior session)
    already verifies and documents the correction, citing this issue by number.
  - `docs/security-plan-contract-pack.md:230` already states the same correction
    correctly ("unrelated alert-engine tables").
  - `#1490`'s own body still shows the original wrong table list, but its first
    comment thread (2026-08-28, "Correction to this issue's own body") already
    disregards it in place, per this project's own "later comments supersede the
    body" convention — the same pattern used throughout this repo, not something
    this session needed to redo.
  **Conclusion: the mistaken association was already corrected everywhere it existed
  before this session started** (contract pack #1490 build, 2026-08-29). No stray
  references were found anywhere else in tooling, docs, `CLAUDE.md`, MCP tool
  descriptions, or other issue bodies. No code or doc changes were needed beyond this
  bookend. `git status --porcelain` clean; branch verified an ancestor of `main`.
