# #4538 — sharepoint:inactive-sites still missing lastModifiedDateTime via $select on bare /sites

- **Status:** ✅ DONE 2026-09-17T23:50:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 5c050da222b75871d2bd73534452fd0cac0eea82 (fix), 523e1172c (plan), 9903c0437→c3bdae90a (IN FLIGHT bookend)

## Log
- 2026-09-17T22:12:00Z ⏳ IN FLIGHT — confirmed live via `run-checks-4481.ts` that the check
  still errors (`lastModifiedDateTime` absent from all 99 items even with `$select` on the
  wire). Root-caused with a scratch script hitting `graphFetchForTenant` directly with 4 URL
  variants: bare `/sites` never projects `lastModifiedDateTime` (not even in its own default
  field set, and it silently drops any `$select` outside that set back to `id` alone); `/sites?
  search=*&$select=...` returns the field on 100% of 143 real items (vs 99 found by bare
  `/sites`) and needs no extra headers. Writing the fix: a manual migration changing the
  check's `endpoint` from `/sites` to `/sites?search=*`, keeping #4524's `select_params`/
  `mapping`/`requireField` untouched. See build-journal/4538-plan.md for full evidence.
- 2026-09-17T23:50:00Z ✅ DONE — ran migration
  `lib/db/migrations/manual/2026-09-17-sharepoint-inactive-sites-search-endpoint-4538.sql`
  against local Postgres (`UPDATE 1`). Re-ran `run-checks-4481.ts` for
  `sharepoint:inactive-sites` against tenant 2080 (mccawsoft2.onmicrosoft.com,
  `c4c814d4-3afe-441e-9145-62461d0a4fd3`) live: `status: "ok"`, `itemCount: 143`,
  `firstItemFields: ["id","lastModifiedDateTime"]`, no `MissingRequiredFieldError`.
  Commit `5c050da22` pushed to `origin/main`, confirmed ancestor.

  **Genuine finding filed mid-build, not fixed here (out of scope for #4538):**
  #4602 — `countIfLastSignInOlderThan` (monitor-executor.ts:1625-1651) is hardcoded
  to compare `signInActivity.lastSignInDateTime`, not the mapping rule's actual
  `sourceField`. For `sharepoint:inactive-sites` that means every site with a
  present `lastModifiedDateTime` (all 143, now that #4538's fix landed) is
  unconditionally counted stale — `inactiveSiteCount: 143` came back equal to
  `itemCount: 143` even though one site's real `lastModifiedDateTime` was 1 day
  old. Filed with full live evidence, parented under #1489 (per this build's own
  dispatch instructions), labeled `bug`, milestone v1.1, board status "AI Batter
  Up".

  `git status --porcelain` clean; branch verified ancestor of `origin/main` via
  the push itself (fast-forward, no conflicts).
