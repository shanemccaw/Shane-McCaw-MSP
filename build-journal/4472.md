# Build journal — Git #4472

- **Status:** ✅ DONE 2026-09-17T23:42:00Z
- **Scope:** platform

## Log

- 2026-09-17T23:22:00Z — Starting. Root cause confirmed: `auth.ts` → `lib/audit.ts` → `auditLogsTable` from `@workspace/db`, missing from the three suites' `mock.module("@workspace/db", ...)` namedExports. Plan written at `build-journal/4472-plan.md`.
- 2026-09-17T23:42:00Z — ✅ DONE — Added `auditLogsTable: {}` to all three suites' `@workspace/db` mocks. Re-running surfaced a second missing export (`getMspConsoleBaseUrl` from `../lib/portal-url.ts`) needed by `auth-impersonation.test.ts` and `auth-account-activation-entitlement.test.ts`; added it to both. `auth-forgot-password-surface.test.ts` needed only `auditLogsTable`. Final verification: `node --experimental-strip-types --experimental-test-module-mocks --test src/routes/auth-impersonation.test.ts src/routes/auth-account-activation-entitlement.test.ts src/routes/auth-forgot-password-surface.test.ts` from `artifacts/api-server` — 33 tests, 33 pass, 0 fail. Commit `a49cd7603` merged into `origin/main` (verified via `verify-branch-merged.mjs`). Working tree clean.
