# #1725 — Ownership / RACI: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-07T00:30:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `b58247c0c` (contract pack), `078965acd` (IN FLIGHT bookend)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — #1524 decided closed (delegations kept as-is, customer-side,
  no code change). Doing a full line-by-line audit of `docs/ownership-raci-contract-pack.md`
  against `main` to confirm it's accurate and regenerate per #1642 if anything changed.
- 2026-09-07T00:30:00Z ✅ DONE — Full line-by-line audit complete. Found exactly one real backend
  change since the pack's prior audit (`152ca0acf3`, 2026-09-03): #1168's `requireTierFeature`
  gate (`523af076f`, 2026-09-04) added to both of surface A's READ routes
  (`GET /portal/ownership`, `GET /portal/ownership/events`) — documented in §1a, and every
  `routes/portal-ownership.ts` line citation the edit shifted has been re-verified against the
  real file and corrected. No other cited file (`lib/portal-ownership.ts`, `msp-ownership.ts`,
  `msp-ownership-book.ts`, `portal-settings-ownership.ts`, `msp.ts` schema) changed — confirmed
  via `git log`, not assumed. §7 updated to record #1524's real closure (keep delegations as-is,
  customer-side, no code change) — this pack's own audit confirms the current `WireOwnDelegation`
  shape and its two write routes already match that decision exactly; no code change was needed
  or made. Read-only build: only the pack + this bookend changed. Verified merged via
  `verify-branch-merged.mjs` (exit 0), working tree clean.
