# #1469 — Build Queue: add a "Verifying" state between session completion and real Done

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-28 ⏳ IN FLIGHT — implementing a new `verifying` bt_build_queue status: a
  session exiting with exit code 0 on a row with a real github_number lands on
  `verifying` (not `done`), stays visible in the active Build Queue view, and is
  promoted to `done` only when a manual GitHub refresh confirms the real issue
  closed (reusing GetOpenIssueNumbersAsync, respecting the manual-refresh-only
  GitHub discipline from #29/#35/#37). Rows with no github_number keep the
  existing immediate-done fallback.
