# #1469 — Build Queue: add a "Verifying" state between session completion and real Done

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 3e323cb38 (bookend), ad136b338 (real work, on origin/main)

## Log
- 2026-08-28 ⏳ IN FLIGHT — implementing a new `verifying` bt_build_queue status: a
  session exiting with exit code 0 on a row with a real github_number lands on
  `verifying` (not `done`), stays visible in the active Build Queue view, and is
  promoted to `done` only when a manual GitHub refresh confirms the real issue
  closed (reusing GetOpenIssueNumbersAsync, respecting the manual-refresh-only
  GitHub discipline from #29/#35/#37). Rows with no github_number keep the
  existing immediate-done fallback.
- 2026-08-28 ✅ DONE — implemented `BuildQueuePostgresClient.VerifyingStatus`
  ("verifying"), an in-SQL CASE in `MarkCompleteAsync` that lands a successful
  exit on `verifying` when the row has a real `github_number` (else falls
  straight to `done`, unchanged), and a new `PromoteVerifyingToDoneAsync` that
  promotes `verifying` -> `done` once a caller-supplied open-issue-number set
  (from the existing `GetOpenIssueNumbersAsync`) shows the issue closed. Wired
  into the two existing manual-refresh GitHub touchpoints (MainWindow's Home-tab
  force-refresh, BuildWatchWindow's dormant on-demand `CheckIssueClosuresAsync`)
  so no new `gh` CLI traffic was introduced. `AreBlockersClearedAsync` treats
  `verifying` the same as `done` so dependent queued builds aren't blocked
  waiting on Shane's manual issue-close. BuildQueuePanel's Active filter,
  reveal-target, graph dot, card colors/pill, and critter mood all render
  `verifying` distinctly (sapphire, magnifier icon). Build Watch's slot reuses
  the existing `Verifying` pill built for #943's orphan-sweep ambiguity, given
  its own tooltip so the two meanings don't get conflated. `dotnet build`
  (Debug) succeeded, 0 errors, only pre-existing warnings. No DB migration
  needed (`status` is a plain text column, no CHECK constraint). Commits
  rebased onto and pushed straight to `origin/main` (fast-forward,
  `ad136b338`); `verify-branch-merged.mjs` confirms the branch is a real
  ancestor of `origin/main`.
