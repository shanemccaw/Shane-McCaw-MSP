# #2533 — Test Pad port: import window/flow

- **Status:** ⏳ IN FLIGHT 2026-09-03T14:47:37Z
- **Scope:** buildconsole
- **Started:** 2026-09-03

## Log
- 2026-09-03T14:47:37Z ⏳ IN FLIGHT — read #2533 body (no comments), #2530's parent Feature and its
  sibling sub-issues (#2531 core services — closed/DONE, #2532 pill/pad windows — closed/DONE, this
  issue the last of the three). Audited ShaneBuilder's real end-to-end import flow:
  `TestPadImportWindow.xaml(.cs)` (two-stage paste→Parse→preview→Import dialog, header-stats line,
  per-row type-chip cycling, feature auto-match dropdown, merge-up/split-back-out/undo-all-merges),
  backed by the already-ported `TestPadImportParser`/`TestPadImportMerger`/`TestPadFeatureMatcher`/
  `TestPadService` in `desktop/BuildConsole/Services/TestPad/` (#2531). Confirmed
  `TestPadFeatureMatcher.Match` already takes BuildConsole's real `GitHubIssueResult` (not
  ShaneBuilder's `GitIssueRow`) and its own doc comment names the real replacement query —
  `GitHubApiClient.SearchIssuesAsync("Feature in:title state:open")` — for ShaneBuilder's
  `GitIssuesService.GetActiveFeaturesAsync()`. Cross-checked BuildConsole's real brush palette
  (`Themes/Colors.xaml`/`DarkTheme.xaml`: `PanelBackgroundBrush`, `CardBackgroundBrush`,
  `Surface0Brush`, `BorderDividerBrush`, `TextPrimaryBrush`/`TextSecondaryBrush`/`TextDisabledBrush`,
  `BlueBrush`/`GreenBrush`/`RedBrush`) against what #2532's already-ported `TestPadWindow.xaml`
  actually uses, and `Services.WindowChromeHelper.Setup(this)` as BuildConsole's real custom-chrome
  dialog pattern (`ManifestViewerWindow`, `MainWindow`). Porting `TestPadImportWindow.xaml(.cs)` now
  as a real modal dialog matching that palette/chrome, wired to the real ported services (no
  reduced/stubbed merge or matching logic), launched from #2532's `TestPadWindow` header next to
  "By feature"/"Copy as markdown".
