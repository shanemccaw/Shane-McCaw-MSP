# #4010 — Onboarding-link accept flow: portal URL fix + consent-kickoff bridge

- **Status:** ✅ DONE 2026-09-14T13:22:17Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 24367e6a8 (implementation), 06f68a3fb (IN FLIGHT bookend)

## Log
- 2026-09-14T13:05:54Z ⏳ IN FLIGHT — fix generate-link URL to /portal/onboarding/:token; add nullable mspId to consent_invite_tokens; new POST /api/public/onboarding/link/:token/start-consent through the existing consent callback tenant-conflict guard; enable "Connect Microsoft 365" in artifacts/portal onboarding-link.tsx; live-verify with synthetic rows and clean up.
- 2026-09-14T13:22:17Z ✅ DONE — generate-link mints /portal/onboarding/:token; consent_invite_tokens.msp_id (migration `lib/db/migrations/manual/4010-consent-invite-tokens-msp-id.sql`, applied to local, line on #1630); POST /api/public/onboarding/link/:token/start-consent burns the link + mints via createConsentInviteForEmail with the issuing mspId; consent callback's single cross-MSP guard now also checks an invite's mspId and new tenants for such invites are created under that MSP (fail-closed on race); portal onboarding-link.tsx Connect button wired. Verified: 68/68 vitest (consent + direct-tenant-provisioning), tsc clean api-server + portal; live E2E on isolated :8099 with synthetic MSP 2651 / zz-test operator — link URL, 200 consentUrl (login.microsoftonline.com/common/adminconsent), token row msp_id=2651, 410 reuse, 404 missing, 403 suspended, callback conflict → tenant-conflict redirect with token unburned; all synthetic rows deleted. Success-path callback not run live (would provision tenant/account/scan) — covered by unit tests. Finding filed: #4043.
