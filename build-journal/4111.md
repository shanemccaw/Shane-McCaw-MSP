# #4111 — Billing: automatic seat pricing from real licensed-user count, with manual service-account exclusion override

- **Status:** ✅ DONE 2026-09-14T21:25:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** f69037beb (rebased to 1d0de8e5e on origin/main)

## Log
- 2026-09-14T20:30:00Z ⏳ IN FLIGHT — investigation done (issue body/comments, license-waste-source.ts header, schema, msp-console routes/frontend nav slot). Building: `resolveActiveLicensedUserCount()` off the existing `license_assignment_snapshots` per-user data, a new `client_billing_overrides` table for the operator's service-account seat exclusion, a new MSP-console route (GET live seat pricing / PUT override / POST apply-to-subscription), and the msp-console Billing nav module.
- 2026-09-14T21:25:00Z ✅ DONE — Shipped: `resolveActiveLicensedUserCount()` (license-waste-source.ts) — distinct users with >=1 paid, non-free SKU assigned, sourced from `license_assignment_snapshots` (never sums per-SKU consumedUnits, which would double-count a user on 2+ SKUs). `client_billing_overrides` table (migration `4111-client-billing-overrides.sql`, run locally against `DATABASE_URL`) — one row per tenant, operator-set service-account seat exclusion + reason. New route `msp-seat-pricing.ts`: GET live seat pricing / PUT override / POST apply (writes `tenant_subscriptions.unitAmountCents`, refuses a Stripe-synced subscription). msp-console `SeatPricing.tsx` + `seat-pricing-api.ts` wired into the previously-unmounted "billing" nav slot in `ConsoleShell.tsx`. Typecheck clean across lib/db, api-server, msp-console. Unit tests added (3 new, all passing) for the distinct-user counting logic. Live-verified against the real local DB: tenant 1's real `/subscribedSkus` catalog resolves (1 paid SKU, 3 correctly excluded as free/unpriced); a temporary snapshot insert (inserted then deleted, no residue) confirmed a user holding 2 paid SKUs counts once, not twice. Filed **#4118** (parented under #1692, the real Feature-tier parent of #4111) for a genuinely pre-existing `nav.test.ts` IA-count drift (24 vs hardcoded 22) found while wiring the new nav module — confirmed unrelated to this build by isolating `git diff` on `nav.ts` (empty) and re-running the isolated test. Full api-server suite (5001 tests) ran clean of regressions from this work; 3 unrelated pre-existing failures (rbac-capability, 2 live-db retention tests) and file-level flakes under parallel load (boot-smoke, nul-byte-guard) both verified to pass in isolation, unrelated domain. Branch verified merged into origin/main. Working tree clean.
