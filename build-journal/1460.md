# #1460 — Migrate /copilot-readiness/remediation-tracker onto the real day-one-baseline scoring model

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 24e12d1164dcf5b47decf59d73dcfa8ecc5bd67c (merged to origin/main)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Investigating: comparing UI/UX of /copilot-readiness/remediation-tracker (old, msp_diagnostic_findings + war-room-pillars) vs /portal-v2/remediation (new, #1381's tenant_pillar_snapshots + pillar-scores) before deciding redirect-vs-port. Checking whether the old route has real customer traffic before changing/removing it.
- 2026-08-28 ✅ DONE — Decided PORT, not redirect: `/copilot-readiness/remediation-tracker` is a genuinely different, real, actively-linked screen (post-purchase project dashboard inside the four-screen Copilot Readiness journey — Reveal → Documents → Remediation Tracker → Proposal → Checkout — with its own chrome, per-signed-phase pricing and "hire Shane for what's left" checkout hand-off) from `/portal-v2/remediation` (the Operate-section CR/hold/runbook workflow tool). Both already share the SAME real task-completion store (`useRemediationTracker()`); only the SCORE (Copilot Gate + six-pillar strip) diverged, sourced from `war-room-pillars` (assessment scan) instead of #1381's day-one-baseline endpoint. Redirecting would have discarded the journey's own chrome/pricing/checkout flow real customers use. Fix: re-pointed the Gate hero, header GateChip and six-pillar strip on `/copilot-readiness/remediation-tracker` (CopilotReadinessRemediationTrackerPage.tsx, RemediationTrackerBody.tsx) onto `useRemediationPillarScores()` (`GET /api/portal/remediation-tracker/pillar-scores`, #1381's real endpoint) — same scoring model portal-v2/remediation already uses, honest null (not a fixture fallback) when unloaded. `view.readinessScore`/`view.pillars` stay untouched for the OTHER three journey screens (Reveal/Documents/Proposal/Checkout), which correctly show the pre-purchase assessment score, a genuinely different real number. Added `data-testid=\"remediation-tracker-pillar-<key>\"` to the pillar mini-cards and extended `test-manifests/copilot-readiness/remediation-tracker-e2e.json` (tag `git-1460`) with an endpoint-contract apiTest plus a uiStep proving the gate score and pillar strip render on the real page. `pnpm -w tsc --noEmit` in msp-portal is clean on the touched files (2 pre-existing unrelated errors in SowScreen.tsx/PortalV2Shell.tsx, untouched by this change). `git merge-base --is-ancestor` confirms 24e12d11 is on origin/main. **Not live-verified via `shaneapp://runTest`**: the dev-server checkout's merge queue (build set 'Portal', 46/56) is currently stuck on an UNRELATED conflict in `test-manifests/portal/security-drilldowns.json` / `portal-v2-security-evidence.tsx` / `secEvidenceData.ts` / `build-journal/1430.md` — files this session never touched (looks like concurrent #1430 work) — so my commit can't merge into the live dev-server checkout to be exercised yet. This is a pre-existing blocker upstream of this change, not something this session caused or should resolve blind. Shane To-Do: once whatever is landing #1430 finishes (or someone clears the stuck 'Portal' build-set queue), re-run `shaneapp://runTest?file=remediation-tracker-e2e.json&onlyTag=git-1460` to get a real pass/fail.
