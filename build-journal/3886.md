# #3886 — Chats panel "New Chat" picker: exclude closed epics, respect Focus milestone

- **Status:** ✅ DONE 2026-09-13T01:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T00:51:43Z — Started. Confirmed `BoardEpic.Status` convention via `GitDetailView.StatePill` (`OrdinalIgnoreCase` against `"CLOSED"`, else treated as open). Confirmed `FocusModeService.Instance.IsActive` / `.ActiveMilestoneNumber` / `.IsIssueInActiveMilestone(int?)` exist as documented. Implementing filter in `PopulateNewChatPickerHost()`.
- 2026-09-13T01:05:00Z — ✅ DONE — `PopulateNewChatPickerHost()` in `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs` now excludes closed epics (`ep.Status != "CLOSED"`, OrdinalIgnoreCase) and, when `FocusModeService.Instance.IsActive`, further narrows to epics where `IsIssueInActiveMilestone(ep.GithubNumber)` is true. "Decide later" row unaffected. Added an honest empty-state message for a fully-filtered-out list. Commit `73325be4a37fc0dc1c4f1299abf62544eb81f3d7` on `main`. Verified: `dotnet build -c Debug` succeeded, 0 warnings/errors. `verify-branch-merged.mjs` confirmed the branch is an ancestor of `origin/main`. No BuildConsole runner/WebView2 available in this session to click through the picker live — build + code inspection is the real verification performed; logic matches the issue's spec exactly (reuses `FocusModeService.Instance` verbatim, no re-derivation).
