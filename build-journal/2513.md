# #2513 — runSlaEngineForTenant resolves customerId as users.id, so sla-status always reads empty

- **Status:** ✅ DONE 2026-09-03T11:01:16Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** ba65211e9 (bookend IN FLIGHT), d40e77480 (fix)

## Log
- 2026-09-03T10:56:53Z ⏳ IN FLIGHT — Investigated `runSlaEngineForTenant()` (`artifacts/api-server/src/lib/sla-engine.ts:276-305`) and confirmed the bug as described in #2513: it resolves its `customerId` arg through `usersTable.id` joined to `tenantsTable`, but every caller (portal route, admin route, dashboard-resolvers) passes the JWT/route `customerId`, which is already a tenant id. Fixing to mirror `runScopeCreepEngineForTenant()`'s shape — apply the id straight to `sla_timers`/policies, no users-table bridge.
- 2026-09-03T11:01:16Z ✅ DONE — Rewrote `runSlaEngineForTenant()` to apply `customerId` straight to `fetchRunningTimers(undefined, customerId)` / `fetchPolicies()`, no `usersTable`/`tenantsTable` join; removed the now-unused `usersTable`, `tenantsTable`, `eq` imports. `npx tsc --noEmit` shows the same pre-existing `lib/db/dist` / `drizzle-orm` resolution errors on every file in this worktree (confirmed identical on an untouched sibling file, `scope-creep-engine.ts`) — a known worktree dependency-isolation issue, not caused by this change; no new errors specific to `sla-engine.ts`. `vitest` itself is unrunnable in this worktree (dangling `node_modules/vitest` symlink into the main checkout, pre-existing, not remedied via `pnpm install` per the hard rule) — no dedicated `sla-engine.ts` test file exists, and every caller test file (`dashboard-resolvers.test.ts`, `dashboard-data.test.ts`, `portal-dashboard-route-collision.test.ts`, `portal-customer-engines-assessment-redaction.test.ts`, `security-secure-score-464.test.ts`) mocks `runSlaEngineForTenant` wholesale, so none exercise the internal id-resolution logic that changed. Verified the actual fix live against local Postgres instead: inside a rolled-back transaction, inserted a `running` `sla_timers` row for `customer_id = 1` (a real tenant id) — the OLD query (`users.id = 1` joined to `tenants`) returned 0 rows (bug reproduced: `users.id = 1` is Shane's own PlatformAdmin row with `tenant_id IS NULL`), the NEW query (`sla_timers WHERE status = 'running' AND customer_id = 1`) returned the 1 real row (fix confirmed). Working tree clean, branch verified merged into `origin/main` via `verify-branch-merged.mjs` (`d40e7748` merged, OK).
