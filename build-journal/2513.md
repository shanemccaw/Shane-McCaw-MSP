# #2513 — runSlaEngineForTenant resolves customerId as users.id, so sla-status always reads empty

- **Status:** ⏳ IN FLIGHT 2026-09-03T10:56:53Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-03T10:56:53Z ⏳ IN FLIGHT — Investigated `runSlaEngineForTenant()` (`artifacts/api-server/src/lib/sla-engine.ts:276-305`) and confirmed the bug as described in #2513: it resolves its `customerId` arg through `usersTable.id` joined to `tenantsTable`, but every caller (portal route, admin route, dashboard-resolvers) passes the JWT/route `customerId`, which is already a tenant id. Fixing to mirror `runScopeCreepEngineForTenant()`'s shape — apply the id straight to `sla_timers`/policies, no users-table bridge.
