# #2076 — Owner=this coupling on BuildWatch/StickyNotes/LinkedIn floaties may also pull MainWindow into view

- **Status:** ✅ DONE 2026-09-06T15:44:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** `4c02beae0` (fix), bookend commits `a458f2d4d` (IN FLIGHT) / this commit (DONE)

## Log
- 2026-09-06T15:24:00Z ⏳ IN FLIGHT — Read #2076 body/comments + #2074's real fix (`MainWindow.FloatingChat.cs`). Applying the same pattern (remove `Owner = this`, replace auto-close-with-app via an explicit `MainWindow.Closed` hook) to `StickyNotesWindow` (`ToggleStickyNotes()`), `BuildWatchWindow` (`ToggleBuildWatch()`), and `LinkedInComposerWindow` (`MainWindow.LinkedInComposer.cs`'s `ToggleLinkedInComposer()`).
- 2026-09-06T15:44:00Z ✅ DONE — Commit `4c02beae0`. Removed `Owner = this` from all three windows in `ToggleStickyNotes()` / `ToggleBuildWatch()` (`MainWindow.xaml.cs`) and `ToggleLinkedInComposer()` (`MainWindow.LinkedInComposer.cs`); replaced the auto-close-with-app behavior each relied on Owner for with an explicit `this.Closed += (_, _) => { try { if (win.IsLoaded) win.Close(); } catch { } };` hook on MainWindow — same pattern #2074 landed for `FloatingChatWindow`. `StickyNotesWindow` and `LinkedInComposerWindow` keep their existing always-on-top behavior unaffected, since both already declare `Topmost="True"` in their own XAML independent of Owner (confirmed no `ForceTopmost()`-style SetWindowPos mechanism was relying on Owner either — neither window has one). `BuildWatchWindow` audited and confirmed it is NOT a Topmost/always-on-top window at all (`ShowInTaskbar="True"`, no `Topmost` in its XAML) — its `Owner = this` was purely for auto-close-with-app, now replaced the same way; no topmost regression risk there. Verified: `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. `verify-branch-merged.mjs` — OK, merged into `origin/main`. `git status --porcelain` — clean.
