# #1437 — Claude Weekly status bar: show Primary AND Secondary usage simultaneously

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 13e16a30d

## Log
- 2026-08-27 ⏳ IN FLIGHT — investigating ClaudeUsageMeterService.RunClaudeCliUsageAsync(), QueueWatcherService.cs secondary-account env pattern (#1420), and status bar UI to add an independent Secondary usage poll + display alongside Primary.
- 2026-08-27 ✅ DONE — ClaudeUsageMeterService now polls the Secondary account (CLAUDE_CONFIG_DIR=SecondaryClaudeConfigDir + stripped CLAUDE_CODE_OAUTH_TOKEN/ANTHROPIC_API_KEY, mirroring QueueWatcherService's #1420 fix) every cycle alongside the unchanged Primary poll, with fully independent state (percent/reset/countdown/projection via new SecondaryWeeklyUsageHistoryStore) and an honest "not configured"/Unavailable/Error state on failure — never silently reuses Primary's numbers. Status bar shows both "Claude Weekly (Primary): X%" and "Claude Weekly (Secondary): Y%" side by side, always visible, independent of the title-bar toggle. Logging on the existing `usage-meter` channel, `[secondary]`-tagged lines for the new poll. `dotnet build BuildConsole.csproj -c Debug` succeeds (0 errors). Honest verification gap: did not launch a live BuildConsole instance to visually confirm the two status-bar meters render side by side and independently disagree under real dual-account conditions — this is desktop-tooling UI behavior (not in-scope for the msp-portal/marketing test-manifest requirement), and a live check needs a real configured Secondary `.claude-secondary` session to be meaningful.
