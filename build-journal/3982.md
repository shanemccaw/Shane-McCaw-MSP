# #3982 — Verifying review view + mouseover quick-actions: confirm/not-a-bug/reject

- **Status:** ✅ DONE 2026-09-14T03:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** be8940a7e

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — investigating TestModeComposerPanel/VisualTestTrackerStore/Send-to-Chat pattern; schema already migrated (#3978) and #3981 (three-state status rename) confirmed DONE on main. Building: Verifying review filter, Design filter (per follow-up comment), mouseover quick-actions (Confirm/Not-a-bug/Reject/Mark for Design), Reject evidence hand-off via existing Send-to-Chat reuse — no GitHub calls.
- 2026-09-14T03:20:00Z ✅ DONE — #3981 had already landed most of the three-state plumbing plus a working Not-a-Bug/Design-toggle pair, so this build's real scope was: (1) a Design filter (bypasses status entirely, "every is_design=true row regardless of status") added to both BugsDocumentView (the real cross-session Postgres "Verifying review" queue) and TestModeComposerPanel's drawer; (2) replaced the old generic Open→Verifying→Closed cycle button in both places with explicit per-state actions (Mark Verifying/Confirm/Reject/Re-open) so Confirm and Reject are only ever exposed on a Verifying row, per the issue's own enablement rule; (3) Reject reopens the bug locally and hands its real captured evidence (VisualTestTrackerEntry.ToMarkdown() in the document view; notes/steps/expected-actual/screenshots in the drawer) to the existing shared SendTextToActiveClaudeChatAsync (#937) path — zero GitHub calls anywhere in the diff (grepped and confirmed: every "GitHub" hit in the diff is doc-comment prose or the entry's own git_issue_number reference, never a GitHubApiClient/Octokit call). Verified against real local Postgres: Confirm→Closed/Fixed, Reject→Open (resolution cleared), Not-a-Bug with reason→Closed/NotABug/reason, Not-a-Bug without a reason blocked by the real chk_vtt_resolution_reason CHECK constraint, is_design toggling independent of status. `dotnet build desktop/BuildConsole/BuildConsole.csproj` — 0 errors. No genuine findings surfaced worth filing separately. Commit be8940a7e pushed and confirmed merged into origin/main; working tree clean.
