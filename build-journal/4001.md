# #4001 — Public Share Pages: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:00:58Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** be4969e2d (real work), 2d23d21a8 (IN FLIGHT bookend) — both merged into `origin/main`

## Log
- 2026-09-14T03:53:02Z ⏳ IN FLIGHT — read #4001/#1663/#2453 + contract pack
  (`docs/public-share-pages-contract-pack.md`) + design export
  (`Design/portal/design_handoff_full_site/screens/Public Share Pages.dc.html`,
  plus the `share-viewer` states in `Documents.dc.html`). About to recreate the
  3 unauthenticated routes (`msp-sow-public.tsx`, `shared-document-public.tsx`,
  `shared-live-documents-public.tsx`) in `artifacts/portal/`, wired to the 5 real
  live endpoints the pack documents, carrying forward request/response shape
  and validation from the archived `portal-archive-2026-08-29` pages.
- 2026-09-14T04:00:58Z ✅ DONE — shipped all 3 pages
  (`shared-document-public.tsx`, `shared-live-documents-public.tsx`,
  `msp-sow-public.tsx`) plus `lib/public-share-types.ts`,
  `lib/public-share-api.ts`, and `components/public-share/PublicShareShell.tsx`
  (dark-forced shell, same precedent as `AuthPageShell`). Reused the existing
  `SignaturePad` component for SOW signing rather than re-implementing a
  canvas. Registered all 3 as flat public routes
  (`/shared-documents/:shareToken`, `/shared-live-documents/:shareToken`,
  `/sow/:shareToken`) outside `RequireAuth`/`ProtectedRoutes` in `App.tsx`,
  matching the paths the server's mint routes already construct
  (`getMspPortalBaseUrl()` + these exact suffixes). Every field traces to the
  contract pack's own wire tables; no fixture module, no invented data.
  Verification: `pnpm run typecheck` (tsc, clean) and `pnpm run build` (vite,
  clean) in `artifacts/portal/` after every rebase; live-curled all 3 real
  endpoints on the local dev api-server (`:8080`) with a bogus token and
  confirmed the 404/410 error-shape strings match what the pages branch on
  (`{"error":"Share link not found"}`, `"This link doesn't exist or has been
  removed"`, `"SOW not found or link has expired"`). Did **not** mint a real
  `live_document_shares` row to exercise the 200 path end-to-end — the local
  DB has no existing rows for any of the 3 tables to test against, and
  minting one would trigger the route's live, metered Anthropic narrative
  generation for all 7 reports purely for verification, which the route's
  own file header already flags as a real cost to be careful with. Instead
  verified the 200 response shape by reading `live-document-shares.ts` in
  full and cross-checking every field against the contract pack and this
  page's types — same rigor the contract pack itself extracted with. No new
  findings filed — the contract pack's own Step-1 result (the `:id` →
  `:shareToken`/`:token` param-name difference) was already carried through
  correctly into every fetch call. `git status --porcelain` clean at both
  IN FLIGHT and DONE. Branch merged into `origin/main`
  (`verify-branch-merged.mjs`: OK).
