# #3102 — portal dashboard priorityItems query filters by run_id alone, with no customer_id predicate backing it

- **Status:** ✅ DONE 2026-09-09T23:31:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** de1014aae (fix), abff26b5e (merge to main)

## Log
- 2026-09-09T23:22:53Z ⏳ IN FLIGHT — investigating `artifacts/api-server/src/routes/portal-customer-engines.ts` `findingRows` query, adding `customer_id` predicate per issue body's suggested fix, sweeping other `mspDiagnosticFindingsTable` readers for the same run-id-only pattern.
- 2026-09-09T23:31:00Z ✅ DONE — Added `eq(mspDiagnosticFindingsTable.customerId, customerId)` to the `findingRows` query's `where` clause in `portal-customer-engines.ts` (commit de1014aae), mirroring the sibling `latestFindingsRun` query's existing predicate. Ran the existing `portal-customer-engines-assessment-redaction.test.ts` suite (5/5 passed) and `tsc --noEmit` on the file (clean, exit 0) to verify. Swept all 15 other `mspDiagnosticFindingsTable.runId` readers in `artifacts/api-server/src` via an Explore agent — no other live instance of the same bug found; every other reader either has `customerId` directly in its own `where`, sources its `runId` from a query already scoped by `customerId`/`mspId`, or is an MSPOperator-only staff route intentionally cross-customer. Filed #3362 (parented under #2491 per dispatch instructions, labeled `bug`+`security`, board status set to "AI Batter Up") for one fragile spot found during the sweep — `msp-diagnostics.ts:1085` (`/portal/diagnostics/runs/:runId`) relies on a preceding ownership check rather than its own `customer_id` predicate; not a live leak today, same class of hardening gap. Merged branch into `main` (commit abff26b5e), pushed, verified ancestor via `verify-branch-merged.mjs` (OK). Working tree clean.
