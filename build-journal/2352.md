# #2352 — Import: per-row merge-up button

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Feature: Test Pad, #2326)
- **Started:** 2026-09-02
- **Commit(s):** 526b45f5496349253663a2f65c09997603cc3a5e

## Log
- 2026-09-02T19:46:00Z ⏳ IN FLIGHT — Test Pad import preview (TestPadImportWindow) already has
  multi-select "Merge N up" (#2353, closed) via TestPadImportMerger.MergeSelectedUp. This issue
  adds the per-row quick action: a single button on each preview row that merges just that one
  row up into whatever row precedes it, without needing to tick the select checkbox first.
- 2026-09-02T20:05:00Z ✅ DONE — Added `TestPadImportMerger.MergeRowUp(candidates, row)`, a
  single-candidate wrapper around the existing fold primitive (refactored the shared fold logic
  out of `MergeSelectedUp` into a private `MergeOneUp` both now call). Wired a per-row "↑ merge
  up" chip into `TestPadImportWindow.BuildPreviewRow` — visible on every row except the topmost
  visible one (nothing above it to merge into) — clicking it folds just that row's text up into
  the preceding row and re-renders, same "+N merged" badge / merged-away skip-on-render behavior
  #2353 already established. Distinct from the multi-select "Merge N up" bar: no select tick box
  needed, one click on one row. Verified with `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj
  -c Debug` — 0 warnings, 0 errors. Branch verified merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs` (526b45f5 — OK). Working tree clean.
