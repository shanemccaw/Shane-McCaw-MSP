# #3340 — Build Queue panel: 'Running' filter should show ONLY running, not Verifying too

- **Status:** ✅ DONE 2026-09-09T19:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-09T19:10:00Z — Starting. Narrow fix to `BuildQueuePanel.ApplyFilter`'s `"Running"`
  case (~line 1556): drop `VerifyingStatus` so it matches exactly `i.Status is "running"`, and
  update the stale #1829 comment above it. `Queued`, `RunningAndQueued`, and the standalone
  `Verifying` filter (#1927) are untouched.
- 2026-09-09T19:20:00Z — ✅ DONE — commit 391eca627 changed `"Running"` in `ApplyFilter`
  (`desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`) to `i.Status is "running"` only,
  replaced the stale #1829 comment with one describing the real #3340 rationale. `dotnet build`
  of `BuildConsole.csproj` succeeded (0 errors/warnings). `Queued`, `RunningAndQueued`, and
  `Verifying` filters left byte-for-byte unchanged. Branch verified merged into origin/main
  (verify-branch-merged.mjs OK, 391eca62). Working tree clean.
