# #2392 — A link matching a Dev URL groups into Dev

- **Status:** ✅ DONE 2026-09-10T17:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 94c37b728 (bookend IN FLIGHT), 4e13632f1 (implementation, reverted), 4a2beaabe (revert), (this commit)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — pulled #2392 body + comment, reviewed sibling
  sub-issues #2389-#2395 (none built yet at the time), and investigated the existing
  ShaneBuilder tab/workspace infra (`TabKind`/`WorkspaceDef`, `OpenKeepAliveBrowserTab`
  in `MainWindow.KeepAliveTabs.cs`) plus BuildConsole's own real Dev/Staging/Production
  URL conventions (`BuildTrackerConfig.cs`) to classify against.
- 2026-09-10T16:00:00Z (implementation, later reverted) — built a real `FavoritesService`
  (persisted `favorites.json`, `ClassifyWorkspace` matching localhost→Dev,
  `*.replit.dev`/`*.replit.app`→Stage, `shanemccaw.com`→Production, else→Web), new
  `web`/`dev`/`stage`/`production` workspace entries replacing the old inert
  `"favorites"` placeholder, `OpenFavoriteTab`/`OpenAddFavoriteFlow`, and a Command
  Palette "Add Favorite Link" entry point. Compiled clean (`dotnet build`, 0 errors).
  Before pushing, rebasing onto current `origin/main` surfaced that sibling issues
  #2393 and #2394 (Stage/Production, same Feature) had, in the meantime, been closed
  as investigation-only: **this issue's own Feature parent (#2388, "Feature:
  Favorites — quick-access links (ShaneBuilder)") was closed `NOT_PLANNED` on
  2026-09-06T17:51:36Z** — "ShaneBuilder is deprioritized in favor of BuildConsole
  (#1202) per Shane's real call this session ... If any of this scope is still
  genuinely wanted, it should be re-filed against BuildConsole with current context
  rather than reopened as-is." That decision was recorded four days before this
  session started but this session did not check for it until after implementing.
- 2026-09-10T17:15:00Z ✅ DONE — no code changed (the implementation above was
  reverted, clean revert, no conflicts). Building the Dev-URL-matching Favorites
  grouping into ShaneBuilder would directly conflict with the recorded `NOT_PLANNED`
  decision on its own Feature parent — a genuine product-decision conflict per
  CLAUDE.md's stop condition, not a missing-backend case. This matches the identical
  handling already applied to sibling issues #2389, #2390, #2391, #2393 and #2394
  under the same closed Feature. #2392 itself was not closed by this session
  (closing is Shane's call); a comment was posted on #2392 explaining the situation
  and recommending re-filing under BuildConsole (#1202) with current context, or
  closing as moot alongside its siblings. Labels: removed `in-flight`, added
  `complete`. Working tree left clean, commits pushed to `main`.
