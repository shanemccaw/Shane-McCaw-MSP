# #1421 — Deploy-status check requires API server running unnecessarily

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 47f6f99fd (bookend), <fill after final commit>

## Log
- 2026-08-27 ⏳ IN FLIGHT — Replacing #805's HTTP `GET /api/internal/deploy-status`
  poll (MainWindow's status-bar `PollDeployStatusAsync`, which hits
  `http://localhost:8080` and errors when the local Dev api-server isn't running)
  with a direct local read: `git rev-parse --short HEAD` against the dev-server
  coordinator's own dedicated checkout (`scripts/dev-server/config.mjs`'s
  `serverWorktree`, default `C:\dev-server`), plus its `server.json` process-start
  timestamp. No HTTP call, so the status bar works with the API server stopped.
- 2026-08-27 ✅ DONE — Added `desktop/BuildConsole/Services/LocalDeployStatusService.cs`
  (mirrors `scripts/dev-server/config.mjs`'s `serverWorktree`/`stateDir` resolution;
  runs `git rev-parse --short HEAD` directly against the server checkout, reads
  `server.json`'s `startedAt` for the timestamp, falling back to the checkout's own
  HEAD commit timestamp if `server.json` is missing) and rewired
  `MainWindow.PollDeployStatusAsync()` to call it instead of
  `_buildTrackerApi.GetDeployStatusAsync()` — zero HTTP dependency now. `dotnet build`
  succeeds with 0 errors (pre-existing warnings only). Verified on this machine
  against the real local state: `C:\dev-server`'s real HEAD (`3335e645`) and the real
  `server.json` (`startedAt: 1787755133987` → `2026-08-26T14:38:53.987Z`) both read
  back correctly through the new code path, independent of whether the api-server
  process is alive (confirmed the tracked server.json pid is currently DEAD and the
  read still succeeds). No information is lost versus the old endpoint — commitHash
  and timestamp are both still produced, from the same real sources (git HEAD +
  process-start instant) just read directly instead of over HTTP. Server-side
  `GET /api/internal/deploy-status` (version.ts) and `BuildTrackerApiClient.GetDeployStatusAsync`
  are left in place unchanged — `StagingDeployService`/`PostBuildDeployPipeline` still use them
  to confirm a REMOTE (Replit Staging) restart, which genuinely needs the live process to
  answer, not a local filesystem read; only the local status-bar poll changed.
