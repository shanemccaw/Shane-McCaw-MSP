# #3274 — Feature: Settings restructure -- House/You/Connected/Activity tabs (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** ee464bb20

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3274's body + Design/design_handoff_shanes_life/README.md
  "Start here" (item 6) and the two relevant "what changed" sections (1b, 16). Real current state
  confirmed: `web/shanes-life/public/app.js` `viewSettings()` is one flat page (Account, Passkeys,
  Notifications, Health context, Banks, Places, Claude (MCP), Home Screen widget, Tesla, Recent
  activity) with no `settingsTab` state and no room header at all (falls through to the old generic
  `#app-header` bar). Plan: add a `settingsTab` module-level tab state (same transient client-only
  idiom as `moneyTab`), a real segmented control matching Money's `.money-tabs`/`.money-tab` visual
  pattern, give Settings its own `roomHeader()` ("Settings · the attic", slate tint, matching Vault's
  `148,163,184`) with a per-tab subtitle line, and re-home every existing section into House / You /
  Connected / Activity per the design's own grouping -- no rebuild of any section's own content.
- 2026-09-08T01:00:00Z ✅ DONE — commit ee464bb20 (`Settings restructure: House/You/Connected/Activity
  tabs (Git #3274)`), rebased onto origin/main (which had moved to include #3276 while this session
  worked) and pushed as a fast-forward, `verify-branch-merged.mjs` confirms merged. Shipped: a real
  `settingsTab` segmented control (`.settings-tabs`/`.settings-tab`, same pattern as Money's own
  `.money-tabs`), a real `roomHeader()` for Settings ("Settings · the attic", slate tint) with a
  per-tab subtitle that follows the active tab (literal design example "the attic · N banks need
  you" reproduced live), and every existing section re-homed (not rebuilt) into House (room order,
  #3215) / You (Account, Passkeys, Notifications, Health context, #3214) / Connected (Banks +
  webhooks, Places, Claude (MCP), Home Screen widget, Tesla) / Activity (Recent activity, #3214).
  `renderBankSettings()` now takes an optional preloaded-data param so the Connected tab's own
  subtitle fetch and its render share one `/api/money/banks` call instead of two.
  **Real live verification**, not simulated: ran the real server against the local `finances`
  Postgres DB, created a real disposable account + WebAuthn passkey (SoftAuthenticator, the same
  mechanism `bin/check.mjs` already uses), obtained a real session cookie, handed it to a real
  headless Chromium (Playwright, launched against an already-cached browser build on disk rather
  than downloading a fresh ~300MB copy) and drove the actual UI: header title + subtitle, all four
  tabs present in order, House the default active tab, and each tab's real content confirmed
  present after a real click. 9/9 checks passed. Scratch verification script deleted before commit;
  working tree was clean at every checkpoint. No schema change, no migration, no new endpoint --
  this was a pure client-side reorganization, matching the issue's own scope.
