# #4297 — MyArchitect portal form-filler (read real DOM, right-panel entry, readback confirmation)

- **Status:** ✅ DONE 2026-09-15T23:44:00Z
- **Scope:** buildconsole (MyArchitect WPF operator cockpit — code lives in shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect `103524a` (`103524ada9472f9b1fe418a5402d5f1b7ed76cd9`) on `main`

## Log
- 2026-09-15T23:30:00Z ⏳ IN FLIGHT — Blockers #4278 (repo extraction) and #4284 (accessibility rules) both CLOSED. Starting the DOM-read + right-panel form-fill + readback feature per the issue body and Shane's 2026-09-15 correction (pre-fill from associated CR).
- 2026-09-15T23:44:00Z ✅ DONE — Shipped in shanemccaw/MyArchitect `103524a`. What landed:
  - **`Models/PortalFormField.cs`** — descriptor for one editable field read from the live DOM (index marker, best-effort label, tag/type, value, placeholder, name, select options). No fabricated data.
  - **`Services/PortalFormScanner.cs`** — (1) `ScanAsync`: a generic best-effort DOM scan (`ExecuteScriptAsync`) of every `input`/`select`/`textarea` on the currently-loaded page with real associated label text (`<label for>` → wrapping `<label>` → `aria-label` → `aria-labelledby` → placeholder → name); stamps each element `data-myarch-ff=<i>` for stable re-selection; explicitly NOT hardcoded per-portal field maps. (2) `InjectAsync`: the React-safe write-back — native value setter (`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set.call(el,v)`) + dispatched bubbling `input`/`change` events; selects matched by option; reads the element's value back and reports whether the framework registered it. (3) `SuggestFromChangeRequest`: pre-fill from the currently-open CR by label correspondence (domain/tenant/description/title/target/ticket/requested-by), and deliberately returns nothing for person/name fields (a CR has no authoritative name value — those use the readback).
  - **`Shell/ShellContracts.cs`** — `PortalFormFillSpec` / `PortalFormFillFieldSpec` added; `RecordWorkspaceSpec.FormFill` optional section.
  - **`Shell/RecordWorkspacePanel.xaml.cs`** — renders the form-fill section inside the existing right-panel record workspace (UI_RULES.md §3, not a new panel): per-field entry (TextBox / ComboBox for selects), a live **letter-spaced readback** (larger Segoe UI via Typography keys, thin-space separated so a transposition like "Kaern"/"Karen" stands out — UI_RULES.md §11), a source-prefill provenance line, and a confirm-armed "Set into form" action reporting the honest live outcome. Built imperatively so live typing doesn't lose focus.
  - **`Themes/ThemeBrushes.cs`** — `Theme.Size`/`Theme.Font` helpers so the readback pulls its size/family from `Typography.xaml` (§10/§11) rather than raw literals.
  - **`MainWindow.xaml` / `MainWindow.xaml.cs`** — a "Read Form" toolbar button next to the dark-mode toggle; captures the currently-open CR context (`_undoTargetChangeRequest`) BEFORE opening the portal-form record, scans the active tab, and opens the form-fill record in the workspace. Rescan re-runs the scan.

### Verification (honest)
- **Clean build ×2:** `dotnet build MyArchitect.csproj` in the shared clone (0 warn / 0 err) AND in a fresh isolated worktree checked out at my exact pushed commit `103524a` (0/0) — proving the commit is self-contained and does not depend on a concurrent session's uncommitted files.
- **Injected JS syntax-validated** (`node --check`) for both the scan and inject scripts.
- **NOT done in-session — real live verification against an actual Microsoft admin page** (e.g. the M365 Admin Center Create User form). This requires an interactive M365 sign-in inside the app's WebView2, which a headless build session cannot drive. The React-safe native-setter + real-event technique is the documented standard for framework-controlled inputs, but the issue explicitly (and correctly) requires confirming it on the real target. **Flagged `Shane To-Do`** with exact manual steps in the issue comment. This is the honest coverage limit the issue asked to be stated up front.

### Concurrent-session note (not mine — left untouched)
The shared checkout `C:\Source\ShaneMcCawConsulting\MyArchitect-clone` had, and still has, uncommitted work from a concurrent #4296 (technical-term autocorrect) session: modified `Shell/CommandMatcher.cs`, `Shell/ShellContracts.cs`, `Shell/RecordWorkspacePanel.xaml.cs`, and new untracked `Models/MonitorCheckSummary.cs`, `Models/Sop.cs`, `Models/TechnicalTerm.cs`, `Shell/TermMatcher.cs`. I committed ONLY my own 7 files via explicit pathspec and verified my commit contains my additions and none of theirs. Their files were left exactly as found.
