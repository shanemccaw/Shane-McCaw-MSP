# #3172 — Feature: GigSparkline component (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** (none — stopped for a product decision, see Log)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — Read #3172, its parent #3086, and the blocker
  #3137 (closed/complete — not actually blocking). Read `web/shanes-life/` end to end:
  it is a plain vanilla-JS PWA (`public/app.js`, 5244 lines, using a hand-rolled `el()`
  DOM helper) with a real Node backend (`src/core/money.mjs`, `src/routes/`), **not**
  React — despite the design contract's own stated stack ("Use the Shane McCaw MSP
  design system (React + Tailwind v4 + shadcn ...)"). Money's Now/Bills tabs are
  already real and shipped (Git #3147, #3148) using this same vanilla pattern.
  Found a real, direct conflict: `Design/design_handoff_shanes_life/README.md`
  states explicitly, twice, under "Non-negotiables (from the contract)" and again in
  the Money screen spec: **"No charts anywhere except one bar per pay period in
  Money."** #3172 asks for a tap-to-scrub sparkline used "wherever a small real
  time-series appears — income history, spending trend, savings trend, per-bill
  funding trend" — i.e. exactly the kind of chart the design contract forbids
  everywhere outside the one Money progress bar. This is a direct conflict with a
  decision already recorded on the design contract, not a missing-column/missing-
  endpoint gap — stopping per CLAUDE.md's "When to actually stop and ask" rule
  rather than building a component that contradicts the contract Shane signed off.
  No code changed. Commented on #3172 laying out the conflict and asking which way
  Shane wants it resolved before any build proceeds.
