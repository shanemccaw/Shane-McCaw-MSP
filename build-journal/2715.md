## #2715 — Test Pad note pop-out is a real modal dialog — should be non-modal always-on-top floaty

- **Status:** ✅ DONE 2026-09-03T19:26:00Z
- **Scope:** buildconsole

### Log
- 2026-09-03T18:41:00Z — Starting: `TestPadNoteDetailWindow.ShowFor` uses `dlg.ShowDialog()` (modal). Fix: match `StickyNotesWindow.xaml`'s convention — `Topmost="True"` in XAML + plain `.Show()`.
- 2026-09-03T19:26:00Z — ✅ DONE — `TestPadNoteDetailWindow.xaml`: added `Topmost="True"`. `TestPadNoteDetailWindow.xaml.cs` `ShowFor`: `dlg.ShowDialog()` → `dlg.Show()`. Confirmed the sole call site (`TestPadWindow.xaml.cs:407`) doesn't rely on the modal block/return value, and `Close()` (Esc + button) has no `DialogResult` dependency. `dotnet build BuildConsole.csproj` — 0 errors/0 warnings. Commit `e0a80407e`, pushed to `origin/main`, verified merged via `verify-branch-merged.mjs`. Working tree clean.
