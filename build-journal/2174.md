# #2174 — Epic #2138: ShaneBuilder — port Toast/Notification system

- **Status:** ✅ DONE 2026-09-02T00:12:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 64f0748ca (bookend IN FLIGHT), 4b511f89e (real port)

## Log
- 2026-09-01T23:53:12Z ⏳ IN FLIGHT — audited live BuildConsole ToastEngine.cs / ToastKind.cs / ToastHostWindow.xaml(.cs) / ToastCard.xaml(.cs); porting into desktop/ShaneBuilder, restyled against the #2147 token dictionary (Themes/Colors.xaml), not the old #2126 palette.
- 2026-09-02T00:12:00Z ✅ DONE — Ported ToastEngine, ToastKind, ToastHostWindow, ToastCard verbatim in behavior into `desktop/ShaneBuilder/Notifications/`, namespace `ShaneBuilder`. Restyled: added a "12. Toast Notifications" section to `desktop/ShaneBuilder/Themes/Colors.xaml` with `Brush.Toast.Info/Success/Warning/Error` — real `StaticResource` aliases onto existing #2147 tokens (`Brush.Accent.Primary`, `Brush.Alert.Success`, `Brush.FocusNudge.Quiet30m.Fg`, `Brush.Alert.Danger.Border`), not new hardcoded hex, since the #2147 doc has no dedicated Info/Warning entries. `ToastCard.xaml` uses `Brush.Bg.Card`/`Brush.Border.Card`/`Brush.Text.Heading`/`Brush.Text.Muted`; the old BuildConsole `IconButton` style doesn't exist yet in ShaneBuilder so the ✕ close button gets a small local `ToastCloseButton` style scoped to the card instead of inventing a global one out of scope for this issue.
  API confirmed unchanged and forward-compatible: `ToastEngine.Show(...persistent: true)` / `ShowPersistent(...)` never starts the auto-dismiss timer (only the ✕ or a self-dismissing `onClick` closes it) — the #2026 persistent-actionable-notification foundation. `onClick` makes the whole card body a hand-cursor click target that fires the callback then dismisses — the #2159 chat-response click-to-jump foundation. Neither is wired to a real trigger yet (out of scope here, per the issue).
  Verification: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors. Live smoke test: temporarily wired all 4 `ToastEngine.*` kinds + `ShowPersistent` into `App.OnStartup`, ran the real built `.exe`, confirmed the process launched and stayed responsive (no crash/exception dialog) with all five toasts firing, then reverted the scratch `App.xaml.cs` edit before committing (not part of the shipped diff).
  Branch verified merged into origin/main via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean at completion.
