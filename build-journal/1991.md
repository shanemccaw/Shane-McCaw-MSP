# #1991 — Add Bash(gh:*) to permission allowlist

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** a878a46e3 (bookend IN FLIGHT), a16850874 (allowlist edit)

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding `Bash(gh:*)` to `.claude/settings.json` allowlist, auditing for other missing entries (dotnet etc.), and checking whether `Bash(...)` rules match `PowerShell` tool invocations.
- 2026-08-30 ✅ DONE — Added `Bash(gh:*)` and `Bash(dotnet:*)` to `.claude/settings.json` `permissions.allow` (commit `a16850874`). Audit: `git`, `psql`/`pg_dump`, `node`, `pnpm`, `grep`/`rg`/`find`/`sed`/`awk`/`wc`/`cat` were already covered; `gh` (issue/PR/api reads every build prompt opens with) and `dotnet` (build/test/run, used across BuildConsole build-journal entries e.g. #1371/#1416/#1617) were the two genuine gaps — both fixed. `git merge-base` is covered by the existing `Bash(git:*)` entry, no separate rule needed.
  - **PowerShell-scoping finding (real, confirmed):** Claude Code's permission syntax is `<ToolName>(<pattern>)`, matched by literal tool name. This very session exposes both `Bash` and `PowerShell` as distinct tools with separate schemas — direct proof they're different tool identities for permission matching. Every entry in `.claude/settings.json` is `Bash(...)`; none is `PowerShell(...)`. If a launched session's shell tool surfaces as `PowerShell` (as #1988's did per the issue body), the entire allowlist is inert for it — a much bigger problem than the missing `gh` line. Filed as **#1993** (title: "settings.json allowlist is Bash-only; inert for PowerShell tool calls"), parented to **#1096** (EPIC: Application Core), board status set to "AI Batter Up".
  - **Verification:** `.claude/settings.json` still parses cleanly (`node -e "require(...)"`), diff is scoped to exactly the two new array entries, SessionStart hook block byte-for-byte untouched. Honest limitation: this session already had working `gh` access from its very first tool call (before the edit), so I cannot demonstrate within this same session that the new `Bash(gh:*)` entry is what suppressed a permission prompt — that would require a fresh interactive session hitting the allow-list path from a denied state. What I *did* verify: the new entries are syntactically identical in form to the existing working entries (e.g. `Bash(git:*)`), and the file parses and loads without error.
  - No SQL/schema work needed for this build. Working tree clean at finish (`git status --porcelain` empty).
