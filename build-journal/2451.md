# #2451 — Diagnostics and Scripts: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T03:15:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** a1d63a695 (pack + bookend, agent/2451-q1334 → merged to origin/main)

Leaf issue under Feature #1660 ("Feature: Diagnostics and Scripts (Portal)"), itself
part of Epic #1485. Phase 2 of the Portal build order (BUILD_QUEUE_METHOD.md §2.1),
following the #1642 pattern: extract the real contract pack from the current, live
backend — never author before the endpoints exist. Output:
`docs/diagnostics-and-scripts-contract-pack.md`.

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — Recovered `C:\wt\2451-q1334` (declared primary
  working dir was a completely empty, unregistered folder — no `.git`, not in `git
  worktree list`; matches Git #2118). Recreated via `git worktree add` off
  `origin/main` and registered with `creatorPid` = this session's `$CLAUDE_PID` so it
  survives sweeps, per the documented recovery recipe. Now verifying which of the
  old portal-v2 endpoints listed in #2451 (`portal/assessment/status`,
  `portal/diagnostics/latest`, `portal/health-benchmark`, `portal/offers`,
  `portal/scripts`, `portal/scripts/:id/download`) are still real/live post-#1673
  restructure, before writing the pack.
- 2026-09-03T03:15:00Z ✅ DONE — Wrote `docs/diagnostics-and-scripts-contract-pack.md`
  (~335 lines) from the real, live backend: verified all seven module surfaces
  (portal/diagnostics/latest, portal/diagnostics/runs/:runId,
  portal/scripts/:checkKey/download, portal/health-benchmark, the `scan` fragment of
  portal/assessment/status, the MSP-operator diagnostics routes, and admin PS-Scripts
  authoring), cited to file:line against `msp-diagnostics.ts`, `portal-assessment.ts`,
  `portal-offers.ts`, `admin-ps-scripts.ts`, `admin-monitor-checks.ts`, and the Drizzle
  schema in `lib/db/src/schema/msp.ts` / `index.ts`. Live-DB tri-state counts queried
  against local `DATABASE_URL`: 6 diagnostic runs (all `partial`), 853 findings, 0
  `monitor_checks` with `requires_customer_script=true`, 0 `script_packages`, 0
  `script_modules`, 0 `industry_benchmark_reference` rows. Confirmed the old
  portal-v2 `portal/offers` reference belongs to the Sales Offer module, not this one,
  and that `portal/scripts` (no id) never existed as a list route (full-repo grep).
  Real finding: the customer script-download path (§1c) is fully wired end-to-end
  but structurally unreachable today — `admin-ps-scripts.ts` (script authoring) and
  `admin-monitor-checks.ts` (the `requiresCustomerScript` flag) are both real, but no
  route anywhere writes `monitor_checks.script_package_id`, so the two can never be
  linked. Filed as **#2506**, parented as a sibling sub-issue of Feature #1660 (not
  the Epic), labeled `bug`, milestone v1.1, `iteration:1` + `area:msp-portal`,
  assigned to Shane, board status set to "AI Batter Up". Rebased onto `origin/main`
  (which had moved to `846be0cdb` mid-session) and pushed directly to `main` per this
  repo's git convention: `a1d63a695`. `verify-branch-merged.mjs` confirms merged
  (exit 0). `git status --porcelain` clean. Read-only build: only the pack + this
  bookend changed. **Also recovered `C:\wt\2451-q1334`**, which was declared as this
  build's primary working dir but was a completely empty, unregistered folder with no
  `.git` at session start (Git #2118 pattern) — recreated via `git worktree add` off
  `origin/main` and registered with this session's own `$CLAUDE_PID` as owner so it
  survives sweeps.
