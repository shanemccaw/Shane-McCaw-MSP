# #3935 — createAuditLog swallows insert failures

- **Status:** ✅ DONE 2026-09-13T06:40:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 2e711352e (on main, verified merged as ce2d062a)

## Log
- 2026-09-13T06:10:00Z ⏳ IN FLIGHT — `createAuditLog` (`artifacts/api-server/src/lib/audit.ts`) returns `Promise<void>` and swallows insert failures. Changing it to return a real `{ ok, error }` result plus adding `createAuditLogOrThrow`, then wiring the compliance-sensitive retention lifecycle (`lib/retention/lifecycle.ts`) call sites to use the throwing variant so an audit-write failure on a delete/restore/purge/acceleration action surfaces as a real error rather than a silent 200.
- 2026-09-13T06:40:00Z ✅ DONE — `createAuditLog` now returns `{ ok, error }` (non-breaking: dozens of fire-and-forget call sites that discard the return value are unaffected). Added `createAuditLogOrThrow`. Wired `lib/retention/lifecycle.ts`'s six `createAuditLog` calls (delete-refusal audit, soft delete, restore, purge, acceleration request, acceleration decision) onto the throwing variant — a `RetentionError`-unaware throw already falls through to a real 500 at every route's existing generic catch block (`msp-poams.ts`, `msp-retention-queue.ts`, `portal-poams.ts` all already have that fallback). Left the other ~65 call sites (marketing purchases, admin ops, billing, team management, etc.) fire-and-forget — genuinely non-compliance-critical, matching the issue's "don't touch every call site blindly" guidance. Verified: `tsc --noEmit` clean on api-server; `msp-retention-queue.test.ts` (11 tests) and the three live-db suites `msp-retention-queue-poam-producer.live-db.test.ts` / `msp-poam-rbd-conversion.live-db.test.ts` / `msp-poams-role-floor.live-db.test.ts` (10 tests) pass against real local Postgres — including the exact FK-violation-prone path #3639/#3841 already fixed. Also spot-checked `dlp-role-group-provisioning.test.ts`, `graph-consent-revoke.test.ts`, `admin-active-directory-user-actions.test.ts` (57 tests) still pass unaffected. Pushed directly to `origin/main`, no PR. `git status --porcelain` clean.
