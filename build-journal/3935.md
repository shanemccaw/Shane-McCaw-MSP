# #3935 — createAuditLog swallows insert failures

- **Status:** ⏳ IN FLIGHT 2026-09-13T06:10:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-13T06:10:00Z ⏳ IN FLIGHT — `createAuditLog` (`artifacts/api-server/src/lib/audit.ts`) returns `Promise<void>` and swallows insert failures. Changing it to return a real `{ ok, error }` result plus adding `createAuditLogOrThrow`, then wiring the compliance-sensitive retention lifecycle (`lib/retention/lifecycle.ts`) call sites to use the throwing variant so an audit-write failure on a delete/restore/purge/acceleration action surfaces as a real error rather than a silent 200.
