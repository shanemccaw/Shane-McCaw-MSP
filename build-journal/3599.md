# #3599 — Blocker ghost-card resolution + filter-switching navigation (Build Queue Panel)

- **Status:** ✅ DONE 2026-09-10T23:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 455e6f753 (fix), a6cba893f (IN FLIGHT bookend)

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — Pulled #3599 body + all 3 comments (original body, self-blocked/WAITING scope addition, and the "WAITING items belong in normal Queue view" decision comment). Read `Controls/BuildQueuePanel.xaml.cs`: `BuildBlockerGhostCard` resolves a blocker only against `_currentGraphNodes` (whatever the active filter rendered), `SelectNode`/the ghost-card click handler never switches filters, and `ApplyFilter`'s narrow filters don't include self-blocked "⏳ WAITING" (`canceled` + `ExitCode==0`) rows. Found an existing `RevealQueueItem(int id)` method (built for Universal Search / BT_REVEAL_QUEUE_ITEM) that already does exactly the "switch filter to wherever the target lives, then select/highlight it" navigation this issue asks for — reusing it for the ghost-card click instead of building new navigation logic. About to: extract the per-item `QueueGraphNode` builder into `BuildItemNode` so it can run against the full `_lastItems` (not just the active filter's render), add `FindLiveNodeForBlocker` (fast path: `_currentGraphNodes`; fallback: `_lastItems`), route the ghost-card click through `RevealQueueItem`, add `IsWaitingSelfBlocked` and fold WAITING into the `Queued`/`RunningAndQueued` filter buckets (excluding it from `Canceled`) per the issue's final decision comment, and fix `RevealQueueItem`'s own targetFilter switch (Verifying was mapping to "Running", which Git #3340 hardened to exclude Verifying — same root-cause class as this issue, fixed in the same pass) plus map WAITING to "Queued" there too.
- 2026-09-10T23:05:00Z ✅ DONE — Shipped in `455e6f753` (rebased onto `origin/main` and pushed directly, per `git conventions`). Changes to `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`:
  1. `FindLiveNodeForBlocker(int blockerNumber)` — new resolver: fast path checks `_currentGraphNodes` (preserves the real rendered `CardElement`), falls through to a lookup against the full unfiltered `_lastItems` (matched the same way `Key` is computed elsewhere: `GithubNumber ?? Id`) and synthesizes an equivalent node via the newly-extracted `BuildItemNode(QueueItem)` helper. `BuildBlockerGhostCard` now calls this instead of only checking `_currentGraphNodes` — a blocker sitting under a different active filter is now correctly detected as live/clickable rather than misreported as "not itself a build in this queue."
  2. The ghost card's click handler now calls the existing `RevealQueueItem(liveNode.Item.Id)` instead of `SelectNode(liveNode)` directly — `RevealQueueItem` already switches the active status filter to whichever one actually shows the target item before selecting/highlighting it (it was built for Universal Search / `BT_REVEAL_QUEUE_ITEM`, reused here rather than duplicated), so a click on a blocker under a different filter now actually navigates instead of being a no-op. Restart pseudo-nodes (no real `Item`) keep the exact same no-op they had before (`RevealQueueItem`/`SelectNode` both require a real `Item`).
  3. `IsWaitingSelfBlocked(QueueItem)` — single source of truth for the existing "⏳ WAITING" vs "🚫 CANCELED" distinction (`status=="canceled" && ExitCode==0`, matching `GhostStatusLabel`/the graph dot/the card pill already in the file). Per the issue's final decision comment: folded into `ApplyFilter`'s `Queued` and `RunningAndQueued` buckets, and excluded from `Canceled` — a self-blocked WAITING item is now visible under the normal working filters, not only "All"/"Canceled." Its own blocked-by ghost card was already covered by fix #1/#2 above regardless of which filter is active.
  4. `RevealQueueItem`'s own `targetFilter` switch updated to match: WAITING routes to "Queued" (not "Canceled"). Also fixed a real pre-existing bug in the same switch, same root-cause class as this issue: a Verifying item was mapping to the "Running" target filter, but Git #3340 hardened "Running" to mean exactly `status=="running"` — so revealing a Verifying item would switch to a filter that still doesn't show it. Now routes to the real standalone "Verifying" filter.
  5. Extracted `BuildItemNode(QueueItem)` out of `RenderQueue`'s per-item loop (used by both the loop and the new `FindLiveNodeForBlocker`) — pure refactor, no behavior change at that call site.

  **Verification:** `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 6 pre-existing unrelated warnings (`FocusModeBar` unused events), both before and after rebasing onto `origin/main`. This is `desktop/BuildConsole` (the WPF app), explicitly out of scope for test manifests per CLAUDE.md — a clean build is the correct verification here, not a manifest. Did not have a live BuildConsole instance with a real multi-filter blocked-item scenario to click-test interactively in this session; the fix reuses `RevealQueueItem`, an already-shipped and exercised navigation path (Universal Search / `BT_REVEAL_QUEUE_ITEM`), for the new call site, and the WAITING/Verifying filter-bucket changes are direct, narrow boolean additions to existing, well-established filter predicates — reviewed by hand against the exact scenarios described in all three issue comments. No fixture/hardcoded data. `git status --porcelain` clean; branch confirmed merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE. No new findings filed — no genuine out-of-scope bug discovered beyond the in-scope Verifying-routing fix folded into this same commit (same root-cause class as the issue itself, not a separate concern).
