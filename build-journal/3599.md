# #3599 — Blocker ghost-card resolution + filter-switching navigation (Build Queue Panel)

- **Status:** ⏳ IN FLIGHT 2026-09-10T22:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — Pulled #3599 body + all 3 comments (original body, self-blocked/WAITING scope addition, and the "WAITING items belong in normal Queue view" decision comment). Read `Controls/BuildQueuePanel.xaml.cs`: `BuildBlockerGhostCard` resolves a blocker only against `_currentGraphNodes` (whatever the active filter rendered), `SelectNode`/the ghost-card click handler never switches filters, and `ApplyFilter`'s narrow filters don't include self-blocked "⏳ WAITING" (`canceled` + `ExitCode==0`) rows. Found an existing `RevealQueueItem(int id)` method (built for Universal Search / BT_REVEAL_QUEUE_ITEM) that already does exactly the "switch filter to wherever the target lives, then select/highlight it" navigation this issue asks for — reusing it for the ghost-card click instead of building new navigation logic. About to: extract the per-item `QueueGraphNode` builder into `BuildItemNode` so it can run against the full `_lastItems` (not just the active filter's render), add `FindLiveNodeForBlocker` (fast path: `_currentGraphNodes`; fallback: `_lastItems`), route the ghost-card click through `RevealQueueItem`, add `IsWaitingSelfBlocked` and fold WAITING into the `Queued`/`RunningAndQueued` filter buckets (excluding it from `Canceled`) per the issue's final decision comment, and fix `RevealQueueItem`'s own targetFilter switch (Verifying was mapping to "Running", which Git #3340 hardened to exclude Verifying — same root-cause class as this issue, fixed in the same pass) plus map WAITING to "Queued" there too.
