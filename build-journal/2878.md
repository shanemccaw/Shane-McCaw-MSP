# #2878 — Fix msp-suspended-banner.test.ts stale @workspace/db mock

- **Status:** ✅ DONE 2026-09-05T00:15:00Z
- **Scope:** platform

## Log
- 2026-09-05T00:02:31Z — Investigated. `msp-suspended-banner.test.ts` mocks `@workspace/db`
  but never added `RETENTION_CLOCK_RUNNING_TENANT_STATUSES`, which #1947/#2765's retention
  work added as a real export consumed by `artifacts/api-server/src/lib/retention/policy.ts`
  and `subscription-state.ts`. Sibling test files (`msp-portal.test.ts`,
  `msp-customers-bulk.test.ts`) already mock it as `["active", "onboarding"]`. Added the same
  to `msp-suspended-banner.test.ts`'s mock.
- 2026-09-05T00:15:00Z — ✅ DONE. Added `RETENTION_CLOCK_RUNNING_TENANT_STATUSES: ["active",
  "onboarding"]` to the `@workspace/db` mock. Also bumped the shared `beforeEach`'s hook
  timeout from vitest's default 20s to 60s (`beforeEach(..., 60_000)`) — the dynamic
  `import("./msp-portal.ts")` inside `buildApp()` is transform-heavy and was intermittently
  blowing the 20s hook timeout under this machine's current concurrent-build load, unrelated
  to the mock fix itself but blocking a clean green run. All 10 tests pass:
  `Test Files 1 passed (1) / Tests 10 passed (10)`.
  Commit: c37397ece "Fix stale @workspace/db mock in msp-suspended-banner.test.ts", merged to
  main via rebase+push at 81e1aae88. `verify-branch-merged.mjs` confirms OK.
