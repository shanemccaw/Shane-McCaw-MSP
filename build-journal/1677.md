# #1677 — Billing: plan state from live subscription endpoints — rebuild in artifacts/portal

- **Status:** 🛑 BLOCKED 2026-09-03T23:13:32Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** none — no code changed

## Log
- 2026-09-03T23:13:32Z 🛑 BLOCKED — Re-dispatched off a status report claiming the real Claude
  Design export had landed. Checked `Design/portal/` directly: it contains only
  `design_handoff_ui_shell/Shell.dc.html` (the shell scaffolding export) — no `Billing.dc.html`
  or any other Billing-specific export. Per CLAUDE.md's portal rule, a page has no design until
  its `.dc.html` exists in `Design/portal/`, and `portal-v2` (the archived page referenced in
  #1677's own body) is explicitly not a fallback target. This issue was already held once for
  this exact reason (see #1677's own comment thread, 2026-09-03T19:11Z) — nothing has changed
  since that hold.
  The real blocker already exists as its own issue: **#1627 "Billing: Claude Design export into
  Design/portal/"** (open, labeled `Shane To-Do` — "Shane exports this himself"). #1677 was
  already `blocked_by` #1627 (and by #1673, closed/done) before this session started — the
  dependency edge already reflected reality, so no edge changes were needed. Set labels back to
  `blocked` (removed the `in-flight` this dispatch had added). No code touched; working tree
  clean.
