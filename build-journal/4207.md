# #4207 — poams bundles at Premier only

- **Status:** ✅ DONE 2026-09-15T04:22:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 93ea34f87

## Log
- 2026-09-15T04:18:31Z ⏳ IN FLIGHT — writing `lib/db/migrations/manual/2026-09-15-poams-premier-tier-4207.sql` (poams → Premier `includedFeatures` only, same additive `||` merge pattern as #1168) and updating `lib/db/src/rbac/access.test.ts` for the new Premier-only entitlement.
- 2026-09-15T04:22:00Z ✅ DONE — Wrote and ran `lib/db/migrations/manual/2026-09-15-poams-premier-tier-4207.sql` against local `DATABASE_URL` (`UPDATE 4`, the four Premier `monitoring_tier` rows; Foundation/Growth untouched). Verified: `SELECT count(*) FROM services WHERE type_attributes->'includedFeatures' ? 'poams'` → 4, all `tier='premier'`. Updated `lib/db/src/rbac/access.test.ts` (PREMIER_FEATURES includes poams; replaced the #3104 "left unassigned" pin with a Premier-only entitlement test; migration-vocabulary test now checks both migration files) — `pnpm exec vitest run src/rbac/access.test.ts` → 24 passed. Migration filename recorded on #1630. Commit `93ea34f87` pushed and confirmed ancestor of origin/main via `verify-branch-merged.mjs`. `git status --porcelain` clean.
