# #4551 — config-pack-dry-run.test.ts #4513 P1 case, "no MT app creds" variant

- **Status:** ✅ DONE 2026-09-17T22:30:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Resolved by:** #4546's commit 098c8ed60 (already on `main`). No new commit
  needed for #4551 itself — this session's own commits are documentation only
  (build-journal/4551-plan.md, this bookend).

## Changes Made
None to product code. #4551's failing case is a side effect of the exact same
defect #4546 fixed (async `vi.mock("./graph.ts", ...)` factory losing the
`graphFetchForTenant` binding race for `license-gate.ts`'s SKU read). 098c8ed60
(merged to `main` before this session started) already fixes it.

## Verification Results
Independently re-ran the exact failing case from #4551's own environment
description (no MT app creds) on current `main`, rather than trusting #4546's
bookend claim alone:

```
cd artifacts/api-server
MT_APP_CLIENT_ID= MT_APP_CLIENT_SECRET= node --env-file=../../.env.local \
  node_modules/vitest/vitest.mjs run src/lib/config-pack-dry-run.test.ts
```

Result: **7/7 pass**. The #4513 P1-tenant test's own logged output shows
`"skuReadError": null` (mock genuinely intercepted the SKU read) and the
refusal reached is `security_defaults_replacement_not_enforcing` — the
`quickstart-v1.disable-security-defaults` / `create-ca-baseline-policy` gate,
matching what the test asserts — not `license_required`. #4551 is confirmed
resolved as a side effect of #4546, citing its commit 098c8ed60.

`git status --porcelain` clean; `verify-branch-merged.mjs` confirms
`agent/4551-q3194` is an ancestor of `origin/main` (0f72ed309).

## Log
- 2026-09-17T22:20:00Z ⏳ IN FLIGHT — read #4551 body/comments and #4546's final
  DONE bookend. #4546's bookend already states explicitly it re-ran with MT app
  creds forced empty (#4551's exact env) and got 7/7 pass — same root cause,
  already resolved. Independently re-verifying rather than trusting the bookend
  alone. See build-journal/4551-plan.md.
- 2026-09-17T22:30:00Z ✅ DONE — independently re-ran the exact case, 7/7 pass,
  skuReadError null, correct refusal code reached. Confirmed #4551 is resolved
  by #4546's commit 098c8ed60. No new code change required.
