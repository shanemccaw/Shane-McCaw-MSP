# #2676 — MSP console: no operator routes exist for Team Management and Invitations

- **Status:** ✅ DONE 2026-09-03T20:22:49Z
- **Scope:** platform

## Log

- 2026-09-03T20:17:12Z — Started. Audited the landed Portal-side contract pack
  (`docs/team-management-and-invitations-contract-pack.md`, #2447/#1656) and the real
  `artifacts/api-server/src/routes/portal-team.ts` (10 routes) it documents. Confirmed via
  `gh issue view` there is no live MSP-side route for Team Management/Invitations
  (`grep requireRole("MSPOperator"...)` across `artifacts/api-server/src/routes/` — zero hits
  for team/invite/roster). Building a new `msp-team.ts` router mirroring the portal contract's
  10 routes but scoped for MSP operator/admin callers across their book of customers
  (`assertCustomerAccess` ownership + staff-scope gate instead of the portal's
  `canManageTeam` capability flag, which is customer-tier only), following the
  `msp-active-directory.ts` / `msp-documents.ts` pattern (`requireRole("MSPOperator")` +
  `resolveMspIdStrict` + `assertCustomerAccess`).
- 2026-09-03T20:22:49Z — ✅ DONE — Added `artifacts/api-server/src/routes/msp-team.ts` (10
  routes, mirroring `portal-team.ts` 1:1 in behavior/data-shape) and mounted it in
  `artifacts/api-server/src/routes/index.ts`. No schema/migration needed — every route reads
  and writes tables `portal-team.ts` already uses (`usersTable`, `mfaEnrollmentsTable`,
  `webauthnCredentialsTable`, `userSessionsTable`, `passwordResetTokensTable`,
  `mfaChallengesTable`, `webauthnChallengesTable`, `mfaBypassCodesTable`). Auth:
  `requireRole("MSPOperator")` + `assertCustomerAccess` (MSP-ownership + per-staff-scope IDOR
  check), the same pattern as `msp-active-directory.ts`/`msp-customer-timeline.ts` — not the
  Portal file's own `denyIfCannotManageTeam`, since its second gate (`canManageTeam`) only ever
  applies to the customer tier and MSP staff/PlatformAdmin bypass it by role, which
  `requireRole` already establishes. `tsc --noEmit` shows only the same pre-existing baseline
  noise (`bcryptjs`/`drizzle-orm` type-decl resolution, `lib/db/dist` not rebuilt) present
  identically on the already-live `portal-team.ts` — confirmed by diffing the two files'
  error output; no new error class introduced. No frontend wiring — `artifacts/msp-console`
  does not exist yet (#2567), so this is backend-only, matching the issue's own scope. No
  findings filed — clean build against an established, already-proven pattern; no gap
  discovered beyond this issue's own scope.
  Commit: `e8380d1f7` — pushed to `main`, verified merged
  (`node scripts/dev-server/verify-branch-merged.mjs` — OK). Working tree clean
  (`git status --porcelain` empty).
