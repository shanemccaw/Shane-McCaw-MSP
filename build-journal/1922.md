# #1922 — Fix `services.category` empty on id=131

- **Status:** ✅ DONE 2026-09-04T07:20:00Z
- **Scope:** platform

## Log
- 2026-09-04T07:12:09Z — Started. Confirmed via direct psql query against local `DATABASE_URL` that `services` id=131 (`M365 Launch Control — Plus Add-On`) has both `category` and `service_type` unset. Confirmed this is NOT part of #1591's Group B/C (project ids 158-167, config_pack ids 169-175/195) — #1591 already fixed those and filed this row as a separate finding. Compared id=131 against sibling add-on rows 176-179 (`Change Control — *`): both share `billing_type='recurring_monthly'`, both are named "... Add-On(s)" layered on top of a subscription tier, both are recurring add-ons not projects/config-packs/assessments. Conclusion: id=131 belongs to the same real kind as 176-179 → `category='Add-ons'`, `service_type='recurring_addon'`.
- 2026-09-04T07:20:00Z — ✅ DONE. Correction found while running the fix: the two columns were actually SQL `NULL`, not empty string (the issue title/#1591's finding described them as `''`) — updated the migration's `WHERE` clause to match reality (`IS NULL OR = ''`) before running. Migration `lib/db/migrations/manual/2026-09-04-services-category-backfill-131-1922.sql` run myself against local `DATABASE_URL`: `UPDATE 1`, self-marked in `simulator_migration_runs`. Verified: id=131 now `category='Add-ons'`, `service_type='recurring_addon'`, matching 176-179's pattern. Appended migration filename to #1630. Commits (real, post-push, verified ancestors of `origin/main` via `verify-branch-merged.mjs`): `7cec78165` (bookend IN FLIGHT), `ce6914f2f` (migration + fix). No code change needed — data-only fix, same as #1591's own resolution. `git status --porcelain` clean.
