# #1435 — Cherry-pick Copilot-designed critters (add, don't replace) with matching stroll/trigger behavior

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 0ce6e5e7de104aa9bf685c3b870d43a5bff9250d

## Log
- 2026-08-27 ⏳ IN FLIGHT — cherry-picking `CritterFactory.cs`, `CritterModels.cs`, `CritterRegistry.cs`,
  `CritterSpawner.cs`, `FocusCharacterLayer.Critters.cs`, `README-CRITTERS.md` from
  `critters/add-more-critters` (files-only, not the branch merge — that branch is stale). Rewriting
  `CritterSpawner`'s motion to match `FocusCharacterLayer.Stroll()`'s full-width traversal instead of the
  as-authored float-in-place, and wiring the new critters into the ambient loop + issues-closed trigger in
  addition to the as-authored build-finished hook. Existing emoji `Companions` pool left untouched.
- 2026-08-27 ✅ DONE — cherry-picked `CritterFactory.cs` (vector art, unmodified), `CritterModels.cs`,
  `CritterRegistry.cs`, `FocusCharacterLayer.Critters.cs` (now a placeholder — the branch's own tip commit
  merged its init logic straight into `FocusCharacterLayer.xaml.cs`), and `README-CRITTERS.md` verbatim.
  Rewrote `CritterSpawner.cs` from scratch: dropped the as-authored fixed-position pop+float-in-place
  entrance/TTL-timer removal, replaced with the same full-width left-to-right/right-to-left traversal,
  7.5-11.5s duration, SineEase, bob+waddle, and fade-in that `FocusCharacterLayer.Stroll()` uses, so the two
  critter systems move identically. Added `AmbientStroll()` and `CelebrateIssuesClosed(count)` alongside the
  existing `SpawnForEvent(success)`, and wired all three into `FocusCharacterLayer.xaml.cs`: a
  `_critterSpawner` field created in `Start()`/cleared in `Stop()`, a 50% independent roll inside
  `AmbientTick()` (interleaved with, not replacing, the existing `Stroll()`/`PeekFromEdge()` roll),
  `CelebrateBuildFinished()`, and `CelebrateIssuesClosed()`. All calls wrapped in try/catch logging to the
  existing `focus-mode` ActivityLog channel, matching the pattern already used in that file. Existing emoji
  `Companions` array/Stroll/PeekFromEdge/Banner/ConfettiBurst/HappyHop/FloatUp logic is untouched — both
  pools are fully additive and can appear on stage simultaneously.
  **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0
  errors (46 pre-existing warnings unrelated to this change, all in files this issue didn't touch).
  No automated UI test exists for Focus Mode's ambient/celebration layer (it's non-interactive,
  `IsHitTestVisible=False`, and BuildConsole is explicitly out of scope for the `test-manifests/` UI-test
  mandate per CLAUDE.md) — compile-clean is the real, honest verification bar available here, consistent
  with the issue's own "cannot be compiled/verified in the chat sandbox" framing (which no longer applies —
  `dotnet` is directly available in this session and was used).
