# #3642 — Weekly rescans fire one full tenant scan per Free user

- **Status:** ✅ DONE 2026-09-12T21:06:32Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** e56f11f1f (rebased to 07e7e8162 on main)

## Log
- 2026-09-12T21:01:55Z ⏳ IN FLIGHT — collapsing weekly-rescan-populations.ts queries to one row per (tenant, packageKey) instead of one row per Free user, per issue #3642.
- 2026-09-12T21:06:32Z ✅ DONE — rewrote `WEEKLY_ASSESSMENT_RESCAN_QUERY` and
  `WEEKLY_RETARGETING_RESCAN_QUERY` in `artifacts/api-server/src/lib/weekly-rescan-populations.ts`
  to `SELECT DISTINCT ON (t.tenant_id, packageKey)`, ordered by `(tenant_id, packageKey, u.id)`
  so the lowest `users.id` is the deterministic representative `clientId`. Preserved the pre-fix
  per-user query text in `LEGACY_WEEKLY_RESCAN_QUERIES` so `seed-system-workflows.ts` converges
  any already-seeded trigger (wf_triggers id 40/42 locally) onto the new query on next startup.
  Verified against local Postgres with the issue's own evidence query: tenant
  `c4c814d4-3afe-441e-9145-62461d0a4fd3` (3 eligible Free users, confirmed live) now yields
  exactly 1 row from the new Sunday query instead of 3; tenant `0a361ab2-9e85-4bbf-8b75-c1ebf042dfba`
  yields exactly 1 row from the new Monday query. `tsc --noEmit` clean on the changed file.
  Rebased onto current `origin/main` (0ebe1b705) and pushed directly (`push origin
  agent/3642-q2468:main`, per this build's isolated-worktree convention) — HEAD 07e7e8162,
  confirmed merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
