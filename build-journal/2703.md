# #2703 — M365 Launch Control rollback is inert - 0/102 templates marked reversible

- **Status:** ✅ DONE 2026-09-03T23:21:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** a813455b7 (rebased/pushed to main; original worktree commit was 8d60ad63f)

## Log
- 2026-09-03T23:08:00Z ⏳ IN FLIGHT — investigating baseline_action_templates reversibility drift; will re-promote reversible=true + reverse_template_id against the real live template_ids and check workflow-executor.ts:883-905's Teams special case.
- 2026-09-03T23:21:00Z ✅ DONE — Confirmed 0/102 rows reversible via local DATABASE_URL, matching the issue. Re-promoted 4 real reciprocal pairs (8 rows) by live endpoint/required_variables match: action.disable-user-signin<->action.enable-user-signin, action.assign-single-license<->microrem.remove-unused-license, action.group-based-license-assign<->action.group-based-license-remove, action.add-group-member<->microrem.remove-stale-group-member. Migration `lib/db/migrations/manual/2026-09-03-launch-control-rollback-repromote-2703.sql` self-run against local DATABASE_URL (8 UPDATE, self-marking INSERT) — verified 8/102 now reversible=true. Added a workflow-executor.ts special case (memberId/userId alias) for the group-member pair since its captured variable name differs from its reverse's required variable name — traced through interp()/resolveBaselineTemplateRequest() to confirm the mismatch would otherwise 400 on replay; full live tsx smoke test was blocked by the known pre-existing worktree lib/db junction resolution gap (unrelated to this change, not chased per the pnpm-install-is-not-a-remedy rule). teams.add_member/teams.remove_member NOT re-promoted — no live Teams conversation-member template exists under any name in the 102-row catalog; filed as #2772 (sibling sub-issue of #2494, labeled bug, board status AI Batter Up), Teams special case in workflow-executor.ts left in place with an explanatory comment. Branch rebased onto origin/main and pushed directly (`git push origin HEAD:main`) since worktree isolation had put commits on `agent/2703-q1460`; verify-branch-merged.mjs confirms merged. `git status --porcelain` clean.
