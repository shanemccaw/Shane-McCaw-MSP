# #3083 — check-drift schema-hash section permanently red

- **Status:** ✅ DONE 2026-09-07T00:00:00Z
- **Scope:** platform

## Log
- 2026-09-07T00:00:00Z — Session started. Pulled issue body + comment. Plan: teach
  check-drift.ts to compare schema/index.ts against the union of `_journal.json`
  generated migrations AND `lib/db/migrations/manual/` files, so schema changes
  carried by a real manual migration aren't reported as false drift. Investigating
  current check-drift.ts implementation and the actual schema/manual-migration gap
  before writing code.
- 2026-09-07T00:00:00Z — Chose option 1 (teach it the manual path), per the
  dispatch's own preference and the issue's own reasoning: sections 2-5 of
  check-drift (including #2930's destructive-migration gate) are genuinely
  useful and were sitting buried under a permanently-red banner.
- 2026-09-07T00:00:00Z — Investigated the real gap via git history rather than
  guessing: `schema-hash.txt` was last stamped at `dca17a108` (#2848,
  2026-09-04). Every commit since that touched `schema/index.ts` —
  `d0ad70164` (#2983), `e2ed8d7a2` (#1923), `79e923937` (#2524) — was
  individually confirmed (via `git show --name-only`) to carry its own
  `lib/db/migrations/manual/*.sql` file in the same commit. The gap is
  entirely legitimate manual-migration-workflow drift, not unaccounted schema
  editing.
- 2026-09-07T00:00:00Z — Implemented reconciliation in
  `scripts/src/check-migration-drift.ts` (`tryReconcileSchemaDrift`): on a
  hash mismatch, walks git history from the last commit that touched
  `schema-hash.txt` to HEAD, and for every commit that touched
  `schema/index.ts` in that range, checks whether the SAME commit also
  touched `lib/db/migrations/manual/` or `lib/db/drizzle/`. If all did, the
  delta is reported informationally as reconciled (exit 0 for that check),
  listing each commit + subject. If any commit lacks a migration, it's named
  explicitly as real, unaccounted drift (hard fail, unchanged from before).
  Also handles uncommitted local schema edits (requires a matching
  uncommitted change under manual/) and fails safe to the original strict
  behavior on any git error (not a repo, shallow-clone anchor missing, etc.)
  — no case can be talked into a false pass. Did NOT stamp/regenerate the
  hash — the real reconciliation code above is what recovers the signal,
  not a stamp that would falsely assert the schema matches the last
  `generate`.
- 2026-09-07T00:00:00Z — Verified: `pnpm --filter @workspace/scripts run
  check-drift` now exits 0, with Check 1 printing the three reconciled
  commits and sections 2-5 fully visible (1 held destructive migration, 5
  auto-approved, journal/orphan checks clean). Confirmed the negative case
  too — an uncommitted schema edit with no matching migration still fails
  the check exactly as before. `npx tsc --noEmit -p scripts/tsconfig.json`
  clean.
- **Status:** ✅ DONE 2026-09-07T00:00:00Z — commit `08748b015` pushed
  directly to `origin/main` (rebased onto `origin/main` first). Working tree
  clean; `verify-branch-merged.mjs` confirms merged. No findings filed — the
  investigated gap turned out to be entirely legitimate manual-migration
  drift, not a bug.
