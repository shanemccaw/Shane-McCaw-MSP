# #4197 — Verify consent callbacks with Microsoft before recording a grant

- **Status:** ✅ DONE 2026-09-15T03:58:48Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 04d75cd9f (fix + tests)

## Log
- 2026-09-15T03:36:58Z ⏳ IN FLIGHT — investigating /consent/callback, /admin/write-consent/callback, /admin/sharepoint-consent/callback; adding a pre-grant client-credentials token check against the claimed tenant
- 2026-09-15T03:58:48Z ✅ DONE — New `artifacts/api-server/src/lib/consent-verification.ts`: before any grant, the callback gets an app-only client-credentials token for the claimed tenant, using the app registration being consented to. It requires a token, a matching `tid` and non-empty `roles`. Retries only replication-lag failures (1.5/3/5s); AADSTS90002 is definitive; platform faults return 503 with the link unburned. Wired into `/consent/callback` (read/graph, before token burn, session flip, customer creation, grant and scans; the `sharepoint` stamp is now gated on its own SharePoint Online token), `/admin/write-consent/callback` (write app), `/admin/sharepoint-consent/callback`, and the MSP mailbox connector callback in msp-settings.ts (same pattern). Verification: vitest consent-verification + consent + msp-settings-portal-links = 89/89 pass; api-server tsc clean. LIVE: synthetic `zz-test-4197` tenants row with a random GUID. Pre-fix :8080: read, write and SharePoint callbacks all 302 success, `graph`/`writeBack`/`sharepoint` stamped granted, tokens burned, 1 diagnostic run + 28 findings + 28 item-detail + 28 monitor-profile rows. This confirms the write and SharePoint callbacks share the exposure. Fixed build :8099: all three plus the create path returned 400 AADSTS90002 `tenant_not_found`, consent stayed `{}`, tokens unburned, no tenants row. Positive control against the real testbed tenant: read/graph ok (32 roles), write/graph ok (17), read/sharepoint ok. All synthetic rows deleted; a re-scan found 0. Findings filed under #1650: #4226 (a real consented GUID under the direct MSP can still be attached by a checkout buyer; residual not closed by this check), #4227 (mailbox connector tenant not bound to the MSP). No migrations. No test manifest: api-server only.
