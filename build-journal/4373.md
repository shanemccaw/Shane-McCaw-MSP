# #4373 — Generalize resolveProspectRole + promote-on-consent swap per product (monitoring/retainer/pack)

- **Status:** ✅ DONE 2026-09-16T16:06:30Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** b310c21ef (work), d76461b42 (IN FLIGHT bookend)
- **Migration:** none (schema already widened by #4372)
- **Findings filed:** #4392 (Ask Shane lane, sub-issue of #4370) — whether a paid Monitoring/Pack buyer's terminal role is `*Consented` or `Customer`; the two checkout orders currently diverge and nothing in #4370 decides it.

## Log
- 2026-09-16T16:06:11Z ⏳ IN FLIGHT — reading direct-tenant-provisioning.ts in full; generalizing resolveProspectRole and ensureClientMspUser's atomic promote-on-consent swap to branch per product category, preserving atomicity/idempotency/cross-MSP backstop; rewording the stale "only tenant-less rung" comments flagged by #4372.
- 2026-09-16T16:06:30Z ✅ DONE — `resolveProspectRole` returns the product's own `*Pending` rung for a tenant-less arrival (monitoring → MonitoringPending, config_pack → PackPending, retainer/unknown → RetainerPending, the #3950 fallback); tenant-present behaviour unchanged (retainer → RetainerConsented, else Customer). `ensureClientMspUser` swaps on `PENDING_TO_CONSENTED_ROLE` keyed on the row's existing rung, in the same UPDATE that links tenant_id/mspId; atomicity, idempotency and the cross-MSP backstop untouched; the tenant-less early return and provisionProspectAccount's no-tenant error gate recognise all three `*Pending` rungs. `ProspectRole` union carries the 4 new rungs; helpers `isPendingProspectRole` / `consentedRoleForPending` / `pendingRoleForCategory` exported for #4374/#4375. Stale single-rung comments reworded in direct-tenant-provisioning.ts, purchase-account-flow.test.ts (x2) and monitoring-onboarding-scan.test.ts. Verified: api-server tsc 0 errors; direct-tenant-provisioning.test.ts 21/21 (3-rung swap, swap keyed on existing rung not desiredRole, *Consented untouched, tenant-less *Pending early return with no error log, Customer tenant-less still errors, cross-MSP backstop still refuses for a *Pending row, already-linked idempotent, helper coverage); purchase-account-flow.test.ts 43/43 live Postgres incl. new proof that a pre-existing tenant-less MonitoringPending/PackPending/RetainerPending row consents into its *Consented rung with the tenant linked against the real users_role_scope_check and is idempotent on a second pass; free-scan-prospect, free-scan-return-link, consent.test, monitoring-onboarding-scan 106/106. Push rebased once onto a moved main. Working tree clean.
