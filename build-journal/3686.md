# #3686 — msp-settings-contract-pack.md's auth-model section is stale

- **Status:** ✅ DONE 2026-09-11T21:32:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** c15412a21 (fix), 8c64fe1ab (IN FLIGHT bookend) — both merged into `main` at 196ffe68

## Log
- 2026-09-11T21:05:00Z ⏳ IN FLIGHT — re-verifying `docs/msp-console/msp-settings-contract-pack.md`'s
  auth-model framing (§0), the `mspRole` enum row (§1e), the invite-role note (§1k), and the role
  hierarchy row (§2) against current `main` (`requireAuth.ts`, `lib/db/src/rbac/legacy-ladder.ts`,
  `lib/db/src/schema/index.ts`) to replace the stale `requireRole()`/`ROLE_ORDER`/7-value-hierarchy
  claims with the real `requireCapability("ladder.*")` / 6-value hierarchy (Git #2460, #3590).
- 2026-09-11T21:32:00Z ✅ DONE — rewrote the four stale sections (§0, §1a, §1e, §1k, §2) in
  `docs/msp-console/msp-settings-contract-pack.md` to cite `requireCapability("ladder.*")`
  (`requireAuth.ts:271-307`) instead of the retired `requireRole()`/`ROLE_ORDER`
  (`requireAuth.ts:84-118`), and the real 6-value `LEGACY_ROLE_ORDER`
  (`lib/db/src/rbac/legacy-ladder.ts:46-53`) instead of the old 7-value hierarchy — `CustomerUser`
  renamed `Customer` and `Assessment` folded into `Free` per Git #3590. Also fixed the
  `GET /connector/mailbox/callback` "no requireRole" phrasing (that function no longer exists at
  all) and added a dated "Updated" note at the top per the pack's own convention. Documentation-only
  — no route behavior, wire shape, or endpoint changed; verified by re-reading the actual current
  code (`requireAuth.ts`, `legacy-ladder.ts`, `schema/index.ts`) line-by-line against every claim
  touched, not by inference. Commit `c15412a21` rebased onto `origin/main` and pushed directly
  (`196ffe68`); `verify-branch-merged.mjs` confirms merged; `git status --porcelain` is clean.
  Mid-build finding filed: **#3727** (sibling sub-issue of Feature #1690) — a separate, still-stale
  in-code comment at `msp-settings.ts:241` names the retired role `Assessment` where the route's
  actual gate (`requireCapability("ladder.free")`) is already correct; not fixed here since it was
  outside this issue's own doc-only scope. Labels synced (`in-flight`→`complete`); issue comment
  posted on #3686.
