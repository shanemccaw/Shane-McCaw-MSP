# #1479 — Lift secondary-account build cap entirely — secondary is now Claude Max 20x

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 8f3c2c412 (first pass — no-op, superseded); bffee480e (rip-out pass, merged to origin/main)

## Log
- 2026-08-28 ⏳ IN FLIGHT — making `AccountCapPolicy.ExceedsSonnetHigh` a permanent no-op (always `false`) now that the secondary account is Claude Max 20x; keeping `HeldStatus`/`SonnetPlusOverflowMilestoneTitle`/helper methods wired for a future one-line flip back. Checking for stuck `held` queue items and updating the `watcher` channel log line at QueueWatcherService.cs ~358.
- 2026-08-28 ✅ DONE (first pass, later REOPENED) — `AccountCapPolicy.ExceedsSonnetHigh(model, effort)` made a permanent no-op (always `false`); helpers/consts left in place. This neuter-not-delete approach was reopened by Shane: the cap was enforced in FOUR places (the first pass touched one), the held-item context menu still hardcoded a Sonnet-only downgrade, and queue #762 stayed stranded at `status='held'`.
- 2026-08-28 ✅ DONE (rip-out pass) — Deleted the cap machinery entirely (commit bffee480e, merged to origin/main; verify-branch-merged.mjs OK). Removed: `Services/AccountCapPolicy.cs` (whole file — `ExceedsSonnetHigh`, `IsOpusModel`, `IsAboveHighEffort`, `SonnetPlusOverflowMilestoneTitle`, `HeldStatus`); `QueueWatcherService` launch-time hold guard + `HoldForSonnetOverflowAsync`; `BuildQueuePanel` hardcoded held-item context menu (the Sonnet-only downgrade that survived the first pass) + `SonnetOverflowBanner` (XAML + `UpdateSonnetOverflowBanner` + `BtnResumeSonnetOverflow_Click`) + held status pill + the now-orphaned `UpdateModelAndEffortHelperAsync` + `HeldStatus` from both Active-filter switches; `BuildQueuePostgresClient` `MarkHeldForOverflowAsync`/`GetHeldOverflowAsync`/`ResumeHeldOverflowAsync` + all `'held'` handling in `ForceClaimAsync`/`CancelAsync`/`MarkLatestRowLimitPausedForIssueAsync`; `MainWindow` `CheckSonnetOverflowReminderAsync`; stale cap comments in `SessionLimitAutoRestartService` + `GitHubApiClient`. Grep of the whole desktop/BuildConsole tree for every cap/held/overflow identifier returns ZERO code survivors (only an unrelated CSS "Overflow lines" note in a design README + build-journal history). `dotnet build BuildConsole.csproj` = 0 errors (46 pre-existing unrelated warnings). Drain: ran the direct-DB `UPDATE ... WHERE status='held' → 'queued'` against local PostgreSQL — instantaneous verify showed 0 held rows and #762 = queued/claude-opus-4-8/high/secondary (account/model/effort preserved, NOT nulled). Cleared the 'Sonnet+ Overflow' milestone off the two open issues carrying it (#1277, #1468; verified milestone=none via the API). **Durability caveat, reported honestly:** a pre-rip-out BuildConsole (PID 41472, `bin\ShanesBuild\`, started 15:49 — Shane's console) is still running and its old watcher re-parks #762 to 'held' within its ~10s tick, so a plain SQL drain can't durably stick while that binary runs. That's why this commit ALSO adds an in-app one-shot startup reclaim (`QueueWatcherService.Start()` → `ReclaimLegacyHeldRowsAsync`, beside the orphan sweep) that flips any leftover 'held' row → 'queued' preserving account/model/effort on the new binary's first launch — so when Shane rebuilds/restarts his console (required anyway to make the rip-out live, and to land #1277), #762 self-drains and launches at Opus 4.8/High on secondary. Not a manual migration, not a separate Shane To-Do — the restart is the inherent deploy step for any BuildConsole code change.
