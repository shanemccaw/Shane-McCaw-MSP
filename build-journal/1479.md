# #1479 — Lift secondary-account build cap entirely — secondary is now Claude Max 20x

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 8f3c2c412 (first pass — no-op); <real hash — fill at DONE for the rip-out pass>

## Log
- 2026-08-28 ⏳ IN FLIGHT — making `AccountCapPolicy.ExceedsSonnetHigh` a permanent no-op (always `false`) now that the secondary account is Claude Max 20x; keeping `HeldStatus`/`SonnetPlusOverflowMilestoneTitle`/helper methods wired for a future one-line flip back. Checking for stuck `held` queue items and updating the `watcher` channel log line at QueueWatcherService.cs ~358.
- 2026-08-28 ✅ DONE (first pass, later REOPENED) — `AccountCapPolicy.ExceedsSonnetHigh(model, effort)` made a permanent no-op (always `false`); helpers/consts left in place. This neuter-not-delete approach was reopened by Shane: the cap was enforced in FOUR places (the first pass touched one), the held-item context menu still hardcoded a Sonnet-only downgrade, and queue #762 stayed stranded at `status='held'`.
- 2026-08-28 ⏳ IN FLIGHT (rip-out pass) — Deleting the cap machinery entirely per Shane's decision, not neutering it: AccountCapPolicy cap methods/consts, QueueWatcherService launch guard + `HoldForSonnetOverflowAsync`, the held-item context menu at BuildQueuePanel.xaml.cs:2483-2553, the `SonnetOverflowBanner` + its "Resume All on Primary" handler, and BuildQueuePostgresClient's `MarkHeldForOverflowAsync`/`GetHeldOverflowAsync`/`ResumeHeldOverflowAsync` + held handling in `ForceClaimAsync`. Then draining the existing held rows in-app (preserving each row's real account/model/effort) and clearing the 'Sonnet+ Overflow' milestone. Verifying against local PostgreSQL that no `status='held'` row remains and #762 is back to `queued` with model=claude-opus-4-8/effort=high/account='secondary'.
