# #1479 — Lift secondary-account build cap entirely — secondary is now Claude Max 20x

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 8f3c2c412

## Log
- 2026-08-28 ⏳ IN FLIGHT — making `AccountCapPolicy.ExceedsSonnetHigh` a permanent no-op (always `false`) now that the secondary account is Claude Max 20x; keeping `HeldStatus`/`SonnetPlusOverflowMilestoneTitle`/helper methods wired for a future one-line flip back. Checking for stuck `held` queue items and updating the `watcher` channel log line at QueueWatcherService.cs ~358.
- 2026-08-28 ✅ DONE — `AccountCapPolicy.ExceedsSonnetHigh(model, effort)` now always returns `false`; `HeldStatus`, `SonnetPlusOverflowMilestoneTitle`, `IsOpusModel`, and `IsAboveHighEffort` left in place unused, and the class doc comment now records the #1479 Max 20x upgrade alongside the existing #1418 history. `QueueWatcherService.HoldForSonnetOverflowAsync` and its "watcher" channel log line (~358) are untouched — they're now simply unreachable since the guard at line ~872 never returns true, which is the intended no-op behavior; no code change needed there. Verified with `dotnet build BuildConsole.csproj` — 0 errors (pre-existing warnings only). Could NOT verify whether any `bt_build_queue` rows are currently `status='held'` — this worktree has no `.env.local`/`DATABASE_URL` available, so the required "check for currently-held builds" step from the issue is unverified; flagged in the issue comment for Shane or the next session to check directly. Merged `agent/1479-q757` → `main` fast-forward, pushed; `verify-branch-merged.mjs` confirms `main` is an ancestor of `origin/main` at 8f3c2c41.
