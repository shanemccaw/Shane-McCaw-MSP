# #4514 — Break-glass prerequisite for identity-ca-hardening-v1 + idempotent quickstart-v1 break-glass steps

- **Status:** ✅ DONE 2026-09-17T14:24:30Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** b9e5830d0 (work), 6cafef998 (IN FLIGHT bookend)
- **Migration:** lib/db/migrations/manual/2026-09-17-break-glass-resolve-steps-4514.sql (run locally; line added to #1630)
- **Findings filed:** #4531 (AI Batter Up), #4532 (Ask Shane), #4533 (Shane To-Do, Ask Shane lane) — all parented #1489

## Log
- 2026-09-17T13:52:54Z ⏳ IN FLIGHT — add a real resolve step to identity-ca-hardening-v1 that finds the break-glass CA exclusion group by naming convention and verifies an enabled Global Admin member (fail closed on none/ambiguous/unverified); give quickstart-v1 create-break-glass-account/create-ca-exclusion-group resolve-then-skip semantics; read-only live-verify against testbed (2 existing accounts, 2 existing groups). BuildConsole buildId 3148.
- 2026-09-17T14:24:30Z ✅ DONE — b9e5830d0: resolve-then-write gains unique / collect + "in:" / onMatch "skip-write" / subject, @odata.nextLink paging (20-page bound, fail closed when a unique/collect/skip read is incomplete), lookup trail. runBaselineTemplateAgainstTenant: skip-write match fires no write, audit action "skipped", existing object returned as data. break_glass_verification_gate refuses a generated secret whose create step was skipped (never applied). loadConfigPack drops resolve-provided vars from operator-required (CA pack operator vars now []). Migration (data only, UPDATE 4/1/1 locally): 3-step resolve on the 4 CA templates that exclude {{breakGlassGroupId}} (unique group -> enabled transitive user members -> active tenant-wide GA among them); skip-write lookups on quickstart-v1 account (all domains) + group. Verification: unit 52/52 (new break-glass-resolve-steps-4514.test.ts); related suites incl. post-rebase #4513/#4516 all pass; api-server tsc clean. Live READ-ONLY on testbed 2080 (break-glass-resolve-steps-4514.live-verify.ts 6/6, lookups only, no write path invoked): all 4 CA templates refuse the 2 exclusion groups as ambiguous (ids 2bf1151a…, 19219599…); each group alone fails (no enabled member — both empty); positive control resolves a real active GA via Finance group; quickstart account lookup finds 00b799b7… + be87e536… and group lookup finds both groups, both refuse rather than create a third. No tenant write. No test manifest: api-server only. guest-mfa-policy intentionally unchanged (GuestsOrExternalUsers only).
