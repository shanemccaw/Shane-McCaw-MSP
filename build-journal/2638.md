# #2638 — Team Management and Invitations: regenerate the contract pack (MSP Console)

- **Status:** ✅ DONE 2026-09-04T06:10:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 7aa47e949 (bookend IN FLIGHT), d62e9fa0d (contract pack, rebased to
  `1490dbd2f` on `main`)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — extract the #1642-pattern contract pack for
  `artifacts/api-server/src/routes/msp-team.ts` (10 routes, MSP-console operator
  counterpart to `portal-team.ts`, built under #2676), per #2638's own dispatch.
- 2026-09-04T06:10:00Z ✅ DONE — wrote
  `docs/team-management-and-invitations-msp-console-contract-pack.md`, a full
  line-by-line audit of all 10 `msp-team.ts` routes cited to file:line, cross-referenced
  against the sibling Portal pack (#2447). Confirmed real DB state via direct `psql`
  (2 real `msps` rows, 0 MSPAdmin/MSPOperator users locally, 1 PlatformAdmin). Confirmed
  orphaned-endpoint status via grep across `artifacts/portal/src`,
  `artifacts/msp-console/src`, `artifacts/msp-website`, `artifacts/shane-mccaw-consulting`,
  `artifacts/admin-panel/src` — zero live callers. Found one genuine bug: the invite
  route (`msp-team.ts:257`) writes the caller's own `mspId` instead of the target
  tenant's real `tenantsTable.mspId`, reachable by a PlatformAdmin inviting across MSPs —
  filed as **#2822**, parented as a sibling sub-issue of Feature #2567, labeled `bug`,
  board status set to AI Batter Up. Verified branch merged into `main`
  (`verify-branch-merged.mjs` — OK) and working tree clean.
