# #2576 — Security Plan: two disconnected models (customer-facing table vs MSP-facing pipeline)

- **Status:** ✅ DONE 2026-09-04T06:19:08Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 7c5b92b82 (bookend IN FLIGHT), 1b23b8b24→742a68a95 (rebased onto main: Security Plan bridge)

## Log
- 2026-09-04T06:15:02Z ⏳ IN FLIGHT — written after investigation + implementation (should have
  been step 1; noting that honestly rather than backdating it). Read #2576's full body + its one
  comment (the BUILD dispatch itself, no superseding content), the settled #1561 architecture
  (closed "not a tenth module... owns almost no data of its own"), `docs/security-plan-contract-pack.md`
  §0.3/§7.1/§8, and both route files in full. Confirmed locally via `psql` that all six
  `portal_security_plans*`/`msp_security_plan_versions`/`msp_security_plan_drafts` tables are
  0 rows — no live data at risk either direction.
- 2026-09-04T06:19:08Z ✅ DONE — Built the real bridge rather than only recommending one: `GET
  /api/portal/security-plan` (`artifacts/api-server/src/routes/portal-security-plan.ts`) now
  additionally resolves and returns `assembledPlan` — the last **signed**
  `msp_security_plan_versions` row for the caller's tenant via the already-tested
  `resolveTenantScope` → `getLastSignedSecurityPlanVersion` path — alongside the unchanged legacy
  `plan` field. This was judged NOT to require stopping for Shane's product call: #1561's own
  closed architecture comment already settled which model is real ("not a tenth module... owns
  almost no data of its own"); what #2576 actually found missing was the bridge implementation,
  not an undecided architecture. The route's header now states plainly that a future customer
  page should build against `assembledPlan`, not `plan`. Left `portal_security_plans` and its 3
  child tables untouched (dropping them is a destructive schema change, out of scope here) —
  filed **#2829** (parented under Feature #1495, milestone v1.1, board status "AI Batter Up") to
  schedule that removal + a contract-pack regeneration once Shane confirms nothing needs the
  legacy shape preserved.
  Verified: `tsc --noEmit` clean on `artifacts/api-server` (zero errors); existing
  `security-plan-{assembly,drift,cross-tenant,prose}.test.ts` — 33 tests — still pass with
  `DATABASE_URL` set; local `psql` re-confirmed all 6 relevant tables at 0 rows both before and
  after the change (no live data touched). No local dev server was reachable to exercise the
  route over real HTTP (connection refused on :8080) — logic verified via types + the existing
  test suite + direct code trace through already-tested helpers instead, per the "genuinely
  unreachable" standing exception.
  Branch merged: `node scripts/dev-server/verify-branch-merged.mjs` confirms `742a68a9` is an
  ancestor of `origin/main`. `git status --porcelain` clean at session end.
  Issue comment posted on #2576 with the full recommendation. Labels: #2576 flipped
  `in-flight` → `complete`.
