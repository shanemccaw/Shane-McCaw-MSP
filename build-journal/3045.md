# #3045 — Change Control: metrics still compute success/failure without cr_pirs.closeCode

- **Status:** ✅ DONE 2026-09-07T15:35:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 38bde0823 (bookend IN FLIGHT), 59b31b407 (fix, rebased/pushed as 187a03555 on main)

## Log
- 2026-09-07T15:05:00Z ⏳ IN FLIGHT — Read #3045 body + comment. Confirmed `cr_pirs`/`closeCode` is real and live (schema `lib/db/src/schema/msp.ts:5278-5343`, migration `lib/db/migrations/manual/2026-09-03-cr-pirs-1502.sql`). Updating `computeChangeMetrics` in `artifacts/api-server/src/lib/portal-change-metrics.ts` to prefer `cr_pirs.closeCode` for the execution it reviews, falling back to `cr_executions.outcome`/`cr_events` when no PIR exists, and correcting the stale header comment.
- 2026-09-07T15:35:00Z ✅ DONE — `computeChangeMetrics` now looks up `cr_pirs` (keyed by `execution_id`, real UNIQUE constraint so no "latest" ambiguity) for a CR's latest settled execution and prefers its `closeCode` (`successful`/`successful_with_issues` → success, `failed`/`rolled_back` → failure) over `cr_executions.outcome`; falls back to the existing outcome/events logic exactly as before when no PIR exists. Rewrote the file's stale header comment which claimed #1502 was still open. `npx tsc --noEmit` on `artifacts/api-server` passes clean (exit 0, zero diagnostics). No existing test manifest covers this pure-aggregation lib function (no route/UI behavior changed — same wire shape, more accurate inputs), and this module isn't itself a portal page needing a `*Data.ts`-style manifest. Verified `agent/3045-q1834`/`59b31b407` is merged into `origin/main` (`verify-branch-merged.mjs` → OK, pushed as `187a03555`). `git status --porcelain` clean.
