# #2131 — Mandatory timestamps on BUILD: comments and bookend status lines

- **Status:** ✅ DONE 2026-08-31T23:35:31Z
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** bbfead25c (main, squashed rebase of 7d73aa664 bookend-in-flight + 5f737d81e real work)

## Log
- 2026-08-31T23:30:00Z ⏳ IN FLIGHT — updating ActiveChatBuildRequestHelper.cs, BatterUpQueueService.cs, CLAUDE.md, and grepping repo for other BUILD:/bookend format templates.
- 2026-08-31T23:35:31Z ✅ DONE — Added mandatory `Posted: <UTC ISO8601>` line to the `BUILD:` comment
  header convention and a UTC ISO8601 timestamp on every bookend status transition (not just at
  file creation). Files touched:
  - `desktop/BuildConsole/Services/ActiveChatBuildRequestHelper.cs` — convention text sent to an
    active chat now describes the `Posted:` line.
  - `desktop/BuildConsole/Services/BatterUpQueueService.cs` — `ParseBuildComment` now tolerates an
    optional `Posted:` line between the `BUILD:` header and the prompt body, parses it without
    letting it leak into the returned prompt text, and returns it as a new nullable
    `BatterUpRow.Posted` (`DateTime?`, null for legacy comments with no `Posted:` line — never
    backfilled). Updated the tuple-deconstruction call sites in `DispatchPanel.xaml.cs` (x2) and
    `LeftSidebar.xaml.cs` (x1) that broke from the added tuple element.
  - `CLAUDE.md` — documented the `Posted:` line as part of the required `BUILD:` header format, and
    added the timestamp requirement to both bookend moments (IN FLIGHT / DONE) in the session
    bookends section.
  - `build-journal/README.md` — bookend template and the "two bookend moments" section now show a
    timestamp on every status line.
  - `docs/build-plans/_ISSUE_TEMPLATE.md` — updated its stale `PLATFORM_BUILD.md` reference to
    `build-journal/<id>.md` and added the timestamp requirement.
  - `docs/handoff-2026-08-31.md` — dispatch workflow section now mentions the `Posted:` line (this
    doc is an explicitly dated/stale snapshot per its own header, updated only for consistency).
  - `desktop/BuildConsole/BUILD_LOG.md` deliberately NOT touched — it is a frozen archive per
    CLAUDE.md ("add nothing new here"); its historical rows predate this rule and stay as-is.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj` succeeds, 0 errors, both
  before and after the origin/main rebase. `node scripts/dev-server/verify-branch-merged.mjs` →
  OK, merged into origin/main at `bbfead25c`. `git status --porcelain` clean.
