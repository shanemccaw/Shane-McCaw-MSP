# #2764 — Retention: accelerated-delete review-queue backend, restore audit trail, notify-on-restore trigger

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — Read #2764 body+comments, #1947 (blocker, DONE/complete, foundation landed in
  `lib/db/src/schema/retention.ts` + `artifacts/api-server/src/lib/retention/*`), #1949 (MSP console queue UI —
  closed NOT_PLANNED/reset, zero landed code, `artifacts/msp-console` doesn't exist), #1948 (customer-facing ghost
  mode, separate issue), #1942 (alert catalogue expansion, different notification mechanism), #1944 (epic). Found
  #1947 already built the full state machine (`requestAcceleration`/`decideAcceleration`/`restore`/`purgeNow` in
  `lifecycle.ts`) with restore-reason-required already enforced and audit logging already wired via
  `createAuditLog`. Real remaining gap for #2764: (1) no query surfacing the pending-acceleration queue with delete
  reason for an operator to read, (2) no route exposing any of this (zero callers anywhere in the codebase), (3) no
  notify-on-restore trigger. Building: `listAccelerationQueue()` in lifecycle.ts, `notifyRetentionRestore()` in
  notification-center.ts wired into `restore()`, and a real `/api/msp/retention/queue` route
  (list/decide/discuss-restore) following the `msp-alerts.ts` convention (requireRole MSPOperator+,
  resolveMspIdStrict, resolveStaffScopedCustomerIds for per-staff scoping).
