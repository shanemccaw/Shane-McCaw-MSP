# #3552 — Build Queue panel: pin QUEUE header (stats/filter/search), only list scrolls

- **Status:** ✅ DONE 2026-09-10T23:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 00d175a11 (implementation), 00f8f516e (post-rebase HEAD pushed to origin/main)

## Log
- 2026-09-10T22:52:49Z ⏳ IN FLIGHT — restructuring `Controls/BuildQueuePanel.xaml` so the
  QUEUE section's header controls (title, `QueueStatusBorder` stats, issue-mirror sync
  countdown, `QueueFilterCombo`, search box) dock/pin to the top of the panel via a new
  `QueueHeaderSection` (DockPanel.Dock="Top", sibling of `BuildSetRollupSection`), while only
  the queue-item list (`QueueGraphContainer` + empty-state placeholders) stays inside the
  existing outer ScrollViewer along with Issues-in-epic/In-Flight/Sessions below it.
- 2026-09-10T23:05:00Z ✅ DONE — implemented, committed (00d175a11), rebased onto moving
  origin/main twice and pushed (00f8f516e). Verified with a clean `dotnet build` of
  `BuildConsole.csproj` (0 warnings, 0 errors — XAML compiles). Confirmed every code-behind
  reference to the moved elements (`QueueStatusBorder`, `QueueGraphContainer`,
  `QueueFilterCombo`, etc.) is by `x:Name`, not structural/parent-relative, so reparenting
  them under the new `QueueHeaderSection` is safe. No live BuildConsole instance was
  launched to visually screenshot the scroll behavior (per Git #2156, never launch
  BuildConsole.exe from a build session to verify) — verification here is the build +
  code-reference audit above, not a rendered screenshot. `verify-branch-merged.mjs`
  confirms the branch is an ancestor of `origin/main`; `git status --porcelain` is clean.
  No findings filed — no genuine bug/gap discovered outside this issue's own scope.
