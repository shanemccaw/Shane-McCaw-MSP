# #1632 — Wire up CheckIssueClosuresAsync (Build Watch eviction) via two explicit triggers

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** `045eea05b` (rebased/pushed to `main`; original worktree commits `a96688abf`, `4dedd2ac8`)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Implementing the two triggers specified in #1632: (1) LeftSidebar.GitBoardOpenIssuesRefreshed event fired at end of PopulateGitTrackerBoard, forwarded by MainWindow into an open BuildWatchWindow to evict/promote off already-fetched data; (2) a manual "Recheck closures" title-bar icon on BuildWatchWindow calling CheckIssueClosuresAsync() directly. No new timer/poll.
- 2026-08-29 ✅ DONE — Both triggers implemented exactly as specified:
  1. `LeftSidebar.GitBoardOpenIssuesRefreshed` (new `EventHandler<HashSet<int>>`) fires at the end of `PopulateGitTrackerBoard`, carrying `openNumbersForPromotion` (hoisted out of the `_db != null` block so it's available unconditionally — no second computation, no extra `gh` call).
  2. `MainWindow` subscribes and forwards into `_buildWatch?.ApplyOpenIssueSet(openNumbers)`, a new public method on `BuildWatchWindow`.
  3. `BuildWatchWindow` gained a new title-bar "Recheck closures" icon (`BtnRecheckClosures`, Segoe MDL2 refresh glyph) whose click handler calls the existing `CheckIssueClosuresAsync()` directly — a genuine one-click `gh` call.
  4. Refactored the shared eviction/promotion body (clear closed-issue slots + `PromoteVerifyingToDoneAsync`) out of `CheckIssueClosuresAsync` into `EvictClosedAndPromoteVerifyingAsync(open, source)`, used by both `CheckIssueClosuresAsync` (fetches its own `gh` set) and `ApplyOpenIssueSet` (given a set, zero `gh` calls). `CheckIssueClosuresAsync` kept its original short-circuit (skip the `gh` call entirely if no occupied slot has a GithubNumber).
  5. No new timer/poll — `PollAsync`'s automatic-poll removal (2026-08-14) is untouched; only the two explicit triggers above ever call the eviction/promotion path now.
  - **Build:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 errors, same 46 pre-existing warnings as before this change (confirmed by diffing warning lists) — no new CS0067 "event never used" warning for the new `GitBoardOpenIssuesRefreshed` event, confirming it's genuinely subscribed.
  - **Live verification NOT run in this environment** — no running BuildConsole instance / WebView2 host reachable to click the real UI, and no throwaway GitHub issue was closed to observe a live slot clear. What was verified instead: (a) a full read-through of the call graph confirming `CheckIssueClosuresAsync` (via `EvictClosedAndPromoteVerifyingAsync`) is invoked from exactly two places — `BtnRecheckClosures_Click` and `ApplyOpenIssueSet` (itself invoked only from `MainWindow`'s `GitBoardOpenIssuesRefreshed` subscription) — no timer or `PollAsync` tick calls it; (b) `dotnet build -c Release` clean as above. Shane should confirm the two triggers live: click the new title-bar icon with a closed-issue slot present, and separately confirm Git Board's own manual Refresh clears a slot without touching Build Watch directly.
  - Rebased onto `origin/main` (it had moved ahead — `ab2368df8` at rebase time) and pushed directly to `main` at `045eea05b`, per the "commit directly to main" convention; `verify-branch-merged.mjs` confirms `agent/1632-q808` is an ancestor of `origin/main`.
  - `git status --porcelain` clean before writing this.
