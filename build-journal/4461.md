# #4461 — Tenant Status card stuck at "Check 1 of N" for a live scan until an SSE event arrives

- **Status:** ✅ DONE 2026-09-17T03:45:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 225e8b57b (pushed to origin/main as d5fc3662 after rebase)

## Log
- 2026-09-17T03:15:22Z ⏳ IN FLIGHT — Investigated. `msp_diagnostic_runs.checks_ok/checks_error/checks_license_gap/checks_total` are only written once, after the whole run finishes (artifacts/api-server/src/lib/diagnostics-runner.ts, after `executeMonitoringPackage` resolves). The poll fallback in `artifacts/portal/src/components/shell/useScanState.ts` (line ~282) derives `index` from those columns while a run is active, so it's always `Math.max(1, 0) = 1` until an SSE event lands. Plan: add a `checks_done` column, write it (throttled) from the runner's `onProgress` callback alongside `checks_total` (captured from the first progress event), expose both on the `active` object in `GET /api/portal/scan-status`, and have useScanState's poll-fallback branch read `checksDone` directly instead of summing the terminal-only columns.
- 2026-09-17T03:45:00Z ✅ DONE — Implemented and live-verified.
  - Schema: added `checksDone` (`checks_done`) to `mspDiagnosticRunsTable` (`lib/db/src/schema/msp.ts`), migration `lib/db/migrations/manual/4461-diagnostic-runs-checks-done.sql` — ran it myself against the local `DATABASE_URL` (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, additive).
  - Runner: `diagnostics-runner.ts`'s `onProgress` now writes `checksDone`/`checksTotal` to the run row, throttled to one write per 2s (`PROGRESS_DB_WRITE_THROTTLE_MS`) plus always on the final check, fire-and-forget with the same non-fatal-log pattern as the existing heartbeat.
  - Route: `GET /api/portal/scan-status` (`portal-assessment.ts`) now returns `checksDone` on the `active` object.
  - Frontend: `scanTypes.ts` carries `checksDone` on `ScanStatusActiveRun`; `useScanState.ts`'s poll-fallback `index` derivation now reads `status.active.checksDone` instead of summing the terminal-only `checksOk+checksError+checksLicenseGap` (always 0 mid-run).
  - Typecheck: `lib/db` rebuilt (`tsc -b --force`), `api-server` and `portal` both typecheck clean for the touched files (no errors from `tsc --noEmit`).
  - **Live verification (real scan, no SSE listener — the exact "different process" scenario in the issue):** merged the branch into the shared dev-server checkout (`request-restart.mjs`, server HEAD `efeb4580`), then ran a real `runDiagnostics({ customerId: 2080 })` against tenant `c4c814d4-…` (Graph consent granted, 28 real checks) from a separate scratch vitest test (no SSE clients attached), polling the row + the real `GET /api/portal/scan-status` HTTP endpoint (signed JWT) every 500ms. Observed `checksDone` climb `1 → 6 → 17 → 18 → 19 → 24 → 28` while `status: "running"`, and the HTTP route correctly returned `active: null` / `lastRunSummary.status: "completed"` once it finished — confirms the poll fallback no longer stalls at "Check 1 of N" for a live run. Scratch test file deleted after use; working tree left clean.
  - `git status --porcelain` clean. Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
