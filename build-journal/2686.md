# #2686 — Detected panel: slow sequential fetch + stale board-column status

- **Status:** ✅ DONE 2026-09-03T17:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 08f906f90

## Log
- 2026-09-03T16:49:02Z ⏳ IN FLIGHT — parallelize ChatDockService.BuildAsync's per-item fetch (Task.WhenAll instead of sequential foreach) and add a real LiveQueueStatus field cross-referenced against BuildQueuePanel.CurrentQueueItems (same mechanism #2548 proved), rendered in ChatDockPanel.BuildItemCard with BoardStatus as fallback.
- 2026-09-03T17:05:00Z ✅ DONE — 08f906f90 pushed and fast-forward-merged into origin/main (verify-branch-merged.mjs confirmed). `ChatDockService.BuildAsync` extracted per-item work into `BuildItemAsync`, fanned out via `Task.WhenAll` instead of a sequential `foreach` (fail-closed per-item behavior preserved). Added `ChatDockItem.LiveQueueStatus`, cross-referenced from a new `IReadOnlyList<QueueItem>? liveQueueItems` param (both call sites now pass `_owner.BuildQueuePanel?.CurrentQueueItems`), matching by `GithubNumber`/`AssociatedIssueNumbers` same as #2548's `ChatDocumentContainer.ComputeAndRenderCounts`, preferring an active row over a terminal one. `ChatDockPanel.BuildItemCard` renders the live-status chip (same icon/color vocabulary as `BuildQueuePanel.GhostStatusLabel`) when present, falling back to the plain `BoardStatus` chip only when no live queue row exists. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. No live GitHub-connected instance available to time the sequential-vs-parallel real load in this session; the parallelization itself is a straightforward, low-risk refactor (Task.WhenAll over already-independent per-item work, same try/catch-per-item shape kept). `git status --porcelain` clean.
