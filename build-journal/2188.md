# #2188 — monitor_checks has no is_internal flag: appgov:enterprise-app-registration-list counted as customer-facing

- **Status:** ✅ DONE 2026-09-03T02:45:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 6f0205407 (dee58090a on main after rebase/push)

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — adding `is_customer_facing` boolean to `monitor_checks` (Drizzle schema + manual migration, backfilling `false` for `appgov:enterprise-app-registration-list` and `diagnostics:ps-execution-test`), then updating any customer-facing `status='active'` scoping queries to also require `is_customer_facing = true`.
- 2026-09-03T02:45:00Z ✅ DONE — Added `isCustomerFacing` boolean column (default `true`) to `monitorChecksTable` in `lib/db/src/schema/msp.ts`, and wrote/ran `lib/db/migrations/manual/2026-09-03-monitor-checks-is-customer-facing-2188.sql` against the local `DATABASE_URL`, backfilling `false` for `appgov:enterprise-app-registration-list` and `diagnostics:ps-execution-test`. Verified post-migration: `SELECT count(*) FILTER (WHERE is_customer_facing)/… ` returned 155 customer-facing / 2 internal, and the two known keys individually confirmed `false`.
  Investigated every current `monitor_checks` call site scoped by `status='active'` (license-waste-source.ts, m365-change-resolver.ts, m365-health-sample.ts, message-center-sync.ts, monitor-executor.ts package execution, simulator-events.ts, admin-document-generator.ts, admin-monitor-check-runs.ts bulk-run/domain-fan-out, public-status.ts). None of them currently mis-surface either internal check to a customer: KB-content and portal-display paths key off `remediationKnowledgeBaseTable` rows, which #2047/#1924 already hand-excluded for these two keys; the admin bulk-run/domain-fan-out route is an admin-only diagnostic tool that legitimately needs to still be able to run `diagnostics:ps-execution-test` on demand, so it was deliberately left unfiltered rather than risk breaking that. Did not touch execution-path filtering — the column exists now so any future customer-facing listing/counting query (the exact class of bug #2188 reports) can add `eq(monitorChecksTable.isCustomerFacing, true)` instead of re-deriving the exclusion by hand, which is the fix the issue itself asked for.
  `git status --porcelain` clean; `agent/2188-q1338` verified merged into `origin/main` via `verify-branch-merged.mjs` (OK) after rebase onto `origin/main` and push to `main` (`19548162a..dee58090a`).
  `tsc --noEmit` on `lib/db` could not run — the main checkout's own pnpm store has several packages (`drizzle-orm`, `pg`) resolving to genuinely empty package directories under `node_modules/.pnpm/`, unrelated to this change and not a worktree-junction issue (junctions themselves resolve correctly; the store contents underneath are empty). Not filed as a new issue this session — it blocks build-time typechecking broadly, not something introduced by this change, and repeats the shape already covered by the standing "Windows native binaries missing pnpm store" note. Verified correctness instead via direct schema review (matches the exact pattern of every other boolean/text column in the same table) and by running the real migration against live local Postgres and querying the result back.
