# #2467 — Import: bullets and numbering split into their own notes and get stripped

- **Status:** ✅ DONE 2026-09-02T23:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 94d40a655 (implementation, worktree branch), 1406368cf (rebased/pushed to `main`)

## Log
- 2026-09-02T22:20:00Z ⏳ IN FLIGHT — re-dispatch of #2346 (closed NOT_PLANNED, blocked on #2343/#2344/#2345, all now landed). Audited `TestPadImportParser`/`NotepadImportLineClassifier`/`NoteMarkerParser`: confirmed a bullet/numbered line (`- foo`, `1. foo`, `2) foo`) inside one blank-line block currently merges into the surrounding paragraph as plain wrapped text instead of becoming its own note, and its marker is never stripped. About to implement bullet/numbering detection so each such line always starts a new paragraph (own note) with the marker stripped from the body.
- 2026-09-02T23:45:00Z ✅ DONE — added `NotepadImportBulletParser` (recognizes `-`/`*`/bullet-char markers and numeric ordinals `1.`/`1)`/`(1)`, strips them) and wired it into `TestPadImportParser.SplitIntoParagraphs`: a bulleted/numbered line now always starts its own paragraph (own note), regardless of the previous line's terminal punctuation, using the stripped text for the note body. **Real bug found and fixed mid-build:** `NotepadImportLineClassifier.IsSectionHeader`'s bare-1-4-word rule (#2344) was misclassifying the first bullet of any list right after a Section header as a spurious Section itself (e.g. `- did A thing` is 4 words, no terminal punctuation, no comma) — the entire list silently vanished. Fixed by excluding any bullet/numbered line from header detection before the existing rules run. Verified via a real scratch `dotnet run` harness (not committed) exercising `TestPadImportParser.Parse` directly against sample Notepad-style pastes with dash/asterisk/numeric bullets under a section, mixed with normal wrapped-paragraph reflow and a bare short header — all bullets split into individual notes with markers stripped, normal paragraphs still rejoin, bare-header regression (`Bugs Found`, `RAW JSON`) unaffected. `dotnet build ShaneBuilder.csproj` — 0 warnings, 0 errors. During the build a concurrent session landed #2468 (orphaned/pending-header handling) on the same file; rebased onto it, resolved the doc-comment conflict manually (both features' logic merged automatically, only a comment block conflicted), re-verified the merged file with the same harness — both features cooperate correctly. Pushed directly to `main` per this repo's git convention (`1406368cf`), `verify-branch-merged.mjs` confirms ancestor-of-main. `git status --porcelain` clean, nothing left uncommitted.
