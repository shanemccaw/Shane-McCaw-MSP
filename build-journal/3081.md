# #3081 — POA&Ms: bidirectional conversion to/from risk acceptance (RBD) (Phase 1b)

- **Status:** ✅ DONE 2026-09-10T05:47:45Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** a97ba1966 (agent/3081-q2152), merged to main at 2fafe6760

## Log
- 2026-09-10T05:25:18Z ⏳ IN FLIGHT — Read #3081 + #1935 architecture decision + the real #3080 (Phase 1a) schema/routes (`msp_poams`, `msp_risk_decisions`, `msp-poams.ts`, `msp-rbd.ts`). Building the two real conversion routes (POA&M→RBD, RBD→POA&M) on the MSP-console side, reusing the existing portal signature ceremony for the new record rather than inventing a second one, plus real conversion pointer columns + an `msp_audit_logs` audit trail.
- 2026-09-10T05:47:45Z ✅ DONE — Shipped both real conversion routes:
  `POST /api/msp/poams/:poamId/convert-to-risk-acceptance` and
  `PATCH /api/msp/rbd/:rbdId/convert-to-poam` (both `MSPAdmin`-gated, 409 on a
  non-`active` source). Schema: `lib/db/migrations/manual/2026-09-10-poam-rbd-
  bidirectional-conversion-3081.sql`, self-run against local Postgres —
  forward/back conversion pointer columns + `conversionReason` on both
  `msp_poams`/`msp_risk_decisions`, and new terminal status values
  (`converted_to_risk_acceptance` / `converted_to_poam`) added to
  `POAM_STATUSES`/`RISK_ACCEPTANCE_STATUSES` so the source row's status
  genuinely reflects the conversion. Real audit trail via `msp_audit_logs`
  (`msp.poam.convert_to_risk_acceptance` / `msp.rbd.convert_to_poam`).
  `tsc --noEmit` clean on both `lib/db` and `api-server`. Verified live
  against real local Postgres via
  `artifacts/api-server/src/routes/msp-poam-rbd-conversion.live-db.test.ts`
  (4/4 passing: both directions, the 409 guard, real cross-linking, the
  audit trail) — test rows confirmed cleaned up after the run. Merged to
  `origin/main` at 2fafe6760, `verify-branch-merged.mjs` confirms ancestry.
  `git status --porcelain` clean. No findings filed — nothing genuinely
  out-of-scope surfaced during this build.
