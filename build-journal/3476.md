# Issue #3476 — Feature: Local Telemetry Console (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T06:35:00Z
- **Scope:** myarchitect

## Log
- 2026-09-10T06:20:00Z — ⏳ IN FLIGHT — re-dispatched against #3476. Found the
  feature was already implemented and merged (commit 0b479d8a6, from an
  earlier same-day session). Verified the merged code against the real
  api-server route registrations instead of trusting the earlier bookend's
  claim at face value.
- 2026-09-10T06:35:00Z — ✅ DONE 2026-09-10T06:35:00Z — Verification found 3 of
  the 5 wired endpoints in `TelemetryService.cs` were pointed at routes that
  don't exist (`/api/portal/remediation-checklist`, `/api/portal/engine-history`)
  or were parsing a response shape the real endpoint doesn't return (Copilot
  deltas). Fixed:
  - SOW progress → corrected to the real `/api/portal/remediation/checklist`.
  - Copilot readiness deltas → switched from the nonexistent customer-session
    `/api/portal/engine-history?pillar=copilot` to the real admin-scoped
    `/api/admin/engines/:key/history?customerId=...`, and fixed the JSON
    parsing to match its real `{series:[{date,score,delta,trendDirection}]}`
    shape (was reading a `snapshots`/`snapshotDate` shape that endpoint never
    returns).
  - Engine dashboard + drift events endpoints were already correct.
  - Doc comments in both files corrected to the real registered paths.
  - `dotnet build MyArchitect.csproj` — 0 errors, 0 warnings.
  - Commit: **27b72219b** (rebased onto and pushed directly to `origin/main`;
    `verify-branch-merged.mjs` confirms ancestor).

## Finding filed
- **#3505** — `TenantService.cs` (from #3457) is entirely hardcoded/fixture
  tenant data (4 fake companies), so the Telemetry Console — and every other
  tenant-scoped MyArchitect view — can only ever show these fake tenants
  until it's replaced with a real API-backed tenant list. Parented as a
  sibling sub-issue of #3457 (the Feature that introduced it), labeled `bug`,
  board status set to AI Batter Up. Not blocking on #3476's own file scope.

## Working tree
- `git status --porcelain` clean at end of session (only files touched were
  committed).
