# Build Journal — #3602

- **Status:** ✅ DONE 2026-09-10T22:52:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T22:30:00Z — Started. Fix: ActivityBar re-click on already-active icon while collapsed doesn't refire ActiveViewChanged (RadioButton.Checked doesn't refire without a state transition). Adding additive PreviewMouseLeftButtonDown handler.
- ✅ DONE — 2026-09-10T22:52:00Z — Added `Btn_PreviewMouseLeftButtonDown` in `Controls/ActivityBar.xaml.cs`, fired when the clicked `RadioButton` is already checked (`IsChecked == true`), invoking `ActiveViewChanged` directly. Wired `PreviewMouseLeftButtonDown="Btn_PreviewMouseLeftButtonDown"` alongside the existing `Checked="Btn_Checked"` on all 8 workspace-view RadioButtons in `Controls/ActivityBar.xaml` (Chats, Explorer, Search, Git, Issues, Automation, GraphApi, Settings). Existing `Btn_Checked`/`Checked` path untouched, so switching to a different icon while collapsed is unaffected. `dotnet build BuildConsole.csproj` succeeded, 0 errors. Merged `agent/3602-q2270` onto `origin/main` at commit `868a13866663bcb293ee165abfa07d6e6e5bd00c` (rebased once after a concurrent push moved `main`); verified `HEAD` is an ancestor of `origin/main`. Working tree clean.
