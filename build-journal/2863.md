# #2863 — dedupe sku_price_reference lookups in licensing.wasteEstimateBreakdown

- **Status:** ✅ DONE 2026-09-04T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 38b2b313e (fix), 4bdb2b8e0 (merge into main)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — investigate dashboard-resolvers.ts:832-878 and cost-engine.ts's computeSkuCostBreakdown; make computeSkuCostBreakdown accept an already-resolved price map instead of re-querying sku_price_reference.
- 2026-09-04T00:20:00Z ✅ DONE — `computeSkuCostBreakdown` (cost-engine.ts) now accepts `opts.priceMap: SkuPriceMap`; when provided, it's used instead of re-querying `sku_price_reference`. `dashboard-resolvers.ts`'s `licensing.wasteEstimateBreakdown` resolver now builds a `resolvedPrices` map alongside its existing paid-only-filter price lookup and passes it through, so `sku_price_reference` is queried once per distinct SKU per resolve instead of twice. Verified: `vitest run dashboard-resolvers.test.ts cost-engine.test.ts` — 24/24 passed; `tsc --noEmit` on the two touched files clean. Merged to `main` via a temp worktree (main was checked out elsewhere in this checkout); `verify-branch-merged` / ancestor check confirmed HEAD is an ancestor of origin/main. Working tree clean.
