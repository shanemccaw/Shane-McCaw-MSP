# #1597 — Portal New Design: Webhooks — contract extraction pack

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** `bac4865cd` (IN FLIGHT bookend), `c0fbd0387` (docs/webhooks-contract-pack.md) — merged into `origin/main` at `a63d3cad6`, verified via `verify-branch-merged.mjs`.


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1597`, but #1597 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1597 is complete.
>
> #1597's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-28 ⏳ IN FLIGHT — Read-only investigation per #1577's contract-pack method (step 3 of #1578, scoped to Webhooks only per #1577's scoping correction). Deliverable: `docs/webhooks-contract-pack.md`, extracting `artifacts/api-server/src/routes/webhooks.ts` (+ `lib/webhook-delivery.ts`, `lib/db/src/schema/msp.ts` outbound_webhooks / outbound_webhook_deliveries) verbatim to file:line, cross-checking the #1278 `customer_tenant_alert_rules` catalog against webhooks' own event vocabulary via direct local Postgres queries. No UI or schema changes. Will post a summary comment on #1597 and commit/push before flipping to DONE.
- 2026-08-28 ✅ DONE — Shipped `docs/webhooks-contract-pack.md` (commit `c0fbd0387`, verified ancestor of origin/main at `a63d3cad6`). Read-only; no product code or schema touched. Key findings: `webhooks.ts` declares no named `Wire*` interface (extracted from its inline `.select()` projections + `DeliveryLogEntry`, `webhook-delivery.ts:310`, instead); three non-overlapping event-name vocabularies exist (the real 41-string `SUBSCRIBABLE_EVENT_TYPES`, the Design fixture's 11-row `WEBHOOK_EVENTS` — verified zero real backing, none ever dispatched — and #1278's 23-row alert-condition catalog), and webhooks ride none of the latter two; `signal.fired` is subscribable but never dispatched; local Postgres shows `customer_tenant_alert_rules` at 23/0 live/pending_detector today, contradicting this issue's quoted "19 live, 4 pending_detector"; signing secret is served in full exactly twice (create, rotate) — the Design prototype's "Reveal" button has no backing endpoint; Rotate/Edit/Delete render unwired despite real backend support, Replay has no backend at all; `outbound_webhooks`/`outbound_webhook_deliveries` are genuinely empty (0 rows) in local dev right now. Summary comment posted on #1597 (not closed). No sub-issues filed for the open gaps — left in the doc's §9 table for Shane to triage, per #1577's DECIDED-needs-an-issue-number rule.
