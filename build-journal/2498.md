# Build Journal — #2498

- **Status:** ✅ DONE 2026-09-03T03:14:00Z
- **Scope:** buildconsole
- **Issue:** #2498 — Build Chain Map: priority reorder doesn't persist sub-issue ORDER (gate edges revert to manual on refresh), sub-issue of Feature #2473
- **Started:** 2026-09-03
- **Commit(s):** `ca2b99589` (work) · `ff468e06d` (IN FLIGHT bookend) — both on `origin/main`, branch `agent/2498-q1360` verified merged via `verify-branch-merged.mjs` (exit 0)
- **Findings filed:** none — the change is self-contained; no new proven-broken behaviour discovered.

## Log
- 2026-09-03T02:56:01Z — ⏳ IN FLIGHT — Session started. Pulled #2498 body + the single BUILD comment (dispatch, `Posted: 2026-09-03T02:37:57Z`). Read the full stack the dispatch names: `Services/BuildMap/ChainPersistence.cs` (diff-based persister — blocked_by edges + board moves only, explicitly NOT sub-issue order), `BuildChainMapWindow.Inspector.cs` `ReorderFeature`/`MoveFeature` (splice `_doc.Order` + `RechainGates` + `PersistThenConfirm`), `Services/BuildMap/BuildChainMapService.cs` (`doc.Order` from `GetSubIssuesAsync(epicNumber)` = the Epic's real sub-issue priority order; `ClassifyEdgeKind` re-derives fanin/gate/manual from that adjacency on every load), and `GitHubApiClient.cs` write surface. Confirmed the gap exactly as filed: Features are the Epic's direct sub-issues; `doc.Order` is the literal order GitHub returns them in; a reorder splices the in-memory list and persists the resulting gate edges (#2481) but never writes the sub-issue priority order back, so the next Reset re-reads GitHub's OLD order and `ClassifyEdgeKind` re-labels the just-written gate edges as `manual`. Plan: (1) add `ReprioritizeSubIssueAsync(parent, subIssueNumber, afterNumber?, beforeNumber?)` to `GitHubApiClient` — GitHub's real `PATCH /repos/{o}/{r}/issues/{parent}/sub_issues/priority` (body: `sub_issue_id` + `after_id`/`before_id`, all database ids resolved from issue numbers the same GET-then-id way `SetBlockedByAsync` already does) — plus a `GetSubIssueOrderAsync` audit read; (2) call it from `ReorderFeature` alongside the existing edge persistence, anchoring the moved Feature to a genuine adjacent sibling (after its new predecessor, or before its new successor when it lands at the top); (3) re-read the parent's sub-issue order back and confirm it matches `_doc.Order` — the done-when criterion. Will verify the endpoint live net-zero (reorder + restore) before wiring.
- 2026-09-03T03:14:00Z — ✅ DONE — commit `ca2b99589` on `origin/main`, branch verified merged (`verify-branch-merged.mjs` exit 0). **What shipped:** (1) `GitHubApiClient.ReprioritizeSubIssueAsync(parent, subIssueNumber, afterNumber?, beforeNumber?)` — GitHub's real `PATCH /repos/{o}/{r}/issues/{parent}/sub_issues/priority` write, resolving all three issue numbers to the database ids the endpoint requires (`sub_issue_id`/`after_id`/`before_id`) via a new `ResolveIssueDatabaseIdAsync` helper (the same GET→`.id` step `SetBlockedByAsync` inlines); throws on non-success, same contract as the other Map writes. (2) `ChainPersistence.PersistReorderAsync(client, epic, movedFeatureNumber, newOrder)` + `ChainReorderResult` — anchors the moved Feature to a genuine adjacent sibling (after its new predecessor, or before its new successor when it lands at the very top, so it never relies on GitHub's implicit "no anchor = top") and **audits by re-reading the Epic's sub-issue order back**, confirming the moved Feature sits in exactly the intended relative position. (3) `BuildChainMapWindow`: `ReorderFeature` now captures the moved Feature number + the new priority order (as real sub-issue numbers) synchronously on the UI thread, then routes through a new `PersistReorderThenConfirm` that runs BOTH the #2481 edge/board diff AND the reorder write under the one `_persistLock` and merges their audited results into one status-strip confirmation + ActivityLog block. Updated the stale "column order is local until GitHub sub-issue reordering lands" copy and both class docs to reflect that both halves now persist. **This closes the exact gap #2498 filed:** the gate `blocked_by` edges (persisted by #2481) no longer re-classify as `manual` on refresh, because GitHub's sub-issue order now matches what `ClassifyEdgeKind` re-derives adjacency from. **Verification (real, against live GitHub, net-zero):** `dotnet build` clean twice (0 warn / 0 err). Ran a throwaway console harness (temp dir, outside the repo, since BuildConsole is single-instance-mutex-guarded and must not be launched) that referenced the compiled `BuildConsole.dll` and drove the ACTUAL `ChainPersistence.PersistReorderAsync` → `ReprioritizeSubIssueAsync` + re-read-audit path against Epic #2473's 13 real Feature sub-issues: moved #2476 to the top (exercises the `before_id`/idx==0 branch) → `intended=True verified=True`, re-read confirmed `2476,2475,2477…`; restored #2476 after #2475 (exercises the `after_id` branch) → `intended=True verified=True`; final re-read `SequenceEqual` to the original order → **net-zero restored ✓**. Harness deleted; working tree clean (only my three files were touched). Not launched (single-instance-mutex rule); BuildConsole is out of scope for test manifests per CLAUDE.md. **Findings filed:** none.
