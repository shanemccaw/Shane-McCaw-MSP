# #4561 — memory-pressure auto-pause never auto-resumes (clear-side zero noise floor)

- **Status:** ✅ DONE 2026-09-17T21:24:34Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** `a80352c43` (fix); bookends `aca47b6cc` (IN FLIGHT), `<this>` (DONE).

## Log
- 2026-09-17T21:23:48Z ⏳ IN FLIGHT — Pulled #4561 + #4543 body/comments/bookend. Confirmed
  the root-cause hypothesis (zero-tolerance `pagefileNotRising = newest <= oldest` on the
  clear/resume side, vs. a real 300MB noise floor on the pause/rise side) matches
  `MainWindow.ResourceMonitor.cs` `EvaluateMemoryPressure` exactly. Building a standalone
  synthetic harness (per #4543's own precedent) to live-confirm before fixing.
- 2026-09-17T21:24:34Z ✅ DONE — Root cause confirmed live via a standalone synthetic
  harness (replica of `EvaluateMemoryPressure`, built outside the repo, deleted after
  use): with realistic ±2MB background-paging noise and RAM held safely under the 80%
  resume threshold for a full 90 simulated seconds, `pagefileNotRising`'s zero-tolerance
  `newest <= oldest` check flipped false on ~half the ticks, resetting
  `_memoryPressureClearSince` every time — the queue never auto-resumed. Fixed in
  `a80352c43` by giving the clear side its own noise floor,
  `PagefileClearToleranceBytes = 32MB` (well above normal jitter, far below the 300MB
  real-pressure `PagefileRiseMinBytes` floor so it can't mask an actual renewed rise) —
  `pagefileNotRising = newest <= oldest + PagefileClearToleranceBytes`. Re-ran the
  harness against the fix: same noise sequence now resumes cleanly at the 30s sustain
  boundary (tick 20, elapsed=30.0s). Ran a second synthetic scenario (genuine sustained
  +50MB/tick rise) to confirm the tolerance does NOT mask real re-pressure — streak
  correctly keeps resetting, never resumes. **Verification:** `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Debug` — 0 Warning(s), 0 Error(s). Real
  live swap-pressure simulation deliberately not run (unsafe on the shared dev box,
  same call #4543 made). Rest of #4543's design (30s sustained cooldown,
  broken-streak-restarts-the-clock semantics, RAM hysteresis, 300MB pause-side floor)
  left intact — tolerance-value fix only, no redesign. `git status --porcelain` clean.
  `verify-branch-merged.mjs` — OK, `a80352c4` merged into `origin/main`. No findings
  beyond the fix itself; no `Shane To-Do`.
