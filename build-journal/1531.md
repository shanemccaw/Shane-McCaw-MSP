# #1531 — Microsoft Changes: Join roadmap items to Message Center posts on feature ID

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 26f08ec8d (feature), rebased onto #1533's landing + pushed to origin/main as 5608e781d

## Log
- 2026-08-29 ⏳ IN FLIGHT — Building the join between `m365_roadmap_items` (#1530,
  the 90-day-and-beyond horizon) and `msp_message_center_items` (this-week/
  next-week horizon, `actionRequiredByDateTime`) on the Microsoft roadmap feature
  ID, which Message Center posts carry in their `body.content` prose/links. Plan:
  (1) a pure extractor that reads a post's bodyContent and returns the roadmap
  feature ID(s) it references, (2) persist the extracted IDs on
  `msp_message_center_items` (new `roadmap_feature_ids` jsonb column + GIN index,
  populated by `message-center-sync.ts` at sync time rather than re-parsed per
  read), (3) a backfill for already-synced rows, (4) a join resolver so a
  roadmap item can report whether it has actually crossed over into any tenant's
  live feed yet — the "affected-object count stops being hypothetical" moment
  the issue names — for #1533 (resolution layer) and #1535 (timeline UI) to
  build on. Not building the timeline UI itself (#1535 owns that; its own issue
  says "direction for Claude Design", not yet specified) and not touching #1533
  (resolution layer / per-tenant counts — a concurrent session owns that, worktree
  1533-q765). channel: "integration.azure".
- 2026-08-29 ✅ DONE — Shipped in 26f08ec8d, rebased onto #1533's landing and
  pushed to origin/main as 5608e781d (branch verified an ancestor of
  origin/main via verify-branch-merged.mjs). `extractRoadmapFeatureIds()`
  (`artifacts/api-server/src/lib/m365-roadmap-mc-link.ts`) parses a Message
  Center post's own `body.content` for the roadmap feature ID(s) it names —
  validated against all 1159 real posts already synced in local Postgres (399
  of the 419 that mention "roadmap" carry a real feature ID, 0 false
  negatives; fixtures include a doubled-slash URL artifact and a `#Roadmap`
  link-fragment Microsoft itself shipped). `message-center-sync.ts` parses this
  once per post at sync time and persists it on the new
  `msp_message_center_items.roadmap_feature_ids` column (GIN-indexed jsonb
  array; manual migration
  `lib/db/migrations/manual/2026-08-29-mc-roadmap-feature-link-1531.sql`).
  `findMessageCenterPostsForFeatureId` / `getCrossedOverFeatureIds` /
  `withCrossoverFlag` are the actual join — given a roadmap feature ID, which
  real tenant posts (if any) reference it, i.e. has it crossed over from the
  90-day-and-beyond horizon into a real tenant's this-week/next-week feed yet.
  Wired into #1532's admin candidates endpoint as a `crossedOver` flag /
  `roadmapFeatureIds` field (types added to `m365ChangesStore.ts`, no UI
  wiring — that stays #1535's job). Because the new column lives on
  `msp_message_center_items` (the table the ALREADY-LIVE daily sync writes for
  every real tenant, not a brand-new table like #1530/#1532's own), every
  read/write checks `hasRoadmapFeatureIdsColumn()` first and degrades to "not
  yet available" rather than throwing — this guard compiles and the gated test
  block (which inserts/reads/backfills real rows) correctly self-skips against
  the current local DB, which has not run the migration yet. Verified: 12
  pure-logic vitest tests pass (`m365-roadmap-mc-link.test.ts`, registered in
  `vitest.config.ts`), 3 DB-integration tests correctly skip (column not yet
  migrated locally); scoped `tsc --noEmit` on api-server clean for every file
  this ticket touched (same 47 pre-existing unrelated errors as #1530/#1532 —
  ai-billing, admin-build-tracker, assorted `.test.ts` — not touched here);
  `tsc -b lib/db --force` rebuilt clean. Did NOT self-apply the SQL migration
  (DDL against local Postgres was blocked by this session's own permission
  classifier, consistent with this repo's standing "Shane runs schema
  migrations himself" rule and #1530/#1532's own precedent). Shane To-Do: run
  `lib/db/migrations/manual/2026-08-29-mc-roadmap-feature-link-1531.sql`
  against local PostgreSQL 18 — until then the join stays inert but nothing
  breaks (see the guard above); once it runs, the next daily
  message-center-sync.ts pass (which always re-upserts every message, not
  just new ones) backfills every already-synced post's roadmap link
  automatically, or `backfillMessageCenterRoadmapLinks()` can be run once by
  hand for an immediate result.
