# #1510 — Risk Register: Signature required on scope expansion, never on contraction

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** ae6101d1db5d7f77dc5d699ddcedbbfb642259d6 (post-push, verified via `verify-branch-merged.mjs`)

## Log
- 2026-08-31 ⏳ IN FLIGHT — building the derivation: comparing `risk_instances`
  scope sets between an RBD version and the one it supersedes to decide
  whether a fresh signature is required (additions present) or the prior
  signature is inherited (subtractions only / no change). Also building an
  audit trail on narrative/score fields (hazardDescription,
  compensatingControls, residualRiskScore, residualRiskLevel) since a
  narrative-only revision requires no signature by the letter of the rule —
  audit trail is the interim answer, per the issue's own text. Foundations
  #1508 (RBD versioning) and #1509 (risk_instances line items) already landed
  on main. Wire contract only, per SCOPE STOP — no `artifacts/portal` UI.
- 2026-08-31 ✅ DONE — shipped at `ae6101d1db5d7f77dc5d699ddcedbbfb642259d6`:
  - `lib/db/src/schema/msp.ts` — `RbdNarrativeSnapshot` type; `msp_rbd_versions`
    gains `scopeInstanceIds`/`scopeAddedInstanceIds`/`scopeRemovedInstanceIds`
    (integer arrays), `requiresSignature`, `signatureInherited`(+
    `FromVersionUid`, no FK, same convention as every other cross-version
    reference in this file), and `narrativeSnapshot` (typed jsonb); new table
    `msp_rbd_narrative_audit` (one row per version transition that actually
    changed a narrative/score field).
  - `lib/db/migrations/manual/2026-08-31-rbd-signature-scope-diff-1510.sql` —
    additive `ALTER TABLE`/`CREATE TABLE`, run against local `DATABASE_URL`
    myself; column/table shape confirmed via `information_schema.columns` in
    the migration's own output. Filename appended to #1630.
  - `artifacts/api-server/src/lib/rbd-scope-diff.ts` — the derivation itself,
    kept **dependency-free from `@workspace/db`** on purpose:
    `computeRbdScopeDiff` (any addition present, or first-version-ever, forces
    `requiresSignature: true`; subtraction-only/unchanged is `false`) and
    `diffNarrativeSnapshot` (field-by-field compare over
    hazardDescription/compensatingControls/residualRiskScore/
    residualRiskLevel — the exact three categories the issue names, plus the
    paired level).
  - `artifacts/api-server/src/lib/rbd-versioning.ts` — `createRbdVersion` now
    runs the scope diff inside its existing supersede-then-insert transaction,
    inherits `signed`/`signedBy`/`signedAt`/`signatureData` onto the new
    version when the diff says to (and the version being superseded was
    itself signed), and inserts one `msp_rbd_narrative_audit` row when
    narrative fields actually drifted — never gates capture, never requires a
    signature for that case, exactly as the issue's own text asks ("audit
    trail is the interim answer" — recorded here, not decided further). New
    `listRbdNarrativeAudit`.
  - `artifacts/api-server/src/routes/msp-rbd-versions.ts` — `POST
    .../versions` now derives `scopeInstanceIds` (every currently-`active`
    `risk_instances` row) and `narrativeSnapshot` from the LIVE
    `risk_instances`/`msp_risk_decisions` rows server-side — never from the
    request body, which is what makes the derivation ungameable (a caller can
    only change the real scope by actually adding/accepting/resolving line
    items through `msp-rbd-instances.ts`'s own routes). New `GET
    .../versions/narrative-audit`. `WireRbdVersion` now carries the new
    fields; the route now 404s if no `msp_risk_decisions` container exists
    for `rbdId` (previously version capture didn't require one — documented
    in the file's own header as a behavior change).
  - `artifacts/api-server/src/routes/portal-rbd-document.ts` —
    `WireRbdVersionSummary` (customer-facing) also carries
    `requiresSignature`/`signatureInherited` now.
  - **"New instance never silently absorbed" is enforced by construction**,
    not a separate check: any id present in a new scope that wasn't in the
    prior one is an addition, and any addition forces `requiresSignature:
    true` — so a version carrying an uncovered instance can never
    auto-inherit a prior signature, proven in scenario v5 below.
  - Verification:
    - `lib/db` — `npx tsc --noEmit` clean; rebuilt its junctioned `dist/`
      (`npx tsc -b lib/db --force`) and confirmed the new symbols
      (`scopeInstanceIds`, `mspRbdNarrativeAuditTable`) are present in the
      emitted `.d.ts`.
    - `artifacts/api-server`'s own `tsc --noEmit` **could not run** in this
      worktree: `node_modules/@workspace/db` is a Mount Point junction to
      `C:\wt\q1075\lib\db`, which **does not exist at all** (confirmed via
      `ls`) — a fully dangling junction, not merely stale, and pre-existing
      (an untouched file, `msp-rbd.ts`, errors identically). This is the
      already-documented worktree-isolation class (#2087/#2089/#2097/#2117/
      **#2121**, most specific match); added this session's specific
      evidence as a comment on #2121 rather than filing a duplicate. Did not
      attempt `pnpm install`.
    - Worked around the broken junction two ways: (1) extracted the pure
      derivation logic into `rbd-scope-diff.ts` with **zero** `@workspace/db`
      dependency, wrote a real vitest suite for it
      (`rbd-scope-diff.test.ts`, registered in `vitest.config.ts`'s explicit
      include list), and **ran it** — 12/12 passing, covering every scenario
      the issue names including the mixed addition+subtraction case ("nobody
      consents to being safer" — must still require a signature) and the
      narrative-only score/level drift case. Ad-hoc isolated `tsc --noEmit`
      on that file + its test (with the same compiler flags the real
      tsconfig uses) is also clean. (2) A comprehensive rolled-back
      transaction directly against the real local Postgres, using the one
      real live `msp_risk_decisions` row (`RBD-2026-575`, id 1), replicating
      `createRbdVersion`'s exact insert logic across 5 versions: v1 (first
      ever, empty scope) → `requires_signature=t, signed=f`; v2 (2 new
      instances added) → `requires_signature=t, signed=f`; v2 signed by a
      simulated customer; v3 (1 instance resolved, subtraction-only) →
      `requires_signature=f, signed=t, signature_inherited=t` (copied from
      v2); v4 (narrative-only — residual score 8→15, level medium→Medium,
      scope untouched) → `requires_signature=f, signed=t` (still inherited)
      **and** a real `msp_rbd_narrative_audit` row recorded the exact
      field-level diff; v5 (a brand-new instance appears on top of the
      otherwise-unchanged scope) → `requires_signature=t, signed=f` — proving
      the new instance was **not** silently absorbed under v4's inherited
      signature. Transaction rolled back; `SELECT count(*)` on all three
      tables confirmed 0 rows left in the shared local DB afterward.
    - `portal-risk-register.test.ts` has 8 pre-existing failures, confirmed
      **not caused by this build**: this session never touched that route,
      `mspRiskDecisionsTable`, or anything in its dependency chain, and the
      failure count/shape is identical whether run alone or alongside the new
      suite. Not reported as mine, per the standing #1765/#1772/#1774/#1777/
      #1778/#1781 note.
  - No `artifacts/portal` UI touched — scope stop honored.
  - Branch verified merged (`node scripts/dev-server/verify-branch-merged.mjs`
    — OK, `ae6101d1` an ancestor of `origin/main`, re-checked after a
    concurrent session advanced `origin/main` further). `git status
    --porcelain` clean.
