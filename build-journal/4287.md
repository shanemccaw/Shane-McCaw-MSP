# #4287 — MyArchitect: left panel top-level link click-to-navigate + separate expand affordance

- **Status:** ✅ DONE 2026-09-15T23:35:00Z
- **Scope:** platform (shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect@d78cd84

## Log
- 2026-09-15T23:21:00Z ⏳ IN FLIGHT — Blockers #4278/#4284 confirmed closed. Cloning shanemccaw/MyArchitect to locate left panel top-level link code and confirm current click/expand behavior before changing it.
- 2026-09-15T23:35:00Z ✅ DONE — Found `Shell/LeftReferencePanel.xaml`/`.xaml.cs` (literally the left panel, `MainWindow.xaml` column 0). Confirmed current behavior first: each top-level Portal Bookmark row already navigated on click (`BookmarkSelected` -> `OpenPortalTabAsync`); there was **no expand affordance at all**. Added a separate chevron `Button` per bookmark (column 0 of a new row `Grid`) that toggles a child-links panel, independent of the row's own click-to-navigate (column 1, unchanged). Added real per-portal child deep links (Active users, Billing, Users, Roles, Subscriptions, Devices, Mailboxes, Incidents, DLP, Teams users/meeting policies, etc.) via a new `ChildLinkSelected(PortalType, string subPath)` event, wired in `MainWindow.xaml.cs` through `TenantPortalUrls.GetDeepLink`. Fixed a real bug found in `GetDeepLink` while wiring it up: it naively concatenated `subPath` after the base URL, which mangled every portal whose base URL carries a `?tid=` query string (M365 Admin, Exchange, Security, Compliance, Teams) by appending the sub-path after the query instead of before it — the method was unused until now so the bug was latent. Rewrote it to insert path-segment sub-paths before the query string and append hash-fragment sub-paths after it. `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors, both before and after rebasing onto a concurrent unrelated commit (`75513fb` — left/right panel icon-bar collapse, no overlap with this change). Pushed to `shanemccaw/MyArchitect` main as `d78cd84`. No fixture/hardcoded customer data — the bookmark URLs/labels are real, static Microsoft admin-portal navigation targets, same category as the pre-existing top-level bookmark list. No genuine mid-build finding beyond the `GetDeepLink` bug, which was fixed directly since it was squarely in scope (the feature depends on it working). No live UI run in this session — WPF app not launched; verified via clean build only.
