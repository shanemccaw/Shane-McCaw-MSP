# #2096 — TickAsync normal queue-pickup path has same UI-thread blocking exposure as pre-fix Run Now

- **Status:** ⏳ IN FLIGHT 2026-09-06T15:33:35Z
- **Scope:** buildconsole

## Log
- 2026-09-06T15:33:35Z — Starting. Wrapping `await LaunchItem(item, buildSetExpected);` in `TickAsync`'s
  `foreach (var item in next)` loop (`QueueWatcherService.cs`, now ~line 1636 — the file has grown since
  the issue was filed at ~1167) in `Task.Run(...)`, mirroring #1881's `SafeLaunch` fix so a real
  multi-item pickup on one `DispatcherTimer` tick can't sequentially stutter the UI thread per item.
