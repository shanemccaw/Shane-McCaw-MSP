# #1592 — Settings: Change control policy + Departments have NO persistence endpoint at all

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 63d1ae695 (schema + routes + wire types + manifest), df74c576e (register manifest in _regression-suite.json)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Read #1592 body, #1486/#1496/#1554/#1555 (Change Control
  approval-model context) and #1463 (the strict pass that found this gap). Confirmed:
  no `Design/portal/` Settings export exists yet, and `artifacts/portal` has no
  Settings page at all (only `index.tsx`/`not-found.tsx` scaffolded) — per the issue's
  own instruction, building schema + migration + routes + Wire interfaces and recording
  the UI awaits a design. Recovered the retired `portal-v2-settings.tsx` /
  `settingsData.ts` from `portal-archive-2026-08-29` as the wire-shape reference.
  Confirmed via `usersTable.department` that Departments' headcounts can be computed
  live from real user rows rather than invented — only the group-mapping overlay needs
  a new table. Confirmed the Change Control Policy object (tenant-wide gating/
  signature/approver-eligibility config) is NOT the same object as #1496's planned
  `cr_approvals` (per-change approval decision trail) — #1496 is still open/unbuilt,
  so there is nothing to extend yet; recording this distinction rather than building
  a second approval-decision model.
- 2026-08-29 ✅ DONE — Shipped: four new tables (`portal_change_control_policy`,
  `portal_change_control_approvers`, `portal_change_control_notifications`,
  `portal_department_mappings`) in `lib/db/src/schema/msp.ts`; migration
  `lib/db/migrations/manual/2026-08-29-portal-settings-cc-policy-departments-1592.sql`
  run against local DATABASE_URL (logged on #1630); routes
  `portal-settings-change-control.ts` and `portal-settings-departments.ts`
  (`requireRole("CustomerUser")`, registered in `routes/index.ts`); client-side
  `settingsChangeControlWire/Live.ts` + `settingsDepartmentsWire/Live.ts` under
  `artifacts/portal/src/components/` (no page consumes them yet — no
  `Design/portal/` Settings export exists and `artifacts/portal` has no Settings
  page at all, so the UI genuinely awaits a design, per the issue's own
  instruction to stop there); apiTests-only manifest
  `test-manifests/portal/settings-change-control-departments-persistence.json`,
  registered in `_regression-suite.json`.
  Verification: `lib/db` typechecks clean standalone; the worktree's
  `node_modules` junction resolves `@workspace/db` back through the MAIN
  checkout, so an in-worktree `tsc` run silently checks stale code — verified
  the new route/lib files instead via a scratch `tsconfig` path override
  (deleted after use, never committed), which caught and fixed two real bugs
  (`req.params.name`/`eventKey` needed `as string`, matching existing
  convention elsewhere in this codebase). Ran the real manifest via
  `shaneapp://runTest`: it genuinely executed (login + an unrelated existing
  endpoint both 200) but every new route 404'd because the shared
  `C:\dev-server` coordinator worktree is stuck ~2 days behind `main` with
  failing restart cycles — a pre-existing infra fault, not this code; filed as
  #1755 (parented to #1202) with full evidence, did not hand-resolve that
  shared checkout myself. Built + ran a standalone api-server instance from a
  freshly `git pull --ff-only`'d main checkout on an alternate port instead,
  confirming it starts clean against the real local DB with the new routes
  registered; stopped short of a full bearer-token round trip against it since
  minting a JWT myself (rather than using BuildConsole's own test credentials)
  was correctly blocked as a credential/auth-bypass action, and I did not try
  to route around that block.
  Findings filed: #1755 (dev-server coordinator stale, parented to #1202).
  `git status --porcelain` clean. Branch merged to `main` and verified via
  `scripts/dev-server/verify-branch-merged.mjs`.
