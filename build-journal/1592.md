# #1592 — Settings: Change control policy + Departments have NO persistence endpoint at all

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** _(fill at DONE)_

## Log
- 2026-08-29 ⏳ IN FLIGHT — Read #1592 body, #1486/#1496/#1554/#1555 (Change Control
  approval-model context) and #1463 (the strict pass that found this gap). Confirmed:
  no `Design/portal/` Settings export exists yet, and `artifacts/portal` has no
  Settings page at all (only `index.tsx`/`not-found.tsx` scaffolded) — per the issue's
  own instruction, building schema + migration + routes + Wire interfaces and recording
  the UI awaits a design. Recovered the retired `portal-v2-settings.tsx` /
  `settingsData.ts` from `portal-archive-2026-08-29` as the wire-shape reference.
  Confirmed via `usersTable.department` that Departments' headcounts can be computed
  live from real user rows rather than invented — only the group-mapping overlay needs
  a new table. Confirmed the Change Control Policy object (tenant-wide gating/
  signature/approver-eligibility config) is NOT the same object as #1496's planned
  `cr_approvals` (per-change approval decision trail) — #1496 is still open/unbuilt,
  so there is nothing to extend yet; recording this distinction rather than building
  a second approval-decision model.
