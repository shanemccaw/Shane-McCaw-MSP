# #2529 — msp_risk_decisions.registerRef has no writer anywhere in the codebase

- **Status:** ✅ DONE 2026-09-04T03:20:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** b94725629f954304a48f834d694144dc16b41e14

## Log
- 2026-09-04T02:55:00Z ⏳ IN FLIGHT — Confirmed via live DB (`msp_risk_decisions` id=1, `register_ref` NULL) and grep that `registerRef` is read-only. Found 6 real insert sites for `msp_risk_decisions` across `m365-change-router.ts`, `portal-change-rejection.ts`, `remediation-tracker-risk-decline.ts`, `shadow-it-governance.ts`, `admin-rbd.ts`, `msp-rbd.ts`. Generation rule chosen: `RR-2026-<zero-padded id>` (matches the doc comment's own example `RR-2026-014`, and the existing `formatChangeRequestCode`/`rbdId` per-row-code convention), assigned via a shared helper right after insert since the row id isn't known until the insert returns.
- 2026-09-04T03:20:00Z ✅ DONE — Added `artifacts/api-server/src/lib/risk-register-ref.ts` (`formatRegisterRef` + `assignRegisterRef`, idempotent via `WHERE registerRef IS NULL`) and wired it into all 6 real writers. Wrote + ran `lib/db/migrations/manual/2026-09-04-backfill-risk-register-ref-2529.sql` against local `DATABASE_URL` — backfilled the one pre-existing NULL row (`id=1` → `RR-2026-001`), self-marked in `simulator_migration_runs`. Updated the now-stale "no writer" doc comment on the schema column. Verified: `pnpm exec tsc --noEmit` on `artifacts/api-server` clean (no errors); live-DB round trip (insert + simulated `assignRegisterRef` UPDATE, rolled back) confirmed the exact format (`RR-2026-063` for id=63) and the idempotency guard (second call = `UPDATE 0`). Read side (`portal-risk-register.ts`) needed no changes — it already reads `row.registerRef`, now backed by real data. `git status --porcelain` clean. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. No new findings filed — this build fully resolves #2529 as scoped.
