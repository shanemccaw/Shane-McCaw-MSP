# Git #3123 — MCP server /.well-known/* SPA-fallback 404 fix

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-07T00:00:00Z — Session start. Read #3123 body: root cause confirmed in src/server.mjs
  lines ~128-131 (SPA catch-all serves index.html for any unmatched GET, including
  mcp-remote's `/.well-known/oauth-authorization-server` discovery probe, breaking JSON parse
  on the client). Plan: add explicit `/.well-known/*` -> 404 JSON before the SPA fallback,
  investigate mcp-remote's real behavior on a 404 vs a `/.well-known/oauth-protected-resource`
  response, then verify live against the running local server with `mcp-remote`.
