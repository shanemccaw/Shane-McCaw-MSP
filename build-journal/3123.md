# Git #3123 — MCP server /.well-known/* SPA-fallback 404 fix

- **Status:** ✅ DONE 2026-09-07T21:50:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-07T00:00:00Z — Session start. Read #3123 body: root cause confirmed in src/server.mjs
  lines ~128-131 (SPA catch-all serves index.html for any unmatched GET, including
  mcp-remote's `/.well-known/oauth-authorization-server` discovery probe, breaking JSON parse
  on the client). Plan: add explicit `/.well-known/*` -> 404 JSON before the SPA fallback,
  investigate mcp-remote's real behavior on a 404 vs a `/.well-known/oauth-protected-resource`
  response, then verify live against the running local server with `mcp-remote`.
- 2026-09-07T21:50:00Z — ✅ DONE. Extracted `mcp-remote@0.8.3`'s real npm package
  (`chunk-6QT6R6YJ.js`) to read its own discovery source: `discoverOAuthProtectedResourceMetadata`
  explicitly special-cases `response.status === 404` as "not implemented" (caught, swallowed) and
  `discoverAuthorizationServerMetadata`'s discovery loop `continue`s past any 4xx -- so a real 404
  is sufficient; there is no product-decision reason to implement a real
  `/.well-known/oauth-protected-resource` response, since bearer-token-only auth already degrades
  cleanly once every well-known probe fails honestly. Live-verified this ALSO against the actual
  running server, which caught a real gap the issue's proposed single-path check missed: mcp-remote
  probes several `/.well-known/...` shapes, including path-relative ones like
  `/mcp/.well-known/openid-configuration`, which a `pathname.startsWith("/.well-known/")` check does
  not catch -- confirmed live returning 200 HTML (the app shell) instead of 404 before widening the
  check to `pathname.includes("/.well-known/")`. Fix: commit 9987420e on `web/shanes-life/src/server.mjs`.
  Full live verification: started the real server locally against the real local Postgres
  (`finances` db, per `%AppData%\ShanesSurvival\settings.json`), minted a real MCP token via
  `npm run issue-mcp-token`, ran the real `npx mcp-remote@0.8.3` bridge against
  `http://localhost:5099/mcp` with that bearer token. Confirmed via server request log: all real
  discovery-probe shapes (`/.well-known/oauth-authorization-server`,
  `/.well-known/oauth-protected-resource`, `/.well-known/oauth-authorization-server/mcp`,
  `/.well-known/openid-configuration/mcp`, `/mcp/.well-known/openid-configuration`) now return 404
  JSON; `mcp-remote` logged "Connected to remote server using StreamableHTTPClientTransport" (no
  JSON-parse crash); a direct JSON-RPC `initialize` + `tools/list` against the same endpoint
  returned the real `shanes-life` tool set (`whoami`, `capture`, `list_captures`, `create_entity`,
  etc.). Confirmed `/api/*` 404s unaffected (`GET /api/nope -> 404`) and the real SPA fallback still
  works for a genuine app route (`GET /shopping -> 200` HTML shell). Cleaned up: deleted the
  verification MCP token from the DB, deleted the scratch `.env` created for this run (gitignored,
  held a real local DB password -- not committed, not left on disk).
  Merged: `agent/3123-q1881` -> `origin/main` via `git push origin agent/3123-q1881:main`
  (`verify-branch-merged.mjs` confirms OK). Working tree clean.
