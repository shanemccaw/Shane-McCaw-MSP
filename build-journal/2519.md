# #2519 — ShaneBot: build the artifacts/portal chat shell + Active Card renderers (gate removed)

- **Status:** ✅ DONE 2026-09-03T11:35:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 6dfcb1d88c2e5575dc44b4fa6ca6a13529f6f2f9 (on origin/main)

## Log
- 2026-09-03T11:10:00Z ⏳ IN FLIGHT — recovered empty/unprovisioned worktree (Git #2118 recurrence, see memory), re-provisioned `C:\wt\2519-q1365` off `origin/main` and registered it. Starting investigation: reading `shanebot-engine.ts`, `support-chat.ts`, `POST /api/msp/support/chat`, #366's test manifest, and the contract pack §6 card-transience rule before touching `artifacts/portal`.
- 2026-09-03T11:35:00Z ✅ DONE — Built the real chat shell + Active Card renderer library in `artifacts/portal`: `/support` page (`pages/support.tsx`) wired to `POST /api/msp/support/chat` (real #361 content-block responses, stateless — client holds/resends the transcript) and `POST /api/msp/support/escalate`; `ChatBubble`/`SuggestedReplies`/`TypingIndicator`; the four real live card renderers (`InvoiceCard`/`SubscriptionCard`/`ScoreCard`/`DataAnswerCard`) dispatched via `ActiveCard.tsx` against `shanebot-engine.ts`'s real `BotCardType` shapes (contract pack §4), with runtime shape guards on the wire `data` (no fixture-as-fallback) and honest handling of a reply with/without a card (contract pack §8); cards treated as transient-per-turn only (§6), never assumed to survive reload. Added the shadcn ui primitives the surface needed (button/card/textarea/badge/avatar/separator), matching admin-panel's existing new-york conventions. Updated `test-manifests/chat/support-chat-active-cards.json` in place (discover-before-create) to the real `/support` route and current portal reality — the rebuilt portal (#1673) has no login page yet, so authenticated UI E2E isn't reachable through the browser for any portal page yet; uiSteps now assert the real, honestly-reachable state (route mounts, RequireAuth's "Sign in required" panel renders) and apiTests hit the real endpoint directly with a bearer token. Commit `6dfcb1d88c2e5575dc44b4fa6ca6a13529f6f2f9` pushed straight to `origin/main` (rebased twice over concurrent landings), fast-forwarded into the main checkout via `refresh-main-server.mjs`, portal frontend restarted and confirmed serving `/portal/support` (HTTP 200). **Verification gap, honestly reported:** hit a severe pre-existing environment corruption — 25 package directories in the shared local pnpm store extracted empty (`express`, `drizzle-orm`, `pg`, `zod`, `jsonwebtoken`, `pino`, `vitest`, etc.), blocking `api-server` from building/running locally at all. Hand-patched 2 of them (`esbuild`, `esbuild-plugin-pino`) per the documented single-package recipe to confirm scope, then stopped rather than whack-a-mole patching the remaining 23 (unbounded scope creep, unrelated to this issue) and filed it as **#2522** (parented under Feature #1616, board status AI Batter Up). `shaneapp://runTest` against the manifest could not complete real apiTests as a result (connection-refused on `localhost:8080`, confirmed pre-existing and unrelated to this build's code) — my own code is verified instead via a clean `tsc --noEmit` (0 new errors; the one pre-existing unrelated error in `copilot-journey` predates this session) and a clean, real `vite build` (`✓ built in 18.16s`) once the local vite-blocking half of the same corruption (`esbuild` under the root `.pnpm` store) was patched. `git status --porcelain` clean in both the worktree and the main checkout; `verify-branch-merged.mjs` confirms `agent/2519-q1365` (6dfcb1d8) is an ancestor of `origin/main`.
