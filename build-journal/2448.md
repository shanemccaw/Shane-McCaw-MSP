# #2448 — Offers and SOW Acceptance: contract pack

- **Status:** ✅ DONE 2026-09-03T03:44:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** ec3fe02e0 (bookend IN FLIGHT), fe12b6802 (contract pack) — rebased/pushed
  to `main` as 2a9eebf3d

## Log
- 2026-09-03T03:16:46Z ⏳ IN FLIGHT — verified all 7 named endpoints are real/live
  (`platform/agreement/current` in `platform-agreements.ts`; `portal/offers`,
  `portal/offers/:id`, `portal/offers/sse` in `portal-offers.ts`;
  `portal/presentations/:id`, `sow-document`, `sign` in `portal-presentations.ts`),
  writing `docs/offers-and-sow-acceptance-contract-pack.md` per #1642 pattern.
- 2026-09-03T03:44:00Z ✅ DONE — wrote
  `docs/offers-and-sow-acceptance-contract-pack.md` (11 sections: wire contracts for
  all 7 named endpoints + 2 pulled-in siblings, real enum unions, 3 findings, honest-
  empty contract, cross-surface edges, orphaned-endpoint check). Filed one real
  finding as **#2511** (`effectivePriceCents` response field on `POST
  /portal/presentations/:id/sign` actually holds dollars, not cents — verified via the
  scoped-SOW drift check's own `/100` conversion), parented as a sibling sub-issue of
  #2448's own Feature #1657, labeled `bug` + `area:msp-portal` + `iteration:1`,
  assigned to Shane, board status set to "AI Batter Up". Two lower-severity
  observations (the `platform/agreement/current` endpoint being an MSP↔Shane MSA/DPA
  concept the old portal-v2 mis-reused for a customer-facing display gate; a stale
  `"active"`-status comment) were flagged in the pack (§9b/§9c), not filed — no live
  breakage today. Rebased onto `origin/main` and pushed directly to `main`
  (2a9eebf3d) — `verify-branch-merged.mjs` confirms merged. `git status --porcelain`
  clean.
