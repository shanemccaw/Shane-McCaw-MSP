# #2713 — Home dashboard: real open/close rate crossing chart

- **Status:** ⏳ IN FLIGHT 2026-09-03T19:00:51Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-03T19:00:51Z ⏳ IN FLIGHT — confirmed #2711 (real data source) is DONE and merged
  (`1bec2d1be`, `GitHubIssueTimeSeriesService`). #2712 (sibling burndown chart) has no hard
  `blocked_by` edge on it — checked, only #2711 is a real dependency — and had not committed
  anything yet as of this check, so there is no landed diff to pull. Its live worktree
  (`C:/wt/2712-q1454`) has real uncommitted work in progress implementing the exact hand-drawn
  Canvas/Polyline convention #2710 documents (card header + ⟳ refresh button + ScopeText +
  Canvas w/ SizeChanged redraw + SummaryText + fail-closed EmptyText). Read that real
  in-progress code directly (not committed, but real and on disk) and am reusing its exact
  structure/naming/fail-closed conventions for the two-line open/close rate chart rather than
  inventing a second approach, per the standing instruction. Building now in HomeView.xaml /
  HomeView.xaml.cs, Column 1 (a different insertion anchor than #2712's Column-2 edit, to avoid
  a collision when both land).
