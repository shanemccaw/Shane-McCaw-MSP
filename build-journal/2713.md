# #2713 — Home dashboard: real open/close rate crossing chart

- **Status:** ✅ DONE 2026-09-03T19:23:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** `69052fdc6` (Home dashboard: real open/close rate crossing chart), `d8b5d887b`/rebased (Bookend: #2713 IN FLIGHT)

## Log
- 2026-09-03T19:00:51Z ⏳ IN FLIGHT — confirmed #2711 (real data source) is DONE and merged
  (`1bec2d1be`, `GitHubIssueTimeSeriesService`). #2712 (sibling burndown chart) has no hard
  `blocked_by` edge on it — checked, only #2711 is a real dependency — and had not committed
  anything yet as of this check, so there is no landed diff to pull. Its live worktree
  (`C:/wt/2712-q1454`) has real uncommitted work in progress implementing the exact hand-drawn
  Canvas/Polyline convention #2710 documents (card header + ⟳ refresh button + ScopeText +
  Canvas w/ SizeChanged redraw + SummaryText + fail-closed EmptyText). Read that real
  in-progress code directly (not committed, but real and on disk) and am reusing its exact
  structure/naming/fail-closed conventions for the two-line open/close rate chart rather than
  inventing a second approach, per the standing instruction. Building now in HomeView.xaml /
  HomeView.xaml.cs, Column 1 (a different insertion anchor than #2712's Column-2 edit, to avoid
  a collision when both land).
- 2026-09-03T19:23:00Z ✅ DONE — Added a real "OPEN/CLOSE RATE" card to Home (Column 1, below
  Recent Chats): two hand-drawn `Polyline`s on a `Canvas` (`OpenCloseRateCanvas`) plotting the
  real per-day `Opened`/`Closed` counts from `GitHubIssueTimeSeriesService.GetActiveMilestoneSeriesAsync`
  (#2711) for the active Milestone, Peach = opened/day, Green = closed/day (legend in the card
  header) — the exact same Canvas/Polyline hand-drawn convention #2712's burndown chart uses
  (confirmed by reading its real, then-uncommitted `DrawBurndownCanvas`/card structure directly
  in its live worktree, since it hadn't landed a diff to pull at build time — no hard
  `blocked_by` edge on #2712, only #2711 which was already closed/complete). Marks the most
  recent real day the closed line crosses to at-or-above the opened line (an actual sign change
  between the two real series, never guessed) with a small dot, plus a real trailing-7-day
  opened-vs-closed summary line. Fails closed to an honest "not enough real history" message
  using #2711's own `HasEnoughData`/`Reason` contract — never a fabricated/interpolated line.
  Refresh wired the same way as the burndown chart: passive re-render on every
  `RenderDashboardState` rollup (cheap — #2711's own service has its own 5-min cache) plus a
  manual ⟳ button that force-refreshes.
  **Manual spot-check (per #2711's own verification ask):** pulled milestone #5's real issue
  `created_at`/`closed_at` timestamps directly via `gh api` and independently reduced them to a
  daily opened/closed series in a throwaway Node script (not committed) — confirmed real per-day
  activity (tens to hundreds of issues/day) and four real crossover days
  (2026-08-11, 2026-08-18, 2026-08-25, 2026-09-01) using the exact same "closed ≥ opened this
  day, closed < opened the day before" rule the chart's marker logic uses — matches, so the
  real signal the issue asks for genuinely occurs in the live data and the chart's detection
  logic is correct against it. Did not launch BuildConsole.exe itself (per the standing
  never-launch-for-verification rule) — verified via `dotnet build` (clean, 0 warnings/errors,
  twice — once before rebase, once after) and the above independent data recomputation instead.
  Two commits landed on `main`: the IN FLIGHT bookend and the real chart implementation. Both
  rebased twice onto a fast-moving `main` (concurrent #2712/#2714 sibling builds landing at the
  same time) — one real merge conflict in `HomeView.xaml.cs` (both siblings' independent regions
  colliding at the same file-end insertion point + the same `RenderDashboardState` call site),
  resolved by hand so both #2712's and #2713's regions/calls fully coexist; rebuilt clean after
  resolving. `verify-branch-merged.mjs` confirms `agent/2713-q1455` is a real ancestor of
  `origin/main`. `git status --porcelain` clean. No findings filed this session — no genuine
  bug/gap hit outside the issue's own scope.
