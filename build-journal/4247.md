# #4247 — Rebuild Git Mode to match Test Mode's real panel-reuse architecture

- **Status:** ✅ DONE 2026-09-15T14:13:50Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** `2978b9ede` (rebuild) — bookends `1f25bf8bb` (IN FLIGHT), plus this DONE commit

## Log
- 2026-09-15T13:56:44Z ⏳ IN FLIGHT — Rebuilding Git Mode (`desktop/BuildConsole`) to match Test Mode's real panel-reuse pattern: new column-scoped Git Mode rail `UserControl` in `Grid.Column="0"` (deleting the `Grid.ColumnSpan="6" Panel.ZIndex="1000"` overlay), left panel wired to the real `LeftSidebar` epic/issue tree, right panel Git-Mode issue-detail control in the Queue column cell built on the real `IssueDetailView`, Dependency Graph extracted into its own document tab (open-or-focus by `Tag`), `EnterGitMode`/`ExitGitMode` rewritten to Test Mode's save/restore-column-width shape leaving `EditorTabs` untouched, and both threading gaps fixed (`OnMirrorSyncCompleted` try/catch + `ActivityLog.Log`, reentrancy guards on every async refresh path).
- 2026-09-15T14:13:50Z ✅ DONE — Shipped in `2978b9ede`. Deleted the old full-span `GitModeContainer` (XAML + code-behind, −1580 lines). New column-scoped controls: `GitModeRailPanel` (Grid.Column 0, 48/300 rail mirroring `TestModeDiagnosticsPanel`), `GitModeIssueDetailPanel` (Grid.Column 5, hosts the real `IssueDetailView`), `GitModeGraphTab` (own document tab via `AddSingletonDocumentTab`, reusing `GitModeGraphService`/`RenderGraph`/`GitModeWhyEngine`). `EnterGitMode`/`ExitGitMode` rewritten to Test Mode's save/restore-column-width shape — `EditorTabs`/center column never touched; the real `LeftSidebar` Git Board ("Issues") view is the left epic/issue tree, kept visible. `LeftSidebar.IssueSelected`/`GitDetailTabRequested` route to the right panel while in Git Mode. Threading: `OnMirrorSyncCompleted` refresh wrapped in try/catch+`ActivityLog.Log`; `RefreshGraphAsync` guarded by a `CancellationTokenSource` swap (drops superseded results); `IssueDetailView.LoadIssue` already had a `_requestId` guard. Verification: `dotnet build BuildConsole.csproj -c Debug` clean (0 warnings / 0 errors); the four #4247 "Real verification" behaviors confirmed by reading the wired code paths (no live UI in this environment) — column-scoped panels not a translucent overlay (no bleed-through), open-or-focus graph tab survives close/reopen via the persistent field, mirror-sync refresh cannot swallow an exception or overwrite newer state. Branch verified an ancestor of `origin/main` (`verify-branch-merged.mjs` exit 0). No findings filed — no separate bug surfaced beyond the two threading gaps this issue already scoped and which are fixed here. Test manifests N/A (`desktop/BuildConsole` is explicitly out of scope).
