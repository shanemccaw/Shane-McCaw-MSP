# #4193 — Permissions engine: migrate the first real dual-gated route(s) to requireAccess()

- **Status:** ✅ DONE 2026-09-15T13:09:15Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 3d440c623 (migration + live-db parity test), 24f82d4b7 (IN FLIGHT bookend)

## Log
- 2026-09-15T12:39:32Z ⏳ IN FLIGHT — Leaf 3 of #1704. Blocker #4192 confirmed CLOSED (2026-09-15T12:36:07Z). Finding routes carrying both a capability gate and requireTierFeature, migrating the first real batch onto requireAccess(), live-verifying RBAC-deny / entitlement-deny / allow per route. BuildConsole buildId 2840.
- 2026-09-15T13:09:15Z ✅ DONE — Migrated the first real dual-gated routes onto `requireAccess()` (3d440c623, on origin/main, verify-branch-merged.mjs OK).

  **Which routes, and why these.** All 20 `requireTierFeature` route call sites also carry a `requireCapability("ladder.*")`, so every one of them is dual-gated. This first batch covers the three modules #4193 names: 7 READ routes, each now `requireAuth, requireAccess("ladder.customer-user", <module>)`:
  - `GET /api/portal/risk-register` (risk_register), `GET /api/portal/policy-decisions` (policy_decisions) — portal-risk-register.ts
  - `GET /api/portal/poams`, `/poams/available-checks`, `/poams/:poamId` (poams) — portal-poams.ts
  - `GET /api/portal/ownership`, `/ownership/events` (ownership) — portal-ownership.ts

  `requireAuth` is mounted explicitly because `requireCapability` ran it internally and `requireAccess` does not. The remaining 13 dual-gated routes (message-center, pii-governance, policy-register, remediation checklist/tracker ×3, runbooks ×2, security-plan, sops ×2) belong to #1698's sweep.

  **Externally-observable differences (all deliberate, from #4192's design).**
  - 403 adds `error.details: { basis: "rbac", capability }`.
  - 402 adds `basis`, `requiredTier` and `upgradePath`.
  - Every denial now writes an audited `warn` on channel `auth`.
  - A tier-read failure now answers **503 unavailable** instead of requireTierFeature's old **500**. This is the only status change, and it happens only on an infrastructure failure, never on a decision.

  Apart from these, status codes and legacy body fields are unchanged for every decision.

  **Verification (honest).**
  - **Live over real HTTP, before vs after.**
    - Setup: an isolated instance of the real `app.ts` (no workers or pollers) from this worktree on :8099, compared against the running :8080 dev server, which still runs the retired two-gate build because Build Set PermissionsEngineFirstMigration deferred its restart.
    - Principals: four synthetic `zz-test-4193-*@example.invalid` users with real bcrypt passwords and real `POST /api/auth/login` tokens: Free on a Premier org (RBAC deny), Customer with no subscription (entitlement deny), Customer on Foundation (mixed), Customer on Premier (allow).
    - Result: **28/28 (principal, route) pairs matched** on status and legacy body. That is 7×403, 12×402 (requiredTier foundation/premier, read from the live catalog), and 9 requests that reached the handler on both servers: 200s, plus a 404 for a non-existent POA&M id and a 400 for /events without params.
    - Audit rows on channel auth: exactly 7 rbac and 12 entitlement.
    - Cleanup: all synthetic rows deleted; residue re-queried at 0 users / 0 tenants.
  - **`requireAccess-first-batch.live-db.test.ts` (committed).** Mounts the real route modules and the retired chain side by side against local Postgres: **31/31 green**, zero residue.
  - **Negative control.** Weakening `/poams/:poamId` to `requireAccess("ladder.free", riskRegister)` made that test fail: 4 failures, exactly that route's cases plus the audit check. The route was restored afterwards.
  - `tsc --noEmit` on api-server: clean.
  - Unit tests (requireAccess, requireCapability-keys, portal-tier-features, portal-risk-register, portal-policy-decisions): 66/66.
  - #4192's requireAccess.live-db test: 6/6.

  **Mid-build incident.**
  - What happened: BuildConsole's sweep removed this build's live worktree `C:/wt/4193-q2840` at 12:57:59Z ("orphaned agent worktree (PID dead or inactive)"), after rescuing uncommitted work into a9a353c8b on `rescued/4193-q2840-2026-09-15T12-56-45-099Z`.
  - Recovery: moved into a self-provisioned `C:/wt/4193-r2` (`--owner-pid` = the live claude.exe), re-verified there, and committed as 3d440c623. The rescue branch and commit are now superseded; I left them in place.
  - Finding: filed **#4244**, parented under #1202. It is a BuildConsole worktree-lifecycle bug, not a permissions finding, so it is not under #1704.

  **Findings filed:** #4244. No migrations. No pre-existing dirty files.
