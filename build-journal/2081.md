# #2081 — Git Board hover popup: show BOTH blocked-by and blocks directions

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Commit(s):** 41d69b457 (implementation), rebased+pushed to main as c158a628a

## Log
- 2026-08-31 ⏳ IN FLIGHT — Read #2081's body/comment and #2061's landed bookend. Audited
  `IssueHoverPopup`/`CreateIssueHeader`/`BuildIssueHoverPopupContent` in
  `Controls/LeftSidebar.xaml(.cs)`: today it shows a state badge, title, snippet, and ONE
  direction only — a plain "Waiting on #N — Title" line sourced from `GitIssue.BlockedByNumber`
  (the sweep's cached first-open blocker). No reverse "what blocks on this" lookup exists
  anywhere in the app — grepped `BuildQueuePanel*.cs` for chain/highlight logic (#2030's
  visual-chain work) and confirmed no `build-journal/2030.md` bookend exists — #2030 is not
  landed. Confirmed via live `gh api` calls that GitHub's real REST issue-dependencies API has
  a genuine reverse endpoint mirroring `blocked_by`:
  `GET /repos/{owner}/{repo}/issues/{n}/dependencies/blocking` — verified non-empty response on
  #1269 (which the platform's own `issue_dependencies_summary.blocking` field also reports as 2).
  This is the real data source — a single REST call per issue, same shape as
  `GitHubApiClient.GetBlockedByAsync`, no need to scan all open issues.
  Plan: add `GitHubApiClient.GetBlockingAsync`, fetch both directions live (async, on hover) in
  `BuildIssueHoverPopupContent`, render two capped relationship lists with real titles, widen the
  popup (XAML) and wrap its body in a ScrollViewer so it stays a hover popup, not a panel.
- 2026-08-31 ✅ DONE — Implemented as planned:
  - `GitHubApiClient.GetBlockingAsync(number)` — new, calls the real
    `GET .../issues/{n}/dependencies/blocking` endpoint (confirmed live via `gh api` before
    writing code: non-empty on #1269, matching its own `issue_dependencies_summary.blocking`
    count of 2). Mirrors `GetBlockedByAsync`'s shape/error handling exactly.
  - `LeftSidebar.xaml.cs` — `BuildIssueHoverPopupContent` now builds a "relationships" panel for
    every OPEN issue (not just blocked ones): seeded synchronously from the sweep's cached
    `BlockedByNumber`/`Title` (so a blocked issue isn't blank while the live fetch is in flight),
    then `LoadIssueRelationshipsAsync` fetches BOTH directions live and replaces it with two
    capped (6, "+N more") real-title lists — "🔒 Blocked by" and "⛔ Blocks". A generation counter
    (`_issueHoverPopupGeneration`, bumped on every show/hide) guards against a slow response from
    a previously-hovered issue landing in the currently-hovered issue's popup. The existing
    Blocked-overrides-build-action precedence (#2061) is unchanged — a blocked issue still skips
    the Dispatch/Cancel/Retry action area, it just now also shows the full relationship picture
    above that early return.
  - `LeftSidebar.xaml` — `IssueHoverPopup` widened (`MaxWidth` 380→440, `MinWidth` 260→280) and
    given a `MaxHeight="520"` + `ScrollViewer` around the body (same pattern as
    `ManifestStepsPopup`/`ManifestStepsFlyoutList` elsewhere in this file) so a long dependency
    chain scrolls instead of growing the popup into a full panel.
  - Confirmed no reverse "what blocks on this" lookup existed anywhere before this build — grepped
    `BuildQueuePanel*.cs` for chain/highlight logic and confirmed no `build-journal/2030.md`
    bookend exists (#2030's Build Queue chain-highlighting is not landed), so this is genuinely
    new, not a duplicate of existing logic.
  **Verification:** `dotnet build BuildConsole.csproj` clean (0 errors/0 warnings) after the
  implementation, and clean again after rebasing onto latest `origin/main`. Did not launch a live
  BuildConsole.exe instance — same reasoning as #2061 (live PAT/DB/queue-watcher side effects,
  and manual mouse-hover isn't drivable from this shell session). Per CLAUDE.md,
  `desktop/BuildConsole` is out of scope for test manifests, so none was written.
  **Merge:** rebased local `agent/2081-q1071` (`41d69b457`) onto `origin/main`
  (`e9dc7f186`), pushed directly to `main` as `c158a628a`; `verify-branch-merged.mjs` confirms
  ancestor. Working tree clean (`git status --porcelain` empty) before writing DONE.
