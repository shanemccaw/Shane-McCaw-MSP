# #1560 — SOPs/Runbooks: Seed the lifecycle procedure set

- **Status:** ✅ DONE
- **Scope:** platform

## Log
- 2026-08-30: Investigated. #1556 (unify) and #1559 (execution hook) are already DONE. IAM-01
  (Offboard = leaver) and IAM-02 (Onboard = joiner) already exist in the testbed seed set
  (admin-testbed.ts seed-sops route). Missing from the lifecycle set named in #1552's resolution:
  mover, promote, demote, de-VIP. De-VIP is scoped per msp-vip-classifications.ts's own header:
  the decision itself is a "told" API call (#1552), not a runbook — what's missing is the
  PROPAGATION runbook (#1548's territory) that withdraws VIP-tier protections once a de-VIP has
  been told.
- 2026-08-30: ✅ DONE — real hash `19bd071dd` (work commit `e539cc5d4`, IN FLIGHT bookend
  `dd1bfc78f`), verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0).
  Added 4 new lifecycle SOP seed definitions to `artifacts/api-server/src/routes/admin-testbed.ts`'s
  `SEED_SOPS` (the same testbed upsert mechanism IAM-01/02/03 already use — `msp_sops` is keyed on
  `(msp_id, sop_id)`, upsert-only, never a blanket delete):
  - **IAM-04 — Move to a New Department or Role** (mover): update job title/department, reassign
    manager, swap department group membership (Graph writes), manual app-access confirmation.
  - **IAM-05 — Promote to Elevated Access**: add to elevated group + assign directory role (Graph
    writes), manual MFA confirmation + manager notification.
  - **IAM-06 — Demote from Elevated Access**: remove elevated group + directory role, revoke
    sign-in sessions (Graph writes), manual manager notification.
  - **IAM-07 — De-VIP: Withdraw VIP Protections**: manual confirmation the platform record already
    shows de-VIP (this SOP propagates a decision already told via `/api/msp/vip-classifications`,
    per that route's own scope note — it does not re-decide it), then Graph writes removing the VIP
    security-group membership and the VIP Conditional Access exclusion, manual manager notification.

  Each procedure is wholly one thing per #1554 (no decision-making step mixed with a propagation
  step, no consequential access change folded into a routine one).

  No schema change — `msp_sops`/`msp_sop_runs` already carry everything these need (#1556, #1559).
  No migration file, so no #1630 line.

  **Verified against the real DB**: every automated step's `graphEndpoint` string was run through
  the real `parseGraphEndpointShape`/`parseLooseObjectBody` parsers from `sop-workflow-graph.ts`
  (copied inline for the check, not reimplemented logic) — all 11 automated steps across the 4 new
  SOPs parse and would materialize as real `graph_write_operation` nodes; none silently degrade to
  manual. Then inserted all 4 rows directly against local `DATABASE_URL` (mspId 1, the real testbed
  MSP — confirmed the only `msp_id` present in `msp_sops`) using the exact upsert the route
  performs, and confirmed via `psql`: ids 45–48, correct `automation_type`/step counts (5, 4, 4, 4).
  `tsc --noEmit` clean on `admin-testbed.ts` (59 pre-existing errors elsewhere, none mine, matching
  #1765/#1772/#1774/#1777/#1778/#1781).

  **Not built, and deliberately out of scope**: the run-triggering UI/page for these SOPs
  (`artifacts/portal` has no page for this module — SCOPE STOP) and the actual propagation wiring
  from a "told" de-VIP call to auto-firing IAM-07 (that's #1548's territory per
  `msp-vip-classifications.ts`'s own header — this build seeds the runbook `#1548` will invoke, it
  does not build the invocation).

  No findings filed — no new gap discovered beyond what #1548/#1559 already record.
