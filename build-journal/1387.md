# Git #1387 — Pillar hero honest-null (port Overview's NoScanValue pattern)

- **Status:** ✅ DONE
- **Scope:** platform
- **Build:** 644
- **Commits:** `1c0821f95` (6 pillar pages), manifest note bump follow-up

## Log
- ⏳ IN FLIGHT — Porting Overview's NoScanValue/NoScanDataState honest-null pattern to the 16 `?? XXX_HERO.field` fallbacks across the 6 remaining pillar hero sections (Security, Governance, Compliance, Licensing, Adoption, Health): score, delta, statusLabel/severity, drift trend verdict.
- ✅ DONE — Replaced all 16 in-scope fixture fallbacks with the shared no-scan-data seam, matching `portal-v2-overview.tsx`:
  - **Score ring** (all 6): `const score = live.score` (no fixture); `hasScanValue()` gates `ringOffset` so the ring draws no arc when unscored; the numeral is now `<NoScanValue value={score} color=… />` → real value or a muted "—".
  - **Delta** (all 6): `live.delta?.text ?? NO_DATA_DASH`, color `live.delta?.color ?? NO_DATA_INK`.
  - **Status label / severity** (Security, Governance, Compliance): `sev?.label ?? NO_SCAN_DATA_LABEL` (Compliance's `sev ? … : NO_SCAN_DATA_LABEL`).
  - **Drift trend verdict** (Security only — the only page with a verdict prop): `pillarTrendVerdict(live.history) ?? NO_SCAN_DATA_LABEL`.
  - Count: Security 4, Governance 3, Compliance 3, Licensing 2, Adoption 2, Health 2 = **16**. Governance's `live.history ?? GOV_HERO.history` (and Security's `SEC_HISTORY`) feed the *sparkline chart geometry*, not any of the four hero fields — deliberately left as-is, same as the issue's "verdict = narrative text" scope.
- **Verification:** `tsc -p tsconfig.json --noEmit` clean — 0 errors in any touched file (2 pre-existing errors remain in untouched `SowScreen.tsx` / `PortalV2Shell.tsx`). The existing `test-manifests/portal/portal-v2-pillar-heroes.json` covers the **live/scored** case, which is unchanged (NoScanValue renders the real value identically); its note + `lastVerifiedAgainstCommit` were bumped. The honest-EMPTY branch only fires on a genuinely never-scanned tenant, which the scored testbed account can't exercise — code-verified, not faked against a scored tenant.
