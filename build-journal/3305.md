# #3305 — Lists room: shelf of list cards, not one long page

- **Status:** ⏳ IN FLIGHT 2026-09-09T02:31:46Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-09T02:31:46Z ⏳ IN FLIGHT — Read Design/design_handoff_shanes_life/README.md's "the
  Lists room" section and the prototype's `d.showLists`/`d.showList` markup + logic
  (`listCards`, `listGroups`, `LIST_HUES`/`listHue`, capture grammar `nlst`/`atl`/`wl`/`rl`).
  Confirmed backend (`src/core/lists.mjs`, `/api/lists*`) already has generic named-list CRUD;
  the real gap is the UI (`viewLists` in `public/app.js` still renders one long page of
  full-width group cards, not the shelf grid + per-list screen) and the capture grammar
  (`src/core/capture-grammar.mjs` had no "new list:" / "gifts list: …" / "add … to the … list"
  rules). Added `named_list_add`/`new_list` capture-grammar rules + a `person_note` guard so a
  "… list" name doesn't collide with it; `lists.listAllListNames` for prefix-match
  candidates. Pure test suite (`node src/core/capture-grammar.test.mjs`) passing 39/39.
  Next: shelf grid UI (2-col, hue-tinted cards), list-detail screen, seed-data migration for
  the extra default lists reaching existing saved state.
