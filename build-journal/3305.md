# #3305 — Lists room: shelf of list cards, not one long page

- **Status:** ✅ DONE 2026-09-09T02:45:10Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** f6fc7f533 (capture grammar), a3438e1d0 (shelf UI + list detail screen)

## Log
- 2026-09-09T02:31:46Z ⏳ IN FLIGHT — Read Design/design_handoff_shanes_life/README.md's "the
  Lists room" section and the prototype's `d.showLists`/`d.showList` markup + logic
  (`listCards`, `listGroups`, `LIST_HUES`/`listHue`, capture grammar `nlst`/`atl`/`wl`/`rl`).
  Confirmed backend (`src/core/lists.mjs`, `/api/lists*`) already has generic named-list CRUD;
  the real gap is the UI (`viewLists` in `public/app.js` still renders one long page of
  full-width group cards, not the shelf grid + per-list screen) and the capture grammar
  (`src/core/capture-grammar.mjs` had no "new list:" / "gifts list: …" / "add … to the … list"
  rules). Added `named_list_add`/`new_list` capture-grammar rules + a `person_note` guard so a
  "… list" name doesn't collide with it; `lists.listAllListNames` for prefix-match
  candidates. Pure test suite (`node src/core/capture-grammar.test.mjs`) passing 39/39.
  Next: shelf grid UI (2-col, hue-tinted cards), list-detail screen, seed-data migration for
  the extra default lists reaching existing saved state.
- 2026-09-09T02:45:10Z ✅ DONE — Shipped the real shelf grid (`viewLists` in `public/app.js`):
  2-column hue-tinted `.list-card`s (`listHue()` -- five named hues + FNV-1a hash fallback,
  ported verbatim from the prototype's `LIST_HUES`/`HUES`/`fnv`), up to 3 open items inline +
  "+N more", a real "{n} left/all done/empty" pill, "{n} done" footer, "new" tag
  (`created_by === "claude"`, the room's existing on-the-fly signal), and a dashed "+" card that
  pre-fills the capture box. Each card opens its own real screen (`#/list/:id` ->
  `viewListDetail`): open items on top, a collapsed "Done · {n}" section (Clear, tap to un-do),
  and a real "Find in {list}" input for a list over 10 items. `roomHeader()` gained an optional
  `sub` line (additive, every other caller unaffected) for the shelf's own "{n} lists · {m} open"
  subtitle. Capture grammar: `new_list` ("new list: Camping" / "new list: Camping: headlamp") and
  `named_list_add` ("gifts list: speaker for DJ", "add tent stakes to the camping list", real
  prefix-match against `lists.listAllListNames` -- a new export, includes Shopping so
  "shopping list: …" still finds the real run -- create-on-miss otherwise); a genuine
  `person_note` collision (a "… list" name would otherwise be claimed by that colon rule first
  and lost to FALLBACK) is closed with a one-line guard, not a reorder. Verified: pure suite
  `node src/core/capture-grammar.test.mjs` 39/39; a disposable-account, real-DB script exercised
  every new rule end to end against the local `finances` database (new list creation, first-item
  capitalization, prefix-match reusing the same list rather than duplicating it, Shopping
  inclusion, person_note regression) then deleted the account, leaving no residue; server boot +
  static-asset smoke test on an alternate port (5551) confirmed no startup/route errors and that
  the edited `app.js`/`app.css` serve correctly. **Deliberately not built:** the issue's own
  scope item 6 ("seed-data migration ... default seed lists on load") -- the design's own
  `LISTS_SEED` is five lists of fabricated demo content (fake movie/book titles, fake gift
  ideas), and seeding that into Shane's real account is exactly what CLAUDE.md's "never invent
  data to display" HARD RULE forbids. Lists already come into being organically (Watch/Books via
  the existing `watch_or_read_list` rule, any other name via the new `new_list`/`named_list_add`
  rules) -- no fabricated seed needed for the shelf to "read" once Shane actually uses it. No new
  findings filed this session beyond this documented scope decision. Working tree clean; branch
  verified merged into `origin/main` (`verify-branch-merged.mjs`).
