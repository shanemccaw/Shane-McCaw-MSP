# #3253 — mirror sync fires a real event so open Batter Up / AI Batter Up views auto-update

- **Status:** ✅ DONE 2026-09-08T17:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-08
- **Commit(s):** 41464073d (real work), f014f7bdf (IN FLIGHT bookend)

## Log
- 2026-09-08T17:30:22Z ⏳ IN FLIGHT — implementing per Shane's REVISED scope (redirect comment,
  2026-09-08T17:06:43Z, supersedes the original toggle+second-timer plan in the issue body):
  a real `GitHubIssueMirror.SyncCompleted` static event raised only on a genuinely successful
  `MaybeSyncAsync`/`SyncAsync`, subscribed by `BatterUpPanel`/`AiBatterUpPanel` on Loaded and
  unsubscribed on Unloaded, refreshing themselves via their own existing local-mirror
  `RefreshAsync()` — zero new GitHub calls, zero new timer, zero new Settings toggle.
- 2026-09-08T17:40:00Z ✅ DONE — shipped exactly per the revised scope:
  - `Services/GitHubIssueMirror.cs`: added `public static event Action? SyncCompleted`, raised
    only when `SyncAsync` completes with `summary.Ok == true` (never on a skip/no-op/failure),
    invoking each subscriber independently via `GetInvocationList()` so one throwing view can't
    swallow a sibling's refresh. Doc comment states plainly it fires off whatever background
    thread the caller (`QueueWatcherService`'s watcher tick) runs on, not the UI thread.
  - `Controls/BatterUpPanel.xaml.cs` / `Controls/AiBatterUpPanel.xaml.cs`: subscribe in
    `Loaded`, unsubscribe in `Unloaded` (same lifecycle idiom `DispatchPanel`'s countdown timer
    already uses) — a closed document tab's persistent instance (see
    `MainWindow.BatterUpTabs.cs`, both panels are hoisted singleton fields, never destroyed)
    stops repainting off-screen and the static event never outlives an open view. The handler
    marshals via `Dispatcher.InvokeAsync` before calling the panel's own existing
    local-mirror `RefreshAsync()` — the exact same call a manual refresh already makes.
  - Real, honest scope check done per the issue: audited `BatterUpQueueService.RefreshAsync`
    and `AiBatterUpQueueService.RefreshAsync` — both already read
    `GitHubIssueMirror.TryGetByBoardStatusAsync` mirror-first (Git #3134) and only fall back to
    a live GitHub walk on a genuine mirror miss/never-synced, so calling either panel's
    `RefreshAsync()` from this event triggers zero new GitHub calls on the common (mirror-hit)
    path — identical behavior to today's manual-refresh button, just triggered reactively.
  - `ChatDockService` (also named in the issue as "e.g. ... chat-dock enrichment") is a
    stateless static builder invoked per-render by whatever hosts the Floating Chat Window
    side dock, not a persistent subscribable view with its own refresh lifecycle — no
    `Loaded`/`Unloaded` hook exists to subscribe from, so it was left out of this event's
    subscriber set. Noted here rather than silently skipped; a future session can revisit if
    that dock ever grows its own persistent instance.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors.
    Deliberately NOT test-manifest work — `desktop/BuildConsole` is out of scope for test
    manifests per CLAUDE.md; a clean build is this session's real verification.
  - Branch merged: `node scripts/dev-server/verify-branch-merged.mjs` → OK, `41464073d` is an
    ancestor of `origin/main`. `git status --porcelain` clean at finish.
