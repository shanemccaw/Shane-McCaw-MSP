# #2991 — Auth Core: wire all 6 real screens to the real endpoints

- **Status:** ✅ DONE 2026-09-06T18:55:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** c2a066ecd (bookend, IN FLIGHT), 51b9f078a (real work)

## Log
- 2026-09-06T18:40:50Z ⏳ IN FLIGHT — building the 6 real Auth Core screens (Sign-In,
  Account Setup, Forgot Password, Reset Password, MFA Challenge, Sign-In Help) against
  `Design/portal/design_handoff_full_site/screens/Auth *.dc.html` and
  `docs/auth-core-contract-pack.md`, into the fresh `artifacts/portal` scaffolding.
- 2026-09-06T18:55:00Z ✅ DONE — All 6 screens wired to their real endpoints
  (`POST /api/auth/login`, `POST /api/auth/mfa/totp/challenge`,
  `POST /api/auth/mfa/bypass`, `POST /api/auth/forgot-password`,
  `POST /api/auth/reset-password`, `GET /api/auth/setup-context`,
  `POST /api/auth/setup-password`, `POST /api/portal/sign-in-help/ticket`,
  `GET /api/status`). MFA challenge implemented as an inline second phase of the
  Sign-In page rather than a separate route (it depends on login's own short-lived
  `mfaToken`). Routes registered in `App.tsx` outside `RequireAuth`/`PortalLayout`.
  Added the missing shadcn `ui/input`/`ui/label`/`ui/tabs`/`ui/alert` primitives and a
  shared `AuthPageShell` (forces the app's own `dark` theme tokens, which land within a
  few percent of the design's literal navy/blue palette — no second palette invented).
  No fixture data anywhere; `lib/auth-api.ts`'s `fetchPlatformStatus` and
  `lib/auth-context.tsx`'s 7 new methods are the only data sources, all real.
  **Verification:** `tsc -p tsconfig.json --noEmit` clean, `vite build` clean. Started a
  worktree-local dev server (port 5299, proxying `/api` to the real live api-server on
  :8080) and confirmed all 5 routes serve real 200s with my actual page source (grepped
  served `.tsx` module content for real design copy — not just a generic SPA
  fallback). Round-tripped 4 real backend calls through that same proxy
  (`forgot-password` → `{ok:true}`/200, bad-email `sign-in-help/ticket` → 400, bad
  credentials `login` → 401, bad-token `setup-context` → 404) and all matched the
  contract pack exactly. `shaneapp://runTest` was **not** run in-session: per its own
  documented behavior (and this repo's own prior manifests), it hits the *deployed*
  build, which does not carry these routes until this commit is merged into the
  dev-server checkout by BuildConsole's own post-build step — running it now would
  only prove the pre-existing 404, not this work. Test manifests written/updated:
  `test-manifests/auth/sign-in.json` (renamed from the stale
  `login-page-restyle.json`, which asserted portal-v2 selectors/routes that no longer
  exist), `test-manifests/portal/sign-in-help.json` (rewritten for the new unslugged
  route), `test-manifests/auth/password-reset-guard.json` (uiSteps added for the new
  forgot/reset-password pages), `test-manifests/auth/account-setup.json` (new).
  Registered in `_regression-suite.json`.
  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before
  writing this. Working tree clean.
  **Finding filed:** #3053 (parented to Feature #1648, `bug` label, board status "AI
  Batter Up") — `portal-sign-in-help.ts`'s `SIGN_IN_HELP_ISSUES` label copy + its
  docblock's design citation are stale, pointing at a retired `.dc.html` file and
  carrying older label text than the current real design export. No customer-facing
  impact (the client sends only `issueKey`), filed as a stale-comment/copy-drift
  finding.
