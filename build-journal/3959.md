# #3959 — KFM check + write automation migrate to Settings Catalog (configurationPolicies)

- **Status:** ✅ DONE 2026-09-14T02:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 447097b9f (IN FLIGHT bookend), 774ba9f47 (intune-not-applicable-400 wire signature), bd39f860d (KFM Settings Catalog migration + tests)

## Log
- 2026-09-14T02:16:54Z ⏳ IN FLIGHT — Pulled #3959 body + BUILD comment. Confirmed via Microsoft Learn + two independent real-world sources (Packt Intune Cookbook GitHub script, rozemuller.com) that `deviceManagement/configurationPolicies` (Settings Catalog) is the real migration target, and that the KFM settingDefinitionId has TWO live variants (`onedrivengsc` vs `onedrivengscv2` category prefix) — so the write must resolve it live, not hardcode either string. Live-probed the testbed tenant (mccawsoft2): both `configurationPolicies` and `configurationSettings` return `400 "Request not applicable to target tenant."` (Intune not provisioned there, same root cause as #2185's 503 on the old surface) — added a new `intune-not-applicable-400` wire signature to `service-availability.ts` so this doesn't regress from a graceful `service_not_configured` resolution to a raw error. Proceeding to author the check + write-pack migration SQL against Microsoft's documented/community-confirmed real shapes; cannot live-fire the actual write on this tenant (same posture as #3800/#2185).
- 2026-09-14T02:35:00Z ✅ DONE — Authored and RAN `lib/db/migrations/manual/2026-09-14-kfm-settings-catalog-migration-3959.sql` against local `DATABASE_URL`: re-pointed `devices:kfm-configuration`'s read to `beta/deviceManagement/configurationPolicies?$expand=settings` (countWhere on `settingInstance.settingDefinitionId` instead of displayName name-match), archived the two `groupPolicyConfigurations`-era templates (`action.create-kfm-grouppolicy-configuration`, `action.set-kfm-definition-values` — status='archived', not deleted), and added a single new template `action.create-kfm-settings-catalog-policy` that deep-inserts the KFM setting (parent choice + dropdown + tenant-ID textbox children) directly into one `configurationPolicies` POST, with the real settingDefinitionId resolved live via a resolve-then-write step (server-filtered on `contains(id,'onedrivengsc_kfmoptinnowizard')`, client-disambiguated via `rootDefinitionId == ""` to pick the parent over its children) rather than hardcoded, since Microsoft serves two live category-prefix variants of this setting. Verified the whole pipeline (resolve step → `interp()` substitution → final Graph body) against the real stored DB row with a standalone script — output matches the confirmed real-world working example exactly. Added unit tests: `resolve-then-write.test.ts` (2 new cases, both v1/v2 category-prefix variants) and `monitor-executor.test.ts` (2 new cases for the new countWhere mapping, including a nested-children-don't-double-count case) — 232/232 tests pass across the 3 touched files (`service-availability.test.ts`, `resolve-then-write.test.ts`, `monitor-executor.test.ts`), `tsc --noEmit` clean. A full-suite `vitest run` afterward showed ~75 unrelated failing files (CRM, billing, retention, webhooks, PCC routes — none importing anything this build touched); several other sessions were landing commits on `main` concurrently during this run, consistent with shared local-DB contention rather than a regression from this change — not filed as a new finding since it wasn't isolated/reproduced as a real, attributable bug. Could NOT live-fire the actual write or confirm the real resolved settingDefinitionId against the testbed tenant: Intune is not provisioned on `mccawsoft2.onmicrosoft.com` (confirmed live, tracked separately under #1867) — same posture #3800/#2185 already documented for this tenant/surface family. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
