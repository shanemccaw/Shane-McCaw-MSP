# #3682 — Audit Log (MSP Console): regenerate the contract pack (real customerId filter landed since #3671)

- **Status:** ✅ DONE 2026-09-11T19:05:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** `1d8fb018e` (docs/msp-console/audit-log-msp-console-contract-pack.md regenerated)

## Log
- 2026-09-11T18:55:00Z ⏳ IN FLIGHT — full line-by-line regeneration of
  `docs/msp-console/audit-log-msp-console-contract-pack.md` per the #1642 pattern.
  Confirmed the pack is stale on more than just the `customerId` filter noted in the
  issue body: the RBAC #2460 cutover (`requireRole("MSPAdmin")` → `requireCapability
  ("ladder.msp-admin")`, landed 2026-09-10) also postdates the pack's §0/§0-footer
  citations, and §6's GraphProbeModal finding (dead-endpoint + fabricated-response)
  is now fully fixed by #3450/#3560 (both closed COMPLETED) — the pack still presents
  it as an open, unresolved finding. Re-verifying every section against current code
  + live local Postgres before rewriting.
- 2026-09-11T19:05:00Z ✅ DONE — Regenerated
  `docs/msp-console/audit-log-msp-console-contract-pack.md` wholesale (commit
  `1d8fb018e`). Corrected the customerId-filter gap the issue named (§1.2), the
  requireRole→requireCapability RBAC #2460 gate-mechanism drift (§0/§1.1, allow set
  unchanged: {MSPAdmin, PlatformAdmin}), and re-labeled §6's GraphProbeModal finding
  as resolved (#3450/#3560 both COMPLETED, verified directly against
  `GraphProbeModal.tsx` — real `adminFetch` call, correct `/api/msp/audit` preset).
  All other sections (writer catalog §2, vocabularies §3, live-data counts §4,
  legacy-table distinction §5) re-verified against the current 154-line route file
  and live local Postgres (535 rows) and found substantively accurate. No new
  out-of-scope finding hit worth filing — the one gap noted (MCP `get_audit_logs`
  tool doesn't expose `mspId`/`customerId`) is documented inline in §2.2 as a minor
  surface-completeness note, not filed as a separate issue (nothing is broken by
  its absence today). No product code, schema, or UI changed — docs-only pass.
  Verification: `verify-branch-merged.mjs` OK, `git status --porcelain` clean.
