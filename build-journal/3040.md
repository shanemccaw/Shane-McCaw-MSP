# #3040 — Ownership / RACI: wire the customer read surface

- **Status:** ✅ DONE 2026-09-06T20:00:13Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** f61ad5327 (feature), 27c642e9a + 0c3aa8bb6 (merge into main)

## Log
- 2026-09-06T19:46:36Z ⏳ IN FLIGHT — Wiring `GET /api/portal/ownership` (§1a of
  docs/ownership-raci-contract-pack.md) into a new read-only Ownership / RACI page
  in `artifacts/portal`, per the real Design export at
  `Design/portal/design_handoff_full_site/screens/Ownership RACI.dc.html`. Write
  routes (assign/reorder/accept/decline/delegations/rows) are explicitly out of
  scope — sibling issue #3041 covers that.
- 2026-09-06T20:00:13Z ✅ DONE — Shipped `pages/ownership.tsx` +
  `lib/ownership-{types,api,matrix,visuals}.ts` +
  `components/ownership/{PersonChip,OwnershipMatrixSection,MinePlaceCard,
  SourcesCard,OverlayExtrasCard}.tsx`. Route registered in `App.tsx`
  (`/ownership`), `moduleNav.ts`'s `raci` row now points at it, and
  `RiskRow.tsx`'s two Ownership/RACI links point at the real page instead of
  coming-soon. New test manifest `test-manifests/portal/ownership.json`
  (registered in `_regression-suite.json`) — the existing
  `portal-v2-ownership.json` covers the retired portal-v2 UI (dead route,
  different testids) and was left alone per CLAUDE.md's portal-v2 retirement.
  **Verification:** `tsc --noEmit` clean, `vite build` clean (2230 modules, no
  errors); the live local dev api-server (port 8080) mounts the route (401
  auth-required, not 404, confirming the path is real and reachable); local
  `DATABASE_URL` confirms the contract pack's documented honest-empty overlay
  still holds (0 `portal_ownership_assignments`/`_delegations`/`_rows`, 2
  tenants) so the page's empty/gap states are exercising real conditions, not
  invented ones. The full UI manifest was NOT run via `shaneapp://runTest` this
  session — that protocol hits the **deployed** build, and this page had not
  yet been merged/redeployed at the time of writing; it's ready for a
  post-deploy run. Merged into `main` (0c3aa8bb6) via two rebase/retry cycles
  (`main` moved twice from concurrent Remediation Tracking/Security Plan
  builds); `verify-branch-merged.mjs` confirms ancestor-of-main. Working tree
  clean at finish. Filed #3061 (parented under Feature #1487, "AI Batter Up")
  for a stale test-manifest note on `risk-register.json` discovered along the
  way — unrelated to this issue, not fixed here.
