# #1793 — Config state: survey every PowerShell cmdlet that works app-only through ps-execution

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `bb21f94d8` (bookend), `d4f3fe4e3` + `.dockerignore` fix (survey harness),
  `f67791179` (schema/migration/driver/doc-generator/route), `a59faa471` (README),
  `5a2926ea0` (budget clamp), `884a6756e` (classifier fix), `0e0af7403` (request deadline),
  `3a40d3211` (deny-list), `98c1c9f9b` (generated document)
- **Migration:** `lib/db/migrations/manual/2026-08-30-ps-capability-survey-1793.sql` — run against
  the local `DATABASE_URL` in-session, verified with `\d`. Logged on the #1630 checklist.
- **Issues filed:** #1852, #1853, #1854, #1855

## Log

- 2026-08-30 ⏳ IN FLIGHT — Verifying the ps-execution auth path (#1400 / #1614 / #1483) via a live
  `get-connection-info` round trip before surveying anything.

- 2026-08-30 ⏳ IN FLIGHT — **All three preconditions verified live, independently.**
  - **#1400** — `13658fc4f`, `790971b38`, `04d0b8b3c` all confirmed ancestors of `origin/main`.
  - **#1614** — `az ad app show 9ea2e409-d1b9-422a-8451-02fa0b98d1c3` returns **both** thumbprints:
    `BC8F5C26BA1FA641C15264278EDCDA8F0F481906` and `251BDCD5895FB127F44648F29999F3B1820CA2D3`
    (start `2026-08-29T03:57:31Z`).
  - **#1483** — SP `1c640fe8-1c23-4510-8235-9ee6938d8f8b` `memberOf` returns **Global Reader**
    (`roleTemplateId f2ef992c-3afb-46b9-b7cf-a126ee74c451`) plus `PS-Execution-DLP-Access`.
  - **Live round trip** — `shaneapp://executeCmdlet` `get-connection-info` against testbed
    `c4c814d4-3afe-441e-9145-62461d0a4fd3`: `ok:true`, `State: "Connected"`,
    `CertificateAuthentication: true`, `TokenStatus: "Active"`, `AppId: 9ea2e409-…`.

- 2026-08-30 ⏳ IN FLIGHT — Harness built. `services/ps-execution/survey.ps1` + six code-owned
  catalog entries. The request carries only `Skip`/`Take`/`BudgetSeconds` **integers** — never a
  cmdlet name — so #209's what-code-runs-stays-code-owned boundary is fully intact. Read-safety
  fails closed on four gates read from the command's own metadata.

- 2026-08-30 ⏳ IN FLIGHT — **Run #1 discarded, marked `failed` in the DB with honest notes.** The
  local api-server was restarted mid-run by the dev-server coordinator; the driver of the time
  recorded 36 transport failures as per-cmdlet `error` rows — exactly the false-negative table this
  issue warns is worse than no table. Driver fixed to retry transport failures and never record
  them, and to mark an unfinishable run `failed` rather than `completed`.

- 2026-08-30 ⏳ IN FLIGHT — **Run #3 abandoned, marked `failed`.** `Get-ScopeEntities` wedged the
  container's parent listener twice (not just its own batch — the whole service stopped answering
  until a manual `az containerapp revision restart`). Escalated onto #1852 with the evidence.
  Added a hard per-request deadline to the driver (`0e0af7403`) and deny-listed the cmdlet
  (`3a40d3211`) so the survey cannot repeatedly take down a shared container.

- 2026-08-30 ✅ DONE — **Survey run #4 completed live**, revision `ca-ps-execution-dev--survey1793d`,
  testbed `mccawsoft2.onmicrosoft.com` (`tenants.id = 1`), 02:17:43 → 02:27:42.
  **1,166 cmdlets enumerated and classified across all three session types:**

  | Session | Enumerated | `ok` | `access_denied` | `cmdlet_unavailable` | `not_supported_app_only` | `error` | `not_attempted` |
  |---|---:|---:|---:|---:|---:|---:|---:|
  | `compliance` | 126 | 55 | 0 | 0 | 0 | 17 | 54 |
  | `exchange` | 365 | 139 | 0 | 3 | 2 | 42 | 179 |
  | `teams` | 675 | 143 | 5 | 0 | 0 | 14 | 513 |
  | **all** | **1166** | **337** | **5** | **3** | **2** | **73** | **746** |

  Shipped: `services/ps-execution/survey.ps1` (code-owned survey, four read-safety gates),
  six catalog entries, `ps_capability_survey_runs` / `ps_capability_survey_results` (Drizzle +
  manual migration, run locally), `scripts/src/ps-capability-survey.ts` (driver),
  `scripts/src/ps-capability-survey-doc.ts` (doc generator),
  `GET /api/simulator/ps-execution/capability-survey` (read route), and
  `docs/powershell-capability-survey.md` — 1,453 lines, generated from the DB, never hand-written.

  **Verification, all real:**
  - Route verified live against the completed run: `?session=teams&status=ok&cmdlet=CsTeamsMeeting`
    → `ok=True run=#4 status=completed rev=ca-ps-execution-dev--survey1793d filtered=7`, returning
    real captured shapes (`Get-CsTeamsMeetingBrandingPolicy` → `Identity`,
    `NdiAssuranceSlateImages`, `MeetingBackgroundImages`, `MeetingBrandingThemes`).
  - `tsc --noEmit` clean for both `artifacts/api-server` and `@workspace/scripts` (the api-server's
    pre-existing unrelated baseline failures are untouched; zero errors in files this session
    changed).
  - Migration verified against local Postgres with `\d ps_capability_survey_results`.
  - Every `.ps1` parse-checked with `[Parser]::ParseFile`.

  **Honest gaps, each filed as its own issue:**
  - **#1852** — no per-cmdlet timeout in the container; one hanging cmdlet destroys the whole
    request's results and (escalated evidence) wedges the parent listener until a manual restart.
  - **#1853** — 130 of 337 working cmdlets returned zero items, so no output shape was captured.
    Directly affects #1795's resource model.
  - **#1854** — all 15 `Test-*` cmdlets deliberately unexecuted (fail-closed: `Test-Mailflow`
    sends real mail), so their app-only support is unmeasured.
  - **#1855** — the dev-server coordinator restarts the api-server mid-flight with no signal;
    it cost this session run #1 and part of run #3.

  **Non-goal respected:** no `monitor_checks` row was created or modified. `SELECT executor_type,
  count(*) FROM monitor_checks` is unchanged at graph 138 / powershell 17 / dns 1 /
  sharepoint-admin 1.

  **No test manifest written** — the Test Coverage rule scopes manifests to
  `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`; this session touched
  `services/ps-execution/`, `lib/db/`, `scripts/` and `artifacts/api-server/` only.

  **Operational note for Shane:** during recovery from the container wedge I ran
  `Stop-Process -Id 31924` believing it was my own survey driver. The dev-all log attributes that
  PID to the **api-server**. Whether it had already crashed on its own (the log shows
  `[api-server:8080] exited with code 4294967295`) or my kill ended it, I got that identification
  wrong. The api-server was restored via the sanctioned
  `node scripts/dev-server/request-restart.mjs --agent 1793-q876`, which merged this work into the
  dev-server checkout (server HEAD `9107a888`) and completed the `config-state-survey` build set's
  single restart. It is confirmed serving.
