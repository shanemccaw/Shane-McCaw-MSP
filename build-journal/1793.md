# #1793 — Config state: survey every PowerShell cmdlet that works app-only through ps-execution

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>
- **Migration:** `lib/db/migrations/manual/2026-08-30-ps-capability-survey-1793.sql` (run against local `DATABASE_URL`)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Verifying the ps-execution auth path (#1400 / #1614 / #1483) via a live `get-connection-info` round trip before surveying anything.
- 2026-08-30 ⏳ IN FLIGHT — **All three preconditions verified live, independently.**
  - **#1400** — `13658fc4f`, `790971b38`, `04d0b8b3c` all confirmed ancestors of `origin/main`.
  - **#1614** — `az ad app show 9ea2e409-d1b9-422a-8451-02fa0b98d1c3` returns **both** thumbprints:
    `BC8F5C26BA1FA641C15264278EDCDA8F0F481906` (`CN=ShaneMcCawMSP-MT-App-2026`) and
    `251BDCD5895FB127F44648F29999F3B1820CA2D3` (`CN=ShaneMcCawMSP-MT-App`, start `2026-08-29T03:57:31Z`).
  - **#1483** — SP `1c640fe8-1c23-4510-8235-9ee6938d8f8b` `memberOf` returns **Global Reader**
    (`roleTemplateId f2ef992c-3afb-46b9-b7cf-a126ee74c451`) plus `PS-Execution-DLP-Access`.
  - **Live round trip** — `shaneapp://executeCmdlet` `get-connection-info` against testbed
    `c4c814d4-3afe-441e-9145-62461d0a4fd3`: `ok:true`, 1 item, `State: "Connected"`,
    `CertificateAuthentication: true`, `AppId: 9ea2e409-…`, `TokenStatus: "Active"`, 70.9s round trip.
  The auth path is genuinely live; the survey is not producing false negatives from a broken connect.
- 2026-08-30 ⏳ IN FLIGHT — Survey harness built and deployed. `services/ps-execution/survey.ps1` +
  six new code-owned catalog entries (`survey-list-commands-*` / `survey-probe-*`). The request carries
  only `Skip`/`Take`/`BudgetSeconds` integers — never a cmdlet name — so #209's
  what-code-runs-stays-code-owned boundary is intact. Read-safety fails closed on four gates from the
  command's own metadata (`Get-*` only; no `SupportsShouldProcess`; a zero-mandatory-parameter set;
  not on the unbounded deny list). Persistence: `ps_capability_survey_runs` /
  `ps_capability_survey_results` (migration run locally, verified with `\d`), driver
  `scripts/src/ps-capability-survey.ts`, doc generator `scripts/src/ps-capability-survey-doc.ts`,
  read route `GET /api/simulator/ps-execution/capability-survey`.
- 2026-08-30 ⏳ IN FLIGHT — **Run #1 discarded as untrustworthy and marked `failed` in the DB with
  honest notes.** The local api-server was restarted mid-run by the dev-server coordinator (a
  concurrent build completed); the driver of the time recorded those transport failures as
  per-cmdlet `error` rows — 36 of them. That is exactly the false-negative table this issue warns is
  worse than no table. Driver fixed: a transport failure is now retried and never written as a row,
  and a run that cannot finish is marked `failed` rather than `completed`. Run #2 is the clean run.
