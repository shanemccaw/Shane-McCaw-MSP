# #1168 — Operational feature gating by tier — mapping + the unconditional-creation rule

- **Status:** ✅ DONE 2026-09-05T02:34:07Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** e017f365c (worktree branch), rebased and pushed to main as 523af076f

## Log
- 2026-09-05T02:13:24Z ⏳ IN FLIGHT — Investigating type_attributes.includedFeatures current state and the 6 already-built customer-scoped portal routes (portal-sops.ts, portal-change-control.ts, portal-risk-register.ts, portal-message-center.ts, portal-ownership.ts, portal-remediation.ts) to apply the creation-unconditional/visibility-only tier gate.
- 2026-09-05T02:34:07Z ✅ DONE — Found all 10 module routes already exist on disk (not just the 6 named in the issue): policy-decisions, risk-register (+ its policy-decisions view), runbooks (+ hold-window events), remediation-tracker (+ verification-guide), remediation-checklist, sops (+ sop-runs), message-center, ownership (+ events), security-plan, pii-governance. Change Control was excluded from this pass — Shane's own 2026-08-21 comment on #1168 already settled it as a real, separately-priced add-on (`requireAddOnEntitlement`, #1173), not a tier-bundled feature, and it is already correctly wired that way.
  Built `lib/portal-tier-features.ts` (`resolveCustomerIncludedFeatures`, `hasTierFeature`, `requireTierFeature`) — resolves a customer's active Monitoring-tier purchase's real `services.type_attributes.includedFeatures` via the same join chain `msp-launch-control.ts` already used, fails closed to `[]` (nothing included) on no active subscription.
  Ran `lib/db/migrations/manual/2026-09-05-portal-tier-included-features-1168.sql` against local Postgres myself: populates real `includedFeatures` for all 12 Foundation/Growth/Premier `monitoring_tier` services rows (Foundation/Growth were `[]`, Premier had only 2 unrelated report-name strings — all preserved, merged not overwritten). Cumulative ladder per #1168's confirmed mapping: Foundation (policy_decisions, risk_register) < Growth (+ runbooks, remediation_tracking, sops_runbooks, message_center) < Premier (+ change_control doc-only, ownership, security_plan, pii_governance).
  Wired `requireTierFeature` onto the customer-facing READ handler of all 9 tier-bundled modules' routes, immediately after the existing role-floor `requireRole(...)` and before the handler — every write/creation path (POST/PATCH/PUT) is untouched, per #1168's "creation unconditional" rule. Confirmed none of the 9 already had an accidental tier check baked in (all only had role-floor checks, a different axis).
  Verified: `pnpm typecheck` clean. Added `portal-tier-features.test.ts` (12 tests, fail-closed coverage) and registered it in `vitest.config.ts`'s include allowlist. Two pre-existing route tests (`portal-risk-register.test.ts`, `portal-remediation-tracker.test.ts`) had their db-mock select queue collide with the new middleware's extra queries — fixed by mocking `portal-tier-features.ts` as a pass-through in those two files (their own concern is scoping/business logic, not the gate, which now has its own dedicated test). Full allowlisted vitest suite: 4416 passed, 18 pre-existing failures all traced to a missing `DATABASE_URL` env var in that particular run (unrelated to this change, zero `portal-*` failures in the failure list).
  Branch rebased onto `origin/main` and pushed directly to `main` (523af076f) per Git conventions. `git status --porcelain` clean. No findings filed — no genuine bug/gap discovered outside this issue's own scope.
