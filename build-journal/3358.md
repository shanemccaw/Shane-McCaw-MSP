# #3358 — Git Board tree core fetch (ListBoardIssuesAsync) never migrated to bt_issue_mirror

- **Status:** ✅ DONE 2026-09-09T23:53:11Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Commit(s):** 9acec15dd (code + migration), 533ef4437 (IN FLIGHT bookend)

## Log
- 2026-09-09T23:25:00Z ⏳ IN FLIGHT — Migrating the Git Board tree's core open-issue fetch (`LeftSidebar.xaml.cs` ~1908 `ListBoardIssuesAsync(Open)`) off the live GraphQL call onto the local `bt_issue_mirror`, matching #3134's Batter Up/AI Batter Up pattern. Will audit the mirror schema against every field the Git Board tree consumes and extend the mirror rather than fall back to live for any gap. Keeping an explicit manual force-live-resync path.
- 2026-09-09T23:53:11Z ✅ DONE — Shipped in commit `9acec15dd` (on `origin/main`, branch verified merged via verify-branch-merged.mjs, exit 0).

  **What shipped:**
  - **Migration** `lib/db/migrations/manual/2026-09-09-bt-issue-mirror-git-board-fields-3358.sql` — extends `bt_issue_mirror` with every field the Git Board tree renders (audited against `GitBoardIssue`'s consumed fields via a full-file field-usage map): `body`, `milestone_title/number`, `parent_number`, `parent_milestone_number`, `sub_issue_count/completed/percent`, `child_issue_numbers`, `database_id`. Adds `bt_milestone_mirror` (per-milestone open/closed header counts — the once-live `GetMilestonesAsync` call the board fired in the SAME try block, so it aborted the board too when the circuit opened). Nulls `bt_issue_mirror_sync_state.last_full_sync_at` so the next `MaybeSyncAsync` tick runs a full walk to populate the new columns; every mirror reader falls back to live in the brief interim (fail-closed via `HasUsableDataAsync`). **Ran against local `DATABASE_URL` in-session** (idempotent; verified columns + table). Appended to release-gate #1630 for Replit/staging.
  - **`GitHubIssueMirror.SyncAsync`** (full walk) now persists those enriched fields off the same `ListBoardIssuesAsync` result it already fetches, plus a best-effort milestone-counts fetch/upsert (+ stale-milestone delete). Incremental pass unchanged (its light REST `since=` shape carries none of the new fields → they default on insert / preserve on update, corrected by the full walk — the same full-walk-authoritative tradeoff `board_status` already uses; no code change needed there).
  - **`GitHubIssueMirror.TryGetBoardIssuesAsync` / `TryGetMilestoneInfosAsync`** — reconstruct the exact `GitBoardIssue` / `GitHubMilestoneInfo` shapes from the mirror (state normalized back to UPPERCASE `OPEN`/`CLOSED` to match the live GraphQL path — several callers compare `State == "OPEN"` exactly). Fail-closed to null on miss/never-synced/error.
  - **`LeftSidebar.PopulateGitTrackerBoardAsync`** — reads mirror-first; a manual Refresh (`forceFresh`) forces a full LIVE re-sync into the mirror first then reads it (guaranteed-fresh escape hatch); falls back to the live `ListBoardIssuesAsync(Open)` walk only when the mirror has never synced. The 🟢 Done-chip closed snapshot (line ~7173) and the ALL-states rollup fetch are deliberately left as-is (see finding + scope).

  **Verification (BuildConsole is out of scope for test manifests — clean build + real SQL validation, per CLAUDE.md):**
  - `dotnet build BuildConsole.csproj -c Debug` → **Build succeeded, 0 Warning(s), 0 Error(s)**.
  - Both mirror read SELECTs run cleanly against the real local schema.
  - Both upsert paths (issue INSERT + DO UPDATE, milestone upsert + stale-delete) validated against the real schema inside a **rolled-back transaction** — column/param alignment correct, new fields persist + read back correctly, **no data pollution** (confirmed 0 rows after ROLLBACK).
  - Did NOT launch BuildConsole.exe (single-instance mutex can courier into Shane's live session — memory `never-launch-buildconsole-exe-for-verification`). Live circuit-open behavior is covered by the fail-closed logic + the forced-full-walk migration reset, both verified structurally.

  **Working tree:** clean (`git status --porcelain` empty apart from this bookend). Only my 3 files touched.

  **Finding filed:** **#3365** (parented under Feature #1789, label `bug`, milestone v1.1, board status "AI Batter Up") — the Git Board's ALL-states rollup fetch (`GetAllIssuesAsync` → `ListBoardIssuesAsync(All)`, `LeftSidebar.xaml.cs:~2131` / `GitHubIssueTimeSeriesService.cs:207`) is STILL a live full open+closed GraphQL walk not covered by the mirror (the mirror only tracks the open set + recently-closed, so it can't serve ALL-states without its own design). Degrades gracefully but remains real rate-limit pressure on the same surface.
