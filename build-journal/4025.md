# #4025 — Projects: project-closure customer sign-off loop has no customer-facing page

- **Status:** ✅ DONE 2026-09-15T04:20:40Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 040d64a56 (main)

## Log
- 2026-09-15T04:05:01Z ⏳ IN FLIGHT — Investigated: backend (`POST /api/portal/projects/:id/closure`, #4058) and the `closure` field on `GET /api/portal/projects/:id` (#1739) already exist. `artifacts/portal/src/pages/project-detail.tsx` already renders a "dead end" banner for the closure request. Building the real sign-off ceremony (SignaturePad + permissionGranted checkbox + optional feedback) into that existing banner, mirroring `RiskDocumentPanel.tsx`'s sign pattern.
- 2026-09-15T04:20:40Z ✅ DONE — Shipped the customer sign-off ceremony: `SignaturePad` (reused, no new component) + a permission checkbox + optional feedback textarea in `project-detail.tsx`'s existing closure card, posting to `POST /api/portal/projects/:id/closure` via a new `useSignProjectClosure` mutation in `projects-api.ts`/`projects-types.ts`. Added a real signed-confirmation state for when `closure.signedAt` is set. Removed the stale "no way to sign off here yet" copy and updated the page's own honesty ledger + header doc comment. `tsc --noEmit` clean on both `artifacts/portal` and `artifacts/api-server`; `vite build` clean. **Live-verified** end-to-end against an isolated `:8099` api-server instance built from this worktree (the shared `:8080` dev-server doesn't have this branch yet — see the live-verify-topology memory): inserted a synthetic `zz-test-closure-4025@example.invalid` client user + a completed project, minted a JWT locally (same `JWT_SECRET`), confirmed `GET .../projects/:id` returns `closure: null`; confirmed `POST .../closure` 404s with no closure-request row; inserted the row; confirmed `GET` reflects it (`signedAt: null`); confirmed `POST .../closure` returns 200 with a real DB-updated row (`signedAt` set, `signerUserId` correct); confirmed a second `POST` returns 409 ("This closure has already been signed"). Deleted all three synthetic rows (`project_closures`, `projects`, `users`) afterward — nothing synthetic left in the DB. Updated `test-manifests/portal/projects.json` with a real `AUTH-GATE-3` apiTest for the new route (tagged `4025-closure-signoff`) and its own honest note on how the full round trip was verified; `lastVerifiedAgainstCommit` set to this build's real commit. No genuine findings to file this session — the backend (#4058) was already complete and correct; this build was a clean frontend wire-up. `git status --porcelain` clean; `verify-branch-merged.mjs`/direct `git push origin HEAD:main` confirms `040d64a56` is on `origin/main`.
