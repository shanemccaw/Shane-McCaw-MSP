# #1759 — Change Control: wire approval policy into the approval path

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** aa0689194 (work), plus this DONE bookend commit

## Log
- 2026-08-29 ⏳ IN FLIGHT — Making `portal_change_control_policy` authoritative: `requiredStages()` floored by `required_signatures`; separation-of-duties gated on `require_separate_approver`; approver eligibility derived live from `users.can_approve_changes`; dropping `portal_change_control_approvers`.
- 2026-08-29 ⚠ NOTE — This session (buildId 851) is a retry. A prior attempt (buildId 849) committed equivalent work as `89c1098e2` on the abandoned worktree/branch `agent/1759-q849` (never merged to origin/main). This session landed the work cleanly on `agent/1759-q851` with a single migration file. `agent/1759-q849` is left untouched.
- 2026-08-29 ✅ DONE — Landed on origin/main as `aa0689194` (branch verified ancestor of origin/main; `git status` clean).
  - `requiredStages(changeClass, risk, policySignatures?)` floors the human stage count with `policy.required_signatures`: `max(policySignatures, riskDerived)`; risk floors it; standard stays 0; null → prior behaviour byte-for-byte.
  - `loadApprovalPolicy(customerId)` reads `required_signatures` + `require_separate_approver` (defaults null/true); threaded through `materializeApprovalsForChange`, `recordApproval`, `recordRejection`, and the register read (`toWire`/`canApproveNow`). Separation of duties now gated on `require_separate_approver`.
  - `GET /api/portal/settings/change-control` returns `eligibleApprovers` derived live from `users.can_approve_changes`. Dropped `portal_change_control_approvers` (table + `PUT .../approvers` route + wire field + `CC_APPROVER_BANDS`/`CcApproverBand`/`isCcApproverBand`). Band concept dropped — `cr_approvals` keys off `approver_role`, not bands.
  - `enforce_freeze_calendar` left in place, marked CURRENT-unenforced (no #1500 freeze calendar to read). `enabled`/`gated` untouched (write-path, #1497).
  - Migration `lib/db/migrations/manual/2026-08-29-drop-cc-approvers-table-1759.sql`. Precondition: `SELECT count(*) FROM portal_change_control_approvers;` = **0** before drop. Run against local DATABASE_URL (self-marks). Line appended to #1630 for release-time run.
  - **#1757 resolved by deletion, not patch** — deriving eligibility from `can_approve_changes` removes the second store #1757 wanted cross-validated.
  - Verification: 22/22 unit tests pass (new floor cases). Settings read + approve/reject gate proven to agree on who may sign via a rolled-back real-data read (user 37 flagged → both return `u37`; rollback → 0). `artifacts/portal` tsc clean. api-server project tsc has only pre-existing/environmental errors (unrelated files + stale `@workspace/db` dist, e.g. `executorRunId` present on main); none in changed files, none reference #1759 symbols.
  - Findings filed: none warranted — the api-server tsc errors are pre-existing on main / stale-dist artifacts, not regressions introduced here.
