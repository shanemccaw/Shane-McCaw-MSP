# #1488 — Portal New Design: Runbooks — contract extraction pack

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 25f9e316 (rebased/merged into main; original commits 105f81b26,
  cc2591b45)


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1488`, but #1488 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1488 is complete.
>
> #1488's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Extracting the Runbooks contract pack (step 3 of #1578,
  per #1577) to `docs/runbooks-contract-pack.md`. Architected jointly with #1493
  (SOPs) — the pack states where the two contracts overlap per that architecture.
  READ-ONLY: no product code, schema, or UI changes. Wire contracts cited to
  file:line, CURRENT vs DECIDED marked, real enum unions only. Known-wrong
  contract carried in deliberately: `portal_runbook_steps.checked` is per-runbook,
  not per-run, so a cycle reset wipes last cycle's completion — no run history
  (#1557). Any real live endpoint with no page consumer gets a sub-issue filed
  at pack time, per the #1485 standing convention.
- 2026-08-29 ✅ DONE — Pack shipped at `docs/runbooks-contract-pack.md`. All eight
  endpoints in `portal-runbooks.ts` documented CURRENT vs DECIDED with file:line
  citations; §2 restates the #1493 joint architecture and its overlap with this
  module (one procedure definition + run record, recurrence as a schedule that
  spawns runs, per #1556/#1557); real enum unions extracted from the Drizzle
  schema (§3); cross-surface hold-window↔CR edge cross-checked against the
  already-shipped Change Control pack (#1505 FK plan); honest tri-state and
  forbidden list documented. One orphaned live endpoint found — `GET
  /portal/hold-windows/:holdId/events` has zero page consumers — filed as **#1619**
  and linked as a real GitHub sub-issue of #1488. Verified via
  `verify-branch-merged.mjs`: OK, merged into origin/main at 25f9e316.
