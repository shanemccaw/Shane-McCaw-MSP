# #2109 — Dynamic Build Queue Map Phase 1: real data layer — blockers, loop detection, errors, age

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** dfbf54576

## Log
- 2026-08-31 ⏳ IN FLIGHT — Auditing bt_build_queue schema + GetNextAsync live-blocker-check pattern before writing the queue-map data/service layer.
- 2026-08-31 ✅ DONE — Shipped the #2108 Phase-1 data layer (dfbf54576). New `BuildQueueMap.cs` (models + pure `BuildQueueMapAnalyzer` static class: `DetectCycles` DFS white/gray/black coloring, `ClassifyError`, `CurrentState`) and `BuildQueuePostgresClient.QueueMap.cs` (partial-class `GetQueueMapAsync`). Reuses the exact `GetNextAsync`/`SelectClaimCandidatesAsync` live-open-issue pattern (Git #1600/#1904): ONE `TryGetOpenIssueNumbersAsync` snapshot per pass, fail-closed → unreachable GitHub leaves each blocker `IsOpenOnGitHub=null` (unknown, still-blocking), never silently "closed". Audit findings: (1) real schema read from migrations — created_at/claimed_at/completed_at/updated_at/build_pid_started_at all exist; QueueItem/MapRow deliberately DON'T expose the first three (the #1384 fixed-ordinal minefield), so the map uses its own dedicated SELECT rather than threading them through MapRow. (2) #2103 (dispatch tracking / build_dispatch_log) is still OPEN, not landed — did not block on it; age uses existing created_at/claimed_at/completed_at/updated_at as designed. (3) Cycle detection is genuinely distinct from chains: edge = item.github_number → each blocked_by number; DFS flags real deadlocks (A→B→C→A), self-loops as single-element cycles, canonicalized (rotated to smallest) + deduped. Error kinds: Failed / OrphanedByRestart (exit -2) / SessionLimit (limit-paused). VERIFICATION: `dotnet build BuildConsole.csproj` clean — 0 warnings, 0 errors. Deadlock algorithm exercised via a standalone TEMP harness (outside the repo, deleted after): 3-cycle→detected, long chain→(none), self-loop→detected, two disjoint cycles + chain tail→both detected/tail ignored, external-issue sink→(none), two-entry-point cycle→deduped to one. All correct. No new migration (additive-column-free; reads existing schema). No fixture/hardcoded data. Working tree clean.
