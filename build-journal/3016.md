# #3016 — Contract Pack: Chat Document Container + tool rail (BuildConsole)

- **Status:** ✅ DONE 2026-09-06T19:24:24Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 03fb37cc8 (contract pack), 583a9cdc0 (IN FLIGHT bookend)

## Log
- 2026-09-06T19:19:28Z ⏳ IN FLIGHT — Writing a real contract pack for `Controls/ChatDocumentContainer.xaml(.cs)` (5-band chat shell, tool rail, `IChatSendableTool` Send-to-Chat contract) to `docs/buildconsole-chatcontainer-contract-pack.md`, documenting ACTUAL built code confirmed by reading the source. Doc-only, no product code change.
- 2026-09-06T19:24:24Z ✅ DONE — Shipped `docs/buildconsole-chatcontainer-contract-pack.md` (13 sections, ~350 lines) in commit `03fb37cc8`, pushed to `origin/main` and verified an ancestor of main (verify-branch-merged.mjs exit 0). Every claim extracted by reading the real source: the 5-band grid + load-bearing cool/warm palette split, Band 0 context bar (epic label + live epic-scoped queue/board status counts + context gauge), the wrench tool table (3 real hosts: Terminal/Log Peek/Git Doctor mini; 9 honest stubs under #1202), the Detected panel on the real ChatDockService, the `IChatSendableTool.GetSendableContent()` Send-to-Chat contract (only real implementer = TerminalView), the app-owned composer + DOM-bridge send, the cross-epic round trip, Inspector states, and the two independent context-threshold sets (40k+/900k budget fill vs raw 60k/85k/100k colour tiers). Supporting method/type citations spot-verified (BoardChat, ChatContextMeterStore.Get, ChatDockService.BuildAsync, the MainWindow.* entry points, CtxPlanPill has no code-behind source). Doc-only — no product code touched; BuildConsole WPF is out of scope for test manifests per CLAUDE.md. Working tree clean. No new findings filed (surface is coherent; the honest gaps in §13 are all deliberate/already-tracked sibling Features under #1202).
