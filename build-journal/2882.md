- **Status:** ✅ DONE 2026-09-06T14:35:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-06T14:10:00Z — Started. Fixing `verifyCaptchaToken()` in `artifacts/api-server/src/lib/captcha.ts` to fail closed (not open) when `TURNSTILE_SECRET_KEY` is missing/empty, per #2882.
  - 2026-09-06T14:35:00Z — ✅ DONE — `verifyCaptchaToken()` now returns `{ success: false, bypassed: false }` (logged via the existing `auth`-channel `log.error`, matching the network-error branch) instead of the old fail-open `{ success: true, bypassed: true }`. Both confirmed call sites (`routes/portal-checkout.ts:198`, `routes/portal-checkout-direct.ts:58`) already gate on `!captchaRes.success`, so no call-site changes were needed. Added `src/lib/captcha.test.ts` (missing key, empty-string key, real-secret-still-works) and registered it in `vitest.config.ts`'s explicit include allowlist. Could NOT execute vitest in this session — `artifacts/api-server/node_modules` is empty in the shared main checkout, a pre-existing environment gap already tracked by #2973 and #2979 (both filed today, unrelated to this build) — verified correctness by static review instead (confirmed both call sites' gating logic, confirmed test compiles logically against the real function signature). Commit: `e08651654` (merged to `main`; ancestor-verified via `verify-branch-merged.mjs`). `git status --porcelain` clean.
