# #2669 — MSP console: no operator routes exist for Runbooks — build the real backend

- **Status:** ✅ DONE 2026-09-03T20:22:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 9c537b3456aee409f0dcf221d664ed989d7fc5db (merged to main)

## Log
- 2026-09-03T19:54:54Z ⏳ IN FLIGHT — Audited `/portal/runbooks` (portal-runbooks.ts) as the
  customer-facing contract and #1683 (Feature: Runbooks MSP Console, NOT ARCHITECTED for full
  authoring) for what an operator genuinely needs. Building MSP-scoped read (list + run history)
  plus the two operator actions #1683 explicitly names that already have a real, proven
  implementation to mirror: mark a step complete on a live run, and extend a hold window. Not
  building runbook-definition authoring / step reordering / run-outcome recording — #1683 itself
  flags those NOT ARCHITECTED. Extracting the wire-building logic out of portal-runbooks.ts into a
  shared lib so both the customer and MSP routes read off one implementation, then adding
  `msp-runbooks.ts` with `requireRole("MSPOperator")` + `resolveMspIdStrict` +
  `customerBelongsToMsp` ownership check per the codebase's own established pattern
  (msp-vip-classifications.ts). No schema change needed — `portal_runbooks` etc. are already
  keyed on `customer_id` (tenants.id) with no FK, same convention `resolveTenantScope`/
  `customerBelongsToMsp` already handle elsewhere.
- 2026-09-03T20:22:00Z ✅ DONE — Shipped `artifacts/api-server/src/lib/portal-runbook-wire.ts`
  (shared wire/read/ownership/cycle-advance logic), `artifacts/api-server/src/routes/msp-runbooks.ts`
  (GET /api/msp/runbooks, PUT /api/msp/runbooks/:runbookId/steps/:position, POST
  /api/msp/hold-windows/:holdId/extend, GET /api/msp/hold-windows/:holdId/events), refactored
  `portal-runbooks.ts` to read off the shared lib (behavior unchanged), and registered the new
  router in `routes/index.ts`. Merged to `main` at `9c537b3456aee409f0dcf221d664ed989d7fc5db`
  (rebased twice onto concurrent sibling MSP-console builds #2670/#2671/#2672 touching the same
  `routes/index.ts`; no conflicts — additive router registrations only).
  **Verification:** real `tsc` was blocked repo-wide by a pre-existing, unrelated pnpm-store
  corruption in the MAIN checkout (`express`/`pg`/`drizzle-orm`/`zod`/etc. all resolve to empty
  directories — `require('express')` itself fails, not just a type-decl gap) — filed as
  **#2734** (URGENT, parented under #1096) rather than self-fixing via `pnpm install`, which
  CLAUDE.md forbids for this class of problem. Verified instead via: (1) `esbuild.transformSync`
  syntax-check on all 4 changed/added files — all pass; (2) a live smoke test against local
  Postgres (seeded a real runbook/run/steps/hold window for tenant 1 / msp 1, exercised every
  route's exact query shape by hand — `customerBelongsToMsp`, the full `loadRunbooksForCustomer`
  read, a step tick that correctly leaves the cycle incomplete, ticking the last step which
  correctly completes the cycle and spawns cycle 2 since the test runbook was `recurring`, a
  hold-window extend, and the event-history read — then deleted the seeded rows). No fixture/
  hardcoded data shipped; the seed rows were scratch verification data only, cleaned up in-session.
  **Findings filed:** #2734 (pnpm-store corruption, URGENT, parent #1096).
  Deliberately NOT built (per #1683's own NOT ARCHITECTED guard): runbook-definition authoring,
  step reordering, run-outcome recording — only the two operator actions #1683 names that already
  have a real, proven implementation to mirror (mark a step complete, extend a hold window).
  `git status --porcelain` clean.
