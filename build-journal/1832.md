# #1832 — Build Queue: right-click Park on a queued item (reverse of Un-park)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 3a57a1ff6

## Log
- 2026-08-30 ⏳ IN FLIGHT — about to read UnparkAsync/QueueBuildAsync/context menu, then add mirrored ParkAsync + Park context-menu action.
- 2026-08-30 ✅ DONE — Added `BuildQueuePostgresClient.ParkAsync(int id)` mirroring `UnparkAsync`'s shape: flips a row to `'parked'` only from `'queued'` OR `'limit-paused'` (judgment call below), no label sync (mirrors `QueueBuildAsync`'s own `!park` label-skip stance). Added "🅿️ Park" context-menu items in `BuildQueuePanel.xaml.cs` for both the `queued` branch and the `limit-paused` branch, same toast/logging pattern as the existing Un-park action.
  - **Judgment call (per #1832's body):** limit-paused items DO get Park — they're genuinely not running, and parking instead of waiting out the session-limit timer is reasonable, same reasoning `CancelAsync` already applies by accepting both `queued` and `limit-paused`. Verifying and running items are excluded, per the issue's own explicit guidance.
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings, 0 errors.
  - `git status --porcelain` clean; HEAD (3a57a1ff6) verified an ancestor of `origin/main` via `git merge-base --is-ancestor`.
  - Not manually re-verified against a live parked/queued row in this session (no interactive DB session run) — the DB flip and menu wiring exactly mirror the already-proven Un-park pair, and the build is clean.
