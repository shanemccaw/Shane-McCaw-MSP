# #3094 — POA&Ms: wire poam.milestone_approaching / poam.expiring alert detectors

- **Status:** ✅ DONE 2026-09-07T21:26:26Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 8af389908 (IN FLIGHT bookend), 11932839d (evaluator + migration, on `main`)

## Log
- 2026-09-07T21:17:51Z ⏳ IN FLIGHT — investigated: `msp_poams`/`msp_poam_milestones` (#3080) confirmed live in local DB, both `poam.*` catalog rows confirmed still `detector_status='pending_detector'`, `enabled=false`. About to build the real evaluator in `customer-tenant-alert-engine.ts`: `poam.milestone_approaching` as the 9-step ladder (90/60/30d, 3/2/1wk, 3/2/1d) off `msp_poam_milestones.due_date` for a still-`active` POA&M's still-`pending` milestone, with severity climbing (info→warning→critical) per firing rather than the catalog's fixed row severity; `poam.expiring` as the separate T-0/lapse condition off `msp_poams.scheduled_completion_date <= CURRENT_DATE AND status='active'`. Then a follow-up additive migration flips both rows live once verified against real seeded POA&M data.
- 2026-09-07T21:26:26Z ✅ DONE — Shipped in `artifacts/api-server/src/lib/customer-tenant-alert-engine.ts`: `evalPoamMilestoneApproaching` (the 9-rung ladder, one condition), `getSeverityOverride` (per-firing severity climb, scoped to only this one condition — every other condition's `rule.severity` path is unchanged), the `poam.expiring` case, and `buildSummary` copy for both. `tsc --noEmit` on `artifacts/api-server` is clean. Verified LIVE by seeding real `msp_poams`/`msp_poam_milestones` rows against the real testbed tenant (`c4c814d4-3afe-441e-9145-62461d0a4fd3`, msp_id=1) and calling the real `evaluateCustomerTenantRules()` in-process: 90d/30d→info, 21d/7d→warning, 3d/1d→critical, a 45d off-ladder milestone did not fire, a `completed`-status milestone at 1d did not fire, a lapsed `scheduled_completion_date` (yesterday, still `active`) fired `poam.expiring` critical, a not-yet-due date (+7d) did not fire, a `completed` POA&M with a lapsed date did not fire — 11/11 real cases correct. All seeded test rows deleted after verification (`msp_poams`/`msp_poam_milestones` both back to 0 rows). Ran `lib/db/migrations/manual/2026-09-07-poam-alert-detectors-live-3094.sql` myself against local Postgres — flips both `poam.milestone_approaching` and `poam.expiring` from `pending_detector`/`enabled=false` to `live`/`enabled=true`, matching the exact pattern every other flipped-live row in this catalog already follows. Re-ran the real evaluator once more with the rules genuinely live and 0 real POA&M rows in the DB — 0 false-positive events, confirming no noise before real data exists. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0) — pushed as `11932839d`. `git status --porcelain` clean. No findings filed — no genuine gap or bug surfaced outside this issue's own scope.
