# #2184 — security: domain monitor_checks endpoint/mapping/severity_rules rewrite

- **Status:** ✅ DONE 2026-09-04T06:56:51Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** fdb1eb2df (on main) — "security: domain monitor_checks endpoint/mapping/severity_rules fidelity fix (#2184)"

## Log
- 2026-09-04T06:32:39Z ⏳ IN FLIGHT — investigating live `monitor_checks` rows for the
  7 affected `security:*` keys, the `ps_cmdlet_key` catalog (`services/ps-execution/
  cmdlet-catalog.ps1`), and real Microsoft Learn cmdlet/endpoint docs before writing
  the corrected `endpoint`/`ps_cmdlet_key`/`mapping`/`severity_rules` for each row.

- 2026-09-04T06:56:51Z ✅ DONE — All 7 rows rewritten in
  `lib/db/migrations/manual/2026-09-04-security-domain-check-fidelity-2184.sql`, run by
  this session against local `DATABASE_URL`. Real endpoints/cmdlets confirmed against
  Microsoft Learn (checked 2026-09-04) and against a LIVE call to the real testbed
  tenant (`mccawsoft2.onmicrosoft.com`, `tenants.id=1`), not guessed:

  1. `security:safe-links-coverage` / `safe-attachments-coverage` /
     `antiphishing-coverage` — switched `executor_type` graph→powershell. Added 3 new
     `cmdlet-catalog.ps1` entries (`get-safe-links-policies` → `Get-SafeLinksPolicy`,
     `get-safe-attachment-policies` → `Get-SafeAttachmentPolicy`,
     `get-antiphish-policies` → `Get-AntiPhishPolicy`, all `Session=exchange`), built +
     deployed to `ca-ps-execution-dev` (revision `q1543a`) via
     `az acr build`/`az containerapp update` per the service's own documented DEV
     deploy path, then called live: real field names confirmed
     (`EnableSafeLinksForEmail`, `ScanUrls`, `Enable` [not "Enabled" — Safe
     Attachments only], `Enabled` [Anti-Phish]). Testbed currently has 2 policies each,
     0 disabled, for all three.
  2. `security:automated-investigation` — reads the alert resource's own real
     `investigationState` property (learn.microsoft.com/graph/api/resources/
     security-alert) instead of `/security/incidents` existence.
  3. `security:password-protection-policy` — reads the real Entra Password Protection
     `directorySetting` (templateId `5cf42378-d67d-4f36-ba46-e8b86229381d`) via
     `GET /beta/settings` (confirmed live: 200 OK, empty — this tenant has never
     customized it) instead of `/domains` existence.
  4. `security:alert-count-by-severity` — genuine per-severity `countEquals` buckets
     (high/medium/low/informational — Graph's `alertSeverity` enum has NO "critical")
     instead of a total-alert count. Confirmed via Explore-agent investigation that no
     `groupBy`-style transform is severity_rules-readable; the multi-`countEquals`
     pattern matches the real `licensing:project-online-detection` precedent.
  5. `security:azure-roleDefinitions-compliance` — added
     `$filter=isBuiltIn eq false` (confirmed live-reachable; the beta
     `isPrivileged eq true` alternative the issue floated returned 400
     `Request_UnsupportedQuery` live, so v1.0 `isBuiltIn` was used instead) plus real
     `severity_rules` (previously `[]`, so it could never fire).

  **End-to-end verification:** built a temp entrypoint
  (`artifacts/api-server/src/scripts/_tmp-verify-2184{,b}.ts`, added to `build.mjs`
  only for the run, then deleted + `build.mjs` reverted — no trace left) calling the
  real `executeMonitorCheck()` against `tenants.id=1` with real mt-app credentials.
  All 5 non-`alerts_v2` checks returned `status: "ok"` with real, correct extracted
  data matching the live PS/Graph calls. The 2 `alerts_v2`-based checks
  (`automated-investigation`, `alert-count-by-severity`) returned `status:
  "license_gap"` — confirmed this is a REAL, PRE-EXISTING tenant condition, not a
  regression: the untouched model checks `security:insider-risk-alerts` and
  `security:open-incidents` return the identical `license_gap` on this same tenant
  right now. Filed as its own finding, **#2837** (parented under #1850 per this
  build's standing rule, board status set to AI Batter Up), since it blocks
  end-to-end verification of a whole check family, not just these two.

  Working tree clean, branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` — OK) before writing this DONE line.
