# #2123 — IssueMentionInjector misses bare issue numbers (no # prefix)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 6166fb4d9 (bookend IN FLIGHT), 23034540b (bare-number heuristic)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Read `IssueMentionInjector.cs` in full (Git #1253/#2066/#2080).
  Confirmed via issue comments that Shane explicitly rejected the "AGENT_PROTOCOLS.md
  standing instruction" alternative — that file only governs Claude Code build agents,
  not claude.ai chat sessions, which is where bare numbers actually get typed. Proceeding
  with the code-level heuristic fix: extend `ISSUE_RE`/`decorateTextNode` with bare-number
  detection, erring toward matching per Shane's stated preference ("I'd rather a few false
  positives than arguing with agent at 3am").
- 2026-08-31 ✅ DONE — Extended `IssueMentionInjector.cs` (`desktop/BuildConsole/Services/`)
  with two new, non-exclusive regexes feeding the existing `decorateTextNode` match/dedup
  pipeline unchanged (real audit confirmed the overlap logic already prioritizes an earlier
  match at the same index, so bare-number candidates were simply appended after the
  existing `ISSUE_RE`/`SQL_RE` loops rather than replacing any of the matching mechanism):
  - `BARE_ISSUE_RE = /\b(\d{4,5})\b/g` — any standalone 4-5 digit run, anywhere in text.
    Static floor, not a live GitHub API query: this runs as injected page-context JS in a
    WebView2 claude.ai tab with no cheap way to look up the repo's real min/max per match.
    Confirmed 2026-08-31 via `gh api .../issues` that this repo's live issues run from
    roughly #300 up through #2123 (this very issue) — the 4-digit floor is a fixed
    approximation of that range, not an exact bound.
  - `BARE_ISSUE_CTX_RE` — a 2-5 digit run at line/bullet start immediately followed by an
    em/en-dash or colon (the exact shape of the reported example, `"1511 — NOT landed"`).
    This structural signal alone is treated as sufficient (no range requirement) so a real
    sub-1000 issue number in that shape (this repo does have some, e.g. #305) still gets
    caught — per Shane's explicit instruction not to require multiple confirming signals.
  Known false positives (accepted, documented, not fixed): years ("In 2026 we..."), ports
  ("Port 8080"), phone-number fragments, generic 4-digit counts ("Ran 4500 tests"), and a
  numbered heading using "N: " or "N — " for something other than an issue. Known false
  negative: a genuinely bare 1-3 digit issue number with no dash/colon structural context
  (e.g. mid-sentence "issue 42") stays undetected — accepted per Shane's stated priority
  (a missed reference is the real problem; a false-positive underline is low-cost).
  Verification: `dotnet build BuildConsole.csproj` succeeded (0 errors/warnings). Ran the
  real regex/dedup algorithm (copied verbatim, not reimplemented) against 12 representative
  strings in a throwaway Node script (deleted after, not committed) — confirmed: the real
  reported example matches; `#1511`/`.sql` filenames still win over bare-number overlap
  with zero double-counting; `1511px`/`#1511px`-style code-like tokens are correctly
  excluded by the `\b` boundary; single-digit/no-context prose ("chapter 5") does not
  match. No `shaneapp://runTest` manifest — this is a WebView2-injected-JS-in-BuildConsole
  change, out of the portal/marketing test-manifest scope (CLAUDE.md Test Coverage
  section), and BuildConsole itself has no live claude.ai session reachable from this
  build session to drive an end-to-end WebView2 hover test against.
  `git status --porcelain` clean at end; branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
