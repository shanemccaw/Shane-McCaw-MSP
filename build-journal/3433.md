# #3433 — Feature: Move Delivery Projects (+ live Kanban board) from Admin Panel to MSP Console (MSPConsole)

- **Status:** 🛑 BLOCKED 2026-09-10T05:10:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none — no code changed

## Log
- 2026-09-10T05:10:00Z ⏳ IN FLIGHT — pulled issue #3433 body + both comments (in order). Confirmed real scope: move `artifacts/admin-panel/src/pages/crm/ProjectDetail.tsx` and `Projects.tsx`, plus `components/KanbanCardModal.tsx` and `components/kanban/TypedCardContent.tsx`, into MSP Console's real surface, along with the real backend routes (`/api/admin/kanban-tasks`, SSE `/api/admin/projects/:id/kanban-events`) — a relocation, not a rebuild, preserving real-time SSE behavior. Checked the build header's `--blocked-by 3424` dependency before touching any code, per the blocked-issue resume protocol.
- 2026-09-10T05:10:00Z 🛑 BLOCKED — `#3424` (Feature: Remove legacy Clients/CRM suite from Admin Panel) is still `state: open`, labeled `blocked` (not `complete`), and is itself blocked on `#3423`, which is blocked on `#3422` — both also still `open`/`blocked`. Real chain: `#3433 → #3424 → #3423 → #3422`, none landed. Confirmed live on `origin/main`: `artifacts/admin-panel/src/pages/crm/Clients.tsx` and `ProjectDetail.tsx` are both still present, so #3424 genuinely has not landed yet. Real collision surface: `artifacts/admin-panel/src/components/shell/workspaceNav.tsx`'s "Delivery" nav section holds three sibling entries in the same block — `del-projects` (line 353, this issue's target, relocating to MSP Console), `del-clients` (line 355, #3424's deletion target), `del-workflows` (line 361, #3425's deletion target — #3425 is itself blocked on this issue for the identical reason, see `build-journal/3425.md`). Editing this file now to relocate `del-projects` while #3424/#3423/#3422 are concurrently queued to edit the same nav section is exactly the shared-file collision class CLAUDE.md's git-conventions section warns against. Also noted: #3424's own body flags that `ClientDetail.tsx` (an #3424 deletion target) imports `KanbanCardModal` — the same live component this issue is relocating — for a "Kanban Bucket Preview," and #3424's session has not yet verified that dependency is safe to sever; moving `KanbanCardModal.tsx` out from under it before that verification lands risks a broken import. Real `blocked_by` edge `#3433 → #3424` already existed (confirmed via `gh api repos/.../issues/3433/dependencies/blocked_by`) and is accurate — left as-is, no new edge needed. Per CLAUDE.md's blocked-issue rules, stopping here rather than proceeding concurrently. No code changed this session; labels swapped `in-flight` → `blocked` and a comment posted on #3433 explaining the wait.
