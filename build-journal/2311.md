# Git #2311 — Epics with no features show an amber "nothing here has a burndown" warning

- **Status:** ✅ DONE 2026-09-02T18:40:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Git Panel, Epic peek panel)
- **Commits:** `aaa6aa57e` (implementation), merged to `origin/main` via `fb840ff5`

## Log

- 2026-09-02T18:15:00Z — ⏳ IN FLIGHT — Session start. #2290 confirmed DONE on origin/main (`79852e2b0`). Scope: when an Epic peek panel has zero features, show an amber "nothing here has a burndown" warning banner. Real data only (GitPanelService feature tree already built by #2290).
- 2026-09-02T18:40:00Z — ✅ DONE — Shipped `aaa6aa57e`. `MainWindow.GitPanel.cs`: `OpenGitEpicPeek` now kicks off the real `GitPanelService.GetFeatureTreeAsync` fetch (same one the tree row already uses) if the epic hasn't been loaded yet, and re-renders the peek when it lands. `RenderGitPeek` adds an amber-bordered banner ("⚠ Nothing here has a burndown — this Epic has no FEATURE sub-issues yet.") gated on real `_gitEpicFeatureTotals[epic.Number] == 0` — no fixture, no guess. Amber hex (`#E2984A`) matches the existing `Brush.Toast.Warning` token.
  **Verification:** `dotnet build` clean (0 warnings / 0 errors). Branch merged and confirmed via `scripts/dev-server/verify-branch-merged.mjs` → OK, `agent/2311-q1193` (`fb840ff5`) is an ancestor of `origin/main`. `git status --porcelain` clean at session end.
  No findings filed this session — no bugs or gaps discovered beyond scope.
