# #1912 — apply-write-app-permissions-1875.mjs declares permissions but grants none on Windows

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 5ddfed9f0 (bookend), 8281237ff (fix)

## Log
- 2026-08-30 ⏳ IN FLIGHT — investigating the az.cmd inline-JSON quoting bug in
  scripts/azure/apply-write-app-permissions-1875.mjs; scope is the DEV app
  registration (9f6f4772-b5be-421f-815e-b392336c373a) only per Shane's
  corrected comment on #1912 — the PROD registration (3308b280-…) stays
  half-applied, read-only, left as-is (that completion is a #1281 GATE item).
- 2026-08-30 ✅ DONE — root cause confirmed: `az rest --body <inline JSON>`
  reached `az.cmd` under Windows `shell:true`, stripping the quotes before
  Graph saw the payload; the temp-file `--required-resource-accesses @file`
  declare step never had this problem. Rewrote the script (8281237ff) to talk
  to Microsoft Graph directly via `fetch()` with a bearer token from
  `az account get-access-token` — no shell in the path for any Graph read or
  write. Also made `--app` a required flag with no default (the old
  `?? "both"` default is how PROD got touched alongside DEV in the first
  place), added a real post-grant readback verification against Entra
  instead of trusting POST exit codes, and turned "already exists" grant
  errors into idempotent no-ops instead of failures with one loud end-of-run
  summary on any real gap.

  **Live verification (read-only + dry run, both against real Entra on
  mccawsoft2.onmicrosoft.com, tenant c4c814d4-3afe-441e-9145-62461d0a4fd3):**
  - DEV app (9f6f4772-…, SP 7cb2d7dd-…): declares all 16 permissions from
    `DERIVED_WRITE_APP_PERMISSIONS`; only 4 are actually granted
    (Application.ReadWrite.All, Group.Create,
    RoleManagement.ReadWrite.Directory, User.ReadWrite.All) — 12 missing,
    confirmed by direct Graph GET on `appRoleAssignments`, not the old
    script's log. `node scripts/azure/apply-write-app-permissions-1875.mjs
    --app dev --consent` (dry run) with the fixed script computes exactly
    those same 12 as the plan — correct.
  - No `DeviceManagementManagedDevices.PrivilegedOperations.All` anywhere in
    `DERIVED_WRITE_APP_PERMISSIONS` or the declared/granted sets — confirmed
    still excluded (`grantRecommended: false` in
    `graph-write-permissions.ts`), per #1903.
  - PROD app (3308b280-…, SP d21af479-…) read the same way, for reporting
    only, per Shane's scope correction: **also** 4/16 granted (Application.
    ReadWrite.All, Group.Create, RoleManagement.ReadWrite.Directory,
    User.ReadWrite.All) — not the 0/16 the issue thread assumed. Not applied
    to, not reverted; left exactly as found. Its completion is a #1281 GATE
    item, not this session's to do.
  - **`--apply --consent` against DEV was attempted once, deliberately, and
    was blocked by this session's own write-plane restriction** ("Permission
    for this action was denied by the Claude Code auto mode classifier") —
    the same wall #1875 hit. Per the issue's own instruction this was not
    worked around. The dry run is proven correct against live Entra state;
    the actual apply (`node scripts/azure/apply-write-app-permissions-1875.mjs
    --app dev --apply --consent`) is handed back to Shane to run himself.
  - `git status --porcelain` clean; branch verified merged into
    `origin/main` via `verify-branch-merged.mjs` before writing this.
  - No schema/DB change in this build — nothing for #1630.
  - No new findings filed; the only correction (PROD's real 4/16 state vs.
    the assumed 0/16) is recorded here and on the issue comment, not filed
    as a separate defect.
