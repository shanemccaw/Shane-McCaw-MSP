# #4101 — runTest Dev routing has no MSP Console service (5177); Admin route rules are stale

- **Status:** ✅ DONE 2026-09-14T23:16:25Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 7e1040845

## Log
- 2026-09-14T23:14:11Z ⏳ IN FLIGHT — confirmed #4100 (blocker) is closed/complete and already merged into this worktree's base (HEAD == merge-base with origin/main). Adding MspConsole service key to DevServiceRouting.cs, /msp-console route rule, /msp-console basePath in services.json, and replacing 3 stale Admin rules (/dashboard, /customers, /m365-health) with /admin-panel and /adminv2.
- 2026-09-14T23:16:25Z ✅ DONE — desktop/BuildConsole/Services/DevServiceRouting.cs: added `MspConsole = "msp-console"` key, a `/msp-console` route rule ahead of Admin's, scoring-dictionary entry, and updated the class-doc port list. scripts/dev-server/services.json: added `"basePath": "/msp-console/"` for msp-console (confirmed live against artifacts/msp-console/vite.config.ts, which self-mounts at MSP_CONSOLE_CANONICAL_BASE the same way admin-panel does) so `OriginForRoute` (Git #4100) mounts it correctly instead of landing on Vite's flat-origin 404. Replaced the three stale Admin rules (`/dashboard`, `/customers`, `/m365-health` — leftover from the retired msp-portal app, not real `artifacts/admin-panel/src/App.tsx` routes) with the real `/admin-panel` and `/adminv2` prefixes. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. `node scripts/dev-server/verify-branch-merged.mjs` — OK, 7e104084 merged into origin/main. `git status --porcelain` clean. No test-manifest changes needed — no existing manifest references msp-console or the stale Admin paths (`admin/tenant-switcher-floaty.json` targets a removed component per the issue and is out of scope for this routing-only fix). No fixture/hardcoded data introduced.
