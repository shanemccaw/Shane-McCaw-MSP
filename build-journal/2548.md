# #2548 — Feature: Chat Document Container (BuildConsole)

- **Status:** ✅ DONE 2026-09-03T15:18:09Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** bdbb42ce8 (IN FLIGHT bookend), 66ef5a3fe (Chat Document Container)

## Log
- 2026-09-03T14:57:02Z ⏳ IN FLIGHT — Investigated: pulled #2548 body + comments, read README-ClaudeChat.md in full, mapped BuildConsole's real chat wiring (no TabDef/TabKind — tabs are `TabItem` + `Tag=BoardChat` + `ChatTabState`; chat epic resolved via `LeftSidebar.GetEpicForChat`; chat host is per-tab `ChatSafeWebView2` built in `OpenChatTab`; `ChatDockService` static + `ChatDockPanel` for Detected data; `OpenFloatingChatWindow(BoardChat)` for Floaty; `ChatContextMeterStore` for context gauge; `FloatingChatBridgeScript` for claude.ai DOM read/write; logging via `ActivityLog.Log(channel, msg)`). Studied ShaneBuilder's landed #2209 reference (WebView2 hosts real claude.ai transcript+composer; native WPF chrome bands wrap it). Building a self-contained `ChatDocumentContainer` UserControl wrapping the existing chat body in the 5 bands.
- 2026-09-03T15:18:09Z ✅ DONE — Shipped the Chat Document Container as `Controls/ChatDocumentContainer.xaml(.cs)` + `Controls/CrossEpicAskDialog.cs` + `MainWindow.ChatContainer.cs`, wired into `OpenChatTab` (wraps the existing WebView2 split grid — build-pane/context-meter/inline-SQL untouched inside `state.SplitGrid`) and `EditorTabs_SelectionChanged` (per-tab context refresh). Clean `dotnet build` (0 warnings, 0 errors). Branch verified ancestor of origin/main (MERGED-OK); working tree clean.

## What shipped (per README-ClaudeChat.md §1–12)
- **Architecture (ShaneBuilder #2209 resolution):** the transcript + composer inside the chat are the REAL claude.ai site via the existing WebView2; every band drawn here is native WPF chrome around it. One `ChatDocumentContainer` per chat tab.
- **Band 1 Context bar (cool #12151c):** epic derived from the tab via `GetEpicForChat` (never hardcoded, §11 fix); epic-scoped counts from REAL data — verifying/in-flight/queued from the live `BuildQueuePanel.CurrentQueueItems` filtered to the epic's children, blocked from GitHub board `blocked` label, complete from GitHub's own `SubIssueCompleted/SubIssueCount` rollup; two-part context gauge (40k overhead + conversation estimate; uses the real `ChatContextMeterStore` estimate when present, else 0.28/char); colour (60/80) + Start-New-Chat (75) cross thresholds together; ⌸ Detected toggle (+ accent dot), ⚒ wrench Popup (PlacementTarget, never screen coords), ⧉ Floaty → `OpenFloatingChatWindow`. `overflow:visible` + `Panel.ZIndex=40`, no ClipToBounds.
- **Band 2 Breadcrumb/Share (warm):** Shane McCaw SaaS / chat crumb; Share copies the real claude.ai conversation URL (hides the pill if there's no URL — never a fake inert one, §3).
- **Band 3/4 Transcript + tool rail:** live WebView2 centre; rail is a sibling column (0↔280px), never an overlay; exactly one panel open at a time (⌸ and wrench tools close each other); shared 36px header pattern (glyph/name/maximize/close). Tool bodies are honest stubs — the 12 tool internals are sibling Features under #1202 (out of scope, as specified).
- **Band 5 Composer (warm):** app-owned, per-tab draft = per-container composer (structural §8 — switching tabs preserves each draft); tools + cross-epic write here appended after a blank line, never auto-sent (§5); Enter sends / Shift+Enter newlines; Send injects+submits into THIS tab's own live claude.ai composer via `FloatingChatBridgeScript` (resolved dynamically from the body subtree so a reopen-swap can't leave it stale), clearing only this tab's draft.
- **Detected panel (§6.1):** hosts the real, already-landed `ChatDockService`/`ChatDockPanel` (mentioned issues + pinned questions, live GitHub board status) — no new backend, no fixtures.
- **Cross-epic round trip (§7):** wrench → "Ask another epic's chat…" picks a destination open chat tab (`CrossEpicAskDialog`, 3 doc presets), writes the stamped question into the DESTINATION composer with a return-address bar there, records a pending card (WAITING) at the top of the Detected panel; "Bring the answer back" → paste box (manual path) → ANSWER READY; "Paste answer into this chat" writes the stamped answer into the ORIGIN composer and clears the card. `FromConversationId` is the required return address.
- **Inspector states (§9):** warning (floating non-blocking notice band) + blocking (overlay covering ONLY the composer band — native WPF over a native band, transcript stays readable). Two visual states only; public hooks `ShowInspectorWarning/ShowInspectorBlocking/ClearInspector` (no inspector engine in this issue).
- **Logging:** `ActivityLog.Log("chat.container", …)` (locked-taxonomy channel).

## Acceptance checklist §12 (in-scope items)
✓ context bar wraps not overlaps, nothing clips · ✓ wrench under wrench, visible/clickable (Popup) · ✓ exactly one rail panel; ⌸/wrench close each other · ✓ rail reflows, never overlays · ✓ panel sections flex:none (DockPanel) · ✓ send-to-chat lands in active tab, blank-line, never auto-sent · ✓ per-tab drafts preserved (per-container) · ✓ cross-epic lands in destination only / answer in origin only / card clears · ✓ context %/colour/Start-New-Chat cross together · ✓ transcript serif (claude.ai's own render) / chrome sans · ✓ Inspector blocking covers only the composer.
N/A (tool internals, deferred to sibling Features): Repo Health cap-5, Graph Write DRY RUN, Log Peek COLD/BURST, Git Map "this chat" panel.

## Honest notes (not findings — sanctioned scope)
- Cross-epic answer capture uses the manual-paste path (README §7 note + open question #2): the one-shot DOM read of another tab's last assistant turn belongs with the Git Map tool Feature; the card holds the return address so the trip isn't lost.
- Detected ⌸ dot loads lazily (lights after the panel is first opened, or immediately when a cross-epic question is pending) to avoid burning GitHub calls on every tab switch.
- Minor cosmetic overlap with the pre-existing `CreateChatContextWrapper` 4px context line (its handoff banner stays collapsed until high context); left as-is to avoid touching that shared helper's 4 other callers.

## Verification
- `dotnet build BuildConsole.csproj -c Debug` — succeeded, 0 warnings, 0 errors.
- BuildConsole is a WPF internal tool: test manifests are explicitly out of scope (CLAUDE.md), and launching `BuildConsole.exe` to drive its UI is prohibited (single-instance courier hazard). Verification is the clean build + acceptance-checklist code review above.
- Branch ancestor check: `git merge-base --is-ancestor HEAD origin/main` → MERGED-OK. Working tree clean.
