# #4224 — LeftSidebar.xaml: 8 raw hex colors have no matching theme brush (New Chat picker + Active Working Epic banner)

- **Status:** ✅ DONE 2026-09-15T05:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 5bb27ae06

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — pulled #4224's full issue body + comment; of the 8 raw hex
  colors listed, line 29 (`#33A6E3A1`) was already resolved by #4205's `GreenWashMediumBrush`
  landing after #4224 was filed — 7 remain, covering 5 unique colors. Adding real matching
  brushes to `Themes/DarkTheme.xaml` for the New Chat picker (button fill, text/glyph, popup
  panel fill/border) and the Active Working Epic banner fill, then wiring them into
  `Controls/LeftSidebar.xaml` in place of the literal hex.
- 2026-09-15T05:20:00Z ✅ DONE — added 5 named brushes to `Themes/DarkTheme.xaml`
  (`NewChatPickerBackgroundBrush`, `NewChatPickerTextBrush`,
  `NewChatPickerPanelBackgroundBrush`, `NewChatPickerPanelBorderBrush`,
  `ActiveWorkingEpicBarBackgroundBrush`) and wired all 7 remaining raw-hex occurrences in
  `Controls/LeftSidebar.xaml` to them (commit 5bb27ae06). `grep -noE '"#[0-9A-Fa-f]{6,8}"'
  desktop/BuildConsole/Controls/LeftSidebar.xaml` now returns nothing. Verification: a real
  `dotnet build BuildConsole.csproj` on this branch hits a **pre-existing, unrelated** MC3000
  XML error in `Controls/GitModeContainer.xaml` (duplicate closing tags, last touched by
  #4189 on `main` before this session started — confirmed via `git diff --stat HEAD~2 HEAD`
  showing my commit touched only `LeftSidebar.xaml`/`DarkTheme.xaml`). Filed as **#4235**
  (URGENT, sibling sub-issue of #1202, labeled `bug`, board status "AI Batter Up"). Applied
  that file's fix as a **scratch, uncommitted** local edit only to unblock a real full-app
  build, confirmed `dotnet build` then succeeds 0 errors/0 warnings with my brush changes
  compiled in, then reverted the scratch edit (`git checkout --`) before finishing — #4235
  carries the real fix, not this commit. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` OK, 86289c95) and working tree confirmed clean
  (`git status --porcelain` empty) before writing DONE.
