# #2607 — Billing: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ⏳ IN FLIGHT 2026-09-03T16:10:00Z
- **Scope:** platform
- **Started:** 2026-09-03

Leaf issue, sub-issue of Feature #1692 (Billing, MSP Console). Backend confirmed real:
`msp-plan-self-service.ts` — 4 routes (current plan, available tiers, change plan, cancel
pending change), all `requireRole("MSPAdmin")`. Extracting the #1642-pattern contract pack:
per-surface wire contracts cited to file:line, CURRENT vs DECIDED on every field, real enum
unions only, cross-surface edges, honest tri-state, forbidden list, orphaned endpoints
listed explicitly. Read-only — no product code, schema, or UI changes.

## Log
- 2026-09-03T16:10:00Z ⏳ IN FLIGHT — Pulled #2607 (body + comment), confirmed its Feature
  parent is #1692. Read `msp-plan-self-service.ts` (455 lines, all 4 routes),
  `msp-plan-pricing.ts` (Stripe Price + Subscription Schedule mechanics),
  `msp-plan-management.ts` (the PlatformAdmin contrast surface), `msp-billing-webhook.ts`
  (schedule-transition finalize/backstop), `msp-entitlement.ts` (`loadTier`,
  `checkTenantAllowance`), `msp-signup.ts` (the `fulfillmentTypeKey` OR-arm precedent from
  #2509), schema (`mspSubscriptionsTable`, `servicesTable`, `MSP_SUBSCRIPTION_STATUSES`,
  `MSP_BILLING_INTERVALS`, `MSP_DUNNING_STATES`). Queried live local `DATABASE_URL`:
  1 `msp_subscriptions` row (msp 1626 → service 131, a synthetic regression-test fixture per
  #2509's bookend, not a real tier), 3 real platform tiers (Free/Growth/Pro, ids 120-122) —
  confirmed a live bug: all 3 have `fulfillment_type='manual'` (not
  `'msp_monthly_subscription'`), relying on `fulfillment_type_key='msp_monthly_subscription'`
  as the real discriminator (the #2509 OR-arm safety net `msp-signup.ts` already has).
  `msp-plan-self-service.ts` (`GET /msp/plan/available`, `POST /msp/plan/change`'s target
  lookup) and `msp-plan-management.ts` (`GET tiers`, `migrate-subscriber`) filter ONLY on
  `fulfillmentType`, missing that same OR-arm — so against live data
  `GET /api/msp/plan/available` returns `[]` and `POST /api/msp/plan/change` 404s for every
  real tier. Confirmed `annualPriceCents` is NULL on all 3 real tiers too (yearly-interval
  self-service change would `PlanPricingError` today — documented as honest current gap,
  not filed separately, since the code already surfaces it as a clear 400). No frontend
  consumer exists for any of the 4 routes (`artifacts/msp-console` does not exist; grep
  across `artifacts/*/src` for `msp/plan/` found no caller) — all 4 orphaned, same shape as
  the #2595 SOPs MSP-console pack's finding. Writing
  `docs/msp-plan-self-service-contract-pack.md`, then filing the fulfillmentType/Key
  mismatch as a sibling sub-issue of #1692.
