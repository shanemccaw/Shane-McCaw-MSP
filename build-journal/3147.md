# #3147 — Feature: Money — Now tab (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:55:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-08T01:20:00Z — Started. Blocker #3137 (Money API/data-access layer) is closed +
  `complete`; removed the stale `blocked_by` edge on this issue. Backend (`src/core/money.mjs`,
  `/api/money/gate`, `/api/money/what-if`, `/api/money/simulate-transfer`, `/api/money/budget-day`,
  `/api/money/habits`) is fully built by #3137 — this issue is pure UI wiring for the Money "Now"
  tab against that real, already-live API. No schema change, no migration needed.
  Protected/Urgent/Already-handled buckets map onto real data already returned by
  `getGateStatus()`: Protected = `gateBills` + `protectedDebts`; Urgent = `otherBills` with
  `funded === false`; Already handled = `otherBills` with `funded === true`. No new table
  invented for a client-side "mark handled" workflow — that's not backed by any real mutation in
  the design's own MCP tool list, so it isn't fabricated here either.
- 2026-09-08T01:55:00Z — ✅ DONE — commit `9f9240574` ("Money -- Now tab (#3147)") on `main`.
  Added the "Money" nav tab + `#/money` route, and `viewMoney()` in `public/app.js` rendering the
  real Available-to-spend card (40px amount, real one bar, real habit "really" line), real
  what-if and transfer-simulator forms calling the live endpoints with real result boxes, a real
  Budget Day card, and real Protected/Urgent/Already-handled buckets built off `getGateStatus()`'s
  own bill split. Bills/Cars/Vault/Wins render an honest "not built here yet" placeholder (each is
  its own Feature per the issue), never fabricated content. `public/app.css` gets the new
  `.money-*` styles (reusing `.budget-bar`/`.budget-bar-fill` for the one bar rather than
  duplicating it).
  Verification: `node --check public/app.js` passes. Could not do a full server-started
  end-to-end run — the shared local `finances` database's `schema_migrations` ledger was being
  actively rewritten by a concurrent build (#3156, Things hub/spoke migration 031) for the
  duration of this session, so `npm run migrate` correctly refused per #3140's own drift guard
  (not this issue's migration to fix). Instead verified every real function the new UI calls
  directly against the live local `finances` database: `getGateStatus()`, `whatIf()`, and
  `simulateTransfer()` all called live, returning real bill accounts, real shortfalls
  (confirmed real, current negative "available to spend" against real balances), and correct
  transfer-simulation math — matching the exact response shapes the new UI code consumes.
  Rebased twice onto `origin/main` mid-session for real concurrent migration-file and
  `app.js` (Lists room, #3155) collisions; resolved cleanly both times. Merged
  `agent/3147-q1904` into `main` as a fast-forward and pushed; `verify-branch-merged.mjs`
  confirms ancestry. `git status --porcelain` clean. No findings filed — no gap outside this
  issue's own scope was found; the migration-ledger drift is transient concurrent-build activity,
  not a bug.
