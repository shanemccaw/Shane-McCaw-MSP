# #3330 — Today's meds-pill card: underlined text + As-needed incorrectly shown as pending

- **Status:** ✅ DONE 2026-09-09T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** f0c530d83

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — fixing text-decoration underline on `.meds-pill` and excluding as-needed batch from `medsPillSection()`'s `current` selection in web/shanes-life/public/app.js
- 2026-09-09T00:20:00Z ✅ DONE — added `a.card { text-decoration: none; color: inherit; }` to app.css (a general fix covering the meds-pill link and the other real `.card`-as-link instance, the "Tonight" recipe teaser, both previously unstyled); reworked `medsPillSection()` in app.js to filter out any batch matching `MEDS_PILL_AS_NEEDED_RE` (same free-text convention as medications.mjs's own `AS_NEEDED_BATCH_RE`) before picking `current`, and to show "All taken" instead of falling through to an as-needed batch once every scheduled batch is taken today. Verified: `node --check` on app.js passes; ran the extracted selection logic standalone against both a real "all scheduled taken, as-needed remaining" case and a "one scheduled still pending" case -- both resolve correctly (no as-needed pick, no pending misrepresentation). No automated UI test harness exists for this app (no test framework wired up in web/shanes-life beyond a few unrelated selftest/*.mjs scripts and one extension test), so no manifest was added; verification was direct code-level. Branch verified merged into origin/main via verify-branch-merged.mjs.
