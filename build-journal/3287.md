# #3287 — Separate real Supercharging rate from home electricity rate for charging cost estimates

- **Status:** ✅ DONE 2026-09-09T00:25:00Z
- **Scope:** platform (web/shanes-life -- Shane's Life app, Tesla Integration Feature #3237)
- **Started:** 2026-09-08
- **Commit(s):** e67cd485 (merged to `main`)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Read #3287 body. Investigating whether Tesla's charging/history
  `location`/`siteLocationName` field can distinguish Supercharger vs. home sessions before assuming
  a location-history cross-reference is needed. Plan: add `supercharge_cost_per_kwh` column
  (migration 064), classify sessions by whether `location` contains "Supercharger" (Tesla's own
  real, long-standing site-naming convention -- e.g. "Chicago, IL Supercharger" -- confirmed by the
  same third-party-integration evidence migration 060's header already cites), pick the correct rate
  per session, set the real $0.39/kWh Supercharging rate via the existing commute-settings
  update path, leave the home rate untouched/null per Shane's own instruction.
- 2026-09-09T00:25:00Z ✅ DONE — Real migration `web/shanes-life/migrations/065_tesla_supercharge_rate.sql`
  (next free number was 065, not 064 -- a concurrent sibling build had already claimed 064 locally)
  ran myself against the real local `finances` Postgres DB: `tesla_accounts.supercharge_cost_per_kwh`
  (numeric, real `DEFAULT 0.39` -- Shane's own already-stated real value, applies to both today's zero
  existing rows and any future real account row), and `tesla_charging_sessions.rate_source`
  (`'home'|'supercharger'`, `DEFAULT 'home'`, checked). `charge_cost_per_kwh` left completely
  untouched -- still the real home rate, still NULL until Shane states it.

  **Investigation answer (confirm-rather-than-assume, as #3287 asked):** Tesla's own
  `charging/history` `location`/`siteLocationName` field DOES reliably distinguish session type --
  Tesla's real, long-standing Supercharger site-naming convention always includes the literal word
  "Supercharger" (e.g. "Chicago, IL Supercharger"), the same real convention third-party
  integrations (TeslaFi, TezLab) already rely on for this exact classification. No separate
  location-history cross-reference (#3217's earlier idea) is needed. New
  `classifyChargingRateSource(location)` in `core/tesla.mjs` implements this
  (`/supercharger/i` match -> 'supercharger', else 'home', including null location). Honest gap,
  same as #3217/#3238's own bookends: no live Tesla connection exists yet, so this is
  best-evidenced, not live-confirmed against a real observed `location` value -- re-check once a
  real account connects.

  `syncChargingSessionsFromTesla` (core/vehicles.mjs) now picks `supercharge_cost_per_kwh` for a
  Supercharger session and `charge_cost_per_kwh` for a home session, instead of one shared rate for
  everything; `getCommuteSettings`/`updateCommuteSettings` expose both rates; the Settings ->
  Tesla card and the Cars detail Charging list (public/app.js) both got a second real input/label
  for the Supercharger rate and now show which rate applied per session.

  **Real, hands-on functional verification** (same approach as #3217/#3238): seeded one disposable
  test user + a real `tesla_accounts` row (real AES-256-GCM encryption via the real `saveTokens()`)
  with `chargeCostPerKwh=0.12` / `superchargeCostPerKwh=0.39`, stubbed ONLY the outbound `fetch()`
  to Tesla's real charging/history endpoint to return one Supercharger session (30.5 kWh, location
  "Chicago, IL Supercharger") and one home session (10 kWh, location "123 Main St"), ran the real
  `syncChargingSessionsFromTesla`, and confirmed against the real local DB: Supercharger session ->
  `rate_source='supercharger'`, cost $11.90 (30.5 × 0.39, correct); home session ->
  `rate_source='home'`, cost $1.20 (10 × 0.12, correct). Disposable test user + its cascaded rows
  deleted afterward, confirmed zero residue. `node --check` clean on every touched file.

  Merged to `main` at `e67cd485` (rebased worktree branch `agent/3287-q2014` onto `origin/main`,
  pushed directly). `node scripts/dev-server/verify-branch-merged.mjs` confirms ancestor of
  `origin/main`. `git status --porcelain` clean (scratch `.verify-3287.mjs` and the `.env` copy
  used to reach the real local DB from this worktree both deleted before this commit).
