# Build Journal — #4177

- **Status:** ✅ DONE 2026-09-14T01:20:00Z
- **Scope:** buildconsole
- **Commit(s):** caf5e580b (merged to main via af36057ae)

## Log
- 2026-09-14T00:00:00Z — Starting: map 47 raw literals in desktop/BuildConsole/Controls/ApiRunnerView.xaml to Typography.xaml/Colors.xaml resources.
- 2026-09-14T01:20:00Z — ✅ DONE — Mapped 44 of 47 raw literals in `ApiRunnerView.xaml` to real `Typography.xaml` resources (`BodyFontSize`/`CaptionFontSize`/`MonospaceFontSize` + `MonospaceFontFamily`); removed redundant inline `FontSize`/`FontFamily` overrides on controls already using the `EditorTextBox`/`TerminalOutputBox` styles so the style's `MonospaceFontSize` actually governs instead of being silently overridden. Left 3 literals untouched, all already documented inline: the decorative ⚡ glyph's `FontSize="48"` (exempt per issue's own decorative-icon-glyph rule), and 2 raw hex wash colors (`#25A6E3A1`/`#25F38BA8`) for which no `GreenBrushTint`/`RedBrushTint` exists — filed as a real finding rather than guessing an inexact brush. Verified with `dotnet build BuildConsole.csproj` (0 warnings/errors). Committed as caf5e580b, merged to `origin/main` at af36057ae. `git status --porcelain` clean.
