# Build Journal — #3793

- **Status:** ✅ DONE 2026-09-12T01:10:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — IN FLIGHT. Starting: dual-signature schema migration for
  `msp_security_plan_versions` (Feature #1689's decision — customer AND MSP both
  sign, independently). Confirmed via direct psql: table has exactly one
  signed/signed_by/signed_at triple, 0 rows currently. Plan: add
  customer_signed_by/customer_signed_at + msp_signed_by/msp_signed_at columns
  (additive, run myself), update `signSecurityPlanVersion` into two independent
  per-party functions, repurpose the MSP-console sign route
  (`msp-security-plan.ts`) to be the MSP's OWN signature (server-derived from
  `req.user`, matching `createdBy`) since it has no live frontend consumer yet,
  keep the portal customer-facing sign route (`portal-security-plan-document.ts`,
  #2949) as the customer's own signature, and redefine "plan of record" /
  drift baseline as the last version where BOTH parties have signed
  (`fullyExecuted`).
- 2026-09-12T01:10:00Z — ✅ DONE — commit `f8acc78c0` on `main`. Ran
  `lib/db/migrations/manual/2026-09-12-security-plan-dual-signature-3793.sql`
  against local Postgres (additive: customer_signed_by/customer_signed_at +
  msp_signed_by/msp_signed_at, backfill no-op — 0 rows existed). Split
  `signSecurityPlanVersion` into `signSecurityPlanVersionAsCustomer`/
  `signSecurityPlanVersionAsMsp`; renamed `getLastSignedSecurityPlanVersion`
  → `getLastFullyExecutedSecurityPlanVersion` (requires both signatures).
  Wired both real call sites: `portal-security-plan-document.ts` (#2949,
  customer) and `msp-security-plan.ts`'s `PATCH .../sign` (repurposed to be
  the MSP's own signature, derived from `req.user`, since no live
  `msp-console` UI ever called its old off-platform-entry shape). Updated
  `portal-security-plan.ts` (plan of record) and `security-plan-drift.ts`
  (drift baseline) to key off "fully executed" (both signed). Left the
  legacy `signed`/`signed_by`/`signed_at` columns in place, unused — a
  second migration (`2026-09-12-security-plan-drop-legacy-signature-columns-3793.sql`)
  drops them but is destructive DDL, so it's Shane's to run (labeled
  `Shane To-Do` on #3793). Appended the additive migration to #1630's
  checklist. Tests: `portal-security-plan-document.test.ts` updated (21
  passing), new `security-plan-dual-signature.live-db.test.ts` added and
  passing against real local Postgres. `api-server`/`portal` tsc --noEmit
  both clean (0 security-plan errors; 2 pre-existing unrelated failures
  confirmed present on `origin/main` beforehand). `lib/db` vitest: 124/124.
  Branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
  `git status --porcelain` clean. Posted completion comment on #3793 and
  appended checklist line on #1630. Labels: #3793 flipped `in-flight` →
  `complete` + `Shane To-Do`. No findings filed — no genuine new gap
  discovered beyond what #3793 itself already documented.
