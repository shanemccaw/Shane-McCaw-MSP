# #1783 — Regenerate pnpm-lock.yaml after artifacts/mockup-sandbox removal

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** cc102eb8d (bookend IN FLIGHT), f2aa2f66e (lock regeneration)

## Log
- 2026-08-29 ⏳ IN FLIGHT — regenerating pnpm-lock.yaml via `pnpm install --lockfile-only` at repo
  root, same method #1754 proved safe. Will verify node_modules junction mtime/realpath
  byte-identical before/after, confirm `artifacts/mockup-sandbox` gone from `importers:`, and
  sweep remaining real on-disk packages against the regenerated lock.
- 2026-08-29 ✅ DONE — `pnpm install --lockfile-only` at repo root regenerated the lock (commit
  `f2aa2f66e`, 213 deletions, 0 insertions, only `pnpm-lock.yaml` touched). Confirmed via
  `fs.lstatSync`/`fs.realpathSync` before and after that `node_modules`'s mtime
  (`2026-08-30T03:27:33.279Z`) and junction target
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\node_modules`) were byte-for-byte unchanged.
  `grep -n "mockup-sandbox" pnpm-lock.yaml` now returns no matches — fully gone from
  `importers:`. Swept all 20 real on-disk packages (6 `artifacts/*`, 13 `lib/*`
  + `lib/integrations/*`, `scripts`) against the lock's importer list and `pnpm -r list --depth
  -1` — every one resolves both ways, nothing orphaned, nothing missing. Pushed both commits
  directly to `origin/main` (bookend `cc102eb8d`→rebased, lock fix `f2aa2f66e`→rebased to
  `d4d51128c` on push); `verify-branch-merged.mjs` confirms merged (`d4d51128c` is an ancestor of
  `origin/main`). `git status --porcelain` clean.
