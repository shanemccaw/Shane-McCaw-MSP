# #3239 — check-migration-numbers.mjs misclassifies orphan as 'missing' when own new migration lands higher

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 4d23e52ea055dc848df934731a70344c1361f22b (main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — fixing `classifyOrphanLedgerFilenames` in
  `scripts/check-migration-numbers.mjs` so a sibling's ahead ledger row is classified by
  whether its number is occupied on disk (real rename/collision hazard), not by comparison to
  this checkout's own highest-on-disk number, which any own new unmerged migration can push
  past an unrelated sibling's unmerged number.
- 2026-09-08T00:20:00Z ✅ DONE — `classifyOrphanLedgerFilenames` now tracks the set of numbers
  actually occupied on disk (across both migrations dirs) instead of a single global "highest"
  number. A row classifies `ahead` iff no on-disk file currently occupies its number; otherwise
  `missing` (fatal) -- correctly covering both the #3140 rename hazard and the #3139
  same-number-collision case. Added selftest case 11a reproducing the exact #3239 repro
  (own new `050_room_order.sql` no longer hides a sibling's harmless `049_...` ahead row).
  All 11 pre-existing selftest cases + the new one pass:
  `node scripts/check-migration-numbers.selftest.mjs` -- "all checks passed". Standalone check
  (`node scripts/check-migration-numbers.mjs`) also passes clean against the real repo state.
  Verified `agent/3239-q1978` is an ancestor of `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`. No new backend/schema needed -- this was a pure
  logic bug in the classifier, no finding to file beyond the issue itself.
