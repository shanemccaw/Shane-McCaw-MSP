# #2319 — Add FeatureNumber to TabDef

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-02T19:28:08Z — Starting. Pulled #2319 body + #2318 (Feature parent) structured index.
  Task: add FeatureNumber to TabDef (desktop/ShaneBuilder/MainWindow.xaml.cs), resolved from the
  real Feature-tier parent of the epic-anchored issue. Every chat row must show it once real.
- 2026-09-02T20:05:00Z — ✅ DONE — commit c3c3243d8 (merged to main at 34a26b5f0). Added
  `TabDef.FeatureNumber` (mutable `int?`, resolved async post-construction, same pattern as
  `GitMapFeature.IsParked`). Added `GitIssuesService.ResolveFeatureTierAncestorAsync(int)` — a
  single nested GraphQL query walking GitHub's real `parent` sub-issue edge up to 4 levels,
  stopping at the first ancestor whose title matches `^feature:\s*` (repo's real naming
  convention). Wired `ResolveChatFeatureNumbersAsync()` at startup (fire-and-forget, mirrors the
  existing `LoadQueueFromDatabaseAsync` pattern), re-rendering the tab strip once real values land.
  `BuildTabPill` now shows a `⬡ #NNNN` badge on every chat row once `FeatureNumber` resolves
  non-null; added the same to the command-palette tab detail panel's new "Feature" section.
  No fabricated data: for the one real chat tab today (`epic-1202`, anchored on #1202 "Epic:
  Build Console"), the real GraphQL parent walk returns `null` (confirmed live —
  `gh api graphql` against #1202 returns `"parent":null`) since #1202 is genuinely top-of-tree
  with no Feature-tier ancestor; no badge renders for it, which is the honest state. Confirmed
  the resolver's algorithm against a second live case: #2319 itself → parent #2318
  ("Feature: Chats…") → matches immediately, `(2318, "Feature: Chats…")`.
  Verification: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors. Live
  `gh api graphql` spot-checks above confirm the walk's real behavior for both the null and
  matching cases. Merge-ancestor check via `scripts/dev-server/verify-branch-merged.mjs` — OK.
  `git status --porcelain` clean.
  No findings filed this session — no genuine gap/bug hit outside scope.
