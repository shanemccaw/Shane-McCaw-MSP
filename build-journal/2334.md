# #2334 — "By feature" regroups the list

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (ShaneBuilder — desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** f9fae41fd

## Log
- 2026-09-02T19:52:00Z ⏳ IN FLIGHT — item 8 of Feature: Test Pad (#2326). Adding a "By feature"
  toggle to TestPadWindow's notes list that regroups notes under a header per
  `TestPadNote.Feature` stamp (from #2331's model, already landed) instead of the current flat
  newest-first order. #2333 (full notes list w/ select+delete+SENT state) is not yet built, so
  this stays scoped to grouping the read-only rows the pad already renders.
- 2026-09-02T20:05:00Z ✅ DONE — Added a "By feature" toggle chip in TestPadWindow's header
  (`TestPadWindow.xaml`) and grouping logic in `Render()` (`TestPadWindow.xaml.cs`): off (default)
  is the existing flat newest-first list; on, notes are bucketed under a header per
  `TestPadNote.Feature` (falling back to "No feature"), features in first-seen order over the
  already newest-first note list, notes within a feature staying newest-first. Real toggle state
  drives both the label/chip highlight and the render path — no fixture data, reads live off
  `TestPadService.Notes`. `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors, both
  before and after rebase onto origin/main. `verify-branch-merged.mjs` confirms a06c41ef is an
  ancestor of origin/main. `git status --porcelain` clean apart from this file. Scope note: #2333
  (notes list select/delete/SENT state) is not built yet, so this pad still renders read-only rows
  — grouping applies on top of that same read-only row, per-feature, as its own real, working
  slice; select/delete/edit remain #2333/#2335's job.
