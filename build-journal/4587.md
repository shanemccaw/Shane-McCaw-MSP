# Build Journal — #4587

Feature: Wire Remediation Checklist "execute" affordance to the real write-execution pipeline (Application Core)

- **Status:** ✅ DONE 2026-09-18T00:00:00Z
- **Scope:** platform
- **Parent:** #1096 (EPIC: Application Core)
- **Commit:** f14aab2ca (Remediation Checklist execute wiring (#4587))

## Log

- 2026-09-17T23:10:00Z — ⏳ IN FLIGHT — Session start. Pulled #4587 body + both comments. Testbed-vs-production posture resolved by Shane's 2026-09-17T23:08:52Z comment: mirror Launch Control's testbed-only restriction (isTestbed = true, write-back consent granted). Read reference files in full: launch-control-change-request.ts, msp-launch-control.ts POST /execute, remediation-fix-route.ts, portal-remediation-fix-routes.ts, remediation-raise-change.ts, msp-change-execution-store.ts, remediation-checklist.ts, workflow-executor.ts.

- 2026-09-18T00:00:00Z — ✅ DONE — Shipped in f14aab2ca (pushed to origin/main; verify-branch-merged OK).
  - **New lib** `artifacts/api-server/src/lib/remediation-execute-fix.ts` — surface-agnostic execution path extending the Launch Control pattern: resolve `checkKey` → live `config_pack_templates` templateId → raise a pre-approved `standard` CR (carrying `remediationCheckKey`) → `runBaselineTemplateAgainstTenant` → record `write_action` + close CR (reuses `recordLaunchControlExecutionOutcome`). All pre-write gates are clean CR-free refusals: open-finding (404), we_can_run (409 with fixRoute), resolvable template (409), connected tenant (400), MSP association (409), testbed-only (403), missing inputs (400), CA-enforcement (409), tenant preconditions (#4545, propagated status). Consent/write-back enforced downstream by `graphWriteForTenant`.
  - **MSP route** `POST /api/msp/customers/:customerId/remediation/checklist/:checkKey/execute` at `ladder.msp-operator` — the direct parallel to Launch Control's own gate.
  - **Testbed-only** first pass per Shane's #4587 decision (mirror Launch Control, not admin-execute-action's production posture).
  - **Verification:** `tsc -p tsconfig.json --noEmit` clean for both touched files — the only tsc errors are pre-existing and unrelated (`src/lib/ca-hold-window-scan.test.ts` `byCategory` drift, present on origin/main, not mine). Real-data check against the testbed tenant (id 2080, McCawSoft, mccawsoft2, isTestbed=t, writeBack=granted): 9 open critical/warning findings map to real active-pack executable templates (e.g. `appgov:risky-permission-grants` → `microrem.remove-risky-app-consent`), and no checkKey maps to >1 distinct template (resolver is unambiguous). The live Graph write itself was deliberately NOT fired in-session: `mccawsoft2` is Shane's real production M365 tenant, and executing e.g. "remove risky app consent" is a consequential real mutation for his hands-on validation, not an unattended build (production boundary).
  - **Filed finding / open decision:** #4643 (sub-issue of #4587, milestone v1.1, board status "Ask Shane") — whether the CUSTOMER portal should expose a live `execute` and at what capability floor. NOT wired here because no `portal-*` route fires a live Graph write today (confirmed via grep); a customer self-firing a live production Graph write is a genuinely new posture with customer-visible consequences — a real product/security decision, not a guess. The shared lib is surface-agnostic, so wiring the portal route later is a ~15-line addition with no lib change.
  - Working tree clean; only my two files changed + this bookend.
