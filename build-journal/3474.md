# #3474 — Feature: Plan Awareness & Hour Utilization (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:22:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 8d8589d57 (real feature work — RetainerService/RetainerModels,
  status bar wiring)

## Log
- 2026-09-10T06:10:00Z ⏳ IN FLIGHT — pulled #3474 body + both comments per standing
  rules. Comment history:
  1. a `BUILD:` dispatch comment (buildSet `MyArchitect`) carrying
     `--blocked-by 3493,3473` explicitly, and instructing to read
     `desktop/MyArchitect/GEMINI.md`, `BUILD_QUEUE_METHOD.md`, `UI_RULES.md`.
  2. a later `BUILD:` dispatch comment (buildSet `BatterUpClearOut`) that omits the
     `--blocked-by` flag but does not say either blocker cleared, and only reiterates
     the standing "pull body + comments, build per real stated scope" instructions.
- 2026-09-10T06:10:00Z 🛑 BLOCKED — checked the real `blocked_by` dependency edges via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3474/dependencies/blocked_by`: two
  edges exist, to **#3473** and **#3493**.
  - **#3473** ("Retainer hours: fix calendar-month reset to real anniversary-based
    Stripe cycle") is open but carries the `complete` label — the real backend fix
    (the actual scope #3474 needs, per its own body: "that endpoint's period logic is
    currently wrong — #3473 is the real, already-scoped fix") has landed in code.
    This half of the blocker is effectively cleared even though the issue itself
    remains open (Shane's own close).
  - **#3493** ("Feature: UI Shell Redesign (MyArchitect)") is still genuinely open and
    `in-flight`, not `complete`. Its own body states explicitly: "Foundational shell
    scaffolding — must land before other Features' UI work." #3474's real remaining
    scope per its own checklist is UI: "Tenant switcher UI: progress bar (used/retained
    minutes), live-updating on tenant switch" — this is exactly the kind of UI work
    #3493 says must wait. Confirmed no shell work has actually landed:
    `desktop/MyArchitect/` has no Fluent.Ribbon shell, no contextual-tab mechanism, no
    tenant-switcher UI region, and `build-journal/3493.md` does not exist.
  - This matches the identical pattern already recorded by sibling sessions on this
    same Epic (#3454) this session: #3459 (shipped only the non-UI service layer, left
    the Console UI panel blocked on #3493), #3463 (blocked, same reasoning), #3464
    (blocked, same reasoning, edges to #3493 + #3459).
  - Per CLAUDE.md's "blocked_by edges are the gate" rule (Git #1987), the edge — not
    the most recent dispatch comment omitting the flag — is what actually gates the
    queue, and #3493's edge reads open/not-complete. The client call to
    `GET /admin/retainer/:customerId` could theoretically be wired ahead of the shell,
    but it has nowhere real to render into yet (no tenant-switcher UI region exists in
    the current shell to host a progress bar), so building it now would mean
    redoing the wiring once #3493 actually defines that surface — the same
    "would just need redoing" reasoning #3474's own body uses for why #3473 had to
    land first.
  - Left the **#3473** edge in place rather than removing it: `complete` is not the
    same as closed, and the rule only requires removing an edge pointing at something
    "closed or about to close" — #3473 is neither, it's confirmed-done-awaiting-review.
  - Stopping, per the "genuinely unreachable/blocked" protocol: labels synced
    (`in-flight` → `blocked`), both edges left intact (both real and current — #3473
    not closed, #3493 not complete), comment posted on #3474 explaining the wait. No
    code changed. Working tree clean (verified via `git status --porcelain`).
- 2026-09-10T20:10:00Z ⏳ IN FLIGHT — Resumed. Re-checked both `blocked_by` edges
  independently rather than taking either dispatch comment on faith:
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3474/dependencies/blocked_by` now
  returns both **#3473** and **#3493** as `state: closed`, `complete`-labeled. `gh
  issue view 3493` confirms the real shell landed (Fluent.Ribbon, contextual tabs,
  status bar with a `ContractHoursTextBlock` placeholder already scaffolded and
  commented "deferred until #3473/#3474 land"). Both blockers genuinely cleared —
  removed both stale edges, labels synced `blocked` → `in-flight`.
  Built the real remaining scope per #3474's own checklist:
  - `Models/RetainerModels.cs` — `RetainerCustomerWire`/`RetainerSettingsWire`/
    `RetainerBucketWire`/`RetainerDetailResponse`, mirroring `SettingsWire`/
    `bucketToWire` in `admin-retainer.ts` exactly (including the real, corrected
    #3473 anniversary-period fields — `isOverMonth`/`overHours`, uncapped, never
    inferred from `remainingHours == 0`).
  - `Services/IRetainerService.cs` + `Services/RetainerService.cs` — real HTTP
    client for `GET /api/admin/retainer/:customerId`, same
    bearer-token/short-TTL-cache/forceRefresh/typed-exception pattern already
    established by `LaunchControlActionsService` (#3460).
  - `MainWindow.xaml` — replaced the placeholder `ContractHoursTextBlock` with a
    real `ContractHoursPanel` (text + `ProgressBar`, collapsed until real data has
    loaded — never a fake/zeroed bar, per UI_RULES.md §1's "never a fake clickable
    stop for read-only state"). This is the bottom status-bar segment UI_RULES.md
    §1 itself names for contract-hour utilization — not a tenant-switcher popup
    overlay, which the issue body's own checklist phrasing suggested but the
    shell's real, already-scaffolded placeholder and UI_RULES.md's spec both place
    in the status bar instead.
  - `MainWindow.xaml.cs` — `_retainerService` field, fanned the bearer token out
    in `ApplyAuthState` (same single fan-out point every other auth-gated
    service uses), `RefreshContractHoursAsync()` called on tenant switch
    (`OnCurrentTenantChanged`) and on auth state change, rendering
    `{used}h / {retained+rolled}h retained` (or `... · {over}h over`, red bar,
    when `isOverMonth`) into the real progress bar.
  - **Honest remaining block, same shape as #3460 hit**: `TryResolveRetainerCustomerId`
    always returns `false` today — confirmed again `Services/TenantService.cs` is
    still fixture data (`Tenant.Id` is `"tenant-001"`, no real numeric
    `tenants.id`), the exact gap already filed as #3540 (parented under #3457,
    the Feature that owns `TenantService`) by the #3460/#3501 sessions. Did not
    reopen or duplicate that finding, and did not rebuild `TenantService` here —
    that would be scope creep into #3457's own responsibility. Until #3540 lands,
    the status-bar segment stays honestly hidden (`ContractHoursPanel.Visibility
    = Collapsed`) rather than showing invented data — this is real, shipped
    production code operating on the real wire shape, not a fixture.
  - `dotnet build` — 0 errors, 0 warnings.
  - `git status --porcelain` clean at session end aside from this bookend; no
    other files were dirty on arrival.
  - `blocked_by` edge count on #3474: 0 (both cleared, none re-added — nothing
    left blocking this Feature's own real remaining scope, which is #3540, a
    separate Feature's gap).
- 2026-09-10T20:22:00Z ✅ DONE — Feature commit `8d8589d57` pushed to
  `origin/main` (rebased twice against concurrent pushes; `main` moved under
  load both times, retried each time). `verify-branch-merged.mjs` confirms
  `agent/3474-q2163` is a genuine ancestor of `origin/main` — safe to close
  out. `git status --porcelain` clean at session end. Both real checklist
  items from the issue body are shipped: the tenant-switch-live-updating
  status-bar progress bar UI, and the real client call to
  `GET /admin/retainer/:customerId`. The one honest remaining gap
  (customerId resolution) is #3540's scope, not a reopen of this Feature —
  #3474 itself is complete against real, non-fixture code.
