# #2699 — Chat: right-click a real text selection → send it (real markdown, table structure preserved) to a new Test Pad note

- **Status:** ✅ DONE 2026-09-03T18:08:22Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** e64b605fd (implementation), e85fb7120→(rebased) IN FLIGHT bookend

## Log
- 2026-09-03T17:59:44Z ⏳ IN FLIGHT — Sub-issue of #2530 (Feature: Test Pad). Adds a native
  right-click "Send Selection to Test Pad" item to every claude.ai chat WebView2 this app owns.
  Investigation confirmed: no existing DOM-selection capture (the sole `getSelection` hit is
  outbound composer insertion in `FloatingChatBridgeScript`); `TestPadService.AddNote` +
  `NoteContextStamper` are the real receiving end, no new persistence needed;
  `Microsoft.Web.WebView2` 1.0.4129.50 fully supports `CoreWebView2.ContextMenuRequested` +
  `CoreWebView2ContextMenuTarget.HasSelection`. Chat WebViews are wired in
  `MainWindow.InjectBuilderButtonsAsync` (Claude tab + per-chat tabs).
  **Point-1 decision (documented at DONE too):** hybrid — use `ContextMenuRequested` to add the
  native menu item (gated on `HasSelection` so it only shows on a real selection), but do NOT read
  `ContextMenuTarget.SelectionText` (that is flattened word-soup that mangles tables). On click,
  run a JS `window.getSelection()` round-trip that walks the selected DOM range into markdown with
  real table structure preserved — this is what point 2 requires.
- 2026-09-03T18:08:22Z ✅ DONE — Shipped in **e64b605fd**. Two files:
  `Services/ChatSelectionCaptureScript.cs` (new — the getSelection→markdown JS) and
  `MainWindow.xaml.cs` (+92: `WireSendSelectionToTestPad` wired into `InjectBuilderButtonsAsync`,
  plus `CaptureSelectionToTestPadAsync`).
  **Point-1 decision (final): hybrid.** Native `CoreWebView2.ContextMenuRequested` adds the menu
  item, gated on `ContextMenuTarget.HasSelection` so it only appears on a real selection — but the
  content is read via a JS `window.getSelection()` round-trip, NOT `ContextMenuTarget.SelectionText`.
  Reason: `SelectionText` is flattened plain text that turns a rendered table into word-soup
  (defeats point 2). WebView2 SDK 1.0.4129.50 supports all of this. The item appends below the
  default menu items.
  **Point-2 (table structure): done and verified.** `cloneContents()` of the selected range keeps
  the real table/tr/td subtree; the walker emits a genuine markdown table — header row, `---`
  separator, body rows, pipes escaped (`\|`), inline bold/links/code preserved; bare `<tr>` runs
  from a partial selection are reparented into a synthetic table so they still render as a table.
  Falls back to `selection.toString()` only when structural walking yields nothing.
  **Verification:** clean `dotnet build` (0 warnings / 0 errors); JS `node --check` passed; a
  fake-DOM functional harness ran the real extracted walker against a thead/tbody table + heading +
  bold prose and produced exactly the expected markdown table (header + separator + 2 body rows,
  escaped pipe, preserved bold). BuildConsole is out of scope for test manifests (per CLAUDE.md),
  so no manifest written. The `.exe` was not launched (per the never-launch rule).
  **Scope note:** wired into the docked Claude tab + per-chat tabs (`InjectBuilderButtonsAsync`),
  which is where Shane reads through transcript tables; the separate Floating Chat window
  (`FloatingChatWindow`, its own WebView2 init) is not wired — a possible small follow-up, not
  needed for this issue's use case. No findings/bugs discovered. Working tree clean.
