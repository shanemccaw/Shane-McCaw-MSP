# #4464 — change-control.json manifest conflict: Premier bypass vs manually-seeded entitlement

- **Status:** ✅ DONE 2026-09-17T04:35:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 1823c90a7, 73a8d5269
- **Findings filed:** #4488 (no non-Premier RBAC test account exists for change-control-addon-gate.json), parented under Feature #1486, board status "AI Batter Up"

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — confirmed #4462 landed (e0c31ef29, DONE bookend). Investigating change-control.json vs change-control-addon-gate.json.
- 2026-09-17T04:35:00Z ✅ DONE — 1823c90a7 (manifest fix), 73a8d5269 (commit-hash record). change-control.json's ENV VARS note no longer references the retired #1173 manually-seeded entitlement row; it now documents the real #4462/#4463 Premier-tier bypass — tenant 2080 (mccawsoft2, the shared RBAC-ladder tenant) already carries a genuine active client_services Premier row (id 176, from the real shanemccaw+premier@outlook.com purchase in #4452), so the "Customer — RBAC test" account is entitled by tier alone with zero tenant_add_on_entitlements rows. No second account needed. Live-verified: ran scripts/db/reset-rbac-test-accounts.mjs for real (additive/reversible), logged in as the fresh shanemccaw+customer@outlook.com, direct-probed the running :8080 api-server — GET /api/portal/change-control and every other gated read (freeze-windows, maintenance-windows, change-catalog, settings/change-control, metrics) all answered 200, none 402; confirmed `select count(*) from tenant_add_on_entitlements where tenant_id=2080` = 0. change-control-addon-gate.json's PRECONDITION note updated in the same fix: it is now honestly marked stale — the shared tenant it also uses is Premier, so it can no longer serve as the un-entitled/gated case; confirmed live (same probe, 200 not 402). That gap is real and separate from this issue's fix, so it was filed as #4488 rather than patched here (would require inventing a second non-Premier tenant/account, which is real new scope, not this conflict's fix). Also honestly noted in change-control.json: A4/A5 + UI steps CC1-CC5 are not currently green because the dev-DB reset wiped msp_change_requests, leaving tenant 2080's register empty — a real, separate seeding gap (not entitlement), not invented/fabricated here. Both manifest JSON files validated (`JSON.parse`). Working tree clean; branch verified merged into origin/main (verify-branch-merged.mjs OK).
