# #1410 — Wire My Architect + Projects to real status_reports table

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** fe583fd89

## Log
- 2026-08-28 ⏳ IN FLIGHT — Investigated. #1397's clientUserId→customerId-bridge fix already covers `portal-projects.ts` (`/api/portal/projects/:id` already reads `status_reports` scoped via `resolveSiblingUserIds`, correctly — no work needed there). Admin write path (`admin-status-reports.ts`) is real and comprehensive — reuse as-is, no new write route. Projects-page "Documents section" claim in the issue investigated and found unfounded: no such inline status-report listing exists in the design spec or the Projects page; the "Documents" text the issue was likely referring to is My Architect's own `RET_DOCS` fixture rail. Real remaining scope: wire My Architect's `weeklyReports`/`outcomes`/`documents` (`useRetainerLive.ts` / `portal-v2-retainer.tsx`) to real `status_reports` rows, scoped via `resolveSiblingUserIds(req.user!.id)` (users.id bridge), not `clientUserId` directly. Extending `GET /api/portal/retainer` to include scoped, `reportStatus: "sent"` status_reports next.
- 2026-08-28 ✅ DONE (fe583fd89) — `GET /api/portal/retainer` now returns the caller's own SENT `status_reports` rows, scoped via `resolveSiblingUserIds(req.user!.id)`. `useRetainerLive.ts` maps each report into `RetWeek`/`RetOutcome`/`RetDoc` honestly per-field (hours/deliverables have no backing column, stay `0`/`[]`; author reuses the same `retainer_settings.architectName` already read for the header; asks prefer the structured `replyThread`, falling back to the legacy `clientQuestion`/`adminReply` pair). `terms` stays fixture (static policy copy, genuinely no backing table). Updated `useRetainerLive.ts`/`portal-v2-retainer.tsx` header comments and `test-manifests/portal/portal-v2-retainer.json`'s notes to reflect the real wiring. Added 2 new backend tests (sibling-scoping + independence from `configured`) — 6/6 `portal-retainer.test.ts` tests pass. `retainerModel.test.ts` (23 tests) pass. tsc clean on both edited packages (api-server, msp-portal — 2 pre-existing unrelated errors in msp-portal untouched by this change). Ran the full `portal-v2-retainer.json` UI manifest live via `shaneapp://runTest` — 14/14 steps passed (against the deployed dev-server build, which is why the pre-existing "unenrolled" assertions still read `unconfigured` — that's the existing account state, unaffected by this session's changes; no enrolled test account exists to exercise the new `live` weeklyReports/outcomes/documents path end-to-end). Branch verified merged onto `origin/main` before writing DONE (`verify-branch-merged.mjs`). Left as a known, documented gap: `deliverables`/`hours` have no matching `status_reports` column (noted honestly in code, not fabricated); the ask box / accept button / PDF buttons remain visual-only (#1407's decision, unchanged — no customer-facing reply-write route exists yet).
