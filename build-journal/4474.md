# #4474 — admin.test.ts hardcoded tenant id=1

- **Status:** ✅ DONE 2026-09-17T00:00:00Z
- **Scope:** platform

## Log

- 2026-09-17T00:00:00Z — ⏳ IN FLIGHT. Fix per issue's own suggestion: resolve a real
  tenant id live (same discipline as #4413's `resolveFixtureIds()`), skip cleanly if
  none exists.
- 2026-09-17T00:00:00Z — ✅ DONE. Replaced hardcoded `MSP = 1` / `OTHER_MSP = 1626` /
  `TENANT = 1` / `MSP_USER = 1` in `lib/db/src/rbac/admin.test.ts` with a live
  `resolveFixtureIds()` resolved before the suite's outer transaction opens, mirroring
  `./integrity-check.ts`'s existing helper. Wrapped the `describe` block with
  `describe.skipIf(!resolved)` so the suite skips cleanly (rather than failing) if no
  real msp user + second msp + customer-role user currently exist. Live-verified: full
  `admin.test.ts` suite passes (11/11) against the real current local dev DB.
  Commit: e681d1bbe (rebased/pushed as b70472e36 on origin/main).
