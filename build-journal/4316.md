# Build Journal — #4316

- **Status:** ⏳ IN FLIGHT 2026-09-18T03:56:35Z
- **Scope:** platform

## Log

- 2026-09-18T03:56:35Z — Wired `resolveLicenseUpliftCost` (#4580) into `free-scan-sow.ts`'s
  totals: added `annualLicenceUpliftDollars` / `licenceUpliftDetail` to `FreeScanSow.totals`,
  resolved per `summary.licenseGapPurchase.gappedCategories` via
  `LICENSE_GAP_CATEGORIES[key].sku.skuPartNumber`, summing `annualUpliftCents` across
  categories that have a priced SKU on file. Added `skuPartNumber: string | null` to
  `LicenseGapSku` (`license-gap-purchase-links.ts`): `AAD_PREMIUM_P2` for Entra ID P2,
  `ATP_ENTERPRISE` for Defender for Office 365, `null` for Purview Suite (no
  `sku_price_reference` row exists) and for the E7 tier-3 consolidation (never resolved
  per-category). Wired the two Commercial Terms rows in
  `artifacts/shane-mccaw-consulting/src/marketing/pages/FreeScanReview.tsx`: "New licence
  budget" now renders when resolvable, and "Net year one impact" subtracts the uplift figure
  when present. When no gapped category has a priced SKU, both stay omitted exactly as
  before — `resolveLicenseUpliftCost` already logs why per-category.
