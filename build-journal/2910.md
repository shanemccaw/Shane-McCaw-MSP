# Git #2910 — Expected one-time events (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T22:15:00Z
- **Scope:** buildconsole (ShanesSurvival desktop, not platform)

## Log
- 2026-09-04T00:00:00Z — Session started. Real scope from #2910: new `expected_one_time_events`
  table + 3 MCP tools (record_expected_event, get_expected_events, mark_event_realized).
  Deliberately NOT linked into gate_status/bill_status shortfall math this pass (per issue).
- 2026-09-04T22:15:00Z — ✅ DONE — Added `migrations/010_expected_events.sql` (ran it myself
  against local `finances` Postgres DB — real next-free number confirmed fresh off
  `migrations/`, 009 was the last one present). Added `ExpectedEventRepository`
  (`src/ShanesSurvival.Core/ExpectedEvents/`) and `ExpectedEventTools` MCP tools
  (`record_expected_event`, `get_expected_events`, `mark_event_realized`), registered in
  `ShanesSurvival.Mcp/Program.cs`. `mark_event_realized` does NOT write into
  debts/income_entries/any account balance, and is idempotent (verified: calling it twice on
  the same event both returned success, second call a safe no-op). Real end-to-end MCP
  verification: ran the actual compiled MCP server over stdio JSON-RPC, called
  `record_expected_event` for the real $2,500 deductible (outflow) and $6,000 contingent
  reimbursement (inflow), confirmed both rows via direct psql against `finances`, called
  `mark_event_realized` on the deductible twice (idempotent, `realized_at` set once,
  `get_expected_events` then correctly excluded it), deleted both TEST rows after —
  `expected_one_time_events` count back to 0. `dotnet build ShanesSurvival.sln` clean (0
  warnings, 0 errors). No linkage into gate_status/bill_status shortfall math, per the issue.
  Commit `a4f6be47` (rebased onto and pushed to `main`; branch confirmed merged via
  `verify-branch-merged.mjs`).
