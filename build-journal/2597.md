# #2597 — SOPs: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:20:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** `aeb954127`

## Log
- 2026-09-12T02:40:00Z ⏳ IN FLIGHT — #3677 (MSP Console Shell) confirmed landed on
  `main` (`7888bf1d6`, merge-base verified). Read
  `Design/MSP_Console/design_handoff_msp_console/README.md` + the `isSops` branch of
  `MSP Console.dc.html`'s logic class (the standalone `SOPs.dc.html` export is an
  empty stub — the combined prototype's logic class is the real spec, screen 31).
  Read `artifacts/api-server/src/routes/msp-sops.ts` and the `msp_sops`/`msp_sop_runs`
  schema in `lib/db/src/schema/msp.ts`.
- 2026-09-12T02:55:00Z ⏳ IN FLIGHT — Added `PATCH /api/msp/sops/:sopId` to
  `msp-sops.ts` (real backend gap: only GET/POST existed, no way to edit or version
  an authored definition — HARD RULE "when the backend doesn't serve what the surface
  needs, BUILD IT"; no schema/migration needed, every field already existed). Wired
  `ConsoleShell`/`ScreenSlot` to mount a module page for `sel.kind==="msp" && page
  === "sops"`; gave the SOPs page its own design-exact eyebrow/title/note in
  `treeModel.ts`. Built `src/pages/Sops.tsx` (Library + Run history tabs per the
  design's own `hasSubTabs`/`subTabs` for `isSops`) and `src/api/sops-api.ts` (real
  fetch hooks: `useSops`, `useCreateSop`, `useUpdateSop`, `useSopRuns`, `useRunSop`,
  no fixtures). Library: category filter chips derived from real data (category-tree
  management), row → definition drawer (view/edit/bump-version via the new PATCH,
  workload/compliance tag attachment), "New SOP" → create via POST, "Run" → run
  drawer (customer picker from the real directory, POST .../run). Run history: table
  from `GET /api/msp/sop-runs`, row → run-detail drawer with full record + logs
  (run-record review). Added the handful of missing Lucide icons the page needs to
  `console/icons.tsx`'s registry.
- 2026-09-12T03:05:00Z ⏳ IN FLIGHT — Verification: `npx tsc --noEmit` clean in
  `artifacts/msp-console`; `PORT=5199 npx vite build` clean (1756 modules, 105.62 kB
  gzip JS). `npx tsc --noEmit` in `artifacts/api-server` clean for `msp-sops.ts` (no
  errors reported against that file). No new automated test coverage was added for
  the new PATCH route (no existing `msp-sops` test file to extend, and this repo's
  vitest config uses an explicit include-allowlist for api-server — out of scope to
  stand up a new suite file for one route in this pass); this is an honest gap, not
  a claimed-covered one. `runTest`/live UI click-through was not attempted this
  session — no live logged-in MSP operator session was available to drive it; the
  unauthenticated path (sign-in-required) is the console's own designed correct
  behavior per `AuthGate.tsx`.
- 2026-09-12T03:20:00Z ✅ DONE — landed on `main` as `aeb954127` (rebased twice past
  two concurrent MSP Console module builds — #2588 Remediation Tracking and a
  Runbooks page — that touched the same shared `ConsoleShell.tsx`/`icons.tsx`;
  resolved both conflicts by merging module registrations into the existing
  `moduleFor(sel)` switch rather than overwriting either). `verify-branch-merged.mjs`
  confirms `agent/2597-q2389` is an ancestor of `origin/main`; `git status
  --porcelain` clean. `tsc --noEmit` and `vite build` re-verified clean after the
  rebases. No findings filed this session — no genuine backend/product gap
  discovered beyond the missing PATCH route, which was built rather than filed.
