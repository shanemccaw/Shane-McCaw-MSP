# #3695 — Managed Tenants (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-11T20:55:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** ae7d50779 (bookend IN FLIGHT), 9cb30c587 (contract pack doc)

## Log
- 2026-09-11T20:35:00Z ⏳ IN FLIGHT — read #3695 + #3666, `msp-customer-directory-metrics.ts`,
  the `GET /api/msp/customers` route, and the design's directory-row shape; live-queried local
  Postgres and exercised the real resolver via `tsx` against both real tenants. Writing the
  read-only contract pack under `docs/msp-console/`.
- 2026-09-11T20:55:00Z ✅ DONE — wrote
  `docs/msp-console/managed-tenants-directory-msp-console-contract-pack.md`, a read-only
  extraction pack for the #3666 directory-metrics resolver behind `GET /api/msp/customers`.
  Every field cited to file:line and cross-checked live: raw-table psql queries for
  `tenant_signal_history`/`msp_diagnostic_runs`/`tenant_monitor_profiles`, plus an in-process
  call (via `tsx`, unmodified source, scratch script deleted after) to the real
  `fetchCustomerDirectoryMetrics()` against both real tenants (id 1 "Jane Jane", id 3 "Test
  Me") — all four figures matched the raw-table cross-check exactly, including one genuine
  `null` (customer 3's `people`, no `identity:department-directory` row exists for it). No
  product code, schema, or UI touched — pure documentation. No new finding filed; §6 of the
  pack notes a UI-only concern (design mock's `dot`/`signalColor` presentational fields have no
  server equivalent, and don't need one) for a future wiring session, not a backend gap.
  `verify-branch-merged.mjs` confirms `agent/3695-q2356` (9cb30c58) is an ancestor of
  `origin/main`; `git status --porcelain` clean.
