# #3915 — Focus bar ETA uses local ClosedSamples instead of real GitHub calendar history

- **Status:** ✅ DONE 2026-09-13T02:48:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** ef9b11bdc00a929c0c50fbf8415e819d1d6b9c16

## Log
- 2026-09-13T02:23:45Z ⏳ IN FLIGHT — Investigating `FocusModeService.BuildProgress`, `_state.ClosedSamples`, and `HomeEtaProjectionService`/`GitHubIssueTimeSeriesService.BuildSeries` to wire the Focus bar ETA to the same real calendar-based GitHub history source.
- 2026-09-13T02:48:00Z ✅ DONE — Rewired `FocusModeService.BuildProgress` (now `BuildProgressAsync`) to fit its ETA window/totalWindow from `GitHubIssueTimeSeriesService.GetMilestoneSeriesAsync` (real per-day local-mirror history), the exact source `HomeEtaProjectionService` already uses for the Home dashboard. `RecomputeGame`/`Activate`/`UpdateBoardSnapshot`/`SetProductionScopeOnly` became async to carry the `await` through; all 3 real call sites (`FocusModeBar.xaml.cs` x2, `LeftSidebar.xaml.cs` x1) updated. Audited `ClosedSamples`/`RecordClosedSample`/`FocusClosedSample`: exactly one reader + one writer, both inside `FocusModeService.cs`, no other real consumer in the app (grep-confirmed) — retired the class and the persisted `FocusPersistState.ClosedSamples` list entirely; a pre-existing `focus-mode.json` with that array round-trips fine (System.Text.Json ignores unmodeled properties). `dotnet build` succeeded, 0 warnings/0 errors. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. No findings filed — no unrelated gaps discovered this session. Working tree clean at close.
