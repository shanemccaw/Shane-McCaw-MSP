# #2840 — Sanctioned dummy test user in the testbed tenant for destructive per-user Graph writes

- **Status:** ✅ DONE 2026-09-06T15:12:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `dfdaf51e5` (real work), `e63b288ad` (IN FLIGHT bookend)

## Log
- 2026-09-06T14:40:00Z ⏳ IN FLIGHT — Shane's decision on #2840: provision a real, dedicated, non-privileged test user in the testbed tenant as the sanctioned target for destructive per-user Graph write testing (delete auth methods, revoke sessions, disable sign-in). Plan: create the user via Graph using the DEV app registration `9f6f4772-…` (agent-modifiable per the Git #1913 gate), verify it holds no directory roles and no group memberships, document its real UPN/objectId in a real doc, and wire it into the scripts/manifests that need a destructive-write target — starting with #1899's `mfa-enforcement-v1` re-registration mechanism.

- 2026-09-06T15:12:00Z ✅ DONE — **The account exists, and #1899's DELETE has now actually fired against a real directory object for the first time.**

  **The account:** `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com`, objectId `bdb21dc3-146a-4d97-a128-a2b8ff618d35`, displayName `ZZ TEST ACCOUNT - destructive Graph write target (Git #2840) - DO NOT USE`, created `2026-09-06T14:22:23Z`. **No directory roles, no group memberships, no licences** — verified by a real `transitiveMemberOf` read, not asserted. Password generated at creation and written to the untracked `.env.local` as `TESTBED_TEST_USER_PASSWORD`; never committed, never in an issue comment.

  **Production-change gate (Git #1913) handled deliberately.** Confirmed the target before writing anything: `GET /organization` returned tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3`, display name **McCawSoft, Inc.**, verified domains including `mccawsoft.com`/`barricademsp.com` — knowingly Shane's real production tenant, entered on his explicit dispatch instruction. Every write went through the **DEV** write app registration `9f6f4772-b5be-421f-815e-b392336c373a`, never the PROD one. That is now mechanical rather than a matter of care: `scripts/azure/testbed-test-user-2840.mjs` refuses the PROD registration `3308b280-…` by appId, refuses any tenant but the testbed one, refuses any UPN outside the reserved `zz-test-*@mccawsoft2.onmicrosoft.com` shape (`assertReservedUpn`), and requires an explicit `--i-mean-it` on every mutating mode.

  **Real destructive writes actually executed** (real calls, real responses, not "the permission is granted therefore it would work"): `POST /users` → **201**; `POST /users/{id}/revokeSignInSessions` → **200 OK**; `POST /users/{id}/authentication/phoneMethods` → **201** (`+1 5555550142`, reserved fictitious 555-01xx range, cannot reach a real handset); and **#1899's fan-out** `GET /users/{id}/authentication/methods` → `DELETE /users/{id}/authentication/phoneMethods/{methodId}` → **204 OK**. #1899 shipped that DELETE unit-tested with the permission grant live-verified but the call itself never once fired, for want of a safe target. Account left clean afterwards — only the non-deletable `passwordAuthenticationMethod` remains.

  **Shipped:** `scripts/azure/testbed-test-user-2840.mjs` (the single entry point, exporting the identity constants); `docs/testbed-destructive-write-test-user-2840.md` (the dedicated record — UPN, objectId, reserved naming convention, safety rails, full verification table, and what it explicitly does *not* unlock); a `CLAUDE.md` subsection under the Production-change gate so a future session finds the sanctioned target instead of concluding none exists (which is exactly what #1899 did); and a cross-reference in `docs/account-security-contract-pack.md` §8 noting this account does **not** close the separate portal-identity-link gap there.

  **Three stale doc claims this work falsified, corrected in the same commit:** (1) `docs/write-app-permissions.md` still listed `UserAuthenticationMethod.ReadWrite.All` as "deliberately not requested" on the grounds its endpoint was not a real Graph collection — #1899 fixed that and the permission is granted + admin-consented on the DEV app (confirmed by a live `verify-write-app-consent-645.mjs` readback), so it moved to the requested set as entry 17 and the heading now reads 17, not 16; (2) `docs/writepackmapping.md:121` still showed the pack step as `POST …/authentication/methods` — the stored `baseline_action_templates` row is `GET`, confirmed by querying the local DB directly; (3) `docs/writepackmapping.md` §9 unknown 1 claimed no successful write payload could be obtained without a forbidden mutation — partially retired with the four real status codes above, while saying plainly that the `assignLicense` body and a completed pack run's finalization payload are still genuinely unobtained.

  **Finding filed — #2981** (`bug` + `security`, parented under **#2489** `Feature: Write-App/Graph Permission Engine`, milestone 5): `mfa-enforcement-v1` reports success after deleting nothing when the Entra methods read is stale. Observed live doing the above — the seed→purge sequence enumerated **1 method, 0 deletable** and silently deleted nothing seconds after the phone method returned 201, and two consecutive `GET`s of the same endpoint *within one process* disagreed (2 vs 1). #1899's executor enumerates and deletes in one pass and reports success on a `0 deletable` read, so a stale replica read is indistinguishable from "this user has no MFA methods." That matters most in `compromised-account-recovery-v1` / `security-incident-response-v1`, where an attacker-enrolled factor minutes old is the whole reason the pack runs. Suggested fix is in the issue; deliberately not applied here.

  **Honest gaps:** only the `phoneMethods` branch of #1899's three-way fan-out was exercised against a real object — Microsoft Authenticator and software-OATH methods cannot be seeded app-only (both need interactive registration), so those two branches stay unit-tested only; same code path, different URL segment, but that is inference, not evidence. No pack was run end-to-end against this account, only the individual Graph calls. Password/privileged-user operations still need a directory role on the app, which is a production change and not agent work. **#2981's board status could not be set to "AI Batter Up"** — that flip is GraphQL-only and GitHub's GraphQL API returned `API rate limit already exceeded for user ID 304104309` on five attempts across ~10 minutes while REST kept working throughout (which is how #2981 got created and parented at all); reset was ~1h out, so per CLAUDE.md's bounded-wait rule I stopped and flagged it in the completion comment rather than looping. #2981 sits at the project default status and needs a manual nudge.

  No test manifest, correctly — manifests are scoped to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`; this is api-server/Graph tooling, and the verification is the real live Graph calls above.

  Branch verified merged into `origin/main` (`verify-branch-merged.mjs` exit 0, `dfdaf51e`). `git status --porcelain` clean; nothing was dirty on arrival.
