# Git #2100 — Malformed JSON body returns 500 instead of parser's own 400

- **Status:** ✅ DONE 2026-09-04T06:20:47Z
- **Scope:** platform

## Log
- 2026-09-04T06:18:40Z — Fixing global error handler at `artifacts/api-server/src/app.ts` to
  respect a caught error's declared `statusCode`/`status` + `expose` fields (the standard
  body-parser/`express.json()` contract) instead of collapsing every non-`ConsentRevokedError`
  to a hardcoded 500. Wired through `apiError()` / `ApiErrorCode.VALIDATION` from
  `lib/api-helpers.ts` so the response carries the platform's standard error envelope
  (`{ error: { code, message, traceId } }`), per the issue's own suggested fix.
- 2026-09-04T06:20:47Z — ✅ DONE — `tsc --noEmit` passes clean. Commit
  `81ce663a36930d182b86673af95ade1ddcf12878` merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs` → OK). Working tree clean
  (`git status --porcelain` empty). A malformed-JSON body now returns the parser's own
  400 in the platform's standard `{ error: { code: VALIDATION_ERROR, message, traceId } }`
  envelope instead of a hardcoded 500, matching the fix the issue itself specified.
