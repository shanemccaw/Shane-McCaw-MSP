# #3489 — Feature: Audit Log (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T06:24:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** none — no feature code changed this session (blocked on #3493)

## Log
- 2026-09-10T06:22:00Z ⏳ IN FLIGHT — pulled #3489 body + both `BUILD:` comments per
  standing rules, read `desktop/MyArchitect/GEMINI.md` and skimmed `UI_RULES.md`.
  Comment history:
  1. `BUILD:` dispatch (buildSet `MyArchitect`) carrying `--blocked-by 3493` explicitly,
     instructing to read GEMINI.md / BUILD_QUEUE_METHOD.md / UI_RULES.md.
  2. later `BUILD:` dispatch (buildSet `BatterUpClearOut`, the one this session was
     actually launched with) that omits the `--blocked-by` flag but does not state
     #3493 cleared — only reiterates the standing "pull body + comments, build per
     real stated scope" instructions.
  Issue body's own checklist is one item: "Client: audit log view, filterable by
  tenant/action type" — entirely client/UI work, against the real, confirmed
  `GET /msp/audit` endpoint (`artifacts/api-server/src/routes/msp-audit-log.ts`).
- 2026-09-10T06:24:00Z 🛑 BLOCKED — same as siblings #3480 / #3482 / #3483. Findings:
  - **All of #3489's scope is client UI**, with no backend gap to close — the audit
    endpoint already exists and is real.
  - **Confirmed #3493 (Feature: UI Shell Redesign) is genuinely unbuilt**:
    `MyArchitect.csproj` has no `Fluent.Ribbon` reference; `MainWindow.xaml` still
    uses the old activity-bar/icon-per-tool chrome (`ActivityBarRadioItemStyle` /
    `ActivityBarButtonStyle`) — the exact "mosh pit" pattern #3493 exists to replace,
    not the 6-tab Ribbon + left/right panel model. `build-journal/3493.md` does not
    exist, and #3493 is open + `in-flight` on GitHub (not `complete`).
  - **GEMINI.md standing rule is explicit**: "UI Shell first, no exceptions... If
    #3493 isn't done yet, do not build ad-hoc chrome (a new icon, a new panel) to
    unblock yourself — stop and flag it instead." Building an Audit Log view now
    would mean inventing throwaway chrome that conflicts with a decision already
    recorded on the epic; the CLAUDE.md "BUILD IT" rule doesn't override this since
    the gap here is UI-shell scaffolding, not a missing backend.
  - **Gate (Git #1987):** a real `blocked_by` edge #3489 → #3493 already existed
    (`GET .../3489/dependencies/blocked_by` returned #3493, open/`in-flight`, not
    stale/closing) — verified current and left in place, not re-created. Synced
    labels on #3489 (`blocked`, removed `in-flight`) and posted an explanatory
    comment.
  - **Unblock path:** once #3493 lands (fixed tabs + right-panel record workspace
    model), wire the Audit Log view into whichever fixed tab UI_RULES.md places it
    on, reading from the existing `GET /msp/audit` endpoint — no new backend work
    needed.
