# #1875 — Write app refused 403 Authorization_RequestDenied on the testbed

- **Status:** ⏳ IN FLIGHT — code merged and verified; the tenant consent grant itself
  could not be executed from this session (see below). Deliberately NOT marked DONE,
  because #1875's step 4 ("consent it and prove one real write succeeds") is not met.
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `08e130170`, `4c02b4b78` (both merged into `origin/main`; ancestor check passed)

## Log

- 2026-08-30 ⏳ IN FLIGHT — Establishing the real consent state of the write app, deriving
  the least-privilege Graph application-permission set from the four write packs' actual
  steps, and making the confirmation UI render a real "write refused by tenant permissions"
  state instead of showing a 403-guaranteed action as ready to fire.

- 2026-08-30 ✅ SHIPPED (steps 1, 2, 3 and 5 of the issue) — `08e130170`, `4c02b4b78`.

  **1. Real consent state, read from Entra rather than trusted.** Minted a real
  client-credentials token and read its `roles` claim, then read the app registrations and
  `appRoleAssignments` directly. Findings:
  - `.env.local`'s `MT_APP_WRITE_CLIENT_ID` is `9f6f4772-…` ("MSP Platform Write (DEV)"),
    **not** `3308b280-…` as #1875, the `graph.ts` comment and `docs/writepackmapping.md` all
    state. Two distinct registrations, both with an SP in the testbed tenant. (→ #1901)
  - Root cause of run 30301's 403, proven by timestamp: the prod write app was granted
    `User.ReadWrite.All` at **2026-08-26T16:14:59Z**, 47 minutes *after* the run failed at
    15:27. It genuinely had no user-write permission at that moment. Not a credential problem.
  - Both write apps hold 4 Graph permissions today. `REQUIRED_WRITE_APP_PERMISSIONS` named
    the same 4 — accurate but badly incomplete.

  **2/3. Derived the set from the packs' real steps, with per-step justification.** Enumerated
  every write from the DB (`config_pack_templates` → `baseline_action_templates` for the four
  executable packs, plus all 14 micro-remediations and the two provisioning helpers), then
  read each operation's least-privileged **Application** permission from its Microsoft Learn
  permissions table. Result: **16 permissions, not 4.** `quickstart-v1` alone is missing 6.
  - `artifacts/api-server/src/lib/graph-write-permissions.ts` — the rule table. Each rule
    cites the Learn page and names the product step. `REQUIRED_WRITE_APP_PERMISSIONS` is now a
    union over it, so the old hand-transcription (and its documented drift risk) is gone.
  - `docs/write-app-permissions.md` — the customer-security-review document.
  - Two permissions are documented but deliberately **not** requested:
    `DeviceManagementManagedDevices.PrivilegedOperations.All` (would confer tenant-wide Intune
    wipe for a $29 sync action → #1903, Shane's decision) and
    `UserAuthenticationMethod.ReadWrite.All` (its step's endpoint doesn't exist → #1899).
  - No `Directory.ReadWrite.All` / `Group.ReadWrite.All` / `Organization.ReadWrite.All` /
    `Sites.ReadWrite.All`; a test asserts their absence.

  **5. The confirmation UI now renders a real refusal state.** `graphWriteForTenant`'s two
  existing gates cannot catch this class — run 30301 passed both and still 403'd — so:
  - `getGrantedWriteAppPermissionsForTenant` (graph.ts) reads what a tenant has *actually*
    admin-consented, from Entra.
  - `GET /api/admin/write-permissions?customerId=` reports all three gates separately plus a
    per-pack / per-micro-remediation verdict.
  - `CatalogTesting.tsx` disables "Run (real)" unless permissions are **affirmatively
    verified**, and renders "Write refused by tenant permissions" listing the missing ones.
    A failed check, or a step with no permission rule, renders as *unverified* — never green.
    (That last case was a bug in my own first cut, caught by the live run below.)

  **Verification (real, not asserted):**
  - `graph-write-permissions.test.ts` — **29/29 pass** (registered in `vitest.config.ts`).
  - `tsc --noEmit` on api-server and admin-panel: **no errors in any file this session
    touched**. Both packages have a pre-existing error baseline in files I did not touch
    (`ai-*`, `sow-claim-binding-560.test.ts`, `BuildTrackerExplorer.tsx`) — left alone.
  - Ran the real derivation against **live Entra + real DB rows**: 13 of 14 packs and 11 of 12
    Graph-transportable micro-remediations correctly report REFUSED with their exact missing
    permissions; only `remediate-remove-waste-license` is genuinely runnable today. Previously
    every one of them rendered green.
  - `scripts/verify-write-app-consent-645.mjs` re-run against the real tenant: 4 granted,
    12 missing.

- 2026-08-30 ⛔ NOT DONE — **step 4 (consent + prove one real write) was not executed.**
  Both attempts to make a write-plane change against Shane's production M365 tenant were
  refused by this session's permission classifier: first a non-mutating Graph
  POST/PATCH/DELETE authorization probe, then `az ad app update`. `az` *is* logged in as
  `smccaw@shanemccaw.com` on the tenant, so the capability exists — this session is simply
  not permitted to use it. I stopped rather than routing around it.

  Left instead: `scripts/azure/apply-write-app-permissions-1875.mjs`, which imports the
  derived list (no restatement to drift), dry-runs by default, snapshots the prior
  `requiredResourceAccess` so `--revert` works, and only ever adds. **Its dry run was executed
  and produces the correct plan** (+12 on each of the two registrations).

  ```
  node scripts/azure/apply-write-app-permissions-1875.mjs --apply --consent
  node scripts/verify-write-app-consent-645.mjs
  ```

  Then `quickstart-v1` end-to-end for the live write proof — note step 1 creates a real user
  in the production tenant and every step is `reversible = false`, so that needs Shane's own
  go-ahead. Issue labelled `Shane To-Do`, **not** `complete`.

- **Findings filed:** #1899 (mfa-enforcement-v1 step 1 targets a non-existent Graph endpoint),
  #1900 (break-glass password persisted in plaintext in `wf_run_node_outputs`), #1901
  (`docs/writepackmapping.md` names the wrong write app id), #1902 (13 Graph endpoints still
  unmapped in the rule table), #1903 (decision: Intune privileged permission vs dropping
  `remediate-device-compliance-gap`). All parented to #1096, board status "AI Batter Up".

- **Working tree:** clean (`git status --porcelain` empty). No pre-existing dirty files were
  present on arrival. No migrations were needed, so nothing was appended to #1630.
