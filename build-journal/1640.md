# #1640 — Build Queue: critter + 💤 badge on the restart-pending card

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** af1236b91

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a critter + 💤 corner badge to `BuildRestartCard` in
  `Controls/BuildQueuePanel.xaml.cs`, reusing `CreateGenericCardMascot` (the existing
  #1803 generic-mascot helper) with a stable seed and a calm sapphire badge distinct
  from the red 🔒 blocked badge. Additive only — not touching existing blocked/
  waiting-for-input/verifying badge logic.
- 2026-08-30 ✅ DONE — `BuildRestartCard` now wraps its content in the same 2-column
  Grid pattern `BuildQueueCard` uses (content col 0, mascot col 1). Critter is
  `CreateGenericCardMascot(seed, CritterMood.Normal, isBlocked: false)`, with
  `seed = p.GithubNumber ?? p.Title.GetHashCode()` — `PersistedQueueDisplayItem`
  carries no `Id`, only `Title` + optional `GithubNumber`, so the GithubNumber is
  used when present (matching the same field `BuildQueueCard`/`CreateQueueCardMascot`
  key off), falling back to a stable hash of `Title` for restart items without one —
  either way the same item always draws the same critter across repaints. Added a
  `💤` corner badge in calm sapphire (`#89B4FA`), same Border/DropShadowEffect/
  Canvas-position visual language as the existing `🔒`/`💬` badges, but visually
  distinct from the red `🔒` blocked badge. Did not touch the existing isBlocked/
  waiting-for-input/verifying badge logic on `BuildQueueCard` or
  `CreateGenericCardMascot`'s own lock-badge branch — additive only.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  warnings, 0 errors. Committed af1236b91, pushed to `origin/main`, verified
  `git merge-base --is-ancestor HEAD origin/main` — MERGED. `git status --porcelain`
  clean at time of writing.
