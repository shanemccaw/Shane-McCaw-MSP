# #4423 — mccawsoft2 testbed tenant + account repeatedly deleted by unidentified concurrent local-dev-DB activity

- **Status:** ✅ DONE 2026-09-17T03:40:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 4d4352d39 (fix, merged to origin/main)

## Log
- 2026-09-17T02:55:00Z ⏳ IN FLIGHT — starting investigation: grepping for reset-dev-database.mjs / reset-rbac-test-accounts.mjs invocations outside manual runs, checking vitest teardown targets, and checking BuildConsole for any scheduled/automatic re-dispatch of stale comments.
- 2026-09-17T03:40:00Z ✅ DONE — Root cause identified with hard evidence, not guessed:

  **Ruled out** (dispatch's items 2 and 3): no vitest suite targets `DATABASE_URL`
  destructively — `lib/db/vitest.config.ts`'s live-DB tests all run inside
  `BEGIN...ROLLBACK` transactions, and `#4397`'s live-db test seeds/deletes only its
  own tagged synthetic rows by id. BuildConsole has no scheduled/automatic job that
  re-runs dispatch instructions or re-executes a stale comment — the reset gates
  (`DevDatabaseResetGate`, `RbacTestAccountsResetGate`, `CleanDevDatabaseChainGate`)
  all require a typed confirm phrase inside the interactive Command Palette.

  **Confirmed root cause (dispatch's item 1, exactly):** the real `scripts/db/
  reset-dev-database.mjs --yes` mode was run for real **six times in roughly 10
  hours** against the shared local dev DB — not by a cron job, but by five separate
  build sessions each treating "live-verify your fix" as license to run the actual
  destructive op against shared state while building/fixing the script itself or
  its BuildConsole Command Center gate wrapper. Matched every one of the 6 real
  `pg_dump` backup files on disk (`C:\Source\ShaneMcCawConsulting\db-backups\`,
  timestamps `164145374Z`/`164638947Z`/`185501841Z`/`195645136Z`/`011237962Z`/
  `021651554Z`) to its own build session's bookend:
  - #4393 (built the script itself): 2 real runs during its own "live verification,"
    16:41:45Z and ~16:46:38Z — its own bookend describes "only msp-1's own McCawSoft
    tenant deleted," not realizing at the time that tenant was the real mccawsoft2
    walkthrough #4318 had just consented.
  - #4415 (built the Command Center gate): 1 real run at 18:55:01Z as test-case E of
    its headless-probe verification (typed the gate's own real confirm phrase).
  - #4417 (built the 3-step "Clean Dev Database" chain): 1 real run at 19:56:45Z,
    explicitly logged in its own bookend ("tenants msp #1 1→0") as part of verifying
    the chain end-to-end for real.
  - #4436 (fixed a credential-leak bug in the script): 1 real run at 01:12:37Z
    reproducing the failure scenario.
  - #4437 (fixed a backup-completion-reporting bug): 1 real run at 02:16:51Z —
    its own bookend candidly documents this as an "incident during verification,"
    caught and restored via `pg_restore` immediately after.

  Confirmed live: the real mccawsoft2 tenant (id 2080, tenant_id
  `c4c814d4-3afe-441e-9145-62461d0a4fd3`) and its `shanemccaw+premier@outlook.com`
  account are present right now (#4437's own restore), independent of this build's
  fix.

  **Fix — a structural guard, not another confirmation dialog:** added
  `tenants.reset_protected` (migration
  `lib/db/migrations/manual/4423-tenants-reset-protected.sql`, run by me against
  local `DATABASE_URL`). `reset-dev-database.mjs` now refuses its **entire** run —
  dry run included, so the BuildConsole gate's confirm phrase never arms either —
  when any tenant in the target MSP's scope carries the flag, regardless of whether
  the script is invoked directly, through the Command Center gate/chain, or through
  an agent's own verification harness, because the refusal lives in the script's own
  shared logic, not a UI layer. Flagged the real mccawsoft2 tenant (id 2080)
  protected. Documented in `scripts/db/README.md` and added a standing CLAUDE.md rule
  (Database section) naming #4423 as the concrete precedent: never live-verify a
  destructive script/gate by actually running its real destructive mode against
  shared state.

  **Live-verified, not just theorized:**
  - `node scripts/db/reset-dev-database.mjs --dry-run` run for real against the live
    DB with tenant 2080 protected → refuses immediately, exit 1, prints exactly
    which tenant blocked it and the manual-unprotect SQL; zero rows changed
    (confirmed by direct query before/after).
  - Confirmed the "no protected tenants" path still returns empty (no false-positive
    refusal) by running the same guard query against an msp scope with none.
  - Created a synthetic verification tenant (`zz-test-4423-…`, id 2107) per this
    issue's own standing instruction, let it and the real tenant (2080) sit through
    ~10 minutes of real wall-clock time while doing the rest of this build's work,
    re-queried — both still present, `reset_protected` flags unchanged. Deleted the
    synthetic tenant afterward (it was created only to prove the fix).
  - `pnpm exec tsc --noEmit` clean in `lib/db` and `artifacts/api-server` (schema
    change only adds a nullable-with-default column).
  - Ran the full `@workspace/db` vitest suite as a regression check: 9/10 files
    clean; 1 pre-existing, unrelated failure found and filed (see below).

  **Finding filed (sibling of #4270, not the fix, genuinely pre-existing):**
  [#4474](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4474) —
  `lib/db/src/rbac/admin.test.ts` hardcodes `TENANT = 1`, which no longer exists
  after any dev-database reset (serial ids never reused) — same class of bug #4413
  already fixed elsewhere, missed here. Confirmed unrelated to this session's schema
  change. Labeled `bug`, parented under #4270, board status "AI Batter Up."

  No fixture/hardcoded data added to the product. Working tree clean;
  `verify-branch-merged.mjs` confirms `4d4352d39` merged into `origin/main`.
