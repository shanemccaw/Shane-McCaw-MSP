# #4062 — Dashboard overviewCounts: POA&Ms, open risks, SOPs, change approval/freeze, Microsoft Changes, never-scanned scores (#4062-#4068)

- **Status:** ✅ DONE 2026-09-14T17:01:48Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 7ef8afab4 (implementation), ce5705438 (manifest verification notes)

## Log
- 2026-09-14T15:32:24Z ⏳ IN FLIGHT — dispatched as one coordinated build covering 6 real findings (#4062, #4063, #4064, #4065, #4066, #4068) all touching `GET /api/portal/dashboard`'s `overviewCounts` in `artifacts/api-server/src/routes/portal-customer-engines.ts`. Parent Feature is #1943 (Dashboard (Portal)). Investigated each route's real backend (portal-poams.ts, portal-risk-register.ts, portal-sops.ts, portal-change-control.ts, portal-message-center.ts) before writing any code. Backend-only pass — no Dashboard UI exists yet (no Design export under Design/portal/).
- 2026-09-14T17:01:48Z ✅ DONE — all 6 findings fixed in `artifacts/api-server/src/routes/portal-customer-engines.ts` (commit 7ef8afab4):
  - **#4062**: `overviewCounts.poamsOverdue` + `poamsApproachingExpiry` ({within7,within30,within60,within90}), real `msp_poams` read scoped by (mspId,tenantId), only `status==="active"` + `!deletedAt` rows counted (matches `computeOverdue()`'s own rule in portal-poams.ts).
  - **#4063**: `overviewCounts.openRisks` (real `msp_risk_decisions.risk_status==='Open'` count) + `openRisksWithPlan` (linked via shared `checkKey`/`additionalCheckKeys` against live — draft/pending_signature/active — POA&Ms; `spawnedByPoamId`/`convertedToPoamId` were confirmed to be one-time conversion pointers, a different relationship, not usable for this).
  - **#4064**: `overviewCounts.sopsRunning` + `sopsQueued` (real `queueStateFor()` split over `msp_sop_runs`) + `sopsExecsThisMonth` (the aggregate fallback the issue itself named, via `isSameUtcMonth()`), since no "waiting on customer" state exists separate from Queued.
  - **#4065**: `overviewCounts.changeRequestsAwaitingApproval` (reuses `portal-change-control.ts`'s own `displayStatus()`) + `freezeActive` (reuses `isWindowActiveAt()` from `portal-change-freeze.ts`), both gated on the same `change_control` entitlement the maintenance-window read already checks.
  - **#4066**: `microsoftChangesThisWeek` now counts `kindForPost()==="d"||"b"` over the whole Message Center corpus instead of a raw 7-day date window (same field name, corrected meaning, per dispatch instruction).
  - **#4068**: `scores.*` now reads `null` instead of `?? 0` for a never-scanned engine, matching `results.summary.compositeScore`'s existing null pattern.

  Verification: `tsc --noEmit` on `artifacts/api-server` passed clean (exit 0). Extended the existing `test-manifests/portal/customer-home-overview.json` (discover-before-create — this manifest already covers `GET /api/portal/dashboard`) with 9 new `apiTests` steps under `sessionTag: "4062-dashboard-rollups"`, each asserting real values cross-checked directly against live local Postgres (`DATABASE_URL`) for the testbed tenant (customer_id=1): `poamsOverdue=0` (its one POA&M is soft-deleted), `openRisks=1`/`openRisksWithPlan=0` (one real Open risk, null checkKey, nothing to link), `sopsRunning=0`, `changeRequestsAwaitingApproval=1` (one real pending_approval CR), `freezeActive=false` (zero freeze windows), `scores.security=null` (zero engine snapshots for this customer — the live #4068 symptom), plus existence checks on `poamsApproachingExpiry` and `microsoftChangesThisWeek`. Registered in `test-manifests/_regression-suite.json` already (pre-existing entry, no change needed).

  Ran `shaneapp://runTest` for real: the main checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) was genuinely stale/dead at dispatch time, so `refresh-main-server.mjs` was invoked directly to ff-pull it to origin/main and restart the live api-server (confirmed via curl). The `onlyTag`-scoped run correctly matched all 9 new steps but every one failed on `{{authToken}}` unresolved — a structural limitation (the shared SETUP login step has no sessionTag, so it never runs under `onlyTag` scoping) unrelated to this change. A full-file run then hit the real, honest blocker: `TEST_PORTAL_EMAIL`/`TEST_PORTAL_PASSWORD` are genuinely `<unset>` in this BuildConsole instance's Settings (confirmed by reading `%AppData%\BuildConsole\settings.json` directly), so login itself never resolved and every authenticated step — not just mine, the pre-existing #2922/#3049/#3344 ones too — cascaded to the same failure. What DID pass for real against the live server running this build's code: both AUTH-GATE 401 steps and both uiSteps (shell mounts, RequireAuth's honest empty state). This is a genuine pre-existing environment gap an agent cannot self-provision (Shane's real testbed password), documented honestly in the manifest's own notes rather than claimed as a clean pass. No new findings beyond the 6 dispatched — nothing else filed.

  `git status --porcelain` clean, `verify-branch-merged.mjs` confirms `agent/4062-q2749` (520955265) is an ancestor of `origin/main`.
