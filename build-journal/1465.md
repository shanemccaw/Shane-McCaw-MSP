# #1465 — Strict pass: Alert Preferences, Copilot pillar page

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 3921372cb (fix + manifest notes), 23ea45215 (pin fix)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Part of #1461. Verify Alert Preferences (portal-v2-alert-preferences.tsx)
  actually reaches the real #1276/#1278/#1279 backend with no fixture fallback remaining. Confirm
  portal-v2-copilot.tsx's real scope (7th pillar alongside Governance/Security/Compliance/Licensing/
  Adoption/Health, or something else) before scoping any fix there.
- 2026-08-28 ✅ DONE — Alert Preferences: confirmed real end-to-end wiring (GET/PUT via
  alertPrefsLive.ts -> routes/portal-alert-preferences.ts, mounted, backed by real
  customer_alert_* tables, ALERT7's "live" assertion previously confirmed post-deploy with a
  real hash, not a placeholder). Found and fixed one real gap: Save stayed enabled during a
  load error, risking a write of fixture-derived (ALERT_PREFS_SEED) data over the tenant's real
  stored preferences — now also disabled while `error` is set. Copilot pillar page: confirmed
  its real scope — it is the standalone Copilot readiness/verdict page at /portal-v2/copilot,
  not one of the six /portal-v2/:pillar dashboards, though its score rides as the 7th slot in
  the shared war-room pillars payload the Overview reads. No fixture leaks found (Halden
  Materials/CP_DELIVERABLES already removed by #1443's 1ef235915, now on main). Its manifest's
  lastVerifiedAgainstCommit was stale (left un-updated by that same #1443 commit); documented
  honestly rather than silently re-pinned as live-confirmed — shaneapp://runTest was unreachable
  this session (shane-msp MCP server CONNECTION_CLOSED), a genuine tool blocker, so CP19-CP21
  could not actually be re-run live this session. Branch verified merged into origin/main via
  scripts/dev-server/verify-branch-merged.mjs before writing DONE.
