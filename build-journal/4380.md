# Issue #4380 — Retainer audit: does connectOffered flow need the same reorder?

- **Status:** ✅ DONE 2026-09-16T15:25:00Z
- **Scope:** platform

## Log
- 2026-09-16T00:00:00Z — Session started. Investigation-only issue (sub-issue of Feature #4376). Pulling Buy.tsx Retainer path to determine account-creation ordering relative to connect offer and payment.
- 2026-09-16T15:25:00Z — ✅ DONE — Investigation only, no code changed (per issue scope). Read `Buy.tsx` directly: all three products share one stage machine where `confirmPayment()` (Buy.tsx:718-740) advances `paying → code/verifying/password/mfa` — payment happens before any password/MFA account exists for Retainer exactly like Monitoring/Packs today. Retainer's only structural difference is `connectOffered`/`connectBlocked` (Buy.tsx:424-445) making Microsoft connect optional/skippable, not required — `RetainerPending` is a legitimate permanent end state. But `doConnect()` (Buy.tsx:622-676) still fires from stage "buy" before any account exists, so a Retainer buyer who *chooses* to connect hits the identical pre-account-consent exposure #4376 is fixing for Monitoring/Packs. Decision: Retainer needs a reorder, but a smaller one than Monitoring/Packs (not a hard `connectBlocked`-style gate) — Account → Connect (optional, still skippable) → Pay, riding the same #4370/#4374 pre-consent account door. Filed as **#4383**, parented as a direct sibling sub-issue of #4376 (Feature-first rule), labeled `bug`, `blocked_by` wired to #4374, `blocked` label applied, board status set to AI Batter Up. Real finding comment posted on #4380: https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4380#issuecomment-5699965216. Real scope note added to #4383 flagging that #4374's shipped body explicitly excludes Retainer from its current scope — needs re-check when #4383 is picked up. No fixture/hardcoded data used anywhere in this investigation.
