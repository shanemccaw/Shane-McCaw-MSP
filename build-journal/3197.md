# #3197 — Migration-number allocation should consult the shared ledger, not just local dirs

- **Status:** ✅ DONE 2026-09-08T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 54a38a343 (rebased/merged to `main`; originally authored as 804a688c7)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — investigating; #3171's merge already resolved the live 044
  collision by renaming to 045 (`55b6b302c`). #3197's real ask is the systemic fix: a next-number
  allocator that consults the shared `schema_migrations` ledger (not just the two local
  migrations directories) so peers' in-flight claims are visible before a number is picked.
- 2026-09-08T00:45:00Z ✅ DONE — added `web/shanes-life/bin/next-migration-number.mjs`, which
  unions on-disk numbers from both migrations directories with the real `schema_migrations`
  ledger and prints the real next-free number, warning loudly when the ledger holds a number no
  local file has yet (a peer's in-flight, not-yet-merged claim). Verified live: ran it for real
  against the local `finances` database (returns `047`, correctly above the real 046 on disk +
  ledger), then inserted/removed a synthetic `999_test_peer_claim.sql` ledger row to confirm it
  surfaces a peer-only claim and computes past it (`1000`). Updated
  `scripts/check-migration-numbers.mjs`'s same-directory-collision error text to point at the new
  script and spell out the rename-orphans-the-ledger-row trap the issue called out, and updated
  `web/shanes-life/README.md`'s migration-numbering section to require running the allocator
  before naming a new migration file. This is advisory on top of, not a replacement for, the real
  enforcement (`assertNoDuplicateMigrationNumbers` #3118/#3139, `assertNoOrphanLedgerRows` #3140),
  which is unchanged. `node scripts/check-migration-numbers.mjs` and
  `node scripts/check-migration-numbers.selftest.mjs` both pass. Rebased twice onto a fast-moving
  `origin/main` (other concurrent Shane's Life builds landing, including #3175's own related
  orphan-ledger classification work) and merged cleanly both times — one real content conflict in
  `README.md` (two builds adding adjacent sections to the same doc), resolved by keeping both.
  `verify-branch-merged.mjs` confirms merged; working tree clean.
