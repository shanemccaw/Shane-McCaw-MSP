# #2225 — blocked_by unblocks on a real git-verified DONE bookend, not only on formal issue close

- **Status:** ✅ DONE 2026-09-02T14:14:51Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** `878117c89` (code: DoneBookendVerifier + launch-gate + Batter Up badge), `1e8e3fdad` (IN FLIGHT bookend). Both verified real commit objects and ancestors of origin/main (`git cat-file -t` == commit, `git merge-base --is-ancestor` exit 0); branch merged per verify-branch-merged.mjs (exit 0).

## Log
- 2026-09-02T14:14:51Z ✅ DONE — Shipped in `878117c89`. **New `Services/DoneBookendVerifier.cs`**: `GetSatisfiedAsync(issueNumbers)` returns the subset satisfied by a real DONE bookend on origin/main — `git show origin/main:build-journal/{N}.md` exists, its `**Status:**` field contains DONE, and a cited commit (from the `**Commit(s):**` field, whole-file fallback) passes BOTH `git cat-file -t <sha>` == `commit` AND `git merge-base --is-ancestor <sha> origin/main` (exit 0). Fails CLOSED on every error (unresolved repo root, missing bookend, non-DONE status, unknown object, non-ancestor, subprocess error). Rate-limited single-ref `git fetch origin +refs/heads/main:refs/remotes/origin/main` (45s cooldown, only when something is actually waiting) so a just-pushed bookend is seen; 30s per-issue result cache. **Real launch gate** (`BuildQueuePostgresClient.SelectClaimCandidatesAsync`, the #1600 watcher that actually decides what launches): a blocker still OPEN on GitHub is now excluded from `stillOpen` when it has a verified DONE bookend — so a blocker counts satisfied on EITHER GitHub-closed OR verified bookend, computed once per pass, only when the live GitHub snapshot succeeded (fail-closed unreachable path unchanged). Also flows through `PeekNextAsync` (shared method) so the dropdown preview matches. **Batter Up badge**: `OpenBlockedByNumbers` KEPT as the honest raw GitHub open/closed signal (task requirement); added `SatisfiedByBookendNumbers` + `BlockingNumbers`, `IsBlocked => BlockingNumbers.Count>0`, and the card "waiting on #N" text now shows genuinely-blocking numbers so it can't say 🔒 BLOCKED on something that will auto-launch. **Verification**: `dotnet build` clean (0 warn / 0 err); the two git checks validated against real data — #2197 (DONE, `12a3ce8c0`) → satisfied; #2225 IN FLIGHT → not; missing bookend → not; fabricated hash → not. BuildConsole is a WPF app (out of scope for test manifests per CLAUDE.md); NOT launched (per never-launch-exe rule) — build-only verification. Findings filed: see issue comment. Working tree clean.
- 2026-09-02T14:08:27Z ⏳ IN FLIGHT — Real audit first. Pulled #2225 body + Shane's BUILD comment. Audited `BatterUpQueueService.cs`: `BatterUpRow.IsBlocked => OpenBlockedByNumbers.Count > 0` (line 35) and `openBlockedByNumbers = blockers.Where(b => !b.IsClosed)` (line 174) are DISPLAY-ONLY — `QueueRowAsync` queues regardless of IsBlocked (blocked-by numbers passed straight through, line 296; comment at 252-256 confirms "the #1600 fail-closed watcher still governs whether a queued blocked item actually launches"). The REAL launch gate is `BuildQueuePostgresClient.SelectClaimCandidatesAsync` (GetNextAsync, line ~391): `stillOpen = blockers.Where(b => live.OpenNumbers.Contains(b))` — an item is held as long as ANY blocker is open on GitHub. Plan: shared `DoneBookendVerifier` (git show origin/main:build-journal/{N}.md → Status DONE + cited commit hash passes `git cat-file -t` == commit AND `git merge-base --is-ancestor <sha> origin/main`), applied at the real gate so a blocker counts satisfied on EITHER GitHub-closed OR verified-DONE-bookend; keep OpenBlockedByNumbers as the real open/closed display badge.
