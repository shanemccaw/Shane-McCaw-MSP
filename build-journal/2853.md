# #2853 — msp-diagnostics.test.ts: drizzle-orm mock missing sql export

- **Status:** ✅ DONE 2026-09-04T14:45:00Z
- **Scope:** platform

## Log
- 2026-09-04T14:20:00Z — Starting: fix `vi.mock("drizzle-orm", ...)` in
  `artifacts/api-server/src/routes/msp-diagnostics.test.ts` to preserve/re-export `sql`
  via `importOriginal`, per issue body.
- 2026-09-04T14:45:00Z — ✅ DONE — commit `19ccbf863` (rebased/merged to main as
  `09bdab6d7`). Fixed the `drizzle-orm` mock per the issue body (`importOriginal`
  to preserve the real `sql` export), then found and fixed two further
  pre-existing mock gaps surfaced once the `sql` error was cleared: (1) missing
  `assertCustomerAccess` export on the `../middlewares/requireAuth` mock, and
  (2) missing `usersTable`/`clientServicesTable`/`servicesTable` exports plus
  `innerJoin` chain support in the `@workspace/db` mock (needed by the POST
  `/run` packageKey-resolution join query). All 3 gaps were fixed in this same
  session, so none were filed as separate findings.
  `npx vitest run src/routes/msp-diagnostics.test.ts` → 10/10 passed (was 8/10).
  Full `npx vitest run` in `artifacts/api-server` → 4230 passed / 59 failed
  (65 pre-existing failing files, unrelated to this change — this issue's
  target file is not among them). `verify-branch-merged` equivalent
  (`git merge-base --is-ancestor HEAD origin/main`) confirmed ANCESTOR after
  push. `git status --porcelain` clean.
