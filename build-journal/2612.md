# #2612 — Webhooks: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:45:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 69c7c477c (on main)

## Log
- 2026-09-12T02:50:00Z ⏳ IN FLIGHT — confirmed blocker #3677 (Console Shell chrome) is genuinely
  merged to main (`7888bf1d6`); read #2612's design (`Design/MSP_Console/design_handoff_msp_console/Webhooks.dc.html`
  + README screen 20) and the real backend (`artifacts/api-server/src/routes/msp-console-webhooks.ts`, #2704).
- 2026-09-12T03:45:00Z ✅ DONE — Built `artifacts/msp-console/src/api/webhooks-api.ts` (real
  hooks against `GET/POST /api/msp/customers/:customerId/webhooks*`) and
  `artifacts/msp-console/src/console/modules/Webhooks.tsx` (Customer endpoints + Deliveries tabs,
  reversible disable/enable with a detail drawer, owner-only locked actions honestly disabled),
  wired into `ConsoleShell.tsx` at `/tenants/:id/wh`. Rebased onto a concurrent Risk Register
  landing (#2582) that touched the same two files (`ConsoleShell.tsx`, `icons.tsx`) — merged
  both sets of icons/routes cleanly, no functionality dropped from either side.
  Deliberately did not build the design's third "What comes in to us" (inbound platform
  webhooks) tab — out of `msp-console-webhooks.ts`'s real scope, and its own mock copy about
  the Stripe billing webhook doing nothing is stale against the real, active
  `msp-billing-webhook.ts`. Filed as **#3760** (sub-issue of #1693, board status AI Batter Up).
  Verification: `tsc --noEmit` clean, production `vite build` clean (1763 modules, 123 kB gzip).
  No unit-test run for this module — `artifacts/msp-console` has a `nav.test.ts` file but no
  `vitest` binary installed in this package yet (pre-existing gap, not introduced here; not
  fixed per the no-`pnpm install`-from-worktree rule). No fixture/hardcoded data — every field
  comes from the live routes. Working tree clean; branch verified merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs`).
