# #4524 — Five monitor checks silently report 0 (false healthy): missing $select / PS-cased field

- **Status:** ✅ DONE 2026-09-17T15:02:28Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 8d72abb23

## Log
- 2026-09-17T14:55:50Z ⏳ IN FLIGHT — investigated the 5 named checks against live `monitor_checks` rows and the real scan `tenant-scans/2026-09-17-testbed-full-scan.json` (tenant 2080, run d1dc0ffe-db89-403a-91b9-d94b77d873b6); confirmed all 5 lack `select_params` for their mapped `sourceField`, and `exchange:distribution-list-count` reads PS-cased `Name` instead of Graph's `displayName`. Applied the issue's own method (`_itemCount > 0` with `<sourceField>_count === 0`) across every active graph check with a count-family mapping rule and found 3 more real instances of the identical defect: `sharepoint:inactive-sites` (`lastModifiedDateTime`), `identity:stale-accounts` (`signInActivity`), `governance:guest-staleness` (`signInActivity`). Writing one migration to add the missing `$select`, fix the field-name mismatch, and mark all 8 mapping rules `requireField: true`.
- 2026-09-17T15:02:28Z ✅ DONE — `lib/db/migrations/manual/2026-09-17-monitor-checks-missing-select-4524.sql` written and run against local `DATABASE_URL` (8 rows updated). Live-verified all 8 checks against testbed tenant 2080 (customerId 2080, McCawSoft) via `shaneapp://executeScan`: `governance:guest-count` → `guestAccountCount: 2` (was 0), `cost:underutilized-premium` → `assignedLicenses_count: 27` (was 0), `cost:group-based-licensing-adoption` → `assignedLicenses_count: 104` (was 0), `governance:dynamic-group-usage` → `membershipRule_count: 0` correctly (real: no dynamic groups on this tenant, field now genuinely read), `exchange:distribution-list-count` → `distributionListCount: 17` real distinct DL names (was 0). `sharepoint:inactive-sites` now fails closed (`checkStatus: error`, `MissingRequiredFieldError`) instead of reporting a false 0 — the field is still not returned by Graph even with `$select` on bare `/sites`, filed as #4538. `identity:stale-accounts` and `governance:guest-staleness` correctly return `checkStatus: license_gap` (real Graph 403 — tenant 2080 lacks Entra ID Premium P1/P2 for `signInActivity`) instead of a fabricated 0. `pnpm vitest run src/lib/__tests__/monitor-executor.test.ts` (artifacts/api-server): 221/221 passed — no code changed, data-only migration. Filed 3 follow-on findings during verification, all parented under #1489 (Feature: Remediation Tracking) and set to AI Batter Up board status: #4537 (count transform counts array-field presence not content, affects underutilized-premium/group-based-licensing-adoption), #4538 (sharepoint:inactive-sites still can't read lastModifiedDateTime via bare /sites $select), #4539 (governance:guest-staleness has no guest filter — counts all users). Working tree clean, branch verified merged into origin/main via `verify-branch-merged.mjs`.
