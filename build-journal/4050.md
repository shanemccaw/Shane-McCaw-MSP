# #4050 — POA&Ms create form has no way for a customer to discover a real checkKey to link a plan to

- **Status:** ✅ DONE 2026-09-15T01:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 0edab8e9d (merged to main as e265f0979)

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — Investigated #4050. Plan: add `GET /api/portal/poams/available-checks`
  to `portal-poams.ts`, sourcing checkKey+checkLabel from the customer's own latest completed/partial
  `msp_diagnostic_findings` run (real data, distinct by checkKey — no fixture catalogue). Then wire
  `PoamCreatePanel.tsx`'s "Check this plan answers" field to a real picker sourced from it, keeping the
  free-text fallback for when no check applies (per #4037's honest no-fixture precedent and this issue's
  own instructions).
- 2026-09-15T01:35:00Z ✅ DONE — Shipped `GET /api/portal/poams/available-checks` (same
  `requireCapability("ladder.customer-user")` + `requireTierFeature(poams)` gate as the sibling READ routes),
  reading distinct `checkKey`/`checkLabel` off `msp_diagnostic_findings` for the customer's latest
  completed/partial run — zero fixture data. Wired `PoamCreatePanel.tsx`'s "Check this plan answers" field
  to a real `Select` picker (added the missing `components/ui/select.tsx` shadcn shim to `artifacts/portal`,
  `@radix-ui/react-select` was already a dependency) when checks exist, with a "type it myself" escape
  hatch back to the original free-text input, and vice versa — the free-text fallback stays the only
  option for a tenant with no completed scan yet. Added `useAvailableChecks()` to `poams-api.ts` /
  `WireAvailableCheck` to `poams-types.ts`, mirroring `useListPoams`'s 402→tierGated shape. Extended
  `test-manifests/portal/poams.json` (A2b) to assert the new route carries the identical live tier gate.
  Verification: `tsc --noEmit` clean in both `artifacts/api-server` and `artifacts/portal`; confirmed the
  real `msp_diagnostic_findings.check_key`/`check_label` columns and live row shape directly against the
  local `DATABASE_URL` (deleted the scratch verify script after). `shaneapp://runTest` was invoked
  (onlyTag=available-checks-4050) but only exercised the pre-merge deployed manifest/build — see
  `shaneapp-runtest-hits-deployed-build-not-worktree` — so it could not yet exercise the new route; the
  real UI/API exercise of this route happens on the next test run after BuildConsole's merge-back.
  No new migration needed (existing table already carries checkKey+checkLabel). No production/PROD-app
  boundary touched. `git status --porcelain` clean; branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` OK) before writing this.
