# #1753 — Retire the /portal/assessment/* route prefix

- **Status:** ✅ DONE 2026-09-03T04:08:53Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** e1ab05c41 (rebased/pushed to main as 6505c3f19)

## Log
- 2026-09-03T03:53:00Z ⏳ IN FLIGHT — Blocking dependency #1674 (Copilot Readiness retirement) confirmed CLOSED, so this issue is unblocked. Investigating current route inventory in artifacts/api-server/src/routes/portal-assessment.ts (27 routes, some already renamed to /portal/pillars/* and /portal/scan-*) and mapping real callers before renaming surviving routes and removing dead SOW/checkout/documents routes.
- 2026-09-03T04:08:53Z ✅ DONE — Full repo-wide caller sweep (background subagent, 54 tool calls) confirmed zero live callers and zero test-manifest coverage for all 14 dying SOW/checkout/documents/doc-workflow/stripe-webhook routes; deleted them outright from portal-assessment.ts (~2270 lines removed), plus the now-dead raw-body stripe webhook middleware in app.ts. Renamed the 13 surviving routes off the "assessment" prefix to /portal/diagnostics/* (status, telemetry-comparison, 7 pillar narrative generators, history, debug-trigger-scan, testbed-status, debug-reset-session), and the two assessment-results routes to /portal/diagnostics/results(/:serviceSlug) (portal-customer-engines.ts, msp-diagnostics.ts). Updated the two real live callers found (artifacts/portal's useTopicHealthLive.ts, shane-mccaw-consulting's usePersonalizationData.ts) and — critically — requireAuth.ts's MFA-setup allowlist, which hard-coded the old /portal/assessment/status path and would have silently broken the mandatory MFA gate for pending Assessment sessions if left unrenamed. Cleaned up now-dead imports in portal-assessment.ts (couponsTable, assessmentSowAgreementsTable, checkoutSessionsTable, stripe/captcha/SSE/audit/zod imports, etc.) and corrected stale path references in doc comments across 7 other files. requireRole("Assessment") deliberately left untouched per the issue's own scope note (RBAC entitlement rung, #1696/#1704). Filed one genuine finding: #2517 (orphaned health-suite/assessment-test dead-code cluster in artifacts/portal, parented to Feature #1621, iteration:1 + area:msp-portal, assigned Shane, board status "AI Batter Up"). Verification: tsc --noEmit on api-server shows only pre-existing environment-level errors (missing lib/db/dist build output, unresolved drizzle-orm/zod/vitest types — documented worktree-isolation issue, not caused by this change); confirmed no new errors in any file I touched by diffing before/after error sets. vitest could not run — the main checkout's own node_modules/vitest is an empty stub directory (pre-existing environment breakage, not a worktree-specific dangling link and not caused by this change; not remedied via pnpm install per standing policy). Rebased onto origin/main and pushed directly (03a2905e7..6505c3f19); verify-branch-merged.mjs confirms merged. Working tree clean.
