# #3177 — Orphaned Shane's Life schema drift: catches table + list_items.requested_by

- **Status:** ⏳ IN FLIGHT 2026-09-08T17:30:19Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** _(fill at DONE)_

## Log
- 2026-09-08T17:30:19Z ⏳ IN FLIGHT — Confirmed #3153's `034_catches.sql` already reconciled the
  reported `catches` table + `list_items.requested_by` drift itself (adopted the live `detail`
  column name, added `dedupe_key`). Nothing left to fix in that schema. The real open item this
  build is scoped to is #3177's own stated question: build a live audit that flags a table/column
  present in the database with no matching migration file, since worktree cleanup does not undo a
  real DB mutation a session already made from inside it. Building `scripts/check-schema-drift.mjs`
  (parses `CREATE TABLE` / `ADD COLUMN` out of both migration directories, diffs against
  `information_schema`) wired as a non-fatal boot-time warning in `web/shanes-life/src/migrate.mjs`
  plus a standalone `bin/check-schema-drift.mjs` CLI.
