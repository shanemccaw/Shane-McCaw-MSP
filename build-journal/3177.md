# #3177 — Orphaned Shane's Life schema drift: catches table + list_items.requested_by

- **Status:** ✅ DONE 2026-09-08T17:58:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 2d31a7b39

## Log
- 2026-09-08T17:30:19Z ⏳ IN FLIGHT — Confirmed #3153's `034_catches.sql` already reconciled the
  reported `catches` table + `list_items.requested_by` drift itself (adopted the live `detail`
  column name, added `dedupe_key`). Nothing left to fix in that schema. The real open item this
  build is scoped to is #3177's own stated question: build a live audit that flags a table/column
  present in the database with no matching migration file, since worktree cleanup does not undo a
  real DB mutation a session already made from inside it. Building `scripts/check-schema-drift.mjs`
  (parses `CREATE TABLE` / `ADD COLUMN` out of both migration directories, diffs against
  `information_schema`) wired as a non-fatal boot-time warning in `web/shanes-life/src/migrate.mjs`
  plus a standalone `bin/check-schema-drift.mjs` CLI.
- 2026-09-08T17:58:00Z ✅ DONE — Commit `2d31a7b39` (pushed to `main`, verified ancestor via
  `verify-branch-merged.mjs`). Shipped: `scripts/check-schema-drift.mjs` (parser + diff logic,
  cross-references the existing ledger ahead/missing classification so a concurrent sibling
  build's real-but-unmerged migration is a caveat, not a false alarm), wired non-fatally into
  `migrate.mjs`'s boot path, `web/shanes-life/bin/check-schema-drift.mjs` standalone CLI,
  `scripts/check-schema-drift.selftest.mjs` (12 assertions, all passing, no DB touched), and a
  new "Database is NOT worktree-isolated" section in `scripts/dev-server/README.md` documenting
  the real gap and this mitigation's honest scope. Verified against the REAL local `finances`
  database: clean audit with the actual current schema; a deliberately-injected test column on
  `catches` was correctly flagged then removed; and a genuine live "ahead" case
  (`054_medication_pharmacy_phone.sql`, a concurrent sibling build's migration already applied
  to the shared DB but not yet merged to this checkout) was correctly reported as a caveat, not
  conflated with real drift. Confirmed #3153's own `034_catches.sql` already fully reconciled the
  originally-reported `catches`/`list_items.requested_by` drift (ledger rows present, live schema
  matches) — nothing left to fix there. `git status --porcelain` clean at finish.
