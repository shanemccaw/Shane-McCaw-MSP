# #3688 — Shot Vault synchronous file scan blocks the UI thread

- **Status:** ✅ DONE 2026-09-11T19:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** bfad92189

## Log
- 2026-09-11T19:20:00Z ⏳ IN FLIGHT — pulled #3688 body + comment, read ShotVaultService.cs,
  ShotVaultWindow.xaml.cs, MainWindow.BuildMatrixTab.cs and BuildQueuePanel.xaml.cs's
  RenderMatrixDrawer/UpdateQueueStatusCounts path. Confirmed the issue's own finding:
  ShotVaultService.ListShots() (Directory.EnumerateFiles + File.GetLastWriteTimeUtc) runs
  synchronously on the UI thread from ShotVaultWindow.Render(), and ShotVaultWindow.BuildTile()
  also does synchronous per-shot BitmapImage decode (file read + JPEG/PNG decode) on the UI
  thread for every visible shot. Both are the real blocking work. BuildMatrixTab.cs itself has
  zero I/O and is a one-shot "send to tab" snapshot, not a live poller; the live Build Matrix
  drawer (RenderMatrixDrawer, called from UpdateQueueStatusCounts on the existing 5s
  _localQueuePollTimer) already guards against rebuild-while-collapsed and does only cheap
  in-memory dictionary/LINQ work over already-loaded queue state — no synchronous I/O, no real
  finding there. Next: move ShotVaultService's scan + thumbnail decode off the UI thread in
  ShotVaultWindow.Render(), add a real loading state.
- 2026-09-11T19:45:00Z ✅ DONE — commit bfad92189. ShotVaultWindow.Render() is now async:
  ScanShotsAndDecodeThumbnails() (ListRuns + per-shot GetTags + per-shot frozen-BitmapImage
  thumbnail decode) runs via Task.Run off the UI thread; a "Loading shots…" status shows the
  instant Render() starts; a _renderGeneration counter discards a stale scan if a newer
  refresh/search/tag-toggle superseded it mid-scan. BuildTile() no longer decodes — it just
  assigns the already-decoded, already-frozen bitmap. Verified: `dotnet build BuildConsole.csproj
  -c Debug` — Build succeeded, 0 errors (6 pre-existing unrelated warnings in FocusModeBar.xaml.cs).
  BuildMatrixTab.cs audited per the issue's own request — confirmed no expensive UI-thread work
  (it's a one-shot snapshot, not a poller; the live drawer's actual 5s-timer-driven refresh path
  does only cheap in-memory work) — documented in the commit message, no code change needed there.
  No genuine new finding surfaced worth filing as its own #1202 sub-issue. Per the issue's own
  request, still need Shane to check %USERPROFILE%\buildconsole_crash.log for the real exception
  behind the hard crash — posted as a request on #3688's own comment. Working tree clean.
