# #3688 — Shot Vault synchronous file scan blocks the UI thread

- **Status:** ⏳ IN FLIGHT 2026-09-11T19:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T19:20:00Z ⏳ IN FLIGHT — pulled #3688 body + comment, read ShotVaultService.cs,
  ShotVaultWindow.xaml.cs, MainWindow.BuildMatrixTab.cs and BuildQueuePanel.xaml.cs's
  RenderMatrixDrawer/UpdateQueueStatusCounts path. Confirmed the issue's own finding:
  ShotVaultService.ListShots() (Directory.EnumerateFiles + File.GetLastWriteTimeUtc) runs
  synchronously on the UI thread from ShotVaultWindow.Render(), and ShotVaultWindow.BuildTile()
  also does synchronous per-shot BitmapImage decode (file read + JPEG/PNG decode) on the UI
  thread for every visible shot. Both are the real blocking work. BuildMatrixTab.cs itself has
  zero I/O and is a one-shot "send to tab" snapshot, not a live poller; the live Build Matrix
  drawer (RenderMatrixDrawer, called from UpdateQueueStatusCounts on the existing 5s
  _localQueuePollTimer) already guards against rebuild-while-collapsed and does only cheap
  in-memory dictionary/LINQ work over already-loaded queue state — no synchronous I/O, no real
  finding there. Next: move ShotVaultService's scan + thumbnail decode off the UI thread in
  ShotVaultWindow.Render(), add a real loading state.
