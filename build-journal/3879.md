# #3879 — Notification Tray 3/4: new tray popout panel

- **Status:** ✅ DONE 2026-09-13T00:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** bdf1e97ad

## Log
- 2026-09-13T00:35:00Z ⏳ IN FLIGHT — building `Controls/NotificationTrayPanel.xaml`/`.xaml.cs`: real history list from `NotificationHistoryStore.All`, per-item bookmark toggle wired to `SetBookmarked`, Clear All wired to `ClearAllNonBookmarked`, honest empty state. Reusing `ToastCard`'s kind→icon/color mapping (`StyleFor`) for visual consistency, exposed as `internal` for that reuse. Panel is a Popup-hostable UserControl (no bell icon here — that's #3880, the sibling issue that will host it in an ActivityBar Popup the same way `WebToolsPopup`/`ShelfPopup` already do).
- 2026-09-13T00:52:00Z ✅ DONE — Shipped `Controls/NotificationTrayPanel.xaml`/`.xaml.cs` (bdf1e97ad): real history list newest-first, per-row ★/☆ bookmark toggle (live re-render on click), "Clear All" (non-bookmarked only), honest "No notifications." empty state. `ToastCard.StyleFor` changed `private`→`internal` so the tray reuses the exact same kind→icon/color mapping the floating toast uses. Content-only UserControl, no Popup/bell icon owned here — matches the WebToolsList/ShelfList convention; #3880 (sibling, not yet dispatched) hosts it. Verification: clean `dotnet build BuildConsole.csproj` (0 warnings, 0 errors) + code-reading confirmation of the wiring. Per Git #2156's standing memory, live UI click-through against a running `BuildConsole.exe` was NOT self-verified (single-instance mutex risk of couriering into Shane's real live session) — that's the stated bar for this class of BuildConsole UI change, not a gap. Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0); working tree clean. No findings filed — no genuine gap/bug hit during this build. Stale `blocked_by #3877` edge removed (that issue is `complete`-labeled, so per the blocked-work-resumption rule this issue was unblocked; no new blocker filed).
