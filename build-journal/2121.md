# #2121 — Worktree isolation: @workspace/db shared-junction dangles cross-worktree (#1988 class)

- **Status:** ✅ DONE 2026-09-06T16:03:28Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** `3b990356e` (regression selftest + README consolidation), `00066b0ce` (IN FLIGHT bookend). DONE bookend committed on top.

Consolidated fix for the whole family: #2088, #2089, #2094, #2097, #2121 — all
distinct symptom reports of the same root cause (worktree `node_modules/@workspace/*`
being a shared junction into the MAIN checkout / an unrelated worktree, so a worktree's
own `lib/db` schema edits are invisible to its `artifacts/api-server` `tsc`/`tsx`/`vitest`,
and one poisoned/dangling link in the shared main checkout silently breaks type
resolution for main + every worktree at once).

## Log
- 2026-09-06T15:43:58Z ⏳ IN FLIGHT — Read #2121 body+comments and #2088/#2089/#2094/#2097 in full; per-package @workspace linking already landed (#2152/#2927) in link-deps.mjs. Investigating remaining gaps (proactive store-doctor scan, sanctioned worktree verify path) before designing the consolidated fix.
- 2026-09-06T16:03:28Z ✅ DONE — Consolidated fix shipped in `3b990356e`. **Root-cause verdict:** the five issues are one bug — a worktree's `node_modules/@workspace/db` was a wholesale junction into MAIN (or, under a provisioning race, an unrelated sibling worktree), so a worktree's own `lib/db` edit was invisible to its consumers and the shared link dangled/repointed cross-worktree. The CODE fix already existed: `#2152` (per-package `@workspace/*` linking in `link-deps.mjs`) + `#2117` (per-worktree `lib/*/dist`) + proactive `store-doctor` scans wired into `provision-worktree.mjs` and `worktree-lifecycle.mjs`. **Verified live in THIS worktree:** `artifacts/api-server` resolves `@workspace/db` → `C:\wt\2121-q1774\lib\db\src\index.ts` (proven with a real marker-injection probe: the resolved file carried the marker; main was NOT polluted; `lib/db/dist` is a real per-worktree dir). **The real remaining gap was no regression guard** (the #2152 fix was hand-verified once, nothing catches a silent regression) and **no documented sanctioned self-verify** (#2094/#2097 ask 2). Added `scripts/dev-server/worktree-isolation.selftest.mjs` — a standalone Windows-only guard that runs the REAL `linkDeps()` over a throwaway fixture and asserts, with Node's real resolver, that a worktree consumer (both an `@workspace`-scope host AND a wholesale-junction/root-fallthrough host = the live hoisted layout) resolves+executes its OWN `lib/db`; main's links never repoint at a worktree; two concurrent worktrees never see each other; third-party still from the shared store; cleanup leaves no delete-through junction. **20/20 assertions pass; a negative control confirmed the old wholesale-junction bug makes it go red (not a rubber stamp).** Documented the consolidated guarantee + obsolete-paths-override self-verify in `scripts/dev-server/README.md`. **Coverage: #2088 ✅, #2089 (both findings) ✅, #2094 ✅, #2097 (poison prevented + proactive scan #2097-ask1 already wired + self-verify #2097-ask2 documented + per-worktree dist #2117) ✅, #2121 ✅ — all five genuinely covered, safe to close as duplicates.** Separate/unrelated tracked gap NOT addressed here: store-doctor reports CLEAN on a zero-entry (missing-content) node_modules — that's #2973 (store CONTENT missing, a different root cause than junction TARGETING). Branch `agent/2121-q1774` verified merged into origin/main via `verify-branch-merged.mjs` (exit 0). No new findings filed (no new bugs discovered; no workarounds left). Working tree clean.
