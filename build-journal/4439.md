# #4439 — StatusBar dark-theme gap (OS-default ControlTemplate leak, same class as ComboBox)

- **Status:** ✅ DONE 2026-09-16T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** 3d9cc75db

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — confirmed current `Themes/DarkTheme.xaml` StatusBar/StatusBarItem styles still match the issue body exactly (property-setter-only, `BasedOn="{StaticResource {x:Type StatusBar}}"`, no override dictionary). About to give `StatusBar` a full `ControlTemplate` following the ComboBox precedent (same file, ~line 829), hosting a real `ItemsPresenter` inside a `Border` so `MainWindow.xaml`'s custom `StatusBar.ItemsPanel` Grid layout is unaffected.
- 2026-09-16T00:30:00Z ✅ DONE — OS-default-template hypothesis CONFIRMED (matches the ComboBox precedent exactly). Replaced the property-setter-only `StatusBar` style (`BasedOn="{StaticResource {x:Type StatusBar}}"`) with a full `ControlTemplate`: a `Border` (Background=CrustBrush, BorderBrush=Surface0Brush, BorderThickness=0,1,0,0) hosting `ItemsPresenter` — `MainWindow.xaml`'s custom `StatusBar.ItemsPanel` Grid (Deploy/queue-counts/usage/clock columns) is unaffected since `ItemsPresenter` still renders whatever panel the control specifies. Also found and fixed the identical gap in `StatusBarItem`'s style (same `BasedOn` pattern, no template override) — gave it a real `ControlTemplate` too (Border + ContentPresenter). Verified at the template level: neither style references `{StaticResource {x:Type ...}}` or any `BasedOn` anymore, so no part of the OS-default Aero/Fluent chrome is inherited — the new templates paint 100% of their own pixels via `TemplateBinding`. `dotnet build BuildConsole.csproj -c Debug` succeeds clean (0 warnings, 0 errors), confirming the templates parse/bind correctly. **Visual confirmation on the running app still needs Shane's own eyes** per the standing "never launch the exe" rule — the build/template-level check above is real verification that the fix is structurally correct, not a substitute for seeing the actual rendered pixels. No fixture/hardcoded data; no other files touched; `git status --porcelain` clean. Commit 3d9cc75db pushed directly to `origin/main`; `verify-branch-merged.mjs` confirms ancestor OK.
