# #2187 — copilot: domain monitor_checks query/label mismatches

- **Status:** ✅ DONE 2026-09-03T03:55:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 4e864a5e4 (bookend IN FLIGHT), 20286ddc5 (real fix, pushed to main)

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — investigated live `monitor_checks` rows for the
  4 checks named in the issue body. Item 4 (`copilot:data-exposure-risk` fan-out) was
  already fixed by `2026-08-08-copilot-data-exposure-risk-real-signal-553.sql` — no
  further action needed there. Working the remaining 3: `copilot:active-usage-rate`,
  `copilot:usage-by-app`, `copilot:license-vs-total-users`.
- 2026-09-03T03:55:00Z ✅ DONE — fixed the 3 remaining checks:
  - `copilot:active-usage-rate`: added real `severity_rules` (was `[]`) using the
    check's own existing fields, no engine change needed.
  - `copilot:usage-by-app`: replaced the broken `groupByCount('appActivity')`
    mapping (nonexistent field) with real per-app mappings against Microsoft's
    documented v1.0 CSV column headers for `getMicrosoft365CopilotUserCountTrend`.
  - `copilot:license-vs-total-users`: fixed a real data-correctness bug — the
    mapping picked an ARBITRARY SKU's `consumedUnits`, not Copilot's. Extended
    `applyMapping`'s `valueWhere` transform (monitor-executor.ts) to support
    matching/extracting against flat top-level items, since no stored check used
    `valueWhere` before this (verified via live query), so no existing behavior
    changed. Added unit tests for the new mode.
  Migration `lib/db/migrations/manual/2026-09-03-copilot-domain-check-query-fixes-2187.sql`
  run against local `DATABASE_URL`, confirmed applied to all 3 checks, self-marked
  in `simulator_migration_runs`.
  A true active/total or license/total RATIO is genuinely NOT computable anywhere
  in this platform's engine today — confirmed by reading `applyMapping`'s full
  transform set, `evalConditionGrammar`, and `tenant-signals.ts`'s `evaluateRule`:
  none support arithmetic across two fields, single-check or cross-check. Filed as
  its own finding, **#2514**, parented as a sibling sub-issue under this issue's
  own Feature parent (#1489), labeled `bug` + `iteration:1` + `area:api-server`,
  assigned to Shane, board status set to "AI Batter Up".
  Verification: `vitest`/`tsc` are both broken repo-wide in this environment
  (pre-existing, already filed as #2501/#2503 by other sessions — pnpm store has
  empty/incomplete package content for `vitest`/`esbuild`). Verified the new
  `valueWhere` logic via a standalone reimplementation harness matching the real
  code (all cases passed). `tsc --noEmit` against `monitor-executor.ts` shows zero
  new errors near the change (only the same pre-existing environment errors every
  file in this checkout currently shows).
  `git status --porcelain` clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0).
