# #4841 — plan

Posted: 2026-09-19T05:18:00Z

## Problem Description
`ca-hold-window-scan.test.ts` builds `ReportOnlyImpactSummary` fixtures without the required `byCategory` property, so `tsc -p artifacts/api-server/tsconfig.json --noEmit` reports six TS2741 errors (lines 82, 100, 108, 115, 123, 153). vitest does not typecheck, so it went unnoticed.

## Root Cause Analysis
#4552 made `byCategory: Record<SignInCategory, CategoryImpactCounts>` required on `ReportOnlyImpactSummary` ([ca-policy-impact.ts](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/main/artifacts/api-server/src/lib/ca-policy-impact.ts)); the #4550 test fixtures were not updated.

## Proposed Changes
- [`artifacts/api-server/src/lib/ca-hold-window-scan.test.ts`](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/main/artifacts/api-server/src/lib/ca-hold-window-scan.test.ts): add a local `emptyByCategory()` helper (all-zero `CategoryImpactCounts` per `SignInCategory`) and add `byCategory: emptyByCategory()` to the six summary fixtures. Test-only; no production code touched.

## Verification Plan
- `tsc -p artifacts/api-server/tsconfig.json --noEmit` — zero errors in this file.
- `vitest run src/lib/ca-hold-window-scan.test.ts` still passes.
