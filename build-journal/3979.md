# #3979 — End & Sync: call SaveEntryAsync before export so the real bug_number lands in report.json

- **Status:** ✅ DONE 2026-09-14T01:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** bb97eab36 (fix), 1d5067065 (post-rebase hash on origin/main)

## Log
- 2026-09-14T01:15:00Z ⏳ IN FLIGHT — Investigating `ExecuteEndSessionSyncAsync` in MainWindow.TestMode.cs and `VisualTestTrackerStore.SaveEntryAsync`; plan is to persist each bug via SaveEntryAsync before building SessionSyncContext.Entries so real bug_number/id values land in report.json instead of 0.
- 2026-09-14T01:45:00Z ✅ DONE — Confirmed `VisualTestTrackerSessionSyncService`'s report.json writer serializes `bugs[].id = b.Id` (the DB primary key `SaveEntryAsync` returns), not just `BugNumber` — same root cause either way, since both come from the same unsaved-entry default of 0. Fixed `ExecuteEndSessionSyncAsync` to resolve the local DB connection via `VisualTestTrackerStore.ResolveConnectionString()` (same pattern as the Bug Tracker tab in MainWindow.xaml.cs) and call `SaveEntryAsync` on each `VisualTestTrackerEntry` — both the per-bug entries from `AllBugs` and the synthetic current-notes entry — before adding them to `context.Entries`, so the real `Id`/`BugNumber` from the upsert's `RETURNING` clause is what ends up in `context.Entries` and therefore in the exported `report.json`. `entry_uuid`'s `ON CONFLICT DO UPDATE` keeps this idempotent regardless of whether the bug was already saved earlier in the session. Verified via `dotnet build -c Debug` (0 warnings, 0 errors) — no live BuildConsole.exe launch per standing "never launch BuildConsole.exe to verify" rule; a real End & Sync click-through with DOM-inspector bug capture is left for Shane's next live Test Mode session to observe non-zero `bugs[].id` values, since this session has no WebView2 UI to drive that flow. Branch verified merged into origin/main (`verify-branch-merged.mjs` OK), working tree clean.
