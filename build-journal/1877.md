# #1877 — Build Queue: add a "Crashed" filter option

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 489a2dd4e

## Log
- 2026-08-30 ⏳ IN FLIGHT — read #1877 body/comments and #1829's landed filter-split diff (ac0a401eb); about to add a "Crashed" filter entry to BuildQueuePanel's ApplyFilter/combo reusing the existing `status == "failed" && ExitCode == -2` orphan criteria, leaving UpdateOrphanRecoveryBanner/bulk-recover untouched.
- 2026-08-30 ✅ DONE — added "Crashed" `ComboBoxItem` to `QueueFilterCombo` in `BuildQueuePanel.xaml`, a `"Crashed"` arm in `ApplyFilter` (`status=="failed" && ExitCode==-2`, same criteria as `UpdateOrphanRecoveryBanner`/`BtnRecoverOrphans_Click`), a matching `RevealQueueItem` target-filter arm, and a "Nothing crashed." empty-state message. `UpdateOrphanRecoveryBanner`/`BtnRecoverOrphans_Click` untouched. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 warnings/errors. Commit 489a2dd4e pushed to origin/main and confirmed ancestor. No test manifest — desktop/BuildConsole is out of scope.
