# #4141 — Change Control: match wired page to design (drop stub section + caveat tags)

- **Status:** ✅ DONE 2026-09-14T23:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** fcf144ea0 (rebased/pushed to main as 5bf724dad)

## Log
- 2026-09-14T23:30:00Z ⏳ IN FLIGHT — Verified via `docs/portal/change-control-contract-pack.md` and `artifacts/api-server/src/routes/portal-change-control.ts` that rollback-as-inverse-change, moving a booked change, and CAB/execution/PIR are still MSP-console-only (no customer-portal route exists for any of the three). `nextWindowDateOrdered` is still conditional (true only when a real `scheduledStart` instant exists, false when falling back to free-text grouping — #1762 did not make it unconditionally reliable). Per Shane's 2026-09-14 decision, removing the `NOT_BUILT` stub section and the `DATE-ORDERED`/`STRING-GROUPED †`/`SUBSTITUTE METRIC †` tags from `change-control.tsx` to match `Design/portal/design_handoff_full_site/screens/Change Control.dc.html` (where `showNotBuilt` is now a dead unused prop and all four StatTile specs carry `hasTag: false`) — this is a visual-only change; the three underlying gaps remain real and unbuilt, and `nextWindowDateOrdered` remains genuinely conditional data, not now-reliable data.
- 2026-09-14T23:45:00Z ✅ DONE — Removed the `NOT_BUILT` array + "Not on this page yet" card, the `tag`/`tagGood` props on the two `StatTile` calls ("Next window", "Emergency") and on the `StatTile` component itself (now fully dead — the design's `hasTag` is `false` on all four tiles), and reworded the stats-footnote paragraph to drop its `†` references to the removed tags while keeping its one remaining real fact (the executed-at stamp is still free text). `pnpm exec tsc --noEmit` in `artifacts/portal` passed clean. No existing test manifest (`test-manifests/portal/change-control.json`) asserted on the removed UI, so none needed updating. Live UI re-verification via `shaneapp://runTest` was not run: the shared dev-server process is currently dead (pre-existing, unrelated to this change — `node scripts/dev-server/status.mjs` showed `server process: pid 5888 DEAD`) and that protocol tests the merged/deployed build, not this worktree, so it would not have exercised these changes ahead of the merge below anyway. Rebased `agent/4141-q2814` onto `origin/main` and pushed directly to `main` (`5bf724dad`); `verify-branch-merged.mjs` confirms the branch HEAD is an ancestor of `origin/main`. `git status --porcelain` clean. **Honest gap record (unchanged by this build, only made visually silent per Shane's decision):** rollback-as-inverse-change, moving a booked change, and CAB/execution/post-implementation-review remain real, unbuilt, MSP-console-only capabilities with no customer-portal route; `nextWindowDateOrdered` remains genuinely conditional (not unconditionally reliable) real data.
