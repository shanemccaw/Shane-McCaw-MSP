# #1416 — BuildConsole: multi-account routing for Claude Code (overflow to secondary account)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 86457ef6d (work), bbdc6030c (IN FLIGHT bookend)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Add a configurable secondary CLAUDE_CONFIG_DIR setting + per-job primary/secondary account selector in the queue UI, and route the launched Claude Code process's CLAUDE_CONFIG_DIR to the selected account. Sequential overflow only, no failover, no concurrency changes.
- 2026-08-27 ✅ DONE — Shipped in 86457ef6d. Added `BuildConsoleSettings.SecondaryClaudeConfigDir` (Settings → General editor, default `~/.claude-secondary`); `QueueItem.Account` + `bt_build_queue.account` column at ordinal 18 wired through every SELECT/RETURNING/INSERT/MapRow in `BuildQueuePostgresClient`; `QueueWatcherService.LaunchItem` sets `CLAUDE_CONFIG_DIR` (with `~` expansion) only when `Account == "secondary"`, else default account exactly as before; `EditBuildPromptDialog` Account dropdown (defaults primary) mirrored to an `--account secondary` header flag; account threaded through all `QueueBuildAsync` callers (queue/retry/resume/pending-update/chat-button) so the choice survives requeue. Verification: `dotnet build -c Debug` → 0 errors (46 pre-existing warnings). Manual migration `lib/db/migrations/manual/2026-08-27-build-queue-account-1416.sql` written AND applied to local PostgreSQL 18 (column verified `account text`; insert/readback of `account='secondary'` round-tripped; full 18-col SELECT executes over 510 rows). No test manifest — test-coverage rule scopes only to artifacts/msp-portal + artifacts/shane-mccaw-consulting, not desktop/BuildConsole. One-time Shane setup left: authenticate the secondary Pro account under the secondary config dir (`CLAUDE_CONFIG_DIR=~/.claude-secondary claude` → login) before first secondary-account build. The migration is already applied locally; it must also run wherever else this bt_build_queue table lives.
