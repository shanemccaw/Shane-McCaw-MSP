# Git #3897 — POA&Ms: wire the real MSP Console plans-of-action module (screen 41)

- **Status:** ⏳ IN FLIGHT 2026-09-13T01:05:00Z
- **Scope:** platform

## Log

- 2026-09-13T01:05:00Z — Claimed dispatch, read issue body + BUILD comment. Confirmed on
  `main`: `artifacts/api-server/src/routes/msp-poams.ts`'s generic `PATCH /msp/poams/:poamId`
  already refuses both `cancelled` and `converted_to_risk_acceptance` as incoming values
  (409) and treats both as terminal alongside `completed` on an existing row — #3452 and
  #3635 are genuinely fixed on `main`, confirmed by reading the route directly (lines
  262–281), not by trusting the issue text. That means the design's `.dc.html` role-pill
  simulation (an MSPOperator bypassing the admin gate through the generic edit route) is
  now stale — there is no such bypass left to simulate. Following the AD OU Assignment
  (#3818) and Partner Revenue (#3820) precedent already on `main`: no role-toggle ship,
  instead an `isAdmin` boolean computed once in `ConsoleShell.tsx` from the real session
  profile and passed down, gating the Cancel / Convert-to-risk-acceptance / Delete plan
  buttons client-side (disabled + tooltip) while the server remains the real, sole gate.
  `msp_poams` / `msp_poam_milestones` both confirmed empty in local Postgres — real empty
  state renders, no fixture needed.
- Building `artifacts/msp-console/src/api/poams-api.ts` (data seam) and
  `artifacts/msp-console/src/pages/poams/PoamsPage.tsx`, wired into `ConsoleShell.tsx` at
  `sel.page === "poams"`, added to `nav.ts`'s Governance group, `embedded`/`forceEmpty`
  props per the shell's mount contract, no role pill shipped.
