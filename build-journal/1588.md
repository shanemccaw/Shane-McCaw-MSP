# #1588 — My Architect: fixture leak — retainerModel.ts:168 hardcodes "Priya Raman" on REAL data

- **Status:** ✅ DONE (moot — target code no longer exists)
- **Labels:** `complete` applied; comments posted on #1588 and #1590
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 0ac70b52d (bookend, pushed to origin/main; verified via verify-branch-merged.mjs) — no code change made or needed

## Log
- 2026-08-30 ⏳ IN FLIGHT — Pulled #1588's body/comments. Target: `retainerModel.ts:168`'s
  `askWho()` hardcoding "Priya Raman" instead of reading `retainer_settings.architectName`
  the way `week.author` already does at `useRetainerLive.ts:218`. Went to open the file.
- 2026-08-30 ✅ DONE — **The file does not exist.** `git log --diff-filter=D -- '**/retainerModel.ts'`
  shows it was deleted in `f40438cdc` ("Portal scaffolding: create artifacts/portal, retire
  artifacts/msp-portal", 2026-08-29 16:27:25 -0400) — the *day after* #1580's audit found this
  leak and one day before this build ran. All of `artifacts/msp-portal` (portal-v2, including
  `retainerModel.ts`, `useRetainerLive.ts`, `portal-v2-retainer.tsx`) was deleted wholesale in
  that commit (preserved historically at tag `portal-archive-2026-08-29`) and replaced with a
  fresh, empty `artifacts/portal` scaffold (#1673) that as of this session has **no retainer
  page at all** — confirmed via `find artifacts/portal/src -maxdepth 3 -type d`, no `retainer`
  directory, and `Design/portal/` has no `.dc.html` export for this module (confirmed via
  directory listing). Per EPIC #1569 ("Not yet architected... Do not build from it in its
  current state") and the fixed backend→pack→Design→wire order on #1485, the module has not
  been rebuilt yet — #1581 (contract pack) → #1745 (Design export) → #1746 (wire) are all still
  open and blocked in that order, per #1746's own body ("There is no UI to wire until an export
  exists in `Design/portal/`"). So the hardcoded-name bug is gone by deletion of the whole
  surface it lived in, not by a targeted fix, and there is nothing left to patch: writing a fix
  into a file that no longer exists, or standing up a new retainer page under `artifacts/portal`
  ahead of its Design export, would both violate this build's own SCOPE STOP. **Sibling issue
  #1590** (`useRetainerLive.ts:162`'s `NO_TERMS = []`) has the identical root cause — same
  deleted file, same non-existent replacement page — and is addressed by the same finding.
  **DB check:** `retainer_settings`, `retainer_work_log`, `status_reports` all confirmed via
  direct `psql` query to still be 0 rows each (unchanged since #1580's audit) — no live data
  exists that this fixture leak could actually be affecting today regardless. Posted this finding
  as a comment on both #1588 and #1590. Did not close either issue (Claude Code never closes
  issues) — recommending Shane close both as superseded once #1746 lands the real page, since
  reopening either against `artifacts/portal` post-#1746 would need its own fresh audit against
  whatever the new page's data flow actually looks like. No migration, no schema change, no
  commit — nothing to run or push beyond this bookend. Working tree confirmed clean
  (`git status --porcelain` empty) before writing DONE.
