# #1932 — Build set send-to-chat button should remember what it already sent

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** db968e5f9 (feature), db6d7b65a (IN FLIGHT bookend); landed on origin/main as f53e4ba05

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1893's landed diff (0d043e857) and BuildRollupRow/send-button
  wiring in `Controls/BuildQueuePanel.xaml.cs` in full. About to add per-build-set sent-issue
  tracking so the send button hides once everything currently Verifying has already been sent,
  and only reappears when a genuinely new item enters Verifying.
- 2026-08-30 ✅ DONE — Added `_sentVerifyingByBuildSet: Dictionary<string, HashSet<int>>` (in-memory
  only, deliberately not persisted across an app restart — see the field's own doc comment for the
  reasoning: a restart means BuildConsole is fresh, and Shane re-seeing an already-sent "landed"
  line in a still-open chat once is a much smaller cost than losing a real send to stale/misloaded
  state across a restart). `BuildRollupRow` now computes `unsentVerifying` (verifying minus what's
  already recorded sent for that build set) and uses it — not the raw `verifying` list — for: (1)
  whether the ✈ button renders at all, (2) the button's tooltip count, and (3) the text actually
  sent. On a successful send (`isError == false`), the sent items are added to the per-set HashSet
  immediately, then the row rebuild that makes the button visually disappear/update is deferred to
  the same 5s timer that already hid the status message (so Shane sees the "Sent" confirmation
  before the row re-renders, rather than it vanishing instantly). A failed send marks nothing, so
  the button stays visible/re-sendable. Commit db968e5f9.
  Rebased onto origin/main and pushed directly (f53e4ba05) per this repo's "commit to main"
  convention. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  warnings/errors. `verify-branch-merged.mjs` confirms f53e4ba05 is an ancestor of origin/main.
  `git status --porcelain` clean. No test manifest — desktop/BuildConsole is explicitly out of
  scope for those.
