# #3613 — Build Queue search should bypass active filters, not just narrow within them

- **Status:** ✅ DONE 2026-09-10T22:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 4467a9ef2 (Build Queue search bypasses active status/build-set filters)

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — Read RenderQueue/ApplyFilter/RunQueueSearch in
  BuildQueuePanel.xaml.cs. Plan: move the ApplyFilter(_lastItems) decision inside
  RenderQueue itself, gated on whether `_queueSearch` is non-empty — when searching,
  bypass status+build-set narrowing entirely (still excluding manually-hidden ids),
  otherwise behave exactly as before. Update all RenderQueue call sites to pass the
  raw `_lastItems` instead of pre-applying ApplyFilter. Remove the ShowBuildSetFilterWarning
  band-aid call from the search path (Dispatch's NotifyBuildDispatched call stays —
  that's a different trigger, unaffected by this issue).
- 2026-09-10T22:55:00Z ✅ DONE — RenderQueue(rawItems) now applies ApplyFilter only when
  `_queueSearch` is empty; while searching it filters `_lastItems` down to just
  !`_manuallyHiddenQueueIds`, so a real match outside the active status/build-set filter
  is found. All 12 call sites updated to pass raw `_lastItems`. Removed the
  `ShowBuildSetFilterWarning("Search")` band-aid (Git #2058) and the now-unused
  `showFilterWarning`/`reason` params — `NotifyBuildDispatched`'s Dispatch-trigger warning
  is unchanged. Also gated the "(build set \"X\")" empty-state suffix on `!searching` so it
  doesn't misname a filter that was actually bypassed. `dotnet build BuildConsole.csproj -c
  Debug` — Build succeeded, 0 errors (6 pre-existing unrelated CS0067 warnings in
  FocusModeBar.xaml.cs). No live BuildConsole instance available to click-test in this
  session; verified by full read of the changed control flow instead — see the commit's own
  comments for the exact before/after reasoning at each call site.
  Commit: 4467a9ef2, pushed straight to origin/main (rebased once, clean fast-forward).
  verify-branch-merged.mjs: OK, ancestor of origin/main. `git status --porcelain`: clean.
  No new findings filed this session.
