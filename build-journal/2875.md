# #2875 — 4 more baseline_action_templates rows store PUT where Graph exposes only DELETE

- **Status:** ✅ DONE 2026-09-04T17:40:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** b6c587d1d (merged to main)

## Log
- 2026-09-04T17:17:16Z ⏳ IN FLIGHT — confirmed current DB state matches issue (5 rows checked:
  delete-group, delete-user, unenroll-device, remove-auth-method all PUT; update-manager
  correctly PUT and untouched). Investigating remove-auth-method's real Graph shape before
  writing the migration.
- 2026-09-04T17:40:00Z ✅ DONE — action.delete-group, action.delete-user, action.unenroll-device
  corrected PUT→DELETE via
  lib/db/migrations/manual/2026-09-04-delete-group-user-device-authmethod-fix-2875.sql, run
  against local Postgres. action.remove-auth-method confirmed to need real investigation, not a
  bare verb swap: Graph has no v1.0 collection-level DELETE on the generic
  /users/{id}/authentication/methods/{id} path (same class of gap #1899 fixed). Since this
  template targets one already-known {{methodId}} rather than a full re-registration fan-out,
  fixed by storing the real first call (GET the method to learn its @odata.type) and adding
  runRemoveAuthMethodAgainstTenant() in workflow-executor.ts to DELETE through the resolved
  typed collection (phoneMethods/microsoftAuthenticatorMethods/softwareOathMethods), wired into
  runBaselineTemplateAgainstTenant() the same way #1899's fan-out is. action.update-manager
  confirmed correctly PUT and left untouched. None of the four has a graph-write-permissions.ts
  rule, so no permission-table change needed. Verified: `tsc --noEmit` clean;
  graph-write-permissions.test.ts 58/58 pass; workflow-executor-core.test.ts 80/80 pass. Commit
  b6c587d1d pushed to origin/main directly (verify-branch-merged.mjs: OK). Working tree clean.
