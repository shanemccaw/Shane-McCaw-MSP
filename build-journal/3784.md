# Build Journal — #3784

- **Status:** ✅ DONE 2026-09-12T06:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T05:30:00Z — Started. Latest issue comment (05:25:47Z) supersedes both body and prior dispatch: full removal of #3713's collapsed icon-rail / circular progress-ring feature, not gating. Proceeding with removal per Shane's explicit instruction.
- 2026-09-12T06:05:00Z — ✅ DONE — Removed `RenderIconRail`, `BuildRailIcon`, `RailSortRank`,
  `IsRailNotable`, `RailIconCacheKey`, `BuildRailOverflowBadge`, `BuildRingArcGeometry`,
  `ShowRailHoverPopup`, the `_railIcons` `KeyedCardPool`, and the `MaxRailIcons`/`RailIconSize`/
  `CollapsedRailWidth` constants from `Controls/BuildQueuePanel.xaml.cs` (grep-confirmed zero
  remaining references anywhere in `desktop/BuildConsole`). Removed `QueueIconRailHost`/
  `IconRailDock`/`RailHoverPopup` XAML and the wrapping `Grid` from
  `Controls/BuildQueuePanel.xaml`; restored `MinWidth="220"` on the root `UserControl`. Restored
  `MainWindow.xaml.cs`'s pre-#3713 collapse behavior (`ColQueue` → width 0 instead of the rail
  width). Reverted `ChatSessionPane.BuildProgressStepRow` back to a private instance method (was
  promoted `internal static` solely so the rail's hover popout could reuse it). Removed the
  now-orphaned `scripts/probes/queue-icon-rail-leak` probe that verified the rail's own
  animation-clock lifecycle. Implementation commit: `f863430cc`. Verified `dotnet build -c
  Debug` clean (0 errors, only pre-existing unrelated warnings). Merged `origin/main`
  (`bdf8de5dc`) into the worktree branch (trivial, no conflicts — unrelated files) and pushed
  directly to `main` at `301e1c2bb`. `git merge-base --is-ancestor 301e1c2bb origin/main`
  confirms genuinely merged. `git status --porcelain` clean before this bookend update.
