# #1573 — Projects: the six existing rows are purchase-flow test artifacts

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** fed5155f6f6b76d00e0b006a8f6eb541aecc82dc

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed via live query that `projects` id 2-7 are exactly
  the six rows the issue describes (all `client_user_id IS NULL`, created 21-23 Jul
  2026, one per service, no `end_date`/`signed_off_at`/`generated_artifacts`/
  `sharepoint_folder_url`/`zoho_project_id`). Investigating the fulfilment/admin
  creation paths to establish whether a project is ever legitimately customer-less,
  then extending the testbed reset SQL to sweep orphan (`client_user_id IS NULL`)
  projects so rows like these can never survive a reset again, regardless of which
  tenant is targeted.
- 2026-08-30 ✅ DONE — Confirmed the six rows are test artifacts (exact match to the
  issue's description, no client, no downstream data populated). Traced the only real
  customer-project creation path (`project-sow-fulfillment.ts`'s
  `fulfillAcceptedProjectOffer()`) and found it structurally refuses to create a
  project with no resolvable owner (`status: "no_owner"`, returns before the insert)
  — so a customer-less project is never legitimate product output today. The one path
  that *can* produce one is `admin-projects.ts` `POST /admin/projects`, which omits
  the `clientUserId` validation `title` already has; filed as its own finding,
  **#1928** (parented to #1570, not fixed here — out of scope for this issue).
  Extended `lib/db/migrations/manual/2026-08-27-testbed-reset-patch-projects-1396.sql`
  with an unconditional orphan-project sweep block (children-first: workflow_steps,
  kanban_tasks, documents, reports, invoices, project_updates, contracts,
  status_reports, project_closures, audit_logs, opportunities,
  client_callback_tokens, quick_win_presentations, then `projects` itself, all keyed
  on `client_user_id IS NULL` rather than a tenant's user array) and ran it against
  local `DATABASE_URL`. Real result: cleared the 6 test-artifact project rows plus
  48 orphaned `workflow_steps` and 6 orphaned `documents` rows that were hanging off
  them (not previously visible — the issue's own live-DB pass didn't check for
  FK-dependent children). Post-run: `SELECT count(*) FROM projects` = 0,
  `count(*) FILTER (WHERE client_user_id IS NULL)` = 0. Note: running this file also
  reset the rest of the local testbed tenant's accumulated test data (13,700 rows
  total across the file's other DELETE blocks) as an honest side effect of running
  the shared reset script, not something scoped narrowly to just the orphan sweep.
  Appended the migration filename to #1630 per the manual-migration convention.
  Filed #1928, parented to #1570, board status set to "AI Batter Up". Branch verified
  merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
