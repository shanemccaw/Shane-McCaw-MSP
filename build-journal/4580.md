# #4580 — Build resolveLicenseUpliftCost — real per-user licence uplift cost resolver

- **Status:** ✅ DONE 2026-09-18T01:23:05Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** caefae4fa (implementation + tests), a4fd1abfe (IN FLIGHT bookend)

## Log
- 2026-09-18T01:14:35Z ⏳ IN FLIGHT — read license-waste-source.ts and cost-engine.ts, implementing `resolveLicenseUpliftCost(tenantId, targetSkuPartNumber)` over `license_assignment_snapshots` + `lookupSkuMonthlyPriceCents`, flat SKU-match v1, null-not-zero discipline.
- 2026-09-18T01:23:05Z ✅ DONE — Added `resolveLicenseUpliftCost` + pure `licenseUpliftPopulationFromAssignments` helper to `artifacts/api-server/src/lib/license-waste-source.ts` (caefae4fa). Reuses `resolveActiveLicensedUserCount`'s exact latest-run + per-user `license_assignment_snapshots` query pattern and `lookupSkuMonthlyPriceCents` from `cost-engine.ts` verbatim; resolves the target SKU's `skuId` via the existing `resolveSubscribedSkuCatalog` for identifying existing holders. v1 flat SKU-match per Shane's 2026-09-17 decision (no superset/bundling logic — a user holding a superset SKU like E5 still counts toward the uplift population). Returns null (never a guessed number) when the target SKU has no price on file / prices at $0, when no `license_assignment_snapshots` run exists for the tenant, or when the latest run has zero rows. Added 5 new unit tests for the pure population-counting helper (mirrors the existing `activeLicensedUserCountFromAssignments` test pattern) — 20/20 tests pass in `license-waste-source.test.ts`. `pnpm tsc --noEmit` in `artifacts/api-server` exits 0. Not wired into any route/UI — issue scope was the resolver only. Working tree clean, branch verified merged into origin/main via `verify-branch-merged.mjs`.
