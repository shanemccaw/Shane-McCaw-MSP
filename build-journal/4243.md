# #4243 — Consent callback decline branch: bind decline to a valid state

- **Status:** ✅ DONE 2026-09-15T14:22:24Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** c6aa7a6d5 (fix + tests)

## Log
- 2026-09-15T14:10:00Z ⏳ IN FLIGHT — bind the `/api/consent/callback` decline branch to a valid, unexpired `state` (invite token / checkout session); unbound declines log and write nothing. Live-verify pre/post on a scratch DB copy.
- 2026-09-15T14:22:24Z ✅ DONE — What changed: the `access_denied` branch of `/api/consent/callback` no longer stamps `graph=declined` on any tenants row matching the bare, public GUID. New `resolveDeclineTarget` in `consent.ts` records a decline only against the customer a live `state` names. That means either (a) an unused, unexpired invite token with a `customer_id`, where that customer's `tenant_id` matches the GUID (the same `resolveCallbackTenant` binding the grant path uses), or (b) an unexpired checkout session whose `tenant_id` already equals the GUID. `checkout_sessions.tenant_id` is only written by a Microsoft-confirmed grant. Every other case writes nothing and logs a warn with a reason. The stamp is now keyed by `tenants.id`, not GUID. Invite burn on decline is restricted to a live token (`used_at IS NULL`). A live token is still burned when its GUID mismatches, as before. **Unit tests:** consent.test.ts 83/83, including 7 new decline cases; the 5 refusal cases were confirmed to fail against the pre-fix route. api-server `tsc --noEmit`: 0 errors. **Live test:** run on a pg_dump scratch copy `zz_test_4243`, so the real dev DB was never written, using a consent-router-only tsx harness with the pre-fix route on :8243 and the fixed route on :8244. It used synthetic `zz-test-4243` Victim/Invitee/Buyer customers (direct MSP, graph granted), a live invite token for Invitee, one unbound checkout session and one session bound to Buyer. Pre-fix: three anonymous GETs (no state / unbound checkout session / made-up token) flipped all three to `declined`. Post-fix, after resetting them to granted: the same three, plus a live token with the wrong GUID, all stayed `granted`. Logged reasons: no_state, checkout_session_not_bound_to_tenant, invite_token_invalid_expired_or_used, invite_token_tenant_mismatch. Positive controls on the fixed build: the live token with its own GUID declined Invitee and burned the token, and the bound checkout session declined Buyer. **Cleanup:** harness processes stopped by port, scratch DB dropped, scratch files removed. The real DB has 0 zz-test-4243 tenants/tokens/sessions, and tenants 1 and 3 are still `granted`. **Findings filed** under #1650: #4253 (callback logs the raw invite token `state` in plaintext, including on a 400 path that leaves it unburned). No migrations. No test manifest: the change is api-server only.
