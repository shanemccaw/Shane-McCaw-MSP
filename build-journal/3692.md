# Build Journal — #3692

- **Status:** ✅ DONE 2026-09-11T20:06:32Z
- **Scope:** buildconsole

## Log
- 2026-09-11T19:50:11Z — Session started. Task: add `design_url` to `bt_epics`, new epic-header context menu action "Set Claude Design URL…", and an accent pill above the progress bar (shown only when set) that opens the URL in the default browser. Sub-issue of #1202 (Epic: Build Console).
- 2026-09-11T20:06:32Z — ✅ DONE — commit `8a2e38f9f` (rebased onto and pushed to `origin/main`; verified ancestor via `git merge-base --is-ancestor`). Shipped:
  - `lib/db/migrations/manual/2026-09-11-bt-epics-design-url-3692.sql` — `ALTER TABLE bt_epics ADD COLUMN IF NOT EXISTS design_url TEXT;` — run by this session against the local BuildConsole Postgres DB (`BUILD_DATABASE_URL`), confirmed via `\d bt_epics`.
  - `Services/BuildTrackerApiClient.cs` — `BoardEpic.DesignUrl`.
  - `Services/BuildQueuePostgresClient.cs` — `GetBoardAsync`'s epic SELECT now reads `design_url`; new `SetEpicDesignUrlAsync(githubNumber, url)` (keyed by `bt_epics.github_number`, repo-scoped, clears on null/blank).
  - `Controls/LeftSidebar.xaml.cs` — `ChatEpicGroup.DesignUrl` threaded through every construction/rebuild site (milestone mode, legacy mode, sub-epic nesting, search-filtered rebuild); `BuildEpicSection` gets a new right-click "🎨 Set Claude Design URL..." menu on the epic header (opens a Save/Clear/Cancel dialog, prefilled with the current URL) and a new accent-tinted pill row inserted between the header and the progress bar, shown only when `DesignUrl` is set, opening the URL via `Process.Start(UseShellExecute=true)`.
  - Verification: `dotnet build` succeeded (0 errors, only pre-existing unrelated warnings). Directly simulated the Save/Clear SQL paths against the real local `bt_epics` row for #1202 (Epic: Build Console) — confirmed `UPDATE ... SET design_url = '...'` sets it and `UPDATE ... SET design_url = NULL` clears it, matching the pill's visibility condition. Did not launch BuildConsole.exe itself to click-test (prohibited per Git #2156) — build success + direct DB-level verification of both write paths is the real verification here.
  - `git status --porcelain` clean at session end; no unrelated dirty files encountered.
