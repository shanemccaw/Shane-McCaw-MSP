# Build Journal — #1397

- **Status:** ✅ DONE
- **Scope:** platform
- **Git #:** 1397
- **Title:** customer data scoped to individual login (clientUserId) instead of customer account
- **Commit:** 4e0dc1b29

## Log
- ⏳ IN FLIGHT — Investigating projectsTable.clientUserId scoping. Narrow fix per Shane: migrate OLD per-user scoping key to already-proven customerId pattern. Fix projectsTable + confirmed query sites (portal-customer-engines.ts ~435, ~536) first, then grep for the same pattern and report by impact.
- ✅ DONE (4e0dc1b29) — Schema question answered: NEITHER drop clientUserId NOR add a customerId column. The proven customer-scoping bridge already exists — `resolveCustomerUserIds(customerId)` / `resolveSiblingUserIds(userId)` in `tenant-signals.ts` (users.tenant_id → tenants.id), whose own doc comment explicitly names `projects.clientUserId` and `client_services.clientUserId` as users.id-shaped tables meant to be read customer-scoped via it. `portal-assessment.ts` + admin routes already use it; the affected routes just never got it. `assessmentSowAgreementsTable` even already has a `customerId` column. No schema migration needed. Fixed the confirmed file thoroughly (portal-customer-engines.ts — all 4 handlers) plus 6 more customer-facing route files (projects, delivery-kanban, documents, messages, presentations, privacy). Kept per-login: notification feed + audit actor (Shane's "maybe alert preferences" exception). Deferred by impact (money-path writes needing sign-off): portal-billing.ts (~11 sites), portal-retainer-billing.ts (~3 sites). tsc clean on all edited files; all touched-file tests green (privacy 6, presentations, messages, delivery-kanban 18, 3 engine tests 15). Also fixed 2 pre-existing red tests touched here.
