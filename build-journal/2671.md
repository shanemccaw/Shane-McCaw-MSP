# #2671 — MSP console: no operator routes exist for Policy Decisions — build the real backend

- **Status:** ✅ DONE 2026-09-03T20:20:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** c72a0ae8e (MSP-side Policy Decisions routes)

## Log
- 2026-09-03T19:56:00Z ⏳ IN FLIGHT — Audited the real `policy_decisions` contract
  (`lib/db/src/schema/msp.ts`, `artifacts/api-server/src/routes/portal-policy-decisions.ts`,
  Git #2024/#1526/#2518). Building the missing MSP-side `msp-policy-decisions.ts` routes
  (view + manage, scoped by `:customerId` via `resolveTenantScope`, same shape as
  `msp-security-plan.ts`'s `resolveOwnedTenant` pattern), plus tests.
- 2026-09-03T20:20:00Z ✅ DONE — Shipped `artifacts/api-server/src/routes/msp-policy-decisions.ts`:
  `GET /api/msp/policy-decisions/:customerId` (every policy decision for one customer in the
  caller's own MSP book) and `PATCH /api/msp/policy-decisions/:customerId/:id/clearance/resolve`
  (manual mark-resolved for a dependency-based decision, #1526 — `manual` trigger only,
  one-shot). Both `requireRole("MSPOperator")`, scoped via `resolveMspIdStrict` +
  `resolveTenantScope` with a `resolveOwnedTenant` helper (mirrors `msp-security-plan.ts`'s own
  — a tenant belonging to another MSP 404s, no cross-MSP existence leak). Registered in
  `routes/index.ts`. Deliberately did NOT build an MSP-side create/author endpoint —
  `policy_decisions` has no unsigned intermediate state and an MSP-typed `signed_by` would
  misattribute the customer's own consent on a real compliance record; documented in the
  route's own header and filed as a real product-decision finding under #1685 rather than
  guessed at.

  Tests: `msp-policy-decisions.test.ts` (11 cases — ownership-scoping 403/400/404s, list shape,
  and the same manual-resolve business rules `portal-policy-decisions.test.ts` already covers:
  manual-only, one-shot, cross-tenant 404, empty-note reject), registered in
  `vitest.config.ts`'s include allowlist.

  **Honest verification gap:** `artifacts/api-server` vitest could not actually be *executed*
  this session — the `node_modules/.pnpm/vitest@4.1.9.../` store entry was pre-existing empty
  (confirmed in both this worktree and the main checkout, unrelated to this build) — filed as
  #2735. `npx tsc --noEmit -p .` DID run and shows the new file producing the exact same
  pre-existing baseline error pattern as the already-shipped `portal-policy-decisions.ts`
  (missing `lib/db/dist`, unresolved `drizzle-orm`/`zod` types) and zero errors beyond that
  baseline — the new code typechecks cleanly relative to the known-good sibling it mirrors.
  Also documented on #2735: while diagnosing the empty vitest store, a heredoc-with-backticks
  shell quoting failure in this environment appears to have triggered an unintended
  `pnpm store prune` (matching output observed: "Removed 29116 files (557 MB) / Removed 645
  packages"), and a later `pnpm store status` check confirmed the store now reports 17 core
  packages (express, pg, drizzle-orm, jsonwebtoken, ...) as "modified" —
  `[ERR_PNPM_MODIFIED_DEPENDENCY]`. This is a real, more-severe-than-first-filed incident,
  updated onto #2735 as a comment; per the repo's own "pnpm install is not a remedy" rule this
  was not self-remediated.

  Findings filed: #2735 (URGENT — broken vitest store + the store-mutation incident above),
  parented under #1685, labeled `bug`, board status set to "AI Batter Up".
