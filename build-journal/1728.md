# #1728 — SOPs / Runbooks: regenerate the contract pack from the finished backend

- **Status:** ✅ DONE 2026-09-03T03:58:44Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `59f07eda5` (SOPs/Runbooks: regenerate contract pack from the finished backend)

## Log
- 2026-09-03T03:49:16Z ⏳ IN FLIGHT — regenerating `docs/sops-contract-pack.md` and
  `docs/runbooks-contract-pack.md` from the real, current backend. Prior packs were
  written 2026-08-29/2026-08-30 (`3b6b14b7f` / `97873aafc`); real drift confirmed since
  then (`5ec915f0c` cycle-affinity `run_id` on hold windows, `57f187873` narrowed
  `POST /api/msp/sop-runs`, `7b8d57450`/`f38ea85b4` CR write-gate + approval-ledger
  materialization on the runbooks CR-creation path). Read-only on product code per
  #1728/#1577; only the pack files and any findings sub-issues are written.
- 2026-09-03T03:58:44Z ✅ DONE — commit `59f07eda5`, verified merged into `origin/main`
  (`verify-branch-merged.mjs` exit 0), working tree clean (`git status --porcelain`
  empty). Both `docs/sops-contract-pack.md` and `docs/runbooks-contract-pack.md`
  regenerated wholesale from the real, finished backend, extracted verbatim and cited
  to file:line — no product code, schema, or UI touched.

  Real drift found and documented since the prior packs (all verified against source
  and, where relevant, direct `psql` reads of local `DATABASE_URL`):
  - **#1556, #1557, #1558, #1559, #1560 — all DECIDED-not-built in the prior packs —
    are now DONE and built.** `msp_sop_runs.origin`/`sopVersion` are real columns;
    `portal_runbook_runs` gives runbooks a real per-cycle history; `portal_sop_custom_steps`
    is a real per-tenant SOP overlay table; `POST /api/msp/sops/:sopId/run` is a real,
    CR-gated, Workflow-Engine-executed execution hook; 4 new lifecycle SOPs (IAM-04–07)
    are seeded — confirmed 15 real `msp_sops` rows for the testbed MSP via `psql`.
  - **#1940 (cycle affinity on hold windows)** — `portal_hold_windows.run_id` is real
    and gates which cycle a window decorates; documented in both packs.
  - **#1938 / #1773 / #1775** — hardening on the execution/CR-authorization/approval-
    ledger paths, all real, cited in both packs.
  - **Corrected a stale premise in both prior packs**: they cited
    `artifacts/msp-portal/src/components/portal-v2/{sopHubData,sopHubModel,useSops,
    sopCreate}.ts` as live consumers. That package was retired for `artifacts/portal`
    (`f40438cdc`, #1921) and none of those four files were carried over — confirmed by
    direct search. Every `/api/portal/sops*` route is currently orphaned from a page's
    perspective (not just the one endpoint the prior packs flagged), and the
    Runbooks-side hooks (`useRunbooks`, `useHoldBadge`) exist and are correct but have
    zero importers anywhere in `artifacts/portal/src`. Documented in both packs' §0 as
    the expected, correct state for a module with no `Design/portal/` export yet
    (confirmed empty at pack time) — not a new bug, and not re-filed as a new
    sub-issue, since the identical root cause is already tracked on #1619/#1620.
  - Confirmed live-empty DB state where relevant: `portal_runbooks`,
    `portal_runbook_runs`, `portal_hold_windows`, `portal_sop_custom_steps`, and
    `msp_sop_runs` are all genuinely 0 rows on this environment right now — documented
    in both packs' honest-empty sections as real, not hypothetical.

  **No new GitHub issue filed.** Every finding in this pass — the widened orphaned-
  endpoint scope, and the still-open gaps carried forward (hold-window notification
  transport, the standard/non-standard runbook-classification rule, the #1548
  lifecycle-invocation wiring, `portal_runbooks.status` staying dead weight, the
  #1539 denied-consent instructions behaviour not found built) — shares its root
  cause with an issue already on the board (#1619/#1620/#1548/#1560's own bookend),
  so nothing here is a genuinely new, previously-untracked defect. Both packs state
  each open gap explicitly rather than resolving or papering over it.

  Working tree left clean; nothing else touched.
