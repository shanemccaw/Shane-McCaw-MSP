# Build Journal — #3101

- **Status:** ✅ DONE 2026-09-09T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-09T00:00:00Z — Started. Fixing artifacts/api-server/vitest.config.ts: maxThreads nested under removed `poolOptions` key (Vitest 4 removed test.poolOptions), so #3066's thread cap is inert. Lifting maxThreads to top-level test option.
- ✅ DONE 2026-09-09T00:20:00Z — Lifted `maxThreads: Math.max(1, Math.floor(cpus().length / 2))` from `test.poolOptions.threads` to a top-level `test` option, per Vitest 4's pool-rework migration guide. Same value/comment/reasoning as #3066. Verified with a real full-suite run (`npx vitest run`): the `DEPRECATED poolOptions` warning that printed on every prior invocation is now gone (`grep -ic deprecated` on the run's full output = 0), confirming the cap is genuinely read again, not just relocated. Full-suite run: 64 files / 34 tests failed, 279 files / 4597 tests passed — that failure set (azure-rm mocking, admin-signal-rules, portal-offers, etc.) is the same pre-existing, unrelated class named in #3082/#3101's own issue body, not caused or fixed by this change. Committed 9f1e276e6 (bookend), then the fix commit on agent/3101-q2085, merged directly into main (fast-forward merge commit) and confirmed via `git merge-base --is-ancestor` that the branch HEAD is an ancestor of origin/main.
- Merged commit on main confirmed ancestor via `git merge-base --is-ancestor agent/3101-q2085 origin/main`.
