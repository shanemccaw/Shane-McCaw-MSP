# Issue #2974 — Route sharepoint-admin read_transport to sharepoint-admin.ts

- **Status:** ✅ DONE 2026-09-07T15:13:28Z
- **Scope:** platform
- **Commit:** 23a02362d651981e797565f0043dac51e0ae17d9 (merged into origin/main; verify-branch-merged.mjs confirmed)

## Log
- 2026-09-07T14:58:39Z — Session started. Pulled full issue body + comments. Task: route
  `read_transport = 'sharepoint-admin'` rows in config-snapshot-collector.ts to the real
  `sharepoint-admin.ts` CSOM client instead of the PowerShell container, per #2943's
  investigation and live probe evidence. Investigating collector + client now.
- 2026-09-07T15:12:20Z — Implemented and verified live against tenant 1 (testbed,
  mccawsoft2.onmicrosoft.com). Changes:
  - `sharepoint-admin.ts`: added `getTenantProperties()` (full CSOM `Tenant` read,
    `SelectAllProperties="true"`, the same query `Get-PnPTenant` issues) and
    `adminHostRestGet()` (read-only GET/POST against the admin host for the proven
    REST/method endpoints).
  - `monitor-executor.ts`: exported `resolveSharePointTenantRef` (was
    module-private) so the config-snapshot collector reuses the SAME SharePoint
    tenant-prefix resolution the existing `sharepoint-admin` check executor
    (#394) already uses, rather than a second implementation — no new column,
    no migration needed.
  - `config-snapshot-collector.ts`: added `collectSharePointAdminResource()`,
    `SHAREPOINT_ADMIN_RESOURCE_MAP` (11 proven-live resource-key mappings) and
    `SHAREPOINT_ADMIN_DEFERRED_REASONS` (12 resource keys with a real, specific,
    honest reason each — fan-out budget, unproven endpoint, XML search shape,
    no read cmdlet recorded, or already served by an existing Graph row — never
    the old blanket "container has no PnP" reason). Split the dispatch branch so
    `sharepoint-admin` no longer falls into `collectPowerShellResource()` at all.
    Also added a `SharePointAuthError` branch to `classifySnapshotFailure` so a
    real SharePoint auth failure classifies as `permission_denied` with real
    wire evidence instead of `unknown_error`.
  - `tsc -p tsconfig.json --noEmit` on `artifacts/api-server`: clean, exit 0.
  - **Live verification**: wrote a temporary vitest test calling
    `collectSharePointAdminResource()` for all 23 real registry rows against
    tenant 1 (real testbed/production M365 tenant, read-only GETs/CSOM `Query`
    only — no writes). Result: **11 of 23 resource types now genuinely collect
    real data** (ODSettings, SPOAccessControlSettings, SPOSharingSettings,
    SPOTenantSettings, SPOOrgAssetsLibrary, SPOTenantCdnEnabled,
    SPOTenantCdnPolicy, SPOHomeSite, SPOHubSite, SPOSiteDesign, SPOSiteScript —
    each returned `objects.length >= 0` with a real `sharepoint-admin:...`
    requestRef, not a `no_executor` skip). The other 12 still return
    `no_executor`, each with its own real, accurate reason (verified in the same
    live run) rather than the prior false "container can't do it" reason. Test
    file deleted after the run — not part of the deliverable, scratch
    verification only.
  - No DB migration was needed — reused the existing `tenants.domain` +
    Graph `/organization` fallback prefix resolution already implemented for
    check #394, per rule 1 (reuse the existing execution layer, no second
    transport/resolution path).
- 2026-09-07T15:13:28Z — ✅ DONE — commit 23a02362d651981e797565f0043dac51e0ae17d9,
  merged into origin/main (verify-branch-merged.mjs confirmed OK). `git status
  --porcelain` clean. 11 of 23 sharepoint-admin resource types confirmed
  genuinely collectable live against tenant 1; the other 12 remain
  `no_executor` with real, individually-stated reasons instead of the old
  false "container has no PnP" reason. Issue #2974 comment posted; labels
  synced.
