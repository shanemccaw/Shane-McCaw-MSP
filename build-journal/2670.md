# #2670 — MSP console: no operator routes exist for Remediation Tracking — build the real backend

- **Status:** ✅ DONE 2026-09-03T20:07:38Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 4651c70e1 (routes), 601d7c74 (merge onto origin/main)

## Log
- 2026-09-03T19:55:04Z ⏳ IN FLIGHT — audited the real customer-facing `/portal/remediation*`
  contract (portal-remediation-tracker.ts, -scores.ts, -export.ts, portal-remediation-checklist.ts,
  -fix-routes.ts, -reveal.ts, -bypass-resolutions.ts) and the established MSP single-customer route
  pattern (`msp-diagnostics.ts`: `requireRole("MSPOperator")` + `assertCustomerAccess(req.user!, customerId)`,
  customerId from `:customerId` param not JWT claim). Building the MSP-side mirror under
  `/api/msp/customers/:customerId/remediation*`, reusing the same `lib/remediation-*.ts` functions
  the portal routes already delegate to (customerId-parameterized, not JWT-coupled) so there is one
  source of truth for the business logic and only the auth/customerId-resolution differs.
- 2026-09-03T20:07:38Z ✅ DONE — shipped 7 new MSP-side route files + wired them into
  `routes/index.ts`, covering every route in the real `/api/portal/remediation*` contract except
  the customer's own signed decline-to-risk (see below):
    - `msp-remediation-tracker.ts` — GET tracker, PUT step, POST verify, GET verification-guide
    - `msp-remediation-tracker-scores.ts` — GET pillar-scores
    - `msp-remediation-tracker-export.ts` — GET export.csv/.pdf, evidence-pack.pdf
    - `msp-remediation-checklist.ts` — GET/PUT checklist, POST raise-change
    - `msp-remediation-fix-routes.ts` — GET fix-routes
    - `msp-remediation-reveal.ts` — POST fix-routes/:checkKey/reveal
    - `msp-remediation-bypass-resolutions.ts` — GET bypass-resolutions (that file's own header
      already said "so an MSP can see both" — this is the first MSP-scoped route that actually
      reaches it)
  Every route: `requireRole("MSPOperator")` + `assertCustomerAccess(req.user!, customerId)` off
  `:customerId`, 400 on a malformed id, 404 (never a distinguishable error) on a customer outside
  the caller's book — the same convention `msp-diagnostics.ts`/`msp-active-directory.ts` already
  use. Each route reuses the exact same `lib/remediation-*.ts` business logic the portal routes
  call, so there is one source of truth for every write rule (idempotent upsert, `completedAt`
  derivation, verification reset on write, `accepted_risk` rejection).

  **Deliberately not mirrored:** `POST .../decline-to-risk`. That route creates a SIGNED customer
  liability record (typed full name + explicit confirmation, `requireRole("CustomerUser")` — a
  higher floor than the rest of that journey specifically because of what it creates). An MSP
  operator completing that on the customer's behalf would misattribute who actually accepted the
  risk — a real product/legal distinction, documented in `msp-remediation-tracker.ts`'s header,
  not a missing route. If Shane wants an MSP-initiated risk-decline flow later it needs its own
  signed-actor design.

  Added `msp-remediation-tracker.test.ts` covering the one thing that actually differs from the
  already-tested customer-facing route: the ownership gate (400 malformed id / 404 non-owned
  customer / 200+correct wire shape once ownership clears), plus the `accepted_risk` rejection and
  the retainer-work-logger hook firing for the MSP actor. The underlying write-rule logic itself
  (`completedAt` derivation, verification reset, catalogue drift) is already proven by
  `portal-remediation-tracker.test.ts` since both routes call the identical code.

  **Verification, honestly:** `tsc`/`vitest` could not be run — the shared MAIN checkout's pnpm
  store has core packages (express, zod, drizzle-orm, vitest — confirmed via direct filesystem
  check: `.pnpm/<pkg>@.../node_modules/<pkg>` extracted with 0 entries) genuinely empty, a
  pre-existing, repo-wide corruption already extensively filed under many open issues
  (#2503, #2515, #2516, #2522, #2525, #2526, #2528, #2556, #2192, #2193, #2502, #2508, #2546,
  #2732) — not something this session introduced or can fix (`pnpm install`/`--force` is
  prohibited as a remedy per CLAUDE.md and would not repair extracted-empty store dirs anyway). No
  duplicate finding filed since the corruption is already exhaustively tracked. Verified instead by
  cross-checking every new import against its real export in the source `lib/*.ts` files (all
  confirmed present with matching signatures) and by mirroring the already-compiling
  `portal-remediation-*.ts` routes near line-for-line, changing only the auth/customerId-resolution
  block.

  Merged onto `origin/main` in-session (merge commit `601d7c74`, no conflicts) —
  `verify-branch-merged.mjs` confirms OK. Working tree clean (`git status --porcelain` empty).
