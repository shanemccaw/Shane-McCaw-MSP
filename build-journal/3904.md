# #3904 — Change Control: direct MSP-role approve/reject route (resolves #3761, option 2)

- **Status:** ✅ DONE 2026-09-13T04:37:15Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 020089429 (pre-existing, landed on main before this build was dispatched — see Log)

## Log
- 2026-09-13T04:20:00Z ⏳ IN FLIGHT — pulled #3904's full body + the single dispatch comment; began investigating
  `portal-change-approvals-store.ts`, `msp-changes.ts`, and
  `artifacts/msp-console/src/pages/change-control/Register.tsx` per the dispatch scope.
- 2026-09-13T04:37:15Z ✅ DONE — Found the entire scope of this issue already shipped on `main`, ahead of this
  dispatch: commit `020089429` ("Direct MSP-side Change Control approval endpoint (Git #3761)", authored
  Sat Sep 12 21:11:52 2026, already an ancestor of `main` before this build started) adds
  `POST /api/msp/change-requests/:id/approve` in `artifacts/api-server/src/routes/msp-changes.ts` (calls
  `recordApproval` with `role: "msp"`, `requireCapability("ladder.msp-operator")` auth/scoping matching every
  other MSP Console write route, refuses with 409 when the change is already on an open CAB agenda via a new
  `isOnOpenCabAgenda()`, and works with zero CAB agenda item required — verified directly in the route body
  and its live-Postgres test `msp-changes-approve.live-db.test.ts`, 3 cases: direct approval of a
  freeze-exception msp stage with no CAB agenda, 409 when parked on an open CAB agenda, 409 via
  `recordApproval`'s own no-pending-stage guard). Reject already worked via the pre-existing generic
  `PATCH /api/msp/change-requests/:id` `status:"rejected"` branch calling `recordRejection` (#3033) — the
  same commit's own message notes "no reject-side gap existed," which matches what's in the file today.
  The same commit also wired `Register.tsx`'s `CrDetailDrawer`: a pending `msp`-role stage now renders real
  `Approve`/`Reject` buttons (`useApproveChangeRequest()` calling the new route, plain `patch.mutate` reject),
  falling back to "Decide via Advisory Board" ONLY when the server actually returns a 409 for an open CAB
  agenda (`approveCabBlocked` state, set only in that `onError` branch) — not as the default path. This is
  exactly what #3904 asked for; there is nothing left to build.
  No code changes made this session — reading the merged commit and the current file state is the entire
  verification. `git status --porcelain` is clean; no files touched.
