# #3993 — Consent and Onboarding: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:55:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 35f674c54 (original), rebased twice onto concurrent landings → a3d3b5653 on origin/main

## Log
- 2026-09-14T04:05:00Z ⏳ IN FLIGHT — Pulled #3993 + #1650 body/comments, the consent-and-onboarding
  contract pack (#2758, `Design/portal/design_handoff_full_site/docs/consent-and-onboarding-contract-pack.md`),
  `Consent and Onboarding.dc.html`'s 6 real scenes, #1972's resolution, and all 5 archived pages
  (`portal-archive-2026-08-29`). Confirmed real gaps to build around:
  - The design's `onboarding-link` scene (accept an MSP-generated invite) has NO backing "start
    consent" endpoint — `mspOnboardingLinksTable` tokens have no bridge into the Microsoft
    consent-redirect machinery (`consentInviteTokensTable`/`buildAdminConsentUrl`), and no `mspId`
    scoping path either. This touches the same tenant-ownership logic behind a real, cited past
    cross-tenant leak (consent.ts's own tenant-conflict guard) — treating as a real product/security
    decision to flag rather than hand-roll; will wire the read-only landing + honest "not available"
    state for Connect and file a sub-issue.
  - `initiate-onboarding.tsx` (the MSP-operator "generate link" form) has NO `.dc.html` anywhere in
    `Design/portal/` or `Design/MSP_Console/` — per CLAUDE.md's hard rule (no design, no UI), not
    building this form's UI in this pass; will note in the sub-issue.
  - The declined page's scope list needs a public read of `REQUIRED_MT_SCOPES` — no such GET route
    exists yet (`POST /public/free-scan/start` bundles it with a lead-capture side effect). Will add
    a minimal `GET /api/public/consent-scopes` (no schema change, pure read of the existing const).
  - `login.tsx`/`App.tsx` in `artifacts/portal` currently have NO staff-role gate at all — any
    authenticated role lands straight on the customer Overview. Will wire the interstitial as a
    real gate, not just a page.
- 2026-09-14T04:35:00Z ⏳ IN FLIGHT — Built: `src/lib/consent-onboarding-api.ts` (wire types/fetchers per
  contract pack #2758), `src/components/consent/ConsentOnboardingShell.tsx` + `CaptchaGate.tsx` (shared
  chrome/Turnstile ported from the archived page's logic, no markup carried forward), and 5 pages:
  `consent-success.tsx` (portal-origin acknowledgement + the same-tab `session`-present finalize
  fallback, wired to `POST /portal/onboarding/contract` → `POST /portal/checkout/free`), `consent-declined.tsx`,
  `consent-tenant-conflict.tsx`, `onboarding-link.tsx` (new — the design's `onboarding-link` scene, backed
  by the previously-unwired `GET /api/public/onboarding/link/:token`), and `portal-identity-interstitial.tsx`,
  wired as a real `RequireAuth` gate in `App.tsx` (not just a page — confirmed `login.tsx`/`App.tsx` had
  zero staff-role gating before this).
  Self-caught mid-build: `GET /api/public/consent-scopes` already exists and is live (#433,
  `public-consent-scopes.ts`, registered in `routes/index.ts`) — a route I almost duplicated in
  `public-services.ts` before noticing the running dev server already answered it (raw array, not
  `{scopes:[...]}`). Reverted the duplicate, wired the frontend to the real shape instead. No schema
  change and no new backend route needed in this build after all.
  Filed **#4010** (sibling sub-issue of #1650, `bug` label, board status "AI Batter Up") for the two
  real gaps that remained honest instead of faked: the onboarding-link's real minted URL
  (`${SITE_URL}/onboarding/:token`) is a bare top-level path this SPA can't serve at its fixed `/portal/`
  mount, and there's no consent-kickoff bridge from an `msp_onboarding_links` token into the Microsoft
  admin-consent redirect machinery (`consentInviteTokensTable`/`buildAdminConsentUrl`) — checked
  `msp-consent.ts` too (an MSP-operator invite-link route) and confirmed it requires an existing
  `customerId`, so it doesn't cover this pre-checkout-prospect case either. `initiate-onboarding.tsx`
  (the MSP-operator "generate link" form) has no `.dc.html` anywhere in `Design/portal/` or
  `Design/MSP_Console/`, so its UI was not built this pass (CLAUDE.md's no-design-no-UI rule) — noted
  in #4010.
  Also updated the stale `test-manifests/portal/identity-interstitial.json` (portal-v2 era —
  `/portal-v2`, `pv2-sidebar`, `TenantSwitcherFloaty`, `mspRole: "Customer"`, none of which exist in the
  rebuilt `artifacts/portal`) in place per the discover-before-create rule, and added
  `test-manifests/portal/consent-onboarding.json` (registered in `_regression-suite.json`) covering the
  live `GET /api/public/consent-scopes` read, the `GET /api/public/onboarding/link/:token` 404 shape, and
  the 3 unauthenticated UI routes' real DOM content. Added `data-testid` attributes to the interstitial
  matching the old manifest's convention (`identity-interstitial-card`/`-role`/`-accept`/`-logout`).
- 2026-09-14T04:50:00Z ⏳ IN FLIGHT — Verification: `tsc --noEmit` clean on `artifacts/portal` (twice —
  once pre-merge, once post-rebase) and `artifacts/api-server`; `vite build` clean on `artifacts/portal`
  (pre-existing sourcemap warnings only, unrelated); both new/edited manifests are valid JSON. Manually
  curl-verified the live dev api-server (`:8080`, running from the shared dev-server checkout) for
  `GET /api/public/consent-scopes` (200, real 35-entry array) and `GET /api/public/onboarding/link/does-not-exist`
  (404, real error body) — both match what the pages/manifest expect. Did **not** get a live `shaneapp://runTest`
  UI run in this session: no `shaneapp://` tool is reachable from this chat session directly, and the
  portal front-end dev server (localhost:26059) was not running to drive a browser against — the manifest
  is written, registered, and ready for the next session/BuildConsole pass with real UI access to run it.
  Honest gap, not a silent claim of a UI pass that didn't happen.
  Rebased twice onto concurrent landings on `origin/main` (once mid-push, once again after — other
  sessions landed #3992 Signup/Agreement/Invite and #3994 Break-glass verify concurrently in
  `App.tsx`/`_regression-suite.json`); both conflicts resolved by keeping both sides' routes/entries,
  re-typechecked clean after each. `git status --porcelain` clean; `verify-branch-merged.mjs` confirms
  `a3d3b5653` is merged into `origin/main`.
- 2026-09-14T04:55:00Z ✅ DONE — Shipped: 5 real pages + shared shell/captcha component + wire-contract
  lib for Consent and Onboarding, wired into `App.tsx` (both as routes and as a real staff-role
  `RequireAuth` gate), plus a test manifest and one stale-manifest correction. No schema/migration
  needed. Filed #4010 for the two backend gaps that would otherwise have been faked. `initiate-onboarding.tsx`
  intentionally not built (no design export exists for it).
