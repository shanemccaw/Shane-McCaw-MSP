# #1447 — Branch-merge verification: DONE bookends must confirm actual merge to main + real Home-screen sweep

- **Status:** ✅ DONE
- **Scope:** both
- **Started:** 2026-08-27
- **Commit(s):** efaec0409 (merged to main)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting Part 1 (bookend merge-verification requirement/script) and Part 2 (real branch-vs-main stranded sweep, distinct from worktree-lifecycle orphan sweep) per issue body.
- 2026-08-27 ✅ DONE — Part 1: `scripts/dev-server/verify-branch-merged.mjs` (git merge-base --is-ancestor check, JSON + human output) + CLAUDE.md's DONE-bookend step now mandates running it before flipping to DONE. Part 2: `scripts/dev-server/check-stranded-branches.mjs` sweeps every `agent/*` branch (local + `origin/agent/*`) against main and reports commits-ahead; new `StrandedBranchService.cs` + `StrandedBranchHealth` wired into `SystemHealthService.RunFullHealthCheckAsync` (reuses the `worktree.cleanup` ActivityLog channel, same underlying concern) and a new, clearly-separate "Stranded Branches (vs main)" row added to HomeView's health card (distinct from the existing Worktrees row/terminology, per the issue's standing rule) — rides the existing 30s health-refresh timer, so it re-runs on a schedule automatically. Deliberately excluded StrandedCount from the AllHealthy pill (many concurrent in-flight agent branches are legitimately "ahead of main" — not a fault); the row still surfaces the real count. Verified: `dotnet build BuildConsole.csproj` succeeds (0 errors); ran both new scripts standalone against the real repo and confirmed correct output (stranded-branch sweep correctly found #1413/#1415/#1402 etc. still unmerged at the time, matching the issue's own root-cause claim). Rebased onto origin/main and pushed directly (commit efaec0409) per this repo's worktree-isolation git convention; ran `verify-branch-merged.mjs` against the pushed result per this issue's own Part 1 requirement — confirmed merged before writing this DONE.
