# #3782 — Correct MSP Plan Self-Service contract pack (§7/§7.2 stale after #2701 fix)

- **Status:** ✅ DONE 2026-09-12T06:39:22Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** a6d6df6d9 (worktree), merged to main at 1261b1c11

## Log
- 2026-09-12T06:29:29Z ⏳ IN FLIGHT — Confirmed #2701's `platformTierWhere`/`isGenuinePlatformTier`
  OR-arm fix is live in `msp-plan-self-service.ts` and `msp-plan-management.ts`
  (extracted to `artifacts/api-server/src/lib/platform-tier.ts`). Confirmed via direct
  `psql` against local `DATABASE_URL` that all three real tiers (120/121/122) still have
  `annual_price_cents IS NULL` — the §7.2 gap is real and unchanged. Now correcting
  `docs/msp-console/msp-plan-self-service-contract-pack.md` §7/§0.1/§9 to reflect current
  fixed behavior, then doing a full line-by-line audit of the rest of the Billing surface
  contract packs per the #1642 pattern.
- 2026-09-12T06:39:22Z ✅ DONE — Corrected
  `docs/msp-console/msp-plan-self-service-contract-pack.md` (§0.1, §1.2, §1.3, §2, §3.2,
  §3.3, §5, §7, §7.2, §8, §9): §7's live bug is now described as fixed by #2701, re-verified
  against current code; §7.2's `annual_price_cents IS NULL` gap re-confirmed real and
  unchanged. §0.1 also corrected — `artifacts/msp-console` now exists (scaffolded by #1680
  after this pack closed) but these 4 routes remain unwired; filed as new finding **#3796**
  (sibling sub-issue of #1692, board status AI Batter Up).
  Full #1642-pattern line-by-line re-audit of `docs/portal/billing-contract-pack.md` (+ its
  `Design/portal/design_handoff_full_site` mirror, kept in sync) found two more real drifts,
  both corrected: (1) §4's documented decimal-dollar exception for `invoicesTable.amount`
  was fixed by **#1610** ~1.5h after that pack closed, old column dropped by #1623 —
  corrected §1.1/§1.3/§4, added the missing `BillingReceiptRow.paid` field. (2) §1.6's
  "zero `client_services` rows" premise was already inaccurate at pack-close (one
  synthetic/testbed row pre-dated it, confirmed via `psql`) — corrected in place, no
  change to the practical "nothing real to wire yet" conclusion.
  Read-only throughout — no product/schema/UI code changed. Committed `a6d6df6d9` in this
  worktree, merged into `origin/main` at `1261b1c11` (fast-forward-free merge via a
  temporary local branch, since another commit had landed on `main` between fetches).
  `git merge-base --is-ancestor` confirmed. `git status --porcelain` clean. Issue #3782
  commented with this summary; `in-flight`→`complete` label swap applied.
