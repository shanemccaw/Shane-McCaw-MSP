# #1536 — Microsoft Changes: date quality — actionRequiredByDateTime is often null

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 34895c696 (feature), pushed to origin/main as 34895c696 — verified an ancestor of main via `verify-branch-merged.mjs`

## Log
- 2026-08-29 ⏳ IN FLIGHT — Read #1536 + #1494/#1530-1533 comments and the code they
  landed, plus the contract pack's #1536 row. Queried the local corpus (1159 real
  Message Center posts): confirmed the real example from the issue body is real
  (`[Rollout Schedule]` sections with phrasing like "Rollout begins in mid-July 2026
  and is expected to complete by early August 2026"), and validated a prose-date
  extractor against all 567 posts carrying that section (0 unexplained misses after
  tuning). Building: an `advisory_date_text` column on `msp_message_center_items`
  (prose, rendered verbatim, never parsed into a Date, never bucket-driving), plus an
  honest `DATE_UNCLEAR` first-class bucket in `portal-message-center.ts` for a post
  that genuinely carries no structural date at all (`hasStructuralDate()` false).
- 2026-08-29 DONE — Shipped `34895c696`, merged to `origin/main`, verified via
  `verify-branch-merged.mjs`. Migration
  `lib/db/migrations/manual/2026-08-29-mc-advisory-date-text-1536.sql` run against local
  Postgres in this session (added `advisory_date_text` to `msp_message_center_items`);
  ran the one-off backfill against the already-synced 1159-row corpus (549 rows got a
  real advisory phrase). `placementForPost()` is now the single source of truth for
  bucket placement in `portal-message-center.ts`, returning the new `DATE_UNCLEAR`
  sentinel for a post with neither `actionRequiredByDateTime` nor `endDateTime`, instead
  of falling back to publish/edit timestamps. Wired onto the wire as
  `advisoryDateText`/`dateConfidence` on every post and a new `dateUnclearCount` /
  `dateUnclearPosts[]` pair. Tests: 61/61 passing across the new
  `m365-message-center-date-quality.test.ts` (9 cases, real post bodies) and 7 new
  cases in `portal-message-center.test.ts`; also sanity-ran the full pipeline against
  all 1159 real rows directly (1078 on-axis, 0 date-unclear today — every real post
  carries `endDateTime` — 549 with advisory text), then deleted the scratch test.
  `tsc --noEmit` clean for every file touched; the one other failure seen
  (`m365-roadmap-mc-link.test.ts`'s DB-backed backfill test, using `randomBytes` for a
  feature ID) is pre-existing flakiness in a file this session never touched — confirmed
  it fails on its own, unrelated to this change, and left alone. Contract pack's #1536
  row updated from OPEN to CURRENT with the built shape. `git status --porcelain` clean
  at completion. Shane To-Do: run the same migration against the Replit/staging DB
  (local Postgres only was run here) before advisory dates populate there.
