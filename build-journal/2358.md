# #2358 — Group header: state pill, count, "Dispatch all"

- **Status:** ⏳ IN FLIGHT 2026-09-02T20:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02

## Log
- 2026-09-02T20:20:00Z ⏳ IN FLIGHT — confirmed #2357 genuinely DONE on `main` (commit
  `b1c359738`, verified via its own bookend, `git log` shows it on this branch's history).
  Real audit: `RenderBatterUpLaneBody` (`MainWindow.BatterUpPanel.cs`) already groups items by
  real parent Feature and renders a header row with a chevron, the Feature number/title, and a
  count pill — but no real state pill for the group's parent Feature, and no "Dispatch all"
  affordance. `BatterUpItemRef`/the lane-count GraphQL query (`ChatGitHubFilter.cs`) currently
  only carry `FeatureNumber`/`FeatureTitle` off the `parent { number title }` edge — no state or
  labels, so there's no real data yet to build a state pill from.
  Plan: extend the SAME `parent` GraphQL sub-selection to `parent { number title state labels(first: 20) { nodes { name } } }`
  (still one query, no second round trip — the same discipline #2357 used for number/title), carry
  `FeatureIsClosed`/`FeatureLabels` on `BatterUpItemRef`, and reuse the exact state-pill vocabulary
  `GitNodeState` already established for Feature rows in the Git Panel tree (CLOSED/BLOCKED/IN
  FLIGHT/COMPLETE/OPEN, same brush keys) rather than inventing a new one. Add a "Dispatch all"
  action link next to the header, reusing the existing `GitEpicActionLink` link-button pattern
  (partial-class sibling in `MainWindow.GitEpicPanel.cs`) — click handler is a stub per this
  issue's own scope (real dispatch semantics are #2366's), sets an honest status-line message
  referencing #2360/#2366 rather than doing anything real. No state pill/Dispatch button on the
  "No Feature" bucket — there's no real parent Feature to show state for or dispatch against.
