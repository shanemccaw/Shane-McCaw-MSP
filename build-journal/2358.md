# #2358 — Group header: state pill, count, "Dispatch all"

- **Status:** ✅ DONE 2026-09-02T20:45:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02

## Log
- 2026-09-02T20:20:00Z ⏳ IN FLIGHT — confirmed #2357 genuinely DONE on `main` (commit
  `b1c359738`, verified via its own bookend, `git log` shows it on this branch's history).
  Real audit: `RenderBatterUpLaneBody` (`MainWindow.BatterUpPanel.cs`) already groups items by
  real parent Feature and renders a header row with a chevron, the Feature number/title, and a
  count pill — but no real state pill for the group's parent Feature, and no "Dispatch all"
  affordance. `BatterUpItemRef`/the lane-count GraphQL query (`ChatGitHubFilter.cs`) currently
  only carry `FeatureNumber`/`FeatureTitle` off the `parent { number title }` edge — no state or
  labels, so there's no real data yet to build a state pill from.
  Plan: extend the SAME `parent` GraphQL sub-selection to `parent { number title state labels(first: 20) { nodes { name } } }`
  (still one query, no second round trip — the same discipline #2357 used for number/title), carry
  `FeatureIsClosed`/`FeatureLabels` on `BatterUpItemRef`, and reuse the exact state-pill vocabulary
  `GitNodeState` already established for Feature rows in the Git Panel tree (CLOSED/BLOCKED/IN
  FLIGHT/COMPLETE/OPEN, same brush keys) rather than inventing a new one. Add a "Dispatch all"
  action link next to the header, reusing the existing `GitEpicActionLink` link-button pattern
  (partial-class sibling in `MainWindow.GitEpicPanel.cs`) — click handler is a stub per this
  issue's own scope (real dispatch semantics are #2366's), sets an honest status-line message
  referencing #2360/#2366 rather than doing anything real. No state pill/Dispatch button on the
  "No Feature" bucket — there's no real parent Feature to show state for or dispatch against.
- 2026-09-02T20:45:00Z ✅ DONE — commit `3d39f6aeb` (pushed to `main`, verified via
  `verify-branch-merged.mjs`, working tree clean). Shipped:
  - `ChatGitHubFilter.cs`: `GetBatterUpLaneCountsAsync`'s existing board-walk GraphQL query's
    `parent` sub-selection extended to `parent { number title state labels(first: 20) { nodes { name } } }`
    — one extra field set on the SAME query, no second round trip. `BatterUpItemRef` carries the
    new `FeatureIsClosed`/`FeatureLabels`. New `LaneLabelConnection`/`LaneLabelNode` response-shape
    classes for the labels edge.
  - `MainWindow.BatterUpPanel.cs`: `RenderBatterUpLaneBody`'s group header row now renders a real
    state pill (`BatterUpFeatureState` — same CLOSED/BLOCKED/IN FLIGHT/COMPLETE/OPEN vocabulary
    and brush keys `GitNodeState` already established for Feature rows in the Git Panel tree, not
    a new one), the existing real count pill, and a "Dispatch all" action link (reusing the
    existing `GitEpicActionLink` link-button pattern from `MainWindow.GitEpicPanel.cs`, a partial-
    class sibling). Per this issue's explicit scope, the click handler
    (`BatterUpDispatchAllClicked`) is a stub — it does nothing destructive, just sets an honest
    "not wired yet — real dispatch semantics land in #2366" status line. Neither the state pill
    nor the Dispatch button render on the "No Feature" bucket (no real parent Feature to show
    state for or dispatch against).
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` clean (0 warnings/0 errors),
    both before and after two required rebases onto concurrent `main` landings (#2288, #2360's
    bookend) — neither touched the same file, so both rebases were clean fast-forwards of my one
    commit. Independently re-ran the extended GraphQL query shape live via `gh api graphql`
    against the real board — confirmed real parent Feature `state`/`labels` come back correctly
    (e.g. #2473, #2469, #2400, #2355 all resolved with `state: OPEN` and real (empty-today)
    labels arrays) — matching exactly what the shipped code now reads. Did not launch the WPF app
    itself — no headless runner exists for ShaneBuilder yet (same gap #2356/#2357 already noted);
    build + live-query cross-check is the real verification done.
  - **Scope confirmation:** stayed inside header state pill + count + Dispatch-all-affordance-only,
    per this issue's own scope line — no real dispatch wiring (left to #2366), no item-level
    changes (#2359/#2360's scope), no grouping/persistence changes (#2357's, already done).
  - No findings filed this session — no genuine bug/gap surfaced outside stated scope.
