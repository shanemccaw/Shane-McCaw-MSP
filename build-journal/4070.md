# #4070 — Windows file-association launches never reach OpenFileTab

- **Status:** ✅ DONE 2026-09-14T15:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 196677910 (rebased to d1bb0986 on main)

## Log
- 2026-09-14T15:03:00Z ⏳ IN FLIGHT — read issue #4070 + comment, starting with App.xaml.cs OnStartup / single-instance guard (#2141) investigation before editing.
- 2026-09-14T15:20:00Z ✅ DONE — App.OnStartup: a single bare file-path argument that isn't a known launch flag (--agent/--dev/--instance[=name], checked via a new IsKnownLaunchFlag helper) and points at a real existing file now synthesizes shaneapp://openfile?ref=<fullPath>, assigned to the same protocolUri local used by the pre-existing forward/single-instance-guard-carve-out/PendingProtocolUri machinery — none of that logic was touched or duplicated. ShaneAppProtocol.CanonicalizeAction: added openfile -> openFile. MainWindow.HandleShaneAppUriAsync: added an openFile case (File.Exists guard, then OpenFileTab(path), then ActivateMainWindowFromProtocol() to foreground, reusing the existing 'activate' foregrounding helper) right after the 'activate' case. Confirmed StartShaneAppProtocolListener's PendingProtocolUri drain already calls the same HandleShaneAppUriAsync dispatch — no second code path needed for cold start. `dotnet build desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 warnings, 0 errors. Verified by reading code (NEVER launch exe rule #2156): traced all 4 real scenarios (already-running forward, cold-start drain, plain no-arg launch, --dev/--agent/--instance launches) through the new branch — no scenario mis-triggers. git status --porcelain clean after commit/push. verify-branch-merged.mjs: OK, d1bb0986 is an ancestor of origin/main.
