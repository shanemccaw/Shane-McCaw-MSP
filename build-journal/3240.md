# #3240 — Money v3 bill sheet: funding-history sparkline drag-to-scrub interaction

- **Status:** ✅ DONE 2026-09-08T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 5c6baf53067a0eb1ac7fe821c74a0f4e87fbe1cf

## Log
- 2026-09-08T17:55:54Z ⏳ IN FLIGHT — adding pointer/touch drag-to-scrub interaction over the
  bill detail sheet's funding-history sparkline SVG (`billSparklineHtml` in
  `web/shanes-life/public/app.js`), deferred from #3212 per its own docblock note.
- 2026-09-08T18:10:00Z ✅ DONE — `billSparklineHtml` (string-based) replaced with
  `billSparklineNode` (real DOM/SVG via `createElementNS`, same SVG_NS pattern already used
  by `lineIcon`), taking an `onScrub(point|null)` callback. Wired real `pointerdown` /
  `pointermove` / `pointerup` / `pointercancel` handlers (pointer capture) that find the
  nearest real data point to the pointer x-position, show a dashed vertical marker + ringed
  dot at that point (design 1e), and drive a callout label above the chart reading
  `{date} · ${balance} of ${target}` (or just `{date} · ${balance}` when the bill has no
  target) -- same shape as the design's own "Aug 21 · $520 of $612". Added the design's
  "drag to scrub" caption between the first/last cycle date labels, shown only when there's
  more than one real point to scrub across. No fixture data -- everything scrubbed is a real
  point out of `d.sparkline` (`GET /api/money/bills/:id`).
  Verified: `node --check public/app.js` passes; `DATABASE_URL=...finances node bin/check.mjs`
  confirms the server boots and reads/writes the real shared Postgres correctly (the
  script's own unrelated WebAuthn/rate-limit failures pre-exist this change and are outside
  its scope -- this is client-only `app.js`, which `check.mjs` explicitly doesn't cover:
  "Not covered here: the browser half of WebAuthn"). No test manifest -- `web/shanes-life`
  is outside the manifest-mandatory scope (`artifacts/msp-portal/`,
  `artifacts/shane-mccaw-consulting/` only, per CLAUDE.md). No findings filed -- nothing
  discovered outside this issue's own real scope. `git status --porcelain` clean.
