# Build Journal — Issue #3562

- **Status:** ✅ DONE 2026-09-11T21:10:00Z
- **Scope:** platform

## Log
- 2026-09-11T00:35:00Z — Session started. Audited local dev DB (53 wf_definitions /
  54 wf_versions) for any `create_kanban_task` node — zero found across all node
  types present. Proceeding with removal of the "marketing" board option from
  `CreateKanbanTaskPanel`/`MARKETING_COLUMNS` in `WorkflowBuilderPage.tsx` and the
  `boardId === "marketing"` branch in `workflow-executor.ts`'s `create_kanban_task`
  case, per #3562.
- 2026-09-11T21:10:00Z — ✅ DONE — Removed the "marketing" board option from
  `CreateKanbanTaskPanel`/`MARKETING_COLUMNS` (`WorkflowBuilderPage.tsx`) and the
  `boardId === "marketing"` branch + now-unused `marketingTasksTable` import from
  `workflow-executor.ts`'s `create_kanban_task` case (live executor + dry-run
  preview). Updated the matching sample payload (`workflow-node-default-samples.ts`)
  and node-registry description (`admin-workflows.ts`), and updated
  `workflow-executor-comms.test.ts`'s marketing-board test to exercise the
  project-board path instead. `npx tsc --noEmit` clean in both
  `artifacts/api-server` and `artifacts/admin-panel`; `workflow-executor-comms.test.ts`
  73/73 passing. Committed `df56ab622` on `agent/3562-q2261`, rebased onto
  `origin/main`, pushed as `901275418` — `verify-branch-merged.mjs` confirms merged.
  Working tree clean. No findings filed — the audit found zero live usage, so this
  was a straight removal, not a stop-and-file case.
