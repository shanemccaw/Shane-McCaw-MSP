# Build Journal — Issue #3562

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:35:00Z
- **Scope:** platform

## Log
- 2026-09-11T00:35:00Z — Session started. Audited local dev DB (53 wf_definitions /
  54 wf_versions) for any `create_kanban_task` node — zero found across all node
  types present. Proceeding with removal of the "marketing" board option from
  `CreateKanbanTaskPanel`/`MARKETING_COLUMNS` in `WorkflowBuilderPage.tsx` and the
  `boardId === "marketing"` branch in `workflow-executor.ts`'s `create_kanban_task`
  case, per #3562.
