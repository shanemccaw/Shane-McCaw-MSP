# Build Journal — #2447

- **Status:** ✅ DONE 2026-09-03T03:16:58Z
- **Scope:** platform
- **Issue:** #2447 — Team Management and Invitations: generate the contract pack from the real backend
- **Part of:** #1656 (Feature: Team Management and Invitations (Portal)) → #1485 (EPIC: Portal)

## Log

- 2026-09-03T03:12:55Z — Session start. Confirmed `artifacts/api-server/src/routes/portal-team.ts`
  is real and live, mounted at `/api` (index.ts:333, `router.use(portalTeamRouter)`) — 10 real
  routes, not the 3 named in #2447's Step 1 (those 3 were the old portal-v2 page's approximate
  naming; the real surface is more granular: `/portal/team`, `/portal/team/invite`,
  `/portal/team/:userId/{sessions,status,mfa-enforcement,unlock,reset-password,temp-password,
  reset-mfa,emergency-bypass}`). Confirmed orphaned — no caller in `artifacts/portal/src`,
  `artifacts/msp-website`, or `artifacts/shane-mccaw-consulting`; no `Design/portal/` team export
  yet. Writing `docs/team-management-and-invitations-contract-pack.md` per the #1642 pattern.
- 2026-09-03T03:16:58Z — ✅ DONE — `docs/team-management-and-invitations-contract-pack.md`
  written and pushed to `main` at `42701a78b` (rebased `agent/2447-q1330` onto `origin/main`
  then pushed `agent/2447-q1330:main`, since this worktree's `main` branch is checked out
  elsewhere). Verified via `verify-branch-merged.mjs`: "OK — merged into origin/main." Working
  tree clean (`git status --porcelain` empty). Documented all 10 real routes on
  `portal-team.ts` (the 3 named in #2447's Step 1 were the old page's approximate naming; the
  real surface is more granular). Confirmed orphaned — no live frontend caller, no
  `Design/portal/` export yet. Three findings noted in the pack's own §5 (derived `lastLoginAt`
  vs. the unused `users.last_login_at` column; the one mutating route with no audit-log call;
  invite always lands at flat `CustomerUser` with no manage-team grant option) — all judged
  below the filing bar (documented for Design/implementation awareness, consistent with the
  precedent the Documents pack (#2449) set for its own similarly low-severity observations), so
  no new issue was filed.
