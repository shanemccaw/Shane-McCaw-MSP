# #3814 — Authentication: wire the real MSP Console sign-in/MFA/reset flow (screen 38)

- **Status:** ✅ DONE 2026-09-13T01:51:44Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 607271753

## Log
- 2026-09-12T19:35:00Z ⏳ IN FLIGHT — Investigated Design/MSP_Console/design_handoff_msp_console/Authentication.dc.html
  (screen 38) plus real auth.ts/mfa.ts routes. Confirmed `artifacts/msp-console` already has a real app shell
  (AuthGate/AuthContext/ConsoleShell) but no `/login`, `/mfa`, `/forgot-password`, `/account/mfa` or
  `/account/sessions` routes — SignInRequired/logout both already redirect to `/login`, which doesn't exist yet.
  Confirmed screen 39 (Account Security, already built) owns admin-reset-other's-MFA, so screen 38's own
  "OPERATOR SIDE" block is out of scope here — self-service only. Building the standalone route tree next:
  sign in, two-factor challenge, forgot/reset password, self-service change-MFA, sign-out/sessions.
- 2026-09-13T01:51:44Z ✅ DONE — commit 607271753. Built the real standalone route tree under
  `artifacts/msp-console/src/auth/`: `/login` (sign-in with distinct 400 / generic-401 /
  "no password set"-401 / 423-lockout states), `/mfa` (totp/sms/passkey/bypass challenge), `/forgot-password`
  (request form, plus the `?token=` reset landing page on the same route), `/account/mfa` (self-service
  TOTP/SMS/passkey enrollment, admin passkey-only gate respected — admin-reset-of-someone-else's-MFA
  correctly left to screen 39's `AccountSecurity.tsx`, not duplicated), `/account/sessions` (active
  sessions + login history + revoke-others/one + sign-out). Extended `AuthContext` with `setSession()`;
  added a real "Security & sessions" nav entry in the shell's Header/ConsoleShell so `/account/mfa` isn't
  a dead end; added a `returnTo` handoff from `AuthGate`'s session-expired panel back through `/login`.
  Added `@simplewebauthn/browser` as a real msp-console dependency (already present in the shared pnpm
  store via admin-panel — no network install run). Design's role/MFA-enforcement toggle pills and the
  "OPERATOR SIDE" admin-reset block were review-only and were not shipped. No backend/DB changes — every
  route wired already existed in `auth.ts`/`mfa.ts`, verified by reading the real handlers, not guessed.
  Verification: `tsc -p tsconfig.json --noEmit` clean, `vite build` clean (985KB main chunk warning is
  pre-existing, unrelated), existing msp-console test suite 16/16 still passing. Test-manifest requirement
  does not apply — CLAUDE.md scopes that mandate to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`
  only; msp-console is neither. Filed #3910 (parented under #3814, board status AI Batter Up, milestone
  v1.1): `forgot-password`'s reset email always links to the customer portal, never the console, for any
  account type — a real cross-cutting gap this issue doesn't own. Branch verified merged into origin/main
  (`verify-branch-merged.mjs`); working tree clean.
