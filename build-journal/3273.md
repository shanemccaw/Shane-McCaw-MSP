# #3273 — Feature: Money nav restructure -- five-cell segmented control, Bankruptcy tab, Banks moves to Settings (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T23:03:14Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 661475f95 (rebased to 33ddc2fab on push, real diff unchanged)

## Log
- 2026-09-08T22:47:59Z ⏳ IN FLIGHT — read `web/shanes-life/Design/design_handoff_shanes_life/README.md`
  "Start here" + Money/Settings sections, confirmed current code state (`MONEY_TABS` has 8 tabs,
  `viewMoneyBanks`/`viewMoneyVault`/`viewMoneyDocuments` still live per #3272's own commit message
  which explicitly leaves that removal to this issue). Plan: reduce `MONEY_TABS` to the real 5
  (Now/Bills/Accounts/Bankruptcy/Cars), delete the now-superseded Vault/Documents money tabs
  (Vault room already replaces them per #3272), move the Banks card into Settings (flat section --
  the House/You/Connected/Activity tab structure itself is #3274's separate scope, not this one),
  move the extension-reveal completion wiring from the old `viewMoneyVault` into the new `viewVault`
  room + repoint `extension/background.js`'s popup URL from `#/money` to `#/vault`, and add the real
  `banksNeedReconnect` signal to `/api/today` so the attic vent's amber arc goes solid when a bank
  needs reconnecting.
- 2026-09-08T23:03:14Z ✅ DONE — Shipped exactly that plan, plus one real regression caught and
  fixed during my own diff review: deleting `viewMoneyDocuments` also deleted `documentRow`/
  `hideDocumentReveal`/`documentReveal`, which `viewVault` (the standalone Vault room, #3272) still
  depends on -- restored them, and added a new `documentAddCard()` (ported from the deleted tab's
  own add-document form) since the Vault room otherwise had no way left to add a document at all
  once Money's own Documents tab was gone.
  Real verification: `node --check` on every touched file; a rolled-back local-DB transaction
  (`BEGIN`/`UPDATE plaid_items ... health_status='login_required'`/`ROLLBACK`) confirmed
  `banksNeedReconnect` reacts correctly to a real health-status change with zero lasting DB
  change; a full `bin/check.mjs` run against an isolated local instance (junctioned
  `node_modules` from the main checkout -- no `pnpm install`/`npm install`, zero download cost;
  own `.env` copy, port 5099 to avoid clobbering a concurrent session's own dev server already
  on :5000) passed 216/221 -- the 5 failures are pre-existing gaps in that ad-hoc instance's own
  `.env` (`SL_VAULT_KEY` unset, `PUBLIC_ORIGIN` left at :5000 while I ran on :5099), unrelated to
  this diff; a scratch assertion added to `bin/check.mjs` (run once, then fully reverted --
  `git diff` on that file confirmed zero trace before the real commit) cross-checked
  `/api/today`'s real `banksNeedReconnect` against a live SQL recomputation and passed
  (`api 0 vs sql 0`). Cleaned up: killed the isolated verification server, removed the scratch
  `.env` and the `node_modules` junction from this worktree; `git status --porcelain` is clean.
  Rebased onto `origin/main` (picked up #3276's unrelated migration-063 work, no file overlap,
  clean rebase) and pushed; `verify-branch-merged.mjs` confirms `agent/3273-q2000` is an ancestor
  of `origin/main` at 33ddc2fab.
  No findings filed -- the one candidate (local schema drift for migration 063's columns,
  surfaced in `bin/check.mjs`'s own boot log) was explicitly self-diagnosed by the app's own
  tooling as a known transient state from #3276's concurrent, not-yet-merged migration, and
  resolved itself once I rebased onto main after #3276 landed.
