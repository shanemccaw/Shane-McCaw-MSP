# #3069 — Feature: multi-instance BuildConsole — repo identity in Settings + --instance launch parameter

- **Status:** ✅ DONE 2026-09-06T22:44:36Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 1c841254d (bookend IN FLIGHT), bdfff6c44 (real implementation)

## Log
- 2026-09-06T22:41:04Z ⏳ IN FLIGHT — implementing the issue's real 2-point plan: move
  `GitHubApiClient.Owner`/`Repo`/`BatterUpProjectId` and `GitHubIssuesService.Repo` from
  hardcoded consts into real `BuildConsoleSettings` fields (defaulting to today's MSP
  values) with a Settings UI section to view/edit them, and add a real `--instance <name>`
  launch parameter (new `Services/InstanceMode.cs`) that suffixes `App.SingleInstanceMutexName`
  and the `%AppData%\BuildConsole\` settings folder so a second instance can run concurrently
  pointed at a different real repo. DB isolation already free via per-checkout `.env.local` —
  no DB work needed, confirmed by the issue's own audit.
- 2026-09-06T22:44:36Z ✅ DONE — shipped in bdfff6c44:
  - New `Services/InstanceMode.cs`: parses `--instance <name>`/`--instance=<name>` once in
    `App.OnStartup`, before the mutex is claimed or any `BuildConsoleSettings` I/O.
  - `App.xaml.cs`: added `EffectiveSingleInstanceMutexName` (suffixes the real
    `SingleInstanceMutexName` with `_<name>` when `--instance` is set); the mutex ctor now
    claims that instead of the bare const. No `--instance` → identical mutex name as before.
  - `BuildConsoleSettings.cs`: `SettingsDir` now resolves to
    `%AppData%\BuildConsole-<name>\` when an instance name is set, else the original
    `%AppData%\BuildConsole\` — byte-for-byte unchanged default. Added real fields
    `GitHubOwner` ("shanemccaw"), `GitHubRepoName` ("Shane-McCaw-MSP"), `GitHubOwnerRepo`
    (computed "owner/repo"), and `BatterUpProjectId` ("PVT_kwHOEiBDdc4BeoiY") — all
    defaulting to today's real MSP values.
  - `GitHubApiClient.cs`: `Owner`/`Repo`/`BatterUpProjectId` are now static properties
    reading `BuildConsoleSettings.Load()` instead of hardcoded `const string`s. Every
    existing interpolated-string call site compiles unchanged.
  - `GitHubIssuesService.cs`: same treatment for its own `Repo` const →
    `BuildConsoleSettings.Load().GitHubOwnerRepo`.
  - `Controls/SettingsTabView.xaml` / `.xaml.cs`: new "GitHub Repo Identity
    (Multi-Instance)" section on the General & API settings page — owner/repo/board-id
    text boxes, a Save button, and a live "which instance is this" indicator line.
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0
    warnings, 0 errors. No DB work needed (per the issue's own confirmed audit — DB
    isolation already comes free via `BuildQueuePostgresClient`'s per-checkout
    `.env.local` resolution). Not live-tested with two real concurrent instances
    side-by-side in this session (no second real repo checkout available here to point
    `--instance` at) — the mutex/settings-path logic itself is straightforward and
    covered by the build succeeding; a genuine two-instance side-by-side run is Shane's
    to try whenever he has a second real repo to aim it at.
  - **Findings filed:** #3070 (parented under #1789, `bug`, board status "AI Batter
    Up") — a few further hardcoded `github.com/shanemccaw/Shane-McCaw-MSP` references
    (`ChatDocumentContainer.xaml.cs`'s epic-click, the default Web Tools "Git" entry)
    that this issue's stated 2-point plan didn't include but that a real second
    instance would still see pointing at this repo.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` (exit 0) before writing this DONE.
