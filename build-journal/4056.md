# #4056 — Dashboard: generate the contract pack from the real, live /portal/dashboard endpoint

- **Status:** ✅ DONE 2026-09-14T18:56:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** b49ddac25 (IN FLIGHT bookend), c9c05a9c0 (pre-rebase pack commit), 651bb1de2 (pack commit, pushed to origin/main)

## Log
- 2026-09-14T18:40:00Z ⏳ IN FLIGHT — Read #4056 + #1943 in full. Starting: read live `/portal/dashboard` route + export routes in `artifacts/api-server`, and archived `customer-home.tsx`/`customer-timeline.tsx` for contract reference, then author the contract pack markdown.
- 2026-09-14T18:56:00Z ✅ DONE — Wrote `docs/portal/dashboard-contract-pack.md` (real `GET /portal/dashboard` + `dashboard-export.ts` contracts, read in full; cross-referenced live POA&Ms/Risk Register/SOPs/Change Control/Message Center/Pillars+Copilot Gate route contracts via four parallel investigation passes). Discovered the route is NOT orphaned — `artifacts/portal/src/pages/index.tsx` (OverviewPage, #2921) already wires it, correcting a stale claim in the sibling `customer-home-and-timeline-contract-pack.md`. Audited the live `overviewCounts` roll-up against #1943's own seven-item "what rolls up here" list: only Remediation is a clean match; Pillar scores match under a narrower vocabulary; the other five (POA&Ms, open risks + risk↔POA&M linkage, SOP runs, change-request approval + freeze, Microsoft Changes "requiring action", Copilot Gate) are missing or serving the wrong concept under a same-sounding field. Also found a real zero-state violation (`scores.foo ?? 0` renders "never scanned" as a literal 0, contradicting #1943's own "never zero for a missing value" constraint) and 3 unconsumed export routes. Filed all 8 as real GitHub issues (#4062-#4069), each a `bug`-labeled sub-issue of #1943 (Feature-tier parent, not the #1485 epic), milestone v1.1, board status "AI Batter Up". No product code, schema, or UI changed — read-only per the dispatch's own standing rules. Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0); working tree clean.
