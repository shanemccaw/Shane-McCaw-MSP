# #1395 — server restart after worktree merge doesn't pull new code (stale fixtures)

- **Status:** ✅ DONE
- **Scope:** platform (dev-server coordinator tooling)
- **Started:** 2026-08-27
- **Commit(s):** `475eaf037` (merge-back self-heal), `c1c2a284e` (main-checkout refresh)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Regression from #1371's merge-back. Root cause traced live:
  the dedicated dev-server checkout `C:\dev-server` carries uncommitted local edits to
  `package.json` + `pnpm-lock.yaml` (Windows-native binaries: `@esbuild/win32-x64`,
  `@rollup/rollup-win32-x64-msvc`, `@tailwindcss/oxide-win32-x64-msvc`,
  `lightningcss-win32-x64-msvc`, `@grpc/*`). `mergeNoEdit`'s plain `git merge` refuses to
  touch a locally-modified tracked file and ABORTS the whole merge-back
  ("Your local changes to the following files would be overwritten by merge"), so
  `mergedCount:0`, `changed:false`, restart skipped (ungrouped) or fired on an
  un-advanced HEAD (build-set) — server frozen at `ed518c9bc` (276 commits behind main),
  serving stale code while appearing to restart. Confirmed in `.logs/dev-server/cycles.log`
  (last two ungrouped cycles both `conflict` on `pnpm-lock.yaml`). Fix: `mergeNoEdit`
  self-heals — on the "would be overwritten" abort, restore exactly the named tracked
  files to HEAD (node_modules is untracked/real, installed binaries untouched) and retry.
- 2026-08-27 ✅ DONE — Two layers fixed.
  **(1) merge-back self-heal (`475eaf037`, `scripts/dev-server/git.mjs`):** `mergeNoEdit`
  now restores the exact tracked files git names in a "would be overwritten by merge"
  abort and retries (bounded 3x). Live-proven against the real stuck `C:\dev-server`:
  it advanced `ed518c9bc`→current main, `autoRestored:["pnpm-lock.yaml"]`. The `Portal`
  build-set manifest is the smoking gun — real builds #1387/#1390/#1391/#1392/#1393 all
  `conflict` on `pnpm-lock.yaml`; my fixed run is the only `merged` one.
  **(2) main-checkout refresh (`c1c2a284e`, new `scripts/dev-server/refresh-main-server.mjs`):**
  found a deeper disconnect — the dev servers Shane verifies against (:8080/:5175) are
  launched by BuildConsole's `DevServicesManager` from the **MAIN checkout**, not the
  `C:\dev-server` the merge-back targets (proven: running `/api/version` reported main's
  HEAD while `C:\dev-server` sat 276 behind). Per Shane's call, the coordinator's restart
  action is now a SAFE ff-only pull of the main checkout + rebuild of the built api-server
  (vite fronts HMR themselves). Never resets/discards a shared checkout. Rides the reaper's
  existing `request-restart` call — no BuildConsole rebuild. Dry-run verified; selftest
  green except the pre-existing scenario-9 `index.lock` flake (my change inert on that path).
  **Shane To-Do:** run `node scripts/dev-server/refresh-main-server.mjs` (or restart API
  Server in BuildConsole) once to pull the running server up to current main now; otherwise
  it self-corrects on the next real build's merge-back. Also note `C:\dev-server` is now
  unblocked and current.
