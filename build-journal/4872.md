# #4872 — get_changes_since delta endpoint (Shane's Life)

- **Status:** ✅ DONE 2026-09-19T17:21:29Z
- **Scope:** platform (code in shanemccaw/shanes-life)
- **Started:** 2026-09-19
- **Commit(s):** shanes-life cf8c65b, 65d5109

## Changes Made
- Migration (renumbered to 081 after a 077 collision): https://github.com/shanemccaw/shanes-life/blob/65d5109/migrations/081_updated_at_delta_sync.sql — set_updated_at() BEFORE UPDATE trigger on entities, lists, list_items, dates, medications, pets, tasks, financial_events; adds updated_at (backfilled) to list_items, tasks, financial_events; range indexes.
- Core: https://github.com/shanemccaw/shanes-life/blob/65d5109/src/core/changes.mjs — getChangesSince, per-room cap 200 with `truncated`, explicit `covered`/`notCovered`/`caveats`, `nextSince` cursor.
- Tool: get_changes_since in https://github.com/shanemccaw/shanes-life/blob/65d5109/src/mcp/tools.mjs
- Commits: https://github.com/shanemccaw/shanes-life/commit/cf8c65b , https://github.com/shanemccaw/shanes-life/commit/65d5109
- Phasing: covered = the rooms named in #4872 step 1 (all 8 tables). Other rooms are listed in notCovered; adding them is a later phase. Deletions are not reported (stated in response caveats).
- Finding filed: #4885 (under #3220) — shanes-life src/migrate.mjs + bin/* import ../../../scripts/*, absent in standalone repo.

## Verification Results
- Live, against a scratch database (template copy of local `finances`, dropped afterwards), migrations 075/076/077(now 081) applied, real core functions: created a task, a list item (plus its list) and a pet after a timestamp; get_changes_since returned exactly those (tasks 1, pets 1, list_items 1, lists 1 — the parent list is a real fourth row) and 0 for entities/dates/medications/financial_events. Raw SQL UPDATE with no app-level stamp bumped updated_at via the trigger; nextSince round-trip empty; invalid timestamp rejected. All 10 checks PASS.
- NOT verified: the MCP tool through the HTTP/protocol layer — src/mcp/tools.mjs imports sharp, not installed in the fresh clone (no download made); only `node --check` + handler is a one-line call to the tested core function. Migration not applied to the real local `finances` DB or Replit; the boot-time runner applies it (note 075/076 are also pending on local finances). But src/migrate.mjs cannot currently load (#4885).

## Log
- 2026-09-19T17:21:29Z ✅ DONE — shipped as above; honest gaps listed under Verification Results.
