# #2773 — Tree rollup pill "0 sub" fix (self-rollup vs cross-epic aggregation)

- **Status:** ✅ DONE 2026-09-04T00:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 301a06e96 (bookend), a10813c53 (fix, rebased to ffa72139b on main)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — read #2773 body + Shane's scope-clarification comment. Real fix: `GitBoardIssueFilters.IsUnderInternalToolingEpic`/`CountsAsRealWork`/`ComputeTransitiveLeafRollup` get an optional `selfRootEpicNumber` so a self-rooted rollup (root IS #1202/#1095) doesn't exclude its own descendants, while cross-epic milestone-bar/Home-dashboard aggregation (unchanged call sites) still excludes them. Per Shane's clarification comment, the Git Board tree's Epic PILL NUMBER itself is redefined separately: count of the Epic's direct Feature-titled children NOT themselves 100% complete (new `ComputeOpenFeatureCount`), not a raw leaf-issue count — while `ResolveRollup`'s (Total, Completed, Percent) used by EpicProgress/GATE fraction/progress bars stays the real transitive leaf rollup (fixed via the same `selfRootEpicNumber` threading).
- 2026-09-04T00:40:00Z ✅ DONE — implemented in `Services/GitBoardIssueFilters.cs` (`IsUnderInternalToolingEpic`, `CountsAsRealWork`, `ComputeTransitiveLeafRollup` all take an optional `selfRootEpicNumber` — null preserves the original cross-epic-aggregation behavior at every existing milestone-bar/Home-dashboard call site, unchanged; new `ComputeOpenFeatureCount(epicRoot, allIssues)` computes the Epic pill's own real definition: count of direct real Feature-titled children whose own transitive leaf rollup is NOT 100% complete) and `Controls/LeftSidebar.xaml.cs` (`ResolveRollup` now threads `it.Number` as `selfRootEpicNumber` — fixes #1202/#1095's own progress-bar %, EpicProgress, GATE fraction; new `ResolveEpicPillCount` backs the "(N sub)" tree-pill text specifically at both title-formatting call sites, using `ComputeOpenFeatureCount` for Epic nodes). `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. No BuildConsole.exe launch (per standing rule — mutex could courier into Shane's live session); verified against real live GitHub data instead: `gh api graphql` confirms #1202 has 33 real direct Feature-titled children (31 currently OPEN issue-state, 2 CLOSED) — consistent with the fix's Feature-centric definition replacing the old raw-68/raw-35 leaf counts. Judgement call from Shane's clarification comment (documented in code): the pill counts open Features only — #1202's 35 direct non-Feature leaf issues are NOT reflected in this specific pill number. Rebased `agent/2773-q1478` onto `origin/main` (clean, no conflicts) and pushed directly to `main` at `ffa72139b`. `verify-branch-merged.mjs` — OK, merged. `git status --porcelain` — clean. No findings filed this session (no new gap discovered beyond the issue's own scope).
