# #2773 — Tree rollup pill "0 sub" fix (self-rollup vs cross-epic aggregation)

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — read #2773 body + Shane's scope-clarification comment. Real fix: `GitBoardIssueFilters.IsUnderInternalToolingEpic`/`CountsAsRealWork`/`ComputeTransitiveLeafRollup` get an optional `selfRootEpicNumber` so a self-rooted rollup (root IS #1202/#1095) doesn't exclude its own descendants, while cross-epic milestone-bar/Home-dashboard aggregation (unchanged call sites) still excludes them. Per Shane's clarification comment, the Git Board tree's Epic PILL NUMBER itself is redefined separately: count of the Epic's direct Feature-titled children NOT themselves 100% complete (new `ComputeOpenFeatureCount`), not a raw leaf-issue count — while `ResolveRollup`'s (Total, Completed, Percent) used by EpicProgress/GATE fraction/progress bars stays the real transitive leaf rollup (fixed via the same `selfRootEpicNumber` threading).
