# #2353 — Import: multi-select tick boxes with "Merge N up"

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Feature: Test Pad, #2326)
- **Started:** 2026-09-02
- **Commit(s):** 95af4a0d6 (rebased/pushed onto main; original worktree commits 0c75c7250 / 92aed3238)

## Log
- 2026-09-02T19:41:51Z ⏳ IN FLIGHT — building the multi-select merge-up UI for TestPadImportWindow (Feature: Test Pad, #2326). Per-row merge-up (#2352) and "+N merged" split-back-out/undo (#2354) are separate open sub-issues not yet built; this issue needs the same underlying merge mechanism, so a minimal, honest version of it (TestPadImportMerger + a "+N merged" trace badge, no split/undo) ships alongside the multi-select checkboxes rather than leaving the merge invisible.
- 2026-09-02T19:58:00Z ✅ DONE — added `TestPadImportCandidate.Selected`/`IsMergedAway`/`MergedChildren`, `TestPadImportMerger.MergeSelectedUp` (folds every ticked row up into whatever unselected row precedes the topmost selection), a new per-row "select for merge" tick box distinct from the existing Include checkbox, a "Merge N up" bar above the preview list that shows live selected count and is disabled when the selection starts at row 0, and a "+N merged" badge on the anchor row. Header stats/Import now read the live (non-merged-away) candidate set. `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors. Rebased onto origin/main and pushed directly (`git push origin HEAD:main`) — verified merged via `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean.
