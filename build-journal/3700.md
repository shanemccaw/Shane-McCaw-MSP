# #3700 — Build Queue: "Ask Shane" items have zero real surfacing

- **Status:** ✅ DONE 2026-09-11T21:19:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 5ced77026 (real work), 562133498 (post-rebase on main)

## Log
- 2026-09-11T21:13:20Z ⏳ IN FLIGHT — implementing the 4-part real fix per the issue body:
  (1) persistent title-bar counter for real, open "Ask Shane" board-status issues, absent
  entirely at zero; (2) immediate persistent + click-through toast the moment an issue
  transitions into the status (reusing ToastEngine.ShowPersistent, the #3518/#1636 pattern);
  (3) a "❓ NEEDS YOUR INPUT" card label on any Build Queue card whose issue is in that set,
  never gated on the card's own raw status (mirrors #3626's no-gating for 🔒 BLOCKED); (4)
  click-through navigation reusing #3599's RevealQueueItem, falling back to opening the real
  GitHub issue when there's no live queue row. Build verified clean before flipping to DONE.
- 2026-09-11T21:19:00Z ✅ DONE — all 4 asks implemented and pushed to main (562133498):
  GitHubApiClient.GetAskShaneIssuesAsync(), Services/AskShaneMonitorService.cs (mirror-first
  read), MainWindow.AskShane.cs (poll off GitHubIssueMirror.SyncCompleted, persistent
  click-through toast on real transition, title-bar badge, feeds card labeling),
  BuildQueuePanel IsAskingShane/ApplyAskShaneSet/pill (no-gating, mirrors #3626), and
  BtnAskShane in MainWindow.xaml. Verified: `dotnet build BuildConsole.csproj` — 0 errors
  (this repo's desktop/BuildConsole is explicitly out of scope for test manifests per
  CLAUDE.md, so a clean build is the real verification here). Branch confirmed merged into
  origin/main via verify-branch-merged.mjs; working tree clean.
