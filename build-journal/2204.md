# #2204 — Settings, redesigned (ShaneBuilder Step 15)

- **Status:** ⏳ IN FLIGHT 2026-09-02T15:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — the freeze applies to desktop/BuildConsole only; #2198 confirms all new console feature work targets ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — Real audit done: ShaneBuilder has ZERO existing Settings surface (no `SettingsTabView`, no "Needs Value" text, `BtnRailSettings` gear has no click handler, no wrench-menu/palette entry). The "31 identical cards" state referenced in the issue body lives only in the frozen legacy `desktop/BuildConsole/MainWindow.SettingsTab.cs` (a different, frozen app) — this is a greenfield build against `desktop/ShaneBuilder/wpf-handoff/readme-phase2.md` Step 15 + the real Settings screen markup/`computeSettings()` logic in `Shell Skeleton v2.dc.html` (lines 1441-1630, 6003-6147). Confirmed via `gh issue view 2204`'s comment thread that this targets ShaneBuilder (buildSet corrected BuildConsole→ShaneBuilder→Settings across 3 follow-up comments) and confirmed 25 real env-var names + real usage counts by scanning `test-manifests/**/*.json` for `{{NAME}}` tokens directly (no fixture data). Building: EnvVar/SettingRow records, ISettingsStore (JSON-backed for plain values, Windows Credential Manager P/Invoke for secrets), real ScanManifests(), the Settings document tab wired to the gear icon + wrench menu + Ctrl+K palette (only 3 real entry points that exist in ShaneBuilder today — there is no separate topbar menu bar built yet).
