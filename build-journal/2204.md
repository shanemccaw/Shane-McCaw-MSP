# #2204 — Settings, redesigned (ShaneBuilder Step 15)

- **Status:** ✅ DONE 2026-09-02T16:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — the freeze applies to desktop/BuildConsole only; #2198 confirms all new console feature work targets ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 8d4248288 (pushed as e17b4620e after rebase onto origin/main)

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — Real audit done: ShaneBuilder has ZERO existing Settings surface (no `SettingsTabView`, no "Needs Value" text, `BtnRailSettings` gear has no click handler, no wrench-menu/palette entry). The "31 identical cards" state referenced in the issue body lives only in the frozen legacy `desktop/BuildConsole/MainWindow.SettingsTab.cs` (a different, frozen app) — this is a greenfield build against `desktop/ShaneBuilder/wpf-handoff/readme-phase2.md` Step 15 + the real Settings screen markup/`computeSettings()` logic in `Shell Skeleton v2.dc.html` (lines 1441-1630, 6003-6147). Confirmed via `gh issue view 2204`'s comment thread that this targets ShaneBuilder (buildSet corrected BuildConsole→ShaneBuilder→Settings across 3 follow-up comments) and confirmed 25 real env-var names + real usage counts by scanning `test-manifests/**/*.json` for `{{NAME}}` tokens directly (no fixture data). Building: EnvVar/SettingRow records, ISettingsStore (JSON-backed for plain values, Windows Credential Manager P/Invoke for secrets), real ScanManifests(), the Settings document tab wired to the gear icon + wrench menu + Ctrl+K palette (only 3 real entry points that exist in ShaneBuilder today — there is no separate topbar menu bar built yet).
- 2026-09-02T16:05:00Z ✅ DONE — Shipped: `Services/SettingsModels.cs` (EnvVar/SettingRow/SettingType/AccountProfile/ClaudeProjectInfo/ISettingsStore, verbatim contract), `Services/WindowsCredentialManager.cs` (real advapi32 P/Invoke CredWrite/CredRead/CredDelete, no NuGet), `Services/SettingsStoreService.cs` (JSON store at `%AppData%\ShaneBuilder\settings.json` for plain values + Credential Manager for secret-shaped keys; real `ScanManifests()` regex-scans `test-manifests/**/*.json` for `{{NAME}}` tokens — verified against the real repo: 25 real vars with real per-file usage counts, e.g. `TEST_PORTAL_EMAIL`/`TEST_PORTAL_PASSWORD` used in 49 manifests, `DEPLOY_URL` in 91), `MainWindow.Settings.cs` (all render logic: header health strip with real navigate+filter chips, left rail grouped Environment/Credentials/Automation/This machine with real count badges, env var rows with reveal/copy/clear, API Tokens & Credentials, Accounts & Tiers with add/remove, Claude Projects with real Primary/Secondary tabs + editable email/plan + user-managed project list, 7 "simple" settings categories as real toggle/text/select rows backed by the store). Wired the one real Settings surface to all 3 entry points that exist in ShaneBuilder today: the gear icon (`BtnRailSettings`, previously had zero click handler), the wrench-menu "Settings" entry, and the Ctrl+K palette. Fixed a real classifier bug caught in self-review (bare `MAIL` substring match would have misclassified `TEST_ADMIN_EMAIL`/`TEST_PORTAL_EMAIL`/`TEST_MAILBOX_ID` as mailer domain — narrowed to `SMTP`/`MAILER`). Verification: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors. Domain classifier dry-run-verified against all 25 real scanned var names. Did NOT touch the API Explorer's autofill-lock UI (`RenderApiExplorerAuthRow`'s "Accounts & Tiers (#2204) isn't built" honest-not-built state) — that wiring is #2202's own scope, which is separately blocked on this issue; #2204's job per the dispatch was only to get the real `AccountProfile` data shape and store right so #2202 can read from it, not to wire the lock itself. Branch verified merged into origin/main (`e17b4620e`) via `verify-branch-merged.mjs`; working tree clean. Commented on #2204 and flipped `in-flight`→`complete` labels.
