# #3090 — Fix bad /login and /account-security paths in MFA enrollment manifest

- **Status:** ✅ DONE 2026-09-12T22:30:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** c6ef98962

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — fixing `test-manifests/portal/account-security-mfa-enrollment.json` lines 75/80: `/login`→`/portal/login`, `/account-security`→`/portal/account-security`. Verifying manifest via shaneapp://runTest afterward.
- 2026-09-12T22:30:00Z ✅ DONE — fixed both lines (c6ef98962), merged into origin/main via rebase. Ran the manifest live via `shaneapp://runTest`: 7/9 steps passed. The 2 failures are NOT caused by this fix: (1) `POST /api/auth/mfa/totp/verify-setup` returns 500 instead of expected 400 for a bad code — pre-existing bug, out of this issue's scope; (2) the UI navigate step still hit `http://localhost:5175/login` (old path) because `shaneapp://runTest` resolves against the separate dev-server checkout, which had not yet picked up this branch's commits at the moment of the run — the same documented structural gap (Git #1371/#1372 worktree isolation) this manifest's own notes already record for prior sessions. Confirmed the fix is correct by cross-checking `test-manifests/portal/account-settings.json` lines 114/138, which use the identical `/portal/login` path with the explicit comment "outer WouterRouter base = '/portal'". `git status --porcelain` clean; `verify-branch-merged.mjs` confirms merged into origin/main.
