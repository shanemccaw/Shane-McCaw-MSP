# Git #2169 — BuildQueueMapWindow NRE on open

- **Status:** ✅ DONE 2026-09-01T23:15:00Z
- **Scope:** buildconsole

## Log
- 2026-09-01T23:00:00Z — Confirmed root cause: BuildQueueMapWindow.xaml.cs constructs the `_timer` field after `InitializeComponent()`, but the XAML sets `AutoRefreshToggle IsChecked="True"` which fires `AutoRefreshToggle_Changed` during `InitializeComponent()`, calling `_timer.Start()` on a null field, throwing a NullReferenceException. Fix: move `_timer` construction + Tick wiring before `InitializeComponent()`.
- 2026-09-01T23:15:00Z — ✅ DONE — Moved `_timer` construction above `InitializeComponent()` in the `BuildQueueMapWindow` constructor (commit 72acf8aa0). `dotnet build -c Debug` succeeded (0 errors, 0 warnings). Did not launch BuildConsole.exe itself for live UI verification per the standing NEVER-launch-BuildConsole.exe rule (single-instance mutex risk against Shane's own live session) — verification here is build success plus direct code inspection confirming the field-initialization-order fix is correct and matches the crash's real stack. Merged to origin/main at 04d3e654.
