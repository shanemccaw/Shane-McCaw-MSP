# #1939 — SOPs execution hook: automated GET/read steps have no execution path

- **Status:** ✅ DONE 2026-09-03T02:45:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 651315563 (implementation), e08be04d5 (IN FLIGHT bookend)

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — Adding a `graph_read_operation` workflow-engine node (the
  read counterpart to `graph_write_operation`) so `buildSopWorkflowGraph` can materialize a GET
  step through the same reused engine, per the issue's own "Suggested fix" section. No CR gate
  for reads — gating `sop-execution.ts`'s Authorization block on whether any materialized step
  is actually a write.
- 2026-09-03T02:45:00Z ✅ DONE — Built exactly the issue's own "Suggested fix": a new
  `graph_read_operation` node type (`workflow-executor.ts`), the read counterpart to
  `graph_write_operation` — fires via `graphFetchForTenant` (read-app credentials), no
  write-back/write-consent gate, blocked in dry-run same as the write node. Registered in
  `node-type-registry.ts` and the `admin-workflows.ts` node catalog; added `graph_read_operation`
  to the `WfNodeData` type union (`lib/db/src/schema/index.ts`) and to the three
  `switchChosenHandle` edge-routing checks alongside `graph_write_operation`.
  `buildSopWorkflowGraph` (`sop-workflow-graph.ts`) now materializes a GET step into this node
  instead of leaving it permanently unmaterialized; `MaterializedSopStep` carries a new
  `kind: "write" | "read"` field. `sop-execution.ts`'s #1497 CR-gate Authorization block is now
  gated on `hasWrites` — a run whose materialized steps are entirely reads needs neither a CR nor
  the testbed-only fallback, matching the issue's own "No Change Control gate is needed for a
  read" statement. `SOP-SEED-IAM-03` step 1 and `SOP-SEED-IR-02` step 2 (the two live examples
  the issue names) now materialize and fire for real instead of silently never running.
  Extended `sop-workflow-graph.test.ts` for GET materialization, the bare-`GET`-with-nothing-after
  edge case, and the `kind` field on every existing assertion.
  **Verification:** `vitest`/`tsc` binaries are unreachable from this worktree AND from the main
  checkout itself (`api-server/node_modules/.bin` junction resolves correctly to the main
  checkout, but the main checkout's own linked `.bin` has neither binary) — a pre-existing
  environment gap, already tracked at #2192/#2193, not something this session introduced or
  fixed (pnpm install is not the remedy per CLAUDE.md's hard rule). Verified instead by: (1)
  `node --experimental-strip-types --check` on every touched `.ts` file (clean, no syntax
  errors); (2) directly exercising `buildSopWorkflowGraph` via
  `node --experimental-strip-types` against three real scenarios (mixed GET+POST, all-GET,
  bare `"GET"` with no path) and confirming the materialized nodes, `kind` values, and node
  shapes matched what the test file now asserts. All changes reviewed by re-reading the diff.
  Working tree clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing this line.
