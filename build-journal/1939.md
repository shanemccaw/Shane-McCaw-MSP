# #1939 — SOPs execution hook: automated GET/read steps have no execution path

- **Status:** ⏳ IN FLIGHT 2026-09-03T02:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — Adding a `graph_read_operation` workflow-engine node (the
  read counterpart to `graph_write_operation`) so `buildSopWorkflowGraph` can materialize a GET
  step through the same reused engine, per the issue's own "Suggested fix" section. No CR gate
  for reads — gating `sop-execution.ts`'s Authorization block on whether any materialized step
  is actually a write.
