# #1456 — MFA hero/banner counts (already fixed)

- **Status:** ✅ DONE
- **Scope:** platform
- **Commit(s) verified against:** e3a236243 (pre-existing fix), dc1e9c1cc

## Log
- Investigated `artifacts/msp-portal/src/pages/portal-v2-security-mfa.tsx` per #1456.
  Found the fix already landed in commit `e3a236243` ("MFA hero banner: derive
  counts from live gapUsers/partialUsers (Git #1431)"), prior to this session.
  Current code drives every hero/banner span from `gapUsers.length`,
  `partialUsers.length`, `gapAdminCount = gapUsers.filter(isAdminGapUser).length`,
  and `partialUnregisteredCount = mfaUnregisteredCount(partialUsers)`. No
  hardcoded literal strings ("8 accounts", "2 are admin", "2 of 5") remain.
- ✅ DONE — no code changes required; issue was already resolved by prior work
  referenced in code comments as Git #1431.
