# #1507 — Risk Register: split the acceptance clock from the review clock

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `33f8b5a34` (merged to `origin/main`; `verify-branch-merged.mjs` exit 0)
- **Migration:** `lib/db/migrations/manual/2026-08-30-risk-review-clock-split-1507.sql` — run against local Postgres in-session, logged on #1630
- **Findings filed:** none new (see notes below)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Separating the review clock from the acceptance `status` on `msp_risk_decisions`. Removing `expired` from the acceptance vocabulary and the policy `decision_state` vocabulary (#1527), adding a real review clock (`review_state` + machine `review_due_at`), migrating any wrong rows to an active acceptance with an overdue review, updating routes + `Wire*` interfaces. Ends at the wire contract — no UI.
- 2026-08-30 ✅ DONE — Shipped the clock split. Details below. Ended at the wire contract as scoped; no UI created.

## What shipped

- **`lib/db/src/schema/msp.ts`** — three new exported vocabulary constants (`RISK_ACCEPTANCE_STATUSES` = `pending_signature | active | revoked`, with `expired` removed; `RISK_REVIEW_STATES` = `on_track | due | overdue`; `POLICY_DECISION_STATES` = `proposed | live | due`, `expired` removed per #1527). Two new nullable columns on `mspRiskDecisionsTable`: `reviewDueAt` (`review_due_at` timestamptz) and `reviewState` (`review_state` text). Updated the `status`/`riskStatus` and `decisionState` comments to name the third (review) clock and drop `expired`.
- **`lib/db/migrations/manual/2026-08-30-risk-review-clock-split-1507.sql`** — adds the two columns (`ADD COLUMN IF NOT EXISTS`), column comments, and corrects any `status='expired'` → `active`+overdue-review and any `decision_state='expired'` → `live`+overdue-review. Self-marks in `simulator_migration_runs`. **Run against local `DATABASE_URL` in-session; verified recorded.**
- **`artifacts/api-server/src/routes/msp-rbd.ts`** — the MSP-side create validator's `status` `z.enum` now references `RISK_ACCEPTANCE_STATUSES`, so `expired` is no longer an accepted acceptance status at the write path.
- **`artifacts/api-server/src/routes/portal-risk-register.ts`** — `WireRisk` and `WirePolicyDecision` gain `reviewDueAt` (ISO) + `reviewState`; `WireAcceptance` loses `until` (the acceptance carries no lifetime); the accept 201 response no longer emits `until`. `iso()` hardened to treat `undefined` as null. Header + inline comments rewritten to describe three clocks.
- **`artifacts/api-server/src/routes/portal-risk-register.test.ts`** — 4 new tests: review clock passes through on the risk wire; review clock served as null when unscheduled; acceptance block has no `until`; policy-decision review clock passes through with `expired` gone; plus a `no until` assertion on the accept 201 response.

## Verification (real, observed)

- **Live DB before migrating:** 1 row total (`RBD-2026-575`, `status='pending_signature'`). **Zero** rows carried `status='expired'` or `decision_state='expired'`. The GOV-A4 "EXPIRED / review date 9 May 2026" cited on #1507 was retired **portal-v2 fixture** data, never a DB row — recorded honestly here, no rows needed migrating locally.
- **Migration:** ran clean (`BEGIN … COMMIT`), 0 rows updated (as expected), 0 `expired` values remaining in either column, both new columns present and nullable, self-marker row written.
- **tsc:** `pnpm run typecheck` in `artifacts/api-server` → **0 errors** (whole project clean, not just my files).
- **vitest:** `portal-risk-register.test.ts` → **16/16 passed** (12 pre-existing + 4 new). It is in `vitest.config.ts`'s explicit include list.

## Notes / behaviour to know

- **Alert-suppression interaction (intended, not a regression):** `customer-tenant-alert-engine.ts` suppresses a finding when a `check_key`-linked `msp_risk_decisions` row has `status='active'`. Migrating an `expired` row to `active` therefore lets it suppress again — correct under the settled model (the acceptance is active; only the review lapsed). No such rows exist locally, so no behaviour changed here.
- **`expiration_date` column kept** (not dropped — destructive DDL is Shane's call, and it is `NOT NULL`). It is simply no longer surfaced as an acceptance `until` on the wire; the review clock owns the date now.
- **No MSP renewal path built**, per #1507. Nobody re-signs; the review is advanced operationally. `reviewDueAt`/`reviewState` are served null-as-null today (no writer yet, same pattern as the register columns) — the operational writer belongs to later sub-issues (#1508 versioning, #1513 overdue-review alerting).
- **Scope stop honoured:** no page/route/component created in `artifacts/portal`; `Design/portal` carries no export for this module.
- **No new findings filed** — nothing broken was discovered that is out of scope; the `expired`→`active` semantics were the assigned work, not an incidental defect.
