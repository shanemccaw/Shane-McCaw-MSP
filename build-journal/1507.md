# #1507 — Risk Register: split the acceptance clock from the review clock

- **Status:** ⏳ IN FLIGHT      <!-- flip to: ✅ DONE -->
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>
- **Migration:** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Separating the review clock from the acceptance `status` on `msp_risk_decisions`. Removing `expired` from the acceptance vocabulary and the policy `decision_state` vocabulary (#1527), adding a real review clock (`review_state` + machine `review_due_at`), migrating any wrong rows to an active acceptance with an overdue review, updating routes + `Wire*` interfaces. Ends at the wire contract — no UI (artifacts/portal has no page; Design/portal carries no export).
