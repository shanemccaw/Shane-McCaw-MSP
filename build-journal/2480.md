# Build Journal — #2480

- **Status:** ✅ DONE 2026-09-03T00:20:00Z
- **Scope:** buildconsole
- **Issue:** #2480 — Build Chain Map: interactions (drag/sentinel/gate/edges/status), sub-issue of Feature #2473
- **Started:** 2026-09-02
- **Commit(s):** `86691f9f2` (work) · `91e591232` (IN FLIGHT bookend) — both on `origin/main`

## Log
- 2026-09-02T23:40:00Z — ⏳ IN FLIGHT — Session started. Blockers #2476 (ChainRules), #2477 (canvas), #2478 (edge layer), #2479 (inspector) all complete/merged. Pulled #2480 body + both comments (dispatch + "screenshots added, use for fidelity"). Read `BuildMap/README.md` full interaction table (lines 213-235), §5 chain rules, and the reference `Build Chain Map.dc.html`'s own `reorder`/`moveFeature` (lines 652-660). Cross-checked every interaction-table row against the existing code: the Inspector-side mutations (board status, sentinel, manual gate, reorder-by-arrow, add/remove blocker, Re-wire, Expand/Collapse, Reset, Delete/Esc) were all wired in #2479. The one genuinely missing interaction is **canvas drag-to-reorder a Feature header onto another** — #2477's own class doc explicitly defers "drag reorder" to #2480, and nothing implements WPF drag-drop on the headers yet. Also found a §5 correctness item: the existing `MoveFeature` uses an array SWAP, while the reference `reorder(from,to)` is a SPLICE (`o.splice(fi,1); o.splice(ti,0,from)`) — identical for adjacent arrow moves but the drag path needs the real splice. Plan: (1) add a shared `ReorderFeature(from,to)` splice on the window matching the reference verbatim (select the moved Feature + the exact confirmation note), refactor `MoveFeature` to call it; (2) add canvas-side drag-to-reorder on Feature headers (click-vs-drag threshold so a plain click still toggles/selects; source header at 0.45 opacity during the drag; `AllowDrop` + `Drop` on every header) raising a new `FeatureReordered` event the window handles via `ReorderFeature`, which then `RechainGates` per §5.2 step 3, keeping fanin + manual edges. In-memory only, same "real write is #2481" precedent.
- 2026-09-03T00:20:00Z — ✅ DONE — commit `86691f9f2` (`Build Chain Map: canvas drag-to-reorder + splice reorder (#2480)`) on `origin/main`, verified via `verify-branch-merged.mjs` (exit 0). Implemented the one missing interaction-table row — **drag a Feature header onto another** — plus the §5 splice-vs-swap correctness fix. Canvas (`ChainCanvasControl`): Feature headers are now WPF drag sources with a click-vs-drag threshold (a plain click still toggles+selects on mouse-up; only movement past `SystemParameters.MinimumHorizontal/VerticalDragDistance` promotes to a drag), the source header renders at 0.45 opacity during the drag (matches the prototype's `opacity: drag===fid ? .45 : 1`), and every header is an `AllowDrop` target whose `Drop` raises the new `FeatureReordered(from,to)` event. Window: new `ReorderFeature(from,to)` ports the reference's `reorder()` verbatim as a **splice** (`RemoveAt(fi); Insert(ti, from)`, `ti` taken before removal) — the old `MoveFeature` used an array **swap**, correct only for adjacent moves, so the non-adjacent drag genuinely needed the splice; then `RechainGates` regenerates gate edges per §5.2 step 3, keeping fanin + manual edges, selects the moved Feature, and posts the reference's exact note. `MoveFeature` (the ←/→ arrows) now delegates to `ReorderFeature(fid, order[i+dir])`, exactly as the reference's `moveFeature` calls `reorder` — one shared splice path, no drift. Also made header-click clear any edge selection (mutual-exclusivity, matching `SelectFeature`). In-memory only; real GitHub persistence remains #2481. Verification: `dotnet build` clean (0 warnings, 0 errors); throwaway harness confirmed the splice matches the reference `reorder()` across adjacent/non-adjacent/to-end/to-front (7/7, incl. adjacent==swap so the arrows don't regress), then deleted. End-to-end WPF drag is not manifest-testable (BuildConsole is out of scope for test manifests per CLAUDE.md) and the app was not launched to verify (single-instance-mutex rule). No findings filed — every other interaction-table row was already correctly wired by #2479; nothing broken was discovered. Working tree clean; branch verified merged.
