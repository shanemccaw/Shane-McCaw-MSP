# #2110 — Dynamic Build Queue Map Phase 2: real-time visual rendering

- **Status:** ✅ DONE 2026-09-01T00:48:25Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 34f9e4651 (work), plus this bookend

## Log
- 2026-09-01 ⏳ IN FLIGHT — Building the Phase-2 visual map against #2109's landed data layer (`BuildQueueMap` / `BuildQueuePostgresClient.GetQueueMapAsync`, commit dfbf54576). New floaty Window rendering each queue item as a node, blocker relationships as connector lines, deadlock cycles flagged unmissably, errors/failed items visually distinct, real per-item age. Live-updating via a DispatcherTimer polling `GetQueueMapAsync`. Basic/default styling — real Design still pending. Reuses the shared `_queueDb` client and the ActivityBar floaty-toggle pattern (same as Build Watch #980).
- 2026-09-01 ✅ DONE — Shipped Phase-2 visual rendering (34f9e4651). New `BuildQueueMapWindow.xaml` + `.xaml.cs`: a floaty, live-updating (6s DispatcherTimer + manual Refresh + a Live toggle) map that binds directly to #2109's `GetQueueMapAsync` snapshot. Renders every queue item as a NODE card laid out left→right by dependency depth (memoized longest-path, same-cycle edges excluded so a genuine loop can't recurse forever); blocker relationships between two queued builds as CONNECTOR LINES on a Canvas (amber = still blocking with an arrowhead into the blocker, faint green = cleared); genuine deadlock CYCLES flagged unmissably three ways — a loud red top banner naming each loop (`#A → #B → #A`), dashed-red loop edges, and a red `DEADLOCK` node badge — not just a chain that closes on itself; ERROR/CRASHED (orphan exit -2)/LIMIT-PAUSED items visually distinct (red/orange accent + explicit badge, driven off `QueueMapErrorKind`); real per-item AGE (time in current state, plus total-in-queue when they differ) + a per-node tooltip. Also surfaces #2109's fail-closed blocker-unverified state as a yellow banner when GitHub was unreachable. Wired into the ActivityBar as a new floaty toggle button (🗺, next to Build Watch) → `MainWindow.ToggleBuildQueueMap()`, same open/close/Owner lifecycle as Build Watch #980, passing the shared `_queueDb` (null in agent mode → "Not connected" state). No new migration, no schema change (pure read of #2109's model). No fixture/hardcoded data — every node/line/count comes from the live snapshot. VERIFICATION: `dotnet build BuildConsole.csproj` clean — 0 warnings, 0 errors (twice). BuildConsole (WPF) is out of scope for test manifests per CLAUDE.md, so verification is the clean build. Branch verified merged into origin/main (verify-branch-merged.mjs, exit 0). Working tree clean.
