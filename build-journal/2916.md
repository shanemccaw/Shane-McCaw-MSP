# #2916 — Batter Up + AI Batter Up 401 on manual refresh (transient, self-resolved — no code bug)

- **Status:** ✅ DONE 2026-09-04T23:15:15Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 15837297a (IN FLIGHT bookend) + this DONE bookend. **No product/code change** — investigation confirmed no bug to fix.

## Log
- 2026-09-04T23:09:07Z ⏳ IN FLIGHT — Pulled #2916 body + comments. Shane's 21:58:54Z correction supersedes the "fresh regression" framing: both panels self-resolved BEFORE any fix landed. Adjusted scope = quick real audit of points 1-2; close honestly if clean.
- 2026-09-04T23:15:15Z ✅ DONE — Audit + live-log diagnosis complete. **No code bug.** Verified against real code AND the live activity log:

  **Point 1 (fresh client per call) — CLEAN.** `BatterUpPanel.RefreshAsync` builds `new GitHubApiClient(settings.GitHubPat)` per call from a fresh `BuildConsoleSettings.Load()` (BatterUpPanel.xaml.cs:273,293). `AiBatterUpPanel.GetClient()` does the same, with the #2737 no-caching fix intact and documented (AiBatterUpPanel.xaml.cs:89-94).

  **Point 2 (PAT resolves correctly / #2770) — CLEAN.** The #2770 resilient-Load (4-attempt retry + loud logging on a genuinely-unreadable existing file) and atomic Save (temp + File.Move) are fully intact (BuildConsoleSettings.cs:751-853). Not disturbed by tonight's edits.

  **Live evidence (activity-2026-09-04.log) — decisive.** `git-board.auth` lines all day: **0**. Empty-PAT warnings: **0**. There was NO real 401 and the PAT was never empty. The real failure was the **#2815 rate-limit circuit breaker** returning a synthetic **403 "rate-limit circuit open"**: tripped 16:52:07, held OPEN 60s, both panels' refresh failed (17 each today), HALF-OPEN probe at 16:53:31, RESET/closed 16:53:32 — the exact ~90s self-recovery Shane reported. Issue filed 21:57Z ≈ 16:57 local, minutes after recovery. NOT a regression from #2900/#2891/#2889/#2890 (those bounded/reduced GitHub load if anything).

  **Point 3 (PAT-length diagnostic) — already exists, permanently.** `GitHubApiClient` ctor logs an empty-PAT warning (length only, never value); `LogIfUnauthorized` attributes any real 401 with token length and distinguishes the empty-token race from a rejected token. No temporary logging needed.

  **Finding filed:** #2926 (parented under #1202, `bug`, board=AI Batter Up) — the panels display the transient, self-recovering circuit-open 403 as an indistinguishable generic hard error, which is what made a 60s self-healing state look like a standing URGENT 401 bug. Root-cause call-volume already tracked by open #2817; #2867 (circuit-stuck-open) closed today.

  Verification: no build required (no code changed); conclusion grounded in code audit + live activity-log evidence. Working tree clean apart from this bookend.
