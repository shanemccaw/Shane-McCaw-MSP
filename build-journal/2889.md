# #2889 — Bound ChatDockService.BuildAsync's unbounded Task.WhenAll GitHub burst

- **Status:** ✅ DONE 2026-09-04T18:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 12ae35ec4e6997619f5a4923b11e990af19f0a51 (merged to main; on the worktree branch as 8ebc32ef2eaea423271d24135be2826e753476f5 before rebase)

## Log
- 2026-09-04T18:12:00Z ⏳ IN FLIGHT — recovered unprovisioned worktree (C:\wt\2889-q1636 was empty, no .git — same class as #2118), registered it, now reading ChatDockService.cs to bound the unbounded Task.WhenAll concurrency per #2889's confirmed root cause.
- 2026-09-04T18:30:00Z ✅ DONE — `desktop/BuildConsole/Services/ChatDockService.cs`: `BuildAsync`'s outer per-mentioned-issue `Task.WhenAll(itemTasks)` fan-out (landed via #2686, the confirmed source of the "7 simultaneous `gh issue view` calls in a 20ms window" burst) is now gated through a `SemaphoreSlim(MaxConcurrentItemFetches = 3)` via a new `BuildItemThrottledAsync` wrapper — still concurrent (keeps #2686's real load-time win, not reverted to sequential), but at most 3 mentioned issues' GitHub work is in flight at once instead of all N simultaneously. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Rebased onto origin/main (048078bcd → 12ae35ec4) and pushed directly to main (`verify-branch-merged.mjs` confirms ancestor). `git status --porcelain` clean.
