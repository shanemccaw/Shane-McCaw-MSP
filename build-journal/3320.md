# #3320 — Real command tray on capture-box focus -- Ctrl+K concept realized (Type/Talk/Scan + Quick actions grid)

- **Status:** ✅ DONE 2026-09-09T04:47:41Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** 5a5a694b6 (merged into main; rebased twice over two concurrent sibling merges, no functional conflicts -- one test-file content conflict resolved by keeping both sibling's and this build's new check() blocks)

## Log
- 2026-09-09T04:19:06Z ⏳ IN FLIGHT — Read issue body + README; scope is `web/shanes-life` (public/index.html + public/app.js + src/core/capture-grammar.mjs). About to read the real design source (`Shanes Life - First Slice Prototype.dc.html`, `d.trayOn` ~line 1805) and existing capture bar wiring before building.
- 2026-09-09T04:47:41Z ✅ DONE — Built the real command tray (Type/Talk/Scan mode toggle + Quick
  actions grid + close, ported 1:1 from the design's `d.trayOn` markup) in
  `public/index.html`/`app.css`/`app.js`, wired to real live data from a new `GET
  /api/capture-tray` endpoint. Direct-run chips submit through the exact same real `#capture`
  submit handler (deterministic capture grammar) a hand-typed capture already uses -- no
  duplicated dispatch. New `heading_to_place` capture-grammar rule (issue's own regex, verbatim)
  + `src/core/location-state.mjs` (migration 071: `users.heading_to`/`heading_to_at`) give the
  "heading to X" phrase a real, persisted location-transition fact, referenced in the toast's
  real per-destination checklist mention (Heading Out list for the Rental, the real to-home
  take-checklist for home, real Tesla preconditioning trigger when connected).
  `tesla_warm_preconditioning` grew a real "cool it down" phrasing for the tray's own
  temperature-aware climate chip. Ported the design's own never-shipped Pantry critter pair
  (`c-pantry`/`c-pantry2`, slot `1y`) into `critters-sprite.svg`/`critters.js` since the tray's
  "Out of…" chip needed it and the design had already drawn it, just never ported.
  **Verification**: `npm run migrate` (071 applied cleanly), `npm run selftest-capture-grammar`
  (55/55, 6 new assertions), `npm run check` against the real local server/DB (217/221 -- the
  4 failures are pre-existing `SL_VAULT_KEY` gaps in this checkout, unrelated to this build), a
  live disposable-user script exercising all 5 real destination/checklist/Tesla-fallback paths
  through the actual database plus a real HTTP call to `GET /api/capture-tray` (all passed, script
  deleted after). Real, honest scope cut: the design's own `trip`/arrival-confirmation Today card
  was not built (not in the issue's 4 stated scope items) -- filed as #3325, parented under
  Feature #3220, board status AI Batter Up. `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms `agent/3320-q2044` (5a5a694b) is an ancestor of
  `origin/main`.
