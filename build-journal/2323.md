# #2323 — New chat opens as a claude.ai tab with the anchor written into its subtitle

- **Status:** ✅ DONE 2026-09-02T19:55:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 8a253531a521d34158ea402c7026f295f9d17150

## Log
- 2026-09-02T19:30:00Z ⏳ IN FLIGHT — Investigated MainWindow.xaml.cs / MainWindow.ChatsPanel.cs /
  NewChatAnchorDialog.xaml.cs. Today `OpenNewChatFlow` (built by #2321) only renavigates the single
  persistent `epic-1202` chat tab and toasts the picked anchor — it never creates a real new TabDef,
  and there's no `Subtitle` on `TabDef` at all. Plan: give `TabDef` a `Subtitle`, have `OpenNewChatFlow`
  create a genuine new chat TabDef (its own Id, real `EpicNumber`/`FeatureNumber` set directly from the
  dialog's explicit pick — no async resolve needed), select it, navigate the shared WebView to
  claude.ai/new, and render the anchor as the tab's subtitle line in the Chats rail row
  (`MainWindow.ChatsPanel.cs` `BuildChatRow`).
- 2026-09-02T19:55:00Z ✅ DONE — Shipped in commit 8a253531. `TabDef` gained `Subtitle` and a
  `featureNumber` ctor param. `OpenNewChatFlow` (rebased cleanly onto #2322's concurrently-landed
  "decide later" work) now creates a real `TabDef` per new chat — Title "New chat", `EpicNumber`/
  `FeatureNumber` set directly from the dialog's explicit pick (no async resolve wait), `Subtitle`
  carrying the full anchor line (`Feature #N "Title" (Epic #M)`, or "Unanchored — decide later"),
  then selects it and navigates the shared WebView to claude.ai/new. `BuildChatRow`
  (`MainWindow.ChatsPanel.cs`) renders that subtitle as its own line under the title, only when
  present. Local `dotnet build ShaneBuilder.csproj` — 0 errors, 0 warnings, both before and after
  the final rebase onto origin/main. `verify-branch-merged.mjs` confirms 8a253531 is an ancestor of
  origin/main. `git status --porcelain` clean.
  Filed finding **#2465** (sibling sub-issue of #2318, `bug`, milestone v1.1, board status "AI
  Batter Up"): real multi-tab chat creation exposes that `SelectTab` never renavigates the shared
  WebView per tab (no per-tab conversation URL exists), and `ArchiveChatTab` orphans a secondary
  tab instead of closing/resetting it correctly — both genuine gaps, out of #2323's own scope.
  Honest gap: `BtnStartNewChat_Click` (the "context getting full" quick-action button in the
  context bar) still renavigates the currently active tab in place rather than opening a new
  anchored tab — it has no anchor disclosure and was out of scope for #2323, which is specifically
  about the `OpenNewChatFlow` (#2321 disclosure) path.
