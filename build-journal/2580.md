# #2580 — Risk Register / RBD: regenerate the contract pack from the finished backend (MSP Console)

- **Status:** ✅ DONE 2026-09-03T18:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `0276b4c2f` (bookend IN FLIGHT), `5d2562e74` (contract pack, rebased to `15a2979e1` on `main`)

## Log
- 2026-09-03T17:40:42Z ⏳ IN FLIGHT — Document phase for Feature #1682 (Risk Register / RBD, MSP
  Console). Confirmed #1680 (msp-console scaffolding) was closed NOT_PLANNED 2026-09-03T15:36Z as
  part of the #1571 epic reset, and #2580/#2581/#2582 were freshly filed 2026-09-03T15:44Z under
  the new 4-step order (API → Document → Design → Wire) — the artifact not existing yet is a
  blocker for #2582 (wire), not for this Document step, since backend is confirmed real. Starting
  full line-by-line audit of the 16 MSP-side routes (`msp-rbd.ts`, `msp-rbd-instances.ts`,
  `msp-rbd-versions.ts`) per the #1642 pattern, writing
  `docs/risk-register-msp-console-contract-pack.md` (operator-facing wire, distinct from the
  existing customer-portal-facing `docs/risk-register-contract-pack.md`).
- 2026-09-03T18:05:00Z ✅ DONE — Read all 3 route files in full (370+264+476 lines), their
  supporting libs (`rbd-instances.ts`, `rbd-versioning.ts`, `rbd-document-render.ts`), and
  cross-referenced `admin-rbd.ts` (the pre-existing, deliberate #1294 AdminV2 surface over the
  same table) plus both real consumers found (`riskDecisionsStore.ts` for `available-checks`,
  `get-risk-register.ts` MCP tool for the list route). Actual route count is **17, not 16** (the
  dispatch spot-check missed `available-obligations`). Verified every schema citation live against
  local PostgreSQL (`msp_risk_decisions`, `risk_instances`, `msp_rbd_versions` — all match Drizzle
  source, including the one real CHECK constraint on `risk_instances.status`).

  Wrote `docs/risk-register-msp-console-contract-pack.md` — 17 routes documented, cross-surface
  edges, real enum table, forbidden list, 4 open-gap notes for #2582's benefit. **One real bug
  found and filed: #2697** — `admin-rbd.ts` (and, lower severity, the MCP `get-risk-register.ts`
  tool) independently hand-duplicate a stale pre-#1507 status enum that still contains `"expired"`,
  so a PlatformAdmin can PATCH a live decision back to `expired` today, contradicting the existing
  customer-portal pack's claim that fix is "shipped, not merely settled" module-wide. #2697 filed
  `bug`, milestone 5, parented as a sub-issue of #1682 (this Feature), board status set to AI
  Batter Up. No `URGENT` prefix — real but not incident-level, one-line import-swap fix.

  Also found and fixed a stale `blocked_by` edge: #1682 itself was still `blocked_by` #1680, which
  had been closed `NOT_PLANNED` 2026-09-03T15:36Z (before this build's own 15:58Z dispatch) as part
  of the #1571 reset. Removed the edge and left an explanatory comment on #1682 per the Git #1987
  discipline, since a closed blocker left wired would have silently misrepresented #1682 (and by
  extension #2580/#2581/#2582) as still blocked.

  **Verification:** every file:line citation in the pack spot-checked against the actual current
  source after writing (`sed -n` against the cited line numbers) — all matched. `git status
  --porcelain` clean apart from this file. `verify-branch-merged.mjs` confirmed `agent/2580-q1404`
  (`15a2979e1`) is an ancestor of `origin/main`. No product code, schema, or UI changed — read-only
  pass as required.
