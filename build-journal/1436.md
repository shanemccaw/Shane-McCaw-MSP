# #1436 — Chat context meter (#1424) not visibly updating — verify live DOM scraper against current claude.ai markup

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** fba8a31d2

## Log
- 2026-08-27 ⏳ IN FLIGHT — investigating ChatContextMeterScript.cs selectors against real claude.ai DOM, MainWindow.xaml.cs WebMessageReceived wiring across the 4 CreateChatContextWrapper call sites, and progress bar layout.
- 2026-08-27 ✅ DONE — Static investigation confirmed hypotheses #3 (wiring) and #4 (layout) from the issue are NOT the cause: all 4 `CreateChatContextWrapper` call sites go through `BuildChatWebView`, whose `Loaded` handler calls `InjectBuilderButtonsAsync` (script injection + `ChatWv_WebMessageReceived` subscription) unconditionally; the progress bar has an explicit `Height=4` in an `Auto` grid row, not clipped. Could NOT live-verify hypotheses #1/#2 (whether the DOM selectors actually match current claude.ai markup) — no available protocol/tool in this headless build session can drive an authenticated claude.ai WebView2 session and inspect its live DOM (`shaneapp://runTest` only covers this repo's own portal/marketing manifests, not external claude.ai). That gap is real and unresolved by this session. Instead of leaving the silent-failure mode as-is, hardened the fix defensively: (1) `ChatContextMeterScript.cs` now layers 2 additional structural fallback selectors (`[data-testid^="conversation-turn"]`, `[data-test-render-count]`) alongside the original 4, additive-only so it can't regress a currently-working match; (2) added an in-page self-diagnostic — if the scraper finds 0 turns for ~10s straight on a URL that looks like a real populated `/chat/<id>` page, it now sets `selectorsLikelyStale` on the postMessage payload; (3) `MainWindow.xaml.cs`'s `UpdateContextMeter` reads that flag, logs it once via `ActivityLog.Log("system.core.chat-context", ...)`, and flips the meter into an explicit gray/warning visual state with an honest tooltip + banner ("Context meter can't read this chat (selectors likely stale) — see Git #1436") instead of silently sitting at value=0 looking identical to "chat just started" — which is exactly what read as "not working" in the original report. Verified via `dotnet build BuildConsole.csproj` — 0 errors, 46 pre-existing warnings unrelated to this change. Flagged the remaining live-DOM-verification gap plainly in the issue comment; a follow-up session with an actual authenticated claude.ai session open in BuildConsole should watch DevTools/the new diagnostic banner to confirm whether the selectors were in fact stale.
