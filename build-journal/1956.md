# Git #1956 — Remediation Tracker guide-catalogue drift guards ENOENT

- **Status:** ✅ DONE 2026-09-02T03:40:00Z
- **Scope:** platform

## Log

- 2026-09-02T02:15:00Z — Started. Confirmed via `find` that `previewRemediationGuide.ts` and
  `remediationLiveGuide.ts` are genuinely missing from the repo at any path — not just moved.
  `git log --diff-filter=D` shows both deleted wholesale in `f40438cd` ("Portal scaffolding:
  create artifacts/portal, retire artifacts/msp-portal"), which explicitly preserved the old
  tree at tag `portal-archive-2026-08-29` before deleting it.
- Plan: restore `previewRemediationGuide.ts` verbatim at the new
  `artifacts/portal/src/components/copilot-journey/` path (its only dependency, `PillarKey`
  from `./journeyTokens.ts`, still resolves — that file is a live re-export shim to the
  `@workspace/copilot-scan-scene` package, unaffected by the msp-portal deletion). Restore
  `remediationLiveGuide.ts`'s real data section (`STEP_CHECK_KEYS`, `STEP_CHECK_GAPS`,
  `PROCESS_ONLY_STEP_IDS`) verbatim too, but WITHOUT the UI-rendering functions
  (`stepEvidence`, `liveTitle`, `buildLiveRemediationSteps`, etc.) that type against
  `JourneyPillarView`/`JourneyView` from the also-deleted `journeyModel.ts` — restoring those
  cascades into `journeyModel.ts` → `sowLiveScope.ts` → more, i.e. resurrecting most of the
  deliberately-retired copilot-journey UI, which conflicts with the #1485 architected
  rebuild order (architect → endpoints → contract pack → Design → wire) and is out of this
  issue's scope. The three drift-guard tests only ever read `STEP_CHECK_KEYS` / the step
  catalogue fields via regex off the raw file text — they don't need the UI functions.

- 2026-09-02T03:40:00Z — ✅ DONE. Restored `previewRemediationGuide.ts` verbatim and
  `remediationLiveGuide.ts`'s real data exports (trimmed of the dead UI functions, per
  the plan above) at `artifacts/portal/src/components/copilot-journey/`, plus the tiny
  `journeyTokens.ts` re-export shim they depend on (still live via
  `@workspace/copilot-scan-scene`). Wired `@workspace/copilot-scan-scene` as a real
  `artifacts/portal` dependency (`package.json` + `tsconfig.json` reference + a manual
  node_modules junction in this worktree, matching the existing per-package linking
  scheme) so `journeyTokens.ts`'s `PillarKey` import resolves — confirmed via
  `npx tsc -p tsconfig.json --noEmit` in `artifacts/portal`, zero errors.

  Fixed the three drift-guard tests' stale `../../../msp-portal/...` path strings to
  `../../../portal/...` (`remediation-tracker-catalogue.test.ts`,
  `remediation-tracker-verification.test.ts`, `portal-remediation-tracker.test.ts`).

  With the guard restored to actually comparing (rather than ENOENT-ing), it caught a
  REAL drift: api-server's own hand-duplicated `STEP_CHECK_KEYS` in
  `remediation-tracker-verification.ts` was missing `s28: ["onedrive:sync-errors"]` —
  present in the real guide (added by #753) but never mirrored server-side. Added it,
  and confirmed `onedrive:sync-errors` is a real, live check key
  (`lib/dashboard-registry/src/metrics.ts:1814`), not fabricated. Also corrected a
  now-stale comment in the same file that still listed s28 as a gap/process-only step.

  **Verification:** could not run the real `vitest`/`tsc` for `artifacts/api-server` in
  this worktree — `node_modules/@types` and the `vitest` pnpm store entry are real
  junctions pointing at genuinely EMPTY directories in the shared main checkout
  (confirmed via `fsutil reparsepoint query` + direct inspection, both dated the same
  recent timestamp, unrelated to this change and pre-existing). Filed as **#2193**
  (parented to #1202), board status set. Instead wrote and ran a standalone script
  (`npx tsx`, not committed) reproducing each of the three tests' exact regex-extraction
  and comparison logic against the real restored/live files — all three PASS. Portal's
  own `tsc --noEmit` ran clean (that package's node_modules was not affected by the
  same corruption).

  Merged `agent/1956-q1156` into `origin/main` directly (`e2370b902`) — clean merge, no
  conflicts. `verify-branch-merged.mjs` confirms merged. Working tree clean.

  Commits: `98f864ebf` (the fix, on `agent/1956-q1156`, merged to main as `e2370b902`).
  Findings filed: **#2193**.
