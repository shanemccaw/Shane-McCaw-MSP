# #1933 — Ownership / RACI: per-workload RACI-membership toggle, surfaced in Settings

- **Status:** ✅ DONE 2026-09-03T03:56:01Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 2e74d63de (impl), 9b7418be0 (merge), pushed to main as e355439bf

## Log
- 2026-09-03T03:43:10Z ⏳ IN FLIGHT — Per issue body + Shane's 2026-08-30 correction comment: this is a per-workload RACI-membership toggle (NOT a tracking-scope/findings-suppression toggle — that framing was retracted). Untracking removes the workload from the RACI accountability matrix only; scanning/alerting/findings continue regardless, and untracking itself is a finding ("disable unused services"). Building: `portal_ownership_workload_membership` table (mirrors `portal_ownership_policy`'s per-customer settings-toggle shape from #2162), GET/PUT routes under `/portal/settings/ownership/workloads`, wiring into `GET /api/portal/ownership` to exclude untracked workloads from the matrix, and a finding emission (`msp_diagnostic_findings`, checkKey `governance:untracked-workload:<key>`) when a still-enabled workload is untracked. POA&M (#1935) is the accept/remediate exit path and is out of scope here per the issue's own note — referenced, not built. Permission gating: no `can()` engine exists yet (#1704 undecided-in-code); following the existing `requireRole("CustomerUser")` + `resolveCustomerId`/`resolveTenantScope` pattern every sibling route uses.
- 2026-09-03T03:56:01Z ✅ DONE — Shipped: `portal_ownership_workload_membership` table + migration (`lib/db/migrations/manual/2026-09-03-ownership-raci-workload-membership-1933.sql`, run against local Postgres, verified via `\d`); `GET/PUT /api/portal/settings/ownership/workloads[/:key]` in `routes/portal-settings-ownership.ts`; `routes/portal-ownership.ts` now excludes untracked workload keys from the RACI matrix read; `lib/ownership-workload-membership.ts` (membership read/write + the "disable unused services" finding evaluator — `msp_diagnostic_findings`, checkKey `governance:untracked-workload:<key>`, severity `warning`, `findingSource: "baseline"`, mirroring `policy-compliance-evaluator.ts`'s non-Graph finding pattern). Full toggle→finding flow verified end-to-end directly against the real local DB schema (insert membership row, insert run+finding, verify read shape — rolled back). **Typecheck could not be run**: a real check found this worktree's `artifacts/api-server` node_modules resolve through junctions into empty content in MAIN's own pnpm store (27+ core packages — `drizzle-orm`, `express`, `esbuild`, etc. — confirmed empty at the physical MAIN path too, not a worktree symptom) — filed as #2515 (parented under #1202, not this Feature — it's dev-tooling/pnpm-store infra, unrelated to RACI). Per CLAUDE.md did not run/prescribe `pnpm install` to work around it. Verified correctness instead via direct SQL against the real schema + manual code review against sibling route conventions (`req.user as AuthUser`, `requireRole`/`resolveCustomerId` patterns already used identically elsewhere in this same file). Contract pack `docs/ownership-raci-contract-pack.md` was NOT regenerated — it already predates #2162's gate-mode toggle (zero `gateMode` hits), so this is a pre-existing gap, not one introduced here; out of this issue's stated scope stop. Branch merged into `main` (verified via `verify-branch-merged.mjs`), working tree clean. Findings filed: #2515.
