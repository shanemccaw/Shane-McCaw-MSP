# #4191 — Permissions engine: build evaluateAccess() — the pure RBAC + tier decision core

- **Status:** ✅ DONE 2026-09-15T02:10:29Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 6938f3a67 (work), 8d2a07520 (IN FLIGHT bookend)

## Log
- 2026-09-15T01:24:33Z ⏳ IN FLIGHT — Leaf 1 of #1704. Building the pure `evaluateAccess()` function (RBAC first, tier entitlement only if RBAC allows, distinct `basis` per denial, human-readable `reason` on both) alongside `lib/db/src/rbac/evaluate.ts`, with full unit coverage of every branch and the ordering guarantee. No new caching (per #4191's documented decision). BuildConsole buildId 2838.
- 2026-09-15T02:10:29Z ✅ DONE — Shipped `evaluateAccess()` in `lib/db/src/rbac/access.ts` (commit 6938f3a67, on origin/main, ancestry verified): RBAC first and unconditionally, tier entitlement only after an RBAC allow, `basis: "rbac"` / `basis: "entitlement"` denials each with a human-readable `reason`, `requiredTier` named from the real `services` catalog rows by `sort_order` (null when no tier bundles the module), `upgradePath` = `/portal/billing`. `PORTAL_TIER_MODULE_KEYS` moved to `lib/db/src/rbac/tier-modules.ts` and re-exported from `portal-tier-features.ts` so all 12 route importers are untouched. `rbac-ladder.ts` snapshot comment now states it is the one permanent caching exception. No new caching. Verification: `lib/db` `tsc -b` clean; api-server `tsc --noEmit` clean; `access.test.ts` 23/23 (incl. Proxy-based proof the tier input is never read on an RBAC denial); lib/db RBAC suites 96/96; api-server portal-tier-features + remediation-tracker + risk-register 69/69; rbac-ladder.test.ts green. Pre-existing failure NOT mine: `rbac-capability.test.ts` RetainerNoConsent case fails identically on baseline c4cb85916 — filed as #4208. Findings filed as siblings under #1704: #4207 (poams bundled by no tier — question lane), #4208 (pre-existing failing test), #4209 (msp-entitlement upgradeUrl dead route). No migration. Working tree clean.
