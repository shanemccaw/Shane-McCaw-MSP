# #2943 — ps-execution PnP/SharePoint session type: feasibility investigation

- **Status:** ✅ DONE 2026-09-06T14:34:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `397c0b1cc` (work), `6d9bc738a` (IN FLIGHT bookend)

## Log
- 2026-09-06T13:55:00Z ⏳ IN FLIGHT — Investigating whether the ps-execution container can realistically gain a PnP/SharePoint session type, or whether the 23 `sharepoint-admin` resource types should be permanently `no_executor` with a documented reason.
- 2026-09-06T14:34:00Z ✅ DONE — **Answer: neither.** Don't build the PnP session type, and don't mark the 23 rows permanently `no_executor`. Both options assume PowerShell is the only way to read SharePoint tenant administration; it isn't.

  **Verified #2943's premise first** — all five container claims re-confirmed true (no PnP module in the Dockerfile, no `sharepoint`/`spo` session type in `child-worker.ps1`, zero `Pnp` catalog entries, zero `Get-Pnp*` survey rows, 23/23 rows gated).

  **What changes the answer:** `artifacts/api-server/src/lib/sharepoint-admin.ts` is already a complete app-only SharePoint tenant-admin client (cert `client_assertion` auth, CSOM `ProcessQuery` on `{prefix}-admin.sharepoint.com`, SPSiteManager REST) — and CSOM `ProcessQuery` is the same wire protocol `Get-PnPTenant` uses. PnP is a *client for* it, not a privileged path to it. `Sites.FullControl.All` + `SharePointTenantSettings.Read.All` are already consented on tenants 1 and 3; the cert env vars are already set.

  **Live verification (real, not asserted):** wrote and ran `scripts/config-state/probe-sharepoint-admin-transport.mjs --tenant 1` against the testbed tenant, read-only. Every probed endpoint returned **200 app-only on the first attempt** — Graph `/v1.0/admin/sharepoint/settings` (28 properties), CSOM `Tenant` (**325 properties**, covering 5/5 SPOSharingSettings + 5/5 ODSettings + 4/4 SPOTenantSettings probe properties), and `/_api/HubSites`, `/_api/SPHSite`, `GetSiteDesigns`, `GetSiteScripts`, `GetStorageEntity`. `0 items` results are an empty testbed tenant, not auth failure (that would be `401 unsupported app only token`).

  **Coverage of the 23:** 8 from one CSOM read, 8 from admin-host REST/method, 2 search-admin (XML, own shape work), 5 per-site fan-out (O(sites) on *any* transport — PnP makes that group slower, not cheaper).

  **Root cause filed as #2974** (sub-issue of #1850, `bug`, AI Batter Up): `config-snapshot-collector.ts:1115` routes `sharepoint-admin` into `collectPowerShellResource()`, so the rows are asked of an executor that structurally cannot serve them while the one that can sits unused in the same process.

  **Shipped:** `docs/sharepoint-admin-transport-decision-2943.md` (decision record, coverage map, tradeoffs, ordering); the probe script; corrected the `#2873` comment block in `build-snapshot-registry.mjs` that asserted building the PnP session type was the follow-up work; `lib/db/migrations/manual/2026-09-06-sharepoint-admin-notes-2943.sql` — **run against local, 23 rows updated**, `notes` text only. `is_collectable`/`not_collectable_reason` deliberately unchanged: the #2873 gate stays right until #2974 lands. Migration recorded on #1630.

  **Honest gaps:** no snapshot has ever run against the local DB (`tenant_config_snapshots` is 0 rows), so the collector path itself was not exercised end-to-end — the transports were verified directly instead, which is what the question actually turned on. The 5 per-site and 2 search-admin resource types were reasoned from their cmdlets and the CSOM object model, not probed live; the 16 tenant-level ones were. `SPOTheme`'s `HideDefaultThemes` is not a `Tenant` property (separate CSOM method) — noted in the doc rather than glossed.

  Branch verified merged into `origin/main` (`verify-branch-merged.mjs` exit 0). Working tree clean; nothing pre-existing was dirty on arrival.
