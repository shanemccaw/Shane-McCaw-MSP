# #2136 — Architecture: board Status columns as source of truth for workflow state

- **Status:** ✅ DONE 2026-09-01T15:04:47Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** c46d8fe5c (code), 3d48fa53f + 5d70e3c09 + this (bookend)

## Log
- 2026-09-01T14:29:48Z ⏳ IN FLIGHT — Read full issue #2136 + all comments. Final decisions: "Git IS the database" for durable workflow state; add real board Status columns (Verifying, Crashed, Parked) alongside Batter Up / AI Batter Up; rewire dispatch/completion logic (QueueWatcherService, BuildQueuePostgresClient VerifyingStatus/LimitPausedStatus) to move board columns via updateProjectV2ItemFieldValue; build a cleanup/migration dialog for stale local verifying/parked/crashed rows. Starting real audit of the two dispatch files + live GraphQL Status field inspection before writing anything.
- 2026-09-01T14:29:48Z ⏳ IN FLIGHT — Audit complete. Board Status field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0` on project `PVT_kwHOEiBDdc4BeoiY`. Existing option ids: Batter Up `09b1927f`, AI Batter Up `a0296971`, Backlog `63cc47c8`, Park `19cfa11c` (already existed!), Done `0003ae3b`, In progress `6cf0ca80`, etc. Board-move mechanism already exists: `GitHubApiClient.SetProjectItemStatusAsync(itemId, optionId)` / `SetIssueStatusByNumberAsync(number, optionId)` / `GetIssueBoardStatusAsync(number)`. Existing Park mirror pattern in `BuildQueuePanel.SyncGitHubParkStatus` (fire-and-forget). Completion→verifying lives in `BuildQueuePostgresClient.MarkCompleteAsync`; verifying→done in `PromoteVerifyingToDoneAsync` (only checks CLOSED, never board/milestone — the #1867 root cause).
- 2026-09-01T14:29:48Z ⚠️ INCIDENT (caused by this session, fully reported) — Added Verifying (`1ac1c7b2`) + Crashed (`6c8640a8`) via `updateProjectV2Field`. My FIRST call omitted the existing options' `id`s, so GitHub DELETED the 11 originals and created 13 fresh options with NEW ids — which CLEARED the single-select Status value on all 2126 board items. I immediately re-ran the mutation passing every original `id` (the input type accepts an optional `id`), which RESTORED all original option ids exactly (Batter Up `09b1927f`, AI Batter Up `a0296971`, Park `19cfa11c`, etc.) + the two new columns with stable ids. So the board STRUCTURE is fully intact and every hardcoded id in code/CLAUDE.md/scripts stays valid. BUT item→status ASSIGNMENTS remain wiped (confirmed: full 22-page walk = 2126/2126 null; per-issue checks null) and are NOT recoverable via the API (no field-value history; `bt_issues.status` local cache is stale Aug-17 and uses a different vocabulary). Impact is bounded: local dispatch queue `bt_build_queue` is UNTOUCHED so running/queued builds are unaffected and no concurrent build takes a destructive action off an empty board scan; what's lost is Shane's board-level triage (which issues sat in Batter Up / AI Batter Up / Park). Did NOT guess-repopulate lanes — that would fabricate board state, the exact bug class this issue kills. Filing a finding + Shane To-Do so Shane re-triages. LESSON: adding a single-select option MUST pass every existing option's id, or all item assignments are wiped.
- 2026-09-01T14:29:48Z ⏳ IN FLIGHT — Implementing the real deliverables: Verifying/Crashed id constants; BoardStatusSync mirror; MarkCompleteAsync returns (status, github#); watcher mirrors verifying→Verifying / failed→Crashed; PromoteVerifyingToDoneAsync reconciles verifying rows against the real board (Park→parked, Crashed→failed, Done/closed→done, fail-closed); cleanup/migration dialog for stale local rows.
- 2026-09-01T15:04:47Z ✅ DONE — Shipped in commit `c46d8fe5c`. Three required pieces all landed:
  (1) **New board columns** Verifying `1ac1c7b2` + Crashed `6c8640a8` added to Status field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0` (Park `19cfa11c` already existed); all 11 original option ids preserved.
  (2) **Dispatch/completion rewired to write board state** — `GitHubApiClient` gains the two id constants; new `Services/BoardStatusSync.cs` is the shared fire-and-forget board-column mirror (existing Park sync now delegates to it); `MarkCompleteAsync` returns `(status, github#)`; the watcher reap mirrors exit-0→Verifying, failure→Crashed, orphan(-2)→Crashed; `ReconcileVerifyingAgainstBoardAsync` clears a local verifying row when its REAL board column moved to Park/Crashed/Done (fail-closed on unreachable board), wired into all 3 manual-refresh sites (MainWindow, BuildWatchWindow, LeftSidebar) — the direct #1867 fix. Local Postgres stays the immediate dispatch cache; the board is now the durable authority.
  (3) **Cleanup/migration dialog** `StaleStateReconcileWindow.cs` (code-behind-only) + launcher on the Build Queue header — lists every local verifying/parked/crashed/limit-paused row against its live board Status with per-row Migrate-to-column / Dismiss. No schema change needed (uses existing statuses); no migration file.
  **Verification (real):** `dotnet build` clean (0 warn / 0 err); new board columns confirmed as live valid options; `GetStaleWorkflowStateRowsAsync` query run against local Postgres returns the real stuck rows — 32 failed, 3 parked, 2 verifying — **including the motivating incident #1867 (row 964, verifying)** which the dialog will now surface; and an end-to-end board move of #2136 to the new Verifying column via the exact `updateProjectV2ItemFieldValue` mutation the mirror runs succeeded and read back `Verifying`.
  **INCIDENT filed as #2140** (parented to #1202, Shane To-Do, AI Batter Up): the id-less `updateProjectV2Field` wiped all board item Status assignments (2126/2126 null) before I restored the option ids; structure intact, assignments unrecoverable — Shane to re-triage Batter Up / AI Batter Up / Park lanes. Local dispatch queue unaffected.
  Branch verified merged into origin/main (verify-branch-merged.mjs exit 0). Working tree clean.
