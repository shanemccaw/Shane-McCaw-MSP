# #2478 — Build Chain Map: edge rendering (SVG layer)

- **Status:** ⏳ IN FLIGHT 2026-09-02T23:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** _fill at DONE_

## Log
- 2026-09-02T23:40:00Z ⏳ IN FLIGHT — Session start. Sub-issue of #2473 (Feature: Build
  Chain Map), item #4 of its structured index. Blocked-by #2477 (canvas/node rendering)
  is closed/`complete` — unblocked. Scope: the `blocked_by` edge SVG-equivalent layer
  per `desktop/BuildConsole/BuildMap/README.md`'s "Edges" section + the reference
  `buildEdgeLayer`/`edgeLook`/`pathD` in `Build Chain Map.dc.html` — orthogonal
  (default) and curved path modes, arrowheads, bundling with ×N labels for collapsed
  Features, the exact stroke/width/opacity/dash table per edge kind, selection-emphasis
  dimming (`dimUnrelated`), hover behavior (invisible 10px hit path, opacity/width bump,
  150ms opacity transition). Drawing into `ChainCanvasControl.EdgeLayer` (the `Canvas`
  #2477 already exposed for this), reading `ChainLayout.OutPort`/`InPort` against
  `Layout`/`Derived`/`ExpandedFeatures`. Inspector wiring for edge details (#2479) and
  edge add/remove mutation (#2480) are out of scope here — this issue only draws the
  layer and owns click-to-select/hover as visual state needed to demonstrate dimming.
