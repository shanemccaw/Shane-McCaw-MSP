# #2478 — Build Chain Map: edge rendering (SVG layer)

- **Status:** ✅ DONE 2026-09-03T01:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 49cbd9293 (work), ebe6fa9a8 (post-rebase HEAD on main), fe636d464 (IN FLIGHT bookend)

## Log
- 2026-09-02T23:40:00Z ⏳ IN FLIGHT — Session start. Sub-issue of #2473 (Feature: Build
  Chain Map), item #4 of its structured index. Blocked-by #2477 (canvas/node rendering)
  is closed/`complete` — unblocked. Scope: the `blocked_by` edge SVG-equivalent layer
  per `desktop/BuildConsole/BuildMap/README.md`'s "Edges" section + the reference
  `buildEdgeLayer`/`edgeLook`/`pathD` in `Build Chain Map.dc.html` — orthogonal
  (default) and curved path modes, arrowheads, bundling with ×N labels for collapsed
  Features, the exact stroke/width/opacity/dash table per edge kind, selection-emphasis
  dimming (`dimUnrelated`), hover behavior (invisible 10px hit path, opacity/width bump,
  150ms opacity transition). Drawing into `ChainCanvasControl.EdgeLayer` (the `Canvas`
  #2477 already exposed for this), reading `ChainLayout.OutPort`/`InPort` against
  `Layout`/`Derived`/`ExpandedFeatures`. Inspector wiring for edge details (#2479) and
  edge add/remove mutation (#2480) are out of scope here — this issue only draws the
  layer and owns click-to-select/hover as visual state needed to demonstrate dimming.
- 2026-09-03T01:15:00Z ✅ DONE — Shipped in 49cbd9293:
  `Services/BuildMap/ChainLayout.cs` (added `ChainEdgeStyle` enum + `PathData(a, b,
  style)` — orthogonal `M a H mid V b.y H b` and curved clamp(36,110) cubic Bezier,
  both real WPF Path Markup Syntax) and the new
  `Controls/BuildMap/ChainCanvasControl.EdgeLayer.cs` partial (edge-look table exact
  per README's stroke/width/opacity/dash rows, arrowhead triangles oriented from the
  real path tangent, ×N bundle labels via FormattedText outline+halo, hover via
  targeted DoubleAnimation on the existing Path rather than a full teardown/rebuild,
  edge click-to-select as a 4th selection kind wired into
  `ChainCanvasControl.xaml.cs`'s existing SelectIssue/SelectFeature/ClearSelection/
  SetDocument for mutual exclusivity). Verification: `dotnet build` clean (0
  warnings, 0 errors — fixed 32 CS8601 nullable warnings from cross-partial-file
  static field references by building brushes locally via the shared `Frozen`
  helper instead). 10/10 assertions in a throwaway console harness compiling the
  real ChainLayout/ChainRules/ChainDoc sources matched hand-computed expectations
  (orthogonal path string, curved dx clamping at both bounds, edge-bundling key
  logic for collapsed vs. expanded Features) — harness deleted after the run, not
  committed. `verify-branch-merged.mjs` confirmed merged into origin/main at
  ebe6fa9a8. `git status --porcelain` clean. Filed #2488 (parented under #2473, AI
  Batter Up label set): the README's Edges table documents edge-selected dim
  opacity as .18, but the .dc.html reference's actual `edgeLook` code uses .1
  (same as issue/Feature dimming) for every dimmed case — implemented per the
  README's literal table since this issue's own scope names README's Edges section
  as the source for "the exact...table".
