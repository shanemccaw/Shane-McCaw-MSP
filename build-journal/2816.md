# #2816 — config_resources model: 2 broken/inapplicable graph_path entries

- **Status:** ✅ DONE 2026-09-04T15:50:00Z
- **Scope:** platform

## Log
- 2026-09-04T15:20:04Z — Session started. Investigating `scripts/config-state/reconcile-live-evidence.mjs` and `build-resource-model.mjs` for the two resource_keys named in the issue: `graph:beta:/policies/b2cAuthenticationMethodsPolicy` (AADB2C 403, non-B2C tenant) and the `authenticationMethodConfigurations` child resource (400 as standalone GET, only reachable embedded in parent).
- 2026-09-04T15:50:00Z — ✅ DONE — Fixed both real model bugs, commit `3eda8cf8` (rebased/pushed to `main`; branch `agent/2816-q1599`).
  1. `scripts/config-state/reconcile-live-evidence.mjs` — `applyLiveEvidence()` gained a new `tenantMismatch` block that reconciles `error_code = 'AADB2C'` live evidence to `availability = 'unavailable'` (distinct from `needs_license`, which stays reserved for real license/feature gaps). `graph:beta:/policies/b2cAuthenticationMethodsPolicy` now correctly reads `unavailable` instead of sitting at `available_now`/`failed_live` forever. `build-resource-model.mjs` and `verify-sample.mjs` console logging updated to report the new `tenantMismatchKeys`.
  2. `scripts/config-state/build-resource-model.mjs` — new `MODEL_CORRECTNESS_UNAVAILABLE` build-time override map marks `graph:v1.0:/policies/authenticationMethodsPolicy/authenticationMethodConfigurations` `unavailable` with a documented reason (400s as a standalone GET; data is real but only reachable embedded under the parent). Applied on the next full rebuild, and applied to the current DB row directly now.
  - Applied both fixes live against the local Postgres DB (`DATABASE_URL`) via a scratch script (`scripts/config-state/_apply-2816.mjs`, run then deleted — not committed) that called the actual shared `applyLiveEvidence()` for fix 1, and issued the identical `UPDATE` the new build-time override will produce for fix 2 (a full `build-resource-model.mjs` rebuild needs a Graph `$metadata` cache not present in this fresh worktree, so a real network fetch would've been required just to re-derive rows the targeted UPDATE already sets correctly). Verified via `psql`: both resource_keys now read `availability = 'unavailable'` with real, honest reasons.
  - `verify-branch-merged.mjs`: OK, `agent/2816-q1599` (3eda8cf8) merged into `origin/main`.
  - `git status --porcelain`: clean.
