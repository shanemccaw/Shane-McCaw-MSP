# #3278 — Recipe 'Missing' ingredients sticker overflows off-screen with a long list

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform (web/shanes-life — Shane's Life app, Recipes feature #3222, Epic #3086)
- **Started:** 2026-09-08
- **Commit(s):** 97a0d2fbc (fix), f81e34cfc (merge to main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — content-level truncation on the "Missing" sticker text (first 2-3 items + "+N more") in web/shanes-life/public/app.js, plus a CSS safety net (max-width, white-space:nowrap, text-overflow:ellipsis, overflow:hidden) scoped to not affect other `.sticker` short-badge uses.
- 2026-09-08T00:20:00Z ✅ DONE — `recipeCard()` in `web/shanes-life/public/app.js` now truncates `Missing …` to the first 3 ingredients + `+N more` when the list is longer than 3. `.sticker` in `web/shanes-life/public/app.css` gained `max-width: min(70vw, 320px); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;` as a content-length-independent safety net — applies to all sticker uses (short badges included), which is safe since it only clips text that already exceeds the cap. `node -c app.js` passed. No live browser run (no `runTest` UI manifest exists for this static vanilla-JS app; `test-manifests/` scope in CLAUDE.md is limited to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`, not `web/shanes-life`). Merged agent/3278-q2006 into origin/main directly (f81e34cfc) since main had moved ahead with unrelated commits (#3272 Vault room) after this worktree was provisioned. `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
