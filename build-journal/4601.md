# #4601 — identity:mfa-method-breakdown countWhere + severity_rules

## Changes Made
- `lib/db/migrations/manual/2026-09-18-mfa-method-breakdown-severity-rules-4601.sql` — reworks `identity:mfa-method-breakdown`'s `mapping` from `count` to `countWhere` filtering `methodsRegistered` for legacy MFA method values (`sms`, `voiceMobile`, `voiceAlternateMobile`, `voiceOffice`), and adds real `severity_rules` (previously `[]`). Run against local Postgres in this session.

## Verification Results
- Migration applied: `UPDATE 1`, `simulator_migration_runs` row recorded.
- Live psql confirms the new `mapping`/`severity_rules` on the row.
- Real code-path verification (throwaway scratch script, not committed): `applyMapping` + `evalConditionGrammar` from `artifacts/api-server/src/lib/monitor-executor.ts` run against 5 synthetic `userRegistrationDetails`-shaped rows — 3/5 users with a legacy method correctly counted, per-user evidence published, severity rule fires only when `legacyMfaMethodCount > 0` (confirmed both a firing and a non-firing/clean case).
- No unit/integration test suite exists for `monitor_checks` catalog rows in this repo (data-only migration + executor logic, not new code) — verification depth matches the comparable prior severity-rules migration (#3799).
- `git status --porcelain` clean at session end. Branch confirmed merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs`.

- **Status:** ✅ DONE 2026-09-18T04:53:36Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** 97d699a91 (bookend+plan), 13cb589e6 (migration + mapping/severity_rules fix)

## Log
- 2026-09-18T04:49:04Z ⏳ IN FLIGHT — pulled #4601 body + single comment (dispatch prompt, no supersede). Confirmed live via psql: `identity:mfa-method-breakdown` mapping is `[{"transform": "count", "sourceField": "methodsRegistered", "targetField": "legacyMfaMethodCount"}]`, `severity_rules: []`. Reviewed `monitor-executor.ts` `countWhere` transform: array-of-OBJECT entries (`overEntries`) skip scalar/string entries as non-object, so `methodsRegistered` (a Graph string array) cannot be filtered entry-by-entry directly. The catalog's existing pattern for this shape is `sourceField: "value"` (whole-item scope, in `WHOLE_ITEM_SOURCE_FIELDS`) + `{{arrayField}} contains 'x'` in the predicate — used live by `identity:ca-legacy-auth-block` (`{{conditions.clientAppTypes}} contains 'exchangeActiveSync'`). Adopting that same shape here rather than inventing entry-level filtering.
- 2026-09-18T04:53:36Z ✅ DONE — wrote `lib/db/migrations/manual/2026-09-18-mfa-method-breakdown-severity-rules-4601.sql`, ran it against local Postgres (`UPDATE 1`, migration self-recorded in `simulator_migration_runs`). New mapping: `countWhere("{{methodsRegistered}} contains 'sms' || ... contains 'voiceMobile' || ... contains 'voiceAlternateMobile' || ... contains 'voiceOffice'")` with `sourceField: "value"`. New severity_rules: one warning rule, `legacyMfaMethodCount > 0`. Verified against the REAL `applyMapping`/`evalConditionGrammar` code (not just JSON validity) with a throwaway scratch script (deleted after use, never committed) exercising 5 synthetic Graph-shaped `userRegistrationDetails` rows — correctly counted 3/5 users with a legacy method, published per-user evidence, and the severity rule fired only on the non-clean tenant (0/2 clean-tenant case correctly did not fire). `git status --porcelain` clean, branch confirmed merged into origin/main via `verify-branch-merged.mjs`. No new finding filed — the two gaps this issue described (wrong semantics, dead severity_rules) are both what this build fixed; nothing further surfaced.
