# #4377 — Monitoring: reorder Buy.tsx to account → consent → pay

- **Status:** ✅ DONE 2026-09-16T17:13:51Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 910d18e4d (server: consent gate, resume, monitoring selection, live-DB test), 2d8c41592 (resume accepts a password-only buyer, mfaEnrolled), 86af59956 (Buy.tsx Monitoring account-first + BuyResumeSignIn), dd870abe2 (manifest lastVerifiedAgainstCommit)
- **Findings filed:** #4407 (first scan uses the tier at consent, not the tier paid for), #4408 (TOTP challenge has no replay protection; bug + security)
- **Migrations:** none

## Log
- 2026-09-16T16:35:14Z ⏳ IN FLIGHT — reading Buy.tsx's Monitoring stage machine and #4374's pre-consent account door, then moving Account to the front of the Monitoring path (Account → Connect → Tier/Pay → Portal).
- 2026-09-16T17:13:51Z ✅ DONE — Monitoring on /buy is now account → read consent → tier & pay → portal, built on #4378's shared account-first stages. Server: read-consent-url refuses Monitoring/Pack sessions with 409 account_required until the session's account has a password + MFA; GET /api/public/purchase/resume (Bearer) returns the signed-in buyer's own session and renews it if lapsed; POST /api/public/purchase/monitoring-selection updates tier/seats in place (seats lock at consent). Client: sign-in-to-resume panel (real /auth/login + TOTP/passkey challenge), ?resume=1 entry used by #4379's portal stub. Verification: api-server + marketing tsc exit 0; consent.test.ts 89/89; account-first-purchase.live-db.test.ts 2/2 against local Postgres; real-browser run (msedge via playwright-core, isolated router host from this worktree on :8099 + vite :5417, real DB) 24/24 across account → connect → context destroyed → sign in → same session restored → tier change → payment intent (Stripe test) → paid → destroyed → sign in → done → portal handoff. Stubbed only: the code email and Microsoft's consent screen (callback provisioning ran for real). Not run: shaneapp://runTest on the edited manifest steps, because the dev-server merge succeeded but the AccountFirstCheckout build set's restart is deferred until #4381 lands, so the dev ports still serve the old code. Synthetic rows cleaned up (0 left). Working tree clean; nothing pre-existing was dirty.
