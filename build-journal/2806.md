# #2806 — Local Map: new window reusing ChainLayout/BuildChainMapWindow's real rendering

- **Status:** ✅ DONE 2026-09-04T09:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `6b6a0d9ff` (merged to `main`)

## Log
- 2026-09-04T08:55:00Z ⏳ IN FLIGHT — confirmed #2805's `LocalSchemaDoc`/`LocalSchemaMapService` landed at `683e42707` (merged `aed82ad5d`, already present in this worktree). Real audit of `ChainLayout.cs`/`ChainCanvasControl.xaml.cs` found `ChainLayout.Compute` is hard-bound to `ChainDoc`'s Epic→Feature→Issue shape (documented explicitly in `LocalSchemaDoc.cs`'s own class doc from #2805) — not reusable as-is for a flat table/FK graph. Reusing what genuinely IS generic (`ChainLayout.ClampZoom`/`FitZoom`/`PathData`, `ChainRect`/`ChainPort`) and writing a new `LocalMapLayout` (DAG-layered layout) + a new `LocalMapCanvasControl` (pan/zoom/click, same interaction feel) + `LocalMapWindow` (new shell), fed by the real live `LocalSchemaDoc`.
- 2026-09-04T09:05:00Z ✅ DONE — shipped in `6b6a0d9ff`: `Services/BuildMap/LocalMapLayout.cs` (new DAG-layered layout — a table's column is the length of its longest real FK dependency chain, cycle/self-FK guarded), `Controls/BuildMap/LocalMapCanvasControl.xaml(.cs)` (new read-only canvas, same pan/zoom/click feel as `ChainCanvasControl`), `LocalMapWindow.xaml(.cs)` (new shell: top bar + stats + status strip + canvas + Inspector), `Resources/Icons.xaml` (+`Icon.Database`). `dotnet build BuildConsole.csproj` clean, 0 warnings/errors. Real verification: a throwaway harness (`ProjectReference` to `BuildConsole.csproj`, deleted after use — same disposable pattern #2805 used) instantiated `LocalMapWindow` against the real live local Postgres database: 387 tables / 416 FK edges loaded, layout computed with zero out-of-bounds nodes, `users` table resolved with 71 real incoming FK edges, table click-select confirmed working, Fit zoom computed, and a real screenshot captured (window fully rendered: title bar, stats chips, navigable graph, Inspector showing `users`' real columns/PK/references). `verify-branch-merged.mjs`: OK, merged into `origin/main`. `git status --porcelain`: clean. Wiring the Git Map / Local Map picker onto the chain-map icon is #2807's own scope (#2804 item #3) — not this issue's.
