# Bookend: #1973

- **Status:** ✅ DONE 2026-09-04T07:34:36Z
- **Scope:** platform

## Log
- 2026-09-04T07:26:47Z — Session started. Investigating `getTenantSettings()` 404 in `artifacts/api-server/src/lib/power-platform-admin.ts:351`. Hypothesis to verify: BAP admin `listTenantSettings` may need POST not GET.
- 2026-09-04T07:29:23Z — Verified live against testbed tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3`: plain POST to the existing `/scopes/admin/listTenantSettings` path still 404'd (hypothesis about the verb alone was insufficient). Checked Microsoft's own REST reference (learn.microsoft.com/en-us/power-platform/admin/list-tenantsettings): the real endpoint is `POST https://api.bap.microsoft.com/providers/Microsoft.BusinessAppPlatform/listtenantsettings?api-version=2020-10-01` — no `/scopes/admin/` segment at all, no request body. Re-verified live with the corrected path/verb: 200 OK, real tenant settings payload returned.
- 2026-09-04T07:31:00Z — Implemented fix in `power-platform-admin.ts`: split `bapGet` into a shared `bapRequest(method)` plus `bapGet`/`bapPostRead`, and switched `getTenantSettings()` to `bapPostRead` against the corrected path. Updated the file header comment that claimed "every request here is a GET" (now stale). `tsc --noEmit` clean. Ran `scripts/verify-power-platform-1869.mts` end to end against the testbed tenant — all 4 calls (token, reachability probe, listDlpPolicies, listEnvironments, getTenantSettings) now succeed live.
- 2026-09-04T07:32:46Z — Ran `vitest run src/lib/__tests__/monitor-executor.test.ts`: 13/203 pre-existing failures, all `vi.mock('../power-platform-admin')` missing the real `PowerPlatformNotRegisteredError` export. Confirmed via `git stash` that the exact same 13 failures reproduce on the base commit before this fix — unrelated to #1973, not introduced by this change. Filed as #2842, parented as a sibling sub-issue of #2489 (this issue's own Feature), labeled `bug`, board status set to AI Batter Up.
- 2026-09-04T07:34:00Z — Rebased onto `origin/main` and pushed directly to `main` (commit `df261dee9`, was `c1a3093e1` on the agent branch before rebase). `verify-branch-merged.mjs` confirms `main` has the commit. `git status --porcelain` clean except this bookend file itself.

✅ DONE — `getTenantSettings()` fixed: BAP's `listtenantsettings` action is POST-only and lives directly under `/providers/Microsoft.BusinessAppPlatform/`, not under `/scopes/admin/`. Fix verified live against the testbed tenant (200 OK, real settings payload) and via the full `verify-power-platform-1869.mts` harness (all 4 calls pass). Commit `df261dee9` on `main`. Filed #2842 for a pre-existing, unrelated test-mock gap discovered along the way (13 failing tests in `monitor-executor.test.ts`).
