# #4354 — Real unified Automation object (backend + client) — wraps Script/Runbook/Remediation Step, links to POA&M/CAB

- **Status:** ✅ DONE 2026-09-16T05:50:00Z
- **Scope:** platform (api-server + lib/db) + MyArchitect client repo (shanemccaw/MyArchitect)
- **Started:** 2026-09-16
- **Commit(s):** Shane-McCaw-MSP `ce58bac59` (backend) + `a744570c4` (remediation-key refinement) · MyArchitect `dcadd9b` (client)

## Log
- 2026-09-16T05:08:22Z ⏳ IN FLIGHT — Building the real unified `automations` wrapper. Confirmed
  the existing `automation_registry` table (Git #3771) is a DIFFERENT concern (a manual catalog of
  Microsoft-ecosystem automations: `power_automate_flow` | `ai_studio_agent`, no execution, no
  underlying-row FK, no POA&M/CAB links) — so a new `automations` table (type
  `script`|`runbook`|`remediation_step`, FK to the wrapped row, nullable `linked_poam_id` /
  `linked_cab_id` / `linked_remediation_step_id`) is justified and not a duplicate. Plan:
  (1) Drizzle schema + manual migration (run locally against DATABASE_URL);
  (2) endpoints: list, create, run (delegate to existing per-type execution — no second executor),
  history (read-through over the existing per-type execution-log sources — confirming a 4th table
  is NOT needed); (3) MyArchitect `IAutomationsService`, Automation List panel wiring, real
  "Link to Automation" actions on POA&M/Kanban, Automation contextual tab (Run/Edit/Delete/View
  History/Link). Investigating underlying Script(#4291)/Runbook(#3479)/Remediation Step(#3471)
  execution + history sources before writing code.
- 2026-09-16T05:50:00Z ✅ DONE — Backend (Shane-McCaw-MSP, on origin/main):
  - `lib/db/src/schema/msp.ts` — new `automations` table (type script|runbook|remediation_step,
    polymorphic `wrapped_ref_id` text, nullable real FKs `linked_poam_id`→msp_poams,
    `linked_cab_id`→msp_change_requests, `linked_remediation_step_id`→remediation_tracker_steps).
  - `lib/db/migrations/manual/4354-automations-wrapper.sql` — run against local DATABASE_URL
    (verified: table + 3 FKs + 3 indexes; FK enforcement confirmed via a rolled-back insert test;
    self-marked in simulator_migration_runs). Additive → agent-run per Database rules.
  - `artifacts/api-server/src/routes/msp-automations.ts` — list/create/patch/delete/run/history under
    `/api/msp/customers/:customerId/automations`. run delegates (no second executor): script →
    Script Library download-token (`lib/script-download-token.ts`, factored out & shared with
    portal-script-library.ts); remediation_step → verify re-scan (emitWorkflowEvent); runbook →
    current-cycle read (no executor exists — finding #4357). history = read-through over
    `script_run_results` / `portal_runbook_runs` / the step's verification fields — NO 4th table
    (confirmed sufficient). remediation_step keyed by stepId text; links accept linkedRemediationStepKey.
  - `artifacts/api-server/src/routes/index.ts` — router registered. tsc: 0 errors.
  Client (MyArchitect `dcadd9b`, on origin/main, dotnet build clean): IAutomationsService + list panel
  + contextual tab (Run/Edit/Delete/View History/Link to POA&M/CAB/Remediation Step) + reverse
  "Link to Automation" on POA&M and Kanban. See MyArchitect build-journal/4354.md.
  Finding filed: #4357 (runbook has no server-side executor — "Ask Shane").
  Note: the shared MyArchitect clone was in a conflicted mid-merge state on arrival (not mine, left
  untouched) — did client work in an isolated worktree. Both branches verified ancestors of their
  respective origin/main; working trees clean.
