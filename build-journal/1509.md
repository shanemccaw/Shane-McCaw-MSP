# #1509 — Risk Register: Risk instances as line items

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 66bedb976c968202d0aa883b522de995e44fd949

## Log
- 2026-08-31 ⏳ IN FLIGHT — building `risk_instances` as a line-item child of
  `msp_risk_decisions`: `foundAt`, `acceptedAt`, and a status/exit-reason
  distinguishing remediated vs. object-ceasing-to-exist. Wire contract only
  (Drizzle schema + manual migration run locally + MSP-side routes) — no UI,
  per SCOPE STOP (no `Design/portal/` export, no `artifacts/portal` page for
  this module yet). Foundation #1508 (RBD versioning) already landed on main.
- 2026-08-31 ✅ DONE — `risk_instances` added as a real FK-child of
  `msp_risk_decisions` (`riskDecisionId` — deliberately NOT keyed on the
  durable `rbdId` text the way `msp_rbd_versions`/#1508 is, since a line
  belongs to exactly one container row for its whole life). Columns:
  `label`, `objectId`, `foundAt` (required), `acceptedAt` (nullable, never
  editable after set — same 409-guarded contract as
  `msp_risk_decisions.acceptedAt`), `status` (`active`/`remediated`/
  `object_removed`, one flat enum matching `remediation_tracker_steps.status`'s
  precedent), `resolvedAt`/`resolutionNote`. Migration
  `lib/db/migrations/manual/2026-08-31-risk-instances-line-items-1509.sql` run
  against the local `DATABASE_URL` myself (table + 3 indexes + a CHECK
  constraint on `status` created; confirmed via `information_schema.columns`).
  Verified directly against the real local DB in a rolled-back transaction
  against the one live container row (`RBD-2026-575`, id 1): inserted 3
  instances, accepted one, resolved one `remediated` and one `object_removed`
  independently, confirmed the CHECK constraint rejects an invalid status and
  the FK rejects an orphan `risk_decision_id` — then rolled back, so no
  fabricated data was left in the shared local DB (`SELECT count(*) FROM
  risk_instances` reads 0 after).
  Routes (MSP-side only, matching #1508's `msp-rbd-versions.ts` precedent —
  no customer-portal counterpart, no UI, per SCOPE STOP):
  `GET /api/msp/rbd/:rbdId/instances`, `POST /api/msp/rbd/:rbdId/instances`,
  `PATCH .../instances/:id/accept`, `PATCH .../instances/:id/resolve`, in
  `artifacts/api-server/src/routes/msp-rbd-instances.ts` +
  `.../lib/rbd-instances.ts`, registered in `routes/index.ts`.
  Did NOT touch `msp_risk_decisions.acceptedAt`'s existing column or route
  behavior — additive only, per the standing rule against destructive/
  behavior-changing edits to a table two other routes already read/write.
  `lib/db` typechecks clean (`npx tsc --noEmit`) in this worktree.
  `artifacts/api-server`'s own `npx tsc --noEmit` could NOT be run against
  these changes from inside this worktree: its `node_modules/@workspace/db`
  is a junction to the MAIN checkout's `lib/db` (confirmed via
  `fsutil reparsepoint query` — a Mount Point, not a dangling link), which
  does not yet have this build's schema changes pre-merge. This is the
  documented worktree dependency-isolation model (junctioned node_modules),
  not a bug — did not attempt to work around it by writing into the
  junctioned `node_modules` (that would mutate the shared main checkout) or
  by running `pnpm install`. Verified correctness instead by (a) `lib/db`
  compiling clean in isolation and (b) exercising the real routes' SQL
  against the local DB directly, as above. Filed as a real finding, #2087,
  parented to #1202 (BuildConsole/dev-server infra) — not #1096, since the
  gap is in the worktree tooling, not application code — recording that a
  worktree session cannot fully verify tsc for a package one level removed
  from the package it edited, pre-merge.
  Migration filename appended to #1630 (additive local DDL, not a `Shane
  To-Do` per the standing exception).
  Branch verified merged (`node scripts/dev-server/verify-branch-merged.mjs`
  — OK, `66bedb97` an ancestor of `origin/main`). `git status --porcelain`
  clean.
