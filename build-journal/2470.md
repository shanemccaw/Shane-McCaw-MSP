# #2470 — Tab-type taxonomy + per-tab WebView2 instancing (port BuildConsole #972/#982 keep-alive)

- **Status:** ✅ DONE 2026-09-02T22:58:22Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Issue:** #2470 — phase 1 of Feature #2469 (Multi-Tab Lifecycle Architecture), Epic #1202 (Build Console)
- **Commit(s):** `e239ca631` (implementation), `64b1ec47c` (IN FLIGHT bookend)

## Log
- 2026-09-02T22:49:29Z ⏳ IN FLIGHT — Starting. Read `desktop/BuildConsole/MainWindow.ShelvedTabs.cs`
  (the proven #972/#982 keep-alive mechanism to port) and mapped ShaneBuilder's current single
  shared `ClaudeWebView`. Today ShaneBuilder hosts exactly one WebView2 (`ClaudeWebView` in
  `ClaudeChatDock`, Grid.Column=1) reused across every chat tab — this is the root of #2465: opening
  a second chat tab (`OpenNewChatFlow`, #2323) renavigates that single view, so switching tabs shows
  the same page in both. Plan: (1) add a real tab-type taxonomy field on `TabDef`
  (`TabKeepAliveClass` keep-alive vs reloadable; chat = keep-alive, all internal/doc tabs =
  reloadable); (2) port `ParkOffscreen` (1×1 at -20000,-20000, never zero — the load-bearing #982
  fix so `CoreWebView2` isn't torn down) into a new `MainWindow.KeepAliveTabs.cs` partial; (3) give
  each keep-alive tab its own persistent `WebView2`, parked in a non-hit-test keep-alive canvas when
  inactive and mounted into the chat dock when active; (4) repoint every `ClaudeWebView.` reference
  to an `ActiveChatWebView` accessor that returns the active keep-alive tab's live view. Reloadable
  tabs untouched.
- 2026-09-02T22:58:22Z ✅ DONE — Shipped on `main` at `e239ca631` (rebased onto origin/main;
  `verify-branch-merged.mjs` exit 0 — `agent/2470-q1309` is a genuine ancestor of origin/main).
  `dotnet build ShaneBuilder.csproj -c Debug` → Build succeeded, 0 warnings, 0 errors.
  - **New** `desktop/ShaneBuilder/MainWindow.KeepAliveTabs.cs` — the ported #972/#982 mechanism:
    `TabKeepAliveClass` enum; `_keepAliveViews` (one `KeepAliveView` per tab id, each its own live
    `WebView2` in its own host `Border`); `ParkOffscreen` (1×1 at -20000,-20000, never zero — the
    load-bearing #982 detail); `EnsureKeepAliveView`/`WireKeepAliveViewAsync` (per-tab CoreWebView2
    + SourceChanged URL tracking + #2325 meter script + message pump keyed to THE tab, so a parked
    tab's stats can't land under the active tab); `ShowKeepAliveTab` (mount active, park all
    others); `ActiveChatWebView` accessor; `NavigateActiveChat`; `SetActiveChatWebViewVisible`;
    `DisposeKeepAliveView`.
  - `MainWindow.xaml` — added the non-hit-test, ClipToBounds `KeepAliveHostCanvas` park canvas and
    replaced the single `<wv2:WebView2 x:Name="ClaudeWebView">` with the `ChatWebViewHost` mount Grid.
  - `MainWindow.xaml.cs` — `TabKeepAliveClass`/`IsKeepAlive` on `TabDef` (chat = keep-alive, all
    internal/doc tabs = reloadable, overridable for future browser tabs); `SelectTab` mounts the
    keep-alive view; `CloseTab` disposes it; every old `ClaudeWebView.` reference (send/read/poll/
    overlay-visibility/navigation) repointed to `ActiveChatWebView`; removed the obsolete
    `EnsureWebViewWired`/`_webViewWired` single-view shim.
  - `MainWindow.ChatsPanel.cs` — `ClaudeWebView_WebMessageReceived` → `OnKeepAliveChatStats(tabId,…)`
    keyed to the sending tab; Archive/Reopen navigate the active tab's own view; doc comments updated.
  - **Done-when:** (1) each keep-alive tab owns a distinct `WebView2` — `_keepAliveViews` per id;
    (2) switching two chat tabs shows each tab's own live page — mount/park reflow (the #2465 fix);
    (3) park never zero-sizes → `CoreWebView2` (login/session) survives park+restore; (4) reloadable
    tabs unaffected — gated on `IsKeepAlive`.
  - **Honest verification note:** verified by clean build + code-level review. No live UI click-test
    run — ShaneBuilder is a WPF app and this is a non-interactive session; test manifests are
    explicitly out of scope for `desktop/` per CLAUDE.md. One deliberate behavior change: the seed
    chat view is now created lazily on first chat-tab selection rather than eagerly at app startup
    (the old XAML `Source=` pre-warmed claude.ai at boot) — a cleaner lifecycle, not a regression
    against any done-when criterion.
  - No findings filed — no separate real bug/gap surfaced (the pre-existing multi-tab Archive
    semantics gap is already recorded in build-journal/2323.md).
