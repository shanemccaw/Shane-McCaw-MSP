# #2949 — Security Plan: build the real customer-facing sign action

- **Status:** ✅ DONE 2026-09-05T02:44:32Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 72ee04224 (route + lib + tests), 52ec0d4cc (contract pack regen), all on `origin/main`

## Log
- 2026-09-05T02:17:42Z ⏳ IN FLIGHT — investigating msp-security-plan.ts / portal-security-plan.ts and msp_security_plan_versions schema to decide signedBy reuse vs distinct customer-sign record, then build POST /api/portal/security-plan/versions/:versionUid/sign
- 2026-09-05T02:44:32Z ✅ DONE — **Real product decision resolved:** REUSE the existing `ClientApprover`/`signedBy` field on `msp_security_plan_versions` — no schema change. The table already had room for both halves: `createdBy`/`createdAt` is the MSP-side seal (already real, already built), `signed`/`signedBy`/`signedAt` is the client's own sign-off, the exact shape `msp_rbd_versions` and `msp_risk_decisions` already use. Confirmed against the direct precedent `portal-rbd-document.ts` (the RBD module already solved this exact problem — `msp.ts`'s own comment already calls the Security Plan version chain "the RBD pattern one level up").

  **Built:** `artifacts/api-server/src/routes/portal-security-plan-document.ts` (new router): `GET /api/portal/security-plan/versions` (history), `GET /api/portal/security-plan/versions/current` (the current, possibly-unsigned sealed version — the review step; deliberately NOT signed-only the way `assembledPlan` is), `POST /api/portal/security-plan/versions/:versionUid/sign` (the real customer sign action — `CustomerUser` role, `resolveTenantScope`-scoped, scoped-read-first so a wrong-tenant versionUid 404s rather than leaking a 409, `ipAddress`/`signatureHash` computed server-side never accepted from the client body, `email` from `req.user` not the body). Added `getSecurityPlanVersionByUid` to `security-plan-versioning.ts` (mirrors `rbd-versioning.ts`'s `getRbdVersionByUid`). Registered in `routes/index.ts`. No migration needed/run.

  **Tested:** `portal-security-plan-document.test.ts`, 10 tests (server-derived audit trail, typed-name floor, tenant-scoped 404 not 409, superseded/already-signed/race 409s, review-before-sign GET returning unsigned content), added to `vitest.config.ts`'s explicit include list. `pnpm run typecheck` clean. Full included vitest suite green with `DATABASE_URL` exported (5024 passed, 9 skipped) aside from one pre-existing, unrelated failing test (`free-scan-prospect.test.ts` — bad `services.is_public` catalog data, nothing to do with Security Plan) — filed as **#2950**, parented under #1096 (Application Core, no Feature-tier parent applied), board status "AI Batter Up".

  **Contract pack regenerated** per #2949's own ask (#1642 pattern, wholesale replacement): `docs/security-plan-contract-pack.md` now documents the already-shipped #2576/#2829 `assembledPlan` bridge (the prior pack's headline finding is now resolved in code) plus this build's new review+sign surface, with the signature-shape decision recorded and cited to real file:line.

  **Verification:** `git status --porcelain` clean. Rebased onto `origin/main` (6 commits landed concurrently, no conflicts) and pushed directly to `main` (fast-forward, `b08786dfa..52ec0d4cc`). `verify-branch-merged.mjs` exit 0 — genuinely merged, not just committed locally.

  **Findings filed:** #2950 (bug, unrelated pre-existing test/data gap, not Security Plan).

  **Open, flagged, not decided here (see contract pack §7):** whether the pre-existing MSP-side proxy-sign path (`PATCH /api/msp/.../versions/:versionUid/sign`, `MSPAdmin`) should eventually be deprecated now that a genuine customer sign path exists — left as an open product call for Design/#1495, not settled by this build. No UI wiring in this build (no `Design/portal` export exists yet for Security Plan) — this is the "build the endpoints" step of the fixed #1485 order of work; Design has not run.
