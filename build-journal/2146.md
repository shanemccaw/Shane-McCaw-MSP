# #2146 — cursor-stealing mention injector fix

- **Status:** ✅ DONE 2026-09-01T17:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 70e2bc1db (fix), b35e7afa9 (bookend IN FLIGHT)

## Log
- 2026-09-01T17:25:31Z ⏳ IN FLIGHT — audited `Services/IssueMentionInjector.cs` `decorateTextNode`'s ancestor skip-walk (line ~324-327); adding `el.isContentEditable === true` bail-out alongside the `SKIP_TAGS` check.
- 2026-09-01T17:40:00Z ✅ DONE — added `if (el.isContentEditable === true) return;` to `decorateTextNode`'s ancestor walk (70e2bc1db). Verified `dotnet build BuildConsole.csproj` succeeds clean (0 warnings/errors). Verified the actual injected-JS logic in a mock-DOM Node harness (scratch, deleted after use, not committed): a text node under a non-contenteditable ancestor is still decorated/`replaceChild`'d exactly as before (no transcript regression); a text node under a contenteditable ancestor is left completely untouched — `replaceChild` never fires, so the cursor-owning node survives. `verify-branch-merged.mjs` confirms HEAD is an ancestor of origin/main. Working tree clean.
