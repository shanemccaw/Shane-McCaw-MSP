# #1557 — SOPs/Runbooks: Recurrence is a schedule that spawns runs — not a row that gets wiped

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 3bfddb896

## Log
- 2026-08-30 ⏳ IN FLIGHT — Split `portal_runbooks` (the schedule) from a new
  `portal_runbook_runs` (one row per cycle) and re-key `portal_runbook_steps` off
  the run instead of the schedule, so a recurring cycle spawns a fresh run record
  with its own check-off state instead of overwriting the last one.
- 2026-08-30 ✅ DONE — Shipped exactly the settled architecture: `portal_runbooks`
  is the schedule (`recurring` boolean added), `portal_runbook_runs` is the new
  one-row-per-cycle table (cycleNumber, startedOn, status, completedAt,
  completedByUserId), and `portal_runbook_steps.runId` replaces `runbookId` as
  the required parent (`runbookId` relaxed to nullable, not dropped — additive).
  Migration `lib/db/migrations/manual/2026-08-30-runbook-cycles-1557.sql` run
  against local Postgres in-session (logged on #1630). New pure module
  `artifacts/api-server/src/lib/portal-runbook-cycles.ts` (isCycleComplete,
  cloneStepsForNextCycle, cycleProgress) — 12/12 unit tests passing, registered
  in vitest.config.ts. `portal-runbooks.ts` GET now returns each schedule's
  current cycle plus `runHistory`; PUT/POST step routes resolve the current run
  internally; ticking every step of a `recurring` schedule's cycle marks it
  complete (who, when) and spawns the next cycle with cloned, unchecked steps.
  `portal-oversharing-sites.ts` and `admin-testbed.ts` updated to create cycle 1
  alongside every runbook they seed (both still work — one-shot, non-recurring).
  Verified: schema shape via `\d` against live Postgres; a real end-to-end
  round-trip test against local Postgres (schedule → run 1 → all steps checked
  → run 1 marked complete → run 2 spawned with cloned unchecked steps → run 1's
  own steps untouched) — run once via a temporary vitest inclusion, then deleted,
  not left in the permanent suite; `tsc -b` clean for every file touched (two
  stale-shared-dist false negatives along the way, both resolved with
  `tsc -b --force` — the shared `lib/db/dist` junction raced with a concurrent
  session's own rebuild, not a defect in this change). `git status --porcelain`
  clean before this entry. `verify-branch-merged.mjs` confirmed `3bfddb896`
  merged into `origin/main`.
  Finding filed: #1940 (parented to #1488, board "AI Batter Up") — hold windows
  have no cycle affinity after this run model; out of scope for #1557 itself.
