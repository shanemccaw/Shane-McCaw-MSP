# #3119 — Critter roster (23 slots, peekers, room watermarks, daily roll) unported

- **Status:** ✅ DONE 2026-09-07T19:59:25Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** df1dee0a4 (Critter roster: 23 slots, peekers, room watermarks, daily roll), merged to main as a fast-forward

## Log
- 2026-09-07T19:57:44Z ⏳ IN FLIGHT — Read #3119's body + the correction comment (BuildConsole
  port explicitly out of scope, per Shane's 2026-09-07 comment). Read
  `web/shanes-life/Design/design_handoff_shanes_life/Shanes Life 14 - Critters.dc.html` and the
  handoff README's Critters section in full. Extracted the complete 23-slot roster (45 `c-*`
  symbols, Idle has 3 variants, the rest 2) + 6 wide `pkw-*` peekers programmatically from the
  .dc.html into `web/shanes-life/public/critters-sprite.svg` (XML-validated, 50 symbols + 26
  gradients, excludes `c-fox`/`gFox` since the sign-in fox is already live per #3117). Built
  `web/shanes-life/public/critters.js`: byte-exact port of the spec's own `fnv1a` hash and
  unpadded `Y-M-D` dateKey, the daily roll (`idx = fnv1a(dateKey+"|"+slotName) % n`), the peek
  roll (`b = fnv1a(dateKey+"|peek") % 6`, four consecutive peekers), and the alt-pair rule for a
  slot appearing twice on one screen. Added `serverDate` to `/api/me` (server-clock unpadded
  `Y-M-D`, matching the spec's own format) so the roll seeds from the server date, not the client
  clock, per the spec.
  **Real scope finding, recorded here rather than silently overreaching:** the live app
  (`public/index.html` / `public/app.js`) is still the early Today/Inbox/Things/Settings shell,
  not the room-based prototype ("Shanes Life - First Slice Prototype.dc.html") the critter spec
  was drawn against — there is no tray Later row, no Meds/Money/Dates/Pets/Lists/People/
  Vault/Wins/Budget Day/Heading Out room yet (those are their own Feature issues, e.g. #3088
  Shopping — Core list, still open). Building those rooms is out of scope for a critters issue
  and would duplicate/conflict with their real Feature issues. Wired the full, complete asset
  system into every real placement that exists today instead of stopping at an empty state:
  the "Next" tray label peeker (`attachPeeker`, first peek roll), the Today "Recent"/Inbox empty
  states (Idle slot, 1e), and the Things room's watermark + "Nothing filed yet" empty state
  (Things slot 1k watermark, Nothing-found slot 1w icon — the one room from the spec's room map
  that genuinely exists in this app). Every other slot (heading, comingup, dinner, home, shop,
  nasa, pets, meds, lists, people, vault, wins, budget, morning, bedtime, timer, builds,
  moneyhdr) and the Later/Meds/Rooms peekers ship complete and ready in critters.js/the sprite
  for their Feature issues to wire in the moment those screens land — nothing needed to be
  guessed or fabricated to do that; the roster metadata (id/name/variants) already matches the
  spec 1:1.
  **Verified live**, not just by inspection: started the real local dev server against the real
  local `finances` Postgres, minted a real single-use passkey enrolment link (`bin/enroll-
  passkey.mjs`) for the placeholder `you@example.com` account, drove it through Playwright with a
  CDP virtual WebAuthn authenticator (real enrolment + sign-in, not a mock), and confirmed in the
  live DOM + screenshots: the sprite injects (51 symbols incl. the existing `c-fox`), the Next
  peeker renders `pkw-bear` positioned per the spec's exact geometry, the Today empty state
  renders `c-idle`, and the Things view renders both its `c-things` watermark and `c-cat`
  not-found icon. Cross-checked all four rolls against an independent, from-scratch
  implementation of the spec's hash for today's real server date (`2026-9-7`) — all four matched
  exactly (idle→c-idle, peek b=4→pkw-bear, things→c-things, notfound→c-cat), confirming the roll
  is byte-exact to the design, not just visually plausible. Cleaned up: killed the dev server,
  deleted the local-only `.env` (gitignored, never committed), removed the two test passkey rows
  from the placeholder account, removed scratch verification scripts.
- 2026-09-07T19:59:25Z ✅ DONE — Committed the real work as df1dee0a4, rebased onto
  origin/main (clean, no conflicts), pushed as a fast-forward to main
  (`agent/3119-q1879:main`, `d51bbd9dd..df1dee0a4`), and confirmed with
  `verify-branch-merged.mjs`: "OK — merged into origin/main. Safe to write DONE."
  `git status --porcelain` is clean. No findings filed — this was a real, complete
  build of the spec'd asset system plus honest, real wiring, not a gap that needed a
  separate issue. The 18 not-yet-wired slots and 3 not-yet-wired peekers are not a
  gap in this issue's own scope; they're pre-built for the Feature issues that will
  build their host rooms (tracked on those issues already, e.g. #3088).
