# Build Journal — #4047

Audit log: real read surface (`/portal/audit*`, `/admin/audit*`).
Part of #1946 (Feature: Full Audit Log), Step 4 of 4.

- **Status:** ✅ DONE 2026-09-14T14:51:13Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 2c4f08e2a (real read surface — portal-audit.ts, audit-logs.ts upgrade, ActivityLog.tsx wiring)

## Log
- 2026-09-14T14:01:12Z ⏳ IN FLIGHT — Pulled #4047 body + its one comment (BUILD dispatch, Posted 2026-09-14T13:33:57Z). Dispatch says `--blocked-by 4044`, but #4044 is already DONE (commit 73099491a, bookend `build-journal/4044.md` ✅ DONE 2026-09-14T14:05:00Z, merged to origin/main — this worktree's HEAD is already an ancestor of origin/main and contains 73099491a) and labeled `complete` on GitHub even though the issue itself is still open (expected — only Shane closes issues). Confirmed unblocked per the standing "picking this back up" rule and cleared the now-stale `blocked_by` edge 4047→4044 via the GitHub dependencies API (`issue_dependencies_summary.total_blocked_by` now 0). Real parent for any findings filed mid-build is #1946 (Feature-tier), not the Epic — confirmed via `parent_issue_url` on #4047 itself.
  Starting investigation: read `docs/audit-log-model-4044.md` (the #4044 spec) for the actor-role/tenant_id/action_category shape, then survey existing `/portal/*` and `/admin/*` (or `/api/msp/*`) route conventions in `artifacts/api-server` for the pattern to follow, plus how tenant-scoping is enforced elsewhere (e.g. `users.tenant_id → tenants.id`, JWT `customerId` claim) so the cross-tenant boundary here is a real query-layer constraint, not a UI filter.
- 2026-09-14T14:51:13Z ✅ DONE — Shipped in commit 2c4f08e2a. **Investigation found:** `/api/audit-logs` (admin-panel, `requireAdmin`) already existed and reads the correct `audit_logs` table but pre-dated #4044 (no actorRole/actionCategory/tenantId filters); `/api/msp/audit` (msp-console) exists but reads a *different, older* table (`msp_audit_logs`) — filed as finding #4061 below, not fixed here (out of scope, a real pre-existing feature). No Design/portal `.dc.html` export exists for an Audit Log page — per CLAUDE.md's fixed #1485 order (architect → endpoints → contract pack → Design → wire), built the endpoint and deliberately deferred the customer-portal frontend page rather than inventing an undesigned one.

  **Shipped:**
  - `artifacts/api-server/src/routes/portal-audit.ts` (new) — `GET /api/portal/audit` (cursor-paginated, `requireCapability("ladder.customer-user")`) and `GET /api/portal/audit/catalogue`. Tenant boundary is hard-coded from `req.user.customerId`, never accepted from a query param — real query-layer enforcement, not a UI filter. Filters: `actionCategory`, `actorRole`, `entityType`, `before` cursor. Coverage caveat (`categorizedRows`/`uncategorizedRows`/`categorizedPercent`) computed live via `COUNT ... WHERE action_category IS [NOT] NULL`, scoped to the tenant's whole trail (not the filtered slice) — no fabricated "% of all real actions" claim, since no route/workflow manifest exists anywhere in the repo to honestly compute that against (confirmed by search).
  - `artifacts/api-server/src/routes/audit-logs.ts` (upgraded) — added `actorRole`/`actionCategory` (incl. `uncategorized`)/`tenantId` filters, `GET /audit-logs/catalogue` and `GET /audit-logs/tenants`, and the same real coverage stat (excluding the active `actionCategory` filter from its own denominator to avoid circularity). Cross-tenant visibility here is the existing `requireAdmin` gate itself — full visibility is the boundary policy, not a filter a customer could flip.
  - `artifacts/admin-panel/src/pages/ActivityLog.tsx` — wired the new Tenant/Action Category/Actor Role filters and a coverage banner showing the real categorized/uncategorized split plus the honest caveat text.
  - `artifacts/api-server/src/routes/index.ts` — mounted `portalAuditRouter`.

  **Verification:** `node ../../node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` clean in `artifacts/api-server` (exit 0, run twice); admin-panel `npx tsc --noEmit` clean (exit 0). Direct DB validation against local Postgres (`DATABASE_URL` extracted via `grep -E '^DATABASE_URL='` — never `source .env.local`, see live #4032 PEM-leak feedback memory): confirmed `audit_logs` currently has 363 rows, 0 with `tenant_id`/`action_category` set (all pre-#4044-migration writes, not backfilled — expected, matches the doc). Validated the exact query shape my endpoints use inside a `BEGIN...ROLLBACK` transaction: inserted a real probe row (`tenant_id=1`, `action_category='action'`), confirmed it was found by the tenant-scoped query and counted correctly into `categorized`, then rolled back — no data left behind.

  **Finding filed:** #4061 (parented under #1946, `bug` label, board=AI Batter Up) — MSP Console's own Audit Log module (`artifacts/msp-console`) reads `msp_audit_logs`, not the real platform `audit_logs` trail this build hardens; MSP operators using that app can't see it.

  Cleared the stale `blocked_by` edge 4047→4044 (see IN FLIGHT log line above) — never re-added since #4044 stayed genuinely complete throughout. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
