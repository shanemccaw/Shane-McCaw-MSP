# #2627 — Consent and Onboarding: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T04:24:12Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 0ac9011a2 (IN FLIGHT bookend), 1080a998f (real work, on `origin/main`)

## Log
- 2026-09-14T04:23:04Z ⏳ IN FLIGHT — read the design export (`Design/MSP_Console/design_handoff_msp_console/Consent and Onboarding.dc.html`, README screen 64), the contract pack (`docs/msp-console/consent-and-onboarding-msp-console-contract-pack.md`), the real `msp-consent.ts`/`msp-onboarding.ts` routes, and the existing `PolicyEngine.tsx`/`nav.ts`/`ConsoleShell.tsx` mount pattern. Implementing `api/consent-onboarding-api.ts` + `console/modules/ConsentOnboarding.tsx`, mounting under Operations at `/ops/consent`.
- 2026-09-14T04:24:12Z ✅ DONE — Added `artifacts/msp-console/src/api/consent-onboarding-api.ts` (all 7 real routes: consent list/detail, invite-link (graph), write-consent/start, sharepoint-consent/start, revoke, onboarding links list) and `artifacts/msp-console/src/console/modules/ConsentOnboarding.tsx` (two tabs: consent grants + onboarding links). Mounted under Operations at `/ops/consent` in `nav.ts`/`ConsoleShell.tsx`; updated `nav.test.ts`'s page-count assertions (17→18 ops pages). No fixture data — every row is a real server response; the empty states use the exact honest-empty copy the dispatch called out (list route drops tenants with three empty grants). **Diverged from the design export's copy on one point, deliberately**: the design (frozen 2026-09-04) says there's no MSP-scoped route to start writeBack/sharepoint consent and shows those as disabled — that's stale. `msp-consent.ts`'s `write-consent/start` and `sharepoint-consent/start` routes now exist and their own header comments name #2818 as the issue they close, confirmed live reading the route file on `main`. Wired both as real, working mint actions instead of reproducing the now-incorrect disabled state (same "design predates a real backend fix" pattern `console/modules/Dlq.tsx`'s own header documents). Also `adminEmail`/`adminDisplayName` — the design's "not on this wire" placeholder — are genuinely on the wire now (`consentRow()` in `consent.ts` projects both), confirmed against a live query of `tenants.consent` in local Postgres; rendered as real "APPROVED BY" values. Verified: `tsc --noEmit` clean, `tsx --test` 16/16 passing, `vite build` succeeds, confirmed real `tenants.consent` jsonb shape and empty `msp_onboarding_links` table against local Postgres directly. No manual SQL migration — no schema change, existing routes/columns only. Test-manifest rule does not apply (scope is `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting` only; msp-console is Shane's internal tool). No genuine new finding beyond the two already-tracked/resolved #2818 items. `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/2627-q2689` merged into `origin/main` (`1080a998f`).
