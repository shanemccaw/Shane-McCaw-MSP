# #2006 — Secondary-account builds silently fall back to Primary when secondary config is bad

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** b550d82b2 (fix), 0ed20a7b0 (IN FLIGHT bookend), merged to origin/main

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — pulled issue body + comments; latest comment (2026-09-12) narrows scope to: fail closed on secondary launch when `SecondaryClaudeConfigDir` is blank/missing/has no valid credentials — mark the queue row failed with a clear reason instead of silently launching on primary. Located the real logic at `QueueWatcherService.cs` around line 2404 (not 1358-1376 as the issue predates a line shift). Implementing.
- 2026-09-12T00:00:00Z ✅ DONE — `QueueWatcherService.cs`'s secondary-account launch path now validates SecondaryClaudeConfigDir non-blank + Directory.Exists + real signed-in credentials (via `ClaudeUsageMeterService.ReadAccessToken`, reused not reimplemented) before ever setting CLAUDE_CONFIG_DIR; any failure calls `MarkLaunchFailedAsync` with a specific reason and aborts, never falling through to primary. Commit b550d82b2, pushed and confirmed merged into origin/main (`verify-branch-merged.mjs`). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 errors. Descoped items (signed-out-primary check, Settings-tab live validation) filed as #3798, parented to Feature #3715, board status AI Batter Up. Comment posted on #2006; labels flipped in-flight→complete. Working tree clean.
