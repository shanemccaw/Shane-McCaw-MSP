# #3036 — Execute #3023's proposed branch cleanup (confirmed-safe categories only)

- **Status:** ✅ DONE 2026-09-06T19:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** (bookend commits, this file only — the real "work" was 96 real
  `git push origin --delete` operations, not product code)

## Log
- 2026-09-06T19:31:00Z ⏳ IN FLIGHT — re-deriving live per-branch categorization (merge-base diff,
  byte-identity vs origin/main, build-journal record on main, prodbase/last-hour exclusions) per
  #3023's methodology, then deleting only category 1 (bookend-only + real record on main) and
  category 3 (real code, byte-identical to main) branches. Categories 2 and 4, and any
  `*-prodbase-*`-style branch, are explicitly out of scope for this issue.
- 2026-09-06T19:55:00Z ✅ DONE — Re-ran `check-stranded-branches.mjs --json` live (239 stranded of
  256 inspected at start of this pass — real time had passed since #3023's snapshot; two more
  branches had already landed a bookend-only tail commit and one had resolved to clean in the
  interim). For every stranded branch, live-computed `git merge-base origin/main <branch>`, diffed
  the branch's own touched files against that merge-base, checked byte-identity against current
  `origin/main`, and checked for a real `build-journal/<id>.md` record on `main`. Excluded 5
  branches with a commit inside the last hour, 2 `*-prodbase-*` branches (#3025's own finding),
  and 3 branches whose main `build-journal/<id>.md` record was still literally
  `IN FLIGHT`/`BLOCKED` text (a stricter-than-required extra safety check beyond what #3036's body
  asked for — all 3 underlying GitHub issues, #2092/#2385/#2863, turned out to already be CLOSED,
  but "when in doubt, leave it alone" per the issue's own instruction, so left untouched).
  **Category 3 (real code, byte-identical to main) came back 0 today** — legitimately, not a bug:
  main has moved on enough since #3023's snapshot (~2h prior) that shared files the category-3
  candidates touched (e.g. `artifacts/api-server/src/routes/index.ts`) no longer match byte-for-
  byte, which is the expected, correct outcome of re-deriving live rather than trusting cached
  data. **Category 1 (bookend-only, real record on main) came back 96** (99 raw matches minus the
  3 excluded above), all with the branch's *entire* diff from its own merge-base under
  `build-journal/` and a resolved (non-IN-FLIGHT/BLOCKED) record already on `main`.
  Re-verified every one of the 96 immediately before deleting (fresh fetch, re-checked
  last-hour/bookend-only/main-record conditions live at delete time), then executed real
  `git push origin --delete` for all 96 (one individually + 5 batches). Confirmed post-hoc: all
  96 targeted branch names return `fatal: Needed a single revision` for
  `origin/<branch>` (i.e. genuinely gone). Re-ran the sweep after: 141 stranded of 164 inspected
  (down from 239/256 — the drop is a bit larger than 96 alone because other concurrent sessions
  were landing/merging work throughout this pass, both adding and resolving branches). Zero local-
  only (unpushed) branches were touched — the local-only stranded set (`agent/1402-q655`, the 2
  prodbase branches, and 3 very-recent `agent/301[7-9]-*` branches) all independently sorted into
  an excluded category on their own. **Real finding filed:** #3060 (sibling of #1789) — a
  confirmed case (`origin/agent/2863-q1623` / issue #2863) where a session's own DONE bookend
  commit claimed `verify-branch-merged` passed, but that commit never actually became an ancestor
  of `origin/main` even though the real code fix did land (via the mergeback recommit path);
  `build-journal/2863.md` on `main` is still permanently stuck reporting `IN FLIGHT`. Labeled
  `bug`, milestone v1.1, board status set to AI Batter Up. No fixture/hardcoded data — all counts
  in this log came from live `git`/`gh` queries run during this session. Working tree clean
  (verified `git status --porcelain` after restoring one accidental `rm -rf` of a pre-existing
  tracked `.scratch/` dir via `git checkout -- .scratch/`, from before this session — see
  completion comment). Branch verified merged into `main` via `verify-branch-merged.mjs` after
  rebase onto current `origin/main` and push.
