# #2691 — DOM injector: color mentioned issue numbers by real live build state

- **Status:** ✅ DONE 2026-09-03T17:13:35Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 52ff74e3889fd0dc87b3b5f3c4027dea6e5f1149

## Log
- 2026-09-03T17:11:21Z ⏳ IN FLIGHT — implementing the 3-point real fix from the issue audit:
  1. `ChatMentionPopupHelper.BuildSetMentionColorsScript` now also resolves each number's
     live build via `LeftSidebar.FindAssociatedBuild` (same lookup the hover tip already
     uses) and includes `liveStatus` in the eager color payload.
  2. `IssueMentionInjector.cs` JS gets two new color constants (in-flight
     running/verifying = green `#A6E3A1`, Batter-Up-waiting/queued = amber `#FAB387`) and
     `colorForMentionInfo`'s priority chain becomes blocked > in-flight > batter-up-waiting
     > isEpic > closed > default.
  3. `BuildQueuePanel` gets a new `QueueRefreshed` event fired at the end of the existing
     ~15s `RefreshAsync` poll tick (no new polling loop). MainWindow subscribes and
     re-pushes mention colors for every number tracked in a new
     `Services.LiveMentionNumberRegistry` (accumulates across the delta-only
     `BT_ISSUE_MENTIONS_SCAN` reports) into every open chat surface — ClaudeWebView,
     embedded chat tabs, and the floating chat window's own tabs.
- 2026-09-03T17:13:35Z ✅ DONE — `dotnet build BuildConsole.csproj -c Debug` succeeds
  clean (0 warnings, 0 errors), both before and after rebasing onto origin/main.
  Priority chain chosen (documented in code): blocked > in-flight (running/verifying,
  green `#A6E3A1`) > Batter-Up-waiting/queued (amber `#FAB387`) > isEpic > closed >
  default — an in-flight build is as operationally urgent as blocked since it's
  happening right now; a Batter-Up-waiting item is informational so it sits below
  in-flight but still ahead of epic/closed. Not live-clicked through WebView2 (no
  BuildConsole.exe launch per the standing rule) — verified by reading the actual
  data flow end to end: `BuildSetMentionColorsScript` → `FindAssociatedBuild` →
  `IssueQuickActionResolver.ClassifyLiveStatus` → JSON payload → JS
  `colorForMentionInfo`, and the hover round-trip's `ChatMentionBuild.LiveStatus`
  carrying the same classification through `window.__btShowIssueTip`. Branch rebased
  onto and pushed to `origin/main` as commit `52ff74e3889fd0dc87b3b5f3c4027dea6e5f1149`;
  `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean. No new
  findings filed — no bug/gap discovered outside the issue's own scope.
