# #2543 — Feature rollup pill shows real sub-issue count but tree renders zero children

- **Status:** ✅ DONE 2026-09-03T14:32:46Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** f8683f147 (fix, on main) — GitHubApiClient.cs transitive milestone inheritance

## Log
- 2026-09-03T14:21:33Z ⏳ IN FLIGHT — Primary worktree C:\wt\2543-q1378 arrived EMPTY (no .git, #2118); recovered via `git worktree add` + manual register/link-deps. Beginning live trace of BuildBoardFromGitHub and the open-issues fetch feeding _lastBoardIssues to find why #2263's real 19 sub-issues don't render in the tree.
- 2026-09-03T14:32:46Z ✅ DONE — Root cause found & fixed. NOT pagination (533 open < 10k cap) and NOT a board-Status filter (the fetch is pure `issues(states:OPEN)`). Real cause: milestone inheritance in `ListBoardIssuesInternalAsync` (GitHubApiClient.cs step 3) was a single, CREATED_AT-DESC-ordered, one-level pass. #2263 (own milestone null, parent Epic #1202 = v1.1) inherits v1.1; but its 18 open children (#2264-#2280, #2412) are visited BEFORE #2263 in DESC order, see it still milestone-less, never inherit, and stay in the synthetic "No Milestone" group. Under Focus Mode (v1.1 only) that group is hard-hidden, so the children drop out of the render's `allKnownIssues` and #2263 shows its real 1/19 pill with ZERO nested children. Fix: added step 4 — transitive milestone inheritance that climbs each milestone-less issue's real ParentNumber chain to the nearest ancestor with a milestone, order-independent. Verified against 530 REAL live open issues (replicated steps 3+4 exactly): pre-fix all 18 open children = "No Milestone"; post-fix all 18 = "v1.1", same group as #2263 → they now nest under it and render under Focus, matching the pill. `dotnet build BuildConsole.csproj -c Release` clean (0 warn/0 err). Branch verified merged into origin/main (verify-branch-merged.mjs OK). No findings filed (no new bug discovered beyond the one fixed). BuildConsole.exe deliberately NOT launched for verification per project rule; verification was against live GitHub data + clean build.
