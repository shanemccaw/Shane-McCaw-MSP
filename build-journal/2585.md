# #2585 — Runbooks: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:12:01Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** f14187074 (rebased/pushed to main as 0d5db8a6b)

## Log
- 2026-09-12T03:04:59Z ⏳ IN FLIGHT — Blocker #3677 (MSP Console Shell) confirmed merged to
  main (7888bf1d6) and an ancestor of this worktree's HEAD. Investigation done: read #2585
  body/comments, #1683 (Feature: Runbooks MSP Console — explicitly **NOT ARCHITECTED** for
  runbook-definition authoring / step reordering / run-outcome recording), #2669 (the backend
  build, closed — built only reads + step-toggle + hold-extend + hold-events, deliberately not
  the rest), `msp-runbooks.ts`, `portal-runbook-wire.ts`, `portal-hold-windows.ts`, the Runbooks
  design (`Design/MSP_Console/design_handoff_msp_console/Runbooks.dc.html` + README screen 15 —
  whose own `honestNote` independently confirms the same scope split: "Ticking a step and
  extending a wait are the two things this console can do to a live runbook... Writing a
  runbook, reordering its steps and recording a cycle outcome are not built"), the MSP Console
  shell (`ConsoleShell`/`ScreenSlot`/`nav.ts`/`tokens.ts`/`console-api.ts` conventions), and the
  customer-facing portal `runbooks.tsx` / `useRunbooks.ts` for field/tone conventions to mirror.
  **Decision:** wiring the two dispatch-named actions that are real and architected (mark a step
  complete, extend a hold window) plus the real reads (runbook list + run history + hold events)
  — NOT inventing runbook-definition authoring, step reordering, or run-outcome recording, since
  that's a direct conflict with #1683's own recorded NOT-ARCHITECTED decision, not a missing
  column. About to build `runbooks-api.ts` + `RunbooksPage.tsx` and wire into `ConsoleShell`.
- 2026-09-12T03:12:01Z ✅ DONE — Shipped `artifacts/msp-console/src/api/runbooks-api.ts` (real
  query + mutation hooks against the four `/api/msp/runbooks*` routes) and
  `artifacts/msp-console/src/pages/runbooks/RunbooksPage.tsx` (Runbooks/Waiting-on tabs, runbook
  cards, hold cards, step-detail/extend/history drawers), wired into `ConsoleShell.tsx` via a new
  `moduleFor(selection)` that mounts it into the screen slot for tenant page id `"run"`. Extended
  `icons.tsx` with the additional Lucide glyphs the design uses (pause, calendar-plus, x, square,
  square-check-big, circle-check-big, circle-minus, clock-alert, fast-forward). Real data states:
  Loading (skeleton), 403 (customer not in this MSP's book — `customerBelongsToMsp`), 404/generic
  error, Empty (no runbooks recorded for the tenant), Ready. No fixture/hardcoded data — every
  field renders from the live payload; tone/status-label vocabulary mirrors
  `artifacts/portal/src/pages/runbooks.tsx`'s own `STATUS_TONE` so an operator sees the same
  colour language the customer does. Commit `f14187074`, rebased and pushed to `main` as
  `0d5db8a6b`; `verify-branch-merged.mjs` confirms ancestor of `origin/main`. Verified: `tsc
  --noEmit` clean; production `vite build` clean (1756 modules, 105 kB gzip). No manifest — this
  is `artifacts/msp-console`, an internal MSP operator tool, not `artifacts/msp-portal` or
  `artifacts/shane-mccaw-consulting`, so the CLAUDE.md test-manifest scope does not apply. No
  headless UI run: the console's auth gate needs a real logged-in MSP operator session, same
  reasoning #3677's own bookend gave. Working tree clean at finish (`git status --porcelain`
  empty). No findings filed — the one real gap (runbook authoring/step-reorder/outcome-recording
  not being architected) is already recorded on #1683 itself, not a new discovery.

  **Scope note, stated plainly:** the dispatch prompt named five actions ("runbook definitions,
  step order, marking a step complete, extending a hold window, recording run outcome"). Three of
  those — authoring a runbook definition, reordering steps, recording a run outcome — have no
  backend endpoint, and #1683 (the Feature this build is under) is explicitly marked **NOT
  ARCHITECTED** for that full surface; the design's own `honestNote` in `Runbooks.dc.html`
  independently states the same split. This is a direct conflict between the dispatch prompt and
  a decision already recorded on the governing issue, not a missing column — so per CLAUDE.md's
  own carve-out, it was not built. Only the two real, backed actions (step-toggle, hold-extend)
  plus the real reads are wired.
