# #2585 — Runbooks: wire the MSP Console UI to the real endpoints

- **Status:** ⏳ IN FLIGHT 2026-09-12T03:04:59Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T03:04:59Z ⏳ IN FLIGHT — Blocker #3677 (MSP Console Shell) confirmed merged to
  main (7888bf1d6) and an ancestor of this worktree's HEAD. Investigation done: read #2585
  body/comments, #1683 (Feature: Runbooks MSP Console — explicitly **NOT ARCHITECTED** for
  runbook-definition authoring / step reordering / run-outcome recording), #2669 (the backend
  build, closed — built only reads + step-toggle + hold-extend + hold-events, deliberately not
  the rest), `msp-runbooks.ts`, `portal-runbook-wire.ts`, `portal-hold-windows.ts`, the Runbooks
  design (`Design/MSP_Console/design_handoff_msp_console/Runbooks.dc.html` + README screen 15 —
  whose own `honestNote` independently confirms the same scope split: "Ticking a step and
  extending a wait are the two things this console can do to a live runbook... Writing a
  runbook, reordering its steps and recording a cycle outcome are not built"), the MSP Console
  shell (`ConsoleShell`/`ScreenSlot`/`nav.ts`/`tokens.ts`/`console-api.ts` conventions), and the
  customer-facing portal `runbooks.tsx` / `useRunbooks.ts` for field/tone conventions to mirror.
  **Decision:** wiring the two dispatch-named actions that are real and architected (mark a step
  complete, extend a hold window) plus the real reads (runbook list + run history + hold events)
  — NOT inventing runbook-definition authoring, step reordering, or run-outcome recording, since
  that's a direct conflict with #1683's own recorded NOT-ARCHITECTED decision, not a missing
  column. About to build `runbooks-api.ts` + `RunbooksPage.tsx` and wire into `ConsoleShell`.
