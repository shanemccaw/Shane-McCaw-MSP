# #1205 — Build-set completion false limitation: "no way to start dev server"

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** a50602a91bfe2194247cac9537aedd338bb250ee (rebased onto main as b040b22d5e7bf94701f1d912e183a0daf0b90028)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Read DevServicesManager.cs (StartServiceAsync/StartAllServicesAsync/
  IsPortOpenAsync/GetServiceStatusAsync) and the real build-set completion path
  (QueueWatcherService.cs → `buildset.mjs close` → coordinator.mjs `maybeFireSetRestart` →
  `refresh-main-server.mjs`'s `refreshMainServer`, the actual restart action wired in since
  Git #1395). Found the real false limitation: `refreshMainServer` receives `only`
  (== `plan.neededRunning`, the exact list of services selective-targeting says must be
  running) but only ever acts on it to decide whether to bounce `api-server` — every
  front-end service in that list is silently dropped on the floor. The code's own comment
  assumes front-ends "self-reload via vite HMR", which is only true if they're ALREADY
  running; a front-end that's genuinely down never gets started, mirroring the exact
  false-limitation shape DevServicesManager.cs already solves via StartServiceAsync +
  IsPortOpenAsync for the manual UI Start buttons. About to fix `refresh-main-server.mjs`
  to actually start any needed-but-down front-end via the same `dev-all.mjs --start
  <service>` call DevServicesManager.cs uses, confirmed via a port-open readiness check.
- 2026-08-28 ✅ DONE — Fixed `scripts/dev-server/refresh-main-server.mjs`: `refreshMainServer`
  now walks the needed-service list (`only` == `plan.neededRunning`, or every configured
  front-end on the safe-fallback full restart) via a new `ensureFrontendsRunning()` and
  starts whichever front-ends are genuinely down — same `dev-all.mjs --start <service>`
  call DevServicesManager.StartServiceAsync uses, confirmed via a real port check
  (`waitForPortReady`, generalized from the existing api-only `waitForApiReady`) instead
  of the previous silent no-op. Verified live end-to-end against the real local dev
  server with `node scripts/dev-server/refresh-main-server.mjs --dry-run`: an
  already-running service (`msp-portal`) correctly reports `alreadyRunning:true`
  (no-op), and a genuinely-down one (`msp-website`) correctly resolves to
  `wouldStart:true` instead of being silently dropped as before. `node --check` passes.
  `node scripts/dev-server/selftest.mjs` run both before and after the fix: 3 of the
  same concurrency-scenario assertions (Scenarios 3/9, real Windows `git index.lock`
  contention under parallel CLI invocations) fail identically on unmodified baseline
  code too — confirmed pre-existing and unrelated (those scenarios exercise the
  `fakeRestart` path, which never touches `refresh-main-server.mjs`). Rebased onto
  origin/main and pushed directly (`a50602a9` → `b040b22d` on main);
  `verify-branch-merged.mjs` confirms `b040b22d` is a real ancestor of `origin/main`.
