# #1205 — Build-set completion false limitation: "no way to start dev server"

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Read DevServicesManager.cs (StartServiceAsync/StartAllServicesAsync/
  IsPortOpenAsync/GetServiceStatusAsync) and the real build-set completion path
  (QueueWatcherService.cs → `buildset.mjs close` → coordinator.mjs `maybeFireSetRestart` →
  `refresh-main-server.mjs`'s `refreshMainServer`, the actual restart action wired in since
  Git #1395). Found the real false limitation: `refreshMainServer` receives `only`
  (== `plan.neededRunning`, the exact list of services selective-targeting says must be
  running) but only ever acts on it to decide whether to bounce `api-server` — every
  front-end service in that list is silently dropped on the floor. The code's own comment
  assumes front-ends "self-reload via vite HMR", which is only true if they're ALREADY
  running; a front-end that's genuinely down never gets started, mirroring the exact
  false-limitation shape DevServicesManager.cs already solves via StartServiceAsync +
  IsPortOpenAsync for the manual UI Start buttons. About to fix `refresh-main-server.mjs`
  to actually start any needed-but-down front-end via the same `dev-all.mjs --start
  <service>` call DevServicesManager.cs uses, confirmed via a port-open readiness check.
