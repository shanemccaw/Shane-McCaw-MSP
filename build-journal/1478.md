# #1478 — Health page shows unchanged numbers after #1471's purge + full service restart

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 7ecad21ec (IN FLIGHT bookend; investigation-only session, no product code changed)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Reordered per Shane's latest comment: first confirm which
  Postgres instance the currently-running local dev app server actually connects to,
  then live-query tenant_monitor_profiles/checkKeys against that exact instance before
  touching the caching-layer theory.
- 2026-08-28 ✅ DONE — Investigation only, no code changed. Confirmed the running local
  dev app server (api-server PID 38100, port 8080, launched via dev-all.mjs) uses
  `postgresql://postgres:...@localhost:5432/shanemccawmsp` (verified in both candidate
  `.env.local` files, no env-var override at User/Machine level, no dotenv usage in
  api-server src — DATABASE_URL flows straight from dev-all.mjs's env load into the
  spawned process). Live-queried that exact instance: `tenant_monitor_profiles` has
  ZERO rows for tenant_id `c4c814d4-3afe-441e-9145-62461d0a4fd3` (tenants.id=1,
  mccawsoft2.onmicrosoft.com, is_testbed=true) across all 5 checkKeys named in the
  issue — in fact zero rows for that tenant_id at all; only two unrelated tenant_ids
  ("Test Me" and an e2e fixture GUID) have any rows. #1471's purge genuinely stuck
  locally. Root cause of "unchanged numbers" is neither a failed purge nor a
  traditional cache: Shane's screenshot numbers (9, 14, 18, 6, 23, 1) are an exact
  digit-for-digit match to the hardcoded `HLT_OBJECTS` fixture array in
  `artifacts/msp-portal/src/components/portal-v2/hltDashboardData.ts` (lines 128-139).
  4 of the 5 checkKeys the issue names were never wired to this card's live data at
  all (documented in that file's own comment above `hltObjectsWithLive`, Git #1340
  follow-up) — permanently fixture regardless of DB state. The 5th
  (`devices:stale-duplicate-records`) IS wired, but `dashboard-resolvers.ts`'s
  `latestCheckProps` returns null on zero rows, and the frontend's `!= null` overlay
  guard (`hltObjectsWithLive`) silently keeps the stale fixture number instead of an
  honest zero/no-data state — a real HARD RULE gap, flagged on the issue for Shane to
  decide rather than reworked here (shared by 2 other "WithLive" hooks reviewed under
  #1233/#1252). Full writeup posted as an issue comment.
