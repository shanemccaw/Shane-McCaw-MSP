# #1605 — Webhooks: wire the mutating buttons — rotate secret, edit, delete

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 4548f9fcd (merged to main; also f92b1f6e6 wiring, 1cf075752 test coverage on the agent branch)

## Log
- 2026-08-29 ⏳ IN FLIGHT — read #1605's body + docs/webhooks-contract-pack.md in full.
  Wiring Rotate secret, Edit endpoint/subscription, and Delete on
  `portal-v2-webhooks.tsx` against their real, already-live backend endpoints
  (`webhooks.ts:251-306` PATCH, `310-336` DELETE, `340-377` rotate-secret), with
  real confirmation/consequence copy for the destructive actions and honest
  error surfacing from each route's real error shape.
- 2026-08-29 ✅ DONE — Rotate secret, Edit endpoint, Edit subscription, and
  Delete are all wired to their real routes via a new `webhooksMutations.ts`
  hook, `RotateSecretPanel.tsx` and `DeleteWebhookPanel.tsx` (real consequence
  copy, real error surfacing), and `FormDrawer` for the two edit flows. All
  three are disabled whenever the page is on the design fixture
  (`dataState !== "live"`) so a click can never target a fixture id. Typecheck
  clean for every touched file (2 pre-existing unrelated errors elsewhere in
  the workspace, untouched by this session). Test manifest updated
  (`test-manifests/portal/account-settings.json`, tag
  `webhook-mutations-1605`) covering all three flows against the real
  Git #1249-seeded webhook row; Rotate is asserted end-to-end for real, Edit
  and Delete are asserted to open with the real endpoint's real data and then
  deliberately cancelled rather than committed, since committing either would
  permanently corrupt the one shared seeded webhook row every other tagged
  block in that same manifest file (WH5/WH8/SETUP-WH) depends on, with no
  re-seed step to undo it. Could NOT execute the tagged run this session via
  `shaneapp://runTest`: it resolves manifests against the shared dev-server
  checkout, and merging this session's commit into that checkout hit real,
  unrelated merge conflicts from a concurrent "Portal" build set
  (`build-journal/1430.md`, `1415.md`, `portal-v2-security-evidence.tsx`,
  `test-manifests/portal/security-drilldowns.json`) — retrying was not worth
  the risk of corrupting another session's in-flight work, so the conflicted
  merge was left auto-aborted (confirmed clean, no `MERGE_HEAD`, no stray
  conflict markers in `C:\dev-server`) and the manifest was verified with
  `node -e "JSON.parse(...)"` instead. Merged to `main` at `4548f9fcd`, verified
  via `verify-branch-merged.mjs` (OK, safe to write DONE). No schema change,
  no new logging channel invented (`notification`, already in the locked
  taxonomy, used for the new `reportClientEvent` failure beacons in
  `webhooksMutations.ts`). Left deliberately out of scope, flagged for a
  follow-up issue if Shane wants them: Pause/Resume (same PATCH `isActive`
  this pass already wires for Edit endpoint, just no dedicated button wiring),
  Reveal and Replay (both real, documented backend gaps per the contract pack,
  not a wiring gap in this page), and the page's pre-existing "no explicit
  empty state when a tenant has zero webhooks" gap (contract pack §5,
  unrelated to this issue's three buttons).
