# #1595 — Portal New Design: Account Security (contract extraction pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 427bec168, 1ef3566fe (rebased onto main as 83878869f)


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1595`, but #1595 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1595 is complete.
>
> #1595's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-28 ⏳ IN FLIGHT — READ-ONLY session. Pulling #1595 (+ #1485, #1577) and
  producing the contract extraction pack for Account Security per #1577's method:
  wire contract cited to file:line, CURRENT/DECIDED fields, real enum unions,
  honest-empty contract, the unbacked-field forbidden list (#1593), sessions-revoke
  write-capability determination, and Security-pillar overlap flag. Output is an
  `.md` for Claude Design under `Design/contract-packs/`. No code changes planned.
- 2026-08-28 ✅ DONE — Wrote `Design/portal/docs/account-security-contract-pack.md`,
  extracted verbatim from `useAccountSecurityLive.ts`, `mfa.ts`, `auth.ts`,
  `session-tracking.ts`, `accountSecurityData.ts`/`accountSecurityModel.ts`, the
  page itself, and the `user_sessions`/`mfa_enrollments` Drizzle schema — every
  field cited to file:line. Confirmed sessions revoke (single + "sign out
  everywhere else") is a real, already-wired write path
  (`DELETE /api/auth/sessions/:id`, `POST /api/auth/sessions/revoke-others`), no
  CR needed under #1486. Confirmed the honest-empty `dataState` null-check pattern
  #1463 found (`mfa !== null && sessions !== null`) and documented it for Design to
  preserve. Lifted the #1593 forbidden list (password age, failed attempts, device
  compliance) out of the hook's header comment into the pack. Flagged (not
  resolved) the MFA-signal overlap with the Security pillar dashboard's tenant-wide
  "MFA Coverage" card — different source, different scope, same label. No code
  changes outside `build-journal/1595.md` and the new contract-pack file. Verified
  `agent/1595-q775` merged into `origin/main` via `verify-branch-merged.mjs`
  (`83878869f`) before writing DONE.
