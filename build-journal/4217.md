# Build Journal — Issue #4217

- **Status:** ✅ DONE 2026-09-15T05:35:00Z
- **Scope:** buildconsole
- **Log:**
  - 2026-09-15T04:30:00Z — Started. Adding `WhiteBrush` (Themes/DarkTheme.xaml) and `ScrimMediumBrush` (Themes/Colors.xaml) to cover the 3 raw hex colors named in #4217, then wiring ScreenshotAnnotationWindow.xaml (2x `#FFFFFF`) and RegionSelectOverlayWindow.xaml (1x `#80000000`) to reference them.
  - 2026-09-15T05:35:00Z — ✅ DONE — Added `WhiteBrush` (`Color="#FFFFFF"`, Themes/DarkTheme.xaml) and `ScrimMediumBrush` (`Color="#80000000"`, byte-identical to the raw literal it replaces, Themes/Colors.xaml). Wired `ScreenshotAnnotationWindow.xaml`'s White ink-swatch `Background` and its `BorderBrush` to `WhiteBrush`, and `RegionSelectOverlayWindow.xaml`'s drag-hint chip `Background` to `ScrimMediumBrush`. Commit `70fadfebc` (rebased/merged into `main` as `009c311a2`). `dotnet build BuildConsole.csproj` currently fails on a pre-existing, unrelated XAML tag-mismatch in `Controls/GitModeContainer.xaml` (confirmed untouched by this diff) — filed as #4237, parented under #1202, board status "AI Batter Up". Verified the 4 touched XAML files parse as well-formed XML (PowerShell `[xml]` parse, all OK) since the full project build is blocked by #4237. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. Working tree clean.
