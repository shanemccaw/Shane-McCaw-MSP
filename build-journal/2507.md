# #2507 — Live-rendered document PDF export navigates to a dead route

- **Status:** 🛑 BLOCKED 2026-09-04T06:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** none — no code change was needed/warranted (see Log)

## Log
- 2026-09-04T02:55:00Z ⏳ IN FLIGHT — investigating `portal-documents.ts` / `portal-url.ts` / `insight-pdf.ts` live-document PDF export path and what it would take to give it a real destination route in `artifacts/portal`.
- 2026-09-04T06:05:00Z 🛑 BLOCKED — confirmed the PDF export code (`portal-documents.ts`, `insight-pdf.ts`, `portal-url.ts`) is already correct; the actual defect is that its destination route (a live "Document Viewer" page for the 7 `LIVE_RENDERED_DOC_TYPES`) does not exist anywhere in `artifacts/portal`, and no `.dc.html` design export exists for it either (`Design/portal/` has only `design_handoff_ui_shell`). Rejected the obvious workaround — a second server-side HTML renderer reusing `htmlToPdf()` — because `insight-pdf.ts:230-243`'s own doc comment records that the live-navigate approach was chosen specifically to avoid that exact drift risk; building it anyway would be a regression of documented intent, not a safe smaller fix. Filed **#2825** (sibling of this issue's own Feature parent, #1658) with full evidence and the real next steps (endpoints → contract pack → Design export → wire). Wired a real `blocked_by` dependency #2507→#2825, relabeled #2507 `blocked` (removed `in-flight`), set #2825's board status to AI Batter Up, and posted the full writeup as a comment on #2507. No code changed — none was warranted without the missing route/design existing first.
