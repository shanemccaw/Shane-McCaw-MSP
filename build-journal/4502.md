# #4502 — check-drift red on origin/main: two commits changed schema/index.ts with no migration

- **Status:** ✅ DONE 2026-09-17T13:44:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 307ccbea6 (rebased onto origin/main as 0816c464b)

## Log
- 2026-09-17T13:31:00Z ⏳ IN FLIGHT — Independently re-verified a41e8e7f9 and b9449cb0c against the real local DB schema: `free_scan_engagements.selected_addons` is `jsonb` regardless of the commit's `$type<>` narrowing, and `WfNode.type` is a pure TS interface narrowing over a `jsonb` column with no DB-level enum/check constraint. Both confirmed genuinely $type-only, no real DDL divergence. Proceeding to run `scripts/src/update-schema-hash.ts` and verify `check-drift` goes green.
- 2026-09-17T13:44:00Z ✅ DONE — Ran `pnpm --filter @workspace/scripts run update-schema-hash`, which updated `lib/db/drizzle/schema-hash.txt` to `c7fa...ac9e0f1` — the exact "Current" hash check-drift had already computed and was comparing against. `check-drift` now reports "✅ No drift — schema, journal, and SQL files are all in sync." both immediately after the change and again after final push. Committed as 0816c464b on origin/main, `verify-branch-merged.mjs` confirms ancestry, working tree clean.
