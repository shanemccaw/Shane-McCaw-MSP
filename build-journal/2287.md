# #2287 — "Tab" button sends the matrix to its own tab

- **Status:** ✅ DONE 2026-09-02T20:52:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 8bce72cec (rebased/pushed as 9f12a35d on main) — "Tab" button sends the Build
  Matrix to its own tab. Superseded an earlier commit that duplicated #2286's drawer; that
  duplicate was reset out before push (see log below), never landed on main.

## Log
- 2026-09-02T19:43:00Z ⏳ IN FLIGHT — Real audit: grepped ShaneBuilder for "Matrix"/"BuildMatrix",
  confirmed the whole Build Matrix Feature (#2281, items 1-7 = #2282-#2288) is unbuilt. Siblings
  #2286/#2285 are also "running" concurrently per bt_build_queue right now, and #2282-#2284/#2288
  are still queued — none of items 1-5 (the drawer itself) exist yet for this issue's "Tab" button
  to send. Found the real, just-merged precedent for this exact mechanism: Git #2365 (Batter Up
  panel "Expand full page here" / "Send to tab", itself reusing Git #2312's Git Panel peek split).
  Building a minimal, real Build Matrix drawer (toolbar chip, 8 real slots off live `_queueItems`,
  count summary, click-to-focus) plus the "Tab" button/doc surface using the same frozen-snapshot
  pattern as #2365, so the Tab feature has real content to send rather than an empty stub. No
  fixture data — slots are read from the same real `_queueItems` (bt_build_queue) already backing
  the Build Queue panel.
- 2026-09-02T20:40:00Z ⏳ IN FLIGHT — First pass built a self-contained minimal drawer (chip, 8
  slots, count summary, click-to-focus) plus the Tab button, since none of #2282-#2286 existed at
  investigation time. On rebase to pick up concurrent `main` work, found #2286 ("Build Matrix:
  8-slot drawer + click-to-focus") had landed mid-session with items 1-5 for real — its own commit
  explicitly scoped Tab-to-own-tab out as "#2287, this issue." Reset hard to origin/main, discarded
  the duplicate drawer, and rebuilt this issue's real scope only: a "Tab" button wired into #2286's
  actual `MatrixDrawer`/`_matrixSlotAssignments` state, plus the send-to-tab document surface
  (`BuildMatrixItemDock`), reusing the exact `Git #2365` (Batter Up "Send to tab", itself #2312's
  Git Panel peek split) frozen-snapshot-on-click pattern.
- 2026-09-02T20:52:00Z ✅ DONE — `dotnet build ShaneBuilder.csproj` clean (0 warnings/0 errors)
  after the rebuild and again after rebasing onto the latest `main`. Committed
  (`ShaneBuilder: "Tab" button sends the Build Matrix to its own tab (#2287)`), rebased cleanly
  onto `origin/main` (no conflicts against #2286's landed drawer), pushed directly to `main` per
  this repo's git convention. `verify-branch-merged.mjs` confirms merged. `git status --porcelain`
  clean of anything this session touched. No fixture data anywhere — every slot in both the live
  drawer and the sent tab comes off the real `bt_build_queue`-backed `_queueItems`.
