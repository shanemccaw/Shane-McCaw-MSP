# #1486 — Portal New Design: Change Control (contract extraction pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `1bc8b87d4` (pack, pre-rebase) → merged to origin/main as `020d46dcb`; bookend IN-FLIGHT `8ee7…`→rebased.


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1486`, but #1486 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1486 is complete.
>
> #1486's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Extracting the Change Control contract pack (#1577 step 3) to docs/change-control-contract-pack.md. Read-only extraction: Wire* from portal-change-control.ts:157, CURRENT vs DECIDED marking against sub-issues #1496–1506/#1554–1555, drift-collector.ts:78 finding, real enum unions, honest tri-state, forbidden list, cross-surface edges. Filing sub-issues for live endpoints no page calls.
- 2026-08-29 ✅ DONE — Shipped `docs/change-control-contract-pack.md`. READ-ONLY: no product code, schema, UI or migrations touched. Extracted the two live endpoints' full wire contract (every field cited to file:line), the four stored + four display enum unions from the Drizzle schema only, a CURRENT-vs-DECIDED map (every DECIDED row carries #1496–1506/#1554–1555; two OPEN GAPs named honestly — notification-rule table and `scheduled_for`-as-timestamp), the drift-collector.ts:78 `crRef` bug extracted with its test:22 assertion and exact mechanism, the honest tri-state (flagging the current fixture-fallback as a hard-rule violation → OPEN GAP), the honest-empty envelope + `scoped` signal, the forbidden list, and the five cross-surface edges (#1505). Orphaned-endpoint check: NO customer-facing orphan for this module — both portal endpoints wired; the MSP-scoped PATCH /msp/change-requests/:id is noted as out-of-scope, capability already tracked by #1496/#1497/#1499, so no new sub-issue filed. Branch verified merged into origin/main (020d46dcb) via verify-branch-merged.mjs (exit 0).
