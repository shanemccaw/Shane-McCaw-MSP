# #2673 — MSP console: no operator routes exist for Diagnostics and Scripts

- **Status:** ⏳ IN FLIGHT 2026-09-03T19:59:19Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-03T19:59:19Z ⏳ IN FLIGHT — Audited `docs/diagnostics-and-scripts-contract-pack.md`
  (#2451, the real Portal contract) and the existing `msp-diagnostics.ts` route file.
  **Finding: the Diagnostics half of this issue's premise is stale** — MSP-operator
  Diagnostics routes (`POST/GET /api/msp/customers/:customerId/diagnostics*`,
  `requireRole("MSPOperator")` + `assertCustomerAccess`) already exist and predate this
  issue (landed 2026-07-11, `f9ee28408`). The real, confirmed gap is **Scripts only**:
  there is no `/api/msp/customers/:customerId/scripts*` route anywhere — MSP operators
  have zero visibility into the requires-script findings / script-package assignment for
  their own customers, mirroring the real customer-facing
  `GET /api/portal/scripts/:checkKey/download` (§1c of the contract pack). Building the
  MSP-operator mirror of that surface next, same file, same auth pattern.
