# #2673 — MSP console: no operator routes exist for Diagnostics and Scripts

- **Status:** ✅ DONE 2026-09-03T20:07:09Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `b7dfb554c` (MSP-operator Scripts routes)

## Log
- 2026-09-03T19:59:19Z ⏳ IN FLIGHT — Audited `docs/diagnostics-and-scripts-contract-pack.md`
  (#2451, the real Portal contract) and the existing `msp-diagnostics.ts` route file.
  **Finding: the Diagnostics half of this issue's premise is stale** — MSP-operator
  Diagnostics routes (`POST/GET /api/msp/customers/:customerId/diagnostics*`,
  `requireRole("MSPOperator")` + `assertCustomerAccess`) already exist and predate this
  issue (landed 2026-07-11, `f9ee28408`). The real, confirmed gap is **Scripts only**:
  there is no `/api/msp/customers/:customerId/scripts*` route anywhere — MSP operators
  have zero visibility into the requires-script findings / script-package assignment for
  their own customers, mirroring the real customer-facing
  `GET /api/portal/scripts/:checkKey/download` (§1c of the contract pack). Building the
  MSP-operator mirror of that surface next, same file, same auth pattern.
- 2026-09-03T20:07:09Z ✅ DONE — Added two real MSP-operator routes to
  `artifacts/api-server/src/routes/msp-diagnostics.ts` (commit `b7dfb554c`), same file,
  same `requireRole("MSPOperator")` + `assertCustomerAccess` pattern every other route in
  that file already uses:
  - `GET /api/msp/customers/:customerId/scripts` — requires_script findings on the
    customer's latest run, joined against `monitor_checks`/`script_modules` so an
    operator can see per-check whether a script package is actually assigned
    (`available: true/false`, real data, never invented).
  - `GET /api/msp/customers/:customerId/scripts/:checkKey/download` — MSP-operator
    mirror of the customer-facing download, same three-step resolution and same
    honest 404s, ownership-scoped by `assertCustomerAccess` instead of
    caller-is-customer.

  No schema/migration needed — `monitor_checks.script_package_id`,
  `script_packages`, `script_modules` already exist (confirmed live against local
  `DATABASE_URL`: schema matches, 0 live `requires_script` findings today, so both
  routes honestly return empty/404 for real customers right now — consistent with
  `docs/diagnostics-and-scripts-contract-pack.md`'s live counts).

  **Verification:** diffed the file before/after my change against `tsc --noEmit`
  output — 11 pre-existing errors exist in the file with or without my change,
  all root-caused by a repo-wide broken pnpm store in the MAIN checkout
  (`drizzle-orm`/other packages have empty payload dirs — already filed and
  tracked: #2528, #2515, #2556, #2192, #2503), not by anything I added. My new
  code adds only additional instances of the exact same pre-existing cascading
  "implicit any" symptom, confirmed by isolated single-file `tsc` (no new error
  *categories*). Could not start a live api-server (same root-cause pnpm-store
  breakage blocks `pnpm run build` repo-wide) to hit the routes over HTTP, so
  this is verified by live-DB schema/data check + route-pattern parity with 9
  sibling routes in the same file, not by an HTTP call — stated honestly, not
  claimed as a live-tested endpoint.
  `git status --porcelain` clean. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`, `b7dfb554c`).
