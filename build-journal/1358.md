# #1358 — Phase 6 of #1352: real UI — locked/teaser view of real scan results

- **Status:** ✅ DONE 2026-09-13T06:46:43Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 1b652cae6 (real work), 1f455966f (manifest hash fixup)

## Log
- 2026-09-13T06:08:20Z ⏳ IN FLIGHT — read issue body + both comments; starting investigation of `usePortalV2Pillars`, #3946's backstop function, and the #1357 Scene 0 package before touching any UI.
- 2026-09-13T06:46:43Z ✅ DONE — Investigation found `usePortalV2Pillars`/`war-room-pillars` (named in #1352/#1358's own text) is stale: retired by the portal-v2 rebuild (#1673) and renamed by #1752; the real, live equivalent is `GET /api/portal/pillars` / `buildPillarSummary()`. Built against that instead — filed the stale-doc gap as #3954 rather than silently working around it.
  - New `GET /api/public/free-scan/results` (`artifacts/api-server/src/routes/public-free-scan-results.ts`, mounted in `routes/index.ts`): resolves the Prospect's customerId from the free-scan `sessionId` (`resolveFlowSession`/`resolveConsentedTenant`, the same pattern `public-purchase-packs.ts` uses) since a Free Scan Prospect never gets a JWT (#656's `hasRealEntitlement` gate — confirmed against `lib/free-scan-prospect.test.ts`). Calls the #3946 backstop (`ensureProspectFreeScanBackstop`) before reading. Reports `status: "scanning"` while `activeRunId`/no `findingsRunId`, else `status: "ready"` with real per-pillar scores + real findings — `PillarFinding.recommendation` is deliberately never serialized (that's what Monitoring pays for).
  - `FreeScan.tsx`: removed the entire authored FIXTURE (`FINDINGS`/`SEVERITY_COUNTS`/`WASTE_HEADLINE`/etc.). The results phase now polls the new endpoint (bounded: 8s × 30 attempts, Git #2160), renders real pillar scores (all 6), real severity counts, real findings (title/evidence/why-it-matters, locked "part of Monitoring" footer instead of a fabricated fix), and real licence-waste $ (from the licensing pillar's own `licensing.annualWaste` stat) when available. Reuses the shared Scene 0 `RevealNoScanGate` (#1357, `@workspace/copilot-scan-scene`) for the real "still scanning" state instead of a fourth copy — added the workspace dependency to `shane-mccaw-consulting/package.json` (real `pnpm install` deferred to the post-merge hook per the worktree pnpm rule; verified locally via a hand-made node_modules junction, same shape `link-deps.mjs` already uses for this package's other `@workspace/*` deps).
  - Real local Postgres tests: `public-free-scan-results.test.ts` (6/6 pass) covers the 400/404/409 ordering gates, the scanning vs. ready states, the backstop firing exactly once for a no-run customer, and that `recommendation` never reaches the wire.
  - `tsc --noEmit` clean for both `api-server` and `shane-mccaw-consulting`; `vite build` clean.
  - Live-verified via `shaneapp://runTest` against the real dev api-server + Vite dev server after merging into the dev-server checkout (Build Set `FreeScan`, [2/2] complete → one restart): full `test-manifests/marketing/free-scan-page.json` run, **22/22 steps passed**, including the two new deterministic `/api/public/free-scan/results` ordering-gate `apiTests` (real HTTP against `localhost:8080`). The OAuth-gated "real ready" path stays covered by the vitest suite against real Postgres rows (documented honestly in the manifest's own notes, same caveat class as the pre-existing consent-grant step).
  - Findings filed (both sub-issues of Feature #1352, milestone v1.1, board status AI Batter Up): **#3954** (Feature #1352's body still names the retired `usePortalV2Pillars`/`war-room-pillars` — doc-only fix), **#3955** (pre-existing hardcoded `$1,240` Monitoring price/basis in `FreeScan.tsx`, violates the no-hardcoding rule — left untouched, out of #1358's scope).
  - Working tree clean; `agent/1358-q2619` confirmed merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs`).
