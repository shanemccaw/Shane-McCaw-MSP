# #4839 — Census checks store their total as the flagged count

- **Status:** ✅ DONE 2026-09-19T05:43:00Z
- **Scope:** platform
- **Started:** 2026-09-19
- **Commit(s):** bdca443c7 (implementation + migration + tests + verify script), 371c21cbf (verify script `--persist`), plan 4c3b59840
- **Dispatch Posted:** 2026-09-19T00:40:00Z (BUILD comment on #4839)
- **Migration:** `lib/db/migrations/manual/2026-09-19-census-checks-real-filters-4839.sql` — run against local `DATABASE_URL`; line appended to #1630 for Replit/Staging (release-time, not a `Shane To-Do`)
- **Findings filed (direct sub-issues of #1621, label `bug`, board "AI Batter Up"):** #4851 (three more Teams policy-coverage checks store the team count), #4852 (`monitor-executor.test.ts` Security Defaults test fails on main)

## Log
- 2026-09-19T05:16:30Z ⏳ IN FLIGHT — Investigating the four checks' `monitor_checks.mapping` transforms against the `governance:ownerless-groups` precedent.
- 2026-09-19T05:27:30Z ⏳ IN FLIGHT — Plan committed (`build-journal/4839-plan.md`). Root cause: not a mis-filtering transform — the stored endpoints (`/sites`, `/teams`) carry no label/storage/activity/guest field, so `count` on `id` could only ever equal the total.
- 2026-09-19T05:43:00Z ✅ DONE — Four checks now filter over real signal-bearing sources; live-verified against tenant c4c814d4; `sitesWithLabelCount` (0) agrees with `sensitivityLabelCount` (0).

## Changes Made
- **New code-owned SharePoint-admin op `site-inventory`** (`sharepoint-admin.ts` `getSiteCollectionInventory` + pure `toSiteInventoryItem`; registered in `SHAREPOINT_ADMIN_OPERATIONS` in `monitor-executor.ts`). Pages CSOM `GetSitePropertiesFromSharePoint`; derives `hasSensitivityLabel` (all-zero guid = none) and `storageUsedPercent` (null when quota is not positive — never a fabricated 0). Throws on CSOM error or non-terminating paging instead of returning a partial list.
- **Migration** rewrites four `monitor_checks` rows (schema_version bumped, descriptions corrected):
  - `sharepoint:site-label-coverage` → `sharepoint-admin` / `site-inventory`, `countWhere('{{hasSensitivityLabel}} == true')`.
  - `sharepoint:storage-near-limit` → same op, `countWhere('{{storageUsedPercent}} >= 90')`.
  - `teams:inactive-teams` → `/reports/getTeamsTeamActivityDetail(period='D90')`, `countWhere('{{Is Deleted}} == "False" && {{Active Users}} == 0')`.
  - `teams:guest-membership` → same report, `countWhere('{{Is Deleted}} == "False" && {{Guests}} > 0')`.
  - All rules `requireField: true` (fail closed on a missing column); `targetField` names unchanged, so `pillar-signal-specs.ts` / `portal-governance-areas.ts` needed no edit.
- **Tests** `census-checks-filtered-counts-4839.test.ts` (11): mappings are read out of the migration file itself and run through the real `applyMapping` against CSV-parsed report rows and derived inventory items.
- **Live verify script** `src/scripts/verify-census-checks-4839.ts <tenant GUID> [--persist]`.
- **Deviation from the plan:** the inactive predicate changed from a `Last Activity Date olderThanDays 90` test to `Active Users == 0` — live data showed the report lags the clock by days (one team last active 86 days before the report's refresh date is 91 days before now). Recorded as a dated amendment in the plan.

## Verification Results
Tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3` (`tenants.id` 2080), real executor path (`executeMonitorCheck`), read-only against the DEV app registration:

| check | before | after | `_itemCount` |
|---|---|---|---|
| `sharepoint:site-label-coverage` `sitesWithLabelCount` | 99 | **0** | 96 |
| `sharepoint:storage-near-limit` `sitesNearStorageLimitCount` | 99 | **0** | 96 |
| `teams:inactive-teams` `inactiveTeamCount` | 18 | **18** | 18 |
| `teams:guest-membership` `teamsWithGuestsCount` | 18 | **0** | 18 |

- **Cross-check the issue asked for:** `sharepoint:site-label-coverage` = 0 and `governance:sensitivity-label-adoption` `sensitivityLabelCount` = 0 — no longer contradictory (script prints `consistent:true`).
- **`inactiveTeamCount = 18` equals the total, which is the original symptom, so it was checked rather than trusted:** the raw D90 report shows `Active Users = 0` on all 18 teams (13 with no activity date, the rest last active 2023-10-06 ×3, 2024-01-11, 2026-06-20). It is a real number on a dormant testbed, not the old bug.
- Storage: highest real usage is 9.7 % of quota, so 0 near the 90 % line. Guests: `Guests = 0` on all 18 teams in the report.
- Persisted fresh profiles for the four checks (`--persist`, same as a scan would) and ran `buildPillarSummary(2080)` — the cards now read "Sites carrying a label 0 of 96 sites", "Sites near storage limit 0 of 96 sites", "Inactive teams 18", "Teams with guests 0 of 18 teams". `verify-pillar-signals-4578.ts 2080`: **575 assertions, 0 failed**.
- `tsc --noEmit` (api-server): exit 0, before and after rebasing onto #4838's registry change. New vitest file: 11/11 pass. Neighbouring suites (`monitor-executor`, `health-object-inventory-checks-1260`, `health-display-denominator-413`, `monitor-evidence-capture-2923`, `portal-governance-areas`, `document-claim-binding-559`): 311 pass, **1 fails** — `monitor-executor.test.ts` "Security Defaults on: suppressed", which fails identically on the untouched main checkout (filed #4852). `drift-check-specs.test.ts` cannot import without `DATABASE_URL` (env, not a code change of mine; noted on #4852).
- No fixture data: every value above came from the live report / CSOM response.

## Honest gaps
- **Replit/Staging not touched** (out of scope by rule). The migration must run there and the api-server build carrying `site-inventory` must be deployed first, or the two SharePoint checks error until it is — noted on #1630.
- **SharePoint denominators now come from CSOM (96 site collections), while `sharepoint:site-count` still reads Graph (99).** Both are real; I did not reconcile the three-site difference.
- The 90 % near-limit threshold is my choice (no Microsoft-defined number for site collections); it lives in the stored mapping so it is data, not code.
- Severity rules for all four checks are still empty (out of scope).
- Two sharing-link checks (`sharepoint:anonymous-links`, `sharepoint:orgwide-links`) are `inactive` but carry the same total-as-count shape — noted on #4851.
- Pre-existing dirty files: none (working tree was clean on arrival).
