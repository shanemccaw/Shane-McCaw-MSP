# #3989 — action.set-mail-flow-rule cannot execute: expose FromScope=NotInOrganization, drop fictional {{condition}}

- **Status:** ✅ DONE 2026-09-14T13:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 60e108746 (redesign + catalog wiring + migration), d5a17fd9d (IN FLIGHT bookend)

## Log
- 2026-09-14T13:30:00Z ⏳ IN FLIGHT — Investigated #3948's exclusion (commit 286fc94c6), confirmed baseline_action_templates row id=75 (action.set-mail-flow-rule) and write_action_catalog row id=190 are still in the broken/demoted state the issue describes. Confirmed #3530 (Key Vault subscription disabled, ca-ps-execution-dev unreachable) is STILL OPEN — live-verify may be blocked; will attempt a real check before concluding that. Proceeding with: redesign template row (FromScope=NotInOrganization, drop {{condition}}), add New-TransportRule to cmdlet-catalog.ps1 + exchange-online-transport.ts allowlists, write manual migration for the data change, then attempt live-verify.
- 2026-09-14T13:45:00Z ✅ DONE — Redesigned `action.set-mail-flow-rule`: body_template is now `{"Name":"{{ruleName}}","SetSCL":"{{scl}}","FromScope":"NotInOrganization"}`, required_variables `["ruleName","scl"]` (dropped the fictional `{{condition}}`). Added `New-TransportRule` to `services/ps-execution/cmdlet-catalog.ps1`'s write entries (AllowedParams: Name, SetSCL, FromScope only) and to `exchange-online-transport.ts`'s `EXCHANGE_ONLINE_CMDLET_KEYS` map, matching #3948's pattern for its 6 sibling write cmdlets. Ran manual migration `lib/db/migrations/manual/2026-09-14-mail-flow-rule-fromscope-redesign-3989.sql` against local DATABASE_URL (verified both rows updated). Unit tests: 39/39 pass (`exchange-online-transport.test.ts` updated — replaced the "New-TransportRule deliberately unmapped" test with a real end-to-end mapping case; `admin-write-actions.test.ts` unaffected, still green). `tsc -p tsconfig.json --noEmit` exits 0. `cmdlet-catalog.ps1` parses clean (Parser.ParseFile, 0 errors).
  **Live-verify: attempted, genuinely blocked, NOT completed** — two real, confirmed blockers, not an assumption: (1) this build is part of buildSet `MSPConsole`; `request-restart.mjs` merged commit 7e3ba018 into the shared dev-server checkout but DEFERRED the restart (11/12 members complete) — the shared :8080 server was still running pre-#3989 code when tested, so the real POST correctly 400'd with the OLD "New-TransportRule has no allowlisted cmdletKey" message (audit log id 55, customerId=1 "Jane Jane"). (2) Independent of that, re-confirmed **#3530 is still live right now** — a direct `get-connection-info` call (unaffected by this build's changes) against the same server reproduced the identical real error: `"The subscription associated with this vault has been disabled. Vault: ShaneMcCawConsulting;location=eastus"`. Even once the buildSet restart fires, ca-ps-execution-dev remains unreachable until #3530 (Shane's own Azure billing/subscription action) clears. Did not retry/poll further — same "genuinely unreachable after a real check" posture as #3948/#3939, which left their own promotions un-live-verified for the identical reason.
  `write_action_catalog` id 190 left at `endpoint_design_pending` (not promoted to `execution_ready`) — same posture #3948 left its 6 sibling rows in (185/188/191/192/194/196), blocked_reason updated to name the real current state and what unblocks it (#3530 clearing + a confirmed live fire). No new finding filed — #3530 already tracks the real blocker and is still open.
  `git status --porcelain` clean. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (7e3ba018).
