# #3957 — Decommission PowerShell script-generation AI capability

- **Status:** ✅ DONE 2026-09-14T13:03:08Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 7704283f1 (fa8a9ba88 after rebase onto origin/main)

## Log
- 2026-09-14T12:47:45Z ⏳ IN FLIGHT — Read #3957 body + Shane's 2026-09-14 decision comment (decommission, not MCP-relay). Investigated the real call graph: `ps-script-gen.ts`'s exported `generateScriptFromService()`/`generateScriptFromDocument()` are actually dead code in production (only their own unit test imports them) — the real, live "unattributed" AI usage the issue traced comes from `admin-ps-scripts.ts`'s own independent inline implementations of the same two features, at routes `POST /admin/ps-scripts/generate-from-service` (line ~481) and `POST /admin/ps-scripts/generate-from-document` (line ~2871), both unversioned `claude-haiku-4-5`, no `withAiAttribution`. Proceeding to remove: the two dead library functions + their test file, the two live routes, and the matching Admin Panel dialogs (`GenerateFromServiceDialog`/`GenerateFromDocumentDialog` in `ScriptGeneratorPage.tsx`). Leaving the file's other AI-assist routes (freeform `generate`, `generate-from-task`, `analyze-permissions`, `fix`, `explain`, `modularize`) untouched — different, unnamed capabilities outside Shane's stated scope.
- 2026-09-14T13:03:08Z ✅ DONE — Removed `generateScriptFromService()`/`generateScriptFromDocument()` and their now-dead prompt constants from `ps-script-gen.ts` (deleted `ps-script-gen.test.ts` entirely, its only consumer); removed the two live SSE routes `generate-from-service`/`generate-from-document` from `admin-ps-scripts.ts` along with now-dead support code (Kanban task-association helpers `titleToWords`/`jaccardSimilarity`/`findBestModuleForTask`/`makeStubModule`, duplicate system prompts, now-unused imports); removed the matching Admin Panel UI (`GenerateFromServiceDialog`, `GenerateFromDocumentDialog`, the "From Service"/"From Document" buttons and their dialog-open state in `ScriptGeneratorPage.tsx`, plus now-unused interfaces). Updated two stale comments/test names that referenced the removed function names by name (`workflow-executor-generate-script.test.ts`, `admin-workflows.ts`) so the repo-wide grep for `generateScriptFromService|generateScriptFromDocument` returns zero hits. Verification: `grep -rn "generateScriptFromService\|generateScriptFromDocument"` across the repo → zero matches. `tsc --noEmit` clean in both `artifacts/api-server` and `artifacts/admin-panel`. Ran the real, affected test suites: `admin-ps-scripts.test.ts` + `admin-ps-scripts-modularize.test.ts` (6/6 pass via `node --experimental-strip-types --experimental-test-module-mocks --test`), `workflow-executor-generate-script.test.ts` (8/8 pass via vitest), `ps-guard.test.ts` (18/18 pass). Left untouched, out of Shane's stated scope: the file's other AI-assist routes (freeform `generate`, `generate-from-task`, `analyze-permissions`, `fix`, `explain`, `modularize`) and its list/view/retire script-library routes. Commit `7704283f1`, rebased and pushed to `origin/main` as `fa8a9ba88`. `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
