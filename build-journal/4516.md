# #4516 — success_criteria not honoured on the Graph pack path

- **Status:** ✅ DONE 2026-09-17T14:16:26Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 02f3bf85c (pushed as 1f1192368 after rebase onto origin/main)

## Log
- 2026-09-17T14:07:21Z ⏳ IN FLIGHT — Investigated live DB state:
  - `success_criteria ? 'captureAs'` = 2 rows (`quickstart-v1.create-break-glass-account`,
    `quickstart-v1.create-ca-exclusion-group`) — confirmed dead: nothing under `artifacts/`
    or `lib/` reads `captureAs`/`captureField`; the real carrier is
    `config_pack_templates.parameter_mapping` (`config-pack-graph.ts:386-409`).
  - `success_criteria ? 'expectStatus'` = 124 rows, values `{200: 41, 201: 31, 204: 51, 202: 1}`.
    Cross-checked the 19 rows with real audit-log execution history
    (`baseline_action_template_audit_log`) — all 19 observed statuses match their declared
    `expectStatus` exactly. The remaining 105 have no execution history to verify against,
    and most are destructive (delete-user, disable-user-signin, etc.) — not safe to fire live
    just to validate metadata.
  - Found a live, currently-active bug this surfaces: `microrem.deactivate-ownerless-team`
    (`POST /teams/{id}/archive`) declares `expectStatus: 202`, which is Graph's real
    documented response for that call — but `graphWriteForTenant`'s hard-coded
    `[200,201,204]` accept list excludes 202, so this write is *always* reported as failed
    today even when Graph behaves exactly as expected.
  - Decision: full replacement of the hard-coded accept list with a strict per-template
    `expectStatus`-only gate is the "more correct" fix the issue prefers, but carries real
    regression risk across 105 unverifiable rows. Going with the conservative middle path
    instead — union `expectStatus` into the accepted-status set (widen-only, never narrows)
    in the Graph pack path, which fixes the live 202 bug with zero regression risk to any
    currently-passing template — plus removing the 2 dead `captureAs`/`captureField` keys
    and documenting `parameter_mapping` as the sole real carrier. Filing a follow-up issue
    for the stricter full-replacement evaluation (needs a live-verification plan Shane can
    review before it's safe to ship broadly).
- 2026-09-17T14:16:26Z ✅ DONE — Shipped in commit `02f3bf85c` (pushed as `1f1192368`
  after rebase onto `origin/main`):
  - `lib/db/src/schema/msp.ts` — doc comment on `successCriteria` clarifying `expectStatus`
    is the only real key and `parameter_mapping` is the sole real output-chaining carrier.
  - `lib/db/migrations/manual/4516-drop-dead-success-criteria-keys.sql` — ran myself against
    local Postgres (`UPDATE 2`); dropped `captureAs`/`captureField` from the 2 rows. Confirmed
    post-run: both rows now `{"expectStatus": 201}` only.
  - `artifacts/api-server/src/lib/workflow-executor.ts` — new exported
    `graphAcceptedStatusCodes(successCriteria)`, called at the Graph write call site instead
    of the hard-coded `[200,201,204]` literal. Unions `expectStatus` into the default accept
    class (widen-only — never narrows), so no currently-passing template can regress, and
    the live `microrem.deactivate-ownerless-team` (Teams archive, real Graph 202) false-failure
    is fixed.
  - `artifacts/api-server/src/lib/workflow-executor-core.test.ts` — 4 new unit tests for
    `graphAcceptedStatusCodes` (default class, already-in-class no-op, real 202 widen case,
    non-numeric expectStatus ignored). Full file: 106/106 pass.
  - `pnpm run typecheck` (api-server): clean, both before and after.
  - Filed follow-up **#4530** for the deferred stricter (narrow-to-exact-`expectStatus`) gate,
    parented as a sibling sub-issue of **#1489** (Feature: Remediation Tracking (Portal)) —
    board status set to "AI Batter Up" (`a0296971`).
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0).
    `git status --porcelain` clean.
  - Labels: flipping `in-flight` → `complete` on #4516 now. No `Shane To-Do` needed — the
    migration was additive and I ran it myself; nothing here needs a manual action from Shane.
