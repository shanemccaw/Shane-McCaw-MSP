# #3043 — Gate status-report publish fan-out through #1704's can() engine

- **Status:** ✅ DONE 2026-09-15T14:18:46Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** dbe0814f5 (rebased/pushed to main as d42da005d)

## Log
- 2026-09-15T14:06:43Z ⏳ IN FLIGHT — #1704 confirmed closed; investigating `notifyStatusReportPublished()` in notification-center.ts and #1704's real `can()` engine shape to wire the recipient filter.
- 2026-09-15T14:18:46Z ✅ DONE — Found #3043's named target (`notifyStatusReportPublished()` / `POST /admin/status-reports/:id/send`) was deleted as dead code in #3495 (2026-09-10), after #3043 was filed but before it was picked up — stale premise, filed as #4252 (parented under #1569, board status "AI Batter Up"). Applied the real fix to the live successor surface instead: `msp-status-reports.ts`'s `POST /msp/status-reports/:id/comments` fan-out had the exact same defect #3043 described (notifies every tenant "client" role user with no permission filter). Added `filterByStatusReportViewAccess()`, which resolves each candidate's rung and runs it through #1704's real `ladderEvaluationInput()` + `evaluateAccess()`, checked against `ladder.customer-user` (the same floor `portal-status-reports.ts` requires to view a report at all) — fails closed on an unavailable RBAC read. Verified against real live local data via a scratch script (deleted before commit): of 13 real tenant users, 3 Free-tier rows evaluated `allowed:false` and 10 Customer-tier rows `allowed:true`, zero `unavailable` outcomes. `artifacts/api-server` typecheck clean; `requireAccess.test.ts` + `rbac-capability.test.ts` (28 tests) pass. Working tree clean, branch verified merged into origin/main (d42da005d).
