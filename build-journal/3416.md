# #3416 — Feature: Remove Document Generator IDE from Admin Panel (AdminPanel)

- **Status:** ✅ DONE 2026-09-10T04:30:25Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 40cc68fe4 (Remove Document Generator IDE from Admin Panel (#3416))

## Log
- 2026-09-10T04:04:03Z ⏳ IN FLIGHT — Investigated callers of DocumentGeneratorIde.tsx and its
  backend routes (admin-document-generator.ts). Confirmed real, independent callers still exist
  for `/tenants`, `/tenants/:id/projects`, `/document-types/:key/generate`,
  `/document-types/:key/suggest-scoping` (and keyless variant), and `/profile-keys` —
  SimulatorDocumentCanvas.tsx, DocumentScopingEditor.tsx, and DocumentTypesManager.tsx all
  depend on these. `/history`, `/history/:id/html`, `/history/:id/archive` are used by the
  separate adminv2 Documents screen (`adminv2/screens/documents/documentsApi.ts`). Only
  `/missing-types` is used solely by DocumentGeneratorIde.tsx — that one route (+ its test
  block) is safe to delete. About to delete the page, its cost test, the `cmd-doc-generator`
  nav entry, the `doc-generator` CommandWorkspace case, the `/missing-types` route, and fix the
  now-stale `/command/doc-generator` featureRoute default in admin-document-types.ts (0 real DB
  rows reference it locally, confirmed via direct psql).
- 2026-09-10T04:30:25Z ✅ DONE — Deleted DocumentGeneratorIde.tsx + its cost test, the
  `cmd-doc-generator` nav entry, the `doc-generator` CommandWorkspace case, and the
  IDE-only `/missing-types` route + its logger binding + test block (all other
  admin-document-generator.ts routes have confirmed live callers, kept unchanged). Fixed
  stale prose comments and the two `/command/doc-generator` guidance strings in
  admin-insights.ts. Hit a real rebase conflict mid-session: a concurrent build (Git #3417,
  commit `2f823941f`) deleted `InsightsOutputs.tsx` while I was working, which was the ONLY
  render site of `DocumentTypesManager.tsx` (reachable via a real, working
  `/command/insights?tab=document_types` deep link — `App.tsx`'s `/command/:section` route
  is generic, so #3417's "no nav config" check didn't catch this live caller, which lived
  inside the very file I was deleting). Combined with my own deletion of
  DocumentGeneratorIde.tsx's "New Document Type" modal, this leaves NO remaining UI path to
  create a document type — filed as #3492 (sibling under #1095, board status AI Batter Up)
  with full evidence, since deciding where (if anywhere) to remount creation is a real
  product decision, not something to build ad hoc. Corrected my own featureRoute fix in
  admin-document-types.ts to point at Simulator Studio (`/system/simulator`, the one
  surviving live surface) instead of the now-also-dead Insights route, referencing #3492.
  Resolved the rebase (CommandWorkspace.tsx conflict: kept both deletions), re-typechecked
  admin-panel + api-server clean, re-ran the affected tests (admin-document-generator.test.ts
  15/15, documents.test.tsx + ai-prompts.test.tsx + DocumentScopingEditor.test.tsx 52/52),
  pushed to main (`40cc68fe4`), verified via verify-branch-merged.mjs (OK — merged into
  origin/main), and confirmed `git status --porcelain` clean.
