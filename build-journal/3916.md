# #3916 — Add `?customerId=` scoping to GET /msp/active-directory/ou-assignment-requests

- **Status:** ✅ DONE 2026-09-13T05:02:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 397c8b7a3

## Log
- 2026-09-13T04:46:13Z ⏳ IN FLIGHT — investigated `msp-active-directory.ts`'s sibling
  `?customerId=` route (`/ous`, added for #2591) and the requests-list route
  (`:417-447`); about to add the same optional `?customerId=` param (via
  `assertCustomerAccess`) to `GET /msp/active-directory/ou-assignment-requests`,
  wire `useOuAssignmentRequests` in `artifacts/msp-console/src/api/ad-ou-api.ts`
  through it, and add real backend test coverage.
- 2026-09-13T05:02:00Z ✅ DONE — added optional `?customerId=` to
  `GET /msp/active-directory/ou-assignment-requests` (validated via
  `assertCustomerAccess`, still folds in `resolveStaffScopedCustomerIds`);
  omitting the param preserves the exact prior whole-book behavior. Wired
  `useOuAssignmentRequests`/`AdOuAssignmentPage.tsx` to pass `customerId`
  server-side instead of filtering client-side. Added 4 new backend unit
  tests (400 on bad customerId, 404 outside caller's book, correct scoping,
  unchanged whole-book behavior) — `msp-active-directory.test.ts` full suite:
  37/37 passing. Typechecked clean on both `api-server` and `msp-console` for
  the touched files. Verified the scoping SQL logic against real (temporary,
  rolled-back) rows in local Postgres — both backing tables (`active_directory
  _ou_assignment_requests`, `active_directory_ous`) are genuinely empty in the
  app today, matching the finding this issue was filed from. Commit
  397c8b7a3, pushed to origin/main and confirmed merged
  (`verify-branch-merged.mjs` OK); working tree clean.
