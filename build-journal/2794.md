# #2794 — POST /msp/sows/:sowId/sign returns 500 in 'signs a sent SOW' test

- **Status:** ✅ DONE 2026-09-04T08:23:17Z
- **Scope:** platform

## Log
- 2026-09-04T08:09:15Z — Starting investigation. Pre-existing test failure found during #2725; sign handler throws, Express default error handler swallows to bare 500. Need real debug pass into the handler.
- 2026-09-04T08:10:57Z — Added a temporary try/catch + console.error around the whole sign handler to surface the real error (per the issue's own instruction not to guess at the fixture).
- 2026-09-04T08:11:46Z — Real root cause found: `POST /msp/sows/:sowId/sign` fire-and-forget-imports `../lib/workflow-executor` to trigger the charge-approval workflow. That module transitively imports `lib/integrations-anthropic-ai/src/client.ts`, which throws synchronously at module-load time when `AI_INTEGRATIONS_ANTHROPIC_BASE_URL`/`AI_INTEGRATIONS_ANTHROPIC_API_KEY` aren't set. In the vitest environment those env vars aren't loaded, so `await import(...)` rejected, the rejection propagated out of the async route handler, and Express's default error handler turned it into a bare 500 — exactly as the issue predicted.
- 2026-09-04T08:11:46Z — Fix: wrapped the `await import("../lib/workflow-executor")` + `emitWorkflowEvent(...)` call in a try/catch in `artifacts/api-server/src/routes/msp-sow.ts`. The `.catch()` on the call already made the *call* non-fatal (existing comment: "charge approval will not trigger") but the *import itself* wasn't guarded — now a load-time failure in that large transitive dependency graph is logged and swallowed the same way, not allowed to 500 a real signed SOW.
- 2026-09-04T08:13:04Z — Removed debug instrumentation. Also found (and fixed) a second, related issue: even after the fix, `msp-sow.test.ts`'s "signs a sent SOW" test was flaky — timing out past the default 5000ms test timeout because the real `workflow-executor.ts` (~11k lines) is expensive to transform/import on a cold run. Added `vi.mock("../lib/workflow-executor.ts", ...)` to the test file so it's a true unit test of `msp-sow.ts` — runtime dropped from 5-9s (flaky) to <1s (reliable across repeated runs).
- 2026-09-04T08:13:04Z to 08:15:54Z — Verified `msp-sow.test.ts` — 31/31 passing, repeated 3x with no flakiness.
- 2026-09-04T08:16:16Z to 08:21:24Z — Ran the full `artifacts/api-server` vitest suite (308s). Result: 97 test files / 225 tests failed — all pre-existing and unrelated to this change (e.g. `msp-financial-aggregator.test.ts` currency-formatting assertions off by 100x, unrelated to SOW signing). `msp-sow.test.ts` does not appear in the failure list. Did not investigate or fix these — out of scope for #2794; the issue body scoped this build strictly to the sign-route 500.
- Checked the other 5 call sites of `await import("../lib/workflow-executor")` across the codebase (`admin-baseline-templates.ts`, `admin-testbed.ts`, `break-glass-verification.ts`, `msp-launch-control.ts` x2) — all are the primary purpose of their handler (not fire-and-forget) and are already wrapped in an outer try/catch that returns a real error response. No further fix needed there.
- Merged branch `agent/2794-q1586` (rebased onto current `origin/main`) straight to `main`: `de4552980..c4a364d75`.
- Filed finding **#2857** (msp-financial-aggregator.ts invoice revenue off by 100x) as a sibling sub-issue of Feature #2568, labeled `bug`, milestone v1.1, board status "AI Batter Up". The other ~96 failing test files from the full-suite run were not individually triaged/filed — too broad to file actionably without real per-file root-causing, which was out of scope for this build; #2857 is the one concrete, evidenced bug I actually traced.
- ✅ DONE — 2026-09-04T08:23:17Z. Commit: `c4a364d75` (was `8d4e1b6d0` on the worktree branch before rebase). `git status --porcelain` clean. `git merge-base --is-ancestor HEAD origin/main` confirms merged.
