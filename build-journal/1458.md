# #1458 — Compliance obligations register (already fixed)

- **Status:** ✅ DONE
- **Scope:** platform
- **Commit(s) verified against:** 45bfef9fe (pre-existing fix), 9860c3d17

## Log
- Investigated `artifacts/msp-portal/src/pages/portal-v2-compliance-obligations.tsx`
  and its backing route per #1458 ("Compliance: Obligations register entirely
  unconditional fixture — found by #1415 (HIGH)").
- Found the fix already landed prior to this session, filed and closed as
  **#1432**, commit `45bfef9fe` ("Wire Compliance obligations register to real
  backend, honest no-data states"). #1432's own investigation found a real
  backing query already existed (`GET /api/portal/compliance-obligations` in
  `artifacts/api-server/src/routes/portal-compliance-obligations.ts`, joining
  `compliance_frameworks`/`compliance_obligations`/`tenant_compliance_scope`
  to the tenant's open `msp_risk_decisions`, built in #1223/#1333) but it was
  never mounted into `routes/index.ts` and the page never called the real
  client hook (`useComplianceObligationsLive`) — so the `CMP_OBLIGATIONS`
  fixture kept rendering unconditionally even though real plumbing existed.
- Confirmed current state is honest, not fixture-fallback:
  - `routes/index.ts:160` mounts `portalComplianceObligationsRouter`.
  - The page reads `useComplianceObligationsLive()` directly; `rows` is
    `dataState === "live" ? obligations : []` — the fixture (`CMP_OBLIGATIONS`)
    is never rendered, even as a fallback on error.
  - Loading state (`pv2-cmpobl-status`, "Loading your obligation register…"),
    load-failure state ("...could not be loaded — no live data available."),
    and empty-catalog state (`pv2-cmpobl-empty`, "No obligations are
    catalogued yet — not yet measured.") are all distinct and honest — no
    silent fixture fallback for any of the three states.
- `test-manifests/portal/compliance-drilldowns.json` already covers this page
  and is registered in `_regression-suite.json`; its
  `lastVerifiedAgainstCommit` (`b91011692`) postdates the `45bfef9fe` fix
  commit, so coverage is current. No manifest changes needed.
- ✅ DONE — no code changes required; issue was already resolved by #1432,
  which is closed. This session's only action is closing the #1458 loop
  (bookend + label sync + issue comment) since #1458 and #1432 were two
  separate issues describing the same defect.
