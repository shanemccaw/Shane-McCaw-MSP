# Build Journal — #3881

- **Status:** ⏳ IN FLIGHT 2026-09-13T04:26:35Z
- **Scope:** platform
- **Log:** tsc fix: `portal-checkout.test.ts:549` mocked `getMspDefaultPaymentMethod` with `.mockResolvedValueOnce(null)`, but the real function (`lib/stripe.ts:268`) returns `Promise<string | undefined>` — `null` is not in the union and never a real production return value (falls back to `undefined` on both the `catch` and the not-found paths). Fixed the mock to `.mockResolvedValueOnce(undefined)` to match the real contract, rather than widening the real type.
