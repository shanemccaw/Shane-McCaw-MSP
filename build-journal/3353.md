# Build Journal — #3353

- **Status:** ✅ DONE 2026-09-10T00:35:00Z
- **Scope:** platform

## Log
- 2026-09-09T23:50:00Z — Started. Registering real purgers for `customer_feature_role_mapping.tenant_id` and `customer_roles.tenant_id` (RBAC tables landed by #2455), per #3353 / #2859 discipline. Both are `tenants.id` integer FKs (ON DELETE CASCADE), keySpace `customerId`, same pattern as `portal_change_control_notifications`/`portal_hold_windows` in `changeControlPurger`.
- 2026-09-10T00:35:00Z — ✅ DONE — Confirmed against #2455's design: a customer-scoped role/mapping row is real customer data (not platform state — `tenant_id IS NULL` rows are the platform defaults, untouched), so it should purge with the tenant like every other module, same as the dispatch note assumed. Registered `rbacPurger` (`customer_roles.tenant_id`, `customer_feature_role_mapping.tenant_id`, both `keySpace: "customerId"`) in `artifacts/api-server/src/lib/retention/purgers/modules.ts`. `customer_user_roles` needs no target — it cascades via its own FK onto `customer_roles.id`. Verified: `tenant-scope-coverage.live-db.test.ts` 7/7 passing (was 6/7); full `src/lib/retention` suite 104/104 passing; `tsc --noEmit` clean. Commit `e899846d` (rebased/fast-forwarded onto `origin/main`; `verify-branch-merged.mjs` confirms merged). No findings filed — this was the exact, scoped gap #3353 already named. Working tree clean.
