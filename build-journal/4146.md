# #4146 — AI Batter Up: filter by Epic, plus per-Epic-group copy button

- **Status:** ✅ DONE 2026-09-14T23:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 5c26d69bc1662bf70dec00907c7d8c85d15d8359

## Log
- 2026-09-14T23:33:00Z ⏳ IN FLIGHT — Read AiBatterUpPanel.xaml/.xaml.cs in full. Found the
  existing copy-button pattern to match: `SettingsTabView.xaml.cs`'s per-row copy button
  (`Content = "📋"`, `Style = FindResource("IconButton")` from `Themes/DarkTheme.xaml`,
  `Clipboard.SetText` + `ToastEngine.Success`). About to add an Epic filter combobox
  (AND-combined with `TxtFilter`) and a per-Epic-group copy button next to
  `BuildEpicGroupHeader`, producing "Check AI Batter Up for your issues:\nGit #n\n...".
- 2026-09-14T23:52:00Z ✅ DONE — Added `CmbEpicFilter` (populated in
  `PopulateEpicFilterOptions` from the distinct Epics actually present in `_allRows`, "All
  Epics" default first, selection preserved across a refresh where possible), combined
  AND-wise with the existing `TxtFilter` text search in `RenderFilteredRows`. Reworked
  `BuildEpicGroupHeader` to return a label+copy-button `Grid` (same `IconButton`
  style/`Clipboard.SetText`/`ToastEngine.Success` pattern as `SettingsTabView.xaml.cs`'s
  per-row copy button) that copies "Check AI Batter Up for your issues:\nGit #n\n..." for
  that group's currently-visible rows only, in on-screen (number-descending) order.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  warnings/errors. No live WPF click-through (no interactive WebView2/UI harness for this
  control), but the XAML/code-behind wiring was read end-to-end against the existing
  `TxtFilter`/`GroupByEpic`/`RenderFilteredRows` flow it extends, and matches the
  established combobox/icon-button conventions elsewhere in `Controls/`. Commit
  `5c26d69bc1662bf70dec00907c7d8c85d15d8359`, verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean (reverted an unrelated
  auto-touched `shaneapp-executeSql.result.json` scratch diff that wasn't mine to commit).
  No new findings filed — no genuine backend/product gap surfaced by this build.
