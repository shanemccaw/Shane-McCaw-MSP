# #1761 — Wizard-raised Standard CRs never display Approved

- **Status:** ✅ DONE 2026-09-01T17:35:00Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** 979af1205 (fix), plus this bookend

## Log
- 2026-09-01T17:20:00Z ⏳ IN FLIGHT — set `approvedBy` at CR-insert time in `portal-change-control.ts`'s `POST /portal/change-control` handler when `changeClass === 'standard'`, mirroring the catalog `execute` route's pattern. About to verify against local DB.
- 2026-09-01T17:35:00Z ✅ DONE — set `approvedBy` to `"Standard change — pre-approved"` at CR-insert time in `portal-change-control.ts`'s `POST /portal/change-control` handler whenever `body.changeClass === "Standard"`, and passed the same value into `materializeApprovalsForChange` (previously hardcoded `null`), mirroring `portal-change-catalog.ts`'s `execute` route pattern exactly. `tsc --noEmit` clean on the touched file (two pre-existing unrelated errors elsewhere, not introduced by this change). Verified by logic trace against `displayStatus()` in `lib/portal-change-control.ts` (approvedBy truthy → "Approved") and `materializeApprovalsForChange`'s `approvedBy` branch (writes an `approved` cr_approvals row, `approver_role: catalog_inherited`). No schema change needed — `approvedBy` column already exists and is used by the catalog route. Branch merged to origin/main, verified via verify-branch-merged.mjs.
