# #3482 — Feature: Change Control Lifecycle (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T21:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 677cd82a1 (real feature work; rebased 5x through heavy concurrent traffic on `main`, same content each time)

## Log
- 2026-09-10T06:13:35Z ⏳ IN FLIGHT — pulled issue #3482 body + both dispatch comments; confirmed real scope: MyArchitect (WPF desktop) Feature under Epic #3454, sibling of #3471, entirely client/UI work (CAB meeting/agenda view, freeze/maintenance window check surfaced pre-execution, PIR filing) against backend endpoints the issue's own audit confirms already exist. Checking the Epic's standing rule before touching any MyArchitect UI.
- 2026-09-10T06:16:00Z 🛑 BLOCKED — Epic #3454's standing rule requires #3493 (UI Shell Redesign) to land before any other MyArchitect Feature's UI work; every remaining item on #3482's own checklist is client/UI work. Verified the existing `blocked_by → #3493` edge is current (#3493 is OPEN, labeled `in-flight` — actively being built elsewhere, not stale/closing). Synced labels on #3482 (`blocked`, removed `in-flight`) and posted an explanatory comment. No code changes; nothing to build here until #3493 ships.
- 2026-09-10T20:20:00Z ⏳ IN FLIGHT — resumed. #3493 (UI Shell Redesign) is now CLOSED/`complete` — verified via `gh issue view 3493` before touching anything. Removed the stale `blocked_by → #3493` edge (`DELETE .../dependencies/blocked_by/5407799446`), swapped labels `blocked` → `in-flight`. Building the real remaining checklist against the now-landed shell (`ShellRegistry`/`ContextualTabSpec`/`RecordWorkspaceSpec` per UI_RULES.md) and the confirmed real backend: freeze/maintenance windows (`msp-change-freeze-windows.ts`, `msp-change-maintenance-windows.ts`), CAB (`msp-change-control-cab.ts`), PIR (`msp-change-pir.ts`), executions (`msp-change-executions.ts`).
- 2026-09-10T21:05:00Z ✅ DONE — Shipped all three real checklist items:
  1. **Freeze/maintenance pre-execution check** — `Models/ChangeCalendarMatching.cs`, a client-side port of the server's own `portal-change-freeze.ts`/`portal-change-maintenance.ts` occurrence-walk matching, evaluated at the instant "now" against `GET /api/msp/change-freeze-windows` + `GET /api/msp/change-maintenance-windows` (no server endpoint evaluates this at execution time). An active freeze now genuinely blocks `OpenChangeRequestRecord`'s "Record human action" action (the action isn't added at all) rather than being purely informational; maintenance coverage is shown as a fact only, since the server itself never enforces maintenance against execution time.
  2. **CAB meeting/agenda view** — `Services/ICabService.cs` + `CabService.cs` against the real `msp-change-control-cab.ts` (Git #1501): browse meetings, open a meeting's real agenda, add eligible changes, record approve/reject/defer (through the real #1496 approval ledger — no second decision model), start/close/cancel a meeting, schedule a new one. New "Change Advisory Board" Home-tab group + `OpenCabMeetingRecord`/`OpenCabAgendaItemRecord`/`OpenScheduleCabMeetingRecord`.
  3. **PIR filing after a completed execution** — `OpenPirPickExecutionRecord`/`OpenPirFilingRecord` in `MainWindow.xaml.cs`: lists executions with no PIR on file yet (`GET .../executions` + `GET .../pirs` by changeRequestId) and files one via `POST /api/msp/change-control/executions/:id/pir`, with the real close-code vocabulary and drift-rescan honesty (#1502) surfaced back.
  - New models: `ChangeFreezeWindow.cs`, `ChangeMaintenanceWindow.cs`, `ChangeCalendarMatching.cs`, `ChangeRequestPir.cs`, `CabMeeting.cs`. Extended `IChangeControlService`/`ChangeControlService` with freeze/maintenance/executions/PIR calls.
  - **Verification:** `dotnet build MyArchitect.csproj` — 0 warnings, 0 errors, both before and after every rebase. Every wire field cross-checked against the real DB schema via direct `psql` (`\d change_freeze_windows`, `change_maintenance_windows`, `cab_meetings`, `cab_agenda_items`, `cr_pirs`, `cr_executions`) — column names/types match the C# models and the server's own `toWire*` functions exactly. No live HTTP round-trip against the gated endpoints in this session — creating a synthetic MSP-operator test user for that was blocked by the environment's own auto-mode classifier; did not retry or work around it. No test manifest required — `desktop/MyArchitect` is a WPF desktop tool, outside the portal/marketing-site scope the Test Coverage section covers.
  - `main` was extremely active with concurrent MyArchitect sessions during this push (POA&Ms, Break-Glass, Runbooks, Support Tickets, Audit Log, SLA Tracking, Session Notes/#3472 all landing in the same window) — rebased onto `origin/main` 5 times, resolving each real conflict by hand (one genuine overlapping edit in `OpenChangeRequestRecord` from #3472's `attestationNote` work, merged to keep both; the rest were adjacent-insertion collisions from unrelated concurrent Features, resolved by keeping both sides' complete content). Rebuilt clean after every resolution before re-pushing.
  - `git status --porcelain` clean. Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs`.
  - No findings filed this session — no bugs hit, no gaps discovered beyond what the issue itself already scoped.
