# #1282 — Health page: wire drift table onto the now-real drift_events store

- **Status:** ✅ DONE
- **Scope:** platform (msp-portal Health page + api-server resolver, test-manifest fix)
- **Started:** 2026-08-28
- **Commit(s):** 912d3917d (main wiring), 91b3063b4 + feecb7e97 + adca1bbac (manifest/lastVerifiedAgainstCommit fixups); `agent/1282-q737` confirmed a genuine ancestor of `origin/main` via `verify-branch-merged.mjs`.

## Log
- 2026-08-28 ⏳ IN FLIGHT — #1287 landed the real drift engine (drift_events / drift_collection_status, per-check spec registry, all 5 executor paths, `resolveDriftEvents`). Now point Health page's `hltDashboardData` 12-row drift fixture at the real store via a real resolver call, with honest not_comparable/error/no-data states instead of the fixture, matching platform's honest-null conventions.
- 2026-08-28 ✅ DONE — New `useHltDriftLive.ts` resolves all 18 registered `drift.*DriftCount` metric keys via `POST /api/dashboard/resolve`; a domain with no spec yet (13 of 18) honestly resolves `no_data`, so widening `drift-check-specs.ts` lights the table up automatically with no frontend change. `hltDashboardData.ts` gained `HLT_LIVE_DRIFT_VERDICT` (the real 4-value `DRIFT_EVENT_VERDICTS` taxonomy given its own honest labels — deliberately NOT force-fit into the fixture's 6-value `HltVerdict`, resolving the verdict-taxonomy decision #1442 explicitly deferred), `hltDriftRowFromLive`/`hltDriftRowsFromLive` (event → display row, verdict-sorted) and `hltHeroStatsWithDriftLive` (real event count + real domain coverage, replacing the fixture's invented "N of 47" outright). `portal-v2-health.tsx`'s drift table + "Config drift" hero stat now render one of three honest states: real per-setting rows, a real "no drift events" clean note (baseline captured, genuinely zero deviations), or the honest no-live-data state (no domain has ever captured a baseline). A genuine not_comparable/error domain (#1287's honest fallback) surfaces as a footer note with the collector's own specific reason. 8 new unit tests added to `hltDashboardData.test.ts` (36/36 green). `tsc --noEmit` clean on all touched files (2 pre-existing unrelated errors elsewhere, confirmed not mine). `test-manifests/portal/portal-v2-pillars.json` H2/H5b/H10 updated to the new honest either/or states (sessionTag `hlt-drift-1282`). **Live-verified twice via `shaneapp://runTest`**: first run (6/6 passed) proved the mechanism but skipped the actual new assertions because `onlyTag` scoping excluded the pre-existing Health-page reset/navigate step; found and fixed that real pre-existing manifest gap (the reset step needed the `setup` tag to survive `onlyTag` scoping, same as the login flow — confirmed empirically, not guessed) and re-ran: **9/9 steps passed** against the real running dev server, including the drift table and hero-stat source markers rendering their honest "empty" state correctly for this tenant's real (unscanned-for-drift) state — proof it renders real data honestly rather than falling back to fixture. `verify-branch-merged.mjs` confirms `agent/1282-q737` is a genuine ancestor of `origin/main`.
