# #4024 — Projects: four live, correctly-scoped endpoints have no frontend caller anywhere

- **Status:** ✅ DONE 2026-09-15T16:29:09Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** a30b2b43c (bookend IN FLIGHT), a61e24bb0 (wire standalone SOW creation)

## Log
- 2026-09-15T16:28:34Z ⏳ IN FLIGHT — Re-ran the issue's own confirming greps. Two of the four original items
  already resolved (#1739). Of the remaining three, `POST /api/msp/sows/:sowId/sign` and the
  `GET`/`POST /api/msp/customers/:customerId/clickwrap` pair are already wired (Feature #2568,
  `artifacts/msp-console/src/api/offers-and-sows-api.ts` + `OffersAndSows.tsx`, landed in commit
  `e71b4c9ec`). Only `POST /api/msp/sows` (standalone SOW creation, no offerId) has zero frontend
  caller. Wiring that one into the existing "Statements of work" tab now.
- 2026-09-15T16:29:09Z ✅ DONE — Added `useCreateStandaloneSow` (offers-and-sows-api.ts) posting
  `{ mspId, customerId, title, description?, amountCents }` to `POST /api/msp/sows`, and a "New SOW"
  form (`NewSowPanel`) in `OffersAndSows.tsx`'s Statements of work tab, reachable via a new button in
  the filter row. On success it opens the created draft in the existing SOW detail panel, which
  already exercises Open document / Sign / Charge / Expire. Verified: `tsc --noEmit` clean in
  `artifacts/msp-console`, `pnpm run build` clean (1973 modules, no errors). No UI runner (Playwright/
  shaneapp://runTest) was invoked — this repo's test-manifest rule (CLAUDE.md "Test Coverage —
  Standing Practice") scopes to `artifacts/portal/` and `artifacts/shane-mccaw-consulting/` only;
  `artifacts/msp-console` is not in that scope. All 3 endpoints named in #4024's narrowed scope now
  have real frontend callers: sign + clickwrap were already wired (#2568), standalone create is wired
  here. `git status --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing this DONE.
