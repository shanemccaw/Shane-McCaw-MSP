# #4024 — Projects: four live, correctly-scoped endpoints have no frontend caller anywhere

- **Status:** ⏳ IN FLIGHT 2026-09-15T16:28:34Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-15T16:28:34Z ⏳ IN FLIGHT — Re-ran the issue's own confirming greps. Two of the four original items
  already resolved (#1739). Of the remaining three, `POST /api/msp/sows/:sowId/sign` and the
  `GET`/`POST /api/msp/customers/:customerId/clickwrap` pair are already wired (Feature #2568,
  `artifacts/msp-console/src/api/offers-and-sows-api.ts` + `OffersAndSows.tsx`, landed in commit
  `e71b4c9ec`). Only `POST /api/msp/sows` (standalone SOW creation, no offerId) has zero frontend
  caller. Wiring that one into the existing "Statements of work" tab now.
