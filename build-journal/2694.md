# #2694 — Chat breadcrumb bar: show real resolved epic (number + title)

- **Status:** ✅ DONE 2026-09-03T17:28:23Z
- **Scope:** buildconsole

## Log
- 2026-09-03T17:24:40Z — Session started. Implementing per issue #2694: add bold resolved-epic label to Band 2 breadcrumb bar in `Controls/ChatDocumentContainer.xaml(.cs)`, reusing `GetEpicForChat` already called in `RefreshContext()`.
- ✅ DONE — 2026-09-03T17:28:23Z. Added `CrumbEpic`/`CrumbEpicSep` `TextBlock`s to Band 2 of `Controls/ChatDocumentContainer.xaml`, bold, between the org crumb and the chat-title crumb. Wired in `RefreshContext()` (`ChatDocumentContainer.xaml.cs`) using the SAME `_owner.LeftSidebar?.GetEpicForChat(_chat)` call already made for `CtxEpicNum` — no second resolution call added. Renders `#{GithubNumber} — {Title}` (e.g. "#1202 — EPIC: Build Console") when an epic resolves; collapses both elements to an honest empty state (no fabricated label) when it doesn't. `CrumbChat` (raw chat title) left untouched — additive only. Updates on the same `RefreshContext()` cadence already wired to tab-select/queue-refresh, no new trigger added. Build verified: `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 0 warnings. `desktop/BuildConsole` is out of scope for test manifests per CLAUDE.md, so no manifest added. Commits: `277f4dc9` (impl, on branch), rebased+merged to `main` as `56c2bed1f`. Branch verified merged via `verify-branch-merged.mjs`. Working tree clean.
