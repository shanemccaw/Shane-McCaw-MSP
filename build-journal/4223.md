# 4223 — Add exact-alpha AccentWashBrush for StreamingConsoleWindow ActiveBadge

- **Status:** ✅ DONE 2026-09-15T05:10:00Z
- **Scope:** buildconsole

## Log
- 2026-09-15T00:00:00Z — Starting. Adding `AccentWashBrush` (`#2A7C8CF0`) to `Themes/Colors.xaml`
  matching the exact alpha value StreamingConsoleWindow.xaml's ActiveBadge used as a raw hex
  literal, and wiring ActiveBadge's Background to it.
- 2026-09-15T05:10:00Z — ✅ DONE — Added `AccentWashBrush` (`Color="#2A7C8CF0"`) to
  `desktop/BuildConsole/Themes/Colors.xaml`, exactly matching the raw alpha (`0x2A` ≈ 16.5%)
  StreamingConsoleWindow.xaml's ActiveBadge previously hardcoded, and repointed
  `ActiveBadge`'s `Background` to `{StaticResource AccentWashBrush}`. Both edited XAML files
  verified well-formed via PowerShell `[xml]` parse. Commit `936d9db08` on `agent/4223-q2889`,
  fast-forward-pushed directly to `origin/main` (`git push origin HEAD:main`); confirmed
  merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
  Discovered while verifying via `dotnet build`: a pre-existing, unrelated MC3000 XML
  mismatch in `desktop/BuildConsole/Controls/GitModeContainer.xaml` (Grid/StackPanel tag
  mismatch around lines 289/478) blocks a full solution build — not caused by this change
  (confirmed via `git log` that file wasn't touched this session). Filed as
  [#4232](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4232), parented under #1202,
  labeled `bug`, board status set to AI Batter Up.
