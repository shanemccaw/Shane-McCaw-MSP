# #3425 — Feature: Remove Delivery Workflow page (/delivery/workflows) from Admin Panel (AdminPanel)

- **Status:** 🛑 BLOCKED 2026-09-10T04:50:12Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none — no code changed

## Log
- 2026-09-10T04:50:12Z 🛑 BLOCKED — Investigated only. Located the real nav entry
  (`id: "del-workflows"`, `artifacts/admin-panel/src/components/shell/workspaceNav.tsx:361`,
  path `/delivery/workflows`) in the same "Delivery" nav section (lines ~344-361 of that
  file) as `id: "del-projects"` (`/delivery/projects`, line 353) — the exact page #3433
  is moving out of Admin Panel into MSP Console in the same file. #3425's own build
  header carries `--blocked-by 3433`, and a real GitHub `blocked_by` dependency edge from
  #3425 → #3433 already exists (confirmed via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3425/dependencies/blocked_by`). #3433 is
  still `state: open`, labeled `in-flight` (not `complete`) — i.e. genuinely still in
  progress. Editing `workspaceNav.tsx`'s Delivery section now (to delete `del-workflows`)
  while #3433 is concurrently editing the same section (to relocate `del-projects`) is
  exactly the shared-file collision class CLAUDE.md's git-conventions section warns
  against. Stopping here per the blocked-issue protocol rather than guessing forward.
  No code changed. `--title` correctly targets a leaf issue (not an epic) — the block is
  a real cross-issue file-collision risk, not a mis-dispatch.
