# #3281 — Tesla routes leak TeslaError as a raw 500 instead of a real 400

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** aae08e78d (fix), 69c5fe6f7 (merge to main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — wrapping the 3 pre-existing uncaught-TeslaError gaps named in the issue (`GET /api/tesla/vehicles`, `GET /api/tesla/climate`, `GET /api/tesla/commute-check`) in `web/shanes-life/src/routes/api.mjs` with the same try/catch → `badRequest` pattern already proven on `/api/tesla/heading-home` and `/api/tesla/charge`.
- 2026-09-08T00:20:00Z ✅ DONE — all 3 routes wrapped in `try { ... } catch (err) { if (err instanceof TeslaError) throw badRequest(err.message); throw err; }`, mechanical, identical to the proven pattern. Verified with `node --check src/routes/api.mjs` (syntax OK) and confirmed via grep that all `teslaCore.*` calls reachable from these 3 routes are now inside the try block. Merged onto `main` (69c5fe6f7), `git merge-base --is-ancestor HEAD origin/main` confirms ancestor, working tree clean.
