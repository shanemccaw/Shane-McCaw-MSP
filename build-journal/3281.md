# #3281 — Tesla routes leak TeslaError as a raw 500 instead of a real 400

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — wrapping the 3 pre-existing uncaught-TeslaError gaps named in the issue (`GET /api/tesla/vehicles`, `GET /api/tesla/climate`, `GET /api/tesla/commute-check`) in `web/shanes-life/src/routes/api.mjs` with the same try/catch → `badRequest` pattern already proven on `/api/tesla/heading-home` and `/api/tesla/charge`.
