# #4284 — Accessibility Design Rule (low-glare colors + dyslexia-aware typography, MyArchitect)

- **Status:** ✅ DONE 2026-09-15T22:32:04Z
- **Scope:** buildconsole (MyArchitect desktop app — private repo shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect `272d578` (palette + XAML retrofit + ribbon) and `932aec4` (code-behind retrofit). Bookend commits are in Shane-McCaw-MSP.

## Log
- 2026-09-15T21:20:00Z ⏳ IN FLIGHT — pulled #4284 body + all comments; verifying prerequisite extraction state before editing any theme file.
- 2026-09-15T21:31:26Z 🛑 BLOCKED — #4284 targets `shanemccaw/MyArchitect`, but that repo does NOT yet contain the app source. The extraction it depended on (#4278) was closed `not_planned` — Shane killed the runaway `git subtree split` and replaced the whole extraction effort with **#4305** (`Feature: Extract MyArchitect into shanemccaw/MyArchitect — simple file copy, NO git history surgery`), which is OPEN and in-flight. `shanemccaw/MyArchitect` was last pushed 2026-09-10 (docs/build-journal only); the real app source (`App.xaml`, `MainWindow.xaml`, `Themes/{Colors,DarkTheme,Typography}.xaml`, `UI_RULES.md`, etc.) still lives in `desktop/MyArchitect/` in this monorepo. Doing the accessibility retrofit against `shanemccaw/MyArchitect` now would edit a repo with no theme files to change; doing it in the monorepo `desktop/MyArchitect/` would race the pending #4305 copy and risk being lost/overwritten by the extraction. Per Shane's direct instruction this session, treating #4284 as genuinely blocked on #4305.
  - Removed the two stale `blocked_by` edges pointing at now-closed #4278 and #4275 (a closing blocker silently releases downstream — Git #1987).
  - Wired the real `blocked_by` edge #4284 → #4305 (internal id 5467518327).
  - Labels: added `blocked`, removed `in-flight`.
  - Posted an explanatory comment on #4284.
  - No code touched; working tree clean apart from this bookend.

## Unblock check (for the next session)
Before resuming, confirm #4305 has landed and the app source is really in `shanemccaw/MyArchitect`:
```
gh api repos/shanemccaw/Shane-McCaw-MSP/issues/4284/dependencies/blocked_by
gh repo view shanemccaw/MyArchitect --json pushedAt
gh api repos/shanemccaw/MyArchitect/contents/Themes/Colors.xaml --jq .name   # must exist
```
If #4305 is closed/`complete` AND `Themes/Colors.xaml` exists in `shanemccaw/MyArchitect`, remove the stale dep edge, flip `blocked` → `in-flight`, and resume the accessibility retrofit there.

## Resumed — unblocked 2026-09-15T21:34:00Z
- #4305 landed (CLOSED + `complete`); GitHub auto-released the `blocked_by` edge on close (Git #1987). Verified `shanemccaw/MyArchitect` is PRIVATE, pushed 2026-09-15T21:33:08Z, and now holds the real app source (`Themes/{Colors,DarkTheme,Typography}.xaml`, `App.xaml`, `MainWindow.xaml`, `UI_RULES.md`, `MyArchitect.csproj`).
- Flipped `blocked` → `in-flight`; cloned the private repo to a local working copy and started the accessibility build (#4284).

## ✅ DONE 2026-09-15T22:32:04Z — accessibility theming shipped to shanemccaw/MyArchitect
Two commits on `shanemccaw/MyArchitect` main (target repo per issue), both pushed to origin/main:
- **`272d578`** — Colors.xaml: no pure white (TextPrimary `#FFFFFF`→`#E4E4E4`); desaturated/darkened every bright accent+status hue (`#389BFF`→`#5B87B3`, `#007ACC`→`#2E5A78`, `#FFD700`→`#C7A24E`, `#4CAF50`→`#5FB88C`, `#F48771`→`#D98A78`, etc.); consolidated scattered surface hexes onto named roles; added `RibbonIconBrush`. Typography.xaml: dyslexia-aware `LineHeight` (~1.45×, `BlockLineHeight`) on all text styles + default TextBlock; confirmed Segoe UI as dyslexia-appropriate; documented WPF's lack of native letter-spacing honestly. UI_RULES.md: new **§11 Accessibility**. FixedRibbonRenderer: ribbon glyphs use muted `RibbonIconBrush` (were Fluent bright-white). Retrofitted every view `.xaml` (13 files): inline hex/FontFamily literals → theme keys.
- **`932aec4`** — added `Themes/ThemeBrushes.cs` (`Theme.Brush/Solid(key)` resolver); retrofitted all 11 code-behind files to themed brushes; removed dead local `Brush(hex)` helpers; added muted Warning/Error dark-surface brushes + muted-but-hue-distinct Microsoft portal bookmark swatch brushes; bookmark swatch icons now soft off-white.

**Verification (honest):** `dotnet build` succeeded, 0 warnings / 0 errors. Grep confirms **zero** raw color hex or inline FontFamily literals remain in any `.xaml` or `.xaml.cs` (excluding the theme dictionaries + the ThemeBrushes fallback). Every `StaticResource`/`Theme.Brush` key referenced resolves to a key defined in `Themes/`. App launched (built exe) and ran 6s with no XAML resource-resolution exception (validates App.xaml + startup window at runtime; MainWindow/panels need login to reach, covered by the static key-resolution check). The final glare/readability visual judgment is Shane's own to make — that's the one thing this session cannot self-verify.
- Findings filed: none (no new bug discovered mid-build).
- Working tree (this monorepo worktree) clean apart from this bookend; the MyArchitect changes live in the private repo, pushed to its main.
