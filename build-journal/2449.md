# #2449 — Documents (Portal): generate contract pack from real backend

- **Status:** ✅ DONE 2026-09-03T03:20:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** ad972432f (contract pack), 59fda1467 (IN FLIGHT bookend) — merged to
  main at 408db5128

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — recovered empty/unprovisioned worktree
  (C:\wt\2449-q1333 had no .git — matches known #2118 pattern), re-registered via
  `git worktree add` + manual `registerWorktree`/`linkDeps` per the documented
  recovery recipe. Verified `portal/reports`, `portal/insights-documents`,
  `portal/documents` endpoints are real and live in
  `artifacts/api-server/src/routes/portal-documents.ts`. Writing
  `docs/documents-contract-pack.md` next, per #1642 pattern.
- 2026-09-03T03:20:00Z ✅ DONE — wrote `docs/documents-contract-pack.md`, extracted
  from all 8 real, live routes in `portal-documents.ts` (portal/reports,
  portal/insights-documents [+view/+pdf], portal/documents/:id/share,
  public/documents/:shareToken [+doc-views]). Confirmed all 8 are orphaned — no
  frontend caller anywhere in the current tree (`artifacts/msp-portal` retired
  2026-08-29; `artifacts/portal` has only its `/` route). Found and filed a real,
  confirmed bug: the live-rendered-document PDF export (7 doc types) navigates
  Chromium to a frontend route (`{slug}/copilot-readiness/documents/{id}`) that only
  ever existed in the now-retired `artifacts/msp-portal` — every such PDF export
  currently 502s. Filed as #2507 (`bug`, `iteration:1`, `area:document-generator`,
  milestone v1.1, assigned Shane, parented as sub-issue of #1658 "Feature: Documents
  (Portal)", board status set to "AI Batter Up"). Commit
  `ad972432f` merged into `origin/main` at `408db5128`; verified with
  `verify-branch-merged.mjs` ("OK — merged into origin/main. Safe to write DONE.").
  `git status --porcelain` clean.
