# #1958 — Worktree pause/resume can silently re-provision fresh, discarding uncommitted work

- **Status:** ✅ DONE 2026-09-06T15:58:29Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 3ba70eb45 (impl, on main), a72d311b1 (this bookend IN FLIGHT)

## Log
- 2026-09-06T15:37:47Z ⏳ IN FLIGHT — Investigating #1958. Git-reflog audit: the #1550 loss was 2026-08-30; the earliest `rescued/*` branch is 2026-08-31T01:55, so Git #1971's `preserveWorktreeWork` rescue did NOT exist yet when #1550 lost work. Confirmed `7d22bf04e` = pushed `Bookend: #1550 IN FLIGHT` (survived only via origin), `c6f17e594` = an unrelated `--notGit` `#d DONE` commit = the newer base the fresh worktree was recreated onto. Removal-side rescue is now in place, but two gaps remain for the pause/resume path: (A) the sweep removes a paused/resumable worktree with real uncommitted/unpushed work instead of retaining it in place, and (B) a resumed session that lands on a re-provisioned worktree is never told its prior work was rescued elsewhere — it silently sees a clean tree. Implementing both fixes in scripts/dev-server.
- 2026-09-06T15:58:29Z ✅ DONE — Shipped both guards (commit 3ba70eb45, verified ancestor of origin/main via verify-branch-merged.mjs):
  - **Sweep Check 4** (`worktree-lifecycle.mjs`): a worktree that still holds uncommitted/unpushed work (dead owner, past the activity grace) is now RETAINED in place for resume — marked stale, one bounded 24h debug window — instead of removed. Once that window expires it is reclaimed, rescuing to `rescued/*` first (#1971); `--force`/`--all` still reclaims immediately. Bounded, so #2537's no-permanent-leak guarantee is preserved.
  - **Re-provision notice**: new `findOrphanedRescueBranches` + `writeReprovisionMarker` drop a visible `.worktree-reprovisioned.json` into a (re-)provisioned worktree naming the `rescued/<name>-*` branch(es) its prior work went to, and `provision-worktree.mjs` returns `priorWorkRescued` (surfaced on the human path + in BuildConsole's activity log via `WorktreeProvisionService`). A resumed session is never silently handed a clean `git status` over discarded work.
  - `detectWorktreeWork`: single shared dirty/unpushed truth used by both the removal rescue and the sweep, with BuildConsole markers filtered.
  - **Verification**: new `worktree-reprovision.selftest.mjs` (19 assertions incl. the full retain→reclaim-with-rescue lifecycle, mtime-backdated for determinism) + updated `worktree-sweep.selftest.mjs` (8 assertions) — both green. BuildConsole `dotnet build` clean (0 warn/0 err).
  - **Finding filed**: #3021 (parented under Feature #1789, AI Batter Up) — pre-existing flaky `scripts/dev-server/selftest.mjs` restart-coalescing scenario (7/1/1 failures across three runs; unrelated to worktree code). Not caused by this build.
