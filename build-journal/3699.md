# #3699 — Fully disconnect Build Matrix for now

- **Status:** ✅ DONE 2026-09-11T21:10:00Z
- **Scope:** buildconsole

## Log

- 2026-09-11T21:00:00Z — ⏳ IN FLIGHT. Real, immediate mitigation requested by Shane (sub-issue
  of #1788): disconnect Build Matrix while #3698's real RenderQueue leak gets fixed, regardless
  of #3689 already fixing Build Matrix's own leak.
- Change: `desktop/BuildConsole/Controls/BuildQueuePanel.xaml` — `MatrixChip` forced
  `Visibility="Collapsed"` so the chip never renders in the QUEUE header and can't be clicked
  open.
- `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` — added `private const bool
  BuildMatrixDisabled = true;`. `BtnMatrixChip_Click` early-returns when disabled.
  `RenderMatrixDrawer()` (called on every queue refresh tick) now early-returns when disabled,
  after force-collapsing `MatrixDrawer` — none of the real polling/rebuild/slot-reconcile logic
  (`KeyedSlotCardHost.Reconcile`, `MatrixSlotCard`, `BuildMatrixSnapshotNow`, etc.) runs anymore.
- Underlying code (`KeyedSlotCardHost`, `MatrixSlotCard`, the "Tab" send-to-document feature,
  `MainWindow.BuildMatrixTab.cs`) left fully intact per the issue — this is a disable, not a
  retirement. Re-enable by flipping `BuildMatrixDisabled` back to `false` and the XAML
  `Visibility` back to `Visible`.
- Verified: `dotnet build -c Debug` succeeds, 0 errors (10 warnings total, including 2 new
  expected `CS0162 Unreachable code` warnings inside the now-dead branches of
  `BtnMatrixChip_Click`/`RenderMatrixDrawer` caused by the `const` flag).
- ✅ DONE — 2026-09-11T21:10:00Z. Commit: 0f323824ec77ad8060799f1a18b72938f2d45146 (pushed to
  origin/main).
