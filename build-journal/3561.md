# #3561 — Redirect Admin-Panel/internal AI calls (workflow-executor nodes) to MCP; leave Portal calls untouched

- **Status:** ✅ DONE 2026-09-13T07:14:06Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 664609f31 (bookend IN FLIGHT); no further source commits — see "Real conclusion" below.

## Log
- 2026-09-13T07:01:49Z ⏳ IN FLIGHT — Starting classification pass on the 9 workflow-executor.ts
  node types (7 Anthropic + 1 OpenAI call sites) per Shane's scope-correction comment on #3561:
  classify each as Portal-reachable (leave alone), Admin-Panel/internal-only (redirect to MCP),
  or ambiguous (leave alone, flag). Tracing real callers before touching any code.

- 2026-09-13T07:14:06Z ✅ DONE — Full evidence-backed classification completed via real caller
  tracing + direct queries against local `wf_definitions`/`wf_versions`/`wf_triggers`/
  `ai_usage_events`. Real disposition per call site:

  - **`action` (actionType `generate_document`, line ~3092-3239)** — **PORTAL-REACHABLE.**
    Confirmed: workflow definition #29 ("Assessment Document Generation — Service-Mapped,
    Sequenced SOW") contains `generate_document` action nodes and is triggered directly by
    the `portal.first_login` event (`emitWorkflowEvent("portal.first_login", ...)` in
    `routes/portal-first-login.ts`) — a genuine customer-facing Portal flow (a customer's
    first portal login generates their real SOW/assessment documents). Also used by
    `purchase.completed`/`sow.generate`/`sow.generation_stalled`-triggered definitions
    (#3, #16, #21), all customer-facing. `ai_usage_events.node_type='consolidated_sow_generator'`
    shows 10 real historical calls. **Left completely untouched**, per the build's own
    instruction.

  - **`generate_article`, `fetch_news_headlines`** — **Admin-only, but exempt by the
    issue's own stated carve-out.** Both are schedule/cron-only triggers (`Weekly Article
    Generator` weekly cron, `Generate Campaign from Current Events` cron) with **no human
    in the loop** — exactly the "fully automated pipeline with no human-in-the-loop to
    relay through Claude.ai" exception #3561's own issue body names as a legitimate reason
    to keep a direct call. `generate_article` has real recent traffic (6 calls, most recent
    2026-09-07). Converting these to require a live Claude.ai/Desktop chat session would
    **break real, currently-working weekly automation** with no human available to replace
    it. Recommendation: **keep as direct calls**, do not redirect. Not code-changed this
    session; recorded here as the classification conclusion.

  - **`ask_ai`, `topic_picker`, `generate_image`, `generate_landing_page`** — **Admin-only,
    dead code.** Zero rows ever in `ai_usage_events` for any of these node types. Zero
    workflow-definition versions of ANY status (published/draft/archived) reference
    `topic_picker`/`generate_image`/`generate_landing_page` at all. `ask_ai`'s one host
    workflow (#6, "Presentation Phase Generator") has a trigger event
    (`presentation.phases_requested`) that no live code path ever emits — unreachable
    except via an Admin manual test-fire. Building new MCP-tool infrastructure for AI
    capability that has never executed once in production is speculative work, and
    whether to invest in migrating vs. simply removing this dead surface is a real product
    decision, not a code-only call. **Filed as #3956** for Shane's decision, parented to
    #1096 (no closer Feature-tier parent above #3561), labeled `bug`, board status "AI
    Batter Up".

  - **`zoho_books_daily_ai_rollup`** — **Not a redirect candidate at all.** Confirmed via
    direct read of its case body (`workflow-executor.ts:7332-7339`): it only aggregates
    *existing* `ai_usage_events` cost rows into a Zoho Books expense line — it makes no
    Anthropic/OpenAI call whatsoever. #3561's own audit table mis-described it as
    "AI-generated financial summary." Correction folded into **#3956**.

  - **`generate_script` (workflow-executor.ts case, line 5987-6072)** — makes **no**
    direct AI call either; it inserts a `pending_script_handoffs` row and pauses the run
    for a human admin to write the script by hand. The real "PowerShell script
    generation" AI capability the issue's audit table was actually describing lives in a
    **different file** entirely:

  - **`generateScriptFromService`/`generateScriptFromDocument` (`ps-script-gen.ts:375,502`)**
    — two real, live Anthropic calls, called exclusively from `routes/admin-ps-scripts.ts`
    (all routes `requireAdmin`, `/admin/ps-scripts/*` — confirmed Admin-only). Neither is
    wrapped in `withAiAttribution`, so they land unmetered in
    `ai_usage_events.node_type='unattributed'` — that bucket's two most recent rows
    (2026-09-09, 4 days before this session) use model `claude-haiku-4-5` with no version
    suffix, exactly matching `ps-script-gen.ts`'s model string — real, recent, active
    usage. Unlike `generate_article`/`fetch_news_headlines`, this feature IS
    human-in-the-loop (Shane clicks "Generate" per script in the Admin Panel UI), so it's
    the single best real fit for the MCP-redirect pattern of everything traced this
    session. **Not redirected in this session** — doing so replaces a synchronous,
    actively-used Admin Panel button with a Claude.ai/Desktop-relay flow, a real
    Shane-visible UX change to a feature with usage this week, which needs his sign-off
    before implementation (also outside the literal 9-node-type scope named in the
    dispatch). **Filed as #3957**, parented to #1096, labeled `bug`, board status "AI
    Batter Up".

  **Net result: no source changes this session.** The one Portal-reachable site was
  correctly left untouched per instructions. Every other real site either (a) is
  legitimately exempt per the issue's own unattended-pipeline carve-out, (b) has never
  executed once in production (a real migrate-vs-delete decision, not a code task), or
  (c) is a live Admin-only feature whose redirect is a genuine UX-changing architecture
  decision needing Shane's sign-off — exactly the "real architecture work... to be
  proposed and signed off before implementation" the original issue body itself called
  for. Findings #3956 and #3957 carry the concrete evidence and decision points forward.

  Verification: `git status --porcelain` clean; branch `agent/3561-q2638` confirmed
  merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`. No
  build/typecheck run — no source files were changed.
