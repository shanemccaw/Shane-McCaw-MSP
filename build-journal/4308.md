# Build Journal — #4308

- **Status:** ✅ DONE 2026-09-15T22:53:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-15T22:41:00Z — First dispatch attempt: found #4307 still open/in-flight with no §4.4 in `BUILD_QUEUE_METHOD.md`. Stopped, labeled `blocked`, commented, pushed a BLOCKED bookend (`ca5586570`).
  - 2026-09-15T22:47:00Z — Re-dispatched. Re-checked #4307: confirmed it landed (`3550cebf4 Add §4.4 Plan & Walkthrough documents standing requirement (#4307)`, `867806087 Bookend: #4307 DONE`), issue #4307 labeled `complete`. Verified §4.4 "Plan & Walkthrough documents (standing requirement, every build)" actually exists at `desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md:298`, referencing `build-journal/{N}-plan.md`, `## Changes Made`, `## Verification Results` — so #4308's cross-references to those names/headers are real. Removed the stale `blocked_by` edge to #4307, flipped labels `blocked`→`in-flight`.
  - 2026-09-15T22:53:00Z — Read `BUILD_QUEUE_BLOCKING_AND_GATING.md` in full. Added new §8 "What a chat checks first when asked about a build's state" after existing §7 ("Verification before closing"), before the old §8 ("Common mistakes"), which is renumbered to §9; old §9 ("Real incidents") renumbered to §10. New §8 covers both real gaps from the issue body: (8a) the read order for a chat verifying a build — plan.md, then DONE's Changes Made/Verification Results, then the raw diff only when documents are missing/incomplete/high-stakes/inconsistent; (8b) the procedure when no DONE bookend exists — check board status, check for real unmerged commits on an `agent/{N}-*` branch and flag stranded work to Shane rather than discarding or re-dispatching over it, otherwise treat as abandoned and safe to re-dispatch, and never close/DONE without §1/§7's real verified bookend. Fixed one stale in-file cross-reference (`§9`→`§10` at the "phantom hashes" note in §7). Checked repo-wide for other files citing this doc's section numbers — none found besides a filename-only reference in `BUILD_QUEUE_METHOD.md`.

## Changes Made
- [BUILD_QUEUE_BLOCKING_AND_GATING.md](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/1c08db0fb/BUILD_QUEUE_BLOCKING_AND_GATING.md) — added §8 (verification order + no-DONE procedure), renumbered old §8→§9 (Common mistakes) and §9→§10 (Real incidents), fixed the one stale internal `§9` cross-reference to `§10`.

## Verification Results
Documentation-only change per the issue — no code/build/test run required. Verified: (1) §4.4 in `BUILD_QUEUE_METHOD.md` genuinely exists before writing any cross-reference to it; (2) grepped the whole file post-edit for `§[0-9]` to confirm every remaining section reference still points at the correct renumbered section; (3) grepped the repo for other files referencing this doc's section numbers — none found; (4) `git status --porcelain` clean; (5) `node scripts/dev-server/verify-branch-merged.mjs` → `OK — 'agent/4308-q2985' (1c08db0f) is merged into 'origin/main'. Safe to write DONE.` Commit: `1c08db0fb`.
  - 2026-09-15T22:41:00Z — Claimed dispatch, pulled #4308's full body + its single BUILD comment. Confirmed #4307 ("Standing process: add Plan + Walkthrough documents...") has NOT landed: `gh issue view 4307` shows `state: OPEN`, label `in-flight`, no DONE comment; `build-journal/4307.md` does not exist yet (only an IN FLIGHT commit `29c6c80f0 Bookend: #4307 IN FLIGHT` on origin/main); `desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md` has no §4.4 section (grep for `4.4` returns nothing). #4308's own instructions require confirming #4307 landed before writing, "so the document names/headers you cross-reference are real and correctly spelled" — that precondition is false. Stopping without writing the new section rather than guessing at §4.4's eventual header name/content.
  - `blocked_by` edge to #4307 already existed (set by the `--blocked-by 4307` dispatch flag) — left in place, it correctly reflects reality.
  - Labeled #4308 `blocked`, removed `in-flight`; posted a comment on #4308 stating the wait and why.
