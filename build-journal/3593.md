# #3593 — Top bar: consolidate Account/Location/Auto/Conservation/Drain into one dropdown menu

- **Status:** ✅ DONE 2026-09-11T02:18:14Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 909ea24be (rebased onto main as 0232d8ff8)

## Log
- 2026-09-11T02:14:29Z ⏳ IN FLIGHT — consolidating the four top-bar toggle Borders
  (Account/Location/Auto/Conservation) + BtnDrainCapped Button in MainWindow.xaml into
  one clickable summary control + a real ContextMenu dropdown, reusing the existing
  BuildQueuePanel "⋯" overflow-menu mechanism (Git #3444) and each toggle's exact
  existing click-handler logic per Shane's 2026-09-11 clarification (single click
  target, no separate menu button).
- 2026-09-11T02:18:14Z ✅ DONE — replaced the five always-visible top-bar controls with
  one `TopBarSummaryBorder` (+ `TopBarSummaryText`) that opens a `TopBarSummaryMenu`
  `ContextMenu` on click, same programmatic-open mechanism `BtnQueueMenu_Click` uses in
  `BuildQueuePanel` (Git #3444) since a `Border`/`ContextMenu` doesn't open on plain
  left-click. Each menu item (`MiAccountToggle`, `MiLocationToggle`,
  `MiAutomationToggle`, `MiConservationToggle`) is a thin `RoutedEventArgs` wrapper
  (`Mi*_Click`) that calls the pre-existing `Top*Toggle_Click(sender, null!)` body
  unchanged; `MiDrainCapped` binds `BtnDrainCapped_Click` directly since its signature
  already matches `MenuItem.Click`. All five settings' underlying logic
  (`DefaultAccount`, `LocationMode`, `UsageAutomationEnabled`, `ConservationModeEnabled`,
  Drain's confirm-then-release) is byte-for-byte unchanged — this was presentation-only.
  New `RefreshTopBarSummaryUi()` aggregator composes the collapsed line (e.g. "Primary ·
  Home · Auto Off · Conservation Off (53% used · resets 4d 1h 14m)") from the same
  `TopAccountToggleText`/`TopLocationToggleText`/`TopAutomationToggleText`/
  `TopConservationToggleText`/`TopConservationUsageText` `TextBlock`s (now living inside
  `MenuItem` headers instead of the old always-visible Borders) — called from the end of
  each `RefreshTop*Ui` method and from `UsageMeter_StatusChanged` so the summary never
  goes stale relative to whichever control last changed. `dotnet build
  BuildConsole.csproj -c Debug` — Build succeeded, 0 errors (pre-existing unrelated
  `FocusModeBar` warnings only), both before and after rebasing onto `origin/main`.
  `node scripts/dev-server/verify-branch-merged.mjs` — OK, `agent/3593-q2281` (0232d8ff)
  merged into `origin/main`. `git status --porcelain` clean. Not launching
  `BuildConsole.exe` for manual click-through per the standing prohibition (Git #2156)
  — build success + code-level trace of every real handler being reused is the
  sanctioned verification for this WPF surface (no test-manifest requirement here).
