# #2445 — Email Authentication Setup: contract pack from real backend

- **Status:** ✅ DONE 2026-09-03T03:19:20Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 359268967 (contract pack), 48055dd6 (IN FLIGHT bookend)

## Log
- 2026-09-03T03:13:14Z ⏳ IN FLIGHT — confirmed `GET /api/portal/email-auth-status`
  (`portal-email-auth-status.ts`) is real and live; writing
  `docs/email-authentication-setup-contract-pack.md` per the #1642 pattern.
- 2026-09-03T03:19:20Z ✅ DONE — wrote `docs/email-authentication-setup-contract-pack.md`,
  extracting the one endpoint (`GET /api/portal/email-auth-status`) plus its
  producing check (`runDnsCheck`/`exchange:dkim-spf-dmarc-status`), the check's real
  DB-queried mapping/severity rules, drift registration, and the
  `security.emailAuthFindingCount` dashboard-registry aggregate that reads the same
  underlying data through a different path. Confirmed the route is real, live, and
  currently orphaned (no frontend caller in `artifacts/portal`; its only prior
  caller, `artifacts/msp-portal/src/pages/email-auth-setup.tsx`, was retired
  2026-08-29). `verify-branch-merged.mjs` confirmed OK; `git status --porcelain`
  clean. No findings filed — the route forwards 4 of the check's 8 extracted
  fields, honestly documented in §2 as Design's call, not a bug.
