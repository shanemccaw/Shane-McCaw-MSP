# #3928 — Retire old portal-v2 risk-register page (#1647 Group A)

- **Status:** ✅ DONE 2026-09-13T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** no-op — nothing to change; verified against a80cea (HEAD at time of check)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — investigating whether `portal-v2-risk-register.tsx`, its `*Data.ts` fixture, route registration and nav entry still exist.
- 2026-09-13T00:00:00Z ✅ DONE — Confirmed the entire `artifacts/msp-portal` tree (which held `portal-v2-risk-register.tsx` and everything else portal-v2) was already deleted wholesale by commit `f40438cdc` ("Portal scaffolding: create artifacts/portal, retire artifacts/msp-portal"). `grep -rn 'portal-v2-risk-register' artifacts/msp-portal/src/` cannot even run — the directory itself no longer exists (confirms zero hits by construction). No `*Live.ts` hook for this page exists anywhere in the repo either, so there was nothing to decide about reuse.
  Verified the real replacement (#1487) is what's actually wired: `artifacts/portal/src/App.tsx:92` routes `/risk-register` to `artifacts/portal/src/pages/risk-register.tsx`, which reads through `artifacts/portal/src/lib/risk-register-api.ts` (`useQuery`/`useMutation` via `@tanstack/react-query`) against the real `artifacts/api-server/src/routes/portal-risk-register.ts` endpoints — no fixture data.
  No code change was needed or made. Nothing to commit for the retirement itself; only this bookend file.
