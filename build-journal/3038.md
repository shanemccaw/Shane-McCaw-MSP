# #3038 — Remediation Tracking: wire the findings-derived checklist (checklist, fix-routes, reveal, bypass-resolutions)

- **Status:** ✅ DONE 2026-09-06T20:02:10Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** e351f5466 (page/hooks/components), 5adf559c9 (honest runTest note + finding pointer)

## Log
- 2026-09-06T19:42:14Z ⏳ IN FLIGHT — wiring §1b-1e of docs/remediation-tracking-contract-pack.md
  (checklist GET/PUT/raise-change/decline-to-risk, fix-routes, reveal, bypass-resolutions) to
  `Design/portal/design_handoff_full_site/screens/Remediation Tracking.dc.html`'s "Your findings"
  and "Fixed outside change control" tabs. No `pages/remediation-tracking.tsx` exists yet on
  `main`; sibling issues #3037 (programme tab, §1a) and #3039 (pillar-scores + export, §1f-1g)
  cover the other two tabs on the same page — building the page shell now with checklist+bypass
  fully wired and the programme tab left as an honest placeholder pending #3037.
- 2026-09-06T20:02:10Z ✅ DONE — shipped `lib/remediation-checklist-{types,api,visuals}.ts`,
  `components/remediation-checklist/{FixRouteLegend,ChecklistItemCard,BypassResolutionCard}.tsx`,
  and `pages/remediation-tracking.tsx` wiring all 3 real GET routes (checklist, fix-routes,
  bypass-resolutions) plus PUT/raise-change/reveal, per §1b-1e. #3037 landed concurrently with
  the same page filename (programme tab, §1a) — merged properly rather than clobbering: the page
  now composes both real tabs (`RemediationProgrammeTab` for programme, mine for
  checklist/bypass), resolved via a real rebase conflict, not a blind take-mine/take-theirs.
  Verified: `tsc --noEmit` clean, `vite build` succeeds, both after every rebase. `shaneapp://runTest`
  actually invoked twice (real attempts, not skipped) — both hit the same dev-server-checkout-stale
  cause #3037's own manifest already documented; recorded honestly in
  `test-manifests/portal/remediation-tracking-checklist.json`'s own notes rather than claimed as
  passing. Test manifest registered in `_regression-suite.json`; renamed to
  `remediation-tracking-checklist.json` (not `remediation-tracking.json`) after discovering #3037
  had already claimed that filename for the programme tab's own manifest — same discover-before-
  create discipline applied mid-build once the collision surfaced.
  Filed #3062 (parented under Feature #1489, board status AI Batter Up): the contract pack's
  §1b/§7.1 are stale — they don't document the checklist's own `decline-to-risk` route (#2869,
  already closed) and still describe #2827's accepted_risk guard as an open gap when it's fixed.
  `git status --porcelain` clean; `verify-branch-merged.mjs` confirms ancestor of `origin/main`.
