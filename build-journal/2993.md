# #2993 — Risk Register: wire the portal UI to the real endpoints (v2)

- **Status:** ✅ DONE 2026-09-06T19:20:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 27db670b7 (merged to main; squashed from ae04581fe/3916c8174/69514ccf8
  across three required rebases onto a fast-moving main)

## Log
- 2026-09-06T15:05:00Z ⏳ IN FLIGHT — Design export confirmed landed at
  `Design/portal/design_handoff_full_site/screens/Risk Register.dc.html`, contract
  pack at `docs/risk-register-contract-pack.md`. Building the real `artifacts/portal`
  page against `GET /api/portal/risk-register`, `GET /api/portal/policy-decisions`,
  and `POST /api/portal/risk-register/:rbdId/accept`. RBD whole-document
  versioning/signing (`portal-rbd-document.ts`, #1512) read-only listing included;
  full document viewer/sign UI deferred per the design's own "prototype does not
  carry the document view yet" note — flagging as its own finding if scope allows.
- 2026-09-06T19:20:00Z ✅ DONE — Wired `/risk-register` in `artifacts/portal`
  (route + sidebar nav entry) to the real, live endpoints: risk register list with
  status chips/liability roll-up/three-clocks card, the likelihood×impact heat map,
  per-risk expandable facts + accept flow (name/statement/checkbox → confirm dialog
  → `POST .../accept`, respecting the real role-based-authority 403/409 gates and
  the write-once guard), the RBD version-history panel (`GET .../versions`, #1512),
  and the risk-derived Policy Decisions card (`GET /api/portal/policy-decisions`,
  distinct from the standalone `policy_decisions` table per contract pack §0.1).
  No fixture module; every number comes from the wire.

  BUILD IT fixes (two real DB columns that existed but the wire never served,
  discovered while wiring): `WireAcceptance.status` (the acceptance's own
  pending_signature/active/revoked state — without it the UI could not tell a
  revoked signature from an active one) and `WireRisk.obligation` (the free-text
  catalogue citation the sibling `WirePolicyDecision` already served). Both are
  additive TS-interface + query-projection changes only — no schema/migration
  needed, both columns already existed.

  Verification: `tsc --noEmit` clean on both `artifacts/portal` and
  `artifacts/api-server`; `vite build` clean on the portal. Full real
  end-to-end pass against a build of this exact commit run standalone on
  :8099 against the real local Postgres (testbed seed re-run from
  `lib/db/migrations/manual/2026-08-21-portal-v2-risk-register-testbed-seed.sql`):
  confirmed `obligation`/`accepted.status` now present on `GET
  /api/portal/risk-register`, `GET /api/portal/policy-decisions` 200, a real
  `POST .../accept` 201 with the new `status:"active"` field then an immediate
  409 on retry (write-once guard), `GET .../versions` returns a clean empty
  array pre-#1512-render. The one mutating test (RBD-2026-103) was reset back to
  its seeded `pending_signature`/`Open` state via direct SQL afterward.

  `test-manifests/portal/risk-register.json` written (apiTests A1-A5 covering
  the read shapes + both new fields, the 409 write-once guard, the 404 on a
  fake rbdId, the honest empty version-history read; uiSteps covering the
  honest sign-in-required gate, since `artifacts/portal` had no real login UI
  as of this build's start — #2991 landed one mid-build, see below), registered
  in `test-manifests/_regression-suite.json`. Confirmed `shaneapp://runTest`
  itself works in this environment (ran `hello-world-sql.json` green). Could
  NOT run this manifest itself via `shaneapp://runTest` in-session: BuildConsole
  resolves manifests from the shared dev-server checkout
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`), which does not have this
  worktree's commit until the normal post-build merge-back — "manifest not
  found" is the real, expected error for a manifest that only exists in an
  unmerged worktree, not a broken protocol. It will read green once merged
  (the apiTests were already independently proven against a local instance of
  this exact code above).

  Found #2991 (Auth Core) landed mid-build with a real `/login` page — the
  manifest's uiSteps note this as a follow-up opportunity (extend with a real
  login block) rather than blocking on it.

  Filed **#3058** (parented under Feature #1487, board status "AI Batter Up",
  labeled `bug`): the whole-document RBD viewer + drawn-signature capture
  (`GET/POST .../versions/:versionUid/document` and `.../sign`, #1512) remain
  live, uncalled endpoints — the design export itself defers this exact scope
  ("This prototype does not carry the document view yet"), and #2993's build
  matched it with an honest informational dialog rather than faking a viewer.

  Merged to `main` at `27db670b7` after three required rebases (main moved
  three times during the session under real concurrent load — sibling builds
  for Auth Core (#2991) and SOPs/Runbooks landed mid-session; both rebases were
  clean auto-merges except `App.tsx`'s route table, resolved by hand each time
  and re-typechecked/rebuilt after each). `verify-branch-merged.mjs` confirms
  ancestry. `git status --porcelain` clean.

  One out-of-band note: a manual mid-build `request-restart.mjs --buildSet
  RiskRegister` invocation (not required for a BuildConsole-launched build —
  the merge-back at completion is BuildConsole's own job) hit a real "add/add"
  conflict merging a concurrent sibling build's own `build-journal/2830.md` into
  the shared dev-server checkout; the shared checkout itself was left clean
  (confirmed), only the buildSet's own bookkeeping recorded a failed slot. Not
  investigated further — out of #2993's scope, and the buildSet mechanism has
  its own failure/close backstop for exactly this.

  Pre-existing, not mine, left alone: the shared dev-server checkout
  (`C:\Source\...\Shane-McCaw-MSP`) had a locally-modified `pnpm-lock.yaml`
  when I first inspected it (unrelated to this build, and it is 95+ commits
  behind `origin/main` on its own local `main` branch — a dev-server-checkout
  freshness issue, not touched here).
