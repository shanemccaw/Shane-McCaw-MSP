# #2973 — Repair empty node_modules for artifacts/api-server and lib/db

- **Status:** ✅ DONE 2026-09-06T14:05:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 58bdf5e73 (repair applies to node_modules only, not tracked in git; bookend/commit is this one)

## Log
- 2026-09-06T13:48:00Z ⏳ IN FLIGHT — confirmed root cause: `artifacts/api-server/node_modules`
  had only `.vite`/`.vite-temp` cache dirs (0 real package entries), `lib/db/node_modules` was
  entirely missing. Reproduced the real failure via
  `pnpm --filter @workspace/api-server test -- src/lib/retainer-hours.test.ts` → `'vitest' is
  not recognized` (missing `.bin` link), matching the issue's `ERR_MODULE_NOT_FOUND` symptom.
  Ran the sanctioned exception `pnpm install` directly in the shared main checkout
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`, not a worktree) per this build's explicit
  dispatch instructions. Install completed in 35.4s, 1120 packages resolved, **0 downloaded**
  (all reused from local store) — no meaningful bandwidth cost. Post-install:
  `artifacts/api-server/node_modules` now has 33 entries, `lib/db/node_modules` now has 7.
- 2026-09-06T14:05:00Z ✅ DONE — Re-verified via
  `pnpm --filter @workspace/api-server test -- src/lib/retainer-hours.test.ts` (real error
  changed from `'vitest' is not recognized` to the script actually running) and directly via
  `npx vitest run src/lib/__tests__/azure-rm.test.ts src/lib/retainer-hours.test.ts` with
  `DATABASE_URL` exported from `.env.local`: **2 test files, 32 tests, all passed.** An
  intermediate run without `DATABASE_URL` exported in that particular shell surfaced 18
  azure-rm failures + a `DATABASE_URL must be set` error from `lib/db` — confirmed as a
  shell-env artifact of that one Bash invocation, not a real regression or a node_modules gap
  (re-ran with the var exported and every test passed). No fix needed there, no finding filed.
  No fixture/hardcoded data touched. `node_modules` is gitignored so the repair itself has no
  diff; only `build-journal/2973.md` is a tracked change. Rebased onto latest `origin/main` and
  pushed directly (commit 58bdf5e73) per repo convention (commit-to-main, no PR).
  `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean.
