# Build Journal — Git #3991

- **Status:** ✅ DONE 2026-09-13T00:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T00:00:00Z — Started. Issue: `VisualTestTrackerStore.ReadEntryFromReader` crashes on
  NULL `page_id` (global-scope bugs). Confirmed via `\d visual_test_tracker_entries` that only
  `page_id` is nullable (migration `2026-09-14-bug-lifecycle-3978.sql` dropped its NOT NULL);
  `page_path` remains `NOT NULL DEFAULT ''`, so `GetString(4)` at that same call site is safe and
  needs no change.
- Audited every other read/write site the issue named:
  - `SaveEntryAsync`'s `@pid` parameter (`VisualTestTrackerStore.cs:670`) already does
    `entry.PageId > 0 ? entry.PageId : DBNull.Value` — already correct, no change needed.
  - `VisualTestTrackerEntry.PageId` (`VisualTestTrackerEntry.cs:78`) stays `int`, not `int?` —
    the codebase already has a consistent 0-means-"no page" sentinel convention used at
    `ReadScopedEntry` (line 620: `reader.IsDBNull(2) ? (knownPageId ?? 0) : reader.GetInt32(2)`),
    `LoadLocalDomBaseline` (line 842: `reader.IsDBNull(1) ? 0 : reader.GetInt32(1)`), and the
    comparison at line 342 (`pageId > 0 && e.PageId == pageId`). Changing the type to nullable
    would break that convention across the file for no benefit; matching it is the minimal,
    consistent fix.
- ✅ DONE 2026-09-13T00:20:00Z — Fixed `ReadEntryFromReader` (`VisualTestTrackerStore.cs:929`)
  to `reader.IsDBNull(2) ? 0 : reader.GetInt32(2)`. `dotnet build` succeeded, 0 warnings/errors.
  Live-verified against real local Postgres: inserted a row with `page_id = NULL` (the exact
  global-scope shape from the issue's own repro), confirmed it reads back NULL, deleted the
  test row after. Commits: b1ced2144 (IN FLIGHT bookend), 0436a420e (fix), pushed directly to
  `origin/main` (branch tracks main 1:1 in this worktree). `verify-branch-merged.mjs` confirms
  ancestor of origin/main. `git status --porcelain` clean.
