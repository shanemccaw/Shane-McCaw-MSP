# #2977 — graph_entity_types.base_type stores raw EDM alias, not resolved qualified_name

- **Status:** ✅ DONE 2026-09-07T15:20:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 67fd5f39181ddd1230993df0a8ecfbd38b9b28ef (merged to main as 6599b583d)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — fix build-resource-model.mjs to store resolved base_type, re-run against local DB, verify join count
- 2026-09-07T15:20:00Z ✅ DONE — `scripts/config-state/build-resource-model.mjs:156` now writes
  `model.resolveType(t.baseType, t.namespace)?.qualifiedName ?? null` instead of the raw
  `t.baseType`. Verified by downloading only the two Graph `$metadata` XML files (not the full
  m365dsc tarball — unnecessary for this fix, avoided the extra download), parsing them with
  `loadGraphModel`, and re-resolving all 2363 existing `graph_entity_types.base_type` rows in the
  local DB with the same `resolveType` call the fixed code now uses at write time. Result:
  `deviceConfiguration.base_type` went from `graph.entity` (alias, unjoinable) to
  `microsoft.graph.entity` (resolved). Join count
  (`base_type = qualified_name AND graph_version` match) went from 20/2363 to **2363/2363**. The
  one-off correction script and downloaded `.cache/config-state/*.xml` were scratch — deleted after
  use, not committed (`.cache/` is gitignored anyway). No fixture/hardcoded data; no schema change.
  Branch verified merged into origin/main via `verify-branch-merged.mjs` before writing DONE.
  Working tree clean (`git status --porcelain` empty).
