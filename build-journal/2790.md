# #2790 — api-server: 76 real test-fixture type errors across 13 lib/*.test.ts files (isCustomerFacing + others)

- **Status:** ✅ DONE 2026-09-04T05:48:48Z
- **Scope:** platform

## Log
- 2026-09-04T05:34:16Z — Session started. Pulled issue body + comment. Fixing shared `isCustomerFacing` gap first (64/76 TS2741 errors, monitor-check test fixtures), then the remaining TS2322/TS2451 across the other 12 files individually.
- 2026-09-04T05:48:48Z — ✅ DONE — `pnpm --filter @workspace/api-server run typecheck` reaches zero errors (verified real re-run). Fixed all 76 real fixture errors across 13 `src/lib/*.test.ts` files:
  - **64× TS2741** — added missing `isCustomerFacing: true` to 8 monitor-check catalog fixture literals in `monitor-executor.test.ts` (`baseCheck`, `labelledCheck`, `fanOutCheck`, `psCheck`, `spCheck`, `ppCheck`, `dnsCheck`, `armCheck`) plus one each in `onedrive-sharing.test.ts` and `sharepoint-sharing.test.ts`.
  - **2× TS2451** — `monitor-executor.test.ts` had two module-level `const SUBSCRIBED_SKUS` declarations (real block-scope redeclaration AND a genuine esbuild/JS syntax error that blocked the whole file from ever transforming/running under vitest, before or since). Renamed the second (nested nested-shape nested fixture) to `SUBSCRIBED_SKUS_NESTED` and its ~10 usages.
  - **9× TS2322** (`denominatorKey`) — added a missing `denominatorKey: null` default to the shared `makeRule()` `SignalDerivationRule` fixture helper in `drift-engine.test.ts`, `engine-determinism.test.ts`, `finding-point-impact-555.test.ts`, `forecasting-engine.test.ts`, `health-engine.test.ts`, `msp-engine.test.ts`, `pillar-coverage.test.ts`, `pillar-trend.test.ts`, `telemetry-comparison.test.ts`.
  - **1× TS2322** (`fixRoute`) — `remediation-raise-change.test.ts` fixture used stale value `"manual"`, not a member of `RemediationFixRoute`. Fixed to `"you_must_run"` (matches the fixture's own `affordance: "copy"` per `FIX_ROUTE_AFFORDANCE`).
  - Commit: `373027324060b0a8a7b928d096ba6169d1d8e47d` on `agent/2790-q1520`, rebased onto `origin/main` and pushed directly to `main` as `091cf935977f0f27e2b359a61725e7bde0dc8b27`. `verify-branch-merged.mjs` confirms OK.
  - **Real runtime verification (not just typecheck):** ran the vitest suites for all 13 touched files with `DATABASE_URL` loaded. 12 of 13 files pass fully (361/377 tests once DB env was loaded correctly). `monitor-executor.test.ts` surfaces 16 pre-existing real test failures (missing `PowerPlatformNotRegisteredError` export on the `../power-platform-admin` mock) that were previously invisible because the file's duplicate `SUBSCRIBED_SKUS` declaration was a hard syntax error blocking it from ever running at all — confirmed pre-existing by reproducing the same 16 failures against the file exactly as it stood before this session's edits. `engine-determinism.test.ts` surfaces 3 pre-existing real score-mismatch failures, also confirmed pre-existing the same way (my only edit there was the additive `denominatorKey: null` default).
  - **Findings filed** (both parented to #1096, no Feature-tier parent, board status AI Batter Up):
    - #2823 — `monitor-executor.test.ts`: `power-platform-admin` mock missing `PowerPlatformNotRegisteredError` export, 16 real test failures.
    - #2824 — `engine-determinism.test.ts`: 3 real pre-existing score-mismatch failures (health/msp roll-up under-count).
  - `git status --porcelain` clean.
