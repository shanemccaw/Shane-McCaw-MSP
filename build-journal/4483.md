# #4483 — Free Scan engagement account recovery (lost password / lost authenticator)

- **Status:** ✅ DONE 2026-09-17T14:08:47Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 961f20289 (IN FLIGHT bookend), 1cc71f136 (API + schema + migration + live-db test), 558668fd4 (/scan/account UI + AdminV2 screen), 61a1bc899 (verify-code timing fix), f8cf4bcfd (manifest)
- **Migration:** lib/db/migrations/manual/2026-09-17-free-scan-account-recovery-4483.sql (additive; run against local DB this session; appended to #1630)

## Log
- 2026-09-17T13:26:05Z ⏳ IN FLIGHT — building a recovery flow scoped to free_scan_accounts: emailed code → new password; lost authenticator requires emailed code + an out-of-band step before MFA re-enrol; sessionVersion bump on either. No change to /auth/forgot-password or users.
- 2026-09-17T14:08:47Z ✅ DONE — Recovery for free_scan_accounts shipped. Lost password: emailed code -> new password; bumps session_version, clears lockout, cancels any factor reset in flight, does NOT sign in (second factor still required). Lost authenticator: emailed code + current password lodges a request -> platform admin approves in AdminV2 (Watch > Needs a decision > Factor resets) with a required identity-check note -> within 72h, code + password again + proof of the new factor swaps it in (held in pending_* until proven), bumps session_version, signs in. The emailed code alone can never re-enrol a factor. Nonce-bound single-use recovery tokens on their own derived key; 202-before-lookup send-code; uniform code_invalid with cost-matched dummy bcrypt on verify; per-account cap 5 codes/24h + 60s resend gap. Nothing on users or /auth/forgot-password touched. Verification: api-server tsc clean; marketing + admin-panel tsc clean; live-db vitest public-free-scan-account-recovery 12/12 + existing #4329 account/remediate 24/24 (re-run after the password-check refactor); admin ribbonAssembly 24/24; request-restart exit 0 (live on :8080 @ f8cf4bcf); curl of recover/send-code 202, verify-code 400 code_invalid, admin queue 401; shaneapp://runTest free-scan-account.json onlyTag=4483-recovery 18/18 PASS. Not done: no live end-to-end against a real paid engagement + real mailbox/SMS on the dev server (covered by the live-db test with mocked mail); the AdminV2 screen was typechecked and ribbon-tested but not opened in a browser session. No findings filed. Working tree clean.
