# #3307 — Shanes Life: no standalone timer capability -- README's own capture grammar item 3 ("8 min timer for pasta") has nowhere to go outside a live Cook/Tonight session

- **Status:** ✅ DONE 2026-09-09T03:15:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal project, milestone 19)
- **Started:** 2026-09-09
- **Commit(s):** ba9062347 (bookend IN FLIGHT), a11375b0b → rebased to 8a297d6bb on origin/main (real work)

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed real gap: `grep -rn "timer" src/ public/app.js`
  finds only `setTimeout`/`setInterval` plumbing and Recipes' own `mealTickTimer`/`mealBeepTimer`,
  both scoped to a live `#/cook/<id>`/`#/tonight` session. No data model, table, or route for an
  ad-hoc timer created from the general capture box. Shane's own real decision comment on this
  issue (2026-09-09) resolves the open design question: server-side timer entity (not
  client-side-only state), reusing the existing real web-push/nudge infrastructure
  (`src/core/nudges.mjs` / `src/core/push-subscriptions.mjs` / `src/push/webpush.mjs`) so the
  alert fires as a real OS notification whether or not the app is open -- "replaces dumb Siri and
  manual clocks."
  Building: migration for a new `timers` table, `src/core/timers.mjs` (create/list/cancel/sweep),
  a capture-grammar rule ("8 min timer for pasta", "set a timer for 5 minutes"), a tight
  `server.mjs` sweep that fires the real push through the existing nudge delivery path
  (bypassing the daily nudge cap -- a directly-requested timer must always fire, same exception
  already documented for meds batches), `GET /api/timers` + `DELETE /api/timers/:id`, and a
  Today-tray card to see/cancel an active timer.
- 2026-09-09T03:15:00Z ✅ DONE — shipped the real feature, not an empty state. Migration
  `069_timers.sql` (`timers` table: label, duration_seconds, fires_at computed at insert,
  fired_at/canceled_at), run myself against the local `finances` database and confirmed via
  `information_schema.columns`. `src/core/timers.mjs`: createTimer (1-86400s real ceiling)/
  listActive/cancelTimer/findDue/markFired. Capture grammar `timer_set` rule (36th rule,
  appended per this file's own established convention of not renumbering earlier rules) +
  `extractTimerDuration`/`formatDuration` pure helpers, both directions ("8 min timer for
  pasta" and "set a timer for 5 minutes[, for the rice]"), 6 new unit tests.
  `server.mjs`'s `runTimerSweep` polls every 20s (vs. the existing 5-min sweeps -- a real
  timer can be "2 minutes," the issue's own stated verification bar) and delivers through the
  existing `queueNudge`/`notifyUser` web-push path with `countsToCap: false` (never held for
  the 1-3/day cap, same real exception already made for meds batches). A matched capture checks
  the new `push-subscriptions.mjs` `hasAnySubscription` and appends an honest "Turn on
  notifications in Settings" warning when there's genuinely no device to push to, instead of a
  silent later failure. `GET`/`POST /api/timers`, `DELETE /api/timers/:id`; `/api/today` carries
  the active list; Today tray renders a real "Timers" card with a working Cancel button.
  MCP tools `set_timer`/`list_timers`/`cancel_timer` give Claude the same real entry point other
  capture-grammar actions already get. README.md documents the feature and adds `timers` to the
  "Which rooms are real today" table.
  Caught and fixed two real bugs live, against the real local database, before commit: (1) a
  Postgres param-type-inference conflict reusing `$3` as both a plain integer column and inside
  an interval expression ("inconsistent types deduced for parameter $3") -- fixed with
  `make_interval(secs => $4)` binding the same value twice rather than chasing casts; (2)
  `activity_log.entity_id` is `uuid`, but `timers.id` is `bigserial` -- the same real trap
  `nudge_events` already worked around; fixed the same way (id moved into `detail`, not
  `entityId`) in both the web route and the MCP tools.
  Verified: `node --check` clean on all 9 touched files; `capture-grammar.test.mjs` 53/53 (post
  merge with a concurrent #3311 build); full `bin/check.mjs` 217/221 (the 4 failures are a
  pre-existing, unrelated `SL_VAULT_KEY` gap in the real local `.env`, confirmed present before
  this session and filed as #3314, parented under this issue's own Feature #3220, not fixed here
  -- generating a real secret is Shane's call, not an agent default); a live HTTP round trip
  against a running server (create/list/cancel/double-cancel-rejected, POST /api/captures
  end-to-end) and a real fired timer observed through the actual 20s sweep, producing a real
  `nudge_events` row (`sent_at` set, `counts_to_cap: false`, correct payload) with zero server
  errors. All disposable rows/sessions cleaned up after verification.
  Rebased once onto a concurrent #3311 (occasional-purchase deal-match nudges) landing on
  `origin/main`; one real, non-overlapping conflict in `nudges.mjs`'s `pushUrlForNudge` (two
  independent new `if` branches), resolved by keeping both.
  `verify-branch-merged.mjs`: OK, `agent/3307-q2035` (8a297d6b) is an ancestor of `origin/main`.
  `git status --porcelain`: clean.
  Filed: #3314 (SL_VAULT_KEY gap, parented under Feature #3220, milestone 19, board status AI
  Batter Up).
