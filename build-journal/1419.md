# #1419 — Global title-bar toggle for Primary/Secondary account

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 1dfcf383f (bookend), 0ae88bd87 (real work)

## Log
- 2026-08-27 ⏳ IN FLIGHT — adding a top title-bar Primary/Secondary account toggle (BuildConsoleSettings.DefaultAccount) mirrored into EditBuildPromptDialog's default selection and the BT_QUEUE_BUILD chat-button path, so new builds default to the toggle's state without needing the Edit Build Prompt dialog. Per-build Account selector from #1416 remains a real override.
- 2026-08-27 ✅ DONE — `BuildConsoleSettings.DefaultAccount` (persisted, default "primary") added; title-bar toggle (`TopAccountToggleBorder`/`TopAccountToggle_Click`) placed next to the shaneapp:// live-execution badge, colored/labeled via `RefreshTopAccountToggleUi`. Wired as the fallback for: (1) the BT_QUEUE_BUILD chat-button queue path via `ResolveDefaultAccountFlag()`, and (2) `EditBuildPromptDialog`'s initial Account-selector state when the prompt carries no explicit `--account` flag. #1416's per-build Account selector/`--account` flag still wins whenever explicitly set. `dotnet build -c Debug` → 0 errors, same 46 pre-existing warnings as baseline, none new. No test-manifest — scope is `desktop/BuildConsole`, outside the msp-portal/marketing test-coverage rule. No DB/schema change (setting lives in `%AppData%\BuildConsole\settings.json`, not Postgres).
