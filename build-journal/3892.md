# #3892 — Testimonials: periodic prompt trigger (phase 1 of 4 — API)

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** df870901b

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — add `GET /api/portal/testimonials/prompt-due` to
  `artifacts/api-server/src/routes/portal-testimonials.ts`, anchoring the 30-day-then-90-day
  cadence settled on the issue (2026-09-14) off `tenantsTable.createdAt`.
- 2026-09-14T00:20:00Z ✅ DONE — Added `GET /api/portal/testimonials/prompt-due`
  (`artifacts/api-server/src/routes/portal-testimonials.ts`), gated by
  `requireCapability("ladder.customer-user")`, already registered via the existing
  `portal-testimonials.ts` router mount in `routes/index.ts` (no new registration needed).

  **Anchor decision:** `customer_testimonials.customer_id` (and every other `customerId`
  column throughout this route file) is `tenants.id`. Checked both candidates named in the
  dispatch: `tenants.createdAt` (the tenant row's own creation timestamp, `NOT NULL`, always
  present) vs. `users.onboardingWizardCompletedAt` (per-USER, nullable — not every user on a
  tenant completes the wizard, and this endpoint answers a per-CUSTOMER/tenant question, not
  a per-user one). No tenant-level onboarding-completion timestamp exists in the schema at
  all (`tenants.status` has an `'onboarding'` enum value but no timestamp recording when it
  flips to `'active'`). Used `tenantsTable.createdAt` as the sole reliable tenant-level
  anchor.

  Logic: `due = daysSinceCustomerStart >= 30 AND (no submission ever OR daysSinceLastSubmission >= 90)`.

  Verification: `tsc --noEmit` clean (`artifacts/api-server` typecheck, no new errors).
  `customer_testimonials` table confirmed live via `\d` against local Postgres
  (`DATABASE_URL`). Ran the real due-check logic as SQL against tenant id 1 (real row,
  created 2026-07-29, ~47 days old, no submissions) — confirmed `due=true`. Verified the
  other two branches inside a rolled-back transaction (`BEGIN`/`ROLLBACK`, no persisted
  data): inserted a real submission row (`days_since_last=0` → not due), then backdated it
  to 95 days (`days_since_last=95` → due again). All three branches of the cadence match the
  endpoint's logic. No test manifest: this change is entirely inside `artifacts/api-server/`
  (a new endpoint's server-side logic), not `artifacts/msp-portal/` or
  `artifacts/shane-mccaw-consulting/`, which is the explicit scope of the mandatory
  test-manifest rule — same precedent as #3891 (no manifest, direct DB verification).
  No fixture/hardcoded data anywhere in this change.

  Branch verified an ancestor of `origin/main` via `verify-branch-merged.mjs` before
  writing DONE. `git status --porcelain` clean.
