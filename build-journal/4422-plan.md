# #4422 — Bump BuildConsole.csproj TFM net8.0-windows → net10.0-windows — plan

## Prerequisite check (real, not assumed)
`dotnet --list-sdks` on this dev box: 2.1.526, 3.1.426, 5.0.405, 5.0.408, 6.0.203,
7.0.202, 8.0.425, **10.0.401**. .NET 10 SDK is present — proceeding.

## Real current state (`desktop/BuildConsole/BuildConsole.csproj`)
- `TargetFramework`: `net8.0-windows`, `UseWPF=true`, `UseWindowsForms=true`
- `Microsoft.Web.WebView2` 1.0.4129.50
- `AvalonEdit` 6.3.1.120
- `Npgsql` 7.0.7 (csproj comment: "7.0.x targets net7.0 directly, matching this app's TFM")
- `System.Diagnostics.PerformanceCounter` 7.0.0
- `System.ServiceProcess.ServiceController` 8.0.0

## Real nuget.org compatibility check (queried live, not guessed)
- `Microsoft.Web.WebView2`: latest stable is `1.0.4191.47` (1.0.4255 is prerelease).
  Not TFM-gated (SDK-style redistributable wrapper) — bump for freshness.
- `AvalonEdit`: `6.3.1.120` IS the latest stable already — no change.
- `Npgsql`: latest stable `10.0.3`. Its nuspec carries explicit dependency groups
  for `net8.0`, `net9.0`, AND `net10.0` — genuinely supports net10 natively (7.0.7
  has no net10 group and its own comment says it targets net7.0 directly). Bump.
- `System.Diagnostics.PerformanceCounter`: latest stable `10.0.12` — this package's
  versioning tracks the runtime version since 8.0; 7.0.0 predates the net8/9/10
  runtime-compat releases. Bump.
- `System.ServiceProcess.ServiceController`: same story, latest stable `10.0.12`. Bump.

## Plan
1. `TargetFramework` → `net10.0-windows`.
2. `Microsoft.Web.WebView2` → `1.0.4191.47`
3. `Npgsql` → `10.0.3` (update the stale csproj comment referencing net7.0)
4. `System.Diagnostics.PerformanceCounter` → `10.0.12`
5. `System.ServiceProcess.ServiceController` → `10.0.12`
6. `AvalonEdit` unchanged (already latest).
7. `UseWPF`/`UseWindowsForms` left as-is — confirm via real build that both are
   still valid together in the net10.0 Windows Desktop workload.
8. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug`, fix any real
   compile errors the TFM bump surfaces (known risk per prior MyArchitect #4409
   upgrade: WinForms implicit-usings can collide with bare WPF type names under
   .NET 10 — check for CS0104 first if the build fails).
9. Note any new warnings even if not all fixed.
