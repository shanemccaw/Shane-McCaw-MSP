# #2981 — mfa-enforcement-v1 reports success after deleting nothing when the Entra methods read is stale

- **Status:** ✅ DONE 2026-09-07T15:34:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `fdf16190f` (fix + tests), `6325e4774` (docs)

## Log
- 2026-09-07T14:59:30Z ⏳ IN FLIGHT — Investigating `runForceMfaReregistrationAgainstTenant()` in `artifacts/api-server/src/lib/workflow-executor.ts` (#1899). The step does a single-pass GET of `/users/{id}/authentication/methods` then DELETEs what that one read saw, and reports success unconditionally — so a stale Entra read-replica response (reproduced live on #2840) makes a partial/no-op wipe indistinguishable from "user has no MFA methods". Plan: verify-after-delete (re-GET) with a bounded convergence retry, and treat an unconfirmed end state as a failed step rather than success.
- 2026-09-07T15:34:00Z ✅ DONE — Shipped the verify-after-delete convergence loop.

  **What shipped**
  - New `artifacts/api-server/src/lib/mfa-reregistration.ts` — `runMfaReregistrationConvergence()`: enumerate → delete → **re-enumerate** until `requiredCleanReads` (2) consecutive delayed reads corroborate that nothing removable is left, bounded by a real read cap and wall-clock budget (6 reads / 45s, 5s initial backoff doubling to a 15s cap). An end state that cannot be corroborated returns **failure**, not success. Deliberately free of db/Graph imports so the logic is unit-testable with no network and no real sleeping.
  - `runForceMfaReregistrationAgainstTenant()` now injects the real Graph calls into that loop and reports the real trail (`verified`, `enumerationReads`, `cleanReads`, `convergenceWaitedMs`, `alreadyAbsentIds`, `unverifiedReason`), which is also written into the `baseline_action_template_audit_log` `afterSnapshot`. `errorType` deliberately stays inside the existing union — `switchChosenHandle` routes `execute_baseline_template`'s outgoing edges by that exact slug, so a new value would be a handle no workflow or builder UI has an edge for.
  - Per Microsoft's own eventual-consistency guidance, a DELETE that returned 2xx is authoritative: a later read still listing that id is stale in the other direction and counts as clean, and a 404 on DELETE means already-absent (tracked separately from real deletes so the audit trail never overstates the run).
  - `scripts/azure/testbed-test-user-2840.mjs` mirrors the same convergence under `--purge-auth-methods`, with the original single-pass behaviour retained under the new `--purge-auth-methods-single-pass` so #2981's repro still reproduces on demand.
  - Docs updated: `docs/testbed-destructive-write-test-user-2840.md` (new flag + a real 2026-09-07 evidence table), `docs/write-app-permissions.md`, `docs/writepackmapping.md`.

  **Verification (real, not inferred)**
  - `src/lib/mfa-reregistration.test.ts` — **13/13 pass**, including the exact #2840 stale-first-read signature, the lag running the other way, 404-on-delete, 403-on-delete, transient read failure, budget/read-cap exhaustion and the asserted backoff schedule.
  - Related suites re-run green: `mfa-reregistration`, `graph-write-permissions`, `config-pack-graph`, `workflow-executor-core`, `workflow-executor-integrations`, `admin-write-actions`, `dlp-role-group-provisioning` — **293/293**.
  - `npx tsc --noEmit` on `artifacts/api-server` — clean.
  - **Live against the real testbed tenant** (DEV write app `9f6f4772…`, sanctioned user `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com`, #2840): the OLD single-pass path reproduced the bug verbatim — `POST phoneMethods → 201`, then `enumerated 1 method(s); 0 deletable`, deleting nothing with no error. The NEW path then deleted that same still-registered method (`DELETE phoneMethods/3179e48a-750b-4051-897c-87b9720928f7 → 204`) and corroborated it across 3 reads → `VERIFIED empty`. A separate run also caught the lag in the opposite direction (read 2 still enumerating a method already deleted with a 204) and correctly treated it as clean. Account left clean — only `passwordAuthenticationMethod` remains.

  **Honest limitation, stated in code and pinned by a test:** no strongly-consistent read exists for authentication methods, so "verified" means N delayed samples agreed — not a proof. A replica lagging past the whole corroboration window would still read empty. What this closes is #2981's actual failure: a single sample, taken with no delay, being the entire success signal.

  **Finding filed:** #3075 — `action.remove-auth-method` (#2875) fails outright when a stale replica 404s its method-type read; same lag mechanism, opposite direction, fails loudly rather than silently. Parented under #2489, labelled `bug` + `security`, board status "AI Batter Up".

  Branch `agent/2981-q1829` verified an ancestor of `origin/main` (`verify-branch-merged.mjs`, exit 0). Working tree clean. No pre-existing dirty files were present on arrival.
