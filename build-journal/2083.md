# #2083 — bootstrap-server.mjs non---link path still prescribes bare `pnpm install`

- **Status:** ✅ DONE 2026-09-06T17:12:00Z
- **Scope:** platform

## Log
- 2026-09-06T17:08:26Z — Starting. Fix the stale non-`--link` message in
  `scripts/dev-server/bootstrap-server.mjs` (line 89, plus usage text at line 11)
  to reflect #1987 (pnpm install is not a remedy / bandwidth gate) and #1988
  (worktree install gate) — state the metered cost, point at `--link` as the
  default, and note the `.pnpmfile.cjs` gates.
- 2026-09-06T17:12:00Z — ✅ DONE — Reworded the usage comment (line 10-11) and the
  non-`--link` console message (was line 89) to state `--link` is the intended
  default, note that a bare re-run refetches the ~1,120-package graph (real
  metered cost), and that any real `pnpm install` still runs subject to the
  repo's `.pnpmfile.cjs` install gates — matches #1987/#1988. Commit
  4f37a9c17, verified merged into origin/main via verify-branch-merged.mjs
  (exit 0); `git status --porcelain` clean.
