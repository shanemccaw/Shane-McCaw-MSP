# #1618 — Policy Decisions: wire "Sign it off" to the real POST /portal/risk-register/:rbdId/accept endpoint

- **Status:** ⏳ IN FLIGHT (BLOCKED)
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** e81f64053ab478e88275b1c0b424ff757916500b (bookend + dependency-graph fix only; no product code changed)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Pulled issue body + comments. Issue's own body says explicitly
  "Do not build this until #1528 resolves" — a genuine data-model/product decision (whether
  `msp_risk_decisions` stays a permanent shared table with `decision_state` as a discriminator,
  or Policy Decisions gets its own create path/fields). #1528 is still OPEN with 0 comments —
  unresolved. Per the standing rules, this is a stop-and-ask, not a build-it: it changes what
  the "Sign it off" write actually captures, not a missing column.
- 2026-08-30 — Found the dependency graph was wrong (Git #1987 class): #1618's only `blocked_by`
  edge pointed at #1547 ("Policy Engine: standing policies are a second object"), which is now
  closed/complete. That edge never actually gated this wire-up — #1547 answers a different
  question. Fixed: removed the stale edge, added the real one (`blocked_by -> #1528`). Labels:
  added `blocked`, removed `in-flight`. Posted an explanatory comment on #1618 naming what Shane
  needs to decide.
- 2026-08-30 ⏳ IN FLIGHT (BLOCKED) — No code changes made; none were appropriate given the open
  product decision. Working tree left clean (`git status --porcelain` empty). No new findings
  filed — the dependency-graph fix was applied directly to this issue's own edges rather than
  filed as a separate issue.
