# #3679 — Remove Build Tracker, Git, and Project Management screens from Admin Panel entirely

- **Status:** ✅ DONE 2026-09-11T19:10:35Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 895d94681

## Log
- 2026-09-11T18:47:40Z ⏳ IN FLIGHT — Pulled issue #3679 body + comment (confirms full scope, no supersede). Confirmed the 21 target files on disk match exactly: 11 in `build-tracker/`, 8 in `git/`, 2 in `project-management/`. About to delete all 21 in one pass, strip TitleBar pill + home dashboard rollup cards, remove nav/route wiring, and grep-verify no remaining imports. UI/routes only per Shane's instruction — not touching `bt_*` DB tables or backend API routes (that's #3651, separate).
- 2026-09-11T19:10:35Z ✅ DONE — Deleted all 21 files across `build-tracker/`, `git/`, `project-management/` in one commit (895d94681). Stripped TitleBar's "In Progress" pill and its `buildTrackerStore` wiring; stripped Shell.tsx's home-dashboard Build Triage / In-Progress Work / Milestones Roadmap / Git & Deploy rollup cards; dropped the now-empty `git`/`build` fixed ribbon tabs from `registry/types.ts` + `RibbonTabs.tsx`; removed every screen-registration import, floating console, and fetch bridge for all three from `AdminV2.tsx`. Also cleaned up second-order breakage caused by the deletion: removed the orphaned `issue`/`chatLink`/`epic`/`milestone` peek kinds and the `milestone` KIND_BADGE entry (owned exclusively by the deleted screens); dropped the dead `isBuildRelated` branch in `shellState.ts`; fixed a real cross-directory import in `run-history/runHistoryActions.ts` (`../git/deployStore`) so a deploy-kind run-history row is now honest read-only history instead of a broken import. Did not touch `bt_epics`/`bt_issues`/`bt_chats` tables or backend API routes, per Shane's explicit instruction (#3651 handles that separately). Verification: `tsc --noEmit` clean; `vite build` succeeds (PORT/BASE_PATH set for the one-off check, output not committed); real grep confirms nothing outside the three deleted directories imports from them (only prose-comment analogies remain, updated where they named a now-deleted file); `vitest run src/adminv2` → 507/525 passing. Fixed the one real regression (`Shell.test.tsx`'s "renders all seven fixed tabs" asserted a Git tab — updated to six, confirmed passing). The remaining 18 failures (`Shell.test.tsx` ×3, `NoScreen.test.tsx` ×4, `workflow.test.tsx` ×11) were individually reproduced against an **unmodified** `main` checkout at `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\artifacts\admin-panel` — confirmed pre-existing, not caused by this change — and filed as #3687 (parented under #1095, board status AI Batter Up). Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0) and working tree clean before writing DONE.
