# #2925 — license_gap messaging inaccurate for testbed tenant

- **Status:** ✅ DONE 2026-09-06T14:05:00Z
- **Scope:** platform

## Log
- 2026-09-06T13:54:40Z — Started. Fixing customer-facing `license_gap` copy in
  `dashboard-resolvers.ts` — testbed tenant genuinely has Defender for O365 licensed
  (confirmed by #2837's live investigation); the real 403 ("Account is not
  provisioned") is caused by the Security & Compliance portal at
  security.microsoft.com never being onboarded, not a missing license. Updating the
  customer-facing message to describe an unonboarded-portal state instead of a
  licensing gap.
- 2026-09-06T14:05:00Z — ✅ DONE — `classifyGraphError()` (graph.ts:711-716) maps
  Graph's "Account is not provisioned" 403 to `license_gap` with the feature string
  `"Microsoft Defender for Office 365"`, and that's the ONLY branch that ever
  produces that exact string. Used that as a safe, narrow signal to branch the
  customer-facing message in both places it's duplicated (per each file's own
  "keep wording identical" comment):
  - `artifacts/api-server/src/lib/dashboard-resolvers.ts` `licenseGapMessage()`
  - `artifacts/api-server/src/lib/diagnostics-runner.ts` `buildFindingDescription()`
    license_gap branch

  New copy: "We couldn't evaluate this because the Microsoft 365 Defender /
  Security & Compliance portal (security.microsoft.com) hasn't been opened yet on
  your tenant. This isn't a licensing gap — Microsoft Defender for Office 365 is
  already licensed here. A Global Admin needs to sign into security.microsoft.com
  at least once to complete onboarding; we'll pick this check back up
  automatically once that's done."

  Updated the one existing test asserting the old wording
  (`dashboard-resolvers.test.ts`). Confirmed the 4 affected checks named in the
  issue (`security:insider-risk-alerts`, `security:open-incidents`,
  `security:automated-investigation`, `security:alert-count-by-severity`) are NOT
  in `license-gap-purchase-links.ts`'s category table, so the purchase-link/upsell
  surface is unaffected by this fix — no change needed there for this issue.

  **Verification note:** `vitest`/`tsc`/`@types/node` are genuinely absent from
  both this worktree's node_modules AND the main checkout's node_modules
  (confirmed — not a dangling worktree-junction issue per Git #1987/#1988; the
  junction resolves correctly to the main checkout, which itself lacks these
  packages). Could not run the test suite or a real typecheck. Did not run
  `pnpm install` per the standing rule. Verified instead by manual diff review —
  the change is two small string-literal branches plus one test-string update,
  no new imports, no structural risk. This gap in the local toolchain (missing
  vitest/tsc/@types/node repo-wide) is itself worth a look but is unrelated to
  this issue's scope; not filing a new issue for it as it's an environment
  question, not a product one — flagging here for visibility only.

  Commits: `c3c6184b71cea905998fd89c38c75d08732a00e` (on `agent/2925-q1720`,
  merged into `origin/main` via a merge commit pushed directly to `main`).
  `node scripts/dev-server/verify-branch-merged.mjs` confirms merged. Working
  tree clean.
