# #2315 — Mirrored feature pairs between Customer Portal and MSP Portal

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 2c59645f1 (rebased onto main as it moved under concurrent #2316/#2317 builds; final merged tip 22465c742)

## Log
- 2026-09-02T19:29:00Z ⏳ IN FLIGHT — building real mirrored-pair matching between EPIC #1485 (Portal) and EPIC #1571 (Portal Admin) sub-issues, off the two epics' own "Feature: X (Portal)" / "Feature: X (MSP Console)" naming convention, real token-overlap match (not a fixture pairing table). Sequenced after #2314 (closed/DONE) in the same file region.
- 2026-09-02T20:05:00Z ✅ DONE — `GitMapService.cs`: new `GitMapMirroredPair` type, `GetMirroredPairsAsync()`, and `ComputeMirroredPairs()` — real stable mutual-best matching by Jaccard token overlap (threshold 0.5) between #1485's and #1571's own real `Feature: … (Portal)` / `Feature: … (MSP Console)` sub-issue titles (no fixture pairing table, no fabricated synonym list). Verified the matcher against the two epics' real live titles (`gh api graphql`): 11 genuine pairs matched correctly (Change Control, Risk Register/RBD, Runbooks, Remediation Tracking, Ownership/RACI, SOPs, Security Plan, Settings, Billing, Webhooks at 100%; Policy Decisions ↔ Policy Decisions + Policy Engine at 67%; Microsoft Changes / Message Center ↔ Microsoft Changes at 50%), and Portal-only features with no MSP Console counterpart yet (e.g. Account Security) correctly stayed unmatched rather than force-paired. `GitMapData` carries `MirroredPairs`/`UnmirroredPortalFeatures`/`UnmirroredAdminFeatures`, fetched globally in `BuildAsync` (same convention as Started-and-Dropped) — non-fatal on failure (logs a warn, leaves the section empty rather than breaking the rest of the Git Map). `MainWindow.xaml.cs`: new "Mirrored Pairs — Portal ↔ MSP Console" section in `BuildGitMapBody` (the single card-builder both the mini rail panel and the full Git Map doc tab already share — no second data path), with `GitMapMirroredPairCard` (per-pair, dims/labels a closed side, shows the real match score when it's a partial match) and `GitMapUnmirroredNote` (honest list of unmatched features on either side).
  Verification: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` clean (0 warnings/errors), both before and after rebasing onto main past the concurrently-landed #2316/#2317. Standalone console-app run of the exact matching algorithm against real live #1485/#1571 titles pulled via `gh api graphql` confirmed the results above. Branch confirmed merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
  No findings to file — the doc's own remaining Git Map gaps (items 4-5 under #2313) are separate leaf issues (#2316/#2317), already in flight, not touched here.
