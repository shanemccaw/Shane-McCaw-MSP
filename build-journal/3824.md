# #3824 — Reinstate QueueWatcherService's periodic claim-check timer (slower interval) alongside #3774's event triggers

- **Status:** ✅ DONE 2026-09-12T20:06:15Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 0bb6c84bf (real fix), 22b2dc89d (IN FLIGHT bookend)

## Log
- 2026-09-12T20:01:39Z ⏳ IN FLIGHT — Pulled #3824 body + comments. About to reinstate a DispatcherTimer driving TickAsync in QueueWatcherService at a slower interval, keep #3774's completion-event + manual-refresh triggers, and make SetPaused(false) trigger an immediate re-evaluation.
- 2026-09-12T20:06:15Z ✅ DONE — Landed at `0bb6c84bf`.
  - **Chosen interval: 30 seconds** (`QueueWatcherService.QueueTickInterval = TimeSpan.FromSeconds(30)`). Worst case 120 batched GitHub checks/hour vs. 360/hour at the original 10s, of a 5000/hour budget.
  - `Start()` arms a `DispatcherTimer` (UI thread, so TickAsync keeps its UI-thread invariant) that calls `TickAsync` every 30s, plus the existing immediate startup tick.
  - #3774's build-completion exit callback (`ArmCompletionTrigger`) and manual refresh (`RequestImmediateReevaluation`) are unchanged and still fire immediately. They now sit alongside the timer.
  - `SetPaused(false)` now fires an immediate tick through a new `RequestTickOnUiThread()` helper, which marshals onto the Dispatcher when called off the UI thread. Its log message now says it re-evaluates now and every 30s. If a tick is already running, the extra tick is skipped by the single-flight guard and the timer picks the queue up within 30s.
  - `MarkAppReady()` also fires an immediate tick. Start()'s first tick normally hits the `_appReady` gate and returns, so under #3774 nothing claimed until an external event arrived. That was the same class of gap. `_started` is now `volatile` because MarkAppReady can read it from a background thread.
  - Updated the stale "~10s" / "no automatic timer" comments and ActivityLog text in QueueWatcherService, plus the Settings max-concurrent copy (SettingsTabView.xaml/.xaml.cs, "~30s poll") and one MainWindow comment.
  - **Tick cost:** the periodic tick runs the same body as before #3774. It does one batched GetNextAsync live check, and only when there is a free slot and the queue isn't paused. The blocker re-sync (3 min), issue mirror (5/30 min), stuck-running sweep (60s) and api-server probe (45s) have their own throttles. Nothing here resembles #3801's whole-queue JSON serialize, which was fixed in BuildQueuePanel.
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` gave 0 errors (10 warnings, all already present before this change). I did not run the live app. Standing guidance says never launch BuildConsole.exe to verify. Shane's live check is still needed: resume with 0 builds running and confirm queued items launch within 30s (or right away on resume). Also confirm that a build finishing and a manual refresh still re-check right away.
  - Findings filed: none.
  - `git status --porcelain` clean; fix commit verified an ancestor of origin/main.
