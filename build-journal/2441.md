# #2441 — Auth Core: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T02:41:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 19a602753 (IN FLIGHT bookend), 3eef7b963 (contract pack)

## Log
- 2026-09-03T02:07:02Z ⏳ IN FLIGHT — verifying the 9 real endpoints from #2441's list are
  still live in the current `artifacts/api-server` (post-#1673 portal restructure), then
  extracting `docs/auth-core-contract-pack.md` from the real code, per the #1642 pattern.
- 2026-09-03T02:41:00Z ✅ DONE — all 9 endpoints (`auth/forgot-password`,
  `auth/mfa/bypass`, `auth/mfa/totp/challenge`, `auth/reset-password`,
  `auth/setup-context`, `auth/setup-password`, `portal/branding`,
  `portal/sign-in-help/ticket`, `status`) confirmed live and mounted, none orphaned.
  Wrote `docs/auth-core-contract-pack.md` from real route code, real Drizzle schema
  enums, real live-DB counts, and the archived `portal-archive-2026-08-29` client pages
  (for shape cross-reference only, per #1648's "carry forward calls, not markup" rule).
  No genuine finding rose to a filed issue — one real note recorded in the pack itself
  (§1): the retired login page never wired SMS/passkey MFA-challenge past TOTP, even
  though `/auth/mfa/sms/send` is real — informational for the new design, not a live
  bug (no current page to be broken). Updated #1648's structured index to check off
  #2441. `verify-branch-merged.mjs` confirmed 3eef7b96 is an ancestor of origin/main.
  `git status --porcelain` clean.
