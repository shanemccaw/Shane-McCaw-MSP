# #4168 — TYPO/COLOR AUDIT: VisualTestTrackerWindow.xaml ignores Typography.xaml (148 raw literals)

- **Status:** ⏳ IN FLIGHT 2026-09-15T01:51:37Z
- **Scope:** buildconsole

## Log

- 2026-09-15T01:51:37Z — Pulled #4168 body + #4162 audit comment. Confirmed real counts via grep:
  138 raw `FontSize="N"`, 10 raw `FontFamily="Consolas"`, 0 raw hex colors — matches #4162's table
  exactly (148 total). Read `Themes/Typography.xaml` (`BodyFontSize`=14, `SubHeaderFontSize`=16,
  `HeaderFontSize`=18, `MonospaceFontSize`=13, `CaptionFontSize`=11, `DefaultFontFamily`,
  `MonospaceFontFamily`) and the full `VisualTestTrackerWindow.xaml` (1191 lines).
  Mapping plan by role, not blind find-replace:
  - 10 elements with raw `FontFamily="Consolas"` (selector/JSON/console/telemetry mono readouts) →
    `{StaticResource MonospaceFontFamily}` + `{StaticResource MonospaceFontSize}` (13).
  - The window's 3 `PrimaryButton`-styled CTAs (Save Bug Entry, Run Console Command, Execute API
    Call) → `{StaticResource BodyFontSize}` (14) — primary actions, not chrome.
  - The window title text ("Visual Test Tracker", raw 12) → `{StaticResource BodyFontSize}` (14)
    — primary readable label, not a caption/badge.
  - Every other raw non-monospace `FontSize` (7/8/9/10/11/12/13, the dense HUD's secondary
    chrome — buttons, labels, checkboxes, badges, section labels) → `{StaticResource
    CaptionFontSize}` (11), the floor per #4168's own instruction ("never go below
    CaptionFontSize unless the element is a decorative icon glyph"). No purely-decorative
    icon-only glyph in this file needs to go below that floor.
