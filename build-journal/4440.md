# #4440 — status bar light bar still visible after #3875 (follow-up)

- **Status:** ✅ DONE 2026-09-16T23:58:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** none needed — already fixed by 3d9cc75db (#4439)

## Log
- 2026-09-16T23:50:00Z ⏳ IN FLIGHT — investigating. Live-checked `Themes/DarkTheme.xaml`'s `StatusBar`/`StatusBarItem` styles and `MainWindow.xaml`'s status bar `Grid.Column="3"` spring cell per the issue's own new lead.
- 2026-09-16T23:58:00Z ✅ DONE — this issue is already resolved on `main`, by a different build. #4440 was filed at 23:43:04Z from a screenshot of the *previously running/deployed* app; the actual fix landed 10 minutes earlier at 23:33:10Z as commit `3d9cc75db` (#4439, `Themes/DarkTheme.xaml`). #4440's body never mentions #4439 — it was investigating the same symptom independently and arrived at the identical root cause (`StatusBar`/`StatusBarItem` styles were property-setters-only `BasedOn` the OS-default Aero/Fluent `ControlTemplate`, which ignores `TemplateBinding Background` for the body/panel area — the same bug class already fixed for `ComboBox`). #4439's fix replaced both styles with full `ControlTemplate`s (`Border` + `ItemsPresenter`/`ContentPresenter`), and that `Border` now paints the *entire* `StatusBar` rectangle — including the column-3 spring `Grid` cell #4440's new lead specifically asked about, since the Border sits underneath `MainWindow.xaml`'s whole custom `ItemsPanel` Grid, not just the individual `StatusBarItem`s. Confirmed by reading both files on current `main`: `Themes/DarkTheme.xaml` lines 1296-1338 already carry the full templates, no `BasedOn="{StaticResource {x:Type ...}}"` remains on either style. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` succeeds clean (0 warnings, 0 errors) confirming the templates parse/bind correctly — same structural verification #4439's own session already did. Live pixel confirmation against the actually-deployed app still needs Shane's own eyes (per the standing "never launch the exe" rule) — the screenshot that prompted this issue predates the fix's deploy, so a fresh screenshot after the next deploy is the real closing evidence. No code change was needed or made this session; no fixture/hardcoded data; `git status --porcelain` clean.
