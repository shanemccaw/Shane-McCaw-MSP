# Issue #4445 — [DEBUG] autofill panel: add copy-Stripe-test-card and copy/set-local-M365-password buttons

- **Status:** ✅ DONE 2026-09-17T02:20:00Z
- **Scope:** platform

## Log
- 2026-09-17T02:00:00Z — Starting: adding two client-side-only buttons to the localhost-only debug autofill panel in `artifacts/shane-mccaw-consulting/src/marketing/pages/Buy.tsx`, plus the addendum fix to `debugFillAccount`'s email domain (`example.com` → real Outlook.com plus-address).
- 2026-09-17T02:20:00Z — ✅ DONE — Commit `5f162efd6` (rebased onto main as `f684240212babcde1c2ed6e7214e50d888ae19f6`) adds "Copy Stripe test card" (hardcodes Stripe's public `4242424242424242` test card, clipboard copy, "Copied!" feedback) and "Copy/Set M365 password" (inline text input, stored in `localStorage["debugM365Password"]`, read/written client-side only, never included in any fetch/request; disabled "Set M365 password" state mirrors `debugFillCode`'s pattern until a value exists) to the existing localhost-only `[DEBUG]` panel in `Buy.tsx`. Also fixed `debugFillAccount`'s generated email from the non-routable `dev.${Date.now()}@example.com` to a real deliverable `shanemccaw+dev${Date.now()}@outlook.com` plus-address per the addendum comment. `tsc --noEmit` clean on `artifacts/shane-mccaw-consulting`. No backend/DB changes — purely client-side. Verified: branch merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
