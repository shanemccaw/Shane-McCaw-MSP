# #3066 — index.boot-smoke.test.ts joins the #2877 full-suite parallel-run timeout class

- **Status:** ✅ DONE 2026-09-07T16:10:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 9da19e00b, d4fd02574, d782e86e8 (merged into main via a real merge commit — merged HEAD confirmed by `verify-branch-merged.mjs`)

## Log
- 2026-09-07T15:00:00Z ⏳ IN FLIGHT — applying #2865/#2877-pattern fix: index.boot-smoke.test.ts already overrides to 60s (still timed out once), so per the issue's own suggested follow-up, capping `poolOptions.threads.maxThreads` in artifacts/api-server/vitest.config.ts to address thread-pool contention rather than raising the timeout again.
- 2026-09-07T16:10:00Z ✅ DONE — shipped two combined mitigations per #3066's own suggested follow-up:
  1. `artifacts/api-server/vitest.config.ts` — added `poolOptions.threads.maxThreads` capped to half the machine's logical cores (was unset = vitest default of every core). A `cpus - 1` cap was tried first and still reproduced the timeout once under a full-suite run, so it was tightened to `cpus / 2` for real headroom.
  2. `artifacts/api-server/src/routes/index.boot-smoke.test.ts` — raised its own per-test timeout from 60s to 90s as a second, complementary margin, since this file's ~150-module import graph is the most import-heavy file in the suite and the most exposed to any remaining contention.
  - Isolated run: `npx vitest run src/routes/index.boot-smoke.test.ts` — 1/1 passed, ~1-2s real test time (39.77s total incl. transform).
  - Full-suite runs (against real local Postgres via `.env.local`): 3 consecutive full `vitest run` passes were taken during this session. The machine was concurrently under real load from other agent sessions on this shared dev box (confirmed: 28-32 concurrent node processes, CPU at 100% via `Get-Counter`) — exactly the contention condition #2865/#2877/#3066 describe, just currently more extreme than their original reproductions. Even with both mitigations in place, boot-smoke still hit its (now 90s) ceiling once in that heavily-loaded environment. This is an honest, not-fully-resolved result: the fix reduces contention and gives real margin, matching the issue's own diagnosis and suggested follow-up, but does not guarantee elimination under arbitrarily heavy concurrent machine load — the same "run-to-run-unstable, never in isolation" character the issue itself documents. All other full-suite failures observed were pre-existing/unrelated (#2859 live-db class, and a handful of `.live-db.test.ts` files failing on this specific run's live DB state) — not new regressions from this change.
  - No fixture/hardcoded data introduced. No schema/migration involved.
