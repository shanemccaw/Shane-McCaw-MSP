# Git #1829 — Build Queue: split "Queued & Running" into separate Running and Queued filters

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Commit:** ac0a401eb733f3428606f98c780683dc379184bf (main)

## Log
- 2026-08-30: Starting. Splitting the combined "Queued & Running" combo entry into "Running" and "Queued" per issue body's recommended split (Running = running + VerifyingStatus, Queued = queued + LimitPausedStatus), changing default `_filter` to "Running".
- 2026-08-30 ✅ DONE — Used the issue's recommended split as-is (no deviation): `Controls/BuildQueuePanel.xaml` combo now has separate "Running" (Tag="Running") and "Queued" (Tag="Queued") entries, first item = Running, so `SelectedIndex="0"` naturally defaults to it. `BuildQueuePanel.xaml.cs`: `_filter` default changed to `"Running"`; `ApplyFilter`'s old combined `"Active"` case split into `"Running"` (`running` + `VerifyingStatus`) and `"Queued"` (`queued` + `LimitPausedStatus`); also updated `RevealQueueItem`'s targetFilter switch, the `QueueEmptyText` per-filter message switch, the `?? "Active"` fallback, and a stale doc comment referencing the old "Active" tab name — all other filters (Parked/External/Done/Canceled/Tests/All) untouched. Verifying and Limit-Paused items remain visible (in Running and Queued respectively) — neither disappeared. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors. Rebased and pushed directly onto origin/main (`ac0a401`); `git merge-base --is-ancestor HEAD origin/main` confirmed. `git status --porcelain` clean.
