# #1996 — Batter Up Free flow toggle bright-blue fix

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 4be6a01b0 (on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — apply the QuietTile ToggleButton style to `BtnFreeFlow`
  (`BatterUpPanel.xaml`), sweep for other unstyled ToggleButtons, verify Release build.
- 2026-08-30 ✅ DONE — Applied the existing `QuietTile` `ToggleButton` style (already used
  by `BuildQueuePanel.xaml`/`HomeView.xaml`) to `BtnFreeFlow`, added as a local resource in
  `BatterUpPanel.xaml`. `QuietTile`'s control template has no `IsChecked` trigger of its own
  (unlike `ExpandCollapseToggleStyle` — icon-only 16x16, wrong shape for a text-content pill —
  and `FilterChip`, whose own `IsChecked` trigger repaints the border blue, which would have
  reproduced the same bug), so `Background`/`BorderBrush`/`Foreground` stay exactly whatever
  `UpdateFreeFlowVisual` (code-behind, untouched) sets per #1870's border/text convention
  (green "▶ Free flow" open, peach "⏸ Gated" closed) — never WPF's default checked-state
  solid-blue chrome. Removed the now-redundant bare `Background`/`BorderBrush`/`BorderThickness`
  overrides from the XAML (the style sets those).
  Swept every other `ToggleButton` in the app (`FilterChip`, `QuietTile` duplicates, `SegmentedToggleBtn`,
  `ExpandCollapseToggleStyle`, `ChatSessionPane`'s inline-templated `InstructionsButton`,
  `LeftSidebar`'s inline-styled `BtnMatchCase`) — all already carry an explicit `Style` or inline
  template with no bare default-chrome checked state. `BtnFreeFlow` was the only unstyled instance
  remaining; no further findings filed for this sweep.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors.
  Rebased onto `origin/main` and pushed directly (`agent/1996-q1038:main`) after several
  concurrent-push retries; verified `4be6a01b0` is an ancestor of `origin/main`. Working tree
  clean.
