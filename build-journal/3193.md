# #3193 — Feature: Pets -- Round 2 visual rebuild (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T17:52:03Z
- **Scope:** platform (web/shanes-life, Shane's Life app -- not part of the pnpm workspace)
- **Started:** 2026-09-08
- **Commit(s):** 6bd36acb9 (rebased/pushed as 3a6371619 on main)

## Log
- 2026-09-08T17:45:15Z ⏳ IN FLIGHT — Note: this file is created after the real work below, not
  before it (a session-ordering miss against the "create first" convention) -- recorded here
  honestly rather than backdated. Confirmed no `Shanes Life NN - Pets.dc.html` export exists
  (per the issue's own premise); found the real design source that does exist instead: contract
  pack Section 6 (prose), README "Screens" #10 (avatar/meta/due-line shape), and the rendered
  `screenshots/06-pets.png` reference. Rebuilding the Pets room to match that, reusing already-
  shipped Round 2 conventions (Shopping's native header #3178, People's initial avatar #3157,
  Meds' amber-due signal) rather than inventing a bespoke look.
- 2026-09-08T17:52:03Z ✅ DONE — Shipped both remaining-scope items from the issue body:
  (1) visual rebuild -- petCardEl gets a 44px initial avatar (.pet-avatar, same convention as
  People's .person-avatar), real species/breed/age meta ("6, born Mar 2020" via new ageYears/
  monthYear helpers -- also fixed the same "born" line on the pet detail page, which previously
  used whenDate's day-level format and silently dropped the year), an amber .when.due line once
  a vaccine enters its lead window (listPets' own surfacesInDays, same signal as Meds'
  .refill-days-left.due), an explicit "Nothing due" fallback, and the real explanatory copy from
  the design's own rendered screenshot: "A vet visit is an appointment with the pet as the
  subject...". (2) generic app-header removal -- while I was mid-build, a concurrent session
  (#3192, Dates) landed the generic `roomHeader()` chrome + `.room-scene`/`.room-glow` this
  issue's own item 2 called for; rebased onto it and made Pets the second real caller (a new
  `.pets-room` wrapper mirrors `.dates-room`'s scoped 22px-card treatment) instead of inventing
  a parallel header of my own. Confirmed dropping the generic bar is safe: its only other real
  function was Sign Out, which Settings' own "Sign out everywhere" already covers -- the same
  check that cleared Shopping/Dates.
  Verified live: ran the real local shanes-life server against the real local Postgres
  (`finances` DB, junctioned node_modules from the main checkout, scratch `.env` copied in and
  removed after -- no metered installs), minted a temp session + two temp pets (one with a
  vaccine 10 days out on a 30-day lead, i.e. inside its lead window) directly in the DB, hit the
  real `/api/pets` and confirmed its `surfacesInDays: -20` contract, and cross-checked the new
  age/month-year math against the design screenshot's own numbers -- Biscuit (born 2020-03-15)
  → "6, born Mar 2020", Pepper (born 2023-06-01) → "3, born Jun 2023", both an exact match. All
  temp DB rows reverted after (0 pets on file again). `node --check` clean on both edits of
  `app.js`. No test-manifest needed -- this app isn't under `artifacts/msp-portal/` or
  `artifacts/shane-mccaw-consulting/`, so that standing-practice section doesn't apply.
  Confirmed no dedicated form UI was reintroduced (Git #3183 already removed Add pet/vaccine/
  care; still gone). `verify-branch-merged.mjs` OK, `git status --porcelain` clean. No findings
  to file -- no live endpoint gaps, no failing pre-existing tests hit, no stale docs found for
  this room.
