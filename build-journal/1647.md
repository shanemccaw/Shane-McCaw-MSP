# #1647 — Retire portal-v2 — per-page, gated on each replacement landing (dispatch: `overview`, `documents`, `projects`, `retainer`, `copilot`)

- **Status:** ✅ DONE (moot — target app no longer exists) 2026-09-14T14:09:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** none — no code change made or needed; this bookend + issue comment only

## Log
- 2026-09-14T14:08:57Z ⏳ IN FLIGHT — Pulled #1647's full body (Group A/B disposition table,
  2026-09-14 update). Dispatch asked to retire 5 named `portal-v2` pages — `overview`,
  `documents`, `projects`, `retainer`, `copilot` — each per the issue's own "What retiring one
  page means" checklist (delete `portal-v2-<page>.tsx`, its `*Data.ts` fixture, route/nav entry,
  keep `*Live.ts` only if unreused), verifying via
  `grep -rn 'portal-v2-<page>' artifacts/msp-portal/src/`.
- 2026-09-14T14:09:00Z ✅ DONE (moot) — Before touching anything, went to locate the target
  files and found `artifacts/msp-portal` does not exist anywhere in this worktree.
  `find artifacts -maxdepth 1 -type d` lists only `admin-panel, api-server, mcp-server,
  msp-console, msp-website, portal, shane-mccaw-consulting` — no `msp-portal`. A repo-wide
  search for `portal-v2-*.tsx` returns zero hits. `git log --diff-filter=D --summary --oneline
  -- '**/portal-v2-overview*'` shows the whole app was deleted wholesale in `f40438cdc`
  ("Portal scaffolding: create artifacts/portal, retire artifacts/msp-portal", 2026-08-29
  16:27:25 -0400, preserved historically at tag `portal-archive-2026-08-29`) — **all five**
  pages named in this dispatch (`overview`, `documents`, `projects`, `retainer`, `copilot`),
  their `*Data.ts` fixtures, routes and nav entries went with it. That predates this issue's
  own 2026-09-14 Group B disposition update by roughly two weeks.

  This is the same class of stale premise already established on sibling issues #1425
  (`e2e9f9882`) and #1588 (`0ac70b52d`) — the 2026-08-29 wholesale deletion moots any
  per-page portal-v2 retirement dispatch, since there is no `portal-v2` app left to retire
  pages from. Confirmed the replacement app (`artifacts/portal`) is the live surface per
  CLAUDE.md's "Customer Portal build — design source" section, and none of it contains
  `portal-v2`-named files (checked via grep, zero hits).

  **No code changed** — nothing to delete, nothing to verify by grep beyond the zero-hit
  check already done above. Verification per the issue's own "Done when" block:
  `ls artifacts/msp-portal/src/pages/portal-v2-*.tsx` → no such directory (already empty by
  virtue of the whole tree being gone); `grep -rn 'portal-v2/[a-z]*Data"' artifacts/msp-portal/`
  → no such directory, zero hits. `git status --porcelain` clean except this bookend.
  Posted an honest comment on #1647 explaining the stale premise; not filing a new finding —
  this is the same already-known root cause as #1425/#1588, not a new one.
