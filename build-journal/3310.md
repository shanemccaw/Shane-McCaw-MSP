# #3310 — Extend weekly-ad deals/coupons schema — Category, ImageUrl, structured DealType, real date range

- **Status:** ✅ DONE 2026-09-09T02:45:00Z
- **Scope:** platform (web/shanes-life — Shane's personal Money project, desktop/ShanesSurvival WPF scraper)
- **Started:** 2026-09-09
- **Commit(s):** b2cfe01a1 (rebased/pushed as c2b9dd29e on origin/main)

## Log
- 2026-09-09T02:24:09Z ⏳ IN FLIGHT — read #3310 + parent #3288 (PublixAdParser), confirmed real schema
  (`item_prices` migration 021+029 unit add, `coupons` migration 029), confirmed WeeklyAdScraperWindow's
  extraction script already locates `img[alt]` per card (image URL genuinely extractable) but found no
  DOM evidence of a per-card category field (not live-verified — no interactive WebView2/browser session
  available in this headless build). Starting migration + MCP tool + parser + UI work.
- 2026-09-09T02:45:00Z ✅ DONE — real migration `web/shanes-life/migrations/066_weekly_ad_deals_category_image_dealtype.sql`
  (category/image_url/deal_type on `item_prices` + `coupons`, valid_to on `item_prices`), run
  live against local Postgres `finances` DB (`applied=1 alreadyApplied=52`). Extended
  `push_deals`/`push_coupons` MCP tool schemas (`src/mcp/tools.mjs`) and `core/prices.mjs`
  (`recordPrice`, `normaliseDealItems`/`normaliseCouponItems`, `pushDeals`/`pushCoupons`,
  `fetchWeeklyAd`, `attachWeeklyAdVerdicts`) to accept/store/read the new fields — verified
  with a real live round-trip against the local DB (pushDeals → pushCoupons → attachWeeklyAdVerdicts,
  cleaned up after). Wired Shopping's real per-item verdict badge (`public/app.js` `verdictBadge`,
  the one existing real display point — no separate deals-browsing UI exists) to show category
  text and a real product thumbnail when present; added `.shop-item-verdict`/`-thumb` CSS.
  Real, confirmed integration check on #3288's PublixAdParser/WeeklyAdScraperWindow/ScrapedAdModels/
  ShanesLifeMcpClient: `imageUrl` is genuinely extractable (the same `img[alt]` element the parser
  already reads the title from also has a real `src`) and now flows scraper → parser → MCP client →
  push_deals/push_coupons. `category` was NOT wired at the scraper — no per-card category grouping
  was found in Publix's real DOM in this pass, and this is honestly **not live-verified** (no
  interactive WebView2/browser session was available in this headless build); the column exists on
  the pipeline for Claude's own conversational reads and any future store, documented as such in
  desktop/ShanesSurvival/README.md. `dotnet build ShanesSurvival.sln` succeeded, 0 warnings/errors.
  No new findings filed — the category gap is the honest answer to the issue's own investigation
  ask, not a separate defect. Branch verified merged into origin/main
  (`scripts/dev-server/verify-branch-merged.mjs`), working tree clean.
