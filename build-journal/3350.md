# #3350 — Batter Up / AI Batter Up refresh fires one live GitHub call per OPEN item (same unbatched anti-pattern as #3347)

- **Status:** ✅ DONE 2026-09-09T22:22:25Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Commit(s):** abfb8f0324fc9b9feebb7da2cea3d9586b972e5e (fix), e0ee600b7 (IN FLIGHT bookend)

## Log
- 2026-09-09T22:15:22Z ⏳ IN FLIGHT — Sub-issue of #1789 (Feature: Dev Server & Deploy Infrastructure). Investigated the open-item refresh path on both panels. Confirmed the per-item burst: `BatterUpQueueService.RefreshAsync` fires, per open board item, `FindBuildCommentAsync` (→ `GetIssueCommentsAsync`, one REST call) **and** `gh.GetBlockedByAsync` (one REST call); `AiBatterUpQueueService.RefreshAsync` fires `FindBuildCommentAsync` per open item. Cold-start double-panel refresh ≈ `2×(#open Batter Up) + 1×(#open AI Batter Up)` live calls. Plan: (1) resolve blocked-by from the local mirror (`bt_issue_mirror.blocked_by_numbers` + blocker states via one batched `GetManyAsync`) — zero GitHub calls on the common mirror-first path; (2) add a batched `BatchGetRecentIssueCommentsAsync` GraphQL primitive on `GitHubApiClient` (aliased `issue(number:).comments(last:N)`, chunked, circuit-aware, throws on rate-limit — same shape as #3347's `BatchGetProjectItemStatusesAsync`) and a shared `ResolveBuildCommentsAsync` resolver used by both panels, with a bounded per-item fallback only for the rare deep-comment-history miss; (3) keep per-item live only on the degraded mirror-not-usable fallback path.
- 2026-09-09T22:22:25Z ✅ DONE — Shipped the de-burst (commit `abfb8f032`). Added `GitHubApiClient.BatchGetRecentIssueCommentsAsync` (aliased `issue(number:).comments(last:20)`, 15 issues/chunk, circuit-aware, throws on a rate-limit chunk — same shape/reuse as #3347's `BatchGetProjectItemStatusesAsync`). Added shared `BatterUpQueueService.ResolveBuildCommentsAsync` used by BOTH panels: short-circuits on an open rate-limit circuit (#3022/#3347 stance), resolves every open item's BUILD: comment in a handful of batched reads, and falls back to a per-item full fetch ONLY when a recent-window miss has a deeper `totalCount` (so a BUILD: comment under a deep later thread is never missed). Batter Up `RefreshAsync` now resolves blocked-by from the local mirror — carries each row's `blocked_by_numbers` and does ONE batched `GetManyAsync` for blocker open/closed state — instead of one `GetBlockedByAsync` REST call per item; a `blocked`-labeled row whose mirror edge set isn't populated yet gets a live safety-net fetch for just that item. The fail-closed #1600 launch gate still re-checks every stored blocker LIVE at launch, so this refresh-time read only needs mirror-freshness. Degraded live-walk fallback (mirror not usable) keeps the pre-#3350 per-item live blocked-by read. **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj` → Build succeeded, 0 warnings, 0 errors. `verify-branch-merged.mjs` → OK, `abfb8f032` merged into origin/main. Live mirror confirmed the current backlog — 31 open Batter Up + 47 open AI Batter Up. **Call count by construction:** before = `2×31 + 47 = 109` per-item live REST calls in a tight loop on a cold-start double-panel refresh; after = comments batched into `ceil(31/15)=3 + ceil(47/15)=4 = 7` GraphQL reads + blocked-by from the mirror = **0 GitHub calls** (one local `GetManyAsync`) on the common mirror path → ~7 calls total, no longer a burst that scales with lane depth. Real data confirmed the mirror-blocked-by invariant: the only open Batter Up item with edges (#3296→#3295) carries the `blocked` label and its edge is captured in the mirror (the sync fetches `blocked_by` precisely for `blocked`-labeled issues). NOT runtime-E2E'd against live GitHub (would require launching BuildConsole.exe — prohibited for verification per repo convention); verification is clean build + branch-merged + real-backlog-confirmed + call-count-bounded-by-construction, same discipline as #3347. No test manifest (BuildConsole/WPF is explicitly out of scope for manifests). No new findings filed. Working tree clean.
