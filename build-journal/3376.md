# #3376 — msp-customer-timeline.ts header docblock misdescribes sales_offers.customerId

- **Status:** ✅ DONE 2026-09-11T04:16:00Z
- **Scope:** platform

## Log
- 2026-09-11T04:00:04Z — Starting. Fixing stale header docblock in `artifacts/api-server/src/routes/msp-customer-timeline.ts` (sales_offers.customerId is tenants.id, not users.id); dropping the now-pointless bridge.get(offer.customerId) dead code per issue body.
- 2026-09-11T04:16:00Z — ✅ DONE — Corrected the header docblock to state `sales_offers.customerId` is a direct `tenants.id` FK (#2730), separately from `insights_generated_documents.customerId` which genuinely is a users.id bridged via msp_users. Dropped the now-pointless `bridge.get(offer.customerId)` dead-code lookup in the offers loop, using `offer.customerId` directly (matches the query's own direct filter against `effectiveCustomerIds`). Comment-only + dead-code removal; no behavior change, no schema/migration needed. Fix commit `b4bc80ebb` (docblock IN FLIGHT `77bfd0bc8`), rebased and merged onto `origin/main` as `c274a670f`. Branch-merged check passed (`verify-branch-merged.mjs` OK), working tree clean.
