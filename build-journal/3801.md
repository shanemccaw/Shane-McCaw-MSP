# #3801 — URGENT: _localQueuePollTimer JSON-serializes + full-rerenders the queue every 5s on the UI thread

- **Status:** ⏳ IN FLIGHT 2026-09-12T16:53:10Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-12T16:53:10Z ⏳ IN FLIGHT — Auditing `BuildQueuePanel.RefreshAsync`'s signature path (`JsonSerializer.Serialize(_lastItems)` on the UI thread every 5s) and `QueueItem`'s real shape for volatile fields that make the signature mismatch on every tick, forcing a full `RenderQueue` + `RenderBuildSetRollup` rebuild continuously.
