# #1504 — Change Control: Scheduling, collision detection, and CR dependencies

- **Status:** ✅ DONE 2026-09-03T13:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 633066b58 (rebased/pushed as 4a622dd45 on origin/main)

## Log
- 2026-09-03T12:03:07Z ⏳ IN FLIGHT — investigated existing Change Control schema
  (`msp_change_requests`, `change_freeze_windows` #1500, `cr_approvals` #1496).
  `scheduledFor` is free text (already flagged as a schema gap in
  `portal-change-control.ts`'s own header). Building: real `scheduledStartsAt`/
  `scheduledEndsAt` timestamps, a `change_maintenance_windows` table (opposite
  semantics to freeze windows — mirrors its scope/recurrence engine but does not
  merge with it), a `change_request_dependencies` (`blocked_by`) table, and
  collision detection on `targetResource` evaluated server-side at submit
  alongside the existing freeze-window enforcement. Scope stop: wire contract
  only, no `artifacts/portal` UI (scaffold has no pages yet).
- 2026-09-03T13:05:00Z ✅ DONE — Delivered exactly the wire contract asked for,
  nothing UI-side:
  - **Schema** (`lib/db/src/schema/msp.ts`): `change_maintenance_windows`
    table (opposite of #1500's `change_freeze_windows` — same scope/recurrence
    engine, deliberately its own table, never merged); `portal_change_control_
    policy.enforce_maintenance_windows` toggle (mirrors `enforce_freeze_
    calendar`); `change_request_dependencies` table (real `blocked_by` edges,
    FK cascade both directions, self-block CHECK, unique edge index). Reused
    #1762's existing `scheduled_start`/`scheduled_end` real-instant columns on
    `msp_change_requests` for collision/maintenance evaluation — no new
    columns needed there.
  - **Migration** (run against local `DATABASE_URL` in-session):
    `lib/db/migrations/manual/2026-09-03-cr-maintenance-windows-collision-dependencies-1504.sql`.
  - **Pure logic**: `portal-change-maintenance.ts` (containment-in-window
    math, own copy of the freeze module's recurrence engine — see its header
    for why not shared), `portal-change-collision.ts` (span-overlap match on
    `targetResource`).
  - **Stores**: `portal-change-maintenance-store.ts`, `portal-change-
    collision-store.ts`, `portal-change-dependencies-store.ts` (CRUD +
    `unresolvedBlockersFor`).
  - **Enforcement, no second path**: maintenance-window + collision checks
    wired into BOTH existing submit doors (`msp-changes.ts` POST and the
    shared `raiseChangeRequest` in `portal-change-control-raise.ts`) right
    alongside the existing freeze checks. `blocked_by` enforcement wired into
    `change-control-write-gate.ts`'s `claimChangeRequestForWrite` — the single
    write-authorization choke point, same as `cr_approvals`.
  - **Routes**: MSP-console CRUD `msp-change-maintenance-windows.ts` (mirrors
    `msp-change-freeze-windows.ts` exactly) and `msp-change-dependencies.ts`
    (GET/POST/DELETE on `/msp/change-requests/:id/dependencies`). Portal-facing
    calendar backing: `GET /portal/change-control/maintenance-windows`
    (mirrors the existing freeze-windows read), plus `blockedBy`/`blocks` on
    `WireChangeRequest` (bulk-fetched, one query pair for the whole register).
    All new routers registered in `routes/index.ts`.
  - **Verification**: `npx tsc --noEmit` on api-server shows zero real errors
    introduced by this work (confirmed by diffing against the SAME pre-
    existing noise class on untouched files like `portal-change-freeze-
    store.ts` — see #2526). Pure-logic modules verified with 24 real
    assertions via `node --experimental-strip-types --test` against a scratch
    file (not committed) since vitest itself is currently unrunnable
    repo-wide. Schema/constraints live-verified with real inserts against the
    local DB inside a transaction (collision-query predicate shape, the
    self-block CHECK, the duplicate-edge unique index, the blocker-status join
    `unresolvedBlockersFor` depends on) — rolled back after, no test data left
    behind.
  - **Finding filed**: #2526 (URGENT, parented to #1096) — the main checkout's
    pnpm store has empty/corrupt extractions for drizzle-orm, pg, express,
    pino, vitest and zod, breaking api-server's build, typecheck, and every
    vitest test repo-wide. Not introduced by this session; confirmed against
    the main checkout directly, not a worktree artifact.
  - **#1630**: migration line appended.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms
    `agent/1504-q841` (633066b58, pushed as 4a622dd45) is merged into
    `origin/main`.
