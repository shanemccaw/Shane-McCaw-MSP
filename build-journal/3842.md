# #3842 — Retention purgers: 5 tenant-scoped columns unregistered + red live-db tests

- **Status:** ✅ DONE 2026-09-13T04:03:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 90ab47ded (fix), 8d99a8ae9 (IN FLIGHT bookend)

## Log
- 2026-09-13T03:52:00Z ⏳ IN FLIGHT — Investigated `purgers/modules.ts`/`coverage.ts`/`declare.ts`. Confirmed all 5 tables (`automation_registry`, `communications_pushes`, `kanban_buckets`, `msp_status_reports`, `training_sessions`) carry `customer_id` (no FK, `tenants.id` keySpace `customerId`, matching the codebase's stated "successor id-space, no FK by design" convention). Child tables `communications_push_checkpoints`/`kanban_cards` cascade via their own `push_id`/`bucket_id` FK, no separate target needed. About to register a new purger module and confirm live-db tests.
- 2026-09-13T04:03:00Z ✅ DONE — Registered `mspConsoleTrackingPurger` in `artifacts/api-server/src/lib/retention/purgers/modules.ts`, covering all 5 undeclared columns, keySpace `customerId`. Baselined the OTHER two files the issue named as red first: `identity-purge.live-db.test.ts` and `post-termination-purge.live-db.test.ts` were already GREEN before any change (verified by running them individually against local Postgres) — only `tenant-scope-coverage.live-db.test.ts` was actually failing; the issue's report of the other two being red did not reproduce. After the fix: `pnpm --filter @workspace/api-server vitest run src/lib/retention` -> 8 files, 104/104 passing, real assertions unrelaxed. `tsc --noEmit` on api-server: no new errors from this change; 2 pre-existing unrelated failures hit (`auth-impersonation.test.ts` TS2352, `portal-checkout.test.ts:549` TS2345) are already filed as #3898 and #3881 — no new finding needed. Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
