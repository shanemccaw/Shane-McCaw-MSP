# #2947 — link-deps.mjs per-package-host backfill short-circuit

- **Status:** ✅ DONE 2026-09-06T14:05:00Z
- **Scope:** buildconsole (dev-server tooling)
- **Started:** 2026-09-06
- **Commit(s):** none needed — see below

## Log
- 2026-09-06T13:52:00Z ⏳ IN FLIGHT — read scripts/dev-server/link-deps.mjs and #2947's full body to confirm the short-circuit described.
- 2026-09-06T14:00:00Z — Found the exact fix already on `main`, landed by commit
  `d756ebae5` ("Fix lib/*/dist TS build failures: missing react devDependency +
  stale per-package worktree links", filed against Git #2927), merged well before
  this build was dispatched. `linkDeps()` (scripts/dev-server/link-deps.mjs:242-264)
  no longer does the flat `if (existsSync(link)) continue;` #2947 reports — it now
  branches on what the existing link at each host actually IS:
  - a reparse point (wholesale junction) → still skipped, correctly, since a
    junction is a live view of `target` and needs no re-sync;
  - a real directory (the per-package-linked-host case, Git #2152) → now calls
    `linkHostPerPackage()` again on every `linkDeps()` run, which is already
    idempotent per-entry (`if (existsSync(dstEntry)) continue`) and backfills any
    package that appeared in the host's `node_modules` (including a brand-new
    `@workspace/<pkg>` entry) after the worktree's own `node_modules` was first
    built. This is precisely the "diff existing junctioned packages against the
    source's current package list" fix #2947's body asks for.
- 2026-09-06T14:03:00Z — Verified this empirically with a standalone scratch
  Node script (not committed — pure temp-dir harness, deleted after): built a fake
  per-package host with `@workspace/somelib` + `lodash` already linked into a fake
  worktree via `linkDeps()`, then added a brand-new `newpkg` entry to the fake
  main host's `node_modules` (simulating a dependency added to `package.json`
  after the worktree existed) and re-ran `linkDeps()` on the SAME worktree with NO
  other state change. Result: `newpkg` was junctioned into the worktree on the
  second run with zero manual intervention — exactly the case #2947 says
  "silently never gets junctioned in any worktree."
- 2026-09-06T14:04:00Z — Confirmed HEAD is already an ancestor of (in fact equal
  to) `origin/main`'s tip (`441fb5378`) — no rebase/push needed, this worktree
  already carries the fix.
- 2026-09-06T14:05:00Z ✅ DONE — No code change required; #2927/d756ebae5 already
  implements and ships the exact fix #2947 requested, verified live above. Posted
  the full evidence as a comment on #2947. `git status --porcelain` is clean (no
  files touched in this worktree; the verification script lived entirely under
  the OS temp dir, never inside the repo).
