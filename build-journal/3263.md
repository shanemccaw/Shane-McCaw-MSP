# #3263 — Barcode scanner fails on Safari (real device) -- BarcodeDetector API doesn't exist there

- **Status:** ✅ DONE 2026-09-08T22:05:00Z
- **Scope:** platform (web/shanes-life — standalone Shane's Life app, Feature: Shopping #3221)
- **Started:** 2026-09-08
- **Commit(s):** 2924df914 (main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — confirmed root cause per issue body: `openScanSheet()` in
  `web/shanes-life/public/app.js` (~line 6974) gates the live-camera scan path on
  `"BarcodeDetector" in window`, which Safari has never implemented (any context, not just PWA).
  Real fallback there today is manual-entry-only. Replacing with a real cross-browser JS decoder
  (`@zxing/browser`, vendored as a static UMD file since this app has no client bundler) that
  decodes frames from the existing `getUserMedia` video stream in software, so Safari/iOS gets a
  real working scanner instead of manual-entry-only.
- 2026-09-08T22:05:00Z ✅ DONE — Vendored `@zxing/browser` 0.2.1 (MIT, UMD, self-contained) at
  `web/shanes-life/public/vendor/zxing-browser.min.js`; replaced the `BarcodeDetector` branch in
  `openScanSheet()` with `loadZxingBrowser()` (lazy `<script>` load, same pattern as the existing
  `loadPlaidLink()`) + `BrowserMultiFormatReader.decodeFromConstraints()` against the same
  `getUserMedia` stream. Camera release (`stopCamera()`) now also stops the ZXing scan controls.
  Verified: `node --check` on app.js; loaded the vendored file in a Node `vm` sandbox with a real
  browser-shaped `window`/`self`/`globalThis` and confirmed it exposes
  `window.ZXingBrowser.BrowserMultiFormatReader.prototype.decodeFromConstraints`; booted the real
  `shanes-life` server against the local `finances` Postgres DB (48 migrations already applied,
  none needed for this change) and curled `/app.js` + `/vendor/zxing-browser.min.js` -- both 200,
  correct `text/javascript` content-type. Pushed straight to `main` (2924df914),
  `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
  **Honest gap:** real-device verification on Shane's actual installed Home Screen PWA (Safari
  iOS) is still outside this session's reach -- no physical iPhone available here, same caveat
  the issue itself names. The separate iOS PWA `getUserMedia` reliability question the issue
  raises (WebKit 185448/212040/215884/273938) is unconfirmed either way and, per the issue's own
  scope note, would be a second, different bug if it surfaces.
