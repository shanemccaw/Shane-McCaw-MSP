# #3836 — Wire MSP Console operator config-state diff view (attribution + lifecycle read + re-run trigger)

- **Status:** ✅ DONE 2026-09-13T04:33:41Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** a90e6086193d38592f4af1bd6baa9b152a001e64

## Log
- 2026-09-13T03:50:51Z ⏳ IN FLIGHT — investigating msp-console config-state routes, #2759's `readDiffChanges()`/`readDiffVerdictRollup()`, and #2820's portal-side wire pattern (`configStateWire.ts`/`configStateLive.ts`) before wiring the operator diff view.
- 2026-09-13T04:33:41Z ✅ DONE — built `artifacts/msp-console/src/pages/config-state/{api.ts,ConfigState.tsx}` and mounted it at the console's already-registered-but-unwired `config` nav id (`sel.kind==="msp" && sel.page==="config"`). Reads `GET /api/msp/config-state/diffs` (list, mode-filterable) and `GET .../diffs/:diffId` (per-change `attribution` + `lifecycle`, whole-diff verdict roll-up), and wires `POST .../diffs/:diffId/attribution` as the re-run trigger. No fixture data — added `treeModel.ts` pageMeta and one new icon (`circle-help`) only.
  Verified live, not just typechecked: seeded one real sealed `drift` diff for the real testbed tenant (`tenants.id=1`) via the actual `attributeDiff()` engine (real CR, real accepted-risk decision, one real unattributed row) — ran the full `config-change-attribution.live-db.test.ts` suite first (12/12 pass) to confirm the engine itself. Curled the real running dev api-server with a real operator JWT and diffed the JSON against my TS types exactly — caught a real bug this way (`POST .../attribution`'s count field is `changesAttributed`, not `attributed`) and fixed it before shipping. Then drove the actual page with a real headless Chromium (playwright-core + the pre-installed local Chromium, no download) against my worktree's own msp-console dev server proxying the real API: real login, real diff card, real drawer with the three real verdicts/lifecycle lines, real "Match against approvals again" click → real 200 → real toast. All verification data (DB rows + synthetic user) deleted afterward; confirmed 0 rows remain.
  Filed **#3942** (parented under #1850, this issue's own Feature; labeled `bug`, `URGENT:` title, board status "AI Batter Up"): `GET /api/msp/auth/me` is documented in the OpenAPI spec and required by every console session's `AuthGate`, but was never implemented server-side on `origin/main` — 404s for every real operator, blocking the whole console past login. Confirmed with a plain `curl` against a real valid JWT, independent of any browser. Worked around it only inside the verification driver (a `page.route` stub), never in shipped code.
  `pnpm run typecheck` clean, `vite build` clean, branch verified merged into `origin/main` (`verify-branch-merged.mjs` exit 0), `git status --porcelain` clean.
