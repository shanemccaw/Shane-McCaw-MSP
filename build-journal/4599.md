# #4599 — Make memory-pressure auto-pause thresholds real, persisted Settings (not hardcoded)

- **Status:** ✅ DONE 2026-09-18T00:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** `0c153e6f7` (BuildConsoleSettings fields), `79bfcd462` (ResourceMonitor + Settings UI wiring)

## Log
- 2026-09-17T23:35:00Z ⏳ IN FLIGHT — Pulled #4599 + #4542/#4543/#4561/#4577 bodies/comments/bookends.
  Confirmed the real existing persistence/live-apply pattern to mirror: `BuildConsoleSettings`
  (`%AppData%\BuildConsole\settings.json`, field-initializer defaults) for the persisted values,
  `MainWindow.SettingsTab.cs` forwarding methods for live-apply, `SettingsTabView` TextBox+Save
  (numeric) / CheckBox (bool) controls for the UI — same shape as #4542's
  `UpdateMaxConcurrentBuildSlots`/`UpdateHardCapBuildSlots`. Unlike those two (which forward into
  `QueueWatcherService`), the memory-pressure thresholds live entirely in
  `MainWindow.ResourceMonitor.cs` (`EvaluateMemoryPressure`, UI-thread poll) — so the new
  forwarding methods set instance fields on `MainWindow` directly rather than on the queue
  watcher. Added the 4 new `BuildConsoleSettings` properties (`MemoryPressureAutoPauseEnabled`
  default true, `MemoryPressurePauseThresholdPercent` default 98, `MemoryPressureResumeThresholdPercent`
  default 92, `MemoryPressureClearSustainSeconds` default 10 — picked per Shane's ask: pause much
  higher/closer to real swapping, resume/release faster). Proceeding to convert the
  `MainWindow.ResourceMonitor.cs` consts to instance fields, wire the on/off gate into
  `EvaluateMemoryPressure`, add forwarding methods, and build the Settings UI section.
- 2026-09-18T00:20:00Z ✅ DONE — Shipped in `0c153e6f7`/`79bfcd462`. Added 4 real
  `BuildConsoleSettings` properties (`MemoryPressureAutoPauseEnabled` default true,
  `MemoryPressurePauseThresholdPercent` default 98, `MemoryPressureResumeThresholdPercent`
  default 92, `MemoryPressureClearSustainSeconds` default 10). Converted
  `MainWindow.ResourceMonitor.cs`'s `PhysicalLoadPauseThreshold`/`PhysicalLoadResumeThreshold`/
  `MemoryPressureClearSustain` from `const`/`static readonly` to real instance fields
  (`_memoryPressurePauseThreshold`/`_memoryPressureResumeThreshold`/`_memoryPressureClearSustain`),
  loaded from settings at startup (`LoadMemoryPressureSettings`, clamped: pause 1-100, resume
  0..pause-1, sustain >= 0) and live-appliable via 4 new public methods
  (`UpdateMemoryPressurePauseThreshold`/`UpdateMemoryPressureResumeThreshold`/
  `UpdateMemoryPressureClearSustainSeconds`/`UpdateMemoryPressureAutoPauseEnabled`) that
  `SettingsTabView`'s new Save/Checkbox handlers call directly on the live `MainWindow` instance
  — no restart needed. Added a real on/off master switch: `EvaluateMemoryPressure` now gates on
  `_memoryPressureAutoPauseEnabled` first — when off it skips pause/resume evaluation entirely
  and, if a pause was already in effect, immediately calls `SetMemoryPressurePause(false)` and
  resets `_memoryPressureClearSince`, so flipping it off for a bedtime run doesn't leave the
  queue stuck paused. `PagefileTrendSamples`/`PagefileRiseMinBytes`/`PagefileClearToleranceBytes`
  deliberately left as internal tuning constants per the issue's own scope note — Shane didn't
  ask to control those. New Settings UI section in `SettingsTabView.xaml` (checkbox + 3
  TextBox+Save threshold/duration controls, same visual/interaction pattern as #4542's Max
  Concurrent/Hard Cap), with real validation in the code-behind: pause threshold must exceed the
  resume threshold and vice versa (cross-checked against the currently-persisted value on each
  save), sustain seconds must be >= 0. Updated the file-header/const doc comments to describe
  these as real, Shane-tunable Settings rather than fixed values.
  **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — 0
  Warning(s), 0 Error(s), both before and after the final rebase onto origin/main. Real
  BuildConsole UI click-through not run (HARD RULE — never launch BuildConsole.exe for
  verification; no `%AppData%\BuildConsole\settings.json` yet exists in this environment to
  attach a live session to either). Instead, per #4543/#4561's own standalone-harness
  precedent: built a throwaway console app (outside the repo, deleted after use) replicating
  the exact `BuildConsoleSettings` camelCase Save/case-insensitive Load serialization and the
  new `LoadMemoryPressureSettings` clamp logic — 15/15 checks passed, covering (1) round-trip
  of all 4 new fields through the real serializer settings, (2) a pre-#4599 settings.json with
  no keys deserializing to exactly the new defaults (98/92/10/enabled), (3) normal values
  passing through the clamp unchanged, (4) a corrupted/hand-edited resume>=pause value being
  clamped down to pause-1 rather than producing a broken live state, (5) an out-of-range pause
  (150) clamped to 100 with resume still correctly below it. `git status --porcelain` clean.
  `verify-branch-merged.mjs` — OK, `89acd656` merged into `origin/main`. No findings filed (no
  separate defects discovered); no `Shane To-Do`.
