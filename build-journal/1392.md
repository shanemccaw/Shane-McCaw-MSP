# #1392 — never-scanned tenants get a fabricated 100 score (catalog-wide fallback)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** b931626dc (fix), 05d673c5e (IN FLIGHT bookend)

## Log
- 2026-08-27 ⏳ IN FLIGHT — `fetchTenantEvaluableSignalKeys` (pillar-coverage.ts) must distinguish "scan scope unresolvable due to error" from "tenant genuinely never scanned". The never-scanned case must NOT use the catalog-wide denominator fallback (that fabricates a full denominator + zero numerator → a perfect 100). Distinguishing signal: whether ANY `msp_diagnostic_runs` row exists for the tenant (`fetchScannedCheckKeys` → new `hasAnyRun`). Also verifying blast radius on individual pillar pages post-#1387.
- 2026-08-27 ✅ DONE — Shipped in b931626dc. `fetchScannedCheckKeys` now returns `hasAnyRun` (true iff ≥1 `msp_diagnostic_runs` row exists, independent of package_key). `fetchTenantEvaluableSignalKeys` branches on it: `hasAnyRun === false` (never scanned) → **empty** evaluable set → `evaluatePillarDisplay`'s `evaluableSignalCount === 0` guard → `not_evaluated`/null, NOT a fabricated 100; runs-exist-but-unresolvable-scope → catalog-wide fallback kept (honest best-effort). war-room-pillar-stats threads `hasAnyRun` through its pre-resolved-scope call. **Blast radius (issue's follow-up):** the 7 other callers that score individual pillar pages (copilot-gate, finding-point-impact, telemetry-comparison, cio-narrative-generator, pillar-trend, admin-signal-rules route, msp-diagnostics route) all invoke `fetchTenantEvaluableSignalKeys` WITHOUT a pre-resolved `scannedCheckKeys`, so they take the internal `fetchScannedCheckKeys` path and pick up the fix automatically — YES, this same catalog-wide fallback explained their remaining post-#1387 fabricated 100s too, and all are corrected by this one change. Verified: 413-spectrum test 8/8 pass (incl. two new #1392 cases proving empty-set → all pillars null AND the unresolvable-scope case still catalog-wide); security-posture/finding-point-impact/pillar-trend 38/38 pass; `tsc --noEmit` clean on all touched files. No schema/migration; unit-test-only coverage (api-server, outside the msp-portal/marketing test-manifest scope).
