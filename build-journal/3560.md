# #3560 — GraphProbeModal fabricates every probe response client-side instead of calling the real API

- **Status:** ✅ DONE 2026-09-10T23:52:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 71b26a137

## Log
- 2026-09-10T23:30:00Z ⏳ IN FLIGHT — Pulled issue #3560 body. Verified all 4 `ENDPOINT_PRESETS` are real GET-only routes (`admin-observability.ts`, `msp-audit-log.ts`) — no write/mutating presets found, so no fabricated-preset carve-out needed. Wiring `handleTestProbe` to `useAdminFetch()` for real fetch() calls, removing fabricated status/headers/latency.
- 2026-09-10T23:52:00Z ✅ DONE — `handleTestProbe` in `artifacts/admin-panel/src/components/GraphProbeModal.tsx` now makes a real GET via `useAdminFetch()`'s authenticated fetch against the actual gateway; status/statusText/headers/body/duration/size all come off the real `Response`. All 4 presets are GET-only (confirmed against route source, not assumed) so all 4 were wired live — no preset was left fabricated. `pnpm exec tsc --noEmit` on `artifacts/admin-panel` is clean (exit 0, 0 errors). Commit `71b26a137` verified as an ancestor of `origin/main` via `verify-branch-merged.mjs`. `git status --porcelain` clean. No findings filed — no write-preset carve-out was needed, and no missing backend gap was hit.
