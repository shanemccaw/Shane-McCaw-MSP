# #2901 — Remove stale #2032 resourceKeys-withholding guard

- **Status:** ⏳ IN FLIGHT 2026-09-05T00:21:04Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-05T00:21:04Z ⏳ IN FLIGHT — #2032 (fixed 2026-09-04, commit 501c9139e) removed the
  cache-key collision that made `resourceKeys` unsafe on `msp-config-state-diffs.ts` and
  `portal-config-state.ts`. Updated the stale doc comments on both files. Added real
  `resourceKeys` support to the MSP operator's `POST /msp/config-state/diffs` (validated,
  passed through to `diffDrift`/`diffAgainstBaseline`/`diffTenants`/`diffPromotion`, same as
  the sibling `admin-config-diffs.ts` route). Left the portal's `GET /portal/config-state/changes`
  withholding `resourceKeys` deliberately — per the issue body, exposing narrowing on a
  whole-tenant-picture read endpoint is a real product decision, not a mechanical follow-up to
  the #2032 fix — and rewrote its comment to say so instead of citing the closed bug.
