# #2114 — Retention policy for configuration snapshots and their objects

- **Status:** ✅ DONE 2026-09-04T06:47:38Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** e853d6a70 (rebased hash on main; local commit was d2bd3008b)

## Log
- 2026-09-04T06:27:58Z ⏳ IN FLIGHT — investigated `tenant_config_snapshots` /
  `tenant_config_snapshot_objects` / `config_diffs` / `config_snapshot_baselines`
  schema (#1795/#1797/#1843) and the existing bounded-accumulation patterns
  (`platform_log_stream_prune` node, `tenant_engine_snapshots` rollup+purge
  workflow) to match conventions. Confirmed `config_snapshot_baselines.snapshot_row_id`
  is already `REFERENCES ... ON DELETE NO ACTION` (DB-enforced protection), but
  `config_diffs.base_snapshot_row_id`/`head_snapshot_row_id` are `ON DELETE CASCADE`
  — so a prune job must explicitly exclude snapshots referenced by any `config_diffs`
  row itself, not rely on the FK to block it (the FK would silently cascade-delete
  the "immutable" diff instead). Building a real count-of-recent-snapshots-per-tenant
  prune: a new `config_snapshot_prune` workflow node type + nightly scheduled system
  workflow, with a real audit table for prune-run history.
- 2026-09-04T06:47:38Z ✅ DONE — Real per-tenant count-cap retention shipped
  (commit `e853d6a70` on main):
  - `config_snapshot_prune` workflow node
    (`artifacts/api-server/src/lib/config-snapshot-retention-nodes.ts`): keeps
    the most recent `keepPerTenant` (default 20) non-`running` snapshots per
    tenant, deletes the rest in one statement, EXCLUDING any snapshot a
    `config_diffs` row (base or head, any diff, ever) or a
    `config_snapshot_baselines` row still references. `config_diffs`' own FK
    is `ON DELETE CASCADE`, so that exclusion is application-enforced, not
    left to the database.
  - Migration `lib/db/migrations/manual/2026-09-04-config-snapshot-retention-2114.sql`
    — ran myself against local Postgres: new `config_snapshot_prune_runs`
    audit table (every run's real candidate/excluded/deleted counts, never
    estimated) + `config_diffs_head_snapshot_row_id_idx` (needed by the
    exclusion check).
  - Nightly scheduled system workflow `__system__: Tenant Configuration
    Snapshot Retention Prune` (cron `0 3 * * *`) in `seed-system-workflows.ts`
    — real ongoing enforcement via `seedSystemWorkflows()` on api-server boot,
    not a one-time cleanup script.
  - Registered node type in `node-type-registry.ts`, wired dry-run + real
    executor cases in `workflow-executor.ts`, added `"config_snapshot_prune"`
    to the `WfNode.type` union in `lib/db/src/schema/index.ts`.
  - **Live-verified end-to-end** against real data on the local DB: 7 real
    snapshots, 4 real `config_diffs` rows, 1 real baseline. Ran the handler
    with `keepPerTenant: 2` — correctly deleted the 3 snapshots with no
    reference (ids 5, 6, 7) and left every diff/baseline-referenced snapshot
    (8, 9, 10) and the newest one (11) untouched; all 4 `config_diffs` rows
    survived; `config_snapshot_prune_runs` recorded the run with the exact
    real counts (candidates=5, protectedByDiff=2, deleted=3).
  - Unit tests: `config-snapshot-retention-nodes.test.ts` (6 tests, mocked
    `db.execute`), registered in `vitest.config.ts`'s explicit include
    allowlist. `lib/db` and `artifacts/api-server` both typecheck clean.
  - Found `#1982` already tracks the pre-existing, unrelated
    `broadcastWorkflowRunError` mock-gap test failures hit while running the
    broader `workflow-executor-core.test.ts` suite (confirmed present on
    `origin/main` before this change too, via a temporary stash) — no new
    finding needed.
  - Branch verified merged into `origin/main`
    (`scripts/dev-server/verify-branch-merged.mjs`); working tree clean.
