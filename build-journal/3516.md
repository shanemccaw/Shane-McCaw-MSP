# #3516 — DoneBookendVerifier only matches bold **Status:**/**Commit(s):** — plain-format bookends silently missed

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** `be7054e4b` (the fix), `8bf26c5cd` (IN FLIGHT bookend) — both rebased onto and
  pushed directly to `origin/main`; `verify-branch-merged.mjs` confirms OK.

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — pulled issue body + comment, confirmed real evidence
  (`build-journal/3457.md`, `3470.md`, `3475.md` all use plain, non-bold `Status:`/`Commit:`
  lines on `origin/main` right now), about to widen `DoneBookendVerifier.cs`'s
  `StatusFieldRx`/`CommitFieldRx`.
- 2026-09-12T00:00:00Z ✅ DONE — widened both `StatusFieldRx` and `CommitFieldRx` in
  `desktop/BuildConsole/Services/DoneBookendVerifier.cs` to accept plain (non-bold)
  `Status:` and singular/plural `Commit:`/`Commits:`/`Commit(s):` lines, in addition to
  the bold markdown forms, anchored to line start so a mid-sentence mention of the word
  isn't picked up as a real field. Left the ancestor-commit verification
  (`git cat-file -t` / `git merge-base --is-ancestor`) untouched per the issue's explicit
  instruction.

  **Verification:**
  - `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors.
  - A scratch regex+git harness (built and run outside the repo, then deleted) confirmed,
    against the real evidence cited by the issue:
    - `build-journal/3457.md` (plain `Status: DONE` / `Commit: e5ccef0a5...`) — OLD regex:
      no match on either field. NEW regex: matches, extracts `e5ccef0a51c0c339d8cc6505efdc938f8fcdbd8b`,
      confirmed a real commit object and a real ancestor of `origin/main`.
    - `build-journal/3470.md` (plain `Status: ✅ DONE` / `Commit: 81deaa198...`) — same
      OLD-fails/NEW-matches result; extracted hash `81deaa198c34f554e544bccdafa8e6c7fa65a99b`
      confirmed a real ancestor of `origin/main`. (Note: this file's live status has since
      moved on from the `🛑 BLOCKED` state the issue originally cited to `✅ DONE` — a
      synthetic plain `Status: 🛑 BLOCKED` string was used to separately confirm
      `VerifyBlockedAsync`'s last-match BLOCKED detection still fires correctly for that
      exact scenario.)
    - `build-journal/3475.md` (plain `Status: DONE` / `Commit: 59bbab3f6...`) — same
      OLD-fails/NEW-matches result; extracted hash `59bbab3f6c43051b9361f1da8bf71f933ba6931b`
      confirmed a real ancestor of `origin/main`.
    - A prose false-positive guard (a mid-sentence, non-line-start mention of the word
      "Status:") confirmed the anchored regex does NOT false-match it — only the real
      field-label line matched.
  - `verify-branch-merged.mjs`: OK — `be7054e4b` merged into `origin/main`.
  - `git status --porcelain`: clean.

  No test-manifest requirement — Test Coverage section explicitly scopes manifests to
  `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`; this is a
  `desktop/BuildConsole` (WPF) change.

  No new findings filed this session — the issue's own described gap was the finding,
  and it's now fixed.
