# #1538 — Remediation: Checklist derived from findings, not a standalone step list

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** b4141c93856ca60b55cb8c3a910ab10e17e4ebae

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1538 body/comment and #1539 (foundation) body/comments.
  Building on #1539's fix-route resolver: a new findings-derived checklist wire
  contract (`GET/PUT /api/portal/remediation/checklist`) that joins
  `msp_diagnostic_findings` (latest scan, the tenant's actual open findings) to
  `remediation_knowledge_base` by `checkKey` for fix content, resolves fix route via
  `resolveFixRoute`, and keys the customer's tracker claim (`remediation_tracker_steps`)
  by `checkKey` — the finding's own stable identity — instead of a hand-authored
  step id, so resolving the finding is what resolves the item. Ends at the wire
  contract; no `artifacts/portal` page (no design export for this module).
- 2026-08-30 ✅ DONE — b4141c93856ca60b55cb8c3a910ab10e17e4ebae (real post-push hash,
  re-checked). Shipped:
  - `lib/remediation-checklist.ts` — `resolveRemediationChecklist(customerId)`:
    latest-run adverse findings (critical|warning) → items joined to
    `remediation_knowledge_base` (published only), config-pack availability, and
    `resolveFixRoute()` (#1539). `isKnownCheckKey()` validates against real
    `monitor_checks.key`.
  - `routes/portal-remediation-checklist.ts` — `GET/PUT
    /api/portal/remediation/checklist(/:checkKey)`. PUT upserts the SAME
    `remediation_tracker_steps` table the legacy s1-s30 route uses (`step_id` is
    plain text, no CHECK constraint — no schema change, no migration needed).
  - `remediation-tracker-verification.ts` — `mappedKeysFor()` self-maps any
    non-`s\d+` `step_id` to its own checkKey, so findings-derived items get the
    same real-rescan verify/drift treatment as the legacy world.
  - Registered in `routes/index.ts`. Does not touch the s1-s30 catalogue, route,
    export or `customer-tenant-alert-engine.ts`'s three SQL counts (they key on
    customer_id + status/verification_state only, never step_id).
  - No migration — no schema change.
  - Verification: tsc clean on all touched files (61 pre-existing errors
    elsewhere, matching #1765 et al — none mine). vitest:
    `remediation-checklist.test.ts` (new, 6/6 passing),
    `remediation-fix-route.test.ts` (all passing),
    `remediation-tracker-verification.test.ts` (14/14 passing — 12 pre-existing +
    2 new `#1538` self-map cases — plus 1 pre-existing unrelated failure, see
    below). Live DB: tenant 1's latest scan has 35 real critical/warning
    findings; `writeBack` consent granted.
  - Branch verified an ancestor of `origin/main`
    (`verify-branch-merged.mjs`); working tree clean.
  - Finding filed: **#1936** — 5 drift-guard tests across 3 remediation test
    files (`remediation-tracker-catalogue.test.ts`,
    `remediation-tracker-verification.test.ts`,
    `portal-remediation-tracker.test.ts`) read source files under
    `artifacts/msp-portal`, which was fully retired before this session
    (pre-existing, not caused by this build). Parented to #1489, board set to
    AI Batter Up.
