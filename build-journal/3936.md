# #3936 — Demote execution_ready write_action_catalog rows on exchange-online:// pseudo-scheme templates

- **Status:** ✅ DONE 2026-09-13T06:35:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 43e4f081b (rebased/pushed as d45751e89 on main)

## Log
- 2026-09-13T06:03:00Z ⏳ IN FLIGHT — Confirmed live state: 13 `baseline_action_templates` rows still
  use the `exchange-online://` pseudo-scheme (unchanged from the issue's list). Of those, 12 are
  linked `write_action_catalog` rows (ids 185-196), all currently `execution_ready`
  (`action.remove-forwarding-rule` has no linked catalog row). Real `status` vocabulary confirmed:
  `endpoint_design_pending`, `blocked_no_workaround`, `metadata_pending`, `execution_ready`. About to
  write an additive `UPDATE` migration demoting those 12 rows to `endpoint_design_pending` and
  recording why in `blocked_reason` (real precedent: catalog id 204 already uses `blocked_reason` for
  exactly this "no real endpoint yet" explanation).
- 2026-09-13T06:35:00Z ✅ DONE — Wrote and ran
  `lib/db/migrations/manual/2026-09-13-demote-exchange-online-pseudo-scheme-3936.sql` against local
  Postgres myself: `UPDATE 12` (ids 185-196), verified idempotent on re-run (`UPDATE 0`). All 12 now
  `status = 'endpoint_design_pending'` with a real explanation in `blocked_reason`. While implementing,
  found and fixed a real, related gap: `POST /msp/:mspId/launch-control/execute`
  (`artifacts/api-server/src/routes/msp-launch-control.ts`) only gated on `catalogRow.templateId`
  being non-null, never on `status` — a demoted row with `templateId` still set remained executable
  via the API. Added an explicit `status !== "execution_ready"` check (409) before proceeding.
  `tsc --noEmit` clean on the touched file (one pre-existing, unrelated error remains in
  `auth-impersonation.test.ts`, not touched here). No dedicated test file exists for
  `msp-launch-control.ts` (route is api-server-internal, outside the msp-portal/marketing
  test-manifest scope) — verified by direct DB query + typecheck instead, no test suite regressed
  (`vitest run` found no test file for this route or `workflow-executor.ts`, consistent with
  pre-existing state, not something this change removed). Option (a) (wiring
  `runBaselineTemplateAgainstTenant` through `ps-execution-client.ts`) explicitly out of scope per
  the issue's own instruction — filed as sibling issue **#3948** under Feature **#2494**, board
  status set to "AI Batter Up". Commit `43e4f081b`, rebased and pushed to `origin/main` as
  `d45751e89`. `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
