# #4331 — Delete debug write-consent route, repoint any portal callers

- **Status:** ✅ DONE 2026-09-17T03:15:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** cdf7d21c1 (Remove debug write-consent route from Portal)

## Log
- 2026-09-16T00:10:00Z ⏳ IN FLIGHT — grepped artifacts/portal for `debug-write-reconsent-link`, found no callers; removing the debug route (`POST /portal/consent/debug-write-reconsent-link`, artifacts/api-server/src/routes/consent.ts) entirely since the real successor `POST /portal/consent/write-consent-link` (#1375) already covers the same flow with a proper auth floor.
- 2026-09-17T03:15:00Z ✅ DONE — Deleted the debug route (69 lines) and its comment block; the only remaining `/portal/consent/write-consent-link` registration is the real #1375 route (`requireCapability("ladder.customer-user")`), confirmed via `grep -n "router.post(\"/portal/consent"`. `npx tsc --noEmit -p .` on artifacts/api-server passed clean (no errors referencing consent.ts). Full `pnpm run build` (typecheck + esbuild) also passed clean. Committed cdf7d21c1, rebased onto and pushed directly to origin/main (`git push origin HEAD:main`), verified merged via `scripts/dev-server/verify-branch-merged.mjs`. Live end-to-end HTTP verification (404 on the debug path, 200/expected-4xx on the real path against a running server) was NOT completed: the change is part of buildSet `SchemaDrift` (19/21 → merged, per `request-restart.mjs --agent 4331`), whose single coordinated restart is deferred until the whole set completes — that restart is outside this session's control. A local standalone server run was also attempted for verification but blocked: it requires exporting the full `.env.local` (many vars incl. secrets) into a shell, and the harness's credential-leakage classifier correctly denied that action; a narrower `DATABASE_URL`-only export got the server past its first fatal check but it then failed on a second required env var (`AI_INTEGRATIONS_ANTHROPIC_BASE_URL`), so a piecemeal env approach doesn't get to a live listener either. No code faults found; this is a verification-environment gap, not a doubt about the change. No fixture/hardcoded data introduced. `git status --porcelain` clean.
