# #4125 — ShaneBot engine: Batch A — Governance & Risk cluster

- **Status:** ✅ DONE 2026-09-14T22:52:53Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** df2f95230 (ShaneBot engine: Batch A — Governance & Risk cluster)

## Log
- 2026-09-14T22:28:04Z ⏳ IN FLIGHT — Investigated shanebot-engine.ts's existing intent→grounding→card pattern, the real backend routes for risk/changes/findings/poams/secplan/raci/policy, the ShaneBot.dc.html reference script (intent regexes + `ownMatch` workload-lookup pattern), and the real Governance pillar signal catalog (`portal-governance-areas.ts`). Starting implementation of the 10 Batch A topics as new `GenericCardData`-shaped cards + grounding prose.
- 2026-09-14T22:52:53Z ✅ DONE — Implemented all 10 Batch A topics (risk, changes, findings, poams, secplan, raci, raci-workload, policy, conditional-access, signal) in `shanebot-engine.ts`'s `buildCustomerContext`, each a real customer-scoped query gated on a (mspId, tenantId) scope derived from the same tenants row already fetched (no extra query). Extended `BotCardType`/`allowedCardTypes` and wired `support-chat.ts`'s `cardsBlock`/`resolveCardData` for the 10 new `[SHOW_CARD:x]` tokens, and threaded the turn's own last user message through for the two dynamic lookups (`raci-workload`'s "who owns X?", `signal`'s governance keyword match). Findings-topic citation corrected from the dispatch's `portal-remediation-tracker.ts` to the real backing already queried in this file (`mspDiagnosticFindingsTable`) — the Card Gallery's actual finding rows are diagnostic scan findings, not the 28-step remediation-guide checklist. Fixed 6 pre-existing test-mock regressions the new queries caused (an extra unconditional scope-resolution DB call, and reusing a route-level orchestration function inside a mocked hot path) by deriving scope inline from the already-fetched customer row and gating every new query — including ownership — behind that scope. Added a dedicated pure-function unit-test suite (10 topic builders, DB-free) plus a new `shanebot-engine-batch-a.live-db.test.ts` that live-verifies 5 of the 10 topics against real Postgres rows for the sanctioned testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1) — every asserted value is queried fresh from the DB at test time and compared to the engine's real output, not hardcoded. Verification: `tsc --noEmit` clean; 78/78 mocked unit tests pass (shanebot-engine.test.ts + support-chat.test.ts); 5/5 new live-Postgres tests pass. Filed #4144 (parented under Feature #4124, board status AI Batter Up) for a leftover soft-deleted synthetic POA&M row found polluting the shared testbed tenant's real `msp_poams` table during live verification. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
