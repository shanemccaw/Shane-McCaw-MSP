# #2785 — Log Viewer core: port LogService.cs multi-source contract into BuildConsole

- **Status:** ✅ DONE 2026-09-04T01:12:58Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** d3edadac4 (LogService port), d75e3e450 (IN FLIGHT bookend)

## Log
- 2026-09-04T01:04:30Z ⏳ IN FLIGHT — Porting `desktop/ShaneBuilder/Services/LogService.cs`'s real `ILogService`/`LogSource`/`LogLine`/`LogQuery`/`ArchiveNode` contract into `desktop/BuildConsole/Services/`. Live per-source audit done first: all 9 sources verified reachable from BuildConsole's real paths — `.logs/dev-all/{shane-mccaw-consulting,portal,admin-panel}.log` (MAIN checkout), `platform_log_stream` (24,103 rows), Postgres server log dir `D:/Program Files/PostgreSQL/18/data/log/`, shared `%TEMP%\bt-build-queue-logs\queue-*.log`, new Console Output ring buffer, honest-empty SSH/Terminal. Finding: `platform_log_stream.id` is `int4` not `int8` — ShaneBuilder's live-tail `(long)`-unbox + `GetInt64` throw on this schema; fixing in the port with `Convert.ToInt64`/`GetInt32`.
- 2026-09-04T01:12:58Z ✅ DONE — Ported `LogService.cs` into `desktop/BuildConsole/Services/LogService.cs` (`namespace BuildConsole.Services`), identical `ILogService`/`LogSource`/`LogLine`/`LogQuery`/`ArchiveNode` data contract so #2786/#2787/#2788 build on it unchanged. BuildConsole-side reuse instead of ShaneBuilder's private copies: `BuildQueuePostgresClient.ParseConnectionString` (URL→Npgsql) and `BuildLogPaths.LogDirectory` (the shared `%TEMP%\bt-build-queue-logs` path); kept ShaneBuilder's own worktree-aware `ResolveMainRepoRoot` (commondir → MAIN checkout) because dev-all logs + `.env.local` live in the MAIN checkout, not the worktree (Git #1395), and BuildConsole's `FindRepoRoot` can return the worktree root. **Real correctness fix over the source:** `platform_log_stream.id` is `int4` — replaced `(long)`-unbox + `GetInt64(0)` with `Convert.ToInt64(...)` on both the MAX(id) init scalar and the row read, so the API Server LIVE tail works (the unfixed source throws `InvalidCastException` here). Filed the latent-bug finding as #2791 (sibling of #2784). Verified with a throwaway console harness (built outside the repo, deleted before commit): `dotnet build BuildConsole.csproj` clean (0 warn/0 err); all 9 sources returned honest data — api 2000 rows all real-level (inferred=0), marketing/portal/admin/sql/build real file lines all `LevelIsInferred=true`, ssh/terminal 0 rows (honest empty), console ring-buffer sink 2 real-level rows; Archive built a 9-day / 154-build tree and opened a real bookend leaf (3276 chars); LIVE `api` tail positively picked up a mid-window inserted marker row (`engine.test`, proving the int4 poll loop reads without throwing) — marker row then deleted. No fixture/hardcoded data. Working tree clean.
