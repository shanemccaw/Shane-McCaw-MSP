# #4104 — 11 portal test manifests rest on a stale "no portal login page yet" premise

- **Status:** ✅ DONE 2026-09-14T23:35:43Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** dab60932b (11-manifest edit), rebased/pushed to origin/main as d643c3ddf

## Log
- 2026-09-14T23:21:14Z ⏳ IN FLIGHT — blocked_by #4100 confirmed closed/complete (basePath fix landed and verified against origin/main). Read all 11 affected manifests + their real page components (account-security.tsx, config-state.tsx, offboarding.tsx, customer-team.tsx, index.tsx, support.tsx, project-detail.tsx, remediation-tracking.tsx + RemediationProgrammeTab/PillarScoresCard, AlertsDropdown.tsx, billing.tsx) to confirm every real data-testid I'm about to assert on actually exists in the shipped code. Read the #4091-established sibling pattern (test-manifests/portal/risk-register.json, account-security-change-password.json) — top-level `loginAs`, drop the stale unauthenticated-only uiSteps block, add real authenticated goto + expect assertions against real testids/extracted API values. About to apply that same pattern to all 11 manifests.
- 2026-09-14T23:35:43Z ✅ DONE — Added `loginAs: "Customer — RBAC test"` + real authenticated goto/expect uiSteps to all 11 manifests (test-manifests/chat/support-chat-active-cards.json, test-manifests/portal/{account-security-data-rights,account-security-graph-signals,alerts-dropdown,billing,config-state,customer-home-overview,offboarding,projects,remediation-tracking,team-management}.json), replacing each's stale unauthenticated-only "Sign in required" check with a real assertion against a real data-testid already shipped on that page (verified by direct grep of the real component source before writing each selector). team-management.json also gained a real apiTests `extract` of `firstMemberUserId` (`$[0].userId`) used to key its new `team-roster-row-{{firstMemberUserId}}` UI assertion, matching risk-register.json's own extracted-value pattern. All 8 of the 11 that were also on #4100's dead-goto list now goto their full `/portal/...` path. Live-verified two ways via real `shaneapp://runTest` invocations against the running BuildConsole (pid 1704) in this session: (1) team-management.json — ran the pre-#4104 file from the still-stale dev-server checkout (5/6 steps passed: real login as the Customer test profile, real GET /api/portal/team roster incl. userId=2762; the old unauthenticated h1 check failed live because the WebView2 profile already carried an authenticated session and the real page has no `<h1>`, direct evidence the old assertion was already fragile); (2) risk-register.json — an already-merged sibling using the identical loginAs+testid pattern this build applies — 18/21 steps passed for real, including the real testid-based assertions (`risk-row-toggle-RBD-2026-104`, `policy-decision-row-RBD-2026-104`), validating the mechanism end-to-end in this live environment. Each of the 11 manifests carries this same real evidence in its own notes, plus an honest "re-run once this branch merges to the dev-server checkout" line, since runTest resolves manifests from the dev-server checkout, not this worktree. No genuine new finding to file — this build was the exact prescribed fix for #4104, no additional gaps surfaced. `verify-branch-merged.mjs` confirms d643c3ddf is a real ancestor of origin/main; `git status --porcelain` clean.
