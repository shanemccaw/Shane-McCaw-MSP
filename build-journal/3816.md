# #3816 — DLQ: wire the real MSP Console dead-letter-queue module (screen 43)

- **Status:** ✅ DONE 2026-09-12T20:05:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 0ce984bbb (bookend IN FLIGHT), b1fb655a0 (DLQ module), rebased/pushed as 53ba8fc76 on `main`

## Log
- 2026-09-12T19:35:00Z ⏳ IN FLIGHT — verified #3446/#3445 fixed on `main` (`isReplayable()` guard + `replayable` field + correct bulk-replay path in `msp-dlq.ts`); building the DLQ module wired to the real, now-working four-route surface.
- 2026-09-12T20:05:00Z ✅ DONE — Built `artifacts/msp-console/src/api/dlq-api.ts` (data seam: list, single replay, bulk replay, resolve, payload save) and `artifacts/msp-console/src/console/modules/Dlq.tsx` (module page: filter chips, parked-item list with bulk-select, detail panel with real replay/discard/handled-by-hand/payload-edit actions). Wired into `nav.ts` (Operations tree entry `dlq`), `treeModel.ts` (page meta) and `ConsoleShell.tsx` (`moduleFor` dispatch) at `/ops/dlq`. Replay and Bulk Replay are built as real, working actions gated by the server's real `replayable` field, per #3446/#3445 both being fixed — not held-closed UI. `tsc --noEmit` clean; `vite build` clean (965.89 kB bundle, no errors). No fixture data — every value comes from `GET /api/msp/dlq`. `verify-branch-merged.mjs` confirmed `53ba8fc76` is an ancestor of `origin/main`. `git status --porcelain` clean.
