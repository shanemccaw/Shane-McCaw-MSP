# #3545 — GET /msp/sales-offers/sse allows PlatformAdmin ?mspId= cross-MSP override

- **Status:** ✅ DONE 2026-09-11T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 5f09008c9 (pushed to main as 15e1bf064)

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — removing the inline PlatformAdmin `?mspId=` override on `GET /msp/sales-offers/sse` (artifacts/api-server/src/routes/msp-sales-offers.ts:139-142), same class already fixed in #2731/#3387. Swapping the hand-rolled ternary for `resolveMspIdStrict(req)`, already imported in this file.
- 2026-09-11T00:20:00Z ✅ DONE — Swapped the inline `user.role === "admin" || mspRole === platformAdmin ? req.query.mspId : user.mspId` ternary for `resolveMspIdStrict(req)`. Since this route verifies its own `?token=` JWT (no `requireAuth` middleware), `req.user` was never populated; added `req.user = user` right after `jwt.verify` so `resolveMspIdStrict` has session context, same as every other route in the file. Removed the now-unused `LEGACY_ROLE` import. Added a regression test in `msp-sales-offers.test.ts` asserting a PlatformAdmin's `?mspId=999` override is ignored and `registerMspOfferSSEClient` is called with the session's own resolved mspId instead. Verification: `tsc --noEmit` clean, `vitest run src/routes/msp-sales-offers.test.ts` — 7/7 passed (was 6/6 before this build). Branch verified merged into origin/main via `verify-branch-merged.mjs`. `git status --porcelain` clean.
