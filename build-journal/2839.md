# #2839 — 64 non-powershell-transport config_resources rows also cite Get-MSCloudLoginConnectionProfile

- **Status:** ✅ DONE 2026-09-04T12:00:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 4a8066fd2

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — starting. Same method as #1896: query the 64 affected
  `config_resources` rows (`graph` 49, `azure-rm` 7, `power-platform` 6, `sharepoint-admin` 2),
  read each resource's real Microsoft365DSC `Get-TargetResource` source at the catalog's cited
  `source_ref` commit, trace past `Get-MSCloudLoginConnectionProfile` to the real underlying
  call, correct `read_cmdlets` via a manual migration run against local `DATABASE_URL`.
- 2026-09-04T12:00:00Z ✅ DONE — Re-ran the issue's own SQL against current state first: live
  counts had drifted from the issue's original snapshot (`graph` 49 -> 16) because an intervening
  migration (#1960, `2026-09-04-config-resource-graph-transport-1960.sql`, commit `9c79b3174`,
  ran ~30 min after #2839 was filed) reclassified 193 rows from `read_transport='graph'` to
  `'powershell'`, carrying some already-polluted `read_cmdlets` values along. The live,
  authoritative set was 31 rows (`azure-rm` 7, `graph` 16, `power-platform` 6, `sharepoint-admin`
  2), not 64. Fetched each affected resource's real `Get-TargetResource` source from
  `raw.githubusercontent.com/Microsoft365DSC/Microsoft365DSC` at commit `f79f297` (same commit
  this catalog's `source_ref` cites — the repo has since moved from `microsoft/Microsoft365DSC`
  to the `Microsoft365DSC/Microsoft365DSC` org, confirmed via the GitHub API's 301 redirect,
  same commit hashes). For rows where a real cmdlet already appeared elsewhere in the array (most
  of them), just dropped the fake helper entry. For the 12 rows where the fake helper was the
  only entry (or the only entries left were also non-real), traced the real read call: 6 resolve
  to `Invoke-AzRestMethod` (direct Azure REST GET), 2 to `Invoke-MgGraphRequest` (direct Graph
  REST GET), 3 to `Invoke-WebRequest` (via M365DSC's own
  `Invoke-M365DSCPowerPlatformRESTWebRequest` helper in
  `Modules/WorkloadHelpers/M365DSCPowerPlatformRESTHelper.psm1`, which wraps `Invoke-WebRequest`
  directly — same shape as #1896's other WorkloadHelpers findings). Wrote and ran
  `lib/db/migrations/manual/2026-09-04-fix-connection-helper-read-cmdlets-nonpowershell-2839.sql`
  locally against `DATABASE_URL` — 31 row `UPDATE`s in one transaction, self-marking `INSERT` into
  `simulator_migration_runs`. Verified post-migration: 0 non-powershell-transport rows remain
  citing the helper.

  Filed **#2872** (parented under #1850, board status "AI Batter Up") for two genuine findings
  discovered mid-trace, out of #2839's own narrow scope: (1) 34 `powershell`-transport rows now
  also cite the helper — a byproduct of #1960's reclassification, confirmed distinct from the 12
  rows #1896 already fixed; (2) `Get-CompareParameters` (and likely `Get-M365TenantId`) is the
  same shape of fake-cmdlet pollution under a different name — confirmed by reading
  `MSFT_AzureBillingAccountPolicy.psm1`: it's a locally-defined, exported helper used only inside
  `Test-TargetResource` for comparison, not a real read call.

  Committed `73d415fe1` in the worktree, rebased onto `origin/main` (which had moved since
  session start — #2841's fix landed at `7b56ca652`), pushed directly to `origin/main` as
  `4a8066fd2`. `node scripts/dev-server/verify-branch-merged.mjs` confirms merged. `git status
  --porcelain` clean.
