# Git #2124 — Epic: #1787 Build Console — Git Board & Chats Panel: Settings UI toggle for PinnedQuestionDetectionEnabled

- **Status:** ✅ DONE
- **Scope:** buildconsole

## Log
- 2026-08-31 ⏳ IN FLIGHT — Starting. Real audit: `PinnedQuestionDetectionEnabled`
  (`desktop/BuildConsole/Services/BuildConsoleSettings.cs:416`, default `false`) is read fresh via
  `BuildConsoleSettings.Load()` on every probe attempt in
  `FloatingChatWindow.xaml.cs:648` (`MaybeProbeForPinnedQuestionsAsync`) — settings.json-only today,
  same precedent as `EnforceWorktreeIsolation`, per #2105's landed bookend. #2122 (max concurrent
  build slots) landed and established a "Border + heading + description TextBlock + control + Save"
  pattern in `Controls/SettingsTabView.xaml` General tab; the closer precedent for a plain on/off
  toggle is the existing Encouragement Critter `CheckBox` block (`EncouragementCrittersEnabledCheck`,
  `Controls/SettingsTabView.xaml.cs:105`) — save-on-Checked/Unchecked, no explicit Save button, no
  restart needed since the setting is loaded fresh at use-time. Plan: add a matching `CheckBox`
  block to `SettingsTabView.xaml`, wire load-in-constructor + Changed handler in
  `SettingsTabView.xaml.cs`, with inline copy stating plainly that this sends a real probe message
  into each open floaty chat (10-min cooldown per chat), consuming real usage against Shane's
  session limits and appearing in that chat's real transcript.

- 2026-08-31 ✅ DONE — Added a matching "Pinned Question Detection" `Border`/`CheckBox` block to
  `Controls/SettingsTabView.xaml` (General & Appearance tab, right before the #2001 title-bar
  readout block), following the Encouragement Critter checkbox convention exactly: save-on-
  `Checked`/`Unchecked`, no separate Save button. Inline copy states plainly, off by default, that
  enabling it "sends a REAL probe message into each open floaty chat (on a 10-minute cooldown per
  chat) asking whether there are outstanding questions for Shane — it consumes real usage against
  Shane's session limits and appears in that chat's real transcript, same as any other message he'd
  type himself." `SettingsTabView.xaml.cs`: constructor now seeds
  `PinnedQuestionDetectionEnabledCheck.IsChecked` from `BuildConsoleSettings.Load()`, and a new
  `PinnedQuestionDetectionEnabledCheck_Changed` handler persists via `settings.Save()` — guarded by
  the existing `_loadingSettings` flag so the constructor's seed doesn't fire a spurious save. No
  live-apply plumbing needed: `FloatingChatWindow.MaybeProbeForPinnedQuestionsAsync` already calls
  `BuildConsoleSettings.Load()` fresh on every probe attempt, so a change here takes effect
  immediately, no restart.

  **Verified:** `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Did not launch
  a live BuildConsole.exe to click the checkbox (would touch Shane's running instance), consistent
  with #2105/#2104/#2122's same caution — the toggle is a straight `BuildConsoleSettings.Load()`/
  `.Save()` round-trip through the identical mechanism #2122 already proved end-to-end this session
  (scratch console harness against the real class), so no new persistence risk here.

  No schema/migration/production-boundary work involved — local desktop-app settings only. No
  findings filed — #2105's setting and #2122's Settings-UI pattern both matched their bookends
  exactly.

  Commit `<pending>` — merged and pushed to `origin/main`, verified via
  `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean after push.
