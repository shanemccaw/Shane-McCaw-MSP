# #1924 — Remediation knowledge base: author the identity: domain, publish

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** `5d850839b` (identity seed migration, on `main`) · `bf9b521c5` (IN FLIGHT bookend)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Author every active `identity:` check (29) into `remediation_knowledge_base` as verified, published rows, against real Microsoft Learn docs fetched this session. Idempotent seed migration in `lib/db/migrations/manual/`, run against local DATABASE_URL. Fan-out issues per remaining domain under #1489. `Shane To-Do` spot-check issue. No schema/route/UI change (#1539 built the columns).
- 2026-08-31 ✅ DONE — Shipped `lib/db/migrations/manual/2026-08-31-remediation-kb-identity-domain-1924.sql` (commit `5d850839b`, verified ancestor of `origin/main`). `remediation_knowledge_base` went from 1 draft/0 published to **29 rows, all 29 identity: checks, all published**. Every row verified against real Microsoft Learn docs fetched this session (6 research passes + independent re-verification of a sample — allowInvitesFrom enum, "<5 GA / <10 privileged", 97%/99% legacy-auth stat all matched verbatim). `verifiedBy` is honest agent attribution on every row (no human name). fix_route_capability: 23 you_must_run / 6 admin_center_only / 0 we_can_run. Migration is idempotent (re-run proven: still 29/29). Fan-out issues filed per remaining customer-facing domain, parented to #1489, board "AI Batter Up": #2041 governance, #2042 security, #2043 devices, #2044 teams, #2045 exchange, #2046 adoption, #2047 appgov, #2048 copilot, #2049 compliance, #2050 cost, #2051 sharepoint, #2052 onedrive, #2053 license, #2054 platform, #2055 m365, #2056 licensing (diagnostics excluded — internal self-test). Human spot-check filed: #2057 (`Shane To-Do`). Migration logged on #1630. Completion comment + labels (in-flight→complete) synced on #1924. No schema/route/UI/config_pack change. Did not close the issue.
