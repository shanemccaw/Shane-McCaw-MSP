# #3360 — portal-team.ts denyIfCannotManageTeam fails OPEN for a caller with no recognised mspRole claim

- **Status:** ✅ DONE 2026-09-11T00:00:30Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 3cc44e3b8 (work), 076cd105d (IN FLIGHT bookend)
- **Findings filed:** #3571 (parented under #1696)
- **Migrations:** none

## Log
- 2026-09-10T23:46:04Z ⏳ IN FLIGHT — pulled #3360 body + both BUILD comments; investigating the live gate against #2458-#2460's capability model before deciding migrate-vs-patch.
- 2026-09-11T00:00:30Z ✅ DONE — The fail-open code the issue quotes (`isCustomerTier` → `return null`) was already removed from live code by #2460 (2868efa79). `denyIfCannotManageTeam` now reads `userHasCapability("customer","team.manage")`: a claim naming no rung gets no rung role, so the caller is decided on explicit `cap.*` grants alone and denied by default. Real local rows confirm allow = {cap.team.manage, MSPAdmin, MSPOperator, PlatformAdmin, ServiceAccount}, deny = []. Not migrated onto `requireCapability`: it decides ladder.* keys only (requireCapability-keys.test.ts:127-139; a customer: key would 503) and runs before the target tenant is known. Shipped in 3cc44e3b8: route-level regression tests with gate 1 forced open (10/10 mutating routes 403 and write nothing for absent/null/unknown/case-variant claims; the grant makes it 200; real gate 1 still 403s the granted caller), unit tests + a read-only live-db test of `customer:team.manage`, and stale comment/doc corrections (portal-team.ts header, parity-check.ts, legacy-ladder.ts + test, team contract pack §0). Verified: api-server vitest 36/36 on touched suites (live-db ran, not skipped); lib/db legacy-ladder 26/26; api-server + lib/db tsc clean; check-rbac-parity 888 ALL AGREED (2 registered fail-closed divergences); mutation check — restoring the pre-#2460 branch turns all 4 route-level #3360 tests red. verify-branch-merged OK; working tree clean. No pre-existing dirty files. Finding: #3571 (stale LEGACY_CAPABILITY_RULES citations for ladder.* and msp:purchases.approve).
