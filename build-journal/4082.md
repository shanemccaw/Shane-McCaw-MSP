# #4082 — Security Plan: real MSP-console contract pack into docs/msp-console/

- **Status:** ✅ DONE 2026-09-14T16:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 1926fd602 (IN FLIGHT bookend), 7563940cd (contract pack)

## Log
- 2026-09-14T15:52:30Z ⏳ IN FLIGHT — reading Portal pack (docs/portal/security-plan-contract-pack.md) and msp-security-plan.ts to produce docs/msp-console/security-plan-msp-console-contract-pack.md
- 2026-09-14T16:10:00Z ✅ DONE — produced `docs/msp-console/security-plan-msp-console-contract-pack.md`. Read-only extraction pass, no product code/schema/UI changed. Cited the Portal pack for the shared assembly/scope/footprint/prose/drift schema rather than re-deriving it, per the issue. Real content: documented the #1689/#3793 dual-signature model (independent `customerSigned*`/`mspSigned*` slots, replacing the single-slot shape the Portal pack still describes in its own now-stale §2.6/§3.6/§5.4) — confirmed live via `psql \d msp_security_plan_versions` (legacy `signed`/`signed_by`/`signed_at` columns confirmed dropped, destructive migration confirmed applied via `simulator_migration_runs`), confirmed `getLastSignedSecurityPlanVersion` no longer exists anywhere in the codebase (grep, zero hits) and that `portal-security-plan.ts` now calls `getLastFullyExecutedSecurityPlanVersion` instead. Confirmed live RBAC floors: `ladder.msp-operator` = {MSPOperator, MSPAdmin, PlatformAdmin} on 8 of 9 routes, `ladder.msp-admin` = {MSPAdmin, PlatformAdmin} (no MSPOperator) on the MSP's own sign route. Confirmed no MSP-console UI page exists for this Feature (Design/MSP_Console/design_handoff_msp_console/github.md:324 lists it on the no-screen-yet line) — expected pre-Design state, not a gap. No genuine bug/finding surfaced — the one divergence found (Portal pack documenting the pre-#3793 signature shape) is documentation drift in a sibling pack, not a product defect, and is addressed by this pack's own existence rather than filed as an issue. `verify-branch-merged.mjs` confirmed ancestor of origin/main; `git status --porcelain` clean.
