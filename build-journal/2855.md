# #2855 — Fix broken stored write-pack templates (delete-ca-policy PUT, assign-app-protection-policy abstract type)

- **Status:** ✅ DONE 2026-09-04T16:07:53Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 89a126ae3 (main)

## Log
- 2026-09-04T15:58:41Z ⏳ IN FLIGHT — investigating graph-write-permissions.ts and baseline_action_templates rows for action.delete-ca-policy and action.assign-app-protection-policy
- 2026-09-04T16:07:53Z ✅ DONE — Confirmed live rows via local Postgres, wrote and ran
  lib/db/migrations/manual/2026-09-04-ca-policy-delete-and-mam-assign-fix-2855.sql:
  action.delete-ca-policy method PUT -> DELETE; action.assign-app-protection-policy
  endpoint managedAppPolicies/{id}/assign -> targetedManagedAppConfigurations/{id}/assign.
  Updated the two matching rules in artifacts/api-server/src/lib/graph-write-permissions.ts
  (pattern/method changed to match, THE STORED METHOD/ENDPOINT IS WRONG warning notes
  dropped now the underlying data is fixed). Updated graph-write-permissions.test.ts:
  replaced the two "stored template is broken" assertions with correctness assertions
  (fixed method/endpoint resolves to a rule with the unchanged permission set; the old
  broken method/endpoint now returns rule: null). 58/58 tests pass. `tsc --noEmit`
  clean except one pre-existing unrelated error in portal-security-plan.ts
  (missing `requireRole` import) that predates this session and was not touched.
  Filed #2875 (parented to #2489, board status AI Batter Up) for the 4 remaining
  PUT-where-DELETE-was-meant rows the issue body flagged as lower priority
  (action.delete-group, action.delete-user, action.unenroll-device,
  action.remove-auth-method) — none currently wired into a Config Pack.
  Rebased agent/2855-q1619 onto origin/main and pushed to main as 89a126ae3;
  verify-branch-merged.mjs confirms merged. Working tree clean.
