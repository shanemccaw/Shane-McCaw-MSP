# #3675 — Chats panel: visual restyle to match the real design handoff (epic cards, chat rows, context-usage ghosting, sub-epic dividers)

- **Status:** ✅ DONE 2026-09-11T17:34:12Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** d96dc77d8

## Log
- 2026-09-11T17:24:26Z ⏳ IN FLIGHT — real-blocked-on #3663 pre-check: confirmed #3663 is genuinely
  DONE (build-journal/3663.md `✅ DONE`, `complete` label, its commit a3025f4c0 is on `main`),
  removed the now-stale `blocked_by` edge on #3675 → #3663. Read `design_handoff_chats_panel/README.md`
  and `Chats Panel.dc.html` directly. Mapped the real rendering path: `Controls/LeftSidebar.xaml.cs`
  `RenderChatsTree` → `BuildEpicSection` (epic card header + progress bar) → `BuildChatCard` (chat row).
  Confirmed real data sources for the design's context-usage readout (`Services/ChatContextMeterStore`,
  `Entry.EstTokens` keyed by `chat.ConversationId`, budget `900_000` — the same real ceiling
  `ChatDocumentContainer.xaml.cs`'s `ContextBudget` already uses) and the sub-epic divider
  (`GitBoardIssue.ParentNumber` — a real epic-to-epic parent link already fetched into
  `_lastBoardIssues`, e.g. #1494 Microsoft Changes → parent #1485 Portal). Added
  `Resources/ChatsPanelBrushes.xaml` (literal design-token brushes from the README, merged into
  `App.xaml`, same established pattern as `Resources/ChatPaneBrushes.xaml`) and an `Icon.MessageSquare`
  Lucide geometry in `Resources/Icons.xaml` (reusing the existing `Icon.ChevronDown` rotated for the
  card's expand/collapse chevron).
- 2026-09-11T17:34:12Z ✅ DONE — rewrote `BuildEpicSection` (bordered card: message-square icon tile,
  `#num` pill, uppercase name, "N chats · last X" subtitle, real transitive-leaf progress bar
  green/amber/accent, rail-lined expanded body, dashed "+ Continue in a new chat" button) and
  `BuildChatCard` (row background/border/dot per real `ChatContextMeterStore` reading, real
  "Nk / 900k ctx" readout + mini bar green/amber/red by the real 70%/100% thresholds, reduced-opacity
  ghosting at the real ceiling — falls back to the prior relative-time readout, never a fabricated
  value, when no context reading is recorded yet). Added a real per-epic `EpicAccentColor` map (the
  six named epics from the README's own sample data, which are this repo's own real standing epics,
  plus a stable-hash fallback for every other real epic) and a `Tint()` alpha helper mirroring the
  mockup's hex+alpha-suffix tinting. Added real sub-epic nesting: `RenderChatsTree` now folds an epic
  group whose `GitBoardIssue.ParentNumber` matches another visible group's `GithubNumber` under that
  parent (e.g. #1494 Microsoft Changes under #1485 Portal), rendered with the design's real
  "SUB-EPIC #N · NAME" divider — replaced the old `(title, GithubNumber, Chats)` tuple list with a new
  `ChatEpicGroup` type carrying real `SubEpics`. Every existing real behavior is unchanged: chat sort
  order, expand/collapse persistence, closed-epic/closed-chat filtering, the in-progress/running/waiting
  stat chips, and every context-menu action (link/unlink, rename, archive, pin, float, chat mapping).
  Left untouched, per the issue's own explicit scope: the New Chat button/dropdown (#3676, already
  landed) and the progress bar's data source (#3663, already landed and confirmed merged before this
  build started).
  **Real, honest verification result:** `dotnet build BuildConsole.csproj -c Debug` succeeds against
  the final pushed HEAD (0 errors, only 3 pre-existing unrelated `FocusModeBar` warnings). Per
  CLAUDE.md's "never launch BuildConsole.exe to verify" rule and this WPF app being out of
  test-manifest scope, correctness beyond the clean build was verified by a full manual re-read of
  both rewritten methods end to end against the design handoff's own README values and `.dc.html`
  structure (colors, radii, padding, the real 900k ceiling, the real green/amber/red thresholds), and
  by tracing every real call site (`BuildEpicSection`/`BuildChatCard`) to confirm no signature/logic
  drift. No live/simulated UI run was performed (no BuildConsole runner reachable to courier a
  `shaneapp://runTest`-style call for this WPF surface, and none exists for WPF per its own
  test-manifest exemption) — this is a code-review-level verification, not a rendered-pixel
  comparison against the reference screenshots.
  No findings filed — no real gap or bug was discovered outside this issue's own scope during the
  build.
  Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs`
  (OK, d96dc77d8). `git status --porcelain` clean.
