# #3396 — MCP tool: get_recent_activity audit log

- **Status:** ⏳ IN FLIGHT 2026-09-10T02:05:00Z
- **Scope:** platform (artifacts/github-mcp-server)
- **Started:** 2026-09-10
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-10T02:05:00Z ⏳ IN FLIGHT — investigated #3396 (blocked-by #3390). #3390 landed on `main`
  (commit `168ba9a29a6fc2e4cab6bc6956cdb4b586686b8a`, labeled `complete`) and rebased this worktree
  onto latest `origin/main` to pick it up.
- 2026-09-10T02:05:00Z — found `get_recent_activity` already fully implemented as part of the
  #3390 scaffold: `src/tools/get-recent-activity.ts` (ToolDef, `limit` 1-200 default 20), backed
  by `src/activity.ts` (`recordActivity`/`listActivity` against the `github_mcp_activity` table),
  wired into `ALL_TOOLS` in `src/tools/index.ts`. Matches this issue's spec verbatim — timestamped,
  newest-first, token label, tool, redacted params, outcome; PAT never appears in rows.
- 2026-09-10T02:05:00Z — real verification already covered by #3390's own `scripts/smoke-check.mjs`
  run in that session: confirms `tools/list` includes `get_recent_activity`, makes several real
  tool calls, then calls `get_recent_activity` and asserts entries are returned (`entries.length > 0`)
  reflecting those calls. Did not re-run pnpm install / the smoke harness myself in this worktree
  (package's `node_modules` isn't linked into either this worktree or the main checkout yet — a
  genuinely new workspace package with nothing installed anywhere, not a dangling-symlink case;
  re-running the full smoke suite would need that install first, which is out of scope for this
  low-effort verification-only build per the pnpm-install-is-not-a-remedy discipline).
