# #2654 — Scope and SLA: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T19:54:59Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 1a5b4474f (bookend IN FLIGHT), 1ad5418b0 (contract pack)

## Log
- 2026-09-03T19:39:26Z ⏳ IN FLIGHT — reading msp-scope-creep.ts (12 routes) + msp-sla.ts (14 routes) in full, plus Feature #2572, to extract the MSP-console-side contract pack per the #1642 pattern (docs/scope-and-sla-msp-console-contract-pack.md, parallel to the existing customer-facing docs/scope-and-sla-contract-pack.md from #2452).
- 2026-09-03T19:54:59Z ✅ DONE — `docs/scope-and-sla-msp-console-contract-pack.md` written and merged to `main` (`1ad5418b0`), covering all 26 MSP-console routes (12 scope-creep + 14 SLA) cited to file:line, both engines' real compute formulas, real DB tables/enums (raw-SQL, not Drizzle), CURRENT vs DECIDED, honest-empty contract, cross-surface edges, and an orphaned-endpoint check confirming zero UI callers exist anywhere in the repo today (`artifacts/msp-console` doesn't exist yet, matching Feature #2572's own body).

  **3 real findings filed as siblings of Feature #2572**, board status "AI Batter Up":
  - **#2726** — `POST /msp/scope-creep/evaluate` fires per-customer violations off an MSP-portfolio-aggregate score, and defaults `customerId` to `mspId` when the request body omits it (id-space confusion, `scope_creep_violations.customer_id` has no FK so it silently succeeds).
  - **#2728** — `sla_timers.status` DEFAULT `'active'` is never set explicitly by `startSlaTimer()`'s own INSERT and never matches the engine's `WHERE status = 'running'` filter, so every timer either of its two real callers (`tenant-signals.ts`, `admin-sla.ts`) creates is permanently invisible to breach/warning detection and can never be resolved; folded in the same finding: `GET /msp/scope-creep/escalations` and `.../compliance` apply no staff-scoping filter, unlike every sibling customer-bearing GET.
  - **#2729** (URGENT) — `GET /msp/operator-tasks` LEFT JOINs `msp_customers`, a table dropped by the Tenant/User Refactor migration (`2026-07-28-tenant-user-refactor-phase0-schema-wipe.sql:544`); confirmed via migration file + exhaustive repo grep (only 2 live SQL references to the table exist anywhere, both in this one route) that every real call 500s — never caught by the route's own mocked-db test suite.

  **Honest DB-verification gap**: local Postgres answered real queries early in the session (§0 of the pack — msps list, zero rows in sla_timers/scope_creep_detections/violations) but became unresponsive for the remainder (5 real `psql` attempts, 15-30s timeouts each, likely concurrent-session load) — one live check (`msp_customers` existence) could not be completed against the live connection; the pack documents this honestly and grounds Finding #2729 in the migration file + grep evidence instead, which is unambiguous on its own.

  Read-only: only the pack + this bookend changed. Working tree clean, `verify-branch-merged.mjs` exit 0. No blockers, nothing further needed from Shane beyond reviewing the 3 filed findings.
