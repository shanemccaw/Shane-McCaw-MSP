# #4531 — CA policy create templates duplicate the policy on every re-run (no existence check)

- **Status:** ✅ DONE 2026-09-17T15:18:30Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** b858e4b1e (work), 733269cd0 (live-verify harness), 83e662e5d (IN FLIGHT bookend)
- **Migration:** lib/db/migrations/manual/2026-09-17-ca-policy-existence-lookup-4531.sql (run locally twice, UPDATE 6 both times; line added to #1630)
- **Findings filed:** #4540 (bug + Shane To-Do, Ask Shane lane: testbed has no Entra ID P1, so no CA write can be live-verified), #4541 (bug, Ask Shane lane: re-run fails closed once an admin enforces the report-only policy). Both parented to #1489.

## Log
- 2026-09-17T14:57:44Z ⏳ IN FLIGHT — add a displayName existence lookup to the 6 CA create templates (identity-ca-hardening-v1 x5, quickstart-v1 CA baseline); skip the POST only when the single existing policy's conditions.users.excludeGroups contains the resolved breakGlassGroupId, fail closed otherwise (multiple matches, missing exclusion). BuildConsole buildId 3158.
- 2026-09-17T15:18:30Z ✅ DONE — b858e4b1e: resolve-then-write adds `skipRequires` on an onMatch "skip-write" step. These are conditions the ONE existing match must also meet; any unmet one fails closed (424, no write), and the reason lists each field's expected vs found value. It also adds the selectMatch conventions "has:" (array holds every listed value, case-insensitive; an empty or unresolved list never matches) and "ieq:" (case-insensitive equality; empty never matches). The migration (data only, idempotent) appends a GET /identity/conditionalAccess/policies lookup, matched case-insensitively on the template's own displayName, to all 6 templates. After #4514's 3 break-glass steps on the 4 that have them: legacy/mfa/signin/user-risk now have 4 steps, guest-mfa and quickstart 1. Skip requires: state enabledForReportingButNotEnforced, same includeUsers, same grant controls, the distinguishing condition (clientAppTypes / risk levels), and, for the 5 that exclude break-glass, excludeGroups containing the resolved {{breakGlassGroupId}}. guest-mfa has no exclusion requirement because its body excludes no group. No match → POST as before; two or more → fail closed with ids. Verification: unit tests in ca-policy-existence-lookup-4531.test.ts pass 30/30 (step JSON parsed from the migration file itself); together with the #4514 and resolve-then-write suites, 96/96; api-server tsc clean. Live on testbed 2080, 2 passed / 3 skipped: all 6 lookups read the real endpoint (200, 1 page, 0 matches), so the POST would proceed. The 4 break-glass templates still stop at #4514's ambiguous-group check first. PARTIAL: the synthetic disabled policy POST (DEV app; includeUsers zz-test-graphwrite-01 only) got 403 "Your tenant is not licensed for this feature" (no Entra ID P1). That leaves the skip, not-the-template's-policy, and two-same-named branches unit-tested only; the 3 tests skip with that stated reason → #4540. Nothing was created; the CA policy list is still []. No test manifest: api-server lib only. Working tree clean.
