# #2594 — Ownership / RACI: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T06:30:45Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 925bdead2 (rebased/pushed as 6e5bb1150 on origin/main)

## Log
- 2026-09-12T05:30:08Z ⏳ IN FLIGHT — Read #2594 body/comments + #1518/#1523/#1524/#1491/#1686/#3677 decisions.
  Design confirmed at `Design/MSP_Console/design_handoff_msp_console/Ownership.dc.html` (screen 37, nav id
  `raci` under governance group, per-tenant route `/tenants/:id/raci`). Real backend for the customer side is
  `routes/portal-ownership.ts` (GET matrix + events) and `routes/msp-ownership.ts` (mine/assign/accept/decline
  already live from #1521/#2162). Missing on the MSP side: a per-customer matrix READ (people/objects/overlay/
  gateMode) and a per-cell event-history READ — portal's GET routes are JWT-scoped to the customer and have no
  MSP/:customerId equivalent. Plan: factor portal-ownership.ts's GET handlers into shared assembly functions,
  add `GET /msp/ownership/:customerId` + `GET /msp/ownership/:customerId/events` + `POST
  /msp/ownership/:customerId/chase` reusing that same assembly (no forked read logic), then build the console
  module (`console/modules/Ownership.tsx`) with mine/coverage/matrix tabs + cell drawer, wired at nav id `raci`.
- 2026-09-12T06:30:45Z ✅ DONE — Shipped. Backend: factored `portal-ownership.ts`'s GET handlers into
  `assembleOwnershipPayload()` / `fetchOwnershipEvents()`; added `GET /msp/ownership/:customerId`,
  `GET /msp/ownership/:customerId/events`, `POST /msp/ownership/:customerId/chase` (new write affordance —
  the design's own honestNote says nothing chases these today); fixed a genuine gap in the existing #1521
  `GET /msp/ownership/mine` — it returned only a raw `holderPersonId` ("u42"), never a display name — by adding
  `holderPersonName`, resolved from the caller's own MSP staff roster. No schema change; all additive routes
  against the existing #1491 tables. Frontend: `api/ownership-api.ts` (react-query hooks), `lib/ownership-matrix.ts`
  (pure cell-merge logic mirroring `artifacts/portal`'s own semantics — no cross-app import mechanism exists
  between `artifacts/*` apps other than a `lib/*` package), `console/modules/Ownership.tsx` (three tabs, cell
  drawer: assign/accept/decline/chase, append-only event log, client-derived missing-owner filter), wired into
  `ConsoleShell.tsx` at nav id `raci` (`/tenants/:id/raci`). Per #1524, no delegation affordance anywhere. Real
  architecture from #1518/#1523/#1524 read and not re-derived.
  Verification: `pnpm run typecheck` clean on both `artifacts/api-server` and `artifacts/msp-console`;
  `vitest run` on `msp-ownership-book.test.ts` + `portal-ownership.test.ts` — 50/50 passed (added a
  `holderPersonName` assertion); `pnpm run build` on `msp-console` succeeds; queried the local DB directly
  (`portal_ownership_assignments` genuinely has 0 rows yet — honest empty state, not a bug; confirmed one real
  MSPAdmin/PlatformAdmin user and two real tenants exist to exercise the read paths against). No test manifest —
  `artifacts/msp-console` is outside CLAUDE.md's Test Coverage scope (msp-portal / shane-mccaw-consulting only).
  No migration, no `Shane To-Do` action, no findings filed — this build didn't hit a genuine gap outside its own
  scope. Commit `925bdead2`, rebased onto `origin/main` and pushed as `6e5bb1150`; `verify-branch-merged.mjs`
  confirms ancestry. Working tree clean.
