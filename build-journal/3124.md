# #3124 — Feature: Recipes — Core list + ingredient matching (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:10:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 27c0d7a09 (feature), 8338d5c99 (merge into main, on main as fast-forward)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — blocker #3088 confirmed closed; building the Recipes room
  (`recipes` table, `GET /api/recipes` with have/missing match against the real Shopping list,
  "Add missing" -> lists.addListItems, real heart-healthy context stated once via
  `users.health_context`, MCP `push_recipes`/`get_recipes`/`get_health_context`/
  `set_health_context`, and the Recipes screen in `public/app.js`). Cook mode/Tonight/Sunday
  ritual explicitly out of scope per the issue.
- 2026-09-08T00:10:00Z ✅ DONE — shipped: `web/shanes-life/migrations/025_recipes.sql` (renumbered
  from 024 after a concurrent #3132 build claimed 024 first; both applied cleanly against the
  real local `finances` database, ledger confirms `025_recipes.sql`), `src/core/recipes.mjs`,
  `GET/POST /api/recipes`, `DELETE /api/recipes/:id`, `POST /api/recipes/:id/add-missing`,
  `GET/PATCH /api/health-context`, MCP `push_recipes`/`get_recipes`/`add_missing_ingredients`/
  `get_health_context`/`set_health_context`, the `#/recipes` screen + nav link, a health-context
  editor in Settings, and a README section. Verified against the real local database (not
  mocked): pushed two real recipes, confirmed real can-make/missing matching against a live
  Shopping run, confirmed "Add missing" genuinely appended the gap onto that run and flipped
  can-make to true, confirmed archive removes a recipe from the list — disposable test user
  created and deleted, no residue. Server boots clean both before and after merging main's
  concurrent #3132 (food preferences) work. `git merge-base --is-ancestor` confirms
  `agent/3124-q1887` is merged into `origin/main`; working tree clean.
