# #3673 — Git panel: rejected Commit & Push fails silently; needs clear error + Pull-then-Push retry

- **Status:** ✅ DONE 2026-09-11T17:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 68582da2f — Git panel: wrap/grow status container, classify push rejection, Pull-then-Push retry (Git #3673)

## Log
- 2026-09-11T17:05:00Z ⏳ IN FLIGHT — investigating GitStatusSummaryText container + BtnGitCommitAndPush_Click / RunGitCommand in desktop/BuildConsole/Controls/LeftSidebar.xaml(.cs)
- 2026-09-11T17:35:00Z ✅ DONE — `GitStatusSummaryText`'s Border now wraps text and grows in
  height (TextWrapping="Wrap", HorizontalAlignment="Stretch") instead of clipping
  horizontally. `RunGitCommand` refactored into `RunGitCommandCore` (returns exit
  code/stdout/stderr, not just bool) so a new `RunGitPushAsync` can classify a real
  push rejection (`! [rejected]`, `failed to push some refs`, `(fetch first)`,
  `(non-fast-forward)`) vs any other push failure. A classified rejection shows a
  clear plain-language message and reveals a new "Pull then Push" retry button
  (`BtnGitPullThenPush_Click`) that runs the existing real `git pull` then retries
  the push, honestly surfacing a genuine pull conflict instead of swallowing it.
  Any other push failure still shows the real, full, non-truncated git error via
  the same (now non-clipping) container. `DoGitCommitAsync`/commit flow untouched
  per the issue's scope. Verified: `dotnet build BuildConsole.csproj -c Debug`
  succeeds, 0 errors (6 pre-existing unrelated warnings in FocusModeBar.xaml.cs).
  Real-world verification bonus: the very push of this fix's own commit hit a
  genuine non-fast-forward rejection against origin/main (rebased + retried
  manually) — confirms the classification condition is a real, live scenario on
  this repo, not a hypothetical. `verify-branch-merged.mjs` confirms
  `agent/3673-q2336` (68582da2) is an ancestor of `origin/main`; `git status
  --porcelain` clean.
