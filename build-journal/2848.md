# #2848 — check-migration-drift.ts permanently red on main (stale schema-hash.txt + 2 orphaned SQL files)

- **Status:** ✅ DONE 2026-09-04T23:45:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** dca17a108 (also rebased onto and pushed via 919b42d28)

## Log
- 2026-09-04T23:20:00Z ⏳ IN FLIGHT — pulled issue body, reading check-migration-drift.ts, investigating schema-hash.txt drift and the two orphaned SQL files (0192_rename_sales_offers_tenant_id.sql, 0201_drop_service_page_trigger_keys.sql).
- 2026-09-04T23:45:00Z ✅ DONE — check-migration-drift.ts is down from 3 real failures to 1:
  - **schema-hash.txt re-baselined** against current origin/main (schema had moved again since #2848 was filed, via #1947's retention foundation work) — check 1 now green.
  - **0192_rename_sales_offers_tenant_id.sql repaired**: confirmed already applied to the local dev DB (`\d sales_offers` shows `customer_id`), registered in `_journal.json`, and marked applied directly in `__drizzle_migrations` — no SQL executed, so nothing fires on merge. Fully resolved.
  - **0201_drop_service_page_trigger_keys.sql deliberately left unregistered** — genuine conflict, reported rather than picked unilaterally: `scripts/post-merge.sh` runs `migrate-dev`/`migrate-prod` automatically on every merge with no human gate, so registering this still-unapplied `DROP TABLE` would let the next merge silently execute a destructive change against dev (and prod, if configured) — directly conflicting with CLAUDE.md's "destructive changes go to Shane" rule. Filed **#2930** (sibling sub-issue of #1944, milestone v1.1, label `bug`, board status "AI Batter Up") documenting the three real options (run it by hand, add a destructive-migration gate to post-merge.sh, or delete the file) — all of which need Shane's call.
  - Verified: `npx tsx scripts/src/check-migration-drift.ts` now reports 0 schema-hash errors, 0 journal-vs-file errors, and exactly 1 orphan (0201) — down from the original 2 checks failing.
  - Branch verified merged (`verify-branch-merged.mjs` → OK), working tree clean (`git status --porcelain` empty).
