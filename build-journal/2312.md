# Git #2312 — Every peek offers "Expand full page here" and, where a document exists, "Send to tab"

- **Status:** ✅ DONE 2026-09-02T18:35:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Git Panel peek, Feature #2289 item 23 of 23)
- **Commit:** `457d6c2cf` (rebased/pushed to `main` as `2b13e4435`)

## Log

- 2026-09-02T18:20:00Z — ⏳ IN FLIGHT — Session start. Dependency #2290 confirmed genuinely DONE
  on origin/main. Scope: add the two standing peek affordances to every Git Panel peek kind
  (Milestone/Gate/Epic/Feature/Issue) — "Expand full page here" (widens the rail in place, no
  tab) and "Send to tab" (opt-in takeover into a real document tab), same convention already
  used by LogPeek's own maximize-to-full-viewer icon and the Git Map mini-panel/doc-tab pair.
- 2026-09-02T18:35:00Z — ✅ DONE — Shipped `457d6c2cf` (pushed to `main` as `2b13e4435`, verified
  ancestor via `node scripts/dev-server/verify-branch-merged.mjs`). Implementation: extracted the
  peek's breadcrumb/identity rendering out of `RenderGitPeek` into a shared
  `RenderGitIdentityBlock(Panel, List<GitCrumb>, ...)` so the rail peek and the new "Send to tab"
  document tab (`GitItemDock` in `MainWindow.xaml`, `RenderGitItemDoc`/`OpenGitPeekInTab` in
  `MainWindow.GitPanel.cs`) render off the same real GitHub-backed data
  (`_gitPeekCache`/`GitPanelService.GetAncestryAsync`) — one data path, two hosts, same precedent
  `GitMapDock` already set for the Git Map panel/full-doc pair. "Expand full page here" toggles
  `LeftPanel.Width` between the normal 280px rail and a 640px expanded width in place (only while
  the Git source is the visible rail panel); collapses automatically when the peek closes back to
  the tree. `TabDef` gained a `GitItemTrail` field (a frozen snapshot of the crumb trail at send
  time, refreshed if the same item is sent again while its tab already exists).
  **Real conflict handled honestly:** two concurrent sessions landed #2301 (Milestone panel) and
  #2302 (Gate detail panel) on `main` mid-build, touching the exact same `RenderGitPeek` function.
  First rebase attempt produced a bad merge (a stale local copy of `origin/main` briefly reverted
  #2301's Milestone panel work in the working tree — caught before commit via `git diff --stat`
  showing an implausibly large removal count, never pushed). Re-did the integration cleanly
  against the true current `origin/main`: the rail peek keeps its full Milestone/Gate
  ring/verdict/checklist bodies (`RenderGitMilestonePanel`/`RenderGitGatePeekBody`, unchanged,
  still writing to `GitPeekHost`); the new "Send to tab" doc surface uses the shared identity
  block plus a generic open/closed+labels fallback for all kinds (not full Milestone/Gate parity
  — those read live-panel-only caches this build doesn't extend to the doc tab). `dotnet build`
  clean (0 warnings / 0 errors) on top of the final `origin/main` state before push.
  **Verification (honest level):** clean build; no live UI click-through (no manifest —
  BuildConsole/ShaneBuilder is out of test-manifest scope per CLAUDE.md; visual QA is Shane's).
  **Finding filed:** #2414 (Send-to-tab shows generic state, not the rail peek's full
  Milestone/Gate panel — a real, deliberate simplification made in this build to stay in scope),
  parented under Feature #2289, board status AI Batter Up.
  Working tree clean at session end; no pre-existing dirty files encountered.
