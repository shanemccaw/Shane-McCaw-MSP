# #2320 — New Chat button at the top of the Chats panel

- **Status:** ✅ DONE 2026-09-02T19:45:00Z
- **Scope:** buildconsole (ShaneBuilder — desktop/ShaneBuilder, per Feature #2318)
- **Started:** 2026-09-02
- **Commit(s):** 5ac14603e (bookend), c849dbdb6 → rebased/pushed as b0fb76b28

## Log
- 2026-09-02T19:30:00Z ⏳ IN FLIGHT — adding a "New Chat" button to the header of the
  Chats left panel in desktop/ShaneBuilder/MainWindow.xaml, wired to a stub handler
  that opens a fresh claude.ai tab (same navigation BtnStartNewChat_Click already
  uses). #2321 builds the real anchor-disclosure flow; this issue is pure UI.
- 2026-09-02T19:45:00Z ✅ DONE — added `BtnNewChat` to the Chats panel header
  (MainWindow.xaml), shown/hidden alongside `ChatsPanelBody` in `OpenLeftPanel`
  (source == "Chat"). Click handler `BtnNewChat_Click` calls a new
  `OpenNewChatFlow()` seam (currently navigates `ClaudeWebView` to
  `https://claude.ai/new`, same as the existing context-full nudge button) —
  #2321 replaces that body with the real anchor-disclosure. `dotnet build`
  (desktop/ShaneBuilder) succeeded, 0 warnings/0 errors. `git status
  --porcelain` clean. Branch verified merged: HEAD (b0fb76b28) is on `main`
  (pushed directly, no separate branch per repo convention). No fixture/hardcoded
  data — no backend/data involved, pure UI wiring.
