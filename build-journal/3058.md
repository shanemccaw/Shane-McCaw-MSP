# #3058 — Risk Register: whole-document RBD viewer + drawn-signature capture

- **Status:** ✅ DONE 2026-09-07T16:16:35Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 45ac8f450 (viewer/sign wiring), 2d617d9ed (test-manifest setup tags), bbc472521 (rbd-versioning UUID guard fix), be4d02f74 (manifest verified-commit record)

## Log
- 2026-09-07T15:37:27Z ⏳ IN FLIGHT — wiring `GET .../document` + `POST .../sign` (portal-rbd-document.ts) into the portal: document viewer for `WireRbdDocument`, a drawn-signature capture pad, and the honest 404 state, per the issue body.
- 2026-09-07T16:16:35Z ✅ DONE — Wired the whole-document RBD signature surface into the portal:
  - Added `WireRbdDocument`/`SignRbdDocumentRequest`/`SignRbdDocumentResponse` wire types (`risk-register-types.ts`).
  - Added `useRbdDocument` (404 → `null`, honest empty state, not a thrown error) and `useSignRbdDocument` React Query hooks (`risk-register-api.ts`).
  - Built `SignaturePad.tsx` — a plain canvas/pointer-event drawn-signature pad (no new dependency; emits a PNG data URL).
  - Rewrote `RiskDocumentPanel.tsx`: the document is rendered in a sandboxed `<iframe sandbox="">` (`srcDoc`, no script execution, no new HTML-sanitizer dependency needed), followed by signer name/title inputs, the signature pad, and a same-dialog confirm step before `POST .../sign`. The pre-existing "not wired yet" informational dialog is gone.
  - **Real bug found and fixed in the same session**: `rbd-versioning.ts`'s `getRbdVersionByUid`/`signRbdVersion`/`generateRbdShareLink` ran `eq(versionUid, <caller string>)` against the `uuid`-typed `version_uid` column with no format guard — a non-UUID `versionUid` (typo, stale link, probe) threw a raw Postgres `invalid input syntax for type uuid` error, surfacing as an uncaught 500 from every caller (`portal-rbd-document.ts`, `public-rbd-document.ts`, `msp-rbd-versions.ts`), contradicting all three routes' own documented 404 contract. Fixed with a `UUID_RE` guard returning `null` (→ the existing 404 path) before the query runs. Not filed separately — fixed in-session, not worked around.
  - Extended `test-manifests/portal/risk-register.json`: DOC1/DOC2 (scoped-404 on both new routes for a nonexistent `versionUid`) and UI8 (honest "No document prepared yet" empty state, since zero `msp_rbd_versions` rows exist anywhere in local/testbed DB — confirmed via direct `psql` SELECT). Tagged `rbd-document`; tagged the pre-existing login/navigation steps `setup` so the `onlyTag` run carries its own auth. **Ran live** via `shaneapp://runTest?onlyTag=rbd-document` against the real dev api-server (refreshed to `bbc47252e` via `refresh-main-server.mjs`) and the real portal frontend: **11/11 steps passed**. `lastVerifiedAgainstCommit` set to `bbc47252e`.
  - `pnpm run typecheck` clean for both `artifacts/portal` and `artifacts/api-server`. `pnpm run build` (portal) succeeds.
  - The document-render/sign *happy path* (a real rendered `WireRbdDocument`, a real `201` sign) is **not** driven end-to-end — no `msp_rbd_versions` row exists anywhere in the local/testbed DB to render one against (rendering needs a real MSP staff `createdByUserId` via the separate `msp-rbd-versions.ts` route, out of this issue's scope). Documented honestly in the manifest's own notes for a future session to extend once a version is seeded.
  - No findings filed — the one real bug discovered (the UUID-guard gap) was fixed in-session rather than worked around.
  - `git status --porcelain` clean; branch verified merged into `origin/main` (`verify-branch-merged.mjs`).
