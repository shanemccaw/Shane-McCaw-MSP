# #3146 — Feature: Dev floaty panel — override date, weather, sky, Next for testing (Shane's Life)

- **Status:** 🛑 BLOCKED 2026-09-08T01:15:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 9cc76f2e1 (bookend only — no feature code)

## Log
- 2026-09-08T01:09:01Z ⏳ IN FLIGHT — investigating real scope: reading #3146's spec, the
  prototype `door()` logic in `Design/design_handoff_shanes_life/Shanes Life - First Slice
  Prototype.dc.html`, and the real state of `web/shanes-life/public/app.js` (the real,
  no-build-step vanilla-JS front end this app actually ships).
- 2026-09-08T01:15:00Z 🛑 BLOCKED — the panel's own spec (item 3) requires wiring live into
  the real `themeKey(date)` logic and real weather sky/particle rendering. Confirmed by grep
  against `origin/main` @ `9cc76f2e1` that neither exists anywhere in `web/shanes-life/`
  yet — that logic is the sibling Features #3144 (Today tray Round 2 / real weather) and
  #3145 (Seasons & holidays theming), both currently `open` and both `in-flight` (other
  sessions actively building them right now). Wired real `blocked_by` edges on #3146 to
  both. Labeled #3146 `blocked`, removed `in-flight`. No feature code written — building the
  panel now would mean faking the exact state shape CLAUDE.md forbids, or shipping controls
  with no real effect (fails the issue's own Verification section). Left this bookend at
  BLOCKED, not DONE. Posted full explanation on the issue.
