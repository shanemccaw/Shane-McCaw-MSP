# #3146 — Feature: Dev floaty panel — override date, weather, sky, Next for testing (Shane's Life)

- **Status:** ✅ DONE 2026-09-10T17:20:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 727cb9297 (pushed to main as c245333b7 after rebase)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — unblocked (both real blockers #3144/#3145 are closed+complete;
  cleared the stale `blocked_by` edges and `blocked` label). Read the README's "Prototype tweaks"
  section and the First Slice Prototype's `door()` logic for the exact `today`/`weather`/`sky`/
  `nextDemo` semantics. Confirmed the real production app (`web/shanes-life/public/`) is plain ES
  modules (no framework/build step); `theme.js`'s `todayOverride()` already reads a `?today=` query
  param (from #3145) but no floaty panel exists yet. Building `public/dev-overrides.js` (override
  store) + `public/dev-panel.js` (the floating UI), wiring `theme.js`/`weather.js`/`app.js` to read
  live overrides instead of requiring a query-string reload.
- 2026-09-10T17:20:00Z ✅ DONE — shipped `public/dev-overrides.js` (live override store,
  localStorage-backed, pub/sub) + `public/dev-panel.js` (the floating always-accessible overlay:
  wrench toggle, `today`/`weather`/`sky`/`nextDemo` controls, a reset-to-auto/live control).
  Dev-only gating: a persistent `?dev=1`/`?dev=0` URL flag (this app has exactly one real account
  -- see src/auth/webauthn.mjs's own "single-user app" comment -- so the real thing being gated
  is clutter on Shane's own daily screen, not a stranger; WebAuthn already gates every session;
  reasoning documented in dev-overrides.js's own header). Wired `theme.js`'s `todayOverride()`,
  a new `weather.js` `weatherOverride()` (exact preview values ported byte-for-byte from the
  prototype's own `door()` `wxText` ternary), and `app.js`'s `viewToday()` (weather+sky into
  `renderTodayHeader`, `nextDemo` into `resolveNextKind` with a scoped preview-data patch so a
  forced Next-card kind never crashes on a missing real field). `nextDemo`'s option set follows
  the real production `resolveNextKind()` output values (doctor/headingHome/dinner/home/generic/
  none) rather than the prototype's own location-demo values (groceries/rental/work/walmart,
  which were its `where`-state concept this app never built -- no location feed yet, see
  build-journal/3144.md); documented in dev-panel.js.
  Verified: module logic (override store, `today`->theme wiring, weather override exact values)
  exercised directly in Node against the real theme.js/weather.js/dev-overrides.js modules; the
  real server boots clean against the local `finances` Postgres DB (`DATABASE_URL` from
  ShanesSurvival's own settings.json) and serves all five changed/new files with real 200s,
  `/api/me` correctly returns `{"user":null}` unauthenticated. Not covered: a real WebAuthn
  passkey sign-in + live WebView2/browser click-through of the panel itself (no passkey/browser
  test harness exists for this personal app, and it is explicitly out of test-manifest scope --
  CLAUDE.md's Test Coverage section only covers `artifacts/msp-portal`/
  `artifacts/shane-mccaw-consulting`). Commit `727cb9297`, rebased and pushed to `origin/main` as
  `c245333b7`. `verify-branch-merged.mjs` confirms merged; working tree clean. Labels synced
  (`in-flight` removed, `complete` added). No new findings to file -- this issue's own scope was
  fully buildable with the sibling Features (#3144/#3145) already landed.
