# #2653 — Diagnostics and Scripts: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T05:40:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 92cf1d563

## Log
- 2026-09-12T05:10:00Z ⏳ IN FLIGHT — Building the Diagnostics module (`artifacts/msp-console`) against
  `Design/MSP_Console/design_handoff_msp_console/Diagnostics.dc.html` (README screen 5) and the real
  `msp-diagnostics.ts` routes: runs list/detail (severity-ranked per #3388, already fixed server-side),
  trigger a run, monitoring packages, and the scripts hand-off list. Per-item acknowledge wired through
  `POST /api/msp/alerts/finding-<id>/acknowledge` (the real mechanism #3399 built, mirroring the cross-tenant
  Alerts feed's own acknowledge action) since `msp_diagnostic_findings` itself has no other resolution route.
- 2026-09-12T05:40:00Z ✅ DONE — Shipped `92cf1d563` ("Diagnostics and Scripts: wire MSP Console UI to
  real endpoints (Git #2653)"): `src/api/diagnostics-api.ts` (new — hooks for monitoring packages,
  resolved package, runs list/detail, trigger, scripts list, script download, acknowledge) and
  `src/console/modules/Diagnostics.tsx` (new — Runs / Findings / Scripts / Packages tabs, mounted at
  `/tenants/:id/diag` in `ConsoleShell.tsx`; 5 new icons registered in `icons.tsx`). No fixture data —
  every row comes from a live route. Findings are rendered in the order the server returns them (the
  #3388 severity-rank fix already lives in `msp-diagnostics.ts`; this client deliberately does not
  re-sort). Verification: `npx tsc --noEmit -p .` clean in `artifacts/msp-console`; `PORT=5199 npx vite
  build` succeeds. `verify-branch-merged.mjs` confirms `agent/2653-q2404` is merged into `origin/main`.
  `git status --porcelain` clean. No new findings filed — the two open questions I checked
  (acknowledge mechanism shape, severity-sort correctness) were already resolved by #3399 and #3388
  before this build started.
