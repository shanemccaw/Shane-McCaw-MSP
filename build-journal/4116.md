# #4116 — Customer portal invoice revision: notification + latest-only default + version history

- **Status:** ✅ DONE 2026-09-14T21:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 6e8e28eb6 (Customer portal: invoice-revision notification, latest-only default, version history)

## Log
- 2026-09-14T21:15:10Z ⏳ IN FLIGHT — Investigated #4109's revise route, notification-center.ts,
  portal-billing.ts, portal-billing-scope.ts, and the real customer-facing billing.tsx page (no
  invoice-detail page exists today — confirmed via billing.tsx's own "What this page deliberately
  does not do" ledger, §1.2). Per Shane's dispatch comment: wiring a real invoice-revised
  notification with a specific-invoice link, filtering the customer's default invoice view to the
  latest (non-superseded) version only, and adding a real invoice-detail + version-history page.
- 2026-09-14T21:45:00Z ✅ DONE — Shipped on commit 6e8e28eb6 (main), real hash after two rebases
  onto concurrent pushes (#4112 landed on the same billing.tsx/test-manifests file mid-build;
  resolved by hand, both sets of changes kept, retypechecked clean after merge).

  What shipped:
  1. `msp-invoices.ts`'s `POST /msp/:mspId/invoices/:id/revise` now fires a real notification
     titled "Your invoice was updated, see what changed", linking to
     `/billing/invoices/{revised.id}` (the new detail page) instead of the generic `/portal/billing`.
  2. `portal-deep-links.ts` gained a dynamic pattern for `/billing/invoices/:id` so that link
     resolves through the SSE push path, matching the existing `/status-reports/:id` precedent.
  3. `portal-billing.ts`'s `GET /portal/invoices` now excludes `status = "superseded"` — the
     customer's default list is latest-version-only, per Shane's decision.
  4. New `GET /portal/invoices/:id/versions` walks the real `supersedesInvoiceId` chain and
     returns each version's own `revisionReason` plus a real field-level diff
     (amount/description/dueDate) against the version it superseded.
  5. New page `artifacts/portal/src/pages/invoice-detail.tsx` at route
     `/billing/invoices/:id`, reusing billing.tsx's existing visual tokens (no `.dc.html` export
     exists for this page — confirmed absent under `Design/portal/`; built directly per Shane's own
     dispatch instruction rather than stopped on). Shows the invoice's current data plus a
     collapsible "See what changed" version-history section. `billing.tsx`'s receipt rows now link
     to it (the existing "Receipt" download button is unchanged).
  6. Extended `test-manifests/portal/billing.json` (tag `4116`) with auth-gate + 400/404 coverage
     for the new versions endpoint and a UI reachability check for the new route.

  Verification: real `tsc --noEmit` clean on both `artifacts/api-server` and `artifacts/portal`
  (post-merge, after resolving the #4112 conflict); real `vite build` succeeds for
  `artifacts/portal`. Confirmed real column names (`client_user_id`, `status`, `version`,
  `supersedes_invoice_id`, `revision_reason`, `amount`) via a real `shaneapp://executeSql` SELECT
  against the local dev Postgres `invoices` table (0 rows currently — no live revision chain
  exists yet to exercise end-to-end). `shaneapp://runTest?file=billing.json&onlyTag=4116` was
  invoked for real and returned a real envelope (`ok:false, "no steps matched session tag
  '4116'"`) because the dev-server checkout BuildConsole resolves manifests against had not yet
  merged this branch at invocation time — the same structural gap this manifest's own #2998/#4112
  notes already document; re-run once the merge/restart cycle lands. Direct `psql`/`DATABASE_URL`
  access was denied by the sandbox's credential-materialization guard (sourcing `.env.local`
  trips it) — used `shaneapp://executeSql` instead, which does not need it.

  No new migration: this build only reads existing #4109 columns (`status`, `version`,
  `supersedesInvoiceId`, `revisionReason`), all already real. No Shane To-Do action.
  `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/4116-q2785` is a
  real ancestor of `origin/main` at 6e8e28eb6.
