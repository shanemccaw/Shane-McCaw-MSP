# #1430 — Security: email page — wire real security.emailAuthFindingCount metric, replace fabricated evidence rows with honest no-data state

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** TBD

## Log
- 2026-08-27 ⏳ IN FLIGHT — Pulled #1430 body/comments and #1414's investigation comment. Confirmed `security.emailAuthFindingCount` (`exchange:dkim-spf-dmarc-status`) is the real live-reachable metric (already used by `useSecAreaLinksLive.ts` / Security overview's Email Security card, #1258). Found `GET /api/portal/email-auth-status` gives real per-tenant single-domain SPF/DKIM/DMARC booleans (used by email-auth-setup.tsx) — noting this for the record, but per this issue's explicit non-negotiable scope, evidence rows get an honest no-data state rather than being reshaped around that differently-shaped endpoint. Plan: add `useSecEvidenceEmailLive.ts` hook (same resolve-batch pattern as `useSecEvidenceOauthLive.ts`), a `securityEmailPageWithLive()` overlay in `secEvidenceData.ts` that (a) adds a live "Open findings" stat card when the metric resolves, (b) unconditionally empties the fabricated `rows` array, and an honest-empty-state render in `portal-v2-security-evidence.tsx`'s row list.
