# #1430 — Security: email page — wire real security.emailAuthFindingCount metric, replace fabricated evidence rows with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 788fa24ec (bookend), d8e745f25 (wiring), 418eb3d9f (manifest sessionTag scoping + lastVerifiedAgainstCommit) — all on `main`

## Log
- 2026-08-27 ⏳ IN FLIGHT — Pulled #1430 body/comments and #1414's investigation comment. Confirmed `security.emailAuthFindingCount` (`exchange:dkim-spf-dmarc-status`) is the real live-reachable metric (already used by `useSecAreaLinksLive.ts` / Security overview's Email Security card, #1258). Found `GET /api/portal/email-auth-status` gives real per-tenant single-domain SPF/DKIM/DMARC booleans (used by email-auth-setup.tsx) — noting this for the record, but per this issue's explicit non-negotiable scope, evidence rows get an honest no-data state rather than being reshaped around that differently-shaped endpoint. Plan: add `useSecEvidenceEmailLive.ts` hook (same resolve-batch pattern as `useSecEvidenceOauthLive.ts`), a `securityEmailPageWithLive()` overlay in `secEvidenceData.ts` that (a) adds a live "Open findings" stat card when the metric resolves, (b) unconditionally empties the fabricated `rows` array, and an honest-empty-state render in `portal-v2-security-evidence.tsx`'s row list.
- 2026-08-27 ✅ DONE — Shipped `useSecEvidenceEmailLive.ts` (resolves `security.emailAuthFindingCount` via `/api/dashboard/resolve`, same pattern as `useSecEvidenceOauthLive.ts`) and `securityEmailPageWithLive()` in `secEvidenceData.ts`: prepends a live "Open findings" stat card (red/green tone by count) when the metric resolves, and unconditionally clears the fabricated "Domains" evidence-row list (fictional `tenant.com` + invented SPF/DKIM/DMARC records) to `[]` regardless of live/fixture state, since it has no per-domain producer. Wired into `portal-v2-security-evidence.tsx` with a new `pv2-ev-email-stats-source` marker and an honest `pv2-ev-rows-empty` message rendered in place of the row list. `typecheck` (tsc --noEmit) clean. New `secEvidenceData.test.ts` (4/4 pass) pins the overlay contract; existing `secEvidenceModel.test.ts` (4/4) unaffected. Updated `test-manifests/portal/security-drilldowns.json` with 2 new tagged uiSteps (sessionTag `1430-email-live`) plus `setup`-tagged login steps for onlyTag scoping. Pushed straight to `origin/main` (rebased cleanly past an unrelated concurrent #1431 MFA-hero-copy edit to the same manifest file — no line overlap). Ran a full-file regression sweep via `shaneapp://runTest` against the deployed dev server (which does carry this pass's code, confirmed via the dev-server checkout's own git log): 38/43 passed; EV9-EV12 (Legacy Auth + Email Security headings/panels) all green, no regression. 5 failures were pre-existing and unrelated to this pass (pv2-mfa-source/pv2-ca-source/pv2-ca-rows-source/pv2-ev-source/pv2-ev-oauth-stats-source all read "fixture" instead of "live" on the testbed tenant — none touch email or files this pass changed). The 2 brand-new tagged steps could not themselves be exercised this session: `shaneapp://runTest` resolves manifest files from BuildConsole's own local main checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`), which was behind `origin/main` (and carrying its own unrelated local-ahead commits) — a shared, actively-used checkout this session correctly did not force-pull. Documented honestly in the manifest's own notes; a follow-up `onlyTag=1430-email-live` run once that checkout syncs will close the loop.
