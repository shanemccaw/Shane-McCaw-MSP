# #3888 — Status Reports: MSP-operator comment endpoints (phase 2 of 4)

- **Status:** ✅ DONE 2026-09-13T06:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 5fb36f3c

## Log
- 2026-09-13T06:20:00Z ⏳ IN FLIGHT — confirmed #3887's `msp_status_report_comments` table exists on main (verified via direct psql). Adding GET/POST comment endpoints to `msp-status-reports.ts` (MSP-operator side, authorType 'msp').
- 2026-09-13T06:20:00Z ✅ DONE — Added `GET`/`POST /api/msp/status-reports/:id/comments` to `msp-status-reports.ts`, reading/writing the existing `msp_status_report_comments` table (no new migration needed — #3887 already created it). `authorType: 'msp'`, ownership-enforced via `assertCustomerAccess` against the report's `customerId`. POST fans out a `createNotification` to every `role='client'` user on the report's customer tenant (same pattern `notifyRetentionRestore` uses). No schema/migration changes required this build. Verified live: `tsc --noEmit` clean, merged+restarted the local dev api-server (pid 14980, HEAD 5fb36f3c), then hit the real endpoints with a signed JWT — GET returned `{comments:[]}` (200), POST created a real row (201), GET after POST showed it in the thread (200), and confirmed 5 real `notifications` rows landed for tenant-1's customer users with `link_path='/status-reports/5'`. Branch verified merged into origin/main via `verify-branch-merged.mjs`.
