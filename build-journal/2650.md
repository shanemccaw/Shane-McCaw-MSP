# #2650 — Requests and Support Chat: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-15T13:45:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 733f792fa (feature), 37a8db9cc (rebased onto origin/main and pushed)

## Log
- 2026-09-15T13:10:00Z ⏳ IN FLIGHT — Authorized by Shane (2026-09-15) to build without a Design export. Read `docs/msp-console/requests-and-support-chat-msp-console-contract-pack.md` and `artifacts/api-server/src/routes/msp-support.ts` for the real 3-route contract. Building an MSP-wide Operations page (org-scoped queue + thread + reply) in `artifacts/msp-console/src`, following the existing `AuditLog.tsx` module pattern, with a visible "Agent-built UI — pending design review" banner per the dispatch requirements.
- 2026-09-15T13:45:00Z ✅ DONE — Added `artifacts/msp-console/src/api/requests-support-api.ts` (data seam for the 3 real routes) and `artifacts/msp-console/src/console/modules/RequestsAndSupportChat.tsx` (list + thread + reply/internal-note UI, with the mandatory "Agent-built UI — pending design review" banner), wired as a new MSP-wide Operations page (`/ops/requests`) via `nav.ts`/`treeModel.ts`/`ConsoleShell.tsx`. Updated `nav.test.ts`'s hardcoded page counts (22→23 ops pages) to match. Verified: `tsc --noEmit` clean, `nav.test.ts` (12/12 pass), `vite build` succeeds. Rebased onto current `origin/main` and pushed directly (`37a8db9cc`); `verify-branch-merged.mjs` confirms the branch is merged. `request-restart.mjs` confirmed the commit merged into the dev-server checkout under Build Set `MSPConsole` (38/45 members) — the set's single coordinated restart is deferred until every member lands, so a live browser check against the running dev server isn't possible from this session without forcing an out-of-turn restart for the other 44 in-flight builds; static verification (typecheck/tests/build) stands in for it. No genuine new finding surfaced beyond what the contract pack already documents (the escalation-vs-request `subject`-text convention, already noted there as a known gap). Working tree left clean.
