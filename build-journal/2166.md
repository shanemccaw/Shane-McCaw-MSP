# #2166 — dlp-role-group-provisioning.ts adds the wrong SP to the Purview DLP role group

- **Status:** ⏳ IN FLIGHT 2026-09-04T06:27:24Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-04T06:27:24Z ⏳ IN FLIGHT — Port the #2161 Gap 3 wrong-SP fix (`global-reader-role-provisioning.ts`'s `resolveTargetServicePrincipalId`, commit `c6e2b4e95`) into `artifacts/api-server/src/lib/dlp-role-group-provisioning.ts`. The DLP chain resolves `MT_APP_CLIENT_ID` (api-server = READ app `4743b130-…` / SP `6bae4b49-…`) but the DLP `powershell` checks run in ca-ps-execution as `9ea2e409-…` / SP `1c640fe8-…`, so the Purview role-group membership lands on a principal that never runs those checks. Plan: (1) resolve target via `PS_EXECUTION_APP_CLIENT_ID` with `MT_APP_CLIENT_ID` fallback, (2) surface resolved appId + source env var in step detail and audit metadata, (3) fix `getDlpProvisioningState`'s membership check to use the same target, (4) update the stale `graph-write-permissions.ts` justification + `docs/tenant-permission-model.md` §7, (5) typecheck + run api-server vitest.
