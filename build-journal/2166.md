# #2166 — dlp-role-group-provisioning.ts adds the wrong SP to the Purview DLP role group

- **Status:** ✅ DONE 2026-09-04T06:49:06Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** `ff13a9608` (IN FLIGHT bookend), `82034aa55` (the fix)

## Log
- 2026-09-04T06:27:24Z ⏳ IN FLIGHT — Port the #2161 Gap 3 wrong-SP fix (`global-reader-role-provisioning.ts`'s `resolveTargetServicePrincipalId`, commit `c6e2b4e95`) into `artifacts/api-server/src/lib/dlp-role-group-provisioning.ts`. The DLP chain resolves `MT_APP_CLIENT_ID` (api-server = READ app `4743b130-…` / SP `6bae4b49-…`) but the DLP `powershell` checks run in ca-ps-execution as `9ea2e409-…` / SP `1c640fe8-…`, so the Purview role-group membership lands on a principal that never runs those checks. Plan: (1) resolve target via `PS_EXECUTION_APP_CLIENT_ID` with `MT_APP_CLIENT_ID` fallback, (2) surface resolved appId + source env var in step detail and audit metadata, (3) fix `getDlpProvisioningState`'s membership check to use the same target, (4) update the stale `graph-write-permissions.ts` justification + `docs/tenant-permission-model.md` §7, (5) typecheck + run api-server vitest.
- 2026-09-04T06:49:06Z ✅ DONE — Shipped `82034aa55`. `dlp-role-group-provisioning.ts` now resolves its target SP through `resolveTargetServicePrincipalId()` (PS_EXECUTION_APP_CLIENT_ID → MT_APP_CLIENT_ID fallback), the exact shape of #2161's `c6e2b4e95`. Resolved appId + source env var recorded in the `resolveServicePrincipal` step detail, on `DlpProvisioningResult` (`targetAppId`/`targetAppIdSource`), at the top level of the `dlp_role_group_provisioning` audit metadata, and in the admin panel DLP card. `getDlpProvisioningState()` resolves the same target so the panel cannot show "member: yes" for a principal the chain no longer targets. Also corrected two now-wrong justification strings in `graph-write-permissions.ts` and updated `docs/tenant-permission-model.md` §7/§9.
  - **Verification (real):** new `src/lib/dlp-role-group-provisioning.test.ts` — 5/5 pass (pins the ps-exec appId in the Graph SP filter, the SP actually passed to `groups.add_service_principal_member`, the audit metadata, the unified-deployment fallback label, and the both-env-vars-unset failure with no writes attempted); registered in the vitest include allowlist. `graph-write-permissions.test.ts` 29/29 pass. api-server `tsc --noEmit` clean; `node build.mjs` clean (exit 0). admin-panel `tsc` has exactly one error, pre-existing and already filed as #1908 (`BuildTrackerExplorer.tsx` lucide `title` prop) — not mine, not touched.
  - **Honest gap:** NOT live-verified against a tenant. Running the chain end-to-end creates an Entra group and a Purview role-group membership in mccawsoft2 — a real write against the tenant that is also Shane's production M365, so out of scope without his say-so. The SP identities the fix targets (appId `9ea2e409-…` → SP `1c640fe8-…`; READ app `4743b130-…` → SP `6bae4b49-…`) were live-verified via Graph on 2026-09-01 by #2161 and are unchanged; `services/ps-execution/README.md:186` independently confirms `ca-ps-execution-dev` is deployed with `MT_APP_CLIENT_ID=9ea2e409-…`.
  - **Config applied:** `PS_EXECUTION_APP_CLIENT_ID=9ea2e409-d1b9-422a-8451-02fa0b98d1c3` appended to `.env.local` in this worktree AND in the main dev-server checkout (gitignored, DEV/testbed scope, agent-modifiable per `docs/tenant-permission-model.md` §9). #2161 documented this local step but never applied it, so both provisioning modules were silently on the MT_APP fallback locally until now. Staging/prod remains a #1281 release-gate action.
  - **Finding filed:** #2834 — `PURVIEW_DLP_ROLE_GROUP_NAME` is unset everywhere, so step 3 of this chain fails on every run regardless of which SP is targeted; needs the real Purview role-group display name from Shane. Parented under #2489 (this issue's Feature), milestone v1.1, label `bug`, board status AI Batter Up. Pre-existing failures NOT re-filed (already tracked): #1772 (api-server vitest baseline — full-suite run this session: 90 files / 236 tests failing, none in a file I touched) and #1908 (admin-panel tsc).
  - `verify-branch-merged.mjs` exit 0 — `agent/2166-q1542` (82034aa5) is an ancestor of `origin/main`. `git status --porcelain` clean; no pre-existing dirty files were present on arrival.
