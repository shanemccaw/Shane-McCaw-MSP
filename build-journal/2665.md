# #2665 — MSP console: POST /msp/change-requests generates a fabricated backup hash

- **Status:** ✅ DONE 2026-09-03T17:47:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** fa48e2a63

## Log
- 2026-09-03T17:38:00Z ⏳ IN FLIGHT — fix POST /api/msp/change-requests to write backupVerified: false and empty backupHash on create, matching portal-change-control.ts's already-fixed pattern.
- 2026-09-03T17:47:00Z ✅ DONE — removed the `Math.random()`-based fake SHA256 hash from `artifacts/api-server/src/routes/msp-changes.ts`'s POST /msp/change-requests; insert now writes `backupVerified: false` and `backupHash: ""`, matching the exact pattern already used in `portal-change-catalog.ts`'s create route. `tsc --noEmit` shows only pre-existing unrelated errors (missing `pino-http`/`vitest` types, stale `lib/db/dist` decls — all documented worktree/build-env issues, none touching this file). Merged to origin/main (fa48e2a63), verified ancestor via `verify-branch-merged.mjs` equivalent (`git merge-base --is-ancestor`). Working tree clean.
