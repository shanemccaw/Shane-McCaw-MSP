# #4782 — shanes-git: generic SSE subscribe/push route (channel-agnostic transport)

- **Status:** ✅ DONE 2026-09-18T22:15:14Z — code-complete + verified against real Postgres; live shanes-git.replit.app needs Shane's manual Replit republish (same as #4697)
- **Scope:** platform (code lives in the separate shanemccaw/shanes-git repo)
- **Started:** 2026-09-18
- **Commit(s):** shanes-git `63f552c` (code), `dd993b1` (its DONE bookend). This repo: only this bookend file (`2bbeaf585` IN FLIGHT, then this DONE).
- **Migration:** none. **Findings filed:** none — nothing broken was found.

## Log
- 2026-09-18 ⏳ IN FLIGHT — read real shanes-git server.ts / db.ts / tokens.ts / index.ts (all matched the issue) before writing.
- 2026-09-18T22:15:14Z ✅ DONE — added `src/pubsub.ts` (dedicated LISTEN client, reconnect + resync, `publish()`), `src/subscribe.ts` (SSE), routes for both auth forms, /mcp GET note fixed. `npm run verify-sse` against real local PostgreSQL 18: 46/46 checks passed (auth forms, fan-out, heartbeat, non-pool LISTEN, killed-listener reconnect, 8000-byte cap, revocation, stop/start); real `src/index.ts` boot also hit with curl. Not verified: the deployed Replit instance, the SIGTERM shutdown path, the 30s dead-TCP ping. Full detail in shanes-git `build-journal/4782.md`.
