# #4042 — Focus bar ETA permanently flat/negative (lifetime OLS fit, no recency window)

- **Status:** ✅ DONE 2026-09-14T13:23:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 8e26d5a08 (fix), 6c66aa1a3 (IN FLIGHT bookend) — landed on main as da5f199d3

## Log
- 2026-09-14T13:17:02Z ⏳ IN FLIGHT — traced root cause in FocusModeService.BuildProgressAsync (fits OLS over the milestone's ENTIRE lifetime series, no recency window, unlike UsageProjection.CurrentWindow's equivalent cut for the usage meter). Verified against real local bt_issue_mirror data for milestone #5 (v1.1): lifetime (~48d) OLS nets -10.5 issues/day (matches the reported permanently-flat/negative symptom); 30-day window still nets -15.4/day; 14-day window nets +10.8/day — a genuinely different, positive verdict. Implementing a 14-day rolling window cut before fitting, scoped to BuildProgressAsync only per the issue's own scope guidance (not touching IssueEtaProjection's shared core, which HomeEtaProjectionService also uses).
- 2026-09-14T13:23:00Z ✅ DONE — Cut `series.Points` to the most recent `EtaRecentWindowDays` (14) real days before building `window`/`totalWindow` in `FocusModeService.BuildProgressAsync`. `dotnet build` on desktop/BuildConsole/BuildConsole.csproj succeeded, 0 warnings/errors. Real-data re-check with the fix's exact math (14-day OLS window) against milestone #5: net pace +10.8 issues/day, 273 open remaining → a real ~25-day ETA instead of the permanent "closing X/day but new issues are landing just as fast" message. IssueEtaProjection's MinEtaSamples(3)/MinEtaSpan(1h) gates are untouched — a young/thin-history milestone still correctly falls back to "not enough history." No test manifest — desktop/BuildConsole is out of scope for those per CLAUDE.md. Commit da5f199d3 confirmed merged into origin/main via verify-branch-merged.mjs; working tree clean.
