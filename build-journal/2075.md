# #2075 — chats/assign-issue self-heal needs a caller signal to disambiguate from milestone numbers

- **Status:** ✅ DONE 2026-09-06T15:38:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 92418c18ab691814800b422ffedf549fb4cbe71d (bookend IN FLIGHT), 613e8c18454f8bd5efc1cfd70d07393a03c25662 → rebased/pushed as c7787f09b (real fix)

## Log
- 2026-09-06T15:13:03Z ⏳ IN FLIGHT — implementing #2075's real fix: add an `isEpicOrIssue`/`is_epic_or_issue`
  caller signal to `POST /admin/build-tracker/chats/assign-issue` and `/chats/unassign-issue`
  (`artifacts/api-server/src/routes/admin-build-tracker.ts`), then a server-side live-GitHub-fetch
  self-heal mirroring `ResolveAndPersistChatLinkAsync` (Git #2068), gated on that signal so a
  GitHub Milestone number sharing the wire parameter is never mistakenly fetched/upserted as an
  issue/epic. Caller-side (`BuildTrackerApiClient.cs` + call sites in `MainWindow.xaml.cs` /
  `Controls/LeftSidebar.xaml.cs`) set the flag `true` only at genuine issue/epic call sites, `false`
  (default) at the milestone-assign site, and computed from `_lastBoardIssues` membership at the two
  combined issue+milestone picker call sites where the target could legitimately be either.
- 2026-09-06T15:38:00Z ✅ DONE — shipped as commit c7787f09b (pushed to `main`). Added
  `resolveChatEpicOrIssue()` (server, `admin-build-tracker.ts`) doing local bt_epics/bt_issues lookup
  first, then — only when `is_epic_or_issue: true` was sent — a live `ghFetchIssue` + `upsertEpicRow`/
  `upsertIssueRow` self-heal, same bounded scope as the desktop `ResolveAndPersistChatLinkAsync`
  (parent epic resolved from the LOCAL table only). Widened `upsertIssueRow`'s `epicId` param to
  `number | null` to allow a standalone issue with no locally-known parent. Both routes now request
  `is_epic_or_issue` in their body and use the shared resolver for both the initial assign lookup and
  the unassign remaining-link recalculation. Caller side: `BuildTrackerApiClient.LinkChatToIssueAsync`/
  `UnlinkChatFromIssueAsync` gained an `isEpicOrIssue` param (default `false`, matching the milestone
  site's existing behavior); set `true` at the two genuine-issue/epic-only call sites
  (`MainWindow.xaml.cs:2755`, `LeftSidebar.xaml.cs` issue-node "Assign Chat to Issue..."), computed
  from `_lastBoardIssues` membership at the two combined issue+milestone picker sites (chat-card
  "Link to Issue/Milestone...", tab-level "Assign to Issue..."), left at the `false` default
  (unchanged) at the milestone-assign site. Verified: `dotnet build BuildConsole.csproj` succeeds
  (0 warnings, 0 errors); `tsc --noEmit` on `@workspace/api-server` shows the same pre-existing
  drizzle-orm/lib-db-decls worktree errors as baseline (none introduced in the touched lines).
  `verify-branch-merged.mjs` confirms merged into `origin/main`; working tree clean.
