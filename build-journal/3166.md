# #3166 — shanes-life migrate runner has no self-service fix for shared-ledger collisions

- **Status:** ✅ DONE 2026-09-07T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** d9ff49af (worktree branch commit), merged to main at c5457a504

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — investigated `web/shanes-life/src/migrate.mjs` +
  `scripts/check-migration-numbers.mjs`; implementing a bounded retry/backoff before
  `assertNoOrphanLedgerRows` hard-fails, plus a self-service `bin/reconcile-ledger.mjs`
  helper scoped to `web/shanes-life/migrations` only, so a session that knows its own
  rename/duplicate is safe can fix its own ledger row without touching another
  session's in-flight state or ShanesSurvival's 001-012 range.
- 2026-09-07T00:45:00Z ✅ DONE — shipped both halves. `migrate.mjs` now retries the orphan
  ledger check up to 3x (~8s) before hard-failing, since the collision can be another
  concurrent worktree's own in-flight rename that self-clears. New
  `web/shanes-life/bin/reconcile-ledger.mjs` (`rename`/`delete` subcommands, `npm run
  reconcile-ledger`) lets a session reconcile its OWN orphan row, hard-scoped to
  `web/shanes-life/migrations` (013+) — it refuses anything in ShanesSurvival's 001-012
  range, refuses touching a row for a file that still exists on disk anywhere, and refuses
  a rename target that isn't a real file on disk. `assertNoOrphanLedgerRows`'s error
  message now points at the new helper. Verified live against the real local `finances`
  DB: the retry loop correctly fired on a REAL, currently-live orphan condition left by
  another concurrent session (`041_plaid_item_health.sql`, `043_account_mask.sql`,
  `044_income_rules.sql`, `044_money_home_tab_decision_tools.sql`) and correctly refused
  to touch it since it isn't this session's own change — left entirely alone, as it should
  be. `reconcile-ledger.mjs`'s guards were independently verified with disposable `999_*`
  ledger rows (successful orphan delete, successful orphan rename onto a real on-disk
  file, refused a real on-disk file, refused a ShanesSurvival-range number, refused a
  rename target absent from disk, refused colliding with an existing ledger row).
  Branch merged into `origin/main` and verified via `verify-branch-merged.mjs` (OK).
  Working tree clean.
