# #2452 — Scope and SLA: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T03:37:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** e0e92726b (IN FLIGHT bookend), fc8743681 (contract pack), 89609a2da (issue-number fix)

## Log
- 2026-09-03T03:27:41Z ⏳ IN FLIGHT — verify portal/customer/scope-status and portal/customer/sla-status endpoints are still real and live, then extract docs/scope-and-sla-contract-pack.md per the #1642 pattern.
- 2026-09-03T03:37:00Z ✅ DONE — Confirmed both `GET /api/portal/customer/scope-status` and `GET
  /api/portal/customer/sla-status` (`portal-customer-engines.ts:173,209`) are real and live.
  Wrote `docs/scope-and-sla-contract-pack.md`: full wire contracts for both routes, the
  `SlaEngineOutput`/`ScopeCreepEngineOutput` fields each route does and doesn't surface, the
  real (non-Drizzle, raw-SQL) `sla_timers`/`sla_policies`/`scope_creep_*` tables and their real
  enum CHECK constraints (confirmed via `psql \d` against local Postgres), honest-empty
  contract, cross-surface edges, and orphaned-endpoint check (zero live frontend callers today —
  expected, no `Design/portal/` export exists yet for this Feature).

  **Real finding, filed as #2513** (sub-issue of Feature #1661, labeled `bug` + `area:engines`,
  board status "AI Batter Up", assigned to Shane): `runSlaEngineForTenant()`
  (`sla-engine.ts:276-305`) resolves its `customerId` argument as a `users.id`, per its own code
  comment — but the `sla-status` route passes it the JWT's `customerId` claim, which is a
  **tenant id** everywhere else in this file. Confirmed live against the real local DB
  (`tenants.id` are low single digits; `users.id` start at 37+, with the one low exception being
  Shane's own tenant-less PlatformAdmin login) that this join never resolves for a real customer,
  so `sla-status` silently returns a fake "all clear" (`on_track`, 0 open requests) regardless of
  actual SLA timer state. The sibling `scope-status` route/engine has no such bug — it scopes
  `scope_creep_*` tables directly by the tenant id, which is the fix `runSlaEngineForTenant`
  needs.

  No fixture/hardcoded data anywhere in this session's output — this was a documentation-only
  build (contract pack extraction), no schema/route/UI changes made.

  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0). Working
  tree clean (`git status --porcelain` empty). Issue #2452 commented and labeled per standing
  rules.
