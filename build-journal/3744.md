# #3744 — WebView2 ProcessFailed handling (OOM/crash auto-recovery)

- **Status:** ⏳ IN FLIGHT 2026-09-12T01:43:45Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T01:43:45Z ⏳ IN FLIGHT — Investigation done (confirmed zero ProcessFailed
  handling anywhere via grep; found the single real init path
  `EnsureWebViewInitializedAsync` in `MainWindow.xaml.cs` used by every WebView2 host:
  chat tabs, `FloatingChatWindow`, `ClaudeTerminalView`, `UiTestExecutor`). About to
  implement: subscribe to `CoreWebView2.ProcessFailed` inside that one init path;
  `RenderProcessExited` → re-navigate the single tab to its last `Source`; on
  `BrowserProcessExited` → null out `_sharedWv2EnvTask` and force-reinit + reload every
  currently-tracked WebView2 against the fresh shared environment; toast + ActivityLog
  on both paths with the real reason/exit code.
