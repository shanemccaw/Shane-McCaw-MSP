# #2704 — MSP console: build the real outbound-webhooks operator backend (list/disable/delivery-log)

- **Status:** ✅ DONE 2026-09-03T20:31:53Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** b3a95d173 (rebased/pushed to main; bookend IN FLIGHT was 5867fbba8)

## Log
- 2026-09-03T20:20:51Z ⏳ IN FLIGHT — Audited `docs/webhooks-contract-pack.md` (#1597) and the real
  `webhooks.ts` portal/admin route file. Building MSP-console operator routes scoped to a target
  customer (`assertCustomerAccess` + `requireRole("MSPOperator")`, same pattern as
  `msp-data-rights.ts`): list a customer's webhooks, reversible disable/enable, delivery log, and
  subscribed-events-vs-real-catalog. New route file, no frontend (blocked separately on #1680 —
  `artifacts/msp-console` doesn't exist yet; this issue is backend-only per its own body). Adding
  `disabledByMspUserId`/`disabledAt`/`disabledReason` columns to `outbound_webhooks` so an MSP
  disable is distinguishable from a customer's own toggle — real data the Feature's own open
  question ("does the customer see the MSP disabled it") will need once decided; not building the
  customer-facing display itself (that's the undecided part).
- 2026-09-03T20:31:53Z ✅ DONE — Shipped `artifacts/api-server/src/routes/msp-console-webhooks.ts`
  (new): `GET /api/msp/customers/:customerId/webhooks`, `GET .../webhooks/:webhookId`,
  `POST .../webhooks/:webhookId/disable`, `POST .../webhooks/:webhookId/enable`,
  `GET .../webhooks/:webhookId/deliveries`, `GET /api/msp/webhooks/event-types`. All gated
  `requireRole("MSPOperator")` + `assertCustomerAccess`. Disable/enable never touches
  label/url/eventTypes/secret — those stay owner-only via the existing
  `/api/portal/webhooks/*` (per #1693's own open question, not decided: "can the operator
  create/edit? disable is clearly right"). Exported `webhooks.ts`'s
  `SUBSCRIBABLE_EVENT_TYPES` and annotate each webhook's subscriptions with
  `unrecognizedEventTypes` — reuses the one real dispatchable catalog rather than inventing
  a second vocabulary (#1607 stays open). Migration
  `lib/db/migrations/manual/2026-09-03-outbound-webhooks-msp-disable-tracking-2704.sql` — ran
  it myself against local Postgres (`ALTER TABLE outbound_webhooks ADD COLUMN
  disabled_by_msp_user_id/disabled_at/disabled_reason`), confirmed via `\d outbound_webhooks`
  and a real insert/update/select round-trip (rolled back, no leftover rows).
  `webhooks.ts`'s owner PATCH now clears those three columns whenever the owner touches
  `isActive` directly, so they only ever reflect an MSP-console-initiated disable.
  **Verification gap, honestly stated:** could not run `tsc`/boot the api-server in this
  worktree — the MAIN checkout's shared pnpm store has empty-extracted payloads for
  `drizzle-orm`/`zod`/`express` (`require.resolve('drizzle-orm')` fails;
  `node_modules/.pnpm/drizzle-orm@.../node_modules/drizzle-orm` is a genuinely empty
  directory), which junctions into every worktree including this one. This is NOT new and
  NOT mine to fix (pnpm install is prohibited as a remedy, Git #1987) — it's already filed
  repeatedly (#2515, #2516, #2522, #2525, #2526, #2528, #2556, #2733, #2734), so no new
  issue filed for it here. Used a `paths`-override tsconfig to confirm my two touched files
  (`webhooks.ts`, `msp-console-webhooks.ts`) surface only that same pre-existing baseline
  noise class, no new/distinct error type. Verified the schema half directly against real
  local Postgres instead (see above) — genuinely tested, not assumed.
  Frontend: none — `artifacts/msp-console` doesn't exist yet (#1693 blocked on #1680);
  this issue is backend-only per its own gap description. No test manifest — scope is
  `desktop/BuildConsole`-equivalent MSP-console backend work with no artifact to test
  against yet, and the standing test-manifest rule is scoped to `artifacts/msp-portal` /
  `artifacts/shane-mccaw-consulting`.
  `git status --porcelain` clean. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`, exit 0) before writing DONE.
