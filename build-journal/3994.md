# #3994 — Break-glass Access: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T03:50:12Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** a0928084e (work), 26c428291 (IN FLIGHT bookend)
- **Findings filed:** #4015 (bug + security, sub-issue of #1651)
- **Migrations:** none (no schema change; only additive response fields plus one new read route)

## Log
- 2026-09-14T03:29:15Z ⏳ IN FLIGHT — Pulled #3994 + #1651; reading `Design/portal/design_handoff_full_site/screens/Break-glass Access.dc.html`, the #2443 contract pack, and the archived msp-portal pages before wiring `break-glass-verify` + `break-glass-status` into `artifacts/portal`.
- 2026-09-14T03:50:12Z ✅ DONE — Shipped in a0928084e:
  - `pages/break-glass-status.tsx` at `/break-glass` and `/break-glass/:runId`. Renders all 4 design states (pending, dead-ended, write-back-blocked, not-pending) plus the real states the design does not draw, each kept distinct: loading, 404 not found, read failed, invalid run ref, no invites yet, claimed link, and every admin-override refusal (400/404/409/502/503). Design copy and ledger kept verbatim.
  - `pages/break-glass-verify.tsx`: public `/break-glass/verify/:token` outside RequireAuth, carried forward from the archived page. Invite emails already link there, and `artifacts/portal` had no such route until now.
  - api (`break-glass-verification.ts`): by-run also returns run context (definition name, pack label via triggerRef), the secret's createdAt, and per-attempt createdAt/expiresAt (the server TTL, not restated client-side). New `GET /api/portal/break-glass` lists pending handoffs for the caller's customer.
  - User-menu row and breadcrumb placed per Shell.dc.html. Override gated client-side on `can("msp","ladder.msp-operator")`; the route stays the gate.
  - **Deliberate gap:** the design's ACCOUNT cell is not rendered. The account identity is not on the customer read (contract §2.2) and is redacted at rest, which is what #4015 is about.
- Verification:
  - `pnpm run typecheck` clean for api-server and portal (portal re-run on the final rebased HEAD); `vite build` of the portal succeeds.
  - Live test on an isolated api-server (worktree build, :8099) against local Postgres, using synthetic `zz-test-*` rows (Customer user, pending run with 3 attempts, completed run), all deleted afterwards (0 rows left):
    - list 200 with pack label resolved
    - by-run pending 200 with createdAt/expiresAt
    - by-run not-pending 200 with run context
    - unknown run 404
    - invite with empty emails 400
    - override as a Customer 404 (role floor held)
  - Not done: an in-browser render (no browser automation available here).
  - No test manifest: CLAUDE.md scopes manifests to artifacts/msp-portal and shane-mccaw-consulting, and runTest hits the deployed build, which does not have this commit yet.
- Pre-existing dirty files: none.
