# #4167 — TYPO/COLOR AUDIT: Controls/HomeView.xaml ignores Typography.xaml/theme brushes

- **Status:** ✅ DONE 2026-09-15T02:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 656d6a4f4 (IN FLIGHT bookend), fd746f39e (implementation)

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — pulled #4167 body + #4162 audit evidence, read HomeView.xaml (900 lines), Typography.xaml, Colors.xaml, DarkTheme.xaml. Confirmed real baseline via grep: 121 raw `FontSize`, 1 raw `FontFamily`, 45 raw hex colors (167 total, matches issue).
- 2026-09-15T02:30:00Z ✅ DONE — mapped all 121 raw `FontSize` + the 1 raw `FontFamily` in `Controls/HomeView.xaml` to real `Themes/Typography.xaml` StaticResource references (107→CaptionFontSize, 9→BodyFontSize, 4→HeaderFontSize, 1→MonospaceFontSize; verified post-edit `FontSize="[0-9]` and `FontFamily="[^{]` counts are both 0). Of the 45 raw hex colors, resolved the 2 with a genuine brush match (`#89B4FA`→AccentColor, `#C6A0F6`→MauveBrush); the remaining 43 (35 fox/bird mascot illustration fills, `#F2CA63` running-icon accent, 7 `#1E3A2F` status-badge placeholders immediately overwritten by hardcoded hex in `HomeView.xaml.cs`) have no matching Colors.xaml/DarkTheme.xaml brush and were left literal per #4167's own instruction — filed as #4210 (parented under #1202, milestone v1.1, board status AI Batter Up) rather than forcing an inexact substitute. Verified `dotnet build BuildConsole.csproj -c Debug` succeeds with 0 warnings/0 errors. Checked `DrawBurndownCanvas`/`DrawOpenCloseRateCanvas` in `HomeView.xaml.cs` — both draw only polylines/ellipses/markers with zero in-canvas text (no `FormattedText`), so the FontSize resource swap carries no clipping risk to the burndown/ETA/open-close-rate charts; the adjacent XAML legend/scope/summary TextBlocks that did resize sit in auto-sizing StackPanels outside the fixed-height (120px) Canvas, so no overflow. Branch verified merged into origin/main via `verify-branch-merged.mjs` before writing DONE. `shaneapp-executeSql.result.json` at repo root showed up modified during the session (last real commit 2026-08-29, unrelated to this issue's SQL — not touched by this session's work) — left it alone per the working-tree-cleanliness rule's "not yours, leave it and name it" clause; not committed, not reverted.
