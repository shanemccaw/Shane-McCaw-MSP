# #1802 — Working-epic highlight: title-text-parsing fallback for tab resolution

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** ea9e4db5c474dbb3feaca7c7bafcb967ab54d995

## Log
- 2026-08-29 ⏳ IN FLIGHT — adding a title-text-parsing fallback to `EditorTabs_SelectionChanged` (untracked-tab case + BoardChat last-resort), resolving via `LeftSidebar.GetEpicByGithubNumber`.
- 2026-08-29 ✅ DONE — added `ExtractTabTitleIssueNumber` (matches `[#NNNN]`, `#NNNN `, bare `#NNNN` at start of title) in `MainWindow.xaml.cs`, wired as a fallback in both the untracked-tab `else` branch (previously always cleared the highlight) and as a last resort inside the `BoardChat` branch when `GetEpicForChat`/`tabState.GithubNumber`/`chat.IssueGithubNumber` all come back empty. Both resolve the extracted number via the existing `LeftSidebar.GetEpicByGithubNumber` — no duplicated resolution logic, existing `BoardChat` chain untouched. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors). Commit `ea9e4db5c` pushed and verified merged into `origin/main` (`git merge-base --is-ancestor` passed). `git status --porcelain` clean. No test manifest — BuildConsole is out of scope for those.
