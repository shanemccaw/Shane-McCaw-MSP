# #1819 — Portal Shell: navigation

- **Status:** ✅ DONE 2026-09-03T14:22:18Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 6e7dee651e69d8c4a916609316022149584497bc (shell implementation), 6893bdd69 (merge into main)

## Log
- 2026-09-03T14:10:55Z ⏳ IN FLIGHT — Read #1819 body + comment, `Design/portal/design_handoff_ui_shell/README.md` + `Shell.dc.html`, `docs/design-system.md`, `docs/pillarmapping.md`, existing `layout.tsx`/`App.tsx`, and confirmed real backend (`GET /api/portal/pillars`, mounted `artifacts/api-server/src/routes/portal-assessment.ts:562`) to back pillar scores + the frame-level severity wash. Scope for this issue per the dispatch + issue body: top bar, pillar tab strip, sidebar module nav, content slot, severity wash — NOT the Tenant Status card / live scan progress (#1824), the three popovers' content (#1820 account, #1821 alerts, #1822 SOP tray), the right-slide panel, ShaneBot dock, or Settings container, which mount inside this shell under their own chained issues. About to implement `components/shell/` + replace `components/layout.tsx`.
- 2026-09-03T14:22:18Z ✅ DONE — Replaced the bare frame in `artifacts/portal/src/components/layout.tsx` with the real shell (`components/shell/PortalShell.tsx` + `TopBar.tsx` + `PillarTabStrip.tsx` + `SidebarNav.tsx` + `moduleNav.ts` + `severityWash.ts` + `usePillarSummary.ts`):
  - Top bar: brand lockup, real breadcrumb derived from route/search, real impersonation banner (`useAuth().isImpersonating` / `returnToAdmin()`), inert-but-focusable SOP/alerts/account trigger buttons (`data-testid`-tagged) for #1820-#1822 to wire popovers onto.
  - Six-pillar tab strip: real per-pillar scores from `GET /api/portal/pillars` (`artifacts/api-server/src/routes/portal-assessment.ts:690`, backed by `buildPillarSummary` in `pillar-summary-stats.ts`); identity colours/icons reused from `@workspace/copilot-scan-scene/journeyTokens` (the same source `docs/design-system.md` documents, already a real portal dependency).
  - Sidebar module nav: the real 11-item order from the design README. Only Overview (`/`) is a real page; every other row and all six pillar tabs route through the existing honest `/coming-soon?feature=` state (Git #1827's `pages/coming-soon.tsx`) — never a dead link, never a page that pretends to exist.
  - Frame-level severity wash: real overall severity band (average of currently-scored shell pillars from the same `/api/portal/pillars` fetch), four cross-faded gradient layers per `docs/design-system.md` §5, 1800ms transition.
  - Forces `.dark` on `<html>` from the shell root, since `index.css`'s `.dark` block is already documented as this app's real Customer Portal palette and `docs/design-system.md` states dark is the portal's only theme.
  - Deliberately NOT built (mount inside this shell under #1820-#1824): Tenant Status card / live scan progress, the three popovers' real contents, the right-slide panel, ShaneBot dock, Settings container.
  - Verification: `npx tsc -p tsconfig.json --noEmit` clean for every new/changed file (22 pre-existing errors elsewhere in the app — `ui/button.tsx`, `ui/badge.tsx`, `support-chat/cards/*`, `pages/support.tsx` — none touching these files, all traced to one root cause below). `npx vite build` transformed all 56 modules (including mine) via esbuild before failing at Rollup's resolution of a pre-existing, unrelated broken dependency.
  - **Finding filed:** #2546 (parented under #1817, `bug`, milestone v1.1, board status "AI Batter Up") — the shared main-checkout `node_modules/.pnpm/class-variance-authority@0.7.1` store entry is empty (confirmed live, not a worktree-junction issue), which blocks `vite build` and `tsc --noEmit` for the whole `artifacts/portal` app via `ui/button.tsx`/`ui/badge.tsx`. Did not attempt to self-heal per CLAUDE.md's `pnpm install` rule — filed for investigation instead.
  - Merged: `agent/1819-q1374` → `origin/main` (fast-forward-safe merge commit `6893bdd69`), confirmed via `verify-branch-merged.mjs`. `git status --porcelain` clean.
