# #3956 — Decommission 4 dead workflow node types

- **Status:** ✅ DONE 2026-09-14T13:01:06Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 9bfd71583

## Log
- 2026-09-14T12:44:59Z ⏳ IN FLIGHT — pulled issue body + Shane's 2026-09-14 decision comment. Scope: remove `ask_ai`, `topic_picker`, `generate_image`, `generate_landing_page` case blocks from `workflow-executor.ts` and their palette entries from `nodeLibrary.ts`. `generate_script` and `zoho_books_daily_ai_rollup` explicitly out of scope. Re-confirming zero live `wf_definitions`/`wf_versions` references before removing.
- 2026-09-14T13:01:06Z ✅ DONE — Re-confirmed live at build time: zero DB references for `topic_picker`/`generate_image`/`generate_landing_page` across any `wf_versions` status; `ask_ai` appears only in def #6 ("Presentation Phase Generator"), whose trigger event (`presentation.phases_requested`) is never emitted anywhere in live code — matches the issue's own documented finding exactly, no new discrepancy. Removed all 4 case blocks (dry-run + live) from `workflow-executor.ts`, removed the 4 entries from `nodeLibrary.ts` (including the now-empty "AI" category), and removed the now-dead test blocks in `workflow-executor-comms.test.ts` / `workflow-executor-content.test.ts` that exercised the removed cases (21 tests would otherwise fail). `tsc --noEmit` clean in both api-server and admin-panel. Full workflow-executor suite: 286/286 passing; one unrelated pre-existing suite (`workflow-executor-redaction-4015.test.ts`) errors on a missing `AI_INTEGRATIONS_ANTHROPIC_BASE_URL` env var at vitest runtime — confirmed pre-existing/unrelated (file untouched by this diff, `git diff --stat` scoped to only the 4 intended files). `node-type-registry.ts`, `patternRegistry.ts`, `admin-workflows.ts` NODE_CATALOG, `workflow-node-default-samples.ts`, `seed-system-workflows.ts`, `WorkflowBuilderPage.tsx`/`RunDetailContent.tsx` property-panel code, and the `lib/db/schema` enum still reference these 4 type strings — left untouched, out of Shane's stated 2-file scope, and touching the builder property panels risks breaking editing of the still-existing (if unreachable) def #6. Filed as a follow-up finding. Branch verified merged into origin/main via `verify-branch-merged.mjs`. Working tree clean.
