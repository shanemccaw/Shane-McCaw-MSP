# #4690 — FalseDoneReconciler Shape A must skip CLOSED issues

- **Status:** ✅ DONE 2026-09-18T16:38:09Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** d9a593f02 (code); 8a6b87384 (IN FLIGHT bookend)
- **Findings filed:** none
- **Manual SQL migrations:** none

## Log
- 2026-09-18T16:36:00Z ⏳ IN FLIGHT — Shane chose Option 2 (skip closed issues in Shape A). Reusing the open-issue snapshot in `FalseDoneReconciler.ReconcileAsync` to gate the `blockedRowIds` loop.
- 2026-09-18T16:38:09Z ✅ DONE — Shape A skips rows whose GitHub issue is CLOSED; `dotnet build` 0 warnings / 0 errors; read-only probe over the real data below. `verify-branch-merged.mjs` exit 0.

## Changes Made
`desktop/BuildConsole/Services/FalseDoneReconciler.cs`: the open-issue snapshot (`TryGetOpenIssueNumbersAsync`) is now fetched once at the top of `ReconcileAsync` instead of after Shapes A/D, and shared. Shape A's loop iterates only BLOCKED-bookend rows whose issue is in the open set; closed ones are left exactly as-is (no `canceled` reset, no board move) and one summary log line names them. If the snapshot is unavailable/empty (untrustworthy) Shape A fails closed and touches nothing that pass. Shape D still runs regardless (it never depended on the snapshot); Shapes B/C keep their existing fail-closed early return, same messages.

## Verification Results
- `dotnet build desktop/BuildConsole/BuildConsole.csproj` → 0 Warning(s), 0 Error(s).
- Read-only probe (real data; exe never launched; the reconciler's write path was NOT executed): `bt_build_queue` rows (BuildConsole DB) 3030/#4367 done, 3256/#4651 verifying, 3257/#4652 verifying. Live `gh`: #4367 CLOSED (Shane To-Do), #4651/#4652 OPEN (blocked). Open snapshot (364 issues) excludes #4367, includes #4651/#4652. Bookends in the real clones: #4367 MyArchitect `🛑 MERGE-BLOCKED`, #4651/#4652 M365Architect `🛑 BLOCKED` (these are the 3 rows #4681 measured as Shape-A-flagged). Applying the new predicate (blocked AND open): #4367 skipped; #4651 and #4652 still flagged.

## Honest gaps
- Not run through the live reconciler write path (would mutate live rows); the verdicts above are the real inputs run through the same predicate by hand. No BuildConsole service test project exists.
- The open snapshot covers the primary repo only; membership is by bare issue number, same as Shapes B/C already do. A row tracking another repo's issue absent from the snapshot is treated as closed, i.e. Shape A leaves it alone (the safe direction). Live, every queue row has repo = Shane-McCaw-MSP.
- Effect appears only when BuildConsole is rebuilt with this change.
