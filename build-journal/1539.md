# #1539 — Remediation: Fix route as a first-class dimension — three item shapes

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Establishing the fix-route dimension: shared enum
  (`we_can_run` / `you_must_run` / `admin_center_only`), authored finding-side
  capability column on `remediation_knowledge_base`, a read-time resolver
  (`shape = min(findingCeiling, tenantWriteCeiling)`) accounting for
  `tenants.consent.writeBack.status`, and a `GET /api/portal/remediation/fix-routes`
  wire contract. Ends at the wire contract — no `artifacts/portal` page.
