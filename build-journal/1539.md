# #1539 — Remediation: Fix route as a first-class dimension — three item shapes

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 5a8f61ea8 (work), 0c1df4f66 (IN FLIGHT bookend)
- **Migration:** `lib/db/migrations/manual/2026-08-30-remediation-fix-route-capability-1539.sql` (run against local DATABASE_URL; logged to #1630)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Establishing the fix-route dimension: shared enum
  (`we_can_run` / `you_must_run` / `admin_center_only`), authored finding-side
  capability column on `remediation_knowledge_base`, a read-time resolver
  (`shape = min(findingCeiling, tenantWriteCeiling)`) accounting for
  `tenants.consent.writeBack.status`, and a `GET /api/portal/remediation/fix-routes`
  wire contract. Ends at the wire contract — no `artifacts/portal` page.
- 2026-08-30 ✅ DONE — Shipped in 5a8f61ea8. `REMEDIATION_FIX_ROUTE` enum +
  `remediation_knowledge_base.fix_route_capability` column (migration run; 1 existing
  row seeded to `you_must_run`). `remediation-fix-route.ts` resolver + 18-case
  vitest suite (added to `vitest.config.ts` include; **18 passed**). Route
  `GET /api/portal/remediation/fix-routes` registered in `routes/index.ts`.
  Verified: `lib/db` tsc clean, api-server tsc clean for all four touched files
  (rebuilt lib/db composite decls into the junctioned dist so project-ref types
  resolve). Live DB check: for tenant 1 (writeBack granted) the endpoint resolves
  one real item — `identity:mfa-registration` (live pack, no KB row) → `we_can_run`.
  Branch verified ancestor of origin/main. Findings filed: see completion comment.
