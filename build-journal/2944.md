# #2944 — portal-messages test is red: client-notification email assertion never fires

- **Status:** ⏳ IN FLIGHT 2026-09-10T16:58:01Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** TBD

## Log
- 2026-09-07T15:50:18Z ⏳ IN FLIGHT — investigating portal-messages.test.ts red assertion; comparing #2849 (createNotification migration) vs #1397 (account-scoped reads) to find which broke the mocked db.select clientUser lookup.
- 2026-09-07T~16:05Z — found real root cause via db.select call-site instrumentation: the test's `@workspace/db` mock is missing `customerNotificationPreferencesTable`/`portalOwnershipAssignmentsTable` exports that `notification-center.ts` (added by #2849) imports. `getCustomerPreference` (notification-center.ts:31) references `customerNotificationPreferencesTable.inAppEnabled` on `undefined`, throws a TypeError synchronously, and its own try/catch swallows it — so that select never fires and never consumes a `mockSelectResultsQueue` slot. #2933's fix (998b4fc05) already patched the FIFO count from 1→3 items and made the test pass again, but for the same "wrong reason" class it called out in its own commit message: the queue realignment happened to work only because getCustomerPreference's select is silently skipped, not because it's genuinely exercised. Confirmed via stack-trace instrumentation on `db.select()` (temporary, removed) that only 3 real selects fired (unread-count, resolveMspUserContext, route's own client lookup) — never getCustomerPreference.
- 2026-09-07T~16:15Z — real fix applied to `artifacts/api-server/src/routes/portal-messages.test.ts`: added the two missing table mocks (matching the real shape already used in the sibling `notification-center-suppress-preference-email.test.ts`), and added the genuinely-missing 4th `mockSelectResultsQueue` entry for the now-real `getCustomerPreference` select (unread-count select was already accounted for). Verified via re-added/removed stack-trace instrumentation that all 4 real select call sites now fire in the correct order and consume the correct queue slot. Test passes 10/10 runs, for the right reason this time.
- 2026-09-08T17:09:53Z — session interrupted before the fix commit landed; BuildConsole rescued the uncommitted worktree state to branch `rescued/2944-q1842-2026-09-08T17-09-54-494Z` (commit c6fab7b31) when the worktree was cleaned up. The original IN FLIGHT bookend commit (f702dae1c, pushed to `origin/agent/2944-q1842`) never got merged into `main` in that window either.
- 2026-09-10T16:58:01Z ⏳ IN FLIGHT (resumed) — worktree reprovisioned fresh off current `origin/main` (now well past where this build started — #2849/#1397/#2933/#2460 etc. all already on main). Recovered the fix from the rescued commit (`git checkout c6fab7b31 -- artifacts/api-server/src/routes/portal-messages.test.ts`), re-verified it applies cleanly and passes against current main (no intervening change to `portal-messages.ts` or the relevant `notification-center.ts` code paths — #2460's RBAC rename only touched an unrelated literal in `fanOutToCustomerWebhook`'s webhook payload). Re-ran `portal-messages.test.ts` + `notification-center-suppress-preference-email.test.ts`: 3/3 passing. Proceeding to commit + push + full-suite verification.
