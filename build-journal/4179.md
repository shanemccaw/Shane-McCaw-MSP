# #4179 — TYPO/COLOR AUDIT: Controls/BugsDocumentView.xaml ignores Typography.xaml

- **Status:** ✅ DONE 2026-09-15T01:55:00Z
- **Scope:** buildconsole

## Log
- 2026-09-15T01:40:00Z — Session start. Mapping 41 raw literals (40 FontSize, 1 FontFamily, 0 raw hex colors) in `desktop/BuildConsole/Controls/BugsDocumentView.xaml` to `Themes/Typography.xaml` resources by role.
- 2026-09-15T01:55:00Z — ✅ DONE — Replaced all 40 raw `FontSize` literals and the 1 raw `FontFamily="Consolas"` with `{StaticResource ...}` bindings, mapped by role/closest value: `10`/`10.5`/`11`/`11.5`/`12` → `CaptionFontSize` (11), `12.5` → `BodyFontSize` (14, list-item title, tie-broken toward prominence), `15` → `SubHeaderFontSize` (16, bug detail title, tie-broken toward prominence), `16` → `SubHeaderFontSize`, `18` → `HeaderFontSize` (title bar icon + "Bug Tracker" title); `FontFamily="Consolas"` (diagnostics textbox) → `{StaticResource MonospaceFontFamily}`. No raw hex colors were present in this file (issue table listed 0). Verified: `grep -c 'FontSize="[0-9]'` / `FontFamily="[^{]'` / hex-color grep all now return 0. `dotnet build desktop/BuildConsole/BuildConsole.csproj` succeeded (0 errors, 0 warnings). Commit `7240d12d` merged into `origin/main` (verified via `verify-branch-merged.mjs`). `git status --porcelain` clean. No genuine mid-build finding beyond what #4179 already scoped.
