# #3983 — Element-level bug status icons in the DOM inspector popover

- **Status:** ✅ DONE 2026-09-14T03:11:26Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 5718087c9 (rebased/merged as 9a0be2571 on main)

## Log
- 2026-09-14T02:54:26Z ⏳ IN FLIGHT — Blockers #3978 and #3981 confirmed actually landed on origin/main (40ee5a08e, 33d01aa5f) despite #3981 showing "open" on GitHub (agents never close issues; that's Shane's call, label sync already done in that session's bookend). Starting investigation of the DOM inspector popover mechanism (`TestModeDiagnosticsPanel`, `BugSubmittedFromDomInspector` in `MainWindow.TestMode.cs`) and the real `selector`/`status`/`resolution` columns on `visual_test_tracker_entries` added by #3978/#3981, to wire a (page_id, selector) lookup into a status icon + count badge + detail view on the popover.
- 2026-09-14T03:11:26Z ✅ DONE — Found the `selector` column #3978 added was never actually wired into any C# code (`VisualTestTrackerEntry` had no `Selector` property; the SELECT/INSERT lists in `VisualTestTrackerStore` never referenced it). Added `Selector` end-to-end: `VisualTestTrackerEntry`, `BugCardViewModel`, `PersistedBugRecord`, the store's SELECT/INSERT/`ReadEntryFromReader` (guarded on `FieldCount > 22`, same pattern as the #3978/#3981 columns), and `SessionHistoryDialog`'s local-JSON round-trip.
  Added `VisualTestTrackerStore.FindPageIdAsync` (read-only page lookup — checking for existing bugs must never itself create a page row as a side effect, unlike `GetOrCreatePageAsync`) and `GetBugsForElementAsync(pageId, selector)` (the real `(page_id, selector)` lookup, newest-first, wrapped in try/catch so a DB hiccup never blocks hovering an element).
  **Real pre-existing bug found and fixed, confirmed live against local Postgres before touching anything else**: `ExecuteEndSessionSyncAsync` in `MainWindow.TestMode.cs` (Test Mode's composer/DOM-inspector sync path — the exact flow #3983 needs) never set `entry.PageId`, so every entry synced through it got the C# default `PageId = 0`. Reproduced directly: `INSERT INTO visual_test_tracker_entries (...) VALUES (..., 0, ...)` against real local Postgres throws `violates foreign key constraint "visual_test_tracker_entries_page_id_fkey" ... Key (page_id)=(0) is not present`, which `SaveEntryAsync`'s try/catch was silently swallowing — meaning no bug logged through the DOM inspector's normal flow had ever actually reached Postgres, and #3983's own lookup could never have found a match for a bug logged through the exact flow it's meant to serve. Fixed by calling `GetOrCreatePageAsync(baseUrl, pagePath)` once per sync (same pattern the standalone `VisualTestTrackerWindow` floaty already uses) and setting real `PageId` on both the per-bug entries and the page-level notes entry. Also hardened `SaveEntryAsync`'s own `@pid` parameter to pass `DBNull` instead of `0` as defense in depth (page_id is nullable per #3978).
  Wired the real lookup: `TestModeDiagnosticsPanel.HandleDomElementInspected` (fires when an element is picked/locked — this codebase's "hover" popover moment) kicks off `ShowElementBugStatusAsync`, which resolves the page via `FindPageIdAsync`, looks up bugs via `GetBugsForElementAsync`, and — guarding against a stale response if the user already moved to a different element — calls back into the WebView (`window.__vttDomShowBugStatus`) to color the in-page popover's new status icon (added to the note-card header markup in `VisualTestTrackerTelemetry.DomInspectorScript`) and set its count. New `ElementBugStatusPresenter` gives one real, distinct color per state: Open=red, Verifying=amber, Closed+Fixed=green, Closed+NotABug=slate (deliberately not just red/green, per the issue).
  **Live-verified the count-badge semantics against the issue's own acceptance criteria** (not the more obvious "raw total" reading): with 2 bugs logged against the same selector, the badge shows `(1)` — one *beyond* the one the icon's color already represents — confirmed by tracing the issue's verification section and adjusting the payload field from `count` to `extraCount = bugs.Count - 1` before shipping.
  Click-through: the popover's icon posts `VTT_DOM_BUG_HISTORY_REQUEST`; `TestModeDiagnosticsPanel` fires a new `BugHistoryRequested` event carrying the locked element + its bug list; `MainWindow.TestMode.cs` opens the new `ElementBugHistoryWindow` (code-only WPF window, same lightweight pattern as `SimpleTextPromptDialog`) listing every bug newest-first: bug number, status/resolution badge, design flag, clickable GitHub issue link (opens the real URL via `Process.Start`), closing build id, created/updated timestamps, resolution reason.
  Reuses the DOM inspector's existing hover/lock/note-card mechanism throughout — no second parallel hover system.
  **Live Postgres verification** (table empty before and after, confirmed via `SELECT count(*)`, all test rows inserted/queried inside `BEGIN`/`ROLLBACK`): (1) the exact `FindPageIdAsync` query shape resolves a real page id; (2) the exact `GetBugsForElementAsync` query shape, given two entries sharing `(page_id, selector)` with different `created_at`, returns both rows ordered newest-first with every column (including `resolution`/`is_design`/`selector`) intact; (3) the exact fixed `SaveEntryAsync` INSERT shape (real page_id + selector, previously page_id=0) now succeeds with no FK violation.
  `dotnet build -c Debug` clean (0 warnings, 0 errors) — one compile break hit and fixed along the way (a JS comment inside the `DomInspectorScript` C# verbatim string used literal `"..."` instead of plain text, terminating the string early; removed the quotes).
  `verify-branch-merged.mjs` OK (5718087c9, merged as 9a0be2571). `git status --porcelain` clean. No findings filed separately — the one real bug found (PageId FK violation) was fixed in this same build, not left as tracked debt.
