# #2634 — Offboarding (MSP Console): regenerate contract pack from finished backend

- **Status:** ✅ DONE 2026-09-03T18:12:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 18b313e18 (bookend IN FLIGHT), c65900e54 (contract pack)

## Log
- 2026-09-03T17:58:00Z ⏳ IN FLIGHT — Confirmed via dispatch comment that the real backend
  (3-route offboarding state machine in `msp-portal.ts`: `/msp/offboarding/request`,
  `/export`, `/archive`) exists, superseding the stale "genuinely blocked" language in the
  issue body. This is a NEW pack for Feature #2566 (Offboarding, MSP Console) — a sibling
  scope to the existing `docs/offboarding-contract-pack.md` (Feature #1653, Offboarding
  Portal), same split pattern as `docs/sops-msp-console-contract-pack.md` vs
  `docs/sops-contract-pack.md`. Writing `docs/offboarding-msp-console-contract-pack.md`
  next, per #1642.
- 2026-09-03T18:12:00Z ✅ DONE — Wrote `docs/offboarding-msp-console-contract-pack.md`
  (c65900e54): all 3 write routes (`/msp/offboarding/request|export|archive`) extracted
  verbatim, cited to file:line, verified against `msp-portal.ts` and confirmed under real
  test coverage (`msp-portal.test.ts`). Verified real DB state via direct `psql` against
  local `DATABASE_URL`: both real `msps` rows currently `offboarding_state = null`.
  Consumer sweep (grep across `artifacts/portal`, `artifacts/msp-website`,
  `artifacts/shane-mccaw-consulting`, `artifacts/admin-panel/src`) confirmed all 3 routes
  are orphaned (no caller) and `artifacts/msp-console` does not exist yet — expected
  pre-scaffolding state, not a defect. Found one real read-only consumer of the underlying
  `offboardingState` column on a *different* route (`GET /api/admin/msps`, feeding
  admin-panel's `MspAdmin.tsx`/`ActiveDirectoryMspPane.tsx`), documented as a cross-surface
  edge, not filed separately. Restated (not re-filed) the already-open #2510
  event-severity-misclassification finding since it affects this Feature's future events
  view too. No new GitHub issue filed — no new defect met the finding bar this session.
  `verify-branch-merged.mjs` confirmed `agent/2634-q1413` (c65900e5) merged into
  `origin/main`; `git status --porcelain` clean.
