# #3303 — Today's night-sky background (Saturn, stars) clips at header instead of continuing into Later section

- **Status:** ✅ DONE 2026-09-09T01:48:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** bad3b2032 (main)

## Log
- 2026-09-09T01:39:15Z ⏳ IN FLIGHT — pulled #3303's real body, confirmed root cause
  (`.today-scene` box sized only to header content, ~130-160px, while the night-sky SVG
  needs ~300px to show Saturn at y=164); read the real Prototype's `showTray` section
  directly to confirm the intended fixed-height structure (outer sky wash 320px, inner
  night SVG pinned to 300px, independent of header content height) before restructuring.
- 2026-09-09T01:48:00Z ✅ DONE — `web/shanes-life/public/app.css`: `.today-scene` gets
  `min-height: 320px` (min, not fixed height, so real long header content/large
  accessibility text still grows the box instead of clipping). `web/shanes-life/public/
  app.js`: `NIGHT_LAYER_HTML`'s SVG gets an explicit `height:300px` (was `100%` of the
  now-320px parent, which forced a slight uniform scale-up to cover the taller box and
  pushed Saturn uncomfortably close to the right edge) -- matches the Prototype's own
  fixed 300px SVG box exactly, 1:1 with the viewBox at the design's reference width.
  Real, live verification: built a standalone HTML harness with the real app.css and the
  real `NIGHT_LAYER_HTML` string extracted verbatim from app.js (no fixture/rewritten
  markup), rendered via system Chrome headless (`chrome.exe --headless --screenshot`,
  Playwright's own bundled browser wasn't installed locally and downloading it would be
  an unjustified metered download) at 402px width. Before the fix Saturn's center
  (y=164 of 300) sits well below the header's real ~130-160px content height and would
  be entirely clipped; after the fix the screenshot shows Saturn's full ring uncut, the
  night sky extending well past the fox bubble, and the following opaque section
  painting cleanly after it with no overlap. Merged to `main` (bad3b2032), verified an
  ancestor of `origin/main` via `verify-branch-merged.mjs`. Working tree clean
  (`git status --porcelain` empty) -- deleted the scratch HTML/PNG test harness files
  after use. No findings to file; no Shane-To-Do action.
