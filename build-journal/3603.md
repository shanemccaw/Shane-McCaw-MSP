# #3603 — Base Button ControlTemplate ContentPresenter alignment fix

- **Status:** ✅ DONE 2026-09-11T02:15:52Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 46470db75 (rebased to 6fbc41477 on main)

## Log
- 2026-09-11T02:12:39Z ⏳ IN FLIGHT — fixing base `Button` ControlTemplate's `ContentPresenter` in `Themes/DarkTheme.xaml` to TemplateBind `HorizontalAlignment`/`VerticalAlignment` to `HorizontalContentAlignment`/`VerticalContentAlignment` instead of hardcoding `Center`/`Center`; then grepping other explicit `HorizontalContentAlignment` usages app-wide to confirm none relied on the broken centered behavior.
- 2026-09-11T02:15:52Z ✅ DONE — fixed `Themes/DarkTheme.xaml`'s base `Button` `ControlTemplate` (the implicit `TargetType="{x:Type Button}"` style every other button style is `BasedOn`) to properly `TemplateBinding` `ContentPresenter.HorizontalAlignment`/`VerticalAlignment` to `HorizontalContentAlignment`/`VerticalContentAlignment` instead of hardcoding `Center`/`Center`. Grepped every explicit `HorizontalContentAlignment` usage app-wide (`ActivityBar.xaml.cs`, `ChatDockPanel.cs`, `MainWindow.ShelvedTabs.cs`, `MainWindow.xaml.cs`, `LeftSidebar.xaml` settings-category buttons, `ChatSessionPane.xaml`, `GitDoctorView.xaml`, `BuildQueuePanel.xaml`/`HomeView.xaml` QuietTile, `ApiRunnerView.xaml`/`TestHistoryWindow.xaml` list container styles) and read each in context: all real `Button`-targeted usages ask for `Left`/`Stretch` and were relying on this fix landing (matching the issue's own Web Tools popup example), not on the broken centered behavior; `ChatSessionPane`'s tool-group button defines its own inline `ControlTemplate` (unaffected); `GitDoctorView`'s Nightmare button explicitly asks for `Center`, WPF's own default (no visible change); the `Stretch` setters on `BuildQueuePanel`/`HomeView` QuietTile target `ToggleButton` and the `ApiRunnerView`/`TestHistoryWindow` ones target `ListBoxItem`/`ListViewItem` — different `TargetType`s, not affected by the `Button` style change. Verified with `dotnet build BuildConsole.csproj -c Debug` — build succeeded, 0 errors (6 pre-existing unrelated `CS0067` warnings in `FocusModeBar.xaml.cs`). Rebased onto `origin/main` and pushed directly (`fec56e10b..6fbc41477`); `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
