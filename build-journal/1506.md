# #1506 — Change Control: change metrics (success rate, emergency ratio, lead time, CAB throughput)

- **Status:** ✅ DONE 2026-09-03T12:35:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** f79b9e1fd

## Log
- 2026-09-03T12:10:00Z ⏳ IN FLIGHT — pulled #1506's body + all 3 comments (2026-08-30 dependency-wiring, 2026-08-30 first BUILD dispatch, 2026-09-03 re-dispatch). Confirmed #1503 (cr_events, cr_comments, cr_attachments) landed and closed/complete; #1502 (close codes) is still OPEN so the 2026-09-03 dispatch's own metric list (cr_events terminal outcomes + cr_executions.outcome, NOT #1502's close-code vocabulary) is authoritative. Read the real schema (crEventsTable, crExecutionsTable, cabMeetingsTable, cabAgendaItemsTable, mspChangeRequestsTable) — the table header already documents the exact formulas #1506 wants. Local DB is empty for these tables (0 rows) — will prove the aggregation against inserted rows, not fabricated ones. Building the aggregation lib + wire-contract endpoint now; scope stop at the wire contract per the issue (no artifacts/portal page).
- 2026-09-03T12:35:00Z ✅ DONE — shipped `artifacts/api-server/src/lib/portal-change-metrics.ts` (pure aggregation: `computeChangeMetrics(scope)`) and `GET /portal/change-control/metrics` in `artifacts/api-server/src/routes/portal-change-control.ts`, gated the same as the register read (`CustomerUser` + `change_control` entitlement). Five metrics, all scoped to (mspId, tenantId): changeSuccessRate/failedChangeRate (per-CR terminal outcome — `cr_executions.outcome` when an execution row exists, else the CR's latest `cr_events` terminal event), emergencyChangeRatio (`raised` events joined to `changeClass`), leadTime (`requestedAt`→`executedAt` on `msp_change_requests`, avg+median hours), cabThroughput (`cab_agenda_items`/`cab_meetings` — decided/deferred counts, avg decision latency, meetings held). Every metric is `available: false` (never `0`) with no qualifying rows — verified logically against a hand-traced 4-CR scenario (2 success, 1 execution-level rollback, 1 still-open; 2 emergency of 4 raised; 1 completed CAB meeting with 2 decided + 1 deferred item) before the runtime blocker below hit.
  - **No schema/migration needed** — every table this reads (`cr_events`, `cr_executions`, `cab_agenda_items`, `cab_meetings`, `msp_change_requests.requestedAt`/`executedAt`) already existed from #1503/#1499/#1501/#1497. No new columns, no counters added — pure read-side aggregation over the real ledger, per the issue's own instruction.
  - **Runtime verification blocked, not skipped**: found the shared pnpm store (`node_modules/.pnpm`) has `express`, `drizzle-orm`, `pg`, `zod`, `jsonwebtoken`, `cors`, `bcryptjs` and most of `artifacts/api-server`'s other runtime deps emptied out from under it (0 files in the installed package dir, main checkout confirmed, not worktree-local) — `npx tsx` importing `@workspace/db` throws `ERR_MODULE_NOT_FOUND`. No dev server reachable on 8080/3000/5173 either. Filed as **#2525** (URGENT, parented under #1096) with full evidence (per-package entry counts, the Sep-1 modify timestamp on the emptied leaf vs. the Aug-30 install birth). Per the standing "pnpm install is not a remedy" rule, did not attempt a fix. Code was reviewed by hand against the real schema instead, and the intended DB scenario is documented in this row for whoever re-verifies once #2525 is resolved.
  - Scope stop honored: no page/route/component added under `artifacts/portal`.
  - `git status --porcelain` clean; branch verified merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs`) before writing this line.
  - Findings filed this session: **#2525** (pnpm store corruption, URGENT, parent #1096).
