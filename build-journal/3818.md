# #3818 — AD OU Assignment: wire the real MSP Console OU placement module (screen 46)

- **Status:** ⏳ IN FLIGHT 2026-09-12T20:05:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T20:05:00Z ⏳ IN FLIGHT — Bookend written after the implementation
  pass (this session's own checkpoint discipline slipped — the file should have
  been created before any edit, per `build-journal/README.md`; recording that
  honestly rather than back-dating it). Investigation: read #3818's full issue
  body (no comments beyond the dispatch), the real contract pack
  (`docs/msp-console/active-directory-ou-assignment-msp-console-contract-pack.md`),
  the real backend (`artifacts/api-server/src/routes/msp-active-directory.ts` —
  7 real routes incl. `GET /msp/active-directory/ous?customerId=` added for
  #2591, which the contract pack predates), the design
  (`Design/MSP_Console/design_handoff_msp_console/AD OU Assignment.dc.html`,
  screen 46, incl. its logic-class mount contract: `embedded` suppresses the
  page's own header, `forceEmpty` passes an empty-state override through), and
  the already-landed MSP Console Shell (#3677: `artifacts/msp-console`,
  `ConsoleShell.tsx` / `nav.ts` / `treeModel.ts` / `ScreenSlot.tsx`) plus its
  sibling module `BreakGlassPage.tsx` as the wiring pattern to follow.
  Implementation: added nav id `"ou"` (label "OU Assignment", icon
  `folder-tree`) as an eighth Access & identity tenant-page child in `nav.ts`;
  registered the `FolderTree` lucide icon in `icons.tsx`; added a `pageMeta`
  override in `treeModel.ts` for `sel.page === "ou"` using the design's own
  exact header copy ("ACTIVE DIRECTORY · OU ASSIGNMENT" / "Who sits in which
  unit"); wrote `src/api/ad-ou-api.ts` (real hooks over all 7 live routes,
  typed response shapes matching the contract pack's own tables — no invented
  fields); wrote `src/pages/ad-ou-assignment/AdOuAssignmentPage.tsx` (two tabs —
  Placements and Requests — OU list, per-OU manual-placement list with a real
  "verify and place" form, a real "move to another unit" picker scoped to the
  same customer's other OUs, a real "clear placement" hard-delete, and the
  customer-request queue with status filters, a request detail panel and
  approve/reject/fulfil actions); wired `sel.page === "ou"` into
  `ConsoleShell.tsx`'s `moduleFor`. Preserved both real, load-bearing
  asymmetries the design and contract pack specify rather than "fixing" them:
  placing an object re-verifies it against Graph (`POST .../assignments`) but
  moving an existing one does not (`PATCH .../ou-assignments/:id` only updates
  `ouId`); an approve/fulfil on a request with `requestedOuId === null` (a
  free-text-only ask) writes nothing but the status, so the Approve button
  reads "Approve (writes nothing)" in that state. No fixture/hardcoded data —
  every row comes from the 7 live `/api/msp/active-directory/*` routes; all
  three backing tables are genuinely empty in local Postgres right now (0 rows
  each, confirmed this session), so the real empty states render, not a
  fabricated placeholder.
