# #1532 — Microsoft Changes: Interpretation layer (authored once, applied to every tenant)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** a9673cfba (feature), rebased + pushed onto origin/main (fb5db1c7a..746246d75)

## Log
- 2026-08-28 ⏳ IN FLIGHT — The interpretation layer for the Microsoft Changes module
  (part of #1494). An interpretation is authored ONCE per MSP and applies to every
  tenant. Fields: change class (retirement/default flip/new feature/breaking change/
  licensing), what it touches (service/protocol/SKU/setting), who acts (Microsoft vs
  admin), controllable + how, and the probe (what to count in a tenant to know if it
  applies). Shane authors; AI proposes the structured reading from bodyContent/roadmap
  description; Shane confirms before it reaches any tenant — no unverified interpretation
  is ever applied. Structurally per-MSP even though there is one MSP today. AdminV2 only
  for the authoring surface (Gallery + Peek + Ribbon + Watch, no sidebar). Scope: schema
  + AdminV2 authoring surface. channel: "integration.azure".
- 2026-08-28 ✅ DONE — Shipped in a9673cfba (pushed to origin/main 746246d75; branch
  verified ancestor of origin/main). Schema: `m365_change_interpretations` table + types
  (M365ChangeClass / status / actor / controllability / M365Touches / M365Probe) in
  `lib/db/src/schema/msp.ts`, per-MSP scoped (msp_id FK, cascade), with feature_id /
  graph_message_id cross-source join keys; manual migration
  `lib/db/migrations/manual/2026-08-28-m365-change-interpretations-1532.sql` incl. a
  PARTIAL unique index (msp_id, feature_id) WHERE feature_id IS NOT NULL. API:
  `artifacts/api-server/src/routes/admin-m365-interpretations.ts` (GET list + counts, GET
  candidates = roadmap/message-center sources with no interpretation yet, POST propose =
  AI reads a source and returns an UNSAVED structured reading, POST create, PATCH,
  POST :id/confirm — the ONLY path to 'confirmed' — POST :id/reject, DELETE), mounted in
  routes/index.ts. AI proposer `artifacts/api-server/src/lib/m365-interpretation-proposer.ts`
  (claude-sonnet-5, strict JSON, controllability guard: drops any opt-out method unless
  the reading is controllable='yes', so an invented opt-out can't slip in; channel
  integration.azure). AdminV2 authoring surface
  `artifacts/admin-panel/src/adminv2/screens/m365-changes/` — store + FetchBridge + Body
  + index; Gallery via command palette (live library), Peek (edit + Confirm/Reject/Delete
  in place, "Unverified — confirm before this reaches any tenant" note on proposed rows),
  Ribbon groups on Home + Watch, Watch live count of proposed-awaiting-confirmation, NO
  sidebar. New peek kind `interpretation` + KIND_BADGE. Verified: scoped `tsc --noEmit`
  clean for all five new files in api-server AND admin-panel (had to rebuild the stale
  `lib/db` project-reference declarations first — the dist was pre-#1530, so #1530's own
  roadmap exports were also missing from it; `tsc -b lib/db --force` fixed both). Full-app
  tsc still has pre-existing unrelated errors (ai-billing, ai-*.test, admin-panel
  build-tracker) not touched here. HARD-RULE honoured: no fixture fallback — an unread
  library / no-MSP / genuinely-empty each render distinct honest states.
  Shane To-Do: run the manual migration against local PostgreSQL 18 before using the
  "Microsoft Changes" AdminV2 surface — until it runs the surface shows an honest "not
  collected" state, never fixture data.
