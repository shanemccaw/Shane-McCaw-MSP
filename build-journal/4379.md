# #4379 — Issue 3 of #4376: real "resume your purchase" stub screen for *Pending accounts

- **Status:** ✅ DONE 2026-09-16T17:45:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 96ffda505, 41c615ede

## Log
- 2026-09-16T17:05:00Z ⏳ IN FLIGHT — investigated #4379/#4374-4378, pending-purchase-gate.ts, portal RequireAuth/interstitial pattern, Buy.tsx product query param; building portal resume-purchase page, wiring RequireAuth + route, resolving pending-purchase-gate.ts TODOs, posting settled route to #4375.
- 2026-09-16T17:45:00Z ✅ DONE — Built `artifacts/portal/src/pages/resume-purchase.tsx`, a real routed screen at `/portal/resume-purchase`. `App.tsx`'s `RequireAuth` renders it unconditionally for any `MonitoringPending`/`PackPending`/`RetainerPending` session on any route (mirrors the existing staff-interstitial mechanism, off the JWT's own `mspRole`). Resolved both `#4379` TODOs in `pending-purchase-gate.ts`: `PENDING_PURCHASE_RESUME_PATH` is now `/portal/resume-purchase`; `PENDING_PURCHASE_GATE_ALLOWED_PREFIXES` needed no addition (the stub's only calls are already covered by `/auth/` and `/public/`). `fetchWithAuth` in `auth-context.tsx` hard-navigates to `resumePath` on a `403 pending_purchase` response as a defense-in-depth backstop. Posted the settled route on #4375 (comment). Mid-build, a concurrent session shipped #4377's real `GET /api/public/purchase/resume` — updated the stub (commit 41c615ede) to read the buyer's actual unfinished checkout session (real seats/status) when one exists for Monitoring/Packs, appending `&resume=1` to the `/buy` link per that session's own follow-up ask; falls back to the JWT-only generic copy for RetainerPending or no session found. No new backend needed beyond what #4377 already shipped — no fixture/hardcoded data anywhere in this build. Verified: `pending-purchase-gate.test.ts` (8/8) and `pendingPurchaseGate.live-db.test.ts` (5/5, real local Postgres) both pass with updated assertions; `tsc --noEmit` clean for both `@workspace/api-server` and `@workspace/portal`. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
