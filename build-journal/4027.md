# #4027 — portal-poams.ts: WirePoam.signed.authorizedBy always null on GET

- **Status:** ✅ DONE 2026-09-14T12:42:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** c2b5dc56c

## Log
- 2026-09-14T12:21:00Z ⏳ IN FLIGHT — wiring `resolveAuthorizedByAsOf`/`resolveRiskAuthoritiesBatch` into `toWirePoam` and both GET routes in `artifacts/api-server/src/routes/portal-poams.ts`, mirroring `portal-risk-register.ts`.
- 2026-09-14T12:42:00Z ✅ DONE — `toWirePoam` now takes a real `authorizedBy: RiskAuthorizedBy | null` param; `GET /api/portal/poams/:poamId` calls `resolveAuthorizedByAsOf(scope.customerId, existing.checkKey, existing.signedAt)` when signed, `GET /api/portal/poams` uses the batched `resolveRiskAuthoritiesBatch` (passing `acceptedAt: r.signedAt`) — same pattern as `portal-risk-register.ts`. tsc clean for the touched file. Live-verified end to end against the real local dev api-server (merged via `request-restart.mjs`, buildSet `PoamAuthorizedByFix`): built a fully synthetic zz-test fixture set (tenant 1801, user 3275 `zz-test-4027-verify@example.com`, a one-off `monitoring_tier` service carrying `poams` in `includedFeatures` since no real tier bundles it yet — #3104's pricing decision is still open, unrelated to this fix), logged in for a real JWT, assigned an Accountable holder via `POST /portal/ownership/assign`, created + signed a real POA&M (`POAM-2026-149`) via the real routes. Confirmed the sign response's own hand-built `authorizedBy` was already correct (per the issue), then confirmed `GET /portal/poams/:poamId` and `GET /portal/poams` BOTH now return the same real, non-null `authorizedBy` (workload `wl-icam`, holder `u3275` "ZZ Test 4027 Verify") instead of the previous always-null value. All synthetic fixtures (tenant, user, service, client_services row, ownership assignment/events, the POA&M) deleted after verification. Branch verified merged into `origin/main` (`c2b5dc56c`); `git status --porcelain` clean.
