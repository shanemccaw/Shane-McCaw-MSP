# #3139 — Migration-number guard does not catch collisions WITHIN web/shanes-life/migrations/

- **Status:** ✅ DONE 2026-09-07T01:30:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** c6d76cbf7 (bookend IN FLIGHT), fae998500/b42a29183 (real fix, rebased onto main)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — confirmed live collisions in web/shanes-life/migrations
  (023_shopping_budget.sql / 023_weekly_ad_coupons.sql, 026_meal_plan.sql / 026_medications.sql),
  both pairs already applied in the local `finances` ledger. Plan: renumber the newer file in
  each pair to the next free number, update the ledger's `schema_migrations.filename` row to
  match (so idempotency is preserved — no re-run), and extend
  `scripts/check-migration-numbers.mjs` to flag same-directory collisions too, with the known
  historical pairs (010_* in ShanesSurvival) allowlisted.
- 2026-09-07T01:30:00Z ✅ DONE — Renumbered `023_weekly_ad_coupons.sql` → `029_weekly_ad_coupons.sql`
  and `026_medications.sql` → `030_medications.sql` on disk; updated the matching
  `schema_migrations.filename` rows in the local `finances` DB in place (both already applied,
  so no re-run). Also found and fixed a related dead ledger row from an earlier rename
  (`024_recipes.sql` → `025_recipes.sql` on disk, stale `024_recipes.sql` row left in the
  ledger with no matching file) — deleted the orphan row. Extended
  `scripts/check-migration-numbers.mjs` with `findSameDirectoryDuplicateMigrationNumbers` /
  updated `assertNoDuplicateMigrationNumbers` to flag a number reused within a single
  migrations directory, not just across the two directories, with the one genuine historical
  pair (`010_debt_is_critical.sql`/`010_expected_events.sql` in
  `desktop/ShanesSurvival/migrations`) allowlisted by exact filename set. A concurrent build
  (#3140, "Guard against renamed-migration orphan ledger rows") landed on `main` mid-session and
  touched the same file for a complementary, non-overlapping concern (detecting a ledger row
  with no matching file on disk at all); rebased cleanly onto it with no conflicts — both guards
  now coexist in the same file. Verified: `node scripts/check-migration-numbers.mjs` exits 0
  clean against the real repo state; a synthetic same-directory collision correctly throws and
  the real allowlisted 010_* pair correctly does not; `web/shanes-life`'s own `runMigrations()`
  run against the real local `finances` DB reports all 18 files "already applied" both before
  and after the #3140 rebase, confirming the renumber + ledger fix caused no re-run. Branch
  verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this. Working
  tree clean (`git status --porcelain` empty).
