# #2532 — Test Pad port: pill window + main pad window, bottom-right, real close-handling

- **Status:** ✅ DONE 2026-09-03T15:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 94cbbe758 (pushed as ef9c60e93 after rebase onto origin/main)

## Log
- 2026-09-03T14:35:00Z ⏳ IN FLIGHT — read #2532 body/comments, #2530 (parent Feature) structured
  index (services #2531 done, this window port #2532, import #2533 separate/not-yet-built),
  ShaneBuilder's TestPadPillWindow/TestPadWindow + TypeChipsControl sources, BuildConsole's already-
  ported Services/TestPad/* (#2531), ToastHostWindow's real positioning/topmost pattern, and
  MainWindow/App.xaml's current (unset → OnLastWindowClose) shutdown wiring. Porting the pill +
  pad windows now, trimming the two sub-features that depend on services BuildConsole doesn't have
  yet (Import → #2533, not built; ClaudeActivityService/AlertActions composer-append bridge → not
  ported) rather than faking them, and wiring real OnExplicitShutdown close-handling per the #2487
  lesson.
- 2026-09-03T15:05:00Z ✅ DONE — Ported `desktop/BuildConsole/TestPad/TestPadPillWindow.xaml(.cs)`,
  `TestPadWindow.xaml(.cs)`, and `TypeChipsControl.xaml(.cs)` (a composer dependency of the pad),
  all reading/writing the real `Services.TestPad.TestPadService`/`TestPadNote`/`NoteMarkerParser`
  from #2531. Positioning/topmost is the exact `SystemParameters.WorkArea` +
  `SetWindowPos(HWND_TOPMOST)` recipe copied from `Notifications/ToastHostWindow.xaml.cs` (pill
  bottom-right, pad just above it). All ShaneBuilder theme brush keys remapped to BuildConsole's
  real Colors.xaml/DarkTheme.xaml keys (e.g. `Brush.Bg.Chip`→`Surface0Brush`), verified against
  those files, not guessed.

  **Real, disclosed scope trim** (not faked/stubbed): two ShaneBuilder sub-features were left out
  because their own backing services were never ported into BuildConsole — Import (a separate
  sibling issue, #2533, explicitly not-yet-built per #2530's structured index) and "Attach
  shot"/Paste Tray (no PasteTrayWindow port exists). Their buttons are simply absent from this
  port rather than wired to something nonexistent. "Send to Claude" and "Copy as markdown" both
  work for real, using `TestPadSendFormatter` + the real clipboard — ShaneBuilder's original
  dropped the formatted block straight into the open chat's composer via a live
  `AlertActions.AppendToComposer` bridge that also doesn't exist in BuildConsole, so "Send to
  Claude" here copies to the clipboard and toasts "paste into Claude's composer" instead of
  fabricating a direct-injection that isn't real. The status band reports the honest unsent count
  only (no fake "Claude is working/free" — that read off ClaudeActivityService, not ported).

  **Close-handling (#2487 lesson)** — `App.xaml` now sets `ShutdownMode="OnExplicitShutdown"`
  (was unset → default `OnLastWindowClose`, confirmed via grep before changing). `MainWindow.xaml`
  wires a new `Closing="MainWindow_Closing"` handler that calls
  `Application.Current.Shutdown()` (guarded against re-entrancy with a `_shuttingDown` flag,
  since `Shutdown()` itself closes every open window — including MainWindow again — as part of
  its own sequence). This explicitly closes the pill, the pad, and every other secondary window
  BuildConsole already has (Sticky Notes, Build Watch, Test Runner, etc.) the moment MainWindow
  closes, not just the new Test Pad windows. The pill is created once at startup
  (`EnsureTestPadPill()`), gated the same way `StartShaneAppProtocolListener()` already is —
  `!AppMode.IsAgent && !App.QuietProtocolCourierLaunch` — so a `--agent`/`--dev`/protocol-courier
  cold start never pops a visible window Shane didn't ask for.

  **Verification:** `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0
  warnings/errors. Every `StaticResource`/`FindResource` brush key used in the three new files was
  manually cross-checked against `Themes/Colors.xaml`/`Themes/DarkTheme.xaml` (all present).
  **Live launch+close+tasklist verification was NOT performed** — `Get-Process -Name
  BuildConsole` showed a real BuildConsole.exe already running from
  `desktop\BuildConsole\bin\ShanesBuild\` at the time (Shane's own live instance), and per
  [[never-launch-buildconsole-exe-for-verification]] (Git #2156 — the single-instance mutex can
  courier a "new" launch straight into an already-running instance), starting another copy or
  driving any BuildConsole window right now risks touching that live session. Asking Shane to do
  the real launch→close-MainWindow→`tasklist`/`Get-Process` check himself once he's free, or to
  confirm no live instance is running first.
