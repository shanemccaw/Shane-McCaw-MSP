# Build Journal — #4549

- **Status:** ⏳ IN FLIGHT 2026-09-17T22:22:00Z
- **Scope:** platform

## Log

- 2026-09-17T22:22:00Z — Session start. Investigated #4522/#4531 CA migrations,
  `ca-policy-promotion.ts`, `resolve-then-write.ts`. Wrote
  `build-journal/4549-plan.md`. Proceeding to implement: strip the
  `(report-only)` suffix from the 5 CA create template display names,
  PATCH `displayName` as part of promotion, and widen the existence-lookup
  match to `in:<clean>,<suffixed>` so re-runs still recognize their own policy.
