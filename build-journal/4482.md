# #4482 — sendSmsOtp logs plaintext OTP & reports success when Twilio unset (any env)

- **Status:** ✅ DONE 2026-09-17T23:26:15Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 83acda07e49f95e155d540d6f2397f400c8c8361 (fix); plan/IN-FLIGHT bookends in prior commits

## Log
- 2026-09-17T23:22:14Z ⏳ IN FLIGHT — Fixing `sendSmsOtp` (`artifacts/api-server/src/routes/mfa.ts`): gate the log-the-code fallback on `NODE_ENV === "development"` (#1380's stricter gate); throw everywhere else so callers surface their real "could not send" path instead of falsely reporting success. All six call paths audited for graceful throw handling — see 4482-plan.md.
- 2026-09-17T23:26:15Z ✅ DONE — Shipped in 83acda07e. The Twilio-unconfigured branch now logs the code (`{ code }`) ONLY when `NODE_ENV === "development"`; in staging/prod/test it logs an error carrying no code and throws `"SMS delivery is not configured."`. Audited all six `sendSmsOtp` paths — the three free-scan routes (`/public/free-scan/account/mfa/sms/setup`, `.../recover/mfa/sms`, `.../login`) catch → 502; the three `/auth/mfa/sms/*` routes propagate to `app.ts:141`'s top-level handler → clean 500 (Express ^5.2.1 forwards async-handler rejections). None crash. Verification: `pnpm --filter api-server typecheck` (tsc --noEmit) passed clean; grep-confirmed the only `code` log is inside the `development` branch. No test manifest — change is in `artifacts/api-server/`, outside the portal/marketing manifest scope. No findings filed (no incidental bugs discovered). No Shane To-Do (additive/reversible, no migration).
