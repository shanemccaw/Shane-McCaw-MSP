# Build Journal — #2481

- **Status:** ✅ DONE 2026-09-03T01:20:00Z
- **Scope:** buildconsole
- **Issue:** #2481 — Build Chain Map: real GitHub persistence (replace localStorage), sub-issue of Feature #2473
- **Started:** 2026-09-02
- **Commit(s):** `1e26401ca` (work) · `c5f9b08b0` (IN FLIGHT bookend) — both on `origin/main`, branch verified merged via `verify-branch-merged.mjs` (exit 0)
- **Findings filed:** #2498 (sub-issue priority ORDER not persisted — out of scope, proven-broken), parented under #2473, board `AI Batter Up`

## Log
- 2026-09-03T00:40:00Z — ⏳ IN FLIGHT — Session started. Blockers #2475 (read layer / ChainDoc) and #2480 (interactions) both CLOSED + `complete`; `dependencies/blocked_by` on #2481 confirms both closed → unblocked. Pulled #2481 body + the single BUILD comment (dispatch, `Posted: 2026-09-02T22:58:28Z`). Read the full stack: `BuildChainMapWindow.xaml.cs` (+ `.Inspector.cs`), `ChainCanvasControl` (link-mode edge add in `OnNodeClicked`), `Services/BuildMap/{ChainDoc,ChainRules,BuildChainMapService}.cs`, and `GitHubApiClient.cs` write surface. Finding: the WPF port has **no localStorage** — #2479/#2480 left every mutation (board status, sentinel, manual gate, reorder, add/remove blocker, Re-wire §5.2) as an **in-memory `ChainDoc` write only**, explicitly deferring real persistence to this issue. So the job is: make each mutating UI action a real, verifiable GitHub write, and audit each by re-reading it back. Write primitives already exist on `GitHubApiClient`: `SetBlockedByAsync` (POST blocked_by), `SetIssueStatusByNumberAsync`/`SetProjectItemStatusAsync` (`updateProjectV2ItemFieldValue`), `GetBlockedByAsync`/`GetIssueBoardStatusAsync` (audit reads). Board option ids: BatterUp `09b1927f`, Backlog `63cc47c8`, Ask Shane `404998bb`, Done `0003ae3b`. Plan: (1) add a strict `RemoveBlockedByEdgeAsync` (dependency-only DELETE, no label strip, throws on failure) + a `bypassCache` param on `GetBlockedByAsync` so audit re-reads aren't served stale by the ETag cache; (2) new `Services/BuildMap/ChainPersistence.cs` — snapshot(before) + snapshot(after) → diff blocked_by edges (by (from,to) pair, kind-agnostic since GitHub has no edge kind) and board statuses → apply real writes → re-read (bypassing cache) to AUDIT each landed → structured result; (3) wire every window mutation to capture a before-snapshot, run the in-memory mutation as today, then persist+audit and fold the real verified result into the status-strip confirmation + ActivityLog. Sub-issue priority ORDER (drag reorder) has no GitHub write primitive in these three named types — its blocked_by (gate) consequences persist, but the column order itself is a known gap to file under #2473.
- 2026-09-03T01:20:00Z — ✅ DONE — commit `1e26401ca` (`Build Chain Map: real GitHub persistence with re-read audit (#2481)`) on `origin/main`, branch verified merged (`verify-branch-merged.mjs` exit 0). **What shipped:** every Build Chain Map mutation now writes back to GitHub for real and audits itself by re-reading. New `Services/BuildMap/ChainPersistence.cs` — a diff-based persister: a mutation captures a `ChainSnapshot` (blocked_by pairs + per-issue board status) before it runs, then hands before+after to `PersistAndAuditAsync`, which computes the real changes and applies them via the three named write types — **blocked_by edge add/remove** (`SetBlockedByAsync` / new strict `RemoveBlockedByEdgeAsync`), **sentinel re-fan-in** (edge diff captures the fan-in rewire), **board-column moves** (`SetIssueStatusByNumberAsync` → `updateProjectV2ItemFieldValue`) — then **re-reads each write back to audit it** (cache-bypassing `GetBlockedByAsync` for edges, fresh GraphQL `GetIssueBoardStatusAsync` for board). Wired all mutation sites in `BuildChainMapWindow.Inspector.cs`/`.xaml.cs`: manual-gate toggle, sentinel change, reorder (drag + arrows), 4 board-status buttons, remove-edge(s), Re-wire §5.2, and canvas link-mode add (single-edge fast path). Edge diff is by raw `(from,to)` pair (kind-agnostic — GitHub stores no edge kind, so a fanin↔gate↔manual re-classification of the same pair is not a real write and doesn't churn). Status-strip confirmation now shows the real verified result (`saved to GitHub ✓ N edges, M board moves, re-read verified`) or an honest `⚠ K/N unverified`; ActivityLog gets per-write detail. Updated the now-stale "edits apply in this window only / land in #2481" inspector copy + both class docs. **Real bug caught + fixed mid-build:** GitHub's remove-dependency endpoint takes the blocker id in the URL PATH (`DELETE .../dependencies/blocked_by/{id}`), not a JSON body — a body-based DELETE 404s. The pre-existing `RemoveBlockedAsync` (Git Board unblock) had exactly this bug and swallowed the 404 silently, so it never actually removed the dependency; fixed it in the same commit (a gap fixed in-build, so documented here, not filed as a separate finding). **Verification (real, against live GitHub, net-zero):** `dotnet build` clean twice (0/0), incl. after rebasing onto 16 concurrent commits. Validated all four board option-id constants live against the project Status field (Batter Up `09b1927f`, Backlog `63cc47c8`, Ask Shane `404998bb`, Done `0003ae3b`). Ran a full audited blocked_by round-trip on a benign closed pair (#2475 blocked_by #2479): POST add → fresh re-read shows `[2479]` → PATH-form DELETE → fresh re-read shows count 0 — the exact write+audit cycle `ChainPersistence` performs, left net-zero. Board-write path (`SetIssueStatusByNumberAsync`) is pre-existing production code (BuildQueuePanel Park/Un-park) so already proven live. Not launched (single-instance-mutex rule); BuildConsole is out of scope for test manifests per CLAUDE.md. **Finding filed:** #2498 (reorder doesn't persist sub-issue ORDER → gate edges reclassify as manual on refresh), parented under #2473, board `AI Batter Up`. Working tree clean; branch verified merged.
