# Build Journal — #2481

- **Status:** ⏳ IN FLIGHT 2026-09-03T00:40:00Z
- **Scope:** buildconsole
- **Issue:** #2481 — Build Chain Map: real GitHub persistence (replace localStorage), sub-issue of Feature #2473
- **Started:** 2026-09-02
- **Commit(s):** _pending_

## Log
- 2026-09-03T00:40:00Z — ⏳ IN FLIGHT — Session started. Blockers #2475 (read layer / ChainDoc) and #2480 (interactions) both CLOSED + `complete`; `dependencies/blocked_by` on #2481 confirms both closed → unblocked. Pulled #2481 body + the single BUILD comment (dispatch, `Posted: 2026-09-02T22:58:28Z`). Read the full stack: `BuildChainMapWindow.xaml.cs` (+ `.Inspector.cs`), `ChainCanvasControl` (link-mode edge add in `OnNodeClicked`), `Services/BuildMap/{ChainDoc,ChainRules,BuildChainMapService}.cs`, and `GitHubApiClient.cs` write surface. Finding: the WPF port has **no localStorage** — #2479/#2480 left every mutation (board status, sentinel, manual gate, reorder, add/remove blocker, Re-wire §5.2) as an **in-memory `ChainDoc` write only**, explicitly deferring real persistence to this issue. So the job is: make each mutating UI action a real, verifiable GitHub write, and audit each by re-reading it back. Write primitives already exist on `GitHubApiClient`: `SetBlockedByAsync` (POST blocked_by), `SetIssueStatusByNumberAsync`/`SetProjectItemStatusAsync` (`updateProjectV2ItemFieldValue`), `GetBlockedByAsync`/`GetIssueBoardStatusAsync` (audit reads). Board option ids: BatterUp `09b1927f`, Backlog `63cc47c8`, Ask Shane `404998bb`, Done `0003ae3b`. Plan: (1) add a strict `RemoveBlockedByEdgeAsync` (dependency-only DELETE, no label strip, throws on failure) + a `bypassCache` param on `GetBlockedByAsync` so audit re-reads aren't served stale by the ETag cache; (2) new `Services/BuildMap/ChainPersistence.cs` — snapshot(before) + snapshot(after) → diff blocked_by edges (by (from,to) pair, kind-agnostic since GitHub has no edge kind) and board statuses → apply real writes → re-read (bypassing cache) to AUDIT each landed → structured result; (3) wire every window mutation to capture a before-snapshot, run the in-memory mutation as today, then persist+audit and fold the real verified result into the status-strip confirmation + ActivityLog. Sub-issue priority ORDER (drag reorder) has no GitHub write primitive in these three named types — its blocked_by (gate) consequences persist, but the column order itself is a known gap to file under #2473.
