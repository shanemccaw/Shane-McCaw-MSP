# #2152 — Worktree @workspace/* runtime resolution fix (per-package linking)

- **Status:** ✅ DONE 2026-09-01T20:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** `3cc35d34a` (fix + README), `a74ef36de` (IN FLIGHT bookend) — both on `main`

## Log
- 2026-09-01T18:20:00Z ⏳ IN FLIGHT — Read #2152/#2151/#2117 in full, audited `link-deps.mjs`. Phase 1: confirm real RUNTIME (not just tsc) resolution breakage via #2151's marker-export scratch-worktree repro. Phase 2: restructure `linkDeps()` to per-package `@workspace/*` linking (third-party stays wholesale). Verify runtime isolation + `store-doctor.mjs` clean.
- 2026-09-01T20:55:00Z ✅ DONE — **Phase 1 (confirmed real):** provisioned a scratch worktree with the CURRENT link-deps, added a marker to its own `lib/db/src`, then from the consuming `artifacts/api-server` ran Node's REAL resolver — `import.meta.resolve("@workspace/db/schema")` + `realpath` resolved to the MAIN checkout (`C:\Source\...\lib\db\src\schema\index.ts`), not the worktree's own. Runtime resolution IS broken exactly like typecheck (#2151). Proceeded to Phase 2. **Phase 2 (`3cc35d34a`):** `linkDeps()` now checks `hostHasWorkspaceScope` per host — a host with an `@workspace` scope (api-server, admin-panel, portal, msp-website, shane-mccaw-consulting, lib/dashboard-canvas, scripts) gets a REAL `node_modules`: every third-party top-level entry junctioned from main as before (dirs junctioned, rare files copied), each `@workspace/<pkg>` junctioned at THIS worktree's own `lib/<pkg>` (target re-anchored from main's real link, falls back to `lib/<pkg>`/`lib/integrations/<pkg>`). Hosts with no `@workspace` stay a single wholesale junction (root/.pnpm unchanged). `findAndUnlinkWorktreeJunctions` now descends a REAL per-package `node_modules` to unlink every inner reparse point (top-level + `@workspace/*`) so worktree removal never deletes THROUGH into the shared store. **Verified end-to-end:** fresh worktree via NEW link-deps — api-server `node_modules` is a real dir, `@workspace/db` → `C:\wt\2152-verify\lib\db`, `ajv` still resolves via main store, root stays a junction; marker in the worktree's own lib/db now resolves+imports at RUNTIME to the worktree (`import.meta.resolve` + executed `import()` returned `worktree-verify`), and is ABSENT from main and the sibling worktree. `store-doctor.mjs` on main CLEAN before and after (1430 links, 0 foreign/dangling/poisoned). Cleanup unlinked all 234 inner junctions, removed the worktree, no delete-through (main `lib/db` intact). Scratch `rescued/*` branches deleted. Working tree clean. Branch verified merged via `verify-branch-merged.mjs`.
