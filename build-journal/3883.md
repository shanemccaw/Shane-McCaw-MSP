# #3883

- **Status:** ⏳ IN FLIGHT 2026-09-13T04:24:32Z
- **Scope:** platform

## Log
- 2026-09-13T04:24:32Z — Investigated the 2 pre-existing failures in
  `artifacts/api-server/src/routes/msp-api-foundation.test.ts`:
  1. `X-Trace-Id is echoed when provided in request` — the test asserted a real
     header value (`"test-trace-123"`) should be echoed back. Confirmed live in
     `src/app.ts`'s request-context middleware that only a **UUID-shaped**
     forwarded `x-trace-id` is ever honoured (arbitrary client strings are
     replaced with a fresh `randomUUID()`, deliberately — the event-bus
     `correlationId` schema requires a UUID). Also confirmed the test's isolated
     Express app never mounted that context-establishing middleware at all
     (it only mounted `msp-v1.ts`'s router), so `getRequestContext()` was always
     `undefined` regardless of the header sent.
  2. `invalid mspId returns 400 with VALIDATION_ERROR code` — failing with 503,
     not 400. Root cause: `requireRole` (#2458) now reads real RBAC ladder rows
     via `middlewares/rbac-ladder-source.ts`, and this node-runner test file was
     the one node-test suite that never supplied `mock.module(...)` for that
     module (every sibling suite behind the real middleware does — see that
     module's own header comment, and `msp-staff.test.ts` /
     `msp-team-invite.test.ts` for the established pattern). The DB mock's
     `select()` shape doesn't match what `readLadderRows()` expects
     (`roleRows.filter is not a function`), so the ladder read throws and the
     gate fails closed to 503.
