# #4529 — Compensating Security Defaults re-enable when a run fails before an enforcing CA policy exists

- **Status:** ✅ DONE 2026-09-17T16:12:40Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 97e91d88b (compensation), eca515049 (live-verify harness)

## Log
- 2026-09-17T14:57:42Z ⏳ IN FLIGHT — Adding a compensating write (re-enable Security Defaults) to the config-pack workflow run path when a disable-security-defaults step succeeded and the run fails before an enforcing CA policy is confirmed, recorded on the run.
- 2026-09-17T16:12:40Z ✅ DONE — 97e91d88b: when a workflow run ends failed/cancelled after an execute_baseline_template step turned Security Defaults off and no later step returned an enforcing CA policy (state "enabled", MFA, All users, All apps, all client app types — judged on the policy Graph returned), executeWorkflowRun/resumeWorkflowRun PATCH /policies/identitySecurityDefaultsEnforcementPolicy {"isEnabled": true} via graphWriteForTenant (gates still apply; 429/5xx retried twice), read it back, and record it on the run: wf_run_node_outputs row `__compensation__.security-defaults`, a wf_run_node_logs row, a suffix on wf_runs.error_message, and a baseline_action_template_audit_log row (source security_defaults_compensation). New: security-defaults-compensation.ts (pure planner), security-defaults-compensation-run.ts; config-pack-preconditions.ts gained enforcingPolicyShapeGap / securityDefaultsWriteEffect / isConditionalAccessPolicyCreate (#4513 behavior unchanged). eca515049: scripts/verify-security-defaults-compensation-4529.mts. Verification: tsc 0 errors (after rebase over #4528); 134 tests pass (12 new planner tests, preconditions, workflow-executor-core). LIVE, testbed tenant 2080 via the real engine: (a) real-disable mode — the disable step was refused by the write-consent gate (tenant 2080 has no writeBack consent: 0 of 6 local tenants do), the run failed, and compensation correctly did NOT fire; Security Defaults stayed on. (b) --recorded-disable mode, run 47634 — a successful disable step recorded on a throwaway run, the real engine failed the CA step (missing breakGlassGroupId, no Graph call), compensation detected the open disable, attempted the real PATCH, the consent gate blocked it, and the failure was recorded on all four surfaces (audit row #73); PASS 7/7, Security Defaults on before and after. GAP, stated plainly: a real "disable succeeds → re-enable succeeds" round trip was NOT live-verified, because it needs write-back consent for the DEV write app (9f6f4772) on mccawsoft2, which is Shane's to grant (Shane To-Do on #4529); once granted, run the harness without --recorded-disable. Findings filed under #1489: #4546 (config-pack-dry-run.test.ts #4513 P1 case already failing on main, graph mock not reaching license-gate). Not covered: a disable done inside a run_workflow child run is compensated only if the child run itself ends failed/cancelled. No migration. Tree clean.
