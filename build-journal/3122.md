# #3122 — Barcode scan's saved price (#3109) doesn't feed the real per-store price history (#3112)

- **Status:** ✅ DONE 2026-09-08T01:11:30Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 9fed99175 (rebased/pushed as 5609fc75e on origin/main)

## Log
- 2026-09-08T01:10:29Z ⏳ IN FLIGHT — investigated `scan.mjs`/`prices.mjs`, confirmed the gap the
  issue describes, and settled the product question the issue itself raised (does Scan need its
  own store-select?) by finding the run already has one: `lists.store` (#3108's "Which store?"
  field). About to wire `saveScan()` to call `prices.recordPrice()` using that field, matching the
  design's own "Shane only types the price" copy for this screen (no new store prompt at scan
  time).
- 2026-09-08T01:11:30Z ✅ DONE — `scan.mjs`'s `saveScan()` now calls `prices.recordPrice()` (real
  `item_prices`/`stores` write, `source: "scan"`) using `list.store` after its existing scalar
  write, when the run has a store set; unchanged (scalar-only) when it doesn't, same requirement
  #3112's own "Log price" form already has. Updated the stale finding-note comment on the
  `/api/lists/:id/scan/save` route to describe the real, now-fixed behavior. No schema change
  needed — `item_prices.source` has no CHECK constraint, so `'scan'` is a valid value already (the
  weekly-ad path already uses a third value, `'weekly_ad'`, the same way).

  **Verified for real** against the local `finances` Postgres DB (not `shanemccawmsp` — this app
  has its own database, see `.env.example`): wrote a throwaway script that created a real list with
  `store = 'Aldi Test Store 3122'`, called the real `saveScan()`, confirmed a real `item_prices` row
  landed with `price_cents=349`, `source='scan'`, correctly joined to the real `stores` row, then
  deleted every row the script created (list, list_items via cascade, item_prices, stores,
  barcode_links) — the DB is left exactly as found. Script itself was scratch and was deleted
  before committing (never part of the diff).

  No test manifest — `test-manifests/` scope per CLAUDE.md is `artifacts/msp-portal/` and
  `artifacts/shane-mccaw-consulting/` only; `web/shanes-life/` is a separate app with no test
  harness in this repo today.

  Working tree clean, branch verified merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs` → OK) before writing this DONE.
