# #3908 — Build Sets panel: add a "Queued" activity-filter chip between All and Running

- **Status:** ✅ DONE 2026-09-13T01:58:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T01:48:32Z — Starting. Wiring a new "Queued" chip into BuildQueuePanel's rollup activity filter row, backed by existing `buckets[k].upNext` data.
- 2026-09-13T01:58:00Z — ✅ DONE — Added `RollupFilterChipQueued` (Tag="queued") between `RollupFilterChipAll`/`RollupFilterChipRunning` in `Controls/BuildQueuePanel.xaml`; added `"queued" => buckets[k].upNext.Count > 0` to the rollup filtering switch and `"queued" => "queued"` to the empty-state `filterLabel` switch in `Controls/BuildQueuePanel.xaml.cs`. `UpdateRollupActivityFilterChipsVisual()` explicitly lists/null-checks chips (not fully generic as the issue body assumed) — added `RollupFilterChipQueued` to its null-guard and its selection-state loop. Verified via `dotnet build` — Build succeeded, 0 warnings, 0 errors. Commit d44ad38189e1ee928b9da97bcc90f8df5f3f727d, pushed to origin/main, verified merged via verify-branch-merged.mjs. Working tree clean.
