# #4292 — MyArchitect: built-in PowerShell script editor (AvalonEdit + real CommandCompletion)

- **Status:** ✅ DONE 2026-09-16T01:36:10Z
- **Scope:** both (build dispatched from Shane-McCaw-MSP; real work lands in shanemccaw/MyArchitect)
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/MyArchitect@1c7f73b ("Script Editor — AvalonEdit PowerShell editor + live CommandCompletion (#4292)")

## Log
- 2026-09-16T01:24:37Z ⏳ IN FLIGHT — Blockers #4278/#4284/#4291 all confirmed CLOSED. Pulled #4292 body + all comments: comment 2 (2026-09-15T20:40:54Z) **supersedes** the body — architecture is now **AvalonEdit** (native WPF), not Monaco/WebView2 (no cross-process bridge; completion source `CommandCompletion` is already in-process C#). Read #3459's real `PowerShellConsoleService`/`HostedConsolePSHost`/`TenantModuleConnectionService`, the shell document model (`MainWindow` `ShowDocument`/`DocumentOverlays`, Console fixed ribbon tab), `UI_RULES.md` §1/§2/§10/§11, `Themes/Colors.xaml`. Confirmed AvalonEdit 6.3.1.120 is already in the local NuGet cache (no download) and ships a real built-in `PowerShell.xshd` (named colors: Comment/String/Char/Punctuation/NumberLiteral/Keywords/Variable/ExceptionKeywords/GotoKeywords/ReferenceTypes/Command/Operators — all light-theme, must be remapped to muted dark tones per §11). Confirmed `LaunchControlAction` carries NO script-content field — Script Library is entitlement metadata, not editable script text — so the real integration points are Run-into-Console (#3459 hosted runspace) + real `.ps1` file Open/Save, NOT a fabricated in-app script store. Plan: add `ICSharpCode.AvalonEdit` ref; add `CompleteInputAsync` (CommandCompletion.CompleteInput against the shared runspace, try-lock so it never fights a running command) to `IPowerShellConsoleService`/`PowerShellConsoleService`; new `Shell/ScriptEditorView` center document with AvalonEdit + native `CompletionWindow` bridge + Run/Open/Save; open it from a new "Script Editor" command on the Console fixed ribbon tab (§2 Open intent); add muted `Syntax*` color keys to `Colors.xaml`; run current buffer into the real Console panel.
- 2026-09-16T01:36:10Z ✅ DONE — Shipped `shanemccaw/MyArchitect@1c7f73b` (pushed to origin/main, confirmed `git merge-base --is-ancestor HEAD origin/main` = YES). What landed: (1) `AvalonEdit` 6.3.1.120 PackageReference (already in local NuGet cache — no download). (2) `IPowerShellConsoleService.CompleteInputAsync` + impl in `PowerShellConsoleService` — real `System.Management.Automation.CommandCompletion.CompleteInput(input, cursor, null, ps)` against the SAME shared hosted runspace `ExecuteAsync` uses (so completions reflect the modules actually imported + the tenant's live Graph/Az context, not a static list); guards the single-threaded runspace with a zero-timeout try-acquire on the existing `_executionLock` so it never blocks behind a running command, and degrades to empty on any failure. (3) `Models/PSCompletionItem.cs` — `PSCompletionItem` + `PSCompletionResult` (carries the engine's `ReplacementIndex`/`ReplacementLength` so an accepted item overwrites the real token). (4) `Shell/ScriptEditorView.xaml`/`.xaml.cs` — native AvalonEdit editor + native `CompletionWindow` bridge (Ctrl+Space + eager trigger on `-`/`.`/`$`/`:` and 3+ letter identifiers; VS Code commit-on-non-identifier), reuses AvalonEdit's real built-in `PowerShell.xshd` grammar with every named color remapped to muted dark-theme `Syntax*` tones (UI_RULES.md §11), real `.ps1` Open/Save/Save As/New (Microsoft.Win32 dialogs, unsaved-discard guard), and Run/F5 → `RunRequested`. (5) `ConsolePanelView.RunScriptAsync` — runs the editor buffer straight into the real Console via the same `ExecuteAsync` path a typed command uses (busy-guarded), so output lands in the one real console pane. (6) MainWindow wiring: `ScriptEditorDocument` center document, Initialize, RunRequested → show Console + RunScriptAsync; new "Script Editor" open-intent command on the Console fixed ribbon tab. (7) `Colors.xaml` muted `Syntax*` + editor-chrome keys. (8) `UI_RULES.md` §2 updated to document placement. **Real integration decision (reported, not guessed):** `LaunchControlAction` carries NO editable script-content field — the Script Library (#3460/#4291) is a server entitlement catalog, so there was no real "open a Library script's text" data to wire; per the issue's own "don't invent a third disconnected script-storage mechanism" the real integrations are Run-into-Console + real `.ps1` file I/O. **Verification:** `dotnet build MyArchitect.csproj` — 0 warnings, 0 errors, before and after rebasing onto origin/main. The novel completion contract was verified live against the real `System.Management.Automation` API (pwsh): `CommandCompletion.CompleteInput("Get-Ch",6,$null,$ps)` → ReplacementIndex=0/Length=6, 1 Command match `Get-ChildItem`; `"Get-ChildItem -"` → 28 ParameterName matches (`-Path`,`-LiteralPath`,…) — exactly the `ReplacementIndex`/`ReplacementLength`/`CompletionText`/`ResultType`/`ListItemText`/`ToolTip` shape the code maps. Full interactive GUI run (login + tenant) not exercised from this headless session; core completion + build both really verified. `git status --porcelain` clean in `shanemccaw/MyArchitect`. No genuine findings/bugs discovered mid-build worth filing separately.
