# #4292 — MyArchitect: built-in PowerShell script editor (AvalonEdit + real CommandCompletion)

- **Status:** ⏳ IN FLIGHT 2026-09-16T01:24:37Z      <!-- flip to: ✅ DONE <timestamp> -->
- **Scope:** both (build dispatched from Shane-McCaw-MSP; real work lands in shanemccaw/MyArchitect)
- **Started:** 2026-09-16
- **Commit(s):** <real hash(es) — fill at DONE>

## Log
- 2026-09-16T01:24:37Z ⏳ IN FLIGHT — Blockers #4278/#4284/#4291 all confirmed CLOSED. Pulled #4292 body + all comments: comment 2 (2026-09-15T20:40:54Z) **supersedes** the body — architecture is now **AvalonEdit** (native WPF), not Monaco/WebView2 (no cross-process bridge; completion source `CommandCompletion` is already in-process C#). Read #3459's real `PowerShellConsoleService`/`HostedConsolePSHost`/`TenantModuleConnectionService`, the shell document model (`MainWindow` `ShowDocument`/`DocumentOverlays`, Console fixed ribbon tab), `UI_RULES.md` §1/§2/§10/§11, `Themes/Colors.xaml`. Confirmed AvalonEdit 6.3.1.120 is already in the local NuGet cache (no download) and ships a real built-in `PowerShell.xshd` (named colors: Comment/String/Char/Punctuation/NumberLiteral/Keywords/Variable/ExceptionKeywords/GotoKeywords/ReferenceTypes/Command/Operators — all light-theme, must be remapped to muted dark tones per §11). Confirmed `LaunchControlAction` carries NO script-content field — Script Library is entitlement metadata, not editable script text — so the real integration points are Run-into-Console (#3459 hosted runspace) + real `.ps1` file Open/Save, NOT a fabricated in-app script store. Plan: add `ICSharpCode.AvalonEdit` ref; add `CompleteInputAsync` (CommandCompletion.CompleteInput against the shared runspace, try-lock so it never fights a running command) to `IPowerShellConsoleService`/`PowerShellConsoleService`; new `Shell/ScriptEditorView` center document with AvalonEdit + native `CompletionWindow` bridge + Run/Open/Save; open it from a new "Script Editor" command on the Console fixed ribbon tab (§2 Open intent); add muted `Syntax*` color keys to `Colors.xaml`; run current buffer into the real Console panel.
