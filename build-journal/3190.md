# #3190 — Feature: Recipes -- Round 2 visual rebuild (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T17:43:55Z
- **Scope:** platform (web/shanes-life -- Shane's personal app, tracked under Epic #3086)
- **Started:** 2026-09-08
- **Commit(s):** 134b4dae7 (rebased/pushed to origin/main as d3511252e)

## Log
- 2026-09-08T17:34:24Z ⏳ IN FLIGHT — investigated #3190/#3183/#3224/#3086 + read
  `Design/design_handoff_shanes_life/Shanes Life 05 - Recipes.dc.html` and the
  README's "Today v3 -- the cute skin" spec directly. Confirmed #3190's own scope is
  the visual rebuild only (blob-shaped containers, rounder cards, pill buttons,
  matching #3144's proven Today treatment) -- not the design file's own turn-2
  "step timer + ingredient checks" addition, which has no existing implementation
  and no existing tracking issue; filing that as a separate finding rather than
  scope-creeping it into this visual-rebuild issue. Reused the app's own existing
  `sticker()`/`pillButton()` helpers (already proven by Today's Next card) instead
  of inventing new component patterns. About to implement: recipe cards + meal-plan
  cards get the 22px blob radius, sticker badges replace the plain chip status
  badge, and the Cook/Add-missing CTAs become pill buttons -- "Remove" stays the
  app's existing `ghost small danger` convention (confirmed still used even in
  already-rebuilt rooms like Shopping, so not itself a Round-2 gap).
- 2026-09-08T17:43:55Z ✅ DONE — shipped `web/shanes-life/public/app.js` +
  `web/shanes-life/public/app.css` (commit `134b4dae7`, pushed to `origin/main` at
  `d3511252e`, rebased clean, no conflicts). Read the README's "Screens" section in full
  (not just the Recipes-specific `.dc.html` turn, which only shows Cook mode + a desktop
  browser mockup) and found the real, explicit, general spec I'd have missed otherwise:
  "Cards 22px; all buttons in 999px pill wrappers... Recipes, Cook, Tonight, Review and
  Shopping keep their solid card-colored header band." Implemented against that:
  - `recipeCard()` + the "This week's plan" meal-plan cards: 22px `.recipe-card` blob
    radius, reusing the existing `sticker()`/`pillButton()` helpers #3144 already proved
    for Today's Next card (no new component pattern invented).
  - Recipe status badge (canMake/missing) is now a rotated sticker (green/amber), not the
    plain `.chip` every other still-unbuilt room uses.
  - Cook / Add missing to Shopping / Remove are now 999px pill buttons; Remove keeps its
    real red destructive meaning via a new `.btn-pill.ghost.danger`.
  - Recipes gets its own native `.recipe-header` ("solid card-colored header band"),
    replacing the generic `#app-header` for this route -- same real shape Shopping (#3178)
    already shipped, duplicated with recipe-prefixed classes per this file's existing
    per-room convention (not a shared abstraction yet -- only two rooms use this shape so
    far). `render()`'s `hasOwnHeader` now includes `"recipes"`. This directly answers
    #3190's own "check whether the generic app-header can be removed" question -- yes,
    per the README's explicit list.
  - Renamed the "Recipes" section h2 to "Saved" (the design's own real desktop-browser
    vocabulary) so it doesn't repeat the new header's own title.
  - Confirmed no dedicated form UI exists in this room (per #3183's own prior audit; also
    grepped the live route for `input`/`textarea`/`<form` -- none).
  **Verification**: `node --check app.js` and a CSS brace-balance check both pass. Booted
  the real server locally (disposable port 5099, temporary gitignored `.env` against the
  real local `finances` Postgres, `node_modules` junctioned in from the main checkout
  rather than reinstalled) -- clean boot, `/`, `/app.js`, `/app.css` all 200, `/api/recipes`
  and `/api/meal-plan` correctly gate at 401 under the new code (not a crash). Server
  killed and the temporary `.env` removed afterward. No authenticated in-browser render
  was possible in this session (WebAuthn/passkey, no platform authenticator available) --
  same honest limitation #3144 and #3178's own bookends recorded for this exact app; diff
  hand-reviewed against the design source line by line instead.
  **Findings filed** (both parented under Feature #3183, board status "AI Batter Up"):
  - **#3255** -- Cook mode has no in-step timer with ingredient checks; the design file's
    own turn-2 "Additions to 1k" (timer chip, `+1 min`/`Stop`, full-screen "Timer done")
    was never built and has no existing tracking issue. Out of #3190's own visual-rebuild
    scope, so filed rather than built here.
  - **#3256** -- Cook mode and Tonight still show the generic `#app-header`, not their own
    "solid card-colored header band" the README names them for alongside Recipes/Shopping.
    Out of #3190's own Recipes-only scope, so filed rather than built here.
  `verify-branch-merged.mjs` exit 0 (merged into `origin/main`); `git status --porcelain`
  clean before writing this DONE.
