# #3650 — msp-sow.ts add_on/subscription checkout: no webhook consumer, mis-targeted customer_email

- **Status:** ✅ DONE 2026-09-13T00:12:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 4132c7e5e (bookend IN FLIGHT), e4bdbe394 (real fix — on origin/main)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Investigated msp-sow.ts accept endpoint (add_on/subscription branch) and msp-billing-webhook.ts. Confirmed: (1) checkout session sets `metadata.fulfillment_type = "msp_offer"` but msp-billing-webhook.ts's handleCheckoutCompleted early-returns for anything that isn't "msp_monthly_subscription"/"msp_platform" signup_source (and drops every "payment"-mode session outright before that check even runs), so an msp_offer session is silently dropped even after successful payment; (2) `customer_email: req.user!.email` sends the Checkout Session email to the accepting MSP staffer, not the customer. About to: add a real `msp_offer` handler in msp-billing-webhook.ts and fix customer_email to resolve the actual customer contact email.
- 2026-09-13T00:12:00Z ✅ DONE — Shipped in e4bdbe394 (pushed to origin/main, confirmed via verify-branch-merged.mjs). (1) msp-billing-webhook.ts: `fulfillment_type === "msp_offer"` is now checked FIRST in `handleCheckoutCompleted`, before the subscription-mode gate, routing to new `handleMspOfferCheckoutCompleted()` — handles both "payment" (one-time add_on) and "subscription" mode; records a real `tenant_subscriptions` row (billingParty: "customer", source: "checkout") for subscription-mode purchases, and calls `resolveFulfillment()` for both modes, mirroring portal-checkout.ts's own "portal_offer" checkout-redirect pattern. (2) msp-sow.ts: added `resolveOfferCustomerId` (shared with the existing project-branch tenant lookup) + `resolveOfferCustomerEmail` (via the canonical `resolveCustomerPortalUserId`); `customer_email` now resolves the offer's real customer contact and is omitted entirely (never defaults back to the staffer) when unresolvable. Checkout metadata enriched with customerId/serviceId/fulfillmentTypeKey/amountCents/serviceName for the webhook to consume. Tests: 38 (msp-sow.test.ts) + 11 (msp-billing-webhook.test.ts) new/updated, plus portal-checkout/msp-marketplace-purchase/resolve-fulfillment/tenant-billing-state suites — 112 total, all green. `tsc --noEmit` clean for both changed files (one pre-existing, unrelated error at portal-checkout.test.ts:549 predates this session, untouched). **Verified end-to-end against real Stripe test-mode API** (customer, payment method, real Subscription, confirmed off_session PaymentIntent) **and the real local Postgres dev DB** via the unmocked `dispatchMspStripeEvent` — both a subscription-mode and a one-time payment-mode msp_offer checkout correctly recorded `tenant_subscriptions`/resolved fulfillment where before neither did anything at all. All verification artifacts (Stripe test customer/subscription/product, tenant_subscriptions/fulfillment_idempotency/client_services/msp_event_store rows) were deleted/canceled/archived after confirming — local DB and Stripe test account left clean, confirmed by re-query. No findings filed — this build's own scope covered both bugs the issue described; no additional gap discovered. `git status --porcelain` clean.
