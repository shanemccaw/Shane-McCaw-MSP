# #3466 — Test Pad notes don't survive app restart (add real persistence)

- **Status:** ✅ DONE 2026-09-10T05:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 236fd6317 (IN FLIGHT bookend) · 7a6b046b8 (impl) · this file's DONE commit

## Log
- 2026-09-10T05:10:06Z ⏳ IN FLIGHT — pulled #3466 body + both dispatch comments (second is a
  re-dispatch, same scope, does not supersede). Investigated the UI layer the body's search never
  checked (`desktop/BuildConsole/TestPad/`). Confirmed live: `TestPadService` is a purely in-memory
  `static List<TestPadNote>` (Services/TestPad/TestPadService.cs) with **zero** persistence — no
  load-on-startup, no save-on-change, no backing table. Every mutation funnels through
  AddNote/UpdateNote/RemoveNote/MarkSent/NotifyMutated. So this is "no real persistence exists at
  all" → build it. Plan: new `bt_test_pad_notes` table (manual migration, run against local
  DATABASE_URL), a `TestPadPersistence` store matching the GitHubIssueMirror/VisualTestTrackerStore
  direct-Npgsql pattern, load-on-startup + save-on-each-mutation wired into the TestPadService funnel.
- 2026-09-10T05:30:00Z ✅ DONE — shipped Test Pad persistence in commit 7a6b046b8:
  - **Migration** `lib/db/migrations/manual/2026-09-10-bt-test-pad-notes-3466.sql` — new
    `bt_test_pad_notes` table (id, text, note_type, screen, feature, build_number, created_at,
    is_sent, is_edited, is_selected, has_shot_slot, monotonic `sort_seq` identity, updated_at).
    Additive; **run against local DATABASE_URL in-session** and self-marked in
    `simulator_migration_runs`. Recorded on the #1630 checklist (comment 5613560630).
  - **`TestPadPersistence.cs`** — direct-Npgsql store (same connection-resolution as
    GitHubIssueMirror/VisualTestTrackerStore): `LoadAllAsync` (newest-first via sort_seq DESC),
    `Upsert`/`UpsertMany`/`Delete`, all gated (SemaphoreSlim) for write ordering and best-effort
    (any DB failure logged + swallowed → pad runs in-memory only, as before).
  - **`TestPadService`** — `InitializeAsync` (one-shot, guarded) seeds from the store; write-through
    added to AddNote/UpdateNote/RemoveNote/MarkSent and a new `SetSelected` funnel for checkbox state.
  - **`TestPadModels`** — Id/CreatedAt made `init` so a note round-trips its original id + file-time.
  - **`TestPadWindow.xaml.cs`** — select checkbox routes through `SetSelected` (persists selection).
  - **`MainWindow.xaml.cs`** — `EnsureTestPadPill` kicks off `TestPadService.InitializeAsync()`.
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` → **0 warnings / 0 errors**.
    Live SQL round-trip against the real `bt_test_pad_notes` (via psql): upsert, newest-first load
    order (sort_seq DESC), mark-sent update, and delete all confirmed; all test rows cleaned up
    (final count 0 — no fixture data left). Per CLAUDE.md, BuildConsole (WPF) is out of scope for
    test manifests; did NOT launch BuildConsole.exe to verify (single-instance-mutex rule).
  - **Branch merged into origin/main** (verify-branch-merged.mjs exit 0). Working tree clean.
  - No findings filed — no separate real bug discovered; the issue was exactly the missing
    persistence layer, now built.
