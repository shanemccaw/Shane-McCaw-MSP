# #3466 — Test Pad notes don't survive app restart (add real persistence)

- **Status:** ⏳ IN FLIGHT 2026-09-10T05:10:06Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** <real hash(es) — fill at DONE>

## Log
- 2026-09-10T05:10:06Z ⏳ IN FLIGHT — pulled #3466 body + both dispatch comments (second is a
  re-dispatch, same scope, does not supersede). Investigated the UI layer the body's search never
  checked (`desktop/BuildConsole/TestPad/`). Confirmed live: `TestPadService` is a purely in-memory
  `static List<TestPadNote>` (Services/TestPad/TestPadService.cs) with **zero** persistence — no
  load-on-startup, no save-on-change, no backing table. Every mutation funnels through
  AddNote/UpdateNote/RemoveNote/MarkSent/NotifyMutated. So this is "no real persistence exists at
  all" → build it. Plan: new `bt_test_pad_notes` table (manual migration, run against local
  DATABASE_URL), a `TestPadPersistence` store matching the GitHubIssueMirror/VisualTestTrackerStore
  direct-Npgsql pattern, load-on-startup + save-on-each-mutation wired into the TestPadService funnel.
