# #1530 — M365 Roadmap ingestion (public unauthenticated API)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** c6f0be3cc (feature), fast-forwarded onto origin/main (694c9d61c..c6f0be3cc)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Schema + sync for M365 Roadmap ingestion. v1 nightly full
  snapshot (1000+ items), v2 OData targeted queries (page cap 50), persist cloud
  instance from the v2 `availabilities` complex type for gov/GCC exclusion (#1537).
  Sync wired as a visible Workflow Engine node — no bare scheduler. Honest-degrade:
  never serve stale roadmap data as current. Manual ALTER TABLE SQL migration, no
  drizzle-kit push. No UI. Part of #1494.
- 2026-08-28 ✅ DONE — Shipped in c6f0be3cc. Two global tables (`m365_roadmap_items`,
  `m365_roadmap_sync_state`) added to `lib/db/src/schema/msp.ts`; manual migration
  `lib/db/migrations/manual/2026-08-28-m365-roadmap-ingestion-1530.sql` (Shane runs it).
  Sync module `artifacts/api-server/src/lib/m365-roadmap-sync.ts`: v1 nightly full
  snapshot + bounded v2 OData targeted path, honest-degrade (records failure in
  sync_state without wiping items or advancing last_success_at; detects relocation /
  non-JSON / <100-item degraded responses). Wired as visible Workflow Engine node
  `m365_roadmap_sync` (registry + executor case + `WfNode["type"]` union + seeded
  "__system__: M365 Roadmap Sync" nightly 02:00 UTC workflow). cloud instances
  persisted per item for the #1537 gov/GCC exclusion. Verified: scoped tsc of the new
  module + synthetic union assignability check both clean against fresh db source;
  probed the real public v1 endpoint live — 1795 items, `tagsContainer.cloudInstances`
  = ["Worldwide (Standard Multi-Tenant)","GCC"], confirming every normalization
  assumption. Full-app `tsc` has pre-existing unrelated errors (ai-billing, tests,
  admin-build-tracker) not touched here. Branch confirmed ancestor of origin/main.
  NOTE for Shane: run the migration before the sync first fires (Shane To-Do).
