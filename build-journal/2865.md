# #2865 — 4 test files intermittently fail only under full-suite parallel vitest run

- **Status:** ✅ DONE 2026-09-04T16:41:48Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** fec25cefb (fix, merged to main as 53a8a9924)

## Log
- 2026-09-04T16:16:19Z ⏳ IN FLIGHT — investigating 4 flaky test files (doc-pipeline.test.ts, live-monitor-nodes.test.ts, portal-assessment-debug-trigger-scan.test.ts, portal-assessment-debug-reset-session.test.ts) that time out under full-suite parallel vitest run but pass in isolation.
- 2026-09-04T16:41:48Z ✅ DONE — Reproduced the root cause directly: 2 full `npx vitest run` passes (DATABASE_URL loaded, matching real CI) showed all 4 files timing out at 5000ms/10000ms on their dynamic `await import(...)` calls under the ~305-file parallel run — confirmed CPU/transform contention, not a logic bug. Fix: scoped `vi.setConfig({ testTimeout: 20_000, hookTimeout: 20_000 })` in each of the 4 files (not a global vitest.config.ts change, per the issue's own instruction), plus removed a redundant `await import("pdf-lib")` in doc-pipeline.test.ts (PDFDocument was already statically imported). Could not hoist the `./portal-assessment` / `../graph` dynamic imports to static top-level imports — tried it, both broke (one on env-var-before-import ordering, one on vi.mock hoisting/TDZ) — reverted those two attempts, kept the dynamic imports with the raised timeout instead. Verified: all 4 pass in isolation, and pass together across 2 consecutive full-suite parallel runs post-fix (0/4 timeouts, down from 3-4/4 failing pre-fix). Could not reproduce the issue's second ask (root-causing `portal-assessment-debug-reset-session.test.ts`'s deleteCalls 2-vs-4 assertion mismatch) — both reproduction attempts hit the timeout shape on all 4 of that file's tests instead, no distinct logic bug surfaced to root-cause. Filed #2877 (parented to #2491, board status AI Batter Up) for a broader, different set of files (admin-observability.test.ts, admin-signal-rules-for-check-511.test.ts, msp-customers-bulk.test.ts, msp-suspended-banner.test.ts, portal-checkout.test.ts, +others less consistently) showing the same failure shape, outside this issue's named scope. `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
