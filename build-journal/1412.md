# Git #1412 — Adoption: remaining unconditional fixture fields not covered by #1387

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** `b45184f6a` (headline/trendCaption honest-null gate + test manifest update), `a9bfa5493` (IN FLIGHT bookend)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Full investigation pass on `portal-v2-adoption.tsx`, same
  methodology as #1387/#1409/#1411. Finding every `ADP_HERO.*` usage (eyebrow,
  headline, standfirst, trendLabel, trendCaption, parkedStripSuffix) and
  classifying each as legitimate static label vs. data value presented as real.
- 2026-08-27 ✅ DONE — Classified all 6 `ADP_HERO.*` usages:
  - **eyebrow** ("Where you are"), **standfirst** (generic philosophy prose,
    no numbers), **trendLabel** ("Adoption index · last 10 scans"),
    **parkedStripSuffix** ("plays parked as business decisions · nothing at
    risk", paired with the separately-fixture `ADP_PARKED_COUNT`): legitimate
    static labels — no tenant-specific numeric claims. Left unconditional,
    matching the same pattern already accepted platform-wide for equivalent
    fields (Licensing/Health `eyebrow`/`trendLabel`, Compliance's unconditional
    `trendLabel`).
  - **headline** ("You have climbed 27 points in ten scans. Six things would
    move the next ten.") and **trendCaption** ("61 → 88 since scan 1. No
    month has gone backwards."): confirmed DATA VALUES presented as real,
    rendering completely unconditionally regardless of `live.score` —
    exactly the class #1387 fixed for score/delta but missed here, because
    these are narrative sentences rather than numeric slots. Gated both on
    `hasScore` (the only live signal this page has — `useLivePillarHero`
    doesn't expose the pillar's real score history, so the specific numbers
    embedded in these sentences can't be live-wired the way score/delta
    were): `hasScore ? ADP_HERO.headline : NO_SCAN_DATA_LABEL` and same for
    `trendCaption`. Matches Compliance's established `trend ? CMP_HERO.trendCaption
    : NO_SCAN_DATA_LABEL` honest-null convention (#1409).
  - The trend chart's own SVG geometry (`adpTrendGeometry()` off fixture
    `ADP_HISTORY`) was deliberately left untouched: #1409 already confirmed
    Adoption has no `live.history` to plot at all (unlike
    Governance/Security/Compliance's `live.history ?? FIXTURE.history`
    pattern), so there is no live signal available to gate the chart shape
    on — only the two text claims were in scope/fixable here.
  - Updated `test-manifests/portal/portal-v2-pillars.json`: A-1's honest-null
    gate documented in its own name/comment, new A-1b step added asserting
    the trendCaption gate. `lastVerifiedAgainstCommit` bumped. The real
    testbed tenant is scored (`hasScore=true`, proven by A-3-src reading
    "live"), so both assertions exercise the LIVE branch (fixture text
    unchanged, no regression); the honest-empty branch is code-verified only
    — no unscanned fixture reachable in this harness, same caveat already
    established for the other five pillars' #1387 fix.
  - **Verification:** `tsc -p tsconfig.json --noEmit` clean on the touched
    file — 0 new errors. 2 pre-existing unrelated errors remain in untouched
    `SowScreen.tsx` / `PortalV2Shell.tsx` (same ones #1387's own bookend
    already noted as pre-existing).
