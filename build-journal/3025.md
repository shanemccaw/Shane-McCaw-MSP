# #3025 — check-stranded-branches.mjs hardcodes origin/main as base, misreports branches on a different real base

- **Status:** ✅ DONE 2026-09-12T22:26:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** `2c4b38b4a` on `main` (implementation + README note)

## Log
- 2026-09-12T22:10:02Z ⏳ IN FLIGHT — Read #3025's issue body + dispatch comment (only one comment,
  no supersession). Confirmed the real repro: `agent/2955-prodbase` / `agent/2960-prodbase-q1711`
  are local-only branches genuinely forked from `production-base-2026-09-05` (ahead-count 0 against
  it, 14/2 against `origin/main`). `production-base-2026-09-05` is a real, discoverable ref
  (`refs/heads/production-base-2026-09-05` + `origin/production-base-2026-09-05`) following an
  existing `<name>-base-<YYYY-MM-DD>` naming convention (see #2953/New Site v2 branch-split plan;
  also referenced as prior art by #3036's bookend, which manually excluded these two branches).
  Plan: `checkStrandedBranches()` discovers all refs matching that `<name>-base-<date>` convention
  as candidate alternate bases, then for each `agent/*` branch computes ahead-count against
  `origin/main` AND every candidate alt base, picking whichever base yields the lowest ahead-count
  (ties favor `origin/main`) as that branch's real resolved base — a per-branch fix, not a second
  hardcoded ref. Report output gains a `resolvedBaseUsed` field per entry so the choice is visible.
  No test harness exists yet for `scripts/dev-server/*.mjs` (no `*.test.mjs` convention there) —
  verifying live against the real repo state (the two known prodbase branches + a normal main-based
  branch) instead of adding a first, isolated test file for one script.
- 2026-09-12T22:26:00Z ✅ DONE — Implemented `listAlternateBaseRefs()` (discovers local+remote refs
  matching `/-base-\d{4}-\d{2}-\d{2}$/`) and per-branch base resolution in `checkStrandedBranches()`:
  for every `agent/*` branch, compute ahead-count against `origin/main` and every discovered
  alternate base ref, keep whichever gives the lowest ahead-count as `resolvedBaseUsed`, ties favor
  `origin/main`. Report gains `altBaseCandidates` (top-level) and `resolvedBaseUsed` (per-entry)
  fields; the non-JSON console printer surfaces the alt base used per stranded branch. **Live
  verification (real repo, no fixtures):** captured old-code output via a tagged
  `git stash push -u -m` of just this file (per the shared-stash-stack safety convention — captured
  its SHA via `git stash list`, restored with `git stash apply <sha>`, then dropped it), ran the
  pre-fix script, then restored my changes — `agent/2955-prodbase` showed `aheadCount: 14`, `agent/2960-prodbase-q1711`
  showed `aheadCount: 2`, both against `origin/main`. After the fix, both resolve
  `resolvedBaseUsed: "production-base-2026-09-05"` with `aheadCount: 0` and move from `stranded`
  into `clean` — matching the issue's exact repro. The other 10 genuinely stranded branches in the
  live sweep (real commits ahead of `origin/main`, no alternate base convention applies) are
  unaffected — confirmed unchanged before/after. Checked the one real consumer,
  `desktop/BuildConsole/Services/StrandedBranchService.cs`: it only reads `aheadCount` by name, so
  the new fields are additive and don't require a C# change. Updated
  `scripts/dev-server/README.md`'s one-line description for this script. `node --check` clean. No
  test file added — no `*.test.mjs` convention exists yet anywhere under `scripts/dev-server/`, and
  this was verified live against real branches instead. No fixture/hardcoded data — every number in
  this log came from live `git`/the script's own `--json` output. Commit `2c4b38b4a` on `main`
  (rebased onto `origin/main` first; `verify-branch-merged.mjs` confirms ancestor). Working tree
  clean at completion (`git status --porcelain` empty; the two scratch `sb.json`/`sb_old.json` files
  created during before/after verification were deleted, and the verification stash entry was
  applied back then dropped — nothing left on the shared stash stack). No new
  finding filed — this issue's own fix is the complete, correct resolution; no additional gap
  surfaced during the build.
