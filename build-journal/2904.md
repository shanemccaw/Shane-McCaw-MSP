# #2904 — Feature: pay-period due-date tracking (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T20:30:00Z
- **Scope:** platform (personal — ShanesSurvival.App, tracked under Milestone 19)
- **Started:** 2026-09-04
- **Commit(s):** 3919ebbfb (feature commit), merged into `main` as 32c65e2e3

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — migrations/005_bill_due_days.sql (accounts.due_day),
  new MCP tools set_bill_due_day / pay_period_due_status, additive only per #2904's scope.
- 2026-09-04T20:30:00Z ✅ DONE — Real migration-number collision found before writing any
  code: `origin/main` had already landed `005_income_tracking.sql` (#2905) and
  `006_emergency_fund_role.sql` (#2907) since this session started; rebased onto current
  `origin/main` and renamed the new file to `migrations/007_bill_due_days.sql`
  (`accounts.due_day INTEGER`, `CHECK (due_day IS NULL OR due_day BETWEEN 1 AND 31)`) — ran
  it myself against local Postgres (`finances` DB) and recorded it in `schema_migrations`.
  Added `AccountRow.DueDay` + `AccountRepository.UpdateDueDayAsync`; new
  `PayPeriodDueService` (Core/Dashboard) computing month-wraparound-aware due windows; two
  new MCP tools on `FinanceTools`: `set_bill_due_day(accountName, dueDay)` (same
  Bill-role-only validation as #2902's `set_bill_target`) and
  `pay_period_due_status(nextPayDate)` (bills due in `[today, nextPayDate)`, real sum,
  Warnings for bill accounts missing `due_day` or `target_amount`). Registered
  `PayPeriodDueService` in `ShanesSurvival.Mcp/Program.cs`. `dotnet build ShanesSurvival.sln`
  clean, 0 warnings/errors. Real end-to-end verification: drove the built
  `ShanesSurvival.Mcp` server over real stdio JSON-RPC (initialize + tools/call), set
  `due_day` on two real accounts (H1 Mortgage=5, H2 Rent=1), confirmed
  `pay_period_due_status` correctly includes/excludes bills by window and correctly
  wraps H2 Rent's day-1 due date into the *next* month when the window crossed a month
  boundary — then reverted both test `due_day` values back to NULL (no real due-day source
  data was available in this session, so no fabricated values were left on Shane's real
  accounts) and deleted the scratch `mcp-smoke-test.mjs` harness. Merged
  `agent/2904-q1655` into `main` as 32c65e2e3 (auto-merged cleanly against concurrent #2903
  debt-tracking work that had landed on `origin/main` meanwhile, no conflicts);
  `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
  No findings to file this session. Note: `C:\Source\ShanesSurvival` (the path #2880's
  milestone description names as canonical) is a stale, out-of-date clone — the real,
  actively-developed copy with current git history is `desktop/ShanesSurvival` in this
  monorepo; worked there, consistent with #2892/#2902/#2905/#2907's own commits.
