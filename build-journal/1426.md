# #1426 — Governance: Oversharing stat band, Top Risks band, Anonymous Links panel — replace unconditional fixture with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** c11e60b90

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting: follow-up from #1413 audit. Three leaks on
  portal-v2-gov-oversharing.tsx (Stat cards band, Top Risks band, Anonymous Links
  panel) all render unconditionally with zero dataState gating. Plan: replace with
  honest no-data states per Shane's direction; wire the Anonymous Links panel only
  if a clean schema mapping exists, otherwise honest no-data too (per issue body,
  do not guess at the missing expiry/active field mapping).
- 2026-08-27 ✅ DONE — c11e60b90. Confirmed via fresh code search (not just
  #1413's audit) that no portal-facing endpoint exists for any of the 3 leaks:
  the one real tenant sharing-capability read (getTenantSharingCapability)
  lives in monitor-executor.ts's scan-time drift machinery, not behind a
  customer route. All three now render the shared `NoScanDataState` component
  (Git #1339) instead of fixture content: Stat cards band and Top Risks band
  unconditionally (no live counterpart anywhere, so no toggle/loading state
  makes sense -- Top Risks is no longer a collapsible toggle since there is
  nothing behind it to reveal); Anonymous Links panel too, per the issue's
  explicit instruction not to guess at the missing expiry/active field mapping
  even though a live endpoint (useOversharingItemsLive, #1275) exists. The
  Sharing Capability stat card's wrench-to-fix-panel gate was removed along
  with the stat cards it lived on (no live fixKey left to gate on) -- FixPanel/
  useFixPanel are now unused on this page and were removed. The Affected Sites
  panel and its accept-risk/runbook flow are UNCHANGED (already clean per
  #1413's audit -- real useOversharingSitesLive feed with a proper
  loading/live/fixture dataState). No new logging channel wired: this is a
  pure frontend render-gap fix with no new backend route/subsystem, and the
  established precedent for this exact honest-no-data pattern elsewhere
  (portal-v2-ownership.tsx Git #1342, portal-v2-change-control.tsx, portal-v2-
  projects.tsx) carries no logger/reportClientEvent call either. Updated
  test-manifests/portal/portal-v2-pillars.json in place (discover-before-create
  -- OV2/OV3/OV21 rewritten to assert the honest strings, OV4 and OV17-OV20
  removed as dead selectors, sessionTag 'oversharing-1426' on the touched
  steps) and bumped its lastVerifiedAgainstCommit to c11e60b90. Verification:
  `tsc --noEmit` clean on the touched file (2 pre-existing unrelated errors
  elsewhere, in files this session never touched); 17/17 green on
  govOversharingData.test.ts (the fixture module itself is untouched --
  still pinned, still tested, kept for a future backend wiring pass per its
  own header). NOT run this session: shaneapp://runTest against the deployed
  dev server -- the shane-msp MCP server that couriers that protocol call
  failed to connect this session (CONNECTION_CLOSED, confirmed via a real
  tool-lookup attempt, not assumed), so the new OV2/OV3/OV21 assertions are
  unconfirmed against a live rendered DOM. Whoever next has a working
  shane-msp connection should run `onlyTag=oversharing-1426` against this
  manifest before trusting it fully.
