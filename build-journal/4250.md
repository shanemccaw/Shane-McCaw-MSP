# #4250 — CLAUDE.md artifacts/msp-portal → artifacts/portal + public SOW test-manifest coverage

- **Status:** ✅ DONE 2026-09-15T14:24:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** bad7d9e4b (rebased to ad9429037 on origin/main)

## Log
- 2026-09-15T14:17:40Z ⏳ IN FLIGHT — replacing all 4 `artifacts/msp-portal` occurrences in CLAUDE.md with `artifacts/portal` (confirmed real dir via `ls artifacts/`), then adding the missing test-manifest coverage for `artifacts/portal/src/pages/msp-sow-public.tsx` (public, unauthenticated SOW viewer/signer) now that the Test Coverage scope line correctly names `artifacts/portal/`.
- 2026-09-15T14:24:00Z ✅ DONE — replaced all 4 `artifacts/msp-portal` occurrences in CLAUDE.md (selective-service-targeting table, Definition-of-done grep command, Test Coverage scope line, and the "run what you write" phase-touching line) with `artifacts/portal`. Added `test-manifests/portal/public-sow-viewer-sign.json` covering the real GET/POST `/api/public/sows/:shareToken` contract (404 unknown token, 410 expired link, 400 malformed sign payload -- read directly from `artifacts/api-server/src/routes/msp-sow.ts`) plus a UI walk of the honest not-found state on `/sow/:shareToken`; registered in `test-manifests/_regression-suite.json`. Attempted `shaneapp://runTest` -- returned `manifest not found or unparseable` because the manifest exists only in this worktree, not yet merged into the dev-server checkout (the same documented Git #1371/#1372 gap `portal/offers-and-sow-acceptance.json`'s own notes describe); verified instead by direct reading of the route file for exact status/body pairs and `JSON.parse` validation of both manifest files. Rebased onto origin/main and pushed (bad7d9e4b -> ad9429037). `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
