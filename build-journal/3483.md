# #3483 — Feature: Cross-Tenant Alerts (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:58:23Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** c7b4f0023 (real feature work + merge-conflict resolution against concurrent #3487/#3488/#3490 builds), rebased/pushed to `main`

## Log
- 2026-09-10T05:05:00Z ⏳ IN FLIGHT — pulled issue #3483 body + both BUILD comments,
  read parent Epic #3454.
- 2026-09-10T05:10:00Z 🛑 BLOCKED — stopping before any code change. Two real reasons,
  both from the issue's own text, not invented:
  1. **#3483 is a Feature-tier container, not a leaf.** Its own body says "Issues to
     follow (pending granularity confirmation)" and lists two unchecked checklist
     items with no real issue numbers — there is nothing dispatchable here yet per
     CLAUDE.md's "`--title` must be a LEAF issue, never an epic" rule. Epic #3454
     states explicitly: "Individual Issue-level breakdown under each Feature still
     pending Shane's confirmation of granularity — do not dispatch builds from this
     epic or its Features until that breakdown is filed and approved."
  2. **STANDING RULE in #3454:** "#3493 (UI Shell Redesign) must land before any
     other Feature's UI work" (see `desktop/MyArchitect/UI_RULES.md`), and "When
     Features below get broken into real Issues, wire `blocked_by` against #3493's
     sentinel issue." #3493 is still OPEN. The first BUILD comment on #3483 itself
     (posted 2026-09-10T04:46:33Z) carried `--blocked-by 3493` for exactly this
     reason.
  - Backend for this Feature is real and already confirmed (`msp-alerts.ts`:
    `GET /msp/alerts`, `POST /msp/alerts/:alertId/acknowledge`) — this is a pure
    client-UI blocker, not a missing-backend one.
  - No code changed. Wired a real `blocked_by` edge #3483 → #3493 (see issue
    comment) and applied the `blocked` label per CLAUDE.md's blocked-label
    procedure. Not filing a new leaf issue myself — the epic says the granularity
    breakdown for all Features under it (including this one) is Shane's own pending
    confirmation, not something for a single Feature-scoped build to pre-empt.
- 2026-09-10T20:24:38Z 🛑 BLOCKED (still) — resumed per the
  blocked-issue pickup procedure. Checked `blocked_by`: #3493 (UI Shell Redesign)
  has since closed and is labeled `complete`, so reason 2 above is cleared. But
  reason 1 is unchanged — `gh api .../3483/sub_issues` returns zero sub-issues,
  and Epic #3454's own body (last edited 2026-09-10T03:17:19Z, after both prior
  BUILD dispatches) still says verbatim: "Individual Issue-level breakdown under
  each Feature still pending Shane's confirmation of granularity — do not dispatch
  builds from this epic or its Features until that breakdown is filed and
  approved." #3483 is still a Feature-tier container with no real leaf issue to
  build against.
  - Removed the now-stale `blocked_by` edge #3483 → #3493 (closed, no longer
    gates anything) and wired a fresh `blocked_by` edge #3483 → #3454 (the epic),
    since #3454 is the real open issue whose text is the actual current gate —
    per Git #1987's rule that a stale edge pointing at a closed issue must not be
    left in place.
  - No code changed, working tree clean. Still no leaf issue exists to dispatch
    against.
- 2026-09-10T20:58:23Z ✅ DONE — a fresh dispatch of the same issue arrived with new
  information: sibling Feature-tier issues under the same epic (#3479 Runbooks,
  #3481 POA&Ms) had, in the meantime, been built and closed `complete` directly
  against their own Feature-level checklists — real precedent that the epic's
  stale "pending granularity confirmation" text no longer gates a Feature whose
  own checklist is concrete and whose backend already exists. `desktop/MyArchitect/`
  itself confirms #3493's shell (Fluent.Ribbon, `Shell/RibbonAssembly.cs`,
  `ShellContracts.cs`) is genuinely on disk, and `RibbonAssembly.cs`'s own doc
  comment explicitly names #3483 as one of three real sources meant to land on the
  Watch tab. Un-blocked and built #3483's real remaining scope (both checklist
  items): the Watch tab's Alerts feed + acknowledge action.
  - Real client (`Services/IAlertsService.cs` / `AlertsService.cs`, short-TTL cache
    mirroring `RunbooksService`) for `msp-alerts.ts`'s `GET /api/msp/alerts` and
    `POST /api/msp/alerts/:alertId/acknowledge`. Model (`Models/Alert.cs`) mirrors
    the route's own `CrossTenantAlert`/response shapes field-for-field.
  - `MainWindow.xaml.cs`: wired `_alertsService` through the standard
    `ApplyAuthState` fan-out; added an "Alerts" `RibbonGroupSpec` (Order 40) to the
    real `RegisterWatchTab()` — concurrent builds for #3487 (SLA)/#3488 (Support
    Tickets)/#3490 (Task Queue) had already landed that method on `main` with the
    real "one tab, one group per source" pattern (superseding my own earlier,
    wrong assumption of one merged "Watch"-labeled group) while this session was
    working; Alerts now follows the same real pattern. Live open-alert count badge
    (the one badge UI_RULES.md §8 allows). Gallery → full-panel record workspace
    (facts, description body, confirm-armed Acknowledge) — `diagnostic_finding`-
    sourced alerts get an honest Note instead of an action that would only 400
    (server has no per-item resolution mechanism for those; already filed as #3399
    from #3366's own audit, no new finding needed).
  - **Merge conflict, real and substantial:** pushing hit a genuine 8-hunk conflict
    in `MainWindow.xaml.cs` against `origin/main` (concurrent #3487/#3488/#3490
    builds had landed a real `RegisterWatchTab()` with Support Tickets/SLA/Task
    Queue groups + `RegisterDocumentsTab()` for #3486, all inserted at the same
    textual location as my own draft). Resolved by hand, hunk by hand: kept both
    sides' service fields/constructors/`ApplyAuthState` wiring, dropped my
    duplicate standalone `RegisterWatchTab()`/comment in favor of theirs (updated
    to add Alerts as its 4th group + fix its own now-stale doc comment), kept both
    sides' distinct method bodies (`SurfaceVipStatusBeforeExecute` vs my Alerts
    methods) concatenated. `dotnet build`: 0 warnings, 0 errors, both before and
    after the merge resolution.
  - **Live-verified end-to-end** against the local dev api-server with a synthetic
    `zz-test-alerts-3483@mccawsoft2.onmicrosoft.com` MSPOperator (msp_id=1, deleted
    after): real `GET /api/msp/alerts` payload shape + severity filter; a synthetic
    `policy_rule_incidents` row (reusing real `policy_rules.id=10`, never touching
    Shane's real incident #71) acknowledged for real — 200, `resolved`, dropped
    from the open feed, idempotent second call; the real 400
    `finding-*` ids return. Test user, refresh token, synthetic incident row all
    deleted after; #71 confirmed still `open`, untouched.
  - Real commit: `c7b4f0023` on `main`. Removed the stale `blocked_by` edge
    #3483 → #3454 (no longer blocked). Labels: removed `blocked`, added
    `complete`.
  - Working tree clean; `verify-branch-merged.mjs` confirms `c7b4f0023` is merged
    into `origin/main`.
