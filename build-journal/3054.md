# #3054 — Dev-server checkout stuck 93+ commits behind origin/main

- **Status:** ✅ DONE 2026-09-06T21:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** bb04fa949 (IN FLIGHT bookend), 7a1acfefc (DONE bookend, rebased onto main)

## Log
- 2026-09-06T21:14:18Z ⏳ IN FLIGHT — investigating; confirmed two stale checkouts (mainRepoRoot `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` 184 behind, and the real merge-target `C:\dev-server` branch `dev-server` 431 behind); about to resync both under the coordinator mutex.
- 2026-09-06T21:20:00Z ✅ DONE — Real fix applied, no code change needed (this was an operational-state issue, not a bug in `request-restart.mjs`/`coordinator.mjs`).
  - `config.mjs` resolves TWO checkouts: `mainRepoRoot` (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`, branch `main` — what BuildConsole's dev servers actually run from) and `serverWorktree` (`C:\dev-server`, branch `dev-server` — the actual merge target `request-restart.mjs`/`coordinator.mjs` merge agent commits into). The issue's evidence (add/add conflict on `build-journal/2830.md`) traces to the latter: `C:\dev-server` was 431 commits behind `origin/main`, carrying 145 of its own scratch merge/bookend commits accumulated since its last reconciliation — exactly the documented-but-never-automated maintenance gap in `scripts/dev-server/README.md` ("`dev-server` ↔ `origin/main` reconciliation is not automated ... reset it to a fresh base").
  - **mainRepoRoot**: `git fetch origin main && git merge --ff-only origin/main` — clean fast-forward, `ff894df83` → `b6b929844` (184 commits). A pre-existing dirty `pnpm-lock.yaml` (local pnpm-store churn, not mine, `git diff HEAD origin/main -- pnpm-lock.yaml` was empty so it didn't block the ff) was left untouched.
  - **`C:\dev-server`**: acquired the coordinator mutex (`lock.mjs tryAcquire`, same mutex `runCycle`/build sets use) so no concurrent agent merge could race the resync. Verified all 145 commits unique to `dev-server` vs `origin/main` were themselves dev-server-authored merge/resolve/bookend commits (no real unmerged feature work), then `git reset --hard origin/main` under the held lock, then released. `dev-server` HEAD: `145ba36ad` → `b6b929844` (matches `mainRepoRoot`/`origin/main` exactly).
  - **Verification**: both checkouts now report `0` commits behind `origin/main` (aside from this session's own bookend commit); coordinator queue/lock dirs empty (no stuck pending requests); `node scripts/dev-server/verify-branch-merged.mjs` confirms this branch is an ancestor of `main`; working tree clean.
  - #2992's actual manifest run (`test-manifests/portal/notification-preferences.json`) is still someone else's follow-up — this build only unblocks the merge-back path per #3054's own scope, it doesn't re-run #2992's verification itself.
  - No finding filed: this is a one-time manual catch-up of already-documented, already-anticipated drift (README explicitly calls this "a deliberate manual step, like promoting Dev→Staging"), not a bug needing a new issue.
