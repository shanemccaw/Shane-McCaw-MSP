# #2288 — Gap: no slot-level cancel, requeue, or reassign yet

- **Status:** ✅ DONE 2026-09-02T21:40:00Z
- **Scope:** buildconsole (ShaneBuilder, `desktop/ShaneBuilder/`)
- **Started:** 2026-09-02
- **Commit(s):** 5947d3ec8 (rebased onto main as fd3afc10b)

## Log
- 2026-09-02T20:56:00Z ⏳ IN FLIGHT — Feature #2281 "Build Matrix" item 7/7. The drawer (items
  1-6, `MainWindow.xaml.cs`'s `RenderMatrixDrawer`/`MatrixSlotCard`/`_matrixSlotAssignments`) is
  real and already on `main` (#2286/#2287). This issue is the drawer's own stated gap: a busy
  slot can only be clicked to focus the build in the queue — no cancel, no requeue, no reassign.
  `QueueReadClient` is explicitly read-only by contract ("every write to bt_build_queue stays
  owned by BuildConsole's own BuildQueuePostgresClient"); confirmed via `\d bt_build_queue`
  against local Postgres that `build_pid`/`build_pid_started_at` (#1839's real fingerprint
  columns) are already there — real, buildable mechanism to genuinely stop a running process
  from a second app, not just flip a DB status while the process orphans.
- 2026-09-02T21:40:00Z ✅ DONE — Added `Services/QueueWriteClient.cs` (the one deliberate,
  narrow exception to `QueueReadClient`'s read-only contract) with three real mutations
  against `bt_build_queue`, matching BuildConsole's own `BuildQueuePostgresClient`
  status/column semantics exactly:
  - `CancelRunningAsync` — kills the real OS process via the #1839 `build_pid`/
    `build_pid_started_at` fingerprint (open by pid, verify creation-time match within
    2s, kill the tree only on a match — never a stranger process from pid reuse), then
    marks the row `status='failed'`, `exit_code=-1` (same as BuildConsole's own Stop).
  - `RequeueAsync` — stop-if-live, then resets the SAME row back to `'queued'`
    (`claimed_at`/`completed_at`/`exit_code`/`session_id`/pid columns cleared) instead
    of inserting a second row — mirrors `QueueBuildAsync`'s own row-reuse branch.
  - `ReassignAsync` — same as Requeue, plus sets `model` to a chosen id from the real
    Claude 5 family (Sonnet 5 / Opus 5 / Haiku 4.5).
  Wired three icon buttons onto a busy Matrix slot card (`MatrixSlotActionRow` in
  `MainWindow.xaml.cs`), a model-choice context menu for Reassign, and
  `RunMatrixSlotActionAsync` tying them to the write client with toast feedback and a
  real re-read (`LoadQueueFromDatabaseAsync`) afterward. No migration needed — `\d
  bt_build_queue` against local Postgres confirmed `build_pid`/`build_pid_started_at`
  already exist (#1839).
  **Verification:** `dotnet build ShaneBuilder.csproj` — 0 warnings, 0 errors. **Not**
  live-fired against a real running row: every `status='running'` row in local Postgres
  right now belongs to another concurrent agent session's real, in-flight build
  (including this build's own queue row, id 1253) — killing one to test would be a real
  destructive action against someone else's work, not a safe test, so this is honestly
  code-reviewed + build-verified rather than live-executed. `git status --porcelain`
  clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this line. Commit
  `5947d3ec8` (rebased onto main as `fd3afc10b`, pushed).
  No findings filed — this build didn't surface a genuine gap outside its own scope.
