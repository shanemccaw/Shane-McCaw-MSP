# #1619 — Runbooks: wire the hold-window audit trail

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** c3b58d037 (bookend IN FLIGHT), a6dfc6e28 (real work, post-push/post-rebase hash on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — `GET /api/portal/hold-windows/:holdId/events` is live
  and fully built (`portal-runbooks.ts:849-892`) but has zero page consumers.
  `Design/portal/` carries no export for the Runbooks page yet (no README, no
  `.dc.html`), and `artifacts/portal/src/components/holds/useRunbooks.ts` (the
  page's data seam, carried over from retired `artifacts/msp-portal` in
  `f40438cdc`) has zero page consumers itself — the page hasn't been wired at
  all yet, per module order. Per the SCOPE STOP, this build ends at the wire
  contract: extending `useRunbooks.ts` with a `loadHoldEvents(holdId)` fetcher
  and the `HoldWindowEvent` type so the endpoint is reachable through the same
  data seam every other hold-window action already uses, without creating a
  page/route/component ahead of Design. #1620 records the identical gap from
  the SOPs side; same endpoint, no separate work needed there.
- 2026-08-30 ✅ DONE — Added `HoldWindowEvent` and `loadHoldEvents(holdId)` to
  `artifacts/portal/src/components/holds/useRunbooks.ts`, wrapping
  `GET /api/portal/hold-windows/:holdId/events` on demand (not part of the
  periodic `/runbooks` payload), returning `readonly HoldWindowEvent[] | null`
  (`null` distinct from an empty history). No backend change — the route
  already existed and worked. Updated `docs/runbooks-contract-pack.md` (§0,
  §1.8, §6) to reflect the hook now wraps the endpoint while it remains
  orphaned from a page's perspective (`Design/portal/` has no export for
  Active Runbooks yet — no page or route was created, per the SCOPE STOP), and
  corrected its stale `artifacts/msp-portal` path references for this module.
  `psql "$DATABASE_URL"` confirms the real local state honestly: 0 rows in
  `portal_hold_windows` and 0 in `portal_hold_window_events` — no live data to
  demo the endpoint's actual payload against, and no page exists to write a
  test manifest for, so none was written. `tsc -p artifacts/portal/tsconfig.json
  --noEmit` is clean, checked both before and after rebase onto `origin/main`.
  `verify-branch-merged.mjs` confirms `a6dfc6e28` merged into `origin/main`.
  Working tree clean (`git status --porcelain` empty).
  **Finding filed:** #1921 (parented to #1485, board status "AI Batter Up") —
  17 contract-pack docs (plus 6 other `docs/*.md` files) still cite the
  retired `artifacts/msp-portal` path after the `f40438cdc` rename to
  `artifacts/portal`; only this module's own doc was corrected here.
