# #3012 — Dispatch-time validation: fail loud early on a BUILD: header the queue cannot satisfy

- **Status:** ⏳ IN FLIGHT 2026-09-10T22:34:36Z
- **Scope:** buildconsole
- **Log:** Starting. Pulled issue #3012 body: parent #1788, follow-up from #1990's retraction ("dispatch-time
  validation is still worth having, but scoped honestly: fail loudly and early on a BUILD: header the queue
  cannot satisfy"). #1990 deliberately did not build this and explicitly retracted a model allow-list.
  Plan: add `BuildHeaderValidation.Validate(model, effort, cwd)` and call it from
  `BuildQueuePostgresClient.QueueBuildAsync` (the single choke point every dispatch path — Batter Up
  auto-queue, chat replies, Build Queue panel, EditBuildPromptDialog — already funnels through before a
  row can be claimed as 'queued'). On failure, force the row to the existing 'parked' status (never
  claimed by the watcher's `WHERE status = 'queued'`) with a clear ActivityLog reason, instead of letting
  it queue and the launched session discover the problem itself. Scope: a `--cwd` that doesn't exist, and
  a model/effort value that is structurally a stray/malformed flag token (e.g. still carrying a leading
  `--`, the tell of two flags run together without a space) — no model allow-list.
