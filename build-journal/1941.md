# #1941 — Remediation: raise a Change Request from a checklist item (backend create path)

- **Status:** ✅ DONE 2026-09-02T03:45:00Z
- **Scope:** platform
- **Started:** 2026-09-02
- **Commit(s):** b0055d44d (feature), 808159a48 (merge to main)

## Log
- 2026-09-02T02:10:00Z ⏳ IN FLIGHT — Investigated #1541 (CR gate) and #1538 (checklist). Confirmed
  POST /api/portal/change-control already accepts an optional `remediationCheckKey` but nothing
  calls it with one. Building a structured backend function/route that raises a real CR FROM a
  checklist item (derives title/target/post from the finding, sets `remediationCheckKey`), reusing
  the wizard's existing create logic rather than duplicating it.
- 2026-09-02T03:45:00Z ✅ DONE — Extracted the wizard's CR-insert/freeze-gate/approval/risk-discharge
  logic into `lib/portal-change-control-raise.ts` (`raiseChangeRequest`, `RaiseChangeRequestError`),
  reused unchanged by `routes/portal-change-control.ts`'s existing wizard POST. Added
  `lib/remediation-raise-change.ts` (`buildRaiseChangeRequestInputForChecklistItem`, a pure
  derivation from a checklist item — critical→Normal / warning→Standard change class, real
  finding content as the proposed payload, no fabricated impacted-user count) and
  `workloadForCheckKey` in `lib/portal-change-control.ts` (checkKey-prefix → workload, since
  `deriveWorkload`'s cmdlet-pattern regex doesn't apply to a checkKey). New route:
  `POST /api/portal/remediation/checklist/:checkKey/raise-change` in
  `routes/portal-remediation-checklist.ts`, gated on the checklist item genuinely being an open
  finding for this tenant (404 otherwise) — raises a real CR with `remediationCheckKey` set, the
  row #1541's `evaluateRevealAuthorization` gate has had none of since it shipped.
  No schema change — `remediation_check_key` already existed from #1541's own migration.
  Tests added: `portal-change-control.test.ts` (`workloadForCheckKey`, pure),
  `remediation-raise-change.test.ts` (pure derivation, new file) — both unit-level, no DB.
  **Verification gap, honestly disclosed:** the main checkout's `node_modules` hoisted layer is
  broken repo-wide (see #2192, filed this session) — `vitest`, `express`, `drizzle-orm`, `zod`
  are all unresolvable, so vitest could not actually be executed and a real `tsc --noEmit` bails
  immediately on a missing `@types/node`. Self-verified instead via: (1) a local, uncommitted
  `--typeRoots` override pointed at a real `@types/node` copy in the pnpm store, confirming the
  ONLY errors touching my new/changed files are the same repo-wide missing-module cascade every
  other file shows (no errors attributable to my code); (2) careful manual side-by-side review —
  `raiseChangeRequest` is a faithful field-for-field extraction of the wizard route's own
  previously-working insert logic; (3) real DB check confirming `tenants` id=1 (testbed) has real
  critical/warning `msp_diagnostic_findings` rows to exercise this against once tooling is fixed.
  Branch merged clean into `origin/main` (verified via `verify-branch-merged.mjs`), working tree
  clean. Filed #2192 (parented to #1202) for the broken node_modules — a real, repo-wide blocker
  affecting every session's ability to run tsc/vitest right now, not caused by this build.
