# #2211 — Feature: Markdown File Viewer + Drag/Drop (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T08:05:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 38f4cdafa (Colors.xaml conflict-marker fix), 444ef6a01 (bookend), e0139649e (feature)

## Log
- 2026-09-02T07:30:00Z ⏳ IN FLIGHT — audited desktop/BuildConsole/Services/MarkdownRenderer.cs and desktop/ShaneBuilder structure (MainWindow.xaml/.cs, Themes/Colors.xaml). Found and fixed a pre-existing unresolved git-conflict-marker bug in Colors.xaml blocking compilation (commit 38f4cdafa, filed as #2212, parented under #1202). Starting the actual port: MarkdownRenderer.cs into ShaneBuilder/Services, OS file drag-drop (.md filter), open dropped files rendered as tabs.
- 2026-09-02T08:05:00Z ✅ DONE — Ported MarkdownRenderer.cs verbatim into desktop/ShaneBuilder/Services (namespace + BrushKeyMap remap onto ShaneBuilder's real Brush.* tokens only; rendering logic unchanged). Found and fixed a second pre-existing unresolved-conflict block in MainWindow.xaml.cs's TabDef class (same merge bcd659c07, also filed under #2212) while wiring the new tab kind through it. Added real OS file drag-and-drop (DataFormats.FileDrop) on DocumentHostGrid, filtered to .md — non-.md drop is a clean toast no-op via ToastEngine. Each dropped .md file opens as its own TabKind.File tab (existing path re-selects instead of duplicating), rendered through MarkdownRenderer into the new MarkdownViewerDock, not raw text; multiple files dropped at once each get their own tab, last one opened is selected. `dotnet build -c Debug` succeeds clean (0 warnings, 0 errors) on desktop/ShaneBuilder.csproj. verify-branch-merged.mjs confirms e0139649e is an ancestor of origin/main. Working tree clean. Findings filed: #2212 (unresolved conflict markers on main).
