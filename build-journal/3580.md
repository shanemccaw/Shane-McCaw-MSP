# #3580 — shanes-git: add an optional repo parameter to every tool, defaulting to Shane-McCaw-MSP

- **Status:** ✅ DONE 2026-09-11T00:50:59Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 6c97fcd28

## Log
- 2026-09-11T00:49:06Z ⏳ IN FLIGHT — threading an optional `repo` param ("owner/repo") through
  every real repo-scoped tool in `artifacts/github-mcp-server`, defaulting to
  `GITHUB_MCP_REPO`/`shanemccaw/Shane-McCaw-MSP` for full backward compatibility. Sub-issue of
  #3578 (Feature: Multi-Repo Support).
- 2026-09-11T00:50:59Z ✅ DONE — Added `resolveRepo()`/`parseOwnerRepo()` to `env.ts`; threaded an
  optional `repo?: {owner,repo}` override through every `github.ts` function that hits a specific
  repo (`repoPath`, `postIssueComment`, `listIssueComments`, `closeIssue`, `getIssueSummary`,
  `listSubIssues`, `addSubIssue`, `removeSubIssue`, `listBlockedBy`, `addBlockedBy`,
  `removeBlockedBy`); added the shared `REPO_SCHEMA_PROPERTY` to `tools/args.ts`; wired an
  optional `repo` input-schema property + `resolveRepo(args.repo)` call into every tool that
  actually addresses a specific repo — `create_issue`, `get_issue`, `update_issue`,
  `search_issues`, `close_issue`, `post_comment`, `list_comments`, `add_sub_issue`,
  `remove_sub_issue`, `list_sub_issues`, `set_blocked_by`, `list_blocked_by`, `move_to_status`,
  `get_board_status`. Replaced `move_to_status`/`get_board_status`'s previously-hardcoded
  `OWNER`/`REPO` constants with `resolveRepo(args.repo)` — `PROJECT_V2_ID` stays a single shared
  constant per the issue's own design (one shared board). `list_board_column`, `server_status`,
  `github_whoami`, `get_recent_activity` deliberately got no `repo` param — none of them do a
  per-repo issue lookup, so a `repo` arg would be a fake no-op. Updated
  `artifacts/github-mcp-server/README.md` and
  `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md`'s tool docs.
  **Verification:** `pnpm run typecheck` in `artifacts/github-mcp-server` is clean. Real live
  calls against the actual `shanemccaw/Shane-McCaw-MSP` PAT: `get_issue({ number: 3580 })` with
  no `repo` returned Shane-McCaw-MSP#3580 unchanged (backward compat confirmed);
  `get_issue({ number: 1, repo: "octocat/Hello-World" })` returned that genuinely different
  public repo's real issue #1 (cross-repo resolution genuinely works — the classic PAT isn't
  restricted to one repo); `get_issue({ number: 1, repo: "not-a-valid-repo-string" })` was
  rejected pre-flight with `repo must be "owner/repo", got: ...`, before any GitHub call fired.
  Branch verified merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs`
  → OK). Working tree clean at completion (`git status --porcelain` empty). No findings filed —
  no new bug/gap discovered outside this issue's own scope.
