# #2998 — Billing: plan state from live subscription endpoints (v2)

- **Status:** ✅ DONE 2026-09-06T18:59:30Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** f8b932495 (bookend IN FLIGHT), 2f37bf1e0 (real work)

## Log
- 2026-09-06T18:52:36Z ⏳ IN FLIGHT — Real Design export (`Design/portal/design_handoff_full_site/screens/Billing.dc.html`) has landed. Building `artifacts/portal/src/pages/billing.tsx` from it, wiring the already-built `billingLive.ts` (receipts + Stripe portal). Note: the design's own logic (`renderVals`, ledger item "§1.6") explicitly documents "No live plan state ... no tenant holds a monthly monitoring subscription today, so there is nothing real to show as your current plan" — the design itself supersedes the issue body's original ask to surface `subscriptionsLive.ts`'s plan-state UI on this page; building strictly to the landed design per CLAUDE.md ("later design may supersede" / ".dc.html is the spec").
- 2026-09-06T18:59:30Z ✅ DONE — Built `artifacts/portal/src/pages/billing.tsx` (new page, `/billing` route in `App.tsx`, real link from `UserMenu.tsx`'s "Billing" row, replacing its old `/coming-soon` placeholder) from the landed `Billing.dc.html` design. Wired real: Receipts (`GET /api/portal/invoices` via `billingLive.ts`, Git #1237) with honest loading/live-empty/live-populated/fixture-failed states, and "Manage payment in Stripe" (`POST /api/portal/billing/customer-portal`). Extended `billingLive.ts` with a `refetch()` (design's "Try again" action, same `attempt`-counter pattern as `useAccountSecurityLive`) and `billingWire.ts` with a real `paid` boolean derived from `invoicesTable.status` (was previously extracted but not mapped through to the row — the Paid/Pending status pill needed it). Monitoring-plan tier cards render the design's own explicitly-labeled illustration ("Illustrative, not your account") — static, not tenant data, matching the contract pack's DECIDED-not-yet-wireable status (#1594, blocked on #1128). The "what this page deliberately does not do" ledger section is reproduced verbatim from the design, including its own documented reason for NOT wiring `subscriptionsLive.ts`/`GET /portal/billing/subscriptions` (Git #1611, real & built) here: no tenant holds a monthly monitoring subscription today, so there is nothing real to show as a current plan — that endpoint stays real, current, and deliberately unwired on this page, matching the landed design as shipped rather than the issue body's original (now-superseded) ask.
  - Verification: real `tsc -p tsconfig.json --noEmit` (0 errors) and real `PORT=5199 vite build` (succeeds) against `artifacts/portal` with this page + route added.
  - `shaneapp://runTest` was not available as a callable tool in this session (checked via ToolSearch, not found) — a real attempt, not skipped by default; test-manifest run deferred per the same structural note `account-security-data-rights.json`/`customer-home-overview.json` already carry (no portal login page yet, and this branch isn't deployed to the dev-server checkout yet).
  - Test manifest: `test-manifests/portal/billing.json` (new), registered in `_regression-suite.json`.
  - Finding filed: **#3056** — `test-manifests/portal/portal-v2-billing.json` targets the retired `portal-v2` route (dead selectors/route since `f40438cdc`); parented to #1598 (Feature: Billing (Portal)), labeled `bug`, board status set to "AI Batter Up".
  - `git status --porcelain` clean; branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
