# #3641 — @workspace/api-zod: 202 extensionless relative imports break Node's ESM loader

- **Status:** ✅ DONE 2026-09-13T00:02:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 2a4fba9f2 (rebased/pushed to `main` as 726df769e)

## Log
- 2026-09-12T23:24:39Z ⏳ IN FLIGHT — confirmed 202 rewrites needed via `node artifacts/api-server/scripts/fix-relative-import-extensions.mjs --check ../../lib/api-zod/src` (matches issue body exactly: 141 scanned, 45 files changed, 0 case mismatches, 0 unresolved). Proceeding with issue's option 2: apply codemod, add `allowImportingTsExtensions` to `lib/api-zod/tsconfig.json`, and wire the codemod as a post-generation step in `lib/api-spec`'s `codegen` script so future `orval` regens don't regress it.
- 2026-09-13T00:02:00Z ✅ DONE — applied the codemod (202 rewrites/45 files, `--check` now reports 0), added `allowImportingTsExtensions` to `lib/api-zod/tsconfig.json`, and wired the codemod into `lib/api-spec`'s `codegen` script right after `orval` so a future regen stays fixed automatically. Barrel `export *` re-exports in `src/index.ts` resolved correctly with no edge-case fix needed. Verified: `pnpm --filter @workspace/api-zod exec tsc --noEmit` clean; api-server's own `relative-import-extensions.test.ts` guard passes; `pnpm --filter @workspace/api-server exec tsc --noEmit` has exactly one failure, pre-existing and unrelated (confirmed via stash-out/re-run) — `portal-checkout.test.ts:549` (#3633's mock passing `null` where the return type is `string | undefined`) — filed as #3881 under Feature #1128, board status set to AI Batter Up. No `@workspace/api-zod` consumer other than api-server exists in the repo; the issue's "MyArchitect client" has no matching `artifacts/` package to check. Commit `2a4fba9f2`, rebased and pushed to `origin/main` as `726df769e`; `verify-branch-merged.mjs` confirms ancestry; working tree clean.
