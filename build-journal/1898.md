# #1898 — Git source-control panel: collapsible commit graph, real branch list, Add to .gitignore

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** b03d57e84f (impl), rebased+pushed to main as 9b5dedd20

## Log
- 2026-08-30 ⏳ IN FLIGHT — reading Controls/LeftSidebar.xaml git panel section and LeftSidebar.xaml.cs PopulateGitGraph / GitChangesTree_SelectedItemChanged before making changes. Plan: collapsible graph section, real branch list panel, right-click "Add to .gitignore" context menu on GitChangesTree.
- 2026-08-30 ✅ DONE — Fixed all three gaps in `Controls/LeftSidebar.xaml` / `.xaml.cs`:
  1. **Collapsible commit graph**: added `BtnGitGraphToggle` (reuses `ExpandCollapseToggleStyle` from #1809) next to the "COMMIT GRAPH" header, gating `GitGraphHost`'s visibility. Defaults collapsed (`IsChecked="True"`, same on/checked=collapsed convention as `BatterUpPanel.BtnCollapse`), matching Shane's stated preference. `PopulateGitGraph()` still builds the graph on every refresh regardless of the toggle, so expanding shows current data immediately with no extra fetch. Updated the stale #860 comment that claimed the old tab-toggle was permanently removed.
  2. **Real branch list**: new `PopulateGitBranches()` runs `git branch -a --no-color` on a worker thread and renders local + remote-tracking branches into a new collapsible `GitBranchListHost` section (own `BtnGitBranchesToggle`, collapsed by default) above the commit box. Current branch shown with a filled dot/bold; local non-current branches check out on click via the existing `RunGitCommand` pipeline (which already re-refreshes the whole panel). Wired into `RefreshGitStatus()` alongside `PopulateGitGraph()` so both refresh from the same trigger (view-switch, manual refresh, own commit/push/pull, #859's FileSystemWatcher).
  3. **Add to .gitignore**: `GitChangesTree`'s per-file context menu (built in `CreateGitFileTreeItem`, which already had Stage/Unstage/Discard/Open File — the issue's claim of "no context menu at all" was against how the panel looked before those actions existed) now has a new "Add to .gitignore" item. `AddToGitignore(GitItem)` appends the file's real repo-relative path (creating `.gitignore` if missing), skips if already present, and — per the issue's required UX distinction — warns explicitly in `GitStatusSummaryText` when the file is already tracked by git (staged, or a working-tree change to a tracked file) that it will keep showing as changed until `git rm --cached`.
  - Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors), run twice — once mid-work, once against the final merged `main` HEAD.
  - Branch `agent/1898-q985` rebased onto `origin/main` and pushed directly to `main` (per Git conventions — commit directly to main, no PR) as `9b5dedd20`. `verify-branch-merged.mjs` confirms merged.
  - No test manifest — desktop/BuildConsole is explicitly out of scope for test manifests.
  - `git status --porcelain` clean at completion.
  - No new findings to file this session.
