# #4103 — 23 test manifests reference dead/non-existent artifacts/portal routes

- **Status:** ✅ DONE 2026-09-14T21:35:27Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 779a9c9ca (money-path goto fix), 9634cb665 (retire 7 dead-route manifests)

## Log
- 2026-09-14T21:30:29Z ⏳ IN FLIGHT — re-grepped `test-manifests/` for `portal-v2` (22 files match today, not 21 — list drifted since the issue was filed). Read each match's real context to separate genuine dead `goto`/asserted-landing navigation from files that only *mention* portal-v2 in a historical note (already migrated, nothing to fix). Real broken set: 7 files (account-settings, compliance-drilldowns, governance-area-cards, governance-area-drilldowns, security-drilldowns, shell-systems, customer-home-landing) — each cross-checked against the real current `artifacts/portal/src/App.tsx` route tree, `pillar.tsx`'s own scope note, and sibling manifests, all pointing the same direction: every one of the 7 is fully superseded by a real, already-built, already-covered current page (pillar-pages.json, policy-decisions.json, account-security-mfa-enrollment.json, webhooks.json, billing.json, settings.json, customer-home-overview.json), with zero current UI equivalent for the retired drill-down/area/shell-chrome concepts they tested. Money-path goto fix applied to both files (mechanical, per dispatch).
- 2026-09-14T21:35:27Z ✅ DONE — Pushed 779a9c9ca (money-path `/portal/shane-mccaw-consulting/login` → `/portal/login` fix, both files) and 9634cb665 (deleted the 7 fully-superseded manifests + deregistered them from `test-manifests/_regression-suite.json`) directly onto `origin/main`, rebasing past 2 intervening commits from other concurrent sessions along the way. `verify-branch-merged.mjs` confirms `agent/4103-q2790` (9634cb66) is an ancestor of `origin/main`; `git status --porcelain` is clean. Filed 2 real findings mid-build, both parented as direct sibling sub-issues of #1790 (this issue's own Feature) and set to AI Batter Up board status: **#4120** (GET /api/portal/governance/areas is live but has zero current UI callers, plus 4 orphaned useSecEvidence*Live/useHltDriftLive/useHltObjectsLive hooks — the same "live endpoint the surface doesn't call" pattern, discovered while deciding governance-area-cards.json/security-drilldowns.json's fate) and **#4121** (money-path-login-direct.json's post-login steps navigate to a `/copilot-readiness` route that doesn't exist anywhere in the current apps and assert scene-based `data-cj-scene`/`verdict-score` testids that don't either — out of #4103's own stated mechanical-goto-only scope for that file, so filed rather than silently expanded).
  Honest gap: could not invoke `shaneapp://runTest` to live-execute the fixed money-path manifests — no `shaneapp://`/runTest tool was exposed to this session (checked via ToolSearch, zero matches, not merely deferred). Verified instead by: JSON.parse validity on every touched/created file, an exhaustive cross-reference of each retired manifest's tested feature against the real current `artifacts/portal/src/App.tsx` route table and every plausible sibling replacement manifest (documented per-file in the retirement commit body), and confirming the money-path files' `#email`/`#password`/`login-submit` selectors are still real against the current `login.tsx`. The other ~15 files the issue's original `grep -rl "portal-v2"` also matched were individually re-checked and left untouched (already correct, portal-v2 only mentioned in a historical note).
  No fixture/hardcoded data introduced. No schema/DB changes. Working tree clean, nothing left uncommitted or belonging to another session.
