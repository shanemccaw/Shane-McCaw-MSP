# #3781 — Microsoft Changes: re-verify and refresh contract pack (stale since #2696)

- **Status:** ⏳ IN FLIGHT 2026-09-12T00:00:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Starting: full line-by-line re-audit of `docs/microsoft-changes-contract-pack.md`
  against current `main` per the #1642 pattern. Confirmed real drift beyond the two known deltas
  (#2696's limit/offset fix, #1678's WireAnalysis): `posts[].routing` (WireRouting) is a whole new
  wire field on surface A not documented at all; two new admin routes
  (`POST .../:id/route`, `GET .../:id/routings`) missing from §1b's table; the pipeline is no longer
  dormant (1 confirmed interpretation, 9 resolutions, 9 routings, 1 auto_created CR — live-queried
  against local DATABASE_URL); auth middleware moved from `requireRole` to `requireCapability`
  (`ladder.msp-operator` / `ladder.customer-user`); and the #2696 fix is a silent NaN fallback to
  defaults (50/0), not a 400 — the issue's own premise of "400, not the old 500" is itself stale.
