# #3781 — Microsoft Changes: re-verify and refresh contract pack (stale since #2696)

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** platform
- **Commit:** `6993d6648` (merged into `origin/main`, verified via `verify-branch-merged.mjs`)

## Log
- 2026-09-12T00:00:00Z — Starting: full line-by-line re-audit of `docs/microsoft-changes-contract-pack.md`
  against current `main` per the #1642 pattern. Confirmed real drift beyond the two known deltas
  (#2696's limit/offset fix, #1678's WireAnalysis): `posts[].routing` (WireRouting) is a whole new
  wire field on surface A not documented at all; two new admin routes
  (`POST .../:id/route`, `GET .../:id/routings`) missing from §1b's table; the pipeline is no longer
  dormant (1 confirmed interpretation, 9 resolutions, 9 routings, 1 auto_created CR — live-queried
  against local DATABASE_URL); auth middleware moved from `requireRole` to `requireCapability`
  (`ladder.msp-operator` / `ladder.customer-user`); and the #2696 fix is a silent NaN fallback to
  defaults (50/0), not a 400 — the issue's own premise of "400, not the old 500" is itself stale.
- 2026-09-12T00:00:00Z ✅ DONE — `docs/microsoft-changes-contract-pack.md` fully re-audited and
  updated (commit `6993d6648`, pushed straight to `main`, verified merged via
  `verify-branch-merged.mjs`). Corrected the #2696 finding (silent default, not a 400), documented
  the new `posts[].routing`/`WireRouting` wire field, added the two missing admin routes
  (`.../:id/route`, `.../:id/routings`), refreshed all live-data counts (pipeline is no longer
  fully dormant: 1 confirmed interpretation, 9 resolutions, 9 routings, 1 auto_created CR), and
  re-pinned every stale line citation across `portal-message-center.ts`, `msp-message-center.ts`,
  `admin-m365-interpretations.ts`, `m365-change-router.ts`, `portal-change-control.ts` and
  `lib/db/src/schema/msp.ts`. Read-only — no product/schema/UI changes. `git status --porcelain`
  clean. No new bugs found beyond pack staleness itself; no findings filed.
