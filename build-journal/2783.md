# #2783 — Every tool rail panel needs a real "Send to Chat" affordance — generic contract, not per-tool retrofit

- **Status:** ✅ DONE 2026-09-04T03:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** f453bdd8411dec8ccfbec3cab5c89d49f8774742

## Log
- 2026-09-04T02:45:00Z ⏳ IN FLIGHT — read #2783's full body + comment; investigated `Controls/ChatDocumentContainer.xaml(.cs)` rail header (RailClose/RailMaximize/RailRefresh, ~line 280-289) and the shared `SendTextToActiveClaudeChatAsync` path (`MainWindow.xaml.cs`, used by SQL Runner #940 and Log Viewer #2786). About to: (1) add a generic `IChatSendableTool` interface, (2) add a rail-header "Send to Chat" icon that's enabled only when `ToolHost.Content is IChatSendableTool sendable && !string.IsNullOrEmpty(sendable.GetSendableContent())`, wired through `SendTextToActiveClaudeChatAsync`, (3) implement `IChatSendableTool` on `TerminalView` returning its real current output/selection.
- 2026-09-04T03:05:00Z ✅ DONE — Shipped all 5 points: (1) `Controls/IChatSendableTool.cs` — the new shared contract, one method `string? GetSendableContent()`; (2) `RailSendToChat` icon added to the rail header in `ChatDocumentContainer.xaml` next to `RailMaximize`/`RailClose`/`RailRefresh`, visibility driven by `UpdateSendToChatAffordance()` (checks `ToolHost.Visibility == Visible && ToolHost.Content is IChatSendableTool sendable && !string.IsNullOrEmpty(sendable.GetSendableContent())`), called on every rail-state transition (OpenTool/CloseRail/OpenDetected) plus the existing 30s `_activeTimer` tick so a tool's changing output doesn't leave the icon stale for long; (3) `RailSendToChat_Click` re-reads `GetSendableContent()` fresh at click time and routes through the exact same shared `MainWindow.SendTextToActiveClaudeChatAsync` path SQL Runner (#940) and Log Viewer (#2786) already use — no new send mechanism invented; (4) `TerminalView` now implements `IChatSendableTool`: `GetSendableContent()` returns the output pane's real selection if one exists, else its full real output history (documented judgement call in the doc comment) — nothing fabricated, straight off `OutputBox`; (5) any future rail tool gets Send-to-Chat automatically just by implementing the interface — no further rail-header changes needed. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Branch verified merged into origin/main via `verify-branch-merged.mjs`. `git status --porcelain` clean. No findings filed this session — no bugs or gaps hit outside scope.
