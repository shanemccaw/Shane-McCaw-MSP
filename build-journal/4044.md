# Build Journal — #4044

Audit log: real schema (actor model, tenant scoping, action catalogue) + fail-open logging wrapper.
Part of #1946 (Feature: Full Audit Log), Step 1 of 4.

- **Status:** ✅ DONE 2026-09-14T14:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 73099491a (schema + migration + wrapper + doc), d856eb77c (IN FLIGHT bookend)

## Log
- 2026-09-14T13:46:23Z ⏳ IN FLIGHT — Extending `audit_logs` schema (actor-role enum → real principals, `tenant_id` scoping, `action_category` catalogue enum) + manual additive migration run against local DB, then widening the `createAuditLog` wrapper. Fail-open+loud logging already landed under Git #3935; this build adds the data model. Investigation done: 358 live rows, 179 call sites, actor_role already carries msp/customer/system/microsoft (latent), 175 distinct actionType / 60 distinct entityType values; tenants.id is the real tenant scope (users.tenant_id → tenants.id).
- 2026-09-14T14:05:00Z ✅ DONE — Shipped in commit 73099491a. **Schema:** `actor_role` widened from `[admin,client]` to the real closed principal set (`AUDIT_ACTOR_ROLES`: admin/client/customer/msp/platform_admin/service_account/agent/microsoft/system — the msp/customer/system/microsoft values were already live but violated the old 2-value type; now first-class). Added `tenant_id` (FK `tenants.id`, `onDelete set null`, indexed) for real tenant scoping. Added `action_category` closed catalogue enum (`AUDIT_ACTION_CATEGORIES`: create/update/delete/action/settings/auth/access/security/system) — the filterable dimension; `action_type` stays open (passed as a variable at some sites, so not hardened). **Migration:** `lib/db/migrations/manual/2026-09-14-audit-log-actor-tenant-catalogue-4044.sql` — additive (ADD COLUMN IF NOT EXISTS + FK + 3 indexes + self-marking INSERT), **run against local DB** (verified: columns present, FK live, insert of actor_role=agent/action_category=security/tenant_id=1 accepted then rolled back — append-only table left untouched). Existing 358 rows NOT backfilled (action_category/tenant_id NULL). Appended to #1630 for Replit/staging. **Wrapper:** `createAuditLog` gains optional `tenantId`/`actionCategory` (best-effort derived via exported `deriveAuditActionCategory` when omitted), and the fail-open catch now serializes err message+stack explicitly (a bare `log.error({err})` flattens non-Errors to `{}` here). **Verification:** api-server `tsc --noEmit` clean (exit 0); grep confirms NO update/delete route against `audit_logs` (only test-teardown deletes); check-drift's schema-drift red is PRE-EXISTING (commits a5a7f4bfa #3409 import-extension fix + 4babfea87 #3625, both ancestors of origin/main) — my commit reconciled cleanly. Filed that pre-existing drift as finding #4055 (parented under #1946, board=AI Batter Up). Branch verified merged into origin/main; tree clean. Fail-open+loud-logging done-when was already satisfied by Git #3935 and is preserved/improved. Doc: `docs/audit-log-model-4044.md`.
