# #3841 — Retention-lifecycle live-db test's synthetic customer actor id has no real users row

- **Status:** ✅ DONE 2026-09-13T04:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** d996f81a5

## Log
- 2026-09-13T03:45:00Z ⏳ IN FLIGHT — fixing `msp-retention-queue-poam-producer.live-db.test.ts` to insert real `users` rows for the MSP-admin and customer test actors instead of hardcoded synthetic ids (1, 2), so `createAuditLog`'s `actorUserId` FK insert genuinely succeeds instead of being silently swallowed; adding a real assertion that the audit row exists.
- 2026-09-13T04:20:00Z ✅ DONE — `d996f81a5` inserts real `usersTable` rows for both the MSP-admin and customer actors in `beforeAll` (mirroring `msp-settings-notification-preferences.live-db.test.ts`'s pattern), signs both JWTs from the real returned ids, and cleans them up in `afterAll`. Added a direct `auditLogsTable` assertion after the acceleration-request step (`actionType === "retention.acceleration_requested"`, `entityId === String(poamId)`) so the test genuinely proves the audit row landed rather than just trusting a 200 status. Ran live against local Postgres (`pnpm --filter @workspace/api-server vitest run msp-retention-queue-poam-producer.live-db`): 1 passed, no FK/error output from `createAuditLog` (previously it logged a swallowed FK violation on `audit_logs_actor_user_id_users_id_fk`). `tsc --noEmit` clean on the touched file. Verified branch merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
  - Filed sibling finding **#3935** — `createAuditLog`'s catch-and-log swallow is a real, separate risk beyond this test (any call site's audit write can fail silently with no signal to the caller). Parented as a direct sub-issue of #1944 (this issue's own Feature), labeled `bug`, milestone v1.1, board status set to "AI Batter Up". Not fixed inline — it's a genuine design decision (whether audit-write failure should ever fail the calling request), not a trivial change.
