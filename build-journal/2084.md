# #2084 — BuildConsole ignores storeHealth from provision-worktree --json

- **Status:** ✅ DONE 2026-09-06T15:45:00Z
- **Scope:** buildconsole

## Log
- 2026-09-06T15:20:00Z — Started. Fixing WorktreeProvisionService.cs:89-99 to parse the
  `storeHealth` object (`{clean, foreign, dangling, poisonedBins, autoRepair?, error?}`)
  that `provision-worktree.mjs --json` already emits on both provision result paths
  (reuse + fresh create), log it to the `worktree.provision` ActivityLog channel when
  `clean=false`, and surface a warning on the Build Watch slot at launch pointing at
  `node scripts/dev-server/store-doctor.mjs` (never prescribing `pnpm install`, per #1987).
- 2026-09-06T15:45:00Z — ✅ DONE — commit `c1c1d09ee` (merged to `main`; `9c570388` on
  `agent/2084-q1784` before rebase). Added `StoreHealthInfo`/`StoreHealthAutoRepairInfo`
  to `WorktreeProvisionService.cs` and parse the real `storeHealth` JSON object on both
  result paths; when unclean (or the scan itself errored), log a loud
  `worktree.provision` ActivityLog line naming `store-doctor.mjs` (never `pnpm install`).
  Added `WorktreeProvisionService.PendingLaunchWarnings` (keyed by queue item id) +
  `BuildStoreHealthWarning(...)`; `QueueWatcherService.LaunchItem` stashes the warning
  right after a successful provision, and `BuildWatchWindow.OccupySlot` pops it and adds
  an `AssistantParagraphTurn` (Kind=Error) as the first turn in that slot's transcript —
  a real, visible warning on the Build Watch slot at launch, not just the log line.
  Verified: `dotnet build` clean (0 warnings, 0 errors). Branch confirmed merged into
  `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain`
  clean. No fixture/hardcoded data — this surfaces the real scan result the provisioner
  already computes.
