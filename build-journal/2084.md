# #2084 — BuildConsole ignores storeHealth from provision-worktree --json

- **Status:** ⏳ IN FLIGHT 2026-09-06T15:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-06T15:20:00Z — Started. Fixing WorktreeProvisionService.cs:89-99 to parse the
  `storeHealth` object (`{clean, foreign, dangling, poisonedBins, autoRepair?, error?}`)
  that `provision-worktree.mjs --json` already emits on both provision result paths
  (reuse + fresh create), log it to the `worktree.provision` ActivityLog channel when
  `clean=false`, and surface a warning on the Build Watch slot at launch pointing at
  `node scripts/dev-server/store-doctor.mjs` (never prescribing `pnpm install`, per #1987).
