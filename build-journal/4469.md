# #4469 — Monitoring: lock tier before consent — Account → Select Tier → Consent → Pay → Login

- **Status:** ✅ DONE 2026-09-17T13:26:23Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 309ee788d (server tier lock + Buy.tsx stage split + live-db test), a3dad37c7 (buy-page manifest steps), bfc90301b (IN FLIGHT bookend)
- **Migration:** none
- **Findings filed:** none. Checked Packs and Retainer for the same #4407 shape: no `config_pack` or `retainer` services row declares a `packageKey`, so their consent-time scan is always the baseline and a later pack/tier change alters nothing that was scanned.

## Log
- 2026-09-17T03:43:33Z ⏳ IN FLIGHT — reading current Buy.tsx / account-first-purchase.ts / consent.ts; splitting Tier & Pay, locking tier before consent
- 2026-09-17T13:26:23Z ✅ DONE — Monitoring now runs **Create account → Select tier → Connect → Pay → Login**.
  - **Login (item 4), read from the code:** not a second authentication. It is the existing post-payment `done` stage. "Open your Portal" mints the single-use, 2-minute portal-handoff token (`POST /api/public/purchase/portal-handoff`, consumed by `/auth/signup-exchange`) and the buyer lands in the Portal signed in on the account created at step 1; the fallback is signing in with that password. Only the rail label is new.
  - **Server:** `updateMonitoringSelection` now refuses a changed slug on a connected session (`tenant_id` set or status `consented`) with 409 `tier_locked`, next to the existing `seats_locked`. An unchanged re-send still returns 200. The lock is repeated in the UPDATE's own WHERE, so a tier change racing the consent callback either lands before the callback's `UPDATE … RETURNING product_slug` (which picks the first-scan package) or is refused. Only the consent callback marks a session consented/tenant-linked, and payment prices from the session row, so the scanned tier is the paid tier (the #4407 bug). `consent.ts`: comment only.
  - **Client (Buy.tsx):** "Tier & pay" split. Select tier = tiers + seat estimate + "Continue with <tier>" (`buy-tier-continue`, writes the selection to the session). Connect = only the chosen tier shown, seats frozen, "Change tier" (`buy-tier-change`) available until the tenant connects. Pay = terms + Stripe, only after connect. Login = done. Resume: a connected session lands on Pay, an unconnected one on Select tier. Head, connect-card and connected-card copy updated to the new order. Packs and Retainer untouched.
  - **Verified:** api-server and shane-mccaw-consulting `tsc --noEmit` clean. `account-first-purchase.live-db.test.ts` 2/2 against local Postgres (step 4 asserts seats_locked, tier_locked, unchanged re-send 200). Regression: consent.test, account-first-purchase / -retainer / -pack live-db, pre-consent live-db, public-purchase-payment.test — 6 files, 128/128.
  - **Real browser** (Edge via playwright-core; isolated app-only host built from this worktree on :8094 + worktree Vite on :5469; real local DB; Stripe test mode): 18/18 — new rail labels; Select tier offers 3 tiers; Continue wrote Premier onto the session; Connect step shows one tier, no seat input, Change tier; Change tier → Growth written before consent; consent URL 200; Pay step only after connect; live 409 `tier_locked` with the session slug unchanged; payment intent metadata `monitoring-growth-smb` (112000 usd; intent cancelled); Stripe Payment Element rendered; paid session + sign-in resume lands on Login/done. Stubbed only: the code email, Microsoft's consent screen (session consented + tenant written in the DB), and the charge itself for the Login check (status set to paid). 0 synthetic rows left.
  - **`shaneapp://runTest marketing/buy-page.json`** against the dev ports (:5173 serves the new Buy.tsx): onlyTag=4469-tier-first 10/10, onlyTag=4377-account-first 13/13.
  - **Caveat:** my `request-restart.mjs` call was cut off when the session restarted. `C:\dev-server` has a3dad37c7 merged, but its api-server `dist` predates it, so the server lock was proven by the live-db test and the isolated host, not the shared :8080. BuildConsole's set merge-back/restart will carry it.
  - `verify-branch-merged` OK. Working tree clean; nothing was dirty on arrival.
