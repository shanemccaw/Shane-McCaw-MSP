# #2165 — sharepoint-onedrive-trend check intermittent 'terminated' error

- **Status:** ✅ DONE 2026-09-04T06:45:00Z
- **Scope:** platform

## Log
- 2026-09-04T06:24:47Z — Session started. Investigation-only dispatch (do NOT implement retry). Task: confirm/refute suspected root cause of `adoption:sharepoint-onedrive-trend` check erroring `status=error, item_count=0, error_message='terminated'` in recurring monitoring run `wf-run-34320-node-execute_pkg` (2026-08-31), check recurrence, propose fix. Report as comment on #2165, no code change.
- 2026-09-04T06:45:00Z — ✅ DONE — Investigation delivered as a comment on #2165 (no retry code, per dispatch scope). Findings: grepped monitor-executor.ts/graph.ts — the literal string `"terminated"` is never thrown in this codebase; it's `err.message` from the generic catch-all (monitor-executor.ts:3640) wrapping the bare, un-timed-out `fetch()` in graphFetchForTenant (graph.ts:844). `TypeError: terminated` is a known Node/undici built-in error for a premature TCP close mid-response-stream — consistent with, not proven against, the large CSV report body from `/reports/getSharePointSiteUsageDetail` (adoption:sharepoint-onedrive-trend's endpoint), which graphFetchPaginated special-cases as CSV (monitor-executor.ts:1748-1761). Queried local Postgres `tenant_monitor_profiles` directly: exactly one `'terminated'` row ever recorded (the same wf-run-34320 row already in the issue), but the local DB has recorded ZERO recurring-workflow runs at all since that run (2026-08-31 03:34:56) — so "no recurrence" is an absence of data, not a verified absence of the bug; flagged this caveat explicitly in the comment along with a scoped bounded-retry proposal (1-2 attempts, short backoff, network-failure-only, ideally scoped to the CSV-report fetch path) for a future dispatch to implement. No code changed this session. Commits: 8627fdfa (IN FLIGHT bookend), this DONE bookend commit. Branch verified merged via verify-branch-merged.mjs after push. `git status --porcelain` clean.
