# #2675 — MSP console: Break-glass Access operator routes

- **Status:** ✅ DONE 2026-09-03T20:26:28Z
- **Scope:** platform

## Log

- 2026-09-03T20:16:17Z — Started. Read #2675 body/comments, Feature #2564 (MSP Console,
  not yet architected), Feature #1651 (Portal) and its landed contract pack #2443
  (`docs/break-glass-access-contract-pack.md`), the real `break-glass-verification.ts`
  route file (5 endpoints, all `/portal/*` + `/public/*`, none `requireRole`-gated),
  and the msp.ts schema for the 3 real tables. Confirmed via grep: zero
  `requireRole("MSPOperator"|"MSPAdmin")` routes exist for break-glass anywhere in
  `artifacts/api-server/src/routes/`. Plan: new `msp-break-glass.ts` with a
  cross-tenant list, a per-customer list/detail, an MSP-console admin-override
  (refactored out of the existing portal route so the security-sensitive reset
  logic has one implementation), and an audit-history read. No schema/migration
  needed — the 3 tables already carry everything these routes read.

- 2026-09-03T20:26:28Z — ✅ DONE. Built `artifacts/api-server/src/routes/msp-break-glass.ts`,
  a new `requireRole("MSPOperator")`-gated route file (admits MSPOperator, MSPAdmin,
  PlatformAdmin per `requireAuth.ts` roleIndex), registered in `routes/index.ts`:
  - `GET /api/msp/break-glass` — cross-tenant list of every `pending_delivery`
    secret across the caller's own MSP book (`resolveMspIdStrict` + per-staff
    scoping via `resolveStaffScopedCustomerIds`, same pattern as `msp-alerts.ts`).
  - `GET /api/msp/customers/:customerId/break-glass` — full pending-secret history
    for one customer (`assertCustomerAccess` ownership check).
  - `GET /api/msp/customers/:customerId/break-glass/:pendingSecretId` — one
    pending secret + its verification attempts; never returns `linkToken` or the
    encrypted/vault-referenced value. 404 for both "not found" and "not this
    customer's".
  - `POST /api/msp/customers/:customerId/break-glass/:pendingSecretId/admin-override`
    — force-reset + reissue.
  - `GET /api/msp/customers/:customerId/break-glass/audit` — override audit trail
    with the acting admin's name/email resolved.

  Refactored the admin-override reset logic out of
  `break-glass-verification.ts`'s portal route into an exported
  `performBreakGlassAdminOverride()` (+ exported `resolvePendingContext` /
  `PendingContext`) so the security-sensitive credential-reset flow has exactly
  one implementation, called by both the portal route and the new MSP route. No
  schema/migration needed — the 3 existing tables already carry everything these
  routes read/write.

  **Verification:** `tsc --noEmit` in this worktree reports ~4000 errors, but a
  parallel run against the untouched main checkout
  (`C:/Source/ShaneMcCawConsulting/Shane-McCaw-MSP`) reports ~3200 — confirmed
  pre-existing environment noise (module resolution for `drizzle-orm`/`zod`
  failing broadly across the whole repo, not something introduced here), matching
  the `api-server-tsc-needs-libdb-decls-and-pnpm-state-reset` class of issue.
  Filtered specifically for `msp-break-glass.ts` and the touched section of
  `break-glass-verification.ts`: zero real type errors beyond that same noise
  (`TS2307`/`TS6305`/`TS7006` from unresolved module types) — no `pnpm install`
  attempted, per the standing hard rule. No test manifest — this is
  `artifacts/api-server` backend routing, out of the portal/marketing test-manifest
  scope.

  **Commits (all on `main`):** `fe9c7e78c` (bookend IN FLIGHT), `0252b687`
  (routes + refactor, rebased onto current `main`). Verified merged via
  `verify-branch-merged.mjs`. Working tree clean.

  No findings filed — the backend gap this issue named is now closed; no new
  gaps discovered while building it.
