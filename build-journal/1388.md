# #1388 — Compliance: 11 of 14 area cards are true 404s (no destination route)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** f53cac24f (route + resolver + page + manifest), 67d4ca59d (manifest pin), 2bfc867d9 (unit test)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Investigating whether a `/portal-v2/compliance/:area` wildcard route (matching Governance's proven pattern) fixes the 11 broken Compliance area cards, or whether named routes are needed. Building accordingly.
- 2026-08-27 ✅ DONE — Wildcard route was the right fix; named per-area routes would require fabricating final copy the design never had. Decisive evidence: design source `Customer Portal Shell.dc.html:13969` shows a compliance area card's `navGo` only ever expanded a finding INLINE (`cmpExpanded`) or did nothing — the cards were never separate pages. (Also found all 14 cards 404, not just 11: none of the card keys match the 3 existing routes open-gaps/decisions/obligations.) Shipped: `/portal-v2/compliance/:area` wildcard route (declared after the 3 literal sub-routes so source-order match keeps them), a `cmpAreaFor` resolver (`cmpAreaModel.ts`), and `PortalV2ComplianceAreaPage` (`portal-v2-cmp-area.tsx`) — finding-backed cards (6/14) render that finding's real design-final detail (obligation/why/evidence/CR-gate wrench/record-decision) from `cmpDrilldownData`; inert cards render an honest pointer into the three registers; unknown slug → NotFound (Governance's precedent). Verification: tsc clean on all new files (2 remaining tsc errors are pre-existing on origin/main in SowScreen.tsx + PortalV2Shell.tsx, unrelated). New `cmpAreaModel.test.ts` 4/4 PASS. Extended `test-manifests/portal/compliance-drilldowns.json` (sessionTag `cmp-area-1388`, AREA1–AREA6) — but a live `shaneapp://runTest` could NOT verify it this session: the dev api-server (localhost:8080) is down in the pre-completion state, so the run aborted at the pre-existing critical CMP1 assertion before reaching the AREA steps (documented deploy-ordering false negative — BuildConsole merges + restarts the dev services on build completion). No Shane action required.
