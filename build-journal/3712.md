# Build Journal — Issue #3712

- **Status:** ✅ DONE 2026-09-12T00:30:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T00:00:00Z — Started. Investigated `CheckMilestoneCompletenessAsync`
  (`GitHubIssueTimeSeriesService.cs:485-500`): it compares GitHub's own per-milestone
  count (`bt_milestone_mirror.open_issues+closed_issues`, issues whose OWN milestone
  field is set) against `mirrored.Count(i => i.MilestoneNumber == milestoneNumber)`,
  where `MilestoneNumber` is the INHERITED/effective milestone (Git #2543 climbs
  ancestors when an issue's own field is blank). Two different sets — the mirrored
  side is a strict superset of the real side, so the gate can pass while genuinely
  missing rows (confirmed slack: +139 on #5, +132 on #16, +124 on #11, per the issue
  body). Plan: add a new `own_milestone_number` column (populated straight from
  GraphQL's `milestone.number`, before any inheritance climb), thread it through the
  mirror's read/write paths, and compare it (own-vs-own) in the completeness gate.
- ✅ DONE 2026-09-12T00:30:00Z — Added `GitBoardIssue.OwnMilestoneNumber`
  (`GitHubApiClient.cs`), populated at every real GraphQL fetch site (open walk,
  closed walk, `IssueParentInfo` targeted batch fetch) and never mutated by the
  #2543 inheritance climb. Added `bt_issue_mirror.own_milestone_number` column
  (`lib/db/migrations/manual/2026-09-12-bt-issue-mirror-own-milestone-number-3712.sql`,
  run by me against both `BUILD_DATABASE_URL` (`bt_issue_mirror` itself, where the
  column lives) and the product `DATABASE_URL` (`simulator_migration_runs`
  self-mark) — confirmed `ALTER TABLE`/`INSERT` both succeeded live. Threaded the
  new column through every read/write path in `GitHubIssueMirror.cs` (full sync,
  incremental targeted upsert, closed-issue backfill, board-row mapper).
  `ReconcileClosedGraphAsync`'s DB-side inheritance walk is untouched — verified it
  only ever writes `milestone_number`, never `own_milestone_number`. Fixed
  `CheckMilestoneCompletenessAsync` to compare `OwnMilestoneNumber` (own field)
  against `bt_milestone_mirror`'s real count (own field) — own-vs-own, matching
  sets on both sides, in place of the old inherited-vs-own comparison.
  `dotnet build` succeeded, 0 warnings/errors. Live-queried the local
  `BuildConsole` DB post-migration: all 13 milestones currently read 0
  `own_milestone_number` mirrored (expected — brand-new column, not yet
  backfilled by a sync pass), which correctly reports "incomplete" rather than
  the old false-"complete" — undercounts (fails closed) until the next sync
  repopulates the column, the safe direction. Commit `65a034466`, pushed to
  `origin/main` at `1c8e75122`. `verify-branch-merged.mjs` confirmed merged.
  `git status --porcelain` clean.
