# #2454 — Regenerate PRODUCT_CATALOG.md from the real services table

- **Status:** ✅ DONE 2026-09-02T21:52:00Z
- **Scope:** platform
- **Started:** 2026-09-02
- **Commit(s):** 65cad813e19bf02f497ceb1be3855f54ffb4e1c2 (PRODUCT_CATALOG.md, real hash on main after rebase+merge)

## Log
- 2026-09-02T21:36:04Z ⏳ IN FLIGHT — pulled #2454 body + comments; confirmed `lib/db/src/schema/index.ts` has no separate `retainer_tiers` table (retainer tiers are `services` rows with `deliveryType = 'retainer'`); starting direct query of the real `services` table via local `DATABASE_URL`.
- 2026-09-02T21:52:00Z ✅ DONE — regenerated `PRODUCT_CATALOG.md` at repo root, entirely from direct `psql` queries against the local `services` table (115 rows / 96 public). Confirmed no dedicated retainer-tier table exists (`retainer_settings` is per-customer runtime state, 0 rows — not a catalog); retainer products are `services` rows with `delivery_type = 'retainer'`. Covered 8 real public categories (retainers, MSP platform subscription, monitoring bundles, Change Control add-ons, assessments, config packs, projects, MSP onboarding) plus an internal-only-rows appendix. Did not read any of the retired prose docs. Real discrepancies found and flagged in the doc itself rather than carried forward silently:
  - All 6 retainer rows have `inclusions`/`features`/`deliverables` = NULL — no backing "what's included" data exists for them.
  - The 12 Monitoring bundle rows have NULL flat price columns; real pricing lives only in `type_attributes` (`pricePerUserMonth`/`monthlyFloor`/`flatMonthlySurcharge`) — documented from there instead.
  - 6 public "project" rows (ids 158-163, the Copilot-rollout pillar engagements) and 4 private "White-Glove Copilot Adoption" rows (164-167) have no price set anywhere (flat columns and `type_attributes` both empty) — flagged, not invented.
  - The $900/mo retainer's real name is "Architect Advisory Retainer," not "Advisory" as one old doc had it.
  No code changes, no migration — document output only, per the issue's own scope. No filed findings beyond what's recorded in the doc itself (these are data gaps in existing rows, not missing endpoints/columns/features — nothing here needs a new GitHub issue).
