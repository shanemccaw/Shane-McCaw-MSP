# Build Journal — Git #2043

- **Status:** ✅ DONE 2026-09-02T03:05:00Z
- **Scope:** platform
- **Title:** Remediation KB: author the devices: domain (12 active checks)

## Log
- 2026-09-02T02:15:00Z — IN FLIGHT. Pulled #2043 and #1924 (authoring standard). Reference implementation: `lib/db/migrations/manual/2026-08-31-remediation-kb-identity-domain-1924.sql` (identity: domain, 29 rows). This build authors the same standard for the `devices:` domain (12 active checks): app-protection-coverage, autopilot-coverage, bitlocker-key-escrow, compliance-policy-coverage, compliant-vs-noncompliant, encryption-status, enrollment-status, kfm-configuration, os-patch-compliance, stale-duplicate-records, unassigned-intune-profiles, update-rings-config. Every row will be verified against real Microsoft Learn docs fetched in-session; draft rows never ship; verifiedBy is honest agent attribution.
- 2026-09-02T03:05:00Z — ✅ DONE. Ran 4 parallel research agents (3 checks each), each fetching real Microsoft Learn / Graph docs URLs in-session — no content written from memory. Authored `lib/db/migrations/manual/2026-09-02-remediation-kb-devices-domain-2043.sql`, 12 published rows, all with honest agent `verified_by` attribution ("Claude Sonnet 5 (build #2043) — agent-authored, verified against Microsoft Learn; awaiting human spot-check"). Ran migration against local DATABASE_URL: devices: rows 0 -> 12 (all published), confirmed idempotent on re-run (ON CONFLICT DO UPDATE, no duplication). Commit `3a6333217` pushed directly to `origin/main` (worktree branch `agent/2043-q1141`), verified merged via `verify-branch-merged.mjs`.
  - **fix_route_capability split**: 11 rows `admin_center_only`, 1 row (`devices:stale-duplicate-records`) `you_must_run`. Unlike the identity: domain's Conditional Access policies (which bake targeting into the policy body itself), most Intune device-configuration/compliance objects require a separate group-assignment API call after creation before they have any real effect — where that assignment step wasn't independently verified as a clean, safe cmdlet path in this session, the row is honestly marked `admin_center_only` rather than overclaiming a complete automated fix.
  - **Finding filed**: #2185 (parented under #1096 EPIC: Application Core, board status "AI Batter Up") — `devices:kfm-configuration`'s own detection query (`GET /deviceManagement/deviceConfigurations`) queries the wrong Graph endpoint; real Known Folder Move policies live under `/deviceManagement/groupPolicyConfigurations` or `/deviceManagement/configurationPolicies`, so the check will always false-negative regardless of tenant config. Not fixed here — out of scope for KB-content authoring; the KB row itself is still published with correct remediation content.
  - **Shane To-Do filed**: #2186 (parented under #1489, labeled `Shane To-Do`) — human spot-check of the 12 agent-authored devices: rows, per the #1924 standard.
  - **#1630 line appended**: migration filename + row-count summary.
  - `git status --porcelain` clean at completion.
