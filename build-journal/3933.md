# Build Journal — #3933

- **Status:** ✅ DONE 2026-09-13T05:16:53Z
- **Scope:** platform
- **Log:**
  - 2026-09-13T05:16:53Z — IN FLIGHT. Task: retire portal-v2 sop-hub/sop-category pages per #1647 Group A.
  - 2026-09-13T05:16:53Z — Investigated: `artifacts/msp-portal` no longer exists at all. Commit `f40438cdc`
    ("Portal scaffolding: create artifacts/portal, retire artifacts/msp-portal") already deleted
    `artifacts/msp-portal/src/pages/portal-v2-sop-hub.tsx`, `portal-v2-sop-category.tsx`, their fixture
    modules (`sopHubData.ts`, `sopCategoryData.ts`, `sopCreate.ts`, `sopHubModel.ts`,
    `sopHubModel.test.ts`), and `useSops.ts`, along with the rest of `artifacts/msp-portal`
    (route registrations and nav entries went with the whole app).
  - 2026-09-13T05:16:53Z — Verified zero hits: `grep -rn 'portal-v2-sop-hub\|portal-v2-sop-category'`
    over the repo (no `artifacts/msp-portal/src/` even exists to search).
  - 2026-09-13T05:16:53Z — Confirmed the real replacement (#1493) is wired and live:
    `artifacts/portal/src/pages/sops.tsx`, routed at `/sops` in `artifacts/portal/src/App.tsx:90`,
    backed by `artifacts/portal/src/components/sops/useSops.ts` which fetches real
    `SOPS_URL`/`SOP_RUNS_URL` endpoints via `fetchWithAuth` — no fixture data.
  - 2026-09-13T05:16:53Z — ✅ DONE — No code change required; the deletion this issue asked for was
    already carried out by `f40438cdc`, and the real `artifacts/portal` SOPs page already supersedes
    it with live data. This is a no-op build, same pattern as #3928/#3929 (also retired by the same
    commit). Working tree left clean; nothing else touched.
