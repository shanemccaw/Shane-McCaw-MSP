# #2453 — Public Share Pages: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T03:35:14Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 56202eed8 (contract pack, merged to origin/main), preceded by bookend commit on `agent/2453-q1336` (rebased/squashed into the same push)

## Log
- 2026-09-03T02:00:00Z ⏳ IN FLIGHT — verifying `public/documents/:id`, `public/live-document-shares/:id`, `public/sows/:id` are still real/live, then extracting contract pack to `docs/public-share-pages-contract-pack.md`
- 2026-09-03T03:35:14Z ✅ DONE — Verified all 3 named endpoints are real and live, but all 3 are keyed by an opaque `:shareToken`/`:token`, not the `:id` the issue text used — documented honestly rather than silently corrected. Wrote `docs/public-share-pages-contract-pack.md` covering `GET /api/public/documents/:shareToken` (condensed, full contract already in `docs/documents-contract-pack.md` §2c-2e — cross-referenced, not re-derived), `GET /api/public/live-document-shares/:token`, `GET/POST /api/public/sows/:shareToken[/sign]`, plus the two sibling mint routes and cross-surface edges (Offers-and-SOW-Acceptance pack, `public-rbd-document.ts`). Orphaned-endpoint check: zero live frontend callers in `artifacts/portal`, `artifacts/msp-website`, `artifacts/shane-mccaw-consulting` — admin-panel only constructs/displays the share URL, doesn't call the endpoints. No new findings filed — the `:id`→`:shareToken` drift is real current state, not a defect. Rebased `agent/2453-q1336` onto `origin/main` twice (main moved concurrently under load) and pushed directly to `main` (`56202eed8`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
