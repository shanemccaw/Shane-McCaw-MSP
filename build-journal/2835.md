# #2835 — Directory-surface monitoring coverage: 35 remaining config_resources gaps after #2763

- **Status:** ✅ DONE 2026-09-04T23:35:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** ea1c514d1 (real work: migration + vitest test + vitest.config.ts registration), landed on origin/main as 75c70b0dc

## Log
- 2026-09-04T22:41:00Z ⏳ IN FLIGHT — pulled #2835's full body + comment. Breakdown of the 35: 12 real permission gaps (403/401/404), 9 real Entra ID P2/Governance license gaps, 3 filter/fan-out-needed endpoints (transitiveRoleAssignments x2, directoryObjects), model-bug/exclusion/PS-followup buckets covering the rest. Re-probing every one of the 35 live against the testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1, app-only) rather than trusting #2763's transcript unchecked.
- 2026-09-04T23:00:00Z ⏳ IN FLIGHT — re-probed all 35 live. Real findings beyond #2763's own transcript: (1) `transitiveRoleAssignments` (directory) actually resolves — the real error is "requires a $filter on the **principal** ID", not "at least one filter" as #2763 recorded; confirmed live with `$filter=principalId eq '<real user id>'` → 200 with real fields. Designed a genuine fan-out check over `/users?$select=id` (same accepted pattern as `identity:pim-groups`'s fan-out over `/groups`), targeting `beta` via an absolute URL (v1.0 doesn't expose this path — confirmed live 400). (2) `transitiveRoleAssignments` (entitlementManagement) stays blocked — its own fan-out source (`entitlementManagement/roleDefinitions`) returns a real 403 on this tenant, same wall as the other entitlementManagement gaps. (3) `directoryObjects` — `getByIds` POST genuinely works with real ids, but has no natural periodic "list" semantic and no existing consumer needing unknown-type ids resolved; left undesigned rather than forced (would be fabricated coverage). (4) 6 real config_resources model-bug rows (enterpriseApps singleton mis-modeled as collection; roleManagement/exchange + resourceNamespaces + transitiveRoleAssignments all genuinely non-functional via Graph — "not supported for AAD accounts"; roleManagement + roleManagement/directory non-queryable containers) all confirmed with the SAME structural 400 under every query shape tried (not a permission/license issue) — corrected via `availability='unavailable'`/`verification_status='failed_live'`, matching the table's own existing `failed_live` precedent, so they stop perpetually reappearing as "available_now, uncovered". Wrote `lib/db/migrations/manual/2026-09-04-directory-surface-coverage-2835.sql`, ran it against local `DATABASE_URL`: 1 new `monitor_checks` row (`identity:transitive-role-assignments`), 5 `monitoring_package_checks` memberships, 1 `config_resource_check_coverage` row (manually inserted — see below), 6 `config_resources` metadata corrections, coverage recompute. Real effect confirmed: `surface='directory' AND availability='available_now' AND check_coverage_count=0` dropped from 35 → 28.
- 2026-09-04T23:20:00Z ⏳ IN FLIGHT — real finding: `map-monitor-checks.mjs`'s `normalizeGraphEndpoint()` doesn't strip an absolute `https://graph.microsoft.com/beta/...` host+scheme before comparing to `graph_path`, so it reports my new check (and 2 pre-existing beta-absolute-URL checks) as "unmatched" even though the real normalized path is an exact match — confirmed live by calling `matchEndpointToResource()` directly. Filed as **#2929** (sub-issue of Feature #1128, labeled `bug`, board status "AI Batter Up"). My migration inserts the `config_resource_check_coverage` row directly (manually verified equivalent), same as #2763's own precedent, so this doesn't block today's coverage — it's a real latent bug in the automated recompute path.
- 2026-09-04T23:30:00Z ✅ DONE — Wrote a permanent vitest regression test (`artifacts/api-server/src/lib/__tests__/directory-surface-coverage-2835.test.ts`, 3 assertions, registered in `vitest.config.ts`'s include list, mirroring #2763's own precedent) using the real testbed-observed shape (a real user genuinely holding Global Administrator, live-observed) plus a real-shaped non-Global-Admin-role case and an empty case — 3/3 pass. `npx tsc --noEmit` on api-server: clean, zero errors. Genuinely still blocked/unactionable, documented in the migration header with real evidence: 12 permission gaps (app-registration Graph permission grants — production-boundary write per Git #1913, out of agent reach), 9 license gaps (Entra ID Governance purchase — same boundary), 1 recategorized-blocked (entitlementManagement transitiveRoleAssignments), 1 undesignable (directoryObjects — no consumer for resolved ids), 2 deliberate static-catalog exclusions (directoryRoleTemplates, schemaExtensions — unchanged from #2763), 2 cheap PS follow-ups still out of scope for a Graph-only pass (m365dsc O365AdminAuditLogConfig/O365OrgCustomizationSetting). Findings filed: **#2929** (map-monitor-checks.mjs absolute-URL matcher bug), parented to Feature #1128, labeled `bug`, board status "AI Batter Up". `git status --porcelain`: clean (only this session's own 3 files). Committing now; will verify branch-merged and push, then flip labels.
