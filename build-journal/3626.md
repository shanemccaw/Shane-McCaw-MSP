# #3626 — Build Queue: remove ALL status gating on blocked/blocking visibility

- **Status:** ✅ DONE 2026-09-11T03:44:58Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 05f0632b4 (BuildQueuePanel.xaml.cs change, pre-rebase) → landed on `main` as `c19a01115`

## Log
- 2026-09-11T03:42:30Z ⏳ IN FLIGHT — removing `IsBlockedEligibleStatus`'s status restriction
  entirely (forward blocked pill + ghost-card gate in `BuildQueuePanel.xaml.cs`), and removing
  the equivalent "candidate.Status != queued" gate in `ComputeReverseBlocks` for the reverse
  "Blocks:" row, per #3626's real body (explicit widening of #3624).
- 2026-09-11T03:44:58Z ✅ DONE — `IsGenuinelyBlocked` no longer calls `IsBlockedEligibleStatus`
  (deleted, no other callers); it now returns based purely on live `blocked_by` data vs.
  `_openIssues`, regardless of the item's own raw status. The ghost-card render gate in
  `BuildQueueCard` dropped the same `IsBlockedEligibleStatus` check. `ComputeReverseBlocks`
  dropped its `candidate.Status != "queued"` filter, so the #3601 reverse "🔗 Blocks:" row is
  also unrestricted by status in either direction — confirmed the reverse row's render call
  site (line ~4676) was already unconditioned on `item.Status`, so only the internal
  `ComputeReverseBlocks` candidate-side gate needed removing.
  Verified: `dotnet build` (Debug, `desktop/BuildConsole/BuildConsole.csproj`) succeeds, 0
  errors, only pre-existing unrelated `CS0067` warnings in `FocusModeBar.xaml.cs`. Rebased
  onto `origin/main` (which moved under #3623 mid-session, non-conflicting file set) and
  pushed; `verify-branch-merged.mjs` confirms `c19a01115` is an ancestor of `origin/main`.
  `git status --porcelain` clean. No genuine new finding surfaced during this build — the
  #3601 reverse-row render call site already matched the "no status gate" intent described
  in the issue; only `ComputeReverseBlocks`'s own internal filter was the gap.
