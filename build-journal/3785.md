# #3785 — New "Build Sets" slide-out panel to the left of Build Queue

- **Status:** ✅ DONE 2026-09-12T06:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** a39906953 (rebased/pushed to main; original pre-rebase commit 6cc0234ed)

## Log
- 2026-09-12T05:52:00Z ⏳ IN FLIGHT — relocating `BuildSetRollupList` (and its filter chips /
  clear affordance) out of the 180px-capped `ScrollViewer` above the Queue header
  (`Controls/BuildQueuePanel.xaml`) into a new slide-out "Build Sets" column to the left of
  the panel, toggled by a new header button, persisted via `BuildConsoleSettings`.
- 2026-09-12T06:20:00Z ✅ DONE — `BuildQueuePanel.xaml` root restructured from a single
  `DockPanel` into a 2-column `Grid` (`BuildSetsColumn` 0↔240px on the left,
  `*`-width main Queue content on the right), mirroring `FloatingChatWindow`'s
  `DockColumn`/`BtnToggleDock` (#2195) slide-out convention. New `BtnToggleBuildSets`
  header button (next to `BtnCollapseQueue`/`QueueDot`) toggles the column via
  `ApplyBuildSetsPanelState()`; state persists in new
  `BuildConsoleSettings.BuildSetsPanelOpen` (default `false`/closed). `RenderBuildSetRollup`
  and every element it populates (`BuildSetRollupList`, `RollupActivityFilterChips`,
  `BuildSetRollupClearText`, `BuildSetRollupSection`) were relocated verbatim into the new
  `BuildSetsPanelBorder` column — zero changes to that method's own logic. Removing the old
  section from the Dock=Top stack lets `QueueHeaderSection` genuinely reflow up into the
  freed vertical space. Verified with a clean `dotnet build` of
  `desktop/BuildConsole/BuildConsole.csproj` (0 errors, only pre-existing unrelated
  warnings) both before and after rebasing onto a concurrent #3786 commit that touched the
  same constructor (non-overlapping hunk, merged cleanly). Real UI click-through in a live
  BuildConsole instance was not additionally performed this session (no running instance
  was launched for it — see CLAUDE.md's rule against launching BuildConsole.exe from a build
  session to "verify"); confirmed instead via a clean build plus direct reading of the
  finished XAML/code-behind against the issue's own acceptance criteria. Working tree clean
  (`git status --porcelain` empty) and branch confirmed merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before this line was written. No findings
  filed — no genuine bug/gap surfaced outside this issue's own scope.
