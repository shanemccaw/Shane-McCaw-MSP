# #1771 — Pre-existing failing test: portal mission-control /engines severity derivation

- **Status:** ✅ DONE 2026-09-04T04:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** af952d1fd (rebased/pushed to main as aaff5f19f)

## Log
- 2026-09-04T03:50:00Z ⏳ IN FLIGHT — investigating stale assertion at `portal-mission-control.test.ts:245`; comparing against `toStatusEntry`'s health-severity thresholds in `portal-mission-control.ts` and the commit history of both.
- 2026-09-04T04:05:00Z ✅ DONE — Confirmed the endpoint is correct and the test is stale. Commit `1c594a121`
  ("Fix Inverted Health Score Display + Hero Polish") intentionally flipped the health-severity thresholds
  in `toStatusEntry` (portal-mission-control.ts) from `score < 60 ? "high" : score < 85 ? "watch" : "good"`
  to `score > 85 ? "high" : score > 60 ? "watch" : "good"`, because `health-engine.ts`'s score is a
  higher-is-worse risk sum, not a 0-100 completion percentage (same direction as security, which was
  already correctly signed). That commit updated the endpoint but never touched this test, which kept
  the pre-fix, inverted expectation (`health: "high"` for a score of 50 — true only under the old,
  buggy thresholds). Fixed the test assertion to `health: "good"` to match current correct endpoint
  behavior (50 is not > 60). Verified: full `portal-mission-control.test.ts` suite now passes
  (11/11, `pnpm exec vitest run`), and `tsc --noEmit` for api-server is clean. No endpoint code changed —
  only the stale test assertion + its comment. Rebased onto `origin/main` (moved twice mid-session) and
  pushed successfully; `verify-branch-merged.mjs` confirms `aaff5f19f` is an ancestor of `origin/main`.
  `git status --porcelain` clean at end of session.
