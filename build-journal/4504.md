# Issue #4504

- **Status:** ⏳ IN FLIGHT 2026-09-17T13:03:38Z
- **Scope:** platform

## Log

- 2026-09-17T13:03:38Z — `compliance:dlp-rule-package-invalid` keyed its `properties`/`mapping`
  on `Name`, which is an empty string on real DLP rule package objects — evidence/findings
  couldn't name the broken package. Per Microsoft Learn, the object's real identifier is
  `RuleCollectionName`. Wrote and ran
  `lib/db/migrations/manual/2026-09-17-dlp-rule-package-key-fix-4504.sql` against local
  Postgres, updating `monitor_checks.properties`/`mapping` for that check key to
  `RuleCollectionName`. Live-verified against the real testbed tenant
  (`c4c814d4-3afe-441e-9145-62461d0a4fd3`) via `run-checks-4481.ts`: real result now shows
  `RuleCollectionName_first="Microsoft Rule Package"` (non-blank), `invalidRulePackages=0`.
