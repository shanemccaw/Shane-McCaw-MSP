# #3564 — Migrate remaining GetAwaiter().GetResult() blocking calls off UI thread in MainWindow.xaml.cs

- **Status:** ✅ DONE 2026-09-10T01:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 81dfc7652

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — reading GEMINI.md / BUILD_QUEUE_METHOD.md / UI_RULES.md and #3554's reference migration pattern, then re-deriving the real current list of `.GetAwaiter().GetResult()` call sites in MainWindow.xaml.cs before migrating them.
- 2026-09-10T01:00:00Z ✅ DONE — Re-derived the real current list: 74 raw text matches, 72
  real blocking call sites (2 were pre-existing #3554 reference comments) across every
  remaining feature area (Task Queue, Support Tickets, SLA, Break-Glass, Audit Log,
  Consent, Change Control calendar-eval + PIR flow, POA&Ms + milestones, CAB
  meetings/agenda, Remediation Tracker, VIP lookup/Script Library/LaunchControl,
  Alerts, Runbooks + hold windows, Document Hub). Migrated all 72 to the exact #3554
  reference pattern (GallerySpec.GetRowsAsync: Loading → await → render), extended the
  same dual-Func pattern to a new `RibbonCommandSpec.LiveCountAsync` (Shell/ShellContracts.cs)
  + `FixedRibbonRenderer.PopulateLiveCountAsync` so badge recomputes on every
  `ShellRegistry.Changed` (every fixed-tab registration + every real SSE push, #3490) no
  longer block either. All 15 GallerySpec registrations and all 4 LiveCount registrations
  in the file now use the async path exclusively. Also fixed
  `FixedRibbonRenderer.BuildGalleryButton.Populate()` to stop firing eagerly at
  button-construction time (it fired one real HTTP request per gallery on every ribbon
  rebuild) — deferred to the real `DropDownOpened` event only, per the issue's explicit
  ask. Real, honest verification: clean `dotnet build` (0 warnings, 0 errors) re-run
  after each area's migration against real compiler errors, not guesses; final grep
  pass confirms **zero** remaining `.GetAwaiter().GetResult()` call sites in executable
  code — every surviving text match is inside an explanatory comment. Commit 81dfc7652
  pushed and confirmed merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean.
