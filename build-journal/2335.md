# #2335 — Click a note body to load it back into the composer; Enter saves, Esc cancels; EDITED tag

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole (ShaneBuilder — Test Pad feature #2326)
- **Started:** 2026-09-02
- **Commit(s):** 905e0c5a1ee1561dfb7f9a9ecba510560259d3ef

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — sub-issue of #2326 (Feature: Test Pad). TestPadWindow shell
  existed with a read-only note list and no composer at all (#2328 also not yet built). Building
  a minimal Composer (Enter files a note, wired to the existing #2330 TypeChipsControl) plus
  #2335's own scope: click a note body loads it back into the composer, Enter saves the edit in
  place (TestPadNote.IsEdited already existed on the model), Esc cancels back to empty. Sent notes
  are not clickable, matching #2336's intended lock.
- 2026-09-02T20:35:00Z ✅ DONE — Composer (TextBox + TypeChipsControl) added to TestPadWindow;
  clicking any not-yet-sent note's body loads it back into the composer with its marker restored,
  Enter saves the edit onto the same TestPadNote (via new TestPadService.UpdateNote, which sets
  IsEdited and refuses to touch an already-sent note) instead of filing a duplicate, and the row
  shows an EDITED tag once that happens; Esc clears the composer and drops out of edit mode
  without touching the note. Rebased three times against concurrent Test Pad sub-issue landings
  (#2332 status band, #2333 select/delete/SENT, #2334 by-feature grouping) that hit the same
  files — merged by hand (kept both features each time) rather than picking one side. `dotnet
  build ShaneBuilder.csproj -c Debug` succeeds after every merge and at the final commit. Branch
  verified merged into origin/main via `scripts/dev-server/verify-branch-merged.mjs`. `git status
  --porcelain` clean. No fixture/hardcoded data — reads/writes the real TestPadService in-memory
  store, same as the rest of the feature.
