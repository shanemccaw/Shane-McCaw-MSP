# #4431 — Buy.tsx Monitoring/Retainer purchases never create the recurring Stripe subscription — only month 1 is charged

- **Status:** ✅ DONE 2026-09-16T20:37:17Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 37d5f3b5e
- **Migration:** `lib/db/migrations/manual/4431-checkout-sessions-purchase-subscription.sql` (run against local; appended to #1630)
- **Findings filed:** #4438

## Log
- 2026-09-16T20:21:29Z ⏳ IN FLIGHT — creating the recurring Stripe subscription after `POST /api/public/purchase/payment-confirmed` for Monitoring/Retainer purchases (trial-anchored so month 1 isn't double-billed), storing its id on the #4403/#4404 `client_services` row, idempotent per checkout session.
- 2026-09-16T20:37:17Z ✅ DONE — `lib/purchase-recurring-subscription.ts` creates the subscription (inline price_data at the charged amount, billing_cycle_anchor one month after the month-1 charge + proration_behavior none; active, no invoice at creation), stores it on `checkout_sessions.purchase_subscription_id` and mirrors it to the (checkout_session_id, service_id) client_services row; idempotent via DB record → Stripe metadata adoption → per-session idempotency key with 409 retry. Wired into payment-confirmed (+ `recurringSubscription` in response) and the set-password / portal-handoff backstops. Verified against real Stripe test mode: live-stripe suite 5/5 (anchor, exactly one charge, full amount on next invoice, id on the right row, concurrent duplicate/first confirms → one subscription, adoption backstop, legacy link, pack no-op); purchase suites 9 files / 58 tests pass; tsc clean. Finding #4438 (skipped-consent Retainer has no Stripe customer → no subscription possible).
