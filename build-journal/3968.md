# #3968 — MSP Console: Azure credential management — UI

- **Status:** ✅ DONE 2026-09-14T15:32:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 0daca2e7c (IN FLIGHT bookend), 2534a756d (real work)

## Log
- 2026-09-14T15:25:03Z ⏳ IN FLIGHT — building the Azure Credential section on MSP Console's per-tenant detail view, wiring the real #3967 routes (`msp-azure-credentials.ts`). Implementation, typecheck, tests and build already verified locally; writing this bookend and committing.
- 2026-09-14T15:32:00Z ✅ DONE — Added `artifacts/msp-console/src/api/azure-credential-api.ts` (GET/POST/PUT/DELETE against `/api/msp/:mspId/clients/:clientUserId/azure-credential`, plus `useResolvedClientUserId` which picks the tenant's canonical client user client-side from its team roster, since #3967's routes key on `clientUserId` and have no tenant/customer resolution of their own) and `artifacts/msp-console/src/console/modules/AzureCredential.tsx` (status card, create/edit/rotate drawer, delete-with-confirm — raw secret never displayed or re-read). Wired into `nav.ts` (new `azurecred` leaf under "Access & identity"), `icons.tsx` (`cloud`), and `ConsoleShell.tsx`'s per-tenant dispatch. Also fixed `nav.test.ts`'s IA page-count assertions, which had already drifted stale (asserted `MSP_PAGES.length === 18`/`CHILD_PAGES.length === 27` against real counts of 20/29) before this build touched the file — no separate issue filed since the fix landed in this same build. Verified: `pnpm run typecheck` clean, `pnpm run test` 16/16 pass, `pnpm run build` succeeds. Branch confirmed merged into origin/main via `verify-branch-merged.mjs` (exit 0); working tree clean.
