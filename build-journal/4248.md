# #4248 — Build MSP Console UI for GET /admin/projects/:id/report-autofill

- **Status:** ⏳ IN FLIGHT 2026-09-15T14:05:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-15T14:05:00Z ⏳ IN FLIGHT — Investigated the route contract (`admin-projects.ts:918`), confirmed #4246 (relocated project detail view) has NOT landed, and confirmed the "Delivery Projects" entity (`projectsTable`, keyed by `clientUserId` -> `usersTable.id`) has no FK to `tenantsTable` (the MSP Console `customerId` axis) — same documented gap `msp-invoices.ts`/Invoices.tsx already carries, so this build follows that same precedent (own client + project picker, not tenant-inherited). Also found a genuine cross-tenant scoping gap in `admin-projects.ts` (re-gated by #4245 from `requireAdmin` to `requireCapability("ladder.msp-operator")` with zero MSP-id scoping added) — filing as its own finding and applying a minimal scoped fix to the two GET routes this build newly makes UI-reachable.
