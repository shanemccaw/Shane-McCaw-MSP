# #3091 — shaneapp://runTest never verifies/restarts the local front-end dev server it navigates uiSteps against

- **Status:** ✅ DONE 2026-09-12T22:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** cd6d766d0

## Log
- 2026-09-12T22:32:42Z ⏳ IN FLIGHT — Read issue #3091 body + comment. Confirmed the gap: `EnsureServerReadyWithProbeAsync` (MainWindow.xaml.cs) only probes `config.ApiBaseUrl` (API server :8080); nothing in `RunManifestAsync` checked the specific front-end port(s) `DevServiceRouting.OriginForServiceKey` navigates uiSteps to. Implementing a `DevServicesManager.EnsureFrontEndReadyAsync(serviceKey, channel, maxWaitSeconds)` that: starts a down service, restarts a service whose process predates the latest real commit under its own `artifacts/<name>` (staleness per the issue's own account), and polls the port before giving up — wired into `RunManifestAsync` for every distinct front-end a manifest's uiSteps actually target, before the first goto. A readiness failure now produces a clear failing synthetic uiStep result instead of silently navigating.
- 2026-09-12T22:40:00Z ✅ DONE — Shipped in cd6d766d0 (`desktop/BuildConsole/Services/DevServicesManager.cs`, `desktop/BuildConsole/MainWindow.xaml.cs`), pushed to origin/main, verified an ancestor of main via `verify-branch-merged.mjs`. `dotnet build BuildConsole.csproj` succeeds clean (0 warnings/errors) both before and after the rebase onto origin/main — no dedicated xunit test project exists for this WPF app (test-manifest rules explicitly exclude `desktop/BuildConsole`), so per the standing verification recipe for this app, Verification = clean build + code-reading confirmation of the wiring (the same one call site `RunManifestAsync`→`RunUiTestAsync` that manual runs, regression sweeps, and `shaneapp://runTest` all share, so the fix covers every caller). Did not launch BuildConsole.exe to drive live UI, per the standing single-instance-mutex hazard — that would risk driving Shane's real running instance. `git status --porcelain` clean. No genuine new findings surfaced worth filing as a sub-issue of #3707. Note: while wrapping up, `gh issue edit --add-label/--remove-label` (GraphQL) hit a real, repeatable "API rate limit already exceeded" (confirmed via 4 real attempts across `gh issue edit` and the claude.ai GitHub MCP, while `gh api rate_limit` itself reported 5000/5000 remaining — a shared governor distinct from GitHub's own quota) — worked around by using `gh api` REST endpoints instead (`POST/DELETE .../issues/3091/labels`, `POST .../issues/3091/comments`), which were unaffected.
