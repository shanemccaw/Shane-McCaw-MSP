# #1556 — SOPs/Runbooks: Unify: one procedure definition, one run record

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `eb771842d` (work), `546bf673c` (bookend IN FLIGHT) — both on `origin/main`, ancestor check passed
- **Migration:** `lib/db/migrations/manual/2026-08-30-sop-run-origin-1556.sql` — run against local Postgres in-session, logged on #1630
- **Findings filed:** none

## Log
- 2026-08-30 ⏳ IN FLIGHT — Unifying the SOP/Runbook procedure model onto the surviving pair (`msp_sops` definition + `msp_sop_runs` run record). Adding `origin` (`policy | lifecycle | remediation | manual`) as a property of the run so a policy-invoked, lifecycle, remediation or hand-started run is the same object with different provenance. `portal_runbooks`/`portal_runbook_steps` is a different object (the customer's active runbook checklists) and is left as-is. Schema + additive migration + writer default + read wire contract; ends at the wire contract (no page — none exists under `artifacts/portal`).
- 2026-08-30 ✅ DONE — Shipped `msp_sop_runs.origin` as the unification's provenance axis.

## What shipped

- **`lib/db/src/schema/msp.ts`** — `MSP_SOP_RUN_ORIGIN = ["policy","lifecycle","remediation","manual"]` (const union + `MspSopRunOrigin` type) and a new `origin` column on `mspSopRunsTable`, `notNull().default("manual")`. Vocabulary enforced in code (enum union), no DB CHECK — the widen-in-code convention `status` already follows. The section header now documents that this run record is THE run record whatever invoked it, and that `portal_runbooks`/`portal_runbook_steps` is a different object sharing the word.
- **`lib/db/migrations/manual/2026-08-30-sop-run-origin-1556.sql`** — `ALTER TABLE msp_sop_runs ADD COLUMN IF NOT EXISTS origin text NOT NULL DEFAULT 'manual'`, plus the self-marking `simulator_migration_runs` insert. Run against local `DATABASE_URL` in-session; column verified present, `NOT NULL`, default `'manual'`.
- **`artifacts/api-server/src/routes/msp-sops.ts`** — `POST /api/msp/sop-runs` now accepts an optional `origin` (`z.enum(MSP_SOP_RUN_ORIGIN).default("manual")`) and writes it. Existing callers keep working; a run without a stated origin is a hand-started one.
- **`artifacts/api-server/src/routes/portal-sops.ts`** — `origin` surfaced on the read wire contract: added to `WireSopRunSummary` (per-SOP run history on `GET /portal/sops`) and `WireSopQueueItem` (live queue on `GET /portal/sop-runs`), populated from `r.origin`. The raw enum value only — no invented display vocabulary; the page owns any label. This is the SCOPE STOP: the contract now carries provenance, no page was built (none exists under `artifacts/portal`).

## Verification

- Migration applied to local Postgres; `information_schema` confirms `origin text NOT NULL DEFAULT 'manual'`.
- Transactional insert test (rolled back): a run inserted without `origin` reads `manual`; one inserted with `origin='policy'` reads `policy`. `msp_sop_runs` row count unchanged (still 0).
- `npx tsc --noEmit` on `artifacts/api-server` is clean for both edited route files (unrelated pre-existing errors elsewhere — #1765/#1772/#1774/#1777/#1778/#1781 — are not mine). `lib/db` declaration build (`tsc -b`) succeeded and the new exports are in the shared dist.

## Notes for the next session

- Nothing writes `msp_sop_runs` from an automated hook yet (#1559) — that wiring is downstream (the policy engine #1548, lifecycle #1552, remediation #1539). This issue lands only the unified data model + contract those will write against; each connects its own writer and sets its own `origin`.
