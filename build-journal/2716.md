# #2716 — Dispatch with no BUILD: comment yet should auto-retry on the next board refresh

- **Status:** ✅ DONE 2026-09-03T19:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 92e3a1df0

## Log
- 2026-09-03T18:52:00Z ⏳ IN FLIGHT — implementing the 3-point plan: pending-retry set on
  `DispatchPanel` populated in the `NoBuildComment` case, a `RecheckPendingBuildCommentsAsync`
  method that re-runs `IssueDispatchService.DispatchAsync` (which reuses
  `BatterUpQueueService.FindBuildCommentAsync`) for every pending issue, wired into the same
  `LeftSidebar.BoardRefreshCompleted` cascade #1813/#2557/#2688/#2711 already extend, with honest
  toast/log lines on auto-dispatch success and on give-up.
- 2026-09-03T19:05:00Z ✅ DONE — shipped in `92e3a1df0` ("Auto-retry Dispatch when no BUILD:
  comment yet (#2716)"), pushed to `main`. `dotnet build BuildConsole.csproj -c Debug` succeeded
  clean (0 warnings, 0 errors). `_pendingBuildCommentRetries` (issue → `{IssueTitle,
  FirstAskedUtc, RecheckAttempts}`) is populated in `DispatchAsync`'s `NoBuildComment` case right
  after asking the active chat; `RecheckPendingBuildCommentsAsync` is wired onto
  `LeftSidebar.BoardRefreshCompleted` in `MainWindow.xaml.cs` alongside the existing
  #1813/#2557/#2688/#2711 hooks on the same event. Each recheck re-runs the real
  `IssueDispatchService.DispatchAsync` path — on `Queued`/`QueuedButBlocked` it removes the entry,
  logs, fires a success toast, and raises `Dispatched` (same visual-refresh hook manual dispatch
  uses); on a terminal outcome that a later comment can't fix (`AlreadyTracked`/`NoPat`/
  `GitHubUnreachable`/`IssueNotFound`/`NoDb`/`Failed`) it stops retrying with a log+toast; on
  `NoBuildComment` it keeps waiting until 30 rechecks or 12h elapsed, then gives up with an honest
  warning toast. A manual Dispatch click for the same issue number clears any pending entry first
  so it can't race a fresh manual attempt. `verify-branch-merged.mjs` confirms `agent/2716-q1461`
  (92e3a1df) is merged into `origin/main`. `git status --porcelain` clean after this commit.
  Not live-UI-verified inside a running BuildConsole instance per Git #2156 (never launch
  BuildConsole.exe / drive its UI from a build session) — verified by clean build + code
  inspection of the exact real methods it reuses (`IssueDispatchService.DispatchAsync`,
  `BatterUpQueueService.FindBuildCommentAsync`, `LeftSidebar.BoardRefreshCompleted`).
