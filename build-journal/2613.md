# #2613 — M365 Launch Control: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 3498c2935 (contract pack), rebased+pushed to `main` as a3908afa5

## Log
- 2026-09-03T16:15:00Z ⏳ IN FLIGHT — read `msp-launch-control.ts` (3 routes: list actions, execute,
  rollback), pulled #2613/#1642/#2494 from GitHub. Extracting the #1642-pattern contract pack next:
  wire contracts per route cited to file:line, real enum unions, cross-surface edges (entitlement,
  catalog→template promotion, audit/rollback), honest tri-state (`included`/`billable_upsell`/
  `a_la_carte`), forbidden list, and live-DB counts. Full line-by-line audit given real tenant-write
  stakes.
- 2026-09-03T18:10:00Z ✅ DONE — wrote `docs/msp-launch-control-contract-pack.md` (3 surfaces:
  GET actions, POST execute, POST rollback), full line-by-line audit including
  `lib/workflow-executor.ts` (runBaselineTemplateAgainstTenant, rollbackExecution),
  `lib/msp-entitlement.ts`, `lib/graph.ts` (graphWriteForTenant's 2 write-back gates),
  `middlewares/requireAuth.ts`, and live DB counts (write_action_catalog: 123 rows/0 with
  template_id; baseline_action_templates: 102 rows/0 reversible; audit log: 25 rows, all
  source=simulator, 0 with before_snapshot). Found and filed 2 real, verified findings as sibling
  sub-issues of the Feature (#2494), both parented + board status set to "AI Batter Up":
  - **#2702** — `write_action_catalog.template_id` is 0/123 populated → POST execute always 409s
    despite 102 real, active `baseline_action_templates` existing unlinked.
  - **#2703** — rollback is completely inert: 0/102 templates marked `reversible`; traced the root
    cause to `lib/db/migrations/manual/2026-07-21-launch-control-rollback.sql`, which is recorded
    in `simulator_migration_runs` as having run, but whose 6 target `template_id` values do not
    exist anywhere in the live `baseline_action_templates` table (a different, untracked seed
    mechanism produced the live 102 rows under a different naming convention).
  Verification: `node scripts/dev-server/verify-branch-merged.mjs` → OK, merged into `origin/main`
  as a3908afa5. `git status --porcelain` clean. Read-only build — no product code/schema/UI
  changed, per #2613's own scope.
