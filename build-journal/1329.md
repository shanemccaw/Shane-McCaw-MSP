# #1329 — Comprehensive testbed reset

- **Status:** ✅ DONE 2026-09-05T00:34:57Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 547acd6e4 (bookend IN FLIGHT)

## Log
- 2026-09-05T00:28:42Z ⏳ IN FLIGHT — Read #1329's full body + comments. Found the comprehensive reset migration this issue asked for was already built and landed on `main` back on 2026-08-26 (`beb1c36bb`), then further extended and folded (per #1471/#1396/#1573) into the single canonical file `lib/db/migrations/manual/2026-08-27-testbed-reset-patch-projects-1396.sql`, which is now the self-sufficient reset covering all 60+ scoped tables plus the projects/orphan-project FK-dependent gap. `simulator_migration_runs` shows it last actually ran 2026-08-30 05:53:04, ~2 minutes before its final edit (`fed5155f6`, the orphan-project sweep) landed — so the orphan-project block has never actually executed against local data, and 5+ days of build-session activity since then has accumulated fresh test residue. No new SQL/schema work is needed for #1329 itself; re-running the existing canonical file against local Postgres to actually clear that residue now.
- 2026-09-05T00:34:57Z ✅ DONE — Ran `lib/db/migrations/manual/2026-08-27-testbed-reset-patch-projects-1396.sql` (unchanged file, no new migration authored — #1329's own coverage is already folded into it) directly against local `DATABASE_URL`. Real result: 4,573 rows deleted across 22 tables that had real accumulated data (msp_diagnostic_findings 755, tenant_monitor_profiles 1240, tenant_check_item_details 579, overshared_items 396, msp_message_center_items 538, m365_service_health_samples 325, platform_log_stream 535, msp_event_store 36, msp_audit_logs 17, ai_usage_events 52, and 12 smaller tables), self-marking `simulator_migration_runs` row updated. Verified post-run: kept identity/consent tables (`tenants`, `consent_invite_tokens`) intact and unchanged; excluded MSP-infra tables (`msp_sharepoint_connectors`, `msp_mailbox_connectors`) untouched; spot-checked reset tables (`tenant_monitor_profiles`, `msp_diagnostic_findings`) now read 0 rows for the testbed tenant. No code change was needed — #1329's real work (schema/migration authoring) had already shipped in prior sessions; this session's job was executing it against current DB state. `git status --porcelain` clean, branch `agent/1329-q1683` (547acd6e4) confirmed merged into `origin/main` via `verify-branch-merged.mjs`. No new findings filed — no gaps discovered beyond what prior sessions' migration comments already documented (the two deliberately-excluded `client_services`/`tenant_add_on_entitlements` tables, and the admin-projects.ts null-clientUserId gap, both already noted in the migration file's own comments from prior sessions).
