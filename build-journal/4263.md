# Issue #4263 — POST /admin/run-script writes no audit row for tenant script execution

- **Status:** ✅ DONE 2026-09-15T16:22:33Z
- **Scope:** platform

## Log
- 2026-09-15T16:20:35Z — Starting. Adding `createAuditLog` call in `admin-m365-run.ts` right after the Azure Automation job is created (real `runResultId`/`jobId` exist), plus one for the refused cross-MSP kanbanTaskId 404 path. #4262 (dead-stub migration) has not landed in this worktree as of this commit.
- ✅ DONE 2026-09-15T16:22:33Z — Commit 6cb0be7af: added `createAuditLog({ actionType: "script_run_started", ... })` right after the Azure Automation job is created and `jobId` is stored (real `runResultId`/`jobId`, `actorUserId`/`actorName`/`actorRole` from `req.user`, `entityType: "script_run"`, `entityId: jobId`, `clientId: customerId`, metadata with `runbookName`, `credentialId`/`appRegistrationId`, `kanbanTaskId`, `mspId`), and a second `createAuditLog({ actionType: "script_run_refused", actionCategory: "security", ... })` in the cross-MSP kanbanTaskId 404 refusal path. `tsc --noEmit` clean for the file. `verify-branch-merged.mjs` confirms `agent/4263-q2931` (6cb0be7af) is an ancestor of `origin/main`. `git status --porcelain` clean. No #4262 migration landed in this worktree — nothing to coordinate with in-session.
