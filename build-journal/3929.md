# 3929 — Retire portal-v2: runbooks — successor #1488 closed

- **Status:** ✅ DONE 2026-09-13T00:00:00Z
- **Scope:** platform

## Log
- 2026-09-13T00:00:00Z — IN FLIGHT. Investigating #3929 (Group A retirement checklist item under #1647) for `portal-v2-runbooks.tsx`.
- 2026-09-13T00:00:00Z — Verified no-op: `artifacts/msp-portal` (including `portal-v2-runbooks.tsx`, its `RunbookSteps.tsx` component, and route/nav registration) was already deleted wholesale in commit `f40438cdc` ("Portal scaffolding: create artifacts/portal, retire artifacts/msp-portal"). `grep -rn 'portal-v2-runbooks' artifacts/` returns zero hits (the `artifacts/msp-portal` directory no longer exists at all).
- `govOversharingRunbooksLive.ts` and `holds/useRunbooks.ts` (the old `*Live.ts` hooks) were carried forward — not deleted — because `artifacts/portal/src/pages/sops.tsx` (the real successor SOPs page, #1488) genuinely reuses `useRunbooks()` for its Active Runbooks section (hold-window summary banner, `GET /api/portal/runbooks`). Confirmed: `sops.tsx:45` calls `useRunbooks()`, `sops.tsx:90` reads `runbooksPayload?.summary`.
- No code changes required in this session — #3929's checklist was already satisfied by prior commit f40438cdc. No findings.
- ✅ DONE 2026-09-13T00:00:00Z — No-op verification only. Commit: this bookend commit itself (no other changes). Verified via `grep -rn 'portal-v2-runbooks' artifacts/` (zero hits) and reading `artifacts/portal/src/pages/sops.tsx` to confirm the live Active Runbooks wiring.
