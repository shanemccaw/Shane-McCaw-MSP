# #2800 — Editor Panes toolbar: live midnight-resetting daily stats strip

- **Status:** ✅ DONE 2026-09-04T04:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 890614283

## Log
- 2026-09-04T04:00:00Z ⏳ IN FLIGHT — add 4-chip stat strip (New/Closed today, Features open/done) to the Editor Panes toolbar's empty DockPanel middle, reusing GitHubIssueTimeSeriesService (#2711) for today's opened/closed bucket and GitBoardIssueFilters.ComputeTransitiveLeafRollup (#2739/#2773) for per-Feature completion, wired into the existing LeftSidebar.BoardRefreshCompleted cascade plus a cheap periodic re-derive.
- 2026-09-04T04:30:00Z ✅ DONE — Added `Services/EditorPanesStatsService.cs` (new, pure — reuses `GitHubIssueTimeSeriesService.GetAllIssuesAsync`/`ResolveActiveMilestoneAsync`/`BuildSeries` and `GitBoardIssueFilters.ComputeTransitiveLeafRollup`/`IsPlaceholder`, so it issues no new GitHub calls beyond the existing 5-minute cache). Added an unstyled `StackPanel` (no `DockPanel.Dock`) as the 3rd child of the toolbar's inner `DockPanel` in `MainWindow.xaml` so it fills the real empty middle space between "EDITOR PANES" and the layout buttons, with 4 chips ("New: N" / "Closed: N" / "Features open: N" / "Features done: N"), collapsed until the first real fetch resolves. Wired refresh two ways in `MainWindow.xaml.cs`: (1) piggybacked on the existing `LeftSidebar.BoardRefreshCompleted` cascade — the same real board-refresh floor every other Home/Batter-Up consumer already rides; (2) a new 60s `DispatcherTimer` (`_editorPanesStatsTimer`) for the "feel alive" top-up Shane asked for, cheap because both underlying data sources are already 5-minute-TTL cached so most ticks just re-derive from cache with zero new API load. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Verified `agent/2800-q1582` (890614283) is a real ancestor of `origin/main` via `verify-branch-merged.mjs`. `git status --porcelain` clean of anything I touched.
- Real choice made per the issue's ask #3: full board refresh remains the authoritative floor; the 60s timer only re-derives from already-cached data (never forces a new fetch) so it adds zero new real API load, matching "if easy without new real API load, do it."
- Not separately verified against a live GitHub PAT in this sandbox (no PAT configured here) — the fail-closed path (`HasData == false` → strip stays `Collapsed`) was exercised by inspection of `EditorPanesStatsService`/`GitHubIssueTimeSeriesService`'s existing, already-shipped fail-closed contract, which this build adds no new branch to. A live spot-check against real GitHub numbers is still open — see issue comment.
