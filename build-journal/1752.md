# #1752 — Rename `war-room` to `pillars` across the API

- **Status:** ✅ DONE
- **Scope:** platform

## Log

- ⏳ IN FLIGHT — starting. Renaming `GET /api/portal/assessment/war-room-pillars` -> `GET /api/portal/pillars`,
  `GET /portal/assessment/pillar-history` -> `GET /api/portal/pillars/history`, `lib/war-room-pillar-stats.ts` ->
  `lib/pillar-summary-stats.ts`, `buildWarRoomPillarStats` -> `buildPillarSummary`, and sweeping comment prose /
  identifiers across the ~39 files `git grep` found. No behavior change — rename only, `WAR_ROOM_ENGINE_PILLAR`'s
  `health` -> `architecture` mapping is preserved exactly, only the constant name changes. `requireRole("Assessment")`
  and the `/portal/assessment/*` prefix are explicitly out of scope per the issue.

- ✅ DONE — commits `b1384f591` (rename) and `55b03fba2` (test-manifest `lastVerifiedAgainstCommit` stamp),
  pushed and merged to `origin/main`. Renamed both route paths, `lib/war-room-pillar-stats.ts` ->
  `lib/pillar-summary-stats.ts` (+ its test file), `buildWarRoomPillarStats` -> `buildPillarSummary`,
  `WAR_ROOM_ENGINE_PILLAR` -> `ENGINE_PILLAR_FOR_DISPLAY_PILLAR` (the `health` -> `architecture` mapping is
  byte-identical, only the name changed), and every other `WarRoom*`/`WAR_ROOM_*` identifier in the same file
  (`WarRoomPillarKey` -> `PillarSummaryKey`, `WarRoomStat*` -> `PillarStat*`, `WarRoomPillarFinding` ->
  `PillarFinding`, etc). Swept comment prose and identifiers across all 39 files `git grep -rl
  "war-room\|warRoom\|WarRoom" -- artifacts lib` found on `main`, plus the 5 `test-manifests/portal/*.json`
  files whose `apiTests[].path` literally called the old route (mechanical path-only edit so existing
  coverage doesn't start 404ing — not new front-end work, no UI touched).
  Left alone per the issue's explicit scope: the `/portal/assessment/*` prefix (#1753, blocked on a
  decision), `requireRole("Assessment")` (still reads `"Assessment"` — RBAC rename is #1696/#1704's job),
  and two categories of stale prose that name files/tables this repo doesn't have: `msp-portal`'s
  `warRoomScan.ts`/`warRoomGovernanceWalk.ts` (retired portal-v2 files, don't exist here) and
  `war_room_interaction_events` (a real, unrelated DB table some migration scripts reference — not part of
  this endpoint at all).
  **Verification:** `tsc --noEmit` clean on every file this session touched (repo-wide run surfaces 53
  pre-existing errors in files this session never touched, confirmed present regardless of this change).
  `vitest` green across all 15 touched-lib test files except one pre-existing failure in
  `registry-source-key-contract.test.ts` (confirmed present in the code before this rename's commit —
  filed as #1758, parented to #1096, root-caused to a stale #441-era assertion never updated after #1115
  legitimately re-added the `adoption.*` stat specs). Live-verified against the real local Postgres testbed
  tenant (customer 1 / `c4c814d4-3afe-441e-9145-62461d0a4fd3`, `mccawsoft2.onmicrosoft.com`): built and ran
  the api-server locally (`node build.mjs` + `node dist/index.mjs`), signed a real JWT, and confirmed
  `GET /api/portal/pillars` returns all seven real pillar keys/scores (governance:54 licensing:50
  adoption:82 compliance:70 health:55 security:55 copilot:49) and `GET /api/portal/pillars/history` returns
  real snapshot history; both old paths now 404. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
  **Finding filed:** #1758 (stale `registry-source-key-contract.test.ts` assertion, parented to #1096,
  board status set to "AI Batter Up").
