# #2643 — Offers and SOW Acceptance: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T06:38:52Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 946e7319d (merged into main as 8c0b23513, on top of concurrent
  rebases — the real committed diff is unchanged across those rebases)

## Log
- 2026-09-12T05:39:14Z ⏳ IN FLIGHT — investigation done (design spec, real backend
  routes in msp-sales-offers.ts/msp-sow.ts/msp-sales-bundles.ts/msp-marketplace-purchase.ts,
  prior #2653 pattern); implementation (sales-api.ts client + Sales.tsx module +
  ConsoleShell wiring + a small msp-sow.ts offerId-filter addition) and
  typecheck/build verification already done in-session — writing this bookend
  late in the sequence rather than as the true first step; about to commit.
- 2026-09-12T06:38:52Z ✅ DONE — Shipped `artifacts/msp-console/src/api/sales-api.ts`
  (new) and `artifacts/msp-console/src/console/modules/Sales.tsx` (new), mounted
  at `/ops/sales` in `ConsoleShell.tsx`. Wires the real Offers list/filter/state
  machine, the real generate-offers and close-stale-offers actions, the real
  accept path (`POST /api/msp/offers/:offerId/accept`, never the PATCH
  `.../state` route, which 422s on "accepted" by design), a SOW view drawer
  (detail, share-link copy, open-document, trigger-charge, expire), the real
  Bundles CRUD + inline assign/revoke, the real Packages catalogue, and a
  cross-bundle "who has what" tab. Small additive backend change: added an
  `offerId` query filter to the existing `GET /api/msp/sows` list route
  (msp-sow.ts) — `offerId` is already an indexed column, not a schema change.
  No fixture module, no fabricated row.

  **Marketplace-purchase checkout action — explicitly NOT a hardened payment
  flow.** Per the dispatch's own instruction, the drawer states plainly,
  before the accept click, that this is real and final and names the open
  financial-correctness risk on this specific path: #3400 (a failed paid
  checkout can strand an offer permanently "accepted" with no charge
  collected), #3405 (free/$0 checkout ignores `services.allow_free_checkout`),
  #3650 (the add-on/subscription checkout has no webhook consumer and can
  mis-target the customer email). No "processing guaranteed" language
  anywhere in the UI. All three issues remain open and unresolved by this
  build — this build only surfaces the real endpoint honestly, it does not
  fix those three.

  Findings filed as sub-issues of #2568 (Feature: Offers and SOW Acceptance),
  labelled `bug`, board status "AI Batter Up":
  - #3787 — `GET /api/msp/monitoring-packages` registered twice
    (msp-sales-bundles.ts silently shadows msp-diagnostics.ts's version),
    breaking the Diagnostics module's package-picker `checkCount` field since
    #2653 shipped. Not fixed here (out of this issue's scope) — Sales.tsx's
    own catalogue read (`useSalesMonitoringPackages`) documents which of the
    two routes actually answers and why.
  - #3788 — no frontend page anywhere renders the public SOW share link
    (`GET`/`POST /api/public/sows/:shareToken`). The real backend route
    works; nothing on the customer side of `artifacts/msp-portal` (or
    elsewhere) consumes it. This build's SOW view drawer surfaces the
    generated share link (with an honest note that nothing renders it yet) —
    it does not build the missing customer-facing page, which is portal
    frontend work outside this MSP-Console-scoped issue.

  Also discovered mid-build, unrelated to my own diff: this worktree's
  `lib/db/dist` declaration output was stale relative to three concurrent
  sessions' schema additions that had already landed on `main`
  (Communications Push #3769, Training Session Log #3770, Kanban #3773) —
  `artifacts/api-server`'s `tsc --noEmit` reported "no exported member" for
  their new tables purely because TS project references resolve
  `@workspace/db` through `lib/db`'s built `.d.ts`, not live source. Ran
  `npx tsc -p lib/db` to regenerate the declarations (no `pnpm install`,
  no dependency-graph change, purely rebuilding this package's own emitted
  types) — resolved cleanly, not filed as a finding since it's a normal,
  expected step after a worktree pulls in schema changes via rebase, not a
  product bug.

  **Verified:** `npx tsc --noEmit -p artifacts/api-server` clean; `npx tsc
  --noEmit -p artifacts/msp-console` clean; `PORT=5198 npx vite build`
  (msp-console) succeeds; `npx vitest run src/routes/msp-sow.test.ts
  src/routes/msp-sales-offers.test.ts src/routes/msp-sales-bundles.test.ts` —
  62/62 passing (the `offerId` filter addition did not touch existing
  behavior; no new test was added for that one-line filter given its size —
  an honest, proportionate gap, not a hidden one). No `shaneapp://runTest`
  manifest written or run — `artifacts/msp-console` is MSP-staff-internal
  operator tooling, outside the CLAUDE.md test-manifest mandate's scope
  (`artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/` only).

  Rebased three times over concurrent sibling MSP Console module builds
  (#2659 Executive View, #2656 Scope & SLA, #2647 Documents, #2594
  Ownership/RACI) — all touched the same `icons.tsx`/`ConsoleShell.tsx`
  files; kept both sides on every conflict. Branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` (8c0b2351). Working tree
  clean (`git status --porcelain` empty) before writing this line.
