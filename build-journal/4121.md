# Build Journal — #4121

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** platform (test-manifests)

## Log
- 2026-09-14T00:00:00Z — Started. Retiring `test-manifests/copilot-readiness/money-path-login-direct.json` per Shane's decision: the scene-based `/copilot-readiness` UI it targets (data-cj-scene, verdict-score, pillar-headline/pillar-score testids) has zero current UI equivalent anywhere in `artifacts/*/src/`. Following the same convention #4103 used for its sibling manifests (`test-manifests/portal/governance-area-cards.json`, `security-drilldowns.json`): full deletion, no archive path — confirmed via `git show --stat 9634cb665` (the #4103 commit) that those files were deleted outright, not moved. Confirmed fresh that `money-path-login-direct.json` is not referenced in `test-manifests/_regression-suite.json` (`grep -n` returned no match).
- 2026-09-14T00:20:00Z — ✅ DONE — Deleted `test-manifests/copilot-readiness/money-path-login-direct.json` (commit `39370586d`, "Retire dead money-path-login-direct.json manifest (#4121)"), rebased onto `origin/main` and pushed directly to `main` at `94b4eac63`. `verify-branch-merged.mjs` confirmed the branch tip is an ancestor of `origin/main`. `git status --porcelain` clean. No genuine new findings surfaced beyond what #4121's own issue body already documented.
