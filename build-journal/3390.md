# #3390 — MCP server scaffold + bearer auth + PAT held server-side only

- **Status:** ⏳ IN FLIGHT 2026-09-10T00:47:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** <real hash(es) — fill at DONE>

Sub-issue of #3377 (Feature: GitHub MCP Server). Foundation for 6 sibling tool issues.

## Log
- 2026-09-10T00:47:08Z ⏳ IN FLIGHT — Building the real GitHub MCP server scaffold at
  `artifacts/github-mcp-server`, modeled on Shane's Life's bearer-token HTTP MCP pattern
  (`web/shanes-life`): JSON-RPC 2.0 over Streamable HTTP, `Authorization: Bearer ghmcp_…`
  minted-once/hashed-store tokens, the GitHub PAT held server-side only (read from
  `GITHUB_MCP_PAT`/`GIT_PAT`, never returned in any tool input/output), and a Recent-Activity
  audit trail in local Postgres. Issue-CRUD/sub-issue/blocked_by/comment/close/board tools are
  deferred to the sibling sub-issues; this lands only the auth+PAT+audit spine plus the
  foundational `server_status` / `github_whoami` / `get_recent_activity` tools that prove it.
