# #3390 — MCP server scaffold + bearer auth + PAT held server-side only

- **Status:** ✅ DONE 2026-09-10T01:01:03Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 168ba9a29a6fc2e4cab6bc6956cdb4b586686b8a (impl), 7c4a9674e (IN FLIGHT bookend)

Sub-issue of #3377 (Feature: GitHub MCP Server). Foundation for 6 sibling tool issues.

## Log
- 2026-09-10T00:47:08Z ⏳ IN FLIGHT — Building the real GitHub MCP server scaffold at
  `artifacts/github-mcp-server`, modeled on Shane's Life's bearer-token HTTP MCP pattern
  (`web/shanes-life`): JSON-RPC 2.0 over Streamable HTTP, `Authorization: Bearer ghmcp_…`
  minted-once/hashed-store tokens, the GitHub PAT held server-side only (read from
  `GITHUB_MCP_PAT`/`GIT_PAT`, never returned in any tool input/output), and a Recent-Activity
  audit trail in local Postgres. Issue-CRUD/sub-issue/blocked_by/comment/close/board tools are
  deferred to the sibling sub-issues; this lands only the auth+PAT+audit spine plus the
  foundational `server_status` / `github_whoami` / `get_recent_activity` tools that prove it.
- 2026-09-10T01:01:03Z ✅ DONE — Landed `artifacts/github-mcp-server` (new `@workspace/github-mcp-server`).
  Hand-written JSON-RPC 2.0 over Streamable HTTP + bearer auth (`ghmcp_` tokens, minted once /
  SHA-256-only store / revocable), GitHub PAT read from `GITHUB_MCP_PAT`/`GIT_PAT` and consumed
  only inside `src/github.ts` (scrubbed from errors), Recent-Activity audit trail. Tables
  `github_mcp_tokens` + `github_mcp_activity`: Drizzle defs in `lib/db/src/schema/index.ts` +
  manual migration `2026-09-10-github-mcp-tokens-and-activity-3390.sql` **run against local
  Postgres in-session** (release-time Replit/Staging line added to #1630).
  **Verification (real):** `tsc` clean on the package AND on `lib/db`; `scripts/smoke-check.mjs`
  boots the server, mints a token, and passes every check — missing/invalid/revoked bearer all
  rejected 401, `github_whoami` returns the real login `shanemccaw` proving the server-side PAT
  works, and **the PAT never appears in any response body** (the issue's core security assertion).
  Branch merged to `origin/main` (`verify-branch-merged.mjs` OK). Working tree clean.
  Findings filed: none (see completion comment). Issue-CRUD/sub-issue/blocked_by/comment/close/board
  tools remain the sibling sub-issues of #3377 — the scaffold's `src/tools/` extension point is ready.
