# #3085 — shaneapp://runTest dev server missing BASE_PATH=/portal/ — every real Portal UI test fails at login

- **Status:** ✅ DONE 2026-09-07T16:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** `ae4d3dc47` (fix, on `agent/3085-q1851`), `39cce827c` (merge into `main`, pushed to `origin/main`)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — pulled issue body + Shane's follow-up comment (must hold in BOTH the local/dev-server context AND real Replit Staging, not just one). Confirmed `artifacts/portal/vite.config.ts` already has a self-heal (unset/`/` BASE_PATH → `/portal/`), added in the initial portal scaffold (f40438cdc, Aug 29) — investigating whether that alone is sufficient or whether shaneapp://runTest actually targets a separate/stale dev-server checkout (same class as #3054) and whether `.replit`/artifact.toml sets BASE_PATH for Staging.
- 2026-09-07T16:40:00Z ✅ DONE — Real fix + real verification.
  - **Root cause status at start:** the local self-heal in `vite.config.ts` and Staging's `artifacts/portal/.replit-artifact/artifact.toml` (`BASE_PATH = "/portal/"`, real Replit-native config, not touched by anything in this repo's scripts) were **already both correct** before I touched anything — confirmed by directly curling the live dev server (`http://localhost:5175/portal/login` → 200 with `/portal/`-prefixed asset URLs; `http://localhost:5175/` → 302 to `/portal/`) before any edit.
  - **What I changed:** per Shane's explicit "set consistently everywhere, not relying on a fallback" ask — `scripts/dev-server/services.json` now carries an explicit `basePath: "/portal/"` on the `portal` service entry (single source of truth already shared with `desktop/BuildConsole/Services/DevServicesManager.cs`), and `scripts/dev-all.mjs`'s `startService()` reads `svc.basePath ?? "/"` instead of hardcoding `BASE_PATH="/"` for every service. Local dev, Staging (`artifact.toml`), and Production (`.replit`) now all state `/portal/` outright instead of local alone depending on `vite.config.ts`'s own fallback (left in place as defense-in-depth for anyone running `pnpm dev` directly).
  - **Verification, for real:** pushed to `origin/main` (`39cce827c`), fast-forwarded the actual mainRepoRoot checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` — the checkout `shaneapp://runTest` actually runs against, same one #3054 found stale) to match, restarted the Portal (5175) and API-server (8080) dev processes so the explicit BASE_PATH was live. Triggered `shaneapp://runTest?file=test-manifests/portal/risk-register.json&onlyTag=setup` for real, in-process. Result: **8/8 steps passed** — real `POST /api/auth/login` (200, real bearer token), `goto http://localhost:5175/portal/login` rendered the real login form, real credential submit, landed on the real authenticated `/portal/risk-register` page and interacted with real DOM.
  - **Staging:** could not (and structurally should not) run a live `shaneapp://runTest` against Staging — `MainWindow.ShaneAppRunTest.cs` hard-locks every `runTest` call to `TargetEnvironment.Dev` (its own "[SECURITY BOUNDARY] ... HARD-LOCKED to TargetEnvironment.Dev" comment), the same Git #1828 gate CLAUDE.md documents. Confirmed Staging's real config source (`artifact.toml`) is correct instead; a live Staging run stays Shane's own manual "Deploy to Staging" action.
  - **Findings filed** (parented under #1202, milestone v1.1, `bug`, board "AI Batter Up"): **#3090** (`account-security-mfa-enrollment.json` uses bare `/login`/`/account-security`, missing the `/portal/` prefix every sibling manifest uses, so it misroutes to the Marketing dev server locally) and **#3091** (`shaneapp://runTest` never verifies/restarts the specific front-end dev server process a manifest navigates to — only the API server is probed, so a stale long-lived front-end process could silently serve wrong config with nothing catching it — the same failure shape this issue describes, though not what actually happened here).
  - **Not re-filed:** #3059 (`shaneapp://runTest` failing to resolve any `test-manifests/portal/*` manifest at all) — still open, no build-journal entry or fix commit found, a distinct (resolution, not routing) failure mode from this issue.
  - **Working tree:** clean in this worktree, in the mainRepoRoot checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`), and in `C:\dev-server` — nothing left uncommitted.
  - Comment posted on #3085; `in-flight`→`complete` label synced.
