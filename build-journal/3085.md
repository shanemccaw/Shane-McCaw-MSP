# #3085 — shaneapp://runTest dev server missing BASE_PATH=/portal/ — every real Portal UI test fails at login

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — pulled issue body + Shane's follow-up comment (must hold in BOTH the local/dev-server context AND real Replit Staging, not just one). Confirmed `artifacts/portal/vite.config.ts` already has a self-heal (unset/`/` BASE_PATH → `/portal/` canonical), added in the initial portal scaffold (f40438cdc, Aug 29) — investigating whether that alone is sufficient or whether shaneapp://runTest actually targets a separate/stale dev-server checkout (same class as #3054, where `mainRepoRoot` = `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` is the real checkout BuildConsole's dev servers run from, not this worktree) and whether `.replit`/artifact.toml sets BASE_PATH for Staging.
