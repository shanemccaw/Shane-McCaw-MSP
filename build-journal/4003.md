# #4003 — Documents: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:16:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** bc083d755 (Documents page), 269d4c068 (test manifest add+register), ff79050e1 (manifest verification evidence)

## Log
- 2026-09-14T03:35:00Z ⏳ IN FLIGHT — Investigated #4003, #1658, #2825, #2866, the documents-contract-pack.md, Documents.dc.html (incl. its logic class), portal-documents.ts (all 8 routes), portal-url.ts, and the current Design/portal screens list. Confirmed: no `.dc.html` export exists anywhere for a live "Document Viewer" route (`/{slug}/copilot-readiness/documents/:id`), and Documents.dc.html's own logic class explicitly designs AROUND that gap (disabled View/PDF buttons + tooltip citing #2507 for the 7 LIVE_RENDERED_DOC_TYPES) rather than fixing it. So #2825/#2866 are NOT resolved by this build — building the Documents list/share page is real, separate work from the still-missing live journey Document Viewer. Proceeding to build customer-documents.tsx (authenticated list) + a public shared-document viewer page against the real 8 portal-documents.ts endpoints, and will leave #2825/#2866 open with an honest evidence comment rather than closing them.
- 2026-09-14T04:16:00Z ✅ DONE — Built `artifacts/portal/src/pages/customer-documents.tsx` (+ `lib/documents-api.ts`, `lib/documents-types.ts`) against `Documents.dc.html`, wired to the 6 authenticated `portal-documents.ts` endpoints (reports, insights-documents list/view/pdf, share, report download); registered `/documents` route + sidebar nav entry. Initially also built a public `/shared-documents/:shareToken` viewer, but a concurrent build (#4001, Feature #1663) landed the same route first (`shared-document-public.tsx`, commit `be4969e2d`) while this build was rebasing — dropped the duplicate (`shared-document.tsx` + its unused public fetch helpers) in favor of theirs during conflict resolution, kept only this Feature's own authenticated page. Verified: `tsc --noEmit` clean, `vite build` clean, direct curl against the live dev api-server confirms `GET /api/portal/reports` and `GET /api/portal/insights-documents` both return real 401 with no bearer and `GET /portal/documents` mounts (200, no 404); direct `psql` confirms `reports`/`insights_generated_documents` are genuinely 0 rows on every tenant locally, matching the empty-state UI. Wrote and registered `test-manifests/portal/documents.json` (5 apiTests + 2 uiSteps); could NOT execute it via `shaneapp://runTest` — 4 real attempts (incl. a sanity check against the already-merged `my-architect.json`) all returned BuildConsole's own diagnosed "checkout is N commits behind origin/main" error (Git #3059, a known, self-resolving staleness condition, not a resolution bug) — documented honestly in the manifest's own notes rather than claimed as run. #2825/#2866: re-confirmed both still genuinely unresolved (no Document Viewer Design export exists, `buildPrintDocumentUrl()`/App.tsx routes unchanged) and posted real evidence comments on each rather than closing them; #2866's `blocked_by` edge on #2825 confirmed still correctly in place. No findings filed — #2825/#2866 already accurately describe the real gap. Working tree clean, branch verified merged into origin/main.
