# #4067 — Dashboard: Copilot Gate never rolled up, and #1943's own text about where it lives is stale

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** e65ec047d

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — corrected #1943's stale bullet (Copilot Gate is served via `GET /portal/remediation-tracker/pillar-scores`, not `/portal/pillars`), and wiring a real `computeCopilotGate()` read into `GET /api/portal/dashboard`'s roll-up in `portal-customer-engines.ts`.
- 2026-09-14T00:20:00Z ✅ DONE — corrected #1943's body text via `gh issue edit`. Added `computeCopilotGate(customerId)` read to `GET /api/portal/dashboard` (`portal-customer-engines.ts`), surfaced as a top-level `copilotGate` field (score/threshold/status/source/evaluation — never throws, degrades to a null score honestly). Fixed two existing dashboard-route tests (`portal-customer-engines-assessment-redaction.test.ts`, `portal-dashboard-route-collision.test.ts`) that broke on the new import — stubbed `computeCopilotGate()` since its real dependency chain (health-engine.ts → priority-engine.ts) references drizzle-orm's `sql` tag at module scope, which those files' scoped drizzle-orm mocks don't carry — and added a real assertion that `copilotGate` reaches the response body. `tsc --noEmit` clean. All 3 touched test files pass (18 tests). Ran the full api-server suite: 76 pre-existing failing files unrelated to this change (known baseline hazard, see memory `api-server-suite-baseline-and-worktree-hazard`) — none involve `portal-customer-engines`, `copilot-gate`, or `dashboard`. Verified branch is an ancestor of origin/main via `verify-branch-merged.mjs`; working tree clean.
