# #3486 — Feature: Document Hub (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:38:28Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** `e4958bdd8` (Document Hub feature work, rebased onto main as `3b1330012`)

## Log
- 2026-09-10T06:18:00Z ⏳ IN FLIGHT — pulled #3486 body + both `BUILD:` comments per
  standing rules, read `desktop/MyArchitect/GEMINI.md` and `desktop/MyArchitect/UI_RULES.md`.
  Comment history:
  1. `BUILD:` dispatch (buildSet `MyArchitect`) carrying `--blocked-by 3493` explicitly,
     instructing to read GEMINI.md / BUILD_QUEUE_METHOD.md / UI_RULES.md.
  2. later `BUILD:` dispatch (buildSet `BatterUpClearOut`) that omits the `--blocked-by`
     flag but does not state #3493 cleared — only reiterates the standing "pull body +
     comments, build per real stated scope" instructions.
- 2026-09-10T06:20:00Z 🛑 BLOCKED — on #3493 (Feature: UI Shell Redesign), same real
  blocker already recorded for sibling Features #3463 / #3464 / #3474 / #3480. Findings:
  - **All of #3486's scope is client UI.** Its checklist is two `Client:` items —
    document hub browse/view for the selected tenant, and PDF/share actions. There is no
    backend to build: the endpoints it names are real and already present
    (`artifacts/api-server/src/routes/msp-documents.ts` —
    `GET/POST /msp/documents`, `GET/PATCH /msp/documents/:documentId`,
    `POST .../publish`, versions endpoints; `artifacts/api-server/src/routes/msp-documents-hub.ts` —
    `GET /msp/documents-hub`, `.../:id/view`, `.../:id/pdf`, `.../:id/share`).
  - **Document Hub attaches into the shell's fixed Documents tab.** UI_RULES.md's fixed-tab
    list is `Home/Console/Watch/Documents/Admin`, with browse/view/PDF/share realised
    through the right-panel full record workspace and the Gallery control (real-data
    dropdown rows: tile/name/sub) that UI_RULES.md names Document Hub as a direct
    consumer of. None of that chrome exists yet.
  - **Confirmed #3493 not started this session:** `desktop/MyArchitect/*.csproj` has no
    `Fluent.Ribbon` package reference; `MainWindow.xaml` has zero `RibbonTab`/`<Fluent:`
    elements; `build-journal/3493.md` does not exist; #3493 is open + `in-flight`
    (not `complete`).
  - **GEMINI.md standing rule** is explicit: UI Shell first, no exceptions — do not build
    ad-hoc chrome (a new tab, a new panel) to unblock yourself; stop and flag it instead.
    Building Document Hub chrome now would conflict with a decision already recorded on
    the epic (UI_RULES.md's fixed-tab + Gallery-control spec), so the CLAUDE.md "BUILD IT"
    rule does not override — that rule is about a missing *backend*, and the backend here
    is real and complete.
  - **Gate (Git #1987):** the real `blocked_by` edge #3486 → #3493 already exists
    (confirmed via `gh api .../3486/dependencies/blocked_by`) and is still valid (#3493
    open/not-complete), so it was left in place, not re-created. Applied the `blocked`
    label, removed `in-flight`, and left a plain-language comment on #3486.
  - **Unblock path:** once #3493 lands (Documents fixed tab + Gallery control + right-panel
    record workspace model), wire the Document Hub browse/view/PDF/share surfaces into the
    Documents tab against the existing, already-real endpoints listed above.
  - No stray dirty files — `git status --porcelain` was clean on arrival and remains clean.
- 2026-09-10T20:32:07Z ⏳ IN FLIGHT — resumed. `#3493` (Feature: UI Shell Redesign) closed
  `complete` at 2026-09-10T14:25:43Z, so the real `blocked_by` edge #3486 → #3493 no longer
  reflects reality. Removed the `blocked_by` edge, swapped `blocked` → `in-flight` on #3486,
  and left a plain-language comment on the issue. Now building the real remaining scope:
  Document Hub wired into the shell's fixed **Documents** tab, using the real
  `GET /api/msp/documents-hub` (+ `.../:id/view`, `.../:id/pdf`, `.../:id/share`) endpoints
  confirmed real and unchanged since the earlier BLOCKED pass.
- 2026-09-10T20:38:28Z ✅ DONE — built #3486's real remaining scope, commit `e4958bdd8`
  (rebased onto `main` as `3b1330012`):
  - `Models/DocumentHubModels.cs`, `Services/IDocumentHubService.cs` +
    `Services/DocumentHubService.cs` — real HTTP client mirroring `msp-documents-hub.ts`'s
    exact response shapes for `GET /api/msp/documents-hub`, `.../:id/view`, `.../:id/pdf`,
    `POST .../:id/share`, same short-TTL cache pattern `RunbooksService` already uses.
  - `MainWindow.xaml.cs`: `RegisterDocumentsTab()` registers a Document Hub "Browse" gallery
    on the shell's fixed Documents tab (§4); `OpenDocumentHubRecord` opens the real
    full-panel record workspace (§3) with facts (category/type/status/dates/project/SOW
    total) and three actions — **View document** (fetches the real sandboxed-viewer HTML,
    writes it to a temp file, opens it in the OS default browser via the same
    `Process.Start(UseShellExecute)` pattern `LaunchExternalButton_Click` already uses —
    MyArchitect has no in-app HTML document renderer and UI_RULES.md doesn't define one, so
    this reuses existing chrome rather than adding new), **Download PDF** (same temp-file +
    launch pattern, real PDF bytes), and **Create & copy share link** (confirm-armed,
    `POST .../share`, real `shareUrl` copied to the clipboard and logged). PDF/Share are
    hidden (a stated `Body` note instead) when the document's real status isn't
    approved/delivered — matches the endpoint's own gate.
  - **Real, honest scope narrowing:** #3486's checklist names "browse/view for the selected
    tenant," but `Tenant.Id` (`Models/Tenant.cs`) is a display string, not the numeric
    `tenants.id` the endpoint's `customerId` filter takes, and `TenantService` remains
    fixture data for that numeric id — the same already-tracked gap `TryResolveLaunchControlScope`
    documents for every other gallery in this file (#3502/#3505/#3540). Filtering by a
    fixture id would be inventing a match, not honoring a real selection, so Document Hub
    browses the caller's whole book (real data, real rows) rather than guessing a filter.
    Once #3502/#3505/#3540 land a real numeric tenant id, wiring the `customerId` query
    param through is a small follow-up, not a redesign.
  - **Verification:** `dotnet build MyArchitect.csproj` — Build succeeded, 0 warnings, 0
    errors (ran twice, before and after a doc-comment fix). No `shaneapp://runTest` manifest
    applies — `desktop/BuildConsole`/`desktop/MyArchitect` is explicitly out of the
    test-manifest scope (CLAUDE.md's Test Coverage section is `artifacts/msp-portal` +
    `artifacts/shane-mccaw-consulting` only).
  - No new findings filed this session — the backend was already real and complete (see
    the earlier BLOCKED pass's audit); no bug hit, no stale doc found beyond what's already
    corrected above.
  - Labels: `blocked` → `in-flight` → `complete` on #3486 (this build leaves no
    Shane-To-Do action). Real `blocked_by` edge to #3493 removed once confirmed stale.
  - `git status --porcelain` clean at every checkpoint; nothing left uncommitted.
  - Branch verified merged: `node scripts/dev-server/verify-branch-merged.mjs` → OK,
    `agent/3486-q2173` (`3b133001`) is an ancestor of `origin/main`.
