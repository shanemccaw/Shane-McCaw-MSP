# #4137 — Portal Alerts: missing rule-count summary line and inconsistent LIVE badge in empty state

- **Status:** ✅ DONE 2026-09-14T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 46bc9533d (real change), b2b0b2aa1 (manifest lastVerifiedAgainstCommit backfill)

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — Investigated design vs wired code, confirmed both gaps. About to add the live-computed "All N seeded rules shown — X live, Y awaiting detectors" summary line to `AlertPreferencesContent.tsx` and make the LIVE badge in `AlertsDropdown.tsx` render unconditionally whenever the popover is open, including the calm/empty-state branch.
- 2026-09-14T00:45:00Z ✅ DONE — Both gaps closed in `46bc9533d`:
  1. `AlertPreferencesContent.tsx` now computes `liveRuleCount`/`awaitingRuleCount` from the real `rules` array (`detectorStatus === "live"`) and renders "All N seeded rules shown — X live, Y awaiting detectors..." (`data-testid="alert-preferences-rule-summary"`), matching the design's dc.html line 233 copy, with live-computed counts instead of the hardcoded 23/19/4.
  2. `AlertsDropdown.tsx`'s pulsing LIVE badge (`data-testid="alerts-live-badge"`) now renders in both the calm/empty-state header and the loaded header — previously only the loaded branch had it.
  - Updated `test-manifests/portal/settings.json` (new UI3B step, tag `4137-rule-summary`, asserts the summary line renders and contains "seeded"/"awaiting detectors") and `test-manifests/portal/alerts-dropdown.json` (documented the LIVE-badge fix and why it isn't asserted as a new uiStep — the calm vs. loaded branch depends on the real testbed account's live notification history, which this manifest doesn't seed/control; same reasoning that manifest's own TEST-DEEPLINK-1 note already documents). Both files' `lastVerifiedAgainstCommit` set to `858838923`.
  - Verification: `pnpm tsc --noEmit` clean, `pnpm run build` (vite) clean, in `artifacts/portal`. `shaneapp://runTest` not run against the new assertion — it hits the deployed dev-server build, not this worktree (documented pattern, e.g. Git #4104's note on the same structural gap), so it would only re-prove pre-existing coverage, not the new testid.
  - No fixture/hardcoded data introduced. No findings filed — no gaps discovered beyond the two the issue named.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `b2b0b2aa1` is an ancestor of `origin/main`.
