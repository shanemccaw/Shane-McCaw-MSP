# #2176 — ShaneBuilder: wire real Git/GitHub API + local Postgres data layer into the app shell

- **Status:** ✅ DONE 2026-09-02T15:10:00Z
- **Scope:** buildconsole (ShaneBuilder app shell)
- **Started:** 2026-09-02
- **Commit(s):** 08e013d76 (feature), cef306a76 (IN FLIGHT bookend), merged to main at 189666d19

## Log
- 2026-09-02T15:00:05Z ⏳ IN FLIGHT — real-audited BuildConsole's MainWindow startup pattern
  for `GitHubApiClient(settings.GitHubPat)` and `BuildQueuePostgresClient.TryCreate`; wiring
  the same real connection shapes (minus BuildConsole's full Git-Board/queue-panel surface,
  which is explicitly out of scope per this issue's own hold/scope-correction comment) into
  ShaneBuilder's MainWindow startup as a real status-bar connectivity line — no fixture data.
- 2026-09-02T15:10:00Z ✅ DONE — Added `Services/GitHubReadClient.cs` (resolves a real PAT from
  BuildConsole's own `%AppData%\BuildConsole\settings.json` or `GIT_PAT` in `.env.local`, hits
  GitHub's real Search Issues API) and `Services/QueueReadClient.cs` (reuses
  `ChatReadClient`'s existing `DATABASE_URL` resolution, real `SELECT count(*) FROM
  bt_build_queue`). Wired both into `MainWindow`'s constructor via
  `RunStartupConnectivityCheckAsync`, rendering into a new `ConnHealthStatus` status-bar
  segment. Verified live end-to-end outside the WPF shell before wiring: the real GitHub
  Search API returned `total_count: 489` open issues using the PAT from BuildConsole's real
  settings.json, and `psql` against the real local `DATABASE_URL` returned `980` real rows in
  `bt_build_queue` — same resolution paths the new C# code uses. `dotnet build` of
  `ShaneBuilder.csproj` succeeds with 0 warnings/errors. Merged cleanly into `origin/main`
  (fast auto-merge, no conflicts); `verify-branch-merged.mjs` confirms ancestry. `git status
  --porcelain` clean. No fixture data anywhere in this change — the existing
  `SeedSampleQueueData`/Build Queue panel fixture is pre-existing, out of scope (its own
  panel-tier wiring, deliberately deferred per this issue's own hold/scope-correction
  comment), and untouched by this build.
