# #3145 — Feature: Seasons & holidays theming system (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:20:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** dc681a6d9 (main, fast-forwarded from `agent/3145-q1902`)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — read the issue, the design handoff README's "Today v3 --
  Seasons and holidays" section, and the First Slice Prototype's real wired logic (`themeKey`,
  `THEME_BASE`/`THEMES`, `themeFor`, `parseToday`, the hat/peeker/weather-particle symbol defs,
  `foxLine`'s real ternary order). Confirmed #3144 (blocker) is done and merged. Building
  `public/theme.js` (the one real rule + flag table + date-override parser), new symbols in
  `critters-sprite.svg` (pkw-ghost, h-witch/h-party/h-santa/h-ny/h-leaf, ph-party/ph-santa/ph-ny,
  dk-leaf/dk-petal/dk-bat/dk-burst/dk-sleigh), and wiring it all into the existing #3144 header
  (holiday sky, orangeMoon, fox/peeker hats, full-phone weather-particle layer, holiday fox line).
- 2026-09-08T02:20:00Z ✅ DONE — shipped in `dc681a6d9` (main):
  - `public/theme.js` (new): `themeKey(date)` and the real per-theme flag table
    (`THEME_BASE`/`THEMES`/`themeFor`), `parseTodayOverride` + `todayOverride()` (a `?today=`
    query param -- the real, testable override mechanism the issue calls for, since no dev
    floaty panel exists yet to drive it) -- all ported byte-for-byte (dates, colors, symbol
    ids, fox lines) from the First Slice Prototype's own wired `door()` logic, not approximated
    from the README's prose alone (which the prototype's own code disagrees with on one point:
    Winter/Spring/Summer never actually override the sky via `thSky` in the prototype's real
    `sky: th.thSky || SKY[skyKey]` -- followed the prototype, per this project's own "design has
    dead renderVals" precedent).
  - `public/critters-sprite.svg`: added the Halloween-only `pkw-ghost` peeker, five fox-hat
    symbols (`h-witch`/`h-party`/`h-santa`/`h-ny`/`h-leaf`, no viewBox on the symbol itself per
    the spec's own warning) and three peeker-hat symbols (`ph-party`/`ph-santa`/`ph-ny`), plus
    five weather-particle symbols (`dk-leaf`/`dk-petal`/`dk-bat`/`dk-burst`/`dk-sleigh`) -- no
    duplicate ids, symbol tag balance verified.
  - `public/critters.js`: new `attachPeekerHat()`, same geometry convention as the existing
    `attachPeeker()`, for the holiday hat that rides on top of the "Next" label's peeker.
  - `public/app.js`/`app.css`: wired into the existing #3144 header -- holiday sky overrides
    time-of-day (`theme.thSky || SKY_GRADIENT[phase]`), the meta row's moon glow/icon goes
    orange during Halloween/birthday week, the fox gets its holiday hat overlay, Halloween's
    `pkw-ghost` replaces the daily-rolled "Next" peeker (with the birthday/christmas/newyear
    peeker hat riding on top where spec'd), a new full-phone `weatherParticlesHtml()` layer
    (leaves/snow/confetti/bats/fireworks/petals/fireflies/sleigh, gated by theme flags, real
    shapes/colors/counts/timings) renders as a `position:fixed` overlay that `render()`'s own
    `view.replaceChildren()` tears down automatically on every route change, and the fox's
    spoken line uses the theme's real `thFox` line (doctor-today still wins, matching the
    prototype's own ternary order; the app's own pre-existing dinner/home/none lines, which
    aren't part of the real spec's fox-line rule, sit below the holiday line as the fallback).
  - Deliberately NOT built, same reasoning as #3144: roof/yard decorations (README "Rooms --
    the house") and the "Later, by moment" balloons' theme colors/pennants -- neither the Rooms
    house grid (#3165) nor the Later balloons row (#3164) exists in this app yet for them to
    decorate. `theme.js`'s flags (`lights`/`snowcap`/`wreath`/`bunting`/`balloons`/`pumpkins`/
    `cake`/`pie`/`turkey`/`snowman`/`tree`/`leafpile`/`tulips`/`sunflower`) are ready the moment
    either lands -- left a comment on each issue (#3164, #3165) pointing at `themeFor()` so
    whoever builds them doesn't re-derive the date rule.
  - **Verification**: `node --check` on every changed/new JS file (clean, both before and after
    the rebase onto current `origin/main`). Real logic verification -- not "unable to verify" --
    by importing `theme.js` directly in Node and asserting `themeKey`/`themeFor` against real
    dates spanning every theme and every boundary the spec names (Oct 1/24/25/27/28/29/31,
    Nov 26/27 [2026's real 4th Thursday, computed independently and checked], Nov 28, Dec 1/25/
    26/31, Jan 1/2, Mar 1, Jun 1, Sep 1), confirming the halloween-week bunting/balloons overlay
    (`true` on Oct 25, `false` on Oct 24) and all four `parseTodayOverride` formats. Verified the
    edited `critters-sprite.svg` for symbol tag balance (64 open/64 close) and zero duplicate
    `id` attributes (93 total, all unique) and the edited `app.css` for balanced braces (276/276).
    No authenticated in-browser render was possible in this non-interactive session (no
    passkey/WebAuthn assertion available, same real constraint #3144 hit) -- the actual
    pixel-level rendering of the holiday sky/particles/hats against the design's own reference
    screenshots (`house-christmas.png`, `house-halloween.png`, `today-christmas-top.png`,
    `today-halloween-night.png`) was NOT visually confirmed live, noted honestly rather than
    claimed. This build touches only static front-end assets (no backend/schema/route changes),
    so a server boot check would have verified nothing about it and was skipped rather than
    performed hollow.
  - Left comments on #3164 and #3165 pointing at the new `theme.js` flags for their own future
    roof/yard and balloons work.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` (exit 0) before writing DONE.
