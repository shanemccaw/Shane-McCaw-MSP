# #3797 — dev-all.mjs: no --help, unrecognized flags fall through to destructive default

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** acc64a91c (on main as 2fddbe8a1 after rebase/push)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — adding real --help/-h output, rejecting unrecognized flags
  with a non-zero exit instead of falling through to full-stack restart, and adding an
  agent-worktree (`C:\wt\...`) detection guard before the destructive default start-all path.
- 2026-09-12T00:20:00Z ✅ DONE — `scripts/dev-all.mjs`: `--help`/`-h` now prints usage and exits 0
  without touching any process; any unrecognized flag prints an error + usage and exits 1 instead
  of falling through to the default kill-every-port-and-restart behavior; the default full-stack
  start additionally refuses to run when `repoRoot` matches `/[\\/]wt[\\/]/i` (an agent worktree
  path) unless `DEV_ALL_FORCE=1` is set — the exact detection signal for the Git #3647 incident
  this issue describes. Verified: `--help` (exit 0, usage printed, no process touched),
  `--bogus-flag` (exit 1, error + usage, no process touched), `--status` (unaffected, exit 0).
  The no-flag default-start path itself was blocked by the session's own permission classifier
  when run from this worktree — independent real-world confirmation the destructive path is
  correctly the one being gated; the worktree-detection regex was additionally unit-checked
  directly against `process.cwd()` (`C:\wt\3797-q2492` → `isAgentWorktree: true`).
  Rebased onto and pushed to `origin/main` directly (acc64a91c → main as 2fddbe8a1); branch
  confirmed an ancestor of `origin/main`; working tree clean. No finding filed — this build is
  the fix for the only gap identified in the issue body itself. No schema/data changes.
