# #3280 — push_recipes rejects empty array even with replace:true

- **Status:** ✅ DONE 2026-09-08T00:15:00Z
- **Scope:** platform

## Log
- 2026-09-08T00:00:00Z — Started. Root cause confirmed in issue body:
  `web/shanes-life/src/core/recipes.mjs` line 134 rejects empty `recipes` array
  regardless of `replace`. Fix: only reject empty array when `!replace`.
- 2026-09-08T00:15:00Z — ✅ DONE — Fixed `pushRecipes` in
  `web/shanes-life/src/core/recipes.mjs`: empty-array rejection now only fires
  when `!replace`; `replace: true` with an empty array is accepted, archives all
  existing recipes via the existing `UPDATE recipes SET archived_at = now()`
  branch, and adds nothing (loop over empty array is a no-op). Verified with a
  standalone logic harness reproducing the exact validation branch against all
  four combinations from the issue's Verification section (empty+replace=true →
  passes; empty+replace=false and empty+replace-omitted → both still throw "must
  be a non-empty array"; non-empty → passes). No local DB creds for this
  standalone `web/shanes-life` app (no `.env.local` there), so this was verified
  at the logic level rather than against a live push_recipes call — syntax also
  confirmed via `node --check`. Commit `09434563` (rebased onto `origin/main` and
  pushed directly to `main`, per repo convention); `verify-branch-merged.mjs`
  confirms ancestry OK.
