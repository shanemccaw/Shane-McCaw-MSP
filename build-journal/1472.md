# #1472 — New panel: Visual Test Tracker (per-page checkbox + notes + fast WebView2 screenshots with history)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 3a2039934 (rebased onto main as 74f6e32d6)

## Log
- 2026-08-28 ⏳ IN FLIGHT — new standalone WPF floaty (separate from StickyNotesWindow): watches WebView2 nav against a configurable base-URL list, per-page Good/Bad checkbox (default Bad) + notes, CapturePreviewAsync full-page + region screenshot capture with on-disk + Postgres-backed history gallery. New Postgres table via manual migration (Shane runs it — Shane To-Do). Building now.
- 2026-08-28 ✅ DONE — shipped VisualTestTrackerWindow (+ RegionSelectOverlayWindow drag-a-box crop tool), VisualTestTrackerStore.cs (Npgsql), VisualTestTrackerCapture.cs (full-page via scrollHeight resize + CapturePreviewAsync, region via CroppedBitmap), new ActivityBar icon, MainWindow WebView_NavigationCompleted wiring filtered to BuildConsoleSettings.VisualTestTrackerBaseUrls. `dotnet build` — 0 errors, no new warnings. Manual SQL migration lib/db/migrations/manual/2026-08-28-visual-test-tracker.sql NOT self-run (schema changes are Shane's own manual-SQL call per project rule) — Shane To-Do label applied; until he runs it the window shows an honest "database not ready" state rather than fixture data. This worktree had no `.env.local`, so the DB path itself (page create/save/screenshot persistence) could not be live-exercised this session — verified by code review + successful compile only; flagged honestly in the GitHub issue comment. Rebased agent/1472-q751 onto origin/main (which had moved to #1471's DONE commit) and pushed directly to main as 74f6e32d6; verify-branch-merged.mjs confirms merged.
