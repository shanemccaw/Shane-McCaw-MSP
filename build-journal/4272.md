# #4272 — Tenant reset Phase 2: execute the reset (destructive)

- **Status:** ✅ DONE 2026-09-15T23:55:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** bookend-only in git (this is a DB data operation — no product code changed). Reset committed to local Postgres `shanemccawmsp` at 2026-09-15T23:4x UTC.

## Log
- 2026-09-15T22:47:00Z ⏳ IN FLIGHT — Phase 2 of #4270. Execute the approved reset per #4271's real audit: reset the 194-table set (149 tenant/customer + 31 §5.B MSP operational/billing/integration + 14 §6 orphan-risk execution/auth/email history), FK-safe order, `users` via `WHERE tenant_id IS NOT NULL` (preserve 6 MSP-staff `tenant_id IS NULL` rows + their auth tokens), preserve `msps` rows and the test/vitest fixtures per Shane's explicit exceptions. Real Phase 1 backup confirmed present; taking my own fresh pre-reset dump before executing. Dev/testbed, backed up.
- 2026-09-15T23:55:00Z ✅ DONE — Reset executed and verified. Committed in one transaction, no FK errors.

  **Scope interpretation (from #4272 comments, later supersedes earlier):** clean slate for the REAL MSP (msp id=1, Shane McCaw Consulting) as if freshly purchased, while leaving the test/vitest fixtures (msps 1626/2362/2363/2497/2729 + tenants 1399/1402/1403/1547 + users 2606/3285) untouched, and preserving the 6 MSP-staff logins (`tenant_id IS NULL`) + their auth tokens. So the reset was **scoped to msp-1 identity**, not a blanket truncate — verified vitest footprint before running (11 tables held vitest rows; all preserved by scoped WHERE clauses).

  **Backups:** Phase 1 `shanemccawmsp_pre-tenant-reset_20260915T194711Z.dump` (46.8 MB) confirmed present + my own fresh `shanemccawmsp_pre-tenant-reset-PHASE2_20260915T232857Z.dump` (49.27 MB, `pg_restore --list` exit 0, 412 TABLE DATA sections) taken immediately before execution. Both in `C:\Source\ShaneMcCawConsulting\db-backups\` (outside the worktree, never committed).

  **What ran:** 194 approved reset tables + 2 external NO-ACTION blockers (`client_services` 1 row, `signup_exchange_tokens` 3 rows — customer-user-scoped rows blocking the approved user delete). FK-safe children-first order (topo over NO-ACTION edges); `wf_run_node_logs`/`wf_run_node_outputs`/`wf_trigger_events` TRUNCATEd (415k rows) and a temporary index on `wf_runs(retriggered_from_run_id)` created for the transaction (dropped before COMMIT) to avoid a pathologically slow un-indexed self-ref cascade. Dry-run (BEGIN…ROLLBACK) validated FK ordering first with zero violations. Total ~168k rows deleted + ~415k truncated.

  **users handled per #4271 §5.A:** `DELETE WHERE msp_id=1 AND tenant_id IS NOT NULL` → 11 msp-1 customer users deleted; 6 staff + 2 vitest preserved (users total 19→8). `msp_refresh_tokens`/`user_sessions` preserved staff+vitest user_ids.

  **Verification (all live queries post-COMMIT):**
  - Identity: msps 6 (msp 1 present); tenants 4 (all vitest, 0 for msp 1); users 8 (6 staff + 2 vitest, 0 msp-1 customers). ✓
  - All 194 reset tables empty-for-scope (only preserved vitest/staff rows remain; the only nonzero deltas were concurrent dev writes AFTER commit — new staff refresh tokens on user 1, new global `platform_log_stream` rows). ✓
  - Every preserve/catalog table unchanged vs the pre-reset snapshot except expected CASCADE collateral from approved reset parents (`checkout_email_verifications`←checkout_sessions, `customer_user_roles`←customer_roles+users, `engine_score_signal_deltas`←tenant_engine_snapshots, `sales_offer_events`←sales_offers, `msp_user_roles` customer rows←users) — each verified to contain NO vitest data before/after. ✓
  - Platform boots: api-server (port 8080) responds; `POST /api/auth/login` for shane@shanemccaw.com returns 401 "Invalid email or password" (auth path runs cleanly against reset DB, not a 500). Admin user id=1 retains password_hash + msp_id=1 + msp_role. MSP admin login intact. ✓

  **Findings filed (siblings of #4270, label `bug`, board "AI Batter Up"):**
  - #4313 — #4271's audit misses tables scoped only via a `users.id` FK (and CASCADE-child tables); future resets must enumerate the transitive FK descendant closure.
  - #4314 — `wf_runs`/`wf_run_node_*` lack indexes on FK columns → cascade deletes pathologically slow (worked around with a temp index this run).

  No product code changed. All scratch/SQL in `/tmp` (outside repo). Working tree clean.
