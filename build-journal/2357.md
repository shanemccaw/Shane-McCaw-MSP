# #2357 — Grouping by feature persists across lanes

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** `b1c359738` (rebased onto `main` after a concurrent #2365 landed real conflicting
  edits to the same file — merged both, not just one side)

## Log
- 2026-09-02T19:30:00Z ⏳ IN FLIGHT — confirmed #2356 genuinely DONE on `main` (commit
  `dd100b551`, verified via its own bookend). Real audit: `ChatGitHubFilter.GetBatterUpLaneCountsAsync`
  currently only tallies per-lane counts from the `ProjectV2.items` board walk — it does not carry
  each item's own number/title or its parent Feature, so `MainWindow.BatterUpPanel.cs`'s
  `RenderBatterUpLaneBody()` only prints a plain "N item(s) in <lane>" line (explicitly scoped out
  in #2356). Building: extend the SAME GraphQL page query (`content { ... on Issue { number title
  state repository { nameWithOwner } parent { number title } } }`) to carry each item's own
  number/title plus its immediate real parent (the GraphQL `parent` edge, same one-level field
  #2300's `GitPanelService.GetAncestryAsync` uses) in the same board walk — no extra per-item
  round trip. Then group each lane's items by that real parent (issue with no parent gets an
  honest "No Feature" bucket, never invented), and persist each group's expand/collapse state in
  a dictionary keyed by parent number that lives on the panel instance (not reset by
  `RenderBatterUpLaneBody`), so switching lanes and back doesn't reset which groups are open.
- 2026-09-02T20:05:00Z ✅ DONE — commit `b1c359738` (pushed to `main`, verified via
  `verify-branch-merged.mjs`, working tree clean). Shipped:
  - `ChatGitHubFilter.cs`: new `BatterUpItemRef` (Number/Title/FeatureNumber/FeatureTitle);
    `GetBatterUpLaneCountsAsync`'s existing board-walk GraphQL query extended with `number title`
    on the item's own content plus `parent { number title }` (the same one-level `parent` field
    #2300's `GetAncestryAsync` uses) — one extra field set on the SAME query, no second round trip
    per item. `BatterUpLaneCounts` now carries `BatterUpItems`/`AiBatterUpItems`/`AskShaneItems`
    (counts are the list lengths, no longer separately tracked ints).
  - `MainWindow.BatterUpPanel.cs`: `RenderBatterUpLaneBody` groups the selected lane's real items
    by `FeatureNumber` (an issue with none gets an honest "No Feature" bucket at key 0, sorted
    last — never an invented key), rendering each group as a chevron header (feature number+title
    + real item count pill, reusing `GitRowShell`/`GitChevron`/`GitCountPill` from the existing
    Git tree pattern) with its items listed underneath when expanded. `_batterUpCollapsedGroups`
    (a `HashSet<int>` instance field) persists collapse state; `RenderBatterUpLaneBody` never
    clears it, so it survives `SelectBatterUpLane` — the actual persistence #2357 asked for.
  - **Concurrent-edit note:** while this build was landing, #2365 (Batter Up panel chrome —
    Expand/Send to tab) merged to `main` first and touched the same file
    (`MainWindow.BatterUpPanel.cs`, adding `_batterUpExpanded`, `BatterUpLaneCount`/
    `BatterUpLaneLabel` helpers, `BatterUpDocSnapshot`, `SetBatterUpExpanded`,
    `BtnBatterUpExpand_Click`, `BtnBatterUpSendToTab_Click`, `RenderBatterUpDoc`). Rebased onto it
    (real 3-way conflict, resolved by hand, not `--ours`/`--theirs`) and kept both: this build's
    grouping reuses #2365's `BatterUpLaneLabel`/new `BatterUpLaneItems` helper instead of
    duplicating the switch; `RenderBatterUpDoc`'s frozen "Send to tab" snapshot only carries counts
    (not items/parents), so its trailer line was reworded from a stale "(#2357 onward)" forward
    reference to an honest "this sent copy is count-only" note rather than faking grouped items it
    never captured.
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` clean (0 warnings/0 errors) both
    before and after the rebase merge. Independently re-ran the extended GraphQL query shape live
    via `gh api graphql` against the real board — confirmed real items grouping correctly under
    their real parent Features (e.g. #2478-#2481/#2483/#2486 all correctly group under #2473;
    #2485 under #2469), matching exactly what the shipped code does with the same query. Did not
    launch the WPF app itself — no headless runner exists for ShaneBuilder yet (same gap #2356
    already noted); build + live-query cross-check is the real verification done.
  - **Scope confirmation:** stayed inside grouping + persistence only, per this issue's own scope
    line — no group-header badges/actions (#2358) or item badges/actions (#2359/#2360) built.
  - No findings filed this session — no genuine bug/gap surfaced outside stated scope.
