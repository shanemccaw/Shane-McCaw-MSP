# #3503 — Evidence/attachment field + endpoint for remediation_tracker_steps & change-control execution

- **Status:** ✅ DONE 2026-09-10T16:42:37Z
- **Scope:** platform

## Log
- 2026-09-10T16:23:47Z — Started. Pulled #3503 body + comments (single dispatch comment, no
  supersession). Real gap confirmed: `desktop/MyArchitect/Services/IEvidencePostClient.cs`
  (added in #3470's commit `bccd5ab2c9bf3eb2e2462771d50baa71ebc4ef46`) has no live server
  implementation. Neither `remediation_tracker_steps` nor `cr_executions` (the change-control
  execution table, #1499) has an evidence/attachment column or child table today
  (`lib/db/src/schema/msp.ts`).
  Plan: new `evidence_attachments` child table (polymorphic `source`/`sourceRefId`, following
  the exact convention `retainer_work_log` already uses for the same two source tables) +
  multer-backed upload/list/serve routes under `/api/msp/*`, scoped by
  `requireCapability("ladder.msp-operator")` + MSP/customer ownership checks matching
  `msp-remediation-tracker.ts` / `msp-change-executions.ts`.
- 2026-09-10T16:42:37Z — ✅ DONE. Shipped in commit `e4a29599` (main):
  - `lib/db/src/schema/msp.ts` — `evidenceAttachmentsTable` (polymorphic
    `source`/`sourceRefId`, matching `retainer_work_log`'s existing convention for
    the same two source tables — no FK, soft link).
  - `lib/db/migrations/manual/2026-09-10-evidence-attachments-3503.sql` — additive
    `CREATE TABLE`, **run myself** against local `DATABASE_URL` this session.
  - `artifacts/api-server/src/lib/evidence-attachments-store.ts` — DB access +
    wire shaping.
  - `artifacts/api-server/src/routes/msp-evidence-attachments.ts` — multer-backed
    upload routes: `POST`/`GET .../customers/:customerId/remediation-tracker/steps/:stepId/evidence`,
    `POST`/`GET .../msp/change-control/executions/:id/evidence`, and a scoped
    `GET /msp/evidence-attachments/:id/file` serve route (never a public static
    mount — this is real tenant evidence). Registered in `routes/index.ts`.
  - Change-control side resolves the portal `customerId` via `tenants.tenant_id`
    (join off `cr_executions.tenantId`, the M365 tenant GUID) rather than
    inventing a placeholder value.
  - **Verified end-to-end against the real local database**: inserted a real
    `remediation_tracker_steps` row and a real `msp_change_requests`/`cr_executions`
    pair, exercised the actual Drizzle insert/select paths for both `source`
    values, confirmed the rows round-tripped, then deleted all scratch rows
    (verified 0 remaining afterward). `pnpm exec tsc --noEmit -p .` in
    `artifacts/api-server` is clean.
  - `#3470`'s own `blocked_by` edge already pointed at `#3503` — no edge changes
    needed here; MyArchitect's own `IEvidencePostClient` wiring is out of this
    issue's stated scope (issue body: "wire `IEvidencePostClient`... tracked back
    on #3470").
  - **Finding filed:** #3522 (`lib/db ships stale dist/ declarations that shadow
    live schema edits during typecheck`) — parented under #1096 (no Feature-tier
    parent on this issue), labeled `bug`, board status set to "AI Batter Up". Hit
    this mid-build: `lib/db`'s `composite`+`declarationMap` tsconfig makes
    TypeScript project-reference resolution redirect to the stale
    `lib/db/dist/*.d.ts` instead of live `src`, producing false "no exported
    member" errors after a genuine schema edit until `lib/db`'s own `tsc -b` is
    re-run. Worked around in this session by rebuilding `lib/db`'s declarations;
    not a `pnpm install`/symlink issue (ruled out — the workspace symlink was
    correct throughout).
  - Working tree clean; branch verified merged into `origin/main`
    (`verify-branch-merged.mjs` exit 0).
