# #3503 — Evidence/attachment field + endpoint for remediation_tracker_steps & change-control execution

- **Status:** ⏳ IN FLIGHT 2026-09-10T16:23:47Z
- **Scope:** platform

## Log
- 2026-09-10T16:23:47Z — Started. Pulled #3503 body + comments (single dispatch comment, no
  supersession). Real gap confirmed: `desktop/MyArchitect/Services/IEvidencePostClient.cs`
  (added in #3470's commit `bccd5ab2c9bf3eb2e2462771d50baa71ebc4ef46`) has no live server
  implementation. Neither `remediation_tracker_steps` nor `cr_executions` (the change-control
  execution table, #1499) has an evidence/attachment column or child table today
  (`lib/db/src/schema/msp.ts`).
  Plan: new `evidence_attachments` child table (polymorphic `source`/`sourceRefId`, following
  the exact convention `retainer_work_log` already uses for the same two source tables) +
  multer-backed upload/list/serve routes under `/api/msp/*`, scoped by
  `requireCapability("ladder.msp-operator")` + MSP/customer ownership checks matching
  `msp-remediation-tracker.ts` / `msp-change-executions.ts`.
