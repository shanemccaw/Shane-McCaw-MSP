# #1609 — Webhooks: delivery log depth — payload snapshot unserved, no paging past 200

- **Status:** ✅ DONE 2026-09-10T21:30:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none from this session — work already shipped on `main` at `39cf1040b` (via superseding issue #3526)

## Log
- 2026-09-10T21:20:00Z ⏳ IN FLIGHT — pulled #1609's full body + comment thread per instructions before touching code.
- 2026-09-10T21:25:00Z ✅ DONE — Found #1609 is already **CLOSED, `NOT_PLANNED`**, with a real
  explanatory comment posted 2026-09-10T17:12:13Z: a fresh Design export landed and the work was
  re-filed as new v2 issues under #1597 rather than re-dispatched under this number. The direct
  successor is **#3526** ("Webhooks: delivery log depth and paging (v2)"), carrying this issue's
  exact body verbatim ("Real, carried forward from #1609 (superseded, dedup limit)").

  #3526 is itself already **CLOSED, `COMPLETED`**. Its own session comment
  (2026-09-10T21:17:53Z) reports both gaps from this body fixed, backend-only:
  - Gap 1 (`requestBodySnapshot` unserved) — now selected in `getDeliveryLog` and returned on
    `DeliveryLogEntry` by all three route callers (portal/admin/msp).
  - Gap 2 (no paging past 200) — real keyset pagination added on the internal serial `id`
    column, `{ entries, nextCursor }`, all three routes accept `?cursor=` and return
    `nextCursor`.

  Verified directly against `origin/main` in this worktree rather than trusting the comment
  alone: `git log origin/main` shows `39cf1040b Bookend: #3526 DONE (e508f3e80)` already landed,
  and `artifacts/api-server/src/lib/webhook-delivery.ts` on `origin/main` contains both
  `requestBodySnapshot` selection (lines 296/324/377) and the `nextCursor`/keyset pagination
  logic (lines 333-382) described in that comment.

  **No code changes made in this session** — the two gaps this issue describes are already
  fixed and merged to `main`. Nothing left to build under this number. Portal UI wiring for
  the same delivery log is out of scope for this backend-only issue per #3526's own comment
  (sibling issue #3527 covers UI).
