# Git #2309 — Feature panel: ring, state, epic, bug count, last build, six state chips, issue list

- **Status:** ✅ DONE 2026-09-02T18:55:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Git Panel, Feature #2289 items 20-21: #2309 + sibling #2310)
- **Commits:** `bd9bd34f1` (implementation), `8eae8e8ec` + `0850df96a` (merges reconciling with concurrent
  #2301/#2302/#2311/#2410 landings in the same file), pushed straight to `main` at `0850df96a`.

## Log

- 2026-09-02T17:41:00Z — ⏳ IN FLIGHT — Session start. Depends on #2290 (Git Panel navigation shell)
  — confirmed genuinely DONE on `main` (`git merge-base --is-ancestor 79852e2b0 origin/main` → ANCESTOR_OK,
  bookend at `build-journal/2290.md` reads ✅ DONE). Building the real Feature detail panel content that
  #2290 deliberately left as an identity-only placeholder: progress ring (closed/total children), state
  pill, epic link (from the real ancestry/breadcrumb chain), open bug count, last build (real
  `bt_build_queue` row joined on `github_number` for the feature's own child issues), six real build-state
  chips (up next / running / verifying / blocked / parked / paused, mirroring the Build Queue band's own
  real taxonomy) and the issue list — then wiring chip clicks to filter that list (#2310). No fixture
  data: state chips and last-build come from a live join against the shared local `bt_build_queue` Postgres
  table via a new `QueueReadClient` method; ring/bug-count/epic come from the real GitHub GraphQL feature
  sub-tree already fetched by `GitPanelService`.
- 2026-09-02T18:55:00Z — ✅ DONE — Shipped `bd9bd34f1`: new `GitPanelService.GetFeatureDetailAsync` (one
  GraphQL call — a feature's own identity + its direct sub-issues, independent of the epic tree's cached
  fetch so a cold/derived open is still correct), new `QueueReadClient.GetLatestByGithubNumbersAsync`
  (`SELECT DISTINCT ON (github_number) ... WHERE github_number = ANY(@nums) ORDER BY github_number,
  updated_at DESC` — one real row per issue number), and the Feature detail panel body in
  `MainWindow.GitPanel.cs`: a donut ring (real closed/total child-issue percentage), the feature's own
  state pill (reusing `GitNodeState`), a clickable "Epic: #N …" link back up the real breadcrumb trail,
  open-bug-count pill, a "last build" line (max `updated_at` across the feature's own number + every
  child), six real build-state chips (up next / running / verifying / blocked / parked / paused —
  `ClassifyBuildState` maps real `bt_build_queue.status` plus `blocked_by_number`/manually-paused-id
  lookups onto them; terminal statuses like done/failed/canceled are honestly excluded, never
  force-mapped), zero-count chips rendered dimmed/unclickable (mirroring the Build Queue band's own
  convention), and the issue list a chip click filters (#2310) — clicking again clears the filter.
  **Verification (honest level):** `dotnet build` clean (0 warnings/errors) after each merge; the real
  GraphQL query for `GetFeatureDetailAsync` and the real `bt_build_queue` join were both run live against
  #2289 (24 real sub-issues) and its own children's actual queue rows (including this very build's own
  `running`/`verifying` rows) to confirm the query shapes and state classification match live data before
  wiring them into the UI. No live WebView2/UI click-through (BuildConsole/ShaneBuilder is out of
  test-manifest scope per CLAUDE.md; visual QA is Shane's).
  **Merge note:** landed concurrently with #2301 (Milestone panel), #2302 (Gate panel), #2311 (no-burndown
  warning) and #2410 (board-status reconciliation), all touching the same `MainWindow.GitPanel.cs` /
  `GitPanelService.cs` / `QueueReadClient.cs` files. Two rounds of `git merge origin/main` were needed;
  the first resolved cleanly by hand, the second produced a diff3 misalignment (a duplicate-`}` collision
  between my `GitStateChip` method and origin's `AfterGitTrailChanged`) that was reconstructed manually
  line-by-line rather than trusted blindly — confirmed correct by a clean `dotnet build` afterward.
  Working tree clean at session end; no pre-existing dirty files encountered. No new findings filed —
  no genuine bug/gap surfaced beyond the two build-time merge conflicts already resolved above.
