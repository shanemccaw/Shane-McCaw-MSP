# Build Journal — #1894

- **Status:** ✅ DONE
- **Scope:** buildconsole

## Log
- Session start: adding back a combined "Running & Queued" filter as a third option in BuildQueuePanel's filter combo, alongside the existing split Running/Queued (Git #1829). Reusing pre-#1829 combined criteria.
- ✅ DONE — Added a third `ComboBoxItem` ("Running & Queued", Tag="RunningAndQueued") to `Controls/BuildQueuePanel.xaml`'s filter combo, and a matching `ApplyFilter` case in `Controls/BuildQueuePanel.xaml.cs` reusing the exact pre-#1829 combined criteria: `queued` + `running` + `LimitPausedStatus` + `VerifyingStatus`. Existing `Running` and `Queued` entries/cases left untouched. Composes with #1834's build-set filter the same way the other filters already do (same `ApplyFilter` pipeline). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors). Commits: `7dd3b0579` (feature, agent/1894-q984), merged to `main` at `1799bac1e`. Verified `1799bac1e` is an ancestor of `origin/main`. Working tree clean.
