# #4033 — dev-server restart merges into a different checkout than the one serving the port

- **Status:** ✅ DONE 2026-09-14T21:41:03Z
- **Scope:** both (scripts/dev-server coordinator + the main checkout's divergence)
- **Started:** 2026-09-14
- **Commit(s):** e3faf4db4 (fix); afec912e4, 0a81c9061, 0ff039139, c5dfbbc27 (stranded QA commits published to origin/main via cherry-pick -x); e885f57cb (IN FLIGHT bookend)
- **Findings filed:** #4122 (QA auto-commit strands unpushed commits on the shared main checkout; sub-issue of #1202, AI Batter Up)

## Log
- 2026-09-14T21:23:13Z ⏳ IN FLIGHT — investigating serverWorktree resolution vs. the live port-8080 process cwd, and the main checkout's local-only commits ahead of origin/main
- 2026-09-14T21:41:03Z ✅ DONE — Problem 1: new scripts/dev-server/serving-checkout.mjs checks the live port holder (port → pid → parent dev-all.mjs → root), whether the commit is in the serving checkout, api staleness from the reflog at process start, and a fresh listener after a rebuild. config.servingRoot is now the single source of truth. refresh-main-server waits for a NEW api listener and `ready` requires verification. request-restart exits 3 "NOT LIVE" when merged but not served. Problem 2: the divergence was 4 unpushed "Automated QA Session" commits (BuildConsole QA auto-commit, push off by default). They were published to origin/main, the skip reason now names local-only commits, and refresh reconciles with `reset --keep` once they're upstream. Verified: existing selftest.mjs 53/0; new serving-checkout.selftest.mjs all pass; live run attributed real :8080 pid 8736 to C:\Source\...\Shane-McCaw-MSP (verified), and an origin-only commit correctly came back NOT live (exit 3); dry-run named the 4 local-only commits. Honest gap: a real reconcile attempt now judges the main checkout lossless, but `reset --keep` refused on a pre-existing uncommitted edit to test-manifests/portal/settings.json there (not mine, modified 17:14:53 -0400, left untouched). That checkout stays at 6938cda03 until that file is committed or reverted; the next restart then moves it automatically.
