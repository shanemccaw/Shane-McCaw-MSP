# #1775 — Change Control: msp-changes.ts POST and portal-runbooks.ts hold-window CRs never materialize an approval-ledger record

- **Status:** ✅ DONE 2026-09-03T02:30:15Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 7b8d57450be50bef26b1e4b65ed45759c7a6e4eb (fix), 99dc6a7fe (IN FLIGHT bookend)

## Log
- 2026-09-03T02:26:03Z ⏳ IN FLIGHT — investigated the two missing call sites named in the issue
  (`msp-changes.ts` POST `/api/msp/change-requests`, and `portal-runbooks.ts`
  `raiseHoldChangeRequest`), confirmed via grep that neither imports/calls
  `materializeApprovalsForChange`, and am wiring both per the exact pattern the working
  paths (`portal-change-control-raise.ts`) already use.
- 2026-09-03T02:30:15Z ✅ DONE — wired `materializeApprovalsForChange` (+ `loadApprovalPolicy`)
  into both gaps:
  - `msp-changes.ts` POST `/msp/change-requests`: added the same non-fatal
    try/catch block right after the existing `raised` `recordCrEvent` call,
    reusing the `tenantForPolicy` row already resolved for the freeze-policy
    check as the `loadApprovalPolicy(customerId)` input (falls back to
    `NO_POLICY` when no tenant row matched, same as every other call site's
    default).
  - `portal-runbooks.ts` `raiseHoldChangeRequest`: same non-fatal block after
    its own `raised` event; added `createdAt` to the insert's `.returning()`
    (was previously `id`-only) so the real DB-assigned creation timestamp
    feeds the ledger the same way the other three call sites do.
  - Both call sites checked type-clean (`npx tsc --noEmit`, zero errors in
    either touched file).
  - Verified `cr_approvals` schema directly against local Postgres
    (`\d cr_approvals`) matches every column `materializeApprovalsForChange`
    writes — real, not assumed.
  - Could not execute `portal-cab-store.test.ts` (the one existing unit test
    that already exercises `materializeApprovalsForChange` directly) — vitest
    itself is broken in this environment: `node_modules/vitest` in the main
    checkout resolves through a real pnpm-store symlink to
    `.pnpm/vitest@4.1.9.../node_modules/vitest`, and that target directory is
    genuinely empty (0 bytes, confirmed via `stat`) — a pre-existing store
    corruption unrelated to this change, not something `pnpm install` fixes
    per the standing rule (an unchanged lockfile install does not repair a
    package whose installed contents are missing). Filing this separately per
    the "file every finding" rule below rather than working around it with a
    forced reinstall.
  - Local dev api-server was not running, so no live HTTP POST verification
    against a running instance; typecheck + direct-schema check + exact
    parity with the already-proven working call sites is the real evidence
    this session has.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms
    `7b8d57450` is an ancestor of `origin/main`.
