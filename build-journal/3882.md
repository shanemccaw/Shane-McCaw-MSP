# Issue #3882

- **Status:** ⏳ IN FLIGHT 2026-09-13T04:23:53Z
- **Scope:** platform

## Log

- 2026-09-13T04:23:53Z — Investigated `admin-script-runner.test.ts`. Confirmed via grep that
  `GET /api/admin/runbook-jobs/output` does not exist anywhere in
  `artifacts/api-server/src/routes` — `admin-script-runner.ts` only exports
  `/admin/script-library`, `/admin/script-download-tokens` (GET/POST/DELETE). No
  `runbook-jobs` route anywhere in production routes.
- Real replacement surface: `GET /api/admin/run-script/:jobRef/status` in
  `admin-m365-run.ts` (line 761). Confirmed the client (`artifacts/admin-panel/src/lib/scriptPoller.ts`)
  already polls this route, not the old one. But the architecture changed, not just the
  path: the old route's entire premise — that the poll request itself updates
  `kanbanTasksTable.taskMetadata` (`runningJobRef`/`lastJobStatus`) to clear the stale
  "Running" badge — no longer holds. `scriptPoller.ts` says explicitly: "the backend owns
  kanban task updates." The real kanban sync now happens server-side in
  `admin-m365-run.ts`'s background job-completion handler (after
  `waitForJobCompletion`), not in the client-poll GET handler at all. The GET route is
  now a pure read of `scriptRunResultsTable` — no DB writes.
- Rewriting `admin-script-runner.test.ts` to target the real route/architecture rather
  than deleting outright.
