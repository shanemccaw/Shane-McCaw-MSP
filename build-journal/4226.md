# #4226 — Refuse consent-callback create/attach when the tenant GUID already has a customer

- **Status:** ✅ DONE 2026-09-15T13:01:51Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 10a845b5b (fix + tests)

## Log
- 2026-09-15T12:39:19Z ⏳ IN FLIGHT — adding a guard in /api/consent/callback (checkout-session path + no-id invite-token path) that refuses when the claimed tenant GUID already maps to ANY customer row, routing to an honest "already registered — contact support" state; live-verify pre/post with a synthetic direct-business customer
- 2026-09-15T13:01:51Z ✅ DONE — `/api/consent/callback` now refuses every create-path consent (checkout session, invite with no customer, MSP onboarding invite) whose tenant GUID already has ANY customer object. Cross-MSP keeps the tenant-conflict ending. Same-MSP gets a non-closing "already registered, contact support" popup page, or a plain 409 on the portal origin. The refusal happens before token burn, session flip, the Microsoft call, grant, account or scan. `resolveOrCreateDirectTenant`/`resolveOrCreateTenantForMsp` now return `created`, and the post-resolve re-check refuses a row raced in after the guard. The checkout session is flipped to consented only after the tenants row is known to be new. Verification: vitest consent + msp-consent + direct-tenant-provisioning + consent-verification = 96/96; api-server tsc clean. (free-scan-prospect.test.ts needs a live DATABASE_URL at import and did not run; that is pre-existing.) LIVE, on a pg_dump scratch copy `zz_test_4226` so the real dev DB was never written, with a consent-router-only harness (no job worker): I moved the real row's GUID aside and seeded a synthetic `zz-test-4226 Existing Direct Customer` (direct MSP, graph granted) on the real consented testbed GUID. Pre-fix :8097: checkout returned 200 "Access granted" and the invite returned 302 success. Both `zz-test-4226-buyer-pre` and `-invitee-pre` users were linked to the synthetic customer (tenant_id 1841, Customer), its adminEmail was overwritten with the buyer's, sharepoint was stamped, and the token was burned. Fixed :8098: checkout returned 200 "Organisation already registered" and the invite returned 409. Consent was unchanged (consentedAt 2026-01-01, owner adminEmail), no users were created, the session stayed pending and the token stayed unburned. Positive control on the fixed build: with no customer row, the same GUID created a new tenant, was granted and linked the buyer, and a second consent on it was then refused with 409. Cleanup: scratch DB dropped. The real DB has 0 zz-test-4226 users, tenants, sessions or tokens, and tenant 1 is unchanged. Findings filed under #1650: #4243 (the decline branch flips any tenant's graph consent to declined from an unauthenticated GUID). No migrations. No test manifest: api-server only.
