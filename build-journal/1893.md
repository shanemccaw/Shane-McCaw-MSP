# #1893 — Build set rollup: send Verifying items as a landed-list to the active chat

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a send button to each build-set rollup header row (#1834's
  rollup), gathering that set's real `verifying` items, formatting "Git #NNNN — landed" per line,
  and routing through the existing shared `SendTextToActiveClaudeChatAsync` path (#937/#940),
  same pattern as `WireSqlRunnerSendToChat`.
