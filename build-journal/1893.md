# #1893 — Build set rollup: send Verifying items as a landed-list to the active chat

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 0d043e857

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a send button to each build-set rollup header row (#1834's
  rollup), gathering that set's real `verifying` items, formatting "Git #NNNN — landed" per line,
  and routing through the existing shared `SendTextToActiveClaudeChatAsync` path (#937/#940),
  same pattern as `WireSqlRunnerSendToChat`.
- 2026-08-30 ✅ DONE — `BuildQueuePanel.BuildRollupRow` (Controls/BuildQueuePanel.xaml.cs) now
  renders a ✈ send button in each rollup row, but only when that build set has ≥1 real
  `verifying` item (a set with zero offers no send affordance at all, per req 3). Click
  gathers the row's real `verifying` int list, formats `Git #NNNN — landed` one per line
  (reusing the existing `FormatIssueRef` helper), and raises a new
  `SendBuildSetVerifyingRequested` event carrying the text plus a per-row status callback.
  MainWindow.xaml.cs wires that event straight to the existing shared
  `SendTextToActiveClaudeChatAsync` (the exact #937/#940 mechanism the issue named — no new
  send mechanism built), same pattern as the neighboring `WireSqlRunnerSendToChat`. The
  callback shows the real inserted/failed outcome inline under the row (auto-hides after 5s),
  the same purpose as SqlDocumentView's `ExecStatus`/`ShowSendStatus`.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors.
  Branch verified merged into origin/main via `verify-branch-merged.mjs`. `git status
  --porcelain` clean. No test manifest — desktop/BuildConsole is explicitly out of scope.
