# Git #3153 — Feature: Money — Catches (renewal, forgotten money, duplicates, borrowed-from-bill, bulk-buy) (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:20:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-08T00:00:00Z — Session started. #3137 (blocker) confirmed CLOSED/complete; no stale
  `blocked_by` edge existed on #3153 (`total_blocked_by: 1` but the dependency itself already
  shows resolved — nothing to remove). Read design contract Section 4 (real expense-cutting
  mechanisms) plus the prototype's own `catches(kind, text, dismissed_at)` schema hint and the
  Money screen's real Catches card spec (README.md money screen section). Rebased worktree onto
  origin/main (picked up concurrent #3149 Cars work) — next free migration number is 033.
- 2026-09-08T02:20:00Z — ✅ DONE — commit `739dd1bd2` on `main`. Built all five real Catches
  detectors in `src/core/catches.mjs` (renewal watch, forgotten-money sweep, duplicate-request,
  borrowed-from-bill, bulk-buy), `migrations/034_catches.sql` (the `catches` table +
  `list_items.requested_by`), `GET/POST /api/money/catches(+/:id/dismiss)`, `get_catches`/
  `dismiss_catch` MCP tools, and `runCatchesSweep()` wired into the existing 6h housekeeping
  loop. Real, separate finding filed and reconciled in the same migration: an orphaned `catches`
  table + `list_items.requested_by` column already existed live in the shared local `finances`
  database with no matching migration/code anywhere in git history — filed as **#3177**
  (parented under #1202, Build Console epic — the worktree-isolation owner), board status set to
  AI Batter Up. Migration numbered 034, not 033 — 033 was live-taken by a concurrent build's
  ledger row (#3151, Wins log) at the time, confirmed via a direct ledger check rather than
  assumed from the checked-out tree; that migration file itself landed on `main` during this
  session's own rebase and now matches the ledger exactly.
  Verified against real live data in the local `finances` DB via a scratch script (written,
  run, deleted before finishing): 14/14 checks passed — each detector fires on a
  deliberately-constructed real row (a real renewal date, a real duplicate list-item pair
  across two requestors, a real small outflow on H1 Mortgage, 5 real small transactions at a
  test merchant), a dismissed catch never resurrects on re-detect, `runDetectors`/
  `dismissCatch` behave correctly, and every test row + catch was deleted afterward (confirmed
  0 residual rows in `catches`/`transactions`/`list_items`/`lists`/`dates`). Route and MCP tool
  registration confirmed via direct import against the final merged code. Full HTTP server boot
  not exercised this session (the sanctioned migration runner, `node bin/migrate.mjs`, WAS run
  successfully post-rebase: "up to date, 22 already applied" — no orphan-ledger error, confirming
  the fix is real and complete). Branch verified merged into `origin/main`
  (`scripts/dev-server/verify-branch-merged.mjs`); working tree clean.
