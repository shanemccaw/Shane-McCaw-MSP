# #4309 — Mailbox connector: fall back to platform's single-tenant mail app when dedicated app isn't configured

- **Status:** ✅ DONE 2026-09-15T23:16:04Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 758c014d0

## Log
- 2026-09-15T23:03:47Z ⏳ IN FLIGHT — investigating `sendMailViaGraphForMsp`/`sendEmailForMspOrThrow` fallback behavior. Found the Path 1/2/3 fallback structure in `artifacts/api-server/src/lib/mailer.ts` (`sendEmailForMspOrThrow`) already gates Path 1 (dedicated app) on `mailboxSendAppCredentialsPresent()` and already falls through to Path 2 (platform mailbox via `getAccessToken()`/`sendMailViaGraph()`) when it's false — this was landed as part of #4241 itself. Today's real state (`MAILBOX_SEND_APP_CLIENT_ID` unset in `.env.local`) already exercises Path 2. Adding an explicit log line for the skip-Path-1 case (previously silent) plus a real test locking in this exact untested combo, then live-verifying a real send.
- 2026-09-15T23:16:04Z ✅ DONE — Real ask (#4309's own "Real scope") was already satisfied by #4241's own Path 1/2/3 structure; no fallback logic needed to be added. Shipped in `758c014d0`: (1) an explicit `log.info` line in the Path-1-skipped branch of `sendEmailForMspOrThrow` naming the fallback explicitly (previously silent — the mechanism was distinguishable only via the later `path` metadata field on success); (2) a new unit test in `msp-mailer.test.ts` locking in the exact untested "dedicated app unset, platform mailbox present" combo (asserts `sendMailViaGraphForMsp` is never even called — no connector lookup attempted); (3) `mailbox-send-fallback.live-verify.ts`, a live-verify test that read real Graph credentials out of `.env.local` (same safe per-line-regex pattern as `scripts/config-state/db.mjs`, never a shell `source`) and sent a REAL email to shanemccaw.inbox2@gmail.com through the platform mailbox fallback — confirmed delivered via Microsoft Graph with `mailboxSendAppCredentialsPresent()` actually `false` (today's real state). `mailbox-send-app.ts` and its multi-tenant scaffolding are untouched, as required. Full test suite for the touched files (`msp-mailer.test.ts`, `mailer.consent-invite.test.ts`, `graph-mailbox-send.test.ts`) passes (16/16); `tsc --noEmit` clean on touched files. No migration needed — no schema change. No new findings discovered. Working tree clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
