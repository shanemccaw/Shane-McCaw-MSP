# #1513 — Risk Register: Overdue review alerting to the MSP

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Building overdue-review alerting on top of the existing `msp_alert_rules`/`msp_alert_events` platform alert engine (`alert-engine.ts`), per #1513's settled architecture: a lapsed review alerts the MSP, never the customer, and never changes the acceptance's validity. Adding a new `risk_review_overdue` condition type, a distinct `review_lapsed` severity, and the review-clock operational writer (`on_track`/`due`/`overdue` transitions on `msp_risk_decisions.review_state`) that #1507's bookend explicitly left for this issue. Rides the existing `alert_evaluate_rules` Workflow Engine node (already seeded, runs every 5 min) — no new workflow machinery. Ends at the wire contract per scope stop — no `artifacts/portal` UI.
