# #1513 — Risk Register: Overdue review alerting to the MSP

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** `f256f5176ec7f508be3469099617e5f128df16fc` (merged to `origin/main`; `verify-branch-merged.mjs` exit 0)
- **Migration:** `lib/db/migrations/manual/2026-08-31-risk-review-overdue-alert-1513.sql` — run against local Postgres in-session, logged on #1630
- **Findings filed:** none new

## Log
- 2026-08-31 ⏳ IN FLIGHT — Building overdue-review alerting on top of the existing `msp_alert_rules`/`msp_alert_events` platform alert engine (`alert-engine.ts`), per #1513's settled architecture: a lapsed review alerts the MSP, never the customer, and never changes the acceptance's validity. Adding a new `risk_review_overdue` condition type, a distinct `review_lapsed` severity, and the review-clock operational writer (`on_track`/`due`/`overdue` transitions on `msp_risk_decisions.review_state`) that #1507's bookend explicitly left for this issue. Rides the existing `alert_evaluate_rules` Workflow Engine node (already seeded, runs every 5 min) — no new workflow machinery. Ends at the wire contract per scope stop — no `artifacts/portal` UI.
- 2026-08-31 ✅ DONE — Shipped at `f256f5176ec7f508be3469099617e5f128df16fc`:
  - **`lib/db/src/schema/msp.ts`** — `"risk_review_overdue"` added to `MSP_ALERT_CONDITION_TYPES`; `"review_lapsed"` added to `MSP_ALERT_SEVERITIES` (now `warning | critical | info | review_lapsed`) with a comment explaining it is deliberately distinct so it never sorts in with an ordinary threshold-breach alert, and explicitly noting this is unrelated to `CUSTOMER_ALERT_SEVERITIES`/#1942's "exactly three values" rule — that rule governs the customer-facing notification catalogue (`customer_notification_preferences`), a different table and a different audience than the internal MSP platform alert engine (`msp_alert_rules`/`msp_alert_events`) this issue explicitly names.
  - **`artifacts/api-server/src/lib/alert-engine.ts`** — new `advanceRiskReviewClock()`: advances `msp_risk_decisions.review_state` (`on_track`/`due`/`overdue`, 14-day due lead before `review_due_at`) for every still-`active` acceptance with a scheduled review, idempotently (`IS DISTINCT FROM` guards, never touches `updated_at` on a row already correct), then returns the live `overdue` count. This is the review-clock operational writer #1507's bookend explicitly left unbuilt for this issue — `status` (the acceptance's own lifecycle) is never touched, only the review flag, per #1507's settled architecture. Wired into the existing `getConditionValue`/`buildSummary` switches as `risk_review_overdue`; `review_lapsed` severity gets its own violet email/badge color, distinct from critical's red and warning/info's amber.
  - **`lib/db/migrations/manual/2026-08-31-risk-review-overdue-alert-1513.sql`** — seeds the one `msp_alert_rules` config row this needs to actually fire (`ensureAlertEngineReady()` deliberately never auto-populates rules — ruleKey `risk_review_overdue`, severity `review_lapsed`, threshold 1, cooldown 1440min/24h so it keeps nagging daily while unresolved rather than firing once and going quiet). **Run against local `DATABASE_URL` in-session**, confirmed via the returned row. `deep_link_path` left `NULL` — no admin-panel Risk Register view exists yet (checked `artifacts/admin-panel/src/App.tsx`'s route table); a fabricated path would be worse than none.
  - **`artifacts/api-server/src/lib/seed-system-workflows.ts`** — updated the `__system__: Alert Rule Evaluation` seeded-workflow description to name the new condition. No new workflow/node type: this rides the already-seeded, already-visible `alert_evaluate_rules` Workflow Engine node (runs every 5 min) exactly as #1513 requires ("Built on the existing msp_alert_rules / msp_alert_events infrastructure, not new machinery" / "the lapse check is one [node] — no bare scheduler" from the build prompt comment).

## Verification (real, observed)

- **Live DB before change:** `msp_risk_decisions` — 1 row (`RBD-2026-575`, `status='pending_signature'`, `review_state` null). `msp_alert_rules` — 7 existing rows, none for this condition. Confirmed via direct `psql` query against local `DATABASE_URL`.
- **Migration:** ran clean (`BEGIN…COMMIT`), inserted exactly 1 new `msp_alert_rules` row (`risk_review_overdue` / `review_lapsed` / threshold 1 / cooldown 1440), self-marked in `simulator_migration_runs`.
- **Review-clock transition SQL:** proven directly against local Postgres inside a rolled-back `BEGIN…ROLLBACK` block with 5 synthetic rows (a 10-days-overdue active row, a 5-days-out active row within the 14-day lead, a 90-days-out active row, an already-`overdue` active row, and a `revoked` row 10 days past due). Result: overdue→`overdue`, due-window→`due`, far-out→`on_track` (unchanged), already-overdue row's `review_state` and `updated_at` both untouched (idempotence confirmed), revoked row's `review_state` untouched and its `status` never touched (acceptance-lifecycle gating confirmed). `overdue_count` came back `2`, matching the two active overdue rows. Rolled back — no residue in the live DB.
- **tsc:** `lib/db` rebuilt clean (`tsc -b .`, no `pnpm install`); `artifacts/api-server` — `tsc --noEmit` **0 errors**, whole project.
- **vitest:** ran the three test files touching the code paths involved — `dashboard-resolvers.test.ts`, `msp-alerts.test.ts`, `portal-risk-register.test.ts`. `msp-alerts.test.ts` and `portal-risk-register.test.ts` passed fully. `dashboard-resolvers.test.ts` had 2 pre-existing failures, both in the unrelated `licensing.wasteEstimateBreakdown` (cost-engine SKU pricing) suite — nothing in this diff touches cost-engine.ts, SKU pricing, or that resolver; not reported as mine per CLAUDE.md's #1765/#1772/#1774/#1777/#1778/#1781 carve-out for pre-existing unrelated failures. `alert-engine.ts` itself has no existing test file (none of its other `evalX` evaluators do either — same house convention followed here: verified live via rolled-back transaction rather than a `pool.query`-mocking vitest file that would test nothing about the actual SQL).
- **Scope stop honored:** no page/route/component created or touched in `artifacts/portal`; `Design/portal/` carries no export for this module (confirmed empty aside from `.gitkeep`).
- **`git status --porcelain` clean** before writing DONE. `verify-branch-merged.mjs` exit 0 against the real post-push hash above.
- **No new findings filed** — nothing broken or out-of-scope was discovered; the review-clock writer gap was the explicitly-named assigned work (per #1507's own bookend), not an incidental defect.
