# #1391 — Overview: "Scan now" button has no onClick handler

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 7b18f76a8

## Log
- 2026-08-27 ⏳ IN FLIGHT — Investigated #1298/#1300's manual scan trigger pattern (PortalV2ScanLanding.tsx, ScanTriggerButton.tsx, copilot-readiness.tsx): `debug-trigger-scan` is the platform's ONLY scan trigger and is hard-gated server-side to testbed tenants — real customers deliberately have no self-serve trigger (anti AI-credit-spam). Wiring `pv2-ov-scan` in portal-v2-overview.tsx to call debug-trigger-scan via fetchWithAuth, gated client-side on `scanStatus.data?.isTestbed` (same second-layer gate as ScanTriggerButton.tsx) — button now only renders for testbed tenants and disables/spins during an active scan.
- 2026-08-27 ✅ DONE — Wired `pv2-ov-scan` onClick to POST /api/portal/assessment/debug-trigger-scan via fetchWithAuth + the shared scan-status context (reportTriggerStarted/reportTriggerError), gated to `scanStatus.data?.isTestbed` so real tenants never see a self-serve trigger — matches ScanTriggerButton.tsx/#1300's established pattern exactly (chose "wire it" over "remove it": #1298/#1300 already prove a manual trigger IS the intended product behavior for a customer-facing scan surface, just testbed-only). Added test-manifests/portal/portal-v2-overview.json step OV4b asserting the button is absent for the real login tenant. `tsc --noEmit` on artifacts/msp-portal is clean (2 pre-existing unrelated errors in SowScreen.tsx/PortalV2Shell.tsx, untouched by this change). Could NOT live-verify OV4b via shaneapp://runTest — it runs against BuildConsole's deployed build, not this worktree's uncommitted state, and "no steps matched session tag" confirmed the deployed manifest predates this commit; will go green once 7b18f76a8 is merged into the dev-server checkout and redeployed. Pushed straight to origin/main (7b18f76a8) per repo convention.
