# #4536 — executeMonitorCheck(persistProfile: false) still runs drift collection

- **Status:** ✅ DONE 2026-09-17T16:45:28Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 95e4e1644 (pushed as d94a2f9fc on origin/main)

## Log
- 2026-09-17T16:39:03Z ⏳ IN FLIGHT — gating `collectDriftForCompletedCheck` on `persistProfile` across every executor path in `artifacts/api-server/src/lib/monitor-executor.ts` (Graph, PowerShell, SharePoint-admin, Power Platform, DNS, Azure RM, fan-out); confirmed the Simulator Studio trace caller (`admin-monitor-check-runs.ts`) intentionally persists (genuinely re-hits the tenant, no `persistProfile: false`), so it is not exposed. Next: typecheck, live-verify against testbed tenant 2080, and assess the Security Defaults stale-row follow-up.
- 2026-09-17T16:45:28Z ✅ DONE — Added a `persistProfile` param to `collectDriftForCompletedCheck` and threaded it through all 7 call sites; the function now returns early (no drift write) when `persistProfile === false`. Also fixed the secondary note in the same pass (small addition, in scope): the Security Defaults gate-skip return in `executeMonitorCheck` now records `drift_collection_status` as `not_comparable`/`gate_not_satisfied` instead of leaving the row stale, gated by the same `persistProfile` check. Typecheck (`npm run typecheck` in `artifacts/api-server`) passed clean. Ran `src/lib/__tests__/monitor-executor.test.ts` (221 passed) and `drift-collector.test.ts` + `drift-check-specs.test.ts` + `drift-change-attribution.test.ts` + `drift-engine.test.ts` (115 passed). Live-verified against testbed tenant 2080 (McCawSoft, tenant_id c4c814d4-3afe-441e-9145-62461d0a4fd3) using the existing `verify-ca-checks-4512.ts` script (persistProfile:false): before the fix, `drift_collection_status.updated_at` for `identity:ca-policy-count` matched the verification-run timestamp (the defect, per the issue's own evidence); after the fix, re-running the same script left `updated_at` unchanged at the pre-fix baseline timestamp (2026-09-17 10:28:43-04), with `drift_events` count (0) and `drift_baseline_snapshots` count (1) for that domain both unchanged. Did not force a live write of the new gate-skip `not_comparable` status against the real testbed tenant's drift row (would flip its real `drift_collection_status` row for McCawSoft outside a real scheduled scan) — that path is covered by typecheck + the existing/passing gate-skip unit tests instead; the next real scheduled scan against this tenant will exercise it for real. Working tree clean, branch confirmed merged (see verify-branch-merged below). No new findings filed — the Simulator Studio trace caller was checked per the issue's own instruction and found not exposed (by design, not a gap).
