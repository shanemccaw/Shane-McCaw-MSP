# #3895

- **Status:** ⏳ IN FLIGHT 2026-09-13T00:36:00Z
- **Scope:** platform

## Log

- 2026-09-13T00:36:00Z — Investigated the two pre-existing failing tests named in the
  build prompt, independently, against local Postgres (`DATABASE_URL` in `.env.local`):
  - `artifacts/api-server/src/lib/__tests__/azure-rm.test.ts` — real isolation bug.
    `azure-rm.ts` imports `./logger.ts`, which transitively imports `@workspace/db`
    (`log-stream-writer.ts`), which throws at module load if `DATABASE_URL` isn't set.
    This suite didn't mock `../logger.ts` the way sibling suites
    (`monitor-executor.test.ts`, `graph-consent-revoke.test.ts`, etc.) do, and 3 tests
    also do a genuine `await import("@workspace/db")` to reach its real
    `coverageStateFor`/`transportHasExecutor` logic — so the whole file only ever
    passed if `DATABASE_URL` happened to already be set in the ambient shell before
    vitest started. Confirmed reproducible: `unset DATABASE_URL` → 18/18 fail with
    `DATABASE_URL must be set`; after fix → 18/18 pass with no ambient env var at all.
  - `artifacts/api-server/src/routes/portal-assessment-debug-reset-session.test.ts` —
    thread-pool contention, same class vitest.config.ts already documents for #2877/
    #3066: this file's own `vi.setConfig({ testTimeout: 20_000 })` gave it zero real
    headroom above the global default it exists to raise. Under load the first test
    timed out at exactly 20s, and its still-in-flight mocked `db.delete()` calls then
    landed asynchronously during the *next* test, inflating that test's
    `state.deleteCalls` count (4 instead of 2) — one root cause producing two distinct
    symptoms, not two separate bugs. Raised to 60s to match `index.boot-smoke.test.ts`'s
    existing precedent for the same contention class.
- 2026-09-13T00:37:00Z — Both files pass together with `DATABASE_URL` unset (22/22).
  Full `vitest run` across the whole suite kicked off to confirm no regression
  (background; see follow-up log line for result).
