# #1736 — Settings: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T21:18:12Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 91850652f (IN FLIGHT bookend), f7ad167b6 (Settings page wiring), 521404adb (test
  manifest), b5262712a (manifest credential fix), 752e43768 (manifest verification record)

## Log
- 2026-09-14T20:58:38Z ⏳ IN FLIGHT — Read `Design/portal/design_handoff_full_site/screens/Shell.dc.html`'s Settings
  container directly (per the dispatch's own instruction) and found it diverges from the dispatch's "five sections"
  framing (Change Control / Departments / Ownership / Alert preferences / Account Security) drawn from `#4057`'s
  contract pack. The landed Design's real `settingsGroups`/`stab` state machine has six tabs: Alert preferences (new,
  `customer_alert_preferences`), Notifications (`#1662`'s 15-category bell), Email authentication, Webhooks,
  Departments (new), and Privacy/your data. Change Control stays only at `/change-control`
  (`settingsChangeControlWire.ts` is already consumed there via `PolicySection.tsx`, not orphaned). Ownership/RACI
  and Account Security each keep their own separate page/popover destination — neither is part of the Settings
  container in the actual design. Building against the landed Shell design, not the pre-Design contract-pack framing,
  per CLAUDE.md's "Design/portal is the live source" rule.
  Implementation: added `GET /api/portal/alert-preferences/rules` (new portal-facing read of the
  `customer_tenant_alert_rules` catalog — no prior customer-facing route served it); added
  `settingsAlertsWire.ts`/`settingsAlertsLive.ts` + `components/settings/AlertPreferencesContent.tsx` (new,
  7-category alert prefs, quiet hours, recipients) and `components/settings/DepartmentsContent.tsx` (new, wires the
  already-built `settingsDepartmentsWire.ts`/`Live.ts`, zero prior consumers per `#4051`); split
  `DataRightsAndPrivacyContent`/`EmailAuthSetupContent` out of their standalone pages (matching the
  `NotificationPreferencesContent`/`WebhooksContent` convention already established) so all six tabs share one fetch
  layer; new `pages/settings.tsx` consolidated page with the design's real 3-group left nav and `?tab=` deep-linking;
  registered `/settings` in `App.tsx`; pointed `UserMenu`'s "Settings" row and `AlertsDropdown`'s "Alert preferences"
  link at the real route instead of `/coming-soon`. No `*Data.ts` fixture module; no schema change needed (both
  backing tables already existed). `pnpm typecheck` clean in both `artifacts/portal` and `artifacts/api-server`.
- 2026-09-14T21:18:12Z ✅ DONE — Wrote/registered `test-manifests/portal/settings.json` and ran it for real via
  `shaneapp://runTest` against the merged dev-server checkout: **23/23 steps passed** (commit `b5262712a`) — real
  login, the new `/api/portal/alert-preferences/rules` route, `GET /api/portal/alert-preferences`,
  `GET /api/portal/settings/departments` (real, honestly-empty `{"departments":[],"unmapped":10}` for the testbed
  tenant), and all six Settings tabs rendering their real content inside the container. Along the way found this
  BuildConsole's `TEST_CUSTOMER_EMAIL`/`PASSWORD` Test Environment Variables are placeholder/unreviewed
  (`NeedsReview: true`, 7-char values) — silently breaks every manifest naming them, including the pre-existing
  `webhooks.json`/`notification-preferences.json`. Filed as `#4119` (parented to `#1596`, labelled `bug` +
  `Shane To-Do`, board status AI Batter Up); worked around in this manifest by using the real, reviewed
  `TEST_RBAC_CUSTOMER_EMAIL`/`PASSWORD` pair instead. Also posted a correction to `#1596`'s own 2026-09-14 note,
  which undercounted the real Settings container as "5 tabs in 2 groups" — it's actually 6 tabs in 3 groups (missed
  the "YOUR ACCOUNT — Privacy and your data" group entirely). `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms `agent/1736-q2782` (752e43768) is merged into `origin/main`. Real commits:
  `91850652f`, `f7ad167b6`, `521404adb`, `b5262712a`, `752e43768`.
