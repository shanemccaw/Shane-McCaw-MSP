# #3812 — Establish one consistent, eye-friendly primary-action button style app-wide

- **Status:** ✅ DONE 2026-09-12T20:01:09Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 344cbcd9a

## Log
- 2026-09-12T19:55:59Z ⏳ IN FLIGHT — Blockers #3808/#3809 confirmed closed. Investigating reference pattern (Ask Claude button, GitDoctorView.xaml.cs ~line 728) and the 6 target buttons across GitDoctorView.xaml(.cs), GraphApiDocumentView.xaml, IssueDetailView.xaml, LeftSidebar.xaml, plus what #3808/#3809 actually landed in DarkTheme.xaml so the new tint brushes/tokens reconcile with their real names instead of duplicating.
- 2026-09-12T20:01:09Z ✅ DONE — Confirmed #3808/#3809 landed as solid MaroonBrush fills (Black text, no border) — not yet the tint pattern this issue establishes; #3811 had already retargeted the old `Brush.Epic.*` local keys to real DarkTheme keys, so nothing to rename here. Added `MaroonBrushTint`/`PeachBrushTint`/`BlueBrushTint`/`SapphireBrushTint` to `Themes/DarkTheme.xaml` (0.13 opacity, matching the proven "Ask Claude" reference exactly; `BlueBrushTint` follows `BlueBrush` in referencing `AccentColor` rather than a literal hex). Applied the tint fill + full-saturation border + full-saturation text pattern to all 6 real buttons from the issue's table: `GitDoctorView.xaml` "End this git nightmare" (Maroon) and "Run Plan" (Peach); `GitDoctorView.xaml.cs` "Delete N selected" (Maroon, reconciling #3808/#3809's still-solid-fill landing to this final pattern); `GraphApiDocumentView.xaml` "⚡ Execute Graph API" (Peach); `IssueDetailView.xaml` "Post Comment" (Blue); `LeftSidebar.xaml` "🚀 Deploy to Staging" (Sapphire). Left the out-of-scope small/thin elements listed in the issue untouched. Verified via `dotnet build BuildConsole.csproj -clp:ErrorsOnly` — 0 errors, no new warnings on any touched file. Live UI click-through/visual spot-check was NOT self-verified — per the standing `never-launch-BuildConsole.exe-for-verification` rule (Git #2156), a freshly-built BuildConsole.exe can courier into Shane's real running instance via its single-instance mutex, so this class of change gets a clean build + code-read verification, not live UI automation; Shane should eyeball the 6 buttons himself. No findings filed — no genuine new bug/gap surfaced during this build. Commit 344cbcd9a pushed directly to origin/main (merged, verified via verify-branch-merged.mjs). Working tree clean (`git status --porcelain` empty).
