# #1474 — Licensing ledger silently substitutes the fixture instead of a real-but-zero ledger

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** d12f4e357 (fix + test manifest update), 86cf32a6b (lastVerifiedAgainstCommit bump)

## Log
- 2026-08-28 ⏳ IN FLIGHT — fixing `ledgerIsLive` in `portal-v2-licensing.tsx` to be `Boolean(liveLedger)` alone (drop the `totalUnassigned > 0` gate), updating `ledgerCards`/`ledgerTotals` accordingly. Recovery buckets stay out of scope (already handled by #1446).
- 2026-08-28 ✅ DONE — `ledgerIsLive` is now `Boolean(liveLedger)`; a real, fully-assigned tenant's real zero-waste ledger renders instead of the fixture. `test-manifests/portal/portal-v2-pillars.json` updated: L6b/L6/L7/L8/L8b now assert the real 'live' source + real extracted totals (A2c/A2d) instead of stale fixture literals; the old L9-L19 fixture-card expand/FixPanel/CR-gate walkthrough is retired with a plain explanation in the file's `notes` (this testbed tenant's ledger can no longer reach that fixture-only path, since its real ENTERPRISEPACK card has `hasActions: false` — a real, named gap for a future session's unscanned/unpriced-SKU test fixture, not built here). `tsc --noEmit` on `artifacts/msp-portal` is clean for the changed file (2 pre-existing, unrelated errors elsewhere in the package, confirmed untouched by this session). NOT LIVE-RUN via `shaneapp://runTest`: the local dev-server checkout's merge-back hit a genuine, unrelated conflict (`security-drilldowns.json`, `build-journal/1430.md`, `portal-v2-security-evidence.tsx`, `secEvidenceData.ts` — none of them files this session touched, from another concurrent session's in-flight work) that auto-aborted cleanly rather than corrupting the shared checkout; verification instead rests on the confirmed clean typecheck and a direct read of `resolveLicenseSkuLedger`'s arithmetic (`monthlyWasteCents = unassigned * price`, so `totalUnassigned === 0` guarantees `totalMonthlyWasteCents === 0`, which is what the new $0/mo assertion depends on). Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing DONE (commit `86cf32a6b413372b33e2d8331105ea53495046e8`).
