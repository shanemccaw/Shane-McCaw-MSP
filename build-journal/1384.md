# Git #1384 — Build Watch chat text box still doesn't send despite claimed fix (commit 9e7842bda)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **GitHub:** #1384 (regression follow-up to #1327, epic #1202)
- **Commit:** see final commit hash below

## Log

- ⏳ IN FLIGHT — Pulled #1384/#1327/#1202 context. Confirmed commit `9e7842bda` exists on origin/main and touches `BuildWatchWindow.xaml.cs`, `FocusImmersiveView.xaml.cs`, `GitDetailView.cs`, `QueueWatcherService.cs`. #1327's fix was verified only with isolated scratchpad harnesses, not the real running UI text box. Shane reports the real Build Watch text box still doesn't send. Investigating the gap between the fixed logic and the actual live UI chat input control wiring.
- ✅ DONE — Found the REAL root cause with live evidence from the running app's own ActivityLog (`%AppData%/BuildConsole/logs/activity-2026-08-26.log`, 21:14:57): Shane sent "a" into the Build Watch box for queue #640 (finished build #1366) → it correctly routed through the #1327 resume path → but the continuation launch threw `Couldn't force-launch continuation #640: Column must be between 0 and 15`, so nothing visibly happened. **The #1327 fix was correct; the resume path it hands off to was itself broken by a pre-existing column-count bug.** `QueueWatcherService` resume → `BuildQueuePostgresClient.ForceClaimAsync` → `MapRow`. `MapRow` reads ordinal 16 (`build_set`), but `ForceClaimAsync`'s `RETURNING` clause was the only one in the file missing `build_set` — it returned just 16 columns (0–15), so `MapRow`'s `r.IsDBNull(16)` threw the Npgsql "Column must be between 0 and 15" out-of-range error. The scratchpad harnesses drove `claude.exe` directly and never went through `ForceClaimAsync`, which is exactly why they passed while the real UI failed — the scratchpad-vs-real-UI gap #1384 asked about. Fix: added `build_set` to `ForceClaimAsync`'s `RETURNING` (1 line + explanatory comment). Verified: (1) re-ran the real stream-json protocol against the live `claude.exe 2.1.247` with BuildConsole's exact flags/framing — idle second-message and mid-turn steer both deliver, confirming the protocol was never the problem; (2) `dotnet build BuildConsole.csproj -c Debug` → 0 errors (42 pre-existing warnings, none on the changed line); (3) confirmed against the real local Postgres that the fixed 17-column list is valid SQL and `build_set` exists at ordinal 16. BuildConsole WPF tooling is outside the msp-portal/marketing test-manifest scope, so no manifest. **Shane To-Do:** rebuild + relaunch BuildConsole — the currently-running `.exe` is the old binary.
