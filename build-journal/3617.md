# #3617 — Critical-path "⛓ blocks N" badge undercounts under an active filter

- **Status:** ✅ DONE 2026-09-12T16:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 257ddc5a6 (fix), 788c0ad55 (merged to main)

## Log
- 2026-09-12T16:31:33Z ⏳ IN FLIGHT — confirmed the bug: `RenderQueue` (BuildQueuePanel.xaml.cs)
  called `ComputeDownstreamBlockCounts(items)` using the status-filtered list instead of
  `rawItems`, the full unfiltered queue snapshot — same root-cause class already fixed for
  `ComputeReverseBlocks`/#3601 and the forward ghost cards/#3599. Applying the same fix shape:
  pass `rawItems` to `ComputeDownstreamBlockCounts`.
- 2026-09-12T16:45:00Z ✅ DONE — changed `RenderQueue`'s call at BuildQueuePanel.xaml.cs to
  `ComputeDownstreamBlockCounts(rawItems)` instead of `ComputeDownstreamBlockCounts(items)`.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` succeeded, 0 errors (only
  pre-existing unrelated warnings). Branch verified merged into origin/main via
  `verify-branch-merged.mjs` (commit 788c0ad55). Working tree clean.
