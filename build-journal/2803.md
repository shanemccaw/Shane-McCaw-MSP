# #2803 — Band 1 context bar: epic name next to #1202

- **Status:** ✅ DONE 2026-09-04T03:39:37Z
- **Scope:** buildconsole

## Log
- 2026-09-04T03:37:12Z — Adding `Cool.EpicName` brush + `CtxEpicName` TextBlock in Band 1 XAML
  (`Controls/ChatDocumentContainer.xaml`), wired in `RefreshContext()`
  (`ChatDocumentContainer.xaml.cs`) from the already-resolved `epic.Title`. Additive only —
  `CtxEpicNum` untouched.
- ✅ DONE — 2026-09-04T03:39:37Z. Added `Cool.EpicName` brush (`#8b93a1`, a new muted tone —
  not reused from any status `Cool.*` color) and a new `CtxEpicName` TextBlock right after
  `CtxEpicNum` in Band 1 (`ChatDocumentContainer.xaml`). `RefreshContext()` now sets
  `CtxEpicName.Text = epic?.Title ?? ""` and collapses it when empty — no fake placeholder.
  `CtxEpicNum`'s color and click-to-open behavior unchanged. `dotnet build BuildConsole.csproj -c
  Debug` succeeded, 0 warnings/errors. Commit `ad65eeb8f` pushed directly to `origin/main`
  (verified via `verify-branch-merged.mjs`). Working tree clean.
