# #4059 — Coupon redemptions are never recorded

- **Status:** ✅ DONE 2026-09-14T15:35:14Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ddf23de6f (rebased/pushed to origin/main as 573b19476)

## Log
- 2026-09-14T15:26:43Z ⏳ IN FLIGHT — Investigated: confirmed nothing writes `coupon_redemptions` or increments `coupons.uses_count`; the one real runtime consumer of a coupon code is `portal-onboarding.ts:607-624` (contract signing). Wiring real redemption recording + real `maxUses` enforcement there.
- 2026-09-14T15:35:14Z ✅ DONE — `artifacts/api-server/src/routes/portal-onboarding.ts`: coupon is now resolved and validated once per signing action (active / not expired / not exhausted via `maxUses` vs `usesCount`); an invalid code now returns a real 400 instead of silently no-opping. After the contracts for that signing action are created, one `coupon_redemptions` row is inserted (`checkoutSessionId: contract-signing:<contractIds>`, real `purchaseAmount` summed from the created contracts' `finalPrice`) and `coupons.uses_count` is incremented, both in one `db.transaction`. `tsc --noEmit` clean. Verified against local Postgres with a disposable maxUses=1 test coupon via a throwaway `tsx` script (deleted after use, no residual rows): redemption row inserted correctly, `uses_count` went 0→1, and the exhaustion check correctly flipped to "not usable" for a second attempt. No HTTP-level test was run (no dev api-server was live in this worktree — confirmed via `node scripts/dev-server/status.mjs`, server process shown DEAD); the DB-transaction logic that was the real risk is verified directly. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. Working tree clean.
