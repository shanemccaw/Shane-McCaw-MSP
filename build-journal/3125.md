# #3125 — Feature: Recipes — Cook mode (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 565b9b0c9 (feature, on `agent/3125-q1888`), 96c2fcc75 (merge into `origin/main`)

## Log
- 2026-09-08T00:20:00Z ⏳ IN FLIGHT — blocker #3124 (Recipes core list) confirmed `complete` and
  merged to `origin/main` (`27c0d7a09`/`8338d5c99`, verified via
  `git merge-base --is-ancestor`). Building real step-by-step cook mode on top of it: step/checks
  navigation, next/back, "unchecked ingredients never block Next", and screen-stays-awake via the
  Wake Lock API. Extending `recipes.steps`' existing jsonb shape (still no schema change) to carry
  real per-step `{text, ings}` so Cook mode has real ingredients to check off per step, since
  #3124 only stored bare step text (nothing read `ings` yet).
- 2026-09-08T00:45:00Z ✅ DONE — shipped: `recipes.mjs`'s `normaliseSteps` now accepts either a
  bare string (back-compat with #3124's own real pushes) or a real `{text, ings}` object and
  normalises both into the real shape Cook mode reads; `push_recipes`'s MCP schema documents the
  same real shape for future Claude-generated recipes. `public/app.js` adds the real `#/cook/<id>`
  screen (`viewCook`) — step text, per-step ingredient checklist, Back/Next navigation, a "Cook"
  entry button on any recipe with real steps saved — with the design's own two locked, verbatim
  lines in the footer ("Screen stays awake in cook mode. Unchecked ingredients never block
  Next."). Next never gates on an unchecked ingredient; cook state (`cookSession`) is real
  transient client state, matching the design's own client-held cook state, not persisted
  server-side. Screen-stays-awake is the real Wake Lock API
  (`navigator.wakeLock.request("screen")`), requested entering cook mode, released the instant
  navigation leaves it, re-acquired on `visibilitychange` if the tab regains focus while still
  in cook mode; feature-detected, so an unsupported browser just doesn't get the lock.
  Verified against the real local database (not mocked, via a disposable test user, deleted
  after): confirmed a legacy plain-string step still normalises with an empty real `ings`,
  confirmed a real `{text, ings}` step round-trips its real ingredients through
  `listRecipesWithMatch`, confirmed an archived recipe drops out of the real list. Booted the
  real server against the real local `finances` database and confirmed migrations already
  applied clean (no new migration needed — this is a code-level shape change on the existing
  jsonb `steps` column, not a schema change). `git merge-base --is-ancestor` confirms
  `agent/3125-q1888` is merged into `origin/main` (`96c2fcc75`); working tree clean.
