# #3125 — Feature: Recipes — Cook mode (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08

## Log
- 2026-09-08T00:20:00Z ⏳ IN FLIGHT — blocker #3124 (Recipes core list) confirmed `complete` and
  merged to `origin/main` (`27c0d7a09`/`8338d5c99`, verified via
  `git merge-base --is-ancestor`). Building real step-by-step cook mode on top of it: step/checks
  navigation, next/back, "unchecked ingredients never block Next", and screen-stays-awake via the
  Wake Lock API. Extending `recipes.steps`' existing jsonb shape (still no schema change) to carry
  real per-step `{text, ings}` so Cook mode has real ingredients to check off per step, since
  #3124 only stored bare step text (nothing read `ings` yet).
