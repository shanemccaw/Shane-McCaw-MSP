# #2929 — normalizeGraphEndpoint() doesn't strip absolute-URL host, under-counting beta-targeted check coverage

- **Status:** ✅ DONE 2026-09-05T01:45:00Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 939d82aaa (pushed to main as 50f6ae0aa)

## Log
- 2026-09-05T01:20:21Z ⏳ IN FLIGHT — fixing `normalizeGraphEndpoint()` in `scripts/config-state/map-monitor-checks.mjs` to strip a real absolute `https://graph.microsoft.com/{version}/...` host+scheme prefix before comparing to `graph_path`.
- 2026-09-05T01:45:00Z ✅ DONE — added a `GRAPH_ABSOLUTE_URL_PREFIX` strip (`https://graph.microsoft.com/(v1.0|beta)`) before the existing query-string/trailing-slash normalization. Verified two ways: (1) unit-level cases for `normalizeGraphEndpoint()` covering the beta absolute URL, a v1.0 absolute URL, and all pre-existing relative-path forms — all pass, none regressed; (2) ran the real `matchEndpointToResource()` against the live local Postgres data for `monitor_checks.id=209` (`identity:transitive-role-assignments`'s real shipped beta endpoint) and `config_resources.id=10073` (`/roleManagement/directory/transitiveRoleAssignments`, graph_version=beta) — now resolves `{ configResourceId: 10073, basis: 'graph-path-exact', confidence: 'high' }` instead of the prior `unmatched`, matching the issue's stated expected outcome exactly. Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0); `git status --porcelain` clean. No test-manifest required — `scripts/config-state/` is a platform script, not `artifacts/msp-portal` or `artifacts/shane-mccaw-consulting`, so it's out of that scope. Not a #1128-parented finding; this session confirmed the fix rather than discovering a new one.
