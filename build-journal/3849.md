# Git #3849 — QueueWatcherService dropped completion-trigger re-run

- **Status:** ✅ DONE 2026-09-13T06:10:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T05:50:00Z — Claimed dispatch, read issue body + BUILD comment. Starting investigation of `QueueWatcherService.TickAsync` single-flight guard around line 1785 and `ArmCompletionTrigger` around line 474.
- 2026-09-13T06:10:00Z — ✅ DONE — Added `_tickRerunRequested` (Interlocked int) alongside the existing `_ticking` guard. When a caller (e.g. `ArmCompletionTrigger`'s build-exit callback) hits the single-flight guard while a tick is already running, it now sets `_tickRerunRequested` instead of the trigger being silently dropped. Split the previous `TickAsync` body out into `TickOnceAsync`; the outer `TickAsync` now loops (`do { reset flag; await TickOnceAsync(); } while (flag was set)`) before releasing `_ticking` in `finally` — still single-flight, coalesced so multiple re-run requests during one pass only cause one extra pass. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` succeeded (0 errors/warnings) both before and after a cosmetic re-indent of `TickOnceAsync`'s body (dropped one level of indent it inherited from the old wrapping `try`). Commit `0970fef6f` (rebased/pushed as `649686a68`), pushed directly to `origin/main` (agent branch had no other unmerged history) and verified via `node scripts/dev-server/verify-branch-merged.mjs` — OK. Working tree clean (`git status --porcelain` empty).
