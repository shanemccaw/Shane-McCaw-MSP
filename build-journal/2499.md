# #2499 — portal/customer/timeline scopes documents/offers to one login, not the customer

- **Status:** ✅ DONE 2026-09-03T03:03:06Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 529f1bfb6ea06228bf00a0499157ad08ef638d4d (fix), merged to origin/main as ea835cc0

## Log
- 2026-09-03T02:54:56Z ⏳ IN FLIGHT — replacing `eq(insightsGeneratedDocumentsTable.customerId, userId)` and `eq(salesOffersTable.customerId, userId)` in `artifacts/api-server/src/routes/portal-customer-timeline.ts` with the `resolveCustomerUserIds(customerId)` multi-login bridge, mirroring the pattern already used 4x in sibling route `portal-customer-engines.ts`.
- 2026-09-03T03:03:06Z ✅ DONE — both `eq(..., userId)` sub-query filters replaced with `inArray(..., customerUserIds)` where `customerUserIds = await resolveCustomerUserIds(customerId)`, computed once and shared by both queries, matching the sibling `portal-customer-engines.ts` pattern exactly. Removed the now-unused `userId` local. Verified: diff review against sibling route's real usage (same import, same call shape); `npx esbuild` isolated-file bundle of the modified file succeeded (no syntax errors). Full `tsc --noEmit` in this worktree fails on unrelated pre-existing errors (`lib/db/dist` not built, `drizzle-orm`/`vitest`/`zod`/`stripe` module resolution) — confirmed identical failure class on the untouched sibling `portal-customer-engines.ts`, so this is a pre-existing worktree dependency-linking gap, not caused by this change (see CLAUDE.md's `pnpm install is not a remedy` section — not attempted/prescribed as a fix). Merged into `origin/main` as `ea835cc0` after two concurrent-push retries (main moved twice under load); `verify-branch-merged.mjs` confirms OK. Working tree clean.
