# #2588 — Remediation Tracking: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:14:57Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 473726bf1 (rebased to 38ca6c28f on main)

## Log
- 2026-09-12T03:09:36Z ⏳ IN FLIGHT — blocker #3677 (MSP Console Shell) confirmed DONE/closed (commit 7888bf1d6); building the Remediation module (Tracker/Checklist/Fix routes/Outside CR tabs) against the real msp-remediation-* routes and mounting it at `/tenants/:id/rem` in the console shell.
- 2026-09-12T03:14:57Z ✅ DONE — Built `artifacts/msp-console/src/api/remediation-{types,api}.ts` (mirroring the customer portal's own remediation-tracker/checklist wire types — no shared package between independent `artifacts/*` apps) and `artifacts/msp-console/src/console/modules/Remediation.tsx`, mounted at `/tenants/:id/rem` via `moduleFor()` in `ConsoleShell.tsx` (joining the dispatcher a concurrent build had already introduced for Runbooks, rather than adding a second one). Four tabs, each against real live endpoints:
  - **Tracker** — `GET .../remediation-tracker/catalogue`, step drawer PUT `.../steps/:stepId`, the pointed re-scan `POST .../steps/:stepId/verify` (the only legitimate closer), `GET .../verification-guide`, and evidence attach `POST`/`GET .../steps/:stepId/evidence` (#3503, multipart upload + blob-fetch view since the file route is bearer-only).
  - **Checklist** — `GET .../remediation/checklist`, `POST .../checklist/:checkKey/raise-change`.
  - **Fix routes** — `GET .../remediation/fix-routes`, CR-gated `POST .../fix-routes/:checkKey/reveal`. `we_can_run` cards deliberately point at Change Control's own Executions page rather than wiring a button to an execution route outside this build's real scope (no route for arming a config-pack fix was among the 8 files this issue named).
  - **Outside CR** — `GET .../remediation/bypass-resolutions` (read-only).
  - Pillar score tiles (`GET .../remediation-tracker/pillar-scores`) and CSV/PDF/evidence-pack exports (blob-download, same idiom as the portal's `RemediationExportControls`).
  Decline-to-risk (`POST .../checklist/:checkKey/decline-to-risk`) was left unwired — the design's own mock shows `accepted_risk` as a locked, customer-only option in the Tracker drawer and has no decline-to-risk affordance in the Checklist tab either, and the dispatch's own scope list (steps/checklist, evidence attach, CR-gated execution, pointed re-scan) doesn't name it.
  No fixture data anywhere — every row/tile/badge comes from a live endpoint.
  **Verified:** `tsc --noEmit` clean, production `vite build` clean (1759 modules, 112 kB gzip), all 11 pre-existing `msp-console` unit tests still pass. No test manifest — `artifacts/msp-console` is out of test-manifest scope per CLAUDE.md. No findings filed — no genuine gap surfaced beyond the two scope notes above (both are correct scope boundaries, not bugs).
  Two rebases were needed against concurrent `msp-console` work (a Runbooks module landing on `ConsoleShell.tsx`/`icons.tsx` at the same time) — merged by joining its `moduleFor()` dispatcher and de-duplicating shared Lucide icon imports, re-verified (typecheck/build/tests) after each rebase. `verify-branch-merged.mjs` confirms `agent/2588-q2388` (38ca6c28f) is an ancestor of `origin/main`; working tree clean.
