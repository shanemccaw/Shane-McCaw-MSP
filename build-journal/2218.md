# #2218 — Git Doctor: add real mini rail panel (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T14:54:10Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit:** `33cbe09af` "Git Doctor: real mini rail panel (ShaneBuilder, #2218)" — real ancestor
  of `origin/main`, verified via `scripts/dev-server/verify-branch-merged.mjs` and
  `git cat-file -e 33cbe09af^{commit}`.

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — Building the Git Doctor mini rail panel per the comment
  already posted on #2194 (README-ClaudeChat.md §6.2: Summary → Findings list → Claude bridge →
  Plan → Run log). Blocked-by #2209 is DONE/closed → unblocked. Real audit before writing code:
  `Services/GitDoctorService.cs` (486 lines, real git integration, already landed, #2194) and
  `MainWindow.xaml(.cs)`'s existing `GitDoctorDock` full-document tab (findings, branch janitor,
  clipboard-based Claude bridge, plan extraction, run log) — reusing every piece of this state
  verbatim, not reimplementing. Established mini-panel precedent audited from #2214 (Repo Health)
  and #2215 (SQL Runner): both bolt a 7th... well their own Nth `ClaudeChatDock` flyout column
  sharing state with a full document via generalized `Render*(StackPanel target)` overloads —
  following that exact pattern for Git Doctor's mini panel as the 7th column (`GdMiniColumn`).
  Plan: generalize `RenderGitDoctorFindingsList`/`RenderGitDoctorPlan`/`RenderGitDoctorLog` to
  take a `StackPanel target` (full-doc + mini both call through), add `RenderGitDoctorMini()` for
  the summary/nightmare-button section, and route the mini panel's Claude-bridge actions through
  the real `AppendToComposer` (not Clipboard) since this panel sits inside `ClaudeChatDock` next
  to the real app-owned composer — the one genuine behavioral difference from the full document.
- 2026-09-02T14:54:10Z DONE — Shipped the Git Doctor mini rail panel onto ClaudeChatDock as a
  new flyout column (GdMiniPanel), toggled via a new BtnToggleGitDoctorMini context-bar button.
  Every piece of state is shared with the real, already-landed GitDoctorDock full document -
  _gdFindings/_gdPlan/_gdLog/_gdRunning/_gdRepoStatus etc. - via generalized
  RenderGitDoctorFindingsList(StackPanel)/RenderGitDoctorPlan(StackPanel)/
  RenderGitDoctorLog(StackPanel) overloads the full document's own render methods now call
  through, plus a new RenderGitDoctorMini() for the mini-only Summary section. No second copy of
  findings/plan/log state, no reimplementation of GitDoctorService. Landed per §6.2: Summary
  (headline, branch/ahead-behind subline, red "Fix My Git Nightmare" button wired to the real
  RunGitDoctorNightmareAsync) -> Findings list (severity dot + title + chip, clickable -
  SelectGitDoctorFinding now re-renders both the full doc's and the mini's own list) -> Claude
  bridge (GdMiniSendFindings_Click writes the selected/all-open findings as markdown via
  AppendToComposer; GdMiniInboundBox + GdMiniExtract_Click parse a pasted reply into _gdPlan
  via a new shared ExtractGitDoctorPlanFrom(string)) -> Plan (checkbox rows, GdMiniRunPlan_Click
  runs the approved commands through the real RunGitDoctorStepsAsync/GitDoctorService) -> Run log
  (mono scroller, streams the same _gdLog the full document's log streams into). The one real
  behavioral difference from the full document (documented in #2194's own bookend as clipboard-
  based): this panel sits inside ClaudeChatDock next to the real app-owned composer, so its bridge
  writes into AppendToComposer instead of the clipboard - never auto-sends, per §5.
  Merge note: two concurrent sessions landed new mini panels on the same two files while this one
  was in flight - #2217 (JSON Viewer + File Browser, columns 6-7) and #2213 (Git Map, column 8).
  Rebased three times against origin/main; resolved by hand each time keeping every panel's own
  toggle button, ColumnDefinition and full Border block intact side by side - GdMiniPanel
  ended up on column 9. dotnet build clean (0 warnings, 0 errors) after each resolution and once
  more post-final-rebase, confirming all six mini panels (Repo Health, SQL Runner, JSON Viewer,
  File Browser, Git Map, Git Doctor) coexist without a name collision.
  Verification: dotnet build -c Debug clean throughout. WPF surface - BuildConsole/ShaneBuilder is
  out of scope for test manifests; did not launch the exe - no new service code was added here
  (only rendering/wiring reusing the already-landed, already-live-verified GitDoctorService), so a
  live click-through was not the deciding verification signal for this pass.
  Committed 33cbe09af, pushed to origin/main, branch verified merged
  (verify-branch-merged.mjs -> OK), commit hash verified as a real object
  (git cat-file -e 33cbe09af^{commit}), working tree clean (git status --porcelain empty).
  No findings filed - the only open gap (README-ClaudeChat.md itself not committed to the repo)
  was already tracked as #2227 by a prior session; not re-filed.
