# #3030 — Security Plan: correct the contract pack — legacy tables actually dropped

- **Status:** ✅ DONE 2026-09-06T19:45:00Z
- **Scope:** platform

## Log
- 2026-09-06T19:30:17Z — Starting: verify portal_security_plan* tables dropped via live DB check, then correct docs/security-plan-contract-pack.md §0.2 and open-items.
- 2026-09-06T19:45:00Z — ✅ DONE — Confirmed live against local `DATABASE_URL` (`information_schema.tables`) that all four legacy `portal_security_plan*` tables (`portal_security_plans`, `portal_security_plan_sections`, `portal_security_plan_rows`, `portal_security_plan_versions`) no longer exist — 0 rows returned. Matches `lib/db/migrations/manual/2026-09-04-drop-legacy-portal-security-plans.sql` (all four `DROP TABLE IF EXISTS`). Corrected `docs/security-plan-contract-pack.md`: §0.2 header/body ("code path removed, tables not yet dropped" → "…tables now dropped"), the migration citation at the top of the pack ("destructive, not yet run locally" → "destructive, run locally 2026-09-06"), the §0.4 row-count table (0 → "dropped — table does not exist"), and removed the now-resolved §7 open item #1 (renumbered the remaining two). No code change — doc-only correction per the issue. Commit c... merged to main: 54c121374.
