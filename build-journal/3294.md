# #3294 — Phase 1: Real schema/drift audit — ShanesSurvival financial core vs shanes-life's own migrations

- **Status:** ✅ DONE 2026-09-08T00:30:00Z
- **Scope:** other (personal project — ShanesSurvival/shanes-life, Shane's milestone, not MSP)
- **Started:** 2026-09-08
- **Commit(s):** e3fc74d17 (audit doc), caff5e111 (IN FLIGHT bookend)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read-only audit: enumerating ShanesSurvival's financial-core tables/columns (`desktop/ShanesSurvival/migrations/`), then cross-checking shanes-life's 52 migrations (`web/shanes-life/migrations/013_*.sql` through `065_*.sql`) for any ALTER touching the same tables. No writes to either database.
- 2026-09-08T00:30:00Z ✅ DONE — Full table/column audit written to `desktop/ShanesSurvival/docs/financial-core-schema-audit-3294.md`: enumerated all 11 ShanesSurvival financial-core tables (plaid_items, accounts, transactions, debts, survival_snapshots, pay_period_plans, pay_period_plan_allocations, income_sources, income_entries, expected_one_time_events, transaction_tags) with every column and which of ShanesSurvival's own migrations 001-012 added it. Cross-checked all 52 shanes-life migrations (013-065, 039 unused — documented renumber collision) and found 9 that touch shared tables (017, 033, 038, 041, 042, 043, 044, 045, 048, 049) — every touch is additive (ADD COLUMN IF NOT EXISTS / CREATE TABLE IF NOT EXISTS / new index/FK), none renames/drops/retypes an existing column, no NOT NULL column added without a default, and ShanesSurvival's own repository code (DebtRepository.cs, IncomeRepository.cs, PlaidLinkService.cs, PlaidSyncService.cs) uses explicit column lists everywhere (no SELECT * / bare INSERT), so it's structurally immune to the new columns. Conclusion: zero open compatibility risk. One documented convention (not a schema-level guarantee) flagged for Phase 2 awareness: shanes-life's own header comments say it treats `accounts.target_amount/balance/role/due_day` as ShanesSurvival's-own-write-only by convention, not enforced by any DB constraint/trigger/role. No database was written to; only `.sql` migration files and `.cs` source were read. `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
