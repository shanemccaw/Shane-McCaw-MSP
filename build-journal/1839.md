# #1839 — Adopt live builds by pid on BuildConsole restart, remount into Build Watch

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30

## Log
- 2026-08-30 ⏳ IN FLIGHT — Restarting BuildConsole while builds run currently marks every `running` row failed -2 (`RecoverOrphanedRunningItemsAsync`), because #822's "anything running must be orphaned" assumption was falsified by #1804 (builds now survive app close via durable-file stdout/stderr). Adding a stored `build_pid` + process-creation-time fingerprint to `bt_build_queue`, replacing the orphan assumption with a real liveness check (OpenProcess + GetProcessTimes creation-time match), adopting live builds back into `_running` + re-arming the raw tailers so their Build Watch slot remounts and streams, and honestly disabling the stdin chat box for adopted builds (the interactive pipe died with the old app). Manual migration → append to #1630, not a Shane To-Do.
