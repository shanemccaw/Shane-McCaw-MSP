# #2483 — Wire Build Chain Map as left-toolbar entry point

- **Status:** ⏳ IN FLIGHT 2026-09-02T23:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-02T23:05:00Z — Started. Blockers #2480/#2481 both closed (`complete`). Real audit of the old "Build Queue Map" (#2110) button/handler: `ActivityBar.BtnBuildQueueMap_Click` → `MainWindow.ToggleBuildQueueMap()` → `new BuildQueueMapWindow(...)`, which reads a real live snapshot via `BuildQueuePostgresClient.GetQueueMapAsync` (real SQL, real gh-open-issue blocker resolution, real deadlock detection) — this was genuinely FUNCTIONAL, not broken, when `_queueDb` is available. It degrades to a clear "Not connected" message only when `AppMode.IsAgent` (agent build sessions) forces `db` to null, which is correct/intended, not a bug. Noting this discrepancy against the issue's "non-functional" framing, but proceeding per #2473/#2483's explicit instruction — retiring this toolbar slot in favor of the new Build Chain Map is Shane's own UI-design decision recorded on the issue, not a bug-fix premise to block on.
- Wired: `ActivityBar.xaml`/`.xaml.cs` — replaced `BtnBuildQueueMap`/`BuildQueueMapToggleRequested` with `BtnBuildChainMap`/`BuildChainMapRequested`. `MainWindow.xaml.cs` — replaced `_buildQueueMap`/`ToggleBuildQueueMap()` with `_buildChainMap`/`OpenBuildChainMap()`, which opens `BuildChainMapWindow` maximized (`WindowState.Maximized`), Owner=this, single-instance (Activate() if already open). Epic number resolution: `LeftSidebar.ActiveEpicGithubNumber` (new public getter added) if Git Board has a "WORKING" epic set, else a small inline prompt dialog (`PromptForEpicNumber`) — no epic-picker existed inside `BuildChainMapWindow` itself (constructor takes a bare `int epicNumber`), so this was necessary to make the toolbar entry usable standalone rather than only reachable from a pre-selected epic.
- `dotnet build BuildConsole.csproj -c Debug` — succeeded, 0 warnings, 0 errors.
- No genuine new findings beyond the "non-functional" premise note above (not filed as an issue — it's a framing note, not a bug; the old code path is being intentionally retired, not fixed).
