# #3159 — Feature: Location-aware content surfacing (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:24:13Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 49062dd3e (bookend), 62bde8642 (feature), 3002a8ea6 (migration renumber 037→040 -- real live collision with a concurrent sibling build), merged to origin/main at c9db0c876

## Log
- 2026-09-08T02:19:23Z ⏳ IN FLIGHT — Investigated the issue's own explicit stop condition
  (browser Geolocation/geofencing feasibility for a Home Screen web app). Confirmed via live
  web search: automatic background geofencing is not buildable in a Home Screen web app on
  either iOS or Android (no browser exposes background geolocation/geofencing; the W3C
  Geofencing API draft was never implemented by any browser, confirmed still stalled as of a
  November 2025 discussion thread) — matches the issue's own hypothesis exactly. Findings
  written to `web/shanes-life/docs/location-aware-content-surfacing-findings.md`. Built the
  honest, buildable foreground half instead of stopping at "can't do it": migration
  `037_places.sql` (real `places` table + `captures.latitude`/`longitude`), `core/places.mjs`,
  `push_place`/`list_places`/`forget_place` MCP tools, `/api/places*` routes, silent geo-tag on
  capture submit + Today "You're at &lt;place&gt;" card (foreground/on-demand only, never a
  forced permission prompt) + Settings → Places read-only list with a Forget button — all
  consistent with the freshly-landed "no forms, anywhere, ever" rule (no add-place form
  anywhere; a place is only ever created via `push_place` from a geo-tagged capture). Applied
  the migration directly via `psql` against the local `finances` DB (idempotent DDL) because
  `npm run migrate`/`npm start`/`npm run check` are currently blocked by the live, already-filed
  #3175 shared-ledger collision (concurrent sibling builds' migrations in the shared ledger not
  yet in this worktree). Verified the new module functions directly against the real DB with a
  disposable-user scratch script (9/9 real checks passed, cleaned up after), and added a real
  `bin/check.mjs` block (Places section, MCP + web routes) for the next session/CI run once
  #3175 clears the boot path.
- 2026-09-08T02:24:13Z ✅ DONE — Hit the real #3175 collision myself while finalizing: the
  migration was first authored as `037_places.sql`, but a concurrent sibling build claimed
  `037_user_display_name.sql` in the shared ledger before mine landed, and `038_debt_due_dates.sql`
  also landed ahead of it. Renumbered to `040_places.sql` (real live check against the ledger,
  not a guess) — no re-run needed, the DDL was already applied under the old name and is fully
  idempotent. Merged origin/main twice more picking up sibling work (People & Patterns #3157,
  Money/debt-due-dates #3161/#3162) with clean auto-merges except two small router-file
  conflicts (pure list-concatenation, no logic changes on either side), resolved by hand.
  Verified branch is a real ancestor of origin/main (`verify-branch-merged.mjs` — OK,
  c9db0c876) and working tree is clean before writing this. Did not build native-app-level
  background geofencing itself — that's a genuine, larger product decision (native wrapper,
  platform choice, cost) explicitly left to Shane, not guessed at here; see the findings doc's
  own "Real, explicit stop point" section. No new GitHub issues filed — the one real gap
  surfaced (the #3175 collision happening live, mid-build) already has an open, accurate issue
  tracking it; this session is additional live evidence for that existing issue, not a new one.
