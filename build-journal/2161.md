# #2161 — Permission/role model re-baseline — redo of #1766

- **Status:** ⏳ IN FLIGHT 2026-09-01T19:53:25Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-01T19:53:25Z ⏳ IN FLIGHT — Redo of #1766 (killed mid-session, work lost to unbounded-wait bug). Plan: (1) re-verify SP-identity findings live via Graph, (2) redo Gap 3 provisioning fix retargeting to correct ps-execution SP, (3) fresh core:premier scan against testbed tenant 1, (4) classify failures, (5) rewrite docs/tenant-permission-model.md. Commit each real piece incrementally, NOT deferred to end.
- 2026-09-01T19:58:00Z ✅ Milestone 1 — SP identities RE-VERIFIED LIVE via Microsoft Graph against testbed tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3` (= mccawsoft2.onmicrosoft.com, confirmed via OIDC discovery = `GRAPH_TENANT_ID`). Carried-forward Gap 3 finding is CORRECT:
  - api-server `MT_APP_CLIENT_ID`=`4743b130-0379-41bf-b863-ec8de96d915a` → SP `6bae4b49-d1e5-470d-9cae-fef901b57f9a` ("MSP Platform - Multi-Tenant Consent (DEV)") → directory roles: **(none)** — does NOT hold Global Reader.
  - ps-execution container `MT_APP_CLIENT_ID`=`9ea2e409-d1b9-422a-8451-02fa0b98d1c3` (services/ps-execution/entrypoint.ps1:71, README.md:186) → SP `1c640fe8-1c23-4510-8235-9ee6938d8f8b` ("MSP Platform — Multi-Tenant Consent") → **holds Global Reader** (roleTemplateId `f2ef992c-3afb-46b9-b7cf-a126ee74c451`, granted manually by #1483).
  - SAME env var name `MT_APP_CLIENT_ID` = TWO DIFFERENT app registrations across api-server vs container. The #1130 provisioning module runs in api-server → resolves `4743b130`/SP `6bae4b49` (no GR, not the SP that needs it), while the PowerShell checks needing Global Reader run in the container as `9ea2e409`/SP `1c640fe8`. => module grants Global Reader to the WRONG SP. Gap 3 root cause reconfirmed against live directory state.
  - Sibling `dlp-role-group-provisioning.ts` (#249) shares the identical `resolveMtAppServicePrincipalId(MT_APP_CLIENT_ID)` bug (adds SP `6bae4b49` to Purview role group instead of container SP `1c640fe8`). To be FILED as a separate finding under #1096.
