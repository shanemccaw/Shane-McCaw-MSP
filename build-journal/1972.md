# #1972 — Power Platform management-app enrolment onboarding step

- **Status:** ✅ DONE 2026-09-03T02:36:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** ba8cb2327 (device-code enrolment routes/lib), 933d27c1b (poll error-handling fix), rebased onto main as d2228247d

## Log
- 2026-09-03T02:28:07Z ⏳ IN FLIGHT — implementing the device-code onboarding
  enrolment flow for Power Platform's management-app registration (per #1906/
  #1869), plus surfacing PowerPlatformNotRegisteredError as its own monitor
  status instead of a generic error.
- 2026-09-03T02:36:00Z ✅ DONE — shipped:
  - `power-platform-admin.ts`: OAuth2 device-code challenge start/poll +
    `adminApplications` registration PUT, using the well-known Microsoft
    Azure PowerShell public client id (`1950a258-227b-4e31-a9cf-717495945fc2`
    — the same one `New-PowerAppManagementApp` uses internally) rather than
    flipping our own `MT_APP_CLIENT_ID` registration to allow public-client
    flows, which would be an app-registration write outside this session's
    scope per CLAUDE.md's production-change gate.
  - `consent.ts`: three new `/public/flow/power-platform-enrollment-{start,
    poll,status}` routes, gated behind existing Graph read consent
    (`resolveConsentedTenant`), mirroring the shape of the existing
    sharepoint/write-consent/compliance-decision routes. The enrolment
    record lands in `tenants.consent.powerPlatformEnrollment` — its own key,
    independent of `graph`/`writeBack`/`sharepoint`, matching the module's
    documented rule that Power Platform state must never cross-contaminate
    Graph consent state.
  - `msp.ts`: new `TenantPowerPlatformEnrollmentRecord` type/key on
    `TenantConsentMap`; new `power_platform_not_registered` value on
    `TENANT_MONITOR_PROFILE_STATUS` (no migration — Drizzle's `text(...,
    {enum})` carries no DB CHECK constraint, purely a TS-level addition,
    confirmed by reading the column definition).
  - `monitor-executor.ts`: `PowerPlatformNotRegisteredError` now persists as
    its own `power_platform_not_registered` status instead of falling into
    the generic `error` branch — the issue's "detection" ask — so the
    portal/dashboards can point at this as an actionable onboarding gap with
    a distinct, filterable status rather than an opaque failure.
  - Where in the onboarding UI this step surfaces is explicitly punted by
    the issue itself ("a product decision for whoever owns that flow — not
    decided here"); this build ships the real backend capability
    (start/poll/status + persisted record) the UI step will call, matching
    the dispatch note's "genuine Phase-1 (API/backend) work" framing.
  - Fixed one self-found bug before finishing: `registerManagementApp`'s
    failure inside `pollPowerPlatformEnrollmentDeviceCode` was originally
    unhandled, which would have surfaced as an unhandled rejection in the
    poll route rather than a resolved `{status:"error"}` — caught and
    turned into a real result (commit 933d27c1b).
  - **Verification:** `tsc`/`vitest`/`esbuild` were all genuinely
    unreachable — the MAIN checkout's own `artifacts/api-server/node_modules`
    has broken per-package junctions (`@types`, `esbuild`, etc. resolve to
    empty real directories, not the pnpm store), confirmed by direct
    `Get-Item`/`Get-ChildItem` probes on both the worktree and the main
    checkout, and by `link-deps.mjs`'s own per-entry linker finding 0
    missing entries to repair (the entries "exist," they're just empty).
    Did **not** run `pnpm install`/`--force` per the hard rule, and did not
    touch the main checkout. Filed as its own finding, #2501 (parented under
    the Feature, #1650), since it blocks verification for anyone building in
    `artifacts/api-server` right now, not just this session. Verification
    here was careful manual review instead: re-read every changed file for
    type/control-flow correctness, confirmed the new schema symbols are
    re-exported through the existing `lib/db` barrel
    (`export * from "./schema"` → `schema/index.ts`'s `export * from
    "./msp"`), and confirmed `mergeConsentKey`'s `keyof TenantConsentMap`
    signature accepts the new `powerPlatformEnrollment` key without changes.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed
    `d2228247d` is an ancestor of `origin/main`.
