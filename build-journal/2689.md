# #2689 — Build Queue card click forces bottom log panel open — should just select/highlight; add right-click Open Build Log

- **Status:** ✅ DONE 2026-09-03T17:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 04a2b0f0a2b893d175cebe48dfe5b8ecd9bbc5f0 (merged into main as 2a1ba22f4d83c336c3490c464b7cf5e7ae57559f)

## Log
- 2026-09-03T16:59:07Z ⏳ IN FLIGHT — implementing per issue's 4-point plan: OpenLogPanel flag on TaskSelectedEventArgs, SelectNode(openLogPanel) param, gate SetBottomPanel in MainWindow handler, add right-click "Open Build Log" context menu item.
- 2026-09-03T17:15:00Z ✅ DONE — `BuildQueuePanel.xaml.cs`: added `OpenLogPanel` (default false) to `TaskSelectedEventArgs`; `SelectNode` now takes an optional `openLogPanel = false` param and passes it through; plain-click handler at the card (`card.MouseLeftButtonDown`) unchanged (still calls `SelectNode(node)` → `OpenLogPanel = false`); added a new "📋 Open Build Log" context-menu item (`BuildCardContextMenu`, now takes the `QueueGraphNode` too) whose click handler calls `SelectNode(node, openLogPanel: true)`. `MainWindow.xaml.cs`'s `BuildQueuePanel_TaskSelected` now always calls `BuildLogView.LoadQueueItem(...)` to keep content fresh, but only calls `SetBottomPanel(true, tabIndex: 0)` when `e.OpenLogPanel` is true. Did not touch the existing "📜 Tail Log" menu item / `ExternalLogWindow`. Verified: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 warnings, 0 errors. Branch merged into origin/main, confirmed via `verify-branch-merged.mjs` ("OK — merged into origin/main"). Working tree clean.
