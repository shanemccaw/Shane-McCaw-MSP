# Build Journal — Git #2325

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Issue:** Gap: no chat archive, no context-size warning per chat row (sub-issue of #2318 Feature: Chats, #1202 Epic: Build Console)

## Log
- 2026-09-02T19:31:00Z — Session started. Real audit: `desktop/ShaneBuilder`'s CHATS rail panel
  (`ChatsPanelBody`) is a static placeholder TextBlock, never rendered with real rows. The app has
  exactly ONE hardcoded chat tab (`epic-1202`) today — items 2-5 of the Chats Feature checklist
  (New Chat button/disclosure/anchor) haven't landed yet, so multi-tab chat doesn't really exist
  yet. `UpdateContextGauge()` already exists but is an honest placeholder: draft-length + 40k fixed
  overhead, explicitly noting "no real tokeniser wired yet" (§2 OQ#3). Plan: port the real DOM-based
  transcript-size estimator from `desktop/BuildConsole/Services/ChatContextMeterScript.cs` (already
  landed, proven infra) into ShaneBuilder, wire it into the shared `ClaudeWebView`, surface a real
  per-row severity badge in the (currently-stub) CHATS panel, and add a real Archive action that
  saves the current chat's identity + last-known context reading and resets to a fresh chat.
- 2026-09-02T19:58:00Z — ✅ DONE — Landed on `main` at commit `da2f53098` (rebased/re-pushed twice
  during the session as concurrent builds moved `main` — final `verify-branch-merged.mjs` run
  confirms `agent/2325-q1264` is a genuine ancestor of `origin/main`). Real audit before writing
  any code: `ChatsPanelBody` was a static `TextBlock` placeholder that never rendered a single
  row, and `UpdateContextGauge()` (Git #2209 §2) was an honest draft-only + 40k-overhead estimate,
  explicitly noting the transcript itself wasn't DOM-readable yet (OQ#3). Also found: ShaneBuilder
  creates exactly one persistent chat tab today (`epic-1202`) — no multi-tab chat-creation code
  exists yet (items #2321-#2324 build that), which shaped "archive" to mean "record + reset the
  one live chat" rather than "close one of several tabs."
  - `desktop/ShaneBuilder/Services/ChatContextMeterScript.cs` (new) — real DOM transcript-size
    scraper, ported from the proven `desktop/BuildConsole/Services/ChatContextMeterScript.cs`
    (message type renamed `SB_CHAT_STATS`).
  - `desktop/ShaneBuilder/Services/ChatContextMeterStore.cs` (new) — per-conversation high-water
    persistence, ported from BuildConsole's own store, retargeted at `%AppData%\ShaneBuilder`.
  - `desktop/ShaneBuilder/Services/ChatArchiveStore.cs` (new) — real local JSON persistence for
    archived chats (`%AppData%\ShaneBuilder\chat-archive.json`); no DB table — this is
    desktop-local session state, not product data.
  - `desktop/ShaneBuilder/MainWindow.ChatsPanel.cs` (new) — real CHATS rail-panel rows: one per
    open chat tab with a severity-colored context badge (green/amber/red off
    `Brush.Toast.Success/Warning/Error`, tiered at 60%/85% of the existing 300k
    `ContextWindowTokens` constant), Archive action, and an ARCHIVED section with Reopen/Delete.
  - `MainWindow.xaml`/`MainWindow.xaml.cs` — wires `AddScriptToExecuteOnDocumentCreatedAsync` +
    `WebMessageReceived` on the shared `ClaudeWebView`; `UpdateContextGauge()` now adds the real
    DOM-scraped transcript size on top of the draft term instead of draft-only; "Archive chat"
    added to the chat-tab right-click menu.
  - Verification: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors, both before
    and after the final rebase onto `main`. No test-manifest requirement — `desktop/BuildConsole`
    (and, by the same standing scope note, ShaneBuilder) is explicitly out of test-manifest scope.
  - No findings filed — the codebase was consistent with what the issue described (a genuine,
    confirmed gap), no new bug/gap surfaced during the build.
  - `git status --porcelain` clean at session end (only `_fixup.mjs`/`_scan_glyphs.mjs` scratch
    files were created transiently to work around Segoe MDL2 PUA glyphs in the existing
    `BuildTabPill` context-menu code that this tool's text editor couldn't reliably match/type;
    both were deleted before the commit).
