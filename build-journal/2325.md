# Build Journal — Git #2325

- **Status:** ⏳ IN FLIGHT 2026-09-02T19:31:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Issue:** Gap: no chat archive, no context-size warning per chat row (sub-issue of #2318 Feature: Chats, #1202 Epic: Build Console)

## Log
- 2026-09-02T19:31:00Z — Session started. Real audit: `desktop/ShaneBuilder`'s CHATS rail panel
  (`ChatsPanelBody`) is a static placeholder TextBlock, never rendered with real rows. The app has
  exactly ONE hardcoded chat tab (`epic-1202`) today — items 2-5 of the Chats Feature checklist
  (New Chat button/disclosure/anchor) haven't landed yet, so multi-tab chat doesn't really exist
  yet. `UpdateContextGauge()` already exists but is an honest placeholder: draft-length + 40k fixed
  overhead, explicitly noting "no real tokeniser wired yet" (§2 OQ#3). Plan: port the real DOM-based
  transcript-size estimator from `desktop/BuildConsole/Services/ChatContextMeterScript.cs` (already
  landed, proven infra) into ShaneBuilder, wire it into the shared `ClaudeWebView`, surface a real
  per-row severity badge in the (currently-stub) CHATS panel, and add a real Archive action that
  saves the current chat's identity + last-known context reading and resets to a fresh chat.
