# #4107 — Chat tabs: force-compute "[#N] - EPIC: Name X% complete" always, stop manual rename

- **Status:** ✅ DONE 2026-09-14T20:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** e0397a6c6 (rebased onto main as d5db2de86)

## Log
- 2026-09-14T19:33:00Z ⏳ IN FLIGHT — read issue #4107, claimed dispatch, starting investigation of OpenChatTab/RenameTab/associateDefaultTitle call sites per #2534/#2079 prior work.
- 2026-09-14T20:05:00Z ✅ DONE — widened OpenChatTab's forced-epic-title resolution (new shared `ResolveForcedEpicForChat`, also tries `GetEpicForIssueNumber` when `chat.EpicId` alone doesn't resolve); new forced format `[#N] - <Epic Title> X% complete` via new `LeftSidebar.GetSubIssuePercentForGithubNumber` (real live rollup off `GitBoardIssue.SubIssuePercent`, Git #2538 — `BoardEpic` itself carries no percent, contrary to the issue body's assumption); stripped the baked `[#N] ` prefix from every `associateDefaultTitle`/`LinkChatToIssueAsync` call site (`MainWindow.GitDetailTabs.cs`, `MainWindow.xaml.cs` x4, `LeftSidebar.xaml.cs` x4, `BuildQueuePostgresClient.cs` x2) so titles are stored raw and the prefix is added exactly once at display time; also found and fixed the same double-prefix bug in a call site the issue didn't enumerate — LeftSidebar's "✨ Auto-Name from Linked Issue(s)" chat-tree action baked `[#N] ` into the persisted title via `RenameChatAsync`; hid "Rename Tab..." (via the same shared resolver) for any chat that resolves to a real forced epic, left visible for standalone chats. `dotnet build` clean (0 warnings/0 errors) both before and after the Auto-Name fix. Logical verification against all 5 scenarios in the issue body (regex `TabTitleIssueNumberRegex` still matches the new `[#N] - ...` format; `GetEpicForIssueNumber`/`GetEpicByGithubNumber` resolution paths traced by hand) — no live BuildConsole UI/WebView2 session was launched to click through it interactively. Rebased onto origin/main and pushed directly (d5db2de86); `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
