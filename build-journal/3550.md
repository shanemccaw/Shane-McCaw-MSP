# Git #3550 — Delete dead `clientThreadReplyEmail` export from mailer.ts

- **Status:** ✅ DONE 2026-09-12T07:15:00Z
- **Scope:** platform

## Log
- 2026-09-10T23:05:00Z — First session start: found this issue deliberately held behind #3398,
  which was still open at the time. Stopped without making a code change, filed the blocked
  state (label + comment), confirmed the `blocked_by` edge to #3398 was already correctly wired.
  That session's bookend commit (`d4c64e5f0`) was pushed only to the `agent/3550-q2232` branch and
  never landed on `origin/main` — the worktree was later reprovisioned fresh off current
  `origin/main` and that commit is not present in this checkout (rescued to
  `rescued/3550-q2232-2026-09-11T23-48-26-029Z`, confirmed identical content, nothing lost).
- 2026-09-12T07:00:00Z — Resumed. Checked #3398: now CLOSED. Removed the stale `blocked_by` edge
  (#3550 -> #3398) and the `blocked` label, posted a resuming comment on #3550. Re-confirmed zero
  callers repo-wide for `clientThreadReplyEmail` (`grep -rn "clientThreadReplyEmail" artifacts/`
  matches only the definition at `artifacts/api-server/src/lib/mailer.ts:631`). Removed the dead
  export (was lines 631–649, including its stale admin-panel status-reports URL reference).
- 2026-09-12T07:15:00Z — ✅ DONE. Also updated the three docs that described the now-deleted
  function so they don't contradict the code: `docs/email-templates-contract-pack.md`
  (§`client-thread-reply`), `test-manifests/mailer/README.md` (row 22), and
  `test-manifests/mailer/email-template-branding-audit.json` (two descriptive strings + bumped
  `lastVerifiedAgainstCommit`). The `client-thread-reply` DB row and its no-trigger-path finding
  are unchanged — only the dead builder function itself is gone.
  - Commits: `b410543b3` (delete export + doc updates), `4d32e911d` (manifest
    `lastVerifiedAgainstCommit` bump). Both pushed directly to `origin/main`
    (`69c9164fd..4d32e911d`).
  - `node scripts/dev-server/verify-branch-merged.mjs` → OK, merged into `origin/main`.
  - `npx tsc --noEmit -p artifacts/api-server` → no errors referencing `mailer.ts`.
  - `git status --porcelain` clean except `.worktree-reprovisioned.json` — an untracked marker
    file dropped by the worktree reprovisioning tooling before this session started (not created
    by this session), left alone per the "already dirty when you arrived and isn't yours" rule.
  - No new findings filed — the only stale references found were the three docs above, which
    this same session corrected directly since they document the exact function being removed.
