# #4105 — Fix pre-existing pcc.test.ts failures (getCatalog DB-backed mock drift)

- **Status:** ✅ DONE 2026-09-14T19:12:28Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 6f5d09c8f (fix, on agent/4105-q2773), landed on main as 0be8ebf12

## Log
- 2026-09-14T19:05:00Z ⏳ IN FLIGHT — Investigating pcc.test.ts failures: #3551 moved `getCatalog` (artifacts/api-server/src/lib/pcc/taxonomy-catalog.ts) from the in-memory `DEFAULT_TESTS` array to a real DB read (`db.select().from(pccTestCatalogTable)`), but `pcc.test.ts`'s `vi.mock('@workspace/db', ...)` still returned an empty `{}` for `db`, so every `/api/pcc/*` route 500s in tests.
- 2026-09-14T19:12:28Z ✅ DONE — Updated `pcc.test.ts`'s `@workspace/db` mock to provide a chainable `db.select().from()` resolving to the same catalog rows the seed migration (`lib/db/migrations/manual/2026-09-13-pcc-test-catalog-3551.sql`) inserted into `pcc_test_catalog`. `pnpm exec vitest run src/routes/pcc.test.ts` (from `artifacts/api-server`): 19/19 passing, 0 failing — no regression in the previously-passing tests. Also ran the full `artifacts/api-server` suite for a sanity check: 4976 passed / 3 failed / 19 skipped across the whole package — the 3 remaining failures (`repo-source-nul-byte-guard.test.ts`, `routes/index.boot-smoke.test.ts` timeouts) are pre-existing, unrelated to this fix, and out of #4105's scope (which named only `pcc.test.ts`). Rebased `agent/4105-q2773` onto `origin/main` and pushed directly to `main` (0be8ebf12); `verify-branch-merged.mjs` confirms merged. Working tree clean. No findings to file — this was a well-scoped, already-diagnosed test-mock fix, not a discovery of a new gap.
