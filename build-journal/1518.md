# #1518 — Ownership / RACI: Acceptance gate — universal, symmetric, cross-boundary

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-31 ⏳ IN FLIGHT — foundation #1515 confirmed closed. Building the acceptance
  gate on `portal_ownership_assignments`: a decline outcome + responder provenance
  (migration `2026-08-31-ownership-raci-acceptance-gate-1518.sql`, run against local
  DB), an actor-must-equal-owner gate on accept/decline (currently any tenant user
  could accept on anyone's behalf), a symmetric MSP-side accept/decline/assign route
  set (`msp-ownership.ts`) so a cross-tenant-boundary MSP holder can act without a
  customer JWT, and a best-effort notification on a newly-pending cell via the
  existing `notification-center.ts` (customer_user / msp_user recipients, whichever
  side the named holder resolves to). Scope stop: wire contract only, no page —
  `Design/portal/` has no Ownership export and `artifacts/portal` has no route for it.
