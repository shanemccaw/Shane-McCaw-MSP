# #2064 — Guard: .pnpmfile.cjs / pnpm-lock.yaml pnpmfileChecksum drift silently re-dirties the shared checkout

- **Status:** ✅ DONE 2026-09-06T15:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** e1f1708f6 (b740e3eeb on origin/main after rebase)

## Log
- 2026-09-06T15:31:16Z ⏳ IN FLIGHT — writing an offline guard (`scripts/dev-server/check-pnpmfile-checksum.mjs`) that recomputes pnpm 11.13.0's `pnpmfileChecksum` from `.pnpmfile.cjs` and fails loudly if `pnpm-lock.yaml`'s committed checksum is missing/mismatched — the exact drift class that caused #2060. Wiring into `desktop/BuildConsole/deploy-shanesbuild.cmd` right after the clean-tree check.
- 2026-09-06T15:40:00Z ✅ DONE — `scripts/dev-server/check-pnpmfile-checksum.mjs` reproduces pnpm 11.13.0's checksum algorithm (sha256-base64 of sha256 of LF-normalized `.pnpmfile.cjs`), pure local file arithmetic, no install/network. Verified against real repo state (currently matches, exit 0) and against three synthetic drift fixtures in `/tmp` (missing checksum line, mismatched checksum, missing lockfile) — all correctly detected with exit 1 and an actionable message naming the exact fix. Wired into `desktop/BuildConsole/deploy-shanesbuild.cmd` right after the clean-tree check and before the `origin/main` fetch, so a drifted checksum reports as a config error via the existing notify-dialog path instead of silently re-dirtying the checkout. Documented in `scripts/dev-server/README.md`. Verified branch is ancestor of `origin/main` via `verify-branch-merged.mjs`; working tree clean.
