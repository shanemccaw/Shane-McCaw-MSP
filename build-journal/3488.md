# #3488 — Feature: Support Tickets (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T21:05:00Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** `9e6283f90` (on origin/main; `verify-branch-merged.mjs` exit 0)

## Log
- 2026-09-10T06:21:37Z ⏳ IN FLIGHT — pulled #3488 body + both comments. Body itself
  says "Issues to follow (pending granularity confirmation)" — no leaf sub-issues
  filed yet under this Feature, just a checklist of real endpoints
  (`GET/POST /msp/support/requests`, `GET /msp/support/requests/:ticketId`,
  `POST .../reply`) and two prospective client pieces (ticket list/detail, reply
  action).
- 2026-09-10T06:21:37Z 🛑 BLOCKED — this is UI work for MyArchitect
  (`desktop/MyArchitect/`), and #3454 (the Epic) states as a STANDING RULE: "#3493
  (UI Shell Redesign) must land before any other Feature's UI work." #3493 is
  itself open, `in-flight`-labeled, and unmerged (24 other Features are listed as
  blocked by it). The first BUILD comment on #3488 already carried `--blocked-by
  3493`, and a real `blocked_by` dependency edge from #3488 → #3493 already exists
  (confirmed via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3488/dependencies/blocked_by`).
  No code changed this session — stopping here per the standing rule rather than
  building UI that would need to be rebuilt once the real shell lands. Matches the
  same pattern already recorded on sibling Features #3480/#3482/#3483 this session
  wave.
- 2026-09-10T20:33:07Z ⏳ IN FLIGHT (resumed, unblocked) — checked #3493 (UI Shell
  Redesign): it is now `state: CLOSED`, labeled `complete`. Per the standing
  unblock procedure, removed the `blocked_by` edge (3488 → 3493) via
  `DELETE /issues/3488/dependencies/blocked_by/5407799446`, confirmed the list is
  now empty, swapped label `blocked` → `in-flight`. Resuming real work on this
  issue's stated scope: MSP support-ticket list/detail + reply action inside the
  MyArchitect WPF shell.
- 2026-09-10T21:05:00Z ✅ DONE — Support Tickets operator surface wired into MyArchitect.
  Commit `9e6283f90` (on origin/main). Clean `dotnet build` (0 warnings/0 errors).

  Both `Client:` checklist items shipped, against the real `msp-support.ts` endpoints —
  no fixture data:
  - **Ticket list + detail** — cross-org `GET /api/msp/support/requests` opens as a
    full-panel record workspace list (a real `configured: false` response — no Zoho
    Desk connection — renders as an honest "not connected" state, not an error or an
    empty list); selecting a row loads `GET .../requests/:ticketId` (operator-visible
    thread, private notes included) as the ticket's contextual record.
  - **Reply action** — write-through message field + two non-destructive actions
    ("Send reply to customer" / "Add internal note") calling
    `POST .../requests/:ticketId/reply` with `isPublic` true/false; a real webUrl
    "Open in Zoho Desk" action when the ticket carries one.

  **How it fits the shell (UI_RULES.md):** Support Tickets is the first group on the
  **Watch** fixed tab — "the one 'what needs me' surface" — since the tab existed in
  ShellContracts.cs's closed FixedTab list but had no group registered yet (Alerts
  #3483 / Task Queue #3490 / SLA #3487 haven't landed). This is an interpretation call
  flagged for Shane: UI_RULES.md §2 names only Alerts/TaskQueue/SLA for Watch, not
  Support Tickets explicitly — Watch was picked over Admin (reserved for "genuinely
  rare, global-scope actions," which a routine support queue isn't) or a brand-new tab
  (the closed FixedTab list forbids that outright). No new tabs/panels/icons outside
  the existing spec either way. Also `@`-palette destination.

  **New code:** `Models/SupportTicketModels.cs`, `Services/ISupportTicketsService.cs`,
  `Services/SupportTicketsService.cs` (HTTP client, same pattern as
  `BreakGlassService` — bearer token from the real session, typed error carrying the
  route's `{error}`), and the Watch-tab group + list/detail/reply wiring in
  `MainWindow.xaml.cs`. `AuthToken` wired through the shared `OnAuthSessionChanged`
  fanout alongside every other MSP-console service.

  Resolved a rebase conflict in `MainWindow.xaml.cs` against `origin/main` (concurrent
  #3486/#3489 sessions had also touched the tab-registration block) — kept both sides'
  registrations, updated the trailing comment. Rebuilt clean after resolving.
  `git status --porcelain` clean; no unrelated dirty files found.
