# #1400 — ps-execution: subprocess-per-request isolation to fix MSAL assembly conflict

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 13658fc4f (restructure), 790971b38 (bookend)

## Log
- 2026-08-27 ⏳ IN FLIGHT — restructuring services/ps-execution/entrypoint.ps1 so each request's Connect-*/cmdlet execution runs in a fresh child pwsh process instead of the long-lived listener process, per #1400 (real fix for #1389's MSAL assembly-load conflict). Designing cert handoff (re-parse parent's in-memory PEM, no extra Key Vault fetch), JSON stdin/stdout contract, concurrency cap for the 0.5 CPU/1Gi container, and child timeout/kill handling. Will benchmark real pwsh cold-start and test compliance-then-teams same-replica against ca-ps-execution-dev only.
- 2026-08-27 ✅ DONE — shipped: entrypoint.ps1 is now a thin dispatcher (no module imports, never calls Connect-*/a cmdlet itself); child-worker.ps1 does the actual per-request connect/invoke/disconnect in a fresh process, importing only the ONE module its session type needs; common.ps1 + cmdlet-catalog.ps1 shared between both. Cert PEM handed to each child over stdin (re-parsed, never a second Key Vault fetch, never written to disk). Timeout+Kill($true) process-tree kill verified locally against a deliberately hung child. Concurrency: loop was already fully serial pre-#1400, so at most one child runs at a time by construction — no semaphore added for a limit the architecture already guarantees. Every child spawn now logs its real wall-clock duration (childElapsedMs) so the true per-request cold-start number surfaces from production logs on first live use, rather than staying a guess — local Windows proxy measured ~2.2-2.9s for a bare pwsh cold start (NOT the real container number; `az containerapp exec` only offers an interactive session this harness can't script for a scripted benchmark).
  Deployed and verified LIVE against ca-ps-execution-dev only (image built via `az acr build`, Docker Desktop wasn't running locally): startup logs confirm exactly ONE Key Vault fetch each for mt-app-cert and ps-execution-bearer-token (acceptance criterion #4), listener up ~1.6s from cold start, and the endpoint correctly returns 401 without a valid bearer token. HONEST GAP: the real proof (#1400 acceptance criterion #2 — a compliance-session request immediately followed by a teams-session request against the SAME warm replica, both succeeding) was NOT run by this session — the live bearer-token secret was blocked from this session by the environment's own safety classifier (reasonably, as a live-credential fetch), and this session did not attempt to route around that. Shane (or a session with that access) needs to run one `get-connection-info` (compliance) request immediately followed by one `get-cs-online-user` (teams) request against https://ca-ps-execution-dev.proudstone-22013f89.eastus2.azurecontainerapps.io/ using the real `ps-execution-bearer-token` secret, and confirm both succeed and `childElapsedMs` in the logs gives the real per-request number.
