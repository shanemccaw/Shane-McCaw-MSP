# #4460 — Interrupted diagnostics scan is never re-run — customer left with zero completed scans

- **Status:** ✅ DONE 2026-09-17T03:40:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 22f97cccd (rebased onto main as f970392b7)

## Log
- 2026-09-17T03:05:00Z ⏳ IN FLIGHT — Implementing Shane's decision: `failInterruptedDiagnosticRuns()` (diagnostics-runner.ts) now checks each interrupted run's customer for prior completed/partial history; if none exists (first scan ever) it fires a real replacement `runDiagnostics()` scan, guarded against a restart-loop re-triggering forever within a 30-minute window. A customer with prior history is left to the existing scheduled-scan cadence.
- 2026-09-17T03:40:00Z ✅ DONE — Added `maybeAutoRescanFirstScan()` in `artifacts/api-server/src/lib/diagnostics-runner.ts`, called from `failInterruptedDiagnosticRuns()` for each newly-interrupted run: no-ops for an orphaned run (customerId null), no-ops for a customer with any prior `completed`/`partial` run (scheduled cadence already covers them), otherwise fires `runDiagnostics({ customerId, packageKey, isAssessmentTriggered: false })` fire-and-forget. Restart-loop guard: skips if the same customer already has another `failed` run with the interrupted-run error message inside the last `FIRST_SCAN_RESCAN_GUARD_MS` (30 min). Live-verified with 3 new vitest cases in `diagnostics-runner-interrupted-runs.live-db.test.ts` against real local Postgres + synthetic `zz-test-4460-*` tenants: (1) a first-scan tenant's interrupted run triggers a real new `runDiagnostics()` row, (2) a tenant with prior completed history gets no new row, (3) the restart-loop guard blocks a second auto-rescan within the window. All 7 tests in the file pass (the 4 pre-existing #4453 tests + the 3 new #4460 tests). `tsc --noEmit` clean for the touched file. Pushed to `origin/main` at f970392b7, confirmed merged via `verify-branch-merged.mjs`. No findings to file — no genuine out-of-scope gaps hit. Working tree clean.
