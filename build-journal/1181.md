# #1181 — SharePoint & OneDrive Oversharing Pack

- **Status:** ✅ DONE 2026-09-10T17:06:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** efe36edbf (prior build — already on origin/main); this session added no product code, bookend only.

## Log
- 2026-09-10T17:04:20Z ⏳ IN FLIGHT — Re-dispatched build. Investigating whether the oversharing pack still needs building before writing any code (duplicate-build check first).
- 2026-09-10T17:06:00Z ✅ DONE — No new work needed. The SharePoint & OneDrive Oversharing Pack was already fully built and landed on `origin/main` in commit `efe36edbf` (2026-08-26). Verified against the real local Postgres (`DATABASE_URL`), not just file exports:
  - `config_packs` row `sharepoint-oversharing-v1` (id 55, status=active) ✓
  - 6-step `config_pack_templates` chain: review `sharepoint:tenant-sharing-capability` → review `onedrive:overshared-files` → `action.enforce-tenant-sharing-policy` → `action.block-external-resharing` → `action.require-invited-user-signin` → `microrem.remove-sharing-link` ✓
  - 3 new `baseline_action_templates` all present, each a single `PATCH /admin/sharepoint/settings` Graph v1.0 call ✓
  - `services` row `sharepoint-oversharing-pack-v1` (id 196, price=$349.00, `type_attributes.packKey=sharepoint-oversharing-v1`) ✓
  - `remediation-catalog.ts:95` fallback-map entry ✓
  - Migration `2026-08-26-sharepoint-oversharing-pack-1181.sql` run-tracked in `simulator_migration_runs` (ran 2026-08-26 19:58) ✓
  - Commit `efe36edbf` confirmed an ancestor of `origin/main` ✓
- Documented executor gaps (per-site CSOM policy; true bulk per-item fan-out removal) needing a `sharepoint-admin` write executor are already tracked in **open #2038** ("Phase 2 write packs: SharePoint/OneDrive sharing hardening"), which itself references `action.enforce-tenant-sharing-policy`. No new finding filed — it would duplicate #2038.
- Shane To-Do (grant `SharePointTenantSettings.ReadWrite.All` on the write app) already captured via the `Shane To-Do` label + the 2026-08-27 comment on #1181. Left as-is.
- Left issue labels untouched: #1181 already carries `complete` (accurate) + `deferred` (v1.2, per Shane) + `Shane To-Do`. Did not flip to `in-flight`/back — the work is genuinely already complete, so churning the BuildConsole status dot would misrepresent it.
