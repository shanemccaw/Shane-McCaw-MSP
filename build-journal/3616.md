# #3616 — Build Queue "send to chat" airplane reports items as "landed" without any real verification — wire through DoneBookendVerifier

- **Status:** ✅ DONE 2026-09-11T02:27:05Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 7f7bda23f (on origin/main; local implementation commit 5a5ef933f rebased onto it)

## Log
- 2026-09-11T02:15:14Z ⏳ IN FLIGHT — investigating BuildRollupRow / RenderBuildSetRollup airplane "landed" logic and DoneBookendVerifier.GetSatisfiedAsync to wire real verification before reporting landed.
- 2026-09-11T02:27:05Z ✅ DONE — `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`:
  - Added `_verifyingBookendSatisfied` cache + `RefreshVerifyingBookendSatisfactionAsync` (background,
    never-blocking, fed by `DoneBookendVerifier.GetSatisfiedAsync`, kicked off every
    `RenderBuildSetRollup` call) and `SplitByBookendVerification` to split a candidate
    not-yet-sent-Verifying list into bookend-verified ("landed") vs "needs attention".
  - Per-build-set row (`BuildRollupRow`) and the epic-level aggregate header
    (`BuildEpicGroupHeader`, #3605) both: (1) gate the ✈ send button on the bookend-verified
    subset only, never the raw Verifying count; (2) render a distinct, honestly-labeled
    "⚠ N needs attention" pill (always visible when non-empty, independent of the send
    button) listing the held-back issue numbers in its tooltip; (3) re-run a final live
    `DoneBookendVerifier.GetSatisfiedAsync` check at the actual click (async click handlers)
    so a send can never act on a stale cached answer; (4) mark only the genuinely-sent,
    bookend-verified issues in `_sentVerifyingByBuildSet` — a held-back needs-attention item
    stays unsent and is offered again once it actually verifies; (5) the send-outcome status
    text honestly reports any held-back count alongside the real send result.
  - Confirmed via direct code read that `RenderBuildSetRollup`'s bucket
    if/else-if/else-continue chain (exact string match against `VerifyingStatus`) already
    cannot let a `canceled` row (a distinct literal, see
    `BuildQueuePostgresClient.IsTerminalStatus`) reach `counts.verifying` — no real gap
    found there. Documented inline with a Git #3616 comment; no behavior change was needed
    for that part of the issue.
  - Build: `dotnet build BuildConsole.csproj -c Debug` — **Build succeeded, 0 errors**, no
    new warnings introduced.
  - Scope note: this is a `desktop/BuildConsole` (WPF) change only — no test-manifest
    requirement (Test Coverage standing practice is scoped to `artifacts/msp-portal` and
    `artifacts/shane-mccaw-consulting`).
  - No fixture/hardcoded data; no schema/DB change needed for this issue.
  - Branch-merged check: `node scripts/dev-server/verify-branch-merged.mjs` → OK, merged
    into origin/main at 7f7bda23f. `git status --porcelain` → clean.
  - No genuine new finding/bug surfaced beyond the canceled-row audit above (which came back
    "no gap found," not a bug) — nothing filed under #1788.
