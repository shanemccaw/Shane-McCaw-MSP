# #3361 — Fix duplicate FK constraints to users(id)

- **Status:** ✅ DONE 2026-09-10T00:35:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** TBD (filled after push)

## Log
- 2026-09-09T23:50:00Z ⏳ IN FLIGHT — pulled issue body/comments, verifying the 7 duplicate FK pairs against live local DB, then writing a manual migration to drop the orphaned Postgres-named `_fkey` constraint from each pair (keeping the drizzle-named `_users_id_fk` one, per the schema-authoritative naming the issue calls out).
- 2026-09-10T00:35:00Z ✅ DONE — confirmed all 7 pairs live (matches issue exactly), confirmed `lib/db/src/schema/index.ts` declares each of the 7 columns via inline `.references()` (drizzle-kit names those `_users_id_fk`), wrote and ran `lib/db/migrations/manual/2026-09-10-drop-duplicate-users-fk-constraints-3361.sql` (DROP CONSTRAINT only — non-destructive per CLAUDE.md's migration gate) against local `DATABASE_URL`. Post-migration verification: 0 duplicate (table,column) pairs remain against `users`, exactly one `_users_id_fk` constraint per pair, `AFTER DELETE` RI trigger count on `users` dropped from 77 → 70 (exactly the 7 removed), and all 5 spot-checked #3099 indexes (`client_automation_runs_client_user_id_idx`, `client_callback_tokens_client_user_id_idx`, `client_documents_client_user_id_idx`, `client_documents_uploaded_by_idx`, `audit_logs_actor_user_id_idx`) are untouched.
