# #2301 — Milestone panel: ring, epics/features/issues/gate-check vitals, stalled-feature warning, per-epic rows with mini rings and weeks-to-done

- **Status:** ✅ DONE 2026-09-02T18:24:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 07386351c (Milestone panel content; rebased onto 06cc6c63c after #2302's
  concurrent Gate-panel push landed first and touched the same two files)

## Log
- 2026-09-02T17:41:00Z ⏳ IN FLIGHT — confirmed #2290 (Git Panel navigation shell) is genuinely
  DONE on origin/main (b64df2c55 is an ancestor). Read #2301's body + its one BUILD: comment.
  Studied `MainWindow.GitPanel.cs`/`Services/GitPanelService.cs` from #2290. Building the real
  Milestone detail panel (item 12 of `ShaneBuilder-FullFeature.md`): big ring, epics/features/
  issues/gate-check vitals, stalled-feature warning, per-epic rows with mini rings and
  weeks-to-done — all real `gh`-sourced GitHub state, no fixtures.
- 2026-09-02T18:24:00Z ✅ DONE — shipped in `07386351c`. `GitPanelService`: added
  closedAt/createdAt/updatedAt to the existing feature-tree GraphQL query,
  `GetGateStatusForMilestoneAsync` (milestone-scoped real GATE: pass/total), and
  `GetMilestoneSnapshotAsync` (throttled 6-wide per-epic fetch + aggregation into
  `GitPanelMilestoneSnapshot`/`GitPanelEpicSnapshot`/`GitPanelStalledFeature`).
  `MainWindow.GitPanel.cs`: `RenderGitMilestonePanel` — real WPF-drawn ring (`GitRing`, arc via
  `ArcSegment`, no image assets) driven by GitHub's own milestone open/closed issue counts, a
  vitals row (open epics / features / issues / gate-check X/Y), a stalled-feature warning banner
  (real signal: `blocked` label, or open >21d with no real forward motion — a child issue
  closing, or a childless feature's own `updatedAt`), and per-epic rows with a mini ring +
  weeks-to-done (real recent-velocity projection off `closedAt` in the trailing 28 days; null
  — "no recent closes" — when there's genuinely nothing to project from, never a fabricated
  ETA). Fetch triggers on milestone-peek open and on breadcrumb navigation back to a milestone
  crumb, cached per milestone number, request-sequence-guarded against stale navigation races.

  Verified against live GitHub state via a standalone scratch console harness (built in
  `%TEMP%`, not committed, deleted after use): milestone "v1.1 - Monitoring & Launch Control" —
  22 open epics, 283 open features, 319 open issues, gate checks 1/4 closed, 1 real stalled
  feature (#1584, blocked by gate #1269), weeks-to-done spot-checked by hand against #1202's
  own 162/424 closed with 162 closed in the last 28 days → ~6.5w (matches the tool's own
  output). `dotnet build -c Debug`: 0 errors, 0 warnings, both before and after rebasing onto
  #2302's concurrent Gate-panel commit (which touched the same two files — resolved as a clean
  interleave, both features' additions kept, no logic altered — confirmed by diffing my
  aggregation methods byte-for-byte against the pre-rebase commit).

  No genuine new finding surfaced during this build (existing `GetOpenGatesAsync`'s
  `--state open` scope for the tree's GATE cards is intentional design, not a gap). Working
  tree left clean; branch verified merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
