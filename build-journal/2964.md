# #2964 — New Site v2 — Hardcode retainer tier pricing in Work With Me / checkout (deliberate exception)

- **Status:** ⏳ IN FLIGHT 2026-09-05T23:53:00Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`; retainer payment route in `artifacts/api-server`)
- **Started:** 2026-09-05

## Log
- 2026-09-05T23:53:00Z ⏳ IN FLIGHT — Read #2964's issue body + 3 comments (in order). Confirmed target branch `production-base-2026-09-05` (NOT main), per [[new-site-v2-workflow-and-branch-split]]. This is a deliberate, explicit exception to the standing no-fixture-data rule — Shane's direction, scoped only to these four retainer prices, for this release. Final values per Shane's last comment (production DB confirmed wrong/stale — different tiers/hours entirely — local Postgres values are the intended, correct ones, no re-verification against Staging/Prod needed): Advisory $900/mo·5hr, Essentials $1,500/mo·8hr, Growth $3,000/mo·16hr, Enterprise $5,500/mo·30hr.
- Recovery note: primary working dir `C:\wt\2964-q1714` was launched completely empty/unprovisioned (no `.git`) — the known [[buildconsole-launches-into-empty-unprovisioned-worktree-2118]] failure. Recovered manually: `git worktree add` off `origin/production-base-2026-09-05`, registered with `--owner-pid` equivalent (`registerWorktree` with `$CLAUDE_PID`), deps linked via `link-deps.mjs`. `scripts/report-progress.mjs` and `build-journal/` don't exist on `production-base-2026-09-05`, so progress reporting runs from the main checkout per [[new-site-v2-workflow-and-branch-split]]; this bookend lives on `main` the same way.
- Checked `origin/production-base-2026-09-05` history for a concurrent duplicate build of #2964 — none found (only #2955/#2957/#2958/#2959/#2960/#2961/#2962 landed so far).
- Plan: replace the live `services` table lookup in `artifacts/api-server/src/routes/public-retainer-payment.ts` with the four hardcoded tier values (single source of truth for both the charge amount and any server-side display data); update `WorkWithMe.tsx` and `RetainerCheckout.tsx` display data to match exactly, so displayed price and Stripe-charged amount can never disagree; add a `logger.child({ channel: "billing" })` binding wherever new logging is warranted; verify against Stripe TEST mode before finishing.
