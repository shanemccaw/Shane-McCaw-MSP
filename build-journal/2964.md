# #2964 — New Site v2 — Hardcode retainer tier pricing in Work With Me / checkout (deliberate exception)

- **Status:** ✅ DONE 2026-09-06T00:14:00Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`; retainer payment route in `artifacts/api-server`)
- **Started:** 2026-09-05
- **Commit(s):** `bcae11e23` on `production-base-2026-09-05`

## Log
- 2026-09-05T23:53:00Z ⏳ IN FLIGHT — Read #2964's issue body + 3 comments (in order). Confirmed target branch `production-base-2026-09-05` (NOT main), per [[new-site-v2-workflow-and-branch-split]]. This is a deliberate, explicit exception to the standing no-fixture-data rule — Shane's direction, scoped only to these four retainer prices, for this release. Final values per Shane's last comment (production DB confirmed wrong/stale — different tiers/hours entirely — local Postgres values are the intended, correct ones, no re-verification against Staging/Prod needed): Advisory $900/mo·5hr, Essentials $1,500/mo·8hr, Growth $3,000/mo·16hr, Enterprise $5,500/mo·30hr.
- Recovery note: primary working dir `C:\wt\2964-q1714` was launched completely empty/unprovisioned (no `.git`) — the known [[buildconsole-launches-into-empty-unprovisioned-worktree-2118]] failure. Recovered manually: `git worktree add` off `origin/production-base-2026-09-05`, registered with `--owner-pid` equivalent (`registerWorktree` with `$CLAUDE_PID`), deps linked via `link-deps.mjs`. `scripts/report-progress.mjs` and `build-journal/` don't exist on `production-base-2026-09-05`, so progress reporting runs from the main checkout per [[new-site-v2-workflow-and-branch-split]]; this bookend lives on `main` the same way.
- Checked `origin/production-base-2026-09-05` history for a concurrent duplicate build of #2964 — none found (only #2955/#2957/#2958/#2959/#2960/#2961/#2962 landed so far).
- Plan: replace the live `services` table lookup in `artifacts/api-server/src/routes/public-retainer-payment.ts` with the four hardcoded tier values (single source of truth for both the charge amount and any server-side display data); update `WorkWithMe.tsx` and `RetainerCheckout.tsx` display data to match exactly, so displayed price and Stripe-charged amount can never disagree; add a `logger.child({ channel: "billing" })` binding wherever new logging is warranted; verify against Stripe TEST mode before finishing.
- 2026-09-06T00:14:00Z ✅ DONE — Committed `bcae11e23` on `production-base-2026-09-05` (pushed, fast-forward, no rebase needed). Changes:
  - `artifacts/api-server/src/routes/public-retainer-payment.ts`: replaced the `services`-table `resolveRetainer` query with a `HARDCODED_RETAINER_TIERS` map (Advisory 90000¢/5hr `serviceId 168`, Essentials 150000¢/8hr `115`, Growth 300000¢/16hr `116`, Enterprise 550000¢/30hr `117` — `serviceId`/`serviceName` kept only for receipt/audit-log continuity, confirmed against local Postgres), for both the payment-intent creation path AND the payment-confirmed re-resolution of the authoritative name (previously a second DB read) — one shared map, no drift possible between the two call sites. Removed now-unused `db`/`servicesTable`/`drizzle-orm`/`isServiceFree` imports.
  - `artifacts/shane-mccaw-consulting/src/pages/WorkWithMe.tsx`: removed the `useServices`/`resolvePublicServicePriceCents` live-catalog fetch for the four tiers; `TIER_META` now carries `name`/`priceCents`/`hours` directly, identical values to the API map above.
  - `RetainerCheckout.tsx` needed no changes — it has no independent price data of its own; it only renders whatever `tier` prop WorkWithMe.tsx passes it, so consistency is structural, not duplicated logic.
  - Existing `src/pages/retainers/Architect{Essentials,Growth,Enterprise}.tsx` detail pages still call `useServices` for these same slugs, but confirmed **unrouted and unimported anywhere** (`grep` of `App.tsx` and the whole `src/` tree) — dead code, not reachable by any user, so no live display/charge inconsistency; left alone as out of this issue's explicit file list.
  - Verification: `tsc --noEmit` clean on both apps for the touched files (api-server baseline ~229 pre-existing unrelated errors, all `TS6305`/`TS7006` from unbuilt `lib/integrations-anthropic-ai` + implicit-any legacy code, none touching `public-retainer-payment.ts`; marketing site 0 errors). Built api-server (`node ./build.mjs`) and ran it locally against real Stripe **TEST mode** keys (`PORT=8099`, local Postgres): `POST /api/public/retainers/payment-intent` for all four real slugs returned `amountCents` 90000/150000/300000/550000 exactly matching the hardcoded values (real Stripe subscriptions/PaymentIntents created), and an unknown slug (`architect-bogus-retainer`) was correctly refused with `not_self_serve`. `billing`-channel log lines confirmed emitted. Test server torn down after verification.
  - Working tree left clean except the pre-existing, un-ignored root `.env.local` (not mine, never staged — same as every other production-base session).
  - No new findings filed — no genuine bug/gap discovered outside this issue's explicit scope (the unrouted retainer detail pages are dead code, not a live inconsistency, and not worth a separate issue at this scale).
