# #3023 — 224 real stranded branches ahead of main, not merged

- **Status:** ✅ DONE 2026-09-06T17:32:02Z
- **Scope:** buildconsole (investigation only; dev-server tooling / branch hygiene)

## Log

- 2026-09-06T17:12:18Z — Session started. Investigating the 224 stranded-branch
  report per issue body: sample a handful, determine real fate (genuinely
  abandoned vs. landed via squash/cherry-pick so the branch itself never became
  a literal ancestor of main), report honest category breakdown, and propose
  (not execute) a safe bulk-cleanup rule if one emerges. No deletion without a
  confirmed-safe rule.
- 2026-09-06T17:32:02Z — ✅ DONE — Investigation-only, no code changed, no
  branches deleted. Ran the live sweep (228 stranded at investigation time, up
  from 224). Computed for all 228: merge-base against `origin/main`, the
  branch's own touched files, whether those files are byte-identical to
  `origin/main` today, and the issue's `build-journal/<id>.md` status on
  `main`. Real breakdown: 92 (40%) are "bookend-only" branches whose entire
  diff from their merge-base is just their own opening bookend commit — real
  work for those issues landed by committing straight to `main` per this
  repo's own git convention, bypassing the branch entirely; 13 (6%) are
  bookend-only with NO trace of the issue on `main` at all — genuinely
  vanished sessions, filed as #3026; 30 (13%) carry real code that is already
  byte-identical to `main` (confirmed landed via the dev-server auto-merge-back
  coordinator, sample-verified on #1543); 93 (41%) carry real code that
  genuinely differs from `main` today and needs individual judgment — sampled
  two sub-patterns: further-iterated-since-landing, and confirmed-superseded
  work (sample-verified on #1488, one of the 13 known-bad contract-pack builds
  CLAUDE.md already documents). Also found and filed #3025: the sweep script
  itself hardcodes `origin/main` as the comparison base for every `agent/*`
  branch, misreporting branches deliberately based on a different real base
  (e.g. `agent/2955-prodbase`, `agent/2960-prodbase-q1711` — New Site v2 work
  on `production-base-2026-09-05`) as diverged. Proposed (not executed) a
  mechanical bulk-cleanup rule covering 135/228 (59%) with zero risk: delete
  any branch that is bookend-only with the issue already resolved on `main`,
  or whose branch-owned files are byte-identical to `main`; flag (don't
  silently delete) the 13 vanished-issue branches; leave the remaining 93 to
  individual review. Full findings and the proposed rule posted as a comment
  on #3023. Findings filed: #3025, #3026 (both parented under #1789, labeled
  `bug`, board status "AI Batter Up"). `verify-branch-merged.mjs` confirms this
  branch is an ancestor of `main` after push; `git status --porcelain` clean
  (scratch working files under `.scratch/` were deleted, never committed).
