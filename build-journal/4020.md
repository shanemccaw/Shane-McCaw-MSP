# #4020 — My Architect (MSP Console): operator-side retainer-hours route

- **Status:** ✅ DONE 2026-09-14T04:48:10Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** `f75178b71` (work), IN FLIGHT bookend `485efc7cd`
- **Migration:** `lib/db/migrations/manual/2026-09-14-retainer-period-closes-4020.sql` (additive, run on local; release line appended to #1630)
- **Findings filed:** #4026

## Log
- 2026-09-14T04:36:00Z ⏳ IN FLIGHT — auditing `lib/retainer-*.ts`, `admin-retainer.ts` and the retainer ledger schema, then building the `requireMspScope` MSP-console-operator route(s) for retainer-hours log / adjust / period-close.
- 2026-09-14T04:48:10Z ✅ DONE — Added `routes/msp-retainer.ts` (`/api/msp/:mspId/...`): customers list, ledger + per-period summary, log / adjust / delete entry, close ended period, reopen (msp-admin).
  - **Schema:** log/adjust are backed by the existing `retainer_settings` and `retainer_work_log` tables. Period close had no backing anywhere, so this adds `retainer_period_closes` (frozen bucket snapshot + lock).
  - **Reuse:** math from `lib/retainer-hours.ts` and `lib/retainer-period-anchor.ts`; pure close rules in new `lib/retainer-period-close.ts`; wire shapes from `admin-retainer.ts`.
  - **Behaviour:** per-customer advisory xact lock on ledger writes; `msp_audit_logs` on every action.
  - **Verification:** api-server `tsc --noEmit` clean. New unit + live-Postgres route suites pass 23/23 (54/54 together with the adjacent retainer-hours / admin-retainer / portal-retainer suites).
  - **Out of scope:** no contract pack (that is #2616's job). #4026 filed: AdminV2 routes and the tracker byproduct hook don't honour the close lock.
