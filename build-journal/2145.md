# #2145 — Security Plan: cross-customer read guard pattern (prep for v1.2)

- **Status:** ✅ DONE 2026-09-01T18:01:24Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** 28fef3239 (merged to main)

## Log
- 2026-09-01T17:56:08Z ⏳ IN FLIGHT — building the fail-closed cross-customer read primitive for Security Plan data, mirrored on #1521's `msp-ownership.ts` guard shape and #1843's `msp-config-state-scope.ts` (no route/UI, per scope).
- 2026-09-01T18:01:24Z ✅ DONE — added `artifacts/api-server/src/lib/security-plan-cross-tenant.ts`:
  `resolveSecurityPlanCrossTenantBook(req)` and `readSecurityPlanAcrossCustomers(req)`.
  Guard shape cited directly from `routes/msp-ownership.ts` (#1521 — `requireRole`,
  `resolveMspIdStrict`/`resolveMspId`, `resolveStaffScopedCustomerIds`) and
  `lib/msp-config-state-scope.ts` (#1843, same "book" pattern with no route wired
  against it). MSP-only role set (`MSPAdmin`/`MSPOperator`/`PlatformAdmin`) —
  CustomerUser and every other customer-facing role is excluded before any DB
  read runs. Fails closed to an empty book (never all-tenants) on: no user, a
  customer-role caller, or MSP-staff with no resolvable `mspId`; per-tenant rows
  that themselves fail `resolveTenantScope` (missing mspId / blank tenantId) are
  dropped rather than widening the book. Per-tenant reads delegate to the
  existing `assembleSecurityPlan(tenantScope, HONEST_SCOPE)` — no new data source,
  no fixtures. Logs under `tenant.portal` (matching `msp-security-plan.ts`).
  No route/page wired — this is the read primitive only, exactly per #2145's
  scope; v1.2's actual cross-customer posture view is expected to call this
  directly.
  Test: `security-plan-cross-tenant.test.ts` (4 cases, registered in
  `vitest.config.ts`) — confirmed by REAL execution (not just inspection) that
  a blank/missing scope (no user, CustomerUser, Free-tier, MSP-staff with no
  resolvable mspId) returns an empty book, all 4 passing. None of those paths
  touch the DB, by design (the guard short-circuits before any query).
  `tsc --noEmit` shows zero errors in the new files (pre-existing unrelated
  errors elsewhere in the package, untouched by this build).
  Branch `agent/2145-q1109` rebased onto `origin/main` and pushed directly to
  `main` (28fef3239); `verify-branch-merged.mjs` confirms merged. Working tree
  clean (`git status --porcelain` empty).
