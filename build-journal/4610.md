# #4610 — Document the buildSet-batch + end-of-batch-web-test convention

- **Status:** ✅ DONE 2026-09-17T00:20:00Z
- **Scope:** platform (docs only)
- **Started:** 2026-09-17
- **Commit(s):** d113071eb (doc edit), 6cd6a5c6d (merged to origin/main)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — adding new section to BUILD_QUEUE_METHOD.md near §4.2/§5 codifying: same buildSet tag per batch, sentinel-fan-in blocked_by (§5.2), end-of-batch web test lives ONLY on the sentinel issue's dispatch prompt. Docs-only, no app code.
- 2026-09-17T00:20:00Z ✅ DONE — added new §5.4 "End-of-batch web test — sentinel-only (Git #4610)" to `desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md`, right after §5.3, combining §4.2 (same buildSet per batch) and §5.2 (sentinel fan-in) into the explicit rule: the end-of-batch test-run instruction goes ONLY on the sentinel issue's own `BUILD:` dispatch prompt, with a concrete worked example (4-issue batch, buildSet=AlertsCritters, sentinel #5004) and the real `gh api .../dependencies/blocked_by` wiring commands. Docs-only — no application code touched; confirmed via `git diff --stat` (1 file, +95/-0). Rebased onto and pushed to origin/main as 6cd6a5c6d; `verify-branch-merged.mjs` confirms ancestor. Working tree clean.
