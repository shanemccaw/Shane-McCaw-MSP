# #3009 — Build queue watcher fires its own redundant `gh issue list` every 10s

- **Status:** ✅ DONE 2026-09-06T16:03:48Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 3ed718447 (implementation), db1b63344 (IN FLIGHT bookend)

## Log
- 2026-09-06T16:01:30Z ⏳ IN FLIGHT — consolidating GetNextAsync's own `gh issue list`
  fetch with the Git Board's already-fetched open-issue snapshot, per issue body: wire
  `LeftSidebar.GitBoardOpenIssuesRefreshed` into `QueueWatcherService` and reuse a recent
  snapshot via the existing `liveOpenIssuesFetcher`/`presuppliedOpen` seam in
  `BuildQueuePostgresClient.GetNextAsync`, falling back to a live fetch only when stale.
- 2026-09-06T16:03:48Z ✅ DONE — `QueueWatcherService` now subscribes to
  `GitBoardOpenIssuesRefreshed` (wired in `MainWindow.xaml.cs`, same idiom as the existing
  BuildWatch/BuildQueuePanel subscriptions) and stores the forwarded open-issue snapshot with
  a timestamp. `TickAsync` now passes a `BuildLiveOpenIssuesFetcher()` delegate into
  `GetNextAsync`'s existing `liveOpenIssuesFetcher` seam: it returns the forwarded snapshot
  when under 60s old (zero-cost, no `gh` call), and falls back to a genuine live
  `GitHubIssuesService.TryGetOpenIssueNumbersAsync()` call only when the snapshot is missing
  or stale. No change to `BuildQueuePostgresClient.cs` was needed — the existing seam already
  supported this. Git #1600 fail-closed semantics unchanged: a live-fetch failure still
  surfaces as `LiveOpenIssuesResult.Failure` and still holds every blocked candidate.
  Commit: 3ed718447. Verified via `dotnet build BuildConsole.csproj -c Debug` — Build
  succeeded, 0 warnings, 0 errors. Branch verified merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
  No findings filed — this was a scoped, well-specified consolidation with no unrelated
  gaps discovered along the way.
