# #2038 — Phase 2 write packs: SharePoint/OneDrive sharing hardening — 10 uncovered checks

- **Status:** ✅ DONE 2026-09-12T16:36:17Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 6c3dfd964 (bookend IN FLIGHT), 83ddd54a6 (migration, post-push hash on main)

## Log
- 2026-09-12T16:28:27Z ⏳ IN FLIGHT — Fan-out from #1925 Phase 2. Investigated all 14 sharepoint:*/onedrive:* monitor_checks live in DB; 2 already `we_can_run` (sharepoint:tenant-sharing-capability, onedrive:overshared-files), 12 uncovered. Fetched real MS Graph docs this session (sharepointSettings resource + update, permission-delete) — `sharingCapability` does NOT gate org-wide/anonymous links directly (org-internal sharing is always allowed at the floor `disabled` value), so per-link removal is the real fix for those two; tenant-wide `sharingCapability` correctly remediates `onedrive:external-sharing-settings`; `isSitesStorageLimitAutomatic` correctly remediates `sharepoint:storage-near-limit`. About to author the migration.
- 2026-09-12T16:36:17Z ✅ DONE — Authored + ran `lib/db/migrations/manual/2026-09-12-sharepoint-onedrive-sharing-hardening-2038.sql` (idempotent, verified via re-run: 0 inserts second time) against local `DATABASE_URL`. New themed pack `sharepoint-onedrive-sharing-hardening-v1` maps 4 checks (`sharepoint:anonymous-links`, `sharepoint:orgwide-links`, `onedrive:external-sharing-settings`, `sharepoint:storage-near-limit`) to real Graph v1.0 writes — 3 new `baseline_action_templates` rows + 1 existing reused. `we_can_run` checks: 30 → 34. The other 8 uncovered checks (`sharepoint:inactive-sites`, `onedrive:active-users`, `onedrive:storage-utilization`, `onedrive:sync-errors`, `sharepoint:site-count`, `sharepoint:storage-utilization`, `sharepoint:site-label-coverage`, `onedrive:departed-user-access`) are documented in the migration header as correctly no-write (reporting-only metrics or a genuine customer/lifecycle decision) — no backend gap. Phase 3 planOnly proof run via a scratch vitest file (not committed, deleted after use) against tenant 1 (mccawsoft2, real testbed): `resolveBaselineTemplateRequest` resolved all 4 templates fully (no `{{}}` left, `missingVariables: []` given a representative payload); `buildConfigPackDryRun` materialized the pack and made a REAL live GET against the tenant for the storage-limit write, returning `isSitesStorageLimitAutomatic: false` (`alreadySatisfied: false`) — proves Graph connectivity and the correct endpoint/shape. No writes applied anywhere, ever, per the issue's own plan-only directive. Appended to #1630. Branch verified merged into main (`node scripts/dev-server/verify-branch-merged.mjs` — OK, `83ddd54a`). `git status --porcelain` clean. No new finding issue filed — the 8 uncovered checks match the exact "correctly no automation" pattern #1925 already established and documented in-line, not a new bug or gap.
