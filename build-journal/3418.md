# #3418 — Feature: Remove Messages page + Communications section from Admin Panel Command workspace (AdminPanel)

- **Status:** ✅ DONE 2026-09-10T19:05:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** d7601b1a7 (merged into origin/main; verified via `verify-branch-merged.mjs`)

## Log
- 2026-09-10T18:05:00Z ⏳ IN FLIGHT — Resuming after #3416 (the shared `workspaceNav.tsx` blocker) closed as `complete`. Rebased this worktree onto `origin/main` to pick up #3416's landed removal of `cmd-doc-generator` before touching the same file. Proceeding to delete `artifacts/admin-panel/src/pages/crm/Messages.tsx`, its backend route, and the Command workspace's `communications` nav section (`cmd-messages`, its only item), per the issue's real scope. Leaving the `/system` workspace's own `communications` section (`sys-inbox`) untouched.
- 2026-09-10T19:05:00Z ✅ DONE — Deleted `artifacts/admin-panel/src/pages/crm/Messages.tsx`; deleted the two backend routes it used (`artifacts/api-server/src/routes/admin-messages.ts` — `GET /api/admin/messages/clients` — and `artifacts/api-server/src/routes/portal-messages.ts` — `GET`/`POST /api/portal/messages` — plus `portal-messages.test.ts`) and their wiring in `artifacts/api-server/src/routes/index.ts`. Removed the Command workspace's `communications` nav section (`cmd-messages` was its only item) from `workspaceNav.tsx`, the `MessagesPage` import/`"messages"` case from `CommandWorkspace.tsx`, and the now-dead `/crm/messages` legacy redirect from `App.tsx`. Confirmed via grep no other caller in the repo hits either deleted route (only `messagesTable` itself, a separate DB table, is read directly elsewhere — `admin-overview.ts`'s dashboard widget, `admin-clients.ts` hard-delete, `support-chat.ts` insert — none of which call the deleted HTTP routes). Cleaned up stale references in `ADMIN_PORTAL_DOCS.md`, `docs/SYSTEM_DOCUMENTATION.md`, and a comment in `notification-center.ts`/its test. The `/system` workspace's own `communications` section (`sys-inbox` / Inbox) is untouched, confirmed still present. `tsc --noEmit` clean on `admin-panel`; `tsc --noEmit` on `api-server` shows only pre-existing errors already tracked at open issue #3522 (stale `lib/db/dist` shadowing live `legacy-ladder.ts` exports) — none in any file this build touched beyond that one already-tracked drift. Rebased twice more over the course of the session as `main` moved concurrently (once with a real merge conflict in `CommandWorkspace.tsx` against #3414's concurrent Overview-page removal — resolved by keeping both deletions). Verified merged via `verify-branch-merged.mjs`. Working tree clean.
