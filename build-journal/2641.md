# #2641 — Offers and SOW Acceptance (MSP Console): regenerate the contract pack (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T19:56:38Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** a92c9a66a (bookend IN FLIGHT), 678cc6461 (contract pack) — rebased and pushed to
  `main` as `01323c46f`

## Log
- 2026-09-03T19:41:09Z ⏳ IN FLIGHT — starting contract-pack extraction against `msp-sow.ts`
  (12 routes, MSP Console SOW authoring surface) per the #1642 pattern, for the Document
  step of #2568 (Feature: Offers and SOW Acceptance, MSP Console). The existing
  `docs/offers-and-sow-acceptance-contract-pack.md` covers the *customer portal* side
  (#2448/#1657) only — this pack is a new, separate file for the MSP-side authoring
  backend, matching the naming convention of the other `*-msp-console-contract-pack.md`
  files already in `docs/`.
- 2026-09-03T19:56:38Z ✅ DONE — Wrote `docs/offers-and-sow-acceptance-msp-console-contract-pack.md`
  (582 lines) covering all 12 routes in `msp-sow.ts` plus the internal `triggerMspCharge()`
  function, `generateSowDocument()`, and cross-surface edges (`sow-expiry-sweep.ts`,
  `workflow-executor.ts`'s `charge_msp_card` node, the seeded `__system__: MSP SOW Charge
  Approval` workflow, `customer-tenant-alert-engine.ts`'s `billing.sow_signed` condition).
  Confirmed live against local PostgreSQL (`\d msp_sows`, `\d sales_offers`). Zero live
  frontend callers of any of the 12 routes anywhere in the tree (expected — `artifacts/msp-console`
  doesn't exist yet). Three real findings filed as sibling sub-issues of #2568 (milestone v1.1,
  board status "AI Batter Up"):
  - **#2725** (`bug`+`security`) — `POST /api/msp/customers/:customerId/clickwrap` has no
    ownership check at all; any authenticated login can forge a customer-agreement acceptance
    record for an arbitrary `customerId`.
  - **#2730** (`bug`) — `sales_offers.customer_id`'s live FK targets `users.id`, but every real
    consumer (this route file's own accept handler, `sales-offer-engine.ts`, and the customer
    portal's own contract pack) treats the value as a `tenants.id` — confirmed live via
    `psql \d sales_offers`; not yet manifesting in data because the only 5 seed rows use
    `customer_id = 1`, which coincidentally satisfies both interpretations.
  - **#2731** (`bug`) — `msp-sow.ts`'s private `getMspIdFromRequest()` duplicates and reproduces
    the wrong half of the canonical `resolve-msp-id.ts` contract, allowing a `PlatformAdmin`
    `?mspId=` override on 5 session-scoped `/msp/...` routes that `resolveMspIdStrict()`'s own
    doc comment says must never allow that override.

  Verification: `node scripts/dev-server/verify-branch-merged.mjs` → OK, `agent/2641-q1414`
  (01323c46) merged into `origin/main`. `git status --porcelain` clean. No product code, schema,
  or UI changed — read-only pack extraction as scoped.
