# #1678 — Author one real interpretation → resolve → route example end-to-end

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** bookend-only (no product/schema/UI code changed — the pipeline works as built; it needed feeding). Deliverable is real DB rows on the local testbed + the #1678 writeup + findings #1699–#1702.

## Log
- 2026-08-29 ⏳ IN FLIGHT — Researching the interpretation authoring path (admin-m365-interpretations.ts), the resolution sweep (#1533) and routing sweep (#1534) to author ONE real interpretation against a live Message Center announcement for the mccawsoft2 testbed tenant, drive it through resolve+route, and record the real wire shapes on #1678 for Design.
- 2026-08-29 ✅ DONE — Authored the first real end-to-end interpretation. Chose MC1287370 ("Auto upgrade of shared calendars from legacy MAPI model to modern REST model"): forced/Microsoft-implemented (the MC body states it is server-side and requires no user/admin action) and honestly measurable (probe `exchange:shared-mailbox-licensing` = 2 shared mailboxes, definitionally in scope). Drove it through the REAL running api-server: created (proposed) → confirmed → resolved (measured, affectedCount 2, stored-data/read-only) → routed via `runM365ChangeRoutingSweep`. Result: routing `auto_created` (intake informed, implementer microsoft), CR-2026-183 (id 83) auto-approved, scheduled by 30 Oct 2026, on the testbed tenant only (customer 3 untouched). Captured the verbatim `WireAnalysis` and posted all shapes + timings to #1678. Key finding: the per-wave post cap (POSTS_PER_WAVE=60, ranked by Microsoft score) currently caps the analysis-bearing post OUT of the customer payload — filed #1699. Also filed #1700 (deriveWorkload mis-categorises Exchange→Identity), #1701 (routing has no on-demand trigger), #1702 (authoring throughput at scale). Answered the architectural question on #1678: interpretation authoring is universal and belongs in AdminV2, not the portal. No migrations run (nothing added to #1630). `git status` clean; branch verified against main before writing DONE.
