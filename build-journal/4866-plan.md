# #4866 — Plan: move web/shanes-life to shanemccaw/shanes-life

## Problem Description
`web/shanes-life/` lives inside `Shane-McCaw-MSP`. Shane wants it in its own repo, `shanemccaw/shanes-life`, as a plain code move. No history preservation (explicit decision). Issues stay in `Shane-McCaw-MSP`.

## Root Cause Analysis
Follow-through on the extraction #3578 deferred. No defect.

## User Review Required
- Replit deployment repoint is Shane's follow-up. Until then the live app will not redeploy from the old path.
- Other files in this repo still reference `web/shanes-life` (BuildConsole DatabaseRegistry, scripts/check-*.mjs, desktop/ShanesSurvival). Out of scope; filed as a finding.

## Proposed Changes
1. Create private empty repo `shanemccaw/shanes-life`.
2. Export the tracked contents of `web/shanes-life/` at current `origin/main` (git archive, so only tracked files) and commit them at the new repo's root in one initial commit; push to main.
3. In `Shane-McCaw-MSP`: `git rm -r web/shanes-life`, one commit, push to main.

## Verification Plan
- Byte-compare (sha256 per file) tracked files of `web/shanes-life` at the pre-removal commit against the new repo's root at main; expect zero differences.
- Confirm `web/shanes-life` is absent on `Shane-McCaw-MSP` main.
- Confirm no issues were created/moved/closed for #3086 tree by this build (only comment on #4866 and any findings filed).
