# #2811 — GET /msp/sla/summary warningTimers permanently 0

- **Status:** ⏳ IN FLIGHT 2026-09-04T08:40:32Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-04T08:40:32Z ⏳ IN FLIGHT — direction (a) picked per dispatch: wire the warning-detection write path (`sla_warning` workflow node) to actually set `sla_timers.status = 'warning'`, mirroring `fireSlaBreachRecord`'s `status = 'breached'`, and keep every `status = 'running'` reader (`fetchRunningTimers`, `computeSlaEngine`, `stopSlaTimer`, `resolveSlaTimer`) in sync so a warning timer doesn't silently drop out of active-timer polling.
