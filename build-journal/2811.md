# #2811 — GET /msp/sla/summary warningTimers permanently 0

- **Status:** ✅ DONE 2026-09-04T08:41:57Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 0c091deba

## Log
- 2026-09-04T08:40:32Z ⏳ IN FLIGHT — direction (a) picked per dispatch: wire the warning-detection write path (`sla_warning` workflow node) to actually set `sla_timers.status = 'warning'`, mirroring `fireSlaBreachRecord`'s `status = 'breached'`, and keep every `status = 'running'` reader (`fetchRunningTimers`, `computeSlaEngine`, `stopSlaTimer`, `resolveSlaTimer`) in sync so a warning timer doesn't silently drop out of active-timer polling.
- 2026-09-04T08:41:57Z ✅ DONE — `0c091deba`. Added `fireSlaWarning()` to `sla-engine.ts` (sets `status='warning'` + `warning_fired_at`, guarded to fire once and only from `'running'`); `workflow-executor.ts`'s `sla_warning` node now calls it instead of a raw UPDATE that only ever touched `warning_fired_at`. Kept every `status = 'running'` reader in sync: `fetchRunningTimers` (all 3 query variants) and `computeSlaEngine`'s evaluation filter now treat `'running'`/`'warning'` as still-active so a warning timer keeps being polled toward breach; `stopSlaTimer` and `resolveSlaTimer` now accept `'warning'` too. Added `'warning'` to the `SlaTimer.status` TS union (previously missing). `GET /msp/sla/summary`'s `status='running'`/`status='warning'`/`status='breached'` buckets are now genuinely mutually exclusive real counts, no query-side change needed (confirmed direction (a), not (b), per dispatch). Verified: `tsc --noEmit` clean; `msp-sla-scope-creep.test.ts` (26 tests) and `msp-sla-operator-tasks.live-db.test.ts` (1 test) both pass; live-verified the status transition + summary-bucket exclusivity against local Postgres in a rolled-back transaction (no persisted test data). Branch confirmed merged into origin/main via `verify-branch-merged.mjs`. Working tree clean at completion.
