# #1590 — My Architect: RET_TERMS never renders — useRetainerLive.ts:162 hardcodes NO_TERMS = []

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** none — investigation only, no code changed

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled issue body + comments, went to fix `useRetainerLive.ts:162`.
- 2026-08-30 ✅ DONE — the target file no longer exists. `artifacts/msp-portal/src/components/portal-v2/useRetainerLive.ts`
  was deleted wholesale by `f40438cdc` ("Portal scaffolding: create artifacts/portal, retire
  artifacts/msp-portal", 2026-08-29 16:27 -0400 — after this issue was filed 2026-08-29T01:21Z),
  preserved only at tag `portal-archive-2026-08-29`. `artifacts/portal` (the #1673 replacement)
  has no My Architect page, and `Design/portal/` has no design export for this module — SCOPE STOP
  forbids building one from this issue.

  The architectural finding is still correct: "how the retainer works" is static per-account
  policy copy, not per-customer DB-backed data (per #1407/#1410, cited in the issue body), so the
  real resolution was always "render real static copy or drop the dead section," never "add a
  backend table." No missing column/table/endpoint was found here — nothing to BUILD IT on.

  Checked the dependency graph before touching it: #1590 is `blocking` #1581 (contract pack) —
  #1581 is `blocked_by` #1590, not the reverse — so no new `blocked_by` edge was added from #1590
  (GitHub correctly rejected the attempt as a cycle). That existing edge is accurate and was left
  alone. Removed the one stale edge that did exist: #1590 was `blocked_by` #1589, which closed
  2026-08-30 — deleted that edge per Git #1987 (a closed blocker silently releases downstream, so
  a stale edge left in place would misreport as still-blocked).

  Labels: removed stale `in-flight`; did not add `complete` (no code shipped) or `blocked`
  (nothing open is actually gating this — it's moot, not blocked).

  Posted a comment on #1590 with the full finding and recommending Shane decide whether to close
  it as superseded once #1581/#1745/#1746 land, or fold it in now (never closed it myself). Also
  left a note on #1581 (contract pack) carrying the "static copy, not a backend field" decision
  forward so it survives the rebuild chain.

  `git status --porcelain` clean, no commits from this session (nothing to commit — target code
  doesn't exist).
