# #1876 — Build Queue card: small dot indicator for real stdout log file

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 58cd2b2fb

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a small top-right corner dot to `BuildQueueCard`
  (BuildQueuePanel.xaml.cs) that lights up when `BuildLogPaths.ForQueueItem(id)`
  exists on disk, backed by a batched/cached existence check (one directory listing
  per short TTL window, not a synchronous `File.Exists` per card per render) so it
  scales to hundreds of queue items without a render-perf regression.
- 2026-08-30 ✅ DONE — Added `BuildLogExistenceCache` (Services/) doing one
  `Directory.EnumerateFiles` listing of `BuildLogPaths.LogDirectory` per 5s TTL
  window, exposed as `HasLog(id)` (in-memory `HashSet<int>.Contains`, O(1) per
  card) instead of per-card `File.Exists`. Added `BuildLogPaths.LogDirectory` so
  both call sites share one derivation. Wired a 6x6 `TealBrush` `Ellipse`,
  top-right corner (`Margin(0,-4,-4,0)` over the card grid), into
  `BuildQueueCard` only when `HasLog(item.Id)` is true, with a tooltip naming
  the real log path — absent otherwise, deliberately quieter than the existing
  status pill. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c
  Release` — clean, 0 warnings/errors. Manual UI runthrough not possible in
  this session (no live BuildConsole instance with real log files to click
  through), so verification is the clean build plus code-reading confirmation
  of the wiring; flagged honestly rather than claimed as a live click-test.
  Commit `58cd2b2fb`, rebased onto and pushed to `origin/main`, ancestor
  check passed. No test manifest — desktop/BuildConsole is out of scope.
