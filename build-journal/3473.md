# #3473 — Retainer hours: calendar-month reset → real anniversary-based Stripe cycle

- **Status:** ⏳ IN FLIGHT 2026-09-10T05:19:28Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-10T05:19:28Z ⏳ IN FLIGHT — investigated all 4 consumers (retainer-hours.ts,
  portal-retainer.ts, retainer-work-logger.ts, admin-retainer.ts) + schema
  (`tenant_subscriptions.currentPeriodStart/End`, real timestamptz columns). Confirmed
  local `retainer_work_log`/`retainer_settings`/`tenant_subscriptions` are all empty —
  no test rows to delete. Designed anchor-day-based period keying
  (`retainer-hours.ts` periodKeyOf/periodKeyBefore/periodKeyAfter + computeMonthBucket
  now take `anchorDay`), a new `retainer-period-anchor.ts` resolving the real anchor
  from `tenant_subscriptions` with fallbacks (latest subscription of any status →
  `retainer_settings.createdAt` → day 1), and wired all 4 consumers to it. Writing
  regression tests next (mid-month-signup case), then running the suite.
