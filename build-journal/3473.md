# #3473 — Retainer hours: calendar-month reset → real anniversary-based Stripe cycle

- **Status:** ✅ DONE 2026-09-10T05:40:15Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** aa6442d77

## Log
- 2026-09-10T05:19:28Z ⏳ IN FLIGHT — investigated all 4 consumers (retainer-hours.ts,
  portal-retainer.ts, retainer-work-logger.ts, admin-retainer.ts) + schema
  (`tenant_subscriptions.currentPeriodStart/End`, real timestamptz columns). Confirmed
  local `retainer_work_log`/`retainer_settings`/`tenant_subscriptions` are all empty —
  no test rows to delete. Designed anchor-day-based period keying
  (`retainer-hours.ts` periodKeyOf/periodKeyBefore/periodKeyAfter + computeMonthBucket
  now take `anchorDay`), a new `retainer-period-anchor.ts` resolving the real anchor
  from `tenant_subscriptions` with fallbacks (latest subscription of any status →
  `retainer_settings.createdAt` → day 1), and wired all 4 consumers to it. Writing
  regression tests next (mid-month-signup case), then running the suite.
- 2026-09-10T05:40:15Z ✅ DONE — commit aa6442d77. Wired all 4 real consumers to the
  new anchor-day-based period keying: `retainer-hours.ts` (periodKeyOf/
  periodKeyBefore/periodKeyAfter + computeMonthBucket now take anchorDay),
  new `retainer-period-anchor.ts` (the priority-rule resolver + its pure
  `anchorDayFromRows` for the bulk admin route, avoiding N+1), `portal-retainer.ts`,
  `retainer-work-logger.ts`, and `admin-retainer.ts` (customers list — bulk
  tenant_subscriptions read, no per-customer round trip; detail route; unscoped POST).
  `period_month` column/wire field kept as-is (no schema change) but its value is now
  the anniversary period's own ISO "YYYY-MM-DD" start date instead of a shared
  "YYYY-MM" — documented in retainer-hours.ts; confirmed the only live frontend reader
  (admin-panel's ledger row label) treats it as an opaque display string already, and
  the new-portal "My Architect" page hasn't been rebuilt yet (#1569 is still open) so
  there is no live wire consumer to break.
  Flagged the mid-history anchor-day-shift edge case (a plan swap) per the issue's own
  instruction to flag rather than invent — zero present impact since
  retainer_work_log/retainer_settings/tenant_subscriptions are all confirmed empty
  locally (0 rows each), so no migration or data deletion was needed.
  Regression tests added/updated across all 4 consumers proving a customer signed up
  mid-month (anchor day 14) resets on the 14th, not the 1st, including work logged the
  day before the anniversary correctly landing in the PRIOR period:
  `retainer-hours.test.ts` (rewritten), `retainer-period-anchor.test.ts` (new),
  `retainer-work-logger.test.ts` (new), `admin-retainer.test.ts` (new),
  `portal-retainer.test.ts` (updated). 43 tests passing (`npx vitest run` on the 5
  files). `npx tsc --noEmit` on api-server: clean, zero errors. Full untargeted
  `npx vitest run` shows 65 pre-existing unrelated failures (missing
  DATABASE_URL/AI_INTEGRATIONS_ANTHROPIC_BASE_URL env vars for that run, one unrelated
  timeout) — zero of them retainer-related, confirmed by grep.
  Verified branch merged into origin/main (`verify-branch-merged.mjs`), working tree
  clean (`git status --porcelain` empty). No findings to file — this was a confirmed,
  decided-fix bug with no ambiguity encountered beyond the one edge case already
  flagged above (which the issue itself pre-authorized flagging rather than solving).
