# #2598 — Microsoft Changes: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T17:53:15Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `545a5bcae` (bookend IN FLIGHT), `ee0a56759` (contract pack update, merged to `main`)

## Log
- 2026-09-03T17:50:40Z ⏳ IN FLIGHT — Full line-by-line audit of
  `artifacts/api-server/src/routes/msp-message-center.ts` (57 lines, one route:
  `GET /api/msp/message-center`, MSPOperator+, scoped by `resolveMspIdStrict`). Confirmed live and
  mounted (`routes/index.ts:216,552`), backed by real `msp_message_center_items` (1157 rows, 1 msp,
  0 null `customerId`, `severity` always `"normal"` locally, `category` 715/403/39 across the three
  real enum values) queried against local `DATABASE_URL`. Extending
  `docs/microsoft-changes-contract-pack.md` with a new §1g (Surface F) per the #1642 pattern —
  file:line citations, real enum values, honest orphaned-endpoint note (no `artifacts/msp-console`
  UI calls it; confirmed #1680 was closed NOT_PLANNED in the #1571 reset, #1688 itself is the live
  Feature stub and still open). Found and verified one real defect along the way: `limit`/`offset`
  aren't NaN-guarded the way `customerId` already is two lines above, and Postgres rejects
  `LIMIT NaN` outright (`ERROR: bigint out of range`, confirmed against local `DATABASE_URL`) — a
  bad query param surfaces as a 500, not a 400. Filed **#2696**, parented as a sibling sub-issue of
  #1688 (this issue's own Feature), labeled `bug`, board status set to AI Batter Up.
- 2026-09-03T17:53:15Z ✅ DONE — `docs/microsoft-changes-contract-pack.md` updated with §1g (Surface
  F), pushed straight to `main` after rebasing onto `origin/main` (`ee0a56759`). Verified merged
  via `node scripts/dev-server/verify-branch-merged.mjs` ("OK — merged into origin/main. Safe to
  write DONE."). `git status --porcelain` clean. #2696 filed, parented under #1688, labeled `bug`,
  board status "AI Batter Up".
