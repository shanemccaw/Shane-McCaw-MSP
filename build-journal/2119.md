# #2119 — Reply/resume creates a brand-new queue row and never touches the original

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 3cad67a9c675abf448c250f9457b2ff702a4ea29

## Log
- 2026-08-31 ⏳ IN FLIGHT — Auditing QuickReplyAsync / QueueBuildAsync / resume dispatch / QueueWatcherService reap loop + schema, then fixing the orphaned-original-row bug (Epic #1788, Build Queue Panel).
- 2026-08-31 ✅ DONE — Option B. A Reply still spawns its own fresh `Reply → <title>` row (genuinely required — must never dedupe onto / re-queue out from under a possibly-running row, and it carries `resume_session_id` so the watcher runs `claude --resume`), but the ORIGINAL row is now resolved via new `MarkSupersededByReplyAsync`: status → `superseded`, linked to the replacement via new `superseded_by_id` FK. The original drops out of the active queue (superseded is in no active filter → "All" only) and its card renders "↩ REPLIED → #N" instead of sitting stuck showing stale active status forever. Guard `status NOT IN (done,failed,canceled,running)`: a running original is left to the watcher, a terminal one keeps its real outcome.
  - Files: migration `lib/db/migrations/manual/2026-08-31-build-queue-superseded-2119.sql` (additive column + FK ON DELETE SET NULL + partial index; **run against local DATABASE_URL**, recorded on #1630 for Replit/staging); Drizzle `lib/db/src/schema/index.ts` (`supersededById`); `BuildQueuePostgresClient.cs` (`SupersededStatus`, `MarkSupersededByReplyAsync`, `superseded_by_id` on `GetQueueAsync`, optional-ordinal read in `MapRow`); `QueueItem.SupersededById`; both Reply call sites wired (popover `QuickReplyAsync` + `💬 Reply…` context menu); `superseded` status pill.
  - Verification: `dotnet build BuildConsole.csproj` clean (0 warnings / 0 errors). Supersede SQL semantics confirmed against real local Postgres in a rolled-back txn — queued→superseded (link set), running/done left untouched. No test manifest (BuildConsole/WPF is out of scope for manifests per CLAUDE.md).
  - Audit / finding: **#2120** filed (parented to Epic #1202, board = AI Batter Up) — the Continue (BuildWatch ~1736, GitDetailView ~2204) and crash-recovery Resume-Session / Recover-All paths share the same disconnected-original-row shape and are NOT yet wired to the supersede mechanism; `SendToChatAsync` (#2104) audited and NOT affected (chat composer, no queue row / no resumeSessionId).
  - Branch `agent/2119-q1078` verified merged into origin/main (verify-branch-merged.mjs exit 0).
