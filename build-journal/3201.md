# #3201 — Catches (Money) has real API/MCP surface but no web UI and no check.mjs coverage

- **Status:** ✅ DONE 2026-09-08T18:05:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 06e4068a2 (Wire the Catches card + check.mjs coverage), pushed to main as f06a5fb34

## Log
- 2026-09-08T17:48:22Z ⏳ IN FLIGHT — read src/core/catches.mjs, the routes, the MCP tools,
  and the design README's Catches line. Wiring a Catches card into the Money "Now" tab
  (public/app.js), backed by the real GET /api/money/catches + POST dismiss endpoints, plus
  real end-to-end coverage in bin/check.mjs.
- 2026-09-08T18:05:00Z ✅ DONE — added `appendCatchesCard()` to `public/app.js`'s Money "Now"
  tab: renders every real undismissed catch (kind label + text) off
  `GET /api/money/catches`, with a "Got it" button wired to
  `POST /api/money/catches/:id/dismiss`, matching the existing Income Rules/Inbox
  single-click-dismiss pattern (no form, per Section 8). Added real end-to-end coverage to
  `bin/check.mjs` for both the REST endpoint and the `get_catches`/`dismiss_catch` MCP tools:
  detectors run, the deterministic first-time forgotten-money sweep is asserted (no fabricated
  dollar figure), dismiss, idempotent re-dismiss, 404 on an unknown/foreign catch id, and the
  `catch.dismiss` audit-trail row. Ran the full `bin/check.mjs` suite against the live local
  server (`http://localhost:5000`): all 11 new Catches checks passed; 217/221 checks passed
  overall. The 4 failures are pre-existing and unrelated to this work — `SL_VAULT_KEY` is not
  set in this environment's `.env`, so the Vault section's checks fail the same way before and
  after this change (confirmed: the var is genuinely absent from the checked-out `.env`, only
  present as a blank placeholder in `.env.example`). Verified `git merge-base --is-ancestor`
  clean via `verify-branch-merged.mjs` and `git status --porcelain` clean before writing DONE.
