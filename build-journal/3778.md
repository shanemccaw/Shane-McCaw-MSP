# #3778 — Thread timerId through operator-tasks SLA breach queue

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** baaff9630 (merged to main via de782fa59, baaff9630)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Add `b.timer_id AS "timerId"` to the SLA-breach half of `GET /api/msp/operator-tasks` (`msp-sla.ts`), thread `timerId` through `OperatorTask` (scope-sla-api.ts) and wire the MSP Console Scope & SLA queue's `TaskDrawer` to resolve the timer directly via `useResolveSlaTimer` when present, instead of only linking out to Admin Panel.
- 2026-09-12T00:20:00Z ✅ DONE — Shipped: `artifacts/api-server/src/routes/msp-sla.ts` now selects `b.timer_id AS "timerId"` (and `NULL AS "timerId"` for the scope-creep-violation half) in the operator-tasks query; `OperatorTask` (scope-sla-api.ts) carries `timerId: string | null`; `TaskDrawer` in `ScopeSla.tsx` now calls `useResolveSlaTimer` directly when `task.type === "sla_breach" && timerId != null`, falling back to the Admin Panel deep link only when no timerId is present. `npx tsc --noEmit` clean for msp-console; api-server tsc shows one pre-existing unrelated failure in `portal-checkout.test.ts` (not touched by this change). Branch verified merged into origin/main (`verify-branch-merged.mjs` OK, baaff9630). Working tree clean.
