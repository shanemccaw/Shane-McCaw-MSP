# #1963 — 4th Intune "never-configured/not-licensed" wire signature unmatched

- **Status:** ✅ DONE 2026-09-04T07:40:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 3f953997f

## Log
- 2026-09-04T07:25:31Z ⏳ IN FLIGHT — read #1963 body + comment, read `service-availability.ts`, about to query the real testbed snapshot (row 8, snapshot `30433ced-b0e5-4161-8270-97bf360ff931`) for the exact unmatched 401 body.
- 2026-09-04T07:40:00Z ✅ DONE — Queried the real testbed snapshot row 8 directly against local Postgres. Confirmed 39 of 41 real `permission_denied`/401 `/deviceManagement*` rows share the same `"_version": 3` + `"ErrorCode":"Forbidden"` Intune envelope #1847 already documents, proxied through backends other than `DeviceFE/StatelessDeviceFEService` (`AndroidSync`, `DeviceEnrollmentFE`, `CompanyTerm`, `CustomerData`, `Notification`, `RoleAdministration`, `WIPReport`). Added `intune-forbidden-envelope-401` to `INTUNE_WIRE_SIGNATURES`, gated on a tolerant-escape-depth regex (real evidence shows the same envelope at 1 vs 3 backslash-escape levels depending on proxy — a fixed-literal match would have reproduced this exact issue). `resolveIntuneServiceState`'s entitlement gating already applies uniformly to every signature, so no change needed there. Added a real-evidence test case; all 11 tests in `service-availability.test.ts` pass. `tsc --noEmit` clean. Verified the fix against the live DB rows directly (node script, not mocked): 39/39 of the target shape now resolve to the signature. The 2 remaining unmatched rows (`autopilotEvents`, `troubleshootingEvents`) are a genuinely different bare-`Message` envelope with no `ErrorCode` field — filed as **#2843**, parented as a sibling sub-issue of #1850 (this issue's own Feature), board status set to AI Batter Up, rather than folded into this fix. Rebased onto `origin/main` and pushed directly to `main` (3f953997f). `verify-branch-merged.mjs` confirms merged. Working tree clean.
