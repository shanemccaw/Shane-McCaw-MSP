# #3515 — URGENT: real, eligible UP NEXT items still not being claimed/launched after #3512

- **Status:** ✅ DONE 2026-09-10T14:03:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** a3a0351d4 (IN FLIGHT bookend), + this DONE bookend. No product/app code changed — investigation build.

## Log
- 2026-09-10T13:42:50Z ⏳ IN FLIGHT — Investigating live claim/launch mechanism (QueueWatcherService.TickAsync / GetNextAsync). Will query live bt_build_queue for #3471 and other UP NEXT items directly, trace the claim path, and confirm live root cause (not assumed same as #3514).
- 2026-09-10T14:03:08Z ✅ DONE — Traced the real claim/launch path (`QueueWatcherService.TickAsync` → `BuildQueuePostgresClient.GetNextAsync`) and the auto-queue path (`BatterUpQueueService.RefreshAndAutoQueueAsync` → `QueueRowAsync`) against the LIVE local `bt_build_queue` + BuildConsole ActivityLog.

  **Conclusion: the claim/launch mechanism is functioning; #3515's premise is stale (like #2013's was).**
  - **0** `queued` rows exist right now → the claim loop correctly has nothing to claim.
  - The watcher claimed/launched/completed builds continuously today (2176–2183: `Finished … Reported completion`); the 3 URGENT investigation builds (#3513/#3514/#3515) claimed+launched within seconds. Free-flow auto-queued #3487–3493 overnight + #3511–3513 today, and runs in the background with the tab closed (#3512 fix, confirmed 09:53 pass).
  - **#3471**: OPEN, `blocked`, board Verifying; its only row (2161) is `canceled` exit 0 (build_set BatterUpClearOut). Already ran, genuinely blocked (real open `blocked_by` chain rooted at #3493, still Verifying). Not eligible.
  - Of 73 open Batter Up items: 35 genuinely blocked (correctly held by #1600); 38 not-blocked but **every one already has a queue row** (26 done / 5 failed / 2 canceled / 2 running / 2 verifying / 1 superseded) — zero not-blocked items have no row. The lane is fully worked.
  - The "idle eligible work" impression = ~30 items canceled overnight ("no work landed") that free-flow's #1997 anti-loop guard (`allowRequeueTerminal=false`) never re-queues; they show as `reappearing … shown for re-queue` but never auto-launch.

  **No code change**, deliberately: the launch mechanism is correct (won't force-launch blocked work); the one real fix touches `BuildQueuePostgresClient`, the same subsystem **#3513 is editing concurrently**, and the canceled rows' root cause is the false-DONE claims #3513 is fixing — so an auto-requeue change must land after/with #3513, not solo-concurrent.

  **Finding filed: #3517** — free-flow never re-queues a supervisorily-canceled (`canceled`, exit 0) item; permanently non-auto-launchable even after its blocker clears. Parented to **#1202** (Feature #1789 is at the 100-sub-issue cap; noted on #3517). Board status: AI Batter Up. Includes a loop-safe fix design.

  Verification: live DB queries + ActivityLog trace (no manifest — BuildConsole/WPF is out of scope for test manifests per CLAUDE.md). Working tree clean; no product code touched. Issue comment posted on #3515.
