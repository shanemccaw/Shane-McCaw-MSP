# #3605 — Build Queue rollup: epic-level aggregate "send to chat" airplane

- **Status:** ✅ DONE 2026-09-11T01:44:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 90076bcc5 (rebased onto main as 7807d7687)

## Log
- 2026-09-11T01:36:14Z ⏳ IN FLIGHT — implementing a real aggregate "✈" button next to `BuildEpicGroupHeader`, summing unsent Verifying items across every member build set under that Epic group, reusing the existing `SendBuildSetVerifyingRequested` pipeline (Git #1893/#1932) and marking all sent items across every affected build set on success.
- 2026-09-11T01:44:00Z ✅ DONE — implemented per the issue body:
  - Factored the per-set unsent-Verifying computation out of `BuildRollupRow` into a shared `GetUnsentVerifying(buildSetKey, verifying)` helper (no behavior change to the existing per-set button).
  - `BuildEpicGroupHeader` now takes `Dictionary<string, List<int>> unsentByBuildSet` (computed in `RenderBuildSetRollup` by calling `GetUnsentVerifying` for every member build set under that Epic group) and renders a real "✈" button next to the header text only when the aggregate is non-empty — applied uniformly to a clean single-Epic group, "(mixed Epics)", and "No Epic" alike (no case needed a different treatment).
  - On click: snapshots the aggregate, sends one combined `"Git #{n} — landed"` message across every unsent item via the existing `SendBuildSetVerifyingRequested` event (same `SendTextToActiveClaudeChatAsync` pipeline MainWindow already wires per-set sends through), then marks every (build-set, issue) pair sent in `_sentVerifyingByBuildSet` on success — so both the aggregate button and every individual set's own airplane read "already sent" afterward with no double-offering.
  - Verified via `dotnet build BuildConsole.csproj -c Debug` — 0 errors, both before and after rebasing onto `origin/main`.
  - No live-UI manual click-through was performed this session (no BuildConsole runner instance with a real multi-build-set Epic group live at the time); the implementation directly reuses the exact real per-set send/mark-sent code path (`BuildRollupRow`'s click handler), which is already live and working — a clean build is the real verification available here. If Shane hits anything off when he next uses it live, flag it back to #3605.
  - No fixture/hardcoded data — everything comes from the real `buckets`/`_sentVerifyingByBuildSet` state already driving the per-set rows.
  - No findings filed — no bugs or gaps discovered outside this issue's own scope.
  - Working tree clean; branch confirmed an ancestor of `origin/main` via `git merge-base --is-ancestor HEAD origin/main` before writing this DONE.
