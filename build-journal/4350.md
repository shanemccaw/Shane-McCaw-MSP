# #4350 — SOW & Assessment expansion — Scope/Finding/Gap/Risk/Evidence objects + POA&M/CAB/Automation conversions

- **Status:** ✅ DONE 2026-09-16T06:16:35Z
- **Scope:** platform (api-server + lib/db) + MyArchitect client repo (shanemccaw/MyArchitect)
- **Started:** 2026-09-16
- **Commit(s):** Shane-McCaw-MSP `5d9c612a8` (backend) · MyArchitect `e9309d5` (client)

## Log
- 2026-09-16T05:54:23Z ⏳ IN FLIGHT — Blockers #4344 (shell), #4353 (Evidence), #4354 (Automation)
  all confirmed CLOSED. Building the real backend + MyArchitect client for the SOW & Assessment
  expansion. New domain objects needing real schema: Scope Item, Finding, Gap, Risk (Evidence
  REUSED from #4353's `evidence`/`evidence_links`; Automation REUSED from #4354's `automations`).
  Plan: (1) Drizzle schema + manual migration for msp-scoped scope_items/findings/gaps/risks,
  run locally against DATABASE_URL; (2) stores + CRUD routes matching msp-evidence/msp-automations
  patterns, reusing evidence_links for evidence attach; (3) conversion endpoints Gap→POA&M,
  Risk→CAB Change, Gap→Automation; (4) MyArchitect SOW & Assessment workspace page (5-zone VS Code
  layout per #4344) with 5 center panels (Scope Definition / Assessment Findings / Gap Analysis /
  Risk Register / Assessment Summary), contextual tabs, and conversion command flows. Mapping
  existing backend + client patterns before writing code.
- 2026-09-16T06:16:35Z ✅ DONE — Both halves shipped, both on their repos' origin/main.
  **Backend (Shane-McCaw-MSP `5d9c612a8`):**
  - `lib/db/src/schema/msp.ts` — 4 new msp+customer-scoped tables
    (assessment_scope_items/findings/gaps/risks) + enums. Peer links (related_*) are soft
    int refs (no FK, avoids a circular finding⇄gap⇄risk cycle); conversion links are real
    nullable FKs to msp_poams/msp_change_requests/automations. Added `scope_item` to
    EVIDENCE_LINK_TYPES (finding/gap/risk already present) so Evidence (#4353) is reused,
    not re-modelled.
  - `lib/db/migrations/manual/4350-sow-assessment-objects.sql` — additive DDL, RUN against
    local DATABASE_URL (verified: 4 tables + 12 FKs; full round-trip insert of all 4 objects
    + Gap→POA&M FK-link join + scope_item evidence link, all rolled back). Self-marked in
    simulator_migration_runs. Additive → agent-run per Database rules; only Replit/staging
    replay is outstanding (release-time, not a blocker).
  - `artifacts/api-server/src/routes/msp-assessment.ts` (+ index.ts registration) — CRUD for
    all four objects under `/api/msp/customers/:customerId/assessment/*` (msp-operator gated,
    assertCustomerAccess, real audit rows), conversions Finding→Gap, Gap→POA&M, Gap→CAB,
    Gap→Automation (auto-populated Script Library template wrapped as a #4354 automation),
    Risk→CAB, Risk→POA&M, and a real GET summary (deterministic per-pillar + overall score
    from open gap/risk severity weights, evidence count from evidence_links, recommended
    next steps from top open gaps). tsc: 0 errors.
  **Client (MyArchitect `e9309d5`, dotnet build 0 errors/0 warnings):**
  - Models/AssessmentObjects.cs, Services/IAssessmentObjectsService.cs +
    AssessmentObjectsService.cs, Controls/SowAssessmentView.xaml(.cs) — extended the read-only
    #3475 viewer with 5 new tabs (Scope Definition / Assessment Findings / Gap Analysis /
    Risk Register / Assessment Summary): editable DataGrids (enum combo columns), per-panel
    command bars (Add/Remove/Attach Evidence/Convert/Refresh), inline edit→PATCH on
    RowEditEnding, all conversion buttons, and the summary panel (score, per-pillar
    breakdown, counts, next steps, Generate Score, Export Assessment Report). No fixture
    data — every row comes from the endpoint.
  Verification: backend tsc clean + live DB round-trip (schema/FK/conversion proven); client
  dotnet build clean. No test manifest — that scope is artifacts/portal + shane-mccaw-consulting
  only (this is api-server + MyArchitect). Both branches confirmed ancestors of origin/main;
  both working trees clean.
