# #3805 — Build Sets panel: grow MainWindow's ColQueue outer width, don't squeeze the Queue's existing 300px

- **Status:** ✅ DONE 2026-09-12T19:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 09faacb4c (real fix), bb3c043f6 (post-rebase main tip after push)

## Log
- 2026-09-12T18:52:00Z ⏳ IN FLIGHT — investigated: `ColQueue` (MainWindow.xaml ~line 835) is a real fixed-width outer column (`Width="300"`); #3785's `BuildSetsColumn` toggle inside `BuildQueuePanel.xaml` only re-partitions that same fixed 300px internally. Found the existing `PinToggled` event (`_isPinned`/`TogglePin` in `BuildQueuePanel.xaml.cs`, consumed by `MainWindow.xaml.cs` ~line 1337) as the established "child control affecting its own parent's outer layout" idiom in this codebase — mirroring it with a new `BuildSetsPanelToggled` event instead of inventing a new pattern. About to implement.
- 2026-09-12T19:05:00Z ✅ DONE — added `BuildQueuePanel.BuildSetsPanelToggled` (event) + public `BuildSetsPanelOpen`/`BuildSetsPanelWidth`; `MainWindow.xaml.cs` tracks `_queuePinned`/`_queueBuildSetsOpen` and recomputes `ColQueue.Width` via `UpdateColQueueWidth()` from both flags together (300 closed, 540 open), reading `BuildSetsPanelOpen` immediately after subscribing so a restart with the panel left open restores already-wide. `ToggleQueuePanel_Click`/`ResetLayout_Click` routed through the same tracked state instead of hardcoding `DefaultQueueWidth`, so neither re-squeezes the Queue if Build Sets is still open. `MinWidth="0"`/splitter (`GridSplitter Grid.Column="4"`) unaffected — we only write `ColQueue.Width` on explicit toggle events, never continuously, so a manual splitter drag isn't fought. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors, only pre-existing warnings unrelated to this change. Did not launch BuildConsole.exe (prohibited for verification). Branch confirmed merged into origin/main via `verify-branch-merged.mjs`; `git status --porcelain` clean.
