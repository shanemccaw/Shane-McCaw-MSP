# #2941 — action.fresh-start-device is labelled Fresh Start but stores windowsDefenderScan

- **Status:** ✅ DONE 2026-09-05T01:45:00Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** ecdd2e350e43ba69af1df377c8d2c6810d32ffad (fix), 918422314 (merge to main)

## Log
- 2026-09-05T01:20:00Z ⏳ IN FLIGHT — Confirmed DB row (baseline_action_templates id 63,
  template_id action.fresh-start-device) stores endpoint
  `/deviceManagement/managedDevices/{{deviceId}}/windowsDefenderScan`, an antivirus scan,
  not a Fresh Start reset. Verified against Microsoft Learn that the real Intune "Fresh
  Start" action is `POST /deviceManagement/managedDevices/{managedDeviceId}/cleanWindowsDevice`
  with body `{"keepUserData": <bool>}`, same permission
  (`DeviceManagementManagedDevices.PrivilegedOperations.All`). No live UI call site invokes
  this template yet (only referenced in the graph-write-permissions catalogue + its test),
  so fixing the endpoint to match the label is the correct resolution — going with option 1
  from the issue body.
- 2026-09-05T01:45:00Z ✅ DONE — Wrote and ran
  `lib/db/migrations/manual/2026-09-05-fresh-start-device-real-endpoint-2941.sql` against
  local Postgres: `baseline_action_templates` row `action.fresh-start-device` now stores
  `POST /deviceManagement/managedDevices/{{deviceId}}/cleanWindowsDevice` with
  `body_template {"keepUserData": false}` (verified by direct query after running).
  Updated `graph-write-permissions.ts`'s permission-catalogue entry and its
  `graph-write-permissions.test.ts` assertion to match. `vitest run
  src/lib/graph-write-permissions.test.ts` passes (82/82, includes an unrelated concurrent
  #2937 change picked up via merge). Committed as ecdd2e350, merged origin/main twice
  (main moved under concurrent load) and pushed as 918422314. Verified HEAD is an
  ancestor of origin/main and `git status --porcelain` is clean. No new findings filed —
  this was the one real defect the issue described, and no other call site references
  `action.fresh-start-device` yet.
