# #2475 — Build Chain Map: real GitHub read layer (ChainDoc model)

- **Status:** ✅ DONE 2026-09-02T23:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 21c0d54446a5358b770129d21de246ebb71397e1

## Log
- 2026-09-02T23:05:00Z ⏳ IN FLIGHT — item #1 of #2473's structured index. Building the real GitHub read
  layer: Epic→Feature→Issue via native sub-issues (GetSubIssuesAsync, real priority order), blocked_by
  edges (GetBlockedByAsync), board Status from the project (GetIssueBoardStatusAsync on
  PVT_kwHOEiBDdc4BeoiY), DONE via DoneBookendVerifier's real §7 merge-ancestor check — reusing existing
  GitHubApiClient/DoneBookendVerifier services rather than re-implementing. Producing the ChainDoc C#
  model per BuildMap/README.md's "State & data model" section.
- 2026-09-02T23:45:00Z ✅ DONE — shipped `Services/BuildMap/ChainDoc.cs` (the C# model) and
  `Services/BuildMap/BuildChainMapService.cs` (the real read layer: Epic→Feature→Issue via
  GetSubIssuesAsync, blocked_by edges via GetBlockedByAsync classified fanin/gate/manual per
  BUILD_QUEUE_METHOD.md §5.2, board Status narrowed to the ChainDoc vocabulary, `done` authoritative via
  DoneBookendVerifier's real §7 check). Commit 21c0d54.
  Real finding mid-build, fixed same commit: `GetSubIssuesAsync` had no pagination and silently
  truncated any parent with >30 real sub-issues (GitHub's real default page size) — confirmed live
  against #1202 (30→99 real children after the fix). Filed as #2484 (parented to #2473, labeled
  `bug`, board status AI Batter Up).
  Verified against real live GitHub data (no fixtures): `dotnet build` clean; a throwaway console
  harness (scratch, not committed) ran `BuildChainMapService.BuildAsync` against real Epic #1202,
  producing 99 Features / 344 issues / 218 edges (39 fanin, 7 gate, 172 manual). Spot-checked by
  hand: #2473's own children's edges classified correctly against the real `dependencies/blocked_by`
  API responses; `done` status for #2235/#2236 traced to their real DONE bookends
  (build-journal/2235.md, build-journal/2236.md) and confirmed those commits are real ancestors of
  origin/main. `git status --porcelain` clean; branch verified merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs`.
  Honest gap: items #2-#9 of #2473's structured index (chain-rules engine, canvas/edge rendering,
  inspector, interactions, persistence, top bar, toolbar wiring) are separate sibling issues
  (#2476-#2483), not in scope here.
