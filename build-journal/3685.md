# #3685 — Test Pad checklist: format doc-path references better, make them open in BuildConsole's real markdown reader, add reveal-in-folder

- **Status:** ✅ DONE 2026-09-11T19:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 04095bc0b

## Log
- 2026-09-11T18:56:00Z ⏳ IN FLIGHT — investigated MarkdownRenderer.cs, GitDetailView.cs, IssueDetailView.xaml.cs, TestPadNoteDetailWindow.xaml.cs. Plan: (1) implicit clickable FileLink treatment for path-looking inline-code spans in the shared MarkdownRenderer, (2) checklist-row layout split (label line + indented path-badge line) for the specific "label — `path`" trailing shape, (3) reveal-in-folder via right-click context menu reusing `explorer.exe /select,"..."`, (4) wire TestPadNoteDetailWindow's OnFileClick using the same FastResolveFileInRepo-style resolution GitDetailView/IssueDetailView use.
- 2026-09-11T19:20:00Z ✅ DONE — commit `04095bc0b` on `main` (pushed via `agent/3685-q2347`, verified ancestor via `verify-branch-merged.mjs`). Real changes:
  - `Services/MarkdownRenderer.cs`: added `RenderOptions.OnRevealInFolder`; added `PathLikeRegex`/`LooksLikeFilePath` (has a directory separator + real extension — broader than the existing bare-mention `FileRegex` whitelist); added shared `CreatePathBadge` helper (same visual as the static inline-code badge, now clickable via `OnFileClick` + a "Reveal in Explorer" context-menu entry when `OnRevealInFolder` is set); the `InlineCode` token case now routes a path-looking span through `CreatePathBadge` instead of the old static Border (non-path inline code is byte-for-byte unchanged); `FileLink` hyperlinks get the same reveal context-menu entry when opted in; `CreateTaskItem` now detects the specific "`<label> — `<path>`" trailing shape (`TrailingPathBadgeRegex`) and renders label + indented path-badge on two lines — any other checklist row (no trailing backtick-path shape) keeps the exact prior single-TextBlock rendering, so `GitDetailView`/`IssueDetailView`/`FloatingChatWindow` are unaffected unless their own text has this shape.
  - `TestPad/TestPadNoteDetailWindow.xaml.cs`: wired `OnFileClick`/`OnRevealInFolder` on the `MarkdownRenderer.Render` call via a new `OpenNotePathAsync` helper — same real resolve-against-repo-root pattern (direct combine → common-subdir probe → pruned BFS by filename) already proven in `GitDetailView`/`IssueDetailView`, then either `MainWindow.OpenFileTab` (real markdown-preview tab) or `explorer.exe /select,"..."`.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 0 new warnings (6 pre-existing unrelated `CS0067` warnings in `FocusModeBar.xaml.cs`). Regex behavior spot-checked via PowerShell `[regex]` against representative inputs (`services/foo.ts` → path-like; bare `foo.ts` → not; the full "label — `path`" checklist line splits into the expected label/path groups). No live BuildConsole UI run in this session — no WebView2/interactive runner was invoked for this WPF-only, non-UI-testable-manifest change (Test Pad/BuildConsole is out of scope for `test-manifests/`, per this repo's Test Coverage section); the real coverage here is the clean build + regex/logic verification above.
  - No findings filed — no genuine out-of-scope bug was hit during this build.
  - `git status --porcelain` clean at completion.
