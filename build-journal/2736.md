# #2736 — Band 1 Start New Chat doesn't archive old chat or carry handoff pointer

- **Status:** ✅ DONE 2026-09-04T04:49:59Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** afc318be62960a048af5d263208e14a1c58d48cf (rebased onto main as 556fba298)

## Log
- 2026-09-04T04:42:23Z ⏳ IN FLIGHT — investigating TriggerHandoffAsync (MainWindow.xaml.cs) and BtnStartNewChat_Click (Controls/ChatDocumentContainer.xaml.cs), planning to wire archive+handoff-pointer logic into the new path and remove the dead original.
- 2026-09-04T04:49:59Z ✅ DONE — Ported TriggerHandoffAsync's real archive + handoff-pointer logic into a new `MainWindow.StartNewChatWithHandoffAsync(chat, oldTab, epicNumber)` (plus a small `FindTabForChatContainer` helper to resolve Band 1's own owning TabItem). `ChatDocumentContainer.BtnStartNewChat_Click` now calls it — archives the old chat via `_queueDb`/`_buildTrackerApi` `ArchiveChatAsync`, builds the new chat URL via `EpicChatUrlBuilder.BuildEpicChatUrl(..., handoffFromChatUrl)`, opens the new tab, and closes the old one — instead of the plain `OpenOrCreateEpicChat` call, falling back to that plain open only if the container's own tab genuinely can't be resolved. The old `TriggerHandoffAsync` method and its dead (headless, never-attached-to-visual-tree since #2727) `handoffBtn.Click` call site were removed — no other real caller remained, per the dispatch comment. Also removed the now-fully-dead `ChatContextMeterState.HandoffInProgress` field (a compiler CS0649 "never assigned" warning surfaced once the only assignment site was removed) and updated stale `<see cref="TriggerHandoffAsync">` doc-comment references elsewhere in MainWindow.xaml.cs to point at the new method. Verified: `dotnet build BuildConsole.csproj -c Debug` — 0 Warnings, 0 Errors. Branch `agent/2736-q1506` rebased onto `origin/main` and pushed directly to `main` (556fba298); `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean.
