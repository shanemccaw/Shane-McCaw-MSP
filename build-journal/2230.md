# #2230 — Queue-map dependency view ignores #2225 verified-DONE-bookend satisfaction

- **Status:** ✅ DONE 2026-09-06T17:13:52Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 06e51e276fc06924bbbb0c1b9763b55cf0375346

## Log
- 2026-09-06T17:12:22Z ⏳ IN FLIGHT — threading DoneBookendVerifier.GetSatisfiedAsync through BuildQueueMap/GetQueueMapAsync so QueueMapBlocker.StillBlocking excludes bookend-satisfied blockers, mirroring SelectClaimCandidatesAsync (commit 878117c89); raw GitHub open/closed kept on IsOpenOnGitHub for display.
- 2026-09-06T17:13:52Z ✅ DONE — Added `QueueMapBlocker.SatisfiedByDoneBookend` (keeps `IsOpenOnGitHub` untouched as the raw display signal, same discipline as `BatterUpRow.OpenBlockedByNumbers`), changed `StillBlocking` to `IsOpenOnGitHub != false && !SatisfiedByDoneBookend`. `GetQueueMapAsync` (BuildQueuePostgresClient.QueueMap.cs) now computes the satisfied set via `DoneBookendVerifier.GetSatisfiedAsync` across every still-open blocker in the snapshot, only when GitHub was reachable — same single-pass fail-closed shape as `SelectClaimCandidatesAsync` (commit 878117c89). `BuildQueueMapWindow.xaml.cs`'s blocker tooltip now distinguishes cleared / open / "open, verified DONE bookend" / unknown instead of a flat open-vs-cleared. Verified: `dotnet build -c Debug` succeeds with 0 warnings/errors (desktop/BuildConsole is out of test-manifest scope per CLAUDE.md). `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0). Commit 06e51e276fc06924bbbb0c1b9763b55cf0375346.
