# #2413 — Build Queue panel (_queueItems) is fixture-seeded, not real bt_build_queue data

- **Status:** ✅ DONE 2026-09-02T20:55:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** e4557da3c4e65b751c2f955e7bdd7292154a834

## Log
- 2026-09-02T20:15:00Z ⏳ IN FLIGHT — Audited `desktop/ShaneBuilder/MainWindow.xaml.cs` (`SeedSampleQueueData`,
  `_queueItems`, `QueueItem`) and `Services/QueueReadClient.cs` (`GetRecentBuildsAsync`,
  `GetLatestByGithubNumbersAsync`, `PaletteBuildQueueRow` shape). Confirmed real `bt_build_queue`
  schema/status vocabulary via direct local Postgres query. About to replace the fixture seed with
  a real read off `bt_build_queue` via the existing `QueueReadClient` (no second client).
- 2026-09-02T20:55:00Z ✅ DONE — Deleted `SeedSampleQueueData()` and its call. Added
  `LoadQueueFromDatabaseAsync()`, called fire-and-forget from the constructor after the first
  (empty) `RenderQueue()`: reads `bt_build_queue` via `QueueReadClient.GetRecentBuildsAsync(150)`,
  backfills any `blocked_by_number` outside that window via `GetLatestByGithubNumbersAsync`
  (reused both existing methods, no second client), derives the reverse "blocks" edge from the
  same loaded rows, and maps the real DB status vocabulary (`queued/running/verifying/done/failed/
  canceled/parked/external/capped/limit-paused/blocked`, confirmed via `\d bt_build_queue` + the
  real status constants in `BuildQueuePostgresClient`/`AccountCapPolicy`/
  `SessionLimitAutoRestartService`) onto the panel's own display vocabulary, overriding a queued
  row to "Blocked" only when its real blocker hasn't reached a terminal status. Fields with no real
  DB column (branch, checklist) are left null/empty rather than fabricated — QueueItem already
  handles both as optional. `git status --porcelain` clean apart from this file.
  Build: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors.
  Verification: no safe launch recipe exists for ShaneBuilder.exe (same caution as BuildConsole.exe
  — Shane's own actively-used tool, no confirmed-safe isolated-launch recipe), so verified via
  direct queries against the real local Postgres reproducing the exact SQL `QueueReadClient` runs:
  confirmed #2308 (blocked_by_number=2290, blocker status done) correctly resolves to Queued (not
  Blocked, since its blocker is terminal); confirmed #2357/#2365 (blocked_by_number=2356, blocker
  status verifying) correctly resolve to Blocked (blocker still open); confirmed 14 real
  blocked_by_number values fall outside the recent-150 window and `GetLatestByGithubNumbersAsync`'s
  own `DISTINCT ON` query resolves 12 of them (2 reference numbers that were never queued
  themselves, so those rows honestly fall back to "unknown" rather than being invented).
  Merged to main via fast-forward (`e4557da` was origin/main's exact next commit — no rebase
  needed); `verify-branch-merged.mjs` confirms OK.
  No new findings filed this session — the audit surfaced nothing else fixture/hardcoded in this
  file's Build Queue path.
