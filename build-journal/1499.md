# #1499 — Change Control: Execution record, rollback, and `crRef` writeback

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 4422ae6ac (schema), ec8cfe68d (migration), 1318aa3d9 + 738bfca5b (pure lib + tests),
  6f31142ad (store), 9784787f8 (routes + crRef writeback wiring), 687c1053c (store integration tests).
  Rebased onto `origin/main` and pushed at `687c1053c`.

## Log
- 2026-08-29 ⏳ IN FLIGHT — Building the CR execution record (`cr_executions`), rollback-as-inverse-CR, planned-vs-actual diff, `rollbackVerifiedAt`, and the authorizing `crRef` writeback. Schema + manual migration + routes + Wire* interfaces only — scope stops at the wire contract (no UI, artifacts/portal is a scaffold).
- 2026-08-29 ✅ DONE — Shipped the full execution/rollback/writeback wire contract.

  **Schema** (`lib/db/src/schema/msp.ts`): new `cr_executions` table binding a CR to its
  executor (`runbook_run` | `write_action` | `human_action`) with a SOFT `wf_run_id` link
  (`wf_runs.id`, no FK — same discipline as `executor_run_id`), `pack_key`, `implementer`,
  `outcome`, the captured `planned_plan` + `actual_outcome` + `plan_matched` + `plan_diff`,
  the `cr_ref` writeback (+`written_back_at`), the human-action attestation columns
  (`attested_by`/`attested_by_person_id`/`attested_at`/`attestation_note`), and
  `rollback_verified_at` + `rollback_outcome`. Plus `msp_change_requests.rollback_of_change_request_id`
  (self-ref, `set null`) so "a rollback is an inverse CR" is representable. `rollbackScriptSnippet`
  stays OFF the wire, as required.

  **Migration** (`lib/db/migrations/manual/2026-08-29-cr-executions-rollback-writeback-1499.sql`)
  — additive, run against local Postgres and verified with `\d cr_executions` against the live
  schema. Self-marks in `simulator_migration_runs`. Appended to #1630's checklist.

  **Pure lib** (`artifacts/api-server/src/lib/msp-change-execution.ts`): `diffPlannedVsActual`,
  `planStepsFromDryRun` / `actualStepsFromRunNodes` (keyed on the `tpl-<id>` workflow node id so a
  captured plan and the run's node outputs align), `deriveExecutionOutcomeFromRunStatus`,
  `isExecutionConfirmed` (a human action is confirmed ONLY by an attestation, never by outcome),
  `canRaiseRollback`, and `WireCrExecution` / `toWireCrExecution`.

  **Store** (`msp-change-execution-store.ts`): `recordExecution`, `settleChangeExecutions` (the
  crRef writeback sweep — CR-<id> written back only once the bound run COMPLETES; a failed run
  gets no writeback), `reconcileExecutionPlan`, `attestHumanAction`, `raiseRollbackChangeRequest`
  (creates the inverse CR + seeds its OWN #1496 approvals via `materializeApprovalsForChange`,
  honouring the #1759 policy floor), and `verifyRollback` (on `verified`, flips the ORIGINAL change
  to `rolled_back`).

  **Routes** (`artifacts/api-server/src/routes/msp-change-executions.ts`, mounted in
  `routes/index.ts`, floor `MSPOperator` + `resolveMspIdStrict`): GET `/msp/change-control/executions`,
  POST `.../executions/human-action`, `.../executions/:id/attest`, `.../executions/:id/reconcile-plan`,
  `.../change-requests/:id/rollback`, `.../executions/:id/verify-rollback`.

  **Writeback wiring**: `config-pack-orchestrator.ts` opens the execution record at run-bind time
  (capturing the approved plan); `index.ts` runs `settleChangeExecutions` in the same 60s tick as
  `settleAuthorizedChangeRequests`, so the crRef is written back on completion of the authorized action.

  **Verification**: 25 tests green (16 pure `msp-change-execution.test.ts` + 9 real-DB integration
  `msp-change-execution-store.test.ts`, create-under-`verify-1499-` marker + `afterAll` cleanup,
  confirmed 0 orphaned rows left in local Postgres). `lib/db` builds clean (`tsc -b`); a warm
  `tsc --noEmit` on `artifacts/api-server` shows 0 errors in any file this build touched. Logging on
  `workflow.change-control` (locked taxonomy). `verify-branch-merged.mjs` → merged. `git status
  --porcelain` clean.

  **No UI wired**, per the build's SCOPE STOP — `artifacts/portal` has no pages for this and
  `Design/portal/` carries no export. No test manifests: manifest scope is `artifacts/msp-portal/` /
  `artifacts/shane-mccaw-consulting/` only; this is api-server.

  **Findings filed:** #1781 (pre-existing api-server `tsc --noEmit` errors in unrelated files —
  ai-billing, ai-usage/cost tests, sow-claim-binding, admin-build-tracker; parented #1096, board
  "AI Batter Up"). Migration filename appended to #1630.
