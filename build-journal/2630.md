# #2630 — Break-glass Access: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:28:25Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** aa255aae3 (rebase of 5d9364337) on main

## Log
- 2026-09-12T03:21:24Z ⏳ IN FLIGHT — Shell (#3677) confirmed genuinely landed on
  main (`7888bf1d6`), not blocked. Design read: `Break Glass.dc.html` (screen 18) +
  README's watchlist tile (screen 1) + "shown-once danger-drawer result" and
  "armed destructive actions" sections. Real backend confirmed: `msp-break-glass.ts`
  (#2675) — cross-tenant pending list, per-customer history, detail+attempts,
  admin-override (shared with the portal's `performBreakGlassAdminOverride`), audit
  trail. Building: `break-glass-api.ts` hooks, tenant-scoped `BreakGlassPage`
  (mounts at `/tenants/:id/bg`), and a cross-tenant watchlist module for the root
  screen, reusing the shell's existing `useBreakGlass()` hook.
- 2026-09-12T03:28:25Z ✅ DONE — Shipped: `break-glass-api.ts` (5 hooks over
  `msp-break-glass.ts`'s 4 routes — customer history, detail+attempts, audit
  trail, admin-override), `BreakGlassPage.tsx` (waiting/everything/forced-resets
  tabs, detail drawer, danger drawer for the override, and a shown-once
  reset-complete result — no raw secret is ever returned by any route, so what's
  shown once is the new pending-secret reference + reissue/sent counts),
  `BreakGlassWatchlist.tsx` (root-screen cross-tenant tile, reuses the shell's
  existing `useBreakGlass()` so it can never disagree with the header pill), and
  5 new lucide icons. No fixture data — every field traces to a real column.
  Verified: `tsc --noEmit` clean, production `vite build` clean (1771 modules).
  No live/manual UI run — the operator auth gate needs a real logged-in MSP
  session that isn't available here (same limitation #3677's own DONE comment
  recorded); the backend routes were read directly and match the design's
  contract field-for-field. Rebased onto two concurrent MSP Console module
  landings (Risk Register #2582, Webhooks #2612) mid-session — merge conflicts
  in `ConsoleShell.tsx`/`icons.tsx` resolved by keeping both sides' additions,
  re-verified clean after. Merged to main: `aa255aae3` (merge-base confirmed via
  `verify-branch-merged.mjs`). Working tree clean. No findings filed — nothing
  in scope surfaced a genuine gap beyond what #1571's cap already documents.
