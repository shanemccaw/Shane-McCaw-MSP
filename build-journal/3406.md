# Build Journal — #3406

- **Status:** ✅ DONE 2026-09-11T04:20:00Z
- **Scope:** platform

## Log

- 2026-09-11T04:00:00Z — IN FLIGHT. Fixing `GET /api/msp/reports/runs` (`artifacts/api-server/src/routes/msp-reports.ts`)
  so `total` is a real `COUNT(*)` against the same `where` conditions instead of `runs.length`
  (the page size). Adding `offset` query param support and a `hasMore` flag.
- 2026-09-11T04:20:00Z — ✅ DONE — commit `e0f2ac87c` (merged to `main`). Replaced
  `res.json({ runs, total: runs.length })` with a parallel real `count(*)::int` query using the
  identical `where` conditions as the page query, added `offset` query-param support (defaulting
  to 0, matching existing `limit` clamp pattern) and a `hasMore` boolean
  (`offset + runs.length < total`). `tsc --noEmit` on `artifacts/api-server` shows no errors
  introduced by this file. `msp_report_runs` is confirmed 0 rows on local Postgres (same as the
  issue's own finding), so the fix is currently latent but correct — `count(*)` now returns the
  real total regardless of the applied `limit`/`offset`. Working tree clean, branch verified
  ancestor of `origin/main` via `git merge-base --is-ancestor`.
