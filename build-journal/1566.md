# #1566 — Security Plan: Authored prose — carried forward, versioned with the assembled content

- **Status:** ✅ DONE 2026-08-31T20:00:00Z
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 237cf7310 (deliverable, confirmed ancestor of origin/main), 9943d0828 (IN FLIGHT bookend)

## Log
- 2026-08-31 ⏳ IN FLIGHT — #1561/#1562/#1563 already shipped the assembled view, the
  RBD-shaped seal/supersession chain, and scope-not-findings filtering, leaving
  `SecurityPlanContent.prose` as an unformalized `string | null` stub (their own comment
  says so: "the prose sub-issue formalizes its structure — carried through verbatim
  here"). #1566 is that sub-issue. Building: (1) a structured `SecurityPlanProse` with
  the four sections the issue names — scope, methodology, exclusions, executive summary
  — each carrying `editedInThisVersion`; (2) the fixed authoring sequence from the issue
  body — freeze assembled state → write/revise prose against that frozen state → seal
  and sign as one version — via a one-per-(mspId,customerId) draft row
  (`msp_security_plan_drafts`) that POST /versions now consumes instead of accepting
  free-text prose inline; (3) carry-forward-by-default with per-section edit marking,
  computed against the baseline captured when the draft was created. No second
  versioning mechanism — reusing `msp_security_plan_versions` unchanged. SCOPE STOP
  unchanged: wire contract only, no artifacts/portal page.
- 2026-08-31 ✅ DONE — Shipped in `237cf7310` on origin/main (confirmed ancestor via
  `verify-branch-merged.mjs`). Delivered:
  • `SecurityPlanProse` / `SecurityPlanProseSection` / `SecurityPlanProseSectionContent`
    (`lib/db/src/schema/msp.ts`) — the four sections the issue names (`scope`,
    `methodology`, `exclusions`, `executiveSummary`), each `{ text, editedInThisVersion }`.
    `SecurityPlanContent.prose` widened from the #1561 `string | null` stub to
    `SecurityPlanProse | null` (null retained only for legacy pre-#1566 seals).
  • `msp_security_plan_drafts` (new table, one row per Security Plan being authored) —
    `frozenContent` (the assembled snapshot as of the last freeze, prose always null
    inside it), `baselineProse` (the carry-forward snapshot captured once at draft
    creation, never mutated again), `prose` (the live edit). Migration
    `lib/db/migrations/manual/2026-08-31-security-plan-prose-1566.sql`, RUN against
    local Postgres (also logged to #1630).
  • `security-plan-prose.ts` — pure `emptyProse`/`carryForwardProse`/
    `carryForwardLegacyOrProse`/`applyProseEdit`. `editedInThisVersion` is always
    computed by diffing against the fixed baseline, never against the section's own
    prior edit, so reverting text to the baseline correctly clears the flag.
  • `security-plan-draft.ts` — `freezeSecurityPlanDraft` (create seeds
    `baselineProse`/`prose` by carrying forward the plan's current version's prose;
    re-freeze only refreshes `frozenContent`, never prose), `getSecurityPlanDraft`,
    `updateSecurityPlanDraftProse`, `deleteSecurityPlanDraft`.
  • `msp-security-plan.ts` — new `POST .../draft/freeze`, `GET .../draft`,
    `PATCH .../draft/prose`. `POST .../versions` (seal) now consumes and deletes the
    frozen draft instead of assembling live/accepting inline prose+scope — 409 if no
    draft exists ("freeze first"). #1563's scope-statement gate now checks the scope
    captured at freeze time.
  • `security-plan-prose.test.ts` (9 pure tests, registered in `vitest.config.ts`) —
    carry-forward-by-default, edit-marking against baseline (not previous edit),
    revert-to-baseline clears the flag, legacy string prose carries nothing forward.
  Also fixed a pre-existing type error in `security-plan-drift.test.ts`'s fixture
  (missing `footprint.scope.statement`, required since #1564 landed concurrently)
  discovered while typechecking this module — one-line, not otherwise reported.

  Verification (observed this session, via a temporary local `@workspace/db` alias
  removed before commit — the shared `node_modules/@workspace/db` junction points at
  the MAIN checkout, not this worktree's edited `lib/db`, #2121's known trap):
  - `npx vitest run` on all three `security-plan-*.test.ts` files: 27/27 pass.
  - `npx tsc --noEmit` (temp `paths` override, deleted after, never committed): 66
    program-wide errors, ZERO in any file this session touched or in `msp.ts` —
    matches #1561/#1562/#1563's own reported baseline (#1765 etc., pre-existing).
  - LIVE-DB SMOKE (real local Postgres, testbed tenant `customerId=1`/`mspId=1`, via a
    temporary unregistered test file deleted after the run): froze -> authored
    scope+executiveSummary prose -> sealed v1 (both marked edited, methodology/
    exclusions carried empty+unedited) -> confirmed draft deleted -> froze again ->
    confirmed baseline carried v1's prose forward with `editedInThisVersion: false`
    on every section -> edited only executiveSummary -> sealed v2 (scope
    `editedInThisVersion: false` with v1's exact text; executiveSummary
    `editedInThisVersion: true`) -> confirmed v1 superseded. Deleted both smoke-sealed
    version rows and any draft row afterward — local DB confirmed back to 0 rows in
    both `msp_security_plan_versions` and `msp_security_plan_drafts`, as found.
  - `git status --porcelain` — clean.

  No findings filed — the gap closed was #1566's own scope (formalizing the prose
  stub #1561 left), not an out-of-scope discovery.
