# #2692 — Build Queue cards: segmented/bubble step progress bar

- **Status:** ✅ DONE 2026-09-03T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 53fa380d3 (bookend), c95286414 (implementation)

## Log
- 2026-09-03T00:00:00Z ⏳ IN FLIGHT — implementing the 4-point fix from the issue body: add a
  segmented step-bubble row to `BuildQueueCard` (`Controls/BuildQueuePanel.xaml.cs`), driven by
  `BuildProgressTracker.GetProgress(item.Id)` (same live data `ChatSessionPane.RefreshProgress`
  already renders in Build Watch), subscribed to the same `ProgressChanged` event for live updates,
  fails closed to today's card (no row at all) when `report == null || report.Total <= 0`.
- 2026-09-03T00:30:00Z ✅ DONE — added `BuildProgressBubbleRow(int queueItemId)` to
  `Controls/BuildQueuePanel.xaml.cs` and wired it into `BuildQueueCard` right after the title row.
  Renders `report.Total` small pill bubbles: index < `Step` (or all up to `Step` when
  `report.IsComplete`) = filled/GreenBrush, index == `Step` (in-progress) = YellowBrush active,
  remaining = neutral Surface0Brush/waiting. Row self-subscribes to
  `BuildProgressTracker.ProgressChanged` on `Loaded` (unsubscribes on `Unloaded`) so bubbles update
  live without a full card rebuild, mirroring `ChatSessionPane.RefreshProgress`'s own pattern. When
  `GetProgress(item.Id)` returns null or `Total <= 0` the row collapses entirely — verified by
  reading the existing gate logic ChatSessionPane already uses; no fake/fixture bar for a build that
  never reports. Nothing else on the card touched (no layout/redesign per the issue's explicit
  scope). Verified: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 Error(s).
  `verify-branch-merged.mjs` confirms `agent/2692-q1440` is merged into `origin/main`. Working tree
  clean (`git status --porcelain` empty) before this final commit.
