# Issue #2442 — Signup, Agreement and Invite: generate the contract pack

- **Status:** ✅ DONE 2026-09-03T03:24:00Z
- **Scope:** platform

## Log

- 2026-09-03T03:16:16Z — Session started. Verifying the five named endpoints
  (`msp/signup/start`, `msp/signup/tiers`, `platform/agreement/accept`,
  `platform/agreement/current`, `public/msp-invite/`) are still real and live, then
  extracting `docs/signup-agreement-and-invite-contract-pack.md` per the #1642 method.
- 2026-09-03T03:24:00Z — ✅ DONE — All five named endpoints confirmed live and unchanged
  in shape by #1673 (routes/index.ts:429/441/443 mount check). Also documented the
  `msp/settings/invites` write side and the customer-onboarding-link cross-surface edge
  for context. Wrote `docs/signup-agreement-and-invite-contract-pack.md` (commit
  `f6c6cd735`). Found a real data-integrity bug while extracting §5: the
  `launch-control-plus-addon` service row has `fulfillmentType =
  "msp_monthly_subscription"` even though it is an add-on, not a platform tier, so it
  leaks into `GET /api/msp/signup/tiers` alongside Free/Growth/Pro and is purchasable
  as a standalone recurring subscription via `POST /api/msp/signup/start`. Filed as
  **#2509**, parented as a direct sibling sub-issue of this issue's own Feature #1649
  (not the Epic), labeled `bug`, iteration:1, area:api-server, assigned to Shane, board
  status set to "AI Batter Up". Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`, commit `f6c6cd735`). Working tree clean.
