# #3432 — Observability pages (sys-observability, sys-alert-rules, sys-customer-alert-rules, sys-exceptions, sys-incidents, sys-pcc) error on load

- **Status:** ✅ DONE 2026-09-10T05:35:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none — investigation-only; no reproducible bug found in current `main`

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — investigating why /system/* Observability pages error on load; checking for shared cause with active drift work before fixing.
- 2026-09-10T05:35:00Z ✅ DONE — Investigated end to end, could not reproduce against current `main`:
  - Checked #2742 (Feature: Data Integrity / Schema Drift Cleanup) — all 6 sub-issues closed, none touch
    observability/exceptions/incidents/customer-alert-rules/pcc code paths. Not the shared cause Shane
    suspected.
  - Confirmed via direct DB query that every table these routes read (`msp_job_queue`, `msp_dlq_store`,
    `outbound_webhook_deliveries`, `portal_wf_runs`, `msp_event_store`, `msp_alert_rules`,
    `msp_alert_events`, `customer_tenant_alert_rules`, `customer_tenant_alert_events`,
    `exception_groups`, `exception_occurrences`, `platform_incidents`) exists.
  - Grepped the entire `.logs/dev-all/api-server.log` history — zero `statusCode: 500` ever logged for
    any of these routes across every restart recorded.
  - Ran a real, live end-to-end check via `shaneapp://runTest` against the running local dev server:
    logged in as the real admin account, then hit all 6 backing GET endpoints
    (`/api/admin/observability/service-health`, `/api/admin/observability`,
    `/api/admin/observability/event-bus`, `/api/admin/observability/alert-rules`,
    `/api/admin/observability/alert-events`, `/api/admin/customer-alert-rules`,
    `/api/admin/exceptions`, `/api/admin/incidents`, `/api/pcc/catalog`) — every one returned 200 with
    real data.
  - Ran a second `shaneapp://runTest` UI pass: logged in as PlatformAdmin through the real login form and
    navigated to all 6 pages (`/system/observability`, `/system/alert-rules`,
    `/system/customer-alert-rules`, `/system/exceptions`, `/system/incidents`, `/system/pcc`) — every page
    rendered its real content with no crash, no error boundary, no blank screen (screenshots captured).
  - Real, unrelated finding filed along the way: `pcc.ts` (`/api/pcc/*`) has **zero** auth middleware on
    any route, including mutating ones (`/run`, `/inject`, `/environment`, `/replay/config`) — filed as
    #3499 (bug + security), parented under #1095, board status set to AI Batter Up.
  - Conclusion: cannot reproduce with a fresh admin session against current `main`. Most likely
    explanation given the timing is a stale browser session — a massive RBAC migration (#2455–#2460,
    "requireRole → requireCapability", 631 route gates) landed on `main` in the hours immediately before
    this issue was filed. Commented on #3432 asking Shane to retry with a hard refresh / fresh login and,
    if it recurs, capture the actual browser console error or network response so a follow-up session has
    a concrete repro.
  - `git status --porcelain` clean; branch verified as ancestor of `main` after push.
