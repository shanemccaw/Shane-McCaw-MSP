# #2592 — Ownership / RACI: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T17:50:46Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** a0911859a (bookend IN FLIGHT), 152ca0acf (contract pack)

## Log
- 2026-09-03T17:45:14Z ⏳ IN FLIGHT — Read-only extraction of the Ownership/RACI contract pack
  (`docs/ownership-raci-contract-pack.md`) per the #1642 pattern, from `msp-ownership.ts`,
  `portal-ownership.ts`, `portal-ownership-policy.ts`, `portal-settings-ownership.ts`,
  `msp-ownership-book.ts`, `ownership-workload-membership.ts` and the schema in
  `lib/db/src/schema/msp.ts`. Full body/comment history of #2592, #1519, #2523 read for context.
  Live DB queried (customer overlay tables + policy + workload-membership all confirmed 0 rows,
  tenants = 2) before the local Postgres connection became unreachable mid-session (repeated
  `psql` timeouts after the first successful batch) — documented honestly in the pack rather than
  fabricated further.
- 2026-09-03T17:50:46Z ✅ DONE — `docs/ownership-raci-contract-pack.md` written and pushed
  (`152ca0acf`), read-only, no product code/schema/UI touched. Covers all 4 real HTTP surfaces
  (customer read+write `portal-ownership.ts`, MSP cross-customer read+write `msp-ownership.ts`)
  plus the two settings surfaces that gate them (`portal-settings-ownership.ts` — gate mode +
  per-workload RACI-membership toggle). Flagged plainly that #1686 (MSP Console operator Feature)
  is still `NOT ARCHITECTED` — surfaces C/D are real and finished but have no screen to draw yet,
  so this pack must not be read as authorizing an MSP-console page build. No new
  finding/bug discovered — the module's real open gaps (delegations' fate #1524, the
  escalation-chain limit #2527, #1686's architecture) were already on record from #1519/#2523
  before this session started; this pack cites them, does not refile them. Branch verified merged
  (`verify-branch-merged.mjs` → OK). `git status --porcelain` clean.
