# #3144 — Feature: Today tray — Round 2 rebuild with real weather (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:35:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 50a243a1c (main)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — read the issue, README's "Today v3" section, the design
  contract pack's fox-dialogue spec (Section 1), and the First Slice Prototype's real `door()`
  logic/markup for sky, weather, meta row and fox bubble. Extending `/api/today` (dates,
  shopping list, meds) next, then the client-side rebuild (weather module, header sky/night/
  weather layers, fox dialogue, Next card cute skin, Meds pill, CSS).
- 2026-09-08T01:35:00Z ✅ DONE — shipped in `50a243a1c` (main, fast-forwarded from
  `agent/3144-q1901`):
  - `public/weather.js` (new): real Open-Meteo fetch (Brevard County coords per spec), WMO
    `weather_code` -> sun/moon/cloud/rain/storm/snow mapping (fog draws as cloud, label "Fog"),
    glow colors, until-it-answers sample fallback.
  - `public/app.js`: Today tray header rebuilt -- time-of-day sky gradient (dawn/day/dusk/
    night, device-local hour), night starfield (26 stars + two sparkles + Saturn + Mars +
    shooting star) for 21:00-06:00, per-condition weather-sky layer (sun rays, clouds, rain/
    storm streaks with real distinct 14/18-streak timings, snow), meta row (date + location +
    weather light w/ icon+glow), fox row (breathing critter + contextual bubble). Fox dialogue
    is opener ("Morning/Afternoon/Evening, Shane.") + a real matching line for whatever's
    actually in the Next card (appointment today / dinner window / open groceries / "nothing
    needs you"), contract pack Section 1's verbatim lines. Next card rebuilt as the design's
    "one card, chosen by rule": blob pebble (real border-radius values), rotated -5° sticker,
    999px pill buttons; falls back to the pre-existing generic due-entity list for anything not
    covered by the four new cases (no regression). Meds pill added from real `getMedsToday()`
    batches (skipped when nothing is tracked -- no invented rows). Capture bar restyled to the
    spec's 999px pill everywhere it renders.
  - `src/routes/api.mjs`: `/api/today` extended with `appointmentToday` (dates.mjs, kind
    appointment/vet due today), `groceries` (real shopping-list open-item count), `meds`
    (`getMedsToday`) -- all real, already-built data; no new tables/migrations needed.
  - Holiday/season theming (flags, particles, roof/yard decorations) deliberately NOT built --
    explicitly out of this issue's scope per its own body, a separate sibling Feature.
  - Also explicitly out of scope and NOT built this session (would have been materially larger
    than this issue's real scope): the "Later, by moment" balloons tray row, and the Rooms
    "house" grid nav (the app's nav is still the existing flat tab bar). Both are real,
    design-specced gaps, filed as their own findings rather than silently dropped:
    **#3164** (Later/balloons) and **#3165** (Rooms house grid), both parented under #3086 and
    set to AI Batter Up. Location-tracking (needed for the Walmart/NASA/Rental Next-card
    variants and the meta row's "from location" qualifier) was already tracked at **#3159**
    before this session -- not duplicated; the meta row honestly shows only "Home" (no invented
    "from location" claim) until that lands.
  - **Verification**: `node --check` on every changed/new JS file. Real schema cross-check
    against the local `finances` Postgres (dates/medications/lists/list_items column shapes
    match what the code reads). Booted the real server locally (`PORT=5099`, disposable) and
    confirmed `/api/today` round-trips correctly (401, not a crash) with the new code loaded;
    the pre-existing live local instance on port 5000 was left running and untouched throughout
    (confirmed via `netstat` before and after). Copied `.env` in for that boot check and removed
    it again afterward (gitignored, never committed). No authenticated in-browser render check
    was possible in this non-interactive session -- no passkey/WebAuthn assertion available --
    so the actual pixel-level rendering of the sky/night/weather layers, fox bubble and Next
    card was NOT visually confirmed in a live browser. Noting that honestly rather than
    claiming a check that didn't happen.
  - `git status --porcelain` clean before this bookend; branch verified merged into
    `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0) before writing
    DONE.
