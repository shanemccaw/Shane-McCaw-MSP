# #4393 — Reusable self-service script: reset dev database to clean-slate state

- **Status:** ✅ DONE 2026-09-16T16:51:57Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 063dcc389 (bookend IN FLIGHT), 0943f19a6 (script + closure-tool exports + README), 1f0876062 (auth-path verification fix)

## Log
- 2026-09-16T16:33:56Z ⏳ IN FLIGHT — Building `scripts/db/reset-dev-database.mjs`, the reusable
  self-service version of #4272's proven manual reset. Read build-journal/4271.md, 4272.md,
  4313.md directly. Design: live-derive the target-MSP row via `msps.is_direct_business = true`
  (no hardcoded msp id), scope `tenants`/`users` via `msp_id`/`tenant_id` (no hardcoded vitest
  fixture ids needed — other MSPs' rows are excluded automatically since they don't match the
  live target-msp scope), reuse #4313's `find-tenant-scoped-tables.mjs` (adding named exports,
  not changing its behavior) to derive/confirm the full FK-reachable table set live rather than
  hardcoding #4271's table list, then compute a real per-table scope via a fixpoint over ALL real
  FK edges into resolved-scope parents (not just the shortest BFS edge #4313 reports), execute
  deletes in reverse-resolution (children-before-parents) order inside dry-run (BEGIN...ROLLBACK)
  then real transaction. Preserve the 26 MSP-authored-config tables from #4271 §4 and the 11
  genuinely-unscoped `wf_*`/execution-history tables from #4271 §6 that carry no FK path to
  tenants/users/msps (full-wipe, matching #4272's proven TRUNCATE decision for those).
- 2026-09-16T16:51:57Z ✅ DONE — `scripts/db/reset-dev-database.mjs` written and live-verified
  twice against the real local dev DB (`shanemccawmsp`).

  **What it does (see the script's own header + `scripts/db/README.md` for full detail):**
  live-derives the target MSP via `msps.is_direct_business = true` (no hardcoded id); reuses
  #4313's `find-tenant-scoped-tables.mjs` (added named exports, no behavior change to its own
  CLI -- verified with `--json` before/after) to discover the reset table population live from
  real `information_schema` FK edges (223 tables found at run time, vs #4271's one-time 206/194
  snapshot -- confirms the live-derivation requirement was real, not cosmetic: the population
  had already drifted); computes each table's scope via a fixpoint over ALL real FK edges into
  an already-resolved parent (not just the shortest edge #4313 reports for display), so a table
  with more than one path into scope is correctly covered; executes in reverse-resolution
  (children-before-parents) order inside `BEGIN...ROLLBACK` first, then the real transaction.
  Preserves the 26 MSP-config tables and full-wipes 9 of the original 14 orphan-risk tables (the
  other 3 turned out to be real FK-reachable via `users` and are handled correctly by the normal
  scoped path instead of a blanket wipe -- see finding below). `msp_refresh_tokens`,
  `sla_breaches`, `sla_compliance_records`, `sla_timers` needed a manual
  `UNCONSTRAINED_FK_SHAPED_EDGES` injection (their `user_id`/`msp_id`/`customer_id` columns
  carry no enforced FK constraint, so pure FK discovery can't see them).

  **Live verification -- two real runs against the real local dev DB:**
  1. First `--yes` run: backup taken + verified (`pg_restore --list` exit 0, 4330 TOC entries,
     `C:\Source\ShaneMcCawConsulting\db-backups\shanemccawmsp_pre-dev-reset_2026-09-16T164145374Z.dump`).
     31 tables changed; `tenants` 5->4 (only msp-1's own "McCawSoft" tenant deleted, all 4 vitest
     tenants untouched with zero hardcoded vitest ids anywhere in the script); `users` 9->8 (the
     one msp-1 customer user deleted); MSP-staff logins preserved: 6; tenants remaining for
     msp #1: 0. `POST /api/auth/login` (deliberately wrong password) -> 401, not 500 -- platform
     boots and the auth path runs cleanly against the reset DB, matching #4272's own real
     verification method exactly (swapped in for a nonexistent `/api/health` route the first
     run incorrectly hit, fixed in commit 1f0876062).
  2. Second `--yes` run (idempotency check, ~5 min later): only 2 tables showed a net change,
     both increases (`msp_job_queue` 0->2, `msp_user_roles` 12->14) -- confirmed these are the
     live dev server's own real concurrent background activity (not from this script, whose
     statements are all `DELETE`, which cannot increase a row count). A live query moments
     later confirmed `tenants` for msp #1 back to 0 and staff count showing 9 (6 original + 3
     pre-existing `tenant_id IS NULL` pending-purchase fixture rows from build #4392's own test
     scenario, created 2026-09-16T12:46Z, hours before this session -- correctly preserved by
     the same `tenant_id IS NULL` rule #4271/#4272 already established, not a new gap).

  **Findings filed (sub-issues of #4270 per this build's standing rules, label `bug`, board
  "AI Batter Up"):**
  - #4399 -- `msp_refresh_tokens.user_id` has no enforced FK constraint to `users(id)` despite
    being shaped exactly like one; same gap confirmed for `sla_breaches`/
    `sla_compliance_records`/`sla_timers`'s `msp_id`/`customer_id` columns (added via a
    follow-up comment on the same issue, same root cause). Real consequence: any tool relying
    on pure `information_schema` FK discovery (including #4313's own closure tool) cannot see
    these columns at all. Worked around in this script with `UNCONSTRAINED_FK_SHAPED_EDGES`;
    real fix is a manual migration adding the constraints, left for #4399.

  No fixture/hardcoded data added. `scripts/db/README.md` documents usage, the two exception
  sets, and the #4399 workaround. Two real backups taken during live verification, both outside
  the repo in `C:\Source\ShaneMcCawConsulting\db-backups\`. Working tree clean; branch verified
  merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs` exit 0) before
  writing this DONE.
