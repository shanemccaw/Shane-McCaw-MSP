# #2664 — MSP console: PATCH /msp/change-requests/:id never checks cr_approvals before allowing a status transition

- **Status:** ✅ DONE 2026-09-03T16:30:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 4c5105a36 (`PATCH /msp/change-requests/:id` approval-ledger gate)

## Log
- 2026-09-03T16:05:00Z ⏳ IN FLIGHT — gate `PATCH /api/msp/change-requests/:id` transitions into scheduled/in_progress/completed against the real `cr_approvals` ledger, mirroring `requiredStages`/`summarizeApprovals` already used by the customer-portal approve flow.
- 2026-09-03T16:30:00Z ✅ DONE — added the gate in `artifacts/api-server/src/routes/msp-changes.ts`: a genuine transition into `scheduled`/`in_progress`/`completed` now loads the CR's `cr_approvals` rows, resolves the tenant's approval policy the same way `portal-change-control.ts` does, and rejects with 409 CONFLICT unless `summarizeApprovals(...).complete` is true. `rejected`/`rolled_back` are intentionally left ungated (rejection never needed approval; rollback only ever follows an already-gated `completed`). Verification: schema for `msp_change_requests`/`cr_approvals` confirmed byte-for-byte against local Postgres (`psql \d`); the reused `requiredStages`/`summarizeApprovals` pure functions already carry their own unit tests in `portal-change-approvals.test.ts`. Could NOT run `tsc --noEmit` or `vitest` in-session — the shared pnpm store has empty package extractions for `zod`/`drizzle-orm`/`vitest`/etc. in the MAIN checkout itself (confirmed: `.pnpm/zod@3.25.76/node_modules/zod` and `.pnpm/vitest@4.1.9.../node_modules/vitest` are both empty dirs), which also means the local dev api-server isn't reachable to curl. This is a pre-existing, already-heavily-tracked repo-wide environment issue (already filed: #2515, #2516, #2522, #2525, #2526, #2528, #2556, #2503, #2508, and more) — not filing a duplicate. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; `git status --porcelain` clean.
