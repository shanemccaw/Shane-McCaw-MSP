# #4512 — CA criticals test real policy state, Security Defaults, P1 license gap

- **Status:** ✅ DONE 2026-09-17T14:32:11Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 1f18ef450
- **Migration:** `lib/db/migrations/manual/2026-09-17-ca-checks-real-state-4512.sql` (run locally; appended to #1630)
- **Findings filed (under #1489):** #4534, #4535, #4536

## Log
- 2026-09-17T14:08:44Z ⏳ IN FLIGHT — Rework ca-mfa-coverage / ca-legacy-auth-block / ca-policy-count to evaluate enabled state, grant controls and conditions; suppress under Security Defaults; license gap when no Entra ID P1.
- 2026-09-17T14:32:11Z ✅ DONE — 1f18ef450. The three checks now use `countWhere` predicates over the real policy objects (enabled + all users + all apps + mfa/authenticationStrength; enabled + all users + exchangeActiveSync/other + block; enabled and report-only counted separately). Signal rules 2780-2782 moved to the new count keys. Security Defaults suppresses all three via the #4503 gate. New `monitor_checks.required_service_plans` column: the executor reads live `/subscribedSkus` service plans and raises the existing LicenseGapError path when neither AAD_PREMIUM nor AAD_PREMIUM_P2 is provisioned. Fixed `license-gate.test.ts` mocks, which were 5/5 failing on HEAD. Verification: monitor-executor + license-gate + 9 related test files pass (265 + 14 + 138 tests), api-server `tsc --noEmit` clean. Live, read-only on testbed tenant 2080 (`src/scripts/verify-ca-checks-4512.ts`): as stored → all three `ok`, severity none, `_gateSkipped` with `isEnabled: true`; gate removed → all three `license_gap` "Requires Microsoft Entra ID P1 or P2", `hasAADP1orP2: false`; pre-#4512 definition → three criticals (defect reproduced). Not live-verifiable on this tenant: the report-only-policy path (0 CA policies, no P1, CA writes out of scope), covered by unit tests with real Graph policy shapes. Side effect of verification: `drift_collection_status` row 650 had its `updated_at` refreshed (0 events), which is what #4536 is about. Working tree clean.
