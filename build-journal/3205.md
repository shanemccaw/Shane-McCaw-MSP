# #3205 — Feature: Money — two-week pay-period cycle card (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T04:55:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 8051aed97 (merged to `main`)

## Log
- 2026-09-08T04:46:44Z ⏳ IN FLIGHT — read #3205 + `Shanes Life 17 - Money v3.dc.html` (options
  2a/2c). Real scope: a shared cycle card at the top of Now and Bills tabs — real date-range
  title ("Aug 28 – Sep 11"), 14 day cells with today marked amber, both paydays named, ‹ ›
  paging back through earlier real cycles, and three real numbers per tab (Now: Came in / Spent
  / Still to fund; Bills: In DirectDeposit / In bill accounts / Still to fund). No new schema —
  reuses `resolvePayCycleWindow`/`computeGateMath` (money.mjs, #3152/#3137) and the real
  `transactions` table (Plaid sign convention: amount<0 credit, amount>0 debit, per
  income-rules.mjs's own header). Building: `getCycleCard(userId, { asOf, cyclesBack })` in
  money.mjs, `GET /api/money/cycle-card`, and a shared `renderCycleCard()` in public/app.js
  wired into both `viewMoney` (Now) and `viewMoneyBills` (Bills).
- 2026-09-08T04:55:00Z ✅ DONE — `getCycleCard(userId, { asOf, cyclesBack })` added to money.mjs:
  walks `resolvePayCycleWindow`'s own real cycle window back `cyclesBack` cycles, builds the real
  14-cell day grid (today/future flags), real payday labels, and real Came-in/Spent transaction
  sums for the viewed window (Plaid sign convention: amount<0 credit, amount>0 debit). Still to
  fund / In DirectDeposit / In bill accounts stay the live current-balance snapshot on every page
  (no historical balance table exists — documented in the function's own header as a deliberate,
  honest choice, not a shortcut). Added `GET /api/money/cycle-card?cyclesBack=N`
  (src/routes/api.mjs) and a shared `renderCycleCard()` + `.money-cycle-*` CSS in public/app.js
  wired atop both `viewMoney` (Now) and `viewMoneyBills` (Bills), sharing one client-side
  `cycleCardOffset` so paging survives a tab switch. No schema change needed — no migration.
  **Verified live**, directly against the real local `finances` Postgres database (not the
  platform's own `shanemccawmsp` DB — Shane's Life is its own real DB per its own `.env.example`):
  called `getCycleCard` for `cyclesBack` 0/1/2 and confirmed the real current cycle (Aug 28 – Sep
  11, today correctly flagged Sep 8, day 12 of 14) and two real distinct prior cycles' real
  Came-in/Spent dollar figures (e.g. cyclesBack=1: $10,378.95 / $10,062.68; cyclesBack=2:
  $26,207.53 / $22,916.79), with Still-to-fund/In-DirectDeposit/In-bill-accounts staying the same
  live snapshot across all three, as designed. Booted the real dev server against that same DB
  (clean boot, migrations up to date) and confirmed `GET /api/money/cycle-card` returns a real
  401 (auth required, not a crash) — proving the route itself is wired correctly. `node --check`
  clean on all three touched JS/mjs files. Commit 9f65b1290 rebased cleanly onto a fast-moving
  `origin/main` (concurrent #3206/#3211 Shane's Life builds landing) with no conflicts, pushed
  directly to `main` as 8051aed97 per this repo's own git convention; `verify-branch-merged.mjs`
  confirms merged. Working tree clean.
