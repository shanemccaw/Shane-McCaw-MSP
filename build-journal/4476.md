# #4476 — alreadyPaid idempotent-replay fix

- **Status:** ✅ DONE 2026-09-18T04:02:10Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** 312f666cc (fix), b5fd4d0d7 (HEAD on main after rebase/push)

## Log
- 2026-09-18T03:49:09Z ⏳ IN FLIGHT — plan written (build-journal/4476-plan.md); about to fix `alreadyPaid` in `artifacts/api-server/src/routes/public-purchase-payment.ts` to read the live intent status on an idempotent replay instead of the cached create response, then verify against real Stripe test mode for Monitoring + Retainer.
- 2026-09-18T04:02:10Z ✅ DONE — Fixed `alreadyPaid` computation in `POST /api/public/purchase/payment-intent`
  (`artifacts/api-server/src/routes/public-purchase-payment.ts`): after `stripe.paymentIntents.create()`, detect an
  idempotent-key replay via Stripe's `Idempotent-Replayed` response header
  (`intent.lastResponse.headers["idempotent-replayed"]`) and only then `stripe.paymentIntents.retrieve(intent.id)` for
  the live status — the normal (non-replay) path stays a single Stripe call. Verified end-to-end against real Stripe
  test mode via a local dev api-server instance (port 8091, built from this worktree, real local Postgres data):
  created a real `checkout_sessions` row for **Monitoring** (`monitoring-growth-smb`, 30 seats) and one for
  **Retainer** (`architect-essentials-retainer`, consent-skipped), for each: POST `/payment-intent` (fresh —
  `alreadyPaid: false`) → confirmed the intent directly via the Stripe SDK with `pm_card_visa` (`status: succeeded`)
  → replayed the identical `/payment-intent` request (same sessionId → same idempotency key) → got back
  `alreadyPaid: true` both times. Server log for the Monitoring case shows the mechanism directly: first call
  `isReplay: false, liveStatus: "requires_payment_method"`; replay `isReplay: true, liveStatus: "succeeded"` (the
  create response's own cached `status` field stayed `"requires_payment_method"` on the replay, confirming the bug
  as described). Also confirmed the full recovery path: POSTing `/payment-confirmed` with the recovered
  `paymentIntentId` returned `{ ok: true, ... }`, and `Buy.tsx`'s existing
  `intentData.alreadyPaid && intentData.paymentIntentId` branch (unchanged, `artifacts/shane-mccaw-consulting/src/marketing/pages/Buy.tsx:1175`)
  is what calls that same `confirmPayment`/`payment-confirmed` path, so it now actually fires. Did not test Packs
  (2 of the 3 product types tested, per the issue's "at least two" ask) — Monitoring and Retainer cover the two
  distinct consent paths (tenant-consented vs. consent-skipped-Retainer), which is the more meaningful variation for
  this fix.
  `tsc --noEmit` on `artifacts/api-server`: pre-existing failures only, in `src/lib/ca-hold-window-scan.test.ts`
  (missing `byCategory` property, unrelated to this change — confirmed via `git log` that file wasn't touched in
  this session and the fix's own file has zero tsc errors).
  Test rows and dev-server process cleaned up after verification (`DELETE FROM checkout_sessions WHERE email IN
  (...)`, dev server process killed). `git status --porcelain` clean before this bookend.
  No migration, no schema change, no fixture data — this was a pure logic fix.
