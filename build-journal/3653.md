# #3653 — Drop BuildConsole's 13 stale tables from the shared product database

- **Status:** ✅ DONE 2026-09-11T19:30:00Z
- **Scope:** both (platform DB/schema + BuildConsole-owned tables)
- **Started:** 2026-09-11
- **Commit(s):** f86def5da (work), 4040466c7 (IN FLIGHT bookend)

Dispatch: BUILD comment by Shane on #3653, Posted: 2026-09-11T18:30:00Z (buildId 2344).

## Log
- 2026-09-11T18:50:00Z ⏳ IN FLIGHT — re-verifying the three preconditions (#3651 BuildConsole DB live, #3652 api-server routes disconnected, #3660 ShaneBuilder deleted), then pg_dump snapshot of the shared DB's 13 tables, confirm the BuildConsole DB holds the data, then DROP + remove Drizzle schema defs.
- 2026-09-11T19:30:00Z ✅ DONE — Preconditions: #3651 closed (BuildConsole + claim/release/run-claude on BUILD_DATABASE_URL only; the other DATABASE_URL readers in BuildConsole never query these tables); #3652 closed (admin-build-tracker.ts is a DB-free catch-all "retired"); #3660 tree has 0 entries on origin/main (issue still OPEN, labeled complete — Shane's to close; #3654 closed). No FKs, views, functions or triggers reached into the 13 from outside the set. Snapshot (outside git): `%USERPROFILE%\BuildConsole-db-snapshots\3653\shanemccawmsp-bt-tables-20260911T190045Z.{dump,sql}`, 13 tables, 6,251 rows (= live sum); restore tested into a scratch DB (6,251 rows), scratch dropped. Parity: 12/13 tables 0 shared-only PKs. The 38 shared-only bt_chat_mentioned_issues ids: 2 tuples re-keyed in BuildConsole, 36 are mentions of closed/non-existent issues that PruneClosedChatIssueMentionsAsync had already removed from the live copy. 0 shared rows newer than BuildConsole on any timestamp. One real divergence: build_dispatch_log id 1236 (#3651's own dispatch) had outcome=verifying/session 5238ac25-… only in the shared copy (BuildConsole had failed/null despite queue row 2326 carrying that session) — restored into the BuildConsole DB by guarded single-row UPDATE before the drop. DROP: `lib/db/migrations/manual/2026-09-11-drop-shared-bt-tables-3653.sql` (single DROP TABLE IF EXISTS x13, no CASCADE, public.users guard — guard tested alone against the BuildConsole DB and refused; @migration-gate: manual; self-marking) run on local shanemccawmsp after re-checking the 6,251 total → 0 relations left, 393 product tables untouched, marker row present. Drizzle Build Tracker defs (7 tables + types, zero importers) removed from lib/db/src/schema/index.ts; `tsc --build` pass; `check-drift` pass. migrate-buildconsole-db.mjs header notes the source is gone (it fails at the source count before touching the target). Not verified: api-server wasn't running locally (:8080 refused), so no live hit on the retired route (it has no DB path). Staging/Replit not touched — destructive line appended to #1630. Issue body said "Shane runs it himself"; ran locally on Shane's explicit BUILD-comment instruction ("the actual DROP"). Findings filed: none (1236 was a one-off from the #3651 cutover, not recurring). Pre-existing dirty files: none.
