# #2359 — Batter Up item: badge, effort, "why it is here"

- **Status:** ✅ DONE 2026-09-02T21:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** `e592d495c` (rebased to `d8406701a` on `main` — origin moved twice under
  concurrent builds while landing; no conflicts either rebase)

## Log
- 2026-09-02T20:15:00Z ⏳ IN FLIGHT — confirmed #2357 genuinely DONE on `main` (commit
  `b1c359738`, verified via its own bookend + `verify-branch-merged.mjs` semantics). Real
  audit: `MainWindow.BatterUpPanel.cs`'s `RenderBatterUpLaneBody()` currently renders each
  item as just `#number` + title, no badge/effort/why-here — explicitly scoped out by #2357
  ("Group header content beyond number/title/count and item content beyond number/title are
  deliberately out of scope"). Corrected badge semantics per #2359's own comment (not the
  stale doc wording): AGENT FOUND = `agent-finding` label present (a build session filed it
  mid-build), CHAT FILED = default (no such label). Building: extend
  `ChatGitHubFilter.BatterUpItemRef` with mutable detail fields (IsAgentFinding/Effort/
  WhyHere/DetailError), add a second BATCHED GraphQL call (aliased `issue(number:)` lookups,
  scoped only to the items actually in the 3 lanes — not the whole ~2,400-item board walk)
  fetching `labels`, `body`, and recent `comments` per item; mirror BuildConsole's own
  `BatterUpQueueService.ParseBuildComment` BUILD-header parser (model/effort/buildSet/
  Posted/prompt) for effort + "why it is here" text (BUILD comment's stated scope,
  falling back to the issue body when there's no BUILD comment yet). Then wire the panel's
  item row rendering to show a real badge pill, effort pill, and a dim why-here line.
- 2026-09-02T21:05:00Z ✅ DONE — commit `d8406701a` on `main` (verified via
  `verify-branch-merged.mjs`, working tree clean). Shipped:
  - `ChatGitHubFilter.cs`: `BatterUpItemRef` gains mutable `DetailLoaded`/`IsAgentFinding`/
    `Effort`/`WhyHere`/`DetailError`. New `EnrichBatterUpItemDetailsAsync` — a SECOND, BATCHED
    GraphQL call (20 issues/round trip via aliased `issue(number:)`, `repository { i2359:
    issue(number: 2359) { body labels(...) comments(...) } ... }`) scoped only to the real items
    already sorted into the 3 lanes by `GetBatterUpLaneCountsAsync` (dozens, not the whole
    ~2,400-item board) — same "no per-item round trip" discipline #2357 used for parent. New
    `ParseBuildComment` mirrors BuildConsole's own `BatterUpQueueService.ParseBuildComment`
    (model/effort/buildSet/Posted/prompt) so both apps read the identical `BUILD:`/`Posted:`
    header convention. New `ExtractLeadParagraph` pulls the real "why it is here" line — a
    BUILD comment's stated scope (after its leading `--flag` line) or the issue body's own
    lead paragraph, falling back honestly to null (never invented text) when both are empty.
  - `MainWindow.BatterUpPanel.cs`: `EnsureBatterUpPanelLoadedAsync` now awaits
    `EnrichBatterUpItemDetailsAsync` (status line: "Loading item details…") right after the
    lane-count walk succeeds, before the first render — a detail-fetch failure doesn't fail the
    whole panel load, items just carry `DetailError`. `RenderBatterUpLaneBody`'s item rows gain
    two real right-column pills (effort label via `BatterUpEffortLabel`, badge via
    `item.IsAgentFinding`) plus a new dim/italic single-line "why it is here" row
    (`BatterUpWhyHereLine`) underneath each item, ellipsis-truncated with the full text as a
    tooltip.
  - **Badge semantics corrected per #2359's own dispatch comment (not the stale design doc):**
    AGENT FOUND = the real `agent-finding` label present (filed by a build session mid-build,
    unplanned); CHAT FILED = default (no such label — planned, filed by Claude.ai chat). Created
    the `agent-finding` label (this build introduces it) — **new standing convention as of
    2026-09-02, not yet written into CLAUDE.md/README: apply `agent-finding` to every issue an
    agent build session files as a mid-build finding, going forward.** That should get folded
    into CLAUDE.md's "Mandatory: file every finding as its own GitHub issue" section in a future
    pass.
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` clean (0 warnings/0 errors)
    after every rebase (3 total — origin/main moved under concurrent builds landing #2364,
    #2401, #2479 while this session worked; all three rebases were clean, no conflicts). Did NOT
    stop at simulated data — re-ran the exact GraphQL query shape live via `gh api graphql`
    against real issues (#2479, #2484, #2496) and independently re-implemented the same
    parse/extract logic in a throwaway Node script fed the real response, confirming badge/
    effort/why-here match what the shipped C# does: #2479 (real dispatched Feature sub-issue) →
    CHAT FILED, effort=high, real scope line; #2484 (a real mid-build finding predating the
    `agent-finding` label) → CHAT FILED (correctly, since it has no `BUILD:` comment/label yet),
    effort=not yet estimated; #2496 (this session's own filed finding, labeled `agent-finding`)
    → AGENT FOUND, not yet estimated, real why-here text. Did not launch the WPF app itself — no
    headless runner exists for ShaneBuilder yet (same gap #2356/#2357 already noted); build +
    live-query cross-check is the real verification done.
  - **Finding filed:** #2496 "Backfill agent-finding label on pre-existing agent-found issues
    (e.g. #2409)" — a real pre-existing mid-build finding (#2409, and likely #2484) predates the
    new label and will show CHAT FILED even though it's a genuine AGENT FOUND case; a dedicated
    backfill pass is needed. Parented as a direct sibling sub-issue of #2355 (not the Epic),
    labeled `bug` + `agent-finding` (this build is the one introducing that label, and #2496 is
    itself a real finding this session made), board status set to "AI Batter Up" per the standing
    rule.
  - No other findings — stayed inside this issue's own item-detail scope; group header content
    (#2358) and item actions (#2360) remain untouched, as before.
