# #1705 — Chat context meter banners: de-brighten all three severity tiers

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 291d19fab5fe34cf20f7e4fa3c677f9531f72a9a (bookend), 55c796422947dec7d92562432cd2c0477b6fb986 (banner restyle)

## Log
- 2026-08-29 ⏳ IN FLIGHT — read issue #1705 (no comments), `UpdateContextMeter` (~5675-5720)
  in `MainWindow.xaml.cs`, and `Themes/DarkTheme.xaml`. Note: `design-system-brief.md`
  referenced by the issue does not exist anywhere in this repo (checked working tree,
  `git log --all` for the filename, and a repo-wide grep for "score-dynamic") — proceeding
  directly from the issue's own fully-specified "Required fix" section (dark `MantleBrush`
  base + low-opacity tier-hue wash/accent stripe + `TextBrush`/`MauveBrush` foreground),
  which restates that same philosophy inline. About to implement the three-tier banner
  restyle in `UpdateContextMeter`.
- 2026-08-29 ✅ DONE — replaced all three flat Yellow/Peach/Red banner fills with
  `MantleBrush` base + a new `BuildTierWashBrush(tierKey, amount)` helper that alpha-blends
  each tier's existing hue into `MantleBrush`'s RGB (Yellow 16%, Peach 22%, Red 30% — red
  runs more saturated per the issue's allowance for the non-dismissible critical tier), plus
  a left accent-border stripe in the same hue at higher opacity (55/70/90%) as a second,
  clearer signal. Foreground swapped `CrustBrush` → `TextBrush` (banner text, close button,
  hand-off button) and `MauveBrush` for the red-tier heading, for legibility against the new
  dark background. Dismiss/re-surface/hand-off button *behavior* and the progress-bar fill
  colors untouched — confirmed by diff, only `Background`/`BorderBrush`/`Foreground`
  assignments and the new helper method changed.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 errors (46
  pre-existing warnings, none touching this change).
  Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. `git status
  --porcelain` clean. No new findings to file.
