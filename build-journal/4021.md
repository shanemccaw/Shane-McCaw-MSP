# #4021 — POA&Ms: generate the customer-Portal contract pack from portal-poams.ts

- **Status:** ✅ DONE 2026-09-14T15:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8be0a7e6e (`f32b0820f` on `origin/main` after rebase) — `docs/portal/poams-contract-pack.md`

## Log
- 2026-09-14T14:10:00Z ⏳ IN FLIGHT — pulled #4021 + full #1935 architecture history + read sibling `docs/msp-console/poams-msp-console-contract-pack.md`; about to read `portal-poams.ts` in full and cross-check the live schema before writing `docs/portal/poams-contract-pack.md`.
- 2026-09-14T15:05:00Z ✅ DONE — read `portal-poams.ts` in full (749 lines, 6 routes — more than the "569 lines, 4 routes" #4021/#1935 both describe; Git #3451 added DELETE + request-acceleration after those issues were last edited), `poam-ref.ts`, `risk-authority.ts`, `portal-tier-features.ts`, `portal-customer-scope.ts`, the retention wiring module, and the `msp_poams`/`msp_poam_milestones` Drizzle schema. Cross-checked live against local PostgreSQL: schema matches Drizzle exactly, no `CHECK` constraints on either table, and confirmed via a live query that no real Monitoring tier currently includes `poams` in `includedFeatures` (both READ routes 402 for every tenant today). Confirmed repo-wide zero frontend consumers for any `/api/portal/poams*` route — expected pre-Design/pre-wire state per this repo's own precedent (Risk Register pack, MSP-console POA&Ms pack), not filed as a finding. Wrote `docs/portal/poams-contract-pack.md` (read-only extraction, no product code/schema/UI changed), committed `8be0a7e6e`, rebased onto `origin/main`, pushed (`f32b0820f`). `verify-branch-merged.mjs` confirms merged. Filed two real, live findings: **#4027** (`portal-poams.ts`'s `WirePoam.signed.authorizedBy` is hard-coded null and never actually attached on either GET route, despite the point-in-time authority data existing on the row and the identical concept working correctly on `portal-risk-register.ts`; parented #1935, `bug`, AI Batter Up) and **#4028** (`msp-poams.ts`'s list/get routes never filter `deletedAt`, so a POA&M soft-deleted on one surface keeps appearing on the other; parented #1571, `bug`, AI Batter Up — the sibling MSP-console pack predates Git #3451 so couldn't have found this). Working tree clean.
