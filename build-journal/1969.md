# #1969 — Queue-poll callers missing the `_db` branch, slow refused-connection fail, log spam

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 0e101c2b4

## Log
- 2026-08-30 ⏳ IN FLIGHT — implementing the three fixes from the issue body:
  1. `_db != null ? ... : ...` branch on `MainWindow.xaml.cs:3641` (PollChatTabBuildStateAsync)
     and `BuildLogView.xaml.cs:96` (RefreshRunningBuildPillsAsync), matching the convention
     already used at MainWindow.xaml.cs:3510-3517, QueueWatcherService.cs:566,
     BuildWatchWindow.xaml.cs:861, GitDetailView.cs:506-508, StartupConnectivityService.cs:405-408.
  2. Fail-fast on a refused local connection in `BuildTrackerApiClient`'s HttpClient (short
     `ConnectTimeout` via `SocketsHttpHandler`, instead of the ~4.1s observed).
  3. Transition-based logging in `TrackAsync` (reachable→unreachable / back, not a line per
     attempt) and the same discipline for `testing.remote-trigger`'s poll-failed log.

- 2026-08-30 ✅ DONE — commit `0e101c2b4`, pushed and confirmed merged into `origin/main`
  (`verify-branch-merged.mjs` OK). What shipped, matching the issue's three required fixes:

  1. **`_db` branch on both broken callers** — `MainWindow.xaml.cs`'s `PollChatTabBuildStateAsync`
     now does `_queueDb != null ? await _queueDb.GetQueueAsync() : await _buildTrackerApi.GetQueueAsync()`,
     same as the five correct call sites cited in the issue. `BuildLogView.xaml.cs`'s
     `RefreshRunningBuildPillsAsync` needed a new `BuildQueuePostgresClient? _db` field + an
     `Initialize(api, db = null)` overload (same shape as `LeftSidebar.Initialize`) — MainWindow's
     one call site updated to `BuildLogView.Initialize(_buildTrackerApi, _queueDb)`.
  2. **Fail-fast on a refused connection** — `BuildTrackerApiClient`'s `HttpClient` now uses a
     `SocketsHttpHandler { ConnectTimeout = TimeSpan.FromSeconds(2) }` instead of the bare default
     handler (which fell through to the 20s overall `Timeout`). Isolated verification (throwaway
     console app against `localhost:8080`, confirmed down for this whole session): default handler
     4334ms per refused attempt → 2079ms with the capped `ConnectTimeout`, matching the real ~4.1s
     from the issue's log evidence.
  3. **Transition-based logging** — `TrackAsync` (now an instance method, was `static`) tracks
     `_serverReachable`/`_consecutiveFailures` per client and only logs the reachable→unreachable
     and unreachable→reachable transitions, suppressing the repeat `-> `/`ok`/`FAILED` lines while
     down. Same discipline applied to `testing.remote-trigger`'s poll-failed catch in
     `TestTriggerTickAsync` (new `_testTriggerPollReachable` bool) — its polling cadence/behavior
     itself untouched, per the issue's scope note.

  **Verification (API server DOWN — the real, unforced state of this session):**
  confirmed via `curl`/`netstat` that nothing listens on :8080 for the whole session (matches the
  issue's real evidence, not a contrived repro). Confirmed the real local Postgres has live queue
  data (`SELECT count(*) FROM bt_build_queue` → 831 rows) that `_queueDb.GetQueueAsync()` now
  serves directly at both fixed call sites, meaning in the real "server down but Postgres up" case
  these two callers no longer touch HTTP at all — zero `[api] x GET queue FAILED` lines is now
  structural, not just faster. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`
  is clean (0 warnings, 0 errors).

  **UP-scenario reasoning (not a live full-stack run — see honesty note below):** did not spin up
  the full `artifacts/api-server` dev stack in this session (heavyweight build+start, and outside
  this issue's scope of "wasted time and misleading logs"). Verified by code inspection instead:
  when the server has been continuously reachable, `TrackAsync`'s `wasUnreachable` is always
  `false`, so it takes the exact original always-log branch unchanged — no new branch, no new
  behavior on the success path. The `SocketsHttpHandler.ConnectTimeout` change only bounds the
  connect phase and has no effect once a connection succeeds. This is inspection-based confidence,
  not a live-UP confirmation — noting that honestly rather than claiming a run that didn't happen.

  No findings filed — this was a pure logging/branching fix with no new gaps surfaced along the
  way. No manual SQL migration. No `Shane To-Do` action. `git status --porcelain` clean before
  this commit.
