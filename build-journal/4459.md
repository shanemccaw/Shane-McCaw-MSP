# #4459 — Visual Test Tracker base_url key differs across tables (watched base vs scheme://authority)

- **Status:** ✅ DONE 2026-09-17T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 72c520686 (rebased to 0da54e3e3 on origin/main)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — standardizing Visual Test Tracker base_url key on the watched-base
  string (what `visual_test_tracker_pages` already uses) across `PageAutoCheckService.BaseUrlKeyFor`,
  `MainWindow.RunPageAutoCheckAsync`, and the three inline `scheme://authority` computations in
  `TestModeComposerPanel.xaml.cs`. Confirmed `visual_test_tracker_dom_mutations` /
  `visual_test_tracker_a11y_audits` have 0 real rows locally — no data migration needed.
- 2026-09-17T00:30:00Z ✅ DONE — `PageAutoCheckService.MatchWatchedBase` is now the single real matcher
  (moved out of `MainWindow.MatchesWatchedVisualTestBaseUrl`, which now forwards to it);
  `BaseUrlKeyFor` returns the matched watched base (preserving any path prefix) instead of
  `scheme://authority`. `RunPageAutoCheckAsync` uses the `watchedBase` it already received instead of
  re-deriving a different key. The composer's three inline `scheme://authority` computations
  (`CheckDomVerificationStatusAsync`, `BtnVerifyDomCheck_Click`, `BtnUpdateDomBaseline_Click`) now call
  `PageAutoCheckService.BaseUrlKeyFor`. Build: `dotnet build BuildConsole.csproj` — 0 errors, 0
  warnings. Verified against the real compiled logic via a scratch `--instance` settings dir (isolated
  under `%AppData%\BuildConsole-verify4459\`, cleaned up after, never touching the real settings.json):
  with `VisualTestTrackerBaseUrls = ["localhost:5175/portal", "localhost:5175/admin"]`,
  `http://localhost:5175/portal/dashboard` and `http://localhost:5175/admin/dashboard` now resolve to
  distinct `BaseUrlKeyFor` keys (`localhost:5175/portal` vs `localhost:5175/admin`), where the old
  `scheme://authority` computation collapsed both to `http://localhost:5175`. Pushed to origin/main as
  0da54e3e3 (rebased from 72c520686); `verify-branch-merged.mjs` confirms ancestry. Working tree clean.
