# Git #2133 — Chat mention hover: loud EPIC badge

- **Status:** ✅ DONE 2026-09-01T00:40:00Z
- **Scope:** buildconsole

## Log
- 2026-09-01T00:31:18Z — Starting. Adding a loud, high-contrast EPIC badge (background-filled pill) alongside the existing OPEN/CLOSED status pill in `IssueMentionInjector.cs`'s `window.__btShowIssueTip`, so an epic mention is unmissable at a glance during fast chat scanning (Shane reported nearly closing epics by mistake ~10 times).
- 2026-09-01T00:40:00Z — ✅ DONE — Added a filled `EPIC` pill (yellow bg `#F9E2AF` on dark text, bold, letter-spaced, ring shadow) shown before the OPEN/CLOSED pill when `isEpic` is true, in `desktop/BuildConsole/Services/IssueMentionInjector.cs` (`window.__btShowIssueTip`). Kept existing 🔷/🔹 icon and OPEN/CLOSED pill unchanged — badges render side by side. `dotnet build desktop/BuildConsole/BuildConsole.csproj` succeeded (0 errors/warnings). No automated UI test exists for this popup; verified by reading the generated HTML/CSS by inspection per the build prompt. Commit `f9947aab8` pushed directly to `main` (fast-forward, main hadn't moved) and confirmed merged via `verify-branch-merged.mjs`. Working tree clean.
