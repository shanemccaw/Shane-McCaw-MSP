# #3478 — admin-insights.ts / insight-pdf.ts rehome

- **Status:** ✅ DONE 2026-09-10T22:42:47Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 1974fad2d (bookend IN FLIGHT), b848d6665 (rehome, on `main`)

## Log
- 2026-09-10T22:39:34Z ⏳ IN FLIGHT — rehoming the 3 live GET routes (`/admin/insights/documents`, `/admin/insights/documents/:id/download`, `/admin/insights/projects`) out of admin-insights.ts into admin-document-downloads.ts, and the generic HTML→PDF helpers (`buildHtmlDoc`/`htmlToPdf`/`renderLiveDocumentToPdf`) out of insight-pdf.ts into html-pdf.ts, then deleting the genuinely dead remainder of both files, per #3478's own rehome plan.
- 2026-09-10T22:42:47Z ✅ DONE — Rehomed the 3 live GETs + `generateInsightsPdf`'s pdf-lib
  helpers into new `admin-document-downloads.ts` (URL paths kept as `/admin/insights/...`
  since that literal string is stored in existing documents' `pdf_url`); renamed
  `insight-pdf.ts` → `html-pdf.ts` (content unchanged) and updated all 10 real
  importers; deleted admin-insights.ts and the genuinely dead consulting/automations/
  telemetry code entirely; removed the 2 now-orphaned payload-preview paths + 4
  schemas from `lib/api-spec/openapi.yaml` and regenerated `lib/api-zod` /
  `lib/api-client-react` via orval; removed 5 vestigial `vi.mock("../routes/
  admin-insights")` stubs; updated stale historical comments across 6 files + 1 test
  manifest. Verified: `pnpm typecheck` on api-server clean; `vitest run` on every
  touched test file — 394 passed, 1 pre-existing unrelated failure
  (`social-media-nodes.test.ts`, missing `AI_INTEGRATIONS_OPENAI_BASE_URL` under
  vitest — an environment-loading issue, not caused by this change). Pushed straight
  to `origin/main` (b848d6665) after rebasing onto a moved main; verified merged via
  `verify-branch-merged.mjs`. Working tree clean. Filed #3555 (parented under #1095,
  board status AI Batter Up) for the pre-existing `social-media-nodes.test.ts`
  standalone-vitest failure.
