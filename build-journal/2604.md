# #2604 — Settings: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-06T17:49:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 93c7f5087d57ee00c4af1615ca2cd415870bdc98 (`docs/msp-settings-contract-pack.md`)

## Log
- 2026-09-06T17:38:49Z ⏳ IN FLIGHT — Read-only extraction pack for MSP Console Settings, per #1642
  pattern. Backend is `artifacts/api-server/src/routes/msp-settings.ts` (1781 lines, ~40 endpoints)
  plus related tables/lib. Beginning full line-by-line audit against the real, current code.
- 2026-09-06T17:49:00Z ✅ DONE — Wrote `docs/msp-settings-contract-pack.md`: full line-by-line
  extraction of all 39 routes across 11 functional groups (A–K), real enum unions cited to
  `lib/db/src/schema/msp.ts` / `schema/index.ts`, cross-surface edges, honest tri-state/empty
  states, live-DB row counts queried against local `DATABASE_URL`, forbidden list, and the
  orphaned-endpoints section — confirmed by repo-wide grep that all 39 endpoints are uncalled by
  any frontend app (`artifacts/msp-console` is a bare #2668 scaffold with no Settings page).
  While drafting, caught my own draft error before committing: the route file's in-code comment
  on `PATCH .../connector/mailbox/write-back` claims "no enforcement logic reads this flag yet",
  which is stale — `mspsTable.writeBackEnabled` is the real fail-closed Gate 1 of
  `graphWriteForTenant()` (`artifacts/api-server/src/lib/graph.ts:1041-1067`), gating every
  tenant-scoped Graph write. Corrected the pack before commit and filed **#3031** (stale-comment
  finding, `bug`+`security`, sibling sub-issue of Feature #1690, board status set to AI Batter Up)
  rather than let the pack repeat the stale claim. `pnpm typecheck` not run — this build touched
  only a markdown doc, no product code/schema/TS changed. Branch verified merged into `main`
  (`verify-branch-merged.mjs`), working tree clean.
