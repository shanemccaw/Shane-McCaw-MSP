# #3244 — Important documents (wills, life insurance, and the like)

- **Status:** ✅ DONE 2026-09-08T12:35:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 56493bd9e (feature), a4d9b7e4a (merge with #3241)

## Log
- 2026-09-08T12:15:23Z ⏳ IN FLIGHT — investigating web/shanes-life (Money room, Vault
  pattern at src/core/vault.mjs + api.mjs + app.js) to build a real "Important Documents"
  entry: doc type/name, plaintext location/summary (real search target), encrypted key
  details reusing the vault's own AES-256-GCM + SL_VAULT_KEY + passkey-gated reveal
  infrastructure (issue's own explicit lean: "same real security model as Vault"),
  kept as its own room/tab (Money → Documents) distinct in content type from Vault per
  the issue body. No MCP tool, same precedent as vault.mjs (genuinely sensitive content).
- 2026-09-08T12:35:00Z ✅ DONE — real, open-ended `important_documents` table (migration
  053 -- 052 was already taken by concurrent sibling #3242's still-in-flight
  052_vault_password_entries.sql; applied+ledgered by hand against local Postgres per
  CLAUDE.md's Database section rather than blocking on that session), `documents.mjs`
  (mirrors vault.mjs's AES-256-GCM/passkey-reveal discipline on the SAME SL_VAULT_KEY),
  full `/api/documents*` route set including real deterministic search (`GET
  /api/documents/search`), and a Money → Documents tab in app.js reusing the vault's own
  visual language. `bin/check.mjs` gained 17 real e2e checks against a live software
  WebAuthn authenticator covering both of the issue's own verification examples ("where's
  my will" and "who's my life insurance beneficiary") plus the full security surface
  (reveal genuinely gated on a fresh assertion, replay/cross-entry assertions refused, real
  per-reveal audit row, delete cascades reveal history). Ran the real suite twice (before
  and after merging origin/main's concurrent #3241 Wins-room-split, which touched the same
  MONEY_TABS array and required a real conflict resolution) -- 251/252 both times; the one
  failure is a pre-existing, unrelated PUBLIC_ORIGIN/scratch-port mismatch in an existing
  Plaid-webhook check, confirmed by inspection, not a regression from this work.
  Branch verified merged into origin/main (`verify-branch-merged.mjs` OK). Working tree
  clean.
