# Issue #4495 — MSP Directory full rename

- **Status:** ✅ DONE 2026-09-17T04:59:42Z
- **Scope:** platform

## Log
- 2026-09-17T04:39:48Z — Starting. Issue body was expanded (scope-correction comment 2026-09-17T04:40:00Z) from display-copy-only to a full frontend+backend rename: `screens/ad/` → `screens/directory/`, `Ad*` → `Dir*` identifiers/files, `screenId "ad"` → `"msp-directory"`, backend `admin-active-directory.ts` split into `admin-active-directory-ou.ts` (OU/policy routes, unchanged paths/behavior) and `admin-msp-directory.ts` (new `/admin/msp-directory/*` paths). No prior bookend or worktree found for this issue — starting fresh against the current (corrected) issue body.
- 2026-09-17T04:59:42Z — ✅ DONE — commit `72223bb94` (rebased/pushed to origin/main; pre-rebase local commit was `87cf0e788`).
  Frontend: `screens/ad/` → `screens/directory/`, all 18 files renamed `Ad*`/`ad*` → `Dir*`/`dir*`, every exported identifier renamed to match, `screenId`/`id`/`area`/`tab`/`route` `"ad"`/`"/ad"` → `"msp-directory"`/`"/msp-directory"` everywhere (index.tsx, DirExplorerTree.tsx, canvases, Shell.tsx's Marketing tile, registry/types.ts's `FIXED_TAB_IDS`/`FIXED_TAB_LABELS`/`MAIN_TAB_IDS`), literal `OU=` rendered text stripped from DirExplorerTree.tsx/DirOuCanvas.tsx, "Active Directory" → "MSP Directory" copy throughout. Legacy `/system/active-directory` page (`ActiveDirectory*Pane.tsx`, `ActiveDirectoryTree.tsx`) left untouched/decommissioned per issue scope — its own old fetch calls against now-moved paths are expected to break; `useDirectoryRoles.ts` (shared with the legacy page) was re-pointed at the new `/api/admin/msp-directory/roles` endpoint so the legacy page's role dropdown keeps working rather than breaking.
  Backend: `admin-active-directory.ts` (2406 lines) split into `admin-active-directory-ou.ts` (OU/policy-engine routes — `ou*`, `ou-assignments*`, `resolveAssignmentCustomer`, `resolveGraphUserByUpn` — unchanged paths/behavior) and `admin-msp-directory.ts` (Tenant/Customer/MSP/User/Group management routes, moved to `/admin/msp-directory/*`). Grepped the repo for other real callers before cutover: found `msp-active-directory.ts` and `portal-active-directory.ts` importing `resolveAssignmentCustomer`/`resolveGraphUserByUpn` from the old file — re-pointed both (+ their mocked test files) at `admin-active-directory-ou.ts`. Three backend test files exercising the moved routes (`admin-active-directory-{credential-ops,delete,user-actions}.test.ts`) renamed to `admin-msp-directory-*.test.ts` with endpoint paths updated. `routes/index.ts` now mounts both new routers in place of the one old one.
  Concurrent-work conflict: a separate concurrent build (#4493, landed on origin/main mid-session) had added new drill-down/search/classifier functionality to the old `AdCustomerCanvas.tsx`/`adApi.ts`/`adTypes.ts` paths. Rebasing onto origin/main after my first push attempt hit real 3-way conflicts in `DirCustomerCanvas.tsx`/`dirApi.ts`/`dirTypes.ts` — resolved by hand, keeping #4493's new functions/types (`fetchDiagnosticRunFindings`, `MonitoringPackageChecks`, the Recent-scans Refresh button, findings search/highlight) and applying the same `Ad→Dir` identifier rename to them.
  Verification: `tsc --noEmit` clean across both `admin-panel` and `api-server` (re-checked after the rebase/conflict resolution too). Tests run and passing: `directory.test.tsx` (9), the three renamed backend test files + `msp-active-directory.test.ts` + `portal-active-directory.test.ts` (176 combined post-rebase), `lib/active-directory.test.ts` (84, part of that 176). `verify-branch-merged.mjs` confirms `72223bb94` is an ancestor of `origin/main`. `git status --porcelain` clean.
  Finding filed: #4500 (Home tab's "Marketing" quick-rollup tile in Shell.tsx opens the MSP Directory screen instead of an actual marketing screen — pre-existing bug, unrelated to this rename, preserved as-is with only the `"ad"`→`"msp-directory"` id mechanically updated to keep it wired to whatever it already pointed at). Parented as a sub-issue of #4490 (this issue's Feature parent), labeled `bug`, milestone v1.1, board status set to AI Batter Up.
