# #2200 — Feature: Log Viewer (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T06:45:00Z
- **Scope:** buildconsole (ShaneBuilder — new isolated project, wpf-handoff Step 10)
- **Started:** 2026-09-02
- **Commit(s):** 0f1764069fa51a7f6fe2d23bdb5ee4d30d7440cb (pushed to origin/main, verified via verify-branch-merged.mjs)

## Log
- 2026-09-02T05:50:00Z ⏳ IN FLIGHT — Pulled issue #2200 body + comments (including the 2026-09-02 level-inference-is-per-source correction). Starting real audit of ShaneBuilder project structure, theming, DB access, and per-source log code paths (9 sources: Websites Marketing/Portal/Admin, Services API Server/SQL Server, Local Build/SSH/Terminal/Console Output) before writing ILogService.
- 2026-09-02T06:45:00Z ✅ DONE — Real audit found: no MVVM/DI in ShaneBuilder (plain code-behind, matches GitDoctorService precedent), Step 9 Tab Workspaces not built yet (confirmed also IN FLIGHT live with Shane per #2198's own body — correctly left untouched), only API Server writes structured levels to `platform_log_stream` (everything else raw stdout with no level field). Built `Services/LogService.cs`: real `ILogService`/`LogSource`/`LogLine`/`LogQuery`/`ArchiveNode` contract, zero inference for API Server (queries `platform_log_stream.level` directly via a new Npgsql connection ported from BuildConsole's `BuildQueuePostgresClient` pattern), `InferLevel` keyword-matching (with a mandatory "guess, not fact" doc comment) for Marketing/Portal/Admin/SQL(Postgres server log)/Build/Console — SSH/Terminal have zero real backing in ShaneBuilder today and return empty rather than fabricated lines. Wired both surfaces: `LogViewerDock` (full document — left rail Sources/Archive, filter bar with search/regex/exclude/HIGHLIGHT/saved-filters/level-pills/history-scrubber, center stream, right inspector with filter chips/line detail/scratch pad) and `LogPeekPanel` (280px flyout bolted onto `ClaudeChatDock`, same mechanism as the existing `DetectedItemsPanel`/`StatDetailPanel`). COLD/BURST/LIVE streaming via a real polling `Tail()` (DB poll for API Server, file-position poll for file-backed sources) merged through a `System.Threading.Channels.Channel`; BURST self-cancels via a `DispatcherTimer` countdown. Archive tab builds a real day→build tree from `build-journal/*.md` (453 real files, 8 real days) and opens a bookend by Git ID (verified with a throwaway console harness against the real repo — real DB rows, real Postgres server log dir at `D:/Program Files/PostgreSQL/18/data/log`, real file tailing, all before deleting the harness). Pinned lines live in a separate `_logPinned` list untouched by filter changes, with "copy as one block" and "send to chat" (clipboard + fenced ```log block, same precedent as Git Doctor's own "send to chat" — `ClaudeChatDock` is a real WebView2, not a composer this app owns).
  Verification: `dotnet build` clean (0 warnings/errors) after every edit; launched the real `ShaneBuilder.exe` via `Start-Process`, confirmed the main window renders with no XAML/resource runtime exception, then cleanly stopped it — could not drive the UI further because simulated keystrokes (SendKeys, to open the command palette) were blocked by the permission classifier, so the Log Viewer document/Log Peek flyout render paths themselves are verified by code review + successful markup compilation, not a live click-through. `git status --porcelain` clean; branch verified merged via `verify-branch-merged.mjs`; commit `0f1764069` pushed straight to `origin/main` (fast-forward, no rebase needed).
  Filed two real gaps found during the build: #2207 (SSH/Terminal sources have zero real capture in ShaneBuilder — out of proportion scope for this issue, each is its own multi-day feature) and #2208 (Archive tab has no real per-build stdout.log keyed by Git issue — only the bookend `.md` is real/openable today), both parented to #2198 and set to "AI Batter Up".
