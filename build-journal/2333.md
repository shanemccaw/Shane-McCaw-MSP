# #2333 — Notes list with per-note select, delete, and SENT state

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Feature: Test Pad, #2326)
- **Started:** 2026-09-02
- **Commit(s):** `d24e21b8b` — "Test Pad: notes list with per-note select, delete, and SENT state (#2333)"

## Log
- 2026-09-02T19:50:00Z ⏳ IN FLIGHT — Read #2333 body + comment. Test Pad shell (#2327, `TestPadService`/`TestPadNote`/`TestPadWindow`) is already on main; `TestPadWindow.Render()` currently draws a plain read-only `TextBlock` per note with no select/delete/SENT affordance and `TestPadService.RemoveNote` already exists but is unwired from any UI. Composer (#2328) is BLOCKED/not yet built, so notes can currently only be filed programmatically — this issue's scope (list rendering: select, delete, SENT state) is independent of the composer and doesn't need it. Plan: add `IsSelected` to `TestPadNote`, replace the plain row in `TestPadWindow.Render()` with a real row (checkbox select, delete button wired to `TestPadService.RemoveNote`, a SENT badge/locked-look for sent notes), build, commit, push.
- 2026-09-02T20:35:00Z ✅ DONE — Added `IsSelected` to `TestPadNote` (`Services/TestPadModels.cs`). Replaced `TestPadWindow`'s plain read-only row with a real per-note `Grid` row (`BuildRow`): a `CheckBox` bound to `note.IsSelected` (raises `TestPadService.NotifyMutated()` on toggle so the pill and any other subscriber stay live), the note text (dimmed once sent), a "SENT" badge pill shown only when `note.IsSent`, and a trash-glyph delete button wired straight to `TestPadService.RemoveNote(note.Id)`. Hit a real merge conflict rebasing onto `origin/main` — a concurrent session had landed #2334 ("By feature" grouping) touching the same `Render()`/`BuildNoteRow` — resolved by keeping #2334's grouping structure and swapping its plain `BuildNoteRow` calls for this issue's richer `BuildRow`, dropping the now-superseded plain row method. `dotnet build ShaneBuilder.csproj -c Debug` succeeded (0 warnings, 0 errors) both before and after the conflict resolution. `verify-branch-merged.mjs` confirms `d24e21b8b` is an ancestor of `origin/main`. `git status --porcelain` clean. No findings filed — this build only touched its own Test Pad files, matching scope.
