# #1471 — Testbed reset: extend to a fully empty testbed — audit every customer/tenant-scoped table, not just the 67 already covered

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 1733cbe67 (IN FLIGHT bookend), f48cf63ed (first pass: new standalone patch file), e89ddb664 (final: folded everything into 1396.sql per Shane's direction)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Live-queried `information_schema.columns` on the real local Postgres 18 instance for every table carrying a `customer_id` or `tenant_id` column, diffing the result against the 67 tables `2026-08-26-comprehensive-testbed-reset-1329.sql` + `2026-08-27-testbed-reset-patch-projects-1396.sql` already cover. Found genuine gaps (Scope Creep Engine, SLA Engine, `drift_collection_status`, `msp_report_definitions`, `tenant_compliance_scope`, `war_room_interaction_events`) plus one deliberately-excluded purchased-state table (`tenant_add_on_entitlements`, same guardrail #1396 applied to `client_services`). Confirmed via SSH the 8 stale `msp_change_requests` rows #1444 found are still present on the Replit staging DB.
- 2026-08-28 ✅ DONE (commit `e89ddb664`) — First pass (`f48cf63ed`) added the 14 new-gap DELETEs as a separate standalone patch file. Shane then asked directly for `2026-08-27-testbed-reset-patch-projects-1396.sql` itself to clean everything when run alone — folded #1329's original 67 DELETEs and all of #1471's new-gap DELETEs into that file's own `DO` block (on top of its existing projects+FK-dependents cleanup), removed the now-fully-duplicated standalone patch file, and rewrote every DELETE to run through a `to_regclass(...)` existence guard. That guard turned out to be load-bearing, not defensive: a live audit found the Replit staging DB's schema genuinely lags local dev by 13 tables (`customer_alert_*`, `drift_*`, `license_assignment_snapshots`, `overshared_items`, `retainer_*`, `tenant_compliance_scope`) — an unconditional `DELETE` against any of them would have aborted the whole transaction on staging. Also discovered via `simulator_migration_runs` that neither #1329 nor #1396 had ever actually been run anywhere before this session (a real, separate gap from #1445's own open question).

  Ran the finished, consolidated `1396.sql` standalone against Replit staging via SSH/psql: purged the 8 stale `msp_change_requests` rows #1444 found, plus 30,483 other real accumulated rows across the tenant's tables that had never been cleared. Verified zero rows remain across every table on staging that actually exists there. Re-ran the same finished file a second time on staging to confirm idempotency — 0 rows deleted (already clean), only the expected missing-table skip notices. Also ran it standalone against local dev DB, confirmed clean.

  No TypeScript touched — pure SQL migration work, so no typecheck/test-manifest run applies (test-manifests scope is `artifacts/msp-portal` / `artifacts/shane-mccaw-consulting` only). No new subsystem/route added, so no new logging channel was wired — the locked logging taxonomy governs application code, not one-off manual SQL migrations.

  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE bookend (`e89ddb664` confirmed an ancestor of `origin/main`).
