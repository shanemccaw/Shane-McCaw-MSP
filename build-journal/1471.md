# #1471 — Testbed reset: extend to a fully empty testbed — audit every customer/tenant-scoped table, not just the 67 already covered

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Live-queried `information_schema.columns` on the real local Postgres 18 instance for every table carrying a `customer_id` or `tenant_id` column, diffing the result against the 67 tables `2026-08-26-comprehensive-testbed-reset-1329.sql` + `2026-08-27-testbed-reset-patch-projects-1396.sql` already cover. Found genuine gaps (Scope Creep Engine, SLA Engine, `drift_collection_status`, `msp_report_definitions`, `tenant_compliance_scope`, `war_room_interaction_events`) plus one deliberately-excluded purchased-state table (`tenant_add_on_entitlements`, same guardrail #1396 applied to `client_services`). Confirmed via SSH the 8 stale `msp_change_requests` rows #1444 found are still present on the Replit staging DB. Writing a new additive patch migration file, then running the full extended reset (1329 + 1396 + new patch) against staging via SSH/psql and confirming zero rows across every covered table for the testbed tenant.
