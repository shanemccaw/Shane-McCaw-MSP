# #3887 — Status Reports: customer-facing read + comment endpoints (phase 1 of 4 — API)

- **Status:** ✅ DONE 2026-09-13T01:35:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 34f7809ce (feature), 5e90f0cef (IN FLIGHT bookend) — rebased and pushed as 49ca633ed on origin/main

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — new `portal-status-reports.ts` router (customer read + comment endpoints), new `msp_status_report_comments` table via manual SQL migration, notification on customer comment via existing `createNotification`.
- 2026-09-13T01:35:00Z ✅ DONE — Shipped `portal-status-reports.ts`: `GET /api/portal/status-reports` (published-only, scoped via `resolveCustomerId`), `GET /api/portal/status-reports/:id`, `GET/POST /api/portal/status-reports/:id/comments`. New `msp_status_report_comments` table (Drizzle schema in `lib/db/src/schema/msp.ts` + `lib/db/migrations/manual/2026-09-13-msp-status-report-comments-3887.sql`, run myself against local Postgres — confirmed via `psql`). Customer POST notifies the report's authoring MSP operator via existing `createNotification` (notification-center.ts) — no new pipeline. Verification: `tsc --noEmit` on `artifacts/api-server` clean except one pre-existing, unrelated test-mock type error (`auth-impersonation.test.ts`, predates this build, confirmed via `git show`); manually replayed the router's exact queries against real local Postgres data (published report id=5/customerId=1: list, detail, insert comment, read thread, cleanup) — all behaved as coded. `shaneapp://runTest` was not used since it hits the deployed build, not this worktree's uncommitted code (see prior session note) — DB-level query replay was the real, actionable verification available pre-merge. `verify-branch-merged.mjs` confirms `agent/3887-q2606` (49ca633ed) is an ancestor of `origin/main`. Working tree clean. No test manifest — scope is `artifacts/api-server/`, outside the msp-portal/marketing-site test-manifest requirement.
