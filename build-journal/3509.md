# #3509 — Duplicate BUILD: dispatch comments posted against the same issue

- **Status:** ✅ DONE 2026-09-10T23:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 20e44fc48 (rebased/pushed as 3f471a0fc on origin/main)

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — investigating what posts/consumes `BUILD:` dispatch
  comments and why two independent flows (MyArchitect-set, BatterUpClearOut-set) both
  targeted #3493 within six minutes. Goal: find + close the real gap (check-before-post
  guard or a single source of truth serializing dispatch decisions).
- 2026-09-10T23:15:00Z ✅ DONE — root cause: `BUILD:` comments are posted by whatever chat
  is "currently active" when a flow decides an issue is ready — DispatchPanel's Dispatch
  box (`DispatchPanel.xaml.cs`) and the Git Board hover popover (`LeftSidebar.xaml.cs`,
  `DispatchOrAskActiveChatAsync`) each independently call
  `BatterUpQueueService.FindBuildCommentAsync`, and on a miss ask their own active chat to
  write+post a `BUILD:` comment (`ActiveChatBuildRequestHelper.BuildAskMessage`) — with
  zero shared record of an outstanding ask across those two call sites, or across any chat
  following CLAUDE.md's Build Queue Method directly outside BuildConsole's UI entirely.
  Classic check-then-act race, confirmed live: two asks landed against #3493 six minutes
  apart. Fix: added `bt_dispatch_claims` (migration
  `lib/db/migrations/manual/2026-09-10-bt-dispatch-claims-3509.sql`, run against local
  `DATABASE_URL` in-session) as one real source of truth — a TTL-bound claim row per
  `github_number`, claimed atomically via `INSERT ... ON CONFLICT DO NOTHING`. Wired into
  `BuildQueuePostgresClient.TryClaimDispatchAsync`/`ReleaseDispatchClaimAsync`, called from
  both `DispatchPanel.xaml.cs` and `LeftSidebar.xaml.cs` before asking the active chat (a
  lost claim shows who holds it + when it expires instead of asking again), and released
  from `IssueDispatchService.DispatchAsync` once a real `BUILD:` comment is found. Exposed
  to any chat working outside BuildConsole via `scripts/dev-server/claim-dispatch.mjs` /
  `release-dispatch-claim.mjs` against the identical table, and CLAUDE.md's "Build-prompt
  header convention" now requires claiming before posting. Verified: `dotnet build` on
  `desktop/BuildConsole/BuildConsole.csproj` — 0 errors/warnings; manually exercised
  `claim-dispatch.mjs`/`release-dispatch-claim.mjs` against the real local DB (claim
  succeeds, second claim on the same issue fails with the real holder's info, release +
  re-claim works, `bt_dispatch_claims` confirmed empty after cleanup). No test manifest —
  this is `desktop/BuildConsole` only (WPF app), out of scope per CLAUDE.md's Test
  Coverage section. Branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` (3f471a0fc). Working tree clean.
