# #3254 — Build Queue panel: next issue-mirror sync countdown

- **Status:** ✅ DONE 2026-09-08T17:45:00Z
- **Scope:** buildconsole

## Log
- 2026-09-08T17:26:43Z — Session started. Task: show next issue-mirror sync countdown (#3113's `bt_issue_mirror_sync_state.last_full_sync_at` + 5min interval) in the Build Queue panel, live-updating, with Syncing/Retrying honest states.
- 2026-09-08T17:45:00Z — ✅ DONE. Real, small, unobtrusive live display added just under the Build Queue panel's existing QueueStatusBorder ("Issue mirror: next sync in Xm Ys" / "syncing…" / "retrying in Xs" / "sync due" / "never synced yet"). Reuses the existing 5s local-refresh cadence (`_localQueuePollTimer`, Git #3074) — no new timer/poll. `GitHubIssueMirror` exposes cheap in-memory `IsSyncing` / `LastAttemptUtc` / `FailedAttemptBackoff` (were private) so the Syncing/Retrying states are honest and don't need a new DB query; the actual next-sync time still comes from the existing `GetSyncStateAsync()` read of `bt_issue_mirror_sync_state.last_full_sync_at`, called on the same tick that already does a local Postgres read. Files: `desktop/BuildConsole/Controls/BuildQueuePanel.xaml`, `BuildQueuePanel.xaml.cs`, `desktop/BuildConsole/Services/GitHubIssueMirror.cs`.
  - Build: `dotnet build` — succeeded, 0 warnings/0 errors (both before and after merging concurrent origin/main work, including #3253's overlapping edit to the same file — merged cleanly, rebuilt clean).
  - No BuildConsole UI runner/live-click verification performed (per the never-launch-BuildConsole.exe rule) — verification is clean build + code review of the real read paths, consistent with this repo's WPF-app verification convention (no test manifest; BuildConsole is out of scope for those).
  - Merge note: my worktree branch `agent/3254-q1986` forked before #3253/#3167/#3177 landed on `origin/main`. Merged `origin/main` into my branch (one auto-merge in `GitHubIssueMirror.cs`, no conflict markers, reviewed the diff) and pushed the merge commit directly to `origin/main` (real commit `605b6acb`), per this repo's "commit directly to main" convention. `verify-branch-merged.mjs` confirms OK.
  - `git status --porcelain` clean at session end.
  - Commit: `605b6acb` (merge of `d5419ac3` "Build Queue panel: next issue-mirror sync countdown (Git #3254)" with `origin/main`).
