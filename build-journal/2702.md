# #2702 — write_action_catalog.template_id is 0/123 populated - execute always 409s

- **Status:** ✅ DONE 2026-09-03T23:22:08Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 44b563f7b (rebased/pushed as e2baf617c on main)

## Log
- 2026-09-03T23:13:05Z ⏳ IN FLIGHT — confirmed 0/123 `write_action_catalog.template_id` set against 102 active `baseline_action_templates`; building a per-row match (action_name/domain intent vs template label/endpoint) and an additive migration to link matched rows and promote their `status` to a new execution-ready value.
- 2026-09-03T23:22:08Z ✅ DONE — Hand-matched all 123 catalog rows against the 102 live
  `baseline_action_templates` rows by domain/intent (label + endpoint, not fuzzy text
  matching). 85 rows had a real, verifiable template match; wrote
  `lib/db/migrations/manual/2026-09-03-write-action-catalog-template-links-2702.sql`
  (85 idempotent, guarded UPDATEs — id + domain + action_name + `template_id IS NULL`)
  setting `template_id` and promoting `status` to a new `execution_ready` value, and
  ran it myself against local `DATABASE_URL`. Verified: `85/123` now have `template_id`
  set, zero orphaned template_id values (left-join check against
  `baseline_action_templates` returns empty), re-running the file is a confirmed no-op.
  7 rows already `blocked`/`blocked_no_workaround` (ids 115, 171, 180-184) left
  untouched — real, previously-documented platform gaps. 31 rows have no real matching
  template today (e.g. "Reset MFA (all methods)", generic "Update CA policy", "Create
  team", "Create site collection", "Enroll device (token)") — left unlinked rather than
  forced, per the issue's own instruction; each is a genuine standing Launch Control
  coverage gap, not a bug in this migration. Two deliberate one-to-many links, both
  verified against real endpoints, not guessed: Groups + Teams "Add/remove member" both
  → `action.add-group-member` (a Team's membership IS group membership under the Graph
  API), and Groups + Teams "Add/remove owner" both → `action.add-group-owner` — only the
  "add" direction has a real template on either side today. Added a Drizzle schema
  comment on `writeActionCatalogTable.status` documenting the real live status
  vocabulary including the new value, and confirmed via `grep`/route read that `status`
  is not read by any route logic (`msp-launch-control.ts`'s execute route gates purely
  on `templateId` being non-null) — this migration keeps the column honest, it does not
  change execute-route behavior beyond unblocking the 409 via `template_id`.
  `npx tsc --noEmit` on `lib/db` shows only pre-existing `drizzle-orm` module-resolution
  errors unrelated to this change (same class across the whole file, not introduced
  here — not remedied via `pnpm install` per standing repo policy). Checked for new
  findings before filing one: the adjacent gaps this work surfaced (Teams-specific
  membership templates missing, rollback `reversible` re-promotion) are already covered
  by sibling issues #2703 and #2772 under #2494 — no duplicate filed. Working tree
  clean; branch verified merged into origin/main (commit `e2baf617c`) via
  `scripts/dev-server/verify-branch-merged.mjs`.
