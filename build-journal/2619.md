# #2619 — Projects: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-14T00:00:00Z
- **Scope:** platform
- **Commit(s):** bfc61da81 (IN FLIGHT bookend, → 09790f210 on main); 1ee6c5ca8 (pack, →
  ddd7f075c on main)

Leaf issue under Feature #2561 (Projects, MSP Console), part of #1571. The real
backend for this Feature's Phase 1 landed and closed under #3773 —
`kanban_buckets`/`kanban_cards`, `artifacts/api-server/src/routes/msp-kanban.ts`.
This issue's own body was written before that landed and held itself
deliberately blocked; the dispatch comment (2026-09-14T03:56:00Z) confirms the
blocker is gone and asks for the #1642-pattern pack now. Read-only — no
product code, schema, or UI changed by this session.

## Log
- 2026-09-14 ⏳ IN FLIGHT — Pulled #2619 (body + dispatch comment), #2561, and
  the Phase 1 backend issue #3773, plus #3768 (nav-placement Feature, explicitly
  NOT a precondition per Shane's 2026-09-14 comment). Read `msp-kanban.ts` (421
  lines, 7 routes) in full, the Drizzle schema (`lib/db/src/schema/msp.ts:9639-9671`)
  and the migration (`lib/db/migrations/manual/2026-09-12-kanban-buckets-cards-3773.sql`).
  Confirmed live against local Postgres: `kanban_buckets`/`kanban_cards` both
  genuinely empty (0 rows) in local dev. Confirmed the `ladder.msp-operator`
  RBAC row's live `allow` list resolves to exactly MSPOperator/MSPAdmin/PlatformAdmin,
  matching the route header's claim. Found a real, undocumented cross-surface
  edge: `portal-delivery-kanban.ts` (`kanban_tasks` table) is a separate,
  pre-existing, already-wired Kanban system (admin-panel CRM `ProjectDetail.tsx`,
  fixed 5-column pipeline, SSE-synced) with no relation to the new
  `kanban_buckets`/`kanban_cards` Phase 1 backend — both literally named
  "kanban," a real disambiguation risk for the #3768 Design pass. Writing the
  pack now at `docs/msp-console/projects-msp-console-contract-pack.md`.
- 2026-09-14 ✅ DONE — Wrote `docs/msp-console/projects-msp-console-contract-pack.md`
  (261 lines, new file — no prior pack existed for this Feature): 7-route wire
  contract table, real per-route auth/ownership shapes, `bucketToWire`/`cardToWire`
  field tables, request-body Zod rules incl. the cross-bucket move constraint
  (`targetBucket.customerId === currentBucket.customerId`) and the
  `description: null` vs omitted distinction, confirmed **no enum vocabulary
  exists yet** (Phase 1 has no `type`/`status` column, by design), honest-empty
  contract confirmed live (both tables 0 rows in local dev), Phase-1-scope
  absences enumerated explicitly (no Phase 2 type field, no Milestones/Comms/
  Training/Automation wiring, no portal-facing read, no bulk endpoints, no
  GET-single route), and a real cross-surface finding: `portal-delivery-kanban.ts`
  (`kanban_tasks`, fixed 5-column pipeline, SSE-synced, wired into admin-panel's
  `ProjectDetail.tsx`) is a separate, already-live Kanban system with no code in
  common with #3773's buckets/cards model — recorded as a Design-input note for
  #3768, not filed as a bug (no drift or defect found; both systems are correct
  on their own terms). Orphaned-endpoint grep confirmed zero frontend callers of
  `msp/kanban`. No `bug`/finding filed — the header docblock and the live
  `ladder.msp-operator` RBAC row both matched the code exactly, nothing to
  report. Pushed to `origin/main` as ddd7f075c; `verify-branch-merged.mjs`
  exit 0; `git status --porcelain` clean.
