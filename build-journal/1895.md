# #1895 — Stop build-resource-model.mjs wiping config_resource_samples; restore the 4 needs_license verdicts

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 1fc2dedd3 (also 7988f5473 pre-rebase)

## Log
- 2026-08-30 ⏳ IN FLIGHT — investigated build-resource-model.mjs's `DELETE FROM
  config_resource_samples` plus the hard `ON DELETE CASCADE` FK on
  `config_resource_samples.config_resource_id` (the same root cause — even removing the
  explicit DELETE, the subsequent `DELETE FROM config_resources` would still cascade-wipe
  samples). Found the four `needs_license` verdicts are CURRENTLY correct in the local DB
  (a concurrent #1795/#1796 session appears to have re-run verify-sample.mjs today,
  2026-08-30 04:48 local), but the wipe bug is still live in code — the next
  build-resource-model.mjs run would revert them. Implementing: (1) stable
  `resource_key` column on `config_resource_samples` + drop the cascade FK, matching the
  precedent already set by `config_resource_property_divergence` and the new
  `config-snapshots.ts` tables (both cite this issue in their own schema comments); (2)
  shared reconciliation module so both `verify-sample.mjs` and `build-resource-model.mjs`
  re-apply `verified_live` / `failed_live` / `needs_license` from surviving samples,
  keyed by `resource_key`, after every rebuild.
- 2026-08-30 ✅ DONE — Both jobs done. (1) `config_resource_samples` now keys on a new
  stable `resource_key` text column; dropped its `ON DELETE CASCADE` FK to
  `config_resources.id`, matching the precedent `config_resource_property_divergence`
  and `config-snapshots.ts` already set for exactly this reason. New shared
  `scripts/config-state/reconcile-live-evidence.mjs` (`refreshSampleResourceLinks` +
  `applyLiveEvidence`) is called by both `verify-sample.mjs` and
  `build-resource-model.mjs` so `verified_live`/`failed_live`/`needs_license` have one
  definition, not two that could drift. Migration
  `lib/db/migrations/manual/2026-08-30-config-resource-samples-stable-key-1895.sql` run
  against local `DATABASE_URL`: 29/29 existing sample rows backfilled with
  `resource_key`, 0 orphaned; logged on #1630. (2) Restored the 4 real `needs_license`
  verdicts (`roleEligibilitySchedules`, `signIns`, `userRegistrationDetails`,
  `riskDetections`) — verified these were ALREADY correct in the local DB going in
  (fresh live evidence timestamped 2026-08-30 04:48 local, matching #1794's exact
  error codes, from what looks like a concurrent #1795/#1796 session's own
  `verify-sample.mjs` re-run), so no fresh Graph call was needed from this session;
  did not hand-set the value. Live end-to-end verification: ran the actual fixed
  `build-resource-model.mjs` (a full real rebuild, 1539 config_resources rows) and
  confirmed directly in Postgres afterward — all 29 sample rows survived, all 4
  resources correctly read `needs_license` again. Carrying the principle: checked
  every other `DELETE FROM` in `scripts/config-state/` (graph_entity_types/properties,
  config_resource_properties, config_resource_check_coverage) — all four are pure
  derivations nothing else ever writes observation into, so wiping+rebuilding them
  every run is correct as-is; `config_resource_samples` was the only offender.
  Also fixed, as unrelated shared infra breakage found and repaired along the way
  (not part of this issue's scope, not committed — it's inside node_modules/pnpm's
  store): `scripts/node_modules/pg` and the corresponding pnpm-store `pg@8.20.0`
  package directory were both broken (a dangling symlink into a since-cleaned-up
  worktree, and an empty extracted package dir), which blocked running ANY
  `scripts/config-state/*.mjs` script against Postgres from this or any other
  session. Repaired in place so the verification above (and future sessions) can
  actually run these scripts. No git-tracked files affected by that repair.
  Working tree clean; pushed straight to `main` (`1fc2dedd3`, rebased cleanly onto
  `origin/main`, `verify-branch-merged.mjs` confirms ancestor). Docs updated:
  `docs/graph-resource-model.md` now records this as resolved. Issue commented,
  labels flipped `in-flight` → `complete`. No new findings filed — no gap found
  outside this issue's own two jobs.
