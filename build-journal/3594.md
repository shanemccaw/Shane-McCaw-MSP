# #3594 — Menu bar: remove dead/unwired items

- **Status:** ⏳ IN FLIGHT 2026-09-11T01:10:42Z
- **Scope:** buildconsole

## Log
- 2026-09-11T01:10:42Z — Started. Removed `_Edit`, `_Selection`, `_Go` top-level menus
  (all-dead / redundant-duplicate). Removed dead items: File → "Open Session…", Run →
  "Run Verification". Per issue's confirmed-with-Shane expanded scope: removed `_Sound`
  menu, moved `BuildCompleteSoundMuted` toggle into `SettingsTabView.xaml` as a new
  `CheckBox` (`MuteCompletionSoundCheck`) on the existing Build Sound settings page,
  matching the `EncouragementCrittersEnabledCheck` pattern. Removed `_Help` menu
  entirely, including its dead "About BuildConsole" item and its two real items —
  "Open DevTools" (kept the underlying `OpenDevTools_Click` handler, still used by the
  title-bar DevTools button) and "🎉 Cheer Me Up!" (removed `CheerMeUp_Click` handler
  entirely — no other caller). Left `EncouragementService`'s automatic triggers and the
  `EncouragementCrittersEnabledCheck` Settings toggle untouched. `dotnet build
  --configuration Release` succeeds clean (pre-existing unrelated `FocusModeBar`
  CS0067 warnings only).
