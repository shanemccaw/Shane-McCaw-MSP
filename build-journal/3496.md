# Build Journal — Issue #3496

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T00:00:00Z — Session started. Task: add explicit "Requeue Terminal" bulk opt-in button to Batter Up panel per Shane's Option A decision (2026-09-10). Investigating BatterUpPanel.xaml/.cs and BatterUpQueueService.QueueRowAsync.
- 2026-09-12T00:20:00Z — ✅ DONE — Added a real "Requeue Terminal" button to `BatterUpPanel.xaml` header row, alongside the Free-flow toggle. Visible/enabled only while `_allRows` currently holds ≥1 row with a non-null `TrackedTerminalStatus` (a real failed/canceled dedup row — see `BatterUpQueueService.RefreshAsync`'s #1997 reappear logic), via new `UpdateRequeueTerminalVisibility()` called everywhere `_allRows` is set. Click handler (`BtnRequeueTerminal_Click`) confirms with a real count + issue-number list, then bulk-calls `BatterUpQueueService.QueueRowAsync(_db, row, ..., allowRequeueTerminal: true, gh: ...)` for each currently-displayed terminal row (same path a manual per-row reappeared-row Queue click already uses), reports real succeeded/failed counts + issue numbers via ActivityLog + a MessageBox, then refreshes. The everyday free-flow/gated auto-queue path is untouched — `RefreshAndAutoQueueAsync`/free-flow `QueueRowAsync` calls still never set `allowRequeueTerminal`, so #1997's anti-loop guard default is unchanged; this button is a separate, deliberate, one-time bulk override. Commit: `9dc7c099b`. Build verified: `dotnet build BuildConsole.csproj -c Debug` — 0 errors. No live Batter Up terminal rows were available in this session to exercise the button end-to-end against real data; verified by code review + a clean build instead (BuildConsole itself is out of scope for test manifests per CLAUDE.md). Branch-merged check (`verify-branch-merged.mjs`) OK; `git status --porcelain` clean.
