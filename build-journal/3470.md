# Issue #3470 — Feature: Screenshot Tool (MyArchitect)
Status: ✅ DONE 2026-09-10T16:55:00Z
Scope: buildconsole
Started: 2026-09-10T03:54:50Z
Completed: 2026-09-10T16:55:00Z
Commit: 81deaa198c34f554e544bccdafa8e6c7fa65a99b

## Resume session (2026-09-10, buildId 2160) — unblocked, client-post-wiring shipped
Picked up from the prior `🛑 BLOCKED` state. Checked both real `blocked_by` dependencies
before doing anything else, per standing rule:
- **#3493** (UI Shell Redesign) — now **closed** (`state: closed`, `complete` label).
- **#3503** (evidence/attachment schema+endpoint) — shipped in commit `e4a295998` on this
  same branch/`main`, labeled `complete`. Real endpoints confirmed live:
  `artifacts/api-server/src/routes/msp-evidence-attachments.ts` — POST/GET
  `.../remediation-tracker/steps/:stepId/evidence`, POST/GET
  `.../change-control/executions/:id/evidence`.

Both conditions for "unblocked" were met (closed / `complete`-labeled) — removed both
`blocked_by` edges via the GraphQL-adjacent REST dependency API, flipped `blocked` →
`in-flight`, and did the actual remaining work: wired `IEvidencePostClient` for real.

**What shipped this session** (commit `81deaa198`):
- `Services/EvidencePostClient.cs` — real HTTP multipart-form POST to both #3503
  endpoints, same base-URL/`AuthToken` pattern as `ChangeControlService`/
  `LaunchControlActionsService` (no live auth/session mechanism exists anywhere in
  MyArchitect yet — #3460/#3501, unchanged by this build; calls will legitimately
  401/403 until that lands).
- `Services/IEvidencePostClient.cs` — split into `PostRemediationStepEvidenceAsync`
  (customerId + stepId) / `PostChangeControlEvidenceAsync` (executionId), returning a
  real `EvidencePostResult` (success/error/attachmentId) instead of a bare bool.
  Removed the now-dead `BlockedEvidencePostClient` (confirmed unreferenced anywhere).
- `Models/ScreenshotEvidenceItem.cs` — added `CustomerId` (int?, required for the
  remediation-tracker target) and `PostedAttachmentId` (marks an item as already
  posted, sourced from `evidence_attachments.id`).
- `Controls/ScreenshotGalleryPanel.xaml[.cs]` — replaced the "Blocked" banner with a
  real Customer ID field + Change Control Execution ID field and a working "Post
  Evidence" button: persists the typed refs, posts the current capture to whichever
  target is filled in (execution id takes precedence if both are set), and shows the
  real result (success + attachment id, or the real error text) — no new tab/panel,
  fits inside the existing gallery panel per UI_RULES.md.

**Verification:** `dotnet build MyArchitect.csproj -c Debug` — 0 errors, 0 warnings.
No BuildConsole/MyArchitect test-manifest requirement (Test Coverage section scopes
manifests to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/` only).
Live UI click-through not run — no WebView2/runTest equivalent exists for a WPF app;
build + code-path review is the real verification available here.

`git status --porcelain` clean; `HEAD` (`81deaa198`) confirmed
`git merge-base --is-ancestor` true against `origin/main` (pushed directly, rebased
once on a non-fast-forward rejection).

No new findings to file — the two real gaps this Feature's own body called out
(#3493, #3503) were both filed and both resolved by the time this session picked
the work back up.

## Re-dispatch session (2026-09-10, buildId 2160)
Duplicate dispatch — full scope already shipped in `bccd5ab2c9bf3eb2e2462771d50baa71ebc4ef46`
(confirmed `git merge-base --is-ancestor` true against `main`). No code changes made. Instead:
- Filed the cross-epic finding this issue's own body called for but never filed: **#3503**
  ("Add evidence/attachment field + endpoint to `remediation_tracker_steps` and change-control
  execution for screenshot posting"), parented under #1571 (Portal Admin), milestone v1.1,
  label `bug`, board status AI Batter Up.
- Wired `blocked_by` edge #3470 → #3503, per the issue body's own instruction.
- Confirmed the pre-existing `blocked_by` edge #3470 → #3493 (UI Shell Redesign) is still
  real/open.
- Labels synced: `in-flight` → `blocked` (both dependencies genuinely open; "Client wiring to
  post evidence" checklist item cannot proceed until #3503 lands, and fuller shell integration
  is pending #3493).
- Posted explanatory comment on #3470 with full detail.
Status left `🛑 BLOCKED` rather than re-claiming `✅ DONE` — the client-post-wiring checklist
item on this Feature is real and still outstanding, gated by #3503.

## What was built (by the earlier session, commit bccd5ab2c)
- Ported full-virtual-screen RegionSelectOverlayWindow and physical-pixel desktop capture DesktopScreenClipService into MyArchitect.
- Dual-sink output: clipboard (CF_BITMAP + PNG stream) and disk in %Pictures%\Screenshots\MyArchitect.
- Local screenshot_evidence JSON cache (ScreenshotEvidenceItem & ScreenshotEvidenceService) persisting metadata (capturedAt, filePath, tenantId/name, activeUrl, caption, step/change refs) to %LocalAppData%\MyArchitect.
- Screenshot review and gallery panel UI (ScreenshotGalleryPanel) integrated in the shell with thumbnail list, image preview, metadata viewer, caption editor, explorer launch, and discard support.
- Hooked up PrintScreen and Ctrl+Shift+S hotkeys, navigation bar ScreenClip button, and Activity Bar gallery toggle.
- Created IEvidencePostClient documenting the cross-epic dependency on Portal Admin #1571 for remote attachment submission.
