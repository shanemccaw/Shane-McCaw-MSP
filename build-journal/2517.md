# #2517 — Orphaned health-suite/assessment-test component cluster

- **Status:** ✅ DONE 2026-09-04T06:12:00Z
- **Scope:** platform

## Log
- 2026-09-04T05:58:50Z — Started. Re-verified issue's own evidence before deleting anything.
  Finding: the issue's claim that `useTopicHealthLive.ts` has zero consumers is **stale** —
  it now has 6 real consumers (`useHltDriftLive.ts`, `useHltObjectsLive.ts`,
  `useSecAreaLinksLive.ts`, `useSecEvidenceEmailLive.ts`, `useSecEvidenceLegacyAuthLive.ts`,
  `useSecEvidenceOauthLive.ts`) importing `resolvedValue`/`resolvedEvents`/`resolvedReason`/
  `resolvedDetail`/`ResolvedMetric` from it. The issue's grep only searched for the literal
  string `useTopicHealthLive` (the hook name), missing that consumers import other named
  exports from the same file. Not deleting that file.
  `assessment-test/types.ts` is genuinely partially dead: `RecommendedOffer`,
  `AssessmentStage`, `PipelineDocumentData`, `AssessmentStageStatus`, `MetricGauge`,
  `TelemetryItem`, `RadarPillarEntry`, `SecurityCoverageData`, `GroupLifecycleData` have zero
  consumers anywhere (confirmed via repo-wide grep). `CopilotReadinessIndicator`,
  `CopilotReadinessLive`, `LicenseWasteSummary` ARE used (imported by `useTopicHealthLive.ts`)
  and are kept.
  Also discovered (new, out of #2517's stated scope): `useTopicHealthLive.ts` itself, plus
  all 6 of its "Live" consumers, form a closed chain with **no page/route consumer** anywhere
  — transitively orphaned. Filing this as its own finding rather than silently expanding
  #2517's deletion scope.
- 2026-09-04T06:12:00Z — ✅ DONE — Removed the confirmed-dead cluster from
  `artifacts/portal/src/components/assessment-test/types.ts` (`RecommendedOffer`,
  `AssessmentStage`, `PipelineDocumentData`, `AssessmentStageStatus`, `MetricGauge`,
  `TelemetryItem`, `RadarPillarEntry`, `SecurityCoverageData`, `GroupLifecycleData`) —
  commit `7877c5a62`. Kept `useTopicHealthLive.ts` and the 3 still-used types
  (`CopilotReadinessIndicator`, `CopilotReadinessLive`, `LicenseWasteSummary`) because the
  issue's own zero-consumers evidence for `useTopicHealthLive.ts` was stale — 6 real
  consumers exist. Filed #2826 (parented to #1485 EPIC: Portal, board status "AI Batter Up")
  for the newly-discovered deeper orphaned chain (that file + its 6 consumers, transitively
  unreachable from any page) rather than deciding build-vs-delete unilaterally.
  Verified: `tsc --noEmit -p artifacts/portal/tsconfig.json` exits 0, no errors.
  `agent/2517-q1529` merged into `origin/main` (verify-branch-merged.mjs: OK).
  `git status --porcelain` clean.
