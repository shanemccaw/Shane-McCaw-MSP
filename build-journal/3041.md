# #3041 — Ownership / RACI: wire the write overlay (5 real write routes)

- **Status:** ✅ DONE 2026-09-06T20:15:05Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** fc98d674c (real work), rebased+pushed as a2ca68457 on `main`

## Log
- 2026-09-06T19:55:42Z ⏳ IN FLIGHT — Read #3041's body + the current
  `docs/ownership-raci-contract-pack.md` (§1b: `WireOwnershipOverlay` and its
  write routes). Confirmed #2527 (escalation-chain gap referenced in the pack)
  is already DONE, unrelated to this build. Confirmed the real Design export
  `Design/portal/design_handoff_full_site/screens/Ownership RACI.dc.html` has
  landed and is the spec.

  **Noted concurrent sibling build:** #3040 ("wire the customer read surface",
  §1a) is dispatched in the same `buildSet=OwnershipRACI` wave, same
  timestamp, and is `in-flight`-labeled right now. Neither `artifacts/portal`
  nor `App.tsx`/`moduleNav.ts` has an Ownership page yet on `origin/main` as of
  this session's start — the read shell and the write overlay both land on the
  same new page file, so this is a genuine same-file race between two sibling
  builds, not a hypothetical one. Since a write overlay makes no sense without
  the page it writes from, building the complete page (read + write) is the
  only way to deliver a real, working feature from this side; will re-check
  `origin/main` before the final push and reconcile/rebase against whatever
  #3040 has landed by then rather than force a duplicate file through.

  Real write surface per the pack (7 POST + 1 GET, all
  `artifacts/api-server/src/routes/portal-ownership.ts`): assign, reorder,
  accept, decline, delegations, delegations/end, rows, events (history read).
  The issue title's "(5 real write routes)" is #1491's original body count
  before reorder/decline were added later — wiring against the CURRENT
  contract pack per the dispatch comment's own instruction, i.e. all of them
  that the Design export actually surfaces a control for.

  Plan: `assign` (cell holder + browse/suggest picker), `accept`/`decline`
  (the "your place on this matrix" panel + per-cell), `rows` (add-a-row panel)
  are all drawn by the Design export — wiring all four to real endpoints.
  `reorder` and `delegations`/`delegations/end` are real, live, tested-by-
  inspection routes with NO control anywhere in this Design export (confirmed
  via grep — no "delegat"/"handover" markup in the .dc.html, and #1491's own
  structured index still lists #1518/#1524 "decide the fate of
  portal_ownership_delegations" as open) — will state that honestly as a
  documented gap rather than inventing UI the design doesn't call for, same
  discipline as `security-plan.tsx`'s and `risk-register.tsx`'s own "what this
  page deliberately does not do" sections.

- 2026-09-06T20:15:05Z ✅ DONE — Built a full first draft (page + API client +
  types + components) before re-checking `origin/main` per plan, and found
  #3040 had landed `✅ DONE` in the interim with a complete, real read-surface
  page under the exact same paths (`pages/ownership.tsx`,
  `lib/ownership-{api,types}.ts`, `components/ownership/*`) — its own ledger
  explicitly said "the five real write routes wire in #3041." Discarded my
  parallel build entirely rather than force a duplicate/conflicting file
  through, rebased clean onto `origin/main`, and instead EXTENDED #3040's
  real landed code with the write overlay:

  - `lib/ownership-api.ts` — added `useAssignOwnership`, `useAcceptOwnership`,
    `useDeclineOwnership`, `useAddOwnershipRow` (kept `useStartDelegation`/
    `useEndDelegation` out — see below), all invalidating the shared
    `["portal","ownership"]` query key on settle so a write always reflects
    the server's own recomputed acceptance/precedence/event-log state.
  - `lib/ownership-matrix.ts` — added `buildCustomRows`: a customer-added row
    (`overlay.rows`, `source: "custom"`) now renders as a real matrix row
    with its own RACI cells (its `rowId` is the `objectId` writes assign
    against), not just a name/sub line — matching the design's own "a row
    you add holds cells exactly like a live one." `source: "coverage"` rows
    stay in `OverlayExtrasCard`'s read-only listing (their name/type are
    genuinely blank in this table — no client-side catalog built to describe
    them, an honest, named limit, not an oversight).
  - `components/ownership/OwnershipMatrixSection.tsx` — added the "add a
    holder" affordance per cell, opening a new `OwnershipPersonPicker.tsx`
    (search + suggestions ranked by who already holds a cell elsewhere on
    the matrix, matching the design's own ranking), wired to `assign`.
  - `components/ownership/MinePlaceCard.tsx` — wired real Accept/Decline
    buttons (was placeholder text "Responding lands with #3041"); decline
    opens a new `DeclineOwnershipDialog.tsx` (reason optional, matching
    #1519's customer-side asymmetry).
  - New `components/ownership/AddOwnershipRowDialog.tsx` — the design's
    "Add a row" panel, wired to the real `POST /portal/ownership/rows`.
  - `components/ownership/OverlayExtrasCard.tsx` — updated its own comment
    now that custom rows render in the matrix; narrowed its listing to
    delegations + coverage-only rows.
  - `pages/ownership.tsx` — wired all of the above together, replaced the
    stale "no write actions yet" ledger bullet with an accurate one covering
    only what's genuinely still unwired (reorder, delegations) and why.

  Deliberately NOT wired: `reorder` and `delegations`/`delegations/end` —
  both real, live routes (confirmed via `psql`-free code read of
  `portal-ownership.ts`), but the Design export
  (`Design/portal/design_handoff_full_site/screens/Ownership RACI.dc.html`)
  draws no control for either anywhere on this screen, precedence carries no
  succession/activation logic to reorder for (contract pack §4), and #1491's
  own structured index still lists #1518/#1524 as an open product decision.
  Stated honestly in the page's own "what this page deliberately does not
  do" ledger rather than invented.

  Verification: `npx tsc -p tsconfig.json --noEmit` clean in
  `artifacts/portal`; `PORT=5183 npx vite build --config vite.config.ts`
  clean (893KB main chunk warning is pre-existing, unrelated to this change).
  No SQL/schema change needed — this build is pure frontend wiring against
  already-real, already-tested backend routes. `git status --porcelain`
  clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (commit `a2ca68457`).

  No new findings filed — the concurrent-build collision with #3040 was
  avoided by discarding and re-basing before push, not a defect that needed
  filing; #1518/#1524 and the coverage-row gap were already on record before
  this session (contract pack §7).
