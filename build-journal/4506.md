# #4506 — Local dev: PORTAL_BASE_URL mismatch breaks every Stripe hosted-Checkout redirect return

- **Status:** ✅ DONE 2026-09-17T14:05:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** c7ddefa33 (IN FLIGHT bookend), 6dbabd4c7 (implementation) — both on `main`

## Log
- 2026-09-17T13:20:00Z ⏳ IN FLIGHT — investigated `getDomainBase()`/`getMspPortalBaseUrl()` in `artifacts/api-server/src/lib/portal-url.ts`; confirmed `.env.local`'s `PORTAL_BASE_URL=http://localhost:3000` is gitignored/per-machine and wins highest priority, so it can't be fixed by editing that file — needs a real code fix. Portal dev port is fixed at 5175 per `scripts/dev-server/services.json` (the real single source of truth), but the actual redirect call sites (`portal-billing.ts`, `portal-add-ons.ts`) all run inside route handlers with a real `req` available. Implementing: when not on Replit (no `REPLIT_DOMAINS`/`REPLIT_DEV_DOMAIN`) and a `req` is passed, `getDomainBase()` now derives the origin from the request's own Origin/Referer header instead of the stale static env value — per the issue's own suggested fallback. Staging/Production path (REPLIT_DOMAINS resolution) is untouched.
- 2026-09-17T14:05:00Z ✅ DONE — Shipped 6dbabd4c7: `getDomainBase(req?)` now prefers the real request Origin/Referer over `PORTAL_BASE_URL`/REPLIT_DOMAINS resolution whenever there's no REPLIT_DOMAINS/REPLIT_DEV_DOMAIN (i.e. genuinely local, non-Replit dev); `getMspPortalBaseUrl(req?)` threads it through; the three real call sites (`portal-billing.ts` return_url + success/cancel_url, `portal-add-ons.ts` success/cancel_url) now pass `req`. `tsc --noEmit` clean, `portal-billing.test.ts` (6 tests) passing. Pushed to `main`, merged into the dev-server checkout via `request-restart.mjs` (verified live at commit c8e25028). Live-verified end-to-end with a REAL Stripe test-mode hosted Checkout: logged in as the #4488 `shanemccaw+customer-addongate@outlook.com` test account (tenant 2191), called `POST /api/portal/add-ons/checkout-session` with `Origin: http://localhost:5175`, drove the real returned `checkout.stripe.com` URL through a headless Chromium (playwright-core) with Stripe's standard `4242 4242 4242 4242` test card, submitted a real subscription, and the real Stripe redirect landed on `http://localhost:5175/portal/change-control?addOnCheckout=success&session_id=cs_test_...` — the actual running portal, not the dead `localhost:3000` from the bug report. Two scratch DB mutations made for this verification (a temporary password reset and a temporary `billing.manage` role grant on the #4488 test account, needed to drive the login+checkout call) were both reverted afterward — the account's password_hash was restored to match the value already on record in BuildConsole's `TestEnvironmentVariables` (`TEST_RBAC_CUSTOMER_ADDONGATE_PASSWORD`), and the added `customer_user_roles` grant was deleted — so the #4488 test fixture is back to its original, designed shape. No new backend/schema needed; no manual migration. No new findings filed — this was a scoped, self-contained fix. Working tree clean (`git status --porcelain` empty), branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
