# #3988 — Focus bar's flat/negative-net-rate ETA reason gets truncated mid-sentence

- **Status:** ✅ DONE 2026-09-13T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** dba3b4196 (rebased/pushed as 8edd8ea92 on origin/main)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — applying fix option 1 (wrap + auto-grow) to FocusModeBar.xaml's EtaText / strip Grid per issue body.
- 2026-09-13T00:30:00Z ✅ DONE — `desktop/BuildConsole/Controls/FocusModeBar.xaml`: main-strip `Grid Height="34"` -> `MinHeight="34"`, `EtaText` gained `TextWrapping="Wrap"` and `MaxWidth` raised 360->560, `TextTrimming` removed (wrapping + no height cap means it never truncates). Short reason/success strings ("~Xd left · Y/day net", "done 🎉", "Calculating…") are all short enough to stay single-line unchanged; the long flat/negative-net-rate sentence now wraps to two lines and renders in full. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/0 errors — did not launch BuildConsole.exe per the never-launch-for-verification rule. Branch verified as ancestor of origin/main via verify-branch-merged.mjs; working tree clean.
