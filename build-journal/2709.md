# #2709 — Home page: remove What To Do Next / Shane To-Do & High Priority / Done Waiting for Review cards

- **Status:** ✅ DONE 2026-09-03T19:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-03T18:33:00Z — Session started. Removing three Home tab cards from `desktop/BuildConsole/Controls/HomeView.xaml(.cs)` per issue's 4-point plan: "WHAT TO DO NEXT (NO OVERWHELM)", "SHANE TO-DO & HIGH PRIORITY", "DONE, WAITING FOR REVIEW" (`DoneHeaderText`). Keeping Recent Chats, Running Now, Quick Actions untouched. Will also clean MainWindow.xaml.cs callers that feed these cards, and note any now-dead underlying logic (e.g. Quick Win picker, #2679 `!issue.IsEpic` recommendation logic) rather than leaving it uncalled.
- 2026-09-03T19:05:00Z — ✅ DONE — Removed all three XAML card blocks (with their bear/cat mascot canvases) from `HomeView.xaml`. Removed dead code-behind: `DoneItemClicked`/`DoneRefreshRequested`/`IssueDetailRequested` events, `RenderDoneWaiting`, the `TodoList`/Shane-To-Do block and the "WHAT TO DO NEXT" quick-win recommendation block (including `_recommendedTask`, `EstimateIssueMinutes`, `FormatEstimate`, `BtnStartNextTask_Click`) inside/around `RenderDashboardState`, and `BtnRefreshDone_Click`. Cleaned up the matching dead wiring in `MainWindow.xaml.cs` (the three event handlers registered in `OpenHomeTab`, the `doneWaiting` list + `RenderDoneWaiting` call in `RefreshHomeRollupAsync`). The #2679-flagged `!issue.IsEpic` quick-win picker logic and the Shane-To-Do list logic have **no other consumer** after this removal — genuinely gone, not orphaned. Kept the open-issue-numbers `gh` fetch + Verifying→Done promotion (`PromoteVerifyingToDoneAsync`) + board reconciliation (`BoardStatusSync.ReconcileQueueAgainstBoardAsync`) in `RefreshHomeRollupAsync` since that's real queue/board reconciliation independent of the removed Done card — just updated its stale comment. Left Recent Chats / Running Now / Quick Actions untouched; column layout reads clean with no dead gaps (verified visually in XAML — StackPanel-based, no fixed-height placeholders left behind). `dotnet build -c Debug`: **0 warnings, 0 errors**. Commit `a90018ef9`, pushed to `origin/main`, verified ancestor via `git merge-base --is-ancestor HEAD origin/main`. `git status --porcelain` clean.
