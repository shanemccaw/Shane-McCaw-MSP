# #4383 — Retainer: move account creation (password/MFA) ahead of optional connect + pay

- **Status:** ✅ DONE 2026-09-17T03:36:30Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 7cfc40497 (work), c18b4b543 (manifest lastVerifiedAgainstCommit), c19cde7ff (IN FLIGHT bookend)
- **Migration:** none
- **Findings filed:** #4476 (payment-intent `alreadyPaid` recovery never fires: status is read from an idempotent create replay, not the live intent; `bug`, AI Batter Up), #4477 (a paid Retainer buyer who never connects is locked to the portal resume stub forever: #4370's pending gate vs. RetainerPending as a permanent end state; `bug`, Ask Shane). Both are sub-issues of #4376.

## Log
- 2026-09-17T03:06:40Z ⏳ IN FLIGHT — blocker #4374 is closed/complete (and #4377/#4378 shipped the account-first stages). Reading Buy.tsx's Retainer path and #4374's provisioning door to confirm whether RetainerPending is accepted as-is, then reordering Retainer to Account → Connect (optional, skippable) → Pay while preserving scanSkipped/connectOffered/RetainerPending.
- 2026-09-17T03:36:30Z ✅ DONE — **#4374's door needed no widening:** `PRE_CONSENT_ACCOUNT_CATEGORIES` already included `retainer`, `pendingRoleForCategory("retainer")` returns RetainerPending, and `provisionProspectAccount`'s consent swap already maps RetainerPending → RetainerConsented. The gap was `ACCOUNT_FIRST_CATEGORIES` (consent gate + resume), which excluded Retainer. **Server:** Retainer added to `ACCOUNT_FIRST_CATEGORIES`, so `/public/flow/read-consent-url` returns 409 account_required until the session's account has a password and MFA. `read-consent-skip` is untouched (needs no consent), so the never-connect path still pays on the recorded skip and ends at RetainerPending. New `POST /api/public/purchase/retainer-selection` (unpaid retainer→public retainer row, in place). The `resume` payload gains `readConsentSkipped`. Payment-confirmed already promoted/entitled account-first sessions generically (RetainerPending and RetainerConsented are not Customer-promotable, the same as the legacy order). **Client:** Buy.tsx `ACCOUNT_FIRST_PRODUCTS` adds retainer: identity → code → password → MFA → buying screen with the optional connect card + Skip → tier & pay → done. The tier is synced onto the kept session before pricing. Step rail: Create account / Connect / Tier & pay / Portal. Sign-in resume maps retainer sessions and restores a recorded skip. #4339's Retainer-only stored-session rehydrate is retired (unreachable once every product is account-first; resume is by sign-in). The portal resume stub maps the retainer category. **Verified:** api-server, shane-mccaw-consulting and portal `tsc --noEmit` exit 0. New `account-first-retainer.live-db.test.ts` 3/3 plus updated `account-first-purchase.live-db.test.ts` 2/2 against local Postgres through the real routers. Regression: consent.test, public-purchase-pre-consent.live-db, public-purchase-account-first-pack.live-db, public-purchase-payment-retainer-entitlement.live-db, public-purchase-payment.test, purchase-account-flow, pendingPurchaseGate (unit + live): 9 files, 191/191. Real browser (Edge via playwright-core; isolated app-only api host from this worktree on :8093 + worktree Vite :5483; real DB; Stripe test mode): 32/33 checks. Covered: arrival on identity with no connect/Skip/pay; consent URL 409 no_account then mfa_not_enrolled then 200 skippable; RetainerPending row with password and no tenant; Skip; tier change → retainer-selection → a $900 Stripe intent on Advisory; test card payment succeeded; a fresh browser signs in and resumes the same session with the skip and tier restored; payment-confirmed; sign-in again lands on done; paid buyer stays RetainerPending/no tenant; client_services row provisioned. The one miss is a script artifact: the portal handoff returned 200, but its body was unreadable after the page navigated away (the body is asserted in the live-db test). Stubbed only: the code email (the code row was inserted directly) and Microsoft's consent screen (the connect path's swap is covered in the live-db test). `shaneapp://runTest buy-page.json onlyTag=4383-account-first`: 14/14 PASS against the dev server, which already serves 7cfc40497 (merged via the AccountFirstCheckout set). The manifest's RET1–RET4 (#1379 buying-screen-on-arrival) were replaced: that screen no longer exists before an account. 0 synthetic zz-test-4383 rows left. `verify-branch-merged` OK. Working tree clean; nothing was dirty on arrival.
