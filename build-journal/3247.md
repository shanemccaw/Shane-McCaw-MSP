# #3247 — Full password vault has no LastPass import path

- **Status:** ✅ DONE 2026-09-08T18:07:51Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 917998d4a (implementation), f950f27a4 (merge into main, pushed)

## Log
- 2026-09-08T18:01:08Z ⏳ IN FLIGHT — adding a dedicated, non-MCP `/api/vault/import` route that
  parses a LastPass CSV export in memory (`vault.importLoginsFromCsv`), maps rows to `createEntry`
  (or `updateEntry` on a re-import match) per row, and never writes the raw upload anywhere; wiring
  an Import card into `viewMoneyVault`.
- 2026-09-08T18:07:51Z ✅ DONE — shipped `vault.importLoginsFromCsv` (RFC4180 CSV tokenizer +
  LastPass column mapping url→site, username→username, password→secret, extra→notes, name→label),
  `POST /api/vault/import` (raw-body upload, same pattern as `/api/media`, never touches `media`/
  `captures`/logs, audit records counts only), and a real "Import from LastPass" card in
  `viewMoneyVault` (`File.text()` → POST, per-row created/updated/error result surfaced). Re-import
  of the same file updates the matching entry by normalised site + username instead of duplicating
  it. Verified against the real local `finances` Postgres DB (agent-verify@example.com test user):
  quoted-comma/escaped-quote/embedded-newline field parsing round-tripped through encrypt+decrypt
  correctly, a row missing `password` reported as a per-row error (not silently dropped), a fully
  blank trailing CSV line skipped without a bogus row, and a re-import with a rotated password
  updated the existing row (no duplicate, `vault` row count unchanged) — all assertions passed,
  scratch rows cleaned up after. Branch verified an ancestor of `origin/main` via
  `verify-branch-merged.mjs`; working tree clean.
