# #2531 — Test Pad port: core services + models

- **Status:** ✅ DONE 2026-09-03T13:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 5050bc3b3 (Test Pad port: core services + models) — pushed to `main`; also
  4c7425606 (IN FLIGHT bookend, superseded by this entry's own commit)

## Log
- 2026-09-03T12:41:11Z ⏳ IN FLIGHT — dispatched. Primary working dir `C:\wt\2531-q1369` was launched
  completely empty (no `.git`) — a recurrence of Git #2118. Recovered manually: reused the leftover
  `agent/2531-q1369` branch (already at `origin/main` tip) via `git worktree add` from the main
  checkout, then registered the worktree + linked deps via `worktree-lifecycle.mjs`/`link-deps.mjs`
  with this session's own `$CLAUDE_PID` as owner (per the known owner-pid-sweep recipe) so it survives
  concurrent sweeps for the rest of the session.
- 2026-09-03T12:41:11Z ⏳ IN FLIGHT — pulled #2531 body + comment, confirmed no supersedes. Audited
  both codebases' dependency surfaces for all 6 target files
  (`TestPadModels.cs`, `TestPadService.cs`, `TestPadFeatureMatcher.cs`, `TestPadImportMerger.cs`,
  `TestPadImportParser.cs`, `TestPadSendFormatter.cs`):
  - `TestPadModels.cs` / `TestPadService.cs`: WPF-only (`BitmapSource`, `Application.Current.Dispatcher`)
    plus two ShaneBuilder-only pure support types (`NoteType`, `NoteContextStamper`) — no BuildConsole
    equivalent exists yet, so those are ported alongside (this port would not compile without them).
  - `TestPadImportParser.cs` / `TestPadImportMerger.cs` / `TestPadSendFormatter.cs`: pure, in-memory —
    depend on the same class of ShaneBuilder-only support types (`NoteMarkerParser`,
    `NotepadImportLineClassifier`, `NotepadImportBulletParser`, `NoteStamp`, `NoteType`), no DB/GitHub/
    logging calls. Ported alongside for the same reason.
  - `TestPadFeatureMatcher.cs` is the one file with a real external dependency: ShaneBuilder's
    `GitIssuesService.StripFeatureTitlePrefix()` (a one-line regex helper) and its `GitIssueRow` data
    shape (only `.Title` is actually read). BuildConsole's real equivalent is `GitHubApiClient`'s
    `GitHubIssueResult` (used by `SearchIssuesAsync`) — same real shape for the field this file
    actually touches (`Title`). Adapting `Match()`'s signature to take `IReadOnlyList<GitHubIssueResult>`
    instead of inventing a parallel `GitIssueRow` type in BuildConsole. `StripFeatureTitlePrefix` has no
    BuildConsole-side home (no `GitIssuesService`/`GitMapService` equivalent there) — kept as a small
    private static helper inside the ported file itself rather than fabricating new shared
    infrastructure beyond what this issue asks for.
  - All 6 target files land together under `desktop/BuildConsole/Services/TestPad/` per the issue body
    (their ShaneBuilder originals are split between `Services/` and `Services/TestPad/`); namespace
    `BuildConsole.Services.TestPad` for all of them and their ported support types, matching the
    existing `BuildConsole.Services.BuildMap` subfolder convention.
- 2026-09-03T13:05:00Z ✅ DONE — ported all 6 target files plus 6 required support types
  (`NoteType.cs`, `NoteStamp.cs`, `NoteContextStamper.cs`, `NoteMarkerParser.cs`,
  `NotepadImportLineClassifier.cs`, `NotepadImportBulletParser.cs`) into
  `desktop/BuildConsole/Services/TestPad/` — 12 files total, namespace `BuildConsole.Services.TestPad`,
  logic and data shapes kept identical to the ShaneBuilder originals. Real plumbing swap:
  `TestPadFeatureMatcher.Match()` now takes `IReadOnlyList<GitHubIssueResult>` (BuildConsole's real
  `GitHubApiClient` issue-row shape) instead of ShaneBuilder's `GitIssueRow`; the one-line
  `StripFeatureTitlePrefix` helper is kept local (no BuildConsole-side `GitIssuesService` to hang it
  off). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` → **0 Warning(s), 0
  Error(s)**. No UI/window wiring in this issue, per scope (that's #2532, blocked_by this issue —
  no action needed there since this issue is what unblocks it, not the reverse).
  `git status --porcelain` clean. Pushed straight to `main` (5050bc3b3) after a real rebase — a
  concurrent session (#2534) landed a commit on `main` mid-session; rebased cleanly, re-verified
  ancestor, re-pushed. Filed a comment on Git #2118 recording this session's recurrence of the
  empty-unprovisioned-worktree bug (own working dir launched with zero files, no `.git`) — did not
  file a new duplicate issue since #2118 is still open and already describes exactly this failure
  shape.
