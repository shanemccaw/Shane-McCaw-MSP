# #3448 — Refresh action reports real, honest sync status via the toast/alert system

- **Status:** ✅ DONE 2026-09-10T02:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 39f085dfd (bookend IN FLIGHT), ff3be6f13 (implementation, rebased onto origin/main)

## Log
- 2026-09-10T02:23:27Z ⏳ IN FLIGHT — Was real-blocked on #3444 (same refresh-completion code path); confirmed #3444's commits (`23e271025`/`e23118365`) are already an ancestor of `origin/main`, so unblocked and proceeding. Read the real closed-sweep/mirror-sync diagnostic already computed by `BatterUpQueueService.SweepClosedCandidatesToDoneAsync` (Git #3347/#2557) — it logs a real moved/already-off-status/deferred breakdown but only to ActivityLog, never surfaced to a toast. Plan: carry that result out of the service layer as a real `ClosedSweepResult` struct instead of a void return, thread it through `RefreshAsync`/`RefreshAndAutoQueueAsync` (BatterUpQueueService) and `RefreshAsync` (AiBatterUpQueueService), expose it as `LastSweepResult` on `BatterUpPanel`/`AiBatterUpPanel`, and have MainWindow's `FullGitRefreshRequested` (the real "existing broad GitHub refresh" #3448 names) build an honest summary via a new `BatterUpQueueService.BuildSyncSummary` helper and hand it to `BuildQueuePanel.LastGitSyncSummary` for `BtnRefreshGitHubTiles_Click`'s completion toast to use instead of the generic "Refreshed Git Board, Batter Up, AI Batter Up, epic issues, and queue!" text.
- 2026-09-10T02:35:00Z ✅ DONE — Implemented exactly that (`ff3be6f13`). `ClosedSweepResult` (TotalCandidates/Moved/AlreadyOffStatus/Deferred/CircuitOpen/Error) added to `BatterUpQueueService.cs`; `SweepClosedCandidatesToDoneAsync` now returns it at every real return point (clean, circuit-open, live-lookup failure, nothing-to-move, batched-move failure, success — cap overflow folded into `Deferred`) instead of `Task` (void), with its existing log lines unchanged. Both `SweepClosedIssuesAsync` private wrappers (BatterUpQueueService + AiBatterUpQueueService) now return it too. `RefreshAsync`/`RefreshAndAutoQueueAsync` (BatterUpQueueService) and `RefreshAsync` (AiBatterUpQueueService) all carry it out in their return tuples. `BatterUpPanel`/`AiBatterUpPanel` expose it as a public `LastSweepResult` property, set on every real refresh pass. New `BatterUpQueueService.BuildSyncSummary(label, result)` turns a real result into Shane's two named example states in spirit — "Batter Up: no issues — every item is current." for the clean case, "Batter Up: was out of sync — N stale item(s) found and cleared." (plus circuit-open/error/deferred variants) for the real-diverged case. `MainWindow.xaml.cs`'s `FullGitRefreshRequested` (the broad GitHub refresh cascade) builds both panels' summaries right after awaiting their real refresh and hands the combined string to `BuildQueuePanel.LastGitSyncSummary`; `BtnRefreshGitHubTiles_Click` now toasts that real string instead of the static "Refreshed Git Board, Batter Up, AI Batter Up, epic issues, and queue!" line, falling back to the old text only if nothing was set. One more caller found and updated for the new tuple shape: `BuildChainMapWindow.Inspector.cs:962`.
  - **Scope decision on #3444's local-only `BtnRefreshQueueLocal_Click`:** deliberately left untouched/toastless. That button's whole point (per #3444's own bookend) is `RefreshAsync(includeGitHubWork: false)` — zero GitHub calls, "safe to click freely" — so it never runs the closed-sweep this issue is about; there is no real Batter Up sync diagnostic available on that path to report. Adding a toast there would be noise on a button explicitly designed to be silent and frequent, not an honest report.
  - Verification: `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. `desktop/BuildConsole` is out of scope for test manifests per CLAUDE.md; no running BuildConsole instance was launched or driven (per the "never launch BuildConsole.exe to verify" rule) — build success + full code read-through of every call site (grepped for all `BatterUpQueueService.RefreshAsync`/`RefreshAndAutoQueueAsync`/`AiBatterUpQueueService.RefreshAsync`/`SweepClosedCandidatesToDoneAsync` callers) is the real verification here.
  - `verify-branch-merged.mjs`: OK — `ff3be6f13` confirmed merged into `origin/main`. `git status --porcelain` clean.
