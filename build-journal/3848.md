# #3848 — Queue header "Active" count undercounts (interactive-only filter)

- **Status:** ✅ DONE 2026-09-12T21:46:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T21:36:59Z — Session started. Real fix: swap `UpdateQueueStatusCounts()`'s
  `active` source in `Controls/BuildQueuePanel.xaml.cs` from
  `_watcher.GetActiveUsageSummary().ActiveBuildCount` (interactive-only) to
  `_watcher.RunningCount` (full `_running.Count`, no filter). `GetActiveUsageSummary()`
  itself left untouched.
- ✅ DONE 2026-09-12T21:46:00Z — Applied the one-line swap + updated doc comment in
  `Controls/BuildQueuePanel.xaml.cs` (`UpdateQueueStatusCounts`). Built
  `desktop/BuildConsole/BuildConsole.csproj` clean (0 errors, 0 warnings). Committed as
  `74285e6b6891e5e1b8db4e1481082031a60bf07c` ("Queue header Active count: use
  RunningCount, not interactive-only ActiveBuildCount (Git #3848)"), rebased onto
  origin/main, pushed. `verify-branch-merged.mjs` confirms merged into origin/main.
  `git status --porcelain` clean.
