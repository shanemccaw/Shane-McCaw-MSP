# #4086 — Re-gate admin-m365-interpretations.ts to requireCapability("ladder.msp-operator")

- **Status:** ✅ DONE 2026-09-14T20:19:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 11dcebc70 (on main)

## Log
- 2026-09-14T20:10:00Z ⏳ IN FLIGHT — replacing `requireAdmin` with `requireCapability("ladder.msp-operator")` on all 12 routes in `artifacts/api-server/src/routes/admin-m365-interpretations.ts`, per Shane's 2026-09-14 decision on this issue (re-gate, don't fork; matches `msp-message-center.ts:24`'s pattern). Code edit + typecheck + `requireCapability-keys.test.ts` done; live-session verification for PlatformAdmin/MSPAdmin/MSPOperator still to run.
- 2026-09-14T20:19:00Z ✅ DONE — all 12 routes re-gated. New live-Postgres regression test `admin-m365-interpretations-rung.live-db.test.ts` proves, against real seeded ladder rows: PlatformAdmin reaches GET/POST create; MSPAdmin reaches GET/POST create (was 403 pre-fix); MSPOperator reaches GET/POST create/POST confirm (was 403 pre-fix); a Customer session is still refused 403. `requireCapability-keys.test.ts` (the catalogue-completeness gate) still passes — 12/12 tests green. Typechecked clean. Pushed to origin/main as 11dcebc70 (after aa1149326, the IN FLIGHT bookend commit). Branch verified merged via `verify-branch-merged.mjs`.
  - Naming flag (not fixed, per dispatch instruction not to rename blind): the file is still named `admin-m365-interpretations.ts` and its routes still live under `/admin/m365/...`, but the gate is no longer admin-only. `artifacts/admin-panel/src/adminv2/screens/m365-changes/m365ChangesStore.ts` is the one real caller today (AdminV2 still uses this path). A rename would be a route-contract change touching that caller and, eventually, whatever MSP Console frontend #1688 builds against this backend — real but separate work, left for a follow-up rather than done blind in this fix.
