# #2494 — Feature: M365 Launch Control (MSP Console) — Phase 3 batch: Users domain + adjacent gap

- **Status:** ✅ DONE 2026-09-13T04:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 254a1b50b (rebased/pushed as 3eec6d64c on main)

## Log
- 2026-09-13T03:52:00Z ⏳ IN FLIGHT — Reconciled live state: `write_action_catalog` is
  123 total / 84 linked / 84 execution_ready (was 85/85 per #2702's 2026-09-03 bookend —
  1-row drift, not investigated further as immaterial); `baseline_action_templates` is
  123 rows / 9 reversible (was 8 per #2703 — consistent, +1 forward progress). 39 catalog
  rows are unlinked (dispatch said ~31 — real drift; templates were added after #2702's
  audit without being retroactively linked, see below), 7 of which are legitimately
  `blocked`/`blocked_no_workaround`. Of the 39, only 4 are in the Users domain (93, 100,
  108, 109) — investigating each individually before authoring anything.
- 2026-09-13T04:20:00Z ✅ DONE — Investigated all 4 Users-domain gaps individually: 93
  (purge deleted user) was genuinely buildable (real Graph `DELETE
  /directory/deletedItems/{userId}`); 100 (add/remove alias), 108 (bulk CSV import), 109
  (update photo) are each blocked by a real architecture gap (Exchange-Online-only write
  path that doesn't exist yet, multi-row orchestration the single-template model can't
  express, and a binary-body requirement `graphWriteForTenant` can't send) — left
  unlinked, not forced. Since Users alone didn't reach a meaningful batch, extended to 2
  more genuinely-buildable new templates (122 Licensing service-plan toggle, 136 CA
  policy state toggle) plus 3 relinks of real `active` templates that already existed
  but were never linked to their obvious catalog row (139/140 CA risk policies, 204
  Defender file detonation) — found via a real anti-join against
  `baseline_action_templates` that isn't in #2702's original audit since these three
  postdate it. Migration
  `lib/db/migrations/manual/2026-09-13-write-action-catalog-users-domain-batch-2494.sql`
  run against local `DATABASE_URL`: 3 `INSERT`s (idempotent, `ON CONFLICT DO NOTHING`),
  6 guarded `UPDATE`s. Verified idempotent (re-run: 0/0/0), verified zero orphaned
  `template_id` via left-join. `write_action_catalog` now 123 total / 90 linked / 90
  execution_ready.

  **Live-tested all 6 promoted rows against the real testbed tenant**
  (`c4c814d4-3afe-441e-9145-62461d0a4fd3`, customer/tenant row id 1, write-back enabled +
  consent granted) via `runBaselineTemplateAgainstTenant()` — not a dry run:
  - `action.purge-deleted-user`: real end-to-end success. Created a disposable
    `zz-test-purge-target-01@mccawsoft2.onmicrosoft.com` identity (reserved `zz-test-*`
    prefix per Git #2840's convention), soft-deleted via the existing
    `action.delete-user` template, confirmed present in `/directory/deletedItems`
    (bounded retry, real Entra replication lag observed — same phenomenon #2840's own
    doc documents), permanently purged via the new template, confirmed genuinely gone
    (bounded retry). No residual state — full lifecycle, nothing left over.
  - `action.toggle-license-service-plan`: real end-to-end success. Assigned the
    real zero-cost `FLOW_FREE` SKU (10000 enabled / 2 consumed at the time — no real
    seat-limit or cost impact) to the reserved `zz-test-graphwrite-01` identity (Git
    #2840) with the `DYN365_CDS_VIRAL` service plan disabled, verified via a real read
    (bounded retry for replication lag), then removed the license again and confirmed
    clean. **Had to `PATCH usageLocation: "US"`** on that reserved identity first — Graph
    refuses any license assignment without one, and it had none. This is now a small,
    permanent, deliberate change to that shared fixture; noted here and in the #2494
    comment so a future session isn't confused by it.
  - `action.create-ca-signin-risk-policy` / `action.create-ca-user-risk-policy`
    (relinked, not authored this session): real 403 "Your tenant is not licensed for
    this feature" — the testbed tenant lacks Entra ID P1/P2, which Conditional Access
    requires. Confirms these templates are correctly wired (reached Graph, got a real
    domain-specific rejection, not a malformed-request error) but couldn't be positively
    verified end-to-end in this tenant. Filed as #3937 (affects every CA-domain action,
    not just these two).
  - `action.submit-file-detonation` (relinked, pre-existing template not authored this
    session): real 403 "Account is not provisioned" — likely a Defender for Endpoint
    provisioning gap, possibly also a genuine endpoint-shape question (`POST
    /security/alerts_v2` with just a comment field creates a new alert rather than
    acting on an existing one). Filed as #3938.
  - `action.set-ca-policy-state` (new, not independently live-fired): couldn't get a
    real `policyId` to toggle since no CA policy could be created in this tenant (same
    Entra P1 gap above) — its request-shape was independently verified correct via
    `resolveBaselineTemplateRequest()` (byte-for-byte expected endpoint/method/body,
    zero missing variables), matching the same substitution path
    `runBaselineTemplateAgainstTenant` uses before firing.

  All scratch test scripts (`artifacts/api-server/scratch-*.mjs`) used for the live
  verification above were deleted before finishing — never committed.

  **Findings filed** (parented as direct sibling sub-issues of #2494 per its own
  Feature-first rule, labeled `bug`, board status "AI Batter Up"):
  - **#3936** — 13 `baseline_action_templates` rows use a fake `exchange-online://`
    scheme with zero executor support (`runBaselineTemplateAgainstTenant` /
    `graphWriteForTenant` only ever build real Graph HTTP requests); at least one
    (`action.convert-user-to-shared-mailbox`, catalog row 188) is already
    `execution_ready` and would silently fail if a technician picked it.
  - **#3937** — Conditional Access actions require Entra ID P1/P2 on the customer
    tenant; no precondition check exists anywhere in Launch Control today.
  - **#3938** — `action.submit-file-detonation`'s real 403, plus the open
    endpoint-shape question above.
  - **#3939** — catalog row 115 ("per-user MFA (legacy)") is marked
    `blocked_no_workaround`, but a real modern-Graph template
    (`action.enforce-per-user-mfa`) already exists unlinked — needs a "disable"
    companion/parameterization before it can be safely promoted; not a mechanical
    relink, real per-action engineering, correctly not attempted in this session.

  Posted the mandatory completion comment on #2494 (also corrects that issue's own
  stale "zero execution-ready" body line — real state has been 84+/123 since #2702/
  #2703 landed on 2026-09-03, now 90/123). Labels synced: #2494 `in-flight` →
  `complete`. Not attempted, correctly out of scope: enabling Entra ID P1 or Defender
  provisioning on the testbed tenant — both are tenant licensing/service-enablement
  actions wrapped under the production-change gate (Git #1913), never a same-session
  agent apply.

  Verified: `node scripts/dev-server/verify-branch-merged.mjs` — `agent/2494-q2569`
  (3eec6d64c) is merged into `origin/main`. `git status --porcelain` clean (this file's
  own edit pending in this same final commit).
