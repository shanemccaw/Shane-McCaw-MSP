# Build Journal — Issue #3787

- **Status:** ✅ DONE 2026-09-12T00:30:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Started. Confirmed route collision: `msp-sales-bundles.ts:143-172` and `msp-diagnostics.ts:261-284` both register `GET /msp/monitoring-packages`; sales-bundles wins (mounted first at routes/index.ts:591 vs :607), diagnostics version is dead code. Fixing by moving the diagnostics-only picker to `GET /msp/monitoring-packages/runnable` and updating `diagnostics-api.ts`'s call site.
- 2026-09-12T00:30:00Z — ✅ DONE — Moved `msp-diagnostics.ts`'s scan-bundle-only picker to `GET /msp/monitoring-packages/runnable`, updated `diagnostics-api.ts`'s `useMonitoringPackages()` call site + doc header, updated `msp-diagnostics.test.ts`'s request path, and corrected the stale cross-reference comment in `sales-api.ts` (its "filed as finding, not fixed" note is now inaccurate — actually fixed here). `msp-sales-bundles.ts` keeps the bare `/msp/monitoring-packages` path unchanged. Verified: `api-server` and `msp-console` both `tsc --noEmit` clean; `msp-diagnostics.test.ts` (11 tests) passes against the new path (one unrelated flaky hook-timeout on first run, passed clean on rerun). Commit `384c9325` rebased and pushed to `origin/main` directly (final SHA on main: `69d73ab3`, per `verify-branch-merged.mjs`). `git status --porcelain` clean at finish. No fixture/hardcoded data introduced. No findings filed — the only gap identified (the collision itself) is exactly what this issue already covers.
