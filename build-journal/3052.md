# #3052 — HttpTestExecutor jsonPath assertions silently no-op on filter syntax and the "equals" key

- **Status:** ✅ DONE 2026-09-12T22:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 16aa547a0 (pushed to main as 94b5ca1a4)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — investigated HttpTestExecutor.cs's TryResolveJsonPath (no `[?(@.field==...)]` filter support) and EvaluateExpectation (only reads "value", never "equals"). Implementing filter-expression support + equals alias, then re-verifying real manifests already using this syntax (ownership.json, settings-change-control-departments-persistence.json, security-overview/alert-volume-drilldown.json), plus checking #3348's ownership.json failure.
- 2026-09-12T22:30:00Z ✅ DONE — Added real `[?(@.field=='literal')]` filter-expression support (single clause or `&&`-joined) to `TryResolveJsonPath`, first-match semantics, plus string/bool/null/number literal comparisons. Fixed `EvaluateExpectation` to honor an `"equals"` key as an alias for `"value"`. `dotnet build` clean (0 warnings/errors). Verified with a reflection-based scratch harness (deleted after use) invoking the internal `EvaluateExpectation` directly against the exact real assertion shapes from `test-manifests/portal/ownership.json`, `settings-change-control-departments-persistence.json`, and `security-overview/alert-volume-drilldown.json` — 8/8 pass, including two negative cases (no match / value mismatch) and a compound `&&` clause. Also ran the real thing: live `shaneapp://runTest?file=ownership.json` — the jsonPath filter for step A3 now resolves correctly (no longer "did not resolve"); the run still reports FAIL overall, but for an unrelated, pre-existing 403 (confirmed via direct SQL against local `DATABASE_URL`: the `ladder.customer-user` RBAC capability mapping still allow-lists the *retired* `CustomerUser` role id from before the #3590 CustomerUser→Customer rename, not the renamed `Customer` id) — filed as its own finding, **#3859**, parented under #1696 (Feature: RBAC Role Model Redesign), board status set to AI Batter Up. `git status --porcelain` clean; branch verified merged into origin/main via `scripts/dev-server/verify-branch-merged.mjs`.
