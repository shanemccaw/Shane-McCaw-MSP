# #1786 — PowerShell command/endpoint fixes — tracking (parallel session)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 9a3e06272 (monitor-executor.ts fan-out + branding-config fix, cmdlet-catalog.ps1
  null-safety fix, DLP date-range margin migration)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Investigating 5 real check errors surfaced by a `core:premier`
  monitoring scan against tenant 1 (real prod M365, testbed): `adoption:planner-usage`,
  `identity:pim-groups` (both fan-out, 0/104 succeeded), `compliance:dlp-incidents`,
  `exchange:mailbox-quota-utilization` (both PowerShell-path, generic
  "resolved cmdlet raised an error" wrapper message), `platform:branding-config`
  (Graph 404, querying tenant GUID as a branding resource ID — looks like a real
  endpoint bug). Digging into PS execution container logs for the two cmdlet
  failures and the actual Graph endpoint for branding-config next.
- 2026-08-29 ✅ DONE — All 5 real, root-caused (not guessed): (1) `identity:pim-groups` —
  monitor-executor.ts fan-out early-exit bailed on a mixed license_gap+transient-failure
  batch, abandoning 100/104 items; now requires the pure signal (`failed === 0`) the
  post-loop throw already used. (2) `platform:branding-config` — added
  `isBrandingNotConfiguredError`, a documented Graph 404 quirk for tenants with no Company
  Branding configured, mirroring the existing Intune #487 precedent. (3)
  `exchange:mailbox-quota-utilization` — real null-ref bug in cmdlet-catalog.ps1
  (`ProhibitSendQuota.ToString() -ne "Unlimited"` string-match was fragile; fixed to use
  `.IsUnlimited` + a null guard before `.ToBytes()`), root-caused via the ca-ps-execution-dev
  container's own Log Analytics trace, deployed live via `shaneapp://deployPsExecution`
  (revision `ca-ps-execution-dev--dev20260830030636`, confirmed via `/healthz`). Also
  granted `Tasks.Read.All` (Application) + admin consent on the MT app registration
  (confirmed live via Graph `appRoleAssignments`) — `adoption:planner-usage` was 403ing on
  100% of groups because this Graph permission was genuinely never granted. (4)
  `compliance:dlp-incidents` — `Export-ActivityExplorerData`'s `StartTime={30DaysAgo}` sat
  exactly at Purview's 30-day ceiling; request/connect latency (~12s observed) pushed it
  fractionally past. Migration backs it to `{29DaysAgo}`, run locally against
  `DATABASE_URL`, not yet applied to Replit/staging (logged on #1630 per the manual-SQL
  convention). Final verification: fresh `core:premier` run (`b0ba76a4`) — 151 checks, 126
  ok, **0 error** (was 5), `identity:pim-groups` now honestly `license_gap` (tenant lacks
  Entra ID P2/Governance) instead of the bogus `error`. `tsc --noEmit`: 0 new errors (107
  pre-existing baseline unchanged). Comment + evidence posted on #1786 via `gh` CLI (MCP
  GitHub tool 403'd on write). `git status --porcelain` clean, `main` verified merged into
  `origin/main` (8b935bfa) before writing this.
