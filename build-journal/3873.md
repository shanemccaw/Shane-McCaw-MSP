# #3873 — Admin Panel (#1095) misclassified as internal tooling in GitBoardIssueFilters

- **Status:** ✅ DONE 2026-09-13T00:55:28Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 33c33c220 (implementation), ec33b2581 (IN FLIGHT bookend)

## Log
- 2026-09-13T00:51:35Z ⏳ IN FLIGHT — removing `1095` from `GitBoardIssueFilters.InternalToolingEpicNumbers` per Shane's confirmed decision on the issue; leaving `1202` in place. Will re-verify Home dashboard burndown/rate/ETA panels and Git Board tree per-Epic rollup pills change.
- 2026-09-13T00:55:28Z ✅ DONE — `GitBoardIssueFilters.InternalToolingEpicNumbers` is now `{ 1202 }` (was `{ 1202, 1095 }`). `CountsAsRealWork`/`IsUnderInternalToolingEpic` no longer exclude issues under #1095's tree, so every consumer that filters through them — `ComputeRealMilestoneCounts` (Focus bar), `GitHubIssueTimeSeriesService.BuildSeries` (Home dashboard burndown/rate/ETA panels), and `ComputeTransitiveLeafRollup` (Git Board tree per-Epic rollup pills) — now includes Admin Panel's real open+closed issues in their counts. Traced through the code (`CountsAsRealWork` → `IsUnderInternalToolingEpic` → set lookup) to confirm the logic change actually propagates to all three named consumers; did not launch BuildConsole.exe to eyeball live numbers, per the standing "never launch BuildConsole.exe to verify" rule — `dotnet build` succeeded clean (0 warnings/0 errors) as the real verification step available here. Also fixed stale doc comments elsewhere in both files that still referenced the old "#1202/#1095" pairing as jointly excluded. No fixture/hardcoded data touched. `git status --porcelain` clean at DONE. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
