# #1404 — Add shaneapp://executeCmdlet (agent-triggered PowerShell checks via the scan system's ps-execution code path)

- **Status:** ✅ DONE
- **Scope:** both
- **Started:** 2026-08-27
- **Commit(s):** `9ad7ce1ba` (bookend IN FLIGHT), `77fcf9ae6` (feature + tests + docs)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Investigating the real shaneapp:// dispatch, ps-execution-client.ts / runPowerShellCheck() code path, cmdlet-catalog allowlist, and #965 testbed gate before adding the executeCmdlet action.
- 2026-08-27 ✅ DONE — Shipped `shaneapp://executeCmdlet` end to end (`77fcf9ae6`):
  - **api-server** `POST /api/simulator/ps-execution/cmdlet` (`admin-engines.ts`, `requireAdminOrIngestToken`) reuses `lib/ps-execution-client.callPsExecution()` — the exact call `monitor-executor.runPowerShellCheck()` makes — so the Dev-vs-Production container selection (#1385 → `ca-ps-execution-dev`), bearer-token/cert handling, and the container cmdlet allowlist are all inherited unchanged. Server-side #965 testbed gate refuses any non-`isTestbed`/inactive tenant and derives the Connect-* `Organization` from the gated tenant's own `tenants.domain` (never an unchecked caller value; `params.Organization` can't override it).
  - **BuildConsole** `executeCmdlet` dispatch + thin HTTP courier (`Services/CmdletExecutionClient.cs`) + client-side #965 `TestbedGate` + settled result envelope + `sql-runner.protocol` logging, mirroring `executeScan`.
  - **Docs** AGENT_PROTOCOLS.md §9 (contract, envelope, and the #1400 `get-connection-info`→`get-cs-online-user` same-replica recipe).
  - **Verification:** api-server `tsc --noEmit` clean (0 errors); `admin-engines.test.ts` **34/34 green** — 12 new route cases (gate refusals, org-mismatch, forced Organization, `PsExecutionError` status mapping) + un-blocked 22 pre-existing tests that crashed at import because the `requireAuth` mock never exposed `requireAdminOrIngestToken` (used by the pre-existing `sql/execute` route), + aligned one stale `sql/execute` assertion with #702's deliberate no-restriction design. BuildConsole WPF builds **0 errors**.
  - **Honest gap (Shane To-Do):** the live #1400 proof (real `get-connection-info` then `get-cs-online-user` against the same warm `ca-ps-execution-dev` replica) could NOT be run this session — the local Postgres was down (`executeSql` returned `Failed to connect to 127.0.0.1:5432`, so the route's testbed-gate DB lookup can't resolve), and the dev api-server isn't yet on this build's code (merge-back is post-build). Once this build is deployed + Postgres is up + `PS_EXECUTION_CONTAINER_URL_DEV`/Key Vault are reachable, run the AGENT_PROTOCOLS §9 two-cmdlet recipe; two OKs closes #1400/#1389.
