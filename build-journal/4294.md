# #4294 — MyArchitect: CSV viewer — evenly-spaced default columns, per-script persisted widths, Raw/Pretty toggle

- **Status:** ✅ DONE 2026-09-16T02:44:52Z
- **Scope:** other (shanemccaw/MyArchitect — extracted repo, not this repo's product/buildconsole split)
- **Started:** 2026-09-16
- **Commit(s):** `shanemccaw/MyArchitect@7272311` (confirmed ancestor of `origin/main`)

## Log
- 2026-09-16T02:36:50Z ⏳ IN FLIGHT — Verified all three blockers via real `blocked_by` edges (`gh api .../4294/dependencies/blocked_by`): #4284 closed/complete, #4305 closed/complete (real replacement for the closed-NOT_PLANNED #4278 — extraction did land), #4292 open but carries the `complete` label (shipped `shanemccaw/MyArchitect@1c7f73b`). Cloned `shanemccaw/MyArchitect` for real investigation — no existing CSV concept anywhere in the app (`grep -ri csv` turns up nothing but an unrelated model field name). Building the CSV grid viewer, per-script column-width persistence, and Raw/Pretty toggle per the issue body, wired into the real Script Editor (#4292) file-open flow since that's the one real place a script's own stable identity (its `.ps1` path) already exists in this app — Script Library's own catalog/template ids exist but that flow returns JSON from a server route, not CSV, so there's no live catalog-action → CSV path yet.
- 2026-09-16T02:44:52Z ✅ DONE — Shipped `shanemccaw/MyArchitect@7272311` ("CSV viewer: evenly-spaced default columns, per-script persisted widths, Raw/Pretty toggle"), rebased onto `origin/main` after a concurrent #4293 (JSON viewer) build landed mid-session, confirmed a real ancestor of `origin/main` post-push. **What shipped:** real `CsvParser` (RFC4180-shaped — quoted fields, embedded commas/newlines, doubled-quote escaping; unit-verified in a scratch console app before cleanup, not just eyeballed), `CsvColumnLayoutPreferenceService` (local JSON at `%LOCALAPPDATA%\MyArchitect\csv-column-layouts.json`, keyed by script identity — never tenant, same convention as `ConsoleHistoryService`/`DarkModePreferenceService`), and `Shell/CsvViewerView` — a real WPF `DataGrid` with Star-weighted (evenly-spaced) default columns, saved-pixel-width override once a layout exists, and a Raw/Pretty toggle-chip pair matching BuildConsole's own `CommandPaletteWindow.RenderSqlDetail` CSV/JSON chip pattern. Wired via a new "View CSV…" button on Script Editor's (#4292) toolbar, keyed to that editor's own currently-loaded `.ps1` path, plus a generic "CSV Viewer" open-intent command on the Console fixed ribbon tab. `UI_RULES.md` §2 updated. **Real constraint found, not a gap I could close in-scope:** Script Library's (#3460/#4291) execute route returns JSON from a server call, not CSV — there is no live catalog-action → CSV output path in this app yet for the issue's "catalog action id / template id" identity to key against, so the identity resolver is forward-compatible (any caller-supplied string) but concretely wired to Script Editor's file identity, the one real script-authoring surface that exists today. **Verification:** `dotnet build MyArchitect.csproj` — 0 warnings/0 errors, both before and after the rebase onto `origin/main`. No live interactive WPF GUI run — same standing constraint every prior MyArchitect session in this file has noted (headless build environment, no display). `git status --porcelain` clean in both repos before finishing.
