# #3321 — As-needed medication dose logging with automatic real timestamp + location

- **Status:** ✅ DONE 2026-09-09T12:54:13Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** 68241e7f6 (rebased onto main as 7df1607bb)

## Log
- 2026-09-09T12:47:14Z ⏳ IN FLIGHT — Investigated real scope (sub-issue of #3226). Confirmed
  real gap: `medications` table has real `batch = 'as needed'` rows (Albuterol, Alprazolam,
  Triamcinolone, Clindamycin, Terbinafine, Ciclopirox) with no dedicated usage-log table.
  `mark_med_batch_taken`/`med_batch_log` is for scheduled batches, not per-dose as-needed
  events. `captures` already carries real geo-tagged latitude/longitude (migration 040). Plan:
  new `medication_usage_log` table (migration 072), `resolveAsNeededMedication` +
  `logMedicationUsage` + `listRecentMedicationUsage` in `src/core/medications.mjs`, a new
  capture-grammar rule (placed last in RULES so it never shadows `queue_take`'s "take X to Y"
  or `pantry_out`'s "used the last of X"), latitude/longitude threaded through
  `runCaptureGrammar`, a new `log_medication_usage` MCP tool, and a simple recent-usage display
  in the Meds room (real timestamp + maps link, no new room).
- 2026-09-09T12:54:13Z ✅ DONE — Shipped in commit 7df1607bb (main), from local
  68241e7f6: migrations/073_medication_usage_log.sql (real table, ran locally
  against the local `finances` DB -- 072 collided with a concurrent sibling
  build's `072_health_metrics.sql`, renamed to the next free number per
  bin/next-migration-number.mjs, re-ran clean); medications.mjs
  (listAsNeededMedications/resolveAsNeededMedication/logMedicationUsage/
  listRecentMedicationUsage, getMedsToday now returns asNeededUsage);
  capture-grammar.mjs's new meds_usage_log rule (checked LAST in RULES so it
  never shadows queue_take's "take X to Y" or pantry_out's "used the last of
  X" -- both share its trigger verbs); latitude/longitude threaded through
  runCaptureGrammar + routes/api.mjs; new log_medication_usage MCP tool
  mirroring the capture-grammar rule; public/app.js Meds room "Recent
  as-needed doses" section. capture-grammar.test.mjs: 5 new unit checks (63/63
  passing). Live-verified end to end against the real local finances DB (real
  user 66ddf602-8103-4c15-bfc6-8914eeeb4e7a, real Albuterol row): the issue's
  own literal acceptance test "I hit my inhaler 2x" via runCaptureGrammar
  produced a real medication_usage_log row (quantity=2, real used_at, real
  lat/lng 28.5721/-80.648), the MCP tool's fuzzy `name` resolution and its
  ambiguity throw ("tablet" -> Alprazolam vs Terbinafine) both verified live,
  and priority-order regressions (queue_take/pantry_out/meds_batch_taken
  still win first) verified live too. All test rows deleted after
  verification -- no residue left in real data. Branch verified merged into
  origin/main (verify-branch-merged.mjs exit 0); working tree clean. No
  findings to file -- no unrelated gaps discovered.
