# #3874

- **Status:** ✅ DONE 2026-09-13T06:32:00Z
- **Scope:** platform

## Log
- 2026-09-13T06:26:52Z — Starting: fix stale OWN8 assertion in test-manifests/portal/ownership.json (pre-#3041 "wires only the read surface" copy was removed by #3041/a2ca68457).
- 2026-09-13T06:32:00Z — ✅ DONE — Confirmed via `git show a2ca68457` + repo-wide grep that "wires only the read surface" no longer exists anywhere on main. Found the real, currently-rendered, always-visible ledger heading ("What this page deliberately does not do", artifacts/portal/src/pages/ownership.tsx:284) and updated OWN8's `textContains` to that stable string. Bumped `lastVerifiedAgainstCommit`. Commits: 2a006806f (fix), 91438ae0d (metadata). Ran the full manifest via `shaneapp://runTest` — it reached and passed the earlier UI steps (login form, submit) but stopped at the login-submit stage because `{{TEST_PORTAL_EMAIL}}`/`{{TEST_PORTAL_PASSWORD}}` are not set in this BuildConsole's Test Environment Variables, so OWN8 itself was never reached live in this run. The fix is grounded in real source inspection (grep + diff), not guesswork, but is NOT live-execution-verified end-to-end due to the missing env vars — an environment/config gap, not a manifest bug. `verify-branch-merged.mjs` confirms merged into origin/main; working tree clean.
