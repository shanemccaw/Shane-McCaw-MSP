# #2282 — "Matrix" chip in the queue toolbar toggles the drawer above the queue list

- **Status:** ✅ DONE 2026-09-03T00:58:49Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02

## Log
- 2026-09-03T00:58:49Z ⏳ IN FLIGHT — Sub-issue of #2281 (Feature: Build Matrix), item 1 of 7.
  BuildConsole launched this build into `C:\wt\2282-q1247` as an **empty, unregistered
  directory** — no `.git`, not in `git worktree list` (Git #2118 recurrence). Recovered per
  the documented playbook: found a stale local branch `agent/2282-q1247` (fully merged
  ancestor of `origin/main`, no unique work), force-deleted it, ran
  `git worktree add "C:/wt/2282-q1247" -b agent/2282-q1247 origin/main`, then registered the
  worktree + linked deps via the dev-server lifecycle functions directly (temp `.mjs`,
  deleted after use, never committed).
- 2026-09-03T00:58:49Z ✅ DONE — Pulled #2282's full body + both comments. The second comment
  (posted 2026-09-03T00:39:12Z by a concurrent #2286 session) already states this issue was
  implemented alongside #2286 in commit `f099320c1` ("Build Matrix: 8-slot drawer +
  click-to-focus (#2286)"), merged to `main` in `b93d68a67`. Verified independently rather than
  taking the comment on faith:
  - `git merge-base --is-ancestor f099320c1 origin/main` → yes.
  - `git merge-base --is-ancestor b93d68a67 origin/main` → yes.
  - `git show --stat f099320c1` commit body explicitly names this issue: "Matrix chip in the
    queue toolbar toggles a real drawer above the queue list (item 1 of the Build Matrix
    feature index, #2282)".
  - Grepped the actual code in this worktree: `desktop/ShaneBuilder/MainWindow.xaml` has
    `BtnMatrixChip` (toolbar chip, line ~2750) wired to `BuildMatrixItemDock`/`MatrixDrawer`
    scaffolding; `MainWindow.xaml.cs` has `BtnMatrixChip_Click` → `RenderMatrixDrawer()`
    (toggles `MatrixDrawer.Visibility` on `_matrixDrawerOpen`), deriving every slot from real
    `_queueItems` rows where `Status == "Running"` — no fixture data, no new DB column.
  - `build-journal/2286.md` (the sibling issue's own bookend, already `DONE`) independently
    confirms `dotnet build ShaneBuilder.csproj` succeeded 0 warnings/0 errors both before and
    after rebase onto `origin/main`, and that `verify-branch-merged.mjs` passed.

  No code changes made in this session — the feature genuinely already exists and is on
  `main`, confirmed by direct inspection, not by trusting the prior comment alone. Nothing to
  file as a finding; no product decision needed.
