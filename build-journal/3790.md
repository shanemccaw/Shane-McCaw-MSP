# #3790 — Archived chats: ghosted inline, not hidden behind "Show Archived"

- **Status:** ✅ DONE 2026-09-12T06:34:49Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 12a1cef2d (rebased/pushed to origin/main as d0f9cd869)

## Log
- 2026-09-12T06:30:19Z ⏳ IN FLIGHT — Located the real control: `desktop/BuildConsole/Controls/LeftSidebar.xaml` (the "Show Archived" CheckBox `ChatShowArchived`) + `LeftSidebar.xaml.cs` (`PopulateChatsTree`'s `showArchivedOnly` filter, `BuildChatCard` renders each per-chat row with the "[#N] Epic..." title + ctx readout). Existing `ctxFull` ghosting in `BuildChatCard` (Opacity, `ChatsPanel.GhostedText`/`GhostedDot`/`BorderSubtle` brushes) is the muted-state visual language to reuse for archived rows. About to: remove the show/hide filter so archived chats always render, ghost them via the same brushes, remove the checkbox.
- 2026-09-12T06:34:49Z ✅ DONE — Removed the `showArchivedOnly` filter from `PopulateChatsTree` (chats no longer drop out for being archived), removed the `ChatShowArchived` CheckBox from `LeftSidebar.xaml` and its now-orphaned `ChatShowArchived_Changed` handler, and updated stale doc-comments/toast text referencing the toggle. `BuildChatCard` now computes `bool ghosted = ctxFull || chat.Archived;` and reuses its existing ctx-full ghosting (Opacity 0.62, `ChatsPanel.GhostedText`/`GhostedDot`/`BorderSubtle` brushes) for archived rows too — no new bespoke visual style. Verified: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 errors (10 pre-existing unrelated warnings). `git diff --stat` scoped to exactly `LeftSidebar.xaml` + `LeftSidebar.xaml.cs`. Rebased onto `origin/main`, pushed directly to `main` (commit `d0f9cd869`), `verify-branch-merged.mjs` confirms ancestor OK. `git status --porcelain` clean. No test manifest — BuildConsole/WPF is explicitly out of scope for test manifests per CLAUDE.md. No backend/DB change needed; this was UI-only.
