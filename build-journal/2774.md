# #2774 — Chat code blocks: shell-command detection + tool-picker dropdown, send to Terminal with multi-line queued execution

- **Status:** ✅ DONE 2026-09-03T23:43:48Z
- **Scope:** buildconsole

## Log

- 2026-09-03T23:33:10Z — Session started in provisioned worktree `C:\wt\2774-q1480` (branch
  `agent/2774-q1480`, clean). Pulled #2774's full body + its single comment (the BUILD dispatch);
  no comment supersedes the body. Read the real precedents named in the audit: `classifyCodeBlock`
  / `buildButtonBar` in `Services/ChatButtonInjector.cs` (sql/prompt split, `BT_LOAD_SQL` postMessage),
  the `BT_LOAD_SQL` handler in `MainWindow.xaml.cs` (~7876, chat-tab resolution via `_chatTabs`), and
  #2769's real diff (`7e2ae2d5b`) — the cached-per-tab `_terminalView` mechanism inside
  `Controls/ChatDocumentContainer` (`OpenTool("terminal", …)` reuses one `TerminalView` per chat tab).
  Confirmed `TerminalView` is a persistent local `powershell.exe` (RedirectStandard{Input,Output,Error}),
  no per-command completion signal today. Implementing the 4-point plan:
  1. `classifyCodeBlock` → new `"shell"` kind via a `SHELL_BLOCK_RE` heuristic (mirrors `SQL_BLOCK_RE`).
  2. `buildButtonBar` → for `kind==="shell"`, a real extensible tool-picker `<select>` ("Select tool ▾")
     listing only really-built tools (currently just Terminal) instead of Send-to-Builder.
  3. New `BT_SEND_TO_TOOL` postMessage → new `MainWindow.xaml.cs` handler (models `BT_LOAD_SQL`
     chat-tab resolution) → new public `ChatDocumentContainer.SendToTool(toolId, text)` that opens the
     terminal rail (reusing `_terminalView`) and hands the text to it.
  4. New reusable `TerminalView.RunQueue(rawText)` — sentinel-gated sequential execution: each line runs,
     and the next only starts once the current command genuinely completes (a per-step `Write-Output`
     sentinel echoed to stdout marks completion + captures `$?`/`$LASTEXITCODE`), with a visible
     per-step queue panel (queued / running / done / failed). Designed as a general TerminalView
     capability, not a one-off.
- 2026-09-03T23:43:48Z — ✅ DONE — All 4 points shipped, `dotnet build BuildConsole.csproj -c Debug`
  clean (0 warnings, 0 errors). Files:
  - `Services/ChatButtonInjector.cs` — added `SHELL_BLOCK_RE` + a 3-way `classifyCodeBlock`
    (sql/shell/prompt), a `SEND_TO_TOOLS` array (only really-built tools; Terminal today), a
    `buildButtonBar` early-return branch rendering a real extensible `<select>` tool-picker for
    `kind==="shell"` (no Send/Edit/Queue/Park), and a `sendToTool` → `BT_SEND_TO_TOOL` post helper.
  - `MainWindow.xaml.cs` — new `BT_SEND_TO_TOOL` handler modeled on `BT_LOAD_SQL`'s chat-tab
    resolution (`_chatTabs` by sender WebView, else selected tab), calling
    `ChatTabState.Container.SendToTool(tool, text)`.
  - `Controls/ChatDocumentContainer.xaml.cs` — new public `SendToTool(toolId, text)`: looks the id up
    in the existing `Tools` tuple table, `OpenTool(...)` (reuses #2769's cached-per-tab `_terminalView`),
    then `_terminalView.RunQueue(text)` for Terminal.
  - `Controls/TerminalView.xaml(.cs)` — new reusable `RunQueue(rawText)`: `SplitCommands` (drops
    blank/`#`-comment lines, joins backtick/backslash continuations), sentinel-gated sequencing
    (`__BT_STEP_DONE__:<token>:$?:$LASTEXITCODE` echoed on the stdin line AFTER each command, so its
    stdout arrival is a genuine per-command completion tick — the next step only starts then), a
    per-step status panel (queued/running/done/failed with live glyph/colour via a
    `TerminalQueueStep : INotifyPropertyChanged`), and a Clear action. Sentinel lines are consumed,
    never echoed to the pane. Relies on PowerShell's confirmed no-echo-of-redirected-stdin behavior
    (the existing view already prints its own `> {cmd}` for exactly that reason), so only the
    variable-expanded result line matches.
  - Verification: clean Debug build + `node`-side unit checks of the embedded injector JS (full
    `vm.compileFunction` parse OK) and the two classifiers against 11 representative blocks — all
    pass, incl. build-dispatch (`--model …`, `BUILD:`) and prose correctly staying `"prompt"`, and
    `pnpm/npx/git/node/az` blocks correctly `"shell"`. Per CLAUDE.md, `desktop/BuildConsole` (WPF) is
    out of scope for test manifests, and BuildConsole.exe must not be launched to drive its UI
    (single-instance mutex could courier into Shane's live session, memory #2156) — so the final
    interactive click-through (open a real chat, pick Terminal on a multi-command block, watch each
    line run in sequence) is left for Shane's own hands-on check; the code paths themselves are
    build-clean and reuse the exact proven `BT_LOAD_SQL`/`_terminalView` patterns.
  - Commit: `35958dc53`, rebased onto and pushed to `origin/main` as `fd9b7114a`
    (`git rev-parse HEAD == origin/main`, ancestor check YES). Working tree clean.
  - No genuine findings/bugs discovered mid-build. No fixture/hardcoded data (WPF wiring only).
