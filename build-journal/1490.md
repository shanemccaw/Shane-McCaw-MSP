# #1490 — Portal New Design: Policy Decisions + Policy Engine (contract pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 98d2687e1 (IN FLIGHT bookend), d5e12127e (contract pack — the real, merged hash on `main`)


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1490`, but #1490 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1490 is complete.
>
> #1490's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Step 3 of #1578 (schema → honest read → **contract pack** → Design →
  wire), scoped to Policy Decisions + Policy Engine per #1577. Read-only: extracting the real
  contract at the policy declares → SOP enacts → check verifies → drift hop, citing every field
  to file:line, marking CURRENT vs DECIDED (with issue numbers), flagging #1525 open rather than
  resolving it, and filing sub-issues at pack time for any real live endpoint this module's pages
  don't call. Output: `docs/policy-decisions-contract-pack.md`. No product code, no schema
  changes, no UI, no `drizzle-kit push`.
- 2026-08-29 ✅ DONE — `docs/policy-decisions-contract-pack.md` written and pushed. Confirmed
  Policy Decisions (#1525–#1529) is real/live (`msp_risk_decisions` via `portal-risk-register.ts`,
  3 consumer pages) while Policy Engine (#1547–#1553) has zero backing anywhere in the codebase —
  verified by grep, not assumed. Verified live against local Postgres: `msp_risk_decisions` holds
  exactly 1 row with a blank `decision_state`, so `GET /portal/policy-decisions` genuinely returns
  `{ decisions: [] }` today — live-genuinely-empty, not a read failure. Verified the real
  `compliance_frameworks`/`compliance_obligations` catalog (5 frameworks, 7 obligations,
  platform-seeded) as real evidence for #1525's flagged-not-resolved question — one of the design's
  four sample obligations (GDPR Art. 5(1)(e)) already exists verbatim in that catalog, the other
  three (ISO 27001, insurance schedule, records schedule) do not. Filed and linked sub-issue #1618
  (`POST /portal/risk-register/:rbdId/accept` is real/live/tested but Policy Decisions' own
  "Sign it off" button doesn't call it) as a real GitHub sub-issue of #1490, per the pack-time
  filing convention. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this line — real hash `d5e12127e`.
  Commented on #1490 and flipped labels `in-flight` → `complete`.
