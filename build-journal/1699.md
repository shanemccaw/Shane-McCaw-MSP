# #1699 — Microsoft Changes: per-wave post cap can drop a post carrying confirmed analysis out of the customer payload

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 7fada72c0 (301054ec4 after rebase onto origin/main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — investigating `capPerWave` / `POSTS_PER_WAVE` in `artifacts/api-server/src/lib/portal-message-center.ts` and `artifacts/api-server/src/routes/portal-message-center.ts`; confirming MC1287370 / interpretation id 4 is evicted from the served payload for mccawsoft2 before making a change.
- 2026-08-29 ✅ DONE — confirmed via direct psql query against local Postgres that interpretation id 4 (status `confirmed`, `MC1287370`, msp_id 1) has a `measured` resolution row (customer_id 1, affected_count 2) for tenant `mccawsoft2.onmicrosoft.com` (tenants.id 1). `capPerWave()` in `portal-message-center.ts` now takes an optional `isPinned` predicate: a pinned (analysis-bearing) post always keeps its slot in its wave, displacing the lowest-scoring un-pinned post rather than growing the wave's budget — except when pinned posts alone in a wave outnumber the budget, in which case every one of them is still kept. The route (`routes/portal-message-center.ts`) now resolves `analysisByMessageId` BEFORE calling `capPerWave` (previously computed after) and passes `(p) => analysisByMessageId.has(p.id)` as the pin predicate. No cap number was raised. Verified against the real DB (customer_id=1, msp_id=1, live corpus): itemCount 487, onAxisCount 475, served 208 both before and after (budget unchanged) — MC1287370 goes from **not served** to **served**. 57/57 tests pass in `portal-message-center.test.ts` (5 new cases added for the pin behaviour); `tsc -p tsconfig.json --noEmit` clean. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0). No schema/migration needed — this is pure derivation logic, no new columns. Posted completion comment + label sync on #1699; no additional findings filed.
