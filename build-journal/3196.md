# #3196 — Money — Bankruptcy tab has real add/edit forms — violates the no-forms design principle

- **Status:** ✅ DONE 2026-09-08T04:06:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 992c187b3

## Log
- 2026-09-08T03:59:41Z — ⏳ IN FLIGHT — Confirmed the violation: `debtForm(d)` in
  `web/shanes-life/public/app.js` builds a real add/edit `<form>` (creditor/balance/type/
  notes inputs, checkboxes, Cancel/Save) for the Bankruptcy tab, per #3163's own bookend.
  Confirmed `set_debt`/`delete_debt` MCP tools (`src/mcp/tools.mjs`) already call
  `money.createDebt`/`money.updateDebt`/`money.deleteDebt` directly — no new backend logic
  needed, matching the issue's stated scope. Plan: remove `debtForm` and the Edit/Delete
  buttons that triggered it (same treatment #3182 gave the Cars tab's own add/delete
  buttons), replace with a static hint pointing at the universal capture box.
- 2026-09-08T04:06:00Z — ✅ DONE — Removed `debtForm(existing)` entirely from
  `web/shanes-life/public/app.js` (creditor/balance/originalBalance/minimumPayment/
  debtType/dueDay/lastPaymentDate/notes inputs + includedInBankruptcy/isCritical/
  isDelinquent checkboxes + Save/Cancel `<form>`), removed the `bankruptcyEditId`
  toggle state and the Edit/Delete buttons in `debtCard()` that drove it (those two
  buttons called the debts REST endpoints directly, bypassing capture the same way
  the form did), and removed the "Add a debt" form card from `viewMoneyBankruptcy()`.
  Each removed spot now shows a static hint pointing at the universal capture box
  (e.g. `Say what changed below, e.g. "<creditor>'s balance is now $X" or "remove the
  <creditor> debt".`), same treatment #3182 gave the Cars tab's own add/delete
  buttons. Confirmed `set_debt`/`delete_debt` (`src/mcp/tools.mjs`) already call
  `money.createDebt`/`money.updateDebt`/`money.deleteDebt` directly — no backend
  change needed, as the issue predicted. `node --check public/app.js` passes; grepped
  for `debtForm`/`bankruptcyEditId` post-edit, zero hits. This app (`web/shanes-life`)
  is standalone (own Postgres DB, not this repo's `DATABASE_URL`) and its
  `bin/check.mjs` explicitly doesn't cover the browser DOM half (per #3181's own
  note) — no `.env` is configured for it in this worktree, so live end-to-end capture
  verification isn't available from this environment. Verification here is static:
  syntax check + reading the real `set_debt`/`delete_debt` handlers confirming they
  call the same real CRUD logic the removed form called.
  Commit: 992c187b3 (pushed directly to `origin/main`, confirmed via
  `verify-branch-merged.mjs`). `git status --porcelain` clean.
