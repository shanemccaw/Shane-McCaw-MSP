# #4295 — MyArchitect: auto-route script output to CSV/JSON viewer based on real detected content type

- **Status:** ✅ DONE 2026-09-16T18:35:30Z
- **Scope:** platform (bookend only) — real code work is in shanemccaw/MyArchitect
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/MyArchitect f008814de2bcc2a9735f2e589561a3d2504735ed

## Log
- 2026-09-16T18:26:36Z ⏳ IN FLIGHT — blockers #4278/#4284/#4292/#4293/#4294 all confirmed CLOSED. Starting: build a real content-type sniffer (JSON parse, then real CSV-shape check) and wire it into the execution result path (#3459 Console output / #3460 RunScriptLibraryAction) so matching output auto-opens the JSON (#4293) or CSV (#4294) viewer instead of plain Console text.
- 2026-09-16T18:35:30Z ✅ DONE — Extended `ConsolePanelView.Append` (Shell/ConsolePanelView.xaml.cs) with a real `IsRealCsv` check (comma delimiter, real header row, consistent column count across every row via #4294's own `CsvParser`) alongside the existing `IsRealJson` sniff — this is the single choke point both the hosted-runspace path (#3459: typed commands, Script Editor's `RunScriptAsync`, queued commands) and the server-side Script Library path (#3460: `RunScriptLibraryActionAsync` → `AppendExternal`) already funnel through, so both auto-route with one change. Added `CsvText`/`ScriptIdentity` bindable dependency properties to `CsvViewerView` (mirroring `JsonViewerView.JsonText`) so it hosts per-entry in the Console's `ItemsControl` DataTemplate with no temp-file round-trip; refactored `Open(filePath, ...)` to share a `LoadDocument` helper with the new in-memory path. `ConsoleOutputEntry` gained `IsCsv`/`Identity`/`IsPlainText`. Per-script column-width persistence (#4294) still works when a real command/script text is known as identity (typed command, script buffer, queued line text) — threaded through the three real-output `Append` call sites; falls back to no persistence (never a synthetic key) for callers with no real identity, e.g. Script Library's status lines. Raw/Pretty toggles on both viewers are untouched — auto-routing only picks which viewer renders the block.
  - **Verification:** `dotnet build MyArchitect.csproj -c Debug` → Build succeeded, 0 Warning(s), 0 Error(s). No test project exists in this WPF app (out of test-manifest scope per CLAUDE.md — that requirement applies to `artifacts/portal`/`artifacts/shane-mccaw-consulting`, not the MyArchitect desktop app) and no live WPF runner was available in this session to click through the Console tab manually — verification here is the clean build plus direct code review of the diff (`git diff --stat`: 4 files, the exact 4 files touched, 126/22 +/-). Stated honestly, not claimed as a live UI run.
  - No genuine findings/bugs discovered mid-build — nothing filed under #3454.
  - Working tree in this platform worktree is clean apart from this bookend file.
