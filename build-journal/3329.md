# #3329 — Capture bar bottom gap + "sticks mid-page" on some rooms -- possible #3264 regression

- **Status:** ✅ DONE 2026-09-09T19:35:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal app, not MSP business work)
- **Started:** 2026-09-09
- **Commit(s):** 4d8dbee45 (IN FLIGHT bookend). No product-code commit -- investigation found no
  reproducible bug; see log below.

## Log
- 2026-09-09T18:49:00Z ⏳ IN FLIGHT — Investigating real, reported capture-bar bottom gap and
  "sticks mid-page" symptoms on `web/shanes-life`. This bookend is being written slightly late
  (after initial code-history investigation had already started, not before) -- noting that
  honestly rather than back-dating it further.
- 2026-09-09T19:35:00Z ✅ DONE — Real, live device-emulation verification, not just static
  analysis (per the issue's own instruction). Method: started the real local server
  (`web/shanes-life`) against the real local `finances` Postgres DB, enrolled a real WebAuthn
  passkey for the existing `agent-verify@example.com` test user via a Chrome DevTools Protocol
  virtual authenticator (same real precedent commit d72445865's own comment names), signed in
  for real, then drove every one of the app's 14 real rooms (Today, Money, Wins, Vault, Meds,
  Tesla, Shopping, Pantry, Recipes, Things, Lists, Dates, Pets, Inbox, People) plus all 5 of
  Money's real internal sub-tabs (the exact original #3264 trigger route) in a real
  402x874 mobile viewport with touch + a real wheel-scroll gesture on each (scrollY up to
  2400px on Money, 1491px on Dates). Also used CDP's `Emulation.setSafeAreaInsetsOverride` to
  set a real 34px safe-area-inset-bottom (the real standard value on every Face-ID iPhone),
  since desktop Chromium otherwise always reports 0 and the gap question can't be tested at
  all without it.

  **Result: neither symptom reproduces anywhere in the current live code.**
  - No horizontal overflow (`document.documentElement.scrollWidth > innerWidth`) on any room or
    Money tab, before or after scrolling.
  - No capture-bar drift: `#capture`'s gap-to-viewport-bottom stayed byte-for-byte constant
    (34px with the safe-area override applied, 0px without it) across every room, every Money
    tab, and every real scroll gesture -- including the exact route (#3264's own Money tabs)
    and every room added since (Pantry #3260s, Vault #3271, Tesla #3271, Wins #3241, Lists
    Round 2, etc). #3264's fix (298bd12c5 + follow-ups d72445865/1595726d5) holds.
  - The "gap": with a real 34px safe-area override applied, `#capture` docks exactly 34px above
    the true viewport bottom on every room -- matching `bottom: var(--safe-bottom)`'s intended
    behavior exactly, not an oversized artifact. **This is expected, correct safe-area
    behavior, not a bug** -- per the issue's own instruction to say so plainly rather than make
    an unnecessary change.
  - Also manually reviewed every ancestor of `#capture` (`html`, `body`, `#app-view`) for the
    same overflow/transform/filter/will-change ancestor-of-fixed-element trap #3264 fixed --
    none currently carry any non-`visible` overflow, transform, or filter; `.view` (a *sibling*
    of `#capture`, not an ancestor) is the only element with `overflow-x: hidden`, exactly as
    #3264 intended. Confirmed there is no `display-mode: standalone` CSS branch either (one
    code path for browser-tab and installed-PWA both, per app.js's own comment at line ~11574),
    so the installed Home Screen PWA Shane actually uses is not a separate untested code path.

  **Honest gap:** this environment is Windows + Chromium (via CDP's real
  `Emulation.setSafeAreaInsetsOverride`, not a guess), not Shane's actual iOS WebKit/Safari
  hardware -- the original #3264 diagnosis was a WebKit-specific containing-block quirk, so a
  live confirmation on Shane's real device remains the authoritative check the issue itself
  asked for. No code-level cause for either symptom was found via live testing *or* via a full
  manual review of the same CSS trap class across every ancestor and every room. No code
  changed. Real cleanup performed: deleted the test passkey credential
  (`webauthn_credentials`) and the scratch `.env`/test script created for this verification,
  restoring `agent-verify@example.com` to its pre-session 0-credential state; stopped the
  server process started for the test.
