# #1169 — Free Monitoring tier — full scan, hard-gated findings-only lead funnel

- **Status:** ✅ DONE 2026-09-04T22:55:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** df11cb0a6 (merged to main d72a5adb6..df11cb0a6)

## Log
- 2026-09-04T22:45:00Z ⏳ IN FLIGHT — Pulled #1169 body + comments. Investigating real infra to reuse
  (isServiceFree/portal-checkout-free.ts/landing_page_only, per issue). Discovered #1169 substantially
  overlaps Epic #1352 "Feature: Free Scan (Marketing)" — Phases 1/2/3/5 are DONE, Phases 4/6/7/8
  (#1356/#1358/#1359/#1360) are open and cover almost the same scope, more specifically (e.g. #1358
  names the exact real endpoint to read). Rather than rebuild in parallel under #1128, proceeding by
  completing the concrete backend gap #1169 actually asks for — the check-depth decision Shane made
  2026-08-21 (full depth, all checks/pillars except PowerShell) was never wired: the existing Free
  Scan product (`license-waste-audit-free`) already gets a REAL scan fired at consent time (the
  generic consent.ts trigger, not gated to monitoring-only) but against the wrong 3-check
  `assess:license-cost-optimization` package. Fixing the packageKey + visibility/isPublic gating is
  the real, well-scoped #1169 backend deliverable; the results-UI/return-link/convert-handoff pieces
  stay with #1352's own phases per their more specific specs, to avoid a duplicate parallel build.
- 2026-09-04T22:55:00Z ✅ DONE — Migration
  `lib/db/migrations/manual/2026-09-04-free-scan-full-depth-package.sql` run
  against local DATABASE_URL: created `monitoring_packages` row
  `core:free-scan-full` (core:premier's 198 real checks minus its 38
  PowerShell-executed checks = 160: 158 graph, 1 dns, 1 sharepoint-admin) and
  repointed the Free Scan product (`services.slug='license-waste-audit-free'`,
  id=15) at it via `type_attributes.packageKey`. Also fixed a real gap found in
  the same pass: `GET /api/catalog/assessments` filters on `is_public`, not
  `visibility` — this row's `is_public=true` meant it was being served on the
  general public assessments catalog despite being the intended
  landing-page-only funnel product. Set `visibility='landing_page_only'` AND
  `is_public=false`; renamed the row "Free 360° Tenant Scan" to match what it
  actually runs and what the marketing /scan page's own copy already promises.
  Updated the locked assertion in `free-scan-prospect.test.ts` to match (it was
  asserting the buggy `isPublic=true` as correct) — ran 3/3 passing against the
  real local DB. No new scan-trigger code was needed: `routes/consent.ts`'s
  existing consent-callback `runDiagnostics` call already fires generically for
  any consented checkout-session product keyed off
  `services.type_attributes->>'packageKey'`, so fixing the packageKey alone
  makes the Free Scan fire a real, full-depth scan at consent time.
  Working tree clean, branch merged to main (df11cb0a6, verified via
  verify-branch-merged.mjs). Posted comments cross-referencing the #1352
  epic-overlap (Phases 4/6/7/8 = #1356/#1358/#1359/#1360) on #1169 and #1356.
  Labels synced (in-flight → complete).
