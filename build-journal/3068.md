# #3068 — dev-server selftest: cross-process concurrency Scenarios 3/9 intermittently fail (residual after #3055 env-isolation fix)

- **Status:** ⏳ IN FLIGHT 2026-09-10T22:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** TBD

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — read the issue + all prior comments, read
  selftest.mjs / request-restart.mjs / coordinator.mjs / queue.mjs / lock.mjs /
  git.mjs / config.mjs. Working hypothesis: `git.mjs` runs every git operation via
  synchronous `execFileSync`, and the coordinator mutex's heartbeat
  (`lock.mjs` `setInterval`) shares the SAME event loop as the lock holder's own
  synchronous merge/restart work. Under a large batch (N merges done sequentially
  in one `runCycle`), that synchronous work can block the holder's own heartbeat
  timer for longer than `staleLockMs`, so a concurrently-waiting process can
  perceive the still-alive, still-working holder as dead, break the lock, and start
  a SECOND concurrent runner against the same worktree — a genuine two-writer race,
  not an env leak. About to instrument Scenarios 3/9 to capture per-child
  stdout/stderr + lock/queue state at the moment of any non-landing result, and run
  repeated iterations to try to reproduce.
