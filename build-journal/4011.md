# #4011 — Activity Timeline: wire MSP Console UI to the real endpoint

- **Status:** ✅ DONE 2026-09-14T04:38:00Z
- **Scope:** platform

## Log

- 2026-09-14T04:23:51Z — ⏳ IN FLIGHT — wiring `Design/MSP_Console/design_handoff_msp_console/Activity Timeline.dc.html`
  into `artifacts/msp-console/src` under Operations against `GET /api/msp/timeline`.
- 2026-09-14T04:38:00Z — ✅ DONE — Wired the design into `artifacts/msp-console/src`
  under Operations (`ops/timeline`, nav id already present in `console/nav.ts`).
  No reconciliation needed per the dispatch — the design was already confirmed
  accurate against `GET /api/msp/timeline`
  (`artifacts/api-server/src/routes/msp-customer-timeline.ts`).
  - New `src/api/timeline-api.ts` — `useTimeline(customerId)` wraps the endpoint
    with `useInfiniteQuery` (cursor pagination via the route's own `nextCursor`/
    `before`).
  - New `src/console/modules/ActivityTimeline.tsx` — customer chips (from the
    console's own live directory, re-queries the server per selection since the
    real endpoint supports `customerId=` natively), type chips (client-side
    filter over loaded pages, matching the design), event rail with detail
    drawer, load-more, error/empty states. No fixture data — every row is the
    real response shape (`CrossTenantTimelineEventDto`).
  - Wired the `sel.kind === "msp" && sel.page === "timeline"` branch in
    `console/ConsoleShell.tsx`, passing the shell's existing `customers` list
    and `navigate` to open a tenant from the drawer.
  - Deliberate divergence from the mock: the design's client-side "staffScope"
    prop and hardcoded scoped-id list are dropped entirely — the real
    `resolveStaffScopedCustomerIds` scoping happens server-side, so the
    component renders exactly what the endpoint returns.
  - Verified: `tsc --noEmit` clean, `vite build` succeeds (both re-checked
    after rebasing onto a concurrent `ConsoleShell.tsx` change from another
    session).
  - `verify-branch-merged.mjs`: OK, `agent/4011-q2691` merged into `origin/main`.
    `git status --porcelain`: clean.
  - Commits: 78d6008be4b5bcacb02035804cf8e90fb8403e7 (implementation),
    5fa37d12429a70be48f45c4c3f33549e4b4b3a4 (IN FLIGHT bookend),
    merged to `origin/main` at 188af6ca5.
