# #2824 — engine-determinism.test.ts: 3 real pre-existing score-mismatch failures

- **Status:** ✅ DONE 2026-09-04T15:35:00Z
- **Scope:** platform

## Log
- 2026-09-04T15:20:12Z — Session start. Investigating health-engine under-count (expected
  19, got 13) and hasFlagA securityImpact bump not affecting score (expected delta 7, got
  0) in `src/lib/engine-determinism.test.ts`. Will trace `computeHealthEngine` in
  `src/lib/health-engine.ts` to find root cause before touching any assertions.
- 2026-09-04T15:35:00Z — ✅ DONE — Finding: **no code change needed, report was stale.**
  #2824 was filed 2026-09-04T05:48:09Z citing 3 failing assertions in
  `src/lib/engine-determinism.test.ts` with expected values 19/50/7. Those exact
  expectations had already been corrected by commit `70e89493a` ("api-server tests: fix
  remaining 26 stale-fixture failures (#1772)"), pushed 2026-09-04T04:57:02-04:00 —
  **before** #2824 was even opened. That commit's own message says explicitly:
  "engine-determinism.test.ts: securityImpact was already correctly excluded from
  computeHealthEngine's sum by dc9e8b7dc; this suite was never updated to match." I.e.
  the real root cause traces to an intentional, documented product decision made back
  in `dc9e8b7dc` (Jul 19 2026): security is scored by a standalone Security Engine and
  combined one level up in `calculateArchitectureHealthScore`, not summed inside
  `computeHealthEngine` itself. #2824's own worked example (governance+security+
  compliance+adoption=19) was based on the pre-`dc9e8b7dc` formula and was already
  stale at the moment it was filed.
  Verification: ran
  `pnpm exec vitest run src/lib/engine-determinism.test.ts` (with `DATABASE_URL` loaded
  from `.env.local`, real local Postgres) — **16/16 tests pass**, including all 3 the
  issue cited: health engine `first.score === 13` (not 19), msp roll-up
  `combinedScore === 44` (not 50), and the sensitivity test asserting the securityImpact
  bump changes the health score by exactly `0` (not 7) since security is intentionally
  out of scope for that engine.
  No engine code touched, no test assertions touched — working tree was already clean.
  Branch `agent/2824-q1603` HEAD (`065911a26`) confirmed ancestor of `origin/main`
  (pushed directly, no divergence).
  This is NOT a genuine new finding under #2491 — it's the same finding #1772/70e89493a
  already made and fixed; filing a duplicate would be wrong. Commented on #2824 with the
  above and recommending it be closed as already resolved (Shane's call, not mine).
