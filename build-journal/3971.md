# #3971 — RBAC: add RetainerNoConsent + RetainerConsented msp_role values, update users_role_scope_check

- **Status:** ✅ DONE 2026-09-14T02:08:27Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** fa3b636fc (rebased to 7cc72792a on origin/main)

## Log
- 2026-09-14T01:40:07Z ⏳ IN FLIGHT — Adding `RetainerNoConsent` + `RetainerConsented` to `LEGACY_ROLE_ORDER` (lib/db/src/rbac/legacy-ladder.ts), keeping the total `Record<LegacyRole,…>` maps (`LADDER_CAPABILITY_KEYS`, `LADDER`, `LEGACY_ROLE`, api-server `ROLE_LABELS`) exhaustive, and updating `users_role_scope_check` in schema + a new manual migration following #3608's NOT VALID → verify → VALIDATE pattern.
- 2026-09-14T02:08:27Z ✅ DONE — Shipped in 7cc72792a. Both enum values are real `LegacyRole` members (inserted between `Free` and `Customer`, preserving every existing rung's relative order → no existing `legacyRequireRole` comparison changes; neither Retainer rung outranks a paid `Customer`). Exhaustive `Record<LegacyRole,…>` maps updated: `LADDER_CAPABILITY_KEYS` (`ladder.retainer-no-consent` / `ladder.retainer-consented`), `LADDER`, `LEGACY_ROLE`, and api-server `ROLE_LABELS`. Migration `lib/db/migrations/manual/2026-09-13-users-role-scope-check-retainer-roles-3971.sql` DROP+re-add of the constraint (NOT VALID → verify 0 bad rows → VALIDATE) — **run against local DB, `convalidated = t`, 0 violations**. Verification: `tsc --noEmit` clean for both `lib/db` and `artifacts/api-server`; `legacy-ladder.test.ts` 37/37 pass (updated the `marketplace.browse-full` rung-list assertion for the two new non-`Free` rungs); DB-backed `customer-admin-billing-roles` + `user-role-sync` pass with DATABASE_URL sourced.
  - **Finding filed:** #3987 (bug, parented to #3970, board = AI Batter Up) — pre-existing failing test `admin.test.ts:211` couples to hardcoded `CUSTOMER_USER = 39`, which does not exist in the current local seed (customer users start at id 2756+). Unrelated to this change; my commit touches none of `admin.ts`/`resolveUserOrgId` and deletes no rows.
