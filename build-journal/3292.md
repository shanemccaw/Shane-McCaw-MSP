# #3292 — Deterministic instant capture grammar (Shane's Life), Ctrl+K precedent

- **Status:** ✅ DONE 2026-09-09T01:56:04Z
- **Scope:** platform (web/shanes-life -- Shane's personal app, Epic #3086, Feature #3220)
- **Started:** 2026-09-09
- **Commit(s):** `9a30fbfbe` (rebased + pushed onto `origin/main`; originally `b25e90e51` on
  `agent/3292-q2019`)

## Log
- 2026-09-09T01:42:16Z ⏳ IN FLIGHT — Read #3292 in full, read BuildConsole's real Ctrl+K
  precedent (`desktop/ShaneBuilder/docs/Command Center (Ctrl+K).md`), read `web/shanes-life`'s
  own design README's canonical "Capture grammar" section (lines 112-123, plus Tesla/Money
  grammar at 52/65/136) and the full real MCP tool surface (`src/mcp/tools.mjs`, 2304 lines) to
  compile an exhaustive, priority-ordered rule set (~29 real deterministic rules) mapped to real
  core functions. Confirmed no schema change is needed (local `finances` DB already at migration
  065, matching this worktree's files). Building `src/core/capture-grammar.mjs`
  (classify + execute, pure-parseable + DB-backed), wiring it into `POST /api/captures` before
  the pending-inbox insert, adding the real dedicated `get_car_charge_status` MCP tool +
  `tesla.getChargeStateOrWaking()` honest wake-state helper (Tesla 408 gap, issue item 6), and
  updating the generic capture bar in `public/app.js` to show a real instant confirmation instead
  of always "Got it." Real unit tests for the pure classifier in
  `src/core/capture-grammar.test.mjs`.
- 2026-09-09T01:56:04Z ✅ DONE — `src/core/capture-grammar.mjs` shipped: 29 real,
  priority-ordered rules (person notes, meds, Tesla warm/trunk/heading-home/commute/reads, money
  what-if/give/put/move/smoke, wins, pet vaccines, birthdays/visits/events/appointments, who-fixed-
  what contacts, vehicle add + maintenance log, medication-ordered, watch/read lists, "where's X",
  thing-location, aisle memory, price logging, grocery add). Wired into `POST /api/captures`
  (`src/routes/api.mjs`) before any pending-capture row is written; `public/app.js`'s generic
  capture bar shows each line's real result. Also closed issue item 6 (Tesla charge-status gap):
  `tesla.mjs`'s `fleetGetRaw` now tags a sleeping-vehicle HTTP 408 with `code: "VEHICLE_ASLEEP"`,
  new `getChargeStateOrWaking()` turns that into an honest waking-car message, new MCP tool
  `get_car_charge_status` uses it. 36/36 real, DB-free unit tests pass
  (`npm run selftest-capture-grammar`) covering classification + every parsing helper. Execution
  paths verified LIVE against the real local `finances` database with disposable accounts created
  and deleted in the same run (both `runCaptureGrammar` in isolation and the full server boot +
  `POST /api/captures` over real HTTP) — real cases confirmed working: "Drill is at home" ->
  Things, "Add Tesla" -> Cars (both cited live-failing examples from the issue, now instant),
  "watch the new Bond movie" -> Watch list, "grab milk, eggs and bread" -> Shopping run, "plumber
  is Ray 321-555-0142" -> contact, "I did it, ..." -> Wins, "smoked" -> smoke log, "pepper rabies
  due february 2027" -> pet vaccine, "did an oil change on the Kia, $45" -> maintenance log,
  "pasta aisle 12 end cap" -> aisle memory, "chicken breasts are $3.49 now" -> price log, "dr
  appointment oct 3rd 2pm dr fonji every 6 weeks" -> Dr. Fonji appointment 2026-10-03 14:00,
  "mom's coming to visit the 12th-18th" -> visit, "want to go to the air show nov 7" -> event, and
  a genuinely ambiguous capture correctly still falls through to the pending inbox untouched.
  "warm it up" against an unconfigured Tesla correctly returns the real, honest "not configured"
  message rather than losing the capture or crashing. No schema change needed (local `finances` DB
  already at migration 065, matching this worktree). One real finding filed and parented under
  Feature #3220 (not built here -- a genuine product decision, per the money/entitlement carve-out
  and this app's own `no AI inference of its own` boundary): #3307, no standalone timer capability
  for the design's own capture-grammar item 3 ("8 min timer for pasta"). Branch rebased cleanly
  onto `origin/main` (no file overlap with concurrent #3295/#3296/#3303 work) and pushed directly;
  `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean; two scratch smoke-test
  scripts and a local `.env` used for verification were deleted before finishing, along with one
  disposable test-account row a `| head -3` pipe had cut short of its own cleanup (found and
  removed by hand, confirmed zero residual `zz-*smoketest*` rows remain).
