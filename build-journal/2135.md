# #2135 — New isolated Build Queue redesign preview window

- **Status:** ✅ DONE 2026-09-01T14:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 4ea7f313cf6c27e8465e1cb48c8a9adbabfa11d4

## Log
- 2026-09-01T14:00:00Z ⏳ IN FLIGHT — Building a genuinely isolated WPF window (BuildQueueDesignWindow) that renders a redesign scaffold of the Build Queue from real data (same source as BuildQueuePanel/BuildQueueMapWindow), toggled from the icon bar. Sub-issue of #1788 (Epic: Build Console — Build Queue Panel).
- 2026-09-01T14:35:00Z ✅ DONE — Shipped `BuildQueueDesignWindow.xaml`/`.xaml.cs` (new, genuinely isolated floaty window) + ActivityBar 🎨 toggle button/event + MainWindow `ToggleBuildQueueDesign()`. Reads REAL live queue via `BuildQueuePostgresClient.GetQueueAsync()` (same source every panel uses) — no fixture data. Renders collapsible Build-Set groups (per-group color coding from the #2126 accent/status palette; Ungrouped bucket last), dense status cards (RUNNING/QUEUED/BLOCKED/VERIFYING/FAILED/CAPPED/LIMIT-PAUSED/PARKED), 2-line-ellipsis titles, meta line (model·effort·cli·account·pid), and dimmed blocker ghost cards resolving each declared blocker to its real title/state from the snapshot. Isolation guarantee: every style/DataTemplate is local to this window; the ONLY thing shared with the app is the #2126 `Colors.xaml`/`Typography.xaml` TOKEN dictionary (pure Color/Brush/Double, no styles) via DynamicResource — editing a card style here cannot leak into the running BuildQueuePanel. Verification: `dotnet build BuildConsole.csproj` → Build succeeded, 0 warnings, 0 errors. WPF app, so no test manifest (out of scope per CLAUDE.md). **v1 tradeoffs (noted per issue):** (1) READ-ONLY — no actions/drag; the real panel keeps owning all queue mutations. (2) "Epic color coding" is realized as per-Build-Set color (QueueItem has no epic field, and the contract pack leaves epic→color to designer discretion; Build Set is the real grouping key), cycled over 5 #2126 tokens. (3) Blocker liveness is the declared-blocker + snapshot cross-reference view, not the live `gh` open/closed check the Build Queue Map (#2110) does — external (non-queued) blockers are shown still-holding (fail-closed), never silently cleared.
