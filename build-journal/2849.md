# #2849 — notifications: ~10 routes bypass createNotification(), skip preference/email/webhook gating

- **Status:** ✅ DONE 2026-09-05T00:03:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 89ed3e9f819add6640e6ce8df31ef42e48785375

## Log
- 2026-09-04T23:33:33Z ⏳ IN FLIGHT — migrating ~10 direct `db.insert(notificationsTable)` call sites listed in #2849 to route through `createNotification()` in `artifacts/api-server/src/lib/notification-center.ts`.
- 2026-09-05T00:03:00Z ✅ DONE — migrated all 11 direct-insert call sites across the 10 files named in #2849 (`routes/quiz.ts`, `routes/portal-messages.ts` x2, `routes/portal-checkout-free.ts`, `routes/leads.ts`, `routes/admin-status-reports.ts` x3, `routes/admin-reports.ts`, `routes/admin-projects.ts`, `routes/admin-documents.ts`, `routes/admin-callback-tokens.ts`, `lib/direct-tenant-provisioning.ts`) to `createNotification()` / `createNotificationForAllAdmins()`, each with a real category drawn from the existing `CATEGORY_STYLES` vocabulary (`lead`, `message`, `onboarding`, `project`, `ai`, `system`). Admin-broadcast sites (all-admins map+insert) now use the existing `createNotificationForAllAdmins()` helper (same pattern already used elsewhere in this file, which itself targets admin users via `recipient: {type: "customer_user", userId}` — no new recipient type invented). `tsc --noEmit` on `artifacts/api-server` passes clean (one real type error caught and fixed along the way: `admin-projects.ts` passed `body: ... ?? null` where `CreateNotificationOptions.body` is `string | undefined`).
  - Filed two real findings as sub-issues of #2742 (not closed by me): #2932 (more direct-insert call sites beyond #2849's named list — `seed-portal.ts`, `workflow-executor.ts` x5, `admin-insights.ts`, `admin-invoices.ts`, `admin-services.ts` x3) and #2933 (a genuine double-email risk: `portal-messages.ts` and `admin-status-reports.ts` already send their own templated email in addition to the notification insert, and `createNotification()`'s `deliverPreferenceEmail` will ALSO fire once a customer opts into email for that category — currently masked because the default preference is `emailEnabled: false`).
  - Verified branch merged into `origin/main` via `verify-branch-merged.mjs` before writing DONE. `git status --porcelain` clean.
