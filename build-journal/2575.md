# #2575 — Git panel: checkbox multi-select on Changes + Stage Selected + one-click Commit & Push

- **Status:** ✅ DONE 2026-09-03T16:25:13Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** cc9590f03

## Log
- 2026-09-03T16:18:49Z ⏳ IN FLIGHT — starting: add IsSelected to GitItem, per-row checkbox on CHANGES section, Stage Selected + Commit & Push buttons in Controls/LeftSidebar.xaml(.cs), reusing RunGitCommand/BtnGitStageAll_Click/BtnGitCommit_Click/push patterns per issue body.
- 2026-09-03T16:25:13Z ✅ DONE — implemented all 5 points: (1) `GitItem.IsSelected`; (2) checkbox added to unstaged-only rows in `CreateGitFileTreeItem`, wired to a persistent `_selectedUnstagedPaths` set; (3) "Stage Selected" button (`BtnGitStageSelected_Click`) runs `git add "<path>"` per checked file via existing `RunGitCommand`, disabled until ≥1 checked; (4) "Commit & Push" button (`BtnGitCommitAndPush_Click`) chains per-file add → extracted `DoGitCommitAsync` (shared with the existing Commit button, no new commit mechanism) → `git push`; (5) selection restored by `RelativePath` match on every `RefreshGitStatusAsync`, pruned for files no longer unstaged. `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Did not touch AddToGitignore, commit-graph collapse, or Stage All, per the issue's explicit out-of-scope list. Real commit: cc9590f03 (verify-branch-merged.mjs confirmed ancestor of origin/main). No findings filed — no genuine bug/gap surfaced outside the scoped work.
