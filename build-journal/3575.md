# Build Journal — Git #3575

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:25:56Z
- **Scope:** buildconsole

## Log

- 2026-09-11T00:25:56Z — Removed `BtnBoardReconcile` button + `BtnBoardReconcile_Click` handler
  from `Controls/BuildQueuePanel.xaml` / `.xaml.cs`, and deleted
  `desktop/BuildConsole/StaleStateReconcileWindow.cs` entirely (confirmed orphaned via grep —
  only reference was the button's own click handler). `FalseDoneReconciler.cs` and its automatic
  wiring in `MainWindow.xaml.cs` were left untouched, per the issue's own instruction. `dotnet
  build -c Debug` succeeded with 0 errors after the removal.
