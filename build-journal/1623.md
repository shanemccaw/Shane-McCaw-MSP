# Build Journal — #1623

- **Status:** ✅ DONE 2026-09-03T02:52:00Z
- **Scope:** platform

## Log

- 2026-09-03T02:31:38Z — Starting: drop `invoices.amount_old_numeric` retired decimal column
  (survives-one-release column from #1610's numeric→cents migration). Confirmed locally: 0 rows
  in local `invoices` table, 0 mismatches between `amount_old_numeric*100` and `amount`. No app
  code references `amount_old_numeric`/`amountOldNumeric` outside the schema comment and the
  #1610 migration file itself. Proceeding to drop the column via a new manual migration + Drizzle
  schema edit.
- 2026-09-03T02:52:00Z — ✅ DONE — Ran
  `lib/db/migrations/manual/2026-09-03-drop-invoices-amount-old-numeric-1623.sql` against local
  `DATABASE_URL` (`ALTER TABLE invoices DROP COLUMN IF EXISTS amount_old_numeric`, self-marking
  INSERT into `simulator_migration_runs`); confirmed via `\d invoices` the column is gone.
  Updated `lib/db/src/schema/index.ts` comment to reflect the column's removal (no Drizzle field
  existed for it to remove — it was only ever a raw SQL rename, never modeled in the schema).
  Rebased onto `origin/main` and pushed directly (`git push origin HEAD:main`) since this worktree
  can't check out `main` locally (already checked out in the primary checkout). Merged commit:
  `59bfe361b`. `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
