# #1762 — scheduled_for is free text, not a timestamp (Change Control)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `73f6f6e52` (work), `e54eab64a` (bookend IN FLIGHT) — both on `origin/main`
- **Migration:** `lib/db/migrations/manual/2026-08-29-cr-scheduled-instant-1762.sql` (run against local Postgres in-session; logged to #1630)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Adding additive `scheduled_start`/`scheduled_end` timestamptz to msp_change_requests; populating on the three write paths; extending freeze enforcement to the booked window; date-ordering buildStats; wiring the contract. No backfill by parsing — existing rows get NULL.
- 2026-08-29 ✅ DONE — Shipped in `73f6f6e52`:
  - **Schema** (`lib/db/src/schema/msp.ts`): added nullable `scheduled_start`/`scheduled_end` (timestamptz) alongside the unchanged free-text `scheduled_for` label. Migration written + run against local Postgres; `\d msp_change_requests` confirms both columns, both nullable. No backfill — 1 existing row, 0 with a non-null `scheduled_start` after the build.
  - **Three write paths populated:** `POST /portal/change-control` and `POST /api/msp/change-requests` accept optional ISO-8601 `scheduledStart`/`scheduledEnd` (end-after-start validated); `m365-change-router.ts` populates `scheduled_start` from `structuralDate(announcement)` — the one honest parse, from Microsoft's own structured date. `scheduled_end` stays null there; NULL where no dueDate.
  - **Freeze enforcement extended to the booked window:** new pure `freezeOverlapsSpan` + `findFreezeForBookedWindow` in `portal-change-freeze.ts`, store `freezeForBookedWindow`. Both create routes now block on either an active-now freeze OR a booked window overlapping a freeze, gated on the same `enabled && enforceFreezeCalendar` pair. When `scheduled_start` is NULL the booked-window check does not fire and the portal 409 body carries `bookedWindowEvaluated:false` so the caller knows the window was not evaluated.
  - **buildStats** date-orders the "next window" when real instants exist (earliest upcoming start), falls back to the prior string-grouping when none do, and reports `nextWindowDateOrdered` so a string-grouped result is never presented as date-ordered.
  - **Wire contract:** `scheduledStart`/`scheduledEnd` added to `WireChangeRequest` alongside `window`. Ends at the wire contract — no page/route added under `artifacts/portal` (no `.dc.html` design exists).
  - **Verification:** `portal-change-freeze.test.ts` extended (+11 cases for the new fns), 22/22 pass. api-server `tsc` clean in all touched files (0 errors); the 47 remaining tsc errors are pre-existing unrelated files (ai-billing, ai-cost-capture, ai-usage-*, sow-claim-binding, admin-build-tracker, msp-diagnostics — the #1765/#1772 class), not mine. Branch merged into `origin/main` (ancestor check passed). Working tree clean.
  - **Left as-is (noted, not fixed):** #1763 (MSP console approval-ledger materialization) — out of scope, untouched. The `portal-change-catalog.ts` and `portal-runbooks.ts` CR-insert paths correctly leave `scheduled_start` NULL (no real instant to record); intended, not a gap.
