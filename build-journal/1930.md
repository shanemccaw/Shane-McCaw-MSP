# #1930 — Git issue detail document: show labels + board status + Send to Batter Up

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 005947d29 (feature work, branch agent/1930-q1017), merged to origin/main as f67fb046e

## Log
- 2026-08-30 ⏳ IN FLIGHT — starting: read GitDetailView.cs + GitHubApiClient.cs, then add labels display, board status fetch/display, and Send to Batter Up action.
- 2026-08-30 ✅ DONE — Added `Labels` to `GitIssue` (LeftSidebar.xaml.cs) and populated it at
  every construction site (board bucket map, milestone-cache map, search result, live
  GetIssueAsync fetch) from the already-fetched `GitBoardIssue`/`GitHubIssueResult`/
  `GitHubIssueDetail` label lists — no invented data, all real GitHub labels. GitDetailView's
  LoadIssue now renders those as small chips (peach for in-flight/Shane To-Do, green for
  complete, red for blocked, neutral otherwise). Added `GitHubApiClient.GetIssueBoardStatusAsync`
  — a real GraphQL `issue(number:).projectItems.fieldValueByName(name:"Status")` read (same
  project id `PVT_kwHOEiBDdc4BeoiY`/field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0` everything else in
  this file already uses) returning the option's real display name plus the ProjectV2Item id,
  fetched in the background alongside comments/blocker. Detail view shows "Board status: <real
  name>" and a "→ Send to Batter Up" button (hidden if already there) that calls the EXISTING
  `SetProjectItemStatusAsync(itemId, GitHubApiClient.BatterUpPromoteOptionId)` mutation (Git
  #1710's `updateProjectV2ItemFieldValue`, unmodified — reused verbatim) and updates the
  displayed status in place on success.
  - Verified the new GraphQL query shape directly against the real board via `gh api graphql`
    for issue #1930 itself: returned `{"id":"PVTI_lAHOEiBDdc4BeoiYzg4mZHo","project":{"id":"PVT_kwHOEiBDdc4BeoiY"},"fieldValueByName":{"optionId":"09b1927f","name":"Batter Up"}}`
    — confirms the query resolves the right project, the right item id, and the real status
    name/optionId (matching `BatterUpPromoteOptionId`, so the button correctly hides for an
    issue already at Batter Up).
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: **Build succeeded, 0
    Warning(s), 0 Error(s)**.
  - No test manifest — desktop/BuildConsole is explicitly out of scope for test manifests.
  - Pushed `agent/1930-q1017`, then merged into `origin/main` (several retries as main moved
    under concurrent load) as commit `f67fb046e`. `verify-branch-merged.mjs` confirms merged.
    `git status --porcelain` clean.
