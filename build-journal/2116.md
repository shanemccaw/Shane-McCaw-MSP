# #2116 — Fix broken `pnpm typecheck` on main

- **Status:** ✅ DONE 2026-09-01T18:03:27Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** 4c08e4c0f (bookend), 403387b93 (fix)

## Log
- 2026-09-01T18:01:55Z ⏳ IN FLIGHT — re-ran `pnpm typecheck` against fresh main; confirmed the compliance_frameworks/policy_decisions/risk_instances cluster and the missing @workspace/db exports the issue originally listed are already fixed on main. Also hit #2117 (worktree lib/*/dist junctions pointing at the main checkout) — repointed node_modules/@workspace/db and rebuilt lib/db, lib/api-zod, lib/integrations-anthropic-ai, lib/integrations-openai-ai-server, lib/dashboard-registry dist from this worktree's own source before trusting the error list. Real remaining/new error set (61 → 0): admin-build-tracker.ts (Express 5 req.params typing, ghMsList/ghData unknown, eq() overload), generated_secret_store_unavailable missing from two ERROR_STATUS Records, msp-diagnostics.test.ts weak-type/index-signature gaps, msp-rbd-instances.ts readonly-tuple cast, onedrive/sharepoint-sharing.test.ts missing armOperation field (new monitor_checks column, #1871), ai_usage_events.eventId is now a uuid string not a serial int (ai-billing.ts + lib/integrations-anthropic-ai's AiUsagePersistResult/AiCallCost + every test fixture that hardcoded a numeric eventId), a vitest 4 vi.fn generic-signature break in ai-usage-sink.test.ts, wf_definitions insert in generated-secret-store.live-verify.ts assuming a status column it never had, SecurityPlanContent.prose now a structured SecurityPlanProse object not a plain string, and a couple of real TS inference gotchas (reduce() over a `match() ?? []` union, an over-narrowed `.catch()` type, a `.push()` used as an implicit sink return, an `as never`-cast fake SDK client) in sow-claim-binding-560.test. ts / ai-usage-metering.test.ts / ai-cost-capture.test.ts.
- 2026-09-01T18:03:27Z ✅ DONE — `pnpm typecheck` (from `artifacts/api-server`) is clean, 0 errors, exit code 0. Rebased onto origin/main (which had landed #2117's real tooling fix — `link-deps.mjs` now builds `lib/*/dist` from the worktree's own source automatically — while this session was in flight; my manual junction workaround didn't conflict with it) and pushed. `verify-branch-merged.mjs` confirms `agent/2116-q1106` (403387b9) is an ancestor of `origin/main`. `git status --porcelain` clean. No manual SQL migration was needed — every fix was a type/test correction, not a schema change.
