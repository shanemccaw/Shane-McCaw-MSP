# #4043 — Consent callback: admin add-client invites (no customerId/mspId) skip the cross-MSP tenant conflict guard

- **Status:** ✅ DONE 2026-09-15T01:25:38Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 427f48405 (fix + tests)

## Log
- 2026-09-15T01:08:43Z ⏳ IN FLIGHT — set `expectedMspId` to the isDirectBusiness MSP for invite tokens carrying neither `customer_id` nor `msp_id` in `GET /api/consent/callback`, so they go through the same cross-MSP tenant conflict guard. Audit platform-admin `/consent/invite-link` callers for tenantId-only re-consent of another MSP's customer first. Live-verify the leak path pre-fix and the refusal post-fix, then clean up.
- 2026-09-15T01:25:38Z ✅ DONE — `GET /api/consent/callback`: invite tokens with no customer_id/msp_id now set expectedMspId to the isDirectBusiness MSP and go through the single cross-MSP guard; the direct create path re-checks the resolved row's mspId and fails closed (race / no direct MSP). Admin audit: both platform-admin invite-link callers (ActiveDirectoryCustomerPane.tsx:737, AdCustomerCanvas.tsx:248) already send customerId; the generated client has no consumers; 0 no-id tokens in the DB. Verified: consent.test.ts 66/66 (4 new), api-server tsc clean. Live on an isolated :8099 instance with synthetic zz-test rows: pre-fix, a no-id token against MSP 1626's tenant → success, graph+sharepoint granted, user 3314 created (leak reproduced); post-fix → tenant-conflict, consent {}, no user, token unburned. All synthetic rows deleted, residue 0. Finding filed: #4197 (bug+security, under #1650), consent granted from unsigned query params without Microsoft confirmation. No migrations. Scope: platform (api-server only).
