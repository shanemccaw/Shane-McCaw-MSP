# #4015 — Break-glass admin-override reads redacted breakGlassAccountId

- **Status:** ✅ DONE 2026-09-14T12:35:55Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 068934bc1 (fix); 34c8c98b6 (IN FLIGHT bookend)
- **Migration:** `lib/db/migrations/manual/4015-break-glass-pending-secret-account-id.sql` — run on local Postgres in-session (ALTER, backfill UPDATE 0, marker row); listed on #1630 for Replit/staging
- **Findings filed:** #4029 (bug + security, sub-issue of #1651), #4030 (bug, sub-issue of #1651)

## Log
- 2026-09-14T08:19:45Z ⏳ IN FLIGHT — investigating gate stamp → redactForPersistence → performBreakGlassAdminOverride round trip; deciding where the account identity persists (queue build 2700, Opus/High)
- 2026-09-14T12:35:55Z ✅ DONE — Reproduced live before the fix: after the gate, wf_runs.payload breakGlassAccountId/breakGlassUserId/principalId were all "[redacted]" and the real override returned 502 (PATCH /users/%5Bredacted%5D). Also found resume broken the same way: quickstart-v1.assign-global-admin-role posts {{breakGlassUserId}} as principalId. Decision: removed breakGlassAccountId from SENSITIVE_PAYLOAD_KEYS (an Entra object id is an identifier, not a credential; the create step's own output already held it unredacted; credentials still redacted by key and value) AND added break_glass_pending_secrets.break_glass_account_id (gate writes at insert, override reads it and carries it onto the replacement row). Contract packs §2.6/§4 + MSP console pack + portal page comment updated. Verification: new unit test 2/2, related config-pack/executor tests 128/128, api-server tsc exit 0; live harness break-glass-admin-override.live-verify.ts 3/3 against zz-test-graphwrite-01 with the generated-secret store deliberately unconfigured (local fallback is the production Key Vault — not written): payload + column carry the real object id, override reached the post-reset 503, lastPasswordChangeDateTime moved on a real Graph read, pending secret untouched; all synthetic rows removed (0 left). Not exercised live: the store/supersede/re-issue success tail (needs a non-production vault), so the carry-forward of break_glass_account_id onto the replacement row is code-verified only. Pre-existing tree was clean.
