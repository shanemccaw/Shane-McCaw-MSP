# #4793 — Decouple Verifying-row visibility from mirror-sync; gate Done on airplane-driven close

- **Status:** ✅ DONE 2026-09-18T22:27:22Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** cea257069 (code + migration); bookends 84e6c3ef6 (IN FLIGHT)

## Log
- 2026-09-18 ⏳ IN FLIGHT — auditing BuildQueuePanel airplane handler + every PromoteVerifyingToDoneAsync call site (incl. #4740's SyncCompleted hook) before code changes.
- 2026-09-18T22:27:22Z ✅ DONE — Audit: automatic Verifying→Done ran from 5 places (LeftSidebar mirror-sync #4740, LeftSidebar Git Board fetch, MainWindow Home reconcile, BuildWatchWindow, QueueWatcherService #3900 event-driven) all via PromoteVerifyingToDoneAsync/PromoteSpecificToDoneAsync, plus the board-Done arm of ReconcileQueueAgainstBoardAsync. Fix: new `bt_build_queue.review_requested_at`, stamped by BuildQueuePostgresClient.MarkReviewRequestedAsync on a SUCCESSFUL ✈ landed-send (both the per-set and epic-aggregate handlers in BuildQueuePanel; the ❓ needs-attention send does not stamp). The shared GetVerifyingCandidatesAsync, PromoteSpecificToDoneAsync and the board-Done arm now require it, so every path is gated at one choke point; #4740's detection/reconcile-without-Git-Board is untouched. MarkCompleteAsync clears the flag on (re-)entering Verifying. Panel shows "issue closed, awaiting review" (from bt_issue_mirror state, local reads only) on closed-but-unreviewed rows. Migration `2026-09-18-bt-build-queue-review-requested-4793.sql` ran by me against local BuildConsole DB (column confirmed via information_schema) and self-marked in the product DB; appended to #1630. Verification: `dotnet build` clean (0 warn / 0 err); the exact gating SQL run against the live BuildConsole DB in a rolled-back txn — real Verifying row #785 (GH #1489): 0 candidates before the flag, 1 after a simulated airplane stamp, 0 after rollback. NOT verified: the WPF UI/live mirror-sync cycle end to end (did not launch BuildConsole, per project rule); the issue's real-build-through-a-sync-cycle check is left for Shane. Findings filed: none. Known consequence of the spec: a Verifying row whose issue is closed outside the ✈ flow stays Verifying (flagged "awaiting review") until ✈ is sent or it is dismissed via the stale-state cleanup window.
