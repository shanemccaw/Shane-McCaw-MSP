# #2506 — Diagnostics: script-package to monitor_check link never wired

- **Status:** ✅ DONE 2026-09-04T06:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 0793e0d7b (rebased to c93fc2517 on origin/main)

## Log
- 2026-09-04T05:49:28Z ⏳ IN FLIGHT — investigated: `monitor_checks.script_package_id` column + Drizzle field + FK
  already exist (migration `2026-07-19-monitor-checks-script-package-link.sql` already ran locally). Real gap is
  narrower than the issue title suggests: `PATCH /api/admin/monitor-checks/:key` (admin-monitor-checks.ts:194-195)
  has an `allowedFields` whitelist that omits `scriptPackageId`, and the admin-panel MonitorChecks.tsx editor has
  no UI control to pick a script package. `GET /api/admin/ps-scripts/packages` already exists and returns real
  script packages to populate a picker. Plan: add `scriptPackageId` to the PATCH whitelist, add a `Select` in the
  admin-panel check editor sourced from that endpoint, wire it to `PATCH`.
- 2026-09-04T06:05:00Z ✅ DONE — commit c93fc2517 on origin/main. Shipped:
  1. `admin-monitor-checks.ts` — `scriptPackageId` added to the PATCH `allowedFields` whitelist and to the POST
     create payload; empty string on PATCH normalizes to `null` (unassign).
  2. `admin-panel/src/pages/MonitorChecks.tsx` — new "Script package" `Select` in the check editor, shown when
     "Requires customer script" is on, populated from the already-real `GET /api/admin/ps-scripts/packages`;
     inline amber warning when no package is assigned yet (download route would 404).
  No schema/migration changes needed — `monitor_checks.script_package_id` (column, Drizzle field, FK, index)
  already existed from `2026-07-19-monitor-checks-script-package-link.sql`; the only real gap was the missing
  write path.
  Verification: `artifacts/api-server` `tsc --noEmit` clean (0 errors). `artifacts/admin-panel` `tsc --noEmit`
  has exactly 1 pre-existing, unrelated error in `BuildTrackerExplorer.tsx` (confirmed via `git log` on that
  file — not touched this session); `MonitorChecks.tsx` itself is clean. Live end-to-end proof: ran a
  transactional SQL harness against local Postgres (`DATABASE_URL`) that inserts a script package + module,
  a `monitor_checks` row, performs the exact `UPDATE ... SET script_package_id = ...` the new PATCH route
  performs, then runs the exact resolution `SELECT` the download route (`msp-diagnostics.ts:699-769`) uses —
  confirmed it resolves `key -> script_package_id -> module filename` correctly; rolled back (no data left).
  No `shaneapp://runTest` UI manifest — this is an admin-panel-only page, not `msp-portal`/marketing, so no
  test-manifest requirement applies. `git status --porcelain` clean before finishing.
  Branch verified ancestor of `main` via `verify-branch-merged.mjs`.
