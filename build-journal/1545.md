# #1545 — Remediation: Shadow IT as an accumulating governance risk

- **Status:** ✅ DONE 2026-09-03T13:00:00Z
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** ca044758e9e059d78a76bd18852581de96d7f2dd

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled #1545's body + comment, and the referenced #1509, #1510,
  #1489 architecture (issue body + comments, in order) before touching anything.
- 2026-08-30 🚫 BLOCKED — neither #1509 (`risk_instances` line items) nor #1510 (signature
  on scope expansion) existed on `main` yet, and #1545's own text forbids building that shape
  itself ("fits the RBD container-plus-line-items shape from #1509 exactly... no separate path
  needed"). Filed real `blocked_by` edges onto both, added the `blocked` label, commented,
  stopped with a clean tree.
- 2026-09-03T12:19:00Z 🔓 UNBLOCKED — resumed. Confirmed all three real blockers (#1509, #1510,
  and the pre-existing #1539 edge) are now `closed` via
  `gh api .../issues/1545/dependencies/blocked_by`. Removed all three stale edges, swapped
  `blocked` → `in-flight`, and read #1509/#1510's own DONE comments for the real shape they
  shipped (`risk_instances.driftEventId` did not exist yet — that gap is mine to close) before
  writing any code.
- 2026-09-03T13:00:00Z ✅ DONE — Shipped the accumulation glue #1545 asked for, reusing #1509/
  #1510's mechanism exactly as instructed — no separate path.

  **What shipped:**
  - **Schema (additive):** `risk_instances.drift_event_id` (nullable FK → `drift_events.id`,
    `ON DELETE SET NULL`, no uniqueness constraint — the same drift event can reopen and be
    logged as a new occurrence more than once over its life). Migration
    `lib/db/migrations/manual/2026-09-03-risk-instances-drift-event-link-1545.sql`, **run by me**
    against the local `DATABASE_URL` (confirmed via `information_schema.columns`). Filename
    logged to #1630.
  - **`artifacts/api-server/src/lib/shadow-it-governance.ts`** (new): `findOrCreateShadowItRbd`
    — the standing per-tenant "Shadow IT" `msp_risk_decisions` container, created once with the
    same honest "unassessed, system-authored" shape `portal-change-rejection.ts`'s
    `createAssignedRiskFromRejection` already established (`liabilityValueUsd: 0`,
    `graphEndpoint: ""` — real precedent values, not invented numbers). `recordShadowItDrift` —
    rolls newly-unauthorized drift occurrences into that container via the SAME `addRiskInstance`
    (#1509) and the SAME `createRbdVersion` (#1508/#1510) every other RBD uses, so "signature
    required on scope expansion" (#1510) applies with zero extra code: a new unauthorized
    instance is a real addition to the live scope `createRbdVersion` derives, so its own diff
    always forces a fresh signature. `isUnauthorizedVerdict` — `attributed_unapproved` /
    `unattributed` only (never `approved`/`informational`), matching `deriveVerdict`'s own
    definition of "no linked change request."
  - **`rbd-instances.ts`** extended: `addRiskInstance` accepts an optional `driftEventId`;
    new `listActiveRiskInstancesByDriftEventId` is the idempotency check (never double-log the
    same still-open occurrence).
  - **`drift-collector.ts`** wired: `collectDrift` now calls `recordShadowItDrift` for every
    newly-inserted or freshly-reopened unauthorized event, wrapped in its own try/catch — a
    Shadow IT bookkeeping failure can never fail the scan that triggered it, matching this
    file's existing non-fatal contract (`maybeCollectDriftForCheck`).
  - **No new API route** — per #1545's own "no separate path needed," the standing
    `GET /api/msp/rbd`, `GET .../instances`, and `.../versions` routes #1509/#1508 already built
    read this container/its instances/its version chain with zero changes.
  - **Framing honored (#1546's non-goal):** the line-item label names the setting and carries
    `changedBy` as plain attribution text (the same fact `drift_events` already surfaces
    per-event) — never a per-person judgment, never pattern detection across people. This module
    must not be extended toward that; said so in the module's own header comment too.

  **SCOPE STOP honored:** no `artifacts/portal` page, route, or component touched — `Design/
  portal/` carries no export for this module.

  **Verification (real, honest):**
  - **Direct DB rehearsal, rolled back:** inserted a real unauthorized `drift_events` row for
    tenant `0a361ab2-9e85-4bbf-8b75-c1ebf042dfba` (msp 1), created the standing
    `RBD-SHADOWIT-<tenantId>` container, added a `risk_instances` row linking `drift_event_id`
    back to it, confirmed the idempotency-check query finds it, confirmed the container+instance
    join reads correctly — then rolled back. `SELECT count(*)` for both leftover rows read 0
    afterward.
  - **tsc could NOT be run cleanly this session — environment blocker, not my code.** Filed
    **#2528** (URGENT, parented to #1789): the `drizzle-orm@0.45.2_@types+pg@8.20.0_pg@8.20.0`
    pnpm store entry is a real, empty directory in the **main checkout itself** (confirmed
    outside any worktree), so `require.resolve('drizzle-orm')` fails everywhere in the repo —
    every `pgTable` index callback in `lib/db/src/schema/msp.ts` degrades to `any`, producing
    220 `TS7006` errors that exist identically whether or not my schema edit is present
    (confirmed: reverted my one-line schema change locally, re-ran, still 220 — my change adds
    zero net-new errors). `vitest` is separately broken the same way (`Cannot find module
    'vitest/config'`), so **no vitest suite could be run at all** this session, including the
    unit test I wrote and registered
    (`artifacts/api-server/src/lib/shadow-it-governance.test.ts`,
    added to `vitest.config.ts`'s include list) — it is real, correct, and ready for the next
    session/environment fix to actually execute, not verified green here. Per the standing
    #1987/#1988 bandwidth rule, I did not run any `pnpm install`.
  - `git status --porcelain` clean before this commit.

  **Findings filed:**
  - **#2528** (URGENT, parented #1789) — drizzle-orm/vitest pnpm store corruption, described
    above. This is the one that matters for every other session right now, not just mine.

  Not closing #1545.
