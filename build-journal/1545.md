# #1545 — Remediation: Shadow IT as an accumulating governance risk

- **Status:** 🚫 BLOCKED
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** none — no product code changed

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled #1545's body + comment, and the referenced #1509, #1510,
  #1489 architecture (issue body + comments, in order) before touching anything.
- 2026-08-30 🚫 BLOCKED — #1545's own text is explicit: "Fits the RBD container-plus-line-items
  shape from #1509 exactly ... **No separate path needed.**" and "Signature on scope expansion
  applies (#1510)". Confirmed by direct search that neither exists yet on `main`:
  `risk_instances` (or any `riskInstance`/`RiskInstance` naming) does not exist anywhere in
  `lib/db/src/schema/msp.ts` or `artifacts/api-server/src`, and there is no `build-journal/1509.md`
  or `build-journal/1510.md`. Both #1509 and #1510 are themselves still labeled `in-flight` on
  GitHub (another session/build owns each), not `complete`.
  Building the container-plus-line-items model myself under #1545 would duplicate #1509's exact
  deliverable under the wrong issue — the same anti-pattern the leaf-issue dispatch rules in
  CLAUDE.md warn against — and #1545's own instruction ("no separate path needed") forbids it
  outright. There is nothing in #1545's scope that can be built independently of that shape:
  the existing `msp_risk_decisions` container has nowhere for shadow-IT instances to attach
  without `risk_instances`.
  Filed the real GitHub "blocked by" dependency (not just a comment) against both #1509 and
  #1510, added the `blocked` label, removed `in-flight`, and commented on #1545 explaining the
  wait. No code touched; nothing to revert.
