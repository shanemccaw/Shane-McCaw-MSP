# #4413 — check-rbac-integrity hardcoded row ids

- **Status:** ✅ DONE 2026-09-16T18:58:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-16T18:41:36Z — Started. Resolving MSP/OTHER_MSP/TENANT/OTHER_TENANT/MSP_USER/CUSTOMER_USER
    live in `lib/db/src/rbac/integrity-check.ts` instead of the hardcoded literals #4272's tenant
    reset invalidated, same discipline `parity-check.ts` pass A already uses. Handling the
    no-real-tenant case (per #4318/#4396) as a clear SKIP instead of an opaque FK violation.
  - 2026-09-16T18:58:00Z — ✅ DONE — `resolveFixtureIds()` added to `lib/db/src/rbac/integrity-check.ts`:
    queries `users`/`msps`/`tenants` live for an msp user + a second msp, and a customer user +
    a second tenant, instead of hardcoded ids; prints a clear SKIP and exits 0 if none exist.
    Live-verified: `pnpm --filter @workspace/db run check-rbac-integrity` against local
    `DATABASE_URL` now resolves MSP=1/OTHER_MSP=1626/TENANT=1403/OTHER_TENANT=1399/MSP_USER=1/
    CUSTOMER_USER=2606 (real msp rows + vitest-fixture tenant rows, since no real non-vitest
    tenant currently exists per #4318/#4396) and runs cleanly:
    `--- RBAC LIVE INTEGRITY: ALL CHECKS PASSED ---`. Commit `418915f6a` (merged to main as
    `2eec54878`). No migration needed — read-only query addition. `verify-branch-merged.mjs`
    confirms merged into origin/main; `git status --porcelain` clean.
