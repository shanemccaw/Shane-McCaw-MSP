# #2960 — New Site v2 — Article page (design pass)

- **Status:** ✅ DONE 2026-09-05T23:24:00Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`)
- **Started:** 2026-09-05
- **Commit(s):** `be38ce6ade86e3afb63135ac585d40c2a7e1d94d` on `production-base-2026-09-05` (real target branch, confirmed by the issue body and dispatch comment — NOT `main`). This worktree's own `main`-based history carries only this bookend file.

## Log
- 2026-09-05T22:52:00Z ⏳ IN FLIGHT — Read #2960's issue body + dispatch comment + follow-up comment confirming `blocked_by #2954` had already cleared (Header/Footer rebuild landed on `production-base-2026-09-05` at `4ad0df12a`). Read `Design/fractional_architecture/README.md` §6 ("Article") and `Article.dc.html` for the exact spec. Confirmed via `git ls-tree` that the real target branch already carries `Design/fractional_architecture/`, `ArticlePage.tsx`, `AuthorBio.tsx`, `ConsultationCTA.tsx`, `ArticleAssessmentCTA.tsx`/`ArticlePersonalizedNudge.tsx` and the 7 published `.md` files under `src/content/articles/`. Same maneuver as #2954: did the real edit in a second worktree checked out from `origin/production-base-2026-09-05` (this main-based worktree keeps only bookend/progress-reporting), this time registered via `provision-worktree.mjs --base origin/production-base-2026-09-05 --link` (an earlier ad-hoc `git worktree add` got swept mid-session by a concurrent cleanup sweep before any edits landed — no work lost, just re-provisioned properly with ownership tracked).
- 2026-09-05T23:24:00Z ✅ DONE — Rewrote `src/pages/ArticlePage.tsx`: header/meta row (category pill, date, reading time, byline with blue→teal gradient SM tile, decorative `FileText` hero glyph, dual radial-gradient hero background), markdown body styling exactly per README §6 (`h2`/`h3`/`p`/`strong`/`ul`/`ol`/`blockquote`/`hr`, incl. the teal-dot vs. Menlo two-digit teal number list-marker split via a `.article-ol` CSS counter added to `index.css`, and a distinct smaller-italic treatment detected for a closing `*...*` line), and a restyled footer share row (LinkedIn/X/copy-link, exact colors/radii/hover states from `Article.dc.html`). Restyled `AuthorBio.tsx` to the exact spec (64px SM tile, radius 18px, blue→teal gradient — was previously blue→violet 90°, wrong per spec; 20px-radius card; exact type scale) while keeping the bio paragraph copy verbatim, unchanged. Removed `ArticleAssessmentCTA`/`ArticlePersonalizedNudge` usage from the page and deleted both component files outright — confirmed no other callers anywhere in the repo via `grep -rn` before deleting. `ConsultationCTA.tsx` at the bottom of the page was left untouched (not in scope, no spec deviation found). No `logger.child({ channel: "growth.marketing" })` binding added: this is a pure client-side React design pass with no new server route/subsystem — same justified omission #2954 already recorded for the identical case.
  - Verified via `tsc --noEmit` (clean, both before and after a rebase onto a concurrent push) and `PORT=… BASE_PATH=/ vite build` (succeeds; pre-existing chunk-size and `%VITE_*%` env-var warnings, unrelated to this change, same ones #2954 already saw).
  - Pushed directly to `origin/production-base-2026-09-05` as commit `be38ce6ad` (rebased once onto a concurrent commit from #2957's Contact page build that landed mid-session; rebase was clean, no conflicts). Confirmed via `git merge-base --is-ancestor HEAD origin/production-base-2026-09-05`.
  - Second worktree cleaned up via `cleanup-worktree.mjs` (253 dependency junctions unlinked first, then removed; an untracked `.env.local` the provisioner itself had copied in was auto-rescued to a throwaway `rescued/2960-prodbase-*` branch by the cleanup tool — not part of this build's own work, no code was in it).
  - Working tree clean on this worktree at completion (`git status --porcelain` empty).
- No new findings filed this session — the only real deviation found (AuthorBio's SM tile using the old blue→violet gradient instead of the spec's blue→teal) was fixed in-scope as part of this same design pass, not filed separately.
