# #4143 — Security Plan: Part II Control Domains grouping has no backing field

- **Status:** ⏳ IN FLIGHT 2026-09-15T00:18:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** TBD — fill at DONE

## Log
- 2026-09-15T00:18:00Z ⏳ IN FLIGHT — investigated #4143, `security-plan.tsx` header
  comment (lines 25-32), `security-plan-assembly.ts`, and the seven source tables.
  Decision: **real migration/new column**, not a derived mapping. Verified live
  against local Postgres that the only existing per-row fields on any of the seven
  source tables (`pillar` on policy/risk, `category` on sops/change-control) are
  REAL but DIFFERENT vocabularies from the design's Identity/Data/Collaboration/
  Change/Monitoring control-domain grouping — e.g. real `msp_risk_decisions.pillar`
  values are "Security"/"Compliance"/"Governance"/null, none of which are a control
  domain. No existing platform-wide taxonomy (checked `HEALTH_PILLARS` in
  `health-engine.ts`) matches the design vocabulary either. A string-mapping
  "derived" classification off those different fields would be exactly the
  "invented display vocabulary mapping onto nothing" the page's own original
  comment already warned against — so it was not a clean derivation, and the
  issue's own text authorizes falling back to a real column in that case.
  Added `control_domain text` (nullable) to all 7 source tables
  (`policy_decisions`, `msp_risk_decisions`, `portal_ownership_rows`, `msp_sops`,
  `remediation_tracker_steps`, `msp_change_requests`,
  `m365_change_interpretations`), ran
  `lib/db/migrations/manual/4143-security-plan-control-domain.sql` myself against
  local `DATABASE_URL` (7 ALTER TABLE, backfill UPDATEs for the 23 real existing
  seed rows across the 5 non-empty tables, self-marking INSERT). Wired
  `security-plan-assembly.ts` to select/pass through the real column on all 7
  modules. Added `SECURITY_PLAN_CONTROL_DOMAINS`/`_LABELS` (identity/data/
  collaboration/change/monitoring) to `lib/db/src/schema/msp.ts`, mirrored in
  `artifacts/portal/src/lib/security-plan-types.ts`. Built a new
  `ControlDomainSchedule.tsx` component and wired a real "Part II — control
  domains" nav section into `security-plan.tsx` (renumbering the existing
  by-module nav to "Part III — schedules" and appendices to "Part IV", matching
  the design reference's own stated structure — "Part II groups those same rows
  by the control domain... Part III presents each register's rows unaltered").
  Rows whose source register has never been classified simply don't appear in
  Part II (honest, matches the existing null-pillar/framework precedent) but
  still appear unaltered in Part III. Updated the page's stale header
  comment/"what this page deliberately does not do" card to match. Updated
  `security-plan-assembly.test.ts`/`security-plan-drift.test.ts` item-shape
  helpers. `tsc --noEmit` clean on both `artifacts/api-server` and
  `artifacts/portal`. `vitest run security-plan-assembly.test.ts
  security-plan-drift.test.ts` — 20/20 passed. Ran
  `test-manifests/portal/security-plan.json` live via `shaneapp://runTest` —
  8/9 passed; the one failure (A5, `$.error.code` expected `"VALIDATION"` vs
  real `"VALIDATION_ERROR"`) is pre-existing drift unrelated to this change,
  fixed in the same commit and filed as #4157 (`runTest` reads the deployed
  checkout, not this worktree, so a fresh run here still shows the old
  behavior until this merges — expected, not a regression). Filed a second,
  unrelated finding as #4156 (URGENT, security): `source .env.local` (a
  documented-but-recurring footgun) leaked the local DEV-tier
  `MT_APP_CERT_PRIVATE_KEY` into this session's tool output while running
  tests; switched to safe single-var extraction for the rest of the session.
  Both findings filed as direct sub-issues of #1495 per the dispatch's
  instruction, board status set to AI Batter Up.
