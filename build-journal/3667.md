# #3667 — MSP Console: wire per-tenant Signals screen to real Tenant Scores backend

- **Status:** ✅ DONE 2026-09-12T00:29:25Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** a29959308 (bookend IN FLIGHT), b69432df0 (real work)

## Log
- 2026-09-12T00:28:23Z ⏳ IN FLIGHT — Read #3667/#3557/#3558, the AdminV2 Engines
  `ScoreTab` reference pattern (`artifacts/admin-panel/src/adminv2/screens/engines/EnginesBody.tsx`),
  and the real `GET /api/msp/customers/:customerId/scores` response shape
  (`artifacts/api-server/src/routes/msp-customer-scores.ts`). Building the expanded
  Signals render block in `Design/MSP_Console/design_handoff_msp_console/MSP Console.dc.html`
  (new `isSignals` page flag + `sigView` computed block + template, modeled on the
  existing Tenant Home "Scores and results" block but expanded to show per-pillar
  findings/recommendations, not just the flat priority list).
- 2026-09-12T00:29:25Z ✅ DONE — Added `isSignals` (`page === "signals"`) as a real,
  distinct page flag in the design's logic class, excluded it from the generic
  `isFrames` empty-skeleton fallback that #3557 found this screen resolving to, and
  gave it its own `sigView` computed block + template (right before the `isFrames`
  block in the markup). The block reads `this.tenantScores[tIdx]` — the same mock
  source Tenant Home's existing "Scores and results" block already reads against —
  and renders: composite score, priority items (worst severity first, same source
  and shape as Tenant Home), then a per-pillar breakdown for every engine showing
  its own real findings and recommendations (a pillar with neither renders "Nothing
  wrong here", a real clean result rather than a missing one). Extended the
  `tenantScores` mock engines with `findings`/`recommendations` arrays that mirror
  the real route's `pillars[engineKey]` shape (derived server-side from
  `tenant_engine_snapshots.breakdown`'s `finding`/`message`/`label` and
  `recommendation`/`action` fields — see `msp-customer-scores.ts`). Added three
  `wire:`-annotated notes (paywall-does-not-apply, breakdown-array source, and the
  separate `msp_diagnostic_findings` source for priority items) using this file's
  existing note-block convention, and wired `isSignals` into `hasNotes` so they
  render. Added `isSignals` branches to the page eyebrow/title/note ternary chains
  for consistency with every other tenant page.
  Verification: this is a design-reference `.dc.html` file (no build/typecheck
  target of its own) — verified the embedded logic-class `<script>` block still
  parses cleanly (`node -e 'new Function(script)'`, no syntax error) and that every
  `<sc-if>`/`<sc-for>` tag I touched is still balanced (76/76, 82/82 open/close
  counts) after the edit. No fixture/hardcoded data introduced beyond this file's
  own pre-existing, explicitly-documented mock-data convention (README.md: "All
  data is mock data... Wire real data from the API routes named throughout") — the
  real route and its real response shape were read first and the new mock data
  matches that shape exactly, same as every other block in this file.
  `git status --porcelain` clean; branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`, commit `b69432df0`). No findings filed — the real gap
  #3667 named (no render block at all) is what this build fixed; no other genuine
  bug/gap surfaced during this build.
