# #3945 — api-server typecheck/build red on main: TS2352 in auth-impersonation.test.ts:53

- **Status:** ✅ DONE 2026-09-13T06:09:49Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 353d75837 (TS2352 fix), b6a3d06e2 (scripts/tsconfig.json allowImportingTsExtensions), all on `main`

## Log
- 2026-09-13T05:57:30Z ⏳ IN FLIGHT — Confirmed the TS2352 at `auth-impersonation.test.ts:53`: `Promise.resolve([])` infers `Promise<never[]>`, cast directly to `Promise<unknown[]> & {returning; onConflictDoUpdate}` which doesn't structurally overlap. Fixed by typing the resolved value as `unknown[]` (`Promise.resolve([] as unknown[])`) so the base `Promise<unknown[]>` overlaps with the intersection target, matching how the sibling stub in the same file's `select` mock and in `msp-team-invite.test.ts` already resolve an explicitly-`unknown[]`-typed value before the `.limit`-style cast. Spot-checked the other 4 files touched by #3643's commit `7b181f8c4` (admin-script-runner.test.ts, auth-account-activation-entitlement.test.ts, leads-stats.test.ts, msp-api-foundation.test.ts) — none share the pattern.
- 2026-09-13T06:00:00Z ⏳ IN FLIGHT — `artifacts/api-server` scoped `pnpm run typecheck` now exits 0. Full root `pnpm run typecheck` still failed on an unrelated, pre-existing gap: `scripts/tsconfig.json` lacks `allowImportingTsExtensions`, which `lib/db/tsconfig.json` and `artifacts/api-server/tsconfig.json` already carry (Git #2458) because `@workspace/db`'s `.ts`-extension imports need it. Fixed by adding the same flag to `scripts/tsconfig.json` (its `typecheck` script already passes `--noEmit`, satisfying the flag's emit-guard requirement).
- 2026-09-13T06:07:00Z ✅ DONE — Real verification, not piped: `pnpm run typecheck` from repo root → real exit 0. Full root `pnpm run build` then reached the per-package vite builds (previously unreachable because typecheck failed first) and surfaced a separate, unrelated pre-existing bug: `artifacts/msp-console`, `artifacts/portal`, `artifacts/msp-website` vite.config.ts all throw `"PORT environment variable is required"` unconditionally, even for `vite build` (not just dev/preview), so the root `build` script cannot succeed without an arbitrary `PORT` exported first — out of scope for this issue (none of the 3 files were touched by #3643 or this fix), filed as **#3953**, parented under #1571 (EPIC: MSP Console, #3945's own parent — no Feature-tier parent exists), labeled `bug`, board status set to AI Batter Up. `artifacts/api-server`'s own `typecheck` and `build` steps (this issue's actual scope) both verified green in isolation. Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
