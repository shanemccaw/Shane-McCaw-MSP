# #3945 — api-server typecheck/build red on main: TS2352 in auth-impersonation.test.ts:53

- **Status:** ⏳ IN FLIGHT 2026-09-13T05:57:30Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-13T05:57:30Z ⏳ IN FLIGHT — Confirmed the TS2352 at `auth-impersonation.test.ts:53`: `Promise.resolve([])` infers `Promise<never[]>`, cast directly to `Promise<unknown[]> & {returning; onConflictDoUpdate}` which doesn't structurally overlap. Fixed by typing the resolved value as `unknown[]` (`Promise.resolve([] as unknown[])`) so the base `Promise<unknown[]>` overlaps with the intersection target, matching how the sibling stub in the same file's `select` mock and in `msp-team-invite.test.ts` already resolve an explicitly-`unknown[]`-typed value before the `.limit`-style cast. Spot-checking the other 4 files touched by #3643's commit `7b181f8c4` (admin-script-runner.test.ts, auth-account-activation-entitlement.test.ts, leads-stats.test.ts, msp-api-foundation.test.ts) for the same pattern next, then running `pnpm run typecheck` directly (not piped) and `pnpm run build`.
