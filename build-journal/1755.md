# #1755 — Dev-server coordinator stuck 2+ days behind main; local shaneapp://runTest hits stale code

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** bookend only (`5c65766825` IN FLIGHT + the DONE update). The fix itself was operational, not source changes: `git reset --hard origin/main` on the `dev-server` branch in `C:\dev-server`, plus a rebuild+restart of the served api-server.

## Log
- 2026-08-29 ⏳ IN FLIGHT — Investigating the stuck `C:\dev-server` coordinator checkout. Plan: confirm it holds nothing unique (`git log dev-server ^origin/main`), understand the `build-journal/1430.md`/`1415.md` add/add conflicts, then rebuild it from a clean `origin/main` so `status.mjs` HEAD matches main and a real `shaneapp://runTest` against `test-manifests/portal/settings-change-control-departments-persistence.json` returns 200s instead of the false 404s. One-time catch-up fix only; no changes to coordinator auto-merge/restart logic.
- 2026-08-29 ✅ DONE — Fixed. Confirmed `C:\dev-server` was disposable: its 14 unmerged commits were all coordinator merge-commits + 3 real content commits (#1430 security-email + #1415/#1430 bookends) that target the **retired** `portal-v2`/`msp-portal` tree (removed from main in `f40438cdc`), with issues #1430/#1415 already closed/`complete`. Nothing resurrectable. The `build-journal/1430.md`/`1415.md` **add/add conflicts** were exactly what the finding suspected: the divergent `dev-server` branch had accumulated its own copies of those bookend paths (from merging the agents' own branches locally), which main also has via the agents' real pushes — so the next merge saw both sides "adding" the same path = add/add. Not a two-writer collision; a stale divergent-branch artifact the rebuild makes moot.
  - **Action:** tagged old HEAD `dev-server-prereset-1755` (reversible), discarded regenerable win32 optional-dep drift in `package.json`, `git reset --hard origin/main` on the `dev-server` branch. Then ran the sanctioned `scripts/dev-server/refresh-main-server.mjs`: its ff-pull of the main checkout safely **skipped** (blocked by a pre-existing uncommitted `pnpm-lock.yaml` that is not mine — see finding #1769), and it rebuilt+restarted the served api-server from the main checkout at `df74c576e` (which contains #1592's departments/change-control routes). One transient build failure (resource contention with concurrently-spawned frontends) left :8080 down briefly; a clean `dev-all.mjs --start api-server` rebuild (exit 0) brought it back.
  - **Verification (real, before→after):** `status.mjs` server HEAD `06263267` (2-days stale) → `17c8ed0e` (clean ancestor of origin/main, ~5 commits = normal cycle latency). Endpoint `GET /api/portal/settings/departments` `404` → `401` (route now served, auth required). Real `shaneapp://runTest` against `test-manifests/portal/settings-change-control-departments-persistence.json` → **PASS 19/19, 0 failed** (was false 404s on all 17 non-setup steps).
  - **Finding filed:** #1769 (parented to #1202, board "AI Batter Up") — the served :8080 api-server (main checkout) is pinned at `df74c576e` because the coordinator's ff-only pull is blocked by an orphaned uncommitted `pnpm-lock.yaml` (byte-identical to origin/main) in the shared main checkout; routes merged *after* `df74c576e` still 404 until Shane resolves that one file. Left untouched per the leave-non-mine-dirty-files-alone hard rule.
  - **Pre-existing dirty files in the shared main checkout, NOT mine, left alone:** `pnpm-lock.yaml` (modified) and untracked `desktop/BuildConsole/UsersRonnieAppDataLocalTempshaneapp-1752-verify.result.json`. My own worktree tree is clean.
