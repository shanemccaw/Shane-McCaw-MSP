# #3973 — RBAC: wire RetainerNoConsent -> RetainerConsented role swap on real consent completion

- **Status:** ✅ DONE 2026-09-14T02:35:22Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 2029742b3 (implementation), 98f4a2520 (IN FLIGHT bookend) — landed on `origin/main` at c0cf83640

## Log
- 2026-09-14T02:24:33Z ⏳ IN FLIGHT — confirmed #3971 (step 1) landed both in code
  (`LEGACY_ROLE_ORDER`/`LEGACY_ROLE` in legacy-ladder.ts) and live locally
  (`users_role_scope_check` migration ran, verified via `pg_get_constraintdef`).
  Traced the real consent-completion path: a Retainer buyer who skipped read
  consent (`checkout_sessions.consent_skipped_at`, #1311) later returns through
  `GET /api/public/flow/read-consent-url` -> the same `GET /api/consent/callback`
  (state = checkout session UUID) that links a tenant for a normal Customer
  account. That callback's checkout-session branch calls
  `provisionProspectAccount` -> `ensureClientMspUser`
  (`artifacts/api-server/src/lib/direct-tenant-provisioning.ts`), which today
  only role-patches when the existing role is the wholly-unbridged `Free`
  default (`stillAtUnbridgedDefault`) — a `RetainerNoConsent` user hitting this
  path gets `tenant_id` set but keeps `RetainerNoConsent`, exactly the
  intermediate/wrong state #3973 must prevent. Implementing the swap in that
  same single `UPDATE` statement next.
- 2026-09-14T02:35:22Z ✅ DONE — implemented the swap in
  `ensureClientMspUser` (`artifacts/api-server/src/lib/direct-tenant-provisioning.ts`):
  the single `UPDATE` that already sets `tenant_id`/`mspId` now also sets
  `mspRole: RetainerConsented` whenever the row's existing role is
  `RetainerNoConsent`, unconditionally (ignoring `desiredRole`, which carries
  the caller's unrelated product-type default) — one atomic statement, no
  intermediate state. Added 3 new unit tests to
  `direct-tenant-provisioning.test.ts` (the swap fires; a non-Free/non-Retainer
  role is untouched; the pre-existing unbridged-Free-default path is
  unaffected) — 6/6 passing. Ran `consent.test.ts` (58/58 passing, unchanged —
  the route itself didn't need to change, it already calls into this
  function) and `tsc --noEmit` (clean).
  Noted, not fixed (out of this issue's scope, owned by sibling #3972 in the
  same build set): `attachPasswordToAccount`/`provisionProspectAccount`
  currently drop the caller's `role` entirely when no tenant is resolvable yet
  (the `tenant ? {...} : undefined` scope object in `ensureClientAccount`),
  which is the real #3950 bug #3972's contract already targets — a live
  `purchase-account-flow.test.ts` run against the real local DB currently
  fails on this for a no-tenant retainer session (`users_role_scope_check`
  violation, msp_role defaults to `Free` which requires a tenant). Did not
  touch that file — #3972 is actively in flight on it concurrently.
  Verified branch is a real ancestor of `origin/main`
  (`verify-branch-merged.mjs` — OK, merged at c0cf83640) and the working tree
  is clean (`git status --porcelain` empty). Cleared the stale `blocked_by`
  edge to #3971 once its landing was confirmed live (both in code and against
  the local DB).
