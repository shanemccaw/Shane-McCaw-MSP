# #3441 — Remove old legacy Tasks tab from Admin Panel Marketing tool (AdminPanel)

- **Status:** 🛑 BLOCKED 2026-09-10T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** (none — investigation only)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — pulled #3441 body + comments, confirmed real backing entry:
  `artifacts/admin-panel/src/components/shell/workspaceNav.tsx:138` —
  `{ id: "mkt-tasks", label: "Tasks", path: `${MARKETING}?tab=tasks`, icon: CheckSquare }`.
  Header carries `--blocked-by 3429`; a real GitHub `blocked_by` edge #3441 → #3429 already exists.
- 2026-09-10T00:00:00Z 🛑 BLOCKED — #3429 is still `open` and labeled `blocked` (blocked on #3428,
  which is itself still `open`/in-flight and blocked on #3427). #3429 and #3428 both edit the same
  shared file this issue needs to touch: `artifacts/admin-panel/src/components/shell/workspaceNav.tsx`.
  A later dispatch comment on #3441 (posted 2026-09-10T04:52:09Z, buildSet=BatterUpClearOut) omitted
  `--blocked-by`, but per CLAUDE.md the live `blocked_by` edge is the real gate, not the dispatch
  comment text, and the edge still points at an open #3429. Treating this as genuinely still blocked
  rather than risking a shared-file collision on `workspaceNav.tsx`. Left the existing `blocked_by`
  edge in place (still accurate), applied the `blocked` label, no code changed. Will pick this back
  up once #3429 closes or is labeled `complete`.
