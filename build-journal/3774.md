# #3774 — Remove QueueWatcherService's automatic 10s timer; event-driven claim-check only

- **Status:** ✅ DONE 2026-09-12T05:10:38Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 798ebd4f7 (real fix), c57695457 (IN FLIGHT bookend)

## Log
- 2026-09-12T05:00:04Z ⏳ IN FLIGHT — Investigated QueueWatcherService's `_timer` (DispatcherTimer, 10s, TickAsync) and confirmed the manual-refresh path (#3767, `LeftSidebar.BoardRefreshCompleted` → `RequestImmediateReevaluation` → `TickAsync`) is already wired and already merged to main. About to: remove the DispatcherTimer entirely, add a real process-exit event (ThreadPool.RegisterWaitForSingleObject on the raw process handle, no polling) to `BuildProcessHandle`, and arm it on every build launch/adopt so a real completion fires the claim-check directly. Also auditing for other automatic GitHub-touching timers per the issue's item 4.
- 2026-09-12T05:10:38Z ✅ DONE — Real fix landed at `798ebd4f7`:
  - `desktop/BuildConsole/Services/QueueWatcherService.cs`: removed `_timer`/`DispatcherTimer` entirely (field, `Start()`'s construction/Tick handler). `Start()` now does one one-shot `TickAsync()` at startup (claims whatever's already queued) and never re-arms anything on a clock. Added `ArmCompletionTrigger(id, entry)`, called right after both real `_running[item.Id] = entry;` sites (fresh launch in `LaunchItem`, and `AdoptRunningItem` for a restart-recovered build) — registers a real exit callback on the build's process handle. `_ticking` changed `bool` → `Interlocked` `int` since TickAsync can now be entered from a thread-pool callback concurrently with a UI-thread manual refresh (the old bool guard was only safe because everything ran on one UI-thread timer before). Confirmed #3767's manual-refresh path (`RequestImmediateReevaluation` → `TickAsync`) already reaches the claim-check — no change needed there, already merged to main.
  - `desktop/BuildConsole/Services/RedirectedProcessLauncher.cs`: added `BuildProcessHandle.RegisterExitCallback(Action)` — `ThreadPool.RegisterWaitForSingleObject` on a non-owning duplicate (`SafeWaitHandle(rawHandle, ownsHandle: false)`) of the real process handle, so the OS itself wakes a thread-pool thread the instant the process exits (no polling), without letting the wrapper's `Dispose()` close the real handle the reap path still needs for `HasExited`/`ExitCode`/`Kill`. The exit callback marshals onto the UI thread via `Application.Current.Dispatcher.BeginInvoke` before calling `TickAsync()`, preserving the pre-existing "TickAsync/_running/_reservedSlots mutated on the UI thread only" invariant documented at the top of the file, with no timer left doing that marshaling for it.
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` — 0 errors (10 pre-existing warnings, unrelated). Wrote and ran a standalone smoke test (outside the repo, in `%TEMP%`, deleted after) that launches a real short-lived process, registers the same `ManualResetEvent`+non-owning-`SafeWaitHandle`+`ThreadPool.RegisterWaitForSingleObject` pattern used in `RegisterExitCallback`, and confirmed: the callback fired the instant the process exited (no poll loop), and the original `Process` handle/`ExitCode` remained valid and correct (`7`) afterward — proving the wrapper's `Dispose()` never touched the real handle. Did not launch the full BuildConsole.exe app to simulate a real end-to-end queue chain (per the standing "never launch BuildConsole.exe to verify" guidance) — the build-succeeds + isolated mechanism verification above is the honest scope of what was actually verified this session; Shane's own real-app verification steps from the issue body (idle silence in ActivityLog, a chained dependent build launching off a real completion, manual refresh still working) are for him to run live.
  - **Audit (issue's item 4):** grepped every `new DispatcherTimer`/`new Timer(` in `desktop/BuildConsole/Services/` and `desktop/BuildConsole/Controls/`. Confirmed `BuildQueuePanel`'s old `_sessionsPollTimer` (the #2900 flag this issue's body cites) was already removed by an earlier session (comment at `BuildQueuePanel.xaml.cs:435-446` documents it). Found ONE other real, remaining automatic GitHub-touching timer: `WaitingRowAutoRequeueService` (Git #3661) arms an unconditional 5-minute `System.Threading.Timer` whose own doc comment says "One `gh issue list` call per tick." Filed as **#3777** (sub-issue of #1789, `bug` label, board status set to "AI Batter Up") rather than fixed in this same pass — it's a distinct service needing its own real completion-event wiring, not a one-line follow-on to this diff.
  - `git status --porcelain` clean. Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0, `798ebd4f7` confirmed an ancestor).
