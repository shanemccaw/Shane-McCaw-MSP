# Bookend: #2791

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T00:00:00Z — Investigated. Issue #2791 asked to fix `TailApiServerAsync`'s
  `platform_log_stream.id` int4-vs-int8 cast bug in `LogService.cs`.
- Found the bug **already fixed** in the BuildConsole port
  (`desktop/BuildConsole/Services/LogService.cs`, commit d3edadac4, Git #2785) — it
  uses `Convert.ToInt64(scalar)` and `Convert.ToInt64(reader.GetValue(0))`, both
  schema-agnostic, with no `(long)` unbox or `GetInt64` cast remaining anywhere in
  the file. Confirmed live against local Postgres: `platform_log_stream.id` is
  `integer`, matching the fix's own header comment.
- The issue's own body says this same fix was already applied to the BuildConsole
  port, and that #2791 tracks fixing the **original** ShaneBuilder source
  (`desktop/ShaneBuilder/Services/LogService.cs`) which still had the throwing form.
  That source tree no longer exists: `desktop/ShaneBuilder/` was deleted entirely on
  2026-09-11 (commit d4974dcb3, Git #3660) as the final step of ShaneBuilder's
  retirement, after its remaining salvageable pieces were ported elsewhere.
- **No code change made** — the premise (a live ShaneBuilder file needing the fix)
  is stale; the only real copy of this code (the BuildConsole port) already has the
  correct schema-agnostic fix and is confirmed correct against the real DB schema.
- `git status --porcelain` clean apart from this bookend file.
