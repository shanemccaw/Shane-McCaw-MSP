# #1940 — Runbooks: hold windows have no cycle affinity after #1557's run model

- **Status:** ✅ DONE 2026-09-03T03:46:36Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 5ec915f0c8d528a88b844d18337c4d228abdf69f (on main; squashed rebase of the two
  session commits — bookend IN FLIGHT + the real fix)

## Log
- 2026-09-03T03:39:23Z ⏳ IN FLIGHT — add `run_id` to `portal_hold_windows` (nullable, additive) and
  thread it through `portal-runbooks.ts` (the runbook↔hold matching at GET /portal/runbooks) and
  `admin-testbed.ts` (seed insert) so a hold window resolves against the specific cycle it was
  raised for, not just the schedule. Per issue body.
- 2026-09-03T03:46:36Z ✅ DONE — Added `portalHoldWindowsTable.runId` (FK -> `portal_runbook_runs`,
  nullable, indexed) in `lib/db/src/schema/msp.ts`; wrote and ran
  `lib/db/migrations/manual/2026-09-03-portal-hold-windows-run-id-1940.sql` against local
  `DATABASE_URL` (confirmed column + FK + index present via `\d portal_hold_windows`). Wired
  `GET /portal/runbooks` in `portal-runbooks.ts` so an open hold window only decorates its
  runbook's status when `runId` matches that runbook's CURRENT cycle (`runsByRunbook`'s last
  entry); a legacy window with `runId = null` falls back to the pre-fix runbookId-only match, so
  existing (0-row, per the issue) data is unaffected. `admin-testbed.ts`'s seed now sets `runId`
  on the hold window it inserts. Also added `runId` to the `WireHoldWindow` wire shape for API
  transparency.

  Verification: migration confirmed applied (column/FK/index present).
  `node --experimental-strip-types --check` passes clean on all three touched `.ts` files.
  Full `tsc`/`vitest` could not be run in this worktree — `vitest`/`tsx` are unresolvable even
  in the main checkout's own `node_modules` (not caused by this change), and `lib/db`'s
  project-reference build (`tsc -b`) has a large pre-existing implicit-any baseline in
  `msp.ts` confirmed present via a stash-diff check before my edit (222 pre-existing error
  lines with my change stashed out). Not filing this as a new finding — it matches the
  already-documented dev-environment tooling class in memory
  (`windows-native-binaries-missing-pnpm-store`), not a fresh discovery.

  `git status --porcelain` clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
