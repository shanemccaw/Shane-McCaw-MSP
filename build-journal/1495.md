# #1495 — Portal New Design: Security Plan (contract extraction pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `4aea9dd66` (pack) + `540f88e10`/`f2eabaabc`/`67c77bb20` (three post-merge
  accuracy updates as sibling packs landed), merged to `origin/main` at `67c77bb20`

## Log
- 2026-08-29 ⏳ IN FLIGHT — Step 3 of #1578's per-module sequence (schema → honest
  read → contract pack → Design → wire), scoped to Security Plan per #1577's
  scoping correction. Pulled #1495 body + settled-architecture comment, #1485
  (epic, all 6 comments), #1577 (method, per-module scoping), #1578 (build
  sequence, Security Plan is structurally last), and sub-issues #1561–1568 in
  full. Read the existing `portal_security_plan*` schema (`lib/db/src/schema/
  msp.ts:4545-4684`), the route (`portal-security-plan.ts`), and the portal
  wire/model/live files (`securityPlanWire.ts`, `securityPlanModel.ts`,
  `securityPlanLive.ts`, `portal-v2-security-plan.tsx`) — a real, admin-authored,
  read-only plan-of-record already exists and serves live data, but it predates
  (migration dated 2026-08-21) and does not implement the settled "assembled
  view over the other eight modules + authored prose, sealed and versioned"
  architecture from #1495's 2026-08-28 comment. Cross-referenced the shipped
  contract packs for the three modules this view borrows from that already have
  one (Change Control #1486, Risk Register #1487, Microsoft Changes #1494) and
  the Drizzle schema directly for the three that don't yet (Ownership/RACI
  #1491 — in flight; Remediation #1489 — in flight; SOPs/Runbooks #1493 — not
  started; no separate Policy Engine table exists yet, per Risk Register
  pack §6). Writing `docs/security-plan-contract-pack.md` next. Per #1564/#1568,
  flagging (not resolving) signature authority and the cross-customer posture
  question. READ-ONLY: no product code, no schema changes, no UI, no
  `drizzle-kit push`.
- 2026-08-29 ✅ DONE — Shipped `docs/security-plan-contract-pack.md`. Extracted the one
  existing live endpoint's wire contract in full (fully consumed by its page, so no orphan
  sub-issue was filed), and stated plainly that the current admin-authored implementation
  (schema/route/page all real, migration dated 2026-08-21) does **not** implement the
  settled 2026-08-28 architecture from #1495's own comment ("a view over the other eight
  modules, owning almost no data of its own") — it currently owns 100% of its own data via
  hand-typed rows, with `to`/`toLabel` as navigation links only, never a real data join. §4
  maps every borrowed field to its owning module with CURRENT/DECIDED/OPEN-GAP status.
  Three sibling contract packs (#1490 Policy Decisions, #1493 SOPs, #1488 Runbooks) landed
  on `origin/main` concurrently while this pack was in flight; re-read each and updated
  §4.1/§4.4 plus provenance against them (three follow-up commits) rather than leaving
  stale "not yet started" claims in a merged document. #1564 (signing authority) and #1568
  (cross-customer posture view) flagged open, not resolved, per the task instruction.
  READ-ONLY: no product code, schema, or UI changed. Verified merged to `origin/main` at
  `67c77bb20` via `verify-branch-merged.mjs` (exit 0).
