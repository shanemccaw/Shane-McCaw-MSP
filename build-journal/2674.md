# #2674 — MSP console: no operator routes exist for Consent and Onboarding

- **Status:** ✅ DONE 2026-09-03T20:12:40Z
- **Scope:** platform

## Log

- 2026-09-03T20:03:47Z — Session started. Audited real MSP-scoped route files
  (`grep requireRole("MSPOperator"|"MSPAdmin")` across `artifacts/api-server/src/routes/`)
  and confirmed the issue's premise: every consent route in `consent.ts`
  (`/consent/invite-link`, `/admin/consent`, `/admin/consent/:tenantId/revoke`,
  `/admin/customers/:customerId/write-consent*`, `/admin/customers/:customerId/sharepoint-consent*`)
  is `requireAdmin`-only — zero MSP-scoped equivalent. Onboarding's
  `POST /api/msp/onboarding/generate-link` already exists (`msp-onboarding.ts`), but there is
  no MSP-side list/visibility route for the links it creates. Read #1650 (Portal contract) and
  #2563 (MSP Console Feature parent — "Not yet architected", scope: "Visibility and management
  of tenant write-consent/read-consent status"). Building real MSP-scoped routes now, same
  pattern as `msp-diagnostics.ts` (`requireRole("MSPOperator")` + `assertCustomerAccess`).
- 2026-09-03T20:12:40Z — ✅ DONE — Commit `724a63e9` (merged to `origin/main`,
  ancestor-verified via `verify-branch-merged.mjs`). Added
  `artifacts/api-server/src/routes/msp-consent.ts` (new file): MSPOperator+ scoped
  `GET /api/msp/consent`, `GET /api/msp/customers/:customerId/consent`,
  `POST /api/msp/customers/:customerId/consent/invite-link`,
  `PATCH /api/msp/customers/:customerId/consent/revoke` — every `:customerId` route
  ownership-checked via `assertCustomerAccess`, reusing `consent.ts`'s own
  `stampConsent`/`mergeConsentKey` as the single writer of `tenants.consent` (exported
  `getCallbackUrl`/`stampConsent`/`consentRow`/`ConsentKey`/`CONSENT_REVOKE_KEYS` from
  `consent.ts` for reuse; widened `stampConsent`'s `where` param from
  `ReturnType<typeof eq>` to `SQL` so an MSP-scoped `and(eq(id), eq(mspId))` condition
  type-checks). Added `GET /api/msp/onboarding/links` to `msp-onboarding.ts` (list
  visibility for the caller's own MSP's onboarding links — `generate-link` already
  existed but had no MSP-side list). Registered the new router in `routes/index.ts`.
  No schema/migration needed — `tenants.consent` and `msp_onboarding_links.mspId`
  already existed. No fixture/hardcoded data. Could not run api-server's vitest suite
  or a full `tsc --build` in this worktree — both fail on pre-existing, already-tracked
  issues (#2528 drizzle-orm pnpm store entry empty; #2502 shared node_modules symlinks
  stripped; #1777 composite lib/db checks stale dist), reproduced identically in the
  main checkout, unrelated to this change. Verified via a scoped
  `tsc --noEmit --skipLibCheck` pass over the touched files, which surfaced zero errors
  traceable to this change. `git status --porcelain` clean. No findings filed — the
  audited gap (this issue's own premise) was the finding, and it's now built.
