# #3591 — FocusModeBar cold-start honest loading state (native counter fallback)

- **Status:** ✅ DONE 2026-09-11T00:56:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 96a980144

## Log
- 2026-09-11T00:49:12Z ⏳ IN FLIGHT — implementing option 1 from the issue: added `HasRealCounts`
  to `FocusMilestone`/`FocusProgress`, set true only via `ComputeRealMilestoneCounts`, false on
  the native-counter fallback; `FocusModeBar.Refresh()` now shows "loading…" instead of the
  fallback number when the active milestone's flag is false.
- 2026-09-11T00:56:00Z ✅ DONE — `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors
  (pre-existing unrelated CS0067 warnings only). `verify-branch-merged.mjs` confirmed
  `agent/3591-q2264` (4de8c3ac) is an ancestor of `origin/main`. `git status --porcelain` clean.
  Not launched/screenshotted live (BuildConsole.exe verification is prohibited per repo rules);
  verified by build success + direct code read of the new gating logic.
