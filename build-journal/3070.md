# #3070 — Remaining hardcoded GitHub repo URL references not covered by #3069

- **Status:** ✅ DONE 2026-09-12T22:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** (filled below at push)

## Log
- 2026-09-12T22:38:20Z ⏳ IN FLIGHT — routing remaining hardcoded `https://github.com/shanemccaw/Shane-McCaw-MSP` literals through settings-backed helpers, per #3070's list.
- 2026-09-12T22:40:00Z ✅ DONE — Fixed `ChatDocumentContainer.xaml.cs:460`'s `CtxEpic_Click` to call `GitHubIssuesService.IssueUrl(EpicNumber.Value)` instead of the inline literal. Fixed `BuildConsoleSettings.cs`'s default "Git" `WebToolEntry` field initializer (was line 507) and its `LoadCore()` backfill check (was lines 992/997) to build the URL from `GitHubOwner`/`GitHubRepoName` (via `settings.GitHubOwner`/`settings.GitHubRepoName` in the backfill, and new `DefaultGitHubOwner`/`DefaultGitHubRepoName` consts in the field initializer, since a field initializer can't reference an instance property — CS0236). Confirmed no remaining `github.com/shanemccaw/Shane-McCaw-MSP` literal in `desktop/BuildConsole/**/*.cs`. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors/warnings. Left `RepoRegistryEntry` seed at `BuildConsoleSettings.cs:846` untouched — it's the intentional Main-repo registry seed, not a leak vector named in #3070's list, and is itself just a default entry a second `--instance` would override via its own settings.json.
