# #4142 — Risk Register: drop the 12-item ledger card to match the new design

- **Status:** ✅ DONE 2026-09-14T23:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** d47c099d1 (IN FLIGHT bookend), 3940e6508 (code change, rebased to 002cd1dfc on main)

## Log
- 2026-09-14T23:30:00Z ⏳ IN FLIGHT — verified all 12 `LEDGER` items in `risk-register.tsx` (lines 34-85) against `risk-register-contract-pack.md` and `portal-risk-register.ts`; none are closed (see below). Removing the "What this page deliberately does not do" card per Shane's 2026-09-14 decision to match `Design/portal/design_handoff_full_site/screens/Risk Register.dc.html`, which carries no ledger section (only a stray unused `ledger: true` state field, no render block).
- 2026-09-14T23:45:00Z ✅ DONE — Removed the ledger card, the `LEDGER` array, the `ledgerOpen` state, and the now-unused `Link` import from `artifacts/portal/src/pages/risk-register.tsx`. `pnpm run typecheck` in `artifacts/portal` passes clean. Branch rebased onto `origin/main` and pushed directly to `main` (`002cd1dfc`); `verify-branch-merged.mjs` confirms merged. Working tree clean.

## Per-item verification against `risk-register-contract-pack.md` (regenerated 2026-09-01) and `portal-risk-register.ts` — all 12 are still genuinely open/true; none closed

These gaps are being **visually dropped from the page**, per Shane's explicit design-match decision, while remaining real and unbuilt on the backend:

1. **No second signature (§9.3)** — still true; per-line accept is a guarded `UPDATE ... WHERE accepted_at IS NULL` (portal-risk-register.ts:660-668), whole-document sign guarded identically (rbd-versioning.ts:220-233). By-design permanent, not something that "closes."
2. **No renewal on your behalf (§9.6)** — still true; contract pack confirms `msp-rbd.ts` grepped for any renew/reissue path: none exists (#1507).
3. **No name check (§9.5)** — still true; `fullName`/`signerName` not matched against account name in either `portal-risk-register.ts` or `portal-rbd-document.ts`.
4. **No document produced on request / viewer+drawn-signature not wired into portal (§9.9)** — still true. The endpoints now exist and are live (#1512: `portal-rbd-document.ts`, `public-rbd-document.ts`), but the contract pack's own §0 table lists consumer as "none" for every one of them — nothing in `artifacts/portal` calls them yet.
5. **No authority check on cross-cutting risks (§9.8)** — still true; #1511 added role-based authority only where `checkKey` resolves to a real workload. Cost/governance/licence categories deliberately resolve to no workload owner (contract pack §9 item 8, explicitly "not an oversight to fix").
6. **No line items (§6.2)** — still true; `risk_instances` (#1509) is real and DB-enforced but MSP-side only (`msp-rbd-instances.ts`) — "no customer wire yet" per contract pack §6.2/§7.3.
7. **No structured owner as an editable field (§4)** — still true; `owner_id` is on `WirePolicyDecision` only, not on `WireRisk` — contract pack §7.1, "unchanged from prior pack — still open."
8. **No fabricated values (§8)** — still true (permanent design rule, `portal-risk-register.ts:61-65`).
9. **No difference between empty register and unidentifiable tenant (§8)** — still true; `scopeOrEmpty` returns `200`/`[]` deliberately either way (contract pack §8).
10. **No provenance for a risk from a declined remediation step (§7.3)** — still true; `spawned_by_remediation_step_id` (msp.ts:5817) is on the DB and written, but has no corresponding `WireRisk` field — contract pack §7.3 item 1, explicitly called a "real gap."
11. **Two registers named "policy decisions" (§0.1)** — still true architecturally; contract pack §7.3 item 2 says Design still needs to pick one or show both distinctly. (Code comment notes #1724 wired the standing `/policy-decisions` page as a real destination, but the two-tables-one-name ambiguity itself is not resolved.)
12. **Risk's own Expired status unsettled (§2)** — still true; contract pack §2 note: "genuinely open and not addressed by any landed issue."

No finding filed — this build did not discover a new gap, it confirmed the 12 disclosed gaps from #4128 are all still real and is dropping their on-page disclosure per an explicit product decision already recorded in the #4142 comment thread.
