# #1537 — Microsoft Changes: Cloud instance filtering — GCC exclusion and the NASA extraction

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** b405099d0 (on `origin/main`)

## Log
- 2026-08-29 ⏳ IN FLIGHT — cloud instance is already ingested/stored per roadmap item
  (`m365_roadmap_items.cloud_instances`, landed in #1530). Building the missing half:
  a shared filter dimension + exposing `cloudInstances` on the wire for the one real
  consumer (`GET /admin/m365/interpretations/candidates`), with the platform default
  (worldwide/exclude-gov) enforced from real data and a `gov`/`all` mode reusable by
  the future NASA extraction.
- 2026-08-29 ✅ DONE — `b405099d0` on `origin/main` (branch verified an ancestor via
  `verify-branch-merged.mjs`; working tree clean). No schema change was needed — the
  data has been live since #1530. What shipped:
  - `artifacts/api-server/src/lib/m365-cloud-instance.ts` — the dimension itself.
    `isGovCloudInstance`/`hasGovCloudInstance` classify Microsoft's own real tags
    (GCC / GCC High / DoD via a substring match, not an invented enum).
    `matchesCloudInstanceFilter`/`filterByCloudInstance` apply one of three modes:
    `worldwide` (this platform's default — excludes a gov-only item, keeps an
    unclassified one, since absent data must never be assumed out of scope),
    `gov` (the mode the future NASA extraction needs — GCC High/DoD signal, drops
    unclassified items as noise there), `all` (unfiltered escape hatch).
    `parseCloudInstanceFilterMode` degrades an unrecognized `?cloud=` value to the
    platform default rather than 500ing.
  - `artifacts/api-server/src/routes/admin-m365-interpretations.ts` — the ONLY real
    consumer of `m365_roadmap_items` outside the sync/link libs (verified by
    grepping every reference to the table). `GET .../candidates` now selects
    `cloudInstances` and puts it on the wire per roadmap candidate, accepts
    `?cloud=worldwide|gov|all` (default `worldwide`), and echoes the resolved
    `cloudMode` in the response so the client can show what's applied.
  - `m365-cloud-instance.test.ts` — 14 pure-logic tests, registered in
    `vitest.config.ts`, all passing. Included a fixture using the exact live
    sample #1530 verified against the real public feed
    (`["Worldwide (Standard Multi-Tenant)", "GCC"]`).
  - **Live-verified against the real local DB**: 1,795 roadmap items, 0 with an
    empty/unclassified `cloud_instances` array. Ran the platform's own SQL
    equivalent of the `worldwide`/`gov` classifiers directly: 190 items are
    gov-only (excluded in `worldwide` mode), 562 carry at least one gov tag
    (kept in `gov` mode) — both counts match the JS classifier's own logic
    exactly, confirmed by direct query, not inferred from code.
  - Scoped `tsc --noEmit` clean for both touched files.
  - **Honest note on an unrelated finding**: running the neighboring
    `m365-roadmap-mc-link.test.ts` (untouched by this build) against the real
    local DB showed 1 pre-existing failing assertion
    (`roadmapFeatureIds` not persisting on a freshly-inserted row in that
    test's own DB-integration case) — not introduced by this change (no file
    this build touched is on that test's import graph beyond `@workspace/db`
    itself), flagged here rather than silently ignored.
  - **Not built here (intentionally out of scope)**: the NASA extraction itself —
    no NASA-specific code exists anywhere in the repo yet (verified by grep). This
    issue built the reusable dimension it will need, not the extraction.
