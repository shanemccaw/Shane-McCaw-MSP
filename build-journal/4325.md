# #4325 — Home/SOW/Telemetry/Kanban/Screenshot Evidence share #4300's fixed-panel bug

- **Status:** ✅ DONE 2026-09-16T03:03:38Z
- **Scope:** platform (MyArchitect desktop app, shanemccaw/MyArchitect repo — not this repo)
- **Started:** 2026-09-16
- **Commit(s):** `69849fc` on `shanemccaw/MyArchitect` main (rebased worktree C:\wt\myarchitect-4325, branch agent/4325-fix)

## Log
- 2026-09-16T03:00:42Z ⏳ IN FLIGHT — confirmed real current state: `MainWindow.xaml.cs`'s
  `DocumentOverlays` array lists 10 entries; #4300 (commit f2b918d) converted VaultPanel/
  ConsolePanel/ReportPanel into real closable `LocalDocumentTabItem` tabs via
  `GetOrCreateLocalDocumentTab`/`GetOrAddLocalDocTab`/`CloseLocalDocumentTab`.
  HomeDashboardDocument, SowAssessmentDashboardView, TelemetryDashboardView,
  ScreenshotEvidenceDocument, KanbanBoardDocument were still shown only via `ShowDocument`'s
  fixed-panel visibility swap, with no tab-strip entry and no close button (ScreenshotEvidence
  had a bespoke in-panel close via `HideDocument`, still not a real tab). ScriptEditorDocument/
  CsvViewerDocument already have their own real tabs from #4292/#4294, out of scope. Applying
  the same conversion Shane's build prompt asked for, now that the abstraction is generalized.
- 2026-09-16T03:03:38Z ✅ DONE — extended `GetOrCreateLocalDocumentTab` to cover all 5 remaining
  `DocumentOverlays` entries (Home, SOW & Assessment, Live Telemetry, Screenshot Evidence,
  Kanban Board), each a real closable `LocalDocumentTabItem` using the existing #4300
  `GetOrAddLocalDocTab`/`CloseLocalDocumentTab` mechanism — no new tab-strip XAML or context-menu
  code needed, it already generalizes. Rewired `EvidenceGalleryPanel.CloseRequested` to close via
  `CloseLocalDocumentTab` instead of the old bare `HideDocument`, so its own in-panel close button
  now drops the tab-strip entry too. Fixed `CloseLocalDocumentTab`'s "fall back to Home" branch to
  skip that fallback when Home itself was the tab just closed (would otherwise instantly reopen
  it). ScriptEditorDocument/CsvViewerDocument left untouched — already real tabs via #4292/#4294,
  outside this issue's and #4300's named scope. `dotnet build MyArchitect.csproj -c Debug` — 0
  warnings, 0 errors, both before and after rebasing onto a concurrently-advanced `origin/main`.
  Pushed to `shanemccaw/MyArchitect` main as `69849fc`; `git merge-base --is-ancestor` confirmed.
  Working tree clean. No fixture/hardcoded data involved — this is pure WPF tab-strip wiring
  against existing live panels/data.
