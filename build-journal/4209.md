# Build Journal — #4209

- **Status:** ✅ DONE 2026-09-15T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-15T00:00:00Z — Started. Task: `msp-entitlement.ts:230`'s `upgradeUrl:
  "/portal/billing/upgrade"` points at a route that doesn't exist in the portal
  SPA. Confirming real mounted routes and the real consumer before deciding
  fix-vs-remove.
- 2026-09-15T00:20:00Z — ✅ DONE — Confirmed `/portal/billing/upgrade` is not a
  mounted route (`artifacts/portal/src/App.tsx` only mounts `/billing`).
  Traced the real consumer of `requirePlanFeature`'s 402 to `artifacts/msp-console`
  (`msp-sales-offers.ts`, `msp-sales-bundles.ts`), whose Billing screen for the
  MSP's own platform-subscription upgrade is itself blocked on a real Design
  export (#2608) — no real surface exists to point at yet. Grepped
  `upgradeUrl` across `artifacts/portal/src`, `artifacts/admin-panel/src`,
  `artifacts/msp-website/src`, `artifacts/msp-console/src`: no client reads
  the field. Removed the dead `upgradeUrl` line from the `UPGRADE_REQUIRED`
  402 response in `artifacts/api-server/src/lib/msp-entitlement.ts` rather
  than pointing it at a broken link. (The sibling `NO_SUBSCRIPTION` 402's
  `upgradeUrl: "/portal/signup"` was left untouched — `/portal/signup` is a
  real mounted portal route, out of scope for this fix.)
  Commits: `ad8f9518f` (IN FLIGHT bookend), `013e65343` (the fix). Both merged
  into `origin/main` — verified via `verify-branch-merged.mjs`. Working tree
  clean.
