# #3710 — Ask Shane: Epic-grouped panel replacing the flat ContextMenu

- **Status:** ✅ DONE 2026-09-11T22:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 764d67385 (main)

## Log
- 2026-09-11T22:05:00Z ⏳ IN FLIGHT — replacing `BtnAskShane_Click`'s flat `ContextMenu`
  (`MainWindow.AskShane.cs`) with an Epic-grouped popup panel, real airplane ("✈") icons
  matching the Build Queue rollup convention (Git #1893/#1932/#3605), sending real
  question/decision text (issue body + latest comment) via the shared
  `SendTextToActiveClaudeChatAsync` primitive. #3700's counter/toast/card-label untouched.
- 2026-09-11T22:40:00Z ✅ DONE — 764d67385 shipped: `BtnAskShane_Click`'s multi-item branch
  now opens a real `Popup` (`ShowAskShanePanel`) grouping the live "Ask Shane" items by their
  resolved Epic via the existing `BuildQueuePanel.ResolveEpicForIssue`/`LeftSidebar.
  GetEpicForIssueNumber` path (no second epic-resolution mechanism). Each row carries a real
  "✈" button matching the Build Queue rollup's own airplane visual/interaction convention;
  clicking it fetches the item's real issue body + most recent comment
  (`GitHubApiClient.GetIssueAsync`/`GetIssueCommentsAsync`) and sends that real text into the
  active Claude chat via the exact shared `SendTextToActiveClaudeChatAsync` primitive (no
  second send-to-chat path). Clicking a row's text (not the airplane) still calls the existing
  `NavigateToAskShaneItem` — nothing that worked before was removed, only the flat-dropdown
  presentation. The single-item fast path, #3700's persistent counter (`UpdateAskShaneBadge`),
  its transition toast (`ToastEngine.ShowPersistent` in `RefreshAskShaneAsync`), and the
  no-gating "❓ NEEDS YOUR INPUT" card label (`BuildQueuePanel.ApplyAskShaneSet`) are all
  byte-for-byte untouched — confirmed via `git diff --stat` showing only
  `MainWindow.AskShane.cs` changed.
  Verification: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 errors (10
  pre-existing warnings in unrelated files, unchanged by this diff). No live BuildConsole
  instance was launched to click-test the popup (see CLAUDE.md's "NEVER launch
  BuildConsole.exe to verify" rule) — verification here is the real, clean build plus a
  direct read confirming the wiring (ResolveEpicForIssue, SendTextToActiveClaudeChatAsync,
  GetIssueAsync/GetIssueCommentsAsync) all target real existing methods with matching
  signatures. No SQL/schema/backend work was in scope — this is a pure BuildConsole
  desktop-app UI change. `git status --porcelain` clean; merge-verified via
  `verify-branch-merged.mjs` → merged into origin/main at 764d67385. No findings filed —
  no genuine bug/gap was discovered outside this issue's own scope during the build.
