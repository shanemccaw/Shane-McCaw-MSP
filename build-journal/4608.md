# #4608 — BuildConsole + total claude.exe memory breakdown next to the system Memory meter

- **Status:** ✅ DONE 2026-09-17T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** be0f11ccc (rebased to 5702a1142 on origin/main)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — confirmed real child process name is `claude.exe` (`ClaudeAgentsService.ClaudeExe` = `~/.local/bin/claude.exe`; `RedirectedProcessLauncher.cs` doc comment corroborates), then adding a real per-process memory breakdown (BuildConsole's own process + SUM/COUNT of running claude.exe) next to `MemoryMeterBar`/`MemoryMeterText` in `MainWindow.ResourceMonitor.cs`/`.xaml`, on the existing 1.5s poll tick.
- 2026-09-17T00:30:00Z ✅ DONE — Added `MemoryBreakdownText`/`MemoryBreakdownTooltip` next to `MemoryMeterText` in `MainWindow.xaml` (Grid.Column="4" StatusBar segment) and `UpdateMemoryBreakdown(totalRamGb)` in `MainWindow.ResourceMonitor.cs`, called from the existing `TickResourceMonitor` 1.5s tick (no new timer). Reads BuildConsole's own `Process.GetCurrentProcess().WorkingSet64` and the real SUM + COUNT of `Process.GetProcessesByName("claude")` (confirmed real child process name from `ClaudeAgentsService.ClaudeExe` = `~/.local/bin/claude.exe`, corroborated by `RedirectedProcessLauncher.cs`'s own doc comment referencing claude.exe — BuildConsole never launches a build under any other process name). Compact line: `BC {mb} MB • claude.exe {count} procs, {gb} GB`; tooltip carries the fuller byte-level breakdown with % of total RAM for each, per the issue's "keep it compact" scope note. Count shown alongside summed memory per Shane's 2026-09-18 addendum comment. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` succeeded, 0 warnings/0 errors. No live BuildConsole launch for UI verification — launching the built exe from a build session for verification is itself prohibited (see the NEVER-launch-exe-for-verification convention); logic was verified by inspection and a clean build only. Commit `be0f11ccc`, rebased onto `origin/main` and pushed as `5702a1142`; `verify-branch-merged.mjs` confirms ancestor. Working tree clean. No findings/bugs surfaced. `--notitle` scope: buildconsole only, no schema/DB/portal changes.
