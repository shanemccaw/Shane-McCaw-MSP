# #4608 — BuildConsole + total claude.exe memory breakdown next to the system Memory meter

- **Status:** ⏳ IN FLIGHT 2026-09-17T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — confirmed real child process name is `claude.exe` (`ClaudeAgentsService.ClaudeExe` = `~/.local/bin/claude.exe`; `RedirectedProcessLauncher.cs` doc comment corroborates), then adding a real per-process memory breakdown (BuildConsole's own process + SUM/COUNT of running claude.exe) next to `MemoryMeterBar`/`MemoryMeterText` in `MainWindow.ResourceMonitor.cs`/`.xaml`, on the existing 1.5s poll tick.
