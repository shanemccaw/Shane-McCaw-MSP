# #1993 — Mirror Bash allow-list entries for the PowerShell tool identity

- **Status:** ⏳ IN FLIGHT 2026-09-04T06:34:00Z
- **Scope:** buildconsole (`.claude/settings.json` permissions)
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T06:34:00Z ⏳ IN FLIGHT — confirmed `PowerShell` is a real, distinct tool identity in
  this harness's own tool list (separate from `Bash`), so `Bash(<pattern>)` allow rules never
  match a `PowerShell(...)` call. Adding mirrored `PowerShell(<pattern>)` entries for the same
  command set already allowed for `Bash` (`git:*`, `psql:*`, `pg_dump:*`, `node:*`, `npx tsx:*`,
  `grep:*`, `rg:*`, `find:*`, `sed:*`, `awk:*`, `wc:*`, `cat:*`, `pnpm:*`, `gh:*`, `dotnet:*`) in
  `.claude/settings.json`.
