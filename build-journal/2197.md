# Build Journal — #2197

Port chat-mention + pinned-questions read layer to ShaneBuilder (once #2195 lands)

- **Status:** ⏳ IN FLIGHT 2026-09-02T07:34:36Z
- **Scope:** buildconsole  <!-- desktop/ShaneBuilder (sibling of BuildConsole), infrastructure only, no UI -->
- **Started:** 2026-09-02
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-02T07:34:36Z ⏳ IN FLIGHT — Real audit done first. Pulled #2197 body + #2195's ACTUAL landed code (commit `719656485`, bookend `a7be0cc87` — both verified real objects). #2195 landed `desktop/BuildConsole/Services/ChatDockService.cs` (merge/filter/chain-walk) plus `GetChatIssueMentionsForUrlAsync`/`GetOpenPinnedQuestionsForChatAsync` on `BuildQueuePostgresClient`. Task: port the READ LAYER (read-only Postgres client + merge/filter logic) into `desktop/ShaneBuilder`, no UI.
  - **Shared-DATABASE_URL assumption VERIFIED (not assumed):** ShaneBuilder's `Services/LogService.cs` (from #2200) already resolves the exact same `<repoRoot>/.env.local` `DATABASE_URL` as BuildConsole's `BuildQueuePostgresClient.CreateFromEnvironment`, with a public `ParseConnectionString` ported from it and a worktree-aware main-repo-root resolver. Same local Postgres, same DB — confirmed against committed code, not a guess. No divergence to flag.
  - **FK gap RESOLVED (not blocked):** audited local Postgres directly. `bt_chat_mentioned_issues` is keyed on `chat_url` text (no `bt_chats` FK) → reads standalone. `chat_pinned_questions.chat_id` FKs `bt_chats(id)`, and `bt_chats.conversation_id` is a UNIQUE text key equal to the UUID in `chat_url` (`https://claude.ai/chat/{conversation_id}`). So ShaneBuilder resolves `chat_id` from a chat URL/conversation-id via the shared `bt_chats` table — no own chat-tracking concept needed. Residual honest caveat: a conversation BuildConsole never recorded a `bt_chats` row for yields chatId=null → empty pinned-questions (real empty state, not faked).
