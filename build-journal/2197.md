# Build Journal — #2197

Port chat-mention + pinned-questions read layer to ShaneBuilder (once #2195 lands)

- **Status:** ✅ DONE 2026-09-02T07:42:08Z
- **Scope:** buildconsole  <!-- desktop/ShaneBuilder (sibling of BuildConsole), infrastructure only, no UI -->
- **Started:** 2026-09-02
- **Commit(s):** `12a3ce8c0` (read layer — 3 files) — verified real object + real ancestor of origin/main via verify-branch-merged.mjs (exit 0)

## Log
- 2026-09-02T07:34:36Z ⏳ IN FLIGHT — Real audit done first. Pulled #2197 body + #2195's ACTUAL landed code (commit `719656485`, bookend `a7be0cc87` — both verified real objects). #2195 landed `desktop/BuildConsole/Services/ChatDockService.cs` (merge/filter/chain-walk) plus `GetChatIssueMentionsForUrlAsync`/`GetOpenPinnedQuestionsForChatAsync` on `BuildQueuePostgresClient`. Task: port the READ LAYER (read-only Postgres client + merge/filter logic) into `desktop/ShaneBuilder`, no UI.
  - **Shared-DATABASE_URL assumption VERIFIED (not assumed):** ShaneBuilder's `Services/LogService.cs` (from #2200) already resolves the exact same `<repoRoot>/.env.local` `DATABASE_URL` as BuildConsole's `BuildQueuePostgresClient.CreateFromEnvironment`, with a public `ParseConnectionString` ported from it and a worktree-aware main-repo-root resolver. Same local Postgres, same DB — confirmed against committed code, not a guess. No divergence to flag.
  - **FK gap RESOLVED (not blocked):** audited local Postgres directly. `bt_chat_mentioned_issues` is keyed on `chat_url` text (no `bt_chats` FK) → reads standalone. `chat_pinned_questions.chat_id` FKs `bt_chats(id)`, and `bt_chats.conversation_id` is a UNIQUE text key equal to the UUID in `chat_url` (`https://claude.ai/chat/{conversation_id}`). So ShaneBuilder resolves `chat_id` from a chat URL/conversation-id via the shared `bt_chats` table — no own chat-tracking concept needed. Residual honest caveat: a conversation BuildConsole never recorded a `bt_chats` row for yields chatId=null → empty pinned-questions (real empty state, not faked).

## Log (cont.)
- 2026-09-02T07:42:08Z ✅ DONE — Shipped read-layer infrastructure (no UI) in `desktop/ShaneBuilder/Services/`:
  - `ChatReadClient.cs` — read-only Postgres client: `GetChatIssueMentionsForUrlAsync(chatUrl)`, `ResolveChatIdByConversationIdAsync`/`ResolveChatIdByChatUrlAsync` (the FK-gap resolver), `GetOpenPinnedQuestionsForChatAsync(chatId)`. Resolves the same `.env.local` DATABASE_URL as `LogService` (#2200), worktree-aware. DELIBERATELY read-only: unlike BuildConsole's version it does NOT port the pre-read `PurgeResolvedPinnedQuestionsAsync` DELETE (filters `resolved_at IS NULL` in the SELECT instead) — BuildConsole still owns writes.
  - `ChatGitHubFilter.cs` — gh-CLI port of the GitHub calls #2195 made via `GitHubIssuesService`/`GitHubApiClient` (ShaneBuilder has neither, nor a PAT/HttpClient): open-number set, issue title, `blocked_by`/`blocking` via `gh api ... --jq`, board status via `gh api graphql`. All five shapes verified live against this repo before porting.
  - `ChatDockDataService.cs` — faithful port of #2195's `ChatDockService` merge/filter/chain-walk: 14-day staleness window, 40-item cap, #1600 fail-closed, depth-3 bidirectional BFS. Values ported verbatim, not re-derived from prose. Plus a `BuildAsync(db, gh, chatUrl)` overload that resolves `chat_id` from the URL for ShaneBuilder's URL-only callers.
  - **Verification (honest):** `dotnet build ShaneBuilder.csproj` → 0 warnings / 0 errors. Every read query verified against real local Postgres: busiest chat_url is the same 449-mention chat #2195 measured (all 449 within 14d → the 40-cap is the real bound); mentions query returns real rows; FK resolver maps `conversation_id f77f8314… → bt_chats.id=44`; pinned-questions JOIN returns 0 rows (real empty — 0 pinned questions exist). gh open/title/blocked_by/blocking/graphql-status all confirmed reachable live. No end-to-end C# run (ShaneBuilder is a WPF WinExe — out of scope for test manifests per CLAUDE.md, and the single-instance rule forbids launching it); verification is build-clean + direct-DB + gh-CLI, not a manifest.
  - **Encountered (not a finding — already fixed on main):** my worktree's stale base (`444ef6a01`) still had unresolved conflict markers in `MainWindow.xaml.cs`; `origin/main` had already resolved them (`e0139649e`/`55f2ea38a`). Rebased onto current `origin/main` → clean build. Nothing to file: not a live bug on main.
  - Pushed to `origin/main` as `12a3ce8c0`; verify-branch-merged.mjs exit 0. Working tree clean.
