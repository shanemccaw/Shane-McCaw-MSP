# #3608 — users_role_scope_check schema/DB drift

- **Status:** ✅ DONE 2026-09-11T05:24:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 63ac6913d (bookend IN FLIGHT), 7842d8e69 (migration + real fix)

## Log
- 2026-09-11T05:17:17Z ⏳ IN FLIGHT — confirmed live local Postgres had no `users_role_scope_check` CHECK constraint (schema declares it, DB lacked it). Confirmed 0/9 existing rows would violate it. Writing + running the NOT VALID → VALIDATE migration to add it.
- 2026-09-11T05:24:00Z ✅ DONE — wrote and ran `lib/db/migrations/manual/2026-09-11-users-role-scope-check-drift-3608.sql` against local `DATABASE_URL`: added `users_role_scope_check` NOT VALID (0 existing violating rows), verified 0 violations again, then `VALIDATE CONSTRAINT`. Live-confirmed: `pg_constraint` now shows `users_role_scope_check` with `convalidated = t`, and a test INSERT violating the rule was rejected by the DB in a rolled-back transaction. Schema and live DB now match. Branch merged into `origin/main` (`verify-branch-merged.mjs` OK), working tree clean.
