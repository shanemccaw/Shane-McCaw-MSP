# #4000 — Scope and SLA: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 3a74b81cd (page/hooks/types/route/nav), 43680fc8f (regression-suite registration), 05183d032 (manifest hash + evidence fill-in)

## Log
- 2026-09-14T03:32:00Z ⏳ IN FLIGHT — read #4000 + #1661 body/comments, contract pack (Design/portal/design_handoff_full_site/docs/scope-and-sla-contract-pack.md), the Scope and SLA .dc.html export, archived customer-scope.tsx/customer-sla.tsx, confirmed #2513 (sla-status id-resolution bug) is closed/complete. Building customer-scope.tsx + customer-sla.tsx as one combined "Scope and SLA" page in artifacts/portal/ per the design, wiring /api/portal/customer/scope-status and /api/portal/customer/sla-status, registering route + sidebar nav entry.
- 2026-09-14T04:10:00Z ✅ DONE — Built `artifacts/portal/src/pages/scope-and-sla.tsx` + `lib/scope-sla-api.ts` + `lib/scope-sla-types.ts`, wired to the two live routes, both polling every 30s (carried forward from the archived pages). Registered `/scope-and-sla` in App.tsx and a "Scope and SLA" sidebar entry in moduleNav.ts (between Change Control and Risk Register — no existing nav placeholder found). Confirmed #2513's fix is live in `sla-engine.ts:279-286` (customerId now applied directly as tenant id) — since the fix has landed, deliberately dropped the design's own stale caveat card citing #2513 as still-open (carrying it forward would show customers a false warning). Verification: `artifacts/portal` tsc --noEmit clean, `vite build` clean; `artifacts/api-server` tsc clean; vitest 31/31 on the two touched suites (portal-customer-engines-assessment-redaction, msp-sla-scope-creep); direct psql confirms sla_timers/scope_creep_detections both 0 rows locally (so the real, honest response today is the on-track/empty state for both engines — no live data exists yet to exercise a non-empty case). Wrote `test-manifests/portal/scope-and-sla.json`, registered in `_regression-suite.json`. `shaneapp://runTest` was invoked (real attempt) and correctly returned `ok:false` — BuildConsole resolves manifests against the dev-server checkout, which hadn't received this commit yet at run time (same documented gap as #1717/change-control.json); honest evidence recorded in the manifest's own notes. No fixture/hardcoded data anywhere. No new findings filed — no bugs discovered beyond the already-filed, already-fixed #2513. Branch verified merged into origin/main (`verify-branch-merged.mjs`), working tree clean.
