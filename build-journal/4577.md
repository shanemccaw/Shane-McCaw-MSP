# #4577 — Manual override to force-clear the memory-pressure auto-pause

- **Status:** ✅ DONE 2026-09-17T00:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** c7585603c

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — adding a real "Force clear" link next to `MemoryPressurePauseText` in the status bar, wired to `QueueWatcherService.SetMemoryPressurePause(false)` + resetting `_memoryPressureClearSince`, without touching the automatic detector.
- 2026-09-17T00:20:00Z ✅ DONE — added `MemoryPressureForceClearText` (MainWindow.xaml) next to the existing memory-pressure indicator, visible only while `IsMemoryPressurePaused`; wired `ForceClearMemoryPressurePause_Click` in `MainWindow.ResourceMonitor.cs` to reset `_memoryPressureClearSince` to null and call `QueueWatcherService.SetMemoryPressurePause(false)` directly, logged via `ActivityLog`. Did not add any "disable auto-pause" toggle — the automatic detector (`MainWindow.ResourceMonitor.cs` poll) is unchanged and will re-pause normally if real pressure returns. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/errors. Commit c7585603c pushed to origin/main (rebased clean); `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
