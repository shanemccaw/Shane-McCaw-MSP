# #3601 — Build Queue: show and navigate to what a build itself blocks (reverse of the existing blocker ghost cards)

- **Status:** 🛑 BLOCKED 2026-09-11T02:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** none — no code changes made this session

## Log
- 2026-09-11T02:00:00Z ⏳ IN FLIGHT — re-checking #3599 (declared blocker) has genuinely landed before starting per this issue's own dispatch instruction.
- 2026-09-11T02:05:00Z 🛑 BLOCKED — #3599 confirmed NOT landed: `state: open`, labeled `in-flight` (no `complete`), zero related commits in `git log`. Direct read of `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`: `SelectNode` (line 4299) and the blocker ghost-card click handler (~4453-4460) still only navigate/highlight within `_currentGraphNodes` (the currently-filtered set) — no cross-filter switching logic exists. That is exactly the gap #3599 is meant to close. Building #3601's reverse "Blocks: #N, #M" pills on top of navigation that doesn't yet work across filters would mean reusing a mechanism that doesn't do the one thing this issue depends on.
- Real `blocked_by` edge #3601 → #3599 already existed (confirmed via `gh api repos/.../issues/3601/dependencies/blocked_by`) — left in place, correct as-is.
- Labeled #3601 `blocked`, removed `in-flight`. Posted an explanatory comment on #3601 stating the blocker and evidence. No `blocked` label change needed on #3599 (it's the blocker, not the blocked issue).
- No genuine new finding discovered mid-build beyond the already-filed #3599 itself.
- `git status --porcelain` clean — no files touched this session.
