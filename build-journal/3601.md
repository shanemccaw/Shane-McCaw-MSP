# #3601 — Build Queue: show and navigate to what a build itself blocks (reverse of the existing blocker ghost cards)

- **Status:** ✅ DONE 2026-09-11T02:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** ab64531cd (implementation), c10d85919 (IN FLIGHT bookend), 78be01e02/f97aea375 (prior session's BLOCKED bookend)

## Log
- 2026-09-11T02:00:00Z ⏳ IN FLIGHT (prior session) — re-checked #3599 (declared blocker) had not landed; self-blocked, no code changes.
- 2026-09-11T02:05:00Z 🛑 BLOCKED (prior session) — labeled `blocked`, posted evidence comment, left the real `blocked_by` edge in place.
- 2026-09-11T02:10:00Z ⏳ IN FLIGHT — re-dispatched. Confirmed #3599 is now genuinely `closed`/`complete` (commit `455e6f753` on main). Removed the now-stale `blocked_by` edge #3601→#3599 and the `blocked` label, restored `in-flight`.
- 2026-09-11T02:20:00Z ✅ DONE — commit `ab64531cd`. Implemented per the full issue body, all in `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`:
  1. **`ComputeReverseBlocks(List<QueueItem> allItems)`** — inverts the existing forward `LiveBlockedBy` relationship: for every real queue item still "queued" whose live (open-issue-filtered) blocked-by set names another item, records that item under the blocker's own GitHub number. Computed from `rawItems` (the full unfiltered queue RenderQueue was called with), not the currently-filtered `items`, so the result is correct regardless of which status filter is active — same reasoning #3599 already established for `FindLiveNodeForBlocker`.
  2. **`_reverseBlocks` field** — recomputed every `RenderQueue` pass alongside the existing `_downstreamBlockCounts`, so the list reflects current real state on every refresh (live updates, no stale snapshot).
  3. **`BuildBlocksRow(List<QueueItem>)`** — renders a "🔗 Blocks: #N, #M" row of real clickable pills, one per blocked item, wired into `BuildQueueCard` right after the existing forward blocker ghost-card row. Not gated on this item's own status (a running/verifying build routinely blocks a queued one).
  4. **Navigation** — each pill's click handler calls the existing `RevealQueueItem(blocked.Id)` (#3599's own cross-filter-aware navigation) — no navigation logic reimplemented here, per the issue's own instruction.

  **Verification:** `dotnet build BuildConsole.csproj -c Debug` — 0 errors, same 6 pre-existing unrelated `FocusModeBar` warnings #3599 already noted. `desktop/BuildConsole` is out of scope for test manifests per CLAUDE.md, so a clean build is the verification here, same as #3599's own precedent. No live interactive click-through was run this session (no running BuildConsole instance with a real multi-blocked scenario available); reviewed by hand against the issue's own 4-point fix list and verification section instead.

  **Finding filed:** #3617 (parented under #1788, `bug` label, board status "AI Batter Up") — the pre-existing "⛓ blocks N" critical-path badge (`ComputeDownstreamBlockCounts`, unrelated to this issue, already shipped) has the same filter-scoping bug #3599 fixed for ghost cards: it's computed from the filtered `items` list rather than the full queue, so it can undercount/read 0 for a blocker whose downstream dependent sits under a different active filter — now visibly inconsistent with this issue's own correctly-scoped "Blocks:" row on the same card. Not fixed here — separate UI element, outside #3601's scope.

  Branch confirmed merged into `origin/main` (`verify-branch-merged.mjs`). Working tree clean (`git status --porcelain` empty).
