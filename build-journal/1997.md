# #1997 — Batter Up silently hides any item with a bt_build_queue row

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** db0ad29174a37a2c26186344d36f644c2de369bb (on origin/main)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Reconcile Batter Up dedup drop against the real queue-row
  status: hide only while the row is live/landed; a failed/canceled row lets the item
  reappear. Log every drop on the `batter-up` channel and surface the suppressed count
  in the panel header. Investigate #1994's trace in bt_build_queue and the logs.
- 2026-08-31 ✅ DONE — Shipped in `db0ad29`. `BatterUpQueueService.RefreshAsync` now
  reconciles each dedup candidate against its real `bt_build_queue` status:
  live/landed (queued/parked/running/verifying/done/…) → hidden **and counted**; a
  terminal-with-no-work-landed row (failed/canceled) no longer vanishes — the item
  reappears carrying `TrackedTerminalStatus`/`TrackedTerminalRowId` with a red
  "↺ FAILED / ↺ CANCELED" pill, and a manual Queue click re-queues that exact row
  (reuseRowId) while free-flow never auto-requeues it (no fail-loop). Every drop logs
  on the `batter-up` channel with the issue number + queue row id/status, and the
  panel header renders "N tracked, hidden" so an empty lane ≠ a fully-suppressed lane.
  Verification: `dotnet build …Release` clean (0/0). Against the real bt_build_queue
  (github-linked latest-per-issue): 47 rows (25 failed + 22 canceled, e.g. row 971 /
  #1873 canceled) now reappear vs the old silent-drop; 798 (done/queued/running/
  verifying/parked) stay hidden but are now counted+logged. Live WPF UI NOT relaunched
  (Shane's BuildConsole is live) — reconcile decision proven deterministically against
  real DB rows, not via the running app.
- 2026-08-31 — #1994 investigation: the issue's premise is **disproven**. Queue row
  1036 is `done`/exit 0 (session f9a3a11c, ran 10:48→10:58). `build-journal/1994.md`
  and commit `d978aedec` (Git #1994) are on origin/main; issue #1994 is CLOSED /
  `complete`. The absent `agent/1994-*` branch is normal post-merge worktree cleanup
  (work committed directly to main). It left Batter Up correctly (a `done` row stays
  hidden). No separate "reported queued but never launched" bug exists here — nothing
  filed.
