# #2370 — Panel: runs newest-first with timestamps and counts

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 65874a074 (rebased onto main as efbe2895c)

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Audited Shot Vault chain (#2367-#2372). #2372 (per-shot Copy,
  DONE) already reads `ShotVaultService.ListShots()` newest-first at single-shot granularity and its
  own doc-comment explicitly defers real "run" grouping to this issue. #2368 (search) and #2369
  (tags) still open/unbuilt; no existing run/session concept anywhere in ShaneBuilder — a shot is
  just a loose timestamped PNG from `DesktopScreenClipService`. Building: group shots captured close
  together in time into a "run", newest-first, each run showing a real timestamp and a real shot
  count, wired into the existing `ShotVaultPanelBody`/`RenderShotVaultPanel` surface rather than a
  parallel one.
- 2026-09-02T20:05:00Z ✅ DONE — Added `ShotVaultRun` + `ShotVaultService.ListRuns()`
  (`desktop/ShaneBuilder/Services/ShotVaultService.cs`): groups the real, newest-first shot list from
  `ListShots()` into runs by a named `RunGap` (5 min) threshold between consecutive shot timestamps —
  no persisted run id exists anywhere, so this is a real, honest in-memory grouping over the actual
  file timestamps, not fabricated data. Runs are returned newest-run-first, each carrying its own
  real shot list (also newest-first) and a real `Count`/`NewestUtc`/`OldestUtc`.
  `MainWindow.ShotVaultPanel.cs`'s `RenderShotVaultPanel` now renders one real header per run (single
  timestamp for a 1-shot run, oldest–newest span for a multi-shot run, plus a real shot-count chip
  reusing the existing `DetectionMetaChip` style) with that run's shot rows nested underneath,
  preserving #2372's per-shot Copy and #2373's "Send to tab" (flattens the same real run list back to
  a flat shot list for its snapshot) unchanged. Verified: `dotnet build ShaneBuilder.csproj -c Debug`
  — Build succeeded, 0 warnings, 0 errors. `git status --porcelain` clean; branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` before writing DONE.
