# #4220 — SqlDocumentView.xaml raw hex #11111B has no matching brush

- **Status:** ✅ DONE 2026-09-15T05:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** eecee4e3d4d78e620185d722291be0cf16169cbd (rebased onto main as 647461d3ef5d60cd953ccd53d3f515f04e92a6ce)

## Log
- 2026-09-15T04:50:00Z ⏳ IN FLIGHT — adding a real `OnAccentTextBrush` (#11111B) resource to
  `Themes/Colors.xaml` for the contrast-text-on-accent-chip role, and wiring
  `SqlDocumentView.xaml`'s two `SegmentedToggleBtn` checked-state Foreground setters
  (lines 29, 41) to it instead of the raw hex literal.
- 2026-09-15T05:10:00Z ✅ DONE — added `OnAccentTextColor`/`OnAccentTextBrush` (#11111B) to
  `Themes/Colors.xaml` and wired both `SqlDocumentView.xaml` checked-state Foreground
  setters to it. Verified the two edited files are well-formed XML and the merge-order
  chain (App.xaml merges Colors.xaml before DarkTheme.xaml) makes the new key reachable
  app-wide. `dotnet build BuildConsole.csproj` currently fails, but on a pre-existing,
  unrelated XAML tag mismatch in `GitModeContainer.xaml` (never touched by this build) —
  filed as #4231 under #1202, board status set to AI Batter Up. Merged onto `origin/main`
  at 647461d3; `verify-branch-merged.mjs` confirms ancestry. Working tree clean.
