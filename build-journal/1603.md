# #1603 — Account Security: surface login history (currently fetched, 99% discarded)

- **Status:** ✅ DONE 2026-09-10T17:14:14Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 8cf05876f (widen wire type + UI section, on `main` as 053555bc3 after rebase)

## Log
- 2026-09-10T17:10:15Z ⏳ IN FLIGHT — was previously held (blocked_by #1625, the Design
  export issue); #1625 closed on 2026-09-06 and its `.dc.html` landed at
  `Design/portal/design_handoff_full_site/screens/Account Security.dc.html`. Removed the
  now-stale `blocked_by` edge and the `blocked` label before starting. Widening
  `WireLoginHistoryRow` in `useAccountSecurityLive.ts` to the real six-field
  `LoginHistoryView` shape (`session-tracking.ts:171-179`) and adding a "Sign-in history"
  section to `account-security.tsx` to render all six fields, with an honest empty state.
- 2026-09-10T17:14:14Z ✅ DONE — widened `WireLoginHistoryRow`/added `loginHistory` to
  `AccountSecurityLiveState` in `useAccountSecurityLive.ts` (real six fields: `id`,
  `loginMethod`, `browser`, `os`, `ipAddress`, `createdAt`, `revoked`; `null` only on a
  genuine read failure, a real empty array on an honest-empty result — same tri-state
  pattern the hook already used for `sessions`). Added a "Sign-in history" section to
  `account-security.tsx` rendering all six fields per row (method via
  `LOGIN_METHOD_LABELS`, the real six-value `login_method` enum from
  `lib/db/src/schema/msp.ts:696` — no invented labels), plus an honest "No recorded
  sign-ins." empty state distinct from loading/error. No schema/route change needed —
  the route already returned all six fields and already logs via
  `logger.child({ channel: "auth" })` (`auth.ts:32`); only the client was discarding four
  of them. Verified: `tsc --noEmit` clean, `vite build` succeeds (`PORT=5199 vite build`,
  996kB bundle, pre-existing chunk-size warning unrelated to this change).
  `git status --porcelain` clean. Branch confirmed merged into `origin/main`
  (`verify-branch-merged.mjs`) before writing DONE.
