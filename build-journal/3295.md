# #3295 — Phase 2: Real, tested one-time financial-data migration (requires Shane's go-ahead to actually run)

- **Status:** ⏳ IN FLIGHT 2026-09-09T01:40:13Z
- **Scope:** other (personal project — ShanesSurvival/shanes-life, Shane's milestone, not MSP)
- **Started:** 2026-09-09
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-09T01:40:13Z ⏳ IN FLIGHT — #3294 (Phase 1 audit) confirmed DONE with zero
  compat risk, so this is unblocked. Building the real, tested, idempotent
  export/import script per Shane's own simplification comment (plain `pg_dump` backup is
  adequate; drop the checksum ceremony; still confirm the script itself is correct and
  idempotent). The issue's own body carries a hard gate: "Do NOT execute the actual real
  data migration without Shane's own explicit, real confirmation in this issue or in
  chat." Neither his simplification comment nor the BUILD dispatch comment contains that
  explicit go-ahead (they ask for the script to be built/confirmed correct, not for it to
  be run against his real live data) — so this session writes and locally tests the
  script but does not execute it against shanes-life's real production Postgres (a
  remote Replit database this session also has no connection string for). Will report
  this stop plainly rather than assuming silent authorization.
