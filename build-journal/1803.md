# #1803 — Batter Up / AI Batter Up: restyle to match Build Queue card pattern

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** c9ce6b2cf (main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — reading BuildQueuePanel.xaml.cs BuildQueueCard (~1754) + critter block (~3400-3520), BatterUpPanel, AiBatterUpPanel before restyling.
- 2026-08-30 ✅ DONE — Added `BuildQueuePanel.CreateGenericCardMascot`/`BuildStatusPill`/`BuildGenericCardShell` (new internal static helpers, `BuildQueueCard` itself untouched) reusing the exact critter-vector pool, stable per-item variant selection, float/glow treatment, status-pill shape and card shell colors. Rewired `BatterUpPanel` and `AiBatterUpPanel` from plain-text `ItemsControl`/`DataTemplate` rows to code-built `Border` cards (mirroring how `BuildQueueCard` itself builds cards) with the critter mascot on the right, status pill + issue badge on top, title/detail/blocked-reason rows, and (AI Batter Up) Yes/No as real in-card footer buttons. All existing data preserved: blocked-by numbers, TRACKED/QUEUED/NO BUILD states, model/effort/buildSet, Yes/No/Refresh still fully functional. Didn't touch `BuildQueueCard` or the main Build Queue panel. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings/errors), rebuilt again post-rebase to confirm. Branch rebased onto `origin/main` twice (main moved concurrently) and pushed directly — confirmed ancestor via `git merge-base --is-ancestor`. `git status --porcelain` clean.
