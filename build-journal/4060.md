# #4060 — Approved portal testimonials never reach the public /api/testimonials feed

- **Status:** ✅ DONE 2026-09-14T15:35:04Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** b54b670e7 (real work), d142587df (IN FLIGHT bookend)

## Log
- 2026-09-14T15:34:27Z ⏳ IN FLIGHT — extend public `GET /testimonials` feed to union approved `customer_testimonials` rows (status=approved, kind=testimonial, permission_to_publish=true), same first-name-only author shape as `project_closures`; fix admin CRM Testimonials page's "Published" grouping to read real approval status for portal rows instead of `permissionToPublish` intent.
- 2026-09-14T15:35:04Z ✅ DONE — `GET /testimonials` (artifacts/api-server/src/routes/admin-testimonials.ts) now unions closure rows and approved portal `customer_testimonials` rows, sorted together, same clientFirstName shape (projectType null for portal rows since they aren't tied to a project). Admin CRM Testimonials page (artifacts/admin-panel/src/pages/crm/Testimonials.tsx) now derives "Published" from `status === 'approved'` for portal rows (still `permissionToPublish` for project_closure rows, which have no review step); added a "Pending Review" section so pending portal rows keep surfacing with their approve/reject controls instead of falling into "Published"; corrected the "Awaiting Permission" section's badge for rejected-but-permitted portal rows. Verified: `tsc --noEmit` clean on both `artifacts/api-server` and `artifacts/admin-panel` (no errors in either touched file). Confirmed `customer_testimonials` real column shape (status/kind/permission_to_publish/body/created_at) directly against local Postgres via DATABASE_URL — table is currently empty locally, so no live row was rendered through the feed, but the query shape matches the real schema and the same query pattern already used by `/admin/testimonials/all`. No fixture/hardcoded data. `git status --porcelain` clean. Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0).
