# #3047 — vitest.config.ts explicit include allowlist silently swallows new test files

- **Status:** ✅ DONE 2026-09-06T16:50:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 816f1be4f (glob include + exclude + logger mock fixes), c9b60b8c4 (merge into main)

## Log
- 2026-09-06T18:40:00Z ⏳ IN FLIGHT — Confirmed the systemic gap is worse than the single reported case: `artifacts/api-server/vitest.config.ts`'s `test.include` allowlist has 326 entries but there are 354 real `*.test.ts` files on disk under `src/` — 28 files (beyond the 2 already fixed incidentally by #2524) have never run in CI/local test runs. Converting `include` to a real glob with an explicit exclude list, then running the full suite before/after to compare real counts.
- 2026-09-06T16:50:00Z ✅ DONE — Converted `test.include` from the ~326-entry allowlist to a real glob (`src/**/*.test.ts`). Of the 28 orphaned files, 20 turned out to genuinely use the `node:test` framework (not vitest) and are already run separately by this package's own `test` script via `node --test` — added to `exclude` with the real reason, not silently dropped. The remaining 8 were real, never-run vitest files; 2 of them (`portal-offers.test.ts`, `script-ingestion.test.ts`) failed on a `vi.mock("../lib/logger")` missing `.child()` — fixed to match this repo's established stub-with-self-referencing-child mock pattern.
  - Before (old allowlist, full local `.env.local` env, live local Postgres): 326 files, 324 passed / 2 failed (both the pre-existing #2859 live-db timeout), 5195/5197 tests passed.
  - After (glob + exclude, same env): 334 files, 331 passed / 3 failed, 5261/5265 tests passed. No silent drop — strictly more real, passing signal (+66 tests), and the file-count math checks out (354 − 20 excluded = 334).
  - Filed 4 new findings surfaced by this fix, all parented under #3047's own Feature parent #1490 and set to "AI Batter Up": #3063 (real retention-purge coverage gap — the #2859 live-db test actually completed instead of timing out, and found `active_directory_ou_assignment_requests`/`status_reports` tenant-scoped columns with no purger and no exemption — labeled `security`), #3064 (script-ingestion.test.ts mock call-count leak, 2 real failing assertions), #3065 (package.json's own `node --test` invocation names 2 deleted files), #3066 (index.boot-smoke.test.ts intermittently joins the closed #2877 full-suite parallel-timeout class now that file count grew).
  - Merged into `main` clean (one real conflict in vitest.config.ts against a concurrent allowlist addition, resolved by keeping the glob — the added file is a standard vitest test already covered by it). `verify-branch-merged.mjs` confirms ancestor of `origin/main`. `git status --porcelain` clean.
