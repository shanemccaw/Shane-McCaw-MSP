# #3702 — Build Queue toolbar: remove Refresh Queue + the ⋯ menu, add Play/Pause and one combined spinning-refresh icon

- **Status:** ✅ DONE 2026-09-11T22:10:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** d0125db52

## Log
- 2026-09-11T22:06:00Z ⏳ IN FLIGHT — confirmed #3701 (real-blocker, same header region) is CLOSED/complete on main (af5d28f18). Starting: remove `BtnRefreshQueueLocal` + the whole `BtnQueueMenu` "⋯" overflow menu from `Controls/BuildQueuePanel.xaml`, add a real always-visible Play/Pause icon button (promoted `MiPauseQueue` logic) and one combined spinning-refresh icon button (Git refresh, then Queue refresh, sequential, icon-only) in their place.
- 2026-09-11T22:10:08Z ✅ DONE — `d0125db52`. Removed `BtnRefreshQueueLocal` and the whole `BtnQueueMenu`/`QueueOverflowMenu` (`MiPauseQueue`, `BtnRefreshGitHubTiles`, `BtnPinQueue`) from `Controls/BuildQueuePanel.xaml`. Added `BtnPlayPauseQueue` (icon-only, wired to the exact same `BtnPauseQueue_Click`/`SyncPauseToggleVisual`/`QueueWatcherService.SetPaused` mechanism `MiPauseQueue` used) and `BtnRefreshCombined` (icon-only, `BtnRefreshCombined_Click`) — one real, sequential Git-then-Queue refresh: awaits the exact same scope the old `BtnRefreshGitHubTiles_Click` covered (Git Board + Batter Up + AI Batter Up + epic issues + in-flight + focus progress), then once that fully completes, the same local-only Postgres re-read `BtnRefreshQueueLocal_Click` used to do. Real spin animation for the whole span via a code-behind-owned `AnimationClock` (`StartRefreshCombinedSpin`/`StopRefreshCombinedSpin`, stopped via `Controller.Stop()` per the #3689 Forever-clock lesson in `KeyedSlotCardHost`) — not `BeginAnimation(dp, null)`, which only unhooks without stopping the clock. `BtnPinQueue`'s menu item is gone; #3701's collapse-arrow chevron (`BtnCollapseQueue`) is the sole surviving `TogglePin()`/`_isPinned` caller. `SetGitHubTilesRefreshInProgress` (called by MainWindow's `FullGitRefreshRequested` wrapper) now targets `BtnRefreshCombined`; a `_combinedRefreshInFlight` bool guards reentry since that wrapper can flip `IsEnabled` back to true mid-way through the combined handler's own Step 2. Updated stray comment references to the old button names in `AiBatterUpPanel.xaml`, `BatterUpPanel.xaml`, `LeftSidebar.xaml.cs`, `MainWindow.xaml.cs`. Verification: `dotnet build -c Debug` in `desktop/BuildConsole` — Build succeeded, 0 errors (10 pre-existing warnings, unrelated to this change). `verify-branch-merged.mjs` confirms `d0125db5` is an ancestor of `origin/main`. `git status --porcelain` clean. No findings filed — no genuine new bug/gap surfaced during this build.
