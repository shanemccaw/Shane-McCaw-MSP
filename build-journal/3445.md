# Issue #3445 — msp-dlq.ts bulk-replay route double-prefixed

- **Status:** ✅ DONE 2026-09-11T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-11T00:00:00Z — Session started. Confirmed live bug: `router.post("/api/msp/dlq/bulk-replay", ...)` at
  `artifacts/api-server/src/routes/msp-dlq.ts:190` is double-prefixed (router already mounted at `/api`), making
  the real live path `/api/api/msp/dlq/bulk-replay` instead of `/api/msp/dlq/bulk-replay`. Fixing per issue body:
  drop the leading `/api` to match the file's three sibling routes.
- 2026-09-11T00:20:00Z — ✅ DONE — Fixed at `6f4a208cb` (`Fix double-prefixed bulk-replay DLQ route (#3445)`):
  changed `router.post("/api/msp/dlq/bulk-replay", ...)` to `router.post("/msp/dlq/bulk-replay", ...)`. `tsc
  --noEmit` clean. Live-hit the still-running (unmerged) dev server: old double-prefixed path
  `/api/api/msp/dlq/bulk-replay` still returns 401 (unpatched process, expected pre-merge), new path
  `/api/msp/dlq/bulk-replay` returns 404 pre-merge — will resolve to the same 401-reaches-route behavior once
  BuildConsole merges this branch into the dev-server checkout and restarts. `verify-branch-merged.mjs` confirms
  `agent/3445-q2303` is an ancestor of `origin/main`. Working tree clean. No UI consumes this route (grep-
  confirmed per issue body), so no other code needed touching.
