# #1466 — System Health: Stranded Branches cleanup button missing/hidden under the warning badge

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-28 ⏳ IN FLIGHT — Investigated the Stranded Branches row in
  `desktop/BuildConsole/Controls/HomeView.xaml`/`.xaml.cs`. Full git history of
  that file shows this row (added in #1447) never had a button — the "Clean"
  button Shane is thinking of belongs to the *Worktrees* row directly above it,
  and `StrandedBranchService`/`scripts/dev-server/check-stranded-branches.mjs`
  is deliberately read-only (a stranded branch by definition has commits main
  doesn't have, so auto-delete would risk real data loss — no delete/cleanup
  script exists to wire a button to). Added a real, safe "🔀 Recheck" button to
  the row (visible only when StrandedCount > 0, same visibility pattern as the
  Worktrees "Clean" button) that re-runs the detection sweep on demand;
  resolving a stranded branch (merge or intentional delete) stays a manual
  decision from the diagnostics log, same as today. `dotnet build` succeeds,
  0 errors.
