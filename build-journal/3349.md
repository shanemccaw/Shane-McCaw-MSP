# Issue #3349 — GitHubRateLimitHandler misreads GraphQL 200-with-RATE_LIMITED body as success

- **Status:** ✅ DONE 2026-09-09T22:15:01Z
- **Scope:** buildconsole

## Log
- 2026-09-09T22:11:55Z — Session started. Fixing GitHubRateLimitHandler.SendAsync so a GraphQL HTTP 200 response whose JSON body carries `{"errors":[{"type":"RATE_LIMITED",...}]}` is recognized as a rate-limit signal (RecordRateLimited) instead of RecordSuccess(), reusing GitHubRateLimitCircuit.LooksLikeRateLimit. Per issue #3349.
- 2026-09-09T22:15:01Z — ✅ DONE — Added `IsGraphQlRateLimitedBodyAsync` to `desktop/BuildConsole/Services/GitHubRateLimitHandler.cs`, scoped to the `/graphql` request path: buffers the response body once, re-attaches an equivalent `ByteArrayContent` so downstream readers (`PostGraphQLIssuesAsync` et al.) see identical content, and checks for a `RATE_LIMITED` error type alongside `GitHubRateLimitCircuit.LooksLikeRateLimit(body)` before calling `RecordRateLimited("GraphQL body")` instead of falling into the old `RecordSuccess()` branch. `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. No test manifest required (BuildConsole/WPF is out of scope per CLAUDE.md). Commit `aed228ac9d56266d11316c3d2c4d0134fdc2ebe1` on `agent/3349-q2074`, fast-forward-pushed directly to `origin/main` (was already an ancestor-clean FF); `verify-branch-merged.mjs` confirms OK — merged. `git status --porcelain` clean.
