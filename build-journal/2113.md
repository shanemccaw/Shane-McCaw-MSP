# #2113 — Unify error envelope shapes on config-state (and shared middleware)

- **Status:** ⏳ IN FLIGHT 2026-09-04T06:20:05Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T06:20:05Z ⏳ IN FLIGHT — confirmed `apiError()` (nested `{ error: { code, message, ... } }`)
  vs `requireAuth.ts`'s bare `{ error: "string" }` at ~10 response sites. Grepped the rest of
  api-server for other genuine Express middleware doing the same bare-string pattern — found
  `app.ts`'s global catch-all error handler also bare-stringing 500s. `mspRateLimit.ts` already
  nests correctly. `msp-entitlement.ts`/`portal-addon-entitlements.ts` use a third shape
  (`{ error: "msg", code, feature, upgradeUrl }` — top-level code, not bare, not nested) that
  no frontend consumer keys off directly; leaving those alone to avoid unrelated blast radius,
  noting as a possible follow-up rather than folding into this fix.
