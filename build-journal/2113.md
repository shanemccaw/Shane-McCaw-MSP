# #2113 — Unify error envelope shapes on config-state (and shared middleware)

- **Status:** ✅ DONE 2026-09-04T06:28:15Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** dae343566

## Log
- 2026-09-04T06:20:05Z ⏳ IN FLIGHT — confirmed `apiError()` (nested `{ error: { code, message, ... } }`)
  vs `requireAuth.ts`'s bare `{ error: "string" }` at ~10 response sites. Grepped the rest of
  api-server for other genuine Express middleware doing the same bare-string pattern — found
  `app.ts`'s global catch-all error handler also bare-stringing 500s. `mspRateLimit.ts` already
  nests correctly. `msp-entitlement.ts`/`portal-addon-entitlements.ts` use a third shape
  (`{ error: "msg", code, feature, upgradeUrl }` — top-level code, not bare, not nested) that
  no frontend consumer keys off directly; leaving those alone to avoid unrelated blast radius,
  noting as a possible follow-up rather than folding into this fix.
- 2026-09-04T06:28:15Z ✅ DONE — commit dae343566. Converted all ~10 bare-string response sites in
  `requireAuth.ts` (requireAuth/requireAdmin/requireRole/requireMspScope/requireCustomerScope) and
  `app.ts`'s global catch-all 500 handler to `apiError()` with the appropriate `ApiErrorCode`.
  Left `app.ts`'s `ConsentRevokedError` branch untouched — separately tested top-level
  `{ code, tenantId, reAuthorizeRequired }` contract, not the generic envelope. Verified: `tsc
  --noEmit` clean on the changed files (pre-existing unrelated `managerUserId` errors in
  notification-center.ts/portal-team.ts are stale worktree `@workspace/db` dist drift from
  commit 609cc5ae7, not caused by this change), `requireAuth.test.ts` 7/7 passing. Rebased onto
  origin/main (which had picked up #2100's malformed-body fix, also in app.ts) and pushed
  directly to main; `verify-branch-merged.mjs` confirms ancestor. Filed #2832 (parented under
  Feature #2492, board status "AI Batter Up") for the remaining route-handler-level bare-string
  sites (dashboard-data.ts, dashboard-executive-summary.ts, msp-launch-control.ts, msp-team.ts,
  msp-sales-offers.ts) outside requireAuth.ts that reproduce the same pattern. Working tree clean.
