# Build Journal — Issue #1493

- **Status:** ✅ DONE
- **Scope:** platform
- **Title:** SOPs / Runbooks — contract extraction pack

## Log

- 2026-08-29 — Session start. Pulled #1493, #1485, #1577, #1578, #1488 (jointly-architected
  sibling) in full, plus portal-sops.ts, lib/portal-sops.ts, portal-runbooks.ts,
  lib/portal-hold-windows.ts, the Drizzle schema for msp_sops / msp_sop_runs /
  portal_runbooks / portal_runbook_steps / portal_hold_windows / portal_hold_window_events,
  and every frontend consumer (useSops.ts, useRunbooks.ts, sopCreate.ts, sopHubData.ts).
  Read the risk-register pack (#1487) as the format precedent. Found one orphaned live
  endpoint: GET /api/portal/hold-windows/:holdId/events has no frontend consumer.
- 2026-08-29 — ✅ DONE — Filed sub-issue #1620 for the orphaned events endpoint and linked it
  as a real GitHub sub-issue of #1493. Wrote `docs/sops-contract-pack.md`: the two routes'
  full wire contract (SOPs library/runs + Runbooks/hold-windows), the joint #1493/#1488
  architecture boundary (why msp_sops+msp_sop_runs and portal_runbooks+portal_runbook_steps
  are two objects and where the line actually falls), real enum unions cited to their
  write-path validators, cross-surface edges (hold decision → change request, the
  Oversharing Sites producer into portal_runbooks), the CURRENT vs DECIDED table against
  sub-issues #1556–#1560, four flagged open gaps (notification transport, standard/
  non-standard classification, the oversharing-catalogue-vs-lifecycle-set question, dead
  `portal_runbooks.status` column), the honest tri-state, and the forbidden list. Read-only:
  no product code, schema, or UI touched. Commits: `3e0d66e6f` (IN FLIGHT bookend),
  `3b6b14b7f` (the pack). Rebased and pushed directly to `main` after two concurrent-push
  retries; `verify-branch-merged.mjs` confirms `agent/1493-q787` (3b6b14b7) is an ancestor of
  `origin/main`.
