# #2738 — Webhooks (MSP Console): generate the contract pack from the real outbound-webhooks backend

- **Status:** ✅ DONE 2026-09-04T01:00:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** `1bffa1b98` — Extract MSP Console Webhooks contract pack from real backend (#2738)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — extracting `docs/msp-console-webhooks-contract-pack.md` from
  the real `msp-console-webhooks.ts` backend (#2704) per the #1642 pattern. Read-only on product
  code; the only file written is the pack itself, plus any sub-issues filed for real gaps found.
- 2026-09-04T01:00:00Z ✅ DONE — wrote `docs/msp-console-webhooks-contract-pack.md` (306 lines):
  the two surfaces (owner `webhooks.ts` vs operator `msp-console-webhooks.ts`), per-endpoint wire
  contract for all 6 operator routes (event-types/list/get/disable/enable/deliveries), the shared
  `WebhookRow` shape incl. its two computed fields (`unrecognizedEventTypes`, `disabledByName`),
  real enum unions (`ownerType`, delivery `status`, the event catalog incl. the
  subscribable-but-dead `signal.fired` case), the disable/enable ↔ owner-PATCH reset cross-surface
  edge, the honest-empty contract (confirmed live: 0 rows in `outbound_webhooks` and
  `outbound_webhook_deliveries`, queried against local `DATABASE_URL` 2026-09-04), a forbidden
  list, and 3 genuine open product questions for Design/Shane (none of which are extraction gaps —
  no new sub-issue filed; #2704's backend build was already clean, this pass surfaced only
  already-known #1693/#1607 open questions, not new bugs). Superseded
  `docs/msp-webhooks-inbound-contract-pack.md` (#2610) for this Feature, which had extracted the
  wrong file. Read-only build — no product code, schema or migration touched. Branch verified
  merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
