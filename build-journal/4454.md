# #4454 — Portal root shows "please sign in" interstitial instead of landing directly on sign-in

- **Status:** ✅ DONE 2026-09-17T02:35:00Z
- **Scope:** platform

## Log
- 2026-09-16T00:00:00Z — Posted: 2026-09-16T00:00:00Z — Starting. Investigating Portal root route/redirect logic to remove the "please sign in" interstitial and land unauthenticated visitors directly on login.tsx.
- 2026-09-17T02:35:00Z — ✅ DONE — Root cause: `RequireAuth` in `artifacts/portal/src/App.tsx` (all `ProtectedRoutes`, including `/`) rendered a "Sign in required" panel with a "Go to sign in →" link instead of routing straight to the login form. Replaced it with `<Redirect to="/login" />` (wouter). No return-to-URL logic existed anywhere in `login.tsx` to preserve or regress — confirmed via grep before making the change. Updated `test-manifests/portal/sops-runbooks.json` U2 (the manifest asserting this gate) to check for the login form directly instead of the retired `require-auth-panel` testid, added a scoped `root-redirect-4454` sessionTag to U1/U2, and ran it live via `shaneapp://runTest` after merging into the dev-server checkout — 7/7 steps passed, confirming `/portal/sops` (unauthenticated) now lands directly on `login-email` with no interstitial hop. Commits: a7d00d6f3 (App.tsx + manifest selector fix), 14cb5161b (sessionTag scoping), and the commit recording `lastVerifiedAgainstCommit` — all rebased onto main; final pushed HEAD is cab57e30f. `tsc --noEmit` clean. Branch verified merged into origin/main; working tree clean. No findings filed — no other bugs surfaced during this build.
