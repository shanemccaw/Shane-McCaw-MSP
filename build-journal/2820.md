# #2820 — Config-change verdict/lifecycle endpoints have no consumer

- **Status:** ⏳ IN FLIGHT 2026-09-12T20:41:26Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T20:41:26Z ⏳ IN FLIGHT — Investigation: #2759 built `attribution`
  (verdict/crRef/rbdRef/matchScope) + `lifecycle` (open/resolved/reopened) onto
  `config_diff_changes` via `config-change-attribution.ts` /
  `config-state-views.ts::readDiffChanges()`, exposed on
  `GET /api/portal/config-state/changes(/:diffId)` and
  `GET/POST /api/msp/config-state/diffs(/:diffId)(/attribution)`. #2820's own body
  said nothing reads it — true when filed (2026-09-04), but #3004 (2026-09-06,
  commit `cc67588c2`) shipped the real `artifacts/portal/src/pages/config-state.tsx`
  page reading `/api/portal/config-state/changes*` and already surfaces the
  diff-level `attribution` roll-up (verdict counts, CR/risk-decision refs) via
  `configStateWire.ts`/`configStateLive.ts`. What #3004 did NOT do: the real gap
  named in #2820's own build prompt — `WireChangeRow.attribution` only carries
  `verdict` (see `configStateWire.ts:150-157`), dropping `crRef`/`rbdRef`/
  `matchScope`, and there is no `lifecycle` field on the wire type at all, so the
  change-group drill-down panel (`openChangeGroupPanel` in `configStateLive.ts`)
  never renders per-change attribution/lifecycle even though the API already
  returns it. MSP Console side: confirmed zero consumer exists at all
  (`grep -rln "config-state" artifacts/msp-console/src` — no hits); the MSP
  Console design (`Design/MSP_Console/design_handoff_msp_console/MSP Console.dc.html`)
  shows the operator config-state surface (snapshots/diffs/baselines/registry/
  tenant book) still at its designed EMPTY-STATE stage, and building the real
  operator diff view + re-run trigger is a materially larger, separate piece of
  work than completing the Portal wiring gap. Plan: land the Portal per-row
  attribution/lifecycle wiring now; file the MSP Console operator diff view +
  re-run trigger as a real sibling sub-issue of Feature #1850, per #2820's own
  explicit instruction for this split.
