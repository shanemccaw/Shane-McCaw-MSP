# #2820 — Config-change verdict/lifecycle endpoints have no consumer

- **Status:** ✅ DONE 2026-09-12T20:57:19Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** `09eb087be` (real code — WireChangeRow/configStateWire.ts/configStateLive.ts),
  `95f7c2642` + `d48f242c5` (manifest hash bookkeeping), all on `main`

## Log
- 2026-09-12T20:41:26Z ⏳ IN FLIGHT — Investigation: #2759 built `attribution`
  (verdict/crRef/rbdRef/matchScope) + `lifecycle` (open/resolved/reopened) onto
  `config_diff_changes` via `config-change-attribution.ts` /
  `config-state-views.ts::readDiffChanges()`, exposed on
  `GET /api/portal/config-state/changes(/:diffId)` and
  `GET/POST /api/msp/config-state/diffs(/:diffId)(/attribution)`. #2820's own body
  said nothing reads it — true when filed (2026-09-04), but #3004 (2026-09-06,
  commit `cc67588c2`) shipped the real `artifacts/portal/src/pages/config-state.tsx`
  page reading `/api/portal/config-state/changes*` and already surfaces the
  diff-level `attribution` roll-up (verdict counts, CR/risk-decision refs) via
  `configStateWire.ts`/`configStateLive.ts`. What #3004 did NOT do: the real gap
  named in #2820's own build prompt — `WireChangeRow.attribution` only carries
  `verdict` (see `configStateWire.ts:150-157`), dropping `crRef`/`rbdRef`/
  `matchScope`, and there is no `lifecycle` field on the wire type at all, so the
  change-group drill-down panel (`openChangeGroupPanel` in `configStateLive.ts`)
  never renders per-change attribution/lifecycle even though the API already
  returns it. MSP Console side: confirmed zero consumer exists at all
  (`grep -rln "config-state" artifacts/msp-console/src` — no hits); the MSP
  Console design (`Design/MSP_Console/design_handoff_msp_console/MSP Console.dc.html`)
  shows the operator config-state surface (snapshots/diffs/baselines/registry/
  tenant book) still at its designed EMPTY-STATE stage, and building the real
  operator diff view + re-run trigger is a materially larger, separate piece of
  work than completing the Portal wiring gap. Plan: land the Portal per-row
  attribution/lifecycle wiring now; file the MSP Console operator diff view +
  re-run trigger as a real sibling sub-issue of Feature #1850, per #2820's own
  explicit instruction for this split.
- 2026-09-12T20:57:19Z ✅ DONE — Portal read-side landed:
  `artifacts/portal/src/components/configStateWire.ts` now carries the full real
  `WireChangeAttribution` (verdict, changeRequestId, crRef, riskDecisionId, rbdRef,
  matchScope, matchCount) and `WireChangeLifecycle` (status, firstDetectedAt,
  lastDetectedAt, resolvedAt, reopenedAt, reopenCount) shapes on `WireChangeRow`,
  field-for-field matched against `lib/db/src/schema/config-attribution.ts` and
  `readDiffChanges()` in `config-state-views.ts`. Added `buildChangeDetailRows()`
  (+ `describeChangeAttribution`/`describeChangeLifecycle` helpers) rendering real
  per-change identity + verdict/CR-or-RBD-ref + lifecycle status; wired into
  `configStateLive.ts`'s `openChangeGroupPanel`, replacing the coarser
  `groupChangesByKind()` (now dead, removed). Test manifest
  `test-manifests/portal/config-state.json` updated with an honest note on the
  change and no fabricated new assertions (see below).
  Verification: real `tsc -p tsconfig.json --noEmit` (0 errors) and real
  `vite build` (succeeds, PORT=5199) against `artifacts/portal`. Also ran the REAL
  `shaneapp://runTest?file=portal/config-state.json` protocol (not skipped) —
  9/10 steps passed (all apiTests real 200/401/404s against the live local
  api-server + a real testbed login); the one uiStep failed, and investigating why
  surfaced a genuine, pre-existing, repo-wide bug: BuildConsole's
  `DevServiceRouting.ServiceKeyForRoute` only recognizes `/portal/`-prefixed or
  slug-at-root paths as Portal, so this manifest's (and literally every other
  `artifacts/portal` manifest's) bare-path `goto` targets (`/config-state`,
  `/billing`, `/account-security`, `/`, …) fall through to the Marketing default
  and silently test the wrong app (confirmed live: navigation landed on
  `localhost:5173`, Marketing's port, and the "actual" text matched Marketing's own
  `NotFound.tsx` copy verbatim). This is NOT a regression from this session's
  change — it's the first time any Portal manifest was ever actually run through
  `shaneapp://runTest` (every sibling manifest's own notes say "NOT YET RUN").
  Filed as **#3835** (bug, milestone v1.1, sibling sub-issue of Feature #1850,
  board status "AI Batter Up") with the real run evidence and file:line root
  cause. NOT locally live-verified end-to-end against real diff/attribution rows:
  this worktree's local Postgres has zero rows in `config_diffs` /
  `config_diff_changes` / `config_change_attributions` / `config_change_lifecycle`
  (checked directly via `psql "$DATABASE_URL"`) — no snapshot has ever been
  collected locally, the same real gap #3004 hit; no data exists to exercise the
  new per-row rendering against without fabricating it, which the HARD RULE
  forbids.
  MSP Console side: confirmed zero consumer exists
  (`grep -rln "config-state" artifacts/msp-console/src` — no hits; the operator
  surface in `Design/MSP_Console/design_handoff_msp_console/MSP Console.dc.html`
  is still at its designed empty-state stage). Filed as **#3836** (bug, milestone
  v1.1, sibling sub-issue of Feature #1850, board status "AI Batter Up") per
  #2820's own explicit split instruction, scoped to the operator diff view +
  attribution re-run trigger specifically (snapshots/baselines/registry/tenant
  book are separate, larger, out-of-scope-here chunks of the same still-empty
  design).
  `git status --porcelain` clean at completion. Branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` (exit 0) before this line was
  written.
