# #3652 — Disconnect api-server /admin/build-tracker + BuildConsole HTTP fallbacks from stale bt_ data

- **Status:** ✅ DONE 2026-09-11T14:55:00Z
- **Scope:** both
- **Started:** 2026-09-11
- **Commit(s):** 2430bcc3b

## Log
- 2026-09-11T14:35:36Z ⏳ IN FLIGHT — Decision (per issue body): disconnect, don't repair.
  Plan: (1) stub `artifacts/api-server/src/routes/admin-build-tracker.ts` to return an
  honest 410 "retired" response for all `/admin/build-tracker/*` routes, no `bt_*` table
  access left in the file; (2) remove/neutralize BuildConsole's `_db == null` HTTP
  fallback paths and the two HTTP-only calls (`GetIssuesForEpicAsync`,
  `StartupConnectivityService` board/in-progress probes) listed in the issue; (3) check
  admin-panel's Build Tracker screen usage and note its real state.
- 2026-09-11T14:55:00Z ✅ DONE — commit 2430bcc3b, pushed to main, verified merged.
  api-server `/admin/build-tracker/*` now returns an honest 410 "retired" JSON body
  for every method/path (no `@workspace/db`/`bt_*` reference left in the file).
  BuildConsole: removed `GetIssuesForEpicAsync` (BuildQueuePanel's epic-issues panel
  now reads GitHub sub-issues only) and `StartupConnectivityService`'s board/
  in-progress launch probes (now `Skipped` with an honest "retired (Git #3652)"
  reason instead of always-Degraded). admin-panel `buildTrackerStore.loadAll()` now
  checks `res.ok` and throws/surfaces a real error instead of `.json()`-parsing a
  410 retired body as `EpicRow[]`/`IssueRow[]`/`ChatRow[]`.
  Scope decision: the many other `_db == null` → `_api.*` HTTP-fallback branches
  across `QueueWatcherService.cs`, `BuildQueuePanel.xaml.cs`, `LeftSidebar.xaml.cs`,
  `MainWindow.xaml.cs` were left in place, not removed — they all DO have a working
  direct-DB path (`BUILD_DATABASE_URL`) as primary and only fall back when that's
  unconfigured; now that the api-server route they'd fall back to honestly 410s
  instead of silently serving stale bt_ data, they already satisfy the issue's own
  accepted alternative ("fail loudly/visibly ... rather than silently serve wrong
  data") without further edits, given "don't over-invest" — this whole path is
  being deleted, not repaired.
  Verification: `tsc --noEmit` clean on both `artifacts/api-server` and
  `artifacts/admin-panel`; `dotnet build BuildConsole.csproj` succeeded (0 errors,
  6 pre-existing unrelated warnings). `git status --porcelain` clean.
  `node scripts/dev-server/verify-branch-merged.mjs` — OK, merged into origin/main.
