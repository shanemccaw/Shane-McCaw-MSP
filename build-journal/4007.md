# #4007 — Consolidate ElementBugHistoryWindow (#3983) and BugScopeHistoryWindow (#3984)

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** buildconsole

## Log

- 2026-09-14T00:00:00Z — ⏳ IN FLIGHT — retargeting `ElementBugHistoryWindow.Show`'s call site in
  `MainWindow.TestMode.cs` to `BugScopeHistoryWindow`, adding the missing "🎨 Design" pill to
  `BugScopeHistoryWindow`'s card render (present in `ElementBugHistoryWindow` but missing from
  `BugScopeHistoryWindow`), then deleting `Controls/ElementBugHistoryWindow.cs`.
- 2026-09-14T00:20:00Z — ✅ DONE — commit `d1827c2f2` (pushed to `origin/main` as `235e4acd5`).
  Retargeted `MainWindow.TestMode.cs`'s `BugHistoryRequested` handler to construct
  `BugScopeHistoryWindow("Bug History", "<n> bug(s) logged against \`<selector>\`", bugs)` instead
  of calling `ElementBugHistoryWindow.Show`. Added the missing 🎨 Design pill to
  `BugScopeHistoryWindow.BuildCard` (`entry.IsDesign`), verified by diff that it was the only
  real gap between the two windows' rendered fields (status badge, resolution, GitHub link,
  closing build id, timestamps were already at parity). Deleted
  `Controls/ElementBugHistoryWindow.cs` — grep confirms zero remaining references anywhere.
  `dotnet build BuildConsole.csproj -c Debug` — succeeded, 0 warnings, 0 errors.
  `verify-branch-merged.mjs` — OK, merged into origin/main. `git status --porcelain` — clean.
