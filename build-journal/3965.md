# #3965 — Portal: Azure credential self-service — test + complete

- **Status:** 🛑 BLOCKED 2026-09-15T14:10:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** (none — verification only, no code changes)

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — Phase 5 of #3960: real e2e verification of GET
  /portal/azure-credential and POST /portal/azure-credential/rotate. #3964 (build) is
  CLOSED, unblocking this. #3530 (Key Vault billing disabled) is still OPEN — checking
  whether the KV write path is reachable before deciding how far verification can go.
- 2026-09-15T14:10:00Z 🛑 BLOCKED — Verified everything short of the actual persisted
  Key Vault write, against real synthetic data (two sibling `zz-test-azcred-3965-*`
  users on tenant 1 plus a real `azure_tenant_credentials` row, all deleted after):
  GET with no auth → 401; GET as the owning sibling → 200 with the metadata-only
  shape (no secret value, `expiresOn: null` — proves the KV read genuinely failed,
  live evidence #3530 is still down); GET as the *other* sibling login on the same
  tenant → same row, confirming `resolveSiblingUserIds` scoping; POST rotate with an
  empty secret → 400; POST rotate against a `certificate`-type row → 400 "Only
  secret credentials can be rotated"; POST rotate against no matching row → 404;
  POST rotate with no auth → 401. Did NOT attempt a rotate with a real secret value
  against a `secret`-type row — `AZURE_KEY_VAULT_URL` in this env is the real
  production `ShaneMcCawConsulting` vault (Git #1913 production Key Vault gate), and
  that write is exactly the piece #3530 blocks regardless. Wired a real `blocked_by`
  edge from #3965 → #3530 and applied the `blocked` label; also removed the stale
  `blocked_by` edge to #3964, which closed since this issue's dispatch comment was
  posted. No code changed this session — pure verification. Resume once #3530
  clears: mint a real secret credential row, rotate it with a real value, confirm
  the Key Vault write and the row's `updatedAt` bump.
