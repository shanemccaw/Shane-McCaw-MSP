# #3492 — Reachable admin UI entry point to create a document type

- **Status:** ✅ DONE 2026-09-10T22:34:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** b9dd18c6c (implementation), rebased onto origin/main at 98ea492dd

## Log
- 2026-09-10T22:27:59Z ⏳ IN FLIGHT — investigated: #3416 (Document Generator IDE
  removal) + #3417 (Insights Engine removal) independently removed both prior UI
  paths to `POST /api/admin/document-types`. Backend route + `DocumentTypesManager.tsx`
  (fully built create/edit form) both still exist but nothing mounts the manager.
  admin-document-types.ts's own comment already points at Simulator Studio's
  Documents node as the one live surface. Adding a "New Document Type" entry point
  there (matching Section 11 Assessments' openModal pattern) in
  `SimulatorLeftTree.tsx` + `ModalContext.tsx`, backed by the existing
  `POST /api/admin/document-types` route — no backend change needed.
- 2026-09-10T22:34:00Z ✅ DONE — added `openModal("new-document-type")` "+" button
  and right-click "New Document Type" item to the Documents section header in
  `SimulatorLeftTree.tsx`, plus a new `NewDocumentTypeModal` in `ModalContext.tsx`
  (key/label/category/pipelineCategory/service/sortOrder/isActive, POSTs to the
  existing audit-logged `/api/admin/document-types` route, dispatches
  `simulator-documents-updated` so the tree refreshes). No backend/schema change —
  the route already existed and needed only a caller. Verified: `tsc --noEmit`
  clean (exit 0); existing `SimulatorLeftTree.test.tsx` +
  `SimulatorCenterCanvas.test.tsx` (14 tests) pass unchanged. Sections/signal
  scoping remain edited afterward via the existing Documents-canvas PUT flow, by
  design (kept the create modal minimal, matching MonitorCheckEditorModal's
  precedent). No new findings filed — this was a pure UI-wiring gap, backend and
  DocumentTypesManager.tsx already existed. Branch verified merged into
  origin/main via `verify-branch-merged.mjs`; working tree clean.
