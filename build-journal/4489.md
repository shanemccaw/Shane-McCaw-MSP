# #4489 — AdminV2 Active Directory: tenant testbed toggle + manual Monitoring/Retainer package assignment (internal, DB-only)

- **Status:** ✅ DONE 2026-09-17T00:40:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** be6c1a3db (pushed as f2e444d1f on origin/main)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — investigating AdCustomerCanvas.tsx, admin-active-directory.ts, admin-services.ts, adKit.tsx conventions before implementing testbed toggle + package assignment swap
- 2026-09-17T00:40:00Z ✅ DONE — shipped both features against `artifacts/admin-panel/src/adminv2/screens/ad/canvases/AdCustomerCanvas.tsx` (not the legacy `/system/active-directory` page):
  - `PATCH /admin/active-directory/customer/:id` now also accepts `isTestbed?: boolean` (column already existed, was read but never editable); Profile section gets a "Testbed" tile with an Enable/Disable button.
  - New `POST /admin/active-directory/customer/:id/assign-service` — DB-only, no Stripe. Marks any current `active` `client_services` row of the same `deliveryType` `completed` ("wipe and start fresh", no new enum value, no reversibility), then inserts the new row via `admin-services.ts`'s own `assignClientService()` — extracted from the existing `POST /admin/client-services` handler so its notification + workflow-template auto-project side effects aren't re-derived. New clientUserId resolved via the existing `resolveDocumentOwnerUserId()` (tenant-signals.ts), same precedent used for document ownership.
  - Frontend: new "Package Assignment" section below "Purchased services", using `AdSelect`/`AdArmedButton`/`AdButton`/`AdOutcome` — no new primitives invented.
  - Verification: `pnpm run typecheck` clean in both `artifacts/api-server` and `artifacts/admin-panel`. Read-side swap logic (join client_services→services filtering by clientUserId/status/deliveryType) confirmed against the real local DB: testbed tenant 2080 (McCawSoft) has one active `bundle_subscription` row (client_services.id=176, service "Premier Monitoring — Micro") that the new query correctly isolates as the row to complete on a Monitoring swap. Live UI click-through not performed (no admin JWT session established in this session) — the DB-backed query logic and full TypeScript wiring were verified instead.
  - No migration needed — `tenants.is_testbed` and `services.delivery_type`/`client_services.status` columns already existed.
  - No findings filed — no genuine bug/gap discovered outside this issue's own scope.
  - `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs` before writing DONE.
