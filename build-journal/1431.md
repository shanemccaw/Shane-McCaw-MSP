# #1431 — Security: MFA hero banner — derive counts from live gapUsers/partialUsers instead of stale fixture literals

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 86efbf5ea6d5157150cc341c3028a8a5fc08a201 (rebased to e3a236243f7891c78dcc4768bf9c652ecdecd55d on origin/main)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Follow-up from #1414 audit. `portal-v2-security-mfa.tsx`'s
  hero/banner copy ("8 accounts still don't have it", "2 are admin accounts",
  "2 of 5 users still haven't registered a method", etc.) is hardcoded literal
  text sourced from fixture constants, never recomputed from the already-live
  `gapUsers`/`partialUsers` arrays (`useMfaRegistrationLive`, #1234). About to
  derive all hero/banner numbers from `gapUsers.length` / `partialUsers.length` /
  an admin count via `.filter(isAdmin)` instead. No new backend needed.
- 2026-08-27 ✅ DONE — Added `isAdminGapUser()` / `mfaUnregisteredCount()` pure
  derivations to `secMfaModel.ts` (unit-tested, 8/8 green in
  `secMfaModel.test.ts`), and wired `portal-v2-security-mfa.tsx`'s hero/banner
  spans to `gapUsers.length` / `partialUsers.length` /
  `gapUsers.filter(isAdminGapUser).length` / `mfaUnregisteredCount(partialUsers)`
  instead of the literal "8"/"2"/"5" strings. Along the way found the old
  literal "2 of 5" didn't even match its own fixture (`MFA_PARTIAL_USERS` has 3
  unregistered of 5) — the fix corrects that too. Added a `reportClientEvent`
  beacon (`engine.dashboard` channel) in `useMfaRegistrationLive.ts` for
  malformed-row drops, since a silently dropped row now means a wrong count on
  screen. Updated `test-manifests/portal/security-drilldowns.json` (new
  MFA4b/MFA10-12 steps, sessionTag `1431-hero-counts`) and bumped
  `lastVerifiedAgainstCommit`. `npx tsx --test` on `secMfaModel.test.ts`: 8/8
  pass. `tsc --noEmit`: clean except 2 pre-existing errors in
  `SowScreen.tsx`/`PortalV2Shell.tsx`, neither touched by this change. Tried
  `shaneapp://runTest` against the new sessionTag twice (before and after
  pushing to `origin/main`) — both returned a real envelope
  ("no steps matched session tag '1431-hero-counts'"), confirming the
  protocol itself works but the dev-server checkout BuildConsole's runner
  reads from hasn't merged this build's commit yet (that merge happens on
  build completion per the worktree-isolation doc, not mid-session) — an
  honest, structural gap, not a "can't verify" default. The manifest steps
  are in place for the next session/CI pass to run live once merged.
