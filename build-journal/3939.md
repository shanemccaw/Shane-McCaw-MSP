# #3939 — Launch Control: row 115 per-user MFA relink (bidirectional body_template)

- **Status:** ✅ DONE 2026-09-13T06:05:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 2d39b092c55cce49f895d298d030a2eba6e96373

## Log
- 2026-09-13T05:25:00Z ⏳ IN FLIGHT — read issue #3939 + Feature parent #2494; starting Step 1 dependency check on `action.enforce-per-user-mfa` body_template FK usage before touching it.
- 2026-09-13T05:35:00Z — Step 1 confirmed: `action.enforce-per-user-mfa` IS in live use (`config_pack_templates` id=156, pack `mfa-enforcement-v1`, the real sold $299 "MFA Enforcement Pack", `services.slug=mfa-enforcement-pack-v1`). Its `body_template` left completely untouched.
- 2026-09-13T05:40:00Z — Step 2: added a new, independent `action.disable-per-user-mfa` template (same real Graph endpoint, `perUserMfaState: "disabled"`), paired to `action.enforce-per-user-mfa` via `reversible`/`reverse_template_id` — Launch Control's existing generic rollback replay (`workflow-executor.ts rollbackExecution()`) covers the "disable" direction with no special-casing needed (both templates require only `userId`).
- 2026-09-13T05:48:00Z — Live-tested BOTH directions against the real testbed tenant (`c4c814d4-3afe-441e-9145-62461d0a4fd3`) via `POST /api/admin/write-actions/:templateId/execute`, targeting the sanctioned Git #2840 synthetic identity `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com`. **Both fail identically**: `PATCH /users/{userId}/authentication/requirements` is not a real Microsoft Graph resource — real 400 `"Resource not found for the segment 'requirements'"`, durably recorded in `baseline_action_template_audit_log` ids 49 (enforce) and 50 (disable). This is a pre-existing defect in the already-sold `action.enforce-per-user-mfa` (live since #1182) — not introduced by this build, but it does mean the pairing/relink work, while structurally correct and symmetric, cannot be verified end-to-end as *working* because the underlying endpoint itself doesn't exist on real Graph.
- 2026-09-13T05:52:00Z — Filed **Git #3951** ("URGENT: action.enforce-per-user-mfa hits a real Graph 400 — the sold $299 MFA Enforcement Pack's core write has never worked"), labeled `bug`+`security`, parented as a direct sibling sub-issue of Feature #2494, board status set to "AI Batter Up". Full evidence (audit log ids, real Graph error text, request-ids) in the issue body.
- 2026-09-13T05:56:00Z — Given that live evidence, Step 3 was completed **honestly, not as originally worded**: `write_action_catalog` row 115 records `template_id='action.enforce-per-user-mfa'` (documents which template was evaluated) but stays at `status='blocked_no_workaround'` — NOT promoted to `execution_ready` — with `blocked_reason` updated to name the real, live-verified cause and point at #3951. Promoting a demonstrably-400ing action to "ready to execute" in Launch Control would be a false promise to an MSP technician about to run it against a real customer tenant.
- 2026-09-13T06:05:00Z ✅ DONE — Migration `lib/db/migrations/manual/2026-09-13-per-user-mfa-disable-template-and-catalog-relink-3939.sql` applied to local `DATABASE_URL` and committed/pushed to `main` at `2d39b092c`. Verified `git merge-base --is-ancestor HEAD origin/main` and clean `git status --porcelain`. No fixture/hardcoded data — both new rows are real catalog data, live-tested against the real tenant. Finding #3951 filed and cross-linked.
