# #1989 — Conservation toggle: park (not downgrade) builds above Sonnet High

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** aa2dbaf0e, ba4ecd77e (088fc070a on origin/main after rebase), 2a66385b7

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled issue body + all 3 comments (binding correction: PARK not
  downgrade; drain button addition). Recovered prior art `AccountCapPolicy.cs` from
  `bffee480e^`. Read the full #1479 removal diff across QueueWatcherService,
  BuildQueuePostgresClient, BuildQueuePanel, MainWindow. Discovered a load-bearing fact not
  in the issue: `"parked"` is ALREADY a live, unrelated status (Git #1638/#1832) — Shane's
  own manual staging area, with its own filter tab, Un-park button, and dedup semantics.
  Reusing it for this cap would conflate two different concepts and corrupt #1638's own
  Parked count/flow. **Decision: park at a NEW distinct status, `"capped"`
  (`AccountCapPolicy.CappedStatus`), and leave `ReclaimLegacyHeldRowsAsync`
  (`BuildQueuePostgresClient.cs:1157`, `QueueWatcherService.cs:443`) completely untouched.**
  Since this cap never creates a `'held'` row, there is no conflict with that legacy
  reclaim at all — confirmed `GetNextAsync` only ever claims `WHERE status = 'queued'`, so a
  `'capped'` row is never picked up by the normal path either. Proceeding to implement.
- 2026-08-30 ✅ DONE — Recreated `desktop/BuildConsole/Services/AccountCapPolicy.cs`
  (`ExceedsSonnetHigh = IsOpusModel || IsFableModel || IsAboveHighEffort`, Fable added
  per the issue). New status `AccountCapPolicy.CappedStatus = "capped"`.
  `QueueWatcherService.LaunchItem` checks the gate first (before worktree
  provisioning/spawn); `isForced` always bypasses it (Run Now / the new per-item
  override), matching the issue's override contract exactly.
  `BuildQueuePostgresClient`: `MarkCappedAsync` / `GetCappedAsync` / `UncapAsync` /
  `DrainCappedAsync`; `IsActiveStatus` and `CancelAsync` extended for `capped`.
  `BuildQueuePanel`: dedicated "Capped" filter tab (distinct from #1638's "Parked"),
  peach status pill/card colors, context menu ("Run at Full Model" — one-shot, does
  NOT touch the toggle; "Cancel"). `MainWindow`: title-bar `TopConservationToggleBorder`
  next to the account toggle (de-brightened peach accent, matches
  `TopAccountToggleBorder`'s own treatment exactly), live primary-account weekly
  percent + reset countdown shown beside it (fed from the same `UsageMeter_StatusChanged`
  numbers already polled for the status bar). `BtnDrainCapped` confirms with the real
  count ("Release N parked builds at full model?"), turns the toggle off FIRST, then
  releases every capped row to `queued` at its untouched original model/effort — the
  deliberate one-shot exception, per the issue. Toggling OFF directly (no Drain) also
  releases everything capped, per the issue's own requirement. Settings:
  `ConservationModeEnabled`, default `false`. Also closed a small consistency gap in
  `ChatButtonInjector.cs`'s injected-button JS (`__btApplyStatus` had no `mode==="capped"`
  branch — functionally harmless fallback, but no peach accent — added one to match).

  **Verification — real, not inferred:**
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
    warnings, 0 errors (twice, after the ChatButtonInjector follow-up too).
  - Settings round-trip: a scratch console app referencing the built `BuildConsole.dll`
    ran the REAL `BuildConsoleSettings.Load()`/`Save()` against a settings.json written
    with other real keys but no `ConservationModeEnabled` key — loaded
    `ConservationModeEnabled = False`. Then `Save()` with it flipped `true` + a fresh
    `Load()` read `True` back. Confirmed the real `%AppData%\BuildConsole\settings.json`
    was empty before and after (restored to its pre-test state; deleted since it never
    existed).
  - Parked-row-survives-restart (the `ReclaimLegacyHeldRowsAsync` conflict the issue
    flagged): against the REAL local Postgres (`DATABASE_URL`), inserted a test row,
    simulated the real claim (`status='queued'→'running'`), ran the exact
    `MarkCappedAsync` SQL (`→'capped'`), then ran the EXACT `ReclaimLegacyHeldRowsAsync`
    SQL (`WHERE status='held'`) as a real watcher restart would — 0 rows affected, the
    capped row was untouched and still `status='capped'` afterward. Also confirmed
    `GetNextAsync`'s own claim-candidate query (`WHERE status='queued'`) does not select
    it. Genuinely survives — not by inspection.
  - Drain releases every row, none stranded: parked 3 real test rows at `capped` with
    distinct model/effort (including NULL/NULL), ran the exact `DrainCappedAsync` SQL —
    all 3 returned to `queued` with their real original model/effort untouched, and
    `SELECT count(*) WHERE status='capped'` afterward was 0.
  - `UncapAsync` (the per-item override's DB half) and `CancelAsync`'s extended status
    list (now includes `capped`) both verified against the same real rows before cleanup
    (`DELETE FROM bt_build_queue WHERE id IN (...)` — all test rows removed, no residue
    left in the shared dev database).
  - Not live-process-tested: an actual `claude.exe` launch via "Run at Full Model" —
    that reuses the exact same `ForceClaimAsync` + `QueueWatcherService.ForceLaunch`
    path the pre-existing "Run Now" feature already uses in production (unmodified by
    this build), and the DB-level flip it depends on (`UncapAsync`) is verified above;
    did not spin up a real build process in this session per the "don't relaunch/kill
    Shane's live BuildConsole.exe" instruction. Stated honestly, not claimed as
    live-verified.

  **Findings:** none worth filing separately — the one gap found (`ChatButtonInjector`'s
  missing `capped` mode branch) was fixed in this same session, not left open.

  Git status clean; branch confirmed merged into origin/main via
  `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
