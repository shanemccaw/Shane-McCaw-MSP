# Git #3269 — Meds batch card doesn't match real design (itemized rows instead of compact summary + colored avatar)

- **Status:** ✅ DONE 2026-09-08T22:15:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-08T00:00:00Z — Session start. Pulled issue #3269 (sub-issue of #3226 Feature: Meds,
  Epic #3086). Real gap: design `Shanes Life 07 - Meds.dc.html` shows the CURRENT/active batch
  itemized (icon avatar per item + swipe-to-take) but any batch that isn't due yet ("later")
  collapses to a single compact summary line + "later" label -- current `medBatchCard()` in
  `public/app.js` always itemizes every batch regardless of due status. Investigating real data
  volume (14 meds / 3 batches) before implementing the collapse + a real "show all" affordance
  for batches that don't fit a single line.
- 2026-09-08T22:15:00Z — ✅ DONE. Queried Shane's real medication data directly
  (`postgresql://.../finances`, local): 15 real meds across 3 real batches --
  `morning` (5), `night` (4), `as needed` (6). Design intent confirmed by re-reading the
  `.dc.html`: the design's "Evening" card (untaken, not the one due right now) is a single
  summary line + "later" label with NO items and NO swipe control, opacity .7 -- only the
  "Morning" card (the one real batch due right now) gets the full itemized+swipe treatment.
  Implemented in `public/app.js`/`public/app.css` (no backend/schema change needed, this was
  purely a rendering-fidelity gap):
  - `medsDaypartRank()` -- keyword-based (not exact-name) day-part rank so it isn't brittle
    against Shane's own real batch names (his real batch is "night", not the design's literal
    "evening"/"bed"); rank 3 = untimed (e.g. "as needed"), which never has a "later" state.
  - `viewMeds()` now orders batches morning -> midday -> evening/night -> untimed, and picks
    exactly one real "current" batch: the earliest untaken timed batch. Every other untaken
    timed batch renders `medBatchCard(batchState, { collapsed: true })`.
  - `medBatchCard()`'s new collapsed branch: critter avatar + bold batch name + a muted summary
    line (item names joined by " · ", truncated to 3 + "+N more" past that) + "later" label --
    matches the design's real Evening card. This is the real, sensible resolution to the
    issue's own "6 items on one line" complication: the CURRENT/expanded batch is never
    truncated (Shane needs to see everything he's about to swipe as taken), only the collapsed
    "later" summary line truncates. Already-taken batches are unchanged (itemized + Undo) --
    the design has no "later, but taken" state and that treatment already reads fine.
  - New CSS: `.med-batch-collapsed` (opacity .7), `.med-batch-summary`.
  - **Real, live browser verification** (not just structural/string checks): started a real
    second instance of this app's own server (`node src/server.mjs`, port 5177, pointed at the
    real local `finances` Postgres) from inside this worktree. Used the repo's own real
    software WebAuthn authenticator (`bin/soft-authenticator.mjs`, the same one `npm run check`
    uses) to create a disposable test account, copy Shane's real 15-medication list into it via
    the real `POST /api/medications` endpoint (not fabricated data -- his real med names/doses,
    read from his real account, written through the real API into a throwaway verification
    account), and sign in with a real passkey assertion. Drove real headless Chromium
    (`playwright-core`, already present locally at
    `%LOCALAPPDATA%\ms-playwright\chromium-1228`) to `#/meds` and took real screenshots at both
    states: (1) Morning untaken -> Morning itemized+swipe, Night collapsed ("Propranolol ·
    Breztri Aerosphere · Montelukast · +1 more" · "later"), As needed itemized (6 rows, no
    collapse); (2) after marking Morning taken -> Morning shows "Taken today ... Undo" (itemized
    unchanged), Night becomes the new current batch (itemized+swipe), As needed unchanged. Both
    screenshots visually match the design's real Evening-card treatment. Also ran
    `npm run check` against the running server before and after: 217/221 pass both times, same
    4 pre-existing `SL_VAULT_KEY`-not-set failures (unrelated to this change, missing local
    secret). Disposable verification account + its medications were deleted at the end; the
    scratch verification script and screenshots were deleted before finishing (never
    committed).
  - Merge: worktree branch `agent/3269-q1997` rebased onto current `origin/main` (12 commits
    had landed since provisioning) and pushed straight to `main`:
    `ff4d103c6` (Meds batch card fix), `5cfae3330` (IN FLIGHT bookend). Confirmed merged via
    `scripts/dev-server/verify-branch-merged.mjs` -> OK. `git status --porcelain` clean.
  - **Finding filed:** #3279 -- Today room's Medicine house-icon "lit"/subtitle logic in
    `api.mjs` hardcodes `batch === "evening" || batch === "bed"`, which never matches Shane's
    real batch name `night`, so the icon never lights up after 8pm and the subtitle never says
    "Before bed at 9" for his real data. Real, confirmed via direct DB query; discovered while
    investigating this issue's own batch-naming/day-part logic, but a different file/room, so
    fixed separately rather than folded into this commit. Parented as a sub-issue of #3226
    (Feature: Meds), board status set to "AI Batter Up".
