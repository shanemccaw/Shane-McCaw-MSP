# #3110 — Feature: Shopping — Weekly-ad verdicts, coupons, multi-buy (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T21:05:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `bd955236e` (real work)

## Log
- 2026-09-07T20:40:00Z ⏳ IN FLIGHT — pulled #3110's real scope. Its `blocked_by` edge (#3088)
  is cleared: #3088 shipped real commit `e21b1d1ee`, labeled `complete`, and its own bookend
  confirms `verify-branch-merged.mjs` passed. Removing the stale `blocked_by` edge and proceeding.
  Initial plan was a standalone `weekly_ad_prices` table, before discovering two other builds
  (#3109 barcode scan, #3112 per-store price history) had landed concurrently on the same Feature
  area in the meantime.
- 2026-09-07T21:05:00Z ✅ DONE — `bd955236e` on `main`. Rebased onto #3112 mid-build and found it
  had already built exactly the right general shape (`item_prices`/`stores`, one row per
  item/store/date/price/source) — rather than a parallel table, this reuses it with
  `source = 'weekly_ad'`, plus a new `unit` column those rows didn't need before (a flyer's
  "$4/lb" does). Coupons/multi-buy are a genuinely different shape and got their own real table.
  1. **Migration** `web/shanes-life/migrations/023_weekly_ad_coupons.sql`: `item_prices.unit`
     column; new `coupons` table.
  2. **`src/core/prices.mjs`** (extended, not replaced — #3112's own file): `pushDeals`,
     `pushCoupons`, `fetchWeeklyAd`, `attachWeeklyAdVerdicts` (cheapest current cross-store
     weekly-ad price + best matching coupon, bidirectional substring match, 10-day freshness
     window so a forgotten flyer push can't win forever).
  3. **MCP tools**: `push_deals`, `push_coupons`, `fetch_weekly_ad`. `get_prices` already existed
     (#3112) and already surfaces the `source` field Claude needs — not re-added.
  4. **`GET /api/shopping`** now also decorates items with `weeklyAdVerdict`, alongside #3112's
     existing `lastPrice` — a different question ("what does the CURRENT ad say" vs. "what did
     this cost last time, anywhere").
  5. **UI**: `public/app.js` renders a real verdict badge on each Shopping row.
  6. **README.md**: new Shopping section, updated "What actually exists" table.
  Rebased through three more concurrent landings mid-build (#3108 aisle memory/ordering, #3111
  budget) — resolved two real merge conflicts (the `GET /api/shopping` decoration order in
  `api.mjs`, and a `bin/check.mjs` section clash where the budget tests' own `push_list` calls
  would have wiped the "bread" item my checks depended on; fixed by having my checks add their
  own item rather than relying on list state left by another section).
  **Verified**: `npm run check` 123/123 PASS against a real local server and the real shared
  `finances` database, re-run after every rebase — 8 new checks cover `push_deals`/`push_coupons`/
  `fetch_weekly_ad` over MCP, real `item_prices`(`source='weekly_ad'`)/`coupons` rows, the real
  `weeklyAdVerdict` on `GET /api/shopping`, the audit trail, and an unmatched item showing no
  verdict. `verify-branch-merged.mjs` confirms `bd955236e` is `origin/main`'s own tip. Working
  tree clean (`.env`/`node_modules` junction added for local dev are both gitignored; my own
  pre-rebase WIP stash was dropped after superseding it with the real commit).
  No findings filed — nothing discovered outside this issue's own scope.
