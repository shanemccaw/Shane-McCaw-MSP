# #3110 — Feature: Shopping — Weekly-ad verdicts, coupons, multi-buy (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-07T20:40:00Z
- **Scope:** platform
- **Started:** 2026-09-07

## Log
- 2026-09-07T20:40:00Z ⏳ IN FLIGHT — pulled #3110's real scope. Its `blocked_by` edge (#3088)
  is cleared: #3088 shipped real commit `e21b1d1ee`, labeled `complete`, and its own bookend
  confirms `verify-branch-merged.mjs` passed. Removing the stale `blocked_by` edge and proceeding.
  Real plan: `list_items` had no data column and #3088's own docs explicitly parked weekly-ad
  verdicts/coupons/multi-buy as this separate Feature — building the real backend
  (`weekly_ad_prices`/`coupons` tables, migration 020, `src/core/prices.mjs`), the MCP tools
  `push_deals`/`push_coupons`/`get_prices`/`fetch_weekly_ad`, verdict decoration on
  `getListDetail`/`getListForShare`, and Shopping UI (`public/app.js`) + shared-link UI
  (`public/share.js`) badges.
