# #2285 — Slot count summary ("4/8 slots")

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** f39cd139c

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Item 4/7 of the Feature: Build Matrix (#2281) checklist.
  Confirmed the Build Matrix drawer itself doesn't exist yet (siblings #2282/#2283/etc. are
  separate open sub-issues, none landed). Confirmed real, non-fixture queue data DOES exist:
  #2413 already replaced `SeedSampleQueueData()` with a real `bt_build_queue` read
  (`LoadQueueFromDatabaseAsync` / `QueueReadClient`) in ShaneBuilder's Build Queue panel — so
  the "depends on #2263 landing real data" blocker #2281 named is already satisfied on the
  ShaneBuilder side. Scoping this build to a real, standalone "X/8 slots" summary — computed
  from real `bt_build_queue` rows (busy = raw status `running` + reserved `limit-paused`,
  mirroring BuildConsole's own `QueueWatcherService` concurrency accounting) against the real
  configured cap (`scripts/build-queue-watcher.config.json`'s `maxConcurrent`, default 8) — and
  wiring it into the Build Queue panel header now, since the full Matrix drawer chip (#2282)
  isn't there yet to host it. Reusable once the drawer lands.
- 2026-09-02T20:35:00Z DONE - Mid-build, `git rebase origin/main` surfaced that a concurrent
  session (#2286, "Build Matrix: 8-slot drawer + click-to-focus") had landed the WHOLE Build
  Matrix drawer while this session was working, including its own real, data-backed slot-count
  summary (`MatrixSlotSummary`/`MatrixChipCount`, `"{occupied}/{MatrixSlotCount} slots"`,
  `occupied` derived from real `_queueItems` with `Status == "Running"`). That already covers
  #2285's own checklist item - real, correct, no fixture data. Rather than land a duplicate
  standalone header chip, reverted my own separate `AgentSlotSummaryChip`/`RenderAgentSlotSummary`
  addition during the conflict resolve, and instead fixed the one real gap in what landed: the
  denominator (`MatrixSlotCount`) was a hardcoded `const int = 8`, not sourced from the real
  configured concurrency cap. Added `Services/AgentSlotConfig.cs` - a read-only mirror of
  BuildConsole's own `BuildTrackerConfig.MaxConcurrent` (same shared, gitignored
  `scripts/build-queue-watcher.config.json`, `maxConcurrent` key, default 8 fallback, same
  convention as `QueueReadClient`'s read-only contract on `bt_build_queue`) - and changed
  `MatrixSlotCount` from a hardcoded const to a property reading the real loaded value
  (`_slotMaxConcurrent`, loaded once at startup via `_logService.MainRepoRoot`). Also kept a
  small, real `RawStatus` field added to `QueueItem` (the un-coarsened DB status, distinguishing
  `running` from `limit-paused` which `MapQueueStatus` folds into `Queued` for display) - unused
  by this specific summary today (0 real `limit-paused` rows in local `bt_build_queue` right
  now) but available without another schema-shaped detour through the same code.
  **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` - 0 warnings, 0 errors, both
  before and after the final rebase. Confirmed against real local Postgres (`bt_build_queue`
  status counts: 3 running, 0 limit-paused, rest terminal/queued) that the live summary would
  read "3/8 slots" once BuildConsole resolves the real `scripts/build-queue-watcher.config.json`
  (confirmed present with `maxConcurrent: 8` in the main checkout - this worktree's own
  gitignored copy is absent, which is expected and exactly why `AgentSlotConfig` falls back to
  the documented default of 8 rather than throwing). `verify-branch-merged.mjs` confirmed the
  pushed commit (f39cd139c) is a real ancestor of `origin/main`. `git status --porcelain` clean.
