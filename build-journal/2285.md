# #2285 — Slot count summary ("4/8 slots")

- **Status:** ⏳ IN FLIGHT 2026-09-02T19:45:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Item 4/7 of the Feature: Build Matrix (#2281) checklist.
  Confirmed the Build Matrix drawer itself doesn't exist yet (siblings #2282/#2283/etc. are
  separate open sub-issues, none landed). Confirmed real, non-fixture queue data DOES exist:
  #2413 already replaced `SeedSampleQueueData()` with a real `bt_build_queue` read
  (`LoadQueueFromDatabaseAsync` / `QueueReadClient`) in ShaneBuilder's Build Queue panel — so
  the "depends on #2263 landing real data" blocker #2281 named is already satisfied on the
  ShaneBuilder side. Scoping this build to a real, standalone "X/8 slots" summary — computed
  from real `bt_build_queue` rows (busy = raw status `running` + reserved `limit-paused`,
  mirroring BuildConsole's own `QueueWatcherService` concurrency accounting) against the real
  configured cap (`scripts/build-queue-watcher.config.json`'s `maxConcurrent`, default 8) — and
  wiring it into the Build Queue panel header now, since the full Matrix drawer chip (#2282)
  isn't there yet to host it. Reusable once the drawer lands.
