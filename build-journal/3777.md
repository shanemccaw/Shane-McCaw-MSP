# #3777 — WaitingRowAutoRequeueService: remove unconditional 5-min GitHub timer

- **Status:** ✅ DONE 2026-09-12T07:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** d33d56de60c33c816fc60508719196fc5f5cd6b3

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — removing WaitingRowAutoRequeueService's unconditional 5-minute Timer (found by #3774's own audit item 4), wiring RunSweepAsync to a real completion event + manual refresh (BoardRefreshCompleted / QueueWatcherService build-finished) instead, matching #3774's pattern.
- 2026-09-12T07:45:00Z ✅ DONE — removed the `System.Threading.Timer` from `WaitingRowAutoRequeueService`; `RunSweepAsync` now runs only via a new public `TriggerSweep()`, wired in `MainWindow.xaml.cs` to `QueueWatcherService.BuildFinished` and `LeftSidebar.BoardRefreshCompleted` (the same two triggers #3774 used for `QueueWatcherService`'s own claim-check). `StartAsync()` keeps a one-shot initial sweep at app open; `Dispose()` is now a no-op. Verified: `dotnet build -c Debug` succeeds, 0 errors/0 warnings. Merged into origin/main (verify-branch-merged.mjs OK), working tree clean.
