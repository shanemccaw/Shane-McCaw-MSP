# #1381 — Remediation Tracker: real live per-pillar score API (rolling before/now + permanent day-1 baseline + real Copilot gate)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-26
- **Commit(s):** 5e036100c

## Log
- 2026-08-26 ⏳ IN FLIGHT — New `GET /api/portal/remediation-tracker/pillar-scores`: per-pillar rolling before/now from `tenant_pillar_snapshots` (previousScore→score of the latest row), a permanent `dayOne` from the earliest snapshot row per pillar, real Copilot gate via `computeCopilotGate`, and per-task point chips from each task's real underlying finding severity. Wire `rtPillarCells`/`rtGate`/`rtHeadline` to consume it with honest-null fallback for single-scan / insufficient-data tenants. Replaces the hardcoded `RT_PILLAR_BASE`/`RT_PILLAR_TARGET` fixture headline.
- 2026-08-26 ✅ DONE (5e036100c) — Built the full pipeline per Shane's fully-decided model across all four issue comments.
  - **API** `artifacts/api-server/src/routes/portal-remediation-tracker-scores.ts` + pure db-free logic `artifacts/api-server/src/lib/remediation-pillar-scores.ts`. `before`/`now` = the latest snapshot row's `previousScore`/`score` (the rolling last-two-scans pair — pillar-snapshot.ts already stamps previousScore, so route and capture agree). `dayOne` = the earliest snapshot row's score per pillar (permanent baseline, distinct from `before` once ≥3 scans). Pillar keying maps the tracker's six to snapshots, `health`→`architecture` (engine's internal pillar surfaced as "Health"); copilot is the gate, not a cell. Copilot gate = real `computeCopilotGate` (fixed 82 threshold, honest-null when uncovered). Per-task point chips = worst real finding severity across each step's mapped checks from the latest run (critical 3 / warning 2 / info 1 / ok 0). Honesty states: `insufficient_data` (no snapshot) / `single_scan` (one scan, no `before`) / `scored`.
  - **Frontend** `remediationScores.ts` (types), `useRemediationPillarScores.ts` (hook), and a rewrite of `remediationModel.ts`'s `rtPillarLive`/`rtPillarCells`/`rtGate`/`rtHeadline` + new `rtTaskPoints` to consume the real scores through `RtCtx.scores`, with honest-null fallback (dash + "not enough data yet" for unscored pillars, "not evaluated yet" for the Copilot gate). Dropped the fixture `RT_PILLAR_BASE`/`RT_PILLAR_TARGET`/`RT_POINTS` gap-toward-target model entirely. Page `portal-v2-remediation.tsx` gate strip + pillar cells re-rendered for the real before→now, day-one marker, and real gate; new testids `pv2-rt-gate-now` / `pv2-rt-copilot-gate` / `pv2-rt-pillar-score-<key>` / `data-rt-scored`.
  - **Retention (Shane's hard requirement) — CONFIRMED SAFE.** The ONLY deletion of `tenant_pillar_snapshots` anywhere in the codebase is the manual whole-tenant testbed-reset migration (`2026-08-26-comprehensive-testbed-reset-1329.sql`), a deliberate full reset for a test tenant — NOT a retention/pruning/cron policy. No scheduled prune exists; the table is retain-forever by design (schema says "retained ... never pruned"). The earliest (day-one) row per pillar can never be deleted by any automated policy. Nothing to exempt because no pruning policy exists.
  - **Tests** — api-server unit `portal-remediation-tracker-scores.test.ts` (9 pass: rolling before/now, day-one distinct, single-scan, insufficient_data, health→architecture map, copilot-never-a-cell, per-task worst-severity weighting) added to the vitest allowlist. Portal `remediationModel.test.ts` rewritten for the new model (39 pass total). Test manifest `test-manifests/portal/portal-v2-remediation.json` extended: API3–API6 + UI RT25–RT28 (sessionTag `rt1381`) cover the new endpoint + DOM. **UI manifest run deferred** — `shaneapp://runTest` hits the DEPLOYED build; the new endpoint 404s until this commit deploys (same as #1364). Re-run post-deploy: `shaneapp://runTest?file=test-manifests/portal/portal-v2-remediation.json&onlyTag=rt1381`.
