# #4497 — MSP Directory Containers: backend (schema + CRUD + assignment + tree/detail)

- **Status:** ✅ DONE 2026-09-17T05:12:54Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** f1a0a19e5 (code, on origin/main); a29997cfc (IN FLIGHT bookend, on origin/main); migration `lib/db/migrations/manual/2026-09-17-active-directory-containers-4497.sql`

## Log
- 2026-09-17T04:45:39Z ⏳ IN FLIGHT — Building the backend for MSP Directory Containers (Feature #4496). This is the real MSP→Tenant→Container→Users/Roles hierarchy, deliberately kept COMPLETELY separate from the existing OU/policy-engine system (`active_directory_ous`/`active_directory_ou_assignments`, Git #1952 — real Microsoft Graph objects verified live via Graph UPN, feeding `policy-compliance-*`). The Container concept is a plain local-data grouping: `active_directory_containers` table + nullable `container_id` on `users` and `customer_roles`, with no Graph lookup, no policy logic, and no separate assignments table. Read `admin-active-directory.ts` in full incl. the "OU manual membership assignment (Git #1952)" section before writing anything.
  - **#4495 split has NOT landed** as of this session start: only `admin-active-directory.ts` exists (no `admin-msp-directory.ts` / `admin-active-directory-ou.ts`, paths still `/admin/active-directory/*`). Per the URGENT coordination comment on #4497, I am therefore building against the pre-split `admin-active-directory.ts` with clearly-named new Container routes. Noting plainly so whoever reconciles the two commits knows the landed state.
  - Out of scope, untouched: `activeDirectoryOusTable`, `activeDirectoryOuAssignmentsTable`, `resolveAssignmentCustomer`, `resolveGraphUserByUpn`, the OU routes, `policy-compliance-evaluator.ts`, `policy-compliance-graph.ts`.
- 2026-09-17T05:12:54Z ✅ DONE — Shipped in commit `f1a0a19e5` (verified ancestor of `origin/main` via `verify-branch-merged.mjs`; working tree clean).
  - **Schema:** `active_directory_containers` (id, tenant_id NOT NULL → tenants ON DELETE CASCADE, name, created_at/updated_at timestamptz) added to `lib/db/src/schema/index.ts`; nullable `container_id` FK (ON DELETE SET NULL) added to `usersTable` (index.ts) and `customerRolesTable` (rbac.ts), each with an index. **Timestamps are timestamptz** (matching the sibling `active_directory_ous` table + Drizzle `withTimezone:true`), a deliberate deviation from the plain `TIMESTAMP` in the issue body, to avoid the naive-timestamp drift the drift-checker flags.
  - **Migration:** `lib/db/migrations/manual/2026-09-17-active-directory-containers-4497.sql` — additive DDL, **run in-session against local `DATABASE_URL`** (verified: table + both columns + all FKs/indexes present; a rolled-back live check on testbed tenant 2080 confirmed user & role `container_id` are SET NULL on container delete). Self-marks in `simulator_migration_runs`. Appended to the #1630 release checklist for Replit/staging. NOT a Shane To-Do (additive, local already current).
  - **Routes (landed in `admin-msp-directory.ts`, NOT the pre-split `admin-active-directory.ts`):** see reconciliation note below. Container CRUD `POST/PATCH/DELETE /admin/msp-directory/container*`; assignment `PATCH /admin/msp-directory/user/:id/container` and `PATCH /admin/msp-directory/role/:roleId/container` (both accept `containerId: number|null`, enforce same-tenant integrity, reject filing a platform-scoped role); tree (`GET .../tree`) and customer detail (`GET .../customer/:id`) now return Containers with member users+roles, with `container_id IS NULL` members still appearing directly under the tenant. All `requireAdmin`, all audited on the `audit` channel, all clearly separated from the OU system (no Graph, no policy, no assignments table).
  - **#4495 reconciliation (the coordination-comment scenario, and it happened live):** #4495 (`admin-active-directory.ts` split → `admin-msp-directory.ts` + `admin-active-directory-ou.ts`, paths `/admin/active-directory/*` → `/admin/msp-directory/*`) landed on `origin/main` DURING this session (commit `72223bb94`). My original commit was written against the pre-split `admin-active-directory.ts`; on rebase it conflicted on the renamed file. I resolved by taking main's post-split `admin-msp-directory.ts` and re-applying every container change onto it with the new `/admin/msp-directory/*` paths — so all my routes/edits are in `admin-msp-directory.ts`, none left behind in a stale file. The commit message still says "admin-active-directory.ts" (written pre-rebase); the actual landed file is `admin-msp-directory.ts`.
  - **Verification:** `pnpm --filter @workspace/api-server run typecheck` clean (exit 0) on the final landed state; `active-directory.test.ts` 89/89 pass (incl. 5 new tests for `buildMspTree` container grouping + `assembleCustomerContainers`). No test manifest — this is api-server/admin work, out of the portal/marketing manifest scope.
  - **Finding filed:** #4502 (`bug`, parented #1096, board "AI Batter Up") — `check-drift` is red on `origin/main` from two PRE-EXISTING, unrelated commits (`a41e8e7f9`, `b9449cb0c`) that made `$type`-only edits to `schema/index.ts` without re-baselining the schema hash. My own commit is correctly reconciled (bundles its migration) and is not among the flagged commits; I did not silently re-baseline shared migration state to mask two other features' commits.
  - Pre-existing dirty files on arrival: none. `git status --porcelain` clean at finish.
