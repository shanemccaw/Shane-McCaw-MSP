# Issue #4479 — exchange:dkim-spf-dmarc-status finding title placeholder

- **Status:** ✅ DONE 2026-09-17T05:04:11Z
- **Scope:** platform

## Log
- 2026-09-17T04:56:51Z — Starting. Fix `buildFindingTitle` in `artifacts/api-server/src/lib/diagnostics-runner.ts` to interpolate `<domain>`-style placeholders in `severity_rules` labels from `extracted_properties`, rather than reword the label. Confirmed via the issue's own SQL this is the only affected row.
- 2026-09-17T05:04:11Z — ✅ DONE — Added `interpolateSeverityLabel` in `artifacts/api-server/src/lib/diagnostics-runner.ts`, called from `buildFindingTitle` where the matched rule's own label is used; it replaces any `<propName>` token with the matching key from the finding's `extractedProperties` (leaving it untouched if no matching property exists). Chose interpolation over rewording per the dispatch's stated preference — it fixes the class generically for any future rule that carries a placeholder, not just this one row. Added 2 new tests to `diagnostics-finding-title.test.ts` (10/10 passing); `tsc --noEmit` clean. Re-ran the issue's own SQL post-fix — it still returns the one `exchange:dkim-spf-dmarc-status` row, because the stored `severity_rules` label is now the deliberate interpolation *template* (the raw DB text is expected to keep `<domain>` as its substitution token; the fix operates at title-render time, not on the stored label text). Confirmed the actual customer-facing title is fixed: `buildFindingTitle` with `extractedProperties.domain = "mccawsoft2.onmicrosoft.com"` now renders `"No DMARC record found at _dmarc.mccawsoft2.onmicrosoft.com"`. Merged to `origin/main` at `8224fa336` (rebased twice over concurrent pushes). `git status --porcelain` clean.
