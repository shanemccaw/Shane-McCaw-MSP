# #1449 — Claude CLI usage-meter poll 20s timeout too short, false "error" on slow claude.exe -p /usage calls

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** ffa46d01e

## Log
- 2026-08-27 ⏳ IN FLIGHT — raising `RunClaudeCliUsageAsync()`'s hardcoded 20s `Task.Delay` timeout to a named constant (45s), and refining the timeout failure reason so it reads distinctly from a generic CLI error.
- 2026-08-27 ✅ DONE — `ClaudeUsageMeterService.cs`: added `private static readonly TimeSpan CliUsagePollTimeout = TimeSpan.FromSeconds(45)` (was inline `Task.Delay(TimeSpan.FromSeconds(20))`), and the timeout branch now logs to the `usage-meter` channel and returns `"timed out after 45s"` as the error reason instead of a passthrough generic string — this flows unchanged through both the Primary (`SetUnavailable`) and Secondary (`SetSecondaryUnavailable`) callers since both already surface `cliError` verbatim, so this single fix covers both meters per the issue. Confirmed `dotnet build -c Debug` succeeds with 0 errors (all warnings pre-existing/unrelated). `git merge-base --is-ancestor` / `verify-branch-merged.mjs` confirms ffa46d01e is an ancestor of origin/main (pushed directly, commit bfe06771e..ffa46d01e).
