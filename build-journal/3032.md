# #3032 — msp-settings.ts credential-reset routes have no target-role ceiling

- **Status:** ✅ DONE 2026-09-06T20:06:19Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** fcb16b522 (fix + regression tests), a74ef6f8a (IN FLIGHT bookend)

## Log
- 2026-09-06T19:55:25Z ⏳ IN FLIGHT — fixing the 5 credential/security-action routes in
  `artifacts/api-server/src/routes/msp-settings.ts` (reset-password, temp-password,
  reset-mfa, mfa-enforcement, status) to reject (403) any target whose effective role
  outranks or equals the caller's, mirroring the `updateRoleSchema`/`createInviteSchema`
  pattern that already keeps `PlatformAdmin` unassignable through this surface. Writing
  a live-Postgres regression test proving an MSPAdmin JWT gets 403 against a
  PlatformAdmin target on all 5 routes, and that the happy path (lower-tier target)
  still works.
- 2026-09-06T20:06:19Z ✅ DONE — all 5 routes now do a target-role ceiling check
  (`roleIndex(target.mspRole) >= roleIndex(effectiveMspRole(caller))` -> 403) after the
  existing mspId-ownership lookup; `mfa-enforcement` and `status` gained the ownership
  lookup they were previously missing entirely (silent no-op on a foreign userId before,
  now a real 404). `roleIndex`/`effectiveMspRole` exported from `requireAuth.ts` so the
  check reuses the exact same ordering/legacy-admin normalization `requireRole()`
  already applies to the caller.

  New live-db test `msp-settings-target-role-ceiling.live-db.test.ts` (registered in
  `vitest.config.ts`'s include allowlist) — 8 tests, all passing in isolation:
  - All 5 routes return 403 for an MSPAdmin JWT targeting a PlatformAdmin at the same
    MSP (the exact live exploit path from the issue body), with the target row proven
    unchanged (password hash, MFA enrollment row, mfaEnforced, isActive all untouched).
  - 3 of the 5 routes (mfa-enforcement, status, temp-password) re-verified to still
    return 200 and really apply the action against a genuine lower-tier MSPOperator
    target — reset-password/reset-mfa happy paths already covered by the existing
    mocked-db `msp-settings-portal-links.test.ts`.

  Verification: `pnpm exec tsc --noEmit` clean; new test file 8/8 passing in isolation;
  pre-existing `msp-settings-user-security.live-db.test.ts` (4/4) and
  `msp-settings-portal-links.test.ts` (4/4) both still green in isolation — no
  regression. (Both live-db files fail with timeouts when run concurrently against the
  same real Postgres pool in the same vitest process — confirmed pre-existing
  thread-pool contention, not a logic defect; each passes cleanly alone.) Boot-smoke
  aggregator import test (`routes/index.boot-smoke.test.ts`) passed on retry (48.6s of
  a 60s budget on this box; unrelated `admin-signal-rules` seeder noise against a
  deliberately-unreachable stub DB, not related to this change).

  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before
  writing this. Working tree clean (`git status --porcelain` empty).
