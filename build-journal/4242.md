# #4242 — Record the MSP's own Entra tenant on `msps` and bind the mailbox connector to it

- **Status:** ✅ DONE 2026-09-15T14:06:45Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 3ee6999e8 (work), a2efab0c1 (IN FLIGHT bookend)

## Log
- 2026-09-15T13:51:30Z ⏳ IN FLIGHT — adding nullable `msps.entra_tenant_id`, a platform-admin route/field in admin-panel's MSP detail view, and enforcement in the mailbox connector connect/callback (warning fallback when unset).
- 2026-09-15T14:06:45Z ✅ DONE — shipped in `3ee6999e8`:
  - **Column:** `msps.entra_tenant_id` text, nullable, stored lowercase, partial unique index `msps_entra_tenant_id_unique`. Migration `lib/db/migrations/manual/2026-09-15-msps-entra-tenant-id-4242.sql` run against local DB; logged on #1630.
  - **Admin surface:** existing platform-admin `PATCH /api/admin/msps/:mspId` (`requireAdmin`) accepts `entraTenantId` (GUID or null); 400 non-GUID, 409 if another MSP already records it, 409 if it is another MSP's customer tenant. AdminV2 Active Directory MSP canvas (`AdMspCanvas.tsx`): "Own Entra tenant" tile (amber "not set") + "Own Microsoft Entra tenant ID" field in Edit profile; `GET /admin/active-directory/msp/:id` now returns the field.
  - **Enforcement (`msp-settings.ts`):** `checkMspOwnTenant` — connect returns 403 (no state minted) and callback returns 403 (state burned, nothing activated) when the mailbox tenant ≠ the MSP's recorded own tenant; the callback re-reads `msps` rather than trusting the state. Unset → #4227 behaviour plus a `log.warn` on every connect and callback.
  - **No real GUID set for any MSP** — MSP 1's own tenant is Shane's fact to record (Shane To-Do). `msps` rows with entra_tenant_id set locally: 0.
  - **Verification:** vitest `msp-settings-portal-links.test.ts` + `active-directory.test.ts` 102/102 (5 new #4242 cases); api-server `tsc --noEmit` 0 errors; admin-panel `tsc --noEmit` 0 errors. Live on an isolated api-server (:8099, worktree build, local DB) with a synthetic `zz-test-4242` MSP (2815), a synthetic customer row for it carrying microsoft.com's tenant (72f988bf, resolved live from Microsoft's discovery doc), and zz-test-4242 MSPAdmin + PlatformAdmin users: MSPAdmin PATCH → 403; non-GUID → 400; another MSP's customer (c4c814d4) → 409; unset + connect → 200 with the warning logged; own tenant set to a synthetic GUID → second MSP claiming it → 409; **connect for the MSP's own customer's mailbox → 403 refused, no state minted; callback with a state minted before the tenant was set, tenant=customer → 403 refused, state burned, replay → 400**; positive control (own tenant = 72f988bf) → connect 200, callback passed the gate and stopped at the Microsoft consent check (400, not consented); clear → 200. `msp_mailbox_connectors` 0 rows throughout. All synthetic rows deleted afterwards (msps/users/tenants/states/audit/refresh tokens/sessions).
  - Findings filed: none.
  - `git status --porcelain` clean; no pre-existing dirty files.
