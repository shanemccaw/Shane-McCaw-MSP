# #2795 — Build Queue cards: add an Epic-name chip below the progress bar

- **Status:** ✅ DONE 2026-09-04T03:16:50Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 6470c2022 (Epic-name chip), f6a6b84c7 (IN FLIGHT bookend) — both merged into `main`

## Log
- 2026-09-04T03:09:05Z ⏳ IN FLIGHT — starting: generalize GetEpicForChat's ancestor-walk resolution to accept a raw issue number, wire a per-card Epic resolution into BuildQueuePanel, render a chip after progressBubbleRow in BuildQueueCard.
- 2026-09-04T03:16:50Z ✅ DONE — Added `LeftSidebar.GetEpicForIssueNumber(int)`, a generalized form of `GetEpicForChat`'s issue-resolution step: checks whether the raw issue number IS an Epic first, then climbs `GitBoardIssue.ParentNumber` all the way to the top (visited-set guard against cycles) — the same real ancestor-walk `GitBoardIssueFilters.IsUnderInternalToolingEpic` already established for #2739/#2773/#2694, reused rather than a fourth resolution path. Added `BuildQueuePanel.ResolveEpicForIssue` (a `Func<int, BoardEpic?>`), wired once by `MainWindow` to `LeftSidebar.GetEpicForIssueNumber` — the same "MainWindow owns both panels, pushes real resolved state into BuildQueuePanel" idiom `ApplyOpenIssueSet` (#1862) already established, but on-demand per render rather than a synced snapshot, so it always reads LeftSidebar's current live board state. Added `BuildQueuePanel.BuildEpicChipRow(QueueItem)`, called right after `progressBubbleRow` in `BuildQueueCard` (the exact real insertion point named in the issue) — renders a small chip (same Border+TextBlock convention as the card's other real badges) with the resolved Epic's real title (truncated at 42 chars, full title in the tooltip). Returns null (no chip rendered, not a fake fallback) when the item has no GitHub issue number or the issue genuinely has no resolvable Epic ancestor.
  - Files: `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs`, `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`, `desktop/BuildConsole/MainWindow.xaml.cs`.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `git diff --stat` confirmed only the 3 intended files changed, all additive (89 insertions, 0 deletions). No test-manifest applicable — this is a `desktop/BuildConsole` (WPF) change, explicitly out of scope for test manifests per CLAUDE.md. No live-render screenshot verification was performed (no WebView2/browser surface here to screenshot — this is a native WPF card); correctness rests on the build succeeding cleanly and the resolution logic being a direct, deliberate generalization of the already-proven `GetEpicForChat`/`GitBoardIssueFilters` ancestor-walk.
  - Branch `agent/2795-q1577` rebased onto `origin/main` and pushed directly to `main` (`f91cb7c57..6470c2022`). `verify-branch-merged.mjs` confirms OK — merged into `origin/main`.
  - `git status --porcelain` clean after commit/push.
