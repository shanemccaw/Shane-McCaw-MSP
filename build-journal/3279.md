# #3279 — Today room's Medicine icon never lights up for a batch not literally named 'evening'/'bed'

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-08T00:00:00Z — Confirmed root cause in `web/shanes-life/src/routes/api.mjs` (~line 230): `bedBatch` lookup uses exact `===` match against `"evening"`/`"bed"`, missing Shane's real batch name `"night"`. Fix: generalize with the same keyword regex already used by `medsDaypartRank()` in `public/app.js` (`/evening|night|bed|dinner/`) rather than adding another literal.
- 2026-09-08T00:20:00Z — ✅ DONE — Fixed `morningBatch`/`bedBatch` lookups in `web/shanes-life/src/routes/api.mjs` to keyword-match (`/morning/i`, `/evening|night|bed|dinner/i`) instead of exact string equality, mirroring `medsDaypartRank()` in `public/app.js`. Verified with a standalone repro against Shane's real batch shape (`morning`/`night`/`as needed`) confirming `bedBatch` now resolves to `night` and `bedDue` is `true` at hour ≥ 20. `node --check` passed. Committed `76315d684` on `agent/3279-q2008`, merged/pushed to `origin/main` (`70bff87b0..76315d684`), `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
