# #3948 — Wire runBaselineTemplateAgainstTenant to route exchange-online:// templates through ps-execution-client.ts

- **Status:** ✅ DONE 2026-09-16T19:25:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 286fc94c6 (transport implementation, on main), 7aece1d42 (catalog blocked_reason refresh migration), 080f61287 (promotion migration: 8 rows → execution_ready)
- **Findings filed:** #3989 (set-mail-flow-rule 'Condition' defect), #4269 (Exchange RBAC write role — CLOSED, Shane executed), #4427 (dev DB tenants table wiped by test run), #4428 (role group lacks Distribution Groups role — Shane To-Do), #4429 (block-outbound-send success-without-effect) — all siblings of #2494

## Log
- 2026-09-14T01:37:32Z ⏳ IN FLIGHT — Second execution transport for the workflow engine: parse `exchange-online://<Cmdlet>` endpoints, map body/required_variables onto PowerShell named parameters, non-HTTP success evaluation, route through ps-execution-client.ts; live-test ≥1 of the 13 templates against the testbed tenant, then promote only the live-verified write_action_catalog rows (of ids 185-196) back to execution_ready.
- 2026-09-14T03:35:00Z 🛑 BLOCKED — Implementation complete and pushed to main as `286fc94c6`: new `exchange-online-transport.ts` (parse/param-map/classify, 22 unit tests), `runExchangeOnlineTemplateAgainstTenant` wrapper + routing branch in `workflow-executor.ts`, 6 IsWrite exchange-session entries in `cmdlet-catalog.ps1` (New-TransportRule deliberately excluded — see #3989), child-worker fail-closed param check + confirm suppression for writes. Locally verified: 22 new + 21 existing tests pass, tsc exits 0, both .ps1 files parse clean. NOT live-verified: `ca-ps-execution-dev` TCP-unreachable (3 real attempts, "Could not connect to server" ~21s each) — #3530's disabled ShaneMcCawConsulting Azure subscription covers Key Vault, Container Apps AND ACR, so neither deploying the updated container image nor firing any cmdlet is possible until Shane restores the subscription. Promoted ZERO write_action_catalog rows — all 12 (ids 185-196) confirmed still `endpoint_design_pending` in the live DB. blocked_by edge #3948 → #3530 wired; `blocked` label set, `in-flight` removed. Resume path: deploy dev container (`shaneapp://deployPsExecution`), live-test ≥1 template (suggest action.create-shared-mailbox against a new zz-test-* shared mailbox), promote exactly the live-verified rows.
- 2026-09-15T16:52:00Z ⏳ IN FLIGHT — Resuming (build queue 2653) after #3530 cleared: blocked_by list on #3948 is now empty and Shane confirmed Azure billing restored 2026-09-15. This session: deploy the updated dev ps-execution container image (`shaneapp://deployPsExecution`), live-test at least one of the 12 supported templates against the testbed tenant (target: `action.create-shared-mailbox` → new zz-test-* shared mailbox, additive/unlicensed), then promote exactly the live-verified `write_action_catalog` rows (subset of ids 185-196) to `execution_ready`. Watching for the flagged Exchange RBAC write-role gap on the app-only identity.
- 2026-09-15T18:42:00Z 🛑 BLOCKED — Session (build 2653) resumed after #3530 cleared and completed the infrastructure + verification tail as far as it can go without Shane: (1) found `cae-smccaw-2184` compute STILL suspended post-billing-restore (`ManagedClusterSuspended`, the #4265 tail) and resumed it with a no-op `az containerapp env update`; (2) deployed revision `ca-ps-execution-dev--b3948b` from current main via `shaneapp://deployPsExecution` (ACR build OK; the update step needed one direct retry after the resume), `/healthz` confirms it serving; (3) live-tested the transport against testbed customer 1 — read control `get-safe-links-policies` through the SAME app-only Exchange session returns real data (session/auth/cert/consent all work), but `action.create-shared-mailbox` (New-Mailbox, audit id 56) and `action.block-outbound-send` (Set-Mailbox, audit id 57) both fail `cmdlet_unavailable` → `insufficient_privilege` — the exact predicted RBAC gap: the MT app (9ea2e409) holds only View-Only Organization Management, no Exchange write role. That grant is a Git #1913 production-boundary action → filed as #4269 (sibling of #2494, bug + Shane To-Do), `blocked_by` edge #3948 → #4269 wired, `blocked` label set / `in-flight` removed. ZERO write_action_catalog rows promoted (all 12 ids 185-196 confirmed still `endpoint_design_pending`); their stale blocked_reason texts refreshed to name #4269 via `lib/db/migrations/manual/2026-09-15-exchange-online-catalog-blocked-reason-refresh-3948.sql` (commit 7aece1d42, ran locally, UPDATE 12, logged on #1630). Resume path after #4269: re-run live tests (anchor `action.create-shared-mailbox` → zz-test-shared-3948@mccawsoft2.onmicrosoft.com) and promote exactly the verified rows; see the 2026-09-15 issue comment for the row-190/row-188 caveats (org-wide transport rule with no allowlisted removal; no safe convert-to-shared target).
- 2026-09-16T19:25:00Z ✅ DONE — #4269's role grant verified working after Exchange-side propagation (~12 min post-grant still failing, converged by next day). En route: found the dev DB tenants table wiped between sessions (testbed row id=1 gone, only vitest strays left) — restored it manually and filed #4427. Live-fired 11 template executions against testbed customer 1 through the transport (audits 62-72): 8 catalog rows verified and PROMOTED to execution_ready via `2026-09-16-promote-exchange-online-verified-rows-3948.sql` (commit 080f61287, ran locally UPDATE 8+4, logged on #1630) — 185 (shared mailbox, read-back confirmed), 187 (room mailbox, read-back confirmed), 189 (set-forwarding + row-less remove-forwarding undo), 191 (send-as), 192 (full-access), 193 (lit-hold, false direction), 194 (archive, ArchiveStatus=Active read-back), 195 (quota, 49.5GB read-back). NOT promoted, accurate blocked_reason set on each: 186 (New-DistributionGroup cmdlet_unavailable — role group lacks Distribution Groups role, filed #4428 Shane To-Do), 188 (no safe convert-to-shared target), 190 (org-wide rule, no allowlisted removal — Shane's call), 196 (REAL FINDING #4429: Set-Mailbox -MaxSendSize "0B" reports success but read-back shows 35 MB org default at +3/+20 min — success without effect). Test artifacts left under zz-test-* prefix (zz-test-shared-3948, zz-test-room-3948), no licenses consumed. Verification honest and complete; #3948's own scope is done.
