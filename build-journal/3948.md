# #3948 — Wire runBaselineTemplateAgainstTenant to route exchange-online:// templates through ps-execution-client.ts

- **Status:** ⏳ IN FLIGHT 2026-09-15T16:52:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 286fc94c6 (transport implementation, on main)
- **Findings filed:** #3989 (action.set-mail-flow-rule template defect — 'Condition' is not a New-TransportRule parameter; sibling of #2494)

## Log
- 2026-09-14T01:37:32Z ⏳ IN FLIGHT — Second execution transport for the workflow engine: parse `exchange-online://<Cmdlet>` endpoints, map body/required_variables onto PowerShell named parameters, non-HTTP success evaluation, route through ps-execution-client.ts; live-test ≥1 of the 13 templates against the testbed tenant, then promote only the live-verified write_action_catalog rows (of ids 185-196) back to execution_ready.
- 2026-09-14T03:35:00Z 🛑 BLOCKED — Implementation complete and pushed to main as `286fc94c6`: new `exchange-online-transport.ts` (parse/param-map/classify, 22 unit tests), `runExchangeOnlineTemplateAgainstTenant` wrapper + routing branch in `workflow-executor.ts`, 6 IsWrite exchange-session entries in `cmdlet-catalog.ps1` (New-TransportRule deliberately excluded — see #3989), child-worker fail-closed param check + confirm suppression for writes. Locally verified: 22 new + 21 existing tests pass, tsc exits 0, both .ps1 files parse clean. NOT live-verified: `ca-ps-execution-dev` TCP-unreachable (3 real attempts, "Could not connect to server" ~21s each) — #3530's disabled ShaneMcCawConsulting Azure subscription covers Key Vault, Container Apps AND ACR, so neither deploying the updated container image nor firing any cmdlet is possible until Shane restores the subscription. Promoted ZERO write_action_catalog rows — all 12 (ids 185-196) confirmed still `endpoint_design_pending` in the live DB. blocked_by edge #3948 → #3530 wired; `blocked` label set, `in-flight` removed. Resume path: deploy dev container (`shaneapp://deployPsExecution`), live-test ≥1 template (suggest action.create-shared-mailbox against a new zz-test-* shared mailbox), promote exactly the live-verified rows.
- 2026-09-15T16:52:00Z ⏳ IN FLIGHT — Resuming (build queue 2653) after #3530 cleared: blocked_by list on #3948 is now empty and Shane confirmed Azure billing restored 2026-09-15. This session: deploy the updated dev ps-execution container image (`shaneapp://deployPsExecution`), live-test at least one of the 12 supported templates against the testbed tenant (target: `action.create-shared-mailbox` → new zz-test-* shared mailbox, additive/unlicensed), then promote exactly the live-verified `write_action_catalog` rows (subset of ids 185-196) to `execution_ready`. Watching for the flagged Exchange RBAC write-role gap on the app-only identity.
