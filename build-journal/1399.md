# #1399 — Projects page honest tri-state dataState + PROJECT_META live wiring

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 5bcffdd7e, 9521dfc45

## Log
- 2026-08-27 ⏳ IN FLIGHT — fixing the same fixture-fallback ambiguity #1398 fixed for useRetainerLive, applied to the Projects page: projectsLive.ts's boolean live/fixture dataState, and PROJECT_META's no-live-source header.
- 2026-08-27 ✅ DONE — `useProjectsLive` now reports 5 real states (loading/live/empty/unconfigured/error) instead of the old boolean live/fixture flag; `projectsWire.ts`'s new `toProjectMeta` wires the header (title, SOW-type label, intro, date-range terms, day-of-schedule) to real `projectsTable` columns and returns `null` for fields with no real column (SOW number, fee, delivery lead) rather than inventing them. `portal-v2-projects.tsx` now renders an honest `NoScanDataState` for the whole project body on unconfigured/error, replacing the fictional Copilot Readiness Assessment fixture shown to every account before. Verification: `projectsWire.test.ts` (18/18 passing, 3 new `toProjectMeta` cases), `tsc --noEmit` clean on all touched files, `test-manifests/portal/portal-v2-projects.json` rewritten (PJ1/PJ2/PJ20/PJ21 now assert the honest empty state + `unconfigured` source marker) and re-registered at its existing spot in `_regression-suite.json`. Ran `shaneapp://runTest` against the manifest — it reported "no steps matched session tag" because `shaneapp://runTest` executes against the DEPLOYED dev-server checkout, which hasn't merged this worktree's commits yet; a true green run needs to happen post-merge, same caveat #1398's own bookend documents.
