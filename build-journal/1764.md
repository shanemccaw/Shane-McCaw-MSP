# #1764 — msp-changes.ts create schema only allows 5 of 8 widened Change Control categories

- **Status:** ✅ DONE 2026-09-03T03:15:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 33c43e6cb

## Log
- 2026-09-03T02:54:30Z ⏳ IN FLIGHT — widen `createChangeRequestSchema.category` in
  `artifacts/api-server/src/routes/msp-changes.ts` from the stale 5-value literal enum
  to the real 8-value `CHANGE_REQUEST_CATEGORIES` exported by
  `artifacts/api-server/src/lib/portal-change-control.ts`, so the MSP console door
  agrees with the portal wizard door and the `msp_change_requests.category` column.
- 2026-09-03T03:15:00Z ✅ DONE — imported `CHANGE_REQUEST_CATEGORIES` from
  `portal-change-control.ts` and replaced the stale 5-value literal `z.enum([...])`
  with `z.enum(CHANGE_REQUEST_CATEGORIES)` (commit 33c43e6cb). Confirmed
  `workloadForCategory`/`categoryForWorkload`/`pillarHintForCategory` already handle
  all 8 categories (SharePoint/Purview/Teams included) with no fallback-to-Identity
  edge case introduced. Pattern matches the identical `z.enum(CHANGE_REQUEST_WORKLOADS)`
  already used in `msp-change-freeze-windows.ts`. Full `tsc --noEmit` in this worktree
  shows pre-existing, unrelated module-resolution errors (zod/drizzle-orm/lib/db dist
  not junctioned into `artifacts/api-server/node_modules` for this worktree — the known
  worktree dependency-isolation gap, #1988) affecting dozens of unrelated files;
  confirmed my one-line change is not the cause (same errors present on unrelated files,
  and the identical `z.enum(CONST_ARRAY)` pattern already ships elsewhere). Branch
  verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
  No test manifest exists for this backend-only route and none was required (no UI/API
  surface touched under artifacts/msp-portal or artifacts/shane-mccaw-consulting).
