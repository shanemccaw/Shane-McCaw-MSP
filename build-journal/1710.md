# #1710 — BuildConsole: "AI Batter Up" review panel — Yes promotes to Batter Up, No demotes to Backlog

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 794fa5512 (feature), ec7adcaf4 (merge-verified HEAD on origin/main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — read #1710's body (no comments yet) and #1709's landed BatterUpPanel/BatterUpQueueService/GitHubApiClient code. About to add a new, separate "AI Batter Up" review panel (polled independently) with per-row Yes/No: Yes calls updateProjectV2ItemFieldValue to promote Status → Batter Up (09b1927f), No demotes to Backlog (63cc47c8). Yes never launches anything directly — #1709's existing panel picks the promoted item up on its own next refresh. #1709's panel/auto-queue logic and the existing Queue/Send to Builder/Cancel actions are untouched.
- 2026-08-29 ✅ DONE — shipped new `AiBatterUpPanel` (XAML + code-behind), `AiBatterUpQueueService`, and additive `GitHubApiClient` members (`GetAiBatterUpIssuesAsync`, `SetProjectItemStatusAsync`, `AiBatterUpBoardIssue`) at commit 794fa5512. Panel sits above #1709's BatterUpPanel in the Build Queue column, polls the real "AI Batter Up" board status independently on its own 90s timer, and shows # / title / model-effort / buildSet (or "no BUILD: comment yet") per row — reusing #1709's `BatterUpQueueService.FindBuildCommentAsync` read-only. Yes calls the real `updateProjectV2ItemFieldValue` mutation to flip Status → Batter Up and does nothing else (no call to `BuildQueuePostgresClient`/`_queueDb` anywhere in the promote path — confirmed by code inspection, since a live WPF UI session can't be driven headlessly from this sandbox); #1709's own BatterUpPanel.Initialize/RefreshAsync loop is what actually queues it on its own next poll, unmodified. No flips Status → Backlog the same way. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` is clean (0 warnings, 0 errors). Verified merged: `git merge-base --is-ancestor 794fa5512 origin/main` passes via HEAD ec7adcaf4. `git status --porcelain` clean. No test manifest — desktop/BuildConsole is out of scope per CLAUDE.md.
