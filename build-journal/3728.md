# #3728 — Retry on a `Reply → …` row drops resume_session_id

- **Status:** ⏳ IN FLIGHT 2026-09-12T00:35:33Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** _pending_

## Log
- 2026-09-12T00:35:33Z ⏳ IN FLIGHT — Found while reconstructing the context of build 2382, whose entire prompt
  was the word `Retry` with no session to resolve it against. Both Retry paths in
  `Controls/BuildQueuePanel.xaml.cs` (`QuickRetryAsync` and the `🔄 Retry (start over)` context
  menu item) pass `resumeSessionId: null` into `QueueBuildAsync`. That is right for a normal
  dispatch row and wrong for a `Reply → …` row, whose prompt is a conversational fragment that
  only means anything against the session it replies to. Live evidence: queue rows 2364 →
  2376 (`resume_session_id` 53af9d01) → 2382 (**NULL**). Plan: carry `ResumeSessionId` forward
  when retrying a resume-only row, leaving start-over semantics untouched for normal builds.
