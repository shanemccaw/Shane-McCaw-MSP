# #2373 — Shot Vault: "Expand full page here" and "Send to tab"

- **Status:** ✅ DONE 2026-09-02T22:52:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 4b956a1aad664adf6f994646a936a62d5069541b

## Log
- 2026-09-02T19:47:00Z ⏳ IN FLIGHT — Read #2373 body + Feature #2367 body/comments. Item 6 of 7 under
  Feature: Shot Vault (`ShaneBuilder-FullFeature.md`). Confirmed the whole Shot Vault panel has zero
  scaffold in code today (only the `notBuilt` rail toggle, `MainWindow.xaml.cs` ~L677/503) — sibling
  builds #2368-#2372 (search/tags/runs/thumbnails/per-shot-copy) are standing up the panel shell
  concurrently. A peer session on #2372 (Panel: per-shot Copy) flagged the same collision risk
  first; coordinated via SendMessage — they land the base `ShotVaultPanel` scaffold first, this
  build then adds the "Expand full page here" / "Send to tab" header chrome on top, following the
  exact established pattern from Git #2312 (Git Panel) / #2365 (Batter Up panel):
  `MainWindow.GitPanel.cs` / `MainWindow.BatterUpPanel.cs`. Waiting on their push to origin/main.
- 2026-09-02T22:52:00Z ✅ DONE — Peer's Shot Vault scaffold landed (`d2c60a94e`, #2372). Rebased on
  top of it and added the real "Expand full page here" / "Send to tab" chrome (commit
  `4b956a1a`), reusing the exact #2312/#2365 mechanism: two header glyph buttons
  (`BtnShotVaultExpand`, `BtnShotVaultSendToTab`, visibility gated on `_leftPanelSource ==
  "ShotVault"`), a `ShotVaultDocSnapshot` record freezing the real shot list at send time (not a
  live drill surface — same honesty convention as `BatterUpDocSnapshot`), a new
  `ShotVaultItemDock`/`ShotVaultItemDocHost` document surface in `MainWindow.xaml`, `TabDef`
  extended with `ShotVaultSnapshot`/`IsShotVaultDoc`, and `RenderShotVaultDoc` wired into
  `SelectTab`. The sent doc's rows reuse the same real `BuildShotVaultRow` (thumbnail + working
  Copy-to-clipboard) the rail panel uses — a "sent" copy still functions, it isn't an inert
  screenshot of the panel. Verified: `dotnet build ShaneBuilder.csproj -c Debug` succeeds, 0
  warnings/0 errors, both before and after the final rebase onto origin/main.
  `node scripts/dev-server/verify-branch-merged.mjs` confirms `agent/2373-q1215` (4b956a1a) is an
  ancestor of `origin/main`. `git status --porcelain` clean. No findings filed — no genuine
  gap/bug surfaced outside this issue's own scope.
