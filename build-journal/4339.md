# #4339 — Buy.tsx rehydrates its checkout session from localStorage

- **Status:** ✅ DONE 2026-09-17T03:06:55Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** ac425555c (code + manifest), a39d8b969 (manifest hash)

## Log
- 2026-09-17T02:58:49Z ⏳ IN FLIGHT — re-verify Buy.tsx stage machine post-#4377/#4378, then rehydrate sessionId from BUY_SESSION_STORAGE_KEY with server-side consent-status confirmation
- 2026-09-17T03:06:55Z ✅ DONE — Re-verified after #4377/#4378: gap still real (initialState sessionId: null, ensureSession short-circuits only on st.sessionId, nothing reads BUY_SESSION_STORAGE_KEY back outside useBuyPackLive). Reorder changes the scope: Monitoring/Packs now create the account before consent and resume by sign-in (#4377, BuyResumeSignIn), so a stored id is deliberately NOT trusted for them. Retainer (still identity → consent → pay → account) now rehydrates on mount: stored id (UUID_RE) + a snapshot of the identity/selection it was minted from (smc_buy_flow_resume; the public session reads return no PII), confirmed via GET /api/public/flow/consent-status — pending/consented resumed (connected when tenant linked + consented, scanSkipped from readConsentSkipped), 404/400 clears storage, paid left alone so the next connect/pay mints fresh; buyer edits made during the check win. invalidated() clears both keys. Verified: shane-mccaw-consulting tsc exit 0; consent-status probed live on :8080 for real paid/pending/expired/invalid sessions (200 paid, 200 pending, 404 session_expired, 400 session_invalid); shaneapp://runTest buy-page.json onlyTag=4339-rehydrate 2/2 PASS. Not verified: the reload itself in a browser (runner cannot seed localStorage). No migrations. No findings filed. Tree clean.
