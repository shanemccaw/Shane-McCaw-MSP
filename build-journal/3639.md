# #3639 — retention-lifecycle audit writes silently fail (clientId FK'd to users.id, tenants.id passed)

- **Status:** ✅ DONE 2026-09-12T21:18:33Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 4d542e6b3

## Log
- 2026-09-12T21:04:22Z ⏳ IN FLIGHT — investigated all 6 `createAuditLog(...)` call sites in `artifacts/api-server/src/lib/retention/lifecycle.ts`. Each already computes and passes a real `users.id` as `actorUserId` (`actor.userId` / `input.actor.userId`), but the `clientId` field on the same call passes `tenantId` instead, violating `audit_logs.clientId`'s FK to `users.id`. Fix: swap each `clientId: <...>.tenantId` to the same real `actor.userId ?? null` already computed for `actorUserId` at that call site — consistent with every other `createAuditLog` caller in the codebase (portal/admin routes all pass a real `users.id`, never a tenant id). Next: apply edits, run a real local-DB insert to confirm the FK now passes.
- 2026-09-12T21:18:33Z ✅ DONE — applied the fix at all 6 call sites (assertDeleteAllowed, softDelete, restore, requestAcceleration, decideAcceleration, purgeNow), commit 4d542e6b3 on origin/main. Verified: (1) `tsc --noEmit` clean; (2) direct `psql` insert into `audit_logs` with `client_id` set to a real `users.id` (1) succeeds where the old `tenants.id` value violated `audit_logs_client_id_users_id_fk`, row inserted and cleaned up; (3) ran `msp-retention-queue-poam-producer.live-db.test.ts` end to end against local Postgres — passes. While verifying, found and filed two related findings, both parented under #1944 and set to AI Batter Up: #3841 (that same live-db test's synthetic customer actor id has no matching real `users` row, so its own audit write now fails on `actor_user_id`'s FK instead — pre-existing, unmasked by this fix, not fixed here) and #3842 (5 tenant-scoped columns with no registered purger/exemption + 2 unrelated live-db test files red in `lib/retention/purgers/` — pre-existing, unrelated to this change). Working tree clean, branch verified merged into origin/main via `verify-branch-merged.mjs`.
