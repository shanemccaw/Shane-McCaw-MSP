# #3257 — Meds batch cards always fully expand -- design shows only current time-of-day batch active

- **Status:** ✅ DONE 2026-09-13T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** none — no code change needed (already resolved by prior commits 967bffbe2 / dbb12d08b / 18880171b)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Investigating `medBatchCard()`/`viewMeds()` in `web/shanes-life/public/app.js` against `Design/design_handoff_shanes_life/Shanes Life 07 - Meds.dc.html` per the issue's real, confirmed mismatch report.
- 2026-09-13T00:20:00Z ✅ DONE — No-op. This was already resolved by later work: Git #3282 (`967bffbe2`, "Meds: always compact one-line-per-batch, merge pet meds/feeding") introduced the `later` flag on `medBatchCard()` and the `.med-batch-collapsed` dimmed one-line row (batch name + item names + plain "later" label, no slide control), and #3324 (`18880171b`) added the real 6am eligibility gate for Morning becoming "current". `viewMeds()` now computes exactly one `current` batch (earliest untaken, clock-eligible, timed batch) and passes `later: true` to every other untaken timed batch, matching `Shanes Life 07 - Meds.dc.html` lines 62-74's collapsed/active split. Verified by reading the current `medBatchCard()`/`viewMeds()` implementation directly (app.js lines ~3768-4113) and confirming the git history predates this issue's filing under #3191/#3282/#3324. No fixture/hardcoded data touched, nothing left uncommitted.
