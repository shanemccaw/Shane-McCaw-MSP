# #3570 — msp-settings approve-purchases grant has no rung check

- **Status:** ✅ DONE 2026-09-11T05:10:07Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** ce2b629fe (fix + live-DB test); 2411d19e0 (IN FLIGHT bookend)

## Log
- 2026-09-11T04:45:50Z ⏳ IN FLIGHT — adding the missing MSPOperator-and-above rung check to `PATCH /msp/settings/users/:userId/approve-purchases` target selection (and the listing's toggle), per the #3570 dispatch.
- 2026-09-11T05:10:07Z ✅ DONE — ce2b629fe: the approve-purchases PATCH now refuses to GRANT `cap.purchases.approve` (400) unless the target's effective rung clears `ladder.msp-operator` — the decide route's own gate, evaluated with the target as principal via `userClearsLadderCapability` (live allow set {MSPOperator, MSPAdmin, PlatformAdmin}, same as #3408's `purchase_rungs`). 503 if the model is unreadable. Revoke stays open so a stale grant can be removed. `GET /msp/settings/users` adds `approvePurchasesGrantable` per user. Verification: api-server `tsc --noEmit` exit 0; new `msp-settings-approve-purchases-rung.live-db.test.ts` 5/5 against local Postgres and 4/5 FAIL against the pre-fix route (Customer/Free grant returned 200, Customer returned by `purchaseApproverUserIds`); neighbouring `msp-settings-target-role-ceiling.live-db`, `msp-settings-user-security.live-db`, `msp-settings-portal-links` pass; `check-rbac-parity` 9/9 PASS. No migration. Finding filed: #3637 (AdminV2 `POST /admin/rbac/user/:userId/roles` still grants `cap.purchases.approve` with no rung check). No pre-existing dirty files in the worktree.
