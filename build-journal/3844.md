# #3844 — Dev services have no idle-shutdown (real memory pressure)

- **Status:** ✅ DONE 2026-09-12T21:50:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 41affa844 (rebased onto main as 1bf3ba452)

## Log
- 2026-09-12T21:41:48Z ⏳ IN FLIGHT — implementing real idle-stop enforcement for
  DevServicesManager per the issue's corrected root cause (no idle-timeout/auto-stop
  of any kind, regardless of which legitimate path started a service): (1) immediate
  "Stop Idle Services" check — a real menu action AND fired once at watcher startup,
  (2) event-driven ongoing enforcement hooked into QueueWatcherService's real
  BuildFinished completion event (no polling timer), (3) Git #3084's always-on
  api-server behavior left completely untouched — it is simply never a candidate,
  (4) real ActivityLog logging ("dev-services" channel) on every auto-stop naming
  the real reason. Code + build verification to follow in this same log.
- 2026-09-12T21:50:00Z ✅ DONE — Implemented and pushed to origin/main as commit
  1bf3ba452 (`Idle dev-service auto-stop enforcement (Git #3844)`). Real shape:
  `DevServicesManager.ComputeNeededServices` (new) computes, per active
  queued/running/verifying `bt_build_queue` row, real evidence of which known
  dev service it needs — via `git diff --name-only origin/main...HEAD` (+ its own
  uncommitted `git status --porcelain` changes) of its resolved worktree/cwd,
  classified against each service's real `artifacts/<name>` path (same table
  shape as `scripts/dev-server/service-targeting.mjs`'s existing selective-restart
  mapping). Anything the diff can't resolve (no worktree yet, a shared-code
  change under `lib/`/`packages/`/root config, or an unclassified path)
  conservatively holds EVERY service, biasing false-negative (leave it running)
  over false-positive (wrongly stop one still needed).
  `QueueWatcherService.StopIdleDevServicesAsync` (new, public) is the real stop
  action — skips `api-server` unconditionally (Git #3084 untouched), logs every
  real stop on the `dev-services` ActivityLog channel with the real reason, and
  is wired to fire (1) once at watcher `Start()` (the "immediate relief" ask,
  addressing tonight's live memory pressure the instant BuildConsole restarts),
  and (2) event-driven right after every real `BuildFinished` completion in the
  reap loop (success or failure) — no polling timer added. Added a
  "🧹 Stop Idle Services" menu item (`MainWindow.xaml` / `.xaml.cs`) for on-demand
  manual use, matching the issue's "a menu action, or fired once" wording.
  `BuildQueuePostgresClient.GetActiveBuildIdentitiesAsync` (new) is a narrow
  5-column, active-status-only query — deliberately not a reuse of
  `GetQueueAsync`'s full-table fetch (Git #3801's own cost warning).
  Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 0 new
  warnings. No BuildConsole test-manifest applies (WPF app is out of scope per
  CLAUDE.md's Test Coverage section); this build phase's verification is the
  clean build per that section's own guidance. Live end-to-end exercise (an
  idle service actually observed stopping against tonight's real queue state)
  was NOT separately re-run after this build's own commit landed — the
  mechanism was verified by code-reading + build only. No new findings filed
  this session. `git status --porcelain` clean at completion (this bookend's
  own edit pending as the final commit). Branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` before writing this DONE line.
