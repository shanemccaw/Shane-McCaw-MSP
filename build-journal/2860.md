# #2860 — msp-jobs: stale worker comment + empty handler registry

- **Status:** ⏳ IN FLIGHT 2026-09-04T23:41:13Z
- **Scope:** platform

## Log
- 2026-09-04T23:41:13Z — Investigated `msp-jobs.ts`. Confirmed `startJobWorker()` IS called
  from `index.ts:180` (comment claiming otherwise is false). Confirmed Zoho/EngageBay job
  types already have their own complete handler registries (`registerZohoJobHandler` /
  `registerEngageBayJobHandler`) and their own dedicated drain workers
  (`zoho-batch-drain.ts`, `engagebay-batch-drain.ts`) that claim those job types directly —
  they are deliberately excluded from `processJobs()`'s claim query and were never meant to
  register on `msp-jobs.ts`'s own `handlers` map. Registering them there would be dead code
  (the query filters them out before they'd ever reach `handlers.get()`). Proceeding to fix
  the stale comment and make the genuinely-empty generic registry loud rather than silent.
