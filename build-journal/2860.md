# #2860 — msp-jobs: stale worker comment + empty handler registry

- **Status:** ✅ DONE 2026-09-04T23:58:00Z
- **Scope:** platform

## Log
- 2026-09-04T23:41:13Z — Investigated `msp-jobs.ts`. Confirmed `startJobWorker()` IS called
  from `index.ts:180` (comment claiming otherwise is false). Confirmed Zoho/EngageBay job
  types already have their own complete handler registries (`registerZohoJobHandler` /
  `registerEngageBayJobHandler`) and their own dedicated drain workers
  (`zoho-batch-drain.ts`, `engagebay-batch-drain.ts`) that claim those job types directly —
  they are deliberately excluded from `processJobs()`'s claim query and were never meant to
  register on `msp-jobs.ts`'s own `handlers` map. Registering them there would be dead code
  (the query filters them out before they'd ever reach `handlers.get()`). Proceeding to fix
  the stale comment and make the genuinely-empty generic registry loud rather than silent.

- 2026-09-04T23:58:00Z — ✅ DONE — Fixed the stale comment at
  `artifacts/api-server/src/lib/msp-jobs.ts:104-112`: it now states plainly that
  `startJobWorker()` is called from `index.ts` and polls live, and explains why
  `handlers` is genuinely empty today (only Zoho/EngageBay producers exist, and
  those are excluded from this worker's claim query by design — they're handled by
  their own `registerZohoJobHandler`/`registerEngageBayJobHandler` registries and
  dedicated drain workers, already fully wired). Did **not** register Zoho/
  EngageBay handlers via `registerJobHandler()` as the build prompt suggested —
  confirmed those job types are excluded from this worker's claim query
  (`job_type NOT LIKE 'zoho.%' AND left(job_type,5)<>'zoho_' AND
  left(job_type,10)<>'engagebay_'`), so registering them there would be dead code
  that can never fire; they are already correctly handled by their own dedicated
  registries/drain workers (`zoho-batch-drain.ts`, `engagebay-batch-drain.ts`).
  The no-handler branch itself was already loud (log.warn + captureDlqFailure +
  DLQ row) — no change needed there. No new backend gap found; no findings filed.
  Commit: `9d7944c52ff3a5e5cb6f99e8e93f3bab7fa21422` (rebased onto main as
  `20b1bcc4`). Verified via `verify-branch-merged.mjs`: merged into origin/main.
  `git status --porcelain` clean.
