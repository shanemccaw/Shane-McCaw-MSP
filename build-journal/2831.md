# #2831 — Monitoring: compliance-surface coverage follow-up (26 remaining gaps from #2762)

- **Status:** ✅ DONE 2026-09-04T23:40:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 9b712e64d (IN FLIGHT bookend, on main as d5015efee after rebase+push), 43735a743 (real work, on main as 4d8270e9c after rebase+push)

## Log
- 2026-09-04T23:05:00Z ⏳ IN FLIGHT — pulled #2831's body/comments and #2762's migration header (the authoritative per-resource reason list). Confirmed live query still shows `surface='compliance' AND availability='available_now' AND check_coverage_count=0` = 26 rows, matching the issue's own count. Confirmed `#2830` (local dev-server / `shaneapp://executeCmdlet` outage) is still OPEN — `node scripts/dev-server/status.mjs` shows the server process DEAD on port 8080 — so the "never probed" bucket (8 cmdlets), the `Get-InsiderRiskEntityList` identity-param bucket, and the `Get-RoleGroupMember` fan-out bucket all remain genuinely blocked exactly as #2762 documented; not re-attempting those.
- Investigated `ps_capability_survey_results` + `config_resource_properties` directly (no fresh PS probe needed) for the two buckets #2762 left open on *design*, not *access*, grounds: the FilePlanProperty family + SupervisoryReviewPolicy/Rule (bucket 5) and the DeviceConditionalAccessRule/DeviceConfigurationRule nested-child bucket (bucket 6). Also found two resources in the live 26 that #2762's own reason list never mentioned at all (`m365dsc:SCPolicyConfig`, `m365dsc:SCDLPSensitiveInformationTypeRulePackage`) — both have real, repeated `status='ok'` survey evidence with genuine risk-bearing fields, a real oversight in the prior pass rather than a documented blocker.
- 2026-09-04T23:20:00Z ⏳ IN FLIGHT — designed 10 real checks against real live-observed/declared field evidence: `IsDlpSimulationOptedIn`/`serverDlpEnabled` (Get-PolicyConfig, live), `IsValid` (Get-DlpSensitiveInformationTypeRulePackage, live), `Disabled`/`CitationUrl` (4 FilePlanProperty subtypes, live — confirmed the OTHER two subtypes, ReferenceId/SubCategory, genuinely have no such field in their real declared `config_resource_properties` schema, so left honestly uncovered), `Reviewers`/`SamplingRate` (SupervisoryReviewPolicy/Rule, declared), `PasswordRequired`/`TargetGroups` (DeviceConditionalAccessRule/DeviceConfigurationRule — made the deliberate rule-vs-policy design call #2762's header asked for: rule-level targeted overrides are a distinct real risk from the tenant-wide policy default, not double-counting). All 10 `ps_cmdlet_key` catalog entries already existed in `services/ps-execution/cmdlet-catalog.ps1` (from #1961's snapshot pass) and were unused by any check before this — no catalog edits needed. Ran the real `matchEndpointToResource` directly against the parsed catalog + full `config_resources` table before writing SQL: all 10 resolved to the intended resource (9 `high`, 1 `low` — `compliance:supervisory-review-policy-no-reviewers`, same pre-existing naming-quirk class as #2762's own `retention-event-type-unlinked`).
- Wrote and ran `lib/db/migrations/manual/2026-09-04-compliance-surface-coverage-2831.sql` against local `DATABASE_URL`: 10 `monitor_checks` rows, 40 `monitoring_package_checks` memberships (core:premier/core:growth/detail:full-item-collection/assess:copilot-readiness), 10 `config_resource_check_coverage` rows, coverage recompute. Real effect confirmed: `surface='compliance' AND availability='available_now' AND check_coverage_count=0` dropped from 26 → 16 (the remaining 16 stay genuinely blocked, unchanged from #2762's own per-resource reasons — 8 never-probed, InsiderRiskEntityList identity error, RoleGroupMember Identity fan-out, 2 cmdlets of unconfirmed existence, ReferenceId/SubCategory with no risk-bearing field, 1 Graph label endpoint).
- 2026-09-04T23:35:00Z ✅ DONE — Wrote a permanent vitest regression test (`artifacts/api-server/src/lib/__tests__/compliance-surface-coverage-2831.test.ts`, 22 assertions, registered in `vitest.config.ts`'s include list, mirroring #2762's own precedent) — 22/22 pass in isolation, and 48/48 pass running alongside #2762's own test file together. `npx tsc --noEmit` on api-server: clean. Ran the full api-server vitest suite twice in the background while doing other verification; the first (no `DATABASE_URL`) run showed 258 passed/48 failed test files, all 48 pre-existing DB-dependent failures unrelated to this change (my new test file was among the passing 258). The second full run showed spurious "Cannot find module" failures across many unrelated files — traced to two `npx vitest run` invocations racing over the same Vite cache concurrently (a self-inflicted artifact of running two full-suite verifications back to back), not a real regression; re-confirmed by re-running just the two compliance-surface test files together afterward (48/48 pass, no cache contention). `git status --porcelain`: clean. `verify-branch-merged.mjs`: OK. Committed as `43735a743`, rebased onto `origin/main` and pushed (`git push origin HEAD:main`) — landed as `4d8270e9c`. No new findings filed this session (the two oversight resources found were fixed in-session, not left as findings). Labels flipped `in-flight`→`complete` on #2831; completion comment posted.
