# #2831 — Monitoring: compliance-surface coverage follow-up (26 remaining gaps from #2762)

- **Status:** ⏳ IN FLIGHT 2026-09-04T23:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T23:05:00Z ⏳ IN FLIGHT — pulled #2831's body/comments and #2762's migration header (the authoritative per-resource reason list). Confirmed live query still shows `surface='compliance' AND availability='available_now' AND check_coverage_count=0` = 26 rows, matching the issue's own count. Confirmed `#2830` (local dev-server / `shaneapp://executeCmdlet` outage) is still OPEN — `node scripts/dev-server/status.mjs` shows the server process DEAD on port 8080 — so the "never probed" bucket (8 cmdlets), the `Get-InsiderRiskEntityList` identity-param bucket, and the `Get-RoleGroupMember` fan-out bucket all remain genuinely blocked exactly as #2762 documented; not re-attempting those.
- Investigating `ps_capability_survey_results` + `config_resource_properties` directly (no fresh PS probe needed) for the two buckets #2762 left open on *design*, not *access*, grounds: the FilePlanProperty family + SupervisoryReviewPolicy/Rule (bucket 5) and the DeviceConditionalAccessRule/DeviceConfigurationRule nested-child bucket (bucket 6). Also found two resources in the live 26 that #2762's own reason list never mentioned at all (`m365dsc:SCPolicyConfig`, `m365dsc:SCDLPSensitiveInformationTypeRulePackage`) — both have real, repeated `status='ok'` survey evidence with genuine risk-bearing fields, a real oversight in the prior pass rather than a documented blocker.
