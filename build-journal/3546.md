# #3546 — Webhooks: 'Edit label, URL and events' is an inert stub despite a real, unchanged PATCH route

- **Status:** ✅ DONE 2026-09-10T21:59:15Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** `da30bd989` (real implementation + manifest), `896afc5ba` (IN FLIGHT bookend)

## Log
- 2026-09-10T21:48:06Z ⏳ IN FLIGHT — building a real `EditWebhookDialog` (pre-filled label/url/event-picker/isActive) in `artifacts/portal/src/components/webhooks/EditWebhookDialog.tsx`, wiring it into `webhooks.tsx`'s `confirm.kind === "edit"` branch to call the real, unchanged `PATCH /api/portal/webhooks/:webhookId` route with only the changed fields, replacing the inert confirm dialog.
- 2026-09-10T21:59:15Z ✅ DONE — Shipped `EditWebhookDialog.tsx` (new file), wired into `pages/webhooks.tsx`: the "Edit label, URL and events" button now opens a real, pre-filled dialog (label/url/event-picker/isActive toggle) instead of the "Editing is wired, this prototype is not" confirm stub. `ConfirmSpec`'s `"edit"` kind and its dead `runConfirm` no-op branch were removed entirely — editing no longer routes through the confirm-modal machinery at all. Submits only the fields actually changed via the same PATCH route already used for the isActive toggle (unchanged, no backend work needed — this was purely a missing surface). `pnpm run typecheck` and `pnpm run build` both pass clean in `artifacts/portal`.
  Extended `test-manifests/portal/webhooks.json` (sessionTag `3546`): TEST5B proves the PATCH contract accepts label+url+eventTypes together (previously only isActive was exercised); UI10-24 create a real endpoint via the UI, open the real `webhook-edit-modal`, verify the pre-filled label/url fields render, cancel without writing (TEST5B already proves the real PATCH round trip from the API side), then clean up via the page's own delete flow so the run leaves nothing behind. Registered already in `_regression-suite.json` (pre-existing entry).
  **Test execution, honest result:** ran via `shaneapp://runTest` against the real worktree path. Both the pre-existing untagged steps (SETUP/TEST1-8) and my new tagged steps failed identically — `unresolved variable(s): {{TEST_CUSTOMER_EMAIL}}, {{TEST_CUSTOMER_PASSWORD}}` (BuildConsole's Settings > Test Environment Variables aren't configured in this environment) and a UI navigation failure against `localhost:5175` (the portal dev server isn't running here). This is a genuine pre-existing environment gap — not something this session introduced or can fix from here (it needs real testbed customer credentials + a running portal dev server, which this session doesn't have standing authority to provision). Confirmed via `curl` that the portal dev server is not listening on 5175. Real verification actually performed instead: `pnpm run typecheck` (clean) and `pnpm run build` (clean, `PORT=5183 pnpm run build` succeeded) in `artifacts/portal`, plus a full read-through of the PATCH route/schema (`webhooks.ts:79-84,274-338`) confirming the wire contract the new dialog calls matches exactly.
  No new findings filed — the only gap (the inert edit stub) was the issue itself, and it's now closed by this commit. `git status --porcelain` clean (apart from this file's own edit). Branch `da30bd989` confirmed pushed directly onto `origin/main` (fast-forward, no divergence).
