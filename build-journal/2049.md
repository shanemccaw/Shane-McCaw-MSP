# #2049 — Remediation KB: author the compliance: domain (7 active checks)

- **Status:** ✅ DONE 2026-09-02T02:56:25Z
- **Scope:** platform
- **Started:** 2026-09-02
- **Commit(s):** f0f4a6635 (bookend IN FLIGHT), 244c59399 (migration, merged to main)

## Log
- 2026-09-02T02:48:02Z ⏳ IN FLIGHT — authoring verified remediation_knowledge_base rows for the 7 active compliance:* checks (audit-log-retention, dlp-incidents, eeeu-site-sharing, label-errors, missing-labels, weak-dlp-policies, zero-dlp-policies), per #1924's authoring standard. Real Microsoft Learn docs to be fetched in-session per row; idempotent seed migration keyed on check_key.
- 2026-09-02T02:56:25Z ✅ DONE — Authored and published all 7 compliance:* rows. Every row verified against real Microsoft Learn pages actually fetched in this session (audit-log-retention-policies, dlp-simulation-mode-get-started, dlp-learn-about-dlp, dlp-create-deploy-policy, turn-external-sharing-on-or-off, create-sensitivity-labels, get-started-with-sensitivity-labels, data-classification-activity-explorer, plus the Get-UnifiedAuditLogRetentionPolicy/New-UnifiedAuditLogRetentionPolicy/Get-Label/Get-LabelPolicy/Set-DlpCompliancePolicy/Set-SPOTenant/Set-SPOSite/Export-ActivityExplorerData PowerShell cmdlet reference pages). All `status='published'`, `verified_by` is honest agent attribution ("Claude Sonnet 5 (build #2049) — agent-authored, verified against Microsoft Learn; awaiting human spot-check") — never a human name. `fix_route_capability`: `you_must_run` for audit-log-retention, eeeu-site-sharing, label-errors, weak-dlp-policies (real verified PowerShell fix exists); `admin_center_only` for dlp-incidents (investigative signal, no single fix), missing-labels (no verified Set-Label toggle for Disabled state was found in the docs fetched — did not fabricate one), and zero-dlp-policies (policy design is a per-tenant decision, not a blind script — mirrors identity:ca-policy-count's reasoning). None set `we_can_run`.
  - Delivery: idempotent seed migration `lib/db/migrations/manual/2026-09-02-remediation-kb-compliance-domain-2049.sql`, keyed on `check_key` via `ON CONFLICT DO UPDATE`. Ran against local `DATABASE_URL`: before 118 rows (118 published) → after 125 rows (125 published), all 7 new rows published. Re-ran the same file a second time to confirm idempotency — row count stayed at 125, no duplicates. Logged on #1630 is N/A (no destructive/manual-only migration — this one is agent-run, additive DDL-free content seed).
  - Commits: `f0f4a6635` (bookend IN FLIGHT, standalone) and `244c59399` (the migration file). Rebased onto current `origin/main` (which had moved 5 commits ahead under concurrent builds) and pushed directly to `main` — fast-forward, no conflicts. `verify-branch-merged.mjs` confirms `agent/2049-q1147` is merged into `origin/main`.
  - Working tree clean (`git status --porcelain` empty) at completion.
