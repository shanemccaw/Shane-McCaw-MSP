# #3471 — Feature: Remediation Plan Execution Mode (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T20:17:30Z
- **Scope:** buildconsole (desktop/MyArchitect) + api-server (small additive route)
- **Started:** 2026-09-10
- **Commit(s):** `8b279bc1e` (real, verified — pushed straight to `main`, fast-forwarded, `verify-branch-merged.mjs`: OK)

## Log
- 2026-09-10T19:59:40Z ⏳ IN FLIGHT — Pulled issue body + all 4 comments. Prior
  session (commit `d3c5cc09f`) shipped the real HTTP clients
  (`RemediationTrackerService`, `ChangeControlService`) and correctly stopped
  there — #3493/#3459/#3460 were all still open at the time. Since then #3493
  (UI Shell Redesign) and #3459 (Embedded PowerShell Console) both closed for
  real (verified via `gh issue view --json state`); #3460 (Script Library) is
  still open with no UI landed (confirmed: no `*ScriptLibrary*` file under
  `desktop/MyArchitect/`). Cleared the two now-stale `blocked_by` edges
  (#3493, #3459); left #3460's edge in place since it still genuinely gates
  the one checklist item this build does NOT attempt (Script-Library-into-
  Console execute wiring). Removed `blocked` label (real work proceeds now),
  kept `in-flight`.

  Scoping this build to the two checklist items #3493 landing unblocks:
  - Unified item browser UI — both tracker steps AND change requests in one
    Home-tab gallery (the issue's own words: "both sources in one view, real
    instruction/action text per item"), replacing the narrower Change-
    Requests-only gallery #3493's own session added as its gallery→
    contextual-tab→workspace proof-of-concept.
  - PUT tracker step status per item type (human-action/attest for change
    requests already wired by that same #3493 session).

  Real gap found along the way: `GET /api/msp/customers/:customerId/
  remediation-tracker` (`msp-remediation-tracker.ts`) returns only
  stepId/status/timestamps — no human-readable title/pillar for a step, so
  there is nothing for a browse row to display as "real instruction/action
  text per item" for tracker steps. The real title/pillar catalogue already
  exists server-side (`remediation-tracker-catalogue.ts`, the same one
  `msp-remediation-tracker-export.ts`'s CSV/PDF already reads) — extracted,
  not authored. Adding one small additive static-data route
  (`GET /api/msp/remediation-tracker/catalogue`) rather than inventing a
  duplicate vocabulary client-side, per the HARD RULE (extend the backend,
  don't stop at an empty state or fake the text).

- 2026-09-10T20:17:30Z 🛑 BLOCKED — Real work shipped and verified, commit
  `8b279bc1e` (rebased onto `origin/main` twice under concurrent load —
  MainWindow.xaml.cs conflicted once with a concurrent #3464 session's own
  additions; resolved by keeping both non-overlapping blocks, verified with
  a clean `dotnet build` after resolution before pushing):

  - **api-server** — `GET /api/msp/customers/:customerId/remediation-tracker/catalogue`
    (`msp-remediation-tracker.ts`): all 28 real steps
    (`remediation-tracker-catalogue.ts` — the same catalogue
    `msp-remediation-tracker-export.ts`'s CSV/PDF already reads, extracted
    not authored) joined with the customer's real stored state, defaulting
    an untouched step to `not_started`/`unverified` (same default the export
    route's own `buildExportRows` already uses). Also returns the real,
    server-owned `statusLabels` + `assignableStatuses` (`accepted_risk`
    excluded — the server's own PUT already rejects setting it directly,
    it's the customer's signed decline-to-risk fact). 13/13 tests pass
    (3 new), `tsc --noEmit` clean. No migration needed — static TS data, no
    schema change.
  - **MyArchitect** — `RemediationTrackerCatalogueStep`/-`Response`/
    `AssignableStatus` models, `IRemediationTrackerService.GetCatalogueAsync`
    + `AuthToken`, wired into `MainWindow`: the Home tab's "Change Requests"
    gallery is now "Remediation Plan" — a genuinely unified browser combining
    tracker-step rows (`BuildTrackerStepRows`) and change-request rows
    (`BuildChangeRequestRows`, unchanged) in one `GallerySpec`
    (`BuildRemediationPlanRows`), real title/instruction text per item.
    `OpenTrackerStepRecord` opens a real record workspace with a
    write-through status cycle-button sourced from the endpoint's own real
    `assignableStatuses` (never a client-invented list) that calls
    `SetStepStatusAsync` for real and re-opens itself with the server's own
    returned state; a "Remediation Step" contextual tab per UI_RULES.md §2.
    `dotnet build`: 0 errors, 0 warnings.
  - **Real, honest gap surfaced, not invented around:** tracker rows show
    a single disabled row stating the real reason — MyArchitect's
    `TenantService` is fixture data with no real numeric `tenants.id`, so
    the customer-keyed tracker endpoint can never actually be scoped today.
    That gap is already filed as **#3540** (open) — same underlying issue
    `TryResolveLaunchControlScope` already documents for Script Library, so
    no new finding was filed; `BuildTrackerStepRows` follows the exact same
    honest-row pattern `BuildScriptLibraryRows` already established. The
    moment #3540 lands a real customer id source, this browse path (and the
    real PUT-status wiring behind it) activates with no further edit needed.
    Change-request rows are unaffected — they scope by `tenant.TenantGuid`,
    which already works, and keep functioning exactly as before.

  **Remaining checklist item — genuinely still blocked:** "Execute wiring —
  pre-fill Script Library (#3460) entry into Console for catalog-backed
  items." Confirmed again this session: no `*ScriptLibrary*` UI file exists
  anywhere under `desktop/MyArchitect/` — #3460's own UI still hasn't landed.
  `blocked_by` edge to #3460 was already correct and is left in place
  (verified via `gh api .../dependencies/blocked_by`); the two stale edges
  to #3493 and #3459 (both now genuinely closed) were removed this session.
  `blocked` label re-applied, `in-flight` removed. `git status --porcelain`
  clean at session end.
