# #3471 — Feature: Remediation Plan Execution Mode (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T06:10:40Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** d3c5cc09f (real tracker + change-control clients — shipped scope)

## Log
- 2026-09-10T06:06:22Z ⏳ IN FLIGHT — Pulled issue body + both BUILD comments.
  Epic #3454's standing rule ("do not dispatch builds from this epic or its
  Features until breakdown is filed and approved") and #3471's own real
  `blocked_by` edges (#3493 UI Shell, #3459 Console, #3460 Script Library —
  all confirmed still OPEN, none built past service-layer scope) both apply
  here, same wall #3459 and #3460 already hit and bookended. Scoping this
  build to the real, non-UI checklist items that don't need the Ribbon shell,
  the Console UI, or the Script Library UI to exist:
  - Client: GET real remediation tracker (`msp-remediation-tracker.ts`)
  - Client: PUT tracker step status
  - Client: GET real change-control queue (`msp-changes.ts`, filtered
    client-side by tenant like #3459's `ChangeRequestReplayService` already
    does — no per-tenant server route exists)
  - Client: POST human-action + POST attest (`msp-change-executions.ts`)
  The two remaining checklist items — the unified item browser UI and the
  Script-Library-into-Console execute wiring — are genuinely blocked on
  #3493/#3459/#3460's UI landing and are not attempted here.
- 2026-09-10T06:10:40Z 🛑 BLOCKED — Shipped `RemediationTrackerService` +
  `IRemediationTrackerService` and `ChangeControlService` +
  `IChangeControlService` (`desktop/MyArchitect/Services/`), with
  `RemediationTrackerStep`/`RemediationTrackerPricing`/`RemediationTrackerResponse`,
  `ChangeRequest` (with a `NumericId` helper decoding msp-changes.ts's own
  `CR-2026-NNN` ↔ database-id encoding) and `ChangeRequestExecution`
  (`desktop/MyArchitect/Models/`): real HTTP clients for
  `GET`/`PUT /api/msp/customers/:customerId/remediation-tracker[/steps/:stepId]`,
  `GET /api/msp/change-requests` (client-side tenant filter, same approach
  #3459's `ChangeRequestReplayService` already uses against this endpoint),
  and `POST /api/msp/change-control/executions/human-action` +
  `POST .../:id/attest`. Typed `RemediationTrackerException`/
  `ChangeControlException` carry the real status + body instead of a silent
  swallow. `dotnet build` — 0 errors, 0 warnings. Commit `d3c5cc09f`
  (pushed straight to `main`, fast-forward — confirmed via
  `verify-branch-merged.mjs`: OK).
  Remaining checklist items — the unified item browser UI (both sources in
  one view) and the Script-Library-into-Console execute wiring — are
  genuinely blocked: #3471's own `blocked_by` edges (#3493 UI Shell, #3459
  Console, #3460 Script Library) are all still open, none of the three has
  landed UI in code (confirmed: `MainWindow.xaml` is still plain
  `ui:FluentWindow`, no Fluent.Ribbon package, no 6-tab shell — same check
  #3460's bookend already made). `blocked_by` edges left in place (already
  present, still accurate); `blocked` label applied, `in-flight` removed.
  No new finding filed — this is the same, already-filed #3493 dependency
  #3459/#3460 already hit, not a new gap.
  `git status --porcelain` clean at session end; no other files were dirty
  on arrival.
