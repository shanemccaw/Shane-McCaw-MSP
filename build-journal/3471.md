# #3471 — Feature: Remediation Plan Execution Mode (MyArchitect)

- **Status:** ⏳ IN FLIGHT 2026-09-10T19:59:40Z
- **Scope:** buildconsole (desktop/MyArchitect) + api-server (small additive route)
- **Started:** 2026-09-10

## Log
- 2026-09-10T19:59:40Z ⏳ IN FLIGHT — Pulled issue body + all 4 comments. Prior
  session (commit `d3c5cc09f`) shipped the real HTTP clients
  (`RemediationTrackerService`, `ChangeControlService`) and correctly stopped
  there — #3493/#3459/#3460 were all still open at the time. Since then #3493
  (UI Shell Redesign) and #3459 (Embedded PowerShell Console) both closed for
  real (verified via `gh issue view --json state`); #3460 (Script Library) is
  still open with no UI landed (confirmed: no `*ScriptLibrary*` file under
  `desktop/MyArchitect/`). Cleared the two now-stale `blocked_by` edges
  (#3493, #3459); left #3460's edge in place since it still genuinely gates
  the one checklist item this build does NOT attempt (Script-Library-into-
  Console execute wiring). Removed `blocked` label (real work proceeds now),
  kept `in-flight`.

  Scoping this build to the two checklist items #3493 landing unblocks:
  - Unified item browser UI — both tracker steps AND change requests in one
    Home-tab gallery (the issue's own words: "both sources in one view, real
    instruction/action text per item"), replacing the narrower Change-
    Requests-only gallery #3493's own session added as its gallery→
    contextual-tab→workspace proof-of-concept.
  - PUT tracker step status per item type (human-action/attest for change
    requests already wired by that same #3493 session).

  Real gap found along the way: `GET /api/msp/customers/:customerId/
  remediation-tracker` (`msp-remediation-tracker.ts`) returns only
  stepId/status/timestamps — no human-readable title/pillar for a step, so
  there is nothing for a browse row to display as "real instruction/action
  text per item" for tracker steps. The real title/pillar catalogue already
  exists server-side (`remediation-tracker-catalogue.ts`, the same one
  `msp-remediation-tracker-export.ts`'s CSV/PDF already reads) — extracted,
  not authored. Adding one small additive static-data route
  (`GET /api/msp/remediation-tracker/catalogue`) rather than inventing a
  duplicate vocabulary client-side, per the HARD RULE (extend the backend,
  don't stop at an empty state or fake the text).
