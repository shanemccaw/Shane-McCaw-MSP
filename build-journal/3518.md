# #3518 — FalseDoneReconciler silently cancels real, dispatched work with zero notification

- **Status:** ✅ DONE 2026-09-10T16:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** f6e7e710a (code), af8d31175 (IN FLIGHT bookend)

## Log
- 2026-09-10T14:40:00Z ⏳ IN FLIGHT — Add real, persistent, dismissible, click-through visibility to every action `Services/FalseDoneReconciler.cs` takes (Shape A BLOCKED reset, Shape B false-done revert, Shape B false-done cancel). Today the reconciler only writes `ActivityLog` lines — nothing Shane sees unless reading raw logs at the exact moment; overnight it reset ~30 real dispatched items to `canceled` silently. Plan: (1) `ReconcileAsync` returns structured per-item notices alongside its count (logging unchanged — correctness untouched); (2) new persistent `ReconciliationNoticeStore` (`%APPDATA%/BuildConsole/reconciliation-notices.json`) that survives app restart with a per-notice `Seen` flag; (3) a persistent (never auto-dismiss) toast summarizing counts, click-through to a new `ReconciliationDetailWindow` listing each affected issue + action + reason, with per-issue "open on GitHub"; (4) startup replay of any still-unseen notices so the overnight case (Shane not looking / app restarted) is covered. Design question resolved toward PERSISTENT + survives-restart, per the issue's explicit steer and tonight's real experience.
- 2026-09-10T16:20:00Z ✅ DONE — Shipped in commit f6e7e710a (verified ancestor of origin/main via `verify-branch-merged.mjs`). Five files: NEW `Services/ReconciliationNoticeStore.cs` (persists each per-row action to `%APPDATA%/BuildConsole/reconciliation-notices.json`, string-enum via `JsonStringEnumConverter`, deduped by Id, bounded to 500 never dropping an UNSEEN notice, `Seen` flag survives restart); `Services/FalseDoneReconciler.cs` (`ReconcileAsync` now returns `ReconciliationResult` = count + per-row `ReconciliationNotice` list — its logging and fail-closed control flow are byte-for-byte unchanged apart from cosmetic reason wording, verified by diff); NEW `ReconciliationDetailWindow.xaml`/`.cs` (lists every affected issue newest-first with action label, reason, click-to-open-on-GitHub; opening marks all seen); `MainWindow.xaml.cs` (on any pass with actions: persist notices + show a PERSISTENT never-auto-dismiss summary toast; plus a startup replay of any still-unseen notices in `RunDeferredStartupAsync`'s finally so the overnight/app-restart case is covered). **Design question resolved: PERSISTENT + restart-durable** (not one-time), per the issue's explicit steer. Verification: `dotnet build BuildConsole.csproj` clean twice (0 warnings, 0 errors); real throwaway round-trip program confirmed the string-enum JSON serializes/deserializes correctly AND that omitting the converter on read throws `JsonException` (why `Load` carries the converter — otherwise a restart would silently drop the persisted notices). desktop/BuildConsole (WPF) is out of scope for test manifests per CLAUDE.md, so no manifest; verification is the clean build + the round-trip check + a full control-flow diff review confirming the reconciler's correctness is untouched. No new findings/bugs discovered (this build only added visibility). Working tree clean.
