# #1611 — Billing: plan state — subscription endpoints are real but unwired

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 00b08ce60

## Log
- 2026-08-29 ⏳ IN FLIGHT — wiring `GET /api/portal/billing/subscriptions` (plan, status,
  renewal/period dates, seat state) into `portal-v2-billing.tsx`, per docs/billing-contract-pack.md
  §1.6. Tier cards/prices/toggles stay out of scope (DECIDED not-yet-wireable, #1594/#1128).
- 2026-08-29 ✅ DONE — `00b08ce60`, verified merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs` → OK). New "Your plan" section on
  `portal-v2-billing.tsx` reads `useSubscriptionsLive()` (`subscriptionsWire.ts` /
  `subscriptionsLive.ts`, new files) off the real `GET /api/portal/billing/subscriptions`
  endpoint: service name, Stripe's raw subscription-status string (rendered honestly, not
  narrowed — per #1611's own instruction), cancel-at-period-end / renewal date, seat count
  (Stripe line-item quantity), and the platform's own `billingInterval` enum. Strict
  loading/live/error tri-state, **no fixture fallback** — a tenant with zero recurring
  `client_services` rows renders an honest empty state (`pv2-bill-plan-empty`), never
  borrowed design content. `portal-billing.ts`'s subscriptions route now also returns
  `quantity` (Stripe's own line-item quantity, already-fetched data, no schema change) and
  `billingInterval`/`pendingBillingInterval` (existing `client_services` columns, not
  previously surfaced on this endpoint) — real logging via the route's existing
  `logger.child({ channel: "billing" })`. Monitoring-plan tier cards / add-on toggles /
  interval-switch calculator are untouched and stay fixture (DECIDED not-yet-wireable,
  tracked #1594, blocked on #1128) — out of scope per the issue.
  Money: Stripe's `stripe.amount` is genuine integer cents, formatted to dollars only at
  the render boundary (`fmtAmount` in `subscriptionsWire.ts`), consistent with §4 of
  docs/billing-contract-pack.md.
  Typecheck: both `artifacts/msp-portal` and `artifacts/api-server` run clean against my
  changes — the only `tsc --noEmit` errors present are pre-existing, in unrelated files
  (SowScreen.tsx, PortalV2Shell.tsx, ai-billing.ts, admin-build-tracker.ts, several
  `.test.ts` files), none touching `portal-billing.ts` or the new billing files.
  Test manifest: new `test-manifests/portal/portal-v2-billing.json`, registered in
  `_regression-suite.json`, asserting the honest live-but-empty plan state the harness's
  unenrolled `TEST_PORTAL_EMAIL` account actually sees. **Not run end-to-end this
  session** — `shaneapp://runTest` requires the change to be present in the local
  dev-server checkout, and `scripts/dev-server/request-restart.mjs --agent 1611-q800`
  failed merging my commit into that checkout on **pre-existing, unrelated conflicts**
  from other sessions' work already on `origin/main` (`build-journal/1430.md`,
  `build-journal/1415.md`, `portal-v2-security-evidence.tsx`,
  `test-manifests/portal/security-drilldowns.json` — none of which this session touched),
  surfaced while the coordinator was catching the dev-server checkout up through a large
  backlog (`[68/87]`). The merge attempt self-aborted cleanly (no lingering `MERGE_HEAD`,
  no stray changes left in the shared checkout) rather than being force-resolved by this
  session. **Flag for Shane:** the dev-server checkout needs someone to manually resolve
  that backlog of conflicts (unrelated to #1611) before local `shaneapp://runTest` can run
  against current `main` for any build. The code itself is committed, pushed and verified
  merged into `origin/main`; only the live in-app UI test run is unexecuted.
