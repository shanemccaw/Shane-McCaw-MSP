# #4441 — StatusBar.SeparatorStyleKey missing (spring-column Separator falls back to OS-default groove)

- **Status:** ✅ DONE 2026-09-17T00:25:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** b17d2c8a5

## Log
- 2026-09-17T00:10:00Z ⏳ IN FLIGHT — confirmed real root cause in `desktop/BuildConsole/Themes/DarkTheme.xaml`: no `{x:Static StatusBar.SeparatorStyleKey}` style override exists (only a generic `TargetType="{x:Type Separator}"` style and a `{x:Static MenuItem.SeparatorStyleKey}` override). Adding a `StatusBar.SeparatorStyleKey` style following the same `MenuItem.SeparatorStyleKey` pattern.
- 2026-09-17T00:25:00Z ✅ DONE — root-cause hypothesis confirmed. Added `<Style x:Key="{x:Static StatusBar.SeparatorStyleKey}" TargetType="{x:Type Separator}">` to `Themes/DarkTheme.xaml` with a real `ControlTemplate` (`Border Background=Surface0Brush`), same pattern as the existing `MenuItem.SeparatorStyleKey` override — a full template was checked and used because a bare property-setter style (as tried for the generic `Separator` style) does not reach a `StatusBar`-hosted separator; only the `x:Key`-scoped style with its own template does. This applies to ALL bare `<Separator>` children of `MainWindow.xaml`'s `StatusBar` (`Grid.Column="1"/"3"/"6"/"8"`), not just column 3 — the other three now get the same real themed style instead of rendering correctly by column-width coincidence, so no regression. `dotnet build BuildConsole.csproj -c Debug` succeeds clean, 0 warnings/0 errors. Commit `b17d2c8a5`, pushed directly to `main`. Visual confirmation on the running app still needs Shane's own eyes per the standing "never launch the exe" rule — this is real structural/build-level verification, not a substitute for seeing the rendered pixels.
