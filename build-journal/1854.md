# #1854 — Per-cmdlet judgment pass on the 15 Test-* cmdlets excluded from app-only survey

- **Status:** ✅ DONE 2026-09-04T07:56:06Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 5e66ee002 (merged to main)

## Log
- 2026-09-04T07:50:15Z ⏳ IN FLIGHT — reading Microsoft Learn docs for each of the 15 excluded `Test-*` cmdlets (9 Exchange, 6 Teams) to establish real read-safety per cmdlet, then updating `Get-SurveyEligibility` in `services/ps-execution/survey.ps1` and `docs/powershell-capability-survey.md` accordingly.
- 2026-09-04T07:56:06Z ✅ DONE — real Microsoft Learn read of each of the 15. 7 confirmed genuinely unsafe (Test-Message, Test-DlpPolicies, Test-OrganizationRelationship, Test-CsTeamsShiftsConnectionValidate, plus Test-DatabaseEvent/Test-DataEncryptionPolicy/Test-MailboxAssistant which have no public Microsoft documentation at all — fail-closed per #1793, not guessed) permanently excluded in `Get-SurveyEligibility` with the real per-cmdlet reason. 8 confirmed genuine reads (Test-ApplicationAccessPolicy, Test-ClientAccessRule, Test-M365DataAtRestEncryptionPolicy, Test-CsEffectiveTenantDialPlan, Test-CsInboundBlockedNumberPattern, Test-CsTeamsTranslationRule, Test-CsTeamsUnassignedNumberTreatment, Test-CsVoiceNormalizationRule) now fall through to the normal gates 2-4 instead of being blocked at the verb gate — all 8 still end up `not_attempted` today (mandatory params or SupportsShouldProcess), but with the accurate reason, and they self-correct automatically if a cmdlet's real metadata ever changes. `docs/powershell-capability-survey.md`'s Test-* section rewritten with the full per-cmdlet table; the historical DB-generated result rows for run #4 were deliberately left untouched (literal record of what that run actually did under the old blanket rule). No live tenant calls made — verified via PowerShell parser clean-parse of survey.ps1 and dot-sourced `Get-SurveyEligibility` against synthetic records covering every branch. `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/1854-q1571` (5e66ee002) is merged into `origin/main`.
