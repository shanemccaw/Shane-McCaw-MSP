# #4281 — Document tab context menu (Close / Close All but this one / Close All / Shelve / Copy URL / Open in Edge)

- **Status:** ✅ DONE 2026-09-16T01:35:00Z
- **Scope:** platform (shanemccaw/MyArchitect, separate repo)
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/MyArchitect@d6dff98

## Log
- 2026-09-16T01:16:00Z ⏳ IN FLIGHT — Confirmed blockers #4278, #4282 (and the real `blocked_by` edge, which points at #4305) are all closed/complete, so #4281 is unblocked. Full scope per body + both comments: Close / Close All but this one / Close All / — / Shelve (reuse #4282's mechanism) / — / Copy URL / Open in Edge (only for URL-bearing docs). Working in isolated worktree `C:\wt\myarchitect-4281` off `shanemccaw/MyArchitect` main, branch `agent/4281-docmenu`. Reading `MainWindow.Shelf.cs` and the tab strip/context menu code next.
- 2026-09-16T01:35:00Z ✅ DONE — Read the real tab strip (`MainWindow.xaml`'s `TabsItemsControl` DataTemplate, per-tab `Border.ContextMenu`, already carrying the one #4282 "Shelve Tab" entry) and the tab model (`Models/PortalTabItem.cs`: every document tab is WebView2-backed with a `Url` property kept live-synced by `NavigationCompleted`/`SourceChanged`, so there's no non-URL document type in this app to special-case). Extended the context menu to the full ordered set: Close / Close All but this one / Close All / separator / Shelve (existing entry, kept, relabeled) / separator / Copy URL / Open in Edge. New handlers in a new isolated partial-class file `MainWindow.DocumentTabMenu.cs` (Close* reuse the existing `CloseTab(PortalTabItem)` central close path per the issue's own instruction to reuse rather than duplicate; Copy URL uses `Clipboard.SetText(tab.Url)` matching the app's existing `CopyConsentUrlToClipboard` try/catch pattern; Open in Edge launches `msedge.exe` directly via `Process.Start(UseShellExecute)`, matching the app's existing external-launch pattern). `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors. Worked in an isolated worktree (`C:\wt\myarchitect-4281`, branch `agent/4281-docmenu` off `shanemccaw/MyArchitect` origin/main), committed, rebased, pushed directly to `shanemccaw/MyArchitect` main as `d6dff98`, verified `d6dff98` is an ancestor of `origin/main`, then removed the worktree/branch. No fixture/hardcoded data — pure UI-shell wiring against the app's own live tab model, no backend involved. No genuine findings/bugs hit during this build. Not live-UI-verified by actually launching the app (no interactive WPF runner reachable from this session for a WPF exe per this repo's own "NEVER launch exe for verification" rule) — verification here is the clean build plus direct reading of the exact reused mechanisms (`CloseTab`, `ShelveTabFromMenu_Click`, `tab.Url`'s live-sync wiring), which were each already proven working by #4282/prior sessions.
