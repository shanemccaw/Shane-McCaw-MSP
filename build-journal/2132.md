# #2132 — #1887 background chat-tab preload keeps ALL persisted tabs' WebView2 live off-screen (unbounded total, only concurrency capped) — the real #2130 lockup cause

- **Status:** ✅ DONE 2026-09-06T15:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 9c3e9e3de (bookend IN FLIGHT), a1de5fade (fix), plus this DONE bookend

## Log
- 2026-09-06T15:26:00Z ⏳ IN FLIGHT — read #2132 full body + all comments (only the BUILD
  dispatch comment present, nothing supersedes the body). Traced root cause confirmed in
  `MainWindow.ChatReopen.cs`: `ReopenPersistedChatTabsInBackgroundAsync` creates a real live
  WebView2 for EVERY persisted tab and parks it permanently in `ChatReopenPreloadHost`. The
  `SemaphoreSlim(2)` bounds only concurrent LOADS, never the total live count — nothing
  disposes a parked preload on successful load (only `TryTakeReopenPreload` via select/close
  removes one), so N persisted tabs = N live claude.ai browsers running simultaneously.
  Fix plan (issue option 1 — bound the TOTAL): cap the preload to a small hot set of the
  most-recently-used persisted tabs (by `SavedAt`); the remaining tabs still get their inert
  placeholder tab in the strip and load normally on click (that path already works). Add a
  configurable cap setting `ChatReopenPreloadMaxTabs` to `BuildConsoleSettings` with a safe
  small default.
- 2026-09-06T15:40:00Z ✅ DONE — shipped at `a1de5fade` (`MainWindow.ChatReopen.cs`,
  `Services/BuildConsoleSettings.cs`):
  1. `ReopenPersistedChatTabsInBackgroundAsync` now splits persisted tabs by `SavedAt` recency
     into a HOT set (`Take(cap)`) that gets the live off-screen preload, and a COLD set
     (`Skip(cap)`) opened via new `OpenColdPlaceholderTab` → `OpenChatTab(selectTab:false)` with
     NO preload. Cold placeholders create no `CoreWebView2`/`msedgewebview2.exe` (WPF never
     realizes a non-selected TabItem's Content) and load normally on click via OpenChatTab's
     existing else branch (no `_pendingReopenSwap` entry → no swap attempt). Total live browsers
     is now bounded to `cap`, not the persisted-tab count.
  2. New `ChatReopenPreloadCap()` reads `BuildConsoleSettings.ChatReopenPreloadMaxTabs` (default
     3, negative clamped to 0) with a `BUILDCONSOLE_CHAT_REOPEN_PRELOAD_MAX` one-run env override.
  3. `EnableChatReopenPreload` default flipped back to **true** — the #2130 kill switch guarded
     the UNBOUNDED total, which is now structurally bounded, satisfying that field's own
     "re-enable once root cause is fixed" contract. Escape hatch unchanged
     (`BUILDCONSOLE_ENABLE_CHAT_REOPEN_PRELOAD=0` or toggle off ⇒ full no-op, no tabs reopened).
  - Verification: `dotnet build BuildConsole.csproj -c Release` clean (0 warnings, 0 errors).
    `verify-branch-merged.mjs` exit 0 (a1de5fade merged into origin/main). `git status --porcelain`
    clean. **NOT live-cold-start-verified** — no interactive WPF/WebView2 session with real
    persisted `OpenChatTabs` state is reachable from this build environment, so the real
    "restart with 10-20 persisted tabs and confirm only `cap` live browsers spin up" pass still
    needs a human. Filed follow-up **#3010** (parented under Feature #1788, labeled `Shane To-Do`,
    board `AI Batter Up`) for that live-restart confirmation. **Findings filed this session: #3010.**
