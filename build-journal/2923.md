# #2923 — Per-object evidence capture in the mapping engine's 10 count transforms

- **Status:** ✅ DONE 2026-09-05T00:50:35Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** `7e7c23643` (implementation + tests), `527a986c4` (IN FLIGHT bookend)

## Log
- 2026-09-05T00:30:38Z ⏳ IN FLIGHT — Investigation done. Confirmed against the real local DB: 204 `monitor_checks`, **0** endpoints using Graph's raw `$count` suffix, and every count-family mapping rule is a JS-side aggregation over the fetched `items` array (`count` ×116, `countTruthy` ×20, `countFalse` ×9, `countWhere` ×28, `groupByCount` ×6, `countEmptyArray` ×5, `countIfLastSignInOlderThan` ×3, `countEquals` ×13, `countDuplicates` ×2). Issue #2923's scope claim holds. Next: add `extractedProperties._evidence` capture to all 10 transforms in `artifacts/api-server/src/lib/monitor-executor.ts`, with a real cap + honest truncation marker, and guard the two consumers that would otherwise be harmed by a new reserved key (`tenant-signals.ts` flat merge, `document-engine.ts` AI-prompt profile scoping).
- 2026-09-05T00:50:35Z ✅ DONE — Shipped in `7e7c23643`. All ten count-family transforms in `applyMapping` (`artifacts/api-server/src/lib/monitor-executor.ts`) now capture the real matching objects into a reserved `extractedProperties._evidence` bag keyed by targetField, alongside the count they already published. Counting and capturing run off the SAME `EvidenceCollector.add()` call, so a count can never disagree with its own evidence. Capped at 50 items per rule with the real `matchedCount` and an explicit `truncated`/`note` marker; zero-match rules write no key at all, so a clean check's row stays byte-identical to before.
  - **No migration, deliberately.** `extractedProperties` is already persisted verbatim into BOTH `tenant_monitor_profiles.extracted_properties` and `msp_diagnostic_findings.extracted_properties` (`diagnostics-runner.ts` spreads the CheckResult's own object into the finding row), so carrying evidence inside that jsonb keeps ONE copy of the fact. A parallel column would be a second copy that can disagree with the count sitting beside it.
  - **Consumer safety checked, not assumed.** `_evidence` added to `CHECK_DIAGNOSTIC_ONLY_KEYS` in `tenant-signals.ts` (a bare per-check name collides on every flat-profile merge — same class as `_itemCount`, Git #545), and excluded from `namespacedProfileEntries` in `document-engine.ts` (`scopeProfileEntries` returns EVERYTHING when a document type has no `included_profile_key_patterns`, so it would otherwise push hundreds of directory objects per tenant into those AI prompts). `monitor-check-trace.ts` and `dashboard-resolvers.ts` already skip `_`-prefixed keys by convention, which is why the key is named that way — neither needed editing.
  - **Real verification.** api-server `tsc --noEmit` exit 0. 22 new tests in `src/lib/monitor-evidence-capture-2923.test.ts` (registered in `vitest.config.ts`) all pass, as do the 203 existing `monitor-executor.test.ts` tests unchanged — which is what pins that the counts themselves did not move. Also green: `tenant-signals`, `tenant-signals-stabilization`, `pillar-coverage`, `pillar-summary-stats`, `document-profile-scoping-544`, `merged-profile-by-check-544`, `bare-diagnostic-keys-removal-545`, `monitor-check-trace`, `build-tenant-profile`, `dashboard-resolvers`, `diagnostics-finding-title`, `global-admin-count-role-members-551`, `cert-secret-expiration-541`.
  - **Blast radius answered with real numbers** (issue task 3). Replayed every distinct check's real captured `raw_response` from the local `shanemccawmsp` database through `applyMapping`: 37 checks, 20 producing evidence, **49 KB of evidence per tenant per run in total**, 17 KB worst case for one check (`identity:legacy-auth-usage`), 323 bytes median. One check hit the cap — `security:azure-roleDefinitions-compliance`, 145 real items → 50 shown, `truncated: true`.
  - Also confirmed against the live DB: 204 `monitor_checks`, **0** endpoints using Graph's raw `$count` suffix — #2923's scope claim that 100% of checks are the cheap case holds.
  - No findings filed: nothing broken was discovered that this build did not fix. The natural next consumer is `buildFindingEvidence` in `pillar-summary-stats.ts`, which today curates evidence from a hand-maintained four-check `CHECK_EVIDENCE_NAME_FIELDS` map — wiring it to `_evidence` is a separate, customer-facing payload change and was left out of scope.
  - Working tree clean; no scratch files left behind; nothing was already dirty on arrival.
