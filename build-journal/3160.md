# #3160 — Feature: Enhanced alerts — slide-down notification actions (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:25:23Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** b70115e58 (merged to main; agent-branch commit hashes were rewritten several
  times by rebases against a fast-moving main -- b70115e58 is the real one on main)

## Log
- 2026-09-08T02:08:46Z ⏳ IN FLIGHT — Read design contract pack Section 10 and the current
  Shane's Life codebase. Real push-notification SENDING infra doesn't exist at all yet
  (sw.js has a `push` handler and comment "nothing sends one yet"; no VAPID keys, no
  push_subscriptions table, no subscribe endpoint, no web-push send call anywhere). This
  issue's real scope ("act on a notification without opening the app") is unbuildable
  without that base infra existing first, so building it is in scope here (HARD RULE:
  BUILD IT, not a product decision). App deliberately carries one runtime dep (`pg`) —
  webauthn/cbor were hand-rolled rather than adding a library, so implementing raw Web
  Push (RFC 8291/8292: VAPID JWT + aes128gcm payload encryption) with Node's built-in
  `crypto` instead of the `web-push` npm package, matching that precedent. Plan: migration
  for push_subscriptions, src/push/webpush.mjs (VAPID + encryption), subscribe/unsubscribe
  API, wire sending into the existing queueNudge() send path, service worker notification
  actions (mark done / snooze / dismiss) with notificationclick doing a background fetch
  instead of opening the app, and a nudge action API endpoint that does the real
  entity-level effect (dates.updateDate done, pets.markVaccineGiven, etc). iOS Safari's
  real support for action buttons on Home-Screen web push is genuinely unverifiable from
  this session (no physical iPhone) — building the full action-button code path per spec,
  documenting the live-verification gap honestly, keeping the existing tap-to-open
  fallback intact either way, per the issue's own stated fallback.
- 2026-09-08T02:25:23Z ✅ DONE — Shipped: migration 036 (push_subscriptions + nudge_events
  action columns, run directly against local `finances`, ledger row inserted by hand since
  a concurrent build's orphan `035_people_patterns.sql` ledger row blocked `runMigrations()`
  itself -- #3166 already tracks that self-service gap); `src/push/webpush.mjs` (hand-rolled
  RFC 8291/8292, no new dependency, matching this app's stated single-dep discipline);
  `src/core/push-subscriptions.mjs`; `nudges.mjs` real send + snooze/dismiss/done actions;
  API routes; `public/sw.js` real notification actions + background-fetch notificationclick;
  `public/app.js` Settings "Notifications" toggle. Verified: encrypt/decrypt round-trip +
  VAPID JWT signature self-check both pass (`npm run selftest-webpush`); full real path
  (subscribe -> queueNudge -> push attempt -> snooze -> redeliver -> dismiss) exercised
  directly against the real local database, all correct, test rows cleaned up. Found and
  fixed a real pre-existing bug in `queueNudge`'s cap-increment UPDATE (unused `$2` bind
  param broke Postgres type inference, silently swallowed since #3136) -- filed as #3189
  since it's a real production gap outside this issue's own scope (a concurrent build,
  #3161, independently hit and fixed the identical bug the same night; both fixes converged
  identically on rebase). Real, resolved technical uncertainty (contract pack §10, updated):
  Safari/iOS honors neither the classic nor the newer Declarative Web Push `actions`
  mechanism for custom action buttons (confirmed via Apple Developer Forums + WebKit's own
  docs, web-sourced -- no physical iPhone available to this session to device-test). The
  full action-button code path ships and works today on Chrome/Android; it degrades safely
  to the design's own already-stated fallback (tap opens the app to the item) on Safari.
  Branch merged to main directly (`b70115e58`) after several live rebases against a
  fast-moving main tonight (concurrent #3157/#3161/#3184/#3159 builds); resolved every real
  conflict by hand (README feature table, api.mjs imports, nudges.mjs's duplicate bug fix).
  `verify-branch-merged.mjs` confirms OK. Working tree clean. Filed: #3189.
