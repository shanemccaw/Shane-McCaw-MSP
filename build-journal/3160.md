# #3160 — Feature: Enhanced alerts — slide-down notification actions (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-08T02:08:46Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-08T02:08:46Z ⏳ IN FLIGHT — Read design contract pack Section 10 and the current
  Shane's Life codebase. Real push-notification SENDING infra doesn't exist at all yet
  (sw.js has a `push` handler and comment "nothing sends one yet"; no VAPID keys, no
  push_subscriptions table, no subscribe endpoint, no web-push send call anywhere). This
  issue's real scope ("act on a notification without opening the app") is unbuildable
  without that base infra existing first, so building it is in scope here (HARD RULE:
  BUILD IT, not a product decision). App deliberately carries one runtime dep (`pg`) —
  webauthn/cbor were hand-rolled rather than adding a library, so implementing raw Web
  Push (RFC 8291/8292: VAPID JWT + aes128gcm payload encryption) with Node's built-in
  `crypto` instead of the `web-push` npm package, matching that precedent. Plan: migration
  for push_subscriptions, src/push/webpush.mjs (VAPID + encryption), subscribe/unsubscribe
  API, wire sending into the existing queueNudge() send path, service worker notification
  actions (mark done / snooze / dismiss) with notificationclick doing a background fetch
  instead of opening the app, and a nudge action API endpoint that does the real
  entity-level effect (dates.updateDate done, pets.markVaccineGiven, etc). iOS Safari's
  real support for action buttons on Home-Screen web push is genuinely unverifiable from
  this session (no physical iPhone) — building the full action-button code path per spec,
  documenting the live-verification gap honestly, keeping the existing tap-to-open
  fallback intact either way, per the issue's own stated fallback.
