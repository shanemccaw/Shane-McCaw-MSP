# #2105 — Epic #1787 Build Console — Git Board & Chats Panel: Pinned Questions Phase 2 (active detection — ask each chat for outstanding questions, parse into individual pins)

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-31

## Log
- 2026-08-31 ⏳ IN FLIGHT — Starting Phase 2 of the pinned-questions system (#2036), building on
  Phase 1 (#2104, commit `964e3cb65`). Scope: real active detection — query each open chat via the
  same WebView2 DOM-injection bridge Phase 1 reused (#2059/#2063/#2065), asking "are there any
  outstanding questions for Shane?", read back the prose response, parse it into distinct question
  rows and persist each as its own row via `BuildQueuePostgresClient.CreatePinnedQuestionAsync`
  (never bundled). Trigger on a real turn-complete/idle event from the existing receive-side
  scraping code (#2059/#2072), not continuous polling. Auditing Phase 1's landed schema/API and the
  receive-side bridge before writing anything.
