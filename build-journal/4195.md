# #4195 — Line-based .env.local loaders silently truncate multi-line values

- **Status:** ✅ DONE 2026-09-15T01:45:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** fdf365c39 (rebased to 6be2ecb23 on main)

## Log
- 2026-09-15T01:29:53Z ⏳ IN FLIGHT — fixing scripts/dev-all.mjs, artifacts/api-server/run-script.mjs, scripts/config-state/probe-sharepoint-admin-transport.mjs to warn by variable name on a dropped continuation line, and to reject (throw) an unterminated quoted multi-line value instead of silently truncating it. Never print the value itself.
- 2026-09-15T01:45:00Z ✅ DONE — all three loaders now: (1) warn with the last-seen KEY name when a non-comment, non-`KEY=` line appears (continuation line dropped), (2) throw a fatal error naming the KEY (never the value) when a quoted value opens but doesn't close on the same line, instead of silently truncating it. Verified: a synthetic multi-line-quoted `.env.local` correctly throws; a normal single-line-quoted `.env.local` still parses correctly; ran the real `probe-sharepoint-admin-transport.mjs` against the repo's actual `.env.local` and it loaded env vars and completed a live SharePoint probe successfully (no exception, no value ever printed). `verify-branch-merged.mjs` confirms `agent/4195-q2843` is merged into `origin/main` at 6be2ecb23. Working tree clean.
