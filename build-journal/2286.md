# #2286 — Clicking a busy slot focuses that build in the queue

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** f099320c1 (Build Matrix: 8-slot drawer + click-to-focus)

## Log
- 2026-09-02T19:50:00Z ⏳ IN FLIGHT — Sub-issue of #2281 (Feature: Build Matrix), item 5
  of 7. Confirmed via grep that no Build Matrix code exists anywhere in ShaneBuilder or
  BuildConsole — this is prototype-only in `ShaneBuilder-FullFeature.md`. All 7 sibling
  sub-issues (#2282-#2288) are open with zero commits against any of them. #2281's own
  stated dependency (#2263, real bt_build_queue data) already landed via Git #2413
  (`e4557da3c`), so the Matrix feature is now unblocked. Since a "click a busy slot to
  focus it" handler is meaningless without the drawer/slot-card scaffold it clicks on,
  building the minimal real scaffold (Matrix chip toggle, 8 slot cards derived from real
  running `_queueItems`, busy/idle dimming, slot count summary) alongside the click-focus
  behavior itself. Slots are a real client-side admission model over `Status == "Running"`
  rows (same pattern as BuildConsole's own `BuildWatchWindow` 8-slot admission), not a new
  DB column — no schema change needed. No fixture/hardcoded slot data.
- 2026-09-02T20:05:00Z ✅ DONE — Shipped `f099320c1` on `main`: Matrix chip (toolbar) toggles a
  real drawer, 8 slot cards derived from `_queueItems` where `Status == "Running"` (stable
  admission via `_matrixSlotAssignments`, freed when a build stops running), busy/idle dimming,
  a real looping opacity pulse on busy slots, slot count summary on both the chip and the drawer
  header, and the click-to-focus behavior this issue asked for: clicking a busy slot expands its
  Build Set, calls the existing `OpenBuildDetail`, then scrolls its card into view and flashes it.
  `dotnet build ShaneBuilder.csproj` succeeded (0 warnings, 0 errors) both before and after the
  final rebase onto `origin/main`. `verify-branch-merged.mjs` confirms `f099320c` is an ancestor
  of `origin/main`. `git status --porcelain` clean. Also implemented #2282/#2283/#2284/#2285 in
  the same commit since a click-to-focus handler is meaningless without the drawer/slot-card
  scaffold it clicks on and none of the 7 sibling sub-issues under #2281 had any code yet
  (concurrent sessions were mid-flight on #2285/#2287 but had not pushed any Matrix code — none
  collided). Left #2287 (Tab-to-own-tab) and #2288 (documented cancel/requeue/reassign gap) alone
  for their own sessions. Did not close any sibling issue — commented on #2282-#2285 noting they
  shipped alongside this commit.
