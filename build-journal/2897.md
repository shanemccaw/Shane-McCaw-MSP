# Build Journal — #2897

- **Status:** ✅ DONE 2026-09-04T19:45:00Z
- **Scope:** platform
- **Feature parent:** #1682 (Feature: Risk Register / RBD, MSP Console)

## Log

- 2026-09-04T19:10:00Z — Session started. Task: regenerate
  `docs/risk-register-msp-console-contract-pack.md` per #2897 — full line-by-line
  audit against the real, current backend (supersedes #2580, already fired once).
  Confirmed real drift already: `msp_risk_decisions.registerRef` is now actually
  written on `POST /api/msp/rbd` (`msp-rbd.ts:236`, via `assignRegisterRef`,
  landed on #2529) — the current doc's §1.1 note claims "no MSP-side route in
  this pack writes any of them" for `registerRef`, which is now stale.
- 2026-09-04T19:45:00Z — ✅ DONE. Read all 3 route files (`msp-rbd.ts` 374
  lines, `msp-rbd-instances.ts` 264 lines, `msp-rbd-versions.ts` 476 lines) in
  full, plus `risk-register-ref.ts`, `admin-rbd.ts` (323 lines), the AdminV2
  `riskDecisionsStore.ts` consumer, and the MCP `get-risk-register.ts` tool
  (now at `artifacts/mcp-server/src/tools/`, moved since the #2580 pass).
  Verified live against local PostgreSQL (`\d msp_risk_decisions` / `\d
  risk_instances` / `\d msp_rbd_versions`) — schema matches the doc exactly.
  Regenerated `docs/risk-register-msp-console-contract-pack.md` whole:
  - `registerRef` moved from documented-absent to documented-written (§1.1,
    §1.3, §4) — `assignRegisterRef` (#2529) now runs on both `msp-rbd.ts`'s
    and `admin-rbd.ts`'s create paths; `admin-rbd.ts`'s own `rbdToWire()`
    still never returns it to the AdminV2 client (noted, not a bug — falls
    inside its own deliberately-excluded "register-extension columns" group).
  - The prior pass's live bug (§6, `admin-rbd.ts` writing `status =
    "expired"`) is confirmed fixed — closed as #2697. Restated as resolved
    history, not re-flagged as live.
  - Every file:line citation in the doc was re-read against current `main`
    and corrected — `msp-rbd.ts` grew 4 lines for the `registerRef` writer,
    shifting every downstream citation in that file; `lib/db/src/schema/msp.ts`
    line numbers shifted ~+141 across the board; `routes/index.ts` mount
    lines moved; `get-risk-register.ts` moved package (`api-server` →
    `artifacts/mcp-server`).
  - No new bug found on this pass. No orphaned-endpoint sub-issues filed —
    every unconsumed endpoint is already staged for #2582 (doc §0.1). No
    product code, schema, or UI was changed.
  - Commit: `02f3c6149` ("Regenerate risk-register-msp-console contract pack
    (#2897)"), merged to `origin/main` at `2e5081d8`. `verify-branch-merged.mjs`
    confirms ancestor-of-main. `git status --porcelain` clean.
