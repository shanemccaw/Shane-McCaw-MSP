# #4395 — Script: create mccawsoft2 customer user accounts + write credentials into BuildConsole's settings.json

- **Status:** ✅ DONE 2026-09-17T19:00:05Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** `5155093b6` (create-portal-test-account.ts)

## Log
- 2026-09-16T18:42:55Z ⏳ IN FLIGHT — dispatched per issue body, blocked-by #4318 (real mccawsoft2 customer must exist first). Confirmed #4318 is CLOSED (stateReason: COMPLETED, closed_at 2026-09-16T18:18:01Z) on Shane's own comment that the live M365 admin-consent + Stripe signup walkthrough completed and a real tenant/account exists.
- 2026-09-16T18:42:55Z 🛑 BLOCKED — Queried the local dev DB directly (`tenants`, `users` tables) before doing anything, per this issue's own standing rule ("Confirm real mccawsoft2 tenant exists (post-#4318) before doing anything — this cannot run against a non-existent tenant"). Result: **no mccawsoft2 tenant row and no premier/testbed account exist right now** — `tenants` has only 4 vitest fixture rows (msp_id 2362/2363/2497, all `is_testbed=false`), `users` has only `shane@shanemccaw.com`, vitest fixtures, and synthetic RBAC-ladder accounts (msp_id=1, tenant_id NULL). Direct lookup for `tenants.id = 1886` (the real tenant #4318/#4396 both cited) returns 0 rows.

  This is the same recurring pattern already documented on #4318 and #4396: a real, genuinely-consented tenant row is created via Shane's live walkthrough, then something running concurrently against the shared local dev DB deletes it again — and it recurred again within ~25 minutes of Shane's own 2026-09-16T18:18:00Z confirmation comment on #4318. Since the actual precondition this issue is blocked on is false again despite #4318 being closed, I am not proceeding (would otherwise have to fabricate/assume tenant state, which violates the "never invent data" rule and this issue's own standing instructions).

  Filed #4423 documenting the recurring instability (parented as a sibling sub-issue of #4270, `bug` label, board status "AI Batter Up") and wired `blocked_by` from #4395 -> #4423, removing the stale `blocked_by` -> #4318 edge (closed, and its closure no longer reflects real DB state). Applied `blocked` label to #4395, removed `in-flight`. Posted an explanatory comment on #4395. No code changed; working tree is clean (only this bookend file added).

- 2026-09-17T03:41:09Z ⏳ IN FLIGHT — Resumed. Re-checked the `blocked_by` edge per the resume protocol: #4423 is labeled `complete` (fix landed in `4d4352d39` — `reset-dev-database.mjs` now refuses its whole run when a tenant is `reset_protected`). Direct DB query confirms a real, live, `reset_protected=true` tenant now exists: `tenants.id=2080` (msp_id=1, customer_name='McCawSoft', tenant_id=c4c814d4-3afe-441e-9145-62461d0a4fd3, status=active), with real account `users.id=4366` (shanemccaw+premier@outlook.com, role=client, msp_role=Customer, msp_id=1, tenant_id=2080). Removed the stale `blocked_by`→#4423 edge, flipped labels `blocked`→`in-flight`, posted a resume comment. Proceeding with the real scope now: reading `BuildConsoleSettings.cs` for the existing settings.json schema/persistence pattern, and #3459/#3460 for the real Graph/PowerShell account-creation execution path.

- 2026-09-17T03:57:00Z ⏳ IN FLIGHT (stopped) — Read `BuildConsoleSettings.cs` in full: the "known test accounts" concept the issue asks for already exists as `UserAccountEntry`/`List<UserAccountEntry> UserAccounts` (Git #3922, Label/TargetApp/LoginUrl/Username/Password/AccountTier/Notes/IsAnonymous), persisted plaintext in `%AppData%\BuildConsole\settings.json` — same plaintext standard as `GitHubPat`/`ZohoApiToken` elsewhere in the same file (no DPAPI/encryption exists anywhere in this app; plaintext-at-rest under the user's own AppData IS the existing standard to match). `TargetApp` is currently a closed set: Marketing/Portal/MSP Console (`SettingsTabView.xaml:1030-1032`) — no M365/Graph/tenant option exists yet.

  Delegated research (Explore subagent) on the real PS/Graph execution path #3459/#3460 use, confirmed: both funnel through `callPsExecution()` (`artifacts/api-server/src/lib/ps-execution-client.ts:154`) against a fixed server-side cmdlet-key catalog (Exchange/Compliance/Teams-oriented, no Graph-user-create cmdlet in it). `POST /api/simulator/ps-execution/cmdlet` (#3459's backend, `admin-engines.ts:1931`) is hard-gated to `tenant.isTestbed === true` (the #965 gate) — and the real mccawsoft2 tenant row (id 2080) has `is_testbed = false`, so that route would 403 against it as-is. The one real, working precedent for creating an M365 user via Graph is `scripts/azure/testbed-test-user-2840.mjs:202-225` (direct Graph `POST /users`, DEV write app registration, testbed-tenant-id guard, `zz-test-*` reserved-UPN gate) — a different mechanism from ps-execution entirely.

  **Stopped rather than proceeding**: "user accounts... via Microsoft Graph/PowerShell against the real mccawsoft2 tenant" has two real, materially-different referents in this codebase — (1) actual M365/Entra ID objects created in Shane's real production tenant via Graph (the #2840-style meaning, real production-directory mutation), or (2) platform/portal login accounts for the mccawsoft2 customer via `provisionProspectAccount`/`direct-tenant-provisioning.ts` (the same mechanism the real signup flow uses, not actually "via PowerShell" in any real sense, but the lighter/reversible action and the one `UserAccountEntry`'s `TargetApp` already models). Per this issue's own standing rule ("If the number/roles/naming of accounts to create isn't clear from context, stop and report rather than inventing an arbitrary set"), posted a full comment on #4395 laying out both readings, the isTestbed finding, and three concrete questions (which mechanism; if M365, how many/naming/license-role; if portal, which TargetApp/customer role). Removed `in-flight` label (no active work pending an answer), set the project board Status field to the real "Ask Shane" lane (option `404998bb`) rather than leaving it on whatever it inherited. No code changed; working tree confirmed clean (`git status --porcelain` empty) aside from this bookend.

- 2026-09-17T19:00:05Z ✅ DONE — Shane answered (2026-09-17T13:34:59Z comment): Option 2, real Portal login account(s) for mccawsoft2, for the `shaneapp://runTest` Playwright harness's `loginAs`, and to investigate what that harness actually reads before writing anywhere.

  **Investigated the real harness first, per instruction.** Confirmed (grep + read, `desktop/BuildConsole/Services/TestManifest.cs:47-53`, `MainWindow.xaml.cs:10591-10606`, `UiTestExecutor.cs:332-413,1335-1391`): a manifest's top-level `loginAs` label is resolved by `RunManifestAsync` against `BuildConsoleSettings.Load().UserAccounts`, matching `Label` case-insensitively (falling back to `Username` if `Label` is blank) — this genuinely is the same `UserAccountEntry` schema investigated pre-clarification, confirmed as the real read path, not assumed.

  **Created the real account via the real provisioning mechanism**, not a raw INSERT — `artifacts/api-server/src/scripts/create-portal-test-account.ts` (committed `5155093b6`), run via `node --import tsx src/scripts/create-portal-test-account.ts` from `artifacts/api-server` against the local `DATABASE_URL`:
  - Resolved the real mccawsoft2 tenant by its Entra GUID (`tenants.id=2080`, refuses to run if it doesn't exist — no fabrication).
  - Inserted `users` row for `shanemccaw+portal-test@outlook.com` directly at the `Customer` rung (tenantId=2080, mspId=1) with a real CSPRNG password, bcrypt-hashed cost 12 — NOT via a `*Pending`-rung pre-consent door, because that path's promote-on-consent swap lands on a product's own `*Consented` rung (e.g. `RetainerConsented`), not `Customer`; confirmed this live on a first attempt (mspRole came back `RetainerConsented`), deleted that row, and fixed the script to insert directly at `Customer` with tenant/msp scope stamped inline instead (matches "real role: likely Customer, matching a normal paying customer" from Shane's clarification).
  - Called the real `provisionProspectAccount({ email, tenantId: <mccawsoft2 GUID>, role: LEGACY_ROLE.customer, ... })` (`direct-tenant-provisioning.ts:477`) — the same function the live signup funnel calls — which resolved the tenant and ran the real customer-bridge linkage. Result: `userId=4722, customerId=2080, mspRole=Customer`.
  - **Live-verified the real login**, not just DB state: `curl -X POST http://localhost:8080/api/auth/login` with the generated credentials → real `200`, real JWT (`mspRole: Customer, customerId: 2080, mspSlug: shane-mccaw-consulting`). This is the exact endpoint `UiTestExecutor.PerformLoginAsync` submits to.

  **Wired the real credentials into `BuildConsoleSettings.UserAccounts`** (`%AppData%\BuildConsole\settings.json`, outside this repo, not tracked in git): new entry `Label: "Customer — mccawsoft2 (real, provisionProspectAccount)"`, `TargetApp: "Portal"`, `Username`/`Password` the real generated pair, `AccountTier: "Customer"`, real `Notes` naming this issue and the script. Confirmed the file round-trips valid JSON afterward and every pre-existing field (GitHubPat, OpenChatTabs, WebTools, the 12 existing UserAccounts profiles) is intact — backed up before editing, backup removed after the write verified clean.

  **Honest gap**: could not invoke `shaneapp://runTest` itself from this shell-only session (no such tool reachable here) to prove BuildConsole's own `loginAs` resolution end-to-end against a manifest naming this new label. Verified as far as this session's tools allow: the harness's real resolution code path (read directly, not assumed) plus a real, direct call to the exact endpoint it submits to, both confirming the credentials are correct and the mechanism is wired the way the code expects. A future manifest using `"loginAs": "Customer — mccawsoft2 (real, provisionProspectAccount)"` should work; that's the one remaining piece an in-BuildConsole session can confirm that this one couldn't.

  **Side effect noted, not a bug**: `provisionProspectAccount` triggers a real `convertLeadForClient` → `zoho_upsert_lead` job enqueue (the same thing every live signup does). Checked `msp_job_queue`: the job status is `pending` with `error_message: "Zoho is not connected"` — local dev has no live Zoho OAuth connection, so this fails closed and never reaches real Zoho. Not filed as a finding: expected behavior of an existing, working feature in an environment where the external integration isn't connected, not something this build broke or a real gap.

  Working tree confirmed clean (`git status --porcelain` empty) before this write. Branch verified merged (`verify-branch-merged.mjs` — `5155093b6` is an ancestor of `origin/main`).

## What Shane needs to do
Nothing required. If/when a portal test manifest wants this specific real-provisioned account (rather than the RBAC-ladder synthetic ones), reference `"loginAs": "Customer — mccawsoft2 (real, provisionProspectAccount)"` — it's in `BuildConsoleSettings.UserAccounts` now. Re-running `create-portal-test-account.ts` is idempotent (finds the existing account by email; pass `--reset-password` to rotate its password if it's ever lost/changed).
