# #1540 — Remediation: Pointed verification scan — the only thing that closes an item

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** c9346a259 (IN FLIGHT bookend), 631dbd7bc (work, real post-push hash)

## Log
- 2026-08-30 ⏳ IN FLIGHT — On-demand targeted check execution against a single
  remediation-tracker step's mapped check(s), writing the real result to
  `remediation_tracker_steps.verificationState`/`.verifiedAt`/`.verifiedByRunId`
  (both columns already exist per #732 — no schema change expected). Wired as a
  visible Workflow Engine node per the standing rule, reusing the existing
  `executeMonitorCheck` Graph re-scan path (deterministic, severity_rules-driven)
  rather than executing `remediation_knowledge_base.validationCommand`'s free-text
  PowerShell directly — that field stays customer-facing guidance, surfaced
  read-only, not executed (arbitrary free-text script execution against a live
  tenant is out of step with the platform's allowlisted ps-execution model).
  Ends at the wire contract — no `artifacts/portal` page.
- 2026-08-30 ✅ DONE — Shipped in 631dbd7bc. New Workflow Engine node
  `remediation_pointed_verify` (registry entry in `node-type-registry.ts`, case
  in `workflow-executor.ts`, added to `WfNode` type union in
  `lib/db/src/schema/index.ts` — composite decls force-rebuilt), seeded as
  `__system__: Remediation Pointed Verification` (event trigger
  `remediation.verify_requested`). It re-runs a step's mapped `monitor_checks`
  key(s) via the same `executeMonitorCheck()` path `execute_monitor_check`
  already uses, classifies severity with diagnostics-runner.ts's
  `classifyCheckSeverity` (exported for this), and writes the verdict through a
  new `applyPointedVerification()` in `remediation-tracker-verification.ts` —
  the same eligibility rule and write shape #732's passive full-rescan
  reconciliation already uses (shared `CLAIMED_STATUSES`), so the two paths can
  never disagree about the same step. New routes on `portal-remediation-tracker.ts`:
  `POST /steps/:stepId/verify` (fires the event, 202, client re-polls
  `GET /portal/remediation-tracker` for the verdict) and
  `GET /steps/:stepId/verification-guide` (read-only, surfaces
  `remediation_knowledge_base.validationStep/.validationCommand/.expectedOutcome`
  for the step's mapped check(s), published rows only — the "already
  anticipated, never wired" fields from the issue body, now wired to a live read
  path; never executed). No migration — #732 and #493 already added every
  column this needed.
  **Verification (real, observed):** `lib/db` tsc clean; api-server tsc clean
  for every touched file (56 pre-existing errors remain, all in files this
  session never touched — ai-billing/ai-usage/sow-claim-binding/admin-build-tracker,
  tracked by #1781). `remediation-tracker-verification.test.ts` +10 tests,
  `portal-remediation-tracker.test.ts` +11 tests, `ai-usage-metering.test.ts`
  (node-type-registry completeness) — **85 passed**. 4 pre-existing failures in
  those same two files are unrelated drift-guard tests reading the retired
  `artifacts/msp-portal` path — already filed as **#1936** by a concurrent
  session, not from this one. Live DB: confirmed `remediation_tracker_steps`
  already carries `verification_state`/`verified_at`/`verified_by_run_id`;
  `remediation_knowledge_base` still has 0 published rows (1 draft) — already
  tracked by #1924. Branch verified ancestor of origin/main
  (`verify-branch-merged.mjs`); working tree clean.
