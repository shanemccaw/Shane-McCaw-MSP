# #1441 — Adoption (top-level pillar page): strict pass — eliminate all remaining fake data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** a94891f95 (real work), de15460f4 (IN FLIGHT bookend)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Full strict pass on `artifacts/msp-portal/src/pages/portal-v2-adoption.tsx`
  beyond #1412's narrower hero-copy fix. Investigation complete: catalogued every
  `ADP_*` fixture domain (hero stats, trend chart, workload rows, department
  heatmap, plays, parked plays, wins, enablers, provenance). Confirmed via
  `useLivePillarHero`/`war-room-pillar-stats.ts` that the hero trend chart's
  `live.history` IS genuinely populated for the adoption pillar (contradicts the
  page's own stale #1409 comment) — real fix available there, matching the
  Governance/Security/Compliance `DriftTrend`/honest-empty pattern. Confirmed
  `useAdpWorkloadsLive` already resolves Copilot active/licensed counts real —
  usable for the "Copilot weekly active" hero stat tile, currently unwired.
  Confirmed via #1254/#1266 (department heat-map join, `identity:department-directory`
  migration) that `ADP_MATRIX` is a genuine, well-documented backend gap (blocked
  on a tenant-wide `displayConcealedNames` decision only Shane can make) — reusing
  that citation for the `NO-BACKEND-TO-WIRE` tag rather than re-investigating.
  Proceeding to implementation: wire what's real, honest-empty + tag everything
  else per the issue's 3-state rule.
- 2026-08-28 ✅ DONE — Real wiring: hero trend chart now draws real
  `live.history` (war-room-pillar-stats.ts computes it generically per
  pillar, adoption included), honest-empty otherwise; "Copilot weekly
  active" hero stat wired off the real counts `useAdpWorkloadsLive` already
  resolves. `NO-BACKEND-TO-WIRE:` tagged + honest "No live data available"
  for every genuine gap: the "Workloads in real use"/"Open plays" hero
  stats, the 4 always-fixture workload rows (new `hasLiveFeed` field
  distinguishes them from the 6 wireable-but-possibly-unscanned rows, which
  correctly show "No scan data available" instead), the parked-count strip
  + parked-plays list, the department heat-map + its coverage badge
  (cited #1254/#1266), the plays panel, the wins panel, the enablers panel.
  Removed the FixPanel/AcceptRiskPanel/ShaneBot-form wiring nothing on the
  page could still trigger once the fixture content it pointed at was gone.
  Updated test-manifests/portal/portal-v2-pillars.json (A-1b/A-4b/A-5/
  A-5b/A-5d/A-5f/A-5h/A-6/A-6b/A-8/A-24/A-25/A-26; old A-9..A-23 CR-gate
  walkthrough removed, it exercised the now-gone plays fixture). Resolved a
  concurrent-session merge conflict in that shared manifest against #1442's
  Health strict pass (kept both session notes).
  **Honest verification gap:** neither `tsc --noEmit` nor `shaneapp://runTest`
  ran this session. tsc: this worktree's pnpm/node_modules setup couldn't be
  invoked safely — the plain `pnpm typecheck` call aborted needing an
  interactive purge confirmation, and retrying with `CI=true` began what
  looked like a rebuild of the **shared** main-checkout `.pnpm` store (other
  concurrent sessions depend on that same junctioned node_modules), so it was
  killed immediately rather than risk it. runTest: it exercises the
  *deployed* dev-server build, not this uncommitted worktree, so running it
  before push would have validated stale code. Verification instead rests
  on: a full manual line-by-line re-read of the diff for JSX/brace balance
  and import correctness (clean), `node -e "JSON.parse(...)"` confirming the
  manifest is valid JSON, and confirming `adpDashboardData.test.ts`'s
  existing assertions are unaffected (it pins the underlying fixture
  constants, which are unchanged — only the page's own rendering of them
  changed; the new `hasLiveFeed` field is additive). Whoever next runs this
  file live should treat every `sessionTag: "1441"` step as freshly-added
  and unconfirmed against the real DOM. Branch verified merged into main via
  `verify-branch-merged.mjs` before writing this DONE.
