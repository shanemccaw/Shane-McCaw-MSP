# #1441 — Adoption (top-level pillar page): strict pass — eliminate all remaining fake data

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Full strict pass on `artifacts/msp-portal/src/pages/portal-v2-adoption.tsx`
  beyond #1412's narrower hero-copy fix. Investigation complete: catalogued every
  `ADP_*` fixture domain (hero stats, trend chart, workload rows, department
  heatmap, plays, parked plays, wins, enablers, provenance). Confirmed via
  `useLivePillarHero`/`war-room-pillar-stats.ts` that the hero trend chart's
  `live.history` IS genuinely populated for the adoption pillar (contradicts the
  page's own stale #1409 comment) — real fix available there, matching the
  Governance/Security/Compliance `DriftTrend`/honest-empty pattern. Confirmed
  `useAdpWorkloadsLive` already resolves Copilot active/licensed counts real —
  usable for the "Copilot weekly active" hero stat tile, currently unwired.
  Confirmed via #1254/#1266 (department heat-map join, `identity:department-directory`
  migration) that `ADP_MATRIX` is a genuine, well-documented backend gap (blocked
  on a tenant-wide `displayConcealedNames` decision only Shane can make) — reusing
  that citation for the `NO-BACKEND-TO-WIRE` tag rather than re-investigating.
  Proceeding to implementation: wire what's real, honest-empty + tag everything
  else per the issue's 3-state rule.
