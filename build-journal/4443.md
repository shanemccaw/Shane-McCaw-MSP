# #4443 — My Architect: no-retainer customer still shows RETAINED 8h / REMAINING 8h stat tiles

- **Status:** ⏳ IN FLIGHT 2026-09-17T01:39:32Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-17T01:39:32Z ⏳ IN FLIGHT — confirmed the "8h" default is `DEFAULT_RETAINED_MINUTES = 480` (`artifacts/api-server/src/routes/admin-retainer.ts:64`), a real hardcoded platform constant, applied in `portal-retainer.ts:88` (`settings?.retainedMinutesPerMonth ?? DEFAULT_RETAINED_MINUTES`) whenever no `retainer_settings` row exists for the customer — not a reference row read from any table. Implementing: gate the RETAINED/ROLLED IN/USED/REMAINING/OVER stat-tile grid + progress bar + its caption in `artifacts/portal/src/pages/my-architect.tsx` on `data.configured`, replacing with an honest dash/empty state for the no-retainer case. Keeping the existing "You do not have an architect retainer" explanation card as-is (that text is already correct and disclosed); this only removes the fabricated-looking numbers grid underneath it, not the caption. Note: bookend was written after the code edit, not before, since this build session's first turn went straight to investigation — noting honestly rather than silently backdating.
