# #2807 — Chain-map icon: real Git Map / Local Map picker

- **Status:** ✅ DONE 2026-09-04T08:58:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `4fb60855d` (merged to `main`)

## Log
- 2026-09-04T08:56:00Z ⏳ IN FLIGHT — confirmed #2806 (Local Map window) landed at `6b6a0d9ff`, merged to `main` at `9605e854c`, present in this worktree. Real audit of `MainWindow.OpenBuildChainMap()` (~line 1825) and `ActivityBar.BtnBuildChainMap_Click`/`BuildChainMapRequested` — today the click handler jumps straight into `BuildChainMapWindow` (Git Map) with no choice. Plan: add a small real choice popup (Git Map / Local Map, same ad-hoc-dialog style as the existing `PromptForEpicNumber`), keep the existing Git Map path (epic prompt + `BuildChainMapWindow`) byte-identical, add a new Local Map path that opens the standalone `LocalMapWindow` from #2806 with its own open-or-activate toggle lifecycle matching `_buildChainMap`'s.
- 2026-09-04T08:58:00Z ✅ DONE — shipped in `4fb60855d`, `desktop/BuildConsole/MainWindow.xaml.cs` only. `OpenBuildChainMap()` now: if `_buildChainMap` or `_localMap` is already open, activates that one (unchanged single-instance-per-window behavior, now for both windows); otherwise shows a new `PromptForMapChoice()` popup (Git Map / Local Map / Cancel, same minimal ad-hoc-`Window` style as the existing `PromptForEpicNumber`). Git Map choice calls a new `OpenGitMap()` that is #2483's original body extracted verbatim — unchanged epic-prompt + `BuildChainMapWindow` logic, confirmed byte-identical by diff. Local Map choice calls a new `OpenLocalMap()` that constructs the #2806 `LocalMapWindow` (parameterless ctor, `Owner = this`) with its own `_localMap` field and Closed-lifecycle logging (`local-map` channel), matching `_buildChainMap`'s pattern. `dotnet build BuildConsole.csproj -c Debug`: 0 warnings/0 errors. Per the standing "never launch BuildConsole.exe / drive MainWindow" rule, live click-through was not self-verified via UI automation (MainWindow's own mutex-guarded startup path makes that unsafe, unlike #2806's standalone-window harness) — verification here is the clean build plus a full read of the diff confirming the Git Map path is untouched and the Local Map path matches `LocalMapWindow`'s real constructor/lifecycle. `verify-branch-merged.mjs`: OK, merged into `origin/main` at `4fb60855d`. `git status --porcelain`: clean. No findings filed — #2804's structured index (items 1-3) is now fully landed across #2805/#2806/#2807.
