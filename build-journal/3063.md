# #3063 — Retention purge coverage gap: active_directory_ou_assignment_requests + status_reports tenant-scoped columns undeclared

- **Status:** ✅ DONE 2026-09-07T15:47:43Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 40efbb2f6 (rebased onto main as 99e5ba0d4)

## Log
- 2026-09-07T15:47:43Z ⏳ IN FLIGHT — investigating the three unaccounted tenant-scoped
  columns `tenant-scope-coverage.live-db.test.ts` reported: `active_directory_ou_assignment_requests.customer_id`,
  `active_directory_ou_assignment_requests.tenant_id`, `status_reports.customer_id`.
- 2026-09-07T15:47:43Z ✅ DONE — both tables hold ordinary customer product data (the
  customer's own AD OU move requests; admin-authored client-facing status reports), not
  audit trail / MSP infra / a settled-elsewhere identity decision, so this resolved as
  real purger coverage rather than an exemption:
  - `status_reports.customer_id` → `documentsPurger` (`customerId` keySpace, matching
    Git #1923's decision that a report is scoped to the customer organisation, not the
    named `clientUserId` addressee).
  - `active_directory_ou_assignment_requests.customer_id` / `.tenant_id` →
    `directoryPurger`, alongside the existing `active_directory_ou_assignments`
    declaration it's the request-side counterpart of.
  - File: `artifacts/api-server/src/lib/retention/purgers/modules.ts`.
  - Verified live against local Postgres (`DATABASE_URL`): `tenant-scope-coverage.live-db.test.ts`
    now passes 7/7 (previously failed on exactly these 3 columns); all of
    `src/lib/retention/` (6 files, 91 tests) passes; `tsc --noEmit` clean.
  - No migration needed — additive purger-registry declaration only, no schema change.
  - No sub-issues filed — this was a straightforward coverage gap, not a further finding.
