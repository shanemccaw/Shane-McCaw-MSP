# #4166 — SettingsTabView.xaml typo/color audit (227 raw FontSize literals)

- **Status:** ✅ DONE 2026-09-15T02:33:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 5a16785f6 (IN FLIGHT bookend), fc9ffb85b (real fix)

## Log
- 2026-09-15T02:22:00Z ⏳ IN FLIGHT — mapping all 227 raw `FontSize` literals in `desktop/BuildConsole/Controls/SettingsTabView.xaml` to real `Themes/Typography.xaml` resource keys by role (file has 0 raw FontFamily/hex-color violations per the issue's own audit, confirmed by re-running the grep counts — this file only needed FontSize mapping).
- 2026-09-15T02:33:00Z ✅ DONE — mapped all 227 raw `FontSize` literals to `HeaderFontSize`/`SubHeaderFontSize`/`BodyFontSize`/`CaptionFontSize` by role (top app-bar header, per-page title rows, nav labels/subsection headers/real data-entry fields, and caption-tier chrome respectively — see commit fc9ffb85b for the full breakdown). Bumped `Height="28"`→`"30"` and `"26"`→`"28"` on the 43 TextBox/PasswordBox/ComboBox controls that moved to BodyFontSize, to avoid clipping. Verified with `dotnet build BuildConsole.csproj` (0 errors, 0 warnings) both before and after the Height bump, and confirmed all 5 Typography.xaml keys resolve through App.xaml's application-level merged ResourceDictionary. Per this project's BuildConsole-verification convention (no UI automation against a possibly-live BuildConsole instance), this was NOT verified via live UI click-through — Shane should open Settings and check each tab visually. Branch verified merged into origin/main via `verify-branch-merged.mjs` before writing DONE. Working tree clean.
