# #2107 — Build Queue: 🔒 BLOCKED badge stays stale until manual Git Board refresh

- **Status:** ✅ DONE 2026-09-06T15:19:40Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 50ee725f0fce43c6144f6b6c9752f8f5c0edd1d9 (pushed to main as 7a9914403839ad27eb8a8df50101648f14cfe822 after rebase)

## Log
- 2026-09-06T15:14:54Z ⏳ IN FLIGHT — confirmed root cause per issue body:
  `BuildQueuePanel._openIssues` is only populated via `ApplyOpenIssueSet()`, fed
  exclusively by `LeftSidebar.GitBoardOpenIssuesRefreshed`, which only fires on
  Issues-tab-open or manual Git Board refresh (2026-08-14 manual-only conversion).
  Going with option 3 from the issue (event-triggered refresh, not periodic polling
  — preserves the manual-only decision) combined with the existing lightweight
  one-shot `gh` call BuildWatchWindow.CheckIssueClosuresAsync already uses for its
  "Recheck closures" button (`GitHubIssuesService.GetOpenIssueNumbersAsync`):
  BuildQueuePanel.RefreshAsync (its own local-DB poll, not GitHub) now detects when
  a queue item transitions into Verifying/terminal state and, on that real
  transition, fires one lightweight `gh issue list` call to refresh `_openIssues`
  — the exact moment option 3 names as "a blocker is most likely to have just
  closed." Also adding a last-refreshed-at cue to the BLOCKED badge/tooltip
  (option 1) so staleness is visible even between transitions.
- 2026-09-06T15:19:40Z ✅ DONE — Shipped in
  `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`:
  - New `AutoRecheckOpenIssuesOnTransitionAsync(previous, current)` called
    fire-and-forget from `RefreshAsync` on every local-DB queue poll tick, diffing
    old vs. new per-item status. Fires exactly one `gh issue list` (via
    `GitHubIssuesService.GetOpenIssueNumbersAsync`) only when some item just
    transitioned into `VerifyingStatus` or `"done"` — never on a timer, guarded
    against overlap by `_autoRecheckInFlight`. Feeds the result through the
    existing `ApplyOpenIssueSet` path (same counts/DAG redraw as a real Git Board
    refresh), so `_openIssues` — and `IsGenuinelyBlocked()`'s 🔒 BLOCKED badge —
    now self-heals the moment a build reaches Verifying/Done, without
    reintroducing periodic polling.
  - New `_openIssuesRefreshedUtc` field, stamped in `ApplyOpenIssueSet`, surfaced
    via a new `FormatAgo` helper in the QUEUE header's tooltip
    ("blockers last checked Xm ago" / "blockers not yet checked this session") —
    option 1 from the issue, so staleness is visible even between transitions.
  - `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings,
    0 errors.
  - Verified `agent/2107-q1760` (commit `7a9914403`) is a real ancestor of
    `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0,
    "Safe to write DONE") before writing this status.
  - `git status --porcelain` clean at session end.
  - No production/GitHub-polling boundary crossed: this uses the same one-shot
    `gh` CLI call already sanctioned for Build Watch's manual "Recheck closures"
    button — event-triggered, not a reintroduced background poll — so the
    2026-08-14 manual-only decision (memory note
    `buildconsole-github-manual-only-refresh`) stands unchanged.
