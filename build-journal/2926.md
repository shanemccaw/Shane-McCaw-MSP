# #2926 — Batter Up / AI Batter Up show transient #2815 circuit 403 as generic hard error

- **Status:** ✅ DONE 2026-09-06T17:12:46Z
- **Scope:** buildconsole

## Log
- 2026-09-06T17:08:35Z — Session started. Fixing BatterUpPanel/AiBatterUpPanel to detect the #2815 rate-limit circuit-open 403 and render a distinct self-healing message instead of the generic "Couldn't read ..." hard error.
- ✅ DONE 2026-09-06T17:12:46Z — Added `GitHubRateLimitCircuit.IsCircuitOpenMessage(string?)` and `RemainingOpenSeconds()` (`desktop/BuildConsole/Services/GitHubRateLimitCircuit.cs`). Both `BatterUpPanel.RefreshCoreAsync` and `AiBatterUpPanel.RefreshCoreAsync` catch blocks now check the caught `ex.Message` against `IsCircuitOpenMessage` and, when true, render "GitHub is rate-limiting BuildConsole right now — this will recover automatically in ~Ns (Git #2815), no action needed." instead of the generic "Couldn't read ..." hard-error text — a real, standing error still shows unchanged. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0 errors. Commit `ec0d078d67e1d192e191a34495b5a663a089f277` on `agent/2926-q1778`, merged into `origin/main` at `540a956a2..` (merge commit pushed directly to `main`); `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean.
