# #2410 — Build Queue: local queue must revalidate item status against GitHub on refresh (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T18:40:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** d84b9eb65 (rebased onto main as 95ebc537 on push)

## Log
- 2026-09-02T18:05:00Z ⏳ IN FLIGHT — Real audit done: `bt_build_queue.status` is BuildConsole's own
  local lifecycle status (queued/running/parked/external/...) and is separate from the real GitHub
  Project "Status" field (Backlog/Batter Up/In Progress/Verifying/...). ShaneBuilder's left-rail
  Build Queue panel (`_queueItems`) is still fixture data (`SeedSampleQueueData`, tracked separately
  under #2263's own audit note) — the only real, end-to-end `bt_build_queue`-backed queue view that
  currently exists in ShaneBuilder is the Command Palette's Builds/Build IDs category
  (`QueueReadClient.GetRecentBuildsAsync` → `_paletteBuilds`, Git #2203). Implementing reconciliation
  there: batched real GitHub Project Status lookup (`ChatGitHubFilter`, already has a working
  single-issue `GetBoardStatusAsync`), cross-referenced against each row's real local status, so a
  row the local queue thinks is active but GitHub now shows Backlog (or any other non-launch status)
  stops rendering as active on the next palette refresh.
- 2026-09-02T18:40:00Z ✅ DONE — Shipped: `ChatGitHubFilter.GetBoardStatusesAsync` (one batched
  `gh api graphql` call with per-issue aliases, real board Status for many issue numbers at once,
  fails open to `null`/"unknown" per row on any lookup failure); `QueueReadClient.IsLocalActiveStatus`
  /`IsLaunchEligibleBoardStatus` (BuildConsole's own real `IsActiveStatus` vocabulary, reused for
  consistency) plus `QueueStatusReconciler.ReconcileAsync`; `PaletteBuildQueueRow.BoardStatus`/
  `IsStale` (mutable, filled in post-construction); wired into `EnsurePaletteRealDataAsync` as a
  fire-and-forget follow-up pass (doesn't block the palette's normal open) that re-renders once the
  real GitHub lookup resolves; Builds/BuildIDs palette rows and the build detail pane now show the
  real reconciled state ("stale — GitHub now says Backlog") instead of only the cached local status.
  Verified: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 errors, 0 warnings. Ran the
  real batched GraphQL query live against this repo (`gh api graphql`) for #1734 (the real issue
  cited in this bug) and #2410 itself — confirmed #1734 is really "Backlog" on the board right now,
  #2410 is really "Batter Up" — proving the query shape and field-parse path are correct against
  live data. `bt_build_queue` row for #1734 (id 1113) is already `status=failed` (Shane's manual
  stop from the original incident), so there's no currently-active row to demonstrate the
  reconciler flipping live in this session — the fix is forward-looking for the next time an active
  row's board status is moved back to Backlog. Real gap not covered by this build: ShaneBuilder's
  left-rail Build Queue panel (`_queueItems`) is still `SeedSampleQueueData()` fixture data (a
  separate, larger foundational gap already tracked on #2263's own audit note) — reconciliation is
  wired only into the one real end-to-end `bt_build_queue`-backed view that exists today, the
  Command Palette Builds/Build IDs category. No test-manifest — out of scope per CLAUDE.md (desktop
  WPF app, not `artifacts/msp-portal` or `artifacts/shane-mccaw-consulting`). Branch
  `agent/2410-q1195` rebased onto `origin/main` and pushed directly to `main` (95ebc537), confirmed
  via `verify-branch-merged.mjs`. No findings filed — no genuine new bug/gap discovered beyond the
  known fixture-data gap already tracked on #2263.
