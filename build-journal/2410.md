# #2410 — Build Queue: local queue must revalidate item status against GitHub on refresh (ShaneBuilder)

- **Status:** ⏳ IN FLIGHT 2026-09-02T18:05:00Z
- **Scope:** buildconsole (ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-02T18:05:00Z ⏳ IN FLIGHT — Real audit done: `bt_build_queue.status` is BuildConsole's own
  local lifecycle status (queued/running/parked/external/...) and is separate from the real GitHub
  Project "Status" field (Backlog/Batter Up/In Progress/Verifying/...). ShaneBuilder's left-rail
  Build Queue panel (`_queueItems`) is still fixture data (`SeedSampleQueueData`, tracked separately
  under #2263's own audit note) — the only real, end-to-end `bt_build_queue`-backed queue view that
  currently exists in ShaneBuilder is the Command Palette's Builds/Build IDs category
  (`QueueReadClient.GetRecentBuildsAsync` → `_paletteBuilds`, Git #2203). Implementing reconciliation
  there: batched real GitHub Project Status lookup (`ChatGitHubFilter`, already has a working
  single-issue `GetBoardStatusAsync`), cross-referenced against each row's real local status, so a
  row the local queue thinks is active but GitHub now shows Backlog (or any other non-launch status)
  stops rendering as active on the next palette refresh.
