# #3937 — Launch Control: Conditional Access actions require Entra ID P1/P2, no precondition check surfaces this

- **Status:** ✅ DONE 2026-09-13T06:56:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 6119d8653 (rebased/pushed as 7db870dcb on origin/main)

## Log
- 2026-09-13T06:31:04Z ⏳ IN FLIGHT — investigated the real write path
  (`graphWriteForTenant` in `artifacts/api-server/src/lib/graph.ts`). Confirmed the
  read path (`graphFetchForTenant`) already classifies a license-gap Graph body via
  `classifyGraphError` and throws a distinct `LicenseGapError`, but the WRITE path
  (`graphWriteForTenant`, the one Launch Control's CA actions actually go through)
  had no equivalent — every 401/403 on a write collapses to a generic
  `errorType: "insufficient_privilege"`, which is exactly the raw passthrough #3937's
  live evidence shows. Also found #3947 (Shane's real decision comment on this
  finding) already dispatched with a broader, proactive precondition-check design
  (schema column + `/subscribedSkus` precheck + UI labeling) but not yet landed in
  code or DB (`write_action_catalog` has no `required_license_skus` column locally).
  Proceeding with a scoped, non-overlapping fix: extend the write path's error
  classification to reuse the same `classifyGraphError` machinery the read path
  already has, and thread a clear `license_gap` errorType + customer-safe feature
  name through `workflow-executor.ts` to the Launch Control execute route — this
  closes the literal "raw 403" gap in #3937 without touching schema/UI, so it won't
  collide with #3947's proactive mechanism landing separately.
- 2026-09-13T06:56:00Z ✅ DONE — implemented and shipped (commit 7db870dcb on
  origin/main): `graphWriteForTenant` (graph.ts) now calls `classifyGraphError`
  before falling through to the generic 401/403 handling and returns
  `errorType: "license_gap"` + a customer-safe `licenseFeature` name;
  `workflow-executor.ts` (BaselineTemplateExecutionResult, RollbackExecutionResult,
  the generic execution path, and the graph_write_operation/execute_baseline_template
  workflow nodes) propagates it; `mfa-reregistration.ts`'s AuthMethodDeleteResult
  type was widened for compatibility; `msp-launch-control.ts`'s execute route now
  returns a clear 409 ("This action requires <feature>...") instead of a raw
  passthrough. Added `src/lib/graph-write-license-gap.test.ts` (2 tests, both
  pass) covering the new branch against the real captured 403 body from this
  issue. `pnpm run typecheck` (api-server) clean. Live-verified against the real
  testbed tenant (c4c814d4-3afe-441e-9145-62461d0a4fd3): both
  action.create-ca-signin-risk-policy and action.create-ca-user-risk-policy now
  return `errorType: "license_gap"` (previously `insufficient_privilege`) via a
  scratch script, deleted after use. Full api-server vitest suite run with
  DATABASE_URL set: 364/370 files passed, 11 pre-existing failures unrelated to
  this change (config-pack-dry-run, monitoring-onboarding-scan,
  purchase-account-flow, retention/tenant-scope-coverage, boot-smoke,
  public-purchase-packs — none touch graph.ts/workflow-executor.ts/
  msp-launch-control.ts/mfa-reregistration.ts). Branch verified merged into
  origin/main via verify-branch-merged.mjs; working tree clean.
  Scoped deliberately to reuse the existing reactive classification the read
  path already had, rather than duplicating #3947's broader proactive
  precondition-check design (schema column + /subscribedSkus precheck + UI
  labeling), which is dispatched separately and had not landed as of this
  session. Left a comment on #3947 noting this landed piece so its own build
  doesn't re-do it.
