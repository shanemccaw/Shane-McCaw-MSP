# #3311 — 'What I like' occasional-purchase list with real deal-match surfacing

- **Status:** ✅ DONE 2026-09-09T02:51:42Z
- **Scope:** platform (web/shanes-life -- Shane's Life, sub-issue of Feature #3229)
- **Started:** 2026-09-09
- **Commit(s):** d97ec901f (feature commit), a5e1be87a (merge onto main)

## Log
- 2026-09-09T02:43:00Z ⏳ IN FLIGHT — Investigated: confirmed the Lists room's existing
  arbitrary-category `getOrCreateListByName`/`push_list` mechanism (same shape Watch/Books/
  Heading Out already use) is sufficient storage for "What I Like" -- no new table. Real, new
  work is the deal-match half: reusing `prices.mjs`'s existing `textsMatch` bidirectional
  substring match (the same one `attachWeeklyAdVerdicts` already uses for Shopping) to check
  freshly `push_deals`/`push_coupons`-pushed items against the list, then queue a real
  `core/nudges.mjs` push notification (kind `deal_match`) -- the same real proactive-surfacing
  mechanism appointment/vaccine/tesla/bank nudges already use, not a third invented mechanism.
  About to: add `lists.getOccasionalListItems` + constants, `prices.matchOccasionalListAgainst`,
  wire `mcp/tools.mjs`'s push_deals/push_coupons handlers, add a dedicated capture-grammar rule
  ("I sometimes get X" / "add X to what I like"), and tests.
- 2026-09-09T02:51:42Z ✅ DONE — Shipped: `lists.mjs` (OCCASIONAL_LIST_NAME/CATEGORY constants +
  getOccasionalListItems), `prices.mjs` (matchOccasionalListAgainst, reusing the existing
  textsMatch bidirectional substring match), `nudges.mjs` (deal_match tap-through route),
  `mcp/tools.mjs` (push_deals/push_coupons now check matches and queue a real deal_match nudge --
  same real proactive-surfacing mechanism appointment/vaccine/tesla/bank nudges already use; Lists
  room tiles are spec'd to stay dark always, so a per-list "N on sale" tile was not the fit),
  `capture-grammar.mjs` rule 27 + 2 new unit tests (subsequent rules renumbered 28-32).
  Verification: capture-grammar.test.mjs 46/46 passing on the post-merge tree (41/41 on my own
  branch pre-merge). Live end-to-end run against the real local `finances` Postgres DB, through
  the actual push_deals/push_coupons MCP tool handlers, using the `agent-verify@example.com`
  scratch account (zero passkeys -- not Shane's real account): capture grammar created "What I
  Like" and added both real items; pushing a matching weekly-ad price queued a real
  `nudge_events` row (kind `deal_match`) with correct title/body; a non-matching item queued
  nothing; a matching coupon push worked the same way. All scratch rows (list, items, store,
  price/coupon rows, nudge_events, nudge cap counter) cleaned up afterward -- verified zero
  residue for that account. Commit d97ec901f on `agent/3311-q2036`, merged onto `origin/main` as
  a5e1be87a; `verify-branch-merged.mjs` confirms ancestry; `git status --porcelain` clean.
  No new GitHub issues filed -- no gap or bug found outside this issue's own scope.
