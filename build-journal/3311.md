# #3311 — 'What I like' occasional-purchase list with real deal-match surfacing

- **Status:** ⏳ IN FLIGHT 2026-09-09T02:43:00Z
- **Scope:** platform (web/shanes-life -- Shane's Life, sub-issue of Feature #3229)
- **Started:** 2026-09-09
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-09T02:43:00Z ⏳ IN FLIGHT — Investigated: confirmed the Lists room's existing
  arbitrary-category `getOrCreateListByName`/`push_list` mechanism (same shape Watch/Books/
  Heading Out already use) is sufficient storage for "What I Like" -- no new table. Real, new
  work is the deal-match half: reusing `prices.mjs`'s existing `textsMatch` bidirectional
  substring match (the same one `attachWeeklyAdVerdicts` already uses for Shopping) to check
  freshly `push_deals`/`push_coupons`-pushed items against the list, then queue a real
  `core/nudges.mjs` push notification (kind `deal_match`) -- the same real proactive-surfacing
  mechanism appointment/vaccine/tesla/bank nudges already use, not a third invented mechanism.
  About to: add `lists.getOccasionalListItems` + constants, `prices.matchOccasionalListAgainst`,
  wire `mcp/tools.mjs`'s push_deals/push_coupons handlers, add a dedicated capture-grammar rule
  ("I sometimes get X" / "add X to what I like"), and tests.
