# #2080 — Chat-mention hover popup (#1253) needs the same state-aware action buttons #2061 added to Git Board rows

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 7c013fdb5 (implementation), rebased+pushed to main as 5b201ecd2

## Log
- 2026-08-31 ⏳ IN FLIGHT — Audited #2061's landed popover (`Controls/LeftSidebar.xaml.cs`:
  `FindAssociatedBuild`, `BuildIssueHoverPopupContent`, `BuildQuickActionArea`,
  `DispatchOrAskActiveChatAsync`, the `RequestDispatchBuild`/`RequestCancelOrStopBuild`/
  `RequestRetryBuild`/`RequestReplyToBuild`/`RequestOpenBuildChat` delegate properties MainWindow
  wires to `BuildQueuePanel.Quick*Async`) and #1253's current chat-mention hover popup
  (`Services/IssueMentionInjector.cs`'s HTML/JS tooltip + `BT_HOVER_ISSUE`/`BT_OPEN_ISSUE`
  message contract, handled in both `MainWindow.xaml.cs` (`ChatWv_WebMessageReceived`) and
  `FloatingChatWindow.xaml.cs` (`OnBridgeMessage`/`ResolveAndShowIssueTipAsync`) — two hosts,
  confirmed both need the same treatment).
  Plan: extract the status→action classification #2061 built inline in `BuildQuickActionArea`
  into a shared `Services/IssueQuickActionResolver`, reuse `FindAssociatedBuild` +
  `DispatchOrAskActiveChatAsync` (making both `internal`) from a new
  `LeftSidebar.BuildChatMentionActionPayload(GitIssue)` that both hosts call, extend
  `__btShowIssueTip`'s JS to render the resulting action button(s)/progress bar/reply box and
  post a new `BT_ISSUE_ACTION` message back, and wire `BT_ISSUE_ACTION` in both hosts to call the
  exact same `BuildQueuePanel.Quick*Async` methods #2061 already established — no second
  implementation of resolution or execution, only a second *renderer* (HTML vs WPF) for a shared
  data shape.
- 2026-08-31 ✅ DONE — Implemented as planned:
  - `Services/IssueQuickActionResolver.cs` (NEW) — `IssueQuickActionResolver.Resolve(QueueItem)`
    holds the exact queued/running/done/failed/other classification that used to live inline in
    `LeftSidebar.BuildQuickActionArea` (labels, progress-bar wiring, stale text, reply
    eligibility), plus the `ChatMentionActionPayload`/`ChatMentionBlocked`/`ChatMentionBuild` DTOs
    the chat-mention popup serializes to JSON.
  - `Controls/LeftSidebar.xaml.cs` — `FindAssociatedBuild` and `DispatchOrAskActiveChatAsync`
    made `internal` (were `private`); `BuildQuickActionArea` refactored to render from
    `IssueQuickActionResolver.Resolve` instead of its own inline switch (WPF-visible behavior
    unchanged — verified label/threshold parity against the pre-refactor code line by line); new
    `internal ChatMentionActionPayload BuildChatMentionActionPayload(GitIssue issue)` reuses the
    same blocked-precedence check, `FindAssociatedBuild`, and resolver to build the payload the
    chat-mention popup needs — one resolution implementation for both surfaces.
  - `Services/ChatMentionPopupHelper.cs` (NEW) — the "resolve #NNN (cache first, live GitHub
    fetch fallback) + build the `__btShowIssueTip(...)` call" sequence that used to be duplicated
    almost verbatim between `MainWindow.xaml.cs` and `FloatingChatWindow.xaml.cs`, now one shared
    `BuildShowTipScriptAsync(int, LeftSidebar?)` both hosts call.
  - `Services/IssueMentionInjector.cs` — `__btShowIssueTip` now takes a 5th `actions` argument and
    renders it: blocked banner, or a status/progress-bar/action-button/reply-box block (Dispatch
    Now / Cancel-Stop / Retry / Open Build / Open Chat, matching #2061's Git Board buttons
    exactly), or the no-tracked-build "Dispatch (asks active chat if no BUILD: yet)" button.
    Buttons post a new `BT_ISSUE_ACTION` message (`{number, buildId, action, message?}`). Added a
    debounced show/hide (`cancelHide`/`scheduleHide`, mirroring `LeftSidebar`'s
    `IssueHoverPopup_MouseEnter/Leave` dispatcher-timer pattern) so the pointer can actually cross
    the gap from the underlined `#NNN` text into the popup to click a button — the previous
    immediate-hide-on-mouseleave only worked because the old popup had nothing worth clicking
    except the close button.
  - `MainWindow.xaml.cs` (`ChatWv_WebMessageReceived`) and `FloatingChatWindow.xaml.cs`
    (`OnBridgeMessage` + new `HandleIssueActionAsync`) both: (1) call `ChatMentionPopupHelper` for
    `BT_HOVER_ISSUE`, (2) handle the new `BT_ISSUE_ACTION` by resolving the `QueueItem` (by
    `buildId` if hover already found one, else `LeftSidebar.FindAssociatedBuild` again) and
    dispatching straight to `BuildQueuePanel.QuickDispatchAsync`/`QuickCancelOrStopAsync`/
    `QuickRetryAsync`/`QuickReplyAsync`/`QuickOpenChat` — the exact #2061 wrappers, no second
    implementation — or to `LeftSidebar.DispatchOrAskActiveChatAsync` for the no-build case.
  **Verification:** `dotnet build BuildConsole.csproj` clean (0 errors/0 warnings). Rebased onto
  `origin/main` (`ea9548473`) and re-verified ancestry after push — rebase applied without
  conflict, tree unchanged from the build-verified commit. Did not launch a live BuildConsole.exe
  instance to manually hover a chat mention — same reasoning #2061 documented (real GitHub PAT,
  local Postgres, active queue watcher; manual mouse-hover isn't drivable from this shell
  session). Per CLAUDE.md, `desktop/BuildConsole` is out of scope for test manifests.
  **Merge:** rebased onto `origin/main`, pushed directly to `main` as `5b201ecd2`;
  `verify-branch-merged.mjs` confirms ancestor. Working tree clean (`git status --porcelain`
  empty apart from this bookend edit).
