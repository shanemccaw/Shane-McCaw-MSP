# Issue #3697 — shanes-git has no file-read capability (get_file_contents / list_directory / search_code)

- **Status:** ✅ DONE 2026-09-12T00:07:51Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** `47c1bb9e6` (tools + docs), `c8b901888` (verification: real >1MB + binary paths)

## Log
- 2026-09-11T21:00:00Z ⏳ IN FLIGHT — Starting. Adding three real read tools to the `shanes-git` MCP server (`artifacts/github-mcp-server`) per #3697's body: `get_file_contents(path, ref?, repo?)`, `list_directory(path?, ref?, repo?)`, `search_code(query, perPage?, repo?)`. All read-only, so no `context` arg, per the #3538 convention.
- 2026-09-12T00:07:51Z ✅ DONE — Shipped all three on the existing server-side `GITHUB_MCP_PAT`, all read-only (no `context`), all taking #3580's optional `repo`. New files: `src/tools/get-file-contents.ts`, `src/tools/list-directory.ts`, `src/tools/search-code.ts`. `src/github.ts` gained `encodeContentsPath()`, `fetchContents()`, `normalizeContentEntry()`, `looksBinary()`, `fetchRepoTree()` and an `options.accept` override on `githubRequest()`; registered in `src/tools/index.ts`. Item 4 (honest size handling) is real: a >1MB file returns `content:null, truncated:true` with the real byte size and `downloadUrl`, rather than GitHub's own `content:""`/`encoding:"none"` which decoded naively reads as an empty file; a binary file returns `content:null, binary:true`; directory / symlink / submodule / missing-path each get their own message naming the real path. Docs updated in the package README, `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md`, and CLAUDE.md's shanes-git section.
- 2026-09-12T00:07:51Z ✅ VERIFIED — `node artifacts/github-mcp-server/scripts/verify-contents-tools.mjs` (new; `npm run verify-contents`): **37/37 checks passed against the REAL GitHub API** with the real server-side PAT. Covers real decoded file text, an explicit `ref`, all four refusal paths, root + subdirectory listings (including this build's own new files read back off `main` after push), both search backends, the `path:`/`filename:`/`extension:` qualifiers, a real >1MB file (`PLATFORM_BUILD_ARCHIVE_2026-08-12.md`, 2,773,504 bytes) and a real binary file (`Design/MSP_Console/.../01-managed-tenants.png`), and that the PAT never appears in any result or error message. `tsc -p tsconfig.json --noEmit` clean.
- 2026-09-12T00:07:51Z ⚠️ FINDING — Filed **#3726** (label `bug`, sub-issue of Feature #3377, board status "AI Batter Up"): GitHub's Code Search API returns `total_count: 0` with `incomplete_results: true` for this repo on **every** query — 7 distinct queries plus 3 repeats, while the identical call against a public repo returns real hits, and the repo's 6,745 blobs are far under GitHub's 500,000-file ceiling. A `search_code` that always answers "0 results" would be read as proof the code is absent, so it shipped with two real backends and always names which one answered in `source`: GitHub's index when it works, else a real PATH search over the recursive git tree (cached 5 min), whose `note` states on every call that it matched paths, not contents. On this repo today it always answers from the tree — real and useful, but path search rather than the content search the name implies. #3726 carries the open question.
- 2026-09-12T00:07:51Z ✅ MERGED — `node scripts/dev-server/verify-branch-merged.mjs` exit 0: `agent/3697-q2371` (`c8b90188`) is an ancestor of `origin/main`. Working tree clean; nothing was dirty on arrival that wasn't mine.
