# #2040 — Phase 2 write packs: Identity/Conditional-Access (identity:*)

- **Status:** ✅ DONE 2026-09-12T16:37:16Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** d1638a1f1 (IN FLIGHT bookend), 0643f507d (migrations, on main)

## Log
- 2026-09-12T16:25:37Z ⏳ IN FLIGHT — Fan-out from #1925 Phase 2, Identity/CA slice (largest, lockout-gated).
  Investigated live DB: 32 `identity:*` monitor_checks, 5 already have execution-ready templates mapped
  (b2b-collaboration-settings, break-glass-health, ca-report-only, mfa-registration, named-locations).
  Doc-verified against Microsoft Graph v1.0 this session (conditionalaccesspolicy / conditionalaccessconditionset
  / conditionalaccessusers / conditionalaccessgrantcontrols) the exact body shapes for the CA-policy family.
  About to author a new `identity-ca-hardening-v1` pack with 5 CA-policy-creation write templates — ALL created
  in `enabledForReportingButNotEnforced` (report-only) state with a break-glass group exclusion, so even a
  future apply cannot lock anyone out — mapped to: `identity:ca-legacy-auth-block`, `identity:legacy-auth-usage`
  (same block policy), `identity:signin-risk-policy`, `identity:user-risk-policy`, `identity:guest-mfa-enforcement`,
  `identity:ca-mfa-coverage`. HARD CONSTRAINT honored: author + planOnly ONLY, no apply against the testbed
  (Shane's real M365 tenant). Flagging (NOT authoring, per "flag ambiguity rather than guess"):
  `identity:sspr-config` (severity flags admin-SSPR-allowed; `allowedToUseSSPR` is a deprecated/ambiguous
  remediation target) and `identity:password-expiration-policy`/`platform:tenant-password-expiration`
  (both have EMPTY severity_rules → never produce a finding to remediate, and setting expiration contradicts
  current Microsoft/NIST never-expire guidance — a product decision, not agent work).
- 2026-09-12T16:37:16Z ✅ DONE — Shipped two additive idempotent manual migrations (both run against local
  DATABASE_URL, both on main at 0643f507d): `identity-ca-hardening-v1` (5 CA-policy creates — legacy-auth-block,
  mfa-coverage, signin-risk, user-risk, guest-mfa — ALL created report-only `enabledForReportingButNotEnforced`
  with break-glass exclusion, so a future apply can't lock anyone out; enforce is the separate
  microrem.enforce-ca-policy step = #1281 gate) and `identity-sspr-admin-v1` (`action.disable-admin-sspr`,
  low-risk PATCH authorizationPolicy allowedToUseSSPR:false). 6 identity checks now `we_can_run`. During doc
  verification I confirmed `allowedToUseSSPR` is admin-specific and NOT deprecated, so sspr-config was authorable
  after all (not the ambiguity I'd flagged pre-verification). **Verification:** replicated
  `resolveBaselineTemplateRequest` (JSON.stringify → interp {{var}} → JSON.parse) against the real DB rows with a
  sample break-glass GUID — all 6 templates resolve to valid JSON, zero leftover `{{}}`, correct endpoint,
  `missingVariables=[]`; migrations idempotent (re-run = 0 inserts). No apply against the testbed — planOnly/authoring
  only, HARD CONSTRAINT honored. All Graph v1.0 shapes doc-verified this session (URLs in each migration header).
  Both migrations appended to #1630. Findings filed: **#3799** (password-expiration checks have empty severity_rules
  + are duplicative; parented to #2494, board=AI Batter Up). `legacy-auth-usage` intentionally not double-mapped
  (shares the block-legacy-auth policy; dup template_id would collapse the pack graph). Branch verified merged to
  main; working tree clean.
