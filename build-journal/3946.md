# Build Journal — Git #3946

- **Status:** ✅ DONE 2026-09-13T06:15:00Z
- **Scope:** platform

## Log

- 2026-09-13T06:02:45Z — Session started. Pulled #3946's full issue body plus
  both comments — the corrected-scope comment ("chat: free-scan audit
  correction 2026-09-13") and the dispatch comment supersede the original
  issue body, which audited the wrong function (`ensureMonitoringScanKickoff`,
  the paid-Monitoring-purchase-onboarding backstop, which is NOT the mechanism
  in scope here and must not be modified).

  Real, corrected task: add a new, narrow, idempotent, additive function
  (new file, adjacent to `artifacts/api-server/src/lib/monitoring-onboarding-scan.ts`)
  that is the Free-Scan-Prospect equivalent of that same backstop pattern —
  takes a caller-resolved `customerId`, no-ops if an `mspDiagnosticRunsTable`
  row already exists for it, otherwise fires `runDiagnostics` (dynamic import
  from `./diagnostics-runner.ts`) with `packageKey: "core:free-scan-full"` and
  `isAssessmentTriggered: true`, fire-and-forget, same log-on-success/failure
  pattern as `ensureMonitoringScanKickoff`.

  **No caller exists yet, by design.** Phase 6 (#1358, locked results view)
  and Phase 7 (#1359, return link) of #1352 are the real intended future
  callers and are both still open/unbuilt. This build adds the engine-layer
  function and its own real tests only — it does NOT wire a caller into
  #1358/#1359. This is not orphaned/dead code; it is a documented, additive
  building block awaiting its two real callers.

- 2026-09-13T06:15:00Z — ✅ DONE — Added
  `artifacts/api-server/src/lib/prospect-free-scan-backstop.ts` exporting
  `ensureProspectFreeScanBackstop(customerId, deps?)`: checks
  `mspDiagnosticRunsTable` for an existing run for the given customerId
  (no-op if found), otherwise fires `runDiagnostics` (dynamic import from
  `./diagnostics-runner.ts`) with `packageKey: "core:free-scan-full"` and
  `isAssessmentTriggered: true`, fire-and-forget with log-on-success/failure
  and never throwing back to the caller — the same shape as
  `ensureMonitoringScanKickoff` (#1314), which is unmodified. Added
  `prospect-free-scan-backstop.test.ts` (3 tests, same integration pattern as
  `monitoring-onboarding-scan.test.ts`: real local Postgres rows, injected
  `triggerScan` so no real scan fires) covering: idempotent skip when a run
  already exists, firing exactly one scan with the correct opts when none
  exists, and never throwing when the injected trigger rejects. All 3 tests
  pass against the real local Postgres (`DATABASE_URL`). `tsc --noEmit -p
  tsconfig.json` run against the whole api-server package produced zero
  errors attributable to the new file (grepped for
  `prospect-free-scan-backstop`, zero matches; pre-existing unrelated errors
  in other files are out of scope). No schema/migration needed — no new
  columns or tables. No caller wired — reaffirmed here per the dispatch's
  explicit instruction not to wire #1358/#1359, both still open.

  Commits (on `main`, verified via
  `scripts/dev-server/verify-branch-merged.mjs` — exit 0, "Safe to write
  DONE"):
  - `31765532a` — Bookend: #3946 IN FLIGHT
  - `62515c162` — Add ensureProspectFreeScanBackstop engine-layer function

  `git status --porcelain` clean at session end. No findings filed — no
  bugs/gaps discovered outside this build's own scope.
