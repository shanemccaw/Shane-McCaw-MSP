# #2780 — "Hide Completed" toggle only checks top-level Epics

- **Status:** ✅ DONE 2026-09-04T00:58:00Z
- **Scope:** buildconsole

## Log

- 2026-09-04T00:49:08Z — Started. Real fix per issue body: `PopulateIssueTreeHierarchyAsync`
  (`Controls/LeftSidebar.xaml.cs` ~line 5671) had no Feature-tier "Hide Completed" check, unlike
  the top-level Epic loop (#2771, ~line 5269). Adding the mirrored check for Feature-tier nodes
  (title starts with `"Feature:"`, `SubIssueCount > 0 && SubIssueCompleted >= SubIssueCount`).
  Also applying Shane's scope amendment: `BuildConsoleSettings.HideCompletedEpics` default
  `false` → `true` (fresh installs only; existing persisted values unaffected).
- 2026-09-04T00:58:00Z — ✅ DONE. Implemented both fixes:
  - `Controls/LeftSidebar.xaml.cs` `PopulateIssueTreeHierarchyAsync`: added the mirrored
    Feature-tier "Hide Completed" check inside the recursive loop; leaf-tier issues unaffected
    (still gated only by the existing `filter` checks).
  - `Services/BuildConsoleSettings.cs`: `HideCompletedEpics` default `false` → `true`.
  - `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
  - Commit `f43a775fb` pushed to `agent/2780-q1488`, then fast-forwarded onto `origin/main`
    directly (`git push origin HEAD:main`). `verify-branch-merged.mjs` confirms OK — merged into
    `origin/main`.
  - `git status --porcelain` clean before this commit.
