# #2780 — "Hide Completed" toggle only checks top-level Epics

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:49:08Z
- **Scope:** buildconsole

## Log

- 2026-09-04T00:49:08Z — Started. Real fix per issue body: `PopulateIssueTreeHierarchyAsync`
  (`Controls/LeftSidebar.xaml.cs` ~line 5671) had no Feature-tier "Hide Completed" check, unlike
  the top-level Epic loop (#2771, ~line 5269). Adding the mirrored check for Feature-tier nodes
  (title starts with `"Feature:"`, `SubIssueCount > 0 && SubIssueCompleted >= SubIssueCount`).
  Also applying Shane's scope amendment: `BuildConsoleSettings.HideCompletedEpics` default
  `false` → `true` (fresh installs only; existing persisted values unaffected).
