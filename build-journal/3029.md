# Git #3029

- **Status:** ✅ DONE 2026-09-06T18:24:15Z
- **Scope:** platform

## Log
- 2026-09-06T17:48:33Z — Starting: add vi.stubEnv() for the 5 required env vars in routes/index.boot-smoke.test.ts so the test is self-contained.
- 2026-09-06T18:24:15Z — ✅ DONE — Added vi.stubEnv() for DATABASE_URL, AI_INTEGRATIONS_ANTHROPIC_BASE_URL, AI_INTEGRATIONS_ANTHROPIC_API_KEY, AI_INTEGRATIONS_OPENAI_BASE_URL, AI_INTEGRATIONS_OPENAI_API_KEY inside routes/index.boot-smoke.test.ts before the dynamic import. DATABASE_URL stubbed to `postgresql://user:pass@127.0.0.1:1/db` (port 1, nothing listening → instant ECONNREFUSED) rather than the more obvious `localhost:5432`, because this dev environment has a real Postgres listening there — a fake-creds connection against it turns several module-scope DB calls in the ~150-route import graph (admin-signal-rules.ts seeders, exception-tracker capture-on-failure, log-stream-writer's 1s flush interval) into real (if fast) failing auth round trips, which combined with the ~150-module cold-import transform cost pushed total run time flakily across the 60s test timeout (confirmed empirically: one run timed out at 60019ms, the next passed at 49500ms with the *original* localhost:5432 stub). Verified passing with the 127.0.0.1:1 fix: 47.52s, well under the 60s budget, with the expected non-fatal ECONNREFUSED warnings logged from the seeders (real failures, not masked). Commit `922417de1` on `agent/3029-q1798`, merged into `origin/main` and verified via `verify-branch-merged.mjs` (OK). Working tree clean.
