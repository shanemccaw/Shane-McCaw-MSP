# Build Journal — #3662

- **Status:** ✅ DONE 2026-09-11T15:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-11T15:05:00Z — Started. Scope: remove Chats panel epic header "N open" pill (LeftSidebar.xaml.cs ~3551-3568) and the Mentioned issue-number chip grid in BuildChatCard (~3805-3836). Display removal only; underlying MentionedIssueNumbers/board-count data untouched.
- 2026-09-11T15:20:00Z — ✅ DONE 2026-09-11T15:20:00Z — Removed both chip/pill elements in `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs`: the epic-header green "N open" `Border`/`TextBlock` block, and the `MentionedIssueNumbers` grid of `#N` chips in `BuildChatCard`. Left `AssociatedIssueNumbers`'s own pill removal (#3595) alone, and left epic title/progress bar/timestamp/chat-count/`#N` epic badge untouched. Underlying `MentionedIssueNumbers`/`_lastBoardIssues` data and detection logic untouched. Verified via `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors (6 pre-existing unrelated warnings). Commit `3f722580f` merged to `origin/main`; `verify-branch-merged.mjs` OK; `git status --porcelain` clean.
