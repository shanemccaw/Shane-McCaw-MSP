# #1891 — Screen clip overlay: right-click cancel

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** ead1f03d2

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a right-mouse-button cancel handler to RegionSelectOverlayWindow, mirroring the existing Escape-key cleanup path (DialogResult=false).
- 2026-08-30 ✅ DONE — added `MouseRightButtonDown="RootCanvas_MouseRightButtonDown"` on RootCanvas, handler sets `DialogResult = false`, mirroring `Window_KeyDown`'s Esc path exactly. Updated on-screen hint text. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 warnings/errors. Verified live with a scratch WPF harness referencing the real overlay source, driving real Win32 `SendInput`/`mouse_event`: left-down starts a drag, move, then a mid-drag right-click -> `DialogResult=False`, `SelectedPhysicalRect=Empty` — no stray selection captured. Commit `ead1f03d2` confirmed merged into `origin/main` via `verify-branch-merged.mjs`.
