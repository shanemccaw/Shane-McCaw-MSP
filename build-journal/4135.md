# #4135 — Account Security: missing "Export tenant evidence" row and delete-facts mismatch

- **Status:** ✅ DONE 2026-09-14T23:55:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** c7b687d27 (IN FLIGHT bookend), d1ba2a60d (real work), pushed to `origin/main` as 43ec67727

## Log
- 2026-09-14T23:46:00Z ⏳ IN FLIGHT — investigated both gaps: confirmed a real tenant-evidence-export endpoint exists (`GET /api/portal/remediation-tracker/evidence-pack.pdf`, already used by `useRemediationExport("evidence-pack")` on the Remediation Tracker page); confirmed via `portal-privacy.ts` / `data-rights.ts` / the data-rights contract pack that the real deletion path never deletes anything and has no tenant-admin-count check, so design's "Deleted"/"Tenant access" facts describe unimplemented behavior while the wired "Retained"/"Confirmation"/"Export first" facts are accurate.
- 2026-09-14T23:55:00Z ✅ DONE — Gap 1 fixed: added the "Export tenant evidence" row (design copy verbatim: "Export tenant evidence" / "Immediate" / "Go to evidence export") to `artifacts/portal/src/pages/account-security.tsx`'s "Your data" card, wired to the real `GET /api/portal/remediation-tracker/evidence-pack.pdf` via the existing `useRemediationExport("evidence-pack")` hook (`artifacts/portal/src/lib/remediation-tracker-export-api.ts`) — no new endpoint needed, no fixture. Gap 2 resolved as a finding, not a code change: re-verified against `portal-privacy.ts:277-306` / `data-rights.ts` / `docs/portal/data-rights-and-privacy-contract-pack.md` §6 that the real deletion path never deletes any row and has no other-admin/tenant-access check anywhere — so design's "Deleted" and "Tenant access" facts describe unimplemented behavior, while the wired "Confirmation" fact matches the real 200 response's own promise text (`portal-privacy.ts:298-301`). Left the wired facts unchanged (no fabricated rows added) and filed the stale-design gap as **#4148** (question-review lane, parented to #1595) for Shane's product decision (automate erasure? block on other admins?) rather than guessing. Verified: `tsc --noEmit` (0 errors) and `vite build` (succeeds) in `artifacts/portal`; updated `test-manifests/portal/account-security-data-rights.json` (UI5, `sessionTag: "4135"`) — a real `shaneapp://runTest?onlyTag=4135` invocation returned "no steps matched" because BuildConsole's dev-server checkout still resolves the pre-merge file (documented in the manifest's own notes, same structural gap prior sessions hit). Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
