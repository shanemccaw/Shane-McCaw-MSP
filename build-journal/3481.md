# #3481 — Feature: POA&Ms (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:35:00Z
- **Scope:** buildconsole (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 48f3a3b42 (real work, merged into `origin/main`; rebased over
  `b92675761` after a concurrent sibling session's push)

## Log
- 2026-09-10T06:18:00Z ⏳ IN FLIGHT — pulled #3481's full body and both dispatch
  comments per standing rules. Comment history:
  1. a `BUILD:` dispatch comment (buildSet `MyArchitect`, posted 04:46:33Z) carrying
     `--blocked-by 3493` explicitly, instructing to read
     `desktop/MyArchitect/GEMINI.md`, `BUILD_QUEUE_METHOD.md`, `UI_RULES.md`.
  2. a later `BUILD:` dispatch comment (buildSet `BatterUpClearOut`, posted
     04:51:36Z) that launched this session — it omits `--blocked-by` but doesn't say
     the blocker cleared, and only reiterates the standing "pull body + comments,
     build the real stated scope" instructions.
- 2026-09-10T06:18:00Z 🛑 BLOCKED — checked the real `blocked_by` dependency edges via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3481/dependencies/blocked_by`: none
  existed yet (empty array) despite the first dispatch comment's `--blocked-by 3493`
  — that comment never got wired into a real edge.
  - Read #3481's own body: real, confirmed backend already exists in
    `artifacts/api-server/src/routes/msp-poams.ts` — `GET/POST /api/msp/poams`,
    `GET/PATCH /api/msp/poams/:poamId`, `PATCH /api/msp/poams/:poamId/cancel`,
    `POST/PATCH/DELETE .../milestones[/:milestoneId]` — confirmed present by direct
    file read, not just the issue's own audit claim. #3481's own remaining checklist
    is 100% client UI: "Client: POA&M list + detail view for selected tenant",
    "Client: milestone CRUD", "Client: cancel action" — all three need somewhere to
    render.
  - Read the parent Epic #3454's body directly: **"STANDING RULE: #3493 (UI Shell
    Redesign) must land before any other Feature's UI work."** It also states
    explicitly: "Individual Issue-level breakdown under each Feature still pending
    Shane's confirmation of granularity — do not dispatch builds from this epic or
    its Features until that breakdown is filed and approved." #3481 has zero real
    sub-issues filed (`gh api .../3481/sub_issues` → empty) — its checklist is still
    prose, not broken-out leaf issues.
  - Checked #3493 directly: still `OPEN`, labeled `in-flight`, not `complete`.
    Confirmed no shell work has actually landed: `desktop/MyArchitect/` currently has
    no Fluent.Ribbon shell, no contextual-tab mechanism, no right-panel record
    workspace region — nowhere for a POA&M list/detail view to dock. `build-journal/
    3493.md` does not exist yet either.
  - This matches the identical pattern already recorded by sibling sessions on this
    same Epic this session: #3472, #3474, #3464, #3470 — all blocked on #3493 for the
    same reason (no shell to host UI work in).
  - Per CLAUDE.md's "blocked_by edges are the gate" rule (Git #1987), a comment saying
    "blocked" is not sufficient — wired the real edge myself since the first dispatch
    comment's `--blocked-by 3493` never actually got applied: `gh api -X POST
    repos/shanemccaw/Shane-McCaw-MSP/issues/3481/dependencies/blocked_by
    -f issue_id=<3493's real id>`.
  - Set label `blocked`, removed `in-flight`.
  - Posted an explanatory comment on #3481 naming the wait and why.
  - No code changed — nothing to commit for the build itself. Working tree clean
    (verified via `git status --porcelain`).
  - Re-dispatch once #3493 is closed/complete and the Epic's per-Feature
    issue-breakdown is filed for #3481.
- 2026-09-10T20:15:00Z ⏳ IN FLIGHT (resumed) — checked the blocker per CLAUDE.md's
  re-pickup rule: `gh issue view 3493` shows `CLOSED`/`COMPLETED`, labeled `complete`.
  `blocked_by` was `[{#3493}]` — real, not stale-empty — so removed it via the
  dependencies API, swapped `blocked`→`in-flight` on #3481. Verified the shell landed
  for real (`desktop/MyArchitect/Shell/*`, `e8460c5fd` on `main`) before building
  against it.

  Building #3481's own 3-item checklist directly (list+detail, milestone CRUD, cancel)
  against the shell #3493 shipped, same precedent #3471's own session already set this
  epic (build the issue's real scope once its blocker clears, without waiting on the
  Epic's separate "per-Feature issue-breakdown" note — no sibling Feature has waited
  on that once its own blocker cleared).
- 2026-09-10T20:35:00Z ✅ DONE — commit `48f3a3b42`, real work shipped and pushed to
  `origin/main` (rebased over `b92675761`, a concurrent sibling session's Runbooks push,
  after resolving one trivial additive conflict in `MainWindow.xaml.cs`'s
  `ApplyAuthState` — both sides just added a new service's `AuthToken = token` line;
  kept both). `verify-branch-merged.mjs` → exit 0, "Safe to write DONE". `git status
  --porcelain` clean.

  **What shipped, real and working** — no fixture data anywhere; every row/write is a
  real call against the already-existing `msp-poams.ts` endpoints:
  - `Models/Poam.cs` — `Poam`/`PoamMilestone`/response DTOs mirroring
    `mspPoamsTable`/`mspPoamMilestonesTable` (`lib/db/src/schema/msp.ts`) field-for-field.
  - `Services/IPoamsService.cs` + `PoamsService.cs` — real HTTP client for all 8 routes
    (list, create, get-one, patch, cancel, milestone create/patch/delete), same
    AuthToken/Authorize/JsonOptions shape as the existing `ChangeControlService`.
  - `MainWindow.xaml.cs`: Home tab "POA&Ms" group (Order 35, between Change Requests and
    Retainer Hours) — a real `GET /api/msp/poams` gallery filtered client-side to the
    active tenant (same approach `BuildChangeRequestRows` already uses, since the list
    route has no server-side tenant filter either), and a "New POA&M" Create command.
  - Full-panel record workspace per POA&M: real Facts (status/original-due/check),
    write-through Edits for the three fields the route allows editing
    (scheduledCompletionDate/interimCompensatingControl/resourcesRequired — each a
    single-field `PATCH` that re-fetches from the server rather than optimistically
    mutating locally), the real milestone list, and a confirm-armed, danger-styled
    "Cancel POA&M" action (`PATCH .../cancel`). A terminal-status plan
    (cancelled/completed/converted_to_risk_acceptance) renders read-only — no Edits, no
    Cancel action — matching the route's own refusal to edit one further.
  - Milestone CRUD via a contextual-tab **Record**-intent "Add Milestone" command
    (UI_RULES.md §2 — never fixed-tab legal) and a milestone detail workspace: Edits
    while `pending` (msp-poams.ts refuses edits once `completed`), confirm-armed "Mark
    Complete" and "Delete" actions. Every mutation reopens the parent POA&M record from
    real server state.
  - Create-new-POA&M workspace (Home tab): client-side required-field validation
    mirroring the server's own `createPoamSchema` (title, weaknessDescription, a valid
    `YYYY-MM-DD` scheduledCompletionDate, interimCompensatingControl,
    resourcesRequired), tenantId/tenantName sourced from the app's existing
    `_tenantService.CurrentTenant` (same fixture-tenant-service precedent every sibling
    Feature already uses — #3502/#3505/#3540 track that gap, not new to this build), a
    real editable primaryDomain field (never derived/guessed), a status cycle limited to
    `draft`/`pending_signature` (the only two values this route accepts at creation —
    `active` is portal-signature-only).
  - `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors, both before and
    after the rebase.

  **No genuine new finding to file this session** — the backend was already fully
  sufficient for all three of #3481's own checklist items; the one known gap touched
  (fixture `TenantService`) is already tracked under #3502/#3505/#3540, not a new
  discovery.

  Labels synced: `in-flight` → `complete`. Posted the completion comment on #3481.
