# #1480 — Account-scoped chats: Chats panel + associations follow the Primary/Secondary toggle

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** a4abc7bc4 (feature work, branch `agent/1480-q760`), merged to `main` at 9e9faec06

## Log
- 2026-08-28 ⏳ IN FLIGHT — starting: add `account` column to `bt_chats` (manual SQL,
  not run by this session), stamp it on chat create + auto-handoff, filter the Chats
  panel + In Progress list + associations (#1362/#1363 epic-resolution paths) on the
  title-bar toggle (#1419's `BuildConsoleSettings.DefaultAccount`), live-refresh on
  toggle flip, cross-account count-only badge, honest "database not ready" state if
  the column doesn't exist yet (same pattern as #1472).
- 2026-08-28 ✅ DONE — shipped in `a4abc7bc4`, merged to `main` at `9e9faec06`
  (`git merge-base --is-ancestor a4abc7bc4 origin/main` confirmed directly).
  `desktop/BuildConsole` build: 0 errors, same 46 pre-existing warnings as
  baseline, none new. `lib/db/migrations/manual/2026-08-28-bt-chats-account-1480.sql`
  written for Shane to review/run — not executed by this session.
  Honest gap: `artifacts/api-server`'s TS typecheck could not be fully
  confirmed clean pre-merge — the `@workspace/db` pnpm workspace symlink is a
  fixed relative path baked into the shared main checkout, so it can only
  ever resolve to the main checkout's copy of `lib/db`, never a worktree's
  own edited copy, regardless of which worktree resolves it. Verified by
  direct inspection that the resolved file contains the new `account` field
  and that my usages mirror the existing `archived`/`archivedAt` field
  pattern exactly; the two account-related tsc errors seen pre-merge were
  this resolution gap, not real type errors, and disappear once `main`
  (and thus the main checkout's own `lib/db`) actually carries the schema
  change post-merge. Separately, while investigating this a shared
  `node_modules/@workspace/db` symlink was accidentally broken and then
  repaired (see issue comment) — worth a standing note for future sessions
  that this package's worktree resolution is structurally limited this way.
