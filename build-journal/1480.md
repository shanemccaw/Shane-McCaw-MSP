# #1480 — Account-scoped chats: Chats panel + associations follow the Primary/Secondary toggle

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-28 ⏳ IN FLIGHT — starting: add `account` column to `bt_chats` (manual SQL,
  not run by this session), stamp it on chat create + auto-handoff, filter the Chats
  panel + In Progress list + associations (#1362/#1363 epic-resolution paths) on the
  title-bar toggle (#1419's `BuildConsoleSettings.DefaultAccount`), live-refresh on
  toggle flip, cross-account count-only badge, honest "database not ready" state if
  the column doesn't exist yet (same pattern as #1472).
