# #1823 — Portal Shell: pillar switcher

- **Status:** ⏳ IN FLIGHT 2026-09-03T15:11:00Z
- **Scope:** platform

## Log
- 2026-09-03T15:11:00Z ⏳ IN FLIGHT — Read #1823 body + comment, confirmed blocker #1819 is
  closed/complete with no live `blocked_by` edges. Read `docs/design-system.md`,
  `Design/portal/design_handoff_ui_shell/{README.md,Shell.dc.html}` (the live design source —
  `Shell.dc.html`'s own `renderVals()` pillar-tab logic), and the real backend
  (`GET /api/portal/pillars` at `artifacts/api-server/src/routes/portal-assessment.ts:691`,
  `GET /api/portal/pillars/history` at `:574` — both already renamed off the
  `/portal/assessment/*` prefix per #1753/#1752, confirmed live in code, not assumed from the
  epic's endpoint table).
- 2026-09-03T15:29:22Z ✅ DONE 2026-09-03T15:29:22Z — Found the pillar switcher itself
  (`artifacts/portal/src/components/shell/PillarTabStrip.tsx` +
  `shell/usePillarSummary.ts`) already built and wired by #1819 (which needed real pillar data
  to make the shell frame functional) — identity colours fixed per pillar
  (`@workspace/copilot-scan-scene/journeyTokens` `PILLARS`, matching `docs/design-system.md`
  §2 verbatim), severity as a separate score-only overlay (`SEVERITY_ON_DARK` +
  `severityForScore`, `>= 60` healthy / `>= 50` attention / `< 50` critical), a null/unscored
  score rendering the honest em dash in `#475569` rather than a red zero, and Copilot excluded
  as a tab (`usePillarSummary.ts` drops the `"copilot"` card — roll-up, not a seventh pillar).
  Confirmed this matches `Shell.dc.html`'s own reference logic (`P`/`PSCORES`/`SEVC` in
  `renderVals()`) exactly, including that the reference's pillar tab strip carries **no**
  ambient glow — the hand-tuned per-pillar glow table (`docs/design-system.md`'s
  "per-pillar ambient glow") belongs to the pillar pages (#1621's scope), not this switcher,
  so it was correctly left out rather than added speculatively.
  - This build's own change:
    (1) added the `#0078D4` `focus-visible` ring to the pillar tab `<Link>`s — the one
    real design-compliance gap found, against the README's own "every ... nav row ... is a
    real focusable control with a visible `#0078D4` focus ring" (the top bar's popover
    triggers already carry this; the pillar tabs didn't);
    (2) rewrote the component's doc comment to record the #1823 design-token audit findings
    above and attribute the switcher to this issue rather than only #1819.
  - **Finding filed:** #2558 (parented under #1817, `bug`, milestone v1.1, board status
    "AI Batter Up") — `SidebarNav.tsx`'s module nav rows (#1819's own component, out of
    #1823's scope) have the same missing-focus-ring gap; not fixed here since it isn't this
    issue's component.
  - Verification: `npx tsc -p tsconfig.json --noEmit` — zero errors in
    `PillarTabStrip.tsx`/`usePillarSummary.ts` (or any file this build touched); the only
    errors printed are the same 22 pre-existing `class-variance-authority`-rooted errors
    #1819 already found and filed as #2546 (still open), none touching these files.
    `npx vite build` transformed all 1731 modules (including mine) via esbuild before failing
    at Rollup's resolution of that same pre-existing broken dependency — did not re-file
    #2546. Because #2546 still blocks a real production build, `shaneapp://runTest` cannot
    render this app live right now (same real blocker #1819/#1820 hit) — this build's own
    change is CSS/accessibility + doc-comment only, not new user-facing behaviour beyond what
    #1819 already shipped, so no new test manifest was written for it; a live-render manifest
    for the shell is better owned once #2546 clears.
  - No fixture/hardcoded data anywhere in this component; both pillar endpoints confirmed
    live in code, not assumed from the epic's endpoint table.
  - Merge/clean-tree verification below.
