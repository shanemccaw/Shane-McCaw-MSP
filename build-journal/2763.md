# #2763 — Monitoring: close coverage gap on the directory surface (41 readable, uncovered config resources)

- **Status:** ✅ DONE 2026-09-04T06:52:49Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 3f16d4852 (real work: migration + vitest test + config), landed on origin/main as 958b0b788; 6e08bfbfb (IN FLIGHT bookend, already on origin/main)

## Log
- 2026-09-04T06:28:09Z ⏳ IN FLIGHT — confirmed #2762 (blocker) has a real DONE bookend and `complete` label; #2763's own `blocked_by` edge (if any) is stale/inactive since the blocker is complete. Pulled the live query: `surface='directory' AND availability='available_now' AND check_coverage_count=0` = 41 rows (matches issue title, not drifted). Cross-checked `graph_path` collisions against the full `config_resources` table (the #2821 pattern #2761 hit) — none found this time, all 39 graph-path rows are unique (2 of the 41 are `m365dsc:`-origin with no `graph_path`: `O365AdminAuditLogConfig`, `O365OrgCustomizationSetting`). Shape of the 41: mostly `graph:beta`/`graph:v1.0` `roleManagement/*` sub-namespaces (cloudPC, defender, deviceManagement, exchange, entitlementManagement, directory) plus a handful of top-level directory resources (`/contacts`, `/contracts`, `/directoryObjects`, `/directoryRoleTemplates`, `/schemaExtensions`, `/admin/cloudLicensing/*`, `/admin/serviceAnnouncement/issues`). Investigating live reachability of each against the testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1, app-only) before designing checks, same discipline as #2760/#2761/#2762.
- 2026-09-04T06:35:00Z ⏳ IN FLIGHT — probed all 39 real Graph endpoints (both API versions) against the testbed tenant app-only (`MT_APP_CLIENT_ID`/`SECRET`, tenant GUID `c4c814d4-3afe-441e-9145-62461d0a4fd3`), via a throwaway script mirroring `scripts/config-state/verify-sample.mjs`'s own auth idiom (deleted before finishing, never committed). Real result: 6 real live 200s with genuine field-based scoring signal (`/admin/cloudLicensing/allotments`, `assignmentErrors`, `assignments`, `/admin/serviceAnnouncement/issues`, `/contracts`, `/contacts`); 12 real permission gaps (403/401/404); 9 real Entra ID P2/Governance license gaps; 3 endpoints needing a filter/fan-out design (`transitiveRoleAssignments` — confirmed even with `ConsistencyLevel:eventual` + a real `roleDefinitionId` filter, still "requires at least one filter"; `directoryObjects`); 3 real config_resources model bugs (`enterpriseApps` modeled as a collection but is a singleton `rbacApplication`; `roleManagement`/`roleManagement/directory` non-queryable containers); `directoryRoleTemplates` (145 items) confirmed a static Microsoft-provided catalog, no tenant state; `schemaExtensions` (250 items, paged) confirmed to be Microsoft Graph's GLOBAL published catalog (any app's `Available` extensions across ALL tenants per Microsoft's own docs), not tenant-scoped state — both excluded on the same real grounds as #2761's `conditionalAccess/templates` exclusion. Deep-dove the 6 real candidates' actual field values (allotments: `allottedUnits`/`consumedUnits`, currently 1/1 = exhausted on all 4 real pools; assignmentErrors: 0 currently; assignments: `disabledServicePlanIds` all empty currently; issues: 100 real items, 2 unresolved (both advisory, 0 incident); contracts: 0 currently; contacts: 2 real org contacts, both `serviceProvisioningErrors`/`onPremisesProvisioningErrors` empty).
- 2026-09-04T06:42:00Z ⏳ IN FLIGHT — designed 6 real checks against the live evidence above (no generic-existence checks — #2184 discipline). Ran the real `matchEndpointToResource` (`scripts/config-state/map-monitor-checks.mjs`) directly against the full `config_resources` table before writing SQL — 6/6 resolve `high` confidence, `graph-path-exact`. Wrote and ran `lib/db/migrations/manual/2026-09-04-directory-surface-coverage-2763.sql` against local `DATABASE_URL`: 6 `monitor_checks` rows, 30 `monitoring_package_checks` memberships (assess:copilot-readiness/core:enhanced-monitoring/core:growth/core:premier/detail:full-item-collection — matching the existing `m365:service-health`/`identity:pim-eligible-roles` directory-surface precedent), 6 `config_resource_check_coverage` rows, coverage recompute. Real effect confirmed: `surface='directory' AND availability='available_now' AND check_coverage_count=0` dropped from 41 → 35.
- 2026-09-04T06:50:00Z ✅ DONE — Wrote a permanent vitest regression test (`artifacts/api-server/src/lib/__tests__/directory-surface-coverage-2763.test.ts`, 15 assertions, registered in `vitest.config.ts`'s include list, mirroring #2762's own precedent) using real testbed-observed item shapes plus synthetic risky shapes to exercise every severity branch — 15/15 pass. Caught and fixed a real latent bug in the row-boundary regex this test pattern copies from #2762: this repo's `core.autocrlf=true` (no `.gitattributes` override) means a *fresh checkout* of the migration file lands with CRLF line endings, and the LF-only `"\n),\n("` boundary literal silently never matches then — confirmed by round-tripping my own migration file through a forced CRLF conversion and back; my file fixed with a `.replace(/\r\n/g, "\n")` normalization before matching, comment explaining why. **The same bug was found genuinely already broken, live, in this exact worktree, in #2762's own copy of the identical pattern** (`compliance-surface-coverage-2762.test.ts`) — confirmed the migration file itself is byte-for-byte unchanged since #2762's commit (`git diff HEAD origin/main` empty) yet the test now fails `expected 40 to be 4` on a fresh run; filed as **#2836** rather than edited out of #2763's own scope. Ran the full api-server vitest suite: 200 passed / 97 failed test files (msp-financial-aggregator rounding bugs etc — all pre-existing/unrelated, confirmed by grep; my own new file not among them). `npx tsc --noEmit` on api-server: clean, zero errors. Findings filed: **#2835** (35 remaining directory-surface resources, full per-resource reason breakdown: 12 permission gaps, 9 license gaps, 3 filter/fan-out-needed, 3 real config_resources model bugs, 2 deliberate static-catalog exclusions, 2 cheap PowerShell-executor follow-ups) and **#2836** (the CRLF test-boundary bug). Both parented to Feature #1128 as direct sub-issues, labeled `bug`, board status "AI Batter Up". Committed as `3f16d4852`, rebased onto `origin/main` and pushed directly (`git push origin HEAD:main`) — landed as `958b0b788`. `verify-branch-merged.mjs`: OK. `git status --porcelain`: clean. Labels flipped `in-flight`→`complete` on #2763; completion comment posted.
