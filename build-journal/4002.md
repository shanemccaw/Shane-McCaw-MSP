# #4002 — Offboarding: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:11:13Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** d7e1053f5 (IN FLIGHT bookend), 0f8831f00 (real work — wire offboarding.tsx + mspName field, merged into origin/main)

## Log
- 2026-09-14T04:09:06Z ⏳ IN FLIGHT — pulled #4002 + #1653 body/comments, confirmed real design export at `Design/portal/design_handoff_full_site/screens/Offboarding.dc.html` and its landed contract pack (#2444). Checked #2766 (ghost-mode UI) and #2767 (post-termination gated screen) explicitly against the landed design: neither is drawn (design's own ledger states "No undo and no reactivate. Nothing resets an inactive organisation from the portal"), so both stay open, not silently dropped. Building `artifacts/portal/src/pages/offboarding.tsx` wired against `docs/offboarding-contract-pack.md` §5/§6 (`POST /api/portal/customer/offboard`, `GET /api/portal/customer/export`), plus a real `mspName` field added to `GET /api/portal/dashboard` (no prior route exposed a CustomerUser's servicing MSP's display name, needed for the brokered-scene copy).
- 2026-09-14T04:11:13Z ✅ DONE — Shipped `artifacts/portal/src/pages/offboarding.tsx` ("Leaving"), `offboardingLive.ts`/`offboardingWire.ts`, registered `/offboarding` in `App.tsx` and a "Leaving" row in `UserMenu.tsx`. Added a real `mspName` field to `GET /api/portal/dashboard` (`portal-customer-engines.ts`) backed by a real `mspsTable` lookup, and updated the two mocked-DB test files covering that route (`portal-dashboard-route-collision.test.ts`, `portal-customer-engines-assessment-redaction.test.ts`) for the new query — 18/18 targeted tests pass. Real `tsc --noEmit` (0 errors) and `vite build` (succeeds) on `artifacts/portal`; real `tsc --noEmit` (0 errors) on `artifacts/api-server`. Test manifest `test-manifests/portal/offboarding.json` written and registered in `_regression-suite.json`; a real `shaneapp://runTest` attempt (both `portal/offboarding.json` and bare `offboarding.json`) returned "manifest not found" because BuildConsole's local instance resolves `test-manifests/` against the shared main checkout, which hadn't picked up this branch yet — same structural gap `test-manifests/portal/billing.json` already documents; re-run once the dev-server merge/restart cycle picks this up. #2766/#2767 left open (real, separately-scoped, explicitly checked — not resolved by this design). No new findings filed. Two rebases onto a moving `origin/main` were needed (concurrent sessions landed #3994 break-glass access, `scope-and-sla.tsx`, etc.); conflicts in `App.tsx`/`UserMenu.tsx` were resolved by keeping both sides. `verify-branch-merged.mjs` confirms `0f8831f00` is an ancestor of `origin/main`. Working tree clean.
