# #3533 — Contract pack #3357 (sales-offers/bundles) file:line citations stale — RBAC retirement (#2458/#2460)

- **Status:** ✅ DONE 2026-09-10T21:37:40Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 6d3230102 (IN FLIGHT bookend), b51d5cd66 / pushed as f1f37da71 (contract pack
  citation re-extraction)

## Log
- 2026-09-10T21:23:57Z ⏳ IN FLIGHT — full re-extraction pass on
  `docs/msp-console/sales-offers-and-bundles-msp-console-contract-pack.md`'s citations against
  current `main`, per issue #3533's own body: `requireRole` → `requireCapability` rename plus
  non-uniform line-number drift across `msp-sales-offers.ts`, `msp-sales-bundles.ts`,
  `requireAuth.ts`, `resolve-msp-id.ts`, `msp-entitlement.ts`, `lib/db/src/schema/index.ts`,
  `lib/db/src/schema/msp.ts`. Substantive analysis and the three filed findings (#3386, #3387,
  #3389) are not believed invalidated — this is a citation re-verification pass, not new analysis.
- 2026-09-10T21:37:40Z ✅ DONE — every citation in the pack re-derived by direct re-read (Read +
  `grep -n`) of current `main`, not carried forward by inference: `msp-sales-offers.ts` (+2 shift
  before its SSE route, +11 uniform after it — the SSE route itself got a real, behavior-preserving
  rewrite replacing the inline `ROLE_ORDER` array with a call to the shared
  `userClearsLadderCapability()` evaluator, §1.2 prose updated accordingly, not just its line
  numbers), `msp-sales-bundles.ts` (+1 uniform throughout, one new import line), `requireAuth.ts`
  (non-uniform — `requireCapability` now `:267-303`, `requireMspScope` `:329-364`,
  `assertCustomerAccess` `:389-417`, `isCustomerBlockedByStaffScope` `:461-464`), `resolve-msp-id.ts`
  and `msp-entitlement.ts` (+1 uniform each), both schema files (schema drift independent of the
  RBAC commits, confirmed by grep against current line numbers). Cross-checked `msp-sow.ts` and
  `portal-offers.ts` too (flagged in the issue body as also drifted, though not one of the "7
  files") — confirmed zero line-count drift in their cited ranges via full diff read, so their §4
  citations needed no change. `sales-offer-engine.ts`, `sse-channels.ts`, `sales-offers.ts`, and
  `msp-sales-bundles.test.ts` confirmed zero/no-net-line diff, so citations into them are untouched
  and still accurate. Verification: every route-boundary citation spot-checked against the real
  file with `sed -n` (all match `router.VERB(` / `);` exactly); full `git diff --stat`/`git diff`
  reviewed before commit. No substantive analysis or finding changes — #3386, #3387, #3389 stand
  exactly as originally filed. No new findings surfaced worth filing under #1571. No product code,
  schema, or UI changed. Branch verified merged into `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs` → exit 0) and `git status --porcelain` clean
  before writing this line.
