# #3210 — Feature: Money — Debts overlay in Accounts, real payoff sparklines (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T04:56:49Z
- **Scope:** platform (web/shanes-life -- Shane's Life, personal, milestone 19)
- **Started:** 2026-09-08
- **Commit(s):** 64b38d883 (rebased/pushed to main; was 5226ac315 on `agent/3210-q1954`)

## Log
- 2026-09-08T04:49:57Z ⏳ IN FLIGHT — read design `Shanes Life 17 - Money v3.dc.html` option
  1d ("Accounts · lower"), confirmed real critical-debt rows in the `finances` DB (H1 Mortgage
  $12,750.00, Treasury Offset Program/IRS $9,966.50, both `is_critical`). No real historical
  balance data exists anywhere (no `debt_balance_history`-equivalent table, `original_balance`/
  `last_payment_date` both NULL for both rows) -- building a real snapshot table + wiring it into
  `createDebt`/`updateDebt` so payoff-progress sparklines and the "paid down since" line are real,
  growing data, not fabricated history. #3172 (GigSparkline component) is still open/in-flight
  elsewhere; building a minimal real inline sparkline here scoped to this feature rather than
  duplicating or blocking on that broader port.
- 2026-09-08T04:56:49Z ✅ DONE — migration 048 (`debt_balance_history`, backfilled from real
  current balances); `recordDebtBalanceSnapshot` wired into `createDebt`/`updateDebt`;
  `getCriticalDebtOverlay()` (real per-debt sparkline points, real combined paid-down summary,
  no fabricated Jan-1 baseline -- reads the real earliest snapshot date instead);
  `GET /api/money/debts/critical-overlay`; Accounts tab "Debts · critical first" card with a
  real inline sparkline SVG per debt. Verified end-to-end against the real `finances` DB: read
  both real critical debts (H1 Mortgage $12,750.00, Treasury Offset Program/IRS $9,966.50),
  confirmed same-day upsert (no duplicate snapshot rows on a same-day edit), confirmed
  `ON DELETE CASCADE`, confirmed paydown math against a temporary synthetic prior-day row (then
  removed it -- real data left exactly as found, one real snapshot per debt). `node --check` on
  all three touched JS/mjs files. Branch rebased onto and pushed straight to `origin/main`
  (64b38d883) -- `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
  No findings filed -- no gap discovered outside this issue's own scope. #3172 (GigSparkline
  component) is still open elsewhere; this sparkline is a minimal real implementation scoped to
  this feature, not that broader port.
