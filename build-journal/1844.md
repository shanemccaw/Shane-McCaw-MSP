# #1844 — Config state: contract pack and design brief (customer portal + MSP operator console)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** `f7e1e8bb7` (IN FLIGHT bookend) · `dac077eee` (the pack)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Blocker #1843 confirmed closed + `complete`. Extracting the real
  configuration-state API surface (`/api/portal/config-state/*`, `/api/msp/config-state/*`), real
  Drizzle schema, real enums, and real testbed volume/fan-out numbers from local Postgres.
- 2026-08-31 ✅ DONE — Shipped `docs/configuration-state-contract-pack.md` (1,538 lines, 12 sections)
  as commit `dac077eee`, merged to `origin/main` (`dac077eee`), ancestor check passed.

  **Extraction, not authoring.** Every endpoint, field, enum and number came from the finished
  #1843 code, the Drizzle schema, or the real local database — nothing aspirational, nothing
  invented. Both audiences covered endpoint by endpoint, with the matching operator management
  action named for every customer read (11 rows in the §1 split; 7 customer endpoints, 14 operator).

  **Measured, not estimated.** Registry 1,539 types / 1,359 collectable / 180 not. Real full
  snapshot (row 10): 1,359 targeted → 93 collected · 62 empty · 1 partial · 425 skipped · 778
  failed; 50,176 objects; 34 MB `object_json`; largest object 173,626 B; mean 701 B;
  `readableFraction` **0.114** — 11.4% of the model is readable on a real consented tenant.
  Fan-out: 3 resource types hold 94% of all objects (`/applicationTemplates` 39,089);
  `/schemaExtensions` is `partial`/`budget_exhausted` at the 50-page cap with 4,949 objects.
  Diff 9: 155 compared / 1,203 not comparable / 340 changes, 273 of them (80%) from one resource
  type. Two of the four real diffs have `changesTotal: 0` **and** `comparableFraction: 0`.

  **#1851's three absorbed requirements answered from code:** collection coverage stated per
  resource type (§4.1–4.3, superseding #1848's stale "62 of 1,539"); drift domains **5 of 18 have
  a producer, 13 do not** (#1794's number is still current; `drift_events` is empty);
  retention **does not exist** and cadence is **manual-trigger-only** (no scheduler; the only
  automation is the 6-hour `sweepAbandonedSnapshots`).

  **Verification, honestly.** All sample payloads in §5/§6 were *executed* — the real
  `config-state-views.ts` functions bundled with esbuild and run against the real `DATABASE_URL`,
  output captured verbatim. `POST /collections` is live-verified via real `wf_runs` row 34415.
  **Not** verified live: no HTTP request against a running api-server — ports 3000/4000/5000/5173/
  8080/8787 all refused connection, no local dev server was up. HTTP status codes and the wire
  envelopes are therefore read from route source, and §9 says so with that exact blocker.

  **Findings filed** (all milestone 5, board status "AI Batter Up"):
  - **#2112** `artifacts/msp-console` does not exist — operator surface has no app package → parented #1571
  - **#2113** Two incompatible error envelopes on the same endpoints (`apiError` object vs `requireAuth` bare string) → #1096
  - **#2114** No retention policy for configuration snapshots — 34 MB / 50,176 rows per snapshot, unbounded → #1096
  - **#2115** `unknown_error` is the largest failure class (304 of 778) with 404/400/401/403 signal unused → #1096

  The double-`/api`-prefix admin-route gap was **not** re-filed — already open as **#2099**.

  No code, schema or migration changed: this issue is documentation-only by design, and nothing in
  the extraction turned up a missing column or endpoint that the pack needed built. Working tree
  clean; scratch probe script removed before the commit; no pre-existing dirty files found on
  arrival.
