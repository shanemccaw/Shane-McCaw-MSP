# #3121 — Concurrent Shopping builds (#3109, #3111) both add list_items.price_cents

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** (investigation only, no code change)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — investigate whether #3109/#3111's independent `price_cents` collision (filed as this issue) still exists on `main`, both being now CLOSED.
- 2026-09-07T00:05:00Z ✅ DONE — Confirmed on `main` the collision was already reconciled by #3111 itself before merge: `020_barcode_scan.sql` (#3109) owns `list_items.price_cents`/`price_source`/`priced_at`; `023_shopping_budget.sql` (#3111)'s own header explicitly cites #3109's migration and instead adds a separate `lists.budget_cents` column — zero column overlap, no dual ownership. `web/shanes-life/src/core/lists.mjs` wires both together correctly (running total from item `price_cents`, over-budget check from list `budget_cents`). No fix needed; nothing left to build. No code changed, no migration run.
