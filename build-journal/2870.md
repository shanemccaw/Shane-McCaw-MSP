# #2870 — Orphaned test manifest `test-manifests/portal/portal-v2-security-plan.json`

- **Status:** ✅ DONE 2026-09-04T20:11:00Z
- **Scope:** platform

## Log
- 2026-09-04T20:02:23Z — Starting: deleting the orphaned manifest (`test-manifests/portal/portal-v2-security-plan.json`) and its registration in `test-manifests/_regression-suite.json:81`, per the issue body's own recommendation. Confirmed via grep: `artifacts/portal-v2/*security-plan*` no longer exists, and `GET /api/portal/security-plan` no longer returns `$.plan.*` (now `{ assembledPlan }`), so the manifest's assertions are dead on both counts. No replacement page exists yet under #1495 to write a real manifest against.
- 2026-09-04T20:11:00Z — ✅ DONE — Deleted `test-manifests/portal/portal-v2-security-plan.json` and removed its `test-manifests/_regression-suite.json:81` registration in commit `823ff6775` (rebased and pushed to `main` as `0d5b5e57`). Verified via `node scripts/dev-server/verify-branch-merged.mjs`: OK, merged. Remaining references to `portal-v2-security-plan` are all historical (past bookends, `PLATFORM_BUILD.md`, `docs/security-plan-contract-pack.md`, `PORTAL_V2_PARALLEL_PLAN.md`) and left untouched as archival record — not live references. `git status --porcelain` clean.
