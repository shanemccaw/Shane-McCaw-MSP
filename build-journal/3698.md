# #3698 — RenderQueue leaks Forever mascot animations on every card, every tick

- **Status:** ✅ DONE 2026-09-11T22:38:48Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** e80434a59 (fix + probe)

## Log
- 2026-09-11T20:59:21Z ⏳ IN FLIGHT — investigating `Controls/BuildQueuePanel.xaml.cs` `RenderQueue()` full-list `QueueCardsHost.Children.Clear()` + rebuild and the Forever mascot animations it discards; plan: generalize #3689's `KeyedSlotCardHost` keyed-diff reuse to the queue card list (explicit `Controller.Stop()` on every discarded card), measured before/after with the `scripts/probes/matrix-pulse-leak` approach.
- 2026-09-11T22:38:48Z ✅ DONE — Shipped the **full keyed-diff host (option 1), not the interim stop-before-clear fallback.**

  **What shipped (e80434a59):**
  - `Controls/KeyedCardPool.cs` (new) — identity-keyed generalization of #3689's positional `KeyedSlotCardHost`, for a card list that reorders, grows, shrinks and nests. `BeginPass`/`Acquire`/`SyncChildren`/`EndPass`; an element whose key is unchanged is reused with its animation still running, and every element dropped has its owned clocks stopped first.
  - `KeyedSlotCardHost` — added a `BeginOwnedAnimation` overload for an `IAnimatable` target (the mascot's `TranslateTransform` and `DropShadowEffect`, not just the card), and `StopOwnedAnimations(root, spare)` which walks a logical subtree. Exactly the two gaps #3691 predicted would need closing.
  - `RenderQueue` acquires its cards, build-set containers and offline banner from the pool; the only `QueueCardsHost.Children.Clear()` left is the broad-view early return, now preceded by `RetireAll()`. `QueueCardKey` covers every input `BuildQueueCard` draws from, including #3700's `IsAskingShane` (landed upstream mid-build); `BlockerGhostInputs` is now shared by the ghost card and the key so they cannot disagree. Tag, tooltip and context menu refresh on every reuse, and the card click resolves its current node, so nothing a click or hover reads goes stale.
  - `scripts/probes/queue-card-leak/` (new, kept diagnostic) — standalone probe, never launches BuildConsole, compiles the real pool + host, modes `legacy`/`interim`/`fixed`.

  **Measured (300s, 20 renders/s, 42 cards = 4795 renders ≈ 4.5h of app time at one render per 5s poll):**

  | | legacy | fixed |
  |---|---|---|
  | discarded still animating | 135,324 | 0 |
  | unchanged cards rebuilt | 135,023 | 0 |
  | cards created | 135,366 | 486 |
  | live clocks (expected) | 224 (62) | 60 (61) |
  | CPU, one core | 102% | 12.5% |
  | render fps | 26 | 75 |
  | allocation | 50.4 MB/s | 2.2 MB/s |
  | gen2 GCs | 692 | 11 |

  Tree order/nesting checked against the intended render every tick: `layoutMismatchTicks` 0, `cardsMissingFloat` 0. Re-parenting a reused card **does** raise Unloaded then Loaded (148 over 300s), so the probe also models #2692's progress-row subscribe-on-Loaded/unsubscribe-on-Unloaded pattern: `subscriptionLostCards` 0, and one `ProgressChanged` invoke reaches exactly the 42 cards on screen — no lost and no doubled subscription. `dotnet build` of BuildConsole: 0 errors, no new warnings in touched files (the two CS0162 "unreachable code" in BuildQueuePanel are upstream #3699's `BuildMatrixDisabled` const).

  **Honest gaps:**
  - The probe's `interim` mode (stop-before-clear, the fallback) was built but not run — the shipped path is `fixed`, so legacy→fixed is the real before/after. `--mode interim` is there if the fallback ever needs measuring.
  - No live click-through in the running app: per the standing rule a session must not launch BuildConsole.exe to verify (its single-instance mutex can courier into Shane's live instance). Visual confirmation in the real app is Shane's.
  - In the probe the legacy live-clock count stays bounded (~200–256) rather than growing without limit, because its own churn forces a gen2 GC about every 0.4s, which collects the discarded cards. The real app collects far less often, so discarded clocks pile up for longer between collections — the height of the pile depends on GC timing, which is why the flat-by-construction fixed result matters more than the legacy ceiling.
  - Queue card mascots still animate while the panel is hidden (not a leak — a bounded, live set — and out of scope here).

  **Findings filed:** #3714 (Batter Up + AI Batter Up clear their row list without stopping the same Forever mascot clocks — same class, third surface, the check #3691 left open), parented under #3706, labelled `bug`, board status AI Batter Up.

  **Board/structure notes:** #3691 (filed from #3689's build) reports this same bug; e80434a59 fixes it, and I commented there rather than closing it — closing is Shane's. #1788 (Feature: Build Queue Panel) is at GitHub's hard 100-sub-issue cap (`HTTP 422`), which is why #3698 had no parent despite its body naming #1788; #3698, #3691 and #3714 are now parented under #3706 (Feature: Build Queue Panel — Part 2), the real overflow Feature. Nothing was dirty in the worktree on arrival, and nothing of mine is left uncommitted.
