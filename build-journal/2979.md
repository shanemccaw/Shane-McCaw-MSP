# #2979 — Shared checkout dependency linking gap + store-doctor false-negative

- **Status:** ✅ DONE 2026-09-13T04:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 550404d48 (bookend IN FLIGHT), no further code commit needed — see below

## Log
- 2026-09-13T04:05:00Z ⏳ IN FLIGHT — Re-verifying live whether the shared main checkout
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) still has unlinked workspace packages
  (originally reported 2026-09-06, held pending an upstream BuildConsole regression fix per
  the issue's own 2026-09-07 comment) and whether `store-doctor.mjs` still false-negatives on
  that state.
- 2026-09-13T04:20:00Z ✅ DONE — **Both halves of this finding are already resolved on `main`,
  by other work landed since the original 2026-09-06/07 report; nothing left for this build to
  code-fix. Re-verified live rather than assumed:**

  **1. Linking gap — resolved.** Every workspace package host in the shared checkout
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) now has real `node_modules` entries
  (`artifacts/api-server`: 33, `lib/db`: 7, `artifacts/admin-panel`: 47, `artifacts/msp-portal`:
  3, etc. — spot-checked all 21 hosts). Only `lib/debug-console` has 0 entries, and that's
  correct — its `package.json` declares zero dependencies. Live re-run:
  ```
  $ cd artifacts/api-server && npx tsc --noEmit
  src/routes/auth-impersonation.test.ts(53,24): error TS2352: ...
  src/routes/portal-checkout.test.ts(549,65): error TS2345: ...
  ```
  This is tsc actually running and reporting real type errors — a categorically different
  result from the issue's cited `TS2688: Cannot find type definition file for 'node'`
  config-level abort. Also re-ran clean against `admin-panel` (`npx tsc --noEmit`, zero
  output/errors).
  ```
  $ cd artifacts/api-server && npx vitest run src/lib/retainer-hours.test.ts
  Test Files  1 passed (1)
       Tests  20 passed (20)
  ```
  vitest genuinely executes and passes (previously: `ERR_MODULE_NOT_FOUND: Cannot find
  package 'vitest'`). Also ran `lib/db`'s vitest suite: it executes (93 passed / 31 skipped
  across 7 files); 3 files hit a `SASL: client password must be a string` DB-auth error,
  which is a shell artifact of this session's own bash not exporting `DATABASE_URL` from
  `.env.local` correctly (identical false-negative shape to the one #2973's own bookend
  called out and correctly did not file), not an infra or code regression — not filed.

  **2. `store-doctor.mjs` false-negative — already fixed, commit `562a5fae2`
  ("store-doctor: detect MISSING node_modules trees (Git #3646)"), already on `main`
  before this build started.** It adds exactly the "declared dependency has no link" check
  this issue asked for (`scanMissingTrees()` — flags a package with real deps whose
  `node_modules` is absent *or* present-but-empty). Verified genuinely live, not just by
  reading the diff:
  ```
  $ node scripts/dev-server/store-doctor.mjs
  store-doctor — scanned C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP
    hosts: 21, links checked: 2565, .bin files checked: 162
    CLEAN — no foreign, dangling, poisoned or missing entries.
  ```
  Built two synthetic fixtures (`--root <temp dir>`, no shared-checkout state touched) to
  confirm the check actually fires rather than just reading "clean" by luck:
  - a package with real deps and **no** `node_modules` dir → `missingTrees: [{ reason:
    "node_modules directory does not exist" }], clean: false`
  - a package with real deps and an **empty** `node_modules` dir → `missingTrees: [{ reason:
    "node_modules exists but has no real package entries" }], clean: false`
  Both correctly reported `clean: false` with the real reason. `store-doctor` reporting
  CLEAN on the real checkout above is therefore a true clean, not the false-negative the
  issue reported.

  **Working tree:** `git status --porcelain` clean apart from this bookend's own edits.
  No other file touched — the linking repair and the store-doctor fix both predate this
  session; this build's own contribution is the live re-verification recorded here.
  `verify-branch-merged.mjs`-equivalent: pushed directly to `origin/main` (rebase+push,
  see commit `ae2c2551b` for the IN FLIGHT bookend landing) — no separate branch to check.

  **No findings filed.** Nothing genuinely broken was found beyond the one shell-env
  artifact noted above (not a real bug, same shape as #2973's own documented non-finding).
