# #3866 — 🔒/🔓 Epic-lock toggle on Build Sets rollup Epic group headers

- **Status:** ✅ DONE 2026-09-12T23:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** f2a37e99e (implementation), ea1f2aeb7 (IN FLIGHT bookend)

## Log
- 2026-09-12T23:08:38Z ⏳ IN FLIGHT — read issue #3866 (via REST API, GraphQL was rate-limited) + sibling #3865; investigated `BuildEpicGroupHeader`, `BuildSetEpicGroupKey`, `_buildSetEpics`, `BuildSetPriorityStore`. Implementing `EpicPriorityStore` (mirrors `BuildSetPriorityStore`'s persistence pattern) + the 🔒/🔓 toggle button + wiring it into the existing render/refresh cycle (no new timer). About to build and verify.
- 2026-09-12T23:20:00Z ✅ DONE — Added `desktop/BuildConsole/Services/EpicPriorityStore.cs` (static, %APPDATA%/BuildConsole-rooted JSON, mirrors `BuildSetPriorityStore` exactly): tracks locked epic numbers plus, per locked epic, the real build-set names *that lock itself* auto-marked Priority (never one Shane marked by hand), via `Lock`/`Unlock`/`IsLocked`/`ApplyLockedEpics`. Wired a 🔒/🔓 toggle button into `BuildEpicGroupHeader` (`BuildQueuePanel.xaml.cs`), rendered only when `key.EpicNumber.HasValue` — restructured the header's early-return so the button still shows even when a group has zero unsent/needs-attention items. Click locks/unlocks and drives the existing `BuildSetPriorityStore.SetPriority` unchanged (no second flag). `RollupEpicHeaderKey` now carries `IsLocked` so the pooled header rebuilds (button visual stays current) whenever lock state flips, not just after a click on that same header. "Stay locked as new build sets appear" piggybacks on the existing `_buildSetEpics` recompute in the refresh loop (`EpicPriorityStore.ApplyLockedEpics(_buildSetEpics)` called right after `ResolveBuildSetEpicsAsync`) — no new timer. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `verify-branch-merged.mjs` confirms `f2a37e99e` is an ancestor of `origin/main`. `git status --porcelain` clean. No fixture/hardcoded data — this is pure client-side WPF state persisted to a local JSON file, same as the existing store it mirrors. No findings filed — no genuine bug/gap discovered outside this issue's own scope.
