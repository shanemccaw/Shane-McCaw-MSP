# #4005 — Data Rights and Privacy: wire the real Design into artifacts/portal

- **Status:** ⏳ IN FLIGHT 2026-09-14T04:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14

## Log
- 2026-09-14T04:10:00Z ⏳ IN FLIGHT — pulled #4005, #1652 (real 2026-09-14 reversal:
  build as its own standalone page, not folded into Account Security), and
  #1604/#1595 (both CLOSED). Confirmed via the real code
  (`account-security.tsx` + `useAccountSecurityLive.ts`) that Account Security
  already wires both `GET /api/portal/data-export` and
  `POST /api/portal/deletion-request` under its own "Your data" section
  (#2996) — this build adds a second, standalone surface calling the same two
  real endpoints, exactly as `Data Rights and Privacy.dc.html` itself
  describes (§5: "one deletion path, two doors"). Read the `.dc.html` export
  and `docs/data-rights-and-privacy-contract-pack.md` (#2549). Building the
  page + live hook now.
