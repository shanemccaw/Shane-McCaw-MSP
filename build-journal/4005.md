# #4005 — Data Rights and Privacy: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 54488b52e

## Log
- 2026-09-14T04:10:00Z ⏳ IN FLIGHT — pulled #4005, #1652 (real 2026-09-14 reversal:
  build as its own standalone page, not folded into Account Security), and
  #1604/#1595 (both CLOSED). Confirmed via the real code
  (`account-security.tsx` + `useAccountSecurityLive.ts`) that Account Security
  already wires both `GET /api/portal/data-export` and
  `POST /api/portal/deletion-request` under its own "Your data" section
  (#2996) — this build adds a second, standalone surface calling the same two
  real endpoints, exactly as `Data Rights and Privacy.dc.html` itself
  describes (§5: "one deletion path, two doors"). Read the `.dc.html` export
  and `docs/data-rights-and-privacy-contract-pack.md` (#2549). Building the
  page + live hook now.
- 2026-09-14T04:35:00Z ✅ DONE — Built the standalone "Privacy and your data"
  page (`artifacts/portal/src/pages/data-rights-and-privacy.tsx`) and its live
  hook (`artifacts/portal/src/components/data-rights-privacy/useDataRightsPrivacyLive.ts`),
  recreated from `Design/portal/design_handoff_full_site/screens/Data Rights
  and Privacy.dc.html` against the real contract pack. Wires the same two
  real endpoints Account Security already calls
  (`GET /api/portal/data-export`, `POST /api/portal/deletion-request`) — a
  deliberate overlap per the design's own §5 ("one deletion path, two
  doors"); no fixture/hardcoded data, all export archive-contents and
  "what this page deliberately does not do" text is literal copy from the
  design describing the fixed real contract, not fabricated values.
  Registered route `/privacy` in `App.tsx` and a "Privacy and your data" row
  in `UserMenu.tsx` next to Account Security. Checked #7 of the contract
  pack's filed finding (`docs/runbooks/data-subject-rights.md` "does not
  exist") — that file now exists (121 real lines), so the finding is already
  stale/resolved; nothing new to file.
  Verification: `pnpm --filter @workspace/portal exec tsc --noEmit` clean;
  `pnpm --filter @workspace/portal build` clean (production bundle built).
  Resolved a real rebase conflict in `UserMenu.tsx` against a concurrently
  landed #4002 (Offboarding "Leaving" nav row) by keeping both additions.
  `verify-branch-merged.mjs` confirms `agent/4005-q2686` (54488b52e) is an
  ancestor of `origin/main`; working tree clean.
