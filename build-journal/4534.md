# #4534 — identity:ca-device-compliance only tests policy existence

- **Status:** ✅ DONE 2026-09-17T18:33:19Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** a75fc98ad

## Log
- 2026-09-17T15:00:20Z ⏳ IN FLIGHT — reused #4512's mechanism: countWhere over real policy state (enabled + grantControls.builtInControls contains compliantDevice) for `identity:ca-device-compliance`, required_service_plans = ["AAD_PREMIUM","AAD_PREMIUM_P2"] for a license gap instead of a warning, no Security Defaults gate (doesn't cover device compliance). Migration file written; about to run it locally and live-verify against testbed tenant 2080.
- 2026-09-17T18:33:19Z ✅ DONE — Migration `lib/db/migrations/manual/2026-09-17-ca-device-compliance-real-state-4534.sql` run locally (monitor_checks + signal_derivation_rules row 2779 updated, schema_version bumped). Live-verified read-only on testbed tenant 2080 (id 2080, tenant_id c4c814d4-3afe-441e-9145-62461d0a4fd3) via `src/scripts/verify-ca-device-compliance-4534.ts`: as-stored now returns `status=license_gap "Requires Microsoft Entra ID P1 or P2"` (tenant has 0 CA policies, AAD_PREMIUM=false, AAD_PREMIUM_P2=false, confirming the old code would have shown a warning here); the pre-#4534 definition (exists over grantControls, no license prerequisite) reproduces the old defect (`status=ok severity=warning`). Tenant 2080 currently has zero live CA policies, so the "a report-only/disabled policy no longer falsely clears the check" case couldn't be exercised against real Graph data without creating a policy against the real testbed/production tenant — covered instead with 6 new unit tests in `monitor-executor.test.ts` (`describe("Conditional Access device compliance check — real policy state (#4534)")`), all passing, plus the full monitor-executor suite (227/227) and `tsc --noEmit` (clean). `docs/signals.json` source_key updated to match. Filed #4556 (Intune all-of gap in `required_service_plans`, any-of only) as a sibling sub-issue of #1489, labeled `bug`, board status "AI Batter Up". Commit a75fc98ad pushed to origin/main directly (rebased twice onto concurrent pushes). Branch verified merged via `verify-branch-merged.mjs`; working tree clean.
