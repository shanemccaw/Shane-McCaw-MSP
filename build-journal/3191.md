# #3191 — Feature: Meds -- Round 2 visual rebuild (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** ef7107e46 (real work), ad45fed29 (merge into main; pushed to `origin/main`)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3191 (sub-issue of closed Feature #3183, itself
  under Feature #3224, under Epic #3086) and the real design file
  `web/shanes-life/Design/design_handoff_shanes_life/Shanes Life 07 - Meds.dc.html`. Confirmed
  `viewMeds()`/`medBatchCard()`/`refillsSection()` in `public/app.js` already carry real
  Round 2 styling for the batch cards themselves (per the issue's own note), so remaining scope
  is: (1) refills section — heading style, per-tier icon, and the missing "Call pharmacy" action
  the design shows next to "Ordered it"; (2) empty state; (3) page chrome — Meds still uses the
  generic `#app-header` (title + Sign out) instead of its own native back/title header like
  Today (#3174) and Shopping (#3178) already have. Confirmed via local `finances` DB
  (`psql -h localhost -p 5432 -U postgres -d finances`) that `medications` had no phone-number
  column at all — the design's "Call pharmacy" button has nowhere real to point. Added
  `migrations/054_medication_pharmacy_phone.sql` (`pharmacy_phone text`, nullable) and ran it
  myself via `npm run migrate` against the local `finances` DB (`applied=1
  054_medication_pharmacy_phone.sql`).
- 2026-09-08T18:10:00Z ✅ DONE — implemented and pushed (ef7107e46, merged into `main` as
  ad45fed29): `medsHeader()`/`.meds-header` gives Meds its own native chrome (back "Today" /
  centered title / spacer), replacing the generic `#app-header` fallback for this route.
  `refillsSection()`/`refillNeedsYouCard()` now carry the design's own tinted icon boxes
  (phone/amber for "Needs you", check/green for "Handled automatically"), a real "Call
  pharmacy" `tel:` link (via `.pill-row`/`pillButton`, the same real component Today's Next
  card already uses) that only ever renders when `pharmacy_phone` is actually set, and a real
  "next delivery `<date>`" line when every auto-refill item shares one real date. Added a real
  pill icon to each batch card's item rows. Along the way found and fixed a real, confirmed
  CSS gap unrelated to any one room: bare `.title`/`.meta` (28 and 45 call sites across the
  whole app) had no base rule outside `.tile`'s own narrower scope at all -- added the design
  README's own general type scale (15/600 row titles, 13 muted sublines) as the real base,
  plus a `.med-batch-title` override for the batch header's own literal 17/700. Empty state
  already used the shared `empty()` helper -- no change needed there.
  **Verified**: real end-to-end round-trip against the local `finances` DB with a disposable
  passkey-authenticated user (`bin/verify-3191.mjs`, deleted after) -- create/patch/GET through
  the real HTTP API confirm `pharmacy_phone` round-trips through create, update and
  `get_medications`, `needsYou`/`handled` carry the right shape for the UI logic, and the
  batch grouping still merges correctly. Deleted the disposable user afterward; no residue in
  the shared database. `node --check` clean on every touched `.mjs`/`.js` file, before and
  after resolving a real merge conflict against concurrent #3190/#3192/#3193 work on the same
  `hasOwnHeader` line in `render()`.
  **Filed #3257** (parented to #3183, the Feature-tier issue #3191 is itself a sub-issue of):
  the batch cards don't yet collapse a not-yet-due batch into the design's dimmed "later" row
  (real product-logic decision -- how "current time-of-day" is derived from a free-text batch
  name -- explicitly out of #3191's own stated remaining scope).
  Verified branch is a real ancestor of `origin/main`
  (`scripts/dev-server/verify-branch-merged.mjs`); working tree clean.
