# #2888 — Move ShanesSurvival desktop app into this repo; audit real Link Account hang

- **Status:** ✅ DONE 2026-09-04
- **Scope:** desktop
- **Started:** 2026-09-04
- **Commit(s):** 1f7cf0b1d (move + fix), 39ebd38ad (IN FLIGHT bookend)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — Copying the real ShanesSurvival.App codebase (its own separate local repo at `C:\Source\ShanesSurvival`, 5 real commits) into `desktop/ShanesSurvival/` here, for real tracking convenience — this is Shane's personal project, not MSP business code. Source repo's `.gitignore` confirmed to genuinely exclude `appsettings.Local.json`/`settings.local.json`/`*.env`; no such file exists on disk in the source checkout either (real settings live at `%AppData%\ShanesSurvival\settings.json`, outside any repo). Copied tree matches `git ls-files` from the source repo exactly — no `bin/`/`obj/`, no stray files, no credentials. `dotnet build ShanesSurvival.sln` from the new location: clean, 0 warnings / 0 errors — confirms `MigrationRunner.FindMigrationsDirectory()`'s walk-up-from-exe logic still finds `migrations/` correctly at the new path. Source repo `C:\Source\ShanesSurvival` left untouched (now a redundant local copy Shane can clean up himself later). Also auditing the real Link Account hang (button opens WebView2 window, hangs on "Loading…" indefinitely, real production Plaid keys confirmed configured) before closing this out.
- 2026-09-04T00:00:00Z ✅ DONE — Real-audited the Plaid Link path top to bottom (PlaidClient, PlaidLinkService, PlaidLinkWindow). `CreateLinkTokenAsync` already has a 30s HttpClient timeout and turns every failure into a visible Result — ruled out as the hang. Found two real gaps matching the "no timeout, silent failure" bug class already fixed once in the Settings connection-test flow: (1) `PlaidLinkWindow`'s WebView2 startup (`CoreWebView2Environment.CreateAsync` + `EnsureCoreWebView2Async`) had no timeout — a hang there (e.g. broken WebView2 Runtime) left the dialog blank forever with nothing on screen; (2) the hosted Link HTML loads `link-initialize.js` from `cdn.plaid.com` with no `onerror` handler and no watchdog — a network stall there leaves "Loading…" on screen indefinitely with zero error surfaced. Fixed both: added a 20s timeout around WebView2 startup and a 20s client-side JS watchdog + `onerror` on the CDN script tag, both now surfacing a specific, visible error instead of hanging silently. Could NOT verify live whether this was Shane's actual root cause (no way to drive Plaid's real hosted OAuth UI or reproduce his exact network/WebView2-runtime environment from here) — documented honestly in the commit. What's guaranteed either way: the hang can no longer be silent; the next report (if any) will say exactly which of the two failure points it is. Verification: `dotnet build ShanesSurvival.sln` clean (0 warn / 0 err) both before and after the fix.
