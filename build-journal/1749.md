# #1749 — Pillar pages: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-06T19:24:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** a721b1de4

## Log
- 2026-09-06T19:05:29Z ⏳ IN FLIGHT — Design export landed (`Design/portal/design_handoff_full_site/screens/Pillar Pages.dc.html`, no contract pack). Wiring pillar summary cards + scores against real `GET /api/portal/pillars` (`pillar-summary-stats.ts`). Drill-down explicitly deferred — #1621 still blocked_by #2923 for finding-drilldown evidence, per dispatch comment.
- 2026-09-06T19:24:00Z ✅ DONE — Built the six real pillar pages (`artifacts/portal/src/pages/pillar.tsx`, route `/pillars/:pillar`), reached from the shell's `PillarTabStrip` (previously routed every tab through `/coming-soon` — now routes to the real page). New `components/pillar/{types,usePillarPage,pillarDisplay}.ts` mirror the real `GET /api/portal/pillars` wire shape (`pillar-summary-stats.ts`'s `PillarSummaryPayload`/`PillarSummaryCard` — already fully built and live, not something this session had to add). Rendered: pillar identity header, real score + honest evaluation state (scored/insufficient_data/not_evaluated with its real reason text), real trend delta derived from the real trend series, real stat tiles with honest per-stat unavailable reasons (never a zero standing in for "not measured"), real ranked findings list, real license-gap upgrade callout, and a real "scanned with N packages / M checks" honesty line. `PortalShell.tsx`'s breadcrumb updated for the new route. No fixture data anywhere — confirmed the design export's own `renderVals()` numbers are the design tool's demo data (not a real contract, per the established `mschanges`-class trap) and did not carry any of them into the page.

  Deliberately NOT wired, per the dispatch's own scope: the design's full per-check "block" breakdown (dozens of per-check tiles/tiers/history) and the click-a-finding-for-full-breakdown + SOP/Runbook remediation-offer flow. #1621's own body leaves "what is a block" and which of the four remediation vehicles a finding offers as open architecture questions not yet settled in chat — building either would mean inventing an answer to a product decision, not filling a missing-backend gap.

  **Housekeeping finding, fixed in place (not filed as a new issue):** #2923 (the finding-drilldown evidence-array work #1621's own dispatch cited as the blocker) closed 2026-09-05, a day before this build's dispatch comment — but the `blocked_by` edge from #1621 to it was still live. Removed that stale edge (`DELETE .../1621/dependencies/blocked_by/<2923's id>`) and left an explanatory comment on #1621 (Git #1987's own rule: a stale edge pointing at a closed issue silently misreports the board). This does NOT reopen drill-down scope — the real gate on that work is the still-unanswered "what is a block" architecture question, not the evidence data, which is now genuinely available.

  Verification: `tsc -p tsconfig.json --noEmit` clean on `artifacts/portal` (including the `pretypecheck` build of `lib/api-client-react`/`lib/copilot-scan-scene`). `vite build` clean (2143 modules, no errors — pre-existing sourcemap warnings on 3 unrelated shadcn files only). New test manifest `test-manifests/portal/pillar-pages.json` written and registered in `_regression-suite.json` (apiTests proving the real `/api/portal/pillars` contract by index against the fixed `PILLAR_SUMMARY_KEYS` order; uiSteps driving login → Governance pillar page → tab-strip navigation to Security pillar page). **Could not live-execute via `shaneapp://runTest` this session** — a real, statable reason: the `mcp__shane-msp__*` tool set exposed to this session this time carries no `runTest`/`executeSql`/`reportProgress` tool (confirmed via ToolSearch across several query phrasings), and separately the local dev-server process was also down on port 8080 at build time (`node scripts/dev-server/status.mjs` showed `pid 67456 DEAD port 8080`). Both are genuine unreachability, not a scope gap — matches the same honestly-documented gap on prior bookends (#1398, #1399, #1401) when the same MCP connection was unavailable.

  Rebased twice onto a fast-moving `origin/main` (concurrent sessions landed #3004/#3014 and their own new portal pages/manifest entries mid-build) and hand-merged the resulting conflicts in `App.tsx` and `_regression-suite.json` — both were pure list-insertion conflicts, no logic lost on either side. Pushed directly to `main` (repo convention) at `a721b1de4`. `verify-branch-merged.mjs` confirms `agent/1749-q1772` (a721b1de4) is a genuine ancestor of `origin/main`. `git status --porcelain` clean.
