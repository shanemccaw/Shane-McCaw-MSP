# #4347 — Feature: Console/Automation workspace (Script Library, Runbooks, CSV viewer, Hold Windows, 3-entry-point script execution)

- **Status:** ✅ DONE 2026-09-16T05:06:22Z
- **Scope:** platform (MyArchitect desktop app, shanemccaw/MyArchitect repo)
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/MyArchitect@ad975c7

## Log
- 2026-09-16T04:52:56Z ⏳ IN FLIGHT — Confirmed blocker #4344 (Core shell rearchitecture) is CLOSED with state_reason "completed", no open blocked_by edges remain on #4347. Provisioned isolated worktree C:\wt\myarchitect-4347 off shanemccaw/MyArchitect (origin/main @ 98ed8f06e). Beginning investigation: locate Console/Automation workspace shell, existing Script/Runbook/Hold Window/History real implementations to wire, per issue scope.
- 2026-09-16T05:06:22Z ✅ DONE — Investigation (via subagent) confirmed: CSV Viewer (#4294), Runbooks/Hold Windows (#3479), console History/Report (#4302) were all already real and fully wired into the Console/Automation workspace from #4344 — no changes needed. Script Library (#4291) was still shown globally in the shared LeftReferencePanel regardless of active workspace; scoped it to Console/Automation only via new `LeftReferencePanel.SetScriptLibrarySectionVisible(bool)`, called from `MainWindow.SwitchWorkspace` on every workspace change (default Collapsed at startup since Home is the initial workspace). Of the three real "Run script" entry points: ad-hoc (Script Library row) was already fully wired to the real `POST /launch-control/execute` path; the other two were confirmed genuinely missing. Added both as pure UI wiring (no schema change — reused the existing real `LaunchControlAction` catalog and `OpenScriptLibraryRecord`/`RunScriptLibraryActionAsync` execution path) via a new `OpenRunScriptPickerAsync` helper: (1) a "Run a script…" action on the remediation tracker step record workspace (`OpenTrackerStepRecord`), scoped to that step's own real customerId; (2) a "Run a script…" action on the support ticket record workspace (`OpenSupportTicketRecordAsync`), scoped to the currently selected tenant since tickets carry no customerId client-side. Updated `UI_RULES.md` §1/§2/§4 to describe the scoped secondary nav and document the three real entry points. Verified: `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors, both before and after rebasing onto concurrent upstream work (#4345/#4348 landed mid-session). Committed in isolated worktree C:\wt\myarchitect-4347 (branch agent/myarchitect-4347), rebased onto origin/main, pushed directly to shanemccaw/MyArchitect@main as ad975c7 (verified `git merge-base --is-ancestor HEAD origin/main`). No fixture/hardcoded data anywhere — all rows come from the real LaunchControlAction catalog. `git status --porcelain` clean in both worktrees before this bookend commit.
