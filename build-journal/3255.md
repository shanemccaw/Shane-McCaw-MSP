# #3255 — Cook mode in-step timer (design turn t2, 2a/2b)

- **Status:** ✅ DONE 2026-09-13T07:00:00Z
- **Scope:** platform

## Log
- 2026-09-13T05:55:00Z — Session started. Investigating `viewCook()` in
  `web/shanes-life/public/app.js` and design spec
  `web/shanes-life/Design/design_handoff_shanes_life/Shanes Life 05 -
  Recipes.dc.html` turn t2 (id 2a/2b) for the in-step timer chip + full-screen
  "Timer done" state. Found the real, current design source is that dir's own
  `Shanes Life - First Slice Prototype.dc.html` (per its CLAUDE.md, "the
  prototype wins") -- it specs the identical feature (`showCook`/`timerOn`
  block), confirming the gap is real and current, not stale.
- 2026-09-13T06:10:00Z — Found the real building block already shipped for a
  different feature: Git #3307's standalone `timers` entity
  (`src/core/timers.mjs`, migration 069) is a real, server-side timer with
  real web-push delivery -- exactly what "the app itself keeps it alive and
  fires a critical alert even if the phone is locked" needs, and a stronger
  precedent than the issue's own suggested `mealSession`-beep-only fallback.
  Built Cook mode's in-step timer on top of that entity instead of a second
  client-only countdown.
- 2026-09-13T06:45:00Z — Implemented: `recipes.mjs` normaliseSteps now stores
  a step's own optional `timer: {label, minutes}` (previously silently
  dropped); `push_recipes` MCP schema documents it; Cook mode
  (`public/app.js`) gained the "Start a N-minute timer" affordance, the
  pinned running chip (+1 min/Stop), the full-screen "Timer done" overlay
  (Stop / "Give it 1 more minute"), and reload reconciliation. Renamed
  `playMealBeep`/`stopMealBeep` -> `playAlarmBeep`/`stopAlarmBeep` (shared,
  not meal-specific).
- 2026-09-13T06:55:00Z — Verified against the real local Postgres `finances`
  DB (not `shanemccawmsp` -- Shane's Life is a standalone app, its own DB):
  a direct `createRecipe()` round-trip confirmed timer
  normalise/validate/store/read (accepted `{label:"Beef",minutes:7}`,
  rejected `minutes:0`), and the app's own `bin/check.mjs` end-to-end suite
  passed 241/245 against a live `node src/server.mjs` (the 4 failures are
  pre-existing, unrelated `SL_VAULT_KEY` gaps, not touched by this build).
  Rebased through two concurrent pushes to `main` (one a real content
  conflict in `viewCook`'s header from a concurrent #3256 build,
  cross-checked mealChipStrip/cookTimerChip placement was preserved), then
  pushed clean.
- 2026-09-13T07:00:00Z — ✅ DONE. Commit `0509bbb2f` (rebased/merged as
  `493cc8a0b` on `origin/main`). Working tree clean, branch verified merged.
