# #1878 — Adopted build composer: make Resume Session directly actionable from the pane

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Read #1839's landed diff (dec5ee4ac) and the current
  `AdoptedReadOnly` composer block in `ChatSessionPane.xaml` (~line 624) plus the
  "▶ Resume Session (crash recovery)" context-menu handler in `BuildQueuePanel.xaml.cs`
  (actual current line ~4187, not ~3064 — the issue's line ref has drifted). Adding a
  one-click Resume button to the AdoptedReadOnly composer, reusing the exact same
  `QueueBuildAsync(..., resumeSessionId, ...)` call. Also found that BOTH
  `BuildWatchWindow.SendSlotInput` and `GitDetailView.HandleBuildSend` already implement
  a working "typed text becomes a real --resume follow-up" continuation path for
  Terminal-mode builds — investigating whether that's reachable from AdoptedReadOnly too.
