# #4202 — "Waiting On You" panel + CLAUDE.md convention fix

- **Status:** ✅ DONE 2026-09-15T02:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** fea4934a9

## Log
- 2026-09-15T02:05:00Z ⏳ IN FLIGHT — Pulled #4202 body + comment (single BUILD comment, matches dispatch, no supersession). Starting: (1) CLAUDE.md "Shane To-Do" section addition, (2) new "Waiting On You" document tab reusing AiBatterUpQueueService's board-column query and GitHubApiClient's label search.
- 2026-09-15T02:55:00Z ✅ DONE — Shipped both real parts. (1) CLAUDE.md's "Shane To-Do" label
  section: added an explicit "commission a Design export" case (#4009/#3894/#3963/#2825/#2866
  precedent), plus a clarifying block distinguishing the `Shane To-Do` label (action) from the
  real "Ask Shane" board status (option id `404998bb`, decision, no proposed build) — additive
  only, no rewording of existing correct text. (2) New "Waiting On You" read-only document tab
  (`Controls/WaitingOnYouPanel.xaml[.cs]`), opened via the existing open-or-focus singleton tab
  recipe (`MainWindow.BatterUpTabs.cs`) and a new title-bar button in `MainWindow.xaml`. Backed
  by `Services/WaitingOnYouService.cs`, which reuses (no new query paths): (a)
  `GitHubIssueMirror.TryGetBoardIssuesAsync` — the same mirror-first mechanism
  AiBatterUpQueueService/AskShaneMonitorService already use — filtered client-side for the real
  `Shane To-Do` label, falling back to `GitHubApiClient.SearchIssuesAsync`'s existing label-search
  support only when the mirror hasn't synced; (b) `AskShaneMonitorService.GetOpenItemsAsync`
  wholesale, unmodified, for the "Ask Shane" board read. Added a small additive `Excerpt` field to
  `AskShaneItem` and a `Body` field to `GitHubIssueResult` so both sections can show the issue's
  own first real body line (never fabricated). Two clearly separated UI sections: "Needs your
  action" (Shane To-Do) and "Needs your decision" (Ask Shane), each row click-opens the real issue.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  warnings/errors. Confirmed repo-wide scope with a real live GraphQL query
  (`label:"Shane To-Do" state:open`) returning #350–#354 (White-Glove Adoption epic — a
  different Epic than #1202 this build itself is under), proving the panel is not scoped to any
  one Epic. No WPF display available in this environment to click through the actual tab, so the
  UI itself is verified by clean build + code review, not a live screenshot — same standing
  constraint every other headless BuildConsole session here has. `git status --porcelain` clean
  after push. Branch verified merged into origin/main (`verify-branch-merged.mjs` — OK).
  No findings filed — no genuine bug/gap hit outside this issue's own scope.
