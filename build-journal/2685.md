# Build Journal — Issue #2685

- **Status:** ✅ DONE 2026-09-03T16:55:33Z
- **Scope:** buildconsole
- **Commit(s):** 9245af38c3c486bd0abefa06a347ec048d64f9b0
- **Title:** Self-blocked builds get falsely marked "done" (dedup-locked forever) — reconcile against real bookend on refresh

## Log

- 2026-09-03T16:47:28Z — Session started. Pulled #2685 body + its single BUILD: comment.
  Investigated the real root-cause chain named in the body: `QueueWatcherService` →
  `MarkCompleteAsync` (exit 0 + github# → `verifying`; exit 0 no-github → `done`), the three
  dedup gates (`IssueDispatchService`, `BatterUpQueueService.RefreshAsync`,
  `BatterUpQueueService.QueueRowAsync`), `FindDedupCandidateAsync`, `MarkSupersededByReplyAsync`
  (guard NOT to be weakened), and `DoneBookendVerifier` (the trusted origin/main bookend reader).
- 2026-09-03T16:47:28Z — Point 3 verified LIVE against the real DB + code paths. Found real
  false-done rows this session: #2385 (row 1224) and #2386 (row 1225) are both `status='done'`
  in `bt_build_queue` while their `origin/main:build-journal/{N}.md` effective `**Status:**` is
  `🛑 BLOCKED`. Confirmed `superseded` does NOT cleanly unblock: it fails all three dead-checks
  (RefreshAsync treats only failed/canceled as dead; QueueRowAsync keys on IsTerminalStatus =
  done/failed/canceled; IssueDispatchService rejects any row), and `superseded` rows with NULL
  `superseded_by_id` already exist in the wild (rows 1266/1274/1279/1283/1284/1285), so
  superseded_by_id can't safely discriminate a reconciliation-superseded row from a reply one.
  **Decision: reset false-done rows to `canceled`** — a real non-blocking terminal state that
  flows through every existing dead-check with ZERO gate changes and ZERO risk to the Reply
  guard, exactly satisfying the goal (re-dispatchable, conscious re-queue, no auto-relaunch).
- 2026-09-03T16:55:33Z — ✅ DONE — shipped in 9245af38c. Changes (BuildConsole/WPF):
  1. `DoneBookendVerifier.GetBlockedAsync(issueNumbers)` — new sibling to `GetSatisfiedAsync`,
     reusing the exact `git show origin/main:build-journal/{N}.md` + rate-limited fetch +
     `StatusFieldRx` machinery, own short-TTL cache. Parses the EFFECTIVE (LAST) `**Status:**`
     line, returns the subset whose effective status contains `BLOCKED`. Fails CLOSED to "not
     blocked" on any uncertainty so a genuinely-done row is never wrongly cancelled.
  2. `BuildQueuePostgresClient.GetDoneGithubRowsAsync()` + `MarkFalseDoneReconciledAsync(id)` —
     new dedicated method (does NOT weaken `MarkSupersededByReplyAsync`'s guard) resetting a
     confirmed false-`done` row to `canceled`, guarded on `status='done'` (idempotent/race-safe).
  3. `Services/FalseDoneReconciler.cs` — reconciliation pass: done+github rows → bookend check →
     reset false-dones to `canceled` → move board Status to Backlog → log each. Fail-soft.
  4. Wired into `MainWindow` `BoardRefreshCompleted` cascade (the manual-refresh event #1813/#2557
     ride), channel `batter-up`.
- 2026-09-03T16:55:33Z — Point 3 verified LIVE end-to-end on real false-done rows #2385 (row 1224)
  and #2386 (row 1225): `MarkFalseDoneReconciledAsync` SQL → `done`→`canceled` (UPDATE 1); re-run →
  UPDATE 0 (idempotency proven). `FindDedupCandidateAsync`'s exact query then returns
  `status='canceled'`. `canceled` ∈ {failed,canceled} (RefreshAsync dead set) AND ∈
  IsTerminalStatus\{done} (QueueRowAsync) ⇒ issue REAPPEARS + re-queues via reuseRowId — dedup no
  longer rejects it; `superseded` ∈ NONE ⇒ would stay locked (confirms the issue's warning; also
  superseded+NULL superseded_by_id already exists in the wild, so it can't discriminate). Board
  move: #2385/#2386 already CLOSED+Backlog so the `SetIssueStatusByNumberAsync(Backlog)` is a
  confirming no-op (same path #2557's closed-sweep uses). `dotnet build` clean (0 warn/0 err).
  Branch verified merged into origin/main. Working tree clean.
- 2026-09-03T16:55:33Z — Findings filed: none — root cause + all named files matched the body.
