# Build Journal — Git #2048

- **Status:** ✅ DONE 2026-09-02T03:05:00Z
- **Scope:** platform
- **Title:** Remediation KB: author the copilot: domain (8 active checks)

## Log

- 2026-09-02T02:10:00Z — Session started. Pulled #2048 body + comments (comment is just the
  BUILD header, full spec lives in the issue body). Confirmed authoring standard via #1924 and
  the reference migration `2026-08-31-remediation-kb-identity-domain-1924.sql`, plus the most
  recent sibling domain migration (`2026-09-02-remediation-kb-security-domain-2042.sql`) as the
  template for structure/tone. Queried local Postgres: 8 active `copilot:*` checks in
  `monitor_checks`, 0 `copilot:*` rows currently in `remediation_knowledge_base` (81 total rows
  across identity/devices/governance/security/teams domains). Pulled each check's real
  `endpoint`/`mapping`/`severity_rules` from `monitor_checks` to ground content in the actual
  live query shape, same as the security-domain migration's implementation-note practice.
  Proceeding to fetch real Microsoft Learn docs per check and author the migration.
- 2026-09-02T03:05:00Z — ✅ DONE — Fetched real Microsoft Learn docs in-session for all 8
  `copilot:*` checks (readiness-prerequisite, license-vs-total-users, active-usage-rate,
  usage-activity, usage-by-app, licensed-but-inactive, data-exposure-risk,
  sensitivity-labels-exist) — licensing/minimum-requirements pages, the Copilot usage-report
  Graph API references (getMicrosoft365CopilotUsageUserDetail /
  getMicrosoft365CopilotUserCountTrend), subscribedSkus API, the SharePoint Advanced
  Management / Restricted Access Control oversharing-remediation docs, and the Purview
  sensitivity-labels doc + New-Label/New-LabelPolicy cmdlet references. Authored
  `lib/db/migrations/manual/2026-09-02-remediation-kb-copilot-domain-2048.sql` (8 published
  rows, idempotent `ON CONFLICT (check_key) DO UPDATE`, commit 18490aa53 → rebased/pushed as
  e0e6e68a5) and ran it twice against local `DATABASE_URL` to confirm idempotency. Real
  before/after row count: **0 → 8** `copilot:*` rows (81 → 100 total KB rows across all
  domains), all `status='published'`. `verified_by` is honest agent attribution throughout
  ("Claude Sonnet 5 (build #2048) — agent-authored, verified against Microsoft Learn; awaiting
  human spot-check"); no `draft` rows shipped. `fix_route_capability` never set to
  `we_can_run` (4 `you_must_run`, 4 `admin_center_only`).
  Filed **#2187** (parented to #1489, board status "AI Batter Up") documenting 4 real
  `monitor_checks` query/mapping gaps surfaced while authoring this content
  (active-usage-rate's count-not-ratio transform, usage-by-app's `appActivity` field mismatch
  against the real Graph response shape, license-vs-total-users' missing total-user
  comparison, and data-exposure-risk's un-wired per-site fan-out) — same class of finding as
  #2042's security-domain header note; not in scope for this content-authoring build.
  `verify-branch-merged.mjs` confirmed `agent/2048-q1146` (e0e6e68a) is an ancestor of
  `origin/main`. `git status --porcelain` clean. Commit hashes: `a2fbbac43` (bookend IN
  FLIGHT), `18490aa53`→`e0e6e68a5` (migration, rebased before push).
