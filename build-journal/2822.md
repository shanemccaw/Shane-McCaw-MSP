# #2822 — msp-team.ts invite writes caller's own mspId instead of target tenant's real owning MSP

- **Status:** ✅ DONE 2026-09-04T15:31:49Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** f0533274dd38adce526271e3edf5b3e72368b103

## Log
- 2026-09-04T15:20:27Z ⏳ IN FLIGHT — investigating `POST /api/msp/customers/:customerId/team/invite` in `artifacts/api-server/src/routes/msp-team.ts`; fix per issue body: use `tenantsTable.mspId` for the target `customerId` instead of `resolveMspIdStrict(req)` (caller's own mspId claim).
- 2026-09-04T15:31:49Z ✅ DONE — fixed: the insert now looks up `tenantsTable.mspId` for the resolved `customerId` (already tenant-access-verified by `assertCustomerAccess`) and writes that instead of the caller's own session `mspId` claim; 404s if the target tenant doesn't exist. Also aligned two pre-existing extensionless relative imports in the same file (`requireAuth`, `client-setup-token`) with the file's own `.ts`-extension convention, needed so the new test can import the real router via node's native test runner. Added `src/routes/msp-team-invite.test.ts` (7 tests): the exact regression scenario (PlatformAdmin inviting across MSPs → inserted `mspId` is the target tenant's, not the caller's), an MSPAdmin same-MSP non-regression check, and a 404-for-nonexistent-customer check — all 7 pass. `tsc --noEmit` clean. Registered the new test file in `package.json`'s `test` script. Filed #2868 (parented under Feature #2567, sibling of this issue, board status "AI Batter Up") for an unrelated pre-existing bug found while editing that script: a missing space concatenates two adjacent test file paths, so `copilot-assessment-personas.test.ts` and `leads-stats.test.ts` silently never run. Branch verified merged into `origin/main` (`verify-branch-merged.mjs`); working tree clean.
