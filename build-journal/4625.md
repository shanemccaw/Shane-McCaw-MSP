# Build Journal — #4625

- **Status:** ✅ DONE 2026-09-18T05:10:33Z
- **Scope:** platform

## Log

- 2026-09-18T05:03:14Z — Started. Pulled #4625 body + comments and #4624 (Feature
  parent) body for full scope. Read current `public-chat.ts`,
  `publicChatConversationsTable` schema, `enqueueEscalationTicket`, and the existing
  test file. Wrote `build-journal/4625-plan.md`. Next: rewrite the route as a
  deterministic form endpoint, rewrite its test file, verify, commit.
- 2026-09-18T05:10:33Z — ✅ DONE — Rewrote `artifacts/api-server/src/routes/public-chat.ts`
  in place as a deterministic `POST /api/public-chat` form endpoint (name, email,
  company, serviceInterest, message → `publicChatConversationsTable`, always
  `needsReview: true`, always `enqueueEscalationTicket` with `notifyEmails: []`/
  `pushNotify: true` preserved exactly). Removed the Anthropic call and the
  guardrail/persona/content-block imports entirely. Rewrote
  `public-chat.test.ts` for the new contract (7 tests, all real-passing).
  Verification: `vitest run src/routes/public-chat.test.ts` — 7/7 passed.
  `tsc --noEmit` on the two touched files — clean (0 errors). Confirmed via
  `grep -n "anthropic"` and `grep -n "public-chat-guardrail\|shanebot-persona\|
  chat-content-blocks\|shanebot-engine"` on the route file — zero matches both.
  Found + filed unrelated pre-existing finding: #4661 (package-wide
  `tsc --noEmit` fails on `ca-hold-window-scan.test.ts`, broken since #4550,
  unrelated to this change) — parented as sibling sub-issue of #4624, labeled
  `bug`, board status set to AI Batter Up.
  Commit: `60bbb0d48` (merged into `origin/main`, verified via
  `verify-branch-merged.mjs`: OK).
  `git status --porcelain` clean.
