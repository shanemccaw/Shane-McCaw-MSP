# #3647 — Portal: let a Customer Admin assign the Billing role to employees (#3629 follow-up)

- **Status:** ✅ DONE 2026-09-12T07:04:38Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 1f4cbc54a (rebased onto main as 17feeaf9b)

## Log
- 2026-09-11T20:31:14Z ⏳ IN FLIGHT — Investigated #3629's landed model (`CUSTOMER_PLATFORM_ROLE_KEYS`,
  `setGrantRole`/`usersHoldingGrantRole` in `rbac-capability.ts`), confirmed no `.dc.html` design
  export exists for a team roster page (`Design/portal/` has no Team/Roster screen), so the new
  role-assignment surface is backend-only this pass per CLAUDE.md's design-source rule. About to:
  (1) add a `PATCH /portal/team/:userId/role` endpoint gated the same as every other mutating team
  route, restricted to the two `#3629` platform role keys; (2) extend `GET /portal/team` to report
  each member's held platform roles; (3) gate billing.tsx's "Manage payment in Stripe" button on
  `can("customer","billing.manage")`.
- 2026-09-12T07:04:38Z ✅ DONE — Shipped: `PATCH /api/portal/team/:userId/role` (portal-team.ts) lets
  a caller holding `customer:team.manage` grant/revoke the two #3629 platform roles
  (`customer-admin`, `billing`) on a teammate, gated identically to every other mutating team route
  (tenant isolation + capability), restricted to those two keys, with a self-demotion guard on the
  caller's own Customer Admin grant. Reused the existing `setGrantRole`/`usersHoldingGrantRole`
  primitive rather than writing a new one. `GET /api/portal/team` now also reports each member's
  `isCustomerAdmin`/`hasBillingRole` off the same grant rows, for whichever future UI wires this.
  billing.tsx's "Manage payment in Stripe" button is now gated client-side on
  `can("customer","billing.manage")` (mirrors UserMenu.tsx's existing `showBilling` pattern; the
  real gate was already server-side). No design export exists yet for a team roster page in
  `Design/portal/`, so no portal UI was built for role assignment itself — backend-only per
  CLAUDE.md's design-source rule; item 3's UserMenu billing-link copy is explicitly left as the
  issue's own noted design/copy decision, not built.

  **Verification:** `artifacts/api-server/src/routes/portal-team.test.ts` — 14 tests, all passing
  (added self-demotion guard, unknown-role/non-boolean validation, grant/revoke cases; existing
  `MUTATING_ROUTES` gate-coverage list updated to include the new route). `tsc -p tsconfig.json
  --noEmit` clean in both `artifacts/api-server` and `artifacts/portal` (after rebuilding a stale
  `lib/db/dist` left behind by a prior commit — pre-existing, unrelated to this build; see
  [[api-server-tsc-needs-libdb-decls-and-pnpm-state-reset]]). New/updated test manifests:
  `test-manifests/portal/team-role-assignment.json` (new, registered in `_regression-suite.json`)
  and `test-manifests/portal/billing.json` (notes updated for the new client-side gate).

  **`shaneapp://runTest` was attempted but could not complete against the real dev server** — a
  real, bounded check (Git #2160): the new manifest reported "manifest not found" (this branch
  wasn't merged/deployed to the dev-server checkout yet at attempt time), and re-running the
  existing `billing.json` manifest against the live dev server returned real connection-refused
  errors on `localhost:8080` (api-server down) and a stale/mismatched UI build on `:5173`.
  `node scripts/dev-server/status.mjs` confirmed the api-server process was dead
  (`pid 67456 DEAD port 8080`) independent of anything in this build.

  **Self-inflicted incident during that investigation, disclosed to Shane in-session:** while
  checking `dev-all.mjs`'s usage, `node scripts/dev-all.mjs --help` was run from this worktree.
  The script does not recognize `--help` and instead ran its default full-stack start **from the
  worktree** (`C:\wt\3647-q2357`) rather than the main checkout, which killed whatever was
  listening on ports 5173/5174/5175/5176/5177/8080 and brought up worktree-rooted replacements on
  5174 (admin-panel) and 5175 (portal). `Stop-Process -Force` was run against the four wrongly-
  rooted PIDs (31024, 8692, 8704, 8396) to kill them; the command returned exit code 1 (possibly
  just a benign "process already gone" artifact) and a subsequent permission-classifier block on
  further process/port-inspection commands (`Get-Process`, `netstat`) meant the final state could
  not be re-verified from this session. `msp-website` (5176) and `api-server` (8080) appeared to
  be independently self-healing (processes rooted in the main checkout, one parented directly by
  `BuildConsole.exe`, consistent with the #3084 api-server supervisor) and were left untouched.
  **Shane needs to confirm the real state of his local `admin-panel`/`portal`/`marketing`/
  `msp-console` dev servers and restart whichever are still down** — flagged to him directly in
  chat this session, and filing a `Shane To-Do`-labeled finding issue for the underlying
  `dev-all.mjs --help` footgun (no recognized help/usage flag; an unrecognized argument silently
  falls through to a full-stack start, which is actively harmful when invoked from an agent
  worktree since it collides with the main checkout's own supervised services on the same ports).

  This build's own code, tests and typecheck are genuinely done and verified; only the
  environment-level `runTest` pass and the dev-server disruption above are the honest gaps, not
  the #3647 work itself.
