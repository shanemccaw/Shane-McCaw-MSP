# #1425 — Governance: Oversharing stat band, Top Risks band, Anonymous Links panel — replace unconditional fixture with honest no-data state

- **Status:** 🛑 BLOCKED 2026-09-10T17:16:21Z (re-verified, unchanged)
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** none — no code change, premise found stale during investigation

## Log
- 2026-09-03T01:50:00Z ⏳ IN FLIGHT — pulled #1425 body + comment, then #1413's full investigation comment it cites.
- 2026-09-03T03:59:13Z 🛑 BLOCKED — Investigated before touching code (per Investigation phase). #1425's target file,
  `portal-v2-gov-oversharing.tsx`, does not exist anywhere in this repo — `artifacts/msp-portal` (the whole portal-v2
  app) is gone, confirmed by `find`/`glob` across the tree returning zero hits for it or `govOversharingData.ts`.
  CLAUDE.md already documents portal-v2 as retired, replaced by the #1485 rebuild reading design from
  `Design/portal/*.dc.html`. Checked `Design/portal/` — it is empty, no export for governance/oversharing exists yet.
  Checked the new portal app (`artifacts/portal/src`) — only three live data hooks exist
  (`govOversharingSitesLive.ts`, `govOversharingRunbooksLive.ts`, `oversharingItemsLive.ts`), no page component wires
  them yet. Checked #1425's own Feature-tier parent, #1621 ("Feature: Pillar Pages, Finding Drill-down & Remediation
  Offer (Portal)") — its own body states plainly: "This issue is at the architecture stage. Nothing here schedules
  build work," with several architecture questions (what is a "block", which findings are remediable, four
  remediation vehicles to reconcile) explicitly unresolved.

  Per CLAUDE.md's fixed order-of-work under #1485 (architect → build endpoints → regenerate contract pack → Design →
  wire) and its explicit instruction ("if you are asked to wire a page and no export is there, say so and stop
  rather than wiring portal-v2 by default — portal-v2 is not a fallback target"), there is no page to fix and
  building one out of order would repeat the exact failure #1485/#1621 exist to prevent. No code changed.
  Wired `blocked_by` → #1621 (see issue comment + dependency edge), labels flipped `in-flight` → `blocked`,
  posted an honest comment on #1425 explaining the stale premise. Not writing DONE — this is unresolved, not
  unmerged, so the ancestor/clean-tree checks don't apply to a no-op branch, but noting for completeness: no
  files were modified other than this bookend, `git status --porcelain` is clean of anything else.
- 2026-09-10T17:16:21Z 🛑 BLOCKED (re-dispatched, re-verified, unchanged) — this build was re-dispatched against
  #1425. Per the standing rule ("before doing anything else, check whether you were left blocked"), checked the
  real `blocked_by` edge first (`GET .../1425/dependencies/blocked_by`) — still points at #1621, which is still
  OPEN. Re-read #1621 in full, including its two comments since the prior session: #2923 (which answered one
  architecture question — finding evidence arrays) closed 2026-09-05 and its now-stale `blocked_by` edge was
  already removed by a later session (2026-09-06), but #1621's own last comment states explicitly that
  "drill-down/remediation-offer is still correctly held back by the open architecture questions this issue's own
  body lists" — i.e. #1621 itself is still unresolved on the parts #1425 needs.

  Also checked whether #1749 ("Pillar pages: wire the portal UI to the real endpoints", a sub-issue of #1621,
  closed) mooted #1425 by shipping a replacement. It did, partially: `artifacts/portal/src/pages/pillar.tsx` is a
  real generic pillar page now live for all six pillars, with real wired stat tiles
  (`pillar-summary-stats.ts` — for governance: `governance.sites`, `governance.overshared`, `governance.exposure`,
  `governance.publicChannels`, all real backend-derived counts, zero fixture) and a real findings list — so the
  "stat band must not be a silent fixture" half of #1425's concern is now satisfied, just under different field
  names than the old `OVERSHARING_STATS` vocabulary (Sharing Capability / External Users / Anonymous Links /
  Sharing Drift), which no longer exists anywhere in code.

  But #1425's other two asks — a "Top Risks" band and a dedicated "Anonymous Links panel" (using the real
  `oversharingItemsLive.ts` / `govOversharingSitesLive.ts` hooks, still unwired into any page) — are NOT covered
  by the new generic page. `pillar.tsx`'s own code comment says so directly: it deliberately does not wire "the
  design's full per-check 'block' breakdown... and the click-a-finding-for-full-breakdown + SOP/Runbook
  remediation offer flow" because "#1621's own body leaves 'what is a block' and which of the four remediation
  vehicles a finding offers as open architecture questions not yet settled in chat." A per-governance-check
  Anonymous-Links breakdown panel is exactly that kind of block-level drill-down. Confirmed the Design export
  (`Design/portal/design_handoff_full_site/screens/Pillar Pages.dc.html`) does still reference "Anonymous" /
  "sharing capability" text, consistent with it living in the still-unbuilt per-check block table, not the
  now-shipped top-level stat band.

  Conclusion unchanged from 2026-09-03: still correctly blocked on #1621 for the Top Risks band and Anonymous
  Links panel. Building either now would mean inventing an answer to #1621's still-open "what is a block" /
  "which remediation vehicle" questions myself — a real product decision, not a missing backend, so per CLAUDE.md
  this is a stop-and-hand-to-Shane case, not a BUILD IT case. No code changed this session either.
  `blocked_by` edge to #1621 confirmed still accurate — left as-is. Posted a re-confirmation comment on #1425.
  `git status --porcelain` clean before and after.
