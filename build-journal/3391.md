# #3391 — MCP tools: core issue operations — create/get/update/search

- **Status:** ✅ DONE 2026-09-10T17:43:11Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 808d71a92 (impl, merged to main), c94f93f20/cdea3cf16 (IN FLIGHT bookend)

## Log
- 2026-09-10T00:47:45Z ⏳ IN FLIGHT — pulled #3391's full body + comments, and its
  blocker #3390's full body + comments, per the dispatch's standing rules.
- 2026-09-10T00:47:45Z 🛑 BLOCKED — #3391 is real-blocked on #3390 (MCP server
  scaffold + bearer auth + PAT held server-side only). Checked for the scaffold
  landing on `origin/main`: no `artifacts/*github-mcp*` (or equivalent) directory
  exists, no `build-journal/3390.md` exists, and #3390 still carries the
  `in-flight` label (not `complete`). The Feature #3377 parent also lists
  `sub-issues-completed: 0/16`. Conclusion: #3390 has genuinely not landed yet —
  there is no bearer-auth/PAT-holding server for these issue-operation tools to
  attach to. Stopping here per the dispatch's own instruction ("confirm the
  scaffold has landed before starting") rather than building ahead of the
  foundation piece.
  - The `blocked_by` edge from #3391 → #3390 was already present and correct
    (set at issue-filing time) — no stale edge to fix, no new edge to wire.
  - Synced labels: removed `in-flight`, added `blocked` on #3391.
  - Left a comment on #3391 stating the block plainly.
  - No code changed, nothing to commit for the real work; this bookend + label
    sync are the only outputs of this session.
- 2026-09-10T17:29:00Z ⏳ IN FLIGHT — Re-checked the blocker: #3390 shipped and
  merged to `main` (commit `168ba9a29a6fc2e4cab6bc6956cdb4b586686b8a`, bookend
  `build-journal/3390.md` at DONE, issue closed with the `complete` label). The
  `168ba9a29` scaffold commit is a confirmed ancestor of this worktree's HEAD.
  Removed the now-stale `blocked_by` → #3390 edge (it would have gone stale the
  moment #3390 closed) and synced labels (`blocked` → `in-flight`). Starting
  real work: adding `create_issue`, `get_issue`, `update_issue`, `search_issues`
  as `ToolDef`s under `artifacts/github-mcp-server/src/tools/`, each calling the
  scaffold's `githubRequest()` — no new auth/PAT plumbing needed.
- 2026-09-10T17:43:11Z ✅ DONE — Shipped `create_issue`, `get_issue`, `update_issue`,
  `search_issues` per the issue's real spec: `create_issue(title, body?, milestone?,
  labels?)` (milestone is the real milestone *number*, e.g. 5 for v1.1, not its
  title), `get_issue(number)`, `update_issue(number, title?, body?, milestone?,
  labels?, state?)` (only provided fields change; `labels` replaces the full set,
  GitHub's own PATCH semantics), `search_issues(query, perPage?)` (real passthrough
  to GitHub's own search syntax, auto-scoped to this repo with `repo:owner/name`,
  same `total_count` + items shape GitHub's search API returns). Added
  `normalizeIssue()`/`RawGitHubIssue`/`repoPath()` to `src/github.ts` — the one
  normalized issue shape all four (and now every sibling tool) return. Adopted the
  concurrently-landed `githubRepo()` helper (from #3392/#3394) rather than
  introduce a duplicate `githubDefaultRepo()`.
  **Verification (real, not assumed):** `tsc --noEmit` clean. `npm run smoke`
  passes (auth/PAT/audit spine unaffected). Then real end-to-end against the live
  repo via the running server + a minted bearer token (not gh CLI): `create_issue`
  created real issue **#3535**, `update_issue` corrected its title (a curl/shell
  UTF-8 encoding artifact on my first call, not a tool bug — confirmed by
  re-running the same update via Node with a real UTF-8 em-dash, which fixed it)
  and added the `bug` label, `get_issue` read it back matching, `search_issues`
  found it by title text. Confirmed the PAT never appeared in any response body
  across all calls; test bearer token revoked afterward. Issue #3535 left open,
  titled "MCP verify (#3391) ... — safe to close", for Shane to review/close
  himself (never closed by this session, per the "you never close an issue"
  rule) — its body explains it's a verification artifact.
  **Concurrent landings merged in:** during this build, sibling sub-issues #3392
  (sub-issue hierarchy + blocked_by), #3393 (post_comment/list_comments), #3394
  (close_issue), and #3395 (move_to_status) all landed on `main` — required 4
  successive rebase+resolve cycles on `src/github.ts` / `src/tools/index.ts` /
  `README.md` to integrate cleanly (naming collision resolved: kept the
  already-landed `githubRepo()` over introducing a duplicate). Every sibling
  tool of Feature #3377's original design is now shipped.
  Commit `808d71a92`, merged to `main` (`verify-branch-merged.mjs` OK). Working
  tree clean (`git status --porcelain` empty).
  Findings filed: none — no new gaps surfaced beyond what the issue's own scope
  covered.
