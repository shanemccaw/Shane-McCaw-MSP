# #2381 — Group cards render at full height inside the scroller — fix flex-shrink

- **Status:** ✅ DONE 2026-09-02T19:52:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-02T19:35:00Z — Starting. Sub-issue of #2379 (Feature: Detected in this chat —
  collapsible groups), sequenced after #2380 (blocked-by, now DONE at 1e6199cf6/6ec798ed0).
  Design reference: `desktop/ShaneBuilder/wpf-handoff/Shell Skeleton v2.dc.html`'s
  `domReaderGroups` — each group is ONE bordered card (`border-radius:8px;overflow:hidden`)
  wrapping its header + body, not loose header/card siblings directly in the scroller's flex
  column. #2380's build put the collapsible header and each `DetectionCard` as flat siblings
  in `DetectedItemsResults` (a `StackPanel` inside a `ScrollViewer`) — no group-card wrapper.
- 2026-09-02T19:52:00Z — ✅ DONE — implementation commit `50438fad7` (bookend IN FLIGHT was
  `168883f49`). Added
  `DetectedGroupCard(groupKey, label, visible, collapsed)` in
  `desktop/ShaneBuilder/MainWindow.xaml.cs`: wraps each group's collapsible header + its
  `DetectionCard` items into ONE `Border` (`CornerRadius=8`, `ClipToBounds=true`), matching the
  design's `overflow:hidden` group-card wrapper instead of loose siblings in the scroller's
  `StackPanel`. This is the WPF analogue of the design's `flex-shrink:0` on the group card:
  header + items now share a single measured `StackPanel` inside that Border, so the card
  always renders at its own real content height and can't be squashed/stretched by the
  scroller around it. `RenderDetectedItemsAsync`'s group loop now adds one `DetectedGroupCard`
  per group instead of a separate header + N separate cards. Gave the header a real background
  (`Brush.Claude.Bg.Button`) and padding to read as the card's title bar, matching the design's
  `#232320` group-header background — previously it was bare, transparent text floating above
  the loose cards.
  Verified: `dotnet build ShaneBuilder.csproj -c Debug` → 0 warnings, 0 errors.
  `verify-branch-merged.mjs` → OK, merged into `origin/main`. `git status --porcelain` → clean.
  No mid-build findings to file this session.
