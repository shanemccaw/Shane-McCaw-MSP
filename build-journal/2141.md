# #2141 — Epic #1202 Build Console — EMERGENCY single-instance guard

- **Status:** ✅ DONE 2026-09-01T16:07:51Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** f197f96dfc143d1f83af35c1b3162a4ce7bc83da (code), plus this bookend commit

## Log
- 2026-09-01T16:02:08Z ⏳ IN FLIGHT — Audited `App.OnStartup` and `Services/ShaneAppProtocol.cs`. Implementing: (1) unconditional named-Mutex single-instance guard at top of `OnStartup`; (2) an `activate` message on the existing shaneapp:// pipe to foreground the live instance; (3) an agent-directed refusal written to `Console.Error` + `ActivityLog` (system.core) with a distinct non-zero exit code (10). Guard deliberately does NOT block shaneapp:// courier launches (reportProgress/executeSql/runTest depend on them) — only a normal full launch on top of a live instance is refused.
- 2026-09-01T16:07:51Z ✅ DONE — Shipped in `f197f96`. `App.xaml.cs`: named system `Mutex` `Global\ShaneMcCawBuildConsole_SingleInstance` acquired at the very top of `OnStartup` (before the shaneapp:// branch). If `!createdNew && protocolUri == null` → courier a `shaneapp://activate` over the existing per-user pipe (`TryActivateRunningInstance`), write the agent-directed refusal to `Console.Error` + `ActivityLog("system.core")` (`WriteSingleInstanceRefusal`), set `Environment.ExitCode = 10`, `StartupUri = null`, `Shutdown(10)`, return — no window built. Carve-out: any launch carrying a shaneapp:// URI is a transient protocol courier and is NOT refused (reportProgress/executeSql/runTest keep working; report-progress.mjs uses the pipe directly + a protocol-launch fallback). `MainWindow.xaml.cs`: new `activate` action branch in `HandleShaneAppUriAsync` → `ActivateMainWindowFromProtocol()` (restore-if-minimized + `SetForegroundWindow`, UI thread). Verified: `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings / 0 errors. Not run (freeze #2138 + this guard itself forbid launching a second instance); BuildConsole/WPF is out of scope for test manifests per CLAUDE.md, so a clean build is the verification.
