# #3600 — Build Queue: clicking a not-yet-dispatched blocker should dispatch it (if ready) or alert why not

- **Status:** ✅ DONE 2026-09-11T01:28:31Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** b65eb7556 (bookend IN FLIGHT), 2fa12a8b9 (real fix, rebased/merged onto main — original pre-rebase hash f4eca74a0)

## Log
- 2026-09-11T01:17:07Z ⏳ IN FLIGHT — pulled issue #3600 body + comments, investigating blocker ghost card UI (Git Board/Focus Mode/Batter Up) and existing dispatch machinery (BatterUpQueueService/IssueDispatchService, bt_dispatch_claims) before making the ghost card clickable to dispatch-if-ready or alert-why-not.
- 2026-09-11T01:28:31Z ✅ DONE — `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`'s `BuildBlockerGhostCard` (a not-yet-dispatched blocker with no live queue row) is clickable now too, distinct blue accent vs. the live case's pink. Click reuses the exact existing dispatch path (`IssueDispatchService.DispatchAsync`: find its own real `BUILD:` comment → dedup-check → queue), with the same Git #3509 `bt_dispatch_claims`-claim-before-ask-active-chat guard for the no-comment case (new `DispatchBlockerFromGhostCardAsync` method + `_ghostDispatchInFlight` local double-click guard). A blocker whose own blockers are still open is still queued (same fail-closed convention every other dispatch entry point already uses) and the toast names the real open blocker number(s) as the honest "why not yet" explanation; every genuinely un-dispatchable case (no PAT, issue not found, GitHub unreachable, no DB, or the active-chat ask having nowhere to go) surfaces a real `ToastEngine` toast naming the actual reason — no silent no-op. No new dispatch path invented. Rebased onto a concurrent same-file commit (Git #3599, `FindLiveNodeForBlocker`/`RevealQueueItem`) mid-session — merged both changes, re-verified clean build after. Verification: `dotnet build BuildConsole.csproj` — 0 errors (both before and after the #3599 merge); no BuildConsole test-manifest requirement per CLAUDE.md scope (portal/marketing only), and BuildConsole.exe was not launched per the standing hard rule (Git #2156) — this WPF UI change has no automated test harness, so verification is the clean build plus a line-by-line trace confirming every branch/signature matches the already-shipped `DispatchPanel.DispatchAsync`/`LeftSidebar.DispatchOrAskActiveChatAsync` mechanics it reuses. No genuine findings/gaps discovered mid-build — nothing filed under #1788. Branch verified merged into `origin/main` (`verify-branch-merged.mjs` — OK) and working tree clean (`git status --porcelain` — empty) before writing DONE.
