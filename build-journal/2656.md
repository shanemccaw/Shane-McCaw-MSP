# #2656 — Scope and SLA: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T05:35:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 47375589a (Scope and SLA wiring)

## Log
- 2026-09-12T05:10:00Z ⏳ IN FLIGHT — Read #2656 body + dispatch comment, confirmed
  `Design/MSP_Console/design_handoff_msp_console/Scope and SLA.dc.html` (README screen
  30) is real and landed, confirmed the MSP Console Shell (#3677) is real and live
  (`ConsoleShell.tsx`, `nav.ts` MSP_PAGES `sla`), and read
  `docs/msp-console/scope-and-sla-msp-console-contract-pack.md` (regenerated post
  #2726/#2728/#2811/#2812/#2898) plus the live route files
  `artifacts/api-server/src/routes/msp-scope-creep.ts` (12 routes) and
  `msp-sla.ts` (14 routes) directly. `#2656`'s own `blocked_by` edge pointed at
  #3677, which is now closed (completed) — clearing that stale edge as part of this
  session per the Git #1987 rule. Next: build the api client + `ScopeSla` module
  page mounted at `/ops/sla`, wired to the real 26-route surface (the honest subset
  the design's own screen actually surfaces), and mount it in `ConsoleShell.tsx`.
- 2026-09-12T05:35:00Z ✅ DONE — Shipped `artifacts/msp-console/src/api/scope-sla-api.ts`
  (real types + react-query hooks for `msp-scope-creep.ts`'s 12 routes and
  `msp-sla.ts`'s 14 routes) and
  `artifacts/msp-console/src/console/modules/ScopeSla.tsx` (5 tabs — queue/
  timers/scope/policies/compliance — + score-a-customer and task-detail
  drawers), mounted at `/ops/sla` in `ConsoleShell.tsx`, plus 5 new real
  `lucide-react` icons in `icons.tsx`. Every honest departure from the
  design's fixture (deep-link fallback for SLA-breach queue tasks, real
  copy-on-write policy edit, merged multi-status detections, joined
  compliance table) is documented in the api file's own header. No fixture
  data anywhere. Verified: `pnpm typecheck` and `pnpm build` both clean in
  `artifacts/msp-console` (including after resolving real rebase conflicts
  against two concurrent sessions' Diagnostics (#2653) and Executive View
  (#2659) modules landing on the same shared files). Local Postgres reachable
  and confirms the contract pack's live-empty state still holds (2 msps, 0
  rows in every engine table) — honest-empty is therefore the real, correct
  state for every list in this build, not a stub. No live BuildConsole/
  shaneapp runner or dev API server was reachable from this session to
  exercise the routes end-to-end over HTTP; `msp-console` is out of the
  CLAUDE.md test-manifest scope (`artifacts/msp-portal/` and
  `artifacts/shane-mccaw-consulting/` only), so no manifest was required.
  Cleared a stale `blocked_by` edge on #2656 pointing at #3677 (already
  closed). Filed #3778 (sub-issue of Feature #2572, board status "AI Batter
  Up") — `GET /msp/operator-tasks`'s SLA-breach rows omit `timer_id`, so the
  queue's task drawer can't resolve the underlying clock in place; documented
  the one-column fix. Commit `47375589a`, merged to `origin/main`
  (`verify-branch-merged.mjs`: OK). Working tree clean.
