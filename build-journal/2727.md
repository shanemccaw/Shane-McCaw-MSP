# #2727 — Chat context bar: retire old handoff banner, move Start New Chat next to toolbar icons

- **Status:** ✅ DONE 2026-09-03T20:13:49Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 09a1d84f3e0aa99e7b00d78d781b65d61dbe59c6 (real work), c72a0ae8e588f746bdeb77cc56ce6e45ba22faa8 (origin/main after merge)

## Log
- 2026-09-03T20:05:03Z ⏳ IN FLIGHT — starting: (1) retire System A's `meterState` banner/progress-bar
  in `MainWindow.xaml.cs` (~line 7025-7339) so it never renders, (2) move `BtnStartNewChat` in
  `ChatDocumentContainer.xaml` out of the left `WrapPanel` into the right `StackPanel`
  (`Grid.Column="1"`) immediately left of `BtnDetected`, (3) port System A's real absolute-token
  tiers (60k/85k/100k/130k + cost estimate) into Band 1's `UpdateGauge()` in
  `ChatDocumentContainer.xaml.cs` so the richer tiering isn't silently dropped.
- 2026-09-03T20:13:49Z ✅ DONE — commit 09a1d84f (merged to origin/main as c72a0ae8, verified via
  `scripts/dev-server/verify-branch-merged.mjs`). Real changes:
  1. **Retired System A**: `CreateChatContextWrapper` in `MainWindow.xaml.cs` no longer attaches
     the `meterState` banner/progress-bar to any visual tree — they're built headless (so
     `UpdateContextMeter`/`TriggerHandoffAsync`/`ChatContextMeterStore.Merge` keep running
     unchanged, still feeding real data to Band 1) but can never render. `CreateChatContextWrapper`
     now returns `wv` directly instead of the old 3-row wrapping grid.
  2. **Moved `BtnStartNewChat`**: out of Band 1's left `WrapPanel` (`ChatDocumentContainer.xaml`)
     into the right `StackPanel` (`Grid.Column="1"`), immediately left of `BtnDetected`.
  3. **Reconciled tiers**: ported System A's real absolute-token tiers (60k/85k/100k/130k) and
     cost estimate (`tokens * 3.00/1_000_000`) into Band 1's `UpdateGauge()` — drives
     `CtxGauge`'s color (reusing the app's real `GreenBrush`/`YellowBrush`/`PeachBrush`/`RedBrush`
     hex values) and `BtnStartNewChat`'s visibility (now fires at 60k conversation tokens, matching
     the old banner's earliest "Start handoff chat" offer, not the old flat 75%-of-300k cutoff) and
     tooltip (tier label + cost + remaining-to-critical). The `CtxBarFill`/`CtxGauge` text's own
     40k-overhead/300k-budget math is untouched — Shane confirmed that half was already correct.
  - **Verification**: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 Warning(s),
    0 Error(s).
  - **Known gap, not silently dropped**: `BtnStartNewChat_Click` calls the plain
    `OpenOrCreateEpicChat`, not the retired `TriggerHandoffAsync`'s richer real logic (archives the
    old chat, carries a handoff-back pointer into the new chat's URL). That's a real functionality
    regression outside #2727's stated 3-point scope — filed as **#2736** (parented to #1202,
    labeled `bug`, board status "AI Batter Up"), per #2727's own "don't silently lose real
    functionality... without a clear reason noted in the bookend" instruction.
  - Working tree left clean (`git status --porcelain` empty at DONE).
