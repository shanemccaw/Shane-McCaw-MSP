# #3004 — Configuration State: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-06T19:20:02Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** cc67588c2 (on `main`, rebased from `c5a955ce0`)

## Log
- 2026-09-06T19:08:29Z ⏳ IN FLIGHT — wiring `artifacts/portal/src/pages/config-state.tsx` against
  `docs/configuration-state-contract-pack.md` and the landed
  `Design/portal/design_handoff_full_site/screens/Configuration State.dc.html` export, per #3004's
  own body (part of #3002; design landed under #3003). Real endpoints:
  `GET /api/portal/config-state/{snapshots,snapshots/current,snapshots/:id,changes,changes/:diffId}`.
- 2026-09-06T19:20:02Z ✅ DONE — real page shipped at `artifacts/portal/src/pages/config-state.tsx`,
  wired via new `configStateWire.ts` (pure shapes/formatters) + `configStateLive.ts` (fetch hook +
  lazy drill-down panel fetchers), reading every number from the five real
  `/api/portal/config-state/*` endpoints — no `configStateData.ts` fixture module exists. Route
  added to `App.tsx` (`/config-state`), `moduleNav.ts`'s `config-state` entry now points at it
  instead of `coming-soon`. New test manifest `test-manifests/portal/config-state.json`
  (apiTests for auth-gates + real-shape assertions + 404-not-403 scoping; uiSteps assert the
  honest unauthenticated `Sign in required` panel, matching the same structural gap #2998/#2996
  document — no portal login page exists yet). Registered in `_regression-suite.json`.
  Verification: real `tsc -p tsconfig.json --noEmit` (0 errors) and real `vite build` (succeeds)
  against `artifacts/portal` with this page + route added, both re-run after rebasing onto
  `origin/main`. **Not run via `shaneapp://runTest`**: no such tool/script is reachable from this
  session (checked — `scripts/dev-server/` has no `runTest`/`run-test` script, and no
  `shaneapp://`-invoking tool is registered), and BuildConsole's local instance resolves UI routes
  against the dev-server checkout, which does not yet have this branch merged/deployed — the same
  gap #2998/#2996/#2921 document for their own uiSteps runs. The design's own hand-authored,
  tenant-specific commentary (workload notes, per-group notes, history notes) was deliberately
  **not** copied verbatim — it describes one specific historical snapshot/diff pair on the testbed
  tenant and would misstate a later collection run or a different tenant; `configStateWire.ts`
  instead derives the same *kind* of honest, evidence-backed commentary live (most-common unread
  reason per workload, largest resource by volume in a diff, real `notComparableReason`
  breakdowns) — see that file's and `config-state.tsx`'s own header comments. No new backend gap
  found — all customer-facing fields the design calls for are served by the real endpoints as
  documented in `docs/configuration-state-contract-pack.md` §5; no findings filed.
  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before this line was
  written. `git status --porcelain` clean at completion.
