# #1552 — Policy Engine: VIP classification — three sources, and who is authoritative

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 39a0bc847 (IN FLIGHT bookend), 5a05b8d9e (work — real post-push hash, re-read
  after push, `verify-branch-merged.mjs` exit 0) — both on origin/main

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1552 body + the 2026-08-28 resolution comment: the PLATFORM
  is authoritative on VIP classification, not the tenant. "Told" (manual, in-platform) is the
  decision; group membership and AD attribute are read hints used only for DISCOVERY/onboarding
  seeding, never sources of truth once the platform holds a classification. A VIP hand-removed
  from their tenant-side groups is DRIFT, not a legitimate change. De-VIP is an act performed
  here (a runbook), not observed from the tenant.
  Building the classification object this resolution requires: a `vip_classifications` table
  (platform-held current state per customer+principal, `source` = `told | discovered_group |
  discovered_attribute`), its manual migration, a wire-shaper lib, and an MSP-side route —
  `POST .../vip-classifications` (Told, always wins, upserts) and
  `POST .../vip-classifications/discover` (seed-only-if-absent, never overwrites an existing
  row of any source — the precedence rule enforced at the write path, not left to the reader).
  Builds on #1547's `standing_policies` (DONE, `2917f7a2c`) as the sibling declarative object;
  does not touch #1548/#1549/#1550/#1553's in-flight parallel work. Ends at the wire contract —
  no portal page (per scope stop).
- 2026-08-30 ⚠️ Mid-session, this worktree's local branch was found reset to an unrelated
  commit ("Bookend: #d DONE") by something outside this session, discarding all uncommitted
  work-in-progress since the IN FLIGHT bookend (schema/lib/route/test files not yet committed).
  The IN FLIGHT bookend commit itself was safe (already pushed to `origin/main` before the
  reset). Recovered by resetting the worktree to `origin/main` and rebuilding the implementation
  from scratch; committed and pushed immediately this time to minimize further exposure.
- 2026-08-30 ✅ DONE — Shipped as `5a05b8d9e` (rebased twice onto a fast-moving main — #1548,
  #1549, #1550/#1551, #1553 all landed mid-session — conflicts were pure list-append collisions
  in `vitest.config.ts`/`routes/index.ts`, resolved by keeping both sides; merge verified by
  `verify-branch-merged.mjs`, exit 0). What landed:
  - **Schema** `lib/db/src/schema/msp.ts`: `vipClassificationsTable` (`vip_classifications`) —
    platform-authoritative CURRENT-STATE per (customer, Graph principal): `source` enum
    (`told | discovered_group | discovered_attribute`), `is_vip`, `discovery_detail` jsonb
    (provenance, null for "told"), `classified_by_person_id`/`classified_by_name` (who told it,
    null for a discovery seed), unique on `(customer_id, principal_id)`. Identified by the Graph
    user object id, the same convention `license_assignment_snapshots`/`overshared_items`
    already use — no local directory-user inventory table exists to FK against.
  - **Migration** `lib/db/migrations/manual/2026-08-30-vip-classifications-1552.sql` — additive
    `CREATE TABLE IF NOT EXISTS` + 3 indexes, self-marking INSERT into `simulator_migration_runs`.
    **Run against the local `DATABASE_URL` in-session**: table + unique index verified (a
    duplicate `(customer_id, principal_id)` insert rejected inside a rolled-back txn, 0 rows
    left behind); separately proved the actual precedence rule end-to-end at the DB level in a
    second rolled-back transaction — a discovery seed lands, a second discovery seed for the
    same principal is a real no-op (`INSERT 0 0` via `ON CONFLICT DO NOTHING`), and a "told"
    call upserts over it (flips `source`, `is_vip`, clears `discovery_detail`) — exactly #1552's
    resolution. Real row count confirmed 0 afterward. Appended to #1630.
  - **Lib** `artifacts/api-server/src/lib/vip-classifications.ts` — pure DB-free
    `WireVipClassification` shaper, `isVipClassificationSource`, `isDiscoverySource`,
    `VIP_DISCOVERY_SOURCES`, and source labels.
  - **Route** `artifacts/api-server/src/routes/msp-vip-classifications.ts` — `GET
    /api/msp/vip-classifications` (list), `POST /api/msp/vip-classifications` ("Told" — upserts,
    always wins, de-VIP is just `isVip: false`), `POST /api/msp/vip-classifications/discover`
    (batch seed, `onConflictDoNothing` — never overwrites an existing row of ANY source).
    `MSPOperator` floor, customer-ownership validated against `tenantsTable.mspId`, mounted in
    `routes/index.ts`. Ends at the wire contract; no portal page.
  - **Test** `vip-classifications.test.ts` (9 assertions, incl. the precedence-rule vocabulary
    checks and "no money/signature field") — registered in `vitest.config.ts` include; **ran
    green (9/9)** with `DATABASE_URL` set, both immediately after rebuilding and again after the
    final rebase+push, against the actually-committed code.
  - **tsc**: not independently confirmed this session — see the filed environment finding below;
    honest "unable to verify" rather than a fabricated clean run.
  - Findings filed: **#1959** (parented to #1202, board status "AI Batter Up") — a real,
    reproduced environment defect: this worktree's `lib/db/node_modules` package symlinks were
    relative paths computed through a SIBLING worktree (`1551-q898`) rather than the main
    checkout, which Node's `fs.statSync`/`require.resolve` cannot follow when reached through
    the worktree's own outer junction (reproduced identically against the pre-existing,
    unrelated `standing-policies.test.ts`, so not specific to this build's files). Worked around
    locally (not committed — node_modules isn't tracked) by rebuilding the junction with
    absolute targets straight to the main checkout's own `.pnpm` store.
  - **Note for whoever picks up #1548's runbook/enactment side**: this route's "told" call is
    the platform-decision surface; propagating a told de-VIP outward to the tenant (removing
    group memberships etc.) is #1548's enactment path, deliberately not built here.
