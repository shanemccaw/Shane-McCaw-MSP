# #3657 — Port Shot Vault (screenshot browser/runs grouping) from ShaneBuilder into BuildConsole

- **Status:** ✅ DONE 2026-09-11T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 0f9d5fd69ca6dc5928a995757d56d0596edba215

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — Real audit done: BuildConsole's own capture mechanism is
  `Services/DesktopScreenClipService.cs` (Git #1866), saving timestamped PNGs
  (`screenclip_yyyy-MM-dd_HH-mm-ss-fff[_n].png`) to `settings.ScreenClipSaveDirectory`
  (default `%Pictures%\Screenshots\BuildConsole`). No screen/monitor tag is encoded into the
  filename (BuildConsole's region-select spans the whole virtual desktop, not one physical
  monitor, so there's no per-shot "screen" identity to tag honestly — unlike ShaneBuilder's own
  fork of the same service, which does encode one). Read ShaneBuilder's real source spec
  (`Services/ShotVaultService.cs` + `MainWindow.ShotVaultPanel.cs`). Issue's own scoped feature
  list: search by shot name/screen, tag filter chips, runs grouping (newest-first, 5-min gap,
  no persisted run id), thumbnail grid, per-shot Copy. DIFF badges / baseline-compare /
  retention-purge / send-to-tab are ShaneBuilder features from later sub-issues (#2371/#2373/#2374)
  NOT named in #3657's own scope — not porting those. Building a standalone `ShotVaultWindow`
  (same convention as `ScreenshotGalleryWindow`/`TestHistoryWindow`: a reused top-level Window,
  launched from a new title-bar icon next to `BtnScreenClip`), backed by a new
  `Services/ShotVaultService.cs` that reads the same real `ScreenClipSaveDirectory` BuildConsole's
  capture already writes to — no fixture data, no second capture path.
- 2026-09-11T00:30:00Z ✅ DONE — Shipped: `Services/ShotVaultService.cs` (real runs grouping,
  search, tag derivation, Copy), `Services/DesktopScreenClipService.cs` +`CopyFileToClipboard`
  (reads an existing shot back to the clipboard — `Capture()` itself untouched), `ShotVaultWindow`
  (new standalone window: search box, multi-select tag chips, run headers, thumbnail grid,
  per-tile Copy, honest empty state naming the real directory), and a new title-bar icon in
  `MainWindow.xaml`/`.xaml.cs` opening/focusing it. `dotnet build` clean, 0 errors (6 pre-existing
  unrelated warnings). Real verification: a scratch console harness referenced the built
  `BuildConsole.dll`, created 3 real PNGs in the real capture directory (which already holds
  ~342 of Shane's own real shots — confirming the vault will show real history immediately, not
  an empty state) with controlled timestamps straddling the 5-min run-gap, and called the actual
  compiled `ShotVaultService` methods directly: run grouping (2 runs, correct newest-first
  membership), tag derivation (screen/date/resolution), search match + no-match, and a real
  clipboard copy (`WinFormsClipboard.SetDataObject`, no exception) all passed. Harness + its test
  PNGs deleted afterward — zero residue in Shane's real screenshot folder. No genuine findings
  hit this session (stayed in scope; no bug worked around, no missing backend). Commit
  `0f9d5fd69ca6dc5928a995757d56d0596edba215`, verified an ancestor of `origin/main`
  (`verify-branch-merged.mjs` exit 0). `git status --porcelain` clean.
