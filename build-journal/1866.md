# #1866 — Screen clipping tool: title-bar icon + PrintScreen, drag a region, to clipboard and to disk

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1866 body + comment (no superseding comments). Read `RegionSelectOverlayWindow`, `VisualTestTrackerWindow.BtnCaptureRegion_Click`, `VisualTestTrackerCapture`, `BuildConsoleSettings`, `ToastEngine`, `ActivityLog`, and the `MainWindow.xaml` title bar. Plan: extend the overlay additively to cover the full virtual screen with per-monitor DPI handling; add a `DesktopScreenClipService` (CopyFromScreen → Bitmap → multi-format clipboard DataObject + timestamped PNG on disk); title-bar icon right of `TopAccountToggleBorder` with a `|` separator; PrintScreen via global `RegisterHotKey(VK_SNAPSHOT)` + in-app `KeyUp` fallback; two new settings (screenshot dir, global hotkey toggle).
