# #3067 — Add real `email_templates` row for `consent-invite`

- **Status:** ✅ DONE 2026-09-06T22:10:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 880772ea8

## Log
- 2026-09-06T21:57:36Z ⏳ IN FLIGHT — Add `consent-invite` to `seed-email-templates.ts` TEMPLATES
  array (matching the hardcoded fallback content already in `admin-clients.ts:872-879`), run the
  seeder against local Postgres to insert the row, and add a regression test proving
  `sendEmailFromTemplate("consent-invite", ...)` renders from the DB row rather than falling
  through to the hardcoded default. The call site already uses `sendEmailFromTemplate` with a
  literal fallback — that already matches every other real send site's convention — so no
  call-site rewrite is needed beyond confirming the fallback content stays in sync with the new
  DB row.
- 2026-09-06T22:10:00Z ✅ DONE — Added `consent-invite` to `TEMPLATES` in
  `seed-email-templates.ts` (subject/bodyHtml matching the prior hardcoded fallback,
  variables `clientName`/`consentLink`). Ran the idempotent `seedEmailTemplates()` seeder
  directly against local Postgres (`DATABASE_URL`) — confirmed via
  `SELECT slug, name, recipient_type FROM email_templates WHERE slug='consent-invite'`: row
  present, `email_templates` count went from 27 → 28. The seeder also runs automatically on
  every server boot (`index.ts`), so this is not a one-off. No manual SQL migration needed —
  this is data seeding through the existing code path, not schema DDL. Call site
  (`admin-clients.ts:872-879`) already called `sendEmailFromTemplate("consent-invite", ...)`
  with a literal fallback, matching every other real send site's convention (`account-setup`,
  `mfa-reset`, etc. all keep the same pattern) — no call-site rewrite was needed once the row
  existed. Added `mailer.consent-invite.test.ts`: two real regression tests — (1) with a mocked
  DB row present, `sendEmailFromTemplate` renders the DB-backed subject/body, substituting
  `{{clientName}}`/`{{consentLink}}`, and asserts the hardcoded fallback text is NOT what got
  sent; (2) with no DB row, asserts the hardcoded fallback is used (defensive path). Both pass:
  `npx vitest run src/lib/mailer.consent-invite.test.ts` → 2 passed. Also ran
  `admin-clients.test.ts` + `msp-mailer.test.ts` together (10 passed) and
  `tsc -p tsconfig.json --noEmit` (clean). A bare full-suite `npx vitest run` surfaced 59
  pre-existing failing files unrelated to this change (missing `DATABASE_URL` /
  `AI_INTEGRATIONS_ANTHROPIC_BASE_URL` env vars when run outside the normal `pnpm test`
  env-loading wrapper, plus pre-existing failures in `portal-offers.test.ts`,
  `script-ingestion.test.ts`, `azure-rm.test.ts` — none touch email templates/mailer) — not
  investigated further as out of scope for this issue. Merge: fast-forwarded `origin/main` to
  `880772ea8` directly (`agent/3067-q1822` was 2 commits ahead with no divergence) and confirmed
  via `scripts/dev-server/verify-branch-merged.mjs` → OK. `git status --porcelain` clean.
