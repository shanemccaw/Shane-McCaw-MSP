# #2337 — "Send to Claude" drops a formatted block with every stamp into the open chat's composer

- **Status:** ✅ DONE 2026-09-02T20:34:00Z
- **Scope:** buildconsole (ShaneBuilder — desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 2a827e118

## Log
- 2026-09-02T19:44:00Z ⏳ IN FLIGHT — item 11 of Feature: Test Pad (#2326). Adding a "Send to
  Claude" action to `TestPadWindow` that formats the selected (or, if none selected, every unsent)
  notes — each carrying its real stamp (screen/feature/build number, from #2331) and type — into
  one block, and drops that block into the currently active chat tab's composer via the existing
  `AlertActions.AppendToComposer` bridge MainWindow already wires up for the Alert Lab reply path.
  Sent notes flip to `IsSent` (the SENT badge #2333 already renders) and are deselected. #2328's
  composer isn't built yet, so notes still only arrive at the pad via whatever fills
  `TestPadService.AddNote` today — this issue only builds the send-out path.
- 2026-09-02T20:34:00Z ✅ DONE — Added `TestPadService.MarkSent(ids)` (flips `IsSent`, clears
  `IsSelected`, raises `NotesChanged`), a new pure formatter `Services/TestPad/TestPadSendFormatter.cs`
  (one line per note: `[TYPE] [Screen · Feature · Build #N] text`, reusing `NoteStamp.Format`'s
  existing bracket shape), and a real "Send to Claude" button in `TestPadWindow` that targets the
  checked-selected unsent notes if any are checked, otherwise every unsent note; on click it
  formats the block, invokes the existing `AlertActions.AppendToComposer` bridge (the same static
  delegate MainWindow already wires to the active chat tab's real composer for the Alert Lab reply
  path — no new bridge needed), marks the sent notes `IsSent`, and toasts the real count sent (or a
  warning if there's nothing to send / no composer reachable). The button label live-updates with
  the real target count and dims to 0.5 opacity when there's nothing unsent.
  Two other Test Pad sub-issues (#2335 composer edit-in-place, #2338 pipeline caption) landed
  concurrently on `origin/main` mid-session; rebased and hand-resolved two real merge conflicts
  (`TestPadWindow.xaml`, `TestPadWindow.xaml.cs`) to keep both slices — the pipeline caption now
  sits directly under the real Send-to-Claude button it captions. `dotnet build ShaneBuilder.csproj
  -c Debug` — 0 warnings, 0 errors, both before rebase and after the conflict resolution.
  `verify-branch-merged.mjs` confirms 2a827e118 is an ancestor of origin/main. `git status
  --porcelain` clean. Scope note: #2328's real multi-note composer landed concurrently too (not
  authored by this session) — this issue only had to build the outbound send path, which is now
  real and wired to the real notes list.
