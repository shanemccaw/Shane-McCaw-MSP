# #2337 — "Send to Claude" drops a formatted block with every stamp into the open chat's composer

- **Status:** ⏳ IN FLIGHT 2026-09-02T19:44:00Z
- **Scope:** buildconsole (ShaneBuilder — desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T19:44:00Z ⏳ IN FLIGHT — item 11 of Feature: Test Pad (#2326). Adding a "Send to
  Claude" action to `TestPadWindow` that formats the selected (or, if none selected, every unsent)
  notes — each carrying its real stamp (screen/feature/build number, from #2331) and type — into
  one block, and drops that block into the currently active chat tab's composer via the existing
  `AlertActions.AppendToComposer` bridge MainWindow already wires up for the Alert Lab reply path.
  Sent notes flip to `IsSent` (the SENT badge #2333 already renders) and are deselected. #2328's
  composer isn't built yet, so notes still only arrive at the pad via whatever fills
  `TestPadService.AddNote` today — this issue only builds the send-out path.
