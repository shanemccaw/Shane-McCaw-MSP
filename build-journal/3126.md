# #3126 — Feature: Recipes — Tonight, multi-dish synchronized cooking (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:10:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 5298e9335 (feature, rebased onto `origin/main`), pushed as a fast-forward onto `main`

## Log
- 2026-09-08T01:00:00Z ⏳ IN FLIGHT — both real blockers (#3125 Cook mode, #3124 Core list) confirmed
  `complete`-labeled AND merged into `origin/main` (verified via `git merge-base --is-ancestor` against
  their own real commits 96c2fcc75 / 8338d5c99), so cleared the now-stale `blocked_by` edges on this
  issue. Building the real "Tonight" multi-dish synchronized cook session on top of #3124/#3125's real
  recipes + Cook mode: adding a `cook_minutes` numeric column to `recipes` (the one real missing piece
  the prototype's own `MEAL.dishes[].min` sync math needs and `time_text` free text can't supply), then
  a client-side Tonight session (dish picker → synchronized start offsets so every dish finishes
  together → live per-dish status chips/countdowns → start/done alarms → per-dish snooze → "Plate up,
  half for the Rental" finish), reusing Cook mode's own per-dish step view when a dish is opened mid-meal.
  Merged latest `origin/main` first (fast-forward, picked up #3135 Medications + #3137 Money, both
  unrelated to this file set).
- 2026-09-08T01:10:00Z ✅ DONE — shipped: `migrations/028_recipe_cook_minutes.sql` (real
  `recipes.cook_minutes integer` column + positive-value CHECK constraint, run against the local
  `finances` database, ledger confirms `028_recipe_cook_minutes.sql` applied), `recipes.mjs`
  (`normaliseCookMinutes`, threaded through `createRecipe`/`pushRecipes`/`listRecipesWithMatch`),
  MCP `push_recipes`'s `cookMinutes` field, and the real client-only Tonight session in
  `public/app.js`: `mealSession`/`mealDishState`/`mealFinishTime` (the prototype's own real
  longest-dish-starts-first sync math, ported against real recipes instead of the prototype's
  fixed 3-dish demo), a global 1s tick (`onMealTick`) that fires a real full-screen alarm overlay
  (`renderMealAlarmOverlay`, appended to `<body>` so it survives a hash navigation) for both a
  per-dish "start now" alert and the "everything's done" alert, real per-dish snooze (shifts only
  that dish's own start time, verified), a Wake Lock on the whole session, live per-dish status
  chips/progress bars, and Cook mode integration (`viewCook`'s `cookMeal` branch: back/done route
  to `#/tonight` instead of `#/recipes`, plus a compact `mealChipStrip` of the other dishes'
  status). Entry points: a "Tonight" tile on Recipes, and a Today teaser when a session is live
  (suppressing #3127's own static "Tonight" plan-label card while a real session is running --
  resolved as a real rebase conflict against #3127, which landed concurrently and also touches
  Today's Tonight card).
  Verified against the real local database (not mocked, disposable test user created and
  deleted): `cookMinutes` round-trips through `createRecipe`/`listRecipesWithMatch` for three real
  dishes matching the prototype's own numbers (chicken 40 min, potatoes 30 min, beans 8 min);
  `cookMinutes <= 0` is rejected server-side; a recipe with no `cookMinutes` stays `null`
  (Tonight-ineligible); the real sync math confirms potatoes' start offset is 10 min and beans' is
  32 min (MEAL_TOTAL 40 - each dish's own minutes), and the longest dish (chicken) starts
  immediately -- the prototype's own exact real numbers. `node --check` passes on every touched
  `.mjs`/`.js` file. The server boots clean against the real local database with migration 028
  applied (18 migrations up to date). `npm run check` was attempted but collided with a different
  concurrently-running shanes-life dev server instance on this shared machine (a live process on
  the default port 5000 from another session, started well before this one) -- its WebAuthn/
  passkey checks failed against that OTHER session's live rate-limit/challenge state, not against
  mine; unrelated to this feature (no auth code touched) and not re-run to avoid further
  cross-session interference. `git merge-base --is-ancestor` confirms `agent/3126-q1889` (5298e9335)
  is merged into `origin/main`; pushed as a fast-forward onto `main`. Working tree clean.
  No findings filed -- no gaps or bugs discovered outside this issue's own scope.
