# #3348 — JSONPath filter-expression evaluator: fix `?(@.type=='control')` bracket filter

- **Status:** ✅ DONE 2026-09-12T22:41:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** ad3a237e (bookend only — no code fix needed, see log)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — investigating HttpTestExecutor's TryResolveJsonPath/TryFilterArray. Found this exact class of fix already landed at 94b5ca1a4 (Git #3052, "JSONPath filter-expression support + equals alias"), merged to main and present in this worktree's HEAD. Verifying live via shaneapp://runTest against test-manifests/portal/ownership.json to confirm the deployed BuildConsole instance actually reflects this fix and step A3 passes.
- 2026-09-12T22:41:00Z ✅ DONE — No code change was needed: the JSONPath filter-expression evaluator (`HttpTestExecutor.TryResolveJsonPath`/`TryFilterArray`, `desktop/BuildConsole/Services/HttpTestExecutor.cs`) already handles `?(@.type=='control')`-style bracket filters correctly, shipped at 94b5ca1a4 for Git #3052. Ran `test-manifests/portal/ownership.json` end-to-end via `shaneapp://runTest` on the live BuildConsole instance: step A3's `$.sources[?(@.type=='control')].live` assertion no longer fails on filter-syntax grounds — its only failure is a downstream 403 that also hits every other apiTests step in the file (A1/A2/A4/A5), i.e. an unrelated auth/RBAC problem, not the JSONPath evaluator. Root-caused that 403 while verifying: `msp_feature_role_mapping.allow` for `ladder.customer-user` still references the stale, pre-#3590 `CustomerUser` role id in `msp_roles` instead of the current `Customer` role id, so every real `Customer`-rung account is denied on all `requireCapability("ladder.customer-user")` routes (40+ routes) in the local DB. Filed as its own finding, **#3867** ("URGENT: ladder.customer-user RBAC mapping denies every Customer-rung account"), labeled `bug`+`security`, milestone 5, parented as a sub-issue of #1491 (this issue's own Feature parent). Could not set #3867's board status to "AI Batter Up" — the GitHub GraphQL API returned a hard rate-limit/circuit error for every GraphQL call this session (ProjectV2 field mutations, `gh issue create`/`gh issue comment`'s internal GraphQL calls) while plain REST calls succeeded throughout; noted honestly as a comment on #3867. Verified `node scripts/dev-server/verify-branch-merged.mjs` — OK, merged into origin/main at ad3a237e. `git status --porcelain` clean.
