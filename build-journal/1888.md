# #1888 — Screen clip: crosshair shows but drag-to-select never registers

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** _(fill at DONE)_

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigating why the #1866 screen-clip overlay shows the crosshair but a mouse drag never draws a selection (only Esc exits). Root-cause hypothesis: `RootCanvas` in `RegionSelectOverlayWindow.xaml` has a **null Background**, so its `MouseLeftButtonDown/Move/Up` handlers never fire over the empty dim area (classic WPF hit-test gotcha). Fix + verify with REAL SendInput mouse drag, not a headless harness.
