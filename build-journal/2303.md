# #2303 — Epic panel: EPIC identity badge, milestone, ring, sub-features, issues closed, burn rate, estimated remaining

- **Status:** ✅ DONE 2026-09-02T18:36:50Z
- **Scope:** buildconsole (desktop/ShaneBuilder — new isolated project, not live BuildConsole)
- **Started:** 2026-09-02
- **Commit(s):** `fd7fe2103` (pushed to `main`; local work commit `930ac2718`/amended, rebased twice against a fast-moving `main` shared with concurrent #2301/#2302/#2309/#2312 sessions)

## Log
- 2026-09-02T17:41:00Z ⏳ IN FLIGHT — Confirmed #2290 (Git Panel navigation shell, dependency) is
  genuinely DONE on origin/main (commit 79852e2b0, bookend b64df2c55, both ancestors of HEAD).
  Pulled #2303's full body + comment (BUILD: dispatch), and the 5 sibling issues (#2304-#2308)
  this single dispatch also covers per its own text ("covering items 14-19"). Read
  `desktop/ShaneBuilder/ShaneBuilder-FullFeature.md` Feature: Git Panel section (items 14-19) as
  the spec. Confirmed concurrently-running sibling builds (#2301 Milestone panel, #2302 Gate
  panel, #2309 Feature panel, #2311, #2312) via `bt_build_queue` — real collision risk on shared
  files `MainWindow.GitPanel.cs` / `Services/GitPanelService.cs`; plan is to do new work in new
  files (`MainWindow.GitEpicPanel.cs`, `Services/GitEpicPanelService.cs`) and touch the shared
  dispatch point with one surgical `if (kind == Epic)` branch. Starting implementation:
  epic-detail GraphQL read (ring/burn-rate/estimated-remaining/milestone due date), on-target
  verdict, per-feature mini stats (six state chips off real `bt_build_queue` + a new
  `shanebuilder_feature_flags` table for Pause since no native paused status exists), blocked-by
  band (REST `dependencies/blocked_by`), Queue all / Park (GitHub Project Status field, real
  option ids already read) / Pause actions, and cross-surface propagation within the running app.
- 2026-09-02T18:36:50Z ✅ DONE — Built `Services/GitEpicPanelService.cs` (epic-detail GraphQL,
  burn-rate/verdict math, blocked_by REST, ProjectV2 Status read/write, `FeaturePauseStore`) and
  `MainWindow.GitEpicPanel.cs` (real WPF ring control, identity/milestone/verdict/blocked-on
  bands, per-feature rows with real state/chips/last-build/idle-gap/hours-left, and the three
  real actions). Ran migration `lib/db/migrations/manual/2026-09-02-shanebuilder-feature-flags-2307.sql`
  against local Postgres (new `shanebuilder_feature_flags` table) — insert/select/delete
  round-trip confirmed via `psql`. Live-verified: the epic-detail GraphQL query against real
  Epic #2198 (36 features/38 leaves, 28 closed, avg cycle 6.1h, burn rate computed by hand and
  checked); the `blocked_by` REST call against real issue #1867→#1909; the exact
  `updateProjectV2ItemFieldValue` mutation shape by using it live to move the Build-Queue-fixture
  finding (#2413) to AI Batter Up on the real project board. `dotnet build ShaneBuilder.csproj`
  — 0 warnings, 0 errors, both before and after rebasing twice onto a fast-moving `main` (real
  conflicts in `MainWindow.GitPanel.cs`/`Services/QueueReadClient.cs` against concurrent
  #2301/#2302/#2309/#2312 work, resolved by adopting their better `GetLatestByGithubNumbersAsync`
  over my own duplicate, and moving the Epic dispatch to the real per-kind branch point their
  refactor introduced rather than my stale surgical-diff location).
  Closed #2303/#2304/#2305/#2306/#2307 individually with real verification evidence in each
  comment, per this issue's own dispatch instruction. Left **#2308 open** — Git Map + Command
  Palette propagation is real and verified (single-sourced `OverlayParkPauseAsync`, wired into
  the one real builder both the mini rail and full Git Map doc tab share), but Build Queue
  propagation is genuinely not done: `_queueItems` there is pre-existing fixture data, not real
  `bt_build_queue` rows, so badging it with real state would be decorating fiction. Filed **#2413**
  (Build Queue fixture-seed finding), parented under #2289, board status set to AI Batter Up.
  `verify-branch-merged.mjs` confirmed merged before writing DONE; `git status --porcelain`
  clean throughout.
