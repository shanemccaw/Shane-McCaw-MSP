# #3238 — Feature: Tesla battery/charging-aware Money nudges (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-08T19:05:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** (pending)

## Log
- 2026-09-08T19:05:00Z ⏳ IN FLIGHT — pulled #3238's full body. Two real, distinct asks:
  (1) low-battery-for-tomorrow's-commute nudge with a real, non-fabricated cost estimate,
  (2) failed-Tesla-payment nudge, explicitly flagged by the issue itself as uncertain and
  to investigate rather than assume buildable.
  Checked the `blocked_by 3158` edge: #3158 carries `complete` label (code shipped,
  hands-on verified against local DB; only the live OAuth round-trip to Tesla's own
  servers is unexercised, pending Shane setting real `TESLA_CLIENT_ID`/`SECRET` on
  Replit — that's #3158's own stated `Shane To-Do`, not a blocker on writing more real
  code against the same, already-real infrastructure). Per the "picking this back up"
  rule, a `complete`-labeled blocker means unblocked. Proceeding.
  Read the shipped `web/shanes-life/src/core/tesla.mjs` (Git #3158): real OAuth,
  encrypted token storage, `fleetGet()` helper, vehicle listing, on-demand climate read
  — all real, documented Fleet API (`vehicle_device_data` scope). No charging/cost code
  exists yet.
  Checked #3217 (Money Cars real charging/mileage tracking, same milestone) for overlap
  per the issue's own "check that issue's own scope before duplicating the
  charging-data-fetch logic" instruction — #3217 is currently `BLOCKED` on #3158 (its own
  bookend, written before #3158 landed) with **no code written**, so there is nothing to
  duplicate. Noted for whoever picks up #3217 next: this build adds a real
  `getChargeState()` read in `tesla.mjs` they should reuse rather than re-fetch.
  **Real investigation of idea #2 (payment-failure signal)**, done via WebSearch/WebFetch
  against Tesla's own developer docs (no live API access exists to test directly — deploy
  #3199 is still open): `developer.tesla.com/docs/fleet-api/endpoints/charging-endpoints`
  documents exactly three charging endpoints — `GET /api/1/dx/charging/history` (paginated
  history), `GET /api/1/dx/charging/invoice/{id}` (returns a **PDF** of a charging invoice
  for one historical event), and `GET /api/1/dx/charging/sessions` (pricing/energy data,
  **Business fleet owners only** — not available to a personal developer account like
  Shane's). None of Tesla's own documentation publishes a response-field schema for any of
  these three endpoints (confirmed by direct fetch of the docs page: "the documentation
  does not specify any response fields"), and community reverse-engineering
  (`github.com/timdorr/tesla-api` discussions) independently confirms the same gap — no
  documented or reverse-engineered field anywhere expresses a payment/invoice status
  (paid/unpaid/declined/failed). The only structured "invoice" artifact Tesla exposes is a
  PDF file, not a queryable status. **Conclusion: idea #2 is not buildable against Tesla's
  real, documented API** — there is no real signal to read, so building it would mean
  either fabricating a status field that doesn't exist or scraping a PDF, neither of which
  is real. Per the issue's own explicit instruction ("should be dropped ... not faked"),
  dropping idea #2 rather than building a fake affordance. Filing this as its own finding
  (see below) so it's tracked, not just noted in prose.
  **Idea #1's own cost-estimate assumption also doesn't hold as stated.** The issue
  proposed "kWh needed × Tesla's own real per-kWh Supercharger rate" — but the same
  investigation shows Tesla's Fleet API has no documented, structured per-kWh pricing
  field either (`nearby_charging_sites`' response schema is equally undocumented; pricing
  only ever surfaces as a PDF invoice for a session that already happened, useless for a
  forward-looking "will this cost you" estimate). This mirrors #3217's own honest fallback
  for the same underlying gap ("leave home charging as a real, stated-by-Shane estimate
  rather than fabricating a computed number Tesla doesn't actually provide") — same real
  API limitation, same real fix: the cost estimate is computed from Shane's own real,
  self-entered settings (his commute distance, his car's real efficiency, his real
  electricity/charging rate), not a value invented or scraped from Tesla. The battery
  shortfall itself (does tonight's charge cover tomorrow's drive) *is* real, live Tesla
  data (`charge_state.battery_range`, documented, real).
  **Building idea #1** now: migration adding real commute-nudge settings to
  `tesla_accounts` (opt-in, all Shane-entered), `getChargeState()` +
  `checkLowBatteryForCommute()` in `tesla.mjs`, wired into the existing 6-hour
  `housekeeping` sweep in `server.mjs` (same real pattern as the day-before appointment
  reminder / vaccine lead reminder — this is a second real, deliberate "look ahead to
  tomorrow" clock exception, not a violation of context-over-clock; it is the same shape
  as the app's own existing day-before exception), a Settings UI section, and API routes.
