# #4041 — Break-glass: persist a credential-uncertain marker after outcome_unknown / replacement_unrecorded overrides

- **Status:** ✅ DONE 2026-09-14T15:26:19Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8d49187b7 (marker + refusals + #4076 callback routing fix + tests + contract packs); 374ddc338 (IN FLIGHT bookend)
- **Migration:** lib/db/migrations/manual/4041-break-glass-pending-secret-credential-uncertain.sql (run locally; appended to #1630)
- **Findings filed:** #4076 (verify/:token shadowed the OAuth callback since 24b7bce05; routing fixed in 8d49187b7, real GA sign-in still unverified; bug+security, sub-issue of #1651, AI Batter Up)

## Log
- 2026-09-14T14:02:31Z ⏳ IN FLIGHT — reading performBreakGlassAdminOverride, the invite/reveal routes and #4029's live harness; plan is a nullable credential_uncertain_at column set on outcome_unknown + replacement_unrecorded, cleared on a successful override, refusing invite/reveal while set (queue build 2734, Opus/High)
- 2026-09-14T15:26:19Z ✅ DONE — Added break_glass_pending_secrets.credential_uncertain_at. #4040 (e10edaba5) landed mid-build and rewrote the override, so the marker was rebuilt on its claim. The claim UPDATE sets coalesce(credential_uncertain_at, now()) before store or tenant, which also covers a crash between reset and record. releaseClaim(why, outcome): definite hand-backs restore the pre-claim value; outcome_unknown and replacement_unrecorded keep it. The record tx clears it on the superseded row. While marked, invite answers 409 detail:"credential_uncertain" pointing at admin-override. Verify link and callback answer 409 before any redirect or code exchange (the callback re-checks after its claim) and retire the link to superseded so it cannot block the recovery override. credentialUncertainAt served on portal by-run/handoffs and MSP list/detail (types updated; no UI renders it yet). Contract packs updated. Found and fixed #4076: verify/:token was registered before verify/callback and answered Microsoft's redirect as token "callback" with 410 (confirmed on the running :8080 dev server), so the role check and reveal were unreachable; :token now next()s "callback". Verification: unit suite 22/22; tsc exit 0 for api-server (on the landed commit), msp-console and portal. Live harness 12/12 on zz-test-graphwrite-01 (GENERATED_SECRET_VAULT_URL="", Graph + Postgres real): replacement_unrecorded reproduced with a real reset, marker set; invite 409 with 0 emails and 0 links; verify link 409 with no redirect; callback 409 with 0 token-endpoint calls; both links superseded; recovery override ok with the marker cleared; on the replacement, invite 200 (sent 1), verify link 302 to the testbed authorize URL, callback reached Microsoft's token endpoint (400 for the placeholder code); #4040's concurrent pair still passes. Earlier live runs were invalid rather than failing: one overlapped #4040's own harness on the same DB and synthetic user (timeouts), one hit a duplicate declaration at transform (no tests ran), and one exposed #4076. Honest gaps: outcome_unknown pinned by unit tests only; the invite email, the portal session and (for success paths) the store were stubbed, as in #4029; no real GA sign-in, reveal or acknowledge; the repeated-override alert went to the platform mailbox during the run. Cleanup: 0 pending secrets and 0 synthetic definitions/runs/audit rows remain. The pre-existing tree was clean.
