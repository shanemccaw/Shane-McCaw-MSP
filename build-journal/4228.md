# Build Journal — #4228

- **Status:** ✅ DONE 2026-09-15T12:45:00Z
- **Scope:** platform

## Log

- 2026-09-15T12:37:21Z — Started. `azure_tenant_credentials.created_at`/`updated_at` are naive
  `timestamp` columns serialized as if UTC while the local server writes NY wall-clock (`SHOW
  timezone` confirmed `America/New_York`). Converting both to `timestamptz` via
  `ALTER COLUMN ... TYPE timestamptz USING <col> AT TIME ZONE 'America/New_York'`, and setting
  `withTimezone: true` in the Drizzle schema (`lib/db/src/schema/index.ts:2084-2085`).
- 2026-09-15T12:45:00Z — ✅ DONE — Ran
  `lib/db/migrations/manual/4228-azure-tenant-credentials-timestamptz.sql` against local
  `DATABASE_URL`. Table was empty (no legacy rows needed reinterpretation), so verified with a
  synthetic `zz-test-4228` row: after migration, `created_at`/`updated_at` serialize with the
  correct `-04` (EDT) offset and `created_at AT TIME ZONE 'UTC'` matched real wall-clock UTC —
  confirming the fix and no longer 4h early. Test row deleted after verification. Schema updated
  to `withTimezone: true` at `lib/db/src/schema/index.ts:2084-2085`. Commit `332cd4e0`, merged
  into `origin/main` (verified via `verify-branch-merged.mjs`). Working tree clean.
