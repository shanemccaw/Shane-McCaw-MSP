# #2120 — Continue/Resume-Session/Recover-All resume paths leave the original queue row disconnected (same family as #2119)

- **Status:** ✅ DONE 2026-09-06T15:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 2d6acd069

## Log
- 2026-09-06T15:05:00Z ⏳ IN FLIGHT — wiring the 4 documented resume/continue call sites
  (BuildWatchWindow Continue, GitDetailView Continue, BuildQueuePanel "▶ Resume Session
  (crash recovery)", BuildQueuePanel "Recover All") to the #2119 supersede mechanism, plus
  a new narrow `MarkOrphanSupersededByResumeAsync` for the two crash-recovery sites since
  `MarkSupersededByReplyAsync`'s guard is a no-op for `failed` originals.
- 2026-09-06T15:35:00Z ✅ DONE — commit 2d6acd069 (rebased onto main as 174571998).
  Wired all 4 audited sites:
  - `BuildWatchWindow.xaml.cs` SendSlotInput continuation → `MarkSupersededByReplyAsync`
  - `Controls/GitDetailView.cs` HandleContinuation → `MarkSupersededByReplyAsync`
  - `Controls/BuildQueuePanel.xaml.cs` "▶ Resume Session (crash recovery)" →
    new `MarkOrphanSupersededByResumeAsync`
  - `Controls/BuildQueuePanel.xaml.cs` `BtnRecoverOrphans_Click` ("Recover All") →
    new `MarkOrphanSupersededByResumeAsync` per recovered item
  Added `BuildQueuePostgresClient.MarkOrphanSupersededByResumeAsync` — narrowly scoped
  to `status='failed' AND exit_code=-2` (the orphan sentinel) so a genuine non-orphan
  failure is never silently relabeled. Flipping the original off `failed` automatically
  clears it from `UpdateOrphanRecoveryBanner`/the Crashed filter, which both test
  exactly that status+exit_code pair — no separate banner/filter code change needed.
  Verification: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0
  warnings, 0 errors. `verify-branch-merged.mjs` confirms merged into origin/main.
  `git status --porcelain` clean.
  Filed as a follow-up finding (out of #2120's audited scope, not folded in):
  #3008 — `BuildWatchWindow.ResumeAdoptedSlot` (Git #1878) has the same shape but its
  original row is typically still `running`, so neither existing guard fits without
  further widening; parented under Feature #1788, board status AI Batter Up.
