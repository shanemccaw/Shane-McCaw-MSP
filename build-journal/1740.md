# #1740 — ShaneBot + Active Cards: regenerate the contract pack from the finished backend

- **Status:** ✅ DONE 2026-09-03T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 0d7b533cb (bookend IN FLIGHT), f849b48bb → pushed as 8953c841f (contract pack regeneration)

## Log
- 2026-09-03T00:00:00Z ⏳ IN FLIGHT — regenerating docs/shanebot-contract-pack.md wholesale now that #2519 (chat shell + card renderers) and #1624 (specific-card-first ordering) landed since the prior pack (fc3466355). Read-only pack build, per #1642 pattern.
- 2026-09-03T00:00:00Z ✅ DONE — `docs/shanebot-contract-pack.md` regenerated wholesale (commit `8953c841f`, pushed to `origin/main`, verified an ancestor via `scripts/dev-server/verify-branch-merged.mjs`). Read-only — no product code, schema, or UI changed. Real work done:
  - Confirmed §10's prior sub-issue #1622 (no portal renderer) is CLOSED and built by #2519; documented the real `artifacts/portal` chat shell + 4 Active Card renderer components (new §8), citing real files/lines/testids.
  - Confirmed §4.4/§9's prior OPEN GAP (no prompt bias toward specific cards over `data-answer`) is CLOSED by #1624; updated §2/§4.4/§7 with the real `cardsBlock` text as it exists today.
  - Re-verified every `lib/db/src/schema/index.ts` citation against current `main` — an unrelated commit (Git #1496, `canApproveChanges`) shifted every citation below line 100 by +34; all schema line numbers in the pack are now correct against HEAD, not copied forward.
  - Added §12 (real enum/vocabulary honesty check) confirming all 3 card-status-relevant columns (`invoicesTable.status`, `mspSalesBundleAssignmentsTable.status`, `mspDiagnosticRunsTable.status`) are `text` + named-constant literals, not `pgEnum` — real citations, not asserted.
  - No new sub-issue filed: the two remaining OPEN GAPs (`finding`, `ticket` card backing) are already tracked on #1616/#1489; the no-nav-entry-point observation is already covered by the open #1741 (Design)/#1742 (wire) chain; the action-layer (`proposedRemediation`/`proposedAction`, regenerate-document/rerun-scan) orphan is out of #1616's scope and already covered by the separate #2450 pack (`docs/requests-and-support-chat-contract-pack.md`).
  - `git status --porcelain` clean; no other files touched.
