# #4456 — Microsoft Changes: unreviewed announcements hide real content, show "unreviewed" pill instead

- **Status:** ✅ DONE 2026-09-17T03:01:28Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 549064d4b (fix), 926ff67ad (manifest verification note; rebased onto main as 1615073f5)

## Log
- 2026-09-17T02:49:22Z ⏳ IN FLIGHT — investigating microsoft-changes.tsx and msp_message_center_items schema to replace the generic "not yet analysed" count panel with real announcement content + an unreviewed pill.
- 2026-09-17T03:01:28Z ✅ DONE — `artifacts/portal/src/pages/microsoft-changes.tsx`: the "In this wave" panel's `wavePosts.filter(p => p.analysis !== null)` was dropping every unanalysed post and rendering only a generic "N announcements · not yet analysed" count. Replaced with a per-post branch that renders the real `p.title`/`p.workload`/`p.publishedAt` alongside an "Unreviewed — awaiting interpretation" pill, matching the existing measured/noise/unmeasured row patterns; removed the now-redundant count-only block. No schema/backend change needed — `WirePost`/`msp_message_center_items` already carried every field used. tsc --noEmit clean on @workspace/portal. Confirmed this is the live real case: DATABASE_URL shows 472 `msp_message_center_items` rows, 0 `m365_change_interpretations` rows (every on-axis post currently analysis=null). Test manifest `test-manifests/portal/microsoft-changes.json` updated with tagged UI10/UI11 steps (sessionTag `4456-unreviewed-pill`) asserting the real content + pill; merged commit 549064d4 into the dev-server checkout via `request-restart.mjs` and fired `shaneapp://runTest?file=microsoft-changes.json&onlyTag=4456-unreviewed-pill` — the SETUP login steps failed with `unresolved variable(s): {{TEST_CUSTOMER_EMAIL}}/{{TEST_CUSTOMER_PASSWORD}}` before UI10/UI11 could run (BuildConsole's Test Environment Variables aren't currently configured for this pair — a regression of #4119, closed 2026-09-15 as fixed). Filed as new finding **#4470**, parented under #1494 (Feature: Microsoft Changes), labeled `bug` + `Shane To-Do`, board status set to AI Batter Up. `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
