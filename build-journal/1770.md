# #1770 — AdminV2 Active Directory: Run scan cannot choose a package

- **Status:** ✅ DONE 2026-09-10T02:21:40Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** dc2ee6972 (implementation), bbb56234a (contract pack), rebased/pushed to `main` as 05e21f25b

## Log
- 2026-09-10T01:51:47Z ⏳ IN FLIGHT — Investigation done (msp-diagnostics.ts resolution chain, adApi.ts/AdCustomerCanvas.tsx call sites, adKit.tsx primitives, monitoring_packages/monitoring_package_checks schema confirmed). About to: (1) add `GET /api/msp/monitoring-packages` (requireRole MSPOperator) listing active packages with real check counts, (2) add `AdSelect` primitive to adKit.tsx, (3) thread `packageKey` through `runAdCustomerDiagnostics` + a picker in `AdCustomerCanvas.tsx` (default to the customer's resolved subscription package via the existing monitoring-package endpoint; skip the prompt when only one package exists), (4) write the contract pack under `docs/msp-console/`. Server's existing resolution fallback chain in `msp-diagnostics.ts:242-261` is untouched.
- 2026-09-10T02:21:40Z ✅ DONE — Built exactly per the issue's own scope. New `GET /api/msp/monitoring-packages` (`msp-diagnostics.ts`, `requireRole("MSPOperator")`) lists real, active `monitoring_packages` with a real `LEFT JOIN monitoring_package_checks` check count, `HAVING count(...) > 0` to exclude 10 active-but-zero-check `cat-*` rows (confirmed live: a different feature, dashboard category tabs, reusing this same table — filed as **#3453**, parented under #1571). New `AdSelect` primitive in `adKit.tsx`. `runAdCustomerDiagnostics(adminFetch, customerId, packageKey?)` now threads an optional override to the existing, **unmodified** server resolution chain. `AdCustomerCanvas.tsx`'s run-scan button/ribbon action opens an inline picker (defaulted to the customer's resolved subscription package via the already-shipped `GET .../monitoring-package` endpoint) only when more than one real package exists; one package (or a failed catalog load) keeps the pre-existing one-click behavior unchanged. Verified: `api-server` + `admin-panel` typecheck clean; `msp-diagnostics.test.ts` (11 tests incl. one new for the route) passes; real check-count query run live against local `DATABASE_URL` before and after the `HAVING` filter (21 active rows → 11 real runnable packages, 3–198 checks each). Contract pack: extended the existing `docs/msp-console/diagnostics-msp-console-contract-pack.md` (#3354) rather than duplicating it — new route documented, consumer map updated (2 previously-orphaned routes now consumed), every pre-existing route's line citation re-verified against the file's real current lines (1264/14, was 1214/13). Finding **#3453** filed (bug, parented #1571, board status set to AI Batter Up) for the dead `cat-*` catalog rows. Branch verified merged into `origin/main` (`verify-branch-merged.mjs` exit 0); `git status --porcelain` clean.
