# #3799 — Password-expiration monitor checks never fire (empty severity_rules) and are duplicative

- **Status:** ✅ DONE 2026-09-13T06:45:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** e964fb4a33744ac509a10cce80c6f752cf4ed424

## Log
- 2026-09-13T06:20:00Z ⏳ IN FLIGHT — pulled issue body + comment, investigating monitor_checks rows for `identity:password-expiration-policy` and `platform:tenant-password-expiration`, comparing severity_rules shape used by comparable password-policy checks, and checking real usage of each profile key before deciding keep-both vs consolidate.
- 2026-09-13T06:45:00Z ✅ DONE — Confirmed live: both checks had `severity_rules=[]`. Checked real usage before deciding: `platform:tenant-password-expiration`'s `monitoring_package_checks` assignments (core:growth, core:premier, core:free-scan-full) are a strict subset of `identity:password-expiration-policy`'s (those three plus core:enhanced-monitoring, detail:full-item-collection); both already have their own published `remediation_knowledge_base` rows and real historical `tenant_monitor_profiles`/`msp_diagnostic_findings` rows (11 + 1 each, live tenant data). Consolidating would hard-delete real historical data for zero net package-coverage gain — a catalog-owner call, not a same-session fix — so decided to KEEP BOTH and give each real `severity_rules` instead. Wrote and ran `lib/db/migrations/manual/2026-09-13-password-expiration-severity-rules-3799.sql` myself against local Postgres (`shanemccawmsp`): both checks now fire `severity: "warning"` when the collected `passwordExpirationDays`/`tenantPasswordExpirationDays` is a finite validity period (`< 2147483647`, Microsoft Graph's own "never expire" sentinel, confirmed live in this tenant's own `extracted_properties`), guarded against `null` so a failed/missing collection can never falsely fire. Verified the UPDATE landed (`SELECT severity_rules FROM monitor_checks WHERE key IN (...)`) and traced the real `evalConditionGrammar`/`resolvePathInData` grammar (`artifacts/api-server/src/lib/monitor-executor.ts`) against this tenant's real collected value (2147483647 → no finding), a synthetic finite value (90 → finding), and null (→ no finding). No code changes needed — `severity_rules` is pure DB config, no literal check-key references found in `artifacts/`. `git status --porcelain` clean, branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
