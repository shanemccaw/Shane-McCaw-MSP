# #3955 — Wire FreeScan.tsx price card to real Products Catalog pricing

- **Status:** ✅ DONE 2026-09-14T13:22:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 42a048ea1 (IN FLIGHT bookend), 02db70296 (real work, on origin/main)

## Log
- 2026-09-14T13:09:58Z ⏳ IN FLIGHT — replace FreeScan.tsx's hardcoded QUOTE_PRICE/QUOTE_BASIS
  with a real read from the monitoring_tier catalog (useCatalog), matching the same Growth-tier
  default and 250-seat fallback Buy.tsx's own ?product=monitoring&scanned=1 checkout uses.
- 2026-09-14T13:22:00Z ✅ DONE — FreeScan.tsx now reads the real monitoring_tier catalog
  (useCatalog) for its upsell price card: finds the live "Growth" tier row whose seat band
  covers 250 seats (Buy.tsx's own `st.seatsFromCatalog || 250` fallback for a
  ?product=monitoring&scanned=1 arrival with no known seat count — the free-scan results wire
  carries no seat field), computes the price with the same formula
  resolveTypeAttributesMonthlyPriceCents/Monitoring.tsx's computeMonthlyPrice use
  (pricePerUserMonth × max(seats, seatCountFloor) + flatMonthlySurcharge), and derives the
  "Growth tier, 250 seats, <real tenantTierLabel> bracket" copy from that same row instead of
  the old fabricated "1,240 seats, Enterprise bracket" string. Verified: `tsc --noEmit` clean,
  `vite build` succeeds. Real catalog checked live via DATABASE_URL — 250 seats resolves to the
  Mid-Market band (monitoring-growth-mid-market, $11/seat, seatCountFloor 101 → $2,750/mo), not
  the old copy's claimed Enterprise bracket. No fixture module added; no seat count invented.
  Pushed straight to origin/main (rebased onto 1b07421c0) since this worktree branch had no
  other unmerged commits. Working tree clean. No mid-build findings warranted a new issue.
