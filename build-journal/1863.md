# #1863 — AI Batter Up: sort highest issue number first, add search-by-number filter

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** a63efe991 (bookend IN FLIGHT), 3a8f4f331 → rebased+pushed to main as a8d808d11

## Log
- 2026-08-30 ⏳ IN FLIGHT — sorting rows descending by Number in AiBatterUpPanel.RefreshAsync
  (confirmed AiBatterUpQueueService has no other caller besides this panel — grep found only
  AiBatterUpPanel.xaml.cs and the service's own file, so sorting in the panel doesn't affect
  anyone else), and adding a compact number/title filter TextBox into the existing 3-column
  header Grid without regressing #1816's single-row collapsed/empty footprint.
- 2026-08-30 ✅ DONE (a8d808d11 on main) — Sorted `_allRows` (`AiBatterUpPanel.xaml.cs`)
  `OrderByDescending(r => r.Number)` in `RefreshAsync`, in the panel only (service confirmed
  single-caller). Added `TxtFilter` (overlay-placeholder TextBox, same trick as the Build
  Queue's #950 `QueueSearchBox`) into `FilterBoxHost`, docked into the existing header Grid's
  column 1 alongside `TxtCount` — no new row. Filter matches numeric substring on
  `r.Number.ToString()` OR case-insensitive substring on `r.Title`; empty filtered result
  shows an inline "no match for …" TextBlock in `RowsList` instead of an empty panel.
  `FilterBoxHost` hides whenever rows==0 or the panel is collapsed (`UpdateFilterBoxVisibility`,
  called from `RefreshAsync` and `BtnCollapse_Click`). Filtering never touches
  `RefreshAsync`'s fetch or `ApplyDecisionAsync`'s Yes/No mutations — both still act on the
  original `AiBatterUpRow` in hand.

  **Verification (real data, measured, not eyeballed):**
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors,
    both before and after the change.
  - Pulled the real "AI Batter Up" board via `gh api graphql` against project
    `PVT_kwHOEiBDdc4BeoiY` / option `a0296971` (the exact query/constants
    `AiBatterUpQueueService`/`GetAiBatterUpIssuesAsync` use) — 15 real open rows, numbers
    1776–1855. Fed them into the panel out-of-order via reflection (`_allRows` field +
    `RenderFilteredRows`) and read back each rendered card's number badge: rendered order was
    `1855,1854,1853,1852,1849,1848,1846,1840,1830,1807,1800,1781,1778,1777,1776` —
    exactly number-descending against the real list.
  - Filter `"18"` (numeric substring) → `1855,1854,1853,1852,1849,1848,1846,1840,1830,1807,1800`
    (correctly excludes 1776/1777/1778/1781, which don't contain "18").
  - Filter `"9999"` (matches nothing) → rendered `no match for "9999"` in the row area, not an
    empty panel.
  - Filter `"ps-execution"` (non-numeric) → title-matched `#1854, #1852` correctly.
  - Clearing the filter → restored the full 15-row sorted list without a refresh.
  - `FilterBoxHost` visibility: `Visible` with rows present + expanded; `Collapsed` with zero
    rows; `Collapsed` when `BtnCollapse` is checked (rows present) — confirmed by reflection
    calls to `UpdateFilterBoxVisibility`.
  - Collapsed/empty footprint measured via a scratch off-screen WPF harness
    (`panel.Measure`/`Arrange` at width 260, with `App.xaml`'s three merged resource
    dictionaries loaded so `StaticResource` lookups resolve): **30.29px** for both the
    collapsed (`BtnCollapse` checked) and expanded-but-zero-rows cases, identical before and
    after this change — the filter box adds no height in either state. Harness was scratch,
    lived outside the repo (`C:\wt\_scratch_measure`), and was deleted after use.
  - Branch merged: `node scripts/dev-server/verify-branch-merged.mjs` → OK, `agent/1863-q960`
    (a8d808d1) confirmed ancestor of `origin/main`.
  - `git status --porcelain` clean at time of writing this bookend.

  No findings filed — no gaps discovered outside the stated scope.
