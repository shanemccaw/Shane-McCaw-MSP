# #1487 — Portal New Design: Risk Register / RBD — contract extraction pack

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `ef0e50dad` (pack), merged to `origin/main` at `14b5f2ac7`


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1487`, but #1487 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1487 is complete.
>
> #1487's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Extracting the Risk Register / RBD contract pack
  (step 3 of #1578, per #1577) to `docs/risk-register-contract-pack.md`.
  READ-ONLY: no product code, schema, or UI changes. Wire contracts cited to
  file:line, CURRENT vs DECIDED marked, real enum unions only, known-wrong
  `status = expired` marked DECIDED-wrong per #1507/#1527.
- 2026-08-29 ✅ DONE — Shipped `docs/risk-register-contract-pack.md`. All three
  live customer endpoints (risk-register GET, policy-decisions GET, accept POST)
  are consumed by `riskRegisterLive.ts`, so no orphan sub-issue was filed — stated
  in the pack. Real enum unions extracted from the `msp-rbd.ts` zod validators +
  schema comments (honestly noting they are `text` columns, not `pgEnum`).
  `status`/`decision_state` = `expired` marked DECIDED-wrong. CURRENT vs DECIDED
  table with every DECIDED row carrying an issue (#1507–#1514); two OPEN GAPS
  called out (#1523 RACI source; residual-score-under-old-signature, no issue yet).
  Tri-state, forbidden list, cross-surface edges (incl. uncalled `check_key` edge)
  documented. READ-ONLY: no product code/schema/UI changed. Verified merged to
  `origin/main` at `14b5f2ac7` via verify-branch-merged.mjs (exit 0).
