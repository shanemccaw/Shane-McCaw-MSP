# #3762 — Status Reports: real schema + endpoints (Phase 1 of 4)

- **Status:** ✅ DONE 2026-09-12T04:06:39Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 02d8db7f8

## Log
- 2026-09-12T04:00:37Z ⏳ IN FLIGHT — investigated #1293's retainer-hours schema pattern and the most recent MSP Console route (#2630 msp-break-glass.ts) as the real convention to follow; about to add `msp_status_reports` table + `/api/msp/*` routes.
- 2026-09-12T04:06:39Z ✅ DONE — Added `msp_status_reports` Drizzle schema (`lib/db/src/schema/msp.ts`) with `draft`/`published` state machine; wrote and ran `lib/db/migrations/manual/2026-09-12-msp-status-reports-3762.sql` against local Postgres (table + 3 indexes created, self-marking INSERT ran). Added `artifacts/api-server/src/routes/msp-status-reports.ts` with the 5 real endpoints (`POST create`, `PATCH` draft-only edit rejecting published with 409, `POST publish` irreversible with 409 on re-publish, `GET list` paginated, `GET one`), gated by `requireCapability("ladder.msp-operator")` + `assertCustomerAccess` on every `:customerId`/report-owning route — same pattern as `msp-break-glass.ts` (#2630). Wired into `routes/index.ts`. Verified: `tsc --build` on libs clean, api-server `pnpm run typecheck` clean, and a real direct-Postgres transaction (insert draft → update to published with `published_at` stamped → rollback) confirmed the full state machine against the live local `msp_status_reports` table, then confirmed the table is empty again. No dev API server was running locally to exercise the routes over HTTP; the restart/merge-back coordinator will pick this up. Commit: `02d8db7f8`. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. No findings filed — no genuine bug/gap hit outside scope during this build.
