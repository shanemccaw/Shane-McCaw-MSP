# #2349 — Import: features auto-match from section or body text; the dropdown is a fallback for unmatched

- **Status:** ⏳ IN FLIGHT 2026-09-02T20:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Test Pad Import, Feature: Test Pad #2326)
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T20:20:00Z ⏳ IN FLIGHT — real feature auto-match for Test Pad's Notepad Import
  (#2343). `TestPadFeatureMatcher` (new) scores each candidate's `Section` (#2344) and body text
  against the live "Feature:" issues from `GitIssuesService.GetActiveFeaturesAsync()` and sets
  `TestPadImportCandidate.MatchedFeature` (the string field #2350 already scaffolded). Every
  preview row gets a real feature dropdown, pre-selected to the auto-match, always offering every
  active feature plus "— No feature —" — the fallback for an unmatched row and the correction
  path for a wrong auto-match. `BtnImport_Click` now stamps `TestPadNote.Feature` from it.
  Rebased/re-applied twice against fast-moving origin/main (#2344/#2350/#2351/#2348 all landed
  concurrently) — final version applies on top of all four.
