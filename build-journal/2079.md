# #2079 — Chat tab header: show linked Git issue number — [#N] Title

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** d84028bba (also on main as 38b0aef4c after rebase+push)

## Log
- 2026-08-31 ⏳ IN FLIGHT — auditing OpenChatTab headerPanel (~MainWindow.xaml.cs:2926-2951), the #1802 reverse-parse near ~line 1363, and RenameTabDialog/PersistedChatTab round-trip before making the change.
- 2026-08-31 ✅ DONE — Audit findings: (1) OpenChatTab's headerPanel title TextBlock (~line 2947-2952) was `chat.Title` only, no linked-issue rendering. (2) The #1802 reverse-parse regex `TabTitleIssueNumberRegex` (~line 1368) already matches `[#N] Title` as its primary pattern — it was written anticipating this exact prefix, so adding `[#{githubNumber}] {chat.Title}` when `githubNumber.HasValue` does NOT collide with or need changes to that path; confirmed unaffected. (3) Found a real collision in `RenameTab` (~line 4506): it read the header TextBlock's raw `.Text` as `currentTitle` for the rename dialog and wrote `dlg.NewTabName` straight back into both the TextBlock and `chat.Title` — with the `[#N] ` prefix now live in that TextBlock, an untouched rename would have baked the prefix permanently into `chat.Title` (which `PersistOpenChatTabs()` persists), producing a doubled `[#N] [#N] Title` on next tab open. Fixed by stripping the linked-issue prefix (looked up via `_chatTabs[tabItem].GithubNumber`) before showing the dialog and re-applying it to the display text only after save, leaving `chat.Title`/persisted title clean. `PersistOpenChatTabs` itself (~line 3586) already stores `chat.Title`, not header text, so it needed no change. Verified: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 warnings, 0 errors. Commit d84028bba pushed to `agent/2079-q1068`, then rebased onto latest `origin/main` and pushed directly to `main` as 38b0aef4c per this repo's "commit to main" convention; `verify-branch-merged.mjs` confirms ancestor-of-main. `git status --porcelain` clean.
