# #3134 — URGENT: Batter Up / AI Batter Up panels still do their own live GitHub project-page scans — redirect to #3113's mirror

- **Status:** ✅ DONE 2026-09-07T23:28:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** `3b5ab9574` (work), `003b7ea67` (IN FLIGHT bookend) — both on origin/main; DONE bookend commit follows

Sub-issue of #1789 (Feature: Dev Server & Deploy Infrastructure, BuildConsole).

The last piece connecting Shane's most-visible pain point (Batter Up never filling in)
to the #3113 mirror. Batter Up / AI Batter Up's own panel scans — both the regular
open-item scan (`GetBatterUpIssuesAsync` / `GetAiBatterUpIssuesAsync`) and the
`(closed sweep)` pass (`GetClosedBatterUpIssuesAsync` / `GetClosedAiBatterUpIssuesAsync`)
— were never in #3113's redirect list, so they keep firing their own separate live
paginated GraphQL project-page walks for data the mirror's whole-board sweep already has.

## Freshness audit (issue asked me to confirm before assuming)
Live DB check @ 2026-09-07T23:xx: mirror last full sync 1.1m ago, ok; whole-board sweep
captured 3115 board statuses. `bt_issue_mirror` holds 20 OPEN in Batter Up (opt
`09b1927f`), 50 OPEN in AI Batter Up (opt `a0296971`), and — critically — 3 CLOSED still
sitting in Batter Up (#3110, #3112, #3114), exactly the closed-sweep targets. So the
open→closed transition preserves board status in the mirror (mark-closed keeps the
option id), which means the closed-sweep case is genuinely covered. 5-min interval is
sufficient: the pain point is "never fills in" (the big live walk fails under rate
limiting), not "5-min lag too slow" — a reliable 5-min-fresh read beats a live walk that
never completes.

## Plan
1. Add `GitHubIssueMirror.TryGetByBoardStatusAsync(optionId, state?)` → `List<MirrorIssue>?`
   (null = not-usable/error → caller falls back to live, per #3113's universal pattern).
2. Expose `AiBatterUpOptionId` publicly (Batter Up already has public `BatterUpPromoteOptionId`).
3. Redirect `BatterUpQueueService` (open scan + closed sweep) mirror-first, live fallback.
4. Redirect `AiBatterUpQueueService` (open scan + closed sweep) mirror-first, live fallback.
   AI Batter Up Yes/No needs the ProjectV2Item id, which the mirror doesn't store — switch
   Yes/No to the existing by-number `SetIssueStatusByNumberAsync` (resolves item id at
   click-time; a click is a live write anyway, not part of the refresh).
5. Build BuildConsole (0 warnings), verify no live project-page fetch in a refresh.

## Log
- 2026-09-07T23:12:00Z ⏳ IN FLIGHT — Opening bookend. Read #3113's full landed diff
  (`GitHubIssueMirror` schema/read API), confirmed mirror freshness via live DB query
  (see audit above). Implementing the redirect next.
- 2026-09-07T23:28:00Z ✅ DONE — Shipped in `3b5ab9574`. Both panels' board scans now read
  the local mirror first, falling back to the live project-page walk only when the mirror
  has no usable data yet (never synced) or errors — the exact #3113 fail-to-live pattern.
  **Added:** `GitHubIssueMirror.TryGetByBoardStatusAsync(optionId, state?)` →
  `List<MirrorIssue>?` (null = fall back to live; non-null authoritative because the sweep
  captured the whole board). **Redirected:** `BatterUpQueueService` open scan + closed
  sweep, `AiBatterUpQueueService` open scan + closed sweep — all mirror-first. The Done-sweep
  move stays a live write (writes never read the mirror). **AI Batter Up Yes/No** switched to
  by-issue-number `SetIssueStatusByNumberAsync` (resolves the ProjectV2Item node id at
  click-time), since the mirror doesn't store that id and a click is a live write anyway, not
  part of the refresh. **Exposed** `GitHubApiClient.AiBatterUpOptionId` publicly for the
  filter. **Verified** against the live mirror (last sync 1.1m ago, ok): the panels' new reads
  return 20 open Batter Up, 50 open AI Batter Up, and closed-sweep #3110/#3112/#3114 — exactly
  what the live scans would return, all as pure local reads (zero live `project page fetch` /
  `(closed sweep)` GraphQL). **Freshness:** 5-min interval confirmed sufficient (the pain point
  is the live walk failing under rate limiting, not staleness). BuildConsole builds clean
  (0 warnings, 0 errors). Branch verified ancestor of origin/main. No findings filed — the
  redirect completed cleanly with no out-of-scope breakage discovered.
