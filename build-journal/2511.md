# #2511 — portal/presentations/:id/sign response field effectivePriceCents actually holds dollars, not cents

- **Status:** ✅ DONE 2026-09-03T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 57fdf6dc6

## Log
- 2026-09-03T00:00:00Z ⏳ IN FLIGHT — renaming `effectivePriceCents` → `effectivePrice` in `POST /api/portal/presentations/:id/sign` (`artifacts/api-server/src/routes/portal-presentations.ts`) to match the dollar-denominated value it actually carries; no live caller exists yet so this is a pure contract fix.
- 2026-09-03T00:20:00Z ✅ DONE — renamed both occurrences (destructure at line 797, response field at line 823) from `effectivePriceCents` to `effectivePrice`; no `*100` conversion added, matching the dollar-denominated flow `deriveEffectiveSowData()`/`resolveScopeAwarePrice()` already use throughout the file. No other live code references this route's field (grep confirmed the only other `effectivePriceCents` hits are an unrelated admin-panel services module, not touched). `tsc --noEmit` on the file shows only pre-existing, unrelated errors (missing `lib/db/dist` build output, `drizzle-orm`/`stripe` module resolution in this worktree) — none on the touched lines. Commit 57fdf6dc6 pushed to origin/main; verify-branch-merged.mjs confirms ancestor. `git status --porcelain` clean.
