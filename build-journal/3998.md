# #3998 — Requests and Support Chat: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:15:40Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** `51cc90388` (merged to `main`)

## Log
- 2026-09-14T04:05:34Z ⏳ IN FLIGHT — Read #3998 + #1659 + #2450 + #2512 in full. Design source is
  `Design/portal/design_handoff_full_site/screens/Requests and Support Chat.dc.html` — a single
  screen (the "Requests" list+detail+open-a-request UI), which itself links out to the existing,
  already-wired ShaneBot chat page (`artifacts/portal/src/pages/support.tsx`, route `/support`,
  wired to `POST /api/msp/support/chat` + `/escalate`). So this build's real scope is: build the
  Requests page against the 4 real `portal-customer-requests.ts` endpoints, and register it.
  Confirmed #2512 (the split 200-vs-503 Zoho-unavailable contract) is already fixed on `main`
  (commit `ac7b38446`) — detail/reply now both return 200 `configured:false` like the list route,
  so the design's stale `#2512` ledger reference will be corrected to reflect current reality,
  not copied verbatim. Next: build the page.
- 2026-09-14T04:15:40Z ✅ DONE — Built `artifacts/portal/src/pages/customer-requests.tsx` (route
  `/requests`), wired to the 4 real endpoints in `portal-customer-requests.ts` (list, detail+thread,
  reply, open-a-request) — no fixture data, every state in the design's own scene switcher handled
  (not-configured, never-opened, live list+detail, closed-request, detail-load-failure with retry,
  reply queued/failed). Registered the route in `App.tsx`, the breadcrumb in `PortalShell.tsx`, and
  an account-menu entry (`Ticket` icon) in `UserMenu.tsx`. Added reciprocal links: Requests →
  ShaneBot (design's own "Quick questions go to ShaneBot" panel) and Support → Requests (this
  session's own addition, `support-view-requests-link`), since the design's own chat surface is the
  already-wired `/support` page — no second chat drawn. Corrected the design's ledger copy: its
  `#2512` line still describes detail/reply as returning a bare 503 for the Zoho-unavailable
  condition; verified directly against the current route file that's already fixed (200
  `configured:false` on all three read/reply routes, commit `ac7b38446`), so the shipped ledger
  copy states the current, true contract instead of the design's stale one.
  Verification: `pnpm run typecheck` (artifacts/portal, `tsc --noEmit`) passed clean, both before
  and after two concurrent-session rebase merges (trivial same-line lucide-react import conflicts
  in `UserMenu.tsx`, resolved by keeping every icon; auto-merged cleanly elsewhere). Real
  `shaneapp://runTest` invoked twice: (1) `onlyTag=3998` on the manifest as pushed — returned "no
  steps matched session tag '3998'", and (2) the full file with no tag — returned 4/4 pre-existing
  apiTests passing (401 auth-gating on all 4 routes, unaffected by this build). Both results are
  the honest, expected shape documented in the manifest's own HONEST LIMIT note:
  `shaneapp://runTest` reads the dev-server's OWN deployed checkout, not this worktree, and this
  build's commit had not yet been merged into it at the time of the run (`buildset.mjs status
  RequestsSupportChat` showed `0 merged` for this member) — so the new sessionTag-3998 uiSteps
  genuinely were not there to match yet. Not a code defect; re-run once the dev-server picks up
  `51cc90388`. No genuine findings beyond the #2512-ledger staleness (fixed in-page, not filed
  separately — it was a design-copy/backend-sync issue this build corrected itself, not a live bug).
  Extended `test-manifests/portal/customer-requests.json` (already registered in
  `_regression-suite.json`) with a real Customer-session apiTest + UI render/navigation steps,
  `lastVerifiedAgainstCommit` bumped to `1f09466a4` (pre-final-rebase commit at time of edit).
  `git status --porcelain` clean at finish. `verify-branch-merged.mjs`: OK, ancestor of `origin/main`.
