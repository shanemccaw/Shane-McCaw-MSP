# #3693 — MSP-staff alert (notification) preferences

- **Status:** ✅ DONE 2026-09-11T20:52:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 49e338da7 (IN FLIGHT bookend), 4686eecf3 (real work, merged into main)

## Log
- 2026-09-11T20:09:23Z ⏳ IN FLIGHT — building real MSP-staff alert preferences, mirroring
  `notification-preferences.ts` (customer-side GET/PATCH) exactly, per real, confirmed v1.1
  scope on the issue.
- 2026-09-11T20:52:00Z ✅ DONE — Added `GET/PATCH /api/msp/settings/notification-preferences`
  (Group L in `msp-settings.ts`), gated `ladder.msp-operator`. New table
  `msp_staff_notification_preferences`, byte-for-byte the same shape as
  `customer_notification_preferences`, migration run against local `DATABASE_URL`. Wired real
  delivery gating into `createNotification()`'s `msp_user` branch
  (`notification-center.ts`'s `getMspStaffPreference()`, mirrors the pre-existing
  `customer_user` branch) — before this, `msp_user` notifications consulted no preference
  table at all. Found + fixed a real, pre-existing schema/DB drift bug hit while testing:
  `notifications.user_id` was `NOT NULL` in the live Postgres DB (nullable in the Drizzle
  schema) and the `msp_user` insert path never set it, so every real `msp_user` notification
  insert was silently failing — fixed via a `DROP NOT NULL` migration (non-destructive per
  the repo's migration gate), reproduced and confirmed fixed by the new live-db test
  (`msp-settings-notification-preferences.live-db.test.ts`, 4/4 passing). Extended
  `docs/msp-console/msp-settings-contract-pack.md` (Group L, §0/§1l/§7) per the issue's own
  instruction, rather than a separate pack. Full `msp-settings*` live-db suite run: 20/25
  pass (5 skip without `DATABASE_URL`), one pre-existing unrelated failure
  (`msp-settings-approve-purchases-rung.live-db.test.ts`, a `users_role_scope_check`
  violation in that test's own seed data, not caused by this build) filed as **#3696**,
  parented under Feature #2562 (Account Security MSP Console), labeled `bug`, board status
  set to AI Batter Up. `tsc --build` (libs) and `tsc --noEmit` (api-server) both clean.
  Branch verified merged into `origin/main` (`4686eecf3`) via
  `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean.
  Extensibility question Shane raised (settings needing a generic categorized-preferences
  shape for v1.2's DNS/white-label settings) is answered honestly in the issue comment,
  not guessed at here — this build stayed within alert preferences' own real scope, matching
  the customer-side table shape 1:1 rather than inventing a speculative categories schema.
