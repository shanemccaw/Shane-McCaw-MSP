# #3332 — Today: let Next section overlap the sky-scene background (z-index) instead of leaving a gap below short content

- **Status:** ✅ DONE 2026-09-09T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** bbeefcb04 (main)

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — pulled #3332's real body. Fix: pull Next section
  up with negative top margin to overlap `.today-scene`'s tail, give it z-index above
  `.today-sky`/`.today-night`/`.today-wx-sky` but confirm it doesn't cover
  `.today-scene-content`. Tune overlap live against both short and long greeting cases.
- 2026-09-09T00:00:00Z ✅ DONE — `web/shanes-life/public/app.js` (`viewToday`): captured
  the real `todayScene` element instead of discarding it, then after `Next` renders,
  computed `tailSpace = todayScene.offsetHeight - sceneContent.offsetHeight` (the real,
  live-measured empty room left in `.today-scene`'s min-height-320px box once its real
  content is laid out, not a guessed constant) and set `next.style.marginTop` to
  `-min(tailSpace, 56)px`. This is mathematically incapable of covering real greeting
  content: when a long greeting already grows the scene past 320px, `tailSpace` is 0 and
  no margin is applied at all. Also added `nextFollowsSceneDirectly` guard so the overlap
  only fires when Next genuinely renders right after the scene (skipped if a Tonight
  teaser, Meals nudge, or filled nearby-place slot landed between them, since pulling
  Next up in that case would overlap the wrong element). `app.css`: new
  `.next-overlap-scene { position: relative; z-index: 1; }` — above the sky/night/weather
  layers (z-index:auto) and intentionally still under `.today-scene-content`'s z-index:2
  as a second line of defense.

  Real, live verification: built a standalone HTML harness with the real app.css and the
  real DOM classes (`.today-scene`, `.today-scene-content`, `.next-overlap-scene`,
  `.next-card-v3`), running the exact overlap-computation snippet extracted verbatim from
  `viewToday`, rendered via system Chrome headless (`chrome.exe --headless --dump-dom` /
  `--screenshot`, same tool #3303 used — Playwright's bundled browser still isn't
  installed locally and downloading it would be an unjustified metered download).
  Confirmed three real cases:
  - Short greeting (today's screenshot case): scene 320px, content 123px, tailSpace 197px
    → margin capped at -56px. Screenshot shows Next's card genuinely overlapping the
    tail of the sky background, no dead gap.
  - Long, multi-line greeting that still fits under 320px: content 286px, tailSpace 34px
    → margin -34px (uncapped, exactly closes the real remaining tail).
  - Long greeting that grows the scene past 320px on its own (content 367px = scene
    367px): tailSpace 0 → margin 0px, Next renders in normal flow immediately after,
    never touching the real greeting text.

  Merged to `main` (bbeefcb04), verified an ancestor of `origin/main` via
  `verify-branch-merged.mjs` (exit 0). Working tree clean (`git status --porcelain`
  empty) — deleted the scratch HTML/PNG test harness files after use. No findings to
  file; no Shane-To-Do action.
