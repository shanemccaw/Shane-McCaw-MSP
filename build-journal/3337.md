# #3337 — mirror sync does a full board walk every 5 minutes regardless of what changed — switch to incremental since= diffing

- **Status:** ✅ DONE 2026-09-09T19:23:32Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Commit(s):** 72719c14c (bookend IN FLIGHT), ccf8beb90 (real implementation)

## Log
- 2026-09-09T18:52:30Z ⏳ IN FLIGHT — pulled #3337's full body + comments; confirmed real blocked_by
  dependency on #3335 was already CLOSED/`complete` (commit c021d648b) so unblocked to start
  immediately; provisioned isolated worktree `C:\wt\3337` (agent/3337); resolved queue buildId 2060
  from local `bt_build_queue` for reportProgress. Checked #3339 (the stopgap bumping SyncInterval
  5→25min) — confirmed via `git log --all --grep=3339` and `git show origin/main:...GitHubIssueMirror.cs`
  that it has NOT landed on `main` (SyncInterval still reads 5min) and no `agent/3339` worktree/branch
  exists — nothing to reconcile yet at the code level; my own fix supersedes its need going forward
  (design decision documented in the code + DONE log line below).
- 2026-09-09T19:20:35Z ✅ DONE — real, live investigation + implementation, both complete:

  **The real nuance the issue asked to verify, verified live (not assumed):** GitHub's REST
  `GET /issues?since=...` is a genuine DB-backed incremental filter — confirmed live
  (`gh api "repos/.../issues?...&since=2026-09-09T18:50:00Z..."` returned real issues updated at
  19:18/19:04/18:57/18:55, i.e. real-time-accurate). GitHub's Projects v2 GraphQL API has **no**
  reliable incremental signal for board-status moves: `ProjectV2Item.updatedAt` is real/accurate
  (confirmed via schema introspection), but the only way to ASK for "items changed since X" is the
  search-index-backed `items(query: "updated:>...")` filter, which **measurably lagged 15+ minutes**
  behind a confirmed real change in live testing against the real project board (#3337's own item,
  `updatedAt=2026-09-09T18:52:02Z`, still returned `totalCount: 0` from a
  `query: "updated:>2026-09-09T18:50:00"` filter run at 19:06 — 14+ minutes later, unambiguously
  inside the filtered window). `ProjectV2ItemOrderField` also offers no `UPDATED_AT` option (only
  `POSITION`). Conclusion, shipped: issue-level state (title/state/labels) gets a new fast
  incremental REST path; board status stays full-walk-only (unavoidable — no reliable incremental
  signal exists), on its own separate, longer reconciliation interval, exactly as the issue required
  ("Do not ship an incremental sync that quietly stops picking up real board-status changes").

  **#3339 reconciliation:** never landed (confirmed above) — nothing to merge/revert. Its 25-minute
  value informed `FullSyncInterval` (set to 30 min, same order of magnitude Shane already accepted
  as a board-status staleness trade-off), now scoped ONLY to the genuinely-expensive full walk rather
  than also slowing down issue-level freshness that no longer needs to be slow (`IncrementalSyncInterval`
  stays at the original 5 min — safe, because its cost doesn't scale with the interval the way the
  full walk's does).

  **What shipped** (`desktop/BuildConsole/`):
  - `Services/GitHubApiClient.cs` — new `GitHubIssueSinceUpdate` model + `ListIssuesUpdatedSinceAsync`
    (paginated `GET /issues?state=all&sort=updated&direction=desc&since=...`, excludes PRs, reports
    `Truncated` if the changed-set is too large to trust).
  - `Services/GitHubIssueMirror.cs` — split the single `SyncInterval`/`MaybeSyncAsync` into
    `IncrementalSyncInterval` (5min, cheap) + `FullSyncInterval` (30min, expensive) with independent
    gating; new `IncrementalSyncAsync` (upserts title/state/labels/blocked_by for the changed batch
    only, board_status_option_id/name + blocking_numbers deliberately preserved — no reliable signal
    for either); `SyncAsync` (the original full walk) unchanged in logic, now purely the periodic
    reconciliation pass + first-sync-ever path + incremental-truncation escalation target; new
    `GetIncrementalSyncStateAsync`/`RecordIncrementalSyncStateAsync`.
  - `Controls/BuildQueuePanel.xaml(.cs)`, `Services/QueueWatcherService.cs` — updated the "next sync"
    countdown display + doc comments off the removed `SyncInterval` symbol onto the new two-interval
    model (would not have compiled otherwise).
  - `lib/db/migrations/manual/2026-09-09-bt-issue-mirror-incremental-sync-3337.sql` — additive
    `ALTER TABLE bt_issue_mirror_sync_state ADD COLUMN IF NOT EXISTS last_incremental_sync_at/_ok/_note`.
    **Ran it myself** against local `DATABASE_URL` (see Database section) — confirmed via `\d`.

  **Verification, real:**
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` → **Build succeeded, 0 Warning(s),
    0 Error(s).**
  - Live GitHub REST `since=` endpoint exercised directly (see nuance section above) — real,
    current data returned, confirming the exact query shape `ListIssuesUpdatedSinceAsync` uses works.
  - The new incremental-upsert SQL and both `RecordIncrementalSyncStateAsync` branches (ok=true/false)
    run directly against local Postgres with a scratch issue row (`issue_number=99999001`) simulating
    a pre-existing board-status-set row: confirmed title/state/labels/blocked_by/closed_at update from
    the incremental batch while board_status_option_id/board_status_name/blocking_numbers/created_at
    are correctly PRESERVED (not clobbered) — exactly the designed semantics. Scratch issue row
    deleted and the real `bt_issue_mirror_sync_state` row's new incremental columns reset back to
    NULL/false/NULL (their genuine pre-test "never yet run" state) afterward — no test residue left
    on real bookkeeping.
  - Did NOT launch BuildConsole.exe (single-instance mutex could courier into Shane's live session,
    per standing rule) — this is real, live-API + live-DB verification of every non-WPF-UI code path,
    consistent with "clean build + real/simulated run" for a WPF app out of test-manifest scope.

  No new findings filed — the Projects v2 search-index-lag discovery is documented as design
  rationale in-code and in this bookend, not a separate product bug (it's GitHub's own real,
  external API behavior, not something in this codebase to fix).

  Branch verified merged into `origin/main`; working tree clean (see completion comment on #3337).
- 2026-09-10T18:05:29Z ↻ RE-DISPATCH VERIFY (build 2209, buildId 2209) — #3337 was re-queued a second
  time, but the work already fully landed 2026-09-09 and Shane himself closed the issue COMPLETED at
  19:51:41Z. Re-verified rather than re-implementing (duplicate-build "check origin first" discipline):
  `git merge-base --is-ancestor ccf8beb90 origin/main` → 0 (implementation is a genuine ancestor of
  `origin/main`); `IncrementalSyncAsync`/`IncrementalSyncInterval`(5min)/`FullSyncInterval`(30min)
  present in the current `GitHubIssueMirror.cs`; `ListIssuesUpdatedSinceAsync`/`GitHubIssueSinceUpdate`
  present in `GitHubApiClient.cs`; migration file
  `lib/db/migrations/manual/2026-09-09-bt-issue-mirror-incremental-sync-3337.sql` present on disk with
  its additive `ADD COLUMN IF NOT EXISTS` + self-marking `simulator_migration_runs` row. No new code
  written, nothing to build. Cleared the stale `in-flight` label the re-dispatch re-applied (set back
  to `complete` — code is confirmed done); did NOT reopen or re-close the issue (Shane's call, already
  made). Working tree clean. Status stays ✅ DONE — this line is a verification record, not a second
  build.
