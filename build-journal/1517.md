# #1517 — Ownership / RACI: Ordered A holders — precedence only, no succession logic

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 03589ebd6 (feature), 414c61a5d (manifest hash fix after rebase) — real post-push hashes on `origin/main`

## Log
- 2026-08-30 ⏳ IN FLIGHT — foundation #1515 (multi-holder unique index) confirmed closed and
  live in local DB (0 rows, new 4-col unique index present). Building explicit precedence
  ordering (an `order_rank` column + reorder endpoint) on top of `portal_ownership_assignments`,
  since #1515 only gave ordering-by-insertion, which cannot be changed without losing a row's
  provenance. Scope stop: wire contract only, no `artifacts/portal` page.
- 2026-08-30 ✅ DONE — Added `order_rank integer NOT NULL DEFAULT 0` to
  `portal_ownership_assignments` (Drizzle schema + manual migration
  `lib/db/migrations/manual/2026-08-30-ownership-raci-a-holder-precedence-1517.sql`, run against
  local `DATABASE_URL`; backfill query is a no-op today, table has 0 rows). `POST
  /portal/ownership/assign` now computes a new holder's rank atomically (one past the cell's
  current max) in the same INSERT, and leaves an existing holder's rank untouched on
  re-assertion. New `POST /portal/ownership/reorder` rewrites a cell's full precedence
  transactionally, rejecting a partial list that doesn't name exactly the cell's current
  holders. `GET /api/portal/ownership` now orders the overlay by `order_rank` (was `id`) and the
  wire shape `WireOwnAssignment` carries `order: number`. Every holder in a cell still has
  identical authority — no succession/activation/timeout logic reads this column, per the
  issue's explicit scope. Verified: `tsc --noEmit` on `artifacts/api-server` shows zero errors
  in `portal-ownership.ts`/`portal-ownership.test.ts` (the 59 remaining errors are the
  pre-existing #1765/#1772/#1774/#1777/#1778/#1781 baseline in unrelated files — confirmed by
  file/line, not assumed); `vitest run src/lib/portal-ownership.test.ts` — 34/34 passed,
  including a new case for the `order` field defaulting to 0. Append/reorder SQL logic also
  smoke-tested directly against local Postgres (insert-three, verify ranks 0/1/2, reorder,
  verify new order — then rolled back, no data left behind). Extended
  `test-manifests/portal/portal-v2-ownership.json` with API10-API15 (apiTests only, tagged
  `1517-reorder`) covering append-at-end, rank-preserved-on-reassert, reorder, reorder
  persistence via the overlay read, and the partial-list rejection — not run via
  `shaneapp://runTest` this session because the change is api-server only (no
  `artifacts/msp-portal` UI touched) and the manifest hits `{{DEPLOY_URL}}`, which does not yet
  carry this commit. Manual migration appended to #1630. `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirmed the branch an ancestor of `origin/main` after each push.
  No findings filed — no gap discovered outside this issue's own scope.
