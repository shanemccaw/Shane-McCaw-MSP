# #3821 — Plan Self-Service: wire the real MSP Console module (screen 49)

- **Status:** ✅ DONE 2026-09-13T02:20:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 03f3cad3a (bookend IN FLIGHT), b8dbe52e5 (nav.test.ts stale-count fix, own commit)

## Log
- 2026-09-13T02:12:27Z ⏳ IN FLIGHT — Read #3821's body + BUILD comment, the contract pack
  (`docs/msp-console/msp-plan-self-service-contract-pack.md`), the design export (`Design/MSP_Console/
  design_handoff_msp_console/Plan Self-Service.dc.html`, screen 49), and the real backend
  (`msp-plan-self-service.ts`). Found the real wiring work already landed on `main` — commit
  `4d2f4be3c` ("MSP Plan Self-Service UI (Git #3796)"), merged ~2h before this build's dispatch, by a
  concurrent session working #3796 (a finding filed by the same earlier #3782 investigation that
  produced this issue's own contract pack). #3796 was closed with a comment believing #3821 was the
  tracking issue for the gap; in fact #3796's own session did the real build. `artifacts/msp-console/
  src/console/modules/PlanSelfService.tsx` + `src/api/msp-plan-api.ts` are wired to all four real
  routes (`current`, `available`, `change`, `cancel-pending-change`), mounted in `ConsoleShell.tsx` at
  `sel.page === "plan"`, nav entry `plan` / "Plan & billing" in `nav.ts`. No fixture data — every
  price/allowance/status/dunning/pendingChange value is the route's own real response. The annual-price
  gap (`annual_price_cents IS NULL` on all 3 real tiers) is surfaced as a real, correct 400/UI note, not
  worked around, matching this issue's own dispatch instructions.
- 2026-09-13T02:20:00Z ✅ DONE — No new product code needed: the module, data seam, nav entry and
  shell mount for this screen were already complete and merged (`4d2f4be3c`) before this build was
  dispatched. This session's own real contribution is `b8dbe52e5`: `pnpm run test` in
  `artifacts/msp-console` was failing (`nav.test.ts` asserted 13 ops pages / 86 command-palette
  entries; real code now has 16 ops pages / 89 entries after several concurrent module landings
  since #3796's session captured those counts) — fixed the stale assertions, re-ran, all 16 tests
  pass. Also re-ran `tsc -p tsconfig.json --noEmit` clean. `verify-branch-merged.mjs`: OK, `b8dbe52e5`
  is an ancestor of `origin/main`. `git status --porcelain` clean. Posted comment on #3821 explaining
  the duplicate-dispatch situation with #3796. No new finding filed — the drifted test count is a
  routine consequence of concurrent module landings, not a defect worth its own issue, and it's
  already fixed here.
