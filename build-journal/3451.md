# #3451 — Retention queue has no producer: no module calls softDelete()/requestAcceleration()/registerRetainedRecordType()

- **Status:** ✅ DONE 2026-09-11T05:29:11Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** cc3724dc7 (bookend IN FLIGHT), 4407c2e6c (real work), rebased/pushed as e43225c80 on origin/main

## Log
- 2026-09-11T05:13:50Z ⏳ IN FLIGHT — investigation confirmed the issue's own claim: `lib/retention/registry.ts`
  and `origin-registry.ts` both ship empty, zero routes call `softDelete()`/`requestAcceleration()`, and
  `record_deletions` has 0 rows locally. Chose the scoped, real fix the issue's own body already names as the
  expected first producer: POA&M soft-delete (#1944's own body — *"POA&M soft delete (#1935) — the trigger"*).
  Implemented end to end: migration adding the soft-delete triple to `msp_poams`, retention wiring
  (`lib/retention/wiring/msp-poams.ts` — `registerRetainedRecordType` + `registerOriginResolver`),
  `DELETE /api/msp/poams/:poamId` (MSP-side soft delete), `DELETE /api/portal/poams/:poamId` +
  `POST /api/portal/poams/:poamId/request-acceleration` (customer-side), and excluded soft-deleted rows from
  both modules' plain list reads. Live-Postgres end-to-end test written and passing
  (`msp-retention-queue-poam-producer.live-db.test.ts`): soft-delete → real `record_deletions` row → customer
  requests acceleration → `GET /api/msp/retention/queue` genuinely returns it → operator approves →
  real purge, ledger row survives. Filed a real, evidenced finding hit mid-build as #3639 (parented under
  #1944): every one of `lifecycle.ts`'s six `createAuditLog()` calls writes a `tenants.id` into
  `audit_logs.clientId`, which is FK'd to `users.id` — every retention audit write has silently failed since
  #1947 landed (swallowed by `createAuditLog`'s own catch). Not fixed here — cross-cutting, pre-existing, out
  of this issue's scope.
- 2026-09-11T05:29:11Z ✅ DONE — full `pnpm --filter @workspace/api-server run typecheck` (tsc -p tsconfig.json
  --noEmit) is clean (0 errors) after rebuilding `lib/db`'s stale project-reference declarations
  (`npx tsc -b --force` in `lib/db` — a pre-existing local build-cache staleness unrelated to this issue, also
  incidentally surfacing/resolving an unrelated `monitoring_packages.kind` type error the same way). Final
  live-Postgres run: `msp-retention-queue-poam-producer.live-db.test.ts` + `msp-poams.test.ts` +
  `msp-poam-rbd-conversion.live-db.test.ts` — 3 files, 8 tests, all passing. DB left clean (0 synthetic rows
  remaining — verified via direct `psql` count). Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` — OK) and `git status --porcelain` clean. Finding #3639 filed under #1944, board
  status set to AI Batter Up. Migration `lib/db/migrations/manual/2026-09-11-msp-poams-soft-delete-3451.sql`
  run against local Postgres in-session (additive `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, no Shane To-Do).
