# #3978 — Schema migration: status enum (CHECK), resolution split, selector/scope columns for bug lifecycle

- **Status:** ✅ DONE 2026-09-14T01:30:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** b93914943 (migration file, rebased/merged as 40ee5a08e on main)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — confirmed `page_id` is currently NOT NULL (via `\d visual_test_tracker_entries` against local DATABASE_URL), 0 existing rows in the table. Writing manual SQL migration per issue body + the `is_design` addendum comment, then running it against local Postgres.
- 2026-09-14T01:30:00Z ✅ DONE — wrote `lib/db/migrations/manual/2026-09-14-bug-lifecycle-3978.sql` (SQL-only, no C# touched) and ran it against the real local Postgres 18 `shanemccawmsp` DB. Verified via `\d visual_test_tracker_entries`: all 3 CHECK constraints present (`chk_vtt_status`, `chk_vtt_resolution`, `chk_vtt_resolution_reason`), all 4 new columns present (`resolution`, `resolution_reason`, `selector`, `is_design`), `page_id` now nullable. `UPDATE ... SET status = 'Closed' WHERE status = 'Resolved'` converted 0/0 rows — table had zero existing rows before and after (confirmed by `SELECT status, count(*) ... GROUP BY status` returning 0 rows pre-migration). Migration self-registered in `simulator_migration_runs`. Rebased onto and pushed directly to `origin/main` (40ee5a08e); `verify-branch-merged.mjs` confirms OK. Working tree clean. Shane must be told this SQL has been run against local dev already — no manual action needed there — but per standing convention this is flagged so #3977's other child issues can proceed against real schema.
