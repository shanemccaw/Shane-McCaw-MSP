# Build Journal — Issue #3453

- **Status:** ✅ DONE 2026-09-11T05:04:33Z
- **Scope:** platform

## Log

- 2026-09-11T04:43:58Z — Started. Issue: 10 `cat-*` rows in `monitoring_packages` are dead
  dashboard-tab taxonomy (from `2026-07-19-customer-dashboard-category-tabs.sql`), not runnable
  scan bundles — zero linked checks, zero code references to their keys. `GET
  /admin/monitoring-packages` (admin-monitor-checks.ts) lists all 21 with no way to tell them
  apart from real scan packages; `GET /msp/monitoring-packages` (msp-diagnostics.ts) works around
  it locally with a `HAVING count(...) > 0` derivation. Fix per the issue's own option (b): add a
  real `kind` discriminator column (`scan_bundle` | `dashboard_category`) to `monitoring_packages`
  so every consumer can filter/label correctly without re-deriving "runnable" from a check-count
  join.
- 2026-09-11T05:04:33Z — ✅ DONE — commit `72898b552` (rebased/pushed to `main` as `c0c64ace0`).
  - `lib/db/src/schema/msp.ts`: added `monitoring_packages.kind` (`scan_bundle` |
    `dashboard_category`), default `scan_bundle`.
  - `lib/db/migrations/manual/2026-09-11-monitoring-packages-kind-discriminator-3453.sql`: ran
    it myself against local `DATABASE_URL` — additive `ALTER TABLE` + `CHECK` constraint,
    backfilled the 10 known `cat-*` rows to `dashboard_category`. Confirmed via psql:
    `dashboard_category` = 10, `scan_bundle` = 11 (all active rows accounted for).
  - `msp-diagnostics.ts` `GET /msp/monitoring-packages`: now filters on `kind = 'scan_bundle'` as
    the primary guard; kept the `HAVING count(...) > 0` check as a second, independent guard.
  - `admin-monitor-checks.ts` `GET/POST/PATCH /admin/monitoring-packages`: no code change needed
    — `db.select()` already returns the whole row so `kind` flows through automatically; new
    packages default to `scan_bundle` via the schema default; PATCH's field allowlist doesn't
    include `kind` (not user-editable, by design — it's a structural discriminator).
  - admin-panel Monitoring Packages screen (`packagesTypes.ts`, `packagesStore.ts`,
    `PackagesExplorerPanel.tsx`, `PackageProperties.tsx`): Explorer panel now splits active
    packages into "Live" (scan_bundle) and a distinct "Dashboard tabs (not scans)" section, drops
    the misleading "runs nothing" amber alarm for `dashboard_category` rows (zero checks by
    design, not a bug), and `silentPackages()` (drives the ribbon's ""N collect nothing"" count)
    excludes them so it no longer raises a false alarm every session. Package Properties shows a
    new read-only Kind field.
  - Verified: `msp-diagnostics.test.ts` (11 pass), `admin-monitor-checks-crud.test.ts` +
    `admin-monitoring-package-usage.test.ts` (17 pass), admin-panel `packages.test.tsx` (27
    pass). `tsc --noEmit` clean for `lib/db`, `artifacts/api-server`, `artifacts/admin-panel`
    (regenerated `lib/db`'s gitignored `dist/` decls first so api-server's project-reference
    build saw the new `kind` field).
  - `git status --porcelain` clean; branch verified merged into `origin/main` via
    `verify-branch-merged.mjs` before writing this.
  - No new findings filed — this build's own scope covered the whole gap the issue described.
