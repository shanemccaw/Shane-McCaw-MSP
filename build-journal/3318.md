# #3318 — Today-tray visual cards for Timer, Med courses, and Tesla charging

- **Status:** ✅ DONE 2026-09-09T04:45:11Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** dbb12d08b

## Log
- 2026-09-09T04:27:04Z ⏳ IN FLIGHT — Read #3318 + Design/design_handoff_shanes_life/README.md
  ("Drawn in the same pass" item 6) and the prototype's real markup for the three visual specs
  (timer chip at Shanes Life - First Slice Prototype.dc.html:308-315/2816, course row line
  744/758/2800-2802, Tesla charging card line 999-1006/2957-2960). Confirmed against
  `web/shanes-life` main: standalone timers already exist (Git #3307, core/timers.mjs) but render
  as a separate "Timers" h2 section, not the blue chip atop Next; med courses already correctly
  leave the batch on a dormant day (medications.mjs:315) but have no course-detail row nor dashed
  rest note; Tesla charging sessions + per-rate-type cost already exist end-to-end
  (core/vehicles.mjs listChargingSessions/syncChargingSessionsFromTesla, Git #3217/#3287) but the
  Tesla room UI has no "Charging · last 30 days" card, no odometer row, and there's no capture
  grammar for "home electricity is 14 cents" yet. Plan: wire all three into their real UI homes
  using the already-real backend data, add the one missing capture-grammar rule and a small
  extend-timer endpoint, no schema changes needed.
- 2026-09-09T04:45:11Z ✅ DONE — Commit dbb12d08b. Shipped all three visuals: (1) Timer chip
  moved into the top of Today's Next section as a real blue-tinted action row with a live
  fires_at countdown, real +1 min (new `timers.extendTimer` + `POST /api/timers/:id/extend`) and
  Stop; (2) Med course row: `medications.getMedsToday`'s `isCourseActiveToday` replaced with a
  richer `courseStatus()` returning day-count/pause-date while active and day-off/resume-date
  while dormant, wired into a real course-detail row on the active batch card and a new dashed
  "resting" note (`courseRest[]`) for the dormant case, which the UI previously just silently
  dropped; (3) Tesla charging card: new "Charging · last 30 days" card in the Tesla room reading
  the already-built `listChargingSessions`/`syncChargingSessionsFromTesla` (#3217/#3287), a new
  `vehicles.getSyncedOdometer` + `GET /api/tesla/odometer` for the closing odometer row, and a
  new capture-grammar rule ("home electricity is 14 cents" -> `charge_cost_per_kwh`). No schema
  changes were needed. Verified live against the real local Postgres (`finances` db, real Shane
  account, `web/shanes-life/.env` copied in from the main checkout for this session only, not
  committed): created+archived real course-med rows confirming courseStatus's math for both
  states; created+canceled a real timer confirming extendTimer's +60s math and the
  already-fired guard; inserted+removed a real Tesla vehicle and two charging sessions confirming
  getSyncedOdometer and the per-session cost/rate-not-set shapes. `node
  src/core/capture-grammar.test.mjs`: 54/54 passed. Branch verified merged into origin/main
  (`verify-branch-merged.mjs`), working tree clean. No `test-manifests/` entry -- that discipline
  is scoped to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/` only; this is
  `web/shanes-life/`, Shane's personal project. No findings filed -- no gaps or bugs surfaced
  outside the scope of this issue.
