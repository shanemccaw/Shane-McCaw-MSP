# #3771 — Automation registry (Power Automate + Power Platform/Azure AI Studio agents) — backend + data model

- **Status:** ✅ DONE 2026-09-12T06:35:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 1a1bdf22a (Automation Registry feature), f12cddf68 (unrelated api-server startup crash fix, filed as #3794)

## Log
- 2026-09-12T04:30:00Z ⏳ IN FLIGHT — starting: automation_registry data model (Drizzle + manual migration), CRUD API under artifacts/api-server, and MyArchitect client wiring following #3464's AdminRetainerService pattern. Backend + MyArchitect only, per issue scope (MSP Console UI is a separate Feature blocked on #3768).
- 2026-09-12T06:35:00Z ✅ DONE — Shipped: `automation_registry` Drizzle schema + manual migration (`lib/db/migrations/manual/2026-09-12-automation-registry-3771.sql`, self-run against local Postgres), real CRUD API (`artifacts/api-server/src/routes/admin-automation-registry.ts`: GET list/POST create/PATCH update/DELETE, requireAdmin-gated, scoped via resolveTenantScope, registered in routes/index.ts), and MyArchitect client wiring (`AutomationRegistryEntry` model, `IAutomationRegistryService`/`AutomationRegistryService` real HTTP client mirroring #3464's AdminRetainerService, and MainWindow Home-tab "Automation Registry" ribbon group — Browse gallery + Add Automation + edit/Save/Delete record, same Edits+Actions+ShellRegistry.OpenRecord shape as OpenLogAdHocHoursRecord, no new UI pattern invented). Verification: `lib/db` rebuilt clean, api-server `tsc --noEmit` clean (0 errors), MyArchitect.csproj builds clean (0 warnings/errors); built and ran the real api-server locally and exercised the full live CRUD lifecycle (create, list, update, delete, 401 unauthenticated, 404 unknown customer) against a synthetic `zz-test-*` admin user and a real Postgres row — all correct; test user/tokens cleaned up afterward, `automation_registry` left empty. **Finding filed:** while starting the local api-server for verification, hit a genuine pre-existing crash (Express 5's path-to-regexp rejects `admin-build-tracker.ts`'s bare `*` wildcard, introduced by #3652, crashes the whole server at startup) — filed as **#3794** (URGENT, sibling under #1571, labeled bug, board status AI Batter Up) and fixed in its own isolated commit (f12cddf68) since it blocked this build's own verification and blocks every other local dev session. Branch verified merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs` → OK); working tree clean.
