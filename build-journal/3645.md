# #3645 — webhook-delivery retry setTimeout keeps process alive in tests

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Starting: unref() the retry setTimeout in
  `artifacts/api-server/src/lib/webhook-delivery.ts` so it can't keep the
  process/test-runner alive; verify with `pnpm --filter api-server test`.
- 2026-09-12T00:20:00Z — ✅ DONE — Added `.unref()` to the retry `setTimeout`
  in `scheduleAttempt`'s failure-path retry scheduling (webhook-delivery.ts,
  `attemptDelivery`, ~line 198-201). Fire-and-forget retry scheduling; safe
  to unref since nothing needs the process to block exit waiting on it.
  Verified directly: `node --experimental-strip-types
  --experimental-test-module-mocks --test src/lib/webhook-delivery.test.ts`
  — 16/16 pass, process exits in ~2.3s (previously the failure-path test
  left the process alive for the full 5.5min retry-delay wall clock).
  Commit b1cb5328d8abbe7ca70ea6a679b724a1a925467b, merged into origin/main
  (verify-branch-merged.mjs confirms ancestor). Working tree clean.
  Noted: full `pnpm --filter api-server test` run showed ~30 pre-existing
  unrelated test failures (azure-rm.test.ts, portal-assessment-debug-reset-session.test.ts,
  a DATABASE_URL-not-set failure in one vitest worker) not touched by or
  related to this change. Filed as #3895, sub-issue of Feature #1128,
  board status set to AI Batter Up.
