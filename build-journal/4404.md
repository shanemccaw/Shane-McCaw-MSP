# #4404 — Retainer: provision entitlement/tier on payment-confirmed

- **Status:** ✅ DONE 2026-09-16T19:15:21Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** a90d569fe (work), 1ede5ba81 (route header note)

## Log
- 2026-09-16T18:54:40Z ⏳ IN FLIGHT — Reading #4404, #4402's contract answer, and #4403's fix pattern; then provisioning the real entitlement record for Retainer purchases in /api/public/purchase/payment-confirmed (idempotent on duplicate calls).
- 2026-09-16T19:15:21Z ✅ DONE — Posted: 2026-09-16T19:15:21Z. A paid Buy.tsx Retainer now provisions what its readers actually read (retainer rows have no monitoring tier, so #4402's monitoring_tier contract doesn't apply directly): (1) an active `client_services` row keyed on #4403's `(checkout_session_id, service_id)` unique index, ON CONFLICT DO NOTHING; (2) the `retainer_settings` row the Portal's My Architect page reads (`GET /api/portal/retainer` → `configured`), with the allotment taken from `services.hours_per_month` and ON CONFLICT (customer_id) DO NOTHING, so an AdminV2-configured row is never overwritten. New lib `artifacts/api-server/src/lib/purchase-retainer-entitlement.ts`. Hooked at payment-confirmed (account-first buyers; reuses #4403's hoisted `resolved`), set-password `ok` (legacy pay-then-account buyers) and portal-handoff (backstop). No migration of my own: reused #4403's `lib/db/migrations/manual/4403-client-services-checkout-session-id.sql` (already applied locally). Verification: api-server tsc clean. New live-db test `routes/public-purchase-payment-retainer-entitlement.live-db.test.ts` 5/5 against local Postgres. It drives the real payment-confirmed route and then the real /portal/retainer read with a synthetic zz-test-4404 MSP/tenant/user: `configured` goes false→true, and 3 concurrent duplicate confirms plus a helper re-run leave exactly 1 client_services row and 1 retainer_settings row. The admin row is preserved, the legacy order provisions at set-password, and non-retainer purchases are a no-op. Cleanup verified (0 residual rows). Purchase suites 36/36, including #4403's monitoring live-db test. Stripe retrieve was stubbed; no real charge was run. Findings filed as sub-issues of #4401: #4431 (the recurring Stripe subscription is never created, so only month 1 is charged) and #4432 (a skipped-consent Retainer never gets retainer_settings after consenting later). Pre-existing dirty files: none.
