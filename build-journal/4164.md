# #4164 — What's Remaining panel: add Epic filter and board-status filter

- **Status:** ✅ DONE 2026-09-15T00:58:48Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** a7ef00805 (feature), 6983d38ab (post-rebase, pushed to origin/main)

## Log
- 2026-09-15T00:54:53Z ⏳ IN FLIGHT — read #4164 body/comments, AiBatterUpPanel's #4146 Epic filter, EpicGroupHeaderHelper, and WhatsRemainingPanel; about to factor a shared EpicFilterHelper and add Epic + board-status filters to WhatsRemainingPanel.
- 2026-09-15T00:58:48Z ✅ DONE — Added `desktop/BuildConsole/Controls/EpicFilterHelper.cs`, a shared
  `EpicFilterOption`/`BuildOptions`/`ResolveSelectedIndex` helper factored out of AiBatterUpPanel's
  #4146 dropdown; AiBatterUpPanel's own `CmbEpicFilter` now goes through it instead of a private
  duplicate struct. `WhatsRemainingPanel` gained a real `CmbEpicFilter` (same shared shape) and a
  `StatusFilterHost` WrapPanel of toggle chips built from the distinct `BoardStatus` values actually
  present in the loaded rows (never a hardcoded list) — both filters combine AND-wise in the new
  `ApplyFilters()` (replaces the old unconditional `Render()`), and each Epic group's copy button
  (`EpicGroupHeaderHelper`) still copies only the currently-visible (filtered) rows since it's built
  from the already-filtered list. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`
  — 0 warnings, 0 errors. Verification honestly: clean build + code-reading confirmation only, per
  this repo's own standing rule against launching/driving the real BuildConsole.exe UI for
  self-verification (Shane's live instance shares the same single-instance mutex) — live click-through
  in the app was not self-verified; Shane should confirm the actual filtering/copy behavior visually.
  `git status --porcelain` clean after commit. Branch verified merged into origin/main
  (`verify-branch-merged.mjs`).
