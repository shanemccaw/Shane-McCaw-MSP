# #2117 — Worktree isolation: lib/*/dist junctions point at the main checkout, not the worktree's own edited source

- **Status:** ✅ DONE 2026-09-01T18:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 2fa869d77

## Log
- 2026-09-01T00:00:00Z ⏳ IN FLIGHT — Read #2117 body/comments. Confirmed in this worktree: `lib/db/dist` is currently a junction to the main checkout's `lib/db/dist` (`fsutil reparsepoint query` shows target `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\lib\db\dist`). Scope per the build prompt: fix only `lib/*/dist` to build from the worktree's own `lib/*/src` via `tsc --build`; leave third-party `node_modules` junctioning untouched.
- 2026-09-01T18:05:00Z ✅ DONE — `scripts/dev-server/link-deps.mjs`: `linkDeps()` no longer junctions `lib/*/dist`; added `buildLibDist(worktreePath)`, which finds every composite `lib/*` (and `lib/integrations/*`) project and runs `tsc --build` against them using the worktree's own already-junctioned `node_modules`. Wired into `provision-worktree.mjs` (after `linkDeps`, gated on `--link`) and `bootstrap-server.mjs` (same). `README.md` updated to match. Commit `2fa869d77` (pushed to main).
  - **Verified:** provisioned a real scratch worktree (`C:\wt\2117-verify-tmp`, `--link`), confirmed `lib/db/dist` there is a REAL directory (`fsutil reparsepoint query` → "not a reparse point"), not a junction — 11/12 composite lib projects built cleanly. Added a marker export only to that worktree's `lib/db/src/schema/msp.ts`, rebuilt via `tsc --build lib/db`, confirmed the marker appears in the freshly-built `lib/db/dist/schema/msp.d.ts` there and nowhere else (not in this worktree, not in main's dist) — the composite/project-reference build step genuinely reflects worktree-only source now.
  - **Found + reported, not fixed here (correctly out of scope):** the SAME marker test showed a plain `pnpm typecheck` in a consuming package (`artifacts/api-server`) still resolves `@workspace/db` into the MAIN checkout's source, not the worktree's — because `node_modules/@workspace/db` lives inside a wholesale-junctioned host directory (Windows junctions redirect atomically; can't override one nested entry). This is the OTHER half the original issue body described and its own repro proved necessary, but the dispatched build explicitly scoped it out ("leave third-party node_modules junctioning ... untouched"). Filed as **#2151** (parented to #1202, board status set to "AI Batter Up"), with the full repro and two suggested directions (per-package node_modules relinking, or a `tsconfig.base.json` `paths` override) — both bigger/riskier changes needing their own review.
  - **Also found (corroborating evidence, not separately filed):** `store-doctor.mjs` reported the MAIN checkout's own `artifacts/api-server/node_modules/@workspace/db` junction currently repointed at a live worktree `C:\wt\2116-q1106` (owner pid alive at the time) — apparently a manual workaround applied while investigating #2116, left in place on the shared store. Not repaired here since that worktree was live and repair is explicit (`store-doctor.mjs --repair`, Git #1988); noted on #2151 and the #2117 completion comment for whoever finishes #2116 to revert.
  - `git status --porcelain` clean, branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.
