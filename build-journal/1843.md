# #1843 — Config state: customer and MSP operator API surface for snapshots, diffs and the resource registry

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** _(filled at DONE)_

## Log

- 2026-08-31 ⏳ IN FLIGHT — Starting. Blocker **#1798 verified CLOSED and `complete`**; the
  `blocked_by` edge on #1843 points at it and nothing else, so this is genuinely unblocked.

  **Investigation done first, against the chain's real landed output rather than this issue's
  expected route list** (#1793 → #1798 bookends, issue comments, and the local database):

  | Landed | Real state |
  |---|---|
  | #1794 | `config_resources` 1,539 rows — the derived MODEL, volatile serial ids |
  | #1795 | `config_snapshot_resource_types` (1,539 registered / 1,359 collectable), `tenant_config_snapshots`, `tenant_config_snapshot_objects`, `tenant_config_snapshot_resource_status` — all keyed on the stable text `resource_key`, all immutability-triggered |
  | #1796 | `config-snapshot-collector.ts` + the `config_snapshot_collect` Workflow Engine node + `__system__: Tenant Configuration Snapshot` (manual trigger) |
  | #1797 | `config-snapshot-differ.ts` — `diffDrift` / `diffAgainstBaseline` / `diffTenants` / `diffPromotion` over one engine; `config_diffs` + 3 tables |
  | #1798 | `admin-config-snapshots.ts`, `admin-config-diffs.ts` — the report surface, `requireAdmin` only |

  **Real volume measured from the testbed, not guessed** (local `DATABASE_URL`, tenant 1):
  6 snapshots; the two full ones (rows 8 and 10) each hold **~50,150 objects across 94
  resources = 34 MB of `object_json`**, largest single object **173,626 bytes**, largest single
  resource `graph:v1.0:/applicationTemplates` at **39,089 objects / 27 MB**. A whole-snapshot
  response is therefore impossible, and object pagination has to be capped in the low tens, not
  the hundreds. Two diffs exist (rows 7 and 9); row 9 is the real full drift — 155 comparable,
  1,203 not_comparable, 340 significant changes.

  Plan: an `/api/portal/config-state/*` customer surface scoped by the JWT `customerId` through
  the existing `resolveCustomerId`, and an `/api/msp/config-state/*` operator surface scoped by
  `assertCustomerAccess` + `resolveStaffScopedCustomerIds`. No second scoping mechanism. Every
  response carries the completeness record. Reads and comparisons only — collection is fired
  through the existing Workflow Engine definition, never a bare call.
