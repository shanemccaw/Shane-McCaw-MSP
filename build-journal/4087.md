# #4087 — docs/portal/microsoft-changes-contract-pack.md: #3781 refresh missed two real additions (#1701, #1744)

- **Status:** ✅ DONE 2026-09-14T21:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** e012b3cd4 (verified merged into origin/main)

## Log
- 2026-09-14T21:20:00Z ⏳ IN FLIGHT — refreshing docs/portal/microsoft-changes-contract-pack.md: add §0/§1b route+routings entries, §1a routing field, §4 cross-refs, remove stale §1g defect note
- 2026-09-14T21:35:00Z ✅ DONE — Verified all three findings against real source and fixed the doc:
  1. §0/§1b: added `POST /admin/m365/interpretations/:id/route` (`:732`) and
     `GET /admin/m365/interpretations/:id/routings` (`:794`), both #1701, with a real explanation
     note in §1b.
  2. §1a: added `routing: WireRouting | null` to the `WirePost` table (`portal-message-center.ts:510`)
     and a new `WireRouting` shape table (`:264-279`), plus a §4 cross-reference under
     "Resolution → Routing".
  3. §1g: removed the stale #2696 "known defect" note — confirmed live in `msp-message-center.ts`
     that `limit`/`offset` (`:35-38`) are now NaN-guarded (`!isNaN(...) ? ... : default`), same as
     `customerId`; already fixed by commit `ef46ada2e`.
  Verification: re-read all three source files in full on `main` before editing (no `git log -S`
  needed — code was already read directly). No code/schema/UI touched, docs-only change.
  Working tree clean; `verify-branch-merged.mjs` confirms `e012b3cd4` is an ancestor of
  `origin/main`.
