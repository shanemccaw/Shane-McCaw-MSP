# #1825 — Build Queue: entirely-blocked build-set sections should sort below sections with active work

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** c94b63e7b (bookend IN FLIGHT), 7718d86c3 (fix)

## Log
- 2026-08-30 ⏳ IN FLIGHT — sorting `buildSetOrder` in `Controls/BuildQueuePanel.xaml.cs` so a build set with at least one non-blocked/active node renders before an entirely-blocked set, preserving first-seen order as tiebreaker. Not touching `OrderByDependency` or node ordering within a set (#1760).
- 2026-08-30 ✅ DONE — `orderedBuildSets` stably re-sorts `buildSetOrder` (LINQ `OrderBy`, stable) by `bucket.All(n => n.IsBlocked) ? 1 : 0`, so any set with at least one non-blocked/active node renders before an entirely-blocked set, first-seen order preserved as tiebreaker. `OrderByDependency` and node/lane ordering within a set (#1760) untouched. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors. Pushed to `origin/main` at 7718d86c3, verified merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
