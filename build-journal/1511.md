# #1511 — Risk Register: Role-based acceptance authority via ownership assignments

- **Status:** 🚧 BLOCKED (no code changed this session)
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** n/a — this bookend commit is the only commit from this session

## Log
- 2026-08-31 ⏳ IN FLIGHT — pulled #1511's full body and all four comments in order.
  The settled architecture (per the 2026-08-30 comments) requires two things that
  do not exist in the codebase today:

  1. **A per-workload RACI row to resolve authority against.** #1523's final
     resolution (2026-08-30) settles that risk acceptance authority is "A on the
     workload the risk resolves to" (via finding → service/workload inheritance),
     not a per-risk assignment. But `portal-ownership.ts` only sources `service`
     rows from `client_services × services` (what the customer *purchased*), not
     M365 workloads derived from the tenant's actual enabled service plans. That
     derivation is filed as its own issue, #2008 (still OPEN, no code landed —
     confirmed zero hits for `servicePlan`/`service_plan`/workload-derivation
     logic in `lib/db/src/schema/msp.ts` or `tenant-signals.ts`). This is the
     identical blocker that stopped the #1516 session on 2026-08-31 (see its
     bookend / the "Bookend: #1516 BLOCKED" commit).
  2. **Point-in-time replay of who held A.** #1511's own body/first comment says
     plainly: "Because the matrix is an append-only event log with derived
     current state ... 'who held A when this RBD was signed' is answerable by
     replay. This issue depends on that log existing." Checked
     `portalOwnershipAssignmentsTable` (`lib/db/src/schema/msp.ts:6489`): it is a
     plain mutable current-state table (`createdAt`/`updatedAt`, unique index on
     customer+object+role+owner, no `supersededAt`/versioning/event rows). The
     append-only log is tracked separately as #1522 (still OPEN — labeled
     `in-flight` from an earlier dispatch, but no event-log table or code has
     landed on `origin/main`; confirmed via `git show origin/main` and a repo
     grep for `ownership_events`/`OwnershipEvent`).

  Also confirmed by reading `artifacts/api-server/src/routes/portal-risk-register.ts`
  and `lib/db/src/schema/msp.ts:5453-5620`: `msp_risk_decisions.owner`/`.ownerId`
  are unwired display columns (never written by any resolution logic), and the
  `POST /portal/risk-register/:rbdId/accept` route accepts any typed `fullName`
  today with no authority check at all. There is nothing partially built here to
  extend — the whole capability rests on #1522 and #2008 landing first.

  Per Git #1987 (a blocking conclusion must become a `blocked_by` edge, not just
  prose) and the precedent set by #1516 in this same session sequence: wired
  real `blocked_by` edges #1511 → #1522 and #1511 → #2008 (both confirmed still
  open), and swapped the `in-flight` label for `blocked`. No schema or code
  changes made — building the authority model against the current
  purchased-service rows and mutable-current-state table would mean redoing the
  resolution semantics twice once the real foundations land, which is exactly
  the approximation Shane told the #1516 session to avoid instead of filing.

  **What Shane needs to do:** nothing immediate. #1522 and #2008 are both
  already-filed issues in the normal queue. Once both land, #1511 unblocks
  automatically via the two dependency edges and a future session can build the
  actual authority-capture + point-in-time-replay logic against real rows.
