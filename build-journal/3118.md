# #3118 — Migration numbers are now shared across two directories with nothing enforcing it

- **Status:** ✅ DONE 2026-09-07T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 9c88f7f36

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — adding a real cross-directory migration-number guard: a shared script both `web/shanes-life/src/migrate.mjs` and `desktop/ShanesSurvival`'s `MigrationRunner.cs` run before applying anything, failing closed on a number collision across `desktop/ShanesSurvival/migrations/` and `web/shanes-life/migrations/`.
- 2026-09-07T00:45:00Z ✅ DONE — Added `scripts/check-migration-numbers.mjs`, exporting `assertNoDuplicateMigrationNumbers()`. It reads both `desktop/ShanesSurvival/migrations/` and `web/shanes-life/migrations/`, extracts each file's leading number, and throws if the same number appears in BOTH directories (deliberately not intra-directory — the existing `010_debt_is_critical.sql` / `010_expected_events.sql` pair stays as an accepted single-runner quirk). Wired into `web/shanes-life/src/migrate.mjs::runMigrations()` — called before any DB work. Ported the identical rule into `MigrationRunner.cs` as `FindDuplicateMigrationNumberError()`/`ExtractNumbers()` (C# can't import the Node module, so it's a parallel implementation of the same rule against the same two directories, located by walking up from `desktop/ShanesSurvival/migrations` to the repo root) — checked before `RunAsync` touches Postgres at all. Updated the two docs that previously only said "check both directories" in prose (`web/shanes-life/README.md`, `013_shanes_life_foundation.sql` header) to point at the real enforcement. Verified: manual duplicate-number fixture in temp dirs throws as expected via the Node script; real repo state (no cross-dir collisions today) passes with exit 0; `dotnet build` on `ShanesSurvival.App.csproj` succeeds with 0 warnings/errors. Branch merged into `origin/main` per `verify-branch-merged.mjs` ("OK"). Working tree clean.
