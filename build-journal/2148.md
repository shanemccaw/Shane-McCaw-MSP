# #2148 — Policy Engine: OU manual assignment — MSP-operator-only or customer self-service?

- **Status:** ✅ DONE 2026-09-03T12:37:27Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 096cdad9d (local) → 139808381 (origin/main, post-rebase)

## Log
- 2026-09-03T12:19:04Z ⏳ IN FLIGHT — Per Shane's 2026-09-03 dispatch comment: build MSP-side
  "set" capability for OU manual assignment under `/api/msp/*`, gated by MSP staff auth
  (not `requireAdmin`), mirroring `artifacts/api-server/src/routes/admin-active-directory.ts`'s
  `active_directory_ou_assignments` CRUD and `policy-compliance-graph.ts` resolution order.
  Customer self-service (view + request changes) is explicitly out of scope — filed as a
  sibling issue per Shane's comment, not built here. No UI this pass unless a Design export
  already exists for the relevant MSP Portal screen.
- 2026-09-03T12:37:27Z ✅ DONE — Added `artifacts/api-server/src/routes/msp-active-directory.ts`:
  `GET/POST /api/msp/active-directory/ou/:id/assignments`, `PATCH/DELETE
  /api/msp/active-directory/ou-assignments/:id`. Gated `requireAuth` + `requireRole("MSPOperator")`
  (MSPAdmin/PlatformAdmin clear that floor) and scoped via `assertCustomerAccess` (ownership +
  per-staff scoping — the same single source of truth every other `/api/msp/*` route uses); a
  target outside the caller's book 404s without disclosing existence, matching
  msp-diagnostics.ts/msp-engine-history.ts. Same table (`active_directory_ou_assignments`), same
  real-Graph-verification-at-assign-time, same `policy-compliance-graph.ts` resolution order.
  Admin-only `requireAdmin` routes in `admin-active-directory.ts` are untouched — only exported
  two of its private helpers (`resolveAssignmentCustomer`, `resolveGraphUserByUpn`) so the new
  file can reuse them instead of duplicating. Registered in `routes/index.ts`.
  Extra MSP-side restriction beyond the admin route's shape: a null-tenantId (platform/MSP-level
  grouping) OU is never a valid target here — only via the admin route — since an OU carries no
  mspId of its own and a tenant-less OU can't be proven to belong to one MSP.
  No UI: `Design/portal/` has no export for this surface yet (confirmed empty).
  Wrote `msp-active-directory.test.ts` mirroring `msp-engine-history.test.ts`'s pattern (auth
  gates, cross-MSP/staff-scope 404s, one happy path per route) — **written but NOT executed**:
  `tsc`/`vitest` are blocked repo-wide right now by a genuine pnpm-store content gap in the
  shared main checkout (`drizzle-orm`, `vitest`, and other packages' `.pnpm` store entries are
  present as empty directories — confirmed via direct inspection, not a worktree-junction issue,
  `store-doctor.mjs` scan only flagged 2 unrelated poisoned `.bin` shims). This is NOT something
  this build introduced or can fix (pnpm install is prohibited from a worktree/not a remedy per
  CLAUDE.md) — it is already extensively filed by concurrent sessions today: #2526, #2528,
  #2503, #2522, #2515, #2525, #2193, #2192, #2502, #2516, #2508, #2501. Not filing a duplicate.
  My route file hit the exact same, and only the same, baseline `tsc` errors as the pre-existing
  `admin-active-directory.ts` it mirrors — no new error class introduced.
  Manual code review: cross-checked every route's auth/scoping/error-shape against
  `msp-diagnostics.ts`, `msp-engine-history.ts`, and `msp-standing-policies.ts`'s established
  conventions in this repo.
  Customer self-service (view + request changes) is genuinely new scope, not built here — per
  Shane's dispatch comment. Confirmed already filed as #2524 ("Policy Engine: OU assignment —
  customer read + request-change (split from #2148)"), so not duplicated by this session.
  `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/2148-q1368`
  (139808381) is an ancestor of `origin/main`.
