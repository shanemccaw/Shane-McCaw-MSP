# #3819 — Marketplace Purchase: wire the real MSP Console purchase-on-behalf module (screen 47)

- **Status:** ✅ DONE 2026-09-13T02:29:32Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 190bdc26f (real work; the IN FLIGHT bookend was `be4f47bd3`, since squashed into history by two rebases onto a moving `origin/main`)

## Log
- 2026-09-13T02:24:28Z ⏳ IN FLIGHT — Investigated #3819's body + real code: #3400 (accepted-before-charged) and #3403
  (retainer billed once) are both genuinely fixed in `msp-marketplace-purchase.ts`; #3404 (fulfillment silently
  discarded for 20 `assessment` + 6 `retainer` rows) is only partially fixed (now logged, still provisions nothing)
  and confirmed still live against local Postgres. Found the customer-safe `MarketplaceService` shape the GET
  catalog route returns doesn't carry `serviceClass`/`fulfillmentTypeKey` at all, so the UI had no way to flag
  #3404 (or the project/per-seat/consultation gates) *before* a purchase — extending the backend response next,
  then building the real MSP Console module against it.
- 2026-09-13T02:29:32Z ✅ DONE — Shipped in `190bdc26f`:
  - `artifacts/api-server/src/routes/msp-marketplace-purchase.ts`: GET catalog now also returns `serviceClass`,
    `fulfillmentTypeKey`, `fulfillmentKnown` (computed live against `fulfillment_types`), `internalCostCents`;
    POST checkout now also returns `fulfillmentStatus` (both paths) and `wholesaleCostCents`/`retailPriceCents`
    (paid path) — all previously logged/discarded, never returned to the caller.
  - `artifacts/msp-console/src/api/marketplace-purchase-api.ts` + `console/modules/MarketplacePurchase.tsx`
    (new), wired into `nav.ts` (new `marketplace` leaf under the tenant Commercial group) and
    `ConsoleShell.tsx`'s `moduleFor()`. No `embedded`/`forceEmpty` props and no card-on-file toggle, matching
    every other rebuilt module and the design README's own "Screen mount" note that both disappear in the
    real rebuild.
  - Fixed a genuinely pre-existing failing test found mid-build: `nav.test.ts` asserted `MSP_PAGES.length ===
    13` against a real array that had already grown to 15 (later 17, after two rebases picked up concurrent
    #3814/#3815/#3818/#3906 landings) — corrected in the same commit, not filed separately, per the "fixed in
    this build" exception.
  - Verification: `msp-marketplace-purchase.test.ts` 19/19 (added coverage for every new field, including a
    real assertion that `fulfillmentKnown` is `false` for the live `assessment`/`retainer` keys and `true` for
    `monitoring_subscription`); cross-checked the same logic directly against local Postgres
    (`fulfillment_types` × live `services` rows) and got an exact match: 19+1 `assessment` + 6 `retainer` rows
    → `fulfillment_known=false`, 12 `monitoring_tier` rows → `true`. `msp-console` full test suite 16/16,
    `tsc --noEmit` clean, `vite build` clean.
  - No new findings filed — the two things I fixed mid-build (the missing wire fields, the stale test
    assertion) were fixed in this same commit rather than filed, per the "fixed in this build" rule. #3400,
    #3403, #3404, #3405 were already filed by the earlier #3367 contract-pack pass; nothing further to add.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` before writing this line.
