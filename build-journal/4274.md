# Build Journal — #4274

- **Status:** ✅ DONE 2026-09-15T00:30:00Z
- **Scope:** buildconsole (desktop/MyArchitect)

## Log
- 2026-09-15T00:00:00Z — Started. Building per-tab dark-mode toggle for MyArchitect portal WebView2 tabs: `PortalTabItem.IsDarkMode`, persisted local preference (default on, sticky), new toggle button left of `ScreenClipButton`, `AddScriptToExecuteOnDocumentCreatedAsync` injection wired into `OpenPortalTabAsync` + live toggle via `ExecuteScriptAsync`.
- 2026-09-15T00:30:00Z — ✅ DONE — commit `503faf12a`. Added `PortalTabItem.IsDarkMode`/`DarkModeScriptId`; new `IDarkModePreferenceService`/`DarkModePreferenceService` (JSON file under `%LOCALAPPDATA%\MyArchitect\dark-mode-preference.json`, default on, sticky once turned off, seeds only newly-opened tabs); new `DarkModeToggleButton` in `MainWindow.xaml` as first child of the nav-bar's `Grid.Column="3"` StackPanel, directly left of `ScreenClipButton`; invert-filter CSS injected via `AddScriptToExecuteOnDocumentCreatedAsync` in `OpenPortalTabAsync` (survives in-tab navigation) with a live toggle path via `ExecuteScriptAsync` for the already-loaded page in `DarkModeToggleButton_Click`; button visual state (opacity/tooltip) reflects the active tab's `IsDarkMode`, updated in `ActivateTab`/`CloseTab`. Toggle is per-tab only — other open tabs are unaffected. `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors. `git status --porcelain` clean.
