# #2329 — Leading marker sets type: `!` bug, `?` question, `+` idea, `.` works

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-02T19:45:00Z — Session start. Sub-issue 3/28 of #2326 (Feature: Test Pad),
  itself a sub-issue of #1202 (Epic: Build Console). Verified via grep: Test Pad does
  not exist anywhere in `desktop/ShaneBuilder` yet — greenfield feature, marked `BUILT`
  in `ShaneBuilder-FullFeature.md` incorrectly (per #2326's own body, already flagged).
  Sibling issues #2327 (Pill) and #2328 (Composer) are dispatched concurrently
  (buildSet=ShaneBuilderQueueOrder) in their own worktrees, both `in-flight`, neither
  pushed any commits yet — no Composer or Pill exists in this worktree to wire into.
  Scoping this build to a self-contained, decoupled parser (`NoteType` +
  `NoteMarkerParser`) under `Services/TestPad/` that #2328's Composer and #2330's Type
  Chips can consume once they land, rather than duplicating Composer/Pill scaffolding
  that #2327/#2328 are building right now (real collision risk on the same new files).
- 2026-09-02T19:58:00Z — ✅ DONE — Added `desktop/ShaneBuilder/Services/TestPad/NoteType.cs`
  (enum: `Note`/`Bug`/`Question`/`Idea`/`Works`) and `NoteMarkerParser.cs` (pure
  `Parse(rawInput) -> (NoteType, Text)` reading `!`/`?`/`+`/`.` as the first
  non-whitespace char, stripping the marker + one run of trailing whitespace from the
  saved text; plus `MarkerFor(NoteType)` for the inverse, for #2330's Type Chips to
  reuse). No UI wiring — the Composer (#2328) and Pill (#2327) don't exist in this
  worktree yet (still `in-flight`, no commits pushed as of this session). This is
  ready for #2328 to call directly once it lands.
  Verified: `dotnet build` on `ShaneBuilder.csproj` — 0 warnings, 0 errors. Real
  invocation via a throwaway standalone `dotnet run` harness (not committed) against
  9 cases (typed marker + text, marker + double space, no marker, empty string,
  whitespace-only, marker with nothing after it) — all produced the expected
  `(NoteType, strippedText)` pairs; `MarkerFor` round-tripped all 4 typed markers and
  returned `null` for `Note`.
  Commit: `b97121c3f` (implementation), rebased onto and pushed straight to `main` as
  `6f0e9028f`; bookend IN FLIGHT commit was `15e89020f` → pushed as `9f079ff33`.
  `verify-branch-merged.mjs` confirms `6f0e9028f` is an ancestor of `origin/main`.
  `git status --porcelain` clean.
  No findings filed — this build did not touch anything outside its own new files.
