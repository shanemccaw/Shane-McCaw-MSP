# #1709 — BuildConsole: read Batter Up + parse BUILD comment, auto-queue on refresh

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** c03c4c2d9

## Log
- 2026-08-29 ⏳ IN FLIGHT — reading existing QueueBuildAsync launch pipeline, blocked-by (#1600) mechanism, and any existing project-board reads, before adding a new Batter Up panel that parses `BUILD:` comments and auto-queues through the existing pipeline. Additive only — Queue button / Send to Builder untouched.
- 2026-08-29 ✅ DONE — Added `Controls/BatterUpPanel.xaml(.cs)` (new, additive, sits above the existing `BuildQueuePanel` in the Build Queue column) and `Services/BatterUpQueueService.cs` (new). `GitHubApiClient.GetBatterUpIssuesAsync` reads the real "Batter Up" board status via a new GraphQL project-items query (Status field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0`, option `09b1927f`); `GetBlockedByAsync` was factored out of the existing `GetOpenBlockedByAsync` (behavior-preserving extraction) to hand every real declared blocker number through to the queue call. Each item's latest `BUILD:` comment is parsed (`model=`/`effort=`/`buildSet=` header line + prompt body); an item with no `BUILD:` comment is shown in the panel and logged, never auto-queued. On every panel refresh (90s timer + manual refresh button), an untracked item (`BuildQueuePostgresClient.FindDedupCandidateAsync` returns null for its github_number) is auto-queued through the existing `QueueBuildAsync` — the exact same pipeline Queue/Send to Builder already use, including worktree provisioning, account routing, and #1600's fail-closed blocked-by watcher (blocked-by numbers are always passed through; the row sits held until GitHub reports the blocker closed, same as every other launch path). No changes were made to `BuildQueuePanel.xaml(.cs)`, `EditBuildPromptDialog.xaml.cs`, `ChatButtonInjector.cs`, or the `BT_QUEUE_BUILD`/`OpenBuildPromptDialogAsync` handlers — confirmed via `git diff --stat` before commit (only `MainWindow.xaml`/`.xaml.cs` and `GitHubApiClient.cs` picked up additive lines). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 Warnings, 0 Errors. Branch verified merged to `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain` clean. No test manifest added (desktop/BuildConsole is out of scope per CLAUDE.md). No new findings filed — this build stayed entirely inside its own scope.
