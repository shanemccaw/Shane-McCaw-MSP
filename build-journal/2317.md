# #2317 — Per-issue "why is this stuck" reasoning on every open issue

- **Status:** ⏳ IN FLIGHT 2026-09-02T19:50:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02

## Log
- 2026-09-02T19:50:00Z ⏳ IN FLIGHT — Sub-issue of #2313 (Feature: Git Map), item 5 of 7 in
  `ShaneBuilder-FullFeature.md`. Sequenced after #2314 (DONE, `7e1418877`) per `--blocked-by 2314`.
  Adding real per-issue "why is this stuck" reasoning to `GitMapFeature` (`Services/GitMapService.cs`)
  off real signals only: (1) real `blocked_by` dependency edges via `GetBlockedByAsync`
  (`GitEpicPanelService`, same REST endpoint CLAUDE.md's own recipe uses) when the feature carries
  the real `blocked` label, (2) real GraphQL `createdAt`/`updatedAt` staleness against
  `GitPanelService.StallThresholdDays` (already-established 21-day threshold) when it doesn't. No
  fabricated text — a feature with no blocker and recent activity gets a null reason and renders
  nothing.
