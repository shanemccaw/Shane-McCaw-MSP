# #2317 — Per-issue "why is this stuck" reasoning on every open issue

- **Status:** ✅ DONE 2026-09-02T20:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 9424e17c5

## Log
- 2026-09-02T19:50:00Z ⏳ IN FLIGHT — Sub-issue of #2313 (Feature: Git Map), item 5 of 7 in
  `ShaneBuilder-FullFeature.md`. Sequenced after #2314 (DONE, `7e1418877`) per `--blocked-by 2314`.
  Adding real per-issue "why is this stuck" reasoning to `GitMapFeature` (`Services/GitMapService.cs`)
  off real signals only: (1) real `blocked_by` dependency edges via `GetBlockedByAsync`
  (`GitEpicPanelService`, same REST endpoint CLAUDE.md's own recipe uses) when the feature carries
  the real `blocked` label, (2) real GraphQL `createdAt`/`updatedAt` staleness against
  `GitPanelService.StallThresholdDays` (already-established 21-day threshold) when it doesn't. No
  fabricated text — a feature with no blocker and recent activity gets a null reason and renders
  nothing.
- 2026-09-02T20:20:00Z ✅ DONE — `GitMapFeature` (`Services/GitMapService.cs`) now carries real
  `CreatedAt`/`UpdatedAt` (added to the existing sub_issues GraphQL query) and a settable
  `StuckReason`. New `GitMapService.OverlayStuckReasonsAsync(features)` (same post-fetch overlay
  convention as `OverlayParkPauseAsync`) computes it per open feature: a `blocked`-labeled feature
  gets a real `dependencies/blocked_by` REST lookup via `GitEpicPanelService.GetBlockedByAsync` —
  names the real open blocker(s), or flags a stale `blocked` label with no real edge; an unblocked
  feature gets real GraphQL staleness (`CreatedAt` old enough AND `UpdatedAt` past
  `GitPanelService.StallThresholdDays`, the same 21-day threshold the Milestone panel's stalled-
  feature detector already uses) as `"no activity in Nd"`. Null reason (recently touched,
  unblocked) renders nothing. Wired into `LoadGitMapEpicFeaturesAsync` in `MainWindow.xaml.cs`
  alongside the existing Park/Pause overlay — single-sourced for both the mini rail panel and the
  full Git Map doc tab (`GitMapFeatureCard`), which now renders the reason as a dim italic line
  under the burndown bar when non-null. Verified: `dotnet build` clean (0 warnings/errors), twice
  (once pre-rebase, once post-rebase after a real merge conflict with concurrent #2315's mirrored-
  pairs commit in the same file — resolved by keeping both independently-added methods, no logic
  changed). Commit `9424e17c5` pushed to `main` after two real concurrent-push rebase/retry cycles
  (main moved under #2315 and another commit mid-session); `verify-branch-merged.mjs` confirms
  ancestor. `git status --porcelain` clean.
