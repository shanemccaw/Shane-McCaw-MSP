# #2332 — Status band: "Claude is working — N waiting" → "Claude is free — send N" → "Nothing waiting"

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Test Pad feature, #2326)
- **Started:** 2026-09-02
- **Commit(s):** fa85aa358

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — investigated TestPad shell (#2327) and confirmed there is no
  existing "Claude is working" signal anywhere in ShaneBuilder — the chat surface is a WebView2
  embedding claude.ai itself (MainWindow.xaml.cs `ClaudeWebView`), not a native session object.
  Building a real DOM-polled busy signal (mirrors the existing `TryReadLastAssistantTurnAsync`
  ExecuteScriptAsync pattern) plus the status band UI in TestPadWindow.
- 2026-09-02T20:05:00Z ✅ DONE — added `Services/ClaudeActivityService.cs` (static, thread-safe,
  never-throws — same shape as `TestPadService`) holding a real `IsWorking` bool + `Changed` event.
  `MainWindow.InitializeTestPad` now starts a 2s `DispatcherTimer` that polls the active chat tab's
  live `ClaudeWebView` DOM via `ExecuteScriptAsync` for claude.ai's own "Stop response"-labelled
  button (the only real signal available since the chat surface is the actual site, not a wrapped
  process) and reports the result into `ClaudeActivityService`. `TestPadWindow` renders a status band
  between the header and the notes list, subscribed to both `TestPadService.NotesChanged` and
  `ClaudeActivityService.Changed`: "Nothing waiting" when `TestPadService.UnsentCount == 0`, else
  "Claude is working — N waiting" / "Claude is free — send N" off `ClaudeActivityService.IsWorking`.
  Rebased onto a concurrent #2333/#2334 landing (notes-list rows + "By feature" grouping) that
  touched the same `Render()` method — merge conflict resolved by keeping both methods side by side
  (`BuildRow` + the new `RenderStatusBand`), reverified with a clean rebuild after resolving.
  Build: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors,
  both before and after the rebase. `verify-branch-merged.mjs` confirms `fa85aa358` is an ancestor of
  `origin/main`. `git status --porcelain` clean. No fixture data — both the note count and the busy
  state are read live off real app/DOM state.
  Known honest gap (not this issue's scope, and not a finding — the composer/"Send to Claude"
  action this band gates on don't exist yet, per #2328/#2337): the claude.ai "Stop response"
  aria-label heuristic is a best-effort DOM read against a third-party site's current markup, same
  category of fragility as the existing `TryReadLastAssistantTurnAsync`/`TrySendToClaudeAsync`
  selectors it's modeled on — if claude.ai's markup changes, this degrades to always reporting
  "free," not a crash.
