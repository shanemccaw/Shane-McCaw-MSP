# #3656 — Feature: Per-Tenant Home Screen (MyArchitect)

- **Status:** ✅ DONE 2026-09-15T23:15:00Z
- **Scope:** platform (MyArchitect desktop app — private repo shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect `65639a9f485f6577022765576c9177c1fad3b8ef` (feature),
  `b36ecbe` (MyArchitect-repo-local bookend). This repo's own commits are bookend-only —
  the real feature code lives entirely in `shanemccaw/MyArchitect`.

## Log
- 2026-09-15T22:45:00Z ⏳ IN FLIGHT — Confirmed MyArchitect's app source now lives in the
  private `shanemccaw/MyArchitect` repo (extracted by #4305, accessibility work #4284 landed
  right before this session started). `desktop/MyArchitect/` no longer exists in this monorepo.
  Cloned `shanemccaw/MyArchitect` to a scratch working copy. Confirmed #3493 (UI Shell Redesign,
  the standing prerequisite for any MyArchitect UI work) is CLOSED/complete — not blocked.
  Audited the Home tab's real current state (`RegisterHomeTab()` in `MainWindow.xaml.cs`):
  it already has several ribbon groups (Tenant, Views, Remediation Plan, POA&Ms, CAB, Retainer
  Hours, Automation Registry) — richer than the issue body's "action buttons only" framing
  suggested, but confirmed no auto-loading dashboard document exists (grep for
  Home/Dashboard/TenantHome concepts came up empty beyond the ribbon buttons). Read the five
  real service interfaces this issue's checklist points at (IRetainerService, ITelemetryService,
  IAssessmentService, IRemediationTrackerService + IChangeControlService, IAlertsService) and
  their real wire models. Read UI_RULES.md in full (theming §10/§11 — zero inline
  FontSize/FontWeight/Foreground literals, use Typography.xaml/Colors.xaml keys only) and the
  existing SowAssessmentView.xaml(.cs)/TelemetryConsoleView.xaml(.cs) as the real reference
  pattern for a tenant-scoped auto-loading document (Initialize/SetAuthToken/LoadForTenantAsync,
  subscribe to `ITenantService.CurrentTenantChanged` directly rather than relying on ShowDocument
  alone, so background refresh happens even while the doc isn't visible). Confirmed the async
  discipline from #3564/#3618: no `.GetAwaiter().GetResult()`, await inside UI event handlers
  resumes on the UI SynchronizationContext already (no manual Dispatcher.Invoke needed for plain
  awaited HTTP calls — that guard is reserved for callbacks originating on a genuine non-UI
  thread, e.g. SSE handlers), each of the 5 data sections' fetch wrapped in its own try/catch so
  one failing source doesn't blank the other four.
  Next: build `Controls/HomeDashboardView.xaml(.cs)` (5 real sections: hour utilization,
  telemetry/what-changed, SOW/assessment, open remediation items, tenant alerts), wire it into
  `MainWindow` (register as a document, tenant-change-triggered refresh, a real "Dashboard"
  ribbon command, and per-section click-through navigation), verify with a real tenant-switch
  repro, then flip this bookend to DONE with the real commit hash.
- 2026-09-15T23:15:00Z ✅ DONE — Built `Controls/HomeDashboardView.xaml(.cs)` combining all
  five real sources: hour utilization (IRetainerService), latest telemetry/drift
  (ITelemetryService), SOW/assessment (IAssessmentService), open remediation items
  (IRemediationTrackerService's real `terminalState == "outstanding"` signal +
  IChangeControlService's non-terminal change requests), and tenant alerts (IAlertsService).
  Subscribes directly to `ITenantService.CurrentTenantChanged` (same pattern as the existing
  SowAssessmentView/TelemetryConsoleView) so all five sections refresh in the background on
  every tenant switch, independent of which document is currently visible — matching the
  issue's own "one auto-loading view triggered by CurrentTenantChanged" framing. Each section
  fetches/renders/fails independently via `Task.WhenAll` over five real HTTP calls, each in
  its own try/catch, so one failing source (e.g. a 403 on Retainer for a non-platform-admin
  operator) never blanks the other four. Wired a new "Dashboard" ribbon command into the Home
  tab's Views group plus a command-palette destination; each section links into its closest
  real full destination (Assessment/Telemetry open their existing real dashboards; Remediation/
  Alerts switch to the ribbon tab hosting their real gallery/live-count group; Hour Utilization
  opens the existing Log Hours record — no dedicated full retainer ledger view exists yet, by
  #3474's own deliberate scope).
  Committed and pushed to `shanemccaw/MyArchitect` main as `65639a9f485f6577022765576c9177c1fad3b8ef`
  (rebased cleanly onto a concurrent #3655 "I'm Working" toggle commit that landed mid-session;
  re-verified with a clean rebuild after rebase).
  **Verification, honest:** `dotnet build` clean (0 warnings/errors) before and after the
  rebase. Grep-confirmed zero raw `FontSize`/`FontWeight`/`FontFamily`/hex-color literals in
  the new file (UI_RULES.md §10). Launched the built exe twice (8s, then 9s runs) — process
  stayed alive/responding both times, and `%LocalAppData%\MyArchitect\logs\crash.log` gained
  zero new entries across either run, confirming `HomeDashboardDocument.Initialize()` (called
  eagerly in the MainWindow constructor) and the new XAML's resource resolution don't throw at
  startup. Read `TenantService.LoadTenantsAsync`'s real `Commit()` wrapper to confirm
  `CurrentTenantChanged` is already safely marshaled onto the UI thread by #3618's fix, so this
  view's identical-pattern subscription is safe. **Could not drive a full live tenant-switch UI
  repro with real backend data in this sandboxed session** — no local dev api-server was
  reachable (`curl localhost:8080/...` → connection refused on all three endpoint families this
  view calls) and no Microsoft device-code sign-in could be completed non-interactively;
  reporting this honestly (Git #2160) rather than claiming a live-data repro that didn't
  actually happen.
  **Finding filed:** #4310 — UI_RULES.md §10's "Update (#4284)" claims the zero-inline-theming
  rule is "fully in force across the whole codebase," but a real grep shows 171 raw
  FontSize/FontWeight/FontFamily literals still remain across 13 pre-existing files. Parented
  under EPIC #3454 (MyArchitect), labeled `bug`, milestone v1.1, board status AI Batter Up.
  Working tree clean in both this worktree and the MyArchitect clone (`git status --porcelain`
  empty in both).
