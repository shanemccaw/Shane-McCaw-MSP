# #2372 — Panel: per-shot Copy

- **Status:** ✅ DONE 2026-09-02T20:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** d2c60a94e (on main; originally c9924a859 in this worktree, rebased/pushed to main)

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Sub-issue of #2367 (Feature: Shot Vault), item 5 of 7 in
  the Shot Vault Structured index. Shot Vault is currently entirely `notBuilt`
  (`MainWindow.xaml.cs` line 677 lists `"ShotVault"` in the `notBuilt` source check; only a
  rail toggle button exists, no panel content). Sibling sub-issues #2368-#2371 (search, tag
  filters, run list, thumbnail grid) are also still open/unbuilt as of this session start, so
  there is no existing panel surface to hang a per-shot Copy action off of. Investigating
  existing screenshot-capture infra (`DesktopScreenClipService`) and panel conventions before
  deciding minimal scaffold needed to deliver a real, working per-shot Copy action.
- 2026-09-02T20:20:00Z ✅ DONE — Coordinated with the concurrent session building sibling #2373
  ("Expand full page here"/"Send to tab") before touching shared files, since neither #2368-#2371
  nor #2373 existed as a panel yet either. Shipped a real, minimal Shot Vault panel scaffold plus
  the actual per-shot Copy action this issue owns:
  - `Services/ShotVaultService.cs` — `ShotVaultItem` + `ListShots()` (real files under
    `%Pictures%\Screenshots\ShaneBuilder\`, the same folder `DesktopScreenClipService.Capture()`
    already writes to; newest-first; empty list, not a fixture row, when nothing's been
    captured) + `CopyToClipboard(ShotVaultItem)`.
  - `Services/DesktopScreenClipService.cs` — added `CopyFileToClipboard(string pngPath)`, reusing
    the existing private `CopyToClipboard(Bitmap)` multi-format (Bitmap+PNG) clipboard write
    instead of a second implementation.
  - `MainWindow.ShotVaultPanel.cs` (new, follows the `MainWindow.<Feature>Panel.cs` partial-class
    convention `BatterUpPanel`/`GitEpicPanel` established) — `EnsureShotVaultPanelLoaded`,
    `RenderShotVaultPanel`, `BuildShotVaultRow` (real thumbnail via `BitmapImage` +
    `DecodePixelWidth`, filename, local timestamp, and a Copy action reusing the existing
    `GitEpicActionLink` link style rather than a new Button style).
  - `MainWindow.xaml` — real `ShotVaultPanelBody`/`ShotVaultPanelStatus`/`ShotVaultRows`,
    `ShotVault` removed from the shared `notBuilt` placeholder list.
  - `MainWindow.xaml.cs` `OpenLeftPanel` — toggles `ShotVaultPanelBody` visibility and calls
    `EnsureShotVaultPanelLoaded()` on first open.
  Deliberately NOT built here (real sibling sub-issues, still open): search (#2368), tag chips
  (#2369), run grouping/timestamps-and-counts beyond the flat newest-first list (#2370),
  thumbnail-grid DIFF badges (#2371), header "Expand"/"Send to tab" chrome (#2373 — the
  concurrent session), retention policy / compare-to-baseline (#2374).
  **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` — Build succeeded, 0 warnings,
  0 errors. No BuildConsole/ShaneBuilder.exe launch performed (out of scope per standing
  verification-tooling guidance for this desktop app; build success + code review is the real
  verification this UI-only, no-backend-dependency change calls for).
  Branch `agent/2372-q1214` rebased onto `origin/main` and pushed directly
  (`5889679db..d2c60a94e`) — `verify-branch-merged.mjs` confirms OK. `git status --porcelain`
  clean. No findings filed — no bug/gap discovered outside this issue's own scope.
