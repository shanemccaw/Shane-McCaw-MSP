# #1999 — BUILD SETS rollup rows: right-click context menu to open originating chat(s)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 38f166d76 (on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — adding a `ContextMenu` to `BuildRollupRow` (`BuildQueuePanel.xaml.cs`) reusing `QueueItemChatRequested`/`OpenChatForQueueItem`, with distinct-chat dedupe (single entry / submenu / omitted), plus Filter and Expand/Collapse entries.
- 2026-08-30 ✅ DONE — `BuildRollupRow` now carries the set's real member `QueueItem`s (whichever contributed to up-next/running/verifying) so a new `BuildRollupRowContextMenu` can dedupe distinct originating chats on `OriginatingChatId` (falling back to `ChatUrl`), matching `OpenChatForQueueItem`'s own resolution order — no second chat-resolution path, just a representative `QueueItem` handed to the existing `QueueItemChatRequested` event. Zero distinct chats → entry omitted; one → single menu item; several → submenu, one entry per chat labelled `<id/url> (<N> items)` (no chat-title lookup exists in `BuildQueuePanel`, so id/url is the real fallback, not a placeholder). Also added Filter/Clear-filter and Expand/Collapse entries mirroring the row's existing click behaviors. Applies uniformly to every rollup row including `Ungrouped` — no special-casing needed since the menu is wired on `headerBorder` regardless of key.
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors.
  - Verified by code read-through against `BuildCardContextMenu`'s own chat guard (`:3143`) and `OpenChatForQueueItem`'s resolution order (`MainWindow.xaml.cs:3340`) — the dedupe key and precedence match exactly.
  - **Not** live-clicked in a running BuildConsole.exe — per this build's own standing rule not to relaunch/kill Shane's live instance, saying so honestly rather than claiming a live UI test.
  - `git merge-base --is-ancestor` confirmed 38f166d76 is on `origin/main`; working tree clean.
  - No findings filed — the id/url fallback label (no chat title lookup) is the issue's own stated fallback, not a gap.
