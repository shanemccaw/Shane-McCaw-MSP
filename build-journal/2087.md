# #2087 — Worktree session cannot typecheck a downstream package against its own in-worktree lib/db changes pre-merge

- **Status:** ✅ DONE 2026-09-06T17:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** `e4fa5491c` (fix + docs, pushed to `main`)

## Log
- 2026-09-06T15:20:00Z ⏳ IN FLIGHT — Read #2087 body+dispatch comment. Per dispatch instructions, auditing whether #2121's landed consolidated fix (commit `3b990356e`, DONE bookend `8b9f404f0`) already resolves this exact symptom before designing a separate fix. #2121's own original issue body describes the identical scenario (worktree lib/db edit invisible to a downstream package's tsc pre-merge) and its DONE comment claims live verification via a marker-injection probe. Verifying independently in THIS worktree (2087-q1776) before concluding.
- 2026-09-06T17:05:00Z ✅ DONE — **Verdict: #2121 does NOT fully cover #2087.** #2121's `worktree-isolation.selftest.mjs` (ran here, 20/20 pass) and a `require.resolve` probe confirmed it correctly fixed **Node module resolution** (`node`/`tsx`/`vitest`) for worktree-local `lib/*` edits. But a **live reproduction** proved a distinct, still-real gap: `artifacts/api-server`'s `tsconfig.json` lists `lib/db` under `"references"` (TypeScript project references + `composite: true`), and under that model plain `tsc -p tsconfig.json --noEmit` gets cross-package types from `lib/db`'s **already-built `dist/*.d.ts`**, not live source — a separate mechanism `#2121`'s `node_modules` junction fix never touches. Proof: added a new export to `lib/db/src/index.ts`, then a probe file in `api-server` importing it — bare `tsc -p --noEmit` failed with `TS2305: has no exported member`, while `tsc --build` (which auto-rebuilds stale referenced projects) passed clean. Repeated the same reproduction for `artifacts/portal` + `lib/copilot-scan-scene` (a second, independent composite-reference chain) with identical results, confirming this isn't specific to `lib/db`. Found `api-server` already carries a `pretypecheck: tsc -b ...` script that silently fixes this for itself (via npm/pnpm's pre-script lifecycle) — but 4 other artifacts referencing a composite `lib/*` package (`admin-panel`, `msp-console`, `msp-website`, `portal`) had no matching hook, and root `tsconfig.json`'s own `references` list (used by root `pnpm run typecheck`'s `tsc --build` step) was missing `lib/copilot-scan-scene` entirely (confirmed composite, referenced by `portal`). **Fix shipped (`e4fa5491c`):** added `pretypecheck: tsc -b <same paths as that artifact's own tsconfig references>` to all four missing artifacts, added the missing `lib/copilot-scan-scene` entry to root `tsconfig.json`, and documented the distinction + sanctioned verify command (`pnpm run typecheck` / `pnpm --filter <artifact> run typecheck`, never bare `tsc -p ... --noEmit`) in `scripts/dev-server/README.md`. **Verification:** re-ran both reproductions after the fix — `pnpm run typecheck` in both `api-server` (pre-existing hook, unaffected) and `portal` (new hook) now resolves the fresh export cleanly with zero errors; `npx tsc --build` at repo root (root `typecheck:libs`) succeeds across all composite libs including the newly-referenced `copilot-scan-scene`; all 4 new `pretypecheck` scripts run standalone with exit 0. All temporary probe files/marker exports reverted before commit; `lib/db/dist` and `lib/copilot-scan-scene/dist` rebuilt clean afterward. Branch `agent/2087-q1776` rebased onto `origin/main`, pushed directly to `main` (`e4fa5491c`), verified merged via `verify-branch-merged.mjs` (exit 0). Working tree clean. No new findings filed — this build's own scope fully covers what it found. No production/app-registration boundary touched.
