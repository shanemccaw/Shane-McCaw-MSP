# #4210 — Map raw status-badge hex colors in HomeView.xaml to real brushes

- **Status:** ✅ DONE 2026-09-15T05:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** ff9666fb2 (real work), merges 5cd255963/32d937c58

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — investigating HomeView.xaml/.cs, Colors.xaml, DarkTheme.xaml for the 4 status-badge colors (#1E3A2F, #3E2E1E, #3E1E1E, #2A2B3D/#1E2A3A)
- 2026-09-15T05:40:00Z ✅ DONE — added 6 opaque badge brushes (StatusSuccessBadgeBrush/StatusWarningBadgeBrush/StatusErrorBadgeBrush/StatusRunningBadgeBrush/StatusActiveBadgeBrush/StatusWarningBadgeAltBrush) to `Themes/Colors.xaml`; wired all 7 XAML `Background="#1E3A2F"` placeholders in `HomeView.xaml` and all 8 `ColorConverter.ConvertFromString(...)` raw-hex call sites in `HomeView.xaml.cs` to reference them. `dotnet build BuildConsole.csproj` succeeds with these 3 files changed (`Controls/HomeView.xaml`, `Controls/HomeView.xaml.cs`, `Themes/Colors.xaml`). Hit an unrelated pre-existing build break in `Controls/GitModeContainer.xaml` (mismatched Grid/StackPanel tags, confirmed byte-identical to origin/main's copy modulo line endings) while verifying — filed as #4239 (URGENT, parented under #1202, AI Batter Up), scratch-patched locally only to confirm my own 3-file diff compiles clean, then reverted the scratch patch before committing. Out of #4210's scope per the issue body: mascot illustration fills (35 instances) and `#F2CA63` icon foreground — both explicitly deferred by #4210 to Shane's own design-system call, not touched here.
