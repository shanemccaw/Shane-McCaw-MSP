# #1508 — Risk Register: RBD versioning and the supersession chain

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** c1eaf649c8c37363b561f04444ece46439869f7b

## Log
- 2026-08-30 ⏳ IN FLIGHT — building `msp_rbd_versions` (whole-document versioning, `supersededAt` chain following `drift_baseline_snapshots`' precedent), a manual migration, and MSP-side routes to create/supersede a version and read current/history. Foundation only, per the issue's explicit scope stop — no `artifacts/portal` UI, no line-item model (#1509), no signature-on-expansion logic (#1510), no role-based authority (#1511).
- 2026-08-30 ✅ DONE — shipped at `c1eaf649c8c37363b561f04444ece46439869f7b`:
  - `lib/db/src/schema/msp.ts` — `mspRbdVersionsTable` (whole-document version rows, `supersededAt` chain identical in mechanism to `drift_baseline_snapshots`; `content` is untyped jsonb on purpose, #1509 hasn't formalized the line-item shape yet)
  - `lib/db/migrations/manual/2026-08-30-rbd-document-versioning-1508.sql` — additive `CREATE TABLE`, run against local `DATABASE_URL`, verified via `information_schema.columns` in the migration output and `simulator_migration_runs` self-marking row
  - `artifacts/api-server/src/lib/rbd-versioning.ts` — `createRbdVersion` (transactional supersede-then-insert), `getCurrentRbdVersion`, `listRbdVersions`, `signRbdVersion` (only the current, unsigned version is signable)
  - `artifacts/api-server/src/routes/msp-rbd-versions.ts` — `GET /api/msp/rbd/:rbdId/versions`, `GET .../versions/current`, `POST .../versions`, `PATCH .../versions/:versionUid/sign`; registered in `routes/index.ts`
  - Verification: `tsc --noEmit` clean on both `lib/db` and `artifacts/api-server` (had to rebuild `lib/db`'s junctioned `dist/` — the running project-reference declarations were stale from before this session's schema edit; rebuilt via `tsc -b lib/db`, no `pnpm install` involved). The full supersede/current/sign-guard transaction sequence proven directly against local Postgres inside a rolled-back `BEGIN…ROLLBACK` block (two versions captured, exactly one `supersededAt IS NULL` row at a time, re-signing a superseded version correctly updates 0 rows) — no residue left in the DB. Live `msp_rbd_versions` row count confirmed at 0 post-migration (nothing has written to it yet, expected — this build is plumbing only). `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed the branch is a real ancestor of `origin/main` before writing this.
  - Migration filename recorded on **#1630** (issue comment).
  - No portal UI was created — scope stop honored (`artifacts/portal` untouched; #1509/#1510/#1511/#1512 are the follow-on issues this table is the foundation for).
