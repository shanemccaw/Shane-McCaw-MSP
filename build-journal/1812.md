# #1812 — Separate dev App Registrations from Production

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigated the 3 App Registrations currently shared between
  `.env.local` and production: MT_APP (`9ea2e409-...`, 33 Graph + 2 Exchange + 1 SharePoint +
  1 Office 365 Mgmt API perms, has a cert), MT_APP_WRITE (`3308b280-...`, 4 Graph perms,
  secret only), and the Script Runner/Graph-email app `ba40ca4d-...` (9 Graph perms, secret
  only, plus 3 Azure RBAC roles on its SP: Automation Operator, Automation Contributor,
  Key Vault Secrets Officer, all scoped to the single existing Automation Account/Key Vault
  — no separate dev copies of those Azure resources exist). Extracted each app's exact
  `requiredResourceAccess` manifest block to replicate verbatim on the new dev apps (avoids
  any permission-name-resolution translation error). Confirmed via a live RBAC query that
  MT_APP/MT_APP_WRITE have zero Azure RBAC role assignments (Graph-permission-only apps).
  Creating the 3 dev App Registrations next.
