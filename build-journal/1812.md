# #1812 — Separate dev App Registrations from Production

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** bc1cad932 (drift-check tooling + baselines; the App Registration creation
  itself is Azure AD state, not a repo commit — see log below for the real IDs)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigated the 3 App Registrations currently shared between
  `.env.local` and production: MT_APP (`9ea2e409-...`, 33 Graph + 2 Exchange + 1 SharePoint +
  1 Office 365 Mgmt API perms, has a cert), MT_APP_WRITE (`3308b280-...`, 4 Graph perms,
  secret only), and the Script Runner/Graph-email app `ba40ca4d-...` (9 Graph perms, secret
  only, plus 3 Azure RBAC roles on its SP: Automation Operator, Automation Contributor,
  Key Vault Secrets Officer, all scoped to the single existing Automation Account/Key Vault
  — no separate dev copies of those Azure resources exist). Extracted each app's exact
  `requiredResourceAccess` manifest block to replicate verbatim on the new dev apps (avoids
  any permission-name-resolution translation error). Confirmed via a live RBAC query that
  MT_APP/MT_APP_WRITE have zero Azure RBAC role assignments (Graph-permission-only apps).
  Creating the 3 dev App Registrations next.
- 2026-08-30 ✅ DONE — Created 3 dev App Registrations with permissions replicated verbatim
  from each prod app's `requiredResourceAccess` manifest (avoids any name-resolution
  translation error): **MSP Platform – Multi-Tenant Consent (DEV)**
  (`4743b130-0379-41bf-b863-ec8de96d915a`, 33 Graph + 2 Exchange + 1 SharePoint + 1 O365
  Mgmt API perms, admin-consented — confirmed 36 live appRoleAssignments), **MSP Platform
  Write (DEV)** (`9f6f4772-b5be-421f-815e-b392336c373a`, 4 Graph perms, admin-consented),
  **Replit ShaneMcCaw.com (DEV)** (`16959be3-40b9-4cc0-b256-3fa771db3533`, 9 Graph perms +
  the same 3 Azure RBAC roles as prod — Automation Operator, Automation Contributor, Key
  Vault Secrets Officer — on the single existing Automation Account/Key Vault, no separate
  dev copies of those Azure resources exist). Generated a new RSA cert for MT_APP (DEV)
  (SharePoint app-only mandates cert, not secret) via openssl, uploaded the public cert,
  confirmed thumbprint match; cert files at `C:\Source\Certs\Dev\mt-app-dev-*.pem`.
  Patched all 10 corresponding `.env.local` vars (MT_APP_CLIENT_ID/SECRET/CERT_PRIVATE_KEY/
  CERT_THUMBPRINT, MT_APP_WRITE_CLIENT_ID/SECRET, AZURE_CLIENT_ID/SECRET, GRAPH_CLIENT_ID/
  SECRET) — never printed any secret/key content to output at any point (charCode-built
  escape sequences, structural-only verification). **Live-verified all 3**: a real
  `core:security-baseline` scan (24 checks, 13 ok, 0 error, 11 honest license_gap,
  including a successful SharePoint cert-auth call) against tenant 1 confirms MT_APP (DEV)
  fully works; standalone client-credentials token acquisition confirmed for MT_APP_WRITE
  (DEV) and ScriptRunner/GRAPH (DEV). Swept the repo for other hardcoded references to the
  3 old prod app IDs (`Grep` tool) — the only 2 real-code hits (`graph.ts` comment,
  `scripts/verify-write-app-consent-645.mjs`) are both deliberately prod-scoped (documents
  what real customers see in Microsoft's own consent screen; verifies prod's actual
  consent state) and correctly stay pointed at prod — left untouched. Built
  `scripts/azure/check-dev-app-registration-drift.mjs` (read-only, requires `az` login) —
  verifies `.env.local` still points at the dev apps (not reverted to prod) and that each
  dev app's live permissions still match a committed baseline snapshot taken from prod at
  creation time (`scripts/azure/app-registration-baselines/*.json`); logs every run to
  `scripts/azure/app-registration-drift.log` (gitignored, local-only). Confirmed clean run.
  `git status --porcelain` clean, all secret-bearing temp files deleted, `main` verified
  merged into `origin/main` (d27cb7a9) before writing this.
