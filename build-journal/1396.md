# #1396 — Testbed reset (#1329) missed the projects table and 14 real FK-dependent child tables

- **Status:** ✅ DONE 2026-09-05T00:35:00Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 55f9165ee (original #1396 patch), e89ddb664 (#1471 folded it into the single self-sufficient reset file) — both already ancestors of `origin/main` before this session started; this session's own commit adds a generalized coverage-audit tool, no change to the reset SQL itself.

## Log
- 2026-09-05T00:30:00Z ⏳ IN FLIGHT — Read #1396's full issue body/comments, then #1329's for context on the reset mechanism this patches.
- 2026-09-05T00:35:00Z ✅ DONE — **The specific gap #1396 reports (projects.client_user_id and its 14 FK-dependent children) was already fixed and merged before this session started.** Confirmed via `git merge-base --is-ancestor`: `55f9165ee` (the original patch) and `e89ddb664` (#1471's later fold-everything-into-one-file consolidation) are both real ancestors of `origin/main`. Confirmed against the live local Postgres 18 database (`DATABASE_URL`): `simulator_migration_runs` shows `2026-08-27-testbed-reset-patch-projects-1396.sql` already ran (2026-08-30), and `projects` currently has 0 rows.

  This build's real remaining ask — "extend the reset mechanism to catch this FK shape class, not just patch the 14 tables named" — was not yet done: every prior pass (#1329, #1396, #1471) discovered gaps via one-off, hand-run `information_schema` queries, then hardcoded the resulting table names into the migration file. Nothing repeatable existed to catch the *next* FK-shape gap before it reaches Shane as a live bug report again.

  Built `scripts/testbed-reset-audit.mjs`: queries REAL foreign-key constraints (not column-name guesses) for every table referencing `tenants(id)`, `users(id)` or `projects(id)`, diffs against the reset migration's actual coverage, and exits non-zero with the concrete gap list if anything new is uncovered. Ran it against the current schema — it does generalize the search correctly (found the previously-fixed `client_user_id` shape immediately, no false negative), but it also surfaced 50 additional real FK edges the reset still doesn't cover (some clear oversights, some — auth/session/MFA/webauthn tables — a real product decision on whether a tenant reset should log the test account out). Filed as its own finding, **#2946**, parented under #2493 (the Feature-tier parent #1396 itself sits under) rather than folded into this bookend, per the "file every finding separately" rule — #1396's own scope was the already-fixed `client_user_id` gap, not the newly-discovered 50.

  No TypeScript/UI touched (test-manifest scope is `artifacts/msp-portal` / `artifacts/shane-mccaw-consulting` only — not applicable). No new subsystem/route added, so no new logging channel needed.

  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE bookend, and `git status --porcelain` is clean after this session's own commit.
