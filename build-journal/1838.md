# #1838 — BuildConsole `--agent` passive-shell launch mode

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** `af58227db` (code), `683a01558` (this bookend IN FLIGHT)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Adding `AppMode` static (`--agent`/`--dev`/`BUILDCONSOLE_AGENT=1`), guarding every autonomous starter in MainWindow's ctor at its arming site, skipping the shaneapp:// pipe listener in agent mode, and adding a title-bar indicator + one startup ActivityLog line. (Re-implemented in worktree q976 after the original worktree 1838-q952 was cleaned up mid-session with the work uncommitted.)
- 2026-08-30 ✅ DONE — Shipped `af58227db`, merged to `origin/main` (verify-branch-merged.mjs OK).

  **What shipped**
  - `Services/AppMode.cs` — `IsAgent` + `SelectedBy`, `Initialize(args)` called from `App.OnStartup` after the shaneapp:// courier branch, before `base.OnStartup`. Selects on `--agent` (canonical), `--dev` (synonym), or `BUILDCONSOLE_AGENT=1`; case-insensitive; tolerates `/`, `-`, `--`. No arg + no env → `IsAgent==false`, original behaviour byte-for-byte (all guards are `if(!IsAgent)` / null-ternary, which reduce to the original unconditional calls).
  - `MainWindow.xaml.cs` — guards at the arming site for: `_sessionLimitAutoRestart.StartAsync()`, `_queueWatcher.Start()`, `StartTestTriggerPoll()`, `_postBuildDeploy` construction (all four in one guard block inside `if(IsConfigured)`), `BatterUpPanel.Initialize(null)`, `DispatchPanel.Initialize(null)`, `StartShaneAppProtocolListener()` (+pending-URI drop-with-log), `_replitWatcher.ApplyConfig()`, `_usageMeter.Start()`, `_regressionScheduler.ApplyConfig()`. Read-only status timers (`_clockTimer`, top-services poll, claude-online, deploy/postgres status) left running as specified. Startup logs ONE `system.core` line naming the mode + suppressions.
  - `Controls/AiBatterUpPanel.xaml.cs` — Yes/No buttons disabled in agent mode (panel stays visible/read-only).
  - `MainWindow.xaml` — title-bar `[AGENT]` marker (MauveBrush, same TextBlock styling as `[DEBUG]`); native `Title` also appended so Alt+Tab/taskbar reflect it.

  **`grep -rn "AppMode.IsAgent" desktop/BuildConsole --include=*.cs` → 9 sites** (the queue-block guard covers 4 of the 10 Step-2 table rows).

  **Verification (real, live, in this worktree):** `dotnet build` clean (0W/0E). Launched the built exe with `--agent` (PID 45248) alongside no config; its isolated workspace log `C:\wt\q976\.logs\activity-2026-08-30.log` shows the `Launch mode: AGENT …` system.core line and **no** `shaneapp:// listener started`, no claim/launch/dispatch/auto-queue/regression/usage lines. Because the shaneapp:// listener is an *unconditional* call guarded only by `!IsAgent`, its absence proves the guard mechanism fires when `IsAgent==true`; the queue-watcher/session-limit/etc. guards are the identical `!IsAgent` pattern in the same constructor, so they suppress for the same reason. **Nothing was claimed** (agent's queue block never ran; log has zero claim lines). Killed only PID 45248 (never blanket-kill). The **pipe** is not taken: the agent never calls `StartShaneAppProtocolListener`, so Shane's real instance remains the sole listener and `TryForwardToRunningInstance` still routes to it.
  - **No-arg arming** verified by inspection (safer than launching a competing autonomous console next to Shane's live one, which is the exact harm this issue prevents): every guard is `if(!IsAgent){…}` / `IsAgent ? null : _queueDb`, so with `IsAgent==false` the code path is identical to before this change.

  **Findings filed:** none — no pre-existing bug/gap surfaced during this work.
