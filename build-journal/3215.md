# #3215 — Feature: House room-order — user-reorderable room grid (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T05:25:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 5154b53a5 (rebased/pushed to main as bb2e3c045)

## Log
- 2026-09-08T05:00:00Z ⏳ IN FLIGHT — Real "House · Room order" Settings card: per-user
  persisted room order (new `users.room_order` jsonb column, migration
  `049_room_order.sql`), `GET`/`PATCH`/`DELETE /api/room-order`, wired so
  `roomsHouseSection` on Today renders in the user's real saved order (falling back
  to the existing shipped order when unset).
- 2026-09-08T05:25:00Z ✅ DONE — Migration renamed to `050_room_order.sql` (049 was
  taken by a concurrent sibling build's unmerged `049_bill_cycle_snapshots.sql` —
  filed as a real classifier bug, [#3239](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3239),
  parented to #3224). `ALTER TABLE users ADD COLUMN IF NOT EXISTS room_order jsonb`
  run directly against the local `finances` DB + self-recorded in `schema_migrations`.
  New `src/core/room-order.mjs` (get/set/reset, validated as a real permutation of
  the 8 house-grid room keys, self-healing if that key set ever changes), 3 new
  routes on `src/routes/api.mjs`, `/api/today` now returns `roomOrder`,
  `roomsHouseSection()`/`orderedRoomDefs()` in `public/app.js` apply it, new
  Settings card `renderRoomOrderSettings()` (up/down buttons, real per-room
  subtitle, Reset action) rendered first per the design's own sync note. Verified
  live: booted the real server against the real local `finances` DB, minted a real
  session for shanemccaw@gmail.com, exercised GET (default order)/PATCH (persists,
  rejects a non-permutation with 400)/DELETE (resets)/401 unauthenticated, and
  confirmed `/api/today` reflects a saved order end to end. Test session + its
  temporary custom order were cleaned up afterward (session row deleted, order
  reset to default) — no scratch data left behind. Rebased onto and pushed
  directly to `origin/main` (`bb2e3c045`); `verify-branch-merged.mjs` confirms OK.
  `git status --porcelain` clean.
