# #4252 — Status report publish has no customer notification

- **Status:** ✅ DONE 2026-09-15T14:45:44Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 4460391ab (core change), 870efdca7 (manifest hash/notes fix) — pushed as 54ec82064 / 55f30dff4 on origin/main

## Log
- 2026-09-15T14:31:44Z ⏳ IN FLIGHT — Investigating existing `customer_notification_preferences` "project" category pattern (#1923), the ladder permission engine (`evaluateAccess()`/`ladderEvaluationInput()` from #1704), and `msp-status-reports.ts:289-322` publish route, per Shane's 2026-09-15 decision comment authorizing rebuild + real setting.
- 2026-09-15T14:45:44Z ✅ DONE — Added `"status_report_published"` to `CATEGORY_STYLES` (notifications.ts), a real toggle on notification-preferences.tsx (CATEGORY_ORDER/CATEGORY_INFO), and wired the fan-out into `POST /msp/status-reports/:id/publish`, reusing the existing `filterByStatusReportViewAccess` (ladder.customer-user, deny wins) already proven on the comments route — permission-filtered from day one. Live-verified against real local Postgres in `msp-status-reports-publish-notification.live-db.test.ts` (new file): a Customer-tier default-opted-in user was notified, a Free-tier user was denied by the RBAC ladder and skipped, and a Customer-tier user with an explicit opt-out row was suppressed by `createNotification`'s own preference gate — all 3 outcomes confirmed by real log lines and a real `notifications` table query, then cleaned up. `tsc --noEmit` clean on both `api-server` and `portal`. Updated+registered test-manifests/portal/notification-preferences.json in place (discover-before-create) with TEST7/UI11-13 tagged `sessionTag: "4252"` — authored but NOT executed via `shaneapp://runTest` this session: `request-restart.mjs`'s merge-back into the shared dev-server checkout failed on a real, pre-existing, unrelated conflict (2 never-pushed local BuildConsole commits in that checkout diverging from `origin/main`), filed as finding #4261 (parented #1202, board status AI Batter Up) — not caused by this build, and the checkout was left clean/untouched. `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/4252-q2926` (55f30dff4) is an ancestor of `origin/main`.
