# #3972 — RBAC: provisionProspectAccount assigns role by real product type, not blanket Customer

- **Status:** ✅ DONE 2026-09-14T02:41:30Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** e05e6784c (implementation), fc020462f (IN FLIGHT bookend)

## Log
- 2026-09-14T02:40:26Z ⏳ IN FLIGHT — implementation complete, running final verification before commit.
- 2026-09-14T02:41:30Z ✅ DONE — `resolveProspectRole(category, hasTenant)` added to
  `direct-tenant-provisioning.ts`; branches Retainer purchases to
  `RetainerNoConsent` (no tenant, skipped consent) or `RetainerConsented`
  (tenant present), leaves monitoring/pack/everything-else on `Customer`
  unchanged. Wired into both real call sites: `purchase-account-flow.ts`'s
  `attachPasswordToAccount` (via `resolveProductCategory`) and `consent.ts`'s
  checkout-session callback (added `category` to its existing `services`
  select). `ensureClientAccount`/`ensureClientMspUser`/`provisionProspectAccount`
  widened to accept the two new roles and to write `mspRole` even with no
  tenant, since `RetainerNoConsent` is the one role `users_role_scope_check`
  (#3971) permits tenant-less — this is what actually fixes the #3950
  constraint violation for a skipped-consent Retainer purchase (a blanket
  `undefined` scope was the bug: it silently defaulted to `mspRole="Free"`
  with `tenant_id=NULL`, which #3971's constraint rejects). Updated the two
  pre-existing test fixtures that raw-inserted a tenant-less default-"Free"
  user (`purchase-account-flow.test.ts`'s "attaches to an EXISTING
  password-less account" and `monitoring-onboarding-scan.test.ts`'s
  `makeUser` helper, both part of #3950's original failure list) to stamp
  `RetainerNoConsent` explicitly. Added direct coverage: a
  `resolveProspectRole` unit-test block (5 cases) plus 3 new
  `attachPasswordToAccount` integration tests asserting the real `mspRole`
  written to the DB for a retainer-with-tenant purchase (RetainerConsented),
  a retainer-with-no-tenant purchase (RetainerNoConsent), and a real
  monitoring purchase (still Customer, provably unaffected).
  Verification: `tsc --noEmit` clean; `purchase-account-flow.test.ts` (39),
  `monitoring-onboarding-scan.test.ts`, `consent.test.ts` (58),
  `free-scan-prospect.test.ts`, `free-scan-return-link.test.ts` — 115 tests
  total, all passing against the real local Postgres. `git status
  --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs`. #3973 (already filed, sub-issue of #3970)
  owns the RetainerNoConsent -> RetainerConsented promotion when a
  no-consent Retainer later actually consents — deliberately out of scope
  here.
