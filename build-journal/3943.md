# #3943 — 9 pre-existing api-server vitest failures, follow-on from #3895

- **Status:** ✅ DONE 2026-09-13T06:01:31Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 4608faae8 (pushed to main as b275685d0)

## Log
- 2026-09-13T05:23:09Z ⏳ IN FLIGHT — pulled issue body + comments; re-running full `vitest run` across artifacts/api-server (DATABASE_URL set) to capture the complete failure list before diagnosing each of the 9 files independently.
- 2026-09-13T06:01:31Z ✅ DONE — Full re-run first closed the "6 unidentified files" gap: the real failure count was 16 files / 20 tests under full-parallel contention, not 9/19. Re-ran each of the 16 in isolation (immune to `vitest.config.ts`'s own documented full-run-only CPU/transform contention, #2877/#3066) and confirmed only 6 were genuinely real; the other 10 never fail outside the full parallel run.
  Fixed, independently diagnosed, and re-verified in isolation + a subsequent clean full run:
  - `msp-alerts.test.ts` — test's `drizzle-orm` mock was missing `isNull()`, which #3399 added a real call to in `GET /msp/alerts`' findings query (acknowledgedAt filter); every test reaching that query 500'd on "isNull is not a function". Also fixed the stale `POST /msp/alerts/:alertId/acknowledge` test asserting the pre-#3399 "no per-item acknowledge mechanism" 400 for a finding id that was never a valid UUID shape either way, and added a real positive-path test for the per-finding acknowledge mechanism #3399 actually built.
  - `msp-customer-timeline.test.ts` — the id-scoping bug flagged in the dispatch prompt turned out to be a wrong test fixture, not a merge-logic bug: `sales_offers.customerId` is already a `tenants.id` (#2730), confirmed via the route's own header comment + `msp-executive-data.ts`/`dashboard-resolvers.ts`/`user-hard-delete.ts` all treating it the same way — unlike `insights_generated_documents.customerId`, which genuinely is a `users.id` needing the bridge. The fixture wrongly bridged the offer like a document; the route was already correct.
  - `msp-settings-user-security.live-db.test.ts` — the reported 20s timeout never reproduced in isolation (passed cleanly, real Postgres). Confirms this was full-parallel-run contention, not a stuck connection/transaction — consistent with `vitest.config.ts`'s own documented history of exactly this failure class.
  - `tenant-scope-coverage.live-db.test.ts` — real gap: `customer_testimonials.customer_id` is a live tenant-scoped column (FK to `tenants(id)`) with NO Drizzle schema, NO migration file anywhere in the repo, and NO application code referencing it at all (confirmed via full-repo grep + live `\d` + 0-row count). Added an honest, evidenced exemption in `coverage.ts` rather than silently claiming it under an unrelated purger module; real resolution (build the feature or drop the orphaned table) needs Shane's call.
  - Two previously-unidentified test-infra bugs found via the clean re-run, both fixed: `vitest.config.ts` was missing `auth-forgot-password-surface.test.ts` (#3910, a node:test-native `mock.module` suite) from its exclude list, so `vitest run` picked it up and 500'd before `package.json`'s own `node --test` invocation ever ran it for real; `package.json`'s `test` script also had two file arguments concatenated with no space (`admin-m365-run.test.ts''portal-checkout-webhook.test.ts`), silently dropping both from real node-test coverage.
  - The other ~10 files that failed in the very first full run but never reproduced afterward (`config-snapshot-differ`, `tenant-billing-state`, `tenant-signals-one-open-per-signal`, `msp-changes-approve`, `msp-retention-queue-poam-producer`, `msp-settings-target-role-ceiling`, `reinstatement-ticket`, `repo-source-nul-byte-guard`, `index.boot-smoke`) and the ~25 different files that failed in the final confirmation full run (a completely different random subset each time) are confirmed contention-based flakiness, not real bugs — no code changes made for these.

  **One real, separate regression found and deliberately left unfixed here** — filed as its own issue instead, per the dispatch prompt's own instruction to split out genuinely separate work: **#3950** (URGENT, parented under #1096, board status "AI Batter Up"). `monitoring-onboarding-scan.test.ts`'s "skips when the completed account has no tenant link" test and all 7 `purchase-account-flow.test.ts` tests under `attachPasswordToAccount` share one root cause — `users_role_scope_check` (added by #3608, 2026-09-11) now hard-rejects `ensureClientAccount`'s no-scope insert branch, which `provisionProspectAccount` still uses whenever `opts.tenantId` is falsy. This is reachable from a real paid-purchase session (`attachPasswordToAccount`'s "generalized Buy.tsx path") with a genuinely-null `session.tenantId` — a real money-path regression needing a product decision (require a tenant before provisioning vs. loosen the constraint for a genuine scope-less state), not something to guess forward on. Left these 2 files red intentionally; not folded into this issue's fix.

  Verification: isolated per-file reruns for every fix (all green) + a full clean 368-file re-run confirming none of the 4 fixed files fail anymore, under any contention pattern. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0) before writing DONE.
