# #4181 — TYPO/COLOR AUDIT: Build Map canvas controls ignore Typography.xaml/theme brushes

- **Status:** ✅ DONE 2026-09-15T04:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 8e5d57fe1 (IN FLIGHT bookend), 10c7dfa36 (fix)

## Log
- 2026-09-15T02:10:00Z ⏳ IN FLIGHT — mapping raw FontSize/FontFamily literals in LocalMapWindow.xaml,
  Controls/BuildMap/LocalMapCanvasControl.xaml, Controls/BuildMap/ChainCanvasControl.xaml to
  Themes/Typography.xaml resources. Raw hex colors (28 across the 3 files) have no matching
  brush in Colors.xaml/DarkTheme.xaml (confirmed by grep) — filing as its own finding per the
  issue's own instruction rather than force-fitting an inexact substitute.
- 2026-09-15T04:05:00Z ✅ DONE — LocalMapWindow.xaml's 10 raw literals (7 FontSize, 3 FontFamily)
  mapped to Typography.xaml resources by role: overline label/secondary text/zoom%/status strip
  → CaptionFontSize; DatabaseNameText (window title) → SubHeaderFontSize; canvas placeholder
  message → BodyFontSize; window-level and the two Consolas usages → DefaultFontFamily/
  MonospaceFontFamily. Verified all 10 literal attributes are gone via grep. Controls/BuildMap/
  LocalMapCanvasControl.xaml and ChainCanvasControl.xaml had zero FontSize/FontFamily literals to
  fix (only the 2 hex colors each named in the issue's audit table). `dotnet build
  BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. The 28 raw hex color
  literals across all 3 files were deliberately left untouched (no matching brush exists in
  Colors.xaml/DarkTheme.xaml) and filed as finding #4225, parented to #1202, board status "AI
  Batter Up". Branch verified merged into origin/main via verify-branch-merged.mjs before this
  bookend. git status --porcelain clean.
