# #1429 — Security: legacy-auth page — wire real identity.legacyAuthCount metric, replace fabricated evidence rows with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 82fe76aeecce0516ab8171d6531225cf1c48e4d1

## Log
- 2026-08-27 ⏳ IN FLIGHT — Follow-up from #1414's audit. Wiring `/portal-v2/security/legacy-auth`'s "Legacy sign-ins" stat card to the real, already-proven-live `identity.legacyAuthCount` metric (same `/api/dashboard/resolve` pattern as `useSecEvidenceOauthLive.ts` / `useSecAreaLinksLive.ts`), and replacing the entirely-fabricated evidence-row list (fictional accounts, invented sign-in counts — no per-account producer exists) with an honest `NoScanDataState` empty state instead of fixture-as-real content.
- 2026-08-27 ✅ DONE — Shipped in `82fe76aee`: new `useSecEvidenceLegacyAuthLive.ts` hook resolves `identity.legacyAuthCount` via the existing `POST /api/dashboard/resolve`; new `securityLegacyAuthPageWithLive()` in `secEvidenceData.ts` overlays only the "Legacy sign-ins" stat card (the one card that semantically matches the metric's real `legacyAuthSignInCount` field, confirmed against `docs/signals.json`) and unconditionally clears the fabricated evidence-row list; `portal-v2-security-evidence.tsx` renders the shared `NoScanDataState` component in its place. Resolved a real rebase conflict against concurrently-landed sibling work (#1428/#1430/#1431, same #1414 audit family) touching the same shared files — merged both sides' logic, and unified the email page's (#1430) ad-hoc empty-state div onto the same `NoScanDataState` component for consistency, updating its own test assertion to match. Verification: `tsc --noEmit` clean on all touched files (2 pre-existing unrelated errors elsewhere, untouched by this change); manifest JSON valid; new apiTest (`LEGACY-AUTH-RESOLVE`) and uiSteps (`EV-LEGACY-STATS-LIVE`, `EV-LEGACY-ROWS-EMPTY`) added to `test-manifests/portal/security-drilldowns.json` with `sessionTag: "1429"`; confirmed directly against the real local Postgres that `identity:legacy-auth-usage`/`legacyAuthSignInCount` is a real, correctly-shaped, already-collecting check (non-testbed tenant has real values 31/35; the actual is_testbed tenant has never run it, so the page will honestly render its fixture fallback there today). **Honest gap:** `shaneapp://runTest` ran the full manifest (38/43 passed, the 5 failures are pre-existing unrelated `fixture`-vs-`live` markers this file's own #1204/#1232/#1233 notes already track) but against the currently-deployed dev-server build, which predates this commit — the new steps didn't exist there yet and were not exercised. The automated dev-server merge-back (`request-restart.mjs`) hit a real content conflict against the dev-server checkout's current HEAD (the same concurrent #1428/#1430/#1431 sessions) and auto-aborted cleanly per its own conflict-safety design; this commit is on `origin/main` but not yet running on the local dev server. Flagged in the manifest's own notes for whoever next gets a clean merge to confirm live.
