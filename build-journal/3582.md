# #3582 — Multi-repo: merge Batter Up / AI Batter Up across every configured repo into one shared view

- **Status:** ✅ DONE 2026-09-11T04:16:10Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 0af8d7fdc (real work + #3623 rebase-merge fix)

## Log
- 2026-09-11T00:41:37Z ⏳ IN FLIGHT — Pulled #3582's full issue body + dispatch comment. Re-checking real status of #3579 (repo_owner/repo_name schema dimension) and #3581 (Settings repo registry) before starting, per the dispatch's explicit instruction and this issue's own stated dependency.
- 2026-09-11T00:47:00Z 🛑 BLOCKED — Confirmed neither #3579 nor #3581 has genuinely landed: both still `OPEN`/`in-flight` (not `complete`), no `build-journal/3579.md` or `3581.md` bookend exists on `origin/main`, and a codebase grep finds zero `repo_owner`/`repo_name` columns and no repo-registry/Tinker settings surface anywhere. Every real-fix item in #3582's body depends on both. Left the existing `blocked_by` edges to #3579/#3581 in place (accurate, not stale), swapped issue label `in-flight` → `blocked`, posted an explanatory comment on #3582 naming exactly what's missing. No implementation started — nothing real to build against without guessing #3579's actual schema shape and #3581's actual query surface. Stopping here per the blocked-workflow rule rather than proceeding or fabricating scaffolding.
- 2026-09-11T03:27:00Z ⏳ IN FLIGHT — resumed: re-checked #3579/#3581 again and both are now genuinely `closed`/`complete` with real `✅ DONE` bookends (commits `677dda0a1` and `b87dba650`/`533986cb7`) and real code on `origin/main` (repo_owner/repo_name columns + composite keys on bt_issue_mirror/bt_build_queue/etc; `RepoRegistryEntry`/`BuildConsoleSettings.ConfiguredRepos`/Settings "Repos" tab). Removed the now-stale `blocked_by` edges on #3582, swapped label `blocked` → `in-flight`, resumed real work. Also found #3583 (per-repo pause) had independently landed in parallel (concurrent sibling session under the same MultiRepo build set) — confirmed it did NOT touch the board-item fetch/dedup/blocked-by logic this issue owns, so no duplicate work.
- 2026-09-11T04:16:10Z ✅ DONE — real implementation, commit `0af8d7fdc`:
  - **Board-item fetch merge (real-fix item 1):** `GitHubApiClient.ScanProjectItemsForStatusAsync`'s repo filter relaxed from "must equal this instance's own hardcoded Owner/Repo" to "must be ONE OF the repos in #3581's Settings registry" (`IsConfiguredRepo`) — a GitHub Projects v2 board genuinely can hold issues from multiple repos, this was the actual single-repo bottleneck. `BatterUpBoardIssue`/`AiBatterUpBoardIssue` now carry each item's real `RepoOwner`/`RepoName` straight off GraphQL's `repository.nameWithOwner`. `BatterUpQueueService.GetBatterUpBoardItemsAsync`/`AiBatterUpQueueService.GetAiBatterUpBoardItemsAsync` merge the mirror-backed primary repo with a live scan of every OTHER configured repo — gated so this costs **zero extra GitHub calls today** (`ConfiguredRepos` is seeded with just the one Main entry) and only activates once Shane actually adds a second repo.
  - **Repo badge (item 2):** a real, visible repo tag on each card in `BatterUpPanel`/`AiBatterUpPanel` (primary repo shows no badge — matches today's look exactly; a secondary repo gets one), coexisting with the existing #3336 Epic-grouping headers.
  - **Dedup/blocked_by threading (item 3):** `BuildQueuePostgresClient.FindDedupCandidateAsync`/`QueueBuildAsync` now repo-scoped (new `repoOwner`/`repoName` params, `WHERE`/`INSERT`/`UPDATE` all carry `repo_owner`/`repo_name`) so a same-numbered issue in a second repo can never collide with this repo's dedup/re-queue lookup — **verified live against local Postgres**: inserted two synthetic rows sharing the same `github_number` in different repos, confirmed the OLD unscoped query returned the WRONG row while the new repo-scoped query correctly isolates each; cleaned up (0 leftover). The #1600 launch gate (`EvaluateCandidatesAsync`, post-#3623-rebase name) now resolves a live open-issue snapshot **per repo** represented among queued candidates instead of one snapshot assumed to be the primary repo — a real correctness gap this issue's own "confirm dispatch/dedup/blocked_by behave correctly for an item from the non-Main repo" verification called for. `GitHubIssuesService.TryGetOpenIssueNumbersAsync` gained an optional `ownerRepo` override.
  - **Unreachable-repo handling (item 4):** every secondary-repo scan/resolve (board scan, BUILD-comment resolution) is wrapped in try/catch — a failure is logged with the real reason and that repo's items are simply omitted that pass; the primary repo's list/behavior is never affected.
  - **Real merge conflict during rebase onto `origin/main`, honestly recorded:** a concurrent session's #3623 fix (claim-time live `blocked_by` edge re-check) landed on `main` while this was in flight and heavily restructured the exact same method (split `SelectClaimCandidatesAsync` into `SelectClaimCandidatesAsync` + a new public `EvaluateCandidatesAsync`, added a `MissingLiveBlockers` field to `CandidateSelection`). Git's line-based rebase auto-merged both changes with **no conflict markers**, but the result did not compile: #3623's new code (`liveEdgeCheck`, the call into `ApplyLiveBlockerEdgesAsync`) referenced the single outer `live` variable this issue's own per-repo refactor had removed. Caught immediately by a build (not assumed clean from a silent auto-merge) and fixed by pointing both references at `primaryLive` (this issue's per-repo-scoped primary-repo snapshot) — semantically correct, since `ApplyLiveBlockerEdgesAsync` is itself explicitly scoped to only the primary/configured repo by its own doc comment. Re-verified clean build after the fix, both before and after `git rebase --continue`.
  - **Verification (real):** `dotnet build BuildConsole.csproj` — clean, 0 errors, both before AND after the #3623 rebase-merge (three separate clean builds across the session). Live SQL round-trip against local Postgres proving the dedup repo-scoping fix (see above). Every secondary-repo code path confirmed gated behind an actual second configured repo existing, so today's single-repo behavior (`ConfiguredRepos` seeded with just the one Main entry) is structurally unchanged — could not be live-exercised end-to-end against a real second GitHub repo/board item (none configured), honestly noted rather than claimed.
  - **Finding filed:** **#3632** — `GitHubIssueMirror` stays single-repo-scoped (every read/write hardcoded to `RepoIdentity.DefaultOwner`/`DefaultName`), so secondary-repo item discovery costs a full live board walk instead of a mirror read, closed-sweep never covers a secondary repo, and the #2225 DONE-bookend liveness check never applies to a secondary repo's own blockers. All three are real, honestly-documented, currently-mitigated (not correctness bugs) limitations explicitly out of this issue's own scope. Parented as a direct sibling sub-issue of #3578 (Feature: Multi-Repo Support, this issue's own Feature-tier parent), labeled `bug`, milestone v1.1, board status set to "AI Batter Up" per the standing rule.
  - `git status --porcelain` clean; branch confirmed merged into `origin/main` (`git merge-base --is-ancestor HEAD origin/main` — OK, and `verify-branch-merged.mjs` — OK).
