# #3669 — Change Control contract pack missing msp-evidence-attachments.ts router

- **Status:** ✅ DONE 2026-09-11T18:34:35Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 6dd103d0f (contract-pack update); 0d9d0f04e (IN FLIGHT bookend)

## Log
- 2026-09-11T18:32:54Z ⏳ IN FLIGHT — read msp-evidence-attachments.ts, its mount in
  routes/index.ts, evidence-attachments-store.ts's WireEvidenceAttachment shape, and
  the dc.html cc.exec note; about to extend
  docs/msp-console/change-control-msp-console-contract-pack.md §1 inventory and §7
  route table to cover the two change-control-scoped evidence routes
  (`POST`/`GET /msp/change-control/executions/:id/evidence`) plus the shared
  file-serving route.
- 2026-09-11T18:34:35Z ✅ DONE — Extended
  `docs/msp-console/change-control-msp-console-contract-pack.md`:
  - §1: noted the ninth, shared router `msp-evidence-attachments.ts` (`#3503`),
    explained why it's not counted among "the eight" (3 of its 5 routes belong to
    Remediation Tracker, out of this pack's scope).
  - New §7a: full route table for the two change-control-scoped evidence routes
    plus the shared `GET /msp/evidence-attachments/:id/file` file-serving route,
    each cited to file:line (`msp-evidence-attachments.ts`,
    `evidence-attachments-store.ts`), with the `WireEvidenceAttachment` shape
    verbatim and the same bare-`{error}`-envelope inconsistency noted for the
    other three routers in §1.
  - Appendix source index: added the evidence-attachment files under a new row.
  All citations verified directly against the real router/store source (line
  numbers checked with grep, not assumed). Commit `6dd103d0f`, pushed and merged
  into `origin/main` (verified via `verify-branch-merged.mjs`). `git status
  --porcelain` clean. No fixture/hardcoded data — doc-only change describing real,
  already-mounted routes; no code/schema change was needed since the router
  already existed and was already wired.
