# Build Journal — #3331

- **Status:** ✅ DONE 2026-09-09T00:15:00Z
- **Scope:** platform

## Log
- 2026-09-09T00:00:00Z — Started. Wiring #3284's cached `last_battery_range`/`last_read_at`
  into the Tesla house-tile fallback subtitle (`web/shanes-life/src/routes/api.mjs`,
  `roomsForToday`), per Shane's real decision on the issue body. No new polling.
- ✅ DONE 2026-09-09T00:15:00Z — Commit `951a45e4e` ("Tesla house tile: show last cached read
  instead of generic fallback (Git #3331)"), merged to `origin/main`. In the Tesla room's
  `else` fallback branch (only), reads `teslaStatus.lastRead` (real `last_battery_range`/
  `last_read_at` from #3284, already returned by `teslaCore.connectionStatus`). If
  `lastRead` is null (never checked), keeps the existing "Nothing needs you" line. If
  present, subtitle is `"{range} mi · read {age}"` using a new server-side `agoShort()`
  helper mirroring `public/app.js`'s own age-formatting convention exactly (minute/hour
  granularity, honest — no cap on staleness). `node --check` passed on the changed file.
  Precondition/scheduled-trunk/shortfall branches untouched. `verify-branch-merged.mjs`
  confirmed the ancestor check; `git status --porcelain` clean.
