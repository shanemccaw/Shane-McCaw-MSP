# #4008 — msp-signup tiers: fix aiCreditAllowance*Value key mismatch

- **Status:** ✅ DONE 2026-09-14T13:15:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** be4628dfe

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — msp-signup.ts:96-98 reads `attrs.aiCreditAllowancePlatformValue`/`attrs.aiCreditAllowanceMspValue`, but DB `type_attributes` stores `aiCreditAllowancePlatform`/`aiCreditAllowanceMsp` (no `Value` suffix), confirmed via direct psql query against local `shanemccawmsp` (Free 50/0, Growth 150/200, Pro 300/500). Fixing the field reads and live-curling `/api/msp/signup/tiers` to confirm non-null values.
- 2026-09-14T13:15:00Z ✅ DONE — Fixed `msp-signup.ts:96-98` to read `attrs.aiCreditAllowancePlatform`/`attrs.aiCreditAllowanceMsp` (commit be4628dfe, merged to origin/main). Correctness verified two ways: (1) direct psql query against local `shanemccawmsp.services.type_attributes` confirming the real key names and values (Free 50/0, Growth 150/200, Pro 300/500); (2) source review of the fixed line against those confirmed keys. Live-curl re-verification of `GET /api/msp/signup/tiers` was attempted via `request-restart.mjs` but blocked by a real, unrelated dev-server infra bug: the coordinator merged/rebuilt into `C:\dev-server` and reported success there, while the actual running api-server process (port 8080) is served from a *different*, diverged checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`, which refuses `origin/main` ff-pull due to local-only commits ahead) — so the live process's `dist/index.mjs` never picked up the fix. Filed as #4033 (parented under #1202) with full evidence rather than continuing to fight it — not a defect in this fix, and not something this session should hand-patch in the shared main checkout. Working tree clean; branch confirmed merged into origin/main via `verify-branch-merged.mjs`.
