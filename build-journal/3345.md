# #3345 — Ownership/RACI: wire the real tier-gated state

- **Status:** ✅ DONE 2026-09-09T20:59:23Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 6d4d3e101 (rebased onto main as 81218a1b4)

## Log
- 2026-09-09T20:55:53Z ⏳ IN FLIGHT — Design/portal/design_handoff_full_site/screens/Ownership RACI.dc.html was updated after ownership.tsx (#3040/#3041) landed, adding a `tierGated` banner state matching `requireTierFeature("ownership")` (#1168) on both real READ routes. `ownership.tsx` already had a `tierBlocked` branch (from #3040/#3041) but it was a minimal placeholder, not the real Design copy/layout — replacing it with the exact Design export copy (three-column "WHAT IS GATED / WHAT IS NOT / UNRELATED" breakdown, "See your plan" link to `/billing`, "Tier changes are made with your MSP" note), matching `tierGated` in the .dc.html at lines 96-118.
- 2026-09-09T20:59:23Z ✅ DONE — Replaced the placeholder `isError && tierBlocked` banner in `artifacts/portal/src/pages/ownership.tsx` with the real Design export copy/layout (headline, body, WHAT IS GATED/WHAT IS NOT/UNRELATED grid, `/billing` link via wouter `Link`, MSP note), added `data-testid="ownership-tier-gated"` / `"ownership-tier-gated-billing-link"`. `pnpm exec tsc --noEmit` in `artifacts/portal` is clean. Commit `6d4d3e101` rebased onto `origin/main` as `81218a1b4` and pushed; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
  - Ran `test-manifests/portal/ownership.json` via `shaneapp://runTest` for verification: 5/7 steps pass (login + the four API assertions this build didn't touch, all against real live tenant-1 data). 2 pre-existing failures unrelated to this change: (1) manifest step A3's `$.sources[?(@.type=='control')].live` jsonPath filter doesn't resolve against the real (present, confirmed-correct-shape) response — filed as **#3348**, parented under Feature #1491, board status "AI Batter Up"; (2) the UI nav step fails because the portal Vite dev server isn't up on :5175 — this build is part of `--buildSet Feature1491`, which defers the dev-server restart until the whole set completes, so this is expected transient infra state, not a code defect. Neither failure involves the banner this issue actually changed; the `tierGated` state itself isn't asserted by this manifest (documented in its own notes — depends on the real test account's real plan entitlement, which isn't controllable here).
  - No new column/table/endpoint was needed — `tierBlocked` (from the `TIER_UPGRADE_REQUIRED` error code) already existed in the page's data layer from #3040/#3041; this was purely a copy/layout fidelity fix to the Design export.
