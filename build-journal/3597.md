# #3597 — bt_issue_mirror_sync_state: real per-repo PK, drop id/CHECK singleton

- **Status:** ✅ DONE 2026-09-12T03:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** dfcc3004d (rebased onto main as a9e48f4d1)

## Log
- 2026-09-12T03:07:07Z ⏳ IN FLIGHT — pulled #3597 body + comment; confirmed real current
  schema of `bt_issue_mirror_sync_state` against local `BUILD_DATABASE_URL`. Plan: additive
  migration (add `repo_owner`/`repo_name`, backfill the existing `id=1` row with
  `RepoIdentity.DefaultOwner`/`DefaultName`, swap PK to `(repo_owner, repo_name)`, drop
  `id` + its singleton CHECK), then update `GitHubIssueMirror.cs`'s sync-state
  readers/writers (`GetSyncStateAsync`, `GetIncrementalSyncStateAsync`,
  `GetClosedBackfillAtAsync`, `HasClosedBackfillAsync`, `GetClosedBackfillStateAsync`,
  `MarkClosedBackfillStartedAsync`, `RecordClosedBackfillProgressAsync`,
  `RecordClosedBackfillAsync`, `RecordSyncStateAsync`, `RecordIncrementalSyncStateAsync`)
  to key on `(repo_owner, repo_name)` via explicit `RepoIdentity.DefaultOwner`/`DefaultName`
  parameters (same pattern the rest of the file already uses for `bt_issue_mirror`), not an
  implicit `id = 1`.
- 2026-09-12T03:35:00Z ✅ DONE — migration
  `lib/db/migrations/manual/2026-09-12-bt-issue-mirror-sync-state-per-repo-pk-3597.sql` written
  and run myself against local `BUILD_DATABASE_URL`. Confirmed: PK is now
  `(repo_owner, repo_name)`, `id`/its CHECK are gone, and the Main repo's real row
  (`shanemccaw`/`Shane-McCaw-MSP`) round-tripped with its real `last_full_sync_at`
  (2026-09-11 22:56:37-04, "ok: 501 open, 3738 board statuses, 10 blocked_by, 0 closed,
  45940ms"), `last_incremental_sync_at`, and `closed_backfill_complete=true` all unchanged —
  only the key changed. `GitHubIssueMirror.cs`'s 10 sync-state read/write call sites
  (`GetSyncStateAsync`, `GetIncrementalSyncStateAsync`, `GetClosedBackfillAtAsync`,
  `HasClosedBackfillAsync`, `GetClosedBackfillStateAsync`, `MarkClosedBackfillStartedAsync`,
  `RecordClosedBackfillProgressAsync`, `RecordClosedBackfillAsync`, `RecordSyncStateAsync`,
  `RecordIncrementalSyncStateAsync`) now key on `(repo_owner, repo_name)` via explicit
  `RepoIdentity.DefaultOwner`/`DefaultName` parameters, matching the existing `bt_issue_mirror`
  pattern in the same file. `dotnet build BuildConsole.csproj` succeeded, 0 errors (10
  pre-existing unrelated warnings). Live-inserted then deleted a second repo's row
  (`shanemccaw`/`shanes-git-mcp-server`) to confirm a genuine second repo's sync state would
  coexist with zero collision against the Main repo's row — no second repo is registered yet,
  so this is schema/query-shape verification, not a real second-repo sync (per the issue's own
  verification scope). No fixture/hardcoded data. Committed as `dfcc3004d` on
  `agent/3597-q2395`, rebased onto `origin/main` and pushed directly as `a9e48f4d1` (per
  CLAUDE.md's "commit directly to main" convention). `git merge-base --is-ancestor HEAD
  origin/main` confirms genuinely merged. `git status --porcelain` clean.
