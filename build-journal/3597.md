# #3597 — bt_issue_mirror_sync_state: real per-repo PK, drop id/CHECK singleton

- **Status:** ⏳ IN FLIGHT 2026-09-12T03:07:07Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** _(fill at DONE)_

## Log
- 2026-09-12T03:07:07Z ⏳ IN FLIGHT — pulled #3597 body + comment; confirmed real current
  schema of `bt_issue_mirror_sync_state` against local `BUILD_DATABASE_URL`. Plan: additive
  migration (add `repo_owner`/`repo_name`, backfill the existing `id=1` row with
  `RepoIdentity.DefaultOwner`/`DefaultName`, swap PK to `(repo_owner, repo_name)`, drop
  `id` + its singleton CHECK), then update `GitHubIssueMirror.cs`'s sync-state
  readers/writers (`GetSyncStateAsync`, `GetIncrementalSyncStateAsync`,
  `GetClosedBackfillAtAsync`, `HasClosedBackfillAsync`, `GetClosedBackfillStateAsync`,
  `MarkClosedBackfillStartedAsync`, `RecordClosedBackfillProgressAsync`,
  `RecordClosedBackfillAsync`, `RecordSyncStateAsync`, `RecordIncrementalSyncStateAsync`)
  to key on `(repo_owner, repo_name)` via explicit `RepoIdentity.DefaultOwner`/`DefaultName`
  parameters (same pattern the rest of the file already uses for `bt_issue_mirror`), not an
  implicit `id = 1`.
