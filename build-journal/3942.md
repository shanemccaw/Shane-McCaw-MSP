# #3942 — MSP Console AuthGate 404s: GET /api/msp/auth/me never implemented

- **Status:** ⏳ IN FLIGHT 2026-09-13T05:22:25Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-13T05:22:25Z ⏳ IN FLIGHT — Implementing `GET /api/msp/auth/me` per `MspUserProfile` schema in `lib/api-spec/msp-openapi.yaml`. Route was documented (`operationId: mspAuthMe`) but never implemented server-side, so every MSP Console session 404s at `AuthGate` and no operator can reach `ConsoleShell`. Will re-derive the profile from JWT/DB (via existing `getMspClaims` in `auth.ts`), never trust the token payload, then verify live with a real operator JWT (curl 200 + real shape) and a real console login (AuthGate mounts ConsoleShell).
