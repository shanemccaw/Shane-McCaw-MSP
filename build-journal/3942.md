# #3942 — MSP Console AuthGate 404s: GET /api/msp/auth/me never implemented

- **Status:** ✅ DONE 2026-09-13T05:48:53Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 045923aae (route impl), b822098b8 (IN FLIGHT bookend)

## Log
- 2026-09-13T05:22:25Z ⏳ IN FLIGHT — Implementing `GET /api/msp/auth/me` per `MspUserProfile` schema in `lib/api-spec/msp-openapi.yaml`. Route was documented (`operationId: mspAuthMe`) but never implemented server-side, so every MSP Console session 404s at `AuthGate` and no operator can reach `ConsoleShell`.
- 2026-09-13T05:48:53Z ✅ DONE — Implemented `router.get("/msp/auth/me", requireAuth, …)` in `artifacts/api-server/src/routes/auth.ts` (commit `045923aae`). Returns the `MspUserProfile` shape `{id, email, name, role, mspRole, mspId, mspSlug, customerId}`, re-derived from the DB via the existing `getMspClaims(userId)` (reads the user's own `users` row + resolves `mspSlug` from `msps`) — never trusted from the JWT payload. 401 on missing/invalid token via `requireAuth`; 401 for a valid JWT whose user row no longer exists. Placed in `auth.ts` (mounted at `/api`, → `/api/msp/auth/me`) to reuse the exact claim-derivation `POST /auth/login` uses.

  **Live verification (real, not typecheck-only):**
  - Created synthetic operator `zz-test-authme-3942@mccawsoft2.onmicrosoft.com` (id 2916, MSPOperator, mspId=1), logged in via real `POST /api/auth/login` → real JWT.
  - Built the api-server from this worktree (esbuild) and ran it on :8099: `GET /api/msp/auth/me` + operator Bearer → **HTTP 200** `{id:2916,email,name:null,role:"client",mspRole:"MSPOperator",mspId:1,mspSlug:"shane-mccaw-consulting",customerId:null}`; no-auth → 401; garbage token → 401.
  - The **real running dev api-server (:8080)** — after the merge+rebuild — returns the same **HTTP 200 + shape** for a real operator JWT (was 404 before; reproduced the original bug first).
  - Drove the exact call `AuthGate.useMspAuthMe()` makes **through the real MSP Console origin (:5177 → `/api` proxy → :8080)**: WITH operator Bearer → 200 + operator profile (→ `isOperator()` true → `<ConsoleShell>` mounts); no-auth → 401 (→ `SignInRequired`). Console SPA serves (200). No playwright/puppeteer is installed anywhere in the repo, so pixel-rendering was not possible; every layer AuthGate's pure mount decision depends on was verified live through the real console origin.
  - Cleanup: killed the :8099 scratch instance; deleted the synthetic user + its `msp_refresh_tokens`/`user_sessions` rows (verified 0 remaining). Working tree clean; `dist` is gitignored.

- 2026-09-13T05:48:53Z 📋 Finding filed: **#3949** (parented under Feature #1850, `bug`, board "AI Batter Up") — `artifacts/api-server` `pnpm run typecheck`/`build` fails on a pre-existing TS2352 in `auth-impersonation.test.ts` (drizzle insert-mock cast). Pre-existing, not caused by this change; dev server survives it only because the coordinator's esbuild path skips typecheck.
