# #1957 — Risk decline for a multi-check remediation step only suppresses alerts on the first mapped check

- **Status:** ⏳ IN FLIGHT 2026-09-03T02:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — Investigated. #1957's own premise ("likely resolved by
  #1509's line-items model") is stale: #1509 shipped `risk_instances` (per-object
  found/accepted dates within one risk), a different axis than "one risk decision maps to
  multiple check keys." Not what's needed here. Proceeding with the smallest additive fix:
  a nullable `additional_check_keys text[]` column on `msp_risk_decisions` (existing
  `check_key` untouched, all three writers — msp-rbd.ts, m365-change-router.ts's #1514 path,
  remediation-tracker-risk-decline.ts — unaffected unless they choose to populate it), plus
  widening `customer-tenant-alert-engine.ts`'s single `NOT_ACCEPTED_AS_RISK` suppression
  query (the one choke point all four suppression call sites already route through) to match
  `check_key = X OR X = ANY(additional_check_keys)`. `remediation-tracker-risk-decline.ts`
  then writes the full mapped-key set for the six multi-check steps instead of only the
  first.
