# #1957 — Risk decline for a multi-check remediation step only suppresses alerts on the first mapped check

- **Status:** ✅ DONE 2026-09-03T02:40:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 066b8536430a9516175e908975206460247e7ea6

## Log
- 2026-09-03T02:05:00Z ⏳ IN FLIGHT — Investigated. #1957's own premise ("likely resolved by
  #1509's line-items model") is stale: #1509 shipped `risk_instances` (per-object
  found/accepted dates within one risk), a different axis than "one risk decision maps to
  multiple check keys." Not what's needed here. Proceeding with the smallest additive fix:
  a nullable `additional_check_keys text[]` column on `msp_risk_decisions` (existing
  `check_key` untouched, all three writers — msp-rbd.ts, m365-change-router.ts's #1514 path,
  remediation-tracker-risk-decline.ts — unaffected unless they choose to populate it), plus
  widening `customer-tenant-alert-engine.ts`'s single `NOT_ACCEPTED_AS_RISK` suppression
  query (the one choke point all four suppression call sites already route through) to match
  `check_key = X OR X = ANY(additional_check_keys)`. `remediation-tracker-risk-decline.ts`
  then writes the full mapped-key set for the six multi-check steps instead of only the
  first.
- 2026-09-03T02:40:00Z ✅ DONE — Commit `066b8536` on `main`. Migration
  `lib/db/migrations/manual/2026-09-03-risk-decisions-additional-check-keys-1957.sql` run
  against local Postgres (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS additional_check_keys
  jsonb`, additive, nullable). Verified end-to-end against the real local DB in a rolled-back
  transaction: inserted a risk decision with `check_key = 'identity:ca-policy-count'` and
  `additional_check_keys = '["identity:ca-mfa-coverage"]'`, then ran the widened
  `NOT_ACCEPTED_AS_RISK` suppression predicate — the sibling key correctly reads suppressed,
  an unrelated key correctly does not. Added two new unit tests to
  `remediation-tracker-risk-decline.test.ts` covering the s8 multi-key case and the
  single-key case (additionalCheckKeys stays `undefined`, not `[]`).
  **Could not run the actual vitest/tsc suites** — this repo's shared pnpm store has
  vitest's package content missing (`node_modules/.pnpm/vitest@.../node_modules/vitest`
  is an empty directory, confirmed with `scripts/dev-server/store-doctor.mjs`, which
  flagged 2 poisoned `.bin` shims in the main checkout), the same pre-existing,
  already-filed environment class as open issues #2501/#2502/#2503/#2192 — unrelated to
  this change, not caused by it, not fixed here per the "pnpm install is not a remedy"
  rule. `git status --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing this.
