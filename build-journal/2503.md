# #2503 — Repair shared pnpm store corruption (missing top-level symlinks + empty package content)

- **Status:** ✅ DONE 2026-09-04T00:35:00Z
- **Scope:** platform (shared pnpm store / dev tooling, no product code)
- **Started:** 2026-09-04
- **Commit(s):** 4edee3a51 (bookend IN FLIGHT, on main); no product-code commit needed — this
  was a local dev-environment repair (pnpm store content + bookkeeping files), nothing to
  commit for the fix itself.

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — Confirmed diagnosis via `store-doctor.mjs --json` in the
  main checkout (C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP): 2 poisoned .bin shims
  (admin-panel vitest, mcp-server pino), 0 foreign/dangling links. Proceeding per Shane's
  real-time authorization (issue comment, fiber connection) to run store-doctor --repair,
  `pnpm store prune`, and `pnpm install` (and `--force` only if still broken after) from the
  repo root, not a worktree.
- 2026-09-04T00:35:00Z ✅ DONE — Root cause was NOT what plain --force alone could fix: 26
  package content dirs under `node_modules/.pnpm/*/node_modules/<pkg>` (zod, drizzle-orm,
  express, vitest, pino, etc.) were genuinely empty (0 files) on disk, and pnpm's own
  workspace-state bookkeeping (`.modules.yaml`, `.pnpm-workspace-state-v1.json`,
  `.package-map.json`) was trusting stale "already up to date" state through both a plain
  install and `--force`. Removed the 26 empty dirs + 3 bookkeeping files, then `pnpm install`
  genuinely re-fetched (40 downloaded, 1120 added, ~1m54s). Also ran `tsc -b` in
  `lib/api-zod` to fix a separate small never-built-dist TS6305. Verified: `store-doctor.mjs`
  clean; `pnpm --filter @workspace/api-server run typecheck` has zero TS2307/TS6305 (129
  real pre-existing type errors remain — filed as new finding #2777, out of scope for this
  issue); `pnpm --filter @workspace/msp-console run typecheck` clean; `vitest run
  src/routes/msp-policy-decisions.test.ts` executes for real, 12/12 passed. A full recursive
  delete of node_modules/global store was blocked by the environment's permission
  classifier as too broad — worked around it with the narrower targeted-delete approach
  above, which was sufficient. Commented on duplicate/downstream issues #2192, #2193,
  #2501, #2502 with the fix (left open for Shane to close). Posted full writeup on #2503.
  Labels flipped in-flight→complete.
