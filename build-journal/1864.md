# #1864 — Title bar: replace ActiveDocTitleText with usage readout, shrink search box

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** d76ea45dc

## Log
- 2026-08-30 ⏳ IN FLIGHT — starting: remove ActiveDocTitleText (9 call sites), wire UsageTrackingService readout into title bar, shrink SearchBorder to fit caption height. buildSet=BuildConsole per Shane's queue header.
- 2026-08-30 ✅ DONE — Removed ActiveDocTitleText element and all 9 code-behind assignments
  (MainWindow.xaml.cs x6, MainWindow.ApiRunnerTab.cs, MainWindow.GitDetailTabs.cs,
  MainWindow.SettingsTab.cs). Replaced with a compact usage/cost readout sourced from
  UsageTrackingService.GetSnapshot(), updated live via UsageTrackingService.Changed (no
  polling/timer); session figures are the headline, all-time breakdown in the tooltip,
  dollar figure explicitly labeled an estimate. Extracted FormatTokens into shared
  Services/UsageFormat.cs (used by both the title bar and Controls/BuildQueuePanel.xaml.cs)
  and fixed a real pre-existing bug found while moving it: the million-scale case's "0.1"
  format string is not a one-decimal specifier (literal digit, drops the decimal
  separator) — 4.7B tokens rendered "47351M tokens" instead of "4735.3M tokens"; fixed to
  "0.0". SearchBorder height is now computed in code from the window's real
  WindowChrome.CaptionHeight instead of a guessed pixel value. DebugBuildMarkerText
  unchanged.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean
  (0 errors/warnings), both before and after rebasing onto 4 intervening concurrent
  commits (including #1866's screen-clipping tool, which also touched MainWindow.xaml —
  rebased cleanly, no conflicts). `grep -rn "ActiveDocTitleText" desktop/BuildConsole`
  returns nothing in code (only a stale historical mention in the frozen
  desktop/BuildConsole/BUILD_LOG.md archive). Live-verified against Shane's actual running
  ShanesBuild instance (which auto-restarted mid-session onto this exact commit via the
  dev-server coordinator): title bar shows no "- Claude.ai" label, the usage readout
  renders and updates live in real time (observed 17.3M → 20.8M tokens as queued builds
  completed, confirming the Changed-event wiring), matches
  %AppData%\BuildConsole\usage-totals.json, and nothing in the search box clips at full
  screen width (1920px maximized). Narrow-width (≈820px) could not be cleanly screenshotted
  in isolation — every extra BuildConsole.exe instance I launched to test it got reaped by
  an existing process-management mechanism within seconds to ~15s of restoring from
  Maximized (not an unhandled exception — no matching .NET Runtime crash event — something
  intentionally closes untracked extra instances), and manipulating Shane's own live window
  is out of scope per CLAUDE.md's "Shane controls ShanesBuild deploys" rule. Verified
  instead by layout reasoning: SearchBorder's Height is now fixed via
  SizeSearchBoxToCaptionHeight (not content-driven), Padding is 8,0, and every child
  (glyph, placeholder, TextBox, Ctrl+K hint) is already VerticalAlignment="Center" —
  MinWidth="180" is comfortably wide enough for those elements at FontSize 12/10, so
  nothing can clip regardless of how far the center column itself shrinks between
  MinWidth=180 and MaxWidth=820. Working tree clean at completion aside from
  `shaneapp-executeSql.result.json`, which was already modified when I arrived and which I
  did not touch (not created or edited by this session).
