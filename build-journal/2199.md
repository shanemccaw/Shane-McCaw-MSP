# #2199 — Feature: Tab Workspaces (ShaneBuilder)

- **Status:** ✅ DONE
- **Scope:** desktop/ShaneBuilder (wpf-handoff Step 9)
- **Started:** 2026-09-02
- **Commit:** `56397dc03` "ShaneBuilder: Tab Workspaces (Step 9)" — real ancestor of `origin/main`, verified via `git merge-base --is-ancestor`. No IN FLIGHT bookend exists — Shane ran this as a live Claude Code session directly against his main checkout, same as #2194, not through the Git-as-Build-Queue. This is the DONE side written retroactively, by chat, against the real landed diff.

## Log

- **Real audit performed against the actual landed diff, not the issue body alone.** `git show 56397dc03 --stat`: 4 files changed, 401 insertions — `MainWindow.xaml` (+36), `MainWindow.xaml.cs` (+312, the bulk of the real logic), `Themes/Colors.xaml` (+23, workspace accent colors), `Themes/Icons.xaml` (+41, new Lucide glyphs — layout-grid, star, activity, zap, file-code, house, undo-2 — real SVG geometry per the commit message, not guessed).
  - Real workspace grouping: `TabKind → WorkspaceId` default map with `TabDef.WorkspaceId` override, matching the readme's contract (`WorkspaceDef`, `TabDef` with `WorkspaceId`/`Ext`/`Badge`).
  - Tab strip clusters into rounded, colour-accented capsules — chip header with icon/label/count, fold-to-chip on click (readme's "Collapse" behavior), dismiss to a workspace box (readme's "Dismiss" behavior) replacing the empty square left of the app name.
  - Workspace box: pip grid + popup with Restore.
  - Home tab: pinned, house-icon glyph, closable once anything else is open, resets fresh when the last tab closes — matches the readme's Home spec exactly.
  - Two real bugs fixed along the way, not glossed over: the workspace box button sat in WindowChrome's caption region and needed `WindowChrome.IsHitTestVisibleInChrome` explicitly set (clicks were being read as window-drags); a `StaysOpen=False` Popup toggled from a plain `Button.Click` was flashing open and immediately self-dismissing — fixed by binding a `ToggleButton` straight to the Popup's `IsOpen` instead.

## What's confirmed vs not, honestly

- **Confirmed real:** the diff itself, the workspace/tab-grouping logic, the two real bugs fixed and how.
- **Not independently verified in this pass:** no live click-through was performed by chat (no interactive session available). Verification is the real diff + the commit's own description of what was fixed, not a fresh screenshot-based check like #2194 got from Shane directly.
- **Scope note:** this is Step 9 of the wpf-handoff Phase 2 spec. Only the tab-clustering/workspace-box mechanics landed — the readme's "Routing" behavior (a newly opened document lands in the right workspace and un-stashes/unfolds it) isn't explicitly called out in the commit message; worth confirming it's covered before treating Step 9 as 100% done against the full spec, not just the visible mechanics.

## Why this bookend looks different from the rest

Same reason as #2194: no `IN FLIGHT` entry exists because this never went through `QueueWatcherService`/the Git-as-Build-Queue. Shane ran it as a live Claude Code session directly in his main checkout. This file exists so #2199 has the same real paper trail as everything else, written against the actual diff.
