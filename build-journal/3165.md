# #3165 — App nav is still a flat tab bar, not the design's "Rooms -- the house" grid (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-08T02:18:51Z
- **Scope:** platform (web/shanes-life -- Shane's personal project, Epic #3086)
- **Started:** 2026-09-08
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-08T02:18:51Z ⏳ IN FLIGHT — Filed a bookend after the fact (should have been step one;
  noting the miss honestly rather than backdating it). Built the real "Rooms -- the house" grid
  from `Design/design_handoff_shanes_life/README.md`'s "Rooms -- the house" section: roof +
  chimney SVG, a real 8-cell two-column floor grid (Things|Lists, People|Dates, Recipes|Pets,
  Shopping|Money), a hand-authored furniture symbol per room in `critters-sprite.svg` (`r-things`,
  `r-lists`, `r-people`, `r-dates`, `r-recipes`, `r-pets`, `r-shop`, `r-money`, each with its one
  quiet CSS loop), and a real lit/dark lamp per room driven by a new `rooms` object on
  `GET /api/today` (`roomsForToday()` in `src/routes/api.mjs`) -- real reads off
  things/lists/pets/recipes/dates/money, no fixture. Trimmed `<nav class="tabs">` in index.html
  down to Today/Meds/Inbox/Settings (the 8 rooms the flat bar used to link are now reached through
  the house grid instead, per the issue's own complaint). Reused existing critter slots
  (things/lists/people/pets/shop/moneyhdr, plus comingup for Dates and dinner for Recipes) rather
  than inventing new artwork. People has no real data model yet (Section 7 "People & Patterns" is
  its own separately-scoped Feature, #3157, already in flight under a different session -- its
  migrations 034/035/036 are already applied against the shared local `finances` DB, confirmed
  while investigating an unrelated schema-ledger mismatch) -- built an honest stub `/people` route
  instead of inventing journal data or leaving a dead link.
  Verified: `node --check` on both edited `.mjs`/`.js` files; the sprite's new symbols validated
  as well-formed XML via .NET's `XmlDocument` (comments stripped first -- a pre-existing unrelated
  `--` inside an old comment at line 99 already made the whole file XML-strict-invalid before my
  change, confirmed via `git show HEAD:...`; not something this issue touches). Ran
  `roomsForToday()` directly against the real local `finances` database (disposable throwaway
  user, deleted after) and got real live output: Money genuinely lit ("short $7,933.70", matching
  real account shortfalls), Dates genuinely lit ("Labor Day today", a real federal holiday row),
  Shopping/Recipes/Things/Lists/Pets all real-empty/dark for a fresh user. Could not boot the real
  HTTP server end-to-end: `schema_migrations` in the shared local DB already has rows for
  `034_catches.sql`/`035_people_patterns.sql`/`036_push_subscriptions.sql`, which don't exist in
  this worktree's `migrations/` directory yet (a concurrent session's in-flight, uncommitted-to-
  this-branch work) -- the app's own Git #3140 startup guard correctly refuses to boot until that
  ledger is reconciled. Not this issue's migrations, not touched.
