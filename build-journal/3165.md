# #3165 — App nav is still a flat tab bar, not the design's "Rooms -- the house" grid (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:25:50Z
- **Scope:** platform (web/shanes-life -- Shane's personal project, Epic #3086)
- **Started:** 2026-09-08
- **Commit(s):** fc510b65a

## Log
- 2026-09-08T02:18:51Z ⏳ IN FLIGHT — Filed a bookend after the fact (should have been step one;
  noting the miss honestly rather than backdating it). Built the real "Rooms -- the house" grid
  from `Design/design_handoff_shanes_life/README.md`'s "Rooms -- the house" section: roof +
  chimney SVG, a real 8-cell two-column floor grid (Things|Lists, People|Dates, Recipes|Pets,
  Shopping|Money), a hand-authored furniture symbol per room in `critters-sprite.svg` (`r-things`,
  `r-lists`, `r-people`, `r-dates`, `r-recipes`, `r-pets`, `r-shop`, `r-money`, each with its one
  quiet CSS loop), and a real lit/dark lamp per room driven by a new `rooms` object on
  `GET /api/today` (`roomsForToday()` in `src/routes/api.mjs`) -- real reads off
  things/lists/pets/recipes/dates/money, no fixture. Trimmed `<nav class="tabs">` in index.html
  down to Today/Meds/Inbox/Settings (the 8 rooms the flat bar used to link are now reached through
  the house grid instead, per the issue's own complaint). Reused existing critter slots
  (things/lists/people/pets/shop/moneyhdr, plus comingup for Dates and dinner for Recipes) rather
  than inventing new artwork. At the time this line was written, People had no real data model
  yet (Section 7 "People & Patterns" was in flight under a concurrent session, #3157 -- its
  migrations 034/035/036 were already applied against the shared local `finances` DB, confirmed
  while investigating an unrelated schema-ledger mismatch), so the People cell was built as an
  honest stub. #3157 landed for real before this branch reached `main` (see the DONE line below)
  -- rebasing surfaced its real `viewPeople`/`people.mjs`/`/api/people`, so the stub was removed
  and the People cell was wired to the real thing instead.
  Verified: `node --check` on both edited `.mjs`/`.js` files; the sprite's new symbols validated
  as well-formed XML via .NET's `XmlDocument` (comments stripped first -- a pre-existing unrelated
  `--` inside an old comment at line 99 already made the whole file XML-strict-invalid before my
  change, confirmed via `git show HEAD:...`; not something this issue touches). Ran
  `roomsForToday()` directly against the real local `finances` database (disposable throwaway
  user, deleted after) and got real live output: Money genuinely lit ("short $7,933.70", matching
  real account shortfalls), Dates genuinely lit ("Labor Day today", a real federal holiday row),
  Shopping/Recipes/Things/Lists/Pets all real-empty/dark for a fresh user. Could not boot the real
  HTTP server end-to-end at that point: `schema_migrations` in the shared local DB had rows for
  `034_catches.sql`/`035_people_patterns.sql`/`036_push_subscriptions.sql`, which didn't exist in
  this worktree's `migrations/` directory yet (that concurrent session's in-flight work) -- the
  app's own Git #3140 startup guard correctly refused to boot until that ledger was reconciled.
  Not this issue's migrations, not touched.
- 2026-09-08T02:25:50Z ✅ DONE — Commit fc510b65a on `main` (verified via
  `scripts/dev-server/verify-branch-merged.mjs`: OK, merged). Rebasing onto `main` picked up
  #3157's real People & Patterns landing, plus #3160 (Web Push) and #3184 (Shared list view) --
  all three merged cleanly except a real content conflict in `app.js`/`index.html` where #3157 had
  independently added its own flat-tab-bar `People` link and `viewPeople`/`TITLES`/routing
  entries: resolved by keeping #3157's real `viewPeople`/`viewPersonDetail`/routing (deleting my
  now-redundant stub `viewPeople`), dropping the flat-bar links it added (this issue's whole point
  is that those 8 rooms are no longer flat-bar links), and rewiring `roomsForToday()` + the Rooms
  grid to treat People as a real room: `people.listPeople(userId).length` drives a real
  `"{n} people · your journal"` subtitle (the README's own literal example wording), always dark
  per spec. Re-ran the live `roomsForToday()` verification post-merge with a real person row
  created for the disposable test user -- `"1 people · your journal"` came back real, all 9
  assertions passed, cleaned up after. `git status --porcelain` clean; nothing else left dirty.
  No new GitHub issues filed -- no findings outside this issue's own scope turned up. `desktop/`
  and `artifacts/` untouched, so no test-manifest obligation (this is `web/shanes-life`, Shane's
  personal project, outside the Test Coverage section's stated scope).
