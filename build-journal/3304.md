# #3304 — Feature: Dates gets a real month calendar (net-new, not yet built)

- **Status:** ✅ DONE 2026-09-09T02:35:35Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** 86cd4c005 (implementation), 2c0615b74 (IN FLIGHT bookend)

## Log
- 2026-09-09T02:26:40Z ⏳ IN FLIGHT — read #3304, the README's "a calendar in Dates" pass (Sep 8
  2026 night), and `Shanes Life - First Slice Prototype.dc.html`'s `d.showDates` block + `calMonth`/
  `calDay` logic. Building the month calendar (nav, weekday row, day grid, legend, day-detail card)
  into `viewDates()` in `web/shanes-life/public/app.js`, reusing the real `/api/dates` data already
  wired for the existing grouped list -- no new endpoint or fixture needed.
- 2026-09-09T02:35:35Z ✅ DONE — shipped in 86cd4c005: `‹ Month YYYY ›` nav card, S-S weekday
  letters, 7-col day grid (today ring, per-kind tint + up to 3 dots, past days dimmed .45, blank
  outside-month cells), a legend of only the kinds present that month, paging clamped to one
  month back / twelve ahead with a Today link, and a tap-a-day detail card (reusing the list's
  own `dateRow()` tile/title/line, or the README's exact empty-day copy) between the calendar and
  the unaffected grouped list. `datesCalMonth`/`datesCalDay` are transient client state, same
  idiom as `moneyTab`/`vaultRoomQuery`, not persisted. New CSS scoped under `.dates-cal-*`.
  Verification: `node --check` syntax pass, plus a scratch 21-assertion logic-equivalence test
  (weekday alignment, event bucketing, today/past/selected state, paging clamps, year rollover,
  Feb leap-year day count, exact README empty-day copy) -- all passed, script deleted after (not
  part of the deliverable, this app has no test-manifest requirement per CLAUDE.md's scope). Not
  exercised against a live browser/server: this app's own DB is Shane's real personal
  ShanesSurvival/`finances` Postgres plus real passkey auth, disproportionate to stand up for this
  check. `git status --porcelain` clean; HEAD confirmed a genuine ancestor of `origin/main` via
  `git merge-base --is-ancestor` after push.
