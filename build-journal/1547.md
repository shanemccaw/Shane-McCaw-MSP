# #1547 — Policy Engine: standing policies are a second, declarative object

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 2917f7a2c (work), 6d3358206 (IN FLIGHT bookend) — both on origin/main

## Log
- 2026-08-30 ⏳ IN FLIGHT — Building the Policy Engine's declarative object: a `standing_policies`
  table (OU-bound target state, distinct from the `msp_risk_decisions` register), its manual
  migration, a wire-shaper lib, and an MSP-side read/author route. Ends at the wire contract —
  no portal page (per scope stop). Foundation for #1548–#1553.
- 2026-08-30 ✅ DONE — Shipped as `2917f7a2c` (rebased onto a fast-moving main, merge verified by
  `verify-branch-merged.mjs`, exit 0). What landed:
  - **Schema** `lib/db/src/schema/msp.ts`: `standingPoliciesTable` (`standing_policies`) — a
    SECOND, declarative object: `target_kind` (`mailbox_attribute | group_membership |
    service_policy`) + `target_state` jsonb (the one map read forward for provisioning #1548 and
    backward for compliance #1553), `ou_id` FK to `active_directory_ous` (the #1547 attachment
    point), nullable `catalog_item_id` FK recording the #1550 relationship, `is_active`
    default-off (#1549), created-by stamp. No obligation, no signature, no money columns — that
    is what makes it distinct from `msp_risk_decisions`.
  - **Migration** `lib/db/migrations/manual/2026-08-30-standing-policies-1547.sql` — additive
    `CREATE TABLE IF NOT EXISTS` + 3 indexes, self-marking INSERT into `simulator_migration_runs`.
    **Run against the local `DATABASE_URL` in-session** (table + indexes verified; FK to a bogus
    OU rejected inside a rolled-back txn; 0 rows left behind). Appended to #1630.
  - **Lib** `artifacts/api-server/src/lib/standing-policies.ts` — pure DB-free `WireStandingPolicy`
    shaper + `isStandingPolicyTargetKind` + kind labels.
  - **Route** `artifacts/api-server/src/routes/msp-standing-policies.ts` — `GET`/`POST
    /api/msp/standing-policies`, `MSPOperator` floor, OU + own-MSP catalog-item validation,
    mounted in `routes/index.ts`. Read + author only (no signature lifecycle — a standing policy
    requires none). Ends at the wire contract; no portal page.
  - **Test** `standing-policies.test.ts` (6 assertions, incl. "no money/signature field") —
    registered in `vitest.config.ts` include; **ran green (6/6)** with `DATABASE_URL` set.
  - **Doc-sync**: updated the two OU-scaffolding comments (`schema/index.ts`,
    `lib/active-directory.ts`) that said "policy semantics explicitly undefined / reserved for a
    future version" to point at the now-real attachment point, keeping the still-valid "no policy
    columns on the OU table" directive.
  - **tsc**: `artifacts/api-server` typecheck observed with 0 errors in any of my files (56
    pre-existing errors remain in unrelated files per #1765/#1772/#1774/#1777/#1778/#1781 —
    not mine). Required rebuilding `lib/db` declarations (`tsc -p`, emitDeclarationOnly) so the
    project-reference `.d.ts` saw the new exports — the documented "api-server tsc needs lib/db
    decls" workflow.
  - Findings filed: none — the OU stale-comment was fixed inline (it was made stale by my own
    change); no other in-scope defect surfaced. Note (not a finding): `active_directory_ous` has
    0 rows in the testbed, so a policy can only be authored once an OU is created via the existing
    admin AD route — that is create-on-demand, not a gap.
