# #2180 — Epic #2138: ShaneBuilder — port critter art + registry

- **Status:** ✅ DONE 2026-09-02T00:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 642184c94 (bookend IN FLIGHT), 10d7bf816 (real port)

## Log
- 2026-09-02T00:30:00Z ⏳ IN FLIGHT — audited live BuildConsole `Controls/CritterRegistry.cs`, `Controls/CritterModels.cs`, `Services/IssueChompAnimation.MeanCritters.cs` (10 Negative builders: Kilowatt, Dustdevil, Sir Blocksalot, Grudge, Wrenchfiend, Doomquack, Snaggletooth, Redtape, Bramblebeast, Glitch), and `Controls/BuildQueuePanel.xaml.cs` lines ~4935-5406 (CritterMood enum + shared eye/blush helpers + 10 Positive builders: Panda, Otter, Hedgehog, Owl, Seal, Raccoon, Hamster, Frog, Koala, Chick). Porting into `desktop/ShaneBuilder/` only, namespace `ShaneBuilder` (matching #2174's convention), no wiring into any panel/trigger.
- 2026-09-02T00:55:00Z ✅ DONE — Ported all 20 real vector builders verbatim into `desktop/ShaneBuilder/`: `Controls/CritterModels.cs` (CritterCategory/CritterInfo), `Controls/CritterArt.cs` (CritterMood enum + shared eye/blush helpers + the 10 real Positive builders extracted cleanly from `BuildQueuePanel.xaml.cs` without dragging the panel's own logic), `Services/MeanCritterArt.cs` (the 10 real Negative builders + swing-prop scaffolding from `IssueChompAnimation.MeanCritters.cs`; the PlayBlocked/PlayNewWork/BuildRandomBlockedElement choreography was intentionally NOT ported — it depends on Whammy/MoWork machinery not present in ShaneBuilder and is out of scope), `Controls/CritterRegistry.cs` (combines both pools into one spawnable `CritterInfo` list, same SpawnWeight/Scale convention as the original). Not wired into any panel/trigger — none exist yet in ShaneBuilder, per the issue.
  Verification: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors. Real live smoke test: temporarily wired `CritterRegistry.All` into `MainWindow`'s constructor, ran the real built `.exe` (`Start-Process` + `WaitForExit`, per the ConPTY verification pattern), confirmed all 20 registry entries (`CritterRegistry.All.Count == 20`) build real Canvas trees with non-empty children (13-27 children each, all `Negative` 74x64 and `Positive` 36x30 as expected) and the process self-exits 0 — then reverted the scratch `MainWindow.xaml.cs` edit (`git checkout --`) before this commit, confirmed by rebuild (0 warnings/errors) and `git status --porcelain` showing only the intended new files.
  Branch verified merged into origin/main via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0) — pushed as a fast-forward directly onto `main` (`2d25a1b3e..10d7bf816`). `git status --porcelain` clean at completion.
