# Git #1992 — TerminalView doc comment claims Replit-POST migration but code still spawns local PowerShell

- **Status:** ✅ DONE 2026-09-06T17:28:40Z
- **Scope:** buildconsole

## Log

- 2026-09-06T17:28:40Z — Investigated `desktop/BuildConsole/Controls/TerminalView.xaml.cs`. The
  doc comment cited in the issue (lines 8-20, claiming a migration to `POST
  /admin/simulator/deploy/console` on the Replit dev server) is already gone. Current class doc
  (lines 13-20) correctly states: *"A real, persistent local `powershell.exe` process on Shane's
  own machine ... not a remote/Replit shell and not a per-command HTTP round-trip."* This matches
  the actual code — `StartShell` still spawns `powershell.exe` via `ProcessStartInfo` with
  redirected stdio, and `Initialize()`/constructor behavior is genuinely local, no Replit POST
  call anywhere in the file.
- Confirmed via `git log`: commit `8f4fbd741` — "TerminalView: fix stale Replit doc/copy - shell
  is local, not remote (#2154)" — already corrected this exact doc/code mismatch, landed before
  this #1992 dispatch. Local PowerShell is the real, current, and correct behavior; no code
  change needed here.
- ✅ DONE — No code changes required; the fix already exists on `main` at `8f4fbd741`. Verified
  clean working tree, verified branch is an ancestor of `main` (this session made no new commits
  to the file itself, only this bookend + a completion comment on #1992).
