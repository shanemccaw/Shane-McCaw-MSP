# #3649 — RBAC live-DB tests insert customer-tier/staff users with no tenant or MSP and fail users_role_scope_check in local dev

- **Status:** ✅ DONE 2026-09-12T21:10:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 19d64c9e7

## Log
- 2026-09-12T21:00:00Z ⏳ IN FLIGHT — confirmed both target files fail against local `DATABASE_URL` with `users_role_scope_check` violations (11/11 in `lib/db/src/rbac/user-role-sync.test.ts`, 1 setup failure in `artifacts/api-server/src/routes/msp-settings-approve-purchases-rung.live-db.test.ts`). Fixing `insertUser` helpers to supply real seeded `tenant_id`/`msp_id`.
- 2026-09-12T21:10:00Z ✅ DONE — `lib/db/src/rbac/user-role-sync.test.ts`'s `insertUser` now always sets a real seeded `tenant_id` AND `msp_id`; the bare-insert test adds `tenant_id` explicitly (relies on the `Free` column default); the "msp_role that is not one of the rungs" test drops `users_role_scope_check` for just its own savepoint since the constraint now makes that state genuinely unwritable (a different, already-covered concern) and the DDL rolls back with the savepoint. `artifacts/api-server/.../msp-settings-approve-purchases-rung.live-db.test.ts` now creates a real seeded tenant scoped to its test MSP and sets `tenantId` on the Customer/Free rows, cleaned up in `afterAll`. Verified: `pnpm --filter @workspace/db exec vitest run src/rbac/user-role-sync.test.ts` → 12/12 pass; `pnpm --filter @workspace/api-server exec vitest run msp-settings-approve-purchases-rung.live-db` → 5/5 pass. Confirmed zero leftover synthetic rows in local Postgres after both runs. Commit 19d64c9e7, pushed to origin/main, branch verified merged (`verify-branch-merged.mjs`). Working tree clean. Filed #3839 (parented under Feature #1696, board status AI Batter Up) for an unrelated pre-existing failure discovered running the full `@workspace/db` suite: `customer-admin-billing-roles.test.ts` (#3629) fails 5/8 because local dev's role catalogs carry both pre-#3590 and post-#3590 role keys simultaneously — out of this issue's scope, not attempted here.
