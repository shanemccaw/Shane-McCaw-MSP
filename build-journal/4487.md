# #4487 — Portal add-on purchase UI (Change Control stub build)

- **Status:** ✅ DONE 2026-09-17T05:54:46Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** e6a7fec93 (feature), 3befece82 (return-banner fix)

## Log
- 2026-09-17T05:14:33Z ⏳ IN FLIGHT — Applying Shane's stub-over-nothing authorization (issue comment 2026-09-17T04:50:08Z): wiring a real purchase control into `artifacts/portal/src/pages/change-control.tsx`'s `change-control-addon-required` state against the already-shipped `/api/portal/add-ons*` endpoints (#4462). Bracket picker + Stripe Checkout redirect + success/cancelled return-state handling, reusing the Buy.tsx purchase-flow pattern adapted to this page's existing shadcn/Tailwind conventions. No Design export needed per the authorization.
- 2026-09-17T05:54:46Z ✅ DONE — Added `artifacts/portal/src/lib/portal-add-ons-api.ts` (React Query hooks for GET add-ons / POST checkout-session / POST checkout-confirmed), `components/change-control/AddOnPurchaseCard.tsx` (bracket picker + Stripe redirect, shown only with `customer:billing.manage`, existing "ask your MSP" text kept for everyone else), and `components/change-control/AddOnCheckoutReturnBanner.tsx` (confirming/confirmed/failed/cancelled return states). Wired into `change-control.tsx`. Live-verified end-to-end against real Stripe test mode: created a synthetic Customer-Admin test user on the pre-existing `zz-test-4488` non-Premier tenant (2191), drove a real Stripe Checkout session for change-control-micro ($99/mo) via Playwright with the real `4242...` test card, confirmed the real `tenant_add_on_entitlements` row + real Stripe test subscription were provisioned, confirmed `GET /api/portal/change-control` flipped from 402 to 200, and confirmed the full Change Control page unlocked live. Caught and fixed a real bug during verification: `AddOnCheckoutReturnBanner` was re-deriving its outcome from the URL on every render, so stripping the query string (to prevent replay-on-refresh) also erased the confirmed/failed message ~1s after it appeared — fixed by capturing the outcome once in state (commit 3befece82). Cleaned up verification side effects: cancelled the real test-mode Stripe subscription and deleted the entitlement row, restoring tenant 2191 to its original non-entitled state for future add-on-gate tests. Filed a genuine finding hit during verification — #4506 (parented under Feature #1486): `PORTAL_BASE_URL=http://localhost:3000` in `.env.local` doesn't match the portal's actual dynamic dev port, so every Stripe hosted-Checkout redirect (this build's, plus the pre-existing `portal-billing.ts` ones) lands on connection-refused locally — a pre-existing local-dev env gap, not introduced by this build, worked around during verification by manually re-driving the real return URL with the captured session id. `pnpm exec tsc --noEmit` clean on `artifacts/portal`. Branch verified merged into `origin/main` before writing DONE; working tree clean.
