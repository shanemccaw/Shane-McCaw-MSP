# #1502 — Post-Implementation Review: close codes, PIR record, drift re-scan

- **Status:** ✅ DONE 2026-09-03T15:09:31Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** fa6ab4941 (real work), 87cbd8310 (merge with origin/main — verified ancestor of main)

## Log
- 2026-09-03T14:55:37Z ⏳ IN FLIGHT — read #1502 body + comments (blocked by #1499, now unblocked — #1499 landed `cr_executions`). Investigated `cr_executions`, `cr_events`/`recordCrEvent`, `monitor-executor.ts`'s CA-only drift attribution (30-day window), and the existing MSP-operator execution route (`msp-change-executions.ts`) as the pattern to follow. Adding `cr_pirs` schema + manual migration next, then the store/route/wire contract. Scope stop: schema, migration, routes, Wire* interfaces only — no UI, per the issue.
- 2026-09-03T15:09:31Z ✅ DONE — Shipped `cr_pirs` (close_code enum: successful | successful_with_issues | failed | rolled_back; required summary; reviewer identity; drift-rescan result columns), real FK + UNIQUE on `execution_id` (one PIR per `cr_executions` row, append-only like `cr_events`), and `pir_recorded` added to `CR_EVENT_TYPES`. Migration `lib/db/migrations/manual/2026-09-03-cr-pirs-1502.sql` — additive, ran against local `DATABASE_URL` in-session (also logged on #1630). `msp-change-pir.ts` (pure derivations + WireCrPir), `msp-change-pir-store.ts` (recordPir/getPirForExecution/listPirsForChange/listPirsForMsp + the real drift re-scan via `monitor-executor.ts`'s `executeMonitorCheck`, Conditional-Access-only per #1497's boundary — every other category is honestly `not_applicable`), `routes/msp-change-pir.ts` (POST/GET under `/api/msp/change-control/executions/:id/pir` + list), wired into `routes/index.ts`. Scope stop honored — no UI, artifacts/portal has no pages and Design/portal/ has no export.
  Verification: real `psql` against local DB proved the FK cascade (deleting a `cr_executions` row cascades its PIR), the unique constraint (a second PIR against an already-reviewed execution is rejected), and `cr_events` accepting `pir_recorded` with no CHECK-constraint DDL needed. A vitest test file (`msp-change-pir-store.test.ts`, registered in `vitest.config.ts`) was written but could NOT be executed — the shared pnpm store is missing extracted content for `drizzle-orm`, `zod`, and `vitest` repo-wide (confirmed empty package dirs in the MAIN checkout, not a worktree-isolation issue), already filed by several other sessions today (#2502, #2503, #2515, #2522, #2526, #2528, #2192, #2193) — not filed again here. `pnpm --filter @workspace/db exec tsc --noEmit` shows only the same pre-existing `TS7006` implicit-any errors present without my change (confirmed via a temporary stash-and-diff of the count: 209 before, 209 after) — none of them are mine. Two new small files were syntax-checked clean with `node --experimental-strip-types --check`.
  `git status --porcelain` clean; branch verified an ancestor of `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` after merging a concurrent unrelated commit (#2514) cleanly.
