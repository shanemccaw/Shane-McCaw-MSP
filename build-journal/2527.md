# #2527 — Ownership / RACI: customer-side decline escalation stops at the assigner, no reports-to chain to climb

- **Status:** ✅ DONE 2026-09-04T02:25:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 609cc5ae7 (real work), 6e63555db (merge into main, pushed as a7ad50a45..6e63555db)

## Log
- 2026-09-04T00:20:00Z ⏳ IN FLIGHT — Investigated #1519/#2527. Confirmed no manager/reports-to
  column exists anywhere in `users`, and no Graph manager-sync exists in this codebase (would be
  a new integration, not a column add). Building the real, buildable half per the issue's own
  suggested scope: a self-referencing `manager_user_id` column on `users`, a real endpoint to set
  it (mirrors the existing `/portal/team/*` capability endpoints), and extending
  `notifyOwnershipDeclined` to walk it when populated. Not building a Graph-sourced auto-sync —
  that's a separate, larger integration decision (new Graph scope/consent) out of this issue's
  medium-effort scope; noting that honestly rather than building it silently.
- 2026-09-04T02:25:00Z ✅ DONE — Shipped:
  - `usersTable.managerUserId` (nullable, self-referencing FK, `onDelete: set null`) —
    lib/db/src/schema/index.ts.
  - Migration `lib/db/migrations/manual/2026-09-04-users-manager-reports-to-2527.sql`,
    run against local Postgres in-session (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS
    manager_user_id`, index, self-marking INSERT). Confirmed column exists via
    `information_schema.columns` query.
  - `PATCH /portal/team/:userId/manager` (artifacts/api-server/src/routes/portal-team.ts) —
    same gate as the other `/portal/team/*` mutating routes (`denyIfCannotManageTeam`),
    same-tenant validation, cycle guard walking the candidate's own chain before accepting.
  - `notifyOwnershipDeclined` (artifacts/api-server/src/lib/notification-center.ts) now
    walks the assigner's real `managerUserId` chain (cycle-guarded, 50-hop cap) and
    notifies every real person on file above them, in addition to the assigner
    (unchanged behavior when no chain is set — the common case today, since nothing
    backfills this column).
  - Explicitly NOT built: a Microsoft-Graph-sourced auto-sync of the manager
    relationship. That's a separate, larger integration decision (new Graph `manager`
    read scope + admin consent) rather than a missing column, and no live
    `/portal/team` UI page exists yet under the new portal design (#1485) to wire a
    "set my manager" control against — so this ships as real, callable backend
    capability (schema + endpoint + escalation logic), the same shape the rest of
    `/portal/team/*` already has, without inventing a UI that has no design export.
  - Verification: `npx tsc --noEmit -p .` in `artifacts/api-server` is clean (after
    rebuilding `lib/db`'s project-reference `.d.ts` via `npx tsc -b .`, needed for the
    referenced-project type to pick up the new column). Existing
    `portal-team.test.ts` still passes (`npx vitest run`). Chain-walk logic verified
    against real local rows: set `42.manager_user_id=57`, `57.manager_user_id=103` on
    real tenant-1 customer users, ran the exact walk query standalone — got
    `[42, 57, 103]` — then reverted both test rows to NULL afterward. Working tree
    left clean (`git status --porcelain` empty). Branch merged into `origin/main`
    (verified via `scripts/dev-server/verify-branch-merged.mjs`).
