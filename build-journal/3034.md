# #3034 — Standing policies have no edit/deactivate route

- **Status:** ✅ DONE 2026-09-06T20:40:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 05b2c221f (feature commit, agent/3034-q1809), merged to main at c9c427f23

## Log
- 2026-09-06T18:20:00Z ⏳ IN FLIGHT — building PATCH /api/msp/standing-policies/:id (deactivate + edit authorable fields, ownership-checked); DELETE deliberately deferred per issue body (FK cascade concern).
- 2026-09-06T20:40:00Z ✅ DONE — added `PATCH /msp/standing-policies/:id` to `artifacts/api-server/src/routes/msp-standing-policies.ts`: edits `ouId`/`title`/`description`/`targetKind`/`targetState`/`catalogItemId`/`sopId` and flips `isActive`, all fields optional but at least one required, same `resolveMspIdStrict` + same-MSP existence checks the file's other routes already use (re-validates OU/catalog-item/SOP ownership for whichever fields actually change), re-fires `policy.standing_policy.activated` on the same condition POST does. No DELETE, per the issue's own explicit decision. No schema/migration needed — every touched column already existed. Added 6 new unit tests to `msp-standing-policies.test.ts` (16 total, all passing); full `npx tsc --noEmit` across api-server is clean. Pushed feature commit 05b2c221f on `agent/3034-q1809`, merged to `origin/main` at c9c427f23 (verified via `verify-branch-merged.mjs`: OK). Working tree clean.
