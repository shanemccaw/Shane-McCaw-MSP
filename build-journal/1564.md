# #1564 — Security Plan: Signature attaches to the scope, not to the plan generally

- **Status:** ✅ DONE
- **Scope:** platform
- **Commit(s):** 4ae74aa27 (real post-push hash on origin/main, verified via `verify-branch-merged.mjs`)

## Log
- 2026-08-31 ⏳ IN FLIGHT — #1561 (assembled view, DONE at 46ff73e92) already built the
  seal/supersession chain, scope filtering (#1563) and the filter footprint (#1565).
  Re-reading #1564's own body: "Settled" says a signed version must record the scope it
  covers as a real, bounded statement ("our identity control posture as of this date"),
  never an unqualified blanket claim; "Consequence" says a scope change produces a new
  version, never an amendment. "Authority" (whether signing requires tenant-level "A" per
  #1511, or stays informational) is explicitly **Open — deliberately not decided** in the
  issue body itself; per the standing "stop only for a genuine product decision" rule,
  that half is left alone, not invented.
  Gap found: `SecurityPlanScope.statement` is caller-supplied and optional, and nothing
  synthesizes one when absent — `security-plan-assembly.test.ts` never exercises it. A
  version can currently be sealed (and later signed) via `POST .../versions` with no
  `scope` body at all, landing a `footprint.scope.statement` of `undefined`: exactly the
  unqualified-claim shape #1564's "Settled" section forbids. Building: an always-non-empty
  synthesized statement (verbatim if the caller supplied one, else derived from the
  honest/scoped state and applied dimensions) baked into the footprint at assembly time,
  plus a top-level `scopeStatement` on the wire version so the bounded claim is legible
  without parsing `content`. No schema/migration change — `content` is already jsonb.
- 2026-08-31 ⚠️ CONCURRENT COLLISION — while committing, a peer session landed
  `dbb20eefb` ("Security Plan: enforce scope statement on scoped seals (#1563)", bookend
  #1563 DONE at `315684e10`), touching the exact same functions in
  `security-plan-assembly.ts` / `msp-security-plan.ts`. Their `scopeMissingRequiredStatement`
  REJECTS a scoped seal with no caller-supplied statement (400) but deliberately requires
  nothing on the honest view — which is precisely #1564's own gap. Reconciled by hand
  (not a blind rebase auto-merge): kept their reject-don't-fabricate enforcement for the
  scoped case untouched, and narrowed `synthesizeScopeStatement` to (a) always fill the
  canonical honest-view statement when none is supplied, and (b) act only as a structural
  last-resort fallback for a scoped view reaching `applyScopeAndFootprint` some other way
  (e.g. the unsealed live `GET .../assembled` view, which isn't gated by the POST
  validation) — the real, human-authored statement the route now requires is what
  actually reaches every seal. Both test suites merged (12 tests total, all passing).
- 2026-08-31 ✅ DONE — reconciled work re-verified and shipped at `4ae74aa27` on
  `origin/main` (rebased twice more over two further concurrent pushes — #1562 DONE at
  `ec6c81996`, no conflicts on the second rebase). `verify-branch-merged.mjs`: OK.
  Verification (all observed this session, not inferred): `tsc -p` (worktree-local
  `@workspace/db` path override per the #2121 pattern) zero errors on
  `security-plan-assembly.ts` / `security-plan-versioning.ts` / `msp-security-plan.ts` /
  `lib/db/src/schema/msp.ts` — the unrelated program-wide errors it also reports
  (`ai-cost-capture.test.ts`, `ai-usage-sink.test.ts`, `admin-build-tracker.ts`,
  `sow-claim-binding-560.test.ts`, `onedrive-sharing`/`sharepoint-sharing`
  `armOperation`, etc.) match the pre-existing #1765/#1772/#1774/#1777/#1778/#1781
  class and are not mine. `vitest` (worktree-local alias override): 12/12 pass in
  `security-plan-assembly.test.ts` (7 pre-existing/#1563 + 5 new #1564 tests). Live-DB
  smoke against real local Postgres (`msp_security_plan_versions`, tenant/customerId 1):
  sealed an honest version with no scope body → `scopeStatement` = "Full assessed
  estate — no scope narrowing applied."; sealed a scoped version with a real caller
  statement → round-tripped verbatim through the jsonb store; confirmed version 1→2
  with the honest one marked `supersededAt` (a scope change is a NEW version, never an
  amendment — #1564's "Consequence"); table confirmed back at 0 rows after cleanup
  (matches its state before and after #1561/#1508 — nothing has actually signed a plan
  yet). `git status --porcelain` clean; no scratch/override files left behind.
  No schema/migration file — the change is TS-type + logic only, `content` was already
  jsonb, so nothing to log on #1630.
  Authority (#1564's own "Open" section — role-based tenant-level "A" for signing vs.
  informational-only) was left alone, exactly as the issue instructs: it is a real
  product decision the issue itself declares undecided, not a missing column.
  No new findings filed — the only gap found (`statement` optional/unenforced on the
  honest path) is what this build closed, and the #1563 collision above was resolved
  in-session rather than needing its own issue.
