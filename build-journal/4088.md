# #4088 — Portal RequireAuth still gates on retired "CustomerUser" — every real Customer account refused login

- **Status:** ✅ DONE 2026-09-14T17:44:28Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 62261b894 (IN FLIGHT bookend), 8b6106b95 (real fix, on `main`)

## Log
- 2026-09-14T17:18:14Z ⏳ IN FLIGHT — investigating App.tsx RequireAuth + portal-identity-interstitial.tsx stale "CustomerUser"/legacy role literals per issue body; will grep artifacts/portal and artifacts/msp-console for further stale references before fixing.
- 2026-09-14T17:44:28Z ✅ DONE — Root cause confirmed by direct read: `App.tsx`'s `RequireAuth` compared `user.mspRole !== "CustomerUser"` (retired by #3590's rename to `"Customer"`), so every real Customer account failed the check and was shown the staff-only identity interstitial. Fixed `App.tsx` (line 108) and rebuilt `portal-identity-interstitial.tsx`'s `ROLE_LABELS`/`ALL_ROLES` against the real, current 8-value `LEGACY_ROLE_ORDER` taxonomy (`lib/db/src/rbac/legacy-ladder.ts`): `Free`, `RetainerNoConsent`, `RetainerConsented`, `Customer`, `ServiceAccount`, `MSPOperator`, `MSPAdmin`, `PlatformAdmin` — kept as a local literal mirror (portal has no `@workspace/db` dependency; not added one, matching existing convention in `account-security-api.ts`/`useRemediationTracker.ts`).

  Per the issue's own instruction, grepped `artifacts/portal/src` and `artifacts/msp-console/src` for any other stale `"CustomerUser"`/`"Assessment"` role literal before calling this done — found and fixed four more real spots in `artifacts/msp-console`: `api/account-security-api.ts`'s `AccountSecurityRole` type union, `console/modules/AccountSecurity.tsx`'s `ROLE_TONE` map + fallback default, `console/modules/StaffRoster.tsx`'s `ROLE_TONE` map + two fallback defaults + a `"Listed, but not staff"` copy ternary that was mislabeling every real Customer account there as "an assessment sign-up" (replaced with a `nonStaffAccountLabel()` helper covering all six non-staff roles), and a stale comment in `api/team-api.ts` asserting invited teammates are written `mspRole: "CustomerUser"` (verified against `msp-team.ts:290` — real code writes `LEGACY_ROLE.customer` = `"Customer"`).

  Updated `test-manifests/portal/identity-interstitial.json` to match (discover-before-create — this manifest already existed and covers this exact gate): its own negative-control assertion was asserting the retired `"CustomerUser"` value, so it would not have caught this regression.

  Typecheck: `tsc --noEmit` clean on both `artifacts/portal` and `artifacts/msp-console` (exit 0, no output).

  **Live verification, both required paths, per the issue's explicit bar:** created two synthetic `zz-test-4088-*@example.com` accounts directly in local Postgres (one `Customer`/tenantId=1, one `MSPOperator`/mspId=1 — `TEST_ADMIN_EMAIL`/`TEST_PORTAL_EMAIL` etc. are genuinely `<unset>` in this BuildConsole instance, same real gap #4062's session hit and #4091 is now queued to fix generically). Ran a scratch manifest (not part of the deliverable, not registered) via `shaneapp://runTest` against the live local dev server (portal on :5175, `devBaseUrl` temporarily pointed there and restored after) — **18/18 steps passed real, live, in-process through `RunManifestAsync`**: staff (MSPOperator) login shows the interstitial with the real label "MSP Operator" (not the old "a staff" fallback), accepts, lands on the Overview; Customer login shows **no** interstitial and lands directly on the Overview — the exact regression this issue reported. Synthetic accounts and their sessions/refresh tokens deleted afterward; `devBaseUrl` restored to its original value.

  **Finding filed:** #4095 (`MspUserProfile.mspRole` OpenAPI enum missing `RetainerNoConsent`/`RetainerConsented` — a real but non-runtime-blocking type-accuracy gap noticed while auditing role vocabularies), parented under Feature #1650, labeled `bug`, board status "AI Batter Up".

  `git status --porcelain` clean. `verify-branch-merged.mjs`: OK — `agent/4088-q2760` (8b6106b9) is merged into `origin/main`.
