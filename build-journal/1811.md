# #1811 — Consent scopes drifted from live app registrations (Tasks.Read.All)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** fdcbdce67

## Log
- 2026-08-30 ⏳ IN FLIGHT — add `Tasks.Read.All` to `REQUIRED_MT_SCOPES`, repair `consent.test.ts`,
  resolve #1812's baseline JSONs to real permission names and two-way diff against
  `REQUIRED_MT_SCOPES` / `REQUIRED_WRITE_APP_PERMISSIONS` / `REQUIRED_SHAREPOINT_APP_PERMISSIONS`,
  count affected tenants.
- 2026-08-30 ✅ DONE — commit `fdcbdce67` (verified merged into `origin/main` via
  `verify-branch-merged.mjs`). Added 4 real missing scopes to `REQUIRED_MT_SCOPES` (31 → 35):
  `Tasks.Read.All` (this issue's headline), plus `DeviceManagementApps.Read.All`,
  `TeamworkDevice.Read.All`, `Exchange.ManageAsAppV2` found via the #1812 baseline audit. Repaired
  `consent.test.ts` (header, length 31→35, membership list now 1:1 with the array); 58/58 tests
  pass. Resolved all 3 #1812 baselines to real permission names via a new self-contained
  `scripts/azure/resolve-baseline-permissions.mjs` (calls `az ad sp show` live, no cached files).
  Write app (`REQUIRED_WRITE_APP_PERMISSIONS`) and SharePoint
  (`REQUIRED_SHAREPOINT_APP_PERMISSIONS`) both matched their live registrations exactly — zero
  drift, including confirming `RoleManagement.ReadWrite.Directory`'s TRANSCRIPTION NOTE is
  genuinely granted. Reported (not added/removed) one ambiguous delegated `User.Read` scope and
  filed **#1830** (parented #1096, AI Batter Up) for a `BitLockerKey.Read.All` casing typo found
  across 3 files plus a stale `docs/pillarmapping.md` scope count. Queried `tenants.consent` on
  both local Postgres and real Replit prod (via SSH) — same 2 rows both places: **2 of 2 granted
  tenants predate the grant and lack `Tasks.Read.All`**; no re-consent flow built per scope stop.
  Full audit posted as a comment on #1811. `git status --porcelain` clean. `tsc --noEmit`: only
  pre-existing unrelated errors (admin-build-tracker.ts, msp-change-executions.ts,
  msp-diagnostics.test.ts, sow-claim-binding-560.test.ts) — none touching files this build changed.
