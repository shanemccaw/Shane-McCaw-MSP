# #3011 — Build queue can't self-recover from a tripped #2815 circuit with no free slots / candidates

- **Status:** ✅ DONE 2026-09-06T17:32:46Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 0103120dd (fix, on main), aa75a64a7 (this bookend, IN FLIGHT), rebased+pushed via `git push origin HEAD:main` (633781b81..0103120dd)

## Log
- 2026-09-06T17:30:45Z ⏳ IN FLIGHT — read #3011's body: `QueueWatcherService.TickAsync`'s
  `if (freeSlots <= 0) return;` (and, separately, an empty/all-unblocked queue) skips the only
  automatic path that can probe and close a tripped Git #2815 rate-limit circuit, since the Git
  Board's own fetch is manual-only. Confirmed #3009 (consolidating the claim-path open-issue
  fetch onto the Git Board snapshot) is already merged into this branch's `main` base. Plan: add
  a demand-driven, throttled self-recovery probe to `TickAsync` that fires even when the claim
  path can't/won't make its own live call, gated on `GitHubRateLimitCircuit.IsTripped` so a
  healthy circuit costs nothing extra (keeping #3009's redundant-traffic fix intact), and reusing
  the circuit's own half-open single-probe lease inside `SubprocessRunner.RunAsync` for throttling.
- 2026-09-06T17:32:46Z ✅ DONE — added `GitHubRateLimitCircuit.IsTripped` and
  `QueueWatcherService.MaybeSelfRecoveryProbeAsync()`, called from `TickAsync` right after the
  existing phantom-running sweep (before the `_appReady`/`_paused` gates, same "background
  self-repair runs regardless" precedent already used there) — so it covers ALL three real gaps
  the issue names: slots full (`freeSlots <= 0` early return), no candidates (empty/all-unblocked
  queue never reaches `GetNextAsync`'s live fetch), and even while paused. A successful probe is
  forwarded through `ApplyOpenIssueSet`, the same #3009 snapshot path. `dotnet build
  BuildConsole.csproj -c Debug` succeeded (0 warnings, 0 errors) both before and after rebasing
  onto current `origin/main`. Rebased my 2 commits (bookend IN FLIGHT + the fix) onto
  `origin/main` (11 commits ahead of this worktree's stale base) and pushed directly to `main`:
  `633781b81..0103120dd`. `verify-branch-merged.mjs` confirms `agent/3011-q1788` (0103120d) is a
  genuine ancestor of `origin/main`. `git status --porcelain` clean. No new backend/DB/UI surface
  needed — this is a pure C#-side watcher-loop fix, no test-manifest applicable
  (`desktop/BuildConsole` is explicitly out of scope for those per CLAUDE.md). No findings filed —
  investigation surfaced nothing beyond the issue's own already-precise diagnosis.
