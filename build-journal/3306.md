### #3306 — Fix 5 real check.mjs failures — Income Gate unconfigured, webhook-origin mismatch, MCP plain-text parsing

- **Status:** ✅ DONE 2026-09-09T02:52:00Z
- **Scope:** platform (web/shanes-life)

**Log**
- 2026-09-08T00:00:00Z — Session started. Located app at `web/shanes-life/` (bin/check.mjs, src/core/money.mjs, src/routes/api.mjs). Investigating the 3 root causes from the issue body.
- 2026-09-09T02:52:00Z — ✅ DONE — Fixed all 3 real root causes in `bin/check.mjs`, commit `86c064ff2`:
  1. **Income Gate check** (~line 982): added a real branch on `sums.gate_name === null` (no
     `accounts.role = 'income_gate'` row) that explicitly asserts the API's `gate.json?.gate?.name`
     is `null` too, instead of relying on `null === null` coincidence.
  2. **Webhook-origin check** (~line 1145): now compares `banks.json?.webhookUrl` against
     `plaid.webhookUrl()` (imported straight from `src/core/plaid.mjs`, which derives it from
     the server's own `config.publicOrigin`) instead of re-deriving `${BASE}/api/plaid/webhook`
     from the checker's own localhost connection URL.
  3. **MCP tool-result parsing** (`toolResult()`, ~line 86): a failed tool call's
     `content[0].text` is real prose (`"<tool> failed: <message>"`), never JSON — `toolResult()`
     was blindly calling `JSON.parse()` on it, which throws and crashes the whole check run
     rather than failing just the one assertion. Fixed to detect `isError`/a parse failure and
     hand back `{ isError: true, text: raw }` instead of throwing.
  - Verified live: ran the real server locally against the shared `finances` Postgres database
    (`DATABASE_URL` from `%AppData%\ShanesSurvival\settings.json`) and `npm run check` twice
    (fresh server restart between runs to clear the in-process rate limiter) — **294/294 checks
    passed** both times, confirming no regression. The specific unconfigured-Income-Gate and
    cross-origin webhook mismatches described in the issue are real-Replit-environment-only
    conditions (locally there IS an `income_gate` account and `PUBLIC_ORIGIN` matches `BASE`),
    so the fix is code-reviewed against the real, confirmed root causes rather than reproduced
    failure-to-pass locally — the branch logic and the source-of-truth comparison are both
    directly traceable to the real code (`src/core/money.mjs` `computeGateMath`'s own
    "No account assigned the Income Gate role yet" warning; `src/core/plaid.mjs`'s
    `webhookUrl()`).
  - `.env` created locally to run the server/check (gitignored, not committed) — one-time
    scratch config, not part of the deliverable.
  - Branch `agent/3306-q2031` rebased onto `origin/main` (moved twice under concurrent load)
    and pushed directly to `main`; ancestor-check and `git status --porcelain` both verified
    clean before this bookend.
