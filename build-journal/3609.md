# #3609 — Weekly Retargeting + Weekly Copilot Assessment rescans fan out over the identical Free population

- **Status:** ✅ DONE 2026-09-11T04:40:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** d76669e5f

## Log
- 2026-09-11T04:05:00Z ⏳ IN FLIGHT — reading both seeded weekly rescan triggers (seed + live wf_triggers) to decide which is redundant post-#3590 and rewrite the fan_out_query/schedule.
- 2026-09-11T04:40:00Z ✅ DONE — Neither rescan retired; re-keyed on purchase (the issue's first option). Sunday "Weekly Copilot Assessment Rescan" = Free users holding an active PAID assessment (`service_type='assessment' AND NOT is_free_offering` — the free scan/snapshots are service_type 'assessment' too), rescanning the purchased package. Monday "Weekly Retargeting Rescan" = Free users on every other tenant (tenant-wide NOT EXISTS). Queries live in new `artifacts/api-server/src/lib/weekly-rescan-populations.ts`. Existing triggers converge at seed time (new `legacyFanOutQueries` + guarded UPDATE, only while the query still equals a previously seeded string) — so Staging self-heals on api-server start; no manual SQL, nothing for #1630. `GET /api/portal/customer/rescoring-status` now reports next_run_at from whichever trigger covers the tenant (it was hard-wired to the Sunday one). Crons unchanged (Sun/Mon 03:00). Verified: api-server `tsc --noEmit` clean; live local DB — Sunday = tenant c4c814d4 (user 37, assess:copilot-readiness), Monday = tenant 0a361ab2 (user 56), tenant overlap none, union == pre-fix tenant set; local triggers 40/42 now hold the new queries. Not verified live: rescoring-status over HTTP (dev server runs main's checkout; typecheck only). Finding filed: #3642 (per-user fan-out scans a multi-user tenant N times per fire) under #1944, AI Batter Up.
