# #4245 — Delivery Projects Kanban: re-gate admin-projects.ts's real task API + SSE for MSP Console operators

- **Status:** ✅ DONE 2026-09-15T13:55:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** b176157e5 (main)

## Log
- 2026-09-15T13:15:00Z ⏳ IN FLIGHT — Starting real route-by-route audit of artifacts/api-server/src/routes/admin-projects.ts to separate genuine delivery/kanban task+SSE routes from genuine platform-admin-only routes, before re-gating anything.
- 2026-09-15T13:55:00Z ✅ DONE — Real audit found NO genuinely platform-admin-only route in
  admin-projects.ts: every one of its 20 HTTP routes plus the kanban-events SSE stream
  operates on a single customer's delivery project (projects / workflow-steps /
  kanban-tasks / project-closures), matching #3433's own framing. Re-gated all of them
  from `requireAdmin` to `requireCapability("ladder.msp-operator")`, matching the pattern
  already used in msp-message-center.ts. The SSE route can't use the middleware
  (EventSource sets no Authorization header) — it now calls `userClearsLadderCapability()`
  directly, the same shape msp-sales-offers.ts's own SSE route already uses. SSE mechanics
  (`setupSSE`, keep-alive, `registerSSEClient`) untouched — only the authorization check
  changed. Also fixed 8 hardcoded audit-log `actorRole: "admin"`/`"platform_admin"` values
  (accurate only while admin was the sole possible caller) to `resolveAuditActorRole(req.user!)`,
  same pattern as msp-active-directory.ts — otherwise MSP-operator actions would have been
  misattributed in the audit trail by the very gate widening this issue asked for.
  Contract pack extracted per the #1642 pattern: `docs/delivery-projects-kanban-contract-pack.md`
  (route-by-route wire contract, known-fragile behaviors, out-of-scope adjacent systems
  `msp-kanban.ts`/#3773 and `portal-delivery-kanban.ts` noted and left untouched).
  Finding filed: #4248 (`GET /admin/projects/:id/report-autofill` has no live UI caller
  anywhere in the repo — grepped) — parented under #3433 as a sibling sub-issue, labeled
  `bug`, board status set to AI Batter Up.
  **Verification:** `tsc --noEmit` clean on `artifacts/api-server`. Targeted test suites
  green: `requireCapability-keys.test.ts` (8/8 — confirms every literal capability key
  reaching `requireCapability` in the whole api-server, including this file's 20 new call
  sites, is catalogued and ladder-decidable), `rbac-ladder.test.ts`, `portal-delivery-kanban.test.ts`
  (37/37 total). Live end-to-end curl/runTest verification against a running api-server
  was **not performed**: `request-restart.mjs --agent 4245-q2907` confirmed the commit
  merged into the shared dev-server checkout (`933f5fe9`) but reported the restart
  **deliberately DEFERRED** — this build is part of `--buildSet MSPConsole`, coalescing
  restarts across the wave (39/45 merged at the time of my request), so the api-server
  serving live traffic was not yet running my change and forcing an out-of-band restart
  would have defeated that coalescing for the other ~6 in-flight builds in the same set.
  Branch-merge check passed (`verify-branch-merged.mjs` — `b176157e` is an ancestor of
  `origin/main`). Working tree clean (`git status --porcelain` empty) at session end.
  #4246 (frontend relocation) is now unblocked to wire against the routes documented in
  the contract pack.
