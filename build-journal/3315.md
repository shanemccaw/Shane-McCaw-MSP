# #3315 — Scan camera shows black box on iOS -- dialog.showModal() called after camera stream already started

- **Status:** ✅ DONE 2026-09-09T00:15:00Z
- **Scope:** platform (web/shanes-life, Shane's Life shopping feature)
- **Started:** 2026-09-09
- **Commit(s):** 0eb36c86e (fix), c0c975c75 (IN FLIGHT bookend) — both on `main`

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — Confirmed root cause in `web/shanes-life/public/app.js`'s
  `openScanSheet()`: `dialog.showModal()` was called only at the very end of the function, after
  the entire async `getUserMedia`/`decodeFromConstraints` camera setup already ran. A native
  `<dialog>` without `showModal()`/`show()` is `display:none` per spec, so the video element was
  still hidden/unpainted while the camera stream started — matches Shane's exact live-device
  symptom (permission granted, no error, but the feed never paints). This also silently broke the
  function's own `if (!dialog.open) { controls.stop(); }` early-close guard, since `dialog.open`
  was always false at that check (showModal hadn't run yet), causing it to stop the scan controls
  immediately after every successful camera start.
- 2026-09-09T00:15:00Z ✅ DONE — Moved `dialog.showModal()` to right after
  `body.append(video, status, resultBox, ...)`, before the async camera setup, and removed
  the now-redundant trailing call. `node -c` syntax check passes. Merged straight to `main`
  (fast-forward, `agent/3315-q2039` -> `main`, verified with
  `scripts/dev-server/verify-branch-merged.mjs`). **Honest verification gap, stated plainly
  per #3315's own request:** no real iOS device is reachable from this session, so the live
  camera-paints-immediately confirmation the issue explicitly asks for was NOT performed --
  same disclosure #3263 made for this identical limitation. The fix is a straightforward
  reorder of one line (root cause is unambiguous per the spec: an un-shown `<dialog>` is
  `display:none`, so its descendant `<video>` cannot paint), and it additionally repairs the
  function's `!dialog.open` early-close guard that depended on the dialog already being open.
  Real device confirmation from Shane is the only remaining verification step.
