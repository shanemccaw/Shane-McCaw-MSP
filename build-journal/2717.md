# #2717 — Verify api-server typecheck + msp-active-directory.test.ts

- **Status:** ✅ DONE 2026-09-04T00:25:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 128ac703b (IN FLIGHT bookend), 54b8001d2 (DONE bookend) — no code changes this session

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — running `pnpm --filter @workspace/api-server run typecheck`, then executing `msp-active-directory.test.ts` for real.
- 2026-09-04T00:25:00Z ✅ DONE — Still broken, honestly reported. `pnpm --filter @workspace/api-server run typecheck` fails with dozens of `Cannot find module 'zod'/'drizzle-orm'/'express-rate-limit'/'vitest'/'pino'/'pdf-lib'` and `TS6305: Output file lib/db/dist/index.d.ts has not been built` errors. Root-caused, not guessed: the shared main checkout's `node_modules/.pnpm` store has widespread **empty extracted package directories** — confirmed via `Get-ChildItem -Recurse` on `zod@3.25.76` (0 files under `node_modules/zod`) and `vitest@4.1.9_...` (same), and a broader sweep found the same for `express`, `ajv`, `cors`, `express-rate-limit`, `jsonwebtoken` under other packages' nested deps. This reproduces from the worktree AND directly against the main checkout's own store (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\node_modules\.pnpm\...`), so it is not a worktree-junction/dangling-symlink issue — the junctions resolve correctly (verified with `fs.realpathSync`); the extracted package contents themselves are missing. Confirmed via direct `node <path>\vitest.mjs run src/routes/msp-active-directory.test.ts`: `Cannot find module '...vitest.mjs'` — vitest itself is one of the corrupted store entries, so `msp-active-directory.test.ts` could not be executed at all.
  - This is a **store corruption problem, not a code logic problem** — per this issue's own instruction, that means no new Feature follow-up filed under this verification-only issue. It's the same class already tracked under Git #1987/#1988, just a different manifestation (empty extracted dirs vs. dangling symlinks into a deleted worktree).
  - Per CLAUDE.md: `pnpm install` / `pnpm install --force` were **not** run — not a remedy for this class, metered-bandwidth cost, and the override belongs to Shane via BuildConsole, not self-granted here.
  - No code changed. Working tree left clean (only this bookend file touched, on its own commits).
