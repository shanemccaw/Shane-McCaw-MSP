# #2177 — Sub-issue of #1202: revert #2175's live BuildConsole dialog swap

- **Status:** ✅ DONE 2026-09-02T00:26:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** c138ba674 (bookend IN FLIGHT), 17561737c (real revert)

## Log
- 2026-09-02T00:20:00Z ⏳ IN FLIGHT — confirmed 8fe12648e (mentioned in the issue as a possible second commit to revert) is a dangling commit object not reachable from origin/main or any branch; only a7396ed3b is actually in history. Reverting a7396ed3b via `git revert` to restore desktop/BuildConsole to its pre-#2175 state (removing AppDialog.xaml/.xaml.cs and restoring the 24 MessageBox.Show call sites across 15 files).
- 2026-09-02T00:26:00Z ✅ DONE — `git revert --no-edit a7396ed3b` (commit `17561737c`), diffstat exactly mirrors the original (17 files, -316/+41 vs the original's +316/-41). Verified `git diff a7396ed3b~1 HEAD -- desktop/BuildConsole` is empty — `desktop/BuildConsole` is byte-for-byte identical to its pre-#2175 state. `AppDialog.xaml`/`AppDialog.xaml.cs` deleted; all 24 `MessageBox.Show` call sites restored across the 15 touched files (`App.xaml.cs`, `Controls/AiBatterUpPanel.xaml.cs`, `Controls/BatterUpPanel.xaml.cs`, `Controls/ChatMappingsDocumentView.xaml.cs`, `Controls/DispatchPanel.xaml.cs`, `Controls/GraphApiDocumentView.xaml.cs`, `Controls/LeftSidebar.xaml.cs`, `Controls/SettingsTabView.xaml.cs`, `EditIssueDialog.xaml.cs`, `MainWindow.xaml.cs`, `NeedsAttentionDetailDialog.xaml.cs`, `NewIssueDialog.xaml.cs`, `StagingDeployDialog.xaml.cs`, `TestHistoryWindow.xaml.cs`, `TestRunnerWindow.xaml.cs`). `dotnet build desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0 errors. Pushed to origin/main. `verify-branch-merged.mjs` exit 0. `git status --porcelain` clean.
