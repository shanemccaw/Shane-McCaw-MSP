# #1886 — Open-tab persistence (#874) only saves on discrete events — crash between events loses recent tabs

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 0418557e2 (Periodic safety-net save for open-tab persistence), merged to
  `main` at ef15332e5

## Log
- 2026-08-30 ⏳ IN FLIGHT — read `PersistOpenChatTabs` (~3386) and every call site
  (open ~2865, rename ~4314, mark-in-progress ~4403, drag ~7244, dock ~7288). Adding a
  periodic 15s safety-net `DispatcherTimer` that unconditionally calls
  `PersistOpenChatTabs()`, rather than a per-change debounce, since `_chatTabs` is
  mutated from many scattered call sites in `MainWindow.xaml.cs` and a periodic sweep
  bounds the crash-loss window without requiring every current/future mutation site to
  remember to call it.
- 2026-08-30 ✅ DONE — chose the **periodic safety-net save** approach explicitly over
  the "also call on tab open" alternative: tab-open at ~2865 already calls
  `PersistOpenChatTabs()` (that specific gap the issue named is not actually open in
  current code), but four other mutation sites already do too and more exist/will exist
  (rename, drag, dock, mark-in-progress) — a periodic sweep bounds the crash-loss window
  to a fixed interval for ALL of them without auditing/tagging every one. Added
  `_persistTabsTimer` (`DispatcherTimer`, 15s interval) started alongside the other
  status-poll timers in the constructor, ticking `PersistOpenChatTabs()` unconditionally
  — the method is already cheap (small in-memory snapshot + JSON save) and already
  self-guards via its own try/catch. Shipped in `0418557e2`, pushed to `main` at
  `ef15332e5`. Verified: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c
  Release` — Build succeeded, 0 Warnings, 0 Errors. `git merge-base --is-ancestor HEAD
  origin/main` confirmed after push. `git status --porcelain` clean.
