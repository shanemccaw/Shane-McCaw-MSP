# #2997 — Billing: invoice detail, pay-invoice, PDF download (v2)

- **Status:** ✅ DONE 2026-09-06T19:15:00Z — superseded by #2998, no independent commit landed
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** none of this session's own — see Log for why; real landed work is #2998's
  `2f37bf1e0` (already on `main`)

## Log
- 2026-09-06T15:05:00Z ⏳ IN FLIGHT — Read issue #2997 + billing-contract-pack.md +
  `Design/portal/design_handoff_full_site/screens/Billing.dc.html`. The landed
  design's own "What this page deliberately does not do" ledger explicitly lists
  "No pay button on a receipt" and "No invoice detail page" as deliberate,
  documented non-goals for this screen (citing §1.2/§1.3 back to this same
  contract pack). Built `artifacts/portal/src/pages/billing.tsx` against that
  design (Receipts, real PDF download, "Manage payment in Stripe", the honest
  gap-ledger disclosure), wired it into `App.tsx`/`UserMenu.tsx`, extended
  `billingWire.ts` with the real `paid` field for the STATUS pill, added
  `refetch()` to `billingLive.ts`, wrote `test-manifests/billing/receipts.json`,
  and retired the stale `test-manifests/portal/portal-v2-billing.json`. `tsc
  --noEmit` and `vite build` both clean. Committed and pushed to this session's
  own branch (`4bc242496`, `70b06f343`).
- 2026-09-06T19:10:00Z — On merging toward `main`, discovered a genuine
  concurrent-duplicate-build collision: sibling issue **#2998** ("Billing: plan
  state from live subscription endpoints (v2)"), also a leaf sub-issue of the
  same Feature **#1598**, was dispatched at essentially the same time against
  the same landed `Billing.dc.html` and independently built **the exact same
  file**, `artifacts/portal/src/pages/billing.tsx` — same Receipts +
  download + "Manage payment in Stripe" wiring, same `billingWire.ts` `paid`
  field, same `refetch()` addition to `billingLive.ts`, same conclusion (from
  the same design ledger) that invoice-detail and pay-invoice are deliberate
  non-goals for this screen. #2998's version merged to `main` first
  (`2f37bf1e0`) and is already labeled `complete` — it also filed the same
  stale-manifest finding I found (#3056, closing the loop on
  `portal-v2-billing.json`) and a related stale-fixture-comment finding
  (#3050).
- 2026-09-06T19:15:00Z ✅ DONE — Read `origin/main`'s already-merged
  `billing.tsx`/`billingWire.ts`/`billingLive.ts`/`test-manifests/portal/
  billing.json` directly and confirmed it fully satisfies #2997's own real
  scope: `GET /api/portal/invoices/:id/download` is wired (per-row `Receipt`
  button, `data-testid="billing-download-<id>"`), and `GET /invoices/:id` /
  `POST /invoices/:id/pay` are documented, on the record, as the design's own
  deliberate non-goals — not silently missing. Reset this branch to
  `origin/main` (`git reset --hard origin/main`) rather than push a
  functionally-duplicate competing implementation over already-landed,
  equivalent work; my own branch's commits remain on
  `origin/agent/2997-q1756` as a record of the independent build. No new
  finding filed for the collision itself — it's a structural side effect of
  two leaf issues under one Feature (#1598) both resolving against the same
  single-page design at the same time, and #2998 already filed the two real
  findings that came out of it (#3050, #3056). `git status --porcelain`
  clean; branch HEAD is `origin/main`'s own tip, trivially an ancestor of
  `main`.
