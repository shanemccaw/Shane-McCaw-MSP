# #3449 — Systematic audit: remaining live/unbatched GitHub call sites (rate-limit circuit)

- **Status:** ✅ DONE 2026-09-10T02:53:47Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** `44eb56110` (IN FLIGHT bookend), `dac6cd330` (inventory doc `docs/github-call-site-audit-3449.md`)

## Log
- 2026-09-10T02:28:23Z ⏳ IN FLIGHT — Comprehensive, systematic audit of `desktop/BuildConsole/` for every remaining live GitHub call site (every `new GitHubApiClient(...)`, every `gh` CLI spawn, every direct GraphQL/REST call) that could burst and trip the #2815 rate-limit circuit. Investigation/inventory task per #3449 body: classify each site as mirror-backed (safe) / rare-manual (safe) / live-bursty (real remaining gap), produce a checkable inventory, and file each confirmed remaining gap as its own sibling sub-issue of Feature #1789. No code fixes in this pass. Read prior-fix context: #3113 (`bt_issue_mirror` root fix), #3358 (Git Board tree migrated), #3365 (residual ALL-states rollup still live), #2815/#2817 (circuit + root volume), #2539 (`SubprocessRunner` = the one git/gh spawn helper).
- 2026-09-10T02:53:47Z ✅ DONE — Inventory-only build (no product code changed; #3449 is an audit/inventory task, and CLAUDE.md excludes `desktop/BuildConsole` from test manifests). Shipped `docs/github-call-site-audit-3449.md` (commit `dac6cd330`) — the complete, checkable, file:line inventory of every `GitHubApiClient`/`gh` call site in `desktop/BuildConsole/`, classified into 5 tiers (real remaining gap / manual-only safe / by-design live / mirror-backed / no-call). Audited against `c31290ce4` via 3 parallel deep-tracing passes over the automatic services, `LeftSidebar.xaml.cs`, and all panels/dialogs, then verified every HIGH/MEDIUM candidate against the real code myself.

  **Key result:** the prior night's fixes closed the big automatic drivers. **Three real remaining automatic live gaps** found + filed as siblings of #1789, all set to board status "AI Batter Up", labeled `bug`, milestone v1.1:
  - **#3467 (HIGH)** — `LeftSidebar.EnrichBlockedStatusCoreAsync` (`:2410`): ~528 live per-issue `GetOpenBlockedByAsync` on every board load (startup + refresh), never migrated to `bt_issue_mirror` (which already carries `blocked_by_numbers`/`blocking_numbers`). Includes the hover companion (`:6636`).
  - **#3468 (MEDIUM)** — `GitHubIssueTimeSeriesService.ResolveActiveMilestoneAsync` (`:472`): live `GetMilestonesAsync` on a 1-min unconditional timer (`_editorPanesStatsTimer`), a path #3359 left untouched; + two stale "TTL-cached" comments (`MainWindow.xaml.cs:1100-1102`, `EditorPanesStatsService.cs:32-38`).
  - **#3469 (MEDIUM)** — Batter Up / AI Batter Up `OnMirrorSyncCompleted` → `RefreshAsync` does live batched enrichment (`BatchGetRecentIssueCommentsAsync`/`BatchGetProjectItemStatusesAsync`) + auto-select `IssueDetailView.LoadIssue` on every mirror sync regardless of panel visibility; + stale "no new GitHub calls" comment (`AiBatterUpPanel.xaml.cs:94-97`). Already batched/circuit-aware so not a burst.

  **Diagnostic worth recording:** at a live hit GitHub's primary REST budget reads `core: 5000/5000, used 0` (`gh api rate_limit`) — so the trips are GitHub **secondary (burst/concurrency)** rate limits, not primary exhaustion, which is exactly why a bursty-call-site inventory is the right lever. Non-BuildConsole sweep clean (`scripts/config-state/fetch-sources.mjs` hits a different repo, unauthenticated, manual).

  **Verification (BuildConsole out of scope for test manifests per CLAUDE.md):** no code changed, so no build/test run; every finding is grep/read-checkable at the cited file:line against `c31290ce4`. Did NOT launch BuildConsole.exe (single-instance mutex hazard — memory `never-launch-buildconsole-exe-for-verification`). Branch `agent/3449-q2145` verified merged into main (see below). Working tree clean.
