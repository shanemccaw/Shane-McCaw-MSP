# #3207 — Feature: Money — Bills v3 refinements (% funded badges, months-behind stickers, per-bill target prompts)

- **Status:** ✅ DONE 2026-09-08T01:15:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 34d109f53 (feature work), rebased/merged onto `main` as 0debaf10e

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3207 + `Shanes Life 17 - Money v3.dc.html` (#2c/#2d), located the real Bills tab in `web/shanes-life` (public/app.js + src/core/money.mjs), about to wire real % funded badges, months-behind stickers, no-target prompts, and a real Behind summary block.
- 2026-09-08T01:15:00Z ✅ DONE — Implemented in `web/shanes-life/src/core/money.mjs` (real `fundedPercent` [0,100 clamp], `computeMonthsBehind()` off real `due_day`/`last_paid_date`, `owedCents` arrears, real `behind`/`behindOwedCents` list sorted by amount, `mask` now loaded on all role accounts) and `web/shanes-life/public/app.js` (`moneyPercentBadge` — red <30%/amber/emerald=100%, `sticker("red", …)` months-behind badge next to bill name, `noTargetPrompt()` for bills with no real target, new "Behind" card on the Bills tab, `moneyBehindRow()`) + `app.css` (`.money-percent-badge` tone variants, `red` sticker tone). No fixture data — all reads go through `GET /api/money/gate` (`getGateStatus`), unchanged endpoint. Verified live against the real local `finances` Postgres DB (`DATABASE_URL=postgresql://postgres:***@localhost:5432/finances`): current real bill accounts all compute correctly (e.g. H1 Mortgage 12% funded/red under real live balances, H2 Rent monthsBehind=0 since real `last_paid_date` 2026-09-04 is current — Behind list is honestly empty right now, no bill is actually behind). Reference-value check from the issue body verified with a simulated same-shape input (Mortgage balance/target = 1492.40/2550 → 59%/amber exactly; Rent 0/2200 → 0%/red; Electric 190.27/52 → 100%/emerald, clamped correctly) — matches design #2d's own stated reference numbers. No new migration needed (`due_day`, `last_paid_date`, `mask` all already existed). Ran into two concurrent-build merge conflicts (Git #3206's `masked`/`mask` work, Git #3211's `bankReconnectHandler`) while rebasing onto `origin/main`; resolved by hand, re-verified `node --check` + live DB read after each rebase. Merged onto `main` after 3 retries (busy night, `main` moved 3x during push). `git status --porcelain` clean.
