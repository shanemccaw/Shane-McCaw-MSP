# #4029 — Break-glass admin-override resets password before confirming the replacement can be stored

- **Status:** ✅ DONE 2026-09-14T13:08:51Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** c7002b14f (fix + tests + contract packs); b1023e268 (IN FLIGHT bookend)
- **Migration:** none
- **Findings filed:** #4040 (concurrent overrides not serialized — bug+security, sub-issue of #1651), #4041 (outcome_unknown / replacement_unrecorded leaves no persisted marker; invites still allowed — bug+security, sub-issue of #1651)

## Log
- 2026-09-14T12:47:26Z ⏳ IN FLIGHT — reading performBreakGlassAdminOverride + generated-secret-store to choose between supersede-and-record vs provisional-store-first for the post-reset store failure; moving the store-configured check before the Graph write (queue build 2711, Opus/High)
- 2026-09-14T13:08:51Z ✅ DONE — Chose store-first (issue direction b). Read generated-secret-store.ts: Key Vault has no rename, and none is needed — the secret name is random and unbound; the DB row written after the reset is the binding, and rollback is the existing delete+purge. New order in performBreakGlassAdminOverride: store-configured check → storeGeneratedSecret → Graph PATCH → record tx. Store unconfigured or throwing → 503 with the tenant untouched. Definite reset refusal (4xx or thrown write-back/consent gate error) → provisional copy purged, 502. Indefinite (5xx or transport throw) → copy kept, 502 outcome_unknown. Post-reset record tx failure → retried once (lookup by secret_ref->>'secretName' first, so a commit-then-disconnect cannot double-insert); still failing → 500 replacement_unrecorded, vault copy kept, old row left overridable. Direction (a) was not taken for the DB-failure residual because superseding needs the same DB that just failed, and with no successor row it would remove the only platform recovery path. Verification: new unit suite break-glass-admin-override-ordering-4029.test.ts 10/10 (+ #4015 redaction suite 2/2); api-server tsc exit 0. Live harness break-glass-admin-override.live-verify.ts against zz-test-graphwrite-01, GENERATED_SECRET_VAULT_URL="". Run 1: 6/7; the recovery re-override got a 502 from Graph whose detail the output filter hid, and it did not reproduce. Run 2: 7/7. Covered live: unconfigured store → 503 with 0 Graph writes; real SDK setSecret to the unreachable https://zz-test-4029.vault.invalid (ENOTFOUND) → 503 with 0 Graph writes; lastPasswordChangeDateTime unchanged after both; random nonexistent object id → Graph 404 → provisional purged; real reset landed + db.transaction forced to fail twice → 500, copy kept, row pending_delivery, lastPasswordChangeDateTime moved; re-run override → ok, old superseded_by_reset, new pending_delivery carrying breakGlassAccountId + secretRef, audit row. Honest gaps: the last three cases used an in-process store stub, because the only reachable vault locally is production (#1913); a real vault's RBAC refusal and throttling were not exercised; no non-production vault exists. Cleanup: 0 synthetic defs/runs/secrets/audit rows remain. Contract packs (portal §2.6, MSP console §1.4) updated. Pre-existing tree was clean.
