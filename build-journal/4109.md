# #4109 — Billing: MSP-console invoice CRUD (unsent) + versioned re-issue with reason (sent)

- **Status:** ✅ DONE 2026-09-14T20:58:06Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 7ed49ca24 (IN FLIGHT), 36eb65295 (real work)

## Log
- 2026-09-14T20:49:37Z ⏳ IN FLIGHT — investigated `admin-invoices.ts` (confirmed DELETE-any-status bug, PATCH also edits sent invoices in place with no versioning) and `invoicesTable` schema (no versioning columns yet); confirmed ownership-join pattern from #3967 (`msp-azure-credentials.ts`: `users.msp_id`) and the reason-required revise-after-close pattern from #4026 (`msp-retainer.ts` adjustments route) as the model to mirror. Next: additive migration for `version`/`supersedes_invoice_id`/`revision_reason` + `superseded` status value, then build `msp-invoices.ts`, then fix the admin DELETE bug.
- 2026-09-14T20:58:06Z ✅ DONE — Shipped `artifacts/api-server/src/routes/msp-invoices.ts`: full draft CRUD (create always starts as `draft`; PATCH/DELETE 409 once sent) plus `POST /msp/:mspId/invoices/:id/revise` (mandatory `reason`, creates a new versioned row, marks the prior row `status: "superseded"`), all gated `requireCapability("ladder.msp-operator")` + `requireMspScope("params")` and scoped via the real `users.msp_id` ownership join (same pattern as #3967). Registered in `routes/index.ts`. Additive migration `lib/db/migrations/manual/2026-09-14-invoice-versioned-reissue-4109.sql` (version/supersedesInvoiceId/revisionReason columns + index) — ran myself against local Postgres, confirmed columns/FK live. Schema: `lib/db/src/schema/index.ts` — extracted `INVOICE_STATUSES` const, added `"superseded"` (plain-text enum, no DB CHECK). Fixed the `admin-invoices.ts` DELETE-any-status bug (draft-only now, 409 otherwise). Verification: `tsc --noEmit` clean on both `artifacts/api-server` and `lib/db`; full lifecycle (create draft → edit → send → revise/supersede) verified via direct SQL mirroring the route's own queries against local Postgres, then cleaned up the test rows — no live/deployed dev-server restart available to this worktree without disrupting the `MSPConsole` buildSet, so HTTP-level verification wasn't attempted; the DB-level check exercises the same queries and constraints the routes run. Two findings filed and parented as siblings of #1692, board status set to AI Batter Up: #4116 (customer-portal display of superseded vs. latest invoice version — real product decision, explicitly deferred per #4109's own issue body) and #4117 (AdminV2's own PATCH route should probably adopt the same versioned-revise model, bigger change deferred here). Working tree clean, branch confirmed merged into origin/main (`verify-branch-merged.mjs` OK) before this bookend.
