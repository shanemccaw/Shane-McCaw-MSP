# #1799 — Build Watch progress panel freezes forever after one explicit reportProgress call

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 52ec29015 (bookend), fed65af6f (fix), merged to `main` at 3d0d32225

## Log
- 2026-08-29 ⏳ IN FLIGHT — making the `HasExplicitReport` lockout in `BuildProgressTracker.Report`
  time-bounded (reuse `BuildProgressReport.StaleThreshold`), so the checklist bridge can resume once
  explicit reporting has gone stale, and rewording CLAUDE.md's progress-reporting section so the 3
  example checkpoints read as a minimum/template rather than a literal fixed script.
- 2026-08-29 ✅ DONE — Added `BuildProgressReport.ExplicitReportIsActive` (true only while
  `HasExplicitReport` is set AND `TimeSinceLastReport < StaleThreshold`, reusing the exact same
  3-minute window the "no progress update in Xm" warning already keys off — no new duration
  invented). `Report()`'s bridge-lockout check now tests `ExplicitReportIsActive` instead of the bare
  permanent `HasExplicitReport` flag, so once a build's explicit reporting goes stale, `BridgeFromChecklist`
  resumes driving the panel from the checklist instead of freezing forever at the last explicit value.
  A fresh explicit call re-arms the lockout immediately, so the common "agent reports reliably" case is
  unaffected. Did not touch explicit-report parsing/delivery (pipe, buildId/step/total parsing, JSON
  snapshot) per the issue's own scope note.
  Separately reworded CLAUDE.md's "Mandatory task planning & explicit progress reporting" section: the
  3 example checkpoints are now explicitly framed as a floor/template to adapt (own real phase count,
  not pinned to 3), and the Phase 3 "Verification & Test Suite" label was reworded to plain
  "Verification" with an explicit note that `desktop/BuildConsole` is out of scope for test manifests,
  so verification there normally means a clean build / real run / manual check, not a manifest.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0
  errors. Wrote a throwaway console harness (`.scratch-verify-1799/`, deleted before finishing, not
  committed) referencing the built `BuildConsole.csproj` to exercise the real `BuildProgressTracker`
  statics end-to-end: (1) explicit report immediately followed by a checklist-bridge call — bridge is a
  no-op, explicit still wins in real time; (2) explicit report, then `LastReportedAtUtc` rewound past
  `StaleThreshold` to simulate genuine staleness — `ExplicitReportIsActive` flips false and the bridge
  call then updates the report from the checklist values; (3) a fresh explicit call after that re-arms
  the lockout — an immediate follow-up bridge call is a no-op again. All 5 assertions passed. Verified
  `agent/1799-q860` (3d0d3222) is an ancestor of `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (OK). `git status --porcelain` clean.
