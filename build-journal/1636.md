# #1636 — Build Queue: mark a build set as Priority (critter + border) and get a persistent, non-navigating completion notification

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 8551345de (feature work), 69e0eadc7 (post-rebase hash pushed to origin/main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — starting: read BuildQueuePanel.xaml.cs header render loop, IssueChompAnimation.cs, ToastEngine.cs/ToastCard.xaml.cs, ChatUrlStore.cs, then build the five pieces per issue body: persisted priority-flag store, right-click Mark-as-Priority toggle, critter+border visual marker, per-build-set terminal-state completion detection, persistent never-auto-dismiss toast with background-tab chat link.
- 2026-08-29 ✅ DONE — shipped all five pieces at commit 69e0eadc7 (origin/main):
  `Services/BuildSetPriorityStore.cs` (new, mirrors ChatUrlStore); a right-click
  "Mark as Priority"/"Unmark Priority" context menu on the build-set header in
  `BuildQueuePanel.xaml.cs`; a warm-accent border + reused "⭐" critter glyph when
  priority; `CheckPriorityBuildSetCompletion` hooked into `RefreshAsync` (deliberately
  walks the FULL unfiltered `_lastItems`, not the render loop's filtered
  `buildSetBuckets`, because the default Active filter drops "done" items and would
  otherwise never observe a finished set) — fires `BuildSetPriorityCompleted` exactly
  once per completion and auto-clears the flag; `ToastEngine.ShowPersistent` +
  `ToastCard`/`ToastHostWindow` changes for a real never-auto-dismiss toast mode; and
  `MainWindow.OpenChatTab`/`OpenChatUrl`/`OpenChatForQueueItem` gained a `selectTab`
  parameter (default true, no existing caller changes behavior) so the toast's chat
  link opens a background tab without touching `EditorTabs.SelectedItem`.
  Logging: new `build-queue-panel.priority` channel.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  errors, only pre-existing warnings untouched by this change.
  Live verification: **not run**. This is a headless environment with no interactive
  Windows desktop session — I cannot right-click a rendered WPF header, watch a real
  build set finish, observe a toast rendering, or confirm the background tab doesn't
  steal focus. Everything above is verified by reading the diff and a clean Release
  build only; the five live checks from the issue body (mark priority + confirm
  critter/border render + persist across restart; let a set finish and confirm exactly
  one non-auto-dismissing toast; click the toast's chat link and confirm the
  previously-focused tab stays focused; confirm the flag auto-clears after firing)
  remain unverified and are explicitly called out as such in the issue comment.
  `git status --porcelain` clean; `69e0eadc7` confirmed an ancestor of `origin/main`
  via `verify-branch-merged.mjs`.
