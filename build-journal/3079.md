# #3079 — script_download_tokens.client_user_id dead column cleanup

- **Status:** ✅ DONE 2026-09-07T17:22:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 43fb1a4e (merged to main)

## Log
- 2026-09-07T17:02:00Z ⏳ IN FLIGHT — confirmed via git history + every writer/reader site that `client_user_id` is genuinely dead (added alongside `customer_id` at creation commit c68d4fe04, never populated by either writer, discarded by its one reader, retention purger treats it as a real addressee but it's always NULL). Dropping it: remove from Drizzle schema + dead code refs, write a manual DROP COLUMN migration for Shane to run (destructive per CLAUDE.md — `lib/db/migrations/manual/` isn't covered by the drizzle-dir `@migration-gate` header, but the Database section's "still go to Shane to run himself" rule applies the same way).
- 2026-09-07T17:22:00Z ✅ DONE — dropped `clientUserId` from the Drizzle schema (`lib/db/src/schema/index.ts`) and all dead code refs (`script-ingestion.ts` select/type, `script-ingestion.test.ts`, `admin-active-directory.ts` per-user delete census, `admin-active-directory-delete.test.ts` mock columns, retention purger's `orColumn` in `modules.ts`). `artifacts/api-server` typecheck passes clean (tsc -b + tsc -p, exit 0). Confirmed local DB has 0 rows in `script_download_tokens` (both total and non-null `client_user_id`), so nothing is lost. Migration `lib/db/migrations/manual/2026-09-07-script-download-tokens-drop-client-user-id-3079.sql` written but **not run** — destructive (`DROP COLUMN`), so it's Shane's to run himself; issue labeled `Shane To-Do`. This unblocks #2983's `customer_id` → `client_user_id` rename across the six affected tables — `script_download_tokens` was the sole table where that target name was already taken. Merged to main at 43fb1a4e; `verify-branch-merged.mjs` OK; `git status --porcelain` clean.
