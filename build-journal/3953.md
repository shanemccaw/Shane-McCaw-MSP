# #3953 — pnpm run build fails: vite configs throw "PORT environment variable is required" for build (not just dev)

- **Status:** ✅ DONE 2026-09-14T02:32:07Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** c065912f7 (bookend, IN FLIGHT), 13dc9785e (real fix)

## Log
- 2026-09-14T02:28:00Z ⏳ IN FLIGHT — Gate the unconditional `PORT` requirement in
  `artifacts/msp-console/vite.config.ts`, `artifacts/portal/vite.config.ts`, and
  `artifacts/msp-website/vite.config.ts` behind `command !== 'build'` using Vite's
  `defineConfig(({ command }) => {...})` function form, per #3953. Already
  implemented and build-verified pre-bookend (see DONE log for detail); writing
  this bookend now to close the gap left by an earlier interrupted turn in this
  session.
- 2026-09-14T02:32:07Z ✅ DONE — All three named vite.config.ts files
  (msp-console, portal, msp-website) fixed: `PORT` is now only required/validated
  when `command !== 'build'` (i.e. `vite dev`/`vite preview`), using Vite's own
  `defineConfig(({ command }) => {...})` function signature. While verifying via
  the exact method the issue specified (`pnpm run build` from repo root, clean
  worktree, no `PORT` env var), discovered the identical unconditional-PORT
  anti-pattern in two more vite configs not named in the issue —
  `artifacts/shane-mccaw-consulting/vite.config.ts` and
  `artifacts/admin-panel/vite.config.ts` — each of which independently broke the
  same root-level `pnpm run build` even after the three named files were fixed.
  Fixed both identically rather than filing as a separate finding, since this
  build was already touching every file with this exact bug and the fix is
  mechanical/low-risk (same pattern already applied 3x). `shane-mccaw-consulting`
  and `admin-panel` (and `msp-website`) also had an unconditional `BASE_PATH`
  throw; defaulted each to its own real canonical base path (read from that
  package's `.replit-artifact/artifact.toml`), matching the pattern
  `portal`/`msp-console` already used — this was necessary for the build to
  actually succeed end-to-end, not scope creep.
  **Verification:** `pnpm run build` from repo root (all 6 workspace packages:
  msp-console, portal, msp-website, shane-mccaw-consulting, admin-panel,
  api-server) — clean worktree, no `PORT`/`BASE_PATH` env vars exported —
  succeeds end to end, exit 0. Confirmed no regression the other direction:
  `vite dev` and `vite preview` (Vite's real `preview()` implementation calls
  `resolveConfig(inlineConfig, "serve", ...)`, i.e. `command === 'serve'`, not
  `'build'`) both still throw the real "PORT environment variable is required"
  error when `PORT` is unset (verified via foreground `timeout`, exit 1 with the
  real stack trace), and both start and bind correctly when `PORT` is set.
  Working tree clean (`git status --porcelain` empty), branch verified merged
  into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before
  writing this DONE. No findings filed as separate issues — the two
  undocumented files were fixed in-session, not left as gaps.
