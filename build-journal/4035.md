# #4035 — Rename basic/enhanced/premium tier vocabulary to foundation/growth/premier

- **Status:** ✅ DONE 2026-09-14T15:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** (filled after commit — see below)

## Log
- 2026-09-14T15:20:00Z ⏳ IN FLIGHT — renaming `MONITORING_TIER_RANK` keys in
  `msp-launch-control.ts` to match the real `services.tier` vocabulary
  (foundation/growth/premier), and migrating `write_action_catalog.min_bundled_tier`
  data to match.
- 2026-09-14T15:35:00Z ✅ DONE —
  1. `MONITORING_TIER_RANK` renamed `basic→foundation`, `enhanced→growth`,
     `premium→premier` (rank order unchanged: 0/1/2) in
     `artifacts/api-server/src/routes/msp-launch-control.ts`.
  2. Ran `lib/db/migrations/manual/4035-min-bundled-tier-real-vocab.sql` against
     local `DATABASE_URL` myself: `UPDATE 28` (basic→foundation), `UPDATE 58`
     (enhanced→growth), `UPDATE 32` (premium→premier) — matches this issue's own
     evidence exactly. Confirmed the 1:1 ordinal mapping is correct by sampling
     real rows before migrating: `basic` rows are low-risk field edits (update
     display name/department/job title...), `enhanced` rows are create/reset/bulk
     actions (create user, reset MFA, bulk CSV import...), `premium` rows are
     destructive/high-risk (delete user, remote wipe, PIM role assignment...) —
     exactly the foundation/growth/premier risk ordering.
  3. Re-verified the scenario: MSP 1 / testbed customer 1 has `services.tier =
     'premier'` (confirmed live). With `MONITORING_TIER_RANK.premier = 2`, every
     one of the 118 previously-mismatched rows (28+58+32, all now
     foundation/growth/premier) has `requiredRank <= 2`, so `resolveTierRank`
     no longer returns `null` for this customer and the tier-axis of
     `computeAvailability` no longer forces `billable_upsell` for any of them.
     I did not re-run the full `computeAvailability` function live (that also
     depends on the independent MSP-capability and license-gate axes, which
     this issue does not touch) — the tier-vocabulary mismatch specifically is
     confirmed resolved for all 118 rows; whether each individual row still
     also needs to clear the capability/license axes is unchanged by this fix.
  4. Found and fixed one other real reference to the old vocabulary against the
     same `services.tier` field: `artifacts/msp-console/src/console/modules/LaunchControl.tsx`
     line 262 (`tierTone`) compared `data.customerTier` against
     `"premium"`/`"enhanced"` for a UI color tone — updated to `"premier"`/`"growth"`.
     Checked `admin-write-actions.ts`, its test, and `catalog-pricing.test.ts` —
     their `basic`/`enhanced`/`premium` references are an unrelated local
     pricing-fixture concept (seat-band ppu), not `services.tier`; left alone.
  5. **Finding filed:** #4074 (parented under #1096 EPIC: Application Core,
     board status AI Batter Up) — `sow-monitoring-addon.ts`'s "pick Enhanced as
     the one designated default quality tier" logic (Git #609) never actually
     matches, because it compares against `tenantTierLabel`, which live data
     shows holds the seat band (Micro/SMB/Mid-Market/Enterprise), not the
     quality-tier name. Pre-existing, unrelated to this issue's `services.tier`
     rename — not fixed here.
  - Verified branch ancestry via `verify-branch-merged.mjs`, `git status --porcelain`
    clean after commit, pushed to `origin/main`.
