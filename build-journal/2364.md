# #2364 — Batter Up rail panel: distinct "needs a home" catch-all (Feature #2355 item 9)

- **Status:** ⏳ IN FLIGHT 2026-09-02T00:00:00Z
- **Scope:** buildconsole (ShaneBuilder)

## Log

- 2026-09-02T00:00:00Z — Confirmed #2357 (grouping by parent Feature) is genuinely DONE on
  `main` (commit `737224863`) before starting, per this issue's own dependency.
- `desktop/ShaneBuilder/MainWindow.BatterUpPanel.cs` — `RenderBatterUpLaneBody`'s existing
  `FeatureNumber ?? 0` grouping (#2357) already sorted the no-parent bucket last and gave it an
  honest "No Feature" label; this build makes it visually distinct and flagged per #2364's real
  scope: warning-tinted header text/count-pill (`Brush.Toast.Warning`), a top/bottom warning
  border on the row, bold text, and the label reworded to "No Feature — needs a home before it
  can be built". No fabricated placeholder Feature — items with no resolvable parent Feature
  still bucket under the real `parent` GraphQL chain #2357 already reads; only the presentation
  changed.
- **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` clean, 0 warnings/0 errors.
