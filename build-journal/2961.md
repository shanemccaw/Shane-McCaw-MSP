# #2961 — New Site v2: Solutions index + 8 topic deep-dive pages (SEO copy only)

- **Status:** ✅ DONE 2026-09-05T23:46:15Z
- **Scope:** platform (marketing site, `artifacts/shane-mccaw-consulting`)
- **Started:** 2026-09-05
- **Commit(s):** 6e6b90833 (bookend IN FLIGHT), 4033393ae (Solutions index + 8 topic pages, rebased
  twice for concurrent `production-base-2026-09-05` pushes from #2955/#2962/the retainer-payment
  commit) — landed on `origin/production-base-2026-09-05` at `4033393ae`, confirmed via
  `git merge-base --is-ancestor`

## Log
- 2026-09-05T23:42:17Z ⏳ IN FLIGHT — Read #2961 body + dispatch comment (2026-09-05T22:39:21Z Posted,
  `--blocked-by 2954`; a follow-up comment confirmed #2954 landed on `production-base-2026-09-05` at
  `4ad0df12a` and cleared the dependency edge, so proceeding). Reset this worktree's branch onto
  `origin/production-base-2026-09-05` (its HEAD, `cf35b5337`, was `agent/2961-q1712`'s starting
  point, not `main`). Read `Design/fractional_architecture/README.md` §7–§8, `Solutions.dc.html`,
  and `Solution - Copilot AI.dc.html` — confirmed all 8 `Solution - *.dc.html` exports share one
  byte-identical `TOPICS` data array (diffed Copilot vs Security: no difference outside the active
  `id`), so one file read supplied every topic's real copy, metrics and project cards (this is
  itself the "one shared template" the issue asks for, already true in the design source).
  `src/pages/Solutions.tsx` and `src/pages/solutions/SolutionTopicPage.tsx` already existed but were
  dead code (old `/projects/:slug` personalization system, unrouted in `App.tsx`, confirmed via
  grep — nothing imports `SolutionTopicPage.tsx` as a component) — rebuilding them in place per the
  README's "this is a design pass on it" framing, matching the plain-Tailwind-arbitrary-value
  convention already established by the rebuilt `Resources.tsx`/`About.tsx`/`Header.tsx`, not the
  old shadcn/design-system components. `Header.tsx`/`Footer.tsx` (#2954) already link every
  `/solutions/*` path — this build wires the routes those links have been 404ing against
  (also the open finding on #2963). About to build the data layer, shared sections, both pages, and
  wire the routes.
- 2026-09-05T23:46:15Z ✅ DONE — Built:
  `src/data/solutionsDeepDive.ts` (the shared `SOLUTION_TOPICS` array — slug, label, accent hex +
  lucide icon exactly per the dispatch's 8 pairs, index-card copy, hero copy, 3-row findings panel,
  two-paths timeline, 3-4 project cards per topic, all copy transcribed verbatim from the `.dc.html`
  sources — sizes genuinely vary: Power Platform has 3 projects, every other topic has 4);
  `src/components/solutions/HowWorkingWithShaneGoes.tsx` and `SolutionsClosePanel.tsx` (the two
  sections shared byte-for-byte between the index and all 8 topic pages, extracted once instead of
  duplicated 9 times); `src/pages/Solutions.tsx` (hero + 8-card grid, Copilot card in the
  violet/blue gradient highlight per spec); `src/pages/solutions/SolutionTopicPage.tsx` (one
  template driven entirely by `SOLUTION_TOPICS` — hero with gradient h1 + decorative topic-accent
  Lucide glyph for Copilot vs solid-accent for the other 7, the "What Shane's review typically
  finds" panel, Two Paths red/teal timeline, The Work by name project cards, deep-dive switcher
  pills, shared how-it-goes + close panel). Wired both routes into `App.tsx`
  (`/solutions`, `/solutions/:slug`) — merged cleanly alongside #2955's/#2962's concurrent
  `App.tsx` changes on two separate rebases. `tsc --noEmit` clean; `vite build`
  (PORT/BASE_PATH set manually) succeeds, re-verified after each rebase. Live-verified with a real
  Playwright (`playwright-core` + msedge channel, per the `site-playwright-verification-recipe`
  memory) run against the actual dev server on all 9 routes — screenshots visually confirmed exact
  fidelity to the `.dc.html` design (hero, metric bars, timeline dots, project cards, active-pill
  switcher, close panel, header/footer chrome) and case-insensitive text-content assertions passed
  on `/solutions`, `/solutions/copilot`, `/solutions/power-platform`, `/solutions/health`.
  No test manifest: this app's page content is copy/marketing with no new API/UI *behavior* (no new
  endpoint, no new interactive state) — matches the CLAUDE.md test-manifest convention's own scope
  (endpoints, flows, integration actions) and the precedent set by #2958's About page rebuild.
  No `logger.child({ channel: "growth.marketing" })` binding added: this app
  (`artifacts/shane-mccaw-consulting`) is a pure client-side Vite/React app with no server-side code
  of its own (confirmed — no `server/` dir, no API routes here), so there is no place a Node logger
  binding is warranted for a static-content page addition; noting this explicitly rather than
  skipping the instruction silently.
  Left `.env.local` alone — already present and untracked when this session started, not created or
  touched by this build (same observation #2958's bookend made). Also left `scripts/report-progress.mjs`
  and `scripts/dev-server/config.mjs` untouched as untracked scratch — copied in from `origin/main`
  purely to invoke `shaneapp://reportProgress` locally (neither file exists on
  `production-base-2026-09-05`), not part of this issue's deliverable, deleted before finishing.
  This resolves the `/solutions/*` portion of #2963's 404 finding (the `/contact`/`/assessment`
  gaps in that same finding were independently resolved by #2962/#2955 in the interim; the `/#tiers`
  anchor gap was resolved by #2955 too) — commented on #2963 to record that.
