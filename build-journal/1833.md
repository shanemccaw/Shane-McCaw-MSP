# #1833 — Build Queue search debounce + broad-filter placeholder

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 4520b926dad0f551603bb4bfade9b6e10e642610

## Log
- 2026-08-30 ⏳ IN FLIGHT — reading QueueSearchBox_TextChanged, RenderQueue, ApplyFilter in Controls/BuildQueuePanel.xaml.cs; about to add DispatcherTimer debounce and a placeholder state for the broad "All"-style filter with empty search, per #1833.
- 2026-08-30 ✅ DONE — `Controls/BuildQueuePanel.xaml.cs`: `QueueSearchBox_TextChanged` now resets/starts a 250ms `DispatcherTimer` per keystroke instead of calling `RenderQueue` directly; only the settled query fires the real render. `RenderQueue` gained an early `isBroadUnsearchedView` check (`!searching && _filter == "All" && _buildSetFilter == null`) that skips the whole swimlane/DAG + card-construction pass and shows a new `QueueBroadFilterPlaceholderText` ("Type a build number to search.") instead — added alongside the existing `QueueEmptyText` in `BuildQueuePanel.xaml`. Narrow filters (Running/Queued/Crashed/Parked/External/Done/Canceled) are untouched — they never hit the `_filter == "All"` branch, so they still render normally with empty search. Typing a real query while on "All" clears the placeholder and renders real (debounced) results as before. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors. Merged to origin/main, verified via `verify-branch-merged.mjs` (OK). No test manifest — desktop/BuildConsole is out of scope.
