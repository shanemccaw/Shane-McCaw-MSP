# #3671 — MSP Console: /api/msp/audit has no customerId filter

- **Status:** ✅ DONE 2026-09-11T18:42:02Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 4abe6dcb6 (pushed to `main`; local pre-rebase form was 00beae27e)

## Log
- 2026-09-11T18:32:58Z ⏳ IN FLIGHT — add a `customerId` query filter to `GET /api/msp/audit` (artifacts/api-server/src/routes/msp-audit-log.ts). Column already exists on `msp_audit_logs`; additive route work only, no schema change needed.
- 2026-09-11T18:42:02Z ✅ DONE — Added an optional `?customerId=` filter to `GET /api/msp/audit`, AND-combined with the route's existing MSP-scoping conditions (an `MSPAdmin` supplying it still only ever sees their own MSP's rows narrowed further to that customer; `PlatformAdmin` can combine it with `?mspId=`). No schema change — `mspAuditLogsTable.customerId` already existed (`lib/db/src/schema/msp.ts:937`); this was purely additive route work per the issue body. Also corrected `docs/msp-console/audit-log-msp-console-contract-pack.md` §1.2, which had explicitly documented the filter's absence — struck the stale claim and noted the fix inline (full contract-pack regeneration stays a separate step per the fixed #1485/#1571-style order; this was a targeted correction of the one now-false sentence, not a regen pass).
  Verification: `pnpm exec tsc --noEmit -p .` in `artifacts/api-server` — clean, no errors. Local dev api-server was not running (`scripts/dev-server/status.mjs` showed the process DEAD on port 8080, a real checked state, not assumed) so live HTTP round-trip wasn't possible; instead verified the real effect directly against local Postgres (`DATABASE_URL`): `msp_id = 1` has 231 rows total vs 2 rows when also filtering `customer_id = 3` — real data, same AND-combined WHERE shape the route now builds via Drizzle's `and(...conditions)`.
  No test manifest — `artifacts/msp-console` has no audit page yet (scaffolding only per the contract pack), and test-manifest scope is limited to `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting`.
  No new finding filed — no bug or gap discovered beyond what #3671 already described.
  Branch `agent/3671-q2342` rebased onto `origin/main` and pushed directly to `main` (fast-forward, `1e5d96abe..4abe6dcb6`). `node scripts/dev-server/verify-branch-merged.mjs` — exit 0, confirmed ancestor of `origin/main`. `git status --porcelain` clean.
