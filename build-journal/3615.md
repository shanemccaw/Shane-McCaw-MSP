# #3615 — Queue status readout: reorder to Queue → Up Next → Active → Blocked → Verifying → Total

- **Status:** ✅ DONE 2026-09-11T02:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 32d5b8ceb80d67a5049d6bfd393b333bb629cd03

## Log
- 2026-09-10T22:30:00Z ⏳ IN FLIGHT — merging QueueActiveSlotsText into QueueStatusCountsText, new order Queue → Up Next → Active → Blocked → Verifying → Total, removing now-redundant XAML element after confirming no other consumers.
- 2026-09-11T02:20:00Z ✅ DONE — grep confirmed `QueueActiveSlotsText` had exactly one XAML element and one code-behind assignment, no other real consumers; merged `active` into `QueueStatusCountsText`'s single string in the exact order Queue → Up Next → Active → Blocked → Verifying → Total (renamed "In queue"→"Queue"), removed the now-redundant `QueueActiveSlotsText` `TextBlock` from `BuildQueuePanel.xaml` and its assignment from `BuildQueuePanel.xaml.cs`. Preserved the provisional-blocked `"*"` suffix + muted foreground + tooltip logic, the live/provisional `QueueStatusBorder` tooltip text, and the click-to-open `QueueNextPopup` handler unchanged. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors (6 pre-existing unrelated warnings in `FocusModeBar.xaml.cs`). Rebased onto `origin/main` and pushed directly — commit `32d5b8ceb` is on `main`; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
