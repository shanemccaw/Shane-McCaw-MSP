# #4290 — MyArchitect: wire "Replay last run" + live-verify Script Library loads real tenant-scoped data

- **Status:** ✅ DONE 2026-09-15T23:45:35Z
- **Scope:** platform (shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** a66ae466eb88aec63020c1cf1f040b6d447c07d6 (shanemccaw/MyArchitect main)

## Log
- 2026-09-15T23:23:00Z ⏳ IN FLIGHT — blockers #4278/#4284 confirmed closed; cloned shanemccaw/MyArchitect, located ChangeRequestReplayService (#3459) and Script Library (BuildScriptLibraryRowsAsync) — replay service had no AuthToken plumbing at all, and no UI ever called it.
- 2026-09-15T23:45:35Z ✅ DONE — Wired `ChangeRequestReplayService.AuthToken` (pushed alongside every other auth-gated service in `ApplyAuthStateAsync`) and added a real "Replay last run" `WorkspaceAction` to the Script Library record workspace (`OpenScriptLibraryRecord`) that calls `GetLastRunAsync`, parses the real `proposedPayload`, and re-opens the record pre-filled — reports outcome into the Console pane. `dotnet build` clean, 0 warnings/errors, both before and after a rebase onto a concurrent push to `origin/main`.

  Live-verified end-to-end against the real local dev api-server + local Postgres, using a synthetic `zz-test-4290-replay@mccawsoft2.onmicrosoft.com` MSPOperator (msp_id=1) created/deleted for this session only: logged in via `POST /api/auth/login`, confirmed `GET /api/msp/1/launch-control/actions?customerId=1` returns real, non-fixture catalog rows with `availability:"included"` (Script Library's own data source — #3460's remaining ask), then executed catalog action 95 (`Disable/enable sign-in`) against the sanctioned destructive-write test target `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com` (docs/testbed-destructive-write-test-user-2840.md) — real 204 Graph write, real CR raised (`CR-2026-1986`). Restored the test account to `accountEnabled:true` afterward via the same DEV write app registration (verified after Entra replication lag cleared).

  **Real finding, filed and parented, not fixed in this repo:** `GET /api/msp/change-requests` showed the just-raised CR's `catalogItemId` as `null` — `msp-launch-control.ts`'s call into `raiseChangeRequestForLaunchControlExecution` never passes `catalogActionId` through, so `ChangeRequestReplayService`'s filter-by-`catalogItemId` can never match a real row. Filed **shanemccaw/Shane-McCaw-MSP#4312**, labeled `bug`, milestone "v1.1 - Monitoring & Launch Control", parented as a sub-issue of #3454 per this issue's standing rule, board status set to "AI Batter Up". Out of scope to fix here since the bug is in `Shane-McCaw-MSP`'s `artifacts/api-server`, not `shanemccaw/MyArchitect`.

  Cleanup: deleted the synthetic operator user + its refresh/session rows from the local DB; no other rows or files left behind. `desktop/MyArchitect/{bin,obj}` in the `Shane-McCaw-MSP` main checkout is pre-existing build-output debris from before this session (predates the #4305 extraction commit) — not touched, not mine, left alone.
