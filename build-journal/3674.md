# #3674 — App Registration expiry alert email links to /crm/clients/:id, now a generic redirect

- **Status:** ✅ DONE 2026-09-12T00:15:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 9b82779ef (bookend only — no product code changed; see log)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — investigating `admin-clients.ts:722` per the dispatched prompt, to fix the App Registration expiry alert email's link-building logic.
- 2026-09-12T00:15:00Z ✅ DONE — investigation only, no code changed. The line the
  issue cites (`admin-clients.ts:722`, the `adminPanelUrl` builder inside
  `PATCH /admin/clients/:id/app-registration`) no longer exists. Commit
  `b23ba7a6d` ("Remove legacy Clients/CRM suite from Admin Panel (Git #3424)",
  landed Sep 11 2026, the day *before* #3674 was filed) deleted that whole route
  — including the `getSecretMetadata`/`appRegExpiryAlertEmail`/`sendEmail` block
  that sent the alert — not just the link inside it. Confirmed via
  `git show b23ba7a6d -- artifacts/api-server/src/routes/admin-clients.ts`.
  `appRegExpiryAlertEmail` (lib/mailer.ts:795) is now dead code with zero call
  sites anywhere in the repo (`rg appRegExpiryAlertEmail` across
  artifacts/ + lib/ matches only its own definition). So there is no live email
  and no live link to fix — #3674's premise was already stale at filing time.
  `ai-next-best-actions.ts` untouched per the correction comment/latest
  dispatch (out of scope). Posted the full finding as a comment on #3674.
  Reinstating the expiry-check-and-alert trigger against the new Azure
  Tenant Credentials surface (`admin-azure-credentials.ts`, which already has
  `EXPIRY_WARN_DAYS`/`expiring-summary` but no email send) is a real product
  decision — whether/how to re-wire alerting at all post-#3424 — left to Shane,
  not invented here.
