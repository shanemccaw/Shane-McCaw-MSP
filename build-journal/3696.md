# Build Journal — #3696

- **Status:** ✅ DONE 2026-09-12T00:15:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Investigated `msp-settings-approve-purchases-rung.live-db.test.ts` for the `users_role_scope_check` violation described in the issue. Found the fix was already landed on `main` by #3649 (commit `19d64c9e7`), which is an ancestor of this worktree's HEAD — `insertUser()` already sets a real seeded `tenantId` for Customer/Free rows. No code change needed here; verifying the suite still passes clean.
- 2026-09-12T00:15:00Z — ✅ DONE — No code change made; #3649 (commit `19d64c9e7`) already fixed the exact issue described in #3696. Verified against local Postgres: `pnpm --filter @workspace/api-server exec vitest run msp-settings` → 5 test files / 25 tests, all pass. Branch merged into `origin/main` (verify-branch-merged.mjs OK, commit `79cc8f88`). Working tree clean.
