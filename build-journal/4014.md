# #4014 — Offers & SOWs: wire the newer contract-pack screen (SOWs.dc.html)

- **Status:** ✅ DONE 2026-09-14T04:40:57Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 48dcd416a (IN FLIGHT bookend), e71b4c9ec (feature work, post-rebase hash on origin/main)

## Log
- 2026-09-14T04:39:06Z ⏳ IN FLIGHT — confirmed #2643's earlier build (Sales.tsx, MSP-wide `/ops/sales`) already wires offer-accept + a per-offer SOW peek, but has no dedicated per-tenant "Offers & SOWs" page, no whole-book SOW list for one customer, no authenticated sign action, and no clickwrap read/write. This is genuinely new scope layered on top, not a duplicate — building `console/modules/OffersAndSows.tsx` + `api/offers-and-sows-api.ts`, mounted per tenant under Commercial per README screen 66.
- 2026-09-14T04:40:57Z ✅ DONE — Shipped `artifacts/msp-console/src/console/modules/OffersAndSows.tsx` (3 tabs: Statements of work, Offers to accept, Customer agreement) and `artifacts/msp-console/src/api/offers-and-sows-api.ts` (customer-scoped hooks against the real `msp-sow.ts`/`msp-sales-offers.ts` routes), mounted at `/tenants/:id/offers-sows` in the Commercial group (`console/nav.ts`, `ConsoleShell.tsx`). Added `customerAgreementSnapshotText` to the shared `MspSow` type in `sales-api.ts` (additive). Wires: whole-book SOW list scoped to the tenant (`GET /msp/sows?customerId=`), SOW detail/sign/charge/expire/document, customer-scoped offers list (`GET /msp/:mspId/sales-offers?customerId=`) + accept, and the clickwrap GET/POST pair — none of which #2643's MSP-wide Sales module reached. The sign action captures a real drawn signature via an inline canvas `SignaturePad` (base64 PNG via `toDataURL`), not fabricated signature data. No fixture/hardcoded data; no new findings beyond the three already filed by the #2641 contract-pack pass (#2725, #2730, #2731), which this build surfaces honestly in the UI copy rather than re-filing. Verification: `pnpm run typecheck` and `pnpm run build` both pass clean in `artifacts/msp-console`; test-manifests are out of scope for `artifacts/msp-console` per CLAUDE.md. Did not live-verify in a running browser — the local dev server serves the merged main checkout, not this worktree, until BuildConsole's own merge-back completes. `node scripts/dev-server/verify-branch-merged.mjs` confirms `e71b4c9e` is an ancestor of `origin/main`; working tree left clean.
