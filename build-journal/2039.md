# #2039 — Phase 2 write packs: Device/Intune hardening (devices:*) — 8 uncovered checks

- **Status:** ✅ DONE 2026-09-12T12:55:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** `7449afb18` (IN FLIGHT bookend), `4cc0d99db`→pushed as `ddeeff0b5` (migration)

## Log
- 2026-09-12T08:05:00Z ⏳ IN FLIGHT — fan-out from #1925 Phase 2. Read #2039 body+comment,
  #1925 (definition of done), #1909 (testbed lacks Intune Plan 1 licensing). Confirmed live
  DB state: 12 `devices:*` monitor_checks total, 4 already `we_can_run`-mapped
  (app-protection-coverage, compliance-policy-coverage, compliant-vs-noncompliant,
  unassigned-intune-profiles via existing `device-compliance-v1` pack). The 8 uncovered are
  the 7 named in the issue body (update-rings-config, bitlocker-key-escrow,
  os-patch-compliance, encryption-status, enrollment-status, kfm-configuration,
  autopilot-coverage) **plus `devices:stale-duplicate-records`**, which the issue body did not
  name but live DB confirms is the 8th uncovered check. Proceeding to author write packs per
  #1925's Phase 2 methodology (real Graph docs fetched this session, additive idempotent
  migration, planOnly dry-run against tenant 1).
- 2026-09-12T12:55:00Z ✅ DONE — Authored `lib/db/migrations/manual/2026-09-12-device-hardening-write-pack-2039.sql`
  (commit `ddeeff0b5` on `main`), run against the local `DATABASE_URL` in-session (output
  confirmed all 8 new `baseline_action_templates` rows + `device-hardening-v1` config pack +
  10 `config_pack_templates` rows inserted; re-queried devices:* mappings afterward to confirm
  against live DB rather than trust the aggregate count, since sibling fan-out builds
  #2037/#2038/#2040 were writing to the same shared local Postgres concurrently).

  **6 of the 8 uncovered checks got real write automation**, each against Microsoft Graph docs
  fetched live this session (URLs in the migration file header):
  - `devices:update-rings-config` — create `windowsUpdateForBusinessConfiguration` (v1.0) +
    assign, reusing the existing `action.update-config-profile-assignment` template (genuine
    reuse — a Windows Update ring is a deviceConfiguration row like any other).
  - `devices:encryption-status` + `devices:bitlocker-key-escrow` — ONE
    `windows10EndpointProtectionConfiguration` profile (BETA endpoint — `bitLockerSystemDrivePolicy`
    recovery-key-escrow settings are beta-only, confirmed absent from the v1.0 property table)
    satisfies both checks via two distinct assign template_ids (dup-template-id-per-pack
    constraint, same technique #1925's governance-groups pack used for its visibility PATCH).
  - `devices:os-patch-compliance` — create `windows10CompliancePolicy` with `osMinimumVersion`
    + assign, reusing `action.update-compliance-policy-assignment`.
  - `devices:autopilot-coverage` — create `azureADWindowsAutopilotDeploymentProfile` (BETA —
    this resource has no v1.0 create op) + assign via the profile's own `assignments`
    relationship (not the per-device-hash `assign` action, which takes deviceIds not a group).
  - `devices:stale-duplicate-records` — `DELETE /devices/{id}` (v1.0) targeting the
    already-flagged stale/duplicate record.

  Risk classification per #2039's own "any policy that could brick enrollment is plan-only"
  caveat: `action.create-bitlocker-protection-profile` (forces encryption tenant-wide) and
  `action.create-autopilot-deployment-profile` (OOBE/enrollment itself) and
  `action.delete-stale-device-record` (destructive directory delete) are all
  `requires_verification_gate = true`; the ring and compliance-policy creates are not gated
  (same risk tier as the existing non-gated device-compliance-v1 templates).

  **2 of the 8 deliberately left with NO template authored** (reasons + evidence in the
  migration file header, and see filed finding below):
  - `devices:enrollment-status` — reporting-only metric (a raw enrollment count/breakdown);
    no single Graph write remediates a count.
  - `devices:kfm-configuration` — genuine architectural gap, not a workaround-able ambiguity:
    real KFM needs `groupPolicyConfigurations` + `definitionValues` (both beta), and the
    `definitionValues` step needs a `definitionId` resolved from a live filtered GET against
    `groupPolicyDefinitions` — a "look up an id from a read, then write with it" step the
    `baseline_action_templates` model (fixed endpoint+body, `{{var}}` substitution only) has
    no way to express. Authoring just the container create would satisfy the check's own
    name-match read logic with zero real device configuration — worse than the gap. **Filed
    as #3800** (parented under Feature #2494, board status set to AI Batter Up), since this
    blocks real automation for any Administrative-Templates-based check, not just KFM.

  **Verification (real, in-session):**
  1. `resolveBaselineTemplateRequest` — all 8 new templates resolved fully against
     representative payloads: endpoint bound with no `{{}}`, valid JSON body of the right
     shape, `missingVariables: []`.
  2. `buildConfigPackDryRun('device-hardening-v1', 1)` against the real connected testbed
     tenant (mccawsoft2, `tenants.id=1`) — materialized the full 10-step ordered plan
     correctly (create→assign chains preserved via `depends_on_override`), gate correctly
     placed on the first gated template in topological order
     (`action.create-bitlocker-protection-profile`), with the other two gated templates
     correctly coalesced onto that same gate (confirmed via the orchestrator's own log line).
     `executable: false` with the expected operator-input variables reported missing
     (ringName, profileId, groupId, profileName, policyName, minimumOsVersion, policyId,
     deviceObjectId) — the correct, honest planOnly output for a set of pure create/delete
     actions (nothing to read pre-execution).
  3. **#1909 licensing-gap check — direct live `graphFetchForTenant` reads against tenant 1**,
     repeated twice for consistency: `GET /deviceManagement/deviceConfigurations` and
     `GET /deviceManagement/deviceCompliancePolicies` both returned a consistent, repeatable
     `503 Service Unavailable` (IIS-level HTML, not a Graph JSON error) both attempts; the
     BETA `GET .../windowsAutopilotDeploymentProfiles` returned `401` with a Forbidden body
     from `proxy.msua01.manage.microsoft.com/DeviceEnrollmentFE/...` — the same
     `intune-legacy-devicefe-401` signature #1847/#1909 already documented as this tenant's
     "Intune not licensed" fingerprint. By contrast `GET /devices?$top=1` (a pure Entra
     Directory object, no Intune dependency) returned a clean `200` with real live data.

     **Honest conclusion on #1909's caveat: 5 of the 6 newly-automated checks
     (update-rings-config, encryption-status, bitlocker-key-escrow, os-patch-compliance,
     autopilot-coverage — everything under `/deviceManagement/*`) have their real live-tenant
     dry-run/read blocked by the same Intune Plan 1 licensing gap #1909 documented, confirmed
     with fresh live evidence this session, not just cited from #1909. Only
     `devices:stale-duplicate-records` (`/devices`, plain Entra Directory, no Intune
     dependency) is genuinely fully testable against real tenant state today.** This does not
     block authoring — per #2039's own instruction, all 8 were authored/doc-verified
     regardless — it only limits how much of today's planOnly proof could exercise real
     `deviceManagement` state, exactly as #1909 predicted. Nothing here needs Shane's
     decision beyond what #1909 already raised; noted so it isn't lost when #1909 resolves.

  Migration line appended to #1630. `git status --porcelain` clean (scratch verification
  scripts under `artifacts/api-server/scratch-verify-*.mts` were deleted before finishing,
  never committed). Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
