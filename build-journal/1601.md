# #1601 — Account Security: wire change-password from the portal page

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 7e5e71632, 1622eb628, 8688191c0, 0bd26ec3a, 23e5c7877

## Log
- 2026-08-29 ⏳ IN FLIGHT — wiring POST /auth/change-password from portal-v2-account-security.tsx: draw all four error states (missing fields, too-short, no-password-set, incorrect-password) plus success with revokedOtherSessions count, per the account-security-contract-pack.md.
- 2026-08-29 ✅ DONE — Wired `useAccountSecurityLive.changePassword()` (7e5e71632) to `POST /api/auth/change-password`, distinguishing the route's four real error states (`ChangePasswordOutcome`, matched on status + the route's own literal message text: `auth.ts:826/831/838/843`) plus a success outcome carrying the real `revokedOtherSessions` count. Added `changePasswordErrorText` (accountSecurityModel.ts) so the UI renders the server's own literal text, never invented copy. Replaced the inert "Change password" button with a real inline form (portal-v2-account-security.tsx, 8688191c0) showing all four error states plus a success line with the real revoked-session count. Typecheck clean (no new errors; two pre-existing unrelated errors in SowScreen.tsx/PortalV2Shell.tsx untouched by this work). Test manifest `test-manifests/portal/account-settings.json` updated in place (discover-before-create match) with a self-contained `change-password-1601` apiTests block (missing-fields, too-short, incorrect-password, and a real success+cleanup round-trip that reverts the testbed account's password back to `TEST_PORTAL_PASSWORD`) and a `change-password-1601-ui` uiSteps block driving the real DOM form. Ran both via `shaneapp://runTest`: apiTests 7/7 passed (real revokedOtherSessions counts observed: 183 then 1 on cleanup). uiSteps: 9/12 passed — the 3 failures are the manifest's own documented persisted-session login-form artifact (this WebView2 already carried a session, so #email/#password never mounted), not a page defect; the actual feature assertion (CP-UI4, the real "Current password is incorrect" text rendering in the DOM from the real 401) passed. The fourth error state ("No password set for this account.", `auth.ts:838`) has no test coverage — no passwordless (SSO-only) testbed account exists in this DB; verified by code reading only, documented in the manifest's own notes. No schema change, no new logging channel (reused the existing `auth` channel the route itself already binds server-side; a client-side beacon via `reportClientEvent` fires only on a genuinely unexpected response shape, not on the four documented validation cases). Verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.
