# #1438 — PII Governance nav badge shows hardcoded '3 exposed' instead of the real live count

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** e4a6ec419 (live badge), 5643c81d5 (manifest verification hash + note)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Investigated. The issue's premise is stale: the PII
  Governance page no longer reads `piiModel.ts`'s `piiExposedCount` (the fixture
  `piiData.ts` exposure filter) at all — it was migrated to real data in a prior
  session, and the real backend (`portal-pii-governance.ts`) has no
  Public/External "exposure" concept, only four aggregate Purview
  sensitivity-label/DLP severity signals. Wiring the nav badge straight to
  `piiModel.ts` would put fixture data back in front of a customer, which the
  repo's hard rule forbids. Plan: add a live badge sourced from
  `GET /api/portal/pii-governance`'s real High-severity finding count (the
  live page's own "personal-data exposure to act on" stat), mirroring the
  existing `useHoldBadge` cached-hook pattern; omit the badge entirely when
  that count is zero or nothing has been collected yet.
- 2026-08-27 ✅ DONE — Added `piiExposedBadge` (piiGovernanceWire.ts, pure) and
  `usePiiExposedBadge` (module-scope-cached hook, mirrors `useHoldBadge`
  exactly, reports fetch failures via `reportClientEvent` on the route's own
  `tenant.portal` channel). `portalV2Nav.ts`'s PII Governance row now carries
  `badge: "pii-exposed"`, resolved live by `PortalV2Shell.tsx` the same way
  `"holds"` is — omitted entirely (never "0 exposed") when the tenant has no
  High-severity findings or hasn't scanned yet. Left `piiModel.ts`/`piiData.ts`
  untouched but corrected their now-stale doc comments (they describe the
  design fixture only; nothing in production code reads them any more).
  Extended `test-manifests/portal/portal-v2-pii.json` with UI13 (sessionTag
  `1438-badge`) asserting the badge is absent for the testbed tenant's real
  zero-High-severity state. Verification: `piiGovernanceWire.test.ts`'s 3 new
  tests pass, full msp-portal suite 1808/1808 pass, `tsc --noEmit` clean (the
  2 remaining errors — `SowScreen.tsx`, `KnowledgeBaseOverlay`'s `onCreate` —
  pre-exist on `origin/main`, confirmed via `git show`, unrelated to this
  change). `shaneapp://runTest?onlyTag=1438-badge` returned "no steps matched"
  because it runs against the deployed build, which does not yet carry this
  commit — the same documented environmental gap as this manifest's existing
  notes, not a new defect; UI13 needs a real run once deployed. Commits
  e4a6ec419 and 5643c81d5, both pushed to `main`.
