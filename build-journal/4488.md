# #4488 — No non-Premier RBAC test account exists for the Change Control add-on gate

- **Status:** ✅ DONE 2026-09-17T05:35:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 942715fef (implementation), 618c2a4f6 (manifest verification note) — both on `main`

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — implementing option 1 from the issue: add a genuinely non-Premier synthetic tenant + RBAC test account (`shanemccaw+customer-addongate@outlook.com`) to `scripts/db/reset-rbac-test-accounts.mjs`, then wire `test-manifests/portal/change-control-addon-gate.json` to it and live-verify the 402.
- 2026-09-17T05:35:00Z ✅ DONE — `scripts/db/reset-rbac-test-accounts.mjs` now resolves/creates a stable synthetic non-Premier tenant (`tenant_id` marker `zz-test-4488-nonpremier-tenant`, idempotent across runs, aborts if it ever picks up a real active `monitoring_tier` client_services row) and binds a new `customer-addongate` Customer rung to it. Ran the script for real against local Postgres: created tenant id=2191 and user id=4691 (`shanemccaw+customer-addongate@outlook.com`). Live-verified via direct API: `POST /api/auth/login` + `GET /api/portal/change-control` returned 402 `ADD_ON_REQUIRED` for the new account, while the existing Premier-tenant `shanemccaw+customer@outlook.com` still returned 200 `scoped:true` — the two accounts now genuinely diverge on entitlement. Updated `test-manifests/portal/change-control-addon-gate.json`'s `loginAs` to a new distinct label and its SETUP apiTest to the new `TEST_RBAC_CUSTOMER_ADDONGATE_EMAIL/_PASSWORD` vars. Pushed to `main`, merged into the dev-server checkout, and re-ran `shaneapp://runTest` (onlyTag=4452-addon-gate) against the merged commit: SETUP/G1/G2 apiTests all PASSED for real; uiSteps' `goto` passed but GU0 failed (30s timeout) because no BuildConsole Settings > Test Environment > User Accounts login profile exists yet for the new `loginAs` label — filed as a Shane To-Do in the manifest's own notes (not a code gap; that list is manually maintained per-machine, separate from the auto-written TestEnvironmentVariables the apiTests already use). Working tree clean, branch verified merged into origin/main.
