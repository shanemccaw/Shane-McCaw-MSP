# Build Journal — Issue #3588

- **Status:** ✅ DONE 2026-09-12T00:10:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Starting: apply the real `writeAtomic` (temp-file + rename) pattern to `restart-hold.mjs`'s `writeHold()`, same fix already applied to `queue.mjs`/`buildset.mjs`/`lock.mjs` (Git #3068 class), to stop a torn write from making an active hold look expired via `isExpired(null)`.
- 2026-09-12T00:10:00Z — ✅ DONE — `writeHold()` in `scripts/dev-server/restart-hold.mjs` now writes to a pid+random temp file and `renameSync`s into place (atomic on same volume), same as `queue.mjs`/`buildset.mjs`. Verified: `node -c` syntax check, and a live `acquire`/`status --json`/`release` round-trip via the CLI, all passed. Commit: `9ab33abf2`. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; `git status --porcelain` clean.
