# Build Journal — Issue #2856

- **Status:** ⏳ IN FLIGHT 2026-09-06T14:10:00Z
- **Scope:** platform (artifacts/api-server/src/lib/graph-write-permissions.ts)

## Log
- 2026-09-06T14:10:00Z ⏳ IN FLIGHT — Shane's decision (Option 2 from #2856's own list): request
  `Group.ReadWrite.All`. This unblocks 3 of governance-groups-v1's 4 steps
  (action.add-group-owner, action.set-group-visibility-private, action.set-team-visibility-private).
  `action.configure-group-expiration-policy` stays permanently refused — only
  `Directory.ReadWrite.All` would unlock it and that is explicitly not being requested. Updating
  the two rules' `grantRecommended`/`notRequestedReason` fields, `DERIVED_WRITE_APP_PERMISSIONS`
  consumers/tests that assert `Group.ReadWrite.All` is never requested, and folding the grant
  delta into #1918's PROD plan.
