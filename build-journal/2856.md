# Build Journal — Issue #2856

- **Status:** ✅ DONE 2026-09-06T14:52:00Z
- **Scope:** platform (artifacts/api-server/src/lib/graph-write-permissions.ts)

## Log
- 2026-09-06T14:10:00Z ⏳ IN FLIGHT — Shane's decision (Option 2 from #2856's own list): request
  `Group.ReadWrite.All`. This unblocks 3 of governance-groups-v1's 4 steps
  (action.add-group-owner, action.set-group-visibility-private, action.set-team-visibility-private).
  `action.configure-group-expiration-policy` stays permanently refused — only
  `Directory.ReadWrite.All` would unlock it and that is explicitly not being requested. Updating
  the two rules' `grantRecommended`/`notRequestedReason` fields, `DERIVED_WRITE_APP_PERMISSIONS`
  consumers/tests that assert `Group.ReadWrite.All` is never requested, and folding the grant
  delta into #1918's PROD plan.
- 2026-09-06T14:52:00Z ✅ DONE — Flipped `grantRecommended`/justification/`notRequestedReason` on
  the `action.add-group-owner` and `action.set-group-visibility-private`/`action.set-team-visibility-private`
  rules in `graph-write-permissions.ts`; `Group.ReadWrite.All` now REQUESTED
  (`DERIVED_WRITE_APP_PERMISSIONS` 23 → 24). `action.configure-group-expiration-policy` untouched —
  stays `grantRecommended: false` with its real `Directory.ReadWrite.All` reason, still visibly
  refused per #1901's pattern, not silently dropped from the pack.
  **Real finding caught mid-build, fixed in the same commit:** `action.delete-group` needs the
  same `Group.ReadWrite.All` scope, but must stay refused (it's the destructive operation the
  whole objection is about) — without a fix, its refusal would have silently evaporated the
  instant `Group.ReadWrite.All` became genuinely held for the owner/visibility steps (the existing
  #2858 test asserting it stays non-runnable would have started failing). Added a new
  `alwaysRefused?: boolean` field to `WritePermissionRule`, threaded it through
  `requiredPermissionsForWrite`'s `notRequested` computation, and set it on the delete-group rule.
  Filed as its own finding, not left implicit: **#3028** (sub-issue of #2489, labels
  `bug`+`security`, board status "AI Batter Up").
  Grant delta folded into **#1918**'s PROD plan via comment (same registration/consent process
  #1918 already covers, no new action item).
  Tests: `graph-write-permissions.test.ts` updated (24-permission list, new #2856 delta test,
  governance-groups refusal/requested split, `alwaysRefused` coverage) — 84/84 passing.
  `tsc --noEmit` clean (0 errors). Commit `23a8a7683`, pushed to `origin/main`. Branch verified
  merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
