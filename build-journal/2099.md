# #2099 — admin-config-snapshots/diffs double /api prefix routing bug

- **Status:** ✅ DONE 2026-09-09T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 7c48d1f3

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed root cause: `app.use("/api", ..., router)` in
  `src/app.ts` mounts `routes/index.ts`'s router at `/api`, but
  `admin-config-snapshots.ts` and `admin-config-diffs.ts` registered their 8 routes with
  a leading `/api/admin/...`, doubling to `/api/api/admin/...`. Working sibling routes
  (`msp-config-state.ts`, `portal-config-state.ts`) register without the leading `/api`.
  Dropped the leading `/api` from all 8 registrations to match.
- 2026-09-09T00:20:00Z ✅ DONE — fix committed (7c48d1f3) and pushed to `main`;
  `verify-branch-merged.mjs` confirms ancestor. Verification: `tsc --noEmit` clean;
  statically imported both route files and enumerated the 8 registered Express routes,
  confirming each now resolves to `/api/admin/config-snapshots*` /
  `/api/admin/config-diffs*` once mounted under `/api` — no more `/api/api/...`
  doubling. The commit was auto-grouped by the dev-server coordinator into build set
  `MSPConsoleFixes` (1/2 members merged), so the local dev api-server's restart is
  deferred until the other member lands — a genuine end-to-end curl/UI check against
  the running dev server was not possible from this session; that restart is outside
  this build's control. Working tree left clean.
