# Git #1976 — Gate PopulateGitBranches() the same way #1968 gated PopulateGitGraph()

- **Status:** ✅ DONE 2026-09-04T16:52:00Z
- **Scope:** buildconsole

## Log

- 2026-09-04T16:29:06Z — Starting. Mirroring #1968's fix pattern
  (`da3d71158`) for `PopulateGitBranches()` in
  `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs`: skip the `git branch -a`
  subprocess + `GitBranchListHost` rebuild while collapsed, add a cheap
  process-free no-op signature (`.git/refs/heads` + `.git/HEAD` +
  `.git/packed-refs` mtimes), and call it explicitly from
  `BtnGitBranchesToggle_Click` on expand.
- 2026-09-04T16:52:00Z — ✅ DONE — Implemented in commit `6b93ddfc`:
  - `PopulateGitBranches()` now returns immediately if `GitBranchListHost`
    isn't visible, and short-circuits on an unchanged
    `ComputeGitBranchesSignature()` (mtimes of `.git/HEAD`,
    `.git/packed-refs`, everything under `.git/refs/heads` and
    `.git/refs/remotes` — extended vs. #1968's graph signature to cover
    remote-tracking branches too, since those render here unlike the graph).
  - `BtnGitBranchesToggle_Click` now calls `PopulateGitBranches()` explicitly
    on expand, same as #1968's `BtnGitGraphToggle_Click`.
  - Signature is re-sampled after the fetch completes (not reused from
    before it), same rationale as #1968.
  - `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0
    warnings, 0 errors.
  - Merged `agent/1976-q1626` into `origin/main` — verified via
    `verify-branch-merged.mjs`: OK.
  - `git status --porcelain` clean.
