# #4110 — Billing: MSP-console subscription cancel, discount, and free-month application

- **Status:** ✅ DONE 2026-09-14T21:16:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** e3d1ca116

## Log
- 2026-09-14T20:16:00Z ⏳ IN FLIGHT — Audited the real subscription-holding table(s): `tenant_subscriptions` (tenant-scoped, Git #2847) is the real per-customer subscription row the MSP-console operator needs to act on — NOT `msp_subscriptions` (the MSP's own platform-tier subscription, `msp_id`-unique) and NOT `invoicesTable`/`client_services` (legacy, `usersTable`-keyed, unrelated axis per #1692's confirmed tenant-scoped Billing screen). Found the real natural hook for discount/free-month: `customer_billing_credits` + `lib/testimonial-credit.ts`'s `issueCreditToNextInvoice()` — already a generic "attach a Stripe coupon to the tenant's active subscription" mechanism (built for testimonial-approval credits, Git #4032), extending it with a `duration_months` column instead of inventing a parallel concept. Building: cancel route (real Stripe cancel/cancel-at-period-end via `stripe.subscriptions`, synced through the existing `syncTenantSubscriptionFromStripe`), discount route and free-month route (both via the generalized credit mechanism). All three gated `ladder.msp-admin` + `requireMspScope`, all three write `msp_audit_logs`.
- 2026-09-14T21:16:00Z ✅ DONE — Shipped `artifacts/api-server/src/routes/msp-subscription-billing.ts`: `POST /api/msp/:mspId/customers/:customerId/subscription/{cancel,discount,free-month}`. Cancel calls Stripe directly (immediate or `cancel_at_period_end`) for a Stripe-backed row and updates the row directly for a `manual`-source row (no Stripe subscription to cancel), then syncs via the same `syncTenantSubscriptionFromStripe()` the `customer.subscription.*` webhook uses and reconciles retention via `syncTenantsAfterStatusWrite()`. Discount and free-month both reuse the generalized `customer_billing_credits` + `issueCreditToNextInvoice()` mechanism (extended `msp_operator_discount` / `msp_operator_free_month` sources), with a new `duration_months` column (migration `lib/db/migrations/manual/4110-msp-operator-subscription-credits.sql`, additive, run myself against local `DATABASE_URL`) making a real "repeating" multi-invoice Stripe coupon possible instead of approximating free-month(s) with a single-invoice credit. Every action writes a real `msp_audit_logs` row. Verification: `pnpm --filter api-server run typecheck` clean; existing `testimonial-credit.test.ts` / `admin-testimonials.test.ts` / `msp-billing-webhook.test.ts` / `tenant-billing-state.live-db.test.ts` / `subscription-freeze.live-db.test.ts` all pass (against the real local DB for the live-db ones); added new unit tests for the two new pure helpers (`creditCouponDuration`, `creditCouponName`); exercised the real `issueCreditToNextInvoice()` end-to-end against the local DB with a live `msp_operator_free_month` row (tenant 1, the testbed tenant) confirming the new source/duration path resolves to the correct, honest `awaiting_subscription` outcome when no Stripe-backed subscription exists — then cleaned up the test row. No msp-console UI was built or touched: #1692 records screen 23 (Billing) as blocked on a real Design export (#2608), and this issue's own body scoped the deliverable to routes only. No genuine standing findings surfaced — both real subscription-creation paths (`msp-marketplace-purchase.ts`, `portal-checkout.ts`) were confirmed already wired to `recordTenantSubscription`. Merged into `origin/main` at e3d1ca116; `git status --porcelain` clean.
