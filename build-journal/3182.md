# Git #3182 — Money — Cars tab has real add/delete vehicle forms — violates no-forms principle

- **Status:** ✅ DONE 2026-09-08T03:20:00Z
- **Scope:** platform

## Log
- 2026-09-07T00:00:00Z — Started. Confirmed the real violation from #3149's own bookend and
  live code: `viewMoneyCars`/`viewCarDetail` in `web/shanes-life/public/app.js` have a
  dedicated "Add vehicle" `<form>`, a dedicated "Delete vehicle" button, and a dedicated
  maintenance-logging input row -- all real, direct violations of contract pack Section 8
  ("no forms, anywhere, ever"). `src/core/vehicles.mjs` already has the real CRUD
  (createVehicle/updateVehicle/deleteVehicle/logMaintenance) #3149 built; `src/mcp/tools.mjs`
  has `get_cars`/`log_car_maintenance` but no create/delete MCP tools yet. Plan: add
  `set_vehicle`/`delete_vehicle` MCP tools (mirroring `set_debt`/`delete_debt` and
  `log_car_maintenance`'s own name-matching), then strip the three dedicated UI elements and
  replace with capture-box hint copy, matching the style already used elsewhere (Lists,
  Places, Person notes).
- 2026-09-08T03:20:00Z — ✅ DONE. Added `set_vehicle` (create/update by id, same shape as
  `set_pet`/`set_debt`) and `delete_vehicle` (fuzzy name match, same resolution as
  `log_car_maintenance`'s own `vehicle` matching) to `src/mcp/tools.mjs`, both calling
  `src/core/vehicles.mjs`'s existing real CRUD directly. Removed the "Add vehicle" `<form>`,
  the "Delete vehicle" button, and the maintenance-logging input row from
  `public/app.js`'s `viewMoneyCars`/`viewCarDetail`, replacing each with hint copy pointing at
  the universal capture box ("Say the name below...", "Say \"remove the {name}\"...", "Say
  what was done below..."), matching the wording style already used elsewhere in the app
  (Lists, Places, Person notes). `GET /api/cars` and `GET /api/cars/:id` (still read by the
  UI) were left as-is; the now-UI-unused POST/PATCH/DELETE REST routes were left in place too
  (real endpoints, not forms -- no reason to remove them).
  Verified live against the real local `finances` Postgres DB: imported `TOOLS` from
  `src/mcp/tools.mjs` directly (bypassing the app's own migrate-on-boot gate, which was
  blocked by an unrelated concurrent session's in-flight migration ledger row -- see below)
  and round-tripped create (`set_vehicle`) -> update -> `get_cars` reflecting it -> log
  maintenance by fuzzy name (`log_car_maintenance`, unchanged) -> delete by fuzzy name
  (`delete_vehicle`) -> `get_cars` confirming zero vehicles remained, plus the "no such
  vehicle" error path. Used the real `you@example.com` test user, not Shane's own row;
  deleted the test vehicle (and its cascaded maintenance-log row) at the end, confirmed
  `select count(*)` back to zero on both tables. `node --check` clean on both changed files.
  Full local server boot was genuinely blocked by an unrelated concurrent session's own
  in-flight migration (`schema_migrations` held `043_account_mask.sql`, a file not yet in
  this worktree's `migrations/` dir -- the real Git #3166 shared-DB class, not mine to
  reconcile) -- worked around it with the direct-import test above rather than touching that
  session's ledger row.
  Grep-verified no `Add vehicle`/`Delete vehicle`/form remnants remain anywhere in the Cars
  section of `app.js`.
  **Real, related finding already filed by another session**: #3196 ("Money — Bankruptcy tab
  has real add/edit forms") covers the same class of violation on the Debts/Bankruptcy tab
  (`viewMoneyBankruptcy`'s own `debtForm`) -- found it while investigating this issue, but it
  was already open and correctly parented under #3086, so no duplicate filed.
  Merged `origin/main` into this branch (clean auto-merge, no conflicts -- a concurrent
  session, #3181, had landed the same fix pattern for Things/Contacts moments earlier) and
  pushed to `main` at `b64ef050c`. `verify-branch-merged.mjs` confirms merged.
  `git status --porcelain` clean.
