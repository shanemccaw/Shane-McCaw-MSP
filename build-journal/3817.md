# #3817 — Retention Queue: wire the real MSP Console retention-queue module (screen 44)

- **Status:** ⏳ IN FLIGHT 2026-09-13T01:45:53Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** TBD

## Log
- 2026-09-13T01:45:53Z ⏳ IN FLIGHT — Read design (`Design/MSP_Console/design_handoff_msp_console/Retention Queue.dc.html`), contract pack (`docs/msp-console/retention-queue-msp-console-contract-pack.md`), and route (`msp-retention-queue.ts`). Re-verified the design's own premise ("nothing can reach this queue") against current code: **it no longer holds**. `artifacts/api-server/src/lib/retention/wiring/msp-poams.ts` (Git #3451, landed after the pack/design) now registers `msp_poams` and both `msp-poams.ts`/`portal-poams.ts` call `softDelete()`/`requestAcceleration()`. `record_deletions` is still 0 rows in local Postgres today (confirmed live), so the empty state is real, but the reason has changed — it's genuinely reachable now, just currently empty, not structurally inert. Filing this as a finding sibling issue under #1571 and adjusting the empty-state copy to stay honest instead of rendering the design's now-false "no record class registered / no callers" claim verbatim. Building the module next: real `GET /api/msp/retention/queue` wiring, real decide/discuss actions, plus the explicitly-labelled "not live data" shape preview exactly as designed.
