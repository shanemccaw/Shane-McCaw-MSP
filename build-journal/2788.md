# #2788 — Log Viewer: decommission BuildLogView bottom panel

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:59:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-04T00:59:00Z ⏳ IN FLIGHT — Confirmed both #2786 (LogViewerDock) and #2787
  (LogPeekPanel) closed `complete` with genuine feature parity (per their own DONE
  comments: same COLD/BURST/LIVE model, real LogService-backed data, pinned lines
  survive filter changes). Audited every real (non-comment) `BuildLogView` call site:
  `MainWindow.xaml:1065` (control instance), `MainWindow.xaml.cs:704`
  (`.Initialize(...)`), `MainWindow.xaml.cs:6001` (`.LoadQueueItem(...)` from
  `BuildQueuePanel_TaskSelected`, the real "Open Build Log" right-click target from
  #2689). Plan: add real per-build scoping to `LogService`/`LogViewerDocumentView`
  (a specific queue item's own `queue-{id}.log`, not the aggregated "Build" source's
  most-recent-5), repoint `BuildQueuePanel_TaskSelected` at
  `MainWindow.OpenLogViewerTab().ScopeToBuild(...)`, remove `BuildLogView.xaml(.cs)`
  and its bottom-panel wiring, then verify `grep -rn BuildLogView desktop/BuildConsole`
  is zero real hits (comment-only mentions elsewhere are fine to leave as history).
