# #2788 — Log Viewer: decommission BuildLogView bottom panel

- **Status:** ✅ DONE 2026-09-04T01:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `320e35eeb` (real work), rebased to `bf34845bf` on push

## Log
- 2026-09-04T00:59:00Z ⏳ IN FLIGHT — Confirmed both #2786 (LogViewerDock) and #2787
  (LogPeekPanel) closed `complete` with genuine feature parity (per their own DONE
  comments: same COLD/BURST/LIVE model, real LogService-backed data, pinned lines
  survive filter changes). Audited every real (non-comment) `BuildLogView` call site:
  `MainWindow.xaml:1065` (control instance), `MainWindow.xaml.cs:704`
  (`.Initialize(...)`), `MainWindow.xaml.cs:6001` (`.LoadQueueItem(...)` from
  `BuildQueuePanel_TaskSelected`, the real "Open Build Log" right-click target from
  #2689). Plan: add real per-build scoping to `LogService`/`LogViewerDocumentView`
  (a specific queue item's own `queue-{id}.log`, not the aggregated "Build" source's
  most-recent-5), repoint `BuildQueuePanel_TaskSelected` at
  `MainWindow.OpenLogViewerTab().ScopeToBuild(...)`, remove `BuildLogView.xaml(.cs)`
  and its bottom-panel wiring, then verify `grep -rn BuildLogView desktop/BuildConsole`
  is zero real hits (comment-only mentions elsewhere are fine to leave as history).
- 2026-09-04T01:20:00Z ✅ DONE — Shipped (commit `320e35eeb`, rebased/pushed as
  `bf34845bf`):
  - `Services/LogService.cs`: `LogQuery` gained a trailing, defaulted
    `BuildQueueItemId` field; `Tail()` gained a matching optional param; `BuildLogFiles()`
    now takes an optional `scopedQueueItemId` that, when set, resolves to exactly that
    build's own `queue-{id}.log` (`BuildLogPaths.ForQueueItem`) instead of the
    aggregated most-recent-5 default used by the unscoped "Build" source. Wired through
    both `Query()`'s "build" case and `TailRawSourceAsync`'s live-tail path.
  - `Controls/LogViewerDocumentView.xaml.cs`: new public `ScopeToBuild(queueItemId,
    epic, task, status)` — restricts the source rail to "Build" only, refreshes the
    COLD query scoped to that one file, and starts LIVE tailing when `status ==
    "running"` (same auto-tail behavior BuildLogView.LoadQueueItem gave the old bottom
    panel). Added a dismissible "build: <epic> — <task>" filter chip (clearing it
    restores all sources and restarts any active stream) alongside the existing chip
    system.
  - `MainWindow.xaml.cs`: `BuildQueuePanel_TaskSelected` (the real #2689 "Open Build
    Log" wiring) now calls `OpenLogViewerTab().ScopeToBuild(...)` on an explicit
    right-click, and silently re-scopes an already-open Log Viewer tab (via new
    `FindOpenLogViewerTab()`) on a plain card click — preserving #2689's original
    "sync without forcing the panel open" rule, now against a document tab instead of
    a bottom-panel Grid. Removed the `BuildLogView.Initialize(...)` call.
  - `MainWindow.xaml`: removed the `<TabItem Header="Build Log"><ctrl:BuildLogView/>`
    entry from `BottomTabs`. Every other tab in that TabControl shifted down one
    index; renumbered every hardcoded `SetBottomPanel(tabIndex:...)` call site that
    depended on the old ordering (`TabIndexForService`, `MenuStartAllServices_Click`,
    `OpenTerminal_Click`, `GitChip_Click`) — audited each one against the new real tab
    order (Terminal=0, Marketing=1, Portal=2, Admin=3, API Server=4, Output=5, Issue
    Detail=6). The pre-existing `SetBottomPanel(tabIndex: 5) // Output tab` call
    (`dotnet build` release-build wiring) was already off-by-one before this change
    (pointed at API Server, not Output) — the post-removal shift makes that literal
    value correct, left as-is with a clarifying comment rather than "fixed" twice.
  - Deleted `Controls/BuildLogView.xaml(.cs)`.
  - `IssueDetailView`'s own bottom-panel tab is unrelated dead code (comment at
    `MainWindow.xaml.cs:994`: "no longer driven by issue clicks" since #840) — left
    untouched, out of scope for this issue.
  - `grep -rn BuildLogView desktop/BuildConsole` still returns hits — all in frozen
    `BUILD_LOG.md` history and in comments (mine and pre-existing) that name the
    decommissioned class for provenance (e.g. "Git #2788 — decommissioned BuildLogView's
    own bottom-panel tab"), consistent with how this codebase already keeps
    `#802`/`#945` references alive in comments long after the code they describe
    changes. Zero *functional* references remain — confirmed by a clean
    `dotnet build BuildConsole.csproj -c Debug` (0 warnings, 0 errors).
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 0
    warnings. No live WebView2/BuildConsole instance launched for UI verification
    (never launch BuildConsole.exe per project rules); build-level + code-path
    verification against the already-proven #2786/#2787 `LogViewerDocumentView`/
    `LogService` contracts is the honest verification here, same standard those two
    builds used. `verify-branch-merged.mjs` confirmed `bf34845bf` is an ancestor of
    `origin/main`. `git status --porcelain` empty before this line was written.
  - No findings filed this session — no new gap discovered beyond what #2786/#2787
    already tracked.
