# Git #3776 — Chats panel: fix same-hue text-on-background contrast on epic badges

- **Status:** ✅ DONE 2026-09-12T05:10:53Z
- **Scope:** buildconsole

## Log
- 2026-09-12T05:09:42Z — Started. Colors-only fix per issue body: change confirmed `Tint(accentColor, ...)` / `new SolidColorBrush(accent)` TextBlock.Foreground sites in `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs` to a neutral text brush. Background/Border accent uses left untouched. Not adopting the structural redesign (nested sub-chats, checklists, epic-picker) from the same reference file — explicitly out of scope.
- ✅ DONE — 2026-09-12T05:10:53Z. Commit `a84acd934` (rebased onto main as `48b5fe5d1`) changed 4 real `Foreground` sites in `LeftSidebar.xaml.cs`: L557 (`BuildNewChatAnchorRow` pill) and L3977 (epic github-number badge) → `TextBrush`; L4046 (Claude Design URL pill) and L4127 (sub-epic divider label) → `ChatsPanel.Text3`. Every `Background`/`BorderBrush`/`Stroke` accentColor use (L3906,3917-18,3967-68,4034-35,4111,4153,4413,4434) left untouched. Grep-confirmed no remaining `Foreground = Tint(accentColor` or `Foreground = new SolidColorBrush(accent` sites. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — 0 errors, 10 pre-existing unrelated warnings. Verified `agent/3776-q2411` (48b5fe5d) is an ancestor of `origin/main`. Working tree clean. No structural redesign (sub-chats/checklists/epic-picker) adopted, per scope. No findings filed — no genuine bug/gap discovered beyond the issue's own scope.
