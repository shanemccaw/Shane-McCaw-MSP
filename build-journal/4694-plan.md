# #4694 — Plan + Walkthrough (standing requirement, #4307)

Scope: `desktop/BuildConsole` only. Removal only — no replacement built here (the local
"remove from queue" action is #4695, already landed; label reads were repointed in #4693).

## Preconditions verified
- `blocked_by` #4693: issue carries `complete`, its DONE bookend (`build-journal/4693.md`) and code
  commit `c9fdc60aa` are ancestors of `origin/main`. The label-read side no longer depends on the labels.

## Real sites (line numbers re-verified on current main; they drifted from the issue after #4695/#4696)
1. `Services/BuildQueuePostgresClient.cs` `QueueBuildAsync` (~1468-1485) — fire-and-forget
   `AddLabelAsync(num,"in-flight")` + `RemoveLabelAsync(num,"complete")` block. Delete whole block.
2. `Services/BuildQueuePostgresClient.cs` `UnparkAsync` (~2579-2595) — same pair. Delete; the
   `num` local and its `RETURNING github_number` read then become unused → drop them too so the
   method stays warning-free.
3. `Controls/LeftSidebar.xaml.cs` (~6871-6900) — `miToggle` "✓ Mark Complete (Ready for Review)" /
   "Remove 'complete' label" menu item + its async handler + `cm.Items.Add(miToggle)`. Delete, plus the
   now-false "Real toggle ... `complete` label" comment above it and renumber the "// 2." comment.
4. `Services/BuildTrackerApiClient.cs:767-768` — `ToggleLabelAsync` (POST to the 410 route). Its only
   caller is #3, so delete the method.
5. Stale doc text that would contradict the code: `BuildQueuePostgresClient` class doc
   ("ToggleLabelAsync — a GitHub label mutation") and `ParkAsync` doc ("No label sync here, unlike UnparkAsync").

## Deliberately NOT touched
- `GitHubIssuesService.AddLabelAsync/RemoveLabelAsync` — become unreferenced; dead-code removal is #4696's scope.
- Server route `admin-build-tracker.ts` 410 stub — api-server, not this issue.
- `GitHubApiClient` `blocked` label DELETE (`:699`) and `blocked` / `Shane To-Do` reads — different labels.
- `MarkCompleteAsync` / "✓ Mark Complete (Hide)" in `BuildQueuePanel` — a *queue-row* status action, unrelated to the
  GitHub `complete` label and not the menu item named in the issue.

## Verification plan
1. `dotnet build desktop/BuildConsole/BuildConsole.csproj` → 0 warnings / 0 errors.
2. Grep: nothing left referencing `ToggleLabelAsync` / `toggle-label`; no `AddLabelAsync`/`RemoveLabelAsync`
   call sites remain outside the definitions in `GitHubIssuesService`.
3. Absence-of-call check on real behaviour: queue + un-park a *throwaway* local (`--notGit`-style / synthetic) row
   through `BuildQueuePostgresClient` is not possible without launching the exe (never-launch-BuildConsole rule,
   Git #2156). Instead: static proof — the only two code paths that ever reached `gh issue edit --add-label/--remove-label`
   for these labels are deleted, and `GitHubIssuesService.AddLabelAsync/RemoveLabelAsync` (the only place that shells
   `gh issue edit`) has zero callers. Also check the real ActivityLog for the `gh issue edit --add-label` lines to
   show the pre-change behaviour they'd have produced. State honestly that a live click-through was not done.
