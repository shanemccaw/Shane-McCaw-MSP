# #2813 — Chat context meter: confirm conversationId re-derivation & persistence live (4th attempt)

- **Status:** ✅ DONE 2026-09-12T00:01:26Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 291ad71c7 (IN FLIGHT bookend), no code changes required — see conclusion below. Finding #3725 filed separately.

## Log
- 2026-09-11T23:59:36Z ⏳ IN FLIGHT — Read #2813 body + all comments + latest dispatch comment. Read `ChatContextMeterScript.cs`, `MainWindow.xaml.cs` (UpdateContextMeter/TryGetLiveContextMeter), `ChatDocumentContainer.xaml.cs` (ResolveConversationMeter/ResolveWebView). Cross-checked live evidence in `%APPDATA%/BuildConsole/logs/activity-2026-09-*.log` (`system.core.chat-context` channel) and `context-meter-highwater.json`. Bookend created retroactively after investigation started — noting the deviation honestly rather than silently backdating it.

- 2026-09-12T00:01:26Z ✅ DONE — **No code fix required — verified live that the current code already does not exhibit the hypothesized bug.** Full honest breakdown of the 5 dispatch items, each backed by real evidence, not static tracing alone:

  1. **Is `conversationId()` re-derived per poll or cached?** — Re-derived fresh every poll. `countStats()` (driven by `setInterval(countStats, 2000)`) calls `store()` on every tick, and `store()` calls `conversationId()` unconditionally every time — there is no memoization in the JS. This was already true before this session (the code's own Git #3167 comment documents it), and it predates/is unrelated to the actual root cause of the earlier freeze, which was a malformed regex (8-4-4-12 instead of 8-4-4-4-12 groups) fixed in commit `898ed9479` (2026-09-08) — well before this build started.
  2. **If one-shot, fix it** — N/A, not one-shot (see #1). No change made.
  3. **Does `Merge()`/`Save()` actually persist?** — Yes, confirmed with real mtime, not a log line: `%APPDATA%/BuildConsole/context-meter-highwater.json` mtime was verified to be the literal current moment while this session was running (file is actively being written by the live BuildConsole process), and its contents hold 16 real conversation UUIDs with real, large token/turn counts (e.g. `9604abb4-...` at 480,510 est. tokens / 870 turns; `110b8198-...` — the exact UUID cited in the code's own #3167 comment — at 326,115 tokens / 1198 turns).
  4. **Can the #2808 live-meter fallback be poisoned by the same broken conversationId?** — No, confirmed independent by code inspection: `ResolveConversationMeter()` (`ChatDocumentContainer.xaml.cs`) falls back to `_owner.TryGetLiveContextMeter(ResolveWebView(), ...)`, which is keyed by the actual `WebView2` object identity in `MainWindow`'s `_contextMeters` dictionary (populated once per tab's webview registration) — never by `conversationId`. `ResolveWebView()` walks the visual tree to find the tab's live WebView2 instance directly. A null/empty conversationId cannot poison this path.
  5. **Verify against a real 300+ message chat open for an extended period** — Confirmed live: conversation `9604abb4-7c04-4860-bb12-be94e582203c` (`context-meter-highwater.json`) genuinely grew from turnCount=812/estTokens=405920 at 10:25:56 to turnCount=870/estTokens=480510 at 19:48:43 (over 9+ hours, same UTC day, 2026-09-11) — the persisted-store `[link 5]` reads tracked this growth correctly the whole time, no freeze observed.

  **Cross-day check for the actual "poisoned on /new" scenario:** grepped `activity-2026-09-09.log`, `-10.log`, `-11.log` (full days) for any `[link 1]` line showing `pathname='/new'` from a webview other than the special `wv=ClaudeWebView` — zero hits across all three days. `ClaudeWebView` is the persistent Home-screen/new-chat-composer webview (`MainWindow.xaml` line 59/959), not attached to any per-chat `ChatDocumentContainer`/Band-1 gauge while sitting on `/new` — its null-conversationId cycling (`/` → `/new`, repeating all day) is expected and functionally inert. Real per-chat tabs are only ever created (`OpenChatTab`, `MainWindow.xaml.cs` ~4062-4089) once a real conversation id is already known, and their WebView2 navigates directly to the resolved `/chat/<uuid>` URL — so the "/new-race, permanently poisoned" scenario in #2813's Sept-11 update hypothesis does not occur in practice for tracked chat tabs.

  **Root cause of the misdiagnosis, filed as #3725** (sibling sub-issue of #3723, `bug` label, board status "AI Batter Up"): the `[link 1]` diagnostic log line gives no visual distinction between the benign `ClaudeWebView` null cycle and a genuine per-chat freeze — both render identically. That ambiguity is the real, concrete reason #2813's Sept-11 update cited the null-conversationId log pattern as evidence against chat #2880: the log lines were real, but belonged to the Home-screen composer, not the named chat.

  **What remains explicitly out of scope, per #2813's own body:** the token-estimate accuracy gap (`chars/4` heuristic vs. real tokenization, missing system-prompt/MCP-tool-schema overhead) — tracked separately under #3723, not folded into this issue. This build did not touch that.

  **Verification method:** live grep of 3 real days of `%APPDATA%/BuildConsole/logs/activity-2026-09-*.log`, real file mtime check on `context-meter-highwater.json`, and static code reading of `ChatContextMeterScript.cs` / `MainWindow.xaml.cs` / `ChatDocumentContainer.xaml.cs` — no BuildConsole rebuild/relaunch was needed since the live evidence came from the currently-running production instance already reflecting the post-#3167 code.

  Findings filed: #3725. Labels synced (`in-flight` → `complete` on #2813). Working tree clean (`git status --porcelain` empty). Branch verified merged into `main` before writing DONE.
