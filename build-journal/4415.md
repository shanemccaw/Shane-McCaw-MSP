# #4415 — Command Center: "Reset dev database" quick action (dry-run default, explicit confirm before --yes)

- **Status:** ✅ DONE 2026-09-16T19:06:40Z
- **Scope:** buildconsole (plus scripts/db/reset-dev-database.mjs output + README)
- **Started:** 2026-09-16
- **Commit(s):** 6a094bc00 (work), 4d1b3c8fc (IN FLIGHT bookend)
- **Plan:** [4415-plan.md](4415-plan.md)

## Log
- 2026-09-16T14:45:51-04:00 ⏳ IN FLIGHT — reading #4415/#3827, scripts/db/README.md and the existing Command Palette + destructive-confirm patterns before wiring the reset action.
- 2026-09-16T19:06:40Z ✅ DONE — shipped and verified against the real local dev DB; details below.

## Changes Made
- `desktop/BuildConsole/Services/DevDatabaseResetGate.cs` (new) — one gate per palette window.
  `PreviewAsync` runs `node scripts/db/reset-dev-database.mjs --dry-run` and arms
  `ConfirmPhrase = "reset msp #<live id>"` only on exit 0 plus the script's own target-MSP and
  dry-run-success lines. `ExecuteAsync(typed)` re-checks the phrase itself, consumes the
  confirmation before launch, runs `--yes` exactly once (no SubprocessRunner crash-retry, UTF-8,
  stdin closed, 20-min timeout), and leads the result with the real `Backup:` path + verify line.
- `MainWindow.CommandPalette.cs` — new "Reset dev database" row (`RunWithResult = gate.PreviewAsync`);
  gate created per `CreateCommandPaletteWindow` and passed to the window.
- `CommandPaletteWindow.xaml.cs` — "reset confirm mode" auto-detected from input shaped
  `reset msp #N` (like SQL mode). Enter / action button reach the real reset only when armed;
  otherwise a locked explanation. Up/Down/Tab inert there; general search suppressed. Real result
  kept in-pane until the input changes; toast backstop in case the palette closes on focus loss.
- Existing confirm pattern checked: `MessageBox YesNo Warning` (BuildQueuePanel "Remove Entire
  Set", DispatchPanel force re-dispatch). Not reused — Yes is the default button (a stray Enter
  confirms) and it deactivates the palette, which closes itself. Reused instead the script's own
  typed-confirmation model, moved into the palette input.
- `scripts/db/reset-dev-database.mjs` — dry run now prints where a real run's backup would go;
  stale header comment ("both modes take a backup") corrected. `scripts/db/README.md` documents
  the Command Center gate.

## Verification Results
- `dotnet build desktop/BuildConsole/BuildConsole.csproj`: 0 warnings, 0 errors (before and after rebase onto origin/main).
- Headless probe (separate exe at C:\t\probe4415 referencing the built BuildConsole.dll; never
  launched BuildConsole.exe, never showed a window) drove the REAL `CommandPaletteWindow`
  `Window_PreviewKeyDown` handler + REAL gate against the local dev DB (`shanemccawmsp` on localhost).
  Its command list held only the reset row built the same way as MainWindow's (MainWindow itself not constructed).
  - A: typed `reset msp #1` BEFORE any preview + Enter → locked, DB counts unchanged, no backup. PASS
  - B: single Enter on the "Reset dev database" row → right pane "DRY RUN ONLY…" + real script
    output incl. backup-location line; gate armed `reset msp #1`; DB unchanged; no backup. PASS
  - C: accidental SECOND Enter on the same row → re-ran the preview only; DB unchanged; no backup. PASS
  - D: wrong phrase `reset msp #2` + Down/Tab/Enter → nothing ran; DB unchanged; no backup. PASS
  - E: exact phrase + Enter → REAL reset: `✓ REAL RESET COMPLETED`,
    `Backup: C:\Source\ShaneMcCawConsulting\db-backups\shanemccawmsp_pre-dev-reset_2026-09-16T185501841Z.dump`
    (exists on disk; pg_restore --list exit 0, 4330 TOC entries); exactly one new backup (2→3);
    wf_runs 124→0, wf_run_node_logs 696→0, wf_run_node_outputs 348→0, wf_trigger_events 124→0,
    exception_groups 2→0; MSP-staff logins 9→9; login probe 401; confirmation consumed; an Enter
    pressed while running did not start a second run. PASS
  - F: Enter again on the same phrase after the real run → no second run, no new backup. PASS
  - Re-run after the UTF-8 fix (preview-only, A–D): all PASS, "§" intact.
- Keyboard-only (#3827 standing requirement): every step above is Enter/typing/arrow/Tab — no click needed.
- Honest gaps: live click-through in Shane's running BuildConsole not self-verified (per the
  never-launch-exe rule); please confirm by eye. msp #1 was already at clean slate (0 tenants) so
  the real reset mostly cleared the global workflow-history tables. "tenants: 4 -> 5" in the
  script's before/after is a concurrent session re-seeding tenant id 1 at 18:57:50Z, about 3 minutes after
  the reset committed — not the reset.

## Findings filed
- None. Checked the script's "4 closure tables skipped" warning (compliance_obligations,
  dashboard_overrides, engagement_offer_firings, simulation_runs): all hang only off preserved
  config parents with no tenant/msp/customer column — benign, not filed.

## Working tree
- Clean at DONE. Scratch probe lives outside the repo (C:\t\probe4415, C:\t\bc4415-out).
