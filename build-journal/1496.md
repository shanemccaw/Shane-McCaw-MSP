# #1496 — Change Control: Approval model + `canApproveChanges` capability flag

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `060fd29c8` (schema + migration + libs + routes), `d93d55646` (route type fix + unit tests)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Building the Change Control approval model (`cr_approvals`), the `canApproveChanges` customer-side capability flag, approval SLA, delegation, and separation-of-duties. Ends at the wire contract: Drizzle schema, manual migration run against local DB, routes + Wire* interfaces. No portal UI.
- 2026-08-29 ✅ DONE — Shipped the full approval MODEL, stopping at the wire contract as scoped (no `artifacts/portal` UI):
  - **Schema.** `lib/db/src/schema/index.ts` — `users.canApproveChanges` boolean (customer-side capability, live-checked, never in the JWT; deliberately NOT `canApprovePurchases`/`canManageTeam`). `lib/db/src/schema/msp.ts` — `cr_approvals` ledger: one row per approver decision (`stage`, `decision` [pending/approved/rejected/superseded], `approverRole` [customer/msp/catalog_inherited/microsoft_forced], `approverPersonId`, `approverName`, `onBehalfOfPersonId` for delegation, `reason`, `decidedAt`, `dueAt` SLA, `escalatedAt`). Quorum expressible (multiple rows per stage); multi-stage gated in order.
  - **Migration (run against local Postgres, self-marked).** `lib/db/migrations/manual/2026-08-29-cr-approvals-model-1496.sql` — additive DDL + backfill of one approval row per existing CR (CR 83 → a `microsoft_forced` approved row). Appended to the #1630 checklist.
  - **Derivations** `artifacts/api-server/src/lib/portal-change-approvals.ts` (pure, unit-tested): `requiredStages` (risk decides count; standard=0/pre-approved, high&critical=2), `slaDaysFor`/`computeDueAt`, `isApprovalBreached`, `violatesSeparationOfDuties` (requester ≠ approver), `summarizeApprovals`, wire mappers.
  - **Store + rejection** `portal-change-approvals-store.ts` (materialize, recordApproval, delegation resolution via `portal_ownership_delegations`, breach sweep) and `portal-change-rejection.ts` (customer rejection → assigned risk; MSP rejection → no risk, different terminal state). Split so `m365-change-router` imports `materialize` with no cycle.
  - **Routes / Wire*.** `routes/portal-change-control.ts` — GET now carries `approvalRecords` + `approvalState` + caller-relative `canApproveNow`; new `POST /portal/change-control/:code/approve` and `/reject` (live `canApproveChanges` gate, SoD + stage-order enforced in the store). Materialization hooked into both the wizard POST and `createRoutedChangeRequest`.
  - **Settled architecture honoured.** Approval attaches to the change (standard/auto-approved changes still get a real approval row); an auto-approved CR's approver is the real party (`catalog_inherited` human / `microsoft_forced`), never "the system"; customer rejection → assigned-risk hook (linkage is #1487's register); MSP rejection → no risk.
  - **Verification.** 20/20 DB integration checks against local Postgres (exercised the real store code paths: multi-stage gating, SoD 403, delegation, customer-reject→risk with linkage, MSP-reject→no risk, standard pre-approval, microsoft_forced auto-approval, idempotent breach escalation) — all temp rows cleaned up, no real data touched. 18/18 pure-lib unit tests (registered in `vitest.config.ts`). api-server `tsc` clean for all touched files (pre-existing unrelated repo tsc redness left untouched — see issue comment). Branch merged to `origin/main`; working tree clean.
