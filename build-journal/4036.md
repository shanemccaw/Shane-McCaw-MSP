# #4036 — Launch Control Plus add-on cannot be granted to an MSP

- **Status:** ✅ DONE 2026-09-14T21:28:05Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 08c1eeedf (bookend IN FLIGHT), bfa0e0862 (implementation)

## Log
- 2026-09-14T21:22:57Z ⏳ IN FLIGHT — new `msp_addon_subscriptions` join table (mspId, serviceId, status, createdAt), wire `loadTier()` in `artifacts/api-server/src/lib/msp-entitlement.ts` to merge active add-on grants' `grantsCapabilityKey` into `tierCapabilities`, seed MSP 1's grant for service 131 (`launch_control_plus`), re-verify resolution.
- 2026-09-14T21:28:05Z ✅ DONE — Added `mspAddonSubscriptionsTable` (`lib/db/src/schema/msp.ts`), no change to `msp_subscriptions`'s `UNIQUE(msp_id)` or its tier resolution. Migration `lib/db/migrations/manual/2026-09-14-msp-addon-subscriptions-4036.sql` run against local `DATABASE_URL` (table + 3 indexes + self-marking INSERT, confirmed via psql). Wired `loadTier()` (`artifacts/api-server/src/lib/msp-entitlement.ts:69-138`) to overlay a `true` grant for every active `msp_addon_subscriptions` row's `services.typeAttributes.grantsCapabilityKey`. Seeded MSP 1 → service 131 (`launch_control_plus`) active row (id=1), re-running #4023's originally intended seed step. Live-verified via `loadTier(1)`: `tierCapabilities.launch_control_plus === true`. `pnpm run typecheck` in `artifacts/api-server` passes clean. Confirmed via grep that no Launch Control action currently gates on `launch_control_plus` yet (only `launch_control_safe_write`/`launch_control_gated_write`, both already true on MSP 1's base Pro tier) — matches the issue's own finding, nothing further to wire on that front. Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
