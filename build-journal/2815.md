# #2815 — URGENT: 3 completions reaped but nothing new launching (queue stuck; _appReady gate)

- **Status:** ✅ DONE 2026-09-04T05:22:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `68ecf75` (code) → merged to `origin/main` as `2e50b866`; `0e54dcf` (IN FLIGHT bookend)

## Log
- 2026-09-04T04:56:36Z ⏳ IN FLIGHT — Pulled #2815 body + both comments. Leading hypothesis: `QueueWatcherService.TickAsync` `_appReady` gate never lifted after restart because `_startupConnectivity.AllSettled` never fired (a connectivity check hanging/looping). Comment 2 adds live evidence: Batter Up + AI Batter Up 401ing simultaneously — points at same startup connectivity check hammering GitHub. Own live corroboration: GraphQL `gh` calls returning "rate limit already exceeded" while REST rate_limit shows full quota = secondary/abuse rate limit from bursty calls. Auditing `_startupConnectivity` + `MarkAppReady` chain next.
- 2026-09-04T05:22:00Z ✅ DONE — Root cause is NOT `_appReady` (that lifted at 00:33:09 per ActivityLog). Real cause: the queue is PAUSED — `QueuePaused=true` in settings.json, set by clicking "Stop" on the #1884 grace banner (`ResolveAutoQueueGrace(pauseQueue:true)` → `SetPaused(true)`, persisted across restarts). Reaping runs above both gates, so 3 reaped fine while `if (_paused) return;` blocked all launches. DB: 80 queued (54 blocker-free), 3 running. Immediate unblock for Shane = Resume the queue (no rebuild). Surfaced the "Stop"-pauses-whole-queue UX trap to Shane as a product decision (not changed unilaterally). Shipped two code fixes he asked for: (1) shared `GitHubRateLimitCircuit` (new files `GitHubRateLimitCircuit.cs`, `GitHubRateLimitHandler.cs`) wired into BOTH GitHub transports — `SubprocessRunner` (gh CLI) and `GitHubApiClient` (HTTP+PAT DelegatingHandler) — so a rate-limit trips one exponential backoff window and both paths short-circuit instead of hammering every tick (ends the retry-storm that kept the limit from clearing); (2) `SelectClaimCandidatesAsync` now FAILS OPEN on the #1904 own-issue self-check for blocker-FREE builds when GitHub is unreachable (they launch), while §4/#1600 blocker verification stays FAIL-CLOSED for builds with real declared blockers (safety property unchanged). Verified: `dotnet build BuildConsole.csproj` clean (0/0) to an isolated output dir; `verify-branch-merged.mjs` OK (`2e50b866` ancestor of origin/main); working tree clean. NOT live-runtime-verified (BuildConsole code change → needs rebuild+restart to go live; single-instance safety rule forbids launching it from this session). Finding filed: #2817 (parented #1202, `bug`, AI Batter Up) — per-tick re-query of already-DONE issues (#2133/#2736) with no dedupe/give-up.
