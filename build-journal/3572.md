# Build Journal — #3572

- **Status:** ✅ DONE 2026-09-11T04:58:00Z
- **Scope:** platform

## Log

- 2026-09-11T04:51:18Z — IN FLIGHT. Registering `evidence_attachments.customer_id`
  with a real retention purger — same gap-class already closed 4x tonight
  (#2980/#3072/#3093/#3353). Adding it to `workflowPurger` in
  `artifacts/api-server/src/lib/retention/purgers/modules.ts` since it's a
  polymorphic child with no FK on `source_ref_id` (soft-link, like
  `retainer_work_log`) pointing at either `remediation_tracker_steps` (this
  module's own table) or `cr_executions` (change-control) — no ordering
  constraint either way, declared alongside `remediation_tracker_steps`.
- 2026-09-11T04:58:00Z — ✅ DONE — commit `aae554842` (rebased to `8758d00a` on
  merge to `origin/main`) declares `{ table: "evidence_attachments", column:
  "customer_id", keySpace: "customerId" }` in `workflowPurger`. Ran
  `tenant-scope-coverage.live-db.test.ts` against local Postgres directly: 7/7
  passed (was 1 failing before the fix). Branch verified merged into
  `origin/main` via `verify-branch-merged.mjs`. Working tree clean.
