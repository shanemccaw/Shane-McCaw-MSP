# #4517 — Audit log entry for tenant isTestbed flag change

- **Status:** ✅ DONE 2026-09-17T14:20:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** cf3407fb1 (rebased onto main as a24f24346)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — Adding audit_logs insert (`tenant_testbed_flag_changed`) to `PATCH /api/admin/msp-directory/customer/:id`, matching the write-consent callback's pattern. Typecheck running, live-verify pending.
- 2026-09-17T14:20:00Z ✅ DONE — Added a `tenant_testbed_flag_changed` audit_logs insert (old/new isTestbed, actor) to `PATCH /admin/msp-directory/customer/:id`, only firing when the value actually changes. `tsc --noEmit` clean. Live-verified against the local dev DB: flipped `tenants.id=2191` (zz-test-4488, synthetic, `reset_protected=false`) using the route's exact read-before-write logic, confirmed a real `audit_logs` row with correct actor/old/new, then reverted the tenant and deleted the scratch audit row — DB left in original state. Pushed to `origin/main` at a24f24346; `verify-branch-merged.mjs` confirms ancestry. Working tree clean.
