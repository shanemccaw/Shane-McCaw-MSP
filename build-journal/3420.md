# #3420 — Feature: Remove Running Scripts page from Admin Panel (AdminPanel)

- **Status:** ✅ DONE 2026-09-11T16:05:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** d399962a9

## Log
- 2026-09-10T05:00:00Z 🛑 BLOCKED — Confirmed real `blocked_by` edge from #3420 → #3419
  (dispatched with `--blocked-by 3419`). Verified the reason is genuine and still live:
  #3420's own scope is deleting the `cmd-running-scripts` nav entry from
  `artifacts/admin-panel/src/components/shell/workspaceNav.tsx` — the same file
  #3419 (`cmd-presentations`) and #3418 (`cmd-messages` + Communications section)
  are also editing. #3419 is itself still `OPEN`/`blocked` (blocked by #3418, which
  is `OPEN`/`blocked`). Both are unmerged. Editing that shared nav file now risks a
  real collision with either concurrent edit.

  No code changed — stopping before touching `RunningScriptsPage.tsx` or the nav
  file, per the blocked_by-is-the-gate rule. Labels swapped `in-flight` → `blocked`.
  Comment posted on #3420 explaining the block.

  Once #3419 lands (closed or labeled `complete`), a fresh session should:
  re-check `dependencies/blocked_by` on #3420, drop the now-stale edge, swap
  `blocked` back to `in-flight`, then delete `RunningScriptsPage.tsx` and the
  `cmd-running-scripts` nav entry.

- 2026-09-10T18:00:00Z 🛑 BLOCKED — Re-checked before doing any work. `blocked_by`
  edge #3420 → #3419 still live; #3419 is still `OPEN` with the `blocked` label
  (itself blocked on #3418, also `OPEN`). #3420's scope still overlaps the same
  shared `workspaceNav.tsx` file both are concurrently editing, so the block
  reason is genuine and unchanged. No code changed, no labels/edges touched
  (already correct). Comment posted on #3420 confirming the re-check.

- 2026-09-11T15:52:00Z ✅ DONE — Re-checked again: #3419 and #3418 are both
  `CLOSED`/`complete`. Removed the stale `blocked_by` edge, flipped
  `blocked` → `in-flight`, resumed the actual work.

  Deleted `artifacts/admin-panel/src/pages/RunningScriptsPage.tsx` and the
  `cmd-running-scripts` nav entry (+ now-unused `PlayCircle` icon import)
  from `workspaceNav.tsx`. Also removed the dead
  import/case wiring to the deleted page from `CommandWorkspace.tsx`
  (`case "running-scripts"`) — required, otherwise the build fails to
  resolve the deleted file. Fixed a now-dead deep link in
  `crm/ProjectDetail.tsx`'s "Customer Submitted" kanban badge, which
  navigated to `/command/running-scripts?customerId=...`; simplified its
  onClick to always call `onCardClick(task)` (the same fallback it already
  used when no `clientUserId`), instead of routing into the now-deleted page.

  Verification: `tsc --noEmit` across `artifacts/admin-panel` passes clean.
  `pnpm run build` errors early on missing `PORT`/`BASE_PATH` env vars the
  dev-server normally supplies — unrelated to this change, not chased
  further since tsc already proves no broken imports/types from the
  deletion. Grep confirms zero remaining references to
  `RunningScriptsPage`/`cmd-running-scripts`/`/command/running-scripts`
  anywhere in `artifacts/admin-panel/src` or `test-manifests/`.

  Commit `d399962a9`, pushed clean (no rebase needed). `git status
  --porcelain` empty. `verify-branch-merged.mjs` confirms
  `agent/3420-q2129` is merged into `origin/main`.

  **Finding filed:** while auditing `RunningScriptsPage.tsx`'s API calls to
  confirm nothing else depended on them, found `/api/admin/script-runs*`
  has no backing Express route anywhere in `artifacts/api-server/src` —
  `RunResultDetailPanel.tsx:573`'s AI-analysis poll (still live, used by
  `ScriptGeneratorPage.tsx`) always 404s and fails silently. Pre-existing,
  out of scope for #3420, unrelated to this deletion. Filed as **#3664**
  under #1095 (no Feature-tier parent on #3420), labeled `bug`, milestone
  v1.1, board status set to AI Batter Up.

  Labels: `in-flight` → `complete` on #3420. Comment posted on #3420.
