# #3725 — chat-context [link 1] diagnostic doesn't distinguish benign ClaudeWebView null cycle from a real per-chat freeze

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 99207d0dd (rebased to 4c305c91a on origin/main)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — investigating ChatContextMeterScript.cs / MainWindow.xaml.cs `[link 1]` logging to tag the benign `wv==ClaudeWebView` on `/new`/`/` null-convId case so it's excluded from real-freeze alerting.
- 2026-09-12T00:00:00Z ✅ DONE — Found the `[link 1]` line in `MainWindow.xaml.cs` (`BT_CHAT_METER_DIAG` handler). `[link 3 BROKEN]` (the other diagnostic named in the issue title) never actually fires for `ClaudeWebView`: it's never registered in `_contextMeters`, so `UpdateContextMeter` early-returns via its own "no registered meterState" branch before reaching the empty-conversationId log — only `[link 1]` needed the fix. Added `isBenignUnattachedComposer`: true when `!matched` and either `ReferenceEquals(diagWv, ClaudeWebView)` (the confirmed case) or the wv has no live `_contextMeters` entry at all (generalizes to any future similarly-parked/unattached view, per the issue's suggested fix). The null-convId branch of the `[link 1]` log line now reads "BENIGN — this wv has no live ChatDocumentContainer/Band-1 gauge attached..." instead of unconditionally reading as the frozen-gauge root cause. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. No genuine new bug found beyond what the issue already described; no sub-issue filed. Working tree clean, branch verified merged into origin/main via `verify-branch-merged.mjs`.
