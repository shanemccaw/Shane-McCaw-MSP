# #1516 — Ownership / RACI: Holder model — roles are groups, job roles are people

- **Status:** 🚧 BLOCKED (no code changed this session)
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** n/a — this bookend commit is the only commit from this session

## Log
- 2026-08-31 ⏳ IN FLIGHT — Pulled the issue body plus its 4 comments in order. The
  body ("roles are groups, job roles are people": A/R must be individuals, C/I may
  be groups) is superseded by three later comments (2026-08-30) that redirect the
  actual work: #1523 settled that RACI attaches to the workload the tenant runs
  (Exchange, SharePoint, etc.), and this module's holder model has no real object
  rows to attach holders to until workload rows exist. Verified against the real
  code: `portal-ownership.ts` sources matrix rows only from `serviceObject()`
  (purchased `client_services`), Microsoft changes, change requests, and hold
  windows. Grepped `lib/db/src/schema/msp.ts` and `tenant-signals.ts` for
  `servicePlan`/`service_plan` — zero hits. The derivation rule Shane settled
  (final "CORRECTION" comment): a service plan enabled on the tenant
  (`/subscribedSkus`, `servicePlans[].provisioningStatus`) gets a RACI row; no
  consumption condition.
- 2026-08-31 🚧 BLOCKED — Per Shane's own explicit instruction on this issue
  ("if deriving the workload set is a build in its own right — it probably is —
  file it and block this issue on it rather than approximating"), filed the
  workload-derivation work as **#2008** ("Ownership / RACI: derive workload rows
  from the tenant's real M365 service-plan estate (subscribedSkus,
  enabled-only)"), parented to #1491, board status "AI Batter Up". Wired a real
  `blocked_by` dependency edge from #1516 → #2008 (`gh api
  .../1516/dependencies/blocked_by`). Labeled #1516 `blocked`, removed
  `in-flight`. Left a plain-language comment on #1516 explaining the block and
  what's needed next. No schema/route/UI changes made — building the holder
  model against today's purchased-service rows would have to be redone once
  #2008 lands real workload rows, which is the exact approximation the standing
  rule and Shane's comment both forbid. `git status --porcelain` clean apart
  from this bookend file. Not writing DONE: no real work shipped this session
  beyond the block itself, and the module's actual build (holder-reference
  schema/route/logic) remains fully unstarted pending #2008.
