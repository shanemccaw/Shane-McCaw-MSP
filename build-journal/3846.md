# #3846 — Node console windows keep spawning during concurrent builds

- **Status:** ✅ DONE 2026-09-12T21:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 238e3af42 (standing rule), 4d12160d4 (windowsHide fixes) — landed on main as 789fa3046 after rebase

## Log
- 2026-09-12T21:29:00Z ⏳ IN FLIGHT — Read #3846 body + comment. Adding no-visible-windows rule to the shared standing-rules template (desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md §4.1), then auditing repo scripts/package.json files for real Windows `start` usage that a normal build/test run would trigger.
- 2026-09-12T21:45:00Z ✅ DONE — Part 1: added the no-visible-windows bullet to `desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md` §4.1's `## Standing rules` BUILD-comment template (the canonical dispatch template all future `--title` builds inherit). Part 2: real audit of `scripts/**` + all `package.json` files found no literal Windows `start` command in any normal-path npm script, but found the real root cause of the reported symptom: `scripts/dev-all.mjs`'s three real service spawns (`runStep`, `startApiServer`, `startViteApp`) and `scripts/dev-server/server-process.mjs`/`refresh-main-server.mjs`'s `dev-all.mjs` launcher spawns all run detached/consoleless with no `windowsHide`, so each real console-subsystem child (node.exe running the api-server, cmd.exe hosting `pnpm run dev` for each Vite front-end) allocates its own new visible console window on Windows when no console is inherited — exactly the "3,4,5,6,7,8 new Node console windows" symptom across concurrent builds each triggering restarts. Also fixed `scripts/report-progress.mjs`'s literal `exec('start "" "<uri>"')` fallback (fires whenever the BuildConsole named pipe isn't listening) the same way — every progress-report call from every concurrent build was popping its own cmd.exe console. Fixed all of these with `windowsHide: true`. Two `.cmd` deploy scripts (`desktop/BuildConsole/deploy-shanesbuild.cmd`, `desktop/MyArchitect/deploy-myarchitect.cmd`) do use the Windows `start` command, but deliberately — they're manual, single-shot deploy scripts that open a Claude Code window for interactive git-cleanup review or launch the freshly-built app itself; both are explicitly meant to be watched interactively, so left as-is per the standing rule's own exception. Verified: `node --check` clean on all 4 edited `.mjs` files; `git diff --stat` reviewed before commit (27 insertions, 3 deletions, 4 files, all in scope). Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0); `git status --porcelain` clean.
