# #1773 — CR authorization gate does not verify the CR describes the pack being executed

- **Status:** ✅ DONE 2026-09-03T02:39:37Z
- **Scope:** platform

## Log

- 2026-09-03T02:35:31Z — Started. Investigated `change-control-write-gate.ts` /
  `config-pack-orchestrator.ts` / `sop-execution.ts`. Confirmed the reported gap:
  `claimChangeRequestForWrite` claims a CR scoped to `(mspId, tenantId)` only —
  neither `packKey` (config-pack path) nor `sopId` (SOP-run path) was ever
  compared against anything stored on the CR.
- 2026-09-03T02:39:37Z — ✅ DONE — commit `f38ea85b4` on `main` (rebased from
  `agent/1773-q1350` @ `bc8bd8652`). Added `msp_change_requests.authorized_target_key`
  (nullable text, `pack:<packKey>` / `sop:<sopId>`) —
  `lib/db/migrations/manual/2026-09-02-cr-authorized-target-key-1773.sql`, run
  against local Postgres and verified via `psql \d msp_change_requests`.
  `evaluateChangeRequestAuthorization` / `claimChangeRequestForWrite` now fail
  closed on a mismatch between a scoped CR's target and what the caller is
  actually about to execute; NULL preserves #1497's original tenant-granularity
  model unchanged for every pre-existing/general CR. Wired both real callers
  (`config-pack-orchestrator.ts`'s `pack:<packKey>`, `sop-execution.ts`'s
  `sop:<sopId>` — both the operator-claim and the policy-auto-raise-then-claim
  path). `policy-enactment.ts`'s auto-raised CR (#1550) now sets
  `authorizedTargetKey` itself — the one path fully closed today, since it's
  always raised for exactly one SOP. `msp-changes.ts` create route accepts an
  optional `authorizedTargetKey` so an operator can pre-scope a CR before
  handing its id to `execute_write_pack`. New unit tests in
  `change-control-write-gate.test.ts` (match / mismatch / unscoped-CR /
  unscoped-request / ordering).

  **Verification note:** could not run `vitest`/`tsc` — the shared main
  checkout's `node_modules` has lost its top-level symlinks (only `prettier`,
  `protobufjs`, `typescript` remain) and at least one `.pnpm` store entry
  (`vitest`) is an empty directory, both confirmed via direct filesystem
  inspection, pre-existing and unrelated to this change. Filed as **#2502**
  (parented under #1486, `AI Batter Up`). Verified instead by: full manual
  trace of every `claimChangeRequestForWrite` call site (3, including the one
  in `sop-execution.ts` that claims its own freshly-auto-raised CR — confirmed
  it now passes the matching `targetKey` so it doesn't reject its own row),
  the live migration + `psql` schema check, and reading the new column through
  to every existing test file that touches the changed functions (no assertion
  breaks on the new optional fields).

  No new findings beyond #2502.
