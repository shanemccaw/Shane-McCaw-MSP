# #1390 — Overview: 5 sections still 100% fixture with zero live branch

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** bd7eb0715 (bookend), 7fac4c1fa (wiring + tests + manifest), a1c1e7593 (DONE bookend; rebased onto #1391/#1392)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Wiring the 5 remaining fixture-only sections on the Tenant health overview (`portal-v2-overview.tsx`) to real data, each gated with the same `dataState === "live"` + honest-empty-state pattern Change Control / accepted-risks / holds already use on this page.
- 2026-08-27 ✅ DONE — All five wired, each with a live source found in-repo (no new endpoints needed):
  - **Microsoft changes** → `useMessageCenter().dataset.live` → new `mcLanesFromLive(posts)` (real M365 Message Center Graph pull; fixed-geometry bar + `impactTone`, ordered soonest-first by month). `dataset.live` is only true for a scoped tenant with ≥1 real post, so a live lane set is never empty.
  - **Project & release schedule** → `useProjectsLive()` (#1241) — `ProjectSchedule` now takes `rows`/`todayPct`/`contractEndPct` props instead of calling `pjRows()` with no argument; honest empty-state when a live tenant has no active project.
  - **Policy decisions** → `usePolicyDecisions()` (`/api/portal/policy-decisions`); `pdLanes` + `flaggedPolicyCount` now take the register as a parameter; honest empty-state when live-scoped with zero decisions.
  - **Evidence pack** → the tenant's REAL verified remediation: `useRemediationTracker()` verification map filtered to `state === "verified"`, joined to the remediation catalogue (`remediationData.ts`) by `stepId` via new `evidenceFromVerifiedSteps()`. A ticked-but-unverified step is a claim, not evidence, and is excluded; honest "No verified fixes yet" empty state (CTA buttons hidden) when none.
  - **Headline sub-line** → new `headlineSub(loaded, lastScanAt)` gates the design's "Pulled from your last scan…" copy on a real scan actually existing (honest "No scan on record yet" otherwise; nothing while the first read is in flight), rather than asserting it unconditionally.
  - **Verification:** `overviewModel.test.ts` +19 cases (77/77 PASS via `tsx --test`), directly covering `mcLanesFromLive`, `pdLanes(decisions)`, `headlineSub` and `evidenceFromVerifiedSteps`. `tsc -p tsconfig.json --noEmit` clean on all touched files. Manifest `test-manifests/portal/portal-v2-overview.json` extended with OV17–OV24 (sessionTag `overview-live-1390`) and its `feature`/`lastVerifiedAgainstCommit` updated.
  - **Live `shaneapp://runTest`:** documented deploy-ordering false negative. BuildConsole runs the dev-server checkout, which does NOT yet contain this session's commit (merge-back happens on build completion), so `onlyTag=overview-live-1390` returned "no steps matched" and the full-file run exercised the pre-#1390 code. The warm full run passed OV1–OV10 (page renders fully — title, scan band, drift chips, pillar strip, all toggles) and then aborted on the **pre-existing** critical step OV11 (change-control lane row), which is data-dependent (a live-scoped tenant with zero current change requests legitimately renders no CR rows — the live-empty branch from #1216) and untouched by this work. My own OV17–OV24 assertions were not reached. No Shane action required; the new steps go green once BuildConsole merges + deploys this commit.
