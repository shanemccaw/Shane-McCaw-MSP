# #1390 — Overview: 5 sections still 100% fixture with zero live branch

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** _pending_

## Log
- 2026-08-27 ⏳ IN FLIGHT — Wiring the 5 remaining fixture-only sections on the Tenant health overview (`portal-v2-overview.tsx`) to real data, each gated with the same `dataState === "live"` + honest-empty-state pattern Change Control / accepted-risks / holds already use on this page. Sources found: MS Changes → `useMessageCenter()` (`dataset.live`, real Graph Message Center posts); Project Schedule → `useProjectsLive()` (rows/todayPct/contractEndPct, #1241); Policy Decisions → `usePolicyDecisions()` (real `/api/portal/policy-decisions`); Evidence Pack → verified `remediation_tracker_steps` (`useRemediationTracker()` verification map, joined to the remediation catalogue by `stepId`); Headline sub → gated on real last-scan state. Building accordingly.
