# #4122 — BuildConsole QA auto-commit strands unpushed commits on the shared main checkout

- **Status:** ✅ DONE 2026-09-15T02:22:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 86a7285a5 (rebased onto main as 7e125412d)

## Log
- 2026-09-15T02:05:00Z ⏳ IN FLIGHT — Implementing option 3 from #4122: `UiAutomationQaSessionService.FinalizeAutomationSessionAsync`'s commit+push path now does fetch + rebase-onto-origin/<branch> + push with bounded retry (new `PushArtifactCommitWithRebaseAsync`), and on any failure path (rebase conflict, exhausted push retries) unwinds the local artifact commit via `git reset --keep origin/<branch>` so the shared checkout never diverges — artifact files survive as uncommitted working-tree changes instead of being lost. Also flipping `BuildConsoleSettings.AutomationQaAutoPushEnabled` default true→ so the fixed push path is actually exercised by default (the false default was the direct cause of the accumulating local-only commits documented in the issue).
- 2026-09-15T02:22:00Z ✅ DONE — `desktop/BuildConsole` built clean (`dotnet build -c Debug`, 0 warnings/0 errors). Rebased onto `origin/main` and pushed directly to `main` as `7e125412d`. `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean. No fixture/hardcoded data involved — this is a pure git-mechanics fix in BuildConsole's own automation service, no test-manifest scope (WPF app is explicitly out of that scope per CLAUDE.md).
