# #2615 — M365 Launch Control: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T04:31:50Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ba986295d (IN FLIGHT bookend), 22ee0bf7a (real work — Launch Control console module + route extensions, merged into origin/main)

## Log
- 2026-09-14T04:14:53Z ⏳ IN FLIGHT — pulled #2615 body + BUILD comment (Posted 2026-09-14T03:44:00Z). Design export confirmed present at `Design/MSP_Console/design_handoff_msp_console/Launch Control.dc.html` (screen 19). Building the Launch Control screen in `artifacts/msp-console/src` wired to `routes/msp-launch-control.ts`; confirming live template readiness from the DB before rendering.
- 2026-09-14T04:31:50Z ✅ DONE — Shipped `artifacts/msp-console/src/console/modules/LaunchControl.tsx` + `src/api/launch-control-api.ts`, mounted at `/tenants/:id/lc` (the existing `lc` leaf in `nav.ts`) via `ConsoleShell.moduleFor`, with the design's exact header copy in `treeModel.pageMeta` (embedded contract: no own header; no review-only tenant/tier/licence toggles ported). Actions tab: domain-grouped catalog, four-state availability pills, live licence line, five filters incl. "Needs a licence", confirmation drawer with seven pre-flight rows that disable Run on any red row, required-variable inputs, and the real execute outcome (incl. `license_gap`). "What has been run" tab: real audit rows with arm-then-confirm Undo wired to rollback where a paired reverse step exists. Readiness copy reads each row's real `status` — live catalog is 80/125 `execution_ready` (8 reversible), not the design's 0. Backend (`routes/msp-launch-control.ts`): GET actions now also returns `tenant` (isTestbed/connected), `writeBack` (MSP switch + writeBack consent status), `licenseRead.error`, and per-action `templateLabel`/`templateDescription`/`reversible`; new `GET /msp/:mspId/launch-control/history?customerId=`; rollback now carries execute's testbed restriction and refuses an audit row whose tenantId isn't the named customer's. No schema change, no migration. Verification: `tsc --noEmit` 0 errors on msp-console (before and after rebase) and api-server; `vite build` succeeds. Live against an isolated worktree api-server on :8099 with a synthetic `zz-test-lc2615@` MSPOperator (deleted afterwards): actions 200 (125 rows, tenant/writeBack/licenseRead present, 11 licence-gated rows, Entra P1/P2 unsatisfied), history 200 (21 rows), no token 401, wrong mspId 403, rollback of non-reversible row 48 passed the new gate and was refused 400 by the executor (no write). No execute or Undo was fired (real tenant). No browser render check (no playwright in repo). No test manifest (msp-console is out of manifest scope). Finding filed: #4023 (MSP 1 has no platform subscription + testbed customer has no monitoring service → all 125 actions resolve a_la_carte locally). Working tree clean.
