# #3322 — Apple Health bridge via Shortcuts automation (oxygen, heart rate -> shanes-life)

- **Status:** ✅ DONE 2026-09-09T13:00:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** a228de621 (merged into main; rebased once over an unrelated concurrent sibling merge, no functional conflicts)

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — Read issue body. Real scope: HealthKit is native-iOS-only,
  so the bridge is a real Shortcuts automation POSTing to a new authenticated ingestion endpoint.
  Studied the existing real precedent for exactly this shape: `tesla_hook_tokens`
  (migration 055) + `/hooks/tesla/:token` (src/routes/tesla.mjs, src/core/tesla.mjs) already
  solves "external iOS Shortcuts automation posts to a bearer-token webhook, no session cookie."
  Also read `widget_tokens` (migration 046) for the same one-established token shape, and
  Settings -> Connected (public/app.js ~9496-9686) for where a new external-data-source section
  belongs, per the issue's own instruction not to build a new dedicated room for two numbers.
  About to build: migration 072 (`health_metric_hook_tokens` + `health_metrics`), a new
  `src/core/health-metrics.mjs` + `src/routes/health-metrics.mjs` mirroring tesla.mjs's hook-token
  section, session-authed CRUD + recent-readings routes in api.mjs, a Settings -> Connected
  "Apple Health" section, and a real, documented Shortcuts recipe. Correlation with #3321's
  medication log is explicitly deferred per the issue body -- not building it this session.
- 2026-09-09T13:00:00Z ✅ DONE — Built migration 072 (`health_metric_hook_tokens` +
  `health_metrics`, run against local Postgres myself: `applied=1 alreadyApplied=58`),
  `src/core/health-metrics.mjs` (hook-token issue/list/revoke + `recordMetric`/`listRecent`,
  copying `tesla_hook_tokens`'s established shape exactly), `src/routes/health-metrics.mjs` +
  server.mjs wiring for the real bearer-in-path ingestion webhook
  `POST /hooks/health-metrics/:token`, session-authed `GET/POST/DELETE
  /api/health-metrics/hook-tokens` + `GET /api/health-metrics` in api.mjs, a real Settings ->
  Connected "Apple Health" section in app.js (token management + recent readings, no new
  dedicated room per the issue's own instruction), and a real, documented Shortcuts recipe
  (`docs/apple-health-shortcuts-recipe.md`) plus a README table row.
  **Verification**: `npm run migrate` applied 072 cleanly; booted the real local server against
  the real local `finances` Postgres and, using the established disposable
  `agent-verify@example.com` test user, ran a throwaway script (deleted after) that hit the real
  HTTP endpoint end-to-end -- valid spo2/heart_rate POSTs returned 201 with the real inserted
  row, an out-of-range spo2 value returned 400, a bogus/revoked token returned 404, and
  `listRecent` returned exactly the two real rows just inserted; deleted the test rows and
  revoked/removed the test token afterward. `npm run check` against the real local server/DB:
  217/221 -- the same 4 pre-existing `SL_VAULT_KEY` failures #3320's own bookend already names
  as unrelated to this checkout, no new failures. Real, honest scope cut: correlation with
  #3321's medication log (issue item 5) was explicitly deferred, not built. `git status
  --porcelain` clean (the local-only `.env` copied in for the live verification run and the
  throwaway verify script were both removed before finishing); `verify-branch-merged.mjs`
  confirms `agent/3322-q2046` (a228de621) is an ancestor of `origin/main`.
