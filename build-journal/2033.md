# #2033 — Build Watch: real progress reporting, then dual-mode panel/window

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 7a1de26a4 (IN FLIGHT), <fill after DONE commit>

## Log
- 2026-08-31 ⏳ IN FLIGHT — Starting: real audit of BuildProgressTracker.cs / ChecklistProgressBridge.cs /
  ChecklistExtractor.cs / MainWindow.ShaneAppReportProgress.cs to confirm the step=0 root cause diagnosed
  in the issue's superseding comments, then fix `Report()` to derive an effective, monotonic step from
  history count when the agent-supplied step is stuck, without disturbing explicit-vs-bridge precedence.
- 2026-08-31 ✅ DONE — Re-confirmed the diagnosis by direct code read of all four named files:
  `BuildProgressTracker.Report()` really does set `report.Step = step` directly (raw, unclamped) while
  its history-append dedupe fires on label change, exactly as the issue's superseding comments describe.
  Real-telemetry check (`%AppData%\BuildConsole\logs\activity-*.log`, Aug 17–31, 1,651 explicit
  reportProgress calls across 437 multi-call builds): **zero** explicit calls with a stuck/step=0 value
  were found — every `step 0/N` line in that window came from the checklist-bridge's legitimate "0 of N
  done" state (isExplicit=false), not the explicit path. So the exact symptom is a real, confirmed
  latent code bug (fixed regardless, since it's cheap/safe and self-heals rather than relying on agent
  compliance per the issue's own instruction), not one observed happening in practice in this log
  window — reporting this honestly rather than claiming it was "actively blocking" real builds.

  Fix: `BuildProgressTracker.Report()` now derives `effectiveStep = Math.Max(step, historyFloor)` for
  explicit reports only (`isExplicit=true`), where `historyFloor` is the count of distinct milestones
  already recorded (own +1 when this call adds a new one, keyed on the same label/step-change criterion
  the history dedupe already used). `report.Step`/`report.Total` are derived from `effectiveStep`; the
  checklist-bridge path (`isExplicit=false`) is untouched — a genuine "0 of N done" there is not a stuck
  value to correct. History rows keep the RAW step (not the corrected value) so the dedupe comparison on
  the next call stays raw-vs-raw. The raw agent-supplied step stays visible in the activity log
  (`(raw step=N)`) whenever it was overridden, per the issue's own diagnostic-trail requirement.

  Also added a short defense-in-depth note to `desktop/BuildConsole/AGENT_PROTOCOLS.md` §8 (step is
  never `0`; the tracker self-heals but agents shouldn't rely on it).

  Verification: `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. Real simulated run via a
  scratch console harness (outside the repo, deleted after use) driving the actual
  `BuildProgressTracker.Report()`/`BridgeFromChecklist()` static methods through 4 scenarios: (A) stuck
  step=0 with changing labels — self-heals 1→2→3→…→6, duplicate re-report doesn't over-advance, final
  correct call reaches 100%/IsComplete; (B) a well-behaved agent's normal 1/3→2/3→3/3 sequence — bit-for-
  bit identical to pre-fix behavior, confirming no regression; (C) checklist-bridge's legitimate "0 of 4
  done" — stays at Step=0, NOT corrected; (D) explicit-vs-bridge precedence lockout — a bridge attempt
  while an explicit report is active is still ignored, report unchanged. All four passed as expected.
  `git status --porcelain` clean; branch verified an ancestor of `main` via direct push (worktree
  fast-forwarded onto origin/main before pushing, no rebase needed).
