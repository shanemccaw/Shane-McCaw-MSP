# #1837 — Chat tab context menu: "Start a new chat on Epic #N" — handoff pointer

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** a1f1c8cb7 (bookend IN FLIGHT), cc1f60f20 / pushed as 71b5d22bc (real work — worktree got swept between the two, see log)

## Log
- 2026-08-30 ⏳ IN FLIGHT — factoring 5 duplicated `bt_prefill` URL builders into `Services/EpicChatUrlBuilder.cs`, adding chat-tab context menu item "Start a new chat on Epic #N" with handoff pointer, and fixing `TriggerHandoffAsync` to pass the predecessor chat URL.
- 2026-08-30 ⚠️ mid-session the worktree got reset back to `origin/main`'s tip between my IN FLIGHT bookend push and my first implementation commit (uncommitted work lost, matches the known "direct-session worktrees get swept" hazard) — redid the implementation from scratch against the then-current file state (line numbers had shifted from unrelated concurrent commits; content at each of the 5 sites was otherwise unchanged) and committed immediately this time.
- 2026-08-30 ✅ DONE — `desktop/BuildConsole/Services/EpicChatUrlBuilder.cs` added: single `BuildEpicChatUrl(baseUrl, pat, epicNumber, handoffFromChatUrl = null, label = null)` builder. All 5 duplicated `bt_prefill` construction sites replaced (`MainWindow.xaml.cs` `MenuNewChat_Click` + `TriggerHandoffAsync`, `MainWindow.GitDetailTabs.cs` `OpenOrCreateEpicChat`, `Controls/LeftSidebar.xaml.cs` milestone + issue/epic "New Chat" menu items — the now-unused `EpicChatPrefillParam` const on the latter was removed too). Added `🧵 Start a new chat on Epic #N` / `🧵 Start a new chat on an Epic...` to the chat-tab context menu's existing section 0a (same `isChatTab`-gated `cm.Opened` pattern as the two neighbours), backed by new `ResolveEpicNumberForTab` (tab GithubNumber → `LeftSidebar.GetEpicForChat` → `GetEpicByGithubNumber`, same order `TriggerHandoffAsync` already used) and `StartSuccessorChat` (reuses `AssignChatTabToIssueAsync`'s exact "no conversation id yet" warning; falls back to `NewChatEpicDialog` when the epic can't be resolved; never touches the predecessor tab). Fixed `TriggerHandoffAsync` to pass `chat.ClaudeUrl` (falling back to `TryGetChatUrlForTab(oldTab)`) as the handoff pointer, logging via the method's own existing `system.core.chat-context` channel when no URL resolves rather than firing silently without one.
  - **Verify:** `dotnet build desktop/BuildConsole/BuildConsole.csproj` — 0 Warnings, 0 Errors.
  - **Verify:** `grep -rn "bt_prefill=" desktop/BuildConsole --include=*.cs` returns exactly one real construction site (`EpicChatUrlBuilder.cs:60`) plus the two pre-existing doc-comment mentions; the five duplicated construction sites are gone.
  - **Verify — byte-identical for the 4 no-handoff callers:** confirmed by direct code-path comparison rather than a runtime harness (the string-building is pure/deterministic with no external state, so tracing both sides is conclusive). `MenuNewChat_Click`, `OpenOrCreateEpicChat`, and `TriggerHandoffAsync`'s pre-#1837 shape all built `label = $"Epic #{n}"` then `prefill = pat-empty ? label : "{pat}\r\n{label}"` then `"{baseUrl}{sep}bt_prefill={EscapeDataString(prefill)}"` — identical to the builder's `label ??= $"Epic #{epicNumber}"` + null-handoff branch. The two `LeftSidebar` sites (milestone: `"Milestone #{n}"`, issue/epic: `"{issueType} #{n}"`) needed a label that isn't "Epic #N", so the builder gained an optional `label` param (not in the issue's literal 4-arg signature, but the issue's own goal — "keeps the existing behaviour exactly when handoffFromChatUrl is null" — is unreachable for those two sites without it, since the existing label text genuinely varies; both now pass their own pre-computed label through unchanged) — with that, output is byte-identical there too. `TriggerHandoffAsync` is the 5th/excluded site: it now always attempts a handoff pointer, so its output is intentionally different from before (that's the fix), never tested for byte-identical no-handoff output.
  - **Verify:** the new context menu item is added to `cm.Items` inside the same block and toggled by the same `visibility` variable as `miInProgress`/`miAssignIssue`/`chatActionsSeparator` — a non-chat tab hides all four together by construction.
  - Branch `agent/1837-q951` confirmed merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs` (OK, 71b5d22bc). `git status --porcelain` clean.
