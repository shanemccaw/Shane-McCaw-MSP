# #3387 — msp-sales-offers.ts and msp-sales-bundles.ts allow PlatformAdmin ?mspId= cross-MSP override on session-scoped routes

- **Status:** ✅ DONE 2026-09-10T21:33:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** ce1a714d893d16365d0f7429361029a70cca9b36

## Log
- 2026-09-10T21:21:47Z ⏳ IN FLIGHT — Same class as #2731. Fixing the 5 flagged routes in
  `msp-sales-offers.ts` (swap `resolveMspId(req)` → `resolveMspIdStrict(req)`) and rewriting
  `msp-sales-bundles.ts`'s private `getMspId()` to `resolveMspIdStrict(req)` across all 11 of
  its routes, per the issue's own fix direction.
- 2026-09-10T21:33:00Z ✅ DONE — Swapped `resolveMspId(req)` → `resolveMspIdStrict(req)` on
  the 5 flagged `msp-sales-offers.ts` routes (generate, events, patch, patch/state, delete);
  deleted `msp-sales-bundles.ts`'s private `getMspId()` re-implementation entirely and
  replaced all 11 of its call sites with the canonical `resolveMspIdStrict(req)` — none of
  that file's routes carry a `:mspId` path segment. `npx tsc --noEmit -p artifacts/api-server`
  is clean (zero errors reference either file). Full diff reviewed — scope matches exactly
  what the issue's own body flagged, nothing extra touched.
  Commit `ce1a714d8` on `main`, verified as ancestor of `origin/main` via
  `verify-branch-merged.mjs`. Working tree left clean.
  Filed one new finding discovered mid-build (same class, one route the issue's own fix
  list deliberately excluded): #3545 — `GET /msp/sales-offers/sse` still hand-rolls an
  inline `?mspId=` override for PlatformAdmin, no `:mspId` in path. Parented under #1571
  (no Feature-tier parent on #3387 itself), labeled `bug` + `security`, board status set
  to AI Batter Up.
