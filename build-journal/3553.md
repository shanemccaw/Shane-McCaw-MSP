# #3553 — Move DispatchPanel out of docked layout into a Ctrl+D modal dialog

- **Status:** ✅ DONE 2026-09-10T22:53:55Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 553354103 (rebased/pushed as 7166ac2b4 on origin/main)

## Log
- 2026-09-10T22:49:44Z ⏳ IN FLIGHT — investigated DispatchPanel.xaml/.cs, MainWindow docking
  (line ~1147) and wiring (Dispatched/Initialize/RecheckPendingBuildCommentsAsync). Confirmed
  Ctrl+D is free (existing binding is Ctrl+**Shift**+D → Sailor Duck mascot, a different chord).
  Plan: host the existing DispatchPanel UserControl instance (unchanged logic) inside a new
  DispatchDialog.xaml modal Window opened via Ctrl+D, remove its docked slot from
  MainWindow.xaml, restart its countdown timer on re-Loaded since Unloaded fires each time the
  dialog closes.
- 2026-09-10T22:53:55Z ✅ DONE — Implemented: new `DispatchDialog.xaml`/`.xaml.cs` (modal Window,
  `SizeToContent`, `WindowStartupLocation="CenterOwner"`, Escape-to-close) hosts the SAME
  app-lifetime `DispatchPanel` instance MainWindow already Initialize's once (now created in code
  under the identical field name instead of an XAML `x:Name`, so every existing call site —
  `Initialize`, `Dispatched`, `RecheckPendingBuildCommentsAsync` — is untouched). Removed
  `DispatchPanel`'s `DockPanel.Dock="Top"` slot from `MainWindow.xaml` so `BuildQueuePanel` below
  genuinely reclaims the space. Wired global `Ctrl+D` (no Shift) in `Window_PreviewKeyDown` to
  `OpenDispatchDialog()`; confirmed no collision — the only existing Ctrl+D-adjacent binding is
  Ctrl+**Shift**+D (Sailor Duck mascot), a distinct chord. `DispatchPanel` gained a `Loaded`
  handler (mirroring `Initialize`'s timer-start block) to restart the auto-restart countdown timer
  each time it's re-parented into a fresh dialog, since `Unloaded` now fires on every dialog close.
  All existing behavior preserved unchanged: issue-number dispatch, "#N is already tracked"
  message, "Dispatch anyway" override (Git #1966), same `BatterUpQueueService.FindBuildCommentAsync`
  / `BuildQueuePostgresClient.QueueBuildAsync` calls — this was a presentation-only change.
  **Verification:** `dotnet build -c Debug` — Build succeeded, 0 Warning(s), 0 Error(s). Manual
  interactive Ctrl+D launch/click-through wasn't run in this non-interactive session (no
  WebView2/BuildConsole runner attached here); the build and a full source read of the wiring are
  the real verification performed. `verify-branch-merged.mjs` confirms `agent/3553-q2227` is an
  ancestor of `origin/main` (commit 7166ac2b4). `git status --porcelain` clean. No findings filed
  — no genuine bug/gap surfaced beyond the change itself.
