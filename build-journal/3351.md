# #3351 — Policy Decisions (MSP Console) contract pack regen — #3034 PATCH landed

- **Status:** ✅ DONE 2026-09-09T21:38:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 25a2788c3 (regenerated pack), 1ad1e5f8e (IN FLIGHT bookend)

## Log
- 2026-09-09T21:23:06Z ⏳ IN FLIGHT — regenerating `docs/policy-decisions-msp-console-contract-pack.md`
  per the issue body: real full line-by-line re-audit of `msp-standing-policies.ts` (now
  6 routes, `PATCH /msp/standing-policies/:id` from #3034 added), `msp-policy-decisions.ts`,
  `msp-policy-engine-settings.ts` against the current backend + live local Postgres state.
- 2026-09-09T21:38:00Z ✅ DONE — regenerated `docs/policy-decisions-msp-console-contract-pack.md`
  (commit 25a2788c3), full re-audit: pulled and re-read `msp-standing-policies.ts` in full
  (now 6 routes, 596 lines), `msp-policy-decisions.ts`, `msp-policy-engine-settings.ts`,
  `standing-policies.test.ts`; added new §3.4 for #3034's `PATCH /:id`; renumbered §3.5-3.8
  and every cross-reference; re-verified every route file:line citation against current code
  (line numbers shifted after the PATCH insertion); re-ran live Postgres checks (row counts
  unchanged, zero CHECK constraints on `standing_policies`, both FK set-nulls
  `msp_diagnostic_findings`/`msp_sop_runs` and the `policy_evaluation_runs` cascade
  re-confirmed); re-ran the repo-wide consumer grep (still zero AdminV2/MCP/msp-console
  consumers). §6 status table and §7.4 updated to reflect #3034 and #3035 both CLOSED with
  real fixes landed (PATCH route + corrected code comment respectively). No new finding
  filed this pass — the re-audit surfaced no gap beyond the already-tracked, still-open
  design questions (§7.2 createdByPersonId omission, §7.3/PATCH's inherited targetState
  validation gap, §7.4's narrower DELETE-vs-deactivate-only question left open by #3034
  itself). Branch verified ancestor of origin/main
  (`node scripts/dev-server/verify-branch-merged.mjs`, exit 0); `git status --porcelain`
  clean. Issue #3351 commented and labels synced (`complete`, `in-flight` removed).
