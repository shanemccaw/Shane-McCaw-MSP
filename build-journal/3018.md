# #3018 — Contract Pack: full-page tools (Log Viewer, Git Doctor, Local/Git Map) — BuildConsole

- **Status:** ✅ DONE 2026-09-06T19:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 8414fcae3 (contract pack), 314a83a21 (IN FLIGHT bookend)

## Log
- 2026-09-06T19:26:07Z ⏳ IN FLIGHT — Auditing the real full-tab tools named in #3018: Log Viewer (`Controls/LogViewerDocumentView.xaml(.cs)` + `Services/LogService.cs`; the issue's `LogViewerDock` name is stale), Git Doctor (`Controls/GitDoctorView.xaml(.cs)`), and Local Map / Git Map (`BuildChainMapWindow.xaml(.cs)` + `BuildChainMapWindow.Inspector.cs`) to author `docs/buildconsole-fulltools-contract-pack.md` from the real code, cited to file:line.
- 2026-09-06T19:45:00Z ✅ DONE — Wrote `docs/buildconsole-fulltools-contract-pack.md` (commit 8414fcae3), authored entirely from reading the real code cited to file:line. Covers all three tools:
  - **Log Viewer** — `LogViewerDocumentView.xaml(.cs)` + `LogService.cs`: hosted as an Editor tab (`MainWindow.OpenLogViewerTab`), the 9 real `LogSource`s (marketing/portal/admin dev-all logs, api=`platform_log_stream`, sql=live Postgres server log dir, build=queue logs, console=in-mem sink; ssh/terminal honestly empty), the COLD/BURST(30s self-cancel)/LIVE stream model + status pill, per-source inferred-vs-real level rule, filter bar (search/regex/exclude/HIGHLIGHT-dims/level pills/scrubber/saved filters), pin/scratch-pad-survives-filter, right inspector, Sources/Archive rail (day→build→bookend tree; documented stdout.log-leaf gap), send-to-chat wiring.
  - **Git Doctor** — `GitDoctorView.xaml(.cs)` + `GitDoctorService.cs`: now a full-width Editor tab (#2809 superseding the stale "sidebar" XAML comment), 6 real detectors (lock/dirty/diverged/worktree detached+prunable/auth/stale≥25) run against the real repo, the two deliberately-unimplemented mockup checks, severity/risk enums + colours, remedy cards, the auth paste-PAT card that also writes `BuildConsoleSettings.GitHubPat`, branch section, commit lookup, run log + Claude extract/plan bridge (clipboard, no composer).
  - **Local/Git Map** — `BuildChainMapWindow.xaml(.cs)` + `.Inspector.cs` + `Services/BuildMap/*`: the shared map picker (Git Map vs Local Map), the Git Map's own dark-blue token palette, real GitHub data path (sub-issues + blocked_by + board Status + DoneBookendVerifier + BUILD-comment model/effort), 7 stat chips, chain-integrity pill, the 5 node states + derivation, the 4 Inspector views verbatim, and the real GitHub write-back+audit persistence (#2481/#2486/#2498). Local Map summarised as its own future pack.
  - Verification: docs-only change — no build/test-manifest applies (BuildConsole is out of scope for test manifests per CLAUDE.md; nothing customer-facing touched). Branch verified ancestor of origin/main via verify-branch-merged.mjs (8414fcae). Working tree clean. No new findings filed (audit surfaced only already-known/in-code gaps: ssh/terminal no backing, stdout.log archive leaf, two unimplemented Git Doctor checks — all documented in the code itself, none new).
