# #3947 — Launch Control: proactive license-gate mechanism for license-dependent actions

- **Status:** ✅ DONE 2026-09-13T07:32:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 359b4bf00 (rebased/pushed as bcf606490 on origin/main)

## Log
- 2026-09-13T06:56:33Z ⏳ IN FLIGHT — read #3947's body + all comments. Confirmed the
  real decided scope (Shane's decision comment): schema column
  `required_license_skus` on `write_action_catalog`, a `/subscribedSkus` Graph read
  helper, backfill of the real CA-domain rows, server-side enforcement re-validating
  before execute, and UI "License required" labeling extending Section 8's existing
  availability states. Confirmed the `--blocked-by 3937` flag was explicitly
  retracted by Shane's own follow-up comment ("no real blockers... proceed without
  waiting") and confirmed via `dependencies/blocked_by` API that no edge exists.
  Also read #3937's own session note: it shipped a smaller reactive fix
  (`graphWriteForTenant` classifies license-gap write errors as `errorType:
  "license_gap"`) but explicitly did NOT touch schema/backfill/precheck/UI — this
  build's scope is fully unclaimed. Starting investigation: locate
  `write_action_catalog` schema, existing Graph read helpers, and Section 8's
  availability labeling code before writing anything new.
- 2026-09-13T07:00:00Z ⏳ IN FLIGHT — investigation complete (dispatched
  Explore agent + manual follow-up). Real findings that shaped scope:
  `write_action_catalog` had no license column at all (confirmed live);
  no existing live `/subscribedSkus` helper (only a cached
  monitoring-snapshot resolver, `license-waste-source.ts`, too stale for an
  execute-time gate) — `account-security-graph.ts`'s
  `getDeviceComplianceSignal` already has the exact live-read pattern to
  reuse; the issue's own "Section 8" doc / 4-state availability model
  doesn't exist anywhere in the repo — `Availability` is a real, live
  3-state enum (`msp-launch-control.ts`) with **zero frontend anywhere**
  consuming Launch Control at all (grepped every `artifacts/*` workspace);
  2 of the 7 CA template ids named in the dispatch have no
  `write_action_catalog` row to backfill onto. Proceeding: schema column →
  migration (run myself against local DATABASE_URL) → live SKU-read helper →
  wire both routes → live-verify against testbed → file the template-gap
  finding.
- 2026-09-13T07:15:00Z ⏳ IN FLIGHT — implemented and ran locally: Drizzle
  column `requiredLicenseSkus` on `writeActionCatalogTable`
  (`lib/db/src/schema/msp.ts`); migration
  `lib/db/migrations/manual/2026-09-13-write-action-catalog-required-license-skus-3947.sql`
  run against local `DATABASE_URL` — `ALTER TABLE ADD COLUMN IF NOT EXISTS`
  + backfilled all 9 live `domain = 'Conditional Access'` rows to
  `["AAD_PREMIUM", "AAD_PREMIUM_P2"]` (`UPDATE 9`, confirmed via psql
  read-back). New `artifacts/api-server/src/lib/license-gate.ts`:
  `getSubscribedSkuPartNumbersForTenant` (live Graph read, 60s per-tenant
  TTL cache matching `writePermissionCache`'s pattern),
  `tenantHasRequiredLicense` (the single shared membership-check helper),
  `describeRequiredLicense`. `msp-launch-control.ts`: `Availability` is now
  a real 4-state union incl. `"license_required"`; GET `/actions` adds a
  `licenseRequirement` reason per action; POST `/execute` reordered to
  resolve `customer.tenantId` before the availability check and blocks
  proactively (409, `errorType: "license_gap"`) before ever raising a
  Change Request or attempting the write — #3937's reactive branch stays as
  defense-in-depth. 12 new tests in `license-gate.test.ts`, all passing;
  `tsc --noEmit` clean; full api-server suite 366/372 files (9 failed
  tests, all the same pre-existing `purchase-account-flow.test.ts`
  `users_role_scope_check` failures #3937's own session already documented
  as unrelated — none touch the files this build changed).
- 2026-09-13T07:25:00Z ⏳ IN FLIGHT — live-verified end-to-end against the
  real testbed tenant (`c4c814d4-3afe-441e-9145-62461d0a4fd3`) via a scratch
  script (`src/scripts/verify-3947-license-gate.ts`, deleted after use, same
  convention as #3937's own bookend): confirmed the tenant genuinely still
  lacks `AAD_PREMIUM`/`AAD_PREMIUM_P2` (real enabled SKUs: `ENTERPRISEPACK`,
  `FLOW_FREE`, `POWER_BI_STANDARD`, `Power_Pages_vTrial_for_Makers`), and
  the real chain (live SKU read → `tenantHasRequiredLicense` →
  `describeRequiredLicense`) correctly computes `license_required` for the
  real CA catalog row `action.create-ca-signin-risk-policy` (id 139), with
  `describeRequiredLicense` producing the exact expected text "Requires
  Microsoft Entra ID P1 or P2". Did not fire a full authenticated HTTP POST
  through the route (would need test JWT/msp/customer setup beyond this
  scope, and the running dev-server instance is on the old, unmerged code
  anyway) — the scratch script exercises the identical functions the route
  calls with no logic between, which is the meaningful live check here.
  Filed finding #3958 (two active CA MFA `baseline_action_templates` rows
  with no `write_action_catalog` row to backfill onto), parented under
  #2494, labeled `bug`, board status set to AI Batter Up. Posted follow-up
  comments on #3937 and #3938 per the dispatch's own instruction (not
  closing either — Shane's call).
- 2026-09-13T07:32:00Z ✅ DONE — committed as 359b4bf00, rebased/pushed as
  bcf606490 on origin/main. Branch verified merged
  (`verify-branch-merged.mjs`); working tree clean
  (`git status --porcelain` empty). Files changed: `lib/db/src/schema/msp.ts`,
  new migration file, new `license-gate.ts` + `license-gate.test.ts`,
  `msp-launch-control.ts`, `docs/msp-console/msp-launch-control-contract-pack.md`
  (addendum documenting the new 4-state enum + #3937's earlier
  `GraphWriteErrorType` change, without re-auditing every pre-existing line
  citation in that pack). No UI wired — none exists yet for Launch Control
  anywhere in the repo (confirmed, not a gap this build introduced).
