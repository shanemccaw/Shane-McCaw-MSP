# #2476 — Build Chain Map: chain-rules engine (§5 logic)

- **Status:** ✅ DONE 2026-09-03T00:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 1eef08d91 (rebased/pushed as cb8d9ffe1 on origin/main)

## Log
- 2026-09-02T23:50:00Z ⏳ IN FLIGHT — item #2 of #2473's structured index (blocked_by #2475, which is
  DONE — unblocked). Building the pure-logic chain-rules engine on top of #2475's real `ChainDoc` C#
  model (`Services/BuildMap/ChainDoc.cs`): `RechainFanin`/`RechainGates`/`RechainAll`/`Derive`, ported
  1:1 from `Build Chain Map.dc.html`'s reference `class Component` (`rechainFanin`, `rechainGates`,
  `rechainAll`, `derive`) per BUILD_QUEUE_METHOD.md §5 exactly — no UI, pure data transforms on the
  ChainDoc graph. Plan: new `Services/BuildMap/ChainRules.cs`, `dotnet build` clean, then a real audit
  against tonight's actual ShaneBuilder Epic #2198 → Test Pad cascade via `BuildChainMapService.BuildAsync`
  (live GitHub data, no fixtures).
- 2026-09-03T00:20:00Z ✅ DONE — shipped `Services/BuildMap/ChainRules.cs`: `ChainRules.RechainFanin`,
  `RechainGates`, `RechainAll`, `Derive` (plus the `Cascade`/`PickSentinel`/`Push`/`FindFeature`/
  `FeatureOf`/`FindIssue` helpers the JS prototype's `class Component` also carries), and the new
  `ChainNodeState`/`ChainFeatureSummary`/`ChainTotals`/`ChainDerived` result types `Derive` returns.
  `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors.
  `git status --porcelain` clean; branch verified merged via `scripts/dev-server/verify-branch-merged.mjs`.
  Real finding mid-build: `Build Chain Map.dc.html`'s own `SPEC`/`seed()` (2198/ShaneBuilder/Test Pad
  worked example numbers) are explicitly documented placeholders (README.md: "Feature and issue numbers/
  titles in the file are placeholders") — real GitHub Epic #2198 is actually "ShaneBuilder — App Shell v2
  Rebuild" with no Test Pad Feature under it right now. The real "Feature: Test Pad" is #2326, parented
  to Epic #1202 (Epic: Build Console), confirmed via `get_parent`. Used #1202/#2326 as the live test case
  instead, per the issue's own "verify against tonight's actual Test Pad cascade" instruction.
  Real audit against live GitHub data via a throwaway console harness (scratch, not committed, deleted
  after use — referenced `BuildConsole.csproj` as a `ProjectReference`, ran with `GIT_PAT` from
  `.env.local`):
    - `BuildChainMapService.BuildAsync(client, 1202)` → 99 Features, 346 issues, 219 real `blocked_by`
      edges read.
    - Feature #2326 "Test Pad": 34 real issues, all in cascade (0 Ask). `ChainRules.PickSentinel` picked
      #2468, exactly matching #2475's independently-derived `DefaultSentinel` on the same Feature — the
      two §5.2-step-1 implementations agree on real data.
    - `ChainRules.RechainAll` + `Derive` over the whole real 99-Feature/346-issue doc: edges went from
      219 → 771 (319 fanin + 278 gate + 174 manual); `Derive` reported 47 gaps. Traced by hand: every one
      of the 47 traces to a genuinely empty-cascade Feature (72 of the 99 real Features currently have 0
      cascade issues — real, sparse GitHub state, not a bug) gating the next Feature's cascade, exactly
      as the spec's own gap-counting rule defines a gap — zero *spurious* gaps.
    - Spot-checked every fan-in edge targets its own Feature's real sentinel, and every gate edge runs
      from a Feature's sentinel to the immediately-next Feature's real cascade, across all 771 edges —
      both held for every edge, not just a sample.
  Honest gap: no unit-test project exists for `desktop/BuildConsole` (CLAUDE.md's Test Coverage section
  scopes automated test manifests to `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting` only, so
  none was added here) — verification is the live audit above, not a committed regression test. Items
  #3-#9 of #2473's structured index (canvas/edge rendering, inspector, interactions, persistence, top
  bar, toolbar wiring) remain separate sibling issues (#2477-#2483), not in scope here.
