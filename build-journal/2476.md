# #2476 — Build Chain Map: chain-rules engine (§5 logic)

- **Status:** ⏳ IN FLIGHT 2026-09-02T23:50:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T23:50:00Z ⏳ IN FLIGHT — item #2 of #2473's structured index (blocked_by #2475, which is
  DONE — unblocked). Building the pure-logic chain-rules engine on top of #2475's real `ChainDoc` C#
  model (`Services/BuildMap/ChainDoc.cs`): `RechainFanin`/`RechainGates`/`RechainAll`/`Derive`, ported
  1:1 from `Build Chain Map.dc.html`'s reference `class Component` (`rechainFanin`, `rechainGates`,
  `rechainAll`, `derive`) per BUILD_QUEUE_METHOD.md §5 exactly — no UI, pure data transforms on the
  ChainDoc graph. Plan: new `Services/BuildMap/ChainRules.cs`, `dotnet build` clean, then a real audit
  against tonight's actual ShaneBuilder Epic #2198 → Test Pad cascade via `BuildChainMapService.BuildAsync`
  (live GitHub data, no fixtures).
