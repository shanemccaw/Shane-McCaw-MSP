# #3260 — Open Food Facts integration for barcode scan (product images + nutrition data)

- **Status:** ✅ DONE 2026-09-08T00:45:00Z
- **Scope:** platform (web/shanes-life — Shane's Life, milestone 19)
- **Started:** 2026-09-08
- **Commit(s):** 5291c4700 (merged to main, fast-forward)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — live-verified the real OFF API shape
  (`status:1`/`"product found"` vs `status:0`/`"no code or invalid code"` in the
  response BODY, not HTTP status — matches the issue's stated gotcha exactly).
  Read #3109 (barcode scan, src/core/scan.mjs), #3112 (per-store price caching
  pattern, src/core/prices.mjs), and the existing `heartHealthy`/`get_health_context`
  MCP wiring in src/core/recipes.mjs + src/mcp/tools.mjs. Next: migration 059
  (global `off_products` cache table, not user-scoped — product nutrition is the
  same for every user) + a new `src/core/nutrition.mjs` module + wiring into
  scan.mjs's three match states + client UI + a `get_nutrition` MCP tool so Claude
  can pull real numbers before `push_recipes`.
- 2026-09-08T00:45:00Z ✅ DONE — shipped `migrations/059_off_nutrition.sql`
  (ran locally against `DATABASE_URL`), `src/core/nutrition.mjs` (cached OFF fetch,
  30-day freshness, FSA-threshold high-sodium/high-saturated-fat/high-sugar flags),
  `scan.mjs`'s `lookupBarcode()` now attaches real product image + nutrition + flags
  to all three match states, a new `get_nutrition` MCP tool (+ `push_recipes`
  description updated to call it alongside `get_health_context`), and client UI
  (product image + per-100g nutrition card with High flags) in `app.js`/`app.css`.
  Live-verified end-to-end against the real OFF API and a real scan/list/user in the
  local DB: known barcode (Nutella 3017620422003) returns real nutrition + correct
  flags; invalid barcode (0000000000000, real `status:0` in body, HTTP 200) degrades
  honestly to `found:false` without breaking the scan flow; repeat lookup reads the
  cache (1ms, no re-fetch). `verify-branch-merged.mjs` confirms 5291c4700 merged into
  origin/main (fast-forward push, no conflicts). `git status --porcelain` clean.
