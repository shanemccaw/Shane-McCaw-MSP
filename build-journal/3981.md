# #3981 — C# rename: IsResolved bool → real three-state status (Open/Verifying/Closed) across the Test Mode bug UI

- **Status:** ✅ DONE 2026-09-14T02:46:40Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 33d01aa5f (merged to origin/main; local commit was d96e97c58 before the final rebase)

## Log
- 2026-09-14T02:24:17Z ⏳ IN FLIGHT — #3978 (blocker) confirmed DONE and migrated against local Postgres (`chk_vtt_status`, `chk_vtt_resolution`, `chk_vtt_resolution_reason`, `resolution`, `resolution_reason`, `selector`, `is_design` all present, verified via `\d visual_test_tracker_entries`). Starting mechanical rename: `IsResolved` bool → real `Status` (Open/Verifying/Closed) + `Resolution`/`ResolutionReason`, plus the `IsDesign` addendum from the 2026-09-13 comment. Grepped `IsResolved` hits in `TestModeComposerPanel.xaml.cs`, `MainWindow.TestMode.cs`, `Services/VisualTestTrackerSession.cs`, `SessionHistoryDialog.xaml.cs` as starting points; full literal grep for `"Resolved"`/`"Open"` still to follow before considering this done.
- 2026-09-14T02:46:40Z ✅ DONE — Replaced `IsResolved` bool with real `Status` ("Open"/"Verifying"/"Closed") + `Resolution`/`ResolutionReason` across `BugCardViewModel` (`TestModeComposerPanel.xaml.cs`), `PersistedBugRecord` (`VisualTestTrackerSession.cs`), and every read/write site: `TestModeComposerPanel` (filter pills, status-toggle cycle, new "Not a Bug" quick action + required-reason prompt, Copy MD, `BtnSaveBug_Click`), `BugsDocumentView` (radio filters, `BtnToggleStatus_Click` cycle, new "Not a Bug" button, `DisplayBugDetail`), `VisualTestTrackerWindow` (its own independent Open/Resolved card toggle and filter — a second real spot the issue's own audit didn't list, found via grep), `MainWindow.TestMode.cs` (`PersistTestModeBugs`, `ExecuteEndSessionSyncAsync`), and `SessionHistoryDialog` (report.json round-trip). Also wired the `IsDesign` addendum through the same models plus a small toggle/badge/checkbox in each surface. `VisualTestTrackerStore`: `SaveEntryAsync`'s INSERT/UPDATE now carry `resolution`/`resolution_reason`/`is_design`; `ReadEntryFromReader` reads them back (ordinals verified live against real Postgres — see below); `UpdateEntryStatusAsync` extended with optional resolution/resolutionReason params; added `UpdateEntryDesignFlagAsync`. Added shared `Controls/SimpleTextPromptDialog.cs` (same chrome as `BuildQueuePanel.PromptForReplyMessage`/`PromptForNoteText`) for the required-reason prompt, used by both `TestModeComposerPanel` and `BugsDocumentView`.
  Concurrent sibling #3980 landed on `origin/main` mid-session (re-export reconcile pass, `GetLifecycleSnapshotAsync`, `AppendBugsSection` refactor, `resolution`/`resolutionReason` added to `VisualTestTrackerEntry`/report.json) — merged both sets of changes during rebase (real 3-way conflicts in `VisualTestTrackerEntry.cs`, `VisualTestTrackerStore.cs`, `VisualTestTrackerSessionSyncService.cs`), keeping #3980's `GetLifecycleSnapshotAsync`/`AppendBugsSection` and adding my `IsDesign` field + `is_design` column wiring on top; fixed a latent off-by-one in my own `ReadEntryFromReader` FieldCount guard while merging (needed `> 21` to safely read ordinal 21, not `> 19`).
  **Live Postgres verification** (table was empty before and after — 0 rows either side): inserted a real row via the exact `SaveEntryAsync` INSERT shape (`status='Open'`) → confirmed `Open` via the exact `ListAllBugsAsync` SELECT, verified all 22 column ordinals match `ReadEntryFromReader` exactly; `UPDATE ... SET status='Verifying'` → confirmed read-back; attempted `resolution='NotABug'` with `resolution_reason=NULL` → confirmed `chk_vtt_resolution_reason` CHECK constraint correctly rejects it (matches the new UI's required-reason prompt); set `status='Closed', resolution='NotABug', resolution_reason='<reason>'` → confirmed full round-trip via the app's real SELECT, which is what `ResolutionDisplay`/`TxtDetailResolution` render. Test row deleted; DB back to its original empty state.
  **Real finding filed as #3991** (sibling sub-issue of #3977, not #3981's own scope): `ReadEntryFromReader`'s `PageId = reader.GetInt32(2)` has no `IsDBNull` guard, and #3978 made `page_id` nullable for global-scope bugs — confirmed live that a NULL `page_id` row reads back correctly from Postgres but `GetInt32` on it would throw. Labeled `bug`, parented to #3977, milestone v1.1, board status "AI Batter Up".
  `dotnet build -c Debug` clean (0 warnings, 0 errors) both before and after the rebase merge. Grep-swept `IsResolved`/`"Resolved"`/`"Open"` across all of `desktop/BuildConsole` post-merge — zero functional hits remain, only explanatory comments. `verify-branch-merged.mjs` OK (33d01aa5f). Working tree clean.
