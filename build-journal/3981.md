# #3981 — C# rename: IsResolved bool → real three-state status (Open/Verifying/Closed) across the Test Mode bug UI

- **Status:** ⏳ IN FLIGHT 2026-09-14T02:24:17Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-14T02:24:17Z ⏳ IN FLIGHT — #3978 (blocker) confirmed DONE and migrated against local Postgres (`chk_vtt_status`, `chk_vtt_resolution`, `chk_vtt_resolution_reason`, `resolution`, `resolution_reason`, `selector`, `is_design` all present, verified via `\d visual_test_tracker_entries`). Starting mechanical rename: `IsResolved` bool → real `Status` (Open/Verifying/Closed) + `Resolution`/`ResolutionReason`, plus the `IsDesign` addendum from the 2026-09-13 comment. Grepped `IsResolved` hits in `TestModeComposerPanel.xaml.cs`, `MainWindow.TestMode.cs`, `Services/VisualTestTrackerSession.cs`, `SessionHistoryDialog.xaml.cs` as starting points; full literal grep for `"Resolved"`/`"Open"` still to follow before considering this done.
