# #2314 — Per-feature burndown and closed/total on GitMapFeature

- **Status:** ✅ DONE 2026-09-02T19:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 7e1418877

## Log
- 2026-09-02T19:29:00Z ⏳ IN FLIGHT — adding ClosedCount/TotalCount to GitMapFeature off real GitHub sub-issue counts, rendering burndown on the Git Map feature card (mini + full doc, single data path).
- 2026-09-02T19:45:00Z ✅ DONE — `GitMapFeature` (`Services/GitMapService.cs`) now carries real `ClosedCount`/`TotalCount`/`BurndownFraction`. `GetFeaturesForEpicAsync` switched from the old flat REST `sub_issues` call to one GraphQL call that also reads each feature's own one-level-deeper `subIssues` (`totalCount` + per-node `state`) — no N+1 round-trip, same nested-`subIssues` shape `GitEpicPanelService.GetEpicDetailAsync` already uses one level deeper. `MainWindow.xaml.cs` `GitMapFeatureCard` renders a real closed/total burndown bar under the feature title (`GitMapBurndownBar`), fed off `BurndownFraction` — null (no sub-issues of its own) renders nothing, no fabricated 0%/100%. Verified: `dotnet build` clean (0 warnings/errors); live `gh api graphql` sanity check against #2313's own real sub-issues confirmed the query shape parses correctly. Both the mini rail panel and the full Git Map doc tab render off the same `GitMapFeatureCard`/`_gitMapFeatureCache`, so no second data path was introduced. Commit `7e1418877` pushed straight to `main` (worktree base was current with `origin/main`), `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
