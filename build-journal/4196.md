# Issue #4196 — requireMspScope/requireCustomerScope/assertCustomerAccess denial logging

- **Status:** ✅ DONE 2026-09-15T01:32:00Z
- **Scope:** platform

## Log
- 2026-09-15T01:29:00Z — Started. Adding `logger.child({ channel: "auth" })` denial logging to
  `requireMspScope`, and to every denial branch of `assertCustomerAccess` (which
  `requireCustomerScope` already calls through, so it inherits the same logging) in
  `artifacts/api-server/src/middlewares/requireAuth.ts`. Logging-only, no allow/deny behavior
  change. Part of #1704, sibling of #4192.
- 2026-09-15T01:32:00Z — ✅ DONE — commit `0d17ede83` on `main` (rebased from
  `agent/4196-q2841`). Added `const log = logger.child({ channel: "auth" })` and a `log.warn`
  call, citing the real ids/roles involved, on: `requireMspScope`'s mismatch branch, and all
  four denial branches of `assertCustomerAccess` (no `mspId` claim, tenant not in user's MSP,
  blocked by per-staff-member scope, own-customerId mismatch, unrecognised role). No log added
  directly to `requireCustomerScope` — it reads its outcome from `assertCustomerAccess`, so it
  inherits the new logging with no duplication. `tsc --noEmit` clean. Ran the real test suites
  exercising these guards (`msp-consent.test.ts`, `dashboard-data.test.ts`,
  `msp-diagnostics.test.ts`, `lib/msp.test.ts` — 51 tests) — all pass, confirming no allow/deny
  outcome changed. `rbac-capability.test.ts` has one pre-existing failing test
  (`RetainerNoConsent` rung expectation) unrelated to this change — verified no diff touches
  `rbac-capability.ts`/`rbac-capability.test.ts` between `origin/main~1` and this commit; not
  filed separately since it's DB-seed/rung flakiness outside this issue's scope, not a new
  finding this build discovered. Working tree clean, branch verified merged into origin/main.
