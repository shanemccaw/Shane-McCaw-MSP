# #2701 — Plan self-service + Plan Management both filter out the real tier catalog

- **Status:** ⏳ IN FLIGHT 2026-09-03T20:19:34Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-03T20:19:34Z ⏳ IN FLIGHT — Confirmed live bug via issue body (real `psql` evidence:
  services 120/121/122 have `fulfillment_type='manual'`, `fulfillment_type_key
  ='msp_monthly_subscription'`). Extracted the OR-arm + `isGenuinePlatformTier()` guard from
  `msp-signup.ts` into a new shared `artifacts/api-server/src/lib/platform-tier.ts`
  (`platformTierWhere()` / `isGenuinePlatformTier()`), and applied it to all 4 queries named
  in the issue: `msp-plan-self-service.ts` (`GET /api/msp/plan/available`, `POST
  /api/msp/plan/change` target lookup) and `msp-plan-management.ts` (`GET
  /api/admin/plan-management/tiers`, `POST .../new-price` target lookup — the issue's
  `:116` line is actually the new-price lookup, not migrate-subscriber; migrate-subscriber
  itself doesn't query `servicesTable` by fulfillment type at all). `msp-signup.ts` now
  imports the shared helper instead of defining its own local copy. Next: typecheck, verify
  against local DB, commit, push.
