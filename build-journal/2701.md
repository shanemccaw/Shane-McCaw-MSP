# #2701 — Plan self-service + Plan Management both filter out the real tier catalog

- **Status:** ✅ DONE 2026-09-03T20:26:11Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 6fd0fb532 (fix, on main) — bb2ba0834 (IN FLIGHT bookend, on main)

## Log
- 2026-09-03T20:19:34Z ⏳ IN FLIGHT — Confirmed live bug via issue body (real `psql` evidence:
  services 120/121/122 have `fulfillment_type='manual'`, `fulfillment_type_key
  ='msp_monthly_subscription'`). Extracted the OR-arm + `isGenuinePlatformTier()` guard from
  `msp-signup.ts` into a new shared `artifacts/api-server/src/lib/platform-tier.ts`
  (`platformTierWhere()` / `isGenuinePlatformTier()`), and applied it to all 4 queries named
  in the issue: `msp-plan-self-service.ts` (`GET /api/msp/plan/available`, `POST
  /api/msp/plan/change` target lookup) and `msp-plan-management.ts` (`GET
  /api/admin/plan-management/tiers`, `POST .../new-price` target lookup — the issue's
  `:116` line is actually the new-price lookup, not migrate-subscriber; migrate-subscriber
  itself doesn't query `servicesTable` by fulfillment type at all). `msp-signup.ts` now
  imports the shared helper instead of defining its own local copy.
- 2026-09-03T20:26:11Z ✅ DONE — Committed, rebased onto origin/main (retried once after a
  concurrent push moved main), pushed successfully as 6fd0fb532 — `verify-branch-merged.mjs`
  confirms `agent/2701-q1447` is an ancestor of `origin/main`. Verification: (1) direct
  `psql` query against local `DATABASE_URL` reproducing the new OR-arm predicate confirms
  it now matches exactly the 3 real tiers (Free/Growth/Pro, ids 120/121/122), all
  `is_public=true`, none carrying `addOnType`/`grantsCapabilityKey` — so
  `isGenuinePlatformTier()` lets all 3 through and none are excluded; (2) `tsc --noEmit`
  on the 4 changed/new files shows zero errors of a new kind vs. a stashed-and-restored
  baseline diff comparison — every remaining error (missing `drizzle-orm`/`zod`/`stripe`
  type declarations, unbuilt `lib/db/dist`) is the same pre-existing worktree
  dependency-resolution class documented in CLAUDE.md (Git #1987) on the unmodified
  baseline too, not something my diff introduced; per that same rule, did NOT run
  `pnpm install` to try to fix it. (3) Attempted `vitest run
  src/routes/msp-plan-self-service.test.ts` (it's on the vitest include allowlist) — failed
  to start with `ERR_MODULE_NOT_FOUND` resolving `vitest/config` from a `.vite-temp` cache
  path pointing at the *main* checkout, not this worktree; same pre-existing
  dependency-resolution class as (2), not fixed via `pnpm install` per Git #1987. Real gap:
  the test itself did not actually execute — flagging honestly rather than claiming a pass.
  Working tree confirmed clean (`git status --porcelain` empty) before this bookend.

  Mid-session note: a `git stash push -u`/`apply` cycle used to diff against baseline
  briefly showed an apparently mass-deleted `.agents/memory/*` in `git status --porcelain`
  output; re-checked from repo root immediately after and confirmed those files were intact
  on disk the whole time and the stash itself (`git stash show --stat`) contained only my
  4 intended files — transient/stale read, not real damage. No other session's files were
  touched.
