# #1768 — Git Board tree: issue titles get crushed to 2-3 characters by badge width reservation — wrap instead of hard-truncate

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 96e4d5e01

## Log
- 2026-08-29 ⏳ IN FLIGHT — reading Controls/LeftSidebar.xaml.cs title-width reservation block (~4794-4857), about to switch title TextBlock from CharacterEllipsis single-line truncation to TextWrapping.Wrap capped at 2 lines.
- 2026-08-29 ✅ DONE — CreateIssueHeader's titleBlock (desktop/BuildConsole/Controls/LeftSidebar.xaml.cs) now uses TextWrapping.Wrap capped at 2 lines (new IssueTitleTwoLineMaxHeight = 32), with CharacterEllipsis kept as last-resort fallback beyond that. Every badge (priority, number, working, no-parent, in-flight, in-flight-descendant, blocked) got an explicit VerticalAlignment=Top so a taller wrapped-title row doesn't stretch their Borders — badge width/padding/styling unchanged, reserved-width constants unchanged. Short-title rows still render at today's compact height since the StackPanel only grows when a title actually wraps. Secondary ask (sidebar manual resize): took a shallow look — MainWindow.xaml's SidebarSplitter is a standard GridSplitter over ColSidebar (MinWidth=0) with existing pin/collapse toggle code in MainWindow.xaml.cs (~5099-5109, ~5225, ~5308); no bug found, it just doesn't persist a manually-dragged width across a pin/collapse toggle — a UX nit, not a correctness bug, left untouched per the issue's lower-priority framing. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: clean, 0 warnings, 0 errors. Verified 96e4d5e01 is a real ancestor of origin/main via `git merge-base --is-ancestor`. `git status --porcelain` clean.
