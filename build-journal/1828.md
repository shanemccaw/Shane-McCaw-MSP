# #1828 — Enforce the documented Dev-only SSH lock on ReplitSshService

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — `ReplitSshService` is a globally reachable singleton with zero
  `TargetEnvironment` enforcement, contradicting `TargetEnvironment.cs`'s own doc comment
  ("Agent/protocol executions (shaneapp://) are hard-locked to Dev"). Adding the real gate at the
  single choke point (`ExecuteCommandAsync`, which every other method routes through): (1) a
  structural agent hard-lock — any `shaneapp://`-originated call refuses SSH regardless of the
  Target Environment selector, via an `AsyncLocal` marker set at the single dispatch point
  (`MainWindow.HandleShaneAppUriAsync`); (2) a default gate that reads the live Target Environment
  selector (`LeftSidebar.GetSelectedTargetEnvironment()` — the single source of truth, same read
  path `LocalSqlExecutor` uses) and refuses unless Staging; (3) an explicit, human-only
  authorization scope for the deliberate manual "Deploy to Staging" action so it keeps working
  regardless of the selector (no regression). Evaluating `PostBuildDeployPipeline` and
  `ReplitWatcherService` honestly — decision recorded at DONE.
