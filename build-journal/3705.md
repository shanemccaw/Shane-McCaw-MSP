# #3705 — SQL Runner: real, extensible database switcher

- **Status:** ✅ DONE 2026-09-11T18:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** b29f34a41

## Log
- 2026-09-11T18:35:00Z ⏳ IN FLIGHT — read #3705 body/comments + #3581 (repo registry, closed/complete). Repo registry is owner/repo+tier — doesn't extend 1:1 to databases (BuildConsole DB and product DB are two separate real databases within the SAME repo entry), so building a small dedicated `DatabaseRegistry` (Key/DisplayName/EnvFile/EnvVar) instead, extensible the same way (one new entry per new repo's DB). Plan: registry service + resolve-with-honest-error, LocalSqlExecutor accepts a database key, SqlDocumentView gets a real dropdown defaulting to Product.
- 2026-09-11T18:55:00Z ✅ DONE — b29f34a41. Added `Services/DatabaseRegistry.cs` (extensible Key/DisplayName/EnvFile/EnvVar list, seeded with product/BuildConsole/shanes-life, honest `Resolve`/`ResolveResult` with no silent fallback). `LocalSqlExecutor` gained a `databaseKey`-aware `ExecuteAsync` overload (2-arg overload preserved, all 14 other call sites unaffected) plus `CheckReachable` for pre-execute UI status. `SqlDocumentView` toolbar got a real ComboBox dropdown (default Product) + green/red reachability dot/tooltip wired to `SelectionChanged`, Execute now runs against the selected database and reports an honest error (not a silent switch) when unreachable. Verification: `dotnet build BuildConsole.csproj` — 0 errors (10 pre-existing unrelated warnings only). Confirmed via direct `psql` that product (.env.local DATABASE_URL) and BuildConsole (.env.local BUILD_DATABASE_URL) are genuinely distinct real databases (product `public.users` count=9; BuildConsole `bt_chats` count=53, a table that doesn't exist on the product DB). Confirmed shanes-life reports honestly unreachable in this checkout (`web/shanes-life/.env` doesn't exist, only `.env.example`) rather than silently falling back to another database — matches the issue's "honest handling of an unreachable database" ask. No live WPF launch performed (per the never-launch-BuildConsole.exe-for-verification rule) — verification is build + direct-DB + code-path reasoning. `verify-branch-merged.mjs`: OK, merged into origin/main. `git status --porcelain`: clean. No findings filed — no genuine bug/gap discovered outside this issue's own scope.
