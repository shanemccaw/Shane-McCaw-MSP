# #2471 — Dismiss (park alive) vs. Close (real kill) — replaces ArchiveChatTab

- **Status:** ✅ DONE 2026-09-02T23:30:03Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 0d74685c8 (bookend), 278577d59 (real implementation)

## Log
- 2026-09-02T23:20:49Z ⏳ IN FLIGHT — Investigated: #2470 (phase 1, per-tab WebView2 keep-alive
  instancing) is already merged/closed. ShaneBuilder already has a per-workspace
  Dismiss/Restore mechanism (`_stashedWorkspaces` + `WorkspaceBoxPopup`) that is functionally
  identical to BuildConsole's Shelf — extending it to individual tabs (new
  `_dismissedTabIds` + a "DISMISSED TABS" section in the same popout) is the correct, minimal
  implementation rather than inventing a parallel mechanism. Plan: generic per-tab
  `DismissTab`/`RestoreDismissedTab`/`CloseDismissedTab` in a new
  `MainWindow.DismissedTabs.cs`, `CloseTab` gains real ChatArchiveStore recording (moved from
  the old `ArchiveChatTab`, now correctly keyed to each tab's own live URL instead of the
  global `_currentConversationUrl`), tab context menu becomes Dismiss/Close/Close Others
  uniformly, Chats panel row's "Archive" becomes "Dismiss".
- 2026-09-02T23:30:03Z DONE — Implemented and pushed to origin/main as 278577d59 (bookend
  itself 0d74685c8). New MainWindow.DismissedTabs.cs adds DismissTab/RestoreDismissedTab/
  CloseDismissedTab plus a DISMISSED TABS section rendered into the existing WorkspaceBoxPopup.
  RenderTabStrip filters dismissed tabs from the strip (they stay in _tabs, same design as a
  stashed workspace); a dismissed keep-alive tab's WebView2 is parked via ParkKeepAliveView
  (alive, never reset/disposed) -- restoring is a plain reflow via SelectTab, no reload. CloseTab
  (real kill, unchanged teardown) now records a closing chat tab's identity into ChatArchiveStore
  via RecordChatArchiveIfNeeded, fixed to key off the CLOSING tab's own tracked keep-alive URL
  instead of the old ArchiveChatTab's shared _currentConversationUrl (silently wrong for any tab
  other than the active one -- a real latent bug fixed in-flight, not filed separately since it
  was fixed as part of this build). Tab context menu unified to Dismiss/Close/Close Others for
  every closable tab (was chat-only "Archive chat" + Close before); Home is never dismissible.
  Chats panel row's Archive button is now Dismiss; its "open chats" list excludes dismissed tabs.
  Also fixed a latent bug the dismiss feature would otherwise introduce: CloseTab's active-tab
  fallback now skips dismissed tabs when picking the next tab to select (previously could have
  set _activeTabId to a hidden/dismissed tab). Cleared a stale blocked_by edge on #2471 pointing
  at the now-closed #2470 at session start.
  Verification: `dotnet build ShaneBuilder.csproj -c Debug` -- 0 warnings, 0 errors, both before
  and after the final rebase onto origin/main. No test-manifest requirement -- ShaneBuilder is
  BuildConsole's sibling desktop app, explicitly out of scope for test manifests per CLAUDE.md.
  No live/manual GUI run performed (no WebView2/interactive runner available in this session);
  verification is the clean build plus a full read-through of the diff against #2471's done-when
  criteria (see completion comment on the issue for the point-by-point mapping).
  verify-branch-merged.mjs -- OK, merged into origin/main. git status --porcelain -- clean.
  No genuine findings filed this session (the one bug found -- _currentConversationUrl mis-keying
  in the old ArchiveChatTab -- was fixed in this same build, not a separate finding).
