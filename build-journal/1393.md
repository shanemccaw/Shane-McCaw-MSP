# #1393 — Scene 0 landing programmatic bypass for runTest manifests

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 6f76c7629

## Log
- 2026-08-27 ⏳ IN FLIGHT — Read PortalV2ScanLanding.tsx (Scene 0 port, #1298/#1300) and portal-v2-overview.tsx's `showLanding` gate. Confirmed the only existing bypass is the isTestbed-gated `[DEBUG] Skip to Overview` button, click-driven. Plan: a `?e2e_skip_landing=1` query param, checked client-side in portal-v2-overview.tsx against the SAME server-verified `scanStatus.data?.isTestbed` flag the debug buttons already gate on (no new server route needed — the flag is already fetched over GET /api/portal/scan-status), so the landing never arms at all for a testbed tenant with the param set, and a real customer setting the param does nothing since their isTestbed is always false server-side.
- 2026-08-27 ✅ DONE — Implemented in portal-v2-overview.tsx: the `landingArmed` effect now skips arming (never mounts PortalV2ScanLanding, no overlay flash) when `?e2e_skip_landing=1` is on the URL AND `scanStatus.data?.isTestbed === true`. Distinct from and independent of PortalV2ScanLanding's own manual [DEBUG] buttons — this stops the landing from ever mounting rather than dismissing an already-mounted one, so shaneapp://runTest manifests reach real Overview content with zero UI interaction and none of the landing's extra journey/customerName fetches. Inert for a real customer regardless of the param, since isTestbed is server-verified and always false for them. Rebased onto origin/main past a concurrent #1390 commit that also touched this file (conflict only in the manifest's `notes` string; the .tsx merged clean) — resolved by hand, both changes intact. `tsc --noEmit` on artifacts/msp-portal: same 2 pre-existing unrelated errors (SowScreen.tsx, PortalV2Shell.tsx) as #1391's bookend noted, none new. Updated test-manifests/portal/portal-v2-overview.json's notes honestly: the bypass isn't live-asserted because the only reachable test tenant (shane-mccaw-consulting) is already-scanned and non-testbed, so there's no reachable never-scanned testbed tenant to prove it actually skips an armed landing — same reachability gap OV16 already documents. Pushed straight to origin/main (6f76c7629, rebased) per repo convention.
