# #1393 — Scene 0 landing programmatic bypass for runTest manifests

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-27

## Log
- 2026-08-27 ⏳ IN FLIGHT — Read PortalV2ScanLanding.tsx (Scene 0 port, #1298/#1300) and portal-v2-overview.tsx's `showLanding` gate. Confirmed the only existing bypass is the isTestbed-gated `[DEBUG] Skip to Overview` button, click-driven. Plan: a `?e2e_skip_landing=1` query param, checked client-side in portal-v2-overview.tsx against the SAME server-verified `scanStatus.data?.isTestbed` flag the debug buttons already gate on (no new server route needed — the flag is already fetched over GET /api/portal/scan-status), so the landing never arms at all for a testbed tenant with the param set, and a real customer setting the param does nothing since their isTestbed is always false server-side.
