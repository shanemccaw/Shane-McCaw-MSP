# #3540 — MyArchitect: real TenantService (replace fixture customer list)

- **Status:** ✅ DONE 2026-09-10T21:04:38Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 78405854be630827c19fdc578988545a451a7b6b

## Log
- 2026-09-10T20:51:04Z ⏳ IN FLIGHT — Investigated. Found the real MSP-facing
  customers endpoint: `GET /api/msp/v1/msps/:mspId/customers` (paginated,
  `requireCapability("ladder.msp-operator")` + `requireMspScope("params")`,
  `artifacts/api-server/src/routes/msp-v1.ts:106-166`) returning real numeric
  `tenants.id`, `mspId`, `name`, `domain`, `industry`, `tenantId` (GUID),
  `tenantUrl`, `status`, `isTestbed`, `createdAt`, `updatedAt`. Next: replace
  `TenantService.cs`'s fixture data with a real HTTP client against it using
  #3501's `AuthService`/bearer-token mechanism, add a numeric id field to
  `Models/Tenant.cs`, wire `MainWindow.TryResolveLaunchControlScope` to use it,
  verify end-to-end against the running api-server.
- 2026-09-10T21:04:38Z ✅ DONE — Shipped in `78405854b`. Replaced the four
  hardcoded fixture tenants with a real `TenantService` client against
  `GET /api/msp/v1/msps/:mspId/customers`, fanning the bearer token the same
  way every other real-endpoint service does (`MainWindow.ApplyAuthStateAsync`).
  `Models/Tenant.cs` gained `CustomerId` (real numeric `tenants.id`) — `Id` is
  now its string form, replacing the old `tenant-001`-style fake id.
  `TryResolveLaunchControlScope`, `TryResolveRetainerCustomerId`, and
  `TryResolveTrackerCustomerId` now resolve a real `customerId` from
  `CurrentTenant.CustomerId` instead of always returning false — closing the
  gap #1899/#3502/#3505 and several doc comments across `MainWindow.xaml.cs`
  pointed at. `TenantSwitcher` now refreshes on the new `TenantsChanged` event
  instead of reading the list once at construction (the real load is async).
  Cleaned up every stale "TenantService is fixture data" comment/user-facing
  string in `MainWindow.xaml.cs` to reflect the real state.

  **Verification (real, end-to-end, not simulated):** inserted a synthetic
  `MSPOperator` test user under `msp_id=1` (`zz-test-myarchitect-3540@…`,
  deleted after the run — DB left pristine), logged in via the real
  `POST /api/auth/login`, confirmed `GET /api/msp/v1/msps/1/customers` returns
  the 2 real tenants under that MSP (`id=1` "Jane Jane", `id=3` "Test Me").
  Then built and ran a throwaway console harness that referenced this
  session's actual `TenantService.cs`/`Tenant.cs`/`ITenantService.cs` files
  and called `LoadTenantsAsync(1)` directly against the running local
  api-server with that real token — confirmed it deserializes correctly and
  populates `CustomerId=1`/`3` matching the DB rows exactly, with
  `CurrentTenant` auto-selected to the first real tenant. `dotnet build` of
  `MyArchitect.csproj` succeeds with 0 errors. Hit one real concurrent-session
  merge conflict in `MainWindow.xaml.cs` (Task Queue #3490 landed on `main`
  mid-session) — resolved by keeping both sets of changes, rebuilt clean, and
  re-verified the merged file compiles before pushing.

  Real known gap, explicitly out of scope here and not silently swallowed:
  `BuildDocumentHubRows` (Document Hub, #3486) still browses the caller's
  whole MSP book rather than filtering by the now-real `CustomerId` — wiring
  that filter is its own scoped change, documented inline where it lives.
