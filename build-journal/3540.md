# #3540 — MyArchitect: real TenantService (replace fixture customer list)

- **Status:** ⏳ IN FLIGHT 2026-09-10T20:51:04Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-10T20:51:04Z ⏳ IN FLIGHT — Investigated. Found the real MSP-facing
  customers endpoint: `GET /api/msp/v1/msps/:mspId/customers` (paginated,
  `requireCapability("ladder.msp-operator")` + `requireMspScope("params")`,
  `artifacts/api-server/src/routes/msp-v1.ts:106-166`) returning real numeric
  `tenants.id`, `mspId`, `name`, `domain`, `industry`, `tenantId` (GUID),
  `tenantUrl`, `status`, `isTestbed`, `createdAt`, `updatedAt`. Next: replace
  `TenantService.cs`'s fixture data with a real HTTP client against it using
  #3501's `AuthService`/bearer-token mechanism, add a numeric id field to
  `Models/Tenant.cs`, wire `MainWindow.TryResolveLaunchControlScope` to use it,
  verify end-to-end against the running api-server.
