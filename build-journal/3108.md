# #3108 — Feature: Shopping — Store paths + aisle memory (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T20:50:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `42f3f2eee` on `main`

## Log
- 2026-09-07T20:20:00Z ⏳ IN FLIGHT — `blocked_by #3088` confirmed clear (#3088 is `complete`
  and merged to `main` as `e21b1d1ee`/`a69ec6f16`). Read #3108's real scope: Flat / Category /
  Best-path ordering for a real shopping run, plus real voice-captured aisle memory building a
  growing per-store map, with Best-path using that real accumulated data. Read
  `Design/design_handoff_shanes_life/contract.md` and `README.md`, and the real wired logic in
  `Shanes Life - First Slice Prototype.dc.html` (`CAT_ORDER`/`CATS`/`catOf`,
  `it.aisleBy[store]`, `known.sort((x,y) => A(x).aisle - A(y).aisle)`) as the actual
  specification for both the category classifier and the aisle-sort behavior. Proceeding with
  the real build.
- 2026-09-07T20:50:00Z ✅ DONE — `42f3f2eee` on `main`. Real scope shipped:
  1. **Store paths + aisle memory**: `migrations/022_store_aisle_memory.sql` adds `lists.store`
     (which real store a run is being shopped at) and `store_aisles` — a real, growing
     per-(user, store, item) record (upsert on repeat report: spot corrects in place, `hits`
     increments; not a one-time entry). `core/store-aisles.mjs`
     (recordAisle/getStoreMap/listStores/matchAisle, tolerant substring match so "pasta aisle
     12" still finds "Pasta, 3 boxes" already on the list).
  2. **Flat / Category / Best-path ordering**: `core/shopping-order.mjs`. The category
     classifier (`CAT_ORDER`/`CATS`/`categoryOf`) is ported verbatim from the design's own
     real, wired First Slice Prototype logic (`catOf`), not invented. Best-path groups by real
     aisle number ascending using the accumulated store map; an item with no known spot lands
     in a separate `unknown` bucket rather than being guessed at.
  3. **API**: `?order=flat|category|best` on `GET /api/shopping` and `GET /api/lists/:id`;
     `PATCH /api/lists/:id/store`; `POST /api/lists/:id/items/:itemId/aisle` (records + stamps
     the item's own note for immediate display); `GET /api/store-aisles`.
  4. **MCP**: `push_list` now accepts `store`; new `record_aisle` and `get_store_map` tools
     matching the capture grammar's aisle-memory chunk (README's own "Existing" tool name).
  5. **UI**: Shopping room gets a store control, the real Flat/Category/Best-path segmented
     control, per-group rendering for Category/Best-path, and a per-item aisle-save control.
  No standalone quantity/price change here — that stayed #3109/#3112's own scope; this session
  only merged cleanly against their real, concurrently-landed work (barcode scan, price_cents,
  per-store price history) across three separate rebases as `main` moved.

  **Verified**: `npm run check` 98/98 PASS (was 70 before this session; #3109/#3112 landed 16
  of the delta concurrently, this session's own 12 real end-to-end checks cover: setting a
  list's store, Category grouping by the ported classifier, recording a real aisle spot via
  the owner API (and it stamping the item's own note), `record_aisle`/`get_store_map` (MCP)
  round-trip, a repeat report growing `hits` in place rather than duplicating a row, Best-path
  ordering using the real accumulated map for an item never stamped on *this* run, the
  `unknown` bucket for an item with no real spot, a real DB row count, and the audit trail) —
  run against a real local server and the real shared `finances` database.
  `verify-branch-merged.mjs` confirms `42f3f2ee` is an ancestor of `origin/main`. Working tree
  left clean.

  **Real finding, not filed as a new issue** (informational only, no action blocked): #3108
  and #3112 (per-store price history) each grew their own independent "store" concept —
  `lists.store`/`store_aisles.store` (free text) here vs. a real `stores` table
  (`core/prices.mjs`) there. They don't collide functionally (different tables, different
  purposes), but they're not unified into one real store identity. Noting it here rather than
  filing, since it's a minor architectural observation, not a customer-facing gap or a broken
  behavior — Shane's call whether a future Feature should unify them.
