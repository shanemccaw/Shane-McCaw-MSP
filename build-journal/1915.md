# #1915 — Azure Lighthouse onboarding: promise/record layer + template generator

- **Status:** ✅ DONE 2026-09-04T07:22:22Z
- **Scope:** platform (backend/schema only — UI explicitly out of scope this dispatch, blocked on #2758 → Design → #1650)

## Log
- 2026-09-04T07:06:24Z — Session started. Read #1915 body + the one dispatch comment (which restates/supersedes scope: backend/schema only). Read `tenant_azure_reach` (#1871) schema and `azure-rm.ts` (existing ARM reach probe, `AZURE_BUILT_IN_ROLE_IDS.Reader`, `armPrincipalCredentials()`). Confirmed local DB has the live `tenant_azure_reach` table matching the Drizzle schema. Plan: new table `tenant_azure_lighthouse_offers` (the promise/record layer, distinct from the observational `tenant_azure_reach`), a template/deep-link generator module, and an admin-only route to drive it — no UI.
- 2026-09-04T07:22:22Z — ✅ DONE — Backend/schema landed:
  - `lib/db/src/schema/msp.ts` — new `tenant_azure_lighthouse_offers` table (`AZURE_LIGHTHOUSE_OFFER_STATES` = offered/completed/revoked; absence of a row is the honest "never offered" state, mirroring `tenant_azure_reach`'s "never probed" discipline). Unique on `(tenant_id, arm_scope_path)`.
  - `lib/db/migrations/manual/2026-09-04-tenant-azure-lighthouse-offers-1915.sql` — run myself against local Postgres (`CREATE TABLE` + 3 indexes, confirmed live via `\d tenant_azure_lighthouse_offers`).
  - `artifacts/api-server/src/lib/azure-lighthouse-onboarding.ts` — real ARM template generator matching Microsoft's documented Lighthouse `mainTemplate.json` shape (`Microsoft.ManagedServices/registrationDefinitions` + `registrationAssignments`, Reader role `acdd72a7-…`), plus the Azure Portal deep-link builder. Managing-tenant identity read from env (`AZURE_LIGHTHOUSE_MANAGING_TENANT_ID` falls back to the already-real `GRAPH_TENANT_ID`; `AZURE_LIGHTHOUSE_PRINCIPAL_ID`/`_DISPLAY_NAME` have no fallback and are a real one-time value only Shane can supply from his own tenant's Entra ID — left empty in `.env.local` with a comment, not fabricated). Unconfigured state throws a clear, honest error rather than generating a broken/fake principal.
  - `artifacts/api-server/src/routes/admin-azure-lighthouse.ts` — admin-only (`requireAdmin`) routes: `POST /admin/tenants/:tenantId/azure-lighthouse-offers` (generate + persist an offer), `GET .../azure-lighthouse-offers` (list; empty array = honest never-offered), `POST .../:id/reconcile` (compares the offer's scope against the tenant's last `tenant_azure_reach` probe's `managedByTenantIds` and flips offered→completed / completed→revoked — the actual mechanism that makes a revoked delegation distinguishable from one never made), `PATCH .../:id` (manual correction). Registered in `routes/index.ts`. No portal/UI route — deliberately out of scope this dispatch.
  - Tests: `artifacts/api-server/src/lib/__tests__/azure-lighthouse-onboarding.test.ts` (11 cases, added to `vitest.config.ts`'s explicit include allowlist) — pins the real template shape, the config-fallback/no-fallback behavior, and the honest unconfigured-error path. All pass.
  - Live verification: `artifacts/api-server/scripts/verify-azure-lighthouse-offer-1915.mts` (run via `pnpm exec tsx`) — exercised the real flow twice against local Postgres: once with no Lighthouse config (confirmed the honest failure path, no fabricated principal), once with demo config against the real testbed tenant `c4c814d4-…` (built the template, inserted+read back a real `tenant_azure_lighthouse_offers` row, confirmed it correctly reads that tenant's live `tenant_azure_reach.state = no_rbac` as distinct and unrelated — exactly the ambiguity #1915 was filed to close). Demo row deleted after verification.
  - Typecheck clean (`lib/db`, `artifacts/api-server`).
  - Commits: `7b5aaeec0` (real work, on `agent/1915-q1554`), merged `origin/main` (no conflicts), pushed to `origin/main` as `c5742461` and to the agent branch. `verify-branch-merged.mjs` confirms `agent/1915-q1554` is an ancestor of `origin/main`.
  - `git status --porcelain` clean at finish.
  - No GitHub findings filed this session — no genuine out-of-scope bug/gap discovered beyond what's already tracked (UI half is #1650, blocked as documented).
