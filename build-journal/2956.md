# #2956 — New Site v2 — Assessment page (design pass, relocate to /assessment)

- **Status:** ✅ DONE 2026-09-06T00:12:00Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`)
- **Started:** 2026-09-05
- **Commit(s):** `abe9dbd8e` on `production-base-2026-09-05`

## Log
- 2026-09-06T00:05:00Z ⏳ IN FLIGHT — Read #2956's issue body + dispatch comment (in order), and `Design/fractional_architecture/README.md` §"Screens / Views" item 4 (Assessment). Confirmed both blockers landed on `production-base-2026-09-05`: #2954 (shared chrome) at `4ad0df12a`, #2955 (Work With Me + routing move, which relocated the Assessment/home content to `/assessment`) at `7a6a2e403`/`b61802bdf`. Cleared the now-stale `blocked_by #2955` edge on this issue (both blockers resolved) and set `in-flight` label. Plan, matching #2954/#2955's split: bookend + progress reporting stay on this `main`-based branch; real page build happens in a separate worktree `C:\wt\2956-prodbase` (`agent/2956-prodbase`, off `origin/production-base-2026-09-05`, deps junctioned), pushed directly to `origin/production-base-2026-09-05`. Scope: apply the new shared Header/Footer (from #2954) + design tokens/chrome to whatever page now serves `/assessment` (post-#2955 relocation) — do not touch quiz logic, Stripe checkout, or `public-assessment-payment.ts`; confirm/add reduced-motion guard.
- 2026-09-06T00:12:00Z ✅ DONE — `src/pages/Home.tsx` (which #2955 already routed to `/assessment`) had its own bespoke sticky header (custom `Logo` + a redundant `#assessment` CTA button) and its own `Footer`/`PersistentChatBubble` imports — not the shared chrome. Swapped both out for the shared `Layout` component (`Header`+`Footer`+`BackToTop`+`EngagementOfferPanel`+`PersistentChatBubble`, the same one About/WorkWithMe already use), matching the exact `paddingTop:72` wrapper pattern `WorkWithMe.tsx` uses to clear the now-fixed 72px header. Reduced-motion: the page's `useReveal`/`useParallax` scroll hooks already gated on `prefers-reduced-motion`, and `.smc-sheen` was already hidden — but the radar hub's orbiting ring (`smcOrb`) and the typewriter headline's blinking cursor (`smcBlink`) were not. Added `@media (prefers-reduced-motion:reduce){...*{animation:none!important}...}` (matching `Assessment.dc.html`'s own reduced-motion rule verbatim) plus an explicit `opacity:1` on the cursor so it lands visible rather than mid-blink. Quiz logic (seven questions, six pillar chapters + benchmark bars, programme cards, scroll-reveal/parallax) and the Stripe checkout flow (`AssessmentFlow.tsx`, `public-assessment-payment.ts`) are byte-for-byte untouched — confirmed via `git diff --name-only`, only `Home.tsx` changed. `tsc --noEmit` and `PORT=5173 BASE_PATH=/ vite build` both clean. No test manifest written: this is a chrome-only visual pass with no new API/UI surface (`Home.tsx`'s quiz/report/checkout behavior is unchanged) and, same as #2955's own manifest noted, `production-base-2026-09-05` isn't the branch the `shaneapp://runTest` dev-server runner targets, so a uiSteps run against it wouldn't actually execute against this code anyway. Rebased onto `origin/production-base-2026-09-05` (which had moved by one commit, #2964) before pushing; pushed clean, fast-forward, as `abe9dbd8e`. Working tree left with only the pre-existing, never-staged, provisioner-copied `.env.local` untracked — not mine, left alone. Cleaned up the `2956-prodbase` worktree.
