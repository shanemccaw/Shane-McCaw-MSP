# #2182 — ShaneBuilder: port the real, hardened ActivityLog diagnostic system

- **Status:** 🛑 BLOCKED 2026-09-02T00:00:00Z
- **Scope:** buildconsole (ShaneBuilder — new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** none — no code changed this session

## Log
- 2026-09-02T00:00:00Z 🛑 BLOCKED — Pulled #2182's full body + comments before touching any
  code (per standing rule). Found the issue itself was put **on hold** at
  2026-09-02T01:36:40Z — 5 minutes *after* this build's own `BUILD:` dispatch comment
  (01:31:39Z) — with an explicit note that the scope "needs to be rewritten" once
  Shane's real logging UI design lands, because the model is changing from
  BuildConsole's current push-based `ActivityLog` (live event-push into the UI,
  `SelfReport()` etc.) to a pull model: "the app writes to file continuously, nothing
  gets read/displayed until he explicitly pulls it."
  Cross-checked whether that design has since landed: **#2200 (Feature: Log Viewer,
  ShaneBuilder)** shipped ✅ DONE the same day (commits `0f1764069` + `2ef0f85eb`,
  both on `origin/main`), building exactly a pull-model `ILogService`
  (`desktop/ShaneBuilder/Services/LogService.cs`) with `Query()`/`Tail()`/`Archive()`,
  per-source level handling, and a new `ConsoleOutputSink` ring buffer as
  ShaneBuilder's first internal logging sink. That is almost certainly the design
  #2182's hold comment was waiting on.
  However, #2182's own body still describes the OLD scope verbatim: port
  BuildConsole's `ActivityLog.cs` (guarded writes, per-sink retry, `SelfReport()`,
  watchdog heartbeat, bounded rotation, `GetHealth()`) — a push-model diagnostic
  layer that the hold comment says is "going away." The queued build prompt for this
  session repeats that same stale, pre-hold scope (it was posted before the hold
  comment landed). Porting `ActivityLog.cs` verbatim now would build exactly the
  model Shane's own comment says is being replaced.
  This is a real product-decision gap, not a missing-backend gap: whether/how
  #2182 should be rewritten to sit on top of `LogService`/`ConsoleOutputSink` (e.g.
  a thin `GetHealth()`-style surface over the new sink) instead of a straight port,
  is Shane's call, not something to improvise in-session. **No code was changed.**
  Stopping here rather than either (a) blindly porting the now-superseded push model,
  or (b) inventing a rewritten scope myself.
