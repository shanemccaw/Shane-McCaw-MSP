# #4374 — Pre-consent account door: real password + MFA before any tenant exists (Monitoring + Packs)

- **Status:** ✅ DONE 2026-09-16T16:31:35Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 99d917f86 (work), 370139b85 (IN FLIGHT bookend)
- **Migration:** none (schema already permits tenant-less *Pending rows per #4372)
- **Findings filed:** #4397 (monitoring scan kickoff backstop never fires for account-first buyers), #4398 (checkout-session returning-customer guard compares a non-lowercased email) — both sub-issues of #4370, AI Batter Up

## Log
- 2026-09-16T16:12:01Z ⏳ IN FLIGHT — reading direct-tenant-provisioning.ts, auth.ts password hashing and the #1310 account-creation / MFA enrollment flow; adding a new provisioning door that inserts a password-bearing `*Pending` users row with real MFA enrollment, reusing the existing hash + MFA mechanisms.
- 2026-09-16T16:31:35Z ✅ DONE — `provisionPendingAccount` (direct-tenant-provisioning.ts): insert-only users row with `passwordHash` set (refuses anything that is not a bcrypt hash), `mspRole` = the product’s `*Pending` rung (refuses any other role), no tenant_id/mspId; an existing account at the address is never modified (`account_exists`). purchase-account-flow.ts: `resolvePreConsentPurchaseSession` (unexpired, status `pending`, no tenant, category monitoring/config_pack/retainer; refuses paid/consented/tenanted/expired/uncatalogued), `createPreConsentAccount` (verified mailbox -> bcrypt(12), the same hash auth.ts uses -> provision; records accountUserId; repeat = no-op), `resolvePreConsentAccountUser`. Routes `/api/public/purchase/pre-consent/{send-verification-code,verify-code,create-account,account-status,mfa/totp/setup,mfa/totp/verify-setup,mfa/passkey/registration-options,mfa/passkey/verify-registration}` reuse the existing #1310 code + TOTP/passkey handler bodies (refactored to take their gate; paid door unchanged, audit rows identical) with the MFA gate keyed on the account THIS session created. provisionProspectAccount and the consent callback untouched. Rebased over #4392 (a528f09ee), which touched the same two lib files; conflicts resolved keeping both. Verified: api-server tsc exit 0; new public-purchase-pre-consent.live-db.test.ts 10/10 against local Postgres through real routers (gate matrix; plaintext/non-pending guards; create-account -> bcrypt(12) row at MonitoringPending/PackPending with no tenant; double-submit no-op; real /auth/login succeeds with the password and returns mfaRequired after TOTP enrollment behind pendingPurchaseGate; MFA not replaceable; existing accounts with/without password untouched and not MFA-enrollable; door closed after consent; consent then swaps to MonitoringConsented with password + MFA intact); regression purchase-account-flow, direct-tenant-provisioning, free-scan-prospect, free-scan-return-link, consent, monitoring-onboarding-scan, pendingPurchaseGate (unit + live) — 9 files 192/192 after the rebase. No zz-test-4374 rows left in the DB. Working tree clean.
