# #2900 — kill the automatic 15s _pollTimer — refresh should ONLY happen on manual click

- **Status:** ✅ DONE 2026-09-04T19:24:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** b3914f7ac (kill automatic 15s/10s timers, fold lost functionality into manual refresh), b3181fef7 (document #2900 audit finding — per-captured-item check already separate)

## Log
- 2026-09-04T19:19:00Z ⏳ IN FLIGHT — removing the automatic `_pollTimer` (15s) and `_sessionsPollTimer` (10s) DispatcherTimer ticks in `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` so nothing refreshes/refills automatically; folding the sessions/rollback checks into the existing manual `BtnRefreshGitHubTiles_Click` handler so no functionality is lost, only its trigger changes from automatic to manual.
- 2026-09-04T19:20:34Z — Shane posted a real scope refinement comment mid-build: keep the one-time startup check, keep any real per-captured/active-item GitHub check (capped at the real concurrency limit, 8), keep manual refresh; only the broad whole-board 15s auto-poll goes. Deferred: an optional 5-10min Batter-Up-only auto-check, explicitly NOT built now.
- 2026-09-04T19:23:00Z ✅ DONE — Real root-cause fix applied and verified:
  - `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`: removed the broad 15s `_pollTimer` → `RefreshAsync()` automatic tick (the exact real driver named in the issue body) and the broad 10s `_sessionsPollTimer` (active sessions + status counts + dev-server rollback check — local, not GitHub, but also automatic with zero user interaction, so removed per Shane's blanket "everything waits for manual refresh" plus the corrected scope, which names only the startup check and per-captured-item check as exceptions). Both dead `DispatcherTimer?` fields removed. The one-time initial load on tab open (`_ = RefreshAsync()`, `_ = RefreshActiveSessionsAsync()`, `UpdateQueueStatusCounts()`, `DevServerRollbackService.CheckForRollbacks(this)`) is preserved unchanged — this is Shane's real exception #1. `BtnRefreshGitHubTiles_Click` (the manual button) now also re-runs `RefreshActiveSessionsAsync()`/`CheckForRollbacks` so no functionality is lost, only its trigger — real exception #3, unchanged.
  - Real audit of exception #2 (per-captured/active-item GitHub check, capped at 8): confirmed via grep + read that `TrackDispatchAsync` in `desktop/BuildConsole/Services/QueueWatcherService.cs` (fired from `LaunchItem`, the real capture/claim moment) already calls `GitHubIssuesService.GetLastLeftBatterUpFamilyAtAsync(issueNumber)` — a real, targeted, single-issue GitHub check for exactly "did it move out of Batter Up" — and this mechanism lives entirely in `QueueWatcherService.cs`, never rode `BuildQueuePanel`'s `_pollTimer` at all, and is naturally capped at the real concurrency limit (`_maxConcurrent`, default 8) since only that many builds can be running/captured at once. Nothing to extract or preserve — it was already its own separate real mechanism, untouched by this fix. Added a doc-comment on `TrackDispatchAsync` recording this audit conclusion for future sessions.
  - `BatterUpPanel`/`AiBatterUpPanel` confirmed (grep, no `DispatcherTimer` in either file) to have no independent timer — they ride `RefreshAsync`'s `QueueRefreshed`/`BoardRefreshCompleted` cascade, so killing the broad 15s tick stops their automatic refilling too, per the issue's own stated root-cause chain.
  - Verification: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 Warnings, 0 Errors, both before and after the audit-doc commit. Live-app idle-gap verification via `ActivityLog` not performed this session (BuildConsole.exe is never launched for verification per standing rule) — the build-level removal of both timer constructions is the real, direct evidence the automatic ticks are gone.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/2900-q1642` is merged into `origin/main` at `b3181fef7`.
