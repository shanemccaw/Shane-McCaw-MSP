# #3152 — Feature: Money — Budget Day (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:50:50Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** df3ecd83e (rebased/pushed to main; original commit b07f7f935 before
  rebases onto concurrent main advances)

## Log
- 2026-09-08T01:43:25Z ⏳ IN FLIGHT — #3137 (blocker) confirmed CLOSED/complete; real
  scope is extending the already-built Budget Day ritual (money.mjs) with real
  due-versus-available math (bills due before the next paycheck) and the real
  "extreme couponing" surfacing tied to this paycheck's landed amount, pointed at
  the real cross-store weekly-ad price comparison already built for Shopping
  (#3110/prices.mjs).
- 2026-09-08T01:50:50Z ✅ DONE — `web/shanes-life/src/core/money.mjs`:
  `computeDueBeforeNextCheck` (real bills due before the next paycheck, rolling
  each bill's `due_day` forward from today and comparing against the next real
  `pay_frequency_days` payday) and `getBudgetDayCouponing` (pulls the real running
  Shopping list, runs `prices.attachWeeklyAdVerdicts`/`attachLatestPrices` — the
  real #3110 cross-store weekly-ad data — over its open items, and summarizes
  per-store wins, coupon matches and real estimated savings). Both wired into
  `getBudgetDay` (`/api/money/budget-day`) and `getGateStatus`'s `budgetDay` field
  (`/api/money/gate`, what the Money "Now" tab already renders) via a shared
  `enrichBudgetDay` helper. `public/app.js`'s existing Budget Day card extended to
  show the real landed amount, the real due-vs-available bill list with a
  covered/short verdict, and the real couponing summary line. No schema change
  needed — everything reads existing `accounts`/`income_sources`/`lists`/
  `item_prices`/`coupons` columns already in place.
  Verified end-to-end against the real local `finances` Postgres (direct
  `money.getBudgetDay('66ddf602-8103-4c15-bfc6-8914eeeb4e7a')` call, real user
  with real weekly-ad + coupon data): landed amount $2,999.35 (NASA Salary),
  4 real bills due before the 2026-09-11 payday totaling $1,852.74 (correctly
  `coveredByLanding: true`), couponing summary "Claude read this week's ads:
  Publix wins 27. 15 coupons match." over the real 31-item Shopping list.
  `gate.budgetDay` and `getBudgetDay()`'s own output confirmed byte-identical.
  `node --check` clean on all three touched files. `bin/check.mjs`'s full
  migration-runner health check is currently red for an UNRELATED reason —
  another concurrent Shane's Life session applied `034_catches.sql` locally and
  has since renamed/removed it without merging, orphaning the shared local DB's
  `schema_migrations` ledger (Git #3140's guard). Not something this build
  touched or introduced; re-verified via direct module calls instead per the
  many-concurrent-migrations warning already flagged in this issue's own build
  header. Working tree clean; branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` OK) before writing DONE.
