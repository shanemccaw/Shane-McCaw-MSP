# #1444 — Change Control: fake data OR stale rows never purged from database — needs root-cause diagnosis first

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** fe556a53d (fix), e51dec44d (manifest lastVerifiedAgainstCommit correction)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting root-cause diagnosis: live-query backing tables for portal-v2-change-control.tsx / portal-change-control.ts routes to determine fixture-vs-stale-data, before deciding on a code fix vs DB-hygiene flag.
- 2026-08-28 ✅ DONE — Root cause confirmed as DB HYGIENE, not fixture/code bug. Live-queried both DBs: local dev `msp_change_requests` has 0 rows; the Replit staging DB (via SSH) has 8 rows, all on the testbed tenant (Jane Jane, `c4c814d4-3afe-441e-9145-62461d0a4fd3`, `is_testbed=true`), all `requested_by` the `buyassessment` test account, titled "Manifest check -- raise a CR from the change-control creation flow" / "Test" — real QA/test-manifest artifacts, not fixture content, and the route/register code is genuinely, correctly live-wired (verified by reading `portal-change-control.ts`, `useChangeControl.ts`, `ccChangeControlWire.ts`). Root cause: `/api/admin/testbed/reset` (`admin-testbed.ts`) never clears `msp_change_requests`, even though its own header comment invites exactly this extension. NOT fixed here per the issue's own instruction — flagged in the GitHub issue comment for Shane's review; no rows deleted, no code fix to the reset endpoint.

  While auditing the page against the issue's standing "every value must be live / honest-empty / `NO-BACKEND-TO-WIRE`-tagged" rule, found and closed several fixture-leak spots Git #1364 hadn't reached: the freeze stat card + briefing freeze banner (always showed the fixture "24–28 Aug"/Halden text regardless of live state), the Gantt's freeze-day striping, the Microsoft Message Center rows/cards/tray entry, the collisions block, the off-timeline tray's CR-0136 fixture entry, and the Catalogue sub-view (fabricated usage stats) — all now gated the same way Git #1364 already gated calendar/review/notif. Added the literal `NO-BACKEND-TO-WIRE:` tag (previously zero occurrences repo-wide) to all 9 genuine-backend-gap spots in this file.

  `test-manifests/portal/change-control.json` updated in place (found via discover-before-create search): CC5/CC6/CC7/CC9/CC10-11/CC13c/CC30-32 rewritten to assert the new honest states (`sessionTag: "cc1444"`), plus three new `data-testid` hooks. **Not executed against the deployed build this session** — `shaneapp://runTest` exercises the deployed dev server, which only picks up this commit after the BuildConsole build completes and merges back; needs a post-deploy `runTest` pass (`onlyTag=cc1444`) to confirm green.

  **Typecheck/test run could not be completed this session** — the shared `node_modules` (junctioned across all worktrees from the main checkout) hit a broken state: `artifacts/mcp-server/node_modules` is a dangling junction pointing at a main-checkout path that doesn't exist, which aborts `pnpm install`'s linking phase workspace-wide before it writes `.bin`/`.modules.yaml` for any package. This was partly aggravated by two of my own `pnpm` commands racing against the shared store early in the session — a mistake I made and want on the record. Diff was reviewed manually for JSX/brace balance instead of via `tsc`. Needs Shane to clear the stale `mcp-server` junction and re-run `pnpm install` at the repo root before typecheck/`runTest` can run clean.

  Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing DONE (`e51dec44d` confirmed an ancestor of `origin/main`).
