# #2771 — Git Board tree: add a 'Hide completed Epics' toggle next to the filter pills

- **Status:** ✅ DONE 2026-09-04T00:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** a00e7ad0d (bookend IN FLIGHT), e8deddbb8 (implementation) — landed on `origin/main` as d350f9c0d after rebase

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — investigated real audit: reusing `GitBoardIssueFilters.ComputeTransitiveLeafRollup` (#2739, already backing `LeftSidebar.ResolveRollup` → `GitIssue.SubIssueCount`/`SubIssueCompleted`). Adding an independent `_hideCompletedEpics` toggle pill next to the existing filter chips (`LeftSidebar.xaml` ~line 560-564) that composes with `_currentFilter`, skipping a top-level Epic + its subtree in `RenderIssuesTreeAsync`'s "⚡ Epics" bucket loop when its real transitive rollup is 100% complete. Persisting via `BuildConsoleSettings.HideCompletedEpics` (existing bool-toggle-settings convention).
- 2026-09-04T00:20:00Z ✅ DONE — implemented all 3 real-fix points:
  1. Added `BtnHideCompletedEpics` ("✅ Hide Completed") pill next to the existing filter chips in `LeftSidebar.xaml`, `SecondaryButton` style, own `HideCompletedEpicsToggle_Click` handler (not `IssueFilter_Click`) so it's an independent toggle, not a 6th exclusive mode. Checked state shown via a green border/foreground swap (`ApplyHideCompletedEpicsToggleVisual`).
  2. In `RenderIssuesTreeAsync`'s "⚡ Epics" top-level-epic loop (`LeftSidebar.xaml.cs`), when `_hideCompletedEpics` is on, an epic is skipped (with its whole rendered subtree) when `SubIssueCount > 0 && SubIssueCompleted >= SubIssueCount` — those two fields already carry the real transitive leaf rollup computed once per board fetch by `ResolveRollup` → `GitBoardIssueFilters.ComputeTransitiveLeafRollup` (#2739), so this reuses the exact real completion signal rather than approximating a third way, with zero extra per-render recompute. Composes with whatever `_currentFilter` chip is active (verified by reading the loop — the two conditions are independent `continue`s).
  3. Persisted via new `BuildConsoleSettings.HideCompletedEpics` bool (existing bool-toggle-settings convention — 25+ precedents in that file), loaded in the `LeftSidebar` constructor and saved on toggle.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. No live BuildConsole instance available to click-test in this session (WPF UI, no manifest/runTest coverage applies — BuildConsole is explicitly out of scope for test manifests per CLAUDE.md); reasoned through the render-loop logic by reading it directly instead of guessing. `git status --porcelain` clean after commit. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (`OK — merged`).
