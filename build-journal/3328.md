# #3328 — Heading Out list has no "run complete" reset action of its own

- **Status:** ✅ DONE 2026-09-09T00:15:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** 0bc4d3168 (Heading Out list run-complete reset)

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — pulled #3328's confirmed gap (no equivalent of things.mjs's clearTakeRun for the Heading Out list); implementing resetHeadingOutList + wiring to trip confirm (Rental) and the Lists room's own Done-head action.
- 2026-09-09T00:15:00Z ✅ DONE — added `resetHeadingOutList(userId)` to `src/core/lists.mjs`
  (resets `done`/`done_at`/`checked_by` back to unchecked for the Heading Out list, matching
  the design prototype's real "I'm here" reset intent and `clearTakeRun`'s "resets for next
  run" precedent, not `clearCheckedItems`'s delete). Wired two real callers: `POST
  /api/trip/confirm` now calls it automatically when the confirmed trip's house is `rental`
  (returns `headingOutReset` count, audit-logged); and a new `POST
  /api/lists/heading-out/reset` endpoint backs the Lists room's own Done-head action — the
  generic list-detail view's "Clear" button now reads "Reset" and calls this new endpoint
  specifically for the list named "Heading Out" (case-insensitive), leaving every other list's
  existing delete-on-clear behavior untouched. Verified: `node --check` clean on all three
  touched files; no test/lint harness exists for `web/shanes-life` to run beyond that. Branch
  verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
