# #2705 — Test Pad note pop-out: rendered markdown view (tables)

- **Status:** ✅ DONE 2026-09-03T18:45:00Z
- **Scope:** buildconsole

## Log
- 2026-09-03T18:20:00Z — Session started. Read full issue body + comment. Plan: (1) add GFM pipe-table
  parsing/rendering to `Services/MarkdownRenderer.cs` `Render`, verified not to change existing
  non-table block parsing used by `GitDetailView`/`IssueDetailView`; (2) add Rendered/Raw toggle to
  `TestPad/TestPadNoteDetailWindow`, default Rendered, Raw keeps the existing NoWrap TextBox source view.
- ✅ DONE — 2026-09-03T18:45:00Z — commit `1bf5c37a1` (merged into `main`).
  - `Services/MarkdownRenderer.cs`: added real GFM pipe-table detection (header row + `|---|:---:|---:|`
    separator with alignment) and rendering as a real bordered WPF `Grid` (header row visually distinct
    via `Surface0Brush` background + SemiBold), inline styling (bold/italic/code/links/#issue) still
    applied per-cell via the existing `PopulateInlines`.
  - `TestPad/TestPadNoteDetailWindow.xaml(.cs)`: added a Rendered/Raw toggle (reused the existing
    `FilterChip` `ToggleButton` style, no new style invented) — default Rendered calls
    `MarkdownRenderer.Render(note.Text)` into a `ContentControl`; Raw keeps #2698's original
    NoWrap-TextBox exact-source `ScrollViewer`, both live in the same `Grid` with visibility toggled.
  - `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors.
  - Verification: standalone STA console harness (outside the repo, deleted after use — not a tracked
    test project, per the BuildConsole test-manifest-out-of-scope rule) referencing the compiled
    `MarkdownRenderer.cs` directly. Confirmed (a) a real 3-column GFM table (with a `:---:`/`---:`
    alignment row) produces a `Border > Grid` with 3 columns × 3 rows (1 header + 2 data), 9 cell
    elements, correct header cell text (`"Feature"`), and the paragraph AFTER the table still renders
    as its own `TextBlock`; (b) existing non-table content (H1/H2 headers, bold/italic/inline-code
    paragraph, unordered list, ordered list, blockquote, horizontal rule, fenced code block) produces
    the exact same element shapes as before (no `Border+Grid` table wrapper anywhere) — confirms
    `GitDetailView`/`IssueDetailView`'s existing non-table rendering is unaffected by this change.
  - Branch `agent/2705-q1449` fast-forward-merged onto `origin/main` (no divergence);
    `verify-branch-merged.mjs` confirms `1bf5c37a` is an ancestor of `origin/main`.
  - `git status --porcelain` clean before this DONE write.
  - No findings filed — no genuine bug/gap discovered outside this issue's own scope.
