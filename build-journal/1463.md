# #1463 — Strict pass: Settings, Webhooks, Billing, Receipt, Account Security

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** aba8b4d04, 4101dfca4, f2be58d57

## Log
- 2026-08-28 ⏳ IN FLIGHT — starting strict 3-state no-fake-data pass across portal-v2-settings.tsx, portal-v2-webhooks.tsx, portal-v2-billing.tsx, portal-v2-receipt.tsx, portal-v2-account-security.tsx (part of #1461 epic).
- 2026-08-28 ✅ DONE — reviewed all five pages against the 3-state rule. Webhooks, Receipt, Account security were already correctly wired (live/loading/fixture with source badges) — no changes needed. Found and fixed two real violations: (1) billingLive.ts's dataState collapsed "the read never resolved" and "this tenant genuinely has zero invoices" into one "fixture" state, so a real empty invoice response silently showed BILL_RECEIPTS's fake rows instead of an honest empty list — fixed to read "live" for any real (possibly empty) array. (2) Settings page's Change control policy and Departments sections are 100% client-only fixture with no persistence endpoint at all — tagged NO-BACKEND-TO-WIRE in settingsData.ts and added an honest "No live data available" badge in the UI instead of letting the interactive toggles silently look saved. tsc --noEmit clean (2 pre-existing unrelated errors in SowScreen.tsx/PortalV2Shell.tsx, confirmed not touched by this diff). Added tagged test-manifest coverage (settings-nodata-1463) for the new badges and a note in account-settings.json documenting the billing fix. Branch verified merged into origin/main (f2be58d57) via verify-branch-merged.mjs. Could NOT get a live shaneapp://runTest pass this session: `request-restart.mjs` hit a merge conflict merging into the shared dev-server checkout, entirely in unrelated files from other concurrent sessions (portal-v2-security-evidence.tsx, secEvidenceData.ts, security-drilldowns.json, build-journal/1430.md) — not mine to resolve. Verified instead via typecheck + code review.
