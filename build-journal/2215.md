# #2215 — Feature: SQL Runner — mini + full (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:55:00Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** `1ca56ea5b` (implementation), pushed straight to `origin/main`; verified merged
  via `verify-branch-merged.mjs` and `git cat-file -e 1ca56ea5b^{commit}`.

## Log
- 2026-09-02T15:05:00Z ⏳ IN FLIGHT — Build header scopes this session to the SQL Runner
  **mini** panel only (README-ClaudeChat.md §6.7: gutter-numbered mono editor (4 rows,
  resizable), Execute, Table/JSON results with row count, Copy CSV / Copy JSON, Send
  result to chat). README-ClaudeChat.md itself is NOT committed to the repo (confirmed —
  already tracked as open finding #2227 from #2209's session); working from #2215's own
  issue body paraphrase of §6.7, which is a complete enough spec for the mini scope.
  Real audit of BuildConsole's existing SQL Runner floaty (`Controls/SqlDocumentView.xaml(.cs)`,
  `Services/LocalSqlExecutor.cs`, `Services/SqlScriptSplitter.cs`) confirms it already has
  Execute / Table+JSON toggle / row count / Copy CSV / Copy JSON / Send to Chat — directly
  portable logic for the mini panel's execution + rendering, ported (not reimplemented) into
  ShaneBuilder. ShaneBuilder mini flyout precedent is `LogPeekPanel` (bolted onto
  `ClaudeChatDock` as a 5th flyout column, same mechanism as `StatDetailPanel`/
  `DetectedItemsPanel`) — following that exact pattern for `SqlRunnerPanel`. DB connection
  reuses `LogService.ParseConnectionString` + `ChatReadClient`'s worktree-aware
  `.env.local` resolution (already landed, same local Postgres). Full document (workspace
  tab) is out of scope for this session per the build header; audited as portable from
  BuildConsole's `SqlDocumentView`, left for a follow-up issue.
- 2026-09-02T15:55:00Z ✅ DONE — Shipped the SQL Runner mini panel onto `ClaudeChatDock` as a 6th
  flyout column (`SqlRunnerPanel`), same bolt-on mechanism as `StatDetailPanel`/`DetectedItemsPanel`/
  `LogPeekPanel`, toggled via a new `BtnToggleSqlRunner` context-bar button + wrench-menu entry.
  Landed: `Services/SqlRunnerService.cs` (real Npgsql execution, JSON row conversion) +
  `Services/SqlScriptSplitter.cs` (verbatim port of BuildConsole's splitter — top-level-semicolon
  splitting that respects string/identifier/dollar-quoted/comment boundaries), both direct ports of
  BuildConsole's already-working `LocalSqlExecutor`/`SqlScriptSplitter`, not reimplementations.
  Connection string resolution reuses `ChatReadClient`'s already-landed worktree-aware `.env.local`
  lookup (new `ChatReadClient.ResolveConnectionStringForSqlRunner()` public wrapper around its
  existing private resolver — one code path, not a second copy). UI: gutter-numbered mono editor
  (`SqlRunnerGutter` + `SqlRunnerEditor`, sharing one `ScrollViewer` so the gutter scrolls in lockstep
  with the text) at a default 90px/~4-row height, resizable via a `GridSplitter`; Execute (button +
  Ctrl+Enter); Table/JSON toggle with row count; Copy CSV / Copy JSON; Send result to chat (writes
  into the shared app-owned composer via the existing `AppendToComposer`, never auto-sends, per §5).
  No fixture data anywhere — every result comes from a real query against the local Postgres
  `DATABASE_URL`. Full-document workspace tab explicitly out of scope for this session (per the
  build header's own instruction) — filed as a real follow-up, #2231 "SQL Runner: full document
  (ShaneBuilder)", parented to #2198, set AI Batter Up.
  Verification: `dotnet build` clean (0 warnings, 0 errors); direct `psql "$DATABASE_URL" -c "SELECT
  1"` confirms the local Postgres the panel targets is live and reachable; launched the real
  `ShaneBuilder.exe` via `Start-Process` (this app has no single-instance mutex, unlike BuildConsole)
  — window rendered and stayed alive for the full check with no XAML/resource runtime exception,
  then cleanly stopped; could not drive the UI further (no keystroke-simulation path available), so
  the Execute→render round trip itself is verified by code review + the DB reachability check above,
  not a live click-through. `git status --porcelain` clean; branch verified merged via
  `verify-branch-merged.mjs`; commit `1ca56ea5b` pushed straight to `origin/main`.
