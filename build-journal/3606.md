# #3606 — Chats panel: pin icon → collapse arrow, splitter never locks

- **Status:** ✅ DONE 2026-09-10T22:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 867345cef

## Log
- 2026-09-10T22:31:00Z ⏳ IN FLIGHT — swap BtnPinSidebar's 📌/📍 emoji for real Segoe MDL2 glyphs (E76B/E76C), remove `SidebarSplitter.Visibility` toggle from `LeftSidebar.PinToggled` and from `ActivityBar_ActiveViewChanged`'s VS-Code-style collapse so the splitter is always draggable, and sync the icon state on manual drag (`SidebarSplitter_DragCompleted` → `LeftSidebar.SyncPinState`).
- 2026-09-10T22:52:00Z ✅ DONE — commit 867345cef: `PinSidebarIcon` now shows real Segoe MDL2
  glyphs (`` expanded / `` collapsed) instead of the pin emoji pair;
  `SidebarSplitter.Visibility` is no longer toggled anywhere (removed from both
  `LeftSidebar.PinToggled` and `ActivityBar_ActiveViewChanged`'s re-click collapse),
  matching `BuildQueuePanel.PinToggled`'s pattern of never hiding its own splitter.
  Added `SidebarSplitter_DragCompleted` → `LeftSidebar.SyncPinState(bool)` so a manual
  drag re-syncs the icon/`_isPinned` flag to the real resulting width; also wired into
  `ToggleSidebar_Click` and `ResetLayout_Click` for the same consistency. `dotnet build
  BuildConsole.csproj` succeeds (0 errors, only pre-existing unrelated warnings).
  `verify-branch-merged.mjs` confirms `867345cef` is an ancestor of `origin/main`;
  `git status --porcelain` clean. No findings filed — no genuine bug/gap surfaced
  beyond the issue's own scope.
