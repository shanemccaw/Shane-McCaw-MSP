# #3277 — Five rooms (Things, People, Money, Wins, Inbox) still show old generic header

- **Status:** ✅ DONE 2026-09-08T22:08:08Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** ecec188be (bookend IN FLIGHT), cfb7b5648 (fix)

## Log
- 2026-09-08T22:01:45Z ⏳ IN FLIGHT — investigated `public/app.js` (`web/shanes-life`): confirmed
  `render()`'s `hasOwnHeader` (~line 8924) only covers `today, shopping, recipes, meds, dates,
  date, pets, lists`; Things, People, Money, Wins, Inbox still fall through to the old generic
  `#app-header` bar. Adding each room's own `roomHeader()` chrome (per #3192/#3193/#3194 pattern)
  and registering the five keys in `hasOwnHeader`. Money coordinated with #3273 (still open) per
  the issue's own note — building compatibly with Money's current tab structure.
- 2026-09-08T22:08:08Z ✅ DONE — added `roomHeader()` calls (with each room's own README/ROOM_DEFS
  tint) to `viewInbox`, `viewThings`, `viewPeople`, `viewWins`, `viewMoney`; registered
  `things|people|money|wins|inbox` in `render()`'s `hasOwnHeader`; dropped each room's now-
  redundant duplicate title `<h2>` (roomHeader already renders the title) while keeping the real
  descriptive subtitle copy underneath byte-for-byte unchanged. Money's header sits above its
  existing unmodified five-tab bar per the issue's own coordination note (#3273 still open).
  `#app-view.no-header`'s padding collapse and `.room-scene`/`.room-glow` CSS are already fully
  generic (proven by Dates/Pets/Lists) — no CSS changes needed.
  Verification: `node --check public/app.js` passes. Booted the real server
  (`node src/server.mjs`) against the real local `finances` Postgres DB (same local instance as
  `shanemccawmsp`, confirmed via `psql -l`) on an alternate port 5099 — Shane's own live instance
  on port 5000 was left untouched throughout. Boot log confirmed `database reachable` and
  `migrate up to date (48 already applied)`. Fetched `/app.js` from the running server and
  confirmed it is byte-identical to the source file on disk (no build/bundle step) and contains
  all five new `roomHeader(view, ..._TINT, ...)` calls. Server process killed cleanly after.
  No test-manifest required — `web/shanes-life` is Shane's own standalone personal app, outside
  the `artifacts/msp-portal` / `artifacts/shane-mccaw-consulting` test-manifest mandate.
  `git status --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing DONE. No findings to file — this was a scoped,
  already-diagnosed fix with no new gaps discovered.
