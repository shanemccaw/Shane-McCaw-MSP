# #4296 — MyArchitect: technical-term autofill/autocorrect (dyslexia accessibility)

- **Status:** ✅ DONE 2026-09-15T23:47:33Z
- **Scope:** other (work happens in shanemccaw/MyArchitect, not this repo)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect@b4c73fb

## Log
- 2026-09-15T23:39:12Z ⏳ IN FLIGHT — Confirmed both blockers cleared (#4284 closed/complete;
  #4278's blocked_by edge was updated to #4305, also closed/complete, since #4278 was replaced
  by #4305's real extraction). Investigated real term sources in shanemccaw/MyArchitect +
  Shane-McCaw-MSP api-server: confirmed 4 real, reachable, `ladder.msp-operator`-gated sources —
  `GET /api/msp/{mspId}/launch-control/actions` (write_action_catalog/baseline_action_templates,
  already wired via LaunchControlActionsService), `GET /api/msp/change-requests` (already wired
  via ChangeControlService), `GET /api/msp/sops` (real, not yet wired), and
  `GET /api/msp/rbd/available-checks` (real monitor_checks label/description catalog, MSP-operator
  readable, not yet wired). Building: generic reusable `TermMatcher` (extracted from
  `Shell/CommandMatcher.cs`'s layered exact→prefix→word-start→substring→acronym→subsequence
  scorer), a `TechnicalTermCatalogService` aggregating the 4 real sources, new `SopsService` +
  `MonitorChecksService` HTTP clients, and a reusable `TechnicalTermAutocompleteBox` WPF control
  wired into the real existing free-text task/record-name fields (POA&M title, POA&M Milestone
  title, Automation Registry name, Script Library RequiredVariables) via a new
  `WorkspaceEdit.SuggestTerms` opt-in flag.
- 2026-09-15T23:47:33Z ✅ DONE — Shipped as shanemccaw/MyArchitect@b4c73fb (16 files changed, 822
  insertions/70 deletions). New: `Shell/TermMatcher.cs` (extracted from `CommandMatcher`, which
  now delegates to it — one real matcher implementation for both the Command Palette and this
  feature), `Models/{TechnicalTerm,Sop,MonitorCheckSummary}.cs`, `Services/{Sops,MonitorChecks,
  TechnicalTermCatalog}Service.cs` + interfaces, `Shell/TechnicalTermAutocompleteBox.{xaml,
  xaml.cs}`. `WorkspaceEdit.SuggestTerms` wired onto the real existing free-text fields: POA&M
  title, POA&M Milestone title, Automation Registry name (create+edit), Script Library
  RequiredVariables. All 4 proposed real sources confirmed reachable and real — none invented:
  write_action_catalog/baseline_action_templates (already wired via LaunchControlActionsService),
  real Change Request titles (already wired via ChangeControlService, `GET
  /api/msp/change-requests`), real SOP titles (`GET /api/msp/sops`, newly wired), real
  monitor_checks labels (`GET /api/msp/rbd/available-checks`, newly wired) — all four gated at the
  same `ladder.msp-operator` capability every other MyArchitect service already runs at.
  Verification: `dotnet build MyArchitect.csproj -c Debug` succeeded with 0 warnings/0 errors, both
  before and after a real merge conflict (a concurrent session landed #4290's Script Library
  "Replay last run" wiring to the exact same RequiredVariables block mid-session) was resolved by
  hand and rebuilt clean. Honest gap: no live WPF/WebView2 GUI run in this session (no interactive
  Windows session available here) — verification is real build success + careful hand-review of
  the conflict resolution, not a manual click-through. No findings filed — no genuine
  bug/gap discovered outside this issue's own scope during the build. No Shane To-Do: no manual
  SQL, no server restart, no Design export needed — this is client-side-only WPF wiring against
  four already-real, already-auth-gated endpoints.
