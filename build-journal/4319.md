# #4319 — Finish FontSize retrofit: 64 raw code-behind literals #4310 left untouched

- **Status:** ⏳ IN FLIGHT 2026-09-15T00:00:00Z
- **Scope:** buildconsole (work performed in shanemccaw/MyArchitect, not this repo)
- **Started:** 2026-09-15
- **Commit(s):** <fill at DONE — real hash from shanemccaw/MyArchitect>

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — verified current state on shanemccaw/MyArchitect
  main (2d475e8): #4310's own bookend (build-journal/4310.md) was left IN FLIGHT and
  never completed. Its XAML retrofit did land (170 → 0 real raw `FontSize="N"` XAML
  literals; the only 2 remaining grep hits are doc comments in Typography.xaml), but
  the 64 raw `FontSize = N` code-behind literals across 8 files
  (MainWindow.Shelf.cs, Shell/CommandPaletteOverlay.xaml.cs, Shell/FixedRibbonRenderer.cs,
  Shell/LeftReferencePanel.xaml.cs, Shell/RecordWorkspacePanel.xaml.cs,
  Shell/ReportPanelView.xaml.cs, Shell/TechnicalTermAutocompleteBox.xaml.cs,
  Shell/VaultPanelView.xaml.cs) were never touched, and `Theme.Size(` still has only 2
  call sites. UI_RULES.md §10's "Update (#4310)" note is currently false — it claims
  the gap is fully closed "verified by re-running this section's own grep with zero
  hits," which is not true for the .cs side. Retrofitting the remaining 64 code-behind
  literals to `Theme.Size("Key", fallback)`, then correcting the UI_RULES.md note to
  state real scope.
