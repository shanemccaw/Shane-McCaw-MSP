# #4319 — Finish FontSize retrofit: 64 raw code-behind literals #4310 left untouched

- **Status:** ✅ DONE 2026-09-15T00:00:00Z
- **Scope:** buildconsole (work performed in shanemccaw/MyArchitect, not this repo)
- **Started:** 2026-09-15
- **Commit(s):** `4541245babad90329c009877028646ce491cbc6c` (shanemccaw/MyArchitect main)

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — verified current state on shanemccaw/MyArchitect
  main (2d475e8): #4310's own bookend (build-journal/4310.md) was left IN FLIGHT and
  never completed. Its XAML retrofit did land (170 → 0 real raw `FontSize="N"` XAML
  literals; the only 2 remaining grep hits are doc comments in Typography.xaml), but
  the 64 raw `FontSize = N` code-behind literals across 8 files
  (MainWindow.Shelf.cs, Shell/CommandPaletteOverlay.xaml.cs, Shell/FixedRibbonRenderer.cs,
  Shell/LeftReferencePanel.xaml.cs, Shell/RecordWorkspacePanel.xaml.cs,
  Shell/ReportPanelView.xaml.cs, Shell/TechnicalTermAutocompleteBox.xaml.cs,
  Shell/VaultPanelView.xaml.cs) were never touched, and `Theme.Size(` still has only 2
  call sites. UI_RULES.md §10's "Update (#4310)" note is currently false — it claims
  the gap is fully closed "verified by re-running this section's own grep with zero
  hits," which is not true for the .cs side. Retrofitting the remaining 64 code-behind
  literals to `Theme.Size("Key", fallback)`, then correcting the UI_RULES.md note to
  state real scope.
- 2026-09-15T00:00:00Z ✅ DONE — retrofitted all 65 raw `FontSize = N` code-behind
  literals across the 8 files named in #4319 (one of the 64 counted by the issue's
  own grep was actually a `big ? 19 : 12.5` ternary its grep pattern didn't match,
  so 65 real literals not 64) to `Theme.Size("Key", fallback)` calls. Reused
  existing `Typography.xaml` keys where the value already matched a role in use
  (`BodyFontSize`, `CaptionFontSize`, `MonospaceFontSize`, `TabHeaderFontSize`,
  `QueryInputFontSize`); added six new keys for roles none of the existing sizes
  covered: `MicroCaptionFontSize` (9), `SmallLabelFontSize` (10),
  `RibbonIconFontSize` (16), `RibbonLargeIconFontSize` (26),
  `FactValueLargeFontSize` (19), `FactValueFontSize` (12.5). While rebasing onto
  origin/main, a concurrent commit (#4310's real successor work) replaced
  `Shell/CommandPaletteOverlay.xaml.cs` with `Shell/CommandPaletteWindow.xaml.cs`,
  carrying 13 of its own fresh raw `FontSize` literals plus reintroducing 2 more
  in `RecordWorkspacePanel.xaml.cs` via a real merge — fixed those in the same
  pass (two more new keys: `TagBadgeFontSize` (8.5), `DetailBodyFontSize` (11.5))
  so the final state is genuinely clean, not clean-at-a-point-in-time. Corrected
  UI_RULES.md §10's "Update (#4310)" note, which had falsely claimed the retrofit
  was complete codebase-wide when the `.cs` side was never touched, and added a
  new "Update (#4319)" note recording the real, verified state. Re-ran both of
  the issue's own grep commands (`FontSize="[0-9]` in `*.xaml`, `FontSize = [0-9]`
  in `*.cs`) plus a broader `FontSize\s*=` scan (to catch non-integer-literal
  forms like the ternary) — zero real hits; the only 2 XAML grep matches are this
  section's own historical doc comments in `Typography.xaml`, not code.
  `dotnet build MyArchitect.csproj -c Debug` succeeds, 0 warnings, 0 errors.
  Pushed to `shanemccaw/MyArchitect` main as `4541245babad90329c009877028646ce491cbc6c`
  after two rebases onto concurrently-landed commits (#4324 and its bookend).
