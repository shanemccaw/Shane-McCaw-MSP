# #4523 — templateNodeId mirror doesn't sanitize dots

- **Status:** ✅ DONE 2026-09-17T22:30:00Z
- **Scope:** platform

## Log

- 2026-09-17T22:12:00Z — IN FLIGHT. Plan written at
  [`build-journal/4523-plan.md`](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/5d821b3b8/build-journal/4523-plan.md)
  (commit `5d821b3b8`). Fixing
  `artifacts/admin-panel/src/components/simulatorConfigPack.ts`'s
  `templateNodeId` to sanitize dots (`.replace(/\./g, "-")`), matching the
  backend's real `nodeIdSafe` (`artifacts/api-server/src/lib/config-pack-graph.ts`)
  and the already-correct marketing-site mirror
  (`artifacts/shane-mccaw-consulting/src/hooks/useBuyPackLive.ts`).
- 2026-09-17T22:30:00Z — ✅ DONE. Fixed and pushed as commit `4bef57a2f`.

## Changes Made

- [`artifacts/admin-panel/src/components/simulatorConfigPack.ts`](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/4bef57a2f/artifacts/admin-panel/src/components/simulatorConfigPack.ts#L21-L24) —
  `templateNodeId` now sanitizes dots (`templateId.replace(/\./g, "-")`) before
  building the `tpl-` node id, matching the backend's `nodeIdSafe`.
- [`artifacts/admin-panel/src/components/simulatorConfigPack.test.ts`](https://github.com/shanemccaw/Shane-McCaw-MSP/blob/4bef57a2f/artifacts/admin-panel/src/components/simulatorConfigPack.test.ts#L70-L83) —
  added a dotted-`templateId` test case pinning the fix (Git #4523).

## Verification Results

- `npx vitest run src/components/simulatorConfigPack.test.ts` (from
  `artifacts/admin-panel`) — **11/11 passed**, including the new dotted-id
  case.
- Local Postgres, re-ran the issue's own blast-radius query against
  `config_pack_templates`: `90 | 20` (90 dotted rows across 20 packs of 92
  total) — confirms the fix addresses the real, currently-seeded data, not a
  hypothetical.
- `node scripts/dev-server/verify-branch-merged.mjs` → exit 0, `agent/4523-q3185`
  is an ancestor of `origin/main` at `4bef57a2f`.
- `git status --porcelain` clean.
