# #4099 — Ctrl+K chat search surfaces archived chats — should exclude them entirely

- **Status:** ✅ DONE 2026-09-14T18:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** f7c82069c

## Log
- 2026-09-14T18:19:00Z ⏳ IN FLIGHT — Locating `CommandCenterSearchService.cs` SearchAsync, adding `AND c.archived IS NOT TRUE` to the Chat branch of the UNION query.
- 2026-09-14T18:35:00Z ✅ DONE — Added `AND c.archived IS NOT TRUE` to the Chat branch of the Ctrl+K search UNION query in `CommandCenterSearchService.cs:150`. Issue/Epic (`bt_issue_mirror`) branch untouched. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors/0 warnings. `archived`/`archivedAt` confirmed as real `bt_chats` columns (already read directly in `LeftSidebar.xaml.cs`), so no schema change needed — pure query fix. Not live-UI-verified against a running BuildConsole instance (build/run verification for this WPF surface is a compile + code-path check, not `shaneapp://runTest`, which targets `artifacts/msp-portal` / `shane-mccaw-consulting`). Branch merged into origin/main (`verify-branch-merged.mjs` OK, e6a2c853), working tree clean.
