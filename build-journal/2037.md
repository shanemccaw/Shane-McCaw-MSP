# #2037 — Phase 2 write packs: App Governance (appgov:*)

- **Status:** ✅ DONE 2026-09-12T16:52:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** `a05fc3bbd` (rebased/pushed to main as `70fbfe6d1`)

## Log
- 2026-09-12T16:21:35Z ⏳ IN FLIGHT — read #2037 body/comments and #1925's reference shape (2026-08-31 migrations); confirmed live DB state: 9 `appgov:*` checks exist, none carry a `check_key`, 3 (`consent-policy-status`, `enterprise-app-count`, `enterprise-app-registration-list`) have empty `severity_rules` (no finding ever fires — informational only). Next: fetch real Microsoft Graph docs in-session for the remediable checks and author `baseline_action_templates` rows + themed pack + mappings + migration, then `planOnly` dry-run against testbed tenant 1.
- 2026-09-12T16:52:00Z ✅ DONE — `lib/db/migrations/manual/2026-09-12-app-governance-write-pack-2037.sql` authored and run against local `DATABASE_URL` (idempotent, verified by re-run: 0 inserts second pass). Discovered 2 pre-existing, already-execution-ready `baseline_action_templates` rows (`action.disable-risky-app`, `microrem.remove-risky-app-consent`) that were never bound to any pack/check_key — reused them (Phase-1 map-first discipline) rather than duplicating. Authored 3 new sibling templates (`action.disable-dormant-service-principal`, `action.revoke-unreviewed-consent-grant`, `action.delete-stale-app-registration`) plus its real single-step reverse (`action.restore-deleted-application`, Graph's own `POST /directory/deletedItems/{id}/restore`), each verified against Microsoft Graph docs fetched this session (serviceprincipal-update, oauth2permissiongrant-delete, application-delete, directory-deleteditems-restore — URLs recorded in the migration file). New `app-governance-v1` config pack binds 5 of the 9 `appgov:*` checks: `workload-identity-risk`, `dormant-service-principals`, `risky-permission-grants`, `unreviewed-consents`, `stale-app-registrations`. `we_can_run`-capable checks: 20 → 25.
  Deliberately left unmapped, documented in the migration: `appgov:cert-secret-expiration` (researched `application:removePassword` — real and safe — and `application:removeKey` — requires a proof-of-possession JWT signed by a key the platform doesn't hold, so certificate removal is structurally not automatable; a password-only partial fix would misreport the combined finding as satisfied) and `appgov:consent-policy-status` / `appgov:enterprise-app-count` / `appgov:enterprise-app-registration-list` (all three carry `severity_rules = []` — no failure mode exists to remediate).
  Phase 3: real `planOnly` dry-run via `buildConfigPackDryRun('app-governance-v1', 1)` against testbed tenant 1 (`tenants.id=1`, `c4c814d4-3afe-441e-9145-62461d0a4fd3` / mccawsoft2.onmicrosoft.com) — script `artifacts/api-server/scripts/verify-app-governance-write-pack-2037.mts`, PASS: all 5 steps resolved a real method/endpoint/body, the orchestrator's verification gate correctly coalesced onto the pack's destructive steps (`action.disable-risky-app` gated; `microrem.remove-risky-app-consent` and `action.delete-stale-app-registration` coalesced into that same gate). No writes executed against the testbed. Migration logged on #1630. No new findings filed — the 40 other orphaned execution-ready templates found during this pass fall outside appgov and are the same already-tracked backlog #1925 itself reported (its own fan-out issues #2038/#2039/#2040 cover the remaining domains). Verified: `verify-branch-merged.mjs` OK (branch `70fbfe6d` ancestor of `origin/main`); `git status --porcelain` clean.
