# #3065 — api-server package.json test script dead file references

- **Status:** ✅ DONE 2026-09-07T00:10:00Z
- **Scope:** platform

## Log
- 2026-09-07T00:00:00Z — Starting: remove 2 dead file references (kanban-auto-fire-routing.test.ts,
  kanban-auto-fire-retry-budget.test.ts) from `artifacts/api-server/package.json`'s `test` script per
  issue #3065.
- 2026-09-07T00:10:00Z — ✅ DONE — Removed both dead paths from `artifacts/api-server/package.json`'s
  `test` script: dropped `src/lib/kanban-auto-fire-routing.test.ts` from the main `node --test`
  invocation, and dropped the trailing `&& node --experimental-strip-types --test
  'src/lib/kanban-auto-fire-retry-budget.test.ts'` clause entirely (it was that file's only entry).
  Verified resulting `package.json` is valid JSON. Commit `bf9d85c76608df2bc714de1be2798a2628e7d95e`
  pushed to `origin/main` directly; `verify-branch-merged.mjs` confirms ancestor of origin/main.
