# #3059 — shaneapp://runTest fails to resolve manifests under test-manifests/portal/

- **Status:** ✅ DONE 2026-09-12T22:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 864fffec9 (runTest stale-checkout diagnosis), 700b7317a (IN FLIGHT bookend)

## Log
- 2026-09-12T22:20:00Z ⏳ IN FLIGHT — investigating the reported manifest-resolution failure against test-manifests/portal/ (security-plan.json, risk-register.json).
- 2026-09-12T22:45:00Z ✅ DONE — Live-verified against the real, running BuildConsole instance: invoked the actual `shaneapp://runTest` protocol against `test-manifests/portal/risk-register.json` and `.../security-plan.json` — both resolved correctly (`manifestPath` populated, `RunManifestAsync` ran all steps; the step failures returned were real, unrelated 403s from the api-server's authz, not resolution failures). Could not reproduce the reported "manifest not found or unparseable" at all today.

  Root-caused instead of blind-fixing: `ResolveManifestPath`'s recursive `Directory.EnumerateFiles` scan is not depth- or directory-name-sensitive (proven standalone against the real repo root — no path-construction/portal-specific bug exists). The real cause: `FindRepoRoot()` anchors on the checkout BuildConsole.exe itself runs from (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`), which only advances via an explicit dev-server merge-back, never a periodic `git pull` — confirmed live, that checkout was sitting 15 commits behind `origin/main` during this build with nothing surfacing it. A manifest merged into `origin/main` shortly before an agent's `runTest` call is genuinely invisible there until the next merge-back lands it; it self-resolves on its own, which is why the original repro (six days old) no longer reproduces.

  Shipped a real fix in that light: `DiagnoseManifestNotFoundAsync` in `desktop/BuildConsole/MainWindow.ShaneAppRunTest.cs` — on a manifest-not-found, best-effort (bounded 5s per git call, never throws/blocks) checks whether the bare filename exists in `origin/main`'s `test-manifests/` tree while the local checkout sits behind it, and says so plainly in both the result envelope's `error` and the activity log, instead of a bare "not found" that sends the next investigator down a path-bug rabbit hole. Verified in isolation against a real synthetic bare-repo-origin + one-commit-behind checkout: fires correctly for the stale case, stays silent (no false positive) for a genuinely nonexistent filename. `dotnet build` clean, 0 warnings/errors.

  Working tree clean (`git status --porcelain` empty); branch confirmed merged into `origin/main` via `verify-branch-merged.mjs`.
