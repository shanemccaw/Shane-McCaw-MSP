# Git #3791 — Status bar: move Deploy to the true far-left edge

- **Status:** ✅ DONE 2026-09-12T06:44:00Z
- **Scope:** buildconsole
- **Log:**
  - 2026-09-12T06:37:36Z — Started. Reordered `MainWindow.xaml` status bar `Grid.ColumnDefinitions`: Deploy is now column 0 (`Auto`), spring (`*`) moved to column 1. `StatusBarItem` for Deploy changed from `Grid.Column="1"` to `Grid.Column="0"`. All other columns (separators, usage meter, Claude status, clock) unchanged — pure reorder, no removals.
  - 2026-09-12T06:44:00Z — ✅ DONE — Commit `3e1863fd3` ("Status bar: move Deploy to true left edge (Git #3791)"), rebased/pushed to `main` as `2199a6dd0947e7c86666e90586644e0d1caed0be`. `dotnet build` succeeded (0 errors, pre-existing unrelated warnings only). `verify-branch-merged.mjs` confirms ancestor of `origin/main`. `git status --porcelain` clean.
