# #1700 — Microsoft Changes: routed CR workload/category mis-derived as Identity for an Exchange/Outlook change

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 734b79af3 (local), landed on main as 008462b75

## Log
- 2026-08-29 ⏳ IN FLIGHT — investigating `createRoutedChangeRequest` / `deriveWorkload` in m365-change-router.ts and lib/portal-change-control.ts; will derive workload from the interpretation's structured service/workload signal instead of the comma-joined targetResource string. Will confirm fix against CR-2026-183 in the DB.
- 2026-08-29 ✅ DONE — added `deriveWorkloadFromTouches(touches)` to `portal-change-control.ts`, reading the interpretation's structured `touches.services`/`settings` directly instead of re-parsing the flattened `targetResource` display string. Wired `m365-change-router.ts`'s `createRoutedChangeRequest` to call it. Left `deriveWorkload` itself untouched — it is still correct for the customer CR wizard (`routes/portal-change-control.ts`) and runbooks (`routes/portal-runbooks.ts`) paths, which genuinely pass technical cmdlet/Graph-endpoint strings. Added unit tests in `portal-change-control.test.ts` covering CR-2026-183's real touches plus each of the eight workloads and the Purview-over-SharePoint clause order. All 46 tests in `portal-change-control.test.ts` + `m365-change-router.test.ts` pass; `tsc --noEmit` on `artifacts/api-server` is clean.
  Data correction: `lib/db/migrations/manual/2026-08-29-fix-routed-cr-workload-1700.sql` recomputes `category` for every existing routed (`source_kind='microsoft_change'`) CR from its interpretation's `touches`, mirroring the fixed function's pattern order. Run against local `DATABASE_URL`: 1 row updated. Logged to #1630.
  Verified directly against the database (not by reading the code path): `SELECT category FROM msp_change_requests WHERE id = 83` (CR-2026-183) now returns `Exchange`, was `Identity`.
  Branch merge verified via `scripts/dev-server/verify-branch-merged.mjs` before writing DONE. `git status --porcelain` clean.
