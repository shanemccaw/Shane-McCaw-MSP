# #4435 — Free role mislabeled as staff on identity interstitial

- **Status:** ⏳ IN FLIGHT 2026-09-16T00:05:00Z
- **Scope:** platform

## Log
- 2026-09-16T00:05:00Z — Started. Plan: give `Free` its own honest copy branch on `portal-identity-interstitial.tsx` (not staff messaging, no Admin Panel button) per issue's default fix.
- **Status:** ✅ DONE 2026-09-16T00:35:00Z

## Resolution
Default (simpler) fix used: gave `Free` its own honest copy branch on
`artifacts/portal/src/pages/portal-identity-interstitial.tsx` instead of
restructuring routing. Investigation confirmed the `PENDING_PURCHASE_ROLES`-style
routing approach would have been more invasive for no real benefit — `Free`
still legitimately needs to land on this client-side gate (RequireAuth fires on
anything `!== "Customer"`), it just needed different copy, not a different route.

- Commit: `4dfd4a006` (rebased/pushed to origin/main as `ac51f889a`)
- Live-verify: confirmed real `POST /api/auth/login` response for a genuine
  `Free`-role DB user (`other.vitest-2984-865828553@scratch.invalid`, scratch
  account) returns `"mspRole":"Free"`; reviewed the interstitial's `isFree`
  branch renders "Your account isn't fully set up yet" (no staff/Admin Panel
  copy, no "Log out and sign in at the Admin Panel" button) for that role.
  Full browser/DOM runTest was not available in this session (no dev
  frontend/WebView2 runner reachable, no Playwright installed) — verification
  is API response + code-path review, not a captured screenshot. Temporarily
  set/reset that scratch user's `password_hash` for the login check, restored
  to NULL afterward.
- `pnpm exec tsc --noEmit` in `artifacts/portal` — clean.
- `verify-branch-merged.mjs` — OK, ancestor of origin/main.
- `git status --porcelain` — clean.
- No findings filed; this was a targeted copy fix.
