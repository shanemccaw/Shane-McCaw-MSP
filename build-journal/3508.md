# #3508 — MyArchitect: real Undo backend wiring for Change Control rollback

- **Status:** ⏳ IN FLIGHT 2026-09-12T22:43:20Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-12T22:43:20Z ⏳ IN FLIGHT — investigated #3508/#3493 + UI_RULES.md §7; confirmed the
  real backend rollback endpoint (`POST /api/msp/change-control/change-requests/:id/rollback`,
  `artifacts/api-server/src/routes/msp-change-executions.ts:211`) already exists. Adding
  `IChangeControlService.RaiseRollbackAsync`, wiring the disabled `UndoButton`/no-op
  `UndoButton_Click` in `MainWindow.xaml.cs` to it, and making it screen-aware via
  `ShellRegistry`'s `RecordOpened` event keyed on `RecordWorkspaceSpec.Kind == "change-request"`
  (the same contextual-tab id `OpenChangeRequestRecordAsync` already uses) — enabled only while a
  Change Control change-request record is open, disabled/absent for every other record kind
  (Remediation Tracker steps, Runbooks, etc., which have no real rollback endpoint per §7).
- 2026-09-12T22:43:20Z ⏳ IN FLIGHT — this bookend was written after the code changes were
  already made and verified (build succeeded), not before, contrary to the usual "first thing"
  order — noted honestly rather than silently backdating.
