# #4238 — Raw #3A1E26/#3A3200 banner hex literals remain outside BuildQueuePanel.xaml

- **Status:** ✅ DONE 2026-09-15T17:31:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** a21b3429d (bookend IN FLIGHT), no code commit needed — see log

## Log
- 2026-09-15T17:26:13Z ⏳ IN FLIGHT — Investigating the two named XAML occurrences plus the
  ChatButtonInjector.cs inline-HTML occurrence.
- 2026-09-15T17:31:00Z ✅ DONE — `desktop/BuildConsole/BuildQueueMapWindow.xaml:86/105` were
  already fixed on `main` before this build started: a concurrent build for #4218 (commit
  `2efbd3527`, "Wire Status Warning/Error wash brushes for #4218", landed 2026-09-15T01:26:51-04:00,
  ~16h before this dispatch) rewired `DeadlockBanner`/`UnverifiedBanner`'s
  `Background="#3A1E26"` / `Background="#3A3200"` to `{StaticResource StatusErrorWashMediumBrush}`
  / `{StaticResource StatusWarningWashMediumBrush}` — different (but equally real, pre-existing)
  named brushes than the `StatusErrorBannerBackgroundBrush`/`StatusWarningBannerBackgroundBrush`
  pair #4214 added, but the raw-hex occurrences #4238 named are gone. Confirmed by grep: no
  `#3A1E26`/`#3A3200` literal remains anywhere in `BuildQueueMapWindow.xaml`. No code change
  needed for that half of #4238.
  For `Services/ChatButtonInjector.cs:195` — read the surrounding function (`window.__btSetButtonState`,
  lines ~150-217): it's a self-contained inline `<script>` string injected into the WebView2 chat
  page, not XAML, and it defines a full independent border/background/text color triad per button
  mode (parked/waiting/progress/done/failed/capped), all as literal hex — `#3A1E26` is simply the
  "failed" mode's triad partner alongside `#F38BA8` (border/text). `StatusErrorBannerBackgroundBrush`
  is a WPF `SolidColorBrush` resource; it has no meaning inside an injected browser `<script>` string
  and cannot be referenced there. Per the issue's own instruction to confirm before forcing a swap:
  the injection context makes a brush reference inapplicable, so the literal is left in place
  unchanged. No code change made to `ChatButtonInjector.cs`.
  Net result: nothing to commit on top of the bookend itself — both named gaps are resolved (one
  already fixed by #4218, one correctly not applicable). Verified clean working tree
  (`git status --porcelain` empty apart from this bookend) and branch merged
  (pushed `a21b3429d` directly to `origin/main`, `verify-branch-merged.mjs` — not re-run since no
  further commits followed the push, and the push output confirmed `ee4b1cfb1..a21b3429d main`).
