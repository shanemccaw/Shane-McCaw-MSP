# #2010 — DNS transport for the configuration snapshot collector

- **Status:** ✅ DONE 2026-09-04T07:00:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 9d7b43429 (rebased onto main as ed87c49bd)

## Log
- 2026-09-04T06:49:08Z ⏳ IN FLIGHT — wiring a `dns` transport into
  `config-snapshot-collector.ts` (#1796), reusing `monitor-executor.ts`'s
  existing `queryTxtRecords`/`DKIM_DEFAULT_SELECTORS` (exported for reuse) for
  SPF/DKIM/DMARC TXT lookups against the tenant's verified domains (read live
  from Graph `/domains`, filtered `isVerified`). Registering a new
  `config_snapshot_resource_types` row (`dns:txt:email-authentication`,
  composite-key identity on domain+recordType+name) via a manual migration,
  and exempting `dns`-transport rows from `build-snapshot-registry.mjs`'s
  retire sweep since they have no `config_resources` source to upsert against.
- 2026-09-04T07:00:00Z ✅ DONE — implemented, migrated, and verified live.
  `config-snapshot-collector.ts` now has a `dns` branch (`collectDnsResource`)
  that reads verified domains from a live Graph `/domains` call and reuses
  `monitor-executor.ts`'s exported `queryTxtRecords`/`DKIM_DEFAULT_SELECTORS`
  for SPF/DMARC/per-selector-DKIM TXT lookups — one object per
  (domain, recordType, name), composite-key identity. Seeded
  `config_snapshot_resource_types` row `dns:txt:email-authentication` via
  `lib/db/migrations/manual/2026-09-04-config-snapshot-dns-transport-2010.sql`,
  run against local Postgres (ran clean, verified with a SELECT). Exempted
  `read_transport = 'dns'` rows from `build-snapshot-registry.mjs`'s retire
  sweep (it upserts only from `config_resources`, which has no DNS-record
  entries) — verified by running the registry rebuild for real and confirming
  the row survived with `is_collectable = true`. Added errno-coded DNS
  resolver failures to `classifySnapshotFailure()` as `transport_error`.
  Live-verified against the real testbed tenant (`tenants.id` 1) via
  `collect-config-snapshot.ts --transports dns`: 9 verified domains × 4
  lookups = 36 real DNS records collected, snapshot sealed `is_complete:
  true`; real SPF found for 5 domains, real DMARC for shanemccaw.com — spot
  checked in the DB. `config-snapshot-collector.test.ts` 13/13 pass.
  `monitor-executor.test.ts` + `config-snapshot-differ.test.ts` have 13
  pre-existing failures (a `vi.mock` missing `PowerPlatformNotRegisteredError`
  export) confirmed present on unmodified `main` before this session touched
  anything (stashed my changes, reran, same 13 failures) — filed as
  **#2838**, parented under #1850 (this issue's own Feature), board status
  set to AI Batter Up. Typecheck clean
  (`artifacts/api-server`: `npx tsc --noEmit`). Commit `9d7b43429`, rebased
  onto `main` and pushed as `ed87c49bd`. Branch-merged check and
  `git status --porcelain` both clean before this line was written.
