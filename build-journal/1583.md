# #1583 — My Architect: the hand-typed advisory ledger

- **Status:** ✅ DONE (coordinating issue — all real work already landed under its own leaf issues)
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** none from this session — no code change; GitHub-side edge fix only

## Log
- 2026-08-30 ⏳ IN FLIGHT — Pulled #1583's body + its one comment (identical, the dispatch
  prompt). #1583 has no formal `sub_issues` via the API, but its body names four leaf work
  items to check: #1589 (foundation, id-space fix), #1588 and #1590 (fixture leaks), #1584
  (ShaneBot placement, gated by #1269).
- 2026-08-30 ✅ DONE — Checked each named leaf issue's own bookend, all already resolved by
  prior sessions on this branch:
  - **#1589** — DONE for real. `portal-retainer.ts` now resolves the caller's tenant exactly
    once and sources `status_reports` from that same tenant, eliminating the dual id-space
    bug. Commit `4c62096d5`.
  - **#1588** — DONE as moot. `retainerModel.ts` (the file with the hardcoded "Priya Raman")
    was deleted wholesale in `f40438cdc`'s portal-v2 retirement, before this build ran. No
    replacement retainer page exists yet under `artifacts/portal` (no Design export either),
    so per SCOPE STOP there is nothing to build against.
  - **#1590** — DONE as moot, same root cause: `useRetainerLive.ts` (the `NO_TERMS = []` file)
    was deleted in the same commit. Architectural finding preserved (retainer terms are static
    per-account copy, not DB-backed) for whenever #1581/#1745/#1746 rebuild the page.
  - **#1584** — genuinely still gated. #1269 (ShaneBot Shane-tested sign-off gate) is still
    **open**. Per this build's own prompt ("must not start early") and Git #1987 (a blocking
    conclusion must be a `blocked_by` edge, not just prose), found #1584 had **no** edge
    recorded despite the prose gate — wired it: `gh api -X POST
    .../issues/1584/dependencies/blocked_by -F issue_id=<1269's real id>`, verified the edge
    now reads back, and added the `blocked` label. Left #1584's existing `Shane To-Do` label
    alone (placement decision, not a build).
  - **DB check:** `retainer_work_log`, `status_reports`, `retainer_settings` all confirmed via
    direct `psql` — still 0/0/0 rows, unchanged, matching prior sessions' findings.
  - Branch verified merged into `origin/main` (`verify-branch-merged.mjs` exit 0).
  - `git status --porcelain` clean before writing DONE — no files touched in the working tree
    this session (the only mutation was the GitHub-side `blocked_by` edge + label).
  - No new findings to file — the two live fixture leaks and the id-space bug were already
    caught and resolved by the sessions preceding this one; the only gap this session found
    (missing `blocked_by` edge on #1584) was fixed in the same action per the #1987 rule, not
    filed as a separate issue.
