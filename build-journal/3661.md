# #3661 — Build Queue: self-blocked "WAITING" items never automatically requeue once their real blocker actually closes

- **Status:** ✅ DONE 2026-09-11T15:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 7e03b671e

## Log
- 2026-09-11T15:05:00Z ⏳ IN FLIGHT — Confirmed live repro against real `BUILD_DATABASE_URL` (`BuildConsole` local Postgres db, not the product `shanemccawmsp` db which holds only a stale pre-migration copy per BuildQueuePostgresClient's own doc comment): `bt_build_queue` id 2129 (Git #3420) is `status='canceled'`, `exit_code=0`, `blocked_by_numbers={3419}`; `gh issue view 3419` confirms `state: closed`. 4 total real rows currently match the self-blocked-WAITING shape (`status='canceled' AND exit_code=0 AND blocked_by_numbers` non-empty). Read #3573 (`SessionLimitAutoRestartService`'s periodic-sweep pattern: 5-min `Timer`, overlap guard via `Interlocked`, `ActivityLog` channel) as the cadence/logging pattern to mirror, and `BuildQueuePostgresClient.EvaluateCandidatesAsync`/`GetNextAsync` as the real "blocker is closed" definition to reuse (GitHub `state:closed` OR `DoneBookendVerifier` — Git #2225). Implementing: `BuildQueuePostgresClient.GetWaitingSelfBlockedAsync`/`RequeueWaitingRowAsync`/`SweepAutoRequeueWaitingAsync` (core DB+decision logic, reuses `EffectiveBlockers`/`DoneBookendVerifier`, fails closed on unreachable GitHub) + new `Services/WaitingRowAutoRequeueService.cs` (periodic timer wrapper, channel `auto-requeue`) wired into `MainWindow.xaml.cs` alongside `_sessionLimitAutoRestart`, gated by `!AppMode.IsAgent` same as the other periodic services.
- 2026-09-11T15:45:00Z ✅ DONE — Commit `7e03b671e` on `main` (pushed directly, verified via `verify-branch-merged.mjs`). Shipped: `BuildQueuePostgresClient.GetWaitingSelfBlockedAsync`/`RequeueWaitingRowAsync`/`SweepAutoRequeueWaitingAsync` (real decision logic: for every self-blocked WAITING row, every declared blocker must be GitHub-closed OR DoneBookendVerifier-satisfied, else the row is left untouched; fails closed on an unreachable GitHub) + new `Services/WaitingRowAutoRequeueService.cs` (5-min periodic `Timer`, `Interlocked` overlap guard, `auto-requeue` ActivityLog channel — same shape as #3573's `SessionLimitAutoRestartService`) + wired into `MainWindow.xaml.cs` alongside `_sessionLimitAutoRestart`, gated `!AppMode.IsAgent`. Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 errors. Live, real (not simulated) verification against `BUILD_DATABASE_URL`: ran `GetWaitingSelfBlockedAsync`'s exact SQL directly — returns all 4 real self-blocked WAITING rows (ids 1355/#1425, 2129/#3420, 2140/#3433, 2291/#3610). Ran the exact `gh issue list --state open` query the sweep's live fetcher uses: confirms #3420's only declared blocker (#3419) is closed → the sweep would correctly auto-requeue id 2129 on its next tick; the other three each still have ≥1 open blocker (#1621, #3424, #3644) → correctly left alone (negative-case verification, not just the one positive case the issue named). Did not manually flip #3420's DB row to `queued` as part of this session — a real desktop BuildConsole instance polls this same shared DB and status='queued' is immediately claimable, so a manual flip risks launching a real, unintended build as a side effect of verification; the periodic sweep itself performs that flip once Shane's running BuildConsole app is rebuilt/restarted with this code. `git status --porcelain` clean. No findings filed this session — no genuine out-of-scope bug/gap discovered.
