# #2028 — Build Queue search: submit-on-Enter only, never live-filter per keystroke

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** a105077cc (merged to main via fast-forward push, 076d1c9ca..a105077cc)

## Log
- 2026-08-31 ⏳ IN FLIGHT — auditing Build Queue search input to find where filtering fires per keystroke; will convert to submit-on-Enter only.
- 2026-08-31 ✅ DONE — Audited `desktop/BuildConsole/Controls/BuildQueuePanel.xaml(.cs)`: the #1833 fix only added a 250ms debounce timer on `TextChanged`, which still fires mid-type after any settled pause — not what #2028 asks for. Replaced it: `QueueSearchBox_TextChanged` now only updates the tracked `_queueSearch` string (for the placeholder `DataTrigger`) and never renders; a new `QueueSearchBox_KeyDown` handler fires `RenderQueue(ApplyFilter(_lastItems))` exactly once, only on `Key.Enter`. Removed the now-dead `_queueSearchDebounceTimer`/`QueueSearchDebounceInterval` fields. Also fixed `RevealQueueItem`'s programmatic search-clear path (previously relied on the debounce-triggering `TextChanged` to re-render after clearing) to explicitly re-render after clearing the box. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors. `git status --porcelain` clean; branch fast-forwarded onto origin/main (verify-branch-merged.mjs: OK).
