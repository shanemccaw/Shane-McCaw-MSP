# #3610 — Drop orphaned pre-GA4 analytics tables (analytics_site_events, analytics_pageviews, analytics_sessions)

- **Status:** ✅ DONE 2026-09-11T20:25:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 389a3f59e (migration file + Drizzle schema cleanup)

## Log
- 2026-09-11T03:35:00Z ⏳ IN FLIGHT — Pulled issue #3610 body/comments. #123's cleanup scoped dropping analytics_site_events, analytics_pageviews, analytics_sessions but never executed the drop; tables confirmed still present in real local DB. About to grep repo-wide to confirm zero live reads/writes before writing a manual-SQL drop migration (not self-executed — destructive per CLAUDE.md).
- 2026-09-11T03:45:00Z 🛑 BLOCKED — Premise disproven: `artifacts/api-server/src/routes/admin-marketing.ts` still queries all three tables live across 4 requireAdmin routes (KPI, analytics, analytics/insights, active-campaign-badges + SSE variant) — matches a comment #123 itself left in `lib/db/src/schema/index.ts:1750-1755`. Direct query confirms all three tables are 0 rows with zero INSERT call sites anywhere (ingestion route deleted by #123) — the dashboards have been silently reading dead data since. Filed #3625 (parented #1095, board status AI Batter Up) to repoint those routes at GA4 (matching #3401's pattern), wired a real `blocked_by` edge #3610→#3625, applied `blocked` label, commented on #3610. No migration written — the tables are not safe to drop yet. No code changed.
- 2026-09-11T20:25:00Z ✅ DONE — Resumed after a fresh dispatch. Synced worktree to latest `origin/main`; re-ran the repo-wide grep + direct Postgres query rather than trusting labels. Both blockers are genuinely fixed in code: #3625 (admin-marketing.ts's 4 dashboard routes now return honest 0/empty pending a GA4 rebuild, #3437) and #3644 (seed-system-workflows.ts's Engagement Offer follow-up workflow no longer references analytics_sessions) — repo-wide grep for the raw table names and Drizzle export names now finds zero live call sites, and all three tables are still 0 rows. Wrote `lib/db/migrations/manual/2026-09-11-drop-legacy-site-analytics-tables-3610.sql` (DROP TABLE IF EXISTS for all three, self-marking INSERT into simulator_migration_runs) and removed the now-dead `analyticsSessionsTable`/`analyticsPageviewsTable`/`analyticsSiteEventsTable` Drizzle definitions from `lib/db/src/schema/index.ts` — nothing imports them anymore. `lib/db` and `artifacts/api-server` both typecheck clean (`tsc -b` + `tsc --noEmit`). **Did not execute the migration** — dropping tables is destructive per CLAUDE.md's Database section, so it's Shane's to run himself; labeled `Shane To-Do` accordingly. Committed `389a3f59e`, pushed to `main`. Cleared both `blocked_by` edges (#3625, #3644 — both confirmed resolved) and the `blocked`/`in-flight` labels; set `complete` + `Shane To-Do`. Commented on #3610 with the full trail and the migration's real repo-relative path.
