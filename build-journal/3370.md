# #3370 — Audit Log (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T01:55:25Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** `74de244dd` (bookend IN FLIGHT), `ba144aef9`/`527c67241` (contract pack, rebased
  hash after push — same content)

## Log
- 2026-09-10T01:39:17Z ⏳ IN FLIGHT — investigated `msp-audit-log.ts` (146 lines, 1 route,
  `GET /api/msp/audit`), the `msp_audit_logs` schema, all ~11 writer call-sites across the
  api-server + mcp-server, and live local Postgres data (522 rows). Confirmed the route is
  genuinely complete (no incompleteness markers). Writing
  `docs/msp-console/audit-log-msp-console-contract-pack.md` next per the #1642 extraction
  pattern — read-only, every field cited to file:line, cross-checked live against local
  Postgres. Also found a real, out-of-scope finding along the way (an admin-panel dev tool,
  `GraphProbeModal.tsx`, fabricates fake probe responses instead of making a real HTTP call,
  and one of its endpoint presets points at a URL — `/api/msp/audit-logs` — that has never
  existed; the real route is `/api/msp/audit`) — filing separately per the mandatory-finding
  rule, not fixing in this pack (out of scope for a read-only extraction).
- 2026-09-10T01:55:25Z ✅ DONE — `docs/msp-console/audit-log-msp-console-contract-pack.md`
  written and pushed to `main` (`527c67241`). Read-only: only the pack and this bookend
  changed; no product code, schema, or UI touched. Wire contract for `GET /api/msp/audit`
  extracted field-by-field (query filters, response aliasing, the `actorEmail`-falls-back-to-
  `actorRole` quirk, six raw columns never reached by this wire), the real cross-surface
  writer catalog (~11 route files + the MCP server's direct-Postgres write path, no shared
  insert helper anywhere), real vocabularies (no DB CHECK on `outcome`/`actionType`/
  `entityType` — all plain `text`), and live counts re-queried against local Postgres (522
  rows total, dominated by AUTH_LOGIN/LOGOUT — the large `msp-settings.ts`-family vocabulary
  is real code with zero live rows locally). Filed the out-of-scope finding as **#3450**
  (GraphProbeModal fabricates fake probe responses; dead `/api/msp/audit-logs` preset),
  parented under **#1095** (EPIC: Admin Panel — the finding is in `artifacts/admin-panel`,
  no Feature-tier parent applies), board status set to "AI Batter Up". Verified:
  `verify-branch-merged.mjs` exit 0, `git status --porcelain` clean.
