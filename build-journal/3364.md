# #3364 — Drift timeline label resolution (id-keyed setting → display name)

- **Status:** ✅ DONE 2026-09-09T20:40:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** d4f8d7902 (rebased; original commit 33109ce15) — "Resolve drift timeline labels to display names instead of raw setting paths (Git #3364)"

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — investigating drift-check-specs.ts, dashboard-resolvers.ts, admin-drift.ts, drift-change-attribution.ts to design a per-domain label resolver.
- 2026-09-09T20:40:00Z ✅ DONE — added `DriftLabelHint` + `labelHint` on `DriftCheckSpec` (ca-policy: displayName, public-teams-discoverable: displayName, eeeu-site-sharing: url), plus `driftDisplayNamesFromBaselineConfig` / `resolveDriftEventLabel` / `driftSpecForDomain` in drift-check-specs.ts. Display names are recovered from the domain's own captured baseline config (`drift_baseline_snapshots.config`) because a nested-property replace event only carries the changed leaf value, not the full object — `drift_events.old_value`/`new_value` alone are not enough for the common case. Wired both `dashboard-resolvers.ts` (customer timeline) and `admin-drift.ts` (operator view, batched per distinct tenantId+domainKey pair) through the same resolver. Fell back to the original `<setting> <verb>` form when no name is recoverable, per the issue's own spec. Added 8 new test cases (39 total in drift-check-specs.test.ts); full drift-related suite (drift-check-specs, drift-change-attribution, drift-collector) = 97/97 passing; `tsc --noEmit` clean. Fixed a real encoding bug my own edit introduced along the way (NUL bytes in place of spaces inside a template literal in admin-drift.ts — caught by `git diff` reporting the file as binary; repaired with a byte-exact substitution before commit). Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
