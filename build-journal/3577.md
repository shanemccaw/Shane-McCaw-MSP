# #3577 — Home dashboard burndown/ETA charts: database-only, real completeness check, honest empty state

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — investigated `HomeView.xaml.cs` (RenderBurndown/DrawBurndownCanvas),
  `GitHubIssueTimeSeriesService`, `HomeEtaProjectionService`, `GitHubIssueMirror`, and the real
  `bt_issue_mirror`/`bt_milestone_mirror` schema+data via direct psql against BuildConsole's own DB.
  Confirmed live: milestone #5 mirror has 663 issues tagged vs `bt_milestone_mirror`'s real
  357+1708=2065 aggregate — genuinely incomplete right now. Implementing: (1) a real per-milestone
  completeness check (bt_issue_mirror scoped count vs bt_milestone_mirror aggregate total), (2) a real
  per-epic completeness check (no bt_milestone_mirror equivalent exists for epics, so walking the
  transitive subtree comparing each node's real mirrored direct-child count against that node's own
  real GitHub `sub_issue_count` — genuinely authoritative per-node, not a re-derivation of the same
  data), (3) converting `GetMilestoneSeriesAsync`/`GetEpicSeriesAsync`/`GetActiveMilestoneSeriesAsync`/
  `GetOpenEpicsAsync`/`GetOpenEpicsInMilestoneAsync`/`ResolveActiveMilestoneAsync` (used exclusively by
  the Home dashboard burndown/rate/ETA cards — confirmed via grep, no other caller) to read ONLY
  `bt_issue_mirror`/`bt_milestone_mirror`, never falling back live, gated by the completeness check.
