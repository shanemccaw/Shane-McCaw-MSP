# #3577 — Home dashboard burndown/ETA charts: database-only, real completeness check, honest empty state

- **Status:** ✅ DONE 2026-09-11T21:19:01Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** c3e0af30a

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — investigated `HomeView.xaml.cs` (RenderBurndown/DrawBurndownCanvas),
  `GitHubIssueTimeSeriesService`, `HomeEtaProjectionService`, `GitHubIssueMirror`, and the real
  `bt_issue_mirror`/`bt_milestone_mirror` schema+data via direct psql against BuildConsole's own DB.
  Confirmed live: milestone #5 mirror has 663 issues tagged vs `bt_milestone_mirror`'s real
  357+1708=2065 aggregate — genuinely incomplete right now. Implementing: (1) a real per-milestone
  completeness check (bt_issue_mirror scoped count vs bt_milestone_mirror aggregate total), (2) a real
  per-epic completeness check (no bt_milestone_mirror equivalent exists for epics, so walking the
  transitive subtree comparing each node's real mirrored direct-child count against that node's own
  real GitHub `sub_issue_count` — genuinely authoritative per-node, not a re-derivation of the same
  data), (3) converting `GetMilestoneSeriesAsync`/`GetEpicSeriesAsync`/`GetActiveMilestoneSeriesAsync`/
  `GetOpenEpicsAsync`/`GetOpenEpicsInMilestoneAsync`/`ResolveActiveMilestoneAsync` (used exclusively by
  the Home dashboard burndown/rate/ETA cards — confirmed via grep, no other caller) to read ONLY
  `bt_issue_mirror`/`bt_milestone_mirror`, never falling back live, gated by the completeness check.
- 2026-09-11T21:19:01Z ✅ DONE — shipped in commit c3e0af30a (pushed to `main`). Implemented and verified
  exactly the plan above:
  - `CheckMilestoneCompletenessAsync` (mirror's scoped total vs `bt_milestone_mirror` aggregate) and
    `CheckEpicCompleteness` (per-node real `SubIssueCount` vs mirrored direct-child count, walked
    transitively) both added to `GitHubIssueTimeSeriesService.cs`.
  - `GetMilestoneSeriesAsync`/`GetEpicSeriesAsync`/`GetActiveMilestoneSeriesAsync`/`GetOpenEpicsAsync`/
    `GetOpenEpicsInMilestoneAsync`/`ResolveActiveMilestoneAsync` rewritten to read only
    `GitHubIssueMirror.TryGetBoardIssuesAsync`/`TryGetMilestoneInfosAsync` — their prior live-GitHub
    fallback removed entirely (each confirmed via repo-wide grep to have no caller outside the Home
    dashboard, so this can't regress Focus Mode / the Git Board tree / editor-panes stats, which still
    use the untouched general-purpose `GetAllIssuesAsync`).
  - `HomeEtaProjectionService.ComputeAsync` and `HomeView.xaml.cs`'s three chart refresh paths (burndown,
    open/close-rate, per-Epic burndown) updated to the new signatures; stale doc comments referencing the
    old 5-minute GitHub cache corrected.
  - **Real verification (live psql against BuildConsole's own Postgres DB, not simulated):**
    milestone #5 → 663 mirrored vs 2065 real (357+1708) → correctly flagged incomplete;
    milestone #17 → 32 mirrored vs 32 real → correctly flagged complete, 13 real distinct activity days
    across a 39-day span (clears `BuildSeries`'s own `MinSpanDays`/`MinActivityDays` gates) → a real chart
    would render; epic #1292 → 4 real direct children all mirrored, both grandchild levels also fully
    mirrored → correctly flagged complete (its own real history is too thin — one single creation day —
    so `BuildSeries`'s pre-existing activity-day gate correctly still shows the honest empty state, which
    is expected, separate, correct behavior); epic #1202 → 100 real direct children, only 52 mirrored →
    correctly flagged incomplete at the root node.
  - `dotnet build BuildConsole.csproj -c Debug` — 0 errors, only pre-existing unrelated warnings.
  - **Follow-up filed per the issue's own point #4:** #3704 — `bt_issue_mirror`'s closed-issue backfill is
    confirmed far from complete for the milestones/epics actually in use (milestone #5, epic #1202, and
    all of milestones 4/6/11/12/13/14/16/18 have zero backfilled rows), so most/all current milestones
    will show the honest empty state until that backfill lands. Attempted to parent #3704 as a sub-issue
    of #1202 per the standing dispatch rule; GitHub rejected it — #1202 is already at its hard 100-sub-issue
    cap — so #3704 is left unparented at the GitHub level with a comment explaining why, and its board
    status set to AI Batter Up.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed `agent/3577-q2365` (c3e0af30a) is
    an ancestor of `origin/main`.
