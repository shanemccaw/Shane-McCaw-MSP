# #2137 — Split BuildQueueDesignWindow into a genuinely separate .csproj

- **Status:** ✅ DONE 2026-09-01T15:01:01Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 865d2aa4d (work), 87e21e911 (IN FLIGHT bookend)

## Log
- 2026-09-01T14:55:16Z ⏳ IN FLIGHT — Extracting the #2135 `BuildQueueDesignWindow` (currently part of `BuildConsole.csproj`, so recompiling it rebuilds the SAME exe Shane's live queue-managing instance runs) into a genuinely separate WPF project — its own `.csproj`, own `.exe`, own build output dir. Audit done: the window uses only (a) `BuildConsole.Services` types (`BuildQueuePostgresClient`, `QueueItem`, `AccountCapPolicy`, `SessionLimitAutoRestartService`, `ActivityLog`) and (b) the #2126 token dicts `Themes/Colors.xaml` + `Themes/Typography.xaml` (both self-contained, StaticResource-internal only). Plan: new `desktop/BuildQueueDesignPreview/` project with a ProjectReference to `BuildConsole.csproj` (share data layer + tokens, don't duplicate), git-move the two window files in, add an `App.xaml`/`App.xaml.cs` that merges the two token dicts from the referenced assembly (pack URI) and constructs its own `BuildQueuePostgresClient.TryCreate(...)` exactly as MainWindow does. NOT wired as any app's default startup. BuildConsole's ActivityBar 🎨 toggle re-pointed to launch the standalone exe (the type leaves BuildConsole).
- 2026-09-01T15:01:01Z ✅ DONE — Shipped in `865d2aa4d`. New `desktop/BuildQueueDesignPreview/` WPF project (WinExe, net8.0-windows, UseWPF) with its OWN `BuildQueueDesignPreview.exe` in its OWN `bin/` — rebuilding it can never touch the live `BuildConsole.exe` (separate output dirs, confirmed: both exes emitted to their own `bin/Debug/net8.0-windows/`). Shares, doesn't duplicate: a `ProjectReference` to `BuildConsole.csproj` gives the data layer (`BuildQueuePostgresClient`/`QueueItem`/`BuildTrackerConfig`/status-policy types), and `App.xaml` merges the #2126 `Themes/Colors.xaml` + `Typography.xaml` token dicts from that assembly via pack URI — the exact two dicts #2135 bound by DynamicResource. `app.ico` linked (not copied). `git mv`'d `BuildQueueDesignWindow.xaml`/`.xaml.cs` into the new project unchanged; added `App.xaml`/`App.xaml.cs` (own entry point, builds its own live client via `TryCreate` against config/`.env.local` DATABASE_URL — real queue, degrades to the window's existing "Not connected" state if no DB) and a local `.gitignore` (bin/obj). BuildConsole's ActivityBar 🎨 toggle re-pointed from an in-process window to launching the standalone exe as its own process (located side-by-side with BuildConsole.exe or under the repo build output, newest wins; toasts if not yet built). NOT wired as any default startup — deliberate separate-exe-for-now per issue point 5. **Verification:** `dotnet build` of BOTH projects → Build succeeded, 0 warnings / 0 errors; standalone exe launched (Start-Process) and stayed running past `OnStartup` (DB init + `window.Show`) for 5s then closed cleanly — proves the cross-assembly pack URIs and DB client construction resolve at runtime, not just compile. WPF app → no test manifest (out of scope per CLAUDE.md). Working tree clean; branch pushed to origin/main. No findings filed (clean extraction, no new problems surfaced).
