# #3389 — Live 'Customer Dashboard Category Tabs' sales bundle stores unprefixed package keys

- **Status:** ✅ DONE 2026-09-10T21:26:15Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** ef0ade410 (fix, on `agent/3389-q2204`) → rebased/pushed to `main` as `78bba917c`

## Log
- 2026-09-10T21:23:25Z ⏳ IN FLIGHT — confirming live DB state, then correcting the one live
  `msp_sales_bundles.monitoring_package_keys` row (and matching `dashboard_templates.target_key`
  rows) to add the `cat-` prefix so they match real `monitoring_packages.key` rows, per #3389's
  own suggested fix direction.
- 2026-09-10T21:26:15Z ✅ DONE — Confirmed live state against local PostgreSQL matched the issue's
  claim exactly: `msp_sales_bundles` bundle `2d9dc63f-1c05-4e21-a7af-32a7defedc44`
  (`msp_id 1`, "Customer Dashboard Category Tabs") had unprefixed
  `monitoring_package_keys`, and 10 `dashboard_templates` rows
  (`template_type='monitoring_package'`) had matching unprefixed `target_key` values —
  both matching zero real `monitoring_packages.key` rows (all 21 live rows are
  `cat-`/`assess:`/`core:`/`detail:`-prefixed).

  Wrote and ran (myself, against local `DATABASE_URL`, additive/corrective UPDATE only,
  no DROP/TRUNCATE) `lib/db/migrations/manual/2026-09-10-fix-dashboard-category-tab-keys-prefix.sql`:
  - `UPDATE dashboard_templates SET target_key = 'cat-' || target_key WHERE
    template_type='monitoring_package' AND target_key IN (...)` → 10 rows updated.
  - `UPDATE msp_sales_bundles SET monitoring_package_keys = <jsonb_agg re-prefixed>
    WHERE bundle_id = '2d9dc63f-1c05-4e21-a7af-32a7defedc44'` → 1 row updated.
  - Self-marking `INSERT INTO simulator_migration_runs` at the end.

  **Verified**: re-queried live DB — the bundle's `monitoring_package_keys` and all 10
  `dashboard_templates.target_key` rows now read `cat-executive`, `cat-identity-access`, etc.,
  and a direct join (`msp_sales_bundles` keys ∩ `monitoring_packages.key`) returns 10/10
  matches (was 0/10 before the fix). This restores the bundle's package-enrichment join in
  `msp-sales-bundles.ts` (`GET /:bundleId`) and the `dashboard-overrides.ts` resolved-list
  gating this migration exists to power — both were dead against 0 real rows before this fix.

  Committed as `ef0ade410` on `agent/3389-q2204`, rebased onto `origin/main` (which moved
  twice concurrently during this session) and pushed directly to `main` as `78bba917c`.
  `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean (only this
  bookend + the migration file, both mine, already committed).

  No product code changed — this was a pure data-correction fix, exactly as scoped by
  #3389's own body and comment. No new finding to file; #3389 itself already fully
  describes and now resolves the gap.
