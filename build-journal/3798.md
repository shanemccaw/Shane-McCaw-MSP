# Build journal — Issue #3798

- **Status:** ✅ DONE 2026-09-13T00:30:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T00:00:00Z — Session started. Follow-on from #2006 (b550d82b2). Adding: (1) signed-out primary account check at launch in QueueWatcherService.cs, (2) Settings-time validation for SecondaryClaudeConfigDir in SettingsTabView.
- 2026-09-13T00:30:00Z — ✅ DONE — commit `1ce0d8cca` (pushed directly to `origin/main`, rebased off `origin/main` first). Both gaps from #3798's body closed:
  1. `QueueWatcherService.LaunchItem` (`desktop/BuildConsole/Services/QueueWatcherService.cs`) now validates `~/.claude` via `ClaudeUsageMeterService.ReadAccessToken` before launching a primary/default-account build — same fail-closed pattern #2006 already applies to secondary. A signed-out primary now fails the launch loudly via `MarkLaunchFailedAsync` with a clear reason instead of the session dying on first token.
  2. `SettingsTabView` (`desktop/BuildConsole/Controls/SettingsTabView.xaml.cs`) now shows a live signed-in / not-signed-in / config-dir-not-found status for `SecondaryClaudeConfigDir`, both on load and after Save, reusing the identical `Directory.Exists` + `ReadAccessToken` checks as the launch-time gate.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `node scripts/dev-server/verify-branch-merged.mjs` confirmed `agent/3798-q2639` is an ancestor of `origin/main` after push. `git status --porcelain` clean.
  - No fixture/hardcoded data involved — this is pure BuildConsole (desktop WPF) internal tooling, out of scope for test manifests per CLAUDE.md.
