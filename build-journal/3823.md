# #3823 — Auto-delete a build's remote agent/* branch on genuinely verified completion

- **Status:** ✅ DONE 2026-09-12T20:18:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** c6c928efe

## Log
- 2026-09-12T19:59:00Z ⏳ IN FLIGHT — investigating WorktreeCleanupService.SweepWorktreesAsync, cleanup-worktree.mjs, DoneBookendVerifier to wire remote-branch deletion gated on issue-closed or verified-DONE-bookend safety check.
- 2026-09-12T20:18:00Z ✅ DONE — c6c928efe. `WorktreeCleanupService.SweepWorktreesAsync` (the same
  event-driven call sites, no new timer) now parses cleanup-worktree.mjs's `--sweep --json`
  `removed[].branchDeleted` output and, for each real local `agent/*` branch it just deleted,
  calls `DELETE /git/refs/heads/<branch>` (new `GitHubApiClient.DeleteBranchRefAsync`) — but only
  when the safety check passes: (a) the branch's numeric issue id resolves to a GitHub issue
  closed with `state_reason` `completed`/`not_planned` (added `GitHubIssueDetail.StateReason`), or
  (b) `DoneBookendVerifier.IsSatisfiedAsync` confirms a real, git-verified DONE bookend. Fails
  closed on no PAT, any API error, or a non-numeric (--notGit) branch id — remote branch left
  alone. `MarkWorktreeStaleAsync` (failed-build path) is untouched; it never deletes a local
  branch, so it never reaches this new code — verified by reading the call sites (only the
  `exitCode == 0` success path and the sweep's own `removeWorktreeSafe` set `branchDeleted`).
  Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors (10
  pre-existing warnings unrelated to this change). No live GitHub delete was exercised in this
  session (no agent/* branch reached the sweep during the build) — the safety-check logic itself
  was verified by code reading against `GitHubIssueVerifier`/`DoneBookendVerifier`'s existing,
  already-proven patterns, not a live end-to-end run; a real confirmation happens the first time
  this sweep next fires against a genuinely closed/DONE agent/* branch, logged to the
  `worktree.cleanup` ActivityLog channel. `git status --porcelain` clean. Branch verified merged
  into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`. No new findings filed —
  no genuine bug/gap surfaced during this build beyond the scoped ask.
