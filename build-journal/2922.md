# #2922 — Customer Home/Overview: real cross-Feature roll-up counts on GET /portal/dashboard

- **Status:** ✅ DONE 2026-09-05T00:53:20Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 7c4edc346 (main)

## Log
- 2026-09-05T00:23:13Z ⏳ IN FLIGHT — investigating existing GET /api/portal/dashboard (portal-customer-engines.ts) and the 5 source Features (risk-register, message-center, change-control maintenance-windows, remediation-tracker, policy-decisions reviewDueAt) to add real roll-up counts.
- 2026-09-05T00:53:20Z ✅ DONE — added a real `overviewCounts` object to GET
  /api/portal/dashboard: `rbdWaiting`/`rbdActive` (msp_risk_decisions.status),
  `microsoftChangesThisWeek` (msp_message_center_items, rolling 7-day
  effectiveDate() window), `changeScheduleThisWeek` (change_maintenance_windows,
  gated on the change_control add-on entitlement; new
  `windowOverlapsRange()` helper in portal-change-maintenance.ts walks a
  recurring window's own cadence so a standing weekly rule still counts),
  `remediationInProgress` (remediation_tracker_steps, terminalState
  "outstanding"), `policiesExpiringSoon` (policy_decisions.reviewState in
  "due"/"overdue", #2518's review clock). All six are 0 when the account's
  tenant row has no resolvable M365 identifier — honest, not an error.
  Extracted `remediationTerminalState()` into its own zero-dependency
  `lib/remediation-tracker-terminal-state.ts` rather than importing the whole
  `portal-remediation-tracker.ts` route file — that file transitively pulls in
  `workflow-executor.ts` -> `sales-offer-engine.ts` -> `priority-engine.ts`,
  which references drizzle-orm's `sql` at module scope and broke 3 unrelated
  tests that mock drizzle-orm/@workspace/db minimally
  (`portal-customer-engines-assessment-redaction.test.ts`,
  `portal-dashboard-route-collision.test.ts`, `portal-customer-offboard.test.ts`)
  — confirmed via a full-suite run before and after, fixed by the extraction,
  re-confirmed green. Updated those two dashboard test files' FIFO mock queues
  for the new `resolveTenantScope()` call that now runs before the route's try
  block. Added `windowOverlapsRange()` unit tests (5 new cases) to
  `portal-change-maintenance.test.ts` — also found and fixed that this
  pre-existing test file was never wired into `vitest.config.ts`'s include
  allowlist at all (fixed in this build's own commit, not filed separately,
  since it's directly needed to verify this build's own change).
  Regenerated `docs/customer-home-and-timeline-contract-pack.md`'s dashboard
  section (§2) for the new field, plus a stale-doc fix: the pack still
  described `results.summary.priorityItems` as a hardcoded `[]`, but #2500
  (closed) gave it a real query some time after the pack was first written.
  **Verification:** full `api-server` vitest suite green — 309/311 test files
  (5035 tests, 5025 passed, 9 skipped); the 2 remaining failures
  (`tenant-billing-state.live-db.test.ts` — a live-Postgres `ON CONFLICT`
  constraint error, `portal-messages.test.ts` — an unrelated email-template
  assertion) are pre-existing and unrelated to this change (confirmed by their
  error text having nothing to do with this build's files). `tsc --noEmit`
  clean. Rebased onto current `origin/main` and pushed directly —
  `verify-branch-merged.mjs` confirms `agent/2922-q1688` (7c4edc346) is merged.
  `git status --porcelain` clean. No findings filed — no genuine gap/bug
  discovered outside what was fixed in this same build.
