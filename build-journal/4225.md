# #4225 — Build Map canvas raw hex palette has no matching brush

- **Status:** ✅ DONE 2026-09-15T05:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 49b8154ec (IN FLIGHT bookend), 20014b7ac (real work — brushes + wiring)

## Log
- 2026-09-15T04:59:32Z ⏳ IN FLIGHT — pulled #4225's full body (no comments beyond the original BUILD: dispatch). Confirmed via grep that the 12 raw hex values (`#0a0d12`, `#0f1319`, `#0d1117`, `#141a22`, `#21262d`, `#161c25`, `#c9d1d9`, `#8b949e`, `#576069`, `#6a8fb5`, `#1d3450`, `#e6edf3`) used across LocalMapWindow.xaml, BuildChainMapWindow.xaml, Controls/BuildMap/LocalMapCanvasControl.xaml and Controls/BuildMap/ChainCanvasControl.xaml have no matching brush in Themes/Colors.xaml or Themes/DarkTheme.xaml. Choosing option (a) from the issue: add a dedicated named "BuildMap" brush set to Colors.xaml (this GitHub-dark hue is deliberately its own design language per BuildChainMapWindow.xaml's own header comment, not force-fit onto the existing Catppuccin-derived chrome brushes) and wire all four files to it.
- 2026-09-15T05:05:00Z ✅ DONE — Added 12 Color/SolidColorBrush pairs (`BuildMap*Color`/`BuildMap*Brush`) to Themes/Colors.xaml, which is merged app-wide via App.xaml so the new keys resolve as `{StaticResource ...}` from any XAML view without touching DarkTheme.xaml. Replaced all matching raw hex literals in LocalMapWindow.xaml (24), BuildChainMapWindow.xaml (25), Controls/BuildMap/LocalMapCanvasControl.xaml (2) and Controls/BuildMap/ChainCanvasControl.xaml (2) with the new brush references — counts match the issue's own evidence table for the two files it counted. Left untouched: hex values inside `<!-- -->` design-doc comments, and BuildChainMapWindow.xaml's unrelated amber Re-wire-pill colors (`#3a2a1c`/`#5a3f2a`/`#f0c9a6`), which aren't in #4225's 12-value palette. Verification: `dotnet build -c Debug` on desktop/BuildConsole.csproj fails with a pre-existing, unrelated XML error in `Controls/GitModeContainer.xaml` (MC3000, mismatched Grid/StackPanel tags) — confirmed by temporarily stashing just my 5 files and re-running the same build, which fails identically on unmodified `main`. That break is already tracked (issues #4231–#4235, filed independently the same day); not a new finding, not caused by this change. In place of the full build, validated well-formedness of all 5 edited files directly via PowerShell's `[xml]` parser — all 5 parse clean. Working tree clean, branch verified merged into origin/main via `scripts/dev-server/verify-branch-merged.mjs` (exit 0) after push.
