# #2988 — Git panel status call missing --untracked-files=all

- **Status:** ✅ DONE 2026-09-06T15:19:12Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** e24de931c (main)

## Log
- 2026-09-06T15:16:14Z ⏳ IN FLIGHT — confirmed real root cause: `LeftSidebar.xaml.cs` ~line 7450 calls
  `git status --porcelain -b` with no `--untracked-files=all`, so an entirely-untracked
  directory (Shane's real Design pack) collapses into one line for the directory itself
  instead of one line per real file. Fixing by adding `-uall` there, and auditing/fixing
  the two other real callers building on the same porcelain semantics
  (`StartupConnectivityService.cs`, `GitDoctorService.cs`) so their untracked counts are
  also accurate on an all-untracked folder.
- 2026-09-06T15:19:12Z ✅ DONE — added `--untracked-files=all` to the real git status call
  at `LeftSidebar.xaml.cs` ~7450, plus the two other real callers reading the same
  porcelain output (`StartupConnectivityService.cs`'s startup git-status check,
  `GitDoctorService.cs`'s dirty-worktree diagnostic). Audited `LeftSidebar`'s existing
  per-line parse loop — it already handles individual `??` lines correctly (maps to
  unstaged status `U`), so no parsing change was needed, only the missing flag.
  Real verification: reproduced the exact collapse in a synthetic all-untracked 3-file
  folder (`git status --porcelain -b` → `?? design-pack/`; same command +
  `--untracked-files=all` → 3 individually-named `?? design-pack/fileN.dc.html` lines),
  matching Shane's real 66-file report. `dotnet build BuildConsole.csproj -c Debug`
  succeeded, 0 warnings/0 errors. Commit e24de931c pushed straight to `main` (rebased
  clean, no conflicts) and confirmed merged via `verify-branch-merged.mjs`.
  `git status --porcelain` clean at session end. No other real callers of
  `git status --porcelain` found in the C# codebase.
