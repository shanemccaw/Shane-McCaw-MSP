# #1582 — My Architect: monthly drawdown incl. over-month state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** cde68a71b (main)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Investigated. The domain math (`retainer-hours.ts`
  `computeMonthBucket`) and wire mapper (`admin-retainer.ts` `bucketToWire`, shared by
  `routes/portal-retainer.ts`) already exist and are correct/tested for the rollover
  arithmetic (Git #1285/#1293) — `retainerModel.ts`/`useRetainerLive.ts` (#1588/#1590's
  targets) are moot, deleted wholesale in the portal-v2 retirement. The real gap: there
  is no explicit, honest "over-month" wire signal. The live AdminV2 consumer
  (`RetainerBody.tsx`) infers it as `remainingHours <= 0`, which wrongly flags a
  customer who used EXACTLY their allotment (a normal, non-remarkable state) as
  "over" — conflating "ran out exactly" with "delivered more than retained," the
  opposite of what this issue asks for ("over-month is a normal state, not an error
  state," shown honestly rather than inferred/capped). Adding an explicit
  `overMinutes`/`overHours`/`isOverMonth` field to the domain model + wire contract so
  both the admin console and the eventual customer page can render "10h retained ·
  12h delivered" without recomputing/misinferring the state client-side. No schema
  change — pure computation + wire extension. DB confirmed still 0/0/0 rows
  (`retainer_settings`/`retainer_work_log`/`status_reports`) via direct psql, so no
  live data is affected. This is the last blocker `#1581` (contract pack) is waiting
  on for this module's read surface.
- 2026-08-31 ✅ DONE — Shipped `cde68a71b` on `main`. `retainer-hours.ts`
  `MonthBucket` gained `overMinutes` (used − (retained+rolled), floored at 0 —
  honest, uncapped) at both `computeMonthBucket` return sites; `admin-retainer.ts`
  `bucketToWire` (shared by `routes/portal-retainer.ts`) exposes `overHours` /
  `isOverMonth`. Fixed the real boundary bug this uncovered in AdminV2's
  `RetainerBody.tsx`: it was flagging a customer who used exactly their allotment
  (remainingHours === 0, not over) as "over" (danger color) — now uses the
  server's explicit `isOverMonth`. Added an "Over" stat (only rendered when
  `isOverMonth`) and relabeled "Used" to "Delivered" to match this issue's own
  design vocabulary. Extended `retainerStore.ts`'s `RetainerBucket` type.
  Verification: `vitest run src/lib/retainer-hours.test.ts
  src/routes/portal-retainer.test.ts` → 21/21 passed (5 new/updated tests,
  including the exactly-at-allotment boundary case and rolled-hours-count-toward
  the threshold case). `tsc --noEmit` clean on both `api-server` and
  `admin-panel` (no `retainer`-related errors). `verify-branch-merged.mjs` → OK,
  merged into `origin/main` at `cde68a71b`. DB re-confirmed 0/0/0 rows via direct
  psql (`retainer_settings`/`retainer_work_log`/`status_reports`) — no live data
  touched, no migration needed (pure computation/wire change). Working tree
  clean. No new findings filed — the boundary bug found and fixed was scoped
  entirely inside this issue's own surface.
