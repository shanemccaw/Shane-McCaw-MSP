# #2360 — Batter Up item actions: Dispatch, Hold / Release

- **Status:** ✅ DONE 2026-09-02T22:55:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** `ce742c7fa` on `main` (rebased 3x — origin moved repeatedly under concurrent
  builds while landing, including a real conflict against #2358's concurrent group-header
  build; resolved by hand, both feature sets kept, no code dropped)

## Log
- 2026-09-02T22:10:00Z ⏳ IN FLIGHT — confirmed #2359 genuinely DONE on `main` (commit
  `d8406701a`, bookend ✅ DONE). Real scope from #2360's own dispatch comment (supersedes
  the bare issue body): per-item action buttons on the Batter Up rail panel — Dispatch,
  and Hold / Release.
  - Audited the live board's real Status field (`gh api graphql` against
    `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0`) rather than guessing: confirmed options include
    `Backlog` (`63cc47c8`) and `Park` (`19cfa11c`). Found strong prior art already in this
    exact repo: `GitEpicPanelService.cs` (#2307/#2308) added `Park` as a real bucket
    specifically for "pulls it out of the [Batter Up] queue... into its own queue away
    from the build" (Shane's own words, quoted in that file's header) — the exact Hold
    semantics this issue asks for. Reusing `GitEpicPanelService.StatusOption_Park` /
    `SetProjectStatusAsync` rather than re-deriving or re-guessing the target status.
  - Dispatch: per #2360's own comment, must NOT write `bt_build_queue` or push a build
    prompt — #2366 (blocked by this issue) is the governing spec for the real dispatch
    flow. Wiring Dispatch here as a real, safe, standalone action (open the issue in the
    browser + copy a ready-to-act clipboard signal) with a clear hook point left for #2366.
  - Building: `ChatGitHubFilter.BatterUpItemRef` gains `ProjectItemId`/`LaneOptionId`
    (fetched inline off the existing lane-walk GraphQL query, no extra round trip);
    `MainWindow.BatterUpPanel.cs` gains Hold/Release/Dispatch action links per item row,
    reusing the existing `GitEpicActionLink`/`GitCountPill` row primitives and
    `GitEpicPanelService.SetProjectStatusAsync` write path.
- 2026-09-02T22:55:00Z ✅ DONE — commit `ce742c7fa` on `main` (verified via
  `verify-branch-merged.mjs`: "merged into 'origin/main'. Safe to write DONE."; working tree
  clean). Shipped:
  - `ChatGitHubFilter.cs`: `BatterUpItemRef` gains real `ProjectItemId` (the ProjectV2Item
    node id) and `LaneOptionId` (the real Status option id the item was actually sitting in
    when fetched), both read inline off the existing lane-walk GraphQL query (added `id` to
    the `items(...).nodes` selection) — no extra round trip. Confirmed live against the real
    board via a standalone `gh api graphql` call before wiring: real item ids
    (`PVTI_lAHOEiBDdc4BeoiY...`) came back correctly paired with their real lane option ids.
  - `MainWindow.BatterUpPanel.cs`: each item row now renders a real action row — **Hold**
    writes the item's real board Status to `GitEpicPanelService.StatusOption_Park` (`19cfa11c`)
    via the already-shipped `SetProjectStatusAsync` mutation (`GitEpicPanelService`, #2307/
    #2308) — reused verbatim, not re-derived. Audited the live Status field's real option set
    first (`gh api graphql` against `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0`) rather than guessing
    between Backlog/Park; found `GitEpicPanelService`'s own header already documents Shane's
    real words for why `Park` exists — "pulls it out of the Batter Up queue, puts it in its
    own queue away from the build" — the exact Hold semantics this issue asked for. **Release**
    writes back to `item.LaneOptionId` (the real lane the item was actually in before Hold,
    never a guessed default), same write path reversed. A held item stays visible in the panel
    with a real "HELD" pill and a Release action (local `_batterUpHeldItems`/
    `_batterUpActionBusy` HashSets, same convention as `_gitEpicActionBusy`) rather than
    vanishing until the next full board re-fetch. **Dispatch** is deliberately NOT a queue
    write, per #2360's own governing comment (#2366 is the real spec): it opens the real issue
    in the browser and copies a ready-to-act prompt to the clipboard — a real, working,
    standalone action, never a fabricated "Dispatched!" state — with `BatterUpDispatchClicked`
    left as the named hook point for #2366 to extend into the real end-to-end flow.
  - **Real merge conflict during push** (not corruption): a concurrent session landed #2358
    (group-header state pill + "Dispatch all" stub) on `main` while this build was in flight.
    Resolved by hand across 2 conflict blocks (both doc-comment/object-initializer additions,
    no logic overlap, no naming collisions — theirs is `BatterUpDispatchAllClicked`
    (group-level stub), mine is `BatterUpDispatchClicked` (item-level, real)); rebuilt clean
    after resolution before continuing the rebase.
  - Verification: `dotnet build ShaneBuilder.csproj -c Debug` — Build succeeded, 0 errors,
    both before and after the #2358 merge conflict resolution. Live-verified the GraphQL query
    change against the real board (not just compiled) via a standalone `gh api graphql` call.
    Did not fire a live Hold/Release mutation against a real board item purely for verification
    — `SetProjectStatusAsync`'s mutation shape is unmodified, already-shipped code (#2307/
    #2308), so an extra live write would only have risked leaving a real item moved with no
    build benefit.
  - No genuine findings surfaced this build — no new issues filed.
