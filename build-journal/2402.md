# Git #2402 — Select findings and send them to Claude; findings update from the conversation

- **Status:** ⏳ IN FLIGHT 2026-09-03T02:24:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-03T02:24:00Z — Started. Worktree `C:\wt\2402-q1235` had to be recovered — it existed
  as a completely empty, unregistered directory (no `.git`), same shape as Git #2118. Recreated
  via `git worktree add` off `origin/main` + manual `registerWorktree`/`linkDeps` per the
  [[provision-worktree-owner-pid-sweep-hazard]] recovery recipe, no work had been done yet so
  nothing was lost.
- Read #2402 body + comment, #2400 (Feature parent), #2401 (sibling, already DONE — added
  `RepoHealthRule.NoFeatureParent`), and `desktop/ShaneBuilder/ShaneBuilder-FullFeature.md`
  cross-cutting Repo Health item 6: "Select findings and send them to Claude; findings update
  from the conversation."
- Audited `RepoHealthService.cs` + `MainWindow.xaml.cs`: the "select findings and send to Claude"
  half is already fully built (checkbox select w/ 5-cap, "pick next 5", `Send N to this chat`,
  markdown work order into composer, sent findings dim to "In chat"). The second half —
  **findings update from the conversation** — is the real gap: once sent, a finding sits
  permanently dimmed as "In chat" with no mechanism to detect the chat actually resolved it and
  clear/refresh it. Building that.
