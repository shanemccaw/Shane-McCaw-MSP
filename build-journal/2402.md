# Git #2402 — Select findings and send them to Claude; findings update from the conversation

- **Status:** ✅ DONE 2026-09-03T02:55:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-03T02:24:00Z — Started. Worktree `C:\wt\2402-q1235` had to be recovered — it existed
  as a completely empty, unregistered directory (no `.git`), same shape as Git #2118. Recreated
  via `git worktree add` off `origin/main` + manual `registerWorktree`/`linkDeps` per the
  [[provision-worktree-owner-pid-sweep-hazard]] recovery recipe, no work had been done yet so
  nothing was lost.
- Read #2402 body + comment, #2400 (Feature parent), #2401 (sibling, already DONE — added
  `RepoHealthRule.NoFeatureParent`), and `desktop/ShaneBuilder/ShaneBuilder-FullFeature.md`
  cross-cutting Repo Health item 6: "Select findings and send them to Claude; findings update
  from the conversation."
- Audited `RepoHealthService.cs` + `MainWindow.xaml.cs`: the "select findings and send to Claude"
  half is already fully built (checkbox select w/ 5-cap, "pick next 5", `Send N to this chat`,
  markdown work order into composer, sent findings dim to "In chat"). The second half —
  **findings update from the conversation** — is the real gap: once sent, a finding sits
  permanently dimmed as "In chat" with no mechanism to detect the chat actually resolved it and
  clear/refresh it. Building that.
- ✅ DONE 2026-09-03T02:55:00Z — Implemented the "findings update from the conversation" half in
  `desktop/ShaneBuilder/MainWindow.xaml.cs`:
  - `RunRepoHealthScanAsync(bool auto = false)` now diffs the freshly-scanned finding Ids against
    the previously-sent set on every rescan. A sent finding whose Id no longer appears in a
    *GitHub-reachable* scan means the real repo state changed (retitled/re-parented/closed) — it
    is pruned from `_rhSent`/`_rhSelected` so it stops rendering as a permanently-dimmed "In chat"
    phantom. An unreachable scan prunes nothing (never claim something was fixed off a failed read).
  - Subscribed to the existing `ClaudeActivityService.Changed` event (the same busy/free signal
    Test Pad's status band already reads) in `InitializeTestPad()`. On the working→free transition
    (a reply just landed), `OnClaudeActivityChangedForRepoHealth()` fires an automatic rescan
    *only* if the Repo Health panel has already been opened once (`_rhLoaded`) and there's at
    least one sent finding to recheck — no polling of its own, no rescans on tabs/chats that never
    touched Repo Health.
  - The auto-rescan path posts a toast ("N sent finding(s) resolved based on the conversation —
    cleared from the list.") only when something genuinely resolved, so it stays silent noise-free
    on every other reply.
  - "Select findings and send them to Claude" itself (checkbox select w/ 5-cap, "pick next 5",
    "Send N to this chat" → composer markdown work order, sent → "In chat") was already fully
    built pre-existing code — nothing to redo there.
  - No new findings discovered this session — the existing Repo Health rule set audited clean on
    the same live scan used to verify.
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors.
  - Commits (real hashes on `main` after rebase): bookend IN FLIGHT + feature commit, tip
    **af0660e5e**. Pushed straight to `main`; `verify-branch-merged.mjs` confirms
    `agent/2402-q1235` is an ancestor of `origin/main` at `af0660e5e`. Working tree clean.
