# #1576 — Projects: Two overlapping catalogs — which is authoritative

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** a61b0ba53 (IN FLIGHT bookend); DONE commit hash below

## Log
- 2026-08-28 ⏳ IN FLIGHT — Read-only investigation. Query local PostgreSQL directly
  (not shaneapp://executeSql) for full `services` row dumps (IDs 34-60, 158-163,
  164-167), FK referential reality across client_services/projects/quotes/orders,
  creation chronology, pricing-column audit, six-pillar mapping structure, and the
  full FK graph into `services`. No writes. Findings to be posted as a comment on
  #1576, not used to make the authoritative-catalog decision (Shane's call).
- 2026-08-28 ✅ DONE — Ran all queries directly against local Postgres
  (`shanemccawmsp`). Findings posted as a comment on #1576:
  https://github.com/shanemccaw/Shane-McCaw-MSP/issues/1576#issuecomment-5459307079
  Headline results: 37 rows exist across the three ranges (not 36); Group A
  (34-60) is referenced by zero rows in every FK-constrained table and only by
  46 *disabled* `sales_offer_rule_groups` rows; Group B (158-163) has 16 draft
  `sales_offers` + 12 *active* rule-group rows + one real `status='paid'`
  checkout session ($24,000, dev tenant); Group C (164-167) has 4 draft
  `sales_offers` + 8 active rule-group rows, no paid checkout. Group A+B are
  both range-priced via `base_price`/`max_price` (33 of 37 rows) — the
  AdminV2 export's "32 of 36 unpriced" only reflects the unused scalar
  `price` column. No `pillar` column or join table exists anywhere in the
  schema — the six-pillar mapping for Group B is name-based inference only,
  never structurally recorded. No code changes, no schema changes, no writes
  of any kind — pure read-only investigation. No further action needed from
  this session; the authoritative-catalog decision itself is Shane's call.
