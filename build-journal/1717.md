# #1717 — Change Control: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-09T21:12:19Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** b1965ec04 (rebased to 28010e17b on origin/main)

## Log
- 2026-09-09T20:57:31Z ⏳ IN FLIGHT — Investigated #1717, Feature parent #1486, the regenerated
  `docs/change-control-contract-pack.md` (#2989), and `Design/portal/design_handoff_full_site/screens/Change Control.dc.html`.
  Findings #3044/#3045/#3046 already filed by the pack regeneration session — nothing new to file
  yet. Beginning the real wire: types/api/visuals lib, then the page + components, using the
  already-real `settingsChangeControlWire.ts`/`settingsChangeControlLive.ts` for the settings
  surface and the route files' own `Wire*` interfaces as ground truth.
- 2026-09-09T21:12:19Z ✅ DONE — Built `artifacts/portal/src/pages/change-control.tsx` plus
  `components/change-control/{RaiseChangeDialog,ChangeActionDialog,ChangeRow,CatalogSection,
  CalendarSection,MetricsSection,PolicySection}.tsx` and `lib/change-control-{types,api,visuals}.ts`,
  wired to all 14 customer routes + the 3-route settings surface per the contract pack. Routed
  `/change-control` in `App.tsx`, gave it a real `builtPath` in `moduleNav.ts`, and repointed the
  three Overview page tiles that used to `comingSoonHref("Change Control", ...)` at the real page.
  Consumed the previously-unconsumed `settingsChangeControlWire.ts`/`settingsChangeControlLive.ts`
  in `PolicySection.tsx`. Two small, real backend extensions (no migration — both DB columns already
  existed): (1) `portal_change_control_policy.enforce_maintenance_windows` was read by the
  catalog-execute path but never exposed on the settings GET/PUT wire — added end to end as
  `CcPolicy.maintenanceWindows`; (2) added a real `activeNow: boolean` per window on the
  freeze-windows and maintenance-windows GETs, computed via the existing
  `isWindowActiveAt`/`spanWithinMaintenanceWindow` pure functions (not duplicated client-side) —
  needed for the page's "a freeze is in effect right now" banner. No fixture data anywhere.
  Findings #3044/#3045/#3046 were already filed by the #2989 pack-regeneration session and are cited
  in-page (catalog/metrics notes), not refiled.
  Verification: `artifacts/portal` `tsc --noEmit` clean (re-checked after rebase), `vite build`
  clean (`PORT=5183 npx vite build`, 959 kB main chunk — a pre-existing warning, not new);
  `artifacts/api-server` `tsc -p tsconfig.json --noEmit` clean; touched vitest suites
  (`portal-change-control.test.ts`, `portal-settings-change-control.test.ts`,
  `portal-change-freeze.test.ts`, `portal-change-maintenance.test.ts`) all pass, 75/75. Rebased onto
  a concurrent Policy Decisions wire-up (#1724) with no conflicts; `verify-branch-merged.mjs`
  confirms `28010e17b` is an ancestor of `origin/main`. `git status --porcelain` clean at finish.
  No BuildConsole-side work in this session, so nothing to note there.
