# #3773 — Feature: Simple Kanban board — buckets + tasks, Phase 1 only — backend + data model (MSPConsole)

- **Status:** ✅ DONE 2026-09-12T05:05:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** cf52285f5 (impl, rebased/pushed to main as 5dd38a6c4)

## Log
- 2026-09-12T04:35:00Z ⏳ IN FLIGHT — Building kanban_bucket + kanban_card Drizzle schema, manual migration, and CRUD API routes per Phase-1-only scope (no card type, no per-type forms, no connection to #3433/#3769/#3770/#3771, no UI yet). Following the same pattern as msp-status-reports (#3762).
- 2026-09-12T05:05:00Z ✅ DONE — Shipped `kanbanBucketsTable`/`kanbanCardsTable` (lib/db/src/schema/msp.ts), manual migration `lib/db/migrations/manual/2026-09-12-kanban-buckets-cards-3773.sql` (run against local Postgres, tables confirmed via `\d`), and CRUD routes in `artifacts/api-server/src/routes/msp-kanban.ts` (POST/GET buckets, PATCH/DELETE bucket, POST card, PATCH edit+move/DELETE card), registered in routes/index.ts. Verified: `tsc --noEmit` clean on api-server after rebuilding lib/db's declaration output; direct-DB smoke test (insert buckets/card, move card between buckets, board-shape query, cascade delete) run in a rolled-back transaction against the real local DB — all passed. Deliberately no card `type` field, no Project Templates, no wiring to #3433/#3769/#3770/#3771, no MSP Console UI screen (blocked on #3768's nav placement) — Phase 1 scope only. Rebased onto origin/main and pushed directly to main (5dd38a6c4); `verify-branch-merged.mjs` confirms merged. Working tree clean.
