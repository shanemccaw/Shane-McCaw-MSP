# Build Journal — Issue #2837

- **Status:** ✅ DONE 2026-09-04T23:20:00Z
- **Scope:** platform (investigation only — testbed tenant licensing/config state)

## Log
- 2026-09-04T22:40:00Z — Session started. Investigating why /security/alerts_v2-based checks return license_gap on testbed tenant mccawsoft2.onmicrosoft.com (id=1). Per #2837, hypothesis is missing Defender for Office 365 Plan license. Confirming via live Graph call against mt-app credentials (DEV app reg 9f6f4772-b5be-421f-815e-b392336c373a).
- 2026-09-04T23:15:00Z — ✅ DONE — Live client-credentials Graph call against Azure AD tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3` (`mccawsoft2.onmicrosoft.com`) using `MT_APP_CLIENT_ID`. `GET /v1.0/subscribedSkus` shows the tenant DOES hold `ATP_ENTERPRISE` (Defender for Office 365 Plan 1) under `ENTERPRISEPACK` (O365 E3), `provisioningStatus: "Success"`. `GET /v1.0/security/alerts_v2` still returns real `403 {"code":"Unauthorized","message":"Unauthorized request - Account is not provisioned."}`. Real cause is NOT a missing license — the tenant's Microsoft 365 Defender/Security & Compliance portal (security.microsoft.com) has apparently never been onboarded by an admin, which the Security Graph API requires independently of SKU assignment. `classifyGraphError()` (artifacts/api-server/src/lib/graph.ts:711-716) correctly classifies the raw "not provisioned" 403 as `license_gap` (the check genuinely can't run), but the customer-facing copy ("isn't licensed on your tenant") is factually wrong for a tenant in this specific state (licensed, just never onboarded) — filed as its own finding, #2925, parented under #1850 (Feature: Tenant Configuration State), labeled `bug`, board status "AI Batter Up". No code changed this session (investigation-only per dispatch scope — this is a tenant-state condition Shane needs to address via security.microsoft.com, not a code defect to fix). Branch `agent/2837-q1670` merged to `main` at `1c3ee2fc0` (verify-branch-merged.mjs: OK). Working tree clean.
- Findings filed: #2925
