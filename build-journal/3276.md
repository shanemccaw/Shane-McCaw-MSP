# #3276 — Feature: Vault Autofill add-on -- 30-day trusted-device UI (chip, popup, options)

- **Status:** ✅ DONE 2026-09-08T23:07:54Z
- **Scope:** platform (web/shanes-life -- Shane's Life, not the MSP product)
- **Started:** 2026-09-08
- **Commit(s):** 36b376126 (backend: migration 063, vault.mjs trust functions, POST
  /api/vault/:id/fill, checkOrigin exemption, logout-everywhere revokes), d3233b7c3
  (extension UI: background/content-script/popup/options rewritten for the trust model;
  app.js extensionReveal trust-toggle screen + deviceTrustRow site text; merged with
  concurrent #3272/#3273 Vault-room/Money-nav work)

## Log
- 2026-09-08T22:53:10Z ⏳ IN FLIGHT — read #3276's body + `Design/design_handoff_shanes_life/README.md`
  and `Shanes Life 19 - Vault Autofill.dc.html` (screenshots 1a-1e); confirmed migration 062's
  `vault_device_trust` table + `vault.always_ask` already landed via #3272. Backend landed:
  migration 063 (`vault_reveals.via`/`device_trust_id`, `vault_device_trust.last_used_site`), run
  against local `finances` DB; `vault.mjs` gained `mintTrustedDevice`/`resolveTrustedDevice`/
  `revokeAllTrustedDevices`/`fillWithTrust`; `POST /api/vault/:id/reveal` now mints a trust token
  on the same real WebAuthn ceremony when asked; new `POST /api/vault/:id/fill` (bearer trust
  token, refuses always-ask entries); `checkOrigin` exempts that one path (extension
  background-context caller, host-permitted but genuinely cross-origin); `logout-everywhere`
  now revokes all trusted devices too.
- 2026-09-08T23:07:54Z ✅ DONE — Extension rewritten for the trust model: `background.js`
  (chrome.storage.session trust cache, `SL_TRY_FILL` one-click fill, `SL_LIST_LOGINS`/
  `SL_SET_ALWAYS_ASK`/`SL_TRUST_STATUS`/`SL_SET_TRUST_DAYS`/`SL_FORGET_DEVICE`,
  `SL_REQUEST_REVEAL` now offers the trust toggle), `content-script.js` (dark pill chip,
  blue one-click Fill vs amber Unlock, "Filled · logged" flash), `popup.js`/`.html` (trust
  card, search, For this site / Everything else, footer links), `options.js`/`.html` (This
  Chrome card with 7/30/90-day picker + Forget, Always-ask list), `bridge.js` (relays the
  minted trust token), `README.md` (supersedes "every fill is a Face ID"). `public/app.js`
  gained a dedicated `extensionRevealCard` overlay screen (design 1c: pulsing ring, "Face ID
  to fill X", the trust toggle read at submit time) wired into the real `viewVault` room
  (not the old `viewMoneyVault`, which a concurrent #3273 commit removed -- merged by hand,
  re-verified after). `deviceTrustRow` now shows "trusted till X · N days · last fill ago,
  site" per design 1e.
  **Verification:** `bin/check.mjs` 294/294 (was 284/284) against a live local server -- 10
  new checks cover mint-on-reveal, the trusted fill's real plaintext + audit row
  (`via='extension-trusted'`, `device_trust_id`, no `credential_id`), the
  `last_used_at`/`last_used_site` stamp, a fake token's 401, an always-ask entry's 409,
  Forget's real revoke. New `bin/verify-vault-trust.mjs` 14/14 -- direct `vault.mjs` coverage
  for `revokeAllTrustedDevices` (logout-everywhere's own revoke-all, which `bin/check.mjs`'s
  one continuous session can't safely exercise mid-run without killing every later section).
  `checkOrigin` exemption confirmed narrow live: a foreign Origin on a normal vault route
  still 403s; only `/api/vault/:id/fill` is reachable cross-origin (401 for a bad token, not
  403). Server + all test rows cleaned up (0 leftover `check+`/`verify-trust+` users). Not
  live-browser-verified (documented in `extension/README.md`, same honest boundary #3242/
  #3243 already state): the real chip/popup/options rendering in Chrome and the real Face
  ID -> trust-toggle -> mint round trip end to end -- needs Shane's own hands on a real
  browser. Merged into origin/main, verified with `verify-branch-merged.mjs`; working tree
  clean.
