# Build Journal — #3806

- **Status:** ✅ DONE 2026-09-12T19:03:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T18:55:15Z — Session started. Task: split ghost card's primary click (currently
  `DispatchBlockerFromGhostCardAsync` directly) into pure navigate-or-notify behavior, and move
  the existing dispatch-ask behavior to a new, separate, explicit secondary control on the ghost
  card. `DispatchBlockerFromGhostCardAsync` itself is not rewritten — only what triggers it
  changes.
- 2026-09-12T19:03:00Z — ✅ DONE — commit `328e9ea3f` (desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs).
  Primary `MouseLeftButtonDown` on a non-live ghost card now shows a `ToastEngine.Info` notice
  ("#N has no build yet and isn't queued.") instead of calling `DispatchBlockerFromGhostCardAsync`
  — no chat is touched. Live-card click behavior (`RevealQueueItem`) is unchanged. Added a
  visually distinct "⚡ Dispatch" pill control in the card's top row (own border/background/
  tooltip, its own `MouseLeftButtonDown` with `e.Handled = true` so it never falls through to the
  card's navigate-or-notify handler) that calls `DispatchBlockerFromGhostCardAsync` unchanged —
  same claim-before-ask (#3509), dedup guard (#3600), and toast reporting as before this change.
  Updated the card's tooltip to describe the new split. Verified: `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0 errors (10 pre-existing
  unrelated warnings). `verify-branch-merged.mjs` — OK, merged into `origin/main`. `git status
  --porcelain` — clean. No manual WebView2/UI click-through was performed this session (no
  BuildConsole instance was launched for manual QA — see CLAUDE.md "NEVER launch BuildConsole.exe
  for verification"); confidence is from a clean compile + direct code read against the issue's
  exact acceptance criteria, not a live click-through.
  No findings filed — no bugs/gaps discovered outside this issue's own scope.
