# Build Journal — #3806

- **Status:** ⏳ IN FLIGHT 2026-09-12T18:55:15Z
- **Scope:** buildconsole

## Log
- 2026-09-12T18:55:15Z — Session started. Task: split ghost card's primary click (currently
  `DispatchBlockerFromGhostCardAsync` directly) into pure navigate-or-notify behavior, and move
  the existing dispatch-ask behavior to a new, separate, explicit secondary control on the ghost
  card. `DispatchBlockerFromGhostCardAsync` itself is not rewritten — only what triggers it
  changes.
