# #1500 — Change Control: Freeze / blackout windows with server-side enforcement

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** e0b31e036

## Log
- 2026-08-29 ⏳ IN FLIGHT — building `change_freeze_windows` (scope: global/tenant/workload,
  recurrence), wiring server-side enforcement at submit into `POST /portal/change-control`
  and `POST /msp/change-requests`, and a `freezeException` path (mandatory justification,
  higher approval bar) that extends #1496's `cr_approvals` ledger with a dedicated
  MSP-only-decidable stage. Schema + migration + routes + Wire* interfaces only — no UI
  page exists to wire (Design/portal/ is empty for this module), per the build's explicit
  scope stop.
- 2026-08-29 ✅ DONE — `e0b31e036`. Shipped:
  - `change_freeze_windows` table (scope global/tenant/workload; one-off or
    weekly/monthly/quarterly/annually recurring span; active flag) —
    `lib/db/migrations/manual/2026-08-29-change-freeze-windows-1500.sql`, run against local
    `DATABASE_URL` and confirmed live via `psql \d` (table, all 5 CHECK constraints, FK).
  - Two additive columns on #1496's `cr_approvals` (`freeze_window_id`, `justification`) plus
    a CHECK constraint enforcing the justification is non-blank whenever a freeze-exception
    stage exists — proven live (an attempted NULL-justification exception row was rejected by
    the real constraint).
  - Enforcement at submit, server-side, in BOTH create paths onto `msp_change_requests`: the
    customer wizard (`POST /portal/change-control`) and the MSP console
    (`POST /api/msp/change-requests`) — neither can bypass a freeze the other blocks. Gated
    on the tenant's own `portal_change_control_policy.enforceFreezeCalendar`.
  - `freezeException`: a mandatory written justification is the only way through an active
    freeze; it materialises as an ADDITIONAL stage on the approval ledger
    (`recordFreezeException`) that only MSP staff may decide — `recordApproval` and
    `recordRejection` now enforce `approver.role === "msp"` on any stage carrying a
    `freezeWindowId`. This is the "higher approval bar" the issue asks for.
  - MSP CRUD for the calendar (`msp-change-freeze-windows.ts`: GET/POST/PATCH) and a
    read-only customer-facing freeze-calendar GET on `portal-change-control.ts` — without a
    write route nothing could create real data to prove enforcement against.
  - Pure recurrence/scope-matching logic (`portal-change-freeze.ts`) unit-tested
    (`portal-change-freeze.test.ts`, registered in `vitest.config.ts`'s explicit `include`
    list — the file is NOT glob-discovered in this project, a real gotcha this session hit
    and fixed for its own test).
  - Verification: `lib/db` composite project rebuilt (`tsc -b --force`) so the new schema
    exports resolve; `artifacts/api-server` full `tsc --noEmit` is clean of anything from this
    build (47 pre-existing errors remain, in 7 files this session never touched — filed as
    #1765). Targeted vitest runs pass (portal-change-freeze.test.ts, portal-change-approvals.test.ts,
    portal-change-control.test.ts). Full `vitest run` (255 files) also run: 68 files/216 tests
    fail pre-existingly, none in a file this build touched or added — filed as #1772.
    Real-DB proof beyond the schema: candidate-window scoping query and the justification
    CHECK constraint both exercised against live rows (msp 1 / real tenant), not just unit
    tests.
  - Scope stop honored: no page/route/component added under `artifacts/portal` — Design/portal/
    is empty for this module.
  - Findings filed (new issues, not fixed here, all parented + set to "AI Batter Up"):
    #1762 (`scheduled_for` is free text, not a timestamp — blocks date-based freeze checks
    against a change's actual window), #1763 (MSP console never materializes the #1496
    approval ledger), #1764 (msp-changes.ts's create schema only allows 5 of 8 widened
    categories), #1765 (47 pre-existing tsc errors, unrelated files), #1772 (68 pre-existing
    vitest failures, unrelated files — one example looks like a real money-formatting 100x
    scaling bug in msp-financial-aggregator.test.ts).
  - Migration filename recorded on #1630 per the manual-SQL-migration convention.
  - Branch verified merged into origin/main (`verify-branch-merged.mjs`); `git status
    --porcelain` clean before this bookend.
