# #2106 — Concurrency overcommit under 40+ queued builds (session-limit slot leak + premature auto-finalize)

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-31 ⏳ IN FLIGHT — Pulled #2106 (Epic #1789). Two confirmed mechanisms to fix in `QueueWatcherService.cs`:
  - **Mechanism 1** — session-limit-parked builds (`LimitPausedStatus`) get their `_running` slot freed the same as a normal exit, so `freeSlots` overcommits while the paused session waits ~10-15 min to auto-resume; when it resumes it adds itself back with no compensating subtraction.
  - **Mechanism 2** — `ScheduleAutoFinalize`'s idle timer force-closes an interactive build's stdin without checking `SubagentActivityInfo` for active background sub-agents, prematurely finalizing a parent that is only idle because it is waiting on its own Task-tool sub-agent.
  - Starting real audit of `TickAsync` reap loop, `RunningEntry`, `SessionLimitAutoRestartService`, `ScheduleAutoFinalize`, and the UI status→color mapping before writing any code.
