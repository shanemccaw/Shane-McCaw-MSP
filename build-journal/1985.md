# #1985 — FindRepoRoot() null coalesced to empty/hardcoded paths at ~10 call sites (silent wrong-root)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** fa1f94f91 (real audit+fix commit), 96b6394d9 (IN FLIGHT bookend)

## Log
- 2026-08-30 ⏳ IN FLIGHT — auditing all `FindRepoRoot()` call sites in desktop/BuildConsole; real coalescing sites found via `grep -rn 'FindRepoRoot()\s*??'`: GitDetailView.cs:2073, TerminalView.xaml.cs:30/148, MainWindow.xaml.cs:454/504, ClaudeUsageMeterService.cs:381, LocalDeployStatusService.cs:59/68, LocalSqlExecutor.cs:50, VisualTestTrackerStore.cs:72. Deciding fail-closed vs tolerable per site next.
- 2026-08-30 ✅ DONE — audited every `FindRepoRoot()` call site (~60 across desktop/BuildConsole via a broader `grep -rn FindRepoRoot`), not just the ~10 the issue named. Fixed the real coalescing sites (fail-closed where a wrong root would be dangerous/meaningless: BuildQueuePostgresClient.TryCreate, LocalSqlExecutor, VisualTestTrackerStore, MainWindow's DB-client + PostBuildDeployPipeline construction, GitDetailView's resumed-build cwd, MainWindow's silent no-op SQL-open); found and fixed one cascade bug not in the issue's list — QueueWatcherService's constructor was silently defaulting a null repo root to `AppDomain.CurrentDomain.BaseDirectory` (the app's own exe folder) and using it as the launch cwd for shared-checkout builds, now fails loud instead; removed both hardcoded-machine-path fallbacks (TerminalView's `C:\Source\...`, LocalDeployStatusService's `C:\dev-server`). Left ~45 already-correctly-guarded sites untouched, documenting the genuinely-tolerable ones in place per the issue's instruction not to apply one blanket treatment. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: clean, 0 warnings/0 errors, both before and after rebasing onto a moved origin/main. Filed one finding: #1992 (TerminalView doc comment claims Replit-POST migration but code still runs a local PowerShell shell — proven false while auditing this file's two coalescing sites), parented to #1791, board status "AI Batter Up". `git status --porcelain` clean. Verified `agent/1985-q1028` (fa1f94f9) is an ancestor of `origin/main` via `verify-branch-merged.mjs`.
