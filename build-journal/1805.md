# #1805 — Batter Up: right-click Start Now — override whatever a queued item is waiting on and launch immediately

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** c853da8db (IN FLIGHT bookend), 00ca061bd (Start Now implementation, on `agent/1805-q1010`), merged to `main` at 0f1db4895

## Log
- 2026-08-30 ⏳ IN FLIGHT — read QueueWatcherService.cs (admission/claim/freeSlots logic, ForceLaunch/Run Now #820), BatterUpPanel.xaml(.cs), and BuildTrackerConfig.cs's MaxConcurrent handling (BuildConsoleSettings.cs itself has no MaxConcurrent — it lives in BuildTrackerConfig from scripts/build-queue-watcher.config.json, default 8). Found a pre-existing comment in BuildQueuePanel.xaml.cs (`#1805 Start Now, #1641 Build Now`) anticipating this exact feature next to "Run Now" (#820), which already force-claims+force-launches a queued row bypassing BOTH the blocker check and the concurrency cap unconditionally. Building "Start Now" as a new, distinct context-menu action on queued rows in BuildQueuePanel that adds the one thing Run Now lacks: a genuine capacity check that is never bypassed.
- 2026-08-30 ✅ DONE — Added `QueueWatcherService.StartNowAsync` (00ca061bd): checks `_running.Count >= _maxConcurrent` FIRST and refuses (no claim attempted) when genuinely full, logging `Start Now: queue #N (title) — Not launched — genuinely at capacity (X/Y running)...` on the `watcher` channel and returning `StartNowOutcome.AtCapacity`. Otherwise clears any per-item manual Pause, captures the row's real held-blocker reason from `HeldBlockerReasons` (Git #1600) BEFORE claiming, force-claims + force-launches via the same proven `ForceClaimAsync`/`ForceLaunch` path Run Now (#820) uses, and logs exactly which real blocker was bypassed or that it was only poll timing. Wired a new "⚡ Start Now" context-menu item on queued rows in `BuildQueuePanel.xaml.cs`, right next to "Run Now", using `ToastEngine` for user feedback (Launched=success toast, AtCapacity/Failed=warning toast with the real reason). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors, both before and after merging onto latest `origin/main`.
  - **#1803 free-capacity mystery — investigated, not a bug.** `%AppData%\BuildConsole\settings.json` has `QueuePaused: true` right now, and the activity log (`activity-2026-08-30.log`) confirms: `[07:49:23.160] [watcher] Queue PAUSED — already-running builds continue; no new queued items will be claimed/started until resumed.` with no matching RESUMED after it. `QueueWatcherService.TickAsync` explicitly skips claiming/launching ANY new queued item while `_paused` is true (`if (_paused) return;`), regardless of free slots — this fully explains #1803 sitting "QUEUED" with visible free capacity. #1803 itself is not in the per-item `PausedBuildIds` list ([582, 845] only), confirming it's the GLOBAL toggle, not a per-item pause. This is the feature working as designed (`BuildQueuePanel.QueuePause.cs`'s header toggle already renders an unmistakable peach "Paused" label + tooltip when active) — not a code defect, so per #1805's own instruction ("if a real bug turns up... file it separately") no new issue was filed; recorded here and in the issue comment instead.
  - Verification: capacity-never-overridden is structural — the `_running.Count >= _maxConcurrent` check runs before any DB claim, so a full queue can never be force-claimed by Start Now. The claim/launch path itself is the exact same proven Run Now (#820) mechanism already live in production, so a real queued item with room to run launches immediately via the same code path, now with the added capacity guard and clearer logging. No BuildConsole test manifest added (desktop/BuildConsole is out of scope per CLAUDE.md).
