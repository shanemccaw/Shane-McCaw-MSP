# #1445 — Remediation Tracker: looks fully fake OR data never removed from database — needs root-cause diagnosis first

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** bc31c04e1 (bookend IN FLIGHT); no code changes — investigation-only, root cause is DB hygiene, not a code bug

## Log
- 2026-08-27 ⏳ IN FLIGHT — Diagnosing root cause: live-querying backing tables (tenant_pillar_snapshots + remediation tracker tables) to determine whether Remediation Tracker shows fixture/fake content, stale unpurged DB rows, or a genuine backend gap, per issue #1445.
- 2026-08-27 ✅ DONE — Root cause is **DB hygiene, not a code/fixture bug**. Read every in-scope file
  (`copilot-readiness-remediation-tracker.tsx`, `RemediationTrackerBody.tsx`, `useRemediationTracker.ts`,
  `remediation-tracker-catalogue.ts`, `remediation-tracker-verification.ts`, `remediation-tracker-pricing.ts`,
  `portal-remediation-tracker*.ts`) — every value is honestly wired to a real DB query with no fixture/demo
  fallback anywhere; `remediation_tracker_steps` is a genuinely empty table for every tenant, so all step ticks
  render as an honest "not started", never invented state.
  Live-queried the real local Postgres (`DATABASE_URL`) directly: the designated testbed tenant
  (`customer_id=1`, `tenant_id=c4c814d4-...`, the one `2026-08-26-comprehensive-testbed-reset-1329.sql`
  targets) is genuinely clean — 0 rows everywhere. But `customer_id=3` ("Test Me", tenant
  `0a361ab2-9e85-4bbf-8b75-c1ebf042dfba`, `is_testbed=false`) carries real, non-fixture leftover data from a
  single scan on 2026-08-09: 98 real `msp_diagnostic_findings` rows + 1 `msp_diagnostic_runs` row, 334
  `tenant_monitor_profiles` rows, and 1 signed+paid `assessment_sow_agreements` row ($24,000, 4 workstreams).
  None of it is purged by the reset script, because that script scopes exclusively to `customer_id=1`'s tenant
  GUID and never touches `customer_id=3`. If Shane's live test hit "Test Me", the tracker's Gate score/pillar
  cards/phase pricing would show real numbers computed from an 18-day-stale scan under a throwaway-sounding
  tenant name — exactly the ambiguity the issue predicted.
  Also confirmed, per the issue's explicit ask: the in-scope tracker (`copilot-journey`'s
  `useCopilotJourney`/`RemediationTrackerBody.tsx`) does **not** read `tenant_pillar_snapshots` at all — its
  pillar/gate scores come from a *separate*, equally real, live-computed path
  (`buildWarRoomPillarStats`/`computePillarDisplayScore` off `msp_diagnostic_findings` +
  `tenant_monitor_profiles`, served by `GET /portal/assessment/war-room-pillars`). The platform's documented
  rolling scan-to-scan / permanent-day-one-baseline scoring model (`tenant_pillar_snapshots`, served by
  `GET /portal/remediation-tracker/pillar-scores`, `portal-remediation-tracker-scores.ts`, Git #1381) is real
  and honest too, but is only consumed by the separate `portal-v2/remediation` tracker
  (`components/portal-v2/*`), not by the tracker this issue scopes. Two parallel, both-real implementations —
  flagged for Shane, not treated as a bug, since nothing in-scope is fake.
  No code changes made — nothing to fix; the standing "real / no-scan-data / no-backend-gap" rule is already
  satisfied everywhere in scope. Full findings posted to the GitHub issue. See that comment for the two
  DB-hygiene items flagged for Shane's own review/decision (stale `customer_id=3` rows; the reset migration
  itself never having run through the tracked `simulator_migration_runs` path).
