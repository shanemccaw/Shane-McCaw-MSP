# #3386 — PATCH /api/msp/sales-offers/:id/state can accept an offer with zero fulfillment, and the result is permanently stuck

- **Status:** ✅ DONE 2026-09-10T21:30:51Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** fb66259ee03134248d8ea239f4daa00850d3f9ad (on main)

## Log
- 2026-09-10T21:19:13Z ⏳ IN FLIGHT — read msp-sales-offers.ts:344-388, sales-offer-engine.ts:456-504,
  msp-sow.ts:133-399 (`POST /api/msp/offers/:offerId/accept`, the real fulfillment path), and
  portal-offers.ts:214-281 (customer-side accept, which also calls fulfillment). Confirmed the
  generic `PATCH /api/msp/sales-offers/:id/state` route only calls `transitionOfferState()`
  (abstract state machine, no serviceClass branching, no SOW/Stripe/tier-gate call) and that
  `accepted: []` in `VALID_TRANSITIONS` means there is genuinely no way out once reached that way.
  Confirmed via grep that no frontend (msp-console/admin-panel) currently calls this generic
  route with `newState=accepted` — it's a live, reachable backend gap, not yet wired into UI.
  Fix direction: block `newState === "accepted"` on the generic PATCH route (per the issue's own
  suggested fix direction option 1), directing callers to the purpose-built
  `POST /api/msp/offers/:offerId/accept` route instead, which already does fulfillment +
  entitlement gating correctly. `rejected`/`expired` transitions need no fulfillment and stay
  allowed on the generic route.
- 2026-09-10T21:30:51Z ✅ DONE — implemented: `msp-sales-offers.ts`'s `PATCH .../state` route now
  returns 422 for `newState === "accepted"` with a message pointing at
  `POST /api/msp/offers/:offerId/accept`, before the offer lookup/`transitionOfferState()` call —
  `accepted` can no longer be reached with zero fulfillment through this route.
  `rejected`/`expired` are unaffected. Added `msp-sales-offers.test.ts` (6 tests, all passing):
  422+no-call for `accepted` (including on a nonexistent offer id, proving the guard fires before
  the DB lookup), `rejected`/`expired` still transition normally, 404 for cross-mspId offers, 400
  for an unrecognized `newState`. `tsc --noEmit` clean. Also ran the sibling suites
  (`portal-offers.test.ts`, `msp-sow.test.ts`, `msp-sales-bundles.test.ts`) alongside mine — 72/72
  pass, no regressions (their one failure on a bare `vitest run` invocation was a pre-existing,
  unrelated missing-env-var issue when bypassing the package's own `pnpm test` wrapper — confirmed
  by supplying `AI_INTEGRATIONS_ANTHROPIC_BASE_URL`/`_API_KEY` from `.env.local` and re-running
  clean; not touched or caused by this change).
  Mid-build a concurrent commit (`ce1a714d8`, "Remove PlatformAdmin ?mspId= cross-MSP override on
  session-scoped msp routes") landed on `main` and renamed `resolveMspId` → `resolveMspIdStrict`
  in this same file, including the `PATCH .../state` route. Rebased cleanly (git auto-merged, no
  conflicts); updated my test's mock to match the new `resolveMspIdStrict` export and re-verified
  green before pushing. Pushed straight to `main` per the worktree-isolation convention (rebase +
  retry twice as `main` moved under concurrent load). `verify-branch-merged.mjs`: OK, merged.
  `git status --porcelain`: clean. Commit `fb66259ee03134248d8ea239f4daa00850d3f9ad`.
  No new findings filed — this build's own fix is the whole scope of #3386; no other real
  problem was discovered in the surrounding code while reading it.
