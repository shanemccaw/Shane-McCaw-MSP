# #3411

- **Status:** ✅ DONE 2026-09-10T23:58:10Z
- **Scope:** platform

## Log
- 2026-09-10T23:46:24Z — Starting: fix stale assertion in `src/routes/msp-staff.test.ts` (line ~279) that expects the old flat `{error: string}` body; `requireMspScope` answers via `apiError()`'s enveloped `{error:{code,message}}` shape per #2113/#2832.
- 2026-09-10T23:58:10Z — ✅ DONE — Rewrote the assertion in `artifacts/api-server/src/routes/msp-staff.test.ts` ("error message indicates access is not permitted") to read `json.error.message` and additionally assert `json.error.code === "FORBIDDEN"`, matching `apiError()`'s real enveloped shape. Ran the file directly (`node --experimental-strip-types --experimental-test-module-mocks --test src/routes/msp-staff.test.ts`): 12/12 pass, 0 fail. Commit `0b77ff29` pushed to `origin/main` (rebased after a concurrent push moved main). Working tree clean.
