# #3944 — api-server test script silently skips admin-m365-run + portal-checkout-webhook tests

- **Status:** ✅ DONE 2026-09-13T06:10:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** b275685d0 (concurrent build landed the identical fix first — see log), a06fc5854 (this session's IN FLIGHT bookend)

## Log
- 2026-09-13T05:44:30Z ⏳ IN FLIGHT — confirmed the missing space bug at `artifacts/api-server/package.json:12` (`admin-m365-run.test.ts''src/routes/portal-checkout-webhook.test.ts`), about to insert the space and run the full `pnpm --filter @workspace/api-server run test` pass. Per #3944's own filing, any failure surfaced by the two long-dormant tests will be filed as a separate sibling issue under #1571, not fixed in this build.
- 2026-09-13T06:10:00Z ✅ DONE — Inserted the missing space and ran the two previously-dormant tests directly (`node --experimental-strip-types --experimental-test-module-mocks --test`, bypassing the `vitest run &&` chain since the full `pnpm test` pass has its own large pre-existing vitest failure baseline unrelated to this fix). `admin-m365-run.test.ts` passes clean (3/3). `portal-checkout-webhook.test.ts` fails at import — its `mock.module("@workspace/db")` stub is missing `tenantSubscriptionsTable`, which `lib/tenant-billing-state.ts` (imported by the route under test) now needs. Filed as its own sibling issue **#3952**, sub-issue of #1571, `bug` label, board status set to "AI Batter Up", per #3944's own instruction not to fix the regression in this build. While preparing to push my own package.json fix commit, discovered a concurrent build (`b275685d0`, "Fix 9 pre-existing api-server vitest failures") had already landed the identical one-line fix plus unrelated vitest-baseline fixes and its own separate finding (#3950) a few minutes earlier — my rebase correctly dropped my duplicate commit ("patch contents already upstream"), so no redundant commit exists. Verified current `artifacts/api-server/package.json:12` has the space; confirmed `#3952`'s underlying gap (`tenantSubscriptionsTable` missing from the mock) is still present in the file as of `origin/main`, i.e. not a duplicate of anything b275685d0 fixed. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. Working tree clean.
