# #1443 — Copilot Readiness assessment output: strict pass — "What the assessment produced" still has fake data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** `4c5bf45d0` (rebased/pushed as `1ef235915`)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Investigating copilot-readiness.tsx, copilot-readiness-proposal.tsx, copilot-readiness-documents.tsx, and the copilot-journey components (journeyPreviewFixture.ts, copilotReadinessReport.ts, previewStatementOfWork.ts, previewRemediationGuide.ts, remediationLiveGuide.ts, pillarGovernancePosture.ts, and others under artifacts/msp-portal/src/components/copilot-journey/) to find where the real, post-scan "What the assessment produced" output still leaks fixture/preview data instead of real live data / honest NoScanData / NO-BACKEND-TO-WIRE.
- 2026-08-28 ✅ DONE (`1ef235915`) — The files listed in the issue were already honest (real per-tenant data, fixture reachable only behind `?preview=design`). The literal "What the assessment produced" heading lives in `portal-v2-copilot.tsx` (the standalone-offer verdict page), not in the listed files — that's where the leak was. Two real leaks found and fixed:
  1. The "produced" card rendered `copilotData.ts`'s `CP_DELIVERABLES` — a hardcoded 5-item list under invented "DOC-01".."DOC-08" numbers — next to a fixed "Nine documents, all issued" note, for every tenant. Now reads the tenant's real 9-document set off `view.generation` via `withLiveDocuments()`/`generationView()` (the same source Scene 9 already renders from), with a real loading-gated note and real per-document status.
  2. One click over, `portal-v2-documents.tsx`/`docLibraryModel.ts` rendered the fictional Halden Materials fixture's `fresh`/`freshNote` (a fake "Telemetry moved since 3 Aug" amber "out of date" band on 2 of 9 real documents) and a fixed "Issued 3 Aug 2026"/"Signed 6 Aug 2026" cover date, for every tenant. Since every owned document renders live from the tenant's own current scan at open time (Git #643), there is no real staleness to report — `docStateFor()` now always reports the true state, "Current", and the cover date comes from the tenant's real `view.tenant.scannedOn`.
  Verification: `docLibraryModel.test.ts` 22/22 green (updated for the new honest shape); `copilotModel.test.ts` 19/19 green when run standalone earlier in the session, later hit an unrelated environment flake (`node_modules/@workspace/copilot-scan-scene` symlink mid-session repointed to another worktree, `ERR_MODULE_NOT_FOUND` — confirmed pre-existing/external, not from this change, since `docLibraryModel.test.ts` — which doesn't touch that chain — kept passing throughout). `tsc --noEmit` on msp-portal clean on every touched file (2 pre-existing unrelated errors in `SowScreen.tsx`/`PortalV2Shell.tsx`, per #1391's own bookend). `shaneapp://runTest` confirmed reachable and invoked for real against `portal-v2-copilot.json&onlyTag=1443`; it reported "no steps matched session tag" against the still-unmerged deployed manifest, the same expected pre-deploy signal #1391/#1381 documented — will go green once this commit deploys. Branch-merge verified via `verify-branch-merged.mjs` before writing this DONE. Follow-up flagged (not fixed, out of this pass's scope): `portal-v2-documents.tsx`'s Regenerate/Export/Share/Ask ShaneBot actions all route through `FormDrawer` with no `onSubmit` wired to any real endpoint — every submit shows a fabricated success message with nothing real behind it. Worth its own issue.
