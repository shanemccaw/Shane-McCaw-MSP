# #3111 — Feature: Shopping — Per-run budget + put-it-back (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T21:10:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `af73d7afc` on `main` (rebased from `a01d630f4` after #3108/#3110 landed under it)

## Log
- 2026-09-07T20:35:00Z ⏳ IN FLIGHT — blocked_by #3088 edge removed (#3088 shipped `e21b1d1ee`,
  `complete`-labeled). Read `web/shanes-life/Design/design_handoff_shanes_life/README.md` +
  `contract.md` (per-run budget, put-it-back card). Investigating `src/core/lists.mjs`,
  `src/routes/api.mjs`, `public/app.js` Shopping mode to add a real stated budget per run,
  a live running total, and an informational put-it-back card at threshold.
- 2026-09-07T21:10:00Z ✅ DONE — `af73d7afc` on `main`. Real scope shipped:
  1. `lists.budget_cents` (`web/shanes-life/migrations/023_shopping_budget.sql`, nullable,
     per-list) -- run against the real local `finances` database myself.
  2. `getListDetail` (`src/core/lists.mjs`) now returns `budget`/`totalCents`/`total`/
     `overBudgetCents` -- a real running total summed from `list_items.price_cents`, the same
     column #3109's real barcode scan is the only thing that ever sets (a plain add stays
     deliberately unpriced, per #3109's own recorded decision -- did NOT reopen that by adding a
     price field to a plain add, after first drafting it that way and then finding #3109/#3112
     had already landed on `main` with that exact call made). `setListBudget` sets/clears it.
  3. `PATCH /api/lists/:id/budget` (dollars, `null` clears).
  4. MCP: new `set_list_budget` tool, plus `push_list`'s own optional `budget` arg for "a $45
     grocery run" in one call.
  5. `public/app.js`: `budgetCard` in Shopping mode -- real running total (click to edit the
     budget), a bar once a budget is set, and an informational put-it-back card (never blocks an
     add, just offers "I put it back" / "Keep" on the most-recently-priced items) once over.
  6. `bin/check.mjs`: 12 new real checks (scan-priced items, real budget PATCH/clear,
     `set_list_budget` + `push_list(budget)` over MCP, put-it-back delete, DB-verified
     `budget_cents` write) -- **114/114 pass** against a real running server + the real local
     `finances` database.

  **Real mid-build correction, worth recording**: the first pass (migration `020_shopping_budget.sql`,
  `price_cents` added to `list_items`, price accepted on a plain add/`push_list`) was built and
  fully verified (86/86) against a stale base -- while it was in flight, sibling sessions on
  #3108/#3109/#3110/#3112 landed real, overlapping work on `main`: #3109 already added
  `list_items.price_cents`/`price_source`/`priced_at` (scan-only, explicitly *not* via a plain
  add) and #3108 claimed migration number 022. Caught this before pushing broken work: reset the
  worktree to `origin/main`, re-read the real landed code (`lists.mjs`, `scan.mjs`,
  `mcp/tools.mjs`, `app.js`, `check.mjs`), and rebuilt #3111 to compose with what was actually
  there instead of duplicating it -- migration renumbered 020 -> 022 -> 023 as the real ledger
  kept moving; local `schema_migrations` ledger rows corrected to match. `verify-branch-merged.mjs`
  confirms `af73d7afc` is an ancestor of `origin/main`. Working tree left clean (`.env` scratch
  file and the port-5912 dev server process, both used only for verification, removed/stopped).
  No findings filed -- nothing discovered outside this issue's own scope.
