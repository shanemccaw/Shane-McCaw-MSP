# #4160 — BugsDocumentView NullReferenceException + screenshot pill click handler

- **Status:** ✅ DONE 2026-09-15T01:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** a97a63238593e32e91a8d6ab0d1fa2e2ed9a0895

## Log
- 2026-09-15T00:41:09Z ⏳ IN FLIGHT — investigating BugsDocumentView.xaml.cs crash (premature Filter_Changed during InitializeComponent) and TestModeComposerPanel screenshot pill click wiring.
- 2026-09-15T01:05:00Z ✅ DONE — Confirmed real root cause by reading BugsDocumentView.xaml: `RadioFilterOpen` is `IsChecked="True"` and declared (line 34) before `LstBugs` (line 80) in document order, so its `Checked` → `Filter_Changed` → `ApplyFilters()` fires mid-`InitializeComponent()` while `LstBugs`/`TxtBugCount` are still null fields, throwing NullReferenceException on every "View in Document" open. Fixed with an `_initialized` guard (false until end of constructor) that makes `Filter_Changed` no-op until real init completes — the real `ApplyFilters()` call still happens via `Initialize()`/`RefreshBugsAsync()` right after construction, so no filter behavior is lost.
  Wired real click handlers for the previously-dead "🖼 Element Screenshot Attached" bug-card pill and the staged-screenshot thumbnails in `TestModeComposerPanel` — both reuse the existing `ScreenshotGalleryWindow` (Git #966) rather than a new viewer, passing the bug's full `Screenshots` list (or the single staged item) so Prev/Next navigates multiple attached shots.
  Verified: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — Build succeeded, 0 Warnings, 0 Errors. Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
  No genuine new findings surfaced beyond the two issues already in scope; nothing filed against #1202.
