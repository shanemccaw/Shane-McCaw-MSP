# #2663 — In Progress chat tracking: fix desync/wrong-link across 4 divergent entry points + add Replace action

- **Status:** ⏳ IN FLIGHT 2026-09-03T16:22:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03

## Log
- 2026-09-03T16:22:00Z ⏳ IN FLIGHT — Read #2663 full body + its single BUILD comment (no superseding content). Re-confirmed all four `ToggleChatInProgress` call sites live (line numbers shifted slightly from the audit):
  - Site 1 — `MainWindow.xaml.cs:3288` (tab-header ⚡ bolt in `OpenChatTab`), uses cached `chat.ConversationId`/`chat.ClaudeUrl`.
  - Site 2 — `MainWindow.xaml.cs:4999` (`MarkChatTabInProgressAsync`, tab right-click), already live-resolves via `TryGetChatUrlForTab` **only as a fallback** when cached fields are empty.
  - Site 3 — `LeftSidebar.xaml.cs:3719` (sidebar chat-card right-click), uses `chat.ConversationId`/`chat.ClaudeUrl` from the board snapshot.
  - Site 4 — `FocusImmersiveView.xaml.cs:577` (immersive chip ✕ unmark), uses `item.ConversationId`/`item.ClaudeUrl` from a persisted `PersistedInProgressChat`.

  Plan: (1) extract ONE shared resolution helper on `MainWindow` (`ResolveChatIdentity`) that reads the chat's REAL identity live off its open tab's WebView2 URL, falling back to cached values only when no live URL resolves — used by all four sites + the new Replace action. (2) Account scoping: default to GLOBAL per dispatch instruction (drop the `InProgressChatsForAccount` filter in `FocusModeBar.RefreshInProgressChats`) — judgment call, flagged for Shane. (3) Add a "Replace with active tab" right-click on `FocusModeBar` chips. Building now.
