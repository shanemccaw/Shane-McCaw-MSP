# #2730 — sales_offers.customer_id FK targets users.id but every live consumer treats it as tenants.id

- **Status:** ✅ DONE 2026-09-04T01:10:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** b1462a8ec (merged to origin/main; local worktree commit 9ccb47bc4 before rebase)

## Log
- 2026-09-04T00:20:00Z ⏳ IN FLIGHT — Confirmed live via `psql \d sales_offers`: `sales_offers_tenant_id_fkey` FK on `customer_id` really does target `users(id)`. Confirmed all 5 live rows have `customer_id = 1`, which is also a valid `tenants.id` (no orphans against `tenants`). Per issue body, 3 independent live consumers (msp-sow.ts accept handler, persistSalesOfferCandidates/runSalesOfferEngineForTenant, portal contract pack) already treat the value as `tenants.id`. Proceeding to repoint the FK at `tenants.id` (not the reverse) — Drizzle schema update + manual migration, run against local DB myself (additive/corrective DDL, no data loss since current values already satisfy `tenants.id`).
- 2026-09-04T01:10:00Z ✅ DONE — Repointed the FK: `lib/db/migrations/manual/2026-09-04-sales-offers-customer-id-fk-tenants-2730.sql` (drops `sales_offers_tenant_id_fkey`, adds `sales_offers_customer_id_fkey` REFERENCES tenants(id) ON DELETE SET NULL), run against local `DATABASE_URL` myself — confirmed live via `psql \d sales_offers`. Drizzle schema (`lib/db/src/schema/index.ts:3204-3205`) updated to match, comment corrected.
  Widened scope beyond the 3 consumers named in the issue body: a full audit (both real INSERT sites into `sales_offers` — `persistSalesOfferCandidates()` and `msp-marketplace-purchase.ts` — both write `tenants.id`) turned up **5 more** consumers that had independently bridged/filtered `salesOffersTable.customerId` as a `users.id`, the same wrong premise the stale FK encouraged: `msp-executive-data.ts` (`gatherExecutiveBook()` — also separately filed as **#2722**, commented there noting the fix), `msp-customer-timeline.ts`, `portal-customer-search.ts`, `portal-customer-timeline.ts` (this bridge was introduced by #2499's own fix, which correctly bridged documents but wrongly applied the same bridge to offers), and `admin-active-directory.ts`'s user hard-delete audit census (row removed — a single user's hard delete no longer nulls a tenant-scoped column; a stray comment in `dashboard-resolvers.ts` calling the column "user-keyed" was also corrected to match its already-correct code). All fixed in the same commit rather than left as a patched-one-call-site FK.
  Docs: annotated `docs/offers-and-sow-acceptance-msp-console-contract-pack.md` §6b and `docs/msp-executive-contract-pack.md` §4 noting the fix, without rewriting the historical bug narrative.
  Verification: `npx tsc --noEmit` clean on `lib/db` (0 errors) and on every one of the 6 touched `artifacts/api-server` files (76 pre-existing errors remain, all in unrelated `*.test.ts` fixtures — confirmed by file-name grep, none touch a file this build modified). Live DB: FK confirmed repointed via `psql \d sales_offers`; a real insert against `customer_id=1` (a live `tenants.id`) succeeds in a rolled-back transaction. `pnpm`/worktree deps were NOT degraded this session — tsc ran directly, no alternate-verification fallback needed.
  Commit `9ccb47bc4` (rebased onto `origin/main`, pushed as `b1462a8ec`). `verify-branch-merged.mjs`: OK. `git status --porcelain`: clean.
  Findings filed/commented: **#2722** commented (pre-existing sibling issue, same root cause — fixed here, not closed by me). No new issue filed — #2722 already covered the largest of the 5 additional consumers found; the other 4 were fixed directly rather than filed separately since they're the same root-cause fix, not independent findings.
