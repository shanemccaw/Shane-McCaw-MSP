# #4371 — Ladder + capability keys: Monitoring/Pack rungs, rename RetainerNoConsent → RetainerPending

- **Status:** ✅ DONE 2026-09-16T15:34:48Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** c456b4e14 (work), 623dbfb75 (IN FLIGHT bookend)
- **Migration:** `lib/db/migrations/manual/2026-09-16-rbac-retainer-pending-rename-4371.sql` — run against local DATABASE_URL (twice, idempotent); Replit/Staging line added to #1630
- **Findings filed:** #4384 (local dev DB customer RBAC seed wiped — pre-existing)

## Log
- 2026-09-16T15:19:09Z ⏳ IN FLIGHT — adding MonitoringPending/MonitoringConsented/PackPending/PackConsented rungs + ladder.* capability keys, renaming RetainerNoConsent → RetainerPending (retired alias), data migration, mechanical rename across artifacts/ and lib/.
- 2026-09-16T15:34:48Z ✅ DONE — LEGACY_ROLE_ORDER is now the 12 rungs #4370 fixed; new keys ladder.monitoring-pending/-consented, ladder.pack-pending/-consented; RetainerPending keeps ladder.retainer-no-consent; RETIRED_ROLE_VALUES.RetainerNoConsent → RetainerPending. Migration renames the platform role (same id, both systems), rewrites users/msp_invites.msp_role, renames the users_role_scope_check branch (new rungs NOT admitted — #4372), seeds the 4 new roles + catalog rows, recomputes all 12 ladder mappings, extends rbac_sync_user_roles, and gives the new rungs #3974's customer mapping decisions (marketplace allow; Pending rungs explicit deny team.manage/changes.approve). Every old-spelling reference in artifacts/ lib/ renamed; only the shim, its test and one history comment remain. Label/tone tables, audit actor mapping, admin client picker, OpenAPI enum + generated clients (hand-edited — orval regenerates ~150 unrelated drifted files) cover the new rungs. Verified: tsc clean for libs, api-server, msp-console, portal, admin-panel; lib/db legacy-ladder + capabilities tests 53/53; api-server rbac-ladder/rbac-capability/requireCapability-keys/direct-tenant-provisioning 40/40; live-DB purchase-account-flow + monitoring-onboarding-scan + rbac-ladder.live-db 56/56; end-to-end migration run inside a rolled-back transaction from a rebuilt pre-#4371 state (tenant-less RetainerNoConsent user → RetainerPending, rung synced in both systems, mappings + CHECK as designed). Not verifiable locally: check-rbac-parity and the customer-system halves of user-role-sync / retainer gating live tests — they fail before and after this change because the local customer RBAC seed is empty (filed #4384).
