# #2220 — API Explorers: add real mini rail panel (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 5c782f17c (pushed to main)

## Log
- 2026-09-02T15:10:00Z ⏳ IN FLIGHT — Real audit done: #2202 (the full API Explorers feature — ITokenBroker, GatedProfile, full explorer document) is NOT landed (its own BUILD dispatch was posted today and is separately in-flight/blocked-by #2204). #2204 (Settings/Accounts & Tiers store) is also NOT landed ("Not started"). #2209 (tool rail container) IS landed/closed — its established mini-panel precedent (LogPeek/SqlRunner/RepoHealth/JsonViewer/FileBrowser/GitMap, all single-purpose flyout columns in MainWindow.xaml/.cs, no generic tool-belt) is what this panel follows. Building the §6.3 mini panel now, standalone, against real local api-server routes (POST /api/auth/login, GET /api/healthz, /api/version, /api/status, etc.) for local mode and a real MSAL-free OAuth2 client_credentials call (HttpClient POST to login.microsoftonline.com) against the DEV app registration + GRAPH_TENANT_ID/GRAPH_CLIENT_ID/GRAPH_CLIENT_SECRET already in .env.local for Graph Read/Write modes. Graph Write LIVE path is implemented but real-audited as DRY RUN default, never fired live in this session (write-safety + production-boundary judgment — Graph Write against even the DEV app reg is a real tenant mutation, not something to trigger gratuitously during a build/verify pass).
- 2026-09-02T15:55:00Z ✅ DONE — Shipped the §6.3 mini panel: one column (ApiExplorerPanel, Grid.Column="12" after a rebase conflict with concurrently-landed #2216/#2218 which took columns 9-11), three modes via three wrench-menu entries (API Runner/Graph Read/Graph Write), auth row (token pill + Graph tenant label / local email+password fields since #2204's gated-profile store doesn't exist — honest not-built-yet message instead of a parallel store), endpoint search+list (real curated catalogs, grep-verified against artifacts/api-server routes for local, documented Graph v1.0 surface for Graph modes), selected-endpoint display, write-safety DRY RUN/LIVE segmented control (write mode only, resets to DRY RUN on every open/mode-switch, never persisted), send+copy, response panel (status chip with distinct success/denied/dry/error tones, ms/size meta, body, paste-into-chat via the shared AppendToComposer bridge). New file desktop/ShaneBuilder/Services/ApiExplorerService.cs holds all real execution: local POST /api/auth/login, Graph OAuth2 client_credentials POST to login.microsoftonline.com (no new MSAL.NET package — plain HttpClient, per the bandwidth-is-a-real-constraint rule), and generic HTTP execution with real success/denied/error tone classification. Verified live (PowerShell, not the app UI): Graph token acquisition against the real DEV app registration/testbed tenant succeeded; a subsequent unpermissioned Graph read correctly came back a real 403, exercising the "denied" tone path end-to-end. Local mode's HTTP path was exercised for the honest-failure case (dev api-server not running locally at verification time → clean connection-refused error surfaced in the response body, no fake success). `dotnet build` clean (0 warnings/errors) both before and after resolving a real rebase conflict against #2216 (PowerShell/Terminal mini panels) and #2218 (Git Doctor mini panel), which landed on main concurrently and took the column indices this session originally used. Branch verified merged into origin/main via scripts/dev-server/verify-branch-merged.mjs; working tree clean.

No new GitHub issues filed — no findings surfaced beyond the already-known #2202/#2204 dependency gaps, which are pre-existing open issues, not new discoveries.
