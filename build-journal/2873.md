# #2873 — sharepoint-admin transport rows: same is_collectable denominator-inflation gap as #2841

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:00:00Z
- **Scope:** platform

## Log
- 2026-09-04T00:00:00Z — Started. Confirmed via DB: 23 `sharepoint-admin` rows, all `is_collectable=true`, all naming `Get-Pnp*`/`Get-MgAdminSharepointSetting`/`Get-CompareParameters` cmdlets — zero of which exist in `PS_CATALOG_BY_CMDLET` (config-snapshot-collector.ts) or its registry mirror `PS_CATALOG_CMDLETS` (build-snapshot-registry.mjs).
- Went further than #2841's "just add catalog entries" framing: checked `services/ps-execution/` directly. The container has **no PnP.PowerShell module baked into the Docker image**, **no "sharepoint" session type** in `child-worker.ps1` (only `exchange`/`compliance`/`teams` exist), and **zero rows** in `ps_capability_survey_results` for any `Get-Pnp*`/SPO cmdlet — i.e. no live-proven evidence any of these 23 cmdlets even work app-only in this container. Per #1961's own stated discipline ("proven-OK entries only, real DEV deployment + health check before use"), adding unfiltered catalog entries now would be fabricating "proven" status with zero survey evidence — the opposite of what that discipline exists to prevent.
- Applying the issue's own stated fallback: gate the 23 `sharepoint-admin` rows `is_collectable=false` with an honest `no_executor` reason, mirroring #2841's fix exactly but for this transport. Filing the real infra gap (module + session type + survey) as its own separate issue rather than papering over it.
