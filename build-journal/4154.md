# Issue #4154

- **Status:** ✅ DONE 2026-09-15T00:15:00Z
- **Scope:** platform

## Log
- 2026-09-15T00:08:15Z — Starting: remove stale `neon` MCP server entry from `.mcp.json`, leave `shane-msp` untouched.
- 2026-09-15T00:15:00Z — ✅ DONE — removed `neon` entry from `.mcp.json` (commit 453c227a9), `shane-msp` untouched. Grepped repo (`git grep -n "mcp.neon.tech"` / `'"neon"'`, excluding node_modules) — only remaining reference is the explanatory prose note in `CLAUDE.md:923` describing why the Neon MCP server is stale; no other config or code references it. Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
