# #2640 — Team Management and Invitations: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T05:05:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 60a959cb5 (bookend IN FLIGHT), 3382dbdc9 (real work)

## Log
- 2026-09-12T04:45:00Z ⏳ IN FLIGHT — Read design (`Design/MSP_Console/design_handoff_msp_console/Team.dc.html`,
  README screen 17), the real backend (`artifacts/api-server/src/routes/msp-team.ts`, 10 routes)
  and its contract pack (`docs/msp-console/team-management-and-invitations-msp-console-contract-pack.md`).
  Page id `"team"` already registered in `nav.ts`'s tree model (Access & identity group); the
  shell's `moduleFor()` in `ConsoleShell.tsx` has no case for it yet. Following the `DataRights`
  module's established pattern (api hook file + module component) to wire: roster list, invite
  drawer, per-member manage drawer (suspend/activate, MFA enforcement toggle, unlock, reset
  password, temp password, reset MFA, sign out sessions, emergency bypass).
- 2026-09-12T05:05:00Z ✅ DONE — Shipped `artifacts/msp-console/src/api/team-api.ts` (roster query +
  9 mutations against the real `msp-team.ts` routes) and
  `artifacts/msp-console/src/console/modules/Team.tsx` (roster table, counters, invite drawer,
  per-member manage drawer with shown-once secrets for temp password / emergency bypass code),
  registered the `"team"` page id into `ConsoleShell.tsx`'s `moduleFor()`, and added the missing
  Lucide icons (`user-plus`, `shield-off`, `user-minus`, `mail`, `key`, `lock-open`, `siren`,
  `smartphone`, `user-check`) the design needs. No fixture data — every value comes from the real
  backend documented in the contract pack. `pnpm run typecheck` and a production `pnpm run build`
  (with `PORT` set) both pass clean. Confirmed real tenant/user rows exist locally (tenant id 1,
  9 real `users` rows) to exercise `GET /api/msp/customers/1/team` against, but the dev api-server
  was not running locally (connection refused on `:8080`) so the route was not hit live
  end-to-end — honestly noted rather than claimed. No new findings to file; the contract pack's
  already-documented gaps (no invite-time role picker, no manager-chain surface on this side, the
  mixed bare-string/`apiError()` envelope) are pre-existing, already-tracked non-decisions for
  this build, not new discoveries. `git status --porcelain` clean; branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` (OK, `f793ca29f`).
