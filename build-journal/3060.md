# #3060 — build-journal DONE bookend can land only on the agent branch while the real code merges to main by a different path — #2863 confirmed example

- **Status:** ✅ DONE 2026-09-12T08:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12

## Log
- 2026-09-12T08:10:00Z ⏳ IN FLIGHT — verifying the #2863 false-DONE claim, then auditing all
  build-journal/*.md files still IN FLIGHT against their tracked issue's real state to find
  other cases of this same shape.
- 2026-09-12T08:35:00Z ✅ DONE — Confirmed #2863's claim is false: `1fad287d0` (the DONE-bookend
  commit on the now-deleted `agent/2863-q1623` / `rescued/*` branches) never became an ancestor
  of `origin/main` and no longer exists on any remote ref. The real fix landed under a different
  hash, `38b2b313e1864ddb2dca1b2acb9c5677a7e4d634` ("Dedupe sku_price_reference lookup in
  licensing.wasteEstimateBreakdown", 2026-09-04T12:16:17-04:00), which IS an ancestor of
  `origin/main`, and `cost-engine.ts`'s `priceMap` change is confirmed live. Posted a correction
  comment on #2863 recording the real landing commit, per the phantom-hash convention (never
  edit the original bookend): https://github.com/shanemccaw/Shane-McCaw-MSP/issues/2863#issuecomment-5649054083

  Audited all 1701 build-journal/*.md files on `origin/main` (via `git grep` on each file's
  final `**Status:**` line, cross-referenced against real `gh issue view` state) for the same
  "DONE claimed via one path, real code merged via a different path" shape. Found 21 files whose
  final header line reads IN FLIGHT; 18 are genuinely still open/blocked/awaiting-Shane's-decision
  (label and issue state agree) and are not bugs. Three are a real, distinct-but-related bug:
  #3121, #3189, #1758 are each CLOSED + labeled `complete`, and each bookend's own `## Log`
  section already contains a real, verified `✅ DONE` entry with real commit hashes — but the
  top-of-file header `**Status:**` line was never flipped from IN FLIGHT to match. Filed as
  #3860 (parented to #3720, this build's own Feature), labeled `bug`, board status set to
  "AI Batter Up". No `2863`-style over-reporting cases found elsewhere in the corpus — #2863
  itself remains the only confirmed phantom-hash case.

  No code changed (this was an audit + correction-comment + finding-filing task, not a product
  code fix). `git status --porcelain` clean; `verify-branch-merged.mjs` confirms this branch's
  HEAD is an ancestor of `origin/main`.

**Findings filed:** #3860
