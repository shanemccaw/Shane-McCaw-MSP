# #4149 — WhatsRemainingPanel: read-only "what's left" document tab

- **Status:** ✅ DONE 2026-09-15T00:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 1207fa5cc (feature), 629ead9fb (IN FLIGHT bookend), on `main`

## Log
- 2026-09-15T00:05:00Z ⏳ IN FLIGHT — read #4149's body/comment, confirmed #4146 landed
  (5c26d69bc) so its clipboard-copy logic will be extracted into a shared helper rather
  than re-derived; read EpicResolver.ResolveTopEpicsAsync, AiBatterUpPanel's
  GroupByEpic/BuildEpicGroupHeader, MainWindow.BatterUpTabs.cs's document-tab recipe, and
  GitHubIssueTimeSeriesService.GetActiveMilestoneSeriesAsync/GetMilestoneSeriesAsync/
  BuildSeries to find the real "remaining" scope (active milestone, CountsAsRealWork,
  currently open). About to build WhatsRemainingService + WhatsRemainingPanel + a new
  title-bar button reusing the open-or-focus document-tab helper.
- 2026-09-15T00:52:00Z ✅ DONE — shipped `WhatsRemainingService` (mirror-only: resolves the
  active milestone via `GitHubIssueTimeSeriesService.ResolveActiveMilestoneAsync`, scopes by
  that milestone + `GitBoardIssueFilters.CountsAsRealWork` + currently-open — the exact same
  scope Home's own "remaining" burndown number already uses, so this panel's count matches
  it by construction rather than by re-deriving a second definition), `WhatsRemainingPanel`
  (plain read-only rows — number/title/real board Status — grouped by
  `EpicResolver.ResolveTopEpicsAsync`'s resolved top Epic with a real "No Epic" fallback
  group), and a new `EpicGroupHeaderHelper` extracted from #4146's AiBatterUpPanel-only
  copy-button code so both panels share the identical "Git #n" clipboard format
  (AiBatterUpPanel's own `BuildEpicGroupHeader` now calls the shared helper too). New
  title-bar button (📋 "What's Remaining") opens/focuses a persistent singleton document tab
  via the existing `OpenBatterUpTab`/`OpenAiBatterUpTab` open-or-focus recipe
  (`MainWindow.BatterUpTabs.cs`).
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0
  warnings/errors), rebuilt again after each rebase onto a fast-moving `main`. Cross-checked
  the real scope definition directly against BuildConsole's own local Postgres mirror
  (`BUILD_DATABASE_URL`): active milestone resolves to #5 "v1.1 - Monitoring & Launch
  Control" (2358 real open+closed issues, the largest open milestone); a SQL replica of
  `CountsAsRealWork` + the milestone/open filter over `bt_issue_mirror` returns 136 real
  rows right now (the issue body's "73" was a snapshot from filing time — scope moves as
  concurrent builds land/close issues; this panel reads live, so it reflects current state,
  not that snapshot). Confirmed `board_status_name` carries real values (Backlog/AI Batter
  Up/Batter Up/Verifying/Ask Shane/Park/Crashed/Architecting) for a real subset of rows —
  rendered as "—" where the mirror hasn't captured one yet, never a guessed default.
  `desktop/BuildConsole` is out of scope for test manifests per CLAUDE.md, so this build's
  verification is the clean build + the live-data cross-check above, not a manifest.
  No live GitHub call anywhere in the new read path (mirror-only, matching every other
  Home-dashboard-scope consumer) — `EpicResolver`'s optional `gh` fallback is still wired
  through (same convention Batter Up/AI Batter Up use) for its own narrow sync-gap case.
  Merged to `origin/main` at 1207fa5cc after 4 fetch/rebase/push retries (main moved under
  concurrent build load each time) — verified via `verify-branch-merged.mjs`. Working tree
  clean. No findings filed — no genuine gap or bug surfaced during this build.
