# #4114 — ShaneBot: rebuild the chat shell + Active Card UI against the real updated design

- **Status:** ✅ DONE 2026-09-14T20:57:41Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 33f62bd2b (IN FLIGHT bookend), 414c67ae9 (real work — merged into origin/main)

## Log
- 2026-09-14T20:56:49Z ⏳ IN FLIGHT — visual rebuild of `artifacts/portal/src/pages/support.tsx`, `components/support-chat/ChatBubble.tsx`/`SuggestedReplies.tsx`/`TypingIndicator.tsx`, and the four Active Card renderers (`InvoiceCard`/`SubscriptionCard`/`ScoreCard`/`DataAnswerCard`) against `Design/portal/design_handoff_full_site/screens/ShaneBot.dc.html`, per #4114 (part of Feature #1616). Behavior/wire contract untouched — same `/api/msp/support/chat` + `/api/msp/support/escalate` calls, same `chat-content-blocks.ts` shapes, only the four real `BotCardType`s rendered (per `docs/portal/shanebot-contract-pack.md` — the design file's much larger "full site" demo fixture content for other, unbuilt pillars/cards was deliberately not carried over).
- 2026-09-14T20:57:41Z ✅ DONE — `414c67ae9` pushed and rebased onto `origin/main` (`verify-branch-merged.mjs` confirms ancestor). `pnpm run typecheck` and `pnpm run build` both pass clean in `artifacts/portal`. No new API/UI wire contract added, so `test-manifests/chat/support-chat-active-cards.json` (existing coverage for this route) needed no update — its testids (`support-chat-input`/`-send`/`-escalate`/`-latest-reply`, `active-card-invoice`/`-subscription`/`-score`/`-data-answer`) are all preserved unchanged. Honest gap: no live browser/`shaneapp://runTest` pass in this session — that protocol isn't reachable from this tool set here; verification is typecheck+build only. `git status --porcelain` clean.
