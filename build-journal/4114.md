# #4114 — ShaneBot: rebuild the chat shell + Active Card UI against the real updated design

- **Status:** ⏳ IN FLIGHT 2026-09-14T20:56:49Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-14T20:56:49Z ⏳ IN FLIGHT — visual rebuild of `artifacts/portal/src/pages/support.tsx`, `components/support-chat/ChatBubble.tsx`/`SuggestedReplies.tsx`/`TypingIndicator.tsx`, and the four Active Card renderers (`InvoiceCard`/`SubscriptionCard`/`ScoreCard`/`DataAnswerCard`) against `Design/portal/design_handoff_full_site/screens/ShaneBot.dc.html`, per #4114 (part of Feature #1616). Behavior/wire contract untouched — same `/api/msp/support/chat` + `/api/msp/support/escalate` calls, same `chat-content-blocks.ts` shapes, only the four real `BotCardType`s rendered (per `docs/portal/shanebot-contract-pack.md` — the design file's much larger "full site" demo fixture content for other, unbuilt pillars/cards was deliberately not carried over). `pnpm run typecheck` and `pnpm run build` both pass clean in `artifacts/portal`.
