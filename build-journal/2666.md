# #2666 — .replit workflow still registers retired artifacts/msp-portal, not artifacts/portal

- **Status:** ⏳ IN FLIGHT 2026-09-03T17:38:49Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** TBD

## Log
- 2026-09-03T17:38:49Z ⏳ IN FLIGHT — Confirmed real state: `artifacts/msp-portal` directory no
  longer exists (deleted, archived at tag `portal-archive-2026-08-29`), `pnpm-workspace.yaml`
  has no `@workspace/msp-portal` package (real package under `artifacts/portal/package.json` is
  `@workspace/portal`). Local dev server already bypasses `.replit` via
  `scripts/dev-server/` (`service-targeting.mjs` already has `{ name: "portal", artifact:
  "artifacts/portal/" }`), so `.replit` only governs the Replit-hosted
  environment (Staging/deploy), not local dev — but it is genuinely stale there: the
  `artifacts/msp-portal: web` workflow block and its reference in the "Start Projects"
  composite workflow both still point at the retired app. Fixing per issue body: rename to
  `artifacts/portal: web`, `artifactId = "artifacts/portal"`, `pnpm --filter @workspace/portal
  run dev`, `cwd = "./artifacts/portal"`, and update the composite workflow's task reference.
