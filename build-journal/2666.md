# #2666 — .replit workflow still registers retired artifacts/msp-portal, not artifacts/portal

- **Status:** ✅ DONE 2026-09-03T17:41:55Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** ffe8d2fba (fix), 1a636a7c1 (bookend IN FLIGHT), rebased onto main from
  agent/2666-q1402

## Log
- 2026-09-03T17:38:49Z ⏳ IN FLIGHT — Confirmed real state: `artifacts/msp-portal` directory no
  longer exists (deleted, archived at tag `portal-archive-2026-08-29`), `pnpm-workspace.yaml`
  has no `@workspace/msp-portal` package (real package under `artifacts/portal/package.json` is
  `@workspace/portal`). Local dev server already bypasses `.replit` via
  `scripts/dev-server/` (`service-targeting.mjs` already has `{ name: "portal", artifact:
  "artifacts/portal/" }`), so `.replit` only governs the Replit-hosted
  environment (Staging/deploy), not local dev — but it is genuinely stale there: the
  `artifacts/msp-portal: web` workflow block and its reference in the "Start Projects"
  composite workflow both still point at the retired app. Fixing per issue body: rename to
  `artifacts/portal: web`, `artifactId = "artifacts/portal"`, `pnpm --filter @workspace/portal
  run dev`, `cwd = "./artifacts/portal"`, and update the composite workflow's task reference.
- 2026-09-03T17:41:55Z ✅ DONE — Renamed the workflow block (`artifacts/msp-portal: web` →
  `artifacts/portal: web`), fixed `artifactId`, `args`, `cwd`, and the "Start Projects"
  composite workflow's task reference. Verified `pnpm --filter @workspace/portal run dev`
  resolves (real `dev`/`predev` scripts exist, vite 7.3.3 starts). Confirmed no remaining
  `msp-portal` references anywhere in `.replit` (`grep -n "msp-portal" .replit` → no matches).
  Port 26059 stays correct — `artifacts/portal/vite.config.ts` reads `PORT` from env, still
  set by the workflow's `env` block. Merged `agent/2666-q1402` onto `origin/main` (rebase +
  push, commit `ffe8d2fba`); `verify-branch-merged.mjs` confirms merged. `git status
  --porcelain` clean. No fixture/hardcoded data involved — pure infra config fix, no code
  behavior change. This build session ran on a BuildConsole-provisioned worktree
  (`agent/2666-q1402`), not directly on `main`, per the mandatory worktree isolation rule.
