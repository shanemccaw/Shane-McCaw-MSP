# #2321 — New Chat opens a disclosure explaining the anchor, listing active features with their epic and state

- **Status:** ✅ DONE 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** d8314639b (work), 8b0abf62e (merge into main)

## Log
- 2026-09-02T19:30:00Z ⏳ IN FLIGHT — depends on #2319 (FeatureNumber resolution) and #2320 (New Chat
  button), both closed/merged. Building the real disclosure dialog `OpenNewChatFlow()` shows before
  navigating to claude.ai/new: lists real open "Feature:" issues with their real parent Epic and
  real state (in-flight/blocked/complete/open), backed by a new `GitIssuesService.GetActiveFeaturesAsync`
  (same real `gh`+GraphQL parent-batch pattern `GetRecentOpenIssuesAsync` already uses). Out of scope
  for this issue: the "no feature yet — decide later" option (#2322) and writing the anchor into the
  new tab's subtitle (#2323) — both separate open sub-issues of #2318.
- 2026-09-02T20:05:00Z ✅ DONE — Added `GitIssuesService.GetActiveFeaturesAsync` (real
  `gh issue list --search "Feature in:title"` filtered client-side to the real `Feature:`/`feature:`
  title prefix, same false-positive guard `GitMapService.GetOpenEpicsAsync` already uses for `Epic:`;
  batched parent lookup reused from `ResolveParentsAsync` — 2 real `gh`/GraphQL calls total, never
  N+1 per epic). Manually verified against live GitHub: raw search returned 103 rows, 77 real
  `Feature:`-prefixed issues survived the client-side filter (e.g. #2318, #2196, #2263, #1493 with
  its real `complete` label) — confirms the filter and label-based state mapping are real, not
  guessed. Built `NewChatAnchorDialog.xaml`/`.xaml.cs` — real custom-chrome dialog (same
  WindowChromeHelper/#2147-token pattern as `AppDialog`) that loads and lists every active Feature
  with its real parent Epic and real state (Complete/Blocked/In flight/Open, off the feature's own
  labels). Wired `MainWindow.OpenNewChatFlow()` (both `BtnNewChat_Click` and the wrench menu's
  "Start a new chat" route through it) to show this dialog first; only an explicit row pick
  navigates `ClaudeWebView` to claude.ai/new (Cancel/Escape/close is a real no-op — no navigation).
  The picked anchor is logged (`ConsoleOutputSink`) and toasted for now — writing it into a real
  anchored tab's subtitle needs #2323 (today's app has exactly one persistent chat tab, re-navigated
  in place; #2323's own remaining work is making that tab creation real). `dotnet build
  desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors, twice (before and after the
  final commit). No test manifest — desktop/ShaneBuilder is Shane's own WPF tool, same
  test-manifest exclusion CLAUDE.md already carves out for desktop/BuildConsole. Merged
  `agent/2321-q1260` into `main` and pushed (`05784b6aa..8b0abf62e`);
  `verify-branch-merged.mjs agent/2321-q1260` confirms merged. `git status --porcelain` clean.
  No findings filed this session — no genuine gap/bug discovered outside this issue's own scope.
