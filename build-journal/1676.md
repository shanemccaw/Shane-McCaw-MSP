# #1676 — Webhooks: rotate secret, edit, delete — rebuild in artifacts/portal

- **Status:** 🛑 BLOCKED 2026-09-03T23:15:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** none — no code changed

## Log
- 2026-09-03T23:15:00Z 🛑 BLOCKED — Pulled #1676's full body + comments before touching
  code, per standing rule. The issue's own owner comment (posted 2026-09-03T19:11:27Z,
  two minutes after this dispatch went out) already says this build was dispatched
  prematurely: "Design/portal/ only contains Shell.dc.html, not this Feature's export."
  Verified against the live repo: `Design/portal/` holds only
  `design_handoff_ui_shell/Shell.dc.html` (the shared shell scaffolding) — there is no
  `Webhooks.dc.html` or any webhooks-specific `.dc.html` export anywhere under `Design/`.
  Per CLAUDE.md, a page has no design until its own `.dc.html` exists in `Design/portal/`,
  and portal-v2 is explicitly not a fallback target — so there is nothing to wire yet.
  The real blocker is #1626 ("Webhooks: Claude Design export into Design/portal/"),
  which is still OPEN and is explicitly "Shane exports this himself — no agent action."
  Confirmed the `blocked_by` edge from #1676 → #1626 is already wired and accurate
  (checked via `gh api .../1676/dependencies/blocked_by`); no edge to add or clean up.
  Reconciled labels: removed `in-flight` (this session is not actively working it),
  left `blocked` in place. Did not touch #1673 (closed, not a real blocker). No code
  was written or changed. Working tree confirmed clean (`git status --porcelain` empty)
  aside from this bookend file itself.
