# #3894 — Testimonials: real stub submission UI in artifacts/portal

- **Status:** ✅ DONE 2026-09-15T03:59:28Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** e495dd26b (real submission UI), 9519dc33b (main after push/rebase)

## Log
- 2026-09-15T03:53:50Z ⏳ IN FLIGHT — Read #3894 + docs/portal (Design/portal/design_handoff_full_site/docs) testimonials-contract-pack.md (#3893). Building a real customer-facing submission surface in artifacts/portal wired to the live `/api/portal/testimonials*` endpoints (portal-testimonials.ts) — a prompt entry point (using the real `/prompt-due` cadence check) plus a submission form matching the real `customer_testimonials` schema fields (`body`, `kind`, `permissionToPublish` — no `rating` column exists, contra the issue's loose paraphrase; contract pack confirms the real fields). Labeling new components "stub UI pending Design review" per the issue's own instruction. No fixture data.
- 2026-09-15T03:59:28Z ✅ DONE — Shipped (commit e495dd26b, merged to main as 9519dc33b):
  - `src/lib/testimonials-api.ts` — real react-query hooks (`useTestimonialPromptDue`, `useTestimonialHistory`, `useSubmitTestimonial`) wired to `GET /api/portal/testimonials/prompt-due`, `GET /api/portal/testimonials`, `POST /api/portal/testimonials`. No fixture data.
  - `src/components/testimonials/TestimonialPromptBanner.tsx` — frame-level entry point mounted in `PortalShell`, surfaces only when the real `/prompt-due` check says `due: true`; links to the new page. Session-only dismiss (no snooze endpoint exists — the real due-check itself resets on submission).
  - `src/pages/testimonials.tsx` — the real submission form (kind picker: testimonial/feedback/suggestion, free-text body up to 4000 chars, permission-to-publish checkbox — matches the real `customer_testimonials` schema; there is no `rating` column) plus the requesting login's own submission history, using this app's existing shadcn/ui "new-york" Card/Button/Textarea/Checkbox/Badge components.
  - Route `/testimonials` added to `App.tsx`; breadcrumb updated in `PortalShell.tsx`.
  - All new components carry an explicit "STUB UI PENDING DESIGN REVIEW" header comment per the issue's instruction.
  - Verified: `pnpm run typecheck` clean, `pnpm run build` succeeds (`artifacts/portal`).
  - Branch ancestry verified (`verify-branch-merged.mjs` OK), working tree clean (`git status --porcelain` empty) before this DONE line was written.
  - No findings filed this session — no genuine bug/gap discovered beyond what #3893's own contract pack already filed (#4031, #4032).
