# #4215 — Delete orphaned test-1314-% services rows + beforeAll guard

- **Status:** ✅ DONE 2026-09-15T12:59:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 084300a89 (merged to `origin/main`)

## Log
- 2026-09-15T12:53:00Z ⏳ IN FLIGHT — Verified the 6 `test-1314-79578fc0-%` rows in `services` still exist, still carry `tier IS NULL`, and still have zero matching users/tenants (re-queried live, matches issue snapshot). Proceeding to delete them and add a `beforeAll` cleanup guard to `monitoring-onboarding-scan.test.ts`.
- 2026-09-15T12:59:00Z ✅ DONE — Deleted the 6 orphaned rows (`DELETE 6`, ids 485-490). Added a `beforeAll` in `monitoring-onboarding-scan.test.ts` sweeping any prior `test-1314-%` residue (services/users/tenants/msp_diagnostic_runs) before the run inserts its own rows. Ran the file directly against local Postgres: 6/6 tests pass. Re-checked `services` for `test-1314-%` residue after the run: 0 rows. `tsc --noEmit` clean for the touched file. Working tree clean. Commit `084300a89` rebased onto and pushed to `origin/main`; `verify-branch-merged.mjs` confirms ancestor. No findings to file this session.
