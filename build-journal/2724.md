# #2724 — autoPublish request param is a dead field on POST /api/msp/documents

- **Status:** ✅ DONE 2026-09-04T00:35:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 64e93e5fed2452f7785920e18e25d9eb2e200614 (merged to main)

## Log
- 2026-09-04T00:20:00Z ⏳ IN FLIGHT — wiring `autoPublish` into `handleDocPublish` (doc-pipeline-nodes.ts) so it actually gates the publish step, and letting the versions route accept an explicit override instead of hardcoding `false`.
- 2026-09-04T00:35:00Z ✅ DONE — `handleDocPublish` (artifacts/api-server/src/lib/doc-pipeline-nodes.ts) now checks `ctx.input["autoPublish"] === false` and skips the actual publish write, leaving the document/version `pipelineStatus` at `version_registered` (an existing enum value — no schema change needed) instead of unconditionally publishing. `POST /api/msp/documents/:documentId/versions` (msp-documents.ts) now reads an `autoPublish` body param (default `false`, preserving prior behavior) instead of hardcoding `false` into the workflow input, so a caller can opt a new version into auto-publish. Verified: `npx tsc --noEmit -p artifacts/api-server` exits 0, clean. Merged to `main` at 64e93e5.
