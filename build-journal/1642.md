# #1642 — Microsoft Changes: regenerate the contract pack from the real, finished backend

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 5b213fa6d (pack, agent/1642-q818 → merged to origin/main); f36a59d80 (bookend IN FLIGHT)

Leaf issue under module epic #1494 (part of #1485). This is the clean-numbered
re-run of the #1494 contract pack: `docs/microsoft-changes-contract-pack.md` on main is
stale — written before #1534 (routing gate), #1536 (date quality) and #1537 (cloud
instance) landed. Treating the existing pack as prior art to be **replaced wholesale**,
not edited. Read-only on product code; the only file written is the pack itself.

## Log
- 2026-08-29 ⏳ IN FLIGHT — Pulling #1642/#1494/#1485/#1577, then re-extracting the wire
  contract from the CURRENT code: `portal-message-center.ts`, `m365-change-router.ts`
  (#1534 `decideRouting`, `m365_change_routings`, `POST /portal/change-control/:code/decline`),
  `m365-message-center-date-quality.ts` (#1536 `advisoryDateText`, `DATE_UNCLEAR`,
  `dateUnclearCount`), `m365-cloud-instance.ts` (#1537 `cloudInstances`, `?cloud=`,
  `cloudMode`). Live DB counts queried against local `DATABASE_URL`. Writing the pack wholesale.
- 2026-08-29 ✅ DONE — Replaced `docs/microsoft-changes-contract-pack.md` wholesale (~440 lines):
  five-surface map (customer route, AdminV2 interpretation/candidates, the portal decline endpoint,
  the routing engine, the sync writers); per-surface wire contracts re-cited to CURRENT file:line
  (both the `scoped:false` and full response shapes, `WirePost`+`advisoryDateText`/`dateConfidence`,
  the smaller `WireDateUnclearPost`, `WireAnalysis`); the #1534 routing gate as built
  (`decideRouting` truth table, intake/implementer derivation, decline→risk, idempotency); real
  enum unions incl. the four routing enums new since the old pack; the real 4-value cloud vocabulary
  and that #1537 filtering is admin-authoring-side, NOT a customer control; three-layer honest-empty
  contract; forbidden list swept from the real route headers (not the retired portal-v2 fixture);
  #1535 open question reframed now that intake is a first-class axis. Key honesty finding recorded
  in the pack: the interpret→resolve→route pipeline is fully built on main but currently DORMANT —
  live DB has 0 interpretations/resolutions/routings/auto-CRs (so every post's `analysis` is null
  today), against 1159 MC items and 1795 roadmap items; `advisory_date_text` populated on 549,
  date-unclear count 0. Read-only: only the pack + this bookend changed. Merged 5b213fa6d to
  origin/main; verified with verify-branch-merged.mjs (exit 0) before writing DONE.
