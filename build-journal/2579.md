# #2579 — Change Control: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:33:37Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** `37f7fcc86` (real work; rebased twice onto concurrent module Features, see log)

## Log
- 2026-09-12T03:25:35Z ⏳ IN FLIGHT — written after investigation + implementation
  were already done in-session (a real process miss: this should have been the
  first file this session touched, per CLAUDE.md's mandatory bookend step; it
  was not, and this line says so plainly rather than backdating it). By this
  point: confirmed #3677 (MSP Console Shell) is genuinely merged (`7888bf1d6`)
  and this worktree already has it; read the Change Control design
  (`Design/MSP_Console/design_handoff_msp_console/Change Control.dc.html` +
  README + `github.md`) and all 9 real backend route files it maps to; built
  the 7-tab module (`artifacts/msp-console/src/pages/change-control/`) wired
  against those real routes; extended `msp-changes.ts` (GET now carries each
  CR's `cr_approvals` ledger) and `msp-change-dependencies.ts` (new aggregate
  `GET /api/msp/change-control/dependencies` endpoint) to serve what the
  register/dependencies tabs need, per the HARD RULE — no fixture data
  anywhere. Verified against real local DB data (msp_id=1, one real CR with 2
  pending customer approval stages; everything else in this area genuinely
  empty, so most tabs correctly render their designed Empty state). tsc clean
  on both packages, production vite build clean, `npm test` green (16/16).

- 2026-09-12T03:33:37Z ✅ DONE — pushed to `main` at `37f7fcc86`. Real collision
  handled along the way: 4 other module Features (#2582, #2588, #2597, #2612,
  #2630) landed on `main` while this was in flight, all touching the same
  `ConsoleShell.tsx` `moduleFor` switch and `icons.tsx` icon registry — the
  exact shared-mount-point collision anticipated in this issue's own dispatch.
  Rebased twice, resolving both conflicts by hand each time: dropped my own
  first-draft `src/console/moduleRegistry.tsx` in favour of the inline
  `moduleFor` convention the other Features had already established (matching
  existing convention over introducing a second competing pattern), re-ran
  tsc/build/test after each rebase before pushing. Filed one real finding as
  #3761 (parented under #1681, board status AI Batter Up): the design's own
  mock shows direct Approve/Reject buttons on a pending MSP-role approval
  stage, but no backend route lets an operator record that decision except
  through a CAB agenda item — wired the drawer to point at the CAB tab instead
  of a nonexistent route, left the actual model decision to Shane. Branch
  merge confirmed via `verify-branch-merged.mjs` (exit 0). Working tree clean.
  Not live-UI-tested via `shaneapp://runTest` — msp-console isn't in this
  repo's mandatory test-manifest scope, and a live run would hit the deployed
  build, not this worktree. Completion comment posted on #2579; labels synced
  (`complete`).
