# #1562 — Security Plan: Versioned and sealed — the RBD pattern one level up

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 6c9a23b18 (IN FLIGHT bookend) + f773f147b (deliverable), both on origin/main

## Log
- 2026-08-31 ⏳ IN FLIGHT — #1562 settles the Security Plan's versioning shape as
  "the RBD pattern one level up": a version is assembled, frozen and signed at a
  point in time; prior versions stay intact; the LIVE view sits alongside, showing
  drift from the LAST SIGNED version. Investigation found #1561's own session
  already built the assembly (`security-plan-assembly.ts`) and the RBD-shaped
  seal/supersession chain (`security-plan-versioning.ts`, `msp_security_plan_versions`,
  mirroring `rbd-versioning.ts`/`msp_rbd_versions` per #1508) as part of its own build,
  including the #1563 scope-not-outcome guarantee and the #1565 filter footprint. The
  one piece of #1562's own settled text NOT yet built: the live-vs-last-signed drift
  comparison. Building that now — no second sealing/versioning stack, reusing
  `msp_security_plan_versions` and `getCurrentSecurityPlanVersion`'s sibling.
- 2026-08-31 ✅ DONE — Shipped in `f773f147b` on origin/main (real hash, confirmed
  ancestor of origin/main via `verify-branch-merged.mjs`). Delivered the one missing
  piece of #1562's settled text — no schema/migration change, pure logic + one route:
  • `getLastSignedSecurityPlanVersion(mspId, customerId)` (`security-plan-versioning.ts`)
    — the LAST SIGNED row, which can differ from `getCurrentSecurityPlanVersion` (the
    current row can itself be unsigned after a later unsigned seal).
  • `computeSecurityPlanDrift` (`security-plan-drift.ts`) — PURE per-module diff
    (added/removed/changed by item id) between the live honest assembly and the last
    signed snapshot's `content`; `getSecurityPlanDrift` wires it to real reads. The
    live side is always the honest (unscoped) view regardless of caller scope params —
    drift answers "what changed since the signature," not "what changed in a slice."
  • `GET /api/msp/security-plan/:customerId/drift` (`msp-security-plan.ts`) — the live
    document + `SecurityPlanDrift` (hasLastSignedVersion, added/removed/changed per
    module, totals). SCOPE STOP unchanged from #1561: wire contract only, no
    `artifacts/portal` page (no `Design/portal` export exists).
  • `SecurityPlanDrift` / `SecurityPlanModuleDrift` / `SecurityPlanDriftChangedItem` /
    `SecurityPlanDriftItemState` types (`lib/db/src/schema/msp.ts`) — pure TS shapes,
    no new table or column, so no migration was needed for this piece.
  • `security-plan-drift.test.ts` (registered in `vitest.config.ts`) — 7 pure tests:
    no-baseline, added, removed, changed, unchanged-produces-nothing, whole-module-
    removed. Verified: vitest 12/12 pass across both security-plan test files
    (7 new + 5 pre-existing #1563 assembly tests, run together after rebase).
  • tsc (override resolving `@workspace/db` to this worktree's own `lib/db`, since the
    real `node_modules/@workspace/db` junction points at the MAIN checkout and would
    otherwise hide my new types): ZERO errors in any file this session touched; the
    ~73 program-wide errors are pre-existing/unrelated (onedrive/sharepoint-sharing
    tests, ai-billing.ts, etc. — #1765 etc.), none in `security-plan-*` files.
  • LIVE-DB SMOKE (real local Postgres, testbed tenant `customerId=1`/`mspId=1`,
    `c4c814d4-3afe-441e-9145-62461d0a4fd3`): assembled → sealed → signed a version;
    confirmed `hasLastSignedVersion=false` before any signature and `0/0/0` drift
    immediately after signing; mutated one real `remediation_tracker_steps` row
    (`not_started`→`deferred`) for that tenant; confirmed drift correctly reported
    exactly that item as `changed` (`remediation-225`, old/new state shown); confirmed
    `getLastSignedSecurityPlanVersion` still pointed at the same signed version
    (unaffected by the live mutation); reverted the row and re-confirmed drift returned
    to `0/0/0`. Deleted the smoke-sealed `msp_security_plan_versions` row afterward —
    local DB left exactly as found (0 rows in that table, as before the session).
  No findings filed this session — the gap closed was #1562's own scope, not an
  out-of-scope discovery. Working tree clean (`git status --porcelain` empty) before
  this DONE line was written.
