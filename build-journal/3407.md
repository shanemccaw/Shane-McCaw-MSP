# #3407 — MSP Reports: silent email-delivery failure is invisible in the Admin Panel UI

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — Investigated `generate_report`'s delivery branch
  (`artifacts/api-server/src/lib/report-nodes.ts:306-352`) and the one live consumer,
  `artifacts/admin-panel/src/pages/MspReports.tsx`. Confirmed the API already returns
  `errorMessage` unconditionally on every run row (`msp-reports.ts` list/get queries select
  it regardless of status) — this is purely a UI-rendering gap, not a missing backend field.
  About to update `StatusBadge` + the run-row actions cell so a `"generated"`/`"delivered"`
  row with a non-null `errorMessage` surfaces a visible warning instead of rendering as a
  plain success badge.
