# #3407 — MSP Reports: silent email-delivery failure is invisible in the Admin Panel UI

- **Status:** ✅ DONE 2026-09-11T00:37:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 165c5cd36 (rebased/pushed to main as 42d2e53b4)

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — Investigated `generate_report`'s delivery branch
  (`artifacts/api-server/src/lib/report-nodes.ts:306-352`) and the one live consumer,
  `artifacts/admin-panel/src/pages/MspReports.tsx`. Confirmed the API already returns
  `errorMessage` unconditionally on every run row (`msp-reports.ts` list/get queries select
  it regardless of status) — this is purely a UI-rendering gap, not a missing backend field.
  About to update `StatusBadge` + the run-row actions cell so a `"generated"`/`"delivered"`
  row with a non-null `errorMessage` surfaces a visible warning instead of rendering as a
  plain success badge.
- 2026-09-11T00:37:00Z ✅ DONE — Updated `StatusBadge` (`MspReports.tsx`) to check for a
  non-null `errorMessage` on a `"generated"`/`"delivered"` row first and render an amber
  warning badge ("Generated/Delivered — delivery issue", tooltip = real error text) instead
  of the plain green success badge; added the same inline warning text next to the PDF
  download button in the run-row actions cell, matching the existing failed-row pattern.
  No backend/schema change needed — `msp-reports.ts` already selects `errorMessage`
  unconditionally on every run row regardless of status. Verified: `pnpm --filter
  @workspace/admin-panel typecheck` (`tsc --noEmit`) passed clean, exit code 0. No test
  manifest added — this repo's Test Coverage section scopes manifests to
  `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/` only; `admin-panel` is
  not in that scope. No findings filed — no other real gap surfaced during this build.
  Commit 165c5cd36, rebased onto `origin/main` and pushed as 42d2e53b4.
  `node scripts/dev-server/verify-branch-merged.mjs` confirms the branch is an ancestor of
  `origin/main`. `git status --porcelain` clean.
