# #4550 — Hold windows scan verdict has no live writer

- **Status:** ✅ DONE 2026-09-18T01:24:32Z
- **Scope:** platform

## Log

- 2026-09-17T23:59:51Z — Traced the real data path (see `build-journal/4550-plan.md`):
  `evaluateCaPolicyImpact` (#4522) is only called by the MSP-operator on-demand
  read endpoint; nothing persists its result to `portal_hold_windows`, and that
  table has no `policy_id` column to scan against in the first place. Building
  the missing column, the writer, and its hourly workflow trigger.
- 2026-09-18T01:24:32Z — ✅ DONE — commit `ebe904d4a473df0d5df1ebc66ac4473e4b627d02`
  (rebased onto `origin/main`, verified an ancestor of it).

## Changes Made

- `lib/db/src/schema/msp.ts` — `portalHoldWindowsTable.policyId` (nullable
  `text("policy_id")`), plus `portal_hold_windows_policy_id_idx`.
- `lib/db/src/schema/index.ts` — added `"ca_policy_hold_window_scan"` to the
  `WfNode.type` union (this is what `tsc` caught and made me fix — see
  Verification Results).
- `lib/db/migrations/manual/4550-portal-hold-windows-policy-id.sql` —
  additive `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` + index. **Ran myself**
  against the local `DATABASE_URL` this session (`ALTER TABLE` / `CREATE
  INDEX` / `INSERT 0 1` — the self-marking row for Simulator Studio's
  Migrations tree).
- `artifacts/api-server/src/lib/ca-hold-window-scan.ts` (new) —
  `deriveHoldScanFromImpact` (pure verdict/evidence-sentence derivation from a
  real `CaPolicyImpact`) and `handleCaPolicyHoldWindowScan` (the Workflow
  Engine node: selects every open, CA-gated hold window, re-runs the real
  #4522 `evaluateCaPolicyImpact` unchanged, writes `scan_verdict`/
  `scan_line`/`scan_source`/`scan_at`). A non-`ok` Graph read is written
  honestly as `watch` with the real reason, never skipped.
- `artifacts/api-server/src/lib/ca-hold-window-scan.test.ts` (new) — 10 unit
  tests: verdict/sentence derivation for every branch (blocked/interrupted/
  no-evidence-yet/clear/read-failed/incomplete-read), plus the DB-write
  handler (scan + error handling + per-tenant scoping).
- `artifacts/api-server/src/lib/node-type-registry.ts` — registered
  `ca_policy_hold_window_scan` (not AI-dependent).
- `artifacts/api-server/src/lib/workflow-executor.ts` — dry-run stub + real
  dispatch case for the new node type (imports and calls
  `handleCaPolicyHoldWindowScan`, mirroring `policy_evaluate_due` exactly).
- `artifacts/api-server/src/lib/seed-system-workflows.ts` — new
  `__system__: CA Policy Hold Window Scan` schedule workflow, hourly cron
  (`0 * * * *`), matching the column's own `scan_cadence` default and the
  established #1163 hourly-reconciliation convention.
- `artifacts/api-server/src/scripts/verify-hold-window-scan-4550.ts` (new,
  kept — same standing as `verify-ca-promotion-4522.ts`) — the live
  end-to-end verification script (see below).

**Deliberately not changed:** `portal-runbook-wire.ts` / `runbooks.tsx` (the
read side already projected `scan_verdict`/`scan_line` correctly — the drop
was entirely on the write side), `admin-testbed.ts` (QA seed endpoint, not a
production path).

## Findings filed

- **#4617** — `portal_hold_windows` is never written outside the
  `admin-testbed` QA seed; there is no live creation path for a hold window
  at all (a separate, larger gap than this issue's scope). Parented as a
  direct sub-issue of Feature #1489, labelled `bug`, board status set to "AI
  Batter Up".

## Verification Results

- **Unit tests:** `ca-hold-window-scan.test.ts` (10/10), plus
  `ca-policy-impact.test.ts`, `msp-ca-policy-promotion.test.ts`, and the
  `node-type-registry` / `workflow-executor-core` drift-guard suites all
  still green (41 total across the targeted run).
- **Typecheck:** `pnpm exec tsc --noEmit -p .` in `artifacts/api-server` —
  clean (exit 0). First run caught a real gap: `WfNode.type` in
  `lib/db/src/schema/index.ts` is a literal string union, and the new node
  type had to be added to it (`TS2678`) — fixed and reconfirmed clean.
- **Live, end-to-end, real Graph + real Postgres** (not mocked): ran
  `node artifacts/api-server/run-script.mjs src/scripts/verify-hold-window-scan-4550.ts`
  twice (before and after the typecheck fix) against the real testbed tenant
  `c4c814d4-3afe-441e-9145-62461d0a4fd3` (`tenants.id` 2080) and the real
  local Postgres:
  1. Inserted a real `portal_runbooks` + `portal_runbook_runs` +
     `portal_hold_windows` row with a `policy_id` and a deliberately-wrong
     pre-scan `scan_verdict`/`scan_line` placeholder.
  2. Ran `handleCaPolicyHoldWindowScan` for real — a genuine HTTPS call to
     Microsoft Graph (`/identity/conditionalAccess/policies/{id}`), no
     mocking.
  3. Confirmed in Postgres that `scan_verdict`/`scan_line`/`scan_source`/
     `scan_at` were actually overwritten (not left as the placeholder):
     `scan_verdict=watch`, `scan_line="Report-only impact could not be read
     from Microsoft Entra (policy_not_found: No Conditional Access policy
     with this id exists on the tenant.). Re-scan before deciding."` — the
     tenant's real, live state (a live `listCaPoliciesForTenant` call earlier
     in the session confirmed it currently has zero CA policies at all).
  4. Called `loadRunbooksForCustomer(2080)` — the exact function
     `GET /portal/runbooks` calls — and confirmed the customer-facing wire
     read reflects the same `scan_verdict`/`scan_line` just persisted, with
     no drop between write and read.
  5. Cleaned up the scratch rows both times.

  All 6 checks PASS on both runs. This closes the loop the issue describes:
  a real Conditional Access report-only impact read now actually reaches the
  customer-facing hold-window card — it is no longer computed and discarded.
  The specific case exercised live is the tenant's real "no such policy"
  state (honestly written, not dropped); the positive "would-block"/
  "would-interrupt"/"clear" branches are covered by the unit tests against
  synthetic Graph responses, the same discipline `ca-policy-impact.test.ts`
  already uses — the tenant currently has no live report-only CA policy to
  exercise those branches against real sign-in traffic (a genuinely separate,
  pre-existing gap — see Findings).
- `git status --porcelain` clean; branch verified an ancestor of
  `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0)
  before writing this DONE.
