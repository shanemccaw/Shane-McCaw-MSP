# #2625 — Consent and Onboarding: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-04T05:40:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** _fill after commit_

## Log
- 2026-09-04T05:00:00Z ⏳ IN FLIGHT — Real backend confirmed built (#2674, commit `1555aab4`). Extracting the
  contract pack per the #1642 pattern from `msp-consent.ts` (4 routes) and `msp-onboarding.ts`'s new
  `GET /api/msp/onboarding/links` route, cited to file:line, cross-checked live against local PostgreSQL.
- 2026-09-04T05:40:00Z ✅ DONE — Wrote `docs/consent-and-onboarding-msp-console-contract-pack.md` (read-only,
  no product code/schema/UI changed). Two real gaps found and filed as **#2818** (parented as a sibling
  sub-issue of #2563, labeled `bug`, board status set to "AI Batter Up"): (1) `adminEmail`/`adminDisplayName`
  silently dropped from every MSP-scope consent response despite the admin tier splicing them back in for
  its own response; (2) no MSP-scoped route to initiate `writeBack`/`sharepoint` consent, only `graph`
  (read) — a real gap against #2563's own "visibility and management" scope. No orphaned-endpoint sub-issue
  filed — every route in this pack is genuinely unconsumed only because `artifacts/msp-console` is still
  scaffolding, confirmed by a real grep against the current tree. Verified live against local PostgreSQL
  (`tenants.consent`, `msp_onboarding_links` columns match the Drizzle source). Working tree clean before
  this commit besides the new pack file.
