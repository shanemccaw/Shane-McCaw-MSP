# #2162 — Ownership / RACI acceptance gate: configurable strict/loose (redo of #1518)

- **Status:** ✅ DONE 2026-09-01T20:23:31Z
- **Scope:** portal
- **Started:** 2026-09-01
- **Commit(s):** 066c2fbe4 (schema + manual migration), 4c90ccb87 (api-server: gate logic, decline
  route, symmetric MSP routes, settings toggle, notification) — both on `agent/2162-q1127`, pushed
  and confirmed present on `origin/agent/2162-q1127` at the same hashes.

## Log
- 2026-09-01T20:02:28Z ⏳ IN FLIGHT — Verified worktree `agent/2162-q1127` (the directory existed
  but had never been checked out — fixed by clearing stray build-cache leftovers and running
  `git worktree add`). Confirmed on the live database that #1518's schema (`responded_by`,
  `responded_at`, `decline_reason` on `portal_ownership_assignments`, migration
  `2026-08-31-ownership-raci-acceptance-gate-1518.sql`) is already applied — not re-running it.
  Read `lib/portal-ownership.ts`, `routes/portal-ownership.ts`, `routes/msp-ownership.ts` (the
  existing file is #1521's read-only cross-customer view, not the symmetric accept/decline/assign
  set #2162 still needs), and `routes/portal-settings-change-control.ts` as the precedent for a
  per-customer settings-policy table. Building: `portal_ownership_policy` table (customerId ->
  gateMode strict|loose, default loose per Shane's recommendation in the issue — matches current
  de facto behaviour, opt-in to strict), `initialAcceptance` updated to take gateMode, a new
  customer-side decline route, an actor-must-equal-owner gate on accept/decline (strict only),
  symmetric MSP-side assign/accept/decline routes, and a best-effort pending-cell notification
  (strict only). Scope stop per issue: wire contract + settings toggle only, no ownership-matrix
  UI page. Open question from the issue (whether C should also carry the gate) is being recorded
  as a recommendation, not decided here.
- 2026-09-01T20:23:31Z ✅ DONE — Built and verified:
  - **Schema** (`066c2fbe4`): `portal_ownership_policy` (customerId -> gate_mode strict|loose,
    default loose), migration `lib/db/migrations/manual/2026-09-01-ownership-raci-gate-mode-2162.sql`,
    run against local `DATABASE_URL` (`CREATE TABLE`, `CREATE INDEX`, `INSERT 0 1`, `COMMIT` — all
    real, no drizzle-kit push). Drizzle schema added by hand to `lib/db/src/schema/msp.ts`, mirroring
    `portalChangeControlPolicyTable`'s shape/convention. Confirmed on the live DB beforehand that
    #1518's own schema (`responded_by`/`responded_at`/`decline_reason`, migration
    `2026-08-31-ownership-raci-acceptance-gate-1518.sql`) is already applied — not re-run.
  - **Gate logic** (`4c90ccb87`): `initialAcceptance(ownerPersonId, roleKey, gateMode)` — strict
    starts R/A at `pending` (#1518's original behaviour), loose is effective immediately (`""`, the
    same "no acceptance to track" value C/I already use). `actorMayRespond(gateMode, ownerPersonId,
    actorPersonId)` — strict requires the actor to BE the named holder; loose passes everyone.
  - **Customer-side write routes**: `/portal/ownership/assign` now resolves this customer's
    `gateMode` before computing acceptance. New `POST /portal/ownership/decline` mirrors `/accept`
    (records `declineReason`/`respondedBy`/`respondedAt`, inserts a `declined` event). Both
    `/accept` and `/decline` 403 with "Only the named holder may accept/decline this cell." when
    strict mode is on and the actor isn't the named owner — closing the "any tenant user could
    accept on anyone's behalf" gap the issue called out. `GET /portal/ownership` now returns
    `gateMode` on the payload.
  - **Settings toggle wire contract** (new `routes/portal-settings-ownership.ts`): `GET`/`PUT
    /portal/settings/ownership(/policy)`. No UI page built — scope stop per the issue
    (`Design/portal/` has no Ownership export). Default gateMode is `"loose"` per Shane's own
    recommendation in the issue ("matches current de facto behaviour, opt-in to strict") — I did
    not stop to re-confirm this because the issue already gave the recommendation with its
    rationale and it changes nothing about any existing customer's behaviour (additive, reversible
    any time from Settings); flagging it here in case Shane wants a different default.
  - **Symmetric MSP-side routes** (`routes/msp-ownership.ts`, new): `POST
    /msp/ownership/:customerId/{assign,accept,decline}`, scoped by `requireCustomerScope("params")`
    (verifies `:customerId` belongs to the caller's own MSP, honouring per-staff-member customer
    scoping — same middleware `assertCustomerAccess` already enforces elsewhere). `/assign` only
    accepts an `ownerPersonId` that resolves to an active MSPAdmin/MSPOperator of the CALLER'S OWN
    mspId (rejects assigning a customer's own user, or another MSP's staff, through this route).
    These exist because the customer-side routes require a customer JWT, which a cross-tenant-
    boundary MSP holder never carries.
  - **Notification** (`notifyOwnershipPending` in `lib/notification-center.ts`): best-effort, fires
    only when an assign produces `acceptance === "pending"` (strict mode only — loose never
    produces a pending cell). Resolves the named holder's own `users` row to route to a
    `customer_user` or `msp_user` notification recipient without a separate "which side" branch at
    the call site. Deliberately does not resolve the object's real name (would cost a second
    `gatherOwnershipObjects` query per assign) — title says which role, not which object.
  - **Open question preserved, not decided**: whether Consulted should also carry the gate when
    strict. Recommendation for Shane: no — C is "must be reachable and respond if asked", which
    already has a real behavioural difference from R/A's "must actively agree to hold this before
    it counts" (C is never even NAMED as blocking anything downstream today), and gating C would
    mean a customer's own consult list is silently unusable in strict mode until each person
    accepts, discouraging thorough C lists. Not implemented either way pending Shane's decision.
  - **Multi-tenant scoping**: every new/changed write is scoped by `customerId` (JWT-derived on
    the customer side, `requireCustomerScope`-verified on the MSP side) on every read/write/update,
    matching the existing routes' own pattern.
  - **Logging**: all new/changed log lines use the `tenant.portal` channel already bound at the top
    of `routes/portal-ownership.ts`/`routes/msp-ownership.ts` — no new taxonomy leaf introduced.
  - **Verification**: `portal-ownership.test.ts` 45/45 passing (added coverage for both gate modes
    on `initialAcceptance` and new coverage for `actorMayRespond`). `tsc --noEmit` on
    `artifacts/api-server` — zero errors on every file this build touched; the package's remaining
    ~140 errors are pre-existing and unrelated (a repo-wide `@workspace/integrations-anthropic-ai`
    resolution gap and pre-existing implicit-`any`s in files this build never opened — not
    attributed here). Live DB query confirmed the new table and #1518's existing columns are both
    real, applied state, not assumed.
  - **Infra note, not part of #2162's own scope**: the assigned worktree directory (`C:\wt\
    2162-q1127`) existed on disk but had never actually been `git worktree add`-ed — cleared stray
    build-cache leftovers and attached it properly to the already-existing `agent/2162-q1127`
    branch before any of the above could start. Separately, the main checkout's own
    `artifacts/api-server/node_modules/@workspace/db` symlink was dangling (pointed at a deleted
    worktree, `C:\wt\2116-q1106`) — exactly the "shared store poisoned" case `store-doctor.mjs`
    already has a name for (Git #1988); repaired it to point at the main checkout's own `lib/db`
    (a `node_modules`-only fix, gitignored, no source/history touched) so typecheck could resolve
    `@workspace/db` at all. Then, since this worktree's own schema edit needed to typecheck against
    ITS OWN `lib/db/dist` rather than main's, rebuilt this worktree's `artifacts/api-server/
    node_modules` with per-package junctions instead of one wholesale directory junction, so only
    `@workspace/db` resolves locally (to this worktree's own built dist) while everything else stays
    shared with main. Both are local, reversible, gitignored tooling fixes — no product code,
    migration, or shared git history was touched by either.
  - `git status --porcelain` clean before this line was written.
