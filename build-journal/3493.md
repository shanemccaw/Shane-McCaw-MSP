# #3493 — Feature: UI Shell Redesign (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T12:03:48Z
- **Scope:** platform (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** e8460c5fd (merged into origin/main)

## Log
- 2026-09-10T06:28:12Z ⏳ IN FLIGHT — read UI_RULES.md, adminv2/SHELL.md, GEMINI.md,
  BUILD_QUEUE_METHOD.md, and the current MyArchitect source (VS-Code-style activity
  bar shell + already-built-but-unwired backend services for Console/Remediation/
  Runbooks per #3459/#3471/#3479/#3460). Also confirmed via `gh api .../sub_issues`
  that #3493 has no real GitHub sub-issues yet — building against the issue's own
  checklist. Confirmed no prior work landed on origin/main for this issue and no
  other live worktree is building it. Note: two separate BUILD dispatch comments
  exist on #3493 (buildSet=MyArchitect/opus-4-8, and buildSet=BatterUpClearOut/
  sonnet-5 — this session) six minutes apart; flagging as a possible double-dispatch
  in queue tooling in the DONE comment.
  Building: Fluent.Ribbon-based fixed-tab ribbon (Home/Console/Watch/Documents/
  Admin) with intent contract enforcement, contextual-tab + Back-group splice
  mechanism, left reference panel (bookmarks + stated-empty VIP/consent/document
  slots), right full-panel record workspace contract, command palette (4 prefixes),
  title bar QAT (tenant switcher + palette trigger), status bar version from
  assembly.
- 2026-09-10T12:03:48Z ✅ DONE — commit e8460c5fd, rebased onto and pushed to
  `origin/main` (verified via `scripts/dev-server/verify-branch-merged.mjs` — exit
  0, "merged into 'origin/main'. Safe to write DONE"). Clean `dotnet build` (0
  warnings, 0 errors) confirmed both before and after the rebase.

  **What shipped, real and working:**
  - Fluent.Ribbon 10.0.1 package + Dark.Blue theme, replacing the VS-Code-style
    activity bar and the 4 dead placeholder icons (Search, Source Control, Run and
    Debug, Extensions & Scripts) entirely.
  - `Shell/ShellContracts.cs` + `ShellRegistry.cs` — the fixed-tab intent contract
    (`Open`/`Create`/`Global` legal, `Record` throws `ShellContractException`),
    ported from AdminV2's `registerScreen`/`ShellContractError` discipline
    (UI_RULES.md §2).
  - `Shell/RibbonAssembly.cs` — trail (most-recent-first, deduped, capped 6), the
    Back-group splice at position 2, and the Watch-tab group-merge-by-label
    mechanism, ported from AdminV2's `registry/ribbonAssembly.ts`.
  - `Shell/FixedRibbonRenderer.cs` — renders `ShellRegistry`'s groups onto a real
    `Fluent:Ribbon`; a fixed tab with nothing registered renders a stated empty
    state, never a fabricated placeholder (Watch/Documents/Admin are honestly
    empty today — their real Features, #3483/#3486/#3487/#3490/#3461/#3489/#3480/
    #3485, haven't landed).
  - `Shell/RecordWorkspacePanel` — full-panel right-panel record workspace
    (facts/edits/actions/list), confirm-arm two-press with no dialog (UI_RULES.md
    §3).
  - `Shell/LeftReferencePanel` — bookmarks folded in from the old activity-bar
    popout; VIP lookup/consent status/document browse are real stated-empty
    sections (#3484/#3485/#3486 not built yet), not fabricated boxes.
  - `Shell/CommandMatcher` + `CommandPaletteOverlay` — `@`/`>`/`#`/`?` prefixes,
    layered matching (exact/prefix/word-start/substring/acronym/subsequence+gap),
    commands recomputed live on every open (UI_RULES.md §5).
  - `Shell/ConsolePanelView` — real Console-tab document wired to the existing
    hosted PowerShell runspace service (#3459), which had been sitting unwired
    pending this shell per its own code comment.
  - Real end-to-end proof of the full gallery → contextual tab → workspace →
    confirm-armed action contract using only already-real client services: Home
    tab's "Change Requests" gallery calls the real
    `GET /api/msp/change-requests` (no invented ids — `IChangeControlService`
    only needs a `tenantGuid`, which `Tenant` genuinely carries), opens a
    contextual tab with the Back group spliced in, and a confirm-armed action
    calling `RecordHumanActionAsync` for real.
  - Script Library gallery is registered (the real mechanism works) but honestly
    states it's blocked on #3501 (no auth/session mechanism ⇒ no real
    mspId/customerId to call `LaunchControlActionsService` with) instead of
    guessing an id, which would have been inventing data.
  - Status bar: app version now reads from the assembly (was hardcoded "v1.1"),
    real live clock added. Contract-hours segment left empty pending #3473/#3474,
    per the spec itself.
  - Title bar Undo button is scaffolded (disabled) — `IChangeControlService` has
    no rollback client method yet, so it's a real no-op, not a fake undo.

  **Genuine remaining scope, filed as real sub-issues of #3493** (not built this
  session — each needs its own dispatch per BUILD_QUEUE_METHOD.md's "every real
  unit of work gets its own issue"):
  - #3506 — Log viewer (Loaded/Live modes), UI_RULES.md §1
  - #3507 — version-update mechanism port (BuildConsole's VersionInfo.cs)
  - #3508 — real Undo backend wiring for Change Control rollback

  **Other finding filed:** #3509 (parented #1202, Build Console epic) — two
  separate `BUILD:` dispatch comments landed on #3493 six minutes apart
  (`buildSet=MyArchitect`/opus-4-8 and `buildSet=BatterUpClearOut`/sonnet-5, this
  session). No actual collision happened this time (only one worktree existed for
  #3493, no prior work on origin/main), but the dispatch path itself duplicated —
  a real queue-tooling gap.

  All 4 filed issues (#3506–#3509) set to board status "AI Batter Up" per the
  standing rule; none closed by me.
