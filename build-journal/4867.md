# #4867 — Batch writes + idempotency keys (Shane's Life MCP)

- **Status:** ✅ DONE 2026-09-19T17:17:17Z
- **Scope:** platform (code lives in shanemccaw/shanes-life)

## Changes Made
- https://github.com/shanemccaw/shanes-life/commit/2d5504ef5c931c95117db423a8a26a816e4ff6de
  - `src/mcp/tools.mjs`: new `batch_create_entities` (≤100 specs, per-item independent results); optional `idempotencyKey` on create_entity, set_debt, set_contact, push_task, log_financial_event via one shared wrapper.
  - `src/core/idempotency.mjs`: claim-then-run, 24h window, failed handler releases claim, in-flight repeat refused.
  - `migrations/077_mcp_idempotency_keys.sql`: new table (additive). Not run against Shane's Life's real DB (not reachable from here); it runs via the app's boot-time migrator.

## Verification Results
Scratch local Postgres DB (dropped after) with migration 077: same key twice = 1 handler run, same result; no key = runs every time; failed handler then retry runs; concurrent same key = second refused; key older than 25h reruns. Batch of 10 mixed specs with stubbed create_entity: 6 created, 4 failed, each reported independently. NOT done: live call through the deployed MCP server (unreachable; the Shanes-Life connector failed to connect), and the real create_entity DB path was stubbed.

## Log
- 2026-09-19T17:17:17Z ✅ DONE — as above. No sub-issues filed. The IN FLIGHT bookend was skipped; this is the only bookend.
