# #3619 — Tenant Scores and Results (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-11T02:47:10Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** `01a8a663e` (bookend IN FLIGHT), `9e1d629da` (contract pack, real hash on
  `main` after rebase — was `32edcaa4f` pre-rebase)

## Log
- 2026-09-11T02:29:26Z ⏳ IN FLIGHT — read #3619 body + BUILD comment, #3558 (backend), #1642-pattern
  reference pack (`docs/msp-console/remediation-tracking-msp-console-contract-pack.md`); read
  `msp-customer-scores.ts`/`.test.ts` in full plus its customer-facing mirror
  `portal-customer-engines.ts`'s `/portal/dashboard` handler; confirmed live against local
  Postgres that `tenant_engine_snapshots` has 0 rows (matches the issue's own stated gap) while
  `msp_diagnostic_findings` has 296 real rows across the 2 real tenants (ids 1, 3) — enough to
  demonstrate the route's `priorityItems` block against real data even though scores/pillars are
  honestly empty. About to write `docs/msp-console/tenant-scores-and-results-msp-console-contract-pack.md`.
- 2026-09-11T02:47:10Z ✅ DONE — wrote and committed
  `docs/msp-console/tenant-scores-and-results-msp-console-contract-pack.md` (343 lines):
  full wire contract for `GET /api/msp/customers/:customerId/scores` cited to file:line,
  real enum unions (tenant status, finding severity, the 12 real `engine_key` values from
  `engine-registry.ts`'s `ENGINE_DEFS`), the §0.2 diff against `/portal/dashboard` (no paywall
  gate on findings OR priority-item text — confirmed by the route's own test file's load-bearing
  assertion — and a deliberately narrower response than the full dashboard payload, per
  Feature #3557's own scope), and the honest live-data state (0 rows in `tenant_engine_snapshots`
  locally, reproduced the route's exact `priorityItems` query by hand against customer 1's real
  296-row `msp_diagnostic_findings` data to show real, non-fabricated output).
  **Found and filed #3621** (parented as a sub-issue of Feature #3557, labeled `bug`, board
  status set to "AI Batter Up"): `scores.governance` is a phantom key — no engine registered in
  `ENGINE_DEFS` (`engine-registry.ts:183-293`) ever writes `engine_key='governance'`, so that
  field on both this route and `portal-customer-engines.ts` is structurally, permanently 0, not
  merely unscanned. Pre-existing on the customer route before this build; surfaced by this
  extraction, not introduced by it.
  Branch verified merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs`
  — OK, `9e1d629d` is an ancestor). `git status --porcelain` clean. No product code, schema, or
  UI touched — read-only extraction as scoped.
