# #3786 — Add a real, quick toggle button to hide/show CritterLoungeControl

- **Status:** ✅ DONE 2026-09-12T06:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** b7e437747

## Log
- 2026-09-12T05:57:01Z ⏳ IN FLIGHT — adding CritterLoungeVisible setting + quick-access toggle button near CritterLounge in BuildQueuePanel.xaml
- 2026-09-12T06:10:00Z ✅ DONE — Added `BuildConsoleSettings.CritterLoungeVisible` (bool, default `true`,
  matching pre-existing always-on behavior). Added a small `IconButton`-styled toggle
  (`BtnToggleCritterLounge`) docked directly above `CritterLoungeControl` in
  `BuildQueuePanel.xaml` (real quick access, not in the Settings tab) with a Segoe MDL2
  eye/eye-off glyph and matching tooltip. Click handler flips the setting, saves it, and
  immediately sets `CritterLounge.Visibility` — no restart needed. Constructor applies the
  persisted value on load so a restart shows the last real choice. `dotnet build` on
  `desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 errors. Pushed directly to
  `origin/main` (b7e437747); `verify-branch-merged.mjs` confirms merged. `git status
  --porcelain` clean apart from this bookend edit itself. No fixture/hardcoded data — this is
  a pure UI/settings feature with no data layer.
