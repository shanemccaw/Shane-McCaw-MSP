# #3356 — Customer Timeline (MSP Console operator side): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T00:10:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 419495266 (bookend IN FLIGHT), f1a24e071 (pack)

## Log
- 2026-09-09T23:20:00Z ⏳ IN FLIGHT — confirmed `msp-customer-timeline.ts` (418 lines,
  `GET /api/msp/timeline`) is real and mounted; no `docs/msp-console/*` pack exists for it
  yet. Read the route, its test file, `portal-customer-timeline.ts` (the file it mirrors),
  supporting libs (`doc-gate-coverage.ts`, `engine-registry.ts`, `requireAuth.ts`,
  `resolve-msp-id.ts`), and cross-checked schema/enums + real row counts against local
  Postgres. About to write `docs/msp-console/customer-timeline-msp-console-contract-pack.md`
  per the #1642 pattern.
- 2026-09-10T00:10:00Z ✅ DONE — wrote and committed
  `docs/msp-console/customer-timeline-msp-console-contract-pack.md` (`f1a24e071`), every
  field cited to file:line and cross-checked live against local Postgres (`msp_diagnostic_runs`
  2 rows, `msp_diagnostic_findings` 296, `sales_offers` 5, `tenant_engine_snapshots`/
  `insights_generated_documents` 0 — confirms the partial-source honest-empty contract).
  Found and filed one real bug during extraction: **#3376**, parented to #1571 (no
  Feature-tier parent exists for #3356) — `msp-customer-timeline.ts`'s own header docblock
  (`:22-24`) misdescribes `sales_offers.customerId` as a `users.id` needing a bridge, when
  it's actually a direct `tenants.id` FK (#2730); the route's own later inline comment
  (`:268-271`) and the actual query code already get it right, only the header prose is
  stale. #3376 labeled `bug`, board status set to "AI Batter Up". No product code, schema,
  or UI changed — read-only extraction as scoped. `verify-branch-merged.mjs` confirms
  `agent/3356-q2092` (`f1a24e07`) is an ancestor of `origin/main`; `git status --porcelain`
  clean.
