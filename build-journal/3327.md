# #3327 — bin/check.mjs has zero live-check coverage for the Pantry room (#3308/#3316)

- **Status:** ✅ DONE 2026-09-09T13:55:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal project, not the MSP app)
- **Started:** 2026-09-09
- **Commit(s):** b0d8c7767

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — adding a real Pantry section to bin/check.mjs covering GET
  /api/pantry (zone/low_at), POST /api/pantry/:id/adjust, the shopping-checkoff restock
  integration, all three pantry capture-grammar rules through the real /api/captures path, and
  GET /api/today's rooms.pantry block, per the issue's own "Real fix" list.
- 2026-09-09T13:55:00Z ✅ DONE — added a "6d-2. Pantry" section to bin/check.mjs (commit
  b0d8c7767) covering every item in the issue's "Real fix" list: GET /api/pantry's real
  zone/low_at, POST /api/pantry/:id/adjust (both real decrements and the direction-validation
  400), PATCH /api/lists/:id/items/:itemId's real restock/reversal against the matching Home
  pantry row, all three pantry capture-grammar rules (pantry_have/pantry_bought/pantry_out)
  resolving end-to-end through the real POST /api/captures path (not just capture-grammar.test.mjs's
  pure matchRule unit tests), and GET /api/today's rooms.pantry lit/subtitle rule. Ran it live:
  Shane's own real shanes-life server was already up on :5000 against the real local
  postgresql://localhost:5432/finances database (junctioned node_modules from the main checkout,
  no install/download); `node bin/check.mjs` came back 233/237, with all 15 new pantry checks
  PASS and the run's own "not one ShanesSurvival row moved" assertion PASS. The 4 pre-existing
  failures are unrelated to this change -- SL_VAULT_KEY is not set in this environment's .env, so
  the vault section fails exactly the way vault.mjs says it will out loud; same gap existed before
  this build. Scratch .env copy and the node_modules junction used only for this live run were
  removed afterward; working tree is clean (`git status --porcelain` empty).
