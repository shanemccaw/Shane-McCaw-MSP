# #3106 — print_tokens.document_id NO ACTION FK aborts 7-year purge

- **Status:** ✅ DONE 2026-09-10T00:41:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 557483f6d54b6107c57e5b364a11559c2531122f

## Log
- 2026-09-10T00:27:41Z ⏳ IN FLIGHT — pulled issue #3106 + comment, investigating print_tokens schema and choosing ON DELETE CASCADE vs SET NULL before writing migration.
- 2026-09-10T00:41:00Z ✅ DONE — `print_tokens.document_id` was `NO ACTION` (confirmed live via `pg_get_constraintdef`). `documentId` is `NOT NULL` in both the live DB and the Drizzle schema, so `SET NULL` was not viable without relaxing that constraint, and an orphaned token with nothing to render would have no remaining purpose anyway — chose `ON DELETE CASCADE` per the issue's own preferred option. Changes:
  - `lib/db/src/schema/index.ts` — `printTokensTable.documentId` reference now carries `{ onDelete: "cascade" }`.
  - `lib/db/migrations/manual/2026-09-10-print-tokens-document-id-cascade-3106.sql` — drops and re-adds the FK constraint with `ON DELETE CASCADE`. Not destructive per the migration-gate rules (constraint drop/re-add, no data touched). **Ran it myself** against local `DATABASE_URL`; live `pg_get_constraintdef` now reads `... ON DELETE CASCADE`.
  - `artifacts/api-server/src/lib/retention/purgers/modules.ts` — updated the `documentsPurger` comment on the `print_tokens` target: the ordering constraint is closed (cascade makes it correctness-independent), target stays declared/first as an explicit audit trail, not for ordering.
  - `artifacts/api-server/src/lib/retention/purgers/identity-purge.live-db.test.ts` — updated the stale comment describing the old NO ACTION ordering probe to reflect the cascade.
  - Verification: (1) real ad-hoc transaction against local DB — inserted a document + a print token held by a user outside the document's own tenant (the MSP-staff residual case), deleted the document directly, confirmed the DELETE succeeded (no abort) and the print token cascaded away; rolled back the scratch txn. (2) `identity-purge.live-db.test.ts` — 6/6 passing against the live local DB with the new constraint in place.
  - No fixture/hardcoded data introduced.
