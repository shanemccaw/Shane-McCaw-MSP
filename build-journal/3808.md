# #3808 — "End this git nightmare" button color swap

- **Status:** ✅ DONE 2026-09-12T19:17:23Z
- **Scope:** buildconsole

## Log
- 2026-09-12T19:16:12Z — Started. One-line resource swap in `desktop/BuildConsole/Controls/GitDoctorView.xaml`: `BtnGitDoctorNightmare` Background from `RedBrush` to `MaroonBrush`, per confirmed issue body. Not touching `RedBrush` itself.
- ✅ DONE — 2026-09-12T19:17:23Z. Swapped `Background="{StaticResource RedBrush}"` to `Background="{StaticResource MaroonBrush}"` on `BtnGitDoctorNightmare` in `desktop/BuildConsole/Controls/GitDoctorView.xaml` (line 34). `RedBrush` untouched — no other use of it affected. Confirmed `MaroonBrush` (`#EBA0AC`, `Themes/DarkTheme.xaml:63`) is lighter than `RedBrush` (`#F38BA8`), so the existing `Foreground="Black"` label text keeps equal-or-better contrast. Commit `7ea1cbeeee4088b278d832f3162e3d09b9e0f393` on `agent/3808-q2498`, merged into `main` and pushed. Verified own branch's HEAD is an ancestor of `origin/main`. `git status --porcelain` clean. No other findings surfaced. This is a WPF XAML-only visual change with no test-manifest scope (BuildConsole is excluded from that requirement).
