# #2330 — Type chips insert the marker for you

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 4a5c82e63 (implementation), rebased/pushed to `main` as 77d3b9134

## Log
- 2026-09-02T20:05:00Z ⏳ IN FLIGHT — Sub-issue of #2326 (Feature: Test Pad), item 4 of
  28. Test Pad does not exist yet in ShaneBuilder (confirmed by #2330's own body's
  grep). Its own real dependencies: #2327 (Pill/pad shell + TestPadService/TestPadNote)
  is still `IN FLIGHT` with nothing pushed to `main`, and #2328 (Composer) is `BLOCKED`
  on #2327 — so there is no live composer to wire a "click a chip → marker lands in the
  textbox" flow into yet. #2329 (leading-marker parsing) already landed on `main`
  (`Services/TestPad/NoteType.cs` + `NoteMarkerParser.cs`) and its own doc comments
  explicitly anticipate this issue, including a ready-made `NoteMarkerParser.MarkerFor(NoteType)`
  helper for "the Type Chips feature (#2330) to insert the marker for the user."
  Following the same collision-avoidance precedent #2329 set: building a self-contained,
  reusable `TypeChipsControl` (WPF UserControl) under `Controls/TestPad/` that renders
  the four type chips and knows how to insert its marker into any `TextBox` handed to
  it (`TargetTextBox` dependency property) or notify via a `ChipClicked` event — ready
  for #2328's Composer to consume once #2327 unblocks it, with zero risk of colliding
  with #2327/#2328's in-flight `Controls`/`MainWindow.xaml` edits since nothing here
  touches either.
- 2026-09-02T20:35:00Z ✅ DONE — Mid-session #2327 landed on `main`
  (`Test Pad pill bottom-right...`, 8d0437765), building the real `TestPadWindow`/
  `TestPadPillWindow`/`TestPadService`/`TestPadNote` shell (no composer TextBox yet —
  that's still #2328's scope). Merged that in, then built
  `desktop/ShaneBuilder/TestPad/TypeChipsControl.xaml(.cs)`: a real WPF UserControl
  with the four type chips (Bug `!`, Question `?`, Idea `+`, Works `.`), colored via
  the existing `Brush.Toast.*` tokens. `TargetTextBox` (a wireable `TextBox` property)
  + `InsertMarker(TextBox, NoteType)` insert the clicked type's marker at the head of
  whatever composer TextBox is handed to it, using #2329's own
  `NoteMarkerParser.Parse`/`MarkerFor` so a chip-inserted marker and a hand-typed one
  are always the identical rule — stripping any marker already leading the text first,
  so a click never stacks two. `ChipClicked` also fires for a caller with no single
  TextBox to aim at (e.g. the Notepad importer's per-row type-correction chips,
  #2351). `dotnet build -c Debug` succeeded, 0 warnings/errors. Verified via
  `verify-branch-merged.mjs`: merged into `origin/main` as 77d3b9134. `git status
  --porcelain` clean. Not wired into `TestPadWindow`/a composer yet — there is still
  no composer TextBox on `main` to wire it into (#2328 was blocked on #2327, which has
  now landed, so #2328 should be unblockable, but had not yet been re-picked-up as of
  this session's end); that wiring is #2328's own job when it lands, same
  collision-avoidance reasoning #2329 used for the parser.
