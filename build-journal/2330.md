# #2330 — Type chips insert the marker for you

- **Status:** ⏳ IN FLIGHT 2026-09-02T20:05:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-02T20:05:00Z ⏳ IN FLIGHT — Sub-issue of #2326 (Feature: Test Pad), item 4 of
  28. Test Pad does not exist yet in ShaneBuilder (confirmed by #2330's own body's
  grep). Its own real dependencies: #2327 (Pill/pad shell + TestPadService/TestPadNote)
  is still `IN FLIGHT` with nothing pushed to `main`, and #2328 (Composer) is `BLOCKED`
  on #2327 — so there is no live composer to wire a "click a chip → marker lands in the
  textbox" flow into yet. #2329 (leading-marker parsing) already landed on `main`
  (`Services/TestPad/NoteType.cs` + `NoteMarkerParser.cs`) and its own doc comments
  explicitly anticipate this issue, including a ready-made `NoteMarkerParser.MarkerFor(NoteType)`
  helper for "the Type Chips feature (#2330) to insert the marker for the user."
  Following the same collision-avoidance precedent #2329 set: building a self-contained,
  reusable `TypeChipsControl` (WPF UserControl) under `Controls/TestPad/` that renders
  the four type chips and knows how to insert its marker into any `TextBox` handed to
  it (`TargetTextBox` dependency property) or notify via a `ChipClicked` event — ready
  for #2328's Composer to consume once #2327 unblocks it, with zero risk of colliding
  with #2327/#2328's in-flight `Controls`/`MainWindow.xaml` edits since nothing here
  touches either.
