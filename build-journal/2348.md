# #2348 — Import: "<need screen shots>" is stripped and flags the note for a shot

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** c76787c6d (Test Pad: Import — strip "need screen shots" marker and flag note for a shot)

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Feature: Test Pad (#2326), item 22/28. When
  `TestPadImportParser` encounters the literal marker "<need screen shots>" inside a
  parsed block, strip it from the note body and flag the resulting candidate so the
  filed note gets the same `HasShotSlot` droppable-thumbnail slot the manual "Attach
  shot" composer chip (#2340) arms — reusing that existing mechanism rather than
  inventing a second one. Also surface the flag in the import preview row so the user
  can see which notes got auto-flagged before Import.
- 2026-09-02T20:35:00Z ✅ DONE — Added `TestPadImportParser.NeedsShotMarker`
  (case-insensitive regex matching `<need screen shot(s)>` with flexible whitespace),
  applied inside `FlushBlock` before `NoteMarkerParser.Parse` so a leading type marker
  (`!`/`?`/`+`/`.`) elsewhere in the block still parses correctly after the tag is
  stripped and whitespace re-collapsed. Added `TestPadImportCandidate.NeedsShot`, wired
  through to `TestPadNote.HasShotSlot` on Import in `TestPadImportWindow.BtnImport_Click`,
  and added a "needs shot" preview chip next to the type chip so the flag is visible
  before import. A block that is *only* the marker (no other content) is correctly
  dropped rather than filed as an empty flagged note, matching the splitter's existing
  empty-block behavior. Rebased three times against concurrently-landed #2344 (Section
  detection) and #2349 (MatchedFeature field) — both resolved by keeping both sides
  (additive fields/logic); `dotnet build` clean (0 warnings/errors) after every rebase
  and the final push. `verify-branch-merged.mjs` confirms `agent/2348-q1286` (c76787c6)
  is an ancestor of `origin/main`. `git status --porcelain` clean. No test manifest —
  `desktop/ShaneBuilder` (the WPF app) is out of scope for test manifests per CLAUDE.md.
