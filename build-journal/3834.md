# #3834 — BuildSetRollup fully rebuilds every row on each queue change

- **Status:** ✅ DONE 2026-09-12T21:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 789647386 (d035d1b23 after rebase onto origin/main)

## Log
- 2026-09-12T21:05:00Z ⏳ IN FLIGHT — Investigating RenderBuildSetRollup and the KeyedCardPool pattern from #3698 to plan the row-reuse refactor.
- 2026-09-12T21:35:00Z ✅ DONE — Replaced RenderBuildSetRollup's `BuildSetRollupList.Children.Clear()` + full
  rebuild with a `KeyedCardPool` (`_rollupCards`) pooling both the Epic-group headers and the
  per-build-set rows, matching #3698's proven pattern for the queue cards. Row/header keys
  (`RollupRowKey`/`RollupEpicHeaderKey`) cover every input the two builder methods read: bucket
  membership, selection/expansion state, a members signature (id + originating-chat identity, for
  the context menu), and the landed/needs-attention send-eligibility split — so a send or a
  bookend-satisfaction flip still forces the affected row to rebuild even though the raw Verifying
  bucket didn't change. Investigated the flagged rising-per-call-time pattern: found no persistent
  event-handler/timer accumulation (the send-outcome `DispatcherTimer` is one-shot and
  self-stopping; no growing collection retains discarded rows) — the rise is consistent with
  GC/layout pressure from allocating hundreds of fresh WPF elements per call while the panel is
  open, which this pooling fix addresses directly. Verified: `dotnet build BuildConsole.csproj -c
  Debug` — Build succeeded, 0 warnings, 0 errors. No BuildConsole test manifest applies (out of
  scope per CLAUDE.md). `git status --porcelain` clean; branch confirmed merged into origin/main
  via `verify-branch-merged.mjs`.
