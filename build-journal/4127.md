# #4127 — ShaneBot engine: Batch C — Tenant & Ops cluster

- **Status:** ✅ DONE 2026-09-15T00:59:42Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 3f96483f1

## Log
- 2026-09-15T00:27:31Z ⏳ IN FLIGHT — researched real backends for all 6 topics
  (tenantStatus → portal-mission-control.ts engine status strip; project → the
  dashboard's own multi-project projectsTable+kanbanTasksTable query,
  confirmed 0 active projects on the testbed tenant today; msChanges →
  portal-message-center.ts's mspMessageCenterItemsTable + effectiveDate();
  diagnostics → reuse the findingRows already fetched by Batch A's wave,
  filtered to severity==="critical"; statusReports → mspStatusReportsTable;
  configState → config-state-views.ts's latestSealedPair/snapshotCompleteness/
  readSnapshotDocument, confirmed 0 sealed snapshots on the testbed tenant
  today). About to implement the 6 topic builders in shanebot-engine.ts and
  wire support-chat.ts's card resolution + system-prompt bullet list.
- 2026-09-15T00:59:42Z ✅ DONE — commit 3f96483f1 (merged into origin/main,
  verified via verify-branch-merged.mjs). Implemented all 6 topics
  (tenantStatus, project, msChanges, diagnostics, statusReports, configState)
  in shanebot-engine.ts's buildCustomerContext, wired support-chat.ts's
  resolveCardData switch + DATA CARDS bullet list. Both judgment calls
  resolved explicitly and documented in the commit message: project reuses
  the dashboard's own multi-project query (0/1/many all fall out of the real
  query; states honestly rather than picking one when >1 — confirmed 0
  active projects on the testbed tenant today); configState confirmed
  against the real config-state-views.ts shape (readableFraction as the
  coverage-percentage proxy, WorkloadRollup matching the design's real
  MicrosoftGraph numbers exactly — confirmed 0 sealed snapshots on the
  testbed tenant today). Fixed test-mock fallout from buildEnginesResponse's
  transitive AI-client import chain (mocked the whole portal-mission-control.ts
  module in both shanebot-engine.test.ts and support-chat.test.ts, same
  pattern support-chat.test.ts already used for listRemediableOffers) and
  added a `.catch()` guard around the new resolveCustomerUserIds() call.
  Verification: full TypeScript typecheck clean; 62 shanebot-engine unit
  tests + 31 support-chat unit tests pass (2 pre-existing #362 call-count
  assertions updated 14→17 for Batch C's 3 new unconditional queries); new
  shanebot-engine-batch-c.live-db.test.ts (6 tests) live-verifies all 6
  topics against real Postgres for the testbed tenant
  (mccawsoft2.onmicrosoft.com, tenants.id=1). Full regression: both prior
  live-db suites (Batch A + B) + public-chat.test.ts + admin-public-chat.test.ts
  + portal-mission-control.test.ts all still pass — 162 tests across 8 files
  total, no regressions from any batch. No findings filed — no genuine
  bugs/gaps discovered outside this issue's own scope during the build.
  Working tree clean (`git status --porcelain` empty) before this bookend.
