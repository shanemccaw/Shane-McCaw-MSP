# Build Journal — #2976

- **Issue:** #2976 — URGENT: manual refresh button lies — "Refreshed!" toast fires before the real Git Board/Batter Up refresh actually completes
- **Status:** ✅ DONE 2026-09-06T14:33:59Z
- **Scope:** buildconsole
- **Branch:** agent/2976-q1745
- **Fix commit:** 29866f84991485b38dfd382e3237e6514eb140fd (on main)

## Log
- 2026-09-06T14:22:00Z — ⏳ IN FLIGHT — Confirmed root cause + found additional real contributors. Three un-awaited/no-op boundaries: (1) `FullGitRefreshRequested?.Invoke()` in `BtnRefreshGitHubTiles_Click` is fire-and-forget; (2) `BoardRefreshCompleted?.Invoke()` cascades Batter Up/AI Batter Up refresh via `async void` handlers, each doing its OWN independent GitHub fetch, also un-awaited; (3) `if (_refreshing) return;` in both panels silently no-ops overlapping refreshes — a direct "2-3 clicks" contributor. Fixing all three.
- 2026-09-06T14:33:59Z — ✅ DONE — Shipped in commit `29866f8` (merged to main, ancestor-verified). BuildConsole builds clean (0 warnings, 0 errors).
  - `FullGitRefreshRequested` changed from fire-and-forget `EventHandler` to an awaitable `Func<Task>` (single subscriber, assigned with `=`); `BtnRefreshGitHubTiles_Click` folds `fullGitRefresh?.Invoke()` into its `Task.WhenAll`, so the success toast + button re-enable only fire once the whole cascade genuinely completes.
  - MainWindow's handler now explicitly `await`s `_batterUpPanel.RefreshAsync()` + `_aiBatterUpPanel.RefreshAsync()` after the board fetch (the board cascade alone did not cover them — each Batter Up panel does its OWN GitHub fetch fire-and-forget off `BoardRefreshCompleted`).
  - `BatterUpPanel.RefreshAsync` + `AiBatterUpPanel.RefreshAsync` now COALESCE onto one in-flight task (`_inFlightRefresh`, replacing the `_refreshing` bool no-op guard): overlapping callers await the same real refresh instead of getting a completed no-op. This kills the "2-3 clicks" drop AND makes the explicit awaits above meaningful.
  - Additional investigation: `RefreshGitBoardWithLoadingFeedbackAsync` is NOT newly broken (one board GraphQL fetch + blocked-by sweep = genuine network time). Real "takes a long time to populate" cause = 3 SERIAL GitHub round-trips per refresh (board, then Batter Up, then AI Batter Up, each fetching independently) — filed as finding **#2982**. Batter Up DOES render fresh data immediately on completion (RenderFilteredRows/SetCount run synchronously before the task completes) — no separate redraw gap.
  - Verification: build only (0/0). Per repo policy never launch BuildConsole.exe to verify (single-instance mutex could courier into Shane's live session). Logic traced end-to-end.
  - Files: `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`, `desktop/BuildConsole/Controls/BatterUpPanel.xaml.cs`, `desktop/BuildConsole/Controls/AiBatterUpPanel.xaml.cs`, `desktop/BuildConsole/MainWindow.xaml.cs`.
  - Findings filed: **#2982** (bug, milestone v1.1) — 3-serial-fetch latency. Could NOT parent under #1202 (at GitHub's 100-sub-issue hard cap — flagged on #2976). Board status "AI Batter Up" for #2982 deferred — GraphQL secondary rate-limit active this session (REST unaffected; comment/labels/finding all landed via REST).
  - Labels: #2976 flipped to `complete` (in-flight removed) via REST.
