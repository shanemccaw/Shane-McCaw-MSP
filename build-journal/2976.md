# Build Journal — #2976

- **Issue:** #2976 — URGENT: manual refresh button lies — "Refreshed!" toast fires before the real Git Board/Batter Up refresh actually completes
- **Status:** ⏳ IN FLIGHT 2026-09-06T14:22:00Z
- **Scope:** buildconsole
- **Branch:** agent/2976-q1745

## Log
- 2026-09-06T14:22:00Z — ⏳ IN FLIGHT — Confirmed root cause + found additional real contributors. Three un-awaited/no-op boundaries: (1) `FullGitRefreshRequested?.Invoke()` in `BtnRefreshGitHubTiles_Click` is fire-and-forget; (2) `BoardRefreshCompleted?.Invoke()` cascades Batter Up/AI Batter Up refresh via `async void` handlers, each doing its OWN independent GitHub fetch, also un-awaited; (3) `if (_refreshing) return;` in both panels silently no-ops overlapping refreshes — a direct "2-3 clicks" contributor. Fixing all three.
