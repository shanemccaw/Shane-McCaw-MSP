# #2787 — Log Viewer: port LogPeekPanel into the wrench menu's Log Peek tool

- **Status:** ✅ DONE 2026-09-04T03:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `75dfcacbd` (IN FLIGHT bookend), `5a8b63d7d` (LogPeekView port + wiring)

## Log
- 2026-09-04T03:10:00Z ⏳ IN FLIGHT — confirmed #2785 (LogService core) and #2783 (generic
  IChatSendableTool rail-header contract) both landed on `origin/main` (labels `complete`,
  DONE bookends read). Read ShaneBuilder's real `LogPeekPanel` source
  (`MainWindow.xaml:1284-1340`, code-behind `MainWindow.xaml.cs:7489-8520`) and BuildConsole's
  real landed `LogService.cs` (#2785), `IChatSendableTool.cs` + `ChatDocumentContainer` rail
  wiring (#2783), and `LogViewerDocumentView` (#2786) for the real conventions to port against.
  About to add `Controls/LogPeekView.xaml(.cs)` and wire it into `ChatDocumentContainer`'s
  "logs" wrench-menu tool slot.
- 2026-09-04T03:35:00Z ✅ DONE — shipped `Controls/LogPeekView.xaml(.cs)`: source chips,
  COLD/BURST(30s self-cancel)/LIVE stream switch, search box, checkbox-per-line list reading
  the real `LogService` contract (#2785). Implements #2783's `IChatSendableTool` —
  checked lines are the real "pin for send" gesture, sent as one fenced log block through the
  rail header's already-generic Send-to-Chat icon, zero per-tool rail wiring needed.
  `ChatDocumentContainer` caches one `LogPeekView` per chat tab (same #2769 precedent as
  `_terminalView`) and wires it into `OpenTool` for `id == "logs"`, replacing the honest stub.
  Wired `RailMaximize` for "logs" → `MainWindow.OpenLogViewerTab()` (the real bridge to the
  full Log Viewer document, #2786) and gated it to only show for tools with a real wired
  destination — removes a previously dead icon on every other stub tool.
  **Verification:** `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. No
  live WebView2/BuildConsole instance reachable from this session (never launch BuildConsole.exe
  per project rules); build-level verification + code-path review against the already-proven
  `LogService`/`IChatSendableTool` contracts is the honest verification here, same standard
  #2783's own build used. No fixture/hardcoded data. `verify-branch-merged.mjs` confirms
  `bf55200ad` merged into `origin/main`. Working tree clean. No findings filed this session —
  the one pre-existing gap noticed (every wrench-menu tool row's glyph is an empty string,
  `ChatDocumentContainer.xaml.cs:80-91`) is a cosmetic, repo-wide pre-existing gap affecting all
  12 tools equally, not something this build's scope introduced or needs to fix.
