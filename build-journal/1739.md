# #1739 — Projects: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T13:35:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 2ee6ac1cffb7de7b5bb7accce06f6d36586c1614

## Log
- 2026-09-14T13:18:43Z ⏳ IN FLIGHT — Investigation done: pulled #1739 (title/body/comments), #1570
  (Feature history — three subsystems on one `projects` table, deferred to v1.2 on 2026-09-13 but
  re-dispatched for this specific wire step on 2026-09-14, treating the later same-author comment
  as controlling), confirmed #1738/#1737 closed, read the design export
  (`Design/portal/design_handoff_billing_roles_and_new_modules/screens/Projects.dc.html` + its
  README) and `docs/portal/projects-contract-pack.md`. Read the real route
  (`artifacts/api-server/src/routes/portal-projects.ts`) and schema (projects, workflow_steps,
  kanban_tasks, project_updates, documents, contracts, status_reports, project_closures). Found the
  real route's response has no `closure` field at all, so the design's real "closure requested" dead
  end can't be drawn without extending the read — planning to add it (existing table, no migration,
  matches the HARD RULE's "extend the route to serve the field" step, not a fixture). Next: build
  `project-detail.tsx` + API/types + SSE hook, wire route `/projects/:id`, verify.
- 2026-09-14T13:35:00Z ✅ DONE — Shipped in `2ee6ac1cffb7de7b5bb7accce06f6d36586c1614`
  (rebased onto origin/main, pushed directly — `verify-branch-merged.mjs` confirms ancestor
  of origin/main). Built `artifacts/portal/src/pages/project-detail.tsx` +
  `lib/projects-{types,api}.ts` (react-query fetch + a `kanban-events` SSE subscription that
  patches the cached tasks array on created/updated/deleted, mirroring admin-panel's
  `ProjectDetail.tsx` pattern), wired at `/projects/:id` in `App.tsx` (no sidebar entry — no
  server list route, link-only per the design/dispatch). Extended
  `artifacts/api-server/src/routes/portal-projects.ts` to also select from the existing
  `project_closures` table and return a `closure` field (no migration — table/columns already
  existed, the route simply never read them); without it the design's real
  closure-requested/#4025 dead-end banner had no field to key off. Wrote/registered
  `test-manifests/portal/projects.json` (auth-gate + real 404 apiTests, honest
  sign-in-required uiStep — no portal login page yet, same gap other portal manifests
  document). Verification: `tsc --noEmit` clean on both `artifacts/portal` and
  `artifacts/api-server`; `vite build` succeeds on `artifacts/portal`; direct query against
  local `DATABASE_URL` confirms `projects` and `project_closures` are both 0 rows right now,
  matching the contract pack and making the manifest's 404 assertion the real current answer.
  `shaneapp://runTest` was attempted for the new manifest — real result:
  `"manifest not found or unparseable: portal/projects.json"`, the same documented
  not-yet-merged-into-the-shared-checkout gap other portal manifests (e.g.
  `test-manifests/portal/offboarding.json`) hit; not re-attempted further per that precedent.
  `git status --porcelain` clean at finish. No new GitHub findings filed — the one gap found
  (missing `closure` field) was normal build-forward work per the HARD RULE, not a defect to
  file. Noted for the next session: #1570 (the Feature) was deferred to v1.2/Backlog on
  2026-09-13, then this specific wire issue was re-dispatched with a landed design on
  2026-09-14 by the same author — treated the later, issue-specific instruction as
  controlling and built it; flagging the sequencing in the issue comment rather than silently
  proceeding.
