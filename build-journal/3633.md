# #3633 — portal-checkout.ts: same "accepted before charge" bug as #3400 (customer self-serve)

- **Status:** ✅ DONE 2026-09-12T19:10:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** b8ff89193

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — read #3633 + #3400 (closed, fix landed in 749c8c404). Mirroring #3400's `recordAcceptedOffer()`-after-charge pattern into `artifacts/api-server/src/routes/portal-checkout.ts` Branch 3 (paid add_on/subscription self-serve checkout).
- 2026-09-12T19:10:00Z ✅ DONE — Extracted `markOfferAccepted()` helper; moved its call site in each of the 3 checkout branches to after real success (free: after rate-limit gates; project: after SOW row created; paid Stripe: only after charge confirmed succeeded, wrapped in its own try/catch so a post-charge marking failure doesn't produce a false "payment failed" response). Added 3 regression tests (missing default payment method, failed PaymentIntent, thrown Stripe error) asserting `db.update` is never reached, plus a positive assertion it IS called on a real success. `tsc --noEmit` clean; `vitest portal-checkout.test.ts` 14/14 passing. Branch verified merged into origin/main (`verify-branch-merged.mjs` OK). Working tree clean. Commit: b8ff89193.
