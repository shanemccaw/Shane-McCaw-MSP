# #4575 — Real per-tick diagnostic logging for stuck memory-pressure pause

- **Status:** ✅ DONE 2026-09-17T22:35:00Z
- **Scope:** buildconsole

## Log
- 2026-09-17T22:20:00Z — Starting. Scope is logging-only per issue body: add real per-tick
  `ActivityLog` output inside `EvaluateMemoryPressure` (`MainWindow.ResourceMonitor.cs`) while
  `watcher.IsMemoryPressurePaused` is true, so the next stuck episode produces real diagnostic
  data instead of a third blind fix guess.
- 2026-09-17T22:35:00Z — ✅ DONE — Added one `ActivityLog.Log("resource-monitor", ...)` line inside
  `EvaluateMemoryPressure`'s already-auto-paused branch, firing every 1.5s tick while
  `watcher.IsMemoryPressurePaused` is true. Logs `dwMemoryLoad`, `pagefileInUse`,
  `oldest`/`newest`/`prev`, `pagefileRising`, `pagefileNotRising` (post-#4561 tolerance), `ramLow`,
  `clearNow`, and `memoryPressureClearSince` (null or elapsed seconds). No behavioral change — pure
  logging addition, per the issue's explicit scope. `dotnet build` of BuildConsole.csproj (Debug):
  0 warnings, 0 errors. Commit `ee45296cc` (rebased onto main as `06e4b2359`), pushed directly to
  `origin/main`. `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
