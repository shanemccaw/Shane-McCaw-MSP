# #2928 — Marketing dev server: @simplewebauthn/browser unresolved import in Buy.tsx

- **Status:** ✅ DONE 2026-09-05T01:23:11Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** (bookend-only; no source change needed — see Log)

## Log
- 2026-09-05T01:09:31Z ⏳ IN FLIGHT — diagnosing whether `@simplewebauthn/browser` is a genuine missing
  workspace dependency for `artifacts/shane-mccaw-consulting` or a dangling/misdirected junction in the
  `C:\dev-server` checkout.
- 2026-09-05T01:23:11Z ✅ DONE — confirmed it is the Git #1987/#1988 junction class, not a missing
  dependency:
  - `artifacts/shane-mccaw-consulting/package.json` already lists `"@simplewebauthn/browser": "^13.3.0"`
    (line 83), and `pnpm-lock.yaml` correctly locks it (`@simplewebauthn/browser@13.3.0`) — the real
    dependency graph is correct, nothing to add there.
  - The main checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) has the correct per-package
    junction: `artifacts/shane-mccaw-consulting/node_modules/@simplewebauthn/browser` → pnpm store
    (`node_modules/.pnpm/@simplewebauthn+browser@13.3.0/...`).
  - The `C:\dev-server` checkout (a per-package-linked host per Git #2152, since
    `artifacts/shane-mccaw-consulting` holds an `@workspace` scope) was simply **missing** that one
    junction — not foreign, not dangling, just never created. `scripts/dev-server/store-doctor.mjs
    --root C:\dev-server` confirms the rest of that host's links are otherwise fine for this package;
    it doesn't flag absent-entirely links (only foreign/dangling), so it correctly reported clean for
    this one. `scripts/dev-server/link-deps.mjs`'s `linkDeps()` also can't backfill it — it skips a
    host entirely once `node_modules` already exists there (`if (existsSync(link)) continue`), so a
    package added to `package.json` after the initial per-package link never gets picked up
    automatically.
  - **Fix applied:** manually created the missing junction, matching the exact pattern of every
    sibling entry in that host:
    `mklink /J C:\dev-server\artifacts\shane-mccaw-consulting\node_modules\@simplewebauthn\browser
    C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\artifacts\shane-mccaw-consulting\node_modules\@simplewebauthn\browser`
    Verified `package.json` resolves through the new junction.
  - Cross-checked every other dependency in that package's `package.json` against `C:\dev-server`'s
    node_modules — no other missing junctions found, so this was an isolated one-off, not a wider
    drift.
  - No `pnpm install` was run anywhere (per Git #1987) — this is not a lockfile/dependency-resolution
    problem, and one would not have fixed it.
  - No `.tsx`/package.json changes were needed in this repo (the dependency declaration was already
    correct), so there is nothing new to push from this worktree beyond this bookend.
  - **Real finding filed:** `linkDeps()`'s per-host `existsSync(link)` short-circuit means any package
    added to a per-package-linked host's `package.json` (Git #2152 hosts:
    `artifacts/{api-server,admin-panel,portal,msp-website,shane-mccaw-consulting}`, `lib/dashboard-canvas`,
    `scripts`) after that host's `node_modules` was first junctioned will silently 404 in every worktree
    and in `C:\dev-server` until someone notices and manually junctions it, exactly as happened here.
    Filed as **#2947**, parented as a direct sub-issue of #1128 (the Feature-tier parent of #2928),
    labeled `bug`, milestone v1.1, board status "AI Batter Up".
