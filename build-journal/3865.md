# #3865 — Priority build sets must actually affect real claim order

- **Status:** ✅ DONE 2026-09-12T22:45:50Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 423750b0a (rebased onto main as 3ad832cda)

## Log
- 2026-09-12T22:38:38Z ⏳ IN FLIGHT — investigated `BuildSetPriorityStore` (Git #1636) and
  `BuildQueuePostgresClient.SelectClaimCandidatesAsync`/`QueuedCandidateSql`. Confirmed both
  `GetNextAsync` and `PeekNextAsync` already funnel through the one shared
  `SelectClaimCandidatesAsync` method, so a single priority-aware sort inserted there — right
  after the `QueuedCandidateSql` fetch, before `EvaluateCandidatesAsync` — covers both the real
  claim path and the read-only peek identically by construction. About to implement: stable
  sort splitting candidates into priority/non-priority tiers via
  `BuildSetPriorityStore.IsPriority(item.BuildSet)`, preserving `created_at ASC` within each
  tier (candidates already arrive in that order from the SQL `ORDER BY`).
- 2026-09-12T22:45:50Z ✅ DONE — implemented the stable partition in
  `SelectClaimCandidatesAsync` (commit 3ad832cda on main) right after the `QueuedCandidateSql`
  fetch, before `EvaluateCandidatesAsync`. Because both `GetNextAsync` and `PeekNextAsync`
  already route through this one shared method, the sort applies identically to the real claim
  path and the read-only preview by construction — no duplicated logic. Also added a visible
  "★ priority" badge on the Next-To-Run preview row (`BuildQueuePanel.MakeNextRow`) so a
  priority-marked wait doesn't look identical to a plain FIFO wait (issue's observability ask).
  `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors.
  **Real, live verification** against the actual local Postgres `BuildConsole` database (not
  simulated): inserted two genuinely ready, unblocked queued rows (--notGit negative
  `github_number` sentinel, no blockers) in two different build sets, one queued ~10 minutes
  before the other. With `freeSlots` capped to 1 (the real concurrency-constrained case the
  issue calls out) and the later-queued build set marked Priority via
  `BuildSetPriorityStore.SetPriority`, `GetNextAsync(1, ...)` claimed the PRIORITY row, not the
  older FIFO row. Un-marking Priority and re-running claimed the older FIFO row as before.
  `PeekNextAsync`'s preview order matched the real claim order in both the priority-on and
  priority-off cases. Test rows deleted after verification — confirmed 0 rows remain
  (`SELECT ... WHERE title LIKE 'TEST-3865%'` → 0 rows). No fixture/hardcoded data introduced;
  scratch verification harness (`C:\tmp\verify3865`) was outside the repo and deleted.
  Branch-merged check: `node scripts/dev-server/verify-branch-merged.mjs` → OK, exit 0.
  `git status --porcelain` clean except this bookend file itself.
  No findings filed this session — the two disconnected pieces described in the issue were
  exactly as described; no additional bugs surfaced.
