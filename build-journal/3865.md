# #3865 — Priority build sets must actually affect real claim order

- **Status:** ⏳ IN FLIGHT 2026-09-12T22:38:38Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T22:38:38Z ⏳ IN FLIGHT — investigated `BuildSetPriorityStore` (Git #1636) and
  `BuildQueuePostgresClient.SelectClaimCandidatesAsync`/`QueuedCandidateSql`. Confirmed both
  `GetNextAsync` and `PeekNextAsync` already funnel through the one shared
  `SelectClaimCandidatesAsync` method, so a single priority-aware sort inserted there — right
  after the `QueuedCandidateSql` fetch, before `EvaluateCandidatesAsync` — covers both the real
  claim path and the read-only peek identically by construction. About to implement: stable
  sort splitting candidates into priority/non-priority tiers via
  `BuildSetPriorityStore.IsPriority(item.BuildSet)`, preserving `created_at ASC` within each
  tier (candidates already arrive in that order from the SQL `ORDER BY`).
