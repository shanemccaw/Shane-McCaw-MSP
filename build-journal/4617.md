# #4617 — Real creation path for `portal_hold_windows`

- **Status:** ✅ DONE 2026-09-18T05:25:00Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** 332cb5e2a (plan), c0a86dd9a (IN FLIGHT bookend), 985e8066f (real implementation, rebased/pushed as 5d893dbbd on main)

## Log
- 2026-09-18T02:20:00Z ⏳ IN FLIGHT — plan written (build-journal/4617-plan.md,
  commit 332cb5e2a). Implementing the real hold-window trigger on the
  already-shipped "Convert to Private" overshare-fix runbook (30-day notice
  period already promised in its own step copy). Filing a sibling Ask-Shane
  issue for the two SOPs (`reduceAdmins`, `manageGuests`) that imply the same
  kind of wait-then-decide gate but have no concrete day count anywhere in
  code or design copy.
- 2026-09-18T05:25:00Z ✅ DONE — See ## Changes Made / ## Verification Results
  below.

## Changes Made

- `artifacts/api-server/src/lib/portal-runbook-hold-triggers.ts` (new) — a
  real trigger catalogue keyed by `runbookKey`, one entry:
  `oversharing-convert-to-private` (step 1 checked → raises a real 30-day
  hold window gating step 4). Documents, in the file header, why CA-policy
  report-only writes and the other two oversharing SOPs were deliberately
  NOT wired (see build-journal/4617-plan.md's "User Review Required").
- `artifacts/api-server/src/lib/portal-runbook-wire.ts` — added
  `maybeRaiseHoldWindowForStep`, colocated with the existing
  `maybeAdvanceCycle`. Inserts a real `portal_hold_windows` row via
  `.onConflictDoNothing` against the existing `(customer_id, hold_key)`
  unique index (idempotent — a re-tick never duplicates or resets the
  clock).
- `artifacts/api-server/src/routes/portal-runbooks.ts` and
  `artifacts/api-server/src/routes/msp-runbooks.ts` — both step-tick routes
  (`PUT .../steps/:position`) now call `maybeRaiseHoldWindowForStep`
  alongside the existing `maybeAdvanceCycle`, same `if (checked)` branch.
- `artifacts/api-server/src/scripts/verify-runbook-hold-trigger-4617.ts`
  (new) — the live verification script (real testbed tenant, real local
  Postgres), kept per the "keep diagnostic scripts" convention.
- **No schema/migration change** — `portal_hold_windows` already had every
  column this needed.
- **No fixture/hardcoded display data.** The row is created by a real user
  action (a real step tick on a real, already-shipped runbook) and read back
  through the existing real `GET /portal/runbooks` path.
- Filed **#4662** (Ask Shane lane, parented under #1485 — #1489 is at
  GitHub's 100-sub-issue cap, confirmed via `sub_issues_summary` before
  falling back) for the `reduceAdmins`/`manageGuests` wait-day ambiguity.

## Verification Results

- `pnpm run typecheck` (artifacts/api-server) — clean for every file this
  build touched. One pre-existing, unrelated failure
  (`ca-hold-window-scan.test.ts` — `ReportOnlyImpactSummary` missing
  `byCategory`) is already tracked as **#4661**, filed before this session
  started; not re-filed.
- `npx vitest run src/lib/portal-runbook-cycles.test.ts
  src/lib/__tests__/portal-oversharing-sites.test.ts
  src/lib/portal-hold-windows.test.ts` — 51/51 passed, no regressions.
- **Live**, against the real testbed tenant (customer 2080, McCawSoft) and
  the real local Postgres — `node run-script.mjs
  src/scripts/verify-runbook-hold-trigger-4617.ts` — ALL CHECKS PASSED:
  - Seeded a real `oversharing-convert-to-private` runbook/cycle/5 steps for
    customer 2080.
  - Ticked step 1 → a real `portal_hold_windows` row was created
    (`id=37` in this run — cleaned up after), `waitDays=30`,
    `gatesStepPosition=4`, `pillar=governance`, `scanVerdict=watch`.
  - The real customer-facing wire (`loadRunbooksForCustomer`, what
    `GET /portal/runbooks` actually returns) showed the window:
    `state: "running"`, `tMinus: "T-29d"`, `daysLeft: 29`.
  - Re-ticking step 1 again created no duplicate row (still exactly 1).
  - Ticking step 2 (not the trigger) raised nothing new.
  - Scratch rows cleaned up at the end of the run.
- `git status --porcelain` — clean (all touched files committed).
- `node scripts/dev-server/verify-branch-merged.mjs` — OK, `5d893dbbd`
  merged into `origin/main`.
