# #1428 — Governance: :area wildcard (governance-sharing-drift) — replace fabricated stat cards/evidence table with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 633ea76b7

## Log
- 2026-08-27 ⏳ IN FLIGHT — Follow-up from #1413 audit. `PortalV2GovDetailPage` (`:area` wildcard route, only real key `governance-sharing-drift`) renders stat cards + evidence table straight from fixture with zero `dataState` gating, and no live counterpart exists anywhere for per-sub-area evidence rows (confirmed in #1413). Stripping the fabricated content and replacing with an honest explicit "No live data available" / "No scan data yet" state, per Shane's direction.
- 2026-08-27 ✅ DONE — `artifacts/msp-portal/src/pages/portal-v2-gov-detail.tsx` (commit `633ea76b7`): stat cards and the evidence table now gate on `useLivePillarHero("governance").dataState` — the same signal the page's own `pv2-gd-source` marker already reports — rather than rendering unconditionally. While `dataState === "live"` (this tenant genuinely scored on governance), a real customer sees the platform's honest `NoScanDataState` (`pv2-gd-stats-no-data` / `pv2-gd-table-no-data`, "No live data available" / "No scan data yet") instead of the fictional "23 drift events" worked example; the fixture only renders while genuinely unscored, the same honest-null case the hero score itself already falls back for. Matches Change Control's Git #1364 `dataState === "live"` precedent rather than the narrower Shane-only `isTestbed` QA flag, so the fix is provable on the same shared testbed tenant the regression manifest already logs into. Heading/purpose/note copy, risks list, policy block and provenance are untouched — out of this issue's explicit scope (stat cards + evidence table only). Added `test-manifests/portal/portal-v2-pillars.json` steps G6b/G6c asserting the honest-empty testids render on the live-scored testbed session; bumped its `lastVerifiedAgainstCommit` to `633ea76b7`. Verification: `tsc --noEmit` clean on the touched file (2 pre-existing unrelated errors elsewhere in the package, confirmed not touched by this diff); `PORT=5173 vite build` clean (2m44s, 5862 modules). **Not live-run**: the `shane-msp` MCP server (shaneapp://runTest's courier) failed to connect (CONNECTION_CLOSED) for the entire session, so G6b/G6c are unconfirmed against the real deployed DOM — flagged honestly in the manifest's own notes and this issue's GitHub comment for the next session to run live.
