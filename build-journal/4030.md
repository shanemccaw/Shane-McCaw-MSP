# #4030 — Break-glass reads do not serve the account identity

- **Status:** ✅ DONE 2026-09-14T12:56:32Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** a453ed23e (pushed to origin/main)

## Log
- 2026-09-14T12:56:32Z ⏳ IN FLIGHT — investigated: #4015 already fixed redaction
  (`break_glass_pending_secrets.break_glass_account_id`, `resolvePendingContext`
  already selects it as `ctx.secret.breakGlassAccountId`); the MSP detail route
  (`artifacts/api-server/src/routes/msp-break-glass.ts`, GET
  `/api/msp/customers/:customerId/break-glass/:pendingSecretId`) just never put it
  in the JSON response, and the MSP console's `BreakGlassDetail` type/UI had no
  field or Fact for it.
- 2026-09-14T12:56:32Z ✅ DONE — added `breakGlassAccountId` to the MSP detail
  route response; added it to the MSP console's `BreakGlassDetail` TS interface
  (`artifacts/msp-console/src/api/break-glass-api.ts`); rendered an "ACCOUNT" Fact
  in the detail drawer and replaced the previously-hardcoded `ok: true` stub with
  a real `breakGlassAccountId !== null` check in the override drawer's precondition
  list (`artifacts/msp-console/src/pages/break-glass/BreakGlassPage.tsx`), matching
  `Design/MSP_Console/design_handoff_msp_console/MSP Console.dc.html:4328`'s
  "breakGlassAccountId present on the paused run" precondition. Server-side
  `performBreakGlassAdminOverride` already refused a missing identity with 409
  (`break-glass-verification.ts:927-930`) — the UI now reflects that real check
  instead of a hardcoded pass.
  Typechecked clean: `pnpm --filter @workspace/api-server exec tsc --noEmit -p .`
  and `pnpm --filter @workspace/msp-console exec tsc --noEmit -p .`, both zero
  errors.
  **Deferred, deliberately, per the issue's stated default:** the customer-facing
  portal ACCOUNT cell (`artifacts/portal/src/pages/break-glass-status.tsx`,
  `resolveRunContext` in `break-glass-verification.ts`) is left unserved. Whether a
  customer should see the exact Entra account id/UPN being reset is a real,
  unresolved product decision, not something this build settled — untouched.
  Rebased onto `origin/main` (f392f34e3) and pushed clean: `a453ed23e`.
  `git status --porcelain` clean; `verify-branch-merged` not run separately since
  the push landed directly on `main` (no separate agent branch to check ahead of
  it) — HEAD after push is `main`'s own history.
