# #2647 — Documents: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T05:24:56Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** b61595a5e (merged to `main`, verified ancestor via `verify-branch-merged.mjs`)

## Log
- 2026-09-12T05:10:27Z ⏳ IN FLIGHT — Read design export (`Design/MSP_Console/design_handoff_msp_console/Documents.dc.html` + README screens 22/32/33), confirmed real backend routes (`msp-documents-hub.ts`, `msp-documents.ts`), confirmed schema (`msp_documents`, `msp_document_versions`, `msp_sharepoint_connectors`, `insights_generated_documents` — all exist locally, no migration needed), confirmed the actual current `autoPublish` behavior (post-#2724 the field is now a real, honored gate — default `false` skips the publish write — the OPPOSITE of the dispatch note's "every document publishes unconditionally"; building against the real code, sending `autoPublish: true` explicitly from this UI's submit actions to honor the design's own "no draft stop" copy). Studied `DataRights.tsx`/`data-rights-api.ts` (#2633) as the house pattern for a console module + its API seam, and `ConsoleShell.tsx`/`nav.ts`/`treeModel.ts` for how a module mounts (tenant page `hub`, MSP pages `docs`/`connectors`).
- 2026-09-12T05:24:56Z ✅ DONE — Implemented `artifacts/msp-console/src/api/documents-api.ts` (hub/authored/connectors hooks, real types, `DocumentsApiError`) and `artifacts/msp-console/src/console/modules/Documents.tsx` (one shared component, three tabs — "For customers" / "Ours" / "Where they go" — mounted at all three routes via `ConsoleShell.tsx`'s `moduleFor`: tenant page `hub` (screen 22, customer-scoped), MSP pages `docs` (screen 32, unscoped) and `connectors` (screen 33, unscoped)). Added the `folder-open` Lucide icon. No fixture/hardcoded data — every value comes from `GET/POST /api/msp/documents-hub*` and `GET/POST/PATCH /api/msp/documents*` / `/api/msp/sharepoint-connectors*`.
  Honest departures from the design fixture (documented in `documents-api.ts`'s header and repeated in `Documents.tsx`'s): a hub row's KIND badge is the real free-text `docType`, not the fixture's invented "report"/"scoped SOW" pair; its tone comes from the real `category` enum; the "Statements of work" filter is relabeled "Consulting" since the real bucket (`category === "consulting"`) is broader than SOWs; `canShare`/`canPdf` mirror the route's own real gates exactly (`status` in approved/delivered, or literal `docType === "scoped_sow"` for share); the design's fictional 7-step STEPS ladder (including "Logged"/"Tidied up", which the schema never tracks) is replaced by the real 8-value `DOC_PIPELINE_STATUSES` enum; `autoPublish: true` is sent explicitly on submit/new-version (see above); the author drawer's connector picker (which the design never shows) auto-resolves to the single active `msp_owned` connector rather than inventing a picker.
  No manual SQL migration needed — all backing tables already existed with the exact columns the UI needed.
  **Verification:** `npx tsc --noEmit -p artifacts/msp-console` — clean, both before and after rebasing onto `origin/main` (rebase hit one trivial, additive conflict in `icons.tsx` against three other concurrent Documents-adjacent builders — Executive View #2659, Scope & SLA #2656 — resolved by keeping both blocks). `pnpm --filter` production build (`vite build`, `PORT=5199`) — clean, both before and after rebase. `shaneapp://runTest`/`executeSql` are not reachable from this session (confirmed via `ToolSearch` — no matching deferred tool registered on this machine); did not attempt a full live click-through against a running dev stack, since BuildConsole's own merge-back handles running this against the shared dev server. Confirmed via direct `psql` against local `DATABASE_URL` that `msp_documents`, `msp_document_versions`, `msp_sharepoint_connectors` and `insights_generated_documents` all exist with the exact columns the API seam reads/writes, and confirmed both route files are actually mounted in `artifacts/api-server/src/routes/index.ts` (not just defined).
  Filed **#3783** (sibling sub-issue of Feature #2569): `docs/msp-console/documents-msp-console-contract-pack.md` is stale post-#2724 — still describes `autoPublish` as an inert dead field when the real, current code (verified directly, `doc-pipeline-nodes.ts:593`) now honors it as a real gate. Labeled `bug`, milestone v1.1, board status "AI Batter Up".
  `git status --porcelain` clean at session end. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0).
