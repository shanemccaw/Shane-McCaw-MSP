# #2647 — Documents: wire the MSP Console UI to the real endpoints

- **Status:** ⏳ IN FLIGHT 2026-09-12T05:10:27Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T05:10:27Z ⏳ IN FLIGHT — Read design export (`Design/MSP_Console/design_handoff_msp_console/Documents.dc.html` + README screens 22/32/33), confirmed real backend routes (`msp-documents-hub.ts`, `msp-documents.ts`), confirmed schema (`msp_documents`, `msp_document_versions`, `msp_sharepoint_connectors`, `insights_generated_documents` — all exist locally, no migration needed), confirmed the actual current `autoPublish` behavior (post-#2724 the field is now a real, honored gate — default `false` skips the publish write — the OPPOSITE of the dispatch note's "every document publishes unconditionally"; building against the real code, sending `autoPublish: true` explicitly from this UI's submit actions to honor the design's own "no draft stop" copy). Studied `DataRights.tsx`/`data-rights-api.ts` (#2633) as the house pattern for a console module + its API seam, and `ConsoleShell.tsx`/`nav.ts`/`treeModel.ts` for how a module mounts (tenant page `hub`, MSP pages `docs`/`connectors`). Next: implement `documents-api.ts` + `Documents.tsx` and wire all 3 screens.
