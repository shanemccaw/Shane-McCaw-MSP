# #3569 — DispatchDialog: custom dark title bar (close-only)

- **Status:** ✅ DONE 2026-09-10T22:40:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T22:26:00Z — Started. Applying WindowChromeHelper pattern (Git #1006) to DispatchDialog.xaml, close-only variant matching ResizeMode=NoResize per issue #3569.
- 2026-09-10T22:40:00Z — ✅ DONE — Added `WindowStyle="None"` + `WindowChrome.WindowChrome` (CaptionHeight=32, ResizeBorderThickness=0, matching NoResize) to DispatchDialog.xaml; restructured root into a `DockPanel` with a custom `CrustBrush` title-bar `Border` (title "Dispatch", one `CaptionButtonClose`-styled Close button, no min/max) above the existing `ContentHost`. `DispatchDialog.xaml.cs` calls `WindowChromeHelper.Setup(this)` from `OnSourceInitialized` and wires `BtnCloseWindow_Click` to `Close()`; existing `Window_KeyDown` Escape-to-close left untouched (no conflict — both just call `Close()`). Build verified clean (`dotnet build BuildConsole.csproj`, 0 errors, pre-existing unrelated warnings only). Commit `4ccce30c75524cc04820e8b15712f9e0967be4eb`, merged into main, verified via `verify-branch-merged.mjs`. Working tree clean.
