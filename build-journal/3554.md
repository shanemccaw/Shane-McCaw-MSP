# #3554 — MyArchitect: real crash risk (global handler, blocking UI-thread async, unsafe async void)

- **Status:** ✅ DONE 2026-09-10T00:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** a4503dca0 (on origin/main, verify-branch-merged OK)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — Investigation done. Confirmed all 3 bugs in the issue.
  Additional audit findings: (1) `MainWindow.OnCurrentTenantChanged` (MainWindow.xaml.cs:5253)
  is a SECOND event-subscribed `async void` with no exception guard — same crash class as
  `OnTenantChanged`. (2) The blocking `.GetAwaiter().GetResult()` pattern is systemic (~70
  call sites, all in synchronous gallery/record builders invoked on the UI thread by
  `FixedRibbonRenderer.Populate`), and the real deadlock source is the small set of service
  methods that still `await` without `ConfigureAwait(false)` (Assessment/Auth/Cab/TaskQueue/
  Telemetry/ScreenshotEvidence/WebViewProfile/PowerShellConsole) while 146 other service awaits
  already use it. Planned fixes: global exception handler (App.xaml.cs), harden both async-void
  event handlers, and eliminate the UI-thread deadlock at the root via ConfigureAwait(false)
  across the service layer + make the named BuildChangeRequestRows call site non-blocking.
- 2026-09-10T00:40:00Z ✅ DONE — Shipped in a4503dca0 (rebased onto origin/main, verify-branch-merged
  OK). (1) App.xaml.cs global exception handler: DispatcherUnhandledException (log + dialog +
  Handled=true so the app survives), AppDomain.CurrentDomain.UnhandledException (log terminating),
  TaskScheduler.UnobservedTaskException (log + SetObserved). (2) New Infrastructure/CrashLog.cs
  never-throwing sink at %LOCALAPPDATA%\MyArchitect\logs\crash.log. (3) TenantModuleConnectionService
  .OnTenantChanged: whole body guarded incl. the catch's ConnectionCompleted invoke — nothing escapes.
  (4) Audit found a 2nd event-subscribed async void — MainWindow.OnCurrentTenantChanged — now guarded.
  (5) Added async GallerySpec.GetRowsAsync path (Loading→await→render) and migrated the Remediation Plan
  gallery (the issue's named line-257 GetChangeRequestsAsync call) off UI-thread-blocking
  .GetAwaiter().GetResult(). Verified: clean `dotnet build`; a real WPF dispatcher harness (temp project,
  since removed) proved an unhandled UI-thread exception is caught (process survives) and both it and a
  guarded async-void throw are logged to the real crash.log.
  CORRECTION vs the IN FLIGHT note above: the blocking-reached services (Auth/Cab/TaskQueue/etc.) already
  use ConfigureAwait(false) after all (their multi-line awaits carry it on the closing line), so there is
  NO active deadlock; the systemic residual is a UI-thread FREEZE (Not Responding → force-kill), not a
  deadlock. Full async-pipeline migration of the other ~69 blocking sites + the eager render-time
  Populate() is deliberately deferred as too large/risky for a stability build and filed as finding
  **#3564** (parented under #3454, board "AI Batter Up"). Findings filed this session: **#3564**.
  Working tree clean; branch verified merged into origin/main.
