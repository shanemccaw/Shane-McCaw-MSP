# Build Journal: #4530

- **Status:** ✅ DONE 2026-09-17T23:48:00Z
- **Scope:** platform

## Log

- 2026-09-17T23:36:20Z — Started. Pulled #4530 body + all comments, and #4516 (the
  originating fix) body + comments, in order. Queried local Postgres directly
  (`baseline_action_templates` / `baseline_action_template_audit_log`) to check
  whether a strict `expectStatus` gate would newly fail any real observed write.
  Result: 0 conflicts across the 26/124 templates with real execution history, but
  98/124 (mostly destructive) still have zero history — not safe to flip unattended.
  Wrote `build-journal/4530-plan.md` (commit `2bfab7249`) proposing shadow
  instrumentation (queryable `expectStatus`/`expectStatusMatch` audit fields + a
  structured warning log) as the safe, buildable piece this session, with the full
  strict gate explicitly flagged as needing more real coverage before it can land
  unattended.
- 2026-09-17T23:48:00Z — ✅ DONE — Landed the shadow-instrumentation piece: commit
  `f0b62f646` (pushed as `c4c959082` on `main`). Full strict gate remains deferred —
  see build-journal/4530-plan.md for the User Review Required section flagging why
  it isn't safely landable unattended this session.

## Changes Made

- `artifacts/api-server/src/lib/workflow-executor.ts`: new exported
  `declaredExpectStatus()` (extracted from `graphAcceptedStatusCodes()`, which is
  unchanged/still widen-only). The Graph pack write call site now computes
  `expectStatusMatch` (true/false/null) and:
  - stamps `expectStatus`/`expectStatusMatch` onto every
    `baseline_action_template_audit_log.after_snapshot` row that declares an
    `expectStatus` (no migration needed — `after_snapshot` is already `jsonb`);
  - logs a structured warning (`workflow.run` channel) whenever a write succeeded
    only via the widened accept class (i.e. `expectStatusMatch === false`).
  Zero change to `result.success`/`result.status` — pure additive observability.
- `artifacts/api-server/src/lib/workflow-executor-core.test.ts`: 3 new unit tests for
  `declaredExpectStatus`.
- `build-journal/4530-plan.md` (commit `2bfab7249`): full evidence — live query
  results, the 26/124-templates-with-history vs. 98/124-unverified breakdown, and
  the explicit User Review Required flag that a full strict gate is not yet safely
  landable unattended.

## Verification Results

- Live DB query against local Postgres (real `baseline_action_template_audit_log`
  data): 0 rows where a real observed status fell inside the default `[200,201,204]`
  class but did not match the template's own declared `expectStatus` — across all
  26/124 declared rows with any execution history. No live Graph writes fired.
- `npx vitest run src/lib/workflow-executor-core.test.ts` (api-server): 109/109 pass
  (was 106 pre-change; +3 new for `declaredExpectStatus`).
- `pnpm run typecheck` (api-server): clean, exit 0.
- `node scripts/dev-server/verify-branch-merged.mjs`: OK — `agent/4530-q3186`
  (`c4c95908`) is merged into `origin/main`.
- `git status --porcelain`: clean (nothing left uncommitted).
