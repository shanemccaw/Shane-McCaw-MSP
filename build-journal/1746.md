# #1746 — My Architect: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T03:45:36Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** b5e5b24c9 (IN FLIGHT bookend), 640165c51 (real work, rebased to b26113517 on origin/main)

## Log
- 2026-09-14T03:34:54Z ⏳ IN FLIGHT — Design export confirmed at `Design/portal/design_handoff_full_site/screens/My Architect.dc.html`, contract pack read (`docs/portal/my-architect-contract-pack.md` under `Design/portal/design_handoff_full_site/docs/`). Backend (`GET /api/portal/retainer`) is already real and complete (built under closed sub-issues #1582/#1583) — this issue is pure UI wiring: types + React Query hook + page + nav entry in `artifacts/portal`, zero fixtures, money fields (`hourlyRateCents`) dropped per contract pack §6.1.
- 2026-09-14T03:45:36Z ✅ DONE — Shipped `artifacts/portal/src/pages/my-architect.tsx` + `lib/my-architect-{api,types}.ts`, wired at `/my-architect`, nav entry added to `moduleNav.ts` (Compass icon, next to Overview per #1569's own body — "Projects" has no sidebar row yet to sit next to), breadcrumb added to `PortalShell.tsx`. Renders the real drawdown bucket, work-log ledger (month-filtered), sent status reports (read-only thread), and the honest unconfigured/no-scope/failed states per the contract pack — `hourlyRateCents` is served by the route and never read here. Also shipped `test-manifests/portal/my-architect.json` (registered in `_regression-suite.json`) asserting the real honest-empty contract against the local DB (`retainer_settings`/`retainer_work_log`/`status_reports` all confirmed 0 rows via direct `DATABASE_URL` query). Verification: `pnpm typecheck` and `pnpm build` both clean, twice (before and after rebasing onto a concurrent build's unrelated route additions in `App.tsx`, which merged without conflict). `shaneapp://runTest` was invoked twice against the new manifest and genuinely could not run — the local dev-server checkout BuildConsole reads manifests from is 6 commits behind `origin/main` and doesn't have this file yet (its own error text: "a stale local checkout (Git #3059)... self-resolves on the next dev-server merge-back"), not a bug in this build. Live UI/API verification is therefore deferred to the next merge-back rather than claimed here. Branch verified merged into `origin/main` (`verify-branch-merged.mjs`), working tree clean.
- Zero fixtures confirmed: no `*Data.ts` module, no hardcoded arrays; every field traces to the real `GET /api/portal/retainer` response.
- No findings filed — no genuine bug/gap discovered mid-build beyond what the contract pack already documents (documents section, customer ask-box replies — both already tracked as open, not-yet-built in the pack itself, §7).
