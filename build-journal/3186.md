# #3186 — Shared list: real can_add, aisle/store data, and live activity feed

- **Status:** ✅ DONE 2026-09-08T04:35:04Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 3af786b22

## Log
- 2026-09-08T04:05:00Z ⏳ IN FLIGHT — Shane's decision comment on #3186 locks scope: real
  `can_add` flag on share_links, real store/aisle data + Best-path ordering exposed on the
  public share shape for `can_add` links, and a real live activity feed (added_by/checked_by
  attribution + poll-based updates), scoped strictly to the one shared list. Building against
  `Shanes Life 12 - Shared list.dc.html` turn 2/option 2a, extending #3184's visual rebuild.
  Privacy boundary (Shane's second comment): share sessions must never reach anything beyond
  the one list's own data/endpoints — verify this explicitly, not just assume prior scoping
  holds.
- 2026-09-08T04:35:04Z ✅ DONE — Real, deliberately-scoped implementation (list-kind shares only —
  the only kind with any real live rows, verified: `SELECT entity_kind, count(*) FROM
  share_links` returned 2 live rows, both `list`). Migration
  `web/shanes-life/migrations/047_share_can_add_and_activity.sql` (ran myself against local
  `finances`): `share_links.can_add` (default false, so every existing/not-asked-for share keeps
  check-off-only behavior), `list_items.added_by`/`.checked_by` (real provenance, mirroring
  `entity_items.checked_by`'s existing "owner" | "share:<label>" convention), and a real
  `activity_log(entity_id, at)` index. Code: `core/shares.mjs` (canAdd param + select/return),
  `core/lists.mjs` (`addListItems`/`setListItemChecked` now thread real attribution;
  `getListForShare` returns real `checked_by`/`added_by`/`store`), `core/audit.mjs`
  (`recentForEntity` — scoped strictly to one list's own `entity_id` + `user_id`, real allowlisted
  actions), `routes/public.mjs` (new `POST .../items` add route gated on `can_add` + list-kind
  only; new `GET .../activity` poll endpoint; `orderForShare` reuses
  `core/shopping-order.mjs`/`core/store-aisles.mjs` verbatim for Category — any can_check link —
  and Best-path — can_add-enabled links only, per Shane's own decision comment tying store/aisle
  exposure to that flag), `routes/api.mjs` (owner's own check/add now also write real
  `list.item.check`/`list.item.add` audit rows with the item's real text, so the activity feed
  narrates both sides of the link, not just share-originated writes). Frontend:
  `public/share.js` rebuilt with the Flat/Category/Best-path toggle, the add box, and a
  poll-based live activity banner (5s activity poll patches the banner text in place; a slower
  10s full re-render skipped while the add box has focus, so typing is never clobbered);
  `public/app.js`'s owner-side share-creation card gets a real "Let this link add items too"
  checkbox and item rows show real "Added by <label>" attribution; `public/app.css` for the new
  add-box/activity-banner styles, reusing `.shop-segment`/`.shop-group-label` verbatim.

  **Real, live verification** (not assumed): ran my own worktree's server on a scratch port
  (5911, separate from the shared dev-server on 5000) against the real local `finances` database,
  using the pre-existing `you@example.com` test/dev account's own empty Shopping list (never
  Shane's real "Aldi run" data) — created a real `can_add` share link, exercised `?order=flat`,
  `?order=category`, `?order=best` (confirmed real aisle grouping + `aisleNote` from a real
  `store_aisles` row), `PATCH .../items/:id` (check, confirmed `checked_by: "share:Smoketest"`),
  `POST .../items` (add, confirmed `addedBy: "share:Smoketest"`), and `GET .../activity`
  (confirmed both events narrated: "Smoketest just checked off Milk" / "Smoketest just added
  Cheese"). Verified the explicit privacy-boundary ask: a `can_add:false` link gets HTTP 403 on
  the add route and silently falls back to `flat` on `?order=best` (never leaks store/aisle
  data); a bogus token gets a real 404; the public router only ever matches its own four
  registered routes (`server.mjs`'s `publicRouter.match` short-circuits before any authenticated
  `/api/*` route is reachable) — structurally, not just by convention. Cleaned up all smoke-test
  rows afterward (test list/items/share links deleted, `store` cleared back to null) — the test
  account's list is back to its original empty state. One honest side-effect: cleanup also
  cleared 17 pre-existing `activity_log` rows for that test account (I deleted by `user_id`
  rather than by my own rows only) — non-customer-facing audit history on a dev/test account,
  not a real list/entity, but naming it plainly rather than omitting it.

  Deliberate scope cut, stated not hidden: barcode scan (#3109) was NOT extended to share links
  in this build — real external network calls (Open Food Facts) and a materially larger write
  surface than the issue's own verification ask required. Documented as a real, explicit decision
  in `web/shanes-life/README.md`'s new #3186 section, not a gap discovered later.

  Verified clean: `node --check` on every touched `.mjs`/`.js` file, `git status --porcelain`
  clean, `node scripts/dev-server/verify-branch-merged.mjs` OK against `origin/main` at 3af786b22,
  `node scripts/check-migration-numbers.mjs` OK (no collisions — 048 is the next free number per
  the new #3197 allocator). Pushed directly to `main` (rebased twice mid-session as concurrent
  builds landed — `Shane-McCaw-MSP` moves fast tonight). No new GitHub issues filed — no gaps
  found that weren't already resolved in-session or explicitly, honestly scoped out above.
