# #2677 — Chats panel lists Features as Epics — IsEpic's "has sub-issues" heuristic is stale post-restructure

- **Status:** ✅ DONE 2026-09-03T16:15:56Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** b36a6fb75 (main)

## Log
- 2026-09-03T16:11:02Z ⏳ IN FLIGHT — Read #2677 body + BUILD comment. Applied the one-line fix to `GitHubApiClient.cs` `GitBoardIssue.IsEpic`: now `ParentNumber == null && (SubIssueCount > 0 || ChildIssueNumbers.Count > 0)`. Grepped every real `.IsEpic` call site (`desktop/BuildConsole/**/*.cs`, excluding `.target_lsb.cs.bak` which is a stale backup file not compiled) — auditing each before building/verifying.
- 2026-09-03T16:15:56Z ✅ DONE — Applied the fix at `desktop/BuildConsole/Services/GitHubApiClient.cs:171`. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` succeeds, 0 errors.

  **Audit of every real `.IsEpic` call site** (grepped across `desktop/BuildConsole/**/*.cs`; `.target_lsb.cs.bak` is a stale, non-compiled backup and out of scope):
  - `GitHubApiClient.cs:882-981` — confirmed `ParentNumber` is a real, populated field (straight off GraphQL's `parent.Number`, with body-text/ChildIssueNumbers fallback resolution), not something null-by-default for real issues — safe to gate on.
  - `BuildQueuePostgresClient.cs:2450` (`LiveBoardIssueInfo.IsEpic`, chat-link fallback resolver) — this is the exact mechanism #2677 also affects: with the old heuristic a Feature mention could get wrongly upserted into `bt_epics`. The tightened definition fixes this too, not just the Chats-panel display list.
  - `LeftSidebar.xaml.cs` "⚡ Epics"/"⚡ Issues" bucket builders (~2519-2614), epic-picker dropdown (~5871), tree glyphs/badges (~5559-5978), `showsNoParent` badge (~5619) — all consume the boolean via copied view-model properties; `showsNoParent` is additionally gated by `HasParentEpic` (computed independently off `ParentNumber`) so it was already correctly excluding Features before this fix and stays correct after.
  - `FocusImmersiveView.xaml.cs`, `MainWindow.GitDetailTabs.cs`, `MainWindow.UniversalSearch.cs`, `MainWindow.xaml.cs`, `ChatMentionPopupHelper.cs`, `GitDetailView.cs`, `HomeView.xaml.cs:198` (epic count stat) — all display/glyph consumers; correctly now show Features as non-epics, which is the intended effect.
  - `FocusModeService.cs:100` (`ActiveChildIssues` sort: epics-first) — Features now sort with the "rest" tier instead of the epics-first tier. Ordering-only, not a correctness bug.

  **Two real regressions found and filed as #2679** (parented under #1202, labeled `bug`, board status "AI Batter Up"), not fixed in this build — kept narrowly scoped to the one-line fix + audit per this issue's own instructions:
  - `HomeView.xaml.cs:233` — `!issue.IsEpic` feeds the "Quick Win" recommended-next-task picker (`BtnStartNextTask`). A Feature-with-children can now be recommended as a quick pick, which it isn't.
  - `FocusModeService.cs:524` — `!issue.IsEpic` feeds Focus Mode's "Quick task" suggestions; comment says "Never epics (those are big)" but a Feature is exactly as big and now slips through.

  Real verification: build succeeded clean. No live-BuildConsole UI relaunch performed per the standing "never launch BuildConsole.exe to verify" rule (#2156) — verification is build success + the audit above tracing the actual data flow (`ParentNumber` population, all consumers).

  Branch `agent/2677-q1403` (commits `4c56ad1fd` bookend, `b36a6fb75` fix) pushed and merged into `main` via fast-forward (`verify-branch-merged.mjs` confirms ancestor). `git status --porcelain` clean.
