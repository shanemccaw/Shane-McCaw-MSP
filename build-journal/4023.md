# #4023 — Launch Control: seed MSP 1 platform tier + testbed customer monitoring tier

- **Status:** ✅ DONE 2026-09-14T13:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 72f4e9a89669b0948fd5785e187971e6884da29 (IN FLIGHT bookend) — no product code changed, only real local seed data via direct SQL against `DATABASE_URL`; this DONE-bookend commit is the only other commit.

## Log
- 2026-09-14T12:55:00Z ⏳ IN FLIGHT — investigating `services` table for Pro platform tier (highest commandCenter caps), `msp_plan_capabilities` service 131, and highest monitoring tier; will seed `msp_subscriptions` for msp_id=1 and `client_services` for tenant 1.
- 2026-09-14T13:10:00Z ✅ DONE — seeded real local Postgres data (no product code touched):
  - `msp_subscriptions` id=60: `msp_id=1, service_id=122 ("Pro"), status='active'` — confirmed `services.id=122` is the real Platform Tier row with `launch_control_safe_write: true` AND `launch_control_gated_write: true` (`type_attributes.tierCapabilities`), the highest commandCenter capability set.
  - **Plus add-on (item 2) NOT seeded — genuinely impossible, not skipped.** `msp_subscriptions` has `UNIQUE (msp_id)` — confirmed live: inserting a second row for `msp_id=1, service_id=131` throws `duplicate key value violates unique constraint "msp_subscriptions_msp_id_unique"`. There is no other table anywhere in the schema that models an MSP add-on subscription (grep-confirmed: `grantsCapabilityKey`/`addOnType`/`launch_control_plus` appear in exactly one file, `platform-tier.ts`, only to *exclude* add-on rows — nothing reads/writes an add-on grant). Filed as **#4036** (schema/product-design gap, real evidence including the exact constraint-violation error), parented under #2494, board status "AI Batter Up".
  - `client_services` id=50: `client_user_id=2757 (shanemccaw+customer@outlook.com, the canonical mspRole='Customer' login for tenant 1), service_id=12 ("Premier Monitoring — Enterprise", tier='premier'), status='active'` — confirmed `id=12` is the real highest monitoring tier (services.tier ∈ {foundation, growth, premier}; premier is highest; Enterprise is the largest size variant, all size variants share the same `tier` value so the choice doesn't affect entitlement resolution).
  - **Re-verify result (real code path, not a re-implementation):** ran the actual `loadTier`/`resolveCustomerMonitoringTier`/`computeAvailability` functions from `msp-entitlement.ts`/`msp-launch-control.ts` via a scratch `tsx` script (deleted after use, never committed) against `mspId=1, customerId=1`, live local DB, live Graph read for tenant SKUs (4 SKUs resolved, no error). Result: `{ a_la_carte: 7, billable_upsell: 118, included: 0, license_required: 0 }` (was `{ a_la_carte: 125 }` per the issue's own baseline evidence). The `a_la_carte: 7` is now confined to the genuine `blocked_no_workaround` rows (`safe_or_gated: null`) — correct behavior, unrelated to this seed. **But 0 rows reach `included`, not because seeding failed — a second, independent bug (filed as #4035) makes `resolveCustomerMonitoringTier`'s real return values (`foundation`/`growth`/`premier`, confirmed via `select distinct tier from services`) permanently unrecognized by `MONITORING_TIER_RANK` in `msp-launch-control.ts`, which only knows `basic`/`enhanced`/`premium` — a vocabulary that exists nowhere in the real `services.tier` column.** This is why every one of the 118 rows with a real `min_bundled_tier` resolves `billable_upsell` instead of `included` even though the customer now holds the actual highest real monitoring tier. Filed as **#4035** with full live evidence, parented under #2494, board status "AI Batter Up".
  - Net honest result: the platform-tier gate (`launch_control_safe_write`/`launch_control_gated_write`) is now fully unblocked by this seed (confirmed: `a_la_carte` dropped from 125→7, and those 7 are legitimately un-gate-able). The monitoring-tier gate (`min_bundled_tier` vs customer tier) is seeded correctly on the data side but cannot resolve `included` until #4035 is fixed — that is a pre-existing code bug this build surfaced and documented with live evidence, not a seeding gap.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.
