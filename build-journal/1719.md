# #1719 — Remediation Tracking: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ⏳ IN FLIGHT 2026-09-04T14:26:06Z
- **Scope:** platform
- **Started:** 2026-09-04

## Log
- 2026-09-04T14:26:06Z ⏳ IN FLIGHT — pulled #1719's full body + both comments (the 2026-08-30
  blocking comment against #1924/#1925, now superseded by the 2026-09-04 dispatch), plus #2828
  (the real, confirmed staleness report filed during #2586's MSP-console pack build) and #1642
  (the pattern this follows). Confirmed live against local PostgreSQL that #1719's own blocking
  comment's premise no longer holds: `remediation_knowledge_base` is now 153 rows, **all
  published** (was 1 row, draft, 0 published), and the fix-route item set (published KB ∪
  execution-ready config-pack check keys) is 153 items, 28 of which resolve through a live
  `we_can_run` pack (was 3-of-92 check-keyed, 1 execution-ready).

  Reading all 15 real customer-facing routes in full (`portal-remediation-tracker.ts` — 5 routes
  including `decline-to-risk` #1542 and `verify`/`verification-guide` #1540, not 2 as the stale
  pack's §1 documented; `-tracker-scores.ts`, `-tracker-export.ts`, `-checklist.ts` — 3 routes
  including `raise-change` #1941, `-fix-routes.ts`, `-reveal.ts`, `-bypass-resolutions.ts`) plus
  their supporting `lib/remediation-*.ts` modules and the `lib/db/src/schema/msp.ts` enum/table
  sections, cross-referencing against `docs/remediation-tracking-msp-console-contract-pack.md`
  (#2586's already-thorough MSP-side audit of the identical business logic, one day old) rather
  than re-deriving everything from scratch. Also confirmed a fact #2586's own pack got wrong at
  write time: `STEP_CHECK_KEYS` now maps `s28` to `onedrive:sync-errors` (#753/#1956) — only
  s18/s27/s30 are real coverage gaps today, not four steps including s28.

  Next: write `docs/remediation-tracking-contract-pack.md` wholesale (treat current file as prior
  art to replace, not edit), covering all 15 real endpoints, the corrected CURRENT/DECIDED table
  (8 of 9 rows move from DECIDED to CURRENT), the full 7-value `REMEDIATION_TRACKER_STEP_STATUS`
  enum + `REMEDIATION_FIX_ROUTE` enum, corrected consumer map (`useRemediationTracker.ts` is real
  and calls 2 of the 15 routes but has zero page consumer of its own — pre-Design expected state,
  no `Design/portal/*Remediation*` export exists yet), and citing #2827 (already filed under
  #2586) for the one live gap this audit re-confirms rather than re-filing it.
