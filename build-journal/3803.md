# #3803 — unlockFulfillment() sourceType/sourceId join-key mismatch

- **Status:** ✅ DONE 2026-09-13T06:10:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 79747e578

## Log
- 2026-09-13T05:55:00Z ⏳ IN FLIGHT — investigated `unlockFulfillment()` (msp-sow.ts)
  and confirmed the real bug: `sourceType: "sow"` in `fulfillment_queue` is
  exclusively written by admin-fulfillment.ts's legacy Copilot-Readiness
  `quickWinPresentationsTable` sync (keyed to that table's integer id) — a
  completely different entity from `mspSowsTable.sowId` (uuid). Fixing by
  giving the modern MSP-catalog SOW pipeline its own `"msp_sow"` sourceType and
  having `unlockFulfillment()` upsert into it (since nothing else ever creates
  this row for an `msp_sows` SOW), rather than repointing the legacy insert
  (which would break real quickWinPresentations bookkeeping).
- 2026-09-13T06:10:00Z ✅ DONE — commit 79747e578: added `"msp_sow"` to
  `FULFILLMENT_SOURCE_TYPES` (lib/db/src/schema/msp.ts; no DB migration needed
  — `source_type` is plain text, no enum/check constraint, confirmed via local
  psql); rewrote `unlockFulfillment()` in msp-sow.ts to upsert
  (`insert ... onConflictDoUpdate` on the existing unique
  `(sourceType, sourceId)` index) a `"msp_sow"`-typed row instead of updating a
  `"sow"`-typed row that could never exist for it; updated both admin-panel
  fulfillment-queue surfaces (adminv2 screen + legacy FulfillmentQueue.tsx
  page) so the new sourceType renders instead of crashing/blanking. Added
  regression coverage in msp-sow.test.ts exercising
  `triggerMspCharge -> unlockFulfillment`'s real upsert shape (2 new tests).
  Verified: `npx tsc --noEmit` clean in lib/db, api-server, admin-panel;
  `npx vitest run src/routes/msp-sow.test.ts` 40/40 passing; scoped full
  `npx vitest run` in api-server also run (299 passed / 71 failed files, none
  touched by this change — pre-existing, evidence added to tracking issue
  #3943). Branch verified merged into origin/main via
  `verify-branch-merged.mjs`; working tree clean. Posted completion comment
  on #3803. No new leaf finding filed beyond the #3943 comment — the only
  other observation (pre-existing broad test-suite flakiness) was already
  tracked there.
