# #2324 — Document breadcrumb: add Feature segment

- **Status:** ✅ DONE 2026-09-02T19:45:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-02T19:40:00Z — ⏳ IN FLIGHT — starting. Depends on #2319 (closed) which added
  TabDef.FeatureNumber, resolved via ResolveFeatureTierAncestorAsync and already wired
  into the tab-strip badge (⬡ #N) and command-palette preview. The chat document
  breadcrumb bar (CrumbEpic / CrumbChat in MainWindow.xaml, rendered by
  RenderClaudeChatContext in MainWindow.xaml.cs) was the one surface still showing
  Epic-only, per #2324's own scope.
- 2026-09-02T19:45:00Z — ✅ DONE — added CrumbFeature + CrumbFeatureSep TextBlocks to
  desktop/ShaneBuilder/MainWindow.xaml (breadcrumb bar, between CrumbEpic and the
  separator to CrumbChat), collapsed by default. Wired in
  RenderClaudeChatContext(TabDef tab) (MainWindow.xaml.cs) to show "⬡ Feature #N" when
  tab.FeatureNumber is resolved, collapsed (not blank) when genuinely null — matching the
  tab-strip badge's own honest-null handling. Added CtxFeature_Click mirroring
  CtxEpic_Click (opens the Feature issue on GitHub). Since FeatureNumber resolves async
  post-construction (ResolveChatFeatureNumbersAsync), added a
  RenderClaudeChatContext(_activeChatTab) re-render call there (previously only called
  RenderTabStrip()) so a late-resolved Feature number doesn't leave the already-rendered
  breadcrumb stuck without it. Verified with `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj`
  — Build succeeded, 0 Warning(s), 0 Error(s).
  Commit: (see below, same push as this bookend).
