# #2466 — Import: a paragraph under a section becomes one note; wrapped lines rejoin

- **Status:** ✅ DONE 2026-09-02T22:40:00Z
- **Scope:** buildconsole (ShaneBuilder desktop app)
- **Started:** 2026-09-02
- **Commit(s):** afc535429 (implementation, rebased onto main as 68923450), e9d91bdd8 (IN FLIGHT bookend, rebased)

## Log
- 2026-09-02T22:25:00Z ⏳ IN FLIGHT — Audited #2344's landed `TestPadImportParser`/`NotepadImportLineClassifier`. Current baseline (from #2343/#2344) already splits on blank lines and section headers, and naively joins every line in a blank-line-delimited block with a single space — the gap this issue closes is that naive join doesn't distinguish a genuinely new note (no blank line between it and the prior line) from a soft-wrapped continuation of the same paragraph. About to replace the naive per-block join with a sentence-boundary-based paragraph splitter within `FlushBlock`.
- 2026-09-02T22:40:00Z ✅ DONE — Replaced `TestPadImportParser`'s naive whole-block join with `SplitIntoParagraphs` + `EmitCandidate`: within one blank-line-delimited block, a line starts a new paragraph/note unless the immediately preceding line ends mid-sentence (no `.`/`?`/`!`), in which case it rejoins as a wrapped continuation. Blank lines and Section-header detection (#2344) are unchanged; the `<need screen shots>` strip/flag (#2348) and marker parsing now run per-paragraph instead of per-block. Verified via a standalone scratch console project (not committed, deleted after use) exercising three cases: a two-line wrapped sentence rejoining into one note, a new sentence on its own line (no blank line before it) correctly splitting into its own note, and section-carry-forward across a blank line with no new header — all matched expected output. `dotnet build ShaneBuilder.csproj` succeeded, 0 warnings/0 errors. No test manifest — desktop/ShaneBuilder is exempt (WPF app, same as desktop/BuildConsole). No findings/bugs discovered this session. Rebased my two commits onto current `origin/main` (main had moved ahead with #2353/#2464 work) and pushed directly to `origin/main`; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
