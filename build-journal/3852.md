# #3852 — CC Index Phase 1a: full-text search columns + GIN indexes on bt_issue_mirror and bt_chats

- **Status:** ✅ DONE 2026-09-12T22:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** e2a55da40 (bookend), 415692810 (migration)

## Log
- 2026-09-12T22:05:00Z ⏳ IN FLIGHT — investigated real schema of `bt_issue_mirror` /
  `bt_chats` in the local `BuildConsole` Postgres db (not `shanemccawmsp`). Confirmed
  `bt_issue_mirror.body` already exists (added by a later sibling migration than the
  issue body assumed) — no need to add it. Writing migration for `search_vector
  tsvector` columns + GIN indexes + triggers on both tables.
- 2026-09-12T22:15:00Z ✅ DONE — wrote
  `lib/db/migrations/manual/2026-09-12-cc-index-search-vectors-3852.sql` and ran it
  myself against the real `BUILD_DATABASE_URL` (BuildConsole's own local Postgres db,
  where `bt_issue_mirror`/`bt_chats` actually live, not the product `shanemccawmsp`
  db). Added `search_vector tsvector` to both tables, an explicit `BEFORE INSERT OR
  UPDATE` trigger function per table (title weight A + body weight B on
  `bt_issue_mirror`; title A + category B + notes C on `bt_chats`), a GIN index on
  each, and a direct backfill UPDATE for pre-existing rows. Self-mark INSERT is
  guarded with `to_regclass('public.simulator_migration_runs')`, matching the
  existing bt_* migration convention (that table lives only in the product db).
  Real verification performed: inserted+updated real test rows in both tables,
  confirmed `search_vector` populated/recomputed correctly on both insert and
  update, confirmed `plainto_tsquery` search returns the row, and confirmed via
  `EXPLAIN` (default planner, no forcing) that `bt_issue_mirror`'s query used a
  real Bitmap Index Scan on `bt_issue_mirror_search_vector_idx` (3,840 rows).
  `bt_chats` (56 rows) — a table this small correctly favors Seq Scan; the GIN
  index is real and valid, just not cheaper than a scan at that row count. Test
  rows deleted after verification. Working tree clean; branch pushed straight to
  `origin/main` (no separate feature branch, per this repo's convention).
