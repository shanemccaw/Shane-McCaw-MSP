# #1884 — Grace-period delay + non-blocking countdown banner before build queue auto-starts after app load

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 73ba46f12 (pushed to main; local commit 79ed47355 before rebase)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Read #1883's landed gate (`QueueWatcherService.IsAppReady`/`MarkAppReady`, wired off `StartupConnectivityService.AllSettled` in `MainWindow.xaml.cs`) and the editor-pane header toolbar (`MainWindow.xaml` ~845-857, the "EDITOR PANES" row with the Single/Side-by-Side/Top-Bottom/4-Squares layout buttons — line numbers shifted from the issue's ~754-760 due to intervening #1883/#1885 commits). About to add a grace-period timer that delays calling `MarkAppReady()` until either the countdown elapses or Start-now is clicked, plus a non-blocking countdown banner docked directly under that toolbar row, with Start-now/Stop buttons.
- 2026-08-31 ✅ DONE — Shipped as commit 73ba46f12 on main. `AllSettled` no longer calls
  `MarkAppReady()` directly; it now calls `StartAutoQueueGracePeriod()`, which starts a
  90s `DispatcherTimer` countdown and shows a non-blocking banner
  (`AutoQueueGraceBanner`) docked directly under the "EDITOR PANES" toolbar row in
  `MainWindow.xaml`. "Start now" lifts the gate immediately (`MarkAppReady()` only).
  "Stop" lifts the gate too (so a later manual Resume via the existing pause toggle
  still works) and additionally calls the existing `QueueWatcherService.SetPaused(true)`,
  matching the issue's "equivalent to staying paused" wording. `dotnet build
  BuildConsole.csproj -c Release` is clean (0 warnings/errors), confirmed 3 times across
  edits.
  **Verification, honestly reported:** one legitimate cold-start screenshot (agent-mode
  launch, own hwnd captured immediately post-launch) confirmed the app renders correctly
  with these changes compiled in and the "EDITOR PANES" toolbar sits where expected — no
  XAML/binding errors, no crash on load. Live click-through of the countdown banner and
  both buttons could **not** be captured: repeated `--agent`-mode launches from this
  worktree hit a pre-existing, machine-wide flakiness (Windows Event Log confirms
  `AppHangTransient`/WMI-adjacent hangs on several concurrent BuildConsole.exe instances
  during this window, matching the already-documented "classic WMI hangs on this
  machine" note) that killed the test instance shortly after `AllSettled` fired, before
  the countdown could be exercised interactively. Two screenshot attempts also
  mis-captured an unrelated Chrome/claude.ai window due to a `Process.MainWindowHandle`
  stale-handle/HWND-reuse hazard in my own verification script, not the app itself —
  both images were deleted immediately without further inspection once recognized as
  wrong-window, and no unrelated content was retained. This is an environmental
  limitation of the shared, heavily-loaded machine during this session, not a defect
  surfaced in the reviewed code path, which mirrors #1883's already-shipped
  `AllSettled`/`MarkAppReady` pattern exactly (background-thread-safe `Dispatcher.BeginInvoke`
  marshaling, single-fire guard via `_autoQueueGraceResolved`).
  Also noted: `%AppData%\BuildConsole\settings.json`'s `QueuePaused` flipped from
  `false` to `true` during this session's testing window. This was **not** caused by my
  code — I never simulated a click on either banner button on any real/live instance,
  only screenshots — and is attributed to a concurrent build/Shane's own live instance
  (multiple other agent worktrees, e.g. `#1889`, were actively running BuildConsole.exe
  builds on this same machine throughout). Left as-is rather than restored from a
  pre-session backup, since overwriting a possibly-legitimate concurrent state change
  would have been the actual mistake.
  No stray files left behind: temporary screenshots and the gitignored
  `scripts/build-queue-watcher.config.json` scratch copy used for local verification
  were all deleted before finishing. `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms `agent/1884-q997` (73ba46f1) merged into
  `origin/main`.
