# #3084 — URGENT: web services (Portal, API server) not auto-starting/restarting

- **Status:** ✅ DONE 2026-09-07T16:45:05Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** 5029e4d81 (fix), f5cfc33e2 (IN FLIGHT bookend)

## Log
- 2026-09-07T16:27:53Z ⏳ IN FLIGHT — Investigating why Portal/API dev servers aren't auto-starting/restarting for agents' `shaneapp://` tests. Reading `scripts/dev-server/coordinator.mjs`, `.logs/dev-all/dev-all.log`, `current-cycle.json`, and confirming #1855's `waitForHoldsToClear` reading plus #3055's merge-conflict auto-resolve. Report findings before proposing any fix.
- 2026-09-07T16:45:05Z ✅ DONE — **Confirmed root cause:** the always-on api-server has no supervisor. Live evidence: :8080 was DOWN, `api-server.log` (209MB) ended abruptly mid-request at 12:20:42 with no stack/shutdown line, meta frozen `status:running` pid 138312 (dead) → abrupt tree-kill; `cycles.log` unchanged since Sep 6 while `buildsets.log` showed members still merging (deferred set restart pending, nothing revived api-server). Confirmed #1855 `waitForHoldsToClear` is bounded (≤2min) and fires restart unconditionally — NOT the cause; #3055 unrelated; passive health check confirmed passive; `dev-all --start portal` doesn't touch api-server. This is a pre-existing architectural gap, not a regression from tonight.
  **Immediate unblock:** started api-server via detached `dev-all --start api-server`; `/api/healthz` → 200. Tests pass again now.
  **Fix (commit 5029e4d81, on main, BuildConsole `dotnet build` clean, 0 warn/0 err):** (1) `QueueWatcherService.TickAsync` → new `MaybeEnsureApiServerUpAsync` always-on supervisor (probes :8080, auto-starts api-server if down; 45s throttle, 120s post-start cooldown, single-console guard, `_appReady`-gated, api-server-only, meta-exists gate, never throws into tick); (2) `MainWindow.ShaneAppRunTest.cs` → `EnsureDevApiServerUpAsync` runTest pre-flight (bounded 90s best-effort, so a dead api-server never turns a run false-red).
  **Verification:** BuildConsole compiles clean; api-server live at :8080 (HTTP 200). Did NOT launch BuildConsole.exe (per #2156). No test manifest — changes are in `desktop/BuildConsole/`, out of manifest scope.
  **Finding filed:** #3092 (bug, sub-issue of #1789) — dev-all.mjs per-service logs never size-rotated (api-server.log hit 209MB).
  **Branch verified merged into origin/main** (verify-branch-merged.mjs exit 0). Working tree clean.
