# Build Journal — #4084

- **Status:** ⏳ IN FLIGHT 2026-09-14T17:04:36Z
- **Scope:** platform (docs only — read-only extraction, no product code/schema/UI changed)

## Log

- 2026-09-14T17:04:36Z — Started. Pulled #4084's body (corrected framing: a root pack
  already exists at `docs/portal/microsoft-changes-contract-pack.md`, covering Surfaces
  A-F) and #1688 (the parent Feature) and #1678 (the real payload-shape example this
  pack must cite). Read the root pack in full (568 lines) and the `training-sessions`
  MSP-console pack as the structural convention to follow. Re-verifying every cited
  route against current `main` before copying, per the issue's own instruction — found
  real drift the root pack (last refreshed by #3781, 2026-09-12) missed:
  - `admin-m365-interpretations.ts` gained two routes since #3781's refresh:
    `POST .../:id/route` and `GET .../:id/routings` (#1701, landed 2026-08-29,
    predates the #3781 refresh — refresh missed it).
  - `portal-message-center.ts`'s customer-facing `WirePost` gained a `routing:
    WireRouting | null` field (#1744, landed 2026-09-06, also predates #3781 —
    refresh missed it too).
  - `msp-message-center.ts`'s `limit`/`offset` NaN guard (root pack's §1g "known
    defect", filed as #2696) is already fixed and closed — root pack's defect note
    is stale.
  - `admin-m365-interpretations.ts` is gated `requireAdmin` (legacy literal
    `role === "admin"`, i.e. PlatformAdmin) on every route, not
    `requireCapability("ladder.msp-operator")` — an MSPOperator/MSPAdmin console
    session cannot reach any interpretation-authoring route today, even though
    Shane's 2026-09-12 decision on #1688 says authoring belongs in the MSP Console.
    Filing this as a real finding (see below).
  Writing `docs/msp-console/microsoft-changes-msp-console-contract-pack.md` next.
