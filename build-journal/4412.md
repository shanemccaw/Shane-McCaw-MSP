# #4412 — BuildEpicGroupHeader: make aggregate "⚠ N needs attention" indicator clickable

- **Status:** ✅ DONE 2026-09-16T18:13:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** 8bbb08caf

## Plan
Companion to #4411 (still OPEN, no bookend/commits — building against its issue body's
intended shape, not a landed implementation). #4411 covers `BuildRollupRow`'s per-chain
"⚠ N needs attention" text; #4412 is the same real feature at `BuildEpicGroupHeader`'s
aggregate level in the same file (`desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`).

Per #4411's clarification comment (applies here too, per #4412's body): don't add a new
button next to the existing indicator — make the existing "⚠ N needs attention" TextBlock
itself the clickable control (same element/position, add click handler + hover/cursor
affordance). Sends the aggregate `needsAttentionByBuildSet` issue numbers to chat via the
same `SendBuildSetVerifyingRequested` pipeline, with "needs a status check, no verified
bookend yet" wording. Separate `_sentNeedsAttentionByBuildSet` tracking dict — must not
reuse `_sentVerifyingByBuildSet` (the ✈ button's own dict), since an item can move from
needsAttention to landed on a later render.

## Log
- 2026-09-16T18:03:36Z ⏳ IN FLIGHT — read #4412/#4411/#4368/#4306/#4391, confirmed current
  `BuildEpicGroupHeader` code matches the issue's description (needsAttentionByBuildSet
  param, static TextBlock at line ~4122, totalNeedsAttention==0 already hides it). About to
  implement the clickable indicator + separate sent-tracking dict.
- 2026-09-16T18:13:08Z ✅ DONE — #4411 landed on `origin/main` (a6b24f3e3) partway through this
  session, as a real ❓ Button replacing the plain text (not a click-handler bolted onto the
  existing TextBlock, despite Shane's earlier clarification comment on #4411 — the ACTUAL
  landed code is what #4412 must match per its own body's instruction to prefer #4411's real
  implementation over the issue text if it lands first). Rebuilt this session's implementation
  to mirror that: `BuildEpicGroupHeader`'s "⚠ N needs attention" TextBlock is now a real ❓
  Button, same style/position as the ✈ sendButton, wired to `SendBuildSetVerifyingRequested`
  with #4411's own "Please check — no verified DONE bookend yet" / "needs attention (no
  verified bookend yet)" message wording verbatim for consistency.

  **Reused #4411's own `_sentNeedsAttentionByBuildSet` dict** (same shape, same per-build-set
  key) instead of declaring a second one — it's the same real concept one level up, not a
  differently-shaped mechanism. Added a `NeedsAttentionSentSignature` dimension to
  `RollupEpicHeaderKey` (mirroring #4411's own `RollupRowKey.NeedsAttentionSentSignature`) so
  the aggregate header's pool key actually changes — and the header rebuilds — after a send.
  Already-sent items are filtered out of the header's own `needsAttentionUnsentByBuildSet`
  (computed inside `BuildEpicGroupHeader` itself, mirroring how `BuildRollupRow` computes its
  own `needsAttentionUnsent` internally) before computing the total/tooltip/click snapshot, so
  a sent item stops reappearing in the aggregate count without affecting the per-row ❓ button's
  own independent computation from the same underlying Verifying/bookend data.

  **Changes:** `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` — `BuildEpicGroupHeader`
  needs-attention section rewritten as a Button + click handler; `RollupEpicHeaderKey` record
  extended; per-issue `sentNeedsAttentionByBuildSet` signature computed in `RenderBuildSetRollup`
  alongside the existing `unsentByBuildSet`/`needsAttentionByBuildSet` computation.

  **Verification:** `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors,
  both before and after rebasing onto `origin/main`'s landed #4411 commit. No UI test manifest —
  BuildConsole (the WPF app) is explicitly out of scope for test manifests per CLAUDE.md; this
  is a Debug build + code-level verification only, no live WebView2/manual click-through was run
  in this session.

  Commit 8bbb08caf pushed directly to `origin/main` (rebased cleanly onto the tip that includes
  #4411's a6b24f3e3). `git status --porcelain` clean at session end. No new findings surfaced —
  the file matched the issue's description of its structure exactly (aside from #4411 landing
  mid-session, which is not a bug, just build-order timing).
