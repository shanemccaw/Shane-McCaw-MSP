# #3181 — Things/Contacts room has real add/edit forms — violates no-forms design principle

- **Status:** ✅ DONE 2026-09-07T00:45:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-07T00:00:00Z — Session started. Pulled issue #3181: sub-issue of #3086 (Epic:
  Shane's Life). Confirmed violation: `web/shanes-life/public/app.js`'s `#/things` room
  built dedicated add/edit forms for things and contacts, violating the no-forms design
  principle (contract pack Section 8). Plan: remove the dedicated form UI, route
  natural-language capture through the existing capture-parsing pipeline into the
  already-built `set_thing`/`set_contact` logic from #3156. Keep the "Where's the…"
  search input since it's a real query, not a data-creation form.
- 2026-09-07T00:45:00Z — ✅ DONE — Removed the `thingForm` (name/place/house + "Save
  location") and `contactForm` (name/trade/did/phone + "Save contact") `<form>` elements
  and their submit handlers from `viewThings()` in `public/app.js`. Confirmed
  `set_thing`/`set_contact` MCP tools (`src/mcp/tools.mjs`) already call
  `things.recordThing`/`contacts.recordContact` directly — no HTTP self-call, no new
  backend logic needed. Updated the two empty-state messages to point at the universal
  capture box (`"Say ... in the capture box"`) instead of referencing the now-removed
  forms "below". Confirmed against `Design/design_handoff_shanes_life/Shanes Life 09 -
  Things.dc.html`, which has no add/edit form markup. Kept the real "Where's the…"
  search input (a query, not data-creation). `node --check public/app.js` passes.
  This app (`web/shanes-life`) is standalone (not in the pnpm workspace, own Postgres
  DB per its own README) and its `bin/check.mjs` explicitly does not cover the browser
  DOM half, so live end-to-end verification of the removed forms isn't available from
  this environment; verification here is static (syntax check + design-source diff +
  confirming the MCP tool call graph is unaffected).
  Commit: 4dedc0453 (merged to origin/main; bookend commit e814d2ac7).
  `git status --porcelain` clean.
