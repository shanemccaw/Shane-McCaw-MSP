# #1901 — graph-write-permissions.ts has no rules for 13 Graph endpoints used by the other Config Packs

- **Status:** ⏳ IN FLIGHT 2026-09-04T07:58:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Parent Feature:** #2489
- **Commit(s):** _(pending)_

## Log
- 2026-09-04T07:58:00Z ⏳ IN FLIGHT — Session opened. Confirmed the real `blocked_by` edge to
  #1975 has cleared: #1975 carries the `complete` label and a real `✅ DONE` bookend
  (`build-journal/1975.md`, commit 6069d7321), and that commit is already an ancestor of this
  worktree's HEAD — so the derivation table I am about to edit already contains #1975's
  corrections. Removed the now-stale dependency edge (`blocked_by` is now 0) and proceeded.

  Scope: add a real rule for each of the 13 Graph endpoints listed in the issue body, each
  citing the Microsoft Learn Permissions table for that exact method + path and quoting its
  Application row verbatim into `documentedApplicationTiers`, per the format #1875 established
  and #1975 hardened.

  Note on the dispatch wording: it says least- vs higher-privileged tiers are "alternatives,
  not an AND-list, per the confirmed pattern from that fix." That is only half of what #1975
  actually confirmed. The two tiers are alternatives, but WITHIN one cell Microsoft separates
  alternatives with `,` and conjunctions with `and` — #1975's whole finding was that three
  documented two-permission conjunctions were correct and must not be reduced. I am following
  Microsoft's real pages and the file's own header rule, not the dispatch paraphrase.
