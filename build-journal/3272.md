# Git #3272 — Feature: Vault room (Shane's Life)

- **Status:** ✅ DONE 2026-09-09T02:55:00Z
- **Scope:** shanes-life (web/shanes-life -- personal project, not MSP business)
- **Commit:** 66aecf545

## Log
- 2026-09-09T02:00:00Z -- Session started. Read issue #3272 + design README
  (`web/shanes-life/Design/design_handoff_shanes_life/README.md`, "Start here" item 3 +
  the "Sep 8 night"/"Sep 8 late" sections). Confirmed real current state: rooms (#3271)
  already added `vault` to ROOM_DEFS/ROOM_KEYS and the house-grid tile/subtitle/lit-rule
  live in `src/routes/api.mjs`, but no `route === "vault"` dispatch and no `viewVault`
  room function exist yet -- Vault is still Money's own sub-tab (`viewMoneyVault`).
  `vault_device_trust` table and `vault.always_ask` do not exist. Backend
  `/api/vault/*` (logins + bill refs, #3242) and `/api/documents/*` (#3244) are both
  real and complete already -- reused, not rebuilt. Confirmed #3276 (separate, open)
  owns the *extension-side* trusted-device UI (chip/popup/options); this issue's own
  scope is the in-app room: search, chips, per-kind cards, the Browser add-on card
  (list + Forget), and the `always_ask` flag/lock/capture grammar. Next migration
  number confirmed live via `node bin/next-migration-number.mjs` = 062.
- 2026-09-09T02:55:00Z -- ✅ DONE. Commit 66aecf545 (rebased onto origin/main, pushed;
  `verify-branch-merged.mjs` confirms ancestor). Landed:
  - `migrations/062_vault_device_trust.sql` -- `vault_device_trust` table (shape of
    `widget_tokens`) + `vault.always_ask boolean DEFAULT false`. Run myself against
    local Postgres (`node bin/migrate.mjs` -> `applied=1 alreadyApplied=48`); schema
    confirmed live via `information_schema.columns`.
  - `src/core/vault.mjs` -- `alwaysAsk` threaded through `toWire`/`listEntries`/
    `ownedRow`/`createEntry`/`updateEntry`; new `listTrustedDevices`/`forgetDevice`.
    Verified against real Postgres with isolated test rows (inserted, exercised,
    cleaned up -- zero residue, confirmed by row-count).
  - `src/routes/api.mjs` -- `GET`/`DELETE /api/vault/device-trust(/:id)`.
  - `public/app.js`/`app.css` -- new `viewVault` room (search pill across logins/
    bill refs/documents, the four chips, per-kind cards reusing the existing
    `vaultRow`/`documentRow`, the real Browser add-on card, always-ask lock +
    edit-form toggle), router dispatch for `#/vault`, `submitVaultCapture` (where's
    my X / new login for X user Y / X always asks / stop asking for X).
  - Real verification: `node --check` on all 3 touched JS/mjs files; local server
    boot (`node src/server.mjs`) confirmed `[migrate] up to date (49 already
    applied)`; full `node bin/check.mjs` run -- 217/221 passing, the 4 failures are
    the pre-existing `SL_VAULT_KEY` gap (#3246, already filed, unrelated to this
    change -- confirmed by `keyIsConfigured() === false` locally, not a regression);
    new routes smoke-tested live (401 without auth = correctly wired, not 404/500).
  - Not in scope for this issue (left alone, per its own text): Money's own Vault/
    Documents sub-tabs (`viewMoneyVault`/`viewMoneyDocuments`) stay live -- #3273
    removes them from Money's nav. Minting a real device-trust token (the
    extension's own Face ID + "Trust this Chrome" ceremony, `POST /api/vault/:id/
    fill`) is #3276's scope; this room only lists + Forgets what already exists,
    so no unused mint function was added.
  - Test manifest: not applicable -- CLAUDE.md's manifest requirement is scoped to
    `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`; this build only
    touches `web/shanes-life/`.
  - `git status --porcelain` clean at finish (only the `.env` copied in for local
    testing, which is gitignored and untracked -- not part of the diff).
