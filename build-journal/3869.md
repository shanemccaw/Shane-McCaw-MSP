# #3869 — Focus bar milestone progress numbers don't match GitHub's authoritative truth

- **Status:** ✅ DONE 2026-09-12T23:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 37b9b4528 (rebased/pushed as b43b3bfd9 on origin/main)

## Log
- 2026-09-12T23:00:00Z ⏳ IN FLIGHT — read issue #3869 body + comment in full; starting investigation into `Controls/FocusModeBar.xaml.cs`, `Services/FocusModels.cs`, `Services/GitHubApiClient.cs` to find what the Focus bar's milestone tile actually queries today.
- 2026-09-12T23:20:00Z ✅ DONE — Landed all three asks:
  1. **Documented the real query path**: `FocusModeBar` reads `FocusModeService.Progress`/`.Milestones`,
     built in `UpdateBoardSnapshot` → `GitBoardIssueFilters.ComputeRealMilestoneCounts(allIssuesForCounts)`,
     which counts every issue that is NOT an Epic/Feature placeholder AND NOT a descendant of
     `InternalToolingEpicNumbers = {1202, 1095}`. This is why the old number (1502) matched none of
     the issue's 3 reference numbers: it silently includes MyArchitect (#3454) descendants and
     disconnected legacy epics like #1094, while also wrongly excluding Admin Panel (#1095).
  2. **Added a real production-scope option**: `GitBoardIssueFilters.ComputeProductionScopedMilestoneCounts`
     — a live reachability walk (reusing the existing `CollectDescendants` BFS) from the 6 real
     shipping epics (`ProductionRootEpicNumbers = {1571,1485,1281,1096,1095,1093}`), which naturally
     excludes #1202/#3454/#1094-style disconnected legacy epics with no hand-maintained exclude-list.
     Wired as a persisted toggle (`FocusModeService.SetProductionScopeOnly`/`FocusPersistState.ProductionScopeOnly`)
     with a PROD/ALL chip on the Focus bar's active milestone tile (`FocusModeBar.xaml`/`.xaml.cs`).
     Deliberately did NOT touch the existing shared `InternalToolingEpicNumbers` default (used by the
     Home dashboard + Git Board tree rollups too) — see finding #3873 filed separately for that.
  3. **Fixed the ETA math** (`IssueEtaProjection.Project`) to fit a NET rate (closed rate minus
     created rate) using an optional parallel `totalWindow` series, fed from data already on hand:
     `FocusClosedSample.Total` (FocusModeService) and `IssueTimeSeriesPoint.CumulativeOpened`
     (HomeEtaProjectionService, same shared core). A flat/negative net rate now reports an honest
     `EtaReason` instead of a confident small ETA. Hand-verified against the issue's own real 30-day
     numbers (2,476 closed vs 2,836 created ≈ -12/day net) — this is exactly the *no honest ETA* case,
     not the old fabricated "~2.9d left."
  - Build verified: `dotnet build BuildConsole.csproj -c Debug` → 0 warnings, 0 errors.
  - Genuine finding filed: **#3873** (parented as a sibling sub-issue of Feature #3870, milestone 5,
    label `bug`) — `GitBoardIssueFilters.InternalToolingEpicNumbers` classifies Admin Panel (#1095) as
    internal tooling, contradicting both CLAUDE.md's own area-epic table and this issue's own
    production-epic classification of #1095. Left the shared default unchanged pending Shane's call.
  - Not done: could not set #3873's project board status to "AI Batter Up" — GitHub GraphQL calls
    hit the app's secondary rate-limit circuit (`RATE_LIMIT` error) despite `gh api rate_limit`
    showing 5000/5000 remaining on both `core` and `graphql` (3 real attempts, all the same error;
    Projects v2 status has no REST equivalent). #3873's label/milestone/parent are set via REST and
    are real; only its board column position is stale (whatever the project's default is) until a
    GraphQL call succeeds.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (b43b3bfd9). Working
    tree clean (`git status --porcelain` empty).
