# #1637 — Stop auto-opening a visible Replit tab on app startup

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** b4a98867a (merged into main as 43d024948)

## Log
- 2026-08-29 ⏳ IN FLIGHT — removing the unconditional `OpenOrFocusReplitWorkspaceTabInternal()` call at `Initialize` (~597), after confirming `ReplitWatcherService`'s hidden `ReplitWatcherWebView` initializes independently via `EnsureWebViewInitializedAsync` and does not depend on the removed call ever having run.
- 2026-08-29 ✅ DONE — removed the call; `ReplitWatcherService` confirmed independent (hidden `ReplitWatcherWebView`, own `EnsureWebViewInitializedAsync` init; `OpenVisibleWorkspaceTab` invoked lazily only from `WakeAsync`, calling `OpenOrFocusReplitWorkspaceTabAsync` itself at that moment). Nothing else in `Initialize` touched. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: clean, 0 errors, only pre-existing unrelated warnings. Merged to origin/main as 43d024948, verified via `verify-branch-merged.mjs`. Working tree clean.
  **Not live-verified** — no running BuildConsole instance/WebView2 runner was driven in this session to visually confirm (a) no tab flash on launch, (b) the watcher status indicator still functions, (c) manual Replit open still works. These remain to be confirmed the next time Shane launches BuildConsole himself; the code-level guarantee (independent hidden watcher, unconditional call removed, nothing else touched) is confirmed by reading both files in full.
