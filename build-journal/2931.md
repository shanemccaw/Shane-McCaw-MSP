# Git #2931 — Feature: transaction tagging rules (ShanesSurvival)

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:00:00Z
- **Scope:** platform (desktop/ShanesSurvival — real separate desktop app, not the MSP platform proper)

## Log
- 2026-09-04T00:00:00Z — Session start. Read #2931 (sub-issue of #2880). Real scope: new
  `transaction_tags` table, `create_transaction_rule` MCP write tool, `spend_bleed` enhanced to
  surface tagged spend as its own line separate from merchant grouping, applied retroactively
  against the existing real 30-day window. Audited `spend_bleed`'s current implementation
  (`DashboardService.LoadMerchantBleedAsync` — SQL `GROUP BY merchant`) before deciding: enhancing
  `spend_bleed` in place is cleaner than a new tool, since the merchant-grouping SQL just needs to
  exclude tag-matched transactions and add a parallel tagged-spend list. Confirmed real DB state
  via direct `psql` against local `finances` DB (see `%AppData%\ShanesSurvival\settings.json`):
  7-Eleven ATM withdrawals of $100.00 (x4), $120.00, $140.00 in the real 30-day window — the real
  first rule from the issue (`7-Eleven`, minAmount 100, tag "Ronnie's medication (cash)") matches
  these 6 real transactions.
