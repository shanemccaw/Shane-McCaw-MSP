# Git #2931 — Feature: transaction tagging rules (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T00:00:00Z
- **Scope:** platform (desktop/ShanesSurvival — real separate desktop app, not the MSP platform proper)

## Log
- 2026-09-04T00:00:00Z — Session start. Read #2931 (sub-issue of #2880). Real scope: new
  `transaction_tags` table, `create_transaction_rule` MCP write tool, `spend_bleed` enhanced to
  surface tagged spend as its own line separate from merchant grouping, applied retroactively
  against the existing real 30-day window. Audited `spend_bleed`'s current implementation
  (`DashboardService.LoadMerchantBleedAsync` — SQL `GROUP BY merchant`) before deciding: enhancing
  `spend_bleed` in place is cleaner than a new tool, since the merchant-grouping SQL just needs to
  exclude tag-matched transactions and add a parallel tagged-spend list. Confirmed real DB state
  via direct `psql` against local `finances` DB (see `%AppData%\ShanesSurvival\settings.json`):
  7-Eleven ATM withdrawals of $100.00 (x4), $120.00, $140.00 in the real 30-day window — the real
  first rule from the issue (`7-Eleven`, minAmount 100, tag "Ronnie's medication (cash)") matches
  these 6 real transactions.
- 2026-09-04T00:00:00Z — ✅ DONE. Real commit `5205f0242` (rebased/pushed to `main` as
  `e62747878`). Shipped:
  - `desktop/ShanesSurvival/migrations/012_transaction_tags.sql` — new `transaction_tags` table
    (id, merchant_pattern, min_amount, max_amount, tag, created_at). **Run myself** against the
    real local `finances` Postgres DB (`%AppData%\ShanesSurvival\settings.json`'s connection
    string) and recorded in that DB's own `schema_migrations` tracking table so the WPF app's
    `MigrationRunner` won't try to re-apply it.
  - `TransactionTagRepository` (Core/Transactions) — list/create, real case-insensitive
    substring match against merchant_name OR name, plus optional amount range.
  - New `create_transaction_rule` MCP write tool (`FinanceTools.cs`).
  - Audited `DashboardService.LoadMerchantBleedAsync` before deciding: enhanced `spend_bleed` in
    place rather than adding a new tool — tag-matched transactions are now pulled out of their
    merchant's SQL grouping and surfaced as their own `TaggedBleed` line
    (`SpendAccountBleed.TaggedSpend`), applied retroactively against the existing real 30-day
    window, no separate backfill needed.
  - WPF `DashboardWindow` renders the same tagged-spend lines above the merchant list.
  - Created the real first rule via the actual MCP tool (`create_transaction_rule("7-Eleven",
    "Ronnie's medication (cash)", minAmount: 100)`) against the live DB, then verified
    end-to-end with a throwaway harness calling the same `DashboardService`/
    `TransactionTagRepository` Core code (deleted before finishing — the MCP stdio
    JSON-RPC transcript itself proved unreadable to capture from this shell environment, but
    tool-call logs confirmed `IsError = False` for both calls, and this harness independently
    proved the real logic): **SR Spend** account real output —
    `TAGGED - Ronnie's medication (cash): $660.00 across 6 transaction(s)`, `7-Eleven:
    $650.12 across 36 transaction(s)`. $650.12 + $660.00 = $1,310.12, exactly matching the
    issue's own cited pre-tagging 7-Eleven total — confirms the split is exact, not an
    approximation.
  - `dotnet build ShanesSurvival.sln` clean (0 warnings, 0 errors) after the change.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
