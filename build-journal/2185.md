# #2185 — devices:kfm-configuration reads wrong Graph endpoint (always false-negative)

- **Status:** ✅ DONE 2026-09-02T03:26:59Z
- **Scope:** platform
- **Started:** 2026-09-02
- **Commit(s):** 5e128f34f

## Log
- 2026-09-02T03:20:30Z ⏳ IN FLIGHT — investigated; confirmed `monitor_checks.key = 'devices:kfm-configuration'`
  reads `/deviceManagement/deviceConfigurations`, which structurally cannot return a KFM
  policy (per #2185's cited Microsoft Learn docs). Comparing sibling `devices:update-rings-config`
  (also a bare `deviceConfigurations` GET, raw `count` mapping, no name filter) against the two
  candidate fixes named in the issue. Deciding factor: this check's OWN existing mapping already
  uses the `countWhere('{{displayName}} contains ...')` name-match transform — the same
  vocabulary/class of loose read the issue calls out for `groupPolicyConfigurations`.
  `configurationPolicies` would need per-policy `settings` fan-out (matching
  `settingDefinitionId`/`templateReference`) with no established convention or fan-out wiring in
  this codebase for that shape. Going with `groupPolicyConfigurations`, keeping the same
  `countWhere` mapping.
- 2026-09-02T03:26:59Z ✅ DONE — **Real finding beyond the issue's own framing, live-verified against
  the real testbed tenant (mccawsoft2.onmicrosoft.com) using MT_APP_CLIENT_ID creds**: neither
  candidate endpoint from the issue actually exists on Graph v1.0. Both `groupPolicyConfiguration`
  and `deviceManagementConfigurationPolicy` (`configurationPolicies`) are beta-only resource types
  (Microsoft Learn doc front-matter: `monikers: graph-rest-beta`, no v1.0 moniker). Live-fired:
  `GET v1.0 /deviceManagement/groupPolicyConfigurations` → 400 "Resource not found for the segment"
  (segment does not exist at v1.0); `GET beta /deviceManagement/groupPolicyConfigurations` → 503
  Intune-service-unavailable — the SAME error class v1.0 `deviceConfigurations` already returns for
  this tenant, i.e. the segment resolves correctly at beta and fails only because this testbed
  tenant doesn't have Intune provisioned, not because of routing. Also confirmed `configurationPolicies`
  returns a `deviceManagementConfigurationPolicy` type whose name field is `name`, not `displayName`,
  further supporting `groupPolicyConfigurations` as the closer fit to the existing mapping.
  Fixed by storing the endpoint as the **full absolute beta URL**
  (`https://graph.microsoft.com/beta/deviceManagement/groupPolicyConfigurations`) — this codebase's
  `graphFetchForTenant`/`graphFetchPaginated` already special-case an absolute `graph.microsoft.com`
  URL as a pass-through to reach `beta` (built in #1796 for exactly this situation; gated to the
  Graph host so no bearer token can leak elsewhere). This is the FIRST `monitor_checks` row to use
  that mechanism (0 of 157 previously, per #1796's own comment). Confirmed the existing
  `matchIntuneWireSignature`/`serviceKeyForGraphPath` "Intune not configured" detection
  (`artifacts/api-server/src/lib/service-availability.ts`) already normalizes absolute `v1.0`/`beta`
  URLs back to a bare path before matching — added 2026-08-30 explicitly for #1796's beta-path
  collector — so this check's 503 will be read as the honest "Intune not configured" state, not
  misreported as a check fault.
  - Migration: `lib/db/migrations/manual/2026-09-02-kfm-configuration-endpoint-fix-2185.sql` — ran
    myself against local `DATABASE_URL`. Updates `monitor_checks.endpoint` and `description` for
    `devices:kfm-configuration` only. `mapping`/`severity_rules` unchanged (same `countWhere`
    transform, same `displayName` field — still correct against `groupPolicyConfigurations`).
    No Drizzle schema change (data-row fix only). `remediation_knowledge_base` row from #2043 not
    touched.
  - No code changes needed — both the absolute-URL beta passthrough and the Intune-unconfigured
    detection normalization already existed and already handle this shape correctly.
  - No existing tests reference `kfm-configuration`; none needed updating.
  - `git status --porcelain` clean before finishing (only this session's own commits).
