# #1503 — Change Control: CR timeline, comments, and attachments

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** `5676b3c93` (schema + migration + libs + routes, rebased through #1500/#1501/#1759 landing concurrently)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Building `cr_events` (immutable state-transition ledger, shaped to also answer #1506's lead-time/success-rate/emergency-ratio/CAB-throughput metrics), `cr_comments`, and `cr_attachments`. Wiring `cr_events` emission into every existing approval/rejection/status-transition write path from #1496 plus the four CR-creation paths. Append-only throughout: no update path, no delete path, no soft-edit. Ends at the wire contract — schema, manual migration run against local DB, routes + Wire* interfaces, proven with real queries. No `artifacts/portal` UI (scaffold has no pages).
- 2026-08-29 ✅ DONE — Shipped the full CR timeline, stopping at the wire contract as scoped (no `artifacts/portal` UI):
  - **Schema** (`lib/db/src/schema/msp.ts`). `cr_events` — append-only ledger: `eventType` (raised/approved/rejected/superseded/scheduled/in_progress/completed/rolled_back), `fromValue`/`toValue`, `stage`, `actorRole` (customer/msp/microsoft/system, never bare "the system"), `actorPersonId`/`actorName`, `reason`, `occurredAt`. `cr_comments` and `cr_attachments` — both append-only, `(mspId, tenantId)` denormalised the same way `cr_approvals` already does. No update/delete path anywhere.
  - **Migration (run against local Postgres, self-marked).** `lib/db/migrations/manual/2026-08-29-cr-timeline-1503.sql` — additive DDL for all three tables + backfill of `cr_events` for the one pre-existing CR, reconstructed from its real `cr_approvals`/`status` history (not invented). Appended to the #1630 checklist.
  - **Store.** `artifacts/api-server/src/lib/portal-change-timeline-store.ts` — `recordCrEvent` (the single choke point every transition routes through), `addComment`, `addAttachment`, and list-by-CR-ids readers.
  - **Wired into every existing #1496 transition.** `recordApproval`, `recordRejection` (both its pending-stage and no-pending-slot branches, plus the `superseded` fan-out), `materializeApprovalsForChange`'s two auto-approval branches, and `declineRoutedChangeToRisk` (the single choke point for both the routed-rejection path and the direct `/decline` route, which never touches `cr_approvals` at all). Plus the four CR-creation paths (`portal-change-control.ts` wizard, `msp-changes.ts`, `m365-change-router.ts` routing, `portal-runbooks.ts` hold-window decisions) and the MSP console's generic status PATCH (scheduled/in_progress/completed/rolled_back/rejected).
  - **Routes / Wire*.** `GET/POST` comments and attachments, `GET` timeline (events+comments+attachments) on both the customer portal (`portal-change-control.ts`) and MSP console (`msp-changes.ts`) sides. `cr_events` has no write route anywhere — system-authored only.
  - **Concurrent landings absorbed cleanly.** #1500 (freeze windows/exceptions), #1501 (CAB), and #1759 (approval policy) all landed on `main` while this was in flight and touch the exact same approval/rejection code paths. Rebased through all three; every conflict was non-overlapping (their code and mine landing on adjacent lines) except two real 3-way merges in `portal-change-rejection.ts`, both resolved by hand and re-verified. CAB's own real-DB integration test (`portal-cab-store.test.ts`) proved the wiring end-to-end without me writing a single line of CAB code: its `recordApproval` call produced a real `cr_events` row that then cascade-deleted on the test's own cleanup — confirmed live via `psql`.
  - **Verification.** 13/13 real-Postgres schema-level checks (insert, enum/FK correctness, `ON DELETE CASCADE`) run via a throwaway script importing the schema directly (documented workaround for worktree `node_modules` being junctioned to the main checkout — see the filed finding #1777). 88/88 pre-existing + concurrent-build vitest tests still pass (`portal-change-approvals`, `portal-change-control`, `portal-change-freeze`, `portal-cab`, `portal-cab-store`). `tsc --noEmit` clean on every file this build touched (47 pre-existing, unrelated repo errors untouched — ai-billing/ai-cost-capture/ai-usage-*/sow-claim-binding/admin-build-tracker/msp-diagnostics). Branch merged to `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs` → OK); working tree clean.
  - **Findings filed:** #1775 (msp-changes.ts POST and portal-runbooks.ts hold-window CRs never materialize a `cr_approvals` record — a real #1496 coverage gap, confirmed still present after #1500's changes), #1776 (filed as local DB drift, then self-corrected via comment — it was actually #1500 landing concurrently, not drift), #1777 (composite `lib/*` package `tsc` project-reference resolution silently checks a stale `dist/*.d.ts` instead of live source — hit and worked around while typechecking this build's own schema changes).
