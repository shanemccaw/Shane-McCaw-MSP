# #1747 — AdminV2: surface the real per-MSP audit trail (mspAuditLogsTable) in AdMspCanvas

- **Status:** ✅ DONE 2026-09-10T23:03:46Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 49f334b6b (feature), 52011b252 (bookend IN FLIGHT), both on `main`

## Log
- 2026-09-10T22:57:17Z ⏳ IN FLIGHT — Investigated: AdMspCanvas.tsx (AdminV2) itself has no fabricated
  audit data (the fabricated `activityLogs` array lived only in the archived, deleted
  `msp-portal/src/pages/msp-detail.tsx`, per the issue body). The real backend endpoint
  `GET /api/msp/audit?mspId=` (artifacts/api-server/src/routes/msp-audit-log.ts, mounted at
  `/api`) already exists, filters by mspId, and requires `ladder.msp-admin`. Adding a
  read-only "Activity" section to AdMspCanvas.tsx wired to this real endpoint — no fixture
  data involved.
- 2026-09-10T23:03:46Z ✅ DONE — Added `AdMspAuditEntry`/`AdMspAuditLogPage` types (adTypes.ts),
  `fetchAdMspAuditLog(adminFetch, mspId, limit)` (adApi.ts), and a real read-only "Activity"
  section in `AdMspCanvas.tsx` loaded independently of the profile fetch, listing actor,
  action, resource and timestamp per row from `GET /api/msp/audit?mspId=`. No backend change
  needed — the endpoint already existed and already filtered by `mspId`. No fixture/demo data
  anywhere in this change. Verified: `tsc --noEmit` on `artifacts/admin-panel` passed clean
  (exit 0). No test manifest required — `artifacts/admin-panel/` is out of the
  msp-portal/marketing-site test-manifest scope per CLAUDE.md. Committed
  `858bf815b`→rebased to `49f334b6b`, pushed straight to `main` (no PR, per repo convention),
  `verify-branch-merged.mjs` confirms ancestry, `git status --porcelain` clean.
