# #3346 — Remediation Tracking: wire decline-to-risk into the checklist section

- **Status:** ✅ DONE 2026-09-09T21:13:00Z
- **Scope:** platform

## Log
- 2026-09-09T20:58:04Z — Starting. Confirmed real gap: `remediation-tracking.tsx` /
  `ChecklistItemCard.tsx` has zero decline/accepted_risk UI. Backend (#2869) is already
  fully built and live: `POST /api/portal/remediation/checklist/:checkKey/decline-to-risk`
  (`artifacts/api-server/src/routes/portal-remediation-checklist.ts`), and the portal-side
  hook `useDeclineChecklistItemToRisk` already exists in
  `artifacts/portal/src/lib/remediation-checklist-api.ts` — this build is UI-only, wiring
  the existing endpoint into `ChecklistItemCard`, following the real precedent already
  shipped for the 28-step programme (`StepActionDialog.tsx` decline mode + `StepRow.tsx`'s
  accepted-risk badge).
- 2026-09-09T21:13:00Z — ✅ DONE. Wired `DeclineToRiskDialog` (new function) into
  `ChecklistItemCard.tsx`: a "Decline and accept the risk" button (gated on
  `status !== "accepted_risk" && verificationState !== "verified"`, matching the
  Design export's `canDecline` predicate exactly) opens a dialog with full-name +
  statement + confirmation checkbox, calling the already-existing
  `useDeclineChecklistItemToRisk` hook against `POST
  /api/portal/remediation/checklist/:checkKey/decline-to-risk` (#2869, already live —
  no backend change needed). Once `status === "accepted_risk"`, the button is
  replaced by a read-only "Signed acceptance" badge, mirroring `StepRow.tsx`'s real
  precedent for the 28-step programme tab. Updated
  `test-manifests/portal/remediation-tracking-checklist.json` notes to document the
  new UI surface and why no click-through UI step drives it live (same permanence
  reasoning the sibling `remediation-tracking.json` manifest already documents for
  its own decline-to-risk route — the write is a permanent, non-idempotent
  `msp_risk_decisions` row, and the checklist's item list depends on the live
  tenant's latest scan having an open finding, not a stable target across runs).
  Commits: `f33ea76e6` (the real change), `4c67ee825`/`dd025761f` (manifest
  `lastVerifiedAgainstCommit` bookkeeping). Branch verified merged into
  `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0).
  `git status --porcelain` clean.
  Verified: `tsc -p tsconfig.json --noEmit` (0 errors); `vite build` (succeeds, with
  `PORT` set as the config requires); `shaneapp://runTest` genuinely invoked against
  `remediation-tracking-checklist.json` — real envelope came back (10/14 steps
  failed), but every failure traces to `{{TEST_CUSTOMER_EMAIL}}` /
  `{{TEST_CUSTOMER_PASSWORD}}` not being set in BuildConsole's Test Environment
  Variables on this machine, a pre-existing environment gap unrelated to this
  change (the two UI steps that don't depend on login — page navigation — passed).
  No new finding filed for it since it's a local test-environment config gap, not a
  product/code defect.
