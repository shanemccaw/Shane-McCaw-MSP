# #2179 — ShaneBuilder: real custom Dialog system (redo)

- **Status:** ✅ DONE 2026-09-02T01:10:00Z
- **Scope:** buildconsole (ShaneBuilder app specifically — not desktop/BuildConsole)
- **Started:** 2026-09-02
- **Commit(s):** 6c6089fe0 (bookend IN FLIGHT), 858673dbe (real port, rebased/pushed as 03ee9f0bf on main)

## Log
- 2026-09-02T00:30:00Z ⏳ IN FLIGHT — redo of #2175 (reverted in #2177 for landing in desktop/BuildConsole instead of desktop/ShaneBuilder). Building AppDialog.xaml/.xaml.cs in desktop/ShaneBuilder/, porting WindowChromeHelper into ShaneBuilder's own namespace, styled against #2147 Brush.*/FontSize.* tokens.
- 2026-09-02T01:10:00Z ✅ DONE — Added `desktop/ShaneBuilder/AppDialog.xaml(.cs)` (custom-chrome dialog, static `AppDialog.Alert/Confirm/Input` API) and `desktop/ShaneBuilder/Services/WindowChromeHelper.cs` (ported from BuildConsole's #1006 helper — ShaneBuilder had no chrome helper of its own yet). Styled against the real #2147 tokens: `Brush.Bg.Window/Panel/Chip/Card`, `Brush.Text.Primary/Muted/Heading`, `Brush.Border.Default/Hover`, `Brush.Accent.Primary`, `FontSize.13`, `FontWeight.SemiBold` — not BuildConsole's #2126 palette. ShaneBuilder currently has zero real `MessageBox.Show` call sites to migrate (near-empty scaffold) — this issue built the real, working component itself, ready for future call sites.
  Real audit before commit: `git status --porcelain | awk '{print $2}' | grep -v "^desktop/ShaneBuilder/"` — empty, confirms no file outside `desktop/ShaneBuilder/` was touched.
  Also fixed a real latent bug this surfaced: `Themes/Colors.xaml`'s `Brush.Toast.*` aliases (added in #2174) were written as `<StaticResource x:Key="X" ResourceKey="Y"/>`, which does NOT actually register a lookup-able key in a merged `ResourceDictionary` — confirmed via a runtime probe (`Resources.Contains("Brush.Toast.Info")` → `false`). `AppDialog.Alert`'s default Info icon hit this directly (`ResourceReferenceKeyNotFoundException`, caught live during the smoke test below); `ToastCard.xaml.cs`'s own `Res()` helper has been silently swallowing this exact failure and falling back to `Brushes.SteelBlue` for every toast kind since #2174 shipped. Fixed by making each `Brush.Toast.*` key a real `SolidColorBrush` carrying the identical hex already defined on its source brush (copied value, not a new color) — `Brush.Toast.Info`/`Success`/`Warning`/`Error` now genuinely resolve.
  Verification: `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors. Real live smoke test of the built `.exe` (PowerShell `Start-Process`, same pattern as #2174): temporarily wired all three `AppDialog.*` calls into `App.OnStartup` with a real `ButtonAutomationPeer` UIA click driving each actually-rendered dialog once its own `Loaded` event fired — Confirm → clicked No → returned `false`; Input → set real text into the real `TextBox`, clicked OK → returned that exact text (`smoke-test-typed-2179`); Alert → clicked OK → closed cleanly. (Along the way, hit and fixed a smoke-test-harness-only issue: default `ShutdownMode.OnLastWindowClose` silently no-ops a second/third `ShowDialog()` once the first dialog closes when there's no real owning `MainWindow` — pinned `ShutdownMode = OnExplicitShutdown` in the scratch harness; this is a harness artifact of driving three dialogs from a bare `OnStartup` with no real MainWindow yet, not a defect in `AppDialog` itself.) All scratch `App.xaml.cs`/`AppDialog.xaml.cs` diagnostic/harness edits reverted before commit — confirmed via `git diff` — not part of the shipped diff.
  Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0, rebased onto current `origin/main` and pushed directly per this repo's git conventions). `git status --porcelain` clean at completion.
