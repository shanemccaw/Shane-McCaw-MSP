# #4332 — Remediation Tracker export prints fixture tenant numbers in every customer's step titles

- **Status:** ⏳ IN FLIGHT 2026-09-17T03:01:38Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-17T03:01:38Z ⏳ IN FLIGHT — Stripping fabricated Halden Materials tenant numbers (site/link/account/seat counts, specific dates, policy names like "CA01") out of the s1-s30 `title` fields in `artifacts/api-server/src/lib/remediation-tracker-catalogue.ts`, its portal mirror `artifacts/portal/src/lib/remediation-tracker-catalogue.ts`, and the drift-guard source `artifacts/portal/src/components/copilot-journey/previewRemediationGuide.ts`. This is the "at minimum" fix named in #4332: titles now read as generic actions instead of another tenant's figures, closing the leak into the signed risk-decline record (`remediation-tracker-risk-decline.ts:226` `catalogue?.title`) and the CSV/PDF exports. Only `title` fields changed — `blastRadius`/`caution`/`verify`/`code.script` in previewRemediationGuide.ts stay the Halden Materials worked example verbatim (preview-only, behind `?preview=design`, per that file's own header). Full retirement in favor of #1538's findings-derived checklist was considered but is a much larger architectural migration (DB schema, tracker UI, phase pricing) already partially underway via the parallel `declineRemediationChecklistItemToRisk` (#2869) path — out of scope for this fix; filing as a separate finding if warranted.
