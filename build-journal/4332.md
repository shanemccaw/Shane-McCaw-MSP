# #4332 — Remediation Tracker export prints fixture tenant numbers in every customer's step titles

- **Status:** ✅ DONE 2026-09-17T03:10:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** f818612cd (bookend IN FLIGHT), 5c6db7ece (real fix)

## Log
- 2026-09-17T03:01:38Z ⏳ IN FLIGHT — Stripping fabricated Halden Materials tenant numbers (site/link/account/seat counts, specific dates, policy names like "CA01") out of the s1-s30 `title` fields in `artifacts/api-server/src/lib/remediation-tracker-catalogue.ts`, its portal mirror `artifacts/portal/src/lib/remediation-tracker-catalogue.ts`, and the drift-guard source `artifacts/portal/src/components/copilot-journey/previewRemediationGuide.ts`. This is the "at minimum" fix named in #4332: titles now read as generic actions instead of another tenant's figures, closing the leak into the signed risk-decline record (`remediation-tracker-risk-decline.ts:226` `catalogue?.title`) and the CSV/PDF exports. Only `title` fields changed — `blastRadius`/`caution`/`verify`/`code.script` in previewRemediationGuide.ts stay the Halden Materials worked example verbatim (preview-only, behind `?preview=design`, per that file's own header). Full retirement in favor of #1538's findings-derived checklist was considered but is a much larger architectural migration (DB schema, tracker UI, phase pricing) already partially underway via the parallel `declineRemediationChecklistItemToRisk` (#2869) path — out of scope for this fix.
- 2026-09-17T03:10:00Z ✅ DONE — 13 of 28 titles rewritten to be generic (e.g. s7 "Audit and fix MFA on the 11 admin accounts" → "Audit and fix MFA on the admin accounts"); 3 universal policy-duration numbers (12-month, 90-day, 14-day) deliberately left alone as those aren't tenant findings. Verified: `remediation-tracker-catalogue.test.ts`/`remediation-tracker-risk-decline.test.ts`/`remediation-tracker-pricing.test.ts`/`portal-remediation-tracker-export.test.ts` — 35 tests pass. Live-verified against the real local Postgres dev DB and the real McCawSoft tenant (customer id 2080): inserted a real `remediation_tracker_steps` row for s7, called the real `declineRemediationStepToRisk()`, confirmed the signed `msp_risk_decisions` row's title came back generic ("Audit and fix MFA on the admin accounts") with a real KB-sourced `hazardDescription`, then cleaned up both rows. Commit 5c6db7ece pushed to origin/main and rebased clean (`verify-branch-merged.mjs` OK). Working tree clean. Issue comment posted, labels synced (in-flight→complete). No new findings surfaced worth filing separately — the only larger-scope item (retiring s1-s30 in favor of #1538's checklist) is already named in the issue body itself, not a new discovery.
