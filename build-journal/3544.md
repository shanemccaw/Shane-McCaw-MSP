# #3544 — Account Security: wire GET /api/portal/account-security/graph-signals

- **Status:** ✅ DONE 2026-09-10T22:35:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 666d141ff (page + hook wiring), fc1782dce (test manifest)

## Log
- 2026-09-10T22:05:00Z ⏳ IN FLIGHT — pure frontend wiring of the already-live, already-built
  `GET /api/portal/account-security/graph-signals` endpoint (#1593) into
  `artifacts/portal/src/pages/account-security.tsx`: the local failed-portal-login line on
  the Password card, and a new "Your Microsoft 365 tenant" card (password age / failed
  sign-ins / device compliance), per `Design/portal/design_handoff_full_site/screens/Account
  Security.dc.html` lines 125-176 and the logic class's `lfDot`/`lfHead`/`lfDetail`/
  `tenantSignals`. No backend change needed.
- 2026-09-10T22:35:00Z ✅ DONE — `useAccountSecurityLive.ts` (666d141ff) now fetches
  `GET /api/portal/account-security/graph-signals` in its own independent effect
  (`graphSignals`/`graphSignalsLoading`/`graphSignalsFailed`, kept separate from the
  MFA/sessions/login-history read so a genuine per-tenant Graph gap never reads as a
  page-level read failure). `account-security.tsx` renders `localFailedLoginLine()` inline
  on the Password card and a new "Your Microsoft 365 tenant" card via `tenantSignalRows()` —
  every value is the server's real `available`/unavailable discriminated-union state with
  its own real `detail` text, no fabricated numbers or fixture data. Wrote and registered
  `test-manifests/portal/account-security-graph-signals.json` (fc1782dce; auth-gate +
  real four-field-shape apiTests, honest RequireAuth uiStep — no portal login page yet,
  #1648, same documented gap `account-security-data-rights.json` already carries).
  Verification: real `tsc --noEmit` (0 errors), real `vite build` (succeeds), a real curl
  against the live local dev api-server confirming the route genuinely 401s unauthenticated.
  `shaneapp://runTest?file=account-security-graph-signals.json` was attempted for real and
  returned "manifest not found or unparseable" — the dev-server checkout hasn't picked up
  these commits yet (same structural gap noted in `account-security-data-rights.json`); the
  manifest is written, registered, and ready to run once that merge/restart cycle lands.
  `verify-branch-merged.mjs` confirms `agent/3544-q2214` is merged into `origin/main`;
  `git status --porcelain` is clean. No new GitHub findings filed — this was a clean, fully
  scoped wire-the-existing-endpoint task.
