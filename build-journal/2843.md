# #2843 — Intune 5th wire signature (bare Message envelope, no ErrorCode)

- **Status:** ✅ DONE 2026-09-04T15:55:17Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** cfa454f94 (implementation), 15bb8220c (merged into main)

## Log
- 2026-09-04T15:49:01Z ⏳ IN FLIGHT — investigated #2843 + #1963's real fix (3f953997f) in artifacts/api-server/src/lib/service-availability.ts; adding intune-bare-message-401 signature for autopilotEvents/troubleshootingEvents (bare `_version`+`Message` envelope, no ErrorCode), tolerant regex per #1963's escape-depth reasoning.
- 2026-09-04T15:55:17Z ✅ DONE — added `intune-bare-message-401` (5th `INTUNE_WIRE_SIGNATURES` member) + `INTUNE_BARE_MESSAGE_RE`/`INTUNE_ERROR_CODE_RE` matcher logic in `artifacts/api-server/src/lib/service-availability.ts`; test using the issue's real evidence body (autopilotEvents) added to `service-availability.test.ts`, plus a non-regression test proving it doesn't mismatch the #1963 ErrorCode envelope. `vitest run service-availability.test.ts` — 12/12 passed. `tsc --noEmit` on api-server showed one pre-existing unrelated error (`portal-security-plan.ts:130 requireRole`), not touched by this change. Rebased onto origin/main and pushed directly to main (15bb8220c) since this worktree's `main` ref is held by another local worktree; `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
