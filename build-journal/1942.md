# #1942 — Alert catalogue: Risk Register, Policy Decisions, POA&M rules

- **Status:** ✅ DONE 2026-09-05T00:45:00Z
- **Scope:** platform
- **Commit:** 263217641 (Alert catalogue: seed Risk Register, Policy Decisions, POA&M rules)

## Log

- 2026-09-05T00:28:00Z — Started. Pulled #1942 body + all 3 comments (retention-lifecycle
  #1944 and deletion-lifecycle #1944-part-5 rules were added to #1942 by later comments, but
  the actual dispatched build prompt scopes explicitly to "Risk Register, Policy Decisions,
  and POA&Ms" — treating the #1944 retention/deletion comments as tracked separately under
  their own module (#1944), not pulled into this build's diff).
- Investigated: `customer_tenant_alert_rules` (the catalogue #1942 is about) is the
  CUSTOMER-facing catalogue (Git #1278), distinct from `msp_alert_rules` (internal MSP-ops).
  Evaluator: `artifacts/api-server/src/lib/customer-tenant-alert-engine.ts`.
- Confirmed real schema state via direct psql:
  - `msp_risk_decisions` has real `status` (pending_signature/active/revoked), `accepted_at`,
    `review_state`/`review_due_at` (#1507 split, populated live by
    `alert-engine.ts#advanceRiskReviewClock`), but NO owner-assignment timestamp and NO
    review-completion/renewal writer route exists anywhere (grepped) — `risk.assigned` and
    `risk.renewed` seeded `pending_detector`.
  - `policy_decisions` (Git #2024) has `created_at`/`updated_at`/`signed_at`, `review_state`/
    `review_due_at` populated by `advancePolicyReviewClock`. No obligation-applicability
    mapping to say a tenant needs a NEW policy identified, and no edit-audit trail to
    distinguish a real edit from an automatic clock touch — `policy.identified` and
    `policy.updated` seeded `pending_detector`.
  - `policy_evaluation_runs.outcome = 'divergent'` is the REAL, already-built detector for
    "policy drifted" (#1797's config-state differ) — settles the issue's own "settle which
    produces it" ask: policy drift comes from here, NOT a duplicate `drift.*`-style row.
  - `msp_sop_runs.standing_policy_id` + `status = 'Completed'` is the real detector for
    "policy executed".
  - POA&M (#1935): confirmed zero tables/columns/routes exist anywhere (grep for
    poam/POA&M/plan_of_action = 0 hits). Both POA&M rules seeded `pending_detector` — no
    detector to seed live, per the issue's own hard rule.
- Found (pre-existing bug, out of this issue's scope, filed separately): the existing
  `review.policy_review_due` evaluator queries `msp_risk_decisions.decision_state IN
  ('due','expired')` — wrong table (should be `policy_decisions`, Git #2024) and 'expired'
  is not a valid `POLICY_DECISION_STATES` value (#1527 removed it). Filed as **#2945**,
  parented as a direct sub-issue of #2748 (Feature: Notification / Alert Engine — the real
  Feature-tier parent, per `gh issue view 1942` → #1096 has no Feature between it and #1942,
  but #2748 lists #1942 in its own structured index, confirming it as the Feature parent),
  labelled `bug`, board status set to "AI Batter Up".
- Migration run: `lib/db/migrations/manual/2026-09-04-alert-catalog-risk-policy-poam-1942.sql`
  — 13 new `customer_tenant_alert_rules` rows (all `alert_category='reviews'`):
  - **Risk Register (#1487):** `risk.identified`, `risk.accepted`, `risk.expiring` (LIVE —
    `msp_risk_decisions.status`/`accepted_at`/`review_state` per #1507); `risk.assigned`,
    `risk.renewed` (pending_detector — no owner-assignment or review-renewal write event
    exists anywhere in the codebase, confirmed by grep).
  - **Policy Decisions (#1490):** `policy.created`, `policy.accepted` (LIVE —
    `policy_decisions.created_at`/`signed_at`, Git #2024); `policy.drifted` (LIVE —
    `policy_evaluation_runs.outcome='divergent'`, settling #1942's own "settle which
    produces it" ask in favor of #1797's config-state differ, NOT a duplicate of
    `drift.*`); `policy.executed` (LIVE — `msp_sop_runs.standing_policy_id` +
    `status='Completed'`); `policy.identified`, `policy.updated` (pending_detector — no
    per-tenant obligation-applicability mapping / no edit-audit trail exists).
  - **POA&Ms (#1935):** `poam.milestone_approaching` (the 9-step ladder collapsed into ONE
    condition per #1942's own instruction, not nine notifications), `poam.expiring` — both
    pending_detector: confirmed zero POA&M tables/columns/routes exist anywhere in the repo
    (grepped for poam/POA&M/plan_of_action). Building the full #1935 schema/conversion-flow
    is a substantial module build in its own right, out of scope for a catalog expansion.
  - `deep_link_path` is NULL on all 13 (per #1942's own hard rule — #1827 confirms
    `artifacts/msp-portal`/portal-v2 is wholesale deleted and `artifacts/portal` has no Risk
    Register/Policy Decisions/POA&M page yet). `admin_deep_link_path` reuses the real, live
    `/system/customer-alert-rules` admin route.
  - Ran myself against local `DATABASE_URL`: `INSERT 0 13`, verify query confirms 36 total
    rows / 6 pending_detector (the 4 pre-existing pending_detector rows from #1278 have
    since been built out and flipped live by unrelated later work).
- Wired the 7 LIVE condition types' evaluators + summary copy into
  `artifacts/api-server/src/lib/customer-tenant-alert-engine.ts` (`getConditionValue` +
  `buildSummary`). `pnpm run typecheck` in `artifacts/api-server`: **exit 0, clean.**
- No test-manifest needed — CLAUDE.md's test-manifest requirement scopes to
  `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting` only; this is an api-server
  engine change with no UI/portal surface yet (none of these three modules have a rebuilt
  page under #1485 to test against — see the deep_link_path note above).
- `git status --porcelain`: clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (OK) after a rebase + push
  (`263217641` on `main`).
- Issue labels: `in-flight` removed, `complete` added. Comment posted on #1942.
