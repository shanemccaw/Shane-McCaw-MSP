# #1942 — Alert catalogue: Risk Register, Policy Decisions, POA&M rules

- **Status:** ⏳ IN FLIGHT 2026-09-05T00:28:00Z
- **Scope:** platform

## Log

- 2026-09-05T00:28:00Z — Started. Pulled #1942 body + all 3 comments (retention-lifecycle
  #1944 and deletion-lifecycle #1944-part-5 rules were added to #1942 by later comments, but
  the actual dispatched build prompt scopes explicitly to "Risk Register, Policy Decisions,
  and POA&Ms" — treating the #1944 retention/deletion comments as tracked separately under
  their own module (#1944), not pulled into this build's diff).
- Investigated: `customer_tenant_alert_rules` (the catalogue #1942 is about) is the
  CUSTOMER-facing catalogue (Git #1278), distinct from `msp_alert_rules` (internal MSP-ops).
  Evaluator: `artifacts/api-server/src/lib/customer-tenant-alert-engine.ts`.
- Confirmed real schema state via direct psql:
  - `msp_risk_decisions` has real `status` (pending_signature/active/revoked), `accepted_at`,
    `review_state`/`review_due_at` (#1507 split, populated live by
    `alert-engine.ts#advanceRiskReviewClock`), but NO owner-assignment timestamp and NO
    review-completion/renewal writer route exists anywhere (grepped) — `risk.assigned` and
    `risk.renewed` seeded `pending_detector`.
  - `policy_decisions` (Git #2024) has `created_at`/`updated_at`/`signed_at`, `review_state`/
    `review_due_at` populated by `advancePolicyReviewClock`. No obligation-applicability
    mapping to say a tenant needs a NEW policy identified, and no edit-audit trail to
    distinguish a real edit from an automatic clock touch — `policy.identified` and
    `policy.updated` seeded `pending_detector`.
  - `policy_evaluation_runs.outcome = 'divergent'` is the REAL, already-built detector for
    "policy drifted" (#1797's config-state differ) — settles the issue's own "settle which
    produces it" ask: policy drift comes from here, NOT a duplicate `drift.*`-style row.
  - `msp_sop_runs.standing_policy_id` + `status = 'Completed'` is the real detector for
    "policy executed".
  - POA&M (#1935): confirmed zero tables/columns/routes exist anywhere (grep for
    poam/POA&M/plan_of_action = 0 hits). Both POA&M rules seeded `pending_detector` — no
    detector to seed live, per the issue's own hard rule.
- Found (pre-existing bug, out of this issue's scope, filed separately): the existing
  `review.policy_review_due` evaluator queries `msp_risk_decisions.decision_state IN
  ('due','expired')` — wrong table (should be `policy_decisions`, Git #2024) and 'expired'
  is not a valid `POLICY_DECISION_STATES` value (#1527 removed it). Filed as its own issue,
  sibling of #2748.
