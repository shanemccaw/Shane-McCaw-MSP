# #3288 — WPF WebView2 automated weekly-ad scraping (feeds existing push_deals/push_coupons pipeline)

- **Status:** ✅ DONE 2026-09-09T00:31:46Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 174e2b3b7 (feature), 3a64da951 (post-rebase, pushed to origin/main)

## Log
- 2026-09-09T00:29:09Z ⏳ IN FLIGHT — investigated Publix's real live weekly-ad page
  (`www.publix.com/savings/weekly-ad/view-all`, redirected from `weeklyad.publix.com`):
  client-rendered (Vuex-store JS), real selectors read off its own shipped skeleton-loader
  markup (`.weekly-ad-card-grids .card-grid`, `.savings-badge`); confirmed a real
  `services.publix.com/api/v4/savings` JSON API exists but requires an unreverse-engineered
  bearer token, so DOM extraction over an authenticated real browser session (WebView2) is used
  instead, matching the issue's own asked-for approach. Confirmed real, existing infra to build
  against: `PlaidLinkWindow` (WebView2 pattern) and shanes-life's real remote MCP endpoint
  (`POST {origin}/mcp`, `Authorization: Bearer slmcp_...`, `web/shanes-life/src/routes/mcp.mjs`)
  — the same real pipeline `push_deals`/`push_coupons` already serve Claude conversations
  through (Git #3110). Building `WeeklyAdScraperWindow` + `PublixAdParser` +
  `ShanesLifeMcpClient` next; this bookend was filed late (after investigation/implementation
  were already underway) — see the completion comment on #3288 for the honest note on that.
- 2026-09-09T00:31:46Z ✅ DONE — shipped `WeeklyAdScraperWindow` (real WebView2 window,
  persistent profile, bounded 20s poll for the real rendered ad grid), `PublixAdParser` (Core,
  pure/unit-testable text parsing: plain price, N-for-$X, $X-off guarded against being misread
  as the sale price, BOGO, unparsed cards reported by name), `ShanesLifeMcpClient` (real
  JSON-RPC client for shanes-life's remote MCP server — push_deals/push_coupons, no second
  storage path), Settings fields (ShanesLifeApiBaseUrl/ShanesLifeMcpToken, never
  hardcoded/logged), a MainWindow button, and a real README section documenting the live
  selectors, setup, and honest verification status. Verified: `dotnet build` clean (0
  warnings/errors); `PublixAdParser` run against representative fixture text in a scratch
  harness (deleted after — no I/O, nothing to keep) — caught and fixed a real bug in-session
  ($X-off badge misread as the item's own sale price). Not live-verified: an actual WebView2
  session against Publix's real page end-to-end (headless build session, no interactive
  Windows desktop). Branch rebased onto origin/main and pushed (3a64da951); verified merged via
  `verify-branch-merged.mjs`. Working tree clean. Findings filed: #3290 (stale MCP-server UI
  text), #3291 (2026-09-05 ShanesSurvival/shanes-life unification decision has no real data
  bridge yet beyond this one-directional weekly-ad path) — both parented under #3234 (Feature
  parent), labelled `bug`, board status "AI Batter Up".
