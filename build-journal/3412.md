# #3412 — Remove Platform Agreements page from Admin Panel

- **Status:** ✅ DONE 2026-09-10T02:41:00Z
- **Scope:** platform

## Log
- 2026-09-10T02:15:25Z — Started. Confirmed no other real caller of `platform-agreements.ts`
  routes (`/api/platform/agreement/*`, `/api/admin/platform-agreements`) anywhere in the
  codebase — the only consumer was `PlatformAgreements.tsx` itself. `msp-signup.ts` /
  `user-hard-delete.ts` query `platformAgreementsTable` / `mspAgreementAcceptancesTable`
  directly (schema, not the route), so the schema stays; only the page, route file, and
  nav entry are being removed per Shane's decision on #3412.
- 2026-09-10T02:41:00Z — ✅ DONE — Deleted
  `artifacts/admin-panel/src/pages/PlatformAgreements.tsx`,
  `artifacts/api-server/src/routes/platform-agreements.ts`, the `sys-platform-agreements`
  nav entry (`workspaceNav.tsx`), its import/case in `SystemWorkspace.tsx`, and its
  import/mount in `artifacts/api-server/src/routes/index.ts`. Re-confirmed (post-rebase onto
  latest `origin/main`, which had touched `platform-agreements.ts` via the #2460 RBAC
  refactor) that no other file references `platform-agreements`, `PlatformAgreements`, or
  the route's public endpoints (`/api/platform/agreement/current`,
  `/acceptance-status`, `/accept`) — grep across `.ts`/`.tsx` came back empty except for
  the deleted file itself. Remaining `platformAgreementsTable` references
  (`msp-signup.ts`, `portal-checkout.ts`, `msp-billing-webhook.ts`,
  `admin-active-directory.ts`) query the Drizzle table directly for unrelated
  signup/checkout/AD flows — untouched, unaffected, schema stays. Committed
  `57871978f`, rebased onto `origin/main` (was `e2a07fbc7`, conflict on the file's
  modify/delete resolved by keeping the delete) as `6c90df013`, pushed directly to
  `main` (`e2a07fbc7..6c90df013`). `verify-branch-merged.mjs` confirms merged;
  `git status --porcelain` clean.
