# Build Journal — #3336

- **Status:** ✅ DONE 2026-09-10T18:14:15Z
- **Scope:** buildconsole

## Log
- 2026-09-10T18:06:36Z — IN FLIGHT. Implementing the corrected 6-point fix per #3336's current body (2026-09-10 update): `parent_number` already exists on `bt_issue_mirror` (via #3358, confirmed via `\d bt_issue_mirror`) and is already read back by `GitHubIssueMirror.MapBoardRow`. Real remaining gap: carry `ParentNumber` onto `GitHubIssueMirror.MirrorIssue` (the model `BatterUpRow`/`AiBatterUpRow`/build-set rollup actually consume), add a shared `EpicResolver` walking the chain to the top ancestor (same shape as `GitBoardIssueFilters.IsUnderInternalToolingEpic`'s cursor/seen walk), then group Batter Up / AI Batter Up by resolved top Epic and nest the BUILD SETS rollup under resolved Epic headers.
- 2026-09-10T18:14:15Z — ✅ DONE — commit `c0278ce14` on `main` (rebased from `agent/3336-q2208` @ `b0c088db5`, then pushed as `origin/main`'s HEAD; `verify-branch-merged.mjs` confirms ancestor OK). Changes:
  - `GitHubIssueMirror.MirrorIssue` now carries `ParentNumber`, reading the already-persisted `bt_issue_mirror.parent_number` (no new migration/sync — confirmed live via `\d bt_issue_mirror` and direct query against #3336/#1789's own real chain: 3336→1789→1202).
  - New `EpicResolver.ResolveTopEpicsAsync` — batch walk to the real top ancestor with a cycle guard, reusing `GitBoardIssueFilters.IsUnderInternalToolingEpic`'s cursor/seen-set shape, adapted to the mirror-backed model.
  - `BatterUpRow`/`AiBatterUpRow` carry `EpicNumber`/`EpicTitle`; `BatterUpPanel`/`AiBatterUpPanel` (`RenderFilteredRows`) group cards under a real Epic header (or an honest "No Epic" section), sorted by Epic number ascending, "No Epic" last.
  - `BuildQueuePanel`'s BUILD SETS rollup (`RenderBuildSetRollup`) resolves each set's real member Epics (`ResolveBuildSetEpicsAsync`, checking EVERY member, not assuming) and nests rollup rows under a real Epic header; a set spanning >1 distinct Epic renders under an honest "(mixed Epics)" header rather than silently picking one.
  - Verification: `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. Spot-checked real mirror rows against actual GitHub parent chains via direct `psql` query (not simulated): confirmed a real 2-level Feature→Epic chain (#1641→#1788 "Feature: Build Queue Panel (BuildConsole)"→#1202 "Epic: Build Console"; also #2009→#1570 "Feature: Projects (Portal)"→#1485 "EPIC: Portal"), a direct 1-level case (#1747→#1095 "EPIC: Admin Panel"), and several real un-parented items among current Batter Up/AI Batter Up board rows (#1781, #1909, #2037-2040, #2129) that correctly have no `parent_number` and will render under "No Epic". No live buildSet currently spans >1 distinct Epic (buildSets are built from one Feature/app by construction) — the mixed-epic path is exercised by code inspection (checked per-member, not assumed single) rather than a live/manually-constructed multi-Epic set; not executed against a running BuildConsole instance per the "never launch BuildConsole.exe to verify" hard rule.
  - `git status --porcelain` clean at completion. No findings filed — no genuine bug/gap surfaced outside this issue's own scope during the build.
