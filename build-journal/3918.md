# #3918 — Azure Tenant Credentials surface has no frontend consumer

- **Status:** ✅ DONE 2026-09-13T00:30:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 505ef8995ffd22208bb077cdfff671a07dfb2883

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Investigated `admin-azure-credentials.ts` route contract,
  confirmed `azure_tenant_credentials` table is empty locally, confirmed zero UI callers via
  grep. Building an Admin Panel list/detail CRUD page under System workspace, wired to the
  real CRUD + expiring-summary routes, plus a per-client attach section.
- 2026-09-13T00:30:00Z ✅ DONE — Built `artifacts/admin-panel/src/pages/AzureCredentials.tsx`:
  list view (GET /admin/azure-credentials, client name resolved via GET /admin/clients),
  create/edit form (POST/PUT, either an existing Key Vault secret name or a raw secret value
  written to Key Vault by the backend), delete (DELETE), and an expiring-summary banner
  (GET /admin/azure-credentials/expiring-summary) — the count badge the route's own doc
  comment described but was never built. Wired into `SystemWorkspace.tsx` and
  `workspaceNav.tsx` under System > Platform > "Azure Credentials". No fixture data — table
  is genuinely empty locally (confirmed via psql), which is correct given it never had a UI.
  Did not embed a section into `ProjectDetail.tsx` (the issue's alternative option) to avoid
  colliding with that page's unrelated `client_app_registrations` concept — a standalone
  page reading the real CRUD contract was the safer, unambiguous build.
  Verified: `npx tsc --noEmit` in `artifacts/admin-panel` — clean, exit 0.
  `git status --porcelain` clean, branch verified merged into origin/main
  (`verify-branch-merged.mjs` — OK).
