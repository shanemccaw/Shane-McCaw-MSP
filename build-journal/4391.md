# Build Journal — #4391

- **Status:** ✅ DONE 2026-09-16T00:20:00Z
- **Scope:** buildconsole

## Log
- Starting: apply same two-row split fix #4368 did for BuildRollupRow, to BuildEpicGroupHeader in desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs
- ✅ DONE — Confirmed current `BuildEpicGroupHeader` code matched the issue's description exactly (single horizontal `headerRow` StackPanel holding title + send button + needs-attention text + lock toggle). Applied the identical two-row Grid pattern #4368 already used for `BuildRollupRow`: row 0 = title (`headerText`) alone, full width; row 1 = a right-aligned `badgeRow` StackPanel holding the send button, needs-attention text, and lock toggle. Reparented the same element instances and existing click handlers (`sendButton.Click`, `lockButton.Click`, `statusText`/`hideTimer` logic) unchanged — no logic rewritten.

### Changes Made
- `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` — `BuildEpicGroupHeader`: replaced single `headerRow` StackPanel with a two-row `headerGrid` Grid (title row 0, right-aligned `badgeRow` row 1 for send/needs-attention/lock).

### Verification Results
- `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
- `node scripts/dev-server/verify-branch-merged.mjs` — OK, `agent/4391-q3044` (d0957724) merged into `origin/main`.
- `git status --porcelain` — clean.
- Commit: `d0957724a` — "Epic-group header two-row split: title full-width, badges/lock right-aligned below (#4391)"
