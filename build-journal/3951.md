# #3951 — Fix action.enforce-per-user-mfa / action.disable-per-user-mfa: beta Graph endpoint

- **Status:** ✅ DONE 2026-09-14T02:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 7369ddfb1

## Log
- 2026-09-14T01:37:24Z ⏳ IN FLIGHT — Investigating whether `graphWriteForTenant` needs the same
  absolute-beta-URL passthrough #3800 added to the read path, then updating
  `action.enforce-per-user-mfa` / `action.disable-per-user-mfa` endpoint columns to
  `https://graph.microsoft.com/beta/users/{{userId}}/authentication/requirements` and live-testing
  both directions against the testbed tenant before deciding whether to promote
  `write_action_catalog` row 115.
- 2026-09-14T02:05:00Z ✅ DONE — `graphWriteForTenant` (graph.ts:1102-1110) already had the
  absolute-beta-URL passthrough #3800 added to the read path — no code change needed, just the
  template's `endpoint` column. Wrote and ran
  `lib/db/migrations/manual/2026-09-14-per-user-mfa-beta-endpoint-fix-3951.sql` against local
  `DATABASE_URL`: updated both `action.enforce-per-user-mfa` and `action.disable-per-user-mfa`
  endpoints to `https://graph.microsoft.com/beta/users/{{userId}}/authentication/requirements`.
  Live-tested both directions against the real testbed tenant
  (`c4c814d4-3afe-441e-9145-62461d0a4fd3`), target: sanctioned Git #2840 synthetic identity
  `zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com` (objectId
  `bdb21dc3-146a-4d97-a128-a2b8ff618d35`), via `POST /api/admin/write-actions/<template>/execute`
  (the real `runBaselineTemplateAgainstTenant()` engine):
  - enforce: real 204, audit log id 53; follow-up GET (via `graphReadForTenantWithWriteToken`
    against the same beta URL, temporary scratch script deleted after use) confirmed
    `perUserMfaState: "enforced"`.
  - disable: real 204, audit log id 54; follow-up GET confirmed
    `perUserMfaState: "disabled"`.
  Both directions clean — promoted `write_action_catalog` row 115 from `blocked_no_workaround`
  to `execution_ready`, cleared `blocked_reason`. Config Pack orchestrator / Launch Control
  delivery path (`config_pack_templates` id=156, the sold `mfa-enforcement-v1` $299 pack) calls
  the same `runBaselineTemplateAgainstTenant()` reading the same DB-sourced endpoint column, so
  it is fixed by construction — no separate code path to touch.
  Commit `7369ddfb1`, pushed to `origin/main`, `verify-branch-merged.mjs` confirms ancestor.
  Working tree clean (`git status --porcelain` empty). No new finding filed — this build fixed
  the exact defect #3939 (this build's own predecessor) had filed as the open finding; nothing
  further to file.
