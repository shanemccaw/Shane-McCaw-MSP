# #4501 — 24 policy:/directory: checks have no remediation_knowledge_base row

- **Status:** ✅ DONE 2026-09-17T13:16:43Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 0666ea0e2 (rebased/pushed to main as 22a4aa840)

## Log
- 2026-09-17T13:00:23Z ⏳ IN FLIGHT — pulled #4501, confirmed via local Postgres that all 24 policy:/directory: check keys have zero remediation_knowledge_base rows; reviewed the existing authoring pattern (#1924/#2041-2056 domain migrations) and the 24 keys' real monitor_checks labels/descriptions to author accurate content next.
- 2026-09-17T13:16:43Z ✅ DONE — authored `lib/db/migrations/manual/2026-09-17-remediation-kb-policy-directory-domain-4501.sql`: real, published `remediation_knowledge_base` rows for all 24 policy:/directory: check keys, content verified against real Microsoft Learn docs searched/fetched this session (URLs in each row's `source_urls`). Ran the migration myself against local Postgres (`shanemccawmsp`); re-ran #4501's own SQL and confirmed 24/24 rows now exist and are `status = 'published'`. Confirmed the read path (`fetchPublishedKnowledgeBaseRows` in `artifacts/api-server/src/lib/remediation-knowledge-base.ts`) matches the exact `check_key IN (...) AND status = 'published'` shape these rows satisfy. Committed as 0666ea0e2, rebased onto origin/main, pushed as 22a4aa840; `verify-branch-merged.mjs` confirms the branch is an ancestor of origin/main. `git status --porcelain` clean. No genuine engineering-gap finding surfaced worth filing separately — the one design nuance noticed (policy:security-defaults' severity_rules fires on `isEnabled == false` alone, without cross-checking Conditional Access coverage) is already honestly disclosed in that check's own severity label text, so it's noted in the migration's row `notes` rather than filed as a new issue.
