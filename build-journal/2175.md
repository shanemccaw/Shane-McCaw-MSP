# Git #2175 — Epic #2138 ShaneBuilder: real custom Dialog system

- **Status:** ✅ DONE 2026-09-01T00:30:00Z
- **Scope:** buildconsole

## Log
- 2026-09-01T00:00:00Z — Started. Audited live `MessageBox.Show` call sites in
  `desktop/BuildConsole`: 24 real sites (issue estimated 16; grown since
  filing), excluding the stray `.target_lsb.cs.bak` backup file and
  `BUILD_LOG.md` (frozen archive, not source). Every real site is either an
  Alert (OK, with Error/Warning/Info icon) or a Confirm (Yes/No). No site
  today uses a text-input MessageBox shape, but the issue explicitly asks
  for Input to be built anyway as the forward-looking third shape. Building
  a custom-chrome `AppDialog` window (WindowChromeHelper + #2126/#2147
  Colors.xaml/Typography.xaml tokens) with a static call-site API:
  `AppDialog.Alert`, `AppDialog.Confirm`, `AppDialog.Input`.
- 2026-09-01T00:30:00Z — ✅ DONE. Built `AppDialog.xaml` / `AppDialog.xaml.cs`
  (custom WindowStyle="None" chrome + shell:WindowChrome + close-button
  title bar, matching BuildWatchWindow/MainWindow's own pattern; dark-mode
  DWM setup via `WindowChromeHelper.Setup`; colors pulled from
  `AppBackgroundBrush`/`PanelBackgroundBrush`/`StatusErrorBrush`/
  `StatusWarningBrush`/`AccentBrush` in Themes/Colors.xaml, sizing from
  `BodyFontSize` in Themes/Typography.xaml). Static API: `AppDialog.Alert`,
  `AppDialog.Confirm` (returns bool, true only on explicit Yes), and
  `AppDialog.Input` (returns string? — the forward-looking third shape;
  no live call site needed it yet but the issue explicitly asked for it).
  Replaced all 24 real `MessageBox.Show` call sites across App.xaml.cs,
  Controls/AiBatterUpPanel, Controls/BatterUpPanel,
  Controls/ChatMappingsDocumentView, Controls/DispatchPanel,
  Controls/GraphApiDocumentView (×3), Controls/LeftSidebar,
  Controls/SettingsTabView (×2), EditIssueDialog, MainWindow (×2),
  NeedsAttentionDetailDialog, NewIssueDialog, StagingDeployDialog,
  TestHistoryWindow (×3), TestRunnerWindow (×2). `grep -rn
  "MessageBox\.Show" desktop/BuildConsole --include=*.cs` now returns only
  doc-comment references inside AppDialog.xaml.cs/ToastEngine.cs and the
  pre-existing `.target_lsb.cs.bak` stray backup (not touched — not this
  session's file, left alone). Full `dotnet build BuildConsole.csproj -c
  Debug` — 0 warnings, 0 errors, before and after the call-site sweep.
  Commits: `a1a56ccf1` (bookend IN FLIGHT), `8fe12648e`→rebased→`a7396ed3b`
  (real work, on `main`). `verify-branch-merged.mjs` confirms merged; working
  tree clean.
