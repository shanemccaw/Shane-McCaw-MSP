# #1409 — Risk-accepted banner and drift chart still fake on Governance/Compliance/Security

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** ebcd2da19 (fix), 62c5f384d + 4cb6ca415 (manifest lastVerifiedAgainstCommit)

## Log
- 2026-08-27 ⏳ IN FLIGHT — fixing risk-accepted banner (hide unless real nonzero accepted-risk count) and drift trend chart (honest empty state instead of live.history ?? FIXTURE.history) on portal-v2-governance.tsx, portal-v2-security.tsx, portal-v2-compliance.tsx.
- 2026-08-27 ✅ DONE — Governance/Security: risk-accepted banner now reads `useRiskRegister()` (`GET /api/portal/risk-register`), filters by pillar + `status === "Accepted"`, and hides the whole banner (count, label, toggle) unless loaded, error-free, and nonzero — never `GOV_HERO.riskAccepted`/`SEC_HERO.riskAccepted`, never a visible "0". Compliance: same contract on the "documented policy decisions on record" strip (`usePolicyDecisions()`), replacing the old `cmpDecisionsCount ?? CMP_ACCEPTED_COUNT` fallback that still showed the fixture while loading. Drift chart: `DriftTrend` now takes a nullable `trend` and renders `NO_SCAN_DATA_LABEL` in an 84px-tall honest-empty slot instead of drawing a fixture's fake declining line when `live.history` has fewer than 2 points; Compliance's own inline chart (it doesn't use the shared component) gets the same treatment. Verification: `tsc --noEmit` clean across `artifacts/msp-portal`. Queried the local dev DB directly (`msp_risk_decisions`) and confirmed this tenant genuinely has zero `Accepted`-status rows for any pillar right now, which is why `test-manifests/portal/portal-v2-pillars.json`'s G2a-G2j (Governance) and S11-S11c (Security) risk-panel steps, and `portal-v2-pillar-heroes.json`'s C5 (Compliance), now assert the honest-absent state instead of the old always-visible one — the panel-innards steps against fixture row RSK-005 are removed with a note pointing at `RiskAcceptedPanel.tsx`/`riskPanelModel.ts`'s own unit tests as the surviving coverage, to be re-added end-to-end once a real Accepted decision exists in this tenant's register. Did NOT run these manifests live via `shaneapp://runTest` — the `shane-msp` MCP server that couriers that protocol failed to connect this session (CONNECTION_CLOSED), a genuine tool-availability gap, not a default "unable to verify."
