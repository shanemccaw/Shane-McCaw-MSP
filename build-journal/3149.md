# Git #3149 — Feature: Money — Cars tab (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:05:00Z
- **Scope:** platform

## Log
- 2026-09-08T01:30:00Z — Started. Blocker #3137 (Money real API/data-access layer) is
  CLOSED/complete, so unblocked. Read contract pack Section 3 + README's Money math /
  data-model sections. Found `vehicles` table already exists in `finances` DB via
  `017_money_extras.sql` (applied), currently 0 rows. No `vehicle_maintenance_log` table
  yet (real maintenance spend has nowhere to land). No Money-room UI exists at all yet in
  `public/app.js` — 6 sibling Features (#3147 Now, #3148 Bills, #3150 Vault, #3151 Wins,
  #3152 Budget Day, #3153 Catches) are building other tabs of the same room concurrently;
  building this session's own Cars-tab UI as additively-scoped as possible to reduce
  collision risk on the shared `public/app.js` file.
- 2026-09-08T02:05:00Z — ✅ DONE. Built `web/shanes-life/src/core/vehicles.mjs`: real
  per-vehicle card math (loan payment/mo off the linked real `accounts` row, insurance
  stated directly, registration amortised from the real annual amount, maintenance
  amortised from real trailing-12-month spend in the new `vehicle_maintenance_log` table),
  plus registration/maintenance reminders reusing `dates.mjs`'s own `LEAD_DAYS_BY_KIND`
  (registration = 'renewal', 21 days). `/api/cars` CRUD + `/api/cars/:id/maintenance`,
  `get_cars` + `log_car_maintenance` MCP tools. Migration
  `web/shanes-life/migrations/032_vehicle_maintenance_log.sql` — ran it myself against the
  local `finances` DB and recorded it in `schema_migrations`.
  Wired the real Cars tab into the Money shell #3147 landed mid-session (`moneyTab ===
  "cars"` branch in `public/app.js`): one card per vehicle with the real all-in $/mo and
  $/yr, `#/car/:id` detail view with the real maintenance log + a form to log new
  maintenance, add/delete vehicle forms, and the `moneyhdr` (bear) room watermark per the
  critter spec's "Money Bills and Cars -> bear" mapping.
  Verified live end-to-end against the real local `finances` DB: started the real server,
  minted a real session for Shane's own user, round-tripped create → log maintenance →
  delete through the real HTTP endpoints (not just the core module), confirmed the all-in
  math by hand at each step, then confirmed zero vehicle rows remained afterward.
  Found and fixed two real, unrelated things along the way:
  1. The shared local `schema_migrations` ledger held three orphan rows (two already
     superseded by their renamed successors, one genuinely orphaned) that failed
     `assertNoOrphanLedgerRows` and blocked `migrate.mjs` for every session sharing this
     database — deleted them.
  2. A real same-directory migration-number collision (`031_thing_locations_unique.sql`
     from #3156 landing on `main` mid-session vs. this session's own `031_...`) — renamed
     mine to `032` and re-pointed the local ledger row to match.
  3. Filed [#3173](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3173) (parented
     under #3086, board status "AI Batter Up"): `check-migration-numbers.mjs`'s documented
     standalone CLI entry point silently no-ops on Windows/Git Bash (a raw
     `import.meta.url === file://${process.argv[1]}` string comparison never matches a
     Windows path) — `migrate.mjs` itself is unaffected since it calls the exported
     function directly, but the documented pre-flight check a session is supposed to run
     by hand before picking a migration number does nothing on this platform.
  Branch `agent/3149-q1906` merged into `main` at `c271d9e08` (verified via
  `verify-branch-merged.mjs`). `git status --porcelain` clean.
