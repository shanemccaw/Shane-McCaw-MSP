# #1542 — Remediation: Items exit to the risk register when the customer declines

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `7e678ab62` (real work, verified on `origin/main` post-push), `eec6a95a8` (IN FLIGHT bookend)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1542/#1539/#1507/#1514/#1489/#1508/#1509. #1542's
  own "Depends on #1514, #1507": #1507 is shipped (acceptance/review clock split).
  #1514 has no build-journal bookend and is still `in-flight`-labelled on GitHub,
  but its actual mechanism (`declineRoutedChangeToRisk` /
  `createAcceptedRiskFromDecline` in `m365-change-router.ts`, landed under commit
  `060fd29c8` / #1496) is real and already on `main`, explicitly commented as
  "(#1514)". Mirroring that shipped pattern for the remediation-tracker side
  rather than re-litigating or waiting on #1514's own bookend. Building: a new
  `accepted_risk` terminal status on `remediation_tracker_steps`, a
  `spawned_by_remediation_step_id` back-pointer on `msp_risk_decisions`, a
  decline-to-risk endpoint reusing the risk-register's signed-acceptance shape,
  and the three-state (Verified/Accepted/Outstanding) read classification.
- 2026-08-30 ✅ DONE — Shipped. `accepted_risk` joins `remediation_tracker_steps`'
  status vocabulary (set ONLY alongside a real signed `msp_risk_decisions` row,
  left out of the re-verification allow-list on purpose so a background rescan
  can never silently overwrite an accepted risk). `msp_risk_decisions.spawned_by_remediation_step_id`
  back-pointer, mirroring `spawned_by_change_request_id`. Migration
  `lib/db/migrations/manual/2026-08-30-remediation-risk-decline-pointer-1542.sql`,
  run against local `DATABASE_URL` (confirmed via `\d msp_risk_decisions` and the
  `simulator_migration_runs` self-marker row, both re-queried after the fact).
  `remediation-tracker-risk-decline.ts` derives every required field from real
  data only (KB summary > scan finding > check label > catalogue title;
  `liabilityValueUsd` always 0, `graphEndpoint` always `""` — never invented),
  and sets both the legacy review-date display copy and #1507's real
  `reviewDueAt`/`reviewState` columns. `POST /api/portal/remediation-tracker/steps/:stepId/decline-to-risk`
  reuses the Risk Register's own signed-acceptance shape, `CustomerUser` floor.
  GET/PUT wire contract gains `terminalState: verified | accepted | outstanding`
  (#1542's own three states); PUT 400s a direct `accepted_risk` claim.
  `remediation-tracker-pricing.ts`: `accepted_risk` joins the
  resolved-but-still-outstanding bucket (phase-ready, fee stays flat).
  SCOPE STOP honored — no page/route/component under `artifacts/portal`; the
  one touch there is a pre-existing (unrouted) vocabulary mirror kept in sync
  so an existing drift-guard test didn't spuriously fail.

  **Real verification, before a concurrent build's `pnpm install` on the
  shared checkout started churning `node_modules` mid-session:** full affected
  test cluster 92/95 passing (3 pre-existing, unrelated ENOENT failures on
  missing msp-portal→portal guide files — filed as #1956); my own new
  `remediation-tracker-risk-decline.test.ts` 6/6; `remediation-tracker-pricing.test.ts`
  15/15 (incl. the new `accepted_risk` case); `portal-remediation-tracker.test.ts`
  28/28 (incl. all new decline-to-risk/PUT-guard cases); `lib/db` and
  `api-server` `tsc` both clean for every touched file (the api-server run
  surfaced 56 pre-existing, unrelated errors in other files — not reported as
  mine, per the standing rule). A real end-to-end INSERT of the exact row
  shape `declineRemediationStepToRisk()` writes was run against local Postgres
  and rolled back, confirming schema compatibility beyond the mocked unit tests.

  **Mid-session recovery, worth recording honestly:** after the first rebase
  (resolving a real conflict with a concurrent #1540 session's own new routes
  in the same file — merged by hand, both sets of routes preserved, both
  test suites' cases combined), this worktree's branch ref was found reset to
  a stale ancestor commit by some external process while I was waiting out the
  `node_modules` churn — `git reflog` showed only a single `reset: moving to
  HEAD` entry, and origin/main did not yet have my commit. The commit object
  itself was still intact locally (`git cat-file -t <sha>` → `commit`), so I
  recovered by resetting the branch back onto it, re-rebasing (clean, no
  conflicts this time) and pushing immediately. Re-verified with `git
  merge-base --is-ancestor` and `scripts/dev-server/verify-branch-merged.mjs`
  — both confirm `7e678ab62` is a real ancestor of `origin/main`. A final
  post-recovery re-run of the test/tsc cluster was blocked by the same
  ongoing shared-`node_modules` churn (`@types/node`, `express`,
  `@workspace/db` intermittently missing from the shared checkout across
  several direct filesystem checks) rather than by anything in this change —
  not reporting a clean final run I did not actually observe.

  Findings filed (parented to #1489, board = AI Batter Up): **#1956**
  (three drift-guard tests ENOENT — `previewRemediationGuide.ts` /
  `remediationLiveGuide.ts` missing from the repo entirely, not just moved),
  **#1957** (a risk decline for one of six multi-check-key steps only
  suppresses alerts on the first mapped check — real limitation, documented
  in `remediation-tracker-risk-decline.ts`'s own header).

  `git status --porcelain` clean. Not closing this issue.
