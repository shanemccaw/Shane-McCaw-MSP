# #4006 — Runbooks: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:26:48Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 3560c098e (page rebuild + manifest update), e5820c0f1 (manifest verification notes)

## Log
- 2026-09-14T04:13:54Z ⏳ IN FLIGHT — Investigated #4006 + #1488 (Feature: Runbooks). Real Design landed at
  `Design/portal/design_handoff_full_site/screens/Runbooks.dc.html` (Shane's 2026-09-14 reversal of the
  joint #1488/#1493 "no separate screen" decision). Found the page/route/nav already exist
  (`artifacts/portal/src/pages/runbooks.tsx`, route `/runbooks`, nav entry, from #2994) built against the
  *old* SOPs.dc.html `view:"runbooks"` layout — not the new standalone design. `useRunbooks` hook already
  carries the full #1557 cycle/run-history shape the new design needs (no backend gap). Rebuilding
  `runbooks.tsx` to match the new master-detail Runbooks.dc.html layout (list + selected detail, past
  cycles, hold-window event timeline via `loadHoldEvents`, "what this page deliberately does not do"
  ledger, action modals) against the real contract pack `docs/portal/runbooks-contract-pack.md`.
- 2026-09-14T04:26:48Z ✅ DONE — Rebuilt `artifacts/portal/src/pages/runbooks.tsx` (master-detail
  layout, past-cycle history, hold-window event timeline via `loadHoldEvents`, "what this page
  deliberately does not do" ledger, Dialog-based action modals for add-step/extend/close-early/
  release/prepare-cr). No backend change needed — `useRunbooks` already carried the full #1557
  cycle/run-history shape. Route (`/runbooks`) and nav entry were already wired from #2994 and are
  unchanged; SOPs.dc.html's own "Active Runbooks" sub-view is a separate, never-wired-separately
  design surface, not a competing implementation. `tsc --noEmit` and `vite build` both pass clean.
  Updated `test-manifests/portal/sops-runbooks.json` (discover-before-create; already covered
  `/runbooks`) with assertions for the redesigned copy/structure, tagged `4006-runbooks-redesign`.
  Ran `shaneapp://runTest` scoped to that tag against the live dev server (merged via
  `request-restart.mjs`, confirmed ancestor): navigation/route-serving passed for real, but the
  authenticated login half could not complete because `TEST_PORTAL_EMAIL`/`TEST_PORTAL_PASSWORD`
  are not configured as BuildConsole Test Environment Variables in this session — a real,
  specific environment-config gap, not a code failure. Substituted direct smoke checks:
  `GET http://localhost:5175/portal/runbooks` → 200, `GET http://localhost:8080/api/portal/runbooks`
  (unauthenticated) → 401. Full honest detail recorded in the manifest's own notes.
  Filed **#4022** (sibling sub-issue of Feature #1488, `bug` label, board status "AI Batter Up") for
  an unrelated finding hit during discovery: `test-manifests/portal/runbooks-hold-windows.json` is
  stale — still registered in `_regression-suite.json` but targets retired `portal-v2` slug-scoped
  routes and stale data-testids from before #1921's retirement of `artifacts/msp-portal`.
  Commits: `3560c098e`, `e5820c0f1`. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`); working tree clean.
