# #2024 — Policy Decisions: real own-table schema + sign-off endpoint

- **Status:** ✅ DONE (backend) — frontend wiring blocked, see Log
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 07ad08e1e (bookend IN FLIGHT), d1d9b41b3 (schema + migration + endpoint)

## Log
- 2026-08-30 ⏳ IN FLIGHT — re-scope of closed #1618/#1528: build Policy Decisions as its own table (own PK, own lifecycle), separate from msp_risk_decisions. Drizzle schema, manual migration, create/sign-off endpoint, wire portal-v2-policy-decisions.tsx signOff().
- 2026-08-31 ✅ DONE (backend only) — Added `policyDecisionsTable` (`policy_decisions`) to `lib/db/src/schema/msp.ts`: own PK, own lifecycle, no FK/discriminator tie to `msp_risk_decisions`, per #1528's decision. Manual migration `lib/db/migrations/manual/2026-08-31-policy-decisions-own-table-2024.sql` run against local Postgres (verified: 20 columns landed, indexes created). New route `artifacts/api-server/src/routes/portal-policy-decisions.ts` serves `GET/POST /api/portal/policy-register`, registered in `routes/index.ts`. POST combines the "Sign it off" form's fields (owner, review cadence, compensating control) with a real signature (typed name, server timestamp, statement, IP, sha256 hash) in one create-and-sign action — there is no unsigned intermediate state on this table. Logging: `logger.child({ channel: "tenant.portal" })`, matching the sibling risk-register route. Verified: `lib/db` and `artifacts/api-server` both typecheck clean (pre-existing unrelated errors in `admin-build-tracker.ts`/`sow-claim-binding-560.test.ts`/`admin-config-pack-run.ts` confirmed unrelated to this change); a direct Drizzle insert/read/cleanup round-trip against the local DB confirmed `decisionState` defaults to `live` and `reviewState` to `on_track` as designed.
  - **NOT done: wiring `portal-v2-policy-decisions.tsx`'s `signOff()`.** That file does not exist — `artifacts/msp-portal` (portal-v2) was retired at `f40438cdc` on 2026-08-29, two days before this issue was filed, in favor of `artifacts/portal` (#1485). `artifacts/portal` has no Policy Decisions page, and `Design/portal/` has zero `.dc.html` exports for this module — #1723 (Claude Design export into `Design/portal/`, "Shane's own action") is still open and unstarted. Per CLAUDE.md's own standing instruction ("no export there means no design — an agent must say so and stop... `portal-v2` is not a fallback target"), building a page or wiring a non-existent file was not attempted. Wired a real `blocked_by` edge from #2024 to #1723 and swapped the `in-flight` label for `blocked` so this is machine-readable, not just a comment. The backend built here (schema + migration + endpoint) is exactly what #1723's Design pass and the eventual wire step will need once the export lands.
  - Working tree clean at merge time (verified `git status --porcelain`); `agent/2024-q1049` verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE line.
