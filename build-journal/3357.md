# #3357 — Sales Offers and Sales Bundles (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T17:26:31Z (re-verified, unchanged)
- **Scope:** platform
- **Commit(s):** 31fbfc360 (pack), 5d3088f99 (IN FLIGHT bookend) — merged to origin/main as
  99fa04e18; verified with `verify-branch-merged.mjs` (exit 0) before writing DONE.

## Log
- 2026-09-10T00:04:30Z ⏳ IN FLIGHT — Read-only contract-pack extraction for
  `msp-sales-offers.ts` (425 lines, 9 routes) + `msp-sales-bundles.ts` (836 lines, 11
  routes), per the #1642/#2641 pattern. Both files, `sales-offer-engine.ts` (534 lines),
  `resolve-msp-id.ts`, the relevant slices of `requireAuth.ts`
  (`requireMspScope`/`assertCustomerAccess`/`isCustomerBlockedByStaffScope`),
  `msp-entitlement.ts` (`requirePlanFeature`), `sse-channels.ts`'s offer-pipeline channel,
  and the Drizzle schema for `sales_offers`/`sales_offer_events`/`msp_sales_bundles`/
  `msp_sales_bundle_assignments`/`monitoring_packages` all read in full. Cross-checked
  live against local PostgreSQL (`\d` on all five tables, row counts, and the one live
  `msp_sales_bundles` row's actual `monitoring_package_keys` against real
  `monitoring_packages.key` values) — a real, only-live-discoverable finding surfaced:
  the one live "Customer Dashboard Category Tabs" bundle (mspId 1, from
  `lib/db/migrations/manual/2026-07-19-customer-dashboard-category-tabs.sql`) stores
  unprefixed package keys (`"executive"`, `"identity-access"`, ...) that match zero real
  `monitoring_packages.key` rows (all 21 of which are prefixed, e.g. `cat-executive`) even
  though the migration's own `_dash_cat` source table — read directly, unchanged since
  its original commit — defines those same 10 keys WITH the `cat-` prefix throughout
  Parts A/B/C of one single transaction. Writing the pack now.
- 2026-09-10T00:12:54Z ✅ DONE — Wrote
  `docs/msp-console/sales-offers-and-bundles-msp-console-contract-pack.md` (11 sections,
  565 lines): full wire contract for all 20 routes across both files (§1/§3), the shared
  `sales-offer-engine.ts` internals (§2), cross-surface edges including the three real
  paths to `sales_offers.state = "accepted"` (§4), real enum unions incl. the never-written
  `suspended` assignment status (§5), orphaned-endpoint check confirming zero live callers
  (§7 — `artifacts/msp-console` exists and runs but is still the bare #2668 scaffold), the
  honest-empty/live-data contract incl. all 21 real `monitoring_packages` rows carrying
  `platform_cost_cents = 0` (§8), and the forbidden list (§9). Three real findings filed as
  sibling sub-issues of #1571 (this issue's own direct EPIC parent — no Feature-tier parent
  exists for #3357), each `bug`-labeled, milestone v1.1, board status "AI Batter Up": **#3386**
  (`PATCH /api/msp/sales-offers/:id/state` can move an offer to the terminal `"accepted"`
  state with zero fulfillment — no SOW, no Stripe charge, no tier gate — and no exit
  transition exists, so the result is permanently stuck), **#3387** (both route files allow a
  `PlatformAdmin` `?mspId=` cross-MSP override on routes shaped exactly like
  `resolveMspIdStrict()`'s own documented no-override convention — same class as the existing
  #2731, two fresh instances), **#3389** (the one live `msp_sales_bundles` row's
  `monitoring_package_keys` match zero real `monitoring_packages.key` rows, discoverable only
  by cross-checking live DB against `lib/db/migrations/manual/2026-07-19-customer-dashboard-
  category-tabs.sql`, whose own source is internally inconsistent with what actually ran).
  Read-only: no product code, schema, or data changed. Merged 31fbfc360 to origin/main (as
  99fa04e18); verified with `verify-branch-merged.mjs` (exit 0) and `git status --porcelain`
  clean before writing DONE.
- 2026-09-10T17:26:31Z ✅ DONE (re-dispatched, re-verified, unchanged) — this build was
  re-dispatched against #3357 (`BuildSet: BatterUpClearOut`). Confirmed `e3c42f67f` (the prior
  session's DONE bookend commit) is already an ancestor of this worktree's `HEAD`, and both the
  pack and this bookend file were already present on disk — the deliverable is genuinely done
  and merged, no re-extraction needed.

  Re-verification did surface real drift: three RBAC-retirement commits landed on `main` after
  the pack was written (`bba7eeb9e`/`3dddd4b26`/`c770ac7dd`, #2458/#2460) that remove
  `requireRole`/`ROLE_ORDER` entirely (replaced by `requireCapability`/a shared ladder
  evaluator) and shift line numbers throughout both route files, `requireAuth.ts`, and both
  schema files (`lib/db/src/schema/index.ts`/`msp.ts`) — confirmed live via `git diff
  31fbfc360..origin/main --stat` and direct `grep -n` against current `requireAuth.ts`/schema
  line numbers. The pack's substantive analysis and its three findings (#3386/#3387/#3389)
  remain correct — this is a mechanical rename plus one route's role-check rewrite, not a
  behavior change — but essentially all of the pack's ~150 file:line citations are now stale,
  breaking its own stated "every field cited to file:line" discipline. Not hand-patched in this
  session (the shifts are non-uniform across 7 files and amount to a fresh re-extraction pass,
  not a citation-only fix). Filed as **#3533**, parented as a sibling sub-issue of #1571 (same
  parent as #3386/#3387/#3389), `bug`-labeled, milestone v1.1, board status "AI Batter Up."
  Posted a comment on #3357 explaining the duplicate dispatch and pointing to #3533. No code
  changed this session beyond this bookend. `git status --porcelain` clean before and after.
