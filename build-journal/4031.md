# #4031 — Repoint admin CRM Testimonials page to merged /api/admin/testimonials/all

- **Status:** ✅ DONE 2026-09-14T18:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8ad574c9b

## Log
- 2026-09-14T17:35:00Z ⏳ IN FLIGHT — reading contract pack §4 field-presence table and current Testimonials.tsx / admin-testimonials.ts route to confirm the real merged response shape before repointing.
- 2026-09-14T18:05:00Z ✅ DONE — `artifacts/admin-panel/src/pages/crm/Testimonials.tsx` now calls `GET /api/admin/testimonials/all` (the merged endpoint) instead of `/api/admin/closures/signed`. Confirmed via the route source (`admin-testimonials.ts:68-141`) that `/admin/testimonials/all` is already a superset of `/admin/closures/signed`'s closure rows plus the new `customer_testimonials` source, so a single call replaces the old one without losing data — no "in addition to" second call needed. Rendering now branches on the real `source: "project_closure" | "customer_testimonial"` tagged union per the contract pack's §4 field-presence table: `projectTitle`/`projectType` only read for `project_closure` rows, `customerName` only for `customer_testimonial` rows, and a new `SourceBadge` distinguishes them. Regrouped from two buckets to three using the union's real shared fields (`permissionToPublish`, `body`): Published (permission + body), Awaiting Permission (body, no permission — a real state for portal submissions that didn't exist for closures alone), Signed Off — No Testimonial (no body, closures only in practice since `customer_testimonials.body` is required at submission). `grep -rn "admin/closures/signed" artifacts/admin-panel/src` now returns zero matches. Verified: `pnpm --filter admin-panel run typecheck` (`tsc -p tsconfig.json --noEmit`) exits 0. No fixture/hardcoded data — all rows come from the real merged endpoint. `git status --porcelain` clean, `verify-branch-merged.mjs` confirmed ancestor of origin/main before and after this commit. No findings filed this session — the only new-work gap identified (#4032, approval→discount mechanism) was already filed by the contract pack itself, not discovered here.
