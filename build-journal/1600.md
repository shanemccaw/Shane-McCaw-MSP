# #1600 — BuildConsole: a build must not start while its GitHub blocker is still open

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** e73bc04dd

## Log
- 2026-08-28 ⏳ IN FLIGHT — Read #1483/#1482 real failure, QueueWatcherService.cs dispatch
  path, BuildQueuePostgresClient.GetNextAsync's local-row blocker check, the mirrored
  HTTP-fallback logic in artifacts/api-server/src/routes/admin-build-tracker.ts
  (isBlockerCleared/queue/next), and 5cfcb364 (Verifying→Done promotion from Git Board
  refresh — confirmed prime suspect: local queue-row state, never GitHub open/closed,
  decides "cleared"). Implementing: live GitHub re-query at dispatch (fail closed on
  unreachable), replacing the local-row check entirely; hold-reason surfaced in queue UI;
  immediate re-evaluation wired to the existing Git Board refresh.
- 2026-08-28 ✅ DONE — e73bc04dd. Both real dispatch paths (BuildQueuePostgresClient.
  GetNextAsync direct-Postgres, and admin-build-tracker.ts's GET /queue/next HTTP
  fallback) now re-query GitHub live for every declared blocker right before claiming,
  no exceptions for a local done/verifying row, a cleanly-exited session, or commits on
  main; GitHub-unreachable holds too (fail closed). Old local-row checks
  (AreBlockersClearedAsync/IsBlockerClearedAsync, isBlockerCleared/areBlockersCleared)
  removed. GitHubIssuesService.TryGetOpenIssueNumbersAsync distinguishes "unreachable"
  from "genuinely nothing open". Queue UI now shows the real live hold reason
  ("waiting on #NNNN (open)") via QueueWatcherService.HeldBlockerReasons.
  LeftSidebar.BoardRefreshCompleted → QueueWatcherService.RequestImmediateReevaluation
  releases a held build the moment Shane refreshes the Git Board, not just on the next
  10s poll. C# build: 0 errors (46 pre-existing warnings, unchanged). TS typecheck: 0
  new errors (43 pre-existing, same set as baseline just at shifted line numbers).
  Verified live end-to-end: created a real open GitHub test issue (#1613, since
  closed), inserted a real bt_build_queue row blocked by it, ran the actual
  GetNextAsync against the real local Postgres DB — held (dbStatus stayed 'queued',
  heldReason "waiting on #1613 (open)"); closed #1613; re-ran the same call — claimed
  (dbStatus flipped to 'running'). Test row deleted afterward. verify-branch-merged.mjs
  confirms e73bc04dd is a real ancestor of origin/main.
