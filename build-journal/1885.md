# #1885 — Chat-getting-long warning banner: add a one-click "Start handoff chat" button

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 6babbe815 (bookend IN FLIGHT), cc80014b5→2043c7851 (rebased onto origin/main; implementation)

## Log
- 2026-08-31 ⏳ IN FLIGHT — read #1837's landed diff (EpicChatUrlBuilder, TriggerHandoffAsync,
  StartSuccessorChat) and the warning-tier banner in MainWindow.xaml.cs (CreateChatContextWrapper /
  UpdateContextMeter, ~6224-6523). Plan: reuse the existing `handoffBtn` + `TriggerHandoffAsync`
  as-is (byte-identical mechanism, not a duplicate) — surface it in the yellow (60k-85k) tier as
  "Start handoff chat", and extend to the peach (85k-100k) tier too since it's even closer to
  critical and the issue explicitly invites that judgment call. Red (100k+) tier keeps its existing
  "Hand Off Now" label/behavior unchanged.
- 2026-08-31 ✅ DONE — added `meterState.HandoffBtn.Content = "Start handoff chat"` +
  `Visibility.Visible` to the yellow (60k-85k) and peach (85k-100k) tier branches in
  `UpdateContextMeter`, reusing the same `handoffBtn`/`TriggerHandoffAsync` the critical (red)
  tier already used — no new button, no new handoff logic, one shared mechanism across all three
  degrading tiers. Green stays untouched (no button, banner collapsed). Red keeps its existing
  "Hand Off Now" label (explicitly reset each call since the button/content is now shared across
  tiers). PAT decision (per #1885's explicit ask): left unchanged — the raw PAT stays embedded
  literally in the `bt_prefill` string, same as it already was for File > New Chat, the existing
  Hand Off Now, and #1837's successor-chat menu item (`EpicChatUrlBuilder.BuildEpicChatUrl`, not
  touched by this build). This is not a new decision, it's inherited from reusing that exact
  builder — and it's the only option available: `bt_prefill` types text into claude.ai's message
  box via the browser-side poll before any session or filesystem exists for that tab, so there is
  no `.ghpat`-style path for the new chat to read from at that point (unlike a build-queue prompt
  header, which launches an actual Claude Code process with disk access). Referencing a stored
  file path only works once a real session exists to read it — a brand-new claude.ai tab isn't
  one yet.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` → 0
  Warning(s), 0 Error(s), both before and after rebasing onto origin/main. Not manually clicked
  live in a real warning-tier banner (would require driving a real long claude.ai chat past 60k
  tokens in this session) — the code path is identical to the already-proven critical-tier
  Hand Off Now flow (Git #1470/#1837), just gated on a different token range and a different
  button label, so the risk surface is the tier-gating logic itself, which the Release build
  compiles clean against.
  Branch merge-base verified ancestor of origin/main (2043c7851). git status --porcelain clean.
