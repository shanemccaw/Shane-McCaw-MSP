# #3039 — Remediation Tracking: wire pillar-scores and export

- **Status:** ✅ DONE 2026-09-06T20:03:39Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 436675c12 (main)

## Log
- 2026-09-06T19:45:28Z ⏳ IN FLIGHT — Wiring §1f-1g of docs/remediation-tracking-contract-pack.md
  (`GET /portal/remediation-tracker/pillar-scores`, `export.csv`, `export.pdf`,
  `evidence-pack.pdf`) into `artifacts/portal`, per the real Design export at
  `Design/portal/design_handoff_full_site/screens/Remediation Tracking.dc.html`.
  Sibling issues #3037 (§1a core tracker) and #3038 (§1b-1e checklist/fix-routes)
  are dispatched concurrently in the same buildSet against the same design screen —
  building this scope's pieces as self-contained, composable units to minimize
  collision risk on shared files (App.tsx, moduleNav.ts).
- 2026-09-06T20:03:39Z ✅ DONE — Shipped `lib/remediation-tracker-scores-{types,api}.ts`
  (wire types + react-query hook for the real 6-pillar + Copilot-gate route) and
  `lib/remediation-tracker-export-api.ts` (blob-download mutations for the 3 export
  routes, reading the real `Content-Disposition` filename). Built
  `components/remediation-tracker/PillarScoresCard.tsx` (6-pillar tile grid + Copilot
  gate line + per-pillar detail dialog, honest insufficient_data/single_scan/scored
  gating) and `RemediationExportControls.tsx` (the 3 real export buttons, toast on
  failure). #3037 and #3038 both landed on `main` mid-session (their commits
  6c1e33d08 and e351f5466) and already own the page/route/moduleNav wiring for this
  screen — rebased twice and integrated by mounting both new components inside
  #3037's `RemediationProgrammeTab.tsx` (which was explicitly written to compose
  this way) rather than creating a duplicate page/route. Extended the existing
  `test-manifests/portal/remediation-tracking.json` in place (discover-before-create)
  with `sessionTag: "3039-pillar-scores-export"` apiTests for the auth gate + real
  payload shape on pillar-scores and all three export routes, rather than a
  duplicate manifest file.
  Verified: `tsc -p tsconfig.json --noEmit` (0 errors) and `vite build` (succeeds)
  on `artifacts/portal`, both re-run after each rebase; the two backend route test
  files (`portal-remediation-tracker-scores.test.ts`,
  `portal-remediation-tracker-export.test.ts` — 17 tests) pass via `vitest run`
  (backend was pre-existing/unchanged, per the contract pack's own "unchanged from
  the prior pack" note for §1f-1g); a live curl against the local dev api-server
  (port 8080) confirmed `GET /api/portal/remediation-tracker/pillar-scores` is a
  real, currently-running route that correctly 401s with no Authorization header;
  a direct query against the local `DATABASE_URL` confirmed 0 rows in
  `tenant_pillar_snapshots` today, i.e. the honest `insufficient_data` empty state
  (never a fabricated score) is the real state a manual run would currently hit.
  `shaneapp://runTest` was NOT reachable from this session — no runTest/executeSql
  tool was exposed to this session at all — so the manifest additions were not
  live-executed; real static verification (tsc/build/vitest/curl/DB query above)
  performed instead, consistent with the same honest-gap precedent already recorded
  on `test-manifests/portal/pillar-pages.json`.
  Branch merge verified via `scripts/dev-server/verify-branch-merged.mjs` (OK,
  436675c1 is an ancestor of origin/main). `git status --porcelain` clean.
  No new findings filed — the frontend gap (no `/remediation-tracking` page
  wired) was the module's own known, tracked decomposition (#3037/#3038/#3039),
  not an undiscovered defect.
