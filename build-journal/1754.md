# #1754 — Regenerate pnpm-lock.yaml (msp-portal → portal rename never landed)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 176fbd7d8 (bookend IN FLIGHT), a6513723c (lock regeneration)

## Log
- 2026-08-29 ⏳ IN FLIGHT — regenerating pnpm-lock.yaml via `pnpm install --lockfile-only` at repo root; verifying node_modules junction (target `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\node_modules`, mtime 2026-08-29T23:59:15.050Z) is untouched before/after; sweeping for other importer drift beyond the portal rename; then checking whether this resolves #1750's realpathSync issue.
- 2026-08-29 ✅ DONE — `pnpm install --lockfile-only` at repo root regenerated the lock (commit
  `a6513723c`). Confirmed via `fs.lstatSync`/`fs.realpathSync` before and after that
  `node_modules`'s mtime (`2026-08-29T23:59:15.050Z`) and junction target
  (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\node_modules`) were byte-for-byte unchanged —
  only `pnpm-lock.yaml` (1798 deletions, 12 insertions) was touched. `artifacts/msp-portal` is
  gone from `importers:`; `artifacts/portal` is present. Swept all 20 real on-disk packages
  matched by `pnpm-workspace.yaml`'s globs (`artifacts/*`, `lib/*`, `lib/integrations/*`,
  `scripts`) against the lock's importer list and `pnpm -r list --depth -1` — every one resolves,
  none orphaned. Checked whether `artifacts/portal`'s source actually references the
  dashboard-canvas/debug-console/json-viewer/@fluentui/react-grid-layout/react-resizable deps the
  issue called out as dropped during #1673 — grep found only a stale comment mentioning
  `dashboard-canvas`, no real imports — so nothing further to fix there; the regenerated lock
  already reflects `artifacts/portal/package.json` accurately. Checked #1750: after the lock fix,
  `fs.realpathSync('node_modules/@workspace/db')` from inside `artifacts/api-server` still resolves
  into the main checkout (`C:\Source\...\lib\db`), not the worktree — the relative symlink issue is
  untouched by this fix. Posted that evidence on #1750 (unrelated, stays open) rather than asserting
  a causal link. Pushed both commits directly to `origin/main` (bookend `176fbd7d8`, lock fix
  `a6513723c`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
