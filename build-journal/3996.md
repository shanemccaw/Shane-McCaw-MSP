# #3996 — Team Management and Invitations: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T04:15:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** `1cd4df094` (the work, rebased to `3bfb8adeb` on `origin/main`)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Read #3996 + #1656 (+ #2895 contract pack, #3647 role-assignment
  follow-up). Design export confirmed at
  `Design/portal/design_handoff_full_site/screens/Team Management.dc.html`. Backend
  (`artifacts/api-server/src/routes/portal-team.ts`) has 12 real routes including the
  #3647 `PATCH /portal/team/:userId/role` and `GET /portal/team`'s `isCustomerAdmin`/
  `hasBillingRole` fields — but the landed design's own state-switcher/logic class
  does not draw any Billing/Customer-Admin role UI at all (no role picker, no role
  badge, `sel.roleLine` only shows title/department). Building the page faithfully to
  the design as given (per "Copy is final" / no unrequested UI-shape decisions), and
  filing a finding for the missing role-assignment surface in the design rather than
  inventing my own UI for it.
- 2026-09-14T04:15:00Z ✅ DONE — Built `artifacts/portal/src/pages/customer-team.tsx`
  (roster + selected-member detail panel, invite modal, per-member access/recovery
  action modals: suspend/reactivate, sign-out-everywhere, MFA enforcement toggle,
  unlock, password-reset email, temp password, MFA reset, emergency bypass code,
  manager/"reports to" picker with client-side cycle guard mirroring the server's
  own), a new `useTeamLive`/`teamWire` wire layer, route registered at `/team` in
  `App.tsx`, nav entry added to `UserMenu.tsx` (gated on holding a `customerId`
  claim, matching `GET /portal/team`'s own auth model). Extended
  `GET /portal/team` (`portal-team.ts`) to also project `managerUserId` — the
  contract pack's own §5 finding noted this was never selected on the roster
  read even though the manager-assignment route existed since #2527; the page's
  "Reports to" panel needed it, so it's now real, wired data rather than a
  second unbuilt lookup.
  Filed **#4013** (parented under #1656, labeled `bug`, board status "AI Batter
  Up") — the landed design draws no Billing/Customer-Admin role-assignment UI
  at all even though the backend (#3647's `PATCH /portal/team/:userId/role`,
  plus the roster's `isCustomerAdmin`/`hasBillingRole` fields) already supports
  it; built the page faithfully to what the design actually draws rather than
  inventing a role picker myself.
  New test manifest `test-manifests/portal/team-management.json` (registered in
  `_regression-suite.json`) — auth-gate + authenticated-roster-read apiTests,
  plus the honest sign-in-required uiStep (no portal login page exists yet, same
  structural gap `billing.json`/`account-security-*.json` already document).
  **Verification:** `tsc -p tsconfig.json --noEmit` clean in both
  `artifacts/portal` and `artifacts/api-server`; `vite build` succeeds in
  `artifacts/portal`; `vitest run src/routes/portal-team.test.ts` in
  `artifacts/api-server` — 14/14 passing (unchanged pre-existing suite, still
  green against the extended `GET /portal/team` response shape).
  `shaneapp://runTest` was **not** run — a real, bounded check (Git #2160): no
  `shaneapp://` tool is exposed in this session (ToolSearch found nothing), and
  independently `node scripts/dev-server/status.mjs` confirms the local
  api-server is `DEAD` on port 8080, unrelated to this build. Same structural
  gap `billing.json`'s own notes document (dev-server checkout doesn't have
  this branch merged/deployed yet).
  Branch verified merged (`node scripts/dev-server/verify-branch-merged.mjs` —
  OK, `3bfb8adeb` on `origin/main`); `git status --porcelain` clean.
