# #2536 — Git panel read-only git queries still swallow stderr and exit code

- **Status:** ✅ DONE 2026-09-03T13:57:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 2fd959e21

## Log
- 2026-09-03T13:47:27Z ⏳ IN FLIGHT — fixing the three read-only git query sites in
  `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs` (`RefreshGitStatusAsync`,
  `PopulateGitBranches`, the commit-graph loader) to drain stderr and check `ExitCode`,
  reusing the exact `RunGitCommand`/`BestGitLine` error-surface #2535 already built,
  instead of an empty `catch {}` silently rendering an empty panel.
- 2026-09-03T13:57:00Z ✅ DONE — all three sites now drain stdout+stderr before
  `WaitForExit`, check `ExitCode`, and on non-zero set
  `GitStatusSummaryText.Text = "✗ git <cmd> failed — <BestGitLine(stderr, stdout)>"`
  (with full raw output on the ToolTip), same surface #2535 built for `RunGitCommand`.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings,
  0 errors. Verified against a real repo: success case (`git status`/`git branch -a`/
  `git log` all clean, exit 0) and a real failure case (corrupted `.git/HEAD` in a
  scratch repo under `/tmp/gittest`, all three commands genuinely exit 128 with a
  `fatal: not a git repository...` stderr line) — confirmed `BestGitLine` correctly
  picks that `fatal:` line via its existing keyword match. Commit `2fd959e21`, pushed
  to `main`, verified merged via `verify-branch-merged.mjs`. Working tree clean.
