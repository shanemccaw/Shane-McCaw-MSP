# #4402 — Discovery: what does the Portal actually read to gate tier/module visibility?

- **Status:** ✅ DONE 2026-09-16T17:00:48Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** afdcd06a2 (bookend IN FLIGHT only — this issue is read-only discovery, no product code changed)

## Log
- 2026-09-16T16:55:53Z ⏳ IN FLIGHT — read #4402 and #4401 in full; starting read-only trace of tier/module gating: DB schema (min_bundled_tier), Portal frontend enforcement in artifacts/portal/src, and how Monitoring's Foundation/Growth/Premier tier names relate to it. No code changes planned — discovery only, answer goes as a comment on #4402.
- 2026-09-16T17:00:48Z ✅ DONE — Traced the real contract and posted it as a comment on #4402 (https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4402#issuecomment-5701345519). Answer: Portal reads `client_services` (status='active') joined to `services` (service_type='monitoring_tier') → `services.type_attributes->'includedFeatures'` jsonb array, via `resolveCustomerIncludedFeatures`/`requireTierFeature` in `artifacts/api-server/src/lib/portal-tier-features.ts:81-101,194-214`, composed since #4191/#4192 into `evaluateAccess()` at `lib/db/src/rbac/access.ts:259-315`. `min_bundled_tier` (on `write_action_catalog`, `lib/db/src/schema/msp.ts:4413`) is a SEPARATE axis gating Launch Control write-actions, not Portal module visibility — same foundation/growth/premier vocabulary (renamed 1:1 by rank per `lib/db/migrations/manual/4035-min-bundled-tier-real-vocab.sql`) but a different table and different consumer. Confirmed live: 12 real `monitoring_tier` rows in `services` (queried directly against local Postgres), Foundation(1-4)/Growth(5-8)/Premier(9-12) by sort_order with includedFeatures counts 2/6/13-17. Portal frontend enforcement cited at `artifacts/portal/src/lib/poams-api.ts:93,130`, `ownership-api.ts:54-55`, `policy-decisions-api.ts:40-41`, `components/sops/useSops.ts:148,183`, `pages/ownership.tsx:75`, `pages/policy-decisions.tsx:75`, `pages/poams.tsx:21,92,156`. No findings requiring a filed issue — this was a clean, already-well-documented contract. No genuine blockers. Working tree clean.
