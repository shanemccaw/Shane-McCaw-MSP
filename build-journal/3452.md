# #3452 — msp-poams.ts generic PATCH lets MSPOperator bypass the MSPAdmin-gated cancel route

- **Status:** ⏳ IN FLIGHT 2026-09-11T04:44:41Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T04:44:41Z ⏳ IN FLIGHT — fix the generic `PATCH /api/msp/poams/:poamId` route
  (`artifacts/api-server/src/routes/msp-poams.ts`) so it refuses an incoming
  `status: "cancelled"` the same way it already refuses `"active"`, closing the
  role-floor bypass described in the issue body. `"completed"` stays untouched —
  the issue explicitly frames it as an open product decision, not a confirmed
  violation.
