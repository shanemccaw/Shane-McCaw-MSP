# #3452 — msp-poams.ts generic PATCH lets MSPOperator bypass the MSPAdmin-gated cancel route

- **Status:** ✅ DONE 2026-09-11T04:49:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** ba0b454ae (fix + live-db regression test), on `main`

## Log
- 2026-09-11T04:44:41Z ⏳ IN FLIGHT — fix the generic `PATCH /api/msp/poams/:poamId` route
  (`artifacts/api-server/src/routes/msp-poams.ts`) so it refuses an incoming
  `status: "cancelled"` the same way it already refuses `"active"`, closing the
  role-floor bypass described in the issue body. `"completed"` stays untouched —
  the issue explicitly frames it as an open product decision, not a confirmed
  violation.
- 2026-09-11T04:49:00Z ✅ DONE — added an explicit `status === "cancelled"` guard on
  the generic PATCH (`msp-poams.ts:249-252`), redirecting callers to the dedicated
  `MSPAdmin`-gated `PATCH /api/msp/poams/:poamId/cancel` route, mirroring the
  existing `"active"` guard immediately above it. `tsc --noEmit` on
  `@workspace/api-server` clean. Added
  `msp-poams-role-floor.live-db.test.ts` (live Postgres, real rows,
  synthetic/suffixed, cleaned in `afterAll`) proving: (1) an `MSPOperator`'s
  PATCH with `status: "cancelled"` now 409s and the row stays `active`; (2)
  `MSPAdmin` can still cancel via the dedicated route; (3) `MSPOperator` can
  still edit ordinary narrative fields via the generic PATCH. Ran it directly
  against local `DATABASE_URL` — 3/3 passed. Also re-ran the sibling
  `msp-poam-rbd-conversion.live-db.test.ts` to confirm no regression — 4/4
  passed. Commit `ba0b454ae` rebased onto and pushed straight to `origin/main`
  (verified via `verify-branch-merged.mjs`). Working tree clean
  (`git status --porcelain` empty). No fixture/hardcoded data introduced.
  `"completed"`'s missing dedicated-route/role-gate question is a real product
  decision (per the issue body) — left alone, not silently built around.
