# Build Journal — #4095

- **Status:** ✅ DONE 2026-09-15T01:18:20Z
- **Scope:** platform

## Log

- 2026-09-15T01:17:20Z — Added `RetainerNoConsent`/`RetainerConsented` to `mspRole` enum in
  `lib/api-spec/msp-openapi.yaml`, matching `LEGACY_ROLE_ORDER` in
  `lib/db/src/rbac/legacy-ladder.ts`. Re-ran orval codegen (`msp-zod` and
  `msp-api-client-react` projects only, to avoid clobbering the unrelated `zod` /
  `api-client-react` projects sharing the same config). Confirmed via
  `grep -rl mspRole` that no other spec (main `openapi.yaml`) references this enum, so no
  further regen needed.
- 2026-09-15T01:18:20Z — ✅ DONE — Real diff isolated to 4 files (verified via
  `git diff --stat` after reverting the many LF/CRLF-only touches orval's clean-output step
  caused across the rest of `generated-msp/`): `lib/api-spec/msp-openapi.yaml`,
  `lib/api-zod/src/generated-msp/mspApi.ts`,
  `lib/api-zod/src/generated-msp/types/mspUserProfileMspRole.ts`,
  `lib/api-client-react/src/generated-msp/mspApi.schemas.ts`. Committed as `5fc635813`
  ("Add RetainerNoConsent/RetainerConsented to mspRole enum"), rebased onto
  `origin/main`, and pushed. `verify-branch-merged.mjs` confirms `agent/4095-q2835`
  (5fc63581) is an ancestor of `origin/main`. `tsc --noEmit` on `lib/api-zod` passes.
  Working tree clean (`git status --porcelain` empty).
