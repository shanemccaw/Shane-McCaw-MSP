# #2092 — policy_decisions.reviewDueAt is never computed from reviewCadence

- **Status:** ⏳ IN FLIGHT 2026-09-03T03:52:00Z (stopped, awaiting Shane's decision — see log)
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** fb16ec8df (this bookend file only — no product code changed)

## Log
- 2026-09-03T03:37:35Z ⏳ IN FLIGHT — investigating: confirmed `reviewCadence` is genuinely
  free text (`z.string().trim().min(1).max(100).optional()` in
  `artifacts/api-server/src/routes/portal-policy-decisions.ts`, no fixed vocabulary anywhere
  in the schema or frontend — module isn't wired to a UI yet per #1724). The schema's own
  comment on `reviewDueAt` (`lib/db/src/schema/msp.ts:8388-8390`) independently reaches the
  same conclusion the issue body does: "parsing free-text cadence into a due date is a real
  product decision, not ordinary backend work."
- 2026-09-03T03:52:00Z ⏳ IN FLIGHT — **stopping to ask, not writing code.** This is the
  CLAUDE.md carve-out ("real product decision... hand those to Shane with the options stated
  plainly"), confirmed by two independent sessions' own code comments reaching the same
  conclusion (this issue's body, and `lib/db/src/schema/msp.ts:8388-8390`). Posted the two real
  options (fixed enum vs. best-effort parser + fallback, and what each needs decided) as a
  comment on #2092: https://github.com/shanemccaw/Shane-McCaw-MSP/issues/2092#issuecomment-5519975160.
  No code changed beyond this bookend. Not writing DONE — this stays IN FLIGHT/awaiting-decision
  until Shane picks an option or supplies the cadence vocabulary, at which point the writer is a
  same-session build (schema + XOR constraint already exist).
