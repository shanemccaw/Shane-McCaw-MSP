# #2877 — Broader set of test files intermittently 5000ms-timeout under full-suite parallel vitest run

- **Status:** ✅ DONE 2026-09-04T18:20:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** c131314b2 (fix), 2e629ef03 (merge to main)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — picking up #2865's follow-on: reproduce the broader file
  list from #2877's issue body (admin-observability.test.ts,
  admin-signal-rules-for-check-511.test.ts, msp-customers-bulk.test.ts,
  msp-suspended-banner.test.ts, portal-checkout.test.ts's "401 without Authorization header"
  case, plus the less-consistent set: admin-signal-rules-category-pillar-469.test.ts,
  admin-signal-rules-import.test.ts, admin-signal-rules-orphan-guard.test.ts,
  msp-diagnostics.test.ts) with real full-suite runs, then decide per-file vi.setConfig patches
  vs. a global vitest.config.ts testTimeout/hookTimeout bump based on real evidence from this
  session's own runs.
- 2026-09-04T18:20:00Z ✅ DONE — Reproduced with `node --env-file=.env.local
  node_modules/vitest/vitest.mjs run` (real DATABASE_URL, matching CI; `--env-file` used
  instead of `source .env.local` because the file contains an unescaped multi-line PEM block
  bash mangles). 3 clean pre-fix full-suite runs consistently showed 10-11 timeout-shaped
  file failures — the issue's named set plus `admin-active-directory-credential-ops.test.ts`
  (new, not in #2877's original list) — confirming the set is real and growing, matching the
  issue's own description. **Judgment call (per the issue's own "Ask"):** given the growing,
  run-to-run-unstable file set, chose a small global `testTimeout: 20_000, hookTimeout: 20_000`
  bump in `artifacts/api-server/vitest.config.ts` over continuing #2865's per-file
  `vi.setConfig()` pattern — a per-file patch would only ever chase the set after the fact,
  and the root cause (thread-pool transform contention under the full ~305-file run) is
  identical across every affected file, so one global knob is the more maintainable real fix.
  #2865's own 4 named files keep their existing per-file `vi.setConfig()` (untouched, no
  conflict — the global value matches what they already set). Verified: 2 post-fix full-suite
  runs dropped from 11 failed files / 20 failed tests to 2 failed files / 11 failed tests.
  Confirmed both remaining failures are real, pre-existing, non-timing bugs (reproduce
  identically in isolation, no other tests running): `dashboard-data.test.ts` (`.toMatch()`
  called against an object, not a string) and `msp-suspended-banner.test.ts` (all 10 tests —
  its manual `@workspace/db` mock is missing `RETENTION_CLOCK_RUNNING_TENANT_STATUSES`, which
  `msp-portal.ts` now statically imports via `subscription-state.ts`). Filed both as new
  issues, parented to #2491 (this issue's own parent Feature), board status "AI Batter Up":
  #2878 (msp-suspended-banner mock gap) and #2879 (dashboard-data assertion-shape bug). One
  residual, much rarer edge case: `repo-source-nul-byte-guard.test.ts` (its own explicit
  30_000ms timeout, unaffected by the global bump since explicit timeouts override it) timed
  out once across 5 total runs (4 post-fix) — did not reproduce again on a follow-up run;
  noted here as an honest residual rather than filed separately, since a single non-reproducing
  occurrence isn't yet a stable finding. `git status --porcelain` clean; branch verified merged
  into origin/main via `verify-branch-merged.mjs` (required an `origin/main` merge first — the
  worktree's base had gone stale by the time this build finished, #2875's work had landed on
  main in the interim; merged cleanly, no conflicts).
