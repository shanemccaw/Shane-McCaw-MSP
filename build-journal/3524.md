# #3524 — Webhooks: unify the event catalog into one vocabulary (v2)

- **Scope:** platform
- **Status:** ✅ DONE 2026-09-10T22:05:00Z
- **Log:**
  - 2026-09-10T22:00:00Z — ⏳ IN FLIGHT — dispatched to build against #3524's own
    scope (unify webhook event catalog). Pulled the issue body + comments before
    touching anything.
  - 2026-09-10T22:03:00Z — Investigation found #3524 was already **CLOSED
    (COMPLETED)** before this build session started — Shane's own comment on the
    issue records it as "duplicate-fulfilled" by commit `66220b1bd`, which had
    already landed on `main` under the original #1607 (which #3524 had refiled/
    superseded). Verified `66220b1bd` is present in this worktree's `main` history.
  - 2026-09-10T22:04:00Z — Verified all four of #3524's scope items against the
    real, current code:
    1. `SUBSCRIBABLE_EVENT_TYPES` is the single authoritative list, served by
       `GET /api/portal/webhooks/event-types` and consumed live by
       `artifacts/portal/src/pages/webhooks.tsx:190` (fetches the endpoint rather
       than hardcoding). ✅ done.
    2. The fixture `WEBHOOK_EVENTS`/`webhooksData.ts` and the older stale hardcoded
       `ALL_EVENT_TYPES` copy no longer exist anywhere in the repo (portal-v2 was
       retired in the `f40438cdc` rename to `artifacts/portal`) — confirmed via
       repo-wide grep, zero hits. ✅ done.
    3. `createWebhookSchema`/`updateWebhookSchema` in
       `artifacts/api-server/src/routes/webhooks.ts` validate `eventTypes` against
       `SUBSCRIBABLE_EVENT_TYPES` via a shared `eventTypesSchema` (landed in
       `66220b1bd`, with a passing vitest guard). ✅ done.
    4. **`signal.fired`** — still listed in `SUBSCRIBABLE_EVENT_TYPES`
       (`webhooks.ts:31`) with no dispatch call site anywhere (confirmed via grep —
       only the declaration and a doc-comment reference in
       `msp-console-webhooks.ts:39` mention it). This was explicitly left as an
       open decision for Shane in the #1607 closing comment ("Not closing this
       issue — that's your call"), but both #1607 (closed `NOT_PLANNED`) and #3524
       (closed duplicate-fulfilled) were closed without that decision actually
       being made, orphaning the open question. **Filed as new finding #3547**,
       parented under #1597 (`Feature: Webhooks (Portal)`, #3524's real Feature
       parent), labeled `bug`, board status set to "AI Batter Up".
  - 2026-09-10T22:05:00Z — No code changes made this session — the real work
    (#1607's scope items 1–3) was already done and merged before this build
    started; item 4 has no safe unilateral resolution (a genuine product
    decision: drop `signal.fired` vs. build its dispatch) and is now tracked as
    its own open, correctly-parented issue instead of silently lost.

**Commit(s):** none — no code change required; this build's only output is the
filed finding #3547 and label/board-status housekeeping on #3524/#3547.

**Verification:** confirmed `66220b1bd` is on `main` (already merged, predates this
build). Confirmed all four scope items' real current state via grep/file reads
against the live worktree, not assumption.

**Labels synced:** #3524 — removed `in-flight`, added `complete` (issue was already
closed by Shane; label reflects that the code state is confirmed correct/complete).
#3547 (new) — `bug`, board status "AI Batter Up".

**Working tree:** clean except this bookend file itself (added by this commit).
