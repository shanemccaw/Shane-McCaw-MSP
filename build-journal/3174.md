# #3174 — Today tray still shows a generic web-page header bar, not the real phone-native design

- **Status:** ✅ DONE 2026-09-08T02:08:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 5c8699fda (rebased/pushed to main as 084e42f15)

## Log
- 2026-09-08T01:54:48Z ⏳ IN FLIGHT — confirmed the generic `<header class="app-header">` (view-title
  + Sign out) at `web/shanes-life/public/index.html:104`. Confirmed `viewSettings()` already has
  "Sign out everywhere" (`app.js:3759`) as the real sign-out path. Scope per the issue: remove the
  header for the Today route only (its own fox/weather scene is the real header per the design
  README, ¶29); leave it in place for every other room, which hasn't had its own Round 2 redesign
  pass yet (design README ¶26 — those rooms get their own "little house back-link" header per
  design, not a bare removal). Reporting real sequencing rather than stripping it everywhere.
- 2026-09-08T02:08:00Z ✅ DONE — implemented (084e42f15): `render()` in `app.js` now hides
  `#app-header` and toggles `#app-view.no-header` for the Today route only; `app.css` collapses
  `.view`'s top padding to `max(0.9rem, env(safe-area-inset-top))` (the same allowance the header
  itself used to reserve) when that class is present, so Today's fox/weather scene starts
  immediately below the native status bar with no header row, matching the real reference
  screenshot. Every other room is unchanged -- header still shows, per the open sequencing
  question in the issue (they haven't had their own Round 2 pass yet). Sign out stays reachable
  via `viewSettings()`'s "Sign out everywhere" (`app.js:3759`).
  Verification: `node -c app.js` passes; diff reviewed and confirmed scoped to exactly the three
  touched files. Live browser verification against the local dev server was attempted but
  blocked by a pre-existing, unrelated local-DB migration-ledger drift (`schema_migrations` holds
  a `034_catches.sql` row that has never existed in this repo's history) -- filed as #3180
  (parented under #3086) rather than guess-fixed, since the fix requires confirming real intent
  first. Static code review is the honest verification level actually achieved here.
  Working tree left clean (`git status --porcelain` empty); branch verified merged into
  `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
