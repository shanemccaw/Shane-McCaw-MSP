# #2354 — Import: merged note shows "+N merged", click to split back out; "Undo merges" resets all

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Feature: Test Pad, #2326)
- **Started:** 2026-09-02
- **Commit(s):** 1936841c020e17f39498bdfffd054f3c0a05c669 (real work), rebased/pushed as 2e412ee59a65eaa2f4fdfc4fc5e77387e6be7473 on origin/main

## Log
- 2026-09-02T19:47:00Z ⏳ IN FLIGHT — Test Pad's Import-from-Notepad merge-up feature (#2352/#2353) already folds selected/per-row candidates up into a preceding row and shows a "+N merged" badge, but the badge wasn't clickable and there was no way to undo a merge. Adding: (1) a pre-merge snapshot (`TextBeforeMerge`/`NeedsShotBeforeMerge`) captured on first fold into a given anchor row, (2) `TestPadImportMerger.SplitBackOut` to restore one anchor's snapshot and un-merge its direct children, wired to a click on the "+N merged" badge, (3) `TestPadImportMerger.UndoAllMerges` (splits every anchor back out) wired to a new "Undo merges" button next to the preview header, visible only while at least one row carries a merge.
- 2026-09-02T19:58:00Z ✅ DONE — Shipped: `TestPadImportCandidate.TextBeforeMerge`/`NeedsShotBeforeMerge` snapshot fields; `TestPadImportMerger.MergeOneUp` captures them on the first fold into a given anchor; `TestPadImportMerger.SplitBackOut(anchor)` restores the snapshot and un-merges the anchor's direct `MergedChildren`; `TestPadImportMerger.UndoAllMerges(candidates)` splits every anchor back out (order-independent, handles nested merge chains); wired the "+N merged" badge's `MouseLeftButtonDown` to `SplitBackOut` + `RenderPreview()`, and added a new `BtnUndoMerges` button next to the preview header (`TestPadImportWindow.xaml`), toggled visible in `UpdateMergeBar` exactly while any row carries a merge. Verified: `dotnet build -c Debug` on `desktop/ShaneBuilder/ShaneBuilder.csproj` — 0 warnings, 0 errors, both before and after rebasing onto origin/main. `git merge-base --is-ancestor HEAD origin/main` confirms the branch landed. `git status --porcelain` clean. No new UI test manifest — this is desktop/ShaneBuilder (WPF), explicitly out of scope for test manifests per CLAUDE.md.
