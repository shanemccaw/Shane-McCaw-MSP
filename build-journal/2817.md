# Build Journal — Issue #2817

- **Status:** ✅ DONE 2026-09-06T16:07:03Z
- **Scope:** buildconsole

## Log
- 2026-09-06T00:00:00Z — Started. Investigating root-cause re-query inefficiency behind #2815's circuit breaker: `gh issue view` spawned every tick for already-DONE issues (#2133, #2736), and `BoardStatusSync.Mirror` re-firing "Verifying" repeatedly for #2736.
- 2026-09-06T16:07:03Z — ✅ DONE — Investigated against real `%AppData%\BuildConsole\logs\activity-*.log`:
  - `BoardStatusSync.Mirror`'s "Verifying" call site (`QueueWatcherService.cs` ~line 1482) fires exactly once per completion, and today's/yesterday's logs confirm it — each `#N` appears once, not repeatedly. The #2736 repeat in the issue's original evidence was a symptom of the #2815 rate-limit storm on 2026-09-04, already resolved by the #2815 circuit breaker + #2890's bounded-concurrency/cooldown fix (merged after this issue was filed).
  - The real remaining root cause: `BuildQueuePostgresClient.GetQueueAsync()` returns EVERY `bt_build_queue` row ever created (no status filter, no time bound), and `BuildQueuePanel.TriggerBackgroundIssueTitleQueries()` fired a background `gh issue view` warm-up for every row's `AssociatedIssueNumbers` + blockers on EVERY refresh tick regardless of the row's status. A terminal (`done`/`canceled`/`failed`) row from days/weeks ago kept contributing its own issue number and its blockers to that warm-up forever — confirmed live in logs: `gh issue view #2133`/`#2815` (both long-done builds) still fired once per app session (~7h apart across restarts on 2026-09-05), and any transient failure (e.g. rate-limit circuit open) was only ever on a flat 60s cooldown (#2890) — retried forever, never truly given up on.
  - Fix (commit `bb64facd`, rebased+pushed as `d37b60f9`):
    1. `TriggerBackgroundIssueTitleQueries` now skips `BuildQueuePostgresClient.IsTerminalStatus` rows entirely before collecting issue numbers to warm — a terminal row's own title already comes from the local `title` column, and nobody is viewing its blocker ghost card anymore.
    2. Added a real escalating give-up (`_titleFetchConsecutiveFailures` + `ApplyEscalatingTitleFetchCooldown`): after 5 straight transient failures for a number, the retry cooldown escalates from 60s to 1h instead of retrying forever at a fixed cadence. A later success resets the count immediately (self-healing).
  - Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
  - Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0) before writing this DONE.
  - `git status --porcelain` clean at completion.
  - No findings filed — the two named symptoms (#2133/#2815/#2736 re-queries, Mirror re-fire) both traced to real, now-fixed causes; no new gap discovered.
