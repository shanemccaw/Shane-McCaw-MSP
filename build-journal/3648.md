# #3648 — Portal billing routes read only the caller's own rows, so a Billing or Customer Admin holder cannot see the org's invoices

- **Status:** ✅ DONE 2026-09-12T21:18:53Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** b8659b13d (pushed as 6559ad2a6 after rebase)

## Log
- 2026-09-12T20:58:50Z ⏳ IN FLIGHT — Investigation done (issue body + Shane's dispatch comment read; confirmed `#3629`'s narrowing, the RBAC capability model, and that neither `invoices` nor `client_services` carries its own tenant id — only `client_user_id`). Implementing a shared `billingScopeUserIds` helper and applying tenant-wide scoping to the 6 handlers Shane's comment names: `portal-billing.ts` `GET /portal/invoices`, `GET /portal/invoices/:id`, `GET /portal/billing/stripe-receipts`, `GET /portal/billing/subscriptions`, and `portal-retainer-billing.ts` lines 72/114/228 (`GET retainer-intervals`, `POST switch-interval`, `POST cancel-interval-switch`). The money-moving writes in `portal-billing.ts` (pay/cancel/resume) are NOT in Shane's named list, so left caller-scoped — that "may a Billing holder act on a colleague's subscription" question is still explicitly open per the issue body.
- 2026-09-12T21:18:53Z ✅ DONE — Added `artifacts/api-server/src/lib/portal-billing-scope.ts` (`billingScopeUserIds`) and wired it into exactly the 6 named handlers. `GET /portal/invoices/:id`'s downstream project/contracts lookups now key off `invoice.clientUserId` (the real owner) instead of the caller, so a cross-user invoice view returns the right project/contracts. The two retainer-billing writes now audit-log the row's real owner as `clientId` while `actorUserId` stays the caller. New tests: `lib/portal-billing-scope.test.ts` (helper unit tests, 4 passing), `routes/portal-retainer-billing-scope.test.ts` (3 passing), plus 3 new cases added to `routes/portal-billing.test.ts` (6 passing total in that file). Ran the full `artifacts/api-server` vitest suite (`npx vitest run`): 4861 passed / 29 failed / 19 skipped across 364 files — the 29 failures (DATABASE_URL not set in this shell, pre-existing azure-rm probe failures) are pre-existing/environmental and do not touch any file this build changed (grepped the run's output for `portal-billing`/`portal-retainer` — zero hits). `npx tsc --noEmit` clean. Filed **#3843** (parented under #1696, milestone v1.1, label `bug`, board status "AI Batter Up") for a genuine but out-of-scope finding: `artifacts/portal/src/pages/billing.tsx`'s "your ledger" / "You have never been invoiced" copy assumes personal-only ownership and is now stale once a Billing/Customer Admin holder sees the whole tenant's rows — a copy/product decision, not a backend gap. Working tree clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this.
