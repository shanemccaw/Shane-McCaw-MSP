# #2914 — Backfill Plaid transaction name/description for already-synced rows (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T22:12:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** bc08c1f21

## Log
- 2026-09-04T21:59:48Z ⏳ IN FLIGHT — Follow-up to #2913. Adding a real /transactions/get-based
  backfill: new IPlaidClient method + pagination, a PlaidBackfillService doing
  `UPDATE transactions SET name = ... WHERE plaid_transaction_id = ... AND name IS NULL`, and a
  UI trigger in MainWindow.
- 2026-09-04T22:12:00Z ✅ DONE — Added `IPlaidClient.GetTransactionsAsync` (Plaid's date-ranged,
  offset-paginated `/transactions/get`, distinct from the cursor-based `/transactions/sync` path)
  + `TransactionsGetRequest`/`TransactionsGetOptions`/`TransactionsGetResponse` wire DTOs in
  `PlaidClient`/`PlaidJsonModels.cs`. New `PlaidBackfillService.BackfillAllAsync` iterates every
  `plaid_items` row, computes the real MIN/MAX date among that item's `transactions` rows with
  `name IS NULL` (skips the item entirely if none), pages `/transactions/get` over that range, and
  runs `UPDATE transactions SET name = @name WHERE plaid_transaction_id = @id AND name IS NULL`
  per returned transaction — never touches `sync_cursor` or an already-named row. Wired to a new
  "Backfill Transaction Names" button in `MainWindow.xaml`/`.xaml.cs`, same pattern as the
  existing "Sync Now" button. `dotnet build ShanesSurvival.sln` clean (0 warnings, 0 errors).
  No test project exists for this desktop app (Test Coverage scope is msp-portal/marketing site
  only) and no live Plaid/Postgres credentials were available in this session to run the backfill
  end-to-end against real data — verification is the clean build + code read-through against the
  proven PlaidSyncService pattern it mirrors. Commit `bc08c1f21`, pushed straight to `origin/main`
  and confirmed merged via `verify-branch-merged.mjs`. `git status --porcelain` clean.
