# #2591 — Policy Decisions + Policy Engine: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-13T00:28:47Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 5e1afa98a (real work), cb7cac5dd (IN FLIGHT bookend)

## Log
- 2026-09-13T00:15:21Z ⏳ IN FLIGHT — Read #2591's body + BUILD comment, the landed design
  export (`Design/MSP_Console/design_handoff_msp_console/Policy Engine.dc.html`), the
  refreshed contract pack (`docs/msp-console/policy-decisions-msp-console-contract-pack.md`,
  #3779), and every real backend route it documents
  (`msp-policy-decisions.ts`, `msp-standing-policies.ts`, `msp-policy-engine-settings.ts`,
  `msp-active-directory.ts`, `msp-rbd.ts`'s `available-obligations`). Building the
  `PolicyEngine` module page against `artifacts/msp-console`'s existing shell (#3677) and
  `Sales.tsx`'s established module patterns. Added one new backend endpoint
  (`GET /api/msp/active-directory/ous?customerId=`) because nothing under `/api/msp/*`
  listed a customer's real OUs — the standing-policy author/edit form genuinely needed a
  picker and the contract pack's own §4 named this exact gap.
- 2026-09-13T00:28:47Z ✅ DONE — Shipped the Policy Engine module page
  (`artifacts/msp-console/src/console/modules/PolicyEngine.tsx`) mounted at `/ops/policy`
  (Operations tree, per README screen 50), wired to `policy-api.ts`'s hooks against every
  real route the contract pack names: per-customer Policy Decisions list + guarded manual
  clearance-resolve; Standing Policies list/author/edit/deactivate (#3034)/evaluate/
  enactment-route preview/evaluation history, rendering `route` and `reason` as two
  separate values per the dispatch's own load-bearing note; the per-customer Policy Engine
  opt-in toggle; the real obligation register (#1525, `msp-rbd.ts`'s
  `available-obligations`) shown read-only, platform-seeded and MSP-authored rows in one
  catalog; and the OU-assignment authority note (#2148) pointing at the real
  `/api/msp/active-directory/ou/:id/assignments` route without duplicating its own
  dedicated screen (#46). Surfaced #3035's settled "no MSP-side create for a policy
  decision" limitation on the screen itself, not worked around. Added the one real backend
  gap found along the way — `GET /api/msp/active-directory/ous?customerId=` — with 6 new
  test cases (`msp-active-directory.test.ts`, 33/33 green). Filed one unrelated,
  pre-existing finding hit while typechecking (#3898, a stale `Promise.resolve` mock cast
  in `auth-impersonation.test.ts`), parented as a sibling of #1685 per the standing rule,
  labeled `bug`, board status set to AI Batter Up.

  **Verified:** msp-console `tsc --noEmit` clean; its 16-case `node:test` suite green
  (including the tree/command-palette count updates from two concurrent Operations-page
  builds — Offboarding #2636, Status Reports #3765 — landing mid-session); a real
  `vite build` succeeds. api-server `tsc --noEmit` clean for every file this build touched
  (2 pre-existing unrelated errors elsewhere, both already tracked — #3881, and #3898
  filed this session); `msp-active-directory.test.ts` 33/33 green.

  Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`
  (exit 0) after real work landed at `5e1afa98a`. Working tree clean
  (`git status --porcelain` empty) before writing this DONE line.
