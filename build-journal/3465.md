# #3465 — portal-billing.ts capability gates (customer:billing.view + billing.manage)

- **Status:** ✅ DONE 2026-09-11T00:31:07Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** `2892e1cc0` (work) — bookend commits `5729b5572` (IN FLIGHT) + this DONE update
- **Migration:** `lib/db/migrations/manual/2026-09-10-rbac-billing-manage-3465.sql` — run against local Postgres in-session; appended to #1630 for Staging
- **Findings filed:** #3587 (who holds billing.view / billing.manage — Shane's decision), #3589 (portal-offers.test.ts red on main, pre-existing)

## Log
- 2026-09-10T23:55:13Z ⏳ IN FLIGHT — gating the 10 `requireAuth`-only routes in `artifacts/api-server/src/routes/portal-billing.ts` through the #2455 capability evaluator: reads on `customer:billing.view`, pay/cancel/resume/resubscribe/customer-portal writes on the real billing-write capability if the model already has one.
- 2026-09-11T00:31:07Z ✅ DONE — Shipped `2892e1cc0`: new `requireCustomerCapability` gate (rbac-capability.ts) on all 13 billing routes — portal-billing.ts's 10 plus portal-retainer-billing.ts's 3 (same surface, also requireAuth-only). Reads → `customer:billing.view`; writes (pay, cancel, resume, resubscribe, customer-portal, switch-interval, cancel-interval-switch) → new `customer:billing.manage`, since no customer-side billing-write capability existed (`msp:purchases.approve` is a different system's authority). billing.manage catalogued, transcribed in legacy-ladder.ts, registered as a #3360 fail-closed divergence in parity-check.ts, seeded to every rung (same as billing.view) so no one's access moved — narrowing is #3587. Verification: new portal-billing-capability-gates.test.ts 45/45 (real routers + real gate: 403 with no handler DB read, view/manage independent, unseeded → 503, no-rung token → 403); requireCapability-keys customer scan green; portal-billing / retainer / portal-team / admin-rbac green; lib/db rbac 62/62; `tsc --noEmit` clean for api-server + lib/db; check-rbac-parity vs local Postgres 962/962 agreed. Re-verified after rebase onto concurrent #3408 RBAC changes. Unrelated reds: portal-offers.test.ts 12/12 red on pre-commit main `0197e75f8` too → #3589; portal-assessment-debug-{trigger-scan,reset-session} time out only under multi-file parallel runs, 5/5 in isolation on `2892e1cc0` (#2865/#3066 contention class). verify-branch-merged OK. No pre-existing dirty files in this worktree.
