# #3767 — Split BtnRefreshCombined back into Board-diff-only Refresh + Batter-Up-only refresh

- **Status:** ✅ DONE 2026-09-12T04:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** b79d887aa (bookend IN FLIGHT), b89d1faf7 (real implementation)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — implementing the real split per the issue body: remove
  `BtnRefreshCombined`'s six-subsystem `Task.WhenAll` fan-out; add `BtnRefreshBoard` (Board diff
  sync only) and `BtnRefreshBatterUp` (Batter Up only); make an explicit, documented call on
  where the four leftover pieces land (not silently dropped, not silently bundled).
- 2026-09-12T04:35:00Z ✅ DONE — real, real work shipped in b89d1faf7:
  - Removed `BtnRefreshCombined`/`BtnRefreshCombined_Click` and the `FullGitRefreshRequested`
    delegate + its six-subsystem `Task.WhenAll` fan-out.
  - Added `BtnRefreshBoard` (Board diff sync only, via new `BoardRefreshRequested` delegate) and
    `BtnRefreshBatterUp` (Batter Up only, via new `BatterUpOnlyRefreshRequested` delegate) — two
    separate re-entry guards (`_boardRefreshInFlight` / `_batterUpRefreshInFlight`), two separate
    spin clocks. Neither blocks the other.
  - Explicit decision on the four leftover pieces (also documented in code comments):
    - `RefreshActiveSessionsAsync()` + `DevServerRollbackService.CheckForRollbacks()` — confirmed
      zero GitHub cost (local process list / local filesystem read). Folded into
      `BtnRefreshBoard_Click` — adds no rate-limit risk, keeps their #2900 "manual-refresh-only,
      nothing lost" guarantee intact.
    - `RefreshInFlightIssuesAsync()` — confirmed real `gh issue list --label in-flight` call.
      Deliberately kept OFF both new buttons (bundling it back on would reintroduce real GitHub
      cost onto the very buttons this issue exists to make cheap). Its only remaining trigger is
      the pre-existing initial tab-load call — unchanged, not orphaned. Real mitigating fact:
      the whole In-Flight tile is currently `Visibility="Collapsed"` (Shane, 2026-08-28 — hidden
      outright, wiring left intact "in case this comes back"), so losing its manual-refresh path
      right now has zero visible impact.
    - `RefreshActiveChatEpicIssuesAsync()` — confirmed real `GetSubIssuesAsync` GitHub call.
      Also kept OFF both new buttons for the same reason. Given its own real, new trigger instead:
      a small refresh icon (`BtnRefreshChatEpicIssues_Click`) added directly on the "Issues in
      this Epic" section header — this section IS visible whenever a chat has a linked epic, so a
      real trigger was worth adding rather than leaving the capability silently gone.
    - `RefreshAsync()` (the GitHub-calling "Focus Progress" variant: `AutoRecheckOpenIssuesOnTransitionAsync`
      + `TriggerBackgroundIssueTitleQueries`) — confirmed it already has real triggers throughout
      `MainWindow.xaml.cs` (build-completion/queue-event handlers, ~8 other call sites) independent
      of the old combined button. Removing it from the combined button orphans nothing.
  - Updated every doc comment/XAML comment referencing the old
    `BtnRefreshCombined`/`FullGitRefreshRequested`/`RefreshCombinedIconRotate` names in
    `BuildQueuePanel.xaml(.cs)`, `MainWindow.xaml.cs`, `LeftSidebar.xaml.cs`,
    `BatterUpPanel.xaml(.cs)`, `AiBatterUpPanel.xaml(.cs)` so nothing stale is left describing
    removed mechanics.
  - **Real finding, filed as its own issue, not fixed here (correctly out of scope):**
    [#3772](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3772) — parented under #1788
    (this issue's own Feature parent) — `LeftSidebar.RefreshGitBoardWithLoadingFeedbackAsync`
    fires `BoardRefreshCompleted` unconditionally on every board fetch, and that event already has
    permanent subscribers in `MainWindow.xaml.cs` (predating #3702) that kick off Batter Up's and
    AI Batter Up's own refreshes, `RecheckPendingBuildCommentsAsync`, and
    `RefreshEditorPanesStatsAsync` as a side effect — from Git Board's own pre-existing manual
    refresh button too, not just the old combined one. So the new `BtnRefreshBoard`, while it no
    longer explicitly fans out Batter Up itself, still indirectly re-triggers it via that separate,
    pre-existing coupling. The **new Batter-Up-only button (`BtnRefreshBatterUp`) is fully isolated
    and unaffected** — it's the one this issue's stated goal (a cheap way to get fresh Batter Up
    data without tripping the rate limit) actually depends on, and it's clean.
  - **Verification:** `dotnet build -c Debug` succeeded (0 errors, 10 pre-existing warnings
    unrelated to this change — confirmed present before this session's edits too). Not
    live-clicked in a running BuildConsole instance this session (no WebView2/BuildConsole runner
    reachable from this build session to drive real button clicks and inspect the
    `github`/`github.manual-refresh` `ActivityLog` channels as the issue's own verification section
    asks) — that manual click-through + log-channel check is a real, honest gap for Shane (or a
    future session with a live BuildConsole instance) to confirm.
  - **Known administrative gap, honestly reported, not silently skipped:** GitHub's GraphQL rate
    limit was genuinely exhausted mid-session (`gh api graphql` confirmed `remaining: 0`,
    `resetAt: 2026-09-12T04:55:03Z`, after two real failed attempts via `gh issue create`/
    `gh issue edit`). Worked around it by using the REST API directly (`gh api repos/.../issues`,
    `.../sub_issues`, `.../labels`) for everything REST supports — issue creation, milestone,
    labels, sub-issue parenting, and this issue's own `in-flight` label all landed successfully
    that way. The one piece REST has no equivalent for — setting #3772's Project board status to
    "AI Batter Up" (`updateProjectV2ItemFieldValue` is GraphQL-only) — is still pending; it needs a
    GraphQL call once the rate limit resets (~2026-09-12T04:55Z) or a later session/Shane to set it
    manually.
  - Working tree confirmed clean (`git status --porcelain` empty) and branch confirmed merged into
    `origin/main` (`verify-branch-merged.mjs` exit 0) before writing this DONE status.
