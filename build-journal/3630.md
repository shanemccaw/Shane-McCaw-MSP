# #3630 — Multi-repo: periodic worktree sweep and secondary-repo clones are not multi-repo-aware

- **Status:** ✅ DONE 2026-09-12T23:10:06Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 73728570d

## Log
- 2026-09-12T22:58:42Z ⏳ IN FLIGHT — Read #3630 body + comment. Read worktree-lifecycle.mjs's
  `sweepWorktrees` (single-repo, hardcoded `config.mainRepoRoot`) and repo-clone.mjs's
  `resolveRepoCheckout` (persistent secondary clone, never evicted). Plan: (1) make
  `sweepWorktrees` iterate every real repoRoot represented in tracking records plus every
  real git clone under the secondary-repos root, following #3584's `repoRoot`/`ownerRepo`
  pattern; (2) add a real eviction path for a secondary clone whose repo was removed from
  Settings > Repos (#3581's registry), gated on no live worktree still pointing at it, wired
  into BuildConsole's `BtnRemoveRepo_Click`.
- 2026-09-12T23:10:06Z ✅ DONE — Shipped both pieces of #3630's scope, commit `73728570d`
  (pushed to `origin/main`, verified ancestor via `verify-branch-merged.mjs`):
  1. `scripts/dev-server/worktree-lifecycle.mjs`: new `repoRootsForSweep(config)` (main repo
     + every recorded `repoRoot` + every real clone under the secondary-repos root);
     `sweepWorktrees` now iterates all of them, tagging each `git worktree list` entry with
     the repoRoot it came from and threading `wtRepoRoot` through
     `detectWorktreeWork`/`preserveWorktreeWork`/`removeWorktreeSafe` (new `repoRootHint`
     fallback param on both) so even a recordless secondary-clone worktree resolves to its
     own real repo, not `config.mainRepoRoot`. `repo-clone.mjs`'s `secondaryReposRoot` is now
     exported for this.
  2. `scripts/dev-server/evict-repo-clones.mjs` (new): reads the #3581 registry straight from
     BuildConsole's own `%AppData%\BuildConsole[-<instance>]\settings.json` (fails closed —
     `ok:false` — if it can't be read/parsed, never treats that as "nothing configured"),
     evicts any secondary clone whose owner/repo is no longer configured, unless a live
     worktree tracking record still points at it. Wired into
     `desktop/BuildConsole/Controls/SettingsTabView.xaml.cs`'s `BtnRemoveRepo_Click` via new
     `WorktreeCleanupService.EvictRemovedRepoCloneAsync()` (fire-and-forget, non-blocking).
  - New selftests, both passing: `worktree-sweep-multirepo.selftest.mjs` (dead-owner worktree
    against a real secondary clone found + reclaimed by the sweep, both WITH and WITHOUT a
    tracking record; secondary clone's own `git worktree list` correctly pruned) and
    `evict-repo-clones.selftest.mjs` (dry-run vs real, still-configured retained, removed
    retained while a live worktree points at it then reclaimed once that record clears,
    non-git directory left alone, fail-closed on unreadable registry).
  - Re-ran existing selftests to confirm no regression: `worktree-sweep`, `repo-clone`,
    `provision-worktree-multirepo`, `worktree-reprovision`, `worktree-isolation` — all pass.
  - `dotnet build BuildConsole.csproj -c Debug`: 0 warnings, 0 errors.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/3630-q2459`
    (73728570d) is an ancestor of `origin/main`.
  - No new findings filed — the two gaps #3630's own issue body identified are exactly what
    this closes; nothing further discovered mid-build.
