# #3444 — Dedicated 'Refresh Queue' button (local-only) + move other icons into a dropdown

- **Status:** ✅ DONE 2026-09-10T02:04:25Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 3240c0ea7 (bookend), e23118365 (implementation, rebased to cab414a0f on main)

## Log
- 2026-09-10T01:58:21Z ⏳ IN FLIGHT — Read issue #3444 body + BUILD comment. Located the real button row (`Controls/BuildQueuePanel.xaml` ~147-205: `BtnPauseQueue`, `BtnRefreshGitHubTiles`, `BtnPinQueue`) and the #3074 local-only refresh path (`RefreshAsync(includeGitHubWork: false)` in `BuildQueuePanel.xaml.cs`). Plan: add new always-visible `BtnRefreshQueueLocal` wired to `RefreshAsync(false)`, and move Pause/Refresh-GitHub/Pin into a single "⋯" overflow `ContextMenu` button, keeping `SyncPauseToggleVisual`'s glyph/color flip intact inside the menu item.
- 2026-09-10T02:04:25Z ✅ DONE — Added `BtnRefreshQueueLocal` (new, always-visible, primary header action) wired to `RefreshAsync(includeGitHubWork: false)` — zero GitHub calls (skips `AutoRecheckOpenIssuesOnTransitionAsync`/`TriggerBackgroundIssueTitleQueries`, both gated on `includeGitHubWork`). Moved `BtnPauseQueue` → `MiPauseQueue`, `BtnRefreshGitHubTiles`, and `BtnPinQueue` into a new `BtnQueueMenu` "⋯" overflow `ContextMenu` (opened on left-click via `BtnQueueMenu_Click`, since `Button.ContextMenu` only opens on right-click by default). All three moved actions keep their existing click handlers/behavior unchanged — `SyncPauseToggleVisual`'s glyph/color flip (`PauseQueueIcon`/`PauseQueueLabel`) now lives inside the `MiPauseQueue` MenuItem's `Header`, its one stray `BtnPauseQueue.ToolTip` reference fixed to `MiPauseQueue.ToolTip`. Verification: `dotnet build BuildConsole.csproj` succeeded with 0 warnings/0 errors (XAML markup compiler validates the new `ContextMenu`/`MenuItem` structure). `desktop/BuildConsole` is out of scope for test manifests per CLAUDE.md, so build success + code read-through is the real verification here — no live click-through was performed (no running BuildConsole instance touched, per the "never launch BuildConsole.exe to verify" rule). `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `cab414a0f` is an ancestor of `origin/main`.
