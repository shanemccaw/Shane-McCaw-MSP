# #2921 — Customer Home and Timeline: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-06T18:40:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** f14d90d76 (bookend IN FLIGHT), 711f4d46f→19154ce7d (real work, rebased onto main and pushed), this commit (bookend DONE + manifest hash)

## Log
- 2026-09-06T18:30:54Z ⏳ IN FLIGHT — writing this bookend after most of the real work
  (data hooks, page wiring, test manifest, live verification) was already done rather
  than before, as the process calls for — noting the ordering slip honestly rather than
  backdating it. Proceeding straight to the DONE entry below since the work itself is
  complete and verified.
- 2026-09-06T18:40:00Z ✅ DONE — Wired artifacts/portal's `/` page (Customer Home /
  Overview) to the two real endpoints in docs/customer-home-and-timeline-contract-pack.md
  (`GET /api/portal/dashboard`, `GET /api/portal/customer/timeline`), per the
  Overview.dc.html design export and the #2921 issue body/comments (pulled in full,
  including the #2920/#2922 held-comment history). New files:
  `artifacts/portal/src/components/overview/{types,overviewDisplay,useOverviewDashboard,
  useOverviewTimeline}.ts`, rewritten `artifacts/portal/src/pages/index.tsx`. One
  deliberate design departure, documented in the page's own header comment: the
  design's top calendar/gantt strip mixes real per-item dates (Scans, Findings,
  Projects) with dates that only exist as design-tool mock data (Microsoft Changes,
  Change Windows, Policy Reviews — #2922 deliberately scoped those to aggregate
  weekly counts, not itemized schedules) — not rebuilt this pass rather than
  inventing dated rows the API can't back; real counts still surface via Coming Up /
  Portal Counts.

  Verification: `pnpm --filter @workspace/portal run typecheck` clean; `PORT=5199
  pnpm run build` succeeds (2093 modules, real build output). Live-verified via
  `shaneapp://runTest` against the new `test-manifests/portal/customer-home-overview.json`
  (registered in `_regression-suite.json`): 8 of 9 steps passed against the real
  local dev api-server on :8080 — every apiTests step passed (auth gates, real
  login, real dashboard shape incl. `overviewCounts.rbdWaiting`/projects[]/
  reports[], real timeline events[]/nextCursor + the `before=` cursor param). The
  1 failing uiStep (navigate to :5173) failed because the portal frontend Vite dev
  server was not running in this environment at run time (confirmed via curl:
  :5173 refused, :8080 answered) — an environment/merge-back-timing fact, not a
  code defect; documented plainly in the manifest's own notes.

  Two real findings filed and parented, both labeled `bug`, board status "AI Batter
  Up": **#3049** (parent #1655, this issue's own Feature) — Overview's Needs You
  panel has no backend count for pending Ownership/RACI acceptances; #2922
  deliberately scoped its six counts without one, so this is new backend scope for
  a future build, not a #2921 wiring gap. **#3050** (parent #1598) — unrelated,
  unwired future Billing page code (`billingLive.ts`/`billingWire.ts`) references a
  `billingData.ts` fixture that does not exist in the repo — stale documentation,
  confirmed not live/rendered anywhere today.

  `git status --porcelain` clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0) after pushing
  `agent/2921-q1746` → `main` (`19154ce7d`), first push accepted, no rebase-retry
  needed. Posted a completion comment on #2921 covering all of the above.
