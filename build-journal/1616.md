# #1616 — Portal New Design: ShaneBot + Active Cards (contract extraction pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** fc3466355 (pack, rebased onto origin/main), 4de39919d (IN FLIGHT bookend)


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1616`, but #1616 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1616 is complete.
>
> #1616's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Read #1616 full body + the authoritative DECIDED comment
  (v1.1 paid card set = nine cards; `shanebot_public` OUT OF SCOPE), plus #1485 (epic),
  #1577 (per-module contract-pack method), #1578 (build sequence), #366 (Active Cards,
  closed/code-complete), #1097 (Engine Core, `bot_instances`), #361 (message format,
  reserved `{type:"card"}` block). Writing the step-3 contract pack for ShaneBot Active
  Cards, `shanebot_paid` ONLY, at `docs/shanebot-contract-pack.md`. For the four EXISTING
  cards (invoice/subscription/score/data-answer): extract `BotGrounding.cardData` builder
  contract in full, cited to file:line. For the five NEW cards (finding, remediation,
  microsoft-change, ticket, document): derive the contract from the owning route's real
  backing shape (portal-remediation-tracker.ts:108, portal-message-center.ts:130 + what
  #1530-#1533 landed, Zoho Desk escalation path, documentsTable + Document Viewer #347).
  Also extracting: the card_router path (parseRequestedCards/routeCards/stripCardTokens +
  allowedCardTypes gating), whether RoutedCard.cardType is genuinely open (string) vs
  closed (BotCardType) and exactly where it widens, whether a `{type:"card"}` block is ever
  actually PERSISTED (checked against the live table, not just the comment), the support-chat
  route contract + message content-block shape, and shanebot-persona.ts's portal surface +
  current card-selection instruction (the data-answer fallback-of-last-resort prompt gap).
  READ-ONLY: no product code, no schema changes, no UI, no drizzle-kit push. #1269's gate is
  not touched. Any real live endpoint or card type with no portal-v2 renderer gets a sub-issue
  filed under #1616 at pack time, not left as a note.
- 2026-08-29 ✅ DONE — fc3466355 (rebased onto origin/main, verify-branch-merged.mjs
  confirms it is a real ancestor). Wrote docs/shanebot-contract-pack.md: full extraction
  of the four EXISTING card contracts (InvoiceCardData/SubscriptionCardData/ScoreCardData/
  DataAnswerCardData) cited to shanebot-engine.ts:139-173 and their builders at :676-732,
  including the undefined-when-no-data rule (each key omitted; data-answer is the exception
  — always built) and what the caller does with an undefined key (resolveCardData returns
  null → no card, support-chat.ts:194-205/624-631). Extracted the card_router path
  end-to-end (parseRequestedCards/routeCards/stripCardTokens, shanebot-engine.ts:950-1009)
  and confirmed RoutedCard.cardType is genuinely open `string` (widens at :971/:987, never
  narrowed back to BotCardType). For the five NEW cards, derived the contract from real
  backing: remediation→WireTrackerStep (portal-remediation-tracker.ts:108, body copy is
  fixture per #1489), finding→OPEN GAP (no Wire* exists; diagnostics-findings vs guide),
  microsoft-change→WirePost+WireAnalysis (routes/portal-message-center.ts:135/:169, with
  #1532 interpretation + #1533 resolution join at :306-357), ticket→Zoho escalation payload
  (zoho-desk.ts:690-699; no ticket READ endpoint = OPEN GAP), document→corrected backing to
  insightsGeneratedDocumentsTable (portal-documents.ts:54-117), NOT documentsTable as the
  DECIDED comment stated (that table is project-attached uploads with no viewer). KEY
  FINDING: a {type:"card"} block is NEVER persisted in any live table — support-chat.ts does
  not call upsertBotConversation (stateless; public-chat.ts:199-200 confirms it "will target
  [persistence] whenever it grows"), and the only writer, public-chat.ts, emits no cards.
  So cards are transient per turn; a returning customer sees none scrolling back. Recorded
  the data-answer fallback-of-last-resort as a PROMPT gap (shanebot-persona.ts says nothing
  about cards; support-chat.ts:269-281 cardsBlock gives data-answer the broadest mandate with
  no specific-card-first ordering). CURRENT vs DECIDED table: every DECIDED row cited to its
  issue; finding + ticket flagged as OPEN GAPs. Filed sub-issue #1622 (linked under #1616)
  for the four existing card types having no portal-v2 renderer. Read-only: no product code,
  schema, or UI touched; #1269's gate not crossed.
