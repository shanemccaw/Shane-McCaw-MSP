# #4026 — Retainer period-close lock not honoured by AdminV2 retainer routes / new post-close adjustment path

- **Status:** ✅ DONE 2026-09-14T13:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** eebd54c1ac53d93a057caa7afcb06ce163f65691

## Log
- 2026-09-14T13:35:00Z ⏳ IN FLIGHT — investigated msp-retainer.ts's close-lock mechanism (advisory lock + retainer_period_closes check) and admin-retainer.ts's three unguarded writers. Plan: extract shared lock/close-check helpers into a new lib file, wire them into admin-retainer.ts's unscoped/PATCH/DELETE; add a new "adjust after close" path on the MSP Console requiring a reason, backed by a new retainer_adjustment_notes table, surfaced to the customer via portal-retainer.ts. Byproduct hook (retainer-work-logger.ts) explicitly out of scope per dispatch.
- 2026-09-14T13:45:00Z ✅ DONE — eebd54c1. Shipped:
  1. `artifacts/api-server/src/lib/retainer-ledger-lock.ts` (new) — the one shared advisory-lock + `retainer_period_closes` check (`withLedgerLock`, `assertPeriodOpen`, `sendLedgerError`). Both `msp-retainer.ts` and `admin-retainer.ts` now import it instead of msp-retainer.ts carrying its own private copy.
  2. `admin-retainer.ts` — `POST .../unscoped`, `PATCH .../entry/:id`, `DELETE .../entry/:id` all now run under the lock and 409 while the entry's period is closed. No override, no bypass flag (Shane's 2026-09-14 decision).
  3. New MSP Console route `POST /msp/:mspId/customers/:customerId/retainer/periods/:periodKey/adjustments` (`ladder.msp-admin`) — the one deliberate override. Requires a non-empty `reason` (400 otherwise), supports create/update/delete against an already-CLOSED period only (400 if the period isn't closed), writes a real `retainer_adjustment_notes` row plus an `msp_audit_logs` `RETAINER_PERIOD_ADJUSTED_AFTER_CLOSE` entry.
  4. New table `retainer_adjustment_notes` — Drizzle schema in `lib/db/src/schema/msp.ts`, migration `lib/db/migrations/manual/2026-09-14-retainer-adjustment-notes-4026.sql`, **run myself against local `DATABASE_URL`** (CREATE TABLE + 2 indexes + self-mark INSERT, confirmed via psql output).
  5. Customer-visible wiring: `GET /api/portal/retainer` now returns `adjustmentNotes`; `artifacts/portal/src/pages/my-architect.tsx` renders a "Revisions to closed periods" card (only when non-empty) showing item, action, reason, before→after hours, date. `admin-retainer.ts` carries the shared `adjustmentNoteToWire` mapper (no actor id, matching `entryToWire`'s own convention) so msp-retainer.ts and portal-retainer.ts render one shape.
  6. Tests: extended `admin-retainer.test.ts` (added `db.transaction`/tx chains + `retainerPeriodClosesTable` + `sql` to its `@workspace/db`/`drizzle-orm` mocks — the close-lock check needed them), extended `portal-retainer.test.ts` (added `retainerAdjustmentNotesTable` mock + queue slot to all 7 existing cases), and added 8 new live-Postgres cases to `msp-retainer.live-db.test.ts`: reason/capability validation, refuses-when-not-closed, create/update/delete against a closed period, the note surfacing through BOTH the operator GET and a real `portal-retainer.ts` request, the audit rows, and — the one that actually proves "the same lock" rather than just similar code — a cross-router case that closes a period via msp-retainer.ts then hits `admin-retainer.ts`'s real router (behind its real requireAuth/requireAdmin, real JWT) and gets 409, while a write into a still-open period from the same router succeeds (200).
  7. `test-manifests/portal/my-architect.json` extended with A6 (`adjustmentNotes` is a real array), tagged `my-architect-4026`, registered manifest unchanged (already in `_regression-suite.json`).
  - Verification: `pnpm run typecheck` clean on `artifacts/api-server` and `artifacts/portal`. `npx vitest run` on all 5 retainer-related suites (admin-retainer, portal-retainer, portal-retainer-billing ×2, msp-retainer.live-db) — 42/42 passing, live-db suite run against real local Postgres with `DATABASE_URL` exported.
  - Honest gap: the new manifest's A6 step was written and tagged but **not executed** this session — `shaneapp://runTest` is not a reachable tool from this chat session (confirmed via ToolSearch: no such tool registered; this is not a BuildConsole-launched interactive runner context). All other verification above is real and was actually run.
  - Out of scope, unchanged: `retainer-work-logger.ts`'s `logRetainerWorkFromTracker` byproduct hook, per the dispatch.
  - No unrelated dirty files found in the worktree at any point in this session.
