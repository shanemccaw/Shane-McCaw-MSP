# #3461 — Feature: Credential Vault (MyArchitect)

- **Status:** ⏳ IN FLIGHT 2026-09-10T17:59:49Z  <!-- resumed: blocker #3493 (UI Shell Redesign) landed & closed 2026-09-10T14:25:43Z; stale blocked_by edge removed -->
- **Scope:** buildconsole (MyArchitect WPF desktop app)
- **Started:** 2026-09-10
- **Commit(s):** (bookend first; feature-work hash added at DONE)

## Log
- 2026-09-10T00:00:00Z 🛑 BLOCKED (prior session) — held on real `blocked_by` #3461 → #3493 (UI Shell Redesign); no code landed.
- 2026-09-10T17:59:49Z ⏳ IN FLIGHT (resumed) — Found myself unblocked: #3493 landed (commit e8460c5fd) and is **closed/`complete`** as of 2026-09-10T14:25:43Z. Per CLAUDE.md "Picking this back up later": removed the now-stale `blocked_by` edge #3461 → #3493, swapped `blocked` → `in-flight` label. `blocked_by` count now 0.
  Building the real remaining scope on this Feature's own checklist (no filed GitHub sub-issues exist — `sub_issues` API empty):
  1. `credential_vault_entries` data model (tenantId FK, label, DPAPI-encrypted blob) — **local-only**, never the Postgres platform DB (spec: "Never syncs to any backend").
  2. DPAPI encryption/decryption — via P/Invoke to `crypt32.dll` (`CryptProtectData`/`CryptUnprotectData`, `CurrentUser` scope = tied to Windows login), zero new NuGet download (Git #1987 bandwidth constraint). Master-password layer via in-box `AesGcm` (PBKDF2-derived key).
  3. Master unlock flow — session-gated: derived key held in memory only while unlocked; `Lock()` zeroes it; create-on-first-run vs enter-to-unlock.
  4. Vault UI — Admin ribbon tab (UI_RULES.md §2 lists "Admin | Vault (#3461)…"), rendered as a center document (`VaultPanelView`, same pattern as ConsolePanel), per-tenant entry list + add/edit/reveal/copy/delete + lock. Plaintext secrets are kept strictly inside `VaultService`/`VaultPanelView` and are deliberately **not** routed through the shared `RecordWorkspaceSpec` shell contract, honoring the spec's hard isolation boundary ("never exposed to Claude Chat or any other module").
