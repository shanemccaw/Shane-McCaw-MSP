# #3461 — Feature: Credential Vault (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T00:00:00Z  <!-- blocked on #3493 (UI Shell Redesign); edge is real and current -->
- **Scope:** buildconsole (MyArchitect WPF desktop app)
- **Started:** 2026-09-10
- **Commit(s):** none — no feature code landed (correctly held on the block)

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — Pulled #3461 body + all comments. Feature = per-tenant, local-only, DPAPI-encrypted credential vault for MyArchitect, session-gated by a master unlock. Deliverable surface is the **Vault entry in the Admin ribbon tab** per `desktop/MyArchitect/UI_RULES.md` §2.
- 2026-09-10T00:00:00Z 🛑 BLOCKED — Confirmed a real, current `blocked_by` edge #3461 → #3493 (Feature: UI Shell Redesign). Verified #3493 is genuinely **not built**: `MyArchitect.csproj` has no `Fluent.Ribbon` reference; `MainWindow.xaml` still uses the old VS-Code "Activity Bar" chrome that UI_RULES.md §8 explicitly removes; no 6-tab Ribbon / Admin tab / left-right flyout split exists; and `build-journal/3493.md` does not exist (its bookend never even started, despite the stale `in-flight` label). `GEMINI.md` standing rule is unambiguous: "#3493 must land before any other Feature's UI work… do not build ad-hoc chrome to unblock yourself — stop and flag it instead." The Vault's whole deliverable is the Admin-tab surface (entry list, add/edit/reveal, master-unlock prompt), so there is no non-UI slice worth building blind against a shell whose contract (full-panel record workspace, confirm-armed actions per UI_RULES.md §3) does not exist yet. Held the block per CLAUDE.md ("the edge is what actually gates the queue"; "still blocked → say so and stop rather than guessing forward"). No feature code written, working tree clean. Re-dispatch once #3493 lands.
