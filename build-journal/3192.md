# #3192 — Feature: Dates -- Round 2 visual rebuild (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T17:46:28Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 3d88c9d28 (rebased/pushed to main; original commit c7dd864e0)

## Log
- 2026-09-08T17:35:02Z ⏳ IN FLIGHT — Investigation done: confirmed no
  `Shanes Life NN - Dates.dc.html` export exists in
  `web/shanes-life/Design/design_handoff_shanes_life/`; #3183's own no-forms audit
  already removed the bare "add a question" input on date detail (dateId/asks) and
  the Dates list view never had a form. Confirmed sibling issue #3191 (Meds Round 2)
  is still open too, so "Meds" in this issue's own text refers to the Today tray's
  already-shipped `.meds-pill` preview, not a rebuilt Meds room page. Real, reusable
  Round 2 building blocks already exist and are proven on Today (#3144/#3174):
  `sticker()`, `pillButton()`, `.btn-pill`, `.next-card-v3`/pebble blob radius, and
  Shopping's (#3178) native `.shop-header` back-link pattern. README's own "Rooms
  (sub pages) — the same skin" section (line 179) gives Dates' real tint
  (`244,114,182`) and the generic top-glow + house-back-link spec every other room
  reuses. Plan: apply that generic room-header pattern (new, reusable `roomHeader()`
  helper + `.room-*` CSS, scoped to Dates for this build) + 22px cards/tiles + pill
  buttons + sticker badge, scoped to a `.dates-room` wrapper so it doesn't reflow the
  rest of the still-unconverted rooms (#3190/#3191/#3193-3195 each get their own
  pass).
- 2026-09-08T17:46:28Z ✅ DONE — Shipped the Round 2 visual rebuild for Dates (list + detail):
  new generic `roomHeader()` + `.room-scene`/`.room-glow`/`.room-header*` CSS (top tint glow +
  house back-link, per README "Rooms (sub pages)"), scoped `.dates-room` wrapper giving 22px
  cards/tiles, 999px pill buttons, and blob-pebble date tiles; `dateRow`'s "New category" badge
  now uses the real `sticker()` component; renamed `SHOP_HOUSE_ICON` -> `ROOM_HOUSE_ICON` since
  it's the generic README back-link icon, not Shopping-specific (fixed the resulting break in
  Recipes' concurrent #3190 build during rebase -- it also referenced the old name); `dates`/
  `date` added to `render()`'s `hasOwnHeader` so the generic app-header hides for both, closing
  #3192's own point 2. Also removed a real, dedicated "How did it go?" visit-notes form on
  date-detail that #3183's own no-forms pass missed -- `attach_visit` already exists over MCP
  for exactly that (contract pack Section 8).
  **Real verification performed**: `node --check` on app.js (syntax); a real headless-Edge
  render of a static fixture built from the actual app.css + the real markup shape, confirming
  visually (screenshots taken, not assumed) that (a) 22px cards, blob date-tile, and pill
  buttons render correctly, and (b) a real CSS stacking bug this build introduced and then
  caught via that render -- a naive `z-index:0` glow painted over the room's own cards, and
  `z-index:-1` made it invisible behind `<body>`'s own background -- is fixed by promoting
  `.dates-room` to `position:relative` so it wins the document-order paint tie. Scratch fixture
  and screenshots deleted before finishing; not committed. No live-server/passkey-auth
  end-to-end check was performed (`bin/check.mjs` requires a running server + real WebAuthn
  authenticator, and only exercises the server/DB half, not app.js/app.css) -- not needed for a
  pure client-side visual change with no schema/API/server surface touched.
  Real conflict during push: #3190 (Recipes Round 2) and this build both touched
  `STICKER_TONE`/`hasOwnHeader`/the `SHOP_HOUSE_ICON` region concurrently; rebased onto
  `origin/main`, resolved by keeping both sides' additions, and found + fixed the
  `SHOP_HOUSE_ICON` reference Recipes still had (would have thrown `ReferenceError` at
  runtime otherwise). Real branch-merged check: `verify-branch-merged.mjs` confirms
  `agent/3192-q1973` (3d88c9d2) is an ancestor of `origin/main`. `git status --porcelain`
  clean of anything this session touched.
  **Findings filed**: none new. (#3191 Meds Round 2 being still-open, contradicting this
  issue's own "already proven out in Today/Meds" language, is a pre-existing inconsistency in
  #3192's own issue text, not a new defect worth its own issue -- noted here for the record.)
