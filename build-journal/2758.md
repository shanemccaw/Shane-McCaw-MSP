# #2758 — Portal: Consent and Onboarding -- contract pack extraction (Phase 2)

- **Status:** ✅ DONE 2026-09-04T05:20:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 3fcde9816 (bookend IN FLIGHT), 3933f9c0f / 681ed94f9 (contract pack,
  after rebase onto origin/main)

## Log
- 2026-09-04T04:44:39Z ⏳ IN FLIGHT — read all 5 archived pages (consent-success,
  consent-declined, consent-tenant-conflict, initiate-onboarding,
  portal-identity-interstitial) plus their real backing routes
  (consent.ts, portal-onboarding.ts, portal-checkout-free.ts, msp-onboarding.ts,
  public-services.ts); about to write docs/consent-and-onboarding-contract-pack.md.
- 2026-09-04T05:20:00Z ✅ DONE — wrote `docs/consent-and-onboarding-contract-pack.md`
  (read-only build, no product code/schema/UI touched). Documents the real wire
  contracts for `GET /api/consent/callback` (the shared backing route for all 3
  outcome pages: success/declined/tenant-conflict), `POST
  /api/portal/onboarding/contract`, `POST /api/portal/checkout/free`, `GET
  /api/public/checkout-session/:id`, `GET /api/services`, `POST
  /api/msp/onboarding/generate-link` plus its 4 sibling routes, and confirms
  `portal-identity-interstitial.tsx` calls no dedicated endpoint of its own (pure
  JWT-role branch). 4 findings recorded in §9, none filed as new bugs — all are
  low-severity staleness/UX-gap notes for Design's awareness (a dead
  archived-page branch that's unreachable from the live popup-consent flow, a
  hardcoded permissions list with a real live source it should read from
  instead, a 409 error path with no distinct UI treatment, and a dead
  `/portal-v2` navigation target left over from the portal-v2 retirement) — same
  disposition the sibling offers-and-sow-acceptance pack used for its own
  comparable low-severity findings. Orphaned-endpoint check: zero callers in
  `artifacts/portal` (no `.dc.html` exists yet, matching #1650's own rule); one
  live caller overall, `AssessmentFlow.tsx` on the marketing site, and only for
  the popup-origin half of the consent flow — the 5 archived "portal"-redirect
  pages this pack documents have no current live caller. Branch verified merged
  into origin/main via `verify-branch-merged.mjs` (exit 0); `git status
  --porcelain` clean.
