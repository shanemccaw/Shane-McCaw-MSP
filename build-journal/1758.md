# #1758 — Fix stale registry-source-key-contract.test.ts assertion

- **Status:** ⏳ IN FLIGHT 2026-09-04T07:51:42Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T07:51:42Z ⏳ IN FLIGHT — ran vitest, confirmed the real failure: "grounds no
  document row in the four phantom adoption stats" asserted `adoption.teamsActive`,
  `.sharePointActive`, `.oneDriveActive`, `.emailActive` must NOT appear in `ALL_SPECS`.
  That was true under #441's original phantom `usage:*` shape, but #1105 later shipped
  real `adoption.*` specs backed by genuine `usage.*Count` metrics — the test was still
  checking against the old #441 absence-shape instead of #1105's real presence-shape.
  Also caught a stale id: the real spec is `adoption.exchangeActive`, not
  `adoption.emailActive` (never existed under either name).
- 2026-09-04T07:51:42Z ✅ DONE — rewrote the case to assert the four real #1105 adoption
  stats each resolve, end to end, through `getMetric` + `classifySourceKey` to a real,
  non-sentinel check — same rigor the rest of the file already applies, now pointed at
  the actual current shape. `vitest run src/lib/registry-source-key-contract.test.ts`:
  7 passed (7), 0 failed.
