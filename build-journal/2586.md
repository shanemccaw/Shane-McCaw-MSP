# #2586 — Remediation Tracking: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-04T06:37:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 9d81a090c (contract pack), plus this bookend's own commit

## Log
- 2026-09-04T06:09:00Z ⏳ IN FLIGHT — pulled #2586's body + comment (real backend confirmed by
  #2670: 7 new MSP-side routes under `/api/msp/customers/:customerId/remediation*`, mirroring the
  real customer-facing `/api/portal/remediation*` contract already documented in
  `docs/remediation-tracking-contract-pack.md`). Following the naming convention already
  established for this project's other MSP-console packs (`docs/risk-register-msp-console-contract-pack.md`,
  `docs/data-rights-and-privacy-msp-console-contract-pack.md`, etc.) — writing a **new**,
  separate `docs/remediation-tracking-msp-console-contract-pack.md` rather than editing the
  customer-facing pack in place, since the two are genuinely different audiences/wire contracts.
  Reading the 7 real route files (`msp-remediation-tracker.ts`, `-tracker-scores.ts`,
  `-tracker-export.ts`, `-checklist.ts`, `-fix-routes.ts`, `-reveal.ts`,
  `-bypass-resolutions.ts`) in full next, cross-referencing against the shared `lib/remediation-*.ts`
  business logic and the portal pack's own §1/§3 enum + wire tables, verifying live against local
  PostgreSQL.
- 2026-09-04T06:35:00Z ✅ DONE — read all 7 MSP route files side-by-side with their 7 portal
  counterparts in full (1,396 + ~1,396 lines), plus `remediation-checklist.ts`,
  `remediation-fix-route.ts`, `remediation-reveal-gate.ts`, `remediation-bypass-resolutions.ts`,
  `remediation-tracker-catalogue.ts`, `retainer-work-logger.ts`, and the relevant
  `lib/db/src/schema/msp.ts` enum/table sections. Verified `remediation_tracker_steps` live
  against local PostgreSQL (`\d remediation_tracker_steps`) — matches the Drizzle source exactly.
  Confirmed `artifacts/msp-console` exists (#1680, closed) but has zero remediation consumers
  today — all 14 endpoints (7 MSP + their 7 portal counterparts already existed) are correctly
  unconsumed/staged, no orphaned-endpoint issue needed.

  Wrote **`docs/remediation-tracking-msp-console-contract-pack.md`** (new file, ~350 lines) per
  the #1642 pattern: per-surface wire contract for all 14 MSP routes, the real 7-value
  `REMEDIATION_TRACKER_STEP_STATUS` enum + `REMEDIATION_FIX_ROUTE` enum, cross-surface edges
  (checklist→CR, fix-route→reveal gate, verified-step→drift-event bypass correlation), the one
  real behavioral difference (`accepted_risk` narrowing on the MSP tracker PUT vs. no
  decline-to-risk route at all on the MSP side), why the retainer-work-logger's role gate is
  structurally redundant (not omitted) on MSP-only routes, and the forbidden list.

  **Two real findings made and filed, both parented under Feature #1684** (per this build's
  standing rules) and set to project board status "AI Batter Up":
  - **#2827** (`bug` + `security`) — `PUT .../remediation/checklist/:checkKey` accepts
    `status: "accepted_risk"` directly with no signed decline-to-risk flow behind it, on BOTH
    the portal (`portal-remediation-checklist.ts:87-198`) and MSP
    (`msp-remediation-checklist.ts:74-174`) routes — unlike the sibling s1–s30 tracker route,
    which correctly rejects that value on both sides. A pre-existing gap in the portal route
    (#1538), inherited faithfully by the new MSP mirror rather than introduced by it.
  - **#2828** (`bug`) — `docs/remediation-tracking-contract-pack.md` (the customer-portal pack,
    written 2026-08-29) is badly stale: 8 of its 9 §2 "DECIDED, not built" rows (#1538–#1545)
    are now closed and built, including a 7th enum value (`accepted_risk`) and an entire enum
    (`REMEDIATION_FIX_ROUTE`) its own §3 never mentions. Recommends a full wholesale
    regeneration, same #1642 method.

  Not filed, only flagged in the pack itself (§7.2/§7.3, below the "flag, don't file" threshold
  already used by `docs/risk-register-msp-console-contract-pack.md`): a missing
  `REMEDIATION_TRACKER_STATUS_LABELS.accepted_risk` display-label entry (cosmetic export-only
  gap), and a small duplicated `MSP_STAFF_ROLES` constant across the two export route files.

  No product code, schema, or UI changed — read-only build, as required.
