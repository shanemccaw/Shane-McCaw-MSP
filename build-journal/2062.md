# #2062 — Blocked build cards: "waiting on #N" needs a real ghost/placeholder card

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 0b8804ee1 (pushed directly to main)

## Log
- 2026-08-31 ⏳ IN FLIGHT — auditing current blocked-card rendering + blocked_by data
  source in BuildConsole's Build Queue panel before writing the ghost-card component.
  Spec is the 2026-08-30 refinement comment on #2062: a real ghost/placeholder card
  (not plain text), always present in the DOM as an anchor for #2030's future
  click-to-highlight chain system (not building #2030's scope here).
- 2026-08-31 ✅ DONE — replaced the plain "waiting on #N" TextBlock in
  `BuildQueuePanel.xaml.cs`'s `BuildQueueCard` (the real per-card renderer, not the
  QueueGraphCanvas mini-map dots) with `BuildBlockerGhostCard(int blockerNumber)`:
  a dimmed, non-interactive-except-jump ghost card built from real data — the live
  `QueueGraphNode` for that blocker number when it's itself a build in this same
  queue render (real title + status via `GhostStatusLabel`, same vocabulary
  `CreateGraphNodeDot` already uses), else the blocker's real GitHub title via the
  existing `_issueTitleCache` gh-CLI background-fetch machinery (extended
  `TriggerBackgroundIssueTitleQueries` to also warm declared-blocker numbers, not
  just `AssociatedIssueNumbers`) with an "OPEN" state (LiveBlockedBy already
  filtered to blockers `_openIssues` reports genuinely open). Never invents a
  title — shows the bare issue ref + "(fetching title…)" until the real one
  lands, then redraws immediately rather than waiting for the next 15s poll.
  Clickable to jump to the blocker's own card when it has one (`SelectNode`);
  carries a typed `BlockerGhostTag(IssueNumber, IsLiveQueueNode)` on `Tag` as the
  DOM anchor for #2030's separate, not-yet-built click-to-highlight chain system
  (out of scope here per the prompt's scoping note).
  Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded,
  0 Warning(s), 0 Error(s). desktop/BuildConsole is out of scope for test
  manifests per CLAUDE.md, so a clean build is the verification bar here.
  Only `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` changed (`git diff
  --stat` confirmed). Committed as 0b8804ee1 on `agent/2062-q1053`, pushed
  directly to `origin/main` (9dbe7b269..0b8804ee1), confirmed merged via
  `verify-branch-merged.mjs`. Working tree left clean.
