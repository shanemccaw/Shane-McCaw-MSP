# #1442 — Health (top-level pillar page): strict pass — eliminate all remaining fake data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 8e94217bc, 7f84b9bdb, c8dfc0702 (merged to main; verify-branch-merged.mjs confirmed `main` HEAD == `c8dfc0702`)

## Log
- 2026-08-27 ⏳ IN FLIGHT — full strict pass on `portal-v2-health.tsx`: wiring the
  4 newly-available object-inventory checks (#1260), converting debt items to real
  per-tenant findings (same #1255 pattern as compliance-gaps), and tagging every
  remaining genuine backend gap (config-drift table, directory-sync stat, debt
  trend history, accepted-risk register) with `NO-BACKEND-TO-WIRE:`.
- 2026-08-28 ✅ DONE — Shipped: (1) `useHltObjectsLive.ts` + `hltObjectsWithLive`
  widened from 3 to 7 of 9 real stale-object-inventory rows, using 4 real
  dashboard-registry metric keys #1260's own migration had added but nothing had
  wired (`intune.unassignedProfileCount`, `governance.emptySecurityGroupCount`,
  `governance.dormantServicePrincipalCount`, `identity.disabledAccountCount`).
  (2) Debt items (`HLT_FINDINGS`) now read the tenant's real per-tenant Health
  findings via a new `hltFindingRowFromLive` mapper — the same #1255 pattern
  `portal-v2-compliance-gaps.tsx` already proved — with a real empty state and an
  honest "No scan data available" state for a never-scanned tenant. (3) Every
  remaining genuine backend gap tagged with the literal `NO-BACKEND-TO-WIRE:`
  comment and rendered as an honest "No live data available" state instead of
  fixture: the config-drift table + its hero stat (investigated — a real
  `drift_events` engine exists and is portal-reachable for aggregate counts, but
  has no shape matching this table's "N of 47 tracked settings + clean/accepted
  rows" inventory), the debt-trend history graph (no per-scan time series of
  "open debt item count" exists anywhere), the "Directory sync" hero stat (Entra
  Connect/hybrid data lives on-premises via PowerShell, no check exists), the
  accepted-risk register (no persistence anywhere in the platform — confirmed by
  reading `AcceptRiskPanel`'s no-op `onConfirm` and the fully-fixture standalone
  Risk Register page), the incident/advisory row kinds on the service-health
  panel, and the 2 of 9 stale-object rows with no real check (App registrations
  with no owner, Credentials expiring in 30 days). (4) Updated
  `test-manifests/portal/portal-v2-pillars.json`'s Health section (sessionTag
  `hlt-1442`) to match — retired H10-H11d/H14-H23 (drift-expand and
  debt-item-FixPanel click-throughs keyed to fixture content that no longer
  renders), added new honest either/or assertions for the wired rows and the
  no-backend states, stated plainly in the manifest's own notes.
  **Verification, honestly reported:** the branch was rebased onto and pushed
  directly to `main` (per this repo's git convention) and `verify-branch-merged.mjs`
  confirms it is a genuine ancestor of `origin/main` — not just locally clean.
  `shaneapp://runTest` was invoked for real (onlyTag=hlt-1442) but failed at the
  manifest's own login prerequisite step (`#email` never rendered) — this
  session independently found the shared dev environment's `node_modules` for
  `msp-portal` was missing core packages (react, wouter) in BOTH this worktree
  AND the main checkout at the start of the session (pre-existing, not caused by
  my edits), and mid-session a concurrent install was observed populating the
  shared store — including, after the fact, a symlink resolving into an
  unrelated worktree (`1446-q728`), evidence another concurrent session's own
  `pnpm install` was racing the same shared checkout at the same time. `tsc
  --noEmit` could not be run to a clean result for the same reason. Verification
  instead rests on: a full manual re-read of the complete diff for syntax/JSX
  balance and import correctness, and the real (if partial) `runTest` envelope
  above proving the trigger and pipeline themselves work end-to-end. Flagged
  plainly in the GitHub issue comment — this is a real gap, not glossed over.
