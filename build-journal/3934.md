# #3934 — Retire portal-v2 security-plan page (Group A / #1647)

- **Status:** ✅ DONE 2026-09-13T04:25:00Z
- **Scope:** platform

## Log
- 2026-09-13T04:20:00Z — Investigated. `artifacts/msp-portal` (portal-v2) no longer
  exists at all: it was deleted wholesale in `f40438cdc` ("Portal scaffolding: create
  artifacts/portal, retire artifacts/msp-portal", 2026-08-29), which included
  `artifacts/msp-portal/src/pages/portal-v2-security-plan.tsx` (715 lines removed)
  along with the rest of the tree. `grep -rn 'portal-v2-security-plan'
  artifacts/msp-portal/src/` → no such directory / zero hits (confirmed via
  `find . -iname '*portal-v2-security-plan*'` across the whole repo too — only a
  same-named migration file `lib/db/migrations/manual/2026-08-21-portal-v2-security-plan.sql`
  remains, which is unrelated DB history, not the page).
- Confirmed the real replacement is live and wired: `artifacts/portal/src/pages/security-plan.tsx`
  imports real components (`DriftPanel`, `ModuleSchedule`, `SignVersionDialog`) and
  `@/lib/security-plan-types` / `@/lib/security-plan-api` — no `*Data.ts` fixture
  import anywhere in the file. It's registered in `artifacts/portal/src/App.tsx` and
  `artifacts/portal/src/components/shell/moduleNav.ts` (real route + nav entry, not
  portal-v2 remnants).
- No further code change needed — this issue's specific deliverable was already
  fully retired as a side effect of the prior wholesale msp-portal removal. Nothing
  to delete, nothing to build.
- **Status:** ✅ DONE 2026-09-13T04:25:00Z — no code changed (verification-only).
