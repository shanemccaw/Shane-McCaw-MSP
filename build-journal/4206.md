# #4206 — Add missing chip/badge wash brushes to Colors.xaml/DarkTheme.xaml + wire ThemeEditorWindow.xaml

- **Status:** ✅ DONE 2026-09-15T03:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 76023f49 (pushed to main; originally 693483eb6ff810713c12610785e4967103c6ca8c on agent/4206-q2882, rebased onto current origin/main)

## Log
- 2026-09-15T03:05:00Z ⏳ IN FLIGHT — investigating Themes/Colors.xaml + DarkTheme.xaml wash brush conventions and measuring base hues before adding the 5 missing brushes.
- 2026-09-15T03:35:00Z ✅ DONE — Added `YellowWashMediumBrush` (`#33F9E2AF`), `RedWashMediumBrush` (`#33F38BA8`), `MauveWashMediumBrush` (`#33CBA6F7`) to `Themes/DarkTheme.xaml` (not Colors.xaml — Colors.xaml is merged before DarkTheme.xaml in App.xaml and can't resolve Yellow/Red/MauveBrush yet), following the existing 20%-tier "Medium" wash convention (`#33` alpha) already used by Colors.xaml's Accent/Success/Error/Warning washes. Measured the target literals against the base hues per the issue's instruction: the "⚠ Bright"/"⚠ Low CR"/"⚠ Outlier" chips and the "EPIC #803" badge each pair their background with a Foreground of YellowBrush/RedBrush/MauveBrush respectively, confirming these (not the semantic Status* colors) are the correct base family — the raw literals were not exact 20%-of-base alpha composites (checked via HSL/RGB blend math against AppBackground/Panel/CardBackground; e.g. the Red chip's hue was pure 0° vs RedBrush's actual ~343°), consistent with #4200/#4201's finding that this file's raw literals weren't reliably on-tier. For the WCAG "PASS AAA" badge (`#234A2E`), reused the existing `StatusSuccessWashMediumBrush` rather than inventing a near-duplicate (it already pairs with `StatusSuccessBrush` foreground at that exact call site). For the "EPIC #803" badge (`#2B2745`), reused the new `MauveWashMediumBrush` rather than adding a 6th brush — it pairs with `MauveBrush` foreground, mirroring the "ISSUE #2126" badge's existing `AccentWashMediumBrush`+`BlueBrush` pattern two rows below it. Wired all 5 raw hex literals in `ThemeEditorWindow.xaml` to these resources. `dotnet build BuildConsole.csproj` succeeded, 0 warnings/0 errors. Branch rebased onto current `origin/main` and pushed directly to `main` (76023f49); `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean before this bookend edit.
