# Git #1423 — Secondary account toggle bright purple/magenta regression

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Commits:** `ecd1b9002` (IN FLIGHT bookend), `0e8d3bd5b` (real work)

## Log
- ⏳ IN FLIGHT — investigating `TopAccountToggleBorder` styling in `MainWindow.xaml.cs` (Git #1419's real landed toggle) against the established dark/muted-COOL-accent-only color constraint (ae7316606, Shane's own words: "Dark color. Bright hurts my eyes. Pinks are bad.").
- ✅ DONE — Confirmed the regression: `RefreshTopAccountToggleUi()` painted the secondary state as a solid full-brightness `MauveBrush` fill (`#CBA6F7`), the same harsh-color class already fixed once in `ae7316606` ("Subtle dark ambient polish"), which moved Focus/Immersive off pink/mauve onto the palette's muted COOL accent (`BlueBrush` `#89B4FA`) as a dark surface with a blue border/text accent, never a solid bright fill. Applied the same treatment here: secondary state now uses `Surface1Brush` background with `BlueBrush` border/text, instead of a solid Mauve fill. Primary state's existing neutral styling (`Surface0Brush`/`Surface1Brush`/`TextBrush`) is unchanged. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` → 0 errors, same 46 pre-existing warnings baseline, none introduced. No test manifest — scope is `desktop/BuildConsole`, outside the msp-portal/marketing-site test-coverage rule. No DB/schema change.
