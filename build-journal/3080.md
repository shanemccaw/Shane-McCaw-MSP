# #3080 — POA&Ms: real schema, core CRUD, milestones, SOW tie-in (Phase 1a)

- **Status:** ✅ DONE 2026-09-07T16:51:40Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** fe1403ef9 (schema), b09e2e138 (MSP-console + Portal CRUD routes),
  b1a40ba29 (retention purger registration, addressing #3093)

## Log
- 2026-09-07T16:30:59Z ⏳ IN FLIGHT — investigated #3080/#1935/#1487/#1491/#1510, audited repo
  for existing POA&M references (none real — only test fixtures and the #1942 alert-catalog's
  own PENDING_DETECTOR rows), designed `msp_poams`/`msp_poam_milestones` mirroring
  `msp_risk_decisions`'s pattern, wrote and ran
  `lib/db/migrations/manual/2026-09-07-poams-schema-3080.sql` against local Postgres. About to
  build MSP-console + Portal CRUD routes.
- 2026-09-07T16:51:40Z ✅ DONE — built `msp_poams` + `msp_poam_milestones` (schema mirrors
  `msp_risk_decisions`'s pattern: unFK'd `checkKey`/`additionalCheckKeys`, #1511-shaped
  role-based accountability reusing `risk-authority.ts` verbatim, `signedAt`/`signedBy`/
  `signedStatement` mirroring RBD's acceptance shape, `scheduledCompletionDate` vs
  `originalScheduledCompletionDate` preserved separately, nullable `sowId` FK onto
  `mspSowsTable.sowId`). Server-generates `poamId` (`poam-ref.ts`, avoiding the
  `registerRef`-never-written gap #2529 had to fix later for RBD). Real CRUD:
  `msp-poams.ts` (MSP-console: list/create/get/update/cancel + milestone CRUD) and
  `portal-poams.ts` (Portal: list/get/create/sign — the real customer signature ceremony,
  server-derived ip/hash/timestamp, RACI-authority-gated, reusing
  `risk-authority.ts`'s resolution functions rather than duplicating them). Both routers
  registered in `routes/index.ts`. Verified: `tsc --noEmit` clean on `@workspace/db` and
  `@workspace/api-server`; a real esbuild-bundled smoke test against local Postgres
  (insert → poamId assignment → milestone insert → read-back → cleanup) passed end to
  end. Filed #3094 (wire `poam.milestone_approaching`/`poam.expiring` alert detectors,
  now unblocked by this schema — #1942's own PENDING_DETECTOR notes), parented under
  Feature #1935, board status set to AI Batter Up. A concurrent session's retention-purge
  coverage work (#3072) surfaced a real gap in this build's own table mid-session (#3093:
  `msp_poams.tenant_id` had no purger declared) — fixed in the same session
  (`poamPurger` in `retention/purgers/modules.ts`), verified via
  `tenant-scope-coverage.live-db.test.ts` (7/7 passing). Deliberately NOT tier-gated via
  `portal-tier-features.ts` — which Monitoring tier bundles POA&Ms is a real
  entitlement/money decision this build does not invent; flagged in the issue comment for
  Shane. Phase 1b (#3081, bidirectional RBD conversion) is unblocked by this landing but
  out of this issue's own scope. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`); working tree clean.
