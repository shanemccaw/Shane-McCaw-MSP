# #3477 — Batch the mirror's own blocked_by population (WRITE side)

- **Status:** ✅ DONE 2026-09-10T04:14:25Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 8ea552f1f (code), 69af2cb1c (IN FLIGHT bookend)

## Log
- 2026-09-10T04:09:06Z ⏳ IN FLIGHT — Both mirror sync paths (`GitHubIssueMirror.cs` incremental ~L901, full ~L1128) fire one live `gh.GetBlockedByAsync(num)` REST call per blocked-labeled issue when POPULATING the mirror. This is the WRITE side (#3467 fixed the READ side). Fix: add a batched `BatchGetBlockedByAsync` to `GitHubApiClient` using the same aliased-GraphQL-subquery pattern as #3347's `BatchGetProjectItemStatusesAsync` / #3350's `BatchGetRecentIssueCommentsAsync` (GraphQL `issue(number:).blockedBy` is confirmed live), then wire both call sites to it so a whole sync costs a handful of GraphQL calls instead of one REST call per issue.
- 2026-09-10T04:14:25Z ✅ DONE — Added `GitHubApiClient.BatchGetBlockedByAsync` (aliased `issue(number:).blockedBy` GraphQL, 25/chunk, circuit-aware, throws on rate-limit — same pattern as #3347/#3350) and wired BOTH `GitHubIssueMirror` sync paths (incremental + full) to it. Full sync now costs ~2 GraphQL calls instead of 26 live REST calls (26 blocked-labeled issues today); incremental N→ceil(N/25). Preserved: un-returned issues keep existing mirrored blocked_by; full sync sets `blockedByPassComplete=false` on partial/throw so the inverse blocking graph isn't refreshed from an incomplete pass. `GetBlockedByAsync` REST kept (still used by BatterUp/ChatDock/IssueDispatch). **Verified live** against real GitHub: batched query returns identical blocker numbers to per-issue REST across all 26 blocked issues (3419→3418, 1867→1909, multi-blocker 3146→3145,3144). Build clean (0 warnings/0 errors). Landed on main as 8ea552f1f, branch confirmed ancestor of main. No findings to file. No `Shane To-Do`. Working tree clean.
