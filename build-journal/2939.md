# #2939 — action.assign-autopilot-profile: beta-only resource + wrong assign body

- **Status:** ✅ DONE 2026-09-05T01:40:00Z
- **Scope:** platform

## Log

- 2026-09-05T01:20:00Z — Started. Pulled #2939's full issue body + comment. Two independent
  defects to fix: (1) windowsAutopilotDeploymentProfile is beta-only, but graphWriteForTenant
  is pinned to GRAPH_BASE (v1.0), so the stored row 404s at transport; (2) the stored body is a
  groupAssignmentTarget shape, but the `assign` action's only documented parameter is
  `deviceIds` (String collection).
- 2026-09-05T01:40:00Z — ✅ DONE — commit `6d4707347` (rebased/pushed to `main` as `1cffc3c4a`).
  Extended `graphWriteForTenant` (artifacts/api-server/src/lib/graph.ts) to accept an absolute
  `https://graph.microsoft.com/{v1.0,beta}/...` URL the same way `graphFetchForTenant` already
  did (#1796), gated to the Graph host. Corrected the `action.assign-autopilot-profile` row via
  `lib/db/migrations/manual/2026-09-04-assign-autopilot-profile-endpoint-body-fix-2939.sql`, run
  against local Postgres: endpoint -> absolute beta URL, body_template ->
  `{"deviceIds": ["{{deviceId}}"]}`, required_variables -> `["profileId", "deviceId"]`. Updated
  `graph-write-permissions.ts`'s `normaliseEndpoint` to strip a graph.microsoft.com v1.0/beta
  absolute host prefix before pattern-matching, and refreshed the rule's justification/
  notRequestedReason now that the transport/body defects are fixed (grantRecommended stays
  false — no Config Pack wires this template yet). 82/82 tests pass in
  graph-write-permissions.test.ts (2 new normaliseEndpoint cases added). `tsc --noEmit` clean.
  Branch verified merged into `main` via `verify-branch-merged.mjs`. Working tree clean.
  No new findings filed — the issue's own evidence was sufficient to implement both fixes
  directly, no additional gap discovered.
