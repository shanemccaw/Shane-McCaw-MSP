# #2939 — action.assign-autopilot-profile: beta-only resource + wrong assign body

- **Status:** ⏳ IN FLIGHT 2026-09-05T01:20:00Z
- **Scope:** platform

## Log

- 2026-09-05T01:20:00Z — Started. Pulled #2939's full issue body + comment. Two independent
  defects to fix: (1) windowsAutopilotDeploymentProfile is beta-only, but graphWriteForTenant
  is pinned to GRAPH_BASE (v1.0), so the stored row 404s at transport; (2) the stored body is a
  groupAssignmentTarget shape, but the `assign` action's only documented parameter is
  `deviceIds` (String collection).
