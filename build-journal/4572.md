# Issue #4572 — Add right-click "Copy commit hash" to the status bar's Deploy segment

- **Status:** ⏳ IN FLIGHT 2026-09-17T00:00:00Z
- **Scope:** buildconsole

## Log
- 2026-09-17T00:00:00Z — Starting. Confirmed current code: `MainWindow.xaml` Deploy `StatusBarItem`'s StackPanel (line ~741) has `MouseLeftButtonUp="DeployStatus_Click"` and no `ContextMenu`. `BtnUpdate` in the same StatusBarItem has a working `ContextMenu` example (`RunUpdateAnywayMeteredItem`). Adding a `ContextMenu` with a "Copy commit hash" `MenuItem` to the StackPanel, plus a click handler in `MainWindow.VersionUpdate.cs` that strips the `"Deploy: "` prefix from `DeployStatusText.Text` and calls `Clipboard.SetText`.
