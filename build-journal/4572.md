# Issue #4572 — Add right-click "Copy commit hash" to the status bar's Deploy segment

- **Status:** ✅ DONE 2026-09-17T00:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-17T00:00:00Z — Starting. Confirmed current code: `MainWindow.xaml` Deploy `StatusBarItem`'s StackPanel (line ~741) has `MouseLeftButtonUp="DeployStatus_Click"` and no `ContextMenu`. `BtnUpdate` in the same StatusBarItem has a working `ContextMenu` example (`RunUpdateAnywayMeteredItem`). Adding a `ContextMenu` with a "Copy commit hash" `MenuItem` to the StackPanel, plus a click handler in `MainWindow.VersionUpdate.cs` that strips the `"Deploy: "` prefix from `DeployStatusText.Text` and calls `Clipboard.SetText`.
- 2026-09-17T00:20:00Z — ✅ DONE — Added `ContextMenu` to the Deploy StackPanel in `MainWindow.xaml` (MenuItem "Copy commit hash") and `CopyDeployCommitHash_Click` handler in `MainWindow.VersionUpdate.cs` (strips `"Deploy: "` prefix, `Clipboard.SetText`). Left-click rebuild behavior untouched — additive only. `dotnet build -c Debug` succeeded, 0 warnings/0 errors. Commit `9dd2949872cd806d9e3ba907a30bd4429db55052`, fast-forward-pushed directly onto `origin/main` per this repo's commit-to-main convention. Verified HEAD is an ancestor of `origin/main` and working tree is clean. No live WebView2/BuildConsole runner instance was launched to click the context menu interactively; verification was build success + code review of the pattern (mirrors the already-working `BtnUpdate` ContextMenu example) — this is a simple additive WPF UI change with no backend/data surface, so no test manifest applies (BuildConsole/WPF app is out of test-manifest scope). No findings surfaced.
