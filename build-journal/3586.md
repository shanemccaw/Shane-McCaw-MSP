# #3586 — Lazy-init OpenAI image/audio/base client so import doesn't require env vars

- **Status:** ✅ DONE 2026-09-11T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** a81b6b633 (fix), merged to main as part of merge commit on origin/main

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — vitest fails to even collect tests when a file
  transitively imports workflow-executor.ts, because
  `lib/integrations-openai-ai-server/src/image/client.ts` (and its siblings
  `client.ts`, `audio/client.ts`) throw at module-load time if
  `AI_INTEGRATIONS_OPENAI_BASE_URL`/`AI_INTEGRATIONS_OPENAI_API_KEY` are unset.
  Converting the `openai` export in all three files to a lazily-constructed Proxy
  (constructs + validates env vars on first real property access, not at import).
- 2026-09-11T00:20:00Z ✅ DONE — Converted `openai` export in `client.ts`,
  `image/client.ts`, `audio/client.ts` (all under
  `lib/integrations-openai-ai-server/src/`) to a `Proxy`-backed lazy singleton:
  construction + the env-var checks now happen on first real property access
  (e.g. `openai.images.generate`), not at module import. Consumer call sites
  (`workflow-executor.ts` and the client files' own functions) needed no changes
  — the `openai.foo.bar()` shape is preserved. Verified: with
  `AI_INTEGRATIONS_OPENAI_BASE_URL`/`AI_INTEGRATIONS_OPENAI_API_KEY` unset,
  `pnpm exec vitest run src/lib/workflow-executor-core.test.ts` (which
  transitively imports the image client) now collects and passes — 80/80 tests
  passed, exit 0 (previously failed at collection). Commit a81b6b633, merged to
  origin/main and confirmed via verify-branch-merged.mjs. Left
  `lib/integrations/openai_ai_integrations/` untouched — grep found zero real
  consumers of that package; it appears orphaned/superseded by
  `integrations-openai-ai-server` and is out of scope for this fix.
