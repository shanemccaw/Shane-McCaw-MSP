# #3586 — Lazy-init OpenAI image/audio/base client so import doesn't require env vars

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — vitest fails to even collect tests when a file
  transitively imports workflow-executor.ts, because
  `lib/integrations-openai-ai-server/src/image/client.ts` (and its siblings
  `client.ts`, `audio/client.ts`) throw at module-load time if
  `AI_INTEGRATIONS_OPENAI_BASE_URL`/`AI_INTEGRATIONS_OPENAI_API_KEY` are unset.
  Converting the `openai` export in all three files to a lazily-constructed Proxy
  (constructs + validates env vars on first real property access, not at import).
