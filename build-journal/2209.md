# #2209 — Chat Document Container (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T14:12:42Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** `2a52b5d84` (implementation), `9fd191d7e` (IN FLIGHT bookend) — both verified ancestors of origin/main

## Log
- 2026-09-02T13:55:40Z ⏳ IN FLIGHT — Building the full Chat Document Container per #2209's
  12-section body spec. Blocked_by (#2197) is closed+complete → unblocked. Audit complete:
  the container substantially exists in `desktop/ShaneBuilder/MainWindow.xaml(.cs)` as
  `ClaudeChatDock` — a 5-band-ish surface (cool context bar with real per-BuildSet stats,
  stat-detail flyout, Detected Items panel *shell*, Inspector warning/blocking states, live
  claude.ai WebView2). Real gaps to close this session:
  (1) §11 bug — context bar epic `#1202` is HARDCODED, must derive from `TabDef` (add
      `EpicNumber`); (2) §6 Detected panel is a fixture empty-state, wire it to #2197's real
      `ChatDockData` (grouped GitIssue detections by board status + pinned-question Questions,
      loose cards with `DetectionKind` chips, Promote to Queue / Dismiss); (3) §2 context gauge
      token estimate (0.28/char + 40k overhead per resolved OQ#3), Start-New-Chat ≥75%, wrench
      menu (WPF Popup — NOT measured screen coords), ⧉ Floaty button, overflow/z-index fix;
      (4) §7/§8 per-tab `ChatDrafts` dictionary + cross-epic question round trip
      (`CrossEpicQuestion` w/ required `FromTabId`). Keeping the working real-claude.ai WebView2
      (non-regressive; it is the real message/draft source) rather than replacing it with a
      native mock transcript that would need fixture messages.
- 2026-09-02T14:12:42Z ✅ DONE — Shipped the Chat Document Container onto the existing
  `ClaudeChatDock`. Verification: `dotnet build` clean (0 warnings, 0 errors); the §6 real-data
  path confirmed directly against local Postgres (`bt_chat_mentioned_issues` = 817 rows / 10 chats,
  `bt_chats` = 40 rows, `chat_pinned_questions` = 0 open — columns match `ChatReadClient`'s SQL
  exactly). WPF surface, so no test manifest (BuildConsole/ShaneBuilder is out of scope for those);
  did NOT launch the exe (single-instance-mutex hazard). What landed vs the 12-section spec:
  §11 epic-derived bug FIX — added `TabDef.EpicNumber`; context bar + breadcrumb read the epic off
  the active tab, hardcoded `#1202` gone. §2 context bar — token-estimate gauge (0.28/char + 40k,
  two-part shape per resolved OQ#3), Start-New-Chat at ≥75%, wrench menu as a WPF `Popup` anchored by
  `PlacementTarget` (NOT screen coords — flagged bug), `Panel.ZIndex=40` + no clip (other flagged
  bug), ⧉ Floaty button. §3 breadcrumb/Share bar (Share copies the real claude.ai conversation URL,
  hides the pill when no real conversation is open — OQ#1). §6 Detected panel wired to #2197's real
  `ChatDockData` via new `Services/ChatDetectionService.cs`: grouped GitIssue by live board status +
  Question from pinned questions, loose cards, `DetectionKind` chips, Promote-to-Queue (stages a real
  build header into the composer, never auto-sends) / Dismiss. §7 cross-epic round trip — real
  `CrossEpicQuestion` w/ required `FromTabId`; writes into the target tab's own draft stamped with the
  return address; Bring-the-answer-back attempts one real DOM read of the destination's last assistant
  turn (OQ#2), falls back to manual paste. §8 per-tab `ChatDrafts` dictionary + app-owned composer
  band (band 5); Send injects into claude.ai's composer via WebView2 `ExecuteScriptAsync`, clears only
  that tab's draft. §9 inspector states already existed, kept. §10 Floaty button + minimal topmost
  mini-window on the SAME conversation. §5 tool-writes-to-composer invariant established across every
  panel action; the 11 tool-panel internals are siblings (#2194/#2200/#2202/#2213–2220), out of scope,
  and a formal shared-header rail component beyond the existing flyout pattern is NOT built (honest
  partial — the container/switching seam is there, the shared-header refactor is deferred). Findings
  filed: #2227 (source doc `README-ClaudeChat.md` not committed) and #2228 (Task/Todo/Commitment
  DetectionKinds have no real data source); both parented to #2198, set AI Batter Up. Committed
  `2a52b5d84`, pushed to origin/main, branch verified merged, tree clean.
