# #1385 — ps-execution Dev/Production container separation

- **Status:** ⏳ IN FLIGHT      <!-- flip to: ✅ DONE  (or leave IN FLIGHT if abandoned) -->
- **Scope:** platform
- **Started:** 2026-08-26
- **Commit(s):** <real hash(es) — fill at DONE>

## Log
- 2026-08-26 ⏳ IN FLIGHT — Provisioning an isolated `ca-ps-execution-dev` Container App in
  `rg-smccaw-2184` (East US 2), mirroring the production `ca-ps-execution` managed-identity +
  Key Vault (ShaneMcCawConsulting) config, building/deploying the current `services/ps-execution`
  image to it via `acrsmccaw2184`, live-smoke-testing `get-connection-info` against the real
  testbed tenant, then routing api-server `ps-execution-client.ts` by the real #91 dev/prod
  environment-tiering (Dev→dev container, Staging/Prod→existing prod container) and documenting
  both deploy targets in `services/ps-execution/README.md`. Closes the safety gap where a single
  shared container served both dev testing and live production traffic.
