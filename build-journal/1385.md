# #1385 — ps-execution Dev/Production container separation

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-26
- **Commit(s):** 710a71a63 (bookend), 381b428ba (routing + README)

## Log
- 2026-08-26 ⏳ IN FLIGHT — Provisioning an isolated `ca-ps-execution-dev` Container App in
  `rg-smccaw-2184` (East US 2), mirroring the production `ca-ps-execution` managed-identity +
  Key Vault (ShaneMcCawConsulting) config, building/deploying the current `services/ps-execution`
  image to it via `acrsmccaw2184`, live-smoke-testing `get-connection-info` against the real
  testbed tenant, then routing api-server `ps-execution-client.ts` by the real #91 dev/prod
  environment-tiering (Dev→dev container, Staging/Prod→existing prod container) and documenting
  both deploy targets in `services/ps-execution/README.md`. Closes the safety gap where a single
  shared container served both dev testing and live production traffic.
- 2026-08-27 ✅ DONE — Shipped in 381b428ba (prod ca-ps-execution untouched throughout).
  **Azure infra (real, live):** provisioned `ca-ps-execution-dev` in rg-smccaw-2184 /
  East US 2 / env cae-smccaw-2184, sharing registry acrsmccaw2184 (image tag
  `ps-execution:dev`, built from current source via `az acr build`) and Key Vault
  ShaneMcCawConsulting. Mirrored prod's config exactly: system-assigned MI (principal
  0369477a-5f31-49d3-b476-4da1e6ed1896) with AcrPull (auto-granted via
  `--registry-identity system`) + Key Vault Secrets User (RBAC vault, no access
  policies); env AZURE_KEY_VAULT_URL + MT_APP_CLIENT_ID=9ea2e409…; ingress external
  targetPort 8080; scale 0–2; cpu 0.5/mem 1Gi. Running revision `dev1385a`.
  **Code:** ps-execution-client.ts `getContainerUrl()` now routes by env.ts
  isProductionEnvironment — Dev→PS_EXECUTION_CONTAINER_URL_DEV, Staging/Prod→existing
  PS_EXECUTION_CONTAINER_URL, dev never falls back to prod. README documents both deploy
  targets with distinct az CLI commands. (.env.local given the dev URL locally — gitignored.)
  **Live smoke test (HONEST):** POST get-connection-info → dev endpoint, testbed tenant
  (TEST_TENANT_ID c4c814d4…, Organization mccawsoft2.onmicrosoft.com). Full container
  plumbing works end-to-end (MI→KV secret round-trip, cert parse thumbprint 251BDCD5…,
  module import, bearer gate all confirmed). But Connect-IPPSSession returned structured
  **502 auth_failed**; console-log root cause: `Could not load file or assembly
  'Microsoft.Identity.Client, Version=4.83.1.0'`. Pre-existing image defect (MSAL assembly
  conflict, likely EXO+MicrosoftTeams sharing one pwsh process) that affects prod
  identically — NOT caused by the dev/prod split, out of #1385's scope, filed as
  follow-up #1389. Typecheck: my two files are clean (absent from api-server tsc errors; the
  pre-existing errors are all in unrelated ai-*/admin-build-tracker/msp-diagnostics files).
