# #4038 — Status Reports: wire the real Design into artifacts/portal (phase 4 of 4)

- **Status:** ✅ DONE 2026-09-14T18:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ae617e1f2 (IN FLIGHT bookend), 9f9448162 (Status Reports page + deep-link fix), e75763599 (fix ClipboardList import collision from concurrent #4037 rebase)

## Log
- 2026-09-14T17:20:00Z ⏳ IN FLIGHT — read #4038/#3435 body+comments, the contract pack (docs/portal/status-reports-portal-contract-pack.md), the design export (Design/portal/design_handoff_billing_roles_and_new_modules/screens/Status Reports.dc.html) and README, and confirmed the real portal-status-reports.ts route matches the pack exactly. Confirmed §0 disambiguation: this is the msp_status_reports table, NOT the existing My Architect retainer status-reports card (status_reports table). Starting implementation: React page + API hook + Shell nav registration in artifacts/portal.
- 2026-09-14T18:05:00Z ✅ DONE — Shipped `artifacts/portal/src/pages/status-reports.tsx` + `lib/status-reports-{api,types}.ts`, wired at `/status-reports` and `/status-reports/:id`, with a sidebar entry after My Architect (`moduleNav.ts`). Master-detail layout: published-only report list (newest-period-first), full report content, a two-sided oldest-first comment thread (`YOUR MSP` / `YOUR ORGANISATION` chips), and a composer enforcing the real 1–10,000 char rule with idle/sending/sent/error states matching the design's own scenes. A `:id` deep link not present in the customer's own published list renders the design's real 404 scene ("That report isn't available to you") derived from the already-loaded list rather than a second network call, since the list query applies the identical customerId+published predicate the by-id route does. Included the design's "what this page deliberately does not do" ledger verbatim, citing the contract pack's own section numbers. No fixture data anywhere — every field comes from `GET/POST /api/portal/status-reports*` (#3887).
  - **Also fixed `artifacts/api-server/src/lib/portal-deep-links.ts`**: every customer-portal notification's `linkPath` is resolved through this file, and it only did exact-match lookups against a static table — so the real `/status-reports/{id}` links `notification-center.ts` already mints (both the MSP→customer publish fan-out and the customer→MSP comment notification) could never resolve to anything but "coming soon," even with this page now live. Added a dynamic-pattern branch for `/status-reports/:id`. Found while wiring this page's own entry point; fixed in the same build rather than filed separately, since it's the same feature's own notification path, not an unrelated discovery.
  - Verification: `tsc --noEmit` clean on both `artifacts/portal` and `artifacts/api-server`; `vite build` (portal) succeeds. Confirmed live against local Postgres: `msp_status_reports` id 5 (tenant 1, published) and `msp_status_report_comments` id 2 exist exactly as the contract pack documents. Confirmed the real dev API server (`:8080`) has `GET /api/portal/status-reports` mounted and auth-gated (401 unauthenticated, not 404). **Not done**: a full authenticated browser walkthrough — no test-portal credentials/JWT were available in this session to mint a customer-scoped token, so the page's rendering wasn't exercised end-to-end in a live browser. Typecheck, build, and direct-route/DB verification are the honest extent of this session's verification.
  - `git status --porcelain` clean; branch verified merged into `origin/main` (`verify-branch-merged.mjs`) before writing this.
