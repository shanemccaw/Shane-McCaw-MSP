# #1446 — Licensing (top-level pillar page): strict pass — every field must be real data, honest no-scan-data, or NO-BACKEND-TO-WIRE tagged

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** b7c52bebb

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting strict pass over `licDashboardData.ts` and
  `portal-v2-licensing.tsx`. Prior sessions (#1230, #1411) confirmed onTable,
  recoveredTotal, ackMonthly, and the 3 recovery buckets have no billing-term/
  finance-register backend anywhere in the schema, but only added a hidden test
  marker — the fixture dollar figures are still what renders on screen. This
  session's job is to re-verify that finding still holds, then replace those
  fields (and any other unmarked field found in this pass) with an honest
  visible "No live data available" state plus a `NO-BACKEND-TO-WIRE:` code
  comment, per Shane's non-negotiable standing rule. No fixture-as-fallback
  left anywhere on the page when done.
- 2026-08-28 ✅ DONE — Confirmed #1230/#1411's finding still holds, then did a
  full-page audit rather than stopping at the 4 named fields: every genuine
  backend gap on the page (hero money figure + sentence, trend, the 3
  HERO_STATS, the 3 recovery buckets + their breakdown panel, finance-strip
  ackMonthly, acknowledged-spend cards, savings ledger, "why the waste recurs"
  policy list) now renders a visible "No live data available" state with the
  specific reason, replacing #1411's hidden-marker-only approach. Every gap
  carries the literal, greppable `NO-BACKEND-TO-WIRE:` comment at its
  declaration in `licDashboardData.ts` and again at its render site in
  `portal-v2-licensing.tsx`. Deliberately left unchanged: the per-SKU licence
  ledger, which #1230 already wired to real `/subscribedSkus` +
  `sku_price_reference` data with its own considered, tested live/fixture
  threshold — a different, already-reviewed matter from the 4 fields this
  issue named; flagged as a possible separate follow-up in the issue comment
  rather than silently expanding this pass's scope.
  Verification: full `msp-portal` `tsc --noEmit` clean on this change (2
  pre-existing errors remain in `SowScreen.tsx`/`PortalV2Shell.tsx`, neither
  touched here — confirmed via `git diff` showing no changes to those files);
  full `msp-portal` unit suite 1811/1811 passing, including new/updated
  coverage in `licDashboardData.test.ts` for `LIC_HERO_STATS`'s new
  reason-based shape and the new `LIC_BUCKET_GAPS` export. A live
  `shaneapp://runTest` UI run of `test-manifests/portal/portal-v2-pillars.json`
  was attempted (BuildConsole.exe confirmed running, protocol trigger
  accepted) but produced no result file after two bounded waits totalling
  ~19 minutes — consistent with heavy concurrent load on the shared instance
  (dozens of other sessions' recent result files present in the same temp
  directory) rather than a refusal or crash. Not claimed as a passing live
  run; the manifest's Licensing steps were rewritten to assert the new honest
  states and are ready for a live run whenever load allows.
  No new client-side logging was wired: this session's changes are pure
  rendering/fixture-data changes with no new fetch or mutation path (the page
  already reads its one real value — score/delta — through the existing
  `useLivePillarHero` seam), so there was no natural event boundary for the
  locked channel taxonomy the way there is for a new subsystem or route.
  Branch verified merged into `main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
