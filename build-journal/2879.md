# #2879

- **Status:** ✅ DONE 2026-09-05T00:12:00Z
- **Scope:** platform

## Log
- 2026-09-05T00:01:17Z — Starting: fix real type mismatch in dashboard-data.test.ts:147 — `.toMatch()` called against `res.body.error` which is the `apiError()` envelope object (`{ code, message, ... }`), not a string. Updating assertion to target the real `message` field.
- 2026-09-05T00:12:00Z — ✅ DONE — Confirmed real envelope shape from `apiError()` (`artifacts/api-server/src/lib/api-helpers.ts:50`): `{ error: { code, message, ... } }`. Confirmed the actual 403 message text ("Access to this customer is not permitted") from `middlewares/requireAuth.ts:404`. Changed `expect(res.body.error).toMatch(/not permitted/i)` to `expect(res.body.error.message).toMatch(/not permitted/i)` in `artifacts/api-server/src/routes/dashboard-data.test.ts:147`. Ran `npx vitest run src/routes/dashboard-data.test.ts` — 17/17 passed. Commit `d670048b5` on `agent/2879-q1679`, rebased and pushed to `main` at `c2bd995c`. `verify-branch-merged.mjs` confirms merged. Working tree clean.
