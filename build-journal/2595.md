# #2595 — SOPs: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T17:52:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** f44e0ad8c (pack), 6e2dc8050 (bookend IN FLIGHT)

## Log
- 2026-09-03T17:42:52Z ⏳ IN FLIGHT — Sub-issue of #1687 (Feature: SOPs, MSP Console),
  a DIFFERENT surface from the existing `docs/sops-contract-pack.md` (which is scoped
  explicitly to `artifacts/portal`'s customer-facing `/api/portal/*` routes and was
  itself just regenerated today under #1728). #2595 covers the MSP-console operator
  side: `artifacts/api-server/src/routes/msp-sops.ts`'s 6 real routes (list/create SOPs,
  run a SOP, list/create/patch sop-runs), consumed today only by `get_running_sops`
  (mcp-server) — `artifacts/msp-console` itself doesn't exist yet (#1680 blocks it).
  Extracting the #1642-pattern pack as a new file, `docs/sops-msp-console-contract-pack.md`,
  read-only against `msp-sops.ts`, `sop-execution.ts`, `sop-workflow-graph.ts`, the
  Drizzle schema, and live local DB state.
- 2026-09-03T17:52:00Z ✅ DONE — Wrote `docs/sops-msp-console-contract-pack.md` (new file,
  ~230 lines): all 6 real `msp-sops.ts` routes extracted verbatim (raw `$inferSelect` row
  shapes, not hand-shaped `Wire*` interfaces — the surface has none, a real finding recorded
  in §1); the full `runSopForCustomer` execution path (§1.3, CR gate/#1497, policy
  enactment/#1548, the #1550 CR-flood auto-raise, #1939's read-skips-CR-gate carve-out);
  real enum unions incl. `versionStatus` having NO enum anywhere (§2); cross-surface edges;
  forbidden list; orphaned-endpoint table showing `get_running_sops` (mcp-server) as the one
  REAL, already-wired consumer of 2 of the 6 routes, with `artifacts/msp-console` confirmed
  not to exist yet (§0.1). Open gaps flagged (§5: no duplicate-sopId 409 handling on
  `POST /msp/sops`, no lock coordination between the PATCH route and `settleSopRuns`,
  `versionStatus` unconstrained) — none met this project's finding bar as a confirmed
  defect, so none filed as a new sub-issue; all are design notes for the not-yet-built
  MSP-console UI. Live DB re-confirmed via `psql`: 15 `msp_sops` rows, 0 `msp_sop_runs`.
  Read-only: no product code, schema, or UI changed. Committed `f44e0ad8c`, pushed to
  `origin/main`, verified merged via `verify-branch-merged.mjs` (exit 0). `git status
  --porcelain` clean.
