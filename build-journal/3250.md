# #3250 — Old flat `.tabs` nav bar never removed -- competes with capture dock for bottom position

- **Status:** ✅ DONE 2026-09-08T18:15:19Z
- **Scope:** platform (web/shanes-life — Shane's Life, milestone 19 "Shane")
- **Started:** 2026-09-08
- **Commit(s):** 9185cf2ed (rebased onto main as 6d53f159a)

## Log
- 2026-09-08T18:06:23Z ⏳ IN FLIGHT — Confirmed root cause via code: `web/shanes-life/public/app.css`'s
  `.tabs` rule is real, live, `position:fixed; bottom:0` and does compete with `.capture`'s
  `bottom: calc(56px + var(--safe-bottom))` offset. But the `.tabs` bar (Today/Meds/Inbox/Settings)
  is not dead code -- it's the only nav path to Meds/Inbox/Settings, since the house grid
  (`ROOM_DEFS` in app.js) only covers 9 of the design's own now-spec'd rooms and Settings has no
  other entry point at all. The current README (Design/design_handoff_shanes_life/README.md)
  documents Medicine and Inbox as real house rooms and Settings as an attic-vent tap target in the
  roof -- so the correct fix is to actually finish that supersession (add Meds + Inbox rooms, add
  the attic vent), not just delete `.tabs` and orphan Settings. Building that now, then removing
  `.tabs` for real.
- 2026-09-08T18:15:19Z ✅ DONE — Shipped as commit 9185cf2ed (main: 6d53f159a). Medicine and Inbox
  are now real house-grid rooms (`roomsForToday()` in `src/routes/api.mjs` computes real lit/
  subtitle state from `medications.getMedsToday()`'s batch data and the real `pendingCaptures`
  count; `ROOM_DEFS` in `public/app.js` gets the two new entries with real furniture symbols added
  to `critters-sprite.svg`; `room-order.mjs`'s `ROOM_KEYS`/`DEFAULT_ORDER` extended to match).
  Settings is now reached via a louvered gable vent tapped in the roof SVG (README "Settings = the
  attic"), its only real entry point. `.tabs` CSS + markup + the `.tabs a` sync loop + the dead
  `#inbox-badge`/`setInboxBadge()` are removed outright from `app.css`/`index.html`/`app.js`.
  `.capture`'s bottom offset drops the +56px it reserved for `.tabs` (now `var(--safe-bottom)`
  directly); `.quick-toast`'s offset shifted down 56px to match. Verified against the real local
  Postgres db (`finances`) and a real running instance on port 5099 (Shane's own live :5000
  instance was never touched -- confirmed still healthy throughout): `/api/today` returns real
  `rooms.meds`/`rooms.inbox` state, and a `setRoomOrder`/`getRoomOrder` round-trip reconciles the
  now-11-key room set correctly. `git status --porcelain` clean; branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` before writing this. No migration needed -- no
  schema change, all new state derived from existing tables.
