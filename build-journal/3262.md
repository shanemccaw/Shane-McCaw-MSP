# #3262 — get_capture_photo has no server-side downscale for photos over 5MB

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** (none — this is a stop-and-ask, no code change)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — pulled #3262's body, re-confirmed the gap against
  current code: `web/shanes-life/src/mcp/tools.mjs` `MAX_INLINE_IMAGE_BYTES = 5 * 1024 * 1024`
  (line 44), `get_capture_photo`'s over-limit branch (line 196-199) returns the same honest
  error the issue quotes — no server-side resize path exists.
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — stopping at a real product/dependency decision, not
  implementing. This app's stated philosophy (`web/shanes-life/README.md`: "one runtime
  dependency (`pg`) ... no framework, no build step") makes adding an image-processing
  library (sharp / jimp / etc.) a genuine product call, not a missing-column-style gap to
  just build. Two real options exist with different consequences:
    1. Add a real image-resize dependency (e.g. `sharp`, native bindings, real install
       cost — this project's own "pnpm install is not a remedy / bandwidth is a real
       constraint" guidance applies to evaluating that cost) and downscale server-side
       before the 5MB inline-content check.
    2. Hand-roll a pure-JS JPEG re-encode/downscale (no new dependency, more code to
       maintain, quality/perf tradeoffs).
  Neither is "no work" — this needs Shane's call on which tradeoff he wants before code
  is written. Posted that as a comment on #3262; left the issue open, un-closed, with
  `in-flight` label still on it since a decision, not a fix, is what's pending.
- No files changed. `git status --porcelain` clean (nothing touched by this session).
