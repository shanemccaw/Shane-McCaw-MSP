# #3262 — get_capture_photo has no server-side downscale for photos over 5MB

- **Status:** ✅ DONE 2026-09-08T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 4fb2736e3768492ab8b9ad810d23ce345cbc3c23 (bookend, decision writeup), 9a413c8884a741aa6c958005930cdc1e2427f732 (jpeg-js version, superseded), dedbea7d98b28c81ec715aa97139cf130ef92f2d (sharp version, final -- on main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — pulled #3262's body, re-confirmed the gap against
  current code: `web/shanes-life/src/mcp/tools.mjs` `MAX_INLINE_IMAGE_BYTES = 5 * 1024 * 1024`
  (line 44), `get_capture_photo`'s over-limit branch (line 196-199) returns the same honest
  error the issue quotes — no server-side resize path exists.
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — first pass stopped at the dependency decision and posted
  a comment on #3262 asking Shane to pick a direction (add a real image-resize dependency vs.
  hand-roll a pure-JS codec). Shane replied "Try again" — read as authorization to make the
  call and implement, not to keep waiting.
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — re-investigated this app's own precedent before picking a
  direction: `bin/make-icons.mjs` hand-rolls a PNG *encoder* against `node:zlib` (no decode
  needed, trivial forward generation) and `src/push/webpush.mjs` hand-rolls RFC 8291/8292 crypto
  on `node:crypto` (well-specified primitives already built in) — both real "no new dependency"
  precedents, but neither is a correct-baseline-JPEG-codec-from-scratch (DCT, Huffman, chroma
  subsampling), which is genuinely high-risk to get subtly wrong and would defeat the whole point
  (silently corrupting the exact photos #3261's tool exists to let Claude see). Picked the
  narrowest real fix: added `jpeg-js` (pure JS, zero transitive deps, no native binary,
  ~small download — confirmed via `npm view jpeg-js version dependencies`) to do JPEG
  decode/encode only; the resize math itself (nearest-neighbor downsample) is still hand-written
  in `src/core/image-resize.mjs`, matching this app's existing style everywhere else.
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — implemented:
  - `web/shanes-life/src/core/image-resize.mjs` — new, `downscaleJpegToFit(bytes, mimeType, maxBytes)`:
    decodes via `jpeg-js`, iteratively drops quality (82→...→25) then dimensions (×0.75, floor
    480px) until the re-encoded JPEG fits under `maxBytes`, or returns `null` if it's not a JPEG
    or can't get under budget at the floor.
  - `web/shanes-life/src/mcp/tools.mjs` `get_capture_photo` — on an over-limit photo, calls
    `downscaleJpegToFit` before erroring; on success returns the downscaled bytes as real inline
    image content plus a `downscaled: {originalByteSize, sentByteSize, width, height, quality}`
    field; on failure (non-JPEG, or still too big at the floor) keeps the same honest-error shape
    the issue described, now naming #3262 instead of pointing at #3261's "not built yet".
  - `web/shanes-life/package.json` — added `"jpeg-js": "^0.4.4"` to `dependencies`; ran
    `npm install` in `web/shanes-life` (standalone npm app, not the pnpm workspace — 15 packages
    total including `jpeg-js`'s own zero transitive deps, real one-time download, justified by
    the actual feature this build adds, not a "fix broken resolution" remedy).
  - `web/shanes-life/README.md` "There is no framework and no build step" — updated from "one
    runtime dependency (`pg`)" to "two ... `pg`, and `jpeg-js`", with the reasoning for the
    exception spelled out explicitly (mirrors the hand-roll-vs-dependency precedent above).
  - `web/shanes-life/src/push/webpush.mjs` — fixed its own now-stale "one runtime dependency"
    comment to match.
  - `web/shanes-life/bin/selftest-image-resize.mjs` — new, following this app's existing
    `bin/selftest-webpush.mjs` / `bin/selftest-plaid-webhook.mjs` convention: builds a real
    ~10MB noisy JPEG in-process (noise resists compression, unlike a flat-color fixture, so it
    reliably lands over the 5MB limit at a real resolution), runs it through
    `downscaleJpegToFit`, asserts the result actually fits under 5MB AND actually re-decodes to
    the claimed dimensions (not just "returned bytes"). Registered as `npm run
    selftest-image-resize`.
- 2026-09-08T00:00:00Z ✅ DONE — real verification run:
  ```
  $ node bin/selftest-image-resize.mjs
  Built a real synthetic JPEG: 10.26MB
  Downscaled to 4.70MB (2400x2400 @ q52) -- fits: true, re-decodes: true
  PASS: real over-limit JPEG downscales to a real, decodable image under the limit
  ```
  `node --check` clean on all three touched/new `.mjs` files. Did **not** run the full
  `bin/check.mjs` e2e (needs a running server + live DB + real passkey round trip) — out of
  scope for this low-effort, single-function fix; the targeted selftest above is the same class
  of coverage this app's own `selftest-webpush`/`selftest-plaid-webhook` scripts already provide
  without a live server. Honest gap: only JPEG is covered by real server-side downscale (the
  common real case — phone camera captures are JPEG); PNG/WEBP/HEIC/HEIF over 5MB still get the
  same honest "can't shrink this" error as before, since `jpeg-js` only decodes JPEG. Not filed
  as a separate follow-up issue — same-class gap as this issue's own scope, not a new finding.
- Real `git status --porcelain` clean at session end (verified below); branch fast-forwarded
  onto `origin/main` and pushed directly, per "Getting work onto origin/main is still your job."
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — **correction:** after pushing the `jpeg-js` version above
  and posting its completion comment, found Shane's own real comment on this issue (posted while
  this session was mid-flight) explicitly naming the direction: *"Real, direct decision from
  Shane... add `sharp`, proceed... Add `sharp` as a real, explicit second dependency (document
  why in README.md's own dependency list...), downscale server-side before the 5MB inline-size
  check... proceed with the real fix."* That's an explicit instruction, not something my own
  jpeg-js reasoning should have overridden. Redid the implementation on `sharp` instead:
  - `npm uninstall jpeg-js && npm install sharp@^0.35.2` (resolved `0.35.4`, real native binary
    for this platform confirmed loadable: `sharp.versions` reports real libvips/mozjpeg/heif
    etc. builds).
  - `src/core/image-resize.mjs` rewritten: `downscaleImageToFit(bytes, maxBytes)` — no longer
    JPEG-only; `sharp`/libvips decodes JPEG/PNG/WEBP/HEIC/HEIF alike (every mime type
    `media.mjs`'s `ALLOWED` set accepts for a photo), always re-encodes as JPEG. This closes the
    "PNG/WEBP/HEIC still error" gap the jpeg-js version had — real improvement, not just a swap.
  - `get_capture_photo` now awaits `downscaleImageToFit` (async, since `sharp` is
    promise-based) instead of calling a sync jpeg-only helper.
  - `package.json`/`package-lock.json`, `README.md`, `webpush.mjs`'s comment all updated to
    name `sharp`, with Shane's own quoted reasoning for the exception (not my own guess at one).
  - `bin/selftest-image-resize.mjs` rewritten to build its synthetic over-limit JPEG via `sharp`
    itself and assert against `downscaleImageToFit`. Re-ran: real synthetic 28.51MB JPEG →
    3.63MB (3200x3200 @ q65), fits: true, re-decodes: true. PASS.
  - `node --check` clean on all touched files.
  - Posted a correction comment on #3262 superseding the earlier jpeg-js completion comment.
