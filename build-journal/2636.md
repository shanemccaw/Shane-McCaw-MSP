# #2636 — Offboarding: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T20:05:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 88e5afadf (offboarding wiring), ad8bf06fe (IN FLIGHT bookend)

## Log
- 2026-09-12T19:36:00Z ⏳ IN FLIGHT — wiring `Design/MSP_Console/design_handoff_msp_console/Offboarding.dc.html`
  to the real `GET /api/msp/dashboard` (offboarding fields) and the 3 real
  `msp-portal.ts` offboarding routes (`/request`, `/export`, `/archive`), mounted
  into MSP Console Shell (#3677) as a new Operations page.
- 2026-09-12T20:05:00Z ✅ DONE — added `src/api/offboarding-api.ts` (read hook over
  `GET /api/msp/dashboard`'s offboarding fields + 3 mutations) and
  `src/pages/Offboarding.tsx` (4-step forward-only stepper, one live action card,
  export package card, design's own notes — all real data, no fixtures), and
  wired the `offboarding` page id into `nav.ts` / `treeModel.ts` /
  `ConsoleShell.tsx`. Verified: `tsc --noEmit` clean, `vite build` clean (commit
  88e5afadf, pushed to `origin/main`, `verify-branch-merged.mjs` confirms
  ancestor). `git status --porcelain` clean. No findings filed — no genuine
  gap discovered beyond what the contract pack already documents (§9, e.g.
  #2510's known event-severity bug, restated in this page's own notes card,
  not re-filed).
