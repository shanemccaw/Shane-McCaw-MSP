# #2577 — Change Control: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-06T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** dd98cd213 (rebased/pushed to main as 8c767d77c)

## Log
- 2026-09-06T14:05:00Z ⏳ IN FLIGHT — Confirmed both real blockers (#2664, #2665) closed; parent
  Feature #1681 (Change Control, MSP Console). Mapping the real backend surface across 8 mounted
  routers (`msp-changes.ts`, `msp-change-catalog.ts`, `msp-change-control-cab.ts`,
  `msp-change-dependencies.ts`, `msp-change-executions.ts`, `msp-change-freeze-windows.ts`,
  `msp-change-maintenance-windows.ts`, `msp-change-pir.ts`) before writing
  `docs/change-control-msp-console-contract-pack.md` per the #1642 pattern. Read-only — no
  product code, schema or UI changes expected.
- 2026-09-06T18:10:00Z ✅ DONE — Wrote `docs/change-control-msp-console-contract-pack.md`
  (603 lines): full wire contracts for all 8 routers, real enum unions (schema-only), CURRENT
  vs DECIDED/OPEN GAP module map, cross-surface edges (write gate #1497/#1773/#1504, drift
  attribution #1497 CA-only 30-day window, rejection→risk #1514, retainer billing hook #1293,
  catalog/rollback/CAB links), and the orphan-check discipline (confirmed zero UI consumers
  anywhere — `artifacts/msp-console/src` is an empty scaffold, expected ahead of #2578/#2579).
  Live-queried the local DB: all 14 Change-Control-family tables are genuinely empty (0 rows).
  Surfaced one real, confirmed gap: the MSP console's generic `PATCH
  /msp/change-requests/:id` never routes a `rejected` transition through `recordRejection`,
  leaving `cr_approvals` rows dangling (false SLA-breach escalations) and, for a routed
  Microsoft change, never flipping `m365_change_routings.decision`. Filed as **#3033**,
  parented under Feature `#1681` (Feature-tier parent — not the Epic), labelled `bug`,
  milestone v1.1, board status set to "AI Batter Up". Commit `dd98cd213` rebased cleanly onto
  `origin/main` (3 retries as main moved under concurrent load) and pushed as `8c767d77c`;
  `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean. No code,
  schema or UI touched — read-only pack as required.
