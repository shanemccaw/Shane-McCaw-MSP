# #4156 — MT_APP_CERT_PRIVATE_KEY: single-line base64 env encoding (structural fix for PEM leak via `source .env.local`)

- **Status:** ✅ DONE 2026-09-15T01:27:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** `d78755ae5` (work); `2061bd52f` (IN FLIGHT bookend)

## Log
- 2026-09-15T01:03:30Z ⏳ IN FLIGHT — locating every consumer of `MT_APP_CERT_PRIVATE_KEY`, then switching it to a base64-of-PEM single-line encoding decoded at load time, re-encoding the shared-checkout `.env.local`, and verifying real cert auth without echoing key material.
- 2026-09-15T01:27:00Z ✅ DONE — Shipped in `d78755ae5`:
  - New `artifacts/api-server/src/lib/mt-app-cert-key.ts` is the single decoder. The canonical form is single-line base64 of the PEM. A legacy raw PEM (real or `\n`-escaped newlines) is still accepted so Replit Secrets (#855) do not break on deploy.
  - Consumers switched: `sharepoint-admin.ts` (the only runtime consumer; `monitor-executor.ts` / `config-snapshot-collector.ts` only name the var in messages), `scripts/src/diagnose-sharepoint-cert.ts`, and `scripts/config-state/probe-sharepoint-admin-transport.mjs`. The README env row is updated.
  - Added unit tests (`mt-app-cert-key.test.ts`) and a live verify entry `src/scripts/verify-mt-app-cert-key-4156.ts`.

  **Premise corrections:**
  1. `MT_APP_CLIENT_ID` is `4743b130` "MSP Platform - Multi-Tenant Consent (DEV)", not the DEV write app `9f6f4772`. It is still a DEV registration.
  2. The old value was broken. Line 31, the only line any loader read, was a corrupt PEM that did not parse. Lines 32-58, the text `source` leaked, were the private key of the **non-DEV** app `9ea2e409` cert `CN=ShaneMcCawMSP-MT-App-2026`, not the DEV key. Confirmed by public-key hash comparison against `az rest` keyCredentials. Filed as #4194.

  **`.env.local`:** the shared checkout `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP\.env.local` (and this worktree's copy) now holds single-line base64 of `C:\Source\certs\Dev\mt-app-dev-private-key.pem`. That key's public key matches the registered DEV cert FAD7BC9F…, which `MT_APP_CERT_THUMBPRINT` already named. The 27 orphaned lines are removed. Backups of the pre-change files are outside the repo in `C:\Users\Ronnie\.shane-msp-env-backups\` and still contain the non-DEV key; see #4194.

  **Verification (real, no key material printed):**
  - vitest `mt-app-cert-key.test.ts` + `monitor-executor.test.ts`: 213/213 pass. `tsc --noEmit` on api-server is clean.
  - `node artifacts/api-server/run-script.mjs src/scripts/verify-mt-app-cert-key-4156.ts` → env single-line, decoded rsa/2048, SharePoint admin token acquired via `getSharePointToken`, and `getTenantSharingCapability` read on testbed tenant 1 returned `1`.
  - `bash source .env.local` now errors only on line 70 (an unquoted path in another var, out of scope). No key lines remain.
  - Baseline before the change: loader value unparseable (`ERR_OSSL_UNSUPPORTED`); orphaned key → `AADSTS700027`.

  **Memory note** `env-local-bash-source-leaks-pem-key.md` is updated to say this is structurally closed. **Findings filed:** #4194 (URGENT, security: rotate the non-DEV cert), #4195 (line-based env loaders silently truncate multi-line values). Both are parented under #1495 with status AI Batter Up.
