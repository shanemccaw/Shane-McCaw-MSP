# #4184 — TYPO/COLOR AUDIT: 9 document/log/terminal viewer controls ignore Typography.xaml (119 raw literals)

- **Status:** ✅ DONE 2026-09-15T03:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 4a26520dc770a6db2e27b412ca56f57ffc5c05d4

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — pulled #4184 (real audit from #4162): 9 files, 119 raw literals (82 FontSize, 20 FontFamily, 17 hex color) across LogViewerDocumentView.xaml, GraphApiDocumentView.xaml, SqlDocumentView.xaml, StartupLoadingView.xaml, ServiceLogView.xaml, IssueDetailView.xaml, TerminalView.xaml, ClaudeTerminalView.xaml, DiffView.xaml. Mapping each raw literal to Typography.xaml/Colors.xaml/DarkTheme.xaml resources by role — terminal/log/sql viewers get MonospaceFontSize/MonospaceFontFamily, not BodyFontSize.
- 2026-09-15T03:20:00Z ✅ DONE — mapped all 9 files. Sizes: exact-match or nearest-neighbor to `CaptionFontSize`(11)/`MonospaceFontSize`(13)/`SubHeaderFontSize`(16)/`HeaderFontSize`(18) by semantic role (monospace/log/terminal/SQL text got Monospace*, regular UI chrome got Caption/SubHeader/Header). Raw `FontFamily="Cascadia Code, Consolas..."` / `"Courier New"` / `"Cascadia Code, Menlo, Consolas"` → `{StaticResource MonospaceFontFamily}` throughout. Left untouched, same exception classes #4178 already established: decorative icon/emoji glyphs (`ServiceLogView.xaml:29` 🌐, `StartupLoadingView.xaml:149,152` ✨⚡ ambient sparkles), the `Segoe MDL2 Assets` icon font (`TerminalView.xaml:147-148`, tracked separately per #4162), `StartupLoadingView.xaml`'s entire bespoke hand-drawn "Sprocket" mascot illustration (16 hex fills/gradient-stops for an original character — not theme surface colors, forcing them onto semantic brushes like RedBrush/YellowBrush would tie decorative art to unrelated Theme Editor sliders), and `SqlDocumentView.xaml:29,41` raw `#11111B` (pre-#2126 Catppuccin Crust value with no current exact-match resource — filed as new issue rather than guessing, see below). Verified: `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/0 errors. Commit 4a26520d pushed to origin/main (rebased twice past concurrent pushes); branch verified merged via `verify-branch-merged.mjs`; working tree clean. Filed **#4220** (`SqlDocumentView.xaml: raw hex #11111B has no matching Colors.xaml/DarkTheme.xaml brush`), labeled `bug`, milestone v1.1, parented as sub-issue of #1202 (confirmed #4184's own real parent via GraphQL — no Feature-tier parent applies), board status set to AI Batter Up.
