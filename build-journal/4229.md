# #4229 — Wire Status Warning/Error wash brushes for BuildQueueMapWindow.xaml/BuildQueuePanel.xaml

- **Status:** ✅ DONE 2026-09-15T05:30:01Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** no new commit — work already merged to `main` via `2efbd3527` (BuildQueueMapWindow.xaml, part of #4218) and `13b8aee36`/`416c8fff6` (BuildQueuePanel.xaml, a separate concurrent session)

## Log
- 2026-09-15T05:30:01Z ⏳ IN FLIGHT — investigating #4229 (blocked-by #4218, confirmed closed/merged): pull the two raw-hex banner sites named in the issue body and wire them to theme brushes.
- 2026-09-15T05:30:01Z ✅ DONE — no code change needed. All four call sites named in #4229 are already on theme resources on `main`, no raw `#3A1E26`/`#3A3200` literal remains at any call site (only the two brush *definitions* in `Colors.xaml` still carry those hex values, which is correct and expected):
  - `BuildQueueMapWindow.xaml` `DeadlockBanner`/`UnverifiedBanner` — wired to `StatusErrorWashMediumBrush`/`StatusWarningWashMediumBrush` in commit `2efbd3527` ("Wire Status Warning/Error wash brushes for #4218").
  - `BuildQueuePanel.xaml` `OrphanRecoveryBanner`/`BuildSetFilterWarningPopup` — wired to dedicated opaque `StatusErrorBannerBackgroundBrush`/`StatusWarningBannerBackgroundBrush` (added specifically because these are solid banner-card backgrounds, not alpha washes) in commit `13b8aee36`, already an ancestor of this branch's HEAD and of `main`.
  - Verified via `grep -rn "3A1E26\|3A3200" desktop/BuildConsole/ --include=*.xaml` — only the two `Colors.xaml` brush definitions match, no call site.
  - `git status --porcelain` clean, no files touched this session.
