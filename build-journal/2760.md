# #2760 — Monitoring: close coverage gap on the policy surface (21 resources)

- **Status:** ✅ DONE 2026-09-04T05:00:57Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 6f0eac3b8 (IN FLIGHT bookend), 20d10abda (18-check migration)

## Log
- 2026-09-04T04:46:40Z ⏳ IN FLIGHT — pulled live query, confirmed 21 `surface='policy' AND availability='available_now' AND check_coverage_count=0` resources. Probed all 21 real Graph endpoints against the testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1, app-only). Findings: 2 resources genuinely unreachable on this tenant (`/policies/accessReviewPolicy` beta = 403 no valid license; `/policies/b2cAuthenticationMethodsPolicy` beta = 403 not a B2C directory) and 1 resource's modeled `graph_path` itself 400s as a standalone GET (`/policies/authenticationMethodsPolicy/authenticationMethodConfigurations` — only reachable embedded under the parent or per-item-id, not as its own collection). Proceeding to design real checks for the remaining 18 confirmed-working endpoints.
- 2026-09-04T05:00:57Z ✅ DONE — Added 18 real `monitor_checks` rows (`policy:*`) for the 18 policy-surface resources with real, working Graph endpoints; each check's `mapping`/`severity_rules` reads the actual field(s) its label names (no generic-existence checks, #2184 discipline). Migration `lib/db/migrations/manual/2026-09-04-policy-surface-coverage-2760.sql` run locally against `DATABASE_URL` (18 checks + 78 package-wiring rows + self-marking insert, all committed). `check_coverage_count` recomputed via the same `matchEndpointToResource` matcher `build-resource-model.mjs` uses (not a full rebuild) — all 18 matched `graph-path-exact`/high confidence. Verified by replaying the real captured testbed response shapes through the actual production `applyMapping`/`evalConditionGrammar` (`monitor-executor.ts`) via a temporary vitest inclusion (reverted after the run, working tree left clean) — all 18 compute correctly, zero mapping warnings. `accessReviewPolicy`'s real 403 license gap was recorded as live evidence and correctly reconciled to `needs_license` via the existing `applyLiveEvidence` pipeline (no check needed, working as designed). The remaining 2 real gaps (`b2cAuthenticationMethodsPolicy` not applicable to this tenant type; `authenticationMethodConfigurations`'s own `graph_path` 400s as a standalone GET) are real config_resources model bugs, filed as **#2816** (sub-issue of Feature #1128, milestone v1.1, label `bug`). `surface='policy' AND availability='available_now' AND check_coverage_count=0` is now 2 rows, down from 21. Labels flipped `in-flight`→`complete` on #2760; completion comment posted. GitHub's GraphQL API hit a persistent shared secondary rate limit for most of the session (confirmed real: `rate_limit` showed full quota while every GraphQL call, including plain reads, still failed) — worked around it by using REST (`gh api .../issues`, `.../sub_issues`, `.../labels`, `.../comments`) for everything that has a REST equivalent. The one thing REST cannot do — setting #2816's Projects-v2 board status to "AI Batter Up" (GraphQL-only) — could not be completed; noted plainly in the #2760 completion comment for Shane or a later session to set. `git status --porcelain` clean.
