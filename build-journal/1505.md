# #1505 — Replace linkedFinding free text with real foreign-key edges

- **Status:** ✅ DONE 2026-09-03T12:38:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 88f545f51ae97efddf6c4390d66d514ac83dd3d7 (main)

## Log
- 2026-09-03T12:11:51Z ⏳ IN FLIGHT — pulled #1505 body + all comments. Investigated the five named edges live against schema + real writers (`msp_change_requests.linkedFinding`, `portal_hold_window_events.changeRequestId`, `drift_events.crRef`/`baselineSnapshotId`, `msp_risk_decisions.checkKey`/`registerRef`/`spawnedByChangeRequestId`/`dischargedByChangeRequestId`, `msp_sop_runs.psaTicketId`↔CR `psaTicketId`). Local DB is essentially empty for these tables (msp_change_requests: 0, portal_hold_window_events: 0, drift_events: 0, msp_sop_runs: 0, msp_risk_decisions: 1 row with every one of these columns null) — no free-text migration needed, will report 0/0/0 honestly. Found `spawnedByChangeRequestId`/`dischargedByChangeRequestId` already exist as plain ints (no FK) — these ARE the risk↔CR forward/back pointers the issue asks for, just not wired as real FKs yet. Found a real bug: `monitor-executor.ts`'s `crRef: \`CR-${cr.id}\`` does not match `parseChangeRequestCode`'s `CR-2026-\d+` format, so drift's crRef can never resolve back to a CR — fixing as part of this build since it directly blocks making that edge real. Proceeding to schema + migration + route/wire work.
- 2026-09-03T12:38:00Z ✅ DONE — Added real FKs for all 5 edges: (1) `msp_change_requests.sourceInterpretationId`/`sourceResolutionId` now FK'd to `m365_change_interpretations`/`m365_change_resolutions` (were bare ints), new `linkedHoldWindowId` FK to `portal_hold_windows` (third writer, `rollbackOfChangeRequestId`, already FK'd since #1499) — `linkedFinding` itself stays free text per rule 4, documented as backed by these 3 real pointers; (2) `portal_hold_window_events.changeRequestId` FK'd to `msp_change_requests`; (3) `drift_events.baselineSnapshotId` FK'd to `drift_baseline_snapshots`, new `changeRequestId` column as the real FK behind the `crRef` display string; (4) `msp_risk_decisions.spawnedByChangeRequestId`/`dischargedByChangeRequestId` FK'd to `msp_change_requests` (the risk↔CR forward/back pointers) — `checkKey` left unFK'd on purpose (documented union-of-sources constraint, same as `remediationCheckKey`), `registerRef` has no writer anywhere in the codebase (verified live) and nothing to FK, filed as #2529; (5) `msp_sop_runs.psaTicketId` and `msp_change_requests.psaTicketId` both indexed (no local `psa_tickets` table to hard-FK to — external PSA system). Also fixed a real bug found while making edge 3 real: `monitor-executor.ts` wrote `crRef: \`CR-${cr.id}\`` which never matched `parseChangeRequestCode`'s `CR-2026-\d+` format — now uses `formatChangeRequestCode` like every other writer. Migration: `lib/db/migrations/manual/2026-09-03-change-control-referential-edges-1505.sql`, run against local `DATABASE_URL` (all 8 FK constraints + 6 new indexes confirmed live via `pg_constraint`/`pg_indexes`; FK enforcement proven with a real violating UPDATE that correctly errored; all 5 edges proven end-to-end with a real `BEGIN`/`INSERT`/`JOIN`/`ROLLBACK` transaction, then confirmed the DB was left clean). Data migration: every touched table was empty locally (msp_change_requests/portal_hold_window_events/drift_events/msp_sop_runs: 0 rows; msp_risk_decisions: 1 row, all 4 relevant columns NULL) — 0/0/0, nothing to backfill, `linkedFinding` untouched. **tsc/vitest could not be run** — confirmed the shared main checkout's pnpm store has empty package dirs for drizzle-orm/pg/zod (pre-existing, already tracked under #2515/#2525/#2192/#2522/#2503/#2193/#2502 — not filing a duplicate). Verified instead via direct SQL against local Postgres and careful manual review of every diff hunk. Rebased twice onto moving `origin/main` (picked up #1504 and #1544, no conflicts) and pushed directly to main: `88f545f51ae97efddf6c4390d66d514ac83dd3d7`. `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean. Filed #2529 (registerRef has no writer) as a sub-issue of #1486 (this issue's Feature-tier parent), board status set to AI Batter Up. Appended migration line to #1630.
