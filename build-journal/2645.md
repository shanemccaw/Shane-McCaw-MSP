# #2645 — Documents (MSP Console): regenerate the contract pack from the real, finished backend

- **Status:** ✅ DONE 2026-09-03T19:49:00Z
- **Scope:** platform
- **Started:** 2026-09-03

Leaf issue, step 3 of #2569 (Feature: Documents, MSP Console). #1642-pattern
extraction: per-surface wire contracts extracted verbatim and cited to file:line,
CURRENT vs DECIDED marked on every field, real enum unions only, cross-surface edges,
honest tri-state, forbidden list, orphaned endpoints listed explicitly. Read-only — no
product code, schema, or UI changed.

Backend confirmed real per dispatch: `msp-documents.ts` (11 routes) + `msp-documents-hub.ts`
(4 routes), both mounted in `routes/index.ts`.

## Log
- 2026-09-03 ⏳ IN FLIGHT — Full read of both route files, `lib/db/src/schema/msp.ts`
  (`mspDocumentsTable`, `mspDocumentVersionsTable`, `mspSharepointConnectorsTable`,
  `DOC_PIPELINE_STATUSES`), `doc-pipeline-nodes.ts` (all 7 node handlers +
  `DEFAULT_DOC_PIPELINE_GRAPH`), `sharepoint-connector.ts`, `requireAuth.ts`
  (`ROLE_ORDER`, `resolveStaffScopedCustomerIds`), `resolve-msp-id.ts`. Live DB
  confirmed via local `DATABASE_URL`: `msp_documents`, `msp_document_versions`,
  `msp_sharepoint_connectors`, `insights_generated_documents` all 0 rows — honest
  zero-state, matches sibling `docs/offboarding-msp-console-contract-pack.md` pattern.
  Consumer sweep (`grep -rl "msp/documents"` across `artifacts/portal`,
  `artifacts/admin-panel`, `artifacts/msp-website`) found zero callers —
  `artifacts/msp-console` doesn't exist yet, expected pre-scaffolding state.
  **Real finding confirmed**: `POST /api/msp/documents`'s `autoPublish` request param
  (default `false`) is a dead field — `DEFAULT_DOC_PIPELINE_GRAPH` has no conditional
  edge and `handleDocPublish` never reads `ctx.input["autoPublish"]`, so every
  document submitted through the pipeline is unconditionally published, regardless of
  the flag's value. Filing as sibling sub-issue of #2569.
- 2026-09-03 ✅ DONE — Wrote `docs/documents-msp-console-contract-pack.md`
  (~590 lines): 15-route wire contract across both files, split into §1 (MSP-authored
  HTML→PDF→SharePoint pipeline) and §2 (read-only aggregation hub over
  `insights_generated_documents` + its own MSP-scoped share-mint route); real enum
  unions (`DOC_PIPELINE_STATUSES`, document status/owner-type, connector mode,
  `ROLE_ORDER`); full pipeline-stage detail for all 7 node handlers incl. the
  text-only `pdf-lib` rendering caveat; honest-empty contract (all 4 backing tables
  0 rows locally); cross-surface edges (shared `insights_generated_documents`/
  `quick_win_result_shares` with the sibling Portal pack, the deactivated-connector
  reuse gap, the already-filed #2510 severity pattern, `resolveBillingMspId` vs
  `resolveMspIdStrict` split); forbidden list; §0.1 orphaned-endpoint table (all 15,
  `artifacts/msp-console` doesn't exist yet). Real finding filed and parented:
  **#2724** — `autoPublish` request param on `POST /api/msp/documents` is a dead
  field (workflow graph has no conditional edges, `handleDocPublish` never reads it,
  every document is published unconditionally regardless of the flag), sub-issue of
  #2569, labeled `bug`, milestone v1.1, board status set to AI Batter Up. Read-only
  pass: no product code, schema, or UI changed — only the pack + this bookend.
  Verified `verify-branch-merged.mjs` exit 0 before writing DONE; `git status
  --porcelain` clean. Commit: (own commit, pushed to origin/main).
