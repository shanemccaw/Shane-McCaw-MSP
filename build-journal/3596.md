# #3596 — Test Run falsely reports "unknown node type" for approval_gate, break_glass_verification_gate, and 5 monitor/remediation node types

- **Status:** ✅ DONE 2026-09-11T05:23:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** c0e4272e3 (fix), 6d52814fa (regression tests)

## Log
- 2026-09-11T05:13:04Z ⏳ IN FLIGHT — fixing executeNode()'s dry-run guard in artifacts/api-server/src/lib/workflow-executor.ts: adding approval_gate and break_glass_verification_gate to STRUCTURAL_TYPES (their real case bodies have clean self-contained inline dry-run branches), and adding explicit makeDryRunOutput() cases for monitor_get_package, monitor_execute_package, config_snapshot_collect, execute_monitor_check, remediation_pointed_verify.
- 2026-09-11T05:23:00Z ✅ DONE — Fix landed at c0e4272e3: added `approval_gate` + `break_glass_verification_gate` to `STRUCTURAL_TYPES` (their real case bodies have clean, self-contained inline `if (dryRun)` branches with no DB/Graph calls), and added 5 explicit `makeDryRunOutput()` cases (`monitor_get_package`, `monitor_execute_package`, `config_snapshot_collect`, `execute_monitor_check`, `remediation_pointed_verify`) mirroring the already-written inline dry-run branches' output shapes exactly. Added 22 regression tests at 6d52814fa (`workflow-executor-core.test.ts`, one describe block per node type) — all pass (102/102 total in that file). `pnpm exec tsc --noEmit` on `artifacts/api-server` clean, no errors in either changed file. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean. No test-manifest needed (change is backend workflow-engine code, not `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting`, per CLAUDE.md scope).
