# #1899 — mfa-enforcement-v1 step 1 real endpoint fix

- **Status:** ✅ DONE 2026-09-04T07:33:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 74a858995 (real work), 82e5463a4 (merge to main)

## Log
- 2026-09-04T07:13:56Z ⏳ IN FLIGHT — Researched the real Graph mechanism for forcing MFA
  re-registration. `POST /users/{id}/authentication/methods` is not a writable Graph v1.0
  collection (confirmed against Microsoft Learn). The real mechanism (same one the Entra
  admin center's "Require re-register MFA" button uses): GET the user's current
  authentication methods, then DELETE each phone / Microsoft Authenticator / software OATH
  method individually. This is a genuine fan-out (1 GET + N DELETEs) that
  `baseline_action_templates`' single-{endpoint,method,body}-call model cannot express, so
  it needed a dedicated executor branch, not just a DB row edit.
- 2026-09-04T07:33:00Z ✅ DONE — Added `runForceMfaReregistrationAgainstTenant()` (a real
  fan-out executor branch in `workflow-executor.ts`, keyed off
  `action.require-security-info-reregistration`) and a `graphReadForTenantWithWriteToken()`
  helper in `graph.ts` for the enumeration GET. Updated `graph-write-permissions.ts` with
  the real DELETE (phoneMethods/microsoftAuthenticatorMethods/softwareOathMethods) + GET
  permission rules — `UserAuthenticationMethod.ReadWrite.All` is now genuinely requested
  (17 permissions total, was 16). Updated the stored `baseline_action_templates` row via
  `lib/db/migrations/manual/2026-09-04-mfa-reregistration-real-endpoint-1899.sql`, run
  against local Postgres (endpoint -> GET, description documents the fan-out).
  Typecheck clean (`tsc --noEmit`); `graph-write-permissions.test.ts` (29),
  `remediation-catalog.test.ts`, `dlp-role-group-provisioning.test.ts` all pass (45
  total). `workflow-executor-core.test.ts`'s 19 failures are pre-existing (confirmed via
  git-stash comparison — a broken `broadcastWorkflowRunError`/`sse-channels` mock,
  unrelated to this change).
  Live-verified the real permission grant: declared + admin-consented
  `UserAuthenticationMethod.ReadWrite.All` on the DEV write app registration
  (9f6f4772-b5be-421f-815e-b392336c373a) via
  `apply-write-app-permissions-1875.mjs --app dev --apply --consent`, confirmed via a real
  Entra readback (`verify-write-app-consent-645.mjs`: all 17 required permissions
  granted). Did NOT execute a live destructive DELETE against a real user — no
  codebase-sanctioned safe/dummy test account exists in the testbed tenant (which is also
  Shane's real production M365 tenant); an Explore search confirmed this. Inventing one
  wasn't this session's call. Attempted a safe read-only GET-only smoke test but ad-hoc
  script/network execution was blocked by the sandbox classifier in this session — did not
  attempt to route around it.
  `git status --porcelain` clean. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`: OK).
