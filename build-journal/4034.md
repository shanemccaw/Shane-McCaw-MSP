# #4034 — Complete decommission of 4 dead workflow node types outside builder support

- **Status:** ✅ DONE 2026-09-15T17:48:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** a41e8e7f9

## Log
- 2026-09-15T17:25:18Z ⏳ IN FLIGHT — pulled #4034 body + comments; starting removal of `ask_ai`/`topic_picker`/`generate_image`/`generate_landing_page` from node-type-registry.ts, admin-workflows.ts catalog, patternRegistry.ts, default/sample-data files, and lib/db schema union + dist decl. Leaving RunDetailContent.tsx/WorkflowBuilderPage.tsx read-only support alone per issue scope.
- 2026-09-15T17:48:00Z ✅ DONE — removed the 4 node types from node-type-registry.ts (isAIDependent/aiCostOwner entries), admin-workflows.ts's FLAT_NODES catalog, patternRegistry.ts (deleted the whole "AI Summarise + Publish" pattern, which was built on `ask_ai`, replacing it with a removal comment matching the existing Pattern 5 convention), workflow-node-default-samples.ts's STATIC_NODE_SAMPLES, and the WfNode.type TS union in lib/db/src/schema/index.ts (+ manually kept lib/db/dist/schema/index.d.ts in sync locally — that dist file is gitignored/not committed; no build script regenerates it, `@workspace/db`'s package.json exports map points at src/*.ts directly). Fixed 4 now-stale assertions in workflow-node-output-samples.test.ts that exercised the removed `ask_ai` fixture. Left RunDetailContent.tsx/WorkflowBuilderPage.tsx untouched (styling/label maps keyed loosely by string, not the WfNode union — confirmed unaffected) and left seed-system-workflows.ts's "Presentation Phase Generator" workflow (def #6, lines 824-969) untouched — its 2 embedded `ask_ai` nodes ("ask"/"ask_title") ARE def #6 itself, the same still-existing published workflow the issue explicitly preserves for read-only viewing; they are not generic default-data entries like the other files, and removing them would mean either leaving dangling graph edges or re-engineering the workflow's AI generation step — a product decision beyond this issue's "remove dead entries" scope, so stopped there per the dispatch's own "stop and report rather than force" instruction. Verification: `tsc --noEmit` clean in both artifacts/api-server and artifacts/admin-panel; workflow-executor test suite 286/286 passing (baseline unchanged; the pre-existing `workflow-executor-redaction-4015.test.ts` 0-test env-var failure is unrelated, present before this build); workflow-node-output-samples.test.ts, node-type-registry.test.ts, variablePicker.test.ts, workflow-triggers.test.tsx, workflow.test.tsx all green after the fixture-test fix. Commit a41e8e7f9 pushed to origin/main; verify-branch-merged.mjs confirmed ancestor; working tree clean.
