# #4077 — msp-consent.ts audits MSP-operator write actions with actorRole hardcoded to 'client'

- **Status:** ✅ DONE 2026-09-14T18:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** (fill in after commit)

## Log
- 2026-09-14T18:00:00Z ⏳ IN FLIGHT — confirmed 4 hardcoded `actorRole: "client"` sites in `artifacts/api-server/src/routes/msp-consent.ts` (lines ~225, ~306, ~381, ~432); fixing to use `resolveAuditActorRole(req.user!)` matching line 153's existing pattern.
- 2026-09-14T18:20:00Z ✅ DONE — replaced `actorRole: "client"` with `actorRole: resolveAuditActorRole(req.user!)` at all 4 write-route `createAuditLog` call sites (invite-link, write-consent/start, sharepoint-consent/start, consent/revoke). Added `artifacts/api-server/src/routes/msp-consent.test.ts` mounting the real router (requireCapability/assertCustomerAccess mocked, createAuditLog mocked, `resolveAuditActorRole` left real via `vi.importActual`) against the revoke route — confirms an MSPOperator session produces `actorRole: "msp"`, an MSPAdmin session also `"msp"`, and a legacy `role: "admin"` session `"platform_admin"` — never `"client"` — in the audit row. 3/3 new tests pass. `tsc --noEmit -p .` on api-server: 0 errors.
