# #3703 — Move top bar summary into the menu bar

- **Status:** ✅ DONE 2026-09-11T21:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 9c91d8ba6

## Log
- 2026-09-11T21:20:52Z ⏳ IN FLIGHT — moving TopBarSummaryMenu's real child items (Account/Location/Auto/Conservation/Drain) into a new compact `_Options` MenuItem next to `_Terminal`, removing the wide `TopBarSummaryBorder` toolbar element, and surfacing the usage%/reset text as the new MenuItem's live tooltip via `RefreshTopBarSummaryUi`.
- 2026-09-11T21:35:00Z ✅ DONE — `_Options` MenuItem added to the top menu strip right after `_Terminal` in `MainWindow.xaml`; `MiAccountToggle`/`MiLocationToggle`/`MiAutomationToggle`/`MiConservationToggle`/`MiDrainCapped` moved in unchanged (same names, same click handlers). Old `TopBarSummaryBorder`/`TopBarSummaryText`/`TopBarSummaryMenu` removed from the toolbar. `RefreshTopBarSummaryUi` in `MainWindow.xaml.cs` now writes the composed Account/Location/Auto/Conservation/usage% string to `MiOptionsMenu.ToolTip` instead of `TopBarSummaryText.Text`; the now-dead `TopBarSummary_Click` handler was removed. Verified with `dotnet build --configuration Release` — build succeeded, 0 errors (10 pre-existing unrelated warnings in BuildQueuePanel.xaml.cs/FocusModeBar.xaml.cs). `git status --porcelain` clean. Branch merged into origin/main (verify-branch-merged.mjs OK) at 9c91d8ba6.
