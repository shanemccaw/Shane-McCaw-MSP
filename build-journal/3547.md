# #3547 — signal.fired decision: remove from SUBSCRIBABLE_EVENT_TYPES

- **Status:** ✅ DONE 2026-09-10T22:52:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 2eca98178 (rebased/merged to main as c63934b68)

## Log
- 2026-09-10T22:45:18Z ⏳ IN FLIGHT — removing `signal.fired` from `SUBSCRIBABLE_EVENT_TYPES` (webhooks.ts:31) and updating the `msp-console-webhooks.ts:39` doc-comment to reference `fulfillment.item.created` instead (confirmed zero dispatch call sites for it, so it's a valid still-subscribable-but-undispatched example).
- 2026-09-10T22:52:00Z ✅ DONE — Removed `signal.fired` from `SUBSCRIBABLE_EVENT_TYPES` (`webhooks.ts:31`). Updated `msp-console-webhooks.ts:39` doc-comment example to `fulfillment.item.created` (verified: zero other refs/dispatch call sites for it). Updated `webhooks.eventtypes.test.ts` — flipped the test that asserted `signal.fired`'s presence to assert its absence, per Shane's decision. Verified against local Postgres: `SELECT count(*) FROM outbound_webhooks WHERE event_types::text ILIKE '%signal.fired%'` → 0 rows, confirming removal is safe. Ran `webhooks.eventtypes.test.ts` — 6/6 passed. Rebased onto origin/main and pushed directly to main (c63934b68); `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean.
