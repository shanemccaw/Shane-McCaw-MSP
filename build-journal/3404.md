# #3404 — msp-marketplace-purchase.ts: fulfillmentTypeKey='assessment' resolves to unknown_type, discarded silently

- **Status:** ✅ DONE 2026-09-11T04:45:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 7bfed8e0c (rebased onto main as 99d2e0bb7)

## Log
- 2026-09-11T04:05:00Z ⏳ IN FLIGHT — Confirmed via psql: `fulfillment_types` has exactly 3 rows
  (`project_sow`, `config_pack`, `monitoring_subscription`), `services.fulfillment_type_key`
  has 21 `assessment` rows (20 reachable via this route's `CUSTOMER_SERVICE_TYPES` catalog per
  issue body) and 6 `retainer` rows that resolve to `resolveFulfillment()`'s `unknown_type`.
  Both call sites in `msp-marketplace-purchase.ts` (`:236` free path, `:402` paid path) call
  `resolveFulfillment()` with a bare `await` and never inspect/log the result, unlike
  `portal-checkout.ts:930-955` which captures `result` and `log.info`s it. Fix per issue's own
  "Fix shape": capture + log the result at both call sites so `unknown_type` is visible in logs.
  The deeper fix (registering real `fulfillment_types` rows for `assessment`/`retainer`) is
  explicitly out of scope per the issue body (product/data decision).
- 2026-09-11T04:45:00Z ✅ DONE — Both `resolveFulfillment()` call sites in
  `msp-marketplace-purchase.ts` (free path ~:251, paid path ~:448) now capture the
  result and `log.info({result, offerId, customerId, serviceId}, ...)` it, matching
  `portal-checkout.ts`'s existing visibility pattern. Added 2 new tests asserting an
  `unknown_type` result is logged on both paths (16/16 tests pass total). `tsc --noEmit`
  clean. Real commit `7bfed8e0c`, rebased onto `origin/main` and pushed as `99d2e0bb7`;
  `verify-branch-merged.mjs` confirms merged. Working tree clean (`git status --porcelain`
  empty). No new findings beyond what's already documented in the issue — the deeper
  fix (real `fulfillment_types` rows) remains a product decision, not filed as a
  separate finding since the issue body already states it as out of scope.
