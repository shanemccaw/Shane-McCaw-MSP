# #3584 — Multi-repo: dispatch/worktree provisioning targets the claimed item's own real repo

- **Status:** ✅ DONE 2026-09-11T03:56:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 4a2dda0c6 (real implementation, pushed directly to `origin/main`)

## Log
- 2026-09-11T00:52:00Z ⏳ IN FLIGHT — Pulled #3584's full issue body + dispatch comment. Re-checking real status of #3579 (repo_owner/repo_name schema dimension on `bt_issue_mirror`/`bt_build_queue`) and #3581 (Settings repo registry) before starting, per the dispatch's explicit instruction and this issue's own stated dependency.
- 2026-09-11T00:52:00Z 🛑 BLOCKED — Confirmed neither #3579 nor #3581 has genuinely landed: both still `state: open`, both only labeled `in-flight` (not `complete`). Codebase check on `origin/main` finds zero `repo_owner`/`repo_name` columns anywhere under `lib/db/migrations/manual/`, and no repo-registry/Main-Tinker settings surface anywhere in `desktop/BuildConsole`. Every real-fix item in #3584's body (resolving the claimed item's real repo from #3579's schema column, using #3581's registry for the resolved clone URL) depends on both existing first. Sibling builds #3582 and #3583 hit and recorded the exact same blocker minutes earlier. Left the existing `blocked_by` edge to #3579/#3581 (wired at dispatch time via `--blocked-by 3579,3581`) in place — confirmed accurate, not stale. Swapped issue label `in-flight` → `blocked`, posted an explanatory comment on #3584 naming exactly what's missing. No implementation started — nothing real to build the repo-resolution logic against without guessing #3579's actual composite-key schema shape and #3581's actual query surface API. Stopping here per the blocked-workflow rule rather than proceeding or fabricating scaffolding against a schema/settings surface that doesn't exist yet.
- 2026-09-11T03:20:00Z ⏳ IN FLIGHT — Resumed: #3579 (`677dda0a1`) and #3581 (`533986cb7`) are both `CLOSED`/`complete`, and #3583 (per-repo pause, `13c04a009`) already landed on top of both. Removed the now-stale `blocked_by` edges on #3584, swapped label `blocked` → `in-flight`, rebased this worktree's branch onto current `origin/main` to build against the real landed schema/registry, posted a resume comment.
- 2026-09-11T03:56:00Z ✅ DONE (`4a2dda0c6`) — Implemented all 4 real-fix points: (1) `QueueWatcherService`'s pre-warm + real launch both resolve the target repo from `item.OwnerRepo` (#3579's schema column) instead of a hardcoded constant; (2) new `scripts/dev-server/repo-clone.mjs` resolves that repo to the existing Main-repo checkout (default, unchanged) or a dedicated persistent secondary clone under `C:\repos\<owner>__<repo>` for a real Tinker-tier repo, threaded through `provision-worktree.mjs`'s new `--repo` flag — every git operation inside the resulting worktree (commit/push/`origin`) genuinely targets the right remote via its own inherited git config; (3) `ComposeLaunchPrompt` now states the build's real target repo explicitly (Main vs. Tinker) in the dispatched prompt; (4) identical worktree isolation/commit/push/bookend rigor regardless of tier — the one deliberate difference is `MergeBackAsync` (dev-server merge-back) skipped for a Tinker repo, since there is genuinely no shared local dev server for a different repo to merge into. Also fixed a related correctness gap found along the way: `worktree-lifecycle.mjs`'s rescue-branch/unpushed-commit logic (`removeWorktreeSafe`/`preserveWorktreeWork`/`detectWorktreeWork`/`findOrphanedRescueBranches`) used to hardcode `config.mainRepoRoot` everywhere, which would have silently stamped a secondary-repo worktree's rescued work into the wrong repo — now resolves against each worktree's own real `repoRoot`, persisted on its tracking record. Real, live, offline verification (two real local bare repos standing in for GitHub, zero network cost): `scripts/dev-server/repo-clone.selftest.mjs` (18 assertions) and `scripts/dev-server/provision-worktree-multirepo.selftest.mjs` (14 assertions, full provision→commit→push→cleanup mechanism), plus every pre-existing worktree-isolation/-reprovision/-sweep/git-maintenance-straydir/store-doctor-autorepair selftest still passing (zero regression). `desktop/BuildConsole`: `dotnet build` clean, 0 errors. Verified `agent/3584-q2258` (`4a2dda0c6`) is a genuine ancestor of `origin/main` via `verify-branch-merged.mjs` before writing this DONE. Filed a genuine finding (#3630, parented under Feature #3578, board status AI Batter Up) for what was deliberately left out of scope: the periodic anonymous worktree sweep and the secondary-repo clone directory itself are not yet multi-repo-aware — the explicit by-name cleanup path used at normal build completion is fully fixed; the blind sweep still only enumerates the Main repo's own git worktrees.
