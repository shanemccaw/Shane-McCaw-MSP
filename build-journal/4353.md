# #4353 — Feature: Real shared Evidence object (backend + client)

- **Status:** ✅ DONE 2026-09-16T05:23:46Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/Shane-McCaw-MSP@65679c0f3, shanemccaw/MyArchitect@13c0ece

## Log
- 2026-09-16T05:03:18Z ⏳ IN FLIGHT — Building the shared Evidence object approved by Shane. Backend in Shane-McCaw-MSP api-server: new `evidence` + `evidence_links` (polymorphic-by-explicit-type) tables, endpoints `POST /evidence`, `GET /evidence?linkedType=&linkedId=`, `POST /evidence/:id/link`, `DELETE /evidence/:id`, real audit rows on attach/unlink. Client in shanemccaw/MyArchitect: `IEvidenceService` + reusable Evidence panel/tab. Foundational for #4345/#4349/#4350. First confirming the existing file-storage mechanism (screenshots/documents) and audit-log pattern before inventing anything.
- 2026-09-16T05:23:46Z ✅ DONE — Confirmed the real file-storage mechanism first: #3503's `evidence_attachments` (`msp-evidence-attachments.ts` + `evidence-attachments-store.ts`) stores a server-relative path under `UPLOADS_DIR/evidence-attachments` via multer disk storage, served only through a scoped auth GET. Reused that exact convention (`UPLOADS_DIR/evidence`) — did NOT invent a new one. Built the GENERAL shared layer distinct from that narrow 1:1 store.

  **Backend (Shane-McCaw-MSP@65679c0f3):**
  - `lib/db/src/schema/msp.ts` — `evidenceTable` (msp-scoped, `kind` file|link, `file_ref`, description, uploader) + `evidenceLinksTable` (FK→evidence cascade, `linked_type` enum of milestone|document|finding|gap|risk|kanban_card|poam, TEXT `linked_id`, unique (evidence_id, linked_type, linked_id)). Enum-as-text + `$inferSelect/$inferInsert` per repo convention.
  - `lib/db/migrations/manual/4353-evidence.sql` — additive DDL, **run against local `DATABASE_URL` in-session** (BEGIN/…/COMMIT, self-marking `simulator_migration_runs` INSERT). Verified table shapes with `\d`.
  - `artifacts/api-server/src/lib/evidence-store.ts` — create/get/link/unlink/listForTarget/listLinks/delete + `toWireEvidence`, all msp-scoped, `onConflictDoNothing` on link (idempotent).
  - `artifacts/api-server/src/routes/msp-evidence.ts` — `POST /api/msp/evidence` (multipart file OR JSON link, optional attach-on-create), `GET /api/msp/evidence?linkedType=&linkedId=`, `POST /api/msp/evidence/:id/link`, `DELETE /api/msp/evidence/:id/link` (unlink), `DELETE /api/msp/evidence/:id`, `GET /api/msp/evidence/:id/file`. Gated `requireCapability("ladder.msp-operator")`; `createAuditLog` on create/link/unlink/delete, `auditPrivilegedRead` on list; `logger.child({ channel: "workflow.evidence" })`. Registered in `routes/index.ts`.
  - Verification: `pnpm run typecheck` (api-server) exit 0. Live DB functional test in a rolled-back transaction — multi-target linking across `milestone` + `poam` on one evidence row, duplicate-link no-op (unique index), msp-scoped join returns the row, cross-msp scope returns 0, cascade delete removes links; `ROLLBACK` (nothing persisted).

  **Client (MyArchitect@13c0ece):** `Models/EvidenceItem.cs` (wire model + `EvidenceLinkType` consts), `Services/IEvidenceService.cs` + `EvidenceService.cs` (list/upload-file/create-link/link/unlink/delete/download, AuthToken per request, typed `EvidenceServiceException`), `Controls/EvidencePanel.xaml(.cs)` — the ONE reusable contextual Evidence panel (`Configure(service, linkedType, linkedId, displayName, customerId)` + `LoadAsync`; attach file, add link, view, detach; real empty/loading/error states; theme keys only per UI_RULES §10/§11, no animation). Registered `EvidenceService` in `MainWindow.xaml.cs` (instantiate + AuthToken on sign-in/refresh). `dotnet build -c Release` — 0 warnings, 0 errors (confirmed again after the concurrent-MainWindow rebase).

  **Foundational reuse:** #4345/#4349/#4350 wire this one `_evidenceService` + `EvidencePanel` into their own workspaces (their concurrent builds' scope) — this build deliberately did NOT edit their workspace files.

  Both branches confirmed ancestors of their `origin/main` (`verify-branch-merged.mjs` for platform; `--is-ancestor` for MyArchitect). Both working trees clean.

  **Migration:** appended to the #1630 release-gate checklist (additive; already applied locally, needs Replit/Staging apply at release — not `Shane To-Do`, not a blocker).

  **Pre-existing dirt (NOT mine, left alone):** the canonical `C:\Source\...\MyArchitect-clone` checkout was already mid-merge-conflict on arrival (`UU Controls/HomeDashboardView.xaml.cs`, `UU MainWindow.xaml.cs`, `AA build-journal/4341.md`, plus several `M`/`A` files) from a prior/concurrent session — did not touch it; worked in an isolated worktree `C:\wt\ma-4353` off `origin/main` instead.

  **Findings filed:** none — no new product gaps discovered (the parallel `evidence_attachments` vs `evidence` split is by design per this issue).
