# #4547 — "Type to guide Claude mid-task" box spawns duplicate resumed builds / no delivery confirmation

- **Status:** ✅ DONE 2026-09-17T16:57:37Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** d03818673 (work), 518e8b73c (IN FLIGHT bookend)
- **Findings filed:** #4553 (Build Queue Reply + Resume Session still resume without a liveness check; parented under #3706 because #1202 is at the 100-sub-issue cap)

## Log
- 2026-09-17T16:24:31Z ⏳ IN FLIGHT — investigating the real Send/resume code path behind the Build Watch guide box before changing anything (buildId 3165).
- 2026-09-17T16:57:37Z ✅ DONE — Root cause confirmed, not assumed. Send wrote to live stdin when it could. Otherwise it queued a `--resume` continuation of the same session **without checking whether the original process was still alive**. The 15s idle auto-finalize closes stdin, but a build with a running Monitor task keeps going and starts turns by itself. Real evidence: activity-2026-09-15.log 17:16–17:22 shows #2957 (stdin closed at 16:26:47, still running Monitor turns in its raw transcript), then #2976, then a third launch, all resuming session c4180db4 until Shane stopped them. Adopted builds (`CanSend` in AdoptedReadOnly) and a second Send during a slow launch (fresh worktree 94s) hit the same hole. Shipped in d03818673: `QueueWatcherService.LaunchContinuationAsync` is now the one guarded path. It kills and confirms exit (20s bound, else launches nothing), reuses the row and worktree, has the reap loop hand the killed entry over silently, handles the pickup loop claiming the row first, and respects #4542's hard cap (queues with the message at the cap). A `StdinClosed` flag stops a closed pipe being treated as live. `--replay-user-messages` gives the real receipt, parsed as UserMessageAck. Each message shows Sending, Sent, Received by Claude, Claude is responding, Queued, or Not delivered (text restored). Messages typed during a resume are held and sent to the resumed session. Shane is asked before a working build is stopped. Yes/No quick replies appear while Claude waits for input, or when a finished build's last message asks a question. The Git detail build pane uses the same path. Repeat messages no longer stack "Continue: Continue:" titles. Verification: `dotnet build` clean (0 warnings, 0 errors) before and after rebasing onto #4542. A reflection harness against the built DLL passed 6/6 (real captured replay line → ack; tool_result parsing unchanged; delivery notifications). A real `claude` CLI e2e with a Monitor task showed the process alive 15s after stdin closed and running a 2nd turn by itself, then kill, confirmed exit, and `--resume` got the isReplay receipt on the same session id while the old process stayed dead. NOT verified: the WPF UI itself end to end (per standing rule, the BuildConsole exe is not launched for verification). Shane's first real Send into Build Watch after this lands is the live UI check.
