# #3975 — Test Pad pill never created on some normal launches

- **Status:** ✅ DONE 2026-09-13T15:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** c521a6b8f

## Log
- 2026-09-13T15:20:00Z ⏳ IN FLIGHT — tracing AppMode.IsAgent / App.QuietProtocolCourierLaunch lifecycle before fixing EnsureTestPadPill() startup gate asymmetry
- 2026-09-13T15:55:00Z ✅ DONE — Traced both flags: `AppMode.IsAgent` is decided once in `App.OnStartup` from `--agent`/`--dev`/`BUILDCONSOLE_AGENT=1` and never re-evaluated; `QuietProtocolCourierLaunch = IsAgent && protocolUri != null` (Git #1889). Every `shaneapp://` protocol launch is registered with a literal trailing `--dev` (`setup-shaneapp-protocol.ps1`). A courier cold start (nothing running to forward to) is built off-screen/unactivated and is meant to handle its one URI then `Shutdown(0)` — but for a slow payload (runTest/executeScan) it stays alive and taskbar-visible for real seconds; if Shane notices and clicks it, Windows brings it to real foreground use while `QuietProtocolCourierLaunch` never clears, and `EnsureTestPadPill()`'s only startup call site (`MainWindow.xaml.cs`, gated on the same flags) had already run and skipped it — permanently, since nothing else ever re-ran the gate. Confirmed a bare `--agent`/`--dev` (no protocol payload) launch is a separate, deliberate agent-verification case that must keep the pill absent regardless of activation — left untouched.
  Fix (`desktop/BuildConsole/MainWindow.xaml.cs`): added a `this.Activated` handler in the ctor, scoped to `App.QuietProtocolCourierLaunch` only, that clears the flag and calls `EnsureTestPadPill()` on the window's first real activation. Also guarded the courier branch's post-`await HandleShaneAppUriAsync` `Shutdown(0)` to re-check the (possibly just-cleared) flag first, and replaced the now-reachable stale "dropping pending cold-start URI" log with an accurate self-heal log line for that path.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Per CLAUDE.md (Git #2156) an agent build session must never launch BuildConsole.exe itself to verify UI behavior, so the taskbar-click self-heal scenario could not be live-exercised from this session; the fix is a narrow, code-traceable change to the exact two call sites the issue named, and is honestly reported as build-verified only, not live-clicked.
  `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed `agent/3975-q2659` (c521a6b8) is an ancestor of `origin/main`.
