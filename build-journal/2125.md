# #2125 — Sync docs/buildconsole-contract-pack.md against tonight's real redesign decisions

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** <feature commit filled below>

## Log
- 2026-08-31 ⏳ IN FLIGHT — Auditing real current state of every cited issue (#1787, #1788, #2014-2017, #2035, #2036, and the new-surface list #2059-#2124), reading landed bookends for closed ones, then reconciling docs/buildconsole-contract-pack.md (Surface Inventory table, ASCII shell diagram, Known UI Debt) to reflect real current code state vs. planned-not-yet-built.
- 2026-08-31 ✅ DONE — Documentation-only reconciliation of `docs/buildconsole-contract-pack.md`. No C#/XAML/schema changes.

  **Real audit performed first (GitHub state + landed bookends + live code grep), NOT assumed from titles.** Key correction to the issue body's own premise: it claimed "#2014, #2021 landed — critters removed from cards." That is **false/premature** — #2014 and #2021 are OPEN with no bookend, and the critter/mascot surfaces are **still live in code** (`CritterLoungeControl` docked at `BuildQueuePanel.xaml:218`, per-card mascots at `CreateQueueCardMascot`, `FocusImmersiveView` instantiated in `MainWindow.FocusImmersive.cs`). All eight shell-redesign epics (#1787, #1788, #2014, #2015, #2016, #2017, #2035, #2036) are OPEN and largely unbuilt, so their decisions are documented as **planned, not yet implemented** rather than shipped.

  **Verified LANDED (closed + bookend + code) and newly documented:** #2059/#2065/#2071/#2074 (FloatingChatWindow — new surface row added), #2104/#2105 (Pinned Questions — new surface row + `PinnedQuestionDetectionEnabled` setting), #2062 (ghost blocked cards), #2070 (live-filtered "waiting on #N" — new Known UI Debt item, with #2107 noted as the still-open deeper case), #2066 (`bt_chat_mentioned_issues` mention tracking), #2080/#2081 (hover popup actions/relationships, folded into FloatingChatWindow row), #2119 (`superseded` status + `SupersededById` field), #2122 (Max Concurrent Build Slots settings control). Marked **planned/open:** #2108–#2111 (Dynamic Build Queue Map), #2124 (PinnedQuestionDetectionEnabled UI toggle — flag exists, UI control not built).

  **Sections changed:** §2 Surface Inventory — added a "Redesign status" banner + an ASCII-diagram caption (top-bar re-homing #2017); annotated `ActivityBar` (#2014 planned 12-icon order), `FocusModeBar`/`FocusImmersiveView`/`FocusCharacterLayer`/`CritterLoungeControl`/`SailorDuckMascotLayer`/`SailorDuckWatermarkControl` as ⚠ slated-for-removal-but-still-live; expanded `BuildQueuePanel` (landed #2062/#2070/#2119 + planned map); added `FloatingChatWindow` and Pinned Questions rows. §3.1 added `SupersededById`; §3.3 added mention-tracking note; §3.5 added `PinnedQuestionDetectionEnabled` + Max-Concurrent control note. §4.1 added `superseded` status row. §8 added the #2070/#2107 debt item (renumbered Dead Wiring to 5).

  Verification: this is a docs-only change (`git diff --stat` = only `docs/buildconsole-contract-pack.md`); no build/test needed (BuildConsole is out of test-manifest scope per CLAUDE.md). Every issue-state claim cross-checked against live `gh issue view` output; every "landed" claim cross-checked against the file/symbol in current worktree code. Working tree left clean.
