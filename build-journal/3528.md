# #3528 — Account Security: surface real login history (v2)

- **Status:** ✅ DONE (no-op) 2026-09-10T17:30:00Z
- **Scope:** platform

## Log

- 2026-09-10T17:30:00Z — Session dispatched against #3528 (buildId 2200). Pulled the
  issue body and all comments per standing rules. Found the issue's own comment
  thread already resolved: the work described in the body (widen
  `WireLoginHistoryRow` in `useAccountSecurityLive.ts` to the full
  `LoginHistoryView` shape — `loginMethod`, `browser`, `os`, `revoked` — plus
  tri-state empty handling) was already fulfilled under the original #1603, merged
  as commit `053555bc3` ("Account Security: surface full login-history wire
  contract (#1603)"), which was running independently and landed before this
  dispatch (#3528, which superseded #1603) started. The repo owner's own comment on
  #3528 confirms: "Real, confirmed fulfilled — commit 053555bc3 ... tsc/vite build
  clean. Closing as duplicate-fulfilled, not re-dispatching." Issue is CLOSED.
- Verified `053555bc3` is a real commit present on `main` via `git log`.
- No code changes made this session. `git status --porcelain` clean on arrival and
  at end — nothing to commit.
- No labels flipped (issue was already closed by the owner before this session
  started; owner-driven closes are outside the in-flight/complete label lifecycle
  this session would otherwise drive).
- Posted confirmation comment on #3528: https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3528#issuecomment-5625950939
