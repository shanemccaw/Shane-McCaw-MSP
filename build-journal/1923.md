# #1923 — Status Reports: customer-scoped + subscriber fan-out (corrects #1589)

- **Status:** ✅ DONE 2026-09-06T18:35:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 1edd4f035 (rebased/pushed to main as e2ed8d7a2)

## Log
- 2026-09-06T14:05:00Z ⏳ IN FLIGHT — Investigated #1589/#1704/#1569; confirmed status_reports has no
  customer/tenant reference (clientUserId -> users.id only), send handler gates notify+audit on
  clientUserId, linkPath is dead portal-v2-era route space. Plan: add customerId FK to tenants on
  status_reports; fan status-report-publish notifications out to every customer_user on that
  tenant (gated per-user by existing customer_notification_preferences, category "project", same
  mechanism createNotification already applies); fix linkPath to route through the existing
  #1827 resolvePortalDeepLink raw-path map instead of a dead /portal/projects path; leave the
  sideways permission check unbuilt per the issue's own sequencing note (waits on #1704, which is
  still open) and file+wire a blocked_by follow-up instead of approximating with a role-ladder
  check. Wire-contract only — no artifacts/portal or artifacts/admin-panel UI changes.
- 2026-09-06T18:35:00Z ✅ DONE — Shipped as commit 1edd4f035 (rebased/pushed to main as e2ed8d7a2).
  Added `status_reports.customer_id` FK (tenants.id, additive, nullable, ON DELETE SET NULL) via
  `lib/db/migrations/manual/2026-09-06-status-reports-customer-id-1923.sql`, run against local
  Postgres (0 existing rows locally, so the backfill UPDATE was a real no-op but included for
  correctness). `POST /admin/status-reports/:id/send` now fans out via the new
  `notifyStatusReportPublished()` (notification-center.ts) to every subscribed customer_user on
  the tenant — reused the existing `customer_notification_preferences` opt-out mechanism and the
  already-live "project" category (no new alert-catalog category invented, per the issue's own
  point 4 caution) — and audits unconditionally now instead of silently auditing nothing for a
  null-addressee report. POST/PATCH /admin/status-reports accept/derive `customerId` so the
  existing admin-panel form (untouched — no UI in this build) keeps working. Also fixed, since the
  read side had the identical bug: `GET /portal/retainer` and `GET /portal/projects/:id` (the two
  real customer-facing reads of `status_reports`) now match `customerId` OR `clientUserId`
  membership, so a null-addressee report is actually visible to the customer instead of silently
  invisible — without this the fan-out would have had nothing to point at. Fixed the dead
  `/portal/projects` linkPath (#1827-class) on all three status-report notification call sites to
  route through the existing `resolvePortalDeepLink` raw-path map. Left the sideways permission
  check (point 3) unbuilt — #1704 is still open — and filed/wired **#3043** (`bug`, parented to
  Feature #1569, real `blocked_by` #1704 edge, `blocked` label, board status set to "AI Batter
  Up") rather than approximating with a role-ladder comparison, per the issue's explicit
  sequencing note. Updated `portal-retainer.test.ts` for the new query shape (all 7 cases pass,
  including a rewritten case proving an unclaimed customer with zero linked logins now still gets
  its own customerId-scoped reports instead of an empty array). `lib/db` + `api-server` both
  typecheck clean; `notification-center-suppress-preference-email.test.ts` and
  `admin-active-directory-delete.test.ts` still pass. Verified branch is a real ancestor of `main`
  via `scripts/dev-server/verify-branch-merged.mjs` before writing DONE. Working tree clean.
  No page/route/component touched under `artifacts/portal` or `artifacts/admin-panel` — wire
  contract only, per the issue's explicit scope stop.
