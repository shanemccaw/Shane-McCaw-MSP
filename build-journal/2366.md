# #2366 — Batter Up: enforce real dispatch flow (no direct queue writes)

- **Status:** ✅ DONE 2026-09-03T01:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** d4a0a6e1a (implementation, on `agent/2366-q1206`), rebased onto
  `origin/main` and pushed as `95625fd75` (`main`)

## Log
- 2026-09-03T01:21:49Z ⏳ IN FLIGHT — confirmed #2360 genuinely DONE on origin/main
  (03cc04d85, verified via `git log` + its own bookend). #2360's own bookend already
  states Dispatch is deliberately NOT a queue write — opens the issue in browser +
  copies a ready-to-act prompt to clipboard, `BatterUpDispatchClicked` left as the
  named hook point for this issue. Starting: audit that claim against the real code,
  add a real visible guard/assertion enforcing the boundary, verify Git-refresh pickup
  path.
- 2026-09-03T01:52:00Z ✅ DONE — Audited `BatterUpDispatchClicked`/`BatterUpDispatchAllClicked`
  in `desktop/ShaneBuilder/MainWindow.BatterUpPanel.cs` directly: #2360's Dispatch already
  only opens the issue in the browser (`Process.Start`) and copies a build-prompt to the
  clipboard — no direct `bt_build_queue` write, no synthetic `BUILD:` comment posted. No
  bug to file against #2360.
  Turned that already-correct boundary into a real, runtime-enforced guard instead of one
  that only exists as a doc comment: new `Services/BatterUpDispatchGuard.cs`
  (`AsyncLocal<bool>`-scoped — `Enter()` marks a Dispatch click's whole call tree, including
  anything awaited inside it, as "inside the no-write zone"; `ForbidIfActive` throws
  `InvalidOperationException` if reached from inside that scope, and never leaks into
  unrelated concurrent app work). Wired into the two real write choke points this app has:
  `QueueWriteClient`'s three `bt_build_queue` mutation methods (`CancelRunningAsync` /
  `RequeueAsync` / `ReassignAsync`, added for Build Matrix's #2288 slot actions — the one
  live class in this app that can write the queue directly) and
  `GitMapService.RunGhAsync` (the only path through which that class could reach
  `gh issue comment`). Both `BatterUpDispatchClicked` and `BatterUpDispatchAllClicked` now
  enter the guard scope for their full duration.
  Verified the guard mechanism itself pre-wiring, in an isolated scratch console app (same
  `AsyncLocal` logic, deleted afterward, not part of the commit): throws inside scope
  (including after an `await`), does not throw outside/after the scope, and does not leak
  into an unrelated concurrent `Task`. `dotnet build ShaneBuilder.csproj -c Debug` —
  succeeded, 0 warnings, 0 errors, both before and after rebasing onto `origin/main`.
  Pushed `agent/2366-q1206` → rebased onto `origin/main` (clean, no conflicts) → pushed
  directly to `main` as `95625fd75`. `verify-branch-merged.mjs` confirms merged (exit 0).
  `git status --porcelain` clean. No genuine findings surfaced this build — no new issues
  filed.
