# #4117 — AdminV2 admin-invoices.ts PATCH still edits sent invoices in place with no versioning/reason

- **Status:** ✅ DONE 2026-09-15T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 3eb2981855eb6a6900b6b0ceed3570b53501c485

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — mirroring #4109's sent/unsent versioned-revise model onto AdminV2's `admin-invoices.ts` PATCH route + admin-panel invoice edit UI. Reusing existing `version`/`supersedesInvoiceId`/`revisionReason` columns from `lib/db/migrations/manual/2026-09-14-invoice-versioned-reissue-4109.sql` — no new migration needed.
- 2026-09-15T00:45:00Z ✅ DONE — `PATCH /admin/invoices/:id` now 409s on a sent
  invoice (`existing.status !== "draft"`) instead of editing in place; added
  `POST /admin/invoices/:id/revise` (zod-validated, mandatory `reason`,
  reuses `version`/`supersedesInvoiceId`/`revisionReason`, marks the prior
  row `superseded`, writes `invoice_revised` audit log, uploads the new PDF
  to SharePoint, notifies the customer) — mirrors `routes/msp-invoices.ts`'s
  `.../revise` from #4109. `admin-panel/src/pages/crm/InvoiceDetail.tsx`
  swaps the status dropdown for a "Revise Invoice" dialog (reason + amount +
  description + due date) once an invoice is sent; a draft keeps the direct
  dropdown; a superseded invoice shows a locked note and a link to the
  newer version. `Invoices.tsx` list page gained a `superseded` filter
  option + status color so the list doesn't render it with the "unknown
  status" fallback. Also fixed a real, evidenced bug found while building
  this: `admin-finance.ts`'s `GET /admin/invoices/:id` was returning
  `amount` as raw integer cents instead of the dollar-string wire contract
  every other invoice-reading route already uses — a 100x-inflated display
  bug on every admin invoice detail view, fixed inline since the revise
  dialog's amount prefill directly depended on it being correct.
  No new migration — `lib/db/migrations/manual/2026-09-14-invoice-versioned-reissue-4109.sql`
  already added the columns this reuses (confirmed live via `\d invoices`
  against the local `DATABASE_URL`). Verification: `tsc --noEmit` clean on
  both `artifacts/api-server` and `artifacts/admin-panel` (no eslint binary
  installed locally, so lint wasn't run); a `BEGIN…ROLLBACK` dry run against
  the real local `invoices` table (test row attached to existing client
  user 56, immediately rolled back, no residue left) reproduced the exact
  revise transaction — confirmed the PATCH-gate condition would 409 and the
  revise insert correctly produces `version+1`/`supersedesInvoiceId`/
  `revisionReason`/`status: superseded` on the prior row. Not
  live-HTTP-verified against a running dev server: the api-server instance
  reachable on this machine (port 8080) is the shared dev-server checkout,
  not this worktree's code, and admin-panel/api-server-internals are outside
  this project's mandatory test-manifest scope (scoped to `artifacts/msp-portal/`
  and `artifacts/shane-mccaw-consulting/` only), so no manifest was written.
  Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0); working tree clean.
