# #4100 — runTest Dev routing drops the app mount base

- **Status:** ✅ DONE 2026-09-14T22:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** d5fda7b1e

## Log
- 2026-09-14T21:35:00Z ⏳ IN FLIGHT — implementing Option 1: `DevServiceRouting.OriginForServiceKey`/`OriginForRoute` prepend the real `basePath`.
- 2026-09-14T22:10:00Z ✅ DONE — Added `basePath` to `admin-panel` in `scripts/dev-server/services.json` ("/admin-panel/", matching its always-on vite.config.ts default; `portal` already carried "/portal/"). Threaded a new `BasePath` field through `DevServicesManager.KnownServices` (parsed from services.json). Added `DevServiceRouting.BasePathForServiceKey` and rewrote `OriginForRoute` to return a COMPLETE URL (origin + basePath + route), guarding against double-prefixing a route that already carries its service's basePath. Fixed `UiTestExecutor.ResolveGotoTarget` — it previously treated the resolver's return value as a bare "origin" and re-combined it with the raw target via `Uri.TryCreate(originUri, target, …)`, which for an absolute-path target REPLACES the base URI's own path per RFC 3986 and would have silently stripped the basePath this fix adds; it now uses the resolver's returned URL directly. Also fixed the initial-load `uiTargetUrl` in `MainWindow.xaml.cs` (was bare origin with no basePath — a manifest with no explicit first `goto` would have loaded straight into the 404 page). Verified: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` succeeds, 0 errors/warnings. Traced the fix against all 11 manifest gotos listed in the issue body (billing, account-security*, config-state, email-auth-setup, offboarding, projects, remediation-tracking, team-management, content/email-templates) — each now resolves to its real mounted page (e.g. `/billing` → `http://localhost:5175/portal/billing`, `/content/email-templates` → `http://localhost:5174/admin-panel/content/email-templates`). Confirmed live against the running local dev servers that the bare paths still 404 pre-fix (`curl http://localhost:5175/billing` → 404, `curl http://localhost:5174/content/email-templates` → 404) — this is BuildConsole-internal `goto` resolution logic (WebView2 navigation inside `UiTestExecutor`), not something `curl` alone exercises; the dotnet build + full code-path trace is the real verification available without launching the WPF app itself (build-launched exe verification is explicitly out of scope per repo convention). No fixture/hardcoded data. No findings beyond the issue's own scope discovered.
