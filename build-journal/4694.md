# #4694 — BuildConsole: stop writing in-flight/complete labels; remove broken "Mark Complete" menu item

- **Status:** ✅ DONE 2026-09-18T17:53:38Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** 11fa6667c2fadcd7ffa9e94a9c0f162e87aa72b2 (code); 5a7e15917 (IN FLIGHT bookend); 64986100f (plan, `build-journal/4694-plan.md`)

## Log
- 2026-09-18T17:47:42Z ⏳ IN FLIGHT — #4693 confirmed cleared (`complete`-labeled, DONE bookend + c9fdc60aa on origin/main). Re-verifying call sites, then writing build-journal/4694-plan.md before any code change.
- 2026-09-18T17:53:38Z ✅ DONE — Queue + un-park no longer write `in-flight`/`complete` labels; "Mark Complete (Ready for Review)" menu item, its handler and `BuildTrackerApiClient.ToggleLabelAsync` are deleted. Build 0 warnings / 0 errors. Live click-through / network-absence check NOT done (never-launch-BuildConsole rule) — see Verification Results. Filed issues: none.

## Changes Made

- `Services/BuildQueuePostgresClient.cs` — `QueueBuildAsync`: deleted the fire-and-forget `AddLabelAsync(in-flight)` + `RemoveLabelAsync(complete)` block. `UnparkAsync`: deleted the same pair and the now-pointless `RETURNING github_number` / `num` read (now a plain `ExecuteNonQueryAsync` rows-affected, same `WHERE status = 'parked'` guard and same bool contract). Fixed two doc comments that would now contradict the code (class summary bullet naming `ToggleLabelAsync`; `ParkAsync` "no label sync here, unlike UnparkAsync").
- `Controls/LeftSidebar.xaml.cs` — removed the Git Board context-menu item "✓ Mark Complete (Ready for Review)" / "Remove 'complete' label", its click handler, and its `cm.Items.Add`; replaced the stale explanatory comment with a #4694 note and renumbered the following "// 2." comment.
- `Services/BuildTrackerApiClient.cs` — deleted `ToggleLabelAsync` (POST to `extension/toggle-label`, the route `admin-build-tracker.ts` answers with HTTP 410); its only caller was the removed handler.
- Untouched by design: `GitHubIssuesService.AddLabelAsync/RemoveLabelAsync` (now unreferenced — dead-code removal is #4696), the api-server 410 stub, `blocked` / `Shane To-Do` label handling, and `BuildQueuePanel`'s "✓ Mark Complete (Hide)" (a queue-row status action, unrelated to the GitHub label). No migration, no schema change, no fixture data.

## Verification Results

- `dotnet build desktop/BuildConsole/BuildConsole.csproj`: **Build succeeded, 0 Warning(s), 0 Error(s)**, run after all edits.
- Grep over `desktop artifacts scripts lib` (.cs/.xaml/.ts/.mjs): **zero** references to `ToggleLabelAsync` / `toggle-label`. `AddLabelAsync(` / `RemoveLabelAsync(` now appear only as the two definitions in `GitHubIssuesService.cs` — no call sites. Remaining `"complete"` string literals in BuildConsole are unrelated (BuildMap chain state, deploy-pipeline stage names, a log message).
- Baseline evidence that the removed calls were real GitHub traffic: today's/yesterday's `%AppData%BuildConsolelogs` contain `gh issue edit --add-label/--remove-label failed for #4523/#4549/#4550/#4551/#4605 …` lines (TLS handshake timeout; rate-limit circuit open, Git #2815) — the label writes were also feeding the rate-limit pressure.
- `verify-branch-merged.mjs`: OK — `agent/4694-q3294` (11fa6667) is merged into `origin/main`. `git status --porcelain` clean.
- **Not verified:** a live run of the rebuilt app showing zero `gh issue edit` label calls on queue/un-park, and a visual confirmation the menu item is gone. Per the never-launch-BuildConsole rule (Git #2156) I did not launch the exe or drive its UI; the running instance is still the pre-change build, so its log cannot show absence yet. Shane: after the next BuildConsole restart, queue + un-park one row and confirm no `[github] gh issue edit --add-label` line appears in the ActivityLog, and that right-clicking a Git Board issue no longer lists Mark Complete.
