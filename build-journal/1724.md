# #1724 — Policy Decisions + Policy Engine: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-09T21:20:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** ea4192eb0 (real wiring), c8a1416ea (manifest verified-commit stamp)

## Log
- 2026-09-09T20:31:00Z ⏳ IN FLIGHT — Wiring `artifacts/portal`'s Policy Decisions page against
  `docs/policy-decisions-contract-pack.md` (#1722) and
  `Design/portal/design_handoff_full_site/screens/Policy Decisions.dc.html` (#1723, confirmed
  current against the pack). Own-table register (`GET/POST /api/portal/policy-register`, `PATCH
  .../clearance/resolve`) plus the obligations catalogue (`GET /api/portal/compliance-obligations`).
  Risk-derived positions stay on Risk Register's own `PolicyDecisionsCard` (already wired) — §9's
  pick-one answered by the design as this page holding the own-table register only.
- 2026-09-09T21:20:00Z ✅ DONE — New `/policy-decisions` page in `artifacts/portal` (types, API
  hooks, visuals, 3 components, page), wired to all 3 real routes this module owns. `moduleNav.ts`
  `builtPath` flipped from `null`; Risk Register's ledger link to the standing register no longer
  routes through coming-soon. Extended `portal-compliance-obligations.ts`'s `WireObligation` with
  `id`/`type` (both already selected/joinable server-side, just not previously on the wire) so the
  create form can cite a real catalog row as `obligationId` (#1525) and draw the AUTHORITY badge —
  additive route change, no schema change. New test manifest
  `test-manifests/portal/policy-decisions.json`, registered in `_regression-suite.json`, run via
  `shaneapp://runTest` against the live local dev server after a real api-server rebuild+restart
  (`refresh-main-server.mjs`, since the buildset restart alone left the API server on a stale
  `dist/index.mjs` — a real gap, not filed separately since it self-resolved with the standing
  tool): **19/19 steps passed**, all real (real login, real 0-row policy-register live-empty state,
  real 8-row compliance-obligations catalogue reaching the DOM under its own testid, real 400/404
  guards, the create form's obligation picker populated from the real catalogue). `pnpm run
  typecheck` clean on both `artifacts/portal` and `artifacts/api-server`. No fixture module, no
  hardcoded data — every row on screen comes from a real endpoint.
  `verify-branch-merged.mjs`: OK, merged into `origin/main` (4d3c348e). `git status --porcelain`:
  clean. No new GitHub findings filed — no genuine gap surfaced beyond what
  `docs/policy-decisions-contract-pack.md` and the design's own known-limits ledger already
  document and cite (carried into the page's own ledger).
