# #4298 — MyArchitect: Script Library — select a CR, auto-fill RequiredVariables from its real data

- **Status:** ✅ DONE 2026-09-16T01:47:45Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/MyArchitect@4045c09

## Log
- 2026-09-16T01:17:39Z ⏳ IN FLIGHT — blockers #4278/#4284/#4291/#4297 confirmed CLOSED; starting investigation of #4297's CR field-matching logic and #4291's Script Library variable auto-fill layer in shanemccaw/MyArchitect, to factor the matching logic into a shared service reused by both.
- 2026-09-16T01:47:45Z ✅ DONE — shanemccaw/MyArchitect@4045c09 (pushed straight to main, merge-base verified ancestor). Factored #4297's `PortalFormScanner.SuggestFromChangeRequest` matching logic out into a new shared `Services/ChangeRequestFieldMatcher` (`MatchLabel` = #4297's original label heuristic, unchanged behavior; `MatchVariable` = new, for Script Library's `RequiredVariables` — matches by exact key against the CR's real `proposedPayload` first, added `ChangeRequest.ProposedPayload` to the C# model to carry it, then falls back to the same known-column heuristic). `OpenScriptLibraryRecord` now takes an optional `selectedCr`; when at least one `RequiredVariables` entry has no `TenantVariableResolver` mapping at all, a real "Select Change Request…" action opens a CR picker (scoped to the action's `CatalogItemId`, falling back to the tenant's full CR list) built from the real `GET /api/msp/change-requests` data via `IChangeControlService.GetChangeRequestsAsync`. A CR-matched value shows a real `SourceLabel` provenance line (extended `WorkspaceEdit` with `SourceLabel`/`NeedsReadback`); a genuinely unmatched variable falls back to manual entry + the same live letter-spaced readback #4297 built for its own fallback case (`RecordWorkspacePanel.BuildEdit`), replacing the technical-term suggestion box for that case. Also wired a CR record's existing "Open in Script Library" jump (`OpenCatalogItemForChangeRequestAsync`) to pass its source CR straight through, so that jump now arrives pre-filled instead of dropping the CR context it already had. `dotnet build` succeeds, 0 warnings/0 errors. No test project exists for this WPF app (no test-manifest requirement — CLAUDE.md's manifest rule scopes to `artifacts/portal`/`artifacts/shane-mccaw-consulting` only). Working tree clean in both worktrees (`C:\wt\4298-q2969` and `C:\wt\myarchitect-4298`).
  - Real finding filed mid-build (not part of this issue's own scope): [#4326](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4326) — UI_RULES.md §10 claims the #4310 FontSize/FontWeight retrofit is "genuinely fully in force across the whole codebase," but its own verification grep only covered `.xaml` files; 77 raw `FontSize`/`FontWeight`/`FontFamily` literals remain across 7 `.xaml.cs` code-behind files (confirmed live on main@4045c09). Parented as a sibling sub-issue of #3454, labeled `bug`, board status "AI Batter Up".
