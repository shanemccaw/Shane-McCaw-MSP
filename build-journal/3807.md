# #3807 — Build Sets toggle hangs app ~5s on first open

- **Status:** ✅ DONE 2026-09-12T20:50:55Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** d0aee7bd3

## Log
- 2026-09-12T20:35:17Z ⏳ IN FLIGHT — profiling the Build Sets toggle first-open hang before patching (hypothesis: non-virtualized BuildSetRollupList first layout pass, unconfirmed)
- 2026-09-12T20:50:55Z ✅ DONE — shipped d0aee7bd3. **Measured cause, which is NOT the issue's hypothesis:** opening Build Sets (#3785, in Shane's running build v1.0.1205, which predates #3805) squeezed the Queue content column from 300px to 60px. Re-measuring the real queue cards at 60px (text wrapping per character, cards ~19,700px tall) took ~3.3s, against ~0.1s at 260px, so each open took **4.2–7.1s** with a Running & Queued view (7.6–9.9s with Done) and each close 0.5–1.1s, on every open, not just the first. The non-virtualized BuildSetRollupList was measured and ruled out: ~0.1–0.4s cold first layout, ~1–13ms warm, 41 sets / 493 visuals. Virtualization was NOT applied. **Fix:** QueueContentColumn.MinWidth is pinned to its pre-open width while Build Sets is open, so opening can never force that reflow even when the host doesn't grow. The toggle now logs handler and click-to-idle ms to ActivityLog (channel build-queue.build-sets-panel). **Verification:** isolated WPF harness (separate exe hosting BuildQueuePanel against the real bt_build_queue, 2,306 rows; Shane's live app never touched), Debug build, loaded machine, host NOT growing (the worst case): open 207ms cold / 2–9ms warm (Done view: 486ms cold / 16–25ms warm), close 6–21ms. dotnet build: 0 warnings, 0 errors. **Not self-verified:** a live click in the real app, and the #3805 center-column shrink in MainWindow (the new ActivityLog line will record real numbers on Shane's next click). **Finding filed:** #3834 (rollup full rebuild on each queue change, 0.2–1.0s UI-thread hitch while open). No pre-existing dirty files.
