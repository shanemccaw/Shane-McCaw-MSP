# #3480 — Feature: Break-Glass Access (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:30:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 3a346d04b (feature) — merged into origin/main; bookends 935e330b7 (IN FLIGHT) + this DONE

## Log
- 2026-09-10T06:12:12Z ⏳ IN FLIGHT — pulled #3480 body + both `BUILD:` comments per
  standing rules, read `desktop/MyArchitect/GEMINI.md` and `desktop/MyArchitect/UI_RULES.md`.
  Comment history:
  1. `BUILD:` dispatch (buildSet `MyArchitect`) carrying `--blocked-by 3493` explicitly,
     instructing to read GEMINI.md / BUILD_QUEUE_METHOD.md / UI_RULES.md.
  2. later `BUILD:` dispatch (buildSet `BatterUpClearOut`) that omits the `--blocked-by`
     flag but does not state #3493 cleared — only reiterates the standing "pull body +
     comments, build per real stated scope" instructions.
- 2026-09-10T06:13:00Z 🛑 BLOCKED — on #3493 (Feature: UI Shell Redesign), same as
  siblings #3463 / #3464 / #3474. Findings:
  - **All of #3480's scope is client UI.** Its checklist is three `Client:` items —
    pending break-glass list (cross-tenant + per-tenant), admin-override action, audit
    trail view. There is no backend to build: the endpoints it names are real and
    already present (`artifacts/api-server/src/routes/msp-break-glass.ts` —
    `GET /msp/break-glass`, `GET/POST /msp/customers/:customerId/break-glass`,
    `POST .../break-glass/:pendingSecretId/admin-override`, `GET .../break-glass/audit`).
    Unlike #3459/#3474 there is no non-UI service layer or backend fix left to land here.
  - **Break-Glass attaches into the shell's Admin fixed tab.** UI_RULES.md §2 places
    "Break-Glass (#3480)" explicitly on the **Admin** fixed Ribbon tab, with the list/
    override/audit surfaces realised through the right-panel full record workspace (§3:
    `facts` / `edits` / confirm-armed `actions` / `list`). None of that chrome exists
    yet — the shell redesign is genuinely unbuilt.
  - **Confirmed #3493 not started:** `desktop/MyArchitect/MyArchitect.csproj` has no
    `Fluent.Ribbon` package reference; `MainWindow.xaml` has zero `RibbonTab`/`<Fluent:`
    elements (still the pre-redesign chrome); `build-journal/3493.md` does not exist and
    #3493 is open + `in-flight` (not `complete`).
  - **GEMINI.md standing rule** is explicit: UI Shell first, no exceptions — "If #3493
    isn't done yet, do not build ad-hoc chrome (a new icon, a new panel) to unblock
    yourself — stop and flag it instead." Building Break-Glass chrome now would also
    conflict with a decision recorded on the epic, so the CLAUDE.md "BUILD IT" rule does
    not override (that rule is about a missing *backend*; the backend here exists).
  - **Gate (Git #1987):** the real `blocked_by` edge #3480 → #3493 already exists and is
    valid (#3493 open/not-complete), so it was left in place, not re-created. Applied the
    `blocked` label, removed `in-flight`, and left a plain-language comment on #3480.
  - **Unblock path:** once #3493 lands (Admin tab + right-panel record workspace model),
    wire the three Break-Glass surfaces into the Admin tab against the existing endpoints.
- 2026-09-10T20:11:32Z ⏳ IN FLIGHT (resuming) — checked the `blocked_by` edge: it points at
  #3493, which is now **closed + `complete`** (`build-journal/3493.md` DONE, commit e8460c5fd on
  origin/main). So #3480 is genuinely unblocked. Removed the `blocked` label, restored `in-flight`,
  and deleted the now-stale `blocked_by` edge #3480 → #3493 (Git #1987 — a closed blocker must not
  keep gating). Resuming per the unblock path above: the shell (Fluent.Ribbon Admin fixed tab +
  right-panel record workspace, `ShellRegistry`/`RecordWorkspaceSpec`) is real, and the four
  break-glass endpoints are real and mounted (`router.use(mspBreakGlassRouter)` in
  `artifacts/api-server/src/routes/index.ts`, under `/api`). Building: a real
  `BreakGlassService` HTTP client (same pattern as `ChangeControlService`), models, and Admin-tab
  wiring for the cross-tenant pending list → detail (attempts) → confirm-armed admin-override →
  per-customer override audit trail — all through the existing shell contract, no new chrome.
- 2026-09-10T20:30:00Z ✅ DONE — commit **3a346d04b** (rebased onto + pushed to origin/main;
  `scripts/dev-server/verify-branch-merged.mjs` → exit 0, "Safe to write DONE"). Clean
  `dotnet build MyArchitect.csproj` (0 warnings, 0 errors).

  **What shipped (all three `Client:` checklist items, real endpoints, no fixture):**
  - `Models/BreakGlassModels.cs` — wire DTOs for all four `msp-break-glass.ts` responses.
  - `Services/IBreakGlassService.cs` + `Services/BreakGlassService.cs` — real HTTP client (same
    shape/conventions as `ChangeControlService`: Web-defaults JSON, bearer `AuthToken` fanned out
    from the real session in `ApplyAuthState`, typed `BreakGlassServiceException` carrying status +
    parsed `{error,detail,blockedBy}`). Base URL `API_BASE_URL` ?? `http://localhost:8080`.
  - `MainWindow.RegisterAdminTab` — new "Break-Glass" ribbon group ("Pending Requests", Open-intent,
    fixed-tab legal) on the Admin fixed tab (UI_RULES.md §2), plus a `@`-palette destination.
  - **Pending list (cross-tenant):** `GET /api/msp/break-glass` → full-panel record workspace list.
  - **Per-secret detail:** `GET .../break-glass/:pendingSecretId` → facts + verification-attempts
    list; opens a contextual "Break-Glass" tab with the shell's Back-group splice.
  - **Admin-override action:** confirm-armed (Danger) `POST .../admin-override` with a required
    write-through `reason` edit + optional comma-separated reissue `emails`; result surfaced honestly
    (new secret id / reissued / sent on success; the server's own 409/5xx refusal message on failure).
  - **Audit trail view:** `GET .../break-glass/audit` per customer → workspace list.
  - **Per-tenant list (history):** `GET .../customers/:id/break-glass` (any status) → workspace list,
    rows drill back into the detail record.
  - Every drill-down is scoped by the **numeric customerId the cross-tenant list itself returns**, so
    none of it depends on the fixture `TenantService` (the known #3540 gap) — no fake ids, no fake rows.

  **Verification:** clean `dotnet build` only. Per CLAUDE.md, test manifests are scoped to
  `artifacts/msp-portal/` + `artifacts/shane-mccaw-consulting/`; `desktop/MyArchitect` (WPF) is
  explicitly out of scope, so a clean build is the correct verification for this desktop app.

  **Findings filed:** none new — the backend was already real and mounted, and the only gap touched
  (fixture `TenantService` / no real numeric customerId) is already tracked as #3540 and is routed
  around here rather than depended on.

  **Notes for Shane:** the four routes require the `ladder.msp-operator` capability; a signed-in
  operator session (post-#3501) is needed for real data — without it the workspace states the honest
  401/403 reason rather than showing anything fabricated.
