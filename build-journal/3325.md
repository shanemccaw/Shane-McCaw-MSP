# #3325 — Today "Trip" arrival-confirmation card not built -- heading_to state exists but unsurfaced

- **Status:** ✅ DONE 2026-09-09T13:53:00Z
- **Scope:** shanes-life (personal project, web/shanes-life/ -- not platform/buildconsole)
- **Started:** 2026-09-09
- **Commit(s):** 32b9a3b28

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — real gap from #3320: `heading_to`/`heading_to_at`
  (location-state.mjs) is set and read internally but never surfaced as its own Today card.
  Building the real `trip` moment: computeLaterMoments reads locationState.getHeadingTo, a new
  Today act row (tripActRow, same pattern as the existing timerActRow) renders destination-
  specific Confirm ("At work"/"I'm home"/"I'm here") + Cancel, both backed by real new
  POST /api/trip/confirm and /api/trip/cancel routes that clear heading_to.
- 2026-09-09T13:53:00Z ✅ DONE — commit 32b9a3b28, merged into main (verified
  `git merge-base --is-ancestor HEAD origin/main`). Added `locationState.clearHeadingTo`
  (idempotent), extended `computeLaterMoments` with a real `trip: { house, at, line }` field
  (12h staleness window, same as `heading_to_place`/`GET /api/capture-tray`; `line` reuses the
  real per-destination copy that rule's own confirmation message already computes), new
  session-gated `POST /api/trip/confirm` / `/api/trip/cancel` routes, and a client `tripActRow`
  (public/app.js) reusing the existing `timerActRow` act-row pattern -- icon, title, real line,
  destination-specific confirm + Cancel buttons -- wired into Today's "Next" section. README
  updated (new "Trip act row (#3325)" section; the old "Not built here" scope-cut note in the
  #3320 section now points at it).
  Real verification: `node --check` on all three touched files; a disposable-user DB script
  (created via `createUser`, deleted after) exercised `setHeadingTo`/`getHeadingTo`/
  `clearHeadingTo` and `computeLaterMoments`'s new `trip` field against the real local `finances`
  Postgres for all three destinations (work/rental/home), idempotent double-clear, and the 12h
  staleness cutoff -- 12/12 real checks passed, script deleted after (not part of the
  deliverable). Booted `node src/server.mjs` against the same real DB and confirmed
  `POST /api/trip/confirm`, `/api/trip/cancel`, and `GET /api/today` are all real, session-gated
  routes (401 unauthenticated, not 404) with a clean boot log ("migrate up to date, 60 already
  applied"). Did not exercise the full browser/WebAuthn login path -- same real limitation
  `bin/check.mjs`'s own header already states for this app.
  Filed #3328 (Heading Out list has no "run complete" reset action of its own -- the design's
  own trip-confirm reset behavior for the Rental destination that this build deliberately did
  not replicate, since #3325's own scope was "clear/update heading_to on confirm," not resetting
  a separate checklist), parented under #3220 (Feature: App Foundation & Infrastructure), board
  status set to AI Batter Up.
  `git status --porcelain` clean at completion; `.env` and the disposable verification script
  copied/created for local server testing were both deleted before the final commit -- neither
  is part of the diff.
