# #2726 — MSP scope-creep evaluate: portfolio-aggregate score fires per-customer violations, defaults customerId to mspId

- **Status:** ✅ DONE 2026-09-04T04:22:24Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** f49524294 — "Fix MSP scope-creep evaluate cross-customer scoping bug (#2726)"

## Log
- 2026-09-04T04:05:31Z ⏳ IN FLIGHT — investigating `POST /api/msp/scope-creep/evaluate` (`artifacts/api-server/src/routes/msp-scope-creep.ts`) and `scope-creep-engine.ts`; a per-customer engine runner (`runScopeCreepEngineForTenant`) already exists and is used elsewhere (dashboard resolvers, engine-registry, portal-customer-engines) — the fix is to wire the MSP evaluate route onto it and require `customerId`, instead of scoring/firing off the MSP-portfolio aggregate.
- 2026-09-04T04:22:24Z ✅ DONE — `POST /api/msp/scope-creep/evaluate` now 400s without `customerId`, verifies the customer belongs to the calling MSP (`tenants.msp_id`) and isn't blocked by staff scoping, then runs `runScopeCreepEngineForTenant(customerId)` and fires any violation with that real `customerId` — never `mspId` as a fallback, never the whole-portfolio aggregate score. `runScopeCreepEngineForMsp` import dropped from this route (still used by `admin-scope-creep.ts`, untouched). Local DB has 0 rows in `scope_creep_detections`/`scope_creep_violations` (confirmed via direct psql), matching the issue's own note — this was a code-path fix, not a live-data repro. Verification: `tsc --noEmit` clean for both touched files; `npx vitest run msp-sla-scope-creep` — 26/26 pass (23 pre-existing + 3 new covering the fix: 400 on missing customerId, 404 on cross-MSP customerId, and the fired violation's customerId matches the explicit customer, never mspId). Fixing the tests surfaced a second, unrelated pre-existing test-mock gap (`logger.child`, drizzle-orm `eq`, missing table stubs) that was silently failing 19/23 of this file's own tests before this session — filed as #2814 (parented under #2572, board status "AI Batter Up"), fixed in the same commit since the new tests needed working mocks anyway. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
