# #2509 — services.fulfillmentType misclassifies an add-on as a platform subscription tier

- **Status:** ✅ DONE 2026-09-03T04:35:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 24b5c79c4 (fix + migration), 060c8b445 (IN FLIGHT bookend) — both on `main`

## Log
- 2026-09-03T04:05:00Z ⏳ IN FLIGHT — Confirmed row 131 (`launch-control-plus-addon`) has
  `fulfillment_type = "msp_monthly_subscription"` in local dev DB, matching the issue body.
  Local dev `msp_subscriptions` has one row for `service_id=131` (msp_id 1626,
  `contact_email = regression-testbed@example.com`, Stripe ids
  `sub_regression_testbed`/`cus_regression_testbed`/`price_regression_testbed`) — a synthetic
  regression-test fixture, not a real customer purchase. About to: (1) correct row 131's
  `fulfillmentType` to `"standard"` via a manual SQL migration, (2) harden
  `GET /api/msp/signup/tiers` and `POST /api/msp/signup/start` in
  `artifacts/api-server/src/routes/msp-signup.ts` to also require `tier IS NOT NULL` /
  exclude rows carrying `addOnType`/`grantsCapabilityKey` so a future add-on row can't
  leak the same way.
- 2026-09-03T04:35:00Z ✅ DONE — Ran
  `lib/db/migrations/manual/2026-09-03-fix-launch-control-addon-fulfillment-type-2509.sql`
  against local Postgres: `services.id=131` `fulfillment_type` `msp_monthly_subscription` →
  `standard`. Checked `tier IS NOT NULL` (the issue's other suggested defensive option)
  against real data first — **not viable**: the three genuine seeded tiers
  (msp-platform-free/-growth/-pro) all have a NULL `tier` column too, so that predicate
  would 400 them. Implemented the other option instead: `isGenuinePlatformTier()` in
  `msp-signup.ts`, excluding any row whose `typeAttributes` carries `addOnType` /
  `grantsCapabilityKey`, applied to both `GET /api/msp/signup/tiers` and
  `POST /api/msp/signup/start`'s acceptance check (the POST route had the identical gap —
  same two columns, same missing guard — so it's fixed too, not just the GET named in the
  issue). Verified via direct SQL + a standalone JS filter simulation (no drizzle-orm
  needed, see below): base WHERE clause now returns only the 3 real tiers; the defensive
  filter independently would also have excluded row 131 on its pre-fix `typeAttributes`
  shape, and doesn't false-positive on any real tier. `tsc --noEmit` for
  `artifacts/api-server` could not run clean — pre-existing, already-tracked pnpm-store
  corruption (empty `.pnpm` package dirs for `drizzle-orm`/`stripe`/`pg`/etc., confirmed via
  `fsutil`/`Get-Item` reparse-point inspection, not a worktree-link issue —
  `store-doctor.mjs --json` shows 0 foreign/dangling links) blocks module resolution
  repo-wide; already filed as #2515/#2503/#2502/#2501/#2192/#2193/#2508/#2516 — not
  re-filed. Local dev server is also down for the same reason
  (`scripts/dev-server/status.mjs` shows the server pid dead on :8080), so the endpoint
  could not be live-curled either; substituted direct-SQL + logic verification, which is
  conclusive for this change (pure data + a null-check filter, no new runtime dependency).
  Production-purchase concern from the dispatch: local dev DB shows no real customer
  purchase of this add-on via self-service signup (only the synthetic regression-test row
  noted above); verifying prod is out of this session's scope per the production-access
  hard rule — flagging for Shane to check prod `msp_subscriptions`/Stripe for any real
  `service_id=131` subscription created before this fix, if that matters to him.
  Branch `agent/2509-q1361` rebased onto `origin/main` and pushed directly to `main`
  (`24b5c79c4`, fast-forward, no conflicts); `verify-branch-merged.mjs` confirms ancestor.
  `git status --porcelain` clean.
