# #3048 — Declare Policy.ReadWrite.SecurityDefaults in write app requiredResourceAccess manifests

- **Status:** ✅ DONE 2026-09-07T15:42:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 9ca6c4a81 (rebased/pushed to main as 0bd961ee2)

## Log
- 2026-09-07T15:20:00Z ⏳ IN FLIGHT — pulled #3048 body + comment. Confirmed the actual
  code-side declaration (`graph-write-permissions.ts` rule for
  `PATCH /policies/identitySecurityDefaultsEnforcementPolicy`) already carries
  `Policy.ReadWrite.SecurityDefaults` from #2846 — `DERIVED_WRITE_APP_PERMISSIONS` is
  already 25, matching the issue body. Confirmed via
  `check-dev-app-registration-drift.mjs` (live, read-only) that neither write app's
  live `requiredResourceAccess` declares it yet (dev app 9f6f4772 holds 17
  assignments, `1c6e93a6-28e2-4cbb-9f64-1a46a821124d` not among them) — matches the
  issue's own finding. Real Entra declare+consent is explicitly out of scope for this
  dispatch. Refreshing `scripts/azure/app-registration-baselines/mtapp-write-prod-required-resource-access.json`
  (the tracked baseline `check-dev-app-registration-drift.mjs` / `resolve-baseline-permissions.mjs`
  compare against) to the full current `DERIVED_WRITE_APP_PERMISSIONS` set (25 Graph
  app roles), resolved to real appRole GUIDs via `az ad sp show` against the Graph
  resource SP (read-only). This also fixes a pre-existing staleness in that file (it
  only had 4 of 24 permissions since #1812, predating #1875's 16-permission
  expansion) — fixed in this same build, so not filed separately (HARD RULE: "a gap
  you fixed in this build" is not a finding).
- 2026-09-07T15:42:00Z ✅ DONE — commit `9ca6c4a81` (pushed to `origin/main` as
  `0bd961ee2`, rebased due to concurrent main churn). Refreshed
  `mtapp-write-prod-required-resource-access.json` from a live, read-only resolve of
  `DERIVED_WRITE_APP_PERMISSIONS` against Microsoft Graph's real appRoles. Confirmed
  `Policy.ReadWrite.SecurityDefaults` resolves to `1c6e93a6-28e2-4cbb-9f64-1a46a821124d`,
  matching the issue body exactly. `graph-write-permissions.test.ts`: 87/87 passing
  (JSON-only change, TS module untouched). `check-dev-app-registration-drift.mjs`
  re-run after the fix now correctly reports the DEV write app as missing 8
  permissions (including SecurityDefaults) instead of falsely flagging 13
  already-granted permissions as "extra" drift. **Not done, deliberately, per the
  dispatch's own scope**: the real Entra `requiredResourceAccess` declare + admin
  consent on either write app registration (DEV `9f6f4772…` or PROD `3308b280…`) —
  that stays a separate follow-up/Shane action per #3048's own framing. Branch
  verified merged into `origin/main` (`verify-branch-merged.mjs`); working tree
  clean. No new findings filed — the one gap discovered (stale baseline) was fixed
  in this same build.
