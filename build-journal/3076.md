# #3076 — Projects: real audit of check-to-remediation-content coverage

- **Status:** ✅ DONE 2026-09-07T15:47:00Z
- **Scope:** platform

## Log
- 2026-09-07T15:31:46Z — Session start. Read-only DB audit per #3076 body + superseding comment (expanded scope: propose real domain→service/project mapping, not just coverage counts). No schema/code changes planned.
- 2026-09-07T15:47:00Z — ✅ DONE. Real live-DB queries against local Postgres (`shanemccawmsp`):
  - `monitor_checks`: 204 total (202 active). `remediation_knowledge_base`: 153 rows, all `status='published'` (0 `draft`). 51 checks have no RKB row at all — real coverage 153/204 = 75.0%.
  - Gap concentrated in `compliance` (20/27 uncovered) and `policy` (18/18 uncovered, zero coverage); `directory` also fully uncovered (6/6).
  - Confirmed the issue's premise: `workflow_templates` has 0 rows, no `services` row has `workflow_template_id` set.
  - Found `services.category='project'` already marks 37 real named/priced project rows (27 implementation-project rows ids 34–60 relevant to the mapping; 10 others — 6 onboarding-sequence phases + 4 Copilot seat-tier subscriptions — excluded as a different shape).
  - Posted the full report (coverage table, 20-domain gap breakdown, proposed domain→project mapping with multi-project flags and true gaps) as a comment on #3076: https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3076#issuecomment-5572885341
  - No schema change, no seed data, no `workflow_templates`/steps/tasks rows created — purely read-only per the issue's own scope. Real next step (mapping decision, `checkKey` addition, template/step creation) is Shane's call per his own comment.
  - Commit: (this file's own commit, see git log).
