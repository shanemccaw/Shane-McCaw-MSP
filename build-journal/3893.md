# #3893 — Testimonials: regenerate contract pack from finished backend (phase 2 of 4)

- **Status:** ✅ DONE 2026-09-14T12:52:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ae5d6e949 (IN FLIGHT bookend), d92128b05 (contract pack)

## Log
- 2026-09-14T12:39:28Z ⏳ IN FLIGHT — read #3893's own body/comments, #3436's full decision
  thread (Admin-approval-gated discount, next-month credit, settled 2026-09-14), and #3891/
  #3892's finished backend (both closed, merged: `2389eff7a`, `df870901b`). Read the real
  code — `portal-testimonials.ts`, `admin-testimonials.ts`, the `customer_testimonials`
  schema/migration — and confirmed via grep that none of the four real endpoints
  (`POST/GET /api/portal/testimonials`, `GET /api/portal/testimonials/prompt-due`,
  `GET /api/admin/testimonials/all`) has a live frontend consumer. About to write the
  contract pack at `Design/portal/design_handoff_full_site/docs/testimonials-contract-pack.md`
  and file the orphaned-endpoint findings as sub-issues of #3436.
- 2026-09-14T12:52:00Z ✅ DONE — wrote
  `Design/portal/design_handoff_full_site/docs/testimonials-contract-pack.md`, extracting
  the real wire contract for all four backend endpoints from #3891/#3892 (request/response
  shapes, the real 30-day/90-day cadence anchored on `tenants.createdAt`, the
  `project_closures`/`customer_testimonials` tagged-union merge on the admin read), and
  reflecting #3436's settled discount decision (approval-gated, next-month credit, not yet
  built). Filed two real findings as sub-issues of #3436 (not the Epic), both labeled `bug`
  and set to AI Batter Up board status: #4031 (admin CRM Testimonials page never repointed
  at the merged `/admin/testimonials/all`) and #4032 (no schema/endpoint exists yet for the
  approval→discount mechanism — a real architecture gap, not a missing column, correctly
  left unbuilt). Verified the orphaned-endpoint claim live via
  `grep -rn "portal/testimonials\|admin/testimonials/all" artifacts/portal/src
  artifacts/msp-website artifacts/shane-mccaw-consulting artifacts/admin-panel/src` — zero
  matches, confirmed. Read-only build — no product code/schema/UI touched. Branch verified
  an ancestor of `origin/main` (`verify-branch-merged.mjs`, commit `d92128b05`); working
  tree clean.
