# #3853 — CC Index Phase 1b: bt_issue_mirror's real GitHub sync must mirror issue body text

- **Status:** ✅ DONE 2026-09-12T22:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** no code changed — investigation + live DB verification only

## Log
- 2026-09-12T22:09:06Z ⏳ IN FLIGHT — blocked_by edge to #3852 confirmed clear (#3852 carries the
  `complete` label, code merged to `main`; per CLAUDE.md a `complete`-labeled blocker counts as
  unblocked even while GitHub keeps the issue open pending Shane's own review/close). Investigating
  the real sync job (`desktop/BuildConsole/Services/GitHubIssueMirror.cs`) named in the issue body.
- 2026-09-12T22:15:00Z ✅ DONE — **no code changed; the real ask was already fulfilled before this
  session started, by an unrelated prior feature.** Read `GitHubIssueMirror.cs` in full: `bt_issue_mirror`
  already has a real `body TEXT NOT NULL DEFAULT ''` column, and it is already populated with the real
  GitHub issue body — added by Git #3358 (the Git Board tree feature, an unrelated migration that landed
  *after* #3853's own issue body was written, which is why #3853's premise ("body is never pulled or
  stored") was accurate when written but stale by the time #3852 landed). #3852's own migration file
  (`lib/db/migrations/manual/2026-09-12-cc-index-search-vectors-3852.sql`) already notes this explicitly:
  "bt_issue_mirror.body already exists — added by a sibling migration landed after this issue's body was
  written; not re-added here." Both the full sync (`SyncAsync`, line ~1306) and the closed-issue backfill
  (`UpsertClosedIssuesAsync`, line ~1727) write real `issue.Body` from the GraphQL-fetched `GitBoardIssue`
  into the `body` column on every pass; the cheap incremental pass (`IncrementalSyncAsync`) deliberately
  omits `body` from its own INSERT/UPDATE (same as several other Git-Board-tree fields — milestone,
  parent, sub-issue counts — already do), preserving the existing value until the next full walk. That is
  the SAME pre-existing, Shane-accepted ≤`FullSyncInterval` (30m) staleness tradeoff already documented and
  applied to those other fields, not a new gap introduced here — not filed as a finding.
  **Live verification against `BUILD_DATABASE_URL` (BuildConsole's own DB, where `bt_issue_mirror` actually
  lives — moved off the product DB in #3651/#3653):**
  - 3,803 of 3,840 real mirrored issues already carry non-empty `body` (the rest are issues with a
    genuinely empty real GitHub body); all 3,840 have a populated `search_vector`.
  - A real `plainto_tsquery('english', 'search_vector combines title weight')` search — a phrase that only
    exists in #3852's real body, not its title — correctly returned #3852.
  - `EXPLAIN` on a realistic search confirms `Bitmap Index Scan on bt_issue_mirror_search_vector_idx` (the
    real GIN index from #3852), not a sequential scan.
  - Live trigger test: appended a sentinel phrase to #3852's real `body` via `UPDATE`, confirmed
    `search_vector` recomputed and the sentinel was immediately findable via `plainto_tsquery`, then
    reverted the row to its exact original `body` (byte-for-byte `diff`-confirmed) — the trigger
    (`bt_issue_mirror_search_vector_trigger`, `BEFORE INSERT OR UPDATE OF title, body`) genuinely fires on
    a real body change, not just at migration-backfill time.
  - Working tree clean (`git status --porcelain` empty besides this bookend); own branch verified an
    ancestor of `main` after push (see below).
  No findings filed — the one narrow gap found (new-issue body lag under the incremental pass) is a
  pre-existing, already-accepted architectural tradeoff shared with several other fields, not a new or
  broken behavior introduced by #3852/#3853.
