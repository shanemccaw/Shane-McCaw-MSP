# #2797 — ActivityBar: fold Admin Center / Customer Portal / Marketing Site into the Web Tools popup

- **Status:** ✅ DONE 2026-09-04T04:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 794b129d1 (implementation), fe8040746 (post-rebase HEAD on main)

## Log
- 2026-09-04T04:00:00Z ⏳ IN FLIGHT — removing the three standalone `Admin Center`/`Customer Portal`/`Marketing Site` `<Button>` rail elements from `Controls/ActivityBar.xaml`, and prepending them as fixed, always-present rows (with a separator) at the top of the `WebToolsList` popup built in `BtnWebTools_Click` (`Controls/ActivityBar.xaml.cs`), reusing the same real navigation path (`QuickNavRequested`) with their original glyphs/colors.
- 2026-09-04T04:20:00Z ✅ DONE — Removed the 3 standalone rail `<Button>`s (Admin Center/Customer Portal/Marketing Site) from `ActivityBar.xaml`, leaving one clean separator between Git Doctor and the Replit Workspace button (no orphaned/duplicate separators). Added a `FixedWebTools` array (same 3 URLs/tooltips/glyphs/brush colors as the removed buttons: shield ``/PeachBrush, person ``/MauveBrush, globe ``/GreenBrush) that `BtnWebTools_Click` now prepends to `WebToolsList` ahead of the existing `settings.WebTools` loop, followed by a real visual separator `Border` before the user-configured entries. New rows reuse the exact same row-building shape (icon TextBlock + label TextBlock) as the configured-tool rows, wired to a new `FixedWebTool_Click` handler that fires `QuickNavRequested` (same real navigation `QuickNav_Click` used) and closes the popup afterward, since these rows now live inside it. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 Warnings, 0 Errors. `node scripts/dev-server/verify-branch-merged.mjs` confirms `agent/2797-q1584` (fe804074) is merged into `origin/main`. `git status --porcelain` clean of anything touched this session.
