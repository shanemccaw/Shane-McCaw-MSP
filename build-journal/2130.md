# #2130 — URGENT: #1887 background tab auto-reopen kill switch + root cause

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** `85b40d1d8` (kill switch) — merged to origin/main

## Log
- 2026-08-31 ⏳ IN FLIGHT — Live incident: #1887's `ChatReopenPreloadHost` background preload locked up Shane's machine + drained the build queue. Shipping a default-OFF kill switch that no-ops the preload path in `MainWindow.ChatReopen.cs`, then tracing the #1884/#1887 startup-sequencing interaction side-by-side to root-cause.
- 2026-08-31 ✅ DONE — Kill switch shipped in `85b40d1d8` (Release build clean, 0 warn/0 err; verified ancestor of origin/main; tree clean). New `BuildConsoleSettings.EnableChatReopenPreload` (default **false**) + `BUILDCONSOLE_ENABLE_CHAT_REOPEN_PRELOAD` env override; `ReopenPersistedChatTabsInBackgroundAsync` no-ops at its single entry point when disabled. Manual tab-reopen-on-click is a separate path, unaffected.
  - **Root cause (traced, not guessed):** the lockup is #1887's preload keeping **all** N persisted tabs' real off-screen `WebView2` (full claude.ai page + multiple `msedgewebview2.exe` per instance) alive simultaneously — the `SemaphoreSlim(2)` caps only *concurrency*, not the *total*, and nothing disposes a parked preload on success (only removed on select/close). N tabs → N live browsers → machine lockup.
  - The queue drain is **NOT** premature and **NOT** a duplicate tick: `AllSettled` (`_allSettledRaised`) and deferred startup (`_deferredStartupBegun`) are both single-shot, so only one `QueueWatcherService`/grace timer exists; the #1884 grace `DispatcherTimer` runs on the UI thread, so preload saturation can only *delay* `MarkAppReady()`, never accelerate it. The drain is the normal post-90s #1884 fan-out landing on an already-thrashing machine — a compounding consequence, not a separate queue bug.
  - **Queue DB state now:** healthy — all `running` rows have live, recent heartbeats; no stale orphans from the killed-Postgres drain. No reconciliation performed (no DB writes).
  - **Full code fix warranted (non-trivial) → filed separately as #2132** (parented #1202, board "AI Batter Up"): bound the *total* live preloads (cap most-recent few, and/or `CoreWebView2.TrySuspendAsync` parked-but-unselected, and/or dispose surplus). Kill switch stays OFF until that lands.
