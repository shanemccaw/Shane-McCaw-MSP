# #1913 — Add the production-change gate rule to CLAUDE.md and the SessionStart hook

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 4bc4fd8f1

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled #1913 body + both comments. Comment #2 (5467790871)
  supersedes the body's framing: the boundary is the **app registration**
  (DEV `9f6f4772-b5be-421f-815e-b392336c373a` agent-modifiable vs. PROD
  `3308b280-e41e-42ba-9f73-73aac2ad3dee` plan-only, wrapped in #1281), not
  "testbed tenant vs. production tenant" — `mccawsoft2.onmicrosoft.com` is both.
  Everything else in the body stands (ca-ps-execution-dev vs ca-ps-execution,
  prod Key Vault `ShaneMcCawConsulting`, Staging/Replit deploys+migrations,
  tenant licensing/service enablement — all wrapped in #1281). About to add the
  rule to CLAUDE.md and to the SessionStart hook payload in `.claude/settings.json`,
  then validate the hook's shell quoting with a real session start.
- 2026-08-30 ✅ DONE — Added a new "Production-change gate" section to CLAUDE.md
  (app-registration boundary, DEV vs PROD IDs named, explicit that stopping at the
  boundary is a *successful* build and distinct from the never-invent-data/BUILD IT
  rule). Also found and fixed a genuine pre-existing bug while doing the shell-quoting
  validation the issue asked for: the SessionStart hook's JSON payload was
  double-escaped (`\\\"` instead of `\"`) inside the bash single-quoted `echo`, so
  every session start was emitting invalid JSON (confirmed by extracting the exact
  command string and running it through bash before my fix — `{\"hookSpecificOutput\":
  ...` with literal backslashes, `JSON.parse` failed). Rewrote using the same
  single-backslash escaping as the already-working PostToolUse hook, appended the new
  gate summary to the payload text (written with no apostrophes, since the whole
  string lives inside a bash single-quoted echo and an apostrophe there would
  terminate the quote early — the exact failure class the issue flagged as
  "previously broke every session start"), then re-extracted and re-ran the fixed
  command through bash and confirmed the stdout now parses as valid JSON with the
  correct `hookSpecificOutput.hookEventName` / `additionalContext` shape, including
  the new gate text and both app-registration IDs. Rebased onto origin/main (only
  unrelated bookend #1912 had landed), pushed at 4bc4fd8f1,
  `verify-branch-merged.mjs` confirms merged, `git status --porcelain` clean. No
  other findings surfaced worth filing as separate issues.
