# #1712 — Risk Register: regenerate the contract pack from the finished backend

- **Status:** ✅ DONE 2026-09-01T20:31:18Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** `8a872d9b5` (regenerate pack), `bfb4ba2df` (merge into main, pushed as `main`)

## Log
- 2026-09-01T20:21:00Z ⏳ IN FLIGHT — regenerating docs/risk-register-contract-pack.md from the finished backend (#1507/#1508/#1509/#1511/#1512/#1514 all landed since the 2026-08-29 pack), read-only, no product/schema/UI changes.
- 2026-09-01T20:31:18Z ✅ DONE — regenerated `docs/risk-register-contract-pack.md` in full against nine landed commits (#1507, #1508, #1509, #1510, #1511, #1512, #1513, #1514, #1527). Verified every DB column cited against the live local Postgres schema (`\d msp_risk_decisions`, `\d risk_instances`, `\d msp_rbd_versions`). Confirmed `expired` fully removed from both `RISK_ACCEPTANCE_STATUSES` and `POLICY_DECISION_STATES` (not just avoided). Confirmed portal-v2/msp-portal fully retired from the repo and `Design/portal/` empty — no page consumes any of this module's endpoints today, which is the expected pre-Design state per #1487's fixed order, not a new orphan finding; no sub-issue filed. Discovered and disambiguated (new §0.1) a second, separate `policy_decisions` table + `portal-policy-decisions.ts` route (#1528, landed 2026-08-31) that must not be conflated with the legacy risk-derived `GET /portal/policy-decisions` this pack documents. Documented two new real gaps found along the way (§7.3): `spawned_by_remediation_step_id` is written but never reaches `WireRisk`, and `public-rbd-document.ts` reads `x-forwarded-for` directly (a deliberate exception, not a bug, but worth flagging) unlike every other signature path in the module which uses `req.ip`. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean. Read-only pass — no product code, schema, or UI changed.
