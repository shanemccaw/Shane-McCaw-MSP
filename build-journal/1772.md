# #1772 — api-server vitest suite: pre-existing failures, unrelated to #1500

- **Status:** ✅ DONE 2026-09-04T04:58:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** f269ca277 (16 files), de4552980 (merge, brings in concurrent #2793's 6-file fix),
  70e89493a (remaining 26 files), 901a7df26 (merge to main, HEAD)

## Log
- 2026-09-04T08:10:00Z ⏳ IN FLIGHT — removed stale `blocked_by` edge to #1778 (closed
  not_planned/superseded 2026-09-04T00:37:34Z, real explanatory comment already present).
  Shane's own re-measurement comment on #1772 (posted 2026-09-04T07:51:36Z, on `main` at
  `98f151a3c`) already showed the post-#1778 baseline: 53 failed files / 221 failed tests.
  Merged worktree branch forward to latest `origin/main` (`0af690d6d`) before re-running the
  suite myself to confirm current state and begin triage.
- 2026-09-04T04:04:00Z — Ran the real baseline myself: `cd artifacts/api-server && npx vitest
  run` with `DATABASE_URL` exported → **48 failed files / 204 failed tests, out of 303 files /
  4694 tests**. Extracted every failing file/error and triaged by root cause.
- 2026-09-04T04:11:00Z — Fixed 16 files myself: 14 were `vi.mock()` factories missing an export
  the real module now needs (`requireAdminOrIngestToken`, `sql`/`and` on drizzle-orm, missing
  `logger.child`, `db.update`) — same `importOriginal`-spread pattern already established
  elsewhere in the suite. The other 2
  (`msp-financial-aggregator[-overlord].test.ts`) were the issue's own flagged "100x
  money-scaling bug" — confirmed **not** a production bug: `invoicesTable.amount` is genuinely
  integer cents (Git #1610 migration), but these tests fed the aggregator a dollar-formatted
  string like `"150.00"` left over from the pre-#1610 `numeric(10,2)` column. Fixed the fixtures
  to real integer cents.
- 2026-09-04T04:14:00Z — A concurrent session landed **#2793** (`c33987d72`, merged via
  `81413dd21`) fixing the identical `logger.child` bug class in 6 files, 2 of which (`msp.test.ts`,
  `msp-mailer.test.ts`) directly overlapped my own fixes. Merged `origin/main`, resolved both
  conflicts by taking `origin/main`'s equivalent fix (functionally identical), which also brought
  in #2793's fixes for `msp-suspended-banner.test.ts`, `msp-diagnostics.test.ts` (partial — its
  remaining 2 failures are a separate, already-filed gap, **#2853**),
  `msp-customers-bulk.test.ts`, `msp-custom-domain.test.ts`. Pushed merged state to `main`.
- 2026-09-04T04:16:00Z — Dispatched 3 background triage agents in parallel over the remaining
  ~28 files (batch A: 6 lib files, batch B: 10 lib files, batch C: 12 route files), each
  instructed to fix ONLY confirmed stale-fixture/stale-mock drift (never touch production code),
  and document anything else as a real finding with file:line evidence.
  - **Batch A** (6 files, 43 tests fixed): `compliance-surface-coverage-2762.test.ts` +
    `document-style-identifier-hygiene-558.test.ts` — migration `.sql` files are checked in CRLF;
    both tests' raw-text matching only worked against LF (one silently matched 10x too much: 40
    jsonb blocks instead of 4; the other never matched at all). `ai-billing-cost-broadcast.test.ts`
    — `recordAiUsage`'s return contract changed per its own JSDoc. `assessment-tool-usage-signal
    .test.ts` — `persona-generation-engine` moved `.create()` → `.stream()` (#283). `dashboard-
    resolvers.test.ts` — `licensing.wasteEstimateBreakdown` now double-queries SKU price and
    reports `excludedUnpricedSkus` not `unknownSkus` (commit `eba935da4`). `engine-determinism
    .test.ts` — `securityImpact` was already correctly excluded from `computeHealthEngine`'s sum
    by `dc9e8b7dc`; this suite was never updated to match.
  - **Batch B** (9 of 10 files needed fixes, 21 tests; 1 file already passed clean): stale
    fixtures/mocks in `m365-roadmap-mc-link`, `msp-executive-data` (dropped bridge query, #2730),
    `msp-ownership-book`/`portal-ownership` (new `declineReason`/`respondedBy`/`respondedAt`
    fields, #1519), `policy-compliance` (new `group_membership` evaluator, #1953),
    `shanebot-engine` (pre-#1610 dollar-string fixture), `sow-pricing` (deliberately superseded
    fail-loudly contract), `workflow-executor-core` (deterministic `check_script_output` +
    new all-phases query), `monitor-executor` (missing `PowerPlatformNotRegisteredError` mock
    export). `live-monitor-nodes.test.ts` passed cleanly alone, no change needed.
  - **Batch C** (12 files, 33 tests fixed): stale fixtures/mocks in `admin-active-directory-
    delete` (FK census count, #2730), `admin-money`/`portal-retainer` (real-wall-clock "this
    month" fixtures pinned to August 2026 via `vi.useFakeTimers()`), `admin-signal-rules-from-
    tenant` (migrated off dropped `msp_customers`/`tenant_consent` tables), `msp-sow`/`portal-
    assessment-debug-{reset-session,trigger-scan}` (missing `workflow-executor`/`sharepoint-
    admin` mocks + AI env vars caused real heavy modules to load and throw), `portal-checkout`
    (missing CAPTCHA request body from a real gate, `a603dfacb`, plus a missing `mspsTable` mock
    for a parent-MSP agreement lookup, `281b29571`), `portal-policy-decisions`/`portal-risk-
    register` (missing `complianceObligations`/`Frameworks` mocks for #1525's cited-authority
    join), `public-purchase-payment` (its "known-not-real" pack slug became real via a later
    migration, #1182 — swapped to a slug confirmed still absent).
  - Batch C also surfaced a real, useful test-infra gotcha while fixing `portal-checkout.test.ts`:
    `vi.clearAllMocks()` does **not** clear queued `mockReturnValueOnce()` implementations (only
    `.mockReset()` does) — filed as a repo-wide finding (**#2865**, folded into the flakiness
    finding below since both surfaced together).
- 2026-09-04T04:53:00Z — Committed both waves (`f269ca277`, `70e89493a`), merged in a second
  concurrent landing (`c4a364d75`, "Fix POST /msp/sows/:sowId/sign 500: make workflow-executor
  import non-fatal" — the exact same root cause my `msp-sow.test.ts` fix addressed test-side;
  confirmed compatible, re-verified `msp-sow.test.ts` still passes 31/31 after the merge). Clean
  merge, pushed to `main` (`901a7df26`).
- 2026-09-04T04:56:00Z — Ran the real full suite again: **5 failed files / 7 failed tests, out
  of 303 files / 4850 tests.** Broke down the remaining 7:
  - 2 tests / 1 file (`msp-diagnostics.test.ts`) already tracked at **#2853** (missing `sql`
    export on a drizzle-orm mock) — not re-filed.
  - 5 tests / 4 files (`doc-pipeline.test.ts`, `live-monitor-nodes.test.ts`, `portal-assessment-
    debug-trigger-scan.test.ts`, `portal-assessment-debug-reset-session.test.ts`) all confirmed
    to pass reliably when run alone (verified each individually, repeatedly) — only fail
    intermittently under the full 303-file parallel run. Filed as **#2865** rather than chased
    further; looks like vitest worker/transform contention plus one genuine-looking
    cross-test-state case (`deleteCalls` count drifting from 2 to 4 under full-suite load only)
    that deserves its own look.
- **Findings filed** (all parented as direct sub-issues of #2491, this issue's own Feature
  parent, per the Feature-first rule; all labeled `bug`; board status set to "AI Batter Up"):
  - **#2862** — `policy-compliance.ts`'s new `group_membership` evaluator has zero test coverage.
  - **#2863** — `dashboard-resolvers.ts` queries `sku_price_reference` twice per SKU (real,
    non-blocking DB inefficiency on a customer-facing dashboard metric).
  - **#2864** — `vi.clearAllMocks()` doesn't clear queued `mockReturnValueOnce()` — a repo-wide
    test-infra footgun, only fixed locally in `portal-checkout.test.ts` under this issue.
  - **#2865** — 4 test files intermittently fail only under the full-suite parallel vitest run,
    always pass alone (transform-contention + one real-looking cross-test-state case).
- ✅ DONE 2026-09-04T04:58:00Z — **Real result: 48 failed files / 204 failed tests → 5 failed
  files / 7 failed tests** (out of 303 files / ~4850 tests). Every one of the 43 files fixed
  this session (16 mine + 27 across 3 parallel triage agents) was confirmed **stale test-fixture
  or stale-mock drift against a real, already-landed, correct production change** — zero
  production code was modified anywhere in this build. 4 genuine findings filed and parented
  under #2491 for what's left (1 already-tracked gap at #2853 not re-filed). `git status
  --porcelain` clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (HEAD `901a7df26`).
