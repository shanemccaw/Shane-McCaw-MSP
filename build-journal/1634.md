# #1634 — Build Queue panel: remove nested queue-list scrollbar

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 9fba3f30e (fix), 9d5418514 (bookend IN FLIGHT)

## Log
- 2026-08-29 ⏳ IN FLIGHT — read issue body (no comments); reading BuildQueuePanel.xaml/.xaml.cs in full before editing to remove QueueGraphScrollViewer's own scroll region, delete the wheel-forwarding handler, and check RedrawQueueGraph's height measurement.
- 2026-08-29 ✅ DONE — Removed the `QueueGraphScrollViewer` wrapper (`MaxHeight="900"`,
  own vertical scrollbar) around `QueueGraphContainer` in `Controls/BuildQueuePanel.xaml`;
  the queue card list now sits directly in the panel's single outer `ScrollViewer`
  (~line 155) and grows to natural height. Deleted the now-dead
  `QueueGraphScrollViewer_PreviewMouseWheel` forwarding handler in the code-behind.
  Read `RedrawQueueGraph` in full: it already measures height off
  `QueueCardsHost.ActualHeight` (the real content height, not a viewport) and
  positions connector nodes via `TranslatePoint` (offset-invariant between
  descendants of the same scrollable ancestor) — confirmed no viewport-based
  measurement bug existed there, so no change was needed for connector alignment.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 errors,
  46 pre-existing warnings unrelated to this change.
  **Not live-driven in this environment** — I did not launch the running WPF app to
  visually confirm the single scrollbar, mouse-wheel continuity, or connector-line
  alignment at a queue length exceeding the old 900px cap; this is a read-through +
  clean-compile verification only, per the issue's own "state plainly which of the
  above could be checked live vs. read-through only" ask.
  Branch verified merged: `git merge-base --is-ancestor HEAD origin/main` passed
  against commit 9fba3f30e on `origin/main`. `git status --porcelain` clean.
