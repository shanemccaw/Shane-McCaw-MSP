# #3625 — admin-marketing.ts still reads dead analytics_sessions/pageviews/site_events tables

- **Status:** ✅ DONE 2026-09-11T05:35:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 38f7952e9 (rebased/pushed to main as 4babfea87)

## Log
- 2026-09-11T04:00:00Z ⏳ IN FLIGHT — Investigating admin-marketing.ts's KPI/analytics/campaign-badge endpoints that query analytics_sessions/analytics_pageviews/analytics_site_events (all 0-row, no ingestion since #123). Removing the dead-table queries and returning honest zero/empty values directly per #3625, to unblock #3610's table drop. Not building a GA4 replacement — that's #3437 (Marketing rebuild, not yet scheduled).
- 2026-09-11T05:35:00Z ✅ DONE — Removed all `analytics_sessions`/`analytics_pageviews`/`analytics_site_events` reads from `admin-marketing.ts`'s `GET /admin/marketing/kpi`, `GET /admin/marketing/analytics`, `GET /admin/marketing/analytics/insights`, `GET /admin/marketing/active-campaign-badges` + its SSE stream variant — each now returns the same honest zero/empty values directly, with a comment pointing at #3437 (Marketing rebuild, not yet scheduled). Updated the stale RETIRED comment on the table definitions in `lib/db/src/schema/index.ts`. `tsc --noEmit` on api-server is clean for this file (pre-existing unrelated `msp-poams`/`deletedAt` errors elsewhere, not touched by this build). Pushed to `origin/main` at `4babfea87`; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean. Filed a real finding, #3644 (`seed-system-workflows.ts`'s Engagement Offer Delayed Follow-Up workflow still queries `analytics_sessions` in a live `sql_query` workflow node — falls through safely today via COALESCE but will hard-fail once #3610 drops the table), parented under #1095, wired `blocked_by` from #3610 → #3644, labeled `bug`, board status set to AI Batter Up.
