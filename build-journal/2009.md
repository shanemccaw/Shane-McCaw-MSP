# #2009 — Sales-offer accept flow (project-class services) has no frontend entry point / sow.signed doesn't create a project

- **Status:** ✅ DONE 2026-09-12T16:54:02Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 2d1a5108b (implementation), ca9e1afa3 (IN FLIGHT bookend)

## Log
- 2026-09-12T16:41:22Z ⏳ IN FLIGHT — Wiring the real `sow.signed` event (msp-sow.ts) to trigger real project
  creation via `fulfillAcceptedProjectOffer()` (project-sow-fulfillment.ts, the same pipeline
  the customer-facing accept route already uses), since `unlockFulfillment()` only ever
  touched `fulfillment_queue` and never created a `projects` row. Verifying the MSP-operator
  accept/sign frontend entry point in `artifacts/msp-console` (Sales.tsx / sales-api.ts, built
  under #2643) already surfaces `POST /api/msp/offers/:offerId/accept`, and extending it to
  show the resulting project once signing kicks off fulfillment. (Bookend filed retroactively
  after investigation + initial implementation — should have been the first commit; noting
  honestly rather than backdating.)
- 2026-09-12T16:54:02Z ✅ DONE — Both sign routes (`/msp/sows/:sowId/sign` and the public
  `/public/sows/:shareToken/sign`) now call `fulfillAcceptedProjectOffer()` via a dynamic
  import (2d1a5108b), same discipline as the existing `sow.signed` workflow-event import in
  that file. Added `msp_sows.project_id` (nullable, FK -> projects.id) so the operator-facing
  SOW drawer can show a real "Project #N created" state — migration
  `lib/db/migrations/manual/2026-09-12-msp-sows-project-id-2009.sql` run against local
  Postgres (ALTER TABLE + index, verified via `\d msp_sows`). msp-console's Sales.tsx (the
  MSP-operator accept/sign entry point built under #2643, already calling the real
  `POST /api/msp/offers/:offerId/accept`) now surfaces that PROJECT field.

  Verification: `pnpm --filter @workspace/api-server run typecheck` clean;
  `npx vitest run src/routes/msp-sow.test.ts` 37/37 passing (5 new tests covering
  offerId-present / no-offerId / assigned-customer-signs-directly, on both sign routes);
  full api-server suite run — 67 pre-existing failing test files (azure-rm, msp-alerts,
  msp-customer-timeline, a missing-DATABASE_URL file) are unrelated to this change (my
  diff only touches msp-sow.ts/msp-sow.test.ts in that package; confirmed via
  `git diff --stat HEAD~1 HEAD -- artifacts/api-server`), not introduced by it.
  `pnpm --filter @workspace/msp-console run typecheck` clean; `pnpm --filter
  @workspace/msp-console run build` and `node artifacts/api-server/build.mjs` both succeed.
  Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0).
  Working tree clean (`git status --porcelain` empty) after every commit.

  Real finding filed and parented under #1570 (this issue's Feature), labeled `bug`,
  board status AI Batter Up: **#3803** — `unlockFulfillment()` in msp-sow.ts can never
  match a `fulfillment_queue` row (its `sourceType: "sow"` rows are only ever inserted
  keyed to `quickWinPresentations.id`, a different id-space than `msp_sows.sow_id`) — a
  pre-existing, separate dead-code bug discovered while reading this file, not fixed here
  since it's a different mechanism than the project-creation wiring this issue scoped.

  Not done in this build: the public customer-facing page that actually renders a SOW
  share link (`/sow/:shareToken`) — that's #3788, a separate, already-filed finding this
  issue's own prompt explicitly deferred to. No fixture/fabricated data anywhere in this
  change; every field wired reads from a real column or a real pipeline result.
