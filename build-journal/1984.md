# #1984 — ComputeGitGraphSignature() misses refs/remotes/** (graph stale after fetch)

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed hypothesis by reading `ComputeGitGraphSignature()` (LeftSidebar.xaml.cs:6237): it samples HEAD, packed-refs, logs/HEAD, refs/heads/** only — no refs/remotes/** or logs/refs/remotes/**. Graph renders `git log --all` which spans refs/remotes. Extending the signature to also sample those two paths, same cheap max-mtime approach (directory mtime, not per-file enumeration, to keep cost negligible on repos with many remote branches). Will verify against a scratch repo: fetch that moves a remote ref with no local branch touched must change the signature; idle window must not.
