# #1984 — ComputeGitGraphSignature() misses refs/remotes/** (graph stale after fetch)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** bf148a99b (on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed hypothesis by reading `ComputeGitGraphSignature()` (LeftSidebar.xaml.cs:6237): it samples HEAD, packed-refs, logs/HEAD, refs/heads/** only — no refs/remotes/** or logs/refs/remotes/**. Graph renders `git log --all` which spans refs/remotes. Extending the signature to also sample those two paths, same cheap max-mtime approach.
- 2026-08-30 ✅ DONE — `bf148a99b`. Added `ConsiderAllFilesUnder()` walking `refs/remotes/**` and `logs/refs/remotes/**` with the same per-file mtime approach already used for `refs/heads` (kept per-file rather than directory-mtime sampling, since per-file enumeration measured negligible against this repo's real `.git`: 3 loose files under `refs/remotes`, 92 under `logs/refs` total — both packed via periodic `git gc`, so already trivial next to the `git log` subprocess this gate exists to avoid). Verified against a real scratch repo (upstream + clone-on-a-non-`main`-local-branch, so a fetch of `origin/main` touches zero local refs): signature before fetch `1788100133073.67`, after a real `git fetch` `1788100173249.87` — changed, and confirmed `refs/heads/main`/`refs/heads/work` mtimes were untouched by the fetch (predate it) while `refs/remotes/origin/main`'s mtime landed at `1788100173246.86`, i.e. the new remote-refs sampling — not refs/heads — is what caught the move. Idle-window re-sample after the fetch stayed byte-identical (stable). `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: 0 warnings, 0 errors. `verify-branch-merged.mjs`: OK, ancestor of origin/main. `git status --porcelain` clean. No separate findings filed — this was a scoped, fully self-contained fix.
