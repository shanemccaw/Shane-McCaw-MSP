# #1978 — Worktree cleanup sweep intermittently aborts with 'repo root not found' (FindRepoRoot returns null)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 66bf00d4d9cd5900fce08e5c257dd02b3e5e5b4e (on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigating FindRepoRoot() null cause against real activity logs + git reflog, then caching repo root at startup with a configured fallback and surfacing failures via Needs Attention/ToastEngine.
- 2026-08-30 🔎 ROOT CAUSE (evidenced) — `activity-2026-08-30.log`: 96 `Cannot sweep worktrees: repo root not found` hits in ~30s bursts that RECOVER on their own (e.g. 01:08:52→01:24:05, then quiet, then resume 01:29:12). The 30s cadence is HomeView's health timer (`HomeView.xaml.cs:834`). `version-update` ticks uninterrupted on v1.0.765 all day → single continuous process, so NOT a relaunch / BaseDirectory issue. Config `scripts/build-queue-watcher.config.json` present with STABLE Aug-23 mtime → NOT deleted/restored (a git checkout would change mtime). Bursts overlap dev-server restart/merge-back cycles + `[git-panel.watcher] Too many changes at once` floods in the main checkout. **Inferred link:** `FindConfigPath()`'s `File.Exists` on the gitignored config file (or a dir along the walk) transiently returns false under heavy concurrent IO during those churn windows → `FindRepoRoot()` returns null → sweep no-ops (and after #1971, `removeWorktreeSafe`'s work-preservation does not run).
- 2026-08-30 ✅ DONE — Fix committed 66bf00d4d and confirmed ancestor of origin/main. `BuildTrackerConfig`: `InitializeRepoRoot()` resolves+caches the root ONCE at `App.OnStartup` (quiet tree), held for the process lifetime; `ResolveRepoRoot()` tries config-file walk → stable `.git`-directory walk → `BuildConsoleSettings.RepoRootOverride` before null; a genuinely-unresolvable root is surfaced via a rate-limited `ToastEngine.Warning` (channel `system.core`) instead of a silent log line. New setting `RepoRootOverride` (empty default). `dotnet build BuildConsole.csproj -c Release` clean (0 warn / 0 err). Could not live-verify against Shane's running BuildConsole.exe (must not relaunch/kill it) — the fix is verified by clean build + the evidence chain above. Finding filed: #1985 (FindRepoRoot null coalesced to empty/hardcoded paths at ~10 call sites), parented to #1791, board "AI Batter Up".
