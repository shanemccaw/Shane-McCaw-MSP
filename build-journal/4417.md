# #4417 — Command Center: "Clean Dev Database" chain (find-tenant-scoped-tables → reset-dev-database → reset-rbac-test-accounts)

- **Status:** ✅ DONE 2026-09-16T20:01:27Z
- **Scope:** buildconsole (plus scripts/db/README.md)
- **Started:** 2026-09-16
- **Commit(s):** 1946daf03 (work), 5c903b998 (plan), d1f755d32 (IN FLIGHT bookend)
- **Plan:** [4417-plan.md](4417-plan.md)

## Log
- 2026-09-16T19:40:43Z ⏳ IN FLIGHT — blockers #4414/#4415/#4416 all DONE on main; reading their gates (IPaletteConfirmGate, PaletteScriptProcess, DevDatabaseResetGate, RbacTestAccountsResetGate) and the palette window before wiring the chain.
- 2026-09-16T20:01:27Z ✅ DONE — chain shipped. Verified against the real local dev DB: preview, a real fail-stop, and the real confirmed chain. Details below.

## Changes Made
- `desktop/BuildConsole/Services/CleanDevDatabaseChainGate.cs` (new) — `IPaletteProgressGate`, phrase `clean dev db #<live msp id>`.
  Composes its OWN private `DevDatabaseResetGate` + `RbacTestAccountsResetGate` (#4415/#4416's gates, not a second
  implementation; private so a chain preview never arms the standalone `reset msp #N` / `reset rbac #N` phrases).
  Step 1 runs through the shared `PaletteScriptProcess`.
  - Preview (row Enter): step 1 for real, step 2 `--dry-run`, step 3 `--dry-run`, streamed step by step; arms only
    if both dry runs succeeded for the same MSP. The sub-gates' standalone "type reset msp #N" headers are stripped.
    It always states the tenant-consent situation, read from the RBAC dry run's own `[resolve]` line.
  - Execute: re-check + consume phrase → step 1 (never blocks) → step 2 via the dev gate's own ExecuteAsync
    (**fail-stop**: step 3 marked NOT RUN) → consent check = fresh step 3 dry run against the reset DB; no live
    mccawsoft2 tenants row → **stop before step 3** with the manual-consent instruction (run "Reset RBAC test
    accounts" alone afterwards, NOT the chain again) → otherwise step 3 via the RBAC gate's own ExecuteAsync.
- `Services/IPaletteConfirmGate.cs` — new `IPaletteProgressGate` (ExecuteAsync with `IProgress<string>`).
- `Services/RbacTestAccountsResetGate.cs` — `PreviewedTenantRowPresent` / `PreviewedTenantMspId` parsed from the
  script's own `[resolve] mccawsoft2 -> …` lines (reuses #4416's detection instead of new handling). Behavior unchanged.
- `CommandPaletteWindow.xaml.cs` — `PaletteCommand.RunWithProgress` (+ `ShowsResultInline`); streamed text replaces
  "Running…" for the selected row; confirm mode renders a progress gate's streamed text while its run is in flight.
- `MainWindow.CommandPalette.cs` — "Clean Dev Database" row + gate registered with the palette.
- `scripts/db/README.md` — documents the chain.

## Verification Results
- `dotnet build desktop/BuildConsole/BuildConsole.csproj`: 0 warnings, 0 errors.
- Headless probe (C:\t\probe4417, separate exe referencing the built BuildConsole.dll; BuildConsole.exe never launched,
  no window shown) drove the REAL `CommandPaletteWindow.Window_PreviewKeyDown` + REAL gates against the local dev DB.
  "Unchanged" = tenants (msp #1 / all), a fingerprint of every script-owned test account, the db-backups count, and the
  settings.json TestEnvironmentVariables hash. wf_runs was only logged: the live api-server wrote to it during the probe (54→58
  in a preview that only rolls back).
  - **Preview phase — ALL PASS:** A: the phrase typed before any preview + Enter → locked, nothing ran. B: one Enter on
    the row → live snapshots showed step 1 RUNNING (2/3 waiting) → step 1 DONE + step 2 RUNNING → step 2 DONE + step 3
    RUNNING → all DONE; final pane "DRY RUN ONLY" + `clean dev db #1`, real FK scan + both dry-run outputs, rolled-back
    passwords redacted, consent note; standalone dev/RBAC gates NOT armed; state unchanged. C: a second Enter re-ran the
    preview only. D: a wrong phrase + Down/Tab/Enter → nothing. D2: `reset msp #1` / `reset rbac #1` stay locked.
  - **Fail-stop phase — ALL PASS:** a real preview, then a failing `pg_dump.exe` (renamed where.exe, exit 2) first on
    the child PATH. Real psql/pg_restore untouched, so step 1 and step 2's own internal dry run really succeeded, and
    step 3 (pure node pg client) would have succeeded if run. Typed phrase + Enter (plus a stray Enter mid-run) → live:
    step 1 RUNNING → step 1 DONE + step 2 RUNNING (3 waiting) → result `✗ CHAIN STOPPED AT STEP 2`, step 2 FAILED,
    step 3 NOT RUN; 0 test accounts created since start; accounts fingerprint, tenants, backups (5) and settings.json
    vars all unchanged; no step 3 output; confirmation consumed.
  - **Real chain phase — ALL PASS:** a real preview, typed `clean dev db #1` + Enter → live: step 1 DONE @4s → step 2
    RUNNING → @140s step 2 DONE + step 3 RUNNING "checking for a consented tenant" → result `■ CHAIN STOPPED BEFORE
    STEP 3 — tenant admin consent needed. The database WAS reset`. Step 2 was a real reset: `✓ REAL RESET COMPLETED`,
    `Backup: …\shanemccawmsp_pre-dev-reset_2026-09-16T195645136Z.dump` verified (pg_restore --list, 4339 TOC entries);
    backups 5→6; tenants msp #1 1→0 (all 5→4); script-owned test accounts 17→8 (the 9 tenant-scoped ones went with
    tenant 1). The mccawsoft2 tenants row is gone, so step 3 did NOT run: 0 accounts created, settings.json vars unchanged.
- Keyboard-only (#3827 standing requirement): every step above is typing/Enter/arrow/Tab — no click needed.
- **Honest gap:** the "consent present → step 3 runs for real inside the chain" branch was not exercised. It can't be
  today without a manual consent: the only mccawsoft2 tenants row belongs to the direct MSP, so every real reset deletes
  it, and the chain will (correctly, per Shane's constraint) stop before step 3 every time until that changes. That
  branch just calls the #4416 gate's own ExecuteAsync, which #4416 verified end to end. I didn't click through the
  live BuildConsole (never-launch-exe rule), so please confirm by eye after deploy.
- Dev DB state left by the real run: msp #1 at clean slate (0 tenants). The 9 tenant-scoped RBAC test accounts are
  gone until consent is granted and "Reset RBAC test accounts" runs; their settings.json pairs now point at deleted accounts.

## Findings filed (sub-issues of #3827, AI Batter Up)
- #4436 — reset-dev-database.mjs prints DATABASE_URL (with password) in pg_dump/psql failure output, shown in the palette pane (bug, security).
- #4437 — DevDatabaseResetGate reports `Backup: <path>` for a backup that never completed when pg_dump fails (bug).

## Working tree
- Clean at DONE. The scratch probe lives outside the repo (C:\t\probe4417, C:\t\bc4417-out); the DB password was scrubbed from its output files.
