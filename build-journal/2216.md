# #2216 — Feature: PowerShell + Terminal — mini panel (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:03:11Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 8945dd4d6 (merged into main; verified via verify-branch-merged.mjs)

## Log
- 2026-09-02T14:26:30Z ⏳ IN FLIGHT — real audit done: read README-ClaudeChat.md §6.8
  (untracked in the main checkout at
  `desktop/ShaneBuilder/wpf-handoff/wpf-handoff/README-ClaudeChat.md`) and the current
  chat-document rail implementation in `desktop/ShaneBuilder/MainWindow.xaml(.cs)`
  (from #2209). Real live sessions confirmed unblocked by Shane's 2026-09-02 comment on
  the issue. Existing precedent: rail panels (Detected Items, Log Peek) are each their
  own Grid column + boolean toggle, not a unified single-slot `ChatToolOpen`. Building
  PowerShell (powershell.exe) and Terminal (cmd.exe) as two more such flyout columns,
  each backed by a real persistent child process per chat tab (dictionary keyed by
  tab id), so a session's scrollback survives a tab switch and is torn down when the
  tab closes or the app exits.
- 2026-09-02T15:03:11Z ✅ DONE — Real persistent child-process sessions verified against actual
  `powershell.exe`/`cmd.exe` (`Services/TerminalSessionService.cs`): a standalone scratch harness
  ran real commands (`Write-Output`, `2+2`, `Get-Location`, `echo`, `cd`, `set /a`) through both
  session kinds and confirmed correct output capture and completion detection via the per-session
  sentinel marker. Two new flyout rail columns (PowerShell `#4f8ff0` "PS>", Terminal `#6ee7b7` "$")
  added to `MainWindow.xaml`/`.xaml.cs` following the existing Detected Items/Log Peek/SQL
  Runner/Repo Health/JSON Viewer/File Browser/Git Map/Git Doctor precedent — one Grid column +
  boolean toggle each, wired into the wrench menu, `RenderClaudeChatContext` (per-tab repaint on
  tab switch), `CloseTab` (per-tab session disposal), and `OnClosed` (full app-exit disposal).
  Sessions are keyed per chat tab in `_psSessionsByTab`/`_terminalSessionsByTab`, same shape as
  `_chatDrafts`. `dotnet build` clean, 0 warnings/errors. This build landed on an extremely hot
  file — 8 other sibling tool-panel builds (#2213-#2215, #2217-#2219, #2176) touched the same
  `MainWindow.xaml`/`.xaml.cs` rail Grid concurrently while this session was in flight, causing
  repeated rebase conflicts; final approach was to reset to the true tip of `origin/main` and
  reapply the two new columns fresh each time rather than trust automatic 3-way merges (which
  twice produced structurally-plausible-but-wrong splices). Final push landed clean on the first
  attempt after the last conflicting build (#2176) merged with a disjoint diff. Working tree
  clean; no unrelated files touched.
