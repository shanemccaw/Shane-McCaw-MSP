# #3117 — Sign-in screen shows the app icon, not the fox critter

- **Status:** ✅ DONE 2026-09-07T20:15:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** a6bec2ed4 (on `main`)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — inline the spec's `c-fox` critter (id 1t, fixed/never-rolled)
  into `web/shanes-life/public/index.html`'s `auth-mark` slot, replacing the generic app-icon PNG,
  per `Shanes Life 14 - Critters.dc.html` and the handoff README's Critters section. Full 23-slot
  critter roster port (peekers, room watermarks, daily roll, BuildConsole vector port) is real,
  separate scope — filing as its own finding, not attempting here.
- 2026-09-07T20:15:00Z ✅ DONE — added a shared `<defs>` block (once, near the top of `<body>`)
  holding the `gFox` gradient and `c-fox` `<symbol>`, then replaced the `/icons/icon-192.png`
  `<img>` in both `#login-view` and `#enroll-view`'s `auth-mark` slot with `<svg><use href="#c-fox">`.
  Path data diffed byte-for-byte against `Shanes Life 14 - Critters.dc.html`'s own `c-fox` symbol —
  identical apart from self-closing-tag syntax. Dropped the now-unused `.auth-mark { border-radius }`
  rule (was sized for the square PNG icon). Commit `a6bec2ed4`, rebased and pushed straight to
  `main` (verified via `verify-branch-merged.mjs`: OK). `git status --porcelain` clean.
  Filed the rest of the critter system (23 slots, peekers, room watermarks, daily roll, BuildConsole
  vector-roster port) as its own finding — **#3119**, parented under #3086 (no Feature-tier parent
  exists between #3117/#3119 and the Epic), board status set to "AI Batter Up". Not a same-scope
  fix: it's real, substantial UI work distinct from this one fixed slot.
