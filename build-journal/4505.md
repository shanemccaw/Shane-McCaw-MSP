# #4505 — Every Premier scan still ends partial: site-sharing fan-out counts a site with no document library as a failed item

- **Status:** ⏳ IN FLIGHT 2026-09-17T13:11:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-17T13:11:00Z ⏳ IN FLIGHT — confirmed root cause: `/sites/getAllSites` enumerates
  `mccawsoft2.sharepoint.com,cd796d82-2d17-473b-8b39-6177ef842677,df5e6502-…`
  ("McCawSoft Apps", `/sites/apps`, real Graph GET /sites/{id} → 200), which 404s
  `itemNotFound` on GET /sites/{id}/drive — verified live against the testbed
  (2026-09-17T13:06Z) via a real client-credentials Graph call. `runFanOutCheck` in
  `artifacts/api-server/src/lib/monitor-executor.ts` counted that 404 as a failed
  fan-out item on every run, so `compliance:eeeu-site-sharing` /
  `copilot:data-exposure-risk` always read `partial`, dragging every `core:premier`
  scan of the testbed down with them. Implementing: classify a per-item 404
  `itemNotFound` on `/sites/{id}/drive...` as an honest no-drive exclusion (new
  `isSiteNoDriveError` classifier + `sourceItemsExcludedNoDrive` counter in
  `_fanOut`), not a failure — same precedent as the existing branding/Intune
  "not configured" classifiers in the same file.
