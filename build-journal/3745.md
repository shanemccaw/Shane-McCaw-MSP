# Git #3745 — Generated MSP api-client type for GET /api/msp/customers stale

- **Status:** ✅ DONE 2026-09-13T04:34:00Z
- **Scope:** platform
- **Commit:** c9fa34451

## Log

- 2026-09-13T04:25:10Z — Confirmed live route response against
  `artifacts/api-server/src/routes/msp-portal.ts:1067-1150` before touching anything:
  the list route returns `{ id, name, domain, status, tenantId, mspId, createdAt,
  seats, people, lastScanAt, openSignals, criticalSignals }` per row (the
  `criticalSignals` field is real — #3746 landed it ahead of this build, confirmed by
  `build-journal/3746.md` DONE and its commit already on this branch's history), plus
  `{ customers, total, page, pageSize }` at the top level — matching the issue body
  except pageSize/criticalSignals weren't in the original filing snapshot.
  Also read the single-customer GET/POST/PATCH routes (lines 1055-1330), which return a
  different, wider shape (`name, domain, industry, tenantId, tenantUrl, status,
  isTestbed, createdAt, updatedAt` — no seats/people/openSignals) and use `name` (not
  `companyName`) throughout create/update bodies too — the generated
  `CreateMspCustomerInput`/`UpdateMspCustomerInput` were equally stale
  (`companyName`/`contactName`/`contactEmail`, none of which the real routes read).
- Regenerated from the real openapi source, `lib/api-spec/msp-openapi.yaml` (the
  actual source of truth `orval` builds `generated-msp/` from — not hand-edited
  generated output):
  - `MspCustomer` → `id, mspId?, name, domain?, industry?, status, tenantId?,
    tenantUrl?, isTestbed?, createdAt?, updatedAt?` (matches the single-customer
    GET/POST/PATCH response).
  - New `MspCustomerListItem` schema (the list route's real, narrower per-row
    shape) with the #3666/#3746 metrics fields, referenced by `MspCustomerList`.
  - `MspCustomerList.limit` → `pageSize`.
  - `CreateMspCustomerInput`/`UpdateMspCustomerInput` rewritten to `name` +
    `domain`/`industry`/`tenantId`/`status`, dropping the never-real
    `companyName`/`contactName`/`contactEmail`.
  - List query params: `limit` default 50→20, max 200→100 (real route caps at 100);
    `status` enum `all|active|inactive` → `all|active|inactive|onboarding` (the real
    statusFilter branch accepts `active|inactive|onboarding`).
  - Ran `npx orval --config ./orval.config.ts` from `lib/api-spec/` to regenerate.
    The same command also touched unrelated `lib/api-zod/src/generated/` (main,
    non-MSP api client) and `lib/api-client-react/src/generated/api.*` files —
    pure line-ending/whitespace churn from the installed orval version, not content
    tied to this fix — reverted those with `git checkout --` to keep this commit
    scoped to the MSP client.
- Switched `artifacts/msp-console/src/api/console-api.ts`'s `useDirectory()` to call
  the now-accurate generated `useListMspCustomers` hook instead of hand-rolling its
  own `fetch` against `/api/msp/customers` — the workaround #3745 itself named. Kept
  the console's own `DirectoryCustomer`/`DirectoryResponse` type aliases in place
  (structurally identical to the regenerated `MspCustomerListItem`/`MspCustomerList`)
  since they're threaded by name through ~20 files across the console (tree model,
  Sales/PolicyEngine/ScopeSla/change-control modules, risk register) — renaming them
  repo-wide was out of scope for this fix and would have been a pure rename with no
  behavior change.
- No new backend endpoint or schema needed — this was purely a client-generation
  staleness fix; no DB migration.
- Verification: `pnpm -w run typecheck:libs` (tsc --build across `lib/*`) clean;
  `artifacts/msp-console`'s own `tsc -p tsconfig.json --noEmit` clean. No other
  package (`admin-panel`, `msp-portal`, `api-server`) references
  `generated-msp`/`MspCustomer`, so no further blast radius to check.
- No fixture/hardcoded data introduced or touched.

- 2026-09-13T04:34:00Z — ✅ DONE — commit `c9fa34451` on `main`
  ("Regenerate MSP api-client for GET /api/msp/customers (Git #3745)"). Branch
  verified merged (`verify-branch-merged.mjs` — OK). `git status --porcelain`
  clean.
