# #1896 — 12 powershell config_resources cite Get-MSCloudLoginConnectionProfile as read_cmdlets

- **Status:** ✅ DONE 2026-09-04T07:08:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 1751e806a

## Log
- 2026-09-04T06:55:00Z ⏳ IN FLIGHT — found this issue's blocked_by edge pointed at #2007, which has a real
  verified DONE bookend (commit 1097ee55a, merged to origin/main) despite still being labeled `complete` and
  open (Shane hasn't closed it yet — expected per policy). Deleted the stale blocked_by edge, relabeled #1896
  in-flight, proceeding. Starting: query the 12 affected config_resources rows, read each resource's real
  Microsoft365DSC .psm1 source to find the actual read cmdlet, correct read_cmdlets via a manual migration.
- 2026-09-04T07:08:00Z ✅ DONE — Queried `config_resources` for `read_transport='powershell'` rows citing
  `Get-MSCloudLoginConnectionProfile` in `read_cmdlets`: 13 total, 12 matching the issue's "not reconciled"
  filter (the 13th, `EXOManagementRoleAssignment` id 10430, was already correctly reconciled against
  `Get-ManagementRoleAssignment` and out of scope). Fetched each of the 12 resources' real
  `Get-TargetResource` source directly from `raw.githubusercontent.com/microsoft/Microsoft365DSC` at commit
  `f79f297` (the same commit this catalog's `source_ref` cites) — individual ~10KB `.psm1` files, not the
  ~500MB tarball. Followed each call chain past the connection helper to the real cmdlet that performs the
  read: 6 resources call `Invoke-MgGraphRequest` directly; 4 resources (`AADVerifiedIdAuthority`,
  `AADVerifiedIdAuthorityContract`, `CommerceSelfServicePurchase`, `FabricAdminTenantSettings`, `SHSpaceUser` —
  actually 5) route through M365DSC's own internal `WorkloadHelpers/*.psm1` helpers
  (`Invoke-M365DSCVerifiedIdWebRequest`, `Invoke-M365DSCLicensingWebRequest`,
  `Invoke-M365DSCFabricWebRequest`, `Invoke-M365DSCServicesHubWebRequest`) that each wrap the real,
  exported `Invoke-WebRequest` cmdlet directly — traced those helper definitions via GitHub code search
  since they live outside the resource's own `.psm1`; `AzureVerifiedIdFaceCheck` keeps its already-real
  `Get-AzResourceGroup` and gets `Invoke-AzRestMethod` added (also real, Az.Accounts). Wrote and ran
  `lib/db/migrations/manual/2026-09-04-fix-connection-helper-read-cmdlets-1896.sql` locally against
  `DATABASE_URL` — 12 row `UPDATE`s in one transaction, self-marking INSERT into
  `simulator_migration_runs`. Verified post-migration: 0 `powershell`-transport rows still cite the helper
  (excluding the already-correct #10430); none of the 12 new cmdlet values previously appeared anywhere in
  `read_cmdlets`, confirming they're genuinely new real values, not duplicates of noise already in the
  catalog. Filed #2839 (parented under #1850, this issue's own Feature, board status "AI Batter Up") for the
  same connection-helper pollution class found in 64 rows across other transports (`graph` 49, `azure-rm` 7,
  `power-platform` 6, `sharepoint-admin` 2) — explicitly out of #1896's `powershell`-only scope, a real
  finding not fixed here. Cleared the stale `blocked_by` edge to #2007 at session start (see log above) —
  no remaining edges. Committed `1751e806a`, verified on `origin/main` via
  `verify-branch-merged.mjs`-equivalent (`git log --oneline` on `origin/main` after push shows the commit
  at HEAD). `git status --porcelain` clean.
