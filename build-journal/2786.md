# #2786 — Log Viewer: port LogViewerDock (full document)

- **Status:** ✅ DONE 2026-09-04T02:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** f40a31c74

## Log
- 2026-09-04T01:20:00Z ⏳ IN FLIGHT — Confirmed #2785 (LogService core) landed (labels: complete, commit d3edadac4). Read ShaneBuilder's real LogViewerDock XAML (MainWindow.xaml:2395-2574) and code-behind (MainWindow.xaml.cs:7488-8424, state fields + RenderLogViewer* family, stream COLD/BURST/LIVE, filter bar, Sources/Archive rail, right inspector + scratch pad). LogPeekPanel (MainWindow.xaml.cs:8425+) is explicitly out of scope — that's #2784's item 3 (a separate sibling issue), not this one. BuildConsole has no ShaneBuilder-style single-file tab/dock system; it uses a real TabControl (`EditorTabs`) hosting `UserControl` documents (precedent: `Controls/SqlDocumentView.xaml` opened via `OpenSqlRunnerTab()`, wired from the `_Terminal` menu's "Open SQL Runner" item). Porting LogViewerDock the same way: a new `Controls/LogViewerDocumentView` UserControl + an `OpenLogViewerTab()` opener wired into that same menu. Theme uses a real Catppuccin brush palette (BlueBrush/RedBrush/YellowBrush/GreenBrush/MauveBrush/etc, `Themes/DarkTheme.xaml`) — will map LogLevel/LogSource colors onto those instead of inventing new named resources; LogSource colors are already carried on `LogService.Sources[].Colour` from the #2785 port, so no duplicate resource needed there either. About to write the UserControl.
- 2026-09-04T02:05:00Z ✅ DONE — Shipped `desktop/BuildConsole/Controls/LogViewerDocumentView.xaml(.cs)`: real port of LogViewerDock (Sources/Archive left rail, filter bar — search/regex/exclude/HIGHLIGHT/saved filters/level pills/history scrubber, center stream, right inspector — filter chips/line detail/scratch pad with pin-survives-filter-change + copy-as-one-block) against BuildConsole's real, already-landed `Services/LogService.cs` (#2785). Same COLD/BURST(30s, self-cancels)/LIVE streaming model, mapped onto BuildConsole's real Catppuccin theme (`Themes/DarkTheme.xaml`) instead of ShaneBuilder's non-existent `Brush.*` keys; log-source colors read straight off `LogService.Sources[].Colour` (single-sourced, no duplicate resource table). Wired into `MainWindow` as `OpenLogViewerTab()`, opened via the `_Terminal` menu's new "Open Log Viewer" item (same precedent as "Open SQL Runner"). Went one step past a literal port: "send to chat" now routes through BuildConsole's real active-chat composer injection (`SendTextToActiveClaudeChatAsync`, the same #937/#940 path `SqlDocumentView` uses) instead of reproducing ShaneBuilder's clipboard-only workaround — that workaround existed there because ShaneBuilder's ClaudeChatDock had no owned composer to inject into; BuildConsole already solved that problem, so this uses the real path. `dotnet build BuildConsole.csproj -c Debug` — 0 errors, 0 warnings. Real verification via a throwaway console harness (`desktop/_tmp_verify_2786/`, deleted before commit, same precedent #2200/#2785 used): constructing the control resolved every real `StaticResource` key the XAML declares; `EnsureLoaded()` rendered the full document (rail/pills/lines/detail/scratch pad/chips) with zero exceptions; COLD `Query()` returned 2000 real lines across all 9 sources independent of streaming state; BURST started and self-cancelled back to COLD on its real Tick-handler path (accelerated countdown, same code path); a pinned line survived a search-text change. All 6 harness checks passed. No findings worth filing as separate issues this session — LogPeekPanel/BuildLogView decommission are already tracked as #2784's own items 3/4, not new discoveries. `git status --porcelain` clean of everything but this build's own files.
