# #2933 — Fix masked double-email risk from #2849's migration

- **Status:** ✅ DONE 2026-09-05T01:44:00Z
- **Scope:** platform

## Log
- 2026-09-05T01:20:43Z — Started. Confirmed via `notification-center.ts` there is currently no flag/option to suppress `createNotification()`'s own `deliverPreferenceEmail()`. Plan: add `suppressPreferenceEmail?: boolean` to `CreateNotificationOptions`, gate the `deliverPreferenceEmail` call on it, and set it `true` at the 4 call sites in `portal-messages.ts` and `admin-status-reports.ts` that already send their own templated email.
- 2026-09-05T01:44:00Z — ✅ DONE — Implemented option 2 from the issue: `notification-center.ts`'s `CreateNotificationOptions` gained `suppressPreferenceEmail?: boolean`, which now gates the `deliverPreferenceEmail(...)` call inside `createNotification()`. Set `suppressPreferenceEmail: true` at all 4 call sites named in the issue: `portal-messages.ts` (client-message-notification, admin-message-notification) and `admin-status-reports.ts` (status-report-reply, admin-thread-reply). In-app notification, unread-count broadcast, and webhook fan-out are unaffected — only the generic preference-gated Graph email is suppressed at these sites.
  While verifying, found and fixed a real pre-existing breakage in `portal-messages.test.ts`: #2849's migration added 2 extra `db.select` calls inside `createNotification` (the preference lookup, and `fanOutToCustomerWebhook`'s `resolveMspUserContext`) that the test's FIFO select-result mock queue was never updated for — the test was passing for the wrong reason (the real `clientUser` lookup silently returned `undefined` from an exhausted queue, so `sendEmailFromTemplate` was skipped entirely, and the assertion of "called 0 times" happened to be satisfied by a different bug). Fixed the mock queue ordering; test now genuinely exercises the code path.
  Added `notification-center-suppress-preference-email.test.ts` (registered in `vitest.config.ts`'s explicit include list) covering the flag directly: proves `deliverPreferenceEmail`'s real Graph `sendMessage` is suppressed when the flag is set (even with `emailEnabled: true`), and still fires normally for a caller that doesn't pass it.
  `tsc --noEmit` clean. `portal-messages.test.ts` + `notification-center-suppress-preference-email.test.ts` pass.
  Real commit: `052ccabb3b6d876ff77878add8e3b6d3cbf9dd2e`, rebased onto `origin/main` and landed as `998b4fc0525bb0b8efe87b8f8d555aae8503fec7` — verified merged via `scripts/dev-server/verify-branch-merged.mjs`.
  `git status --porcelain` clean at finish.
