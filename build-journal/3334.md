# #3334 — Service worker cache version never bumped since #3160

- **Status:** ✅ DONE 2026-09-09T18:56:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 74bcdf938

## Log
- 2026-09-09T18:49:30Z ⏳ IN FLIGHT — confirmed root cause via `git log` (public/sw.js untouched
  since b70115e58 / #3160): fixing the immediate CACHE version bump in `web/shanes-life/public/sw.js`
  and wiring a structural fix so it can't recur (tie the cache version to the real running deploy's
  git commit sha, computed once at server boot and substituted into `/sw.js` at serve time, instead
  of a manually-maintained string).
- 2026-09-09T18:56:00Z ✅ DONE — 74bcdf938 pushed to `origin/main`. `web/shanes-life/public/sw.js`'s
  `CACHE` now carries a `__SW_BUILD_ID__` placeholder; `web/shanes-life/src/server.mjs` computes
  `SW_BUILD_ID` once at boot (`git rev-parse --short HEAD`, cwd `config.root`; falls back to
  `Date.now()` if git is unavailable in the deploy environment) and serves `/sw.js` with that
  placeholder substituted in, instead of as a raw static file. Verified: `node --check` on both
  files, plus a real standalone run of the exact substitution logic against the live repo, which
  confirmed the resulting `CACHE` resolves to `shanes-life-shell-<real short sha>` and the
  placeholder is fully gone (`replaceAll`, after an initial `.replace()` attempt was caught
  matching the placeholder text inside the file's own preceding comment first -- fixed before
  landing). Did **not** boot the full `server.mjs` against a live DB for this: it connects and
  runs real migrations on start against Shane's real shared Postgres per `README.md`
  ("Shares ShanesSurvival's real Postgres"), which is not something to do for a one-line
  static-serving change. The issue's own "Verification" section already names the real live
  test as a post-deploy check (a real device with the old SW installed picks up the new
  app.js/app.css on next load) -- deploying is Shane's own action per `README.md`'s "Deploying
  to Replit" section, out of agent scope; that live check happens on his next real deploy.
  Working tree left clean; branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`.
