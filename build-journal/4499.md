# #4499 — Legacy /system/active-directory: replace with deprecation banner

- **Status:** ✅ DONE 2026-09-17T05:10:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** b93b0f78c

## Log
- 2026-09-17T05:00:00Z ⏳ IN FLIGHT — confirmed #4492/#4493/#4495 all real DONE bookends, replacing ActiveDirectoryPage.tsx content with a deprecation notice linking to `/adminv2/msp-directory`.
- 2026-09-17T05:10:00Z ✅ DONE — `ActiveDirectoryPage.tsx` (still routed at `/system/active-directory` via `SystemWorkspace.tsx`'s `case "active-directory"`) now renders only a "This page has moved" notice with a button that navigates to `/adminv2/msp-directory` (real deep-link path, confirmed by reading `ShellContext.tsx`'s `ADMINV2_BASE`/`navigate` logic — `subRoute` strips `/adminv2`, so `navigate("/msp-directory")` internally maps to `/adminv2/msp-directory` externally). All data-fetching removed — the page no longer imports `ActiveDirectoryTree`/`ActiveDirectoryCenterCanvas`. Left `ActiveDirectoryCustomerPane.tsx`/`ActiveDirectoryTree.tsx`/etc. as dead code per issue scope (later cleanup, not required here). Verified: `pnpm --filter admin-panel exec tsc --noEmit -p .` clean, no errors. Branch verified merged into origin/main (285df7bff), working tree clean.
