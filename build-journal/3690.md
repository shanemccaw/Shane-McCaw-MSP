# #3690 — IssueDetailView ObjectDisposedException race (WebView2 Loaded/NavigationCompleted vs disposal)

- **Status:** ✅ DONE 2026-09-11T19:32:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** f0ebfcea9

## Log
- 2026-09-11T19:24:37Z ⏳ IN FLIGHT — read issue #3690 body/comments, located `RenderChatColumnAsync` in `desktop/BuildConsole/Controls/IssueDetailView.xaml.cs`. Plan: extend the existing `myRequest != _requestId` stale-guard into the `Loaded` and `NavigationCompleted` handlers, plus wrap the actual `.CoreWebView2` access in try/catch for `ObjectDisposedException` as a defensive backstop.
- 2026-09-11T19:32:00Z ✅ DONE — `NavigationCompleted` now checks `myRequest != _requestId` before touching `wv` at all, and catches `ObjectDisposedException` specifically around `ExecuteScriptAsync`. `Loaded` checks the same stale-request guard first, then wraps the actual `wv.CoreWebView2` read/`Navigate` call in try/catch for `ObjectDisposedException` (the null-check alone wasn't a safe read once disposed — accessing `.CoreWebView2` on a disposed control is itself what throws). `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors, only 6 pre-existing unrelated warnings (FocusModeBar unused events). Commit f0ebfcea9 pushed directly to `origin/main`, verified merged via `scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain` clean. No fixture/hardcoded data involved — pure defensive-code fix, no new findings to file.
