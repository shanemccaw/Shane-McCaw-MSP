# #2826 — health-suite/useTopicHealthLive.ts and its 6 Live hook consumers are transitively orphaned

- **Status:** ✅ DONE 2026-09-04T15:30:34Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 59ba96640 (this bookend, IN FLIGHT)

## Log
- 2026-09-04T15:28:22Z ⏳ IN FLIGHT — investigating whether the 7-file orphaned Live-hook chain (useTopicHealthLive.ts + its 6 consumers) should be wired into a real page or deleted, per the issue's own instructions.
- 2026-09-04T15:30:34Z ✅ DONE — Investigation-only resolution, no code changed. The chain
  (`useTopicHealthLive.ts` + `useHltDriftLive.ts`, `useHltObjectsLive.ts`,
  `useSecAreaLinksLive.ts`, `useSecEvidenceEmailLive.ts`, `useSecEvidenceLegacyAuthLive.ts`,
  `useSecEvidenceOauthLive.ts`) is the surviving data-seam half of the old portal-v2
  pillar-page wiring — commit `f40438cdc` carried the hooks into the new `artifacts/portal`
  scaffold but deleted the 8 presentational components that rendered them, since portal-v2
  itself was retired. `artifacts/portal/src/pages/` has no governance/compliance/adoption/
  copilot/architecture/licensing/security page yet. The owning Feature is **#1621**
  (Pillar Pages, Finding Drill-down & Remediation Offer), whose own body states it is "at
  the architecture stage" with unresolved questions and no `Design/portal/` export for any
  pillar page. Per CLAUDE.md's mandatory order (architect → endpoints → contract pack →
  Design → wire), wiring now would repeat the exact "UI against prose" failure #1621 exists
  to avoid. The endpoints these hooks call are real and already proven
  (`/api/portal/diagnostics/status`, `/api/portal/mission-control/overview`,
  `POST /api/dashboard/resolve`), so deleting would throw away correct, reusable work.
  **Decision: keep the code as-is, don't wire, don't delete.** Re-parented #2826 from Epic
  #1485 to Feature #1621 (via `sub_issue_write add --replace_parent`), wired a real
  `blocked_by` edge #2826 → #1621, set labels `bug`+`blocked` (removed `in-flight`), and
  posted a full explanatory comment on #2826. `verify-branch-merged.mjs` confirms this
  branch's commit is an ancestor of `origin/main` (fast-forward push,
  `agent/2826-q1604:main` → `566274751`). `git status --porcelain` clean.
