# #3902 — LoadCore() hidden Save() side-effect / PAT wipe

- **Status:** ✅ DONE 2026-09-13T01:30:26Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 555bb0dc6

## Log
- 2026-09-13T01:15:24Z ⏳ IN FLIGHT — remove Save() from BuildConsoleSettings.LoadCore() (Path A) and add a write-side guard so no Save() can persist a blank credential over a real one on disk.
- 2026-09-13T01:30:26Z ✅ DONE — 555bb0dc6. LoadCore() had THREE hidden Save() calls (WebTools Git, ConfiguredRepos, UserAccounts backfills); all removed, backfills in-memory only. Save() now: refuses Degraded (unreadable-file defaults) instances; refuses MissingFile instances once the file exists; re-reads disk under a cross-process mutex and carries forward a real GitHubPat/ZohoApiToken rather than blanking it (refuses the write if disk unreadable and a credential is blank); unique temp file per write; replace retried. Reads share Delete and take the same lock. SaveClearingCredential(name) is the only clear path (Settings tab Save buttons, empty box). Root cause found in verification: File.Move replace throws UnauthorizedAccessException while any reader has settings.json open; the old in-LoadCore Save() hit that, Load() didn't retry that exception type and returned blank defaults, which callers then Save()d = PAT wipe. Path B: live settings.json Git WebTools URL matches the computed string exactly today (backfill not firing on current file). Verification: dotnet build 0 errors; scratch harness against compiled BuildConsole.dll in an isolated --instance folder, 13/13 PASS incl. 8s 8-writer/8-reader storm (0 blank reads, PAT intact); same harness on pre-fix origin/main fails 4 guard checks and crashes in the storm. BuildConsole.exe not launched. Finding filed: #3907 (last-writer-wins for non-credential fields), under #1708, AI Batter Up. No migrations.
