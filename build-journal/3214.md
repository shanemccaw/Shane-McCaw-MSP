# #3214 — Feature: Settings redesign (Account, Passkeys, Notifications, Health context, Claude tokens, Widget management, Recent activity) — Shane's Life

- **Status:** ✅ DONE 2026-09-08T05:25:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 00aceaed0 (implementation), d3c7ea12c (merge into main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Investigated: most of #3214's scope (Account/Passkeys/Notifications/Health
  context/Claude MCP tokens/Widget tokens/Recent activity) is already built in `viewSettings()`
  (public/app.js). Confirmed screenshot `v4-settings-room-order.png` is actually sibling issue
  #3215's scope, not this one. Real gaps against the account + claude-widget screenshots:
  1. Account card is missing the "Signed in with a passkey. No password exists for this account."
     statement.
  2. Home Screen widget has no screenshot-count usage signal or "Preview the widget page →" link
     (046_widget_tokens.sql explicitly chose not to track a call count for widgets — the design
     now calls for exactly that, so extending the schema).
  3. Recent activity shows only the bare action string, not the real per-client tool-call summary
     the screenshot shows (e.g. "Claude Desktop · set_medication · Vitamin D3, morning, auto-refill").
  Implementing all three now.
- 2026-09-08T05:25:00Z ✅ DONE — Shipped all three real gaps:
  1. Account card now states "Signed in with a passkey. No password exists for this account."
     (real fact -- migration 013 never added a password column at all, verified by grep).
  2. `migrations/051_widget_token_screenshot_count.sql` adds `widget_tokens.screenshot_count`,
     incremented on every real `/widget/t/:token` render in `resolveWidgetToken`. UI now shows
     "N screenshots · last X ago" (new `agoShort()` helper) instead of a bare "last loaded".
     Added a session-authed `GET /api/widget-preview` route (reuses the same `computeNextCard`
     + `renderWidgetPage` the real widget uses) behind a new "Preview the widget page →" link.
  3. Recent activity now shows a real per-client tool-call summary ("Claude Desktop ·
     set_medication · Vitamin D3, morning"), not just the bare internal action string. The real
     MCP tool name is captured automatically via a new `src/mcp/tool-context.mjs`
     (AsyncLocalStorage), set once in protocol.mjs's one dispatch point, and read back inside
     `audit.record()` -- no changes needed to any of the ~50 individual tool handlers.
  Reused existing real audit-log infra (`activity_log`/`audit.record`) rather than building a
  second log, per the issue's own ask.

  Migration note: local Postgres ledger briefly collided with two other concurrent sessions'
  in-flight migrations (049_bill_cycle_snapshots.sql, 050_room_order.sql, from siblings not yet
  on origin/main) — a known, documented condition (Git #3166/#3175), not a defect in this work.
  Resolved by merging origin/main (which had since gained both files) before finishing; ran
  `node bin/migrate.mjs` clean afterward (38 already applied, none pending) and confirmed with a
  live server boot (`node src/server.mjs`) plus a real `curl` against `/api/widget-preview`
  (401 unauthenticated, as expected) and `/` (200). Backend behavior (tool-name auto-capture,
  screenshot_count increment) was also directly verified against the real local DB with a
  throwaway script (audit row showed `detail.tool: "set_medication"`; two resolves produced
  `screenshot_count: 2`), then the test rows were deleted and the script removed.

  Verified `git merge-base --is-ancestor` clean via `scripts/dev-server/verify-branch-merged.mjs`
  after pushing to `origin/main` directly (`d3c7ea12c`). Working tree clean at finish.

  Not touched: Passkeys, Notifications, Health context, Claude (MCP) token list, and the base
  widget-token/MCP-token CRUD — all already fully built and matched the design screenshots as
  found. `v4-settings-room-order.png` is sibling issue #3215's scope, not this one (confirmed via
  `gh api .../3086/sub_issues`).
