# #3214 — Feature: Settings redesign (Account, Passkeys, Notifications, Health context, Claude tokens, Widget management, Recent activity) — Shane's Life

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Investigated: most of #3214's scope (Account/Passkeys/Notifications/Health
  context/Claude MCP tokens/Widget tokens/Recent activity) is already built in `viewSettings()`
  (public/app.js). Confirmed screenshot `v4-settings-room-order.png` is actually sibling issue
  #3215's scope, not this one. Real gaps against the account + claude-widget screenshots:
  1. Account card is missing the "Signed in with a passkey. No password exists for this account."
     statement.
  2. Home Screen widget has no screenshot-count usage signal or "Preview the widget page →" link
     (046_widget_tokens.sql explicitly chose not to track a call count for widgets — the design
     now calls for exactly that, so extending the schema).
  3. Recent activity shows only the bare action string, not the real per-client tool-call summary
     the screenshot shows (e.g. "Claude Desktop · set_medication · Vitamin D3, morning, auto-refill").
  Implementing all three now.
