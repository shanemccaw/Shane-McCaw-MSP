# #1643 — deploy-shanesbuild.cmd never pulls before building

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** ab3a3dad7

## Log
- 2026-08-29 ⏳ IN FLIGHT — adding a fail-closed `git pull` step before `dotnet build` in `desktop/BuildConsole/deploy-shanesbuild.cmd`, printing the resulting commit after a successful pull.
- 2026-08-29 ✅ DONE — `ab3a3dad7`. Added, before the clean/build step: (1) a `git status --porcelain` dirty-tree check that fails closed with a clear message and exit 1; (2) `git fetch origin main` + `git merge --ff-only origin/main`, failing closed (exit 1, clear message) on fetch failure or on non-fast-forward (diverged history); (3) prints `git log -1 --oneline` after a successful pull so the deployed commit is visible. No changes to the existing clean/build/stop-process/robocopy/relaunch flow. Verified in an isolated throwaway git repo (not the real checkout) by simulating: up-to-date run (no-op pull, correct commit printed, exit 0), behind-origin run (fast-forwards, prints new commit, exit 0), dirty working tree (fails closed, exit 1, no pull attempted), and diverged local history (fetch succeeds, ff-only merge fails, exit 1). All four matched expected behavior. Branch verified ancestor of origin/main; working tree clean.
