# #1526 — Policy Decisions: Dependency-based clearance — decisions that are not on a date cycle

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 0dcbc6d32 (feature), 2ce07a76a + 92aa0fe17 (merge/push to main)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Issue body pre-dates #1528's split of Policy Decisions
  into its own `policy_decisions` table (portal-policy-decisions.ts); building the
  dependency-clearance columns there (the current live object) rather than on
  `msp_risk_decisions`. Schema + manual migration
  (`2026-08-31-policy-decision-dependency-clearance-1526.sql`) run against local
  DB: `review_cadence`/`review_state` now nullable, new `clearance_condition` /
  `clearance_trigger_type` / `clearance_trigger_sku_part_number` /
  `clearance_resolved_at` / `clearance_resolved_note` columns, a
  `policy_decisions_review_xor_clearance_chk` CHECK, and a seeded
  `policy_clearance_resolved` `msp_alert_rules` row. Next: create/read wire path
  in `portal-policy-decisions.ts`, manual mark-resolved endpoint, and the
  observable `license_sku` detector in `alert-engine.ts`.
- 2026-08-31 ✅ DONE — Wired `portal-policy-decisions.ts`: create schema now
  takes `reviewCadence` XOR `clearanceCondition` (zod `superRefine`, mirroring
  the DB CHECK), plus `PATCH /api/portal/policy-register/:id/clearance/resolve`
  for the not-machine-observable ('manual') half. `alert-engine.ts` gets
  `advancePolicyClearances()`, the observable half — reuses the tenant's
  already-collected `/subscribedSkus` data (`service-availability.ts`'s
  `SUBSCRIBED_SKU_CHECK_KEYS`, now exported) to auto-resolve a `license_sku`
  dependency the moment it lands, no new Graph call. 14 new vitest cases
  (`portal-policy-decisions.test.ts`, registered in `vitest.config.ts`'s
  explicit include list) — all pass.
  During the build, hit a real cross-worktree infrastructure bug: the shared
  MAIN checkout's own `node_modules/@workspace/db` junction was poisoned,
  pointing at an unrelated stale worktree (`C:\wt\1525-q941\lib\db`), which
  made every worktree session's `tsc` resolve `@workspace/db` against a stale
  type shape. Diagnosed and repaired with the documented
  `node scripts/dev-server/store-doctor.mjs --repair` (no `pnpm install`
  used). Filed as its own finding: **#2097** (parented under #1202, milestone
  v1.1, board status "AI Batter Up") — also names a related gap (no isolated
  way for a worktree session to typecheck its own edit to a `lib/*` package
  without writing through the shared junction into the main checkout's
  `dist/`, which happened here and was reverted by rebuilding main's own
  `dist` from main's own committed source before proceeding).
  Merged with a concurrent, independent build (#1527, "Policy decision review
  overdue" — the DATE-clock counterpart on the same table) via two
  `git merge origin/main` passes; both clocks coexist cleanly by construction
  (a dependency-based row always has `review_due_at = NULL`, so
  `advancePolicyReviewClock` never touches it). Verified: `origin/main` HEAD
  `92aa0fe17` — `verify-branch-merged.mjs` confirms ancestor; `git status
  --porcelain` clean; 30/30 relevant vitest tests pass
  (`portal-policy-decisions.test.ts` + `portal-risk-register.test.ts`);
  `policy_decisions` confirmed empty locally both before and after (0 rows),
  so this was a pure additive/structural migration with nothing to backfill.
  Findings filed: #2097.
