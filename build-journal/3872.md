# #3872 — Optional --epic override on the BUILD: dispatch header

- **Status:** ⏳ IN FLIGHT 2026-09-13T00:05:37Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-13T00:05:37Z ⏳ IN FLIGHT — investigated real parsing sites (BuildPromptHeader's
  `--flag value` grammar is the actual home for `--model`/`--effort`/`--title`/`--blocked-by`,
  not `BatterUpQueueService.ParseBuildComment`'s `key=value` BUILD: line, despite the issue
  body's wording); about to add `--epic <N>` parsing there, a new `epic_number` column on
  `bt_build_queue` (BUILD_DATABASE_URL), wire it through `QueueBuildAsync`, and make
  `ResolveBuildSetEpicsAsync` prefer an explicit override over `EpicResolver` DB inference.
