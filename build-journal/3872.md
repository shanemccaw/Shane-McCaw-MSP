# #3872 — Optional --epic override on the BUILD: dispatch header

- **Status:** ✅ DONE 2026-09-13T00:19:11Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** fb84193b3 (implementation, on origin/main)

## Log
- 2026-09-13T00:05:37Z ⏳ IN FLIGHT — investigated real parsing sites (BuildPromptHeader's
  `--flag value` grammar is the actual home for `--model`/`--effort`/`--title`/`--blocked-by`,
  not `BatterUpQueueService.ParseBuildComment`'s `key=value` BUILD: line, despite the issue
  body's wording); about to add `--epic <N>` parsing there, a new `epic_number` column on
  `bt_build_queue` (BUILD_DATABASE_URL), wire it through `QueueBuildAsync`, and make
  `ResolveBuildSetEpicsAsync` prefer an explicit override over `EpicResolver` DB inference.
- 2026-09-13T00:19:11Z ✅ DONE — shipped in fb84193b3 (pushed to origin/main):
  `BuildPromptHeader.ParseEpicNumber(prompt)` (new `--epic <N>` parser, same grammar as
  `--blocked-by`); new nullable `epic_number` column on `bt_build_queue`
  (`lib/db/migrations/manual/2026-09-13-build-queue-epic-number-3872.sql`, run myself against
  local `BUILD_DATABASE_URL` and self-marked in the product `DATABASE_URL`'s
  `simulator_migration_runs`); `QueueBuildAsync` reads the header and writes the column on both
  insert and update, same pattern as the existing `--blocked-by` header enforcement;
  `QueueItem.EpicNumber` threaded through `GetQueueAsync` (new optional trailing ordinal 27,
  #1384 fixed-ordinal contract preserved) and `GetQueueChangeStampAsync`'s change-detection
  hash; `BuildQueuePanel.ResolveBuildSetEpicsAsync` now lets a member's explicit `EpicNumber`
  bypass `EpicResolver`'s `parent_number` walk directly, falling back to real DB inference for
  every member with no override; `BUILD_QUEUE_METHOD.md` §4.1 documents the new flag.
  Verified: `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. `ParseEpicNumber` checked
  against 7 real header shapes (with/without the flag, `#`-prefixed, non-numeric, empty/null) —
  all passed. Real end-to-end round-trip against the live local `BUILD_DATABASE_URL`:
  `QueueBuildAsync` with a `--epic 1788` header → `bt_build_queue.epic_number` → `GetQueueAsync`
  → `QueueItem.EpicNumber == 1788`; probe row (id 2558) deleted after. Branch verified merged
  into `origin/main` via `verify-branch-merged.mjs` (exit 0); working tree clean.
  No findings filed — no genuine bug/gap surfaced beyond the issue's own scope.
