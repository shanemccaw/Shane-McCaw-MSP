# #3623 — URGENT: Build #3585 was claimed and ran despite real, open blocked_by dependencies (#3582, #3584)

- **Status:** ✅ DONE 2026-09-11T03:44:29Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 9508f13f6 (fix); bookend commits 1a9ff9c1e (IN FLIGHT) + this one
- **Findings filed:** #3627, #3628 (both sub-issues of #1788, labeled `bug`, board status AI Batter Up)
- **Migrations:** none (no schema change)
- **Pre-existing dirty files:** none

## Log
- 2026-09-11T03:10:00Z ⏳ IN FLIGHT — Pulled #3623's full body + dispatch comment, and #3585's body + comments. Starting evidence-first investigation: how #3585 was actually launched (tick claim vs. alternate path), whether `bt_build_queue.blocked_by_numbers` was populated from `--blocked-by 3582,3584`, whether a live GitHub blocker fetch ran before the claim, and why #3585's claimed bookend commit is not on `origin/main`. No fix written until the failing layer is proven from real data.
- 2026-09-11T03:44:29Z ✅ DONE — Root cause proven from real data, fixed in 9508f13f6, verified 50/50 against real rows.
  - **How #3585 launched:** Batter Up free-flow auto-queue (activity log 20:28:31 local, no `blocked-by=` in the line), then the normal tick claim (`Build set 'MultiRepo': launching member 3585`). No Start Now / Run Now.
  - **blocked_by_numbers:** NULL on row 2260 (and on #3582/#3584's rows 2256/2258 and #3624's row 2290), though each prompt's header carried `--blocked-by`. 29 rows since 2026-09-02 have this shape. The issue mirror fetches `blocked_by` only for `blocked`-labeled issues and writes `{}` for every other issue. #3585's edges were wired 00:19:42Z; the label came at 00:52:48Z, from its own session. Batter Up queued from the mirror's view and never read the header flag.
  - **Live fetch before claim:** it ran, but only for the own-issue check (#3585 open). The row declared no blockers, so there was nothing to check. The gate behaved correctly on the empty set it was given.
  - **Session self-check (layer 2):** it did run. #3585's session detected the open blockers, labeled the issue `blocked`, commented, and wrote a BLOCKED bookend, but b8f1f4836 is only on agent/3585-q2260 / dev-server, never pushed to main (same for #3584's e5baf29f6). The row read Verifying on exit 0. Filed as #3628.
  - **Fix (9508f13f6):**
    - (A) `QueueBuildAsync` always enforces the prompt header `--blocked-by` (shared parser: `Services/BuildPromptHeader.cs`).
    - (B) Batter Up reads live GitHub edges before inserting; if that read fails, it doesn't queue (fail closed).
    - (C) The claim path re-reads live `blocked_by` edges for every would-be-claimed row, holds on any open edge the row was missing, and records it on the row.
    - (D) Periodic re-sync (3 min, or ~20s after a board refresh) merges live edges into every tracked row. This covers Shane's #3610 → #3625 mid-run case. Add-only.
    - (E) The open-issue list cap was 500 with 505+ open issues, so #77/#350–#353 read as closed in the gate. The cap is now 5000, and a full-cap result reports failure.
  - **Verification:**
    - Build clean (0 errors; same 6 warnings as baseline).
    - Offline harness against real DB rows + real GitHub, 50/50 PASS:
      - OLD gate on real row 2260 → READY (bug reproduced).
      - Fix C → HELD "waiting on #3584, #3582 (open)".
      - Fix A → HELD by the plain stored-set gate, and still held when the edge read fails.
      - #3610's row 2291 held on #3625.
      - Control (open issue, no edges) stays READY.
      - 500-cap list now fails closed.
      - All 29 gap rows' headers parse.
      - A real parked `QueueBuildAsync` insert of #3585's prompt wrote {3582,3584} (row deleted).
      - Re-sync dry run found 10 real rows missing live edges, incl. #3610 +#3625.
    - Edge-recording SQL dry-tested on a TEMP table copy (merge-not-replace confirmed).
  - **Honest gaps:**
    - The live BuildConsole still runs the pre-fix build until Shane rebuilds/restarts it from main ≥ 9508f13f6 (labeled Shane To-Do).
    - Applying the re-sync to the 10 real rows from the harness was denied by the session's permission classifier, so those rows are unchanged; the shipped re-sync corrects them on its first run after restart.
    - Re-sync is add-only by design: removing a GitHub edge does not release a queued row (Start Now does). Flagged to Shane as a decision.
    - Rows 2256/2258 relaunched at 23:30 local legitimately (#3579/#3581 both closed by then).
