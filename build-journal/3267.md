# Git #3267 — Cyclical/course medication tracking (Terbinafine)

- **Status:** ✅ DONE 2026-09-08T20:48:17Z
- **Scope:** platform (web/shanes-life -- Shane's Life personal project, Epic #3086 / Feature #3226)

## Log
- 2026-09-08T00:00:00Z -- Session start. Read #3267: sub-issue of #3226 (Meds Feature),
  Epic #3086 (Shane's Life). Real gap: `medications` has no concept of a cyclical course
  window (e.g. Terbinafine, 15 days on then dormant for a repeating cycle). Plan: add
  `course_start_date`/`course_active_days`/`course_cycle_days` columns (nullable), teach
  `getMedsToday`'s batch computation to skip a course medication outside its active window,
  extend `set_medication` MCP tool + REST route with the new fields, no UI change needed
  beyond "just don't appear when dormant" (already true once excluded from batch items).
- 2026-09-08T20:48:17Z -- ✅ DONE. Confirmed local `finances` DB (the real dev DB for this
  personal app) had zero `medications` rows -- the real Terbinafine row the issue references
  lives on the deployed Replit instance, not local dev, and isn't reachable from this session.
  - `web/shanes-life/migrations/061_medication_course_cycle.sql` -- added `course_start_date`
    (date), `course_active_days`/`course_cycle_days` (integer) to `medications`, all nullable,
    with CHECK constraints (all-or-none; active_days > 0 and <= cycle_days). Ran it myself
    against local `DATABASE_URL` (`finances` db) -- `applied=1 alreadyApplied=47`.
  - `web/shanes-life/src/core/medications.mjs` -- `cleanCourseFields` (all-or-none validation,
    with partial-update merge so resetting just `courseStartDate` on an existing course works --
    the issue's own "starting my 15-day Terbinafine course today" capture case) and
    `isCourseActiveToday` (days-since-start modulo cycle-days window check); wired into
    `createMedication`, `updateMedication`, and `getMedsToday`'s batch-building loop (a med
    outside its active window is skipped from batch items -- no separate paused UI, matches
    scope item 4). REST routes in `src/routes/api.mjs` were already thin passthroughs, no
    change needed there.
  - `web/shanes-life/src/mcp/tools.mjs` -- extended `set_medication`'s schema/description with
    `courseStartDate`/`courseActiveDays`/`courseCycleDays`.
  - Verified live against the local DB with a real course medication row: created one started
    5 days ago (15-active/75-cycle) -- appeared in its `night` batch; created a second started
    20 days ago (past its 15 active days, still mid-cycle) -- correctly absent; reset the
    second one's `courseStartDate` to today via a partial update (the "starting my course
    today" capture shape) -- correctly reappeared; cleared all three fields back to null --
    confirmed row reads back as an ordinary (non-course) medication. All four real assertions
    passed. Scratch verify script deleted after the run, not committed.
  - Did NOT do the issue's "immediate follow-up" (migrating Shane's real Terbinafine row into
    these fields) -- that data doesn't exist in the reachable local DB, and the issue itself
    says to ask Shane for the real `course_cycle_days` ("a couple of months" isn't precise
    enough to compute from) before it can be set. That's a real product-decision stop, not a
    build gap; flagged in the issue comment for Shane.
  - Commit: 128a1b7b "Cyclical/course medication tracking for Meds (Git #3267)". Branch
    verified merged into origin/main via `verify-branch-merged.mjs`. `git status --porcelain`
    clean.
