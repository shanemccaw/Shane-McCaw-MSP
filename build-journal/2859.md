# #2859 — Retention: register the real per-module tenant-data purgers for the 7-year post-termination purge

- **Status:** ✅ DONE 2026-09-06T14:47:21Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `087787f18` — Per-module tenant-data purgers for the 7-year post-termination purge (#2859)

## Log
- 2026-09-06T13:52:47Z ⏳ IN FLIGHT — starting. #2765 built the post-termination purge scheduler; `registerTenantDataPurger()`'s registry ships empty so `purgeTerminatedTenant()` refuses with `no_purgers_registered`. This session builds the real per-module purgers, wires them into api-server startup, and verifies against the real local PostgreSQL that they fire only for genuinely due tenants.
- 2026-09-06T14:47:21Z ✅ DONE — shipped in `087787f18`.

  **What shipped**
  - `artifacts/api-server/src/lib/retention/purgers/modules.ts` — 18 module declarations
    (change-control, risk-register, config-drift, monitoring, alerting, engine-scoring,
    policy-engine, sla, scope-creep, ownership, workflows, compliance, documents,
    commercial, tenant-integrations, message-centre, platform-ops, directory) covering
    every tenant-scoped column the running database has.
  - `purgers/declare.ts` — the shared execution shape: four explicit id spaces
    (`customerId` = tenants.id, `tenantGuid` = the M365 GUID, `userId` = this tenant's
    users.id, and a guarded `ambiguousCustomerId` for the seven legacy tables the codebase
    reads both ways), `to_regclass` skip-and-report for tables absent on an environment
    (#1471's real staging lag), identifier validation, and a per-module `audit`-channel
    line with per-table counts.
  - `purgers/coverage.ts` + `tenant-scope-coverage.live-db.test.ts` — the anti-rot
    guarantee: the test asks the RUNNING database what is tenant-scoped and fails on
    anything no module claims and no exemption explains. It caught two real gaps during
    this session, including a table another in-flight session added mid-run.
  - `purgers/index.ts` — `registerAllTenantDataPurgers()`, called once from
    `src/index.ts` in the same block that schedules the sweep. Explicit call, not an
    import side effect, so the refusal path stays reachable and importing the barrel
    cannot arm an irreversible destructive path.

  **Declarations name real SQL tables, not Drizzle symbols** — eleven genuinely
  tenant-scoped tables (`scope_creep_*` ×6, `sla_*` ×4, `war_room_interaction_events`)
  exist only as manual migrations with no ORM definition, so an ORM-based purge would
  silently skip all of them.

  **Verification — real, against the local PostgreSQL, not asserted**
  - `post-termination-purge.live-db.test.ts`: two scratch tenants under one scratch MSP,
    one lapsed 8 years (due) and one lapsed 1 year (not due), both seeded with real rows
    in three id spaces across three different modules. The due tenant's rows are destroyed
    and it is stamped `post_termination_purged_at`; the not-due tenant beside it is
    untouched and unstamped; an empty registry still refuses without touching a row;
    a second purge reports `already_purged`; a second arming call is a no-op, not a throw.
  - `pnpm vitest run src/lib/retention` — **5 files, 78 tests, all passing.**
  - `npx tsc --noEmit -p tsconfig.json` — **clean, exit 0.** (It caught a real defect:
    `status: "cancelled"` is not in the tenants status union; corrected to `"inactive"`.)
  - Scratch rows confirmed fully removed afterwards by direct psql query.
  - Branch ancestry confirmed: `node scripts/dev-server/verify-branch-merged.mjs` → OK.

  **Findings filed** (all set to board status "AI Batter Up"):
  - **#2980** — `retention_reinstatement_requests` is tenant-scoped but no purger claims
    it, so it survives the purge. Sub-issue of #1944. Recorded in `TENANT_SCOPE_UNCLAIMED`
    rather than silently declared, because the table is not on main and belongs to a
    sibling build.
  - **#2983** (`bug` + `security`) — `customer_id` means BOTH `tenants.id` and
    `users.id` on seven tables. `portal-privacy.ts` reads `tenant_signal_history` with a
    `tenants.id` against a column the live FK constrains to `users.id`, so a data-rights
    export can return a different principal's rows; the testbed-reset migration misses all
    6,590 live rows for the same reason. Sub-issue of #1944.
  - **#2984** — the purge leaves the customer's `users` rows intact. A real product
    decision (~50 FK edges into `users(id)`, and the `SET NULL` set blanks audit
    attribution), so it is handed over rather than settled inside a table declaration.
    Recorded as a stated exemption in `coverage.ts`. Sub-issue of #1944.
  - **#2985** (URGENT) — the MAIN checkout's `artifacts/api-server/node_modules` is empty
    (1 entry vs 36 in the dev-server checkout), so every worktree junctioned to it cannot
    resolve a single dependency, and `store-doctor.mjs` reports CLEAN in that state.
    Parented to #1202 (Build Console) rather than #1944 because it is a worktree/dev-server
    defect, not a retention one.

  **Environment note.** #2985 blocked all typecheck/test work at the start of this session.
  Per CLAUDE.md no `pnpm install` was run and nothing was downloaded; resolution was
  restored by re-running the repo's own `scripts/dev-server/link-deps.mjs` against the
  intact `C:\dev-server` checkout (41 junctions created), and `lib/*/dist` was built
  per-worktree with the same module's `buildLibDist()`. This worktree's
  `artifacts/api-server/node_modules` therefore junctions into `C:\dev-server` rather
  than the main checkout — untracked, and unlinked normally by `cleanup-worktree.mjs`.

  **Honest gaps:** the `users` rows (#2984) and `retention_reinstatement_requests`
  (#2980) are NOT purged; both are recorded in source with reasons and filed. #2985 could
  not be parented to #1202 — GitHub refused with "Parent cannot have more than 100
  sub-issues", so it is filed and on the board but unparented.
