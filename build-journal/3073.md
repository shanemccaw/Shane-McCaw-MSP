# Git #3073 — ChatDockService board-status/chain-walk fan-out burst

- **Status:** ✅ DONE 2026-09-07T00:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-07T00:00:00Z — Session start. Investigating whether #2889's bounded-concurrency
  fix covers ChatDockService's per-mention internal `GetIssueBoardStatusAsync` +
  `WalkChainAsync` (blocked-by/blocks) fan-out, or whether that's a separate uncovered gap.
- 2026-09-07T00:20:00Z — ✅ DONE — Real gap confirmed: `ChatDockService.BuildAsync`'s
  #2889 gate (`using var gate = new SemaphoreSlim(MaxConcurrentItemFetches)`) is
  constructed LOCAL to each call, so it only bounds concurrency WITHIN one chat dock's
  own refresh. `MainWindow.xaml.cs`'s `BoardRefreshCompleted` cascade (#2688) fires
  every open chat tab's `ChatDocumentContainer.RefreshDetectedAsync` off the SAME event
  dispatch — each independently builds its own local gate of 3. With N open chat tabs
  mentioning distinct issue numbers, that's N×3 real GitHub calls bursting at once on
  one board refresh — exactly the "dozens of distinct numbers within milliseconds"
  symptom in #3073, and #2889's internal per-item fan-out (title + board-status + 2
  chain walks) was never the actual gap the issue asked about.
  Fix (`desktop/BuildConsole/Services/ChatDockService.cs`): promoted the gate to a
  `static readonly SemaphoreSlim SharedItemFetchGate`, shared process-wide across every
  concurrent `BuildAsync` invocation (every chat dock), not per-call — total in-flight
  GitHub calls for chat-dock enrichment now caps at `MaxConcurrentItemFetches` (3)
  app-wide regardless of how many docks refresh at once. Also added a per-issue-number
  cooldown (`MetadataFetchCooldownUntil`, 60s, matching #2890's established shape) so a
  persistently-failing number's board-status/chain fetch isn't re-attempted on every
  dock refresh across every chat tab that mentions it.
  Verified: `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors.
  Commit: a6d858a7ea47c07c18d444a90129ac1c204ee22d — pushed directly to `origin/main`
  (rebased cleanly, fast-forward push). `verify-branch-merged.mjs` confirms ancestor of
  main. `git status --porcelain` clean.
  No fixture/hardcoded data. No new findings filed — the confirmed gap is the fix
  itself, scoped to #3073 as dispatched.
