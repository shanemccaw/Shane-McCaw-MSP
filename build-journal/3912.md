# #3912 — Closing tabs quickly crashes with ObjectDisposedException in BuildChatWebView

- **Status:** ✅ DONE 2026-09-13T02:25:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 22dc3f99c8fb391af829be5e1294bf21509bf125

## Log
- 2026-09-13T02:10:30Z ⏳ IN FLIGHT — investigating BuildChatWebView Loaded handler and InjectBuilderButtonsAsync in MainWindow.xaml.cs
- 2026-09-13T02:25:00Z ✅ DONE — Wrapped the post-await `wv.CoreWebView2` navigate access in
  `BuildChatWebView`'s `Loaded` handler (MainWindow.xaml.cs ~3714-3730) in
  `try { ... } catch (ObjectDisposedException) { }`, matching the issue's suggested fix exactly.
  Confirmed `InjectBuilderButtonsAsync` already has a blanket `catch { return false; }` (line
  ~7745-7770) so it already swallows `ObjectDisposedException` — no change needed there.
  Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
  No live BuildConsole instance to reproduce the fast-tab-close repro interactively in this
  session; the fix matches the confirmed real crash site and stack trace exactly.
  Found a second, distinct instance of the same hazard shape in `OpenChatTab`'s own `Loaded`
  handler (unguarded `wv.CoreWebView2.WebMessageReceived +=` after an await, ~line 3555) — filed
  as #3914, parented as a sub-issue of #1787, labeled `bug`, board status set to AI Batter Up.
  Not fixed here — out of this issue's stated scope (BuildChatWebView + InjectBuilderButtonsAsync
  only).
  `git status --porcelain` clean. Branch verified merged into origin/main via
  `verify-branch-merged.mjs`.
