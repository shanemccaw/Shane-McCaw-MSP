# #1870 — Batter Up feed gate: split board read from auto-queue, add Free flow toggle + per-row manual queue

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** `b5b2cba5c` (feature), `7aedb9f3f` (IN FLIGHT bookend)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Splitting `BatterUpQueueService.RefreshAndAutoQueueAsync` into `RefreshAsync` (read-only) + `QueueRowAsync` (queue one), adding a persisted `BatterUpFreeFlow` setting (default off), and wiring a header toggle + per-row manual queue action into `BatterUpPanel`.
- 2026-08-30 ✅ DONE — Shipped in `b5b2cba5c`.
  - **Step 1 (split):** `BatterUpQueueService.RefreshAsync(gh, queueDb, log)` resolves every board row exactly as before (BUILD: parse, `GetBlockedByAsync` open/closed split, `FindDedupCandidateAsync` already-tracked drop per #1808) and **queues nothing** — no `QueueBuildAsync` reachable. `QueueRowAsync(queueDb, row, log)` queues one already-resolved row; owns the dedup guard + the verbatim auto-queue log line; blocked-by passed straight through (no bypass). `RefreshAndAutoQueueAsync` is now `RefreshAsync` + `QueueRowAsync`-per-eligible-row, preserving the `(Rows, JustQueuedCount)` shape and the always-false `justQueued`/`alreadyTracked` displayed-row flags. Added `BatterUpRow.Prompt` so the manual path can queue without re-reading GitHub.
  - **Step 2 (setting):** `BuildConsoleSettings.BatterUpFreeFlow` (bool, **default off**). Read live each tick.
  - **Step 3 (UI):** header `BtnFreeFlow` toggle (4-col grid, collapsed footprint unchanged); state mirrored in `TxtCount` ("· gated"/"· free flow") + the toggle's own label/colour. Per-card `Queue` button following the #1803 footer pattern — disabled with reason tooltip when no BUILD: comment; on success shows "Queued ✓" then the row drops off next refresh (#1808); double-click guarded two ways (disable-on-click + `FindDedupCandidateAsync`). Blocked rows queueable, no bypass.
  - **Step 4:** `QueueWatcherService.SetPaused` untouched; gates independent.
  - **Verification:** `dotnet build …BuildConsole.csproj -c Release` → **clean, 0 warnings/0 errors**. `grep -n "QueueBuildAsync" BatterUpQueueService.cs` → only invocation is line 207 inside `QueueRowAsync` (rest are doc comments); read path has none. `bt_build_queue` count **781 before → 781 after** (static work added zero rows). settings.json on disk has no `batterUpFreeFlow` key → deserializes as default **false (gated)**, so an existing install lands gated and a restart cannot silently resume auto-queueing.
  - **Honest gap:** the interactive WPF-UI runtime steps (restart-persistence observed live, click-to-queue-one, rapid double-click) were verified by static call-graph/build/code review, not by driving the running WPF app — there is no shaneapp:// protocol to click WPF controls (those cover WebView2 web UI + SQL only). Free-flow-off-queues-zero is proven statically: with `freeFlow=false` the panel calls `RefreshAsync` alone, and `QueueRowAsync`/`QueueBuildAsync` are unreachable from it.
