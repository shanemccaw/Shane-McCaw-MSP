# #1870 — Batter Up feed gate: split board read from auto-queue, add Free flow toggle + per-row manual queue

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Splitting `BatterUpQueueService.RefreshAndAutoQueueAsync` into `RefreshAsync` (read-only) + `QueueRowAsync` (queue one), adding a persisted `FreeFlowBatterUp` setting (default off), and wiring a header toggle + per-row manual queue action into `BatterUpPanel`.
