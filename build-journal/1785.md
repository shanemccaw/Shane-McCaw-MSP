# #1785 — Git Board tree: replace full-text status badges with colored dots + legend

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 49d926413

## Log
- 2026-08-29 ⏳ IN FLIGHT — read #1785's body/comments and #1768's landed commit
  (96e4d5e01), then read `Controls/LeftSidebar.xaml.cs`'s badge-building block in
  full. About to: replace the Working / In Flight / In Flight ↓ / Blocked badges
  in CreateMilestoneHeader, CreateEpicHeader and CreateIssueHeader with small dots,
  revert the issue title TextBlock to single-line CharacterEllipsis, shrink the
  reserved-width constants, and add a one-time legend row above the tree. Note:
  the issue's inventory table also lists a "Waiting for input" 💬 badge as
  "confirmed in code" for this block — it is not; that badge only exists in
  `BuildQueuePanel.xaml.cs` (per running build-queue-item state), with no wiring
  to `GitIssue` at all. Filing that gap as its own finding rather than inventing
  a legend entry for a dot that never renders.
- 2026-08-29 ✅ DONE — commit 49d926413 (`Git Board tree: colored status dots +
  legend, revert to single-line titles (#1785)`). Replaced the Working / In
  Flight / In Flight ↓ / Blocked text pills in CreateMilestoneHeader,
  CreateEpicHeader and CreateIssueHeader with a shared `CreateStatusDot`
  Ellipse helper — solid fill for Working/In Flight/Blocked, a hollow ring
  for In Flight ↓ so the exact-issue vs. descendant cases stay visually
  distinct with the text gone. Reverted #1768's `TextWrapping.Wrap`/2-line
  cap on the issue title back to single-line `CharacterEllipsis`. Collapsed
  the old per-badge text-pill width constants into one shared
  `IssueStatusDotReserve`; kept a separate `IssueNoParentBadgeWidth` for the
  still-text "NO EPIC" pill (out of this ask). Added a one-time legend row
  above the tree in LeftSidebar.xaml (Working/In Flight/In Flight ↓/Blocked).
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`: clean,
  0 warnings, 0 errors — real, ran in this session. Pushed to
  `agent/1785-q859`, then fast-forward-merged onto `origin/main`
  (915a5a74e..49d926413); `git merge-base --is-ancestor HEAD origin/main`
  confirmed. `git status --porcelain` clean at DONE. Filed finding #1800
  (parented to #1202, board status set to "AI Batter Up") for the
  "Waiting for input" badge #1785's own inventory table claimed exists in
  this file but doesn't — it's a BuildQueuePanel-only indicator with no
  wiring to `GitIssue`, left out of the dot conversion and legend rather than
  inventing a legend entry for a dot that never renders.
