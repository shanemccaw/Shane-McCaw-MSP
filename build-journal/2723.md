# #2723 — MSP staff-management routes claim to persist MFA-enforcement and session-revoke but don't

- **Status:** ✅ DONE 2026-09-04T03:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 43e480b51 (fix), 1f46de6dc (HEAD after rebase, pushed to origin/main)

## Log
- 2026-09-04T02:59:40Z ⏳ IN FLIGHT — investigated `artifacts/api-server/src/routes/msp-settings.ts`; confirmed both routes write only `msp_audit_logs` and never persist the claimed effect. About to apply the fix prescribed in the issue body: real `db.update(usersTable)` for MFA enforcement, and the real `revokeAllOtherSessions` helper (already used by `portal-team.ts:81`) for session revoke.
- 2026-09-04T03:05:00Z ✅ DONE — applied exactly the fix prescribed in the issue body:
  - `PATCH /api/msp/settings/users/:userId/mfa-enforcement` now runs
    `db.update(usersTable).set({ mfaEnforced: !!enforced, updatedAt: new Date() }).where(and(eq(usersTable.id, userId), eq(usersTable.mspId, mspId)))`
    before the audit log + response, matching the customer-facing analog `portal-team.ts:337`.
  - `DELETE /api/msp/settings/users/:userId/sessions` now calls the real
    `revokeAllOtherSessions(userId, null)` helper (`lib/session-tracking.ts`, the same one
    `portal-team.ts:81` already uses) which revokes both `user_sessions` and
    `msp_refresh_tokens` rows, and returns a real `revokedCount` instead of the fixed
    `"All sessions revoked"` string.
  - **Verification:** `tsc -p artifacts/api-server/tsconfig.json --noEmit` ran clean against
    `msp-settings.ts` (project-wide run surfaced only pre-existing unrelated errors in
    `monitor-executor.test.ts`, not touched here — no shared pnpm-store issue blocked this
    typecheck). No BuildConsole/portal frontend consumes either route yet (`#2662` not wired,
    `artifacts/msp-console` has zero references to either endpoint) so the response-shape
    change (`message` → `revokedCount`) is safe.
    Live-verified the exact SQL both fixes now issue directly against local Postgres
    (`DATABASE_URL`): toggled `users.mfa_enforced` true→false on a real row (id 50) and
    confirmed persistence each way; inserted synthetic `user_sessions` +
    `msp_refresh_tokens` rows for that same user, ran the exact `revokeAllOtherSessions`
    update pair, confirmed both `revoked_at` columns set, then deleted the synthetic rows
    (no residue left in the DB).
  - No fixture/hardcoded data introduced. No schema change needed — both columns
    (`users.mfa_enforced`, `msp_refresh_tokens.revoked_at`, `user_sessions.revoked_at`)
    already existed and were already used by the customer-facing analogs.
  - `git status --porcelain` clean; branch verified ancestor of `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` (OK).
