# #3677 — MSP Console has no real chrome/Shell (build the real Console Shell)

- **Status:** ⏳ IN FLIGHT 2026-09-12T01:36:59Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-12T01:36:59Z ⏳ IN FLIGHT — Building the real MSP Console Shell chrome in
  `artifacts/msp-console` against `Design/MSP_Console/design_handoff_msp_console/`
  (`Console Shell.dc.html` + `README.md`, cross-referenced with the full
  `MSP Console.dc.html` logic class for the authoritative two-root IA: 9 Operations
  pages + 7 tenant page groups). Investigation confirmed the real backend contract:
  `GET /api/msp/customers` (#3666, closed/complete — returns
  `{customers:[{id,name,domain,status,tenantId,mspId,createdAt,seats,people,lastScanAt,openSignals}],total,page,pageSize}`),
  `GET /api/msp/break-glass` (`{pending:[...]}`), `GET /api/msp/auth/me`
  (`MspUserProfile{role,mspRole,...}`), all gated `requireCapability("ladder.msp-operator")`.
  Auth gate reuses admin-panel's `/api/auth/refresh` + `setAuthTokenGetter` pattern,
  scoped on `mspAuthMe.mspRole ∈ {PlatformAdmin,MSPAdmin,MSPOperator}`.
  Plan: dark design tokens in index.css; nav/IA module (tree, breadcrumbs, command
  palette, selection↔route mapping for `/tenants`, `/tenants/:id`, `/tenants/:id/:page`,
  `/ops/:page`); Header (logo, context path, ⌘K search, conditional break-glass pill,
  notifications + user popovers); collapsible TreeSidebar (260px ↔ 56px rail);
  Breadcrumbs; StatusBar; routed ScreenSlot outlet (module pages mount here, dispatched
  separately, each blocked_by this issue); CommandPalette; and all five states (Default,
  Tree collapsed, Command palette, No tenants, Loading) driven by real data — the
  design's STATE chip group is a prototype affordance and is NOT shipped per the README.
  No new npm deps (bandwidth / no pnpm-install-from-worktree — Git #1987): faithful
  custom components using already-present deps (react, lucide-react, cva/clsx/
  tailwind-merge, wouter, @tanstack/react-query, sonner), matching the .dc.html's own
  inline-token approach for pixel fidelity. No fixture/hardcoded data.
