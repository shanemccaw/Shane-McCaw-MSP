# #3677 — MSP Console has no real chrome/Shell (build the real Console Shell)

- **Status:** ✅ DONE 2026-09-12T01:58:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 03c6abe76 (IN FLIGHT bookend), 7888bf1d6 (shell implementation)

## Log
- 2026-09-12T01:36:59Z ⏳ IN FLIGHT — Building the real MSP Console Shell chrome in
  `artifacts/msp-console` against `Design/MSP_Console/design_handoff_msp_console/`
  (`Console Shell.dc.html` + `README.md`, cross-referenced with the full
  `MSP Console.dc.html` logic class for the authoritative two-root IA: 9 Operations
  pages + 7 tenant page groups). Investigation confirmed the real backend contract:
  `GET /api/msp/customers` (#3666, closed/complete — returns
  `{customers:[{id,name,domain,status,tenantId,mspId,createdAt,seats,people,lastScanAt,openSignals}],total,page,pageSize}`),
  `GET /api/msp/break-glass` (`{pending:[...]}`), `GET /api/msp/auth/me`
  (`MspUserProfile{role,mspRole,...}`), all gated `requireCapability("ladder.msp-operator")`.
  Auth gate reuses admin-panel's `/api/auth/refresh` + `setAuthTokenGetter` pattern,
  scoped on `mspAuthMe.mspRole ∈ {PlatformAdmin,MSPAdmin,MSPOperator}`.
  Plan: dark design tokens; nav/IA module; Header; collapsible TreeSidebar; Breadcrumbs;
  StatusBar; routed ScreenSlot outlet; CommandPalette; all five states driven by real
  data (the design's STATE chip group is a prototype affordance and is NOT shipped).
  No new npm deps (bandwidth / no pnpm-install-from-worktree — Git #1987): faithful
  custom components using already-present deps, matching the .dc.html's own inline-token
  approach for pixel fidelity. No fixture/hardcoded data.
- 2026-09-12T01:58:00Z ✅ DONE — Commit `7888bf1d6` on `main` (verify-branch-merged
  OK; working tree clean). Built the full MSP Console Shell in `artifacts/msp-console`:
  * `src/console/` — tokens.ts (verbatim design tokens), icons.tsx (explicit lucide
    map), nav.ts (two-root IA: 9 Operations pages + 7 tenant groups / 23 pages, from
    the design logic class; selection↔route mapping), treeModel.ts (pure tree/rail/
    crumb/command/status builders), Header/TreeSidebar/Breadcrumbs/StatusBar/
    CommandPalette/ScreenSlot/ConsoleShell/AuthGate.
  * `src/contexts/AuthContext.tsx` — reuses the platform `/api/auth/refresh` + bearer
    pattern; scope gate on `GET /api/msp/auth/me` mspRole (operator+), mirroring
    `requireCapability("ladder.msp-operator")`.
  * `src/api/console-api.ts` — live hooks: tree ← `GET /api/msp/customers` (#3666),
    break-glass pill ← `GET /api/msp/break-glass`, signal feed ← `GET /api/msp/alerts`.
  * Real routes `/tenants`, `/tenants/:id`, `/tenants/:id/:page`, `/ops/:page`; all
    five states (Default / Tree collapsed / Command palette / No tenants / Loading)
    driven by real data (prototype STATE chip group deliberately not shipped);
    ⌘K palette; index.css dark canvas (#020617) + smc keyframes; deleted the old
    placeholder `pages/index.tsx`.
  Verification: `tsc --noEmit` exit 0; production `vite build` exit 0 (1754 modules,
  98 kB gzip — icon tree-shaking confirmed); 11 new unit tests in
  `src/console/nav.test.ts` (route↔selection round-trip, tree/crumb/command builders,
  status-dot mapping) all pass via `tsx --test`. No test manifest — msp-console is out
  of the manifest scope (only msp-portal / shane-mccaw-consulting are in scope). No
  live browser E2E: the operator auth gate requires a real logged-in MSP session that
  can't be produced headlessly here; the gate's unauthenticated path (sign-in-required)
  is itself the correct behaviour and the backend endpoints were verified live via
  #3666 and by reading the route code.
  Findings filed (both parented under #1096 EPIC: Application Core — **#1571 is at
  GitHub's hard 100-sub-issue cap** so the dispatch's "sibling of #3677 under #1571"
  wasn't possible; used the api-server/schema area-epic fallback; both set to board
  status "AI Batter Up"):
  * #3745 — generated MSP api-client type for `GET /api/msp/customers` is stale (wrong
    field names, missing the #3666 directory metrics); the shell defines its own
    `DirectoryResponse` and fetches directly as a workaround.
  * #3746 — customer directory metrics expose only `openSignals` (a count, no
    severity), so the tree status dot renders 3 honest states, not the design's 4
    (no red/critical); needs a severity split in the metrics.
