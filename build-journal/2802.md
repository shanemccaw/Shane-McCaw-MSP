# #2802 — Context gauge still not updating after #2781's fix + ContextBudget 300k→900k

- **Status:** ✅ DONE 2026-09-04T03:40:37Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 23e7f4f38 (on main)

## Log
- 2026-09-04T03:32:30Z ⏳ IN FLIGHT — starting: (1) live diagnosis of gauge/turn-count not updating post-#2781, (2) confirmed ContextBudget 300_000 → 900_000 change + audit for other hardcoded thresholds in ChatDocumentContainer.xaml.cs.
- 2026-09-04T03:40:37Z ✅ DONE — Real root cause found by static code trace (no live BuildConsole session was launched to verify per the standing "never launch BuildConsole.exe" rule — this is genuinely a code-structural bug, provable by reading the wiring): `ChatDocumentContainer`'s 30s `DispatcherTimer.Tick` was wired to `UpdateActiveTime()` + `UpdateSendToChatAffordance()` only — `UpdateGauge()` was NEVER called from it. #2781 correctly fixed the `_contextMeters` registration gap, so `ChatContextMeterStore` genuinely was being written on every `BT_CHAT_STATS` poll, but nothing told Band 1 to re-read the store and repaint — `UpdateGauge()` only ran on construction, a manual resize (`CtxBarBorder_SizeChanged`), or composer text changes. A tab merely being watched (or reselected — WPF `TabControl` detaches/reattaches `Content` so `Loaded` refires on reselect) sat frozen. This also explains Shane's newest evidence that "Messages:" is stuck at "—": that field is set by `UpdateActiveTime()`, which reads the same `ChatContextMeterStore.Get(_chat.ConversationId)` — same root cause, not a separate bug. Fix: `Tick` and `Loaded` now call `RefreshContext()` (which calls both `UpdateGauge()` and `UpdateActiveTime()` together), and `Loaded` also fires it immediately rather than waiting up to 30s. Also fixed `ContextBudget` 300_000 → 900_000 per Shane's confirmed decision, and a second independently-hardcoded raw value found in the audit: the gauge's `"/ 300k ctx"` label was a literal string, not derived from the constant — now `FormatK(ContextBudget)`. Audited the 60k/85k/100k color-tier + Start-New-Chat thresholds in the same method: confirmed via the existing Git #2727 comment these are deliberately absolute per-conversation degradation points ported from the retired banner, NOT a percentage of `ContextBudget`, so correctly left unchanged. Verified: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0 errors. Commit `23e7f4f38` rebased onto and pushed directly to `origin/main` (ahead by 2 commits when I started: #2803/#2801 bookends had landed). `git status --porcelain` clean. `verify-branch-merged.mjs` confirms ancestor of `origin/main`. No new findings filed — the bug was structural, not a separate discovered issue; commented full findings on #2802 itself.
