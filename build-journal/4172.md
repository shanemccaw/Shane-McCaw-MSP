# #4172 — TYPO/COLOR AUDIT: ChatDocumentContainer.xaml ignores Typography.xaml/theme brushes

- **Status:** ⏳ IN FLIGHT 2026-09-15T01:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-15T01:40:00Z ⏳ IN FLIGHT — pulled #4172 + #4162 evidence. `ChatDocumentContainer.xaml` deliberately ports a LOCAL Cool./Warm. palette from a README-ClaudeChat.md spec (Git #2548, comment at top of file: "Do NOT harmonise the palette split") — the 26 hex values in `UserControl.Resources` (lines 20-45) are that palette's legitimate definitions, not raw-literal violations, same carve-out #4162's own audit gives `Themes/*.xaml`. Real remediation: map all 45 raw FontSize literals to Typography.xaml keys by role (bucketed <=12.5→Caption(11), 13-15→Body(14), 15.5-17→SubHeader(16)), map the 2 raw `FontFamily="Consolas"` to `MonospaceFontFamily` (17 `Segoe MDL2 Assets` icon-glyph literals left alone per issue text), and replace the 10 genuinely-inline raw hex consumer literals with either an existing local Cool./Warm. resource (1 exact match) or a newly-added local named resource following the file's own convention (6 new) — never with a global Colors.xaml/DarkTheme.xaml brush, which would violate the file's own documented "don't harmonise" instruction. One raw `#000000` DropShadowEffect shadow color left untouched (universal shadow black, not a themed value).
