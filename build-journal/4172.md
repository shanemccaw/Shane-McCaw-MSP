# #4172 — TYPO/COLOR AUDIT: ChatDocumentContainer.xaml ignores Typography.xaml/theme brushes

- **Status:** ✅ DONE 2026-09-15T02:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** e1e7bb009 (fix, rebased/pushed as 42ee5a723 on origin/main)

## Log
- 2026-09-15T01:40:00Z ⏳ IN FLIGHT — pulled #4172 + #4162 evidence. `ChatDocumentContainer.xaml` deliberately ports a LOCAL Cool./Warm. palette from a README-ClaudeChat.md spec (Git #2548, comment at top of file: "Do NOT harmonise the palette split") — the 26 hex values in `UserControl.Resources` (lines 20-45) are that palette's legitimate definitions, not raw-literal violations, same carve-out #4162's own audit gives `Themes/*.xaml`. Real remediation: map all 45 raw FontSize literals to Typography.xaml keys by role (bucketed <=12.5→Caption(11), 13-15→Body(14), 15.5-17→SubHeader(16)), map the 2 raw `FontFamily="Consolas"` to `MonospaceFontFamily` (17 `Segoe MDL2 Assets` icon-glyph literals left alone per issue text), and replace the 10 genuinely-inline raw hex consumer literals with either an existing local Cool./Warm. resource (1 exact match) or a newly-added local named resource following the file's own convention (7 new: Warm.CtxIconHover, Warm.ShareHover, Warm.SubtleBorder, Warm.OnDangerText, Warm.WarningAmber, Warm.OverlayOuter, Warm.OverlayInner) — never with a global Colors.xaml/DarkTheme.xaml brush, which would violate the file's own documented "don't harmonise" instruction. One raw `#000000` DropShadowEffect shadow color left untouched (universal shadow black, not a themed value).
- 2026-09-15T02:05:00Z ✅ DONE — all 45 raw FontSize + 2 raw FontFamily="Consolas" literals replaced with `{StaticResource ...}` references to Typography.xaml keys. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Filed #4204 (sub-issue of #1202, milestone v1.1, label bug, board status AI Batter Up) documenting the audit-methodology gap this surfaced (local per-file palette dictionaries need the same carve-out `Themes/*.xaml` already gets). Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
