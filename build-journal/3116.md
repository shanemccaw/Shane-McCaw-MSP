# Git #3116 — Undecided: rooms read entities/entity_items or their own typed tables?

- **Status:** ✅ DONE 2026-09-07T19:35:31Z
- **Scope:** platform

## Log
- 2026-09-07T19:26:16Z ⏳ IN FLIGHT — Opening bookend. Real decision already recorded on #3116 by
  Shane: rooms use their own typed tables, not the generic `entities`/`entity_items` pair. This
  build's real scope is making the share layer (`share_links`, `core/shares.mjs`,
  `routes/public.mjs`) work against a typed table, starting with Shopping's real shape
  (`lists`/`list_items`, migration 016) since Shopping (#3088) is the first room and hasn't built
  yet. Plan: migration 019 adds `entity_kind` + `list_id` to `share_links` with an XOR check,
  relaxes `entity_id` to nullable; `core/lists.mjs` (new, minimal — just what the share layer
  needs, not full list CRUD, which stays #3088's scope) backs list-kind resolution; `shares.mjs`,
  `routes/public.mjs`, `routes/api.mjs`, `mcp/tools.mjs` branch on kind so a list can be shared
  the same way an entity already is.
- 2026-09-07T19:35:31Z ✅ DONE — `2999decc4` (rebased onto `9c88f7f36`/`e1ce7c0df`, #3118's
  migration-number guard, which still passes clean against the new 019 file). Shipped:
  - `web/shanes-life/migrations/019_share_links_typed_targets.sql` — `share_links` gets
    `entity_kind` (`entity`|`list`) + `list_id` (FK → `lists(id)`), `entity_id` relaxed to
    nullable (not destructive — `DROP NOT NULL` is on this repo's own non-destructive list), and
    an XOR check constraint so exactly one real target is ever set. Ran it myself against the
    real shared database (`finances`) — applied cleanly on top of 013-018.
  - `src/core/lists.mjs` (new) — deliberately minimal: an ownership check and a share-shaped read
    (`getListForShare`, normalised onto the same field names `core/entities.mjs` already returns
    so the share layer stays kind-agnostic) and the one write a share link can make
    (`setListItemChecked`). Full list CRUD/capture wiring is left to #3088, which hasn't built yet.
  - `src/core/shares.mjs` — `createShareLink`/`resolveShare`/`listShareLinks` all branch on kind
    now; `routes/public.mjs` routes the tick-an-item write to `entities` or `lists` depending on
    which the token actually points at; `routes/api.mjs` (`POST`/`GET /api/shares`) and
    `mcp/tools.mjs`'s `create_share_link` tool both accept `listId` alongside the existing
    `entityId`.
  - `bin/check.mjs` — added a real end-to-end section: inserts a real `lists`/`list_items` row
    (Shopping's own CRUD doesn't exist yet, so this uses the same real table #3088 will read/write
    directly), mints a share link against it, opens it with no session, ticks the real
    `list_items.done`/`done_at` off through it, confirms the audit trail (`list.item.check`) and
    `GET /api/shares?listId=`, and confirms revocation really revokes.
  - **Real verification:** `node bin/migrate.mjs` applied 019 cleanly against the real shared
    database; `node scripts/check-migration-numbers.mjs` (#3118's own guard) clean; `npm run
    check` (`bin/check.mjs`) **58/58 PASS** (was 50/50 before this build — 8 new checks for the
    list-kind share path), re-run after the rebase onto #3118's landed commits, still 58/58. Not
    one ShanesSurvival row moved. Test server (`PORT=5177`) stopped; port confirmed free
    afterward.
  - No findings filed — this build closed the gap #3107 flagged rather than surfacing a new one.
