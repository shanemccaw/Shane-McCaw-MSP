# #1408 — Projects FooterActions leaks fixture content in honest-empty state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 34588bfb6

## Log
- 2026-08-27 ⏳ IN FLIGHT — move FooterActions inside the live dataState branch so the fixture CR number/SOW/docs links no longer render below the honest "no active project" empty state.
- 2026-08-27 ✅ DONE — Moved `<FooterActions />` (artifacts/msp-portal/src/pages/portal-v2-projects.tsx) inside the `live`/`empty` ternary branch, after `TaskBoard`, so it no longer sits unconditionally outside the whole dataState conditional. No real CR/SOW/docs link data flows through `useProjectsLive`, so honest-empty (gate it out) was the correct fix over faking real values. Updated test-manifests/portal/portal-v2-projects.json PJ16 to assert the footer is now `absent` for the unenrolled TEST_PORTAL_EMAIL account (dataState 'unconfigured'), replacing the old assertion that it was always visible. tsc --noEmit on msp-portal shows 2 pre-existing unrelated errors (SowScreen.tsx, PortalV2Shell.tsx) — none in the touched file.
