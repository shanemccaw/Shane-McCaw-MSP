# #3343 — Batter Up items with just-changed/new board status invisible up to 30 min after #3337 (incremental sync inserts NULL board_status)

- **Status:** ✅ DONE 2026-09-09T22:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Commit(s):** 4d77fed7c5290ac73ed16fd63a5207a7c25d013d

## Log
- 2026-09-09T21:48:00Z ⏳ IN FLIGHT — Pulled #3343 body + comments. Root cause: #3337 made board status full-walk-only; incremental pass INSERTs new/newly-moved issues with NULL board_status, so they're invisible to the Batter Up mirror read (TryGetByBoardStatusAsync) and Free Flow for up to 30 min. Implementing targeted single-issue board-status fetch on incremental insert/change.
- 2026-09-09T22:20:00Z ✅ DONE — Chose fix option 1 (targeted board-status fetch on incremental insert/change) — strongest because it keeps #3337's rate-limit win (no full walk, fires at most once per incremental pass, only when something changed) and needs no per-read live fallback. `GitHubIssueMirror.IncrementalSyncAsync` now calls the existing aliased/chunked/rate-limit-circuit-aware `GitHubApiClient.BatchGetProjectItemStatusesAsync` for just its changed set and upserts real board status; a miss/skip/failure (circuit open, throw, off-board) is CASE-guarded to preserve existing rows (never wipe) and NULL only fresh inserts. Commit 4d77fed. Verified: `dotnet build BuildConsole.csproj` succeeded, 0 warnings/0 errors. Symptom 2 (count → 0): confirmed as a downstream consequence of the SAME root cause (invisible NULL-board items) combined with normal #1997 suppression of already-queued items — no separate count-zeroing defect found (the mirror read fails-closed to live when unpopulated, and both sync passes are transactional + preserve-on-failure, so the count can't spuriously zero while genuine unsuppressed Batter Up rows exist). BuildConsole is out of scope for test manifests (per CLAUDE.md); verification is the clean build. Not launching BuildConsole.exe to verify (single-instance mutex could courier into Shane's live session).
