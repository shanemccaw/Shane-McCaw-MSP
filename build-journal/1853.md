# #1853 — Config state: 130 of 337 working cmdlets have no captured output shape

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `d8cb6823c` (bookend), `5c651bd31` (schema + migration + derivation
  script + doc regeneration)
- **Migration:** `lib/db/migrations/manual/2026-08-30-ps-survey-dsc-derived-shapes-1853.sql`
  — run against the local `DATABASE_URL` in-session, verified with `\d`. Logged on #1630.
- **Issues filed:** #2007 (parented to #1096, board status "AI Batter Up")

## Log
- 2026-08-30 ⏳ IN FLIGHT — Shane's decision (option 2): derive property shapes for the
  130 shapeless `ok` cmdlets from Microsoft365DSC's resource definitions, labelled
  `derived_from_dsc` and never presented as live evidence. Plan: match each shapeless
  cmdlet to `config_resources` (powershell transport, already carrying DSC MOF
  properties from #1794) via `read_cmdlets`; store the derived property set and gap
  reasons on `ps_capability_survey_results` (new, additive columns — `property_names`
  itself is never touched); regenerate `docs/powershell-capability-survey.md` with a
  new section carrying the corrected coverage. Real match rate confirmed by direct
  query before writing any code: 54 of 130 (41.5%) match a DSC resource with a real
  non-connection property set; 76 do not and will be recorded as an explicit gap.

- 2026-08-30 ✅ DONE — Shipped exactly the plan above, matching the direct-query numbers.

  **Schema.** Five new nullable columns on `ps_capability_survey_results`
  (`derived_property_names`, `derived_from_m365dsc_resources`, `shape_derivation`,
  `derivation_gap_reason`, `shape_derived_at`) — additive, `property_names` untouched.
  Migration ran clean against local Postgres (`\d ps_capability_survey_results`
  confirms all five columns), self-marking row inserted, logged on #1630.

  **Matching.** `scripts/config-state/derive-ps-shapes-from-dsc.mjs` — for each `ok`
  survey row (latest completed run, #4) with `property_names IS NULL`, matches the
  cmdlet name (case-insensitive, exact match only) against `config_resources.read_cmdlets`
  (powershell transport), unions the matched resource(s)' non-connection
  `config_resource_properties` (`source = 'm365dsc-mof'`), and writes the result
  labelled `derived_from_dsc`. No match, or a match with zero non-connection
  properties, writes `derivation_gap_reason` instead — never silently left blank.
  Idempotent, re-runnable after any `build-resource-model.mjs` rebuild.

  **Real match rate — not 130 of 130, as instructed.** 54 of 130 (41.5%) derived a
  real DSC shape; 76 (58.5%) have no matching DSC resource and are recorded with the
  exact reason. By session: compliance 14/34, exchange 29/55, teams 11/41. Some
  cmdlets (e.g. `Get-RetentionCompliancePolicy`) matched TWO DSC resources — DSC's
  Policy/Rule split sharing one read cmdlet — and both contributed to the unioned
  property set, correctly attributed in `derived_from_m365dsc_resources`.

  **Doc.** `scripts/src/ps-capability-survey-doc.ts` extended with a genuinely new
  generated section, "DSC-derived shapes for shapeless cmdlets (Git #1853)" — a
  match-rate summary table, the full list of derived shapes with their source DSC
  resource(s), and the full list of unmatched cmdlets with their gap reason. Verified
  by diff that regenerating `docs/powershell-capability-survey.md` produced ONLY
  additions (`git diff` shows zero `-` lines) — the original "returned no items, so no
  shape observed" per-cmdlet cells are untouched, per Shane's instruction not to edit
  the earlier reasoning.

  **Verification, all real:**
  - `lib/db` `tsc --noEmit`: clean.
  - `scripts` `tsc --noEmit`: clean.
  - Dry-run vs. real run of `derive-ps-shapes-from-dsc.mjs` produced identical numbers
    (54/76), matching the direct SQL query run before any code was written.
  - Spot-checked `Get-AuthenticationPolicy` in the DB: correctly carries both matched
    resource keys (`EXOAuthenticationPolicy`, `EXOAuthenticationPolicyAssignment`) and
    their unioned 15 properties.
  - Confirmed `property_names` is untouched: 207 `ok` rows still have it non-null,
    unchanged from before this session.
  - **Hit `pnpm`'s auto dep-status-check trying to run `pnpm install` non-interactively**
    when regenerating the doc via `pnpm --filter @workspace/scripts run
    ps-capability-survey-doc` — per the standing #1987 rule, did not run it; called
    `scripts/node_modules/.bin/tsx.CMD` directly instead, which needed no install.

  **Finding filed:** #2007 (parented to #1096, board "AI Batter Up") — one
  `config_resources` row (`MSFT_EXOAtpProtectionPolicyRule`) has `resource_key` /
  `display_name` / `m365dsc_resource` all set to the literal string `"ResourceName"`
  instead of the real resource name, from #1794's `parse-m365dsc.mjs:218-219`. Its
  properties parsed correctly and were used for `Get-ATPProtectionPolicyRule`'s
  derived shape regardless — only the resource's own identity fields are wrong. Out
  of #1853's scope (matching against the existing model, not re-parsing DSC), so
  filed rather than fixed inline.

  Read-only against the tenant throughout — no writes, no seeding, exactly as
  instructed. Branch verified merged into `origin/main` (`verify-branch-merged.mjs`,
  exit 0, `5c651bd3`); `git status --porcelain` clean.
