# #1563 — Security Plan: Scope filtering is legitimate; findings filtering is not

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** dbb20eefb7cddfc3d93192db22bdcff164f0de98

## Log
- 2026-08-31 ⏳ IN FLIGHT — #1561's commit (46ff73e92) already built almost all of
  #1563's requirement (dimension-only scope, honest-view default, filter footprint)
  as part of the same assembled-view build. Investigation found one real gap left
  open by #1563's own "to build" list: "every filtered view carries its own scope
  statement" exists as an optional field but is not enforced — a scoped seal can
  currently go through with no statement. Implementing enforcement + tests.
- 2026-08-31 ✅ DONE — Added `scopeMissingRequiredStatement` (pure function,
  `artifacts/api-server/src/lib/security-plan-assembly.ts`) and wired it into
  `POST /api/msp/security-plan/:customerId/versions` (`msp-security-plan.ts`) as a 400
  validation error: a scope with any dimension constraint (`pillar`/`framework`) must
  carry a non-empty `statement`, or the seal is rejected before assembly runs. The
  honest (unconstrained) view needs no statement. No schema/migration change — this is
  application-layer validation over the existing `SecurityPlanScope.statement` field
  from #1561's commit, not a new column.

  Verified #1563's OTHER two requirements were already fully delivered by #1561
  (commit 46ff73e92, same session that built the assembled view):
  scope operates ONLY on dimensions (`pillar`, `framework` — `SECURITY_PLAN_SCOPE_DIMENSIONS`
  in `lib/db/src/schema/msp.ts`), never on outcome — the wire scope schema (zod) has no
  outcome key to even express one, and an unclassifiable row is retained, never dropped,
  per `isExcludedByScope`. The honest view (`HONEST_SCOPE = { dimensions: {} }`) is the
  default, not a separate mode.

  Real verification, not inferred:
  - `npx vitest run src/lib/security-plan-assembly.test.ts` — 6/6 pass (5 pre-existing +
    1 new: `requires a scope statement on a constrained scope, but not on the honest view`).
  - `npx tsc --noEmit -p .` in `artifacts/api-server` — 105 pre-existing errors, none in
    any file this session touched (matches the standing #1765/#1772/etc note; not mine).
  - `psql "$DATABASE_URL" -c "select count(*) from msp_security_plan_versions;"` — 0 rows
    (honest: nothing sealed yet in local dev, table exists from #1561's migration).
  - `node scripts/dev-server/verify-branch-merged.mjs` — OK, ancestor of origin/main.
  - `git status --porcelain` — clean.

  No new finding filed: #2085 (business_unit dimension has no backing column) was
  already filed by the #1561 session and covers the one other real gap in this area;
  did not duplicate it.
