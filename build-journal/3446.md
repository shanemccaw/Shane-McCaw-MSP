# #3446 — msp-dlq.ts Replay/Bulk Replay have no replayability guard

- **Status:** ✅ DONE 2026-09-11T04:42:01Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** a4c92f055 (on `main`)

## Log
- 2026-09-11T04:35:15Z ⏳ IN FLIGHT — investigated `msp-dlq.ts` (single + bulk replay routes) and
  the reference `isReplayable()` pattern already in `admin-dlq.ts`. Confirmed live: 32 unresolved
  rows in local `msp_dlq_store`, 0 with `workflowKey` in payload — every replay attempt through
  the MSP-console route fails today. No frontend consumer of `GET /msp/dlq` exists yet
  (`artifacts/msp-console` has no DLQ/Replay UI), so this is a backend-only fix per the issue body:
  port `isReplayable()` into `msp-dlq.ts`'s single-item and bulk replay routes, and add a
  `replayable` field to the list response.
- 2026-09-11T04:42:01Z ✅ DONE — ported `isReplayable()` (identical logic to `admin-dlq.ts`) into
  `msp-dlq.ts`: `GET /msp/dlq` now stamps `replayable` per row; `POST /msp/dlq/:dlqId/replay` 400s
  with an actionable message instead of letting the throw become a bare 500; bulk replay checks
  each item's payload before calling `replayDlqItem` and reports a per-item failure reason for
  non-replayable rows instead of calling it and catching the throw. Re-queried local Postgres after
  rebasing onto #3445's route-path fix (also landed in this same file, no conflict): still 0/30
  unresolved rows carry a `workflowKey`, confirming the guard now fires for every real live row.
  Added `msp-dlq.test.ts` (none existed) mirroring `admin-dlq.test.ts`'s pattern — 7 new cases,
  all passing; ran alongside the 10 pre-existing `admin-dlq.test.ts` cases (17/17 pass). `tsc
  --noEmit` clean. Rebased onto `origin/main` (which had moved since branch start — #3445's
  same-file fix and others) and pushed directly to `main` at `a4c92f055`.
  `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
  No product decision needed; no findings filed — the fix matched the issue's own prescribed
  approach exactly. No manual SQL migration; no Shane To-Do action.
