# #3051 — Email Templates: real contract pack for all 31 templates, for Design's visual refresh

- **Status:** ✅ DONE 2026-09-06T21:15:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `1c7d231fe` (rebased/pushed as `e45f8deba` on `main`)

## Log
- 2026-09-06T20:47:00Z ⏳ IN FLIGHT — read-only contract-pack extraction: line-by-line audit of `lib/seed-email-templates.ts`, `admin-panel/src/pages/EmailTemplates.tsx`, and every real call site that sends each template. Building the pack under `Design/portal/` (or docs, per #1485 conventions) with real slug/name/subject/variables/bodyHtml citations and confirmed trigger call sites.
- 2026-09-06T21:15:00Z ✅ DONE — Wrote `docs/email-templates-contract-pack.md`. Real path correction: the seed file actually lives at `artifacts/api-server/src/lib/seed-email-templates.ts`, not `lib/seed-email-templates.ts` as the issue body states. **The issue's "31 real templates" claim does not match current reality**: the `TEMPLATES` array defines 24 entries; the live local `email_templates` DB table has 27 rows (24 seeded + 3 orphan out-of-band rows — `kanban-document-exhausted`, `kanban-script-exhausted`, `manual-script-escalation` — with zero code references). This matches #976's identical 2026-08-13 audit against commit `555278b4`, independently re-confirmed against current HEAD via fresh grep + a live local-DB query (`SELECT count(*) FROM email_templates` → 27; per-slug list captured in the pack §0).
  Pack covers, per the issue's own contents list: the `branded-layout` shell (§1), the `tenant-health-block` reusable snippet (§2), and all 24 code-seeded inner templates grouped into 5 real modules (§§3–7) — each with real slug/name/subject/variables (verbatim from `seed-email-templates.ts`), real bodyHtml cited to file:line, and real trigger call site(s) confirmed via grep (not assumed from the name). Re-confirmed #976's still-true findings: `purchase-confirmation`'s DB row is never read by any real send path (all 3 real purchase-confirmation sends call the hardcoded `purchaseConfirmationEmail()` builder directly); `client-thread-reply` has zero wired send path. Send-volume signal: queried the local dev `email_events` table directly (54 total `sent` rows, 2026-07-21 to 2026-09-05) and pattern-matched subjects per template, honestly caveated in §9-10 as local-dev-only traffic, not production volume, with the `sendEmailWithAttachment` instrumentation gap noted.
  **New finding beyond #976's own coverage** (their audit enumerated existing DB rows; this session additionally grepped every `sendEmailFromTemplate`/`getEmailTemplateOrFallback` call site): `admin-clients.ts:872-879` sends a live `"consent-invite"` email on every admin-added-client flow, but no `email_templates` row exists for that slug at all — invisible to the Admin Panel editor and unreachable by the PUT-import follow-up this issue's body describes. Filed as **#3067** (parented under #1485 per the Feature-first/fallback rule since #3051 has no Feature-tier parent; board status set to "AI Batter Up"). Not fixed in-session — explicitly a Shane decision per the issue's own read-only scope.
  Verification: `node scripts/dev-server/verify-branch-merged.mjs` → OK, `agent/3051-q1812` (e45f8deba) merged into `origin/main`. `git status --porcelain` clean. No code changed — pure documentation, no build/test to run.
