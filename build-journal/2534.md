# #2534 — Chats Panel real redesign (Milestone→Epic→Chat, epic_id-driven)

- **Status:** ✅ DONE 2026-09-03T13:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 1f67b7b29 (code), a93694af6 (this bookend, IN FLIGHT), + DONE commit

## Log
- 2026-09-03T12:51:37Z ⏳ IN FLIGHT — Auditing LeftSidebar.xaml.cs (PopulateChatsTree/GetEpicForChat), FocusModeService.cs, BuildQueuePostgresClient.cs epic_id read/write before redesigning the Chats panel to Milestone→Epic→Chat driven by bt_chats.epic_id.
- 2026-09-03T12:55:00Z ⚠️ INFRA — Original worktree C:\wt\2534-q1370 was SWEPT empty mid-session (files + .git + branch all gone) while my process (PID 66788) was alive. Re-provisioned at C:\wt\2534z with --owner-pid 66788. Old empty dir left locked by shell handle; harmless. Filed as finding #2537 (parented to #1202, board AI Batter Up).
- 2026-09-03T13:35:00Z ✅ DONE — Redesigned the Chats panel (LeftSidebar.RenderChatsTree/BuildEpicSection + MainWindow.OpenChatTab). When Focus is engaged with a milestone (Shane's real active = #5 v1.1), the panel lists EVERY open epic in that milestone from the live board (_lastBoardIssues, IsEpic && !IsClosed && MilestoneNumber==active), and hangs each epic's real chats under it matched by the epic's GitHub number against each chat's resolved bt_chats.epic_id (GetEpicForChat's EpicId-first path) — chats ordered updated_at desc, most-recently-active epic first. Zero-chat epics still render (auto-expanded) with a "+ New Chat" button; epics with chats get "+ Continue in a new chat". Both reuse the existing EpicChatRequested → OpenWebTab(associateIssueNumber) path, so bt_chats.epic_id is set on create via the existing AssociateChatWithIssueAsync→LinkChatToIssueAsync write (req 5, no second write path). New helper StartNewEpicChat forces the persisted/tab title to [#N] EpicName. OpenChatTab now forces the tab title to "[#<epic github number>] <Epic Name>" for any chat with a real epic_id (req 6) — display-only, state.GithubNumber untouched so per-chat build matching still keys on the leaf issue. Added a green "<N> open" per-epic pill (real open sub-issue count). No active milestone → legacy chat-derived grouping unchanged.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors). Live-DB check against local Postgres: 65 epics, 11 chats carry epic_id (11 bt_chat_issues rows); grouping by epic_id → #1202 Build Console (7 chats), #1485 Portal (3), #1096 App Core (1) — matches the design-reference screenshot exactly; all three epics confirmed on GitHub milestone 5. Could NOT launch BuildConsole.exe to exercise the live render (forbidden: single-instance mutex can courier into Shane's live session) — build + real-data verification is the ceiling for a WPF surface. Commit 1f67b7b29.
  Not wired (flagged, no fake data): per-chat context % / FULL meter and the colored Feature/buildSet pill from the design reference — chats carry no context-meter or buildSet field to read; the existing per-chat issue pills (real bt_chat_issues numbers with open/closed dots) already stand in for the "associated issues checklist." Left as follow-ups rather than inventing data.
