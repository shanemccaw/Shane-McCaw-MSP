# #3522 — lib/db ships stale dist/ declarations that shadow live schema edits during typecheck

- **Status:** ✅ DONE 2026-09-12T16:49:12Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** dad39792c (fix), 0c5a7db96 (IN FLIGHT bookend)

## Log
- 2026-09-10T23:59:44Z ⏳ IN FLIGHT — Opus/High dispatch (buildSet=MonitoringPackage, buildId 2240). Goal: close the stale-dist class for every `tsc` invocation path, not add another script-specific pretypecheck wrapper on top of #2778's.
- 2026-09-12T16:49:12Z ✅ DONE — **Root cause is the consumer's `"references"`, not `declarationMap` and not `composite`.** A TS project reference makes a plain `tsc -p` substitute a referenced composite project's built `dist/*.d.ts` for its source files (source-redirect applies in `tsc -p`; tsserver/editors use source, which is why the editor never showed it). `declarationMap` is irrelevant to that redirect, and `lib/db` needs `composite` for root `tsc --build` — so neither of the dispatch's two suggested levers is the real one. Removing the consumer-side references is, and it covers every invocation path at once (bare `tsc --noEmit`, `pnpm run typecheck`, editor), because `@workspace/*` then resolves through `package.json` `"exports"` to live `src/**/*.ts`.

  **Shipped (dad39792c):** removed `"references"` from all six artifact tsconfigs (api-server, admin-panel, msp-console, msp-website, portal, shane-mccaw-consulting), each with a comment saying not to re-add them; deleted the five now-dead `pretypecheck: tsc -b ...` hooks (#2087/#2778's fix, which only covered the `pnpm run typecheck` path); rewrote the `scripts/dev-server/README.md` bullet that told agents "never a bare `tsc -p ... --noEmit`" (that guidance is now obsolete and was itself the workaround); corrected the now-stale `compositeLibDirs` docstring in `scripts/dev-server/link-deps.mjs`.

  **Real A/B verification, same source at the same moment.** Appended a scratch export `__repro3522Marker` to `lib/db/src/schema/msp.ts` plus a scratch consumer in api-server, with `lib/db/dist/schema/msp.d.ts` confirmed stale (`grep -c __repro3522Marker` → 0). Ran two typechecks in parallel: a temp `tsconfig.repro3522.json` carrying the original four `references` → exit 2, **1 error, `src/__repro3522.ts(1,10): error TS2305: Module '"@workspace/db"' has no exported member '__repro3522Marker'`**; the fixed `tsconfig.json` → **exit 0, 0 errors**. All five front-ends typecheck clean with references removed (admin-panel, msp-console, msp-website, portal, shane-mccaw-consulting — 0 errors each, exit 0). Root `tsc --build` exit 0. Also confirmed no Vite/Vitest/esbuild config reads tsconfig `references`, so only `tsc` resolution changed. All scratch files removed.

  **Filed as a finding:** #3801 — BuildConsole's `.worktree-reprovisioned.json` marker is untracked and not gitignored, so it permanently dirties `git status` in a reprovisioned worktree.

  **Not mine, left alone:** `.worktree-reprovisioned.json` (untracked, written by BuildConsole when it re-provisioned this worktree mid-session — twice; my uncommitted edits were rescued to `rescued/3522-q2240-2026-09-12T00-46-14-793Z` per #1971 and I re-applied them by hand).

  Branch verified merged (`verify-branch-merged.mjs` OK, dad39792c on origin/main); working tree otherwise clean.
