# #2363 — Batter Up group header: hide "Dispatch all" for an all-Ask-Shane group

- **Status:** ⏳ IN FLIGHT 2026-09-03T01:38:26Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-03T01:38:26Z

## Log
- 2026-09-03T01:38:26Z ⏳ IN FLIGHT — confirmed #2358 (group header incl. Dispatch all,
  commit `3d39f6aeb`) and #2361 (Ask Shane classification, commit `e432e4c4e`) genuinely
  DONE on `main` via their own bookends. Real audit: `RenderBatterUpLaneBody`
  (`MainWindow.BatterUpPanel.cs`) already computes `askShaneNumbers` (the real Ask Shane
  membership set from `counts.AskShaneItems`, by issue number) before grouping, and
  already renders "Dispatch all" unconditionally for every non-"No Feature" group. Plan:
  reuse that same `askShaneNumbers` set — no new classification logic — to check whether
  every item in a group is an Ask Shane item, and skip rendering the "Dispatch all" link
  for that group when so, since there's nothing dispatchable in an all-questions group.
  Note: had to recover this build's worktree first — `C:\wt\2363-q1203` was declared as
  the primary working dir but was a completely empty, unregistered folder (no `.git`),
  matching the known #2118 failure class; recovered via the documented recipe
  (`git worktree add` targeting the existing empty dir directly, then registered +
  linked deps via the lifecycle-tracker functions with `$CLAUDE_PID` as owner).
