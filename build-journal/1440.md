# #1440 — Compliance (top-level pillar page): strict pass — eliminate all remaining fake data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** c86d7fe78 (merged to origin/main; verified via verify-branch-merged.mjs)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting strict fake-data pass on portal-v2-compliance.tsx (top-level pillar page), plus regression re-verify of portal-v2-compliance-obligations.tsx / -gaps.tsx / -decisions.tsx already fixed in #1432/#1433. Standing rule: every value resolves to real data, honest "No scan data available", or "No live data available" + NO-BACKEND-TO-WIRE: comment.
- 2026-08-28 ✅ DONE — Fixed 5 leaks on the top-level pillar page and its drill-downs: (1) the scan strip's Scan-N/gaps-closed-since-scan-1/next-scan-in sentence was 100% fixture with no backend producer anywhere -> honest "No live data available" + NO-BACKEND-TO-WIRE comments; (2) the trend caption below the sparkline stated a fixed "Eight points over ten scans" regardless of the real trend -> new `cmpTrendCaption()` derives it from the real series; (3) tagged the two pre-existing hero-tile gaps and six pre-existing area-card nodata reasons with the new NO-BACKEND-TO-WIRE tag; (4) a deeper leak found while re-verifying #1338/#1433: `resolveCmpArea` treated an empty finding map as ambiguous between "never scanned" and "scanned and healthy", so a never-scanned tenant resolved every backed area card live/green with the fixture magnitude, and the cluster-area drill-down's finding-body gate let the fixture finding paragraph render under that fabricated green status (and under a genuinely healthy real one) -> threaded real `everScanned` through, widened the finding-body gate to require a real non-green status; (5) the Open gaps drill-down's empty state asserted "from your last scan" for a possibly-never-scanned tenant -> gated on the same real signal. Verification: pillarDashboardModel.test.ts + cmpAreaWiring.test.ts extended, 40/40 passing; `tsc --noEmit` clean against these files (2 pre-existing unrelated errors elsewhere, untouched); the retargeted compliance-drilldowns.json AREA4/AREA4b assertions were checked against the LIVE deployed testbed tenant's real DB via a read-only `shaneapp://executeSql` query (governance:retention-policy-coverage is genuinely severity=info/healthy right now; governance:sensitivity-label-adoption is genuinely severity=warning), not guessed. Live `shaneapp://runTest` of the new/changed UI steps was NOT completed this session: the new sessionTag steps aren't yet resolvable ("no steps matched") because BuildConsole merges a worktree session's commits into the local dev-server checkout only on session completion, and a same-session `runTest` against an existing tag confirmed the local dev server at localhost:5173 isn't currently running (concrete "Navigation Error" envelope, not an assumption) — an honest gap, not a skipped step. Branch verified merged into origin/main via `verify-branch-merged.mjs` before writing this DONE.
