# #4048 — Restore Platform Agreements (admin page + backend) — revert #3412

- **Status:** ✅ DONE 2026-09-14T18:40:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 2b98ead2278d70a6f05784aeb27ba79b9da19998

## Log
- 2026-09-14T18:05:00Z ⏳ IN FLIGHT — Pulled #4048/#3412 body+comments, extracted deleted code via `git show 57871978f~1:<path>`, confirmed live RBAC pattern (`requireCapability("ladder.platform-admin")` replaces retired `requireRole`, `requireCapability("ladder.free")` covers any authenticated MSP user). Schema (`platformAgreementsTable`, `mspAgreementAcceptancesTable`) confirmed intact in `lib/db/src/schema/msp.ts`. About to restore `artifacts/api-server/src/routes/platform-agreements.ts`, mount it, and restore the admin page + nav entry + `SystemWorkspace.tsx` case.
- 2026-09-14T18:40:00Z ✅ DONE — Restored `artifacts/api-server/src/routes/platform-agreements.ts` (mounted in `routes/index.ts`), `artifacts/admin-panel/src/pages/PlatformAgreements.tsx`, the `sys-platform-agreements` nav entry in `workspaceNav.tsx`, and its case in `SystemWorkspace.tsx`. Re-gated on live RBAC: `requireCapability("ladder.platform-admin")` for the admin CRUD/publish routes (replaces retired `requireRole("PlatformAdmin")`), `requireCapability("ladder.free")` for the MSP-authenticated acceptance-status/accept routes (replaces bare `requireAuth`). Public current-agreement read stays unauthenticated. Confirmed `platform_agreements` and `msp_agreement_acceptances` tables intact in local Postgres — no migration needed. `pnpm typecheck` clean in both `artifacts/api-server` and `artifacts/admin-panel`. Did not build `accept-agreement.tsx` (Portal-side, out of scope per #4048, still blocked on #1649's Design export). Commit `2b98ead22`, pushed to `main`, `verify-branch-merged.mjs` confirms merged, working tree clean. No new findings to file.
