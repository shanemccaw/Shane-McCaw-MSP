# #2621 — Projects: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-14T15:51:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** b8bd85a11 (bookend), 33a16907b (Projects wiring), 2a14dd353 (nav.test.ts count fix)

## Log
- 2026-09-14T15:40:00Z ⏳ IN FLIGHT — Design export (`Projects.dc.html`) and contract pack
  (`docs/msp-console/projects-msp-console-contract-pack.md`) both confirmed present. Building
  the real MSP Console `Projects` module (Operations, per-customer buckets/cards board) against
  the 7 live `msp-kanban.ts` routes — new `api/msp-kanban-api.ts` client, `console/modules/
  Projects.tsx`, and wiring into `nav.ts`/`treeModel.ts`/`ConsoleShell.tsx`/`icons.tsx`.
- 2026-09-14T15:51:00Z ✅ DONE — Shipped `console/modules/Projects.tsx` (customer picker,
  add/rename/delete bucket, add/edit/move/delete card, honest empty state, a labelled
  illustrative-board toggle only reachable when not embedded) and `api/msp-kanban-api.ts`
  (react-query hooks over all 7 real `msp-kanban.ts` routes). Wired into `nav.ts` (new
  `projects` MSP_PAGES entry + `kanban` icon), `treeModel.ts` (pageMeta), `icons.tsx`
  (registered `Kanban`), and `ConsoleShell.tsx` (`moduleFor` case, mounted `embedded`). No
  fixture/mock data — every real row comes from `GET /api/msp/customers/:id/kanban/buckets`;
  both tables are genuinely 0 rows in local dev today, matching the pack. Verified: `tsc
  --noEmit` clean, `pnpm run build` clean, `pnpm run test` 16/16 passing (updated
  `nav.test.ts`'s IA-count assertions, which shifted from 21→22 MSP_PAGES after rebasing
  picked up the concurrently-merged Retainer Hours op-page, #2618). `git status --porcelain`
  clean; `verify-branch-merged.mjs` confirms `agent/2621-q2752` is an ancestor of
  `origin/main` at `77ef4ddf8`. No findings filed — the contract pack (#2619) had already
  confirmed the backend clean with no drift.
  Not built: drag-and-drop reordering (backend never renumbers positions — pack §2 — so a
  naive per-card PATCH on drag would desync siblings; this Phase-1 UI only ever appends,
  matching the design's own logic class, which also has no drag events). No per-customer
  card-count badge on the customer-picker buttons (unlike the design's mock state) — the
  backend has no cross-customer aggregate route (pack §5, "no bulk/batch endpoints"), so
  showing a live count per unselected customer would mean an N+1 fetch on every render;
  fabricating one was not an option under the no-invented-data rule. This is a real,
  current absence, not a product decision — a future aggregate endpoint could add it back.
