# #2695 — BUILD SETS filter chips (#2693) can hide the entire section with no way back

- **Status:** ✅ DONE 2026-09-03T17:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 4661e51a5

## Log
- 2026-09-03T17:40:00Z ⏳ IN FLIGHT — Fixing RenderBuildSetRollup in Controls/BuildQueuePanel.xaml.cs: separating the unfiltered any-activity count (gates section visibility) from the activity-filtered `orderedKeys` count (gates row list), and adding an honest empty-state row when the filter matches zero sets but real activity exists elsewhere.
- 2026-09-03T17:52:00Z ✅ DONE — Split `orderedKeys.Count == 0` into two checks: `anyActivityKeys` (pre-#2693 unfiltered any-activity set, gates `BuildSetRollupSection.Visibility`) and the post-filter `orderedKeys` (gates the row list). When the filter narrows to zero rows but real activity exists, `BuildSetRollupList` now shows an italic "No build sets currently {running/verifying}" row instead of collapsing the whole section. `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 errors. `git merge-base --is-ancestor HEAD origin/main` confirmed after push. Commit 4661e51a5. Working tree clean.
