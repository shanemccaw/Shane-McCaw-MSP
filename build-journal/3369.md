# #3369 — Reports (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T00:51:13Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** a43149a61 (bookend IN FLIGHT), 7da30f4e7 (contract pack), merged to `main` at 493bf72cd

## Log
- 2026-09-10T00:46:05Z ⏳ IN FLIGHT — read `msp-reports.ts` (979 lines, 19 routes), its test file,
  `report-nodes.ts`, `compileReportToHtml.ts`, `resolve-msp-id.ts`, the `msp.ts` schema for the four
  report tables, and confirmed the real wired caller (`artifacts/admin-panel/src/pages/MspReports.tsx`)
  and the second real writer (`rbd-document-render.ts`, #1512). Cross-checked live against local
  Postgres: all four report tables are genuinely empty (0 rows), `portal_wf_workflows` has never
  registered `msp.report.generation`, admin user `id=1` carries `msp_id=1` so the wired Admin Panel
  page resolves real MSP 1 scope. About to write
  `docs/msp-console/reports-msp-console-contract-pack.md` per the #1642 extraction pattern.
- 2026-09-10T00:51:13Z ✅ DONE — wrote `docs/msp-console/reports-msp-console-contract-pack.md`
  (committed `7da30f4e7`), a full extraction of all 19 routes across Definitions/Runs/License-Waste/
  Canvases/Schedules, every field cited to file:line, cross-checked against real live local Postgres
  state (all report tables empty, `client_m365_profiles` empty, `portal_wf_workflows` never seeded).
  No genuinely half-built route found — every route has real DB read/write, real validation, real
  error handling; completeness verdict recorded in the pack's §0. Confirmed this module is
  **partially wired** (not orphaned like #2657's msp-executive pack) via
  `artifacts/admin-panel/src/pages/MspReports.tsx` (4 of 19 routes live: list definitions, trigger,
  list runs, download) and via `rbd-document-render.ts` (#1512)'s independent direct-DB writer of
  the same two tables for `risk_decision_document` runs — both are real cross-surface facts not
  previously documented anywhere. Two real, currently-latent gaps found and filed as new leaf
  issues under this issue's own parent #1571 (no Feature-tier parent exists between #3369 and
  #1571): **#3406** (`GET .../runs`'s `total` field is the page size, not a real row count — no
  `offset` param exists either) and **#3407** (a run whose email-delivery leg fails silently stays
  `status: "generated"` with an `errorMessage`, but the Admin Panel UI only surfaces `errorMessage`
  on `status: "failed"` rows, so the failure is currently invisible in the wired UI). Both parented
  to #1571 via the real `sub_issues` API, labeled `bug`, milestone v1.1, board status set to
  "AI Batter Up". Branch verified merged into `origin/main` at `493bf72cd`
  (`verify-branch-merged.mjs` exit 0); `git status --porcelain` clean.
