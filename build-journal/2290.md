# Git #2290 — Tree renders Epic → FEATURE → Issue with collapsible feature rows

- **Status:** ✅ DONE 2026-09-02T18:10:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Git Panel navigation shell, items 1–11 of Feature #2289)
- **Build:** queue #1188, buildSet GitPanel
- **Commits:** `79852e2b0` (implementation), `8dfccfb49` (IN FLIGHT bookend)

## Log

- 2026-09-02T17:45:00Z — ⏳ IN FLIGHT — Session start. Scope: Git Panel navigation shell in desktop/ShaneBuilder (#2290–#2300): tree Epic→FEATURE→Issue, clickable levels opening peek panels (placeholder content — items 12–23 out of scope), breadcrumb chips, named back button, cold-open ancestry derivation. Real data via GitMapService/GitIssuesService/GitHubReadClient; no fixtures.
- 2026-09-02T18:10:00Z — ✅ DONE — Shipped `79852e2b0` (verified ancestor of origin/main): new `Services/GitPanelService.cs` (open milestones via REST, open `GATE:` issues, one-GraphQL-call two-level epic sub-tree with per-feature open/bug counts, `parent`-chain ancestry walk), new `MainWindow.GitPanel.cs` (tree + peek + breadcrumb + named back + cold open), `MainWindow.xaml` GitPanelBody placeholder replaced with real container, load triggers + `AlertActions.OpenIssueInGitPanel` wired to the real cold-open path.
  **Verification (honest level):** `dotnet build` clean (0 warnings / 0 errors); both GraphQL queries probed live exactly as the service issues them — ancestry for #2290 resolved `#2290 → FEATURE #2289 → EPIC #1202, milestone v1.1 (5)`, feature tree for #1202 returned 97 features and computed #2289's real counts (23 children, 23 open, 0 open bugs); no live UI click-through (no manifest — BuildConsole/ShaneBuilder is out of test-manifest scope per CLAUDE.md; visual QA is Shane's).
  **Sibling items satisfied by this build (all implemented and code/build/data-verified, per-item):** #2291 (state pill + bug count + open count on feature rows), #2292 (milestone rows open in panel), #2293 (GATE cards open in panel), #2294 (epic opens in panel), #2295 (FEATURE name opens in panel), #2296 (issue opens in panel), #2297 (labelled breadcrumb chips MILESTONE v1.1 › EPIC #1202 … › FEATURE …), #2298 (every crumb clickable, click truncates the trail), #2299 (back button names its destination, "Back to Git tree" at depth 1), #2300 (cold open derives ancestry via real GraphQL parent chain). Peek bodies are intentionally minimal identity placeholders — items 12–23 (#2301+) are later builds.
  **Findings filed:** #2409 (epic sub-issue fetch caps at one GraphQL page of 100; #1202 already at 97 — in-build mitigation renders an honest cap notice; pagination still needed), parented under #2289, board status AI Batter Up.
  Working tree clean at session end; no pre-existing dirty files encountered.
