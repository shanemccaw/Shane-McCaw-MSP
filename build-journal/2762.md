# #2762 — Monitoring: close coverage gap on the compliance surface (34 readable, uncovered config resources)

- **Status:** ✅ DONE 2026-09-04T06:22:53Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 25a09d51d (real work, on main as 2126f66f9 after rebase+push), 7f977c69a (IN FLIGHT bookend, on main as 9d5d7b54a)

## Log
- 2026-09-04T05:59:00Z ⏳ IN FLIGHT — confirmed #2761 (blocker) has a real DONE bookend. Pulled the live query: `surface='compliance' AND availability='available_now' AND check_coverage_count=0` = 36 rows (not 34; issue's cached number has drifted slightly, using the live set per the issue's own instruction). 35 of 36 are `m365dsc:SC*`-origin resources needing Security & Compliance Center PowerShell cmdlets (Connect-IPPSSession); 1 is Graph (`/informationProtection/policy/labels`). Investigating current live state of the ps-execution PowerShell pipeline before designing checks against it (recent history: #1389 MSAL conflict fixed by #1400; #1482 stdout-contamination fixed; #1614 Shane-To-Do cert sync between Key Vault `mt-app-cert` and app `9ea2e409...` — closed, needs live re-confirmation). Also noting #1961 is `in-flight` concurrently, adding *unfiltered snapshot-shaped* catalog entries to the same `cmdlet-catalog.ps1` file for a different consumer (config-snapshot-collector) — my own entries are check-shaped (PostFilter/mapping/severity_rules), a different shape per #1961's own text, so no scope overlap expected, but noting the shared-file collision risk on `services/ps-execution/cmdlet-catalog.ps1` going into this build.
- 2026-09-04T06:05:00Z ⏳ IN FLIGHT — attempted live PS-execution smoke test via `shaneapp://executeCmdlet?cmdletKey=get-connection-info&tenantId=1`: refused, `testbed gate refused: ... target machine actively refused it (localhost:8080)`. Confirmed real: `node scripts/dev-server/status.mjs` shows `server process: pid 14260 DEAD port 8080`; `curl -m 3 http://localhost:8080/api/health` connection refused. Tried the sanctioned agent restart path, `node scripts/dev-server/request-restart.mjs --agent 2762-q1513`: failed with a real add/add merge conflict in the shared `C:\dev-server` checkout on `build-journal/1561.md`/`1582.md` (stale artifacts from other worktrees), rolled back cleanly, no restart fired. Per CLAUDE.md's "genuinely unreachable" escape valve (a few real attempts with real evidenced failure, not an unbounded loop): stopped retrying, filed **#2830** (parented to Feature #1128, `bug`, board status "AI Batter Up"), and proceeded using real alternate live evidence instead — `ps_capability_survey_results` (#1793's own live app-only capability survey against the SAME testbed tenant, `mccawsoft2.onmicrosoft.com`/tenants.id=1), which had ALL 10 of my candidate cmdlets confirmed `status='ok'` across 3-4 independent runs each, with real observed item counts (up to 225 for `Get-DlpSensitiveInformationType`, 47 for `Get-ProtectionAlert`).
- 2026-09-04T06:15:00Z ⏳ IN FLIGHT — designed 10 checks against the real survey evidence (live-observed `property_names` where the tenant has live instances; `derived_property_names`/M365DSC-schema-declared otherwise — both cases labeled honestly in the migration). Ran the real `matchEndpointToResource`/`resolvePsCmdletCatalog` (`scripts/config-state/map-monitor-checks.mjs`) directly against the real parsed `cmdlet-catalog.ps1` + full `config_resources` table before writing SQL — confirmed 9/10 resolve `high` confidence (exact DSC-resource-noun match), 1 (`compliance:retention-event-type-unlinked`) resolves `low` (real, unique, sole match — just a pre-existing scoring-algorithm naming-mismatch quirk, recorded honestly not overstated). Wrote and ran `lib/db/migrations/manual/2026-09-04-compliance-surface-coverage-2762.sql` against local `DATABASE_URL`: 10 `monitor_checks` rows, 40 `monitoring_package_checks` memberships (core:premier/core:growth/detail:full-item-collection/assess:copilot-readiness — matching the existing `compliance:weak-dlp-policies` precedent), 10 `config_resource_check_coverage` rows, coverage recompute. Real effect confirmed: `surface='compliance' AND availability='available_now' AND check_coverage_count=0` dropped from 36 → 26 (the remaining 26 filed as a real follow-up, **#2831**, same parenting/labels/board-status, with the exact per-resource reason list). Added 10 unfiltered entries to `services/ps-execution/cmdlet-catalog.ps1` (no collision with #1961's concurrent in-flight work — different cmdletKeys, and unfiltered-by-design means #1961 can reuse these for the snapshot collector rather than duplicating).
- 2026-09-04T06:20:00Z ✅ DONE — Wrote a permanent vitest regression test (`artifacts/api-server/src/lib/__tests__/compliance-surface-coverage-2762.test.ts`, 26 assertions, registered in `vitest.config.ts`'s include list, mirroring #1301's zero-dlp-policies-check precedent: reads mapping/severity_rules straight out of the shipped migration, drives them through the real `applyMapping`/`classifySeverity`) — **this caught 2 real bugs before shipping**, both fixed in the migration + a corrective `UPDATE` against the already-inserted DB rows: (1) `compliance:protection-alert-policy-disabled`'s severity_rules had the `info` rule listed before the `warning` rule — `classifySeverity` is first-match-wins, not highest-severity-wins, so a disabled default alert was silently classifying as `info` instead of `warning`; reordered. (2) `compliance:record-tag-missing-reviewer`'s countWhere expression used a parenthesized OR (`{{Regulatory}} == true && ({{ReviewerEmail}} == null || {{ReviewerEmail}} == "")`) — `evalConditionGrammar` does NOT support parenthesized grouping (confirmed by reading `monitor-executor.ts`'s own `evalClause`, which naively splits on top-level `||` then `&&`), so the malformed clause matched almost everything; rewrote using the `length==0` operator (`{{Regulatory}} == true && {{ReviewerEmail}} length==0`), which correctly treats null/undefined/empty-string as length 0 with no OR needed. Full 26/26 pass after both fixes (`npx vitest run src/lib/__tests__/compliance-surface-coverage-2762.test.ts`). Ran the full api-server vitest suite too: 207 passed / 89 failed test files, all 89 pre-existing/unrelated (financial-aggregator rounding bugs etc — my own new file is not among them, confirmed by grep). `npx tsc --noEmit` on api-server: clean, zero errors. Findings filed: **#2830** (local dev-server outage blocking `shaneapp://` verification platform-wide — a real add/add merge conflict in the shared `C:\dev-server` checkout plus a separately-observed dangling `node_modules/pino` junction) and **#2831** (the 26 remaining compliance-surface resources, with the full per-resource reason breakdown). Committed as `25a09d51d`, rebased onto `origin/main` and pushed directly (`git push origin HEAD:main`) — landed as `2126f66f9`. `verify-branch-merged.mjs`: OK. `git status --porcelain`: clean. Labels flipped `in-flight`→`complete` on #2762; completion comment posted.
