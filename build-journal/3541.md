# #3541 — human-action after Console-executed catalog scripts has no real changeRequestId to attest against

- **Status:** ✅ DONE 2026-09-12T23:27:05Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 8d7c1f31f (rebased onto main as 1aeeaf00f3c9a5648af873fbac127cd89afb7d05)

## Log
- 2026-09-12T23:00:27Z ⏳ IN FLIGHT — Investigated #3541 + #3464 + the real `msp-launch-control.ts` / `msp-change-executions.ts` / `msp-change-execution-store.ts` / `portal-change-catalog.ts` code. Confirmed the real gap and the real fix: `msp-launch-control.ts`'s `POST /execute` never creates a `msp_change_requests` row, so there is nothing for `human-action` to attest against. Also confirmed `human_action` was the wrong executor classification to begin with — `cr_executions.executor_kind` already has a real `write_action` value ("a change a code path can confirm"), which is exactly what a Launch Control Graph write is; `human_action` is reserved for "a change only a person can make... no code path observes it". Fix: `POST /msp/:mspId/launch-control/execute` now raises a real, pre-approved `standard` CR (same governance shape `POST /portal/change-catalog/:id/execute` (#1498) already established) before executing, then records the execution as `write_action` and closes the CR to `completed` on success — entirely server-side, no client attestation call needed. Extending `recordExecution` to support an immediately-known outcome for a synchronous (non-wf_run) write action.
- 2026-09-12T23:27:05Z ✅ DONE — Shipped in commit `8d7c1f31f` (real hash on `main`: `1aeeaf00f3c9a5648af873fbac127cd89afb7d05`). New `artifacts/api-server/src/lib/launch-control-change-request.ts`: `raiseChangeRequestForLaunchControlExecution` (raises a real, pre-approved `standard` msp_change_requests row — domain→category and safeOrGated→riskLevel mapped from the real `write_action_catalog` shape, `approvedBy` left null so `materializeApprovalsForChange`'s 0-stage branch records "Standard change — pre-approved by policy" rather than naming a human who never signed off) + `recordLaunchControlExecutionOutcome` (records the write as a `write_action` cr_execution, closes the CR to `completed` on success). `msp-change-execution-store.ts`'s `recordExecution` extended with an optional `outcome` for a synchronous write_action/runbook_run with no `wf_run` (so `settleChangeExecutions`'s wf_run-join sweep never needs to see it — it's already confirmed at record time). `msp-launch-control.ts`'s execute route wires both calls around the existing Graph write and returns a real `changeRequest: {id, code}`. MyArchitect: `LaunchControlExecuteResponse` carries the new `changeRequest` field; `RunScriptLibraryActionAsync`'s Console output now surfaces the real CR code. No migration needed — reused existing `msp_change_requests`/`cr_executions` tables and columns verbatim. Verified: 4 new live-DB tests (against local Postgres, rows cleaned up in `afterAll` — confirmed via psql post-run) + 25 pre-existing `msp-change-execution-store`/`msp-change-execution` tests all pass (29/29 total); `tsc --noEmit` clean on `artifacts/api-server`; `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors. `verify-branch-merged.mjs` — OK, merged into `origin/main`. `git status --porcelain` clean. No findings filed this session (no genuine out-of-scope bug hit — the rollback route (`POST /launch-control/rollback/:auditLogId`) does not yet raise its own inverse CR via `raiseRollbackChangeRequest`, which would be a natural follow-up for full governance parity, but is a real enhancement beyond this issue's stated scope, not a bug — left for a future issue if Shane wants it, not filed as a finding since it isn't a defect in what's shipped here).
