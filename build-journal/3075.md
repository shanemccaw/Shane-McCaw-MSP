# Git #3075 — action.remove-auth-method fails outright when a stale Entra replica 404s the method-type read

- **Status:** ✅ DONE 2026-09-07T17:12:49Z
- **Scope:** platform
- **Build:** 1854 (BuildConsole), branch `agent/3075-q1854`

## Log

- **⏳ IN FLIGHT 2026-09-07T16:46:14Z** — Opened. `runRemoveAuthMethodAgainstTenant()`
  (`artifacts/api-server/src/lib/workflow-executor.ts:904`) resolves the method's
  `@odata.type` with a single un-retried `graphReadForTenantWithWriteToken` GET, which
  throws on any non-2xx — so a 404 from an Entra replica that has not converged fails the
  whole step without ever attempting the DELETE. Same replica-lag class #2981 fixed for the
  MFA re-registration fan-out, running in the read direction. Plan: reuse
  `mfa-reregistration.ts` (the module #2981 already established for this lag class) rather
  than inventing a second mechanism — add the read-half convergence primitive there, wire
  `runRemoveAuthMethodAgainstTenant` to it, carry the real trail into the audit row, and
  unit-test it against real fakes with an injected `sleep`.

- **✅ DONE 2026-09-07T17:12:49Z** — Shipped in `f2f046c23` ("action.remove-auth-method replica-lag read
  convergence"), on `origin/main` (`verify-branch-merged.mjs` exit 0).

  **What changed**
  - `artifacts/api-server/src/lib/mfa-reregistration.ts` — added
    `runAuthMethodResolutionConvergence` + `AuthMethodResolutionPolicy` /
    `DEFAULT_AUTH_METHOD_RESOLUTION` (requiredAbsentReads 2, maxReads 4, 5s→15s backoff,
    30s budget). Deliberately placed in #2981's own module, beside
    `runMfaReregistrationConvergence`, rather than starting a second mechanism — same lag
    class, same evidence, same backoff shape, opposite direction.
  - `artifacts/api-server/src/lib/workflow-executor.ts` — `runRemoveAuthMethodAgainstTenant`
    now resolves the method type through that loop. A 404 must survive delayed re-reads
    before it is believed; 403/429/5xx still propagate as read failures. The audit row's
    `afterSnapshot` carries the real trail (`resolutionReads`, `resolutionWaitedMs`,
    `absentReads`, `unresolvedReason`).
  - `artifacts/api-server/src/lib/graph.ts` — `graphReadForTenantWithWriteToken` now throws a
    typed `GraphWriteTokenReadError` carrying the real HTTP status, with
    `isGraphWriteTokenReadNotFound()` as its 404 predicate, so replica lag is told apart
    from a 403 without regexing an error string. Message text unchanged.
  - `artifacts/api-server/src/lib/mfa-reregistration.test.ts` — 9 new tests (file 13 → 22).

  **Deliberate non-change, stated on the issue:** a corroborated-absent method still reports
  a FAILED step rather than a success. The fan-out can treat a 404 as "already gone" because
  its ids come from Graph's own enumeration; here the id is operator-supplied and can simply
  be wrong, so calling it done would manufacture the false-success class #2981 exists to
  prevent. What #3075 fixes is the reliability gap — the failure is now corroborated instead
  of one unlucky sample.

  **Verification (real, this session)**
  - `npx tsc -p tsconfig.json --noEmit` in `artifacts/api-server` — exit 0, clean.
  - `npx vitest run src/lib/mfa-reregistration.test.ts` — **22/22 passed**.
  - `npx vitest run workflow-executor` — **5 files, 293/293 passed**.
  - Full api-server suite with `DATABASE_URL` loaded: **332/340 files, 5266 passed, 24
    failed**. Every failure is pre-existing and unrelated: 4 files fail only on unset AI
    integration env vars (`AI_INTEGRATIONS_ANTHROPIC_BASE_URL`,
    `AI_INTEGRATIONS_OPENAI_BASE_URL`) and pass once `.env.local` is exported;
    `portal-customer-engines-assessment-redaction` and `portal-dashboard-route-collision`
    (9 failures, 500-vs-200) were confirmed failing **identically with my four files reverted
    to the pre-fix commit** — same 9 failed / 6 passed; the remaining two are
    `*.live-db.test.ts` timeouts.
  - No migration needed — no schema change. No test manifest: this is `artifacts/api-server`
    internals, outside the manifest scope (`msp-portal` / `shane-mccaw-consulting`).
  - Not live-fired against the testbed tenant: doing so means a real destructive per-user
    Graph write, and the convergence logic is unit-tested against real fakes precisely so it
    does not need one. The lag itself is already proven live in #2981's own evidence.

  **Findings filed:** #3100 — `action.remove-auth-method` and the MFA fan-out disagree on
  what a **DELETE** 404 means (`bug`, `security`, milestone v1.1, sub-issue of #2489, board
  status "AI Batter Up"). Resolving it needs a real semantics call on whether "already
  absent" is a satisfied remediation for an operator-supplied id, so it was not decided
  in-session.

  Working tree clean (`git status --porcelain` empty). Nothing was dirty on arrival.
