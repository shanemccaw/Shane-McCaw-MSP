# Git #3075 — action.remove-auth-method fails outright when a stale Entra replica 404s the method-type read

- **Status:** ⏳ IN FLIGHT 2026-09-07T16:46:14Z
- **Scope:** platform
- **Build:** 1854 (BuildConsole), branch `agent/3075-q1854`

## Log

- **⏳ IN FLIGHT 2026-09-07T16:46:14Z** — Opened. `runRemoveAuthMethodAgainstTenant()`
  (`artifacts/api-server/src/lib/workflow-executor.ts:904`) resolves the method's
  `@odata.type` with a single un-retried `graphReadForTenantWithWriteToken` GET, which
  throws on any non-2xx — so a 404 from an Entra replica that has not converged fails the
  whole step without ever attempting the DELETE. Same replica-lag class #2981 fixed for the
  MFA re-registration fan-out, running in the read direction. Plan: reuse
  `mfa-reregistration.ts` (the module #2981 already established for this lag class) rather
  than inventing a second mechanism — add the read-half convergence primitive there, wire
  `runRemoveAuthMethodAgainstTenant` to it, carry the real trail into the audit row, and
  unit-test it against real fakes with an injected `sleep`.
