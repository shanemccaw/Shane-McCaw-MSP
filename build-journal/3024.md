# #3024 — C# code-behind Green/Red/Peach/Yellow/Sky brush status-vs-decorative audit

- **Status:** ✅ DONE 2026-09-12T22:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** c8b47dcad (rebased+pushed onto `main` as 9f58249e2; verified via
  `verify-branch-merged.mjs`)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Read #3024 body + comment, and #2127's landed
  fix pattern (81a0931ec, XAML pass) for the `FindResource`/`TryFindResource`/
  `SafeBrush` lookup-key retarget convention (genuine status sites →
  `StatusSuccess/Error/Warning/RunningBrush`, decorative/categorical sites left
  alone). Confirmed the three files #3024 says are already partially fixed
  (`TestRunnerWindow.xaml.cs`, `DeviceCodeWindow.xaml.cs` fully retargeted — 0
  remaining hits; `StreamingConsoleWindow.xaml.cs` — 1 remaining hit, the
  categorical `SkyBrush` Sql-log-level color, correctly left decorative).
  Re-ran the issue's own grep
  (`grep -rn "PeachBrush|GreenBrush|RedBrush|YellowBrush|SkyBrush" --include=*.cs
  desktop/BuildConsole`) fresh against current `main`: 41 files, ~393 hits (issue's
  380/39 count is a few commits stale but the same real surface). About to work
  through all 41 files site-by-site via parallel sub-agents split into 7 file
  groups, each doing the same status-vs-decorative judgment call #2127 did for
  XAML, retargeting only genuine status-meaning lookups.
- 2026-09-12T22:35:00Z ✅ DONE — All 7 groups completed the site-by-site read +
  classify pass across the full 41-file/~393-hit surface (one file,
  `FloatingChatWindow.xaml.cs`, was accidentally dropped from every group's
  assignment during dispatch — caught in reconciliation and fixed directly:
  its `ShowInlineMessage(isError)` `RedBrush`/`GreenBrush` pair, same pattern
  as its siblings, retargeted to `StatusErrorBrush`/`StatusSuccessBrush`).
  Result: **33 files touched, 224 genuine status-meaning lookup sites
  retargeted** to `StatusSuccessBrush`/`StatusErrorBrush`/`StatusWarningBrush`/
  `StatusRunningBrush`, preserving each file's existing lookup mechanism
  (`FindResource`/`TryFindResource`/local `SafeBrush` helper) — only the
  key-string argument changed. 8 files (`ManifestViewerWindow.xaml.cs`,
  `LogViewerDocumentView.xaml.cs`, `AiBatterUpPanel.xaml.cs`,
  `ActivityBar.xaml.cs`, `GraphApiDocumentView.xaml.cs`,
  `BuildSetColorRegistry.cs`, `ChatDocumentContainer.xaml.cs`,
  `StreamingConsoleWindow.xaml.cs`) were confirmed entirely decorative and
  left untouched. 173 decorative/categorical lookup sites remain across the
  surface by design (HTTP-method color coding, note/issue-number badge chips,
  build-set color registry, CTA button backgrounds, celebration confetti,
  log-level categorical tags, manifest-phase type legends, GitHub open/closed
  polarity, account-tier badges, etc.) — same discipline #2127 applied to
  XAML. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/0
  errors, before commit. Diff is a clean 224 insertions/224 deletions
  (one-line key-string swaps only) across 33 files.
  Real finding surfaced mid-audit and filed as its own sibling issue under
  #1791 (this issue's Feature parent), not fixed in-scope here: **#3862** —
  `GitDoctorView.xaml.cs`'s run-log line renders unconditionally green
  regardless of actual command success/failure (the `(failed)` suffix is
  text-only). Labeled `bug`, milestone 5, board status set to "AI Batter Up".
  Committed as c8b47dcad, rebased onto `origin/main` and pushed as 9f58249e2.
  `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
  Full scope of #3024 completed in one pass — no remainder to split off as a
  sibling issue.
