# #3024 — C# code-behind Green/Red/Peach/Yellow/Sky brush status-vs-decorative audit

- **Status:** ⏳ IN FLIGHT 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Read #3024 body + comment, and #2127's landed
  fix pattern (81a0931ec, XAML pass) for the `FindResource`/`TryFindResource`/
  `SafeBrush` lookup-key retarget convention (genuine status sites →
  `StatusSuccess/Error/Warning/RunningBrush`, decorative/categorical sites left
  alone). Confirmed the three files #3024 says are already partially fixed
  (`TestRunnerWindow.xaml.cs`, `DeviceCodeWindow.xaml.cs` fully retargeted — 0
  remaining hits; `StreamingConsoleWindow.xaml.cs` — 1 remaining hit, the
  categorical `SkyBrush` Sql-log-level color, correctly left decorative).
  Re-ran the issue's own grep
  (`grep -rn "PeachBrush|GreenBrush|RedBrush|YellowBrush|SkyBrush" --include=*.cs
  desktop/BuildConsole`) fresh against current `main`: 41 files, ~393 hits (issue's
  380/39 count is a few commits stale but the same real surface). About to work
  through all 41 files site-by-site via parallel sub-agents split into 7 file
  groups, each doing the same status-vs-decorative judgment call #2127 did for
  XAML, retargeting only genuine status-meaning lookups.
