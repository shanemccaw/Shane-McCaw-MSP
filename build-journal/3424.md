# #3424 — Feature: Remove legacy Clients/CRM suite from Admin Panel (AdminPanel)

- **Status:** ✅ DONE 2026-09-11T17:00:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** b23ba7a6d

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — pulled issue #3424 body + comments (in order), confirmed scope: delete `artifacts/admin-panel/src/pages/crm/Clients.tsx`, `ClientDetail.tsx`, `ClientM365HealthTab.tsx`, their backend routes, and both nav entries (Clients + Delivery > Clients). Noted the real dependency to check before deleting: `ClientDetail.tsx` imports `KanbanCardModal` for a "Kanban Bucket Preview" — a different, live board also used in `ProjectDetail.tsx` (moving to MSP Console in a later issue, not this one). Checked the build header's `--blocked-by 3423` dependency before touching any code.
- 2026-09-10T00:00:00Z 🛑 BLOCKED — `#3423` (Feature: Remove Diagnostic Shares page from Admin Panel) is still `state: open`, labeled `in-flight`, not `complete` — and is itself `🛑 BLOCKED` (its own bookend, `build-journal/3423.md` on `origin/main`, records it waiting on `#3422`, which is also still open/`in-flight`). Real chain: `#3424 → #3423 → #3422`, all three touching the same Admin Panel CRM nav/routes surface (`artifacts/admin-panel/src/pages/crm/*`, the shared nav config, and shared App routes). Confirmed `DiagnosticShares.tsx` (the #3423 target) is still present on `origin/main`, so #3423 genuinely has not landed. Real `blocked_by` edge from #3424 → #3423 already exists (confirmed via `gh api repos/.../issues/3424/dependencies/blocked_by`) and is accurate — left as-is, no new edge needed. Per CLAUDE.md's blocked-issue rules, stopping here rather than proceeding concurrently: deleting Clients/CRM nav+route entries at the same time #3423/#3422 are (or will be) deleting sibling nav+route entries in the same files risks a real collision. No code changed this session; labels swapped `in-flight` → `blocked` and a comment posted on #3424 explaining the wait.
- 2026-09-11T16:30:00Z ⏳ IN FLIGHT — resumed. Confirmed unblocked: `#3423` and its own blocker `#3422` are both now `complete`-labeled and merged into `origin/main`. Removed the stale `blocked_by` edge, swapped `blocked` → `in-flight`. Investigated the issue's flagged Kanban risk directly: `ClientDetail.tsx`'s "Kanban Bucket Preview" was pure inline JSX over locally-computed task data — it never imported `KanbanCardModal` at all (the issue body's premise was stale); the real board's only import lives in `ProjectDetail.tsx`, confirmed untouched. Audited every consumer of `admin-clients.ts` / `admin-client-documents.ts` routes across the whole repo (admin-panel, portal, msp-console, msp-website, mcp-server, lib) to determine which of the ~24 routes were exclusive to the 3 doomed pages vs. load-bearing for other live features (Reports, Invoices, Projects/ProjectDetail, EmailActivity, the real Kanban board's client picker, TenantSignals, workflow builders, mcp-server tools).
- 2026-09-11T16:55:00Z ✅ DONE — Deleted `Clients.tsx`, `ClientDetail.tsx`, `ClientM365HealthTab.tsx`, both nav entries (`pipe-clients`/"Accounts" section and `del-clients` in `workspaceNav.tsx`), and the now-orphaned `M365ProfileWizard.tsx` (sole caller was `Clients.tsx`). Rewrote `admin-clients.ts` down to the 5 routes still used elsewhere (enriched, with-azure-credentials, command-center, health/summary, bare list); deleted `admin-client-documents.ts` entirely (ClientDetail-only) and unregistered it from `routes/index.ts`; deleted `admin-clients.test.ts` (tested only the now-removed DELETE route). `/crm/clients/:id` in `App.tsx` is now a redirect to `/pipeline/clients` rather than a bare 404 — a live deep link still points at it (the App Registration expiry alert email in `admin-clients.ts`, and the AI next-best-actions prompt's suggested link format) — same precedent as `/crm/leads/:id` in #135; filed the resulting context-loss tradeoff as #3674. Updated `ADMIN_PORTAL_DOCS.md` §3.1/3.2/8.4 to record the removal, and fixed §3.7 (M365 Intelligence) which had been left stale since #3422. Verified: `tsc --noEmit` clean for both `admin-panel` and `api-server`; ran the full `api-server` vitest suite — 26 pre-existing failures unrelated to this change (missing `DATABASE_URL` in the raw invocation, msp-alerts/timeline, azure-rm probe tests), none touching `admin-clients`/`admin-client-documents`. Verified own branch merged into `origin/main` via `verify-branch-merged.mjs` (commit `b23ba7a6d`) and `git status --porcelain` clean before writing DONE. Findings filed: #3674 (parented under #1095, board status "AI Batter Up").
