# #4342 — Feature: Projects section on the MyArchitect Tenant Dashboard

- **Status:** ✅ DONE 2026-09-16T04:04:01Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** shanemccaw/Shane-McCaw-MSP@7861f3480, shanemccaw/MyArchitect@2a52df8

## Log
- 2026-09-16T03:22:21Z ⏳ IN FLIGHT — confirmed `GET /api/admin/projects` (artifacts/api-server/src/routes/admin-projects.ts:227, this repo) has no customer/tenant filter, only `clientUserId`. `usersTable.tenantId` (lib/db/src/schema/index.ts:109) references `tenantsTable.id`, matching the issue's `customerId` = `tenants.id` model. Adding an additive `?customerId=` query param here (option 1, preferred), then wiring a Projects card into MyArchitect's `Controls/HomeDashboardView.xaml` reusing `IProjectsService`. Provisioned isolated worktree `C:\wt\4342-myarchitect` (branch `agent/4342-myarchitect`) for the MyArchitect-repo half of this work.
- 2026-09-16T04:04:01Z ✅ DONE — Backend: added optional `?customerId=` filter to `GET /api/admin/projects` (Shane-McCaw-MSP@7861f3480), resolving `customerId` (tenants.id) server-side to every `usersTable` row with matching `tenantId`, then filtering `projects.client_user_id IN (...)` alongside the existing MSP-scope and `clientUserId` filters. Verified the new subquery shape directly against local Postgres in a rolled-back transaction (scratch user under `tenant_id=1402` + scratch project — came back correctly through the filter, then `ROLLBACK`). `pnpm run typecheck` in `artifacts/api-server` passed clean (exit 0).
  UI: new PROJECTS card in MyArchitect's `Controls/HomeDashboardView.xaml` (MyArchitect@2a52df8) — real title/status/progress rows (top 3, newest first) or an honest empty ("No projects for this tenant yet.")/error state, following the existing Retainer/Telemetry/Assessment/Remediation/Alerts card pattern exactly (loading/content/error panel triples, `DashboardCardStyle`, no animation per UI_RULES.md §11, no hardcoded data per §10). `IProjectsService.GetProjectsAsync` gained an optional `customerId` parameter (`ProjectsService` builds `?customerId=` alongside the existing `?clientUserId=`); `HomeDashboardView` wires it through `tenant.CustomerId`. Link-through opens the existing real Kanban Board gallery. Hit a real rebase conflict against a concurrent Sales-Offers build (#4341) landing the same file at the same time — resolved as a straight union (both sections/fields kept, nothing dropped) and rebuilt clean after each rebase.
  Verification: `dotnet build MyArchitect.csproj -c Release` — 0 warnings, 0 errors, both before and after the rebase. Both worktrees' branches confirmed ancestors of their respective `origin/main` (`verify-branch-merged.mjs` for Shane-McCaw-MSP; `git merge-base --is-ancestor` for MyArchitect). Both working trees clean.
