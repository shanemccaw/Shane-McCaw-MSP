# #3427 — Remove legacy Status Reports from Admin Panel

- **Status:** ✅ DONE 2026-09-10T05:25:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** b6f5a620f (main) — "Remove legacy Status Reports from Admin Panel (#3427)"

## Log
- 2026-09-10T05:13:06Z ⏳ IN FLIGHT — Pulled issue #3427 body + both BUILD comments (later comment, posted after this
  session's own --blocked-by 3426 dispatch, dropped the blocker/buildSet and lowered effort — treating this session's
  own dispatch flags as authoritative since they're what actually launched it). Audited real DB state: `status_reports`
  table = 0 rows in local dev. Audited all real consumers of the 3 named files beyond the issue's own file list and
  found the audit undercounted scope: `ProjectDetail.tsx` imports `StatusReportForm` directly and threads an `onReply`
  handler through `DraggableCard`/`DroppableColumn`/`KanbanBoard` that calls `admin-status-reports.ts`'s `/reply`
  route; `ClientDetail.tsx` has a live read-only "Status Reports" section backed by a separate route
  (`admin-client-documents.ts`, not deleted) with 3 dead-end nav links to the page being removed; `Projects.tsx` has
  one more dead-end nav button. Deleting only the literal 3 files would have broken the TypeScript build. Proceeding
  with full teardown across all real consumers (not just the 3 named files) since the table is empty in local dev and
  the issue's own framing ("Replit-era pass," replacement tracked separately in #3434/#3435) supports full removal,
  not partial.
- 2026-09-10T05:25:00Z ✅ DONE — Deleted the 3 named files + nav entry, plus the real consumers found in
  ProjectDetail.tsx (embedded `StatusReportForm` dialog + Kanban-card "Customer Question" reply thread threaded
  through DraggableCard/DroppableColumn/KanbanBoard), ClientDetail.tsx (whole read-only "Status Reports" section —
  its own nav destinations were gone so it was dead-ended), Projects.tsx (one nav button), workspaceNav.tsx
  (`extraPrefixes` array), and IDEShell.test.ts (2 assertions). Left `admin-client-documents.ts`'s
  GET .../status-reports route, `admin-ai-draft.ts`'s POST .../status-reports/ai-draft route, and
  mailer.ts/notification-center.ts's now-unused exports in place (each file also serves other live routes; filed
  as a finding rather than editing shared files beyond this issue's real scope) — **filed #3495** under #1095,
  board status AI Batter Up. `admin-panel` + `api-server` both typecheck clean (`npx tsc --noEmit`, exit 0, run
  three times: pre-commit, post-first-rebase, post-second-rebase). `IDEShell.test.ts` 24/24 pass. Rebased twice
  onto a fast-moving `origin/main` (other concurrent sessions landed commits between each attempt); pushed clean
  to main at b6f5a620f. `verify-branch-merged.mjs` confirms ancestor OK. `git status --porcelain` clean at end.
  Labels synced (`complete` on #3427, `in-flight` removed). No blockers, no Shane-To-Do action.
