# #3427 — Remove legacy Status Reports from Admin Panel

- **Status:** ⏳ IN FLIGHT 2026-09-10T05:13:06Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-10T05:13:06Z ⏳ IN FLIGHT — Pulled issue #3427 body + both BUILD comments (later comment, posted after this
  session's own --blocked-by 3426 dispatch, dropped the blocker/buildSet and lowered effort — treating this session's
  own dispatch flags as authoritative since they're what actually launched it). Audited real DB state: `status_reports`
  table = 0 rows in local dev. Audited all real consumers of the 3 named files beyond the issue's own file list and
  found the audit undercounted scope: `ProjectDetail.tsx` imports `StatusReportForm` directly and threads an `onReply`
  handler through `DraggableCard`/`DroppableColumn`/`KanbanBoard` that calls `admin-status-reports.ts`'s `/reply`
  route; `ClientDetail.tsx` has a live read-only "Status Reports" section backed by a separate route
  (`admin-client-documents.ts`, not deleted) with 3 dead-end nav links to the page being removed; `Projects.tsx` has
  one more dead-end nav button. Deleting only the literal 3 files would have broken the TypeScript build. Proceeding
  with full teardown across all real consumers (not just the 3 named files) since the table is empty in local dev and
  the issue's own framing ("Replit-era pass," replacement tracked separately in #3434/#3435) supports full removal,
  not partial.
