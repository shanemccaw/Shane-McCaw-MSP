# #2985 — Verify api-server/lib/db node_modules emptied-tree defect still reproduces

- **Status:** ✅ DONE 2026-09-11T05:42:17Z
- **Scope:** platform (dev-server / worktree tooling)
- **Started:** 2026-09-11
- **Commit(s):** 7e02f920f9 (IN FLIGHT), b6942ad719 (DONE)

## Log
- 2026-09-11T05:30:50Z ⏳ IN FLIGHT — verifying whether the main checkout's `artifacts/api-server/node_modules` (and `lib/db/node_modules` per #3000 fold-in) is still emptied; will run store-doctor.mjs and a fresh-worktree tsc+vitest check before deciding close-vs-fix.
- 2026-09-11T05:42:17Z ✅ DONE — defect does NOT reproduce on current main (@ e801acbbe, later rebased through b3a8e5f6).
  Real evidence, in order:
  1. `git fetch origin && git reset --hard origin/main` against the MAIN checkout at
     `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` (was already porcelain-clean before reset).
  2. Real entry counts in the main checkout: `artifacts/api-server/node_modules` = 36,
     `lib/db/node_modules` = 10, `artifacts/admin-panel/node_modules` = 50,
     `artifacts/portal/node_modules` = 36, `artifacts/msp-website/node_modules` = 35,
     root `node_modules` = 741. Both hosts named in the issue and its #3000 fold-in are
     populated — no repeat of the empty/`.vite-temp`-only state.
  3. `node scripts/dev-server/store-doctor.mjs` → `hosts: 21, links checked: 2565, .bin files
     checked: 162` → `CLEAN — no foreign, dangling or poisoned entries.` (real output, verbatim).
  4. Fresh worktree via `node scripts/dev-server/provision-worktree.mjs 2985-verify --link`
     (base `origin/main @ b3a8e5f6`, 1014 dependency dirs junctioned, 12 `lib/*/dist` built).
     From that worktree's `artifacts/api-server`: `require.resolve('drizzle-orm')` and
     `require.resolve('@workspace/db')` both resolved — the latter to the worktree's own
     `lib/db/src/index.ts`, confirming no #2121/#2094 realpath-into-main-checkout hazard.
     `npx tsc --noEmit` → clean, zero output/errors. `npx vitest run` → 291 files / 4810 tests
     passed, 2/19 skipped; 27 test failures across 67 files, all pre-existing and unrelated to
     dependency resolution (assertion-content mismatches in `msp-alerts.test.ts` and
     `msp-customer-timeline.test.ts`, `azure-rm.test.ts` failures, and a `DATABASE_URL must be
     set` error from running `npx vitest run` directly without the project's full `.env.local`
     loading path — an environment-loading gap, not a module-resolution failure; zero
     `Cannot find module`/`ERR_MODULE_NOT_FOUND` errors anywhere in the run). Worktree cleaned
     up afterward via `cleanup-worktree.mjs 2985-verify` (1014 junctions unlinked, branch
     deleted).
  5. #3000 fold-in findings re-checked: (a) `lib/db/node_modules` populated (10 entries, item 2
     above) — not reproducing. (b) `store-doctor.mjs`'s `MISSING`-state gap is still real —
     confirmed by direct code read: `nodeModulesHosts()` (line 115) silently drops any host
     whose `node_modules` doesn't exist, and even a host with an empty `node_modules` (the
     actual failure shape) passes the `existsSync` filter and then contributes zero links to
     scan, so `out.clean` stays `true` either way. No `MISSING` state exists anywhere in the
     file (`grep -n "MISSING" scripts/dev-server/store-doctor.mjs` → no matches). (c)
     `@workspace` realpath hazard: re-checked in item 4 above — resolves correctly to the
     worktree's own `lib/db`, not the main checkout, so `link-deps.mjs`'s existing per-package
     `@workspace` re-pointing is doing its job.
  - Filed **#3646** (preventive follow-up: add the `MISSING`-tree check to `store-doctor.mjs`),
    parented under **#1202** (Epic: Build Console, #2985 has no Feature-tier parent), labeled
    `bug`, milestone v1.1, board status set to "AI Batter Up".
  - Closed **#2985** as `completed` with real evidence posted as an issue comment.
  - No fix applied — nothing to fix; the checkout is healthy. No code changes in this session
    beyond this bookend file.
