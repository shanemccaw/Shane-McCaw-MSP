# #3992 — Signup, Agreement and Invite: wire the real Design into artifacts/portal

- **Status:** ✅ DONE 2026-09-14T03:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 08a3a3ff4 (feature), a1db0e45f (IN FLIGHT bookend)

## Log
- 2026-09-14T03:30:00Z ⏳ IN FLIGHT — Read contract pack + `.dc.html` design (3 scenes: tiers/success/invite). Confirmed live: `GET/POST /api/msp/signup/*`, `GET/POST /api/public/msp-invite/:token[/accept]`. Discovered the `/api/platform/agreement/*` routes the contract pack certified live on 2026-09-03 were deliberately deleted by #3412 ("no longer needed per Shane") on 2026-09-10 — no GET endpoint remains to read a published agreement, and no admin UI remains to ever publish one. Live DB confirms the only `platform_agreements` row is unpublished. Building signup/success/invite against the 3 designed scenes; the standalone `accept-agreement.tsx` page has neither a design export nor a live backend anymore, so it is out of scope this session — filing a finding, not inventing a design or resurrecting a deleted route.
- 2026-09-14T03:45:00Z ✅ DONE — Built `signup.tsx` (tiers off `GET /api/msp/signup/tiers`, `POST /api/msp/signup/start`), `signup-success.tsx` (polls `GET /api/msp/signup/success`, 5s/12x bound), `accept-invite.tsx` (`GET/POST /api/public/msp-invite/:token[/accept]`), a shared `SignupFlowShell` header, and `lib/msp-signup-api.ts`. Registered `/signup`, `/signup/success`, `/invite/:token` in `App.tsx` (the first two paths are load-bearing — they match `msp-signup.ts`'s own Stripe `success_url`/`cancel_url` literally). `tsc --noEmit` clean; `vite build` clean; live-curled all 5 real endpoints against the running local dev api-server (`:8080`) to confirm current response shapes match what was wired, including the live #2509 fix (add-on row already filtered server-side) and the real tier data (Free/Growth/Pro, no fixtures). Started the portal's own vite dev server locally and confirmed all 3 new routes 200 through the `/api` proxy with no server-side errors — did not go further into rendered-DOM/click-path verification: no `shaneapp://runTest`/WebView2 protocol was reachable from this interactive-chat session (this build was not BuildConsole-launched), and this repo's standing rule is no Playwright anywhere. Filed two real findings as sub-issues of #1649: #4008 (bug, AI Batter Up — `msp-signup.ts:96-98` reads the wrong `typeAttributes` key names, `aiCreditAllowance*` always null) and #4009 (bug, Ask Shane — `accept-agreement.tsx` has neither a Design export nor a live backend anymore after #3412; real product decision on whether it's still wanted). Working tree clean; branch verified merged into `origin/main` (08a3a3ff4).
