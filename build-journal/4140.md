# #4140 — Consent and Onboarding: missing finalize step 4, downgraded 409 banner, missing SERVICE field, missing ledger

- **Status:** ✅ DONE 2026-09-15T00:04:45Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ddbccc10e (implementation), c3dad0a8d (IN FLIGHT bookend)

## Log
- 2026-09-14T23:59:43Z ⏳ IN FLIGHT — investigated issue #4140 + design source (`Design/portal/design_handoff_full_site/screens/Consent and Onboarding.dc.html`) + contract pack; about to implement the 4 cited gaps across `consent-success.tsx`, `onboarding-link.tsx`, and shared `ConsentOnboardingShell.tsx`.
- 2026-09-15T00:04:45Z ✅ DONE — all 4 gaps fixed, real commit ddbccc10e on main:
  1. Added the 4th "Free registration completed" step to `consent-success.tsx`'s finalize step list (`done: finalized`).
  2. Replaced the generic `<Alert variant="warning">` for the consent-first 409 with a dedicated dashed-amber banner (`AlertCircle`, "409 · consent-first" code, "Reconnect Microsoft 365" + "Contact your provider" buttons). Reconnect reuses the existing checkout-URL-building logic (extracted into `goToCheckout()`, same URL the pre-existing "Continue to checkout" fallback used); Contact your provider matches the existing `consent-tenant-conflict.tsx` precedent (`window.history.back()`) — no new backend endpoint invented.
  3. Added the SERVICE field to `onboarding-link.tsx`'s info grid (name via a real `GET /api/services` catalog lookup keyed on `link.serviceId`, pricing note via the existing `serviceIsFree()` helper; honest "Not specified" fallback when a link carries no `serviceId`, since that field is genuinely nullable server-side).
  4. Added a shared `ConsentLedger` component ("What these pages deliberately do not do") to `ConsentOnboardingShell.tsx`, wired into all 5 pages (`consent-success.tsx` all 3 branches, `consent-declined.tsx`, `consent-tenant-conflict.tsx`, `portal-identity-interstitial.tsx`, `onboarding-link.tsx` both terminal branches) — content/section refs (`§1`, `§9a`, etc.) carried verbatim from the design's own ledger array, same convention as `data-rights-and-privacy.tsx`'s LEDGER.
  Explicitly left untouched per the issue's own note: `portal-identity-interstitial.tsx`'s role-pill list (stale-design case, not a wired-code gap).
  Verification: `pnpm run typecheck` (artifacts/portal) — clean. `pnpm run build` (artifacts/portal) — succeeded (pre-existing sourcemap/chunk-size warnings unrelated to this change). No new backend/DB work needed — no schema or endpoint gaps found, this was a pure frontend wiring gap. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
