# #1427 — Governance: 5 area pages (orphaned-teams/team-owners/device-inventory/device-lifecycle/sharing-drift-legacy) — replace 100% fabricated content with honest no-data state

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 1b0d65157 (bookend), 0b54a7f90 (implementation)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Follow-up from #1413 audit (confirmed no backend exists
  for any of this content). All 5 routes share `portal-v2-gov-area.tsx`, which reads
  its entire page — including the title itself — from `govAreaData.ts` fixtures with
  zero `dataState` gating anywhere. Plan: strip every fabricated field (titles,
  stats, per-device rows, per-team rows, drift events) from the rendered page and
  replace with an honest `NoScanDataState` block; keep only a static, count-free
  per-slug category label for navigational identity (not tenant data). Fixture
  module stays in place as a documented backend-gap reference (matches the
  convention other pillars used), but the page will never render its data-bearing
  fields again. Confirmed no overlap with sibling in-flight branches #1428
  (`governance-sharing-drift` `:area` wildcard, a different component) and #1426
  (Governance oversharing page, also different).
- 2026-08-27 ✅ DONE — Shipped in `0b54a7f90`. `portal-v2-gov-area.tsx` no longer
  renders any of `govAreaData.ts`'s fixture content for any of the 5 routes; each
  now shows a static, count-free category title (`govAreaTitleFor`) plus an honest
  `NoScanDataState` block (`govAreaDetailFor` for the per-check detail line). The
  three original fixture maps (`GOV_LIST_PAGES`/`GOV_DRIFT_PAGES`/
  `GOV_INVENTORY_PAGES`) stay in the module, unused by the page, as documented
  reference for a future real per-sub-area backend. Updated
  `test-manifests/portal/governance-area-drilldowns.json` (GOV2/GOV4/GOV7 assert
  the new honest titles, GOV3/GOV5/GOV6/GOV8 assert the honest empty-state marker
  and its detail line instead of fabricated rows/stats/events; new steps tagged
  `gov-1427`). Verification: `tsc -p tsconfig.json --noEmit` clean for every
  touched file (2 pre-existing unrelated errors remain in untouched
  `SowScreen.tsx`/`PortalV2Shell.tsx`); `govAreaModel.test.ts` 10/10 green
  (including new coverage for `govAreaTitleFor`/`govAreaDetailFor`, with a
  no-digit assertion on every detail line). **Honest gap:** a live
  `shaneapp://runTest` UI-step run was attempted but blocked — merging this
  commit into the shared dev-server checkout hit a real merge conflict in
  `test-manifests/portal/security-drilldowns.json` (an unrelated file from a
  concurrent sibling build in the same `Portal` build set), so the running dev
  server does not yet reflect this code. The tool aborted the merge cleanly (no
  broken checkout state left behind); I did not attempt to resolve that
  conflict myself since it belongs to a manifest file I never touched. The
  GOV1-8 UI assertions above are code-verified only pending that conflict's
  resolution and the next server restart — model-layer tests and typecheck are
  the real, live-verified result this session actually has.
