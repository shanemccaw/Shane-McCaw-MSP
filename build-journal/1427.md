# #1427 — Governance: 5 area pages (orphaned-teams/team-owners/device-inventory/device-lifecycle/sharing-drift-legacy) — replace 100% fabricated content with honest no-data state

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Follow-up from #1413 audit (confirmed no backend exists
  for any of this content). All 5 routes share `portal-v2-gov-area.tsx`, which reads
  its entire page — including the title itself — from `govAreaData.ts` fixtures with
  zero `dataState` gating anywhere. Plan: strip every fabricated field (titles,
  stats, per-device rows, per-team rows, drift events) from the rendered page and
  replace with an honest `NoScanDataState` block; keep only a static, count-free
  per-slug category label for navigational identity (not tenant data). Fixture
  module stays in place as a documented backend-gap reference (matches the
  convention other pillars used), but the page will never render its data-bearing
  fields again. Confirmed no overlap with sibling in-flight branches #1428
  (`governance-sharing-drift` `:area` wildcard, a different component) and #1426
  (Governance oversharing page, also different).
