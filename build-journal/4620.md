# Build Journal — #4620

- **Status:** ✅ DONE 2026-09-18T05:05:38Z
- **Scope:** buildconsole

## Log
- 2026-09-18T05:02:35Z — Started. Pulled issue #4620 body + comment (BUILD dispatch). Investigated
  existing `<unset>` handling: `TestRunVariables` already has a full "pause-on-unset" mechanism
  (Git #953/#961/#803) that correctly treats `<unset>`/needsReview config vars as absent for
  manifest interpolation and fails/prompts at the start of each step, not deep into interpolation.
  Found the real remaining gap: `MainWindow.ShaneAppRunScan.cs`'s `GetVar` (backing
  `shaneapp://runScan`) reads `TEST_PORTAL_EMAIL`/`TEST_PORTAL_PASSWORD` directly and only checks
  `IsNullOrWhiteSpace`, so a literal `<unset>` value passes through to a real login attempt and
  fails as a confusing remote auth error instead of the clear "credentials not set" message.
  See build-journal/4620-plan.md for full root cause + proposed fix.
- 2026-09-18T05:05:38Z — ✅ DONE — Fixed `GetVar` in `MainWindow.ShaneAppRunScan.cs` to treat a
  value literally equal to `TestManifestVariableScanner.AutoDefaultValue` (`<unset>`) as absent,
  same as null/blank, so it falls into the existing "testbed Assessment credentials not set"
  `Fail(...)` branch instead of reaching `LoginAsync` with the placeholder string. Commit
  `f638a8e34` (pushed, merged into origin/main — verified via
  `node scripts/dev-server/verify-branch-merged.mjs`, exit 0). `dotnet build
  desktop/BuildConsole/BuildConsole.csproj` succeeded, 0 warnings/errors. `git status --porcelain`
  clean.

## Changes Made
- `desktop/BuildConsole/MainWindow.ShaneAppRunScan.cs` — `GetVar` closure now returns `null` for
  a stored value equal to `TestManifestVariableScanner.AutoDefaultValue` (`<unset>`), not just for
  null/blank, so `TEST_PORTAL_EMAIL`/`TEST_PORTAL_PASSWORD` still carrying the scanner's
  never-reviewed placeholder are surfaced as "not set" up front instead of reaching a real login
  attempt.
- No schema/migration/UI changes. Manifest-interpolation `<unset>` handling
  (`TestRunVariables`) was already correct and untouched.

## Verification Results
- `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0 Warning(s), 0 Error(s).
- Code-read confirmation: a `TestEnvironmentVariables` entry with `Value == "<unset>"` for either
  var now returns `null` from `GetVar`, hitting the pre-existing
  `Fail("testbed Assessment credentials not set — ...")` branch instead of `ScanRunnerClient.LoginAsync`.
- `desktop/BuildConsole` is out of test-manifest scope per CLAUDE.md; no manifest changes needed.
- `git status --porcelain` clean; branch verified merged into `origin/main` (commit `f638a8e34`).
