# #2693 — BUILD SETS rollup: add All/Running/Verifying filter chips

- **Status:** ✅ DONE 2026-09-03T17:45:00Z
- **Scope:** buildconsole

## Log
- 2026-09-03T17:18:00Z — Starting: add activity filter chips (All/Running/Verifying) next to BUILD SETS header in BuildQueuePanel, composing with existing any-activity filter in RenderBuildSetRollup.
- 2026-09-03T17:45:00Z — ✅ DONE — Added `RollupFilterChipStyle`/`RollupFilterChipTextStyle` + three chips (All/Running/Verifying) in `BuildQueuePanel.xaml`'s BUILD SETS header DockPanel; added `_rollupActivityFilter` field (separate from `_buildSetFilter`), a composed `.Where(...)` clause in `RenderBuildSetRollup`'s `orderedKeys`, `RollupActivityFilterChip_Click`, and `UpdateRollupActivityFilterChipsVisual` (accent-tint selected-state, same convention as `BuildRollupRow`) in `BuildQueuePanel.xaml.cs`. `dotnet build BuildConsole.csproj` succeeded, 0 errors. Rebased onto and pushed to `origin/main` as `75cd4d73b`; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
