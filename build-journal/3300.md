# #3300 — Things room is missing the "Next [place] run · Take" checklist section

- **Status:** ✅ DONE 2026-09-09T02:45:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal project, milestone 19)
- **Started:** 2026-09-09
- **Commit(s):** ee328a962 (bookend), b2ebfcee7 → rebased to 0d0b578d9 on origin/main

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed real gap per #3300/#3289: `viewThings()`
  (public/app.js:1997-2096) has no "Next [house] run · Take" section, and the `things` table
  (migration 016) has no schema to back one (no quantity, no take-for-house flag, no grocery
  flag). Building schema (new migration) + backend (things.mjs + api.mjs + capture grammar +
  MCP tool) + UI (viewThings) per the two real design sources (First Slice Prototype + Shanes
  Life 09 - Things.dc.html).
- 2026-09-09T02:45:00Z ✅ DONE — shipped the real feature, not a rebuild-declined empty state.
  Migration `067_things_take_checklist.sql` adds `things.take_for_house` / `take_done` /
  `quantity` / `is_grocery`, run myself against local `DATABASE_URL` (`finances` db) and
  self-recorded in `schema_migrations`. Backend: `things.mjs` gained `queueForTake` (upsert by
  name, defaults a not-yet-tracked thing's home to "Home"), `listTakeChecklist` (grouped by
  destination house), `setTakeDone`, `clearTakeRun` (the real "run happened" state change --
  checked items' `house` moves to the destination, mirrors Heading Out's own "All set", #3158).
  `GET /api/things` now returns a `take` group; added `POST /api/things/take`, `PATCH
  /api/things/:id/take`, `POST /api/things/take/:house/clear`. Capture grammar rule 30
  ("take/bring X to <house>") + MCP tool `queue_take` give Claude the same real entry point.
  UI: `viewThings()` renders "Next {house} run · Take" between "Just logged" and "By house"
  (design order), reusing the existing `.shop-check`/`.chip.teal` patterns for the checkbox row
  and grocery badge -- no new CSS, no fixture data.
  Verified: all 6 touched files `node --check` clean; `capture-grammar.test.mjs` passes
  (40/40, including the new `queue_take` case) both before and after the final rebase; the
  server boots clean with the new code loaded (`node -e "import('./src/server.mjs')"`); the new
  `things.mjs` functions were exercised end-to-end against the real local Postgres `finances`
  db (create+queue with Home default, group-by-house, check, upsert-by-name re-queue,
  clear-run moving the real `house` column, then cleaned up the scratch row and deleted the
  disposable verification script -- nothing scratch left in the tree).
  `git status --porcelain` clean. `verify-branch-merged.mjs` confirms `agent/3300-q2028`
  (0d0b578d) is an ancestor of `origin/main`.
  Real, honest gap not closed here: no HTTP/WebView2 UI test was run (this app has no
  `shaneapp://runTest` harness or passkey-auth bypass for automated browser testing -- the
  MSP-platform test-manifest requirement is explicitly scoped to `artifacts/msp-portal` and
  `artifacts/shane-mccaw-consulting`, not this standalone app); verification stopped at
  DB-function + server-boot level, which is real but not full end-to-end UI coverage.
