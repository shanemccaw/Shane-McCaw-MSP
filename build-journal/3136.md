# #3136 — Feature: Dates (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:40:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-07T00:00:00Z — Starting. Real tables already exist (migration 014 in
  `web/shanes-life/migrations/`, applied to the real local `finances` Postgres DB — NOT
  `shanemccawmsp`). Building the real API + MCP tool layer + Dates room UI on top:
  lead-time table by kind, on-the-fly category, ask-next-time, visit notes/photos,
  day-before appointment reminders via `nudge_events`, and a real live OPM federal-holidays
  scraper (their old JSON feed is dead/404; the real HTML table at
  opm.gov/policy-data-oversight/pay-leave/federal-holidays/ is live and reachable) refreshed
  monthly.
- 2026-09-08T00:40:00Z — ✅ DONE. Built `src/core/dates.mjs` (createDate/listDates/getDate/
  updateDate/deleteDate, addAsk/attachAskToProvider/setAskAsked, addVisit/addVisitPhoto,
  nextOccurrence for arbitrary real recurrence, findDueDayBeforeReminders), `src/core/
  nudges.mjs` (real 1-3/day cap writer, first real caller of migration 018's nudge_events),
  and `src/core/federal-holidays.mjs` (real OPM HTML scrape + upsert, monthly refresh gate).
  Wired real `/api/dates*` + `/api/federal-holidays*` routes, MCP tools `push_date`,
  `list_dates`, `get_date`, `attach_ask`, `attach_visit`, `get_federal_holidays`, and a real
  Dates room + Date-detail screen in the vanilla-JS app (`public/app.js`/`app.css`,
  `#/dates` tab). Wired day-before appointment reminders + monthly OPM refresh into
  `server.mjs`'s housekeeping loop (also run once at boot).

  Real, live end-to-end verification against the local `finances` Postgres DB and the real
  OPM page (not mocked): `refreshFederalHolidays()` pulled 55 real holidays across
  2026-2030 from `opm.gov/policy-data-oversight/pay-leave/federal-holidays/` and wrote them;
  created a real recurring appointment ("every 6 weeks"), confirmed `nextOccurrence`
  projects it forward correctly across a DST transition (Nov 1 2026), confirmed
  `attachAskToProvider` + `addVisit`/photos + `findDueDayBeforeReminders` all round-trip
  through the real DB; `listDates` correctly merges real user dates and real federal
  holidays sorted by real due date. Found and fixed two real bugs during this verification
  (not shipped broken): the OPM HTML parser was dropping every holiday that carries a
  footnote marker (Washington's Birthday, Independence Day, every year) because the
  date-cell regex was anchored to end-of-string; and `nextOccurrence` read a pg `date`
  column via `toISOString()` (UTC) instead of local getters, which silently drifted by a
  day whenever server-local and UTC disagreed on the calendar date, including across a DST
  boundary. Test data cleaned from the real DB afterward; federal_holidays real rows left
  in place as real data, not test debris. Boot-smoke-tested the full server (new imports,
  route wiring, `/healthz`, 401 on the new authed routes) after every rebase.

  Real scope line, not built here: Pets (screen 10/11's vet-visit linkage, vaccine tracking)
  has real schema (migration 015) but no core module/routes/UI/MCP tools at all — filed as
  its own Feature, **#3141**, parented to #3086 (Epic), since #3136's own issue body only
  asked for Dates.

  Commits: `47fa0ac20` (feature, rebased twice onto concurrently-moving `main` — Recipes/
  Medications/Money landed mid-session; resolved two real merge conflicts in `app.js`/
  `api.mjs`, both additive), on `main` as `261bfb363`. Findings filed: #3141. Working tree
  clean, branch verified merged into `origin/main`.
