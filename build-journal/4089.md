# #4089 — Wire #3911's 11 real RBAC test accounts into Settings > User Accounts & Tiers

- **Status:** ⏳ IN FLIGHT 2026-09-14T17:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-14T17:52:00Z ⏳ IN FLIGHT — read #3911's real 11 `TEST_RBAC_*_EMAIL`/`_PASSWORD` pairs out of the live `%AppData%\BuildConsole\settings.json` `TestEnvironmentVariables`, confirmed `settings.UserAccounts` only held the stale two fake defaults (`standard_test_user`/`enterprise_test_user`) plus #3923's two throwaway `loginAs` verify profiles and one genuine Admin account. About to: (1) remove the fake-default seeding code in `BuildConsoleSettings.LoadOrCreateDefaults`, (2) write the real 11 `UserAccountEntry` rows into the live settings.json with correct `AccountTier`/`TargetApp`/`LoginUrl` per role, (3) drop the two #3923 throwaway profiles per this issue's follow-up comment.
