# #4089 — Wire #3911's 11 real RBAC test accounts into Settings > User Accounts & Tiers

- **Status:** ✅ DONE 2026-09-14T17:58:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 7b5b2eb5c (bookend), cbb4928d6 (real code fix)

## Log
- 2026-09-14T17:52:00Z ⏳ IN FLIGHT — read #3911's real 11 `TEST_RBAC_*_EMAIL`/`_PASSWORD` pairs out of the live `%AppData%\BuildConsole\settings.json` `TestEnvironmentVariables`, confirmed `settings.UserAccounts` only held the stale two fake defaults (`standard_test_user`/`enterprise_test_user`) plus #3923's two throwaway `loginAs` verify profiles and one genuine Admin account. About to: (1) remove the fake-default seeding code in `BuildConsoleSettings.LoadOrCreateDefaults`, (2) write the real 11 `UserAccountEntry` rows into the live settings.json with correct `AccountTier`/`TargetApp`/`LoginUrl` per role, (3) drop the two #3923 throwaway profiles per this issue's follow-up comment.
- 2026-09-14T17:58:00Z ✅ DONE — Code: `desktop/BuildConsole/Services/BuildConsoleSettings.cs` `LoadOrCreateDefaults` no longer seeds the fabricated `standard_test_user`/`enterprise_test_user` entries; a fresh/reset `UserAccounts` list now starts genuinely empty (commit cbb4928d6). Data: read the real 11 `TEST_RBAC_<ROLE>_EMAIL`/`_PASSWORD` pairs from the live `%AppData%\BuildConsole\settings.json`'s `TestEnvironmentVariables` (unchanged, left in place per the issue's own instruction) and wrote 11 real `UserAccountEntry` rows into `settings.UserAccounts` — real email usernames, real bcrypt-backed passwords, `AccountTier` set to the real role name (`Free`/`Customer`/`ServiceAccount`/`MSPOperator`/`MSPAdmin`/`PlatformAdmin`/`customer-admin`/`billing`/`cap.team.manage`/`cap.changes.approve`/`cap.purchases.approve`), `TargetApp`/`LoginUrl` split MSP-side roles (ServiceAccount/MSPOperator/MSPAdmin/PlatformAdmin) → "MSP Console" `http://localhost:5177/login`, customer-side roles (Free/Customer/customer-admin/billing/cap.*) → "Portal" `http://localhost:5175/portal/login` — both real URLs pulled from the same `DefaultLoginUrlForTargetApp` mapping `SettingsTabView.xaml.cs` itself uses (matches `scripts/dev-server/services.json` ports). `ActiveUserAccountId` set to the Free RBAC account. Removed the two stale fake-default entries and, per this issue's own follow-up comment, removed #3923's two throwaway `loginAs` verification profiles (`Git #3923 loginAs verify (Customer, Portal)` and the deliberately-broken-password one) from `settings.json`; left #3923's synthetic DB user `shanemccaw+loginas-verify-3923@outlook.com` (users.id=2826) untouched in the database — flagging per the comment's own "don't delete DB rows unasked" instruction, Shane's call whether to clean it up. Kept the one genuine non-test entry (`Admin` / `shane@shanemccaw.com`) untouched. Verified: settings.json integrity after the write (109 top-level props intact, all 53 `TestEnvironmentVariables` entries intact, `GitHubPat` intact, 12 `UserAccounts` — 11 real RBAC + genuine Admin); `dotnet build BuildConsole.csproj -c Debug` succeeded with 0 warnings/0 errors; `verify-branch-merged.mjs` confirms `agent/4089-q2762` (cbb4928d6) is an ancestor of `origin/main`; `git status --porcelain` clean. No genuine findings/bugs surfaced beyond what the issue already described — nothing new filed.
