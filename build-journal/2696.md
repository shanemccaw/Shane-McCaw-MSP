# #2696 — msp-message-center: non-numeric limit/offset crashes query with 500 instead of 400

- **Status:** ✅ DONE 2026-09-03T20:24:00Z
- **Scope:** platform

## Log
- 2026-09-03T20:16:13Z — Starting: guard `limit`/`offset` in `artifacts/api-server/src/routes/msp-message-center.ts` the same way `customerId` is already guarded, falling back to defaults (50/0) on NaN.
- 2026-09-03T20:24:00Z — ✅ DONE — fixed `limit`/`offset` NaN guard in `artifacts/api-server/src/routes/msp-message-center.ts` (commit 4b839cd4), falling back to 50/0 on an invalid value instead of letting `NaN` reach `.limit()/.offset()`. Merged into `main` at ef46ada2 (`verify-branch-merged.mjs` confirms ancestor). `tsc --noEmit` on the api-server package reports pre-existing worktree module-resolution errors (`@workspace/db` dist not built, `drizzle-orm` unresolved) unrelated to this change — a known worktree dependency-junction artifact, not caused by this fix.
