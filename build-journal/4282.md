# #4282 — MyArchitect: Shelf drawer in the bottom status bar (port BuildConsole's keep-alive Shelve/Restore mechanism)

- **Status:** ✅ DONE 2026-09-16T00:05:00Z
- **Scope:** platform (shanemccaw/MyArchitect, separate repo)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect@0f398ec13c564f427d873cd856c3d80347163d09

## Log
- 2026-09-15T23:30:00Z ⏳ IN FLIGHT — Confirmed blockers #4278 (superseded by #4305, complete) and #4284 (complete) are both closed/done, so #4282 is unblocked. Read `desktop/BuildConsole/MainWindow.ShelvedTabs.cs` for the real Shelve/Restore mechanism. Cloning `shanemccaw/MyArchitect` next to port the pattern into a bottom-status-bar drawer.
- 2026-09-16T00:05:00Z ✅ DONE — Confirmed MyArchitect had no existing off-screen keep-alive canvas (its regular tab switching only ever toggles Visibility on WebViews that stay children of `WebViewsContainer`), so built the minimal one this issue authorized (`ShelfHostCanvas`). Added a "Shelve Tab" tab context-menu entry, a `ShelfButton`/`ShelfPopup` drawer in the bottom status bar (`Placement="Top"` — expands upward, not an activity-bar popout), and the Shelve/Restore mechanics in a new `MainWindow.Shelf.cs` partial class, directly porting BuildConsole's `MainWindow.ShelvedTabs.cs` shape (park the live WebView2 1x1 off-screen on shelve, reparent the same instance back into `WebViewsContainer` on restore — no dispose, no reload). Also fixed `MainWindow_Closed` to dispose shelved tabs' WebViews too (they're no longer in `_tabs`). `dotnet build` clean (0 warnings/errors). Live-verified end-to-end by launching the real app: right-clicked a live, signed-in "M365 Admin" tab → "Shelve Tab" → tab vanished from the strip, tab count dropped, Shelf button appeared ("Shelf (1)") → clicked it → drawer opened upward from the status bar showing the real row (icon + title) → clicked the row → tab restored to the tab strip, re-activated, on the exact same URL/session (no reload). Committed and pushed to `shanemccaw/MyArchitect` main as `0f398ec`. No fixture/hardcoded data — this is pure UI-shell mechanism, no backend involved. No genuine findings/bugs hit during this build.
