# #4227 — Bind MSP mailbox connector callback to the MSP's own tenant

- **Status:** ✅ DONE 2026-09-15T12:56:45Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** b9197887b (work), 6eb1e2d28 (IN FLIGHT bookend)

## Log
- 2026-09-15T12:35:02Z ⏳ IN FLIGHT — investigating the mailbox connector callback in msp-settings.ts, the #4043 resolveCallbackTenant pattern, and what the read-app registration declares for Mail.Send.
- 2026-09-15T12:56:45Z ✅ DONE — shipped in `b9197887b`:
  - No MSP-level tenant record exists (`msps` has none; `msp_connector_configs`, `msp_sharepoint_connectors`, `msp_mailbox_connectors` are all empty locally). So the expected tenant is resolved from Microsoft: `resolveEntraTenantForDomain` (`lib/consent-verification.ts`) reads the tenant GUID from the mailbox domain's public OpenID discovery `issuer`.
  - `POST /connector/mailbox/connect` records that GUID as `msp_mailbox_consent_states.expected_tenant_id`, uses it as the admin-consent tenant hint (was `common`), returns 400 for a non-Microsoft 365 domain, 503 if Microsoft is unreachable, and 409 if the tenant is another MSP's customer.
  - `GET /connector/mailbox/callback` checks `bindMailboxCallbackTenant` (the resolveCallbackTenant equivalent): mismatch or unbound state → 400, state burned, nothing activated. It re-checks cross-MSP (409), requires `Mail.Send` via `requireAnyRole`, and writes the bound GUID, never the query string, to the connector.
  - Mail.Send confirmed absent, not assumed: `az ad app show` on DEV read `4743b130`, DEV write `9f6f4772` and PROD write `3308b280` declare no `b633e1c5-…` role. A live testbed read token has 32 roles and no Mail.Send.
  - Migration `lib/db/migrations/manual/2026-09-15-msp-mailbox-consent-expected-tenant-4227.sql` run locally; logged on #1630.
  - Verification: vitest `consent-verification.test.ts` + `msp-settings-portal-links.test.ts` 28/28 pass; api-server `tsc --noEmit` 0 errors. Live test on an isolated api-server (:8099, worktree build, local DB) with temporary `zz-test-4227-*` MSPAdmin users (deleted afterwards): (A) synthetic MSP 1626 + mccawsoft2 mailbox → 409 cross-MSP; (B) MSP 1626 + microsoft.com mailbox → state bound to 72f988bf, callback with the different, really consented tenant c4c814d4 → **400 refused**, state burned, no connector row; (C) MSP 1 + mccawsoft2 mailbox → callback with matching tenant → 400 "does not include permission to send mail"; same state with another tenant → 400 mismatch. `msp_mailbox_connectors` still 0 rows. Note: `0a361ab2` is NOT consented to the read app (token 401), so c4c814d4 was the only real consented tenant available.
  - Findings filed (siblings under #1650): #4241 (no registration declares Mail.Send, so the connector cannot send; app-model decision → Ask Shane), #4242 (security: an MSP can still bind to one of its OWN customers' tenants; needs a signed id_token or a platform-set MSP tenant).
  - `git status --porcelain` clean; no pre-existing dirty files.
