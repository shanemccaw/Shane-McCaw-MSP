# #2768 — Re-validate/rewrite Diagnostics-Scripts, Pillar Pages, Scan Progress SSE contracts

- **Status:** ✅ DONE 2026-09-14T14:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8b71a7472 (IN FLIGHT bookend), 861deae38 (real pack rewrite)

## Log
- 2026-09-14T13:35:00Z ⏳ IN FLIGHT — pulled #2768 body + comments; blockers #1850/#1944 both CLOSED, so unblocked. Confirmed no contract pack file exists anywhere in docs/ or Design/ for "Pillar Pages" (#1621) — #1621's own comments show it was deferred to v1.2 and moved to Backlog on 2026-09-13, still at the architecture stage, never produced a pack. Proceeding with real git-diff-against-extraction-commit freshness checks on the two packs that do exist: docs/portal/diagnostics-and-scripts-contract-pack.md (extracted at 79130e2ec, #2451) and docs/portal/scan-progress-sse-contract-pack.md (extracted at fae6ca4ee, annotated at aa7d11439, #2520).
- 2026-09-14T14:20:00Z ✅ DONE — Real, substantive staleness found and rewritten in both packs (git-diff-against-extraction-commit method on every cited source file, live-DB counts re-queried against local `DATABASE_URL`). Diagnostics and Scripts (#1660): headline finding #2506 (unreachable script-download path) was fixed 2026-09-04 — rewrote to the real wireable-but-unused state; `/portal/assessment/status` renamed `/portal/diagnostics/status` (#1753); RBAC `requireRole`/`CustomerUser` → `requireCapability`/`Customer` (#2460, #3590) throughout; two new MSP-operator Scripts routes (#2673) added to §0/§1f; two AI-generation admin routes decommissioned today (#3957) removed from §1g; live-DB counts refreshed (`monitor_checks` now 204 rows, `config_resources` now 1,538 — confirms the real trigger expansion landed); ~90-286 line file:line citations corrected throughout. Scan Progress SSE (#2520): the pack's own headline claim ("zero customer-facing callers in artifacts/portal") is now false — #1824 wired a real, live consumer (`useScanState.ts`) that closes the exact gap (#2521) this pack's own original extraction filed; rewrote §5 in full to document it, plus the same RBAC-rename citation fixes. Pillar Pages (#1621) has **no contract pack anywhere in the repo** (confirmed via full search of `docs/` and `Design/`) — it's still at the architecture stage and was deferred to v1.2 on 2026-09-13, the day before this dispatch; nothing existed to freshness-check, so nothing was fabricated. No new GitHub issues filed — every finding traced to an issue already tracked (either already closed/fixed, or already explained by #1621's tracked deferral). Verified: `verify-branch-merged.mjs` exit 0, `git status --porcelain` clean, branch pushed to `origin/main`. Commits: 8b71a7472 (bookend), 861deae38 (real work).
