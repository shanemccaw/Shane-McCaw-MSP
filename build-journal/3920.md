# Build Journal — #3920

- **Status:** ✅ DONE 2026-09-13T02:55:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T02:37:00Z — Starting: switch ChatDocumentContainer context gauge from token-based to message-count-based (cap 50, tiers 30/43/50), per #3920 spec.
- 2026-09-13T02:55:00Z — ✅ DONE — `desktop/BuildConsole/Controls/ChatDocumentContainer.xaml.cs`
  `UpdateGauge()`: bar fill/color now driven off `turnCount` (real message count, already resolved
  by `ResolveConversationMeter()`) instead of the token estimate. Added `MessageChurnCap = 50`.
  Tiers rescaled to 30/43/50 (60%/85%/100% of cap, same ratios as the old 60k/85k/100k token
  points). `BtnStartNewChat` visibility threshold moved from `conversationTokens >= 60_000` to
  `turnCount >= 30`. `CtxGauge.Text` now shows `"{turnCount}/50 msgs"`; the token breakdown
  (`BuildOverheadTooltip`) stays available as `CtxGauge.ToolTip` on hover, unchanged.
  `ContextBudget`/`CharsPerTokenFactor`/overhead estimator left untouched, feeding only the
  tooltip now. `dotnet build desktop/BuildConsole/BuildConsole.csproj` — clean, 0 warnings/errors.
  Commit `de6288417`, rebased and pushed to `origin/main` as `d7f52bb54`.
  `verify-branch-merged.mjs` confirms ancestor of `origin/main`. `git status --porcelain` clean.
  No findings filed this session.
