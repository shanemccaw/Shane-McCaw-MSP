# #1450 — Chats panel: hide builds/chats whose linked GitHub issue is closed; rename "DONE" badge to "VERIFY"

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** 1db33500c

## Log
- 2026-08-28 ⏳ IN FLIGHT — starting: rename DONE badge to VERIFY, extend closed-epic filter pattern down to individual issue/build level in LeftSidebar.xaml.cs.
- 2026-08-28 ✅ DONE — `LeftSidebar.xaml.cs`: (1) the `BuildChatCard` "DONE" build-status badge now displays "VERIFY" (same green styling, no behavior change to the underlying `build.Status` value). (2) Added `ComputeOpenGithubNumbersOrNull()` / `IsIssueConfirmedClosed()` / `AreAllLinkedIssuesClosed()` helpers reusing the exact same fail-open `_lastBoardIssues` OPEN-only signal the #839 closed-epic filter already trusts. `RenderChatsTree` now hides a whole chat card once EVERY GitHub issue number linked to it (its own `AssociatedIssueNumbers` plus any linked build's `GithubNumber`) is confirmed closed, logging via `ActivityLog.Log("git-board.chats", ...)` (same channel the closed-epic filter already uses). `BuildChatCard` separately drops an individual build badge once its own linked issue is confirmed closed, even if the chat itself stays visible. No toggle/recall mechanism added (per the issue — Shane finds old builds via Build Queue search). `bt_chats`/queue-item rows are untouched; this is a view-level filter only. Verified via `dotnet build BuildConsole.csproj` — 0 errors (only pre-existing warnings, none introduced by this change). No test-manifest needed: `desktop/BuildConsole` is explicitly out of scope for the test-manifest standing practice (internal tooling, not customer-facing product). Commit `1db33500c`, pushed directly to `main` (fast-forward), confirmed merged via `node scripts/dev-server/verify-branch-merged.mjs` (exit 0).
