# #1615 — Microsoft Changes: resolve/resolutions endpoints have no caller in AdminV2

- **Status:** ✅ DONE 2026-09-01T17:45:00Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** c82d53521 (rebased/merged to main as b7ae0669c)

## Log
- 2026-09-01T17:20:00Z ⏳ IN FLIGHT — wiring `POST .../resolve` and `GET .../resolutions` into `m365ChangesStore.ts` and the AdminV2 Peek/Ribbon UI for an interpretation, so Shane can trigger an on-demand resolve and see per-tenant resolution numbers.
- 2026-09-01T17:45:00Z ✅ DONE — `m365ChangesStore.ts`: added `ResolutionOutcome`/`ResolveRunState` types, `resolutionsByInterpretation`/`resolveRuns` state, `loadResolutions()` (GET `.../resolutions`) and `resolveInterpretation()` (POST `.../resolve`, synchronous — not a polled workflow run like routing) — mirrors the existing `loadRoutings`/`routeInterpretation` pattern (#1701). `index.tsx`: peek now lazy-loads resolutions alongside routings for a confirmed interpretation, adds a "Resolve now" action beside "Route now", and merges the resolution ledger with the routing ledger into one per-tenant list ("Per-tenant resolution & routing") keyed by customerId — PeekModel only supports a single `list` section, so resolution status (measured/not_measured/error, affected count, basis) and routing decision are shown together per tenant rather than as two separate lists. `tsc --noEmit` on admin-panel passes for both changed files (one pre-existing unrelated error in build-tracker/BuildTrackerExplorer.tsx, not touched here). No schema/migration needed — both endpoints already existed and were already correct per #1533's contract. Rebased onto origin/main and pushed directly (b7ae0669c); `verify-branch-merged.mjs` confirms OK; working tree clean.
