# #4480 — 8 active config-pack templates with no Graph write-permission rule

- **Status:** ✅ DONE 2026-09-17T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 4a8b2a260

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — Confirmed the 8 endpoints live against `config_pack_templates`/`baseline_action_templates` (app-governance-v1, device-hardening-v1, kfm-configuration-v1). Adding `GRAPH_WRITE_PERMISSION_RULES` entries for each, each citing a real Microsoft Learn permissions table, same pattern as #1901. Extending `graph-write-permissions.test.ts` for the new derived-set size and a real guard test that fails if any active pack template resolves to `rule: null`.
- 2026-09-17T00:00:00Z ✅ DONE — Added 8 rules to `artifacts/api-server/src/lib/graph-write-permissions.ts`: `DELETE /applications/*` (Application.ReadWrite.All, reused — action.delete-stale-app-registration), `DELETE /devices/*` (Device.ReadWrite.All, NEW — action.delete-stale-device-record; the two testbed-critical rules, verified extra carefully against real MS Learn pages), `POST /deviceManagement/deviceConfigurations` (DeviceManagementConfiguration.ReadWrite.All, reused, covers both action.create-bitlocker-protection-profile and action.create-windows-update-ring since both normalise to the same path), `POST /deviceManagement/deviceCompliancePolicies` (reused), `POST /deviceManagement/windowsAutopilotDeploymentProfiles` + `.../*/assignments` (DeviceManagementServiceConfig.ReadWrite.All, NEW), `POST /deviceManagement/configurationPolicies` (reused). DERIVED_WRITE_APP_PERMISSIONS grew 25 -> 27 (only Device.ReadWrite.All and DeviceManagementServiceConfig.ReadWrite.All are genuinely new; the previously-unwired action.assign-autopilot-profile rule becomes runnable as a documented side effect). Extended graph-write-permissions.test.ts: explicit coverage for all 8 endpoints, updated derived-set-size/delta assertions, and added a live regression guard (queries `config_pack_templates` join `baseline_action_templates` for every active pack via the real local DATABASE_URL) that fails if ANY active pack template resolves to `rule: null` — ran it live, 0 unmapped across the whole catalogue. Live-verified: `npx vitest run src/lib/graph-write-permissions.test.ts` → 100/100 passing (up from 87). `npx tsc --noEmit` on api-server passed. No test-manifest needed (change is api-server lib, not artifacts/portal or artifacts/shane-mccaw-consulting). Considered a follow-up finding for the sanctioned #2846-style "no rule for an active template" gate as a pre-commit/CI check beyond the test suite, but the test itself already satisfies the issue's own suggested fix — no new issue filed. Working tree clean; branch verified merged into origin/main via verify-branch-merged.mjs before writing this.
