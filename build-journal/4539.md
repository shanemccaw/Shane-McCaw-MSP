# #4539 — governance:guest-staleness has no guest filter

- **Status:** ⏳ IN FLIGHT 2026-09-17T23:36:05Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-17T23:36:05Z ⏳ IN FLIGHT — investigated `governance:guest-staleness` (monitor_checks row, endpoint `/users`, mapping `countIfLastSignInOlderThan(90)` on `signInActivity`, `filter_params` empty). Confirmed via local Postgres it has no `userType` scoping. Precedent found: `exchange:distribution-list-count` embeds its `$filter` in the endpoint URL, `identity:break-glass-health` and `appgov:enterprise-app-registration-list` use the `filter_params` column on `/users`/`servicePrincipals`. `governance:guest-count` instead filters client-side via `countEquals('Guest')`. Chose `filter_params = "userType eq 'Guest'"` on the `/users` endpoint (server-side Graph filter, same shape as `identity:break-glass-health`) so `signInActivity` is only evaluated over the guest population. `monitor-executor.ts` already adds `ConsistencyLevel: eventual` automatically whenever `$filter=` is present in the URL, so no code change is needed — just the migration.
