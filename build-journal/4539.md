# #4539 — governance:guest-staleness has no guest filter

- **Status:** ✅ DONE 2026-09-17T23:39:02Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 1efb6059c (bookend IN FLIGHT), 69623cd7e (plan), 5becbea84 (migration + fix)

## Log
- 2026-09-17T23:36:05Z ⏳ IN FLIGHT — investigated `governance:guest-staleness` (monitor_checks row, endpoint `/users`, mapping `countIfLastSignInOlderThan(90)` on `signInActivity`, `filter_params` empty). Confirmed via local Postgres it has no `userType` scoping. Precedent found: `exchange:distribution-list-count` embeds its `$filter` in the endpoint URL, `identity:break-glass-health` and `appgov:enterprise-app-registration-list` use the `filter_params` column on `/users`/`servicePrincipals`. `governance:guest-count` instead filters client-side via `countEquals('Guest')`. Chose `filter_params = "userType eq 'Guest'"` on the `/users` endpoint (server-side Graph filter, same shape as `identity:break-glass-health`) so `signInActivity` is only evaluated over the guest population. `monitor-executor.ts` already adds `ConsistencyLevel: eventual` automatically whenever `$filter=` is present in the URL, so no code change is needed — just the migration.
- 2026-09-17T23:39:02Z ✅ DONE — plan written at `build-journal/4539-plan.md` (69623cd7e). Migration `lib/db/migrations/manual/2026-09-17-guest-staleness-guest-filter-4539.sql` written and run against local `DATABASE_URL` (`UPDATE 1`, `schema_version` 2→3); re-ran the same file and confirmed a genuine no-op (`UPDATE 0`, `schema_version` unchanged) proving the guard is idempotent for replay on Replit/Staging via #1630. No application code change needed — verified by reading `appendQueryParams`/`graphFetchPaginated` in `artifacts/api-server/src/lib/monitor-executor.ts`, which already appends `filter_params` as `$filter=` and attaches `ConsistencyLevel: eventual` whenever `$filter=` is present, the exact mechanism `identity:break-glass-health` already exercises on the same `/users` endpoint. Live Graph re-evaluation on tenant 2080 not possible: confirmed via `simulator_check_runs` that this check currently returns `status: license_gap` ("Requires Microsoft Entra ID Premium (P1/P2)") on that tenant regardless of this fix — pre-existing, out of scope for #4539. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean. Issue comment posted; `in-flight`→`complete` label sync applied.

## Changes Made
- `lib/db/migrations/manual/2026-09-17-guest-staleness-guest-filter-4539.sql` (new) — sets `monitor_checks.filter_params = "userType eq 'Guest'"` for `key = 'governance:guest-staleness'`, idempotent guarded UPDATE + `simulator_migration_runs` self-mark. Run against local Postgres in-session.
- No application/UI code changes.

## Verification Results
- Migration run: `UPDATE 1` (schema_version 2→3). Re-run: `UPDATE 0` (no-op, schema_version unchanged) — idempotency confirmed.
- Row confirmed: `governance:guest-staleness.filter_params = "userType eq 'Guest'"`.
- Code path confirmed by inspection (no code change required): `appendQueryParams` + `graphFetchPaginated` in `monitor-executor.ts` already handle `filter_params` → `$filter=` + `ConsistencyLevel: eventual` correctly; same mechanism already live for `identity:break-glass-health`.
- Live tenant re-run not possible: `governance:guest-staleness` returns `license_gap` on testbed tenant 2080 (no Entra ID Premium P1/P2), confirmed via `simulator_check_runs`, unrelated to this fix.
- `verify-branch-merged.mjs`: exit 0, merged into `origin/main`.
- `git status --porcelain`: clean.
