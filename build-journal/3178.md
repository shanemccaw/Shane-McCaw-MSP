# #3178 — Feature: Shopping — Round 2 visual rebuild (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T03:19:48Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** e1343438 (Shopping Round 2 visual rebuild — merged to `main`)

## Log
- 2026-09-08T03:08:05Z ⏳ IN FLIGHT — read the issue body, `Shanes Life 04 -
  Shopping.dc.html` (a design-decision doc: options 1h/1i/1j plus layered
  additions 2a/2b, not a single final mock), the README (confirms "Shopping /
  Shared list — unchanged from the first slice"), and the real, fully-wired
  `Shanes Life - First Slice Prototype.dc.html` Shopping screen (the actual
  canonical spec per that README line). Resolved both real open questions the
  issue named directly from source rather than guessing:
  - **Bottom bar**: the prototype's own `submitCapture(door)` proves this is the
    SAME single capture box reused per room, with a room-aware placeholder
    ("Add, or say where you found it" when `showShop`) and room-aware dispatch
    (`inShop` branch adds plain text straight to the run; an "X aisle N note"
    phrase saves aisle memory directly) — never a second bar alongside the
    universal one.
  - **Per-item actions (Remove/Log price/History/aisle-edit)**: confirmed by
    exhaustive search that the canonical row markup has zero persistent
    affordance for any of the four — the whole row is `onClick=toggle`. Chosen
    interaction model: a small tap-to-expand chevron per row reveals a detail
    panel with the existing Remove/Log-price/History/aisle-save controls
    unchanged — this is a real engineering decision (not a product/money
    decision), stated here rather than guessed silently.
  Next: implement the header chrome, combined store+budget row, compact item
  rows, and the capture-bar wiring described above.
- 2026-09-08T03:19:48Z ✅ DONE — implemented in `web/shanes-life/public/app.js`
  + `app.css` (commit e1343438, rebased/pushed to `main`):
  - **Chrome**: native `.shop-header` (house-icon back-to-Today link, store name
    + live "N left"/"all done" subtitle, header-right scan icon, 3px gradient
    bar) replacing the generic `#app-header` for the shopping route only, same
    pattern #3174 used for Today (`render()`'s `hasOwnHeader` toggle).
  - **Store + budget**: one `storeBudgetRow()` — location-pin pill + budget
    pill side by side, each tap-to-edit inline (no more `prompt()`), replacing
    the old separate store-input-box + "Update store" link.
  - **Item rows**: compact `shoppingItemRow()` — 28px checkbox, name, a
    passive "Aisle N · note" subline (the same text the aisle-save endpoint
    already writes onto `item.note`), right-aligned real price/estimate, and
    a coupon/verdict pill only when one applies. Remove / Log price / History
    / aisle-save moved behind a per-row tap-to-expand chevron
    (`itemDetailPanel()`) — a stated engineering decision, not a design
    literal, since the canonical prototype's row has no persistent affordance
    for any of the four (see the IN-FLIGHT log entry for the investigation).
    Grouped rows now render in one continuous `.shop-list` card with hairline
    dividers, matching the design, instead of the generic app-wide
    `.checklist` pill-per-row look.
  - **Capture bar**: resolved as one universal bar, not two — `render()` now
    swaps the real `#capture-text` placeholder to "Add, or say where you
    found it" on the shopping route, and its submit handler dispatches
    through a new `submitShoppingCapture()` extracted from the design's own
    `submitCapture(door)`: comma/and/then-separated chunks each add a real
    item, or — when a store is set and a chunk matches "<item> aisle <n>
    <note>" against a fuzzy-matched open item (`findShoppingItem`, the same
    exact/startsWith/includes/word-match chain as the design's own
    `findItem`) — save real aisle memory instead. Removed the now-redundant
    mid-page "Add to Shopping" box and the standalone "Scan" button.
  Filed **#3202** (parented under #3178, board status "AI Batter Up") for the
  one real out-of-scope gap found along the way: no "Everything's in the
  cart" / "Done shopping" completion card (design 04 / First Slice Prototype
  option 1j) — the live app only has a generic "Clear checked items" button.
  Verification: `node --check app.js` and a CSS brace-balance check both pass;
  full diff hand-reviewed against the design source line by line. Live
  browser verification was not available — this app is a standalone Node
  service outside the pnpm workspace with only `.env.example` present (no
  real `.env`/DB connection in this worktree), so `shaneapp://runTest` and
  the local-Postgres path this repo's other apps use don't apply here; same
  honest "static review" verification level #3174's own bookend recorded for
  this exact app. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE;
  `git status --porcelain` clean.
