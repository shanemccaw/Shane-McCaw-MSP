# #3312 — Cook mode: real pantry depletion when ingredients are checked off

- **Status:** ✅ DONE 2026-09-09T03:15:00Z
- **Scope:** personal (web/shanes-life -- Shane's Life app, milestone 19, not MSP platform)
- **Started:** 2026-09-08
- **Commit(s):** f2d51ee0e (rebased onto main as 05a300a6a)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Investigated #3312/#3308's own finding: Cook mode
  (`public/app.js` `viewCook`) has zero server round-trip on ingredient checkoff today.
  Per Shane's real decision recorded on #3308 ("real, sensible default ... yes, but
  confirm the exact real UX ... silent decrement vs a confirmable step") and the
  standing-rules line for this build ("silent decrement, no confirmation"): implementing
  a silent, unambiguous-fuzzy-match decrement-by-1 (floored at 0) call fired the moment a
  Cook-mode ingredient checkbox transitions unchecked->checked. No fabricated pantry rows
  for a non-matching ingredient text.
- 2026-09-09T03:15:00Z ✅ DONE — Shipped `pantry.depleteForCookCheckoff(userId, ingredientText)`
  (src/core/pantry.mjs): both-ways substring fuzzy match against real `pantry_items` rows with
  quantity > 0 (same match rule `hasPantryQuantity` already uses, NOT `tieredFind`'s narrower
  exact/starts-with/contains ladder or `adjustPantryQuantity`'s upsert-creates-a-row behavior);
  exactly one unambiguous match decrements by 1, floored at 0; zero or multiple matches is a
  silent no-op, never a fabricated row. Wired `POST /api/pantry/deplete-checkoff` (src/routes/
  api.mjs) and the Cook-mode ingredient checkbox's `change` handler (public/app.js `viewCook`) to
  fire it only on the real unchecked->checked transition (never on uncheck -- a misclick doesn't
  hand quantity back; Shane can correct with a plain "I have X" capture after), silently
  (`.catch(() => {})`), matching the "silent decrement, no confirmation" decision this build's own
  standing rules recorded. No schema change needed -- `pantry_items` (migration 068) and its
  quantity/CHECK-constraint floor already existed from #3308.
  Live-verified against the real local Postgres `finances` database (same instance/credentials as
  the platform's own local Postgres, per `.env.example`; no `.env.local` exists in this worktree
  for `web/shanes-life` so the connection string was passed via env var, not written to disk): a
  disposable test pantry row was inserted under a real existing user id, decremented via the real
  function end-to-end, confirmed the non-matching-ingredient case creates zero rows, confirmed the
  floor-at-0 behavior, and confirmed a further checkoff against an already-zeroed row is a
  harmless no-op (consistent with `hasPantryQuantity`'s own `quantity > 0` semantic) rather than an
  error. All assertions passed; the scratch verification script and its test rows were deleted
  after the run -- nothing left behind. `node --check` passed on all three touched files.
  Commit `f2d51ee0e` pushed to `origin/main` as `05a300a6a`;
  `verify-branch-merged.mjs` confirms the branch is a real ancestor of `main`.
  `git status --porcelain` is clean. No new GitHub findings to file for this build --
  the implementation matched #3308's own recorded default exactly, no gaps hit.
