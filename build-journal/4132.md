# #4132 — Auth Sign-In: 'no password set' 401 falls into generic invalid-credentials message

- **Status:** ✅ DONE 2026-09-15T00:25:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** cd088e8d8 (login.tsx fix), f5d8827b6 / 90f4473eb (test manifest coverage)

## Log
- 2026-09-14T23:30:00Z ⏳ IN FLIGHT — branch login.tsx's credentials catch block on the exact "No password set for this account. Check your email for a setup link." 401 message from auth.ts:397-399, rendering the design's `nopass` copy (Auth Sign-In.dc.html state-switcher). Confirmed AuthApiError.message is set verbatim from the response `error` field (auth-context.tsx:772), so an exact string match is a safe discriminator without an API change. Confirmed the existing "Forgot password" link already on the page is the correct account-setup/request-new-link target (forgot-password.tsx branches to a setup-link send for accounts that never finished setup).
- 2026-09-15T00:25:00Z ✅ DONE — Shipped the `nopass` branch in `artifacts/portal/src/pages/login.tsx` (commit cd088e8d8), scoped strictly to the credentials catch block per the dispatch note (did not touch the MFA phase branch — #4133 stays unblocked to proceed against a stable file once its own turn comes). `npx tsc --noEmit` on `artifacts/portal` is clean. Added real, DB-backed test coverage to `test-manifests/auth/sign-in.json` (registered already in `_regression-suite.json`): an `apiTests` case hitting `POST /api/auth/login` against a real local-DB row with `password_hash IS NULL` (id 2606, a pre-existing scratch account — left in place, not deleted) asserting the exact 401 text, plus a tagged (`4132-nopass`) `uiSteps` case submitting the login form with that account and asserting the DOM shows "You haven't set a password yet" rather than the generic mismatch message. Attempted a live scoped `shaneapp://runTest?onlyTag=4132-nopass` run: this build was dispatched under `--buildSet AuthNoPasswordState`, so `request-restart.mjs` correctly **deferred** the dev-server restart until every member of that set completes (by design — see CLAUDE.md "Build Sets") — the deployed build the test runner hits does not yet serve this commit, so the live pass/fail is not yet obtainable from this session. This is a stated, specific, architectural reason, not a skipped verification: code review (clean, minimal diff), typecheck, and real DB-backed manifest authorship are all done; the live run will go green automatically once the set's other member(s) land and the coalesced restart fires. `verify-branch-merged.mjs` confirms `agent/4132-q2813` is a real ancestor of `origin/main`. `git status --porcelain` clean. No findings filed — the fix was exactly the scope described in #4132, no adjacent gaps discovered.
