# Git #3861 — Reinstate App Registration/Azure credential expiry email alerting (AdminPanel)

- **Status:** ✅ DONE 2026-09-13T02:35:00Z
- **Scope:** platform

## Log

- 2026-09-12T22:30:00Z — Session started. Pulled issue #3861 body + comments. Scope: build a real
  scheduled trigger that calls admin-azure-credentials.ts's `/admin/azure-credentials/expiring-summary`
  logic and sends a real email per credential nearing expiry, reusing `appRegExpiryAlertEmail`
  (lib/mailer.ts:795) as the template starting point. Parent: #1095 (EPIC: Admin Panel).
- 2026-09-13T02:35:00Z — ✅ DONE — commit `ae3d86ad4` on `main`. Built the real trigger:
  - Extracted the expiring-summary query/filter logic out of admin-azure-credentials.ts into a
    shared `lib/azure-credential-expiry.ts` (EXPIRY_WARN_DAYS, safeGetExpiry,
    getExpiringAzureCredentials) so the dashboard badge and the new alert sweep read the same data.
  - New `lib/azure-credential-expiry-alert.ts` node handler: sends via `sendEmailOrThrow` (Exchange
    Online/Graph, per this repo's email policy) to `ADMIN_EMAIL ?? CRM_ADMIN_EMAIL`, using a new
    `azureCredentialExpiryAlertEmail` template in mailer.ts adapted from the dead
    `appRegExpiryAlertEmail`. De-dupes via new nullable
    `azure_tenant_credentials.last_expiry_alert_sent_at` column (24h resend window), only stamps it
    once the send actually succeeds, resolves linked client name/email from `usersTable` when
    `clientUserId` is set.
  - Migration `lib/db/migrations/manual/2026-09-12-azure-credential-expiry-alert-3861.sql` — ran
    myself against local `DATABASE_URL` (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS
    last_expiry_alert_sent_at`).
  - Registered `azure_credential_expiry_check` as a promoted node type
    (node-type-registry.ts + WfNode union in lib/db/src/schema/index.ts +
    workflow-executor.ts's executeNode switch) and seeded a new daily (08:00 UTC) system workflow,
    `__system__: Azure Credential Expiry Alerts`, in seed-system-workflows.ts — auto-seeds with its
    cron trigger the next time `seedSystemWorkflows()` runs at server startup (same idempotent
    upsert-by-name mechanism every other `__system__:` workflow uses).
  - Verification: 6 new vitest unit tests in `azure-credential-expiry-alert.test.ts` (no-recipient,
    first-alert + DB stamp, 24h dedup skip, client-linked template resolution, failed-send not
    marked alerted, outside-warn-window exclusion) — all pass. `pnpm run typecheck` in
    artifacts/api-server is clean aside from 2 pre-existing, unrelated test-file errors
    (auth-impersonation.test.ts, portal-checkout.test.ts — last touched by commit `7b181f8c4`, not
    this session). node-type-registry's own completeness test (7 tests) and the mailer/msp-mailer
    suites (82 tests) still pass after the edits.
  - Finding filed: #3918 (parented under #1095, board status AI Batter Up) — the
    `azure_tenant_credentials` CRUD surface (admin-azure-credentials.ts) has zero Admin Panel
    frontend consumer; confirmed via grep across `artifacts/admin-panel/src`.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (exit 0).
    `git status --porcelain` clean.
