# #3362 — portal diagnostics run-detail findings query relies on prior ownership check, not its own customer_id predicate

- **Status:** ✅ DONE 2026-09-10T23:53:21Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** e99e9b6ff (rebased onto main as 1472b97e4)

## Log
- 2026-09-10T23:47:18Z ⏳ IN FLIGHT — Add `eq(mspDiagnosticFindingsTable.customerId, customerId)` to the findings query's own `where` clause in `GET /portal/diagnostics/runs/:runId` (`artifacts/api-server/src/routes/msp-diagnostics.ts` ~line 1134), mirroring #3102's fix in `portal-customer-engines.ts`.
- 2026-09-10T23:53:21Z ✅ DONE — Added the `customerId` predicate to the findings query's own `where` clause. `tsc --noEmit` clean. Live-data verify against local Postgres: 0 rows where `msp_diagnostic_findings.customer_id` differs from its run's `msp_diagnostic_runs.customer_id` (no live leak, as the issue body predicted). Pushed to `origin/main` as `1472b97e4`; `verify-branch-merged.mjs` confirms ancestor of main. `git status --porcelain` clean.
