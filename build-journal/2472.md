# #2472 — Wire the four keep-alive tab categories through the new lifecycle

- **Status:** ✅ DONE 2026-09-02T23:23:50Z
- **Scope:** buildconsole (desktop/ShaneBuilder — not desktop/BuildConsole)
- **Started:** 2026-09-02
- **Commit(s):** f3e7d9973 (worktree branch), merged to main as 7b65d86d8

## Log
- 2026-09-02T23:17:24Z ⏳ IN FLIGHT — pulled #2472/#2470/#2469 in full. Audited real
  tab-opening paths: only Claude.ai chat tabs (OpenNewChatFlow, #2318/#2323) have a
  real in-app entry point today, and #2470 already classified those keep-alive by
  default (`IsChat` → `KeepAliveClass.KeepAlive`). Gemini/AI Studio, product
  websites, and Azure/M365 Admin have NO real tab-opening code path in ShaneBuilder
  yet (grep confirms — no menu/button/dialog opens any of the three; #2158 Web
  Shelf, the mechanism that will eventually open arbitrary URLs, is itself still
  unscoped-in-detail per its own body). Git/filesystem document tabs are already
  reloadable by default (no `IsChat`, no override). Proceeding to: generalize the
  keep-alive plumbing beyond its current claude.ai hardcode so it is category-aware
  and ready for those three once a real entry point lands, make the reloadable
  classification explicit at the Git-doc/Markdown-viewer construction sites, and
  file the missing-entry-point gap as a finding under #2469 rather than inventing
  unscoped UI for it.
- 2026-09-02T23:23:50Z ✅ DONE — Implemented (commit 7b65d86d8 on main): added a
  `BrowserTabCategory` enum + `BrowserCategory` field on `TabDef`; a generic
  `OpenKeepAliveBrowserTab(idPrefix, title, url, category)` factory in
  `MainWindow.KeepAliveTabs.cs` giving any future entry point correct keep-alive
  classification + per-tab WebView2 instancing for free; a category-aware
  `KeepAliveInitialUrl` fallback (no longer silently defaulting a non-Claude
  category to claude.ai — fails loud to about:blank + a warn log instead); a new
  bare `GenericBrowserDock`/`GenericBrowserWebViewHost` mount point (XAML +
  `SelectTab`/`MountKeepAliveView` wiring) distinct from the chat-chrome-carrying
  `ClaudeChatDock`, so a non-chat keep-alive tab actually renders instead of
  landing on the "nothing here yet" stub; and explicit
  `keepAliveClass: TabKeepAliveClass.Reloadable` on the two real reloadable-tab
  construction sites (`OpenGitPeekInTab`, `OpenMarkdownFileTab`).
  Verification: `dotnet build ShaneBuilder.csproj -c Debug` and `-c Release` both
  clean, 0 warnings/0 errors. Claude.ai chat tabs already survive a dismiss (park)
  +restore cycle with session/login intact per #2470's own verification — the
  mechanism is unchanged for that path, only extended.
  Real gap found and filed: Gemini/AI Studio, product website, and Azure/M365
  Admin have NO real tab-opening entry point anywhere in ShaneBuilder today (only
  Claude.ai chats do) — filed as **#2485**, parented under #2469 (Feature-tier),
  board status set to "AI Batter Up". Building the actual entry points would mean
  inventing unscoped UI (#2158 Web Shelf itself flags this as an open question,
  not a decision) — out of scope for #2472's own contract, so the generalized
  mechanism above is what's ready; #2485 documents what's still missing and why.
  Working tree clean; branch verified merged into origin/main
  (verify-branch-merged.mjs: OK).
