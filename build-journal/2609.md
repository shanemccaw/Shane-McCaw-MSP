# #2609 — Billing: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-15T13:55:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** e70beba6b (real work, landed as 35078eef7 after two rebases)

## Log
- 2026-09-15T13:10:00Z ⏳ IN FLIGHT — Shane authorized building this screen directly, no Claude Design export exists (#2608 never landed). Read the real backend: #4109 (invoice CRUD + versioned re-issue, `msp-invoices.ts`), #4110 (subscription cancel/discount/free-month, `msp-subscription-billing.ts`), #4111 (seat pricing, already has UI — `SeatPricing.tsx`), #4112 (retainer interval switch proposal, already has UI — `RetainerIntervalSwitch.tsx`). Both #4111/#4112 already stack in the "billing" nav slot in `ConsoleShell.tsx`. `docs/msp-console/billing-msp-console-contract-pack.md` (2026-09-14, pre-dates #4109-#4112) confirms this real backend closes exactly the gaps it found.
  Real finding while reading the invoice route: `msp-invoices.ts`'s `invoicesTable.clientUserId` keys off `usersTable` — a legacy client-portal-user axis structurally unrelated to `tenantsTable` (no FK between them, confirmed against schema and already documented in the contract pack / the route file's own header). Every other module mounted in this nav slot (Seat Pricing, Retainer Switch, the new Subscription actions) is tenant-scoped (`customerId` = a `tenants.id`). Invoices cannot be silently mapped onto the selected tenant without inventing a correlation that does not exist in the schema — so the Invoices tab gets its own independent client-user picker (`GET /msp/:mspId/clients`, new) rather than inheriting the page's selected tenant. Documented plainly in the module's own UI copy, not hidden.
  UX decision (build requirement #4): four distinct capabilities → a tabbed `BillingScreen.tsx` wrapper (Subscription | Seat Pricing | Retainer Switch | Invoices) replaces the prior flat vertical stack of just the two modules — a flat stack of four was going to be an unreadably long scroll, and Invoices genuinely needs its own scoped picker UI that doesn't belong bleeding into the tenant-scoped tabs. One agent-built banner at the top of the whole screen (not per-tab), since the entire Billing screen lacks a design export.
  Backend gap found and being filled per the HARD RULE (build it, don't stop at empty state): #4110 shipped write-only routes (cancel/discount/free-month), no GET to read current subscription state or credit history before acting — adding `GET /msp/:mspId/customers/:customerId/subscription` (read-only, reuses existing `findActiveSubscription`/`subscriptionToWire`/`serializeCredit` helpers already in-file, no new table/migration). Also adding `GET /msp/:mspId/clients` to `msp-invoices.ts` for the invoice client picker (same reasoning).
  Next: write those two GET routes, then the two new frontend api files + two new modules + the tabbed wrapper, then typecheck and wire into `ConsoleShell.tsx`.
- 2026-09-15T13:55:00Z ✅ DONE — Shipped the full Billing screen (`BillingScreen.tsx`), replacing the prior flat two-module stack with a 4-tab UX (Subscription | Seat Pricing | Retainer Switch | Invoices) — the real UX judgment call requirement #4 asked for, reasoned above: four distinct capabilities in one flat scroll was already going to be unreadable with just two, and Invoices genuinely needs its own client picker rather than bleeding into the tenant-scoped tabs.
  - `SubscriptionBilling.tsx` + `subscription-billing-api.ts` — wires #4110's three write routes (cancel/discount/free-month) plus a new read-only `GET /msp/:mspId/customers/:customerId/subscription` I added to `msp-subscription-billing.ts` (reuses the file's own existing `findActiveSubscription`/`subscriptionToWire`/`serializeCredit` helpers — no new table, no migration). Shows the real current subscription state and credit history before offering any action; honest empty state when no subscription/credits exist (real DB state today: `tenant_subscriptions` and `customer_billing_credits` are both empty tables system-wide, confirmed via direct `psql` against local `DATABASE_URL`).
  - `Invoices.tsx` + `invoices-api.ts` — wires #4109's full invoice CRUD + revise surface. Real finding, documented in the module headers: `invoicesTable.clientUserId` → `usersTable` is a legacy client-portal-user axis with **no FK to `tenantsTable`** (confirmed against schema — already noted in `docs/msp-console/billing-msp-console-contract-pack.md` and `msp-invoices.ts`'s own header, not new information). Every other tab in this screen is tenant-scoped; Invoices cannot be silently mapped onto the selected tenant without inventing a correlation the schema doesn't have, so it carries its own independent client-user picker fed by a new `GET /msp/:mspId/clients` route I added to `msp-invoices.ts`. Documented plainly in the UI copy ("a separate list from the tenant tree on the left"), not hidden. Draft invoices: create/edit/delete/send. Sent invoices: revise (reason required, versioned) or mark paid. Superseded invoices show which version replaced them.
  - `Icon` registry (`console/icons.tsx`): added `percent`/`gift` (Lucide `Percent`/`Gift`) for the discount/free-month actions — merge-conflicted once against a concurrent build's `Megaphone` addition (#2600, Microsoft Changes), resolved by keeping both additive stacks.
  - Agent-built banner: dashed-border strip at the top of the whole `BillingScreen`, reading "Agent-built UI — pending design review" with a one-line explanation citing #2608 — visible on every tab, not per-module, since the whole screen lacks a design export.
  - No dev-facing chrome: no raw route paths or HTTP status codes rendered anywhere in the UI; API errors are always the server's real `error` message string.
  - Verification: `tsc --noEmit` clean across `artifacts/api-server` and `artifacts/msp-console` (re-run after two rebases picked up concurrent work); `pnpm run build` (vite) succeeds for `msp-console`. No existing unit tests cover either edited route file, so nothing to regress-check there. Both new GET routes' real queries verified directly against the local Postgres `DATABASE_URL` (not simulated): `GET /msp/:mspId/clients`'s query returns real `msp_id=1` client-portal rows (18 real rows, e.g. id=56 "Test Customer"); the subscription route's two queries execute cleanly and honestly return zero rows, matching real current DB state (`tenant_subscriptions` and `customer_billing_credits` are empty system-wide today, `invoices` is empty too post-#4109's own cleanup) — confirmed via `\d invoices`/`\d tenant_subscriptions` that every column my routes select genuinely exists with the expected type. No live HTTP round-trip attempted — no dev-server instance was reachable from this worktree without disrupting the shared `MSPConsole` buildSet other concurrent builds (#4109-#4112) are part of, same reasoning those bookends already recorded.
  - No test manifest: `artifacts/msp-console` is explicitly out of the test-manifest scope (CLAUDE.md — manifests apply only to `artifacts/msp-portal` and `artifacts/shane-mccaw-consulting`).
  - No genuine new finding beyond what the contract pack/route headers already documented (the `usersTable`/`tenantsTable` axis split) — not re-filed as a new issue since it was already known, just newly UI-relevant.
  - Two rebases required during this session as concurrent work landed on `origin/main` (`icons.tsx` conflicted once against #2600's `Megaphone` addition — resolved additively, both kept); `ConsoleShell.tsx` auto-merged cleanly against #2600/other concurrent module additions both times.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (OK, 35078eef7) and `git status --porcelain` clean before writing this DONE.
