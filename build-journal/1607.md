# #1607 — Webhooks: one event catalog (reconcile three competing vocabularies)

- **Status:** ✅ DONE 2026-09-10T17:24:21Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** `66220b1b` (validation + test), `701d9fba` (this bookend, IN FLIGHT)

## Log
- 2026-09-10T17:14:57Z ⏳ IN FLIGHT — Reconcile the webhooks event catalog. Investigation
  complete: confirmed the ONE real dispatchable catalog is `SUBSCRIBABLE_EVENT_TYPES`
  (`artifacts/api-server/src/routes/webhooks.ts:28-44`), served by `GET
  /api/portal/webhooks/event-types`. The Design fixture's 11-row `WEBHOOK_EVENTS`
  and the stale hardcoded `ALL_EVENT_TYPES` copy are **already gone** — portal-v2/
  msp-portal was retired in the `f40438cdc` rename and not carried into `artifacts/portal`
  (grep for `WEBHOOK_EVENTS`/`ALL_EVENT_TYPES`/`webhooksData` returns nothing). #1278's 23
  alert conditions are a separate mechanism (in-portal alert feed), not webhook events —
  merging them would author a parallel set (forbidden by #1485 / this session's HARD
  CONSTRAINT). Plan: (1) enforce `SUBSCRIBABLE_EVENT_TYPES` as the authoritative gate by
  validating `eventTypes` on POST + PATCH; (2) FLAG `signal.fired` for Shane rather than
  removing it unilaterally — see the signal.fired note in the DONE comment.
- 2026-09-10T17:24:21Z ✅ DONE — Shipped the reconciliation's enforcement half. `SUBSCRIBABLE_EVENT_TYPES`
  (`webhooks.ts:28-44`) is now the single authoritative gate: `createWebhookSchema` (POST) and
  `updateWebhookSchema` (PATCH) reject any `eventTypes` value outside the canonical
  currently-dispatchable catalog with a 400, instead of silently persisting a dead subscription
  (`webhooks.ts` `eventTypesSchema` superRefine). Added `webhooks.eventtypes.test.ts` (6 vitest
  cases, all green) asserting the real exported schemas accept canonical types, reject all 11
  retired-fixture invented strings (`finding.created`, `drift.detected`, …), default `[]`, and
  documenting that `signal.fired` is still present.
  Verification: `pnpm run typecheck` rc=0; `vitest run webhooks.eventtypes.test.ts` 6/6 pass;
  DB (`outbound_webhooks`) confirmed 0 rows total and 0 subscribing to `signal.fired`.
  RECONCILIATION JUDGMENT: three vocabularies → one. (A) `SUBSCRIBABLE_EVENT_TYPES` is the real,
  dispatchable catalog and is now canonical + enforced. (B) the 11-row fixture `WEBHOOK_EVENTS`
  is already gone (portal-v2/msp-portal retired in `f40438cdc`; grep for
  `WEBHOOK_EVENTS`/`ALL_EVENT_TYPES`/`webhooksData` returns nothing — nothing to delete, the
  fixture did not survive). (C) #1278's 23 alert conditions are a separate mechanism (in-portal
  alert feed / Alert Preferences), NOT webhook events — folding them in would author a parallel
  set, forbidden by #1485 and this session's HARD CONSTRAINT; left untouched.
  FLAGGED, NOT DECIDED (HARD CONSTRAINT): `signal.fired` removal from the subscribable list is a
  drop of a term live in code — it is imported and documented by name in the live
  `msp-console-webhooks.ts` (`:39`, `:58`). That downstream reference did not exist when the
  2026-09-01 build comment said "remove it". Handed to Shane in the #1607 comment rather than
  removed unilaterally; DB shows zero live subscriptions so removal is safe whenever he confirms.
  "Consumed live" by a portal page is out of scope / not possible this session: no portal
  webhooks page exists in `artifacts/portal` and no `Design/portal/Webhooks.dc.html` export is
  present — page wiring is a separate sub-issue under #1597. Working tree clean; branch merged to
  `origin/main` (verify-branch-merged rc=0). Issue NOT closed (Shane's call).
