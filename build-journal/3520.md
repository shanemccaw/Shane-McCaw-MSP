# #3520 — Build Queue panel summary total omits Verifying rows

- **Status:** ✅ DONE 2026-09-10T17:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T16:22:35Z — Starting. Finding where the Build Queue panel computes its summary total (queued/blocked/up-next/verifying) and fixing it to include Verifying, per #3519's live ground truth (12 queued + 1 running + 31 verifying = 44 real active rows vs panel showing ~12/13).
- 2026-09-10T17:05:00Z — ✅ DONE. Confirmed live: `ComputeQueueStatusCounts`/`UpdateQueueStatusCounts` in `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs` already breaks Verifying out as its own labeled count in the QUEUE header string, but there was no genuine "total" figure at all — `InQueue` (queued + limit-paused) was the only summable number in the string, and that's what Shane was reading as "the total," which structurally excludes Verifying. Added a real `Total => InQueue + Verifying` to the `QueueStatusCounts` struct (every row genuinely active/in-progress, not terminal) and surfaced it as the first segment of the header text (`Total: N · In queue: N · Blocked: N · Up next: N · Verifying: N`), updated the XAML placeholder text and doc comment to match. `dotnet build -c Release` on `desktop/BuildConsole/BuildConsole.csproj`: 0 errors, 0 warnings. Commit `59e0afa9f` on `agent/3520-q2192`, pushed to origin, merged into `origin/main` via a throwaway `main-merge-3520` branch (deleted after push) — `verify-branch-merged.mjs` confirms OK, ancestor of `origin/main`. `git status --porcelain` clean.
