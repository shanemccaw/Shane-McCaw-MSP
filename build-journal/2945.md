# Build Journal — Issue #2945

- **Status:** ✅ DONE 2026-09-06T13:58:37Z
- **Scope:** platform

## Log
- 2026-09-06T13:55:29Z — Started. Fixing `review.policy_review_due` in
  `artifacts/api-server/src/lib/customer-tenant-alert-engine.ts` — confirmed
  wrong-table bug (reads `msp_risk_decisions`, should read `policy_decisions`
  per Git #2024) plus an invalid `decision_state` value (`'expired'`, removed
  by Git #1527).
- 2026-09-06T13:58:37Z — ✅ DONE — Rewrote the `review.policy_review_due`
  case to `SELECT COUNT(*)::text AS n FROM policy_decisions WHERE tenant_id
  = $1 AND (decision_state = 'due' OR review_state = 'due')`, matching the
  sibling `review.risk_acceptance_due`/`risk.expiring` cases and
  `advancePolicyReviewClock`'s own vocabulary. Ran the fixed query directly
  against local Postgres (`policy_decisions`, 0 rows for tenant '1' — schema
  confirmed, no runtime error). `tsc --noEmit` on `artifacts/api-server`
  produced no errors for the changed file. No schema/migration change
  needed — `policy_decisions` already existed (Git #2024). No fixture data
  used. Commit `fb480eda5` on `agent/2945-q1724`, rebased onto
  `origin/main` and pushed as `2bd0e07c9` on `main`.
  `verify-branch-merged.mjs` confirms merged. `git status --porcelain`
  clean.
