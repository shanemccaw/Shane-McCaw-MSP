# #3538 — require a caller-supplied chat/context identifier on every MCP write call

- **Status:** ✅ DONE 2026-09-10T18:12:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** e31310dc6 (implementation), ed38132c1 (post-rebase push to main), 30c669ce4 (bookend IN FLIGHT)

## Log
- 2026-09-10T18:05:00Z ⏳ IN FLIGHT — confirmed all 6 sibling MCP tool issues (#3391-3396) closed
  and merged to origin/main (`808d71a92`, `75b7fe378`, `26d9790da`, `ee2db6157`, `2dfaab7d4`,
  scaffold `168ba9a29`). Read #3538 body + #3377 (Feature parent). Starting implementation:
  required `context` string param on every write tool (create_issue, update_issue,
  add_sub_issue, remove_sub_issue, set_blocked_by, post_comment, close_issue, move_to_status),
  pre-flight-rejected before any GitHub call, recorded via the existing activity-trail params
  capture (no schema change needed there — `params: args` already includes it). Visible
  `[chat: <context>]` tag decision pending — will document reasoning in README.
- 2026-09-10T18:12:00Z ✅ DONE — implemented `requireContext()` / `CONTEXT_SCHEMA_PROPERTY` /
  `tagCommentWithContext()` in `src/tools/args.ts`, wired into all 8 write tools. Decisions made
  and documented in README.md's new "Required `context` on every write" section: (1) shape is
  free text, no fixed vocabulary (build id / chat label / issue number all fit); (2) recorded
  automatically via the existing `runTool()`/`recordActivity()` path, zero new plumbing; (3)
  visible `[chat: <context>]` tag applied to every real comment body this server writes
  (`post_comment` AND `close_issue`'s NOT_PLANNED comment), not just `post_comment` alone — board
  moves/create/update/sub-issue/blocked_by tools have no comment-body text field to tag, so
  those stay audit-log-only. `npm run typecheck` clean, `npm run smoke` passed. Real verification:
  a `post_comment` call with no `context` was rejected pre-flight; the same call with
  `context: "build-2207"` succeeded and posted a real, visibly-tagged comment on #3538 itself
  (doubles as the mandatory finishing comment). Branch confirmed merged into origin/main via
  `verify-branch-merged.mjs`. `git status --porcelain` clean. No findings worth filing as a
  separate sibling issue — the sibling tool issues (#3391-3396) were all already closed/correct.
