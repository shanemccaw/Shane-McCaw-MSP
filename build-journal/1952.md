# #1952 — Policy Engine: OU membership manual override

- **Status:** ⏳ IN FLIGHT 2026-09-01T18:00:00Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-01T18:00:00Z ⏳ IN FLIGHT — Pulled #1952's full issue body + all 5 comments. Shane's final decision (2026-09-01T17:26:07Z): keep Graph `department`-field match as primary/automatic OU-membership resolution; add a real manual override table so an object can be explicitly assigned to an OU, taking precedence over the department-match guess. Building: `active_directory_ou_assignments` table (mirrors `portal_ownership_assignments` explicit-row-wins-over-derived convention), resolution-order change in `policy-compliance-graph.ts` (manual assignment checked first, department match as fallback for un-assigned objects), and sibling admin routes in `admin-active-directory.ts` next to the existing OU CRUD.
