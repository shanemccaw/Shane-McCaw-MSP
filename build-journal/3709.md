# #3709 — shanes-git: add real batch tools (reparent, move_to_status, close)

- **Status:** ✅ DONE 2026-09-12T00:44:17Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 282dc1838 (implementation + docs + verification script), 7a6dd6243 (IN FLIGHT bookend)

## Log
- 2026-09-12T00:39:28Z ⏳ IN FLIGHT — re-verified #3708 and #3697 are genuinely landed on main
  (both `complete`-labeled, both fix commits present in `git log -- artifacts/github-mcp-server`).
  Implementing `batch_reparent_sub_issues`, `batch_move_to_status`, `batch_close_issues` in
  `artifacts/github-mcp-server`, each running its existing single-item tool's real logic once per
  item with independent per-item success/failure reporting, never all-or-nothing. Note: this
  bookend file is being created after the code changes were already drafted rather than as the
  literal first step — a process miss on my part this session; recorded honestly rather than
  silently treated as compliant.
- 2026-09-12T00:44:17Z ✅ DONE — Implemented all three batch tools in
  `artifacts/github-mcp-server`:
  - `batch_reparent_sub_issues(moves: [{issueNumber, fromParent, toParent}])` —
    `src/tools/batch-reparent-sub-issues.ts`, backed by new `reparentSubIssue()` /
    `addSubIssueWithHierarchy()` in `src/hierarchy.ts` (the latter also now used by
    `add_sub_issue` itself, replacing its old inline duplicate). A remove-succeeds/add-fails
    partial failure is reported distinctly as `partial: true`.
  - `batch_move_to_status(moves: [{number, status}])` — `src/tools/batch-move-to-status.ts`,
    backed by new exported `moveIssueToStatus()` in `src/tools/move-to-status.ts`.
  - `batch_close_issues(closures: [{number, stateReason, comment?}])` —
    `src/tools/batch-close-issues.ts`, backed by new exported `closeOneIssue()` in
    `src/tools/close-issue.ts` (keeps the standing Git #2167 NOT_PLANNED-needs-a-comment rule).

  Every batch tool runs one item at a time, sequentially (never in parallel — avoids GitHub's
  own secondary rate limits), returns `{ totalAttempted, succeededCount, failedCount, results[] }`,
  and is never all-or-nothing — verified directly, not just asserted.

  All three registered in `src/tools/index.ts`. Docs updated:
  `artifacts/github-mcp-server/README.md` (new "Batch tools" section + updated context-required
  tool list + updated Verify section), `artifacts/github-mcp-server/package.json` (new
  `verify-batch` script), `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md` (new
  "Bulk board/hierarchy management" section + updated context-required tool list).

  **Real verification (issue's own ask, honored literally):** wrote
  `scripts/verify-batch-tools.mjs`, which creates 12 real, clearly-labeled disposable test
  issues (#3729–#3740 in shanemccaw/Shane-McCaw-MSP), runs a real batch of 11 operations
  through `batch_move_to_status` (10 real successes + 1 real expected failure — a nonexistent
  issue number, chosen over an at-cap-parent failure so this script cannot trigger a real
  overflow-Feature CREATE write), a real batch of 11 through `batch_reparent_sub_issues` (10
  real successes + 1 real expected failure), and a real batch of 13 through `batch_close_issues`
  (12 real successes + 1 real expected failure) — confirmed via direct `getParentIssue`/
  `listSubIssues`/`get_board_status`/`get_issue` reads that real GitHub state matches every
  reported success, then closed all 12 real test issues via `batch_close_issues` itself as
  cleanup. **Ran for real — `npm run verify-batch` — ALL CHECKS PASSED.** Also re-ran the
  pre-existing `npm run smoke`-adjacent `verify-hierarchy` and `verify-contents` scripts to
  confirm the `add_sub_issue`/`move_to_status`/`close_issue` refactors didn't regress them —
  both still ALL CHECKS PASSED. `npm run typecheck` clean throughout.

  **Findings:** none — no genuine bug/gap discovered outside this issue's own scope during this
  build.

  Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs`
  (exit 0) before writing this DONE. `git status --porcelain` clean.
