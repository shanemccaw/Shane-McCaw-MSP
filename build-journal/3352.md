# #3352 — docs/: reorganize into per-product folders

- **Status:** ✅ DONE 2026-09-09T21:38:40Z
- **Scope:** platform (docs/ tree reorg)
- **Started:** 2026-09-09
- **Commit(s):** e8ad4194b (docs/: reorganize into per-product folders (#3352); rebased
  onto origin/main after #3351 landed a concurrent regeneration of
  docs/policy-decisions-msp-console-contract-pack.md — resolved as a real merge, not
  a discard, keeping #3351's regenerated prose and this build's path fixes)

## Log
- 2026-09-09T21:20:00Z ⏳ IN FLIGHT — starting: audit all references to the 87 flat
  docs/*.md files repo-wide, classify each into docs/marketing, docs/portal,
  docs/msp-console, docs/admin-panel, docs/buildconsole, or leave cross-cutting docs
  at docs/ root (or docs/shared/), git mv every move, update every found reference
  (including doc-to-doc relative links), then re-grep to confirm zero stale
  references remain outside historical build-journal entries.
- 2026-09-09T21:38:40Z ✅ DONE — 65 of 87 docs/*.md files moved via real `git mv`
  (100% rename-detected) into docs/marketing/ (2), docs/portal/ (34),
  docs/msp-console/ (21), docs/buildconsole/ (7), docs/admin-panel/ (1). 22 genuinely
  cross-cutting docs (architecture.md, architecture-overview.md, deployment.md,
  known-gaps.md, design-system.md, tenant-permission-model.md,
  graph-resource-model.md, workflow-node-reference.md, SYSTEM_DOCUMENTATION.md,
  email-templates-contract-pack.md, pillarmapping.md, writepackmapping.md, and 10
  others) left at docs/ root per the issue's own explicit option, rather than forced
  into a wrong product folder.
  Reference audit was run BEFORE any move (repo-wide grep, all 87 filenames) and
  again AFTER (re-grep), per the issue's mandated order. 107 files across the repo
  needed real reference fixes — code comments in artifacts/**/*.ts and *.tsx,
  lib/db/migrations/manual/*.sql, test-manifests/**/*.json,
  .agents/agent_assets_metadata.toml, and every doc-to-doc cross-reference inside the
  contract packs themselves (prose citations both `docs/x.md`-style and bare
  `x.md`-style, fixed via a boundary-aware script to avoid the
  `webhooks-contract-pack.md` substring-collision trap against
  `msp-console-webhooks-contract-pack.md` / `msp-webhooks-inbound-contract-pack.md`).
  The two real markdown relative links between docs/admin-panel/admin-panel.md and
  docs/workflow-node-reference.md (one moved, one didn't) were fixed by hand for
  correct `../` vs `./admin-panel/` depth — a generic find/replace would have
  corrupted them.
  Design/portal/design_handoff_full_site/ (its own local `docs/` snapshot copy +
  sync-record.md, used by the Claude Design handoff workflow) was confirmed to be a
  physically separate directory, not the repo's docs/ tree, and left untouched — its
  README.md/sync-record.md path citations resolve against its own local docs/
  subfolder, not repo-root docs/.
  Final re-grep confirms **zero stale references** to any old `docs/<file>.md` path
  remain anywhere in the repo outside build-journal/*.md (historical) and
  PLATFORM_BUILD_ARCHIVE_2026-08-12.md (frozen archive), both explicitly exempted by
  the issue. All doc-to-doc relative markdown links under docs/ verified to resolve
  to real files post-move (one pre-existing, unrelated broken link found in
  docs/weighted-scoring-investigation-413.md pointing at a since-removed code file —
  not a docs/ path, not touched by or introduced by this build).
  Push was rejected once (`origin/main` had moved under #3351's concurrent build,
  which regenerated docs/policy-decisions-msp-console-contract-pack.md at its old
  path); rebased onto origin/main, git auto-detected the rename across the conflict,
  manually resolved the one real content conflict (kept #3351's regenerated prose,
  applied this build's path fix inside it), re-verified zero conflict markers and
  zero stale references repo-wide, then pushed clean.
  `node scripts/dev-server/verify-branch-merged.mjs` confirms `agent/3352-q2071`
  (e8ad4194) is an ancestor of `origin/main`. `git status --porcelain` is empty.
  No fixture/hardcoded data involved — this is a pure docs/ file-organization task.
  No genuine product findings surfaced during this build (the one broken link found
  is pre-existing, unrelated, and in a doc that didn't move — not filed as a new
  issue, since it's not something this build's scope touched or caused).
