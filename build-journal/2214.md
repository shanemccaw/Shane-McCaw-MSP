# #2214 — Feature: Repo Health — mini + full (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:55:00Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** `fc5ee3e4c` (implementation, merged with concurrent #2215 SQL Runner),
  `9c7d29731` (IN FLIGHT bookend) — both verified real objects and ancestors of origin/main

## Log
- 2026-09-02T15:05:00Z ⏳ IN FLIGHT — Real audit done first. `README-ClaudeChat.md`
  §6.6 (the issue's cited spec source) does NOT exist anywhere in the repo — confirmed
  via `find . -iname README-ClaudeChat.md` (no results) and already independently filed
  as open finding #2227 by the prior #2209 session ("source doc not committed"). Not
  a full blocker: the issue body itself states the mini-panel spec in enough concrete
  detail to build against (scan line, 4 rule tiles, capped-5 checkbox list, "In chat"
  lock, "Send N to this chat" footer writing a markdown work order), and explicitly
  flags the full-document layout as "not in this README — real audit needed, same
  caveat as Git Map" — i.e. even the real doc wouldn't have specified that part.
  Building against the issue body + this session's own audit of the established
  ShaneBuilder mini+full convention (LogPeekPanel/LogViewerDock, DetectedItemsPanel,
  GitDoctorDock — all real, landed code) rather than waiting on #2227.
  GraphQL pattern to reuse for the 4 rules confirmed real and already proven in this
  codebase: BuildConsole's `GitHubApiClient.ListBoardIssuesInternalAsync` (subIssuesSummary
  + parent{number milestone{number}}) and ShaneBuilder's own `ChatGitHubFilter` (gh CLI
  shell-out port, no PAT/HttpClient in ShaneBuilder — same convention this build follows).
  Plan: `Services/RepoHealthService.cs` (gh api graphql fetch of open issues + 4-deep
  parent chain, in-process rule evaluation for Depth/Naming/Stale/Orphan) + mini flyout
  column (5th column, same mechanism as LogPeekPanel) + full `RepoHealthDock` tab
  (same skeleton as GitDoctorDock/LogViewerDock).
- 2026-09-02T15:55:00Z ✅ DONE — Shipped `Services/RepoHealthService.cs` (new, 300 lines):
  real `gh api graphql` scan of this repo's own 493 open issues (paginated, subIssuesSummary
  + 4-deep parent{...} chain), evaluated in-process for the four rules. Wired into
  `MainWindow.xaml`/`.xaml.cs`: a 5th `ClaudeChatDock` flyout column (`RepoHealthPanel`,
  same bolt-on mechanism as `LogPeekPanel`/`DetectedItemsPanel`) with scan line, 4
  rule-counter tiles, capped-5-checkbox findings list + "pick next 5", already-sent rows
  locked to "In chat"; a full `RepoHealthDock` tab (same skeleton as `GitDoctorDock`/
  `LogViewerDock`) with the same findings list + a rule-description detail pane. "Send N
  to this chat" writes a real markdown work order via the existing `AppendToComposer`
  (§5 tool-writes-to-composer invariant), with the closing instruction distinguishing
  directly-fixable (Depth/Naming) from must-be-reported (Stale) findings per the issue's
  own footer contract.
  Real audit corrected the Naming rule mid-build: an exact `"Epic: "` match produced ~35
  false positives against real epics on THIS board — `gh api graphql` against the live
  493 open issues showed both `Epic:` (Git #1202) and `EPIC:` (CLAUDE.md's own area-epic
  table: #1096/#1095/#1485/#1571/#1093) are genuinely in active, correct use, plus `GATE:`
  (#1281) as a third legitimate container prefix. Fixed to accept any of the three
  case-insensitively; re-ran against the live 493 and got ~32 real violations (mostly
  `Feature: X (Portal)` issues that grew real sub-issues without a rename — exactly the
  issue body's own "epic-shaped but wrong Feature: prefix" case) and 0 false positives.
  Orphan rule found 8 real violations (e.g. #1860/#1859/#1858/#1857/#1856 under closed
  parent #1842) against the same live data; Depth found 0 (honest — no open issue in this
  repo currently exceeds 3 ancestor levels); Stale validated against real issue bodies
  (#2222's `artifacts/api-server/src/lib/diagnostics-runner.ts` reference correctly NOT
  flagged — file exists).
  README-ClaudeChat.md §6.6 (the issue's cited spec source) is confirmed NOT in the repo —
  already tracked as open finding #2227, not re-filed. Built from the issue body's own
  detailed mini-panel spec plus this session's real audit of the established ShaneBuilder
  mini+full convention instead.
  Merge note: #2215 (SQL Runner) landed concurrently on the same two files and took the
  5th flyout column slot (`SqlRunnerColumn`) first; rebase conflict resolved by hand,
  RepoHealthColumn placed as the 6th column — both panels verified present and complete
  post-merge.
  Verification: `dotnet build -c Debug` clean (0 warnings, 0 errors) both pre- and
  post-merge. WPF surface — BuildConsole/ShaneBuilder is out of scope for test manifests;
  did NOT launch the exe (single-instance-mutex hazard, same as #2209/#2215). Real GraphQL
  query shape and all four rules independently validated against the live repo via direct
  `gh api graphql` calls (see log above) before and after wiring into C#.
  Committed `fc5ee3e4c`, pushed to origin/main, branch verified merged
  (`git merge-base --is-ancestor HEAD origin/main` → true), commit hash verified as a real
  object (`git cat-file -e`), working tree clean (`git status --porcelain` empty).
  No new findings filed this session beyond the already-open #2227.
