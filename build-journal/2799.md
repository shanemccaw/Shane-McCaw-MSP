# #2799 — Git Doctor mini panel (chat tool-rail): Summary → Findings → Claude bridge → Plan → Run log

- **Status:** ⏳ IN FLIGHT 2026-09-04T08:24:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** _(IN FLIGHT — filled at DONE)_

## Log
- 2026-09-04T08:24:00Z ⏳ IN FLIGHT — Building the chat tool-rail MINI Git Doctor panel per README-ClaudeChat.md §6.2 (Feature #2194's real bridge flow), the sibling to #2798's full ActivityBar view (landed `afb9cc1e6`, `complete`). Data sources are #2798's real `GitDoctorService` (`RunChecksAsync`/`ComputeBranchesAsync`/`GetRepoStatusAsync`/`RunStepsAsync`). New body-only `GitDoctorMiniView` UserControl hosted in `ChatDocumentContainer` ToolHost for the `gitdoctor` wrench tool id, cached one-per-tab like `_terminalView` (#2769) / `_logPeekView` (#2787). Implements `IChatSendableTool` (#2783) for the generic rail Send-to-Chat icon. The Claude bridge (paste reply → Extract commands → individually approve → Run approved) is the substantive real part, ported from the full view's Extract/plan/run logic. maximize-2 → full Git Doctor ActivityBar view.

## Plan
1. `Controls/GitDoctorMiniView.xaml(.cs)` — body-only UserControl, §6.2 layout: Summary (headline, `branch · ahead/behind`, "Fix My Git Nightmare"), Findings list (dot + title + severity chip, clickable), CLAUDE BRIDGE (send findings + paste textarea + Extract commands), Plan (approvable checkbox rows + Run N approved), Run log. Implements `IChatSendableTool`.
2. `ChatDocumentContainer` — `_gitDoctorMiniView` cache field; `OpenTool` `"gitdoctor"` branch; maximize-2 wired for gitdoctor → full view.
3. `ActivityBar.SelectGitDoctor()` + `MainWindow.OpenGitDoctorView()` for the maximize-2 target.
