# #2799 — Git Doctor mini panel (chat tool-rail): Summary → Findings → Claude bridge → Plan → Run log

- **Status:** ✅ DONE 2026-09-04T08:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** `5387326e3` (implementation), `6bbdd9582` (IN FLIGHT bookend) — both on `origin/main`; DONE bookend committed on top.

## Log
- 2026-09-04T08:24:00Z ⏳ IN FLIGHT — Building the chat tool-rail MINI Git Doctor panel per README-ClaudeChat.md §6.2, sibling of #2798's full ActivityBar view (landed `afb9cc1e6`, `complete`). #2798 confirmed landed before starting; pulled its real `GitDoctorService` contract.
- 2026-09-04T08:40:00Z ✅ DONE — Shipped. Clean build (0 warnings / 0 errors) after rebasing onto origin/main; pushed to main; verify-branch-merged.mjs confirms merged; working tree clean.

## What shipped
- **`desktop/BuildConsole/Controls/GitDoctorMiniView.xaml(.cs)`** — new body-only `UserControl` implementing `IChatSendableTool`, the README §6.2 mini panel: **Summary** (real headline + `repo · branch · N ahead · N behind` subline + big red "Fix My Git Nightmare" button with backup-branch-first sub-line) → **Findings** (severity dot + ellipsised title + severity chip rows, clickable to select) → **Claude bridge** (`CLAUDE BRIDGE` label + "send findings" writing into the composer + 3-row mono "Paste Claude's commands…" textarea + "Extract commands") → **Plan** (individually-approvable checkbox command rows + "Run N approved") → **Run log** (140px mono scroller). All data is #2798's real, landed `GitDoctorService` running actual git against this repo — `GetRepoStatusAsync` / `RunChecksAsync` / `RunStepsAsync`. No seeded/fixture data.
- **`Controls/ChatDocumentContainer.xaml.cs`** — `_gitDoctorMiniView` cached one-per-tab (same precedent as `_terminalView` #2769 / `_logPeekView` #2787, so a mid-run log + extracted-but-unrun plan survive rail close/reopen); `OpenTool` `"gitdoctor"` branch hosts it and wires its in-panel "send findings" to the tab's `AppendToComposer` (the §5/§6 never-auto-send invariant); `RailMaximize` shown for `gitdoctor` and routed to the full view.
- **`Controls/ActivityBar.xaml.cs`** — `SelectGitDoctor()` (checks `BtnGitDoctor`, raising `ActiveViewChanged` the same as a real click).
- **`MainWindow.xaml.cs`** — `OpenGitDoctorView()`: the maximize-2 target — selects the rail icon, force-expands the sidebar, and `SwitchView("GitDoctor")` (works even when Git Doctor is already active / sidebar collapsed).

## The three issue points
1. **Mini view hosted in the tool rail** — done, exact TerminalView/#2769, LogPeekView/#2787 hosting pattern (cached-per-tab in `ChatDocumentContainer.OpenTool` for the wrench `gitdoctor` id, body-only, container owns the rail header chrome).
2. **`IChatSendableTool` (#2783)** — done: `GetSendableContent()` returns the approved plan as a fenced ```bash block once one is extracted, otherwise the open-findings markdown; null (icon hidden) when there's genuinely nothing. Same generic rail Send-to-Chat icon as every other tool.
3. **The Claude bridge (the substantive part) — built for real, not a stub** — `ExtractPlan` uses the same real parser as the full view (`GitDoctorView.ExtractGitDoctorPlanFrom`): strips a leading `$ > #` prompt marker, drops fence lines, and keeps only lines starting with a known git-repair verb (`git|del|cmdkey|ssh|rm`), so surrounding prose never becomes a runnable step. Each parsed command starts approved and is individually toggleable; only approved commands run, via `GitDoctorService.RunStepsAsync`, streaming into the run log. Nothing runs unapproved.

## Honest adaptation (noted, same class as #2798's own mini-deferral note)
README §6.2 says a findings row "selects the finding in the full document." In BuildConsole the full document (#2798 `GitDoctorView`) is a **separate control instance**, not shared live state (the same structural difference #2786/#2798 already documented for Log Viewer). So selection is honestly adapted: clicking a finding highlights the row **and** previews that finding's plain-English + the doctor's recommended remedy commands inline (real data from the real finding, not a stub), and the rail-header **Maximize** opens the full document. This is an adaptation of the shared-state original, not a reduced port.

## Verification
- `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — **succeeded, 0 warnings / 0 errors** (after rebasing onto origin/main). Restore from cache, no download.
- **Real data-path check (no UI launch, per CLAUDE.md #2156 — never launch BuildConsole.exe / drive its UI; single-instance mutex could courier into Shane's live session):** ran the same underlying git the service runs against this actual repo — **67 local branches** (≥ the service's 25 stale-branch threshold → the stale-branches finding genuinely fires, with real merged/gone counts and `ShowsBranches`), **12 worktrees** (real input to the detached/prunable checks), working tree clean. Confirms `RunChecksAsync` produces real findings for this repo, i.e. real findings would render in the panel.
- **Bridge parser check:** fed a realistic prose+fenced Claude reply (`$ git fetch origin` / `git rebase origin/main` surrounded by explanation and ``` fences) through the same extraction logic — it correctly yields exactly the two runnable commands, stripping the `$` prompt and dropping prose/fence lines. Confirms paste → Extract → approvable Plan works end to end.
- Not launched in the UI (correct verification for this WPF change; BuildConsole is out of scope for test manifests per CLAUDE.md).
- `verify-branch-merged.mjs` — confirms the branch is an ancestor of `main`. `git status --porcelain` clean.

## Findings filed
None. No new bug/gap discovered mid-build — the #2798 `GitDoctorService` contract was complete for everything §6.2 needs, and the container/rail hosting pattern (#2769/#2787/#2783) already existed to plug into.
