# #2712 — Home dashboard: real burndown chart

- **Status:** ✅ DONE 2026-09-03T19:02:52Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 3f0c1b4d1 (rebased/pushed as 605fd5e16 on origin/main)

## Log
- 2026-09-03T18:58:22Z ⏳ IN FLIGHT — #2711 confirmed landed (`1bec2d1be`, bookend `bb431d1c9`): `GitHubIssueTimeSeriesService.GetActiveMilestoneSeriesAsync()` gives a real per-day series (`Points[].OpenCount` running burndown) with fail-closed `HasEnoughData`/`Reason`. Building the real burndown card on `HomeView` — hand-drawn WPF `Canvas`/`Polyline` per this codebase's convention (Build Queue graph), consuming that series directly, honest "not enough history" empty state, no new charting dependency.
- 2026-09-03T19:02:52Z ✅ DONE — Shipped as `605fd5e16` on `origin/main`. Added a "BURNDOWN" card to `HomeView.xaml` (Column 2, in the space #2709 freed up) with a `Canvas` chart, scope label, summary line, and an honest empty-state text block. `HomeView.xaml.cs` wires `RefreshBurndown()`/`RenderBurndown()`/`DrawBurndownCanvas()`: fetches `GitHubIssueTimeSeriesService.GetActiveMilestoneSeriesAsync()`, hand-draws a filled line+area chart of the real running `OpenCount` per day scaled to the canvas's actual pixel size, redraws on `SizeChanged`, and renders `series.Reason` verbatim (never a fabricated curve) when `HasEnoughData` is false. Wired into the existing 10s home-rollup tick (`RenderDashboardState`) — cheap, since #2711's service caches for 5 minutes — plus a manual ⟳ refresh button that force-bypasses that cache.
  **Verification:** `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. Live `gh api` spot-check of open milestones confirms milestone #5 "v1.1 - Monitoring & Launch Control" (920 closed + 491 open = 1411 total) is the real milestone with the most total issues among open milestones — the one `ResolveActiveMilestoneAsync`'s own selection rule (most total open+closed issues among open milestones) will pick, matching the expected active v1.1 milestone. Did not launch BuildConsole.exe itself for UI verification (single-instance mutex hazard into Shane's live session, per standing guidance) — verification is build + code review + live-data spot-check of the resolution logic's real inputs, consistent with #2711's own verification approach.
  Branch verified merged into `origin/main` (`verify-branch-merged.mjs`: OK). Working tree clean (`git status --porcelain` empty).
  No genuine new finding surfaced during this build — the burndown card is a straight consumer of #2711's already-verified data shape.
