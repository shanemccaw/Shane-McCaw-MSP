# #3171 — Feature: Money — Home-tab decision tools (Distribute Paycheck, Period Review, Skip Suggestions, Transfer Instructions)

- **Status:** ✅ DONE 2026-09-08T03:10:14Z
- **Scope:** platform (web/shanes-life, Shane's Life personal project)
- **Started:** 2026-09-08
- **Commit(s):** 4134000b5 (backend), fb27562a4 (UI), 55b6b302c (migration renumber 044→045), all merged to `main`

## Log
- 2026-09-08T02:55:35Z ⏳ IN FLIGHT — Read #3171, #3147, #3137, contract pack Section 12, and
  the real cited source in `shanemccaw/Finance-Tracker` (`FINANCE_TRACKER_AUDIT.md` §1/§5,
  `app/(tabs)/index.tsx`).
- 2026-09-08T03:10:14Z ✅ DONE — Added migration `045_money_home_tab_decision_tools.sql`
  (`accounts.bill_category` + real backfill from ShanesSurvival's own bill-naming convention,
  `paycheck_distributions` table). Added to `money.mjs`: `getPeriodReview` (real
  Available/Spent/Still Due over the current pay cycle, cash-flow identity `opening + deposits =
  current_balance + spent`), `getSkipSuggestions` (category-priority ranked, shown only while a
  real shortfall exists, excludes bills already paid this cycle), `previewDistribution` /
  `applyDistribution` (proportional split of an entered paycheck across every short bill's real
  shortfall, persisted as an editable, never-executes-a-transfer plan), `getTransferInstructions`
  / `markDistributionTransferred`, `setBillCategory`. Wired all as routes in `api.mjs` and as UI
  in `public/app.js`'s Money "Now" tab, right after the Available-to-spend card.

  Verified live, twice: (1) direct `money.mjs` function calls against Shane's real account data
  (all four tools' math checked against real bill shortfalls/balances, including the
  paid-this-cycle exclusion catching a real edge case — H2 Rent, paid 2026-09-04, correctly
  excluded from both Period Review's Still Due and Skip Suggestions); (2) a real HTTP round-trip
  against a booted `server.mjs` with a minted real session cookie — GET on all four
  read endpoints, POST distribute → GET transfer-instructions → POST mark-transferred → PATCH
  bill category, all against real data, all correct. Test rows (sessions, paycheck_distributions)
  deleted afterward; no test artifacts left in the real database.

  This session ran into heavy concurrent activity on the shared local `finances` database
  (migrations 041-044 all landing from other live sessions mid-build) and rebased four times to
  stay current; one real migration-number collision (044 claimed by a concurrent session's own
  `044_income_rules.sql`) was caught and resolved by renumbering this session's file to 045
  before either landed on `main`. Filed #3200 for a real, transient orphan-ledger condition hit
  along the way (since resolved as the concurrent sessions pushed; also surfaced that a separate
  session shipped real bounded-retry + self-service reconcile tooling for this exact problem
  class mid-session, Git #3166).

  Verified branch is a real ancestor of `origin/main` (fast-forward merge, no PR needed) and
  working tree is clean.
