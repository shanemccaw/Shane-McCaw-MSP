# #3171 — Feature: Money — Home-tab decision tools (Distribute Paycheck, Period Review, Skip Suggestions, Transfer Instructions)

- **Status:** ⏳ IN FLIGHT 2026-09-08T02:55:35Z
- **Scope:** platform (web/shanes-life, Shane's Life personal project)
- **Started:** 2026-09-08
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-08T02:55:35Z ⏳ IN FLIGHT — Read #3171, #3147, #3137, contract pack Section 12, and
  the real cited source in `shanemccaw/Finance-Tracker` (`FINANCE_TRACKER_AUDIT.md` §1/§5,
  `app/(tabs)/index.tsx`). Added migration 044 (`accounts.bill_category` + real backfill,
  `paycheck_distributions` table) and ran it directly against local `finances` (worked around a
  real pre-existing orphan-ledger block in `migrate.mjs` -- filed separately). Added
  `getPeriodReview`, `getSkipSuggestions`, `previewDistribution`/`applyDistribution`,
  `getTransferInstructions`/`markDistributionTransferred`, `setBillCategory` to `money.mjs`, and
  their routes in `api.mjs`. Verified all four live against Shane's real account data (deleted
  the test distribution rows afterward). UI wiring in `public/app.js` still in progress.
