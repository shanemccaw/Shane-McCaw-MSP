# #2688 — Detected panel never auto-refreshes

- **Status:** ✅ DONE 2026-09-03T00:30:00Z
- **Scope:** buildconsole

## Log
- 2026-09-03T00:00:00Z — Session started. Sub-issue of #1202. Fixing `Controls/ChatDocumentContainer.xaml.cs`/`MainWindow.xaml.cs` so the Detected panel's `RefreshDetectedAsync` subscribes to the same `BoardRefreshCompleted` cascade already fires for BatterUpPanel/AiBatterUpPanel (#1813, #2557).
- 2026-09-03T00:30:00Z — ✅ DONE — Wired each chat tab's `ChatDocumentContainer.RefreshDetectedAsync()` to `LeftSidebar.BoardRefreshCompleted` at tab creation (`MainWindow.xaml.cs`, in the same block that constructs `ChatTabState`), unsubscribed on `CloseTab` via a new `ChatTabState.DetectedRefreshHandler` field to avoid leaking the container. Kept the existing manual `RailRefresh_Click` path as-is (additive, per the issue's real fix). Verified with a clean `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Merged `agent/2688-q1435` into `origin/main` at e838bc26, confirmed via `verify-branch-merged.mjs`. Working tree clean.
