# #3922 — Settings: named multi-login profile list

- **Status:** ✅ DONE 2026-09-13T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** eef2fafa8 (merged as eb6934a3f on origin/main)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Audited `Controls/SettingsTabView.xaml.cs` / `Services/BuildConsoleSettings.cs`. Found an existing multi-entry `UserAccountEntry`/`UserAccounts` list (add/edit/delete UI already wired under "User Accounts & Gating Tiers") that is the real reuse target — extending it with Label, TargetApp (Marketing/Portal/MSP Console, matching `scripts/dev-server/services.json` titles), LoginUrl, and IsAnonymous rather than inventing a second storage path. Same plaintext settings.json persistence as GitHubPat/UserAccounts today.
- 2026-09-13T00:00:00Z ✅ DONE — Added Label/TargetApp/LoginUrl/IsAnonymous to `UserAccountEntry`; extended the add/edit/delete UI (label box, target-app combo pre-filling a default local-dev login URL, anonymous checkbox that disables URL/email/password, role/tier tag combo made editable for free-text values like `MSPOperator`/`Customer:Growth`) and the render/edit/save/delete/cancel handlers in `SettingsTabView.xaml.cs`; fixed the WebTester autofill overlay in `MainWindow.xaml.cs` to display Label instead of a now-possibly-blank Username. `dotnet build` clean (0 errors/warnings). Standalone System.Text.Json round-trip test confirmed all 4 new fields serialize/deserialize correctly for Marketing/Portal/MSP Console/Anonymous profiles, and that a pre-#3922 (no new keys) settings.json entry still deserializes with safe defaults. Live UI click-through in the running app was NOT self-verified (standing rule against driving BuildConsole.exe from a build session, Git #2156) — Shane should add a handful of real profiles (all 3 target apps + one anonymous) in his own running instance, restart, and confirm reload/add/edit/delete. `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
