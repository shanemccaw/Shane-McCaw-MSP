# #2001 — Hide title-bar usage/cost readout behind a setting, default off

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** b0bdd02c3 (e3f4e1f02 bookend)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Adding `BuildConsoleSettings.ShowUsageReadout` (default false), driving
  `UsageReadoutBorder.Visibility` off it (Collapsed), and a Settings tab toggle that applies live.
  `UsageTrackingService` and `UpdateUsageReadout` stay intact and wired — tracking continues while hidden.
- 2026-08-30 ✅ DONE — `ShowUsageReadout` added to `BuildConsoleSettings.cs` (default `false`, same
  field-initializer convention as every other setting there — a settings.json without the key
  deserializes to `false`). `MainWindow.xaml.cs`'s `UpdateUsageReadout()` (still wired to
  `UsageTrackingService.Changed`, unchanged) now also calls a new `RefreshUsageReadoutVisibility()`
  that sets `UsageReadoutBorder.Visibility` from the setting (`Collapsed`, not `Hidden`). Settings tab
  ("Title Bar" section, next to the Encouragement Critter toggle) gained a `ShowUsageReadoutCheck`
  checkbox that persists the setting and calls `RefreshUsageReadoutVisibility()` live on the running
  `MainWindow` (`Application.Current.MainWindow as MainWindow`) — same live-apply pattern as the
  existing Home/Rental `RefreshLocationToggle()`. `dotnet build -c Release` — 0 warnings, 0 errors.
  Not live-verified against a running BuildConsole.exe process per instructions (never relaunch/kill
  Shane's live instance) — verified by code build only. Nothing else in the title bar touched. No
  second trim candidate noticed; no accuracy-bug root cause was apparent in `UsageTrackingService`
  from this pass, so neither optional finding was filed.
