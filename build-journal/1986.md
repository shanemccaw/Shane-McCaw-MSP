# #1986 — BuildConsole: Home/Rental location switch (gate metered network ops)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** `734b8f515` (implementation, on origin/main) + this bookend

## Log
- 2026-08-30 ⏳ IN FLIGHT — Home/Rental title-bar switch modelled on the account toggle (#1419) + Conservation Cap (#1989); persists in BuildConsoleSettings (default Home); injects BUILD_NETWORK=metered|unmetered into every launched build; one-shot per-op override that never flips mode; `.pnpmfile.cjs` metered-install block (preinstall hook proven dead under pnpm 11 — see findings).
- 2026-08-30 ✅ DONE — Shipped in `734b8f515`.
  - **Setting:** `BuildConsoleSettings.LocationMode` (default `"Home"`); `CurrentNetworkIsMetered()` / `CurrentNetworkLabel()` helpers. Home is the default in every ambiguous case (missing key → C# field default; corrupt settings → `Load()`'s catch returns a fresh instance). Round-trip verified by construction against the proven `DefaultAccount` pattern; NOT live-app verified (did not launch/kill Shane's live BuildConsole, per the build's instruction).
  - **Title-bar toggle:** `TopLocationToggleBorder` next to the account toggle; Red accent when Rental, neutral when Home; distinct `"Rental — override active"` state while a one-shot override is live. Reflected on launch + synced live from the Settings combo.
  - **Settings tab:** Location combo (Home/Rental) with gating copy; live-syncs the title-bar toggle.
  - **Gate — env var:** `QueueWatcherService.LaunchItem` injects `BUILD_NETWORK=metered|unmetered` into every launched build from the LIVE toggle (same `envOverrides` dict as the confirmed-working `CLAUDE_CONFIG_DIR`; reaching-the-child is inferred from that proven path, not separately live-run). A `network` header flag can only TIGHTEN to metered, never loosen.
  - **Gate — enforcement beyond UI:** repo-root `.pnpmfile.cjs` `readPackage` hook refuses `pnpm install` when `BUILD_NETWORK=metered` (hard-abort, exit 1). Unit-tested: metered/METERED → blocked, unmetered/absent → allowed. The issue's proposed `preinstall` extension was verified DEAD under pnpm 11.13.0 (root preinstall never runs during install; `npm_config_*` not exposed to pnpmfile) — filed as **#2004** (parented to #1789, board "AI Batter Up").
  - **One-shot override:** right-click Update → "Run anyway (metered)" on the version-update deploy. Never flips `LocationMode`; shows "Rental — override active"; expires on completion; logged on `system.core`/`version-update`. UI-only — no flag/env/settings an agent can reach.
  - **Audit (deliverable):** VersionUpdate = local rebuild + small `git fetch`; DevServerRollbackService = zero network (reads local JSON); deploy/staging = bytes consumed REMOTELY on Replit, local side sends only tiny SSH/HTTP; worktree provisioning junctions deps (no download). The real 1,120-package local cost is agent `pnpm install`, caught only by `.pnpmfile.cjs`. Issue's named byte-puller hypotheses did not hold — reported honestly.
  - **Verification:** `dotnet build BuildConsole.csproj -c Release` clean (0 errors). Working tree clean; `734b8f515` confirmed ancestor of origin/main.
