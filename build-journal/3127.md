# Git #3127 — Feature: Recipes — Sunday meal-planning ritual + Today surfacing (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:32:00Z
- **Scope:** platform (web/shanes-life)

## Log
- 2026-09-08T00:15:00Z — Session start. `blocked_by` #3124 and #3132, both open but `complete`-labeled
  (real code done, awaiting Shane's review/close) — per the standing unblock rule ("every issue in
  it is closed/complete-labeled"), proceeding. Read contract.md Section 5's real Sunday
  meal-planning ritual paragraph, README.md, `src/core/recipes.mjs`, `src/core/food-preferences.mjs`,
  `src/mcp/tools.mjs`, `src/routes/api.mjs`, and the First Slice Prototype's `mealTeaser`/
  `dinnerLine` lines. Scope for this issue is the plan + simple nudges/teaser only — explicitly
  NOT the prototype's real-time multi-dish Cook-mode timer state machine (that's a separate
  sibling Feature per #3124's own "Explicitly not in scope" section, and #3127's own body doesn't
  ask for it either).
- 2026-09-08T00:32:00Z — ✅ DONE. Built:
  - `web/shanes-life/migrations/026_meal_plan.sql` — `meal_plan_entries` table (real per-day/
    per-meal-slot plan row, nullable `recipe_id`, `dish_text` free-text fallback). Run myself
    against local `finances` (the real shared ShanesSurvival DB, per README) — `applied=1`.
  - `src/core/meal-plan.mjs` — `pushMealPlan`/`listMealPlan`/`archiveMealPlanEntry`/
    `getTodayNudges`. Server-clock-based today/tomorrow (matches `serverDateKey()`'s discipline).
  - `GET/DELETE /api/meal-plan`, `GET /api/today` extended with `mealNudges`/`tonight`
    (`src/routes/api.mjs`).
  - MCP tools `push_meal_plan`/`get_meal_plan` (`src/mcp/tools.mjs`), naming deliberately
    distinct from #3124's existing `push_recipes` (which pushes recipe definitions, not a
    weekly schedule) to avoid a tool-name collision.
  - Web UI: Today's real "Tonight" teaser card + remaining meal nudges, Recipes' real
    "This week's plan" section with per-entry Remove (`public/app.js`). README table row added.
  - Verification: a real, disposable smoke script drove `pushMealPlan`→`getTodayNudges`→
    `listMealPlan`→`archiveMealPlanEntry` against the real local database (today's dinner nudge
    named the real pushed recipe, tomorrow's lunch nudge named the real pushed dish text,
    `tonight` matched) — passed, then deleted (scratch, not committed). Ran the app's own real
    e2e harness (`npm run check`, `bin/check.mjs`) against a real running server on the real
    database: **123/123 checks passed**, including the "not one ShanesSurvival row moved"
    assertion — no regression from these changes.
  - Concurrent builds landed on `main` during this session (#3125 Cook mode, #3135 Medications,
    #3137 Money habits) — real merge conflicts in `api.mjs`/`tools.mjs`/`README.md` (import
    ordering + adjacent route-block insertions), resolved by hand (kept both sides), re-verified
    with `node --check` on every touched file and a second full `123/123` `npm run check` run
    against the merged code before pushing to `main`.
  - `agent/3127-q1890` merged into `main` at `31f15f998` (verified via
    `verify-branch-merged.mjs`: OK). `git status --porcelain` clean.
  - No findings to file — no gaps or bugs discovered outside this issue's own scope.
