# #2739 — Real progress/report counts include Epic and Feature placeholder issues — exclude both everywhere

- **Status:** ✅ DONE 2026-09-03T21:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** c6e6d7da6 (IN FLIGHT bookend), 8610d385b (real fix)

## Log
- 2026-09-03T21:05:00Z ⏳ IN FLIGHT — implementing the 3-point plan from #2739's body + 2 scope-amendment comments: a shared `GitBoardIssueFilters` placeholder/internal-tooling-epic predicate (Services/GitBoardIssueFilters.cs), used to fix (1) the milestone progress bar (FocusModeService.UpdateBoardSnapshot), (2) Home dashboard's BuildSeries, and (3) the Git Board tree's per-Epic transitive rollup pills (LeftSidebar.xaml.cs) — plus the audit-point-3 follow-up (GitMilestone's own native-counter consumers: the milestone detail tab via GitDetailView.cs, and the critter-cheer % via EncouragementService). Local `dotnet build` green after each phase.
- 2026-09-03T21:35:00Z ✅ DONE — all 3 fixes + the audit-point-3 follow-up landed in 8610d385b, `dotnet build` clean (0 errors). Spot-verified against live GitHub data (gh api): v1.1 milestone (#5) carries 115 Feature-titled + 14 Epic-titled issues previously counted as real work (now excluded), and #1202 alone has 64 direct real sub-issues (all now excluded per the internal-tooling-Epic amendment) — confirms the fix has real, non-trivial impact on the actual numbers Shane screenshotted. Two additional out-of-scope consumers of the same native one-level `subIssuesSummary` (Chats panel `EpicProgress`, GATE card fraction) found and filed as #2743, parented under #1202, board status set to "AI Batter Up". `git status --porcelain` clean; branch verified merged into origin/main via verify-branch-merged.mjs.
