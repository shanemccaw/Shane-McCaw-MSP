# #4473 — Six config packs have every config_pack_templates row duplicated

- **Status:** ✅ DONE 2026-09-17T01:10:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 9facd8852

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — confirming real DB state of the 6 duplicate-pair packs, then deleting higher-id duplicates, adding unique index on config_pack_templates(pack_id, template_id, coalesce(check_key,'')), removing redundant duplicate FK constraints, and finding/fixing the seed script that ran twice on 2026-08-21 17:15.
- 2026-09-17T01:10:00Z ✅ DONE — Confirmed live DB matched the issue's snapshot exactly (rows==uniq mismatch on the same 6 packs, ids 125-154 byte-for-byte duplicates of 95-124, no FK into config_pack_templates.id from elsewhere). Ran lib/db/migrations/manual/2026-09-17-dedupe-config-pack-templates-4473.sql against local DATABASE_URL: deleted the 30 duplicate rows, added unique index config_pack_templates_pack_template_check_uniq on (pack_id, template_id, coalesce(check_key,'')), dropped the two redundant duplicate FK constraints (config_pack_templates_pack_id_fkey, config_pack_templates_template_id_fkey). Added matching Drizzle uniqueIndex to lib/db/src/schema/msp.ts. Searched lib/db/migrations/manual/, scripts/, artifacts/ for anything that seeded these 6 packs' templates — no seed file exists anywhere in the repo (matches the issue's own finding), so whatever double-inserted them ran outside version control (no committed script to fix/remove). Verified: tsc --noEmit on lib/db clean, config-pack-orchestrator.test.ts 3/3 passing, re-ran the issue's own detection query post-fix — rows==uniq for all 20 config packs. Merged into origin/main (verify-branch-merged.mjs OK), working tree clean. No mid-build finding to file — this is the complete fix for #4473.
