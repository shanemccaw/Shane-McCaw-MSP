# #1380 — [DEBUG] localhost-gated auto-fill for Buy.tsx/FreeScan.tsx test forms

- **Status:** ✅ DONE 2026-09-10T17:12:54Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** e1dcf2bfb + c0ae4bc09 (pre-existing, already on main) — this session authored NO new product code

## Log
- 2026-09-10T17:12:54Z ⏳ IN FLIGHT — re-dispatched against #1380; checking whether the feature already landed before building.
- 2026-09-10T17:12:54Z ✅ DONE — DUPLICATE DISPATCH. The localhost-gated [DEBUG] autofill was already built, merged, and verified on 2026-08-27 (work commit `e1dcf2bfb`, PLATFORM_BUILD bookend `c0ae4bc09`; both confirmed ancestors of `origin/main` via `git merge-base --is-ancestor`). Verified real code state this session rather than rebuilding:
  - **Backend dev-only exposure** — `artifacts/api-server/src/routes/public-purchase-account.ts`: `isLocalDevCodeExposure = process.env.NODE_ENV === "development"` (line 116). `POST /public/purchase/send-verification-code` returns `devVerificationCode: code` and logs a `[DEV]` warn line ONLY inside `if (isLocalDevCodeExposure)` (lines 257–264); the normal branch (line 266) returns no code. Correctly on the **purchase** flow Buy.tsx actually calls — NOT `public-assessment-account.ts` (the build prompt named the assessment file; the landed work made the correct correction). Server-side gate is `=== "development"` (stricter than the file's `isDev` `!== "production"`), independent of the frontend gate.
  - **Buy.tsx** — `artifacts/shane-mccaw-consulting/src/marketing/pages/Buy.tsx`: `isLocalhost = window.location.hostname === "localhost"` (lines 345–346); all debug UI inside `{isLocalhost && (…)}` (line 4240). Fills name/email/company, password `DebugTester2026A` (passes real strength rule), and reads `devVerificationCode` from the response (line 695/709).
  - **FreeScan.tsx** — same `isLocalhost` runtime check (lines 928–929); UI gated `{isLocalhost && phase === "start" && (…)}` (line 2232). Fills name/company/work-email + consent.
  - **Negative case:** gate is strict `=== "localhost"`, so `127.0.0.1` and every deployed host render nothing; backend field is inert unless `NODE_ENV=development`. Both gates independent, as required.
  - **TOTP/MFA secret** — `POST /public/purchase/mfa/totp/setup` already returns `secret` in plaintext by design (line 619, needed for QR/secret enrollment). Already client-visible — no dev-only exposure needed. Matches the prior investigation.
  - **Stripe** — untouched, per issue (Payment Element iframe cannot be injected by design; Magic Fill covers test mode).
  No code changed this session; issue comment posted. Working tree left clean (only this bookend added). Branch HEAD verified an ancestor of `main` before writing DONE.
