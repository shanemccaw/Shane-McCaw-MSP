# Issue #1807 — Batter Up GraphQL scan: content fragment silently blanks title/number for non-Issue items

- **Status:** ✅ DONE 2026-09-06T00:20:00Z
- **Scope:** buildconsole

## Log
- 2026-09-06T00:00:00Z — Session started. Fixing GitHubApiClient.ScanProjectItemsForStatusAsync to skip/log project items whose content matched no fragment (Number==0 && Title==null) instead of letting them flow into BatterUpRow/auto-queue blank. Default: skip+log, not adding PullRequest fragment (no evidence PRs need to work in Batter Up).
- ✅ DONE 2026-09-06T00:20:00Z — Added an explicit `issue.Number == 0 && issue.Title == null` check right after the null-content guard in the shared `ScanProjectItemsForStatusAsync` (desktop/BuildConsole/Services/GitHubApiClient.cs), logging via `ActivityLog.Log("git-board.data", ...)` and skipping, before it falls into the generic repository-mismatch skip that previously absorbed it silently. This single fix point covers all three callers: `GetBatterUpIssuesAsync`, `GetAiBatterUpIssuesAsync`, and `GetClosedIssuesInStatusAsync`. Did not add a `PullRequest` fragment — no evidence in the issue or codebase that PRs are meant to work in Batter Up, so default skip+log was the right call. `dotnet build` succeeded (0 warnings, 0 errors). Commit `574b82e93` pushed directly to `origin/main` (fast-forward from `2c81a48b1`); `git merge-base --is-ancestor HEAD origin/main` confirms ANCESTOR; `git status --porcelain` clean.
