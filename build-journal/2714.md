# #2714 — Home dashboard: real per-Epic + Milestone ETA projection panel

- **Status:** ✅ DONE 2026-09-03T19:05:26Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** `4baf5779c` (implementation), `33f6c5a91` (IN FLIGHT bookend)

## Log
- 2026-09-03T18:56:00Z ⏳ IN FLIGHT — Blocker #2711 confirmed landed (closed + complete). Investigating FocusModeService.cs ETA logic (~660-687) and #2711's GitHubIssueTimeSeriesService before extending per-Epic + Milestone ETA projection panel on Home.
- 2026-09-03T19:05:26Z ✅ DONE — Shipped the Projected Completion panel on Home.
  - **Extracted** FocusModeService's honest ETA-projection core (least-squares pace fit +
    MinEtaSamples(3)/MinEtaSpan(1h)/flat-pace confidence gates + verbatim reason strings) into a
    shared `IssueEtaProjection.Project(...)`. `FocusModeService.BuildProgress` now delegates to it
    with behavior preserved byte-for-byte (verified by reading the diff: same gates, same reason
    strings, same null-Eta/0-IssuesPerDay on the gate paths).
  - **Added** `HomeEtaProjectionService.ComputeAsync` — resolves the active GitHub milestone
    (#2711 `ResolveActiveMilestoneAsync`), fits one Milestone-level projected release date over
    the milestone's real daily cumulative-closed series, and one projected completion date per
    OPEN Epic in that milestone over each Epic's own real descendant series (#2711
    `GetMilestoneSeriesAsync` / `GetOpenEpicsInMilestoneAsync` / `GetEpicSeriesAsync`). Every
    scope that can't be honestly projected (too little real history, flat pace, all-closed,
    GitHub unreachable) carries the honest reason instead of a fake date.
  - **Added** the "PROJECTED COMPLETION" card to HomeView (Column 0), rendered in code-behind:
    milestone summary row + per-epic rows (projected date badge or honest reason), a manual ⟳
    that forces a fresh GitHub pull, and an honest fail-closed empty state. Loaded on Home tab
    open with `force:false` so it reuses #2711's 5-minute issue-fetch cache and respects the
    manual-refresh-only discipline.
  - **Verification:** `dotnet build BuildConsole.csproj` → Build succeeded, 0 Warnings, 0 Errors.
    Confirmed against live GitHub that milestone 5 (v1.1) is the largest open milestone
    (490 open / 921 closed → `ResolveActiveMilestoneAsync` picks it) with real multi-day close
    history, and that real open Feature/Epic containers exist under it (#2710, #2667, …) — so the
    panel renders real content, not an empty state. Did NOT launch BuildConsole.exe (per the
    single-instance-courier hazard rule) — build-only + live-data verification.
  - Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0); working tree clean.
  - No findings filed — no bug or gap discovered this session.
