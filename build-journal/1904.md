# #1904 — Queue claim never checks if the candidate's OWN issue is closed

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 1ad1ec2896fa4af0d46dfaa8aa8a443ee3d11a36

## Log
- 2026-08-30 ⏳ IN FLIGHT — Adding a self-check to SelectClaimCandidatesAsync so a queue row whose own github_number is closed is held (not claimed) with a clear reason, reusing the same live open-issue fetch used for blocker checks. Skipping local --notGit sentinel rows. Plus a one-time cleanup scan of the real queue.
- 2026-08-30 ✅ DONE — `SelectClaimCandidatesAsync` now extends the fail-closed #1600 live-open-issue check to each candidate's OWN github_number: the single open-issue snapshot (blocker fetch, reused — no second `gh` call) is now also fetched when any candidate has a real (positive) issue to verify, and a candidate whose own issue GitHub reports closed is HELD with `"underlying issue #N is closed — this queue row needs manual review/cancellation"` rather than claimed. --notGit (negative sentinel) and null github_number rows are exempt. Commit 1ad1ec2896fa4af0d46dfaa8aa8a443ee3d11a36.
  - **Verified against real compiled code** (throwaway external harness reflecting into the real private `SelectClaimCandidatesAsync`, temp rows inserted into real `bt_build_queue`, real live open-issue set, harness + temp rows removed afterward): closed not_planned #1873 row → HELD with the exact reason naming #1873; open #1795 row → READY (no regression); --notGit sentinel (-9999) row → READY (exempt). `dotnet build -c Release` clean (0 warnings, 0 errors).
  - **One-time cleanup scan of the real current queue:** 69 `status='queued'` rows carry a real (positive) github_number; cross-referenced against the live open-issue set (412 open, well under the 500 fetch cap → no false-closed risk), **0** of them map to a closed issue. The queue is currently clean — the originally-reported #1873 row is no longer queued. No rows need Shane's manual cancellation right now.
