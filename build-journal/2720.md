## #2720 — URGENT: Batter Up items occasionally (~daily) never launch — even manual force-dispatch does nothing, no error shown

- **Status:** ✅ DONE 2026-09-03T19:47:00Z
- **Scope:** buildconsole

### Log
- 2026-09-03T19:29:24Z — Starting. Real investigation per issue body: audit `BuildQueuePostgresClient.SelectClaimCandidatesAsync` (~line 311) and `GetNextAsync` (~line 252-267) claim path against live queue data for (1) a phantom stuck `running` row holding a concurrency slot, (2) a manual force-dispatch button that silently no-ops before the real claim path, (3) a swallowed exception in the claim/launch chain. Not guess-fixing blind — confirming against real data first.
- 2026-09-03T19:47:00Z — ✅ DONE — Live DB check: zero `running` rows right now (ruled out candidate #1 as the CURRENT cause). Live `.logs/activity-2026-09-03.log` grep for #2715 (this session's own prior build) found the real mechanism: `15:17:29` Shane's manual "Start Now" force-dispatch on queue #1457 reached the real claim path fine ("no real blocker was holding it") then failed at `15:17:30` — "Worktree provisioning FAILED ... C:\wt\2715-q1457 exists but is not a registered git worktree" — matching candidate #2 (force-dispatch hits a real gate that fails invisibly to Shane; only ActivityLog saw it). Confirmed `C:\wt\2715-q1457` on disk is genuinely EMPTY (0 bytes, no `.git`, absent from `git worktree list`) — this is Git #2118's long-unexplained "empty unprovisioned worktree" mystery, now identified. Same failure message recurs 9–19×/day across 5 separate days in the real logs; ~19 other stray empty dirs currently sit in `C:\wt\` in the identical shape. Verified live (direct `git worktree add` + running `provision-worktree.mjs` itself end-to-end, both ok:true, cleaned up after) that `git worktree add` tolerates a pre-existing EMPTY target dir. Real fix: `scripts/dev-server/provision-worktree.mjs` — when the target path exists, isn't a registered worktree, AND is confirmed empty, fall through to the normal create path instead of hard-failing; a genuinely non-empty stray dir still hard-fails exactly as before (never deletes real work). Commit `f7119ce1b`, pushed to `origin/main`, verified merged via `verify-branch-merged.mjs`. Working tree clean. No new C# build needed (pure Node script change).
