# #2994 — SOPs / Runbooks: wire the portal UI to the real endpoints (v2)

- **Status:** ✅ DONE 2026-09-06T19:09:38Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 55559c518 (main wiring), 407b696db + 9dc237868 (manifest
  real-login update), all on `main` as of 3cba9e256

## Log
- 2026-09-06T18:41:05Z ⏳ IN FLIGHT — Real Design export confirmed at
  `Design/portal/design_handoff_full_site/screens/SOPs.dc.html`, backed by
  `Design/portal/design_handoff_full_site/docs/sops-contract-pack.md` and
  `docs/runbooks-contract-pack.md`. Building `/sops` and `/runbooks` pages in
  `artifacts/portal` per that design, wiring `GET/POST /api/portal/sops`,
  `GET /api/portal/sop-runs`, `POST /api/portal/sops/:sopId/custom-steps`, and
  the existing (already-correct, previously-unconsumed) `useRunbooks.ts` /
  `useHoldBadge.ts` hooks for `/api/portal/runbooks` + hold-window decisions.
- 2026-09-06T19:09:38Z ✅ DONE — Shipped `/sops` (Library/Queue/History tabs,
  stat cards, filters, detail panel, New Procedure + Add-custom-step forms)
  and `/runbooks` (runbook cards with tickable steps, hold-window cards, a
  decision panel driving the real close-early/release/prepare-cr writes) in
  `artifacts/portal`. New `useSops.ts` hook; fixed `useRunbooks.ts`'s client
  wire types, which were missing the real `recurring`/`currentRunId`/
  `cycleNumber`/`runHistory`/`runId` fields (#1557/#1940) even though the
  backend has carried them for a while. Wired `moduleNav.ts` (`builtPath`),
  `App.tsx` routes, `SopTray.tsx`'s two links, and `useHoldBadge` into
  `SidebarNav`'s Runbooks row (built, previously zero consumers). New shared
  `SlidePanel.tsx` right-slide panel per the design's own shared pattern.
  Rebased twice onto concurrent landings mid-session (Notification
  Preferences' `_regression-suite.json` entry; Auth Core's real `/login`
  page + `App.tsx` restructure) — resolved both conflicts by hand, kept both
  sides' routes.
  **Verification:** `tsc --noEmit` clean and `vite build` clean after every
  rebase. Directly curled the live local api-server as the real testbed
  account (customerId 1): `GET /api/portal/sops` returned the real 15
  `msp_sops` rows in the exact `WireSopsPayload` shape my `useSops.ts` types
  expect (including `meta[id].code`/`level`, which the design's own mock
  objects don't name the same way as the real wire — caught and fixed
  against the real route source, not the design mock); `GET
  /api/portal/sop-runs` returned an empty queue + 15 real "Version
  published" audit rows; `GET /api/portal/runbooks` returned a genuinely
  empty `{runbooks:[],holds:[],summary:{text:"No hold windows"}}` (0 rows
  in `portal_runbooks`/`portal_hold_windows` — a live-empty state, not a
  bug). Exercised both real writes end-to-end: `POST /api/portal/sops`
  created a real row and it appeared in the next `GET`; `POST
  /api/portal/sops/:sopId/custom-steps` added a real overlay step and it
  appeared with `isCustom: true` and the expected `"title — description"`
  text join. New `test-manifests/portal/sops-runbooks.json` (registered in
  `_regression-suite.json`) carries these as real `apiTests`, plus `uiSteps`
  driving the real login page that landed concurrently (#2991) and
  asserting a real seeded library row + both pages' honest-empty states —
  not executed via `shaneapp://runTest` this session: my own accidental
  `request-restart.mjs --help` invocation (a mistake — that flag isn't
  recognized, so it ran with defaults) confirmed the dev-server checkout is
  coordinated by this build's own `buildSet=SOPsRunbooks` and correctly
  refused to land my commit early (a real merge conflict against a
  concurrent buildSet member for #2830, auto-aborted cleanly — `C:\dev-server`
  verified clean afterward), so the running dev server won't reflect these
  new routes until that buildSet's own coordinated restart fires, which is
  not this session's call to force. Filed **#3057** (parented under Feature
  #1493, `AI Batter Up`): 16 `test-manifests/portal/portal-v2-*.json` files
  are still registered in `_regression-suite.json` but target the retired
  `artifacts/msp-portal` surface (`f40438cdc`, #1921) and will fail on any
  real run. `git status --porcelain` clean; branch verified merged into
  `origin/main` (`scripts/dev-server/verify-branch-merged.mjs`, exit 0).
