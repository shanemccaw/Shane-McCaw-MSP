# #3399 — msp_diagnostic_findings has no per-item resolution/acknowledge mechanism (Alerts feed, Git #3366)

- **Status:** ✅ DONE 2026-09-11T00:40:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** b38bf7fd8

## Log
- 2026-09-11T00:00:00Z ⏳ IN FLIGHT — investigated the two options the issue itself raised (own acknowledge column vs. routing through `remediation_tracker_steps`), decided in favor of a dedicated `acknowledged_at`/`acknowledged_by_user_id` column pair on `msp_diagnostic_findings` (mirrors `policy_rule_incidents.resolved_by_user_id`'s shape) — `remediation_tracker_steps` is a customer-facing decision lifecycle with real side effects and only covers ~30 steps' worth of check_keys, so most findings would have no mechanism under that route.
- 2026-09-11T00:40:00Z ✅ DONE — added `acknowledgedAt`/`acknowledgedByUserId` to `mspDiagnosticFindingsTable` (lib/db/src/schema/msp.ts), wrote + ran migration `2026-09-11-msp-diagnostic-findings-acknowledged-3399.sql` against local Postgres (additive `ALTER TABLE`, confirmed via `\d msp_diagnostic_findings`, 296 pre-existing rows unaffected), and wired `POST /msp/alerts/:alertId/acknowledge`'s `finding-*` branch in `artifacts/api-server/src/routes/msp-alerts.ts` to actually acknowledge (idempotent, audit-logged via `createAuditLog`, staff-scope checked, matches the `incident-*` branch's shape) instead of returning 400; `GET /msp/alerts` now excludes acknowledged findings from the feed. Verification: `pnpm`-equivalent `npx tsc --noEmit -p .` in `artifacts/api-server` is clean (had to rebuild `lib/db`'s stale `dist/schema/msp.d.ts` via `npx tsc -b .` first — real project-references quirk, not a fresh finding worth filing). Rebased onto `origin/main` and pushed directly (`b38bf7fd8`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
