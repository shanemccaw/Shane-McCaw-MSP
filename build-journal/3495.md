# Build Journal — Issue #3495

- **Status:** ✅ DONE 2026-09-10T22:33:50Z
- **Scope:** platform

## Log
- 2026-09-10T22:22:24Z — Starting: remove orphaned Status Reports server endpoints/exports per #3495's audit (follow-up to #3427 teardown).
- 2026-09-10T22:33:50Z — ✅ DONE — Removed all 3 pieces identified in #3495's audit plus the schema/join cleanup, commit 07f2b6c777167defd0758cc0f218b6db379dda37 on main:
  - `admin-client-documents.ts`: removed dead `GET /admin/clients/:clientId/status-reports`.
  - `admin-ai-draft.ts`: removed dead `POST /admin/status-reports/ai-draft` + its now-unused helpers/types (`buildContext`, `PERSONA`, `Activity`, `NextStepItem`, `AiDraftBody`) and the now-unused `getPrompt` import. The unrelated live route `POST /admin/ai/suggest` was left untouched.
  - `mailer.ts`: removed unused exports `statusReportReplyEmail`, `adminThreadReplyEmail` (both confirmed zero callers repo-wide).
  - `notification-center.ts`: removed unused export `notifyStatusReportPublished` (confirmed zero callers).
  - `admin-projects.ts` + `lib/db/src/schema/index.ts`: removed the permanently-dead `kanban_tasks.statusReportId` enrichment join in `GET /admin/kanban-tasks` (no writer since #3427) and the Drizzle field itself.
  - Wrote `lib/db/migrations/manual/2026-09-10-drop-kanban-tasks-status-report-id-3495.sql` to drop the now-unreferenced `kanban_tasks.status_report_id` column — **destructive, deliberately NOT run by this agent** per CLAUDE.md's Database section (same pattern as #2460's capability-column drop). Recorded on #1630's release checklist.
  - Verified: `tsc --noEmit` clean for both `@workspace/api-server` and `@workspace/db`. Repo-wide grep confirms zero remaining references to any of the removed symbols/routes.
  - Filed new finding **#3550** (`clientThreadReplyEmail` in mailer.ts, also dead, not named in #3495's own audit) — parented under #1095, board status AI Batter Up.
  - Branch verified merged into main (`verify-branch-merged.mjs` exit 0). Working tree clean.
