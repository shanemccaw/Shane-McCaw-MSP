# Git #3604 — Remove tab-header In Progress bolt button (+ context menu, superseded scope)

- **Status:** ✅ DONE 2026-09-11T01:40:00Z
- **Scope:** buildconsole

## Log
- 2026-09-11T01:32:54Z — Started. Read #3604 body + the in-flight supersede comment (also remove
  the tab context menu's "⚡ Mark as In Progress" item, not just the tab-header button).
- 2026-09-11T01:40:00Z — ✅ DONE — Removed `boltBtn`/`UpdateBoltAppearance`/its Click wiring/
  `boltTab` from the tab-header-building code, and removed `miInProgress` (the context menu's
  "⚡ Mark as In Progress" item) plus its Click wiring and visibility toggle from
  `AttachTabContextMenu`, in `desktop/BuildConsole/MainWindow.xaml.cs`. Left untouched per scope:
  `FocusModeService`'s `InProgressChats`/`IsChatInProgress`/`ToggleChatInProgress`, the sidebar's
  own conditional In Progress icon (`BuildChatCard`), and `MainWindow.FocusMode.cs`'s Replace In
  Progress flow. `dotnet build BuildConsole.csproj -c Debug` succeeded clean (0 errors, only
  pre-existing unrelated `FocusModeBar` CS0067 warnings).
  Commit: 87d6eb59e (code) / f23b8cb04 (IN FLIGHT bookend).
  **Flag for follow-up (per issue's own request):** `ToggleChatInProgressResolved` and
  `MarkChatTabInProgressAsync` are now dead code with zero real callers left anywhere in the
  file (confirmed via grep) — the null-guarded bolt-button lookup inside
  `MarkChatTabInProgressAsync` (~old line 5532) is also unreachable now. Left in place, not
  removed, per the issue's explicit instruction not to remove speculatively.
  Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0). Working tree
  clean (`git status --porcelain` empty).
