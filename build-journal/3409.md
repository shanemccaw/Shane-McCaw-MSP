# #3409 — @workspace/db/rbac extensionless relative imports break Node's ESM loader

- **Status:** ✅ DONE 2026-09-10T23:15:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** e84c41ad9 (rebased/merged to main as a5a7f4bf)

## Log
- 2026-09-10T22:50:00Z ⏳ IN FLIGHT — Investigating extensionless imports in lib/db/src/rbac and its transitive imports (../schema/rbac -> ../schema/index) per #3409's evidence. Plan: add real `.ts` extensions to all extensionless relative imports across the affected module graph, matching the pattern #2458 already established (`allowImportingTsExtensions: true` + explicit `.ts` suffix).
- 2026-09-10T23:15:00Z ✅ DONE — Added explicit `.ts` extensions to every extensionless relative import across `lib/db/src/rbac/**` and its transitive `lib/db/src/schema/**` dependency graph (21 files, 56 import lines). Verified: `node --experimental-strip-types --experimental-test-module-mocks --test 'src/routes/msp-rbac.test.ts'` (artifacts/api-server) now passes 37/37 — was `ERR_MODULE_NOT_FOUND` before. `lib/db` vitest: 90/90 pass. `lib/db` tsc --noEmit: clean. Ran the full 21-file node-runner suite and found two pre-existing, out-of-scope issues, both filed as sub-issues of #1696 and set to AI Batter Up: #3574 (same extensionless-import class at 253-file scale in artifacts/api-server/src/lib, not fixed here) and #3576 (msp-staff.test.ts asserts a stale error-response shape, pre-existing test bug, not fixed here). Rebased agent/3409-q2236 onto origin/main and pushed to main (a5a7f4bf); verify-branch-merged.mjs confirms merged. Working tree clean.
