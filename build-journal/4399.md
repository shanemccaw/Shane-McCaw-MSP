# #4399 — 7 missing FK constraints (msp_refresh_tokens.user_id, sla_breaches/sla_compliance_records/sla_timers msp_id+customer_id)

- **Status:** ✅ DONE 2026-09-16T19:26:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 79587fc78 (real work), 811385c34 (IN FLIGHT bookend)

## Log
- 2026-09-16T19:13:00Z ⏳ IN FLIGHT — pulled issue #4399 body + comment (superseding), confirmed live via pg_constraint that all 7 FKs are missing. Ran orphan-row check: sla_breaches/sla_compliance_records/sla_timers have zero orphans on msp_id/customer_id; msp_refresh_tokens.user_id has 5 orphaned rows (unexpired, unrevoked tokens for deleted user_ids — consistent with the very gap this issue describes). Writing migration next.
- 2026-09-16T19:26:00Z ✅ DONE — Wrote `lib/db/migrations/manual/2026-09-16-missing-fk-constraints-4399.sql` (NOT VALID -> verify -> VALIDATE pattern). Ran it against local `shanemccawmsp`: all 6 sla_* constraints validated clean (0 orphans); msp_refresh_tokens_user_id_fkey added NOT VALID only (5 real orphans — not silently deleted/force-added, filed as #4433, parented under #4270, board status "AI Batter Up"). Re-run confirmed idempotent. Updated Drizzle schema (`lib/db/src/schema/msp.ts` mspRefreshTokensTable.userId, `lib/db/src/schema/sla.ts` slaTimersTable/slaBreachesTable/slaComplianceRecordsTable msp_id+customer_id) to match — `tsc --noEmit` clean. Removed `scripts/db/reset-dev-database.mjs`'s now-dead `UNCONSTRAINED_FK_SHAPED_EDGES` workaround; live-verified `find-tenant-scoped-tables.mjs` now resolves msp_refresh_tokens/sla_breaches/sla_compliance_records/sla_timers via the real FK edge (all depth 1), and the reset script's `--dry-run` still succeeds unchanged (223 discovered tables, same as before). Pushed to origin/main as 79587fc78, verified ancestor via verify-branch-merged.mjs. Working tree clean. Posted comment + labels on #4399.
