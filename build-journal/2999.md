# 2999 — Reinstatement request: what happens after a gated customer files one

- **Status:** ✅ DONE 2026-09-07T17:22:40Z
- **Scope:** platform
- **Issue:** [#2999](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/2999)
- **Branch:** `agent/2999-q1852` — merged, `verify-branch-merged.mjs` exit 0
- **Commits:** `28d86e5c0` (IN FLIGHT bookend), `62ef87ec2` (the work)
- **Migration:** `lib/db/migrations/manual/2999-reinstatement-request-ticket-columns.sql`
  — additive, run against the local `DATABASE_URL` in-session (7 nullable columns +
  1 partial index). Recorded on **#1630** for Replit/Staging at release time.
- **Findings filed:** **#3098**, **#3099** (both `bug`, both sub-issues of Feature #1944,
  both set to *AI Batter Up*)

## Log

- ⏳ IN FLIGHT 2026-09-07T15:52:10Z — Session start. Pulled #2999's body and both BUILD
  comments in order. The second comment (Posted 2026-09-07T15:46:32Z) is an explicit
  **correction** superseding the first (15:43:20Z): Shane's real decision is
  **option 1 + option 3** — auto-resume (already built) plus a real Zoho Desk ticket via
  `enqueueEscalationTicket()` — **not** option 4 (a new Shane/Platform operator queue).
  My dispatch prompt carried the older option-4 framing; built the correction.

- ✅ DONE 2026-09-07T17:22:40Z — Options 1 + 3 shipped.

  **1. Auto-resume — confirmed still holds, no new work.** Ran
  `msp-cascade.live-db.test.ts` against live Postgres: **8/8 pass**, including the case
  asserting that when the MSP resumes, `readOpenReinstatementRequest()` returns null and
  the row reads `status = 'resolved'`, `resolution = 'portal_reopened'`.

  **3. A real Zoho Desk ticket, raised the moment the row lands.** New
  `artifacts/api-server/src/lib/retention/reinstatement-ticket.ts` reuses
  `enqueueEscalationTicket()` — the exact path `POST /api/portal/customer/requests`
  already uses — hung off the insert in `submitReinstatementRequest()` (created path
  only; `already_open` raises no second ticket). No new operator queue, no new Zoho
  surface. Options 2, 4 and 5 explicitly not built.

  **The ticket goes to the PLATFORM's Desk, not the customer's MSP's.** This is what
  answers #2999's own objection to option 3 ("it lands in the MSP's Desk, so it inherits
  option 2's problem"): addressed to `ZOHO_DEFAULT_MSP_ID`, never `tenants.msp_id`, so a
  cascade request does not go to the delinquent party. Also confirmed against live
  Postgres that this is the only Desk that exists — exactly one `zoho_connection` row,
  `msp_id = 1`, status `connected`; any other MSP would raise `ZohoNotConnectedError` at
  drain time. The Desk *contact* is still the customer who asked.

  **The ticket is recorded on the request row**, because `retention_reinstatement_requests`'
  own docblock rules out building any option on "a notification fired once and stored
  nowhere". `ticket_job_id`/`ticket_enqueued_at` at request time;
  `ticket_zoho_id`/`ticket_number`/`ticket_url`/`ticket_created_at` written back by the
  drain from `handleCreateTicketJob()` once Zoho confirms the ticket; `ticket_error` when
  none could be raised at all, so a silently-failed notification stays distinguishable
  from one merely still queued. Surfaced on the wire as a three-state `ticket` object
  (`created` / `queued` / `unavailable`) on `GET /api/portal/retention/subscription-gate`.

  **Closure path confirmed single.** `resolveOpenReinstatementRequests()` remains the one
  mechanism writing `status`/`resolution`; nothing in the ticket path touches either, and
  the ticket body says so in plain words so a responder restores billing rather than
  closing the record by hand. Asserted directly: a confirmed ticket leaves the request
  `open` with `resolution` null.

  **Verification (real, all against live local Postgres):**
  - New `reinstatement-ticket.live-db.test.ts` — **7/7 pass**: one job queued and no more
    on a double-submit; `msp_id = ZOHO_DEFAULT_MSP_ID` and `!= ` the customer's MSP;
    description carries customer + tenant id, MSP + msp id, `MSP cascade: yes`, the lapse
    timestamp, the request timestamp and the customer's verbatim note; drain write-back
    records the ticket and leaves `status` open; `resolveOpenReinstatementRequests()`
    still resolves and does not erase the ticket; the no-contact-email path records
    `ticket_error` and queues nothing.
  - `msp-cascade.live-db.test.ts` — 8/8 (unchanged, option 1).
  - `subscription-freeze.live-db.test.ts` + `subscription-gate.test.ts` — 42/42.
  - `tsc -p tsconfig.json --noEmit` on api-server — clean, exit 0.
  - Scratch rows verified removed afterwards (0 leftover msps/users/requests; job queue
    back to its prior 66 rows).

  **Honest exceptions.** Under a full parallel `src/lib/retention` run, three live-db
  files reported failures that all pass in isolation — the local-Postgres contention this
  repo's own `vitest.config.ts` already documents (#2877/#3066). One failure is genuinely
  real and pre-existing: `tenant-scope-coverage.live-db.test.ts` is red on
  `msp_poams.tenant_id`, from #3080, unrelated to this build → filed as **#3098**.

  **Findings filed:**
  - **#3098** — `tenant-scope-coverage.live-db.test.ts` red: `msp_poams.tenant_id`
    claimed by no purger and carrying no exemption, since #3080.
  - **#3099** — 53 FK columns referencing `users` carry no index, so every
    `DELETE FROM users` sequentially scans all 53 tables (~1.4s warm / ~5.4s cold on a
    near-empty dev DB). Hit as a 20s `afterAll` hook timeout in the new test; worked
    around with an explicit `60_000` timeout and the measurement written beside it, not
    fixed.

  No test manifest: this build touches `artifacts/api-server/` and `lib/db/` only, both
  outside the manifest scope (`artifacts/msp-portal/`, `artifacts/shane-mccaw-consulting/`).

  Working tree clean; nothing pre-existing was dirty on arrival.
