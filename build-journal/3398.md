# #3398 — Feature: Remove Build/Git tooling from Admin Panel (AdminPanel)

- **Status:** 🙋 ASK SHANE 2026-09-11T02:10:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** (none — no deletion performed; issue's own premise conflicts with real code, see log)

## Log
- 2026-09-10T02:06:13Z 🛑 BLOCKED — Pulled #3398's body + comment (single BUILD comment,
  no supersession). Before touching any files, checked GitHub's real `blocked_by`
  dependency edge on #3398 per the standing "blocked" protocol
  (`gh api repos/.../issues/3398/dependencies/blocked_by`) — it genuinely IS
  blocked by #3401 ("Feature: Remove leftover legacy site-analytics remnants from
  Admin Panel"), which is still **open**, still labeled `in-flight`, not
  `complete`. #3401 touches the same admin-panel nav/route registration surface
  this issue needs to edit (left-rail/route tables), so the two are correctly
  serialized rather than run concurrently — that's the real reason for the
  `--blocked-by 3401` on this build's header. Stopping here per the hard rule:
  a genuinely open, non-`complete` blocker means stop, don't guess forward. No
  files touched. Labels flipped `in-flight` → `blocked` on #3398; comment posted
  explaining the wait. Will resume once #3401 clears (closes or is labeled
  `complete`), re-checking the dependency list first per the standard
  pick-back-up procedure.
- 2026-09-10T17:34:21Z 🛑 BLOCKED (re-verified, no change) — Fresh session picked
  this back up and re-checked
  `gh api repos/.../issues/3398/dependencies/blocked_by` per the standard
  pick-back-up procedure before touching anything. Still genuinely blocked:
  #3401 is still **open**, still labeled `blocked` (not `complete`), and is
  itself blocked by **#3441** ("Feature: Remove old legacy Tasks tab from
  Admin Panel Marketing tool"), which is also open. The chain is
  #3398 → #3401 → #3441, all open. No files touched, no label changes (3398's
  `blocked` label and its `blocked_by → #3401` edge were already correct from
  the prior session). Stopping again per the hard rule rather than guessing
  forward. Will resume once #3401 clears.
- 2026-09-11T02:10:00Z 🙋 ASK SHANE — Fresh session, chain cleared: #3401 is now
  labeled `complete` (was `blocked`, now empty `blocked_by`), and #3441 (what
  #3401 was itself waiting on) is closed/`complete`. Removed the now-stale
  `blocked_by → #3401` edge on #3398 (`gh api -X DELETE
  .../3398/dependencies/blocked_by/5406364207`), flipped label `blocked` →
  `in-flight`, and started the real deletion work.

  **Stopped before deleting anything — the issue's premise does not match the
  real code.** Per the issue's own step 4 ("Confirm no other admin-panel
  screen imports anything from these two directories... before removing")
  and the identical caveat on sibling issue #3401 ("If it turns out to still
  be serving something real, stop and report back rather than deleting live
  functionality"), I ran the real grep this gate calls for and it fails hard:

  - `artifacts/admin-panel/src/adminv2/screens/project-management/index.tsx:6`
    imports `syncFromGitHub, MILESTONE_ETA_KEY, openHeadlineMilestone,
    SYNC_GITHUB_KEY` from `../build-tracker/buildTrackerStore`, and
    `index.tsx:8-9,96-97` registers `BuildTrackerExplorer` and
    `BuildTrackerProperties` (both on the 11-file deletion list) as
    Project Management's own left/right rail panels.
  - `project-management/ProjectManagementBody.tsx:20-28` imports 17 real
    functions (`getSnapshot, subscribe, milestoneById, epicsForMilestone,
    issuesForEpic, createMilestone, updateMilestone, deleteMilestone,
    assignEpicToMilestone, selectMilestone, selectEpic, epicIsUnassigned,
    epicBubbleStatus, epicCardState, issueCardState, formatIssueAge,
    estimateEpicHours, epicIsDone`) plus types (`MilestoneRow, EpicRow,
    MilestoneStatus, githubIssueUrl`) from `buildTrackerStore.ts`/
    `buildTrackerTypes.ts` — the actual Gantt/Milestone-Timeline engine
    behind Project Management's "Milestones & Roadmap" feature.
  - The dependency runs both ways: `build-tracker/BuildTrackerExplorer.tsx:38`
    and `BuildTrackerProperties.tsx:22` import `STATUS_COLOR, STATUS_LABEL`
    back from `project-management/ProjectManagementBody.tsx` — a circular
    cross-directory dependency proving these are one intertwined feature,
    not two independent screens.
  - `artifacts/admin-panel/src/adminv2/screens/run-history/runHistoryActions.ts:24`
    imports `openConsole, runTyped, setInput` from `../git/deployStore` —
    Run History's real "re-run" action for any Deploy-Console-originated run
    calls directly back into `screens/git/deployStore.ts`.
  - `shell/TitleBar.tsx:56-61,177-248,341` — shell chrome itself (not a
    "screen") imports `buildTrackerStore`/`buildTrackerTypes` to render the
    title bar's "🔥 N In Progress" quick-access pill + chat-linking dropdown.
  - `shell/Shell.tsx:266-270,344-346,356-358,370,373-374,412,486,509,601-628` —
    the AdminV2 home dashboard (`NoScreen()`, "Pickup where you left off")
    pulls epics/issues/milestones + `unlinkedChats()`/`setTriageActive()`
    from `buildTrackerStore`, and `deployStore`/`findDeployOperation` from
    `screens/git/`, to render its "Build Triage", "In-Progress Work",
    "Milestones Roadmap", and "Git & Deploy Sync" rollup cards — i.e. the
    landing screen every AdminV2 session opens to.
  - `AdminV2.tsx:27,48,60,142` mounts `FloatingDeployConsole` and
    `BuildTrackerFetchBridge` unconditionally at shell level, not gated
    behind visiting `/git` or `/build-tracker`.

  Net: `buildTrackerStore.ts`, `buildTrackerTypes.ts`, `BuildTrackerExplorer.tsx`,
  and `BuildTrackerProperties.tsx` are the real Epic/Issue/Milestone(+Chat)
  data-and-UI layer Project Management is built on, not isolated Build-Tracker
  remnants; `screens/git/deployStore.ts` is a real Run History dependency; and
  both stores also feed the shell's title bar and home dashboard. Deleting
  either directory in full, as literally instructed, breaks TypeScript
  compilation for Project Management, Run History, TitleBar.tsx, and
  Shell.tsx's home dashboard — none of which are Build/Git tooling duplicating
  BuildConsole; they're distinct, real, already-built features (an actual
  Gantt chart, a re-run action, the app's own landing screen).

  Not proceeding with a guessed split either — there are several plausible
  remediation shapes with real, different consequences (what survives where,
  whether Project Management keeps reusing Explorer/Properties as-is, how
  Run History's re-run path is preserved), laid out for Shane in the issue
  comment. No files deleted, no source edited. Set board status to **Ask
  Shane** (`404998bb`) via the real GraphQL mutation. Labels left at
  `in-flight` (not `blocked` — nothing else needs to land first; this needs
  Shane's decision, not a wait).
