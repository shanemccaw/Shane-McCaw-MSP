# Build Journal — #3326

- **Status:** ✅ DONE 2026-09-09T00:40:00Z
- **Scope:** platform (personal project — Shane's Life, milestone "Shane")
- **Log:**
  - 2026-09-09T00:00:00Z — Starting: pantry capture-grammar gaps (query rule + lvl auto-detect) per #3326.
  - 2026-09-09T00:40:00Z — ✅ DONE — commit `dbd850015` (rebased onto main, same tree pushed directly to `origin/main`).
    - Gap 1 ("do we have X" queries): added `pantry_query` rule to `web/shanes-life/src/core/capture-grammar.mjs`,
      ported verbatim from the design's own `pHave` regex (First Slice Prototype.dc.html). Read-only, no DB write.
      Backed by a new `pantry.findPantryItemForQuery` (scoped to a stated house, or every house when none is
      stated) and a new `pantry.qtyLabel` (ported from `public/app.js`'s `pantryQtyLabel`) so the toast echoes the
      real, already-displayed label.
    - Gap 2 (bare spice/oil statements never become a real `unit = 'lvl'` row): added `pantry.isSpiceOrOilName`
      (the design's own `SPICY` regex) and `classifyLevelUnit` to `web/shanes-life/src/core/pantry.mjs`, wired into
      both `setPantryQuantity` and `adjustPantryQuantity` so a brand-new row for a spice/oil-shaped name with no
      stated unit (or a jar/bottle unit) lands as a real level row at Full, instead of a plain count that could
      never reach the Out/Almost out/Half/Full display path. Also added a narrow new `pantry_spice_bare` capture
      rule (gated strictly on `isSpiceOrOilName` to avoid the same "I have X" false-positive risk rule 33's own
      comment already flags) for the genuinely unit-less statements the issue cites verbatim ("I have salt",
      "we're low on paprika", "bought cumin").
  - Tests: extended `capture-grammar.test.mjs` with real coverage for both new rules (do-we-have/how-many/any
    query shapes, place-suffix house resolution, spice-gated bare statements, and the non-spice false-positive
    guard) — 71/71 passing (`node src/core/capture-grammar.test.mjs`, run with `DATABASE_URL` pointed at the
    local `shanemccawmsp` Postgres so `config.mjs` doesn't throw at import time).
  - **Verification limitation, stated honestly:** could not run the live DB write path end-to-end
    (`setPantryQuantity`/`adjustPantryQuantity` against a real `pantry_items` row) or `npm run check` in this
    session — the real local `shanes_life` Postgres database (the one `web/shanes-life/.env.example` points at)
    is missing the ShanesSurvival base schema (001-012: `accounts`, `transactions`, `plaid_items`, `debts`), and
    `bin/migrate.mjs` correctly refuses to proceed without it ("point DATABASE_URL at the database the WPF app
    really uses"). That base schema is only ever applied by running the real ShanesSurvival WPF app once against
    that database — not something this session can do. Unit-level coverage (pure regex classification + the
    write functions' own code path review) is real and passing; a live DB write/read round-trip for this specific
    change was not independently re-verified beyond that.
  - No migration needed — no schema change, only new/extended JS logic in already-existing modules.
  - No separate finding filed — both gaps were the issue's own stated, already-fully-scoped content; nothing
    additional discovered outside what #3326 already described.
