# #4384 — Local dev DB: customer RBAC seed wiped

- **Status:** ✅ DONE 2026-09-16T17:39:55Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 79a4bb71e (bookend), d5fc8c7e7 (repair migration + parity-check.ts fix)

## Log
- 2026-09-16T17:29:59Z ⏳ IN FLIGHT — investigation confirmed the wipe: `customer_roles` has only the 4 #4371 rows (Monitoring/PackPending/Consented), `customer_feature_role_mapping` is fully empty (0 rows), while `msp_roles` (13) / `msp_feature_role_mapping` (14) are fully intact. Root cause traced to #4272 (Phase 2 tenant-data reset, executed 2026-09-16T00:33Z): #4271's audit (Phase 1) categorized `customer_roles`/`customer_feature_role_mapping` as "Reset" (per-tenant data, since the tables carry a `tenant_id` column) while correctly placing their `msp_id`-scoped twins `msp_roles`/`msp_feature_role_mapping` on "Preserve" — an asymmetric miscategorization that let #4272's reset delete the platform-default (`tenant_id IS NULL`) RBAC catalog rows along with genuine per-tenant rows. This exact finding is already filed and open as #4400 (filed by an earlier session working #4396) — not re-filing a duplicate; proceeding to write the repair migration #4400 itself calls for, which also satisfies #4384's ask. `integrity-check.ts:128`'s `DELETE FROM customer_roles` (the mechanism #4384's body speculated about) is ruled out: it's a symmetric delete of BOTH `msp_*` and `customer_*` inside a transaction that always rolls back (or aborts safely on crash) — it cannot explain an asymmetric wipe of customer-only tables while msp stays fully intact.
- 2026-09-16T17:39:55Z ✅ DONE — wrote and ran `lib/db/migrations/manual/2026-09-16-rbac-repair-customer-catalog-wipe-4384.sql` against local `DATABASE_URL`: reseeded the 12-rung ladder + customer-admin/billing/cap.team.manage/cap.changes.approve into `customer_roles` (4→16 rows), rebuilt all 5 `customer_feature_role_mapping` rows from scratch (0→5), re-converged all 11 real users' rung/cap.changes.approve/cap.team.manage/billing memberships (`customer_user_roles` 2→11). Fixed a real bug found during verification in `lib/db/src/rbac/parity-check.ts`: pass A's KNOWN-divergence lookup keyed only by real `user <id>`, so real MonitoringPending/PackPending/RetainerPending test accounts never matched the divergences already registered by rung name for pass B's synthetic shapes — added a role-name fallback. Also caught and fixed a gap in my own first migration draft: `marketplace.browse-full`'s allow set was missing `customer-admin` (#3629 section 2c appends it there too, easy to miss since it's grouped with team.manage/changes.approve in that file). **Live-verified:** `pnpm --filter @workspace/db run check-rbac-parity` → `8132 comparisons, ALL AGREED (11 registered fail-closed divergences)`. `user-role-sync.test.ts` + `retainer-no-consent-gating.test.ts` → 22/22 passed. `tsc --noEmit` on `lib/db` clean. **Honest permanent gap** (stated in the migration's own header, not papered over): `customer-admin` role MEMBERSHIP (who was hand-granted Customer Admin via AdminV2 before the reset) has no backing column anywhere and is unrecoverable — confirmed live that zero real (non-vitest) users hold any customer-admin-shaped account today, so nothing is silently lost, but if a real org had one assigned before 2026-09-15, Shane needs to redo it by hand. **Staging/Replit:** attempted the real, sanctioned `shaneapp://executeSql` protocol (not raw SSH) — it returned `ok:true` with `customer_roles=16, customer_feature_role_mapping=5, msp_roles=13`, i.e. exactly this session's own already-repaired local counts. That means the configured `apiBaseUrl` this pipe currently targets is local dev, not a distinct Staging/Replit endpoint — so this call did NOT independently confirm Staging/Replit's state, and I'm not claiming it did. The real basis for believing Staging/Replit is unaffected is #4272's own closing comment, which states plainly that its reset "was the local dev/testbed database only. Replit/Staging/Production were not touched." Reported honestly rather than assumed: a real protocol attempt was made, it landed on the same local DB, and the only actual evidence about Staging/Replit is #4272's own execution record. Filed **#4413** (sibling of #4370, `bug`, board "AI Batter Up"): `check-rbac-integrity`'s hardcoded scratch row ids (tenant 1, tenant 3, user 39) no longer exist post-#4272-reset — separate, pre-existing gap, does not block this repair (not one of the two required verification suites). Did not re-file #4400 (already open, exact same finding). `git status --porcelain` clean; branch verified merged into `origin/main` (5f850552).
