# #4083 — Requests and Support Chat: real MSP-console contract pack into docs/msp-console/

- **Status:** ✅ DONE 2026-09-14T16:28:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** cd415eff3 (pack), ac2cfe385 (post-rebase push HEAD), 147fa0932 (IN FLIGHT bookend)

## Log
- 2026-09-14T16:05:00Z ⏳ IN FLIGHT — relocating/reformatting §16-19 (MSP-console operator
  content) from `docs/portal/requests-and-support-chat-contract-pack.md` into
  `docs/msp-console/requests-and-support-chat-msp-console-contract-pack.md`, re-verifying
  every cited route/line against live code in `artifacts/api-server/src/routes/msp-support.ts`
  and `artifacts/api-server/src/lib/zoho-desk.ts` first, per #4083. Found one real drift so
  far: the source §16-18 auth citation (`requireRole("MSPOperator")` per retired `ROLE_ORDER`)
  is stale — live code now gates via `requireCapability("ladder.msp-operator")` (#2458/#2460
  RBAC-ladder migration), same effective floor (MSPOperator/MSPAdmin/PlatformAdmin pass) but a
  new 503 "unavailable" fail-closed path exists that the old pack never documented. Correcting
  this in the new pack rather than copying it verbatim.
- 2026-09-14T16:28:00Z ✅ DONE — `docs/msp-console/requests-and-support-chat-msp-console-contract-pack.md`
  committed and pushed to `origin/main` (`cd415eff3`, rebased onto `ac2cfe385`). All three
  routes (`GET /api/msp/support/requests`, `GET /api/msp/support/requests/:ticketId`,
  `POST /api/msp/support/requests/:ticketId/reply`) re-verified line-for-line against live
  `msp-support.ts`/`zoho-desk.ts` — every logic/response/line-number citation from the source
  addendum matched exactly except the auth-mechanism drift noted above, which is corrected in
  the new pack. Orphaned-endpoint check re-run live: still zero callers anywhere in
  `artifacts/msp-console`, `artifacts/portal`, `artifacts/msp-website`,
  `artifacts/shane-mccaw-consulting`, `artifacts/admin-panel`; no `Design/msp-console/` export
  exists yet — same honest "backend built, Design/wire not started" state. No new
  findings/bugs filed — the only real gap (no structured escalation-type field) was already
  filed under the source addendum, not new to this pass, so not re-filed. Read-only work: no
  schema/migration, no product code, no UI touched. `verify-branch-merged.mjs` confirms
  `agent/4083-q2758` (ac2cfe38) is an ancestor of `origin/main`. `git status --porcelain`
  clean.
