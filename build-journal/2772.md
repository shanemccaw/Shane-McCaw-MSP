# #2772 — No live baseline_action_templates for Teams member add/remove — rollback special case is dead code

- **Status:** ⏳ IN FLIGHT 2026-09-04T05:33:26Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T05:33:26Z ⏳ IN FLIGHT — Investigating whether to add real `action.add-team-member`/
  `action.remove-team-member` baseline_action_templates rows (issue's option 1) or remove the
  dead `teams.add_member`/`teams.remove_member` special case in `workflow-executor.ts` (option 2).
  Confirmed against local DATABASE_URL: `write_action_catalog` id 177 ("Teams | Add/remove
  member") and id 178 ("Teams | Add/remove owner") are already deliberately linked
  (`2026-09-03-write-action-catalog-template-links-2702.sql`, status `execution_ready`) to
  `action.add-group-member` / `action.add-group-owner` — the same group-membership templates
  Groups uses — with that migration's own comment explicitly stating "a Microsoft Team's
  membership IS group membership under the hood (a Team is backed by an M365 Group), so the
  same Graph call genuinely serves both catalog rows." `action.add-group-member` was already
  re-promoted reversible (paired with `microrem.remove-stale-group-member`) by #2703, including
  the memberId/userId alias special case in `workflow-executor.ts`. So Teams member add/remove
  is NOT actually missing a live template — it already works today through the real, live
  group-membership pairing under a different id. The `teams.add_member`/`teams.remove_member`
  ids the dead special case names were seeded by an earlier, never-actually-applied dotted-id
  naming scheme (`2026-07-20-launch-control-phase3-templates.sql` — its own
  `simulator_migration_runs` row says it ran 2026-08-06, but 0/102 live rows use dotted ids;
  same phantom-migration pattern #2703 already found for the July 21 rollback file) that was
  superseded by the current `action.*`/`microrem.*` catalog. Proceeding with option 2: remove
  the unreachable special case, since inventing new `action.add-team-member` rows would just
  duplicate a Graph call the live catalog already deliberately routes through group membership.
