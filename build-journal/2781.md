# #2781 — context gauge never updates for the primary docked chat tab (ClaudeWebView not registered in _contextMeters)

- **Status:** ✅ DONE 2026-09-04T00:58:39Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** b7e0eb508 (fix), 6abcb95f6 (IN FLIGHT bookend)

## Log
- 2026-09-04T00:53:30Z ⏳ IN FLIGHT — investigating CreateChatContextWrapper's ClaudeWebView early-return and UpdateContextMeter's silent no-op in MainWindow.xaml.cs; checking git blame to determine if pre-#2727 or a #2727 regression.
- 2026-09-04T00:58:39Z ✅ DONE — `git blame`/`git log -L` on the `wv == ClaudeWebView` line confirmed this predates #2727: it dates back to b4bbb1694 (#1424, the original context-meter implementation) and has never registered a meterState for ClaudeWebView. NOT a #2727 regression — #2727 only made it newly visible by making Band 1 the primary indicator. Root cause was actually two layers: (1) the early-return itself, and (2) CreateChatContextWrapper was never even *called* with ClaudeWebView from anywhere in the app — BuildChatWebView always constructs a fresh WebView2 per chat tab; ClaudeWebView is the one statically-declared shared primary WebView2, initialized via InitializeClaudeTabAsync, which never routed through the wrapper function at all.
  Fix: (a) added an explicit `CreateChatContextWrapper(ClaudeWebView)` call in `InitializeClaudeTabAsync` (MainWindow.xaml.cs) so the primary tab gets the same headless meterState every other chat tab already gets — safe since #2727 made the whole banner/progressBar headless/parentless for every wv; (b) removed the now-unnecessary `if (wv == ClaudeWebView) return wv;` special case in `CreateChatContextWrapper`; (c) `UpdateContextMeter`'s silent `return` on a missing meterState now logs once per distinct missing `wv` via `ActivityLog` (new `_loggedMissingContextMeterFor` HashSet, cleared in `DisposeWebView2Descendants` alongside `_contextMeters`) so this class of bug can't silently degrade unnoticed again.
  Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Not live-UI-verified against a running BuildConsole instance per the standing "never launch BuildConsole.exe for verification" rule (single-instance mutex risk of couriering into Shane's live session) — verified by code path tracing + git blame + clean build instead. No findings filed; no test manifest required (BuildConsole/WPF is explicitly out of test-manifest scope). Working tree clean; merged into origin/main (verify-branch-merged.mjs: OK).
