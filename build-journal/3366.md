# #3366 — Alerts (MSP Console): build real acknowledge/dismiss actions

- **Status:** ✅ DONE 2026-09-10T01:05:00Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 453c10b62 (real work), bdaf342e8 (IN FLIGHT bookend) — both on `main`

## Log
- 2026-09-09T23:59:00Z ⏳ IN FLIGHT — Auditing `GET /api/msp/alerts`'s two source
  tables (`policy_rule_incidents`, `msp_diagnostic_findings`) for an existing real
  resolution mechanism before building anything.
- 2026-09-10T01:05:00Z ✅ DONE — Real audit found:
  - `policy_rule_incidents` already has a real resolution mechanism: the Signal
    Policy Engine (`policy-engine.ts`, `evaluateAllPolicies`) auto-resolves
    `status: open→resolved` the moment a rule's condition clears. No manual,
    operator-triggered transition existed.
  - `msp_diagnostic_findings` genuinely has **no** resolution mechanism anywhere
    in the codebase (immutable per-run finding rows, no status column). Flagged
    as a separate finding rather than invented ad hoc, per this issue's own
    instruction — filed as **#3399**, sub-issue of **#1571**, board status set
    to "AI Batter Up".

  Built: `POST /api/msp/alerts/:alertId/acknowledge`
  (`artifacts/api-server/src/routes/msp-alerts.ts`) drives the SAME real
  `policy_rule_incidents.status` column manually (no parallel status, no
  parallel table). Added `resolved_by_user_id` (nullable — NULL = engine
  auto-resolve, non-null = the MSP staff user who manually acknowledged) via
  `lib/db/migrations/manual/2026-09-09-policy-rule-incidents-resolved-by-user-3366.sql`,
  **run myself against local Postgres** (`ALTER TABLE ... ADD COLUMN IF NOT
  EXISTS resolved_by_user_id integer;` — verified via `\d policy_rule_incidents`
  before and after). Route is ownership-checked (mspId + per-staff customer
  scope via `isCustomerBlockedByStaffScope`), idempotent for an
  already-resolved incident, and audit-logged
  (`msp_alerts.incident.acknowledged`). A `finding-*` alert id returns a clear
  `400 VALIDATION_ERROR` pointing at #3399 rather than silently no-op'ing or
  inventing a mechanism.

  No frontend consumer exists yet for this endpoint (no `artifacts/msp-console`
  built yet — #1571's own body says so), so no test-manifest/UI wiring applies;
  scope was backend-only per the issue body ("Once built, the contract pack
  needs real extraction — separate follow-up, not part of this issue").

  **Verification:** 18 unit tests added/updated in
  `artifacts/api-server/src/routes/msp-alerts.test.ts` (auth/role gates, real
  status transition + audit log call, idempotency, cross-MSP 404, staff-scope
  404, finding-id 400, garbage-id 400) — all passing. `tsc --noEmit` clean
  across `artifacts/api-server` and `lib/db` (rebuilt `lib/db`'s composite
  `dist/` — the referenced-project declarations were stale from before this
  session's schema edit). Full round-trip also verified directly against the
  real local Postgres database in one transaction (insert open incident →
  simulate the exact acknowledge UPDATE → confirm `resolved`/`resolved_by_user_id`
  → idempotent re-check), then rolled back — no residue left in the table
  (confirmed via `select count(*)`).

  Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (exit 0) before writing this.
  `git status --porcelain` clean.
