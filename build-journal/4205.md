# #4205 — Add missing theme resources (scrim/green-wash/shadow) + retarget call sites

- **Status:** ⏳ IN FLIGHT 2026-09-15T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — pulled #4205 body + comments, confirmed the 5 raw-hex
  literals named in the issue against current `Themes/Colors.xaml`/`Themes/DarkTheme.xaml`.
  Added `ScrimBrush` (#95000000, ~58%) + `ScrimLightBrush` (#40000000, ~25%) to
  `Themes/Colors.xaml`; added `GreenWashMediumBrush` (#33A6E3A1, 20% tier) to
  `Themes/DarkTheme.xaml` next to `GreenBrush`. Documented `DropShadowEffect
  Color="#000000"` as an accepted, permanent exception (shadows are colorless by
  convention; `DarkTheme.xaml`'s own `SoftRowShadow`/`SoftCardShadow` already hardcode it)
  rather than inventing a themed substitute. Retargeted all real call sites:
  `MainWindow.xaml` (TopShaneAppIndicator green wash, TabSwitcherOverlay scrim,
  DockGuideOverlay light scrim), `Controls/LeftSidebar.xaml` (ShaneAppIndicator green
  wash), `RegionSelectOverlayWindow.xaml` (Window Background scrim — exact byte-identical
  value, so this resolves rather than contradicts the #4182 "no inexact substitute" note
  left in that file; left that file's separate `#80000000` hint-chip literal untouched,
  out of #4205's named-tier scope, still tracked under #4182).
