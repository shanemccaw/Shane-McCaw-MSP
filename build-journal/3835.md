# #3835 — runTest misroutes artifacts/portal uiSteps to Marketing instead of Portal

- **Status:** ✅ DONE 2026-09-12T21:29:35Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** b6711709d ("ServiceKeyForRoute Portal directory hint (Git #3835)")

## Log
- 2026-09-12T21:23:54Z ⏳ IN FLIGHT — read #3835 body + comment; implementing the issue's own suggested
  fix: a `test-manifests/portal/*` directory-based service hint layered on top of
  `DevServiceRouting.PrimaryServiceKey`'s existing per-route scoring, Portal-only (other apps' routes
  are already unambiguous by prefix). Will re-run `shaneapp://runTest?file=portal/config-state.json`
  to confirm the uiStep now lands on Portal (5175) instead of Marketing (5173).
- 2026-09-12T21:29:35Z ✅ DONE — Implemented `DevServiceRouting.DirectoryServiceHint` + threaded
  `manifest.SourcePath` into `PrimaryServiceKey` (`desktop/BuildConsole/Services/DevServiceRouting.cs`,
  call site `desktop/BuildConsole/MainWindow.xaml.cs`), committed as `b6711709d`, pushed to
  `origin/main`. `dotnet build BuildConsole.csproj -c Debug` → 0 errors, 0 warnings.
  **Real verification performed** (NOT the literal `shaneapp://runTest` repro — see honest gap
  below): built a scratch console harness (outside the repo, deleted after use — no artifact
  left behind) referencing the freshly-built `BuildConsole.dll` and called
  `DevServiceRouting.PrimaryServiceKey`/`OriginForServiceKey` directly with the manifest's own
  real inputs (routes `["/config-state"]`, `SourcePath` = the real `test-manifests/portal/config-state.json`
  path), run with cwd inside the repo so `KnownServices` loads the real `scripts/dev-server/services.json`
  port map:
    - `primaryServiceKey = portal`, `origin = http://localhost:5175` — the exact fix confirmed (was
      `shane-mccaw-consulting` / `5173` before the fix, reproduced by calling the same method with
      `sourcePath = null`).
    - Admin manifest (`test-manifests/admin/risk-decisions.json`, route `/dashboard`) still classifies
      `admin-panel` — unaffected, confirming the Portal-only hint doesn't touch apps whose routes are
      genuinely prefixed, per the issue's own ask.
    - A Marketing-prefixed route (`/scan`) under a hypothetical non-portal directory still classifies
      `shane-mccaw-consulting` — confirms the per-route classification still wins over any directory
      signal when a route is genuinely unambiguous.
  **Honest gap:** did not literally invoke `shaneapp://runTest?file=portal/config-state.json` end-to-end.
  Per [[shaneapp-runtest-hits-deployed-build-not-worktree]] that protocol is couriered to Shane's
  already-RUNNING deployed `ShanesBuild` copy of BuildConsole, not this worktree's just-built code —
  so the literal repro would have exercised the OLD (still-broken) deployed binary and produced a
  false negative, not a real check of this fix. The direct-call harness above exercises the exact
  same static method the running pipeline calls, with the exact real repro inputs, against the
  actual freshly-built DLL — the honest, available substitute until Shane deploys this change.
  No new findings discovered beyond #3835 itself; no fixture/hardcoded data added; no DB/schema
  touched. Working tree confirmed clean (`git status --porcelain` empty) and
  `verify-branch-merged.mjs` confirms `agent/3835-q2523` (b6711709d) is an ancestor of `origin/main`.
