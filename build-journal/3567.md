# #3567 — Home dashboard milestone progress bar undercounts (bt_issue_mirror fix)

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:12:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-11T00:12:00Z ⏳ IN FLIGHT — investigated HomeEtaProjectionService /
  GitHubIssueTimeSeriesService / GitHubIssueMirror. Confirmed root cause via direct
  psql: `bt_issue_mirror` has only 986 rows total (504 open/482 closed) — it's built
  from an OPEN-issues walk + incremental mark-closed + a partial closed backfill, not
  a complete historical mirror. `bt_milestone_mirror` already carries the REAL
  GitHub milestone object counts (milestone #5: 346 open + 1597 closed = 1943 total)
  and `GitHubIssueTimeSeriesService.ResolveActiveMilestoneAsync` already reads those
  accurate counts into `ActiveMilestone.OpenIssues/ClosedIssues` — but
  `HomeEtaProjectionService.ComputeAsync`/`BuildRow` discards them and instead uses
  `IssueTimeSeries.TotalIssues/CurrentClosed/CurrentOpen`, which are derived from the
  incomplete `bt_issue_mirror`-scoped issue set. That's the actual undercount.
  Implementing: wire the milestone row's Total/Closed/Open (and the ETA projection's
  `remaining` input) from `ActiveMilestone`'s already-accurate bt_milestone_mirror-backed
  counts, while keeping the daily cumulative-closed series (pace fit) sourced from the
  local mirror exactly as it already is (Git #3512) — no live GitHub call added.
