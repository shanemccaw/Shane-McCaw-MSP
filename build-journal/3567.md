# #3567 — Home dashboard milestone progress bar undercounts (bt_issue_mirror fix)

- **Status:** ✅ DONE 2026-09-11T00:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** ef638da28 (real fix), 76415a3fa (IN FLIGHT bookend)

## Log
- 2026-09-11T00:12:00Z ⏳ IN FLIGHT — investigated HomeEtaProjectionService /
  GitHubIssueTimeSeriesService / GitHubIssueMirror. Confirmed root cause via direct
  psql: `bt_issue_mirror` has only 986 rows total (504 open/482 closed) — it's built
  from an OPEN-issues walk + incremental mark-closed + a partial closed backfill, not
  a complete historical mirror. `bt_milestone_mirror` already carries the REAL
  GitHub milestone object counts (milestone #5: 346 open + 1597 closed = 1943 total)
  and `GitHubIssueTimeSeriesService.ResolveActiveMilestoneAsync` already reads those
  accurate counts into `ActiveMilestone.OpenIssues/ClosedIssues` — but
  `HomeEtaProjectionService.ComputeAsync`/`BuildRow` discards them and instead uses
  `IssueTimeSeries.TotalIssues/CurrentClosed/CurrentOpen`, which are derived from the
  incomplete `bt_issue_mirror`-scoped issue set. That's the actual undercount.
  Implementing: wire the milestone row's Total/Closed/Open (and the ETA projection's
  `remaining` input) from `ActiveMilestone`'s already-accurate bt_milestone_mirror-backed
  counts, while keeping the daily cumulative-closed series (pace fit) sourced from the
  local mirror exactly as it already is (Git #3512) — no live GitHub call added.
- 2026-09-11T00:35:00Z ✅ DONE — `HomeEtaProjectionService.BuildRow` now accepts optional
  `totalOverride`/`closedOverride`/`openOverride` params; `ComputeAsync`'s milestone row passes
  `active.TotalIssues/ClosedIssues/OpenIssues` (already `bt_milestone_mirror`-backed via
  `ResolveActiveMilestoneAsync`, confirmed real: milestone #5 = 346 open + 1597 closed = 1943
  total via psql against `bt_milestone_mirror`) instead of the series-derived counts built over
  the incomplete `bt_issue_mirror` (986 rows repo-wide). The corrected `open` count also now
  feeds `IssueEtaProjection.Project`'s `remaining` argument, so the projected ETA date itself is
  computed against the real remaining-issue count, not the undercounted one. Per-Epic rows are
  unchanged (no per-Epic mirror table exists; left as-is deliberately). Fail-closed behavior
  unchanged (`Available=false`/honest `Reason` paths untouched). Build verified clean:
  `dotnet build BuildConsole.csproj` — 0 errors, only pre-existing unrelated warnings.
  Commit: ef638da28. Branch verified merged into origin/main
  (`node scripts/dev-server/verify-branch-merged.mjs` — OK). `git status --porcelain` clean.
  Filed a distinct sibling finding, #3577 (parented under #1202, board status "AI Batter Up"):
  the Home dashboard BURNDOWN CHART (`HomeView.xaml.cs` `RenderBurndown`) has the same
  undercount, but is NOT the same fix — it draws its own curve directly off the (incomplete)
  series `Points`, so swapping only the summary-line numbers the way this fix did for the ETA
  row would visually mismatch the drawn chart. Needs its own product/architecture call.
