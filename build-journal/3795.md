# #3795 — Status bar: simplify to Current Session only, click opens a real popup with Weekly · all models (both accounts)

- **Status:** ✅ DONE 2026-09-12T06:38:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** da41a2cb5 (rebased to d2b83be34 on main)

## Log
- 2026-09-12T06:31:39Z ⏳ IN FLIGHT — removing the two permanent Weekly status-bar segments (`WeeklyUsageStatusText`/`SecondaryWeeklyUsageStatusText`) from `MainWindow.xaml`, making the Current Session segment clickable, and adding a real `Popup` (same idiom as `ShelfPopup`/`WebToolsPopup`) that shows Current session + Weekly · all models for both accounts, reusing `ClaudeUsageMeterService`'s existing `DisplayText`/`ToolTip` strings verbatim (no reformatting) per #2002.
- 2026-09-12T06:38:00Z ✅ DONE — `MainWindow.xaml`: removed the two always-visible Weekly (Primary/Secondary) status-bar segments + their refresh buttons/separators, renumbered the remaining status-bar Grid columns, made `UsageStatusText` clickable (`Cursor="Hand"` + `MouseLeftButtonUp`), and added `UsagePopup` — a real WPF `Popup` (`StaysOpen="False"`, `AllowsTransparency`, `PopupAnimation="Fade"`, same idiom as this file's existing `ShelfPopup`/`WebToolsPopup`) anchored to the Current Session item, showing Current session + Weekly · all models (Primary) + Weekly · all models (Secondary) as three dot+text rows. `MainWindow.xaml.cs`: `UsageStatusText_Click` toggles `UsagePopup.IsOpen`; `UsageMeter_StatusChanged` now paints the popup's three rows with the exact same `status.DisplayText`/`WeeklyDisplayText`/`SecondaryWeeklyDisplayText` + `ToolTip` strings and dot-color logic the old status-bar segments used (no reformatting — the #2002 fixes for real-primary-regardless-of-toggle and no-guessed-reset-countdown carry over unchanged); removed the now-dead `BtnWeeklyUsageRefresh_Click` handler and the disabled-button bookkeeping for the two removed refresh buttons (the single remaining `BtnUsageRefresh` already refreshes both accounts in one poll cycle per #1437). Verified: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — Build succeeded, 0 errors (10 pre-existing unrelated warnings). No test manifest — desktop/BuildConsole is explicitly out of scope for test manifests. Branch verified merged into `origin/main` via `verify-branch-merged.mjs` (OK, d2b83be3). `git status --porcelain` clean. No findings filed — no genuine bug/gap discovered outside this issue's own scope.
