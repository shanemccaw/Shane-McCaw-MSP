# #3655 — Feature: "I'm Working" Time Tracking Toggle (MyArchitect)

- **Status:** ✅ DONE 2026-09-15T23:08:00Z
- **Scope:** buildconsole (MyArchitect desktop app — private repo shanemccaw/MyArchitect)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect `36baf44` ("I'm Working" time tracking toggle, #3655) on
  `main`. Bookend commits (this file, and shanemccaw/MyArchitect's own local
  `build-journal/3655.md`) are separate.

## Log
- 2026-09-15T22:53:56Z ⏳ IN FLIGHT — pulled #3655's body + the `BUILD:` dispatch comment.
  MyArchitect's real app source no longer lives in this monorepo (`desktop/MyArchitect/`
  doesn't exist here) — it was extracted to the private repo `shanemccaw/MyArchitect` by
  #4305 on 2026-09-15, confirmed via `gh repo view` (pushed 2026-09-15T22:31:47Z) and
  `build-journal/4284.md`'s own record of the same move. Cloned `shanemccaw/MyArchitect`
  locally to do the real work there; this monorepo bookend is the standing convention, the
  actual commits land on `shanemccaw/MyArchitect`'s own `main`.

  Confirmed the real prerequisite gate is clear: #3540 (TenantService fixture data blocking
  every customer-scoped call) is CLOSED/`complete` — `TenantService.cs` now loads real
  `tenants.id`-backed customers from `GET /api/msp/v1/msps/:mspId/customers`, and
  `MainWindow.xaml.cs` already has a real `TryResolveRetainerCustomerId`/
  `TryResolveLaunchControlScope` resolution path used throughout. No new backend needed —
  reusing `IAdminRetainerService.LogUnscopedHoursAsync` (`POST
  /api/admin/retainer/:customerId/unscoped`), the exact same real endpoint #3464 already
  wired for the manual "Log Ad-Hoc Hours" workspace.

  **Tenant-switch-mid-timer decision (stated explicitly, per the issue's own requirement):
  AUTO-STOP and log against the tenant that was active when the timer started** — not
  blocking the switch. MyArchitect is a multi-tenant operator cockpit where switching
  tenants is routine, uninterrupted navigation (UI_RULES.md's own governing rule: "a
  one-off task should not move you off what you were doing"); blocking the switch would
  trap the operator on whichever tenant happened to have a timer running. Auto-stop opens
  the same non-blocking, always-docked record workspace (UI_RULES.md §3) the manual Stop
  path uses, pre-filled for the ORIGINAL tenant's elapsed time — the operator reviews and
  confirms at their own pace, already on the new tenant.

  Building: status-bar "I'm Working" toggle (title bar QAT is a closed, documented list per
  UI_RULES.md §1 — Tenant Switcher/Palette/Undo/Log Viewer only; the status bar is the
  shell's own documented segment area and already hosts the sibling contract-hours meter
  this feature logs into, so it's the better real fit), live elapsed display, Stop opens the
  real log-entry workspace, and wiring the existing #3474 contract-hours meter to refresh
  immediately after any ad-hoc entry is logged (was missing entirely — `SubmitAdHocHoursAsync`
  never refreshed it, so the manual "Log Ad-Hoc Hours" path had the same stale-meter gap
  this issue's checklist calls out).

- 2026-09-15T23:08:00Z ✅ DONE — Real commit **`36baf44`** on `shanemccaw/MyArchitect` main
  (rebased once against a concurrent #3656 bookend commit; retried and pushed clean).
  Shipped, against the checklist:
  - **Start/Stop toggle** — new `IsWorkingButton` status-bar segment (next to the #3474
    contract-hours meter it feeds). Idle: `PlayCircle24` + "I'm Working". Running:
    muted `StatusWarningBrush` `PauseCircle24` + live `hh:mm:ss` elapsed, ticking every
    second via a `DispatcherTimer`. No inline color/font literals — existing theme keys
    only (UI_RULES.md §10).
  - **On Stop** — opens the same non-blocking, always-docked record workspace
    (`RecordWorkspaceSpec`/`_shellRegistry.OpenRecord`) `OpenLogAdHocHoursRecord` (#3464)
    already uses, pre-filled with the real elapsed hours (editable), for item/pillar/
    finding/outcome. Confirm-armed "Log Hours" POSTs via the existing
    `IAdminRetainerService.LogUnscopedHoursAsync` — no new backend, no fixture data.
  - **Tenant-switch-mid-timer** — **auto-stop and log against the tenant that was active
    when the timer started**, chosen explicitly over blocking the switch (full reasoning
    above). Wired into `OnCurrentTenantChanged`: compares the new tenant's `CustomerId`
    against the running session's captured `CustomerId`; if different, stops the timer
    and opens the Stop workspace pre-filled for the *original* tenant before the switch's
    own handling (portal tab activation, contract-hours refresh) proceeds.
  - **#3474 hour-utilization bar wiring** — fixed the real, pre-existing gap: neither the
    timer's Stop path nor the original manual "Log Ad-Hoc Hours" ribbon action ever
    refreshed `ContractHoursPanel` after a successful POST. `SubmitAdHocHoursAsync` now
    calls `RefreshContractHoursAsync()` after a successful log when the entry's
    customerId matches the currently-active tenant — so a logged session (from either
    entry point) updates the meter live, no restart or tenant re-switch required.
  - Sign-in/no-customer-selected guard reuses the same real `TryResolveLaunchControlScope`
    (#3540) resolution every other customer-scoped action in this file uses — Start
    refuses (with a console message) rather than starting a timer with no real
    destination to log against.

  **GEMINI.md deviation, stated explicitly:** `shanemccaw/MyArchitect`'s own GEMINI.md
  says to bookend in that repo's local `build-journal/<N>.md` too. #4284 (same day,
  landed just before this session) did not do this and only bookended on the monorepo.
  A concurrent session on #3656 (observed mid-session via a rebase conflict on push) *did*
  write a local `build-journal/3656.md` there — so that convention is genuinely live, not
  stale. Followed #3656's real precedent: added `shanemccaw/MyArchitect`'s own
  `build-journal/3655.md` (IN FLIGHT then DONE, two separate commits, real commit hash
  recorded) alongside this monorepo bookend.

  **Verification:** `dotnet build MyArchitect.csproj -c Debug` — 0 Warning(s), 0 Error(s),
  run twice (once mid-build, once against the final pushed `36baf44` tree). No UI test
  harness exists for this WPF app (test-manifests/ scope is `artifacts/portal/` and
  `artifacts/shane-mccaw-consulting/` only per CLAUDE.md — desktop apps are explicitly
  out of scope). No findings filed — no new bug/gap discovered beyond the #3474 refresh
  gap, which was fixed in-session rather than filed separately (a small, directly related
  fix to code this issue's own checklist named, not a new independent finding).

  **Working tree:** this monorepo worktree (`C:\wt\3655-q2981`) is clean except for one
  leftover: a scratch clone directory (`.myarchitect-clone/`, used to work against the
  private repo directly) is now empty but could not be deleted — Windows reports it
  locked by another process even after `dotnet build-server shutdown` and killing the
  identified leftover `dotnet.exe` PID. It is untracked, empty, and was never part of
  this repo's git history, so it cannot produce a merge conflict on `git pull`. Naming it
  here rather than silently leaving it. `git status --porcelain` in `shanemccaw/MyArchitect`
  itself is clean.
