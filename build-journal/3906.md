# #3906 — Focus bar's honest "no confident ETA" reason withheld to tooltip; add Calculating…/refresh

- **Status:** ✅ DONE 2026-09-13T01:43:17Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** fbb189dce (merged to main as 34eb46c95)

## Log
- 2026-09-13T01:36:43Z ⏳ IN FLIGHT — investigated FocusModeBar.xaml.cs/FocusModeService.cs; Part 1 makes `p.EtaReason` visible in `EtaText.Text` instead of tooltip-only. Part 2 adds a "Calculating…" state (reusing `FocusProgress.HasRealCounts`, the same cold-board signal the progress bar already uses) plus a manual refresh button wired to `LeftSidebar.PopulateGitTrackerBoardAsync(forceFresh: true)` — the real existing method that feeds `FocusModeService.UpdateBoardSnapshot` → `RecomputeGame` → `BuildProgress`/ETA, same as the Home dashboard's own `BtnRefreshEta` pattern. About to implement.
- 2026-09-13T01:43:17Z ✅ DONE — Part 1: `FocusModeBar.Refresh()`'s ETA block now sets `EtaText.Text = p.EtaReason` (visible) instead of `""` with the reason tooltip-only; tooltip kept as a mirror for hover detail. Part 2: added `_etaRefreshInProgress` + a new `EtaRefreshBtn` (⟳, IconButton style, next to EtaText) whose click handler raises a new `FocusModeBar.EtaRefreshRequested` (`Func<Task>?`) event; `MainWindow.FocusMode.cs` wires it to `LeftSidebar.PopulateGitTrackerBoardAsync(forceFresh: true)` — the SAME real manual-Git-refresh method already used elsewhere (no second fetch path invented), which flows through `FocusModeService.UpdateBoardSnapshot` → `RecomputeGame` → `BuildProgress` and fires `StateChanged` on completion. `EtaText` shows "Calculating…" whenever `!p.HasRealCounts` (cold-board, matches the progress bar's own existing signal) OR `_etaRefreshInProgress` is true (a manual refresh actually in flight), covering both the "never computed this session" and "recomputing now" cases from the issue body. Confident-ETA and "done 🎉" cases are unchanged. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Per CLAUDE.md, BuildConsole (WPF) is out of scope for test manifests and never launched directly for verification — a clean build is the sanctioned verification here. No genuine findings/bugs discovered outside this issue's scope; nothing filed. `git status --porcelain` clean before finishing. Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0) before writing DONE. Commit fbb189dce, pushed/rebased onto origin/main as 34eb46c95.
