# Build Journal — #4208

- **Status:** ✅ DONE 2026-09-15T00:35:00Z
- **Scope:** platform

## Log

- 2026-09-15T00:10:00Z — Investigated pre-existing failing test `rbac-capability.test.ts`'s
  #3360 case: `RetainerNoConsent` gets `allow` for `customer:team.manage`, test expects `deny`.
  Confirmed against `lib/db/src/rbac/parity-check.ts`'s `KNOWN_FAIL_CLOSED_DIVERGENCES`: this is
  a documented, deliberate divergence from #3974 — the live DB (seeded by
  `2026-09-14-rbac-retainer-noconsent-gating-3974.sql`) correctly DENIES RetainerNoConsent on
  `team.manage`/`changes.approve`, and RetainerConsented on `team.manage`. The frozen
  `LEGACY_CAPABILITY_RULES` transcription in `lib/db/src/rbac/legacy-ladder.ts` predates both new
  rungs and cannot know about them (its `isCustomerTier` catchall allows what it doesn't name) —
  that transcription is intentionally NOT touched (it is `parity-check.ts`'s pinned oracle for the
  ORIGINAL seven rungs).
- The actually-stale piece is the unit-test fixture
  `artifacts/api-server/src/test-setup/rbac-capability-fixture.ts`, which derived its mock
  `customer_feature_role_mapping` rows purely from `LEGACY_CAPABILITY_RULES.decide()` with no
  accounting for the known #3974 overrides `parity-check.ts` already documents. Fixed by applying
  the same three known deny overrides in the fixture's `mappingRows()`.
- 2026-09-15T00:35:00Z — ✅ DONE — Commit `60c36134d` (on `origin/main`; verified merged via
  `verify-branch-merged.mjs`). Fix: `artifacts/api-server/src/test-setup/rbac-capability-fixture.ts`
  now bakes in the three `#3974` known deny overrides (RetainerNoConsent×team.manage,
  RetainerNoConsent×changes.approve, RetainerConsented×team.manage) that `parity-check.ts`'s
  `KNOWN_FAIL_CLOSED_DIVERGENCES` already documents as deliberate — `legacy-ladder.ts`'s frozen
  `LEGACY_CAPABILITY_RULES` transcription was correctly left untouched (it predates both new
  rungs and is `parity-check.ts`'s pinned oracle for the original seven). No conflict between
  #3974 and the test's expectation once traced to the real cause — #3974's DB seed was always
  authoritative; only the unit-test fixture had not been taught the same override.
  Verification: `rbac-capability.test.ts` 15/15 pass (was 1 fail/25 pass);
  `marketplace-catalog-scope.test.ts`, `requireAccess.test.ts`,
  `portal-billing-capability-gates.test.ts` (the other suites reading this fixture) all pass;
  `tsc --noEmit` in `artifacts/api-server` clean. No schema/migration/DB change needed — this
  was a test-fixture-only fix. No findings filed — no genuine new bug found beyond the one
  #4208 was already opened to resolve. `git status --porcelain` clean.
