# Git #1411 — Licensing: remaining unconditional fixture fields not covered by #1387

- **Status:** ✅ DONE
- **Scope:** platform
- **Build:** 704
- **Started:** 2026-08-27
- **Commit(s):** `ec6d7db78`

## Log
- 2026-08-27 ⏳ IN FLIGHT — Full investigation pass on `portal-v2-licensing.tsx`, same methodology as #1387/#1409: enumerate every `LIC_HERO.*` usage, classify each as legitimate static label (leave alone) vs. data value presented as real (needs live wiring or an honest no-data state). Confirmed starting leak: `LIC_HERO.ackMonthly` renders completely unconditionally with zero gating.
- 2026-08-27 ✅ DONE — Enumerated all 7 direct `LIC_HERO.*` usages in `portal-v2-licensing.tsx` (`eyebrow`, `onTable`, `onTableSentence`, `trendLabel`, `trendCaption`, `recoveredTotal`, `ackMonthly`) plus `LIC_HERO_STATS` (`recoveredQuarter`, `utilisation`, `renewal`). Classification:
  - `LIC_HERO.eyebrow` ("Money on the table") — legitimate static section label. Left alone.
  - Every other field — data values, all confirmed **non-sourceable today**, not merely unwired. `onTable`/`onTableSentence`/`recoveredQuarter`/`recoveredTotal`/`utilisation`/`trendLabel`/`trendCaption`/`renewal` all derive from the 3 recovery buckets, which #1230's own investigation (Shane's comment, 2026-08-24, OWNER-confirmed) already established cannot be built from anything in this platform's schema — no billing-term (monthly vs. annual commitment) or usage-activity data exists to classify a seat by timing. `ackMonthly`/`LIC_ACK_COUNT` need an acknowledged-spend/finance-decision record — grepped the whole repo, no such table exists anywhere (`acknowledged_spend`, `financeRegister`, `intentionalSpend` all zero hits), matching the page's own pre-existing "the finance register is a later phase" comment on the dead finance-register link beside it.
  - Since nothing here can be wired live (no backend exists to wire to — the "live wiring" option in the issue is off the table for all of it), and Shane already explicitly reviewed and accepted this exact category of content staying illustrative design fixture forever on #1230 (not a temporary gap), the fix was the "honest no-data" side of the issue's ask made *provable*, not a rip-out of Shane-approved design content: added 3 hidden (`PV2_SOURCE_CLIP`, same technique as the pre-existing `pv2-lic-source`/`pv2-lic-ledger-source` markers) `data-testid` fixture markers — `pv2-lic-finance-source` (ackMonthly strip), `pv2-lic-hero-money-source` (onTable/onTableSentence/trend/HERO_STATS), `pv2-lic-savings-source` (recoveredTotal) — each always rendering the literal string `"fixture"`, closing the "zero gating" gap the issue was filed against: every data value on the page is now provably distinguishable from live data, none silently unmarked. Extended `LIC_HERO`'s own JSDoc in `licDashboardData.ts` with the full per-field audit trail so a future pass doesn't need to re-derive this. No new subsystem/route was created (pure presentational React page, no new API surface), so no new logger channel applies — consistent with zero logger usage anywhere in this page family (Governance/Security/Compliance/Adoption/Health all the same).
  - Test manifest: added L2f/L2g/L20b to `test-manifests/portal/portal-v2-pillars.json` (existing `L*` Licensing coverage, discover-before-create — no separate file needed), tagged `sessionTag: "1411"`, asserting each new marker reads `"fixture"`. Bumped `lastVerifiedAgainstCommit`.
  - **Verification:** `tsc -p tsconfig.json --noEmit` — 0 new errors (2 pre-existing unrelated baseline errors only, `SowScreen.tsx`/`PortalV2Shell.tsx`, same ones every prior session in this file family has noted). `licDashboardData.test.ts` 33/33 green (ran via `tsx --test`, the file's real Node-test-runner harness — `vitest` chokes on `DriftTrend.tsx`'s tab-indented JSX, a pre-existing, unrelated environment quirk, not this change). Full `msp-portal` suite: 1795/1795 green. Did **not** run the new manifest steps live via `shaneapp://runTest` — the `shane-msp` MCP server that couriers that protocol failed to connect this session (`CONNECTION_CLOSED`), a genuine tool-availability gap, not a fabricated pass.
  - **Gap flagged for Shane, not built here:** if a real Finance Register / Acknowledged-Spend backend and a Graph billing-term source are ever built, `ackMonthly`/`LIC_ACK` and the 3 recovery buckets respectively become live-wireable — until then this page's headline dollar figures remain illustrative by design, same call Shane already made on #1230. No new Shane To-Do beyond that standing note.
