# #2518 — policy_decisions.reviewDueAt: constrain reviewCadence to fixed enum, compute due date (Option A)

- **Status:** ✅ DONE 2026-09-03T11:07:14Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** acec199d158af6a854983806320a29f065454598 (on `main`)

## Log
- 2026-09-03T10:58:04Z ⏳ IN FLIGHT — constrain `reviewCadence` to enum (Monthly/Quarterly/Semi-Annual/Annual/Biennial), compute `reviewDueAt` from cadence + `createdAt` at create time, per #2518's decided Option A.
- 2026-09-03T11:07:14Z ✅ DONE — Shipped:
  - `lib/db/src/schema/msp.ts` — added `REVIEW_CADENCES` const/type, constrained `reviewCadence` column to `text({ enum: REVIEW_CADENCES })`, updated schema comments (the "no fixed cadence vocabulary" / "nothing computes reviewDueAt yet" claims were now false and rewritten).
  - `artifacts/api-server/src/routes/portal-policy-decisions.ts` — create-schema's `reviewCadence` field is now `z.enum(REVIEW_CADENCES)` (400 on anything outside the set); added `computeReviewDueAt(cadence, anchor)` using `setUTCMonth` (native month-overflow handling, not manual day math); `reviewDueAt` is computed at create time from cadence + the `signedAt` instant (also written explicitly to `createdAt` so the anchor used and the row's own `createdAt` are byte-identical, not a few ms apart via `defaultNow()`). NULL for a dependency-based (clearance) row, unchanged.
  - `artifacts/api-server/src/lib/alert-engine.ts` — `advancePolicyReviewClock`'s comment updated; `review_due_at` is no longer permanently NULL on every row.
  - `lib/db/migrations/manual/2026-09-03-policy-decision-review-cadence-enum-2518.sql` — ran against local DB (documentation + run-tracking marker; no DB-level CHECK on the enum, matching this table's existing house convention of enforcing enums at the app layer, same as `CLEARANCE_TRIGGER_TYPES`; `policy_decisions` was 0 rows at authoring time, verified via psql, so nothing to backfill).
  - `artifacts/api-server/src/routes/portal-policy-decisions.test.ts` — added enum-rejection test, reviewDueAt-computed-from-cadence test (asserts the actual `db.insert().values()` call, not just the mocked response), and a reviewDueAt-stays-null-for-dependency-based test.

  **Verification:** `psql` confirmed `policy_decisions` was empty before running the migration (nothing to backfill). Ran the migration against local Postgres — succeeded. `tsc --noEmit` on `artifacts/api-server` shows only pre-existing, already-filed environment breakage (empty pnpm store copy of `vitest`/`drizzle-orm`/`zod` etc. — see #2501, #2192, #2508, #2515, #2516; confirmed the same failure reproduces from the MAIN checkout too, not a worktree-only issue) — no NEW errors in the touched region of `portal-policy-decisions.ts`. `npx vitest run` on this route's test file is genuinely unrunnable in this environment for the same reason (`ERR_MODULE_NOT_FOUND: vitest/config`) — already filed, not re-filed here. Verified the date-rollover logic (`setUTCMonth` overflow behavior for all 5 cadence values, including a Jan-31 edge case) with a standalone `node -e` script outside the broken module-resolution path — correct. Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
