# #3284 — Tesla room's last read is never persisted -- any re-render wipes it back to "not read yet"

- **Status:** ✅ DONE 2026-09-09T00:15:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal Tesla Integration Feature #3237)
- **Started:** 2026-09-09
- **Commit(s):** 8a4b701a6

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed root cause per issue body: viewTesla()'s "Right
  now" card (public/app.js) held the last real climate/charge read only in local JS variables;
  render()'s replaceChildren() (and #3268's pull-to-refresh) wipe them. No server-side
  persistence existed. Adding migration 064 (tesla_accounts last-read columns), wiring
  getVehicleClimateState()/getChargeState() to persist after a real read, GET /api/tesla/status
  to return it, and viewTesla() to pre-populate from it.
- 2026-09-09T00:15:00Z ✅ DONE — shipped in 8a4b701a6, pushed to main. Migration 064 (7 nullable
  columns on tesla_accounts) applied against local Postgres (`node bin/migrate.mjs`, then one
  manual `ALTER COLUMN last_battery_range TYPE numeric(6,2)` to fix a decimal-precision mistake
  caught during verification). tesla.mjs's getVehicleClimateState()/getChargeState() now UPDATE
  those columns after every successful real Fleet API read; connectionStatus() returns them as a
  `lastRead` object on GET /api/tesla/status. app.js's viewTesla() pre-populates the Right Now
  card from status.lastRead on load (with an honest "read X ago" from the real last_read_at) and
  runCheckNow() now shares the same paint function so both paths render identically.
  Honest verification gap: no real Tesla OAuth connection exists in the local dev database (0
  rows in local tesla_accounts) -- Shane's real connected account lives on the deployed instance,
  same local-Tesla gap migrations 055/060 already documented, and this session has no access to
  that deployment. Verified instead with a real, live round-trip against local Postgres: seeded a
  synthetic tesla_accounts row, ran the exact UPDATE statements getVehicleClimateState()/
  getChargeState() now run, confirmed connectionStatus() reads back the correct values with
  correct types (numeric columns cast back to JS numbers), then deleted the synthetic row. Working
  tree clean; branch verified merged into origin/main via verify-branch-merged.mjs.
