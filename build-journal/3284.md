# #3284 — Tesla room's last read is never persisted -- any re-render wipes it back to "not read yet"

- **Status:** ⏳ IN FLIGHT 2026-09-09T00:00:00Z
- **Scope:** platform (web/shanes-life -- Shane's personal Tesla Integration Feature #3237)
- **Started:** 2026-09-09
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed root cause per issue body: viewTesla()'s "Right
  now" card (public/app.js) held the last real climate/charge read only in local JS variables;
  render()'s replaceChildren() (and #3268's pull-to-refresh) wipe them. No server-side
  persistence existed. Adding migration 064 (tesla_accounts last-read columns), wiring
  getVehicleClimateState()/getChargeState() to persist after a real read, GET /api/tesla/status
  to return it, and viewTesla() to pre-populate from it.
