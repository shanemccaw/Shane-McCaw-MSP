# #3579 — Multi-repo: add real (repo_owner, repo_name) dimension to bt_issue_mirror / bt_build_queue

- **Status:** ✅ DONE 2026-09-11T01:11:07Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 677dda0a1

## Log
- 2026-09-11T00:25:00Z ⏳ IN FLIGHT — pulled #3579 body + comment, parent Feature #3578, sibling sub-issues #3582/#3583/#3584; read real live schema for every `bt_*`/`build_dispatch_log` table via direct psql against local `DATABASE_URL`. About to design the additive migration (repo_owner/repo_name columns + composite keys) and audit real call sites in `BuildQueuePostgresClient.cs` / `GitHubIssueMirror.cs`.
- 2026-09-11T01:11:07Z ✅ DONE — Real, additive migration `lib/db/migrations/manual/2026-09-10-multi-repo-issue-key-dimension-3579.sql` ran cleanly against local `DATABASE_URL` (re-run confirmed idempotent). Added `repo_owner TEXT NOT NULL DEFAULT 'shanemccaw'` / `repo_name TEXT NOT NULL DEFAULT 'Shane-McCaw-MSP'` to every table keyed on a bare GitHub issue/milestone number: `bt_issue_mirror` (PK → `(repo_owner, repo_name, issue_number)`), `bt_dispatch_claims` (PK → composite), `bt_epics` (unique index → composite), `bt_issues` (unique index → composite), `bt_chat_issues` (unique index → `(chat_id, repo_owner, repo_name, issue_number)`), `bt_chat_mentioned_issues` (unique index → `(chat_url, repo_owner, repo_name, issue_number)`), `bt_milestone_mirror` (PK → `(repo_owner, repo_name, number)`); plus columns-only (no key change, per #3582/#3584's own explicit scope for threading dedup logic) on `bt_build_queue` and `build_dispatch_log` (its lookup index widened to `(repo_owner, repo_name, issue_number, dispatched_at)`). Verified live: a second-repo row (`someoneelse/shanes-life`) sharing an existing issue_number inserted successfully alongside the original — no collision.

  **Full call-site audit, every real query touched:**
  - `desktop/BuildConsole/Services/BuildQueuePostgresClient.cs` — `TryClaimDispatchAsync`/`ReleaseDispatchClaimAsync` (bt_dispatch_claims), `ResolveAndPersistChatLinkAsync`'s bt_epics/bt_issues find+upsert (incl. `ON CONFLICT` target fix), `LinkChatToIssueAsync`/`UnlinkChatFromIssueAsync`'s bt_chat_issues insert/delete, `RecordChatIssueMentionsAsync`/`PruneClosedChatIssueMentionsAsync` (bt_chat_mentioned_issues, incl. `ON CONFLICT` target fix), `GetBoardAsync`'s bt_epics list.
  - `desktop/BuildConsole/Services/GitHubIssueMirror.cs` — every bt_issue_mirror read (`TryGetAsync`, `GetManyAsync`, `TryGetTitleAsync`, `TryGetByBoardStatusAsync`, `TryGetBoardIssuesAsync`), the incremental-sync upsert, the full-sync upsert + mark-closed sweep, `UpsertTitleOnFallbackAsync`, and bt_milestone_mirror's upsert/delete-sweep + `TryGetMilestoneInfosAsync` — all `ON CONFLICT` targets widened to the new composite keys. New `RepoIdentity.cs` holds the shared default (`shanemccaw`/`Shane-McCaw-MSP`).
  - `artifacts/api-server/src/routes/admin-build-tracker.ts` — the **parallel** Drizzle-based bt_epics/bt_issues sync path this route runs independently of the C# side (still live per `BuildQueuePostgresClient.cs`'s own doc comment — the API server still serves `BuildQueuePanel`'s display + label toggling): `upsertEpicRow`/`upsertIssueRow`, the full-sync bulk upsert loop (`epicsUpserted`/`issuesUpserted`, nested-epic linking, previous-status lookups), `resolveChatEpicOrIssue`, `set-issue-state`, `sync-epic`'s parent lookup, the in-progress-label batch lookup, `GET /epics`, `GET /issues`, `GET /extension/board`, `chats/assign-issue`/`unassign-issue` — every `onConflictDoUpdate({ target: ... })` widened to `[repoOwner, repoName, githubNumber]`.
  - `lib/db/src/schema/index.ts` — Drizzle table defs for `btEpicsTable`/`btIssuesTable`/`btChatIssuesTable`/`btChatMentionedIssuesTable`/`btBuildQueueTable`/`buildDispatchLogTable` updated to match the real migration (repo columns + composite unique indexes) so the TS side's compile-time shape matches the DB.
  - `scripts/dev-server/claim-dispatch.mjs` / `release-dispatch-claim.mjs` — the standalone Node claim path onto `bt_dispatch_claims` (I use this myself for the `BUILD:` dispatch-claim gate) — `ON CONFLICT` target fixed, all three queries repo-scoped.

  **Deliberately left alone** (documented, not missed): `bt_build_queue`'s own dedup (`FindDedupCandidateAsync`)/blocked_by claim logic and the equivalent dedup check in `admin-build-tracker.ts`'s queue-item POST — threading a real per-item repo value through those is explicitly #3582's ("Dedup/blocked_by/dispatch-comment logic... needs the real repo context threaded through") and #3584's own scope, not this issue's. `bt_issue_mirror_sync_state` is a singleton (`id=1`, not issue-number-keyed) — filed as **#3597**. ShaneBuilder's own paused, read-only DB clients (BoardReadClient/ChatReadClient/QueueReadClient) weren't touched — filed as **#3598**.

  **Verification:** migration ran clean + idempotent against local `DATABASE_URL`; `dotnet build BuildConsole.csproj` succeeded (0 errors, only 6 pre-existing unrelated warnings); `pnpm run typecheck` in `artifacts/api-server` (which builds `lib/db` first) passed clean; live cross-repo collision test passed. Existing Batter Up/Build Queue behavior is unaffected — every existing row defaulted to `shanemccaw`/`Shane-McCaw-MSP`, and every query now explicitly filters/writes that same default.

  Findings filed: #3597, #3598 (both parented as sub-issues of Feature #3578, board status AI Batter Up).
