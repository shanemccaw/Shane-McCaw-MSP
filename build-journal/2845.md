# #2845 — User.ReadWrite.All narrowing on the 3 user-write rules

- **Status:** ✅ DONE 2026-09-06T14:50:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 054c0e023a4716c3363c97b279e372444fba9a74

## Log
- 2026-09-06T14:30:00Z ⏳ IN FLIGHT — confirming via Microsoft Graph documentation whether
  User.ReadUpdate.All authorizes the accountEnabled PATCH; then deciding whether to narrow
  the 3 rules per #2845's next-action list.
- 2026-09-06T14:50:00Z ✅ DONE — Confirmed via Microsoft Learn (user-update page) that
  User.ReadUpdate.All does NOT cover accountEnabled: Microsoft's per-property table requires
  User.EnableDisableAccount.All + User.Read.All instead, plus an app-only directory-role
  assignment that's out of agent reach (Git #1913). Separately found that #2858 (landed after
  #2845 was filed) added two rules — DELETE /users/* and PUT /users/*/manager/$ref — that
  document User.ReadWrite.All as their ONLY application permission with no narrower
  alternative, so User.ReadWrite.All is unconditionally in DERIVED_WRITE_APP_PERMISSIONS
  regardless of the 3 rules #2845 named. Narrowing those 3 rules would add permissions to the
  requested set without removing User.ReadWrite.All — net larger ask, zero real privilege
  reduction. Did NOT apply the narrowing; updated the two rules' justification comments in
  graph-write-permissions.ts to record both findings (commit 054c0e023a4716c3363c97b279e372444fba9a74,
  comment-only, no permissions[]/DERIVED_WRITE_APP_PERMISSIONS change, verified by loading the
  file directly since api-server's vitest suite is unrunnable — filed as #2986). Posted full
  findings as a comment on #2845 (left open, not closed — Shane's call). Board status
  "AI Batter Up" not set on #2986 — GitHub GraphQL rate-limited for this account for the whole
  session; REST API calls (label/sub-issue) succeeded fine.
