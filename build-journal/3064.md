# #3064 — script-ingestion.test.ts mock call-count leak

- **Status:** ✅ DONE 2026-09-07T15:47:24Z
- **Scope:** platform

## Log
- 2026-09-07T15:44:01Z — Starting: fix mock call-count leak in `artifacts/api-server/src/routes/script-ingestion.test.ts` per issue body (missing mock reset in beforeEach / shared mock instance leaking across describe blocks).
- 2026-09-07T15:47:24Z — ✅ DONE — Root cause: the outer `beforeEach` only did `vi.resetModules()`, never resetting the module-scope `mockSelect`/`mockInsert`/`mockUpdate` `vi.fn()`s, so call counts accumulated across every test case in the file (matches the issue's own diagnosis). Added `vi.clearAllMocks()` to that `beforeEach`. After that fix, "insert" settled to exactly 1 call as expected, but "update" still showed 2 (not a leak) — the route's fire-and-forget `runIngestionAnalysis()` also calls `db.update()` on `scriptRunResultsTable` once it settles, so a single successful request legitimately produces 2 `mockUpdate` calls (token burn + AI-analysis status update). Rewrote that assertion to check the update was called with the token-burn table shape (`expect.objectContaining({ tokenHash: "tokenHash" })`) instead of a raw total-call-count, since the fire-and-forget call is real, correct app behavior, not something the test should suppress. All 16 tests in the file pass, verified stable across 4 repeated runs (no flake from the un-awaited fire-and-forget promise).
  - Commit: `6585cdf28` (Fix mock call-count leak in script-ingestion.test.ts (#3064)), merged into `main` at `1146ff934`.
  - Branch verified merged via `node scripts/dev-server/verify-branch-merged.mjs` — OK.
  - `git status --porcelain` clean.
