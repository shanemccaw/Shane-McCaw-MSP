# #1501 — Change Control: CAB — membership, meetings, agenda, ECAB

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** dea23e039, 6edaac6f0, 1563d298a, 85ebd9c1e, 5c98a45b3 (rebased onto
  main as d54e014f7, 0e28828bf, 620ea4380, c90a23330, 68d5995f6; pushed to `main` at
  68d5995f6)

## Log
- 2026-08-29 ⏳ IN FLIGHT — building `cab_members`, `cab_meetings`, `cab_agenda_items`
  schema + manual migration, MSP-operator CAB routes (membership, scheduling, agenda,
  decisions, minutes, ECAB retroactive approval), reusing the #1496 approval model for
  recorded decisions. Backend/wire-contract only per the build's SCOPE STOP — no
  `artifacts/portal` UI, no design export exists for this page.
- 2026-08-29 ✅ DONE — shipped `cab_members` / `cab_meetings` / `cab_agenda_items`
  (Drizzle schema in `lib/db/src/schema/msp.ts` + manual migration
  `lib/db/migrations/manual/2026-08-29-cab-membership-meetings-agenda-1501.sql`, run
  against local Postgres and verified with `\d` against the live schema), the pure
  derivations (`artifacts/api-server/src/lib/portal-cab.ts`), the DB store
  (`portal-cab-store.ts`), and the MSP-operator routes
  (`routes/msp-change-control-cab.ts`, floor `MSPOperator` + `resolveMspIdStrict`,
  mounted in `routes/index.ts`) — membership CRUD, meeting scheduling/start/close/
  cancel, agenda eligibility (structurally excludes standard changes — eligibility is
  "has a pending #1496 approval slot", so a pre-approved standard change has none to
  have), agenda add/decide/defer, and an ECAB path where every agenda item on an
  `ecab` meeting is `isRetroactive = true`. A CAB decision is recorded through the
  SAME `recordApproval` / `recordRejection` the customer register uses (#1496) — no
  second approval model; `cab_agenda_items.cr_approval_id` is the join back to the
  real ledger. Closing a meeting compiles its minutes from the agenda's own recorded
  recommendations.

  20 tests: 14 pure (`portal-cab.test.ts`) + 6 real-DB integration
  (`portal-cab-store.test.ts`, follows `purchase-account-flow.test.ts`'s
  create-under-marker/`afterAll`-cleanup discipline) — all green, confirmed no
  orphaned rows left in the local DB after the run. `lib/db` itself builds/typechecks
  clean (`tsc -b .`, 0 errors) and a warm-cache `tsc --noEmit` on `artifacts/api-server`
  shows 0 errors in any file this build touched. Filed #1778 (parented to #1096) for a
  pre-existing, unrelated toolchain quirk hit along the way: a COLD `tsc --noEmit`
  (no prior `lib/db` build) phantom-fails on any export declared late in the
  ~5,300-line `msp.ts`, including symbols that predate this build (`CC_GATE_KEYS`)
  — not a regression from this session, not spent further time on it.

  Rebased cleanly onto `main` after three concurrent Change Control builds landed
  underneath (#1497 CR-as-gate, #1498 standard change catalog, #1500 freeze windows)
  — conflicts were pure line-adjacent additions (new imports/router.use calls, new
  vitest include entries, a new schema block ahead of mine); resolved by keeping both
  sides, re-verified with the full test run above post-rebase, pushed to `main` at
  `68d5995f6`. `verify-branch-merged.mjs` confirms merged. `git status --porcelain`
  clean.

  No UI wired, per the build's SCOPE STOP — `artifacts/portal` has no pages for this
  and `Design/portal/` carries no export for it.

  Findings filed: #1778 (cold-tsc toolchain quirk, parented #1096, board status "AI
  Batter Up"). Migration filename appended to #1630's checklist.
