# #2950 — Fix stale isPublic assertion in free-scan-prospect.test.ts

- **Status:** ✅ DONE 2026-09-06T00:05:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** none (no code change required — see log)

## Log
- 2026-09-06T00:00:00Z ⏳ IN FLIGHT — Investigating the claimed stale assertion at
  `artifacts/api-server/src/lib/free-scan-prospect.test.ts:99-116` (`isPublic=true`
  for `license-waste-audit-free`, said to be a second block distinct from the one
  #1169 already fixed).
- 2026-09-06T00:00:00Z ✅ DONE — **False premise, no code change made.** Read the
  file at current HEAD (`441fb5378`, which has `df11cb0a6` — #1169's own fix commit
  — as an ancestor). There is only ONE `describe`/`isPublic` assertion block in this
  file (lines 99-127), not two. It already reads:
  ```
  expect(svc.isPublic).toBe(false);
  expect(svc.visibility).toBe("landing_page_only");
  ```
  with a comment explicitly citing #1169's fix. `git log -p` on the file confirms
  `df11cb0a6` ("Free Scan: repoint at full-depth... fix public-catalog leak
  (#1169)") is the commit that flipped this exact assertion from `true` to `false`
  and updated the `it()` description accordingly — this IS "the one #1169's own
  session already updated," and no second/different block exists anywhere else in
  the file (`grep -n isPublic` on the whole file returns only this one occurrence).
  Confirmed via `git merge-base --is-ancestor df11cb0a6 HEAD` that this worktree's
  branch already contains #1169's fix. No stale assertion exists to fix.
  - Could not additionally run the suite live (`npx vitest run`) — `vitest` is not
    resolvable from either this worktree's or the main checkout's `node_modules`
    (`ERR_MODULE_NOT_FOUND` for `vitest/config`; confirmed the same gap exists in
    `C:\Source\...\Shane-McCaw-MSP\artifacts\api-server\node_modules` directly, so
    this is a pre-existing environment gap, not a worktree-junction dangling-link
    issue, and not something introduced by this session). Per CLAUDE.md, did not
    run `pnpm install`/`--force` to try to fix it. Verification here is by direct
    source + git-history read instead, which is conclusive for this specific
    question (does the file still assert `true`? — no).
  - No `blocked_by` edge needed — this isn't blocked, the work item itself doesn't
    exist. Posted an explanatory comment on #2950 with this finding.
  - Working tree: `git status --porcelain` is clean (no files touched, nothing to
    commit).
