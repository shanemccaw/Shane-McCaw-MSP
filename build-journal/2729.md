# #2729 — GET /msp/operator-tasks joins dropped table msp_customers, 500s every call

- **Status:** ✅ DONE 2026-09-04T04:27:13Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 8bb09c61314ab16aea6ca2527aa2c9833a6d6434

## Log
- 2026-09-04T04:05:22Z ⏳ IN FLIGHT — confirmed via psql that `msp_customers` no longer
  exists (dropped by `2026-07-28-tenant-user-refactor-phase0-schema-wipe.sql`) and that
  `artifacts/api-server/src/routes/msp-sla.ts:517,533` still `LEFT JOIN msp_customers c`
  in the `GET /msp/operator-tasks` route. Fixing to join `tenants` (column
  `customer_name`, not `name`) per the pattern in `admin-customer-alert-rules.ts` /
  `msp-reports.ts`. Also adding a real Postgres-backed test alongside the existing
  mock-only suite, since the mocked test never caught this.
- 2026-09-04T04:27:13Z ✅ DONE — repointed both `LEFT JOIN msp_customers c` in
  `msp-sla.ts`'s `GET /msp/operator-tasks` at `tenants c` (`c.customer_name` in place
  of `c.name`). Added `msp-sla-operator-tasks.live-db.test.ts` — a real-Postgres test
  (skips cleanly with no `DATABASE_URL`) that seeds a synthetic msp/tenant/breach/
  violation and hits the live router end-to-end, confirming 200 + correct
  `customerName`/`customerId` instead of the historical silent 500. Registered it in
  `vitest.config.ts`'s explicit include allowlist. While verifying the existing mock
  suite still passed, hit and fixed two unrelated pre-existing mock gaps in
  `msp-sla-scope-creep.test.ts` that were blocking verification (missing `logger.child`
  and no `db.select` stub for `resolveStaffScopedCustomerIds`) — a concurrent session
  landed its own fix for the same two gaps first (`f49524294`, Git #2726); rebasing hit
  a real conflict on that file, resolved by keeping their fix and dropping my redundant
  duplicate. Filed a genuine separate finding as #2812 (sibling under Feature #2572):
  `POST /msp/customers/bulk` action `"tag"` in `msp-portal.ts` still issues
  `UPDATE msp_customers`, and unlike this fix it's not a drop-in rename — `tenants` has
  no `tags` column at all, so that endpoint stays dead until a real product decision
  (add the column vs. retire the feature). Verification: `pnpm vitest run
  msp-sla-scope-creep msp-sla-operator-tasks.live-db` — 27/27 pass. `tsc --noEmit`
  shows zero new errors in any file this session touched (remaining repo-wide errors
  are pre-existing baseline drift, unrelated). `verify-branch-merged.mjs` confirms
  `8bb09c61` is merged into `origin/main`. `git status --porcelain` clean.
