# #4049 — accept-agreement.tsx: generate the contract pack once Platform Agreements is restored (#4048)

- **Status:** ✅ DONE 2026-09-14T19:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** d9dc33ff3 (contract pack), 4bc4c71f6 (IN FLIGHT bookend)

## Log
- 2026-09-14T18:50:00Z ⏳ IN FLIGHT — Confirmed #4048 genuinely closed (stateReason completed, DONE bookend + verify-branch-merged comment present). Pulled #4049/#4048/#4009 full bodies+comments. About to read the restored `artifacts/api-server/src/routes/platform-agreements.ts` and admin page, and check local Postgres for the real state of `platform_agreements`/`msp_agreement_acceptances`, then write the read-only contract pack for `accept-agreement.tsx`'s three endpoints.
- 2026-09-14T19:20:00Z ✅ DONE — Wrote `Design/portal/design_handoff_full_site/docs/accept-agreement-contract-pack.md` (read-only, no product/schema/UI changes). Extracted the three endpoints' real wire contracts from the restored `platform-agreements.ts`, noted the RBAC gate change from #2442's pack (`requireAuth`/`requireRole("PlatformAdmin")` → `requireCapability("ladder.free")`/`requireCapability("ladder.platform-admin")` per #4048's #2460-era restore). Confirmed live against local Postgres: `platform_agreements` still has only the one never-published `id=1 "Test"` row, `msp_agreement_acceptances` still 0 rows — same real state #4049 asked to be re-checked, unchanged by #4048's restore. Confirmed no `.dc.html` Design export exists yet for a standalone `accept-agreement.tsx` scene (still blocked per #4009). Found and filed a real finding — stale header comments in `artifacts/portal/src/lib/msp-signup-api.ts:9-17` and `artifacts/portal/src/pages/signup.tsx:26-40` still claim the backend "was deleted ... no way left to publish one," which #4048 falsified — filed as **#4073**, parented as a direct sub-issue of Feature #1649 (not the Epic), labeled `bug`, board status set to "AI Batter Up". Did not file a second orphaned-endpoint issue for the three endpoints having no page consumer — that's the same already-tracked gap #4009 covers, not a new finding. Removed the now-stale `blocked_by` edge on #4049 → #4048 (#4048 is closed). Pack pushed as commit `d9dc33ff3`, rebased and pushed clean onto `origin/main` (`139f4a547`). `verify-branch-merged.mjs`: OK, merged. `git status --porcelain`: clean.
