# #4434 — Paid Copilot assessment purchase never provisions a client_services entitlement row

- **Status:** ✅ DONE 2026-09-16T00:40:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** d60e9a8dd

## Log
- 2026-09-16T00:10:00Z ⏳ IN FLIGHT — wiring a `client_services` insert for the base assessment purchase in `public-assessment-payment.ts` `/payment-confirmed`, mirroring `createRescanSubscription`'s existing shape for the add-on.
- 2026-09-16T00:40:00Z ✅ DONE — added `recordBaseAssessmentClientService()` in `artifacts/api-server/src/routes/public-assessment-payment.ts`, called once inside `/payment-confirmed`'s "first confirm only" block (guarded by the same `order.status !== "paid"` check that already gates the audit log/email, so a replayed confirm cannot double-insert). Writes `clientUserId` (looked up by buyer email, same pattern `recordRescanClientService` uses), `serviceId: order.serviceId` (the base assessment's own catalog row, already resolved server-side — never trusts the client), `status: "active"`, `progress: 0`, `startDate: new Date()`. Non-fatal try/catch, same as the add-on's mirror — a missing local row is never allowed to fail the payment response.
  - `tsc --noEmit` on `artifacts/api-server` — clean, no errors.
  - No existing test file for this route (`public-assessment-payment.test.ts` does not exist) — verified instead with a direct transaction against the local `DATABASE_URL` Postgres: inserted a synthetic `zz-test-4434-verify@example.com` user + a `client_services` row with the exact shape the new code writes (service id 25, "Copilot Readiness Assessment"), then ran `portal-ownership.ts`'s real dashboard join query (`client_services JOIN users JOIN services`) against it — returned the row correctly (`status: active`, `service_name: Copilot Readiness Assessment`). Entire verification ran inside `BEGIN;...ROLLBACK;` — nothing persisted to the real database.
  - `portal-billing.ts`'s retainer-billing reads filter on `stripeSubscriptionId IS NOT NULL`, so this one-time purchase correctly does not surface there (expected — that surface is for recurring services only).
  - No genuine new finding beyond what #4434's own body already documented (the rest of "paid-order provisioning" — contract/project/invoice — remains explicitly out of scope, unchanged from the file's own docstring). Updated that docstring to reflect the entitlement row now being written.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; `git status --porcelain` clean.
