# #3164 — Today tray has no 'Later, by moment' balloons row (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T03:20:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 8fc39a4f9 (rebased onto main as 7e07f94ed)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — building the README "Later, by moment" balloon row: real
  per-moment signals (Heading out via a conventionally-named list, Coming up via existing
  dates.listDates, Dinner via meal-plan tonight + client mealSession, At the Rental via an open
  (fixed_on IS NULL) contacts row against the Rental, Idle via pendingCaptures), then the balloon
  UI itself in public/app.js + app.css.
- 2026-09-08T03:20:00Z ✅ DONE — shipped `computeLaterMoments()` (api.mjs), `getHeadingOutSignal()`
  (lists.mjs), `getOpenRentalJob()` (contacts.mjs), and the full balloon-row UI in app.js/app.css
  (`laterSection`/`laterRow`/`laterBalloonSvg`, plus a new peeker roll on the "Later, by moment"
  label). Added `dk-bird`/`dk-cloud` symbols to critters-sprite.svg, ported byte-for-byte from
  the design handoff's own embedded sprite. No new tables -- Heading out and At the Rental both
  reuse existing real mechanisms (the generic Lists room; an unresolved `contacts` row) rather
  than inventing backend the way the issue warned they might need.
  Verified against a real local Postgres: `shanessurvival` only had migrations 001-012 applied
  (the WPF app's own baseline) -- ran `node bin/migrate.mjs` against it, which applied
  013_shanes_life_foundation.sql through 040_places.sql (27 files, all additive). This is real,
  standing progress on the still-open #3115 finding ("two dead local Postgres databases... empty
  shanessurvival, orphaned shanes_life") -- `shanessurvival` is no longer empty, though #3115
  itself stays open since the local/Replit-hosted split it describes isn't resolved by this.
  Seeded a real test user + real rows (a "Heading Out" list, a date 6 days out, an unresolved
  Rental contact, today's meal plan, a pending capture) through the actual core modules, called
  `computeLaterMoments()` directly, confirmed all five moments resolved correctly, then deleted
  the test user (cascaded cleanly -- verified zero rows left in every touched table).
  `node --input-type=module --check` passed on every touched file. Branch rebased onto and
  pushed to `origin/main` (7e07f94ed); `verify-branch-merged.mjs` confirms merged.
  No test manifest: this app is a standalone Replit-hosted personal project outside
  `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`, the two directories the Test
  Coverage section scopes manifests to.
