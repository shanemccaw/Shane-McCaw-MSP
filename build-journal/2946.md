# #2946 — Extend testbed reset to cover customer/tenant-scoped tables found by audit tool

- **Status:** ✅ DONE 2026-09-05T01:55:00Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 96b51677a (main)

## Log
- 2026-09-05T01:20:00Z ⏳ IN FLIGHT — extending `lib/db/migrations/manual/2026-08-27-testbed-reset-patch-projects-1396.sql`
  (the single self-sufficient testbed reset) to cover the ~32 real customer/tenant-scoped
  tables `scripts/testbed-reset-audit.mjs` found missing FK coverage for, per #2946's
  issue body. Deliberately NOT touching the auth/session/webauthn/MFA/push-subscription
  set (`mfa_challenges`, `mfa_enrollments`, `password_reset_tokens`, `user_sessions`,
  `webauthn_challenges`, `webauthn_credentials`, `push_subscriptions`) — that's a real
  open product decision per the issue, not mine to make.
- 2026-09-05T01:55:00Z ✅ DONE — extended the reset migration with 44 new customer/tenant
  FK edges swept: a new users(id)-scoped loop (19 tables), an extended
  tenant_id(integer)-scoped loop (13 tables, folding in the prior standalone
  tenant_compliance_scope block), a dedicated config_diffs block (two tenant columns),
  active_directory_ou_assignments folded into the existing customer_id loop, and
  emails.linked_project_id handled alongside the projects cleanup. Ran the migration
  against local Postgres (`DATABASE_URL`): first run swept 2950 real rows with zero FK
  violations; re-run is idempotent (0 rows). Fixed `scripts/testbed-reset-audit.mjs`'s
  `coveredTables()` to recognize the new `table:column` ARRAY pairs and literal
  `DELETE FROM` statements, and added the 7 auth/session/identity tables to its
  `DELIBERATE_EXCLUSIONS` with the same reasoning the issue used. `node
  scripts/testbed-reset-audit.mjs` now exits 0 (was exit 1 / 50 gaps). Auth/session/MFA/
  passkey/push-subscription tables left completely untouched, exactly as the issue asked —
  that product decision (does a tenant reset log the test account out) was not made here.
  Committed as `96b51677a`, rebased onto and pushed directly to `origin/main` (verified via
  `verify-branch-merged.mjs`). Working tree clean.
