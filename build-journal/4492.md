# #4492 — AdUserCanvas: wire MSP/tenant reassignment UI action

- **Status:** ✅ DONE 2026-09-17T04:52:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** b7c2c4f4f (rebased/pushed as 1a16271ec on origin/main)

## Log
- 2026-09-17T04:29:05Z ⏳ IN FLIGHT — Investigating `AdUserCanvas.tsx`, `adApi.ts`'s `setAdUserAssignment`, and the AD search endpoint to design a real MSP/Customer picker wired to the existing backend route.
- 2026-09-17T04:52:00Z ✅ DONE — Confirmed the backend route (`PATCH /admin/active-directory/user/:id/assignment`) and `setAdUserAssignment` client wrapper were already real and correct, and that `AdUserCanvas.tsx` genuinely never called it (issue's audit was accurate). Added a reassignment picker to the "Role" section: a debounced type-ahead (`searchAdDirectory`, same pattern `AdExplorerTree.tsx`'s universal search already uses) scoped to whichever target type the account's current role actually requires (`useDirectoryRoles().linkageRequirementFor` → "msp" or "customer"; the picker doesn't render at all when the requirement is "none"). Selecting a result calls `setAdUserAssignment` and surfaces the real server error via the same `outcome` convention `changeRole`/`toggleEntitlement` already use — so the backend's `planAssignmentChange()` rejection (e.g. reassigning a CustomerUser to no tenant) is surfaced honestly, not swallowed. No fixture/hardcoded MSP or tenant list — every option comes from the real search endpoint.
  - Verified: `pnpm run typecheck` (admin-panel) clean; `pnpm run build` (admin-panel) succeeds.
  - No schema/backend changes needed — both the route and the client wrapper were already complete per the issue's own audit.
  - No new finding filed — the issue's audit findings were all confirmed accurate as stated; no additional gap discovered while wiring this.
  - Commit `b7c2c4f4f` rebased onto `origin/main` and pushed as `1a16271ec`; `verify-branch-merged.mjs` confirms ancestor of `origin/main`. `git status --porcelain` clean.
