# #3296 — Phase 3: Real WPF narrowing -- remove financial read/write, scope remaining real DB needs

- **Status:** 🛑 BLOCKED 2026-09-09T02:05:00Z
- **Scope:** other (personal project — ShanesSurvival/shanes-life, Shane's milestone, not MSP)
- **Started:** 2026-09-09
- **Commit(s):** none — no code changed, blocked before implementation started

## Log
- 2026-09-09T02:05:00Z 🛑 BLOCKED — Pulled #3296's real scope: remove ShanesSurvival's
  own financial read/write code paths (Dashboard, GATE/shortfall calcs, bills/debts UI)
  "now that shanes-life is the sole source of truth," plus scope WPF's remaining real DB
  needs. Checked #3295 (Phase 2, the declared blocker) before touching any code. #3295's
  own DONE bookend and its final issue comment are explicit and honest: the real,
  idempotent migration script (`web/shanes-life/bin/migrate-financial-core-data.mjs`,
  commit `1b4157605`) was written and end-to-end tested against a local **scratch
  stand-in** database only — it was deliberately never run with `--execute` against
  shanes-life's real production Postgres, because neither Shane's 2026-09-09
  simplification comment nor the BUILD dispatch comment for #3295 contained the explicit
  "go ahead, run it now" that issue's own hard gate requires before touching his real
  live financial data. #3295 carries the `complete` label (code confirmed done) but is
  still open, and its real-world effect is zero rows moved into shanes-life's actual
  production database.

  That means #3296's own stated premise — "now that shanes-life is the sole source of
  truth" — is currently false. Shane's real financial data (1298 rows across 11 tables)
  still lives only in ShanesSurvival's local `finances` Postgres. If this session removed
  ShanesSurvival's own financial read/write paths now, his live personal app would lose
  its only working read path to his real data with no working replacement, since
  shanes-life's production DB has nothing in those tables yet.

  This is a genuine blocker, not a code gap I can build my way through: the actual
  `--execute` run against shanes-life's real production database is explicitly Shane's
  own call (per #3295's issue body and its own enforced env-var gate), not something an
  agent session performs. Stopping here — no ShanesSurvival code touched, nothing
  removed.

  **What's next:** once Shane runs the real migration (`--execute` against shanes-life's
  actual production `DATABASE_URL`, per #3295's final comment) and the row-count parity
  is confirmed against production, #3296 is genuinely unblocked and this narrowing work
  can proceed as scoped.

  Wired a real `blocked_by` edge: #3296 → blocked by #3295 (was already present from the
  build dispatch's `--blocked-by 3295`; left in place since it still accurately reflects
  reality — #3295's code is done but its real effect, the actual data migration, is not).
  Applied `blocked` label, removed `in-flight`. Left a plain-language comment on #3296
  stating what's being waited on. No files modified; working tree confirmed clean.
