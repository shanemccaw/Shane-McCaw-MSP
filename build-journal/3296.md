# #3296 — Phase 3: Real WPF narrowing -- remove financial read/write, scope remaining real DB needs

- **Status:** ✅ DONE 2026-09-10T17:35:00Z
- **Scope:** other (personal project — ShanesSurvival/shanes-life, Shane's milestone, not MSP)
- **Started:** 2026-09-10
- **Commit(s):** `590f337f7`/rebased to `a6c5b31da` (real narrowing work), `2ebceafcf` (IN FLIGHT bookend) — both on `main`

## Log
- 2026-09-10T17:20:00Z ⏳ IN FLIGHT — Resuming after the prior session's real `blocked`
  stop. Confirmed #3295 (Phase 2, the real migration) is genuinely done: Shane's own
  2026-09-09 comment on #3295 confirms he ran
  `migrate-financial-core-data.mjs --execute` himself, 1298/1298 rows inserted into
  shanes-life's real production Postgres, 10/11 tables exact row-count parity (`debts`
  off by one real row entered directly via MCP the same session, confirmed not a
  migration artifact). shanes-life's production DB is genuinely now the sole source of
  truth for the financial core. Removed the stale `blocked_by` edge (→ #3295, now
  closed/complete) and the `blocked` label; posted a comment on #3295 noting the
  unblock. Starting real Phase 3 work: reading every file under
  `desktop/ShanesSurvival/` to precisely scope what's financial-read/write (Dashboard,
  GATE/shortfall, bills/debts, pay-period plans, income, expected events, Plaid link+
  sync+backfill, the entire `ShanesSurvival.Mcp` server — all of it built on those same
  financial repositories with no other purpose) vs. what genuinely stays (the Groceries
  weekly-ad scraper, #3288's real narrowed role; Settings; the DB connection/migration
  infra itself). Real conclusion on "what else WPF's DB still needs" traced from code,
  not guessed: the weekly-ad scraper pushes directly through shanes-life's own MCP
  pipeline and writes nothing to WPF's local Postgres — once financial code is removed,
  there is currently no real feature left that reads or writes WPF's local database at
  all. Documenting this plainly rather than inventing a placeholder schema.
- 2026-09-10T17:35:00Z ✅ DONE — Real scope 1 (remove financial read/write) complete:
  deleted `Accounts/AccountRoleWindow`, `Dashboard/DashboardWindow`, all of `Plaid/*`
  (App); `Accounts/*`, `Dashboard/*` (Dashboard/PayPeriodDue/PayPeriodForecast),
  `Debts/DebtRepository`, `ExpectedEvents/ExpectedEventRepository`, `Income/*`,
  `PayPeriodPlans/*`, `Transactions/*` (Core); the entire `ShanesSurvival.Mcp` project
  (all 8 tools were built on the repositories above — nothing non-financial was left to
  expose), removed from `ShanesSurvival.sln`. Trimmed `MainWindow` to Settings/Recheck
  Connection/Apply Migrations/Weekly Ad only; trimmed `SettingsWindow`/`AppSettings` to
  drop the Plaid credential fields. `migrations/*.sql` and the local `finances`
  Postgres tables left untouched (inert, not dropped — a separate, genuinely
  destructive decision out of this issue's own scope).
  Real scope 2 (scope remaining DB needs, "don't guess"): traced from the actual code
  left standing, not guessed — the Weekly Ad scraper writes nothing to WPF's own local
  Postgres (it pushes straight through shanes-life's MCP pipeline). Honest conclusion:
  as of this narrowing, **no feature in this app reads or writes its own local
  database**. Kept the connection-string field/`DatabaseConnectionTester`/
  `MigrationRunner` anyway as cheap standing infrastructure (removing DB infra
  entirely wasn't this issue's scope), documented plainly in README's new "Local
  database" section rather than inventing a placeholder schema to justify keeping it.
  `dotnet build ShanesSurvival.sln`: clean, 0 warnings, 0 errors (real build, not
  simulated).
  **Real finding filed**: while confirming shanes-life is genuinely ready to be sole
  source of truth, found `web/shanes-life/src/routes/api.mjs`/`money.mjs` explicitly
  defer `role`/`target_amount`/`is_gate`/`due_day`/`last_paid_date` persistence to
  "ShanesSurvival's own MCP tools" — which were read-only and never wrote those fields;
  the real write path was always `AccountRoleWindow`, which this build just removed.
  That leaves genuinely no working write path anywhere for those fields. Filed
  **#3532**, parented under #3293 (the Feature-tier parent of this issue, per the
  Feature-first rule), labeled `bug`, board status set to "AI Batter Up".
  `git status --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` (`a6c5b31da`, after rebasing onto a moved `origin/main`
  and pushing directly — the worktree's own `agent/3296-q2022` branch fell behind after
  that rebase+direct-push and its own push was rejected, which is expected/harmless
  since the real commits are already on `main`).
