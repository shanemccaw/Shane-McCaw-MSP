# #2711 — Home dashboard foundation: fetch + cache real GitHub `closed_at` + daily open/close time series

- **Status:** ✅ DONE 2026-09-03T18:50:01Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** `1bec2d1be` (impl: GitHubApiClient.cs closed_at/created_at + GitHubIssueTimeSeriesService.cs); `6cbf6dca7` (IN FLIGHT bookend)

## Log
- 2026-09-03T18:43:02Z ⏳ IN FLIGHT — Foundational piece for #2710 (Home progress dashboard), blocking #2712/#2713/#2714. Audited `GitHubApiClient.cs`: confirmed `closed_at`/`closedAt`/`ClosedAt` appears NOWHERE in the codebase (repo-wide grep, zero hits) — the GraphQL board query fetches neither `createdAt` nor `closedAt`. Plan: (1) extend the GraphQL issue node + `GitBoardIssue` model + REST `GitHubIssueDetail` to carry real `createdAt`/`closedAt`; (2) build `GitHubIssueTimeSeriesService` producing a real per-day series (opened/day, closed/day, running open-count, cumulative closed) scoped by Milestone and by Epic, cached with a real TTL; (3) fail-closed honest "not enough data" signal when GitHub is unreachable or history is too thin — never a fabricated/interpolated series.
- 2026-09-03T18:50:01Z ✅ DONE — Shipped in `1bec2d1be`.
  - **`GitHubApiClient.cs`:** added real `CreatedAt`/`ClosedAt` (`DateTimeOffset?`) to `GitBoardIssue`, added `createdAt closedAt` to the GraphQL board node selection + `IssueNodeData` + the node→model map, and added REST `closed_at` to `GitHubIssueDetail` (which already carried `created_at`). `ClosedAt` is null while an issue is open.
  - **`GitHubIssueTimeSeriesService.cs` (new):** fetches every real repo issue (open+closed, with the new timestamps) once via `ListBoardIssuesAsync(All)`, cached 5-min TTL (force-refresh available); reduces to a real per-day series `{ Opened, Closed, OpenCount (running burndown), ClosedCumulative }` via the pure static `BuildSeries`; scoping helpers `GetMilestoneSeriesAsync`, `GetEpicSeriesAsync` (transitive descendants), `GetOpenEpicsInMilestoneAsync`, `ResolveActiveMilestoneAsync`/`GetActiveMilestoneSeriesAsync`. Fail-closed: `IssueTimeSeries.HasEnoughData==false` + honest `Reason` when no PAT / GitHub unreachable / span < 2 days / < 2 activity days — never an interpolated curve (gap days carry running counts forward = real "nothing happened", not fabrication).
  - **Verification (real):** live `gh api graphql` confirmed `createdAt`/`closedAt` return real values on real closed issues (e.g. #2707 created 18:26Z / closed 18:40Z, a known this-session closure). Spot-checked the daily reduction against all real milestone-5 (v1.1) issues: sum-opened − sum-closed (1000 − 548) = **452 = currently-open (452)** exactly — the invariant `BuildSeries` maintains (running OpenCount at today == real open count). Real daily counts confirmed (2026-09-03: 229 opened / 151 closed; 2026-09-02: 108 / 98). Clean `dotnet build` (0 warnings, 0 errors). Per CLAUDE.md, did NOT launch BuildConsole.exe to verify (single-instance courier hazard); verified the data path directly against live GitHub instead.
  - **No fixture/fabricated data.** No test manifest (out-of-scope: `desktop/BuildConsole` WPF app, per CLAUDE.md Test Coverage scope). No findings filed — no new bug discovered; the missing `closed_at` was the issue's own premise, now fixed.
  - **Consumers:** #2712 reads `Points[].OpenCount` (burndown); #2713 reads `Points[].Opened`/`.Closed` (rate crossing); #2714 reads `Points[].ClosedCumulative` per Epic + milestone via the scoping helpers for ETA. Working tree clean; HEAD confirmed ancestor of `origin/main`.
