# #2711 — Home dashboard foundation: fetch + cache real GitHub `closed_at` + daily open/close time series

- **Status:** ⏳ IN FLIGHT 2026-09-03T18:43:02Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** <real hash(es) — fill at DONE>

## Log
- 2026-09-03T18:43:02Z ⏳ IN FLIGHT — Foundational piece for #2710 (Home progress dashboard), blocking #2712/#2713/#2714. Audited `GitHubApiClient.cs`: confirmed `closed_at`/`closedAt`/`ClosedAt` appears NOWHERE in the codebase (repo-wide grep, zero hits) — the GraphQL board query fetches neither `createdAt` nor `closedAt`. Plan: (1) extend the GraphQL issue node + `GitBoardIssue` model + REST `GitHubIssueDetail` to carry real `createdAt`/`closedAt`; (2) build `GitHubIssueTimeSeriesService` producing a real per-day series (opened/day, closed/day, running open-count, cumulative closed) scoped by Milestone and by Epic, cached with a real TTL; (3) fail-closed honest "not enough data" signal when GitHub is unreachable or history is too thin — never a fabricated/interpolated series.
