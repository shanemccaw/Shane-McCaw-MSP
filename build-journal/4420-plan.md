# #4420 — Plan

Verify whether #4416 (Reset RBAC test accounts) and #4417 (Clean Dev Database chain) actually
incorporated Shane's tenant-admin-consent comment (posted 2026-09-16T18:16Z on each, before either
build's own IN FLIGHT bookend started — #4416 at 19:14Z, #4417 at 19:40Z) — the real constraint that
`reset-rbac-test-accounts.mjs` cannot create tenant-scoped rungs until mccawsoft2's tenant admin
consent has been manually granted (#4318).

## Steps
1. Pull #4420, #4416, #4417 full bodies + comments + real DONE bookends (build-journal/4416.md,
   build-journal/4417.md). Pull #4318 for the real query shape.
2. Read the actual shipped code: `RbacTestAccountsResetGate.cs`, `CleanDevDatabaseChainGate.cs`,
   and `scripts/db/reset-rbac-test-accounts.mjs` — confirm the no-consent path is handled for real
   (clear message, no hang, no silent/fabricated success), not just claimed in the bookend prose.
3. If a genuine gap exists, fix it per this issue's body (add the missing check to whichever gate
   lacks it). If both already handle it correctly, make no code change and say so plainly.
4. Bookend + issue comment + labels.

## Expectation going in
Unverified — read the real code before concluding either way.
