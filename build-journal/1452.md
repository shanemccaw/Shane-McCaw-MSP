# #1452 — Add Copilot-designed critters into the IssueChompAnimation mascot rotation (build-done chomp)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-28
- **Commit(s):** `bd8948d2a` (merged to main as `f1476f93f`)

## Log
- 2026-08-28 ⏳ IN FLIGHT — Extending `BuildMascot(int type)` in `desktop/BuildConsole/Services/IssueChompAnimation.cs` to add the Positive-category Copilot critters from `Controls/CritterFactory.cs` + `CritterRegistry.cs` (#1435) alongside the existing 5 hand-built mascots (Fox/Bear/Cat/Bird/Sprocket), additive only, each with its own new chomp sound-effect string, wrapped/scaled to the existing ~60x56 mascot canvas size, eligible in all three chomp variants (single, Epic mega-chomp, Milestone parade). `FocusCharacterLayer`/`CritterSpawner`/Focus Mode untouched, per scope.
- 2026-08-28 ✅ DONE — Added all 20 Positive-category Copilot critters (`CritterRegistry.All.Where(Category == Positive)`) into `BuildMascot()`'s rotation as cases beyond the original 5, each with a distinct new chomp sound string (SPLAT!/SQUEAK!/TWINKLE!/SNIP!/WHIRL!/SPARKLE!/FLUFF!/MUNCH!/BUZZ!/SWIPE!/POOF!/NIBBLE!/ROAR!/ZAP!/SHIMMER!/POP!/TWEET!/FLUTTER!/ZOOM!/SLURP!, none duplicating the original YUM/GULP/CRUNCH/PECK/CHOMP). Negative-category critters (Grumpkin, Poutlet, etc.) intentionally excluded — those belong to the separate "new issue" grump spawn, not this celebratory chomp. Added `WrapCopilotMascot()` to uniformly scale each CritterFactory canvas (native sizes ranging 38x34 to 64x54) into the existing ~60x56 mascot footprint, centered — verified the fit math for all 20 critters (all land within 44-60px on the long axis, matching the original mascots' 54-64x48-56 native range). All 3 call sites that draw from the rotation (single/Epic shared selection, Milestone-parade line, party-stage dancers) now pick from the full pool via `Rng.Next(MascotCount)` / new `PickParadeMascotIndices()` — previously the two parade sites hardcoded indices 0-4 so new critters could never appear there. Logging: reused the existing `git-board.critters` channel (already the established leaf for this exact critter-spawn feature area, per `LeftSidebar.xaml.cs`) rather than inventing a new one — logs which Copilot critter got picked on each chomp. `FocusCharacterLayer`/`CritterSpawner`/Focus Mode untouched, per scope. Verified: `dotnet build -c Release --no-incremental` succeeds, 0 errors (46 warnings, all pre-existing, none in the touched file). HONEST gap: no live WPF visual screenshot was taken in this non-interactive build session (no attached BuildConsole/WebView2 GUI runner reachable here for a desktop-app screenshot) — verification is compile-clean + the scale-fit math above; Shane should eyeball a real build-done/issue-closed/milestone-closed chomp to confirm the Copilot critters read well at that scale. Branch confirmed merged into main via `verify-branch-merged.mjs` before writing this DONE.
