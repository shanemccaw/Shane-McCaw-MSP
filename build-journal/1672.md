# #1672 — AdminV2: rehome the four platform-admin pages before msp-portal is deleted

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** c357c1e9b, 3a2cc362e (rebased/pushed as eb49f992 on origin/main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — recovering msps.tsx, msp-detail.tsx, customer-detail.tsx (admin/* half
  only) from `portal-archive-2026-08-29` tag and rebuilding them in AdminV2
  (`artifacts/admin-panel`) using the Gallery + Peek + Ribbon + Watch convention, no sidebar.
  Excluding `project-kanban.tsx` per issue body — that's Shane's product decision (#1570 vs
  AdminV2), will record the exclusion explicitly. Excluding the `msp/customers/:id` half of
  customer-detail.tsx — that's operator-console (#1571) audience, will file a new issue for it.
- 2026-08-29 ✅ DONE — Found the AD screen (`adminv2/screens/ad/`) already had real MSP/customer
  canvases (Phase 2, read-only + suspend/reactivate) backed by
  `/api/admin/active-directory/*`. Rather than duplicate a parallel list/detail screen, extended
  those existing canvases with exactly the capability the archived pages had that AD's Phase 2
  deliberately left out: MSP profile edit (name/domain/isTestbed/contact/notes/address via the
  real `PATCH /api/admin/msps/:id`), MSP-level impersonation (`POST
  /api/admin/msps/:id/impersonate`), and the customer canvas's write-back consent admin card
  (`GET /api/admin/customers/:id/write-consent[/start]`) — the one genuinely admin-scoped piece
  of customer-detail.tsx. Added 5 real columns to `msps`
  (`primary_contact_name/email/phone`, `address`, `notes`) via
  `lib/db/migrations/manual/2026-08-29-msp-contact-fields-1672.sql`, run against local Postgres
  in-session; extended the Drizzle schema, `msp-admin-settings.ts`'s create/update schemas, and
  `admin-active-directory.ts`'s MSP detail select to serve them. Deliberately did NOT port: the
  archived `tier` field/dropdown (hardcoded Silver/Gold/Platinum/Enterprise — the no-hardcoded-tier
  rule, and the real value already lives at `subscription.tierName`), the `status` field on the
  generic edit form (suspend/reactivate stay the only sanctioned status transition), and
  msp-detail.tsx's Telemetry/Billing/Audit-Log tabs (confirmed fabricated demo data — see that
  file's own "Fallback demo data wrapper" comment; filed as issue #1747 since a real
  `mspAuditLogsTable` + read endpoint already exist for the audit-log piece). Deliberately
  excluded `project-kanban.tsx` per the issue body's own instruction — mixes `admin/projects`
  with `portal/delivery-kanban-tasks` and `portal/projects`, a product decision for Shane (#1570
  vs AdminV2), not built here. Filed #1718 (parent #1571) for the `msp/customers/:id` operator-
  console surface (diagnostics, marketplace, edit/disable) — none of that is AdminV2's audience.
  Filed #1750 (parent #1202) as an infra finding: worktree `node_modules/@workspace/*` symlinks
  are relative and, because `node_modules` itself is junctioned from the main checkout, resolve
  back to the main checkout's copy of `lib/*` packages rather than the worktree's own edits —
  discovered because it produced a false-positive `tsc` error against my own (correct) schema
  change. Verified: `tsc --noEmit` clean for both `artifacts/api-server` and
  `artifacts/admin-panel` (zero errors touching any file this session changed; the pre-existing
  unrelated errors in both packages predate this session). Migration line appended to #1630.
  Rebased onto `origin/main` and pushed directly (no PR, per repo convention) —
  `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
