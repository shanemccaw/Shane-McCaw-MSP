# #3850 — Command Center epic-name matching

- **Status:** ✅ DONE 2026-09-12T21:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** de37df36c

## Log
- 2026-09-12T21:48:43Z ⏳ IN FLIGHT — wiring the Command Center's epic-*name* fuzzy
  match branch: CommandPaletteWindow gains a real epic-title match (against
  LeftSidebar's real board-fetched epics), rendered as "EPIC"-tagged result rows,
  Enter/click calling the existing `OpenOrCreateEpicChat(int)` verbatim.
- 2026-09-12T21:52:00Z ✅ DONE — shipped in de37df36c. `LeftSidebar.GetAllEpics()`
  added (real `_lastBoardIssues.Where(i => i.IsEpic)` source — the exact same
  epic source `CreateNewIssueAsync`'s own epic picker already reads, no second
  GitHub round-trip). `CommandPaletteWindow` takes that list in its constructor,
  case-insensitive substring-matches typed text against each real Epic title in
  both the "All" tab and the (previously always-empty) "Git Epics" category tab,
  and renders every real match as its own row (never guesses among multiple
  matches). Enter/click on a matched row raises `EpicOpenRequested(epicNumber)`,
  which `MainWindow` wires to the existing `OpenOrCreateEpicChat(int)` verbatim —
  no new chat-open/latest-chat-lookup logic, per the issue's explicit instruction
  to reuse `FindChatForIssue`/`OpenOrCreateEpicChat` as-is.
  Note vs. the issue body: `LeftSidebar.Epics` (`List<GitEpic>`) named in the issue
  is actually a per-milestone bucket ("⚡ Epics" grouping in the tree), not a flat
  list of real Epics with numbers — confirmed by direct read. The real flat list of
  every Epic (number + title) is `_lastBoardIssues.Where(i => i.IsEpic)`, the exact
  source `CreateNewIssueAsync`'s existing epic picker already uses; wired the new
  `GetAllEpics()` accessor to that instead so the fuzzy-match branch queries real
  data, not the wrong property. This is a naming correction only — the underlying
  claim ("real, already-populated Epic list exists to match against") was correct.
  `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj`: **0
  warnings, 0 errors**, both before and after the rebase onto current `origin/main`.
  No live WebView2/BuildConsole-instance run performed this session (build-only
  verification) — manual keyboard-only confirmation of "type substring → Enter →
  opens most-recent chat" is worth a quick pass next time BuildConsole itself is
  relaunched, but the wiring reuses `OpenOrCreateEpicChat` unchanged, which is
  already exercised by every other epic chat entry point in the app.
  `git status --porcelain` clean. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`).
