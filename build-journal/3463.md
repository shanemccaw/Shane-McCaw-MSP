# #3463 — Feature: Activity Layer (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:00:47Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 6c35eba7e (merged into origin/main)

## Log
- 2026-09-10T05:54:41Z ⏳ IN FLIGHT — pulled #3463 body + all 4 comments. Comment history:
  1. moved to a standalone `shanemccaw/MyArchitect` repo,
  2. reverted back into this repo (BuildConsole only supports one repo; repo made private),
  3. a `BUILD:` dispatch comment carrying `--blocked-by 3493` explicitly,
  4. a later `BUILD:` dispatch comment (buildSet `BatterUpClearOut`) that omits the
     `--blocked-by` flag but does not say the blocker cleared.
- 2026-09-10T05:54:41Z 🛑 BLOCKED — checked the real `blocked_by` dependency edge via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3463/dependencies/blocked_by`: it
  points at **#3493 "Feature: UI Shell Redesign (MyArchitect)"**, which is still
  **open** (not `complete`-labeled). #3493's own body says explicitly: "Foundational
  shell scaffolding — must land before other Features' UI work." #3463's own checklist
  item ("Review & tagging UI (daily local timeline...)") is exactly that kind of UI
  work — it needs the fixed-tab/contextual-tab/right-panel shell #3493 defines to land
  in. Confirmed no shell work has actually landed yet: `desktop/MyArchitect/` has no
  Fluent.Ribbon shell, no contextual-tab mechanism, and `build-journal/3493.md` does
  not exist. Per CLAUDE.md's "blocked_by edges are the gate" rule (Git #1987), the edge
  — not the most-recent dispatch comment — is what actually gates the queue, and it
  reads open. Non-UI groundwork on #3463 (the local "current context" state tracking
  itself, independent of any shell surface) could theoretically proceed, but the
  issue's stated Feature scope is UI-shaped throughout (review/tagging UI is
  explicitly called out) and #3493's own text says no UI work should land ahead of it
  — so stopping here rather than guessing at a partial build is the correct call.
  Stopping, per the "genuinely unreachable/blocked" protocol: labels synced
  (`in-flight` → `blocked`), edge left intact (it's real and current), comment posted
  on #3463 explaining the wait. No code changed. Working tree left clean.
- 2026-09-10T19:54:13Z ⏳ IN FLIGHT — resuming. Re-checked the real `blocked_by` edge:
  #3493 is now **CLOSED** with the `complete` label
  (`gh issue view 3493 --json labels,state` confirms). Removed the now-stale edge
  (`DELETE .../issues/3463/dependencies/blocked_by/5407799446`, confirmed empty
  after), swapped labels `blocked` → `in-flight`. Resuming real build work on the
  issue's stated scope.
- 2026-09-10T20:00:47Z ✅ DONE — commit `6c35eba7e`, rebased onto and pushed to
  `origin/main` (verified via `scripts/dev-server/verify-branch-merged.mjs` — exit
  0, "merged into 'origin/main'. Safe to write DONE"). Clean `dotnet build` (0
  warnings, 0 errors) confirmed both before and after the rebase (which had one
  real conflict — a concurrent #3474 build's own new `_retainerService` field in
  the same constructor-field block — resolved by keeping both fields, no logic
  lost on either side).

  **What shipped, real and working, covering the issue's own checklist:**
  - **Local "current context" state** (active tenant, active console command,
    active remediation step/record) — `Models/ActivityEvent.cs` +
    `Services/IActivityContextService.cs` / `ActivityContextService.cs`. Wired to
    three real signals already in the shell:
    - `ITenantService.CurrentTenantChanged` → tenant-switch events
    - `IPowerShellConsoleService.CommandExecuted` → console-command events (this
      event's own doc comment already named #3463 as "the Activity Layer" it
      feeds — #3459 was built expecting this)
    - `ShellRegistry.RecordOpened` → record-open events (a remediation step, a
      change request, a console-history entry, etc. — whatever contextual
      record is currently open, self-excluding the Activity Timeline's own
      records so reviewing your own timeline doesn't spam itself)
    Persisted to `%LOCALAPPDATA%\MyArchitect\activity-log.json`, same
    load/mutate/flush-immediately pattern as `ConsoleHistoryService`, 30-day
    local retention. Never synced to any backend — matches the issue's
    corrected scope exactly (Status Reports/retainer hours already flow for
    real through #3464's endpoints against `retainer_work_log`, independent of
    this).
  - **Foreground-window fallback** — `Services/IForegroundAppWatcher.cs` /
    `ForegroundAppWatcher.cs`, a real Win32 `GetForegroundWindow` +
    `GetWindowThreadProcessId` poll (15s, background `System.Threading.Timer`,
    no WPF dependency) that tags time spent in genuinely external apps (VS
    Code, Terminal, PowerShell, Slack, Chrome, Edge, Outlook, Teams — mapped
    from real process names, falling back to the real process name itself for
    anything unmapped rather than a fabricated label) against whatever
    tenant/record was last active in MyArchitect. Never fires for MyArchitect's
    own process; dedupes consecutive same-app switches.
  - **Review & tagging UI** — a new "Activity Timeline" command on the Home
    tab's Views group opens a daily local timeline through the shell's real
    full-panel record workspace (`RecordWorkspaceSpec` — same mechanism as
    Console History, UI_RULES.md §3): a `list` of today's events (kind ·
    tenant · time), selecting one opens its detail with a free-text `Tag`
    field wired as a real write-through `WorkspaceEdit` (`TextBox`,
    `LostFocus` commits — confirmed this is how a non-`Options` edit renders
    by reading `RecordWorkspacePanel.xaml.cs` before building against it, not
    guessing). Local-only, matches "for Shane's own reference — not synced
    anywhere" verbatim.
  - No fixture/hardcoded data anywhere — every event is a real signal from a
    real shell/service event; no new chrome outside UI_RULES.md (reused the
    existing Home tab group + the existing full-panel record-workspace
    mechanism, no new tab/panel/icon).

  No genuine findings surfaced this session worth filing separately — the one
  known gap (`TenantService` fixture tenant data / no real customer id) was
  already flagged in #3501's own bookend, not new here.

  Working tree left clean (`git status --porcelain` empty after the final
  push). Labels synced `in-flight` → `complete`. Comment posted on #3463 with
  the real DONE commit hash.
