# #3961 — Portal: Azure credential self-service — API endpoints

- **Status:** ✅ DONE 2026-09-14T02:01:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 9b65af6ec (endpoints), e7efd6096 (IN FLIGHT bookend)

## Log
- 2026-09-14T01:40:43Z ⏳ IN FLIGHT — Building `GET /portal/azure-credential` (metadata only) and `POST /portal/azure-credential/rotate`, reusing `admin-azure-credentials.ts`'s client-scoped pattern + `setSecretValue` Key Vault path, gated by the real customer-session middleware (`requireAuth`), scoped via `resolveSiblingUserIds`. No schema change (table already supports `clientUserId`).
- 2026-09-14T02:01:00Z ✅ DONE — Shipped `artifacts/api-server/src/routes/portal-azure-credential.ts` and registered it in `routes/index.ts`.
  - **Auth (audited, not assumed):** customer-session middleware is `requireAuth` (`middlewares/requireAuth.ts`) — the same guard every other `/portal/*` route uses. `requireAdmin`/`requireCapability` were rejected as they gate admin/ladder routes, not the customer portal.
  - **GET /portal/azure-credential** — resolves the caller's full sibling-login set (`resolveSiblingUserIds(req.user.id)`), matches the credential by `clientUserId IN (...siblings)` (a credential belongs to the customer account, not one login), returns an explicit metadata-only allow-list (id, displayName, tenantId, clientId, credentialType, expiry via `safeGetExpiry`, updatedAt) or `null` when none is registered. Never `...row`, never the secret value.
  - **POST /portal/azure-credential/rotate** — accepts `{ clientSecretValue }`, 400 if empty, 404 if no credential, 400 if not a `secret`-type credential. Writes the new value in-place to the row's existing `keyVaultSecretName` via `setSecretValue` (that name already follows the shared `client-${clientUserId}-appreg` convention, so this reuses it and can never orphan a secret), 502 on KV failure, then touches `updatedAt`. Returns the same metadata-only view; the new secret is never echoed.
  - **No schema change** — `azure_tenant_credentials` already carries `clientUserId`. Local table is empty (0 rows), so GET returns `null` cleanly; no fixture data introduced.
  - **Verification:** `tsc -p tsconfig.json --noEmit` clean (exit 0); `node build.mjs` esbuild bundle clean (exit 0). Live HTTP smoke not run — the dev server serves the previously-deployed build, not this worktree, until BuildConsole merges back; verification is the passing typecheck + build. Branch confirmed merged into origin/main via `verify-branch-merged.mjs`.
  - Findings filed: none.
