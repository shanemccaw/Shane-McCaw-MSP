# #3077 — 103 tenant_signal_history rows orphaned with NULL scope

- **Status:** ✅ DONE 2026-09-07T17:05:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 7388d3f19 (delete migration), 33de3336a (IN FLIGHT bookend) — both on main

## Log
- 2026-09-07T16:49:40Z ⏳ IN FLIGHT — investigating the 103 NULL-scope tenant_signal_history rows per #3077; confirming reasoning (unattributable, #2983 fixed the class going forward) before deleting.
- 2026-09-07T17:05:00Z ✅ DONE — Confirmed the issue's reasoning holds: msp_id=1 spans two tenants (id 1, id 3) that both already have their own correctly-attributed rows, no msp_diagnostic_runs or tenant_engine_snapshots row exists for msp_id=1 anywhere near the orphaned rows' fired_at window (2026-08-04 to 2026-08-07), and no other table FKs into tenant_signal_history.id — the 103 rows are genuinely unrecoverable to any tenant. Wrote and ran `lib/db/migrations/manual/2026-09-07-delete-orphaned-tenant-signal-history-3077.sql` against local DATABASE_URL. Real before/after: 6591 → 6488 total rows (DELETE 103), orphaned-row count 103 → 0, remaining 6488 rows all carry real customer_id/client_user_id attribution. Migration self-recorded in `simulator_migration_runs`. Rebased onto origin/main and pushed directly to main (7388d3f19); `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean. No new findings surfaced beyond what #3077's body already documented — GET /api/portal/data-export, the 7-year purge, and policy-engine.ts's evaluateAllPolicies now have zero orphaned rows to ever be blind to for this table.
