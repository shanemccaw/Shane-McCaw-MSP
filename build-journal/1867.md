# #1867 — Configure Intune on the testbed tenant so the device-management surface returns real data

- **Status:** 🚫 BLOCKED (genuine `Shane To-Do` — Intune licensing purchase decision)
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** `32c2b108e` (IN FLIGHT bookend), `6dbeb43c5` (BLOCKED bookend, real post-push hash)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Picking this up per the dispatch prompt. Reading #1867's full body
  and all 5 comments in order first, since the thread already went through two reversals
  (licensed-via-E3 → not licensed → confirmed not licensed) before this build was launched.

- 2026-08-30 🚫 BLOCKED — The issue's own final comment (2026-08-30T09:00:39Z, already posted
  before this build started) had already concluded the premise is false: the testbed does not
  hold an Intune licence, so there is no configuration work to do — only a purchase decision,
  which is Shane's, not an agent's, per the issue's own step 2. This session verified that
  conclusion against live state rather than re-deriving it: queried `tenant_service_availability`
  directly (local `DATABASE_URL`) and confirmed `#1847`'s classifier is live and current —
  `tenant_id=c4c814d4-...` (tenants.id=1, `is_testbed=true`, matches the authorized scope
  exactly), `service_key=intune`, `state=not_licensed`, `last_observed_at=2026-08-30 09:40:42-04`.
  Also confirmed `#1847`'s own bookend is `✅ DONE` (commits `d6779fc54`/`e3f70a48c`).

  **No tenant writes made. No code changed.** Formalized the blocked state structurally:
  removed the stale `blocked_by` link to `#1847` (now closed/`complete`) and added the real one
  to `#1909` (the tenant licensing finding, still open); swapped the `in-flight` label for
  `blocked`. No new finding filed — `#1909` already covers this exactly.

  Posted a comment on `#1867` recording the live verification and the honest stop:
  https://github.com/shanemccaw/Shane-McCaw-MSP/issues/1867#issuecomment-5469386568

  **What Shane needs to do:** decide whether to buy an Intune plan (standalone, or via
  Microsoft 365 E3/E5) for the testbed tenant `mccawsoft2.onmicrosoft.com`. This is the genuine
  `Shane To-Do` blocking `#1867` — not something recorded via the label on this issue (the issue
  itself carries `blocked`/`blocked_by #1909`, which is the correct mechanism here), but worth
  restating plainly: an agent cannot purchase licensing.
