# #4348 — Admin/Security workspace: Vault, Break-Glass, Audit Log, Consent matrix (Priority 4)

- **Status:** ✅ DONE 2026-09-16T05:35:00Z
- **Scope:** MyArchitect (shanemccaw/MyArchitect repo). Real primary view for the Admin/Security
  domain workspace built by #4344's shell rearchitecture (currently the honest "landing prompt"
  placeholder). Backend surfaces are all pre-existing/real: Vault (#3461, local-only), Break-Glass
  (#3480), Audit Log (#3489), Consent (#3485).

## Log
- 2026-09-16T05:00:00Z ⏳ IN FLIGHT — Confirmed #4344 (blocker) is CLOSED and its shell landed on
  MyArchitect `origin/main` (commit f185015). Confirmed all four Admin/Security backend surfaces
  already have real, working services + full-panel record workspaces wired into the Admin command
  bar (`MainWindow.xaml.cs` `RegisterAdminTab`, `OpenBreakGlassPendingListAsync`,
  `OpenAuditLogAsync`, `OpenConsentStatusListAsync`) — this issue's real remaining scope is (1) the
  domain's dedicated **primary view** (`ShowWorkspaceLanding` currently renders a placeholder for
  `FixedTab.Admin`, per #4344's own bookend: "Watch/Admin→honest landing prompt — no fabricated
  data, dedicated view built as part of this domain's own work"), and (2) auditing that every
  compliance-critical action (issue/reissue secret, grant/revoke consent, approve/deny break-glass)
  writes a real audit-log row, reporting any gap as a genuine finding rather than fixing/ignoring
  silently.
  Working in an isolated worktree `C:/wt/ma-4348` (branch `agent/4348-work`) off MyArchitect
  `origin/main`, not the shared `MyArchitect-clone` checkout (which is diverged and holds two
  foreign uncommitted files from another session — left untouched, same hazard #4344's bookend
  documented).
- 2026-09-16T05:35:00Z ✅ DONE — Implementation commit **2cfc5f8** on MyArchitect `origin/main`
  (verified: `git merge-base --is-ancestor HEAD origin/main` → HEAD == origin/main == 2cfc5f8).

  **What shipped:** `Controls/AdminSecurityDashboardView.xaml`/`.xaml.cs` — the real primary
  view for the Admin/Security domain workspace, replacing #4344's honest landing-prompt
  placeholder for `FixedTab.Admin`. Four real cards, each reading through the exact same
  already-real, already-authenticated services the Admin command bar's own actions use (no new
  backend, no second data path):
  - **Vault** (#3461) — per-tenant, local-only; shows Not-set-up / Locked / Unlocked+entry-count
    for the active tenant (`IVaultService`, DPAPI+master-key state, no HTTP call).
  - **Break-Glass** (#3480) — cross-tenant pending count + top-3 rows (`IBreakGlassService.GetPendingAsync`);
    row click reuses the existing `OpenBreakGlassSecretRecordAsync` full-panel detail.
  - **Audit Log** (#3489) — cross-tenant recent-5 timeline (`IAuditLogService.GetAuditLogAsync`);
    row click reuses the existing `OpenAuditLogEntry` full-panel detail.
  - **Tenant Consent matrix** (#3485) — cross-tenant recent-5 three-flag rows (`IConsentService.GetAllAsync`);
    row click reuses the existing `OpenConsentDetailRecordAsync` full-panel detail.
  Wired into `MainWindow.xaml`/`.xaml.cs`: added to `DocumentOverlays`, a real closable tab
  entry, `SwitchWorkspace(FixedTab.Admin)` now shows this view instead of the landing overlay,
  `ShowDocument` reloads all four cards each time it's shown (same per-show-refresh pattern as
  `HomeDashboardDocument`/`VaultPanel`).

  **Compliance-critical audit-write-path check (issue's own item 2)** — confirmed by reading
  the real backend routes, not assumed:
  - Break-Glass admin-override ("issue/reissue secrets") **does** write a real audit row —
    `createAuditLog(...)` in `msp-break-glass.ts:322`, dedicated `break_glass_override_audit`
    trail also exposed per-customer in MyArchitect already (`OpenBreakGlassAuditRecordAsync`).
  - Approve/deny break-glass is the same admin-override action (there is no separate
    approve/deny endpoint in `msp-break-glass.ts` — the four real routes are: pending list,
    per-customer history, per-secret detail, admin-override).
  - **Real finding:** Consent grant/revoke/invite (`msp-consent.ts` — `invite-link`,
    `write-consent/start`, `sharepoint-consent/start`, `consent/revoke`) all call
    `createAuditLog(...)`, but that writes to `audit_logs` (`auditLogsTable`) — a **different**
    table than the one MyArchitect's own Audit Log view reads (`GET /api/msp/audit` →
    `msp_audit_logs`/`mspAuditLogsTable`). Same root-cause table split as #4061 (closed
    2026-09-14, fixed for the web MSP Console via a second "Business Action Log" tab reading
    `GET /api/audit-logs`) — MyArchitect never got the equivalent client, so these rows are
    real but currently unreachable from any MyArchitect view, and unlike Break-Glass, Consent
    has no dedicated per-customer audit-trail endpoint at all. Filed as **#4355** (parented
    under Epic #3454, labels `bug`+`security`, board status "AI Batter Up"), not fixed here —
    out of scope for this issue's "wire the existing real services into a dashboard" build.

  **Verification:** `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors (twice:
  before and after rebasing onto the current `origin/main` for push). A live GUI run was not
  performed — MyArchitect is an auth-gated WPF desktop app on Shane's active desktop; launching
  it would disrupt his session (same reasoning #4344's own DONE bookend documents). Verification
  here is the clean Debug build (compiles + validates all XAML/BAML) plus a static review of
  every service call and event wire-up added.

  **Shared-clone note:** worked in an isolated worktree (`C:/wt/ma-4348`, now removed) off
  `origin/main`, not the shared `MyArchitect-clone` checkout — which was diverged and, as of
  this session, sitting mid-rebase-conflict (`UU` on `MainWindow.xaml.cs`/`HomeDashboardView.xaml.cs`)
  from a different, unrelated concurrent session. Left entirely untouched. `git status --porcelain`
  in my own worktree was clean before removal.

  **This repo's own working tree** (`C:/wt/4348-q3013`, Shane-McCaw-MSP) is clean apart from
  this bookend file.
