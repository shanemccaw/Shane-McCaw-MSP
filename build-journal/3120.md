# #3120 — Wire real design system tokens into Shane's Life

- **Status:** ✅ DONE 2026-09-07T20:10:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** aaec5820b — Wire real design-system tokens into Shane's Life (#3120)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — porting the real design-system token files
  (`colors.css`, `typography.css`, `spacing.css`, `fonts.css`, `elevation.css`) from
  `Design/design_handoff_shanes_life/_ds/…/tokens/` into `web/shanes-life/public/tokens/`,
  then rewiring `public/app.css` + `public/index.html` to actually use them
  (`hsl(var(--...))`) instead of the invented placeholder palette, applying the
  README's shared chrome (padding/gaps/card/section-label spec) and real Inter type
  scale, and confirming the #3117 fox critter still renders against the real theme.
- 2026-09-07T20:10:00Z ✅ DONE — Ported the 5 token files verbatim (diff-confirmed
  byte-identical to the source) into `public/tokens/`, added a `tokens.css` @import
  manifest mirroring the design system's own `styles.css` entry point, and linked it
  ahead of `app.css`/`share.html`. Rewrote `app.css` to consume the real tokens
  directly (`hsl(var(--background))`, `hsl(var(--card))`, `hsl(var(--card-border))`,
  `hsl(var(--muted-foreground))`, `hsl(var(--primary))`, Inter via `var(--font-sans)`)
  in place of the old invented hex palette (`#101317`, `#f4b35a`, etc — all confirmed
  gone by grep). Applied the handoff README's shared chrome verbatim: 62px `.view` top
  padding, 14px section gaps, 16px card/tile radius, 54px tile row min-height, 11px/600
  /.1em uppercase section labels, 17/700 header title, 15/600 row titles, 13px muted
  sublines. `<html>` now carries the design system's own `.dark` scope permanently
  (app has no light mode). Updated the two places that can't read a CSS custom
  property (the `theme-color` meta tag in index.html/share.html, and
  manifest.webmanifest's background/theme color) to the real computed
  `--background` dark hex, `#07182c`. Rebased onto origin/main after #3119 (critter
  roster) landed underneath this work; resolved one real conflict in `app.css`
  (kept #3119's critter/peeker/watermark CSS and its `.empty svg` markup change,
  recolored with the real tokens instead of the retired `var(--muted)`).

  **Verification (real, not skipped):** started the actual `src/server.mjs` against
  the local `finances` Postgres DB (migrations already applied, 7 up to date) and hit
  it with a real headless Chrome (`chrome.exe --headless=new`, already installed on
  this box — no download) screenshot. Sign-in screen matches
  `Design/design_handoff_shanes_life/screenshots/01-sign-in.png`: dark navy
  background, navy card, Electric-Blue primary button, correct type. Also verified
  the Today tray's *shared chrome* (header, `NEXT`/`LATER, BY MOMENT` section labels,
  card/tile rows) against `02-today-tray.png` using a scratch harness that loads the
  real running server's `tokens.css`/`app.css` with sample row markup (room-specific
  live data is out of scope per the issue and not yet built) — matches. Grep-confirmed
  zero hardcoded hex remains outside `public/tokens/` except the two literal
  `theme-color`/manifest values (can't reference CSS vars) and the #3117 fox critter's
  own fixed illustration palette (explicitly a separate, fixed palette per the
  handoff's Critters section, not a design token) — the fox still renders correctly
  against the new background. Scratch verification artifacts (`.env` with the local
  DB password, screenshots, scratch HTML) were deleted before finishing; working tree
  left clean. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE.
