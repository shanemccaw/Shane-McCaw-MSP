# #2368 — Panel: search by shot name or screen

- **Status:** ✅ DONE 2026-09-02T19:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 6c49832d9 (rebased onto main as 51ccc7618)

## Log
- 2026-09-02T19:50:00Z ⏳ IN FLIGHT — Feature #2367 (Shot Vault) item 1. Real audit: DesktopScreenClipService
  saves loose PNGs with no screen/name metadata. Adding a real capture-time "screen" tag
  (System.Windows.Forms.Screen.FromRectangle) encoded into the filename, parsed back out by
  ShotVaultService, and a real live search box (filename or screen) in the rail panel — no fixture data.
- 2026-09-02T19:58:00Z ✅ DONE — DesktopScreenClipService.SaveToDisk now resolves the real monitor a
  capture rect landed on via System.Windows.Forms.Screen.FromRectangle/AllScreens and writes it into the
  filename as `_screenN` (empty tag, not a guess, if resolution fails). ShotVaultService parses that tag
  into a new ShotVaultItem.Screen ("Unknown" for pre-#2368/hand-dropped shots), adds ListRuns(shots)
  overload for re-grouping a filtered subset, and MatchesQuery(shot, query) — case-insensitive substring
  match against real filename or real screen label. MainWindow.ShotVaultPanel.cs wires a new
  ShotVaultSearchBox (same TextBox convention as ApiExplorerSearchBox) that live-filters
  RenderShotVaultPanel's runs/tiles; DIFF badges stay computed over the full unfiltered chronological
  list per #2371's contract. `dotnet build ShaneBuilder.csproj` — 0 warnings, 0 errors. Branch verified
  merged into origin/main (verify-branch-merged.mjs), working tree clean. No fixture/hardcoded data —
  every field on screen is real filename/timestamp/screen-monitor data.
