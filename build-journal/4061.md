# #4061 — Audit Log module: second tab for the real `audit_logs` business trail

- **Status:** ✅ DONE 2026-09-14T16:05:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 59de40700 (IN FLIGHT bookend), 75ef620de (real implementation)

## Log
- 2026-09-14T15:35:00Z ⏳ IN FLIGHT — widen `GET /api/audit-logs` gate from `requireAdmin` to `requireCapability("ladder.msp-admin")`, add a second "Business Actions" tab to MSP Console's Audit Log module (both mounts: per-tenant leaf + `/ops/audit`), wired to that endpoint via a new independent hook/component. Tab 1 (`msp_audit_logs`) left untouched.
- 2026-09-14T16:05:00Z ✅ DONE — implemented and pushed as 75ef620de:
  - `artifacts/api-server/src/routes/audit-logs.ts`: `GET /audit-logs` gate widened `requireAdmin` -> `requireCapability("ladder.msp-admin")`.
  - `artifacts/msp-console/src/api/business-audit-log-api.ts` (new): `useBusinessAuditLog` hook wrapping the now-reachable endpoint.
  - `artifacts/msp-console/src/console/modules/BusinessActionLog.tsx` (new): the second tab's UI, independent fetch/filters/table.
  - `artifacts/msp-console/src/console/modules/AuditLog.tsx`: original body extracted unchanged into `SecurityEventsTab`; new `AuditLog` wrapper renders a tab switcher between it and `BusinessActionLog`. Both mounts in `ConsoleShell.tsx` needed no change — they already render `<AuditLog .../>`.
  - Real verification (not simulated): `tsc --noEmit` clean in both `artifacts/api-server` and `artifacts/msp-console`. Direct local-Postgres query confirmed `audit_logs` has 366 real rows (6 categorized) and the `ladder.msp-admin` capability row is seeded (`msp_feature_role_mapping`, `msp_id IS NULL`), so the gate won't fail-closed 503. Ran `userClearsLadderCapability()` directly (via `tsx`, against the live local RBAC snapshot, scratch script deleted after) for three real cases: `{role:"client", mspRole:"MSPAdmin"}` (the exact case `requireAdmin` used to 403) -> `allow`; `{role:"admin"}` (admin-panel's existing caller) -> `allow`, no regression; `{role:"client"}` with no mspRole -> `deny`, correctly still blocked. Did not stand up the full dev-server stack (out of scope for a medium-effort auth-gate change already proven at the decision-function level against real data).
  - `git status --porcelain` clean; `node scripts/dev-server/verify-branch-merged.mjs` confirmed `agent/4061-q2748` (75ef620d) is an ancestor of `origin/main`.
  - No findings filed this build — no genuine out-of-scope bug hit.
