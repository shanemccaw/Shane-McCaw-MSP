# #3001 — URGENT: live-verify Location/Conservation toggle actually gates network-heavy ops

- **Status:** ✅ DONE 2026-09-06T15:20:12Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 9aaa095bb (IN FLIGHT bookend), plus this DONE bookend commit. Investigation only — no product/BuildConsole code changed.

## Log
- 2026-09-06T15:13:27Z ⏳ IN FLIGHT — Investigation-only audit of the Location (Home/Rental) + Conservation toggle. Three real verification points: (1) what Conservation:On concretely gates in UsageAutomationService/AccountCapPolicy; (2) whether the toggle genuinely persists and is read at the moments that matter (startup, mid-session switch); (3) which real network-heavy operations (worktree provisioning, pnpm installs, GitHub polling, WebView2 preloads) are NOT covered by this gate but arguably should be. Report honest gaps.
- 2026-09-06T15:20:12Z ✅ DONE — Audit complete; full report posted on #3001. Findings:
  - The issue conflates TWO orthogonal toggles. **Conservation Cap (#1989)** gates model tier (parks Opus/Fable/xhigh to protect the weekly token budget; sole consumer QueueWatcherService.cs:1748) — NOT bandwidth. **Location=Rental (#1986)** is the real metered/bandwidth gate.
  - **Location=Rental hard enforcement is narrow but covers the risk that matters:** `.pnpmfile.cjs:184-199` hard-aborts `pnpm install`/`--force` (~1,120 pkgs) when BUILD_NETWORK=metered; version-update deploy held behind a Shane-only one-shot override (MainWindow.VersionUpdate.cs:186-195). BUILD_NETWORK is injected into every launched build (QueueWatcherService.cs:1898-1917) but read by exactly ONE consumer repo-wide — `.pnpmfile.cjs`; for all other vectors it is an advisory signal.
  - **Persistence PASS:** read at startup (MainWindow ctor 407/418), fail-safe defaults (Home/OFF on missing/corrupt), atomic Save (temp+Move), retrying Load (#2770), title-bar toggle + Settings combo write the same field and cross-sync; every gate reads Load() fresh (no caching) so mid-session switch applies to the next launched build with no restart.
  - **Gaps (Point 3):** worktree provisioning + 30s version check are LOCAL-only git ops (no network — correctly out of scope). Genuinely uncovered by the hard gate: dotnet/NuGet outside deploy, npm/yarn install, git clone, direct curl/Invoke-WebRequest, usage-meter poll, WebView2 loads — advisory only. Filed as **#3006** (sub-issue of #1789, bug, board=AI Batter Up, not URGENT — dominant risk covered).
  - **Action for Shane:** flip the Location toggle to Rental on arrival (defaults Home, never auto-detected by design); after that pnpm installs are refused and builds carry BUILD_NETWORK=metered.
  - Verification: grounded in real file:line reads; no code changed so no build needed. Working tree clean apart from this bookend.
