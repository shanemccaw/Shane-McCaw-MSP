# #2205 — Feature: Settings-in-place cross-cutting rule (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:55:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — the freeze applies to desktop/BuildConsole only)
- **Started:** 2026-09-02
- **Commit(s):** fbf900b93 (pushed as a09c4ca2b after rebase onto origin/main)

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — Blockers #2204 and #2194 both confirmed CLOSED with real
  landed code (`build-journal/2204.md`, `build-journal/2194.md`). Real audit of #2194's landed
  auth-finding UI done: `GitDoctorService.CheckAuthAsync` (Services/GitDoctorService.cs:330) finds
  "GitHub rejected your credentials" and offers one remedy — `git credential-manager github login`
  (an interactive OS browser flow) — no paste-a-PAT path exists today. Real audit of the Graph
  tenant picker done: `RenderApiExplorerAuthRow` (MainWindow.xaml.cs:8773, GraphRead/GraphWrite
  branch) renders a plain text row off `ApiExplorerService.ResolveGraphCredentials`, which reads
  `GRAPH_TENANT_ID`/`GRAPH_CLIENT_ID`/`GRAPH_CLIENT_SECRET` only from `.env.local` — no gear, no
  inline add path, and no read from the Settings store (`SettingsStoreService`, #2204's real
  store at `%AppData%\ShaneBuilder\settings.json` + Windows Credential Manager for secrets).
  Building: (1) a Settings-store-backed override for the Graph App Registration
  (`graph:tenantId`/`graph:clientId`/`secret:graph:clientSecret`/`graph:tenantLabel`), read by
  `ResolveGraphCredentials` ahead of `.env.local`, with a gear + inline add/edit form on the
  tenant picker row that writes it; (2) a paste-a-fresh-PAT field on Git Doctor's auth finding
  that applies the PAT as the real git credential (`git credential approve` via stdin) AND writes
  it to the same `secret:token:github` key Settings' API Tokens card already reads/writes.
- 2026-09-02T15:55:00Z ✅ DONE — Shipped both items, both write to the real
  `SettingsStoreService` store (`%AppData%\ShaneBuilder\settings.json` + Windows Credential
  Manager for secrets), not a local copy:
  1. `Services/ApiExplorerService.cs` — `ResolveGraphCredentials(repoRoot, settingsStore = null)`
     now checks a Settings-store override (`graph:tenantId`/`graph:clientId`/
     `secret:graph:clientSecret`/`graph:tenantLabel`) first, falling back to `.env.local` only if
     unset. `MainWindow.xaml.cs`'s `RenderApiExplorerAuthRow` (GraphRead/GraphWrite tenant row)
     gets a ⚙ button that opens `BuildGraphAppRegistrationEditor()` — an inline
     Tenant ID/Client ID/Client secret/Label form with Save (validates all three required fields
     unless a secret is already stored) and Clear (reverts to `.env.local`).
  2. `Services/GitDoctorService.cs` — new `ApplyGitHubPatAsync(pat)` runs
     `git credential approve` over stdin (protocol/host/username/password), the same plumbing
     command a successful interactive login itself calls, landing the credential in Windows
     Credential Manager without an OS browser flow, then re-verifies with the same
     `ls-remote --exit-code origin -h` the "auth" check itself runs. `MainWindow.xaml.cs`'s
     `RenderGitDoctorDetail` renders `BuildGitDoctorPastePatCard` under the existing remedy cards
     only for `sel.CheckId == "auth"`, with a masked PAT box + Apply button
     (`ApplyGitDoctorPastedPatAsync`) that also writes the PAT to `secret:token:github` — the
     exact key Settings' API Tokens card (`BuildTokenCard`, #2204) already reads/writes — and
     re-runs `LoadGitDoctorChecksAsync()` on success so the finding clears for real.
  Verification: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors. Not
  live-exercised against a genuinely rejected GitHub credential or a missing Graph app
  registration (neither condition is present on this machine right now — the local repo's stored
  git credential and `.env.local`'s Graph creds are both currently valid) — the code paths were
  read-verified against the real, already-proven git-credential-approve/ls-remote plumbing and
  the real Settings-store Get/Set contract used identically elsewhere in this file (`BuildTokenCard`,
  the autofill-lock account editor). Branch verified merged into `origin/main` (`a09c4ca2b`) via
  `verify-branch-merged.mjs`; `git status --porcelain` clean. Commented on #2205 and flipped
  `in-flight`→`complete` labels. No findings filed — nothing genuinely out of scope surfaced.
