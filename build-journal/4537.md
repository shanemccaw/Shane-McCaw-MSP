# #4537 — count transform counts array-field PRESENCE not CONTENT

- **Status:** ✅ DONE 2026-09-17T23:55:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** f48e0032a (plan+bookend), 863415a0d (fix+test) — landed on origin/main at ce29c316f

## Log
- 2026-09-17T22:20:00Z ⏳ IN FLIGHT — pulled issue #4537 + comments, wrote build-journal/4537-plan.md, confirmed live DB state of the two named checks' mapping (`cost:underutilized-premium`, `cost:group-based-licensing-adoption`), audited all 112 `"transform": "count"` mapping rows for other array-typed `sourceField`s. About to fix `case "count"` in monitor-executor.ts.
- 2026-09-17T23:55:00Z ✅ DONE — fixed `case "count"` in `artifacts/api-server/src/lib/monitor-executor.ts` to skip empty arrays (not just null/undefined), matching the array-detection pattern already used by `countEmptyArray`. Scalar-field `count` behavior is byte-identical to before. Added a regression test asserting count on an array-typed sourceField excludes empty-array items while still counting non-empty ones. Full `monitor-executor.test.ts` + `monitor-evidence-capture-2923.test.ts` suite: 250/250 passed. `pnpm --filter api-server run typecheck`: passed. Filed #4601 (parented under #1489, labeled `bug`, board status "AI Batter Up") for the one other real finding from the audit: `identity:mfa-method-breakdown` shares the array-presence defect on `methodsRegistered` and additionally has empty `severity_rules` and wrong semantics for its "legacy vs phishing-resistant" description — a check-design fix, not a transform-level bug, so filed separately rather than folded in here. Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.

## Changes Made
- `artifacts/api-server/src/lib/monitor-executor.ts` — `case "count"`: skip values where `Array.isArray(v) && v.length === 0`, in addition to the existing `v == null` skip.
- `artifacts/api-server/src/lib/__tests__/monitor-executor.test.ts` — new test `#4537: count on an array-typed sourceField excludes items whose array is empty`.
- No schema/migration change — `monitor_checks.mapping` rows for the two named checks were already correct; the bug was purely in the transform's implementation.
- Filed finding #4601 (sub-issue of #1489) for `identity:mfa-method-breakdown`'s related-but-separate defects.

## Verification Results
- `pnpm exec vitest run src/lib/__tests__/monitor-executor.test.ts src/lib/monitor-evidence-capture-2923.test.ts` — 250/250 passed.
- `pnpm --filter api-server run typecheck` — passed, no errors.
- `node scripts/dev-server/verify-branch-merged.mjs` — OK, branch merged into origin/main.
- `git status --porcelain` — clean.
