# #3558 — Tenant Scores and Results (MSP Console): build the real backend endpoint

- **Status:** ✅ DONE 2026-09-11T00:24:07Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 799c9c01f (rebased onto main; original commit 485d4b519)

## Log
- 2026-09-11T00:07:06Z ⏳ IN FLIGHT — investigating existing `/portal/dashboard` scoring logic, `tenantEngineSnapshotsTable`, and existing `/api/msp/*` ownership-check patterns before building `GET /api/msp/customers/:customerId/scores`.
- 2026-09-11T00:24:07Z ✅ DONE — added `artifacts/api-server/src/routes/msp-customer-scores.ts`: real `GET /api/msp/customers/:customerId/scores`, `requireCapability("ladder.msp-operator")`, ownership via `assertCustomerAccess` (same chokepoint as `msp-remediation-tracker-scores.ts`/`msp-engine-history.ts`), reading real `tenant_engine_snapshots` + `msp_diagnostic_findings` rows — same composite/per-engine/pillar shape as the customer-facing `/portal/dashboard`. Confirmed decision (per #3557's own body and this issue's dispatch): the customer-side #164 paywall does NOT apply to the MSP operator view — findings/recommendations are always full text. Registered in `routes/index.ts`. Added `msp-customer-scores.test.ts` (4 tests: 400 on malformed customerId, 404 without disclosure for an out-of-book customer, real composite/per-engine/pillar scores unredacted for an owned customer, honest zero-composite payload for a customer with no snapshots yet) — run directly via `pnpm exec vitest run src/routes/msp-customer-scores.test.ts` inside `artifacts/api-server`: **4/4 passed**. `tsc -p tsconfig.json --noEmit` (api-server) passes clean. No schema/migration needed — `tenant_engine_snapshots` and `msp_diagnostic_findings` already existed with the needed columns. Local DB has 0 rows in `tenant_engine_snapshots` (confirmed via direct psql) — nothing to seed for this build, but noted as an honest gap: this endpoint can't be live-eyeballed against real local data until a real scan populates that table. Rebased onto `origin/main` (was a few commits behind) and pushed directly to `main` — `799c9c01f`. `verify-branch-merged.mjs` confirms ancestor of `origin/main`. `git status --porcelain` clean.
