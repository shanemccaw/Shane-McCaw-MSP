# #2213 — Feature: Git Map — mini + full (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T13:05:00Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** `15239d898` (verified ancestor of origin/main)

## Log
- 2026-09-02T12:00:00Z ⏳ IN FLIGHT — Real audit before writing code. `README-ClaudeChat.md`
  (the doc §6.5 cites) does not exist anywhere in the repo or git history — confirmed via
  `git log --all` and filesystem search. This is already a known, filed, still-OPEN finding:
  #2227 ("README-ClaudeChat.md (549-line #2209 source spec) is not committed to the repo"),
  filed by the #2209 session. Not re-filing a duplicate. The mini-panel layout IS usable
  though: #2213's own issue body carries a direct paraphrase of §6.5 ("pending cross-epic
  questions on top, then Focus Build card ... Started-and-Dropped card ... Epics list ...
  expanded epic shows its features as cards"). The full-document layout has NO surviving
  spec anywhere (#2213's own body: "Full spec for the standalone document isn't in this
  README ... real audit needed"). Per the build prompt's explicit instruction, building
  mini fully and flagging full-document layout as an open gap rather than guessing it.
  Architecture audit: no generic "tool rail" container exists yet (#2209 shipped only a
  partial seam — `_openRailToolByTab` dict, unused). The one real, working precedent for a
  toggleable rail-mini-panel is the existing Detected Items panel (`DetectedItemsColumn`,
  wrench-menu toggle, `RenderDetectedItemsAsync`) — following that exact pattern for Git
  Map's mini panel. For "full document": Git Doctor/Log Viewer/Markdown Viewer/Screenshot
  Clipper each already have a full tab (`TabDef` kind + Dock + command-palette entry) with
  "mini" tracked as a SEPARATE later issue (#2218/#2219/#2220) — Git Map is the reverse
  (neither exists yet, both requested in this one issue). Single-source constraint: building
  one `GitMapService` read layer off `bt_epics` (Postgres) + `gh` CLI (sub-issues, in-flight/
  blocked labels, board status) + `bt_build_queue` (builds-since counts) + git log timestamps
  on `build-journal/*.md`; both mini and the full tab call the same service, no second data
  path.
- 2026-09-02T13:05:00Z DONE — Shipped Services/GitMapService.cs (real gh-CLI epic/feature reads;
  bt_epics/bt_issues confirmed stale, last synced 2026-08-17, missing #2198 entirely, so NOT used)
  plus two new read-only ChatReadClient methods (GetMostRecentBuildQueueStatusAsync,
  CountDoneBuildsSinceAsync). Mini panel: new GitMapColumn/GitMapPanel flyout following the
  established Log Peek precedent exactly (glyph/name/maximize-2/close header, wrench-menu toggle),
  rendering pending cross-epic questions targeting the active epic (real, existing CrossEpicQuestion
  state), a Focus Build card (real epic sub-issue carrying the in-flight label, real open-gap
  count, real bt_build_queue status — no cross-process build-progress percent exists, confirmed
  BuildProgressTracker is in-process-only), a Started-and-Dropped card (real build-journal/*.md
  bookends still non-DONE for issues still OPEN on GitHub, real builds-since count off
  bt_build_queue), and an Epics list (real open "Epic:"-titled issues, active chat's epic pinned
  first tagged THIS CHAT, expand lazy-loads real sub-issues as feature cards with Focus/Send
  actions). Full document: new GitMapDock tab, deliberately reusing the SAME card-builder methods
  in one wide column rather than inventing a distinct layout — README-ClaudeChat.md (the doc that
  would define that layout) does not exist in the repo; confirmed still-open at #2227 (filed by the
  prior #2209 session), not re-filed. Verification: dotnet build clean (0 warnings, 0 errors) after
  three separate rebases onto concurrently-landing sibling mini-panel builds (#2214 Repo Health,
  #2215 SQL Runner, #2217 JSON Viewer/File Browser) that touched the same MainWindow.xaml/.cs
  flyout-column region — each rebase re-verified with a clean build before push. Did not launch the
  exe (same single-instance-mutex hazard #2209 documented). No test manifest — BuildConsole/
  ShaneBuilder is out of scope for those. Real data sources verified directly before writing code:
  `gh issue list --search "Epic in:title"` (client-filtered to a real ^epic: prefix — GitHub's
  search matches substrings), `gh api .../sub_issues --jq '[.[]|{number,title,state,labels}]'`,
  psql counts against local bt_build_queue/build-journal for three real Started-and-Dropped
  candidates (#2085, #1544, #1519 — all OPEN with non-DONE bookends). Committed 15239d898, pushed
  to origin/main, verify-branch-merged.mjs confirms ancestor, git status --porcelain clean.
