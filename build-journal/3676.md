# #3676 — Chats panel: replace New Chat modal (NewChatEpicDialog) with the real inline epic/gate picker panel

- **Status:** ⏳ IN FLIGHT 2026-09-11T17:09:19Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T17:09:19Z ⏳ IN FLIGHT — read issue #3676 + design handoff (`desktop/BuildConsole/design-reference/design_handoff_chats_panel/README.md`), confirmed real write path (`LeftSidebar.StartNewEpicChat` → `EpicChatRequested` → `AssociateChatWithIssueAsync`/`LinkChatToIssueAsync`, Git #2534), confirmed `NewChatEpicDialog`'s only real callers are `MainWindow.MenuNewChat_Click` (File > New Chat) and `MainWindow.StartSuccessorChat`'s fallback — neither is the Chats panel itself, whose own "New Chat" icon button is currently a no-op for the Chats view. About to add the real inline picker panel (full-width New Chat button + expanding panel below it, per the .dc.html/README) to `LeftSidebar.xaml`/`.xaml.cs`, wired to the real epic list (`_chatEpicById`) and the existing `StartNewEpicChat` write path, plus a new unassociated-chat path for "No feature yet — decide later".
