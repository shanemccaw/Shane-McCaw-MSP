# #3676 — Chats panel: replace New Chat modal (NewChatEpicDialog) with the real inline epic/gate picker panel

- **Status:** ✅ DONE 2026-09-11T17:26:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** b9971cbe2

## Log
- 2026-09-11T17:09:19Z ⏳ IN FLIGHT — read issue #3676 + design handoff (`desktop/BuildConsole/design-reference/design_handoff_chats_panel/README.md`), confirmed real write path (`LeftSidebar.StartNewEpicChat` → `EpicChatRequested` → `AssociateChatWithIssueAsync`/`LinkChatToIssueAsync`, Git #2534), confirmed `NewChatEpicDialog`'s only real callers are `MainWindow.MenuNewChat_Click` (File > New Chat) and `MainWindow.StartSuccessorChat`'s fallback — neither is the Chats panel itself, whose own "New Chat" icon button is currently a no-op for the Chats view. About to add the real inline picker panel (full-width New Chat button + expanding panel below it, per the .dc.html/README) to `LeftSidebar.xaml`/`.xaml.cs`, wired to the real epic list (`_chatEpicById`) and the existing `StartNewEpicChat` write path, plus a new unassociated-chat path for "No feature yet — decide later".
- 2026-09-11T17:26:00Z ✅ DONE — shipped in `b9971cbe2`. Added a real full-width "New Chat" button (`BtnNewChatPicker`) below the Chats panel's search field in `LeftSidebar.xaml`, whose click flips a `RotateTransform`'d chevron and reveals `NewChatPickerPanel` — an inline `#0E1420`/`#262C36`/8px-radius panel, not a popover/modal — built per the design handoff's "New Chat dropdown" section. `PopulateNewChatPickerHost` (LeftSidebar.xaml.cs) renders the explainer row, one real anchor row per open epic/gate from `_chatEpicById` (same real source the epic-header list renders from; label derived from the epic's real GitHub title, `EPIC:`/`GATE:` as authored), and the "No feature yet — decide later" dashed-dot closing row. An anchor row click calls the existing `StartNewEpicChat` (Git #2534's real `AssociateChatWithIssueAsync`/`LinkChatToIssueAsync` write path) unchanged; the decide-later row calls a new `StartNewUnassociatedChat`, which opens a chat via the same `EpicChatRequested` event with `associateIssueNumber: null` — lands in the existing "Unlinked" bucket, same as any chat opened unassociated today. `NewChatEpicDialog` is untouched and still reachable from its two other real callers (`MenuNewChat_Click` / File > New Chat, and `StartSuccessorChat`'s fallback) — deliberately out of this issue's scope (the Chats panel's own New Chat flow only), confirmed via grep before finishing. Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 errors (6 pre-existing unrelated warnings in `FocusModeBar.xaml.cs`). Per Git #2156, BuildConsole.exe itself is never launched from a build session to verify — build success + code read-through is the real verification path here. `node scripts/dev-server/verify-branch-merged.mjs` — OK, `agent/3676-q2338` (b9971cbe2) merged into `origin/main`. `git status --porcelain` — clean. No schema/backend change needed; no fixture/hardcoded data (epic list is live `_chatEpicById`, the real board fetch). No findings filed — no bugs/gaps hit outside this issue's own scope.
