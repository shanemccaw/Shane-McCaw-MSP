# #4406 — Buy.tsx code-expiry copy drift (hardcoded "ten minutes" vs real 15-minute TTL)

- **Status:** ✅ DONE 2026-09-16T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** e22ccb8eb84c4727186d71542bf16145f31f6b53

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — derive Buy.tsx's on-page code-expiry copy from the real `expiresAt` returned by send-verification-code, instead of the hardcoded "ten minutes" string, per the issue's own suggested direction.
- 2026-09-16T00:20:00Z ✅ DONE — added `codeExpiresAt: string | null` to Buy.tsx's `State`, captured `data.expiresAt` from the `send-verification-code` response in `sendCode`, and replaced the hardcoded "The code expires in ten minutes." with `codeExpiryText`, computed from `codeExpiresAt` (rounds up to whole minutes, falls back to "Check your email for the code." before a code has been sent). `pnpm run typecheck` in `artifacts/shane-mccaw-consulting` passes clean. Pushed to `origin/main` at e22ccb8e; `verify-branch-merged.mjs` confirms ancestor. Working tree clean.
