# #1620 — SOPs/Runbooks: GET /portal/hold-windows/:holdId/events - live per-window audit trail, no page calls it

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** a3f0b11b4 (bookend IN FLIGHT), 97873aafc (real work, post-push hash on origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigated. This is a duplicate finding of #1619 (both
  filed at #1493 contract-pack time against the same endpoint,
  `GET /api/portal/hold-windows/:holdId/events`, `portal-runbooks.ts:849-892`) — #1619
  from the Active Runbooks/hold-windows side, #1620 from the SOPs/Runbooks side. #1619
  (commit `a6dfc6e28`, merged to `origin/main`) already added `HoldWindowEvent` and
  `loadHoldEvents(holdId)` to `artifacts/portal/src/components/holds/useRunbooks.ts` —
  the one data seam this module also depends on — and its own log explicitly records
  "#1620 records the identical gap from the SOPs side; same endpoint, no separate work
  needed there." No new backend or hook work is needed for #1620.
- 2026-08-30 ✅ DONE — Confirmed via direct read of
  `artifacts/portal/src/components/holds/useRunbooks.ts` that `HoldWindowEvent` and
  `loadHoldEvents(holdId)` are present and correctly wrap the endpoint, exactly as
  #1619 recorded. `psql "$DATABASE_URL"` confirms real local state honestly: 0 rows in
  `portal_hold_windows` and 0 in `portal_hold_window_events` (matches #1619's own
  observation — no live data exists locally to demo the endpoint's payload against).
  `npx tsc -p artifacts/portal/tsconfig.json --noEmit` is clean. No page exists to call
  the hook (`Design/portal/` has no export for Active Runbooks/SOPs yet, per the
  #1485 module order and this build's own SCOPE STOP rule) — no page/route/component
  was created, matching #1619's stopping point. Updated
  `docs/runbooks-contract-pack.md` §6 to record #1620 as the duplicate SOPs-side
  finding covered by the same #1619 wire contract. `verify-branch-merged.mjs`
  confirms `97873aafc` merged into `origin/main`. Working tree clean
  (`git status --porcelain` empty). No new issues filed — this was a duplicate of an
  already-filed, already-resolved finding, not a new gap.
