# #1620 — SOPs/Runbooks: GET /portal/hold-windows/:holdId/events - live per-window audit trail, no page calls it

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigated. This is a duplicate finding of #1619 (both
  filed at #1493 contract-pack time against the same endpoint,
  `GET /api/portal/hold-windows/:holdId/events`, `portal-runbooks.ts:849-892`) — #1619
  from the Active Runbooks/hold-windows side, #1620 from the SOPs/Runbooks side. #1619
  (commit `a6dfc6e28`, merged to `origin/main`) already added `HoldWindowEvent` and
  `loadHoldEvents(holdId)` to `artifacts/portal/src/components/holds/useRunbooks.ts` —
  the one data seam this module also depends on — and its own log explicitly records
  "#1620 records the identical gap from the SOPs side; same endpoint, no separate work
  needed there." No new backend or hook work is needed for #1620.
