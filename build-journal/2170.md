# Git #2170 — Remove dead Build Queue Design Canvas button + orphaned folder

- **Status:** ⏳ IN FLIGHT 2026-09-01T00:00:00Z
- **Scope:** buildconsole

## Log
- 2026-09-01T00:00:00Z — IN FLIGHT. Audited: `BtnBuildQueueDesign`/`BuildQueueDesignToggleRequested` live in
  `desktop/BuildConsole/Controls/ActivityBar.xaml(.cs)` and `desktop/BuildConsole/MainWindow.xaml.cs`
  (`ToggleBuildQueueDesign`, `FindBuildQueueDesignPreviewExe`, `_buildQueueDesignProc` field). No `.sln` file
  exists anywhere in the repo currently, so there's no stale solution-project reference to clean up. The
  target folder `desktop/BuildQueueDesignPreview/` still has a real `.csproj` + code in git despite Shane
  having deleted it locally. Only historical mentions elsewhere are in `build-journal/2135.md` and
  `build-journal/2137.md` (prior sessions' own records — left untouched).
