# #3189 — nudges.mjs queueNudge cap-increment UPDATE was broken since #3136

- **Status:** ⏳ IN FLIGHT 2026-09-07T00:00:00Z
- **Scope:** platform (Shane's Life)
- **Started:** 2026-09-07
- **Commit(s):** none needed — already fixed by b70115e58 / 50493c6a7 (see log)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — investigating whether this finding is already resolved,
  per the session header's note that #3175/concurrent builds hitting the same shared-ledger
  problem tonight may already have landed a fix.
- 2026-09-07T00:05:00Z ✅ DONE — Confirmed already fixed. `web/shanes-life/src/core/nudges.mjs`
  `queueNudge()`'s cap-increment `UPDATE nudges SET ...` now binds only `[userId, overCap]` as
  `$1`/`$2` — the dangling unreferenced `cap` bind param described in the issue is gone. Git
  history shows this was fixed independently by two concurrent builds the same night: #3160
  (`b70115e58` / merged via `adc45b97d`) and #3161 (`50493c6a7` / merged via `f90e331b3`), both
  already on `main`. The code even carries an inline comment naming #3161/#3160/#3189
  explicitly as the same bug three sessions converged on. No code change required from this
  session — filing this bookend + issue comment to close the loop per the "file every finding"
  rule's intent (confirming resolution rather than duplicating work).
