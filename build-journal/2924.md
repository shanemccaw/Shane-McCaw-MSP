# #2924 — Dashboard theme incomplete + Critical Debts notes dump (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-05T00:55:37Z
- **Scope:** buildconsole (desktop/ShanesSurvival — a separate WPF app under desktop/, not the platform/portal)
- **Started:** 2026-09-05
- **Commit(s):** 8a9dea142 (main)

## Log
- 2026-09-05T00:37:55Z ⏳ IN FLIGHT — auditing DashboardWindow.xaml + InstrumentPanelTheme.xaml for the
  incomplete dark-theme gap (window chrome / section headers / ScrollViewer), and the Critical Debts
  card's raw `notes` dump; then fixing both and attempting a real screenshot.
- 2026-09-05T00:55:37Z ✅ DONE — a real interactive Windows desktop session IS available here (this
  is not a headless/CI environment); launched the real built exe, drove it with real UI Automation
  (invoked "Open Dashboard", enumerated the window by title), and captured a real PNG screenshot of
  the live `DashboardWindow` against real local Postgres data (same `finances` DB #2919 verified
  against) — confirming the exact bug from the issue's own screenshot: everything outside an
  explicitly-`Background`-set `Border` (the title row, "Critical Debts"/"Pay-Period Forecast"
  headers, the gaps between cards, the ScrollViewer chrome) rendered on plain white with
  light/muted text, while cards with an explicit `Background="{StaticResource PanelBackgroundBrush}"`
  rendered correctly dark.
  **Root cause, confirmed live via a temporary diagnostic (`EventManager.RegisterClassHandler` +
  `DependencyPropertyHelper.GetValueSource`, removed before finishing):** the app-wide
  `<Style TargetType="Window">` in `Theme/InstrumentPanelTheme.xaml` is real, correctly merged into
  `Application.Resources`, and resolvable via `TryFindResource(typeof(Window))` with the right
  `#FF1B2430` Background setter — but WPF's automatic implicit-style lookup was never actually
  applying it to any `Window` instance in this app: `MainWindow.Style` measured `null` and
  `Background` measured `#FFFFFFFF` (`BaseValueSource=DefaultStyle`, i.e. the OS default) at
  runtime. Adding an explicit `Style="{StaticResource {x:Type Window}}"` on the `<Window>` root
  element made `Style` resolve to the real theme style and `Background` become the real
  `#FF1B2430` — confirmed by the same diagnostic before removing it, and by a second real
  screenshot after the fix showing the whole window (title row, both section headers, the gaps
  between cards, the scrollable area) as one cohesive dark theme with no white regions left.
  Applied that same explicit `Style` reference to all five real `Window`-derived XAML files in
  the app (`MainWindow.xaml`, `Dashboard/DashboardWindow.xaml`, `Accounts/AccountRoleWindow.xaml`,
  `Plaid/PlaidLinkWindow.xaml`, `Settings/SettingsWindow.xaml`) — the same explicit-Style gap #2919
  didn't catch, real per the issue's own audit request. Also added a real `ScrollViewer` Style
  (`WindowBackgroundBrush`) and a themed flat `ScrollBar`/`Thumb`/`RepeatButton` template to
  `InstrumentPanelTheme.xaml` so the scrollable-area chrome and scrollbar track/thumb match the
  dark palette instead of falling back to the default light system scrollbar.
  Critical Debts card: the raw `debts.notes` dump (verbatim, always-visible, wrapped body text)
  is replaced with a collapsed-by-default `Expander` labeled "Details" — the short real status
  line (`FormatDebtStatus`: e.g. "Delinquent — 150 day(s) past due") and real balance/minimum
  payment (`BuildCriticalDebtRow`'s existing `statusBlock`/`balanceBlock`) stay as the card's
  primary always-visible body exactly as they already were; the full real notes text (servicer
  confirmations, payment-plan mechanics, dates) is still reachable one click away, not deleted.
  No new backend/data-shape changes needed — this reused the two fields already rendered.
  **Real screenshot verification, this time actually done** (superseding #2919's "no real
  screenshot capture was possible" gap): launched the built exe twice (before/after the fix) via
  `Start-Process` + `System.Windows.Automation` (`InvokePattern` on the real "Open Dashboard"
  button) + `EnumWindows`-by-title to find the real top-level `DashboardWindow`, then
  `Graphics.CopyFromScreen` against its real `GetWindowRect` bounds — both screenshots read visually
  as expected (before: white banner/header regions exactly matching the issue's description;
  after: one cohesive dark instrument-panel look, Critical Debts cards showing a short real status
  line plus a "Details" chevron instead of the raw notes wall of text). Screenshots were scratch
  files under `C:\temp\ss-shots\` (outside the repo, not committed) and have been deleted after
  review — not part of the deliverable. One incidental capture during script debugging grabbed an
  unrelated foreground window (a different, unrelated chat conversation that happened to have
  focus) instead of the app; that file was deleted immediately without being examined further or
  used for anything, noted here for honesty.
  `dotnet build ShanesSurvival.sln` clean in both Debug and Release (0 warnings/0 errors).
  `git status --porcelain` clean (only this bookend + the real fix's own files — the temporary
  `App.xaml.cs` diagnostic was fully reverted to its original content before committing, confirmed
  via `git diff` showing zero net change to that file, so it is not part of the commit). Branch
  verified merged into `origin/main` via `verify-branch-merged.mjs` (`8a9dea142`) before writing
  this DONE — pushed directly to `main` per the worktree-isolation convention (rebased onto
  `origin/main`, fast-forward push, no conflicts).
