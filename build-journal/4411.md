# #4411 — BuildRollupRow: clickable ❓ needs-attention button

- **Status:** ✅ DONE 2026-09-16T18:41:00Z
- **Scope:** buildconsole

## Log
- 2026-09-16T18:23:00Z — Pulled #4411 body + #4368's body for real file structure. Read
  `BuildRollupRow` (desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs) in full —
  confirmed `needsAttention` pill is currently a plain `TextBlock` at line ~4659, next to
  the `✈` `sendButton` (landedEligible-gated). `_sentVerifyingByBuildSet` (line 293) is the
  ✈ button's own sent-tracking dict — per the issue, must NOT be reused for the new button.
  `SplitByBookendVerification`/`GetUnsentVerifying` confirmed as the real, already-computed
  source for `needsAttention`. `BuildEpicGroupHeader`'s own aggregate needs-attention text
  (line ~4124) is explicitly out of scope per the issue body — not touching it.

## Plan
- Add `_sentNeedsAttentionByBuildSet` dict (own tracking, same shape as `_sentVerifyingByBuildSet`).
- Add a `NeedsAttentionSentSignature` field to `RollupRowKey` so the pooled row rebuilds when
  this new dict changes for its key (mirrors how `LandedSignature` already does this for ✈).
- Replace the needs-attention `TextBlock` in `BuildRollupRow` with a `Button` (❓ glyph),
  styled/positioned like `sendButton`, gated on `needsAttentionUnsent.Count > 0` (absent when 0).
- Click handler sends `needsAttentionUnsent`'s live issue numbers via the same
  `SendBuildSetVerifyingRequested` pipeline, distinct wording ("needs attention — no
  verified DONE bookend yet" vs. the ✈ button's "landed"), marks sent in the new dict only.

## Changes Made
- `desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`:
  - Added `_sentNeedsAttentionByBuildSet` (own dict, separate from `_sentVerifyingByBuildSet`).
  - Added `NeedsAttentionSentSignature` to `RollupRowKey` + its call-site computation, so the
    pooled row rebuilds when this new dict changes for its key.
  - Replaced the plain `needsAttentionPill` `TextBlock` in `BuildRollupRow` with a clickable
    `Button` (❓ glyph + live count), styled/positioned identically to the ✈ `sendButton`;
    absent (not disabled) when `needsAttentionUnsent.Count == 0`.
  - Click handler sends the row's live `needsAttentionUnsent` issue numbers through the same
    `SendBuildSetVerifyingRequested`/`ShowStatus` pipeline the ✈ button uses, with distinct
    "needs attention (no verified bookend yet)" wording, and marks sent only in
    `_sentNeedsAttentionByBuildSet` — never `_sentVerifyingByBuildSet` — per the issue's
    explicit design constraint.
  - Did NOT touch `BuildEpicGroupHeader`'s own aggregate needs-attention text — explicitly
    out of scope per the issue body (ask-Shane-first).

## Verification Results
- `dotnet build -c Debug` in `desktop/BuildConsole`: **Build succeeded, 0 Warning(s), 0 Error(s)**.
- `desktop/BuildConsole` is explicitly out of scope for test manifests (CLAUDE.md Test
  Coverage section) — a clean build is the real verification for this WPF change.
- `node scripts/dev-server/verify-branch-merged.mjs`: OK — merged into `origin/main`
  (`a6b24f3e3`).
- `git status --porcelain`: clean.

## Commits
- `46e770897` — Bookend: #4411 IN FLIGHT
- `a6b24f3e3` — BuildRollupRow: clickable ❓ needs-attention button (#4411)
