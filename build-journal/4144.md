# #4144 — Hard-delete leftover soft-deleted POA&M row (id 151) from testbed tenant

- **Status:** ✅ DONE 2026-09-14T23:29:26Z
- **Scope:** platform
- **Started:** 2026-09-14

## Log
- 2026-09-14T23:20:00Z ⏳ IN FLIGHT — Confirmed live via direct `DATABASE_URL` query that `msp_poams` id 151 still existed and matched this issue's description exactly: `title = "Regression harness plan -- safe to delete"`, `poam_id = POAM-2026-151`, `tenant_id = c4c814d4-3afe-441e-9145-62461d0a4fd3` (confirmed this is `tenants.id = 1`, the sanctioned testbed tenant `mccawsoft2.onmicrosoft.com`), already soft-deleted (`deleted_at` set). `msp_poam_milestones` had 0 rows referencing it.
- 2026-09-14T23:22:00Z ✅ DONE — Hard-deleted: `DELETE FROM msp_poam_milestones WHERE poam_id = 151;` (0 rows) then `DELETE FROM msp_poams WHERE id = 151;` (1 row deleted).
- Origin search: grepped the tree for `"Regression harness plan"` (no literal match — not hardcoded fixture text in any file) and for live-db tests touching `msp_poams`. Found the origin's *discoverer*, not its *writer*: `artifacts/api-server/src/lib/shanebot-engine-batch-a.live-db.test.ts` (from build #4125, `build-journal/4125.md`) is a **read-only** live-db test that queries this tenant's real `msp_poams` rows filtered to non-deleted, and its own comment says "the one seeded row on this tenant is soft-deleted regression fixture data" — i.e. #4125 found this row already sitting there during its live verification and is the build that filed #4144 (this issue) for cleanup. #4125's test code is correctly written (real query, filters `deleted_at IS NULL`, doesn't write to tenant 1). The other two live-db POA&M tests found (`msp-poam-rbd-conversion.live-db.test.ts`, `msp-retention-queue-poam-producer.live-db.test.ts`, and `msp-poams-role-floor.live-db.test.ts`) all correctly provision their own synthetic scoped MSP/tenant per run with `afterAll` hard-delete cleanup — none of them write into tenant 1.
- **No further origin identifiable within reasonable bounds** — whatever actually inserted+soft-deleted row 151 predates #4125 and left no other trace in the current tree or recent build-journal entries. Per the issue's own instruction, not guessing at a fix for code that can't be identified. Row is confirmed gone; no code changes were needed or made.

Working tree: no files modified other than this bookend. `git status --porcelain` clean prior to this commit.
