# #1558 — SOPs/Runbooks: Per-tenant custom steps as an overlay on a versioned definition

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Building the settled architecture from #1558's own body and
  the sops-contract-pack §5 row: definition (`msp_sops`, MSP-authored, versioned) +
  per-tenant overlay (new `portal_sop_custom_steps`), precedent
  `portal_ownership_assignments`. Also closing the run's own half of the
  version-ambiguity requirement — `msp_sop_runs.sop_version`, captured server-side at
  run start — since #1558's text explicitly ties the overlay to "which version of the
  MSP procedure was actually followed on a given run". Schema + additive migration +
  writer wiring + read/write wire contract; ends at the wire contract per the SCOPE
  STOP (no page exists under `artifacts/portal`, `Design/portal/` carries no export for
  this module).
