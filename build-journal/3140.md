# #3140 — Renaming an applied migration leaves a stale schema_migrations row and silently re-runs it

- **Status:** ✅ DONE 2026-09-08T01:30:00Z
- **Scope:** both
- **Started:** 2026-09-08
- **Commit(s):** 2bb9fcf77 (bookend IN FLIGHT), b39f853e2 (fix, rebased/pushed to main as 9794e2e9)

## Log
- 2026-09-08T01:09:05Z ⏳ IN FLIGHT — read `web/shanes-life/src/migrate.mjs` and
  `desktop/ShanesSurvival/.../MigrationRunner.cs`. Going with the issue's first proposed
  direction (loud fail-closed refusal) rather than the stable-id header: it needs no ledger
  schema change and directly turns the silent re-run into a readable stop before anything
  executes. Adding `assertNoOrphanLedgerRows` to `scripts/check-migration-numbers.mjs`
  (shared by both runners already for the #3118 cross-directory number check), wiring it into
  `migrate.mjs`, and a parallel `FindOrphanLedgerFilenamesError` in `MigrationRunner.cs`.
- 2026-09-08T01:30:00Z ✅ DONE — Both runners now refuse to run, loudly, before applying
  anything, if `schema_migrations` holds a filename that doesn't exist in either migrations
  directory. Verified: (1) unit-level call of `findOrphanLedgerFilenames`/`assertNoOrphanLedgerRows`
  against the exact repro in the issue body (`024_recipes.sql` renamed to `025_recipes.sql`)
  correctly flags `024_recipes.sql` as orphaned; (2) same functions against the real current
  file lists of both migrations directories (31 files, the closest proxy to a healthy ledger
  available without live `finances` DB credentials in this worktree) return zero false
  positives; (3) `dotnet build src/ShanesSurvival.App/ShanesSurvival.App.csproj` succeeds with
  0 warnings/0 errors. Did not run against the live `finances` Postgres database itself — no
  `.env`/`DATABASE_URL` for it exists in this worktree (Shane's Life's own env file is
  gitignored and wasn't present), and the repo's shared local Postgres (`shanemccawmsp`, from
  root `.env.local`) is the unrelated MSP-platform database, not this personal side project's
  `finances` DB — so this stayed at the unit-test level described above rather than a live
  end-to-end run. Pushed straight to `main` (per Git conventions) as commit `9794e2e9`
  (rebase of `b39f853e2` onto `origin/main`). `git status --porcelain` clean.
