# #4150 — MSP Console navigation restructure (2026-09-15 design refresh)

- **Status:** ✅ DONE 2026-09-15T00:24:28Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** f53a7176a (nav restructure), 1a83416c9→e70f7e08f (post-rebase fix for concurrent #4152 removal)

## Log
- 2026-09-15T00:18:59Z ⏳ IN FLIGHT — reorder `g.access`/`g.comm` CHILD_GROUPS and add MSP_GROUPS (Client Delivery / Access & Accounts / Billing & Lifecycle) to `nav.ts`, wire TreeSidebar/treeModel/ConsoleShell to render the new Operations grouping.
- 2026-09-15T00:24:28Z ✅ DONE — `g.access` reordered (Team → OU Assignment → Break-glass → Launch Control → Webhooks → Azure Credential); `g.comm` reordered (Contracts → Offers & SOWs → Status Reports → Marketplace → Documents → Billing). Added `MSP_GROUPS`/`MSP_TOP_LEVEL_IDS`/`groupForMspPage` to nav.ts (three named collapsible Operations groups reusing the existing `Group` type: Client Delivery, Access & Accounts, Billing & Lifecycle); Executive view/Projects/Retainer hours stay top-level. Wired `treeModel.ts` (`buildTreeNodes` gained an `openMspGroups` param + group-header/child rendering) and `ConsoleShell.tsx` (`openMspGroups` state, `toggleMspGroup` handler, deep-link auto-expand) to render/toggle the new grouping with the same collapsible pattern `CHILD_GROUPS` already uses per tenant. Did not touch Workflows/Agents placement or Azure Credential's position, per issue scope. #4152 (Workflows/Agents removal) landed concurrently mid-build; rebased cleanly, then fixed nav.ts/treeModel.ts/nav.test.ts comments and IA-count assertions (24→22, cmd count 141→139) that #4152 made stale, in a second commit. 17/17 `pnpm test` pass, `pnpm typecheck` clean. `verify-branch-merged.mjs` confirms `origin/main` ancestry; `git status --porcelain` clean.
