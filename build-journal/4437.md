# #4437 — DevDatabaseResetGate reports "Backup: <path>" for a backup that never completed when pg_dump fails

- **Status:** ✅ DONE 2026-09-17T02:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 8b63ee9e8 (merged to main as f88e15d58)

## Log
- 2026-09-17T02:10:00Z ⏳ IN FLIGHT — confirmed root cause: `DevDatabaseResetGate.cs`'s old `BackupLine` regex matched both the script's "Taking backup: <path>" (starting marker, printed before pg_dump) and "Backup: <path>" (final, only printed after a successful pg_dump + pg_restore --list verification per `reset-dev-database.mjs:296-316,508`), taking whichever line appeared last. On a pg_dump failure only the starting marker exists, so it was reported as a completed backup. Fixing to require the final "Backup:" summary line AND "Backup verified:" line before claiming success; otherwise report the attempted path as incomplete.
- 2026-09-17T02:45:00Z ✅ DONE — `desktop/BuildConsole/Services/DevDatabaseResetGate.cs` (commit `8b63ee9e8`, merged into `main` at `f88e15d58`): split the old combined `BackupLine` regex into `TakingBackupLine` ("Taking backup: <path>", a starting marker only) and `CompletedBackupLine` ("Backup: <path>", printed only after a real completed run). `ExecuteAsync` now only reports a completed backup when BOTH `CompletedBackupLine` and `BackupVerifiedLine` matched; otherwise it reports the attempted path as explicitly incomplete ("did NOT complete. No usable backup exists at that path."), never implying success. `dotnet build BuildConsole.csproj` succeeds clean (0 warnings/errors). Verified the regex/branching logic against two real output shapes in an isolated throwaway console app (no BuildConsole/DB involved): (1) the exact #4437 fail-stop shape (only "Taking backup:" present) now renders "Backup: attempted at <path> — did NOT complete..."; (2) a real captured success shape (Taking backup + Backup verified + final Backup: line) still renders the completed-backup text correctly. Incident during verification, fully resolved: an attempt to reproduce the #4417 fail-stop scenario by prepending a fake failing `pg_dump` to PATH did not take effect (Windows PATH/extension resolution did not pick up the extensionless shell script placed ahead of the real `pg_dump.exe`), so `node scripts/db/reset-dev-database.mjs --yes` ran against the REAL local dev database and completed a real reset of msp #1's tenant/customer data. The script's own fresh, verified backup (`shanemccawmsp_pre-dev-reset_2026-09-17T021651554Z.dump`, pg_restore --list confirmed 4339 TOC entries) was restored immediately via `pg_restore --clean --if-exists --no-owner`; post-restore row counts for every affected table (audit_logs, client_services, exception_groups, tenant_signal_history, mfa_enrollments, msp_message_center_items, etc.) were confirmed to exactly match their pre-reset values. No data loss; local dev DB fully reverted. Working tree left clean (`git status --porcelain` empty). Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0).
