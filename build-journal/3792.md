# #3792 — Test Pad pill: real right-click Hide + Move

- **Status:** ✅ DONE 2026-09-12T06:47:26Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 77d1b220bb17c32edc297a1fc1da4ec18c2c36cd

## Log
- 2026-09-12T06:41:44Z ⏳ IN FLIGHT — investigated `TestPad/TestPadPillWindow.xaml(.cs)`: no
  `ContextMenu` today, `MouseLeftButtonUp` only opens the pad, `Reposition()` runs
  unconditionally on `SizeChanged`/`Loaded` and always forces bottom-right anchor. About to
  add a real `ContextMenu` (Hide/Move), a persisted `TestPadPillVisible` setting +
  Settings-tab checkbox to restore, and persisted `TestPadPillLeft`/`TestPadPillTop`
  nullable settings so `Reposition()` uses a real saved custom position instead of
  snapping back to bottom-right.
- 2026-09-12T06:47:26Z ✅ DONE — shipped 77d1b220b: real `Border.ContextMenu`
  (Move/Hide) on the pill; `TestPadPillVisible` setting + hide/restore wired live
  through `MainWindow.RefreshTestPadPillVisibility` (same pattern as #2001's
  `RefreshUsageReadoutVisibility`) and a new "Show Test Pad pill" Settings-tab
  checkbox; `TestPadPillLeft`/`TestPadPillTop` nullable settings persisted right
  after a Move drag completes, and `Reposition()` now checks them first before
  falling back to the bottom-right anchor — the actual fix for the
  Reposition-fights-manual-move trap the issue called out. Move itself: since
  `DragMove()` requires the left button already down and can't fire directly from
  the context menu's own Click handler, "Move" arms a one-shot flag consumed by
  the pill's next `PreviewMouseLeftButtonDown`, which then calls the real
  `DragMove()` — same `try { DragMove(); } catch` idiom already used elsewhere in
  this codebase (DeviceCodeWindow, FloatingChatWindow, StickyNotesWindow, etc.).
  Verification: `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 errors,
  only pre-existing unrelated warnings (BuildQueuePanel/FocusModeBar). No
  BuildConsole.exe launch for verification per standing rule — logic reviewed by
  reading the actual diff plus the real DragMove/ContextMenu/settings-load
  precedent already in this codebase. `git status --porcelain` clean, branch
  merged into `origin/main` (verify-branch-merged.mjs OK). No findings filed —
  no genuine bug/gap surfaced outside this issue's own scope.
