# Git #3900 — event-driven Verifying→Done promotion

- **Status:** ✅ DONE 2026-09-13T04:14:23Z
- **Scope:** buildconsole

## Log

- 2026-09-13T04:12:21Z — Investigated. Confirmed `PromoteVerifyingToDoneAsync` (#1469) is only
  called from 3 manual-refresh sites (MainWindow, BuildWatchWindow, LeftSidebar). Implementing a
  narrow, event-driven trigger piggybacked on `QueueWatcherService.TickAsync`'s existing 30s tick,
  per the issue's own real fix plan — no new blanket timer.
- 2026-09-13T04:14:23Z — ✅ DONE. Implemented and pushed as `dcb83f494` (merged into `origin/main`,
  verified via `verify-branch-merged.mjs`). Real changes:
  - `GitHubApiClient.BatchGetIssueStatesAsync` — new small aliased-GraphQL batch read
    (`issue(number:) { state }`), same pattern as `BatchGetBlockedByAsync`/`BatchGetProjectItemStatusesAsync`.
  - `BuildQueuePostgresClient.GetVerifyingGithubNumbersAsync` / `PromoteSpecificToDoneAsync` — narrow
    read/write pair, factored out of `PromoteVerifyingToDoneAsync`'s shared candidates query
    (`GetVerifyingCandidatesAsync`), with no dependency on a full open-issue snapshot.
  - `QueueWatcherService.MaybeCheckVerifyingIssuesClosedAsync`, wired into the existing `TickAsync`
    (30s `_tickTimer`, same tick `MaybeResyncLiveBlockersAsync`/`MaybeSyncIssueMirrorAsync` already
    ride) — no new timer. Single-flight, throttled (20s min gap), rate-limit-circuit-aware. Reads the
    tiny Verifying-issue-number set from local Postgres every tick (near-zero cost, almost always
    empty); only when non-empty does it spend one small GraphQL read on just those numbers, and
    promotes/mirrors to Done any that are actually CLOSED.
  - Verified: clean `dotnet build` (0 errors/warnings) both before and after the rebase onto
    `origin/main`.
  - `git status --porcelain` clean at session end.
  - No findings filed this session — no genuine bug/gap discovered outside scope.
