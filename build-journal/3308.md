# #3308 — Feature: Real pantry tracking (reverses earlier ruled-out decision)

- **Status:** ✅ DONE 2026-09-09T02:55:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 7ef5c95c15342ab7a7b584aad7300abe62f35f14

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Investigated `web/shanes-life` (Shane's Life personal
  app, Epic #3086). Confirmed real precedent: Feature-tier issues under #3086 are the
  buildable unit (each gets its own migration + core/*.mjs + capture-grammar rule(s) +
  MCP tools + routes + UI, per #3124/#3125/#3156/#3292 etc.). Plan: build the real pantry
  core end-to-end in this session (schema, capture grammar, Recipes canMake integration,
  MCP tools, read route, UI as a tab in the Shopping room per the issue's own "decide which
  fits better" instruction), update contract.md Section 5 per the issue's exact required
  text, and split the Cook-mode depletion question (item 4) out to its own real sub-issue
  under #3308 — confirmed live that Cook mode (`cookSession`/`mealSession` in app.js) is
  deliberately client-only/never persisted, so wiring real decrement means a genuinely new
  server round-trip that doesn't exist yet, a separable scope from the pantry core itself.
- 2026-09-09T02:55:00Z ✅ DONE — Real, shipped: migration 068_pantry_items.sql (real
  `pantry_items` table, house-scoped upsert unique index, run myself against local
  `finances` DB); `core/pantry.mjs` (setPantryQuantity/adjustPantryQuantity/
  depletePantryItem/listPantryItems/groupPantryByCategory/hasPantryQuantity); three new
  capture-grammar rules (pantry_have/pantry_bought/pantry_used_last) + 5 new unit tests
  (44/44 passing); `recipes.mjs`'s canMake/missing now check real pantry inventory too
  (buildHaveChecker), not just the Shopping run; `set_pantry_item`/`get_pantry` MCP tools;
  `GET /api/pantry`; a Pantry tab inside the Shopping room UI (decided over a new top-level
  room — no real critter/furniture SVG assets exist to build one, and Pantry shares
  Shopping's own real CAT_ORDER grocery-category vocabulary); contract.md Section 5 updated
  with the issue's own exact required text. Real bug caught and fixed in the same session:
  the Shopping room's capture-bar shortcut (`state.route === "shopping"`) unconditionally
  intercepted ALL typed text on that route, which would have swallowed pantry statements
  verbatim as literal shopping-list items — gated it to the list tab specifically.
  **Real verification**: a disposable-account integration script exercised all three
  capture-grammar rules, the upsert/additive/deplete core functions, house-scoped
  uniqueness, category grouping, and the Recipes canMake extension against the real local
  database (21 real assertions, all passing, zero residue left behind); server boot
  confirmed clean on an alternate port with `GET /api/pantry` correctly 401'ing unauthenticated
  and `GET /api/mcp` tools list confirmed carrying both new tools (87 total). Filed
  #3312 (Cook mode real pantry depletion on ingredient checkoff — item 4 of #3308's own
  scope) as a real sub-issue of #3308, with the live investigation finding that Cook mode's
  own state is deliberately never persisted server-side, so this needs a genuinely new
  round-trip; labeled `bug`, milestone "Shane", board status "AI Batter Up". Rebased onto
  origin/main twice for real concurrent landings (Things-room checklist #3300, Lists-room
  shelf #3305) — one real conflict in capture-grammar.mjs (both sides added a rule at the
  same array position), resolved by keeping both rule sets and renumbering comments; no
  functional collisions. Working tree clean, branch verified merged into origin/main
  (`verify-branch-merged.mjs` exit 0).
