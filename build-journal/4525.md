# #4525 — #2106 sub-agent finalize guard: tool name mismatch (`Task` vs `Agent`)

- **Status:** ✅ DONE 2026-09-17T00:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 5a9262fd7 (rebased onto main as a3bc8479f)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — confirmed current code still matches issue body: `IsSubagentOrBackgroundTool` (QueueWatcherService.cs) matches `Task`/`*subagent*`/`workflow`/`background_task`, not `Agent`. `RunningEntry.BackgroundTasks` (#4520) already tracks background Agent calls via stream-json task lifecycle, so the #2106 finalize-guard defers correctly for background work regardless of this bug. `ActiveSubagents` (tool_call/tool_result based) is what's broken — going with option (a): fix the name match so foreground `Agent` calls populate `ActiveSubagents`, restoring Build Watch's "⚡ running N background agent(s)" status line for real agent calls, and correct the stale #2106 comments that describe a `Task`-tool mechanism that no longer matches the CLI.
- 2026-09-17T00:40:00Z ✅ DONE — `IsSubagentOrBackgroundTool` now matches `Agent` (kept the old `Task` match as a defensive fallback, commented as pre-rename). Corrected the `RunningEntry.ActiveSubagents` field doc, the `AppendEvent`/#2106 comment block in `ScheduleAutoFinalize`, and the new `IsSubagentOrBackgroundTool` doc comment to state plainly that ActiveSubagents/this guard only ever matters for a FOREGROUND Agent call (a turn can't be idle while one is outstanding, so it's a defensive backstop, not the live mechanism) and that background Agent calls are tracked and deferred via `BackgroundTasks` (#4520) instead — the #2106 comments previously implied ActiveSubagents was still doing real work for background calls, which #4520 already superseded. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. No test manifest: `desktop/BuildConsole` is explicitly out of the test-manifest scope in CLAUDE.md, and no existing unit-test project covers `QueueWatcherService`. Commit 5a9262fd7, pushed/rebased onto origin/main as a3bc8479f; `verify-branch-merged.mjs` confirmed OK. No new findings surfaced — the issue's own analysis (background work already fixed by #4520) held up under re-verification. Working tree clean.
