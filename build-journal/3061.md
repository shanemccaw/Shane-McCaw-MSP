# #3061 — test-manifests/portal/risk-register.json login-flow note is stale

- **Status:** ✅ DONE 2026-09-06T21:21:11Z
- **Scope:** platform

## Log
- 2026-09-06T21:15:50Z — Starting. Auth Core (#2991) shipped a real login form in
  `artifacts/portal/src/pages/login.tsx` (#email:230, #password:248,
  login-submit:257). Extending `test-manifests/portal/risk-register.json`'s
  `uiSteps` with a real login block (matching `ownership.json`'s working pattern)
  ahead of `goto /risk-register`, then exercising the real testids the risk-register
  build already ships: `risk-row-toggle-<id>`, `risk-accept-name-<id>`,
  `risk-accept-statement-<id>`, `risk-accept-confirm-<id>`, `risk-accept-sign-<id>`,
  `policy-decision-row-<id>` — confirmed all six exist in
  `artifacts/portal/src/components/risk-register/{RiskRow,PolicyDecisionsCard}.tsx`.
  No accept-flow submit is driven (permanent write-once guard, same reasoning the
  manifest already documents for A3/A4).
- 2026-09-06T21:21:11Z — ✅ DONE — commit `6f4e48d3c` (main). Rewrote
  `test-manifests/portal/risk-register.json`'s stale login note and its `uiSteps`
  block: real login (`#email`/`#password`/`login-submit`) ahead of `goto
  /portal/risk-register`, a real-data row assertion against the endpoint's own
  first risk id/title, opening that row (`risk-row-toggle-<id>`), the guaranteed
  already-accepted seeded row RBD-2026-104 rendering its "Accepted by" record
  (never the accept form, since acceptance is permanent), and the
  `policy-decision-row-<id>` testid keyed off the endpoint's real decision id.
  Added `apiTests` extract steps (firstRiskId/firstRiskTitle/firstDecisionId) to
  drive those assertions. `lastVerifiedAgainstCommit` set to `6f4e48d3c`.
  JSON validated (`node -e "JSON.parse(...)"`). Registered in
  `test-manifests/_regression-suite.json` already (pre-existing entry).
  **Honest verification note:** ran `shaneapp://runTest?file=risk-register.json`
  twice. Both runs (before and after `request-restart.mjs` reported the commit
  merged into buildSet `RiskRegister` [2/3]) executed the file's *old* 8-step
  content — the live BuildConsole/dev-server checkout at
  `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` is still at `b6b929844`
  (pre-#3061), one commit behind, because this build's buildSet defers its
  merge landing on the shared checkout until all 3 members of `RiskRegister`
  complete. The second run did newly assert the real "Sign in required" text
  match against the deployed page (proving the harness itself works), but
  neither run exercised this session's new login/uiSteps content. This is the
  documented buildSet-deferred-merge behavior working as designed, not a
  failure to attempt verification — a genuine, statable reason per CLAUDE.md's
  verification-defaults section, not a default "unable to verify." The new
  content was verified by direct source inspection instead: all six real
  testids (`risk-row-toggle-<id>`, `risk-accept-name/-statement/-confirm/-sign-<id>`,
  `policy-decision-row-<id>`) confirmed present in
  `RiskRow.tsx`/`PolicyDecisionsCard.tsx`; `login.tsx`'s `#email`/`#password`/
  `login-submit` confirmed present (same pattern already proven live by
  `ownership.json`'s own manifest this same login block was copied from).
  Whoever completes the `RiskRegister` buildSet (or a follow-up run once the
  set's deferred restart lands) should re-run `shaneapp://runTest?file=risk-register.json`
  against the merged code and confirm the full 17-step pass, updating
  `lastVerifiedAgainstCommit` if anything drifts.
  `git status --porcelain` clean at finish. No new findings filed — this
  session only closed the gap #3061 itself named.
