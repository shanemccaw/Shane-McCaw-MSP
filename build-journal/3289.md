# #3289 — URGENT: Real, live-verified UI audit across all 13 rooms (Shane's Life)

- **Status:** ✅ DONE 2026-09-09T01:12:26Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** none — this is an audit-only issue by its own explicit non-goal ("this is an
  audit, not a rebuild"); no product code changed. Only this bookend file and the IN FLIGHT
  bookend commit belong to this session.

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3289 (sub-issue of #3220, Feature: App Foundation &
  Infrastructure, under Epic #3086). Real scope: for each of the 13 real rooms in `ROOM_DEFS`
  (money, wins, vault, meds, tesla, shopping, recipes, things, lists, dates, pets, inbox,
  people), do real live Playwright verification against the real local server + real local
  `finances` Postgres DB (same pattern #3264/#3269/#3274 already proved out — disposable test
  account, real WebAuthn assertion via `bin/soft-authenticator.mjs`), screenshot each room's
  real rendered state, and compare directly against its real design source — the First Slice
  Prototype (`Shanes Life - First Slice Prototype.dc.html`) first per Shane's own standing
  precedence rule, falling back to the room's own one-off `.dc.html` only where the Prototype
  has nothing for that room. File one real issue per genuine structural gap found, parented
  under #3220 (or a more specific room Feature where one exists). This is an audit, not a
  rebuild — no restyling of rooms that are already genuinely correct.
  Environment confirmed: copied the real `.env` (finances DB connection string) from the main
  checkout into this worktree (not committed, gitignored); `pg`/`sharp` resolve from the
  workspace-root `node_modules` (already junctioned into this worktree) even though
  `web/shanes-life` has no local `node_modules` of its own; `playwright-core` 1.61.1 present at
  workspace root with Chromium already cached at `%LOCALAPPDATA%\ms-playwright\chromium-1228`
  (no download needed).
- 2026-09-09T01:12:26Z ✅ DONE — real live audit completed against a real running server
  (`web/shanes-life/src/server.mjs`, port 5179) + the real local `finances` Postgres DB. Real
  disposable account created, enrolled with a real WebAuthn passkey via
  `bin/soft-authenticator.mjs`, session cookie obtained via the real `/api/auth/enroll/*`
  endpoints. Seeded every room through the real write API: copied Shane's own real data where
  the local DB had it (15 real medications, 31 real shopping-list items), created fresh
  real-workflow entries where the local DB genuinely had zero rows even under his own real
  account (wins/vault/vehicles/things/lists/dates/pets/people all confirmed empty before
  seeding). Generated a local-only `SL_VAULT_KEY` (pure local dev secret, not a production
  credential) so the Vault room could be verified fully encrypted/decrypted rather than showing
  "not configured". Drove real headless Chromium (`playwright-core`, cached Chromium, no
  download) to screenshot all 13 real rooms. Design comparison used the real First Slice
  Prototype driven live/interactively in the same browser (signed in, scrolled to the house
  grid, clicked into each of the 13 rooms for a real per-room screenshot of design intent), per
  Shane's own precedence rule, falling back to each room's dedicated one-off `.dc.html` where
  relevant.

  **Result: 8 of 13 rooms confirmed matching** (Medicine, Shopping, Recipes, Lists, Dates, Pets,
  Inbox, People room-screen-itself), **5 real gaps found and filed**: #3298 (Money 5-tab
  segmented control CSS still hardcodes an 8-column grid from an earlier tab count, causing
  illegible overlap), #3299 (Vault: 4 section headings missing padding, clipped by the card's
  own rounded corner), #3300 (Things: "Next [place] run - Take" checklist section entirely
  missing, confirmed against 2 independent design sources with zero backing schema), #3301
  (People: Today house-cell subtitle not singular/plural aware, "1 people"), #3302 (Wins: raw
  ISO timestamp shown instead of a formatted date). Tesla could not be fully verified live — no
  `TESLA_CLIENT_ID`/`SECRET` configured in this local dev environment (matches the prior #3286
  finding); its own honest "not configured" fallback state renders correctly. Full structured
  per-room report posted as a comment on #3289. All 5 findings labeled `bug`, parented under the
  most specific open Feature that exists (#3234 Money, #3230 Things, #3231 People; #3220 App
  Foundation as fallback for Money/Wins/Vault findings with no dedicated open Feature), and
  board status set to "AI Batter Up".

  **No product code changed** — this issue is audit-only per its own explicit non-goal. Real
  cleanup performed: disposable account deleted (cascade confirmed 0 orphaned rows across every
  user-scoped table), both scratch servers (app server port 5179, static design-file server
  port 8899) stopped, every scratch script/screenshot deleted, `git status --porcelain` clean
  throughout and at the end.

  `node scripts/dev-server/verify-branch-merged.mjs` confirms this worktree's branch is a real
  ancestor of `main` (this session's only real commit is the IN FLIGHT bookend below — pushed
  immediately after creation, already merged before any of the audit work began, since the
  audit itself produced no code commits).
