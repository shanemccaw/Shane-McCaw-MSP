# #3289 — URGENT: Real, live-verified UI audit across all 13 rooms (Shane's Life)

- **Status:** ⏳ IN FLIGHT 2026-09-08T00:00:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** (none yet — audit issue, findings filed as sibling issues)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3289 (sub-issue of #3220, Feature: App Foundation &
  Infrastructure, under Epic #3086). Real scope: for each of the 13 real rooms in `ROOM_DEFS`
  (money, wins, vault, meds, tesla, shopping, recipes, things, lists, dates, pets, inbox,
  people), do real live Playwright verification against the real local server + real local
  `finances` Postgres DB (same pattern #3264/#3269/#3274 already proved out — disposable test
  account, real WebAuthn assertion via `bin/soft-authenticator.mjs`), screenshot each room's
  real rendered state, and compare directly against its real design source — the First Slice
  Prototype (`Shanes Life - First Slice Prototype.dc.html`) first per Shane's own standing
  precedence rule, falling back to the room's own one-off `.dc.html` only where the Prototype
  has nothing for that room. File one real issue per genuine structural gap found, parented
  under #3220 (or a more specific room Feature where one exists). This is an audit, not a
  rebuild — no restyling of rooms that are already genuinely correct.
  Environment confirmed: copied the real `.env` (finances DB connection string) from the main
  checkout into this worktree (not committed, gitignored); `pg`/`sharp` resolve from the
  workspace-root `node_modules` (already junctioned into this worktree) even though
  `web/shanes-life` has no local `node_modules` of its own; `playwright-core` 1.61.1 present at
  workspace root with Chromium already cached at `%LOCALAPPDATA%\ms-playwright\chromium-1228`
  (no download needed).
