# #2514 — Cross-check ratio rule type (`profile_key_ratio`) for `evaluateRule()`

- **Status:** ✅ DONE 2026-09-03T15:25:31Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 4d660c91b (rebased/pushed as 901717c0d on origin/main)

## Log
- 2026-09-03T15:05:10Z ⏳ IN FLIGHT — Investigation done (issue body/comments, `evaluateRule()`,
  `signal_derivation_rulesTable`, live DB rows, and every real SQL/Drizzle site that builds
  `SignalDerivationRule[]` for the real evaluation pipeline). Starting implementation: schema
  column + migration, `profile_key_ratio` case in `evaluateRule()`, wiring `denominatorKey`
  through `fetchSignalRulesAndGroups()` (the primary loader nearly every engine uses) plus the
  other independent SQL sites, retuning `signal.copilot.license-vs-total-users` (rule id 2735)
  to prove it live, and admin-panel form support so the type is actually editable, not just
  evaluator-recognized.
- 2026-09-03T15:25:31Z ✅ DONE — Shipped option (b): new `ruleType: "profile_key_ratio"` in
  `evaluateRule()` (`artifacts/api-server/src/lib/tenant-signals.ts`) reading `sourceKey`
  (numerator) and the new `denominatorKey` column (denominator) off the same live merged
  cross-check tenant profile, fixed `<` direction (mirrors the existing single-direction
  `threshold` ruleType). Explicitly does not repeat #553's rejected design — stated in the
  code comment and the migration header — since both values come from the same
  already-merged, already-fresh profile in the same evaluation call, not a stored/cached
  per-check output. Schema: `signal_derivation_rules.denominator_key` (nullable text,
  additive) added via Drizzle schema + `lib/db/migrations/manual/2026-09-03-signal-derivation-rules-ratio-type-2514.sql`,
  **run myself** against local Postgres (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, verified
  via `psql`). Retuned `signal.copilot.license-vs-total-users` (rule id 2735) from a
  meaningless `itemCount > 0` placeholder to a real ratio
  (`copilotLicenseCount / totalUserCount < 50%`) — real, live proof, confirmed via `psql`
  SELECT post-migration. Wired `denominatorKey` through every real SQL/Drizzle site that
  builds `SignalDerivationRule[]` for evaluation, not just the schema: `fetchSignalRulesAndGroups()`
  (priority-engine.ts, the primary loader nearly every engine — health/security/drift/
  forecasting/msp/crm/sales-offer — uses), `admin-signal-rules.ts` (getAllRules, POST, PATCH,
  DELETE, all three import paths, version-restore, export payload), `msp-engines.ts`,
  `portal-presentations.ts`, `admin-insights.ts`. `db.select().from(signalDerivationRulesTable)`
  call sites (workflow-executor.ts, admin-engines.ts) get it for free via the Drizzle schema
  change. Admin-panel form support added (`signalRuleForm.ts` RULE_TYPES entry + denominator
  field + validation, `SignalRuleEditorModal.tsx` input) so the type is actually editable
  end-to-end, not evaluator-only dead code from the admin UI's perspective — fixed the stale
  "recognizes exactly these seven types" comment in the same file. Tests added:
  `evaluateRule` display-text case (`tenant-signals.test.ts`) plus a
  `computeTenantSignals — profile_key_ratio (#2514)` describe block covering firing, at/above
  threshold, zero-denominator guard, missing-denominatorKey guard, missing-profile-key guard.
  **Verification, honestly reported:** a real, severe, PRE-EXISTING pnpm store corruption in
  the shared main checkout (several core packages — drizzle-orm, vitest, zod, express,
  stripe, pino, pino-http, pdf-lib — have empty unpacked payload) blocked running the real
  `vitest`/`tsc` suite for this change; confirmed via `fs.lstatSync`/`fs.readdirSync` this is
  not caused by or specific to this build, filed as **#2556** (parented under #1489, board
  status AI Batter Up). Verified instead: (1) `pnpm run typecheck` on the changed files shows
  only the same pre-existing environmental errors present before my diff — none reference
  `denominatorKey` or any line I touched; (2) the retuned DB rule confirmed directly via
  `psql`; (3) the exact `profile_key_ratio` case body run in isolation via plain `node -e`
  (no imports, byte-identical logic) — all 7 hand-checked cases (firing, not-firing, zero
  denominator, missing denominatorKey, missing profile key, the real 5%/80% coverage
  scenarios) pass. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs` (901717c0d). Working tree clean
  (`git status --porcelain` empty). Real commit: 4d660c91b.
