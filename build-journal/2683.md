# #2683 — Detected panel: mouse-wheel scroll is janky — nested ScrollViewers fighting each other

- **Status:** ✅ DONE 2026-09-03T16:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 4a50a78cd (47c93c35d after rebase-push to main)

## Log
- 2026-09-03T16:30:00Z ⏳ IN FLIGHT — restructuring `ChatDocumentContainer.xaml`'s `DetectedScroller` region so `DetectedDockHost` (hosting `ChatDockPanel`, which already owns its own internal `ScrollViewer`) is no longer nested inside an outer `ScrollViewer`. `CrossEpicCards` keeps a fixed-top region above it. `ChatDockPanel.cs` is not touched.
- 2026-09-03T16:45:00Z ✅ DONE — `DetectedScroller` changed from an outer `ScrollViewer` to a `Grid` (same `x:Name`, so code-behind's `.Visibility` toggle needed no change): `CrossEpicCards` now sits in a Row="Auto" region with its own dedicated small `ScrollViewer` (`MaxHeight="220"`) so a long run of cross-epic cards can still scroll independently without pushing the dock off-screen; `DetectedDockHost` fills the remaining Row="*" directly, un-nested, relying solely on `ChatDockPanel`'s own internal `ScrollViewer` for mouse-wheel scrolling. `ChatDockPanel.cs` and `FloatingChatWindow.xaml` are untouched (confirmed via `git diff`, no changes). `dotnet build` on `desktop/BuildConsole/BuildConsole.csproj` succeeded, 0 warnings/0 errors. Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean. Live BuildConsole.exe launch/UI drive was NOT performed per the standing rule against launching it for verification (single-instance mutex risk) — verification is build-clean + structural diff review confirming the fix is scoped exactly to the layout restructure the issue described.
