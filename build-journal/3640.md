# Build Journal — Issue #3640

- **Status:** ✅ DONE 2026-09-12T00:15:00Z
- **Scope:** platform

## Log

- 2026-09-12T00:00:00Z — IN FLIGHT. Removing two dead `vi.mock("../lib/ai-prompts", ...)` calls in
  `admin-services-catalog.test.ts` and `admin-services-price-cents.test.ts`. Confirmed the
  `getPromptText`/ai-prompts module family was deleted in commit `7e3e4a476` (removed
  `admin-ai-prompts.ts` route and the entire adminv2 ai-prompts screen); zero remaining callers of
  `getPromptText` in `artifacts/api-server/src`. Ran both test files after removing the mocks — 20/20
  pass, confirming the mock was intercepting nothing.
- 2026-09-12T00:15:00Z — ✅ DONE. Committed `817fb63e5` (removed both dead mocks) on top of
  `452796d75` (this bookend's IN FLIGHT commit), rebased onto `origin/main`, pushed directly to
  `origin/main`. `verify-branch-merged.mjs` confirms ancestor OK. `git status --porcelain` clean.
  No new backend gaps or findings surfaced — this was a pure dead-code removal, no sub-issue filed.
