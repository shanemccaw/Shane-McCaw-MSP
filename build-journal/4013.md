# #4013 — Team Management: Billing / Customer Admin role surface

- **Status:** ✅ DONE 2026-09-14T17:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 25bcbdd8a

## Log
- 2026-09-14T17:10:00Z ⏳ IN FLIGHT — Read #4013's body, the landed design
  (`Design/portal/design_handoff_billing_roles_and_new_modules/screens/Team
  Management.dc.html`), its README and contract pack, the existing
  `customer-team.tsx` (#3996) page and its `teamWire.ts`/`useTeamLive.ts`, and
  the real `PATCH /portal/team/:userId/role` route. Confirmed the roster read
  already carries `isCustomerAdmin`/`hasBillingRole`/`managerUserId`. Found one
  real backend gap the design's copy depends on: no field tells the UI whether
  a role holder is the auto-granted "billed party" (#3629 trigger) vs a manual
  grant — adding `isBilledParty` to `GET /portal/team` to serve it honestly
  instead of guessing or dropping the copy.
- 2026-09-14T17:45:00Z ✅ DONE — Commit `25bcbdd8a` (rebased onto and pushed to
  origin/main). Shipped: (1) `GET /portal/team` now returns `isBilledParty`
  per member, derived from real `invoices`/`client_services` rows addressed
  to them — the same condition the #3629 `rbac_grant_billing_to_billed_party`
  trigger acts on; verified queryable against local `DATABASE_URL` via psql.
  (2) `teamWire.ts` gained the `isBilledParty` field and `AssignableRole`
  type. (3) `useTeamLive.ts` gained `setRole(userId, role, granted)` against
  the real `PATCH /portal/team/:userId/role`. (4) `customer-team.tsx` now
  draws the design's role chips on roster rows, a Held/Not-held Roles block
  on the detail panel (capabilities spelled out per role, self-revoke
  refusal for a caller's own Customer Admin role, the #3629 auto-grant note
  drawn only against the real billed party), and grant/revoke confirm modals
  carrying the design's exact copy. Also fixed a latent bug hit while editing
  this file's modal dispatch: `ActionModal` was unconditionally mounted
  alongside `InviteModal` for `modal.kind === "invite"`, and `SPECS` has no
  `"invite"` key, so opening Invite would have thrown — not filed separately
  since it was fixed in the same pass, not left as a workaround.
  Verification: `tsc --noEmit` clean in both `artifacts/api-server` and
  `artifacts/portal`; `vitest run src/routes/portal-team.test.ts` 14/14
  passed; `vite build` clean in `artifacts/portal`; real `psql` query
  against local `DATABASE_URL` confirming the new query's tables/columns.
  `shaneapp://runTest` was genuinely unreachable this session — two real
  `Start-Process` attempts against the confirmed-running local BuildConsole
  process produced no result file within 90s/20s; documented honestly in
  both `test-manifests/portal/team-management.json` and
  `test-manifests/portal/team-role-assignment.json` (also updated to reflect
  the now-13-route surface and the role UI landing) rather than claimed as
  run. `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed
  the branch is an ancestor of `origin/main`. No new GitHub findings filed —
  the one real gap found (missing `isBilledParty`) was built, not left open.
