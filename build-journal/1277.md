# #1277 — BuildConsole: auto-deploy ps-execution Docker container to Azure (dev-only, agent-callable)

- **Status:** ✅ DONE
- **Scope:** both
- **Started:** 2026-08-28
- **Commit(s):** `879755d96` (IN FLIGHT bookend), `fddaa4afc` (implementation) — both on `origin/main`

## Log
- 2026-08-28 ⏳ IN FLIGHT — Building an agent-callable (`shaneapp://`) Docker build + ACR push
  (`acrsmccaw2184`) + revision deploy of `ps-execution` to **`ca-ps-execution-dev`** only
  (rg `rg-smccaw-2184`, East US 2). Prod `ca-ps-execution` is hard-guarded and unreachable from
  this path (#1385). Precondition confirmed: `az account show` succeeds non-interactively
  (Pay-As-You-Go / McCawSoft tenant). Baseline serving revision before this work:
  `ca-ps-execution-dev--fix1400b-1787847728` (image `:dev`, traffic 100). Adding a container
  `/healthz` revision self-report, an api-side `integration.azure` serving-revision reader, and
  BuildConsole `deployPsExecution` / `psExecutionRevision` protocol actions so #1482/#1483 can
  iterate without a manual hand-deploy and can verify WHICH revision is actually serving (the
  #1434 stale-verify failure mode).
- 2026-08-28 ✅ DONE — Shipped in `fddaa4afc`. Container gained an unauthenticated `GET /healthz`
  revision self-report; BuildConsole gained `PsExecutionDeployService` + `shaneapp://deployPsExecution`
  / `shaneapp://psExecutionRevision` (DEV-only by construction — no Container-App-name parameter, so
  prod `ca-ps-execution` is structurally unreachable, #1385); api-server gained
  `ps-execution-revision.ts` (`getServingPsExecutionRevision`, channel `integration.azure`).
  **Real end-to-end verification against live Azure** (not simulated): `az acr build` pushed image
  digest `248e0778…`; `az containerapp update --revision-suffix dev1277a` created revision
  `ca-ps-execution-dev--dev1277a`, now **active at 100% traffic** (prior `fix1400b` → 0%); `/healthz`
  returns `{"revision":"ca-ps-execution-dev--dev1277a",…}` confirming the new image is genuinely
  serving; the bearer-gated cmdlet path still 401s (gate intact). BuildConsole builds clean (0
  errors); the two new api-server files typecheck clean (the 25 `tsc` errors are pre-existing, in
  unrelated `*.test.ts` files I never touched). No test manifest written — the changed code is in
  `services/ps-execution` + `artifacts/api-server` + BuildConsole, all outside the
  `artifacts/msp-portal` / `artifacts/shane-mccaw-consulting` manifest scope. **Shane To-Do:** a
  BuildConsole restart is required before #1482/#1483 are queued (per the sequencing comment), and
  the `shaneapp://` protocol handler picks up the two new actions only after that restart.
