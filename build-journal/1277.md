# #1277 — BuildConsole: auto-deploy ps-execution Docker container to Azure (dev-only, agent-callable)

- **Status:** ✅ DONE
- **Scope:** both
- **Started:** 2026-08-28
- **Commit(s):** `879755d96` (IN FLIGHT bookend), `fddaa4afc` (implementation), `8ee7f01e1`
  (re-verification bookend, this session, no code changes) — all on `origin/main`

## Log
- 2026-08-28 ⏳ IN FLIGHT — Building an agent-callable (`shaneapp://`) Docker build + ACR push
  (`acrsmccaw2184`) + revision deploy of `ps-execution` to **`ca-ps-execution-dev`** only
  (rg `rg-smccaw-2184`, East US 2). Prod `ca-ps-execution` is hard-guarded and unreachable from
  this path (#1385). Precondition confirmed: `az account show` succeeds non-interactively
  (Pay-As-You-Go / McCawSoft tenant). Baseline serving revision before this work:
  `ca-ps-execution-dev--fix1400b-1787847728` (image `:dev`, traffic 100). Adding a container
  `/healthz` revision self-report, an api-side `integration.azure` serving-revision reader, and
  BuildConsole `deployPsExecution` / `psExecutionRevision` protocol actions so #1482/#1483 can
  iterate without a manual hand-deploy and can verify WHICH revision is actually serving (the
  #1434 stale-verify failure mode).
- 2026-08-28 ✅ DONE — Shipped in `fddaa4afc`. Container gained an unauthenticated `GET /healthz`
  revision self-report; BuildConsole gained `PsExecutionDeployService` + `shaneapp://deployPsExecution`
  / `shaneapp://psExecutionRevision` (DEV-only by construction — no Container-App-name parameter, so
  prod `ca-ps-execution` is structurally unreachable, #1385); api-server gained
  `ps-execution-revision.ts` (`getServingPsExecutionRevision`, channel `integration.azure`).
  **Real end-to-end verification against live Azure** (not simulated): `az acr build` pushed image
  digest `248e0778…`; `az containerapp update --revision-suffix dev1277a` created revision
  `ca-ps-execution-dev--dev1277a`, now **active at 100% traffic** (prior `fix1400b` → 0%); `/healthz`
  returns `{"revision":"ca-ps-execution-dev--dev1277a",…}` confirming the new image is genuinely
  serving; the bearer-gated cmdlet path still 401s (gate intact). BuildConsole builds clean (0
  errors); the two new api-server files typecheck clean (the 25 `tsc` errors are pre-existing, in
  unrelated `*.test.ts` files I never touched). No test manifest written — the changed code is in
  `services/ps-execution` + `artifacts/api-server` + BuildConsole, all outside the
  `artifacts/msp-portal` / `artifacts/shane-mccaw-consulting` manifest scope. **Shane To-Do:** a
  BuildConsole restart is required before #1482/#1483 are queued (per the sequencing comment), and
  the `shaneapp://` protocol handler picks up the two new actions only after that restart.
- 2026-08-29 ⏳ IN FLIGHT — Re-verification session (build 762). BuildConsole has since been
  restarted, so the running binary should now include the `shaneapp://deployPsExecution` handler
  for the first time. Verifying the auto-deploy path end to end THROUGH the real handler (not by
  driving `az` directly), and re-checking whether the #1031 dependency question is actually
  resolved.
- 2026-08-29 ✅ DONE — No code changes this session; pure verification against the live system.
  Confirmed `fddaa4afc` genuinely landed (all four files present, wired: `PsExecutionDeployService.cs`,
  `MainWindow.ShaneAppDeployPsExecution.cs`, `ps-execution-client.ts`, `ps-execution-revision.ts`).
  Confirmed `deployPsExecution`/`psExecutionRevision` are routed in the running
  `MainWindow.xaml.cs` action dispatcher and `BuildConsole.exe` (PID present) is the restarted
  binary. **Triggered a real deploy through the handler** (`shaneapp://deployPsExecution?path=...
  &suffix=dev1277c762&resultRef=...`, NOT `az` by hand): `az account show` succeeded
  non-interactively (no #1031 device-code prompt needed — confirms Shane's 2026-08-28 sequencing
  comment that dropped the #1031 dependency for this issue), `az acr build` pushed a fresh image,
  `az containerapp update --revision-suffix dev1277c762` created revision
  `ca-ps-execution-dev--dev1277c762`. **Independently confirmed live** two ways: `az containerapp
  revision list` shows it as the sole active revision at 100% traffic, and `GET /healthz` on the
  container itself reports `{"revision":"ca-ps-execution-dev--dev1277c762",...}` — the new code is
  genuinely serving. **Real bug found and reported (not fixed this session, out of scope for a
  verify-only pass):** the handler's own JSON result envelope from that same call reported a STALE
  revision (`ca-ps-execution-dev--dev1482fix3`, traffic 0) — `GetServingRevisionAsync` /
  `ParseFirstActiveRevision` in `PsExecutionDeployService.cs` appears to read the control-plane
  revision list too early (before Azure's transition settles) and/or takes the first `active` entry
  from the array rather than the one carrying live traffic. The DEPLOY is real and correct; the
  handler's immediate self-reported confirmation is not reliably read back yet — flagged on the
  issue for a follow-up fix. **Dependency question, answered directly:** #1277 does NOT need #1031.
  Shane's own 2026-08-28 sequencing comment on this issue explicitly dropped that dependency (`az`
  context already exists on the build machine; #1031 covers a different, interactive PowerShell
  *test*-session path). The issue's most recent comment (`5459185490`, "NOT closed... correctly
  parked behind #1031") is STALE — it predates `fddaa4afc` landing and BuildConsole's restart, and
  its premise (a grep finding nothing) is no longer true against the real tree.
