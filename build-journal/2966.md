# #2966

- **Status:** ✅ DONE 2026-09-06T00:42:25Z
- **Scope:** artifacts/shane-mccaw-consulting

## Log
- 2026-09-06T00:41:39Z — Confirmed #2965's bg color fix (`bg-[#020617]`) already present in Layout.tsx. Unmounting `<PersistentChatBubble />` from Layout.tsx only (not deleting PersistentChatBubble.tsx or chat-content-blocks.ts). Also confirmed `EngagementOfferPanel` imports/uses `ChatCTA` (which calls `openChat()`) as its own CTA button — same stale assumption, out of scope here, will flag as a comment on #2966 rather than fix.
- ✅ DONE 2026-09-06T00:42:25Z — Removed the `PersistentChatBubble` import and `<PersistentChatBubble />` mount from Layout.tsx (`git diff --stat` confirmed only Layout.tsx changed, 2 deletions). `PersistentChatBubble.tsx` and `chat-content-blocks.ts` left in place. Commit `1381473c1`, pushed to `production-base-2026-09-05`. Posted comment on #2966 (issuecomment-5555857201) flagging that `EngagementOfferPanel` has the same stale assumption via `ChatCTA`/`openChat()` — not fixed here, out of this issue's reviewed scope.
