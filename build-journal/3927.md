# Build Journal — Issue #3927

- **Status:** ✅ DONE 2026-09-13T05:12:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-13T05:04:31Z — Started. Task: retire `artifacts/msp-portal/src/pages/portal-v2-change-control.tsx` per #1647 Group A.
  - 2026-09-13T05:12:00Z — ✅ DONE — Investigated and found the entire `artifacts/msp-portal` directory (including `portal-v2-change-control.tsx`, its `*Data.ts` fixture, route registration, nav entry, and `*Live.ts` hook) was already deleted wholesale in commit `f40438cdc` ("Portal scaffolding: create artifacts/portal, retire artifacts/msp-portal"), which predates this issue. Confirmed `grep -rn 'portal-v2-change-control' artifacts/msp-portal/src/` — directory does not exist, zero hits. Confirmed the real replacement — `artifacts/portal/src/pages/change-control.tsx` — is routed at `/change-control` in `artifacts/portal/src/App.tsx`, and reads real data via `useChangeControlRegister`/`useChangeMetrics`/`useFreezeWindows`/`useMaintenanceWindows` from `@/lib/change-control-api.ts` against the live `docs/portal/change-control-contract-pack.md` contract — no fixture module. No code changes needed beyond this bookend. Merged to `main` at `b1b71adde`.
