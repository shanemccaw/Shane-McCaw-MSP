# #2919 — Feature: Dashboard visual redesign — instrument-panel theme (ShanesSurvival)

- **Status:** ⏳ IN FLIGHT 2026-09-04T22:49:20Z
- **Scope:** platform (desktop/ShanesSurvival)
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T22:49:20Z ⏳ IN FLIGHT — pulled #2919's real design spec (approved 2026-09-04).
  Confirmed #2918 (`pay_period_forecast`) is `complete` and removed the now-stale `blocked_by`
  edge on #2919. Located the real ShanesSurvival code at `desktop/ShanesSurvival/` in this
  monorepo worktree (three projects: `ShanesSurvival.App` WPF, `ShanesSurvival.Core` shared
  services, `ShanesSurvival.Mcp` MCP tools) — the separate `C:\Source\ShanesSurvival` checkout
  is a stale/unrelated older copy, not touched. Read `DashboardService`/`DashboardResult`
  (gate_status equivalent: `TopLineAmount`/`IsCovered`/`IncomeGateBalance`/`TotalShortfall`),
  `DebtRepository`/`DebtRow` (`IsCritical`, `IsDelinquent`, `DaysPastDue` — #2915) and
  `PayPeriodForecastService`/`PayPeriodForecastResult` (#2918, two `PayPeriodForecastCycle`
  records with `DueBills`/`TotalDue`/`AvailableAmount`/`Delta`/`IsCovered`). No DI container in
  this app — `MainWindow` manually `new()`s services and passes them into window constructors;
  will follow the same pattern for the new `PayPeriodForecastService`/`DebtRepository` wiring.
  Building: new app-wide WPF resource dictionary (colors/typography per spec) merged into
  `App.xaml`, and `DashboardWindow` restructured to GATE readout → critical debts strip →
  This cycle/Next cycle panels, wired to real data from the three services above — no new
  backend logic, reuse only.
