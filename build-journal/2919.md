# #2919 — Feature: Dashboard visual redesign — instrument-panel theme (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T22:58:52Z
- **Scope:** platform (desktop/ShanesSurvival)
- **Started:** 2026-09-04
- **Commit(s):** 8f4f77151d5a37ea8cf5129f8d9d45b7c2b611bd

## Log
- 2026-09-04T22:49:20Z ⏳ IN FLIGHT — pulled #2919's real design spec (approved 2026-09-04).
  Confirmed #2918 (`pay_period_forecast`) is `complete` and removed the now-stale `blocked_by`
  edge on #2919. Located the real ShanesSurvival code at `desktop/ShanesSurvival/` in this
  monorepo worktree (three projects: `ShanesSurvival.App` WPF, `ShanesSurvival.Core` shared
  services, `ShanesSurvival.Mcp` MCP tools) — the separate `C:\Source\ShanesSurvival` checkout
  is a stale/unrelated older copy, not touched. Read `DashboardService`/`DashboardResult`
  (gate_status equivalent: `TopLineAmount`/`IsCovered`/`IncomeGateBalance`/`TotalShortfall`),
  `DebtRepository`/`DebtRow` (`IsCritical`, `IsDelinquent`, `DaysPastDue` — #2915) and
  `PayPeriodForecastService`/`PayPeriodForecastResult` (#2918, two `PayPeriodForecastCycle`
  records with `DueBills`/`TotalDue`/`AvailableAmount`/`Delta`/`IsCovered`). No DI container in
  this app — `MainWindow` manually `new()`s services and passes them into window constructors;
  will follow the same pattern for the new `PayPeriodForecastService`/`DebtRepository` wiring.
  Building: new app-wide WPF resource dictionary (colors/typography per spec) merged into
  `App.xaml`, and `DashboardWindow` restructured to GATE readout → critical debts strip →
  This cycle/Next cycle panels, wired to real data from the three services above — no new
  backend logic, reuse only.
- 2026-09-04T22:58:52Z ✅ DONE — added `Theme/InstrumentPanelTheme.xaml` (real hex palette +
  Segoe UI/Consolas typography, merged into `App.xaml`, implicit Window/TextBlock/Button/
  CheckBox/TextBox/PasswordBox/Border styles) and rethemed the few hardcoded LightGray/Gray
  locals in MainWindow/SettingsWindow/AccountRoleWindow so the theme genuinely applies
  app-wide. Restructured `DashboardWindow` to GATE readout (biggest element, real covered/
  danger verdict + real balance-vs-shortfall supporting text) → Critical Debts strip (real
  `is_critical` debts, left-bordered danger-accent rows, real delinquency status + real
  notes) → This Cycle/Next Cycle panels side by side (real `PayPeriodForecastService`,
  tabular-aligned due bills, real covered/short verdicts) → kept existing real Spend Bleed/
  Warnings/Current Plan panels below, rethemed. Wired `PayPeriodDueService`/
  `PayPeriodForecastService`/`DebtRepository` into `MainWindow`'s existing manual-DI pattern.
  No new backend logic — pure reuse of `DashboardService`/`DebtRepository`/
  `PayPeriodForecastService` as-is. Verified end-to-end against the real local `finances`
  Postgres DB via a throwaway scratch console project (outside the repo, deleted before
  finishing, not committed) calling the exact same Core services the UI calls: GATE readout
  correctly computes SHORT by $5,444.69 (Income Gate $1,495.64 + Reserve $993.37 − shortfall
  $7,933.70); Critical Debts strip correctly surfaces H1 Mortgage (150 days past due,
  delinquent) and Treasury Offset Program (delinquent, real payment-plan notes surfaced via
  the real Notes field — no fabricated "payment plan" status value); Cycle 1 Covered $620.50
  and Cycle 2 Short $1,216.21 — both cross-checked to match #2918's own recorded verification
  exactly, confirming danger color only appears on the genuinely short states and covered
  color only on the genuinely covered ones. `dotnet build ShanesSurvival.sln` clean (0
  errors/0 warnings). No real screenshot capture was possible from this non-interactive
  session (no live WPF/WebView2 desktop session to render into) — verification instead
  covers every real number/verdict the new layout renders, cross-checked against real DB
  state and #2918's own recorded figures; a visual screenshot pass against the approved
  mockup is still open and is the one part of the issue's verification checklist not done
  here. `git status --porcelain` clean (only build-journal/2919.md and the real feature
  commit's own files). Branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing this DONE (pushed directly to main per
  worktree-isolation convention, linear fast-forward, no rebase needed).
