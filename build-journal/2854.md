# #2854 — action.pim-approve-elevation can never run: mark appOnlyUnsupported

- **Status:** ✅ DONE 2026-09-04T16:05:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 3fec849d9 (already on main, prior session/build under #1901)

## Log
- 2026-09-04T15:58:32Z ⏳ IN FLIGHT — pulled #2854 body + comment, began verifying
  `graph-write-permissions.ts` for the requested `appOnlyUnsupported` rule.
- 2026-09-04T16:05:00Z ✅ DONE — Verified, not re-implemented: commit `3fec849d9`
  (landed under #1901, before this build was dispatched) already added the exact
  rule requested — `PATCH /roleManagement/directory/roleAssignmentApprovals/*/steps/*`
  carries `appOnlyUnsupported: true`, `permissions: []`, and the full justification
  quoting Microsoft's "Not supported." cells across all three permission tables
  (graph-write-permissions.ts:555-582). It is explicitly the only
  `appOnlyUnsupported: true` rule in the table (comment at line 555-556). Confirmed
  end-to-end wiring: `admin-write-permissions.ts` computes and returns
  `appOnlyUnsupportedSteps` as its own category, distinct from `unmappedSteps` and
  `missing`, so the executable correctly reports non-READY instead of the false-green
  an ordinary `permissions: []` rule would have produced. No code change was needed
  this session.
  The issue's own body already documents this (`What #1901 already did about it`)
  and lays out three remaining product options (drop the step / re-scope as a human
  approval / delegated auth) — that is a genuine product decision for Shane, not
  something to resolve in-session; left open for his call, not closed by this build.
