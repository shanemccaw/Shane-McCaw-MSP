# #2115 — Sharpen classifySnapshotFailure() using real observed wire evidence

- **Status:** ✅ DONE 2026-09-04T06:46:50Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 0ee9068d2 (main), 4f42ca55b (IN FLIGHT bookend)

## Log
- 2026-09-04T06:41:00Z ⏳ IN FLIGHT — Investigated real snapshot data (row 10, the largest real
  snapshot: 778 failures, 304 `unknown_error` = 39%, confirming the issue's numbers exactly) and
  folded in #1962's smaller/earlier snapshot-row-8 evidence. Implementing classifier branches
  keyed on the observed literals, plus two real wire-evidence capture-gap fixes found along the
  way (`LicenseGapError` not carrying `httpStatus`, and a non-JSON-body Graph response throwing a
  bare `Error` instead of `GraphPaginatedError`).
- 2026-09-04T06:46:50Z ✅ DONE — Real changes, commit 0ee9068d2 on `main`:
  - `artifacts/api-server/src/lib/config-snapshot-collector.ts`: `classifySnapshotFailure()` gained
    branches for `not_applicable_to_account_type` ("not supported for AAD accounts", "not
    applicable to target tenant", `AADSTS500011` — 178 rows on snapshot row 10) and
    `endpoint_not_found` (plain 404, OData "Resource not found for the segment", Graph's nested
    `apiNotFound` — 173 rows on snapshot row 10); extended `not_supported_app_only` to the Graph
    412 literal. The catch block's status/body extraction was also extended to read
    `LicenseGapError.httpStatus`/`.rawBody`, closing the real capture gap.
  - `lib/db/src/schema/config-snapshots.ts`: added the two new `SnapshotSkipReason` values with
    real observed-evidence doc comments. Plain `text` column, no CHECK constraint tied to the
    enum values — **no manual SQL migration needed**, confirmed via `\d
    tenant_config_snapshot_resource_status` (only a `reason_required` CHECK on
    status/skip_reason-nullness, nothing enumerating the values).
  - `artifacts/api-server/src/lib/graph.ts`: `LicenseGapError` gained an optional `httpStatus`
    field (default `null` for backward compat), set from the real `res.status` at its one throw
    site. This was the actual cause of 31 of the snapshot's 32 no-http_status/no-error_code rows —
    the status was sitting right at the throw site and was simply never carried.
  - `artifacts/api-server/src/lib/monitor-executor.ts`: the non-JSON-Graph-body branch now throws
    `GraphPaginatedError(res.status, resolvedEndpoint, bodyText)` instead of a bare `Error` — this
    was the real cause of the 32nd no-evidence row (`graph:beta:/solutions/migrations`).
  - New `artifacts/api-server/src/lib/config-snapshot-collector.test.ts` (13 tests, all real
    observed literals lifted verbatim from the DB investigation below), registered in
    `vitest.config.ts`'s explicit include allowlist.
  - **Verification:** ran the real DB queries against local Postgres (`DATABASE_URL`) to pull
    snapshot row 10's actual `skip_reason`/`http_status`/`error_code` breakdown and the 32
    no-evidence rows' real bodies before writing any classifier branch — every literal above is
    copied verbatim from that output, not guessed. `npx vitest run
    src/lib/config-snapshot-collector.test.ts` → 13/13 passed. `npx tsc --noEmit -p .` (after
    rebuilding `lib/db`'s `tsc -b` project-reference output, stale from before this session's
    schema edit) → clean, no errors in any touched file.
  - **Pre-existing, unrelated test failures found and filed, not fixed:** `npx vitest run
    src/lib/__tests__/monitor-executor.test.ts` fails 13/203 both before and after this session's
    changes (confirmed via a temporary isolated git stash of just this session's diff) — the
    `vi.mock("../power-platform-admin", ...)` factory doesn't export the real
    `PowerPlatformNotRegisteredError` class, so `monitor-executor.ts`'s `instanceof` check on it
    throws inside the mock. Filed as #2833, parented under #1850 (this issue's own Feature
    parent), labeled `bug`, board status set to AI Batter Up. Not fixed in this session — out of
    #2115's scope and pre-existing.
  - **Working tree:** clean (`git status --porcelain` empty) after the final push.

