# #2233 — Feature: Chat Tool Belt (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T15:45:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** 7b05676a4 (on main) — Chat Tool Belt: unify all 12 tools into one wrench menu (#2233); ca1e5adee (bookend IN FLIGHT, folded in via rebase)

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — Real audit: docs/Toolbelt.md and wpf-handoff/notes/Toolbelt.md are confirmed stale (describe the old vault/batter/matrix/gate/health/settings belt, not the real 12-tool wrench menu). Found the wrench menu (`BtnWrench_Click`/`BuildWrenchMenu` in MainWindow.xaml.cs, Git #2209 §2) already exists and already wires 10 of the 12 real tools (Log Peek, SQL Runner, JSON Viewer, Windows File Browser, Git Map, PowerShell, Terminal, API Runner, Graph Read, Graph Write). Missing: Git Doctor (#2218, DONE) and Repo Health (#2214, DONE) — both landed with real service/panel state but never added as wrench-menu entries. Also found 7 scattered standalone toggle buttons in the context bar (BtnToggleLogPeek, BtnToggleSqlRunner, BtnToggleRepoHealth, BtnToggleJsonViewer, BtnToggleFileBrowser, BtnToggleGitMap, BtnToggleGitDoctorMini) duplicating access to tools already reachable from the wrench menu — exactly the "scattered per-panel toggle buttons" the issue calls out for removal. Confirmed via build-journal/*.md that all 9 in-scope tool issues (#2194, #2200, #2213, #2214, #2215, #2216, #2217, #2218, #2219, #2220) are DONE; #2202 (full API Runner/Graph Read/Graph Write document) is out of scope per the dispatch and stays on the mini-rail path (#2220, already DONE) the wrench menu already used.
- 2026-09-02T15:45:00Z ✅ DONE — Added Git Doctor (#2218) and Repo Health (#2214) entries to `BuildWrenchMenu()` (reusing `_gdMiniOpen`/`BtnToggleGitDoctorMini_Click` and `_repoHealthOpen`/`BtnToggleRepoHealth_Click`, no new panel logic), reordered the menu to match the real 12-tool spec order, and removed the 7 now-redundant scattered per-panel toggle buttons from the chat context bar (BtnToggleLogPeek, BtnToggleSqlRunner, BtnToggleRepoHealth, BtnToggleJsonViewer, BtnToggleFileBrowser, BtnToggleGitMap, BtnToggleGitDoctorMini) — their click handlers and backing panel/column state are untouched, only the duplicate bar entry point is gone. `BtnToggleDetectedItems` and `BtnFloaty` were kept since Detected panel / Floaty aren't part of the 12-tool spec. Rewrote `docs/Toolbelt.md` with the real audited 12-tool spec + status table (it previously described an unbuilt per-tab accumulating-belt design lifted from an old Shell Skeleton v2 mockup) and flagged `wpf-handoff/notes/Toolbelt.md` as the stale historical design-export note it is, pointing readers to the real doc. Verification: `dotnet build ShaneBuilder.csproj -c Debug` succeeded both before and after the XAML button removal (0 errors); launched the built `ShaneBuilder.exe` standalone (no other instance was running — confirmed via `tasklist` before and after) and it stayed alive 5s with a real main window (smoke test that the new `BuildWrenchMenu()` doesn't throw on construction/paint), then terminated cleanly by PID. A live screenshot capture was attempted for visual confirmation but blocked by the session's own permission classifier (window-focus/screen-capture PowerShell denied) — not a project rule, just this session's guardrail; smoke test + code review stand in for it. No test manifest written — ShaneBuilder is internal tooling, explicitly out of the msp-portal/marketing-site test-manifest scope. Working tree confirmed clean (`git status --porcelain` empty) and branch confirmed merged into `origin/main` via `verify-branch-merged.mjs` (commit 7b05676a4).
