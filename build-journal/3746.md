# #3746 — Customer directory metrics lack signal severity — MSP Console tree can't show the critical (red) status dot

- **Status:** ✅ DONE 2026-09-13T00:12:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** cb3c51fe1 (bookend IN FLIGHT), a9dbbc62d (real work)

## Log
- 2026-09-13T00:06:00Z ⏳ IN FLIGHT — extend `CustomerDirectoryMetrics` with a real severity split (`criticalSignals`) so `treeModel.ts`'s `statusDotColor` can render the design's 4th (critical/red) tree status dot.
- 2026-09-13T00:12:00Z ✅ DONE — Added `criticalSignals: number` to `CustomerDirectoryMetrics`
  (`artifacts/api-server/src/lib/msp-customer-directory-metrics.ts`): one combined raw-SQL
  GROUP BY query joins `tenant_signal_history` (resolvedAt IS NULL) against a
  `signal_derivation_rules` ∪ `signal_rule_groups` severity map (platform-default
  `msp_id IS NULL` rows — zero MSP-specific severity overrides exist today, confirmed
  against local Postgres), counting `severity = 'critical'` alongside the existing
  openSignals count — still one query, not a second query per tenant. Chose a plain
  count over a `worstSeverity` enum: `statusDotColor` only needs "is any open signal
  critical", and `openSignals - criticalSignals` already answers "any non-critical
  open signal exists", so no `warningSignals` counterpart was added.
  `artifacts/msp-console/src/console/treeModel.ts`'s `statusDotColor` now renders
  `#f87171` (critical) when `criticalSignals > 0`, ahead of the existing
  warnings/healthy/never-scanned checks — the design's 4th tree-sidebar state is now
  reachable. `artifacts/msp-console/src/api/console-api.ts`'s `DirectoryCustomer` type
  and `artifacts/api-server/src/routes/msp-portal.ts`'s empty-metrics fallback both
  carry the new field. #3745 (generated MSP api-client staleness) has not landed —
  msp-console reads its own `DirectoryCustomer` type directly, not the generated
  client, so nothing there needed touching; the real route response carries
  `criticalSignals` regardless of that separate staleness.
  Verified against real local Postgres: customers 1 and 3 genuinely have 5 and 3 open
  critical signals respectively (not fixture data) — confirmed by running the exact
  query directly against `tenant_signal_history`/`signal_derivation_rules`/
  `signal_rule_groups`. Unit tests updated and passing: `msp-customer-directory-metrics.test.ts`
  (7/7, api-server vitest), `nav.test.ts` (16/16, msp-console tsx --test). Both
  packages typecheck clean (`tsc --noEmit`). No fixture/hardcoded data anywhere.
  `git status --porcelain` clean; branch verified merged into `origin/main`
  (`verify-branch-merged.mjs` — OK).
