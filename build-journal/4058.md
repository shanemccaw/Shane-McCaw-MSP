# #4058 — Projects: build the missing customer-facing closure sign-off endpoint

- **Status:** ✅ DONE 2026-09-14T15:22:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 4559426df (on main)

## Log
- 2026-09-14T19:05:00Z ⏳ IN FLIGHT — read admin-projects.ts closure-request route, project_closures schema, and portal-projects.ts GET /:id auth pattern; about to add POST /api/portal/projects/:id/closure on portal-projects.ts (update-only, never inserts).
- 2026-09-14T15:22:00Z ✅ DONE — Added `POST /api/portal/projects/:id/closure` (artifacts/api-server/src/routes/portal-projects.ts) using the same sibling-login customer-scoping as the existing `GET /api/portal/projects/:id`. Updates the existing `project_closures` row set by admin's `closure-request` route (signatureDataUrl, permissionGranted, feedback, signedAt, signerUserId); never inserts. 404 when no closure row exists or the project isn't the caller's; 409 if `signedAt` is already set. `GET /api/portal/projects/:id`'s `closure` field (from #1739) already reflected `signedAt`/`permissionGranted`/`feedback` — no change needed there. Commit 4559426df, pushed straight to origin/main (worktree's agent/4058-q2739 branch rebased onto main first). Live-verified against the real local Postgres + the real running dev api-server (merged via request-restart.mjs + refresh-main-server.mjs): created a real synthetic project (id 70, client_user_id 2757, status completed) + a real project_closures row (id 29) mirroring what admin's closure-request insert produces, signed it via a real HTTP POST (200, row updated with real signedAt/signatureDataUrl/permissionGranted/feedback/signerUserId), confirmed GET /api/portal/projects/70 reflects the signed state, confirmed a second sign attempt returns a real 409, then deleted both synthetic rows. tsc --noEmit passed clean. No fixture/hardcoded data added. `git status --porcelain` clean.
