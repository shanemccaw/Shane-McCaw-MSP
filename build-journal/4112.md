# #4112 — Billing: MSP-console retainer interval switch proposal, requiring customer approval

- **Status:** ✅ DONE 2026-09-14T21:22:16Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 19277acfd (main implementation), 3a4f840fb (manifest lastVerifiedAgainstCommit follow-up)

## Log
- 2026-09-14T20:47:31Z ⏳ IN FLIGHT — read portal-retainer-billing.ts's existing switch-interval/cancel-interval-switch state machine and retainer-pricing.ts mechanics; researching MSP-console (Feature #1692, blocked on Design #2608 for the full Billing screen) and the customer portal billing page before extending the state machine with a propose/approve state.
- 2026-09-14T21:22:16Z ✅ DONE — Shipped the propose/approve/reject state machine, extending (not forking) the existing switch-interval mechanics:
  - Schema: `client_services.proposed_billing_interval` / `proposed_by_user_id` / `proposed_at` (Drizzle + `lib/db/migrations/manual/2026-09-14-retainer-interval-switch-proposal-4112.sql`, run against local Postgres and verified via `\d client_services`).
  - New MSP-console routes (`artifacts/api-server/src/routes/msp-retainer-billing.ts`): propose-interval-switch / cancel-proposal, `ladder.msp-operator` + IDOR-checked against the customer's own tenant. Never touches Stripe.
  - New customer routes in `portal-retainer-billing.ts`: approve-interval-proposal / reject-interval-proposal, `billing.manage`-gated, reusing a newly-extracted `applyIntervalSwitch` helper shared with the existing self-service switch-interval route (no reimplemented scheduling math).
  - Real audit log entries (propose/approve/reject/withdraw) via the existing `createAuditLog`, and a real portal notification fan-out on propose via `createNotification`.
  - MSP-console operator UI: a narrow `RetainerIntervalSwitch` module mounted at the previously-unwired "billing" nav slot (Feature #1692's full Billing screen stays blocked on Design export #2608 — this is a deliberately separate, small write affordance, not that screen). A concurrent build (#4111, Seat Pricing) claimed the same nav slot for a different feature; resolved the rebase conflict by stacking both components rather than dropping either.
  - Customer portal: an approve/reject card on `billing.tsx`, rendered only when a proposal is pending — explicitly documented as distinct from the page's own excluded "hypothetical-repricing calculator" (the customer only ever decides on an already-made proposal, never picks a target interval themselves).
  - Extended `test-manifests/portal/billing.json` with `sessionTag: "4112"` apiTests (5 new: 3 auth-gates, a live retainer-intervals read, a reject-404 check). Honestly documented in the manifest's own notes: NOT run live via `shaneapp://runTest` this session — no `shaneapp://` tool was reachable from this build session (checked via a real tool-availability search, not assumed), and the shared dev server that protocol targets runs the merged main checkout, which didn't carry this branch yet. Verified instead by real, executed evidence: `tsc --noEmit` clean (0 errors) across `artifacts/api-server`, `artifacts/msp-console`, `artifacts/portal`; `portal-retainer-billing.test.ts` / `portal-retainer-billing-scope.test.ts` / `portal-billing-capability-gates.test.ts` (55/55 passing, including the refactored switch-interval path); a full `vitest run` across api-server confirming no regressions (3 pre-existing/unrelated failures confirmed via isolation re-runs, not caused by this change — an rbac-capability consent test, a slow nul-byte-guard scan, and DATABASE_URL-dependent live-db tests); and the real manual migration run against local Postgres.
  - Verified branch is an ancestor of origin/main (`verify-branch-merged.mjs`, both commits) and `git status --porcelain` clean before writing DONE.
