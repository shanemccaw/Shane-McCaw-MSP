# #3760 — Inbound webhook activity backend for MSP operators

- **Status:** ✅ DONE 2026-09-13T01:40:00Z
- **Scope:** platform
- **Started:** 2026-09-12

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — building a real backend surface exposing `msp-billing-webhook.ts`'s real inbound Stripe event activity to MSP Console operators, per #3760 (Feature #1693). Plan: new `inbound_webhook_events` table + manual migration, log every event `dispatchMspStripeEvent` receives (source, providerEventId, eventType, outcome, summary, mspId), new read route(s) under msp-console, wire `Webhooks.tsx`'s inbound tab against it — replacing the stale "nothing happens with them" claim for the Stripe card only. The design's second "Internal service callbacks" card describes `msp-webhooks.ts`, a genuinely still-stubbed route already tracked separately by #2700 — left as honest static copy, not touched by this build.
- 2026-09-13T01:40:00Z ✅ DONE — commit `363435977`.
  - **Schema + migration:** `inboundWebhookEventsTable` (`inbound_webhook_events`) added to `lib/db/src/schema/msp.ts`; `lib/db/migrations/manual/2026-09-12-inbound-webhook-events-3760.sql` written and **run by me** against local Postgres (`shanemccawmsp`) this session — `CREATE TABLE`, 3 indexes, self-marking insert all confirmed via psql.
  - **Backend:** `dispatchMspStripeEvent` (`artifacts/api-server/src/routes/msp-billing-webhook.ts`) now writes one real receipt row per Stripe event it is called with — production webhook route AND the admin-testbed billing simulator both flow through it, so simulated test events are logged too. Records `outcome` (`processed`/`ignored`/`error`), a real per-event-type `summary` (drawn from the file's own documented handler behavior, updated to also cover #3796's concurrently-merged msp_offer checkout path), and `mspId` resolved via a real `mspSubscriptionsTable` lookup (with a metadata.mspId fallback for the one-time msp_offer checkout path, which has no Stripe subscription to look up). New `GET /api/msp/customers/:customerId/webhooks/inbound` route in `msp-console-webhooks.ts`, gated `ladder.msp-operator` + `assertCustomerAccess`, scoped by mspId resolved from `tenants.mspId`.
  - **Frontend:** `artifacts/msp-console/src/console/modules/Webhooks.tsx` gets its third "What comes in to us" tab, wired against `useInboundWebhookActivity` (`webhooks-api.ts`). Stripe card now shows real `configured`/`totalReceived`/`lastReceivedAt`/per-event-type `acted` state and a real recent-activity list, replacing the stale "nothing happens with them yet — billing state is not updated from these" claim. The second "Internal service callbacks" card (describes `msp-webhooks.ts`, tracked separately by #2700, genuinely still a no-op stub) is left as honest static copy — not wired to a backend that doesn't exist. Registered two new icons (`credit-card`, `server`) in `icons.tsx`.
  - **No fixture data.** Every value on the Stripe card comes from a real query against `inbound_webhook_events`/`tenants`.
  - **Live verification this session:** dispatched real synthetic-but-faithful Stripe events (an unhandled type, and `customer.subscription.updated`/`invoice.payment_failed` against the real, sanctioned `regression-testbed-msp` subscription) through the actual `dispatchMspStripeEvent`, confirmed correct `outcome`/`summary`/`mspId` rows via psql, replicated the new route's exact query composition (count, `selectDistinct` acted-types) and confirmed correct results, then deleted the synthetic rows and restored the testbed subscription to its clean baseline (`status=active`, `dunning_state=NULL`, `payment_failed_at=NULL`).
  - **Typecheck + tests:** `artifacts/api-server` and `artifacts/msp-console` both typecheck clean (two pre-existing, unrelated errors in `auth-impersonation.test.ts`/`portal-checkout.test.ts` confirmed present before my changes, untouched by me). `msp-billing-webhook.test.ts` — 11/11 passing (grew from 7 during a concurrent #3796 merge, all still green).
  - **Concurrent merge:** rebased onto `origin/main` twice for concurrent landings; one real conflict in `icons.tsx` (another session's `CreditCard` import for #3796) resolved by keeping both icons. `verify-branch-merged.mjs` confirms `agent/3760-q2484` is merged into `origin/main` at `363435977`.
  - **No findings filed** — no genuine out-of-scope bug/gap surfaced beyond what prior sessions (#2610/#2700) had already documented and filed.
  - Working tree clean (`git status --porcelain` empty) at completion.
