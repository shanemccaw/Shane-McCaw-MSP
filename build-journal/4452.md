# #4452 — Change control register 402 despite "add-on active"

- **Status:** ✅ DONE 2026-09-17T02:42:56Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** b7bd8aeea (fix + new manifest), aad6ed087 (manifest verified-commit stamp)

## Log
- 2026-09-17T02:20:46Z ⏳ IN FLIGHT — tracing why GET /api/portal/change-control returns 402 while the header reads "change control add-on active"; also checking the concurrent GET /api/portal/risk-register 500.
- 2026-09-17T02:42:56Z ✅ DONE — Root cause traced, not assumed to match #4401:
  - **402 is the gate working as designed.** The account (shanemccaw+premier@outlook.com, tenant 2080) paid only for `monitoring-premier-micro` (checkout 20eddaff…, client_services 176 active). It never bought the Change Control add-on. `tenant_add_on_entitlements` has 0 rows platform-wide. Change Control is a separately priced add-on per #1173 and #1168 (2026-08-21); Premier's `includedFeatures` "change_control" is marked doc-only.
  - **The actual page bug:** "change control add-on active" was a hardcoded literal (change-control.tsx:95), so it proved nothing. The page rendered the 402 as "Couldn't load the change register", and React Query retried it. Fix: the register hook turns 402 ADD_ON_REQUIRED into a value, and the page now shows a separate "add-on not active" state. The header only says "add-on active" after the register returns 200. Reads behind the same gate no longer fire, and "Raise a change anyway" stays available (POST is ungated per #1168).
  - **risk-register 500 is unrelated and transient.** The portal Vite log shows `http proxy error: /api/portal/risk-register … ECONNREFUSED` at 9:48:28–29 PM, while the API server was restarting (new pid booted 21:48:32). It returns 200 for the same account now.
  - **Verification:** portal `tsc --noEmit` clean. Live browser check (playwright-core/Edge) of the worktree portal Vite against the real :8080, using a synthetic zz-test-4452 Customer on a synthetic tenant. Gated: one 402, the add-on card, no failure card, the dialog opens. Entitled (temporary row): "Live · genuinely empty", header "add-on active", all 5 change-control reads 200. The synthetic user, tenant, entitlement and related audit/session rows were deleted afterwards.
  - **runTest (honest):** `shaneapp://runTest` on the new `test-manifests/portal/change-control-addon-gate.json` (onlyTag 4452-addon-gate): **0/4 passed**. SETUP login returned 401 because the Customer RBAC test account doesn't exist in the reset dev DB. See #4464.
  - **Findings filed (all under Feature #1486):** #4462 (the add-on can never be granted: no purchase path, no entitlement writer; AI Batter Up; blocked_by #4463), #4463 (Ask Shane: does Premier include Change Control?), #4464 (change-control.json's entitled premise is stale after reset; AI Batter Up; blocked_by #4463).
  - No migration. A temporary password hash on scratch user 3285 was restored byte-for-byte. Working tree clean.
