# #1917 — Reclassify 7 azure-rm resources above Lighthouse-delegable scope to `unavailable`

- **Status:** ✅ DONE 2026-09-04T03:26:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** a75d508d0

## Log
- 2026-09-04T03:10:00Z ⏳ IN FLIGHT — applying the already-confirmed #1917 finding: 5
  billing-account-scope + 2 tenant-root microsoft.aadiam-scope `azure-rm` resources sit
  above anything Azure Lighthouse can delegate, so `coverageStateFor()` must stop
  reporting them `uncovered`.
- 2026-09-04T03:26:00Z ✅ DONE — `coverageStateFor()` (lib/db/src/schema/config-state.ts)
  now takes the resource's own `availability` and returns a new `unavailable`
  `CONFIG_COVERAGE_STATES` value ahead of covered/uncovered (still behind
  `no_executor`). Both call sites + the summary tallies + the `?coverage=` filter in
  artifacts/api-server/src/routes/admin-config-resources.ts updated to the four-state
  model. Ran `lib/db/migrations/manual/2026-09-04-azure-rm-lighthouse-scope-unavailable-1917.sql`
  myself against local `DATABASE_URL` (5 rows + 2 rows updated) to replace the generic
  `availability_reason` on the 7 rows with the real family-specific reason ("billing-account
  scope, not delegable via Azure Lighthouse" for the 5 billing resources; "tenant-root
  microsoft.aadiam scope, not delegable via Azure Lighthouse" for the 2 diagnostic-settings
  resources) — `availability` itself was already `unavailable` from an earlier pass.
  Added 3 new coverageStateFor cases to config-state.test.ts (unavailable-wins-over-check-count,
  no_executor-still-wins-over-unavailable, falls-through when availability isn't 'unavailable').
  Verified: `pnpm --filter @workspace/db test` — 12/12 pass. `pnpm --filter api-server exec
  vitest run src/lib/__tests__/azure-rm.test.ts` — 18/18 pass (unaffected, confirms
  coverageStateFor stays backward-compatible with the 2-arg call sites there). `tsc --noEmit`
  clean in both lib/db and artifacts/api-server after rebuilding lib/db's stale dist/*.d.ts
  (an existing incremental-build artifact, unrelated to this change). No second consent flow
  built — per the issue, that stays a separate product decision. verify-branch-merged.mjs:
  OK, merged into origin/main. git status clean. No findings filed — this dispatch is the
  confirmed #1917 finding's own fix, not a new discovery.
