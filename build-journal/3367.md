# #3367 — Marketplace Purchase (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T00:52:10Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 96ab174d3 (contract pack), rebased+pushed to `main` as 6f966849; bookend commits
  d08b7c7c2 (IN FLIGHT), this DONE update

## Log
- 2026-09-10T00:45:25Z ⏳ IN FLIGHT — read `msp-marketplace-purchase.ts` (450 lines, 2 routes) + its
  test file in full, cross-referenced `portal-marketplace.ts`, `requireAuth.ts`, `catalog-pricing.ts`,
  `resolve-fulfillment.ts`, `tenant-billing-state.ts`, `audit.ts`, `sse-channels.ts`, `stripe.ts`, and
  the relevant schema (`salesOffersTable`, `servicesTable`, `mspSubscriptionsTable`,
  `tenantSubscriptionsTable`, `fulfillmentTypesTable`). Cross-checked live against local Postgres.
  Writing `docs/msp-console/marketplace-purchase-msp-console-contract-pack.md` next.
- 2026-09-10T00:52:10Z ✅ DONE — wrote and committed
  `docs/msp-console/marketplace-purchase-msp-console-contract-pack.md` (96ab174d3), read-only,
  every field cited to file:line, cross-checked live against local Postgres (`services`,
  `sales_offers`, `msp_subscriptions`, `fulfillment_types`). File confirmed genuinely complete as a
  surface (both routes live, mounted, tested) but audit surfaced 4 real, live-reachable bugs, all
  filed and parented under #1571, board status "AI Batter Up": **#3400** (paid-checkout failure
  leaves a permanently "accepted" sales offer with no charge collected — most severe), **#3403**
  (serviceClass=retainer, 6 live rows, charged once via PaymentIntent instead of a recurring Stripe
  subscription), **#3404** (fulfillmentTypeKey="assessment", 20 live rows, matches no
  `fulfillment_types` row and the discarded result gives no signal), **#3405** (free $0 path ignores
  `services.allow_free_checkout`). No product code, schema, or UI changed. Rebased onto
  `origin/main` and pushed directly (`6f966849`); `verify-branch-merged.mjs` confirms merged.
  `git status --porcelain` clean.
