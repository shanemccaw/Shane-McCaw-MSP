# #2827 — Remediation checklist PUT accepts accepted_risk with no signed decline flow (portal + MSP)

- **Status:** ✅ DONE 2026-09-04T20:35:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** dd382135e

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — add the same `accepted_risk` rejection guard the s1–s30
  tracker PUT routes already have (`portal-remediation-tracker.ts`, `msp-remediation-tracker.ts`)
  to the sibling findings-derived checklist PUT routes
  (`portal-remediation-checklist.ts`, `msp-remediation-checklist.ts`), which currently accept it
  with no signed `msp_risk_decisions` row behind it.
- 2026-09-04T20:35:00Z ✅ DONE — the session that opened this bookend never actually landed the
  guard on `main` (its worktree was swept; the diff only survived on an orphaned
  `rescued/2827-q1605-...` branch). Found and folded into `dd382135e` (#2869's build, which
  builds directly on this guard and could not ship without it existing): both PUT routes now
  reject a bare `status: "accepted_risk"` with 400, pointing at the new
  `POST .../decline-to-risk` route #2869 adds. Verified via
  `remediation-tracker-risk-decline.test.ts` + a clean `tsc --noEmit`.
