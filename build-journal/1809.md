# #1809 — Batter Up / AI Batter Up collapse toggle renders bright blue

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** c38cf8d4f (rebased/pushed as 2ac591875 on origin/main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — applying existing `ExpandCollapseToggleStyle` (DarkTheme.xaml ~1082) to `BtnCollapse` in `BatterUpPanel.xaml` and `AiBatterUpPanel.xaml`, removing the overridden bare Background/BorderThickness.
- 2026-08-29 ✅ DONE — applied `Style="{StaticResource ExpandCollapseToggleStyle}"` to `BtnCollapse` in both `BatterUpPanel.xaml` and `AiBatterUpPanel.xaml`, removing the bare `Background`/`BorderThickness`/`Foreground`/`Padding`/`Content` overrides that WPF's checked-state chrome was ignoring anyway; kept `IsChecked="True"` (the #1806 collapsed-by-default value, rebased in concurrently) and `Cursor="Hand"`. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors. Pushed to origin/main as 2ac591875; verified ancestor via `git merge-base --is-ancestor`. `git status --porcelain` clean.
