# #3268 — Feature: Pull-to-refresh gesture (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T22:10:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 916fe4a20 (real work, `web/shanes-life/public/{app.js,app.css,index.html}`), merged to `main` at 25cf5443f

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — investigating public/app.js render() and current room content DOM to design custom touch-based pull-to-refresh gesture (iOS PWA standalone mode has no native pull-to-refresh).
- 2026-09-08T22:10:00Z ✅ DONE — added `attachPullToRefresh()` in `web/shanes-life/public/app.js`: touchstart/touchmove/touchend handlers, live only when `window.scrollY === 0` and dragging downward (never mid-scroll), with a resisted drag distance driving `#view`'s transform and a new `#ptr-indicator` spinner's opacity/scale; crossing a 64px threshold and releasing calls the existing `render()` (~line 8901) to genuinely re-fetch/redraw the current room. Wired once at startup, gated off while `#app-view` is hidden (sign-in/enrolment). New CSS in `app.css` (`.ptr-indicator`, `.ptr-settling`, spinner keyframe) and a new indicator element in `index.html`.
  Verification actually performed: `node --check` on `app.js` (clean); a real local server boot (`node src/server.mjs`) against Shane's own local `finances` Postgres DB (the same DB the WPF app uses) came up clean with no migration/runtime errors, and the served `index.html`/`app.js`/`app.css` were confirmed to carry the new markup/logic. Full on-device verification — the issue's own stated bar: a real installed iOS Home Screen PWA, not desktop Chrome — is still Shane's to do; that environment does not exist on this build machine, and WebAuthn passkey sign-in makes it impractical to script an authenticated in-app touch test here. No findings filed; this was pure additive UI work with no backend/schema gap and no production boundary touched.
