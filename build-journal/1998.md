# Git #1998 — Build Queue card: show model and effort

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Commit:** c135b73202df669eda56cdc00ac8da8d0f4d0883

## Log
- IN FLIGHT — starting: add always-visible compact model/effort badge to `BuildQueueCard`
  (`desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`), plus a capped-reason badge on
  `capped` cards using `AccountCapPolicy.IsOpusModel`/`IsFableModel`/`IsAboveHighEffort`.
- ✅ DONE — `BuildQueueCard` now renders a compact `Model · Effort` badge (e.g.
  `Sonnet · High`, `Opus · xhigh`) in the top row of every card, in every status state —
  not gated on Conservation Cap. Added `ShortModelName`/`ShortEffortName` helpers (null in →
  null out, unrecognized model id passed through as-is, never guessed). On `capped` cards a
  second peach badge (matching #1989's existing capped colour language — no new accent)
  spells out which half of `AccountCapPolicy.ExceedsSonnetHigh` tripped the gate — Opus,
  Fable, and/or xhigh effort — via `AccountCapPolicy.IsOpusModel`/`IsFableModel`/
  `IsAboveHighEffort` directly, not re-derived. Both badges live in the horizontal (non-
  wrapping) top row alongside the existing issue-# and blocks-N badges, so card height is
  unchanged. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0
  warnings/errors, both before and after the rebase onto origin/main.
  Verified `c135b7320` is an ancestor of `origin/main` via
  `git merge-base --is-ancestor HEAD origin/main`. `git status --porcelain` clean.
  Not verified against Shane's live running BuildConsole.exe — did not relaunch/kill it per
  the build's standing rule; verification here is the clean build plus code-level review of
  every status branch (queued/running/verifying/capped/parked/failed/done/external/paused/
  blocked/waiting-for-input) confirming the badge logic runs unconditionally for all of them.
  No findings filed — nothing outside scope was discovered broken in this file.
