# #3637 — AdminV2 user-role grant can assign cap.purchases.approve below MSPOperator

- **Status:** ✅ DONE 2026-09-12T21:18:01Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** be5463ef7 (fix + tests); ff3c026f5/70b5d0238 (IN FLIGHT bookend)

## Log
- 2026-09-12T21:00:22Z ⏳ IN FLIGHT — Confirmed the gap: `POST /admin/rbac/user/:userId/roles`
  (admin-rbac.ts) → `assignUserRole` (lib/db/src/rbac/admin.ts) is a bare INSERT with no rung
  check, so a PlatformAdmin can grant the platform `cap.purchases.approve` role to a below-MSPOperator
  user; `usersHoldingCapability`/`purchaseApproverUserIds` then read that membership as real
  purchase-approval authority. #3570 closed the same gap only on the msp-settings toggle route.
  Plan: add a generic capability-role→ladder-floor registry in legacy-ladder.ts and gate the AdminV2
  route with the same `userClearsLadderCapability` evaluator #3570 used, applied to every capability
  role that carries a floor (currently cap.purchases.approve → MSPOperator).
- 2026-09-12T21:18:01Z ✅ DONE — Shipped in be5463ef7. Added `CAPABILITY_ROLE_GRANT_FLOORS`
  registry + `capabilityRoleGrantFloor` (legacy-ladder.ts: cap.purchases.approve → MSPOperator,
  the only rung-gated capability role; cap.team.manage / cap.changes.approve deliberately
  floorless per #3408). Added DB helpers `roleGrantFloor` / `getUserLadderIdentity` (admin.ts,
  barrel-exported) and gated the AdminV2 `POST /admin/rbac/user/:userId/roles` grant route with
  the same `userClearsLadderCapability` evaluator #3570 uses — refuses a floored capability-role
  grant to a below-floor target (400), 503 on unavailable model, 404 on missing user; revoke
  stays open. Generic: driven off the registry, so any future floored capability role is covered.
  Verification (all real, this session): api-server tsc --noEmit clean; lib/db tsc --noEmit clean;
  admin-rbac.test.ts 22/22 (4 new guard cases); legacy-ladder.test.ts 37/37 (3 new registry
  cases); admin.test.ts 11/11 live-DB rollback (3 new — roleGrantFloor resolves MSPOperator off
  the real seeded cap.purchases.approve role). Branch merged into origin/main (verify-branch-merged
  exit 0). No test manifest — api-server is out of scope for manifests per CLAUDE.md. No findings
  filed (no new gap discovered). No Shane To-Do (no manual migration; the fix is code-only).
