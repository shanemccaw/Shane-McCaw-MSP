# #1806 — Batter Up / AI Batter Up: default to collapsed on load

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** a0fb027af (rebased onto origin/main as e34a3961c)

## Log
- 2026-08-30 ⏳ IN FLIGHT — flip BtnCollapse IsChecked default to True on BatterUpPanel.xaml and AiBatterUpPanel.xaml; confirm initial render (RowsScroller/TxtEmpty) actually starts collapsed, not just the toggle state.
- 2026-08-30 ✅ DONE — Flipped `BtnCollapse` `IsChecked` default to `True` (Content `▸`) in both `BatterUpPanel.xaml` and `AiBatterUpPanel.xaml`. Confirmed `Initialize`/`RefreshAsync` never set `RowsScroller.Visibility` on load (only `BtnCollapse_Click` did) — the default `Visibility="Visible"` meant the panel rendered expanded on first launch regardless of the toggle's checked state. Added `Visibility="Collapsed"` directly on `RowsScroller` and `TxtEmpty` in both XAML files so the initial render genuinely starts collapsed, matching the toggle default; `RefreshAsync`'s own `anyVisible = BtnCollapse.IsChecked != true` check already keeps `TxtEmpty` collapsed consistently on subsequent refreshes. Toggle mechanism, row rendering (code-behind card builders from #1803), and everything else untouched. Rebased twice through concurrent #1803 (card-restyle) landings on main — resolved the two XAML merge conflicts by keeping #1803's `StackPanel`-based row container and layering this fix's `Visibility="Collapsed"` on top. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 warnings, 0 errors. Branch verified merged: `git merge-base --is-ancestor HEAD origin/main` passed at e34a3961c. `git status --porcelain` clean.
