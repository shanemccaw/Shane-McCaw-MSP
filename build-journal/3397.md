# #3397 — Point BuildConsole-Chat-Tools-Reference.md + dispatch docs at real `shanes-git` MCP connector

- **Status:** ✅ DONE 2026-09-10T20:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** d8bf198d5 (doc), 760212618 (IN FLIGHT bookend)

## Log
- 2026-09-10T20:40:45Z ⏳ IN FLIGHT — Read #3397 body + comments (real architecture correction: `shanes-git` is a standalone Replit Repl, not hosted inside Shane's Life). `BuildConsole-Chat-Tools-Reference.md` does not exist anywhere in the repo (confirmed via repo-wide search) — creating it fresh at `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md` documenting the real `shanes-git` connector and its real tool surface (already shipped in `artifacts/github-mcp-server`, per Feature #3377/#3390-#3395/#3538). No other doc in-repo instructs raw-PAT `bash_tool`+`curl` GitHub usage or implies Shane's-Life hosting, so no additional dispatch-doc edits are needed beyond the new reference doc.
- 2026-09-10T20:52:00Z ✅ DONE — Created `desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md` (commit `d8bf198d5`) covering: real standalone `shanes-git` deployment (own Replit Repl, own Postgres, own scoped PAT), explicit correction that this does NOT live inside Shane's Life, full real tool list (`server_status`, `github_whoami`, `get_issue`, `create_issue`, `update_issue`, `search_issues`, `post_comment`, `list_comments`, `close_issue`, `move_to_status`, `add_sub_issue`, `remove_sub_issue`, `list_sub_issues`, `set_blocked_by`, `list_blocked_by`, `get_recent_activity`), the required `context` param on every write tool (Git #3538), and the connect command. Verified via repo-wide grep that no other doc references raw-PAT `bash_tool`+`curl` GitHub usage as a standing convention needing correction (matches found were only in frozen/historical archives — `PLATFORM_BUILD.md`, old `build-journal/*` entries, audit docs — not live dispatch conventions, so left untouched per the frozen-archive rule). Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; `git status --porcelain` clean.
