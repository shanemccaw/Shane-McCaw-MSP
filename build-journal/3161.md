# #3161 — Feature: Bill due-date and Tax Levy notifications (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:35:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 50493c6a7

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — starting: build a real bill due-date reminder sweep
  reusing `money.getGateStatus`'s own `dueDay`/shortfall data (no second tracker), plus a
  distinct Tax Levy nudge for the real $242/month IRS installment in `debts`, following the
  existing day-before/vaccine reminder sweep pattern in `src/server.mjs` + `core/nudges.mjs`.
  #3137 (blocker) is closed/complete — unblocked.
- 2026-09-08T02:35:00Z ✅ DONE — `core/money.mjs`: `findDueBillReminders` (accounts.due_day,
  role='bill', 3-day lead, kind 'bill') and `findDueDebtReminders` (new `debts.due_day` column,
  5-day lead, kind 'debt_due' — generic by design, not name-matched to "Treasury Offset
  Program"; today it's the only debt row with a real due_day). Migration
  `migrations/038_debt_due_dates.sql` adds `debts.due_day` + backfills the real Tax Levy row
  (due_day=12) — run myself against local `finances`. Renumbered from 037 to 038 mid-session
  after `bin/check.mjs` caught a live ledger collision with a concurrent session's own
  `037_user_display_name.sql` (real-time collision, exactly the kind this issue's own prompt
  warned about); ledger row fixed up via `UPDATE schema_migrations`. Wired into
  `server.mjs`'s existing housekeeping sweep (boot + 6-hourly) as `runMoneyDueReminders`,
  same day-scoped dedup convention as the appointment/vaccine reminders beside it.
  Also fixed a real, pre-existing bug hit while verifying: `core/nudges.mjs`'s `queueNudge`
  bound `cap` as an unused `$2`, so Postgres rejected every real call ("could not determine
  data type of parameter $2") — silently swallowed by every caller's try/catch, meaning no
  nudge_events row (appointment/vaccine included) had ever actually been written via this path
  before this fix. Verified end-to-end against the real local `finances` DB: real candidates
  computed correctly at multiple `asOf` dates, `queueNudge` now succeeds, day-scoped dedup
  correctly excludes an already-queued row on a second pass; test rows cleaned up after.
  `bin/check.mjs`'s full boot check still fails on a *pre-existing, unrelated* orphan-ledger
  condition (035/036/037 files from other concurrent sessions not yet merged to origin/main) —
  not caused by this build, not fixable from here. Working tree clean; branch verified merged
  into origin/main (`node scripts/dev-server/verify-branch-merged.mjs`).
