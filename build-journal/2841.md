# #2841 — powershell-transport rows: is_collectable=true without a real catalog match

- **Status:** ✅ DONE 2026-09-04T16:05:00Z
- **Scope:** platform

## Log

- 2026-09-04T15:30:00Z — IN FLIGHT. Fixing `scripts/config-state/build-snapshot-registry.mjs`
  so it only marks a `read_transport = 'powershell'` row `is_collectable = true` when the
  row's own `read_cmdlets` actually has a match in `PS_CATALOG_BY_CMDLET`
  (`artifacts/api-server/src/lib/config-snapshot-collector.ts`), not just because
  `powershell` is generally executor-backed. Same denominator-inflation shape #1960 fixed
  for `graph`, at the scale this issue documents (398 of 404 `is_collectable`
  `powershell` rows).
- 2026-09-04T16:05:00Z — ✅ DONE. Added `PS_CATALOG_CMDLETS` +
  `PS_NON_READ_HELPER_CMDLETS` (mirrored sets, same convention `EXECUTOR_BACKED`
  already uses to mirror `EXECUTOR_BACKED_TRANSPORTS`) and `hasCatalogedPsCmdlet()` to
  `build-snapshot-registry.mjs`; `powershell` rows with no real catalog match now get
  `is_collectable=false` / `not_collectable_reason='no_executor'` with a notes string
  naming the missing cmdlet(s), matching the runtime `collectPowerShellResource()`
  message. Re-ran the script against local DB (no network, additive UPSERT).
  Verified: of 404 `powershell`-transport rows, 88 remain `is_collectable=true` and all
  88 independently re-verify against the real catalog cross-check query; 316 flipped to
  `is_collectable=false`/`no_executor`. Commit `11fb606c0` (rebased onto main as
  `7b56ca652`), pushed directly to `origin/main` (this repo's convention is commit
  directly to main). `git status --porcelain` clean.
  Filed **#2873** (sibling sub-issue of #1850, board status "AI Batter Up") for the
  identical gap on `sharepoint-admin` transport (23 rows, all `Get-Pnp*` cmdlets, none
  in `PS_CATALOG_BY_CMDLET`) — explicitly out of scope for #2841, which is scoped to
  `powershell` transport only. Note: the fix commit's message references "#2842" for
  this finding before it was actually filed as #2873 — correcting that here since the
  commit message itself isn't editable after push.
