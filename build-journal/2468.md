# #2468 — Import: bare short lines ("Run Test", "RAW JSON") split into their own notes

- **Status:** ✅ DONE 2026-09-02T22:24:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Test Pad)
- **Started:** 2026-09-02
- **Commit(s):** 127ae9011 (parser change), b2b530d9b (rebased/pushed to `origin/main`)

## Log
- 2026-09-02T22:10:00Z ⏳ IN FLIGHT — re-dispatch of #2347 (which self-blocked on #2344 and stopped
  before writing code; #2344 is now genuinely done). Read #2344's own landed implementation
  (`NotepadImportLineClassifier`, `TestPadImportParser.ParseCore`) — its own DONE log names the
  exact gap this issue closes: "a header line with nothing following it before the next blank
  line/section currently produces no candidate at all (silently dropped) rather than becoming its
  own note." That is this issue's real scope: a line that classifies as a Section header
  (`NotepadImportLineClassifier.IsSectionHeader`) but never gets a real content line filed under it
  before it's superseded (by another header or EOF) was never really a header — demote it and file
  it as its own note instead of dropping it.
- 2026-09-02T22:24:00Z ✅ DONE — Reworked `TestPadImportParser.ParseCore`'s header handling: a
  header-classified line no longer commits to `currentSection` the instant it's seen. It's held as
  a `pendingHeaderRawLine` until either (a) a real content line is filed under it — at which point
  it's confirmed as a genuine Section, exactly matching #2344's existing behavior (a header can
  still have a blank-line gap before its content and still count) — or (b) it's superseded by
  another header line or hit at end-of-paste with nothing ever filed under it, in which case
  `ResolveOrphanedHeader()` files the raw line itself as its own note (under whichever section was
  last *confirmed*, cascading correctly through chains of back-to-back orphaned headers) instead of
  silently dropping it. Verified with a scratch console harness (run from outside the repo/worktree,
  deleted after) exercising the real `TestPadImportParser.Parse` pipeline against: two consecutive
  bare headers ("Run Test", "RAW JSON") followed by real sentence content — "Run Test" correctly
  demotes to its own note (no section) while "RAW JSON" is legitimately confirmed as the section for
  the sentence that follows it, matching the issue's own worked example; a real header with a
  blank-line gap before its content (unchanged, still a legit Section); a header with content
  followed by a second, truly-orphaned header at EOF (the trailing header demotes to a note under
  the prior confirmed section); and two consecutive real headers each with their own content
  (unchanged). `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj` clean (0 warnings/errors).
  `node scripts/dev-server/verify-branch-merged.mjs` confirms `agent/2468-q1308` (b2b530d9) is an
  ancestor of `origin/main`. `git status --porcelain` clean. No test manifest —
  `desktop/ShaneBuilder` is BuildConsole-class internal tooling, out of the portal/marketing
  test-manifest scope. No findings filed this session — no genuine gap/bug discovered outside this
  issue's own scope.
