# #3203 — Shopping running total only ever updates via barcode scan

- **Status:** ✅ DONE 2026-09-08T03:52:05Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 8f0cd8959 (fix + log_price tool), merged to main at d6b52a4c0

## Log
- 2026-09-08T03:48:25Z ⏳ IN FLIGHT — confirmed root cause: `lists.mjs#getListDetail`'s
  `totalCents` sums `list_items.price_cents`, and only `core/scan.mjs`'s real barcode-scan
  save path ever writes that column. `prices.recordPrice` (the manual "Log price"
  form/`POST /api/prices`) only writes the separate `item_prices` history table. Implementing:
  (1) `recordPrice` also syncs `list_items.price_cents/price_source/priced_at` for a real
  shane-sourced observation, so the total moves without a scan; (2) a `log_price` MCP tool as
  the capture-grammar entry point for "chicken breasts are $3.49 now"; (3) remove the "Log
  price" form from `app.js`, routing through the capture box per the #3181/#3182/#3196
  no-forms pattern. Total-semantics question (checked-vs-all) already resolved by the
  existing #3111 code comment in `getListDetail` — "all items on the run," unchanged.
- 2026-09-08T03:52:05Z ✅ DONE — `core/prices.mjs#recordPrice` now, after writing the
  `item_prices` history row for a real `source: "shane"` observation, also runs an UPDATE
  syncing `price_cents`/`price_source: 'manual'`/`priced_at` onto every matching open
  `list_items` row for that user (case/whitespace-normalised text match, same as everywhere
  else in this app) — weekly-ad pushes deliberately excluded, they stay a separate
  `item.weeklyAdVerdict` concern. Added `log_price` to `src/mcp/tools.mjs` as the real
  capture-grammar entry point ("chicken breasts are $3.49 now" / "paid $12 for the detergent
  at Aldi"). Removed the "Log price" form + button from `public/app.js`'s `priceTools()`,
  replacing it with capture-box guidance text, matching the #3181/#3182/#3196 no-forms
  pattern; kept the "History" read-only button as-is. Live-verified against the real local
  Postgres `finances` DB (not a mock): added a real item to the real Shopping list for a real
  user, called `recordPrice(..., source: "shane")`, confirmed `list_items.price_cents` and
  `getListDetail`'s `totalCents` both updated from the logged price without any scan —
  before: `null`/`0`, after: `349`/`349`, `price_source: "manual"`. Test rows cleaned up
  after. Commit `8f0cd8959`, merged onto `main` at `d6b52a4c0` (`verify-branch-merged`-style
  ancestor check passed: `git merge-base --is-ancestor HEAD origin/main` → true). Working
  tree clean at finish. No new issues found worth filing separately for this build.
