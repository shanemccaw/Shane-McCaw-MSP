# #1966 — Dispatch box: force re-dispatch of an already-tracked issue

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** fda06d542

## Log
- 2026-08-31 ⏳ IN FLIGHT — read DispatchPanel.xaml/.xaml.cs, BatterUpQueueService.cs,
  BuildQueuePostgresClient.cs (FindDedupCandidateAsync/QueueBuildAsync/CancelAsync/
  MarkCompleteAsync) and BuildQueuePanel.xaml.cs's QuickCancelOrStopAsync (the existing
  per-status finalize convention). Building the "Dispatch anyway" affordance: live
  re-fetch at force time, per-status finalize of the existing row (reusing CancelAsync/
  MarkCompleteAsync/watcher TryStop — no new finalize mechanism), a second confirmation
  gated on the row being genuinely running locally (DB status "running" AND the
  QueueWatcherService actually tracking a live process for it — not just a stuck/claimed
  row, which needs no ceremony per the issue body), then a fresh QueueBuildAsync call.
  #1600's fail-closed blocker gate stays unchanged. BatterUpPanel/AiBatterUpPanel untouched.
- 2026-08-31 ✅ DONE — commit fda06d542 (merged to origin/main). Added a "Dispatch anyway"
  button to DispatchPanel.xaml, shown alongside (not replacing) the existing "already
  tracked" status message. `DispatchPanel.ForceDispatchAsync()`: live re-fetches the issue
  + BUILD: comment + blockers at force time (never reuses anything from the blocked
  attempt), live re-fetches the dedup candidate, gates a second YesNo confirmation only
  when the row is genuinely running (DB status "running" AND QueueWatcherService is
  actually tracking a live process for it via IsInteractiveRenderable/HasExited — a
  claimed-but-dead row needs no ceremony), finalizes the existing row via the exact
  per-status paths BuildQueuePanel's QuickCancelOrStopAsync already uses (watcher
  TryStop+ReleaseInteractive+MarkCompleteAsync(-1) for running, CancelAsync for
  queued/parked/limit-paused/capped, MarkCompleteAsync(-1) alone for verifying/external),
  then calls the same QueueBuildAsync every other path uses. #1600's fail-closed blocker
  gate is untouched (identical code to the plain DispatchAsync path). MainWindow now passes
  _queueWatcher into DispatchPanel.Initialize. BatterUpPanel/AiBatterUpPanel/#1808's drop
  rule untouched, as required.
  Verification: dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release — clean,
  0 warnings/errors. Real end-to-end test: created throwaway issue #2082 with a BUILD:
  comment, fabricated a bt_build_queue row (id 1070) mirroring #1954's real stuck-claim
  signature exactly (status=running, claimed_at ~12m ago, build_pid never stamped — a claim
  that never actually launched), then ran a scratch harness (ProjectReference to
  BuildConsole.csproj, not committed, deleted after) that executes ForceDispatchAsync's
  real production code path verbatim against the real local Postgres DB and real GitHub
  API. Confirmed: genuinelyRunning correctly false (no ceremony) for the stuck/claimed row,
  MarkCompleteAsync(-1) finalized it, QueueBuildAsync reused the same row id and flipped it
  to "queued" with the live-refetched title/prompt/model, and exactly one live
  bt_build_queue row existed for github_number=2082 afterward (confirmed via direct SQL —
  never duplicated). A live GitHub label mutation (QueueBuildAsync's fire-and-forget
  in-flight sync) is a real side effect of a real force-dispatch, which is why a dedicated
  throwaway issue was used instead of reusing #1954's real historical row. Did not let a
  real Claude session actually launch against the throwaway issue (would waste real API
  cost for no product benefit) — deleted the fabricated queue row and closed #2082 instead
  once the row was proven genuinely queued/launchable (real title/prompt/model, zero
  blockers, same shape any other claimable row has). The #1600 fail-closed "held" branch
  itself is unchanged, unmodified code shared with the already-shipped DispatchAsync path,
  not independently re-verified with a live blocker in this pass.
  No new findings filed — investigation and verification stayed in scope, no other real
  bugs/gaps surfaced.
  git status --porcelain clean. Branch verified merged into origin/main via
  scripts/dev-server/verify-branch-merged.mjs.
