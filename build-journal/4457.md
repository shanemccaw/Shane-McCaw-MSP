# #4457 — Git Board: remove the "UNASSIGNED" status pill

- **Status:** ✅ DONE 2026-09-17T02:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 05218b038

## Log
- 2026-09-17T02:20:00Z ⏳ IN FLIGHT — confirmed current code matches issue body (SetLiveMilestones ternary, demo Feature #1215, xaml DataTriggers). Applying: drop "UNASSIGNED" fallback (Status = "" instead), remove UNASSIGNED DataTriggers from PillBorder/TextBlock styles, add StringNonEmptyToVisibilityConverter binding on PillBorder to collapse it when Status is empty, update demo data.
- 2026-09-17T02:35:00Z ✅ DONE — `SetLiveMilestones` ternary now yields `""` instead of `"UNASSIGNED"`; XAML `PillBorder` gained a `Visibility` binding via `StringNonEmptyToVisibilityConverter` (reused existing converter from `Converters/ChatPaneConverters.cs`, registered locally) so the pill collapses entirely when `Status` is empty, and both `UNASSIGNED` `DataTrigger`s (Border style + TextBlock foreground) were removed. Demo Feature #1215 no longer sets `Status` (defaults to none shown). `LocateAndExpandEpic`'s `FOCUS`/`ACTIVE` check confirmed unaffected — no change needed. `dotnet build BuildConsole.csproj -c Debug` clean, 0 warnings/errors. Commit 05218b038 pushed and rebased onto origin/main (verify-branch-merged OK). No genuine findings surfaced beyond what the issue already named.
