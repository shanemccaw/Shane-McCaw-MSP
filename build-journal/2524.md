# #2524 — Policy Engine: OU assignment — customer read + request-change (split from #2148)

- **Status:** ✅ DONE 2026-09-06T18:25:15Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 094b57d23 (pushed to main as 79e923937 after rebase)

## Log
- 2026-09-06T18:06:07Z ⏳ IN FLIGHT — investigated #2524 + #2148 + the Policy Decisions
  contract pack + `Design/portal/` (no `.dc.html` covers OU assignment/Policy Engine —
  staying backend-only, matching #2148's own precedent). Deciding the CR-pipeline-vs-
  dedicated-table sub-question next: `msp_change_requests` models a real M365 tenant
  configuration write (backup snapshot, rollback script, freeze-window gating, the
  `executorRunId` write-authorization claim) — an OU assignment change is NOT a tenant
  write at all (`msp-active-directory.ts`'s POST only reads Graph to verify the object
  exists, then writes our own DB row; no Graph write, ever). Shoehorning this into the CR
  table would mean fabricating meaningless values for `targetResource`/`psaTicketId`/
  `backupHash`/`rollbackScriptSnippet` and misfiling an internal grouping request
  alongside real tenant-config changes in the Change Control register. Building a
  dedicated, lightweight `active_directory_ou_assignment_requests` table instead, with a
  real MSP-side list+resolve surface so the request is a real round trip, not a black
  hole.
- 2026-09-06T18:25:15Z ✅ DONE — Shipped:
  - `lib/db/src/schema/index.ts` + `lib/db/migrations/manual/2026-09-06-ou-assignment-requests-2524.sql`
    (ran myself against local Postgres — real columns verified live): new
    `active_directory_ou_assignment_requests` table.
  - `artifacts/api-server/src/routes/portal-active-directory.ts` (new): customer-scoped
    `GET .../ou-assignment` (this tenant's OUs + real manual assignments, DB-only — "OU
    assignment" IS the manual-override row, no live Graph needed, matching the MSP-side
    read's own convention), `GET/POST .../ou-assignment-requests`. Floors at
    `requireRole("CustomerUser")` — real employee UPNs, above the free Assessment tier.
  - `artifacts/api-server/src/routes/msp-active-directory.ts`: added
    `GET/PATCH .../ou-assignment-requests` so the MSP can see and resolve a request;
    approving/fulfilling one with a real `requestedOuId` immediately applies the real
    assignment via the same Graph-verified upsert the existing POST route uses.
  - Registered `portalActiveDirectoryRouter` in `routes/index.ts`.
  - No UI this pass: no `.dc.html` export in `Design/portal/` covers OU
    assignment/Policy Engine read or request-change —
    `Policy Decisions.dc.html` itself explicitly excludes standing
    policies/target settings from that page. Backend-only, matching #2148's own
    precedent.
  - Tests: `portal-active-directory.test.ts` (new, 17 cases) +
    `msp-active-directory.test.ts` (extended, +14 cases for the new MSP routes) — 37/37
    passing. Both files had to be added to `artifacts/api-server/vitest.config.ts`'s
    explicit `test.include` allowlist to run at all — discovered `msp-active-directory.test.ts`
    (#2148) had never been added and had silently never run since landing. Fixed
    incidentally here; filed as its own finding, **#3047**, sibling of this issue's Feature
    parent **#1490**, board status set to "AI Batter Up".
  - `tsc -p tsconfig.json --noEmit` clean (whole `api-server` package, no new errors).
  - Verified real DB state before building: `active_directory_ous` and
    `active_directory_ou_assignments` are genuinely empty on local Postgres (0 rows each)
    — confirmed live, not assumed; every response this build serves for that state is a
    genuine empty register, not a fabricated row.
  - Branch verified merged into `origin/main` (`verify-branch-merged.mjs`) at `79e923937`;
    working tree clean.
