# #4397 — Monitoring scan kickoff backstop never fires for account-first buyers

- **Status:** ✅ DONE 2026-09-16T18:50:08Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 4f896e0fc (fix), 891951153 (live-db test) — landed on main as c54c5724 (after rebase)

## Log
- 2026-09-16T18:40:37Z ⏳ IN FLIGHT — read #4397 body + comments, read the account-first flow (#4374/#4377/#4378) to confirm account-first buyers never reach set-password after paying (promoteAccountFirstBuyerOnPayment / public-purchase-payment.ts confirm handler is the one step they do reach); adding the ensureMonitoringScanKickoff call there instead of set-password's already_set branch.
- 2026-09-16T18:50:08Z ✅ DONE — Fixed by moving the kickoff to the payment-confirmed step (public-purchase-payment.ts's `/payment-confirmed` handler), per the issue's own suggested "or move the kickoff to the payment-confirmed step" shape: after the existing account-first Customer promotion block, resolve the paid session and call `ensureMonitoringScanKickoff` when `accountUserId` is already recorded (which it is for every account-first buyer, set by `createPreConsentAccount` at pre-consent account creation, well before payment). No-op for legacy pay-then-account sessions (accountUserId isn't set until set-password) and for non-monitoring products (ensureMonitoringScanKickoff's own product gate). Typechecked clean (`tsc --noEmit`). Live-verified with a new live-db test (`public-purchase-payment-account-first-monitoring-kickoff.live-db.test.ts`) that seeds a real msp/tenant/user/checkout-session shaped exactly like the account-first door's output and drives the real `resolvePaidPurchaseSession` + `ensureMonitoringScanKickoff` call path the route now uses — 3/3 passed against local Postgres, confirming: (1) the session resolves with the pre-consent-recorded accountUserId at payment time, (2) the kickoff fires and a diagnostic run trigger is invoked with the tenant as customerId, (3) it is idempotent (no-op) when a run already exists. Test rows swept in afterAll, confirmed empty via a throwaway scratch check afterward (not committed). No fixture/hardcoded data — everything reads real catalog/DB rows. No genuine findings surfaced beyond #4397 itself; nothing new filed. Working tree clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
