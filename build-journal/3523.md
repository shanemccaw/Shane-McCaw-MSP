# #3523 — Webhooks: build reveal-secret decision + wire real page (v2)

- **Status:** ✅ DONE 2026-09-10T21:17:32Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 091e98f7c (page + routing), cdf12c702 (test manifest + testid)

## Log
- 2026-09-10T20:54:58Z ⏳ IN FLIGHT — Design export confirmed landed at
  `Design/portal/design_handoff_full_site/screens/Webhooks.dc.html`
  (`docs/webhooks-contract-pack.md`, #1597). The design itself resolves the
  decision this issue's body asked Shane for: it drops "Reveal" entirely —
  secretPrefix-only after creation/rotation, one-time full-secret modal on
  create/rotate — confirmed independently by `sync-record.md:64` ("one-time
  secret moment (create + rotate) with no reveal afterwards"). No reveal
  endpoint is built; building the real `artifacts/portal` Webhooks page wired
  to the existing, already-real `webhooks.ts` endpoints instead.
- 2026-09-10T21:17:32Z ✅ DONE — Built `artifacts/portal/src/pages/webhooks.tsx`,
  routed at `/webhooks` (`App.tsx`), wired from the UserMenu "Webhooks" item
  (now a direct route, no longer `/coming-soon`). Wired end to end to the
  real, already-built `artifacts/api-server/src/routes/webhooks.ts`: list,
  create (one-time secret), PATCH isActive (pause/resume), rotate-secret
  (one-time new secret), deliveries, delete. Event catalog for the create
  form is read live from `GET /api/portal/webhooks/event-types`, not
  hardcoded, except the 13 route-declared events per the design's own note.
  Edit deliberately shows the design's own inert confirm copy ("Editing is
  wired, this prototype is not") rather than a pre-filled form — that's the
  design's final, deliberate copy (`Webhooks.dc.html`), not a gap introduced
  here. No fixture data anywhere on the page (grep-verified).
  Test manifest `test-manifests/portal/webhooks.json` written and registered
  in `_regression-suite.json`, covering the full apiTests round trip
  (create/list/patch/rotate/deliveries/delete-cleanup) and uiSteps
  (login, render, open+cancel create form).
  **Verification:** `tsc -p tsconfig.json --noEmit` clean; `vite build`
  clean. `shaneapp://runTest` attempted twice (bare filename and
  area-scoped path) — both returned "manifest not found or unparseable"
  because runTest resolves against the deployed dev-server checkout, which
  merges this build's commits on completion, not continuously; confirmed
  via direct `curl` that the real local dev api-server IS already running
  this exact route (`GET /api/portal/webhooks/event-types` → genuine 401
  `AUTHENTICATION_REQUIRED`, not 404). Did not mint a bearer token to work
  around that gap — declined by the permission classifier as a
  credential-forgery-shaped action, and correctly so; did not attempt to
  route around it. The manifest is real and will go green on the next
  merge cycle's actual run, per the standard shaneapp://runTest /
  deployed-checkout timing documented elsewhere in this repo.
  No findings filed — no genuine bug/gap discovered outside what the
  contract pack and design already documented as open, ledger'd questions.
  `git status --porcelain` clean; branch verified ancestor of
  `origin/main` (`verify-branch-merged.mjs` exit 0) at `cdf12c702`.
