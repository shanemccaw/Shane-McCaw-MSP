# #3037 — Remediation Tracking: wire the s1-s30 core tracker grid

- **Status:** ✅ DONE 2026-09-06T19:54:42Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 6c1e33d08 (rebased from da5230ad1)

## Log
- 2026-09-06T19:42:34Z ⏳ IN FLIGHT — Wiring §1a of docs/remediation-tracking-contract-pack.md
  (portal-remediation-tracker.ts: GET tracker + pricing, PUT step status, POST verify,
  GET verification-guide, POST decline-to-risk) to a new real page, against the Design
  export's "28-step programme" tab (`Design/portal/design_handoff_full_site/screens/Remediation Tracking.dc.html`).
  Scope boundary confirmed against sibling issues dispatched in the same buildSet:
  #3038 owns §1b-1e (checklist/fix-routes/reveal/bypass tabs), #3039 owns §1f-1g
  (pillar-scores panel + CSV/PDF/evidence exports). This build implements only the
  step grid + phases/fees pricing + hire CTA, isolated into their own component files
  to minimize merge surface with the two sibling builds landing under the same page.
- 2026-09-06T19:54:42Z ✅ DONE — Shipped `artifacts/portal/src/lib/remediation-tracker-{types,api,catalogue,visuals}.ts`
  (wire types, react-query hooks against the 5 real §1a routes, a real 28-step
  catalogue mirrored from api-server's own `remediation-tracker-catalogue.ts`,
  display swatches) and
  `artifacts/portal/src/components/remediation-tracker/{StepRow,PhaseGroup,
  PricingPanel,StepActionDialog,RemediationProgrammeTab}.tsx` (the real steps grid
  grouped by phase/pillar, the live phases-and-fees pricing panel, and the
  toggle/other-action write, pointed re-verify, verification-guide, and
  decline-to-risk signed-acceptance actions). Wired a new, real, reachable page
  (`pages/remediation-tracking.tsx`, `/remediation-tracking` route in `App.tsx`,
  `moduleNav.ts`'s "Remediation" nav item flipped off `builtPath: null`). No
  fixture/hardcoded data anywhere — every row comes from `GET
  /api/portal/remediation-tracker`.
  Wrote `test-manifests/portal/remediation-tracking.json` (auth gates, real
  payload shapes, the `accepted_risk` signed-only 400 guard, unknown-stepId 400s,
  a no-mapped-check 404, and a reversible PUT round-trip so it stays re-runnable),
  registered in `_regression-suite.json`.
  **Verification, honest:** `tsc -p tsconfig.json --noEmit` — 0 errors. `vite build`
  — succeeds. `shaneapp://runTest` was actually invoked (twice, against this
  manifest and the pre-existing `risk-register.json`, by bare filename and full
  repo-relative path) — all four attempts returned
  `{ ok:false, error:'manifest not found or unparseable' }`. Root-caused directly
  (not assumed): BuildConsole's dev-server checkout (`C:\dev-server`, branch
  `dev-server`) is pinned at commit `145ba36ad`, hundreds of commits behind
  `origin/main`, with no `test-manifests/` tree yet at all — confirmed by the
  identical failure against `risk-register.json`, an already-merged manifest. This
  is the same structural staleness gap `test-manifests/portal/config-state.json`'s
  own notes already document, not a defect in this build; the manifest is written
  to re-run cleanly once that checkout catches up.
  Scope confirmed against sibling builds in the same `buildSet=RemediationTracking`:
  #3038 owns §1b-1e, #3039 owns §1f-1g — neither wired yet at commit time.
  Rebased once against a concurrent, unrelated build (#3027, Security Plan) that
  touched the same 3 shared files (`App.tsx`, `moduleNav.ts`,
  `_regression-suite.json`) — trivial line-level conflict in `App.tsx` only,
  resolved by keeping both routes.
  Working tree clean; `verify-branch-merged.mjs` confirms `agent/3037-q1802` (HEAD
  `6c1e33d08`) is a real ancestor of `origin/main`. No findings filed — no gap
  fell outside this issue's own explicit scope.
