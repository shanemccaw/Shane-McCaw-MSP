# #3611 — Filter dropdown reorganization (Archive/Crashed/Capped/Tests)

- **Status:** ✅ DONE 2026-09-11T04:18:36Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** f68550b54 (rebased/pushed to main as f9b81ccd4)

## Log
- 2026-09-11T01:49:36Z 🛑 BLOCKED — Dispatch instructions required re-confirming #3607 had genuinely landed (real `archived` column on `bt_build_queue`) before starting. Checked directly: `information_schema.columns` for `bt_build_queue` filtered on `archiv%` against the local Postgres `DATABASE_URL` returns zero rows — no `archived`/`archived_at` column exists. #3607 is still `OPEN`, unlabeled. Grep of `desktop/BuildConsole/Services/*.cs` and `lib/db/migrations/manual/` confirms the only real `Archived`/`ArchivedAt` usage today is the pre-existing `bt_chats` mechanism (`BuildQueuePostgresClient.cs` ~2575-2576) — nothing yet for `bt_build_queue`. #3611's own first deliverable depends on that column and the reconciler wiring #3607 describes, so there is nothing real to wire yet. The `blocked_by` edge from #3611 → #3607 already existed (set via the `--blocked-by 3607` dispatch flag) and remains correct; applied the `blocked` label (removed `in-flight`) and posted an explanation comment on #3611. Working tree left clean — no files touched other than this bookend.
- 2026-09-11T03:57:09Z ⏳ IN FLIGHT — Resumed (new session). Re-checked #3607: now `CLOSED`/`complete`. Verified for real (not just the label): `bt_build_queue.archived` (boolean) + `archived_at` (timestamptz) columns exist in local Postgres, and `BuildQueuePostgresClient.cs` has the real Rule A/B reconciler methods (~1600-1664) plus `QueueItem.Archived` wired through the row mapper. Removed the stale `blocked` label and the now-closed `blocked_by` edge, posted a resuming comment on #3611, and am proceeding with the real filter-dropdown work (Archive filter add; Crashed/Capped/Tests filter removal with pills in the normal view).
- 2026-09-11T04:18:36Z ✅ DONE — Implemented per the issue body: (1) added a real "Archive" ComboBoxItem + ApplyFilter case consuming `i.Archived`; (2) removed the "Crashed"/"Capped" ComboBoxItems and ApplyFilter cases — both now surface directly in the normal Queued/RunningAndQueued views via a new `IsCrashed(item)` helper (single source of truth, also reused by `UpdateOrphanRecoveryBanner`/`RecoverOrphanedBuildsAsync`) and the existing `AccountCapPolicy.CappedStatus` check; (3) added a distinct "💥 CRASHED" status pill on `BuildQueueCard` (the "CAPPED — ABOVE SONNET HIGH" pill already existed, no changes needed there); (4) investigated `RenderTestsTree()`/the "Tests" tab per the issue's own point 4 — confirmed it was never actually a queue-item filter (no `QueueItem` is ever "marked Tests"; it rendered an unrelated static index of `test-manifests/` files against `test-results/`, fully decoupled from `_lastItems`) — so there was no real "Tests-tagged item" to fold into the normal view, and removed it along with `TestStatusStyle` and all `_filter == "Tests"` special-case branches. Also updated `RevealQueueItem`'s targetFilter mapping (crashed/capped → Queued, archived canceled → Archive) and fixed stale comments in `BuildQueuePostgresClient.cs`/`QueueWatcherService.cs` referencing the removed Capped tab. Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 errors (6 pre-existing unrelated `FocusModeBar` warnings only). Did not launch BuildConsole.exe to verify (memory: never do this for verification) — relied on clean build + careful diff review of the filter/pill logic instead, per this task's own guidance that BuildConsole verification is "a clean build, a real/simulated run, or a manual check," not a test manifest (BuildConsole is explicitly out of scope for those). No genuine findings/bugs discovered outside the issue's own scope — the Tests-tab mismatch was exactly what the issue's own point 4 asked me to investigate and resolve, not a separate discovery. Committed as f68550b54, rebased onto origin/main and pushed as f9b81ccd4. `git status --porcelain` clean; `verify-branch-merged.mjs` confirms ancestry.
