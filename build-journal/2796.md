# #2796 — Worktree provisioning degrades over time (stale branches, loose objects, stray C:\wt dirs)

- **Status:** ✅ DONE 2026-09-06T17:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 7a7fd0163 (merged to main)

## Log
- 2026-09-06T15:30:00Z ⏳ IN FLIGHT — investigated real repo state (380 branches incl. 27 local
  `agent/*` + 279 `origin/agent/*`; 422 loose objects post an earlier manual pack; 34 dirs under
  `C:\wt\` vs 8 real registered git worktrees — confirms the issue's stray-dir class). Building
  `scripts/dev-server/git-maintenance.mjs` (merged-branch prune verified via
  `merge-base --is-ancestor`, periodic `git gc`, stray-dir sweep reusing the existing
  junction-safe/uncommitted-work-safe removal helpers) and wiring it into BuildConsole as a
  throttled periodic sweep alongside the existing worktree sweep.
- 2026-09-06T17:45:00Z ✅ DONE — shipped `scripts/dev-server/git-maintenance.mjs`
  (pruneMergedAgentBranches / pruneLooseObjects / sweepStrayWorktreeDirs, each independently
  gated) and wired it into `QueueWatcherService.MaybeRunGitMaintenance()` (throttled to once
  per 6h, called from `Start()` and `TickAsync()`) via
  `WorktreeCleanupService.RunGitMaintenanceAsync`. Ran it for real against the main repo this
  session: 73 stale `agent/*` branches deleted (each verified a real ancestor of `origin/main`
  before deletion, 0 failures), `git gc` correctly skipped (115 loose objects, below the 2000
  threshold — the repo is currently healthy), 26 stray `C:\wt\*` dirs reclaimed (34 -> 8 real
  registered worktrees at the time; verified after via `git worktree list` + a fresh directory
  count). A manual-backup dir (`_stray-2162-q1127-backup`) and dirs with recent activity were
  correctly retained, not touched. `dotnet build -c Debug` on `desktop/BuildConsole` succeeds,
  0 errors/warnings. Merged to `main` at `7a7fd0163`, confirmed via
  `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
