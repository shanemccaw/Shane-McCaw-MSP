# #3169 — Feature: Income Rules + transaction auto-matching (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T03:15:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** `8f1fd8faf` (core layer + routes + MCP tools + UI + check.mjs coverage),
  `701d790b9` (merged into `main`, verified ancestor)

## Log
- 2026-09-08T02:45:00Z ⏳ IN FLIGHT — Blocker #3137 confirmed CLOSED/complete, so this is
  unblocked. Real port from `shanemccaw/Finance-Tracker`'s `income.tsx` (IncomeRule,
  scanTransactions `:282-355`) plus the audit's own real gap ("no way to change which income
  source is primary despite it driving all cycle math"). Real local schema already has
  `income_sources`, `income_entries`, `transactions` (shared with ShanesSurvival) but no
  `income_rules` table, no `income_sources.is_primary`, and no `income_entries.transaction_id`
  for dedup — building all three plus the core scan logic, REST routes, MCP tools, and a
  read-only UI card (this app's own "Section 8: no forms, anywhere, ever" rule means rule
  CRUD is MCP/conversational, not a web form — same pattern already established for
  set_habit/log_smoke/vault). Found one real pre-existing data quirk worth noting: 9 real
  `income_entries` rows for the one real income source ("NASA Salary") already exist with no
  `transaction_id` (manually recorded via ShanesSurvival's own MCP tool before this feature
  existed) — scan will backfill-link those to their real matching transactions instead of
  creating duplicates.

- 2026-09-08T03:15:00Z ✅ DONE — Shipped the full real feature.

  **What shipped**
  - `migrations/044_income_rules.sql` — `income_rules` table (name, account_id, source_id,
    match_type contains/starts_with/exact, match_text, min_amount, max_amount, is_active),
    `income_sources.is_primary` + a partial unique index enforcing at most one real primary
    source, `income_entries.transaction_id` + a partial unique index for real dedupe. Run by me
    against the local `finances` database via `node bin/migrate.mjs` (renumbered 041->044 after
    a real concurrent-session ledger collision -- 041-043 had already been claimed on the shared
    local Postgres by other in-flight worktree sessions before my branch picked up their files
    via a `main` merge).
  - `src/core/income-rules.mjs` -- CRUD, name-based account/income-source resolution (reuses
    `money.resolveAccount`), pure `matchesRule()`, and `scanTransactions()`. Scan reads
    already-synced real transactions (no live Plaid call -- #3107's unified DB means they're
    already there), matches real credit deposits, and backfill-links a pre-existing untracked
    `income_entries` row before ever inserting a new one -- the real historical-gap handling
    noted above.
  - REST routes (`api.mjs`): `GET/POST/PATCH/DELETE /api/money/income-rules[/:id]`,
    `POST /api/money/income-rules/scan`, `POST /api/money/income-sources/:id/primary`.
  - MCP tools (`tools.mjs`): `list_income_rules`, `add_income_rule`, `update_income_rule`,
    `delete_income_rule`, `set_primary_income_source`, `scan_income_transactions` -- rule CRUD
    is conversational per contract Section 8 ("no forms, anywhere, ever"), matching this app's
    own established set_habit/set_food_preferences pattern (and validated further by a
    concurrent session's own #3182 commit removing add/edit forms from the Cars tab during this
    same build window).
  - Read-only UI card in the Money "Now" tab (`app.js`): income sources with a Set-primary
    button, rules with a Remove button, a Scan button -- no add/edit form.
  - `bin/check.mjs` real coverage, run live against my own server instance on port 5057/5058
    (not the already-running shared instance on 5000, which was serving other code): discovers
    or creates ONE real permanent rule (DirectDeposit's real "COM2 TREAS 310" Treasury deposits
    -> the real "NASA Salary" income source), runs a real scan, runs a second real scan and
    asserts zero new created/linked (the issue's own verification bar), pure `matchesRule()`
    boundary tests, the primary-flag round trip, and disposable rule CRUD that never matches a
    real transaction.

  **Real verification result -- 210/214 checks passed.** The 4 failures are pre-existing and
  unrelated: `SL_VAULT_KEY` is not set in this machine's local `web/shanes-life/.env` (copied
  from the main checkout, which also lacks it), so the Vault section's 4 checks fail exactly as
  they would have before this build. Re-ran a second time after two more `origin/main` merges
  landed mid-build (Things/Contacts and Cars form-removal commits) -- my own routes/UI/MCP
  tools confirmed intact via `node --check` + grep both times; the second live run's result
  (210/214, same 4 pre-existing failures) is what's recorded here.

  **Real, live scan proof (the issue's own verification line):** first scan against the real
  rule found 6 real matching "COM2 TREAS 310" deposits on the real DirectDeposit account
  (06-17 through 08-27, $2,999.33-$3,663.32), linked all 6 to their pre-existing real
  `income_entries` rows (0 created); a second scan immediately after created and linked zero
  new entries, `alreadyLogged: 6`. No real duplicate was created at any point, and
  `assertSurvivalUntouched`'s own row-count check passed (an UPDATE that links `transaction_id`
  doesn't change `income_entries`'s row count -- its own doc comment was updated to say so
  honestly rather than leave the old "never updates in place" claim stale).

  **Real, permanent artifact left behind, on purpose:** the rule "NASA Salary -- COM2 TREAS 310
  (set up by check.mjs)" now exists permanently in the real database, matching real future NASA
  Salary deposits automatically on the next scan. Not test residue -- a real, working, wanted
  first rule. Shane can edit/delete it by asking Claude (`update_income_rule`/
  `delete_income_rule`), same as any other income rule.

  **Findings filed:** #3201 (Catches has real API/MCP surface, no web UI, no check.mjs
  coverage -- found while looking for UI precedent; parented under #3153, board status
  "AI Batter Up").

  **Not done, deliberately:** no `is_primary`-driven rewrite of Budget Day's own math -- this
  app already treats every active income source equally by soonest `next_pay_date` (unlike
  Finance-Tracker's single-primary-drives-everything model), so `is_primary` ships as the real,
  editable label the audit's gap asks for, not a math rewire nothing here needs.
