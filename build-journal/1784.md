# #1784 — Batter Up / AI Batter Up panels find nothing (MaxPages=10 cap + walk timeout)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** ec8c6b3e9 (fix), preceded by IN FLIGHT bookend commit

## Log
- 2026-08-29 ⏳ IN FLIGHT — Fixing ProjectV2 pagination in GitHubApiClient (MaxPages headroom, loud cap log, resilient per-page timeout, investigate reverse pagination) for both GetBatterUpIssuesAsync and GetAiBatterUpIssuesAsync.
- 2026-08-29 ✅ DONE — Shipped in ec8c6b3e9 (pushed to origin/main, verified ancestor). Both `GetBatterUpIssuesAsync` and `GetAiBatterUpIssuesAsync` now share `ScanProjectItemsForStatusAsync`, which walks the ProjectV2 items connection **backward** (`last`/`before`) instead of forward.
  - **Reverse pagination adopted** — investigated and confirmed viable against the real project (`PVT_kwHOEiBDdc4BeoiY`): 1,781 items / 18 forward pages, and every Batter Up / AI Batter Up match sits in forward index 1606–1774 (the last ~175 items). Backward walk reaches all matches in ~2 pages.
  - **MaxPages 10 → 100** (10,000-item ceiling) with real headroom; loud `ActivityLog` WARNING if the cap is ever hit (no more silent truncation). `EmptyPageStopThreshold = 3` early-stops the backward walk only after the cluster is passed (300-item safety gap), preserving correctness while avoiding a full backlog re-scan every 90s poll.
  - **Timeout resilience** — `HttpClient.Timeout` 20s → 60s, plus per-page retry-with-backoff (`PostProjectItemsQueryWithRetryAsync`, 3 attempts, 500ms/1s) so one slow page no longer cancels the whole walk.
  - **Before/after (live):** old forward `MaxPages=10` found **0** for both panels (matches at fwd pages 17-18, unreachable). New reverse walk finds **7** Batter Up (#1617, #1640, #1641, #1780, #1782, #1783, #1785 — incl. all 5 named on the issue) in 5 pages walked, and **19** AI Batter Up in 4 pages walked. #1755 correctly excluded (CLOSED).
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` → clean (0 warnings, 0 errors).
