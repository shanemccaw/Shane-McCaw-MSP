# #3344 — Overview: wire the real no_tenant_scope state

- **Status:** ✅ DONE 2026-09-09T21:00:16Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 65d0fc858 (rebased/pushed to main as 65d0fc8580d011e127671b04ee6c308572c61a72)

## Log
- 2026-09-09T20:54:14Z ⏳ IN FLIGHT — starting investigation: find dashboard response signal for tenantScope null, and the Overview index.tsx + Design/portal/design_handoff_full_site/screens/Overview.dc.html state legend for no_tenant_scope.
- 2026-09-09T21:00:16Z ✅ DONE — Confirmed via `portal-customer-engines.ts` that no existing dashboard-response field distinguished "six counts a true 0 because nothing is due" from "six counts a true 0 because `resolveTenantScope(customerId)` came back null" — that's a real gap per the issue's step 1, so I added one (`tenantScopeResolved: boolean`) rather than guess a heuristic. Wired `index.tsx` to it: header state badge reads `SCOPE UNRESOLVED` (amber), Coming Up's empty state reads "No resolvable tenant identifier to schedule against.", and a Portal Counts note line reads "Tenant identifier unresolvable — six counts are a real 0. RACI roles are scoped to your account, not the tenant, so that one still reads." — all copied verbatim from `Overview.dc.html`'s own state legend/copy (`no_tenant_scope` entry, `noComingLine`, `countsNoteLine`). The NEEDS panel and forward-looking counts already excluded tenantScope-dependent rows automatically once the backend zeroed them, so no change was needed there. `pnpm run typecheck` clean in both `artifacts/api-server` and `artifacts/portal`. Added an apiTests step (`DASHBOARD-TENANTSCOPE`, sessionTag `3344-no-tenant-scope`) to the existing `test-manifests/portal/customer-home-overview.json` (discovered via grep, no duplicate manifest created) asserting the new field. Ran it via `shaneapp://runTest` against the live dev api-server: "no steps matched session tag" — expected and honest, same shape as the manifest's own prior `#3049` note: this branch hadn't gone through the dev-server merge/restart cycle at run time. `verify-branch-merged.mjs` confirms `agent/3344-q2067` (65d0fc85) is an ancestor of `origin/main`. `git status --porcelain` clean.
