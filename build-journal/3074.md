# #3074 — Split RefreshAsync: resume local Postgres queue re-read, keep GitHub work manual-only

- **Status:** ✅ DONE 2026-09-07T16:17:28Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** 4ed84912b (rebased/pushed to main as fdcebcc17)

## Log
- 2026-09-07T16:07:43Z ⏳ IN FLIGHT — Read #3074 body + comment. Audited `BuildQueuePanel.RefreshAsync()`
  (`Controls/BuildQueuePanel.xaml.cs` lines 819-885) in full. Confirmed the two genuinely GitHub-calling
  parts inside it: `AutoRecheckOpenIssuesOnTransitionAsync` (fire-and-forget `gh` call via
  `GitHubIssuesService.GetOpenIssueNumbersAsync`, gated on a Verifying/Done transition) and
  `TriggerBackgroundIssueTitleQueries` (fire-and-forget per-issue-number `gh issue view` title warm-up).
  Everything else in the method (`_db.GetQueueAsync()`/`_api.GetQueueCachedAsync()`, the priority/exclusive
  build-set completion checks, `ReportActiveBuildSets`, all the `Render*`/`UpdateQueueStatusCounts`/
  `UpdateOrphanRecoveryBanner` UI work) is local-only (Postgres read + in-memory state), zero GitHub calls.
  Plan: add an `includeGitHubWork` param to `RefreshAsync`, gate the two GitHub-calling blocks behind it,
  keep all existing `await RefreshAsync()` call sites defaulting to `true` (unchanged manual-refresh
  behavior), and add back a short (5s) `DispatcherTimer` in `Initialize()` that calls
  `RefreshAsync(includeGitHubWork: false)` automatically so the Queued/Running/Verifying/Done badges
  stay live without resurrecting #2900's automatic GitHub polling.
- 2026-09-07T16:17:28Z ✅ DONE — Implemented the split in `Controls/BuildQueuePanel.xaml.cs`
  (commit fdcebcc17 on main, local commit 4ed84912b): `RefreshAsync(bool includeGitHubWork = true)`
  gates `AutoRecheckOpenIssuesOnTransitionAsync` and `TriggerBackgroundIssueTitleQueries` behind the
  flag (both are the only genuinely `gh`-calling parts of the method — confirmed by reading the full
  body line-by-line). Added `_localQueuePollTimer` (5s `DispatcherTimer`) in `Initialize()` calling
  `RefreshAsync(includeGitHubWork: false)`. Confirmed the sole `QueueRefreshed` subscriber
  (`MainWindow.PushLiveMentionColorsAsync`) is itself local-only (reads
  `LiveMentionNumberRegistry.Snapshot()` + LeftSidebar's already-cached board state — no new `gh`
  call), so firing it every 5s doesn't reintroduce #2900's regression through that side door. Every
  existing manual `await RefreshAsync()` call site is unaffected (`includeGitHubWork` defaults `true`).
  Verified: `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0
  errors. Pushed to `origin/main` (rebased once on a non-fast-forward reject, retried clean).
  `verify-branch-merged.mjs` confirms ancestor-of-main. `git status --porcelain` clean. No findings
  filed — this was a scoped, confirmed-root-cause fix with no new gaps discovered.
