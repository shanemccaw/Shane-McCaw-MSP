# Build Journal — #2486

- **Status:** ✅ DONE 2026-09-03T02:23:10Z
- **Scope:** buildconsole
- **Issue:** #2486 — Build Chain Map: keep local `bt_build_queue` in sync with Map-driven GitHub board writes, sub-issue of Feature #2473
- **Started:** 2026-09-03
- **Blocked-by:** #2481 (CLOSED + `complete`) — unblocked at session start.
- **Commit(s):** `24c7e246e` (work) · `d8232d79f` (IN FLIGHT bookend) — both on `origin/main`, branch verified merged via `verify-branch-merged.mjs` (exit 0).
- **Findings filed:** none (no new proven-broken defect discovered; the pre-existing sub-issue-ORDER gap is already filed as #2498 under #2473 by the #2481 session).

## Log
- 2026-09-03T02:23:10Z — ✅ DONE — commit `24c7e246e` on `origin/main`, branch verified merged (`verify-branch-merged.mjs` exit 0), working tree clean. **What shipped:** the Build Chain Map's real GitHub board writes (#2481) now reconcile the local `bt_build_queue` dispatch cache, so the Map's Batter Up ↔ Backlog toggle and board moves actually affect live dispatch, not just the GitHub label. **Generalized the reconcile (reused, not reinvented):** `BuildQueuePostgresClient.ReconcileVerifyingAgainstBoardAsync` → `ReconcileQueueAgainstBoardAsync` — now covers the whole pre-dispatch surface (Verifying + still-`queued` rows) and matches by board Status **OptionId** against `GitHubApiClient`'s known constants (robust to display-name drift). Mapping: Verifying rows unchanged (Park→parked / Crashed→failed / Done→done); QUEUED rows add Backlog→canceled (**the core fix** — `GetNextAsync` never checks the board, so a Backlogged item kept dispatching), Park→parked, Crashed→failed, Done→canceled. Per-row **CAS guard** on the exact status read (a concurrent queued→running claim can never be clobbered); fail-closed per row (board-read error / off-board → leave). `BoardStatusSync.ReconcileVerifyingAgainstBoardAsync` renamed to `ReconcileQueueAgainstBoardAsync` and all **3 existing call sites** updated (Home tab, Build Watch, Git Board refresh) — they pass null scope → **Verifying-only, identical #2136 cost/behavior, zero regression**. **Perf-safe scoping (real finding mid-build):** live DB has **63 `queued` rows**, so a blanket per-queued-row board read on every manual refresh would be a 63-call latency/API regression AND could wrongly cancel hand-queued-Backlog rows — so the queued-row scan is scoped via a new `onlyQueuedGithubNumbers` param to exactly the issues a Map edit moved (a handful), unambiguous intent. **Wired into the Map's own save cycle:** `BuildChainMapWindow` gains a `_queueDb` (resolved once at load via `BuildQueuePostgresClient.TryCreate`; missing DATABASE_URL is non-fatal), and `PersistThenConfirm` → new `SyncQueueAfterBoardMovesAsync(result)` runs after every persist that moved a board column: (1) the generalized reconcile scoped to the moved numbers (Backlog cancel etc.), (2) the CREATE side — issues promoted to Batter Up flow into the queue through the ONE real #1870 pipeline (`BatterUpQueueService`), free-flow gated exactly like the Batter Up panel, scoped to promoted numbers (never a whole-board auto-queue); free-flow OFF leaves them for a manual Queue click. Fail-soft: a queue-sync error is logged, never a persist failure (the GitHub write already landed). **Note on "call BoardStatusSync.Mirror or the equivalent real write":** the Map's board moves already go through `ChainPersistence.PersistAndAuditAsync` → `SetIssueStatusByNumberAsync` with a re-read audit (#2481) — the audited equivalent real write — so no redundant fire-and-forget `Mirror` was added. **Verification (real, net-zero):** `dotnet build` clean twice (0 warn / 0 err), incl. after rebase onto concurrent commits. Live GitHub GraphQL confirmed every board OptionId constant against the project Status field (Backlog `63cc47c8`, Batter Up `09b1927f`, Done `0003ae3b`, Park `19cfa11c`, Crashed `6c8640a8`, Verifying `1ac1c7b2`, Ask Shane `404998bb`, AI Batter Up `a0296971`). Ran the exact reconcile SQL against local Postgres inside a transaction, ROLLED BACK (net-zero): a scratch `queued` row is (1) selected by the scoped candidate query, (2) NOT selected under empty/unscoped scope (proves existing sites leave queued rows alone), (3) canceled by the CAS UPDATE (1 row), (4) unaffected on a re-run guarded on `status='queued'` (proves the concurrent-claim guard). BuildConsole is out of scope for test manifests (CLAUDE.md); not launched (single-instance-mutex rule).
- 2026-09-03T02:12:17Z — ⏳ IN FLIGHT — Session started. Blocker #2481 confirmed CLOSED + `complete` → unblocked. Pulled #2486 body + the single BUILD dispatch comment (`Posted: 2026-09-02T23:34:20Z`). Read the real code: `Services/BoardStatusSync.cs` (Mirror + ReconcileVerifyingAgainstBoardAsync), `BuildQueuePostgresClient.ReconcileVerifyingAgainstBoardAsync` (Git #2136, scoped to `status='verifying'` rows only) + its 3 call sites (BuildWatchWindow, LeftSidebar/Git Board, MainWindow/Home tab), `BatterUpQueueService` (the real board→`bt_build_queue` create pipeline #1870, dedup via FindDedupCandidateAsync, free-flow gate `BatterUpFreeFlow`), `GetNextAsync`/`SelectClaimCandidatesAsync` (dispatch gate — checks blockers-closed + own-issue-open, but **never** the board Status column, confirming the gap), and the Map's persist choke point `BuildChainMapWindow.Inspector.cs` `PersistThenConfirm`/`ApplyPersistResult` + `ChainPersistence.PersistAndAuditAsync` (#2481's diff-based persister; `ChainPersistResult.StatusChanges` names exactly which issues had board moves — the precise reconcile scope). **Confirmed failure:** GetNextAsync will still claim+dispatch a `queued` row whose board was moved to Backlog by the Map; and a Map Batter Up move doesn't itself create the queue row. **Plan (per dispatch's architecture decision, reusing not reinventing):** (1) generalize the DB reconcile to cover `queued` rows too (Backlog→canceled, Park→parked, Crashed→failed, Done→canceled; verifying rows unchanged: Park→parked/Crashed→failed/Done→done; skip --notGit negative sentinels; per-row CAS guard on the exact status read so a concurrent claim can't be clobbered), matching board OptionId against the known constants; (2) rename BoardStatusSync wrapper + DB method to `ReconcileQueueAgainstBoardAsync` and update the 3 existing call sites; (3) wire the generalized reconcile into the Map's own save/refresh cycle (`ApplyPersistResult`), and for the create side reuse `BatterUpQueueService` (free-flow gate honored) so a Map Batter Up move flows into `bt_build_queue` through the one real pipeline — no second reconcile path. Real GitHub reads to audit each `bt_build_queue` row landed as intended, same discipline as #2481/#2480.
