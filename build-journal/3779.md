# #3779 — Policy Decisions + Policy Engine: contract pack refresh

- **Status:** ✅ DONE 2026-09-12T06:25:19Z
- **Scope:** platform

## Log
- 2026-09-12T06:19:32Z — Started. Dispatch claimed. Read #3779 body + comments. Located current
  pack (`docs/msp-console/policy-decisions-msp-console-contract-pack.md`, regenerated #3351,
  2026-09-09 — already documents #3034/#1525). Confirmed #3035 already CLOSED and already
  correctly documented in the pack's own §2.4/§6/findings. Found `msp-active-directory.ts`
  (#2148/#2524, MSP-side OU-assignment routes) is real, live, mounted, and genuinely part of the
  Policy Engine's OU-resolution chain but **not documented anywhere in the current pack** —
  auditing it now for inclusion.
- 2026-09-12T06:25:19Z — ✅ DONE. Re-verified #3351's regeneration line-by-line: #3034 and #3035
  already correctly folded in, no drift. Real gap found and closed: added new §3.9 documenting
  `msp-active-directory.ts` (Git #2148/#2524 — 6 real, live, tested routes reading/writing
  `active_directory_ou_assignments`/`active_directory_ou_assignment_requests`, which
  `policy-compliance-graph.ts` checks first when resolving OU membership for a standing policy),
  plus updated the consumer map, cross-surface edges, row counts, findings section and appendix.
  Confirmed live via local Postgres (`\d active_directory_ou_assignments`,
  `\d active_directory_ou_assignment_requests`, both 0 rows — honest-empty, matching every other
  table in this pack). Also restated in the pack that #3035's product question is NOT open — it
  was a stale code-comment discrepancy already closed and correctly recorded; there is no live
  "should MSP staff author a policy_decisions row on a customer's behalf" question outstanding.
  No new finding filed — the gap was documentation-only (real, live, already-tested code simply
  missing from the pack), not a product/backend gap. Read-only, no product/schema/UI changes.
  Commits: `0a88ae758` (IN FLIGHT bookend), `d18846bfc` (pack refresh). Branch verified merged
  into origin/main via `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
