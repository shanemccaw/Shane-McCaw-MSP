# #2365 — Batter Up rail panel: "Expand full page here" / "Send to tab"

- **Status:** ✅ DONE 2026-09-03T00:29:59Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-03
- **Commit(s):** 6d5cb27b1 (implementation), 1951c3085 (bookend), on main

## Log
- 2026-09-03T00:20:00Z ⏳ IN FLIGHT — item 10 of 11, Feature #2355 (Batter Up rail panel).
  Depends only on #2356 (closed/complete). Real audit first: grepped
  `MainWindow.xaml.cs`/`MainWindow.GitPanel.cs` for the existing expand/send-to-tab
  mechanism; found Git #2312's real implementation (widen `LeftPanel` in place for
  "Expand full page here", open a `GitItemDock` document tab for "Send to tab", one
  shared identity-rendering path for both). No other rail panel (Shot Vault, Chats)
  has this chrome yet — Git Panel is the one real mechanism, reused rather than
  reinvented.
- 2026-09-03T00:29:59Z ✅ DONE — wired the same widen-in-place / send-to-tab split onto
  the Batter Up panel:
  - `MainWindow.xaml`: two header glyph buttons (`BtnBatterUpExpand` ⛶, `BtnBatterUpSendToTab`
    ↗) next to the shared `LeftPanelTitle`, visible only while Batter Up is the open rail
    source; a new `BatterUpItemDock` document surface (mirrors `GitItemDock`) showing the
    frozen lane/count snapshot.
  - `MainWindow.xaml.cs`: `OpenLeftPanel` toggles the two buttons' visibility per source
    and collapses the expanded width back on leaving the panel; `TabDef` gained a
    `BatterUpSnapshot` (new `BatterUpDocSnapshot` record) mirroring `GitItemTrail`;
    `SelectTab` shows/hides `BatterUpItemDock` and calls `RenderBatterUpDoc`.
  - `MainWindow.BatterUpPanel.cs`: `SetBatterUpExpanded` (reuses Git #2312's
    `GitPeekExpandedWidth`/`LeftPanelWidth`), `BtnBatterUpSendToTab_Click` (builds a real
    snapshot off the live `_batterUpCounts` + selected lane, opens/refreshes/focuses the
    `batterup-doc` tab), `RenderBatterUpDoc` (renders the same lane-chip/status content
    into the doc host, frozen and inert — not a live drill surface, same pattern
    `RenderGitIdentityBlock`'s non-clickable doc breadcrumb uses).
  - No fixture/invented data — both surfaces read the real
    `ChatGitHubFilter.GetBatterUpLaneCountsAsync()` counts already wired by #2356.
  - **Verification:** `dotnet build desktop/ShaneBuilder/ShaneBuilder.csproj -c Debug` —
    0 warnings, 0 errors. No new test-manifest need (BuildConsole/ShaneBuilder WPF is
    explicitly out of scope for test manifests per CLAUDE.md).
  - No findings filed — audit turned up the real reusable mechanism cleanly, no gaps
    worth a sibling issue.
