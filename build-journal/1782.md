# #1782 — BuildConsole Services menu is a stale hardcoded list

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 7335bcd27 (work), 563851cf6 / bd7d5ce46 (merges into main)

## Log
- 2026-08-29 ⏳ IN FLIGHT — extracting scripts/dev-all.mjs's SERVICES array into a
  shared scripts/dev-server/services.json config; DevServicesManager.KnownServices to
  read the same file at startup; BuildConsole's title-bar Services menu to build itself
  from KnownServices instead of a fixed XAML list. Investigating mcp-server's shape
  (stdio MCP server vs port-based Vite service) before deciding whether it belongs in
  the shared config.
- 2026-08-29 ✅ DONE — Created `scripts/dev-server/services.json` as the single source
  of truth for the 5 port-based dev services (api-server, shane-mccaw-consulting,
  admin-panel, portal, msp-website). `scripts/dev-all.mjs` now reads its `services`
  array from that file instead of an inline array.
  `desktop/BuildConsole/Services/DevServicesManager.KnownServices` now loads from the
  same JSON at startup (icon glyphs stay in a small C#-side lookup with a fallback
  glyph, since they're cosmetic/BuildConsole-only). BuildConsole's title-bar Services
  menu is now built dynamically in `MainWindow.BuildServicesMenu()` from whatever
  `KnownServices` contains — no fixed per-service `MenuItem` list remains in XAML or
  code-behind. Existing Marketing/Portal/Admin/API Server behavior (dedicated
  bottom-panel tab + `ServiceLogView`, start/stop/log mechanics) is byte-for-byte
  unchanged; a service without a dedicated tab (e.g. Website today, or any newly added
  artifact) still gets a full dynamic Start/Stop/Open-in-Edge menu.
  **mcp-server investigated (issue item 4):** `artifacts/mcp-server/package.json` has
  no `dev` script, and `artifacts/mcp-server/src/index.ts` connects via
  `StdioServerTransport` — a stdio MCP server with no HTTP port/URL concept at all. It
  does not fit the port-based Vite/api-server shape this config assumes, so it is
  listed under an `excluded` key in `services.json` with the reason documented, rather
  than forced in. **mockup-sandbox** (also named in the issue) does not exist under
  `artifacts/` on disk today — nothing to add for it.
  **Verified:** `node scripts/dev-all.mjs --status` lists the same 5 services with the
  same names/ports as before. Simulated adding a 6th artifact
  (`mockup-sandbox`, port 5177) to `services.json` only — `dev-all.mjs --status` picked
  it up immediately, and a standalone C# program running the exact same
  `System.Text.Json` parsing logic as `DevServicesManager.LoadKnownServices()` also
  picked it up, with zero C# code change. Reverted the simulated addition before
  committing. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` is
  clean (0 warnings, 0 errors), both before and after merging concurrent `main`
  changes. Branch merged and pushed to `origin/main`
  (`git merge-base --is-ancestor` confirmed). `git status --porcelain` clean.
