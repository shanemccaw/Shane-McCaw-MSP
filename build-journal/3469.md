# Issue #3469 — Batter Up / AI Batter Up auto-refresh live enrichment regardless of panel visibility

- **Status:** ⏳ IN FLIGHT 2026-09-10T03:24:07Z
- **Scope:** buildconsole

## Log
- 2026-09-10T03:24:07Z — Session started. Reading issue #3469 body + comments, then the real call sites named: `Controls/AiBatterUpPanel.xaml.cs`, `Controls/BatterUpPanel.xaml.cs`, `AiBatterUpQueueService.cs`, `BatterUpQueueService.cs`. Plan: gate the sync-driven `RefreshAsync` (the batched BUILD-comment resolution, closed-sweep, and auto-select detail load) behind an actual panel-visibility check, so a closed tab does zero live GitHub work per mirror sync — only the mirror-backed board list/count still updates. Also correct the stale "no new GitHub calls" comment at `AiBatterUpPanel.xaml.cs:94-97`.
