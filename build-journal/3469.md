# Issue #3469 — Batter Up / AI Batter Up auto-refresh live enrichment regardless of panel visibility

- **Status:** ✅ DONE 2026-09-10T03:44:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T03:24:07Z — Session started. Reading issue #3469 body + comments, then the real call sites named: `Controls/AiBatterUpPanel.xaml.cs`, `Controls/BatterUpPanel.xaml.cs`, `AiBatterUpQueueService.cs`, `BatterUpQueueService.cs`. Plan: gate the sync-driven `RefreshAsync` (the batched BUILD-comment resolution, closed-sweep, and auto-select detail load) behind an actual panel-visibility check, so a closed tab does zero live GitHub work per mirror sync — only the mirror-backed board list/count still updates. Also correct the stale "no new GitHub calls" comment at `AiBatterUpPanel.xaml.cs:94-97`.
- 2026-09-10T03:44:00Z — ✅ DONE — commit `bc6a5523e` (rebased/pushed as `84ace2f5a` on `origin/main`). Added `IsVisible`-gated branch in both panels' `OnMirrorSyncCompleted`: when the document tab is open, the existing full `RefreshAsync()` still runs; when closed, a new zero-live-call `RefreshCountOnlyAsync()` reads only the local mirror board count via new `AiBatterUpQueueService.GetMirrorOnlyOpenCountAsync()` / `BatterUpQueueService.GetMirrorOnlyOpenCountAsync()` (no closed-sweep, no `ResolveBuildCommentsAsync`, no auto-select detail load, no free-flow auto-queue). Added `IsVisibleChanged` handlers so a tab opening after being closed gets a real `RefreshAsync()` immediately instead of waiting on the next mirror sync. Corrected the stale `AiBatterUpPanel.xaml.cs:94-97` comment claiming "no new GitHub calls" — it now documents the real gap this issue closes. `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `verify-branch-merged.mjs` — OK, merged into `origin/main`. `git status --porcelain` clean. No new findings filed — the gap this issue itself describes is what was fixed; no further undocumented gap surfaced during the work.
