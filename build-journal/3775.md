# #3775 — Tenant Contracts screen: real read-view over SOW + subscription + scope-creep data

- **Status:** ✅ DONE 2026-09-15T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** a2843ef4e

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — starting: investigating existing SOW/services/scope-creep backends and msp-console nav wiring for tenant screen 21 "Contracts". Authorized by Shane (2026-09-15) to build the UI directly since the design skeleton is a placeholder.
- 2026-09-15T00:45:00Z ✅ DONE — shipped `/tenants/:id/contracts` (Contracts.tsx), a real aggregation read view over: SOWs (`useSowsForCustomer`, reused from `offers-and-sows-api.ts`), active `client_services` rows (new `GET /api/msp/:mspId/customers/:customerId/services` in `msp-customer-services.ts`, general/unfiltered version of what `msp-retainer-billing.ts` narrows to recurring-monthly), and the scope-creep ledger (open detections reused from `scope-sla-api.ts`, violations-for-customer added in `contracts-api.ts` — the route already supported `?customerId=` server-side). No new schema. Visible "Agent-built UI — pending design review" banner per Shane's authorization (design skeleton for screen 21 is a placeholder). No dev-facing chrome (routes/status codes) rendered to the user.
  Verification: `pnpm run typecheck` clean and `pnpm run build` clean for both `artifacts/msp-console` and `artifacts/api-server`, before and after rebasing onto concurrent `main` commits. Direct local-Postgres row-count spot check (`client_services`/`msp_sows`/`scope_creep_violations`) was attempted but blocked by this session's sandbox ("Credential Materialization" denial on reading `.env.local`'s `DATABASE_URL`) — not run live. `shaneapp://runTest`/`executeSql` protocol tools were not present in this session's tool list to fall back on. Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
  No genuine findings/bugs discovered mid-build to file.
