# #3113 — URGENT: local Postgres mirror of GitHub issue state (batched GraphQL sync), routine reads stop hitting GitHub

- **Status:** ⏳ IN FLIGHT 2026-09-07T18:37:34Z
- **Scope:** buildconsole
- **Started:** 2026-09-07
- **Commit(s):** <fill at DONE>

Sub-issue of #1789 (Feature: Dev Server & Deploy Infrastructure, BuildConsole).

Implements the **revised** scope (Shane's 2026-09-07T18:34:21Z architectural redirect,
which supersedes the original batching-only plan): maintain a real, local, persistent
mirror of GitHub issue state in the existing Postgres DB (`bt_issue_mirror`), have
routine read call sites read from that LOCAL mirror instead of calling GitHub per issue,
and reserve real live GitHub calls for (a) the periodic batched sync/diff (via GraphQL
`nodes(ids:)`, chunked ≤100) and (b) genuinely authoritative real-time checks.

## Log
- 2026-09-07T18:37:34Z ⏳ IN FLIGHT — Opening bookend. Planned work: (1) audit each of the 4
  real call sites (`BuildQueuePostgresClient`, `ChatDockService`, `GitHubIssuesService`,
  `StartupGitHubCoordinator`) to classify each GitHub read as safe-to-mirror vs
  must-stay-live-authoritative; (2) create `bt_issue_mirror` table + migration, run it
  against local DATABASE_URL; (3) build a batched GraphQL `nodes(ids:)` sync/diff service
  and a mirror read/write service; (4) migrate the safe reads to the mirror, keep the
  authoritative checks live; (5) build BuildConsole to verify compile.
