# #3923 — UiTestExecutor: loginAs manifest field runs a real login sequence against Settings' login profiles

- **Status:** ✅ DONE 2026-09-13T05:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 2272f555c2c4aaec9ba686839c26abe31f0ac149

## Log
- 2026-09-13T05:20:00Z ⏳ IN FLIGHT — starting: dispatch claimed, #3922 confirmed landed (commit eef2fafa8/2bb8a7adc, no blocked_by edge outstanding). Investigating UiTestExecutor.cs, test-manifests schema, and #3922's UserAccountEntry/BuildConsoleSettings login-profile shape before adding the `loginAs` primitive.
- 2026-09-13T05:55:00Z ✅ DONE — commit `2272f555c2c4aaec9ba686839c26abe31f0ac149` (merged into `origin/main`, verified via `verify-branch-merged.mjs`). Added optional top-level `loginAs` manifest field (TestManifest.cs), resolved by RunManifestAsync against Settings' real UserAccounts login-profile list (#3922), threaded through TestRunnerWindow.RunUiTestAsync into UiTestExecutor.RunAsync. A real, non-anonymous profile is logged in (native-DOM value-setter JS injection, same #871 technique) before the manifest's own steps run, polling for the real app-agnostic authenticated signal (redirect away from /login, audited per target app: Portal/Admin/MSP Console). A bad login aborts the whole run at step 0 with a clear reason. loginAs omitted/unresolved/anonymous is unchanged behavior. `dotnet build`: 0 errors, 0 warnings. NOT self-verified via a live shaneapp://runTest WebView2 run — the already-running BuildConsole instance runs pre-#3923 compiled code, and exercising it would test the old executor, not this change; per Git #2156 this session did not launch/drive a fresh BuildConsole.exe to work around that. Left in place for Shane's own click-through once he restarts BuildConsole with this merged build: two real login profiles in his live settings.json (`Git #3923 loginAs verify (Customer, Portal)` — real creds; `Git #3923 loginAs verify (deliberately broken password)` — wrong password), backed by a real synthetic Customer user in local Postgres (`shanemccaw+loginas-verify-3923@outlook.com`, tenant_id=1, msp_role=Customer, users.id=2826, additive INSERT only, no schema change). A manifest with `loginAs` set to either label plus one uiStep asserting `[data-testid='overview-notifications-chip']` visible on `http://localhost:5175/portal/` (zero login uiSteps) proves the real profile lands authenticated and the broken one aborts cleanly at step 0. No findings filed — straightforward extend-the-existing-manifest/executor build. `git status --porcelain` clean.
