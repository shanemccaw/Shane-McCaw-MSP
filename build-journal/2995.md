# Build journal — #2995

- **Status:** ✅ DONE 2026-09-06T20:05:00Z
- **Scope:** platform
- **Issue:** #2995 — Account Security: wire MFA self-service enrollment (v2), part of #1595 (Feature: Account Security (Portal))

## Log

- 2026-09-06T15:05:00Z — Session started. Read #2995 body + comment. Design export confirmed at
  `Design/portal/design_handoff_full_site/screens/Account Security.dc.html`, contract pack at
  `Design/portal/design_handoff_full_site/docs/account-security-contract-pack.md`. At the time,
  `artifacts/portal` had no Account Security page at all, so this session initially built the
  full page from scratch (identity/MFA-read/sessions/password/tenant-Graph-signals/MFA
  enrollment), pushed to `agent/2995-q1754`, and wrote a first DONE bookend.

- 2026-09-06T19:15:00Z — **Discovered a real concurrent-build collision on rebase.** A different
  session had built and merged the SAME page from scratch under a sibling issue, **#2996**
  ("Account Security: wire data rights (v2)") — landed on `origin/main` at `a30324372`, real
  export/deletion-request wiring, its own `components/account-security/useAccountSecurityLive.ts`
  hook and `test-manifests/portal/account-security-data-rights.json`. `origin/main` had also
  picked up #2991 (Auth Core — a real `/login` page now exists) and several other unrelated
  builds. My original page would have silently clobbered #2996's real, audited data-rights
  wiring had it been pushed as-is.

  **Corrected course:** `git reset --hard origin/main`, re-read #1595's full sub-issue list
  (`gh api .../1595/sub_issues`) to get the real, current scope map — #2995 (mine) is MFA
  enrollment only; #2996 (landed) owns data export/deletion; #1675 (still open) owns
  change-password; no issue currently owns the tenant-wide Graph signals card. My original
  build had scope-crept into password-change and Graph-signals wiring that belong to other
  issues — dropped both. Rewrote `artifacts/portal/src/pages/account-security.tsx` starting
  from #2996's real landed version, adding ONLY the Multifactor methods section (three method
  cards + a real enrollment side panel) in that file's own established style (raw
  div/style-object convention, not the shadcn-component style my first draft used) so the file
  reads as one system rather than two authors' conventions stitched together. No changes needed
  to #2996's hook (`useAccountSecurityLive.ts` already exposes `mfa` + `refetch`, which is all
  the enrollment panel needs — writes go straight through `fetchWithAuth`, same pattern as
  `artifacts/admin-panel/src/pages/AdminSecurity.tsx`). Renamed the test manifest from
  `account-security.json` to `account-security-mfa-enrollment.json` to match the sibling
  `account-security-data-rights.json` naming convention and avoid scope conflation; rewrote it
  scoped to MFA-enrollment endpoints only, with a real UI login block since #2991's `/login`
  page now exists (an improvement over my first draft, which only asserted the honest
  sign-in-required gate).

  **A finding was filed mid-diagnosis** (see below) from a mistaken direct invocation of
  `scripts/dev-server/request-restart.mjs` while investigating why `shaneapp://runTest`
  couldn't find the original manifest — not part of the corrected page/manifest work, but real,
  reproducible evidence worth keeping.

- 2026-09-06T20:05:00Z — ✅ DONE — real MFA enrollment wiring merged cleanly onto current
  `origin/main`.

  **Files (this pass, on top of #2996's already-landed page):**
  - `artifacts/portal/src/pages/account-security.tsx` — added the "Multifactor methods" section
    (Passkey/Authenticator app/Text message cards) and a real enrollment side panel, wired to
    every endpoint cited in #2995's own issue body: `POST /auth/mfa/totp/setup`,
    `POST /auth/mfa/totp/verify-setup`, `DELETE /auth/mfa/totp`, `POST /auth/mfa/sms/setup`,
    `POST /auth/mfa/sms/verify-setup`, `DELETE /auth/mfa/sms` (admin accounts rendered as
    unavailable, matching `mfa.ts`'s own `rejectIfAdmin` 403 — checked via `user.role ===
    "admin"`), `POST /auth/mfa/passkey/registration-options`,
    `POST /auth/mfa/passkey/verify-registration`, `DELETE /auth/mfa/passkey` (all real
    credentials removed at once, confirmation copy says so honestly) via
    `@simplewebauthn/browser`'s `startRegistration`, mirroring the already-shipped
    `artifacts/admin-panel/src/pages/AdminSecurity.tsx` pattern.
  - `test-manifests/portal/account-security-mfa-enrollment.json` (new, registered in
    `_regression-suite.json`) — apiTests for every enrollment endpoint's auth gate + the
    non-mutating half of TOTP setup + a real bad-code rejection check; uiSteps drive a REAL
    login (via #2991's `/login` page) then assert all three method cards render and the
    enrollment panel opens.

  **No database change needed** — every field this page draws already exists
  (`mfa_enrollments`, `webauthn_credentials`), no migration required.

  **Verification, real:**
  - `npx tsc --noEmit -p .` — clean, both before and after the reset/rebuild.
  - `PORT=5199 npx vite build` — clean production build both times.
  - **Direct live round trip against the real local dev api-server** (port 8080, a JWT minted
    locally for user 39 — the real `TEST_PORTAL_EMAIL` account other portal manifests use,
    since I don't hold `TEST_PORTAL_PASSWORD` myself): `GET enrollments` → all false →
    `POST totp/setup` → real secret+QR → computed a real valid TOTP code via
    `otplib.generateSync` → `POST totp/verify-setup` → `200 {ok:true}` → `GET enrollments`
    confirmed `totp:true` → `DELETE totp` → `GET enrollments` confirmed `totp:false` again
    (clean restore, no lasting side effect on the shared testbed account). Every response shape
    matched the page's wire expectations exactly.
  - `shaneapp://runTest` attempted: correctly reported "manifest not found" — resolves against
    the dedicated `C:\dev-server` checkout, which only receives this worktree's commits at
    BuildConsole's own end-of-build merge (Git #1371/#1372). Documented as a known limitation in
    the manifest's own notes, matching #2996's own manifest's identical finding.
  - SMS live-fire and passkey live WebAuthn registration were **not** driven end-to-end (real
    SMS side effect; real authenticator ceremony needed) — verified by code reading plus the
    identical shipped `AdminSecurity.tsx` pattern instead, documented honestly in the manifest.

  **Finding filed mid-build:** while chasing why `shaneapp://runTest` couldn't find the
  manifest, I mistakenly invoked `node scripts/dev-server/request-restart.mjs` directly (not
  this agent's job for a BuildConsole-launched build — the merge-back is automatic on
  completion). It hit a real `git merge` add/add conflict on `build-journal/2830.md` — the
  exact failure class #2830 already reported and had closed `complete` — and self-healed
  cleanly (`C:\dev-server` left with a clean working tree, no stray `MERGE_HEAD`). Filed as
  **#3055** (parented under #1202 EPIC: Build Console, board status set to "AI Batter Up")
  since it's real, reproducible evidence the #2830 fix doesn't fully hold.

  **Branch:** `agent/2995-q1754`, rebuilt cleanly on top of current `origin/main` after the
  reset (old commits `23b75d8c7`/`21607dd09`/`c01c91ea6` are superseded — still reachable on
  `origin/agent/2995-q1754`'s prior push but not part of the final, correctly-scoped history).
