# #3213 — Money: Cars v3 refinements (real capture phrasing, undo toast, all-in cost line)

- **Status:** ✅ DONE 2026-09-08T05:05:59Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 5721c25fc (rebased/pushed as 7849604ec on main)

## Log
- 2026-09-08T05:01:05Z ⏳ IN FLIGHT — read design 1f ("Cars · no forms") and #3149/#3182's existing Cars code; #3182's own no-forms fix already shipped (confirmed closed), building the four real refinements on top: onboarding capture-phrase examples, real "just now" sticker + 5s Undo toast on a fresh maintenance capture, confirming the existing all-in $/mo·$/yr line, and the "$X/mo, was $Y before today's $Z" maintenance-average delta line.
- 2026-09-08T05:05:59Z ✅ DONE — shipped all four real refinements from design 1f:
  1. Onboarding "Say it, it lands" card on the Cars tab with the three verbatim example
     phrases, dismissible ("Got it"), persisted via `localStorage.carsOnboardingDismissed`.
  2. Real "just now" sticker + a genuine 5-second Undo toast, driven by comparing each
     vehicle's newest `vehicle_maintenance_log` row id against a per-vehicle
     `localStorage` "last seen" id (captures classify asynchronously in a separate Claude
     conversation -- see captures.mjs's own header -- so there is no synchronous
     request/response moment to hang this off; first-ever view of a vehicle is recorded
     silently so pre-existing history never reads as "just now"). Undo calls a new real
     endpoint, `DELETE /api/cars/:id/maintenance/:entryId` ->
     `vehicles.deleteMaintenanceEntry`, which deletes exactly that one row, scoped to the
     vehicle and user.
  3. Confirmed the all-in `$X/mo · $Y a year all-in` line already exists and is correct
     (#3149's own work) -- no change needed there.
  4. New real "$X/mo, was $Y before today's $Z" delta line: `vehicles.mjs`'s `buildCard`
     now also returns `maintenance.averagePerMonth`, `maintenance.previousAveragePerMonth`
     (the trailing-12-month average recomputed with the single newest entry pulled back
     out) and `maintenance.lastEntry`.
  Verification: `node --check` on all three touched JS/mjs files passed. Live-verified the
  real math and the real undo end-to-end against the local `finances` Postgres DB
  (web/shanes-life's own DB, not shanemccawmsp) using the `you@example.com` test user:
  created a real vehicle, logged $34 then $84 maintenance (avg $2.83 -> $9.83/mo,
  previousAveragePerMonth $0 -> $2.83, matching the design's delta shape), called
  `deleteMaintenanceEntry` to undo the $84 entry and confirmed the average and lastEntry
  reverted to the pre-undo state exactly, then deleted the test vehicle -- no residue left
  in the DB. Did not spin up the HTTP server itself to hit the DELETE route over HTTP;
  the route is a one-line delegate to the directly-verified `vehicles.deleteMaintenanceEntry`,
  matching the existing two-param route pattern used elsewhere in api.mjs (e.g.
  `/api/entities/:id/items/:itemId`). No test harness exists for this app (no `test`
  script, no test files) to run in addition. Branch verified merged into origin/main
  (`verify-branch-merged.mjs` exit 0) and working tree clean before writing this.
