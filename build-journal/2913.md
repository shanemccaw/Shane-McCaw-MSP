# #2913 — Feature: Capture Plaid's real transaction name/description (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T21:20:00Z
- **Scope:** buildconsole <!-- personal project, desktop/ShanesSurvival -->
- **Started:** 2026-09-04
- **Commit(s):** afcff13fc

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — capturing Plaid's raw `name` field end-to-end: JSON models →
  migration → sync upsert → repository → `recent_transactions` MCP tool fallback, plus scoping the
  real backfill limitation (cursor-based `/transactions/sync` only captures `name` going forward).
- 2026-09-04T21:20:00Z ✅ DONE — `afcff13fc`. Added `PlaidTransactionJson.Name`/
  `PlaidTransactionInfo.Name` (Plaid's raw `name` field), migration
  `009_transaction_name.sql` (`transactions.name TEXT`, ran myself against the real local
  `finances` Postgres DB — confirmed via `\d transactions`), `PlaidSyncService` upsert now
  stores `name` on insert + `ON CONFLICT` update, `TransactionRepository`/`TransactionRow`
  now select/expose it, and `recent_transactions` (FinanceTools.cs) now falls back to `Name`
  before "Unknown merchant". `dotnet build ShanesSurvival.sln` clean (0 warnings, 0 errors).
  Real backfill limitation (already-synced rows stay `name = NULL` until re-synced, since
  `/transactions/sync` is cursor-based/incremental — a real backfill needs the date-ranged
  `/transactions/get` endpoint) filed honestly as follow-up **#2914**, parented as a sub-issue
  of #2913 per the Feature-first rule (this issue is itself Feature-tier), board status set to
  AI Batter Up. Branch verified merged into `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean.
