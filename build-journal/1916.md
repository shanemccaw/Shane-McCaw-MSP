# #1916 — Fix transport-folding bug mislabeling Azure DevOps resources as azure-rm

- **Status:** ✅ DONE 2026-09-04T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 68d33fb87

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — `readTransportFor()` in
  `scripts/config-state/build-resource-model.mjs:52` folds `Azure DevOps` into
  the same `azure-rm` bucket as `azure` / `Azure Service Management` via a
  single `||` condition, mislabeling 4 real ADO* Microsoft365DSC resources
  (`m365dsc:ADOOrganizationOwner`, `ADOPermissionGroup`,
  `ADOPermissionGroupSettings`, `ADOSecurityPolicy`). Taking the issue's
  reversible fix: add `azure-devops` to `CONFIG_READ_TRANSPORTS`, stop the
  fold, re-label the 4 rows.
- 2026-09-04T00:20:00Z ✅ DONE — added `azure-devops` to
  `CONFIG_READ_TRANSPORTS` (`lib/db/src/schema/config-state.ts`); fixed
  `readTransportFor()` to check `Azure DevOps` before the `azure` /
  `Azure Service Management` branch and route it to `azure-devops` instead of
  folding into `azure-rm`. Rather than re-running the full
  fetch-sources.mjs → build-resource-model.mjs pipeline (a real network cost —
  clones the Microsoft365DSC repo and downloads Graph `$metadata` — not
  justified when the effect on exactly these 4 known rows is fully
  deterministic), wrote and ran
  `lib/db/migrations/manual/2026-09-04-config-resources-azure-devops-transport-1916.sql`
  against local Postgres (additive re-label, `UPDATE ... WHERE read_transport
  = 'azure-rm'`, self-marked in `simulator_migration_runs`), then re-ran
  `build-snapshot-registry.mjs` (pure local-DB derivation, no network) to
  propagate the change into `config_snapshot_resource_types`. Verified live:
  `SELECT resource_key, workload, read_transport FROM config_resources WHERE
  read_transport = 'azure-devops' AND resource_key LIKE 'm365dsc:ADO%'` returns
  all 4 rows; the registry table shows the same 4 rows with
  `read_transport = 'azure-devops'`, `is_collectable = false`,
  `not_collectable_reason = 'no_executor'` (no Azure DevOps executor exists
  yet — separate future scope per the issue). `tsc --noEmit` on `lib/db`
  clean. Branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` (68d33fb87). Working tree clean.
