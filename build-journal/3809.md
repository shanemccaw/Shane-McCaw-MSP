# #3809 — Git Doctor Delete button danger color

- **Status:** ✅ DONE 2026-09-12T19:41:00Z
- **Scope:** buildconsole

## Log
- 2026-09-12T19:19:28Z — Session started. Retargeting Git Doctor's "Delete N selected" button off the misleadingly-named `Brush.Epic.AppCore` to a real danger tone, matching #3808's `MaroonBrush`-equivalent (`#EBA0AC`).
- 2026-09-12T19:41:00Z — ✅ DONE — Registered a properly-named local resource `Brush.Danger.Soft` (`#EBA0AC`, same hex #3808 used for `MaroonBrush`) in `Controls/GitDoctorView.xaml.cs`, and retargeted the "Delete {N} selected — backup tag first" button's `Background` from `Brush.Epic.AppCore` to it, keeping `Foreground=Brushes.Black` (same legible combo already confirmed on #3808's nightmare button). Left the item-3 rename of `Brush.Epic.AppCore`/`Brush.Epic.Gate` alone — both are used extensively throughout this file for their real (non-Epic) accent purposes (selection highlight, reachability badges, hunt panel, etc.), so a blind rename in a low-effort build risked breaking unrelated visuals; flagged as a nice-to-have, not done. `dotnet build -c Debug` succeeded, 0 errors. Commits: `002c8579f` (bookend IN FLIGHT), `72789f994` (fix). Merged into `origin/main` via `31d2abaa` — verified via `verify-branch-merged.mjs`. `git status --porcelain` clean.
