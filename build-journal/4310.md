# #4310 — Retrofit raw FontSize/FontWeight/FontFamily literals to Typography.xaml styles

- **Status:** ✅ DONE 2026-09-16T01:40:10Z
- **Scope:** buildconsole (work performed in shanemccaw/MyArchitect, not this repo)
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect@2d475e8 (main), on top of 7b2167b (both pushed to main)

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — retrofitting 171 raw FontSize/FontWeight/FontFamily
  literals across 13 named files in shanemccaw/MyArchitect to Typography.xaml style keys,
  adding small BasedOn variant styles where needed (per #3656's
  Controls/HomeDashboardView.xaml DashboardSectionLabelStyle pattern), then correcting
  UI_RULES.md §10's "Update (#4284)" note to reflect real scope.
- 2026-09-16T01:40:10Z ✅ DONE — real repo state at retrofit time was 15 files / ~239
  raw FontSize+FontWeight attribute occurrences (grew from the issue's 171/13 snapshot
  since it was filed; FontFamily was already fully migrated, zero raw hits found).
  Retrofitted all 15 files (Controls/{TelemetryConsoleView,SowAssessmentView,
  ScreenshotGalleryPanel,TenantSwitcher,HomeDashboardView}.xaml, MainWindow.xaml,
  LoginWindow.xaml, RegionSelectOverlayWindow.xaml, Shell/{LeftReferencePanel,
  ConsolePanelView,VaultPanelView,RecordWorkspacePanel,ReportPanelView,
  LogViewerOverlay,CommandPaletteOverlay}.xaml) to Typography.xaml style keys
  (HeaderTextStyle/SubHeaderTextStyle/BodyTextStyle/CaptionTextStyle/ButtonTextStyle/
  InputTextStyle/MonospaceTextStyle), standalone size/family resource keys for
  non-TextBlock elements, or small per-file BasedOn variant styles for uncovered
  weight/size combos (same pattern as #3656's DashboardSectionLabelStyle). Added two
  new standalone Typography.xaml keys (TabHeaderFontSize=12, QueryInputFontSize=15).
  Two more raw literals surfaced mid-build from a concurrent #4292 merge landing on
  main (a duplicate block in MainWindow.xaml, and new file Shell/ScriptEditorView.xaml)
  — fixed those too in a follow-up commit so the repo-wide grep is genuinely zero.
  Corrected UI_RULES.md §10's "Update (#4284)" note (it had wrongly claimed the
  FontSize/FontWeight retrofit was already done) and added a real "Update (#4310)"
  note confirming the rule is now genuinely fully in force.
  Verification: `grep -rnE 'FontSize="[0-9]|FontWeight="[A-Za-z]|FontFamily="[A-Za-z]'
  --include=*.xaml . | grep -v Themes/` returns zero matches repo-wide. `dotnet build
  MyArchitect.csproj -c Debug` succeeds, 0 warnings / 0 errors. All 16 touched XAML
  files confirmed well-formed XML. Both commits (7b2167b, 2d475e8) pushed to
  shanemccaw/MyArchitect main and confirmed via `git merge-base --is-ancestor` (YES).
  Worktree C:\wt\myarchitect-4310 and its local branch cleaned up.
