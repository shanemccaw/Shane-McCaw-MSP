# #3212 — Money — Bill detail sheet (envelope breakdown, funding history, Vault link)

- **Status:** ✅ DONE 2026-09-08T05:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 8c18c1422 (rebased/pushed to main as 8345cbbbc)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — resolved via #3209 (computed-breakdown approach, no shadow
  ledger). Building: `bill_cycle_snapshots` table + capture job, `GET /api/money/bills/:id`
  (money.mjs's `getBillDetail`), vault.bill_account_id linkage for the "Payment reference in
  Vault →" jump, and the real bottom-sheet UI (public/app.js) wired from Bills + Accounts tap.
- 2026-09-08T05:20:00Z ✅ DONE — shipped `migrations/049_bill_cycle_snapshots.sql` (real
  `bill_cycle_snapshots` table + `vault.bill_account_id`), `money.mjs`'s
  `captureBillCycleSnapshots()` (run at boot + the existing 6h housekeeping sweep) and
  `getBillDetail()`, `vault.mjs`'s `findEntryForBillAccount()` + `billAccountId` on
  create/update/list, `GET /api/money/bills/:id`, and the real bottom-sheet UI in
  `public/app.js`/`app.css` wired from both Bills and Accounts tabs, with the vault add
  form gaining an optional bill link. Verified against local Postgres (`finances` db):
  migration applied cleanly and is idempotent on re-run, `captureBillCycleSnapshots` +
  `getBillDetail` exercised directly against real bill accounts (H1 Mortgage) with real
  output, server boots clean, `GET /api/money/bills/:id` returns a real 401 unauthenticated
  (not 404/500) confirming the route is live. Filed #3240 (drag-to-scrub sparkline
  interaction, deliberately deferred) under Feature #3235, milestone "Shane", board status
  AI Batter Up. Branch verified merged into origin/main (`verify-branch-merged.mjs`
  exit 0); working tree clean.
