# #2680 — Build Queue search box: size, clear button, Dispatch auto-search

- **Status:** ✅ DONE 2026-09-03T16:24:30Z
- **Scope:** buildconsole

## Log
- 2026-09-03T16:18:16Z — Starting. Fixing 3 issues in DispatchPanel/BuildQueuePanel per issue #2680 audit:
  1. QueueSearchBox font/height too small
  2. Clear button (✕) doesn't reset filter on empty text
  3. Manual Dispatch should auto-search Queue for dispatched issue number
- 2026-09-03T16:24:30Z — ✅ DONE — commit `2533c1a88` (rebased onto main as `e56877e9f`, pushed to `origin/main`). Fixed all three:
  1. `Controls/BuildQueuePanel.xaml` — `QueueSearchBox` FontSize/placeholder FontSize now bind to `{StaticResource BodyFontSize}` (14, was hardcoded 12); Height bumped 26→32.
  2. `Controls/BuildQueuePanel.xaml.cs` — `QueueSearchBox_TextChanged` now detects the non-empty→empty transition and immediately re-runs the filter (extracted into shared `RunQueueSearch`), fixing the shared ✕ clear button and select-all-delete without touching #2028's Enter-only behavior for non-empty text.
  3. `Controls/DispatchPanel.xaml.cs` — audited ALL subscribers of `Dispatched` first: only one found, `MainWindow.xaml.cs` ~line 636. Changed `Dispatched` from `EventHandler?` to `Action<int>?`, carrying the issue number captured before `TxtIssueNumber.Text` is cleared, at both invoke sites (normal dispatch ~line 276, force re-dispatch ~line 444). Added `BuildQueuePanel.SearchAndFocus(int)` (sets `QueueSearchBox.Text` + runs the search immediately — programmatic trigger, not typed input, so bypassing Enter-only is correct per the issue). Wired into the existing `MainWindow.xaml.cs` handler, now `async`, awaiting `RefreshAsync()` before calling `NotifyBuildDispatched()`/`SearchAndFocus()` so the search runs against the freshly-dispatched row.
- Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors.
- No fixture/hardcoded data. `git status --porcelain` clean at finish. Branch verified an ancestor of `origin/main` before writing DONE.
- No new findings filed — scope matched the issue's own audit exactly, no additional bugs discovered.
