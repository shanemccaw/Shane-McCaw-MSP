# #2737 — AiBatterUpPanel caches GitHubApiClient forever

- **Status:** ✅ DONE 2026-09-04T02:59:00Z
- **Scope:** buildconsole

## Log
- 2026-09-04T02:47:17Z — Starting: fix AiBatterUpPanel.xaml.cs to build a fresh GitHubApiClient per refresh, matching BatterUpPanel.xaml.cs's convention, instead of caching `_gh` indefinitely with `??=`.
- 2026-09-04T02:59:00Z — ✅ DONE — Removed the `_gh` field and its `??=` caching in `AiBatterUpPanel.GetClient()`; it now builds a fresh `Services.GitHubApiClient(settings.GitHubPat)` every call, matching `BatterUpPanel.RefreshAsync`'s convention. A rotated GitHub PAT now takes effect on the next manual refresh with no restart required. Build verified clean (`dotnet build -c Debug`, 0 errors/0 warnings). Commits: b85888308 (fix), merged to main at 2398944b0.
