# #1593 — Account Security: password age, failed-attempts, device compliance backing

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 5afb2af7a

## Log
- 2026-08-29 ⏳ IN FLIGHT — Pulled #1593's body (no comments). Discovered that
  `artifacts/msp-portal` (and the `useAccountSecurityLive.ts` hook this issue is
  about) was retired wholesale today at 16:27 by `f40438cdc` (Portal scaffolding:
  create `artifacts/portal`, retire `artifacts/msp-portal`), hours after this issue
  was filed — the Account Security page no longer exists in the codebase, and no
  `.dc.html` export exists yet under `Design/portal/` for its replacement. Per
  CLAUDE.md's fixed #1485 ordering (architect -> build the endpoints -> regenerate
  contract pack -> Design -> wire), proceeding with the "build the endpoints" step
  only — no frontend wiring is possible or appropriate until Design produces a page.
  Verified all three items directly against the real testbed tenant
  (mccawsoft2.onmicrosoft.com, c4c814d4-3afe-441e-9145-62461d0a4fd3) via live Graph
  calls before writing any code.
- 2026-08-29 ⏳ IN FLIGHT — Real-tenant Graph verification results: (1) password age
  — `GET /users?$select=lastPasswordChangeDateTime` returns 200 with real data for
  all 24 users (18 stale >90d, oldest since 2014) — BUILDABLE. (2) failed sign-in
  attempts — `GET /auditLogs/signIns?$filter=status/errorCode ne 0` returns 403
  `Authentication_RequestFromNonPremiumTenantOrB2CTenant` — this tenant has no
  Entra ID Premium P1/P2 (`subscribedSkus` confirms only ENTERPRISEPACK/FLOW_FREE/
  POWER_BI_STANDARD/Power_Pages_vTrial) — genuine, real limit for this tenant
  shape, not a wiring gap. (3) device compliance — tenant's only device-management
  service plan is `INTUNE_O365` (the limited Office-apps-only MDM bundled in E3),
  and it is `PendingActivation`, not `Success`; no `INTUNE_A` (full Intune) is
  present, and `GET /deviceManagement/managedDevices` 401s/503s consistently —
  genuine, real limit for this tenant shape. Also confirmed via direct DB query
  that portal users have no verified identity link to an M365 UPN (testbed portal
  users are personal outlook.com/gmail.com addresses, not `@mccawsoft2.onmicrosoft.com`
  accounts) — so any Graph-backed signal here can only be tenant-wide, never
  "this specific portal user's own" fact. Proceeding to build: a live (uncached)
  tenant-wide password-age signal, real endpoint + Wire contract; failed-sign-ins
  and device-compliance built as real code paths too (so a tenant that DOES have
  the required license gets real data), each degrading to an honest
  `available:false` + specific reason for a tenant of this shape. No new DB
  storage needed — everything is cheap enough to compute live on each request
  (mirrors `getInitialDomainForTenant`'s live-per-request pattern), so no
  migration file for this issue.
- 2026-08-29 ✅ DONE — Shipped in commit `5afb2af7a` (pushed, confirmed ancestor of
  `origin/main` via `verify-branch-merged.mjs`): `lib/account-security-graph.ts` +
  `routes/portal-account-security-graph.ts` (new `GET
  /api/portal/account-security/graph-signals`), registered in `routes/index.ts`,
  13 new tests across `account-security-graph.test.ts` +
  `portal-account-security-graph.test.ts` (both registered in `vitest.config.ts`'s
  include list), all passing (`npx vitest run` — 13/13). `npx tsc --noEmit`
  confirmed zero new type errors from these files (pre-existing unrelated errors
  in other files are untouched by this build). Four signals shipped, not three:
  password age (tenant-wide Graph, real data for this tenant), failed sign-ins
  (tenant-wide Graph via `/auditLogs/signIns` — genuinely `available:false` for
  this tenant, real `entra_premium_required` reason, real code path for a
  licensed tenant), device compliance (tenant-wide Graph via
  `/deviceManagement/managedDevices` gated on a real `subscribedSkus` check —
  genuinely `available:false` for this tenant, real `no_intune_license` reason,
  real code path for an Intune-entitled tenant), and local failed-login lockout
  state (`users.failed_login_attempts`/`last_failed_login_at`/`locked_until` —
  real, already-populated portal-own data, zero Graph calls, always answerable —
  this closes the contract pack's own separate citation of that column under the
  same "failed attempts" item). `docs/account-security-contract-pack.md` §8
  records the full resolution with real evidence; §2 and §5 updated so the doc
  doesn't contradict itself about what's still unbuilt. No schema/migration was
  needed (nothing new is persisted — live-per-request, matching
  `getInitialDomainForTenant`'s existing pattern), so nothing to append to #1630.
  No frontend wiring: `artifacts/msp-portal` (this issue's own
  `useAccountSecurityLive.ts`) was retired wholesale in `f40438cdc` hours after
  #1593 was filed, and no `Design/portal/` export exists yet for a replacement —
  per CLAUDE.md's fixed #1485 order this is deliberately backend-only, the
  "build the endpoints" step ahead of Design. `git status --porcelain` clean.
  No new findings filed — nothing surfaced beyond what #1593 itself asked to be
  determined and built.
