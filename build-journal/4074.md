# #4074 — sow-monitoring-addon.ts default-tier resolution bug

- **Status:** ✅ DONE 2026-09-14T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 0c3c238a4

## Log
- 2026-09-14T00:00:00Z ⏳ IN FLIGHT — Confirmed via direct DB query against local `services` table that `type_attributes.tenantTierLabel` holds the seat band (Micro/SMB/Mid-Market/Enterprise), and the real quality tier (Foundation/Growth/Premier) lives in `type_attributes.packageKey` (e.g. `"core:growth"`) and the row name/slug prefix. Fixing `resolveTenantMonitoringAddon()` in `artifacts/api-server/src/lib/sow-monitoring-addon.ts` to group/select by the real quality-tier vocabulary instead of the stale basic/enhanced/premium match against the seat-band field.
- 2026-09-14T00:20:00Z ✅ DONE — Rewrote `resolveTenantMonitoringAddon()` to derive the quality-tier key from `type_attributes.packageKey` (`"core:<tier>"`) instead of matching `tenantTierLabel`, renamed `TIER_LABEL_ORDER` to `foundation`/`growth`/`premier`, and changed the default match from the stale `"enhanced"` to `"growth"` (the real successor per Git #609's "middle tier" default, confirmed by a research subagent reading #609's closing verification comment). Updated `sow-monitoring-addon.test.ts` fixtures to the real `packageKey` vocabulary — `pnpm run typecheck` and `vitest run src/lib/sow-monitoring-addon.test.ts` (13/13 passed) both clean. Live-verified: ran `resolveTenantMonitoringAddon()` three times against the real local DB (testbed tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3`) — all three calls deterministically resolved `defaultTierId: "growth"` regardless of `Map` insertion order, priced at the real Foundation-band-equivalent fallback seat count. Commit `0c3c238a4` pushed straight to `origin/main` (fast-forward from `81fb72b99`); `verify-branch-merged.mjs` confirms ancestry. `git status --porcelain` clean.
