# #1921 — Contract packs cite retired artifacts/msp-portal paths after the portal scaffolding rename

- **Status:** ✅ DONE 2026-09-04T03:40:00Z
- **Scope:** platform (docs only)
- **Started:** 2026-09-04
- **Commit(s):** aa7d11439a62fa429f2819dff0f6cd1922c9b3fb (on origin/main; also
  e7c56d6b912e4fadfb469db7398515831bfe72ec pre-rebase on `agent/1921-q1558`)

## Log
- 2026-09-04T02:59:00Z ⏳ IN FLIGHT — Pulled issue body + comment. Confirmed 32 files under `docs/*.md` still
  cite `artifacts/msp-portal` (99 raw hits). Audited every distinct referenced path against the current
  `artifacts/portal` tree: only 3 files survived the rename (`billingLive.ts`, `billingWire.ts`,
  `useMfaRegistrationLive.ts` — moved from `components/portal-v2/` straight to `components/`, no subdir).
  Every `portal-v2-*.tsx` page, `dashboard.tsx`, `webhooks.tsx`, `offboarding.tsx`, `customer-*.tsx`,
  `email-auth-setup.tsx`, `consent-declined.tsx`, and the Data/Model/Wire/Live layer files for billing
  receipts/risk-register/account-security/ms-changes/policy-decisions/compliance-obligations/webhooks were
  NOT carried over — `artifacts/portal/src/pages` currently holds only `coming-soon.tsx`, `index.tsx`,
  `not-found.tsx`, `support.tsx` (the #1485 rebuild scaffold). Proceeding to correct/annotate each of the
  31 remaining files following the pattern already fixed in `docs/runbooks-contract-pack.md`.
- 2026-09-04T03:40:00Z ✅ DONE — Fanned out 4 parallel subagents (8 files each) covering the
  remaining 31 files. 16 needed real edits (99 raw `artifacts/msp-portal` hits total); the other
  15 already carried correct historical/archive-tag framing from prior sessions and needed no
  change. Every edit either (a) corrected the path for the 3 real survivors
  (`billingLive.ts`, `billingWire.ts`, `useMfaRegistrationLive.ts` — moved from
  `components/portal-v2/` to `components/`, no subdir) or (b) swapped the prefix to
  `artifacts/portal` with an inline "NOT carried over" annotation citing `f40438cdc` and #1921.
  Left `git show <archive-tag>:...` citations and existing retirement prose alone — those are
  legitimate historical references, not stale live-path claims. Final sweep
  (`grep -n "artifacts/msp-portal" docs/*.md` filtered for un-annotated hits) found zero remaining
  unqualified stale references. Verified `git status --porcelain` clean, rebased my branch onto
  `origin/main`, pushed directly to `origin/main` (fast-forward), and confirmed via
  `scripts/dev-server/verify-branch-merged.mjs` — OK, merged. No new findings filed; this was a
  docs-correctness audit with no code/schema/backend surface touched, so no DB work, no tsc/vitest
  needed.
