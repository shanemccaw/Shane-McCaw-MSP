# #4633 — Document that a plain comment triggers nothing (only two real ways to act on new information)

- **Status:** ✅ DONE 2026-09-17T00:20:00Z
- **Scope:** platform (docs only)
- **Started:** 2026-09-17
- **Commit(s):** d97c76cdf (doc edit, pushed to origin/main)

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — pulled #4633's full body and #4610's full body/bookend for precedent. Adding new §4.6 to `BUILD_QUEUE_METHOD.md` near §4 (right before the §5 break), stating plainly that a plain issue comment does not trigger any BuildConsole action, and giving the two real paths (fresh BUILD: dispatch, or Resume Session with an explicit re-pull-comments instruction) plus a worked example. Docs-only, no app code.
- 2026-09-17T00:20:00Z ✅ DONE — added new §4.6 "A plain comment triggers nothing — the only two real ways to act on new information (Git #4633)" to `desktop/BuildConsole/BuildMap/BUILD_QUEUE_METHOD.md`, placed right after §4.5 (the last §4 subsection) and before the §5 break. States plainly there is no comment-watching/auto-redispatch mechanism, gives the two real paths (fresh `BUILD:` dispatch + Batter Up, or Resume Session with an explicit re-pull-latest-comments instruction), and a concrete worked example (#6001 with a closed build and a missed edge case, wrong vs. right actions). Docs-only — confirmed via `git diff --stat` on the commit (1 file, +54/-0), no application code touched. Pushed directly to `origin/main` as `d97c76cdf`; `verify-branch-merged.mjs` confirms it's an ancestor. Working tree clean (`git status --porcelain` empty).
