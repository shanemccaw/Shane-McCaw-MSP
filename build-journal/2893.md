# #2893 — Plaid Link hangs: NavigateToString gives null origin, Plaid's postMessage throws (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T00:20:00Z
- **Scope:** desktop
- **Started:** 2026-09-04
- **Commit(s):** 99f18f17f (the fix), e03c94ef0 (IN FLIGHT bookend)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — Confirmed root cause per issue body: `PlaidLinkWindow.StartWebViewAsync()` uses `LinkWebView.NavigateToString(...)`, which gives the document an opaque `null` origin. Plaid's own `flink.js` bundle calls `window.postMessage(msg, targetOrigin)` internally for its own iframe comms and Chromium throws `SyntaxError: Invalid target origin 'null'` — uncaught inside Plaid's bundle, so it never reaches this app's own onerror/watchdog handling from #2888. Implementing the fix exactly as specified: write the Link HTML to a real temp file, use `CoreWebView2.SetVirtualHostNameToFolderMapping` to give it a real virtual origin (`https://shanessurvival.local/link.html`), navigate to that instead of `NavigateToString`, clean up on window close. Confirmed via grep this is the only `NavigateToString` call in ShanesSurvival.
- 2026-09-04T00:20:00Z ✅ DONE — Implemented exactly the fix from the issue: `StartWebViewAsync` now writes `BuildHtml(_linkToken)` to a real temp file under `%TEMP%\ShanesSurvival-PlaidLink-<guid>\link.html`, calls `CoreWebView2.SetVirtualHostNameToFolderMapping("shanessurvival.local", <folder>, CoreWebView2HostResourceAccessKind.Allow)`, and navigates to `https://shanessurvival.local/link.html` instead of `NavigateToString`. Added `CleanUpLinkHtmlFolder()` wired to the window's `Closed` event — clears the virtual host mapping and deletes the temp folder, both best-effort so cleanup never blocks the dialog closing. Kept #2888's 20s WebView2-startup timeout and JS watchdog untouched, as instructed (different, additional failure mode). Confirmed via grep this was the only `NavigateToString` call anywhere in ShanesSurvival — no other spot needs the same fix. `dotnet build ShanesSurvival.sln`: clean, 0 warnings / 0 errors. Pushed `agent/2893-q1640` fast-forward onto `origin/main` (99f18f17f); `verify-branch-merged.mjs` confirms merged, working tree clean. **Honest verification gap**: could NOT live-verify the fix actually reaches Plaid's real bank-selection screen — this is a WPF desktop GUI app requiring real interactive launch, real Plaid credentials/network, and driving Plaid's real hosted UI, none of which is reachable headless from this session (same real constraint #2888 hit). What IS confirmed: the specific mechanism causing the null-origin `postMessage` exception (the opaque `"null"` origin from `NavigateToString`) is structurally eliminated — the page now loads under a real `https://` virtual origin, which is the standard, documented fix for this exact WebView2 failure class. Shane's own next real Link Account attempt is the true confirmation of whether this was the full root cause.
