# Git #2070 — Build Queue Panel: stale "waiting on #N" badge/text

- **Status:** ✅ DONE
- **Scope:** buildconsole

## Log
- 2026-08-31: Session started. Confirmed root cause per issue: `BuildWaitingOnText()` builds "waiting on #N" from raw `BlockedBy` (via `CleanBlockers()`) with no filtering against `_openIssues`, unlike `IsGenuinelyBlocked()` which does filter. Issue comments also report a deeper instance: the badge itself (`IsGenuinelyBlocked()`) shows stale BLOCKED when `_openIssues` itself hasn't refreshed after a blocker closes (#1582/#1589, #2105/#2104). Auditing current code before changing anything.
- 2026-08-31: ✅ DONE — Confirmed diagnosis against current code: `BuildWaitingOnText()` (BuildQueuePanel.xaml.cs:3384) and two other callsites read `node.BlockedBy` (raw `CleanBlockers()` output, unfiltered) while `IsGenuinelyBlocked()` (line 1004) filters the same list against `_openIssues`. Added `LiveBlockedBy(node)` applying `IsGenuinelyBlocked`'s exact filter (same cold-start fail-safe: raw list when `_openIssues == null`) and switched `BuildWaitingOnText`'s fallback text, the "waiting on" row's visibility gate, and `SelectNode`'s `StatusDetails` to use it — badge and text can no longer disagree. `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/0 errors. Commit `4d3761c64`, fast-forwarded onto `origin/main` (base was already current, no rebase needed). Working tree clean.
  - Filed the deeper finding from the issue's own comments (badge itself stale because `_openIssues` only refreshes on Git Board manual-refresh/tab-open, per the 2026-08-14 manual-refresh-only policy) as its own issue rather than fixing in-session, since resolving it means reconsidering that explicit no-background-polling decision — a real product tradeoff, not a code gap. Filed as **#2107**, parented under #1202 (Epic: Build Console), board status set to "AI Batter Up".
