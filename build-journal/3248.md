# build 1981 (branch `agent/3248-q1981`, tracked GitHub issue `#3248`) — Vault edit + delete UI

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** other — `web/shanes-life/` (Shane's personal app, not the platform monorepo proper)
- **Started:** 2026-09-08
- **Commit(s):** b3c60c31c "Vault: per-row edit and delete UI (Git #3248)"

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — dispatched to add per-row edit and delete to the
  vault room (`web/shanes-life/public/app.js`'s `vaultRow`), wiring onto the already-real,
  already-tested `PATCH`/`DELETE /api/vault/:id` (`src/routes/api.mjs`, `src/core/vault.mjs`,
  live since #3150). No schema and no new endpoint needed.
- 2026-09-08T00:20:00Z ✅ DONE — `vaultRow` now renders Edit/Delete buttons beside Reveal
  (`.vault-row-actions`). Edit opens an inline form prefilled from the entry (label, site,
  username for a login, masked hint for a bill reference, plus an optional "new
  password"/"new account number" field with the same Generate button the add card uses) and
  PATCHes only the fields that actually changed — the password field is never re-typed to
  save an unrelated field, matching `updateEntry`'s own "secret omitted = ciphertext
  untouched" contract, and the check suite's assertion of that ("renaming an entry does not
  re-encrypt its value") is unaffected since no backend code changed. Delete uses this app's
  existing `confirm()` idiom (same pattern as `careRow`'s "Remove") before calling
  `DELETE /api/vault/:id`. Both refresh the list via the room's own `refresh()`, threaded in
  as `onChanged`. No schema change, no new endpoint — pure UI wiring per the issue's stated
  scope.
  Verification: `node --check public/app.js` passes. `npm run check` (`bin/check.mjs`) needs
  a running server + its own configured database for this standalone app — no `.env.local`
  is provisioned for `web/shanes-life` in this worktree, so it wasn't run; the backend
  routes/logic it exercises (`updateEntry`/`deleteEntry`) are unchanged by this commit. Read
  the full `updateEntry`/`deleteEntry` implementations in `src/core/vault.mjs` and the
  `PATCH`/`DELETE` routes in `src/routes/api.mjs` to confirm the request shapes this UI sends
  match what the server expects.
  Rebased onto `origin/main` (was 2 commits behind after concurrent #3240 landed) and pushed
  directly to `main` per repo convention. `verify-branch-merged.mjs` confirms merged;
  `git status --porcelain` clean. No findings filed — no gap found beyond the issue's own
  scope.
