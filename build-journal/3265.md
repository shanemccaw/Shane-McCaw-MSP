# #3265 — Today's "Recent" section shows empty state instead of not appearing

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 85f7a8856 (on main as dfe21ec7e after rebase-push)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — wrapping the entire "Recent" section (header + content) in `data.recent.length > 0`, matching the existing guard on the "waiting in the inbox" card above it, per issue body.
- 2026-09-08T00:20:00Z ✅ DONE — `web/shanes-life/public/app.js` ~line 1787: removed the always-rendered `<h2>Recent</h2>` + illustrated `empty(...)` fallback; the whole section (header + content) now only builds/appends when `data.recent.length > 0`, same pattern as the "waiting in the inbox" card just above it. `node --check` passed. `verify-branch-merged.mjs` confirms `agent/3265-q1991` is an ancestor of `origin/main` (pushed as dfe21ec7e). `git status --porcelain` clean aside from this bookend edit.
