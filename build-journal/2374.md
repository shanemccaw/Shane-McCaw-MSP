# #2374 — Gap: no retention policy, no compare-to-baseline in the panel

- **Status:** ✅ DONE 2026-09-02T20:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Shot Vault, Feature #2367 item 7)
- **Started:** 2026-09-02
- **Commit(s):** 4cf623c52 (Shot Vault feature), rebased+pushed as 3dd2ac1a3

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — implementing real retention purge (ShotVaultService.ApplyRetention,
  settings-store-backed policy) and real compare-to-baseline (pin a shot, ShotVaultTile.HasDiffFromBaseline)
  in the Shot Vault rail panel, per Feature #2367 item 7's real audit gap.
- 2026-09-02T20:20:00Z ✅ DONE — Shipped both real gaps from Feature #2367 item 7:
  - **Retention policy**: `ShotVaultService.ApplyRetention(shots, maxAgeDays)` permanently deletes shot
    files older than the selected window (there is no separate metadata store — the PNG on disk IS the
    vault entry, so retention can only mean actually removing the file). Policy (`Keep forever` / 7 / 30
    / 90 days) is a real `ComboBox` in the panel, persisted via the existing `SettingsStore`
    (`shotvault:retentionDays`), applied automatically the first time the panel opens each session and
    on every policy change, plus an explicit "Purge now" link. Deleted-count is reported honestly via
    toast (0 deleted is silent, no fake "cleaned up!" noise).
  - **Compare-to-baseline**: a real "Set as baseline" / "Clear baseline" link on every tile pins one
    shot; `ShotVaultService.BuildTiles` now also computes `HasDiffFromBaseline` for every shot against
    that one pinned shot's own real perceptual hash (reusing the hash already computed for the existing
    chronological DIFF chain — no second decode pass). The pinned shot renders a `BASE` badge + accent
    border; any shot whose real pixels differ from it renders `≠ BASE`. Baseline auto-clears if the
    pinned shot ages off the list (purged or deleted by hand) rather than silently comparing against a
    file that no longer exists. Status line reports the real "N differ from the pinned baseline" count
    alongside the existing chronological-DIFF sentence.
  - No fixture data anywhere — every number/badge is a real computed value off the actual files on disk.
  - **Verification**: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors, both before and
    after the rebase onto `origin/main`.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.
  - `git status --porcelain` clean at session end.
  - No genuine new finding surfaced during this build — none filed.
