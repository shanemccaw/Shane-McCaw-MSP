# #1821 — Portal Shell: alerts dropdown

- **Status:** ✅ DONE 2026-09-03T15:36:20Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `442eb3192` (real work: server-side deep-link resolution + AlertsDropdown UI + test manifest), merged into `origin/main` at `1e7bf867e` (fast-forward-safe merge, confirmed via `verify-branch-merged.mjs`)

## Log
- 2026-09-03T15:28:58Z ⏳ IN FLIGHT — Read #1821 body + comment, `docs/alert_preferences.md`, `Design/portal/design_handoff_ui_shell/README.md` + `Shell.dc.html` (the real Alerts-popover markup at line 565-623), the real `notifications.ts` routes (`GET /portal/notifications`, `/unread-count`, `/stream` SSE, `/activity-feed`, `PATCH /:id/read`, `POST /read-all` — all mounted under `/api` per `app.ts:112`), `notification-center.ts` (createNotification/broadcastNotification), `sse-channels.ts`/`sse-hub.ts` (confirmed the SSE wire format: plain `data: {...json}\n\n`, no custom `event:` name), and `portal-deep-links.ts` (confirmed via its own header comment this issue is its intended second consumer, after the customer-alert email path). Confirmed #1819 (blocker) is closed/merged and its shell already left inert, `data-testid`-tagged trigger buttons in `TopBar.tsx` for this issue to wire. Queried the real local DB: `notifications` table has 80 real `customer_user`/`personal` rows (user 39, 50 unread/30 read, category `offer`, `linkPath=/customer-offers` — not in `portal-deep-links.ts`'s 8-entry map, so it resolves through the honest unmapped-path fallback to `/coming-soon`, which I verified by tracing the real resolver code against this real row). About to build the dropdown against these real endpoints.
- 2026-09-03T15:36:20Z ✅ DONE — Shipped:
  - **Server** (`artifacts/api-server`): `notifications.ts`'s `/portal/notifications` and `/portal/notifications/activity-feed` GET routes now resolve every row's `linkPath` through the existing `portal-deep-links.ts` map, adding a `deepLink: {href, available, label}` field — scoped to the portal-only routes, never `/notifications` or `/msp/notifications` (those point at real admin-panel routes the portal map doesn't know about). `notification-center.ts`'s live SSE push does the same for the `customer_user` broadcast branch (the only branch that sets `userId`), so a pushed notification never carries a dead `/portal-v2/*` href either.
  - **Client** (`artifacts/portal`): new `components/shell/notifications/` (`AlertsDropdown.tsx`, `useNotifications.ts`, `notificationDisplay.ts`, `types.ts`) — real bell trigger + badge (personal unread count), popover with LIVE indicator (SSE-connected), unread pill / "Up to date" / "Mark all read", Personal / All activity tabs (the "route into the full activity feed" the issue asks for — switching tabs, not a separate page), TODAY/EARLIER grouped rows (severity-coloured border, category pill, unread dot, chevron for linked rows), footer "Open activity feed" + "Alert preferences" (links to `/coming-soon` — no Settings page exists in `artifacts/portal` yet, matching the shell's own no-dead-links rule). Calm empty state ("Nothing needs your attention") only when the customer has literally zero notifications ever; zero-unread-with-history renders "Up to date" inside the normal populated header — never an error, never a blank hole.
  - **Merge conflict resolved for real, not mechanically**: while this was in flight, #1820 (account dropdown) and #1822 (SOP dropdown) landed on `main` and introduced a shared `openPopover` state in `TopBar.tsx` (`null | "user" | "sop"`, with their own comment reserving `"alerts"` for this issue). Reworked `AlertsDropdown` from a self-contained `open` boolean into a controlled `{open, onToggle, onClose}` component wired into that same shared state, so alerts/SOP/account are genuinely mutually exclusive (one popover open at a time, matching the design's own constraint) rather than three independent booleans that could all show open together.
  - Preferences UI intentionally NOT rebuilt here — out of scope per the issue; footer links out only.
  - **Verification**: `npx tsc -p tsconfig.json --noEmit` clean for every changed/new file in both apps, re-run after each merge (diffed against each app's own pre-existing baseline — #2546's empty `class-variance-authority` store still blocks portal-wide `tsc`/`vite build` for unrelated `ui/button.tsx`/`ui/badge.tsx`/`support-chat/*` files, same 14-error set #1819 already documented, none touching this build; api-server's ~3900 pre-existing `drizzle-orm`/`lib/db` resolution errors are a standing worktree environment issue, also unrelated). Traced the real local DB (80 real `customer_user` rows, user 39) against the resolver code to confirm the unmapped-path fallback behaves correctly on real data. `shaneapp://runTest` could NOT be invoked this session — the `shane-msp` MCP courier reported `CONNECTION_CLOSED` (checked directly via `ToolSearch`), and `node scripts/dev-server/status.mjs` confirmed the local dev-server process is genuinely `DEAD` on port 8080 — a real, stated check, not an assumed limitation. Did not attempt to self-heal by running `pnpm`/building the api-server myself, per CLAUDE.md's "pnpm install is not a remedy" and the pre-existing broken `lib/db`/`drizzle-orm` resolution state that a local build would hit. `test-manifests/portal/alerts-dropdown.json` is written (apiTests hitting the real endpoints with a real testbed login, uiSteps asserting the honest unauthenticated `RequireAuth` state — `artifacts/portal` has no login page yet, #1648, same documented gap #2519's manifest already has) and registered in `_regression-suite.json`, but not executed this session — an honest gap, not a false pass claim.
  - Merged: `agent/1821-q1384` → `origin/main` (`1e7bf867e`, several fast-forward-safe merges of a fast-moving `main` under concurrent load), confirmed via `verify-branch-merged.mjs`. `git status --porcelain` clean.
  - No findings filed — no genuine out-of-scope bug hit this session (the one adjacent gap, `linkPath=/customer-offers` not being in `portal-deep-links.ts`'s 8-entry map, is expected/by-design: that map only covers the 8 raw `/portal-v2/*` `deep_link_path` values `customer_tenant_alert_rules` seeds, and the honest unmapped fallback to `/coming-soon` is exactly the intended, already-documented behavior for any other `linkPath` — not a bug to file).
