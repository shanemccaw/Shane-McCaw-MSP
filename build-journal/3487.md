# #3487 — Feature: SLA Tracking (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:44:45Z
- **Scope:** buildconsole (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** e153b8794 (real work, merged into `origin/main`; rebased twice
  over concurrent sibling pushes — a trivial `ApplyAuthState` auto-merge, and a
  real structural conflict with #3488's Support Tickets group, both also
  registering `RegisterWatchTab()`/the Watch tab)

## Log
- 2026-09-10T20:39:00Z ⏳ IN FLIGHT — pulled #3487's full body and all three comments
  before starting:
  1. `BUILD:` dispatch (buildSet `MyArchitect`, posted 04:46:33Z) — carries
     `--blocked-by 3493`, instructs reading `GEMINI.md`/`BUILD_QUEUE_METHOD.md`/
     `UI_RULES.md`.
  2. `BUILD:` dispatch (buildSet `BatterUpClearOut`, posted 04:51:36Z) — this
     session's own launch. Standing rules only.
  3. A prior session's comment (posted 06:19:38Z) recording a real `🛑 BLOCKED`
     conclusion on #3493 (shell not yet built) with the `blocked_by` edge already
     wired — matching board `blocked` label was in place on arrival.

  Re-checked the blocker per CLAUDE.md's re-pickup rule before doing anything else:
  `gh issue view 3493` → `CLOSED`/`state_reason: completed`, labeled `complete`.
  `blocked_by` list was `[{#3493}]` (real, not stale-empty) — removed the edge via
  `DELETE .../dependencies/blocked_by/<id>`, swapped `blocked` → `in-flight` on
  #3487. Verified the shell actually landed before building against it: read
  #3493's own DONE comment (merged `62f97f739`, shell landed `e8460c5fd`), confirmed
  `desktop/MyArchitect/Shell/*.cs` is real and present on disk, `UI_RULES.md` §2's
  Watch tab spec ("Alerts #3483, Task Queue #3490 and SLA breaches #3487,
  consolidated into shared themed groups, not three separate tabs") matches the
  shell's own `MainWindow.xaml.cs` comment. Sibling Watch-tab Features (#3483,
  #3490) confirmed still `blocked`/unbuilt — no collision on the tab.

  Audited the real backend per #3487's own body: `artifacts/api-server/src/routes/
  msp-sla.ts` (policies/breaches/escalations/compliance/timers/evaluate/summary,
  all `requireCapability("ladder.msp-operator")`) and `msp-m365-sla.ts`
  (`GET /api/msp/m365-sla`) both read directly, confirmed present and real —
  matches the issue's audit claim.

  Real gap hit, same one already filed and tracked elsewhere in this Epic
  (#3502/#3505/#3540): `ITenantService` is fixture data — no numeric `tenants.id`,
  only a fake local id + a real-looking `TenantGuid`. `msp-sla.ts`'s
  breaches/compliance/escalations rows carry only a numeric `customerId`, no GUID,
  so a genuine "selected tenant" filter isn't resolvable today for those three —
  same gap `TryResolveLaunchControlScope`/`BuildScriptLibraryRows`/
  `BuildTrackerStepRows` already state honestly elsewhere in this file. Built
  those three galleries showing the real, unfiltered MSP book instead of guessing
  an id (never fabricated a customerId or a fake tenant match). `msp-m365-sla.ts`
  is different: its response carries the real tenant GUID per customer, which the
  fixture `TenantService` already has — so **M365 Uptime is genuinely filtered to
  the selected tenant today**, no gap to state.

  **What shipped, real and working** — no fixture data anywhere; every row is a
  real call against the already-existing `msp-sla.ts`/`msp-m365-sla.ts` endpoints:
  - `Models/SlaModels.cs` — `SlaPolicy`/`SlaBreach`/`SlaEscalation`/
    `SlaComplianceRecord`/`M365SlaCustomer`/`M365SlaService` + response envelopes,
    field-for-field against the routes' real column aliases.
  - `Services/ISlaService.cs` + `SlaService.cs` — real HTTP client (policies,
    breaches, escalations, compliance, m365-sla, timer resolve), same
    AuthToken/Authorize/JsonOptions shape as `RemediationTrackerService`. Wired
    into `MainWindow.xaml.cs`'s `ApplyAuthState` fan-out alongside the other
    MSP-scoped services.
  - `MainWindow.xaml.cs`: new Watch-tab "SLA" group (`RegisterWatchTab`, Order 10)
    with five real galleries — Breaches (live-count badge), Escalations
    (live-count badge), Compliance, Policies, M365 Uptime — each opening a real
    full-panel record workspace. Breaches carries a genuine confirm-armed
    "Resolve Timer" action (`POST /api/msp/sla/timers/:timerId/resolve` — the
    real resolvable unit; there is no per-breach resolve endpoint). Every gallery
    fails honestly (a stated, real error row) on a transport/auth failure rather
    than showing anything fabricated.
  - `dotnet build MyArchitect.csproj` — 0 warnings, 0 errors.

  **No genuine new finding to file this session** — the fixture-`TenantService`
  gap this build hit for three of five galleries is already tracked under
  #3502/#3505/#3540, not a new discovery; noted honestly in-code (doc comments on
  `RegisterWatchTab`) rather than re-filed.
- 2026-09-10T20:44:45Z ✅ DONE — commit `e153b8794`, real work shipped and pushed
  to `origin/main`. Two rebases were needed while finishing (concurrent sibling
  Watch-tab sessions landing under the same #3454 Epic):
  1. `origin/main` moved to add Document Hub (#3486) and VIP Classifications
     (#3484) — trivial auto-merge (`ApplyAuthState`'s new-service-line additions).
  2. `origin/main` moved again to add Support Tickets (#3488) — a real structural
     conflict: both #3488 and this build independently added a
     `private void RegisterWatchTab()` method registering the *same* fixed tab.
     Resolved by hand into one method with two `RegisterFixedTabGroup` calls
     ("Support Tickets" Order 10, "SLA" Order 20) and one merged doc comment,
     keeping both feature's full helper-method sets intact — no functionality
     dropped from either side. `dotnet build` clean (0/0) both before and after
     each rebase.

  `node scripts/dev-server/verify-branch-merged.mjs` → exit 0, "Safe to write
  DONE". `git status --porcelain` clean.

  Labels synced: `in-flight` → `complete`. Posted the completion comment on
  #3487.
