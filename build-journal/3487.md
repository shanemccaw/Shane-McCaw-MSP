# #3487 — Feature: SLA Tracking (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T06:22:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** (bookend only — no code changes, genuinely blocked)

## Log
- 2026-09-10T06:18:00Z ⏳ IN FLIGHT — pulled issue #3487 body + both dispatch comments (BuildSets MyArchitect and BatterUpClearOut). Confirmed real scope: MyArchitect (WPF desktop) Feature under Epic #3454, sibling of #3482/#3483, entirely client/UI work (SLA compliance/breach view for the selected tenant) against backend endpoints (`msp-sla.ts`, `msp-m365-sla.ts` — `/msp/sla/compliance`, `/msp/sla/breaches`, `/msp/sla/escalations`, `/msp/sla/policies`, `/msp/m365-sla`) the issue's own audit confirms already exist. Checked the Epic's standing rule before touching any MyArchitect UI.
- 2026-09-10T06:22:00Z 🛑 BLOCKED — Epic #3454's standing rule requires #3493 (UI Shell Redesign) to land before any other MyArchitect Feature's UI work; #3487's own (and only) checklist item is client/UI work that needs a shell surface to dock into. Verified `desktop/MyArchitect/` still has no shell scaffolding — only `MainWindow.xaml`, `RegionSelectOverlayWindow`, and PowerShell console services; none of #3493's checklist items (fixed tabs, right-panel record workspace, contextual tabs, etc.) are built yet. Verified the existing `blocked_by → #3493` edge is current (#3493 is OPEN, labeled `in-flight` — actively being built elsewhere, not stale/closing) — no edge changes needed. Also noted #3487's own checklist is unresolved ("pending granularity confirmation" — the single item isn't broken into concrete deliverables), so a scoping pass is still needed even after #3493 ships. Synced labels on #3487 (`blocked`, removed `in-flight`) and posted an explanatory comment. No code changes; nothing to build here until #3493 ships.
