# #2624 — Account Security: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-13T00:35:10Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** `649885abc` (Account Security wire, includes the #3896 fix), pushed to `main`

## Log
- 2026-09-13T00:08:10Z ⏳ IN FLIGHT — Investigation done: pulled #2624 body/dispatch comment,
  confirmed #2623 (design export) landed and #3677 (shell) landed. Verified #3032 (target-role
  ceiling) is fixed on `main` (commit `fcb16b522`, real live-DB regression test
  `msp-settings-target-role-ceiling.live-db.test.ts`) for the 5 credential routes — the design's
  own "no route checks the target's role" framing is now stale for those 5 and must not ship as
  a UI-only guard. Found the session-revoke route (`DELETE /msp/settings/users/:userId/sessions`)
  was NOT included in the #3032 fix (commit message explicitly lists only the 5) — still a real
  target-ceiling gap, to be filed as a sibling finding under #2562. Read the full contract pack
  (`docs/msp-console/account-security-msp-console-contract-pack.md`) and design export
  (`Design/MSP_Console/design_handoff_msp_console/Account Security.dc.html`), and the README's
  "embedded contract" section (screen 39, mounts under Operations, `embedded`/`forceEmpty`
  disappear in the real rebuild per the README itself). Next: implement the module + wire it into
  the console tree/shell, extend `GET /api/msp/audit` with entityType/entityId filters for a real
  per-user audit trail, extend `GET /msp/settings/users` to return `mfaEnforced`.
- 2026-09-13T00:35:10Z ✅ DONE — Shipped `AccountSecurity.tsx` +
  `api/account-security-api.ts`, mounted at `/ops/acctsec` (registered in `nav.ts`,
  `treeModel.ts`, `ConsoleShell.tsx`). Wires all six real `msp-settings.ts` routes:
  reset-password, temp-password, reset-mfa, mfa-enforcement, status, sessions revoke.
  Removed the design's now-stale "ceiling enforced by this screen only" framing —
  the page renders the server's real response/403, it does not recompute its own
  role-order guess. Extended `GET /msp/settings/users` to also return `mfaEnforced`
  + `tenantId` (real columns, no schema change) and `GET /msp/audit` to accept
  `entityType`/`entityId` filters, so the Audit trail panel reads real persisted
  history instead of a fixture or ephemeral log. Found and fixed a live gap in the
  same pass: `DELETE /msp/settings/users/:userId/sessions` was the one route
  #3032's own fix missed — added the same `rejectIfTargetOutranksCaller` call, plus
  2 new live-Postgres regression tests (10/10 pass in
  `msp-settings-target-role-ceiling.live-db.test.ts`). Filed as #3896, sibling
  sub-issue of #2562, labeled `bug`+`security`, board status AI Batter Up, fixed in
  this same build (noted on the issue). Verified: api-server `tsc --noEmit` clean;
  msp-console `tsc --noEmit` clean, `pnpm test` 16/16, `vite build` clean;
  api-server's `msp-settings-target-role-ceiling.live-db` (10/10),
  `msp-settings-user-security.live-db` (4/4), `msp-settings-portal-links` (4/4) all
  pass against local Postgres. Rebased twice over concurrent landings of #2591
  (Policy Engine) and offboarding, resolving `nav.ts`/`nav.test.ts`/`treeModel.ts`/
  `ConsoleShell.tsx` conflicts by keeping both builds' MSP_PAGES entries (12 total).
  Merged into `origin/main` at `649885abc`, verified via
  `verify-branch-merged.mjs`. Working tree clean.
