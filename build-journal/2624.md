# #2624 — Account Security: wire the MSP Console UI to the real endpoints

- **Status:** ⏳ IN FLIGHT 2026-09-13T00:08:10Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-13T00:08:10Z ⏳ IN FLIGHT — Investigation done: pulled #2624 body/dispatch comment,
  confirmed #2623 (design export) landed and #3677 (shell) landed. Verified #3032 (target-role
  ceiling) is fixed on `main` (commit `fcb16b522`, real live-DB regression test
  `msp-settings-target-role-ceiling.live-db.test.ts`) for the 5 credential routes — the design's
  own "no route checks the target's role" framing is now stale for those 5 and must not ship as
  a UI-only guard. Found the session-revoke route (`DELETE /msp/settings/users/:userId/sessions`)
  was NOT included in the #3032 fix (commit message explicitly lists only the 5) — still a real
  target-ceiling gap, to be filed as a sibling finding under #2562. Read the full contract pack
  (`docs/msp-console/account-security-msp-console-contract-pack.md`) and design export
  (`Design/MSP_Console/design_handoff_msp_console/Account Security.dc.html`), and the README's
  "embedded contract" section (screen 39, mounts under Operations, `embedded`/`forceEmpty`
  disappear in the real rebuild per the README itself). Next: implement the module + wire it into
  the console tree/shell, extend `GET /api/msp/audit` with entityType/entityId filters for a real
  per-user audit trail, extend `GET /msp/settings/users` to return `mfaEnforced`.
