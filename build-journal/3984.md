# #3984 — Page-level + global bug status icon, combined, in the per-tab browser toolbar

- **Status:** ✅ DONE 2026-09-14T03:05:38Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** 853fa1613 (merged to origin/main)

## Log
- 2026-09-14T03:01:33Z ⏳ IN FLIGHT — Blockers #3978 (closed, schema migrated locally) and #3981 (labeled `complete`, commit 33d01aa5f on origin/main) both cleared. Located the real per-tab browser toolbar: NOT XAML, built in code-behind per tab inside `OpenWebTab(...)` (`MainWindow.xaml.cs` ~line 3134), the `navBar` Grid (~lines 3242-3388) with Back/Forward/Refresh/URL/Go/Fill-Login. The "lock/account-dropdown from #3921/#3922/#3923" is `btnFillLogin` (key glyph ``, "Fill Login — show autofill profile picker") + its `autofillOverlay` popup, listing `BuildConsoleSettings.UserAccounts` (#3922). One toolbar instance per tab, closing over that tab's own `wv`/`url` — tabs opened via `OpenChatTab`/other non-browser tab helpers don't have this toolbar.
  #3983 (sibling: element-level bug icons in the DOM inspector popover) is a live concurrent build right now, not yet landed (only its IN FLIGHT bookend commit exists) — its issue body also depends on reusing #3984's detail-view component, i.e. mutual "reuse the other one" instructions with neither built yet. Building a generic, reusable `BugScopeHistoryWindow` now (new files `BugScopeHistoryWindow.xaml`/`.xaml.cs`) that takes any `IReadOnlyList<VisualTestTrackerEntry>` + a scope description, so whichever of #3983/#3984 lands second can point at this one instead of building a second — noting this explicitly here since the file-creation order is a real race, not a design choice.
  Added `VisualTestTrackerStore.GetToolbarBugSummaryAsync(baseUrl, pagePath)`: page-level query (`page_id = <read-only-looked-up id> AND selector IS NULL`) + global query (`page_id IS NULL AND base_url = @b`), combined newest-first. Uses its own `ReadScopedEntry` reader (not the shared `ReadEntryFromReader`, which does an unguarded `GetInt32` on `page_id` ordinal 2 — the real NULL-page_id bug already filed as #3991 by the #3981 session) so a genuinely-NULL `page_id` on a global row never throws.
- 2026-09-14T03:05:38Z ✅ DONE — Wired the icon into `OpenWebTab`'s `navBar` (new column 7, next to `btnFillLogin`): a colored dot (`StatusVisual.For`, shared with the detail window — Open=yellow, Verifying=sky, Closed+Fixed=green, Closed+NotABug=gray/overlay) + `(n)` count badge, hidden until a scoped bug actually exists for the tab's matched base URL. Refreshes on every `wv.NavigationCompleted` via the same `MatchesWatchedVisualTestBaseUrl`/pagePath-extraction logic the Visual Test Tracker floaty already uses (Git #1472) — one instance per tab, closing over that tab's own `wv`, matching how the rest of this toolbar (`btnBack` etc.) is already built per-tab in code-behind, not shared XAML. Click opens the new `BugScopeHistoryWindow` (borderless floaty, same chrome pattern as `SessionHistoryDialog`) listing every matching bug newest-first: status badge, resolution+reason once closed, clickable GitHub issue link (opens `https://github.com/shanemccaw/Shane-McCaw-MSP/issues/<n>`), closing build id, created/updated timestamps.
  **Live Postgres verification** (real INSERTs inside a transaction, `ROLLBACK`ed at the end — table back to its exact prior state): created a page row (`zz-test.example.com/dashboard`), inserted a page-level bug (`page_id` set, `selector` NULL, status `Open`) and a global bug one minute later (`page_id` NULL, `base_url` set, status `Verifying`). Ran the exact two `GetToolbarBugSummaryAsync` queries directly — page-level query returned only the page-level row, global query returned only the global row, and the combined+sorted result put the newer global `Verifying` row first, page-level `Open` second — exactly the display order and status the UI code computes. Confirms the "shows combined on the matching page" and implicitly "shows global-only on a different page of the same site" cases (the page-level query is scoped to an exact `page_id`, so a different `page_path` naturally returns zero page-level rows and only the global set).
  `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj`: clean, 0 warnings, 0 errors (one real compile fix mid-build: a `??=` with an immediately-invoked lambda on the right-hand side doesn't parse in C# — rewrote as a plain `if (x == null) { ... }` block).
  No fixture/hardcoded data — every bug shown comes from a real `visual_test_tracker_entries` row via a live query.
  **Real finding, not filed as a new issue** (already covered by an existing one): confirmed live that #3983 (element-level bug icons, parented under #3977) is a genuinely concurrent, not-yet-landed build in worktree `C:\wt\3983-q2665` (only its IN FLIGHT bookend commit exists as of this session) — its issue text also says to reuse #3984's detail-view component, so whichever of the two lands second should point its click-through at `BugScopeHistoryWindow` (in `desktop/BuildConsole/BugScopeHistoryWindow.xaml`/`.xaml.cs`) rather than building a second one. No new issue needed since #3983 already exists and already carries that instruction both ways.
  `verify-branch-merged.mjs`: OK, `853fa1613` merged into `origin/main`. `git status --porcelain`: clean (nothing else touched).
