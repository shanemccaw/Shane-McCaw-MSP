# #3909 — Retention Queue design/contract-pack premise is stale

- **Status:** ✅ DONE 2026-09-13T04:44:09Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 5ae7f0a6b (rebased onto main as 04ec3e46c59a00d113bb7215944dde4732589232)

## Log
- 2026-09-13T04:34:03Z ⏳ IN FLIGHT — Confirmed live: `registerRetainedRecordType({ recordType: "msp_poams" })` in `artifacts/api-server/src/lib/retention/wiring/msp-poams.ts:47`; `softDelete()` called from `routes/msp-poams.ts:695` and `routes/portal-poams.ts:644`; `requestAcceleration()` from `routes/portal-poams.ts:721`. `SELECT count(*) FROM record_deletions` in local Postgres → still 0 rows. About to regenerate `docs/msp-console/retention-queue-msp-console-contract-pack.md` §4 and refresh `Design/MSP_Console/design_handoff_msp_console/Retention Queue.dc.html`'s isLive empty-state copy, then check `artifacts/msp-console/src/modules/retention/RetentionQueue.tsx` for matching copy.
- 2026-09-13T04:44:09Z ✅ DONE — Rewrote the contract pack's abstract, §4 (finding renamed from "structurally unreachable" to "genuinely reachable, currently empty"), §5's record-type vocabulary note, §6's honest-empty note, §7's cross-surface-edges note, and the Orphaned-endpoint check (a real frontend caller — `RetentionQueue.tsx`, Git #3817 — now exists). Refreshed `Retention Queue.dc.html`'s isLive empty-state heading/body, the `blockers` tile values/notes (now `msp_poams` / `msp-poams.ts · portal-poams.ts` / `portal-poams.ts` instead of "None"), the shape-view "Not live data" note, and the shared `notes` list's two entries that repeated the stale "no row can ever reach this queue" claim. Confirmed `artifacts/msp-console/src/modules/retention/RetentionQueue.tsx` (Git #3817) already carried the corrected reasoning verbatim in its own header comment and empty-state copy — no code change needed there. `record_deletions` remains 0 rows in local Postgres (re-confirmed live before commit) — the honest empty state is preserved, only the reasoning behind it changed. Working tree clean; branch verified merged into origin/main via `verify-branch-merged.mjs`. No findings filed — this was a documentation-only correction, no new bug discovered.
