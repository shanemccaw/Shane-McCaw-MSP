# Build Journal — #4279

- **Status:** ✅ DONE 2026-09-15T22:59:00Z
- **Scope:** platform (shanemccaw/MyArchitect deploy-myarchitect.cmd)

## Log

- 2026-09-15T22:57:07Z — Pulled #4279's full body + both dispatch comments (later comment
  added `--blocked-by 4278,4284`; #4278 was replaced by #4305 which landed, #4284 also
  landed and closed — both blockers clear). Read `deploy-myarchitect.cmd`,
  `deploy-myarchitect-notify.ps1`, `MyArchitect.csproj`, `Directory.Build.targets` directly
  in the local `shanemccaw/MyArchitect` clone at `C:\wt\ma-work`. Findings: (1) the
  pnpmfile-checksum check calling
  `node "%PROJECT_DIR_NOSLASH%\..\..\scripts\dev-server\check-pnpmfile-checksum.mjs"` is
  genuinely monorepo JS tooling (checks `.pnpmfile.cjs`/`pnpm-lock.yaml` drift) with no
  relevance to this standalone C#/WPF repo — removing it per the issue's own guidance
  rather than repointing it; (2) `origin` remote in the local clone already points at
  `https://github.com/shanemccaw/MyArchitect.git` (confirmed via `git remote -v`) — no
  local-machine step needed, already correct; (3) `MyArchitect.csproj` and
  `Directory.Build.targets` were already updated for the extracted repo as part of #4305 —
  no further monorepo-relative paths found in those two files.
- 2026-09-15T22:59:00Z — ✅ DONE — Removed the pnpmfile-checksum check block from
  `deploy-myarchitect.cmd` (7 lines) in `shanemccaw/MyArchitect` — real commit
  `25c1c9241b05d905f34c6de176fe94a8932f6646` ("Remove monorepo-relative pnpmfile-checksum
  check from deploy-myarchitect.cmd (#4279)"), pushed directly to `main` in
  `shanemccaw/MyArchitect`. Confirmed post-fix: `grep -n "pnpmfile\|scripts.dev-server"` over
  both `deploy-myarchitect.cmd` and `deploy-myarchitect-notify.ps1` returns no matches;
  `git remote get-url origin` in the local clone already read
  `https://github.com/shanemccaw/MyArchitect.git` before and after — no local-machine
  re-point step was needed. No other monorepo-relative paths found in either shell script.
  No source/UI changes, so no `dotnet build` run — item 3's "any other monorepo-relative
  path" search covered both files named in the issue plus csproj/targets while
  investigating. Working tree clean in both repos (`git status --porcelain` empty), leftover
  temp stash entry used to reorder around a concurrent fast-forward was dropped after
  reapplying.
