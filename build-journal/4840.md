# #4840 — Security Defaults flag on CA sibling checks

- **Status:** ✅ DONE 2026-09-19T05:19:44Z
- **Scope:** platform (api-server)
- **Started:** 2026-09-19
- **Commit(s):** `49d297fc4` (fix), verify script commit on main after it. Plan: [4840-plan.md](4840-plan.md)
- **Findings filed:** none

## Log
- 2026-09-19 ⏳ IN FLIGHT — plan written.
- 2026-09-19T05:19:44Z ✅ DONE — two of three checks stamped; third has no gate (see below).

## Changes Made
`monitor-executor.ts` gate-skipped block: `identity:ca-legacy-auth-block` and `identity:ca-mfa-coverage` now stamp `securityDefaultsEnabled: true` (flag only; ca-policy-count unchanged). **`identity:ca-device-compliance` was deliberately not changed:** `monitor_checks` has no gate_endpoint/gate_expression for it, so it has no gate-skipped path and a stamp would be dead code.

## Verification Results
Live against tenant c4c814d4 (tenants.id 2080), `verify-ca-siblings-security-defaults-4840.ts`, persistProfile false:
```
identity:ca-legacy-auth-block: status=ok _gateSkipped=true securityDefaultsEnabled=true
identity:ca-mfa-coverage: status=ok _gateSkipped=true securityDefaultsEnabled=true
identity:ca-device-compliance: status=license_gap (no gate; needs Entra P1/P2 + Intune)
```
Not run end-to-end: the SIGNALS card itself. The reason mapping was confirmed by reading `pillar-signals.ts:154` (`securityDefaultsEnabled === true` → `security_defaults_active`, checked before `gate_skipped`). Stored rows update on the next real scan.
