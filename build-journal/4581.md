# #4581 — Wire licence uplift cost into Portal's Licensing Pillar narrative generator

- **Status:** ✅ DONE 2026-09-18T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** a3e17200e

## Log
- 2026-09-17T00:00:00Z ⏳ IN FLIGHT — read #4580's real `resolveLicenseUpliftCost` implementation, mapped `LICENSE_GAP_CATEGORIES` SKUs to real `sku_price_reference` part numbers (AAD_PREMIUM_P2, ATP_ENTERPRISE; Purview Suite has none on file), designed an extra-stats hook on the shared `pillar-report-narrative.ts` loop, about to wire it into the Cost Waste Summary section.
- 2026-09-18T00:20:00Z ✅ DONE — added an optional `resolveExtraStats`/`extraStatsQualifier` hook to `pillar-report-narrative.ts` (the shared loop five reports use), a `resolveLicenseUpliftStats` resolver in `licensing-alignment-narrative-generator.ts` that prices each of a tenant's own gapped `LICENSE_GAP_CATEGORIES` via #4580's `resolveLicenseUpliftCost`, and folded the result into the Cost Waste Summary section with three honest unavailable reasons (`no_sku_mapped`, `no_price_on_file`, `no_license_snapshot`) — never a guessed figure. Loosened the report's `NO_SKU_RECOMMENDATION_RULES` prompt with one narrow carve-out for this grounded figure only. Mid-session rebase collided with a concurrent build (#4316, "Wire resolveLicenseUpliftCost into Free Scan SOW Commercial Terms") which had independently added the identical `skuPartNumber` field to `license-gap-purchase-links.ts` with the same three SKU values (AAD_PREMIUM_P2 / ATP_ENTERPRISE / null for Purview) — resolved by keeping #4316's version (theirs: `skuPartNumber: string | null`, required) and dropping my duplicate. Added 7 new unit tests (`licensing-alignment-narrative-generator.test.ts`) covering all three unavailable reasons, the real-price path, and multi-category tenants; full existing suite for the touched files (142 tests) still green. `tsc --noEmit` clean except 6 pre-existing unrelated failures in `ca-hold-window-scan.test.ts`. Verified merged into origin/main (`verify-branch-merged.mjs` OK) and working tree clean before writing DONE.
