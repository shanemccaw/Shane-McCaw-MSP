# #3911 — RBAC test account reset (tenant mccawsoft2)

- **Status:** ✅ DONE 2026-09-13T02:43:17Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 158bd80d3 (script), 50ea412e1 (IN FLIGHT bookend) — pushed to main as 6533bea80

## Log
- 2026-09-13T02:34:54Z ⏳ IN FLIGHT — Investigation complete: confirmed `DATABASE_URL` resolves to
  `localhost:5432/shanemccawmsp` (local dev, not production); resolved tenant `mccawsoft2` to
  `tenants.id = 1` (`msp_id = 1`, Entra tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3`,
  `is_testbed = true`) via `tenants.tenant_id`. Confirmed live (not from migration snippets) that
  the current MSP-side ladder enforced by `users_role_scope_check` is the 6-rung post-#3590 set —
  `Free, Customer, ServiceAccount, MSPOperator, MSPAdmin, PlatformAdmin` — resolving #3839's open
  question for this task: the CHECK constraint only accepts these six; `CustomerUser`/`Assessment`
  are retired spellings that exist only as legacy rows in the `customer_roles`/`msp_roles`
  catalogs, not valid `users.msp_role` values. Confirmed `users` is the single unified table for
  both MSP-side and customer-side accounts (no separate `customer_users` table) — the customer-side
  "roles" (`customer_roles`/`customer_user_roles`) are additional grants layered on top of a
  Customer/Free-tier `users` row, not a second account table. Read the live
  `rbac_sync_user_roles()` trigger function to confirm exactly which grants auto-sync from which
  columns. About to run `artifacts/api-server/scripts/reset-rbac-test-accounts-3911.mjs` (new
  script, reuses the `randomBytes`/`bcrypt`/real-`pg`-connection pattern from
  `artifacts/mcp-server/scripts/e2e-create-account.mjs`) — dry-run first, then for real.
- 2026-09-13T02:43:17Z ✅ DONE — Ran `reset-rbac-test-accounts-3911.mjs` for real against local dev
  (`localhost:5432/shanemccawmsp`), tenant `mccawsoft2` (`tenants.id = 1`). **Deleted** 7 stale
  test/temp accounts scoped to that tenant: `shanemccaw+assessment1@outlook.com` (id 37),
  `shanemccaw+buyassessment@outlook.com` (39), `shanemccaw@outlook.com` (42), `test@test.com` (50),
  `shanemccaw@gmail.com` (55), `shanemccaw+premier@outlook.com` (57),
  `shanemccaw+1317pack@gmail.com` (103) — each had FK-dependent rows in `contracts` (2),
  `notifications` (5), `client_services` (2), `live_document_shares` (4), `password_reset_tokens`
  (1), swept via the script's iterative FK-sweep (discovered live from `information_schema`, not
  guessed). `shane@shanemccaw.com` (id 1) confirmed byte-identical before/after — never touched,
  excluded by literal email match in the DELETE's own WHERE clause. **Created** 11 new accounts,
  one per real, currently-defined RBAC role confirmed live against this database: the 6-rung
  MSP-side ladder (`shanemccaw+free@outlook.com` id 2756 · Free, `+customer` 2757 · Customer,
  `+serviceaccount` 2758 · ServiceAccount, `+mspoperator` 2759 · MSPOperator, `+mspadmin` 2760 ·
  MSPAdmin, `+platformadmin` 2761 · PlatformAdmin) plus the customer-side platform-default roles
  layered on a Customer-tier account (`+customer-admin` 2762 · `customer_roles(customer-admin)`
  membership, `+billing` 2763 · `customer_roles(billing)` membership, `+cap.team.manage` 2764 ·
  `users.can_manage_team=true` + membership row, `+cap.changes.approve` 2765 ·
  `users.can_approve_changes=true`, auto-synced to `customer_user_roles` by the live
  `rbac_sync_user_roles` trigger) and the one msp-side capability-column role beyond the ladder
  (`+cap.purchases.approve` 2766 · MSPOperator-tier + `users.can_approve_purchases=true` +
  `msp_roles(cap.purchases.approve)` membership, granted manually since the sync trigger only ever
  revokes this row, never grants it). Passwords are real CSPRNG output (`node:crypto randomBytes`),
  bcrypt-hashed at cost 12 in the DB — never written as plaintext to any table. **Verified**: every
  one of the 11 accounts' password bcrypt-compares true against its own stored hash, AND a real
  HTTP `POST /api/auth/login` against the live local dev api-server (`localhost:8080`) returned
  `200` with a real JWT for all 11. Plaintext credentials written to BuildConsole's own
  `%AppData%\BuildConsole\settings.json` → `TestEnvironmentVariables`, as 22
  `TEST_RBAC_<ROLE>_EMAIL` / `TEST_RBAC_<ROLE>_PASSWORD` pairs (namespaced `RBAC_` to avoid
  colliding with the pre-existing generic `TEST_ADMIN_*`/`TEST_CUSTOMER_*`/`TEST_PORTAL_*` pairs
  already there) — plaintext lives only there, nowhere else. No genuine bug/finding surfaced during
  this build (the pre-existing #3839 dual-role-key state was confirmed and resolved for this task's
  purposes via the live `users_role_scope_check` constraint, not a new finding). Branch verified
  merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs` — OK, 6533bea8).
  Working tree clean (`git status --porcelain` empty) before this line was written.
