# #3724 — Context gauge token estimate is structurally blind to system overhead and MCP tool-schema cost

- **Status:** ✅ DONE 2026-09-12T02:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 686d35638 (IN FLIGHT bookend), 424edc12c (real work), this commit (DONE bookend)

## Log
- 2026-09-12T02:05:00Z ⏳ IN FLIGHT — Resuming after #2813 closed (confirmed: `dependencies/blocked_by`
  on #3724 is now `[]`; `in-flight` label re-applied, stale edge to #2813 already removed in the
  prior worktree's session before this one started — see the q2381 worktree's earlier BLOCKED
  bookend attempt, which never reached `main` because it was pushed to its own agent branch instead
  of `main`; that branch's commit is superseded by this file). Pulled all 4 real comments on #3724
  in order — the last one (posted 2026-09-12T01:54:23Z, "tool content undercounted") supersedes the
  prior dispatch on scraper scope: it adds a real, sharper finding (claude.ai's DOM may render
  tool-use/tool-result content separately from message bubbles, so the scraper's
  `[role="article"][aria-posinset]` walk plus `innerText`-first read may skip most of a tool-heavy
  session's real content) and revises the task list accordingly. Real repro evidence already exists
  in `activity-latest.log`: convId=adb43bd0-6bab-4ab7-8ab8-67ec02d06deb (chat #1202, 16 messages,
  tool-heavy per Shane's own comment) persisted `estTokens=2017` — ~126 tokens/message, implausible
  for a session Shane describes as heavy MCP tool use, corroborating the undercount without needing
  a new live DOM inspection.
  **Honesty note on live-DOM confirmation:** this session has no browser/DOM-inspection capability —
  checked `AGENT_PROTOCOLS.md`'s full real surface (`executeSql`, `runTest`, `executeScan`,
  `executeCmdlet`, `reportProgress`); none reads an arbitrary open tab's DOM. I cannot literally
  confirm live whether claude.ai renders tool blocks as a separate element type. The fix below is a
  mechanically-justified improvement (switch to `textContent`, which includes CSS-hidden/collapsed
  content that `innerText` excludes — a real, general DOM API fact, not contingent on knowing
  claude.ai's exact class names) plus a defensive best-effort selector net for common tool-block
  patterns, clearly flagged as unverified against live markup. Filing this as a genuine finding for
  real live verification as follow-up (see Findings below).

- 2026-09-12T02:20:00Z ✅ DONE — Real code shipped, commit `424edc12c` on `main`:
  1. **`ChatContextMeterScript.cs`** — text extraction switched to `textContent`-first (mechanically
     captures CSS-hidden/collapsed content `innerText` excludes — real DOM-API fact, not contingent
     on knowing claude.ai's exact markup); added per-key word-count tracking; added a separate
     heavy-turn (code/JSON-flagged) char accumulator; added a defensive, best-effort selector pass
     (`[data-testid*="tool" i]`, `[data-testid*="mcp" i]`, `[aria-label*="tool" i]`) for tool content
     that might render OUTSIDE the counted `[role="article"]`, guarded by `el.closest(...)` against
     double-counting the nested case.
  2. **`ChatContextMeterStore.cs`** — `Entry` gains `WordCount`/`HeavyCharCount` (default 0, backward
     compatible with existing persisted `context-meter-highwater.json`).
  3. **`MainWindow.xaml.cs`** — replaced flat `charCount / 4.0` with a documented word/char-blended
     prose estimate (`Math.Max(charCount/4.0, wordCount/0.75)`) plus a denser ratio (`/3.0`) applied
     only to the heavy-content excess over its "fair share" of the prose baseline (avoids
     double-counting since word count isn't split heavy/normal in JS). Still an approximation, not
     real BPE tokenization — documented as such in the code.
  4. **`ChatContextOverheadEstimator.cs`** (new) — `SystemPromptOverheadTokens = 15_000`, documented
     as the issue's own cited external reference-design estimate (context-coach), NOT a measurement
     (BuildConsole has no way to read Anthropic's real system prompt from a WebView2). Plus
     `GetMcpToolSchemaOverhead()`, computed from `mcp-tool-inventory.json` (new): REAL tool
     name+description text for `shanes-git`'s actual 24-tool surface (extracted verbatim from
     `BuildConsole-Chat-Tools-Reference.md`, not invented), extrapolated by real average
     tokens/tool for `Shanes-Life-Production`'s known-but-undocumented 92-tool count (that count is
     Shane's own direct observation, recorded in the issue body — not fabricated; only its per-tool
     schema text is genuinely unavailable in this codebase, so it's extrapolated, clearly flagged
     `IsExtrapolated=true`). Re-reads the JSON file on every gauge tick — "refreshed when the tool
     list changes" means "edit the file, next tick reflects it," since there is no live push signal
     for a claude.ai account's connector state (confirmed: nothing in this codebase can enumerate it).
  5. **`ChatDocumentContainer.xaml(.cs)`** — `UpdateGauge()` replaced the flat opaque
     `FixedOverhead = 40_000` with `systemPromptOverhead + mcpOverhead.TotalTokens`, and the
     displayed `CtxGauge.Text` now reads `"{conv} conv + {ovh} ovh / {budget} ctx"` instead of one
     folded number, with a full itemized breakdown (conversation / system prompt / each MCP
     server's real-vs-extrapolated tokens / draft / total) in `CtxGauge.ToolTip`.

  **Real verification performed (task item 5):** `dotnet build BuildConsole.csproj` — 0 errors, 10
  pre-existing unrelated warnings. Could not launch BuildConsole.exe itself to verify live (per the
  standing rule against an agent session launching it) and could not open a real claude.ai chat
  from this session (no browser). Instead loaded the built `BuildConsole.dll` in-process (PowerShell
  `Add-Type`/reflection, forcing `BuildTrackerConfig`'s cached repo-root to this worktree's real path
  since the ad-hoc host has no `App.OnStartup` to do it — a testing-harness artifact, not a
  production code path) and called the REAL, compiled
  `ChatContextOverheadEstimator.GetMcpToolSchemaOverhead()` against the REAL
  `mcp-tool-inventory.json`. Actual output: `shanes-git` (real, 24 tools) = 590.8 tokens,
  `Shanes-Life-Production` (extrapolated, 92 tools) = 2,264.5 tokens, MCP total = 2,855.3 tokens;
  plus the 15,000 system-prompt baseline = **17,855 tokens of real, non-trivial, non-placeholder
  overhead** — versus 0 tokens of MCP overhead ever counted before this build. That is a real,
  substantial number confirming the estimator computes real values from real data, not a stub.

  **Honesty on what's measured vs. approximated (as the issue explicitly asked):**
  - REAL/measured: the conversation-text char/word counts themselves (once #2813's persistence
    fix landed), and the MCP tool-schema token count for `shanes-git` specifically (real doc text).
  - DOCUMENTED APPROXIMATION, not measured: the system-prompt baseline (15K, external citation),
    the word/char-blended token ratios (4.0/0.75/3.0 — commonly-cited rough averages, not this
    project running Claude's actual tokenizer), and `Shanes-Life-Production`'s extrapolated overhead
    (real tool COUNT, extrapolated per-tool cost).
  - NOT CONFIRMED: whether claude.ai's DOM genuinely renders tool-use/tool-result content
    separately from message bubbles — no live DOM-inspection capability exists from this build
    session (checked `AGENT_PROTOCOLS.md`'s full real surface). Filed as **#3749** (sibling under
    Feature #3723, milestone v1.1, label `bug`, board status "AI Batter Up") rather than silently
    presenting the defensive selector-pass fix as confirmed.

  **`git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/3724-q2393` (424edc12)
  is merged into `origin/main`.** No manual SQL migration in this build (pure BuildConsole/WPF
  change) — nothing to append to #1630. Findings filed: **#3749**.
