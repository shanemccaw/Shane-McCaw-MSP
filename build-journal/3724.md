# #3724 — Context gauge token estimate is structurally blind to system overhead and MCP tool-schema cost

- **Status:** ⏳ IN FLIGHT 2026-09-12T02:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-12T02:05:00Z ⏳ IN FLIGHT — Resuming after #2813 closed (confirmed: `dependencies/blocked_by`
  on #3724 is now `[]`; `in-flight` label re-applied, stale edge to #2813 already removed in the
  prior worktree's session before this one started — see the q2381 worktree's earlier BLOCKED
  bookend attempt, which never reached `main` because it was pushed to its own agent branch instead
  of `main`; that branch's commit is superseded by this file). Pulled all 4 real comments on #3724
  in order — the last one (posted 2026-09-12T01:54:23Z, "tool content undercounted") supersedes the
  prior dispatch on scraper scope: it adds a real, sharper finding (claude.ai's DOM may render
  tool-use/tool-result content separately from message bubbles, so the scraper's
  `[role="article"][aria-posinset]` walk plus `innerText`-first read may skip most of a tool-heavy
  session's real content) and revises the task list accordingly. Real repro evidence already exists
  in `activity-latest.log`: convId=adb43bd0-6bab-4ab7-8ab8-67ec02d06deb (chat #1202, 16 messages,
  tool-heavy per Shane's own comment) persisted `estTokens=2017` — ~126 tokens/message, implausible
  for a session Shane describes as heavy MCP tool use, corroborating the undercount without needing
  a new live DOM inspection.
  **Honesty note on live-DOM confirmation:** this session has no browser/DOM-inspection capability —
  checked `AGENT_PROTOCOLS.md`'s full real surface (`executeSql`, `runTest`, `executeScan`,
  `executeCmdlet`, `reportProgress`); none reads an arbitrary open tab's DOM. I cannot literally
  confirm live whether claude.ai renders tool blocks as a separate element type. The fix below is a
  mechanically-justified improvement (switch to `textContent`, which includes CSS-hidden/collapsed
  content that `innerText` excludes — a real, general DOM API fact, not contingent on knowing
  claude.ai's exact class names) plus a defensive best-effort selector net for common tool-block
  patterns, clearly flagged as unverified against live markup. Filing this as a genuine finding for
  real live verification as follow-up (see Findings below).
