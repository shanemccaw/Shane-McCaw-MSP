# Build Journal — #3585

- **Status:** ✅ DONE 2026-09-12T02:50:00Z
- **Scope:** buildconsole
- **Posted:** 2026-09-12T02:50:00Z

## Log

- 2026-09-10T23:05:00Z — First pass: #3582/#3584 both still OPEN, real `blocked_by` edges already
  wired, applied `blocked` label, stopped (see git history on `agent/3585-q2260` for that commit;
  this worktree was later reprovisioned and that commit lives only on
  `rescued/3585-q2260-2026-09-12T01-26-26-297Z`, not in this checkout's history).
- 2026-09-12T02:32:00Z — Resumed. Re-checked #3582/#3584: both CLOSED, labeled `complete`. Removed
  the stale `blocked_by` edges (both targets closed), removed `blocked` label, re-applied
  `in-flight`, posted a resuming comment on #3585.
- 2026-09-12T02:50:00Z — Real verification pass, no code changes (none needed — nothing widened).
  Traced every real call site with concrete evidence:
  - `desktop/BuildConsole/Services/GitHubIssueMirror.cs` — every `bt_issue_mirror`/
    `bt_milestone_mirror` read/write (including `TryGetBoardIssuesAsync`, the one Git Board's
    `LeftSidebar.PopulateGitTrackerBoardAsync` calls first) is explicitly bound to
    `repo_owner = @owner AND repo_name = @repo` using `RepoIdentity.DefaultOwner`/`DefaultName`
    (`"shanemccaw"`/`"Shane-McCaw-MSP"`, the Main repo) — see line ~488-501's own Git #3579 comment:
    "the Git Board stays scoped to this repo (Main-tier repo, per Feature #3578's own design) —
    bound explicitly."
  - `desktop/BuildConsole/Services/GitHubApiClient.cs` — `ListBoardIssuesAsync`/
    `ListBoardIssuesInternalAsync` (the live-GitHub fallback when the mirror isn't warm, line
    ~1096-1160) issues a GraphQL query hardcoded to a single `repository(owner: Owner, name: Repo)`
    where `Owner`/`Repo` (line 268-269) read `BuildConsoleSettings.GitHubOwner`/`GitHubRepoName`
    — a scalar setting deliberately kept separate from the multi-repo `ConfiguredRepos` list
    (confirmed in `RepoIdentity.cs`'s own doc comment). Never touches `IsConfiguredRepo`/
    `GetAllConfiguredRepos()`.
  - `desktop/BuildConsole/Services/HomeEtaProjectionService.cs` — its milestone/epic resolution
    delegates entirely to `GitHubIssueTimeSeriesService`, which itself reads ONLY through
    `GitHubIssueMirror.TryGetBoardIssuesAsync`/`TryGetMilestoneInfosAsync` (same Main-repo-scoped
    queries above) — confirmed no direct SQL or live GitHub call bypasses that scoping.
  - Repo-wide grep of every call site of `GetAllConfiguredRepos()`/`GetMainTierRepos()`/
    `GetTinkerTierRepos()`/`IsConfiguredRepo`: confined to `SettingsTabView.xaml.cs` (#3581's
    Settings UI) and `BatterUpQueueService.cs`/`AiBatterUpQueueService.cs` (#3582's merged Batter
    Up/AI Batter Up, plus the one `GitHubApiClient` ProjectV2-items-scan helper that backs those
    two services specifically, gated on `BatterUpProjectId`, never on Git Board's own
    `repository(owner:,name:)` query). Zero overlap with Git Board/Home/epic-tree code paths.
  - #3584's actual diff (`git show 4a2dda0c6 --stat`) is entirely `scripts/dev-server/*.mjs`
    (worktree/dispatch provisioning) — no touch to any `desktop/BuildConsole` C# display surface,
    so it carries no widening risk for this issue's scope at all.

  **Conclusion: nothing widened. Git Board, Home dashboard, and the epic/milestone tree remain
  correctly scoped to the Main repo only**, exactly as designed. No separate bug filed — there is
  nothing to file.

  **One honest gap, not a bug:** the issue's own "Real verification" step asks to configure a real
  Tinker-tier repo and confirm live zero-cross-contamination. Checked `settings.json`'s
  `ConfiguredRepos` — only the Main repo (`shanemccaw/Shane-McCaw-MSP`) is currently registered;
  no second repo has been added via #3581's Settings UI yet. Registering one wasn't done in this
  session — that's a real state change to Shane's live repo registry, not something to do
  unilaterally inside a low-effort verification pass. The source-level evidence above is complete
  and conclusive on its own (every relevant call site traced with file:line), but the live
  "add a Tinker repo, watch Git Board ignore it" smoke test remains for whenever a second repo is
  actually registered.

No findings filed. No code changed. Branch verified merged (this session made no new commits
requiring push beyond this bookend).
