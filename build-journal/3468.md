# #3468 — ResolveActiveMilestoneAsync mirror-first migration + stale comment fix

- **Status:** ✅ DONE 2026-09-10T03:44:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 270c2bba7acb7138261d33b72d1e585a9f4f954c (rebased onto main as 29db146d58defab9f8699cbc45b2d413f5a970a7)

## Log
- 2026-09-10T03:24:22Z ⏳ IN FLIGHT — investigated `GitHubIssueTimeSeriesService.ResolveActiveMilestoneAsync` (:465-491, live `GetMilestonesAsync` every call, no mirror/cache) and `GitHubIssueMirror.TryGetMilestoneInfosAsync` (#3358, `bt_milestone_mirror`-backed). About to migrate `ResolveActiveMilestoneAsync` to read the mirror first, falling back to live `GetMilestonesAsync` on a mirror miss (matching #3358/#3359's pattern), and correct the two stale "already TTL-cached" comments in `MainWindow.xaml.cs:1100-1102` and `EditorPanesStatsService.cs:32-38`.
- 2026-09-10T03:44:00Z ✅ DONE — `ResolveActiveMilestoneAsync` now reads `GitHubIssueMirror.TryGetMilestoneInfosAsync()` first (same open+total-issue "most active" selection logic applied to the mirrored `GitHubMilestoneInfo` rows), falling back to the live `GetMilestonesAsync` path only on a mirror miss. Corrected the two stale comments at `MainWindow.xaml.cs:1100-1102` (editor-panes 1-minute timer) and `EditorPanesStatsService.cs:32-38` that falsely claimed this path was already 5-minute-TTL cached. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. Grep-verified: `ResolveActiveMilestoneAsync` reads `GitHubIssueMirror` before constructing a `GitHubApiClient` (per the issue's own verification criterion). No new findings beyond what #3468 already documented; no fixture/hardcoded data added. Branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain` clean.
