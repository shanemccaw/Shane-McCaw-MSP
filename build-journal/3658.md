# #3658 — Port Build Matrix (8-slot agent grid) from ShaneBuilder into BuildConsole

- **Status:** ✅ DONE 2026-09-11T14:56:32Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 6afcf97e3

## Log
- 2026-09-11T14:46:40Z ⏳ IN FLIGHT — starting: audit ShaneBuilder's BuildMatrixPanel source and BuildConsole's real GetActiveUsageSummary/QueueWatcherService slot-tracking data structure before building the UI.
- 2026-09-11T14:56:32Z ✅ DONE — Real audit found the actual data structure backing GetActiveUsageSummary: QueueWatcherService's private `_running` dictionary (int queue-item id → RunningEntry). Added a new public accessor `GetRunningBuildIds()` exposing that same real membership (not a re-derived parallel set off the DB Status column). Built the Build Matrix drawer in BuildQueuePanel (a collapsible chip in the QUEUE header): up to `MaxConcurrent` (real configured cap, default 8) slot cards, busy/idle, pulsing while busy, real issue#/title/build set/model, live status pill (RUNNING/NEEDS INPUT/PAUSED sourced from GetInteractiveState — same vocabulary GitDetailView's own chat pill uses), stable per-build slot assignment across re-renders (mirrors ShaneBuilder's own `_matrixSlotAssignments` algorithm). Clicking a busy slot calls the existing `RevealQueueItem` (#3599's real cross-filter navigation) to focus that build in the queue below. Added `MainWindow.BuildMatrixTab.cs` for the "Tab" action — freezes the current slot state into its own read-only document tab, same open-or-focus recipe as Settings/Batter Up tabs, refreshing (not duplicating) on re-send. No fixture data — idle slots render honestly idle. Commit 6afcf97e3, pushed to origin/main and confirmed merged (`verify-branch-merged.mjs` exit 0). Verified via clean isolated `dotnet build` (0 errors, only pre-existing unrelated warnings) and a full diff review (pure additions, 4 files, no incidental edits) — BuildConsole.exe itself was not launched, per the standing hard rule against launching it for verification from a build session. Working tree confirmed clean (`git status --porcelain` empty) before this bookend.
