# #2356 — Batter Up rail panel shell: lane switch (All / Batter Up / AI Batter Up / Ask Shane) + live counts

- **Status:** ✅ DONE 2026-09-02T01:00:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** dd100b551

## Log
- 2026-09-02T00:00:00Z ⏳ IN FLIGHT — real audit done: MainWindow.xaml.cs's BatterUp rail source is
  in the shared `notBuilt` placeholder list (~line 456); no panel content exists. ChatGitHubFilter
  has `GetBoardStatusAsync`/`GetBoardStatusesAsync` (issue→status, #2410) but no reverse
  status→items query. Board Status option ids confirmed live via `gh api graphql`: Batter Up
  `09b1927f`, AI Batter Up `a0296971`, Ask Shane `404998bb` (field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0`,
  project `PVT_kwHOEiBDdc4BeoiY`). Board is ~2,403 items total. Building: `ChatGitHubFilter.GetBatterUpLaneCountsAsync()`
  (backward `items(last:100, before:cursor)` walk per BuildConsole's established
  `ScanProjectItemsForStatusAsync` convention, no early-stop per #1995), a new
  `MainWindow.BatterUpPanel.cs` partial-class file wiring 4 lane tabs (reusing the existing
  `FsCountChip` chip control) into a new `BatterUpPanelBody` panel body, removing "BatterUp" from
  the shared not-built placeholder list.
- 2026-09-02T01:00:00Z ✅ DONE — commit `dd100b551` (pushed to `main`, verified via
  `verify-branch-merged.mjs`, working tree clean). Shipped:
  - `ChatGitHubFilter.GetBatterUpLaneCountsAsync()` — real backward-paginated GraphQL walk of the
    whole `PVT_kwHOEiBDdc4BeoiY` project's `ProjectV2.items` connection, tallying OPEN issues in
    this repo by Status optionId (Batter Up `09b1927f` / AI Batter Up `a0296971` / Ask Shane
    `404998bb`), same no-early-stop pagination shape as BuildConsole's
    `GitHubApiClient.ScanProjectItemsForStatusAsync` (#1784/#1995), shelled through `gh api
    graphql` per this file's established no-PAT convention.
  - `MainWindow.xaml`: new `BatterUpPanelBody` (status line + `BatterUpLaneTabs` WrapPanel +
    `BatterUpLaneBody`), "BatterUp" removed from the shared `NotBuiltPanelBody` placeholder list.
  - `MainWindow.xaml.cs`: `OpenLeftPanel` now shows the real panel and fires
    `EnsureBatterUpPanelLoadedAsync()` on first open, instead of routing BatterUp through the
    not-built placeholder.
  - `MainWindow.BatterUpPanel.cs` (new): load/render for the 4 lane tabs, reusing the existing
    `FsCountChip` control (Filter Studio's own chip, same file/class) for the tab UI so tab
    styling/selection matches an already-proven pattern instead of a new one. Selecting a lane
    updates a plain "N item(s) in <lane>" status line — item rendering/grouping is explicitly out
    of scope here (#2357 onward).
  - **Verification:** `dotnet build ShaneBuilder.csproj -c Debug` — Build succeeded, 0 errors.
    Independently re-ran the identical GraphQL query/pagination shape via a scratch Node script
    (not committed) against the real live board: full 25-page walk, real counts confirmed
    Batter Up=29, AI Batter Up=121, Ask Shane=4, All=154 — matches the exact fields/logic the
    shipped C# method uses. Did not launch the WPF app itself (no test harness/headless runner
    exists for ShaneBuilder yet); build + live-query cross-check is the real verification done.
  - **Scope confirmation:** "All" lane = union of the 3 actionable lanes (Batter Up + AI Batter Up
    + Ask Shane), matching the dispatch comment's stated definition. `FilterStudioScopeList`'s
    "Batter Up — not registered" row (a separate Filter Studio facet-registration concern,
    unrelated to this panel) was deliberately left untouched — out of this item's scope.
  - No findings filed this session — no genuine bug/gap surfaced outside the stated scope.
