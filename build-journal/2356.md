# #2356 — Batter Up rail panel shell: lane switch (All / Batter Up / AI Batter Up / Ask Shane) + live counts

- **Status:** ⏳ IN FLIGHT 2026-09-02T00:00:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** TBD

## Log
- 2026-09-02T00:00:00Z ⏳ IN FLIGHT — real audit done: MainWindow.xaml.cs's BatterUp rail source is
  in the shared `notBuilt` placeholder list (~line 456); no panel content exists. ChatGitHubFilter
  has `GetBoardStatusAsync`/`GetBoardStatusesAsync` (issue→status, #2410) but no reverse
  status→items query. Board Status option ids confirmed live via `gh api graphql`: Batter Up
  `09b1927f`, AI Batter Up `a0296971`, Ask Shane `404998bb` (field `PVTSSF_lAHOEiBDdc4BeoiYzhZBRB0`,
  project `PVT_kwHOEiBDdc4BeoiY`). Board is ~2,403 items total. Building: `ChatGitHubFilter.GetBatterUpLaneCountsAsync()`
  (backward `items(last:100, before:cursor)` walk per BuildConsole's established
  `ScanProjectItemsForStatusAsync` convention, no early-stop per #1995), a new
  `MainWindow.BatterUpPanel.cs` partial-class file wiring 4 lane tabs (reusing the existing
  `FsCountChip` chip control) into a new `BatterUpPanelBody` panel body, removing "BatterUp" from
  the shared not-built placeholder list.
