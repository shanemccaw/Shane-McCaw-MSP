# #3167 — URGENT: chat context gauge shows dangerously wrong low usage while the real conversation is compacting — conversationId extraction never recovers after a false-empty read

- **Status:** ✅ DONE 2026-09-08T17:34:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-08
- **Commit(s):** 898ed9479 (fix + comment updates), verified ancestor of origin/main

## Log
- 2026-09-08T17:26:49Z ⏳ IN FLIGHT — Pulled #3167 body + comments. Investigating the ChatContextMeter chain (ChatContextMeterScript.cs JS + host-side ChatWv_WebMessageReceived / ChatContextMeterStore.Merge / UpdateContextMeter) to confirm live whether the one-shot conversationId hypothesis holds before fixing.
- 2026-09-08T17:34:08Z ✅ DONE — Root cause found and fixed. The issue's hypothesised MECHANISM (one-shot/memoised convId that never re-derives) is FALSE: `conversationId()` is re-read on every 2s poll via `store()`, which resets the accumulator when the id changes, so a /new → /chat/<uuid> transition already recovers. The real defect is a MALFORMED regex at `ChatContextMeterScript.cs:36`: it was `8-4-4-12` (four hyphen groups, one {4} missing) instead of a real UUID's `8-4-4-4-12` (five groups), so it returned null for EVERY well-formed /chat/<uuid>. With the id permanently null, `ChatContextMeterStore.Merge` was skipped forever and the persisted per-conversation meter was never written — the "never recovers after a false-empty read" symptom is real, just via a broken regex, not caching. CONFIRMED LIVE, no exe launch needed: today's real ActivityLog (channel system.core.chat-context) shows `[link 1] ... pathname='/chat/110b8198-222f-40dd-8b9a-493b399c3246' convId=<NULL>` — a valid UUID pathname yielding null; reproduced the exact null→match flip with `node` against those real logged pathnames. Fixed the regex to the real 5-group UUID (matching the working chat-association regexes at MainWindow.xaml.cs ~4912). Also fixed the SAME malformed-UUID regex at `MainWindow.xaml.cs:8180` (chat→build originating-id extraction, silently dropped the id off a build queued from a string-tagged tab) — same defect class, fixed in the same pass. `dotnet build` BuildConsole.csproj = 0 warnings / 0 errors. Not launching BuildConsole.exe (single-instance mutex could courier into Shane's live session). Point 3 (live-meter fallback #2808): confirmed the fallback is what was showing 207.5k — it reads meterState.EstimatedTokens which updates every poll regardless of convId, so it was a working stopgap, not itself poisoned; with the regex fixed the persisted store now works as primary. Point 4 (207.5k/900k looked "safe" while a real compaction hit): the numerator was the live fallback's real reading; the 900k denominator (ContextBudget, Git #2802) is Shane's own recorded 2026-09-04 decision (Sonnet 5 = 1M native window, 900k safety margin). A compaction at ~207k suggests that chat's real window was NOT 1M — but changing ContextBudget conflicts with a recorded decision and I cannot confirm chat #2880's actual model/window, so I did NOT change it; raised it as a follow-up question for Shane on the issue rather than assuming. Test manifests N/A (desktop/BuildConsole is out of scope). Working tree clean.
