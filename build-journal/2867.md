# #2867 — URGENT: #2815's rate-limit circuit stuck open past its 15-min max window

- **Status:** ✅ DONE 2026-09-04T14:33:34Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 40b494eea (fix), 06745d709 (IN FLIGHT bookend)

## Log
- 2026-09-04T14:30:32Z ⏳ IN FLIGHT — Investigating GitHubRateLimitCircuit half-open live-lock (#2867). Token confirmed healthy; breaker still reporting open 20+ min. Confirming the herd/half-open bug in real code before fixing.
- 2026-09-04T14:33:34Z ✅ DONE — Confirmed candidate #2 live in code: `ShouldShortCircuit` returned false for EVERY concurrent caller the instant `_openUntilUtc` elapsed, releasing a thundering herd instead of the single probe its own doc comment promised. That burst re-triggers GitHub's secondary rate limit each window, re-tripping the breaker indefinitely on a healthy token. Fixed by adding a true single-probe half-open: the window-elapse hands exactly ONE call through and leases `_openUntilUtc` forward 30s (`ProbeLease`) so the rest keep short-circuiting until the probe reports back; a lost probe just lets the lease elapse into a fresh probe, so it can never wedge open. Confirmed candidate #3 (MaxWindow clamp) is correct as-is, and candidate #1 (false-positive re-trip) is NOT the cause — the synthetic circuit-open text uses "rate-limit" (hyphen) while `LooksLikeRateLimit` matches "rate limit" (space), so it can't self-reclassify. Verification: `dotnet build BuildConsole.csproj` clean (0 warn / 0 err). Did NOT launch BuildConsole.exe (single-instance-mutex courier hazard). Commit 40b494eea on origin/main; branch ancestor check passed. No findings filed (root cause fixed in-session, no separate defect discovered).
