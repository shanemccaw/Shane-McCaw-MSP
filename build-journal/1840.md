# #1840 — SSH private key on disk lets an agent shell bypass ReplitSshService's Dev-only gate

- **Status:** ✅ DONE 2026-09-06T16:02:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** `cd274286c` (fix: CLAUDE.md hardening + docs/security/1840 closure plan) · `615193795` (IN FLIGHT bookend)

## Log
- 2026-09-06T15:39:37Z — ⏳ IN FLIGHT — Security audit of the residual vector left by #1828. #1828 gated the in-app `ReplitSshService` path (agent/`shaneapp://` hard-locked to Dev). This issue is the other half: an agent build session has a real shell and can run `ssh -i "$HOME\.ssh\replit" <user>@<host> "<cmd>"` directly — bypassing `ReplitSshService` and its gate entirely — because the private key sits readable on disk and CLAUDE.md documents the exact working invocation. Auditing the key handling, the trust boundary, and each of the three proposed remediations before changing anything.
- 2026-09-06T16:02:00Z — ✅ DONE — Audited real on-box state: key `~/.ssh/replit` is a 419-byte **plaintext** OpenSSH private key owned by `Ronnie`; the agent build session runs as that **same Windows user**, so options 1 (ACL) and 3 (DPAPI/at-rest) provably CANNOT close the same-user vector (identity-based ACLs + CurrentUser DPAPI both grant the agent what they grant BuildConsole). Option 2 (remote `authorized_keys` forced-command) is the only real closure. **Split delivery:** (a) In-repo, agent scope — rewrote CLAUDE.md's "Remote server access (SSH)" section: removed the copy-paste `ssh -i …` recipe + key-location pointer, added a HARD RULE prohibiting any agent session from invoking `ssh`/handling the Replit key directly (routes all remote access through the gated `ReplitSshService`). Only agent-facing copy of the recipe was CLAUDE.md L663; build-journal/1828.md mention is historical, left. (b) Shane-only remote step — authored `docs/security/1840-replit-ssh-key-hardening.md` (turnkey forced-command wrapper allowlist matching the exact commands `ReplitSshService` sends, exact `authorized_keys` line, install+verify steps, honest option analysis, key-rotation recommendation) and tracked the actionable step on **#1281** (release gate) so it survives #1840 closing. **Verification:** docs/guidance-only change, no `.cs` touched, so no build required; `git status` clean; branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0). Commented on #1840 + #1281; labels `in-flight`→`complete` + `Shane To-Do`. No new finding issue filed — the residual is #1840's own known option 2, carried on #1281 per the production-change gate, not a separate defect.
