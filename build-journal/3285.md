# Build Journal — #3285

- **Status:** ✅ DONE 2026-09-08T00:20:00Z
- **Scope:** platform (web/shanes-life, Shane's personal app)

## Log
- 2026-09-08T00:00:00Z — Started. Confirmed root cause via issue body:
  `sendVehicleCommand(userId, command, body = {})` defaults to an empty body;
  `openTrunkNow()` called `actuate_trunk` with no body at all, which Tesla's real API
  rejects with `invalid_value` since `actuate_trunk` requires `which_trunk`.
- 2026-09-08T00:15:00Z — Fixed both real dispatch paths in
  `web/shanes-life/src/core/tesla.mjs`: `openTrunkNow()` now sends
  `{ which_trunk: "rear" }`, and `dispatchDueCommands()` (the scheduled
  checkout-to-trunk path fired by `scheduleCheckoutTrunkOpen()`) now attaches the same
  body when replaying a stored `actuate_trunk` command. Used "rear" per the design
  handoff's own "pops the rear trunk" language and Shane's stated intent (Model 3, no
  front-trunk release in this flow).
- 2026-09-08T00:20:00Z — ✅ DONE — Commit `e377c849ee9cac7efe50565113d82dba7abd3661`
  merged into `main` at `16cb0700...` (via `verify-branch-merged.mjs`, confirmed OK).
  `node --check` passed; no existing unit tests cover `tesla.mjs` (none added — this
  is Shane's personal app, outside the msp-portal/marketing test-manifest scope).
  **Real, physical live verification (the trunk actually popping) was NOT done by this
  session** — that requires Shane's own Tesla app session / physical presence per the
  issue's own verification criteria ("confirmed by him directly"). Code fix is landed;
  Shane should tap "Open the trunk" in the Tesla room to confirm live.
