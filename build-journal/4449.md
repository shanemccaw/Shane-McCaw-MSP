# #4449 — Dev-only scan-pause: in-memory flag + gate at executeMonitoringPackage() + Portal dev-build toggle

- **Status:** ✅ DONE 2026-09-17T03:07:25Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** e682cc965 (merged to main; local worktree HEAD 1fd6fb887 pre-rebase)

## Log
- 2026-09-17T02:54:17Z ⏳ IN FLIGHT — Investigated executeMonitoringPackage() call sites, dev-only route convention (index.ts NODE_ENV registration), triggerScheduledWorkflows chain, and Portal dev-UI precedent (admin-panel's import.meta.env.DEV banner). Starting implementation: in-memory pause flag + getter/setter in monitor-executor.ts, triggeredBy param on executeMonitoringPackage(), dev-only pause/resume routes, threading triggeredBy through all real call sites, and a Portal dev-only floating toggle.
- 2026-09-17T03:07:25Z ✅ DONE — Shipped in commit `e682cc965` (pushed straight to main): process-level `scheduledScansPaused` flag + `isScheduledScansPaused()`/`setScheduledScansPaused()` in `monitor-executor.ts`; `executeMonitoringPackage()` now takes a required `triggeredBy: "scheduled" | "manual"` and returns an honest `dev_paused_skipped` result (never a fake completed scan) for a scheduled run while paused; new dev-only routes `POST /api/dev/scans/pause`, `POST /api/dev/scans/resume`, `GET /api/dev/scans/pause-state` registered only when `NODE_ENV !== "production"` (mirrors `admin-dev-seed.ts`'s conditional-import pattern — 404 in prod, not an in-handler check); threaded `triggeredBy` through every real call site (`workflow-executor.ts`'s `monitor_execute_package` node handler now looks up the firing run's real `wf_runs.triggerType` since that node serves both scheduled-cron and manual/webhook/event runs; `diagnostics-runner.ts` and `portal-delivery-kanban.ts`'s admin run-monitoring route are always `"manual"` — neither is ever reached by the Workflow Engine's scheduled trigger); Portal `DevScanPauseToggle` floating button gated on `import.meta.env.DEV`, mounted in `App.tsx`, structurally unreachable in a production build. Verification: `pnpm exec tsc --noEmit` clean on both `artifacts/api-server` and `artifacts/portal`; `vitest run` on `monitor-executor.test.ts` + `item-detail-collector.test.ts` — 225/225 passing, including 3 new dev-pause tests (scheduled run skipped without touching the DB, manual run still executes while paused, resume clears the flag). Live-verified against the real code + real local Postgres (`DATABASE_URL`): booted the real `dev-scan-pause` router + `executeMonitoringPackage()` in-process — `GET /api/dev/scans/pause-state` → `{paused:false}`; `POST /pause` → `{paused:true}` and a `triggeredBy:"scheduled"` call returned `runStatus:"dev_paused_skipped"` with the log line `"monitor-executor: scan skipped — dev pause active"`; a concurrent `triggeredBy:"manual"` call was NOT skipped (ran the real check-loading path, `runStatus:"no_checks"` for the nonexistent test package — proving it wasn't short-circuited); `POST /resume` → `{paused:false}` and a subsequent scheduled call again ran for real (not skipped). No findings filed — no genuine bug/gap surfaced outside this issue's own scope. Working tree clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs`.
