# #2678 — Chat Document Container: composer band always shows even empty

- **Status:** ✅ DONE 2026-09-03T16:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 792cd1366

## Log
- 2026-09-03T16:25:00Z ⏳ IN FLIGHT — primary worktree `C:\wt\2678-q1428` was launched completely
  empty (no `.git`, not in `git worktree list`) — a recurrence of Git #2118. Recovered manually
  per the known recipe (`git worktree add` from the main checkout + `registerWorktree` +
  `linkDeps`) before starting real work. Now investigating `Controls/ChatDocumentContainer.xaml`
  band 5 (composer) per the issue's real audit + 3-point plan.
- 2026-09-03T16:45:00Z ✅ DONE — `desktop/BuildConsole/Controls/ChatDocumentContainer.xaml` +
  `.xaml.cs`: band 5 (`ComposerBand`) is now `Visibility="Collapsed"` by default and shown only
  when `ChatComposer.Text` is non-empty or `ComposerReturnBar` (cross-epic return address) is
  visible, wired through `UpdateComposerBandVisibility()` from `AppendToComposer`,
  `ChatComposer_TextChanged`, `ComposerReturnClear_Click`, and post-send in `SendComposerAsync`.
  `InspectorBlockingOverlay` lives in the same Grid.Row cell, so `ShowInspectorBlocking()` now
  force-shows the band and `ClearInspector()` re-runs the visibility check — caught mid-build,
  not a follow-on bug. The hardcoded "Sonnet 5"/"Medium" labels are removed (not bound): checked
  and confirmed `BoardChat`/`bt_chats` carries no model/effort column and a chat tab isn't 1:1
  with a queued build's model/effort selection, so there's no real live per-tab state to bind to
  — per the issue's own instruction, dropped rather than left fake. The underlying composer
  mechanism (tool "send to chat" staging, cross-epic round trip, real DOM-bridge send) is
  untouched — only its visibility is gated. Verified: `dotnet build BuildConsole.csproj -c Debug`
  — 0 warnings/0 errors. Commit `792cd1366` pushed directly to `main` (rebased onto
  `origin/main`, worktree branch `agent/2678-q1428` discarded after push, per repo convention of
  committing directly to main). `verify-branch-merged.mjs` confirms merged. Working tree clean.
  Filed a fresh comment on the already-open #2118 (this build's own worktree hit the same empty-
  worktree bug) rather than a duplicate issue.
