# #3542 — GitHub MCP: add get_board_status read tool

- **Status:** ✅ DONE 2026-09-10T21:05:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** cb92b6c6c0e19a758d1d7ef82d93f840dec8b7b9 (merged to main)

## Log
- 2026-09-10T20:40:00Z ⏳ IN FLIGHT — implementing get_board_status(number) mirroring move-to-status.ts's GraphQL pattern
- 2026-09-10T21:05:00Z ✅ DONE — added `get_board_status(number)` (artifacts/github-mcp-server/src/tools/get-board-status.ts),
  registered in src/tools/index.ts, documented in README.md's tool list. Read-only, no `context`
  required per #3538. `tsc --noEmit` clean. Live-verified against real issues: #3542 returns
  `{ number: 3542, onBoard: true, status: "Batter Up" }`, #3534 returns
  `{ number: 3534, onBoard: true, status: null }` (genuinely unset Status field) — both
  cross-checked against a manual `gh api graphql` read of the same query. Every real issue probed
  (#3542, #3534, #10, #50, #100, #200) already has a project item, so the `onBoard: false` path
  wasn't exercised against a live example; that branch mirrors `move_to_status`'s own
  already-proven-in-production `itemId` lookup (same find-over-projectItems pattern used there
  when adding a fresh issue to the board), so the logic is covered by that existing production
  path even without a fresh live negative case. Branch merged to main directly
  (rebased onto current origin/main, pushed as cb92b6c6). No findings filed. Working tree clean.
