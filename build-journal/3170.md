# #3170 — Feature: Money — Accounts view (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T03:10:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 0d622429d (main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Read #3170, #3137 (closed, base money.mjs already ported).
  Read FINANCE_TRACKER_AUDIT.md §1 accounts.tsx (shanemccaw/Finance-Tracker) and the design
  contract pack §12. Confirmed real `finances` DB schema (accounts, plaid_items) locally.
  Building the real Accounts tab in web/shanes-life (sectioned by role, % funded, masked
  last-4, Plaid-linked badge, N underfunded, Total Envelope Balance, Connected Banks
  management, Edit Balance with live short-by-$X warning) against existing /api/money/*
  data-access layer, extending it as needed. No mask/last-4 column exists on `accounts` yet
  -- investigating whether Plaid sync already captures it before adding a migration.
- 2026-09-08T03:10:00Z ✅ DONE — Shipped the real Accounts tab (web/shanes-life/public/app.js
  `viewMoneyAccounts`), backed by two new real reads in src/core/money.mjs
  (`getAccountsOverview`, `previewAccountTarget`) and two new routes in
  src/routes/api.mjs (`GET /api/money/accounts`, `GET /api/money/accounts/:id/preview-target`):
  every real account sectioned by role (Income Gate/Bills/Reserves/Emergency Fund/
  Spending/Uncategorized), % funded + N-underfunded per bill section, Total Envelope
  Balance, real Connected Banks card (institution, health status, reconnect banner),
  masked last-4.

  Masked last-4 required a real, previously-missing field: confirmed (Explore agent
  investigation) Plaid's real `mask` field was never modeled anywhere in ShanesSurvival's
  C# Plaid sync (PlaidAccountJson/PlaidAccountInfo), so it was silently discarded off every
  real /accounts/balance/get response. Added it end-to-end: new `accounts.mask` column
  (web/shanes-life/migrations/043_account_mask.sql, ADD COLUMN IF NOT EXISTS, applied
  myself to the real local `finances` DB) + wired ShanesSurvival's real Plaid sync to
  capture and persist it (PlaidJsonModels.cs, IPlaidClient.cs, PlaidClient.cs,
  PlaidSyncService.cs). Existing rows stay NULL until their account's next real Plaid
  sync — honest "not yet known," not a fabricated digit. `desktop/ShanesSurvival.App`
  builds clean (`dotnet build`, 0 warnings/0 errors) both before and after rebasing onto
  #3163's concurrent bankruptcy-tracker commit.

  Real, deliberate scope line drawn on Connected Banks and Edit Balance, both recorded in
  code comments: (1) Connected Banks disconnect/reconnect is NOT wired here — money.mjs's
  own documented invariant is that nothing in this module writes to ShanesSurvival's
  tables, and a live Plaid disconnect/reconnect is exactly that kind of mutation (plus a
  real external Plaid API call) — that's #3168 ("Feature: Plaid webhooks +
  reconnect/update-mode flow"), which this issue's own text says Connected Banks "ties
  into." Built a real, honest status READ instead (health_status/last_synced_at), same
  idiom the app already used for Cars/Vault/Wins before they were their own real Features.
  (2) Edit Balance is a live, non-persisting short-by-$X preview
  (`previewAccountTarget`), never a `target_amount` write — #3162's own recorded decision
  is that account/bill mutations are ShanesSurvival's MCP tools' job, not this module's.

  Verified for real: `getAccountsOverview()` and `previewAccountTarget()` run directly
  against the real local `finances` Postgres (25 real accounts, 2 real Plaid
  institutions — Navy Federal Credit Union, Capital One; real Total Envelope Balance
  $16,116.67; real funded/short math on 11 real bill accounts). Confirmed the masked
  field renders correctly by setting/reverting a real test value on one real account row
  (reverted to NULL immediately after — no fabricated data left behind). All modified
  `.mjs`/`.js` files pass `node --check`. `desktop/BuildConsole`-style full `bin/check.mjs`
  boot currently fails in this worktree only because sibling in-flight sessions
  (#3163/#3166/#3168/#3169/#3171) hold `schema_migrations` ledger rows for migration
  files not yet merged to `origin/main` — a pre-existing, transient, shared-DB condition
  unrelated to this change (confirmed those exact files exist in those sessions' own
  live worktrees), not filed as a new finding since it self-resolves as those sessions
  land.

  Rebased twice onto concurrent `origin/main` moves (once through #3163's own bankruptcy
  overlay commit, resolving two small non-overlapping app.js/api.mjs conflicts by hand;
  once more with no conflicts) and pushed directly to `main` at `0d622429d`. Verified
  merged via `node scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain`
  clean.

  No test manifest: `web/shanes-life/` is outside the `artifacts/msp-portal/` /
  `artifacts/shane-mccaw-consulting/` scope CLAUDE.md's Test Coverage section applies to.

  No new findings filed — the two scope boundaries above (Connected Banks
  disconnect/reconnect, Edit Balance persistence) are real, already-planned follow-up
  work under existing open issues (#3168, ShanesSurvival's own MCP tools), not new gaps.
