# #3551 — Real table for the PCC test catalog

- **Status:** ✅ DONE 2026-09-13T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 28eeab1873c4ed3aba00650b795762355bf13e6f

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — investigating `lib/pcc/taxonomy-catalog.ts` (actual path: `artifacts/api-server/src/lib/pcc/taxonomy-catalog.ts`), `test-runner.ts` and `pcc.ts` route; about to add a real `pcc_test_catalog` table, seed it from `DEFAULT_TESTS`, and wire `/catalog` + `runSuite` to read from it.
- 2026-09-13T00:45:00Z ✅ DONE — added `pccTestCatalogTable` (`lib/db/src/schema/index.ts`, table `pcc_test_catalog`); wrote and ran `lib/db/migrations/manual/2026-09-13-pcc-test-catalog-3551.sql` against local `DATABASE_URL` (CREATE TABLE + index + 8-row seed from the exact `DEFAULT_TESTS` array, self-marking insert into `simulator_migration_runs`); replaced `DEFAULT_TESTS` with `taxonomy-catalog.ts`'s new `getCatalog()` DB reader; wired `pcc.ts`'s `GET /catalog` and `PccTestRunner.runSuite` (`test-runner.ts`) to call it instead of importing the constant. `artifacts/admin-panel/src/pages/PccDashboard.tsx` needed no change — same `{tests: PccTest[]}` response shape. Verified: `pnpm exec tsc --noEmit` clean in both `lib/db` and `artifacts/api-server`; queried `pcc_test_catalog` directly, confirmed all 8 rows match the original array's content exactly. Commit 28eeab1873c4ed3aba00650b795762355bf13e6f pushed to origin/main and confirmed merged via `verify-branch-merged.mjs`. `git status --porcelain` clean. No findings filed — this was a clean, self-contained fix with no discovered side issues.
