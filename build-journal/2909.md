# #2909 — Feature: Reserve account role + gate_status total-available math (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T21:20:00Z
- **Scope:** shanessurvival
- **Started:** 2026-09-04
- **Commit(s):** 5fe20f8ac

## Log
- 2026-09-04T21:00:00Z ⏳ IN FLIGHT — Audited live `accounts_role_check` on the real local
  `finances` Postgres DB: currently `('income_gate', 'bill', 'spend', 'emergency_fund')` —
  #2907 (Emergency Fund) has landed. Also confirmed the code for #2907 lives in this monorepo
  at `desktop/ShanesSurvival/` (migrations up to 007), not the separate standalone
  `C:\Source\ShanesSurvival` checkout referenced in #2880's epic body — that standalone repo
  is stale (last commit is 003_account_roles.sql, no remote configured). Proceeding to add
  the `reserve` role: migration 008, `AccountRole` enum case, WPF dialog option, and the
  `gate_status` total-available math (Income Gate + reserve accounts) in `DashboardService`
  (shared by both the WPF dashboard and the MCP `gate_status`/`bill_status` tools).
- 2026-09-04T21:20:00Z ✅ DONE — commit `5fe20f8ac` on `main`.
  **What shipped:**
  1. `migrations/008_reserve_account_role.sql` — widens `accounts_role_check` to also allow
     `'reserve'`, on top of the real audited live set (`income_gate`, `bill`, `spend`,
     `emergency_fund`), never regenerated from the original three. Ran it myself against the
     real local `finances` Postgres DB; confirmed via `pg_get_constraintdef` afterward that all
     four previously-allowed roles are still present alongside the new `reserve`.
  2. `AccountRole` enum (`ShanesSurvival.Core`) — new `Reserve` case with DB value/parse/
     display-name wiring.
  3. WPF "Assign Account Roles…" dialog — "Reserve" is now a selectable role (no target/GATE
     fields enabled for it — no savings-goal concept for a reserve account, just real usable
     balance).
  4. `DashboardService.ComputeAsync` (shared by the WPF dashboard top-line and the MCP
     `gate_status`/`bill_status` tools) — real total-available math: Income Gate balance + sum
     of every real `role = 'reserve'` account's balance (a reserve account with no synced
     balance yet is excluded from the sum and called out in Warnings, same convention as bill
     shortfalls). Top-line = total available − total shortfall.
  5. `gate_status` MCP tool output now shows each piece separately — Income Gate balance, each
     reserve account and its balance, the combined total-available line — before the final
     covered/short-by-$X line. WPF dashboard top-line subtext also shows the reserve breakdown.
  6. Did **not** auto-reassign the real miscategorized Capital One accounts ("Jake",
     "Emergency Fund" — both currently `income_gate` alongside the real `DirectDeposit`
     account) — left for Shane to reassign himself in the dialog, per the issue.

  **Real finding, filed separately:** #2907 (Emergency Fund role) was closed `complete` but its
  code was never actually reviewed against the real repo state before this session started —
  turned out fine (the code genuinely is on `main` at `desktop/ShanesSurvival/`), but a stale
  standalone clone at `C:\Source\ShanesSurvival` (no remote, last commit is migration 003) does
  NOT reflect current reality and could mislead a future session that looks there first per
  #2880's now-outdated "code lives at C:\Source\ShanesSurvival" epic body. See #2911.

  **Verification (real, end-to-end, against the real local `finances` DB):**
  - Audited the real live `accounts_role_check` constraint at build time via `psql` before
    writing the migration — confirmed it was `('income_gate', 'bill', 'spend',
    'emergency_fund')` (i.e. #2907 had genuinely landed), then re-confirmed after running 008
    that all four are preserved plus `'reserve'`.
  - Temporarily reassigned the real "Jake" account (Capital One, real balance $132.46,
    previously `income_gate`) to `role = 'reserve'`, ran a throwaway console harness against
    the real project DLLs calling `FinanceTools.GetGateStatusAsync()` directly against the real
    local DB. Real output: Income Gate ("DirectDeposit") $1,495.64 + Reserve (Jake) $132.46 =
    Total available $1,628.10; total shortfall $7,544.10; Short by $5,916.00 — math checks
    out ($1,495.64 + $132.46 = $1,628.10; $7,544.10 − $1,628.10 = $5,916.00). The "N accounts
    assigned Income Gate" warning correctly dropped from 3 to 2 once Jake moved to Reserve.
  - Reverted "Jake" back to `role = 'income_gate'` afterward — the test reassignment was mine
    for verification, not Shane's real decision, which stays his to make in the dialog.
  - `dotnet build ShanesSurvival.sln` clean — 0 warnings, 0 errors.
  - Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree
    clean (`git status --porcelain` empty).

  Findings filed: #2911 (stale standalone ShanesSurvival clone / outdated epic body).
