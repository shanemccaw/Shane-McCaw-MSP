# #1544 — Remediation: Unauthorized-change notification to both sides

- **Status:** ✅ DONE 2026-09-03T13:15:00Z
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** daa14b005 (bookend/blocked), <fill after push>

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled #1544's real body + comments; found it genuinely
  blocked on #1518's cross-boundary notify work (not yet landed). Labeled
  `blocked`, wired `blocked_by -> #1518`, corrected a stale `blocked_by -> #1539`
  edge left from dispatch boilerplate. No code changed. (`daa14b005`)
- 2026-09-03 ⏳ IN FLIGHT — resumed. #1518 closed NOT_PLANNED, superseded by #2162
  (configurable strict/loose acceptance gate) which landed for real
  (`msp-ownership.ts` symmetric cross-boundary routes, `notifyOwnershipPending`,
  `resolveGateMode`, decline provenance columns) and is itself now closed
  `completed`. Removed both stale `blocked_by` edges (#1518, #2162 — both
  closed), swapped label `blocked -> in-flight`.

  Implemented #1544's actual spec: every drift alert notifies both the MSP
  (already real — admin email/push dual-delivery in
  `customer-tenant-alert-engine.ts`) AND the customer's real Accountable ("a")
  RACI owner for the affected object. "The affected object" resolves without
  inventing any new vocabulary: `drift_events.domain_key` -> the real check key
  that produces it (`drift-check-specs.ts`, new `checkKeyForDriftDomain`,
  reverse of the existing `driftSpecForCheck`) -> the real workload that check
  category is already mapped to for risk-acceptance authority
  (`tenant-workloads.ts`'s established `resolveWorkloadForCheckKey`, Git #1511)
  -> that workload's live RACI object id (`"wl-" + key"`) -> its real "a" holder
  row(s) in `portal_ownership_assignments`. Three of five live drift domains
  resolve to a real single-workload owner (ca-policy -> icam/Identity, 
  tenant-sharing-capability -> sharepoint, email-authentication -> exchange);
  the other two (public-teams-discoverable, eeeu-site-sharing) honestly resolve
  to no single owner, because their check categories (governance/compliance)
  are cross-cutting by the SAME pre-existing #1511 design, not a new gap.

  New: `notifyDriftAccountableOwners` (notification-center.ts) — resolves the
  live "a" holders on a workload object and routes each to `customer_user` or
  `msp_user` via the existing dual-boundary notification machinery
  (`createNotification`), same pattern as `notifyOwnershipPending`. Excludes a
  `declined` holder. Wired into `customer-tenant-alert-engine.ts`'s firing loop
  for `alert_category === "drift"`, independent of `notify_customer` (an
  Accountable owner is customer-named RACI, not the generic delivery toggle).

  No new tables/columns — genuinely "no new machinery" per the issue's own
  text: reuses `customer_tenant_alert_rules/events`, `portal_ownership_
  assignments`, and the existing notification-center dual-boundary routing.

  Added test coverage in `drift-check-specs.test.ts` (already registered in
  vitest.config.ts) for the new `checkKeyForDriftDomain` inverse lookup and the
  domain->workload routing (including the two honest no-owner cases).
  **Could not run vitest** — pre-existing, already-filed environment breakage
  (pnpm store copy of vitest@4.1.9 is empty; confirmed via `ls` on the real
  target, not assumed): #2508, #2516, #2503, #2515, #2501, #2502, #2192, #2193
  all cover this exact class already, nothing new to file. `tsc --noEmit`
  confirms no new errors from this change beyond the same pre-existing
  `@workspace/db`/drizzle-orm resolution cascade every other file in this
  worktree already shows (e.g. `tenant-workloads.ts:311`, untouched by me).
  Confirmed via live `psql` against local `DATABASE_URL`: `portal_ownership_
  assignments` has zero `wl-*` "a" rows today (no customer has assigned an
  Accountable owner on a workload yet) — real, honest current state; the new
  code correctly reports 0 notified in that case rather than fabricating one.
