# #1544 — Remediation: Unauthorized-change notification to both sides

- **Status:** ⏳ IN FLIGHT (BLOCKED)
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** none — no product code changed

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled #1544's real body + comments. Actual spec (drift
  notification to both the customer's Accountable owner and the MSP, via existing
  `msp_alert_rules`/`msp_alert_events`) explicitly depends on #1518's cross-boundary
  acceptance work ("#1518's cross-boundary acceptance work means one RACI assignment
  can notify both sides of the tenant boundary"). #1518 itself lists the two things
  this issue needs as still missing — an assignee-notify mechanism and a way for a
  person outside the customer's tenant to act on a pending cell — and is currently
  `in-flight` under a separate build, not yet landed. Confirmed no cross-boundary /
  cross-tenant notify code exists under `lib/` or `artifacts/api-server` yet.
  Found and corrected a stale structural `blocked_by -> #1539` dependency set at
  dispatch time from generic module-intro boilerplate (#1539 is unrelated to this
  issue's actual content and already closed/complete); replaced it with the real
  `blocked_by -> #1518`. Labeled `blocked`, removed `in-flight`. Posted explanation
  on #1544. No schema/route/UI work started — nothing to build against an
  unresolved prerequisite. Session ends here; re-dispatch once #1518 lands.
