# #1414 — Security sub-pages: audit all 5 routes for unconditional fixture fallback

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 452e3d37b (bookend open); this DONE commit

## Log
- 2026-08-27 ⏳ IN FLIGHT — Auditing all 5 Security sub-page routes (mfa, ca, oauth,
  legacy-auth, email) for unconditional fixture fallback, same methodology as
  #1387/#1409/#1413: locate `?? FIXTURE.field` patterns and elements rendering
  outside/after a dataState conditional; classify static-label (fine) vs.
  data-value-presented-as-real (needs live wiring or honest no-data state).
- 2026-08-27 ✅ DONE — Investigation-only, no code changed. Audited all 5 routes.
  Findings (posted in full on the issue):
  1. `/portal-v2/security/mfa` (`portal-v2-security-mfa.tsx`) — the "gaps"/"partial"
     state hero and banner copy ("8 accounts still don't have it", "2 are admin
     accounts", "8 accounts without MFA", "Enable MFA for all 8", "2 of 5 users
     still haven't registered a method", "Send enrollment reminder to 2 users") is
     hardcoded literal text that does not track `gapUsers.length`/`partialUsers.length`
     — when `useMfaRegistrationLive` (#1234) is live and the real row count differs
     from the fixture's 8/5, the row list itself updates but the surrounding
     headline/banner keeps citing the old fixture numbers. Needs those numbers
     derived from the live arrays instead of literal strings.
  2. `/portal-v2/security/ca` — no new fixture leak; `rowsAreLive` correctly gates
     bands/stats (#1232). The "22 named Conditional Access policies" heading and
     "Baseline policies" stat are static methodology counts (the fixed baseline
     framework size, not a live tenant value) — classified fine, matches the
     file's own documented acknowledgment.
  3. `/portal-v2/security/oauth` — correctly overlays live `enterpriseAppCount`/
     `riskyPermissionGrantCount` onto 2 of 5 stat cards (#1233); the other 3 stat
     cards + evidence rows + top-risks + secondary panel stay fixture, but this is
     documented in-code as a real backend gap (no per-item server producer) —
     acceptable per the honest-gap rule, not a silent fixture-as-live case.
  4. `/portal-v2/security/legacy-auth` — HIGH: entirely unconditional fixture, no
     live hook exists for this route at all. A real backing metric
     (`identity.legacyAuthCount` / `identity:legacy-auth-usage`) is already
     registered and already consumed live elsewhere (Security overview page via
     `useSecAreaLinksLive`, #1258), but never reaches this drill-down — fully
     fabricated evidence rows (fictional accounts, sign-in counts) render
     unconditionally with no dataState distinction.
  5. `/portal-v2/security/email` — HIGH: same unconditional-fixture pattern as
     legacy-auth, and the clearest gap of the five: #1258 explicitly registered
     real Email Security checks (`security.emailAuthFindingCount`,
     `exchange:dkim-spf-dmarc-status`) and already wired them live onto the
     Security overview page's "Email Security" card, yet
     `/portal-v2/security/email`'s stat cards and evidence rows (fabricated
     domain "tenant.com", SPF/DKIM/DMARC records) never consume them — 100%
     `EVIDENCE_PAGES["security-email"]` fixture with no live overlay at all.
  Filed as issue comment for Shane's/next session's decision on live-wiring
  legacy-auth and email (both #4 and #5 have a real, already-proven-reachable
  backing metric, unlike oauth's documented partial gap).
