# #1491 — Portal New Design: Ownership / RACI (contract extraction pack)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 869bb028c (IN FLIGHT), ae0a4ba09 (DONE — contract pack)


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1491`, but #1491 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1491 is complete.
>
> #1491's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-29 ⏳ IN FLIGHT — Step 3 of #1578's per-module sequence (schema → honest
  read → contract pack → Design → wire), scoped to Ownership/RACI per #1577's
  scoping correction. Pulled #1491 body+comment, #1485/#1577/#1578 body+comments,
  and sub-issues #1515-#1524 in full. Read `lib/portal-ownership.ts`,
  `routes/portal-ownership.ts`, the three `portal_ownership_*` Drizzle tables, and
  the msp-portal consumers (`ownershipWire.ts`, `ownershipModel.ts`,
  `ownershipPersist.ts`, `OwnershipMatrix.tsx`, `portal-v2-ownership.tsx`,
  `portalV2People.ts`). Confirmed all 2 GET + 5 POST endpoints have real UI
  callers — no orphaned endpoint to sub-issue. Writing
  `docs/ownership-raci-contract-pack.md` next.
- 2026-08-29 ✅ DONE — Wrote `docs/ownership-raci-contract-pack.md`: full wire
  contract for `GET /api/portal/ownership` + all 5 write endpoints, every field
  cited to file:line and marked CURRENT/DECIDED with its settling issue number,
  the real 7-value `OwnObjectType` enum with live/unbacked status, the honest-empty
  tri-state (and the finding that `read-failed` and `live-genuinely-empty`
  currently collapse into one `"fixture"` state with no distinguishing field —
  flagged as an open gap, no issue filed since it's not an orphaned endpoint),
  the forbidden-declarations list, the cross-surface edge to #1511's risk
  acceptance authority, and a CURRENT vs DECIDED master table separating settled
  architecture (#1515-#1524) from genuinely open questions (#1518's C-gate,
  #1523's runbook/SOP inheritance question, #1524's delegation fate). No product
  code, schema, or UI touched — read-only investigation as required. Endpoint
  audit found zero live-but-uncalled endpoints, so no sub-issue was filed.
  Branch verified merged into main via `verify-branch-merged.mjs` before writing
  this line. Commit `ae0a4ba09`.
