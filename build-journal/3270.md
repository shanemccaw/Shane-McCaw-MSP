# #3270 — Tesla is connected but has no room and doesn't appear in Cars

- **Status:** ✅ DONE 2026-09-08T22:23:54Z
- **Scope:** platform (web/shanes-life — Shane's personal "Shane's Life" app, milestone 19 "Shane", not MSP business work)
- **Started:** 2026-09-08
- **Commit(s):** `3833c17ba` (rebased onto main as `e79cc2eac`)

## Log
- 2026-09-08T21:52:58Z ⏳ IN FLIGHT — Investigated gap #1 (no `tesla` entry in `public/app.js` ROOM_DEFS,
  confirmed via grep) and gap #2 (local `finances` DB has zero `tesla_accounts` and zero `vehicles`
  rows for any user, confirming the issue's own hypothesis that no Cars entity exists yet).
  Read `Design/design_handoff_shanes_life/README.md`'s real Tesla-room spec + `v5-tesla-room.png` /
  `v5-tesla-warming.png` screenshots (tint `34,211,238`, `r-tesla` furniture, `heading` critter pair
  — already exists in `critters.js`/`critters-sprite.svg`). Plan: add `tesla` to `ROOM_DEFS` +
  `roomsForToday` (non-waking tile state only — no live Tesla read from the Today tray, matching
  tesla.mjs's own "nothing polls on its own" discipline), build a dedicated `#/tesla` room
  consolidating Settings' scattered Tesla UI (checkout-to-trunk toggle/pending list, commute
  settings, climate check) plus new real controls (Heading home, Warm it up start/stop, Open the
  trunk) needing two small new tesla.mjs commands (`auto_conditioning_stop`, on-demand
  `actuate_trunk`) and their routes, and a real Tesla→Cars auto-link (name-match an existing
  vehicle or create one, `setTeslaSynced`) wired into vehicle selection + a retroactive
  self-heal/manual action in the room for Shane's already-connected real account. Out of scope
  (separate Design bundle items, not this issue): Vault room, Money nav rebuild, Settings tabs,
  capture bar redraw — staying strictly to #3270's own two gaps.
- 2026-09-08T22:23:54Z ✅ DONE — Both real gaps closed.

  **Gap #1 (no dedicated room):** built the real Tesla room (`viewTesla`, `public/app.js`):
  Right now (on-demand climate+charge read via new `GET /api/tesla/charge`, never polled), Do
  (Heading home via existing `triggerHeadingHome`; Warm it up start/stop via two new tesla.mjs
  commands -- `auto_conditioning_stop` did not exist before this build; Open the trunk with a
  real two-tap client confirm via a new on-demand `actuate_trunk` route), On its own
  (checkout-to-trunk toggle/pending list + the commute-check card, both moved out of Settings),
  and a Cars-link status card. Settings' Tesla section slimmed to connect/disconnect + vehicle
  picker + hook tokens + a link to the room, per the design handoff's own instruction that the
  room, not Settings, owns the controls.

  **Gap #2 (no Cars link):** `vehicles.autoLinkTeslaVehicle` -- name-matches an existing Cars
  vehicle or creates a new one, wired into `/api/tesla/vehicles/select` (real vehicle selection)
  and a new idempotent `/api/tesla/link-to-cars` self-heal the room calls on load, closing the
  gap for Shane's own already-connected account (which selected a vehicle before this auto-link
  existed).

  **Concurrent duplicate-scope collision (Git #3271), resolved during rebase, not a mistake on
  either side:** #3271 ("Tesla + Vault rooms in the house grid") landed on `main` mid-session,
  adding the same `tesla` ROOM_DEFS entry + `r-tesla` furniture symbol + a real,
  hook/scheduled-command/nudge-based tile-lit rule -- but explicitly left the room *page* unbuilt
  ("Known, deliberate interim gap... those room PAGES are separate, later steps," per its own
  commit message). Genuinely complementary, not wasted work: dropped my simpler
  `tesla.mjs#getRoomTileState` and the duplicate ROOM_DEFS/furniture entries in favor of #3271's
  richer versions during the rebase, kept my room page + commands + auto-link (which #3271
  explicitly deferred), and re-ran both verification scripts against the merged result -- all 21
  assertions (9 real-DB + 12 real-HTTP) still pass.

  **Verified, real, not simulated:** two scratch Node scripts (deleted before finishing, DB
  residue confirmed zero via `psql`) -- one against the real local `finances` Postgres directly
  (`autoLinkTeslaVehicle` create/idempotent/name-match, 9 assertions), one full HTTP round-trip
  through a real, disposable passkey-enrolled user against a live local server (`/api/today`'s
  tesla+vault tiles, the auto-link over real HTTP, every new route's real success/error path, 12
  assertions). Shane's own real, live-connected Tesla account lives only on the deployed Replit
  instance, which this session has no reachable/sanctioned path to (no local
  TESLA_CLIENT_ID/SECRET, no SSH per the standing HARD RULE) -- the local `finances` DB itself
  confirmed zero `tesla_accounts`/`vehicles` rows for any user before this build, consistent with
  the issue's own hypothesis. Real, honest gap: the two-tap trunk confirm and the Warm-it-up
  start/stop button's live state were exercised via seeded fake connections and real
  route/error-path checks, not against a real vehicle in this session.

  **Real finding filed:** #3281 -- several pre-existing `/api/tesla/*` routes (`GET
  /api/tesla/vehicles`, `GET /api/tesla/climate`, `GET /api/tesla/commute-check`) leak an
  uncaught `TeslaError` as a raw 500 instead of a real 400, found while verifying my own new
  `/api/tesla/charge` route hit the same gap (fixed in my own route; the three pre-existing ones
  are out of this issue's scope). Parented under Feature #3237, board status set to AI Batter Up.

  No migration needed -- every table used (`tesla_accounts`, `vehicles`,
  `tesla_scheduled_commands`, `nudge_events`) already existed.
