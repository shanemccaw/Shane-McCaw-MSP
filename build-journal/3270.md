# #3270 — Tesla is connected but has no room and doesn't appear in Cars

- **Status:** ⏳ IN FLIGHT 2026-09-08T21:52:58Z
- **Scope:** platform (web/shanes-life — Shane's personal "Shane's Life" app, milestone 19 "Shane", not MSP business work)
- **Started:** 2026-09-08
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-08T21:52:58Z ⏳ IN FLIGHT — Investigated gap #1 (no `tesla` entry in `public/app.js` ROOM_DEFS,
  confirmed via grep) and gap #2 (local `finances` DB has zero `tesla_accounts` and zero `vehicles`
  rows for any user, confirming the issue's own hypothesis that no Cars entity exists yet).
  Read `Design/design_handoff_shanes_life/README.md`'s real Tesla-room spec + `v5-tesla-room.png` /
  `v5-tesla-warming.png` screenshots (tint `34,211,238`, `r-tesla` furniture, `heading` critter pair
  — already exists in `critters.js`/`critters-sprite.svg`). Plan: add `tesla` to `ROOM_DEFS` +
  `roomsForToday` (non-waking tile state only — no live Tesla read from the Today tray, matching
  tesla.mjs's own "nothing polls on its own" discipline), build a dedicated `#/tesla` room
  consolidating Settings' scattered Tesla UI (checkout-to-trunk toggle/pending list, commute
  settings, climate check) plus new real controls (Heading home, Warm it up start/stop, Open the
  trunk) needing two small new tesla.mjs commands (`auto_conditioning_stop`, on-demand
  `actuate_trunk`) and their routes, and a real Tesla→Cars auto-link (name-match an existing
  vehicle or create one, `setTeslaSynced`) wired into vehicle selection + a retroactive
  self-heal/manual action in the room for Shane's already-connected real account. Out of scope
  (separate Design bundle items, not this issue): Vault room, Money nav rebuild, Settings tabs,
  capture bar redraw — staying strictly to #3270's own two gaps.
