# #3484 — Feature: VIP Classifications (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:52:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 094a725c0 (real feature work, rebased onto and pushed to `origin/main`;
  verified via `scripts/dev-server/verify-branch-merged.mjs` — exit 0)

## Log
- 2026-09-10T20:33:29Z ⏳ IN FLIGHT — resumed. #3493 (UI Shell Redesign), the sole real
  blocker recorded on the prior 🛑 BLOCKED session (2026-09-10T06:19:38Z), is now genuinely
  closed/`complete` (commit `e8460c5fd`, merged to `main`, confirmed via `gh issue view 3493`
  and the real `LeftReferencePanel`/`RecordWorkspacePanel`/`FixedRibbonRenderer` files on disk).
  Removed the now-stale `blocked_by` edge #3484 → #3493 and relabeled `blocked` → `in-flight`
  before starting real work, per the Git #1987 discipline.
- 2026-09-10T20:52:00Z ✅ DONE — built the real remaining scope: Feature #3484's own
  checklist item, "Client: VIP classification lookup, surfaced in Console (#3459) and
  Remediation execution (#3471) flows before acting on a user."

  **Shipped, real and wired against the already-shipped backend** (no backend change needed —
  `artifacts/api-server/src/routes/msp-vip-classifications.ts` already served
  `GET/POST /api/msp/vip-classifications`):
  - `desktop/MyArchitect/Models/VipClassification.cs` — wire-shape mirror of
    `toWireVipClassification`, no invented field.
  - `desktop/MyArchitect/Services/IVipClassificationsService.cs` +
    `VipClassificationsService.cs` — real HTTP client (`GetClassificationsAsync`,
    `SetToldAsync`), same `AuthToken`/base-URL/exception pattern as
    `RemediationTrackerService`. Wired into `MainWindow`'s constructor and
    `ApplyAuthState` token fan-out alongside the other MSP services.
  - `Shell/LeftReferencePanel.xaml(.cs)` — the stated-empty "VIP LOOKUP" section (from
    #3493's own left-panel scaffold) is now a real UPN search box + Check button, calling
    back into `MainWindow.RunVipLookup`, which resolves the real MSP+customer scope
    (`TryResolveLaunchControlScope`) and reports the real classification (told/discovered,
    classified-by, date) or an honest reason none could be checked.
  - `MainWindow.xaml.cs`'s `RunScriptLibraryAction` — the shared execution path both Console
    (#3459) and Remediation execution (#3471) run catalog-backed items through (#3471's own
    body: "execution runs through the embedded Console") — now calls
    `SurfaceVipStatusBeforeExecute` first: any filled-in `RequiredVariables` value that looks
    like a UPN (`Contains('@')`) is checked for real against `msp-vip-classifications` and the
    result appended to the Console output pane before the run fires. This is a surface, not a
    gate, matching the issue's own wording ("surface whether a user is VIP-classified").
  - `dotnet build`: 0 errors, 0 warnings.

  **Known, already-tracked gap this inherits (not re-filed):** `customerId` is not yet
  resolvable anywhere in MyArchitect — `TenantService` is fixture data with no real numeric
  `tenants.id` (tracked at #3540/#3502/#3505, owned by #3457). Until that lands, both the Left
  Panel VIP lookup and the pre-Run VIP surfacing honestly report "no real customer id yet
  (#3540)" instead of guessing an id — same idiom #3460 (Script Library) and #3471
  (Remediation) already established for this exact gap. No fixture/hardcoded classification
  data anywhere. The moment #3540 lands, this path activates with no further edit needed.

  **Not attempted (out of #3484's own scope):** discovery-seed UI
  (`POST /msp/vip-classifications/discover`) — that's a bulk onboarding operation, not the
  per-user safety-check surface this issue asks for.
