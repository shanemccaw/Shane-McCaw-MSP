# #3484 — Feature: VIP Classifications (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T06:19:38Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** none — no feature code changed this session (blocked on #3493)

## Log
- 2026-09-10T06:15:00Z ⏳ IN FLIGHT — pulled #3484 body + both `BUILD:` comments per
  standing rules. Comment history:
  1. `BUILD:` dispatch (buildSet `MyArchitect`) carrying `--blocked-by 3493` explicitly,
     instructing to read GEMINI.md / BUILD_QUEUE_METHOD.md / UI_RULES.md.
  2. later `BUILD:` dispatch (buildSet `BatterUpClearOut`) that omits the `--blocked-by`
     flag but does not state #3493 cleared — only reiterates the standing "pull body +
     comments, build per real stated scope" instructions.
- 2026-09-10T06:19:38Z 🛑 BLOCKED — on #3493 (Feature: UI Shell Redesign), same as
  siblings #3463 / #3464 / #3474 / #3480. Findings:
  - **All of #3484's remaining scope is client UI.** Its own checklist is one item:
    "Client: VIP classification lookup, surfaced in Console (#3459) and Remediation
    execution (#3471) flows before acting on a user." The backend is real and already
    shipped — `artifacts/api-server/src/routes/msp-vip-classifications.ts` implements
    `GET/POST /api/msp/vip-classifications` and `POST
    /api/msp/vip-classifications/discover`, wired in `routes/index.ts`. There is no
    backend gap left to build here.
  - **VIP lookup attaches into the shell's left panel.** UI_RULES.md (Feature #3493)
    explicitly places "VIP lookup" on the shell's **left panel** (reference/read-only:
    bookmarks, VIP lookup, consent status, document browse) — chrome that does not
    exist yet.
  - **Confirmed #3493 not started (re-checked this session):**
    `desktop/MyArchitect/MyArchitect.csproj` has no `Fluent.Ribbon` package reference;
    `MainWindow.xaml` has the pre-redesign chrome (no `RibbonTab`/left-panel/right-panel
    workspace); `build-journal/3493.md` does not exist; #3493 is open and labeled
    `in-flight`, not `complete`.
  - **GEMINI.md standing rule**: UI Shell first, no exceptions — do not build ad-hoc
    chrome to unblock yourself; stop and flag it. Building a VIP-lookup surface now
    would mean inventing a left-panel/gallery mechanism ahead of and in conflict with
    the shell spec #3493 is defining, which the CLAUDE.md "BUILD IT" rule doesn't
    override (that rule is about a missing *backend*; the backend here exists).
  - **Gate (Git #1987):** the real `blocked_by` edge #3484 → #3493 already existed
    (created by the dispatching `--blocked-by 3493` flag) and remains valid (#3493 is
    open/not-complete) — left in place, not re-created. Applied the `blocked` label,
    removed `in-flight`, left a plain-language comment on #3484.
  - **Unblock path:** once #3493 lands (left-panel reference workspace + gallery
    control), wire the VIP lookup surface into it against the existing
    `msp-vip-classifications` endpoints — Console (#3459) and Remediation execution
    (#3471) flows both read from it before acting on a user.
