# Git #3919

- **Status:** ✅ DONE 2026-09-13T00:15:00Z
- **Scope:** platform
- **Log:** Adding `!isLoading && !!accessToken` enabled gate to the four ungated hooks in artifacts/msp-console/src/api/webhooks-api.ts (useCustomerWebhooks, useWebhookDeliveries, useAllCustomerDeliveries, useInboundWebhookActivity), matching the pattern in team-api.ts/runbooks-api.ts/etc.
- **Log:** ✅ DONE 2026-09-13T00:15:00Z — Commit 1ec7d25fc (merged to main as 76c5c1a8f) gates all four hooks. `useAllCustomerDeliveries` required renaming its own `isLoading` destructure to `authLoading` to avoid a name collision with its existing aggregate `isLoading` return value. `npx tsc --noEmit -p artifacts/msp-console` is clean. Confirmed `Overview.tsx` (Tenant Overview roll-up) consumes `useCustomerWebhooks`, so the #3822 race is fixed by this same change. `verify-branch-merged.mjs` confirms HEAD (76c5c1a8f) is an ancestor of origin/main. `git status --porcelain` clean.
