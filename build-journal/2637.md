# Issue #2637 — Git panel: remove the commit graph entirely

- **Status:** ✅ DONE 2026-09-03T16:35:00Z
- **Scope:** buildconsole

## Log
- 2026-09-03T00:00:00Z — Session started. Removing PopulateGitGraph, BtnGitGraphToggle_Click,
  ComputeGitGraphSignature, GitGraphCommit/GitGraphMaxCommits, and the XAML COMMIT GRAPH
  header/BtnGitGraphToggle/GitGraphHost from `Controls/LeftSidebar.xaml(.cs)`. Leaving
  PopulateGitBranches/GitBranchListHost untouched per issue scope.
- 2026-09-03T16:35:00Z — ✅ DONE. Removed the commit graph entirely: XAML COMMIT GRAPH header,
  BtnGitGraphToggle, GitGraphHost; code-behind PopulateGitGraph, BtnGitGraphToggle_Click,
  ComputeGitGraphSignature, GitGraphCommit, GitGraphMaxCommits/GitGraphLaneWidth/
  GitGraphRowHeight/GitGraphDotRadius/GitGraphLeftPad/GitGraphLaneColors, GitLaneBrush/
  GitLaneX/GitRowY helpers, and all three PopulateGitGraph() call sites in RefreshGitStatus.
  PopulateGitBranches/GitBranchListHost left untouched, confirmed still wired.
  Grep for GitGraph/CommitGraph across desktop/BuildConsole (both before and after) returns
  zero hits in real source — the only pre-existing match was an unrelated, already-tracked
  `.target_lsb.cs.bak` stray backup file (not touched by this build; filed as #2684, parented
  under #1202). `dotnet build` clean, 0 warnings/0 errors. Hit two real concurrent-session
  rebase conflicts against `main` (a #2575 checkbox-multi-select commit landed mid-session)
  and resolved them by hand, re-verifying build clean after each. Commit
  c155f60d9 rebased/pushed as d9e58c8f3 on origin/main; verify-branch-merged.mjs confirms
  ancestor of origin/main. `git status --porcelain` clean.
  Findings filed: #2684 (stray tracked .bak file polluting grep, parented under #1202).
