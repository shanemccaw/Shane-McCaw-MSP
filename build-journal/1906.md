# Git #1906 — Enrol the service principal as a Power Platform management application

- **Status:** ✅ DONE
- **Scope:** platform
- **Issue:** #1906 (parent research/build #1869; original finding #1849)
- **Branch:** main
- **Commit(s):** no code changes — this session performed a live production tenant action
  and a read-only verification, not a code build. This bookend commit itself is the only
  commit.

## Log

- ⏳ **IN FLIGHT** — 2026-08-30 — Session opened. Goal: enrol app
  `4743b130-0379-41bf-b863-ec8de96d915a` as a Power Platform management application in
  tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3` via a single
  `PUT https://api.bap.microsoft.com/providers/Microsoft.BusinessAppPlatform/adminApplications/{clientId}?api-version=2020-10-01`
  using a live tenant-admin **user** token (az first, device code as fallback), then prove the
  **app-only** executor can read via `scripts/verify-power-platform-1869.mts`, and report the
  tenant's real DLP posture. Read-only after the single PUT — no DLP policy created, modified or
  deleted, no other tenant change. Production change authorised by Shane explicitly in chat (see
  his 2026-08-30 comment on #1906); not a precedent for other production changes.
- ✅ **DONE** — 2026-08-30 — `az account get-access-token --resource https://service.powerapps.com/`
  returned a live, valid Global Admin user token for `smccaw@shanemccaw.com` on tenant
  `c4c814d4-3afe-441e-9145-62461d0a4fd3` (per #1875) — device code was not needed, Shane was
  not interrupted. Issued the single PUT: HTTP 200,
  `{"applicationId":"4743b130-0379-41bf-b863-ec8de96d915a"}`. Token held only in-process for
  that one call, never written to disk/env/db/log, discarded immediately.
  **Verified with the existing app-only executor**, not the user token: after fixing
  unrelated, pre-existing dangling `node_modules` symlinks in the shared checkout that were
  blocking any direct `tsx` run (filed as #1974; worked around locally with worktree-scoped
  junctions, discarded, no lasting change), `scripts/verify-power-platform-1869.mts` ran clean:
  - `probePowerPlatformReachability()` → `state: ok`, 2 environments
  - `listDlpPolicies()` → **0 policies** — a real, confirmed governance finding for
    tenant `c4c814d4-3afe-441e-9145-62461d0a4fd3`: this tenant currently has zero DLP
    policies configured.
  - `listEnvironments()` → 2 real environments returned (incl. "McCawSoft", Production sku)
  - `getTenantSettings()` → real 404 (endpoint/verb bug, pre-existing, unrelated to
    enrolment — filed as #1973)
  All reads confirmed working where every one previously 403'd. No DLP policy created,
  modified or deleted; no other tenant change. Filed the onboarding consequence as #1972
  (parented under #1485), the `getTenantSettings` 404 as #1973 (parented under #1096), and
  the dangling-symlink infra bug as #1974 (parented under #1202) — all three set to board
  status "AI Batter Up". `git status --porcelain` clean; branch verified merged into
  `origin/main` via `verify-branch-merged.mjs`.
