# #2896 — Team Management and Invitations (MSP Console): regenerate the contract pack

- **Status:** ⏳ IN FLIGHT 2026-09-04T20:08:55Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-04T20:08:55Z ⏳ IN FLIGHT — full line-by-line re-audit of
  `docs/team-management-and-invitations-msp-console-contract-pack.md` against the real,
  current `msp-team.ts` and related backend, per #2896. Confirmed real drift since the
  #2638 extraction: #2822's `mspId` fix landed (invite now reads `tenantsTable.mspId` for
  the target tenant, adds a new 404 "Customer not found" case), and #2832 landed
  independently (not named in #2896's own body — found during the audit) converting the
  two `assertCustomerAccess` 403s in this file (§0.1/§1/§2) and `requireRole`'s own 403
  to the nested `apiError()` envelope, while `resolveTargetCustomerId`'s 400/403/404s
  (§0.2, used by all 8 §4 routes) remain the old bare-string shape — a real, current
  inconsistency the old pack predates. #2527 added `users.managerUserId` +
  `PATCH /portal/team/:userId/manager`, Portal-side only; msp-team.ts has no equivalent
  route or roster field — documenting as a cross-surface gap, not filing (matches #2567's
  own build-order: extraction only, no page-shape decisions).
