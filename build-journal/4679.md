# #4679 — Self-halted-blocked builds never auto-resume when their real blockers clear

- **Status:** ⏳ IN FLIGHT 2026-09-18T14:52:47Z
- **Scope:** buildconsole
- **Started:** 2026-09-18

## Log
- 2026-09-18T14:52:47Z ⏳ IN FLIGHT — Investigating why a build that self-halts blocked (writes a 🛑 BLOCKED bookend + `blocked` label, wires a `blocked_by` edge, exits 0) never auto-resumes once its real blockers clear. Confirmed via code + live `bt_build_queue`: such a row lands at `verifying` (exit 0, has github_number), and none of the three existing recovery paths reach a `--cwd`/cross-repo build. Distinct gap from #3517/#3521 (which key off the `canceled+exit0` state the row never reaches). Plan written to build-journal/4679-plan.md.
