# #1359 — Free Scan: Prospect "view your results" signed return link

- **Status:** ✅ DONE 2026-09-13T07:12:01Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 1c45dd7fb (token table, lib, routes, consent email, page), 9c6053cdf (real-Postgres tests), 15932bbe9 (rework onto #1358's locked results), 8b5eb2de7 (manifest)

## Log
- 2026-09-13T06:33:36Z ⏳ IN FLIGHT — building a distinct, narrowly-scoped signed return-link token (own table, not accountSetupTokensTable), real Graph email delivery, and replay-resistance verification against /setup-password + authenticated portal routes.
- 2026-09-13T07:12:01Z ✅ DONE — Shipped a separate `free_scan_return_links` table (sha256 of the token only, CHECK pins `purpose` to `free_scan_results`, `fsr_` + 43 base64url token, 14-day TTL, minting a new link revokes older ones). Nothing in `/auth/*` reads this table. `POST /api/public/free-scan/return-link/results` re-checks on every use that the account is still an active, unentitled Free Prospect of the same customer, then serves #1358's locked payload (`lib/free-scan-locked-results.ts`, extracted from #1358's route so both doors share one projection with no `recommendation`). No cookie, JWT or email is returned. The consent callback emails the link via Graph to free-assessment Prospects; `POST /api/public/free-scan/return-link` re-sends it with the same 202 whether or not the account exists. `/scan/results` opens FreeScan.tsx's own results phase in return mode (token read from the URL fragment, then stripped). Purge declaration added for `free_scan_return_links.customer_id`. Migration `lib/db/migrations/manual/2026-09-13-free-scan-return-links-1359.sql` run on local and logged on #1630. **Verification:** api-server and marketing tsc clean; vitest 25/25 (new free-scan-return-link.test.ts, #1358 public-free-scan-results.test.ts, #1355 free-scan-prospect.test.ts, tenant-scope-coverage.live-db) on real Postgres. Live, on an isolated api-server (:18359) built from this code with seeded synthetic rows: the valid token returned 200 with the locked payload and no recommendation. The token was refused by /auth/setup-password (400), setup-context (404), reset-password (400), signup-, impersonate-, print- and document-print-exchange (401), refresh via body and cookie (401), mfa/verify (401), and Bearer on /auth/me/context, /auth/sessions, /portal/pillars, /portal/customer/timeline and /msp/auth/me (401). No response set a cookie or carried a JWT, and no setup token, reset token, session or refresh token was created. Entitled account got 403, expired link 410, tampered token 401. Real Graph send via the re-send route to zz-test-graphwrite-01@mccawsoft2.onmicrosoft.com was accepted (Email sent, email_events row, no failed_notifications). Manifest `marketing/free-scan-page.json` sessionTag `1359-return-link` apiTests ran 5/5 against the isolated build. **Not verified:** `shaneapp://runTest` was not run, because shared :8080 does not serve the new route until the FreeScan build set restarts (confirmed 404), so the new uiSteps have not been executed. The consent-callback email branch was not driven live, because it needs a real Microsoft admin consent; it is typechecked and code-reviewed only. **Findings filed:** none new; the stale war-room-pillars reference is already #3954. Seeded test rows deleted. No pre-existing dirty files.
