# #1646 — Send to Builder blank window / Queue builds exit -1

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** cf3c814d9 (bookend), 2b0499637 (fix)

## Log
- 2026-08-29 ⏳ IN FLIGHT — investigating both symptoms with real evidence (DB query,
  log files, ActivityLog, direct repro) per the issue's mandated order.
- 2026-08-29 ✅ DONE — Root-caused Symptom 2 (Queue exit -1/1): every recent worktree
  provisioning call succeeded (real path returned) but `provision-worktree.mjs`
  unconditionally wrote a plain-text "env : copied/skipped ..." line to stdout via
  `logEnvCopy()` BEFORE the final `--json` line (#1633 landed this print
  unconditionally). Reproduced live: `JsonDocument.Parse(stdout.Trim())` in
  `WorktreeProvisionService.cs` throws on that leading text every time (confirmed via
  a direct PowerShell `System.Text.Json` repro), so the C# catch-block reports
  `Ok=true` with a null `Path`/`Error`. `QueueWatcherService.LaunchItem` then reads
  that as a provisioning failure with a blank reason ("Worktree provisioning FAILED
  ... : .") and calls `MarkLaunchFailedAsync` (hardcoded exit_code 1) — before
  `claude.exe` is ever started. Confirmed via `bt_build_queue` (queue #816-819, #781
  all `failed`/exit_code=1) and the ActivityLog `watcher`/`worktree.provision`
  channels showing `path=` empty on every failure since ~14:29. No
  `queue-<id>.log` file exists for any of these ids — proof claude.exe never
  launched. Fixed: moved `logEnvCopy()` out of `provisionWorktree()` into `main()`'s
  non-`--json` branch only, so `--json` stdout is pure JSON again. Verified fixed by
  re-running the exact failing provisioning name (`1644-q819`) through both `--json`
  (now parses clean via the same `System.Text.Json` call C# makes) and the plain path
  (still prints the env notice for a human running it directly).
  Symptom 1 (Send to Builder blank window): confirmed NOT broken — `run-claude.ps1`
  correctly parsed and launched a real `claude.exe` for queue #820 (mybuilder-last-run.log
  shows the real prompt received at 15:21:01, 3 live claude.exe processes running).
  This is the unannounced side effect #1638 already predicted: stdout/stderr are now
  redirected into `queue-820.log` instead of the visible console, so the window looks
  blank even though the launch worked. Secondary finding: while the build is still
  running, `queue-820.log` stays at 0 bytes (buffered, not streamed) — Start-Process's
  file-redirection doesn't flush until the process exits, so the log looks equally
  "blank" mid-run, compounding the console being blank. Not fixed in this session
  (separate, non-blocking UX gap from the exit -1 bug) — worth a follow-up if Shane
  wants live tailing during a Send-to-Builder run. Different root causes for the two
  symptoms; only Symptom 2 was an actual blocking bug.
  `git status --porcelain` clean, branch verified merged into origin/main
  (2b0499637).
