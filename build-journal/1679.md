# #1679 — Git Board refresh still freezes input + critter animations (residual RenderIssuesTree UI-thread block)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** 8eb3d566c (real work), bd46dd77e (IN FLIGHT bookend) — both on `main`

## Log
- 2026-08-29 ⏳ IN FLIGHT — follow-up to #1635, which cut the redundant second RenderIssuesTree call but left the first ~1.5-2s synchronous rebuild blocking the UI thread on every refresh (measured in #1635's off-screen harness at 257 open issues / 49 epics / 10 milestones). Plan per issue body: try the chunked dispatcher-frame fix first (yield between batches so input/animation ticks interleave), escalate to full MVVM/virtualization restructure only if chunking genuinely fails.
- 2026-08-29 ✅ DONE — Shipped in 8eb3d566c (`main`). Took path 1 (chunked dispatcher frames) — it solved it; the MVVM/virtualization restructure was NOT needed. `RenderIssuesTree` became `RenderIssuesTreeAsync` (with an exception-guarded `async void` wrapper keeping every synchronous call site unchanged): construction still runs on the UI thread, but yields via `Dispatcher.Yield(DispatcherPriority.Background)` whenever a burst exceeds 40ms, at every loop level including the recursive `PopulateIssueTreeHierarchyAsync` (so a deep epic like #1485 can't form one long burst). A version stamp (`_issuesTreeRenderVersion`) supersedes an in-progress render the moment a newer one starts — the stale pass bails at its next yield without touching the tree. Verified via an off-screen WPF harness replicating #1635's exact measurement shape (257 issues / 49 epics / 10 milestones, warm, with a 16ms dispatcher timer as the input/animation proxy, incremental layout at each yield): worst continuous dispatcher stall OLD ~2.5-4.3s → NEW 166-195ms across 3 runs (~93-95% cut), wall time roughly unchanged. Residual worst burst is a single milestone's layout attach — a few dropped frames at worst, input processes between every burst. New per-render `git-board.data` log line reports wall / total blocked / burst count / longest burst so Shane's own next real refresh gives live numbers. `dotnet build -c Release`: 0 errors (46 pre-existing warnings, unchanged). Branch verified merged into origin/main via verify-branch-merged.mjs before writing this. Not clicked live in Shane's running BuildConsole (same constraint as #1635 — would mean restarting his production process or popping a second GUI window); the harness measures the identical construction+pacing pattern, and the in-app log line will show the real numbers on his next refresh.
