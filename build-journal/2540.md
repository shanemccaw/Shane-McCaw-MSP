# #2540 — Chats Panel doesn't refresh when Focus is engaged

- **Status:** ✅ DONE 2026-09-03T14:10:08Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** 9fbfefee3

## Log
- 2026-09-03T14:06:42Z ⏳ IN FLIGHT — subscribing LeftSidebar to FocusModeService.StateChanged so the Chats panel re-renders when Focus is engaged/disengaged, matching FocusImmersiveView/HomeView's existing subscribe/unsubscribe pattern.
- 2026-09-03T14:10:08Z ✅ DONE — Real audit confirmed: `LeftSidebar` (Controls/LeftSidebar.xaml.cs) had NO subscription to `FocusModeService.Instance.StateChanged` anywhere in the file (grep confirmed zero hits before the fix), unlike `FocusImmersiveView.xaml.cs:95/113` and `HomeView.xaml.cs:87/96` which both wire+unwire it. Fix (9fbfefee3): `LeftSidebar`'s constructor now does `FocusModeService.Instance.StateChanged += OnFocusStateChanged;` (wrapped in try/catch, matching HomeView's pattern) and unsubscribes on `Unloaded`, matching FocusImmersiveView's pattern (LeftSidebar itself lives for the MainWindow's lifetime as a named XAML child, same lifetime shape as the existing unguarded `ShaneAppStreamService.Instance.StatusChanged` subscription already in that constructor). `OnFocusStateChanged` dispatches to the UI thread and calls `RenderChatsTree()` directly (not `PopulateChatsTree()`) because engaging/disengaging Focus changes only `RenderChatsTree`'s own `milestoneMode` gate (`focusSvc.IsActive && activeMilestone.HasValue && _lastBoardIssues.Count > 0`, LeftSidebar.xaml.cs:2996-3002) — the board data itself (`_lastBoardIssues`) is unchanged, so a cheap re-render is correct and matches the file's own documented rule at line ~2444 ("Chats — MUST call RenderChatsTree directly ... NOT PopulateChatsTree"). Falls back to a full `PopulateChatsTree()` only if `_lastBoardIssues` is still empty (board hasn't loaded yet), the same fail-open shape `PopulateChatsTree` itself already documents. **Real trace verification** (cannot launch BuildConsole.exe to click-test live, per the issue's own standing rule): engaging Focus → `FocusModeService.ToggleFocus`/`SetActiveMilestone`-style setters call `RaiseStateChanged()` (FocusModeService.cs:731) → `StateChanged` event now fires `LeftSidebar.OnFocusStateChanged` (new subscriber) in addition to the two pre-existing subscribers → `Dispatcher.Invoke` → `RenderChatsTree()` re-evaluates `milestoneMode` fresh against the now-current `focusSvc.IsActive`/`ActiveMilestoneNumber` → the Chats panel repaints scoped to the just-engaged milestone instead of the stale legacy all-epics view. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 Warning(s), 0 Error(s). Branch verified merged into origin/main (`verify-branch-merged.mjs` — OK). `git status --porcelain` clean. No findings filed — this was a scoped, fully-fixable bug with a confirmed root cause and no side discoveries.
