# #1598 — Contract extraction pack for Billing (read-only, for Design)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** `49d3b424d` (IN FLIGHT bookend), `9bc8c4d71` (docs/billing-contract-pack.md) — rebased onto main as `a9f926d4a` (docs/billing-contract-pack.md content) on origin/main


> **CORRECTION (2026-08-29): this bookend was written against an EPIC, in error.**
>
> This build was dispatched with `--title 1598`, but #1598 is a module epic under #1485 — a
> container for scope, not a unit of work. The `DONE` above refers **only to the contract
> extraction pack this session produced**. It does **not** mean module #1598 is complete.
>
> #1598's real state is its children. At the time of this correction the #1485 tree stood at
> 5 of 92 children closed.
>
> This mistake made all thirteen module epics read as finished, which led to twelve of them
> being closed in one sweep with their children still open, and later caused a re-dispatch
> against #1494 to short-circuit on this file. Builds now target leaf issues only — see
> `CLAUDE.md`, "`--title` must be a LEAF issue, never an epic."


## Log
- 2026-08-28 ⏳ IN FLIGHT — Pulling #1598 body/comments, #1485, #1577; extracting Billing
  Wire* contract (invoices, receipts, plan state), enum unions, money contract, the
  #1463 honest-empty fix, portal-v2-receipt.tsx fallback case, My Architect (#1569)
  boundary, and #1128/#1594 forbidden-list status into a .md pack for Design. Read-only —
  no product code changes expected.
- 2026-08-28 ✅ DONE — Shipped `docs/billing-contract-pack.md` on origin/main (verified
  ancestor via `verify-branch-merged.mjs`: "OK — 'agent/1598-q776' (a9f926d4) is merged
  into 'origin/main'"). Read-only, no product code touched. Covers invoices
  (`GET /portal/invoices` → `WireInvoice`/`BillingReceiptRow`), the Stripe-sourced
  receipt detail page (`GET /portal/billing/stripe-receipts`), and plan state
  (`GET /portal/billing/subscriptions` + cancel/resume/resubscribe/customer-portal —
  real and CURRENT on the route, but unwired into `portal-v2-billing.tsx`'s Monitoring
  tiles, which stay fixture per #1594/#1128). Real enum unions extracted for invoice
  status/type, client-service status, billing type/interval, plus Stripe's own
  pass-through invoice/subscription status vocabularies (not ours to redefine). Money
  contract: single platform Stripe account confirmed by absence of any
  `stripeAccount`/Connect usage; the one genuinely integer-cents field is
  `StripeReceiptWire.amount` (receiptWire.ts:19, off Stripe's own `amount_paid`) —
  flagged honestly that `invoicesTable.amount`, the field Receipts itself renders, is
  actually `numeric(10,2)` decimal dollars, not cents, converted to cents only at the
  Stripe API call boundary. Confirmed and documented the #1463 honest-empty fix
  (`rows !== null ? "live" : "fixture"`, zero real invoices is "live" not "fixture").
  Documented `portal-v2-receipt.tsx`'s fixture fallback as a legitimate per-item Stripe
  lookup miss, not a violation. New finding for Design and for whoever architects My
  Architect's own contract pack next: `portal-v2-retainer.tsx:302-304,369` currently
  renders a real live dollar figure (`hourlyRateCents`-derived `$X/mo`) on the "My
  Architect" page, which contradicts #1569's own stated boundary ("time only, never
  money") — recorded, not fixed here (out of Billing's scope). Posting summary comment
  on #1598 next; issue left open per instructions.
