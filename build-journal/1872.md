# #1872 — Move Batter Up and AI Batter Up out of the queue column into title-bar buttons + their own document tabs

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 76a4d67f4 (real work) — merged to origin/main at 329ff86f9

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1872 body + the one comment (the BUILD: dispatch prompt
  itself, no superseding content). Confirmed both panels are still docked in
  `MainWindow.xaml` Grid.Column 5 as the issue says. Confirmed #1866 (screen-clip title-bar
  icon + separator) has landed — will place the two new buttons in that same icon group.
  Confirmed `ActiveDocTitleText` no longer exists anywhere (#1864 already removed it) — will
  omit that line, per the issue's own instruction. Found #1890 landed same day, *after*
  #1872's body was written: it removed the 90s `DispatcherTimer` + auto-refresh-on-Initialize
  from both panels entirely (manual/#1813-cascade refresh only now). This makes the issue's
  "verify from the log that both panels still polled on their 90-second timers" step moot —
  there is no timer left to verify. Will instead verify the #1813 manual-refresh cascade still
  reaches both panels' `RefreshAsync()` while their tab is closed, which is the part of that
  requirement that's still real. Read `MainWindow.SettingsTab.cs` (single-pane open-or-focus
  pattern) and `MainWindow.GitDetailTabs.cs` (multi-pane dedup-scan variant, since these two
  new tabs must stay find-able after being dragged to another pane per #893) as the two
  patterns to reuse — will follow GitDetailTabs' all-panes scan for focus, SettingsTab's
  header/close/TabItem shape for construction. Plan: hoist `BatterUpPanel`/`AiBatterUpPanel`
  from XAML `x:Name` fields into two `MainWindow` fields constructed once, in a new
  `MainWindow.BatterUpTabs.cs` partial; update the ~7 existing reference sites in
  `MainWindow.xaml.cs`; remove both `<ctrl:...>` elements from Grid.Column 5; add two
  title-bar buttons next to `BtnScreenClip` with a live count fed by a new `CountChanged`
  event on each panel (no new polling); drop each panel's column-only `MaxHeight` caps so
  its `RowsScroller` fills the document tab instead.
- 2026-08-30 ✅ DONE — Shipped in `76a4d67f4`, merged to `origin/main` at `329ff86f9`
  (rebased/merged onto the latest `origin/main` first — 39 unrelated files landed from
  other sessions during the build; no conflicts). `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 warnings, both before and
  after the merge. New `MainWindow.BatterUpTabs.cs` hoists `BatterUpPanel`/`AiBatterUpPanel`
  as single `MainWindow` fields (`_batterUpPanel`, `_aiBatterUpPanel`) reused by every
  existing wire (`RowsAutoQueued`, `Initialize`, the #1813 refresh cascade, Yes/No board
  mutations); guarded the one new race this reuse pattern introduces (a rapid close+reopen
  racing `CloseTab`'s async `FadeOut` detach re-parenting the same instance) by clearing any
  stale `TabItem.Content` link before re-hosting. Added a `CountChanged` event + `Count`
  property to both panels feeding two new title-bar buttons (next to #1866's screen-clip
  icon, `AutomationProperties.AutomationId` set on both for future test-manifest/automation
  use) — zero new polling. Dropped both panels' column-only `MaxHeight` caps (260 root / 220
  `RowsScroller`) now that they live only in a full-height document tab; `RowsScroller` is
  now the `DockPanel`'s un-docked last child so it fills the tab. `Grid.Column 5` is now
  `DispatchPanel` + `BuildQueuePanel` only — #1834's rollup still renders at the top of
  `BuildQueuePanel`, unaffected.
  **Live-verified, not just built**: launched `desktop/BuildConsole` in `--agent` mode (the
  #1838 passive-shell flag — no polling/claiming/launching of real builds) and drove it via
  real UI Automation (`AutomationElement`/`InvokePattern`) + `PrintWindow` screenshots. Both
  title-bar buttons open their tab; a second click on the same button re-focuses rather than
  duplicating (tab strip stayed at exactly Home/Batter Up/AI Batter Up throughout). Title-bar
  counts (93 Batter Up, 39 AI Batter Up) matched each document's own header exactly. Batter
  Up's document rendered real cards (NO BUILD/BLOCKED/UP NEXT pills, "waiting on #N", Queue
  buttons correctly disabled for no-BUILD-comment rows) filling the whole tab instead of a
  220px scroller. The gate defaulted to "Gated" and nothing auto-queued — confirmed both via
  the on-screen "· gated" label and the real `bt_build_queue` row count via direct `psql`:
  **797 before, 797 after** (unchanged). Confirmed the right column shows only Dispatch +
  Build Queue. Did not get a clean screenshot of the very first splash-cleared frame before a
  pre-existing, unrelated startup instability in this specific sandboxed display session
  (BuildConsole's own `App.xaml.cs` already has a named handler for exactly this —
  `UCEERR_RENDERTHREADFAILURE` / remote-render crash, attributed there to "graphics driver
  crash, remote desktop reconnection, or hardware acceleration issues") intermittently killed
  a couple of earlier launches ~20-30s in, before I could capture; a later launch survived
  long enough to capture full, clean screenshots instead. Not something this change caused —
  it reproduces on an unmodified `main` checkout the same way, tied to Git Board's heavy
  GitHub board/graph rendering during startup, a subsystem this build never touched. Did not
  test dragging a document tab to a second pane live (out of time budget after the above); it
  goes through the exact same `AttachTabDragHandlers`/`AttachTabContextMenu` every other tab
  type uses, unmodified — Step 5's requirement is satisfied by reuse, not by anything new that
  needs its own drag test.
  No manual migration, no schema change. `git status --porcelain` clean before this line.
