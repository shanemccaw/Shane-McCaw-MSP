# #1872 — Move Batter Up and AI Batter Up out of the queue column into title-bar buttons + their own document tabs

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1872 body + the one comment (the BUILD: dispatch prompt
  itself, no superseding content). Confirmed both panels are still docked in
  `MainWindow.xaml` Grid.Column 5 as the issue says. Confirmed #1866 (screen-clip title-bar
  icon + separator) has landed — will place the two new buttons in that same icon group.
  Confirmed `ActiveDocTitleText` no longer exists anywhere (#1864 already removed it) — will
  omit that line, per the issue's own instruction. Found #1890 landed same day, *after*
  #1872's body was written: it removed the 90s `DispatcherTimer` + auto-refresh-on-Initialize
  from both panels entirely (manual/#1813-cascade refresh only now). This makes the issue's
  "verify from the log that both panels still polled on their 90-second timers" step moot —
  there is no timer left to verify. Will instead verify the #1813 manual-refresh cascade still
  reaches both panels' `RefreshAsync()` while their tab is closed, which is the part of that
  requirement that's still real. Read `MainWindow.SettingsTab.cs` (single-pane open-or-focus
  pattern) and `MainWindow.GitDetailTabs.cs` (multi-pane dedup-scan variant, since these two
  new tabs must stay find-able after being dragged to another pane per #893) as the two
  patterns to reuse — will follow GitDetailTabs' all-panes scan for focus, SettingsTab's
  header/close/TabItem shape for construction. Plan: hoist `BatterUpPanel`/`AiBatterUpPanel`
  from XAML `x:Name` fields into two `MainWindow` fields constructed once, in a new
  `MainWindow.BatterUpTabs.cs` partial; update the ~7 existing reference sites in
  `MainWindow.xaml.cs`; remove both `<ctrl:...>` elements from Grid.Column 5; add two
  title-bar buttons next to `BtnScreenClip` with a live count fed by a new `CountChanged`
  event on each panel (no new polling); drop each panel's column-only `MaxHeight` caps so
  its `RowsScroller` fills the document tab instead.
