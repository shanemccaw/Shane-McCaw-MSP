# #3460 — Feature: Script Library (MyArchitect)

- **Status:** ⏳ IN FLIGHT 2026-09-10T17:58:12Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 54abd1490 (LaunchControlActionsService — real, shipped scope)

## Log
- 2026-09-10T05:46:14Z ⏳ IN FLIGHT — Pulled full issue body + 4 comments. GEMINI.md's
  standing rule ("UI Shell first, no exceptions") and the earlier BUILD comment's
  `--blocked-by 3493` both block the two UI checklist items (Library browse UI,
  Run-into-console wiring) until #3493 (Feature: UI Shell Redesign) actually lands
  in code — confirmed it hasn't: MainWindow.xaml still uses plain `ui:FluentWindow`
  (WPF-UI), no Fluent.Ribbon package, no 6-tab shell, no contextual-tab mechanism;
  only UI_RULES.md (the spec doc) has landed. #3493 has no build-journal file and
  is still OPEN/in-flight. Proceeding with the one checklist item that is genuinely
  not UI chrome: the real client + caching for
  `GET /msp/:mspId/launch-control/actions?customerId=X`, which the Feature UI will
  consume once #3493 lands.
- 2026-09-10T05:53:55Z 🛑 BLOCKED — Shipped `LaunchControlActionsService` +
  `ILaunchControlActionsService` (`desktop/MyArchitect/Services/`) +
  `LaunchControlAction`/`LaunchControlCatalog` models
  (`desktop/MyArchitect/Models/LaunchControlAction.cs`): real HTTP client for
  `GET /api/msp/:mspId/launch-control/actions?customerId=X`, short-TTL
  per-MSP+customer in-memory cache with `forceRefresh`/`InvalidateCache`, and a
  real `LaunchControlActionsException` (status + body) instead of a silent
  swallow. `dotnet build` — 0 errors, 0 warnings. Commit 54abd1490 (pushed to
  main via rebase, hash `4152dde8e` on main).
  Remaining checklist items — Library browse UI (category-grouped) and
  run-into-console wiring with parameter pre-fill — are genuinely blocked:
  GEMINI.md's "UI Shell first, no exceptions" standing rule and the earlier BUILD
  comment's `--blocked-by 3493` both require #3493 (Feature: UI Shell Redesign)
  to land before any other Feature's UI work. Confirmed #3493 hasn't landed in
  code: `MainWindow.xaml` is still plain `ui:FluentWindow` (WPF-UI), no
  Fluent.Ribbon package reference, no 6-tab shell, no contextual-tab mechanism —
  only its spec doc (UI_RULES.md) has landed. #3493 has no `build-journal/3493.md`
  and is still OPEN/`in-flight`. `blocked_by` edge #3460→#3493 was already present
  (confirmed via the dependencies API) and left in place; `blocked` label applied,
  `in-flight` removed.
  Also filed #3501 (parented under #3454, board status "AI Batter Up"): MyArchitect
  has no auth/session mechanism anywhere — no Authorization header, no token, no
  mspId concept in any service — so this client (and every other auth-gated
  MyArchitect call) will 401/403 until that's built. That's a separate,
  cross-cutting gap, not something #3460 itself should fix.
  `git status --porcelain` clean at session end; no other files were dirty on
  arrival.
- 2026-09-10T17:58:12Z ⏳ IN FLIGHT — Resuming. Latest dispatch comment
  (2026-09-10T14:26:03Z) confirms #3493 (UI Shell Redesign) is now genuinely
  CLOSED — verified independently: `gh issue view 3493` shows `state: CLOSED`,
  real commit e8460c5fd merged (`git merge-base --is-ancestor` on origin/main),
  Fluent.Ribbon shell, `ShellRegistry`/`FixedRibbonRenderer`, contextual tabs
  and the Console tab all present in code. The stale `blocked_by` edge
  #3460→#3493 has been removed (it pointed at a closed issue and was silently
  releasing nothing further downstream, but was stale per Git #1987's rule
  regardless) and replaced with #3460→#3501, the real current blocker — #3501
  ("MyArchitect has no auth/session mechanism") is confirmed still OPEN, and is
  what actually gates live Script Library rows: `Tenant` carries only a
  `TenantGuid` (string), never a numeric `mspId`/`customerId`, so
  `LaunchControlActionsService.GetActionsAsync` still cannot be called for
  real. `in-flight` label applied, `blocked` removed while real remaining work
  proceeds.
  Real remaining scope per the latest dispatch: build the Gallery row mapping
  (tile/name/sub from real `LaunchControlAction` fields, domain-based
  grouping) and the run-into-console wiring with `requiredVariables` pre-fill,
  as real production code — not exercised against live data (still blocked by
  #3501) but not a fixture either: it operates on the real
  `LaunchControlAction`/`LaunchControlCatalog` model shapes the endpoint
  actually returns, and `BuildScriptLibraryRows()` keeps stating the honest
  block (no invented mspId) until #3501 lands.
