# #3460 — Feature: Script Library (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T19:05:35Z
- **Scope:** buildconsole (desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 54abd1490 (LaunchControlActionsService), b2211c36f (Gallery mapping +
  execute wiring), bb3c74f34 (#3501 — landed mspId resolution into
  `TryResolveLaunchControlScope` as part of its own auth wiring)

## Log
- 2026-09-10T05:46:14Z ⏳ IN FLIGHT — Pulled full issue body + 4 comments. GEMINI.md's
  standing rule ("UI Shell first, no exceptions") and the earlier BUILD comment's
  `--blocked-by 3493` both block the two UI checklist items (Library browse UI,
  Run-into-console wiring) until #3493 (Feature: UI Shell Redesign) actually lands
  in code — confirmed it hasn't: MainWindow.xaml still uses plain `ui:FluentWindow`
  (WPF-UI), no Fluent.Ribbon package, no 6-tab shell, no contextual-tab mechanism;
  only UI_RULES.md (the spec doc) has landed. #3493 has no build-journal file and
  is still OPEN/in-flight. Proceeding with the one checklist item that is genuinely
  not UI chrome: the real client + caching for
  `GET /msp/:mspId/launch-control/actions?customerId=X`, which the Feature UI will
  consume once #3493 lands.
- 2026-09-10T05:53:55Z 🛑 BLOCKED — Shipped `LaunchControlActionsService` +
  `ILaunchControlActionsService` (`desktop/MyArchitect/Services/`) +
  `LaunchControlAction`/`LaunchControlCatalog` models
  (`desktop/MyArchitect/Models/LaunchControlAction.cs`): real HTTP client for
  `GET /api/msp/:mspId/launch-control/actions?customerId=X`, short-TTL
  per-MSP+customer in-memory cache with `forceRefresh`/`InvalidateCache`, and a
  real `LaunchControlActionsException` (status + body) instead of a silent
  swallow. `dotnet build` — 0 errors, 0 warnings. Commit 54abd1490 (pushed to
  main via rebase, hash `4152dde8e` on main).
  Remaining checklist items — Library browse UI (category-grouped) and
  run-into-console wiring with parameter pre-fill — are genuinely blocked:
  GEMINI.md's "UI Shell first, no exceptions" standing rule and the earlier BUILD
  comment's `--blocked-by 3493` both require #3493 (Feature: UI Shell Redesign)
  to land before any other Feature's UI work. Confirmed #3493 hasn't landed in
  code: `MainWindow.xaml` is still plain `ui:FluentWindow` (WPF-UI), no
  Fluent.Ribbon package reference, no 6-tab shell, no contextual-tab mechanism —
  only its spec doc (UI_RULES.md) has landed. #3493 has no `build-journal/3493.md`
  and is still OPEN/`in-flight`. `blocked_by` edge #3460→#3493 was already present
  (confirmed via the dependencies API) and left in place; `blocked` label applied,
  `in-flight` removed.
  Also filed #3501 (parented under #3454, board status "AI Batter Up"): MyArchitect
  has no auth/session mechanism anywhere — no Authorization header, no token, no
  mspId concept in any service — so this client (and every other auth-gated
  MyArchitect call) will 401/403 until that's built. That's a separate,
  cross-cutting gap, not something #3460 itself should fix.
  `git status --porcelain` clean at session end; no other files were dirty on
  arrival.
- 2026-09-10T17:58:12Z ⏳ IN FLIGHT — Resuming. Latest dispatch comment
  (2026-09-10T14:26:03Z) confirms #3493 (UI Shell Redesign) is now genuinely
  CLOSED — verified independently: `gh issue view 3493` shows `state: CLOSED`,
  real commit e8460c5fd merged (`git merge-base --is-ancestor` on origin/main),
  Fluent.Ribbon shell, `ShellRegistry`/`FixedRibbonRenderer`, contextual tabs
  and the Console tab all present in code. The stale `blocked_by` edge
  #3460→#3493 has been removed (it pointed at a closed issue and was silently
  releasing nothing further downstream, but was stale per Git #1987's rule
  regardless) and replaced with #3460→#3501, the real current blocker — #3501
  ("MyArchitect has no auth/session mechanism") is confirmed still OPEN, and is
  what actually gates live Script Library rows: `Tenant` carries only a
  `TenantGuid` (string), never a numeric `mspId`/`customerId`, so
  `LaunchControlActionsService.GetActionsAsync` still cannot be called for
  real. `in-flight` label applied, `blocked` removed while real remaining work
  proceeds.
  Real remaining scope per the latest dispatch: build the Gallery row mapping
  (tile/name/sub from real `LaunchControlAction` fields, domain-based
  grouping) and the run-into-console wiring with `requiredVariables` pre-fill,
  as real production code — not exercised against live data (still blocked by
  #3501) but not a fixture either: it operates on the real
  `LaunchControlAction`/`LaunchControlCatalog` model shapes the endpoint
  actually returns, and `BuildScriptLibraryRows()` keeps stating the honest
  block (no invented mspId) until #3501 lands.
- 2026-09-10T18:07:38Z 🛑 BLOCKED — Investigated the real
  `POST /api/msp/:mspId/launch-control/execute` route (`msp-launch-control.ts:185-302`)
  end to end — request `{catalogActionId, customerId, variables}`, response
  `{result: BaselineTemplateExecutionResult & {reversible}, tenant}` — and shipped
  the real remaining scope against that exact shape:
  - `LaunchControlExecuteResult`/`LaunchControlExecuteTenant`/
    `LaunchControlExecuteResponse` (`Models/LaunchControlAction.cs`) — mirror the
    real response fields exactly, nothing invented.
  - `ILaunchControlActionsService.ExecuteAsync` /
    `LaunchControlActionsService.ExecuteAsync` — real POST client, same
    auth-header/`LaunchControlActionsException` pattern as the existing GET.
  - `ConsolePanelView.AppendExternal` — lets an execute result land in the same
    output pane a typed command's output lands in (the execute route runs
    server-side, not through the hosted PS runspace, so this isn't feeding the
    result back through the runspace itself).
  - `MainWindow.BuildScriptLibraryRows` now does the real Gallery row mapping
    (UI_RULES.md §4 — tile = 2-char safe/gated code, the destructive-vs-read-only
    signal the spec asks for; sub = domain + availability/status; sorted by
    Domain then SortOrder) whenever `TryResolveLaunchControlScope` can resolve a
    real id pair. `OpenScriptLibraryRecord` opens the real record workspace —
    real facts, one write-through `WorkspaceEdit` per real `RequiredVariables`
    entry — and the confirm-armed "Run" action calls `RunScriptLibraryAction`,
    which executes for real and reports success/fail/missing-variables/
    audit-log-id into the Console tab. `TryResolveLaunchControlScope` still
    returns `false` — confirmed again this session that `Tenant` carries only a
    string `TenantGuid`, never a numeric `mspId`/`customerId`, and #3501 is still
    `OPEN` — so `BuildScriptLibraryRows` keeps stating the honest block instead
    of guessing an id. The mapping/wiring above it is real, not fixture: it
    operates on the real model shapes the endpoint returns and is ready to go
    the moment #3501 resolves the id.
  `dotnet build` — 0 errors, 0 warnings, verified again after rebasing onto
  origin/main (real conflicts in `MainWindow.xaml.cs`/`ConsolePanelView.xaml.cs`
  against a concurrent peer session's #3459 console-history/tenant-status work —
  resolved by keeping both sides' additions, non-overlapping insertion points,
  rebuilt clean after).
  Commit `b2211c36f`, pushed to `origin/main`,
  `git merge-base --is-ancestor` confirmed.
  Also filed #3539 (`GallerySpec.Searchable` set `true` by two real galleries —
  Change Requests from #3493, Script Library here — but never read anywhere in
  `FixedRibbonRenderer.BuildGalleryButton`, a dead property), parented as a
  sub-issue of #3493, board status "AI Batter Up", labeled `bug`.
  `blocked_by` edge #3460→#3501 left in place (still the real, current blocker);
  `blocked` label re-applied, `in-flight` removed. `git status --porcelain`
  clean at session end; no other files were dirty on arrival.
- 2026-09-10T19:05:35Z ✅ DONE — Resumed on "#3501 landed". Verified independently
  rather than taking the prompt on faith: `gh issue view 3501` shows `state:
  CLOSED`, labels `bug`+`complete`; `blocked_by` on #3460 listed #3501 as
  closed/complete, so removed the now-stale edge (Git #1987 — a comment saying
  "blocked" is worthless once the edge is gone or stale; here the edge itself
  confirmed real clearance). `blocked` label removed, `in-flight` re-applied
  while confirming.
  This session's own worktree (`C:\wt\3460-q2156`) came up completely empty —
  no `.git`, not registered in `git worktree list` — matching Git #2118 (launch
  into an empty/unprovisioned worktree). Self-healed via
  `provision-worktree.mjs 3460-q2156 --link --owner-pid $$` off current
  `origin/main` rather than guessing at lost work; confirmed via `git cat-file
  -t`/`merge-base --is-ancestor` from the main checkout first that both this
  issue's real commits (`b2211c36f`, bookend `32e330c1f`) and #3501's
  (`bb3c74f34`, bookend `9fc42f234`) were already safely merged into
  `origin/main` — no data was actually lost, only the local worktree.
  Read #3501's own bookend: it shipped `AuthService`/`SessionStore`/
  `LoginWindow`, fanned the resulting bearer token out to
  `LaunchControlActionsService.AuthToken` among others, and itself wired
  `MainWindow.TryResolveLaunchControlScope`'s `mspId` half from the real
  session (`_authService.MspId`). Confirmed in code
  (`MainWindow.xaml.cs:456-461`): `mspId` now resolves for real;
  `customerId` is still hardcoded `0` because `TenantService` is fixture data
  (fake tenant GUIDs, no real numeric `tenants.id`) — the #3501 session
  correctly identified this as a **separate, differently-scoped** gap and
  filed it as #3540, parented under #3457 (Feature: Core Shell + Tenant
  Switcher — the feature that actually owns `TenantService`), not under
  #3460. Rebuilding `TenantService` here would be scope creep into #3457's own
  named responsibility, so this session did not do that.
  All 3 of #3460's own checklist items are real, shipped, and reviewed in
  full this session (`MainWindow.xaml.cs:456-640` — `BuildScriptLibraryRows`,
  `OpenScriptLibraryRecord`, `RunScriptLibraryAction`, all operating on real
  `LaunchControlAction`/`LaunchControlCatalog`/`LaunchControlExecuteResponse`
  model shapes, no fixture data, honest fallback rows for the two real
  remaining reasons (`not signed in` / `no real customer — TenantService is
  fixture data`) instead of a fake row). `dotnet build` — 0 errors, 0
  warnings, verified again against the current `origin/main` tip. No new code
  changes were needed this session — the real remaining scope was already
  shipped by the prior BLOCKED entries; this entry's job was confirming the
  unblock was real, confirming nothing was lost, and closing out the bookend
  honestly.
  `verify-branch-merged.mjs` — worktree HEAD is `origin/main`'s current tip
  (no divergence to merge). `git status --porcelain` clean; no files were
  dirty on arrival. Labels: `in-flight` removed, `complete` added.
  `customerId` resolution (and therefore live Gallery rows / real executions)
  still needs #3540 to land first — that is #3457's scope, not a reopen of
  this issue.
