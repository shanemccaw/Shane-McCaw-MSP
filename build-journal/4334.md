# #4334 — Interrupted checkout blocks the buyer's own retry ("tenant already registered")

- **Status:** ✅ DONE 2026-09-16T02:49:22Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 56cac8081 (fix + tests), b943d4c30 (IN FLIGHT bookend)

## Log
- 2026-09-15T22:34:12Z ⏳ IN FLIGHT — Starting: fix the same-MSP branch of the cross-tenant consent guard in `artifacts/api-server/src/routes/consent.ts` (`GET /api/consent/callback`) so a create-path consent landing on a tenant that has never completed a paid purchase resumes onto the existing tenant row instead of refusing. Paid-tenant collision and the cross-MSP `endTenantConflict` branch stay unchanged.
- 2026-09-16T02:49:22Z ✅ DONE — Shipped in 56cac8081.

  **What changed** (`artifacts/api-server/src/routes/consent.ts`):
  - New `tenantHasCompletedPurchase(customerId, tenantGuid)` helper. Probe 1 (primary, cheapest): a `checkout_sessions` row with `status='paid'` matched on `tenant_id` — the GUID this callback itself stamps, one single-table lookup, no join, and the row every self-service purchase flips (`public-purchase-payment.ts:627`, `public-assessment-payment.ts:837`). Probe 2 (fail-closed backstop, only if probe 1 misses): an `active` `client_services` row joined to `users` on `users.tenant_id` (indexed `users_tenant_id_idx`), covering a purchase that never went through a checkout session at all. Both run only on the already-rare collision path; the second exists so a genuinely paying customer's tenant stays unattachable.
  - The same-MSP arm of the #4226 guard now splits on that answer: **paid → unchanged refusal** (`endTenantAlreadyRegistered()`, no token burn / session flip / grant / account, Microsoft not even asked); **never purchased → resume**, setting `resumeExistingCustomerId`.
  - New resolution branch (a2): resumes onto the existing row via `resolveCallbackTenant(id, tenant)` — the same id-bound, GUID-must-match shape the "invite names a customer" path uses — instead of calling `resolveOrCreateDirectTenant`/`resolveOrCreateTenantForMsp` (which would return `created:false` and be refused by the post-guard race check). A row that changed or vanished between the guard and here still fails closed.
  - Everything downstream is unchanged and runs against that row: `graph` + `sharepoint` stamps, the checkout-session flip to `consented` with this tenant, prospect provisioning, `consent.granted`, diagnostics, item-detail collection, DLP provisioning.
  - A resumed **invite** consent now also flips an `onboarding` customer to `active`, as a first-time one does (it would otherwise be parked in `onboarding` forever).
  - Ending copy: a resumed consent gets its own positive, self-closing popup — "Already connected — continuing to payment" — instead of the "contact support" dead end.
  - Two stale block comments above `endTenantConflict`/`endTenantAlreadyRegistered` that asserted "none may attach to an existing one" were corrected, since that is no longer true for the unpaid case.
  - **Cross-MSP `endTenantConflict` untouched** (out of scope, still fails closed, asserted by a new test).

  **Verification (real, run this session):**
  - `npx vitest run src/routes/consent.test.ts` → **88/88 pass**, including 5 new #4334 cases: unpaid resume lands on payment; unpaid-but-entitled still refuses; cross-MSP still refuses (and proves the purchase probes are never even reached); a resumed row that no longer matches the GUID fails closed; an MSP-invite resume succeeds and flips `active`. Three pre-existing same-MSP refusal tests were updated to seed a real paid session so they keep asserting the refusal for the paid case.
  - `npx tsc --noEmit -p tsconfig.json` (api-server) → clean.
  - Both probe queries run for real against local PostgreSQL 18 (`DATABASE_URL`): as raw SQL via psql, and as the exact Drizzle builders (including the `innerJoin`) via a throwaway script since deleted. The paid probe was also exercised against a real inserted `status='paid'` row inside a transaction that was rolled back — it matched the row, and `count(*)` after `ROLLBACK` confirmed zero leftovers.
  - `src/lib/free-scan-prospect.test.ts` (nearest live-DB neighbour) → 3/3 pass with `DATABASE_URL` set. Without it, it fails at import on the known pre-existing module-scope `@workspace/db` requirement — environmental, not this change.

  **Dispatch item 5 — `already_has_account` verified, no separate finding:** `public-services.ts:194-201` 409s only when `existingUser?.passwordHash` is set, and a consent-time Prospect is created with `passwordHash` left NULL (`direct-tenant-provisioning.ts:374`) — a Free Scan / unpaid prospect cannot set a password at all (`hasRealEntitlement` refuses). So the email check does **not** independently refuse the resumed buyer; the two checks agree. A buyer who has actually paid *does* have a password and is stopped there, which is consistent with the paid branch still refusing here.

  **Finding filed:** **#4339** — "Buy.tsx never rehydrates its checkout session — every reload before payment orphans the session (root cause behind #4334)". Parented as a sibling sub-issue of Feature #1309, labelled `bug`, milestone v1.1, board status "AI Batter Up". This fix makes the consequence survivable; the upstream orphaning is still live (`Buy.tsx:210` inits `sessionId: null`, writes the storage key at :542, and nothing but `useBuyPackLive.ts:195` — Pack stage only — ever reads it back).

  **Working tree:** clean. No files were dirty on arrival. No migration was needed (no schema change).
