# #1994 — Build Queue card right-click: open the build's Git issue document

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** d978aedec05a40d5be5cfc830e24f07949b626c6

## Log
- 2026-08-30 ⏳ IN FLIGHT — wiring "Open Git #N" (+ side-by-side variant) into
  `BuildQueuePanel.BuildCardContextMenu`'s shared tail (fires for every status
  branch since they all fall through to it), new `OpenGitIssueRequested` event,
  subscribed in `MainWindow.xaml.cs` to the existing `OpenGitDetailByNumberAsync`.
  Hidden when `GithubNumber` is null OR a negative `--notGit` local sentinel.
- 2026-08-30 ✅ DONE — `dotnet build BuildConsole.csproj -c Release` clean, 0
  warnings/errors. Menu item is added once at the shared tail of
  `BuildCardContextMenu` (after all the per-status if/elseif branches), so it
  appears on every state: queued, running, verifying, parked, limit-paused,
  capped, external, failed/done. Guard is `GithubNumber is int n && n > 0`, so
  it's hidden both for truly-null rows (e.g. Reply-spawned builds) and for
  `--notGit` local builds (negative sentinel per `LocalBuildId.FormatRef`) —
  neither is a real GitHub issue to open. Reuses the existing
  `MainWindow.OpenGitDetailByNumberAsync(number, sideBySide)` — no second
  issue-opening path built. Not live-clicked in Shane's running BuildConsole.exe
  (never relaunch/kill his live process per standing rule); verified by reading
  the wiring end-to-end and a clean Release build instead. Rebased onto
  origin/main and pushed; merge-base --is-ancestor confirms HEAD is on main.
