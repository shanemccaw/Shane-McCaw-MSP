# #2830 — Fix dead dev-server coordinator (add/add merge conflict + pino junction)

- **Status:** ✅ DONE 2026-09-04T23:35:00Z
- **Scope:** buildconsole (dev-server coordinator infra)

## Log
- 2026-09-04T00:00:00Z — IN FLIGHT. Starting: resolve add/add conflict on
  build-journal/1561.md / build-journal/1582.md in the shared C:\dev-server
  checkout, verify/repair the `pino` junction under
  artifacts/api-server/node_modules, then confirm restart succeeds.
- 2026-09-04T23:35:00Z — ✅ DONE. Root cause: the shared `C:\dev-server`
  checkout's `dev-server` branch had merged individual agent-branch commits
  directly for `build-journal/1561.md` and `build-journal/1582.md`
  (`91109541f`, merging commits from the `1561-q906`/similar agent branches
  rather than from `origin/main`), which left those two paths with a history
  permanently divergent from `origin/main`'s own commits for the same files
  (`ca4ae0f88` etc.) — an unrecoverable add/add conflict that reproduced on
  *every* subsequent `request-restart.mjs` call from any worktree, no matter
  whose commit was being merged. Fixed by merging `origin/main` into
  `C:\dev-server`'s `dev-server` branch directly, resolving the two add/add
  conflicts by taking `origin/main`'s (newer, DONE) content for both files —
  commit `2ad6786c7` in `C:\dev-server`. Confirmed fixed:
  `request-restart.mjs --agent 2830-q1667` went from `status: "conflict"` to
  `status: "merged"`; a concurrent build (#2916) landed cleanly on top
  immediately after (`145ba36ad` in `C:\dev-server`). Re-ran
  `request-restart.mjs --agent 2830-q1667-verify` afterward — no conflict,
  `status: "already-live"`.
  Separately investigated the issue's item #5 (malformed `pino` junction /
  `Cannot find module 'pino'`): `store-doctor.mjs` (both default root and
  `--root C:\dev-server`) reports CLEAN, and a direct `readlinkSync` +
  `path.resolve` check on
  `C:\dev-server\artifacts\api-server\node_modules\pino` resolves to a real,
  existing `.pnpm/pino@9.14.0/node_modules/pino` — not poisoned. The
  "duplicated path" read in the issue's `fsutil reparsepoint query` dump is
  the junction's substituteName+printName pair printed back to back (normal
  reparse-buffer layout), not actual corruption. Ran
  `node scripts/dev-server/bootstrap-server.mjs --link --launch` to bring the
  server up fresh: `artifacts/api-server` built and started successfully
  (esbuild succeeded, `pino` resolved with no error) — pid 67456, confirmed
  `alive port 8080` via `status.mjs`, and `curl http://localhost:8080/api/...`
  now returns real Express 404s instead of connection-refused. The
  `pino`/esbuild failure in the issue's evidence item #5 was a stale
  2026-09-03 log entry that did not reproduce once the merge conflict (the
  actual blocker) was cleared and a fresh build ran.
  Also noted but out of scope for this fix: `bootstrap-server.mjs --link`
  reported real `lib/*/dist` TS build failures for
  `lib/db/src/schema/config-state.test.ts` (missing `vitest` types) and
  `lib/integrations-openai-ai-react` (missing `react` types) — filed as
  **#2927**, and Marketing's dev server separately failed to resolve
  `@simplewebauthn/browser` imported by `Buy.tsx` — filed as **#2928**. Both
  parented as direct sibling sub-issues of #1128 per this build's dispatch
  header, labeled `bug`, board status set to "AI Batter Up".
  Working tree clean; branch merged into `origin/main`.
