# #1890 — Remove Batter Up / AI Batter Up automatic polling

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 0fb054170a675e160e9253f55621f6cfa037c819

## Log
- 2026-08-30 ⏳ IN FLIGHT — removing the 90s DispatcherTimer + auto-refresh-on-Initialize
  from BatterUpPanel.xaml.cs and AiBatterUpPanel.xaml.cs, preserving #1813's cross-trigger
  from Git Board's manual refresh (MainWindow.xaml.cs LeftSidebar.BoardRefreshCompleted).
- 2026-08-30 ✅ DONE — removed the 90s `DispatcherTimer` and the `_ = RefreshAsync()`
  auto-call from both `BatterUpPanel.Initialize` and `AiBatterUpPanel.Initialize`, and
  dropped the now-dead `_timer` fields. Judgment call: applied the same removal to AI
  Batter Up, not just Batter Up — Shane named Batter Up explicitly, but the identical
  timer pattern exists in AiBatterUpPanel and items awaiting his Yes/No review benefit
  from the same "only when I ask" philosophy; no reason found for it to behave
  differently. `#1813`'s cross-trigger (Git Board's manual refresh cascading into both
  panels' `RefreshAsync()`, `MainWindow.xaml.cs:698-699`) is untouched — still a real,
  Shane-initiated manual action, not automatic polling. `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings/errors.
  Commit merged to origin/main, verified via `git merge-base --is-ancestor`.
