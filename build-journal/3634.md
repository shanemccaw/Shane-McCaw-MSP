# #3634 — portal-checkout.ts:655 and msp-sow.ts:347: same narrow serviceClass==="subscription" check as #3403 — retainer catalog items charged once via customer self-checkout / SOW-accept

- **Status:** ✅ DONE 2026-09-11T13:32:17Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 96ab71700

## Log
- 2026-09-11T13:29:50Z ⏳ IN FLIGHT — widen `portal-checkout.ts:655` and `msp-sow.ts:347`'s narrow `serviceClass === "subscription"` checks to also match `billingType === "recurring_monthly"` (same root cause/fix pattern as #3403), and confirm the resulting Stripe Subscription is actually recorded via `recordTenantSubscription()` for each flow, not just a widened branch with nothing recording the result.
- 2026-09-11T13:32:17Z ✅ DONE — commit 96ab71700. Widened both checks. Confirmed live retainer rows (services 115/116/117/118/119/168) via direct local DB query. `portal-checkout.ts` never called `recordTenantSubscription()` at all (unlike `msp-marketplace-purchase.ts`, which #2847 wired) — added the call, gated on real Stripe outcome per the #3403 trap. `msp-sow.ts` had a second, deeper pre-existing bug: `sessionParams["mode"]` was unconditionally forced back to `"payment"` immediately after being set to `"subscription"`, so NO purchase through that branch — retainer or otherwise — ever created a real Stripe Subscription; fixed the mode/line-item/trial_period_days logic to match the proven `portal-checkout-direct.ts` pattern. Added regression tests to both route test files (portal-checkout.test.ts 12/12 passing, msp-sow.test.ts 32/32 passing). `tsc --noEmit` clean. Filed #3650 (parented under #1571, board status "AI Batter Up") for the separate, deeper finding that `msp-sow.ts`'s add_on/subscription branch still has no webhook consumer at all for `fulfillment_type: "msp_offer"`, and that its Checkout Session `customer_email` targets the accepting MSP staffer rather than the customer — both pre-existing, out of narrow scope for #3634, left for Shane's call per the CLAUDE.md product-decision carve-out. Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0); working tree clean.
