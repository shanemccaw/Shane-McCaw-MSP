# #3859 — ladder.customer-user allow-list vs renamed Customer role (access-control regression)

- **Status:** ✅ DONE 2026-09-13T04:25:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 0cd0b63c2 (IN FLIGHT bookend), DONE bookend commit; **no code/data change** — already fixed by #3867 (`2e70e16e1`)

## Log
- 2026-09-13T04:22:16Z ⏳ IN FLIGHT — Investigating the reported `ladder.customer-user` 403 regression; auditing all `legacy-ladder` mappings against live DB before any change, per the dispatch's load-bearing audit-before-fix instruction.
- 2026-09-13T04:25:00Z ✅ DONE — **No change required; already resolved by #3867.** Findings, all live-verified against `DATABASE_URL`:
  - The 403 was caused by a 2026-09-12 16:57 re-run of the #2457 seed (via #3638) that resurrected `CustomerUser`/`Assessment` with new ids and rewrote every `ladder.*` allow-list off them. Repaired by `lib/db/migrations/manual/2026-09-12-rbac-repair-2457-rerun-3867.sql` (#3867, commit `2e70e16e1`), which ran here 2026-09-12 19:05:33. The #2457 seed now refuses to run after #3590 (line 89), closing the recurrence class. #3867 already registered on #1630 for staging.
  - **`CustomerUser` fully retired** (#3590): 0 role rows, 0 `users`/`msp_invites` storing the value.
  - **All 8 `msp_feature_role_mapping` rows audited** — every allow/deny id resolves to a real current role; no stale `CustomerUser` reference in any `legacy-ladder` row. `ladder.customer-user` allows the renamed `Customer` id `9b4dfb27-…`.
  - **Gate verified** (user 39 gone after reseed; used Customer user 2757): holds `9b4dfb27` → clears `ladder.customer-user` per `evaluateCapability` set-intersection; `Free` correctly denied. `check-rbac-parity` live: **3120 comparisons, ALL AGREED**.
  - `shaneapp://runTest ownership.json` could NOT exercise the gate here — `TEST_PORTAL_EMAIL`/`PASSWORD` unset in BuildConsole Test Env Vars, so all 9 failures were `unresolved variable`/login, NOT the reported 403. Env-config gap, not a regression.
  - Issue comment posted; labels `in-flight`→`complete`. Closing is Shane's call (likely duplicate-of/fixed-by #3867). No findings filed (already-fixed is not a finding). Working tree clean.
