# #2761 — Monitoring: close coverage gap on the identity surface (20 measured, 2 real closes)

- **Status:** ✅ DONE 2026-09-04T05:57:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** cbdbca0a8 (IN FLIGHT bookend), e75e5f309 (2-check migration)

## Log
- 2026-09-04T05:38:41Z ⏳ IN FLIGHT — confirmed #2760 (blocker) is DONE/complete, pulled the live query: 20 `surface='identity' AND availability='available_now' AND check_coverage_count=0` resources. Probed all 20 real Graph endpoints against the testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1, app-only). Cross-referenced each resource's real underlying Graph endpoint (derived from its `read_cmdlets`, since 14 of the 20 are `m365dsc:`-origin rows with `graph_path` left null by the extraction) against the FULL `config_resources` table, not just `surface='identity'`. Finding: the large majority are duplicate/unlinked nodes of a resource the `graph:*`-origin extraction already modeled separately (under `policy`/`groups`/`directory`/`identity` surfaces) — some already covered, some sharing a not-yet-covered path with their `graph:` twin. Because `matchEndpointToResource`'s exact-path match can only ever credit ONE resource id when two rows share the same real `graph_path`, these can never independently show covered no matter how correct the check is — writing a check for them would silently do nothing measurable for the identity-surface number. Proceeding to write real checks only for the resources confirmed both live-working AND uniquely modeled (no path collision), and to file the duplicate-model pattern as a real finding.
- 2026-09-04T05:57:00Z ✅ DONE — Added 2 real `monitor_checks` rows (`identity:app-federated-identity-credentials`, `identity:app-proxy-connector-groups`) for the 2 identity-surface resources that were genuinely live-working AND uniquely modeled (no `graph_path` collision with another `config_resources` row). Both endpoints confirmed with a real GET to the testbed tenant (mccawsoft2.onmicrosoft.com, tenants.id=1, app-only) — federatedIdentityCredentials via a real app id, connectorGroups directly. `config_resources.graph_path` was null on the extraction for both (m365dsc-origin, parser didn't detect a REST endpoint even though `read_cmdlets` names a known Microsoft Graph SDK cmdlet with a 1:1 REST mapping) — populated with the real, live-verified endpoint in the migration itself so `matchEndpointToResource` could credit them; both now match `graph-path-exact`/high confidence (`config_resource_check_coverage` + `check_coverage_count` recomputed the same way, not a full rebuild). Verified by replaying `mapping`/`severity_rules` through the real `applyMapping`/`evalConditionGrammar` (`monitor-executor.ts`) against both the real (empty) testbed shape and a synthetic risky shape, via a temporary vitest inclusion (reverted after the run, working tree left clean) — 4/4 assertions pass. Migration `lib/db/migrations/manual/2026-09-04-identity-surface-coverage-2761.sql` run locally against `DATABASE_URL` (2 checks + 10 package-wiring rows + 2 config_resources UPDATEs + coverage recompute + self-marking insert, all committed).

  The real story of this build is why only 2 of 20: 10 of the 20 are duplicate/unlinked `config_resources` rows describing an object the `graph:*`-origin extraction already modeled separately elsewhere (8 already covered under that other row — `AADAccessReviewDefinition`, `AADAuthenticationFlowPolicy`, `AADCrossTenantIdentitySyncPolicyPartner`, `AADExternalIdentityPolicy`, `AADGroupsNamingPolicy`, `AADNamedLocationPolicy`, `AADRoleEligibilityScheduleRequest`, `AADSecurityDefaults`; 2 colliding with an also-uncovered row — `AADPasswordRuleSettings` and `AADPolicyFeatureRolloutPolicy`). Because `matchEndpointToResource`'s exact-path match can only ever credit ONE resource id per real `graph_path`, none of these 10 can independently show covered no matter how correct a new check is — this systemic pattern is filed as **#2821** (sub-issue of Feature #1128, milestone v1.1, label `bug`), parented and board status set to "AI Batter Up". 4 are real 403 license gaps (`continuousAccessEvaluationPolicy`, `accessReviews/policy`, `agentRiskDetections`, `servicePrincipalRiskDetections`). 1 is a real 403 tenant-type gap (`AADB2CAuthenticationMethodsPolicy`, same non-B2C exclusion #2760 already recorded for the policy surface's own copy). 2 are real 200s but static Microsoft-provided reference catalogs with no tenant-controlled state to check (`conditionalAccess/templates`, `authenticationStrength/authenticationMethodModes`). 1 has no real readable data source — its own recorded `notes` field already flags the sole cmdlet (`Get-MSCloudLoginConnectionProfile`) as an M365DSC-internal connection helper, not tenant data (`AADOnPremisesPublishingProfilesSettings`).

  `surface='identity' AND availability='available_now' AND check_coverage_count=0` is now 18 rows, down from 20 (2 real gaps closed; the remaining 18 are structurally uncloseable-by-check until #2821 lands, or are real license/tenant-type/non-data exclusions). Labels flipped `in-flight`→`complete` on #2761; completion comment posted. Branch verified merged into origin/main (`verify-branch-merged.mjs`). `git status --porcelain` clean.
