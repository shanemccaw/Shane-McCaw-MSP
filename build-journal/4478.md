# #4478 — 24 policy:/directory: checks resolve to no pillar

- **Status:** ✅ DONE 2026-09-17T05:06:18Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 68782a7ec (bookend), 5bb2b52d0 (fix)

## Log
- 2026-09-17T05:05:11Z ⏳ IN FLIGHT — investigated `pillarForCheckKey`/`PILLAR_CHECK_DOMAINS`/`buildCheckKeyPillarMap`, confirmed all 24 `policy:`/`directory:` checks exist in `monitor_checks` with zero existing `signal_derivation_rules` rows; writing per-check `signal_derivation_rules` rows (real pillar assignment, zero impact/weight so no scoring behavior changes) rather than a blanket prefix map, since `directory:*` alone spans licensing/governance/security/health.
- 2026-09-17T05:06:18Z ✅ DONE — added `lib/db/migrations/manual/4478-policy-directory-checks-pillar-rules.sql` (24 `signal_derivation_rules` rows, one per check, judged individually: 17 `policy:*` → security, `policy:terms-of-use-agreements` → compliance, `directory:cloud-licensing-*` (3) → licensing, `directory:org-contact-provisioning-errors` → governance, `directory:partner-delegated-admin-relationships` → security, `directory:service-health-active-incidents` → health (stored as engine pillar `architecture`, per `ENGINE_PILLAR_FOR_DISPLAY_PILLAR`)); ran it against local `DATABASE_URL` myself. Live-verified by re-running the real `buildCheckKeyPillarMap`/`pillarForCheckKey` path (the same one `export-scan-findings-4471.ts` uses) for all 24 keys — first pass caught a real bug in my own fix (stored `pillar='health'` instead of `'architecture'`, which `pillarForRulePillar` silently drops since `health` isn't a real `RadarPillar` value), fixed, re-verified: 24/24 now resolve. `pillar-summary-stats.test.ts` + `pillar-coverage.test.ts` (69 tests) still pass. Branch verified merged into `origin/main` (5bb2b52d). Working tree clean. Filed #4501 (parented to #1489, board status AI Batter Up) for the separate `remediation_knowledge_base` gap #4478's own body noted in passing (0 rows for all 24 checks) — different table/code path, out of scope for a pillar-attribution fix.
