# #1816 — Batter Up: collapse to minimal footprint when empty, consolidate refresh into one shared button in the Build Queue header

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 95c078bad (agent branch + merged into main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — read #1816's issue body + comment, and #1813's landed diff (15655a062: MainWindow.xaml.cs wires `LeftSidebar.BoardRefreshCompleted` → `BatterUpPanel.RefreshAsync()` + `AiBatterUpPanel.RefreshAsync()`; `BoardRefreshCompleted` fires from `LeftSidebar.PopulateGitTrackerBoardAsync` on every successful fetch). Found `BuildQueuePanel.xaml`'s header (~112-127) already has `BtnRefreshGitHubTiles`, which raises `FullGitRefreshRequested` → `LeftSidebar.PopulateGitTrackerBoard(forceFresh: true)` → `BoardRefreshCompleted` → (via #1813) Batter Up + AI Batter Up refresh. That existing button already IS the shared refresh path the issue asks for — repurposing/relabeling it instead of adding a duplicate second button that would do the exact same thing. Now implementing: (1) update its tooltip/toast to name Batter Up + AI Batter Up explicitly so it reads as the one shared control: (2) remove the individual `BtnRefresh` buttons from BatterUpPanel.xaml/AiBatterUpPanel.xaml; (3) collapse both panels' empty state (zero rows) into an inline single-line header note instead of the separate `TxtEmpty` block/margin, keeping `TxtEmpty` only for the rarer no-PAT/error messages.
- 2026-08-30 ✅ DONE — `BuildQueuePanel.xaml`'s existing `BtnRefreshGitHubTiles` (header, ~112-127) is now documented/labeled as the one shared refresh control (tooltip + toast text updated to name Batter Up + AI Batter Up); no second button added since this one already cascades via `FullGitRefreshRequested` → `LeftSidebar.PopulateGitTrackerBoard(forceFresh:true)` → `BoardRefreshCompleted` → #1813's `BatterUpPanel.RefreshAsync()`/`AiBatterUpPanel.RefreshAsync()` wiring — untouched. Removed the individual `BtnRefresh` button from both `BatterUpPanel.xaml` and `AiBatterUpPanel.xaml` headers (and their `BtnRefresh_Click`/`IsEnabled` toggle code, now dead since `_refreshing` already guards re-entrancy without a button to disable). Collapsed both panels' empty ("zero rows") state: the "no items" message now shows inline in the header's `TxtCount` ("— none open") instead of the separate `TxtEmpty` block, so an empty panel's footprint is just its one header row; `TxtEmpty` is kept only for the rarer no-PAT/refresh-error messages, tracked via a new `_emptyMessageActive` field so `BtnCollapse_Click` still shows/hides those correctly. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 errors. Committed 95c078bad, pushed straight to `main` (rebase not needed — branch was already current), confirmed via `node scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain` clean.
