# #3429 — Feature: Remove Client Records Documents from Admin Panel, superseded by MSP Console/Portal (AdminPanel)

- **Status:** 🛑 BLOCKED 2026-09-10T05:05:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none — no code changed

## Log
- 2026-09-10T05:05:00Z 🛑 BLOCKED — Investigated only. Located the real nav entry
  (`id: "del-documents"`, `artifacts/admin-panel/src/components/shell/workspaceNav.tsx:414`,
  path `/crm/documents`) in the "Client Records" nav section (lines ~412-414). #3429's own
  build header carries `--blocked-by 3428`, and a real GitHub `blocked_by` dependency edge
  from #3429 → #3428 already exists (confirmed via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3429/dependencies/blocked_by`). #3428
  ("Remove Contracts/Agreements section from Admin Panel Finance workspace") is still
  `state: open`, labeled `in-flight` (not `complete`) — genuinely still in progress — and
  targets the same shared file (`fin-contracts` entry, "Agreements" section, lines
  ~438-440 of `workspaceNav.tsx`), confirmed via
  `grep -n "del-documents\|fin-contracts" artifacts/admin-panel/src/components/shell/workspaceNav.tsx`.
  A later comment on #3429 (posted 2026-09-10T04:52:07Z, `buildSet=BatterUpClearOut`)
  re-dispatched without the `--blocked-by` flag, but the real GitHub dependency edge is
  the gate per CLAUDE.md's "blocked_by edges are the gate, not just a comment" rule — a
  dispatch comment omitting the flag does not clear a live edge, and #3428 has not closed
  or been marked `complete`. Editing `workspaceNav.tsx` now (to delete `del-documents`)
  while #3428 concurrently edits the same file (to delete the whole "Agreements" section)
  is exactly the shared-file collision class CLAUDE.md's git-conventions section warns
  against, and the same class already confirmed on sibling issues #3421/#3425 this
  session. Stopping here per the blocked-issue protocol rather than guessing forward. No
  code changed. `--title` correctly targets a leaf issue (not an epic). Left the existing
  `blocked_by` edge to #3428 in place (already correct, not stale). Applied the `blocked`
  label (removed `in-flight`) and posted a comment on #3429 explaining the wait. No
  findings filed — nothing else was investigated once the real blocker was confirmed at
  the top of the session.
