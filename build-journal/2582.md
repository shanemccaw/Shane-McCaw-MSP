# #2582 — Risk Register / RBD: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-12T03:23:43Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** e69fa92af (original), rebased to 68e120510 on `main`

## Log
- 2026-09-12T03:05:45Z ⏳ IN FLIGHT — Investigation done: confirmed #3677 (MSP Console
  Shell) closed/complete, blocked_by edge already satisfied; read
  `Design/MSP_Console/design_handoff_msp_console/Risk Register.dc.html` + README
  screen 14; read the three real backend route files (`msp-rbd.ts`,
  `msp-rbd-instances.ts`, `msp-rbd-versions.ts`) and the full
  `mspRiskDecisionsTable` schema (review clock: `reviewDate`/`reviewDueAt`/
  `reviewState`, register ref `registerRef`). About to build the Risk Register
  module page (register table, filters, create-a-risk drawer, detail drawer with
  Decision/Affected objects/Documents/Changes tabs) and mount it in
  `artifacts/msp-console/src/console/ConsoleShell.tsx` at `page === "risk"`.
- 2026-09-12T03:23:43Z ✅ DONE — Built and wired the Risk Register module page:
  `artifacts/msp-console/src/api/rbd-api.ts` (hooks against every real route
  named in the dispatch plus their supporting reads: `GET /api/msp/rbd`,
  `/available-checks`, `/available-obligations`, `POST /api/msp/rbd`, `PATCH
  .../sign`, `PATCH .../revoke`, `GET/POST/PATCH .../instances[/accept|/resolve]`,
  `GET/POST .../versions`, `POST .../versions/:versionUid/share`, `GET
  .../versions/narrative-audit`); `artifacts/msp-console/src/modules/
  risk-register/{RiskRegister,CreateRiskDrawer,RiskDetailDrawer,format}.tsx`
  (register table with the review-clock column, filters, "Accept a risk"
  drawer, and a Decision/Affected objects/Documents/Changes detail drawer);
  extended `console/icons.tsx`'s registry; mounted in `ConsoleShell.tsx`'s
  shared `moduleFor()` at `sel.page === "risk"`. No fixtures — every number
  reads from the live tables.
  Verification: `tsc -p tsconfig.json --noEmit` clean, `vite build` clean
  (both re-run after merge). Queried the local `DATABASE_URL` directly:
  confirmed 5 real `msp_risk_decisions` rows exist, 4 of them share
  `tenant_id = c4c814d4-3afe-441e-9145-62461d0a4fd3`, which is the exact
  `tenants.tenant_id` for tenant id 1 ("Jane Jane") — proves the client-side
  tenant-scoping join (`RbdRow.tenantId === DirectoryCustomer.tenantId`) will
  hit real, matching rows once live. Did not do a live HTTP round-trip:
  `node scripts/dev-server/status.mjs` showed the local api-server process
  DEAD (port 8080 not listening) — a genuine, stated unreachable state, not
  skipped by default.
  Concurrent peer sessions (#2588 Remediation Tracking, #2597 SOPs) landed on
  `main` mid-build, both editing the same shared `ConsoleShell.tsx`/`icons.tsx`
  — rebased twice and manually merged both real conflicts (a duplicate
  `moduleFor` function, overlapping lucide icon imports) rather than
  overwriting either side; both re-verified with a clean typecheck + build
  afterward.
  Merged to `main` at `68e120510` (originally committed as `e69fa92af`,
  rebased twice). `verify-branch-merged.mjs` confirms ancestor-of-main.
  `git status --porcelain` clean.
  No `msp-console` test manifest — out of `test-manifests/`'s scope (only
  `artifacts/msp-portal` and `artifacts/shane-mccaw-consulting` are covered).
  No findings filed — the backend was already complete and correct; nothing
  discovered warranted a new issue.
