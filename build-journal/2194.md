# #2194 — Git Doctor: real git-health diagnostic + guided-fix panel

- **Status:** ✅ DONE (partial — Step 13 only, of the wpf-handoff Phase 2 spec)
- **Scope:** desktop/ShaneBuilder (new app — successor to BuildConsole for the App Shell v2 rebuild, confirmed by Shane 2026-09-01)
- **Started:** 2026-09-01
- **Commit:** `9679ec773` "ShaneBuilder: Git Doctor (Step 13)" — real ancestor of `origin/main`, verified via `git log`. No IN FLIGHT bookend exists for this one — it was built directly by Shane running a Claude Code session outside the Git-as-Build-Queue, so it never passed through the normal per-build bookend lifecycle. This is the DONE side written retroactively, by chat, against the real landed diff — not a build-queue bookend.

## Log

- **Real audit performed against the actual landed code before writing this, not the issue body alone:**
  - `desktop/ShaneBuilder/Services/GitDoctorService.cs` (486 lines, new file) — real `GitDoctorFinding`/`GitDoctorRemedy`/`GitDoctorStep`/`GitDoctorBranch`/`GitDoctorCommitInfo` records matching the wpf-handoff Step 13 contract shape closely. `RunChecksAsync()` runs real git commands (not seeded data) to populate findings. `ComputeBranchesAsync()` — branch janitor. `LookupCommitAsync(shaPrefix)` — real commit-hash lookup with reachability. `RunStepsAsync()` — `IAsyncEnumerable<GitDoctorStepResult>`, streams remedy execution.
  - `desktop/ShaneBuilder/MainWindow.xaml.cs` — real wiring: `OpenGitDoctor()`, `EnsureGitDoctorLoadedAsync()`, `LoadGitDoctorChecksAsync()`, `BtnGitDoctorRecheck_Click`. Findings render for real, re-run checks works. Claude bridge is clipboard-based (`Clipboard.SetText`/`Clipboard.SetImage` calls present) rather than direct text-injection — correct call, since claude.ai runs in a WebView2 this app doesn't control the composer of.
  - **Live-verified by Shane directly** (screenshot, 2026-09-01): real findings against his actual machine's repo — "39 local branches" (real count, not the "131" figure from an earlier audit this session, because branch count is a live number that changes as builds land/branches get created), filter tabs (All/Merged/Unmerged/Remote gone/Older than 90d) all populated with real data, a real recommended remedy card with real `git tag`/`git branch -d` commands, and a working "Delete N selected — backup tag first" action button.

## What's confirmed vs not, honestly

- **Confirmed real and working:** findings populate from live git state, branch janitor UI with real branch list + filters, the backup-tag-first remedy is presented with real commands.
- **Not independently verified in this pass** (no evidence either way — not claiming broken, just unconfirmed): the full "End this git nightmare" one-click multi-step runner; whether `LookupCommitAsync`'s reachability check has been exercised against a real hash; whether the "paste a Claude reply → Extract commands → Run N approved" round-trip has actually been run once, versus just wired.
- **Scope note:** this is Step 13 of 16 in the wpf-handoff Phase 2 spec (`desktop/ShaneBuilder/wpf-handoff/README.md` and `readme-phase2.md` provided by Shane). Steps 9–12 and 14–16 (Tab Workspaces, Log Viewer, Alerts/Critters, API Explorers, Command Center, Settings redesign) are not built. Entry point today is Ctrl+K only, per the commit message — the other entry points (topbar zap menu, the git-health alert's "Fix Git", the "Git Pull" quick action) depend on later steps landing first.

## Why this bookend looks different from the rest

No `IN FLIGHT` entry exists because this build never went through `QueueWatcherService`/the Git-as-Build-Queue — Shane opened a Claude Code session directly against his main checkout to walk through the Design handoff steps interactively, same pattern used for the original App Shell v1 work. This file exists so #2194 has the same real paper trail as everything else, written against the actual diff rather than trusted on report alone.
