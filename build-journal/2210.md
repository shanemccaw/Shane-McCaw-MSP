# #2210 — Port BuildConsole's screen-clip tool into ShaneBuilder

- **Status:** ✅ DONE 2026-09-02T07:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 1ca749bbb

## Log
- 2026-09-02T07:20:00Z ⏳ IN FLIGHT — real audit done of `desktop/BuildConsole/Services/DesktopScreenClipService.cs`,
  `RegionSelectOverlayWindow.xaml(.cs)`, the global-PrintScreen-hotkey wiring in `MainWindow.xaml.cs`
  (`RegisterScreenClipHotkey`/`ScreenClipWndProc`/`Window_PreviewKeyUp`), and ShaneBuilder's existing
  `Notifications/ToastEngine.cs` + `Services/LogService.cs` (`ConsoleOutputSink.Log`, the app's real
  logging sink — no `ActivityLog` class exists in ShaneBuilder). About to port the desktop-mode-only
  region-select overlay + capture service into ShaneBuilder, wire a title-bar icon + global/in-app
  PrintScreen, and point disk save at `MyPictures\Screenshots\ShaneBuilder` (not BuildConsole's literal
  folder name).
- 2026-09-02T07:45:00Z ✅ DONE — ported `RegionSelectOverlayWindow.xaml(.cs)` (desktop-mode only — the
  BuildConsole original's WebView2/DIP crop mode has no caller in ShaneBuilder, left behind rather than
  carried over unused) and `Services/DesktopScreenClipService.cs` into ShaneBuilder, logic-for-logic
  (multi-monitor physical-pixel select, dual clipboard+disk sink with independent-failure handling, STA
  requirement, double-capture guard, clean Esc/too-small no-op). Disk save defaults to
  `MyPictures\Screenshots\ShaneBuilder`. Wired the `BtnScreenClip` title-bar icon + global PrintScreen
  (`RegisterHotKey`) + in-app `PreviewKeyUp` fallback in `MainWindow.xaml`/`.xaml.cs`, mirroring
  BuildConsole's own wiring. ShaneBuilder has no persisted settings store, so (unlike BuildConsole) the
  save directory and hotkey-enabled flag aren't user-configurable yet — filed as its own finding, #2221
  (parented to #1202, board status "AI Batter Up"), rather than built silently as a fixed default.
  Hit and fixed a real build break: `UseWindowsForms`'s own `ImplicitUsings` add global
  `using System.Drawing;`/`using System.Windows.Forms;`, which collided project-wide with WPF's
  unqualified `Brush`/`Color` elsewhere in ShaneBuilder (CS0104, 54 errors); removed those two implicit
  usings in `ShaneBuilder.csproj`.
  Verification: `dotnet build ShaneBuilder.csproj -c Debug` — 0 errors, 0 warnings. Launched the real exe
  and confirmed it stays alive past hotkey registration (6s, no crash). Genuine UI click-through of the
  drag-a-region flow wasn't possible in this non-interactive session — noted honestly, not claimed as
  tested.
  Commit: 1ca749bbb (pushed to `main`, verified ancestor via `verify-branch-merged.mjs`; `git status
  --porcelain` clean).
