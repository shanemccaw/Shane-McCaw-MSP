# #2217 — Feature: JSON Viewer + Windows File Browser — mini + full (ShaneBuilder)

- **Status:** ⏳ IN FLIGHT 2026-09-02T16:10:00Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** _fill at DONE_

## Log
- 2026-09-02T16:10:00Z ⏳ IN FLIGHT — Build header scopes this session to the JSON Viewer
  (§6.9) + Windows File Browser (§6.10) **mini** panels only. README-ClaudeChat.md itself
  is NOT committed to the repo (already tracked as open finding #2227 from #2209's
  session, same gap #2215 hit) — working from #2217's own issue body, which states the
  spec directly: JSON Viewer = 6-row paste textarea, Prettify, error line on invalid
  input, formatted output, Copy JSON, seeded with a small object so it's never blank on
  first open. Windows File Browser = indented tree, folders expand in place, selecting a
  file shows full path (word-break, Copy Path) — **read-only by design**, no edit
  capability. Real audit: the SQL Runner mini panel just landed on `main`
  (`1ca56ea5b`, #2215) establishes the exact bolt-on-flyout mechanism to reuse — a new
  `GridColumn` + `Border` panel bolted onto `ClaudeChatDock`'s existing
  `StatDetailPanel`/`DetectedItemsPanel`/`LogPeekPanel`/`SqlRunnerPanel` column stack,
  toggled via a context-bar button + wrench-menu entry. No existing file-tree control in
  either ShaneBuilder or BuildConsole to port from (unlike SQL Runner's port from
  `SqlDocumentView`) — File Browser tree is new, built as a standard WPF lazy-expanding
  `TreeView` over local `DriveInfo`/`DirectoryInfo`, no port available.
