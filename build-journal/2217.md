# #2217 — Feature: JSON Viewer + Windows File Browser — mini + full (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T14:44:37Z
- **Scope:** buildconsole (ShaneBuilder WPF, new isolated project)
- **Started:** 2026-09-02
- **Commit(s):** `e2306b7ee` (implementation), pushed straight to `origin/main`; verified
  merged via `verify-branch-merged.mjs` and `git cat-file -e e2306b7ee^{commit}`.

## Log
- 2026-09-02T16:10:00Z ⏳ IN FLIGHT — Build header scopes this session to the JSON Viewer
  (§6.9) + Windows File Browser (§6.10) **mini** panels only. README-ClaudeChat.md itself
  is NOT committed to the repo (already tracked as open finding #2227 from #2209's
  session, same gap #2215 hit) — working from #2217's own issue body, which states the
  spec directly: JSON Viewer = 6-row paste textarea, Prettify, error line on invalid
  input, formatted output, Copy JSON, seeded with a small object so it's never blank on
  first open. Windows File Browser = indented tree, folders expand in place, selecting a
  file shows full path (word-break, Copy Path) — **read-only by design**, no edit
  capability. Real audit: the SQL Runner mini panel just landed on `main`
  (`1ca56ea5b`, #2215) establishes the exact bolt-on-flyout mechanism to reuse — a new
  `GridColumn` + `Border` panel bolted onto `ClaudeChatDock`'s existing
  `StatDetailPanel`/`DetectedItemsPanel`/`LogPeekPanel`/`SqlRunnerPanel` column stack,
  toggled via a context-bar button + wrench-menu entry. No existing file-tree control in
  either ShaneBuilder or BuildConsole to port from (unlike SQL Runner's port from
  `SqlDocumentView`) — File Browser tree is new, built as a standard WPF lazy-expanding
  `TreeView` over local `DriveInfo`/`DirectoryInfo`, no port available.
- 2026-09-02T14:44:37Z ✅ DONE — Shipped both mini panels onto `ClaudeChatDock` as a 7th/8th
  flyout column (`JsonViewerPanel`/`FileBrowserPanel`), same bolt-on mechanism as
  `SqlRunnerPanel`/`RepoHealthPanel`, each toggled via a new context-bar button
  (`BtnToggleJsonViewer`/`BtnToggleFileBrowser`) + wrench-menu entry.
  **JSON Viewer:** 6-row paste `TextBox` seeded on first open with `{ "hello": "world",
  "count": 1 }`, Prettify button runs `JsonDocument.Parse` + indented `JsonSerializer`,
  invalid input shows the parse error on a dedicated error line (cleared as soon as the
  user edits again), Copy JSON copies the formatted output.
  **Windows File Browser:** indented `TreeView` rooted at real `DriveInfo.GetDrives()`
  (ready drives only), folders lazy-expand in place via a placeholder-child pattern
  (`Directory.GetDirectories`/`GetFiles`, real filesystem, no fixture data), selecting a
  file shows its full path in a word-break `TextBlock` and enables Copy Path; selecting a
  folder disables Copy Path. **Read-only by design, confirmed** — no
  create/rename/delete/write code path exists anywhere in the new section
  (`FileBrowserDirectory_Expanded`/`PopulateFileBrowserChildren`/
  `BtnFileBrowserCopyPath_Click` are the only file-system-touching methods, all reads).
  `UnauthorizedAccessException`/`IOException` on a folder expand render an inline
  "(access denied)"/"(error: …)" row instead of throwing.
  Verification: `dotnet build -c Debug` — 0 warnings, 0 errors. Did not launch the live
  WPF app to click-test — ShaneBuilder is Shane's own actively-developed app (same
  single-instance/live-session collision risk already established for BuildConsole,
  `never-launch-BuildConsole.exe` memory) — build-clean is the verification for this
  session, same precedent as #2215/#2214's own sessions.
  Real audit: `README-ClaudeChat.md` itself is still not committed to the repo — already
  tracked as open finding **#2227**, not re-filed here (no new finding this session).
  Working tree clean; only `MainWindow.xaml` + `MainWindow.xaml.cs` touched.
