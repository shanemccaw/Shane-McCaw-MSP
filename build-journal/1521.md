# #1521 — Ownership / RACI: Cross-customer MSP view — what do I hold, everywhere

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** `f683fb76c` (feature), `90b4f9fde` (IN FLIGHT bookend)

## Log
- 2026-08-31 ⏳ IN FLIGHT — foundation #1515 confirmed closed/merged (multi-holder
  unique index landed). Read full #1491 architecture (point 7: cross-customer MSP
  view) and #1521's own body/comment. Confirmed via local DB: MSP identity already
  exists as real schema (`users.mspId`/`mspRole` CHECK constraint — MSPAdmin /
  MSPOperator / ServiceAccount need `mspId`, PlatformAdmin needs neither), and
  `portal_ownership_assignments.owner_person_id` is unrestricted free text (the
  `assign` route does not validate membership against the per-customer people
  list), so a cross-customer read keyed on `personIdForUser` over MSP-side users
  does not require #1520 (customer-side MSP-as-assignable-person UI) to land first
  — it is a genuinely separate, buildable read. Building `GET
  /api/msp/ownership/mine`: scans `portal_ownership_assignments` across every
  tenant in the caller's book (mirrors `msp-executive.ts`'s
  `resolveMspIdStrict` + `resolveStaffScopedCustomerIds` scoping), filtered to
  rows whose `owner_person_id` resolves to an MSP-side user, cross-referenced
  against each matched customer's real objects (service / cr / change / freeze,
  reusing the same assembly `portal-ownership.ts`'s GET route already does) so
  each holding carries a real object name, not a bare id. No schema change
  needed. Scope stop: wire contract only, no page.
- Found and filed as #2091 (parented to #1491, board status "AI Batter Up"):
  local DB carries columns (`responded_by`/`responded_at`/`decline_reason` on
  `portal_ownership_assignments`) from a migration
  (`2026-08-31-ownership-raci-acceptance-gate-1518.sql`) that is self-marked as
  run in `simulator_migration_runs` but was never committed to `main` or this
  worktree — orphaned local schema drift from an interrupted #1518 session, not
  my work, left untouched.
- 2026-08-31 ✅ DONE — built `GET /api/msp/ownership/mine`
  (`artifacts/api-server/src/routes/msp-ownership.ts`), backed by a pure
  mapping module `artifacts/api-server/src/lib/msp-ownership-book.ts` (5 vitest
  cases, all passing) plus a shared `gatherOwnershipObjects` helper extracted
  from `routes/portal-ownership.ts` (behavior-preserving refactor — diffed
  against `origin/main`, zero net change to that route's own output). No
  schema change: `owner_person_id` was already unrestricted free text and MSP
  identity already exists via the real `users_role_scope_check` boundary
  (MSPAdmin/MSPOperator/ServiceAccount with `mspId`, or PlatformAdmin), so
  this read does not require #1520 to land first — it will simply start
  returning rows once a cell is actually assigned to an MSP-side person, by
  #1520's work or otherwise. `mspRole IN ('MSPAdmin','MSPOperator',
  'ServiceAccount') AND mspId = caller's mspId`, OR `mspRole = 'PlatformAdmin'`
  is the exact "MSP-side person" boundary used. Scoping mirrors
  `msp-executive.ts` (`resolveMspIdStrict` + `resolveStaffScopedCustomerIds`).
  `byCustomer` reports every in-scope customer including zero-count ones, per
  #1491 note 7 ("dense for some, empty for others is real information").
  Verified against real local Postgres: 2 tenants, 1 MSP-side user
  (`shane@shanemccaw.com`, PlatformAdmin), 0 rows in
  `portal_ownership_assignments` — the endpoint would honestly return
  `holdings: []`, `byCustomer: [{count:0},{count:0}]`, exactly matching that
  real state. `pnpm exec tsc --noEmit` on `artifacts/api-server`: 64
  pre-existing errors (documented territory per #1765/#1772/etc.), zero in any
  file this build touched. `git status --porcelain` clean before DONE — one
  pre-existing dirty file found on arrival, `shaneapp-executeSql.result.json`
  (a report-progress side-effect artifact, timestamp-only diff), reverted with
  `git checkout --` rather than committed. Rebased onto `origin/main` (which
  had moved twice during this session — #1512, #1527, #1522) with a clean
  fast-forward apply, no conflicts. Pushed at `f683fb76c`, confirmed merged via
  `verify-branch-merged.mjs`. No test manifest: standing rule scopes that
  requirement to `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting`,
  and this build's scope stop is the wire contract only — no page.
