# #1815 — Build Queue: new cards should fade in and settle smoothly

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 65639c9f0 (merged to origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — reused `Services/UiFadeHelper.cs` FadeIn plus a new
  Height 0→natural DoubleAnimation on new queue card insertion in
  `Controls/BuildQueuePanel.xaml.cs` (`RenderQueue`), gated on a persisted
  known-keys set so only genuinely new cards animate (the panel fully rebuilds
  `QueueCardsHost.Children` on every render, so without this every card would
  re-fade on every poll).
- 2026-08-30 ✅ DONE — `Controls/BuildQueuePanel.xaml.cs`: added
  `AnimateNewQueueCardIn` (170ms fade via `UiFadeHelper.FadeIn` + a
  Height 0→measured-natural-height `DoubleAnimation` with `QuadraticEase`,
  Height reset to `NaN`/Auto on completion so future content changes still
  auto-size). Gated by `_knownQueueCardKeys`/`_hasRenderedQueueOnce` so only a
  node `Key` genuinely new since the previous render animates — the very
  first render and unchanged cards on subsequent polls never re-animate.
  Purely visual; no queue/build logic touched.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean,
  0 warnings, 0 errors. Merged `agent/1815-q873` into `origin/main` at
  65639c9f0; verified via `git merge-base --is-ancestor`. `git status
  --porcelain` clean.
