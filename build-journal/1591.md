# #1591 — Services: service_type is empty string on Groups B/C — latent signature-requirement bypass

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** e905419a6 (bookend IN FLIGHT), 8da0e1895 (migration + fix)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed real DB state: 18 `services` rows have `service_type = ''`
  diverging from a non-empty `category` — Group B: `category='project'`, ids 158-167 (10 rows);
  Group C: `category='config_pack'`, ids 169-175 + 195 (8 rows). Every other populated
  project/config_pack/retainer/assessment row has `service_type` equal to its `category`. Writing
  a manual additive-only `UPDATE` migration to set `service_type = category` on exactly those 18
  rows, matching the pattern the issue documents for Group A (34-60) and 158-167.
- 2026-08-30 ✅ DONE — Ran `lib/db/migrations/manual/2026-08-30-services-service-type-backfill-1591.sql`
  against the local `DATABASE_URL` (`shanemccawmsp`): `UPDATE 18`. Verified by direct query:
  all 18 targeted rows (158-167, 169-175, 195) now carry `service_type` equal to `category`;
  no `project`/`config_pack` row remains empty. Confirmed the two signature-gate call sites
  (`shane-mccaw-consulting/src/pages/Checkout.tsx:544`, `artifacts/api-server/src/routes/portal-onboarding.ts:481-483`)
  already compare `serviceType === "project" || "retainer"` correctly — the fix was data-only,
  no code change needed, and grepped the repo to confirm nothing else depended on the empty-string
  state. Also found (not fixed — out of #1591's scope, needs a product decision on the right
  `category` value) a separate row, id=131, with `category` itself empty; filed as **#1922**,
  parented to #1096 EPIC: Application Core, board status set to "AI Batter Up". Appended migration
  filename to #1630 per the manual-migration checklist rule. Branch verified merged into
  `origin/main` via `verify-branch-merged.mjs` (OK, 8da0e1895). Working tree clean.
