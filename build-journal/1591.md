# #1591 — Services: service_type is empty string on Groups B/C — latent signature-requirement bypass

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed real DB state: 18 `services` rows have `service_type = ''`
  diverging from a non-empty `category` — Group B: `category='project'`, ids 158-167 (10 rows);
  Group C: `category='config_pack'`, ids 169-175 + 195 (8 rows). Every other populated
  project/config_pack/retainer/assessment row has `service_type` equal to its `category`. Writing
  a manual additive-only `UPDATE` migration to set `service_type = category` on exactly those 18
  rows, matching the pattern the issue documents for Group A (34-60) and 158-167.
