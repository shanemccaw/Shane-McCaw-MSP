# #3796 — MSP Plan Self-Service UI: wire the 4 real routes into artifacts/msp-console

- **Status:** ⏳ IN FLIGHT 2026-09-13T01:18:34Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-13T01:18:34Z ⏳ IN FLIGHT — Contract pack (`docs/msp-console/msp-plan-self-service-contract-pack.md`) and design export (`Design/MSP_Console/design_handoff_msp_console/Plan Self-Service.dc.html`, README screen 49) read in full. Wiring the console's new "Plan & billing" Operations page against the four real `msp-plan-self-service.ts` routes: `GET /api/msp/plan/current`, `GET /api/msp/plan/available`, `POST /api/msp/plan/change`, `POST /api/msp/plan/cancel-pending-change`. New files: `src/api/msp-plan-api.ts` (data seam), `src/console/modules/PlanSelfService.tsx` (page). Nav/shell wiring: `nav.ts` (+`plan` MSP page), `icons.tsx` (+`credit-card`), `treeModel.ts` (pageMeta copy), `ConsoleShell.tsx` (moduleFor dispatch), `nav.test.ts` (updated ops-page counts). Unlike the design mock, every price/allowance/status rendered is the route's own real response — no fixture data.
