# #1887 — Auto-reopen previously-open tabs in the background during the loading screen, not after

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 0c48430ff (bookend), 05ac178ce (implementation), rebased/pushed as 4681241b9

## Log
- 2026-08-31 ⏳ IN FLIGHT — read #1887/#1886/#1882 in full, read MainWindow.xaml.cs's
  OpenChatTabs/_chatTabsAtLaunch usage and #1882's startup overlay sequencing. Building
  a background auto-reopen: persisted OpenChatTabs entries get a real placeholder tab
  immediately (unselected) plus a genuinely-loading background WebView2 parked in a new
  ChatReopenPreloadHost canvas (same off-screen-but-alive keep-alive trick
  MainWindow.PinnedTabs.cs already uses, since WPF never realizes a non-selected
  TabItem's Content so Loaded/navigation never fires for it otherwise); swapped into the
  real tab the moment it's selected. Capped at 2 concurrent page loads. Deliberately
  fire-and-forget from RunDeferredStartupAsync — not wired into MarkShellReady/the
  #1882 overlay dismiss gate, so it can never re-introduce the "overlay hangs on real
  work" failure #1882 just fixed.
- 2026-08-31 ✅ DONE — implemented in MainWindow.ChatReopen.cs (new partial-class file,
  same isolation convention as MainWindow.PinnedTabs.cs) + small hooks in
  MainWindow.xaml/.xaml.cs: `ChatReopenPreloadHost` canvas, `OpenChatTab` reuses a
  finished/in-flight preload when a genuine user-driven open races ahead of it,
  `EditorTabs_SelectionChanged` swaps a background-loaded preload into its placeholder
  tab the moment it's selected, `CloseTab` cleans up an abandoned preload if its
  placeholder tab is closed before ever being selected. Gated behind
  `!AppMode.IsAgent` (same convention as the usage meter / Replit watcher) so an
  agent-launched build session never spins up real claude.ai page loads. Verification:
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 errors),
  both before and after the rebase onto origin/main. Branch verified merged via
  `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
  **Not live-UI-verified** — no interactive WPF/WebView2 session available in this
  build environment to do a real restart-with-persisted-tabs check per the issue's own
  "Verification required" section; that real-restart confirmation (tabs genuinely
  loaded the instant the overlay dismisses, and that this doesn't meaningfully worsen
  overall startup time) still needs a human pass. Filed as #2128 (parented to #1202)
  so it isn't lost, and left un-closed here rather than claimed as done.
