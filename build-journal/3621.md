# #3621 — scores.governance phantom key

- **Status:** ⏳ IN FLIGHT 2026-09-11T05:20:15Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-11T05:20:15Z ⏳ IN FLIGHT — Confirmed both call sites (`msp-customer-scores.ts:192`,
  `portal-customer-engines.ts:865`) hardcode `governance: scores.governance ?? 0` even though
  `governance` is not in `ENGINE_DEFS` (engine-registry.ts:183-293) and nothing ever writes an
  `engine_key = 'governance'` row. Grepped `artifacts/portal/src` and `artifacts/msp-console/src`
  for any live consumer of this specific `scores.governance` field — none found (the only
  `governance` pillar hits are the unrelated Remediation Tracker pillar-score model, a separate
  system with its own `remediation-tracker-scores-types.ts`, not fed by these two routes).
  Applying option (b) from the issue body: dropping the hardcoded `governance` default from both
  routes, since nothing populates it and building a whole new scoring engine is a separate,
  undecided scope. Proceeding to edit both files.
