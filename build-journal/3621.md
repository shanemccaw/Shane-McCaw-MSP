# #3621 — scores.governance phantom key

- **Status:** ✅ DONE 2026-09-11T05:25:51Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** ceea5bd74 (fix), 37fc83663 (pre-rebase, superseded by ceea5bd74 on push)

## Log
- 2026-09-11T05:20:15Z ⏳ IN FLIGHT — Confirmed both call sites (`msp-customer-scores.ts:192`,
  `portal-customer-engines.ts:865`) hardcode `governance: scores.governance ?? 0` even though
  `governance` is not in `ENGINE_DEFS` (engine-registry.ts:183-293) and nothing ever writes an
  `engine_key = 'governance'` row. Grepped `artifacts/portal/src` and `artifacts/msp-console/src`
  for any live consumer of this specific `scores.governance` field — none found (the only
  `governance` pillar hits are the unrelated Remediation Tracker pillar-score model, a separate
  system with its own `remediation-tracker-scores-types.ts`, not fed by these two routes).
  Applying option (b) from the issue body: dropping the hardcoded `governance` default from both
  routes, since nothing populates it and building a whole new scoring engine is a separate,
  undecided scope. Proceeding to edit both files.
- 2026-09-11T05:25:51Z ✅ DONE — Removed the hardcoded `governance: scores.governance ?? 0`
  default from `msp-customer-scores.ts:190` and `portal-customer-engines.ts:864`; both now build
  `scores` from five named defaults (`security`, `health`, `drift`, `sla`, `scope_creep`), still
  spread with the real `scores` map on top, so a real `governance` engine value would ride
  through automatically the day one is registered in `ENGINE_DEFS`. Updated the #3619
  contract-pack doc (`docs/msp-console/tenant-scores-and-results-msp-console-contract-pack.md`)
  to mark §3's finding resolved. Vitest for both routes' test files (9 tests) and `tsc --noEmit`
  for `artifacts/api-server` both pass clean. Pushed to `origin/main` as `ceea5bd74`
  (rebased on top of `a3b122b5a`, an unrelated concurrent bookend commit). Verified via
  `scripts/dev-server/verify-branch-merged.mjs`: OK, merged. `git status --porcelain` clean.
  No new finding filed — this build's own scope was the finding from #3619.
