# #2500 — portal/dashboard results.summary.priorityItems is a hardcoded empty array, no backing query

- **Status:** ✅ DONE 2026-09-03T03:06:15Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 91dee6c96 (bookend), b7e1f04e2 (real fix)

## Log
- 2026-09-03T03:04:42Z ⏳ IN FLIGHT — building the real query behind `results.summary.priorityItems` on `GET /api/portal/dashboard` (`portal-customer-engines.ts:638`), per #2500's own body: read the customer's most recent `msp_diagnostic_findings` run, filter to critical/warning severity, worst-first. Same #164 paywall pattern as the adjacent `pillars[engineKey]` block gates the finding text for unpaid customers.
- 2026-09-03T03:06:15Z ✅ DONE — `priorityItems` now built from a real query: latest `msp_diagnostic_findings.run_id` for the customer, its critical/warning rows, sorted critical-before-warning then newest-first, capped at 5, `title`/`description` gated by the same #164 `isPaidTier` check the pillars block already uses (unpaid gets `severity`/`checkKey` only). Verified against real local Postgres data for testbed tenant 1 (`msp_diagnostic_findings`, 853 real rows; hand-ran the equivalent SQL and got 5 real critical findings back — DLP, OAuth consent, stale app regs, break-glass). Updated the two existing route tests that mock the handler's exact FIFO db-call order (`portal-customer-engines-assessment-redaction.test.ts`, `portal-dashboard-route-collision.test.ts`) for the 2 new queries, and added 3 new tests covering the paywall gate, severity/recency sort, and the real empty-array no-findings case. **Could not execute vitest in this worktree** — `npx vitest`/direct CLI both fail with a pre-existing `ERR_MODULE_NOT_FOUND` resolving `vitest/config` (confirmed real, reproducible, unrelated to this change — same class as the documented pnpm-store/worktree-symlink gaps in project memory, not something `pnpm install` fixes per the hard rule). Tests were traced by hand against the mock FIFO queue and are logically sound; flagging the vitest-execution gap honestly rather than claiming a run that didn't happen. `git status --porcelain` clean, branch verified merged into `origin/main` via `verify-branch-merged.mjs`. Filed the vitest-in-worktree gap as #2508 (sibling of #1655), board status set to AI Batter Up.
