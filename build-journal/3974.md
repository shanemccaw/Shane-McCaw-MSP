# #3974 — RBAC: customer_feature_role_mapping gating audit for RetainerNoConsent in Portal

- **Status:** ✅ DONE 2026-09-14T02:59:19Z
- **Scope:** platform
- **Commit:** 3002585b5 (merged to main as d546c8610)

## Log
- 2026-09-14T02:42:19Z — Started. Verified blocker #3971 (msp_role enum + users_role_scope_check
  for RetainerNoConsent/RetainerConsented) is genuinely done in code and live in local Postgres
  (constraint confirmed via psql). Investigated the real mechanism: `customer_feature_role_mapping`
  allow/deny arrays reference `customer_roles.id` by stable uuid, and `readPlatformRoleId` looks up
  a rung's role row by `key` — but #3971 only added the enum value + CHECK constraint, it did not
  seed `msp_roles`/`customer_roles`/`rbac_capabilities`/`msp_feature_role_mapping` rows for the two
  new rungs (confirmed live: no such rows exist yet). Without those, RetainerNoConsent/RetainerConsented
  resolve to zero role ids and are denied EVERYTHING, including billing — which the real product
  decision requires to be allowed. Also found the billed-party mechanism (#3629's trigger on
  `invoices`/`client_services`) already grants the `Billing` role independent of msp_role, so billing
  access for a billed RetainerNoConsent user works without any customer_feature_role_mapping change.
  Proceeding to: seed the missing role/capability/ladder rows, add explicit deny rows for
  team.manage/changes.approve/marketplace.browse-full, and write a live-db test analogous to
  `customer-admin-billing-roles.test.ts`.
- 2026-09-14T02:59:19Z — ✅ DONE. Wrote and ran
  `lib/db/migrations/manual/2026-09-14-rbac-retainer-noconsent-gating-3974.sql` against local
  Postgres: seeded `msp_roles`/`customer_roles` rows for RetainerNoConsent/RetainerConsented,
  `rbac_capabilities` catalog rows for `ladder.retainer-no-consent`/`ladder.retainer-consented`,
  recomputed all `msp_feature_role_mapping` ladder floors from the current 8-rung
  LEGACY_ROLE_ORDER, and — a real gap found mid-build — updated the `rbac_sync_user_roles()`
  trigger function (#3408/#3590) whose hardcoded rung array had never been told about the two
  new rungs, so a RetainerNoConsent/RetainerConsented user's `*_user_roles` rung membership
  never synced (production's JWT-claim-based route gate doesn't depend on this, but
  AdminV2/`loadRbacContext` reads do). Edited `customer_feature_role_mapping`: explicit DENY for
  RetainerNoConsent on `team.manage`/`changes.approve` (tenant-scoped, no tenant exists);
  explicit ALLOW for RetainerNoConsent AND RetainerConsented on `marketplace.browse-full`
  (catalog breadth, not tenant-scoped — parity with Customer). Left `billing.view`/
  `billing.manage` untouched: the #3629 billed-party trigger already grants `Billing`
  independent of msp_role once a Retainer purchase creates an invoice/client_services row —
  confirmed live via a real `insertInvoice` in the new test.
  Wrote `lib/db/src/rbac/retainer-no-consent-gating.test.ts` (10 tests, registered in
  `lib/db/vitest.config.ts`'s explicit include list) — all pass against the real local DB.
  Ran `pnpm --filter @workspace/db run check-rbac-parity`: found 80 new fail-closed
  divergences (RetainerNoConsent/RetainerConsented have no old-model correspondent, so the
  legacy transcription's catchall branches disagree the same way #3360/#3590 already do for
  "unrecognised" roles) — registered 3 new `KNOWN_FAIL_CLOSED_DIVERGENCES` entries in
  `parity-check.ts` (one per real shape×capability pair, not per flag-combo) and re-ran clean:
  "4575 comparisons, ALL AGREED (5 registered fail-closed divergences)".
  `pnpm exec tsc --noEmit` clean in both `lib/db` and `artifacts/api-server`.
  `pnpm exec vitest run` in `lib/db`: 133/134 pass — the one failure
  (`admin.test.ts > resolveUserOrgId`) is the pre-existing, already-filed #3987
  (hardcoded user id 39 no longer in the local seed), unrelated to this change, confirmed by
  checking `users` directly (`id=39` does not exist) and that the hardcoding predates this
  session. `check-rbac-integrity` fails for the identical pre-existing reason (same hardcoded
  `CUSTOMER_USER = 39`).
  Live-simulated a real RetainerNoConsent principal against the actual production decision
  functions (`userClearsLadderCapability`, `userHasCapability` from
  `artifacts/api-server/src/middlewares/rbac-ladder.ts`/`rbac-capability.ts`, not a mock):
  `ladder.free` → allow, `ladder.customer-user` → deny (so every tenant-scoped route gated at
  the Customer floor — e.g. `portal-active-directory.ts`'s `requireCapability("ladder.customer-user")`
  — 403s), `team.manage`/`changes.approve` → deny, `marketplace.browse-full` → allow,
  `billing.view` with no bill → deny (correctly delegated, not unconditional). Scratch
  verification script deleted after use, not committed.
  No findings filed — the two real gaps found mid-build (missing role/ladder seed rows, stale
  sync-trigger rung array) are both squarely #3970 step-4 work and are fixed in this same
  migration, not separate issues. `git status --porcelain` clean. Pushed to `origin/main` as
  `d546c8610`; `verify-branch-merged.mjs` confirms ancestry.
