# #3664 — Remove dead AI-analysis polling in RunResultDetailPanel.tsx

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** b5750a6de4350f95a6a6afd90cd48ebfbdabf58f

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — removing pollAnalysis/analysisPollRef/isAnalyzing dead code and its never-built GET /api/admin/script-runs/:id call from RunResultDetailPanel.tsx, plus sweeping for other customer_upload/AI-analysis remnants in the same component/callers.
- 2026-09-12T00:20:00Z ✅ DONE — Removed `pollAnalysis`/`analysisPollRef`/`isAnalyzing` state+effect and the
  `GET /api/admin/script-runs/:id` call (no such route exists in `artifacts/api-server/src`). Also removed
  the `isAnalyzing` prop and its "AI analyzing results…" loading skeleton from `FindingsTab`, and the header
  "AI analyzing…" badge that consumed it. Dropped now-unused `useRef`/`useCallback` imports. `RunResultDetailPanel.tsx`
  still renders correctly for the real remaining cases (automated/manual runs, awaiting_upload manual-upload flow,
  score-impact/m365-score/raw-output tabs) — those paths were untouched. Verified via `tsc --noEmit` on
  admin-panel: no errors reference this file. Swept the component/callers for other `customer_upload`/AI-analysis
  remnants: the only remaining reference is the `executionSource: "... | "customer_upload"` type literal in
  `RunResultsSidebarPanel.tsx` (a historical data-value tag, not dead code calling anything) — left as-is.
  Separately found while sweeping: `AwaitingUploadActions` (manual-execution upload flow, status `awaiting_upload`)
  calls `POST/GET /api/admin/manual-scripts/:id/{download,upload}`, which also have no registered route in
  `artifacts/api-server/src/routes` — filed as a new finding (see issue link), out of scope for this task since
  it's the separate "manual" execution flow, not the customer_upload/AI-analysis path this issue targeted.
  Filed as #3845 (sibling sub-issue of Feature #3420), board status AI Batter Up.
