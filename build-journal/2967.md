# #2967

- **Status:** 🔄 IN FLIGHT — Posted: 2026-09-06T00:53:26Z
- **Scope:** artifacts/shane-mccaw-consulting
- **Dispatch:** Posted: 2026-09-06T00:53:26Z — `--model claude-opus-4-8 --effort high --title 2967` — Fix revenue-blocking Stripe retainer checkout bug (invalid `colorBackground` alpha) on `production-base-2026-09-05`.

## Log
- Posted: 2026-09-06T00:53:26Z — Pulled #2967 body + only comment (the dispatch itself; nothing superseding). Confirmed root cause in `artifacts/shane-mccaw-consulting/src/pages/work-with-me/RetainerCheckout.tsx` line 78: `colorBackground: "rgba(2,6,23,0.55)"` — Stripe's `appearance.variables.colorBackground` rejects alpha (HEX/rgb()/hsl() only), which breaks the Payment Element's ready-state and makes `confirmPayment()` throw an out-of-band `IntegrationError` that strands the buyer on "Confirming with Stripe…". Plan: (1) opaque approximation of the translucent slate for `colorBackground`, (2) ~20s timeout/fallback in `pay()` so any future hang surfaces an error instead of an infinite spinner, (3) verify end-to-end through the real browser UI with a Stripe test card. Working in the `production-base-2026-09-05` worktree (`C:/wt/2965-q1715`).
