# #2095 — AdoptedReadOnly composer: no way to thread typed text through as a --resume follow-up

- **Status:** ✅ DONE 2026-09-06T15:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** aafd05d76 (fix), 74824a8c4 (IN FLIGHT bookend)

## Log
- 2026-09-06T15:25:00Z ⏳ IN FLIGHT — restoring a Send affordance to ComposerMode.AdoptedReadOnly, reusing the exact existing SendSlotInput/HandleBuildSend continuation branch (already proven for Terminal mode); wiring is only a XAML visibility + CanSend gate change, no new backend logic needed since OwnsInteractive is always false for an adopted build.
- 2026-09-06T15:45:00Z ✅ DONE — Extended `ChatTranscriptModel.CanSend` to include `ComposerMode.AdoptedReadOnly`; changed the shared composer TextBox/Send Border's visibility in `ChatSessionPane.xaml` from `InteractiveOrTerminal` to `Interactive,Terminal,AdoptedReadOnly` (the multi-value form `ComposerModeVisibilityConverter` already supported). Since `ChatSessionPane` is the one shared control both `BuildWatchWindow.SendSlotInput` and `GitDetailView.HandleBuildSend` already wire `SendRequested` into, no new backend/dispatch code was needed — both call sites' existing branch-2 (`OwnsInteractive` always false for adopted) continuation now fires for typed AdoptedReadOnly text automatically. Kept "▶ Resume Session" as a separate, visually distinct one-click action (original prompt, no typed guidance) per the issue's option 2 — typed text + Send is the with-guidance path, both landing on the same resume continuation. Removed the now-redundant Stop button from the AdoptedReadOnly note grid (Stop already lives on the composer above it once AdoptedReadOnly shares that Border) to avoid a duplicate control. Reworded the AdoptedReadOnly placeholder text (BuildWatchWindow.SetSlotState, ApplyInteractiveState, GitDetailView.ReconcileStatusPill) away from the live-stdin "steer it mid-task" wording, since Send here always launches a fresh continuation, never a stdin write. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `node scripts/dev-server/verify-branch-merged.mjs` confirmed `agent/2095-q1785` (c7f7683db) merged into `origin/main`. `git status --porcelain` clean.
