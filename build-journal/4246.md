# #4246 — Delivery Projects Kanban: relocate the real typed-card UI from Admin Panel to MSP Console

- **Status:** ✅ DONE 2026-09-15T14:33:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** `96443115e0fb76f08d1fd62b683dff931759068e`

## Log
- 2026-09-15T14:04:22Z ⏳ IN FLIGHT — Confirmed #4245 (blocker) is closed/complete and its re-gate landed on main. Audited real scope: `pages/crm/ProjectDetail.tsx` (3390 lines, real dnd-kit drag-and-drop + SSE kanban board), `pages/crm/Projects.tsx` (978 lines, global project list — NOT tenant-scoped), `components/KanbanCardModal.tsx` (1592 lines), `components/kanban/TypedCardContent.tsx` (1332), `ChecklistClosureDialog.tsx`/`ChecklistClosureForm.tsx`. Traced every dependency (`@/hooks/use-toast`, `@/lib/auditFormatter`, `@/lib/scriptPoller`, `@/components/RunScriptConfirmDialog`, `@/components/RunLibraryScriptDialog`, `@/components/ui/dialog`, `@/components/ui/alert-dialog`, `@/components/ui/button`, `@dnd-kit/core`) and confirmed which are safe to move outright vs. must stay in admin-panel because a genuinely unrelated admin-panel page also uses them (`RunLibraryScriptDialog.tsx` ← `ScriptGeneratorPage.tsx`; `lib/auditFormatter.ts` ← `ActivityLog.tsx`; `lib/scriptPoller.ts` ← both of those). Found a real entanglement: `pages/workspaces/DeliveryWorkspace.tsx`'s default case renders the CRM `Projects.tsx` list live at `/delivery/projects` — this is a genuinely separate live consumer the issue text didn't anticipate; resolving it below rather than stopping, since it's a routing/plumbing fact, not a product decision.
- Nav placement decision: msp-console's `nav.ts` already documents that its own `MSP_PAGES` "projects" entry (Simple Kanban, #3773, `/ops/projects`) is a *different* system from this one (its own comment names `ProjectDetail.tsx` as this issue's target explicitly). Since admin-panel's `Projects.tsx` list is genuinely global (fetches `/api/admin/projects` across all clients, not filtered by tenant), this belongs in Operations (`MSP_PAGES`), not the per-tenant `CHILD_GROUPS` tree. Placed as a new leaf `delivery-projects` ("Delivery Projects", icon `layers` — distinct from the existing `kanban` icon already used by Simple Kanban) inside the existing "Client Delivery" `MSP_GROUPS` bucket, mounted at `/ops/delivery-projects`.
- 2026-09-15T14:33:00Z ✅ DONE — Relocated `ProjectDetail.tsx`, `Projects.tsx` (→`ProjectsList.tsx`), `KanbanCardModal.tsx`, `components/kanban/*` into `artifacts/msp-console/src` (real dnd-kit drag-and-drop, live `kanban-events` SSE, typed-card rendering all preserved byte-for-byte apart from routing plumbing). Ported wouter routes to local component state via a new `DeliveryProjects.tsx` wrapper. Copied (not moved — admin-panel's own `ActivityLog.tsx`/`ScriptGeneratorPage.tsx` still need them) `auditFormatter.ts`, `scriptPoller.ts`, `RunLibraryScriptDialog.tsx`; added a sonner-backed `useToast` adapter since msp-console has no shadcn toast stack; copied shadcn `dialog`/`alert-dialog`/`button` primitives; added `@dnd-kit/core`, `@radix-ui/react-dialog`, `@radix-ui/react-alert-dialog`, `@radix-ui/react-slot` to msp-console's `package.json`. Added Material Symbols Outlined font link to msp-console's `index.html` (KanbanCardModal/TypedCardContent use it). Ran `pnpm install` **in the main checkout** (`C:\Source\...`, not this worktree — the new deps resolved from the existing lockfile graph via admin-panel's own identical version pins, 0 downloaded/0 added), copied the resulting `pnpm-lock.yaml` into this worktree, then re-synced junctions via `linkDeps()` — never ran `pnpm install` from inside the worktree itself. Re-gated exactly two additional read routes (`GET /admin/clients`, `GET /admin/workflow-templates`) from `requireAdmin` to `requireCapability("ladder.msp-operator")` — the New/Edit Project pickers this relocated page depends on that #4245's own scope (`admin-projects.ts` only) didn't cover; left every other route in those two files untouched. Dropped two dead cross-app links with no msp-console equivalent (`Client →` to `/crm/clients/:id`, `Open Script` to `/command/scripts`) rather than stubbing them. In admin-panel: deleted the relocated files, redirected `/crm/projects` and `/crm/projects/:id` to `/delivery/projects`, replaced `DeliveryWorkspace.tsx`'s default case (previously the live Projects list — a real consumer the issue body didn't mention) with a "moved to MSP Console" notice linking to `/msp-console/ops/delivery-projects`, relabeled the admin-panel sidebar's "Projects" entry to "Projects (Moved)". **Verification:** `tsc --noEmit` clean (0 errors) on msp-console, admin-panel, and api-server; `vite build` succeeds on both msp-console and admin-panel. Live-in-browser verification was blocked by a pre-existing, unrelated environmental issue — the shared serving checkout (`C:\Source\...`) has a diverged local `main` (2 unpushed commits ahead, ~40+ behind origin at time of check) that isn't mine to force-reconcile, so `request-restart.mjs` merged my commit into the `C:\dev-server` mirror but reported NOT LIVE on the actual serving ports (the known dev-server/serving-checkout desync class, not introduced by this build). **Two real findings filed** as direct sub-issues of #3433 (Feature-tier parent), board status "AI Batter Up": **#4254** (`bug`) — `TypedCardContent.tsx` calls a dead `/api/admin/runbook-jobs` endpoint (pre-existing since #3882, reproduced as-is in the relocated copy — a real design question, not a one-line path swap). **#4255** (`bug`+`security`) — the typed-card "Run Script" action still 403s for non-admin MSP operators because `POST /api/admin/run-script` and `GET /api/admin/clients/with-azure-credentials` remain `requireAdmin`-only; deliberately not re-gated here since widening who can trigger Azure Automation script execution against a client tenant is a real security decision, not a mechanical extension of #4245's read-only-picker pattern. Working tree clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
