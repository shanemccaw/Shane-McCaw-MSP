# #3342 — URGENT: items sitting in the Build Queue aren't being built

- **Status:** ✅ DONE 2026-09-09T20:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Commit(s):** 9d32d8f3a (fix), plus this bookend

## Log
- 2026-09-09T20:01:59Z ⏳ IN FLIGHT — Investigating three connected symptoms: (1) Batter Up → local queue auto-pull never firing, (2) Batter Up count dropping to 0 after a dispatch, (3) queued items not auto-claiming/launching. Tracing item 1 first per Shane's revised scope.
- 2026-09-09T20:20:00Z ✅ DONE — Root cause of "nothing launches" (symptom 3 + "still had to manually launch") found LIVE and fixed: a persisted "Build Only This Set" exclusive hold on build set **"ShanesLife"** never auto-cleared, so `SelectClaimCandidatesAsync` held every queued row of every OTHER set indefinitely. The hold's only auto-clear required ALL members terminal but counted only done/failed/canceled — ShanesLife had 2 `superseded` members among 189 otherwise-terminal rows, so `members.All(terminal)` was false forever. Confirmed live: 0 active builds while #3338/#3340 sat unblocked/"up next".
  - **Fix (commit 9d32d8f3a):** `IsTerminalStatus` now includes `superseded` (single canonical terminal definition; both UI auto-clear checks call it, the drifted local set removed). Added a dispatch-path self-heal in `SelectClaimCandidatesAsync`: a stale exclusive hold whose set has no non-terminal rows is cleared right there on the watcher tick, independent of the UI panel.
  - **Verified:** `dotnet build` 0 warnings/0 errors. Self-heal EXISTS query run against live local Postgres → `f` for ShanesLife (clears) / `t` for BuildConsole (correctly stays held while #3338/#3340 queued). BuildConsole is out of scope for test manifests (per CLAUDE.md) — verification is build + live-DB validation, not a manifest.
  - **Shane action:** rebuild + restart BuildConsole to pick this up; on the next watcher tick (~90s app-ready + 1 tick) the stale "ShanesLife" hold auto-clears and #3338/#3340 launch. The current running instance won't self-heal until restarted (the hold is loaded into memory at startup; the file is Shane's runtime state, left untouched).
  - **Finding filed:** #3343 (sibling of Feature #1789, `bug`, board "AI Batter Up") — distinct #3337 regression behind symptoms 1 & 2: the incremental mirror sync INSERTs new/just-moved issues with NULL board status (board status is full-walk-only, 30-min), so a just-moved-to-Batter-Up item (#3342) is invisible to the Batter Up mirror read / Free Flow auto-queue for up to 30 min. Not data corruption (existing rows' board status is preserved). Left for Shane's triage — partly a deliberate #3337 latency tradeoff.
  - `git status --porcelain` clean; branch merged to origin/main and verified.
