# #3104 — POA&Ms: real tier-gating, customer Portal side only

- **Status:** ✅ DONE 2026-09-07T21:20:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** cfdba9e6506dbbd94b21261fc136a21dd8a134a7

## Log
- 2026-09-07T21:13:55Z ⏳ IN FLIGHT — add `poams` module key to `portal-tier-features.ts`,
  apply `requireTierFeature("poams")` to the customer-facing Portal read routes in
  `portal-poams.ts` (list + get-by-id), matching #1487/#1490's own pattern. MSP-console
  routes (`msp-poams.ts`) stay untouched/ungated. Tier assignment itself (which
  Foundation/Growth/Premier tier(s) actually bundle POA&Ms) is a separate real
  pricing/product decision — not obvious from an existing convention (POA&M isn't a
  1:1 clone of Risk Register, it's a new object type), so this build adds the gate
  mechanism only and does NOT write the `includedFeatures` migration assigning it to a
  tier. Flagging that as a follow-up for Shane rather than guessing.
- 2026-09-07T21:20:00Z ✅ DONE — added `poams: "poams"` to `PORTAL_TIER_MODULE_KEYS`
  in `artifacts/api-server/src/lib/portal-tier-features.ts`; applied
  `requireTierFeature(PORTAL_TIER_MODULE_KEYS.poams)` to both customer-facing READ
  routes in `artifacts/api-server/src/routes/portal-poams.ts`
  (`GET /portal/poams`, `GET /portal/poams/:poamId`) — matching the exact
  `policyDecisions`/`riskRegister` pattern in `portal-risk-register.ts`. The two POST
  routes (create, sign) stay unconditional per #1168's "creation always unconditional"
  rule, and `msp-poams.ts` (MSP-console side) is completely untouched/ungated, per
  Shane's decision comment on #3080. Because no `services.type_attributes
  .includedFeatures` row anywhere carries `"poams"` yet (that tier-assignment SQL is
  intentionally NOT part of this build — real pricing decision, separate from this
  issue), both gated routes currently 402 for every customer until Shane decides which
  tier(s) include it and that migration is written — fails closed, not silently open.
  Verified: `tsc --noEmit` clean on `@workspace/api-server`; existing
  `portal-tier-features.test.ts` (12 tests) still green. `verify-branch-merged.mjs`
  confirms `agent/3104-q1869` merged into `origin/main`; `git status --porcelain`
  clean. Commit: cfdba9e6506dbbd94b21261fc136a21dd8a134a7.
