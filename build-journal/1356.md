# #1356 — Phase 4 of #1352: real scan trigger at consent callback

- **Status:** ✅ DONE 2026-09-13T05:50:00Z (no code change — dispatched task retracted, already satisfied)
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 9388700b2 (IN FLIGHT bookend; this DONE bookend is a separate follow-up commit)

## Log
- 2026-09-13T05:45:00Z ⏳ IN FLIGHT — investigating dispatched task (add new Prospect scan-kickoff call site); a later comment on #1356 (2026-09-13T05:22:08Z) retracts the dispatch, claiming the consent callback already fires the scan for Free Scan Prospects. Verifying that claim by direct code read before doing anything else.
- 2026-09-13T05:50:00Z ✅ DONE — Verified the retraction directly against code (not just trusting the comment): `artifacts/api-server/src/routes/consent.ts` (`GET /api/consent/callback`) already resolves `customerId: inviteRecord?.customerId ?? prospectCustomerId ?? consentTenant.id` (line ~1013) and fires `runDiagnostics` fire-and-forget at consent time, where `prospectCustomerId` is set (lines ~903-905) at the exact branch documented as the Free Scan / #1355 Prospect path. `artifacts/api-server/src/lib/monitoring-onboarding-scan.ts`'s own header comment independently confirms "A scan trigger already exists: the shared GET /api/consent/callback ... fires runDiagnostics() fire-and-forget at consent time" and that its own module is a narrower later backstop scoped only to paid `serviceType === "monitoring_tier"` purchases, not the mechanism Free Scan needs. Building the dispatched new function would have created a second, duplicate scan-trigger implementation this issue's own definition-of-done explicitly rules out ("no second scan-triggering implementation"). No code changed. Posted honest findings comment on #1356. Not closing the issue myself (Shane's call, matching the two prior comments' own recommendation to close-as-satisfied or narrow).
