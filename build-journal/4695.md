# #4695 — BuildConsole: local "remove from queue" action, decoupled from GitHub

- **Status:** ✅ DONE 2026-09-18T17:39:27Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** b2dbd96dd (code); plan in build-journal/4695-plan.md

## Log
- 2026-09-18T17:33:30Z ⏳ IN FLIGHT — investigating the existing Mark Complete / Stop actions and bt_build_queue status vocabulary before planning.
- 2026-09-18T17:39:27Z ✅ DONE — added a local-only "🗑 Remove from Queue" card context-menu item + `BuildQueuePostgresClient.DismissFromQueueAsync` (one UPDATE reusing the existing `archived`/`archived_at` columns; no new status, no schema change, no migration). dotnet build 0 warnings/0 errors; app not launched (#2156). No findings filed (Mark Complete removal is already #4694).

## Changes Made
- `Services/BuildQueuePostgresClient.cs` — new `DismissFromQueueAsync(id)`: sets `archived=TRUE, archived_at=NOW()`; queued/parked/limit-paused/capped/external rows also become `canceled` with `exit_code=-1` (can never launch; -1 not 0 so it is not read as a ⏳ WAITING self-block); done/failed/verifying/canceled/superseded keep their status; `running` refused (0 rows) — use the existing ⏹ Stop first. Never a DELETE.
- `Controls/BuildQueuePanel.xaml.cs` — new "🗑 Remove from Queue (local only)" context-menu item (disabled on running rows, requires the direct DB, logs to ActivityLog, then `RefreshAsync`); `ApplyFilter` excludes archived rows from every filter except "Archive"; `IsCrashed`, `ComputeQueueStatusCounts`, `RenderBuildSetRollup`, `ResolveBuildSetEpicsAsync` skip archived rows; `RevealQueueItem` routes any archived row to the Archive filter.
- Not touched: "Mark Complete (Hide)" (its removal is #4694), Stop's own reap/board-mirror behaviour, no Restore action (row stays fully intact under the Archive filter).

## Verification Results
- `dotnet build` desktop/BuildConsole: 0 warnings, 0 errors.
- Real local `BuildConsole` DB, inside a transaction that was ROLLED BACK (residue count 0), using synthetic `zz-4695-*` rows and the same UPDATE as the C# method (hand-copied into psql, not invoked through the app): queued/parked/limit-paused → canceled, exit -1, archived, archived_at set; done and verifying → status unchanged, archived; running → untouched (refused); a second run changed 0 rows (idempotent).
- Persistence: `archived` is a DB column, so it survives refresh/restart by construction; NOT verified by restarting the app or clicking the menu item (app not launched, #2156).
- Zero GitHub: grep shows no GitHub/BoardStatusSync/_api reference in `DismissFromQueueAsync` or the menu handler; the post-action `RefreshAsync` only calls GitHub on a transition INTO verifying/done (`AutoRecheckOpenIssuesOnTransitionAsync`), which dismiss never causes. Not verified from a live ActivityLog capture of a real click.
- `verify-branch-merged.mjs`: OK — HEAD is an ancestor of origin/main.
