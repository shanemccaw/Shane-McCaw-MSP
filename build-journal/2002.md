# Build Journal — Issue #2002

Usage meters wrong in three ways: Primary row shows toggled account, weekly reset falls back to hardcoded Sunday 9pm, Secondary errors every poll.

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Build/Queue:** 1045 · **Account:** primary

## Log

- ⏳ IN FLIGHT — Session opened (build 1045). Investigation found the substantive fix for all three defects is **already committed and merged to origin/main** by two prior commits — `c1b9000ed` (Fix Claude usage meter status bar defects) and `ef91b5f14` (Replace CLI text scraping with Anthropic OAuth usage API) — both confirmed ancestors of `origin/main`; my worktree copy of `ClaudeUsageMeterService.cs` is byte-identical to origin/main. Prior session landed the code but never wrote this bookend, flipped the label, or commented on the issue. This session verifies the merged state and closes out the bookkeeping.
