# #2622 — Account Security (MSP Console): regenerate the contract pack from the finished backend

- **Status:** ✅ DONE 2026-09-06T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 43e2ab228 (pack, agent/2622-q1738 → merged to origin/main as 3c1b78391)

## Log
- 2026-09-06T14:05:00Z ⏳ IN FLIGHT — Leaf issue under Feature #2562 (Account Security,
  MSP Console), sibling of #2574 (MSP Staff Roles and Onboarding). Dispatch confirms real
  backend in `msp-settings.ts` reset-password/MFA routes. Read-only pack regeneration per
  the #1642 pattern. Investigating which `msp-settings.ts` routes are genuinely
  Account-Security-support-scoped (MSP staff assisting a *customer* with their own
  security, per #2562's body) vs. MSP-staff-roster management (already fully documented
  in `docs/msp-staff-roles-and-onboarding-contract-pack.md` under #2574) — must not
  duplicate scope across the two packs.
- 2026-09-06T18:10:00Z ✅ DONE — Wrote
  `docs/account-security-msp-console-contract-pack.md` (~230 lines). Real finding: the
  same five `msp-settings.ts` routes documented in the #2574 pack's §4
  (reset-password/temp-password/reset-mfa/mfa-enforcement/status) are also the ONLY
  backend that could serve #2562's stated scope — no tenantId/customerId-scoped
  "assist a managed-tenant customer" route exists anywhere in the repo (confirmed via
  a research sub-agent + independent greps across msp-staff.ts, msp-staff-search.ts,
  msp-support.ts, admin-impersonation.ts). Customer-impersonation (`msp-staff.ts:11`)
  is real but explicitly out of scope per #2562's own "never a shared/bypass
  credential" wording. Traced `usersTable.mspId` vs `.tenantId` via the
  `users_role_scope_check` CHECK constraint — `mspId` on real `CustomerUser` rows is a
  Phase-0 merge artifact, not a deliberate "these are our customers" design.
  **Real security finding filed and parented: #3032** (URGENT, `bug`+`security`,
  sibling sub-issue of #2562, board status "AI Batter Up") — none of the five routes
  compares target `mspRole` to caller `mspRole`; a real MSPAdmin at the same MSP could
  reset/hijack the owning PlatformAdmin's own account today (not yet exploited — zero
  real MSPAdmin/MSPOperator accounts exist in the live DB). Also corrected one stale
  claim in the #2574 pack (not edited — out of this issue's scope): its "no test
  exercises mfa-enforcement/sessions" line predates `msp-settings-user-security.live-db.test.ts`
  (commit `aed211957`, 2026-09-04), which now covers both. Read-only: only the new pack
  + this bookend changed. Committed `43e2ab228`, merged to `origin/main` as `3c1b78391`;
  verified with `verify-branch-merged.mjs` (exit 0). `git status --porcelain` clean.
