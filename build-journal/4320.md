# #4320 — GET /admin/clients/enriched has no CLIENT_LADDER_ROLES/mspId scoping

- **Status:** ✅ DONE 2026-09-16T16:33:30Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** ac926513ecc3f03d38c15dc2a8ba56595136aab3

## Log
- 2026-09-16T16:32:31Z ⏳ IN FLIGHT — apply CLIENT_LADDER_ROLES + mspId scoping (mirroring admin-clients.ts:591-604, #4256) to GET /admin/clients/enriched at admin-clients.ts:41-49.
- 2026-09-16T16:33:30Z ✅ DONE — `GET /admin/clients/enriched` (admin-clients.ts:41-68) now filters `usersTable.mspRole IN CLIENT_LADDER_ROLES` and scopes to `resolveMspId(req)` when resolved, exactly mirroring the `/admin/clients` pattern (#4256). Typechecked clean (`tsc --noEmit`, no errors in admin-clients.ts). No dev-server instance was reachable locally to hit the route over HTTP, so verified the exact filter logic directly against the real local Postgres DB: before the fix, `role='client'` returned 8 rows including the 5 RBAC-ladder staff accounts (ServiceAccount/MSPOperator x2/MSPAdmin/PlatformAdmin, all `mspId=1`); running the new `role='client' AND mspRole IN CLIENT_LADDER_ROLES` filter (cross-platform, no `?mspId=`) against the same table returns exactly the 3 real customer-ladder rows (Free, Customer, Customer) and excludes all 5 staff rows. No migration needed (no schema change). Branch verified merged into origin/main via `verify-branch-merged.mjs` (exit 0). Working tree clean.
