# #3371 — DLQ (Dead Letter Queue) (MSP Console): real contract pack extraction

- **Status:** ✅ DONE 2026-09-10T01:53:35Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** cfe126f64 (bookend IN FLIGHT), 5f671e2a6 (pack, → 13cb0ccc8 on origin/main)

## Log
- 2026-09-10T01:49:53Z ⏳ IN FLIGHT — Read-only contract-pack extraction per #1642's pattern for
  `artifacts/api-server/src/routes/msp-dlq.ts` (4 routes: GET /msp/dlq, POST
  /msp/dlq/:dlqId/replay, PATCH /msp/dlq/:dlqId, POST /api/msp/dlq/bulk-replay). Traced the full
  `msp_dlq_store` landscape: this module (surface A) plus two siblings sharing the same table
  (`admin-dlq.ts` surface B, `portal-wf-api.ts`'s `/dlq` block surface C), all four writers
  (`portal-workflow-engine.ts`, `msp-jobs.ts`, `zoho-batch-drain.ts`, `engagebay-batch-drain.ts`),
  and `replayDlqItem`'s real mechanics. Queried live local Postgres directly: 32 real
  `msp_dlq_store` rows, all Zoho job-queue failures, 0 with a `workflowKey` in payload, 0
  resolved. Live-verified via curl against the running local dev server (localhost:8080) that
  `POST /api/msp/dlq/bulk-replay` 404s at its documented path (double-`/api` mounting bug) while
  `POST /api/api/msp/dlq/bulk-replay` reaches real auth middleware (401). Writing
  `docs/msp-console/dlq-msp-console-contract-pack.md` now; will file the confirmed gaps as sibling
  sub-issues of #1571 (no Feature-tier parent exists for #3371) before closing out.
- 2026-09-10T01:53:35Z ✅ DONE — Wrote and pushed
  `docs/msp-console/dlq-msp-console-contract-pack.md` (8-section pack: 3-surface map over
  `msp_dlq_store`, per-route wire contract for all 4 `msp-dlq.ts` routes, real enum unions,
  every writer of the table, object/table map, cross-surface structural facts, the live
  honest-empty contract, and open gaps). Filed the 3 real, live-verified gaps found as sub-issues
  of #1571 (no Feature-tier parent exists for #3371), all milestone v1.1, label `bug`, board
  status "AI Batter Up":
  - **#3445** — `msp-dlq.ts:190`'s bulk-replay route is double-`/api`-prefixed, 404s at its
    documented path; live-verified via curl against localhost:8080 (404 vs. 401 one level down).
  - **#3446** — `msp-dlq.ts`'s Replay/Bulk Replay routes call `replayDlqItem` with no
    replayability precheck (unlike sibling `admin-dlq.ts`'s `isReplayable()`); confirmed live that
    0 of the 32 real rows currently in `msp_dlq_store` have a `workflowKey`, so Replay 500s for
    every row a technician could click today.
  - **#3447** — `docs/runbooks/dlq-replay.md` describes replay mechanics (atomic `attemptCount`
    increment, re-dispatch to the original handler) that don't match `replayDlqItem`'s real
    implementation; the increment helper it describes (`incrementDlqAttempt`) is dead code with
    zero call sites.

  No genuine incompleteness marker found in `msp-dlq.ts` itself — confirmed per the issue's own
  instruction not to assume completeness from line count alone; the two real breakages found are
  documented honestly in §7 rather than papered over. Read-only: only the pack + this bookend
  changed, no product code touched. `verify-branch-merged.mjs` exit 0 (`13cb0ccc8` on
  `origin/main`); `git status --porcelain` clean.
