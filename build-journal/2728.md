# #2728 — sla_timers status default mismatch + scope-creep staff-scoping gap

- **Status:** ✅ DONE 2026-09-04T04:11:41Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 764d3a8ef (pushed as d258cd758 after rebase)

## Log
- 2026-09-04T04:03:09Z ⏳ IN FLIGHT — investigating sla_timers status default mismatch (startSlaTimer never sets status='running' while every reader expects 'running') and missing resolveStaffScopedCustomerIds() filter on GET /msp/scope-creep/escalations and GET /msp/scope-creep/compliance. Part of #2572.
- 2026-09-04T04:11:41Z ✅ DONE — picked direction (b): sla_timers.status column DEFAULT changed 'active' → 'running' (artifacts/api-server/src/index.ts), startSlaTimer() now also explicitly INSERTs status='running' (sla-engine.ts), GET /msp/sla/summary's activeTimers count updated to read status='running' instead of the dead 'active' value (msp-sla.ts). Added resolveStaffScopedCustomerIds() filter to GET /msp/scope-creep/escalations and GET /msp/scope-creep/compliance (msp-scope-creep.ts), matching the sibling detections/violations GETs' pattern. Ran lib/db/migrations/manual/2026-09-04-sla-timers-status-default-running-2728.sql against local DATABASE_URL myself (ALTER COLUMN DEFAULT + defensive UPDATE of any stray 'active' rows — 0 rows affected, confirms issue body's "0 live sla_timers rows" claim). Verified column default now 'running' via psql. `tsc --noEmit` on artifacts/api-server passed clean (exit 0). Filed a related finding as #2811 (warningTimers permanently 0 — nothing ever sets status='warning'), parented under #2572, board status set to AI Batter Up. Branch verified merged into origin/main (d258cd758). Working tree clean.
