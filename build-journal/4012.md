# #4012 — Audit Log: wire the MSP Console UI to the real endpoint (both mounts)

- **Status:** ✅ DONE 2026-09-14T05:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 1b07ed8a7

## Log
- 2026-09-14T04:31:12Z ⏳ IN FLIGHT — Read `Audit Log.dc.html` + README screen 63 pack and tree-placement table. Confirmed `GET /api/msp/audit` (`msp-audit-log.ts`) already has the `customerId` filter (#3671) live on `main`, and `mspAuditLogsTable`'s real columns match the design's "stored but never on the wire" list exactly (customer_id, entity_id, correlation_id, ip_address, user_agent, actor_service_account_id). Building `audit-log-api.ts` + `AuditLog.tsx`, mounted twice via `ConsoleShell.tsx` (tenant leaf `audit` with `customerId`/`customerName`, and `/ops/audit` with neither).
- 2026-09-14T05:10:00Z ✅ DONE — Shipped `artifacts/msp-console/src/api/audit-log-api.ts` (typed `useAuditLog` hook + `isFallbackActor`/`actionConvention`/`HIDDEN_COLUMNS` helpers, no fixture data) and `artifacts/msp-console/src/console/modules/AuditLog.tsx` (search/action-type/date/outcome filters, PlatformAdmin MSP-scope chips, paginated table, raw-metadata detail panel). Mounted twice from `ConsoleShell.tsx`: `sel.kind==="page"&&sel.page==="audit"` (per-tenant, `customerId`/`customerName`) and `sel.kind==="msp"&&sel.page==="audit"` (Operations, unscoped). Added the `audit` entry to `MSP_PAGES` in `nav.ts` (the per-tenant `CHILD_GROUPS` leaf already existed) and bespoke `pageMeta` copy for both mounts in `treeModel.ts`. Verification: `pnpm --filter msp-console run typecheck` and `run build` both clean (re-ran after rebasing onto two concurrent landings — Staff Roster #2662, Launch Control #2615 — merged without conflict); direct local-Postgres query confirmed 602 real `msp_audit_logs` rows (14 failures, 0 partial, 48 carrying a customer id) to exercise against; `GET /api/msp/audit` on the running dev api-server returns a real 401 (route mounted, auth-gated) confirming the endpoint is live. Honest gap: no interactive browser/`shaneapp://runTest` pass was done — no MSP-console test login credentials were available in this session, and I did not attempt to reset or guess Shane's real admin password to get one. No fixture/mock data anywhere in the new files.
