# #4486 — Cancelling a purchased add-on subscription never revokes tenant_add_on_entitlements

- **Status:** ✅ DONE 2026-09-17T05:17:30Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** a1db47023
- **Migration:** none (the `canceled` status was already allowed by the CHECK constraint)
- **Findings filed:** none

## Log
- 2026-09-17T05:10:30Z ⏳ IN FLIGHT — adding `customer.subscription.deleted` handling to the portal Stripe webhook so a cancelled portal_add_on subscription flips its entitlement row to `canceled`; live-verify in Stripe test mode.
- 2026-09-17T05:17:30Z ✅ DONE — a1db47023. `POST /api/portal/stripe/webhook` now has a `customer.subscription.deleted` case. When `subscription.metadata.checkout_kind === portal_add_on`, it calls `cancelPortalAddOnSubscription` (portal-add-ons.ts). That runs `cancelAddOnEntitlementForSubscription` (addon-entitlement-provisioning.ts), which updates only the `active` row matching `stripe_subscription_id` to `canceled`, then writes a `portal_add_on_canceled` audit row. A replay, or a late deletion of a subscription that a re-subscribe already replaced, matches nothing. The reactivation path was not changed. Live-verified in Stripe test mode with a synthetic zz-test-4486 msp/tenant/user. Created a real subscription sub_1UGXg4RQlCXtbN2mumtSfAf4 with the route's metadata shape, via the API rather than hosted Checkout, and provisioned it with ensureAddOnEntitlement: row 52 active, gate entitled. Cancelled it in Stripe. Fetched the real event Stripe generated (evt_1UGXg8RQlCXtbN2mmRvPgne1), signed it with the real STRIPE_WEBHOOK_SECRET and POSTed it to the real router: 200, row 52 canceled, gate not entitled, audit row written. A bad signature got 400. Re-subscribed with sub_1UGXgARQlCXtbN2m8xe0DSOf: reactivated. Replaying the old deletion left the row active on the new sub. Stripe can't reach localhost, so delivery was a local signed POST, the same as `stripe listen`. The enabled test-mode endpoint for /api/portal/stripe/webhook already subscribes to customer.subscription.deleted. Live mode was not checked (production boundary). Fixtures, Stripe customer and subs removed; product archived. Tests: live-db 7/7 (one new), webhook node:test 12/12, api-server tsc 0 errors. Working tree clean; no pre-existing dirty files.
