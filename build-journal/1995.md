# #1995 — Batter Up scan silent early-stop after 3 empty pages

- **Status:** ⏳ IN FLIGHT
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1995's body + comments. Note: a later comment on the
  issue (2026-08-30T14:55:44Z) retracts the original framing — the early-stop does NOT
  explain Shane's reported "new items don't show up" symptom (that's #1997, a separate
  dedup-drop bug); the panel screenshot showed old NO-BUILD items stuck, not promoted
  items missing. The comment explicitly says this early-stop bug is still real and
  worth fixing on its own merits, independent of #1997 — proceeding on that basis.
  Confirmed root cause live before touching code: `PVT_kwHOEiBDdc4BeoiY` has 1,995
  items. Forward-scanned the whole board for Status=`a0296971` (AI Batter Up) — real
  matches cluster in forward pages 18-20 only. Promoted issue #741 (an old, unclassified
  issue sitting at forward position ~729, forward page 8 of 20) to AI Batter Up, then
  ran the OLD backward-walk-with-early-stop logic verbatim against the live board: it
  stopped at backward page 6 (3 consecutive empty pages after finding the recent
  cluster), returned 72 matches, and never reached #741 — bug reproduced exactly as
  the issue predicted. Fixed `ScanProjectItemsForStatusAsync`
  (`desktop/BuildConsole/Services/GitHubApiClient.cs`): removed `EmptyPageStopThreshold`
  and the early-stop entirely (board position carries no information about Status, so
  no position-based heuristic is sound); kept backward pagination as a scheduling
  optimization only; the walk now always continues to `hasPreviousPage == false` (or
  the pre-existing `MaxPages` runaway guard, which already logged loudly on exhaustion).
  Added a log line on the `git-board.data` channel on every natural end-of-board return
  (page count + match count), so a full walk is never silent either. Re-ran the FIXED
  full-walk logic against the same live board: reached backward page 20 (start of the
  1,995-item board), found 73 matches including #741. Cleared #741's Status field back
  to its original null value afterward (`clearProjectV2ItemFieldValue`), confirmed via a
  follow-up read that it's back to no Status — no residue left on the real board.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0
  warnings, 0 errors. All investigation scratch scripts (`scan-board-tmp*.mjs`,
  `sim-backward-*.mjs`, `promote-item.mjs`, `clear-item.mjs`, temp `.graphql` files)
  deleted before commit; `git status --porcelain` clean apart from this bookend and the
  one source file touched.
