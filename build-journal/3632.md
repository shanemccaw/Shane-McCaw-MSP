# #3632 — GitHubIssueMirror stays single-repo-scoped

- **Status:** ✅ DONE 2026-09-12T23:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** f3035b1009d0c5c011149872653030f5ac7f9669 ("GitHubIssueMirror: genuinely multi-repo board-status sync (Git #3632)")

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Read issue #3632 body + BUILD comment (local bt_issue_mirror + REST comments, gh graphql was rate-limited). Starting investigation of `desktop/BuildConsole/Services/GitHubIssueMirror.cs` and its callers to thread real per-row `repo_owner`/`repo_name` through every read/write, mirroring #3579's pattern for `bt_build_queue`/`bt_epics`/`bt_issues`.
- 2026-09-12T23:20:00Z ✅ DONE — Confirmed via `psql` that `bt_issue_mirror`'s schema already carries real `repo_owner`/`repo_name` columns + PK (from #3579) — no migration needed, this was purely a threading gap. Shipped:
  - `GitHubApiClient.GetAllIssueBoardStatusesAsync()` relaxed its board-item repo filter from "must equal this instance's own Owner/Repo" to `IsConfiguredRepo` (same relaxation #3582 applied to `ScanProjectItemsForStatusAsync`), and now returns a `List<IssueBoardStatus>` carrying each item's real `Number`/`Title`/`State`/`HtmlUrl`/`RepoOwner`/`RepoName` instead of a repo-blind `Dictionary<int,...>`.
  - `GitHubIssueMirror.SyncAsync` now persists a genuine mirror row (real title/state/board-status/url) for every OTHER configured repo's board item, not just the primary repo's open-issue walk (new step 4b), inside the existing sync transaction.
  - `TryGetByBoardStatusAsync` gained `allConfiguredRepos` (default `false` — every pre-existing caller unaffected); `BatterUpQueueService`/`AiBatterUpQueueService`'s `GetBatterUpBoardItemsAsync`/`GetAiBatterUpBoardItemsAsync`/`SweepClosedIssuesAsync` now read with it, resolving issue items 1 (secondary-repo discovery from mirror, not a live walk) and 2 (closed-sweep now covers secondary repos). The live secondary-repo scan is kept only as a de-duplicated gap-filler for the window between full syncs.
  - `MirrorIssue` now exposes its own `RepoOwner`/`RepoName` (columns already existed).
  - Item 3 (per-repo `DoneBookendVerifier`/blocker liveness for a secondary repo) is explicitly NOT done — the issue's own body already frames it as a future follow-up needing a per-repo local clone (tied to #3584's worktree-provisioning work), not something this session's scope covers.
  - Verification: clean `dotnet build` (0 warnings, 0 errors); the new secondary-repo upsert/select SQL validated against the real local `bt_issue_mirror` schema in a `BEGIN; ...; ROLLBACK;` transaction (confirmed round-trips correctly, nothing persisted). Full live end-to-end verification (a real second repo configured + a real board item in Batter Up on it) was NOT run — no second repo is currently configured in this BuildConsole instance's Settings (`ConfiguredRepos` is still seeded with just Main, exactly as the issue body states), so there is no live secondary-repo board item to observe today; the code path is ready for when one is configured.
  - `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.
