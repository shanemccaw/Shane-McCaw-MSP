# #3464 — Feature: Real Hour-Logging Integration (MyArchitect)

- **Status:** 🛑 BLOCKED 2026-09-10T05:58:17Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** none — no code changed

## Log
- 2026-09-10T05:58:17Z 🛑 BLOCKED — Pulled full issue body + all 4 comments before starting, per standing rules. The issue's real checklist (PUT remediation-tracker step wiring, POST human-action after Console-executed catalog scripts, POST unscoped hours ad-hoc UI) is real client wiring inside `desktop/MyArchitect`, the WPF app filed under Epic #3454.

  #3464 already carries real `blocked_by` edges to #3493 (UI Shell Redesign) and #3459 (Embedded PowerShell Console) — set by an earlier dispatch on this same issue (see the 04:46 BUILD comment with `--blocked-by 3493,3459`). Both are still OPEN, neither carries `complete`. Verified via `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3464/dependencies/blocked_by`.

  This matches the parent Epic #3454's own standing rule: "#3493 (UI Shell Redesign) must land before any other Feature's UI work." #3464's checklist items are UI/client wiring, and the catalog-backed execute path additionally needs #3459's console to exist to pre-fill/execute into. Neither premise has changed since the earlier dispatch set the edges.

  Per the "picking this back up later" protocol: dependency list is non-empty and neither dependency is closed/complete, so this session stops here rather than guessing forward. Applied `blocked` label, removed `in-flight`. Left the existing `blocked_by` edges in place (still accurate — not stale, not pointing at anything closed). Posted a plain-language comment on #3464 explaining the wait. No code changes; no unrelated files touched; working tree left clean.
