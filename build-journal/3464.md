# #3464 — Feature: Real Hour-Logging Integration (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:12:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 1e80dc2c8553702eab137ba636bf6b54f61a7909 — "Ad-hoc retainer hour-logging client (#3464)"

## Log
- 2026-09-10T05:58:17Z 🛑 BLOCKED — (prior session) see below for full text; both blockers have since closed.
- 2026-09-10T19:58:00Z ⏳ IN FLIGHT (resumed, unblocked) — #3493 and #3459 are both CLOSED with `complete` as of earlier today. Removed the now-stale `blocked_by` edges to both (`DELETE .../dependencies/blocked_by/<internal id>` for each), flipped the label `blocked` → `in-flight`. `gh api .../3464/dependencies/blocked_by` now returns `[]`.

  Re-audited the issue's real checklist against actual code (not assumed from the issue body alone):
  - **PUT remediation-tracker step** — `Services/IRemediationTrackerService.cs` + `RemediationTrackerService.cs` already exist as a real HTTP client for `PUT /api/msp/customers/:customerId/remediation-tracker/steps/:stepId`. Confirmed via `grep` this service is genuinely unused by `MainWindow.xaml.cs` (no UI trigger calls it yet) — the client exists but nothing in the shell fires it.
  - **POST human-action for catalog-backed changes** — `Services/IChangeControlService.cs`/`ChangeControlService.cs` (`RecordHumanActionAsync`) IS wired end-to-end: `MainWindow.xaml.cs`'s Home tab "Change Requests" gallery opens a real change-request record workspace with a confirm-armed "Record human action" button that calls it for real, existing `msp_change_requests` rows.
  - **POST human-action after Console-executed catalog scripts** — re-checked against the real backend: `POST /msp/change-control/executions/human-action` (`msp-change-executions.ts`) requires a real `changeRequestId` resolved via `loadScopedChange`, i.e. an existing `msp_change_requests` row. The Console's catalog-script path (`RunScriptLibraryAction` → `POST /api/msp/:mspId/launch-control/execute`, `msp-launch-control.ts`) is a completely separate system (`write_action_catalog`/`baseline_action_templates`) that never creates or references a change request — `LaunchControlExecuteResult` has no `changeRequestId` field, and `msp-launch-control.ts` has zero references to `human-action` or `changeRequestId`. The issue's premise ("MyArchitect calls this after executing a catalog action through the Console") does not correspond to a real mapping in the backend — there is no changeRequestId to attest against for a Launch Control execution. Filing this as a finding rather than inventing a fake changeRequestId or silently skipping it.
  - **POST unscoped ad-hoc hours** — confirmed genuinely unbuilt: no `AdminRetainerService`/model/UI anywhere in `desktop/MyArchitect`. This is the one real, buildable remaining item.

  Proceeding to build the ad-hoc hours client (service + shell UI) for the confirmed-real gap, and filing the human-action/Console-script mismatch as its own finding.
- 2026-09-10T20:12:00Z ✅ DONE — Built the real, missing ad-hoc hour-logging client:
  - `Models/RetainerWorkLogEntry.cs` — wire-mapped to `entryToWire`'s real shape (admin-retainer.ts), no fabricated fields.
  - `Services/IAdminRetainerService.cs` + `AdminRetainerService.cs` — real `POST /api/admin/retainer/:customerId/unscoped` client. Notes in its own doc comment that this route is gated by `requireAdmin` (platform-admin role), a narrower gate than the `ladder.msp-operator` capability the remediation-tracker/change-control routes use.
  - `MainWindow.xaml.cs` — new Home tab "Retainer Hours" group, "Log Ad-Hoc Hours" command opens a record workspace with `item`/`hours`/`pillar`/`finding`/`outcome` write-through Edits and a confirm-armed "Log Hours" action, same gallery-less shape `OpenScriptLibraryRecord` already established. Gated by the same honest `TryResolveLaunchControlScope` customerId check the Script Library gallery already uses — TenantService is fixture data (no real numeric customer id anywhere yet, tracked at #3502/#3505/#3540), so this opens a stated-blocked workspace today rather than guessing a customer id or faking data. Real POST result (success with the real logged entry id/hours/state, or the real HTTP failure) reports to the Console pane, same feedback channel `RunScriptLibraryAction` already uses.

  `dotnet build MyArchitect.csproj -c Debug` → Build succeeded, 0 Warning(s), 0 Error(s) (verified twice — once before, once after a concurrent-session rebase brought in #3463's Activity Layer commit; rebuilt clean both times).

  Re-audited the other two checklist items against real code, not assumption:
  - **PUT remediation-tracker step** — `RemediationTrackerService.cs` is a real, complete HTTP client, but genuinely unwired to any UI trigger in `MainWindow.xaml.cs`. Left as-is — wiring an actual "mark step complete" UI is real remediation-tracker Feature work (#3471), not this hour-logging issue's own scope; the byproduct logging happens server-side once that UI exists and calls the endpoint.
  - **POST human-action for catalog-backed changes** — already fully wired (Home tab Change Requests gallery → confirm-armed "Record human action").
  - **POST human-action after Console-executed catalog scripts** — confirmed this premise doesn't map to real backend behavior (Launch Control executions have no `changeRequestId`; see the IN FLIGHT log above for the full grep evidence). Filed **#3541** ("human-action after Console-executed catalog scripts has no real changeRequestId to attest against"), labeled `bug`, milestone v1.1, parented as a direct sub-issue of #3464 (this issue is itself the Feature — no separate Feature-tier parent to check per the standing rule), board status set to "AI Batter Up".

  `verify-branch-merged.mjs` → OK, `agent/3464-q2159` (1e80dc2c8) is merged into `origin/main`. `git status --porcelain` clean. Progress reported to buildId 2159 at all 3 checkpoints (Investigation → Implementation → Verification).
