# #3464 — Feature: Real Hour-Logging Integration (MyArchitect)

- **Status:** ⏳ IN FLIGHT 2026-09-10T19:58:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** none yet — in progress

## Log
- 2026-09-10T05:58:17Z 🛑 BLOCKED — (prior session) see below for full text; both blockers have since closed.
- 2026-09-10T19:58:00Z ⏳ IN FLIGHT (resumed, unblocked) — #3493 and #3459 are both CLOSED with `complete` as of earlier today. Removed the now-stale `blocked_by` edges to both (`DELETE .../dependencies/blocked_by/<internal id>` for each), flipped the label `blocked` → `in-flight`. `gh api .../3464/dependencies/blocked_by` now returns `[]`.

  Re-audited the issue's real checklist against actual code (not assumed from the issue body alone):
  - **PUT remediation-tracker step** — `Services/IRemediationTrackerService.cs` + `RemediationTrackerService.cs` already exist as a real HTTP client for `PUT /api/msp/customers/:customerId/remediation-tracker/steps/:stepId`. Confirmed via `grep` this service is genuinely unused by `MainWindow.xaml.cs` (no UI trigger calls it yet) — the client exists but nothing in the shell fires it.
  - **POST human-action for catalog-backed changes** — `Services/IChangeControlService.cs`/`ChangeControlService.cs` (`RecordHumanActionAsync`) IS wired end-to-end: `MainWindow.xaml.cs`'s Home tab "Change Requests" gallery opens a real change-request record workspace with a confirm-armed "Record human action" button that calls it for real, existing `msp_change_requests` rows.
  - **POST human-action after Console-executed catalog scripts** — re-checked against the real backend: `POST /msp/change-control/executions/human-action` (`msp-change-executions.ts`) requires a real `changeRequestId` resolved via `loadScopedChange`, i.e. an existing `msp_change_requests` row. The Console's catalog-script path (`RunScriptLibraryAction` → `POST /api/msp/:mspId/launch-control/execute`, `msp-launch-control.ts`) is a completely separate system (`write_action_catalog`/`baseline_action_templates`) that never creates or references a change request — `LaunchControlExecuteResult` has no `changeRequestId` field, and `msp-launch-control.ts` has zero references to `human-action` or `changeRequestId`. The issue's premise ("MyArchitect calls this after executing a catalog action through the Console") does not correspond to a real mapping in the backend — there is no changeRequestId to attest against for a Launch Control execution. Filing this as a finding rather than inventing a fake changeRequestId or silently skipping it.
  - **POST unscoped ad-hoc hours** — confirmed genuinely unbuilt: no `AdminRetainerService`/model/UI anywhere in `desktop/MyArchitect`. This is the one real, buildable remaining item.

  Proceeding to build the ad-hoc hours client (service + shell UI) for the confirmed-real gap, and filing the human-action/Console-script mismatch as its own finding.
