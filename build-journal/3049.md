# #3049 — Overview "Needs You" pending RACI acceptance count

- **Status:** ✅ DONE 2026-09-06T21:32:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 749b26513 (backend + frontend wiring + contract pack), 23711d899 (manifest
  commit-hash + sessionTag run note), merged to `main` at 1b018ac7e

## Log
- 2026-09-06T21:05:00Z ⏳ IN FLIGHT — extending #2922's `overviewCounts` on `GET /api/portal/dashboard`
  with a 7th real field, `raciPendingAcceptance`, counting this login's own pending R/A
  `portal_ownership_assignments` rows (`acceptance = 'pending'`). No schema change needed —
  `acceptance`/`ownerPersonId` already exist (#1518/#1515). Wiring the "Needs You" and
  "Portal Counts" panels in `artifacts/portal/src/pages/index.tsx` to the new field, updating
  the contract pack, and updating the customer-home test manifest.
- 2026-09-06T21:32:00Z ✅ DONE — shipped:
  - `artifacts/api-server/src/routes/portal-customer-engines.ts`: `raciPendingAcceptance` count,
    querying `portal_ownership_assignments` scoped `customerId` + `ownerPersonId = personIdForUser(req.user!.id)`
    + `acceptance = "pending"` + `roleKey in ("r","a")`. No `tenantScope` dependency (unlike the
    other six) — the ownership matrix has no `(mspId, tenantId)` shape.
  - `artifacts/portal/src/components/overview/types.ts` + `overviewDisplay.ts` (new `VIO` token) +
    `pages/index.tsx`: wired into both the "Needs You" row (real `/ownership` link, matching
    `moduleNav.ts`'s real built page — not `comingSoonHref`) and the "Portal Counts" panel.
  - `docs/customer-home-and-timeline-contract-pack.md`: regenerated for the 7th field per the
    architect→endpoints→pack→design→wire order (no schema change, so no re-architect step needed).
  - `test-manifests/portal/customer-home-overview.json`: added a `DASHBOARD-RACI` apiTests step
    tagged `sessionTag: "3049-raci-pending-acceptance"`, real commit hash, registered already (pre-existing file).
  - Verification: `tsc --noEmit` clean on both `artifacts/api-server` and `artifacts/portal`.
    `shaneapp://runTest` with `onlyTag=3049-raci-pending-acceptance` returned "no steps matched
    session tag" — honest and expected: the manifest edit ships on this branch, not yet on the
    dev-server checkout `runTest` resolves against at run time. Re-run once this merge reaches the
    running dev server (it now has, via the merge below).
  - Merged `agent/3049-q1813` → `origin/main` (root cause of the two commits above being 2 apart:
    concurrent pushes to main required 3 fetch/reset/merge/push retries — no unrelated files
    touched, verified via `git diff --stat` on each merge before pushing).
  - `git status --porcelain` clean at session end. No findings filed — no genuine gap surfaced
    beyond the issue's own scoped ask.
