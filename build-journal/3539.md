# #3539 — Gallery `Searchable` flag never wired to real search/filter in FixedRibbonRenderer

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** dc150eb4f

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — investigating GallerySpec.Searchable and FixedRibbonRenderer.BuildGalleryButton per UI_RULES.md §4; will add a real search/filter TextBox to gallery rendering when Searchable is true.
- 2026-09-12T00:00:00Z ✅ DONE — `FixedRibbonRenderer.BuildGalleryButton` now reads `gallery.Searchable`: when true, a real filter `TextBox` is pinned as the dropdown's first item (own `MenuItem` host, `StaysOpenOnClick`, `Focusable=false` on the host so the child `TextBox` itself gets focus), and typing narrows the shown rows by case-insensitive substring match against `Name`/`Sub`. Filtering (`RenderFilteredRows`) only removes/re-adds the row `MenuItem`s below the search box — it never rebuilds the `TextBox` itself, so focus/caret survive every keystroke. Up/Down/Left/Right are swallowed in the search box's `PreviewKeyDown` so the hosting Menu's own arrow-key navigation never steals a keystroke mid-filter. Non-searchable galleries (none currently exist besides the two `true` ones named in the issue) are unaffected — no TextBox item added, same flat list as before. Verified: `dotnet build MyArchitect.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. Not manually driven through the live WPF UI in this session (desktop/BuildConsole is out of scope for test manifests, and MyArchitect has no automated UI-test harness to invoke here) — a human click-through of the Home tab's Change Requests gallery and Console tab's Script Library gallery is the real remaining verification step. Branch `agent/3539-q2453` rebased onto `origin/main` and pushed directly to `main` (commit `dc150eb4f`); `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean at session end. No new findings filed — this is exactly and only the fix the issue asked for.
