# #2805 — Local Map: real Postgres schema read layer (tables + real foreign-key edges)

- **Status:** ✅ DONE 2026-09-04T04:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 683e42707 (real work), 84ca8dc99 (merged onto origin/main)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — audited `Services/BuildMap/ChainDoc.cs`/`ChainLayout.cs`/`BuildChainMapService.cs` full fields per the issue's real audit requirement; deciding reuse vs. new parallel model before writing the real Postgres schema read layer.
- 2026-09-04T04:45:00Z ✅ DONE — real audit concluded `ChainDoc`/`ChainLayout` are entirely GitHub-issue-tracker-specific (Epic→Feature→Issue hierarchy, board Status, BUILD-comment Model/Effort, fanin/gate Sentinel/Gates) with no honest analog for a flat table+FK graph, and `ChainLayout.Compute` is hard-coded to that exact shape rather than a generic node/edge layout — built a NEW parallel model instead of reusing `ChainDoc`.
  - Added `desktop/BuildConsole/Services/BuildMap/LocalSchemaDoc.cs` — `LocalSchemaTable` (schema, name, id, column count, real PK columns) + `LocalSchemaEdge` (constraint name, from/to table id, real from/to columns) + `LocalSchemaDoc` (database, generated-at, tables, edges).
  - Added `desktop/BuildConsole/Services/BuildMap/LocalSchemaMapService.cs` — real live-Postgres read layer via `information_schema.tables`/`.columns` (tables + column counts), `.table_constraints`+`key_column_usage` (real PKs), `.table_constraints`+`key_column_usage`+`constraint_column_usage` (real FKs). Reuses `BuildQueuePostgresClient.ParseConnectionString` + `BuildTrackerConfig.FindRepoRoot()` so it resolves the exact same `DATABASE_URL` (`.env.local`, local Postgres 18, `shanemccawmsp`) BuildConsole already uses for the build queue — the same database the app schema lives in.
  - Real verification: built a throwaway console harness (`ProjectReference` to `BuildConsole.csproj`, deleted after use — no scratch files left behind) that called `LocalSchemaMapService.BuildAsync()` against the real live local database. Real result: `Database: shanemccawmsp, Tables: 387, Edges: 416`; real `users` table found (38 columns, PK `id`); real `tenants` table found; 3 real FK edges into `public.users` printed with real constraint names (e.g. `account_setup_tokens_user_id_users_id_fk`); 0 composite FKs. Cross-checked against direct `psql` queries against the same `DATABASE_URL` — counts matched exactly (387 tables, 416 FK constraints, 0 composite FKs in `public`).
  - Real perf fix in the same commit: a live timing check found the FK query's `constraint_column_usage` join, combined with a `tc.table_schema <> ALL(@excluded)` predicate, defeats Postgres's planner on this real database (3.5s → 39s for the same 416 rows). No real FK is ever defined on a `pg_catalog`/`information_schema` table, so the predicate was dropped from that one query with no correctness loss — the existing `tablesById` membership check already filters out-of-scope edges for free.
  - Build verified clean (`dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors) both before and after merging in concurrent `origin/main` changes.
  - Merged `origin/main` twice (fast-moving branch, two intervening pushes) with no conflicts touching these files; pushed to `origin/main` at `84ca8dc99`; `scripts/dev-server/verify-branch-merged.mjs` confirms `agent/2805-q1588` is a real ancestor of `origin/main`.
  - `git status --porcelain` clean at completion.
  - #2806/#2807 (Local Map window, Git/Local map picker) can now build against this real `LocalSchemaDoc` contract.
