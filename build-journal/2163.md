# #2163 — Policy Engine: OU membership manual override + department-match fallback (redo of #1952)

- **Status:** ✅ DONE 2026-09-01T20:25:00Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** 0fba6a0c3 (merged to main)

## Log
- 2026-09-01T20:00:00Z ⏳ IN FLIGHT — Pulled #2163's full issue body. This is a redo of #1952,
  which died mid-session with no bookend/commit. Checked for orphaned progress per the issue's
  own instruction: found `rescued/1952-q1103-2026-09-01T18-03-39-021Z` (auto-rescued by the
  worktree sweep) and `origin/agent/1952-q1103`. The rescued branch contains real, complete work
  matching #2163's full scope: the `active_directory_ou_assignments` Drizzle schema + manual
  migration, `policy-compliance-graph.ts`'s resolution-order change (manual assignment checked
  first, department-match as fallback), and sibling admin routes (list/create/move/delete) in
  `admin-active-directory.ts`, plus the one caller-site update in `policy-compliance-evaluator.ts`
  for `observeOuMailboxSizes`'s new signature. The rescued branch's diff also carries ~20 unrelated
  files (stale base drift — BuildConsole/ShaneBuilder WPF changes, other build-journal deletions,
  an unrelated `security-plan-cross-tenant.ts` deletion) that are NOT part of #1952/#2163's scope —
  cherry-picking only the 5 relevant files into this worktree rather than merging the branch
  wholesale.
- 2026-09-01T20:25:00Z ✅ DONE — Cherry-picked the 5 relevant files (schema, manual migration,
  `policy-compliance-graph.ts` resolution-order change, `policy-compliance-evaluator.ts` caller
  update, `admin-active-directory.ts` assignment routes) into this worktree; the other ~20 files
  in the rescued branch's diff were unrelated stale-base drift and were left out. Ran the manual
  migration against local Postgres (`postgresql://localhost:5432/shanemccawmsp`) — the table
  already existed (the killed #1952 session had run it before dying), confirmed columns via
  `information_schema.columns`. Full `tsc --noEmit` blocked by the already-tracked worktree
  `node_modules/@workspace/db` junction defect (this worktree's junction dangles into a deleted,
  unrelated worktree `C:\wt\2116-q1106` — confirmed via `fsutil reparsepoint query`; same failure
  class as open issues #2121/#2097/#2088/#2094/#2087/#2089/#2152, not re-filed as a duplicate);
  confirmed via baseline comparison that every "`Cannot find module '@workspace/db'`" error also
  appears on files this change never touched, so the diff adds no new type errors on top of that
  pre-existing baseline. Committed as `0fba6a0c3` on `agent/2163-q1126`, pushed, then fast-forward
  merged directly onto `origin/main` (`main` had not moved since this worktree's base — no rebase
  needed). `verify-branch-merged.mjs` confirms ancestry; `git status --porcelain` clean.
  Filed a follow-up finding per the issue's own instruction — the manual-assignment routes are
  PlatformAdmin-only (`requireAdmin`), and whether this should be customer-self-service is a real
  product decision, not one this build should guess at: **#2164**, parented to #1096 (Application
  Core), board status set to "AI Batter Up".
