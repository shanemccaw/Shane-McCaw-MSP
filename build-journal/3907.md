# #3907 — BuildConsoleSettings Load-mutate-Save is last-writer-wins (general case)

- **Status:** ✅ DONE 2026-09-13T06:52:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 99d0c9867 (BuildConsoleSettings general Load/Save merge fix)

## Log
- 2026-09-13T06:24:33Z ⏳ IN FLIGHT — Read #3907 body/comments + #3902's already-established credential-guard pattern in `desktop/BuildConsole/Services/BuildConsoleSettings.cs`. Designing a general baseline-diff field merge in `Load()`/`SaveGuarded()` (not a per-call-site fix, per the issue's own steer given 69 call sites): `Load()` captures the exact on-disk JSON at read time as a private, non-serialized baseline snapshot; `Save()` re-reads fresh on-disk state under the existing cross-process `SaveGate` and, for every writable field OTHER than the two already-protected credentials, adopts the fresh disk value whenever this instance's current value still matches its own baseline (i.e. this instance never touched that field) — a field that DOES differ from baseline is this instance's deliberate change and always wins. This generalizes #3902's "mine beats theirs for what I actually touched" credential rule to every field, without touching any of the 69 call sites.
- 2026-09-13T06:52:00Z ✅ DONE — Implemented and pushed as `99d0c9867`. Real change entirely inside `BuildConsoleSettings.cs`: `Load()` now stamps the raw on-disk JSON onto the returned instance as `_baselineJson` (captured BEFORE the existing in-memory WebTools/ConfiguredRepos/UserAccounts backfills, so a backfill still reads as "changed" and persists on the next explicit Save(), unchanged from today); `SaveGuarded()` re-reads fresh on-disk state once (reused by both the existing #3902 credential guard and the new general merge), then for every writable field other than the two protected credentials adopts the fresh disk value whenever this instance's current value still equals its own Load()-time baseline (never touched) via reflection + JSON structural equality — a field that differs from baseline is this instance's deliberate change and always wins. After a successful write the baseline is recaptured from what was actually written, so a long-lived instance's next Save() diffs against current reality. Verified: `dotnet build BuildConsole.csproj -c Debug` succeeds (0 errors); a standalone `--instance`-scoped harness (own isolated %AppData% folder, deleted after, never touched the real settings.json) confirmed (1) the general race is fixed — two instances loading the same state, one saving field X, the other (holding a stale copy) saving different field Y, a fresh reload shows BOTH changes instead of A's stale write reverting X; (2) #3902's credential guard is unchanged — a stale blank GitHubPat still can't wipe a real on-disk PAT via plain `Save()`; (3) `SaveClearingCredential` still allows a deliberate blank. `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE. No fixture/hardcoded data involved — this is a pure mechanism fix in the settings persistence layer, no new call sites touched.
