# #3271 — Add Tesla + Vault rooms to house grid, 13-room layout (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T22:20:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** 3a01db926

## Log
- 2026-09-08T22:08:57Z ⏳ IN FLIGHT — Read issue #3271 + Design/design_handoff_shanes_life/README.md
  "Start here" + Rooms/Vault/Tesla sections. Scope is the house-grid tile only (ROOM_DEFS/ROOM_KEYS,
  furniture symbols, roomsForToday lit rules, odd-count wide-last-room CSS, default order) — the
  Vault/Tesla room PAGES themselves are separate, later Start-here steps, out of scope here. Real
  default order per README's "late" pass (which supersedes the "evening" 12-room order the issue
  body itself quotes): Money | Wins; Vault | Medicine; Tesla | Shopping; Recipes | Things; Lists |
  Dates; Pets | Inbox | People — 13 rooms, odd, so People spans both columns. Implementing:
  vault.hasOpenReveal()/documents.hasOpenReveal() (real vault_reveals/important_document_reveals
  reads), tesla.getPreconditioningStatus() (real /hooks/tesla ping, never a live Fleet API poll)
  and tesla.getTodaysBatteryShortfall() (real today's nudge_events row), wired into
  roomsForToday(); ROOM_DEFS/ROOM_KEYS reordered + tesla/vault added; r-vault/r-tesla furniture
  symbols added to critters-sprite.svg; odd-count `:last-child:nth-child(odd)` CSS rule in
  app.css. No schema/migration needed — every read is against tables that already exist.
- 2026-09-08T22:20:00Z ✅ DONE — Shipped on `main` at 3a01db926 (rebased from 3594bb470 to clear
  origin/main after concurrent commits landed; content unchanged). Real verification: node --check
  passed on every touched .js/.mjs file; a scratch script (written, run, then deleted -- never
  committed) imported the real vault.mjs/documents.mjs/tesla.mjs/api.mjs modules against the real
  local `finances` Postgres (migrations through 061 already applied) using the real
  shanemccaw@gmail.com user row: 20/20 checks passed, covering every lit-rule branch honestly —
  Vault dark/lit around a real inserted-then-expired vault_reveals row, Tesla's four branch
  priorities (not connected / preconditioning active via a real hooks row / trunk pending via a
  real tesla_scheduled_commands row / commute shortfall via a real nudge_events row / idle) — and
  confirmed zero residue left in Shane's real rows afterward. `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms 3a01db926 is an ancestor of origin/main.
  Known, deliberate interim gap (not a bug, not filed as a finding): tapping the new Vault/Tesla
  house-grid tiles currently falls through to the Today view (no viewVault()/viewTesla() route
  yet) — those room PAGES are separate, later Start-here steps in the same coordinated design
  pass (#3086's own Vault/Tesla sub-issues), not this issue's scope.
