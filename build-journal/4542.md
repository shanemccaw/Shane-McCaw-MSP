# #4542 — URGENT: max-concurrent-builds cap not enforced (ran 8 with cap 6, hard crash)

- **Status:** ✅ DONE 2026-09-17T17:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 6bd72cfe1 (count in-flight launches against the cap), d1943012d (two-threshold shared-count model: soft auto-max + hard cap)

## Log
- 2026-09-17T15:44:17Z ⏳ IN FLIGHT — Investigating real root cause of concurrency-cap overrun (8 ran with cap 6). Candidates per issue: BatterUpQueueService.cs, QueueWatcherService.cs.
- 2026-09-17T16:05:00Z — Root cause #1 (launch-window): `QueueWatcherService` owns it. Cap is NOT stale (UpdateMaxConcurrent live-applies, #2122/#3824) and tick-vs-tick can't overrun (`_ticking` single-flight). The real leak: a claimed build lives only in `_launching` — never `_running`/`_reservedSlots` — for its entire provisioning window (worktree add + prewarm + spawn, "can be long"), and no capacity gate counted `_launching`. Fixed in 6bd72cfe1: `OccupiedSlots = running + reserved + launching`, both gates derive from it, claimed rows reserved for the whole claim→registered window.
- 2026-09-17T16:30:00Z — Live test (soft-only) exposed a 3/2: the 3rd was a manual Build Watch **Continue** (`LaunchItemExplicit`, `isForced`) which — like Run Now (#820 `ForceLaunch`) — bypassed the cap entirely. Shane then supplied the real model (issue comments 16:11/16:14): TWO thresholds, ONE shared real count.
- 2026-09-17T16:55:00Z — Root cause #2 (the real bug): the auto-dispatcher and the manual paths checked two independent per-source counts. Implemented (d1943012d): a **soft auto-dispatch max** (auto/tick) and a **hard cap** (absolute ceiling for Run Now / Start Now / Continue), both checked against the same `OccupiedSlots` via one atomic `TryReserveSlot(id, limit)`. Manual callers reserve BEFORE force-claiming so a hard-cap refusal never leaves a stuck `running` row. New `HardCap` config + live-apply + Settings field; queue-panel slot count = hard cap.
- 2026-09-17T17:20:00Z ✅ DONE — Live-verified against the recompiled binary (watcher restart 12:36, my `soft N, hard M` log format confirms the new code is running): the auto-dispatcher held at the configured cap counting ALL builds (interactive "Continue" builds included) — at soft=2 held 2/2, at soft=3 held exactly 3, never overran; soft-max live-apply worked across 2→3→6. Clean build (0 warnings/0 errors), branch merged into origin/main (verify-branch-merged OK), working tree clean. HONEST GAP: Shane tested by varying the SOFT max only (hard cap stayed at its default 8 throughout), so the distinct soft<hard headroom + hard-cap **refusal** path was not separately exercised live — it is the same `TryReserveSlot` mechanism proven working for the soft gate, and is spot-checkable anytime by setting Hard Cap = soft+1 and Run-Now'ing one past it. Shane confirmed "it's working." No findings filed (no separate defects discovered).
