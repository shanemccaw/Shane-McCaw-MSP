# #3917 — Fix stale route-count claim in AD/OU-assignment MSP-console contract pack

- **Status:** ✅ DONE 2026-09-13T05:10:00Z
- **Scope:** platform
- **Started:** 2026-09-13
- **Commit(s):** 167134ec0

## Log
- 2026-09-13T05:05:41Z ⏳ IN FLIGHT — verified live route count in `msp-active-directory.ts`
  (7, via `grep -n "router\.\(get\|post\|patch\|delete\)("`), confirmed #3916's
  `?customerId=` addition to `GET .../ou-assignment-requests` already landed, and read the
  previously-undocumented `GET /msp/active-directory/ous?customerId=` route (`:96-130`).
  Updating `docs/msp-console/active-directory-ou-assignment-msp-console-contract-pack.md`
  to correct the "6 real routes" claim, add a real §1h for the `/ous?customerId=` route, and
  document §1f's `?customerId=` param.
- 2026-09-13T05:10:00Z ✅ DONE — doc-only fix. `docs/msp-console/active-directory-ou-assignment-msp-console-contract-pack.md`
  corrected: "6 real routes" -> "7 real routes"; added §1h documenting
  `GET /msp/active-directory/ous?customerId=` (previously undocumented, added for #2591);
  documented §1f's `?customerId=` param (added for #3916); fixed the other stale "6 routes"
  references throughout the file. Verified live: `msp-active-directory.ts` has 7
  `router.get/post/patch/delete(` calls; both new/updated routes read and confirmed against
  current code. Commit `167134ec0`, rebased and pushed to `origin/main`
  (`verify-branch-merged.mjs` confirms merged). `git status --porcelain` clean. No new
  findings beyond what's already documented in the pack's own "Finding" section (unrelated,
  pre-existing, already filed).
