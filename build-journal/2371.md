# #2371 — Panel: thumbnail grid with DIFF badges

- **Status:** ✅ DONE 2026-09-02T20:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 3fd491b82 (feature), c68685bcd + a83edcb5d (merges onto main)

## Log
- 2026-09-02T19:45:00Z ⏳ IN FLIGHT — Read #2371 body + parent #2367 + ShaneBuilder-FullFeature.md item 4. Existing Shot Vault rail panel (#2372) renders a vertical row list with thumbnails; this issue converts that to a wrapped thumbnail grid and adds a real, computed DIFF badge (perceptual hash of each shot vs. the chronologically previous shot — no fixture/hardcoded diff state).
- 2026-09-02T20:35:00Z ✅ DONE — Shipped `ShotVaultService.BuildTiles`/`ShotVaultTile` (real MD5 of a downsampled bitmap per shot, compared to the chronologically previous shot — no fixture/random DIFF state) and rewrote the rail panel's row list into a `WrapPanel` thumbnail grid, each tile carrying a real DIFF badge (`Brush.Alert.Danger.Border`, via the existing `GitCountPill` helper) only when its own pixels actually changed. Merged with two concurrently-landed sibling builds that touched the same files (#2370 run grouping, #2373 Expand/Send-to-tab) — DIFF is now computed once across the true chronological order and each tile renders inside its own run's grid; the "Send to tab" document view got the same real tile grid + DIFF badges rather than staying on the old row renderer. Verified with a clean `dotnet build` (0 errors) after each merge. Commit `3fd491b82`, merged onto `origin/main` at `a83edcb5d`; `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
