# #4300 — MyArchitect: center-loading surfaces (Credential Vault + others) need to be real closable Document tabs

- **Status:** ✅ DONE 2026-09-16T01:35:29Z
- **Scope:** platform (MyArchitect, `shanemccaw/MyArchitect`)
- **Started:** 2026-09-16
- **Commit(s):** `f2b918db28427cd08e122ddee353eb08b866407d` in `shanemccaw/MyArchitect` (pushed directly to `main`)

## Log
- 2026-09-16T01:18:33Z ⏳ IN FLIGHT — Blockers #4278/#4284/#4281 confirmed CLOSED. Cloned/worktreed `shanemccaw/MyArchitect` at `C:\wt\myarchitect-4300` (branch `work/4300` off `origin/main`). Next: read the real Document/tab shell code and Credential Vault's actual rendering path to confirm Shane's 2026-09-15 comment (fixed panel swap, no TabItem, no close button) before making any change.
- 2026-09-16T01:35:29Z ✅ DONE — Confirmed the bug directly in code: `MainWindow.xaml.cs`'s `ShowDocument(FrameworkElement)` is a fixed-panel visibility swap over a `DocumentOverlays` array (Home/Sow/Telemetry/Console/Vault/Report/ScreenshotEvidence/Kanban) — none of them ever entered the real `TabsItemsControl`/`_tabs` (`PortalTabItem`, WebView2-backed) tab strip that has the #4281 header/close/context-menu. Also confirmed the named "audit" targets are NOT the same bug: Script Library's record workspace and the other Admin-tab surfaces (Break-Glass #3480, Audit Log #3489, Consent Status #3485) already use the right-panel full record workspace (UI_RULES.md §3), not this fixed-panel mechanism.
  - Added `Models/LocalDocumentTabItem.cs` — a WebView2-free tab model, deliberately separate from `PortalTabItem`/`_tabs` so Vault content structurally cannot flow through the shared WebView2/portal infrastructure #3461 isolates it from.
  - Added a second, non-scrolling `ItemsControl` (`LocalDocTabsItemsControl`) in the same tab-strip row as the portal tabs, same visual style (`TabBorderStyle`), same #4281 Close/Close All but this one/Close All context menu (Shelve/Copy URL/Open in Edge omitted — no WebView2 session or URL exists on a local doc tab for those to act on).
  - Wired Credential Vault (confirmed bug), Console (named in the audit scope), and Report (rides along — same mechanism, same Console ribbon group as its sibling) through this new tab flow. `ShowDocument`/`ActivateTab`/`CloseLocalDocumentTab` keep the tab strip's active-highlight and open-tab count in sync between portal tabs and local doc tabs.
  - `dotnet build MyArchitect.csproj -c Debug` — Build succeeded, 0 Warning(s), 0 Error(s).
  - Filed finding **#4325** (parented under Epic #3454, per this issue's own standing rule, milestone v1.1, label `bug`, board status "AI Batter Up"): the other 5 `DocumentOverlays` entries (Home/Sow/Telemetry/Kanban/ScreenshotEvidence) share the identical non-tab architecture but were not named in #4300's scope, so left unconverted rather than assumed — Shane's call whether to extend the same fix to them.
  - Pushed directly to `shanemccaw/MyArchitect` `main` (`f2b918db2842`), verified `git merge-base --is-ancestor HEAD origin/main` — ancestor confirmed. `git status --porcelain` clean in both the MyArchitect worktree (removed after push) and this bookend's own repo.
