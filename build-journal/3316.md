# #3316 — Rebuild Pantry as its own dedicated 14th room (corrects #3308)

- **Status:** ✅ DONE 2026-09-09T04:57:43Z
- **Scope:** platform (Shane's personal "Shane's Life" app, `web/shanes-life/` — not the MSP platform under `artifacts/`; parent Feature #3220 is milestone 19 "Shane")
- **Started:** 2026-09-09
- **Commit(s):** `56f45325b` (main, after rebase onto `origin/main` past a real concurrent conflict with #3320's own critters.js/critters-sprite.svg additions — resolved by deduping the pantry critter symbol, both real ports were byte-identical)

## Log
- 2026-09-09T04:25:22Z ⏳ IN FLIGHT — Read issue #3316, `Design/design_handoff_shanes_life/README.md`'s Pantry section, and the First Slice Prototype's `showPantry`/`showZone` blocks + logic class (`PANTRY_TABLE`, `pantryZoneOf`, `pantryOnRun`, `pantryRestock`, capture-grammar regexes). Confirmed real local `pantry_items` table exists (migration 068) and is genuinely empty (no fixture data to fake). Starting real work: migration 070 (`low_at` column), `core/pantry.mjs` zone/restock logic, `room-order.mjs`, `routes/api.mjs` (room lit rule, checkoff restock, /api/pantry response), `capture-grammar.mjs` (out-of/no-more/used-the-last-of + house suffix), critter/furniture SVG port (`c-pantry`/`c-pantry2`/`r-pantry`, verbatim from the design's own `<symbol>` defs), and `public/app.js`/`app.css` for the real standalone Pantry room + Zone screen, removing the old Shopping-tab Pantry (Git #3308).
- 2026-09-09T04:57:43Z ✅ DONE — Shipped all 6 real scope items: (1) `viewPantry`/`viewPantryZone` as ROOM_DEFS' real 14th room (`pantry` inserted after `shopping`, before `recipes` — 14 rooms, even, no more wide ground-floor spanner), lime tint, real "something at 0 not on the run" lit rule in `roomsForToday`; (2) Home/Rental toggle, real cross-place "Do we have…" search, Running low section with real Add to run/Add all and a collapsible "already on the run" list, 8-tile zone grid (Lists shelf pattern); (3) Zone screen with real -/quantity/+ 44px controls, a direct write via new `POST /api/pantry/:id/adjust`; (4) migration 070 (`low_at`, additive, ran against local DB myself) + `zoneOf()` mapping the design's 8 zones onto category, PLUS a real bug found and fixed during direct-DB verification — mapping zone from `category` alone silently misclassified most real spice names (`categoryOf("paprika")` etc. → `"Other"`, not `"Pantry"`, since shopping-order.mjs's own keyword list never covered them) into a generic Dry-goods catch-all; re-implemented `zoneOf()` by porting the design's own complete `PZ_WORDS` keyword table verbatim instead, with category only as a final fallback — verified correct afterward; (5) capture-grammar's `pantry_have`/`pantry_bought` now accept a real place suffix, and `pantry_used_last` became `pantry_out` (`"out of X"`/`"no more X"`/`"used the last of X"`), which zeroes AND joins the run instead of falling to the inbox, including for a genuinely unknown-but-grocery-shaped name; (6) checking a real shopping-run item off restocks the matching Home pantry row via `restockHomeFromRun`, unchecking reverses it — wired into the existing `PATCH /api/lists/:id/items/:itemId` route, gated to the real `category === 'shopping'` singleton list only.

  **Verified live** against the real local `finances` DB (not simulated): a one-off script (deleted after use, per the working-tree-clean rule) directly exercised `setPantryQuantity`/`adjustPantryQuantity` (zone + `low_at` override), `restockHomeFromRun` (both directions, both real run-item text shapes), `adjustPantryItemById` (count −1, level +1→Full), the real `pantry_out` capture rule end-to-end via `runCaptureGrammar` (both a known and an unknown item), and `roomsForToday`'s own pantry lit rule — 11/11 real assertions passed. The existing `capture-grammar.test.mjs` pure-unit suite: 58/58 passing (56 mine + 2 from a concurrent #3320 addition merged in via rebase). Migration 070 applied and confirmed idempotent on re-run.

  **Real conflict during push:** `origin/main` had moved (#3320, a concurrent command-tray build) and had independently ported the *exact same* `c-pantry`/`c-pantry2` critter symbols (byte-identical, both extracted verbatim from the same design source) at a different insertion point, plus an identical `pantry` SLOTS entry in `critters.js`. Rebased, deduplicated the symbol defs (kept one real copy, kept my own `r-pantry` furniture which #3320 didn't need), merged the two comments into one that names both real consumers, and re-verified (syntax + the full DB-backed check) after resolving. Confirmed genuinely merged: `node scripts/dev-server/verify-branch-merged.mjs` → OK.

  **Findings filed** (both real, both explicitly out of #3316's own stated scope, both sub-issues of the real Feature parent #3220): [#3326](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3326) — the Pantry room's own locked footer copy promises a "do we have cumin" capture-grammar query that doesn't exist, and no real capture path ever creates a `unit = 'lvl'` row, so the whole Out/Almost out/Half/Full display path (correctly implemented in this build) is currently unreachable; [#3327](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/3327) — `bin/check.mjs` has zero live-HTTP check coverage for the entire pantry feature surface, pre-existing since #3308.

  Working tree clean; `git status --porcelain` empty. No `Shane To-Do` — the one manual-DDL migration (070) was additive and run by me against local, per the Database section's own rule.
