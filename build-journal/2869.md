# #2869 — Remediation checklist items have no decline-to-risk flow (checkKey-addressed)

- **Status:** ✅ DONE 2026-09-04T20:15:49Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** dd382135e (both the decline-to-risk feature and #2827's guard — see build-journal/2827.md)

## Log
- 2026-09-04T20:06:02Z ⏳ IN FLIGHT — add `POST /portal/remediation/checklist/:checkKey/decline-to-risk`
  and its MSP mirror, mirroring `declineRemediationStepToRisk` (#1542), writing against `checkKey`
  instead of an s-id. Note: #2827 (the guard this issue is a follow-up to) was found orphaned —
  its bookend says IN FLIGHT but the guard was never actually merged to `main` (only survives on
  `rescued/2827-q1605-...`, an uncommitted-work-rescue branch from a swept worktree). Bringing
  #2827's guard forward as part of this build since #2869 cannot be finished without it existing.
- 2026-09-04T20:15:49Z ✅ DONE — shipped in `dd382135e`: `declineRemediationChecklistItemToRisk()`
  added to `remediation-tracker-risk-decline.ts` (resolves evidence directly from the already-open
  finding via `resolveRemediationChecklistItem`, no `REMEDIATION_TRACKER_STEP_CHECK_KEYS` mapping
  needed since a checklist item's identity IS the finding). New routes:
  `POST /portal/remediation/checklist/:checkKey/decline-to-risk` (`requireRole("CustomerUser")`,
  matching the s1–s30 route's own signed-liability floor) and its MSP mirror
  `POST /msp/customers/:customerId/remediation/checklist/:checkKey/decline-to-risk`
  (`requireRole("MSPOperator")` — this checklist module's own established "MSP acts on the
  customer's behalf" precedent, unlike msp-remediation-tracker.ts's stricter no-mirror rule for
  the s1–s30 world; documented inline in both files). Also completed #2827's orphaned guard on
  both PUT routes as a prerequisite (see build-journal/2827.md). No schema change (`step_id` is
  plain text, no CHECK constraint). Verified: `tsc --noEmit` clean, `remediation-tracker-risk-decline.test.ts`
  (17 tests incl. 3 new for the checklist path) + `remediation-checklist.test.ts` +
  `portal-remediation-tracker*.test.ts`/`msp-remediation-tracker.test.ts` (58 tests) all pass.
  `git status --porcelain` clean; branch verified merged into `origin/main` via
  `verify-branch-merged.mjs`. No route-level test files existed for the checklist routes
  themselves (only lib-level), so none were extended at that layer.
