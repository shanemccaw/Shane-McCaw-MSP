# #1779 — Dispatch #___ direct-queue button

- **Status:** ✅ DONE
- **Scope:** buildconsole

## Log
- Read #1779's full body (no comments existed on the issue). Read #1709's landed
  `BatterUpQueueService` (`FindBuildCommentAsync`/`ParseBuildComment`) and `GitHubApiClient`
  (`GetIssueAsync`, `GetIssueCommentsAsync`, `GetBlockedByAsync`) before writing anything.
- Added `desktop/BuildConsole/Controls/DispatchPanel.xaml(.cs)` — a "Dispatch #___" text
  input + button placed above `AiBatterUpPanel`/`BatterUpPanel` in the Build Queue column
  (`MainWindow.xaml`). On submit it:
  1. Fetches the given issue directly via `GitHubApiClient.GetIssueAsync` — NOT filtered by
     board status, so it bypasses the Batter Up gate by design.
  2. Reuses `BatterUpQueueService.FindBuildCommentAsync` (the exact #1709 parser, not a
     second one) to read its `BUILD:` comment. No comment found → clear "No build prompt
     found on #NNNN yet." message, nothing queued.
  3. Checks for an existing dedup row via `BuildQueuePostgresClient.FindDedupCandidateAsync`
     (same convention `BatterUpQueueService` already follows) so a re-dispatch of an
     already-tracked issue reports "already tracked" instead of double-queuing.
  4. Queues through the exact same `BuildQueuePostgresClient.QueueBuildAsync` pipeline
     #1709/#1710/BuildQueuePanel all already use, passing through real GitHub
     `blocked_by` numbers — the existing #1600 fail-closed watcher governs whether it
     actually launches, so a real open blocker is queued-but-held, same as every other
     launch path, and the panel's status message says so explicitly.
  5. Fires a `Dispatched` event so `MainWindow` refreshes the sibling `BuildQueuePanel`,
     mirroring `BatterUpPanel.RowsAutoQueued`.
- Did not touch `BatterUpPanel`, `AiBatterUpPanel`, or `BuildQueuePanel`'s own
  Queue/Send to Builder/Cancel actions — purely additive.
- `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean, 0 warnings,
  0 errors (verified both before and after rebasing onto `origin/main`).
- No test manifest — `desktop/BuildConsole` is explicitly out of scope for those.
- Rebased onto `origin/main` (`e3ecbd37e`) and pushed directly to `main`:
  commit **`4bbdca401`** (squashes the IN FLIGHT bookend + the feature commit through the
  rebase). `git merge-base --is-ancestor HEAD origin/main` confirmed merged after push.
- `git status --porcelain` clean before writing this DONE bookend.
