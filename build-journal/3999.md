# Build journal — #3999

- **Status:** ✅ DONE 2026-09-14T04:35:00Z
- **Scope:** platform

## Log

- 2026-09-14T03:30:00Z — Started. Diagnostics and Scripts: wire the real Design
  (`Design/portal/design_handoff_full_site/screens/Diagnostics and Scripts.dc.html`)
  into `artifacts/portal/`. Contract pack (#2451,
  `docs/portal/diagnostics-and-scripts-contract-pack.md`) is current and verified
  against today's code — line numbers shifted slightly since 2026-09-03 but every
  route/shape it documents still matches. #2506 (script-package → monitor_check
  link) landed and is closed.
  Checked #2768's staleness caveat: `config_resources` has grown to 1538 rows
  (real PowerShell/config-resources expansion has landed at the engine level),
  but `monitor_checks.requires_customer_script` is still 0 across all 204 checks,
  and `script_packages`/`script_modules` are still 0 rows — the specific
  "100+ new script-backed checks" scale concern #2768 raised for THIS module has
  not actually materialized yet, so today's pack's list/layout assumptions still
  hold. Building against the current real pack.
  Design is a single screen (no separate Scripts list page — old `scripts.tsx`
  route folds into this one; contract pack §0 confirms `/portal/scripts` never
  existed as a list endpoint). Plan: new `pages/customer-diagnostics.tsx` at
  route `/diagnostics`, new sidebar nav entry, wire `/api/portal/diagnostics/latest`,
  `/api/portal/health-benchmark`, reuse the shell's existing `useScanState()` for
  the scanning/never-scanned strip (same real `/api/portal/scan-status` data the
  Tenant Status card already polls), and wire
  `/api/portal/scripts/:checkKey/download` on any live `requires_script` finding.

- 2026-09-14T04:35:00Z — ✅ DONE — commit `d25a7de18` (merged to `main` at
  `54d1c0667`). Shipped: `artifacts/portal/src/pages/customer-diagnostics.tsx`
  (route `/diagnostics`), `artifacts/portal/src/components/diagnostics/{types,
  useDiagnosticsPage,diagnosticsDisplay}.ts`, a new `Diagnostics` sidebar row
  (`moduleNav.ts`), and `test-manifests/portal/diagnostics-and-scripts.json`
  (registered in `_regression-suite.json`). Wires `GET /api/portal/
  diagnostics/latest`, `GET /api/portal/health-benchmark`, and `GET
  /api/portal/scripts/:checkKey/download` (reachable in code, currently
  structurally empty in live data — 0 `requires_customer_script` checks exist
  anywhere, confirmed via direct `DATABASE_URL` query this session). Reused
  the shell's existing `useScanState()` for the scanning/never-scanned strip
  instead of a second poll of the same table.
  No new findings filed — #2506 (the one real gap the #2451 contract pack
  identified) is already closed/landed.
  Verification: `tsc -p tsconfig.json --noEmit` clean and `vite build` clean
  on `artifacts/portal`, both re-run after rebasing onto `origin/main`.
  `shaneapp://runTest` was tried and genuinely could not run yet — confirmed
  by direct inspection that neither the new page nor the new manifest exist
  in the shared dev-server checkout at build time, since this build's
  worktree only merges into that checkout after completion (see the
  manifest's own notes; same Git #2160 "genuinely unreachable" class as
  `scan-status-progress.json`'s existing #2546 note). Re-run the manifest via
  `shaneapp://runTest?file=diagnostics-and-scripts.json` once this lands
  there.
  `git status --porcelain` clean; branch verified an ancestor of
  `origin/main` via `verify-branch-merged.mjs`.
