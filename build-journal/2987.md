# #2987 — Git panel Commit should default to staging everything (VS Code convention)

- **Status:** ✅ DONE 2026-09-06T15:32:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-06
- **Commit(s):** 4f1940132

## Log
- 2026-09-06T15:22:37Z ⏳ IN FLIGHT — investigated `LeftSidebar.xaml.cs`'s `DoGitCommitAsync`/`BtnGitCommit_Click`/`BtnGitStageSelected_Click`; confirmed blocked_by #2988 was stale (that fix already landed, commit e24de931c) and cleared the edge. Implementing stage-everything-when-nothing-explicitly-selected fallback before commit.
- 2026-09-06T15:32:00Z ✅ DONE — added `_lastStagedItems` field (populated from `RefreshGitStatusAsync`'s already-parsed staged list) and taught `DoGitCommitAsync` to check it before committing: if nothing is staged and nothing is explicitly checked in Changes, run `git add -A` first (matching VS Code's default-stage-all convention); if specific rows ARE checked, stage exactly those instead. `BtnGitCommitAndPush_Click`'s own duplicate per-file staging loop was removed since the decision now lives entirely in `DoGitCommitAsync`, which it already calls. Verified: `dotnet build BuildConsole.csproj -c Debug` → 0 warnings/errors; the underlying `git add -A` + commit sequence hand-verified against a scratch repo with mixed untracked (a design-pack-style folder of 3 files) + modified-tracked content — commits everything shown in one commit, matching VS Code, tree ends clean. Pushed as commit 4f1940132; `verify-branch-merged.mjs` confirms ancestor-of-main. `git status --porcelain` clean.
