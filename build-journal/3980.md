# #3980 — Keep committed report.json a live mirror after End & Sync

- **Status:** ✅ DONE 2026-09-14T02:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 40eef9c25 (IN FLIGHT bookend), 309872cae (real work), merged to origin/main as 28fb97350

## Log
- 2026-09-14T01:10:30Z ⏳ IN FLIGHT — pulled #3980 + #3979, confirmed #3979 (SaveEntryAsync fix) merged into origin/main, unblocking this build. Investigating VisualTestTrackerSessionSyncService/VisualTestTrackerStore/QueueWatcherService's tick pattern before designing the re-export mechanism.
- 2026-09-14T02:35:00Z ✅ DONE — Added VisualTestTrackerReExportService: a real poll (DispatcherTimer, 60s + immediate on-launch tick, wired in MainWindow.xaml.cs) that reads live Postgres lifecycle state (status/git_issue_number/resolution/resolution_reason) for every bug uuid referenced by a committed report.json, and on drift surgically patches those four fields into the JSON tree plus the "Bugs & Issues Logged" section of summary.md, then re-commits+pushes via a new StageCommitPushAsync helper extracted from ExecuteGitSyncAsync (reused, not duplicated). Also: report.json now serializes gitIssueNumber/resolution/resolutionReason for the first time (previously never exported at all — the real gap this issue was filed over); VisualTestTrackerEntry gained Resolution/ResolutionReason properties; VisualTestTrackerStore's two list queries + ReadEntryFromReader now read those two DB columns (added by #3978's already-applied migration), and a new GetLifecycleSnapshotAsync does a direct (non-local-JSON-overlay) bulk read for the reconcile diff. Build: `dotnet build -c Debug` on desktop/BuildConsole — 0 warnings, 0 errors. Real end-to-end verification (not simulated): inserted a real bug row into local Postgres (entry_uuid=verify3980-test-uuid-0001), hand-authored a matching report.json/summary.md in a throwaway git repo (C:\Temp\verify3980\repo, git-init'd, committed as baseline), directly UPDATEd the row's status/git_issue_number/resolution in Postgres via psql (simulating a #3985-style dispatched-build direct write with BuildConsole itself never running), then ran ReconcileAsync directly via a temporary console harness project (ProjectReference to BuildConsole.csproj) — confirmed report.json's bugs[0] picked up status=Closed/gitIssueNumber=9999/resolution=Fixed, summary.md's table Status cell flipped Open→Closed, and a real new commit landed (`a413485`, message "Re-export: lifecycle updated for 1 bug(s) (verify3980-test-uuid-0001).", diff exactly 2 lines changed in each file — nothing else touched). Re-ran the same reconcile pass a second time with no DB change: SessionsUpdated=0, no new commit — confirmed idempotent. Cleaned up: deleted the test Postgres rows, deleted the throwaway harness/repo under C:\Temp\verify3980. **Open question answered explicitly (per the issue body):** BuildConsole does NOT need a separate standalone startup-reconcile mechanism beyond what's implemented — the timer fires an immediate pass on launch (not just after the first 60s interval), which already IS the startup catch-up pass for "Postgres changed while this instance was closed." The one accepted, unchanged limitation: if Postgres changes while BuildConsole is fully closed and no BuildConsole session is ever opened again before chat next reads the file, nothing commits the update — this poll only runs while BuildConsole itself is running, and no other process in this architecture can push a git commit. That gap is structural, not something this build could reasonably close, and matches the issue's own framing of the tradeoff. No new findings filed — all gaps found (report.json never serializing gitIssueNumber/resolution/resolutionReason; the C# model missing Resolution/ResolutionReason despite #3978's migration) were fixed in this same build, not worked around.
