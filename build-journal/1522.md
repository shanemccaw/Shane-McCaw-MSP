# #1522 — Ownership / RACI: Append-only cell event log

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** e3e613b5d (schema + migration), f38e00153 (route wiring + tests)

## Log
- 2026-08-31 ⏳ IN FLIGHT — added `portal_ownership_events` (append-only: assigned |
  accepted | declined | cleared | reassigned, actor + reason + created_at, never
  updated/deleted), Drizzle schema in `lib/db/src/schema/msp.ts`, migration
  `lib/db/migrations/manual/2026-08-31-ownership-raci-append-only-event-log-1522.sql`
  run against local `DATABASE_URL`. `portal_ownership_assignments` stays as the
  materialised current-state view per the issue's stated option ("or it becomes a
  materialised current-state view over the log") — rewriting every route to derive
  state from a full replay is out of scope with no product need for it yet. Next:
  wire the event log into the assign/accept write routes transactionally, add a
  read endpoint for cell history, verify against real data.
- 2026-08-31 ✅ DONE — wired the event log into `POST /portal/ownership/assign`
  (transactional: `assigned` the first time a holder appears in a cell,
  `reassigned` on every re-assert, `cleared` for an explicit gap) and
  `POST /portal/ownership/accept` (one `accepted` event per holder actually
  updated; also gave `accept` an optional `ownerPersonId` body filter, since
  #1515 made a cell hold many rows and this route was still updating every
  holder in the cell on one call — a real pre-existing gap, fixed in the same
  pass rather than filed separately). Added `GET /portal/ownership/events`
  (customerId-scoped, `objectId`+`roleKey` required, `ownerPersonId` optional)
  to replay one cell's full history. Pure helpers (`assignEventType`,
  `toWireEvent`) added to `artifacts/api-server/src/lib/portal-ownership.ts`
  with 8 new unit tests; full suite 38/38 passing
  (`artifacts/api-server/src/lib/portal-ownership.test.ts`, already in
  `vitest.config.ts`'s include list). Verified the transaction sequence and the
  `event_type` check constraint directly against local Postgres (real INSERT/
  UPDATE sequence matching the route logic, rolled back, no residue).
  `pnpm --filter @workspace/api-server run typecheck` inside this worktree
  falsely reports the new export missing — filed as its own finding, **#2094**
  (parented to #1202), because `node_modules/@workspace/db` in this worktree
  junctions to the *main checkout's* `lib/db`, not this worktree's, for
  dist-less packages; confirmed clean with a throwaway `paths`-override
  tsconfig pointed at this worktree's own `lib/db/src`, deleted after use (not
  committed). No portal page wiring (SCOPE STOP — no `Design/portal/` export
  for this module yet; that's #1727). Working tree clean, branch verified
  merged into `origin/main` via `verify-branch-merged.mjs`.
  Migration appended to #1630. Findings filed: #2094.
