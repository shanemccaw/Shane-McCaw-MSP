# #2992 — Notification Preferences: wire the portal UI to the real endpoints (v2)

- **Status:** ✅ DONE 2026-09-06T19:05:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 16700c9ae11881efb5023ee1e2d7cb47ffd257c6 (merged directly to `main`)

## Log
- 2026-09-06T18:30:17Z ⏳ IN FLIGHT — Design export `Design/portal/design_handoff_full_site/screens/Notification Preferences.dc.html`
  landed; wiring `artifacts/portal` (the live #1485 portal) to the real, already-built
  `GET`/`PATCH /api/portal/notification-preferences` endpoints per the contract pack
  (`docs/notification-preferences-contract-pack.md`) and the archived page's proven
  request/response shape. No backend changes needed — endpoints already CURRENT.
- 2026-09-06T19:05:00Z ✅ DONE — Shipped `artifacts/portal/src/pages/notification-preferences.tsx`
  (route `/notification-preferences`, exporting a reusable `NotificationPreferencesContent`
  for a future Settings-container build), plus shadcn `Switch`/`Tooltip` primitives
  (radix deps already present). Wired to real `GET`/`PATCH /api/portal/notification-preferences`
  — no fixture data, no backend changes needed. Wrote and registered
  `test-manifests/portal/notification-preferences.json` (apiTests: real login + GET/PATCH
  round-trip on the `security` category with restore; uiSteps: real login through #2991's
  new login page + render/toggle/discard). Typecheck (`tsc --noEmit`) and `vite build` both
  clean. **`shaneapp://runTest` itself could not be run this session** — the shared
  dev-server checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) is 93 commits
  behind `origin/main`, and `request-restart.mjs`'s merge-back fails with a spurious
  add/add conflict on an unrelated file (`build-journal/2830.md`, already resolved on
  `main`) before the restart/test-run step is ever reached — confirmed via 2 real,
  bounded retries with identical failure (Git #2160 discipline), filed as **#3054**
  (parented #1202). This blocks every build in the `NotificationPreferences` build set,
  not just this one. Filed **#3052** (parented #1202) separately: `HttpTestExecutor.cs`'s
  jsonPath resolver has no `[?(@...)]` filter support and never reads an `"equals"` key,
  both already silently broken in several existing manifests — worked around in my own
  manifest by asserting on the route's real fixed array index instead. Merged directly to
  `main` (`git merge-base --is-ancestor` confirms ancestor); `git status --porcelain`
  clean.
