# #1824 — Portal Shell: scan status and progress

- **Status:** ✅ DONE 2026-09-03T15:34:46Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 6888d1f86 (implementation), 8908335f7 (merge into main)

## Log
- 2026-09-03T15:19:39Z ⏳ IN FLIGHT — investigation done (read #1824 body/comments, #2520 SSE contract pack,
  Design/portal/design_handoff_ui_shell/README.md + Shell.dc.html logic class for the scan-status
  Tenant Status card, confirmed #1819 nav shell already merged with a `SidebarNav footerSlot` reserved
  for this card, confirmed real current paths for `/portal/scan-status` and `/portal/scan-plan`
  (untouched by #1753's rename), confirmed zero live callers so far). Building the Tenant Status card +
  scan log panel next: real poll (scan-status/scan-plan) + real run-scoped SSE stream
  (`/api/msp/customers/:customerId/diagnostics/runs/:runId/sse`), combined per the SSE contract pack's
  §4 "read together, not as alternatives" rule and the README's 8-phase state machine
  (none/running/late-join/complete/partial/failed/disconnected/cache-cleared).
- 2026-09-03T15:34:46Z ✅ DONE — Built:
  - `artifacts/portal/src/components/shell/scanTypes.ts` — real wire types for scan-status,
    scan-plan and the three SSE event shapes (hand-mirrored, no cross-artifacts type sharing
    per CLAUDE.md).
  - `artifacts/portal/src/components/shell/useScanState.ts` — the combined real source: adaptive
    poll of `/api/portal/scan-status` (3s active / 45s idle), `/api/portal/scan-plan` fetched once
    per run, and a live `EventSource` against
    `/api/msp/customers/:customerId/diagnostics/runs/:runId/sse?jwt=...` only while a run is
    actually active (never for an already-finished run — its replay cache is already gone).
    Derives the design's 8-phase state machine, honestly distinguishing a terminal event this
    browser tab actually witnessed live (real findings count + per-check log) from a cold page
    load discovering a finished run after the fact (`cache-cleared`: real persisted counts only,
    no invented findings tally, no invented log — per the HARD RULE).
  - `TenantStatusCard.tsx` (mounts into `SidebarNav`'s `footerSlot`, reserved by #1819) +
    `ScanLogPanel.tsx` (the right-slide scan log, content derived at render time per the README's
    own "never snapshotted" rule) — copy taken verbatim from `Shell.dc.html`'s own logic class
    where the demo's data model matches reality; adjusted only where the demo's canned data
    claimed a number the real backend genuinely doesn't have (cache-cleared's findings tally).
  - CTA ("Run first/new scan") wired to the real
    `POST /portal/diagnostics/debug-trigger-scan`, gated client-side on `isTestbed` — matches that
    route's own real server-side testbed-only guard ("Real customers never get a self-serve scan
    trigger").
  - Backend addition: `artifacts/api-server/src/routes/portal-assessment.ts`'s
    `/portal/scan-status` now returns `lastRunSummary.errorMessage` when `status === "failed"` —
    real gap: a page opened cold after a run died had no way to show why (only a live SSE watcher
    of that exact run ever saw the message). Additive field off the already-existing
    `error_message` column (confirmed live on local Postgres — no migration needed).
  - `docs/scan-progress-sse-contract-pack.md` (#2520)'s own finding that #1817's endpoint table
    was missing the SSE route is already resolved — the table now lists it. No new finding to file.
  - Test manifest: `test-manifests/portal/scan-status-progress.json` (apiTests for scan-status/
    scan-plan + uiSteps for the card/panel), registered in `_regression-suite.json`.
  - **Verification, honestly:** `tsc --noEmit` clean for every new/changed file in both
    `artifacts/portal` and `artifacts/api-server` (confirmed via `git stash`/reapply that the
    remaining errors — #2546's `class-variance-authority` gap in portal, `lib/db/dist` staleness
    in api-server — are pre-existing baseline, identical error count before and after this
    change, none touching these files). Confirmed the new `error_message` column live against the
    real local Postgres schema (`psql \d msp_diagnostic_runs`). Could **not** execute the test
    manifest via `shaneapp://runTest` this session: its local courier resolves manifests from the
    shared main checkout, which doesn't have this file until merge, and no local dev server was
    running to hit the endpoints directly (`curl`/`netstat` confirmed nothing listening on
    3000/8080). This is the Git #2160 "genuinely unreachable, after a real check" case, documented
    honestly in the manifest's own notes and here rather than a silent skip. `artifacts/portal`'s
    own `vite build` is separately blocked by the pre-existing, already-filed #2546 (reproduced
    live, unrelated to this change) — 1737/1737 modules from this change transformed cleanly
    before that unrelated dependency failure.
  - Merged: `agent/1824-q1387` → `origin/main` (merge commit `8908335f7`), confirmed via
    `verify-branch-merged.mjs`. `git status --porcelain` clean.
