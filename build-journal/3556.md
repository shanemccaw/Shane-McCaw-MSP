# #3556 — New Chat prefill (EpicChatUrlBuilder) still embeds a raw GitHub PAT

- **Status:** ✅ DONE 2026-09-10T23:03:15Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** ec5386d51

## Log
- 2026-09-10T22:59:37Z ⏳ IN FLIGHT — read issue #3556 body + comments, read `Services/EpicChatUrlBuilder.cs` and confirmed the 6 real call sites (`Controls/LeftSidebar.xaml.cs:4664,5280,6390`, `MainWindow.GitDetailTabs.cs:281`, `MainWindow.xaml.cs:5672,6751,8019`). About to remove the raw-PAT prefill line, add the `shanes-git` MCP instruction line, drop the `pat` parameter from `BuildEpicChatUrl`, and update all 6 call sites.
- 2026-09-10T23:03:15Z ✅ DONE — Removed the `{pat}\r\n{label}` raw-PAT prefill in both `BuildEpicChatUrl` branches (plain and handoff-from-URL), added the fixed `McpInstructionLine` constant ("Use the shanes-git MCP connector for any GitHub operation ... do not paste or request a raw PAT. See desktop/BuildConsole/BuildConsole-Chat-Tools-Reference.md ..."), dropped the now-unused `pat` parameter from the signature, and updated all 6 real call sites. Where a call site's local `pat` variable was still consumed by its pre-existing `ActivityLog` "PAT present/absent" line (out of scope per the issue body, item 5), the variable and log line were left untouched — only the argument to `BuildEpicChatUrl` was dropped (LeftSidebar.xaml.cs:6386, MainWindow.xaml.cs:5671,6749). Where `pat` became fully unused after dropping the argument, the `var pat = ...` line was removed (LeftSidebar.xaml.cs:4662,5277, MainWindow.GitDetailTabs.cs:280, MainWindow.xaml.cs:8003). BuildConsole's own internal `GitHubApiClient`/Settings PAT usage (Board rendering, closing issues, dispatch) is untouched, per the issue's explicit out-of-scope item 4. Verified: `grep -rn "pat" desktop/BuildConsole/Services/EpicChatUrlBuilder.cs` returns zero matches; `dotnet build BuildConsole.csproj -c Debug` succeeded with 0 errors both before and after the final rebase onto `origin/main`. Branch confirmed merged via `node scripts/dev-server/verify-branch-merged.mjs`. Working tree clean (`git status --porcelain` empty). Commit ec5386d51 pushed directly to `origin/main` (this worktree's `agent/3556-q2241` branch was already an up-to-date mirror of main plus these two commits — rebased and pushed straight through, no PR).
