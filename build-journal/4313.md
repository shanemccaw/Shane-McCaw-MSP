# #4313 — Transitive FK descendant closure tool for tenant-reset audits

- **Status:** ✅ DONE 2026-09-15T00:25:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** a639fbeb7 (bookend IN FLIGHT), (fill final hash after this commit)

## Log
- 2026-09-15T00:10:00Z ⏳ IN FLIGHT — writing scripts/db/find-tenant-scoped-tables.mjs: transitive FK descendant closure of tenants/users/msps (including FK-to-users) plus CASCADE-child tables, via live information_schema.
- 2026-09-15T00:25:00Z ✅ DONE — `scripts/db/find-tenant-scoped-tables.mjs` written and live-verified against the real local dev DB (`DATABASE_URL`). BFS walk of every real FK edge in `information_schema` from roots `tenants`/`users`/`msps`, reporting each reached table's shortest path and whether the reaching edge is `ON DELETE CASCADE` vs a blocking rule (`NO ACTION`/`RESTRICT`/`SET NULL`). Confirmed it surfaces the exact real gap #4272 found by hand: `client_services` and `signup_exchange_tokens` (both `NO ACTION` -> users.id, blocking), plus the CASCADE-child tables `checkout_email_verifications`, `customer_user_roles`, `engine_score_signal_deltas`, `msp_user_roles`, and `sales_offer_events` (present, though live schema shows it as `SET NULL` not `CASCADE` -- reported honestly per the real constraint, not per the issue's prose). `--json` output mode verified to parse. No schema/data change; tooling only. No genuine new finding/bug beyond what #4271/#4272/#4313 already document, so no sibling issue filed under #4270.
