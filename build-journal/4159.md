# #4159 — End & Sync dialog: Product Name/Area dropdown text invisible, auto-detected value never seen

- **Status:** ✅ DONE 2026-09-15T01:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** a97b46691ac49a542418b9313ce639dbbe90e74b

## Log
- 2026-09-15T00:40:00Z ⏳ IN FLIGHT — pulled issue body + comment, starting investigation of SessionSyncDialog.xaml CmbProductName styling and DetectArea/DevServiceRouting mapping
- 2026-09-15T01:05:00Z ✅ DONE — Root cause confirmed by reading `DarkTheme.xaml`'s global `ComboBox`
  `ControlTemplate` (Git #935's dark-theme rewrite): it's a full custom template with NO
  `PART_EditableTextBox` part. `SessionSyncDialog.xaml`'s `CmbProductName` (`IsEditable="True"`)
  already had an explicit `Foreground="{StaticResource TextBrush}"` set — that was never the gap.
  Without the named editable-textbox part, WPF falls back to its own unstyled internal text box
  for the editable/selected text, which is invisible against the dark theme regardless of the
  ComboBox's own Foreground. Confirmed `SetProductNameSelection` (SessionSyncDialog.xaml.cs:67-83)
  and `MainWindow.TestMode.cs`'s `ExecuteEndSessionSyncAsync` already correctly call
  `VisualTestTrackerExportService.DetectArea` and set `_context.ProductName` before the dialog
  opens — the auto-detection feature itself was never missing, only its visibility.
  Fix: added a real `TextBox x:Name="PART_EditableTextBox"` (with its own minimal ControlTemplate
  carrying `PART_ContentHost`) to the ComboBox template in `desktop/BuildConsole/Themes/DarkTheme.xaml`,
  bound `Foreground="{TemplateBinding Foreground}"`, and added an `IsEditable=True` trigger to swap
  visibility between it and the existing `ContentSite` ContentPresenter. This is a global template
  fix — it also corrects `ExportBugDialog`'s `CmbTargetArea` and `ChatMappingsDocumentView`'s and
  `SettingsTabView`'s editable ComboBoxes, all of which share the same template and had the same
  latent bug.
  Cross-checked `VisualTestTrackerExportService.DetectArea` against `DevServiceRouting.ServiceKeyForRoute`
  per the issue's ask — found 3 real disagreements (different unmatched-route default MSP_Console
  vs Marketing; `/content` and `/view-as` unrecognized by DetectArea; `/retainers`, `/monitoring`,
  `/buy`, `/records`, `/lp`, `/copilot-assessment` unrecognized by DetectArea). Filed as
  **#4163**, parented under **#1791** ("Feature: UI Bugs & Dead Wiring (BuildConsole)") — #1202
  itself now rejects non-Feature direct children (Git #3708 hierarchy enforcement), so #4163 could
  not be filed as a direct sibling of #1202 as #4159's body suggested; #1791 is the correct
  Feature-tier fallback. Board status set to AI Batter Up. Not patched inline, per instructions.
  **Verification:** `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean,
  0 warnings/0 errors, both before and after rebase onto origin/main. Per the standing
  never-launch-the-exe-for-verification rule (Git #2156 — BuildConsole's single-instance mutex can
  courier a "new" launch into Shane's real running instance), live UI click-through in a real
  running BuildConsole was **not** self-verified this session — the fix is verified by a clean
  build plus a full read of the affected template, trigger wiring, and the auto-detection call
  chain (`ExecuteEndSessionSyncAsync` → `SetProductNameSelection` → the fixed template). Shane
  should confirm live that the Product Name/Area box now shows its auto-detected value on End &
  Sync, and that typing a manual override still works.
  Branch verified merged into origin/main (`verify-branch-merged.mjs`), working tree clean.
