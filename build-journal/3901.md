# #3901 — Ctrl+D Dispatch "No GitHub PAT configured" regression since #3829

- **Status:** ✅ DONE 2026-09-13T02:10:22Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 00d55e6f1

## Log
- 2026-09-13T02:06:04Z ⏳ IN FLIGHT — check ActivityLog settings.load, then fix the Command Center dispatch path (Path A) or restore Ctrl+D → DispatchDialog (Path B).
- 2026-09-13T02:10:22Z ✅ DONE — Path A. ActivityLog (activity-2026-09-12.log) showed the "no GitHub PAT configured" failure was app-wide, not Ctrl+D-specific: the watcher logged it from 20:46 to 21:07, followed by `settings.load` degraded-read warnings (4× JsonException, 21:22–21:27) and `settings.save` PROTECTED/REFUSED lines. That is the #3902 hidden-Save-in-Load PAT wipe / torn-file bug (fixed in 555bb0dc6 at 21:28), not #3829's rebinding — Ctrl+D calls the same `IssueDispatchService.DispatchAsync` the old dialog did. After the ~22:00 restart, dispatches #3906, #3908, #3911 and #3912 all queued through that shared path, and settings.json holds a 40-char GitHubPat. Hardening shipped in 00d55e6f1: `BuildConsoleSettings.IsDegradedRead` (JsonIgnore) is new, and `DispatchAsync` retries a degraded read three times (250/500/750ms). If the read still fails, the message says settings.json couldn't be read instead of the false "No GitHub PAT configured". Verification: isolated `dotnet build` of BuildConsole.csproj succeeded with 0 errors. I did not press Ctrl+D myself (agents must not launch BuildConsole.exe), so Shane's hands-on Ctrl+D check is still pending. Path B (revert to DispatchDialog) was not needed. No findings filed.
