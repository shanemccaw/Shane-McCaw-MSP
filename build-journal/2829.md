# #2829 — Retire legacy portal_security_plans table

- **Status:** ✅ DONE 2026-09-04T15:43:45Z
- **Scope:** platform

## Log
- 2026-09-04T15:29:36Z — Starting. Confirmed via psql: all 4 legacy tables
  (`portal_security_plans`, `portal_security_plan_sections`,
  `portal_security_plan_rows`, `portal_security_plan_versions`) still 0 rows
  locally. No consumer of `WireSecurityPlanPayload.plan` outside
  `portal-security-plan.ts` itself (grepped `artifacts/`, `lib/`). Plan:
  (1) remove `plan` field + legacy read path from the route (code, self-executed),
  (2) write the DROP TABLE migration file but do NOT run it — dropping tables is
  destructive DDL per CLAUDE.md's Database section, goes to Shane, not
  self-executed, (3) regenerate the contract pack once the drop actually lands
  (blocked on Shane running the migration — noted honestly, not done this session).
- 2026-09-04T15:43:45Z — ✅ DONE — Removed `plan`/legacy read path from
  `artifacts/api-server/src/routes/portal-security-plan.ts` (`WireSecurityPlanPayload`
  is now `{ assembledPlan }` only); removed the four legacy table definitions from
  `lib/db/src/schema/msp.ts`; wrote (did not run)
  `lib/db/migrations/manual/2026-09-04-drop-legacy-portal-security-plans.sql`.
  `tsc --noEmit` clean on `lib/db` and `artifacts/api-server`. No other code or
  test-manifest under this repo referenced the removed types/tables except one
  already-dead manifest, filed as #2870 (parented #1495, board status AI Batter
  Up) rather than fixed here — it tests a retired portal-v2 page. Commit
  8d9fb1b7e (rebased to c066690b), pushed and merged to `origin/main` — confirmed
  via `verify-branch-merged.mjs`. Item 3 of #2829 (contract pack regen) is left
  for a future session once Shane runs the drop migration — `Shane To-Do` label
  applied to #2829, issue commented. `git status --porcelain` clean at end.
