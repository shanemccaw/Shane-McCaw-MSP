# #4288 — MyArchitect: real editable curated navigation menu (replaces Azure's native left nav)

- **Status:** ✅ DONE 2026-09-15T21:55:00Z
- **Scope:** platform (shanemccaw/MyArchitect, not this repo's own code)
- **Started:** 2026-09-15
- **Commit(s):** `d28c4152f11648336172d301fc25ce2da8859e73` in `shanemccaw/MyArchitect` (main)

## Log
- 2026-09-15T21:45:00Z ⏳ IN FLIGHT — blockers #4278 (superseded by #4305, COMPLETED), #4284 (COMPLETED), #4287 (COMPLETED) all cleared. Cloned shanemccaw/MyArchitect into an isolated working copy, read LeftReferencePanel (the #4287 bookmark tree), TenantPortalUrls/PortalType navigation mechanism, and the DarkModePreferenceService/ConsoleHistoryService local-JSON persistence convention. Verified all 5 seed Azure deep-link URLs (Subscriptions, Resource groups, Entra ID overview, Key Vault, Cost Management) against real Microsoft docs / public code via web search, not guessed. About to build the editable tree data model + UI + persistence as a new "AZURE NAVIGATOR" section alongside the existing fixed Portal Bookmarks tree.
- 2026-09-15T21:55:00Z ✅ DONE — Shipped a real, editable, persisted Azure Navigator tree in `shanemccaw/MyArchitect`: `Models/AzureNavNode.cs`, `Services/IAzureNavigatorService.cs` + `AzureNavigatorService.cs` (JSON at `%LOCALAPPDATA%\MyArchitect\azure-navigator.json`, seeded with the 5 verified blades on first run), and a new "AZURE NAVIGATOR" section in `Shell/LeftReferencePanel.xaml`/`.xaml.cs` reusing #4287's expand-chevron/click-to-navigate row pattern, with an Edit-mode toggle exposing per-row add-child/rename/edit-URL/move-up/move-down/indent/outdent/confirm-armed-delete via a context menu. Wired into `MainWindow.xaml.cs` through the same `OpenPortalTabAsync`/`TenantPortalUrls.GetDeepLink(PortalType.AzurePortal, subPath)` mechanism #4287 already uses. Verification actually run, not assumed: (1) standalone `dotnet build -c Debug` — 0 warnings/0 errors; (2) a real launch of the built exe (`MyArchitect.exe`) stayed alive 15s with no crash/exception and wrote the real seeded JSON to `%LOCALAPPDATA%`, confirmed by reading that file back; (3) a throwaway console harness (not committed) exercised `AzureNavigatorService` directly — seed count, AddNode, RenameNode, SetNodeUrl, MoveNode (including the first-item boundary case), IndentNode, OutdentNode, DeleteNode, and a fresh-instance disk-reload round-trip all passed. Real merge conflict with a concurrently-landed #4291 (Script Library section, same file) resolved by keeping both additions; rebuilt clean after resolving. `git merge-base --is-ancestor` confirms the commit is a real ancestor of `origin/main`. Filed a separate finding, **#4319** (parented under #3454, milestone v1.1, labels `bug`, board status AI Batter Up), for a pre-existing, codebase-wide gap discovered while matching this file's conventions: ~234 raw `FontSize` literals across the app contradict UI_RULES.md §10's "zero inline styles" rule and its own "#4284: retrofit done" claim — not fixed here since it's out of scope for #4288 and far too broad to bundle into this build. Working tree left clean (verified via `git status --porcelain`); one harmless empty leftover directory (`MyArchitect-work/`, a Windows file-handle lock on an already-fully-committed scratch clone) is untracked and invisible to git status, noted here for transparency only.
