# #3313 — Page/room transitions are an instant hard cut

- **Status:** ✅ DONE 2026-09-09T03:12:21Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 1c3ef61f5 (implementation, rebased/merged as 1d298cbad on main)

## Log
- 2026-09-09T03:08:31Z ⏳ IN FLIGHT — confirmed root cause (render() in web/shanes-life/public/app.js does view.replaceChildren() with no transition); checked README + First Slice Prototype .dc.html for a stated transition preference (none found), implementing the issue's own suggested ~180ms opacity crossfade next.
- 2026-09-09T03:12:21Z ✅ DONE — added `@keyframes room-fade-in` + `.view.room-fade-in` (opacity 0→1, 180ms ease-out) to web/shanes-life/public/app.css, applied by removing/reflowing/re-adding the class at the end of render() in web/shanes-life/public/app.js so it fires on every navigation including pull-to-refresh's own re-render. Real verification: node --check on app.js; a throwaway Playwright harness (deleted before finishing, not committed) against the real app.css confirmed (1) the animation genuinely runs opacity 0→1 and finishes cleanly, (2) typing into the new room's input works immediately mid-fade -- interactivity is never blocked, (3) the fade re-triggers on a second navigation, not just once, (4) it coexists with pull-to-refresh's own `transform`/`ptr-settling` on the same #view element with zero clobbering (different CSS properties). Rebased agent/3313-q2038 onto origin/main and pushed straight to main (1d298cbad); verify-branch-merged.mjs confirms merged. git status clean.
