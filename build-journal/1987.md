# #1987 — CLAUDE.md rule: `pnpm install` is never the remedy, `--force` is prohibited, bandwidth is a real constraint

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled issue body + all 3 comments. Later comments
  supersede/extend the body: comment 1 adds the agent-self-override half (never
  grant yourself an exception to the bandwidth gate, don't retry, don't route
  around it, don't clear it via settings — fail closed, report); comment 2/3
  add a fourth, related rule (a blocking conclusion must become a real
  `blocked_by` edge, not just prose — #1867 evidence). Adding all of it to
  CLAUDE.md, mirrored into `.claude/settings.json`'s SessionStart hook
  `additionalContext` payload (same injection mechanism as the `d1f4cbf`/
  `f18e632` precedent). Watching the bash single-quote wrapping trap from
  `05a13e4e` — no apostrophes/smart quotes in the payload text.
