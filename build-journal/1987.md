# #1987 — CLAUDE.md rule: `pnpm install` is never the remedy, `--force` is prohibited, bandwidth is a real constraint

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** d11e157fa (bookend IN FLIGHT), 6f8494142 (real work)

## Log
- 2026-08-30 ⏳ IN FLIGHT — pulled issue body + all 3 comments. Later comments
  supersede/extend the body: comment 1 adds the agent-self-override half (never
  grant yourself an exception to the bandwidth gate, don't retry, don't route
  around it, don't clear it via settings — fail closed, report); comment 2/3
  add a fourth, related rule (a blocking conclusion must become a real
  `blocked_by` edge, not just prose — #1867 evidence). Adding all of it to
  CLAUDE.md, mirrored into `.claude/settings.json`'s SessionStart hook
  `additionalContext` payload (same injection mechanism as the `d1f4cbf`/
  `f18e632` precedent). Watching the bash single-quote wrapping trap from
  `05a13e4e` — no apostrophes/smart quotes in the payload text.
- 2026-08-30 ✅ DONE — `6f8494142`. Added two CLAUDE.md sections: (1)
  `## pnpm install is not a remedy, and bandwidth is a real constraint (Git
  #1987)` under Workspace/monorepo, with the 4 rules from the issue body plus
  the self-override addendum from comment 1, and a cross-reference note on the
  one legitimate `pnpm install` case (adding/changing a workspace dependency at
  repo root) so the two do not read as contradictory; (2) `### A blocking
  conclusion must become a blocked_by edge, not just a comment (Git #1987)`
  appended after the existing "blocked" label section, with the #1867 evidence
  from comments 2/3. Confirmed #1988 (worktree dependency isolation) is a real,
  open issue before citing it. Mirrored both additions verbatim (paraphrased to
  avoid quote marks) into the SessionStart hook's `additionalContext` string in
  `.claude/settings.json` — same single mechanism, no second injection path
  invented.
  Verification: decoded the hook's actual `command` field out of the committed
  JSON and ran it for real in bash — `bash -n` syntax-clean, executed with exit
  0 and empty stderr, and the resulting stdout parses as valid JSON with
  `hookEventName: SessionStart` and an `additionalContext` string containing
  both `PNPM INSTALL IS NOT A REMEDY` and `BLOCKED_BY EDGES ARE THE GATE`
  verbatim. Repeated this after the commit landed (against the committed file,
  not just the working copy) with the same result. Also confirmed by direct
  character scan that the only two `'` characters in the whole `command`
  string are the wrapper's own opening/closing bash quotes at index 5 and the
  last index — nothing leaked from the added payload text, so the `05a13e4e`
  quoting trap does not recur here. Did not additionally spin up a live
  interactive Claude Code session to check `/context` — the concrete
  hook-decode-and-execute above is the "demonstrate the injection path
  concretely" alternative the issue itself offers, and is a stronger check
  than a manual eyeball since it runs the literal bytes the harness would run.
  `git merge-base --is-ancestor 6f8494142 origin/main` — OK, HEAD on
  `agent/1987-q1040` is genuinely merged (pushed directly to `main`, no
  divergent branch). `git status --porcelain` clean (including after deleting
  the ad-hoc `.tmp_verify*/` scratch dirs used only for the bash re-run — never
  committed, already removed). No findings filed — this build only added
  documentation/rule text; #1988 (the real worktree-isolation bug this rule
  points agents at) was already open before this session started.
