# #3620 — Build Queue: self-blocked WAITING cards don't show blocked-by ghost card

- **Status:** ✅ DONE 2026-09-10T22:45:00Z
- **Scope:** buildconsole

## Log
- 2026-09-10T22:30:00Z — Extended ghost-card render gate in `BuildQueuePanel.xaml.cs` to include `IsWaitingSelfBlocked(item)` alongside `item.Status == "queued"`, per #3620's real fix (matching #3599's predicate).
- 2026-09-10T22:45:00Z — ✅ DONE — one-line fix committed at 892b380f0 (rebased onto main as fa4af3ced), pushed and merged to `origin/main`. Verified with `dotnet build desktop/BuildConsole/BuildConsole.csproj` — Build succeeded, 0 errors. `verify-branch-merged.mjs` confirms ancestor of main. `git status --porcelain` clean.
