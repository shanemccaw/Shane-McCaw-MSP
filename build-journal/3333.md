# #3333 — Wins header missing critter avatar + correctly-positioned subtitle

- **Status:** ✅ DONE 2026-09-09T00:10:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-09
- **Commit(s):** e48327f3 (fix), merged to main via merge commit on origin/main

## Log
- 2026-09-09T00:00:00Z ⏳ IN FLIGHT — confirmed root cause via issue body: `viewWins()` calls `roomHeader(view, WINS_TINT, "Wins")` without `{ icon, sub }`, and renders a separate bare subtitle paragraph. Fixing to match Lists'/Pantry's correct usage pattern.
- 2026-09-09T00:10:00Z ✅ DONE — replaced the bare `roomHeader(view, WINS_TINT, "Wins")` + separate `<p class="muted small">` subtitle with `roomHeader(view, WINS_TINT, "Wins", { icon: critterIcon("wins", { size: 36 }), sub: "real things that got done" })`, matching the confirmed-correct pattern used by Lists (line ~2845) and Pantry (line ~8886). `node --check` passed. Merged into `origin/main`; `verify-branch-merged.mjs` confirms ancestor. Broader finding (Inbox/Things/People/Vault/Money/Tesla/Settings all missing `{icon, sub}` too) was already noted in the issue body itself — not re-filed separately since the issue author already flagged it as its own follow-up scope.
