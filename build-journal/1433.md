# #1433 — Compliance :area wildcard — gate FindingBlock on nodata classification

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** 91805e956 (bookend open); b91011692 (the real fix); 1ae8b81af (manifest lastVerifiedAgainstCommit fixup)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Per #1415's investigation comment, `PortalV2ComplianceAreaPage`'s
  `FindingBlock` is unconditionally `CMP_FINDINGS[link.finding]` with no relationship to
  `areaRes.dataState`. For the 3 cards `cmpAreaWiring.ts` classifies `nodata`
  (compliance-disposition, compliance-preservation-lock, compliance-holds), this produces a
  direct on-page contradiction: an honest "Not measured" status pill immediately followed by
  a fully fabricated finding body with invented counts. Gating `FindingBlock` on `!nodata` for
  those 3 cards, rendering the same honest "no producing check yet" message the inert
  non-finding cards already use. Leaving the other 3 live-classified cards' fixture body alone
  (documented, lower-priority, same treatment as security/oauth in #1414) per the issue's scope.
- 2026-08-27 ✅ DONE — `portal-v2-cmp-area.tsx`: added `showFinding = finding !== null && !nodata`
  and gated `<FindingBlock>` on it; the 3 nodata cards now fall through to the existing honest
  registers-pointer panel. Added a defensive `reportClientEvent` beacon (channel
  `engine.dashboard`) for the case a finding-backed card has no `CMP_AREA_BACKING` entry at all
  (a classification-completeness bug the unit test catches at test time but nothing caught at
  runtime before this). Extended `test-manifests/portal/compliance-drilldowns.json` (AREA7-10,
  sessionTag `cmp-area-1433`) asserting the disposition card shows "Not measured" + the honest
  registers panel and never renders `pv2-cmparea-finding`. Verification: `tsc --noEmit` on
  `artifacts/msp-portal` shows zero new errors (2 pre-existing errors in unrelated
  `SowScreen.tsx`/`PortalV2Shell.tsx`, untouched by this change); `cmpAreaWiring.test.ts`
  10/10 passing unchanged (no logic change to that module, only the page's render gate).
  `shaneapp://runTest?file=portal/compliance-drilldowns.json&onlyTag=cmp-area-1433` WAS actually
  invoked this session (per CLAUDE.md's verification-defaults rule — don't assume "unable to
  verify"). The protocol is real and reachable: it returned a genuine envelope, not a launch
  failure — `{"ok":false,"error":"no steps matched session tag 'cmp-area-1433'"}`. That is the
  expected, honest result: the manifest's own notes document `runTest` hits the DEPLOYED build,
  and this fix (and its new AREA7-10 steps) is only committed/pushed to `origin/main`, not yet
  merged into the dev-server checkout and redeployed — so the deployed manifest copy has no
  `cmp-area-1433`-tagged steps yet. The new steps are written and registered, ready for the next
  session (or a post-deploy follow-up) to run once this lands on the deployed build.
