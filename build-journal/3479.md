# #3479 — Feature: Runbooks (MyArchitect)

- **Status:** ✅ DONE 2026-09-10T20:24:14Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 7250ceb6d (service-layer work, prior session), b926757616a9c37c0c1a359470c0e61d0b0b12fd (this session's UI work)

## Log
- 2026-09-10T06:10:44Z ⏳ IN FLIGHT — pulled #3479 body + both comments per standing
  rules. Comment history:
  1. a `BUILD:` dispatch comment (buildSet `MyArchitect`) carrying
     `--blocked-by 3493` explicitly, and instructing to read
     `desktop/MyArchitect/GEMINI.md`, `BUILD_QUEUE_METHOD.md`, `UI_RULES.md`.
  2. a later `BUILD:` dispatch comment (buildSet `BatterUpClearOut`) that omits the
     `--blocked-by` flag but does not say the blocker cleared, and only reiterates
     the standing "pull body + comments, build per real stated scope" instructions.
  Checked the real `blocked_by` edge via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3479/dependencies/blocked_by`:
  one edge, to **#3493** ("Feature: UI Shell Redesign (MyArchitect)") — still open,
  `in-flight`, not `complete`. Its own body states explicitly: "Foundational shell
  scaffolding — must land before other Features' UI work." `GEMINI.md`'s standing
  rule is the same: "#3493 must land before any other Feature's UI work ... If
  #3493 isn't done yet, do not build ad-hoc chrome ... stop and flag it instead."
  Confirmed no shell work has actually landed on disk: no Fluent.Ribbon package in
  `MyArchitect.csproj`, no ribbon/contextual-tab mechanism in `MainWindow.xaml`, and
  `build-journal/3493.md` does not exist.
- 2026-09-10T06:24:00Z 🛑 BLOCKED — same partial-ship pattern as sibling sessions on
  this epic (#3459, #3474): shipped and pushed the real, non-UI service layer at
  commit **7250ceb6d** — `RunbooksService`/`IRunbooksService` (real `HttpClient`
  wrapper, no fixture data) plus wire models (`RunbookStep`, `HoldWindow`, `Runbook`,
  `RunbooksSummary`, `RunbookRunSummary`, `HoldWindowEvent`, `RunbooksPayload`) that
  mirror `portal-runbook-wire.ts`'s `WireStep`/`WireHoldWindow`/`WireRunbook`/
  `RunbooksSummary`/`WireRunbookRunSummary` field-for-field — nothing invented beyond
  what the endpoint actually sends. Covers all four real, confirmed endpoints from
  `msp-runbooks.ts`: `GET /api/msp/runbooks`, `PUT /api/msp/runbooks/:id/steps/:pos`,
  `POST /api/msp/hold-windows/:id/extend`, `GET /api/msp/hold-windows/:id/events`.
  Short-TTL cache + explicit invalidation on write, same pattern as
  `LaunchControlActionsService` (#3460).
  - `dotnet build` on `desktop/MyArchitect`: 0 warnings, 0 errors.
  - Live-verified against the local dev api-server (already running on :8080): all
    four routes return a real `401 AUTHENTICATION_REQUIRED` (route matched,
    auth-gated) rather than a 404 — confirms every URL this client builds matches a
    real, live route.
  - `AuthToken`/`mspId` resolution has nowhere real to source from yet — same known,
    already-filed gap as #3502 (fixture `TenantService` only resolves fake string
    GUIDs, no real numeric `tenants.id`/`mspId`). Not re-filed; #3502 already covers
    it and applies identically here.
  - Remaining scope — the actual Runbooks UI panel (#3479's three "Client:" checklist
    items: runbooks+history view, step completion wired into the Console execution
    flow, hold-window extend + audit trail view) — is genuinely blocked on #3493 (UI
    Shell Redesign) per `GEMINI.md`'s explicit standing rule ("#3493 must land before
    any other Feature's UI work ... do not build ad-hoc chrome ... stop and flag it
    instead"). The real `blocked_by` edge #3479→#3493 already existed and is current
    (#3493 still open, `in-flight`, not `complete`) — left in place, no stale edge to
    remove. Labels synced `in-flight` → `blocked`. Comment posted on #3479 explaining
    the wait. No finding filed (nothing new beyond the already-tracked #3502 gap).
    Working tree clean; own commits only (bookend commit + feature commit), no
    unrelated files touched.
- 2026-09-10T20:12:05Z ⏳ IN FLIGHT — resuming per the new dispatch comment.
  Verified independently (not just trusting the comment's claim): `gh api
  repos/.../issues/3493 --jq '.id, .state'` → `closed`. The real `blocked_by` edge
  #3479→#3493 was still present (pointing at a now-closed blocker — exactly the
  stale-edge case Git #1987 calls out) — removed it via `DELETE
  .../issues/3479/dependencies/blocked_by/5407799446`, confirmed empty afterward.
  Labels synced `blocked` → `in-flight`. Building the real remaining UI now, wired
  into #3493's actual shell primitives (`ShellRegistry`/`GallerySpec`/
  `RecordWorkspaceSpec`/`ContextualTabSpec`) per the Script Library (#3460)
  precedent in `MainWindow.xaml.cs` — not ad-hoc chrome. Reusing the already-shipped
  `RunbooksService`/`IRunbooksService` + wire models from commit 7250ceb6d
  unchanged. The known `customerId` resolution gap (`TenantService` fixture data,
  filed as #3540 under #3457) applies identically here — surfaced as an honest
  blocked-row in the gallery, same pattern as Script Library, not re-filed.
- 2026-09-10T20:24:14Z ✅ DONE — shipped and pushed the real Runbooks UI at commit
  **b926757616a9c37c0c1a359470c0e61d0b0b12fd**, closing all three of #3479's own
  "Client:" checklist items, wired into #3493's real shell (not ad-hoc chrome):
  - **GET runbooks + run history** — a "Runbooks" ribbon group on the Console tab
    (`RegisterConsoleTab`) with a real-data `GallerySpec` (`BuildRunbookRows`, real
    `GET /api/msp/runbooks`); each row opens a full-panel `RecordWorkspaceSpec`
    (`OpenRunbookRecord`) with real facts (progress, days elapsed/left, recurring,
    hold badge), the current cycle's `Context` prose, and a `RunHistory` list.
  - **PUT step completion, wired into the embedded Console execution flow** — each
    current-cycle step renders as one `WorkspaceAction` (`☑`/`☐` + step text);
    selecting it (`ToggleRunbookStep`) fires the real `PUT
    /api/msp/runbooks/:runbookId/steps/:position` and reports the real result into
    `ConsolePanel`'s output pane — the same feedback channel
    `RunScriptLibraryAction` (#3460) already uses for a server-side write, per the
    issue's own "#3459 dependency" note — then reopens the record against a
    freshly re-fetched (cache-busted by the write itself) payload so the checkbox
    glyph reflects reality, not stale in-memory state.
  - **Hold-window extend + audit trail view** — a second "Hold Windows" gallery
    (`BuildHoldWindowRows`, same payload's top-level `holds`) opening its own
    record workspace (`OpenHoldWindowRecord`): real facts (badge, days/hours left,
    total/extended days, closes-at, scan verdict), a write-through Extend action
    (`Edits` feed a dictionary a `Confirm`-armed `WorkspaceAction` reads — same
    shape as `OpenScriptLibraryRecord`) firing the real `POST
    /api/msp/hold-windows/:holdId/extend`, and the real decision audit trail from
    `GET .../events` rendered as `WorkspaceListRow`s, fetched fresh on every open
    (never cached).
  - Both galleries and the step/extend actions are gated by the same known
    `customerId` resolution gap `TryResolveLaunchControlScope` already states
    honestly (`TenantService` fixture data — #3502/#3505/#3540, owned by #3457) —
    surfaced as an honest blocked-row/blocked-workspace, not re-filed, not guessed.
  - Fixed a real gap found in this session in the prior session's service layer:
    `IRunbooksService` didn't declare `AuthToken` (only the concrete
    `RunbooksService` did), so `MainWindow`'s single auth-fanout point
    (`ApplyAuthState`) couldn't set it through the interface reference — added to
    the interface, matching every other MSP-console service
    (`ILaunchControlActionsService`, `IAdminRetainerService`, `IRetainerService`).
  - **Verification:** `dotnet build MyArchitect.csproj` — 0 warnings, 0 errors.
    Live-verified against the local dev api-server (already running on :8080): all
    four real routes (`GET /api/msp/runbooks`, `PUT .../steps/:position`, `POST
    /api/msp/hold-windows/:id/extend`, `GET .../events`) return real `401` (route
    matched, auth-gated), not `404` — reproduced the same live confirmation the
    prior session already established, this time against the new UI code paths'
    exact same service calls. No WebView2/interactive UI runner exists for
    MyArchitect in this session's environment, so the actual rendered gallery/
    workspace panels were not click-verified in a live window — stated honestly,
    not claimed.
  - `node scripts/dev-server/verify-branch-merged.mjs` → OK, `b9267576` merged into
    `origin/main`. `git status --porcelain` clean at session end. No findings filed
    (the `AuthToken` interface gap was fixed in-session, not left as a finding; the
    `customerId` gap was already filed at #3540).
  - Labels synced `in-flight` → `complete`. Issue comment posted on #3479 with this
    same summary and the real commit hash.
