# #3479 — Feature: Runbooks (MyArchitect)

- **Status:** 🛑 BLOCKED (partial real work shipped) 2026-09-10T06:24:00Z
- **Scope:** platform (MyArchitect desktop app, desktop/MyArchitect)
- **Started:** 2026-09-10
- **Commit(s):** 7250ceb6d (feature work)

## Log
- 2026-09-10T06:10:44Z ⏳ IN FLIGHT — pulled #3479 body + both comments per standing
  rules. Comment history:
  1. a `BUILD:` dispatch comment (buildSet `MyArchitect`) carrying
     `--blocked-by 3493` explicitly, and instructing to read
     `desktop/MyArchitect/GEMINI.md`, `BUILD_QUEUE_METHOD.md`, `UI_RULES.md`.
  2. a later `BUILD:` dispatch comment (buildSet `BatterUpClearOut`) that omits the
     `--blocked-by` flag but does not say the blocker cleared, and only reiterates
     the standing "pull body + comments, build per real stated scope" instructions.
  Checked the real `blocked_by` edge via
  `gh api repos/shanemccaw/Shane-McCaw-MSP/issues/3479/dependencies/blocked_by`:
  one edge, to **#3493** ("Feature: UI Shell Redesign (MyArchitect)") — still open,
  `in-flight`, not `complete`. Its own body states explicitly: "Foundational shell
  scaffolding — must land before other Features' UI work." `GEMINI.md`'s standing
  rule is the same: "#3493 must land before any other Feature's UI work ... If
  #3493 isn't done yet, do not build ad-hoc chrome ... stop and flag it instead."
  Confirmed no shell work has actually landed on disk: no Fluent.Ribbon package in
  `MyArchitect.csproj`, no ribbon/contextual-tab mechanism in `MainWindow.xaml`, and
  `build-journal/3493.md` does not exist.
- 2026-09-10T06:24:00Z 🛑 BLOCKED — same partial-ship pattern as sibling sessions on
  this epic (#3459, #3474): shipped and pushed the real, non-UI service layer at
  commit **7250ceb6d** — `RunbooksService`/`IRunbooksService` (real `HttpClient`
  wrapper, no fixture data) plus wire models (`RunbookStep`, `HoldWindow`, `Runbook`,
  `RunbooksSummary`, `RunbookRunSummary`, `HoldWindowEvent`, `RunbooksPayload`) that
  mirror `portal-runbook-wire.ts`'s `WireStep`/`WireHoldWindow`/`WireRunbook`/
  `RunbooksSummary`/`WireRunbookRunSummary` field-for-field — nothing invented beyond
  what the endpoint actually sends. Covers all four real, confirmed endpoints from
  `msp-runbooks.ts`: `GET /api/msp/runbooks`, `PUT /api/msp/runbooks/:id/steps/:pos`,
  `POST /api/msp/hold-windows/:id/extend`, `GET /api/msp/hold-windows/:id/events`.
  Short-TTL cache + explicit invalidation on write, same pattern as
  `LaunchControlActionsService` (#3460).
  - `dotnet build` on `desktop/MyArchitect`: 0 warnings, 0 errors.
  - Live-verified against the local dev api-server (already running on :8080): all
    four routes return a real `401 AUTHENTICATION_REQUIRED` (route matched,
    auth-gated) rather than a 404 — confirms every URL this client builds matches a
    real, live route.
  - `AuthToken`/`mspId` resolution has nowhere real to source from yet — same known,
    already-filed gap as #3502 (fixture `TenantService` only resolves fake string
    GUIDs, no real numeric `tenants.id`/`mspId`). Not re-filed; #3502 already covers
    it and applies identically here.
  - Remaining scope — the actual Runbooks UI panel (#3479's three "Client:" checklist
    items: runbooks+history view, step completion wired into the Console execution
    flow, hold-window extend + audit trail view) — is genuinely blocked on #3493 (UI
    Shell Redesign) per `GEMINI.md`'s explicit standing rule ("#3493 must land before
    any other Feature's UI work ... do not build ad-hoc chrome ... stop and flag it
    instead"). The real `blocked_by` edge #3479→#3493 already existed and is current
    (#3493 still open, `in-flight`, not `complete`) — left in place, no stale edge to
    remove. Labels synced `in-flight` → `blocked`. Comment posted on #3479 explaining
    the wait. No finding filed (nothing new beyond the already-tracked #3502 gap).
    Working tree clean; own commits only (bookend commit + feature commit), no
    unrelated files touched.
