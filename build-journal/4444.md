# #4444 — Remediation Tracker: "Phases and fees" card appears hardcoded, not wired to a real source

- **Status:** ✅ DONE 2026-09-16T00:20:00Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** b495384c2 (bookend only — investigation, no product code changed)

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — Read-only discovery: trace whether "Phases and fees" card ($16,200/$11,800/$8,200, NOT READY badges, verified/accepted/claimed/untouched counts) is real-wired or hardcoded. Found `PricingPanel.tsx` renders `pricing` prop verbatim from the GET response; traced to `computeRemediationTrackerPricing()` in `remediation-tracker-pricing.ts`, called server-side in `portal-remediation-tracker.ts` against real `remediation_tracker_steps` DB rows for the JWT's `customerId`. Confirmed via direct psql query against local `DATABASE_URL`: customer 1 (test tenant) has exactly one row (`s1`, `not_started`/`unverified`) — everything else is the documented "no row = not_started" convention. This is real, wired data showing the correct degenerate/all-untouched state, not a fixture. Posting full evidence as an issue comment; not closing the issue myself (that's Shane's call per repo policy) despite the dispatch prompt's "close as not-a-bug" phrasing.
- 2026-09-16T00:20:00Z ✅ DONE — Posted full file:line evidence as an issue comment (https://github.com/shanemccaw/Shane-McCaw-MSP/issues/4444#issuecomment-5707132909): the dollar figures are `PILLAR_FEE`, a real fixed-fee schedule confirmed with Shane 2026-08-11 (#734), computed server-side via `computeRemediationTrackerPricing()` from real DB rows every request — not hardcoded/fixture. The NOT READY badges and 0/0/0/N counts are the correct rendering of this test tenant's genuinely-untouched state (1 row total, `not_started`). Recommended closing as not-a-bug; did not close it myself. No code changed, no new finding to file, no `blocked_by` edge needed. Labels flipped `in-flight` → `complete`.
