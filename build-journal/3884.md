# #3884 — Chat Mappings window silently loads nothing (BuildTrackerApiClient dependency)

- **Status:** ✅ DONE 2026-09-13T00:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 6efc7c3c7 (fix), 70739d8ea (IN FLIGHT bookend)

## Log
- 2026-09-13T00:05:00Z ⏳ IN FLIGHT — confirmed via direct read of Shane's real `scripts/build-queue-watcher.config.json` that `ApiBaseUrl`+`ingestToken` are both set, so `_buildTrackerApi.IsConfigured` is actually TRUE (not the literal cause the issue hypothesized). Real cause: `_api` field is null until `RunDeferredStartupAsync` (async, deferred) reaches line ~598; `ChatMappingsDocumentView.LoadDataAsync()`'s `if (_api == null) return;` is a genuine silent no-op that permanently freezes the singleton tab (never re-Initialized on reopen) with `TxtStatus` stuck on its XAML default "Loading chats...". Also confirmed `LocalSqlExecutor.ExecuteAsync`'s Dev-path body never uses `api` for the real connection — vestigial requirement. Implementing: nullable `api` param on `LocalSqlExecutor.ExecuteAsync`, remove all `_api == null` early-return guards in `ChatMappingsDocumentView`, add a live diagnostic log line, let existing try/catch fail loudly (TxtStatus + ToastEngine) instead of silently no-op.
- 2026-09-13T00:45:00Z ✅ DONE — Shipped in `6efc7c3c7`. Made `LocalSqlExecutor.ExecuteAsync`'s `api` param nullable and moved the null/`IsConfigured` requirement to only the genuine Staging/Production branch (the Dev/local path never touched `api` for the connection anyway). Removed all 7 silent `if (_api == null) return;` guards in `ChatMappingsDocumentView.xaml.cs` — every call site already has a real try/catch that sets `TxtStatus.Text` + `ToastEngine.Error`, so a genuine failure now surfaces loudly instead of a silent no-op. Added a live `ActivityLog` diagnostic in `Initialize()` reporting whether `_api` was null/unconfigured/fine, for any future session. **Second real bug found and fixed in the same pass:** every one of the 9 `LocalSqlExecutor.ExecuteAsync` call sites in this file used the 2-arg overload, which defaults to `DatabaseRegistry.DefaultKey` ("product" / `shanemccawmsp`) — but `bt_epics`/`bt_chats`/`bt_chat_issues` genuinely live in BuildConsole's own database (`BUILD_DATABASE_URL` / "buildconsole" key). Confirmed via direct `psql`: those tables don't exist in `shanemccawmsp` (`ERROR: relation "bt_epics" does not exist`), and do exist with real data (58 rows) in the BuildConsole db. Added `DatabaseRegistry.BuildConsoleKey` and passed it explicitly on all 9 call sites — without this the window would have gone from silently-empty to loudly-broken, not actually working. Verified: clean `dotnet build BuildConsole.csproj` (0 warnings/errors, twice); direct `psql` against the real `BUILD_DATABASE_URL` running the exact `bt_chats`/`bt_epics` queries this view now issues, confirming real rows come back. No test manifest — `desktop/BuildConsole` is explicitly out of scope for those. `git status --porcelain` clean; branch verified merged into `origin/main` via `scripts/dev-server/verify-branch-merged.mjs` before writing this. No other findings surfaced — the only other `LocalSqlExecutor` callers query `product`-database tables (`monitor_checks`, `simulator_migration_runs`), which were already correct with the default key.
