# Build Journal — #3393

- **Status:** ✅ DONE 2026-09-10T17:39:00Z
- **Scope:** platform

## Log

- 2026-09-10T17:32:00Z — Resumed after being blocked on #3390 (see prior 5fc98d4e6 commit). #3390
  is now CLOSED, labeled `complete`. Removed the stale `blocked_by` edge #3393→#3390 and the
  `blocked` label; set `in-flight`. Proceeding to implement `post_comment(number, body)` and
  `list_comments(number)` in `artifacts/github-mcp-server`.
- 2026-09-10T17:39:00Z — ✅ DONE — Shipped `post_comment(number, body)` and `list_comments(number)`
  in `artifacts/github-mcp-server`. Merged/rebased against a concurrent sibling build (#3394,
  `close_issue`) which had already added `postIssueComment()`/needed `githubRepo(): {owner,repo}`
  shape in `src/github.ts` — reused that instead of duplicating, and added a matching
  `listIssueComments()` there for symmetry; `src/tools/post-comment.ts` and
  `src/tools/list-comments.ts` now just call those. Registered in `src/tools/index.ts`,
  documented in README.md. `npx tsc --noEmit` clean. `npm run smoke` passed. Real verification:
  called the tools directly (not the gh CLI) against the live repo — posted a real comment on
  #3393 itself (visible on the issue), then `list_comments` confirmed it came back oldest-last in
  a chronologically-ascending list. Commit ee2db6157 (`git merge-base --is-ancestor` verified
  merged into origin/main). Working tree clean.
