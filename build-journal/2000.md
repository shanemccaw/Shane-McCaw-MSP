# #2000 — Search/filter boxes: shared inline ✕ clear affordance

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** a78573897 (on origin/main; rebased from 00af74459)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Investigated all 15 sites + the 3 existing bespoke clears.
  Building one `SearchTextBox` style (control template with the ✕ baked in) in
  `Themes/DarkTheme.xaml`, plus a small attached-behavior class
  (`Behaviors/SearchTextBoxBehavior.cs`) to wire the ✕ click and Escape-to-clear
  without per-site code-behind. About to apply the style to all 15 boxes and
  consolidate 2 of the 3 existing bespoke clear buttons onto it.
- 2026-08-30 ✅ DONE — Shipped the `SearchTextBox` style + `SearchTextBoxBehavior`
  and applied it to all 15 boxes from the issue inventory. `dotnet build -c
  Release`: 0 warnings, 0 errors. Consolidated 2 of the 3 existing bespoke
  clears (TestHistoryWindow.BtnClearSearch, SettingsTabView.BtnClearSearch +
  BtnClearTestEnvSearch) onto the shared style, deleting their dead Click
  handlers. LeftSidebar.xaml:292 `BtnClearSearch` stays standalone by design —
  documented inline: it resets SearchInputBox + SearchIncludesBox together
  plus SearchResultsTree/SearchSummaryText, a whole-form reset rather than
  "clear this box's text"; both of those boxes still got their own inline ✕.
  MainWindow.TitleSearchBox opts out of the shared Escape handling
  (`behaviors:SearchTextBoxBehavior.HandlesEscape="False"`) because it already
  owns a richer Escape (dismiss the command-palette dropdown, in
  MainWindow.UniversalSearch.cs) — extended that existing handler to also
  clear the text so the two behaviors don't race on the same routed event.
  Corrected an inference in the issue body: `TestHistoryWindow.xaml:83` (cited
  as the reference/consolidation target) is actually `BtnClearFilter_Click`,
  an unrelated external filter-banner clear — left untouched, out of scope.
  The real per-box clear being consolidated is `BtnClearSearch` at (current)
  line ~133. Verified per site (per requirement 3) that every TextChanged
  handler is a plain XAML-attribute wire-up (none register via code-behind
  `+=`, contrary to the issue's stated distinction) and that `.Clear()`
  correctly raises TextChanged in every case, including BuildQueuePanel's
  debounced QueueSearchBox and LeftSidebar's Enter-triggered
  SearchInputBox/SearchIncludesBox (which don't live-filter on keystroke
  either, so `.Clear()` alone — no extra behavior — is the correct parity).
  No new GitHub issues filed — no gaps found beyond the corrected inference
  above, itself resolved in this same session. Branch verified merged via
  `scripts/dev-server/verify-branch-merged.mjs` before writing DONE. Working
  tree clean (`git status --porcelain` empty at completion).
  Not independently verified: a live BuildConsole.exe visual check — did not
  relaunch/kill Shane's running instance per the build's own rule; verification
  is the clean Release build plus source-level trace of each site's wiring.
