# #3088 — Feature: Shopping — Core list + capture (Shane's Life)

- **Status:** ✅ DONE 2026-09-07T20:30:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** `e21b1d1ee` (real work), `cb088354b`/`d26f8023d` (bookends)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — pulled #3088's real current scope and checked its
  `blocked_by` edges before starting any implementation.
- 2026-09-07T00:00:00Z 🛑 BLOCKED — the dispatch header's stated blocker, #3107 (DB
  unification), is closed/complete; its stale `blocked_by` edge was removed, along with
  #3116's (also closed/complete). But a real, still-open blocker the dispatch header
  didn't know about remains: **#3120** ("Wire real design system tokens into Shane's Life")
  explicitly names #3088 and its siblings as needing to wait for the shared token system
  to land first. Left/confirmed the real `blocked_by` edge to #3120, added the `blocked`
  label, removed `in-flight`, and posted an explanatory comment on #3088. Stopping here —
  building the Shopping list/capture UI now would mean reconstructing colors independently
  of the token system #3120 is about to establish, exactly what #3120 exists to prevent.
  No code changes made. Working tree clean apart from this bookend.
- 2026-09-07T20:10:00Z ⏳ IN FLIGHT — resuming. Re-checked `blocked_by`: #3120 is now labeled
  `complete` (its real commits `aaec5820b`/`551723dd1` confirmed as ancestors of `origin/main`
  after a fetch+rebase of this worktree, which was stale). Removed the now-cleared `blocked_by`
  edge to #3120 and the `blocked` label; proceeding with the real build.
- 2026-09-07T20:30:00Z ✅ DONE — `e21b1d1ee` on `main`. Real scope shipped, all on the typed
  `lists`/`list_items` shape #3116 decided rooms should use:
  1. **Core list**: `src/core/lists.mjs` — `getOrCreateShoppingList` (singleton "one run" per
     the capture grammar's `grocery words -> the run`), `addListItems`, `setListItemChecked`
     (already existed from #3116), `deleteListItem`, `clearCheckedItems`, `replaceListItems`.
     Owner HTTP surface in `src/routes/api.mjs`: `GET /api/shopping`,
     `GET/POST /api/lists/:id[/items]`, `PATCH/DELETE .../items/:itemId`,
     `POST .../clear-checked`.
  2. **Capture-box/MCP wiring**: new MCP tools `push_list` (add or `replace:true` for a fresh
     run, with an optional share minted in the same call), `get_list`, `check_list_item` —
     `src/mcp/tools.mjs`.
  3. **Full-screen Shopping mode**: `#/shopping` in `public/app.js`/`public/index.html` — reuses
     the app's already-token-correct shared chrome (cards, checklist rows, chips) rather than a
     bespoke pixel clone of one of `Shanes Life 04 - Shopping.dc.html`'s three alternate layout
     options; barcode scan / aisle memory / cart-swipe are each their own separate Feature.
  4. **No-login share**: reused #3116's `share_links` layer unchanged (`listId` support was
     already there); `share.js`/`share.html` needed zero changes since `getListForShare`
     already normalises onto the same shape the public route expects.
  No schema/migration needed -- #3116 already shipped the columns this used.
  **Verified**: `npm run check` 70/70 PASS against a real local server (port 5911, avoiding a
  live instance already on 5000) and the real shared `finances` database -- 12 new checks
  cover the full owner CRUD path and all three new MCP tools end-to-end, including a real
  `list_items.done` write confirmed by a direct DB read after `check_list_item`. Two bugs
  found and fixed during verification: a real shape inconsistency in the owner PATCH route
  (was returning entity-shaped `checked_at` instead of the `done`/`done_at` shape the rest of
  the owner API and `app.js` use), and two of my own new test assertions using the wrong field
  name for the MCP tools' intentionally entity-shaped return. `verify-branch-merged.mjs`
  confirms `e21b1d1ee` is an ancestor of `origin/main`. Working tree left clean.
  No findings filed — nothing discovered outside this issue's own scope.
