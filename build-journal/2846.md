# #2846 — Policy.ReadWrite.SecurityDefaults vs the CA-write pair on quickstart-v1 step 5

- **Status:** ✅ DONE 2026-09-06T18:25:13Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `056b1ac0e` (merged to main; `git merge-base --is-ancestor` exit 0)

## Log
- 2026-09-06T17:36:00Z ⏳ IN FLIGHT — Shane authorised (2026-09-05) the single controlled live test #2846's "Next action" asks for: on the DEV app registration `9f6f4772-b5be-421f-815e-b392336c373a` only, grant `Policy.ReadWrite.SecurityDefaults` without `Policy.ReadWrite.ConditionalAccess`, issue the real `PATCH /policies/identitySecurityDefaultsEnforcementPolicy` that quickstart-v1 step 5 makes, and record the real 204-or-403. Git #1913 gate discipline: this one authorised test only, DEV app reg only, state restored afterwards.
- 2026-09-06T18:25:13Z ✅ DONE — Question settled with four real PATCHes against `mccawsoft2.onmicrosoft.com`, DEV write app only. Every call sent the tenant's own current value (`isEnabled: true`) so only the authorisation decision was exercised and no tenant state changed; each token's `roles` claim was decoded and asserted before its call. Results: (1) `Policy.Read.All` + `Policy.ReadWrite.ConditionalAccess` → **204**; (2) `Policy.Read.All` alone → **403** (req-id ccf1e3ed-600d-4caa-a79b-c7ca08240f29); (3) `Policy.Read.All` + `Policy.ReadWrite.SecurityDefaults`, CA-write absent → **204**; (4) `Policy.ReadWrite.SecurityDefaults` alone → **403** (req-id dfbee186-bd42-4da8-b28b-4423e5506c32). Conclusion: Microsoft's operation page IS the stale one, but the issue's premise that SecurityDefaults suffices *alone* is wrong — `Policy.Read.All` is still genuinely required. Rule narrowed to that pair. Shipped: new `WritePermissionRule.confirmedByLiveTest` field as the single gated exception to the #1975 doc-quoting rule; `scripts/azure/securitydefaults-permission-probe-2846.mjs` committed so the result is re-runnable. Verification: 87/87 in `graph-write-permissions.test.ts`, `tsc --noEmit` clean on api-server. Honest cost: `DERIVED_WRITE_APP_PERMISSIONS` grows 24 → 25 (CA-write stays for step 6), now asserted in a test. State restored: DEV app role assignments diffed against the pre-test snapshot — 17 before, 17 after, zero added, zero missing; Security Defaults still `isEnabled: true`. No PROD app registration, Key Vault secret, or Staging deploy touched.
- 2026-09-06T18:25:13Z 📋 FILED — **#3048** (sibling of #2489, labels `bug`+`security`, board "AI Batter Up"): both write app registrations must declare and consent to `Policy.ReadWrite.SecurityDefaults`. PROD half written as a plan on **#1281** per the Git #1913 gate. Nothing is broken until it runs — step 5 still succeeds on the CA-write grant.
- 2026-09-06T18:25:13Z ⚠️ NOTE — The full api-server vitest run shows 54 files failing on missing `DATABASE_URL` / `AI_INTEGRATIONS_ANTHROPIC_BASE_URL` plus a set of `azure-rm.test.ts` failures. All pre-existing and environmental; none touch the derived permission set (only `graph-write-permissions.test.ts` consumes it, and it is green).
