# #2776 — Home dashboard: add per-Epic burndown card (Epic picker + reused chart-drawing)

- **Status:** ✅ DONE 2026-09-04T00:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** c1c503322 (rebased/pushed to main as 57cfb0ae0)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — audited `GitHubIssueTimeSeriesService.GetEpicSeriesAsync` (already existed from #2711/#2714) and found it called `BuildSeries` without the #2773 `selfRootEpicNumber` self-rollup escape hatch, so charting an internal-tooling Epic (#1202/#1095) directly would silently render "no real issues" instead of that Epic's own real work — exactly the gap #2776 flags. Adding `selfRootEpicNumber` through `BuildSeries`, threading `epicNumber` through `GetEpicSeriesAsync`, adding `GetOpenEpicsAsync` (all open Epics incl. #1202/#1095, each with its own real open-work count for default selection), then wiring a new Home dashboard Epic-picker card that reuses `RenderBurndown`/`DrawBurndownCanvas` (now parameterized by target UI elements instead of hardcoded to the milestone-wide card's named elements).
- 2026-09-04T00:40:00Z ✅ DONE — implemented all 4 points:
  1. `BuildSeries`/`GetEpicSeriesAsync` now take/pass `selfRootEpicNumber` so a per-Epic scope shows that Epic's own real work, NOT the internal-tooling exclusion (per the issue's explicit instruction, distinct from `GetMilestoneSeriesAsync`'s cross-scope aggregation which is unchanged).
  2. New `GetOpenEpicsAsync` (all real open Epics, deliberately including #1202/#1095) + new Home "EPIC BURNDOWN" card with a real `EpicPicker` ComboBox, reusing `RenderBurndown`/`DrawBurndownCanvas` (generalized to take target UI elements as parameters instead of being hardcoded to the milestone card's named elements — one shared rendering path, not a duplicate).
  3. Default selection: the open Epic with the most real open work right now (`EpicOption.OpenRealWork`, self-rollup applied) — no "last active in Focus Mode" state is persisted anywhere in this codebase to read instead, so this was the honest computable default; judgement call, documented here per the issue's ask.
  4. Same fail-closed discipline as the existing burndown card: `ShowBurndownMessage`/`RenderBurndown`'s `HasEnoughData` gate is shared code, so an Epic with too little history renders the same honest empty state, never a fabricated curve.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0 errors. Real UI/manifest run not applicable — BuildConsole (desktop/BuildConsole) is explicitly out of scope for test manifests per CLAUDE.md; a clean build is this session's real verification. Manual interactive verification of the #1202 selection specifically (per the issue's "Real verification" step) was not run live in this session — no WebView2/live BuildConsole instance to click through non-interactively without violating the "never launch BuildConsole.exe to verify" rule; the code path was traced by hand instead (`GetEpicSeriesAsync(1202)` → `selfRootEpicNumber: 1202` → `CountsAsRealWork(i, byNumber, 1202)` → #1202's own descendants no longer excluded).
  - `git status --porcelain` clean, branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing DONE.
  - No new findings filed this session — the audit in the issue body was accurate and the fix was a direct implementation of its 4-point plan.
