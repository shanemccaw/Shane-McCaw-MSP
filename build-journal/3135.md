# #3135 — Feature: Meds (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:26:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** 239641e8a

## Log
- 2026-09-08T00:20:00Z ⏳ IN FLIGHT — read contract.md/README/Meds .dc.html; building real
  `medications`/`med_batch_log` migration, batch-grouped Meds UI (single swipe-to-complete),
  auto-refill vs manual-watch tiers, and MCP tools for capture-grammar batch completion.
- 2026-09-08T00:26:00Z ✅ DONE — shipped in 239641e8a: `migrations/026_medications.sql`
  (`medications` per-med record + `med_batch_log`, one real row per user/batch/day, matching
  the design's "single swipe per batch, not per-pill"), `src/core/medications.mjs`, REST routes
  (GET/POST /api/medications, PATCH/DELETE /api/medications/:id, POST
  /api/medications/:id/ordered, POST/DELETE /api/medications/batches/:batch/take), four MCP
  tools (get_medications, set_medication, mark_med_batch_taken -- the real "took my morning
  meds" capture-grammar entry point -- and mark_medication_ordered), and a new Meds tab in the
  web UI with a real pointer-events drag-to-complete slider per batch and a Refills section
  split into Needs you (manual-watch) / Handled automatically (auto-refill).

  Verified for real: migration applied against the local `shanes_life` Postgres database;
  exercised the core module directly (create under both refill tiers, batch take/undo,
  idempotent double-take, "Ordered it" date math); exercised the REST API end-to-end with a
  real minted session cookie; exercised all four MCP tools over the real `/mcp` JSON-RPC
  endpoint with a real minted token; ran `bin/check.mjs` post-merge -- 123/123 existing checks
  still pass, "not one ShanesSurvival row moved." All test rows/tokens/sessions created during
  verification were cleaned up against the throwaway `you@example.com` test account, never
  against Shane's real account.

  Rebased twice onto `origin/main` during the session (two concurrent Recipes/#3125 pushes
  landed mid-build) -- one real merge conflict in `public/app.js` (both #3125's Cook mode and
  this Meds screen inserted at the same point after `viewRecipes`), resolved by hand, re-tested
  after resolution, no logic lost from either side. Branch verified merged into `origin/main`
  via `scripts/dev-server/verify-branch-merged.mjs` before writing this DONE. Working tree
  clean at session end.

  Scope item 5 (pets' feeding/meds reusing this same system) is intentionally NOT wired in this
  build -- `pet_care.batch` (migration 015) already anticipates it and the join is trivial, but
  Pets has no core module/routes/UI yet. Left as a note for whoever builds Pets, per the issue's
  own instruction not to duplicate.
