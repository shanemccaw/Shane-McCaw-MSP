# Git #2156 — Claude terminal as document tab

- **Status:** ✅ DONE 2026-09-01T00:00:00Z
- **Scope:** buildconsole

## Log
- 2026-09-01T00:00:00Z — Session started. Task: remove Claude TabItem from bottom panel, add "Open Claude Terminal" menu entry under _Terminal menu that opens ClaudeTerminalView as a full Document tab, reusing MainWindow.GitDetailTabs.cs's document-tab pattern (focus-existing-or-open de-dup). ConPTY/xterm.js session logic unchanged.
- 2026-09-01T00:00:00Z — ✅ DONE — Audited `MainWindow.GitDetailTabs.cs` in full first. Removed the
  "Claude" `TabItem` from the bottom panel (`MainWindow.xaml`). Added "Open Claude Terminal"
  under the existing `_Terminal` menu. Added `MainWindow.ClaudeTerminalTab.cs`: a single
  lazily-created `ClaudeTerminalView` field, opened/focused via the same
  `FocusExistingGitDetailTab` dedup helper `MainWindow.GitDetailTabs.cs` already uses (reused
  directly, not re-implemented), added to `EditorTabs` as a real document tab via an
  `AddGitDetailTab`-equivalent helper (`AddClaudeTerminalTab`). `ClaudeTerminalView`'s
  ConPTY/xterm.js session logic (`Controls/ClaudeTerminalView.xaml.cs`) was not touched —
  hosting/placement change only. `dotnet build --configuration Release` succeeds, 0
  warnings/errors.
  Commits: `fdff6c2aa` (feature), `29fbd4bed` (bookend IN FLIGHT). Merged to `origin/main` via
  fast-forward push (`e03c93d36..fdff6c2aa`); `verify-branch-merged.mjs` confirms OK.
  `git status --porcelain` clean.
  **Live UI click-through was NOT self-verified in this session** — an attempt to launch the
  built exe and drive its menu via UIAutomation/SendKeys was stopped by Shane mid-attempt
  ("stop touching my current build console"), because BuildConsole's single-instance mutex can
  courier a "new" launch straight into his real running instance rather than an isolated copy
  (see memory `never-launch-buildconsole-exe-for-verification.md`, added this session so future
  sessions don't repeat this). Shane said he'll verify manually. Real audit + build + wiring
  review stand as this session's verification; the interactive click-through/de-dup/session-
  persistence check is Shane's to confirm.
