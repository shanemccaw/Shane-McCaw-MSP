# #3241 — Feature: Wins — pull out of Money into its own house-grid room

- **Status:** ✅ DONE 2026-09-08T12:30:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** e5ae5a51a

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — pulling Wins out of Money's tab switcher into its own
  top-level house-grid room: new ROOM_DEFS entry (route #/wins, existing "wins" critter slot
  1n/c-wins already built for this, new r-wins furniture symbol), new roomsForToday() Wins
  subtitle, ROOM_KEYS/DEFAULT_ORDER update in room-order.mjs, new top-level viewWins(view) route,
  drop Wins from MONEY_TABS (five tabs remain: Now/Bills/Accounts/Cars/Vault — Banks/Bankruptcy
  stay as-is, unaffected by this issue). No data/schema change — wins table, createWin/listWins
  untouched per the issue's own scope.
- 2026-09-08T12:30:00Z ✅ DONE — e5ae5a51a. Wins is now its own real house-grid room: new
  ROOM_DEFS entry (public/app.js), new r-wins mantel-shelf furniture symbol
  (public/critters-sprite.svg, no trophy/badge imagery), ROOM_KEYS/DEFAULT_ORDER extended
  (src/core/room-order.mjs), and a real Wins subtitle in roomsForToday() (src/routes/api.mjs) —
  lit 3 days after the most recent real win with that win's own text, fading dark after, or
  "Nothing logged yet". viewMoneyWins() promoted to a top-level viewWins(view) with its own page
  header; Wins dropped out of MONEY_TABS. Underlying data (wins table, createWin/listWins,
  detectMoneyWins, GET/POST /api/money/wins) is untouched, exactly per the issue's stated scope.
  Verified: node --check on all touched files; sprite symbol tags balanced (75/75) with r-wins
  present; booted the real server against the real local `finances` Postgres (junctioned
  node_modules + a copied .env, both removed after) and ran bin/check.mjs end-to-end — 208/213
  passed, the 5 failures pre-existing/unrelated (webhook-origin + no SL_VAULT_KEY in the test
  env). Called roomsForToday() directly for both real users — confirmed the empty-state subtitle,
  then inserted a real temporary win row, confirmed lit=true with its real text as subtitle, then
  deleted it (wins table back to 0 rows, no residue). Branch verified merged into origin/main via
  scripts/dev-server/verify-branch-merged.mjs; working tree clean.
