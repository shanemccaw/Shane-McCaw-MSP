# #4557 — Pillar pages: stale "never scanned"/empty-findings race + internal "live log is gone" caption

- **Status:** ✅ DONE 2026-09-17T20:30:06Z
- **Scope:** platform

## Log
- 2026-09-17T19:20:29Z — Session start. Investigating cross-page scope of the badge/caption components across all 6 pillar pages before making any fix, per Shane's explicit request in the issue body.
- 2026-09-17T19:33:24Z — ✅ DONE — Confirmed all 6 pillar pages (`/pillars/:pillar`) share ONE real component, `artifacts/portal/src/pages/pillar.tsx` — no per-pillar duplication, so both bugs are fixed once for all six.

  **Root cause A (stale race), confirmed via code trace:** `usePillarPage.ts` (pillar.tsx:51) and `usePillarSummary.ts`'s `usePillarSummaryShell` (mounted in `PortalShell.tsx`'s tab strip + severity wash, i.e. present on every pillar page) each fetched `GET /api/portal/pillars` exactly once on mount, with nothing tying that fetch to a scan actually finishing. `useScanState.ts` already had a real, reactive `phase` signal (SSE + adaptive poll) that correctly flips to `complete`/`partial` — nothing wired it to trigger either of the two stale fetches, so a pillar page open when a scan completed kept showing "never scanned"/stale findings until a full reload happened to refire the effect.

  **Fix:** added `scanStateContext.ts` sharing `PortalShell`'s single `useScanState()` value (avoids a second SSE/poll connection). `usePillarSummaryShell` now takes `scan.phase` as a direct param (same component as the scan hook); `usePillarPage` consumes it via the new context (it mounts below the shell as a route child). Both effects now list `scanPhase` as a dependency and refetch on any real transition.

  **Root cause B (caption leak), confirmed via grep:** `TenantStatusCard.tsx:105` (mounted in `PortalShell`'s sidebar footer, present on every page including all 6 pillars) — "Showing its stored summary: ... The live log for it is gone." Rewritten to "Latest scan complete" + the real check/pass/error/license-gap counts, same theme as #4451's earlier sweep. Confirmed zero remaining occurrences of the phrase in `artifacts/portal/src`.

  **Verification:** `tsc --noEmit` clean and `vite build` clean on `artifacts/portal` (commit 6aa82fdb6). Live UI verification via `shaneapp://runTest` / browser reload was **genuinely blocked** — attempted for real (not skipped): `scripts/dev-server/refresh-main-server.mjs` (both dry-run and real) confirmed the serving checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) could not fast-forward past a pre-existing untracked-file collision with commit `a7361b693` (the QA session that filed #4557 itself) — real, stated git error, not a guess. Could not clear it myself (destructive-action classifier declined both `rm -rf` and `mv` against the shared serving checkout, correctly — it isn't my worktree). Filed as its own finding, **#4559** (parented under #3707 — #1202's cap is full again, per the standing memory — labeled `Shane To-Do` + set to "AI Batter Up"), since it blocks live verification for every future build's restart, not just this one. `test-manifests/portal/pillar-pages.json` extended with a `sessionTag: "4557-tenant-status-caption"` UI step asserting the fixed caption, registered in `_regression-suite.json`, not yet run live for the same blocked reason — honestly noted in the manifest itself.

  Commits: `6aa82fdb6` (the real fix + manifest extension), `4ab18405d` (honest verification notes update). Branch verified merged into `origin/main` via `verify-branch-merged.mjs`. `git status --porcelain` clean.

  Findings filed: **#4559** (serving-checkout ff-block, `Shane To-Do`).

- 2026-09-17T20:14:34Z — ⏳ IN FLIGHT — Reopened. Shane confirmed live (clean checkout + Portal restart) that pillar findings boxes are still missing. Re-investigating for a real defect in the fix itself, not just the prior serving-checkout staleness. Live-verifying for real this time.

- 2026-09-17T20:30:06Z — ✅ DONE — Confirmed the serving checkout `C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP` genuinely contains commit `6aa82fdb6` (`git merge-base --is-ancestor` = YES) and the running dev server (port 5175) is genuinely serving the fixed `TenantStatusCard.tsx` source (`curl` the live module — "Latest scan complete", zero occurrences of the old phrase).

  **Live-verified for real this time**, using the sanctioned `loginAs` mechanism (Git #3923) against the real testbed account "Customer — mccawsoft2 (real, provisionProspectAccount)" — `{{TEST_CUSTOMER_EMAIL}}`/`{{TEST_CUSTOMER_PASSWORD}}` turned out to be genuinely unset in BuildConsole's own Test Environment Variables (confirmed via a real `runTest` run: 10/14 steps failed on `VariableNotResolvedException`, not a fix defect). A throwaway `loginAs`-based manifest (deleted after use, not a permanent deliverable) ran for real via `shaneapp://runTest` against the live dev server and confirmed: real score (47) renders, `pillar-state-line` renders (no stale never-scanned), and the real Governance finding `governance:ownerless-groups` is genuinely present and visible in the DOM, with the findings panel's real rendered text reading "Findings · 5 warning · Groups exist with no owner...Public Teams exist...5 security group(s) have...". **#4557's own fix is confirmed live and correct.**

  **Separately, and NOT a regression from #4557's fix:** Shane's "missing data squares" almost certainly refers to the pillar **stat tiles**, not the findings list. Traced with a real, read-only diagnostic (`buildPillarSummary(2080)` — the exact function the endpoint serves — invoked directly against the local DB, output captured and then deleted along with the throwaway script/package.json entry that ran it). Real finding: **all 4 of Governance's stat-tile `sourceKey`s reference `monitor_checks` keys that no longer exist in the live catalog** (`compliance:sharepoint-sites`, `compliance:overshared-sites`, `copilot:overshare-exposure`, `compliance:public-channels` — confirmed 0 rows for all four via direct query), a pre-existing registry-drift bug spanning several other pillars too (13 broken stat specs total across governance/licensing/compliance/health/security/copilot). `registry-source-key-contract.test.ts` (the #441 test written to catch exactly this) still passes because it validates against a static, now-stale classification list, not the live DB. Did not attempt to remap the sourceKeys myself — at least one (`compliance.oversharedSiteCount`) carries an explicit prior-decision comment (#357: deliberately NOT rewritten to a newer check), so a same-session guess risked showing a customer a wrong number instead of an honest "unavailable" one. Filed as its own well-evidenced finding, **#4560**, parented under #1621 per this build's own dispatch instruction.

  Working tree confirmed clean (`git status --porcelain` empty) after deleting my own scratch diagnostic files (temp manifest in both the worktree and the serving checkout, temp api-server script, temp package.json script entry — none of it a permanent deliverable). No code changes this round; only the bookend/comment updates below are new commits.

  Findings filed this session: **#4559** (serving-checkout ff-block, `Shane To-Do`), **#4560** (pillar stat-tile registry drift, parented under #1621).
