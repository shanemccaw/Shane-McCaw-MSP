# #4040 — Break-glass admin-override mutual exclusion (claim before reset)

- **Status:** ✅ DONE 2026-09-14T14:48:11Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** e10edaba5 (fix + tests + docs + migration)
- **Migration:** lib/db/migrations/manual/4040-break-glass-override-claim.sql (additive; run on local DB; listed on #1630)
- **Findings filed:** none

## Log
- 2026-09-14T17:05:00Z ⏳ IN FLIGHT — claiming the pending secret row with a conditional UPDATE ... WHERE status='pending_delivery' RETURNING before the tenant password reset, releasing on refusal; live-reproduce with two concurrent overrides on the zz-test synthetic user.
- 2026-09-14T14:48:11Z ✅ DONE — Live-reproduced first on the UNFIXED code: a new concurrent-pair case in break-glass-admin-override.live-verify.ts (both contexts resolved first, then two performBreakGlassAdminOverride calls under Promise.all, real Postgres + real Graph PATCH on zz-test-graphwrite-01, in-process store stub because the only reachable vault is production per #1913) returned ok:true for BOTH (new pending rows 10 and 11) — both reset the tenant. Fix: performBreakGlassAdminOverride now claims the row (UPDATE SET status='reset_in_progress', reset_claim_token, reset_claimed_at=now() WHERE id AND (status='pending_delivery' OR stale reset_in_progress >15m) RETURNING id) before the attempts check, store, or Graph. Zero rows → 409 (detail override_in_progress when another override holds it). Every refusal (live links, no account id, store unconfigured/throws, definite Graph refusal, gate error, outcome unknown, replacement unrecorded) hands the claim back, token-conditional. Supersede in the record tx is conditional on the claim token; a lost (stale-taken-over) claim rolls back as 500 claim_lost, vault copy kept. Terminal-state purge and MSP watchlist treat reset_in_progress as undelivered; MSP console gets a status tone and keeps the reset form for a stale claim. Verification: unit suite 16/16 (10 updated #4029 branch-order cases + 6 new #4040); api-server tsc exit 0; msp-console tsc exit 0; live harness AFTER fix 8/8 twice (one run lost to a vitest worker start timeout while tsc ran in parallel — no tests executed, no Graph writes; one later run killed mid-flight left one synthetic def/run/secret, deleted by hand). Concurrent case asserts exactly one ok, one 409, Graph writes +1, store calls +1, one pending_delivery row (the winner's), old row superseded_by_reset, no reset_in_progress left, one audit row, lastPasswordChangeDateTime moved; log shows "admin-override refused — another override holds this secret (tenant credential not changed)". Cleanup: 0 break_glass_pending_secrets, 0 zz-test defs, 0 zz-test audit rows. Honest gaps: concurrency was two concurrent in-process calls on separate pool connections, not two HTTP requests — an HTTP pair cannot reach success locally without the production vault; a real vault was not exercised. Contract packs (portal §2.6/§3.1/§5, MSP console §1.1/§1.2/§1.4/§2) updated. Tree was clean on arrival.
