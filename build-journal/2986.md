# #2986 — api-server node_modules/vitest symlink missing (regression, was #2516)

- **Status:** ✅ DONE 2026-09-07T15:35:00Z
- **Scope:** platform (dev-server tooling)
- **Started:** 2026-09-07
- **Commit(s):** b892b1fd6

## Log
- 2026-09-07T15:10:00Z ⏳ IN FLIGHT — read #2986's body + comment. Confirmed current
  main-checkout state: `artifacts/api-server/node_modules/vitest` IS present and
  healthy right now (real symlink into `node_modules/.pnpm/vitest@4.1.9_.../vitest`),
  and `npx vitest run src/lib/graph-write-permissions.test.ts` passes (87/87). So the
  immediate symptom is not currently reproducing — investigating why it recurred
  after #2973's `pnpm install` fix, per the dispatch's explicit root-cause ask.
- 2026-09-07T15:30:00Z ✅ DONE — Real root cause found in `.logs/dev-server/cleanups.log`:
  the shared store poisoning that produced #2986's symptom (foreign=670, dangling=393
  entries) happened at 2026-09-06T14:36:52.634Z — **inside** the window #2986 was filed
  in (14:34:29Z) — via `removeWorktreeSafe` (worktree-lifecycle.mjs) sweeping worktree
  `2950-q1726`, and that was **before** the Git #1980 store-doctor auto-repair canary
  landed (commit `f59d77f76`, 2026-09-06T15:46:51-04:00 = 19:46:51Z). So #1980 itself
  already explains and fixes the poisoning window that produced this exact instance —
  confirmed live: `artifacts/api-server/node_modules/vitest` is healthy right now and
  the full vitest suite for `graph-write-permissions.test.ts` passes (87/87).
  BUT: grepping every "removed" log entry in cleanups.log SINCE #1980 landed shows
  **zero** further entries from `removeWorktreeSafe` at all — every removal since has
  come through a second, separate code path, `sweepStrayWorktreeDirs`
  (git-maintenance.mjs, action `git-maintenance:stray-dirs`), which duplicates the
  exact same junction-unlink + `rmSync` removal logic but **never got the #1980
  canary** — it only ever logged `removedCount`/`retainedCount`, no `storeAfterRemoval`
  at all. So the store-poisoning class #2986 reports is NOT fully closed by #1980;
  #1980 fixed one of two call sites and left this one silently unprotected — the real,
  distinct gap the dispatch asked me to confirm or rule out.
  **Fix (commit `b892b1fd6`):** wired `scanSharedStore` + `repairSharedStore` into
  `sweepStrayWorktreeDirs` the same way `removeWorktreeSafe` already has them, logging
  `storeAfterRemoval` (with `autoRepair`) into `cleanups.log` for this path too. Added
  a focused self-test, `scripts/dev-server/git-maintenance-straydir-autorepair.selftest.mjs`,
  that plants a foreign junction in a throwaway repo, sweeps a stray dir, and asserts
  the store is scanned, auto-repaired, and confirmed clean afterward — mirroring the
  existing `store-doctor-autorepair.selftest.mjs` for the other call site. Both
  selftests pass (11/11 and 8/8 assertions respectively). Re-ran `store-doctor.mjs
  --json` against the real main checkout: `"clean": true`. No fixture/hardcoded data
  touched. `verify-branch-merged.mjs`-equivalent check
  (`git merge-base --is-ancestor HEAD origin/main`) confirms `b892b1fd6` is on
  `origin/main`. `git status --porcelain` clean in both this worktree and the
  additional main-checkout working directory.
  No new sub-issue filed — this is a direct, complete fix for #2986's own root cause,
  not a separate finding.
