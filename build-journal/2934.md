# #2934 — Dashboard hero number — lead with pay-period verdict, not flat monthly shortfall

- **Status:** ✅ DONE 2026-09-04T21:15:00Z
- **Scope:** platform (desktop/ShanesSurvival)

## Log
- 2026-09-04T00:00:00Z — Started. Blocked-by #2924 already landed (86e83c19a), so no merge
  collision risk remains. Promoting #2918's pay_period_forecast Cycle 1 verdict to the primary
  hero in DashboardWindow, demoting gate_status's flat shortfall to honestly-labeled supporting
  text below it.
- 2026-09-04T21:15:00Z — ✅ DONE — commit `2ceeb574e` ("Dashboard hero: lead with Cycle 1
  pay-period verdict, demote flat shortfall (#2934)"), pushed straight to `main`
  (`4ad3bb05f..2ceeb574e`). `DashboardWindow.xaml.cs`: `RenderHero` now drives the large hero
  text/border from `PayPeriodForecastResult.Cycle1`'s covered/short verdict (e.g. "SHORT
  $372.50 before Sep 11"), with the old flat gate_status shortfall demoted to smaller supporting
  text below it, honestly labeled ("$X across every bill if it all came due today"). Falls back
  to the flat gate_status verdict as the hero (labeled as a fallback) only when Cycle 1 genuinely
  can't be computed. Critical Debts strip left untouched, still directly below the hero.
  `dotnet build ShanesSurvival.sln` clean (0 warnings, 0 errors), both before and after rebase
  onto `origin/main`. Real screenshot verification: launched the built app, used UI Automation
  to click "Open Dashboard", captured the live window against the real local Postgres DB +
  real Plaid data — confirmed the Cycle 1 verdict ("SHORT $372.50 before Sep 11") is now the
  single largest, most prominent element on screen, the flat shortfall figure ($7,933.70) is
  still present and accurate as smaller supporting text, and Critical Debts is unchanged in
  position. `git merge-base --is-ancestor HEAD origin/main` confirmed clean; `git status
  --porcelain` clean.
