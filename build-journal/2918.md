# #2918 — pay_period_forecast — two-cycle covered/short view (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T19:10:00Z
- **Scope:** platform (desktop/ShanesSurvival)
- **Started:** 2026-09-04
- **Commit(s):** d503d74dfac7bd5aa6c8cab44a98d5f195995b59

## Log
- 2026-09-04T18:40:00Z ⏳ IN FLIGHT — investigated PayPeriodDueService (#2912), gate_status
  (DashboardService/#2909), and IncomeRepository (#2905). Building new `pay_period_forecast`
  MCP tool: no params, pulls next_pay_date/pay_frequency_days from active income sources,
  computes Cycle 1 (today→next_pay_date, compared vs gate_status available funds) and Cycle 2
  (next_pay_date→next_pay_date+pay_frequency_days, compared vs sum of expected_per_cycle),
  reusing PayPeriodDueService for due-window math in both cycles. Honest limitation: no active
  income source with pay_frequency_days set → clear message, no guessing.
- 2026-09-04T19:10:00Z ✅ DONE — added `IncomeRepository.ListActiveAsync` (deterministic
  "primary source" pick: earliest real next_pay_date among active sources with both
  pay_frequency_days and next_pay_date set — no is_primary column exists), new
  `PayPeriodForecastService`/`PayPeriodForecastModels` reusing `PayPeriodDueService` for both
  cycles' due-window math and `DashboardService` for Cycle 1's real available-funds figure, and
  wired the `pay_period_forecast` MCP tool (no params) in FinanceTools.cs. Cycle 2's covered/
  short compares against the real summed `expected_per_cycle` across every active income
  source that has it set. `dotnet build ShanesSurvival.sln` clean (0 errors/warnings). Real
  end-to-end verification against Shane's live `finances` Postgres DB: primary source resolved
  to "NASA Salary" (next_pay_date 2026-09-11); Cycle 1 (2026-09-04→2026-09-11) TotalDue=$1868.51
  vs. Available=$2489.01 → Covered $620.50, cross-checked to match a manual
  `pay_period_due_status`+`gate_status` call exactly (both `matches=True`); Cycle 2
  (2026-09-11→2026-09-25, computed as next_pay_date + pay_frequency_days=14) TotalDue=$4215.56
  vs. expected_per_cycle=$2999.35 → Short $1216.21. Verification script was a throwaway scratch
  project outside the repo (`/tmp/pp_verify`), deleted before finishing — not committed.
  `git status --porcelain` clean. Branch verified merged into `origin/main` via
  `verify-branch-merged.mjs` before writing this DONE (commit pushed directly to main per
  worktree-isolation convention).
