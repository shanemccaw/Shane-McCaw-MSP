# #2203 — Command Center right panel per-category (ShaneBuilder)

- **Status:** ✅ DONE 2026-09-02T16:05:00Z
- **Scope:** buildconsole (ShaneBuilder app shell)
- **Started:** 2026-09-02
- **Commit(s):** 747530521 (feature), edb7eabb1 (IN FLIGHT bookend), eb521d4f0 + c91e6d8ab
  (merges resolving a real `BuildQueueRow` type-name collision with concurrently-landed #2201),
  merged to `main` at c91e6d8ab

## Log
- 2026-09-02T15:20:00Z ⏳ IN FLIGHT — real-audited #2176 (confirmed DONE, but scoped only to a
  status-bar connectivity check — no per-category real data was wired). Surveyed existing real
  data sources to build on instead of inventing: `GitMapService` (real epics/features via `gh`),
  `GitHubReadClient`/`gh` (issues), `QueueReadClient` (real `bt_build_queue` rows over local
  Postgres), `SqlRunnerService` (real SQL execution), `TerminalSession` (real child-process
  shell), `.logs/dev-all/*.meta.json` (real per-service status written by `scripts/dev-all.mjs`),
  and the real per-build stdout log at `%TEMP%\bt-build-queue-logs\queue-<id>.log`. Building the
  per-category right pane on top of these — no fixture data for any new surface.
- 2026-09-02T16:05:00Z ✅ DONE — Every Ctrl+K category now renders its own real right pane:
  Git Epics (real epics + "contains" tiles, `GitMapService` extended with milestone), Git Issues
  (real open issues + real parent-epic via a batched GitHub GraphQL lookup, new
  `GitIssuesService.cs`), Builds & Build IDs (real `bt_build_queue` rows, `QueueReadClient`
  extended with `GetRecentBuildsAsync`; real last-10 stdout lines off the real per-build log,
  new `BuildLogTailReader.cs`; best-effort real step/% when a progress snapshot exists, honest
  coarse status otherwise), Claude & URLs (real epic tie + real cross-referenced builds; context
  meter is an honest "not available" note, not a guess), Services (real status/port/pid off
  `scripts/dev-all.mjs`'s own `.meta.json`, real Start/Stop, new `DevServicesReadClient.cs`,
  ported from BuildConsole's frozen `DevServicesManager.cs`), Terminal (a real dedicated
  `TerminalSession`, the search box IS the prompt), SQL (real `SqlRunnerService` execution +
  real on-disk `.sql` files, new `RepoSqlFileScanner.cs`). Terminal/SQL are exempt from the
  palette's text filter; the right pane is now a real scroll region (bounded grid) instead of a
  fixed-height `StackPanel`.
  Ran a multi-agent code review on the diff before finalizing — it found and I fixed two real
  bugs: (1) `RenderPaletteTerminalPane`/`RenderPaletteSqlPane` never cleared
  `CommandPalettePreview.Children`, so the right pane stacked duplicate content on every
  keystroke/session update; (2) the old `_queueItems` fixture rows were still being added to the
  "Builds" category alongside the new real `_paletteBuilds` rows — removed, "Builds"/"Build IDs"
  are real-only now. Also fixed: a Terminal/SQL quick-open row selected from the "All" category
  was a no-op (now switches into that category instead of silently doing nothing), a
  `DevServicesReadClient.KnownServices` concurrent-dictionary-mutation hazard across overlapping
  `GetAllAsync` calls (now snapshots before its async loop), `BuildLogTailReader.TailLines`
  buffering the whole stdout file just to keep 10 lines (now seeks backward in bounded chunks),
  and `QueueReadClient.GetRecentBuildsAsync` lacking the fail-closed try/catch its sibling
  services all have.
  Filed 2 real findings: #2236 (`report-progress.mjs`'s local progress snapshot is
  worktree-relative, so a separate cross-process reader can't see it for isolated-worktree
  builds — discovered empirically this session) and #2237 (no real data source for the Claude &
  URLs context meter or the Services "Open in tab" button — both left as honest gaps rather than
  fabricated). Both parented to #2198, board status set to "AI Batter Up".
  Merging hit a real, concurrent type-name collision: #2201 (Alerts and Critters, landed on
  `main` while this was in flight) added its own `BuildQueueRow` record to the same
  `QueueReadClient.cs` file for its own purpose (the Alerts/Critters build-runner watcher).
  Resolved by renaming this issue's type to `PaletteBuildQueueRow` and keeping both real,
  distinct read paths — #2201's `BuildQueueRow`/`GetRecentAsync()` and this issue's
  `PaletteBuildQueueRow`/`GetRecentBuildsAsync()` — side by side in the same file.
  `dotnet build ShaneBuilder.csproj` succeeds with 0 warnings/errors, including after the merge
  that brought in #2201/#2220/#2233/#2234's concurrent work. `verify-branch-merged.mjs` confirms
  `agent/2203-q1179` is a real ancestor of `origin/main`. `git status --porcelain` clean.
  Not live-clicked through in a running WPF session this build (no interactive
  display/WebView2-safe launch path in this environment, and BuildConsole.exe's own
  single-instance-mutex caution applies the same way to ShaneBuilder.exe if Shane has a live
  session running) — verified instead by: successful build, every real data path independently
  confirmed against live `gh`/GraphQL/Postgres output matching this code's own parsing shapes
  exactly, and a 4-agent code review pass whose findings were fixed and re-verified.
