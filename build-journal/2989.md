# #2989 — Change Control: regenerate the contract pack

- **Status:** ✅ DONE 2026-09-06T16:05:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 109ee3fab (IN FLIGHT bookend), 0cd63663d (regenerated contract pack)

## Log
- 2026-09-06T15:20:00Z ⏳ IN FLIGHT — pulled #2989, #1486, #1715, and the pre-deletion pack
  (`020d46dcb`) for context. Starting full line-by-line audit of the real, current Change
  Control backend (routes, lib, schema, workflow) to regenerate
  `docs/change-control-contract-pack.md` per the #1642 pattern — not a restoration of the
  deleted file.
- 2026-09-06T16:05:00Z ✅ DONE — Ran a full line-by-line re-extraction of the real, current
  Change Control backend via 6 parallel research passes (register+lib, approvals/rejection/
  write-gate, catalog/freeze/maintenance, execution/CAB/PIR/collision/dependencies/timeline/
  metrics, schema, client seam + drift bug), then wrote
  `docs/change-control-contract-pack.md` from scratch against that evidence (not a restoration
  of the deleted 8/29 file). Confirmed via `psql` against the local DB and direct file reads
  that the module grew from the old pack's 2 routes to 14 real customer-portal routes plus a
  3-route customer settings surface; almost every DECIDED row in the old pack (#1496-#1506,
  #1554, #1555, #1759, #1761, #1762) is now CURRENT. Corrected two stale premises along the
  way: the customer portal package is `artifacts/portal`, not `artifacts/msp-portal` (doesn't
  exist), and this issue's own claim of "no wire code exists yet" is only true for the
  register/dashboard — `settingsChangeControlWire.ts`/`settingsChangeControlLive.ts` already
  exist (unconsumed by any page, no fixture fallback). Also confirmed the old pack's "live bug"
  (drift-collector `crRef` never populated) has its format half fixed (#1505,
  `formatChangeRequestCode` now used) though the blanket Conditional-Access-only attribution
  limitation is unchanged by design.

  Filed 3 real, code-confirmed findings as new sub-issues of #1486 (Feature parent), labeled
  `bug`, milestone v1.1, board status "AI Batter Up":
  - #3044 — standard-catalog `execute` skips freeze/maintenance/collision enforcement that the
    ordinary CR-raise path runs.
  - #3045 — change metrics still compute success/failure without `cr_pirs.closeCode`; the
    file's own header comment falsely claims #1502 is still open.
  - #3046 — `escalateBreachedApprovals()` is real and unit-tested but has zero live callers —
    approval SLA breaches are computed but never escalated.

  Verification: `node scripts/dev-server/verify-branch-merged.mjs` — OK, `0cd63663d` merged
  into `origin/main`. `git status --porcelain` clean. No fixture/hardcoded data introduced —
  this build only wrote a documentation file plus 3 GitHub issues, no product code changed.
