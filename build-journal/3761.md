# #3761 — Direct MSP-side Change Control approval endpoint (no CAB required)

- **Status:** ✅ DONE 2026-09-13T01:12:50Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** f82e5a116 (rebased/pushed as 020089429 on main)

## Log
- 2026-09-12T23:47:06Z ⏳ IN FLIGHT — Read #3761 body/comments (resolved by Shane: CAB
  is optional per-tier, but the MSP technical-review approval stage must still be
  decidable regardless). Read `recordApproval` (`portal-change-approvals-store.ts:256`),
  the customer-side caller (`portal-change-control.ts:1144`), and the existing CAB-only
  msp-role decision path (`portal-cab-store.ts` `recordAgendaDecision` /
  `msp-change-control-cab.ts`). Confirmed no separate CAB tier/entitlement gate exists
  today — `msp-change-control-cab.ts` only requires `ladder.msp-operator`, so the real
  gap is a missing direct endpoint, not a tier branch. Added
  `POST /api/msp/change-requests/:id/approve` in `msp-changes.ts`, reusing
  `recordApproval` exactly like the customer route and the #3033 reject branch already
  in this file reuse `recordRejection`. Added `isOnOpenCabAgenda` guard
  (`portal-cab-store.ts`) so a change already parked on an open CAB agenda item can't
  be double-decided through this new direct path. Reject already worked via the
  existing generic `PATCH /msp/change-requests/:id` `status: "rejected"` branch (#3033)
  — no reject gap found. Next: wire the msp-console UI's "Decide via Advisory Board"
  button to call the new endpoint directly when the change is not on an open agenda.
- 2026-09-13T01:12:50Z ✅ DONE — Wired `useApproveChangeRequest` (`api.ts`) and the
  Register.tsx detail drawer's pending `msp`-role stage row to real Approve/Reject
  buttons; falls back to "Decide via Advisory Board" only on a 409 from the new
  `isOnOpenCabAgenda` guard. Added a live-Postgres regression test
  (`msp-changes-approve.live-db.test.ts`, 3 cases: direct approval of a
  freeze-exception msp stage — the one real production of a pending `msp`-role
  stage today, via `recordFreezeException`; 409 when already on an open CAB
  agenda; 409 via `recordApproval`'s own no-pending-stage guard) — all passing
  against the real local database. Re-ran the existing reject/CAB live-db suites
  (9 cases) with no regressions. `api-server` and `msp-console` both typecheck
  clean. No fixture/hardcoded data — every value flows through `recordApproval`
  and the real `cr_approvals` ledger. Committed as f82e5a116, rebased twice onto
  a moving `origin/main` and pushed as 020089429; `verify-branch-merged.mjs`
  confirms the branch is an ancestor of `origin/main`. `git status --porcelain`
  clean. Posted a comment on #3761 summarizing this. No genuine new findings
  filed — the CAB tier-gating question the issue asked about turned out to have
  no existing gate to reuse (confirmed: `msp-change-control-cab.ts` requires only
  `ladder.msp-operator`, no tier/entitlement check), so the fix is the direct
  endpoint itself, not a tier branch.
