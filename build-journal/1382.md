# #1382 — Remediation Tracker: gate headline/pillar/gate numbers behind honest no-scan-data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-28
- **Commit(s):** 30cf7cb2f (bookend), 372191dad (fix) — rebased+pushed as 3bcad06df on main

## Log
- 2026-08-28 ⏳ IN FLIGHT — pulled #1382 + #1381; found #1381's landed session already did the full gating
  (rtHeadline/rtPillarCells/rtGate all read the real per-pillar score API via
  useRemediationPillarScores/RtLiveScores, honest-null throughout, per remediationModel.ts/remediationScores.ts).
  Removing the now-dead RT_PILLAR_BASE/RT_PILLAR_TARGET fixture constants left behind in remediationData.ts,
  then verifying tests/typecheck before closing this out.
- 2026-08-28 ✅ DONE — Confirmed #1382's ask (gate headline/pillar-cell/gate numbers behind honest no-scan-data)
  was already fully implemented by #1381's session: `rtHeadline`/`rtPillarCells`/`rtGate` in remediationModel.ts
  all read `RtLiveScores` (remediationScores.ts) via `useRemediationPillarScores`, with `insufficient_data` /
  `single_scan` / `scored` status honestly rendered ("—" / "not enough data yet" / "not evaluated yet") for
  every case the real API can't produce a number for. No fixture fallback remains in any consumer. Only real
  change this session: removed the now-fully-dead `RT_PILLAR_BASE`/`RT_PILLAR_TARGET` constants left behind
  unused in remediationData.ts (commit 372191dad), per the standing no-fixture-fallback rule. Verified: `tsc
  --noEmit` clean in msp-portal; `remediationModel.test.ts` 39/39 pass; api-server
  `portal-remediation-tracker-scores.test.ts` 9/9 pass; existing `test-manifests/portal/portal-v2-remediation.json`
  (RT25-RT28 / API3-API6, sessionTag `rt1381`) already covers the honest-null UI states from #1381 — not re-run
  this session (shane-msp MCP connection was down; no code/test-manifest content changed that would invalidate
  its prior pass). `verify-branch-merged.mjs`: OK, merged into origin/main at 3bcad06df.
