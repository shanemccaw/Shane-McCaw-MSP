# #2308 — Build Queue panel: real Park/Pause overlay via GitEpicPanelService

- **Status:** ✅ DONE 2026-09-02T00:30:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)

## Log
- 2026-09-02T00:00:00Z ⏳ IN FLIGHT — Confirmed #2413 (Build Queue panel real-data wiring) is
  genuinely DONE on origin/main (`e4557da3c`, `verify-branch-merged.mjs` OK). Wiring the existing
  `GitEpicPanelService.OverlayParkPauseAsync` — already proven against Git Map's mini rail/full doc
  tab and the Command Palette — into the Build Queue panel's rows, now that they're real
  `bt_build_queue` reads instead of fixture data.
- 2026-09-02T00:30:00Z ✅ DONE — Added `IsParked`/`IsPaused` (mutable) to `QueueItem`. After
  `LoadQueueFromDatabaseAsync` builds real rows and calls `RenderQueue()`, fire-and-forget
  `OverlayQueueParkPauseAsync` builds a `GitMapFeature` list from the rows' real `GithubNumber`s,
  calls the existing `GitEpicPanelService.OverlayParkPauseAsync` (same call Git Map's mini
  rail/full doc tab and the Command Palette already make — no new overlay logic, no second GraphQL
  query shape), maps `IsParked`/`IsPaused` back onto the matching `QueueItem`s, then re-renders.
  `BuildQueueCard` shows `PARKED`/`PAUSED` chips next to the status pill using the same
  `Brush.Status.Parked`/`Brush.Status.Paused` tokens and `Chip(...)` pattern Git Map's feature card
  already uses. No new fixture data, no fabricated fields — a row simply shows no chip when neither
  is true, same honest-empty convention as the rest of this panel.
  Commit: `9af994432` "Build Queue panel: real Park/Pause overlay via GitEpicPanelService (#2308)".
  Build: `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors.
  Merge: fast-forward push `agent/2308-q1208:main` (branch tip was already origin/main's exact
  next commit, no rebase needed); `verify-branch-merged.mjs` confirms OK.
  `git status --porcelain` clean apart from this file.
  No new findings filed this session.
