# #4573 — 65 dashboard-registry sourceKeys name no row in the live monitor_checks catalog

- **Status:** ✅ DONE 2026-09-19T04:16:00Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** `093809469` (the registry change: 6 remapped, 46 retired, 13 left, with tests) · `1c9df1d16` (blocker → issue links, pillarmapping.md addendum) · `e0e7143e5` (plan `build-journal/4573-plan.md`) · `d734bc70c` (IN FLIGHT bookend)
- **Findings filed (children of #1621, label `bug`, board "AI Batter Up"):** #4829, #4830, #4831, #4832, #4833, #4834 · **Decision issue (board "Ask Shane"):** #4835
- **Migration:** none (registry + tests + doc only; no schema or catalog-data change)

## Log
- 2026-09-19T03:34:37Z ⏳ IN FLIGHT — read issue body + comment (BUILD dispatch Posted 2026-09-18T14:20:00Z); auditing CATALOG_DRIFT_BACKLOG per domain against live monitor_checks
- 2026-09-19T03:54:27Z ⏳ IN FLIGHT — plan committed (`e0e7143e5`). Its first draft carried an unchecked `Posted:` time (2026-09-18T20:05:00Z); corrected to the commit's real time in the DONE commit.
- 2026-09-19T04:16:00Z ✅ DONE — backlog 65 → 13. 6 remapped, 46 retired to `not_collected:`, 13 left with a stated blocker each. Registry package 17/17, api-server 8 contract/registry files 137/137 + the picker cases, live resolve through the real resolver on the one data-bearing tenant. Not verified: the two Intune remaps against real device rows (no tenant has Intune data). Full-suite api-server run and api-server `tsc` were NOT done (see Verification Results).

## Changes Made

All registry edits are in `lib/dashboard-registry/src/metrics.ts` and `sourceKeyContract.ts`. A **remap** changes only `sourceKey`. A **retire** sets `sourceKey: "not_collected:<old-domain>-<tail>"`, `status: "not_collected"`, `smartEligible: false` and drops the smart config (label, valueType, shape, scope kept, so stored widgets and their renderers are untouched; the 38 stored template widgets on affected metrics use Stat/Trend/Timeline/Heatmap/Distribution, none uses Smart or ScoreRing). Every disposition is an individual decision with its evidence in `build-journal/4573-plan.md`.

| Domain (backlog keys) | Remapped | Retired | Left in backlog |
|---|---|---|---|
| intune → devices:* (8) | `non-compliant-devices` → `devices:compliant-vs-noncompliant`; `unencrypted-devices` → `devices:encryption-status` | `unenrolled-devices`, `jailbroken-devices`, `rooted-devices`, `high-threat-devices`, `config-drift` (5) | `outdated-devices` (candidate counts every device; #4829) |
| collaboration (6) | `teams-channels` → `teams:channel-sprawl` | `forwarding-mailboxes`, `shared-mailbox-signin`, `mailboxes`, `inbox-rules`, `delegation-grants` (5) | — |
| compliance (10) | `guest-users` → `governance:guest-count`; `orphaned-teams` → `teams:ownerless-teams` | `active-ediscovery`, `external-invites`, `missing-retention-tags`, `orphaned-sites`, `public-channels` (5) | `overshared-sites`, `sharepoint-sites`, `onedrive-external` (#4835) |
| copilot (2) | `overshare-exposure` → `copilot:data-exposure-risk` | — | `license-readiness` (distribution needs an aggregation transform) |
| dynamics (7) | — | all 7 | — |
| power-platform (2) | — | both | — |
| platform (7) | — | all 7 | — |
| workflow (7) | — | all 7 | — |
| audit (3) | — | `directory-audits`, `provisioning` (2) | `signins` (#4832) |
| identity (2) | — | `disabled-accounts`, `passwordless-adoption` (2) | — |
| security (8) | — | `attack-simulation`, `malware-alerts`, `phishing-alerts`, `secure-score-drift` (4) | `active-alerts` (2 metrics), `high-severity-alerts`, `risk-detections`, `secure-score-controls` (5 keys) |
| licensing (3) | — | — | `duplicate-assignments` (#4830), `inactive-user-licenses` (#4831), `sku-utilization` (3) |
| **Total (65 keys)** | **6** | **46** | **13** |

Other changes: `CATALOG_DRIFT_BACKLOG` now carries a `blockers` record (one machine-checked entry per remaining key) and `confirmedOn: 2026-09-18`; `CATALOG_DRIFT_BACKLOG` is exported from the package index; new tests in `lib/dashboard-registry/src/registry.test.ts` and `artifacts/api-server/src/lib/registry-source-key-contract.test.ts`; a dated addendum in `docs/pillarmapping.md` §8 superseding its "67 phantom keys" text.

Near-misses that would have shown a confidently wrong number (all left OUT of the remap set): `devices:os-patch-compliance` (`count(osVersion)` in a field named `outdatedOsDeviceCount`), `exchange:auto-forwarding-rules` (counts spam-filter policies, 0 or 1, under a mailbox caption), `security:secure-score-by-category` (would render one bucket `unknown = 90`, verified over the real stored row), `cost:duplicate-assignments` (structurally always 0), `license:unused-assigned` (malformed `countWhere`), `devices:enrollment-status` (the opposite quantity).

## Verification Results

**Counts:** of the 65 keys, **6 resolved (remapped)**, **46 retired to `not_collected:`**, **13 remain**, each with its real blocker in `CATALOG_DRIFT_BACKLOG.blockers` (5 of the 13 wait on a product decision → #4835 "Ask Shane"; 3 wait on a fix to the candidate check → #4829/#4830/#4831; 1 on #4832; the rest need an aggregation transform or a Defender-licensed tenant). Metric level: 66 metrics were affected (`security:active-alerts` backs two): 6 remapped, 46 retired, 14 unchanged.

**Live evidence (real local DB, real `resolveMetric`, tenant `c4c814d4-…`, customer 2080, 2026-09-18):**
- `collaboration.teamsChannelCount` → ok, **27** (`mapping:channelCount`, 18 teams, not truncated)
- `compliance.guestUserCount` → ok, **2** (`mapping:guestAccountCount`, of 27 users)
- `compliance.orphanedTeamCount` → ok, **0** (`mapping:ownerlessTeamCount`, of 18 teams)
- `copilot.overshareExposureCount` → ok, **1** (`mapping:copilotExposedSiteCount`, of 93 sites scanned)
- The two Intune remaps → `not_available / service_not_configured` (Intune not licensed on the only tenant). **Not verified against real device rows**; they are verified against the check mapping and the executor's `countEquals` / `countFalse` only. That is stated in the code comments and the plan.
- Retired metrics → `not_available / not_collected`; still-backlog metrics unchanged (`unknown_check_key`).
- 0 of the 65 keys exist in `monitor_checks` in any status and 0 have a `tenant_monitor_profiles` row (queried).

**Tests:** `lib/dashboard-registry` `tsx --test` 17/17 (4 new); api-server `vitest` on `registry-source-key-contract` 14/14 (6 new: the real `pickMappedValueField` picks the intended field for every remap, including the six-field `copilot:data-exposure-risk`), and the 8 registry-touching files (`dashboard-resolvers`, `pillar-summary-stats`, `portal-pii-governance`, `security-posture-narrative`, `security-secure-score-464`, `dashboard-data`, `copilot-readiness-narrative`, `portal-governance-areas`) 137/137. `tsc --noEmit -p lib/dashboard-registry` exit 0.

**Not done, stated plainly:** the full api-server suite was not run (memory notes a known 85-file live-db import baseline there), and api-server `tsc` was not run (the only api-server change is a test file, which vitest compiled and ran). No `shaneapp://runTest` manifest: this change is confined to `lib/dashboard-registry` and an api-server test, outside `artifacts/portal/` and `artifacts/shane-mccaw-consulting/`, so the manifest rule does not apply.

**Working tree:** `git status --porcelain` empty before this commit; no stash used; scratch files (a picker test and a live-resolve test, both under `artifacts/api-server/src/lib/zz-scratch-*`) were created and deleted before any commit. `node scripts/dev-server/verify-branch-merged.mjs` exit 0 (HEAD `1c9df1d16` merged into `origin/main`). No pre-existing dirty files. No manual SQL migration, so nothing appended to #1630.
