# #2706 — Test Pad rendered markdown: real/clickable task checkboxes that persist

- **Status:** ✅ DONE 2026-09-03T18:39:58Z
- **Scope:** buildconsole

## Log

- 2026-09-03T18:37:08Z — Started. Confirmed #2705 (Rendered/Raw toggle + GFM table support)
  is real and merged to main (`1bf5c37a1`). Plan: add opt-in `RenderOptions.OnTaskToggled`
  to `MarkdownRenderer.CreateTaskItem`/`Render` (default null = unchanged static glyph for
  `GitDetailView`/`IssueDetailView`), add `MarkdownRenderer.ToggleTaskLine` helper, wire
  `TestPadNoteDetailWindow`'s Rendered view to flip the real line + call
  `TestPadService.UpdateNote` + re-render.
- 2026-09-03T18:39:58Z — ✅ DONE — Implemented all 3 points of the real fix:
  1. `RenderOptions.OnTaskToggled` (`Action<int,bool>?`, default null) added; `CreateTaskItem`
     wraps the ☑/☐ glyph in a clickable `Border` (Cursor=Hand, `MouseLeftButtonDown`) only when
     it's set — default behavior for `GitDetailView`/`IssueDetailView` (neither sets it)
     verified unchanged by inspecting both call sites.
  2. `TestPadNoteDetailWindow` now tracks `_note` as an instance field, wires
     `OnTaskToggled` on every `RenderNote()` call, and on toggle calls the new
     `MarkdownRenderer.ToggleTaskLine(text, lineIndex, newChecked)` helper, then
     `TestPadService.UpdateNote(note.Id, updatedText, note.Type)` for real persistence, then
     re-renders. No-ops (no visual/persistence change) if the note is already `IsSent`
     (same lock `UpdateNote` itself enforces) or the line no longer parses as a task item.
  3. `lineIndex` passed to `CreateTaskItem`/the callback is the loop's own `i` — the real
     index into `markdown.Replace("\r\n","\n").Split('\n')`, the exact same array `Render`
     itself walks — and `ToggleTaskLine` re-derives that same split from the note's current
     text before editing, so the edit lands on the correct line even with headers/tables/etc.
     interleaved.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors.
    No `runTest`/UI manifest applicable (out of test-manifest scope per CLAUDE.md —
    BuildConsole/WPF is explicitly excluded). Manual interactive click-through not run in
    this non-interactive session (no launched BuildConsole.exe instance per the
    never-launch-BuildConsole.exe-for-verification rule); build + code-level trace of the
    click → `ToggleTaskLine` → `TestPadService.UpdateNote` → `RenderNote()` path is the real
    verification available here.
  - Commits: `8d372e2b5` (work), merged to `origin/main` at `e21599670`.
    `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
  - No findings filed — no genuine gap/bug hit outside scope during this build.
