# #2126 — Real Colors.xaml/Typography.xaml resource dictionary

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 797a3abd0 (merged into `main`, verified via `verify-branch-merged.mjs`)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Read #2126 body + comment in full, pulled exact hex/typography
  values. Audited `Themes/DarkTheme.xaml` (1326 lines, Catppuccin Mocha palette, merged via
  `App.xaml`) and grepped all ~138 inline hardcoded color literals + font-size distribution
  across the WPF app (58 files). Decision: LAYER, do not replace — DarkTheme.xaml's brush
  keys (CrustBrush/MantleBrush/BaseBrush/TextBrush/Subtext0/1Brush/OverlayBrush/BlueBrush)
  are already referenced pervasively via StaticResource across every panel; retargeting their
  underlying Color values to pull from new Colors.xaml/Typography.xaml achieves "every panel
  picks up the new theme automatically" without a mass rename across 58 files. Full replacement
  of DarkTheme.xaml was rejected — most of it (Surface0-2, Mauve/Sapphire/Lavender/Teal/Pink/
  Flamingo/Rosewater/Maroon, all Style templates) has no defined replacement in #2126 and a
  from-scratch rewrite risked losing template/behavior work unrelated to this issue.
- 2026-08-31 ✅ DONE — Shipped:
  - `Themes/Colors.xaml` (new) — the 12 exact values from #2126's body (4 backgrounds incl.
    border, 3 text tiers, 1 accent, 4 status colors), each as both a `Color` and a
    `SolidColorBrush` resource, plus two accent/success "wash" brushes (10%/20% alpha) used by
    hover/selection fills that already existed at those exact opacities.
  - `Themes/Typography.xaml` (new) — `BodyFontSize=14`, `SubHeaderFontSize=16`,
    `HeaderFontSize=18` (the app already used both 16 and 18 for headers in roughly equal
    measure — kept as two sizes rather than collapsing #2126's "16-18px" range to one),
    `MonospaceFontSize=13`.
  - `App.xaml` — merged both new dictionaries into `Application.Resources` *before*
    `Themes/DarkTheme.xaml`.
  - `Themes/DarkTheme.xaml` retargeted (not replaced — see IN FLIGHT log for why): the
    brush keys every panel already references via `StaticResource`
    (CrustBrush/MantleBrush/BaseBrush/TextBrush/Subtext0Brush/Subtext1Brush/OverlayBrush/
    BlueBrush) now pull their `Color` from Colors.xaml instead of literal Catppuccin hex —
    background layering order preserved (Crust=darkest=AppBackground #0d1117 <
    Mantle=Panel #161b22 < Base=Card #1c2128), Subtext0+Subtext1 both collapsed onto the
    one new TextSecondary tier (#2126 defines 2 mute tiers, Catppuccin had 3 — documented
    in DarkTheme.xaml's own comment), Overlay→TextDisabled (verified: used only for
    placeholder/meta/disabled-like text), Blue→Accent (verified: used only for
    interactive/selection/focus, a clean 1:1). Crust/Mantle/Base kept their pre-existing
    alpha channel (CC/CC/66) — an unrelated window-transparency effect — with only the RGB
    swapped. Also retargeted the 4 ambient glow DropShadowEffects and 3 accent-selection
    "wash" literals that hardcoded `#89B4FA` directly (bypassing BlueBrush entirely, so they
    would NOT have picked up the new accent color) to reference the new resources.
    Body-text FontSize setters (Menu/MenuItem/TextBox/Button/ComboBox/TabItem/DataGrid,
    was 13) now reference `BodyFontSize`; EditorTextBox (was 13, unchanged value) and
    TerminalOutputBox (was 12 — **below the new 13px monospace minimum, bumped**) now
    reference `MonospaceFontSize`. Left untouched, with the reasoning inline as XAML
    comments: Surface0/1/2 (no replacement value defined in #2126, would have required
    inventing hex not given), Mauve/Green/Red/Peach/Yellow/Sky/Sapphire/Lavender/Teal/
    Pink/Flamingo/Rosewater/Maroon (real audit found MIXED status + decorative usage,
    see #2127 below), DataGridColumnHeader/StatusBar/ToolTip/BubbleToolTip FontSize=12
    (deliberately-compact chrome, not body text).
  - Converted genuine hardcoded-inline-color bypasses (found via grepping all 3-8 hex
    digit `Color=`/`Background=`/`Foreground=`/`BorderBrush=` literals app-wide, 138 hits
    across 58 files) that were literal duplicates of the newly-retargeted colors, to
    `{DynamicResource}`/`{StaticResource}` references so they pick up the new theme too:
    `BuildWatchWindow.xaml`, `StreamingConsoleWindow.xaml` (Mantle/Crust/Base window-chrome
    literals + one Blue-wash), `Controls/LeftSidebar.xaml`, `MainWindow.xaml`,
    `Controls/HomeView.xaml`, `Controls/CritterLoungeControl.xaml`,
    `Controls/StartupLoadingView.xaml` (+ `.xaml.cs`, which had its own hardcoded C# copy of
    7 Catppuccin brushes for connection-status dots — now looks up the correct
    `StatusSuccess/Warning/Error/RunningBrush` by key at the call sites that have real status
    meaning, matching the existing `FocusCharacterLayer.Res()` pattern), `Controls/
    ApiRunnerView.xaml`, `Controls/ChatSessionPane.xaml`, `RegionSelectOverlayWindow.xaml`
    (the drag-to-select rectangle, genuinely "selected state" per #2126's Accent
    definition). Left untouched, with reasoning: `Resources/ChatPaneBrushes.xaml` (already
    its own separate, deliberate resource dictionary — not an inline bypass),
    mascot/critter-art decorative gradients, black scrim/shadow overlays, and the
    `#33A6E3A1` shaneapp:// live-indicator badges (GreenBrush itself wasn't retargeted, so
    no incoherence was introduced by leaving them).
  - **Real audit finding filed, not guessed at inline**: opened
    [#2127](https://github.com/shanemccaw/Shane-McCaw-MSP/issues/2127) (parented to #1202,
    board status "AI Batter Up") documenting the mixed status-vs-decorative usage of
    Green/Red/Peach/Yellow/Sky found during the audit, with concrete file:line evidence, so
    a future session can do the site-by-site pass instead of a risky blanket recolor here.
  - **Verification**: `dotnet build BuildConsole.csproj -c Debug` → Build succeeded, 0
    Warnings, 0 Errors (WPF's markup compiler validates all `StaticResource`/
    `DynamicResource` keys and Color-vs-Brush type usage at compile time, so this catches
    resource wiring mistakes). Also launched `BuildConsole.exe` directly, confirmed it
    stayed running 6s+ with no startup crash (App.xaml's resource merge happens on
    `InitializeComponent`, so a malformed Colors.xaml/Typography.xaml would have crashed
    immediately), then killed that specific PID. No `shaneapp://runTest` manifest — per
    CLAUDE.md, `desktop/BuildConsole` is explicitly out of scope for test manifests.
  - `git status --porcelain` clean after commit; branch verified an ancestor of `main` via
    `verify-branch-merged.mjs` before writing this DONE.
