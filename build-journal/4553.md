# #4553 — Build Queue Reply and Resume Session can still launch a second live process on a running session

- **Status:** ✅ DONE 2026-09-17T17:23:15Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** fc8a0235d (work), 34b9828da (IN FLIGHT bookend)
- **Findings filed:** #4554 (Retry on reply rows, crash-recovery Resume Session, and Recover All still resume without a session liveness check; parented under #3706)

## Log
- 2026-09-17T17:14:26Z ⏳ IN FLIGHT — routing Build Queue Reply (panel + Git Board popover) and Build Watch ▶ Resume Session through #4547's guarded `QueueWatcherService.LaunchContinuationAsync` (buildId 3168).
- 2026-09-17T17:23:15Z ✅ DONE — Confirmed the cited paths on current code: Reply at BuildQueuePanel.xaml.cs ~7006 and QuickReplyAsync ~6848, and BuildWatchWindow `ResumeAdoptedSlot` ~2229. I also found the same unguarded Resume Session in `GitDetailView.HandleBuildResume`. All four now go through `LaunchContinuationAsync`; no second guard was built. Reply is one shared `SendReplyAsync`, which also covers every QuickReplyAsync caller (Git Board popover, sidebar inline reply, floating chat, the BT_ protocol reply). It writes to live stdin when it can. Otherwise it asks before stopping a working process, then stops it, confirms the exit, and resumes. Resume Session re-sends the original prompt under its own title. The original evidence showed the session live under a **different** row (#2953 replied while #2947 ran it), so the guard now looks up the session's live process under any row. It uses the captured session id or the launch-time `ResumeSessionId` (new field on RunningEntry). It stops and reuses that row, and refuses to launch when the session already runs in more than one process. Other extensions: a `TitlePrefix` (Reply → / Continue: / none) and `ChatUrl` on the request, plus a shared `WouldInterruptActiveWork` ask-first check. #4547's two composers use that check too. Verification: `dotnet build` 0 warnings, 0 errors. A reflection harness against the built DLL passed 12/12. It used real OS processes and covered: two live rows on a session led to a refusal with both left alive; the live process under another row was found, the prompt appeared only while it was working, and it was killed with the exit confirmed before any DB write; with no live process, nothing was stopped. The harness wrote a few "HARNESS #4553" / queue #99000x lines to the AppData activity log. NOT verified: the WPF UI end to end, because the BuildConsole exe is not launched for verification.
