# #3788 — Public SOW share link has nowhere to land

- **Status:** ✅ DONE 2026-09-15T14:05:00Z — no code change; closing out with an honest
  finding instead
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** none of mine touch the actual gap — see log below. Finding filed as
  #4250 (parented under #2568, board status "AI Batter Up"), commit ae2db18cb.

## Log
- 2026-09-15T13:15:00Z ⏳ IN FLIGHT — dispatched to build a new public SOW view/sign page
  on `artifacts/shane-mccaw-consulting` per this issue's own `BUILD:` comment (posted
  2026-09-15T13:01:00Z), which asserts "no Design export exists for this page anywhere
  in the tree." Investigating before writing any code, per standing practice, since the
  backend (`msp-sow.ts`) already existed at the time of the original 2026-09-12 triage
  comments this dispatch built on.
- 2026-09-15T14:05:00Z ✅ DONE — **the gap #3788 describes is already closed, on `main`,
  since before this build was dispatched.** `artifacts/portal/src/pages/msp-sow-public.tsx`
  (route `/sow/:shareToken`, no auth) already renders `GET /api/public/sows/:shareToken`
  and drives `POST /api/public/sows/:shareToken/sign` with a real drawn-signature pad
  (`SignaturePad.tsx`), shipped under Feature #1663 ("Public Share Pages (Portal)") /
  #4001 ("Public Share Pages: wire the real Design into artifacts/portal"), both closed,
  landed at commit `be4969e2d` on 2026-09-13 — two days before this issue's `BUILD:`
  comment (2026-09-15) asserted "no Design export exists for this page anywhere in the
  tree." That premise was true on 2026-09-12 (Shane's own triage comments) but stale by
  2026-09-15: `Design/portal/design_handoff_full_site/screens/Public Share Pages.dc.html`
  and its contract pack (`docs/portal/... public-share-pages-contract-pack.md`) already
  existed, and the real page was already built against them.
  Confirmed the real customer-facing link already points there, not at anything on the
  marketing site: `artifacts/msp-console/src/console/modules/Sales.tsx:1207` builds
  `shareUrl = ${window.location.origin}/sow/${s.shareToken}` — the exact route
  `msp-sow-public.tsx` serves. Backend hookup to #2009 (SOW-signed → real project
  creation) fires unconditionally server-side in `msp-sow.ts`'s
  `triggerProjectFulfillmentFromSignedSow`, regardless of which frontend calls
  `/sign` — already correct, already tested (`msp-sow.test.ts` covers both the public
  view and public sign routes, including the project-fulfillment kickoff).
  Building a second, competing implementation on `artifacts/shane-mccaw-consulting`
  against the same real endpoints would not be "honest, no backend exists" — it would be
  a genuinely harmful duplicate of a real money/legal-signature surface that already
  ships, wired, tested, and reachable from the one link MSP operators actually send
  customers. Did not build it. Filed the one real, lasting finding that came out of this
  investigation as **#4250** (parented under #2568 per this dispatch's own standing
  rule, labeled `bug`, board status "AI Batter Up"): CLAUDE.md itself still says
  `artifacts/msp-portal/` in two load-bearing places (a verification grep that can never
  fail, and a test-manifest scope line), but that directory doesn't exist — the real app
  is `artifacts/portal/`, which has zero test-manifest coverage for this exact real
  money/legal flow. No `pnpm typecheck`/test run needed — no source file was touched;
  `git status --porcelain` is clean apart from this bookend and the finding-filing
  commit, both mine.
