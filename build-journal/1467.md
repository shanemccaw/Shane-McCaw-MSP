# Build Journal — Issue #1467

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Commits:** 39b30d43a (bookend IN FLIGHT), 24fab658e (fix)

## Log
- 2026-08-28: Session start. Investigated System Health "Local Dev Server" check — found it in `desktop/BuildConsole/Services/SystemHealthService.cs`, `CheckDevServerAsync()`, hitting `/api/health` (404, nonexistent). Real endpoint confirmed at `artifacts/api-server/src/routes/health.ts` as `/api/healthz`. Fixing.
- 2026-08-28: ✅ DONE — Changed `CheckDevServerAsync()`'s `healthUrl` from `/api/health` to `/api/healthz` (commit 24fab658e). Verified live against the real local api-server on `localhost:8080`: `curl http://localhost:8080/api/healthz` → 200, `curl http://localhost:8080/api/health` → 404 — confirming both the bug's root cause and the fix. Grepped the rest of `desktop/BuildConsole` for other stray `/api/health"` references; none found. Merged and pushed to `origin/main` at 24fab658e; `verify-branch-merged.mjs` confirms the branch is a genuine ancestor of `origin/main`.
