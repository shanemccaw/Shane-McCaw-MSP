# #3529 — Account Security: change-password — wire in artifacts/portal (v2)

- **Status:** ✅ DONE 2026-09-10T21:24:53Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 7c4e9c94a (change-password wiring), fe0118233 (test manifest), 471484a06 (manifest note update)

## Log
- 2026-09-10T21:15:28Z ⏳ IN FLIGHT — Design export confirmed landed
  (`Design/portal/design_handoff_full_site/screens/Account Security.dc.html`).
  Page already exists at `artifacts/portal/src/pages/account-security.tsx`
  (MFA/sessions/login-history/data-rights already wired by #2995/#1603/#1604).
  Wiring the one remaining gap: the "Password" card / `POST /auth/change-password`,
  carried forward from the retired portal-v2 implementation (#1601,
  `git show portal-archive-2026-08-29:artifacts/msp-portal/src/pages/portal-v2-account-security.tsx`).
- 2026-09-10T21:24:53Z ✅ DONE — Wired `POST /api/auth/change-password` into
  `useAccountSecurityLive.ts` (`changePassword`, `ChangePasswordOutcome`,
  `changePasswordErrorText`, `changePasswordSuccessText`) and added the "Password"
  card to `account-security.tsx` (title, "No forced expiry" note, toggle, inline
  current/new password form), all four of the route's own documented error states
  (`auth.ts:826/831/838/843`) rendered verbatim, real `revokedOtherSessions` count
  surfaced on success. Out of this issue's scope, left for separate wiring and
  filed as **#3544** (parented to #1595, board status AI Batter Up): the
  tenant-wide "Your Microsoft 365 tenant" signals card and the inline
  local-failed-login summary line the same design shows in the Password card,
  both backed by `GET /api/portal/account-security/graph-signals` (#1593, built
  but never wired to any UI). Verification: real `tsc -p tsconfig.json --noEmit`
  (0 errors) and real `vite build` (succeeds) against `artifacts/portal`. Wrote
  `test-manifests/portal/account-security-change-password.json` (registered in
  `_regression-suite.json`) — a real `shaneapp://runTest` attempt was made
  (`file=account-security-change-password.json&onlyTag=change-password-3529`)
  and returned "manifest not found or unparseable": BuildConsole's local instance
  resolves `test-manifests/` against the dedicated dev-server checkout, which only
  receives this worktree's commits at BuildConsole's own end-of-build merge — the
  same structural gap #2995's/#2996's own sibling manifests documented for their
  builds. Re-run once merged. Branch verified merged into `origin/main`
  (`verify-branch-merged.mjs`, exit 0) and working tree clean
  (`git status --porcelain` empty) before writing this DONE.
