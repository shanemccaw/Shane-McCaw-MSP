# #2801 — Build Queue card's Epic chip should be clickable (activate/reopen tab)

- **Status:** ✅ DONE 2026-09-04T03:36:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-04
- **Commit(s):** 9a10f04be6c0d1fdf56e5aa6ad10185c3e4d08ea

## Log
- 2026-09-04T03:27:17Z ⏳ IN FLIGHT — investigating BuildEpicChipRow, PersistedChatTab.EpicId, BuildConsoleSettings.OpenChatTabs, OpenChatTab, GetEpicForIssueNumber/GetEpicForChat to wire chip click handler per issue's 4-point plan.
- 2026-09-04T03:36:00Z ✅ DONE — Added `BuildQueuePanel.EpicChipClicked` event, wired the chip `Border` (`BuildEpicChipRow`) with `MouseLeftButtonDown` (same convention as the existing `chatBadge`/`blockBadge`), and added `MainWindow.ActivateOrReopenEpicChat(BoardEpic)`: (1) iterates `_chatTabs` across all four panes, resolves each open tab's real epic via `LeftSidebar.GetEpicForChat`/`GetEpicForIssueNumber`, and reuses `OpenChatTab`'s own already-open dedupe to activate a match; (2) falls back to a fresh `BuildConsoleSettings.Load().OpenChatTabs` lookup filtered by `EpicId`, most-recent `SavedAt`, reopened via the existing `Home_ResumeChatRequested` path (#2707's Reopen All mechanism); (3) a real `ToastEngine.Warning` when genuinely nothing matches either case — no fake/wrong tab activation. `dotnet build -c Debug` on `desktop/BuildConsole/BuildConsole.csproj`: **Build succeeded, 0 Warning(s), 0 Error(s)**. Real, non-fabricated data throughout — no fixtures. Hit a real shared-checkout git-object hazard mid-push (several `.pack` files in the main checkout's shared `.git/objects/pack/` had gone missing their `.idx` — a concurrent background auto-pack race, not caused by this session); regenerated them read-only via `git index-pack` (safe, additive, touches no refs) and the push then succeeded cleanly. Branch verified merged via `verify-branch-merged.mjs`. Working tree clean.
