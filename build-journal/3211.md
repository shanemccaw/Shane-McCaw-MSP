# #3211 — Feature: Money — Connected banks reconnect UI in Accounts (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T00:45:00Z
- **Scope:** platform (web/shanes-life — Shane's personal project, tracked here per Epic #3086)
- **Started:** 2026-09-08
- **Commit(s):** fbba3f893 (rebased/pushed to main as 86f6dbdae)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — Read design 1d (`Shanes Life 17 - Money v3.dc.html`,
  option `#1d`, "Accounts · lower · Debts overlay, Connected banks, Income rules"). Found
  `#3168` already shipped the real Plaid item-health + update-mode reconnect flow behind the
  dedicated Money → Banks tab (`viewMoneyBanks`, `bankRow()`), but the Money → Accounts tab's
  own "Connected Banks" card (`viewMoneyAccounts`) was left a deliberate read-only stub with
  the comment "Real disconnect/reconnect actions land with the Plaid reconnect Feature -- not
  wired here yet." That stub is exactly this issue's scope.
- 2026-09-08T00:45:00Z ✅ DONE — `web/shanes-life/public/app.js` (commit fbba3f893, on main as
  86f6dbdae): the Accounts tab's Connected Banks card now fetches the real
  `/api/money/banks` item-health data and renders it through the same `bankRow()` component
  the Banks tab uses — real green/amber status dot per institution, real "N accounts · synced
  N minutes ago" / "access expires …" copy, and a real Reconnect button that opens Plaid Link
  in update mode and returns to the current screen on success. Extracted the shared
  `bankReconnectHandler()` so both tabs drive one real reconnect flow instead of two. Added
  design 1d's verbatim safety copy ("Balances only; nothing moves from this app.") to
  `bankRow`'s reconnect-needed state.
  Verification: `node --check public/app.js` passes. `npm run check` against the real local
  `finances` Postgres database: 209/213 — every Banks/Connected-Banks assertion passed; the 4
  failures are pre-existing Vault/`SL_VAULT_KEY` env-passing gaps unrelated to this change
  (same failure mode #3168's own comment already documented, not caused by this session).
  `git merge-base --is-ancestor` / `verify-branch-merged.mjs` confirms the branch is merged
  into `origin/main`. Working tree clean.
  No new backend/schema work was needed — `#3168` had already built `GET /api/money/banks`,
  the reconnect-token/reconnect-complete routes, and `plaid_items` health columns; this
  session only wired the existing Accounts-tab stub to that real data instead of duplicating
  it. No findings filed — no new gap discovered outside this issue's own scope.
