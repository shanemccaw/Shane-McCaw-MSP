# #3607 — Canceled queue should auto-prune (soft-archive) rows whose real GitHub issue is already closed

- **Status:** ✅ DONE 2026-09-10T23:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 7b9d8a5e9 (Drizzle schema + migration), cdf64cae0 (reconciler + UI wiring)

## Log
- 2026-09-10T22:40:00Z ⏳ IN FLIGHT — pulled full issue body + all 3 comments (the body already reflects the final corrected decision: soft-archive via new `archived`/`archived_at` columns on `bt_build_queue`, matching `bt_chats`'s exact pattern, NOT a hard delete — two rules: A) canceled + real github_number + issue confirmed closed (prefer `bt_issue_mirror`, live fallback if missing/stale); B) canceled + no real github_number at all → archive directly). Confirmed via live `psql` query against local `bt_build_queue` that "local #N" rows (Rule B's motivating case) actually carry a NEGATIVE `github_number` sentinel (e.g. `-13`, Git #1645), not NULL as the issue body assumes — Rule B implemented as `github_number IS NULL OR github_number <= 0` to actually match Shane's real screenshot data. Also confirmed a pre-existing, narrower mechanism already partially covers Rule A: `FalseDoneReconciler`'s Shape C (Git #3521) already moves a stale canceled+github-linked+closed-issue row to `status='superseded'` using the open-issue snapshot (not the mirror). That stays in place unchanged; this build adds the archived-column mechanism on top per Shane's explicit, later, in-flight-superseding decision — Shape C will usually win the race (runs first, same method), so the new Rule-A archive path mainly catches anything Shape C's snapshot-based check misses/skips, using the mirror-preferred check the issue explicitly asked for. Applied migration `2026-09-10-bt-build-queue-archive-3607.sql` against local Postgres (ALTER TABLE bt_build_queue ADD COLUMN archived/archived_at, matching bt_chats' pattern) — ran myself, additive DDL. Also confirmed sibling issue #3611 (blocked_by #3607) is written expecting exactly `QueueItem.Archived` + `items.Where(i => i.Archived ...)` — validates this design. Now implementing: QueueItem.Archived/ArchivedAt, BuildQueuePostgresClient read/write methods, FalseDoneReconciler Shape D (Rule A + Rule B), Canceled board filter excludes archived, ActivityLog logging before every archive.
- 2026-09-10T23:05:00Z ✅ DONE — Implemented Shape D in `FalseDoneReconciler.ReconcileAsync` (runs BEFORE the open-issue-snapshot fetch/Shapes B/C so a snapshot hiccup can never suppress it): `ArchiveCanceledNoIssueRowsAsync` (Rule B) and `ArchiveCanceledClosedIssueRowsAsync` (Rule A, mirror-preferred with a 30-min staleness window matching `UsageAutomationService.StaleAfter`'s existing convention, live `GetIssueAsync` fallback, never archives on ambiguous/missing data). Added `BuildQueuePostgresClient.GetCanceledUnarchivedWithIssueRowsAsync`/`GetCanceledUnarchivedNoIssueRowIdsAsync`/`ArchiveCanceledQueueRowAsync` (idempotent, guarded `status='canceled' AND archived=FALSE`). `QueueItem.Archived`/`ArchivedAt` added; `GetQueueAsync`'s display query extended with new trailing ordinals 24/25 (appended after existing repo_owner/repo_name at 22/23 per the #1384 fixed-ordinal contract — verified the exact SQL text runs correctly via psql before trusting the ordinal reads). `BuildQueuePanel`'s "Canceled" filter now excludes `i.Archived` (leaves "All" untouched — #3611, blocked_by this issue, adds the dedicated Archive filter). Two new `ReconciliationActionKind` values wired into the existing ActivityLog-backed `log(...)` callback, `ReconciliationNoticeStore`, and both summary-count call sites (MainWindow toast + ReconciliationDetailWindow). `dotnet build BuildConsole.csproj` succeeded, 0 errors. **Live-verified against the real local `bt_build_queue`, not simulated:** Rule B's exact query matched exactly the 9 real rows Shane's own screenshot showed (`local #6`..`local #13`, ids 186-189/222-225, github_number -6..-13 confirmed via direct query — plus one genuine `github_number IS NULL` row, id 977); ran the equivalent guarded UPDATE for real, confirmed idempotent (0 rows on re-run), confirmed the rows stay real/queryable (`status` still `'canceled'`, `archived=true`, `archived_at` set) while a `status='canceled' AND archived=false` board-view simulation now returns 0 for them. Rule A's 29 real candidate rows were checked against the live `bt_issue_mirror` directly — all 29 are genuinely `open` right now, so none archived (correct); no closed-issue candidate exists at the moment to exercise the positive Rule-A archive path, but the safety-critical negative path (never archive an open/ambiguous issue) is confirmed correct against real data. `verify-branch-merged.mjs` confirms `agent/3607-q2283` (cdf64cae0) is a real ancestor of `origin/main`; `git status --porcelain` clean. Real commits: `7b9d8a5e9` (Drizzle schema + migration file, run against local Postgres), `cdf64cae0` (reconciler + DB methods + UI wiring). No findings filed — the one real discrepancy found (issue body assumed `github_number IS NULL` for local builds; real data uses the Git #1645 negative sentinel) was corrected in-scope within this same build, not left as a gap. Labels synced (`in-flight`→`complete`); completion comment posted on #3607.
