# #2769 — Feature: Terminal tool in the chat wrench menu

- **Status:** ✅ DONE 2026-09-03T23:15:05Z
- **Scope:** buildconsole

## Log

- 2026-09-03T23:07:12Z — Session started. Real worktree-provisioning miss hit first: `C:\wt\2769-q1475`
  was declared as primary working dir but was completely empty (no `.git`), same shape as the
  known #2118 class. Recovered manually: `git worktree add` reused the already-existing, unmerged
  (identical-to-origin/main) branch `agent/2769-q1475`, then registered it with
  `scripts/dev-server/worktree-lifecycle.mjs` using `$CLAUDE_PID` as owner so it survives sweeps,
  and linked deps via `scripts/dev-server/link-deps.mjs`. Proceeding with #2769's real work: embed
  the existing `Controls/TerminalView` into the chat tool rail's wrench-menu Terminal tool,
  replacing the generic `ToolStubBody` for that one tool id, with the TerminalView instance cached
  per chat tab so a live shell session survives rail close/reopen.
- 2026-09-03T23:15:05Z — ✅ DONE — Real audit confirmed the issue's description of `TerminalView`
  (module `desktop/BuildConsole/Controls/TerminalView.xaml(.cs)`) is a parameterless-constructor
  `UserControl` wrapping a real, persistent local `powershell.exe` process
  (RedirectStandard{Input,Output,Error}, one process per view instance, output streamed to a
  `TextBox`) — note the issue body's description of it as "xterm.js-backed" is stale/inaccurate;
  there is no xterm.js or `Assets/xterm/terminal.html` in this repo's actual TerminalView, but the
  real control is exactly as reusable/working as described, so this doesn't change the plan.
  Implemented the 4-point plan against `Controls/ChatDocumentContainer.xaml(.cs)`:
  1. Added `ContentControl x:Name="ToolHost"` (`Visibility="Collapsed"`) alongside
     `DetectedScroller`/`ToolStubBody` in the tool rail body Grid.
  2. `OpenTool` now branches on `id == "terminal"`: hides `ToolStubBody`, shows `ToolHost`, sets
     its `Content` to the tab's `TerminalView` instance. Every other tool id is unchanged
     (`ToolStubBody` as before).
  3. **Session persistence (the real point of this feature)**: added a `private TerminalView?
     _terminalView` field on `ChatDocumentContainer` — one instance per chat tab, since
     `ChatDocumentContainer` is itself one-per-tab. `OpenTool` does `_terminalView ??= new
     TerminalView()`, so the same live shell (and its running command, cwd, output history)
     survives rail close/reopen; `CloseRail` only sets `RailColumn.Width = 0`, it does not touch
     `ToolHost.Content` or unload it, and `TerminalView`'s own `Unloaded` handler (which kills the
     underlying shell process) only fires when the whole tab is torn down — not on a mere rail
     close. `OpenDetected` was also updated to hide `ToolHost` (previously only hid
     `ToolStubBody`), so switching to the Detected panel and back to Terminal doesn't leave two
     panels visible.
  4. All 11 other tool ids (`logs`, `api-local`, `api-read`, `api-write`, `gitdoctor`, `gitmap`,
     `health`, `sql`, `ps`, `json`, `files`) untouched — still show the generic stub.
  - Verification: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0
    errors. Out of scope for a test manifest per CLAUDE.md (desktop/BuildConsole is Shane's own
    tooling, not customer-facing product) — no interactive WPF harness available in this
    non-interactive session to click-drive the rail, so the real functional confirmation asked
    for in the issue body ("open wrench menu, click Terminal, run a real command, close/reopen
    rail, confirm same session") is left for Shane's own hands-on check; the code path itself
    (single cached field, no unload on rail close, `TerminalView`'s own process-per-instance
    lifetime) is the same pattern already used successfully for `_dockPanel`/other per-tab-cached
    controls in this file.
  - Diff: `desktop/BuildConsole/Controls/ChatDocumentContainer.xaml` (+6),
    `desktop/BuildConsole/Controls/ChatDocumentContainer.xaml.cs` (+28/-2). No fixture/hardcoded
    data — this is a WPF wiring change only, no data layer involved.
  - Commits: `7660a60bc` (Embed real TerminalView as the chat wrench menu Terminal tool),
    rebased/merged to `main` as `7e2ae2d5b`. `main` HEAD verified via
    `scripts/dev-server/verify-branch-merged.mjs` — OK, merged.
  - No genuine findings/bugs discovered mid-build beyond the stale xterm.js description noted
    above, which isn't itself a bug worth filing (cosmetic issue-body inaccuracy, doesn't affect
    behavior or planning).
