# #1561 — Security Plan: Assembled view over the eight modules

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** <fill at DONE>

## Log
- 2026-08-31 ⏳ IN FLIGHT — Building the Security Plan assembled view. Reuses the RBD
  versioning/supersession pattern (`msp_rbd_versions` / `rbd-versioning.ts`, #1508) one
  level up (#1562): assemble the eight modules' real per-tenant state into a frozen,
  signed, sealed version, with a live view alongside showing drift. Scope filtering
  operates on dimensions only, never findings (#1563); every version carries a sealed
  filter footprint — filters applied, what was excluded, excluded count (#1565). Ends at
  the wire contract — no `artifacts/portal` page (SCOPE STOP).
