# #1561 — Security Plan: Assembled view over the eight modules

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 46ff73e92 (deliverables) + 29e675c8d (IN FLIGHT bookend), both on origin/main

## Log
- 2026-08-31 ⏳ IN FLIGHT — Build the Security Plan assembled-view backend (Portal New
  Design epic #1485, foundation issue #1561). It owns almost no data: it reads/versions/
  seals what the other eight modules produce (Policy #1490, Risk #1487, Ownership #1491,
  SOPs #1493, Remediation #1489, Change Control #1486, Microsoft Changes #1494). Reuse
  the RBD versioning/supersession shape from #1508 (`msp_rbd_versions`) one level up
  (#1562) — no second sealing stack. Enforce scope-not-findings filtering (#1563) and a
  sealed filter footprint (#1565). SCOPE STOP: schema + migration + api-server route +
  wire types only; no page under artifacts/portal.
- 2026-08-31 ⚠️ RECOVERY — Original worktree `C:\wt\1561-q906` was swept during a session
  restart with the deliverable code still uncommitted (only the IN FLIGHT bookend was
  committed). Work survived in `rescued/1561-q906-*` branches. Reconstructed all files
  onto a fresh `origin/main` base in worktree `C:\wt\q1077`, taking the more complete
  12:09 rescue (which refactored the scope/footprint core into a pure, unit-tested
  `applyScopeAndFootprint`). Re-verifying before DONE.
- 2026-08-31 ✅ DONE — Shipped in `46ff73e92` on origin/main. The Security Plan
  assembled-view backend, ending at the wire contract (no artifacts/portal page —
  SCOPE STOP; no design export exists). Delivered:
  • `msp_security_plan_versions` — the seal/supersession chain, deliberately the
    `msp_rbd_versions` (#1508) shape one level up (#1562): full self-contained
    `SecurityPlanContent` snapshot in `content`, `superseded_at IS NULL` = current,
    `signed`/`signedBy`(ClientApprover)/`signedAt`. Drizzle schema in
    `lib/db/src/schema/msp.ts` + migration
    `lib/db/migrations/manual/2026-08-31-security-plan-versioning-1561.sql` (RUN
    against local Postgres; also logged to #1630).
  • `security-plan-assembly.ts` — reads the seven source modules for the caller's
    real tenant (bridging both keying conventions via `resolveTenantScope`), applies
    scope and computes the #1565 footprint. Scope (#1563) narrows ONLY on real
    dimensions (`pillar`, `framework`); NO outcome filter exists in the code path; a
    row the dimension can't classify is retained, never dropped. `business_unit` has
    no backing column and is NOT faked (finding #2085).
  • `security-plan-versioning.ts` — supersede-then-insert txn + guarded sign, mirroring
    `rbd-versioning.ts`.
  • `msp-security-plan.ts` route (MSP-side, mirrors `msp-rbd-versions.ts`):
    GET assembled / GET versions / GET versions/current / POST versions (seal) /
    PATCH .../sign. Registered in `routes/index.ts`. `channel: "tenant.portal"`.
  • Pure #1563/#1565 test `security-plan-assembly.test.ts` (registered in
    `vitest.config.ts`).
  Verification (observed this session): vitest 5/5 pass; live-DB smokes pass —
  assemble→seal→supersede→sign→refuse-superseded, and scope exclusion+footprint
  counting (a framework-classified risk row excluded and counted). tsc (override
  resolving `@workspace/db` to the worktree `lib/db`): ZERO errors on my files; the
  66 program-wide errors are pre-existing/unrelated (#1765 etc.) plus TS6059 rootDir
  artifacts of the override itself, none in the real build.
  Findings filed: #2085 (no `business_unit` backing column, →#1495), #2086 (`pillar`
  NULL on all rows, →#1096), #2121 (worktree isolation: shared `@workspace/db`
  junction dangles cross-worktree, →#1202).
  NOTE: original worktree was swept mid-session with uncommitted work; recovered from
  `rescued/1561-q906-*` and relanded — see the RECOVERY line above.
