# #3400 — msp-marketplace-purchase.ts: paid checkout failure leaves a permanently 'accepted' sales offer with no charge collected

- **Status:** ⏳ IN FLIGHT 2026-09-11T04:05:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T04:05:00Z ⏳ IN FLIGHT — Read issue #3400 body + BUILD comment. Confirmed via full read of
  `artifacts/api-server/src/routes/msp-marketplace-purchase.ts` that the `sales_offers` insert (state
  "accepted") + SSE broadcast happen unconditionally before the paid (Stripe) path even starts, and
  every failure exit in that path (Stripe not configured, no saved customer id, no default payment
  method, non-active/trialing subscription, non-succeeded PaymentIntent, thrown exception) leaves that
  row terminally "accepted" with no charge and no audit log. Fix: move the insert + broadcast to occur
  only after Stripe confirms the charge succeeded, for the paid branch only (free path is unaffected
  since it never risks a failed charge).
