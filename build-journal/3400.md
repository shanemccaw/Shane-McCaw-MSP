# #3400 — msp-marketplace-purchase.ts: paid checkout failure leaves a permanently 'accepted' sales offer with no charge collected

- **Status:** ✅ DONE 2026-09-11T04:35:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 23ef25613 (bookend IN FLIGHT), 86d65b3f2 (real fix + regression tests), rebased onto
  main at 749c8c404

## Log
- 2026-09-11T04:05:00Z ⏳ IN FLIGHT — Read issue #3400 body + BUILD comment. Confirmed via full read of
  `artifacts/api-server/src/routes/msp-marketplace-purchase.ts` that the `sales_offers` insert (state
  "accepted") + SSE broadcast happen unconditionally before the paid (Stripe) path even starts, and
  every failure exit in that path (Stripe not configured, no saved customer id, no default payment
  method, non-active/trialing subscription, non-succeeded PaymentIntent, thrown exception) leaves that
  row terminally "accepted" with no charge and no audit log. Fix: move the insert + broadcast to occur
  only after Stripe confirms the charge succeeded, for the paid branch only (free path is unaffected
  since it never risks a failed charge).
- 2026-09-11T04:35:00Z ✅ DONE — Refactored `POST /msp/customers/:customerId/marketplace/checkout` in
  `msp-marketplace-purchase.ts`: the `sales_offers` insert + SSE broadcast are now pulled into a shared
  `recordAcceptedOffer()` helper called (a) immediately in the free ($0) path (no Stripe risk) and (b)
  only after Stripe confirms a successful PaymentIntent/subscription in the paid path — every failure
  exit now returns without ever writing a "accepted" row. Also handled the inverse edge (charge
  succeeds but the post-charge insert itself fails) by logging a CRITICAL reconciliation line instead
  of telling the client the purchase failed (would invite a double charge on retry). `tsc --noEmit`
  clean. `vitest run msp-marketplace-purchase.test.ts`: 14/14 passing — all 11 pre-existing tests still
  pass unchanged, plus 3 new regression tests asserting `db.insert` is never called on a failed
  PaymentIntent, a missing Stripe customer id, or a thrown Stripe error. Verified branch is merged into
  origin/main (`verify-branch-merged.mjs` — OK) and `git status --porcelain` is clean.
  Also found and filed the identical bug pattern in `portal-checkout.ts` (the customer's own self-serve
  checkout, not in #3400's scope) as **#3633**, parented under #1485 (EPIC: Portal), labeled `bug`,
  board status set to "AI Batter Up".
