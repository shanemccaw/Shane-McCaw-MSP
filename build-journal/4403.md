# #4403 — Monitoring: provision entitlement/tier on payment-confirmed

- **Status:** ✅ DONE 2026-09-16T19:13:30Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 30b401066 (schema + migration), 5faffeb26 (provisioning + wiring + live-db test)

## Log
- 2026-09-16T18:54:27Z ⏳ IN FLIGHT — provision a real `client_services` row (status='active', service_id → services.service_type='monitoring_tier' matching purchased tier) in `/api/public/purchase/payment-confirmed` per #4402's traced contract, idempotent across repeat calls for the same PaymentIntent.
- 2026-09-16T19:13:30Z ✅ DONE — `ensureMonitoringEntitlement` (artifacts/api-server/src/lib/monitoring-entitlement-provisioning.ts) writes the #4402-contract row (client_services status=active, client_user_id=session.accountUserId, service_id → services.service_type='monitoring_tier'). Called from /payment-confirmed (account-first buyers), set-password `ok` (legacy pay-then-account buyers), and portal-handoff (backstop). Idempotency: new nullable `client_services.checkout_session_id` + unique (checkout_session_id, service_id), with ON CONFLICT DO NOTHING. Migration `lib/db/migrations/manual/4403-client-services-checkout-session-id.sql` run locally; Replit line added to #1630. Verification: api-server tsc 0 errors; new live-db test (4 tests) + existing payment/kickoff/account-flow suites all pass (25 + 65 tests); LIVE on :8080 at 5faffeb26 with a real Stripe test-mode Premier SMB purchase ($1,210): GET /api/portal/security-plan went 402 TIER_UPGRADE_REQUIRED → 200, 1 row after the first confirm, still 1 row after 3 concurrent duplicate confirms. Synthetic rows cleaned up. Live run used an already-paid session to avoid real Graph mail, so the unchanged mark-paid/mail block didn't run in it (the live-db test covers the consented→paid path). Finding filed: #4430 (serving-checkout.mjs process-table JSON parse falsely reports NOT LIVE; parented to #1202 because it's dev-server tooling, not purchase fulfillment). Retainer (#4404) reuses the new column.
