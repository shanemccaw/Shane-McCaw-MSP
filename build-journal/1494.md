# #1494 — Microsoft Changes: contract extraction pack for Claude Design

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 4c5c5495e (agent/1494-q781), merged to main as 42d5bb2ce

## Log
- 2026-08-29 ⏳ IN FLIGHT — Read-only contract extraction pack per #1577's method, scoped to
  Microsoft Changes (#1494), step 3 of #1578's per-module sequence. #1530-#1533 (roadmap
  ingestion, roadmap↔MC join, interpretation layer, resolution layer) are shipped on main.
  Extracting the wire contract from portal-message-center.ts and the four sub-issues' code,
  producing docs/microsoft-changes-contract-pack.md. No product code, no schema changes, no UI.
- 2026-08-29 ✅ DONE — Shipped docs/microsoft-changes-contract-pack.md (450 lines): per-surface
  wire contract for all three live surfaces (customer portal route, AdminV2 interpretation/
  resolution route, the sync jobs), CURRENT-vs-DECIDED table with issue numbers, real enum
  unions pulled from the Drizzle schema, the honest-empty tri-state (loading / live-genuinely-
  empty / read-failed) — flagging that the frontend currently conflates "live, genuinely empty"
  with "never connected", the forbidden-field list off `useMessageCenter.ts`'s `toPost` mapper,
  and the cross-module read-only edge (sync jobs are the sole writer; page actions write to
  change control/hold windows elsewhere, never here). While building it, found two live,
  tested endpoints (`POST/GET .../:id/resolve` and `.../:id/resolutions`) with no caller
  anywhere in AdminV2 — filed and attached as a real sub-issue, #1615, under #1494 at pack
  time, per the standing convention. Merged commit 4c5c5495e onto origin/main as 42d5bb2ce;
  verified via `verify-branch-merged.mjs` before writing DONE.
