# #78 — Chats panel epic-row: show real GitHub issue number

- **Status:** ✅ DONE 2026-09-10T00:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** none — investigation-only, feature already present in the live code path

## Log
- 2026-09-10T00:00:00Z ⏳ IN FLIGHT — dispatched prompt (unrelated to Git #78's actual
  body, which is an "Add EngageBay API" spec — the build header's task text is what
  was actually followed) asked to make the Chats panel epic-row show its real GitHub
  issue number next to the title, matching the Git Board / Build Watch convention,
  while keeping title truncation/ellipsis.
- 2026-09-10T00:05:00Z ✅ DONE (no code change) — read the live epic-row renderer,
  `BuildEpicSection` in `desktop/BuildConsole/Controls/LeftSidebar.xaml.cs`
  (this is the active card-list path `RenderChatsTree`/`ChatsHost` builds every
  render; the older `ChatsTree`/`TreeViewItem` path is explicitly dead — see the
  comment at line 3246: "Card list is rebuilt on every render. Keep the (hidden)
  legacy tree empty."). `BuildEpicSection` already renders a `#{githubNumber}`
  `TextBlock` (lines ~3553-3558) docked to the right of the epic title, added
  before the title's own `TextBlock` (which has `TextTrimming.CharacterEllipsis`
  and no explicit width, so it fills/truncates within the remaining DockPanel
  space and the number badge stays visible regardless of title length) — this is
  exactly the requested behavior.
  Confirmed via `git log` that this was a prior, already-merged fix:
  commit `411183f97` "Chats tree epic-row: show real GitHub issue number"
  (2026-08-14) is an ancestor of this worktree's HEAD
  (`git merge-base --is-ancestor 411183f97 HEAD` → true). That commit actually
  targeted the legacy `TreeViewItem`-based `BuildChatEpicHeader`/`ChatsTree`
  renderer; the current live `BuildEpicSection` card renderer independently
  carries the same `#{githubNumber}` badge already, so the live UI already
  matches the requested convention. No gap found, no code change made.
  Working tree left clean (`git status --porcelain` — nothing touched).
