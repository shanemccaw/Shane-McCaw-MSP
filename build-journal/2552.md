# #2552 — Notification Preferences: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T15:55:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** `b1835acbc` (IN FLIGHT bookend), `bf6011f33` (docs/notification-preferences-contract-pack.md) — merged into `origin/main` at `2f6ae595c`, verified via `verify-branch-merged.mjs`.

## Log
- 2026-09-03T15:30:00Z ⏳ IN FLIGHT — Read-only extraction pass, #1642/#1577 contract-pack method, for Feature #1662 (Notification Preferences, under Epic #1485). Deliverable: `docs/notification-preferences-contract-pack.md`, extracting `artifacts/api-server/src/routes/notification-preferences.ts`, `lib/db/src/schema/index.ts` (`customer_notification_preferences`), and `artifacts/api-server/src/lib/notification-center.ts` (the real preference-gating consumer) verbatim to file:line, cross-checked against the archived page (`portal-archive-2026-08-29:artifacts/msp-portal/src/pages/customer-notifications.tsx`) and live-queried against local Postgres. Per #2552's own default, scoped to the notification-preferences surface only — Webhooks already has its own pack (`docs/webhooks-contract-pack.md`, #1597) and is referenced, not re-extracted. No UI or schema changes planned. Will post a summary comment on #2552 before flipping to DONE.
- 2026-09-03T15:55:00Z ✅ DONE — Shipped `docs/notification-preferences-contract-pack.md` (commit `bf6011f33`, verified ancestor of `origin/main` at `2f6ae595c`). Read-only; no product code or schema touched. Key findings: the route has no named `Wire*` interface (extracted from its two inline object-literal responses instead); `GET` always returns all 15 `KNOWN_CATEGORIES` regardless of what's stored (no loading/empty distinction is backend-expressible); `PATCH`'s `category` field is unvalidated against the known list (open gap, not filed — low severity, single-tenant-scoped write); the route applies no role/tenant scoping beyond `requireAuth`, unlike Webhooks' `resolveOwner()` (harmless today since only `customer_user` recipients are ever gated by this table); a real, previously-undocumented cross-surface edge confirmed by direct code read — a customer's webhook subscribed to `notification.<category>` never fires for a category that customer has `inAppEnabled: false` for, because `fanOutToCustomerWebhook` is only reached after that check passes in `createNotification`; `customer_alert_preferences` (#1236/#1278) is a `DECIDED`, deliberately separate taxonomy per an existing schema comment. Local Postgres confirms `customer_notification_preferences` genuinely empty (0 rows) today. Summary comment posted on #2552 (not closed). No sub-issues filed — the two gaps found are low-severity/harmless-today observations, documented in the pack's §11 summary table for Shane to triage rather than filed as standalone bugs.
