# #2544 — GATE card shows every GATE:-titled issue, not just the top-level release gate

- **Status:** ✅ DONE 2026-09-03T00:20:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** ae74b87ed (bookend), 6fdfcc3e1 → pushed to main as 84b60972c

## Log
- 2026-09-03T00:00:00Z ⏳ IN FLIGHT — filtering RenderGateCards (LeftSidebar.xaml.cs, Git #2538) to only include GATE:-titled issues with ParentNumber == null, so #1269/#1918 (real sub-issues of #1281) no longer render as peer cards.
- 2026-09-03T00:20:00Z ✅ DONE — `RenderGateCards` now filters `_lastBoardIssues` on `i.ParentNumber == null` in addition to the existing `IsGateTitle`/open/milestone checks, reusing the app's existing native sub-issue `ParentNumber` field (no new heuristic). In the real v1.1 milestone this means only `#1281` (parent: none) renders as a GATE card; `#1269` and `#1918` (real sub-issues of `#1281`) are excluded and already counted in `#1281`'s own `SubIssueCompleted`/`SubIssueCount` rollup fraction shown on its card. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean (0 warnings, 0 errors) both before and after rebase onto main. Branch verified merged into origin/main via `verify-branch-merged.mjs` (commit 84b60972c). Working tree clean.
