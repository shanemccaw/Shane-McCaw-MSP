# Git #2477 — Build Chain Map: canvas layout + node rendering

- **Status:** ✅ DONE 2026-09-03T00:26:00Z
- **Scope:** buildconsole
- **Branch/worktree:** agent/2477-q1314 (C:\wt\2477-q1314)
- **Dispatch:** Posted: 2026-09-02T22:58:28Z (BUILD comment on #2477, buildSet=BuildChainMap)
- **Commits:** e17555500 (work), 4e375d81c (IN FLIGHT bookend)

## Log

- ⏳ IN FLIGHT 2026-09-02T23:59:30Z — Session start. Blockers #2475 (ChainDoc read layer) and
  #2476 (ChainRules §5 engine) both closed/`complete` — unblocked. Scope: auto-layout canvas per
  `desktop/BuildConsole/BuildMap/README.md` "Canvas geometry" constants table + node rendering
  (Epic card, Feature header collapsed 204×94 / expanded 412×94, issue rows, sentinel card, gate
  pill), zoom 0.35–1.6 with Fit. Edges are #2478, inspector #2479, interactions #2480, top bar
  #2482, toolbar wiring #2483 — not this build. Plan: pure layout math in
  `Services/BuildMap/ChainLayout.cs` (ports included for #2478), rendering in
  `Controls/BuildMap/ChainCanvasControl` off the real ChainDoc/ChainRules types from #2475/#2476.
- ✅ DONE 2026-09-03T00:26:00Z — Shipped in e17555500:
  `Services/BuildMap/ChainLayout.cs` (exact port of the prototype's `layout`/`outPort`/`inPort`/
  `zoomBy`/`fit` with the README constants table verbatim; ChainPort/ChainRect/ChainLayoutResult
  for #2478's edge layer) and `Controls/BuildMap/ChainCanvasControl.xaml(.cs)` (dot-grid stage,
  epic-tree rail, Epic card, Feature headers with stacked state bar + feature-state text, issue
  rows, sentinel cards with fan-in counts, gate pills incl. manual dashed gutter line, selection
  emphasis borders, expand/collapse, Ctrl+wheel zoom, ExpandAll/CollapseAll/ZoomIn/ZoomOut/Fit
  API, EdgeLayer host + Rendered/SelectionChanged/ZoomChanged/GatePillClicked events for
  #2478–#2482). Verification: `dotnet build` clean (0 warnings, 0 errors; ChainCanvasControl.baml
  confirmed in obj output); all 6 Lucide icon geometries validated through real WPF
  `Geometry.Parse`; layout math verified numerically via a throwaway console harness compiling
  the real ChainLayout/ChainRules/ChainDoc sources — 15/15 assertions matched hand-computed
  README-table values (head/row/sentinel rects, stage 980×292, collapsed-feature node omission,
  fit/clamp maths, out/in ports incl. collapsed-header stand-in); harness deleted after the run.
  Not run live in-app: nothing routes to the control until #2483 wires the toolbar entry — a
  live visual pass against `BuildMap/screenshots/` belongs to that wiring build. Known WPF
  fidelity gap noted in-code: TextBlock has no letter-spacing, so eyebrow tracking (.09–.13em)
  is dropped. No findings filed — no new issues discovered. verify-branch-merged: OK
  (e1755550 is an ancestor of origin/main); `git status --porcelain` clean.
- ✅ DONE (retry verified) 2026-09-03T01:05:00Z — A retry dispatch (queue build 1323) re-ran this
  issue. Verified against origin/main before doing anything: ChainLayout.cs +
  ChainCanvasControl.xaml(.cs) present on main, this bookend already DONE with real hashes, #2477
  closed and `complete`-labeled, completion comment posted. No code changed; nothing to redo.
