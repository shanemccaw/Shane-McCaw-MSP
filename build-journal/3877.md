# #3877 — Notification Tray 1/4: new NotificationHistoryStore — persisted history + bookmark state

- **Status:** ✅ DONE 2026-09-13T00:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 8ee1f7d9175f396cd1c7f4b038bfb911073bbeed

## Log
- 2026-09-12T23:45:00Z ⏳ IN FLIGHT — implementing `Services/NotificationHistoryStore.cs`, same static/%APPDATA%/BuildConsole JSON persistence pattern as `ChatUrlStore`/`BuildSetPriorityStore`/`QuickThoughtStore`: `Add`, `SetBookmarked`, `ClearAllNonBookmarked`, `All`/`Bookmarked` accessors, rolling cap that exempts bookmarked entries.
- 2026-09-13T00:35:00Z ✅ DONE — `NotificationHistoryStore.cs` added, `dotnet build` clean (0 warnings/errors). Verified via a standalone scratch console app (not checked in, cleaned up after use): Add x3, bookmark one, `ClearAllNonBookmarked()` leaves only the bookmarked entry (confirmed by Id + `Bookmarked==true`), and a fresh process reload from the persisted JSON reproduces the same bookmarked survivor — restart persistence confirmed. Working tree left clean (`git status --porcelain` empty). Branch verified merged into `origin/main` at 8ee1f7d9. Commit 8ee1f7d9175f396cd1c7f4b038bfb911073bbeed.
