# #2660 — MSP Staff Roles and Onboarding: regenerate the contract pack from the finished backend (#1642 pattern)

- **Status:** ✅ DONE 2026-09-03T17:05:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** cd2e7b0ec (pack), 8327adb3e (IN FLIGHT bookend), 3adc5cb2 (merge → origin/main)

## Log
- 2026-09-03T16:10:00Z ⏳ IN FLIGHT — Pulled #2660 body + dispatch comment + Feature #2574
  (scope: "Real scoped accounts and RBAC for people joining the MSP itself, not customer
  data"). Dispatch comment names `msp-staff.ts` (2 routes: impersonate-customer,
  fulfillment-queue) + `msp-staff-search.ts` (1 route: cross-domain search) as "confirmed
  real backend." Read both files + their tests — neither is actually a Staff
  Roles/Onboarding surface: `msp-staff.ts` is customer-impersonation + fulfillment-queue
  triage, `msp-staff-search.ts` is a Cmd+K search helper. Found the real matching backend:
  `msp-settings.ts`'s own header doc block names it directly — "Team / Users: GET
  /msp/settings/users, PATCH /msp/settings/users/:userId/role, DELETE
  /msp/settings/users/:userId" — plus the fuller live route list (customer-scopes GET/PUT,
  approve-purchases, reset-password/temp-password/reset-mfa, mfa-enforcement, status,
  sessions, and the invites POST/GET/DELETE block) is the real staff-account +ONBOARDING+RBAC
  surface for this Feature. Writing the pack against the real matching surface
  (`msp-settings.ts`'s Team/Users + Invites block, ~14 routes) as the primary subject,
  citing `msp-staff.ts`/`msp-staff-search.ts` as real adjacent MSP-staff-facing surfaces
  (not roles/onboarding) in a cross-surface note. Will file the file-identification mismatch
  as a sibling sub-issue of #2574 per the finding rules.
- 2026-09-03T17:05:00Z ✅ DONE — Wrote
  `docs/msp-staff-roles-and-onboarding-contract-pack.md` (~475 lines, #1642 pattern): 19
  real routes across `msp-settings.ts` (Team/Users + Invites block, `:535-993, :1526-1770`)
  and `msp-onboarding.ts`'s public accept-side (`:369-672`); per-route wire contracts cited
  to file:line; real enum unions (MSP_ROLES vs ROLE_ORDER, the 2-value invitable-role subset,
  derived 3-state invite lifecycle); cross-surface edges incl. the real relationship to
  `msp-staff.ts`/`msp-staff-search.ts` (real but a different Feature's surface, not this
  one's); orphaned-endpoint sweep (all 19 routes, zero consumers, `artifacts/msp-console`
  confirmed not to exist); live DB state via local `DATABASE_URL` (2 MSPs, 9 users carrying
  mspId, 0 MSPAdmin/MSPOperator, 0 invites, 0 staff customer-scope rows — genuine cold start).
  Found and filed 2 real, evidenced bugs: `PATCH .../mfa-enforcement` and
  `DELETE .../users/:userId/sessions` (`msp-settings.ts:923-940, :971-993`) both write only
  an audit-log row and return a false-success payload — neither persists its claimed effect
  (confirmed against the real, working customer-side analogs `portal-team.ts:337` and
  `portal-team.ts:81`). Filed as **#2723** (milestone v1.1, labels `bug`+`security`, parented
  as sub-issue of Feature #2574, board status set to "AI Batter Up"). Read-only pass — no
  product code, schema, or UI changed; only the pack + this bookend were written. Merged
  `agent/2660-q1418` into `origin/main` (3adc5cb2) after a clean `git merge origin/main`;
  verified with `verify-branch-merged.mjs` (exit 0) before writing DONE. `git status
  --porcelain` clean.
