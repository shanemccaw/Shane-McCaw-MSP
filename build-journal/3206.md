# #3206 — Feature: Money — Plaid-inferred paid status, not a checkbox (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T01:15:00Z
- **Scope:** platform (web/shanes-life)
- **Started:** 2026-09-08
- **Commit(s):** `4ada44d5b`

## Log
- 2026-09-08 ⏳ IN FLIGHT — Reading #3206 + `Shanes Life 17 - Money v3.dc.html` (Turn 2,
  option 2a + the Mapping flag: "Paid ✓ can't be a checkbox. It's inferred when Plaid
  sees the debit leave the bill account."). Investigating the real, current
  `src/core/money.mjs` gate-status implementation (#3137/#3147/#3148) before deciding
  what, if anything, actually needs to change.
- 2026-09-08 ✅ DONE — Investigation answer, confirmed rather than assumed: `bill.funded`
  was already exactly the real Plaid inference (`shortfall === 0` off the account's real
  `current_balance` vs `target_amount`; `current_balance` is only ever written by
  ShanesSurvival's own Plaid sync). Grepped `src/` and `public/` end to end for any manual
  paid checkbox/toggle/route — none exists. `last_paid_date` is the only manual bill field
  and was already excluded from this math (migration 011's own comment). So the real gap
  was narrower than the issue title implied: the design's own example row ("Electric H2 ·
  due today · $190.27 in ···1523 for $52") shows the masked account number backing the
  inference, and this app had never surfaced it despite already having it
  (`accounts.mask`, migration 043, real Plaid-populated). Added `mask` to
  `loadRoleAccounts`' SELECT, threaded through `billFromAccount`/`billOut` as `masked`
  (same "•••• 1234" convention `getAccountsOverview` already uses), and wired it into
  `moneyBillMeta` so every bill row (Now tab's Protected/Urgent/Already handled, and the
  Bills tab) shows which real account and how much is actually in it. No migration
  needed — `mask` already existed as a column, this only added it to one SELECT.
  Verified live against the real local `finances` database: a scratch script (not
  committed) confirmed `funded` matches real `balance >= target` for every one of
  Shane's 11 real bill accounts today (none currently funded — real data, not a fixture).
  A synthetic unit check via `computeGateMath` with a mocked $190.27-in-account-for-$52
  bill reproduced the design's own example exactly: `funded: true`,
  `masked: "•••• 1523"`. `node --check` passes on both touched files. Commit `4ada44d5b`,
  rebased cleanly onto a concurrent #3211 landing (different section of the same
  `app.js`), pushed to `origin/main`, confirmed merged via
  `verify-branch-merged.mjs`. Working tree clean.
