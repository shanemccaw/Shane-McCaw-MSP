# Git #3078 — recordSignalTransitions can open the same signal twice

- **Status:** ⏳ IN FLIGHT 2026-09-07T16:12:00Z
- **Scope:** platform
- **Branch:** agent/3078-q1856

## Log

- ⏳ IN FLIGHT 2026-09-07T16:12:00Z — Starting. `recordSignalTransitions`
  (`artifacts/api-server/src/lib/tenant-signals.ts`) reads "which signals have an open
  row" then inserts for the difference, with no unique constraint or idempotency key
  behind the read, so two overlapping evaluations of the same customer both insert.
  Plan: partial unique index on `(customer_id, signal_key) WHERE resolved_at IS NULL`
  (Drizzle schema + manual migration run locally), dedupe the existing duplicate open
  rows first, and make the INSERT an explicit `ON CONFLICT ... DO NOTHING`.
