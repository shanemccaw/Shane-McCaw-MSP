# #3826 — Command palette: Git Pull's real result text should show in the palette's own right pane

- **Status:** ✅ DONE 2026-09-12T21:15:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 0ea7ed0df (implementation), 488a7939b (IN FLIGHT bookend)

## Log
- 2026-09-12T20:56:00Z ⏳ IN FLIGHT — investigating CommandPaletteWindow, MainWindow.CommandPalette.cs, LeftSidebar RunGitPullAsync/RunGitCommandCore, and checking Paste Manual Build / Recover Builds for the same gap.
- 2026-09-12T21:15:00Z ✅ DONE — `LeftSidebar.GitCommandResult` made internal, added `RunGitPullWithResultAsync()` returning the full Stdout/Stderr/ExitCode. `BuildQueuePanel.RecoverOrphanedBuildsWithResultAsync()` added (Recover Builds had the exact same discarded-result gap — its summary/failure text only ever reached a toast); `RecoverOrphanedBuildsAsync()` now delegates to it, unchanged for other callers. `CommandPaletteWindow.PaletteCommand` gets a new `RunWithResult` hook: the palette stays open (doesn't close-then-fire like plain `Run`), shows "Running…", awaits the real result, and renders it in place of `DetailBody` — wired for both the results-list Enter/double-click path and the quick-action tile click path. `MainWindow.CommandPalette.cs` wires Git Pull and Recover Builds through it; existing toasts kept (not replaced), per the issue. Checked Paste Manual Build — it opens the real Edit Build Prompt dialog synchronously with nothing to discard, so no gap there; left unchanged. `dotnet build BuildConsole.csproj -c Debug` — 0 warnings, 0 errors. `verify-branch-merged.mjs` — OK, merged into origin/main. `git status --porcelain` clean.
