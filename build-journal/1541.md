# #1541 — Remediation: CR gate — diff before approval, executable after

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** 52b6cf516 (bookend IN FLIGHT), ef41632cf (real work — real post-push hash, verified an ancestor of origin/main)

## Log
- 2026-08-30 ⏳ IN FLIGHT — diff (pre/post JSON) is already unconditional on the CR
  register; building the gate for the executable half — a structured CR↔checkKey
  link, a pure reveal-authorization rule, a `script_revealed` cr_events type, and
  the gated reveal route for a `you_must_run` fix's PowerShell.
- 2026-08-30 ✅ DONE — ef41632cf. Confirmed the diff half (`preChangeSnapshot`/
  `proposedPayload`) already ships unconditionally on `portal-change-control.ts`'s
  register — nothing to change there. Built the executable-gate half:
  - `msp_change_requests.remediation_check_key` (+ index) — structured CR↔checkKey
    link, additive migration `lib/db/migrations/manual/2026-08-30-remediation-cr-gate-1541.sql`,
    run against local `DATABASE_URL`. Logged to #1630.
  - `cr_events.event_type` gains `script_revealed` (TS-enum only, no DDL —
    verified live the column carries no CHECK constraint).
  - `remediation-reveal-gate.ts`: pure `evaluateRevealAuthorization` (fail-closed:
    no CR → withheld; CR unapproved → withheld; rejected → withheld; approved →
    authorized and stays authorized through execution/rollback) + real-DB
    `findRevealCandidates`/`recordScriptReveal`.
  - `POST /api/portal/remediation/fix-routes/:checkKey/reveal` — serves the
    verified PowerShell steps only once an approved CR authorizes it; records
    the reveal as a `script_revealed` cr_events row every time (not proof of
    execution — the record that we told them).
  - `we_can_run` execution path needed no new gate — already fail-closed by
    #1497's `change-control-write-gate.ts`.
  - Wizard's `POST /portal/change-control` accepts optional `remediationCheckKey`
    so a future "raise a change for this item" affordance can populate the link;
    exposed `remediationCheckKey` on the register's wire read for parity with
    `linkedFinding`.
  - Ends at the wire contract — no `artifacts/portal` page (no design export for
    this module).

  **Verification (real, observed):** `remediation-reveal-gate.test.ts` — 13
  passing (pure truth table + real-DB round trip against local Postgres,
  cleaned up in `afterAll`; live count confirmed `msp_change_requests`/
  `cr_events` both 0 rows before and after). `lib/db` tsc clean; api-server tsc
  clean on all 5 touched files (56 pre-existing unrelated errors elsewhere,
  matching #1765/#1772/#1774/#1777/#1778/#1781 — not mine). Branch verified an
  ancestor of `origin/main` (`verify-branch-merged.mjs`) after rebasing
  through a real merge conflict with the concurrently-landed #1538 (kept both
  sides' additions in `vitest.config.ts`/`routes/index.ts`). Working tree
  clean.

  **Finding filed:** #1941 (parented to #1489, board = AI Batter Up) — nothing
  in the codebase yet raises a CR from a remediation item, so
  `remediation_check_key` has no real row to ever populate until that action
  exists.
