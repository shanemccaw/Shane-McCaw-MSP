# #3565 — Feature: generate_script node -> human-in-the-loop hand-off (remove direct Anthropic call)

- **Status:** ⏳ IN FLIGHT 2026-09-11T00:33:17Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-11T00:33:17Z ⏳ IN FLIGHT — Investigated #3541 (no directly-reusable changeRequestId pattern; that's a
  different system). Found the real reusable mechanism instead: `approval_gate` /
  `break_glass_verification_gate` in workflow-executor.ts already pause a run via the
  `pauseForApproval` sentinel + `wf_runs.status = "awaiting_approval"`, and a single generic
  `resumeWorkflowRun(runId, nodeId, resumePayload, decisionNote)` resumes BFS from the gate's
  successors. Designing `generate_script`'s hand-off on top of that same mechanism: a new
  `pending_script_handoffs` table + admin routes to list/complete/cancel, reusing the existing
  manual `POST /admin/ps-scripts[/packages]` routes for Shane to actually save the
  Claude-Code-generated, tenant-verified script. Now implementing the executor change, routes,
  and admin-panel UI.
