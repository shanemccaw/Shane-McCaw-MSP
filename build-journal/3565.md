# #3565 — Feature: generate_script node -> human-in-the-loop hand-off (remove direct Anthropic call)

- **Status:** ✅ DONE 2026-09-11T01:05:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 95eba2597 (pending_script_handoffs table), 2fae890b7 (executor + routes + admin-panel UI + docs)

## Log
- 2026-09-11T00:33:17Z ⏳ IN FLIGHT — Investigated #3541 (no directly-reusable changeRequestId pattern; that's a
  different system). Found the real reusable mechanism instead: `approval_gate` /
  `break_glass_verification_gate` in workflow-executor.ts already pause a run via the
  `pauseForApproval` sentinel + `wf_runs.status = "awaiting_approval"`, and a single generic
  `resumeWorkflowRun(runId, nodeId, resumePayload, decisionNote)` resumes BFS from the gate's
  successors. Designing `generate_script`'s hand-off on top of that same mechanism: a new
  `pending_script_handoffs` table + admin routes to list/complete/cancel, reusing the existing
  manual `POST /admin/ps-scripts[/packages]` routes for Shane to actually save the
  Claude-Code-generated, tenant-verified script. Now implementing the executor change, routes,
  and admin-panel UI.
- 2026-09-11T01:05:00Z ✅ DONE — Shipped in two commits. `95eba2597`: `pending_script_handoffs`
  Drizzle schema + `lib/db/migrations/manual/2026-09-10-pending-script-handoffs-3565.sql`, run
  against local `DATABASE_URL` in-session (confirmed table + FKs via `psql \d`). `2fae890b7`:
  `generate_script`'s executor case rewritten to pause the run (same `pauseForApproval` sentinel
  approval_gate uses) instead of calling `generateScriptFromService`/`generateScriptFromDocument`
  (removed that import entirely — those two functions and the manual
  `POST /admin/ps-scripts[/packages]` routes are unchanged and still work; only the automated
  workflow node changed); new `GET/POST /admin/workflows/pending-script-handoffs[...]` routes
  (list/complete/cancel) in admin-workflows.ts, resuming via the existing `resumeWorkflowRun()`
  with the same `{ scriptId, packageId, title, category }` shape the node used to produce inline;
  new Script Hand-off banner on admin-panel's RunDetailPage.tsx; `node-type-registry.ts` flipped
  `generate_script` to `isAIDependent: false` (no longer gates on MSP AI balance, correctly, since
  it makes no AI call); nodeLibrary.ts / workflow-node-reference.md / architecture-overview.md
  copy updated to describe the hand-off instead of the removed AI call.

  Verification: rewrote `workflow-executor-generate-script.test.ts` for the new pause behavior (8
  tests, passing) plus confirmed no regressions in `node-type-registry.test.ts`,
  `ai-usage-metering.test.ts`, `config-pack-graph.test.ts`, `ps-script-gen.test.ts`, and
  `workflow-executor-content.test.ts` (58 tests, includes a `generate_script` dry-run case —
  unaffected since dry-run for this node was never touched). `api-server` and `admin-panel` both
  `tsc --noEmit` clean (0 errors, exit 0) after rebuilding `lib/db/dist`. Confirmed the new table
  + its FKs directly against local Postgres. Did NOT live-verify the new HTTP routes against the
  running dev server — it's still serving the pre-merge `main` checkout (confirmed: existing
  `/api/admin/workflows/pending-approvals` returns 401, my new
  `/api/admin/workflows/pending-script-handoffs` returns 404) because this build is part of the
  `GenerateScriptHumanHandoff` buildSet, which defers the merge+restart until every member of the
  set completes — did not force a manual restart to avoid disrupting that coordination.

  Finding filed: #3596 (Test Run / dry-run falsely reports "unknown node type" for
  `approval_gate`, `break_glass_verification_gate`, and 5 monitor/remediation node types — a
  pre-existing dead-code bug in the `STRUCTURAL_TYPES` dry-run guard, found while confirming
  `generate_script`'s own dry-run path was unaffected). Parented under #3561, labeled `bug`,
  board status set to AI Batter Up.

  Branch verified merged into `origin/main` (`verify-branch-merged.mjs` — OK). Working tree
  clean (`git status --porcelain` — empty).
