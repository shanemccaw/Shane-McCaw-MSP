# #3638 — fix stale requireAuth.ts:80-93 citation in ladderCapabilityDescription()

- **Status:** ✅ DONE 2026-09-12T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Starting: fix stale source citation in `ladderCapabilityDescription()`
  (legacy-ladder.ts:293-301) and reseed the affected `rbac_capabilities` rows, mirroring #3571's
  fix to `LEGACY_CAPABILITY_RULES[].source`.
- 2026-09-12T00:20:00Z — ✅ DONE — Fixed `ladderCapabilityDescription()` and the identical
  string built in `lib/db/migrations/manual/2026-09-09-rbac-seed-current-model-2457.sql` to
  cite `requireAuth.ts:115-123, 135-138 as of 3dddd4b26^ (ROLE_ORDER, roleIndex, pre-#2460)`
  instead of the stale `80-93` (retired-comment block post-#2460), same correction #3571
  already applied to `LEGACY_CAPABILITY_RULES[].source`. Re-ran the seed SQL against local
  Postgres (`DATABASE_URL`) — the seven `rbac_capabilities` ladder rows now carry the
  corrected description (verified via `SELECT`). `legacy-ladder.test.ts`: 34/34 passed.
  Commit `cc9ab879` (rebased/pushed directly onto `origin/main`, verified merged via
  `verify-branch-merged.mjs`). `git status --porcelain` clean.
