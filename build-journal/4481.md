# #4481 — 7 core:premier checks never succeed (5 Graph 400s, 2 PowerShell)

- **Status:** ✅ DONE 2026-09-17T05:39:50Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 2ade7c058 (Graph beta endpoints + mailbox-quota mapping migration + run-checks-4481.ts), b9fc1f463 (ps-execution container fixes), c582b5a47 (IN FLIGHT bookend)
- **Migration:** `lib/db/migrations/manual/4481-premier-never-succeeded-checks.sql` — run against local DATABASE_URL (6 UPDATEs); recorded on #1630
- **Findings filed:** #4503, #4504, #4505 (all parented under #1489, AI Batter Up)

## Log
- 2026-09-17T05:08:00Z ⏳ IN FLIGHT — investigating the 5 Graph "Resource not found for the segment" checks against Microsoft Learn, then the DLP rule-package exit 137 and mailbox-quota-utilization cmdlet error (build 3129)
- 2026-09-17T05:39:50Z ✅ DONE — Root causes, each confirmed against Microsoft docs and live evidence:
  - **5 Graph checks** (cloudLicensing allotments/assignments/assignmentErrors, app proxy connectorGroups, externalIdentitiesPolicy): all beta-only per Microsoft Learn (graph-rest-beta moniker only), but stored as relative endpoints, which graphFetchPaginated prefixes with v1.0. Live app-only probe on the testbed: v1.0 returns 400 with the exact scan error; beta returns 200 on all five. Fix: absolute beta URLs (migration). None retired.
  - **exchange:mailbox-quota-utilization**: container Log Analytics showed `[Deserialized.Microsoft.Exchange.Data.ByteQuantifiedSize] does not contain a method named 'ToBytes'` on every run. EXO V3 returns deserialized objects without methods (Microsoft Q&A 1921124). Fix: parse the "(N bytes)" string. Also fixed the check's mapping, which counted a field (TotalItemSize) the script never emits (would always be 0), and added the missing severity rule (warn if any mailbox is at or above 90% of quota).
  - **compliance:dlp-rule-package-invalid**: exit 137 was an OOM about 2 min after the cmdlet returned. #2852's key sanitizer walked the rule pack's multi-MB SerializedClassificationRuleCollection byte[] element by element in the 1Gi container. It worked on 09-08/09-09 and broke with the 09-15 dev image. Fix: byte[] passes through the sanitizer whole (4MB: 1167ms → 84ms locally; JSON shape unchanged), and a new catalog ExcludeProperties key drops the two unused rule-pack blobs. No memory-limit raise needed.
  - **Live verification**: deployed to ca-ps-execution-dev only (revision dev4481b). Via the new `src/scripts/run-checks-4481.ts` (real executeMonitorCheck, persistProfile:false), all 7 return status=ok. Mailbox: 4/4 quotas and sizes parsed, max 0.5% (container log). DLP: 25s, IsValid present. Full core:premier scan run 9f37acc2: checksError 0 (was 7), 166 ok / 19 license_gap / 11 service_not_configured / 2 partial.
  - **Honest gap**: the run still ends `partial`, because two site-sharing fan-out checks 404 on one site with no document library on every run (predates this work) → filed #4505. Production ca-ps-execution is still v11 (2026-08-06) and was NOT redeployed (production gate #1913). Redeploy plan posted on #1281. Also filed #4503 (app proxy: app-only access documented as unsupported, and the info finding fires wherever App Proxy is disabled) and #4504 (DLP rule package Name is empty; should key on RuleCollectionName).
  - verify-branch-merged OK; git status clean.
