# #2881 — api-server: process guards missing (no typecheck gate, no route-index boot smoke test)

- **Status:** ✅ DONE 2026-09-06T14:04:55Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** e2261aad3 (main) — "api-server: typecheck gate on build + routes/index boot smoke test (#2881)"

## Log
- 2026-09-06T13:55:44Z ⏳ IN FLIGHT — investigated: the missing-import bug itself (requireRole) is already fixed and merged (dup of #2874, closed). Real remaining scope per the issue's own body: (1) confirm/wire a real typecheck gate for artifacts/api-server, (2) add a boot smoke test that imports src/routes/index.ts and asserts it evaluates without throwing, so any route file's module-scope evaluation error is caught as a class. Confirmed #2778 already added a `pretypecheck`/`typecheck` script pair to artifacts/api-server/package.json, and the root `pnpm run typecheck` already fans out to it via the `--filter "./artifacts/**"` workspace filter — so the typecheck gate itself already exists at the root script level; there is no CI workflow (`.github/workflows/` is empty) and no husky hook in this repo to wire it into, so "runs in CI/dev flow" here means the root `pnpm run typecheck` command, which already covers this package. Proceeding to add the boot smoke test, registered in vitest.config.ts's explicit include allowlist per project convention.
- 2026-09-06T14:04:55Z ✅ DONE — Shipped two changes in `artifacts/api-server`:
  1. **Typecheck gate now actually runs in the dev/build flow, not just as a standalone script.** `#2778` had already added `pretypecheck`/`typecheck` scripts to `package.json`, but nothing ever invoked them automatically — `build` was just `node ./build.mjs` (esbuild, which does not type-check), and `dev` is `pnpm run build && pnpm run start`. Changed `build` to `pnpm run typecheck && node ./build.mjs`, so every local `pnpm run dev` restart and every deploy build now runs the existing `tsc -b` (lib deps) + `tsc --noEmit` (api-server itself) gate before the server can start — this is the actual missing wiring the issue asked for, not a duplicate of #2778's scripts.
  2. **Boot smoke test**: `src/routes/index.boot-smoke.test.ts` — imports `./index` (the ~150-route-module aggregator #2876 documents) and asserts it evaluates without throwing and exports a real Express router (`.use`/`.handle` present, not just truthy). Registered in `vitest.config.ts`'s explicit `include` allowlist per the project's known convention (new test files there are otherwise silently skipped). `testTimeout` raised to 60s for this one test via the test's own third `it()` argument, on top of the file's already-raised 20s file-level default, per #2876's note about the large import graph making a cold import here genuinely slower than a typical unit test. Manually verified `@workspace/db`'s pool construction (`new Pool({...})`) is lazy — no real connection attempt at import time — so the smoke test doesn't require a live DB or network to pass.

  **Honest verification gap**: could not actually execute the new test or re-run `tsc`/`vitest` locally this session — the shared main checkout's own `artifacts/api-server/node_modules` and `lib/db/node_modules` are completely empty (0 entries, not dangling symlinks — `store-doctor.mjs` reports CLEAN because it only scans for dangling/foreign reparse points, not zero-entry directories), so even a pre-existing already-passing test (`src/lib/retainer-hours.test.ts`) fails the same way (`ERR_MODULE_NOT_FOUND: Cannot find package 'vitest'`). Confirmed this is a real, current environment gap (not caused by this session's edits) with bounded real checks (~3 minutes, no busy-loop) before stopping, per the "genuinely unreachable" rule — did NOT run `pnpm install` to fix it, per the standing rule. Filed as **#2973** (parented under #2491, the Feature this issue's own parent, board status set to AI Batter Up) with full evidence, so whichever session resolves that gap also gets a real re-verification target for this issue's two changes. The code is correct on manual review (verified `routes/index.ts`'s actual exports/shape, `package.json` script composition, and the `vitest.config.ts` include-list convention against the existing file), but is not locally test-verified.

  Branch `agent/2881-q1719` rebased onto and pushed directly to `origin/main` (`e2261aad3`); `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
