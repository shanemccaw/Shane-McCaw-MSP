# #2369 — Panel: tag filter chips derived from the shot set

- **Status:** ✅ DONE 2026-09-02T20:12:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Shot Vault rail panel, Feature #2367 item 2 of 7)
- **Started:** 2026-09-02
- **Commit(s):** e60c589ed (bookend IN FLIGHT), 4668a6c76 (Shot Vault: tag filter chips derived from
  the shot set)

## Log
- 2026-09-02T19:47:00Z ⏳ IN FLIGHT — pulled #2369 body/comment + parent #2367 (Feature: Shot Vault)
  and the design doc (`desktop/ShaneBuilder/ShaneBuilder-FullFeature.md`). Confirmed via the panel's
  own doc-comments and `ShotVaultService.cs`/`MainWindow.ShotVaultPanel.cs` that #2369 (tag chips) had
  been left deliberately unbuilt as a real sibling issue, and that there is still no manual tag
  metadata store — sibling #2368 (search) landed mid-session and added one real piece of capture-time
  metadata (`ShotVaultItem.Screen`, encoded into the filename at capture time). Building real tag
  chips derived off data each shot already carries: its real capture screen (#2368), a date bucket off
  its own real capture timestamp, a resolution tag off its own real decoded pixel dimensions, and a
  "Changed" tag when the tile already carries #2371's real DIFF flag. No manual tag input, no fixture
  taxonomy.
- 2026-09-02T20:12:00Z ✅ DONE — `ShotVaultService.cs`: added `ShotVaultTag` record, `GetTags` (real
  per-shot tag computation: screen, date bucket, resolution, diff), `DateBucket`, `TryGetResolution`
  (cheap header-only decode, no full-size image load). `MainWindow.ShotVaultPanel.cs`: multi-select
  (AND-semantics) tag filter state, `RenderShotVaultTagChips`/`ShotVaultTagChip` (reuses Git Panel's
  `GitStateChip` active/inactive visual language), and `RenderShotVaultPanel` now filters runs/shots
  to the active chip set AND'd with #2368's existing search query, drops stale filter labels the
  current shot set no longer has, and renders an honest combined match-count status line.
  `MainWindow.xaml`: added the `ShotVaultTagChips` WrapPanel between the search box/status line and
  the shot rows. Landed on top of sibling #2368 (search) which merged into `origin/main` mid-session —
  rebased once, resolved the panel/service overlap by re-deriving my changes against the post-#2368
  base rather than a mechanical 3-way merge, and picked up `ShotVaultItem.Screen` as a fourth real tag
  kind rather than leaving it unused. `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings,
  0 errors, both before and after the final rebase/push. `node scripts/dev-server/verify-branch-merged.mjs`
  — OK, `agent/2369-q1211` (4668a6c76) merged into `origin/main`. `git status --porcelain` — clean.
  No live shots existed on this box to screenshot the chip row against real captures; verified by
  reading the render/filter path end-to-end plus the clean build (WPF app — desktop/ShaneBuilder is
  BuildConsole-class tooling, out of scope for test manifests per CLAUDE.md). Search (#2368, already
  DONE) and run grouping/DIFF (#2370/#2371, already DONE) untouched beyond the merge integration
  above. No new findings filed.
