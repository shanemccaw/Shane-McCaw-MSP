# #2812 — POST /msp/customers/bulk dead "tag" action

- **Status:** ✅ DONE 2026-09-04T00:20:00Z
- **Scope:** platform

## Log
- 2026-09-04T00:00:00Z — Pulled issue body + both comments. Shane's decision comment (2026-09-04T04:16:56Z)
  is explicit: remove the dead `action === "tag"` branch, do NOT add a `tenants.tags` column — nothing
  reads/displays tags anywhere in the live codebase.
- 2026-09-04T00:20:00Z — ✅ DONE — Removed `action === "tag"` branch from
  `artifacts/api-server/src/routes/msp-portal.ts` (dead code hitting the dropped `msp_customers`
  table on every call), its now-dead tests in `msp-customers-bulk.test.ts`, and the stale
  `tags` CSV column/comment from the `export` action. Typecheck clean on both files. Commit
  `625f8d6f7`, merged to `main` at `41539646b` (verified via verify-branch-merged.mjs). Found
  an unrelated pre-existing test failure (all 9 tests in msp-customers-bulk.test.ts fail due to
  a missing `RETENTION_CLOCK_RUNNING_TENANT_STATUSES` mock export) — filed as #2861, parented
  under #2572.
