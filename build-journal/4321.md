# #4321 — Buy page pack-selection state bug investigation

- **Status:** ✅ DONE 2026-09-16T16:47:50Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** 0f7a2babd (Buy.tsx effect-deps fix), a611b0b75 (manifest repoint + lastVerifiedAgainstCommit bump)

## Log
- 2026-09-16T16:37:53Z ⏳ IN FLIGHT — investigating Buy.tsx pack-selection state derivation on SPA-internal navigation between two ?packs= URLs; git-bisect since ac02085210 shows no relevant commit to Buy.tsx (only an unrelated [DEBUG] autofill commit). Live re-run of test-manifests/marketing/buy-page.json now shows 40/43 passing (was 38/43 when filed) — the PACK1 (entra,ca) failure no longer reproduces; only PACK6 (entra,mfa "coming soon" gate) still fails. Direct psql query against local `services` confirms "MFA Enforcement Pack" (id 195) is now a REAL row — the catalogue has grown since #1304, so the gate correctly no longer treats mfa as unavailable. This is not a Buy.tsx regression; the manifest's own notes anticipated exactly this ("if MFA Enforcement is later onboarded as a real services row, repoint at whatever is genuinely not-yet-real then"). Proceeding to repoint the manifest at "Copilot Readiness Pack" (key `copilot`), the only pack key still missing a real services row.
- 2026-09-16T16:47:50Z ✅ DONE — Root cause confirmed as catalog drift, not a Buy.tsx state-derivation regression (no relevant commit exists since ac02085210). Two real changes shipped: (1) `Buy.tsx`'s URL-preselected-pack availability filter effect was missing `availablePackKeys` from its dependency array (a genuine latent staleness bug found while auditing the derivation the issue pointed at — fixed, commit 0f7a2babd); (2) `test-manifests/marketing/buy-page.json` PACK6 repointed from `mfa` (now a real services row, id 195) to `copilot` (Copilot Readiness Pack, the only pack key of 15 still lacking a real services row) — commit a611b0b75, which also bumps `lastVerifiedAgainstCommit`. Live-verified via `shaneapp://runTest?file=buy-page.json` against the synced main checkout: **43/43 steps passing** (was 40/43 before the fix, 38/43 per the original filing). Filed #4405 (parented under #4270, labeled `bug`, board status "AI Batter Up") documenting the real process gap this exposed: the not-yet-available pack gate has no early warning when the catalogue eventually covers all 15 pack keys. `git status --porcelain` clean; branch verified merged into origin/main (pushed directly, no separate PR per this repo's "commit directly to main" convention).
