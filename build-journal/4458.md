# #4458 — Test Mode A11y rescan uses a 3-rule inline script, not the real RunAccessibilityAuditAsync scanner

- **Status:** ✅ DONE 2026-09-16T00:45:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** 4006e16cd (merged to origin/main)

## Log
- 2026-09-16T00:00:00Z ⏳ IN FLIGHT — investigating TestModeDiagnosticsPanel.BtnRescanA11y_Click, VisualTestTrackerTelemetry.RunAccessibilityAuditAsync, and VisualTestTrackerWindow's existing usage pattern before rewiring the rail to the real scanner.
- 2026-09-16T00:45:00Z ✅ DONE — BtnRescanA11y_Click now calls VisualTestTrackerTelemetry.RunAccessibilityAuditAsync (same scanner #4442's auto-check and the floating VisualTestTrackerWindow use), renders the full violation list (category badge, rule, message, selector, click-to-highlight via ScrollToAndHighlightElementAsync) in a new A11yIssuesContainer, and persists through VisualTestTrackerStore.SaveA11yAuditAsync (page resolved via GetOrCreatePageAsync) so this rail and #4442's auto-check share one row in visual_test_tracker_a11y_audits. Deleted the old inline 3-rule script entirely (confirmed via grep — no remaining references). Verified: `dotnet build desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0 errors. Live UI click-through was not self-verified (BuildConsole.exe UI automation is out of scope for agent self-verification — Git #2156, single-instance mutex risk of driving Shane's live session); confirmed instead by reading the identical call pattern already proven in VisualTestTrackerWindow.xaml.cs and PageAutoCheckService.cs, and by confirming visual_test_tracker_a11y_audits' schema directly against the local DATABASE_URL. `git status --porcelain` clean; branch verified merged into origin/main via verify-branch-merged.mjs.
