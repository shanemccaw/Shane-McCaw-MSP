# #2463 — Sent notes are locked from editing

- **Status:** ✅ DONE 2026-09-02T22:20:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder — Test Pad, Feature #2326)
- **Started:** 2026-09-02
- **Commit(s):** `e30d226c9` (IN FLIGHT bookend), `4030c1a91` (real work — tooltip affordance)

## Log
- 2026-09-02T22:05:00Z ⏳ IN FLIGHT — real audit first: confirmed #2335 (item 9, click-to-edit)
  merged to origin/main in commit `905e0c5a1`, and confirmed its own commit already implements
  item 10's lock as a side effect (`TestPadService.UpdateNote` refuses a sent note;
  `TestPadWindow.BuildRow` only wires the click-to-edit handler for `!note.IsSent`, with
  `Cursor.Arrow` + dimmed text on a sent row). The real blocker #2336 hit is genuinely cleared.
  Proceeding to verify + close the one real gap #2336 flagged as open: no tooltip/affordance
  explaining WHY a sent row isn't clickable.
- 2026-09-02T22:20:00Z ✅ DONE — spec item 10 ("Sent notes are locked from editing") was already
  fully implemented on `main` before this session touched anything: no click handler wired for a
  sent row, `Cursor.Arrow` instead of `Hand`, dimmed text, and `TestPadService.UpdateNote` refuses
  to mutate a sent note server-side too — a real, complete lock, not just a UI-only one. Added the
  one genuine gap: a `ToolTip` ("Sent — locked from editing") on a sent row so the lock has an
  actual affordance instead of silently doing nothing on click (commit `4030c1a91`). Verified with
  a real `dotnet build ShaneBuilder.csproj -c Debug` — 0 warnings, 0 errors. Branch confirmed
  merged into `origin/main` via `verify-branch-merged.mjs`. `git status --porcelain` clean at
  session end. No fixture/hardcoded data — the lock reads/writes the real `TestPadService` store.
  No new findings filed — nothing broken or missing was discovered outside this issue's own scope.
