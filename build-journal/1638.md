# #1638 — Chat button bar: Park button + visible duplicate/already-ran check

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** e642d4d8ec812442c5dc79d46cd78691e2db1528

## Log
- 2026-08-29 ⏳ IN FLIGHT — starting. Scope per issue body + locked follow-up comment:
  (1) Park button in ChatButtonInjector.cs, (2) `parked` status in bt_build_queue,
  (3) un-park action in Build Queue panel, (4) "Parked" filter option, (5) generalized
  visible dedup check (active-match vs terminal-state re-run confirm) across
  Queue/Park, (6) "Send to Builder" tracking — per the issue's locked follow-up
  comment: insert a real `bt_build_queue` row with new status `external` (never
  claimed by the watcher, never admitted to Build Watch's 8 slots), pass its id
  through `mybuilder://` as `queueId=`, have `scripts/run-claude.ps1` redirect real
  stdout/stderr to the existing `BuildLogPaths.ForQueueItem(queueId)` convention and
  write the real exit code back via `psql` after `-Wait` returns, and add an
  "External" filter option reusing the same log-tail viewer. About to read
  `Services/ChatButtonInjector.cs`, `MainWindow.xaml.cs`'s `BT_QUEUE_BUILD`
  (~5749)/`BT_SEND_TO_BUILDER` (~5736) handlers, and
  `Services/BuildQueuePostgresClient.cs`'s `QueueBuildAsync` (~360) in full before
  writing anything.
- 2026-08-29 ✅ DONE — all six pieces built. (1) Park button added to
  ChatButtonInjector.cs's injected bar, posting BT_QUEUE_BUILD with an added
  `park: true` flag (chosen over a second message type — every other field is
  identical). (2) `QueueBuildAsync` takes a `park` param controlling the final
  status ('parked' vs 'queued') on both its insert and githubNumber-upsert paths —
  no schema change, the status column is already free text. (3) `UnparkAsync`
  (flips parked → queued, mirrors `RequeueLimitPausedAsync`) wired to a "▶ Un-park"
  context-menu action on a parked card, plus `CancelAsync` widened to accept
  'parked' so a staged item can be dropped without un-parking first. (4) "Parked"
  ComboBoxItem added to `QueueFilterCombo`/`ApplyFilter`, confirmed excluded from
  "Active" (the watcher's `GetNextAsync` claim query is `WHERE status = 'queued'`,
  so this was already structurally safe — just needed to be findable).
  (5) `FindDedupCandidateAsync` — github-linked builds match the latest row for
  that issue number regardless of status; local (--notGit) builds match a
  whitespace-normalized exact prompt comparison bounded to the last 24h (stated,
  arbitrary window). Run before both Queue and Park insert a row: an active match
  (queued/parked/running/verifying/limit-paused/external) flips the button to
  "Already X" via a new `__btAlreadyExists` JS hook, clickable (new
  `BT_REVEAL_QUEUE_ITEM` message → `BuildQueuePanel.RevealQueueItem`) to jump to
  the existing item instead of duplicating; a terminal match (done/failed/
  canceled) shows an explicit WPF confirm ("already ran — run it again?") instead
  of the old silent reset, and only on Yes reuses that exact row via a new
  `reuseRowId` param. (6) Send to Builder — locked decision from the issue's
  follow-up comment: tracked purely for dedup/visibility/log-capture, kept
  behaviorally identical (external `mybuilder://` launch, outside the 8-slot cap,
  hands-off `--permission-mode auto`). `QueueExternalAsync` inserts a plain
  status='external' row (confirmed invisible to both `GetNextAsync`'s
  `WHERE status = 'queued'` claim and `BuildWatchWindow.AdmitNewRunning`'s
  `WHERE status == "running"`); its id passes through `mybuilder://` as
  `queueId=`; `run-claude.ps1` now redirects real stdout/stderr into the same
  `BuildLogPaths.ForQueueItem(id)` convention the in-app watcher already uses, and
  writes the real exit code back via a direct `psql` call against the local
  `DATABASE_URL` (read from `.env.local`, same convention as the rest of the repo)
  after the process exits — best-effort/non-fatal if `psql`/`DATABASE_URL` aren't
  found. New "External" filter option plus a "📜 Tail Log" context-menu action
  (new `Controls/ExternalLogWindow.cs`, a small standalone live-tailing viewer,
  same file-tail pattern as `MainWindow.TailBuildLog`) make a launched-outside-
  the-cap build findable and its output readable again.
  Logging: `chat-button.status` and `build-queue` (both pre-existing channels) for
  button-state/park-state changes; new leaf channels `build-queue-panel.dedup` and
  `build-queue-panel.external`, following the existing
  `build-queue-panel.epic`/`.epic-filter` leaf convention.
  Verification: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release`
  clean (0 errors, 46 pre-existing warnings, none new). `run-claude.ps1` parses
  clean via the PowerShell AST parser. **Not live-driven in this environment** — no
  running BuildConsole instance/WebView2 available here, so the park → filter →
  restart → un-park flow, duplicate detection, and the re-run confirm were
  code-reviewed against the exact same claim/filter/card/context-menu code paths
  already proven live by the pre-existing Active/Done/Canceled/limit-paused flows,
  but were not click-tested end to end in a running app. Rebased cleanly onto
  origin/main (no conflicts) and pushed directly, per this repo's branch
  convention. `git status --porcelain` clean; branch verified merged into
  origin/main via `scripts/dev-server/verify-branch-merged.mjs` before writing
  this DONE.
