# #3643 — 5 api-server node-runner tests fail at link time: mock.module("@workspace/db") stubs miss names production now imports

- **Status:** ✅ DONE 2026-09-12T23:55:00Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** d8b3d8037 (rebased/pushed to main as 7b181f8c4)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Starting: fix @workspace/db mock.module stubs in 5 node-runner test files (admin-script-runner, auth-account-activation-entitlement, auth-impersonation, leads-stats, msp-api-foundation) so each passes standalone with --experimental-test-module-mocks.
- 2026-09-12T23:55:00Z ✅ DONE — All 5 files now link cleanly (no more mock.module SyntaxErrors).
  Results running each standalone with `node --experimental-strip-types --experimental-test-module-mocks --test <file>`:
  - auth-account-activation-entitlement.test.ts: 8/8 pass
  - auth-impersonation.test.ts: 18/18 pass (also fixed a stale fixture field —
    fakeMspUserRow.customerId → tenantId, matching getMspClaims()'s post-tenant-refactor
    query shape — and extended the mock db's insert() chain to support
    .returning()/.onConflictDoUpdate())
  - leads-stats.test.ts: 33/33 pass (mock stub extended to ~80 names across leads.ts's
    much larger transitive import graph)
  - msp-api-foundation.test.ts: 25/27 pass — 2 remaining failures are pre-existing
    dead-code-path gaps unrelated to mock completeness (RBAC ladder capability check
    fails closed to 503 against an always-empty mock select; X-Trace-Id echo test
    asserts a non-UUID id the real app.ts deliberately never forwards) — filed as #3883
  - admin-script-runner.test.ts: links cleanly now but all 10 tests fail — the route it
    exercises (`GET /api/admin/runbook-jobs/output`) no longer exists in production;
    the real logic lives in admin-m365-run.ts now — filed as #3882
  Findings #3882 and #3883 filed as sub-issues of #1128 (this issue's Feature parent),
  labeled `bug`, board status set to "AI Batter Up".
  Verified branch merged into origin/main (7b181f8c4) via verify-branch-merged.mjs;
  working tree clean.
