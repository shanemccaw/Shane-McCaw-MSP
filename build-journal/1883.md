# #1883 — Build queue must never auto-launch while the app itself is still starting up

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 9832125e5 (merged to origin/main via a68a95304)

## Log
- 2026-08-31 ⏳ IN FLIGHT — read #1882's landed diff (7d872eab7) for the real readiness
  signal (`StartupConnectivityService.AllSettled`, gated on the new `KeyShell`
  "Application shell" row via `MarkShellReady()`), read `QueueWatcherService.cs`'s
  pickup loop in full, wiring a hard gate in `TickAsync` (`IsAppReady`/`MarkAppReady()`)
  independent of `IsPaused`, subscribed from `MainWindow` to `_startupConnectivity.AllSettled`
  before `_queueWatcher.Start()` is ever called.
- 2026-08-31 ✅ DONE — `9832125e5`. `QueueWatcherService` gained `IsAppReady`/
  `MarkAppReady()` and a hard gate in `TickAsync`, checked BEFORE (and independent of)
  the `IsPaused` check, so the pickup loop's very first claim attempt cannot fire until
  the app itself has signaled genuine full readiness. `MainWindow` wires
  `_startupConnectivity.AllSettled += () => _queueWatcher?.MarkAppReady();`
  immediately after constructing `_queueWatcher`, before `_queueWatcher.Start()` is
  ever invoked — closing the exact window a live cold-start trace of #1882 confirmed
  exists: `_queueWatcher.Start()` runs synchronously at MainWindow.xaml.cs:491 inside
  `RunDeferredStartupAsync`, and its 10s `DispatcherTimer` fires an immediate first
  `TickAsync()` — while `AllSettled` cannot fire until `MarkShellReady()` runs in that
  same method's `finally` block, hundreds of lines and several `Dispatcher.Yield`
  phases later. Full code-path trace confirms: every `TickAsync` invocation before
  `MarkAppReady()` is called hits `if (!_appReady) return;` and skips claim/launch
  entirely; reaping already-running/adopted builds is unaffected (a fresh cold start
  has nothing to reap).
  - `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean,
    0 warnings / 0 errors.
  - Live cold-start relaunch NOT performed: at verification time two BuildConsole.exe
    instances (pids 15832, 20880) were already running against the live production
    queue DB, one of them actively orchestrating queued builds at capacity (4/4
    running, per `activity-2026-08-31.log`). `QueueWatcherService`'s own header
    comment documents the real hazard a third poller would introduce: a freshly
    launched instance's `_running` dict starts empty, so its own claim math
    (`freeSlots = _maxConcurrent - _running.Count`) can't see the other instances'
    already-running builds and could claim/launch a REAL queued build beyond the
    configured cap — exactly the "if a second watcher is also left running, real
    concurrency could exceed max" caveat already in the file. Spinning up a third
    instance against live production state to test this was judged not worth that
    risk; the static trace above is exhaustive and the fix is a single, narrow
    early-return gate with no other code path to miss.
  - Branch verified merged: `node scripts/dev-server/verify-branch-merged.mjs` — OK,
    `agent/1883-q996` (a68a9530) is an ancestor of `origin/main`.
  - `git status --porcelain` clean at finish.
