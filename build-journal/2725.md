# #2725 — msp-sow.ts clickwrap POST has no ownership check - cross-tenant forgery of customer-agreement acceptance

- **Status:** ✅ DONE 2026-09-04T03:15:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** be2aa261a (bookend), 2d481be24 (fix, on main)

## Log
- 2026-09-04T00:20:00Z ⏳ IN FLIGHT — Confirmed via read of `artifacts/api-server/src/routes/msp-sow.ts`: `POST /api/msp/customers/:customerId/clickwrap` (lines 946-995) has no ownership/scope check, while sibling `GET` (lines 893-944, check at 911-913) does. About to add the same check to POST before the insert, per the issue body's suggested fix.
- 2026-09-04T03:15:00Z ✅ DONE — Added `const mspId = user.mspId ?? getMspIdFromRequest(req);` + the same `mspId && customer.mspId !== mspId && user.role !== "admin"` → 403 check to the POST route, exactly mirroring the GET route's existing guard. Verified via `artifacts/api-server/src/routes/msp-sow.test.ts` (`node_modules/.bin/vitest run src/routes/msp-sow.test.ts`, run directly since node_modules is junctioned into this worktree) — added two regression tests (`GET rejects a caller from a different MSP with 403`, `POST rejects a caller from a different MSP with 403 (Access denied) — regression for #2725`), both pass, plus all 5 pre-existing clickwrap tests still pass (30/31 total pass in the file). Also fixed a pre-existing broken `logger.child` mock in this same test file (was causing 0 tests to be collected, a TypeError at import time via `msp-entitlement.ts`) so the suite could actually run — filed the same gap in 6 *other* test files as #2793, and the one remaining unrelated pre-existing failure in this file ("signs a sent SOW", 500 instead of 200) as #2794, both parented under this issue's Feature (#2568) and set to AI Batter Up. Commit `2d481be24` is on `origin/main` (verified via `verify-branch-merged.mjs`). Working tree clean.
