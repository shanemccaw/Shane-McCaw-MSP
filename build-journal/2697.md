# #2697 — admin-rbd.ts can still write status="expired", undoing #1507's fix

- **Status:** ✅ DONE 2026-09-04T03:03:00Z
- **Scope:** platform

## Log
- 2026-09-04T02:52:35Z — Session started. Confirmed the regression: `admin-rbd.ts:43` defines a
  local `RBD_STATUSES = ["active", "pending_signature", "expired", "revoked"]` instead of
  importing the canonical `RISK_ACCEPTANCE_STATUSES` (`lib/db/src/schema/msp.ts:5939`, no
  `expired`), and applies it to both `createSchema.status` (line 177) and `patchSchema.status`
  (line 268) on `mspRiskDecisionsTable.status`. Secondary same-root-cause dup in
  `artifacts/mcp-server/src/tools/get-risk-register.ts:17`. Fixing per issue body: import
  `RISK_ACCEPTANCE_STATUSES` from `@workspace/db` in both files, drop the local const.
- 2026-09-04T03:03:00Z — ✅ DONE. `admin-rbd.ts` now imports `RISK_ACCEPTANCE_STATUSES` from
  `@workspace/db` and uses it in both `createSchema.status` and `patchSchema.status` (the two
  live write paths); the local `RBD_STATUSES` const is removed. `get-risk-register.ts`'s
  read-only status filter had `expired` dropped from its local array — kept as a local copy
  rather than importing `@workspace/db`, since mcp-server has no dependency on that package
  today and adding one would require a root `pnpm install`, which the hard rule forbids running
  from inside a worktree (Git #1987/#1988). Verified: `npx tsc --noEmit` clean in both
  `artifacts/api-server` and `artifacts/mcp-server` for these files (api-server's pre-existing,
  unrelated failures are in `monitor-executor.test.ts`, nothing to do with this change). Direct
  query against local `DATABASE_URL`: `msp_risk_decisions` currently has zero rows with
  `status='expired'` (only `pending_signature`), so no live data needed backfilling — this closed
  a validation-layer regression before it produced bad rows. Commit `efecba0b4` on
  `agent/2697-q1497`, merged into `origin/main` at `981587585`; `verify-branch-merged.mjs`
  confirms ancestry. `git status --porcelain` clean.
