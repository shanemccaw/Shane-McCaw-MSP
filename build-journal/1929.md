# #1929 — config_resources counts 129 Graph bound Functions as configuration resources

- **Status:** ✅ DONE 2026-09-04T07:34:12Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 4fe716239

## Log
- 2026-09-04T07:10:12Z ⏳ IN FLIGHT — Read #1929 body + comments. Found a correction comment adding
  `--blocked-by 1896`. Checked #1896: real verified DONE bookend (`build-journal/1896.md`, commit
  `1751e806a`), confirmed present on `origin/main` via `git log origin/main --oneline`. Unblocked;
  deleted the stale `blocked_by` edge (`DELETE .../1929/dependencies/blocked_by/5291092525`),
  relabeled #1929 `in-flight`. Parent confirmed as #2489 (Feature: Write-App/Graph Permission
  Engine), not the #1096 Epic.
- 2026-09-04T07:34:12Z ✅ DONE — Took the issue's keep-and-mark option, matching the precedent
  #1795's snapshot registry already set (`graph_container_kind = 'function'` registered
  non-collectable, never dropped). Implementation:
  - `lib/db/src/schema/config-state.ts`: `coverageStateFor()` gains a state, `operation`,
    evaluated first (ahead of `no_executor`); new `isOperationResource()` helper.
  - `artifacts/api-server/src/routes/admin-config-resources.ts`:
    `GET /api/admin/config-resources/summary` now returns `resourcesOperations`,
    `operationProperties` and `resourcesCoverageEligible` (`resources − resourcesOperations`),
    and excludes function-kind rows from `resourcesCoveredByAtLeastOneCheck`,
    `resourcesEntirelyUncovered`, `resourcesWithNoExecutor` and `properties`.
    `GET /api/admin/config-resources` gained `coverage=operation` as a filter value.
  - `artifacts/admin-panel/src/pages/ConfigResourceModel.tsx`: added an "Operations (excluded)"
    stat tile, an `operation` coverage badge, and coverage % now computed against
    `resourcesCoverageEligible`.
  - `docs/graph-resource-model.md`: new `#1929` section restating the real, corrected
    denominator (1,410 resources, `resourcesCoverageEligible`) and property-count (17,311
    excl. connection params / 22,328 raw) figures, live-verified against local Postgres.
    Functions remain real rows in `config_resources` — never dropped.

  **Concurrency note:** while implementing, a concurrent session (#1917 — "Reclassify
  Lighthouse-out-of-scope azure-rm resources to unavailable") landed on `origin/main` and had
  independently added a 4th `coverageStateFor()` parameter/state (`unavailable`) to the exact
  same function my own 3rd-parameter (`graphContainerKind`) change touched. First push attempt
  (commit `78ca96c72` on `agent/1929-q1560`) hit a real 3-way conflict on rebase in
  `config-state.ts`, `config-state.test.ts` and `admin-config-resources.ts`. Rather than resolve
  a messy 3-way diff, I aborted the rebase, branched fresh off `origin/main`
  (`agent/1929-q1560-v2`), and reapplied #1929's logic on top of #1917's already-landed code —
  producing a 5-state `coverageStateFor(transport, checkCoverageCount, availability,
  graphContainerKind)` with correct precedence: `operation` → `no_executor` → `unavailable` →
  `covered`/`uncovered`. Also backfilled `ConfigResourceModel.tsx`'s own type/badges for #1917's
  `unavailable` state, which had landed on the backend (#1917's own commit) but not yet the UI —
  found while touching the same coverage-badge switch for `operation`; not a separate finding,
  fixed inline since it was directly adjacent to my own edit.

  **Verification:** `lib/db` `config-state.test.ts` 17/17 passing (14 original/#1917 tests + new
  operation-state tests). `api-server` `azure-rm.test.ts` 18/18 passing (unaffected by the
  signature change). `api-server` + `admin-panel` `tsc -p tsconfig.json --noEmit` clean for every
  touched file (ran `npx tsc -b` in `lib/db` first to refresh the composite-project `.d.ts`
  output the other two packages' project references read). `build-snapshot-registry.mjs
  --dry-run` unaffected (unrelated to this fix; ran to confirm no regression). New SQL logic
  hand-verified directly against the live local `shanemccawmsp` Postgres: 90 covered + 1,296
  uncovered + 6 no_executor + 18 unavailable + 129 operations = 1,539 — exactly partitions the
  model, no double-counting or gaps.

  Commit `78ca96c72` on the now-deleted `agent/1929-q1560` branch was superseded and never
  merged (its own bookend-only precursor commit, `eacbb7428`, is also gone with that branch —
  this file did not exist on `main` before this DONE entry, so there is no earlier IN FLIGHT
  commit hash to cite). Real work commit `4fe716239` on `agent/1929-q1560-v2`, rebased onto and
  pushed directly to `origin/main`, confirmed merged via
  `node scripts/dev-server/verify-branch-merged.mjs`. Stale branches
  (`agent/1929-q1560` local+remote) deleted after confirming supersession. `git status
  --porcelain` clean.

  No new GitHub findings filed this session — the one adjacent gap found (the UI's stale
  `unavailable`-state type/badges from #1917) was fixed inline rather than filed, since it was
  directly touched by this session's own edit to the same switch statement.
