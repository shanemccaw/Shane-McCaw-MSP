# #1826 — notifications.category unconstrained free text (34 distinct values vs 7 real alert categories)

- **Status:** ✅ DONE 2026-09-04T07:43:02Z
- **Scope:** platform

## Log
- 2026-09-04T07:38:11Z — Session start. Investigation-only dispatch per issue body: report real distribution + audience cross-reference, propose constraint (global enum vs per-audience). No schema change, no row remap in this dispatch.
- 2026-09-04T07:43:02Z — ✅ DONE — Investigation delivered as a comment on #1826 (https://github.com/shanemccaw/Shane-McCaw-MSP/issues/1826#issuecomment-5537343344). Real findings:
  - Live `notifications` table (local `DATABASE_URL`): 171 total rows, only 2 real `(category, recipient_type)` states — `(NULL, platform_admin)` ×91, `(offer, customer_user)` ×80. None of the seeded seven catalog categories have ever landed a row.
  - The original "34 distinct values" static grep was a false count: it matched `category:` across the whole `artifacts/api-server/src` tree, not just `notifications` inserts. Traced every real `notificationsTable.values(...)` insert and every `createNotification()` call site — the real code-driven set is 7 literals (`ownership`, `drift`, `approval`, `simulator`, `system`, `offer`, `message_center`) plus one always-`platform_admin` operator-free-text field on the `create_notification` workflow-builder node. The rest of the 34 belong to unrelated tables/fields (`clientHealthHistoryTable.category`, `documentTypesTable.category`, `articlesTable.category`, admin-services catalog, drift-check registry keys, workflow scoring taxonomies) or, for `speaking/media_press/nasa_employment/career_mentorship`, to `lib/public-chat-guardrail.ts` — a genuine unrelated-column-name collision, confirmed exactly as the issue suspected.
  - Proposal posted: single global enum (audience split doesn't need a separate vocabulary — `recipientType` already scopes who receives it), built from the seeded seven plus the six real non-catalog code values; two open product decisions left for Shane (constrain the workflow-builder free-text field vs. leave it, and whether to backfill the 91 `NULL` rows to a `general` value or keep `NULL` as a real state).
  - No `ALTER TABLE`, no enum constraint, no row remap made — per the issue's own instruction, this dispatch is the report + proposal only.
  - Filed a real secondary finding as its own issue: #2849 ("notifications: ~10 routes bypass createNotification(), skip preference/email/webhook gating") — parented under #2742 (Feature: Data Integrity / Schema Drift Cleanup), labeled `bug`, board status set to "AI Batter Up". The 91 `NULL`-category rows trace to these direct-insert routes bypassing the centralized helper's preference/email/webhook/SSE-category logic entirely.
  - Commits: none to application code (investigation-only dispatch, no scripts/queries persisted — ad hoc `psql` reads only). This file is the only tracked change.
  - Verification: `git status --porcelain` clean at session end; branch ancestor check run below.
