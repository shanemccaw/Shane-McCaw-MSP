# #1549 — Policy Engine: Continuous evaluation — reconciliation loop, not an onboarding-only trigger

- **Status:** ⏳ IN FLIGHT
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** (fill at DONE)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1549 body + comments and the cross-referenced #1547 (foundation,
  merged `2917f7a2c`), #1548, #1550, #1553, #1498. Building the continuous-evaluation
  reconciliation loop as a visible Workflow Engine node (workflow-executor.ts / seed-system-workflows.ts,
  the system that already seeds real schedule+event system workflows), with the per-customer
  opt-in gate (`tenants.policy_engine_opt_in`) and a durable `policy_evaluation_runs` register.
  Scoped narrowly to the trigger/loop mechanism per #1549's own SETTLED text — the actual
  Graph-state comparison has no OU-membership model to read yet (a real, pre-existing gap,
  deliberately reserved by #1547/active-directory.ts), so this build records that honestly as
  `not_evaluable` rather than fabricating a compliant/divergent verdict. Enactment (#1548) and
  finding-generation (#1553) are separate, not-yet-built issues and are not touched here.
