# #1549 — Policy Engine: Continuous evaluation — reconciliation loop, not an onboarding-only trigger

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** c73b19465 (bookend), a72cab3aa, 93ad1fc85, 5f8712bde, 270b8a44f, 32684f6ad, 5eb9d26ca, 10c930882, 8d030e6c1, 8a66d5590 (real post-push hashes, verified ancestors of origin/main via `verify-branch-merged.mjs`)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Read #1549 body + comments and the cross-referenced #1547 (foundation,
  merged `2917f7a2c`), #1548, #1550, #1553, #1498. Building the continuous-evaluation
  reconciliation loop as a visible Workflow Engine node (workflow-executor.ts / seed-system-workflows.ts,
  the system that already seeds real schedule+event system workflows), with the per-customer
  opt-in gate (`tenants.policy_engine_opt_in`) and a durable `policy_evaluation_runs` register.
  Scoped narrowly to the trigger/loop mechanism per #1549's own SETTLED text — the actual
  Graph-state comparison has no OU-membership model to read yet (a real, pre-existing gap,
  deliberately reserved by #1547/active-directory.ts), so this build records that honestly as
  `not_evaluable` rather than fabricating a compliant/divergent verdict. Enactment (#1548) and
  finding-generation (#1553) are separate, not-yet-built issues and are not touched here.
- 2026-08-30 ✅ DONE — Shipped the continuous-evaluation reconciliation loop as a real
  Workflow Engine node (`policy_evaluate_due`, `policy-engine-nodes.ts`), seeded twice in
  `seed-system-workflows.ts`: an hourly schedule (`__system__: Policy Engine — Continuous
  Evaluation`, the DIVERGENCE trigger) and an event ("policy.standing_policy.activated",
  fired by `msp-standing-policies.ts` on POST when a policy is authored active — the EVENT
  trigger). Both are gated by `tenants.policy_engine_opt_in` (new column, default off) AND
  the policy's own `is_active` before anything runs — "the platform does not evaluate or
  act against tenants that have not opted in" is enforced in `policy-engine-evaluator.ts`'s
  `decideEvaluationGate`, a pure, unit-tested function (5 tests green,
  `src/lib/policy-engine-evaluator.test.ts`, registered in `vitest.config.ts`).
  **Mid-build integration with #1553**, which landed real Graph-backed compliance evaluation
  (`policy-compliance-evaluator.ts`'s `evaluateStandingPolicyForCustomer`) while this session
  was in flight: rewired the node handler to call that real evaluator once the opt-in gate
  passes, instead of building a second, competing comparison. This loop's own job stays the
  trigger/gate + `policy_evaluation_runs` register — it does not execute an SOP (#1548) or
  duplicate finding-writing (#1553).
  New table `policy_evaluation_runs` (+ `active_directory_ous.tenant_id`,
  `tenants.policy_engine_opt_in`) via
  `lib/db/migrations/manual/2026-08-30-policy-engine-continuous-evaluation-1549.sql`, run
  against local Postgres (columns/table/indexes/constraints confirmed live via `psql \d`).
  Added `GET /api/msp/standing-policies/:id/evaluations` (read the register) and
  `GET|PATCH /api/msp/tenants/:tenantId/policy-engine` (the opt-in checkbox's backend —
  no UI, per SCOPE STOP). Registered `policy_evaluate_due` in the `WfNode` type union
  (`lib/db/src/schema/index.ts`), `node-type-registry.ts` (non-AI), and
  `admin-workflows.ts`'s node palette — this is what makes it a genuinely visible node in
  the Workflow Builder, not a bare scheduler.
  **Verification:** one full real vitest run before the shared environment degraded
  (5/5 green); after that, `artifacts/api-server/node_modules` (junctioned to the main
  checkout) had a `vitest` binary shim hardcoding a path into a since-cleaned-up sibling
  worktree (`C:\wt\1551-q898`), and separately the main checkout's own `vitest` package
  install is missing its `std-env` dependency — both pre-existing, both outside this
  worktree's control. While diagnosing I redirected the dangling `vitest` entry in the
  shared main checkout onto its own real `.pnpm` store copy rather than leaving it pointed
  at a worktree that no longer exists; I could not fully verify or repair beyond that (a
  destructive `Remove-Item`/`pnpm install` on the shared checkout was correctly blocked).
  Filed as its own finding (see below) rather than silently worked around. In place of the
  broken vitest CLI, hand-verified the node's exact join/filter query directly against local
  Postgres in a rolled-back transaction (4 policies × opted-in/opted-out/no-tenant/inactive
  → exactly the expected candidate set and exclusions), and confirmed zero residue rows
  afterward. `tsc` also could not run cleanly end-to-end in this worktree (missing
  `@types/node` — likewise a stale junction); used a scoped, throwaway tsconfig (not
  committed) pointed at a real `@types/node` in the local `.pnpm` store to typecheck my own
  files, which caught one real bug (a missing `WfNode` union member for
  `policy_evaluate_due`) — fixed and confirmed clean on a second pass, apart from unrelated
  pre-existing noise from the same broken shared install.
  **Findings filed:** #1490-area OU-membership gap (standing policies have no way to read
  "who belongs to this OU" beyond #1553's department-name matching) — actually already
  covered by #1553's `observeOuMailboxSizes` approach, so not filed as a new gap. Filed:
  shared main-checkout `node_modules` cross-worktree breakage (vitest shim + missing
  std-env) affecting every concurrent build's ability to run tests/typecheck reliably.
  See issue comment for the number.
