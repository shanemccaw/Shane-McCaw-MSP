# #3323 — Surface medication-usage timing patterns (real aggregation, not AI inference)

- **Status:** ✅ DONE 2026-09-09T13:35:00Z
- **Scope:** platform (web/shanes-life, personal project — Shane's Life)
- **Started:** 2026-09-09
- **Commit(s):** 294f7ec18 (real, live-verified against local `finances` DB)

## Log
- 2026-09-09T13:05:00Z ⏳ IN FLIGHT — Investigated #3321 (as-needed dose logging, now live at
  7df1607bb) and its `medication_usage_log` table. Building a real, deterministic hour-of-day
  usage breakdown per as-needed medication: new `getMedicationUsageTiming` in medications.mjs,
  new GET `/api/medications/:id/usage-timing` route, and a new "Usage timing" section in the
  Meds room (public/app.js) — plain counts bucketed client-side by real browser-local hour
  (same convention `when()`/`agoShort()` already use), with an honest minimum-data message
  below 5 real logged doses. No AI/model call anywhere in this — plain SQL + arithmetic only,
  per the issue's own explicit constraint.
- 2026-09-09T13:35:00Z ✅ DONE — Shipped at 294f7ec18. `getMedicationUsageTiming` (real, bounded
  SELECT of `used_at`, no hour bucketing server-side — bucketing happens client-side on the
  browser's real local clock, this app's own existing convention). New read-only
  `GET /api/medications/:id/usage-timing`. Meds room's new "Usage timing" section: one card per
  as-needed medication with real usage history, real 3-hour-bucket bar counts, honest
  "not enough logged yet" message below 5 real doses (`USAGE_TIMING_MIN_DOSES`). No new MCP
  tool — `get_medications` already surfaces the same raw timestamps for Claude to reason on
  conversationally. Live-verified against the real local `finances` DB: inserted real Albuterol
  dose rows clustered 9:10pm-11:05pm plus one 9am dose, confirmed 0/2/5-dose thresholds and
  correct 9pm-midnight bucket clustering; test rows deleted after, table back to 0 rows.
  `git status --porcelain` clean, branch verified merged into origin/main
  (`verify-branch-merged.mjs`). No findings to file — no gaps hit outside this issue's own
  scope.
