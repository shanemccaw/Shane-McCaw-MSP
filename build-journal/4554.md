# #4554 — Build Queue Retry (reply rows), crash-recovery Resume Session and Recover All can still resume a session that is live under another row

- **Status:** ✅ DONE 2026-09-17T21:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 096a32edd (work), 19128eff7 (IN FLIGHT bookend)
- **Findings filed:** #4611 (QueueWatcherService.LaunchItem builds `--resume` with no session-liveness check at all, so a queued row already carrying resume_session_id — a continuation left queued at the hard cap, or a limit-paused row resumed in place by SessionLimitAutoRestartService — bypasses every UI guard; parented under #3706)

## Log
- 2026-09-17T18:05:00Z ⏳ IN FLIGHT — routing the four remaining unguarded `--resume` call sites in `BuildQueuePanel.xaml.cs` (popover Retry, context-menu Retry, crash-recovery ▶ Resume Session, Recover All) through #4547/#4553's `QueueWatcherService.LaunchContinuationAsync`. No second guard mechanism (buildId 3223).
- 2026-09-17T21:05:00Z ✅ DONE — Confirmed all four cited sites still matched fc8a0235d's shape on current code before editing (QuickRetryAsync ~6776, context-menu Retry ~7604, ▶ Resume Session ~7566, Recover All ~1545). All four now go through #4547/#4553's `LaunchContinuationAsync`; no second guard was built. The guard itself was extracted out of #4553's `SendReplyAsync` into one shared `ResumeSessionGuardedAsync` + a `GuardedResumeRequest`/`GuardedResumeOutcome` pair, so Reply, Retry, Resume Session and Recover All are one path: a live writable stdin for the session gets the text directly; otherwise `LaunchContinuationAsync` stops whichever row owns the live process, confirms its exit (or launches nothing), reuses that row and resumes. It checks `GetLiveQueueIdsForSession`, not the clicked row — for all four of these the clicked row's own process is gone by definition, which is exactly why the old per-row check would have read all-clear every time. Detail: the two Retry copies (popover + context menu) collapsed into `RetryQueueItemAsync`; a start-over Retry (`resumeSessionId == null`) is deliberately unchanged, still queued for the dispatcher. `ResumeCrashedSessionAsync` keeps #2120's `MarkOrphanSupersededByResumeAsync` because `LaunchContinuationAsync`'s own `MarkSupersededByReplyAsync` deliberately skips a `failed` row, which every orphan is. Recover All takes the guarded path ONLY for an orphan whose session is genuinely live under another row; every other orphan keeps the plain re-queue, so a bulk crash recovery still paces through the dispatcher rather than force-launching the whole batch, and a live+working session is left alone and reported ("N left running") instead of popping a modal per item. Verification: `dotnet build` 0 warnings, 0 errors. A reflection harness against the built DLL passed 18/18 using real OS processes and never touching the real queue DB (the client was constructed against an unreachable 127.0.0.1:1): IL scan proved RetryQueueItemAsync, ResumeCrashedSessionAsync, RecoverOrphanedBuildsWithResultAsync and SendReplyAsync all really call ResumeSessionGuardedAsync and QuickRetryAsync calls RetryQueueItemAsync; a session live+working under a DIFFERENT row was skipped by the bulk path with the other row named and its process left alive; an idle live row got the text on its real stdin with no second process; two live rows on one session were refused with both left alive; with nothing live it went through LaunchContinuationAsync and failed on the unreachable DB, creating no row (`Git #4547 continuation of queue #900004 not launched: Failed to connect to 127.0.0.1:1` in the real activity log); a missing session id refused outright. The harness wrote "HARNESS #4554" / queue #9000xx lines to the AppData activity log and was deleted afterwards. NOT verified: the BuildConsole WPF UI end to end — per the standing rule the exe is not launched for verification, so Shane's first real Retry / Resume Session / Recover All after pulling and rebuilding is the live check.
