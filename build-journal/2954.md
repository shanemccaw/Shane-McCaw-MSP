# #2954 — New Site v2 — Shared Chrome (Header/Footer/Brand Mark rebuild)

- **Status:** ✅ DONE 2026-09-05T23:35:00Z
- **Scope:** platform (marketing site — `artifacts/shane-mccaw-consulting`)
- **Started:** 2026-09-05
- **Commit(s):** `4ad0df12ac5505f057a42f64c168fa6ab5d62c6e` on `production-base-2026-09-05` (real target branch, confirmed by the issue body — NOT `main`). This worktree's own `main`-based history carries only this bookend file (`d19b9ea8a`, `main`).

## Log
- 2026-09-05T22:52:00Z ⏳ IN FLIGHT — Read #2954's issue body + dispatch comment. Confirmed real target branch is `production-base-2026-09-05` (a much older base than `main` — no `build-journal/`, no `Design/fractional_architecture/` there). Read the Shared Chrome spec from `Design/fractional_architecture/README.md` (only present on `main`) and the current `Header.tsx`/`Footer.tsx` on `production-base-2026-09-05`. Plan: keep this worktree on `agent/2954-q1704` (main-based) for bookend/progress-reporting tooling only; do the actual Header/Footer rebuild in a second worktree checked out from `origin/production-base-2026-09-05`, commit + push there directly (this issue's target branch is not `main`, so the usual "commit to main" convention does not apply here — the issue body is explicit about this).
- 2026-09-05T23:35:00Z ✅ DONE — Rebuilt `Header.tsx` (fixed 72px bar, new nav order Assessment · Solutions dropdown · Fractional Architecture · About Shane · Resources · Contact, 36×36 gradient ShieldCheck brand mark, "Talk to Shane →" CTA, mobile hamburger + accordion) and `Footer.tsx` (5-column layout Brand / Get Started / Solutions×8 / Company / Legal, updated bottom-bar disclaimer) to match `Design/fractional_architecture/README.md`'s Shared Chrome section exactly — including exact link targets extracted from the design's own footer markup (`Contact.dc.html`). Verified via `tsc --noEmit` (clean) and `vite build` (succeeds; pre-existing chunk-size/env-var warnings unrelated to this change) in a second worktree checked out from `origin/production-base-2026-09-05`, junctioned deps from the main checkout via `link-deps.mjs` rather than `pnpm install`. Pushed directly to `origin/production-base-2026-09-05` as commit `4ad0df12a` (fast-forward from `347b0b1e0`), confirmed via `git merge-base --is-ancestor`. Second worktree cleaned up (junctions unlinked first via `findAndUnlinkWorktreeJunctions`, then removed) — main checkout's `node_modules` verified intact afterward. No logger channel binding added: Header/Footer are pure client-side React components with no new server route/subsystem, so the `growth.marketing` logging instruction doesn't apply here. No DB query was needed — no config-driven values in this change (nav labels/hrefs and design tokens are all static, from the README spec). Dropped the previous footer's live version/uptime indicator (`useVersionInfo`) since it isn't part of the new design; the hook file itself is untouched, now unused only by `Footer.tsx`. `/assessment`, `/about`, `/contact`, `/solutions/*` routes don't exist in `App.tsx` on `production-base-2026-09-05` yet — expected: #2954 is dispatched to run first, alone, and every other page issue under this Feature is `blocked_by` it and will add its own route + page. Working tree clean on both worktrees at completion (verified `git status --porcelain`).
