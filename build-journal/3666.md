# #3666 — MSP Console: no real backend for the "Managed Tenants" directory screen

- **Status:** ✅ DONE 2026-09-11T19:05:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** e1b7ae679

## Log
- 2026-09-11T18:33:00Z ⏳ IN FLIGHT — Confirmed via #3665's audit: the MSP Console root
  directory screen (`sel.kind === "root"`) has no real backend, only the design's static
  `tenantData` fixture. `GET /api/msp/customers` (msp-portal.ts) already exists but only
  returns `{id, name, domain, status, tenantId, mspId, createdAt}` — none of
  seats/people/lastScan/openSignals. Plan: add a batched directory-metrics resolver
  (seats from `/subscribedSkus` via license-waste-source.ts's `totalEnabledSeats`,
  people from `identity:department-directory`'s `totalUserCount`, lastScan from
  `msp_diagnostic_runs`, openSignals from `tenant_signal_history`) and extend the
  existing route additively — no new duplicate endpoint, no fixture data. The
  msp-console frontend (`artifacts/msp-console`) is still an unbuilt scaffold
  (#2667/#1571), so this build is backend-only; wiring the directory UI is out of
  scope until that Feature lands a real page to wire into.
- 2026-09-11T19:05:00Z ✅ DONE — Commit `e1b7ae679` on `main`: added
  `artifacts/api-server/src/lib/msp-customer-directory-metrics.ts`
  (`fetchCustomerDirectoryMetrics`) and wired it additively into the existing
  `GET /api/msp/customers` route in `msp-portal.ts`. Real data sources: seats =
  `license-waste-source.ts`'s `resolvePaidSeatFigures().provisioned` (PAID seats
  only — see the module's own header for the real bug this avoided: the
  unfiltered `/subscribedSkus` total put the testbed tenant at "1,020,001 seats"
  because its real `POWER_BI_STANDARD`/`FLOW_FREE` SKUs report
  `prepaidUnits.enabled` of 1,000,000/10,000, Microsoft's own free/trial-SKU
  convention, not real purchased seats); people = `identity:department-directory`'s
  `totalUserCount`; lastScanAt = latest `msp_diagnostic_runs.completedAt` among
  completed/partial runs; openSignals = count of `tenant_signal_history` rows with
  `resolvedAt IS NULL`. No fixture/hardcoded data — every figure traced to a real,
  already-collected table.
  Verification: full `api-server` typecheck clean (`tsc --noEmit`, exit 0); 6 new
  unit tests in `msp-customer-directory-metrics.test.ts` (normal/unclaimed/
  never-scanned/multi-tenant/truncated-page cases), all passing; existing
  `msp-portal.test.ts` suite (21 tests) still passing; live-verified end to end
  against the local `DATABASE_URL` for both real testbed tenants (customer 1:
  seats=1, people=25, lastScanAt=2026-09-06T23:07:08.981Z, openSignals=73;
  customer 3: seats=25, people=null, lastScanAt=2026-08-09T16:40:06.521Z,
  openSignals=50 — all cross-checked by hand against the raw rows via `psql`).
  Branch verified merged into `main` (`verify-branch-merged.mjs` exit 0);
  `git status --porcelain` clean. No new GitHub findings filed — the one real
  data-shape trap found mid-build (free/trial SKUs inflating a naive seat total)
  was fixed in this same commit, not left as a separate issue. Frontend wiring
  of the directory screen remains out of scope (msp-console is still an unbuilt
  scaffold per #2667/#1571).
