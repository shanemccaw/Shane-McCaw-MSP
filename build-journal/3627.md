# #3627 — Issue mirror stores blocked_by = {} for every issue not yet labeled 'blocked'

- **Status:** ✅ DONE 2026-09-12T19:21:11Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 3cc634103 (code fix), b128b5f3f (IN FLIGHT bookend)

## Log
- 2026-09-12T19:14:34Z ⏳ IN FLIGHT — Fixing `GitHubIssueMirror` so `blocked_by` is fetched for every changed/edge-bearing issue regardless of the `blocked` label. Incremental sync: fetch blocked_by for all changed issues (bounded). Full sync: add `blockedBy{totalCount}` to the board walk and fetch for any issue with edges, not just label-carriers.
- 2026-09-12T19:21:11Z ✅ DONE — Removed the `blocked`-label gate on the mirror's `blocked_by` fetch in both sync paths.
  - **Full sync** (`SyncAsync`): selector changed from `Labels.Any(==blocked)` to `BlockedByCount > 0`, where `BlockedByCount` is a new `GitBoardIssue` field populated from a `blockedBy(first: 1) { totalCount }` addition to the existing paginated board-walk GraphQL query (`GitHubApiClient.ListBoardIssuesInternalAsync`) — zero extra requests, label-independent. Only issues with a real edge are fetched, so cost is unchanged; the inverse `blocking_numbers` graph is now more complete, not less.
  - **Incremental sync** (`IncrementalSyncAsync`): now fetches `blocked_by` for every changed issue (the since= delta is tiny), bounded by the same `MaxBlockedByFetchesPerSync` (200) cap. Dropped the "pre-fill [] for non-blocked" loop — `BatchGetBlockedByAsync` returns [] for edge-less issues (fresh, correct) and real numbers for edge-bearing ones; issues past the cap or not returned preserve their existing mirrored value (haveBlockedBy=false), never wiped.
  - **Verification:** `dotnet build BuildConsole.csproj` — Build succeeded, 0 errors (10 pre-existing unrelated warnings). Live real case: **#3765** (OPEN, no `blocked` label) declares real GitHub edges `[3677, 3764]`, yet `bt_issue_mirror` stored `blocked_by_numbers = {}` (last synced 2026-09-12 15:07) — the exact bug. The new `blockedBy(first:1){totalCount}` field, queried against the live GitHub API, returns `2` for #3765, and `blockedBy(first:N){nodes{number}}` (the shape `BatchGetBlockedByAsync` uses) resolves `[3677, 3764]` — proving the fix catches an edge wired before the label lands. Cannot run the C# sync in-process from a build session (never launch BuildConsole.exe to verify); verification is: clean build + live GraphQL field validation + demonstrated live bug case.
  - Branch verified merged into origin/main (verify-branch-merged.mjs OK). Working tree clean.
