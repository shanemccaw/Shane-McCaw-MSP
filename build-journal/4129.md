# #4129 — Overview: Timeline Matrix/List toggle + Open Offers card

- **Status:** ✅ DONE 2026-09-15T00:10:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 7cb42452c (backend route + frontend implementation), c99ab1658 (data-testid), 2cf3a85fd (manifest), 424b426fd (manifest live-run result)

## Log
- 2026-09-14T23:30:00Z ⏳ IN FLIGHT — Investigating: extend timeline route with Microsoft Changes/Change Windows/Policy Reviews sources, build Matrix/List toggle, Open Offers card, section reorder, per #4129.
- 2026-09-15T00:10:00Z ✅ DONE — Added `GET /api/portal/customer/timeline/matrix` (portal-customer-timeline.ts), a real windowed feed for Microsoft Changes (publishedAt), Change Windows (scheduledStart/scheduledEnd, #1762) and Policy Reviews (reviewDueAt), kept separate from the existing cursor-paginated `/timeline` route (that route's `before=` cursor assumes past-only monotonic traversal, which breaks for forward-dated sources — documented in both routes' headers). Built `TimelineCard.tsx` (Matrix/List toggle, 4 marker lanes + 2 bar lanes, window presets) and the Open Offers card (`useOpenOffers.ts`, `GET /api/portal/offers` filtered to `state==="sent"`) in `index.tsx`, reordered sections to match the design (Timeline → Work In Flight/Portal Counts/Open Offers/Reports → Needs You/Coming Up/Happened), and updated the page's stale header comment now that the gap it documented is closed. Two deliberate simplifications vs. the design noted in code comments: native `title` tooltips instead of the mouse-tracked popover, no marker collision-clustering.
  - Verification: `tsc --noEmit` clean on both `artifacts/api-server` and `artifacts/portal`. Extended `test-manifests/portal/customer-home-overview.json` with 6 new apiTests steps (sessionTag `4129-timeline-matrix`), registered already in `_regression-suite.json`. Ran the full manifest live via `shaneapp://runTest` against the real local dev api-server/DB: **28/28 steps passed**, including real non-fixture Microsoft Changes rows and a real narrower window on `back=2&fwd=2`. Direct `DATABASE_URL` queries beforehand confirmed the real counts the new apiTests assertions are based on (494 message-center rows for the testbed tenant, 74 inside the default window; 0 change-window/policy-review rows with a real scheduled/due date today).
  - Findings filed: #4151 (stale `comingSoonHref` links in Overview's pre-existing Coming Up/Portal Counts panels for 3 modules that now have real pages — parented to #1655) and #4153 (dev-server build-set coordinator latches closed on a bookend-only commit and never re-fires restart for the real code merge — parented to #3707). Both set to AI Batter Up board status.
  - `refresh-main-server.mjs` was used directly to force the dev-server checkout current after the build-set issue above stalled the normal `request-restart.mjs` path — documented in #4153 and in the manifest's own LIVE RUN RESULT note.
  - `node scripts/dev-server/verify-branch-merged.mjs`: OK, `agent/4129-q2801` merged into `origin/main`. `git status --porcelain`: clean.
