# #3871 — Build Sets rollup epic-resolution gap (IncrementalSyncAsync never wrote parent_number)

- **Status:** ✅ DONE 2026-09-13T00:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** 76878010e (real fix), ae2d9ee69/7bad87173 (IN FLIGHT bookend, rebased)

## Log
- 2026-09-12T23:16:00Z ⏳ IN FLIGHT — investigated `GitHubIssueMirror.IncrementalSyncAsync` (confirmed its
  INSERT/UPDATE column list at ~line 1061 has no `parent_number`/`milestone_number`/`sub_issue_count`,
  matching the issue's own grep). Confirmed `GitHubIssueSinceUpdate` (the REST `since=` shape this pass
  already fetches) carries no parent/milestone/sub-issue fields either — that data is GraphQL-only
  (`ListBoardIssuesInternalAsync`'s `parent { number milestone { number } }` / `subIssuesSummary`).
  Plan: add a new small batched aliased-GraphQL fetch (`BatchGetParentInfoAsync`), following the exact
  same scoped/capped/rate-limit-circuit-aware pattern `BatchGetProjectItemStatusesAsync` (board status)
  and `BatchGetBlockedByAsync` (blocked_by) already use in this same incremental pass, scoped to the
  small `changed` set only — not a wholesale new full-walk call. Then wire `parent_number`,
  `milestone_number`/`milestone_title`, `sub_issue_count`/`completed`/`percent` into the incremental
  UPSERT with CASE-guarded preserve-on-miss, same as the existing `haveBoard`/`haveBlockedBy` columns.
  Next: `EpicResolver` narrow live-fallback for a still-null parent, then the mixed-epics header naming
  fix in `BuildQueuePanel.xaml.cs`. Verifying live against #3640/#3858/#3864/#3865 → Epic #1202.
- 2026-09-13T00:35:00Z ✅ DONE — all three real fixes landed (commit `76878010e`, rebased twice onto
  concurrent `origin/main` moves, including a real conflict merge with #3872's `--epic <N>` override
  feature that landed mid-session; resolved by keeping #3872's needsInference/explicit-override split
  and threading the new live-fallback `GitHubApiClient` through it).
  1. **Primary fix** — `GitHubApiClient.BatchGetParentInfoAsync` (new, aliased/chunked GraphQL, same
     rate-limit-circuit-aware pattern as `BatchGetProjectItemStatusesAsync`/`BatchGetBlockedByAsync`)
     wired into `GitHubIssueMirror.IncrementalSyncAsync`'s INSERT/UPDATE column list
     (`parent_number`, `parent_milestone_number`, `milestone_number`, `milestone_title`,
     `sub_issue_count`, `sub_issue_completed`, `sub_issue_percent`), CASE-guarded preserve-on-miss.
  2. **Live fallback** — `EpicResolver.ResolveTopEpicsAsync` takes an optional `GitHubApiClient`; when
     a node's own locally-mirrored `parent_number` is null it makes ONE capped (40/resolve) live fetch
     before giving up. Wired into all 3 real consumers: Batter Up, AI Batter Up, Build Sets rollup.
  3. **Reporting** — `DescribeBuildSetEpicGroup` now renders `(mixed: #1202, #3454)` instead of a bare
     `(mixed Epics)`.
  **Live verification** (throwaway untracked harness under `_scratch_verify_3871/`, deleted before
  commit — never part of any commit):
  - `BatchGetParentInfoAsync` real GraphQL fetch matched `gh issue view`'s real parent for
    #3640/#3858/#3864/#3865/#1096/#1128/#1202/#1708/#1788 exactly.
  - Reproduced the real bug live: #3864/#3865 (mirror rows exist, `parent_number` NULL) resolved to
    NOTHING via the pre-fix mirror-only walk.
  - Confirmed the fix: same walk + live fallback resolved #3858/#3864/#3865 → Epic #1202 ("Epic: Build
    Console") and #3640 → Epic #1096 ("EPIC: Application Core") — #3640 is in the separate
    `MonitoringPackage` build set, not `BuildConsole`, so #1096 is its genuine real epic; this corrects
    the issue body's premise that #3640 also belongs to #1202 (not a code defect — the fix resolves
    each set to its own real epic, which is the correct behavior).
  - Ran the REAL (reflection-invoked, since `IncrementalSyncAsync` is `private`) incremental sync
    against a live 90-minute `since=` window: `Ok=true`, 86 changed issues, 86 parent-infos
    targeted-fetched, 0 errors. Direct `psql` read afterward confirmed #3864→1788, #3865→1708,
    #3858→1708, and #3871 itself (this session's own issue, "Sub-issue of #1788")→1788 — all now
    correctly populated by ONE real incremental pass, closing the sync gap itself, not just papering
    over it with the live fallback.
  - Re-derived full chains for both named build sets purely from mirror data post-fix: `BuildConsole`
    set (#3858/#3864/#3865/#3829/#3840) all → Epic #1202; `MonitoringPackage` set
    (#3640/#3641/#3643/#3645/#3574) all → Epic #1096 — each set now resolves to its own single real
    Epic, no more "(mixed Epics)"/"No Epic".
  - `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors (both before and after the #3872 merge).
  No new findings filed — the one apparent discrepancy (#3640's real epic) is a correction of the
  issue body's illustrative example, not a code defect. `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms `76878010e` is an ancestor of `origin/main`.
