# #3163 — Feature: Bankruptcy/debt tracker, ported from Finance-Tracker (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T02:55:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 97a6fa896 (feature), 144332262 (bookend IN FLIGHT)

## Log
- 2026-09-07T00:00:00Z ⏳ IN FLIGHT — starting: read #3163 body, confirmed #3137 (blocker) is CLOSED/complete. Next: audit Finance-Tracker's BankruptcyItem shape (read-only) and ShanesSurvival's real `debts` table before deciding overlay vs separate table.
- 2026-09-08T02:55:00Z ✅ DONE — Audited `shanemccaw/Finance-Tracker` (local clone at `C:\Source\FinanceTracker\Finance-Tracker`): `BankruptcyItem` (FinanceContext.tsx:160-169, 1087-1112) is fully modeled + CRUD'd but never wired to any screen (FINANCE_TRACKER_AUDIT.md §6, "dead sub-feature"). Read ShanesSurvival's real `debts` table (2 real rows: H1 Mortgage arrears, Treasury Offset/IRS installment, both `is_critical`). Decision — overlay onto `debts`, not a second list — was independently already on record in `docs/shanes-life-design-contract-pack.md` §12, confirming the investigation. Shipped: migration `042_bankruptcy_overlay.sql` (`debt_type`/`original_balance`/`last_payment_date`/`included_in_bankruptcy`, backfilled on the 2 real rows), `src/core/money.mjs` `listDebts`/`getDebt`/`createDebt`/`updateDebt`/`deleteDebt`, `GET/POST /api/money/debts` + `PATCH/DELETE /api/money/debts/:id`, MCP `list_debts`/`set_debt`/`delete_debt`, and a new Money "Bankruptcy" tab in `public/app.js` (list/add/edit/delete). Migration renumbered 041→042 after a live concurrent-session number collision (`041_plaid_item_health.sql`, a different in-flight session, not yet merged at time of writing) — same pattern migration 038's own header documents; ledger row fixed up by rename, not re-run. Verified: direct CRUD round-trip against the real local `finances` DB (create → update → delete, confirmed back to the real 2-row baseline), plus `getGateStatus`'s `protectedDebts` confirmed byte-identical before/after against the real Shane user — the exact "surface existing debt data, don't duplicate it" verification the issue asked for. Pushed straight to `main` (97a6fa896), fast-forward, `verify-branch-merged.mjs` confirms merged. Working tree clean. No findings to file.
