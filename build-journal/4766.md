# #4766 — Ctrl+D Dispatch: Feature number dispatches every ready sub-issue beneath it

- **Status:** ✅ DONE 2026-09-18T18:55:48Z
- **Scope:** buildconsole
- **Started:** 2026-09-18
- **Commit(s):** ddfd550ba

## Log
- 2026-09-18T18:52:30Z ⏳ IN FLIGHT — auditing IssueDispatchService sub-issue fetch + DispatchChainAsync reporting pattern, then adding Feature fan-out to DispatchPanel.DispatchAsync (Epics untouched).
- 2026-09-18T18:55:48Z ✅ DONE — Feature fan-out shipped (ddfd550ba). IssueDispatchService.ResolveFeatureAsync reuses GitHubApiClient.GetSubIssuesAsync (leaf = no sub-issues → null, no extra call) + GetIssueAsync title; "EPIC:" prefix (case-insensitive, so "Epic:" too) → null, Epic behavior untouched. DispatchFeatureAsync calls the existing DispatchAsync once per OPEN sub-issue, one report line per member (Queued/QueuedButBlocked = dispatched; AlreadyTracked/NoBuildComment reported honestly, never force-dispatched or chat-asked in bulk), closed children skipped and counted in the header, zero-open Feature shows "nothing to dispatch". Wired into Ctrl+D's real path (CommandPaletteWindow.DispatchTypedIssueAsync — Ctrl+D no longer opens DispatchPanel since #3829) AND DispatchPanel.DispatchAsync. Scope confirmations: closed sub-issues skipped silently-but-counted; zero-open gets an explicit message. Verification: clean dotnet build (0 warn / 0 err); confirmed via gh api that sub_issues state is lowercase open/closed and real titles (#3827/#4692 "Feature:", #1202 "Epic:") match the detection. NOT live-verified end-to-end in the app (never launch the BuildConsole exe). No migration. No findings filed. Tree clean.
