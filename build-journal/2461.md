# #2461 — RBAC: rewire existing AdminV2 AD-style UI to the new roles/user_roles model

- **Status:** ✅ DONE 2026-09-09T23:39:41Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** 0e5e0c0b8 (rebased to f57d18742 on main)

## Log
- 2026-09-09T23:10:00Z ⏳ IN FLIGHT — Read #2461 body + #1696 architecture comments (2026-08-29,
  2026-09-09) and #2455's landing commit (884515a1e). Confirmed #2455's blocked_by is real and
  cleared: `msp_roles`/`msp_user_roles`/`msp_feature_role_mapping` and their customer-side twins,
  plus the shared evaluator (`lib/db/src/rbac/`), landed additive-only with zero call sites moved.
  #2457 (express current roles as data) and #2458 (move enforcement) have NOT landed yet, so the
  new tables have no real assignment data and MSP_ROLES/requireRole remain the live enforcement
  path — unchanged by this build, per the architecture comment's explicit sequencing. Read the
  existing shipped AdminV2 AD UI (adApi.ts, adTypes.ts, AdUserCanvas.tsx, AdMspCanvas.tsx, the
  admin-active-directory.ts route file) per the issue's own "do not rebuild" instruction. Building
  new admin CRUD routes + UI panels to manage the new msp_roles/customer_roles/user_roles/
  feature_role_mapping tables, additive alongside the existing ladder-based role/entitlement UI
  (which stays wired to MSP_ROLES until #2458 lands).
- 2026-09-09T23:39:41Z ✅ DONE — Shipped:
  - `lib/db/src/rbac/admin.ts` (+`admin.test.ts`, 8 real-DB cases, rolled back): CRUD for
    org-scoped roles, grant/revoke user membership (surfacing the DB scope trigger's refusal
    as `{ ok: false }`), read/set a capability's allow/deny mapping. Fixed a real type bug this
    build's own first real caller surfaced in `lib/db/src/rbac/load.ts`'s `RbacDb` type
    (`NodePgDatabase<Record<string, never>>` rejected the fully-typed `db` singleton) — widened
    to `NodePgDatabase<any>` since nothing here uses the schema-typed relational API.
  - `artifacts/api-server/src/routes/admin-rbac.ts` (+`admin-rbac.test.ts`, 18 HTTP-plumbing
    cases, DB-mocked): the REST surface for the above, `requireAdmin`-gated, audit-logging every
    write.
  - `artifacts/admin-panel/src/adminv2/screens/ad/AdRbacPanels.tsx`: `AdRbacUserRolesSection`
    (embedded in `AdUserCanvas`) and `AdRbacOrgRolesPanel` (embedded in `AdMspCanvas` /
    `AdCustomerCanvas`) — additive alongside the existing `DirectoryGroupRole` ladder UI, which
    is untouched (still what's enforced until #2458).
  - Verified: `lib/db` full suite 89/89 passing (incl. #2457's concurrent legacy-ladder tests,
    landed mid-build and rebased onto cleanly); `admin-panel`'s existing `ad.test.tsx` 9/9 still
    passing against the new backing data (issue's own Done-when); `api-server`'s existing
    admin-active-directory suite 45/45 still passing; `lib/db`, `api-server`, `admin-panel` all
    `tsc --noEmit` clean. No fixture/hardcoded data — every panel reads real rows through the
    new routes.
  - No findings filed — no genuine out-of-scope bug hit; the one real bug found (the `RbacDb`
    type) was fixed in this same build, which CLAUDE.md's finding rule explicitly excludes
    ("Not a finding: ... a gap you fixed in this build").
  - Merged: `verify-branch-merged.mjs` confirms `f57d18742` is an ancestor of `origin/main`.
    Working tree clean (`git status --porcelain` empty). #2457 landed concurrently mid-build;
    rebased twice onto it cleanly (one real conflict in `lib/db/vitest.config.ts`'s `include`
    list, resolved by keeping both new entries).
