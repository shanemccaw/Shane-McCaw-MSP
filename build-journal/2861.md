# #2861 — msp-customers-bulk.test.ts mock missing RETENTION_CLOCK_RUNNING_TENANT_STATUSES export

- **Status:** ✅ DONE 2026-09-04T16:15:00Z
- **Scope:** platform

## Log
- 2026-09-04T16:05:37Z — Starting. Confirmed via issue body: `@workspace/db` mock in
  `artifacts/api-server/src/routes/msp-customers-bulk.test.ts` doesn't export
  `RETENTION_CLOCK_RUNNING_TENANT_STATUSES`, needed at module load by
  `src/lib/retention/subscription-state.ts:87` (imported transitively via
  `msp-portal.ts:30`), failing all 9 tests in the file. Fix: add the export
  (`["active", "onboarding"] as const`, matching `lib/db/src/schema/retention.ts:92`)
  to the mock.
- 2026-09-04T16:15:00Z — ✅ DONE — Added `RETENTION_CLOCK_RUNNING_TENANT_STATUSES:
  ["active", "onboarding"] as const` to the `@workspace/db` mock in
  `msp-customers-bulk.test.ts`. `npx vitest run msp-customers-bulk` now passes all
  9 tests (was failing all 9 before the fix). Fix commit `5a0abac4d35f435e4e9b0ee8bdb247565cc7583b`,
  rebased and pushed to `main` at `95c1e7ddcdf28d84b27581c9a8818e8416e2acd7`. Ancestor
  check confirmed (`verify-branch-merged`/`merge-base --is-ancestor` = ANCESTOR).
  Working tree clean.
