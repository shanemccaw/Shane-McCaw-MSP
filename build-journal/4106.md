# Issue #4106 — Build Queue card: "Copy Build Log (Last 10 Lines)" context menu item

- **Status:** ⏳ IN FLIGHT 2026-09-14T19:05:49Z
- **Scope:** buildconsole

## Log
- 2026-09-14T19:05:49Z — Starting. Adding a "Copy Build Log (Last 10 Lines)" MenuItem next to the existing "Open Build Log" item in `BuildCardContextMenu` (`Controls/BuildQueuePanel.xaml.cs`), reading `BuildLogPaths.ForQueueItem(item.Id)`, last 10 lines via `File.ReadLines(...).TakeLast(10)`, `Clipboard.SetText` in try/catch, `ToastEngine` success/warning.
