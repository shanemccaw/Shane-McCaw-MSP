# Issue #4106 — Build Queue card: "Copy Build Log (Last 10 Lines)" context menu item

- **Status:** ✅ DONE 2026-09-14T19:16:00Z
- **Scope:** buildconsole

## Log
- 2026-09-14T19:05:49Z — Starting. Adding a "Copy Build Log (Last 10 Lines)" MenuItem next to the existing "Open Build Log" item in `BuildCardContextMenu` (`Controls/BuildQueuePanel.xaml.cs`), reading `BuildLogPaths.ForQueueItem(item.Id)`, last 10 lines via `File.ReadLines(...).TakeLast(10)`, `Clipboard.SetText` in try/catch, `ToastEngine` success/warning.
- ✅ DONE 2026-09-14T19:16:00Z — Added `miCopyBuildLog` in `BuildCardContextMenu` (`desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs`), gated on `Services.BuildLogExistenceCache.HasLog(item.Id)` (same gate as "Open Build Log"/the log-exists dot badge), reading `Services.BuildLogPaths.ForQueueItem(item.Id)` via `File.ReadLines(path).TakeLast(10)`, joined with `Environment.NewLine`, `Clipboard.SetText` in try/catch, `ToastEngine.Success`/`Warning`/`Error` for the three outcomes. `dotnet build desktop/BuildConsole/BuildConsole.csproj` — 0 warnings, 0 errors. Commit `a53c63e18` pushed directly to `origin/main` (fast-forward from `d902f1174`). `verify-branch-merged.mjs` confirms merged; `git status --porcelain` clean.
