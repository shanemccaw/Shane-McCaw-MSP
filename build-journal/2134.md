# #2134 — Color chat #NNNN mentions by issue type/status at render time

- **Status:** ✅ DONE 2026-09-01T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-01
- **Commit(s):** 9c1fc6e7a

## Log
- 2026-09-01T00:00:00Z ⏳ IN FLIGHT — Read issue #2134; investigating IssueMentionInjector.cs (`makeIssueSpan`, `decorateTextNode`) to add eager type/status color resolution off the existing IsEpic cache, complementing #2133's hover EPIC badge.
- 2026-09-01T00:30:00Z ✅ DONE — Added `window.__btSetMentionColors` (JS) so `makeIssueSpan` colors the mention SPAN itself (Epic `#F9E2AF`, Blocked `#F38BA8`, Closed `#6C7086`, default `#89B4FA`; priority blocked > epic > closed > default) instead of only on hover; `_mentionSpans` tracks live spans per number with disconnected-span pruning. `ChatMentionPopupHelper.BuildSetMentionColorsScript` resolves purely off `LeftSidebar.BuildDetailIssue` cache (no live GitHub fetch) and is pushed the moment `BT_ISSUE_MENTIONS_SCAN` reports fresh on-screen numbers, wired in both `MainWindow.xaml.cs` and `FloatingChatWindow.xaml.cs` (the latter previously had no handler at all for that message). The existing hover round-trip (`__btShowIssueTip`) also recolors, since it can carry a fresher/live-fetched resolution. Numbers not yet cached keep the default blue until hover resolves them (graceful fallback, matches the pattern the issue asked for). `dotnet build BuildConsole.csproj -c Debug` succeeded, 0 warnings/0 errors. Branch verified merged into origin/main via `verify-branch-merged.mjs`. `git status --porcelain` clean.
