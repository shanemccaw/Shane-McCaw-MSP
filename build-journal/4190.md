# #4190 — Milestone mirror completeness off-by-one gate

- **Status:** ✅ DONE 2026-09-15T02:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** 99ce85a5e (pushed to main as 660981716)

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — starting investigation: query bt_issue_mirror/bt_milestone_mirror to find the real missing issue for milestone #5 gap, then relax CheckMilestoneCompletenessAsync gate.
- 2026-09-15T02:10:00Z ✅ DONE — Part 1: queried bt_issue_mirror/bt_milestone_mirror (BuildConsole db) directly and diffed against a live GitHub GraphQL pull of milestone #5's real issue set. Found exactly 2 real issues missing at investigation time: #4165 and #4197, both created within the hour before the check ran. By the time of the second query the incremental sync had already caught up on its own (mirroredTotal now == realTotal, 2392/2392) — a transient sync-lag near-miss, not a stuck/broken sync path. No sync-code fix needed for Part 1; documented honestly per the issue's own "transient vs stuck" framing.
  Part 2: relaxed `CheckMilestoneCompletenessAsync` (desktop/BuildConsole/Services/GitHubIssueTimeSeriesService.cs) from a strict `mirroredTotal < realTotal` gate to `gap > CompletenessGapTolerance` (tolerance = 2), logging via ActivityLog when a gap is tolerated. Audited `CheckEpicCompleteness` — same shape, but applied the tolerance to the SUM of missing issues across the whole transitive subtree (not per node), since a per-node tolerance would compound across a large tree and defeat the bound. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` clean, 0 warnings/errors. No test manifest — BuildConsole (WPF) is out of scope for test manifests per CLAUDE.md. Branch verified merged into origin/main via `verify-branch-merged.mjs`; working tree clean.
