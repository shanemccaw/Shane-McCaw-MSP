# #4285 — MyArchitect: Home tab (no-tenant start, multi-column layout, tenant card list)

- **Status:** ✅ DONE 2026-09-15T23:21:49Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** shanemccaw/MyArchitect@daef31e

## Log
- 2026-09-15T23:16:56Z ⏳ IN FLIGHT — Confirmed blockers #4278 (superseded by #4305, closed) and #4284 both closed; cloned shanemccaw/MyArchitect into C:\wt\MyArchitect-4285; investigating current Home tab / tenant model / startup behavior.
- 2026-09-15T23:21:49Z ✅ DONE — Real fix, no fixtures. `TenantService.LoadTenantsAsync` no longer falls back to `_tenants.FirstOrDefault()` when there's nothing to restore, so `MainWindow_Loaded` no longer auto-opens a portal tab — app genuinely starts with no tenant selected. `HomeDashboardView.xaml` restructured into a real 2-column `Grid` (column 0 fixed-width, column 1 `*`) so a future column is a contained addition. Column 0 is a complete, real tenant card list (name, tenant GUID, domain) bound to `ITenantService.Tenants`/`TenantsChanged`, each card wired to the same `SelectTenant` call the title-bar `TenantSwitcher` already uses, with the active tenant's card visibly highlighted (`TenantCardActiveStyle`, `AccentBrush`, no new inline styles — §10 clean, grep-verified). Column 1 keeps the existing per-tenant dashboard (#3656) content unchanged, but now shows an honest "Select a tenant from the list on the left…" prompt instead of five cards stuck on "Loading…" when nothing is selected. `dotnet build MyArchitect.csproj -c Debug` — 0 warnings, 0 errors. Pushed straight (no rebase needed) to `shanemccaw/MyArchitect` main as `daef31e`. Did not launch the WPF exe from this session (same reasoning as BuildConsole's own "never launch the app exe from an agent session for verification" — a GUI process launched headless doesn't give real signal and risks hanging the session); verification here is the clean build + grep-verified theming compliance + direct code reading of the wiring, not a live screenshot.
