# #2938 — shebang+CRLF .mjs class fix

- **Status:** ✅ DONE 2026-09-06T14:10:00Z
- **Scope:** platform

## Log
- 2026-09-06T13:55:00Z — IN FLIGHT. Adding `.gitattributes` rule enforcing LF on `.mjs` files, then grepping for other shebang+`.mjs` files that would be equally vulnerable to CRLF corruption (per #2821's investigation, restored into #2938's body).
- 2026-09-06T14:10:00Z — ✅ DONE. Added `*.mjs text eol=lf` to `.gitattributes` (commit 09981448). Confirmed committed blobs were already LF and the observed CRLF was purely a local Windows working-tree artifact of `core.autocrlf=true`; verified the new rule forces LF on a fresh checkout regardless, then renormalized every tracked `.mjs` file's working-tree copy (delete+checkout) so nothing stayed dirty. Grepped repo-wide for shebang-bearing `.mjs` files: ~32 found across `scripts/`, `scripts/dev-server/`, `scripts/config-state/`, `scripts/azure/`, `artifacts/mcp-server/scripts/` — all now covered by the repo-wide rule (no per-file fix needed). Verified branch merged into origin/main via `verify-branch-merged.mjs` (OK), `git status --porcelain` clean.
