# #3467 — LeftSidebar blocked-by sweep: migrate EnrichBlockedStatusCoreAsync + hover to bt_issue_mirror

- **Status:** ✅ DONE 2026-09-10T03:33:11Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** `405623e69` (IN FLIGHT bookend), `6e6720173` (mirror-first blocked-by sweep + hover relationships)

## Log
- 2026-09-10T03:27:15Z ⏳ IN FLIGHT — Migrate the Git Board's blocked-by enrichment off live per-issue GitHub REST calls onto the local `bt_issue_mirror` graph, same mirror-first / fail-closed-to-live shape as #3358 (board tree) and #3113/#3134. Two call sites in `Controls/LeftSidebar.xaml.cs`: (1) `EnrichBlockedStatusCoreAsync` (`:2392`/`:2410`) — fires one `GetOpenBlockedByAsync` per open issue (~528 at scale) on every board load (startup / Issues-tab open / manual refresh / post-write reload); the single biggest remaining automatic GitHub burst. (2) companion hover path `LoadIssueRelationshipsAsync` (`:6627`/`:6636`) — 2 live calls per hovered issue. Mirror already carries `blocked_by_numbers` / `blocking_numbers` / `state` / `labels` (`GitHubIssueMirror.cs:210-211`); resolve blocked status from the mirror graph (first still-OPEN blocked_by), falling back to the live sweep only on a mirror miss/never-synced/error. Scope caveat (per issue body, honest): mirror sync fetches blocked_by ONLY for `blocked`-labeled issues (`GitHubIssueMirror.cs:113-116`) — relies on the repo convention that `blocked` label is always set alongside a blocked_by edge (CLAUDE.md blocked-label protocol). Verified this convention holds in the sync code; documenting the assumption rather than silently trusting it.
- 2026-09-10T03:33:11Z ✅ DONE — Shipped in commit `6e6720173`. Both call sites now read `bt_issue_mirror` before ever constructing a `GitHubApiClient`.
  - **`EnrichBlockedStatusCoreAsync`** — new `TryResolveBlockedStatusFromMirrorAsync(openIssues)` does two batched `GetManyAsync` reads (the open issues' own rows for their `blocked_by_numbers`, then every referenced blocker's row for its own open/closed state + title) and replicates `GetOpenBlockedByAsync`'s exact contract (blocked iff a still-OPEN blocked_by exists; reported blocker = first open one). Because `blocked_by_numbers` stores ALL declared blockers (open+closed — confirmed against the sync's `GetBlockedByAsync` upsert), the still-open filter consults each blocker's own row, exactly as `ChatDockService.WalkChainFromMirrorAsync` does. Returns null → the original concurrency-6 live sweep, unchanged, only on a mirror miss/never-synced/error. Git #1367 Whammy/Sparky diff preserved: `previousKnownBlocked` captured before mutation, diff computed uniformly from populated `IsBlocked` for both sources, first-sweep flood-guard (`previousKnownBlocked == null`) intact.
  - **`LoadIssueRelationshipsAsync` (hover)** — new `TryLoadIssueRelationshipsFromMirrorAsync(number)` builds both directions (`blocked_by_numbers` + inverse `blocking_numbers`) into `GitHubIssueResult`s from the mirror; live `GetBlockedByAsync`+`GetBlockingAsync` pair only on a mirror miss.
  - **Fail-toward-showing-the-badge:** a blocker/related issue the mirror has never heard of is treated as still-open, so a stale badge self-corrects next sync rather than firing a spurious Sparky "unblocked!".
  - **Verification:** `dotnet build BuildConsole.csproj -c Debug` → **Build succeeded, 0 Warning(s), 0 Error(s)**. Grep-check (from the issue): mirror resolve at `:2417` precedes `new GitHubApiClient(pat)` at `:2439` (now only in the live-fallback else). Did NOT launch BuildConsole.exe (single-instance mutex courier hazard — memory `never-launch-buildconsole-exe-for-verification`); BuildConsole is out of scope for test manifests per CLAUDE.md. No findings filed (clean migration; the `blocked`-label scope caveat was already flagged in the issue body and is verified, not a new gap). Branch verified merged into main; working tree clean.
