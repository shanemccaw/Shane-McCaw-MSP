# #2316 — Focus-build parking reasons ride on the GitMapFocusBuild card

- **Status:** ✅ DONE 2026-09-03T00:22:53Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** ad48fea39 (rebased/pushed as 5c318479c on origin/main)

## Log
- 2026-09-02T19:35:00Z ⏳ IN FLIGHT — investigated `GitMapFocusBuild`/`GitMapService`/`GitEpicPanelService`.
  Real state: `GitMapFocusBuild` only ever represents the currently in-flight feature; when nothing
  is in-flight the Focus Build card shows a generic "no in-flight" note even if the epic's previous
  focus build was just Parked. `Park`/`Unpark` (`GitEpicParkClicked`) writes the GitHub Project
  Status field but never captures *why*. Plan: prompt for a reason on Park via `AppDialog.Input`,
  post it as a real `gh issue comment` (`Parked: <reason>`), and have `ResolveFocusBuildAsync` fall
  back to the epic's most recently parked feature (real Project Status="Park" overlay) + its real
  latest "Parked:" comment when no feature is in-flight, surfaced on the same `GitMapFocusBuildCard`.
- 2026-09-03T00:22:53Z ✅ DONE — implemented and shipped:
  - `Services/GitMapService.cs`: `GitMapFocusBuild` gained `IsParked`/`ParkReason`. `ResolveFocusBuildAsync`
    now falls back to the epic's real Parked feature (via `GitEpicPanelService.OverlayParkPauseAsync`)
    when nothing is in-flight, and reads the real reason via new `GetLatestParkReasonAsync` (`gh api`
    on the issue's own comments, matching the most recent `Parked: …` one — null when none exists).
  - `Services/GitEpicPanelService.cs`: new `PostParkReasonCommentAsync` posts `Parked: <reason>` as a
    real `gh issue comment` when Park is invoked with a reason.
  - `MainWindow.GitEpicPanel.cs`: `GitEpicParkClicked` now prompts via `AppDialog.Input` for the real
    reason before writing the Park status (Unpark unaffected; a cancelled/blank prompt still parks,
    just without a reason comment).
  - `MainWindow.xaml.cs`: `GitMapFocusBuildCard` renders the Parked variant (PARKED badge, Parked
    palette, real reason text or an honest "No reason recorded." — never fabricated); the empty-state
    note now reads accurately ("…and none is Parked.").
  - Verification: `dotnet build ShaneBuilder.csproj -c Debug` — Build succeeded, 0 errors (re-verified
    after rebasing onto a concurrent session's #2315 mirrored-pairs commit to the same file — clean
    rebase, no conflicts, rebuild still green).
  - Commit `ad48fea39` (local), landed on `origin/main` as `5c318479c` after rebase; `verify-branch-merged.mjs`
    confirms merged; `git status --porcelain` clean.
  - No new findings filed this session.
