# #2915 — is_critical flag on debts — never bury a real critical debt again (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T00:35:00Z
- **Scope:** platform (ShanesSurvival)
- **Started:** 2026-09-04
- **Commit(s):** 27e369953

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — adding `debts.is_critical` (migration), `record_debt` optional
  `isCritical` param, `get_debts` critical-first section, then marking H1 Mortgage and Treasury
  Offset Program critical for real during verification.
- 2026-09-04T00:35:00Z ✅ DONE — `migrations/010_debt_is_critical.sql` adds `debts.is_critical`
  (boolean, default false, same Shane-assigned/never-inferred semantics as `accounts.is_gate`);
  ran it myself against the local `finances` Postgres DB and recorded it in `schema_migrations`.
  `record_debt` gets an optional `isCritical` param (unset on update leaves it unchanged, same
  partial-update convention as `isDelinquent`/`notes`). `get_debts` now lists critical debts in
  their own always-surfaced section first, then the rest worst-days-past-due first — same visual
  pattern `bill_status`/`gate_status` use for GATE-tier bills.
  Verified end-to-end against the real local DB: called the real `DebtRepository.UpsertAsync`
  path (the same code `record_debt` calls) to mark H1 Mortgage and Treasury Offset Program
  critical — both real, already-on-file debts that genuinely qualify (foreclosure/levy risk) —
  confirmed both now sort first via `ListAsync`, added one throwaway non-critical test row,
  confirmed it sorted normally in the second section, then deleted it. `dotnet build
  ShanesSurvival.sln` clean, 0 warnings/errors. Working tree clean; branch merged into
  `origin/main` (verified via `verify-branch-merged.mjs`).
  No new findings filed — no gap discovered outside this issue's own scope.
