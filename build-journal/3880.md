# #3880 — Notification Tray 4/4: bell icon on ActivityBar

- **Status:** ✅ DONE 2026-09-13T01:05:00Z
- **Scope:** buildconsole

## Log
- 2026-09-13T00:45:00Z — Started. Blockers #3879 (tray panel) and #3877 (store) both labeled
  `complete` and merged to main already. Claimed dispatch via claim-dispatch.mjs. Implementing
  bell icon in `Controls/ActivityBar.xaml` that opens a Popup hosting `NotificationTrayPanel`
  (#3879), with color reflecting `NotificationHistoryStore.Bookmarked.Any()` — no count badge.
- 2026-09-13T01:05:00Z — ✅ DONE — commit `00aea511d`. New `BtnNotifications` bell button in
  `Controls/ActivityBar.xaml` opens a `Popup` hosting `NotificationTrayPanel` (#3879), reloading
  it fresh every open (`NotificationTray.Refresh()`), same convention as `BtnWebTools`/`BtnShelf`.
  Bell color is the only signal — `PeachBrush` when `NotificationHistoryStore.Bookmarked.Count > 0`,
  `Subtext1Brush` otherwise — no count/badge added, per the issue's explicit ask. Added a
  `NotificationHistoryStore.Changed` event (fired from `Add`/`SetBookmarked`/
  `ClearAllNonBookmarked`) so the bell's color updates live from either the tray or a floating
  toast's own bookmark button, without polling. Verified: `dotnet build BuildConsole.csproj -c
  Debug` — Build succeeded, 0 warnings, 0 errors. Branch verified merged into origin/main via
  `verify-branch-merged.mjs`. Working tree clean. No findings filed — no gaps hit outside this
  issue's scope. Feature #3876 is now fully implemented (last of its 4 sub-issues).
