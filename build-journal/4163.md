# #4163 — DetectArea and DevServiceRouting.ServiceKeyForRoute disagree on route→area mapping

- **Status:** ✅ DONE 2026-09-15T01:07:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-15
- **Commit(s):** ce8439603

## Log
- 2026-09-15T01:03:58Z ⏳ IN FLIGHT — have `VisualTestTrackerExportService.DetectArea` delegate to `DevServiceRouting.ServiceKeyForRoute` for path-shaped input before falling back to its own keyword/port heuristics, and align the unmatched-route default with `DevServiceRouting`'s (Marketing) instead of `MSP_Console`. Then verify `SessionSyncDialog`/`ExportBugDialog` consumers still resolve correctly for routes already working today.
- 2026-09-15T01:07:00Z ✅ DONE — `DetectArea` (`desktop/BuildConsole/Services/VisualTestTrackerExportService.cs`) now tries `DevServiceRouting.ServiceKeyForRoute` on `pagePath` and on the url's path component first, mapped through a new `AreaForServiceKey` switch, before falling through to the existing port/keyword heuristics unchanged. Final unmatched-route fallback changed from `MSP_Console` to `Marketing`, aligning with `DevServiceRouting.DefaultServiceKey`. `dotnet build BuildConsole.csproj -c Debug` succeeded (0 errors/warnings). Verified via `Add-Type` + reflection against the built DLL: all 3 previously-broken cases (`/content`, `/view-as`, and the 6 missing Marketing routes `/retainers` `/monitoring` `/buy` `/records` `/lp` `/copilot-assessment`) now resolve correctly (Admin-Panel / Marketing), a fully-unrecognized route now returns Marketing instead of MSP_Console, AND every previously-working case (all 5 real ports, `/admin/dashboard`, `/portal/...`, and the legacy keyword-only fallbacks for `billing`→Portal, `msp-website`→MSP_Marketing, `console`→MSP_Console with no path) still resolves unchanged. Confirmed both consumers (`MainWindow.TestMode.cs`'s `ExecuteEndSessionSyncAsync` / SessionSyncDialog auto-fill, and `ExportBugDialog.xaml.cs`'s `SetAreaSelection`) just forward `DetectArea`'s return value into the Area combobox/selection, and every value it can now return is already in `DefaultAreas`. No fixture/hardcoded data touched. Branch verified merged into origin/main (`verify-branch-merged.mjs`); working tree clean.
