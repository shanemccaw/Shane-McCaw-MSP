# #3027 — Security Plan: wire the portal UI to the real endpoints (v2)

- **Status:** ✅ DONE 2026-09-06T19:47:03Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** 80018559f (local) / merged to `main` as 7b30cd561 — "Security Plan: wire portal UI to real endpoints (v2)"

## Log
- 2026-09-06T19:45:50Z ⏳ IN FLIGHT — investigation, backend implementation (drift endpoint),
  frontend wiring (types/api hooks/page/dialog/route/nav), typecheck, unit tests and a real
  `vite build` are already done at the point this file is first written; this entry is filed
  late (should have been the very first commit per CLAUDE.md's bookend convention — noted
  honestly rather than backdated). Remaining before DONE: bookend commit, issue comment,
  final clean-tree check, push.
- 2026-09-06T19:47:03Z ✅ DONE — Merged to `main` as `7b30cd561`. Summary of what shipped:
  - **Backend:** new `GET /api/portal/security-plan/drift` in
    `portal-security-plan-document.ts`, reusing the existing `getSecurityPlanDrift`/
    `computeSecurityPlanDrift` (`security-plan-drift.ts`) that was previously MSP-only —
    the real live-vs-last-signed comparison the design's "Changes since signing" section
    needs. 3 new unit tests added (13/13 passing in
    `portal-security-plan-document.test.ts`).
  - **Frontend:** new customer-facing page at `/security-plan`
    (`artifacts/portal/src/pages/security-plan.tsx`) plus
    `security-plan-{types,api}.ts` and `components/security-plan/{SignVersionDialog,
    ModuleSchedule,DriftPanel}.tsx`. Wires the plan-of-record header, the real customer
    sign action (`portal-security-plan-document.ts`'s `#2949` sign route), all seven
    real assembled module schedules, authored prose (scope/methodology/exclusions/
    executive summary), the new drift panel, and version history — every number from a
    real endpoint per `docs/security-plan-contract-pack.md`. Route wired in `App.tsx`,
    `moduleNav.ts` `builtPath` flipped from `null` to `/security-plan`.
  - **Deliberate scope divergence from the Design/portal export** (documented inline in
    the page and in the commit message): the design's "Part II — Control Domains"
    grouping (Identity/Data/Collaboration/Change/Monitoring) and its cross-module "still
    needs attention" roll-up were NOT built — neither maps to any real field on
    `SecurityPlanAssembledItem` or the seven source tables (`security-plan-assembly.ts`);
    building either would be inventing a display vocabulary onto data that does not carry
    it, which CLAUDE.md's HARD RULE forbids. This is not a missing-backend case (the
    HARD RULE's "BUILD IT" half) — there is no real dimension to build a backend for.
  - **Test manifest:** new `test-manifests/portal/security-plan.json`, registered in
    `_regression-suite.json`.
  - **Verification:** `tsc --noEmit` clean on both `artifacts/api-server` and
    `artifacts/portal`; `vitest run` 13/13 passing
    (`portal-security-plan-document.test.ts`); a real `vite build` of `artifacts/portal`
    with the new page wired in (2225 modules, no errors). `shaneapp://runTest` could
    **not** run the new manifest — see finding below; verified by the other real means
    above instead.
  - **Finding filed:** #3059 — `shaneapp://runTest` cannot resolve any manifest under
    `test-manifests/portal/` (reproduced against my new manifest AND an already-merged
    one, `risk-register.json`, ruling out a worktree-freshness explanation; a control
    manifest under `test-manifests/smoke/` resolved fine). Parented as a sibling
    sub-issue of Feature #1495 per this build's own dispatch instructions, labeled `bug`,
    board status set to "AI Batter Up".
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `7b30cd56` is an
    ancestor of `origin/main`.
