# #1364 — Change Control: honest no-data gating of freeze calendar / CAB agenda / notification rules

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-26
- **Commit(s):** <filled at push>

## Log
- 2026-08-26 ⏳ IN FLIGHT — Gate the freeze calendar, CAB agenda and notification-rules sections behind NoScanDataState on live tenants (no backing table; they leak fictional "Halden Materials" people).
- 2026-08-26 ✅ DONE — `portal-v2-change-control.tsx`: `CalendarView`, `ReviewView` and `SettingsView`'s notifications block + policy Active-freeze field now render `NoScanDataState` (#1339) when `ctrl.dataState === "live"`, instead of the design's fictional worked example (`CC_FREEZE`/`CC_FREEZES`/`CC_CAB`/`CC_NOTIF`). Fixture/demo mode (unscoped tenant) still shows the worked example, matching the CR register's own fixture fallback. New testIds `cc-calendar-no-data` / `cc-review-no-data` / `cc-notif-no-data` / `cc-settings-active-freeze`. Test manifest `test-manifests/portal/change-control.json` updated: CC35-CC37, CC40-CC42, CC47/CC47b/CC47c now assert the honest no-data states on the live shane-mccaw-consulting tenant (tagged `cc1364`). Verified: esbuild tsx syntax check PASS, manifest JSON valid. UI manifest run deferred — asserts against the DEPLOYED build, so it needs this commit to deploy first. Real freeze/CAB/notification tables are separate future work.
