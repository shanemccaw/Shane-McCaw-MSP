# #2778 — lib/db composite project never built — phantom TS2305/TS6305 errors inflate #2777's real drift count

- **Status:** ✅ DONE 2026-09-04T01:10:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 7fc729ff0 (api-server pretypecheck wiring)

## Log
- 2026-09-04T00:45:00Z ⏳ IN FLIGHT — confirmed real repro (rm .tsbuildinfo + lib/db/dist, tsc --noEmit -p . in artifacts/api-server produces TS6305 phantom errors). Proceeding to build lib/db for real, re-run typecheck against #2777's 129-error baseline, fix real remainder, and wire lib/db build into pre-typecheck scripts.
- 2026-09-04T01:10:00Z ✅ DONE — Root cause confirmed: `pnpm --filter @workspace/api-server run typecheck` (the exact command #2777's evidence used) bypasses root package.json's own `typecheck:libs` (`tsc --build`) step, so it hits phantom TS2305/TS6305 against a never-built `lib/db` dist. Built `lib/db` for real (`tsc -b .` — clean, no errors) and confirmed the fix by re-running the scoped api-server typecheck from a fully clean state (no dist, no .tsbuildinfo anywhere): went from 2441 errors (mostly phantom TS7006/TS6305 cascade when lib/db, api-zod, integrations-anthropic-ai, integrations-openai-ai-server were all stale) down to a real, stable **76 errors across 13 files**, all TS2741/TS2322/TS2451 in test files (`src/lib/__tests__/*.test.ts`, `src/lib/*.test.ts`) — well below #2777's original 129/55 phantom-inflated baseline. Wired the fix permanently: added a `pretypecheck` script (pnpm's built-in pre-hook) to `artifacts/api-server/package.json` that runs `tsc -b` against api-server's own referenced composite projects (lib/db, lib/api-zod, lib/integrations-anthropic-ai, lib/integrations-openai-ai-server) — verified it fires automatically and produces the correct 76-error result from a cold state via `pnpm --filter @workspace/api-server run typecheck` directly, independent of the root orchestration script. Did not attempt to fix the real 76-error remainder — per this issue's own closing note, that's #2777's per-Feature triage scope; posted the accurate updated count there. Branch merged to origin/main (verify-branch-merged.mjs OK, commit 7fc729ff0), working tree clean.
