# #1498 — Change Control: Standard change catalog — pre-approved templates bound to config packs

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** aa472b9f2 (schema + migration), 4d8633af4 (routes), cee47c5fe (type-cast fix)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Read #1498 body (no comments) + the two settled-decision issues it
  depends on (#1554 whole-runbook standard/non-standard purity, #1555 permanent-until-revoked, no
  review cycle). Read `docs/change-control-contract-pack.md` §5's catalog row and
  `portal-change-control.ts` / `routes/portal-change-control.ts` / `msp-changes.ts` /
  `portal-change-approvals-store.ts` for the existing Change Control model this plugs into — the
  `cr_approvals.approver_role = 'catalog_inherited'` value and its comment ("Populated once the
  change catalog carries an approver; the column exists now so the model is complete") already
  anticipate this table. SCOPE STOP per the build header: schema + manual migration + routes +
  Wire* interfaces only, proven against local Postgres — no UI, `artifacts/portal` stays untouched.
  Plan: `change_catalog_items` table (governed object: draft/approved/revoked, signed+dated
  approver identity, signed+dated+reasoned revocation, no expiry/review-cycle column by design per
  #1555), a nullable `msp_change_requests.catalog_item_id` FK, MSP-side CRUD+approve+revoke routes,
  and a customer-side list+execute route that raises an auto-approved `standard` CR carrying the
  catalog item's real human approver (reusing the existing `materializeApprovalsForChange`
  "approvedBy present at creation" branch — no changes needed there).
- 2026-08-29 ✅ DONE — Built exactly the four deliverables the header scoped, stopped at the wire
  contract (`artifacts/portal` untouched — still a scaffold, `Design/portal/` still only `.gitkeep`).
  **Schema:** `changeCatalogItemsTable` (`lib/db/src/schema/msp.ts`) — `status` `draft|approved|revoked`,
  signed+dated approval (`approved_by_person_id`/`name`/`at`), signed+dated+reasoned revocation
  (`revoked_by_*`/`revoked_reason`), deliberately no expiry/review-cycle column (#1555). Plus
  `msp_change_requests.catalog_item_id` (nullable FK, `ON DELETE SET NULL`).
  **Migration:** `lib/db/migrations/manual/2026-08-29-change-catalog-1498.sql` — run for real against
  local `DATABASE_URL` via `psql` (confirmed live via `\d` + FK constraints), self-marked in
  `simulator_migration_runs`, line appended to #1630.
  **Routes:** `routes/msp-change-catalog.ts` (`/api/msp/change-catalog` — list/author at `MSPOperator`,
  approve/revoke at `MSPAdmin`) and `routes/portal-change-catalog.ts` (`/api/portal/change-catalog` —
  list approved items + `POST /:id/execute`, which raises a real `standard`-class CR carrying the
  catalog item's real approver on `approvedBy`, reusing `materializeApprovalsForChange`'s existing
  "already-approved at creation" branch unmodified so `cr_approvals` records `approver_role =
  'catalog_inherited'` with that same real name). Both registered in `routes/index.ts`.
  **Verified:** full lifecycle (draft → approve → execute → revoke → confirm prior CR untouched)
  proven end-to-end with a real, rolled-back transactional SQL script against local Postgres — zero
  rows persisted. `lib/db` typechecks clean standalone. The worktree's `node_modules` junction
  resolves `@workspace/db` to the MAIN checkout's `lib/db` (stale relative to this branch), so a
  plain `tsc` run reported unrelated noise from that staleness; verified instead with a scoped,
  uncommitted `paths`-override tsconfig pointed at this worktree's real source — zero errors in
  either new route file or the new table, only pre-existing unrelated errors elsewhere.
  **Finding filed:** #1761 (wizard-raised Standard CRs never display Approved — `approved_by` not
  set at creation), parented to #1486, board status set to AI Batter Up. Not fixed here — out of
  this issue's scope; #1498's own catalog `execute` route avoids the same bug by setting `approvedBy`
  explicitly.
  **Merge/cleanliness:** `verify-branch-merged.mjs` exit 0 (merged into `origin/main`); `git status
  --porcelain` clean. Commits: `aa472b9f2` (schema+migration), `4d8633af4` (routes), `cee47c5fe`
  (type-cast fix).
