# Build Journal — #2512

- **Status:** ✅ DONE 2026-09-04T05:58:15Z
- **Scope:** platform

## Log
- 2026-09-04T05:53:19Z — Starting: align Zoho-unavailable error shape across the three
  `portal-customer-requests.ts` routes (list/detail/reply). List already returns
  `200 { configured: false, requests: [] }`; detail and reply both return a bare
  `503 { error: "Ticketing is not available right now." }` for the identical
  `ZohoNotConnectedError` condition. Fixing detail + reply to return a structured
  `configured: false` 200 shape consistent with list, per the issue body's own
  suggested fix.
- 2026-09-04T05:58:15Z — ✅ DONE — Fixed `artifacts/api-server/src/routes/portal-customer-requests.ts`:
  - `GET /api/portal/customer/requests/:ticketId` (detail): on `isZohoUnavailable(err)`
    now returns `200 { configured: false, request: null, thread: [] }` instead of a bare
    `503 { error: ... }`. Success path now also stamps `configured: true` for shape
    parity across the whole route.
  - `POST /api/portal/customer/requests/:ticketId/reply`: on `isZohoUnavailable(err)`
    now returns `200 { configured: false, queued: false, message: "Ticketing is not
    available right now." }` instead of a bare `503`. Success path (202) now also
    stamps `configured: true`.
  - Updated `test-manifests/portal/customer-requests.json`'s HONEST LIMIT note to
    describe the aligned contract and bumped `lastVerifiedAgainstCommit`. The
    manifest's existing `apiTests` only assert 401 auth-gating (unaffected by this
    change — no credentialed Zoho session available in this environment to exercise
    the new response bodies live); ran no new test since none of the shape change is
    reachable without a live Zoho-connected session, consistent with the manifest's
    own documented HONEST LIMIT.
  - Verification: `tsc --noEmit` against `artifacts/api-server/tsconfig.json` passed
    clean (exit 0) — real compiler verification, not skipped. Reviewed diff by hand;
    change is additive-field-only, no route signature or auth changes.
  - Commit: `ac7b38446` ("Fix inconsistent Zoho-unavailable error shape across
    customer requests routes"), pushed to `origin/main`. Branch verified merged via
    `scripts/dev-server/verify-branch-merged.mjs`. Working tree clean at finish.
  - No new findings filed — this session's own work fully resolved the issue as
    scoped; no unrelated gaps discovered.
