# #2894 — Regenerate MSP Staff Roles and Onboarding contract pack (post-#2723 fix)

- **Status:** ✅ DONE 2026-09-04T20:15:01Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** `74d05c250` (regenerated `docs/msp-staff-roles-and-onboarding-contract-pack.md`),
  merged to `main` at `1a8e62e3e`

## Log
- 2026-09-04T20:03:05Z ⏳ IN FLIGHT — full line-by-line re-audit of
  `docs/msp-staff-roles-and-onboarding-contract-pack.md` against
  `msp-settings.ts` + `msp-onboarding.ts` now that #2723 (MFA-enforcement
  persistence + session-revoke persistence) landed in `1f46de6dc`. Pack was
  extracted before that fix and documents both routes as no-ops (§4, §8.1/§8.2)
  — real behavior change since last extraction, per #2894's own body.
- 2026-09-04T20:15:01Z ✅ DONE — regenerated the full pack line-by-line
  (`74d05c250`), not an incremental patch. Confirmed via direct source read
  (not carried-forward line arithmetic): §4's `mfa-enforcement` PATCH now
  writes `users.mfa_enforced`, and the per-user `sessions` DELETE now calls
  the real `revokeAllOtherSessions()` and returns a real `revokedCount` — both
  match their customer-facing analogs in `portal-team.ts`. Every file:line
  citation re-verified against current source: `msp-settings.ts` routes
  shifted +9 lines (from #2723's own insertion), `msp-onboarding.ts` invite
  routes shifted +47 lines (from an unrelated #2674 route addition), schema
  citations shifted from unrelated schema commits since the last extraction.
  `artifacts/msp-console` now exists (scaffolding only — 6 files, no real
  page, no route consumer) — corrected the prior pack's "frontend doesn't
  exist" framing; all 19 routes remain orphaned. Live DB state re-queried via
  direct `psql` against local `DATABASE_URL` — identical to the prior
  extraction (2 MSPs, 9 `mspId`-carrying users, 0 `MSPAdmin`/`MSPOperator`,
  0 invites, 0 staff-customer-scope rows).

  **Real finding filed: #2906** — the prior pack's "Tests confirming real
  coverage" citation (`msp-settings.test.ts`) does not exist and has no git
  history at that path. Real test coverage for this whole surface is two
  link-format assertions in `msp-settings-portal-links.test.ts`; **neither of
  #2723's two now-fixed routes has any test** — a regression could silently
  reintroduce the exact no-op #2723 fixed with nothing in CI to catch it.
  Filed, parented as a sibling sub-issue of #2574 (the Feature this issue's
  own dispatch names), milestone v1.1, labeled `bug`, board status set to
  "AI Batter Up".

  Verification: `node scripts/dev-server/verify-branch-merged.mjs` — OK,
  merged into `origin/main` at `1a8e62e3e`. `git status --porcelain` — clean.
  Read-only pass: no product code, schema, or UI changed.
