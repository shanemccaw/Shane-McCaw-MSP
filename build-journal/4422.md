# #4422 — Bump BuildConsole.csproj TargetFramework net8.0-windows → net10.0-windows, real package-version compatibility pass

- **Status:** ✅ DONE 2026-09-16T18:32:29Z
- **Scope:** buildconsole
- **Started:** 2026-09-16
- **Commit(s):** f784fce28 (bookend IN FLIGHT), f6cc32a5d (real TFM/package bump)

## Log
- 2026-09-16T18:28:16Z ⏳ IN FLIGHT — Prerequisite `dotnet --list-sdks` confirms .NET 10 SDK (10.0.401) present. Read #4422 + parent #4421 bodies, read current csproj, queried nuget.org live for real compatible versions of each PackageReference (see `build-journal/4422-plan.md`). Proceeding with TFM bump + package bumps.
- 2026-09-16T18:32:29Z ✅ DONE — Changes Made: `desktop/BuildConsole/BuildConsole.csproj` `TargetFramework` `net8.0-windows` → `net10.0-windows`. Real per-package nuget.org compatibility check (versions checked live, not guessed): `Microsoft.Web.WebView2` `1.0.4129.50` → `1.0.4191.47` (latest stable, not TFM-gated); `Npgsql` `7.0.7` → `10.0.3` (7.0.7's own comment said it targeted net7.0 directly and its nuspec has no net10 group; 10.0.3's nuspec carries explicit `net8.0`/`net9.0`/`net10.0` dependency groups — genuinely supports net10 natively); `System.ServiceProcess.ServiceController` `8.0.0` → `10.0.12` (latest stable, versioning now tracks the runtime); `AvalonEdit` `6.3.1.120` was already the latest stable version on nuget.org — left unchanged. `System.Diagnostics.PerformanceCounter` `7.0.0` PackageReference was **removed** rather than bumped: under net10.0-windows the SDK flags it `NU1510` ("will not be pruned... automatically available, does not need to be referenced explicitly") — a real new warning the TFM bump itself surfaced. Removed the explicit reference and rebuilt clean; the type is still referenceable with zero build impact. `UseWPF`/`UseWindowsForms` carried over unchanged, confirmed compatible together on net10.0-windows by a real clean build in both configurations. No CS0104 WPF/WinForms bare-name collision was hit (the class of regression seen on the MyArchitect #4409 net10 upgrade) — this file's usings didn't trigger it. Verification Results: `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — Build succeeded, 0 Warning(s), 0 Error(s) (after removing the PerformanceCounter reference; before removal it was 3 NU1510 warnings, 0 errors). `-c Release` — Build succeeded, 0 Warning(s), 0 Error(s). Per the standing "never launch the exe for verification" rule, no live UI click-through was done — code-level `dotnet build` verification only. `git status --porcelain` clean after commit. Branch verified merged into `origin/main` via `node scripts/dev-server/verify-branch-merged.mjs` (OK, `f6cc32a5d` merged) before writing this DONE. No genuine finding/bug surfaced during this build worth filing as a sibling sub-issue of #4421 — the upgrade went cleanly.
