# #2932 — notifications: additional direct db.insert(notificationsTable) call sites beyond #2849's list

- **Status:** ✅ DONE 2026-09-05T01:39:00Z
- **Scope:** platform
- **Started:** 2026-09-05
- **Commit(s):** 72358bcca (rebased/pushed to main as d9c5544d1)

## Log
- 2026-09-05T01:21:30Z ⏳ IN FLIGHT — converting the remaining direct `db.insert(notificationsTable)` sites (workflow-executor.ts x5, admin-insights.ts, admin-invoices.ts, admin-services.ts x3) to `createNotification()`/`createNotificationForAllAdmins()`; evaluating seed-portal.ts:769 for seed-only exemption.
- 2026-09-05T01:39:00Z ✅ DONE — converted all 9 real-runtime sites named in the issue
  (`workflow-executor.ts` create_notification node x2 paths, publish_article draft
  notify, approval_gate x2; `admin-insights.ts` automation report-ready; `admin-invoices.ts`
  new-invoice; `admin-services.ts` x3) to `createNotification()` / `createNotificationForAllAdmins()`.
  `seed-portal.ts:769` confirmed genuinely seed/test-only (fixed historical demo timeline,
  one row pre-marked `read: true`, no real user ever meant to receive live email/webhook
  fan-out from it) — left as a direct insert per the issue's own open question, documented
  inline at the call site. `grep -rn "db.insert(notificationsTable)" artifacts/api-server/src`
  now shows only `notification-center.ts` itself and `seed-portal.ts:769`. `tsc --noEmit`
  passes clean. Commit d9c5544d1 confirmed an ancestor of `origin/main`
  (`node scripts/dev-server/verify-branch-merged.mjs` — OK). `git status --porcelain` clean.
