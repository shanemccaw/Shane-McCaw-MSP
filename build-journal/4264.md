# #4264 — Re-gate GET /admin/ps-scripts to ladder.msp-operator, narrow projection

- **Status:** ✅ DONE 2026-09-15T16:55:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 60272aad6 (merged into main as 6c26bf15a)

## Log
- 2026-09-15T16:41:00Z ⏳ IN FLIGHT — re-gating `GET /admin/ps-scripts` (admin-ps-scripts.ts) from `requireAdmin` to `requireCapability("ladder.msp-operator")`, narrowing the response to `id, title, category` (KanbanCardModal.tsx's picker fields, excluding `script_body`), and fixing `loadScripts()`'s silent-empty failure mode in KanbanCardModal.tsx.
- 2026-09-15T16:55:00Z ✅ DONE — `GET /admin/ps-scripts` re-gated to `requireCapability("ladder.msp-operator")` and its SQL/response narrowed to `id, title, category` only (no `script_body`, `description`, `tags`, etc.). `KanbanCardModal.tsx`'s `loadScripts()` now sets a `loadError` state on a non-ok response (403-specific message vs generic) and on fetch throw, and the picker renders that message instead of silently showing nothing. Verified: `tsc --noEmit` clean for both `artifacts/api-server` and `artifacts/msp-console`; no existing test covered this route (grepped `admin-ps-scripts.test.ts`, no hits) so nothing broke. Branch `agent/4264-q2934` rebased onto `origin/main` and pushed directly to `main` (commit 6c26bf15a); `verify-branch-merged.mjs` confirms ancestor. `git status --porcelain` clean.
