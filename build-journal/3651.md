# #3651 — Migrate BuildConsole's 11 bt_ tables to their own BuildConsole database (BUILD_DATABASE_URL)

- **Status:** ✅ DONE 2026-09-11T14:18:15Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** 0d59e8178 (work), c59e361d0 (IN FLIGHT bookend)

## Log
- 2026-09-11T14:03:31Z ⏳ IN FLIGHT — pg_dump safety snapshot of the 11 bt_ tables, schema+data migration into the local `BuildConsole` database, per-table row-count verification, then switch `BuildQueuePostgresClient.TryResolveConnectionString` to `BUILD_DATABASE_URL` only. Shared-DB bt_ tables are NOT dropped (explicitly deferred).
- 2026-09-11T14:18:15Z ✅ DONE — Safety snapshot: `%USERPROFILE%\BuildConsole-db-snapshots\3651\shared-db-bt-tables-20260911T140740Z.{dump,sql}` (plus the migration tool's own `buildconsole-tables-20260911T141031Z.dump`), outside git. Migrated **13** tables, not 11: the issue's "zero cross-references" audit missed `build_dispatch_log` (FK → bt_build_queue, 1233 rows, the #2103 re-dispatch counter) and `chat_pinned_questions` (FK → bt_chats), both queried on the same BuildQueuePostgresClient connection. Schema came from pg_dump of the live tables (the real current DDL), so no new `lib/db/migrations/manual/` file. Row counts from one exported snapshot, source = target for every table: bt_build_queue 2114, bt_chat_issues 20, bt_chat_mentioned_issues 913, bt_chats 52, bt_dispatch_claims 2, bt_epics 66, bt_issue_mirror 1061, bt_issue_mirror_sync_state 1, bt_issues 765, bt_milestone_mirror 12, bt_test_pad_notes 0, build_dispatch_log 1233, chat_pinned_questions 0; every serial sequence is ahead of its column's max. Code: `TryResolveConnectionString` / `TryCreate` read BUILD_DATABASE_URL= only (no DATABASE_URL fallback, config databaseUrl override dropped from this path); stale Neon doc comments fixed; claim-dispatch.mjs / release-dispatch-claim.mjs / run-claude.ps1 moved to BUILD_DATABASE_URL; Home "Clear stuck item" now cancels direct-Postgres first. New re-runnable tool `scripts/dev-server/migrate-buildconsole-db.mjs` (`--resync` for cutover). Verification: `dotnet build` 0 errors; the built DLL (loaded via reflection, BuildConsole.exe not launched) resolved to database `BuildConsole` and read real data (GetQueueAsync 2114, GetBoardAsync 66 epics / 52 chats, CountDispatchesSinceAsync, GetOpenPinnedQuestionsAsync). The resolver skipped the DATABASE_URL line even though it comes before BUILD_DATABASE_URL in .env.local. A separate negative test (a .env.local with only DATABASE_URL) never ran: pwsh spawns failed with EPERM three times. Claim/release round-trip landed only in the BuildConsole DB (shared: 0). Shared DB left untouched: 13/13 tables present, bt_build_queue still 2114. Findings: #3652 (api-server /admin/build-tracker routes + HTTP fallbacks still on the product DB's stale copy, bug), #3654 (ShaneBuilder still on DATABASE_URL, bug), follow-up #3653 (drop the shared copies; blocked_by #3652 + #3654). None could be parented under #1202, which is at GitHub's 100-sub-issue cap. **Shane To-Do:** close BuildConsole, run `node scripts/dev-server/migrate-buildconsole-db.mjs --resync`, then launch the new build. Pre-existing dirty files: none.
