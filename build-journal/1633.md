# #1633 — Worktree provisioning never copies .env.local

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 1cce9a237 (rebased/pushed to main as 88b1c3672)

## Log
- 2026-08-29 ⏳ IN FLIGHT — copying `.env`, `.env.local`, `.env.*.local` from `config.mainRepoRoot` into new worktrees at provision time, unconditionally (not gated behind `--link`), idempotent, filenames-only logging.
- 2026-08-29 ✅ DONE — added `copyEnvFiles()` to `scripts/dev-server/link-deps.mjs` and wired it into both the fresh-create and reused-worktree paths of `provisionWorktree()` in `scripts/dev-server/provision-worktree.mjs`, unconditional (not gated behind `--link`). Verified live: provisioned a real worktree at `C:\wt\verify-1633-env` via `provision-worktree.mjs --link`, confirmed `.env.local` landed byte-identical to the main checkout's copy, confirmed `node_modules`/`dist` junctions (#1372) unaffected, confirmed a second provision call is idempotent (logs "already present, skipped"), then cleaned it up. No `.env` file exists at the main repo root currently, only `.env.local` — nothing to copy there, correctly skipped without failing. No changes needed in `desktop/BuildConsole/Services/WorktreeProvisionService.cs` — it dispatches to `provision-worktree.mjs` generically, so no `dotnet build` was required. Merged to `origin/main` at `88b1c3672`, verified via `verify-branch-merged.mjs`, working tree clean.
