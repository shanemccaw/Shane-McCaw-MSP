# #2219 — Log Viewer: real mini rail panel (Log Peek)

- **Status:** ✅ DONE 2026-09-02T14:58:00Z
- **Scope:** buildconsole (desktop/ShaneBuilder)
- **Started:** 2026-09-02
- **Commit(s):** `c0b99d04c` (feature)

## Log
- 2026-09-02T14:49:23Z ⏳ IN FLIGHT — Real audit of #2200's landed Log Peek code
  (MainWindow.xaml/.xaml.cs, commit `0f1764069`) against README-ClaudeChat.md §6.4
  (posted verbatim as a comment on #2200 2026-09-02T07:25:59Z). Found the mini panel
  already implements COLD/BURST/LIVE mode row, source chips, search box, multi-select
  checkboxes, and "Send N to chat" footer (fenced ```log via shared `SendLogLinesToChat`).
  Real gap found: `RenderLogPeekLines` colored the whole message text by
  `LogLevelBrushKey(line.Level)` with no real-vs-inferred distinction — the full Log
  Viewer document's outline-vs-filled level pill (`BuildLogLineRow`) never carried into
  the mini view, contradicting §6.4's explicit "don't re-derive it" instruction.
  Fixing: extracted the pill into a shared `BuildLogLevelPill(line, fontSize)` used by
  both surfaces, wired into `RenderLogPeekLines`.
- 2026-09-02T14:58:00Z ✅ DONE — Real, working. Commit `c0b99d04c` on `origin/main`
  (verified via `verify-branch-merged.mjs`: OK). Everything else in §6.4 (COLD/BURST/LIVE
  row, source chips, search box, multi-select checkboxes capped at 100/most-recent-first,
  "Send N to chat" fenced ```log footer, 280px bolt-on flyout with maximize-to-full-doc)
  was already real and correct from #2200's earlier build — this build's real contribution
  is the missing real-vs-inferred level pill parity, extracted into a shared
  `BuildLogLevelPill` used by both the mini panel and the full Log Viewer document so
  neither surface can drift from the other. `dotnet build` clean, 0 warnings/errors, both
  before and after the rebase onto `origin/main`. No fixture/hardcoded data — reads the
  same live `LogService`/`platform_log_stream`/file sources as the full document.
  `git status --porcelain` clean apart from this bookend edit. No new findings to file —
  the only real gap was the one fixed here.
