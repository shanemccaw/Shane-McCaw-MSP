# #1979 — Build Queue re-queries unresolvable issue numbers every refresh

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-30
- **Commit(s):** 3da8ae4288fa2ce7e5fa83a434480ef12d0b2f8c

## Log
- 2026-08-30 ⏳ IN FLIGHT — confirmed #8/#79/#20617 are genuinely unresolvable
  (`gh issue view` returns real GraphQL "Could not resolve to an issue or pull
  request" for all three, not a rate-limit/auth symptom). Implementing a
  negative cache in `BuildQueuePanel`/`GitHubIssuesService` that only caches
  hard on that specific GraphQL not-found string, leaving other failures
  (couldn't start `gh`, other non-zero exit, bad JSON) uncached so a transient
  blip is retried next refresh instead of permanently blanking a real title.
- 2026-08-30 ✅ DONE — `GitHubIssuesService.GetIssueTitleAsync` now returns an
  `IssueTitleLookup { Title, NotFound }`; `NotFound` is set only when stderr
  contains the exact GraphQL "Could not resolve to an issue" text `gh` emits
  for a genuinely nonexistent number. `BuildQueuePanel` adds a new
  `_unresolvableIssueNumbers` HashSet, checked alongside the existing
  `_issueTitleCache`/`_pendingFetches` in `TriggerBackgroundIssueTitleQueries`,
  and populated only when `NotFound` is true in `FetchAndCacheIssueTitleAsync`.
  Any other failure path (couldn't start `gh`, other exit codes, bad JSON) is
  left uncached, so a transient network/auth/rate-limit blip is retried next
  refresh rather than permanently blanking a real title until restart.
  `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Release` — clean,
  0 warnings/errors. Verified #8/#79/#20617 unresolvable via live `gh issue
  view` before writing the fix (real GraphQL not-found for all three).
  Rebased onto origin/main and pushed directly (per repo convention — commit
  to main, no PR branch); `verify-branch-merged.mjs` confirms
  3da8ae4288fa2ce7e5fa83a434480ef12d0b2f8c is an ancestor of origin/main.
  `git status --porcelain` clean. No new findings filed — root cause was
  exactly as described in the issue, no separate bug uncovered.
