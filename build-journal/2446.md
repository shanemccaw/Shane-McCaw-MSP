# #2446 — Customer Home and Timeline: contract pack

- **Status:** ✅ DONE 2026-09-03T02:44:00Z
- **Scope:** platform
- **Parent:** #1655 (Feature: Customer Home and Timeline (Portal)), part of #1485 EPIC: Portal

## Log

- 2026-09-03T02:22:25Z — Session start. Verified both cited endpoints are real and live:
  `GET /api/portal/customer/timeline` (`portal-customer-timeline.ts`) and
  `GET /api/portal/dashboard` (`portal-customer-engines.ts`). No `Design/portal/` export
  and no frontend consumer exist for either yet (both orphaned, backend-only) — honest
  state, not a blocker for this contract-extraction pass. Found one real scoping gap in
  the timeline route's documents/offers sub-queries (single-login `userId` instead of the
  `resolveCustomerUserIds`/`resolveSiblingUserIds` multi-login bridge its own sibling
  `portal/dashboard` route in the same file uses) — writing up as a finding.
- 2026-09-03T02:44:00Z — ✅ DONE — Wrote
  `docs/customer-home-and-timeline-contract-pack.md` (per-endpoint wire contracts cited
  to file:line, real enum unions, honest tri-state, cross-surface edges, orphaned-endpoint
  check). Filed 2 real findings as sibling sub-issues of #1655: #2499 (timeline's
  documents/offers sub-queries scope to one login, not the customer — real customer
  impact, missing events) and #2500 (`results.summary.priorityItems` is a hardcoded `[]`
  with no backing query). Both labeled `bug`, `iteration:1`, `area:msp-portal`, assigned
  to Shane, board status set to "AI Batter Up". Real commit on `main`: `b9df64cbc`
  (contract pack + both bookend updates, after two rebase/retry cycles against a
  moving `origin/main`). Verified `b9df64cbc` is an ancestor of `origin/main` via
  `scripts/dev-server/verify-branch-merged.mjs`. `git status --porcelain` clean.
