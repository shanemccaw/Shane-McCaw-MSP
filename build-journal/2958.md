# #2958 — About Shane page: restore hidden route + design pass

- **Status:** ✅ DONE 2026-09-05T23:30:00Z
- **Scope:** platform (marketing site, `artifacts/shane-mccaw-consulting`)
- **Started:** 2026-09-05
- **Commit(s):** 91c445eb8 (bookend IN FLIGHT), daacdd6a2 (About page rebuild + route restore, rebased to d3b727e0c on push) — landed on `origin/production-base-2026-09-05` at `d3b727e0c`, confirmed via `git merge-base --is-ancestor`

## Log
- 2026-09-05T23:14:51Z ⏳ IN FLIGHT — Read #2958 body + comments. `docs/about-page-content-details.md`
  does not actually exist anywhere on `production-base-2026-09-05`, `main`, or any branch, despite
  the dispatch claiming it was committed — using the real existing `src/pages/About.tsx` copy plus
  `Design/fractional_architecture/About Shane.dc.html` directly as source of truth instead (both are
  real, verified content). Restoring the `/about` route + Header/Footer nav link, and reskinning the
  page to the Fractional Architecture design tokens per the `.dc.html`. Also found: App.tsx registers
  no routes at all for `/contact`, `/assessment`, `/solutions/*` even though the already-merged
  Header/Footer (#2954) link to them — filing as a separate sibling finding under #2953.
- 2026-09-05T23:30:00Z ✅ DONE — Restored `/about` route in `App.tsx` (Header/Footer already linked to
  it, unused since #633). Rebuilt `src/pages/About.tsx` verbatim from
  `Design/fractional_architecture/About Shane.dc.html` (the real, pixel-accurate source — used
  instead of the non-existent `docs/about-page-content-details.md`): hero + violet stat panel
  (30+/NASA/20+/2026), Available for Engagements panel, Who I Help (3 cards), Why IT Leaders Bring
  Me In (6 triggers + side panel), Background + sticky pull-quote with SM avatar, The NASA Advantage
  (4 cards), Career Timeline (3 rows per the design — see open-item note below), Award band,
  Philosophy (4 principles), Core Competencies (20 chips), How You Can Work With Me (6 link cards,
  real routes only — no `/projects` or `/assessments` invented), final CTA with both "Book a
  Consultation" and "Start at $900/mo" buttons. Dropped the old page's separate "How Organizations
  Engage" 6-item grid per the design (keeps only the one grid). `tsc --noEmit` clean; `vite build`
  (PORT/BASE_PATH set manually, no dev server needed) succeeds. No test manifest — this app's page
  content is copy/marketing, no new API/UI *behavior* to cover, and the repo's existing
  `test-manifests/` convention targets exactly that (endpoints, flows, integration actions).
  Filed **#2963** (URGENT, parented under #2953, board status AI Batter Up) for the real
  site-wide finding: Header/Footer already link to `/contact`, `/assessment`, `/solutions/*`, none
  of which are registered in `App.tsx` — all 404 today. Posted a comment on #2958 covering both
  flagged open items (award naming, and the "How You Can Work With Me" link destinations) — both
  turned out to be resolved by reading the design file's own markup rather than needing Shane's
  input; said so plainly rather than silently picking. `.env.local` was already present and
  untracked when this session started — not created or touched by this build, left alone.
