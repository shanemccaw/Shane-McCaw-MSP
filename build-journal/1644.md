# #1644 — Chats panel In Progress badges must honor account toggle

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-29
- **Commit(s):** e047c860e

## Log
- 2026-08-29 ⏳ IN FLIGHT — add `IsChatInProgressForAccount` to FocusModeService (reusing
  `InProgressChatsForAccount`'s cross-reference), switch the Chats panel's three
  `IsChatInProgress` call sites in `LeftSidebar.xaml.cs` to it.
- 2026-08-29 ✅ DONE — added `FocusModeService.IsChatInProgressForAccount(conversationId, account)`,
  implemented purely by delegating to the existing `InProgressChatsForAccount(account)` (no
  duplicated cross-reference logic), preserving its fail-open behavior. Switched all 3 real
  Chats-panel call sites in `LeftSidebar.xaml.cs` (the "other account" badge count, the
  per-group in-progress count, and the per-card highlight/context-menu state) — the "other
  account" badge passes `otherAccount` (it already filters chats to that account before
  counting), the other two pass `BuildConsoleSettings.CurrentAccountLabel()`. Did not touch
  `InProgressChatsForAccount` itself or Focus Mode's own usage of it. `dotnet build
  desktop/BuildConsole/BuildConsole.csproj -c Release` — 0 errors, 46 pre-existing warnings
  (none new). Merged `agent/1644-q819` into `main` (fast-forward, e047c860e); verified
  `e047c860e` is an ancestor of `origin/main`. `git status --porcelain` clean.
