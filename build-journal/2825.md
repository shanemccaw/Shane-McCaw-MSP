# #2825 — Live Document Viewer route for the 7 live-rendered Copilot Readiness reports

- **Status:** ✅ DONE 2026-09-15T04:35:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 16e84efb3

## Log
- 2026-09-15T04:04:09Z ⏳ IN FLIGHT — Pulled #2825's full body + all comments. Confirmed the
  2026-09-15 correction comment is the real, current scope (Design-blocked framing from earlier
  comments is stale — Shane's own correction: the 7 reports are already designed/live via
  `live-document-shares.ts` + `shared-live-documents-public.tsx`, no Design export needed).
  Real scope: (1) `GET /portal/live-documents/:docType` in api-server, authenticated, reusing
  `LIVE_DOCUMENT_SHARE_REPORTS`'s generator functions + `buildPillarSummary()`, scoped to
  `req.user.customerId`; (2) new portal page at `/:slug/copilot-readiness/documents/:docType`,
  registered in App.tsx, reusing `shared-live-documents-public.tsx`'s visual pattern but
  rendering ALL sections in full for the one selected report; (3) `[data-print-ready="true"]`
  marker for `renderLiveDocumentToPdf()` (lives in `html-pdf.ts`, not `insight-pdf.ts` as the
  issue text says — stale file reference, real function confirmed at
  `artifacts/api-server/src/lib/html-pdf.ts:260`); (4) confirm `docPrintToken` exchange
  (`auth-context.tsx`) lands here correctly.
  Confirmed sibling #2866 ("navigates to a dead route", labeled `blocked`) is the issue that
  wires the customer-documents.tsx View/PDF buttons to this route — out of scope here, not
  touching `customer-documents.tsx`.
- 2026-09-15T04:35:00Z ✅ DONE — Commit `16e84efb3`, pushed to `origin/main`
  (`git merge-base --is-ancestor` verified via `verify-branch-merged.mjs`). Shipped:
  (1) `GET /api/portal/live-documents/:docType` in `live-document-shares.ts`, reusing
  `LIVE_DOCUMENT_SHARE_REPORTS` (the exact same generator functions + `buildPillarSummary()`
  the public share route already calls), scoped to `req.user.customerId`, no fixture; (2)
  `artifacts/portal/src/pages/live-document-viewer.tsx` registered at
  `/:slug/copilot-readiness/documents/:docType` in App.tsx (standalone, outside
  ProtectedRoutes/PortalLayout — same reasoning as Accept Agreement, no nav chrome in a
  printed PDF), rendering every section's full HTML for the one selected report and setting
  `[data-print-ready="true"]` once loaded; (3) extracted `PillarTile` out of
  `shared-live-documents-public.tsx` into `components/public-share/PillarTile.tsx` so both
  pages reuse the identical visual pattern instead of duplicating it (no new visual style
  invented).
  Typecheck clean on both `artifacts/api-server` and `artifacts/portal` (`pnpm run
  typecheck`, zero errors).
  Live-verified against REAL customer-scoped data: built api-server with esbuild and ran an
  isolated instance on :8099 against the real local Postgres (bypassing the shared
  dev-server checkout — see the #4230 finding below for why). Minted a real login for a
  synthetic test customer (`test.customer.0915-live-doc-viewer@example.com`, tenant_id=1
  "Jane Jane", the existing seeded test tenant with real completed `msp_diagnostic_runs`),
  deleted after verification along with its refresh tokens/sessions. Confirmed: real 200
  response with real tenant data (7 real pillars/findings, `companyName: "Jane Jane"`), the
  3 real narrative section keys/headings for `copilot_readiness`, a real 404 for an unknown
  docType, and a real 401 with no Authorization header. The narrative HTML itself came back
  `omittedReason: "generation_failed"` for all 3 sections — confirmed via server log this is
  a real outbound-network restriction in this sandboxed shell (`AggregateError
  [EADDRNOTAVAIL]` on the Anthropic fetch), NOT a code defect: the endpoint's graceful
  degradation (200 + per-section omission, matching the public share route's own documented
  behavior) worked exactly as designed. The `[data-print-ready="true"]` marker placement in
  the success-render branch was confirmed by code review (matches exactly what
  `renderLiveDocumentToPdf()` waits for, `html-pdf.ts:278`); could not exercise a real
  browser/headless-Chromium render of it in this session — no playwright/puppeteer is
  installed in the repo, and `shaneapp://runTest` targets the deployed build on the shared
  serving checkout, which was genuinely unreachable this session (see below).
  **Could not live-verify against the shared dev server / `shaneapp://runTest`**: the
  serving checkout (`C:\Source\ShaneMcCawConsulting\Shane-McCaw-MSP`) has 10 real local-only
  commits (a "Git Mode" BuildConsole feature, Phases 3-9, never pushed) that block every
  `request-restart.mjs` fast-forward — genuinely unreachable after a real check (multiple
  `request-restart.mjs` attempts, real NOT LIVE / diverged-checkout evidence in
  `.logs/dev-server/cycles.log`), not something to force from here since it would mean
  resetting a shared checkout carrying someone else's unpushed work. Filed as **#4230**
  (distinct root cause from the already-open #4122, which is scoped to the QA auto-commit
  service only), parented to Epic #1202, board status "AI Batter Up".
  `git status --porcelain` clean. Working tree left with nothing uncommitted.
