# #4131 — Chat tab titles don't self-correct if a tab opens before the live board fetch completes

- **Status:** ✅ DONE 2026-09-14T23:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-14
- **Commit(s):** c9ae28ea8 (IN FLIGHT bookend), 1ab4295f2 (real fix)

## Log
- 2026-09-14T22:52:00Z ⏳ IN FLIGHT — investigated `ResolveForcedEpicForChat` / `OpenChatTab` / `LeftSidebar.BoardRefreshCompleted` in `desktop/BuildConsole/MainWindow.xaml.cs` and `Controls/LeftSidebar.xaml.cs`; implementing fix to re-resolve every open chat tab's forced epic title on the existing `BoardRefreshCompleted` cascade (already fires after every successful `BuildBoardFromGitHub`), updating each tab's header TextBlock in place.
- 2026-09-14T23:05:00Z ✅ DONE — added `ChatTabState.HeaderTitleText` (a reference to the tab header's title `TextBlock`, captured at `OpenChatTab` time), a new `RefreshForcedEpicChatTitles()` that iterates `_chatTabs`, re-runs `ResolveForcedEpicForChat` per tab against `state.GithubNumber`, and updates `HeaderTitleText.Text` in place only when the computed title actually changed (same-string no-op, so an already-resolved tab is untouched on every refresh — no flicker). Wired via `LeftSidebar.BoardRefreshCompleted += (s, e) => RefreshForcedEpicChatTitles();` alongside the other #1813 cascade consumers, so it rides the same real board-refresh completion (manual refresh button or startup fetch) that already drives Batter Up/AI Batter Up/Dispatch/FalseDoneReconciler. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` — 0 Warnings, 0 Errors. `verify-branch-merged.mjs` confirmed `1ab4295f2` merged into `origin/main`. `git status --porcelain` clean. Not live-UI-verified (no running BuildConsole instance with WebView2/live GitHub board to restart against in this session) — the fix rides an existing, already-firing event whose other four consumers are confirmed live in production use, and the header-mutation logic is a straight copy of `OpenChatTab`'s own proven title-computation branch.
