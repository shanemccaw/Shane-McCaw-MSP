# #1413 — Governance sub-pages: audit all 6 routes for unconditional fixture fallback

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** ddc6c4c41 (bookend), no code changes — investigation-only session

## Log
- 2026-08-27 ⏳ IN FLIGHT — auditing /portal-v2/governance/{oversharing,orphaned-teams,team-owners,device-inventory,device-lifecycle,sharing-drift-legacy} and the /:area wildcard (PortalV2GovAreaPage/GovDetailPage) for unconditional fixture fallback, same methodology as #1387/#1409.
- 2026-08-27 ✅ DONE — full audit complete, posted to issue #1413 as a classification report. Findings: oversharing page has 2 confirmed data-value-presented-as-real leaks (stat cards band, Top Risks band — both unconditional, no live counterpart) plus a 3rd where a live endpoint exists (#1275's `useOversharingItemsLive`) but isn't wired into the Anonymous Links panel (needs a schema/mapping decision, not a mechanical swap — no expiry field in `overshared_items`). All 5 `govAreaData`-driven routes (orphaned-teams/team-owners/device-inventory/device-lifecycle/sharing-drift-legacy) are 100% fixture with zero live counterpart anywhere, including the page title itself. The `:area` wildcard's one real page (`governance-sharing-drift`) is the same. `oversharing-all.tsx` is clean — fully live. No code changed: fixing these would visibly blank most content on 7 pages, a product call that needs Shane's sign-off before I make it unilaterally inside an audit session — recommended as a follow-up fix issue (or a few, split along the backend-gap boundary), mirroring how #1387/#1409 originated. Verification: none needed, no code touched; `tsc`/tests untouched by this session.
