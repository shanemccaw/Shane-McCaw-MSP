# Git #1988 — Worktree dependency isolation: worktree `pnpm install` poisons the shared main-checkout node_modules

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Branch:** agent/1988-q1027 (worktree C:\wt\1988-q1027, buildId 1027)
- **Commits:** 2e9a39594 (work), b0cdee2a6 (IN FLIGHT bookend) — both on origin/main, verified via verify-branch-merged.mjs

## Log

- 2026-08-31 — Session start. Pulled #1988 body + all comments + the six duplicate bodies
  (#1951/#1955/#1959/#1964/#1967/#1974). Investigation first: reading
  scripts/dev-server/* lifecycle scripts and WorktreeProvisionService.cs before changing
  anything. Key early facts: pnpm on this machine is 11.13.0; root package.json has a
  `preinstall` guard that #1986's build empirically proved pnpm 11 never runs during
  install; `.pnpmfile.cjs` (metered gate, #1986) already exists on origin/main with its
  `pnpmfileChecksum` committed (#2060) — the worktree fail-closed guard will extend that
  same proven hook.
- 2026-08-31 — Root cause proven with evidence: pnpm 11.13.0's BUILT-IN default
  `verify-deps-before-run: "install"` (read from the installed bundle) auto-installs on
  any `pnpm run`-family command when dep status looks drifted — which a junctioned
  worktree always does — writing through the junctions with worktree-anchored paths
  (#1951's "Recreating …node_modules"). Manual agent installs are the other call site.
  No dev-server script runs `pnpm install`. Offline scratch tests proved `.pnpmfile.cjs`
  `preResolution`+`readPackage` fire on EVERY install variant (fresh / up-to-date /
  headless / modules-mismatch recreate) and a throw aborts pre-write.
- ✅ DONE — commit 2e9a39594 on origin/main: fail-closed worktree install gate in
  `.pnpmfile.cjs` (no override when junctions escape the root; `WORKTREE_ISOLATED_INSTALL=1`
  for junction-free local installs) + lockfile `pnpmfileChecksum` updated byte-exact;
  `verifyDepsBeforeRun: false` in pnpm-workspace.yaml; new
  `scripts/dev-server/store-doctor.mjs` (scan + explicit `--repair`, never automatic)
  wired into provision (`storeHealth`, pure-JSON verified) and post-removal canary
  (`storeAfterRemoval` in cleanups.log); teardown hardened (lstat-based junction
  detection incl. dangling; removal REFUSES to proceed if a junction couldn't be
  unlinked, marks stale instead). Verified live: real `pnpm install` in this worktree
  exits 1 blocked pre-write; real shared store scans clean (19 hosts, 1430 links, 135
  shims); synthetic fixture detection+repair pass; scratch-repo teardown test shows
  dangling junction unlinked and junction-target sentinel surviving removal. Not
  live-tested: the refuse-removal branch (needs a held junction handle). Findings filed:
  #2083 (→#1789 bootstrap-server install guidance drift), #2084 (→#1791 BuildConsole
  should surface storeHealth). No C# changed, so no dotnet build required. Working tree
  clean at close.
