# #3286 — Tesla "Sync now" button in Cars never shows the real result

- **Status:** ✅ DONE 2026-09-08T00:30:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** cb9192b5a (main)

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — confirmed root cause in `web/shanes-life/public/app.js` Sync now onClick: response from `POST /api/tesla/sync` is discarded, button never re-enabled on success path. Implementing real success/failure feedback + finally-based re-enable.
- 2026-09-08T00:30:00Z ✅ DONE — `web/shanes-life/public/app.js` Sync now onClick now captures the real `{ odometer, charging }` response from `POST /api/tesla/sync`, renders a real visible message per half (odometer mileage synced / charging session count synced, or the real `reason`/`message` on failure -- `no_tesla_synced_vehicle`, `no_odometer_data`, or a real `TeslaError` code/message), and re-enables the button in a `finally` block on every path (success, real failure, and thrown error). Old stale result text is cleared before each new attempt. Verified: `node --check` passes; cross-checked the UI branches 1:1 against the real `syncOdometerFromTesla`/`syncChargingSessionsFromTesla` implementations in `web/shanes-life/src/core/vehicles.mjs` (lines 549-624) to confirm every real response shape (`synced`, `reason`, `message`, `currentMileage`, `count`) is handled. **Could not live-test against Shane's actual connected Tesla from this session** -- this app's real Tesla-linked instance runs on Replit (per its own README, "Deploying to Replit" section), which an agent session is barred from reaching (CLAUDE.md hard rule: no SSH/direct access to Replit/Staging). The local Postgres `shanessurvival`/`shanes_life` databases in this dev environment predate the Tesla-linking migration (no `tesla_synced`/`current_mileage` columns on `vehicles` yet) and there is no local Tesla OAuth credential to sync against regardless. Real live verification against the actual Tesla is Shane's own next step on the deployed instance.
