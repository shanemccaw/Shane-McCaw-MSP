# #3684 — MSP-level impersonation writes to the wrong audit table

- **Status:** ✅ DONE 2026-09-11T20:45:00Z
- **Scope:** platform
- **Started:** 2026-09-11
- **Commit(s):** 4c1424108

## Log
- 2026-09-11T20:30:00Z ⏳ IN FLIGHT — fixing `POST /admin/msps/:mspId/impersonate` and
  `POST /admin/impersonate/:userId` (`artifacts/api-server/src/routes/admin-impersonation.ts`)
  to also write into `mspAuditLogsTable` (`msp_audit_logs`), the table AdMspCanvas's own
  Activity panel actually queries (`GET /api/msp/audit`) — per the issue's own recommended
  fix ("in addition to" `auditLogsTable`, not instead of it).
- 2026-09-11T20:45:00Z ✅ DONE — commit `4c1424108`. Both impersonation routes now also
  write to `mspAuditLogsTable` via `writeAuditLog()` (exported from
  `msp-admin-settings.ts` for reuse), in addition to the existing `auditLogsTable` write —
  no regression to any other consumer of `audit_logs`. Confirmed the bug live against the
  local dev DB before fixing (`audit_logs`: 16 `admin_impersonated` + 3
  `admin_impersonated_msp` rows; `msp_audit_logs`: 0 for either action type — matches the
  issue body exactly). No schema change needed; `msp_audit_logs` already had every column
  used. Verified: `node ../../node_modules/typescript/bin/tsc -p tsconfig.json --noEmit`
  from `artifacts/api-server` — clean, zero errors (after rebuilding a stale
  `lib/db/dist` left behind by a concurrent, already-merged #3680 commit — gitignored
  build artifact, not a real bug, not filed). Branch verified merged into `origin/main`
  via `verify-branch-merged.mjs` before writing this. Working tree clean. No test
  manifest — scope is `artifacts/api-server`, outside the msp-portal/marketing-site
  manifest requirement.
