# #1953 — Policy Engine: group_membership compliance evaluator

- **Status:** ✅ DONE 2026-09-01T17:45:00Z
- **Scope:** platform
- **Started:** 2026-09-01
- **Commit(s):** 995c434c2 (main)

## Log
- 2026-09-01T17:30:00Z ⏳ IN FLIGHT — #1952 (OU membership manual override + department-match) confirmed merged to main as Git #2163 (commit 0fba6a0c3), so building the `group_membership` compliance evaluator on top of it, same `graphFetchForTenant` pattern `policy-compliance-graph.ts` already uses. `service_policy` stays `not_evaluable` per the issue's own scoping (needs ps-execution, not a pure Graph read).
- 2026-09-01T17:45:00Z ✅ DONE — added `GroupMembershipTargetState`/`evaluateGroupMembershipCompliance` to `policy-compliance.ts`, added `group_membership` to `EVALUABLE_TARGET_KINDS`. Added `observeOuGroupMemberships` to `policy-compliance-graph.ts`, using Graph's own `POST /users/{id}/checkMemberGroups` per OU member (resolved the same manual-override-then-department-match way `observeOuMailboxSizes` already does) against the policy's declared `groupIds`. Wired both into `evaluateStandingPolicyForCustomer` in `policy-compliance-evaluator.ts`, branching on `policy.targetKind` to build the right observation/finding shape (`checkKey: policy:<id>:group_membership`). `service_policy` untouched, still `not_evaluable`. `pnpm --filter api-server exec tsc --noEmit` shows only pre-existing `@workspace/db` module-resolution errors across the whole package (dangling `node_modules/@workspace/db` in this worktree, unrelated to this change — same class as the standing `pnpm install is not a remedy` note) plus two pre-existing implicit-`any` warnings in code I didn't touch; no new errors introduced by this change. Committed as 995c434c2, rebased onto origin/main, pushed, verified ancestor-of-main via `verify-branch-merged.mjs`, working tree clean.
