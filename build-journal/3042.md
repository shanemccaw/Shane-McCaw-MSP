# #3042 — Compliance Obligations: tenant-authored create route (MSP console)

- **Status:** ✅ DONE 2026-09-07T15:52:00Z
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 363999ac5 (portal-compliance-obligations.ts leak fix), 7e91de73a (new route + tests)

## Log
- 2026-09-07T15:34:00Z ⏳ IN FLIGHT — building POST /api/msp/customers/:customerId/compliance-frameworks
  (+ nested compliance-obligations create) per #3042's decided scope (MSP-console side,
  tenant-authored rows only). Mid-investigation found and fixed a real cross-tenant leak in
  `portal-compliance-obligations.ts` (customer-facing GET had zero mspId/tenantId scoping,
  which this build's new write path would have armed) — see commit for detail.
- 2026-09-07T15:52:00Z ✅ DONE — Shipped `msp-compliance-frameworks.ts`:
  `POST`/`GET /api/msp/customers/:customerId/compliance-frameworks` and nested
  `POST`/`GET .../compliance-frameworks/:frameworkId/compliance-obligations`, `MSPOperator`-gated,
  `resolveOwnedTenant` ownership check (same shape as `msp-security-plan.ts`), mspId/tenantId
  always server-derived from the resolved tenant scope — never client-supplied. An obligation can
  only be authored under a framework the SAME MSP already created for the SAME tenant (global or
  another tenant's framework 404s). Registered in `routes/index.ts`. Also fixed, same build:
  `portal-compliance-obligations.ts`'s customer-facing GET had zero mspId/tenantId predicate at
  all — this build's new write path would have leaked every tenant-authored framework/obligation
  to every customer on the platform the moment one existed; scoped it to
  `isNull(mspId) OR (mspId, tenantId) = caller's own`, matching `portal-policy-decisions.ts:337`'s
  existing pattern. 10 new unit tests (`msp-compliance-frameworks.test.ts`), all passing
  (`npx vitest run src/routes/msp-compliance-frameworks.test.ts`). `pnpm run typecheck` clean in
  `artifacts/api-server`. No migration needed — schema/columns already existed from #1525.
  Branch verified merged into `origin/main` via `verify-branch-merged.mjs`; working tree clean.
  No test manifest — this is `artifacts/api-server` route/schema work, not
  `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting` UI surface (out of that scope per
  CLAUDE.md's Test Coverage section).
