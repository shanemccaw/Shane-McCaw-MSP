# #2557 — Batter Up / AI Batter Up manual refresh: auto-sweep closed issues off the board status

- **Status:** ✅ DONE 2026-09-03T16:34:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** f9befa0e6 (main: db2eb7b63 after rebase)

## Log
- 2026-09-03T16:21:07Z ⏳ IN FLIGHT — implementing the 5-point fix plan from the issue body: new
  closed-issue read path on `GitHubApiClient`, wired into `BatterUpQueueService.RefreshAsync` and
  `AiBatterUpQueueService.RefreshAsync` as a sweep-before-list step.
- 2026-09-03T16:34:00Z ✅ DONE — implemented exactly the 5-point plan:
  1. `GitHubApiClient.ScanProjectItemsForStatusAsync` refactored into a shared
     `(targetOptionId, includeItemId, label, state)` overload; the existing OPEN-only
     public callers (`GetBatterUpIssuesAsync`/`GetAiBatterUpIssuesAsync`) are UNCHANGED
     (delegate to the overload with `"OPEN"` hardcoded, same behavior as before). New
     `GetClosedIssuesInStatusAsync(targetOptionId, label)` + `GetClosedBatterUpIssuesAsync`/
     `GetClosedAiBatterUpIssuesAsync` convenience wrappers filter `state == "CLOSED"`
     instead — shared pagination logic, not a copy-paste, since the parameterized helper
     touched cleanly.
  2. Wired into `BatterUpQueueService.RefreshAsync` (sweeps `BatterUpOptionId`) and
     `AiBatterUpQueueService.RefreshAsync` (sweeps `AiBatterUpOptionId`) — both are what
     `BatterUpPanel.RefreshAsync` / `AiBatterUpPanel.RefreshAsync` already call on every
     manual refresh, so no new trigger was needed.
  3. Each swept item calls the existing `SetIssueStatusByNumberAsync(number, DoneOptionId)`
     and logs individually (success or failure) — `batter-up` channel for Batter Up,
     `ai-batter-up` channel for AI Batter Up — so a sweep is traceable, not silent.
  4. The sweep runs BEFORE the open-only board read in both services, so a just-closed
     item can never flash into the visible row list on the same refresh it's being swept.
  5. Cost: one extra CLOSED-filtered walk per refresh, same bounded pagination as the
     existing OPEN walk; refresh is manual-only (#1890) so this is not a hot path.
  - Verification: `dotnet build -c Debug` — 0 warnings, 0 errors, both before and after
    the rebase onto `origin/main`. `node scripts/dev-server/verify-branch-merged.mjs` —
    OK, `agent/2557-q1395` (db2eb7b6) confirmed merged into `origin/main`. No live
    end-to-end manual-refresh click was run against a real closed-but-still-in-status
    test issue in this session (no BuildConsole instance launch — see CLAUDE.md's
    never-launch-BuildConsole.exe rule); the 14 real stale items Shane found and swept
    manually this session are already at Done, so there was no live stale item left to
    exercise the new sweep against end-to-end. Logic is a direct, symmetric mirror of the
    existing OPEN-only read path plus the already-proven `SetIssueStatusByNumberAsync`
    mutation (used manually 14 times this session per the issue body), reviewed by hand
    against both call sites.
  - `git status --porcelain` clean before finishing.
