# #4277 — Schema drift: 29 live tables have no Drizzle definition

- **Status:** ✅ DONE 2026-09-15T23:20:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 03a9a53a5 (rebased/pushed as 2208ae6b3 on origin/main)

## Log
- 2026-09-15T22:45:00Z ⏳ IN FLIGHT — pulled issue #4277 body + comments, starting live information_schema audit of the 29 tables and the 3 reverse-drift entries
- 2026-09-15T23:20:00Z ✅ DONE — Authored real `pgTable` definitions for all 27 genuinely-missing app tables (the issue's "29" included 2 infra-adjacent counts; confirmed 3 true infra tables `__drizzle_migrations`/`schema_migrations`/`simulator_migration_runs` are intentionally excluded, per the issue itself) in 4 new schema files: `lib/db/src/schema/scope-creep.ts` (7 tables), `sla.ts` (7 tables), `visual-test-tracker.ts` (4 tables), `misc.ts` (9 tables: custom_signals, project_templates, project_template_tasks, quiz_pain_mappings, service_page_trigger_keys, service_required_scripts, service_script_sets, shanebuilder_feature_flags, war_room_interaction_events). Every column type/default/constraint/index/FK was taken from live `information_schema`/`\d` output on the local dev DB, not guessed — one invented enum (`visual_test_tracker_entries.severity`) was caught and removed after checking real row data showed values ('Bug', 'Blocker') with no DB-enforced check constraint, so it stays plain text.
  Reverse-drift resolution (all 3 confirmed, not assumed): `some_table` is doc-comment example code in `retention.ts:151` (inside a `/** ... */` block) — not a real `pgTable` literal, nothing to do. `engagebayConnectionTable` (msp.ts) and `publicChatConversationsTable` (index.ts) are both real, actively-used definitions — genuinely wired into live routes (`artifacts/api-server/src/routes/engagebay.ts`, `public-chat.ts`, `admin-public-chat.ts`) that would fail at runtime with no such live table. Built the missing tables via `lib/db/migrations/manual/4277-reverse-drift-missing-tables.sql`, run against local `DATABASE_URL` myself (`CREATE TABLE`/`CREATE INDEX`, additive, no gate needed) — verified resulting `\d` output matches the Drizzle definitions exactly.
  Verification: live base-table count 410 → 412 (as expected: +2 new real tables). Re-ran `comm -23` (live tables vs `pgTable` literals) — output is now just the 3 intentionally-excluded infra tables. `comm -13` (schema-only vs live) — output is just `some_table`, matching its confirmed comment-only status. `lib/db` vitest suite: 158/158 passing (with `DATABASE_URL` exported — the 4 apparent RBAC-test failures on a first bare run were my own missing env export, not a regression, confirmed by re-running with it exported). `api-server` `tsc --noEmit`: clean, zero errors. No fixture/hardcoded data introduced — every value in the new schema is either a live default read straight off `information_schema` or an inferred column shape.
  Commit `03a9a53a5`, rebased onto `origin/main` and pushed as `2208ae6b3`. `verify-branch-merged.mjs` confirms ancestor. Working tree clean (`git status --porcelain` empty).
