# #2589 — Policy Decisions + Policy Engine: regenerate the MSP Console contract pack (#1642 pattern)

- **Status:** ✅ DONE 2026-09-06T18:10:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** `9d65cf815` (contract pack, on `agent/2589-q1736`) → merged/pushed to `main` as `ff5aee119`

## Log
- 2026-09-06T00:00:00Z ⏳ IN FLIGHT — Read #2589 body + comments, Feature #1685, module epic #1490,
  backend build #2671, sibling customer-pack issue #1722. Confirmed real backend now exists:
  `artifacts/api-server/src/routes/msp-policy-decisions.ts` (Policy Decisions, MSP-side) and
  `msp-standing-policies.ts` + `msp-policy-engine-settings.ts` (Policy Engine, MSP-side) are all
  mounted and live. Auditing both objects against schema + local Postgres, then writing
  `docs/policy-decisions-msp-console-contract-pack.md` (new file — no prior MSP-console pack
  exists for this module yet, only the stale customer-portal one).
- 2026-09-06T18:10:00Z ✅ DONE — Wrote `docs/policy-decisions-msp-console-contract-pack.md`
  (9 real, mounted MSP-side routes across `msp-policy-decisions.ts`, `msp-standing-policies.ts`,
  `msp-policy-engine-settings.ts`; both objects — Policy Decisions and Policy Engine — audited
  line-by-line against the route files, `lib/db/src/schema/msp.ts`, and live local PostgreSQL).
  Two real findings filed and parented as sibling sub-issues of #1685 (this issue's own Feature,
  per the Feature-first rule), both set to AI Batter Up on the board:
  - **#3034** — standing policies have no PATCH/DELETE at all once authored; an authored-active
    policy has no MSP-side off-switch (confirmed by repo-wide grep, zero `update(standingPoliciesTable)`
    calls anywhere).
  - **#3035** — `msp-policy-decisions.ts:53-54`'s own header comment claims a sibling issue was
    filed under #1685 for the "no MSP-side create" product decision; no such issue actually
    exists (the #2671 completion comment says the opposite). Filed as the real place for that
    question to live, and to flag the stale code comment.

  Also flagged in the pack (not filed): the customer-portal pack's own "Policy Engine ... Fully
  unbuilt" claim (2026-08-29) is now stale — #1547–#1553 all shipped real code since (verified all
  seven `state: CLOSED` via `gh issue view`).

  Verified live against local PostgreSQL: `policy_decisions`, `standing_policies`,
  `policy_evaluation_runs` all genuinely 0 rows; `compliance_obligations`/`compliance_frameworks`
  grew (7→8, 5→6) since the customer pack's own read — ISO 27001 A.5.18 was added.

  Branch rebased onto `origin/main` and pushed directly to `main` (`ff5aee119`) —
  `verify-branch-merged.mjs` confirms OK. `git status --porcelain` clean.
