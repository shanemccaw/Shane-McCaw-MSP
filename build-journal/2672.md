# #2672 — MSP console: operator routes for Requests and Support Chat

- **Status:** ✅ DONE 2026-09-03T20:25:00Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** bb9227fb5

## Log
- 2026-09-03T20:05:00Z ⏳ IN FLIGHT — audited the real, landed Portal-side contract
  (#1659 / docs/requests-and-support-chat-contract-pack.md) confirming
  `enqueueEscalationTicket()`/Zoho Desk are the real backing store for both customer
  requests and ShaneBot chat escalations. Building the MSP-operator counterpart routes
  (`GET /msp/support/requests`, `GET /msp/support/requests/:ticketId`,
  `POST /msp/support/requests/:ticketId/reply`) against the same Zoho Desk plumbing,
  scoped to the caller's own MSP org rather than one customer Contact.
- 2026-09-03T20:25:00Z ✅ DONE — shipped `bb9227fb5` (merged into `main` as `289bc2c34`):
  three real MSP-operator routes in new `artifacts/api-server/src/routes/msp-support.ts`
  (`GET /msp/support/requests`, `GET /msp/support/requests/:ticketId`,
  `POST /msp/support/requests/:ticketId/reply`), backed by three new org-scoped functions
  added to `lib/zoho-desk.ts` (`listDeskTicketsForOrg`, `getDeskTicketById`,
  `getDeskTicketThreadForOperator`) — org-wide siblings of the existing per-Contact
  functions `portal-customer-requests.ts` already uses, registered via
  `requireRole("MSPOperator")` + `resolveMspIdStrict`, same pattern as every other
  MSP-scoped route (`msp-message-center.ts`). No fixture data — every read is a live
  Zoho Desk call scoped to the caller's own MSP org.
  **Verification:** `tsc --noEmit` shows zero new errors introduced by this change (the
  remaining errors are pre-existing, environment-wide `lib/db` build/`node_modules`
  resolution gaps unrelated to this diff). Could NOT run the real regression suite
  (`support-chat.test.ts`, `portal-mission-control.test.ts`, `zoho-desk.test.ts`) —
  `vitest` in `artifacts/api-server` is broken repo-wide right now: its pnpm-store entry
  for the pinned `vitest@4.1.9` is genuinely empty in the MAIN checkout (not a
  worktree-isolation symlink issue). Filed as #2733 (parented to #1096, board status "AI
  Batter Up") with full repro evidence rather than silently skipping verification or
  running a prohibited `pnpm install --force`.
  **Branch-merged check:** `node scripts/dev-server/verify-branch-merged.mjs` — OK.
  `git status --porcelain` — clean.
