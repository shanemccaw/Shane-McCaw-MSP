# #3006 — Metered gate hard-blocks only pnpm; extend to other large-download vectors

- **Status:** ✅ DONE 2026-09-12T00:00:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-12
- **Commit(s):** ade12b8b3 (main)

## Log
- 2026-09-12T00:00:00Z ⏳ IN FLIGHT — Read #3006 body + comment (resolved: widen hard block beyond
  pnpm to other large-download vectors named in the issue). Investigated current state:
  `.pnpmfile.cjs` hard-blocks pnpm; `scripts/guard-non-pnpm-install.cjs` (root `preinstall`)
  already unconditionally refuses `npm install`/`yarn install` regardless of metered state
  (stronger than "advisory"); the BuildConsole version-update deploy is already gated via
  `BuildConsoleSettings.CurrentNetworkIsMetered()` in `MainWindow.VersionUpdate.cs` (reads the
  Location setting directly, not `BUILD_NETWORK` env — so it's independent of whether a build
  session has that env var). Remaining genuinely-advisory-only vectors found: (1) `git clone`
  of a secondary repo in `scripts/dev-server/repo-clone.mjs` (`resolveRepoCheckout`), (2)
  `dotnet build`/`dotnet restore`/`dotnet publish` run directly by a build session against any
  desktop `.csproj` (BuildConsole, MyArchitect, SuperShopper, WebTester) outside the deploy
  path — no existing gate reads `BUILD_NETWORK` there. Plan: add a shared
  `scripts/dev-server/network-gate.mjs` module (same throw-and-report pattern as
  `.pnpmfile.cjs`), wire it into `repo-clone.mjs` before the actual `git clone`, and add
  `desktop/Directory.Build.props` with an MSBuild `BeforeTargets="Restore"` target that fails
  when `BUILD_NETWORK=metered` for all desktop projects.
- 2026-09-12T00:00:00Z ✅ DONE — Implemented and verified. `scripts/dev-server/network-gate.mjs`
  added (`isMetered`/`refuseIfMetered`, same message pattern as `.pnpmfile.cjs`, no
  agent-reachable override). Wired into `scripts/dev-server/repo-clone.mjs`: the first-time
  `git clone` of a secondary repo now refuses when `BUILD_NETWORK=metered`; the reuse/`fetch
  --prune` path for an already-cloned repo is deliberately untouched (small, incremental).
  Added `desktop/Directory.Build.targets` (not `.props` — must import after the SDK's own
  Sdk.targets so the real `Restore` target exists to hook `BeforeTargets` onto) with a
  `RefuseNuGetRestoreWhenMetered` target that `<Error>`s when `BUILD_NETWORK=metered`; applies
  to every project under `desktop/` (BuildConsole, MyArchitect, SuperShopper, WebTester) via
  MSBuild's normal upward directory search. Confirmed independent of the pre-existing
  BuildConsole version-update deploy gate (`MainWindow.VersionUpdate.cs` ->
  `BuildConsoleSettings.CurrentNetworkIsMetered()`), which reads the Location setting directly
  from BuildConsole.exe's own process and never has `BUILD_NETWORK` set on itself.
  **Real, live verification (not simulated):** `BUILD_NETWORK=metered dotnet restore
  BuildConsole.csproj` and `dotnet build BuildConsole.csproj` both failed closed — real exit
  code 1, a real `error : BLOCKED: ...` line, zero packages restored — while
  `BUILD_NETWORK=unmetered` and no-env-var both restored/built normally (exit 0, real dll
  output). Confirmed the same gate doesn't false-positive on MyArchitect.csproj or
  SuperShopper.csproj (`dotnet restore`, no env var, exit 0). Found and fixed a real XML
  pitfall along the way: an MSBuild `Directory.Build.targets` comment containing a literal
  double-hyphen (e.g. an option flag name) makes the WHOLE file silently fail to import
  ("due to the file being invalid", no error surfaced) — reworded the comment to avoid it.
  `scripts/dev-server/repo-clone.selftest.mjs` extended with 3 new cases (first-time clone
  refused when metered + never touches disk; reuse-of-already-cloned-repo unaffected) — all
  12 cases (9 pre-existing + 3 new) pass. Direct `curl`/`Invoke-WebRequest` of an arbitrary
  large asset remains a named, documented residual limit (no shared hook every invocation is
  guaranteed to pass through) — same honest-limits class `.pnpmfile.cjs` already states for
  its own bypass flag; not silently claimed solved.
  Commit: `ade12b8b3` on `main` (via `agent/3006-q2433`, confirmed ancestor of `origin/main` by
  `verify-branch-merged.mjs`). `git status --porcelain` clean. No genuine new findings/bugs
  discovered outside this issue's own scope — nothing filed under #3720.
