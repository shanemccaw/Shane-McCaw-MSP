# #1574 — Projects: Zoho integration has never been used — remove or justify

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-30
- **Commit(s):** b0205f905 (bookend IN FLIGHT), 849efb855→18f44d3f9 (real work, post-rebase hash)

## Log
- 2026-08-30 ⏳ IN FLIGHT — Confirmed real DB state: `projects.zoho_project_id` NULL on
  all 6 rows, 0 rows in `msp_job_queue` for any `zoho_*project*` job type ever queued.
  Confirmed `zoho-project-board.tsx` (the only frontend consumer of
  `portal-zoho-projects.ts`) no longer exists — deleted when `artifacts/msp-portal` was
  retired (commit `f40438cdc`). Confirmed no replacement page exists in `artifacts/portal`
  and SCOPE STOP forbids building one. Grepped `artifacts/portal/src`, `artifacts/admin-panel/src`
  for any remaining caller of `/portal/projects/:id/zoho-project*` — none.
  Separately confirmed `zoho-projects-nodes.ts` / `zoho-projects.ts` / `zoho-projects-read.ts`
  ARE genuinely wired into the shared workflow-automation engine
  (`node-type-registry.ts`, `workflow-executor.ts`) the same way Zoho CRM/Desk/Books nodes
  are — that layer is justified and stays. Decision: remove the dead
  `portal-zoho-projects.ts` route (the msp-portal board's own API, orphaned by the
  board's deletion) and its now-unused `projects.zoho_project_id` schema field; keep the
  workflow-node layer as-is.
- 2026-08-30 ✅ DONE — Removed `artifacts/api-server/src/routes/portal-zoho-projects.ts`
  (388 lines) and its mount in `routes/index.ts`; removed `projects.zohoProjectId` from
  the Drizzle schema (`lib/db/src/schema/index.ts`) — now fully unreferenced by code.
  Physical `projects.zoho_project_id` column left in place in the DB (dropping it is
  destructive DDL, out of scope for self-execution; it's harmless and inert). Updated
  `zoho-projects-read.ts`'s header comment to stop describing the deleted board.
  Confirmed `zoho-projects-nodes.ts`/`zoho-projects.ts`/`zoho-projects-read.ts` stay —
  they're genuinely wired into the shared workflow-automation engine
  (`node-type-registry.ts`, `workflow-executor.ts`) the same way Zoho CRM/Desk/Books
  nodes are, so that layer is justified, not dead.
  **Verification:** `tsc --noEmit` on `artifacts/api-server` and `lib/db` — no new
  errors (pre-existing unrelated errors in `standing-policies`/`msp-sops`/
  `admin-build-tracker`/etc. per #1765/#1772/#1774/#1777/#1778/#1781, not introduced
  here). `vitest run` on the four zoho `*.test.ts` files already in
  `vitest.config.ts`'s include list — 64/64 passed. No test-manifest, docs, admin-panel
  or portal reference touched (grepped, none found).
  No new finding filed — this build's own scope fully resolved #1574's "remove or
  justify" question with no unrelated defect uncovered along the way.
  Branch verified merged via `verify-branch-merged.mjs` before writing DONE
  (`18f44d3f9` is an ancestor of `origin/main`). Working tree clean.
