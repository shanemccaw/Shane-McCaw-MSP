# #3941 — Milestone completeness gate ("historical data not yet fully synced") never clears

- **Status:** ✅ DONE 2026-09-13T04:45:30Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 63425d502
- **Migration:** `lib/db/migrations/manual/2026-09-13-bt-issue-mirror-closed-backfill-schema-version-3941.sql` (run against the local `BuildConsole` db; logged on #1630)
- **Findings filed:** none

## Log
- 2026-09-13T04:34:14Z ⏳ IN FLIGHT — Narrowed per Shane's 04:22 comment to symptom #2 only (#1/#3 self-corrected). Live-investigating closed-issue backfill state (bt_issue_mirror_sync_state, ActivityLog issue-mirror) before any fix.
- 2026-09-13T04:45:30Z ✅ DONE — Root cause: the backfill wasn't slow and it wasn't stuck. It had finished (`closed_backfill_complete=t`, last run 2026-09-12 19:24 EDT, "3362 mirrored closed vs 3362 real"), but #3712 added `own_milestone_number` two hours after that (21:19 EDT). The steady-state check only compares closed row counts, so it never walked the closed issues again. Result: 3,427 of 3,460 closed rows still had NULL `own_milestone_number`. For milestone #5 that was 33 mirrored against 1,949 real closed issues (GitHub search confirms 1,949 issues and 0 PRs). Full syncs were succeeding every 5–30 min, and the ActivityLog had no backfill lines after the walk completed. Fix: a `closed_backfill_schema_version` that makes an out-of-date walk re-run once, a per-milestone own-milestone coverage check in the daily reconcile, and the closed upsert now writes `own_milestone_number` as-is instead of COALESCE. Verified: BuildConsole builds clean (0 errors, 0 warnings). The migration ran locally (version reads 0, so the next sync will re-walk). The new gap query run against the live DB returns `milestone #5: 33 vs 1949`. Not yet seen live: the gate actually clearing. That needs the running BuildConsole to pick up this build; the re-walk is about 4 chunks × one per ~5-min incremental tick, so roughly 20 minutes.
