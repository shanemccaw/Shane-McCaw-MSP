# #1975 — Write-app permission derivation: read-only permission attributed to write operations

- **Status:** ⏳ IN FLIGHT 2026-09-04T00:00:00Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** (pending)

## Log
- 2026-09-04T00:00:00Z ⏳ IN FLIGHT — Session opened. Confirmed the real `blocked_by` edge to
  #1899 has cleared: #1899 carries the `complete` label and a real `✅ DONE` bookend
  (`build-journal/1899.md`, commits 74a858995 / 82e5463a4), and that work is already an
  ancestor of this worktree's HEAD. Removing the now-stale dependency edge and proceeding.
  Task: correct `artifacts/api-server/src/lib/graph-write-permissions.ts` where the
  derivation concatenated Microsoft Learn's "least privileged" and "higher privileged"
  permission tiers (which are ALTERNATIVES, not an AND) into a single `permissions` array,
  leaving read-only `Policy.Read.All` attributed to PATCH/POST write operations.
