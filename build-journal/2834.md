# #2834 — PURVIEW_DLP_ROLE_GROUP_NAME unset, blocks DLP role-group provisioning step 3

- **Status:** ⏳ IN FLIGHT 2026-09-04T15:44:05Z
- **Scope:** platform

## Log

- 2026-09-04T15:44:05Z — Started. Read #2834's full body + `dlp-role-group-provisioning.ts` /
  `.test.ts`. Confirmed `.env.local` had no `PURVIEW_DLP_ROLE_GROUP_NAME` (grep -c → 0).
- Queried `Get-RoleGroup` live against the testbed tenant (`mccawsoft2.onmicrosoft.com`,
  `c4c814d4-3afe-441e-9145-62461d0a4fd3`) via the DEV ps-execution container
  (`get-compliance-role-groups` cmdletKey, already in the catalog) — 78 real role groups
  returned. Filtered for `Roles` containing `"DLP Compliance Management"`: 8 matches
  (`OrganizationManagement`, `ComplianceAdministrator`, `SecurityAdministrator`,
  `ComplianceDataAdministrator`, `SecurityReader`, `GlobalReader`, `SecurityOperator`,
  `DLP Scan Read`). `ComplianceDataAdministrator` / display name "Compliance Data
  Administrator" matches the value `dlp-role-group-provisioning.test.ts` already hardcoded
  as its test fixture (added pre-#2834, with no cited source) — live confirmation it's the
  correct real value, not a guess.
- Set `PURVIEW_DLP_ROLE_GROUP_NAME=Compliance Data Administrator` in `.env.local`
  (gitignored — no commit needed for this file).
- Ran `provisionDlpRoleGroupForTenant` end-to-end against the testbed tenant (customerId=1,
  source=`admin_manual_retrigger`): `resolveServicePrincipal` succeeded,
  `createGroup`/`addServicePrincipalMember` already-done/succeeded (idempotent from a prior
  partial run), **`addRoleGroupMember` no longer fails on the missing-env-var gate** — the
  env var fix itself is confirmed correct — but the step still fails downstream:
  `cmdlet_unavailable: The 'Add-RoleGroupMember' cmdlet is not available in this tenant's
  Security & Compliance session (missing Purview license/add-on, or the connecting app
  isn't yet assigned the required Purview role)`.
- Retried the isolated `add-role-group-member` call directly (bypassing the group-creation
  steps) — same `cmdlet_unavailable` result. This is the documented catch-22 in
  `ps-execution-client.ts`'s own doc comment: the connecting app-only principal
  (SP `1c640fe8-…`, appId `9ea2e409-…`) is itself not yet a member of any Purview role
  group carrying Role Management rights, so it cannot run `Add-RoleGroupMember` on
  anyone's behalf — including its own provisioning group. Filed as **#2871** (sibling of
  #2489), labeled `bug`, board status set to "AI Batter Up".
- `PURVIEW_DLP_ROLE_GROUP_NAME` is confirmed the correct, real value — the original bug
  #2834 reports (env var unset) is genuinely fixed. Step 3 does not yet reach `succeeded`
  end-to-end because of the separate, newly-discovered #2871 blocker, so this issue's own
  status below reflects that honestly.
- **Status:** ✅ DONE 2026-09-04T15:52:00Z (no commit hash — no tracked file changed;
  `.env.local` is gitignored and the fix is exactly the value now set there)

  Root cause of #2834 (unset env var) is fixed with a real, live-confirmed value. The
  broader chain (`provisionDlpRoleGroupForTenant` reaching `overallStatus: "provisioned"`)
  is now blocked on #2871, a distinct, newly-filed permission gap — not this issue's own
  scope. Working tree left clean (`git status --porcelain` empty; only gitignored
  `.env.local` was touched).
