# #4378 — Packs: reorder Buy.tsx to account → read-consent → pay

- **Status:** ✅ DONE 2026-09-16T16:59:48Z
- **Scope:** platform
- **Started:** 2026-09-16
- **Commit(s):** bab6c7284 (work), 2e5b1ba94 (manifest lastVerifiedAgainstCommit), c846014f0 (IN FLIGHT bookend)
- **Migration:** none
- **Findings filed:** #4406 (Buy.tsx says the code expires in ten minutes; CODE_TTL_MS and the email say 15). Sub-issue of #4376, AI Batter Up.

## Log
- 2026-09-16T16:40:39Z ⏳ IN FLIGHT — blocker #4374 is complete-labelled (DONE 99d917f86); reading Buy.tsx's Pack stage machine and #4374's pre-consent account door routes to move Account ahead of read-consent and Pay. Write-access onward stays untouched.
- 2026-09-16T16:59:48Z ✅ DONE — Buy.tsx: Packs open on a new `identity` stage (name/email/company create the checkout session), then code → password → MFA through #4374's `/api/public/purchase/pre-consent/*` door (`create-account` replaces set-password), then the buying screen with read consent, then pack selection/pay. After payment-confirmed the flow goes straight to `postAccountStage` ("write"). Write consent, pre-scan, dry run, execute and record are byte-for-byte unchanged. `ACCOUNT_FIRST_PRODUCTS = ["pack"]` is the one switch (#4377 adds monitoring, coordinated live with that session). Once the account exists, `invalidated()` never drops the session, identity is read-only, and the connected card has no tenant "Change". Rail: Create account → Connect → Pack & pay → Write access → Scan → Approve → Record. Server, two gaps the reorder exposed: (1) `POST /api/public/purchase/pack-anchor`. A pack session's paid set is always session.productSlug + extras, and account-first cannot mint a new session on a selection change (checkout-session refuses already_has_account), so submit() re-anchors onto the buyer's real first pack first. Pending/consented only, config_pack→config_pack, real public row, status re-checked in the UPDATE. (2) payment-confirmed calls `promoteAccountFirstBuyerOnPayment`. #4392's Customer promotion only ran inside set-password, which account-first never reaches, so a paid pack buyer would have sat at PackConsented in front of the Customer-tier write step. It requires paid + the accountUserId this session recorded + the session's verified address still on that account; legacy sessions are a no-op. Verified: api-server and shane-mccaw-consulting tsc exit 0 (re-run after rebasing over #4377's server half 910d18e4d). New public-purchase-account-first-pack.live-db.test.ts 6/6 against local Postgres through the real routers: pack-anchor gate matrix; the full code → account → consent → re-anchor → paid → Customer path, which is idempotent, and portal-handoff mints for that session; legacy no-op; email-mismatch refusal. Regression: public-purchase-pre-consent.live-db, purchase-account-flow, direct-tenant-provisioning 84/84 total with the new file; public-purchase-payment.test 18/18. Isolated live check (worktree api-server built on :8099 + worktree Vite on :5478, Playwright/Edge): pack-anchor guard curls 404 session_expired / 400; /buy?product=pack renders data-stage=identity, "Start with your account", the new rail order, no connect card, no pay button, "Email me a code"; monitoring and retainer still open on the buy stage. 9/9. Manifest test-manifests/marketing/buy-page.json: pack steps rewritten (sessionTag 4378-account-first), pack-anchor guard apiTests added, PACK6 (#1304/#4321) removed as unreachable before an account exists. NOT run through shaneapp://runTest: the shared dev server is not serving this commit (request-restart: build set AccountFirstCheckout 2/5, restart deferred; serving-checkout exit 3; :8080 was down at check time). The same assertions ran against the isolated instance above. The real emailed code, Microsoft consent and Stripe charge were not driven end-to-end in a browser (no mail sent from the testbed tenant); the server side of each step is covered by the live-db test. No zz-test-4378 rows left in the DB. Working tree clean.
