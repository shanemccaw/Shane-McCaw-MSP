# #2444 — Offboarding: generate the contract pack from the real backend

- **Status:** ✅ DONE 2026-09-03T03:18:26Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** 1985b54375519e9e230909be34ff86177648354f (rebased/pushed as
  6c857505945fe06a08ccdde1bd7753d0998ee7fb on origin/main)

## Log
- 2026-09-03T03:09:50Z ⏳ IN FLIGHT — verifying the 4 named endpoints (msp/dashboard,
  msp/offboarding/export, msp/offboarding/request, portal/customer/export) are real
  and live, then extracting docs/offboarding-contract-pack.md per the #1642 pattern.
- 2026-09-03T03:18:26Z ✅ DONE — all 4 named endpoints confirmed real/live
  (`msp-portal.ts`, `portal-customer-engines.ts`), plus 2 real siblings
  (`/msp/offboarding/archive`, `/portal/customer/offboard`) needed to honestly
  cover the same state machine/Feature. Wrote
  `docs/offboarding-contract-pack.md` — 6 endpoints fully documented (wire
  contracts, real enum unions, honest-empty states, cross-surface edges,
  orphaned-endpoint check confirming zero live callers in `artifacts/portal`).
  Found and filed a real bug: `/msp/events`'s severity-derivation branch
  (`msp-portal.ts:729`) checks a prefix (`msp.offboarding`) that matches none
  of the 3 real offboarding lifecycle event types actually written
  (`msp.cancellation_requested`/`msp.export_ready`/`msp.archival_flagged`), so
  2 of 3 lifecycle transitions render as low-severity "info" instead of
  "warning" — filed as #2510, sub-issued under this issue's own Feature parent
  #1653, labeled `bug`, board status set to AI Batter Up. Branch verified
  merged into origin/main (`verify-branch-merged.mjs`). `git status
  --porcelain` clean.
