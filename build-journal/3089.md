# #3089 — Conditional Access policy creation/deletion is one opaque whole-array drift event with no policy identity

- **Status:** ⏳ IN FLIGHT 2026-09-09T23:14:58Z
- **Scope:** platform
- **Started:** 2026-09-09
- **Commit(s):** <fill at DONE>

## Log
- 2026-09-09T23:14:58Z ⏳ IN FLIGHT — Re-key `buildCaPolicyDriftConfig` from the positional `{ policies: [...] }` array to an id-keyed `{ policies: { "<policyId>": ... } }` map, so a created policy is an `add` at `/policies/<id>`, a deleted one a `remove`, and a state flip a `replace` at `/policies/<id>/state` — and #2819's per-setting attribution can name the object instead of falling back to a resource-level scope. Confirmed live state before starting: one current `ca-policy` baseline (id 94, tenant c4c814d4…, config `{"policies": []}`, not signed) and **zero** `drift_events` rows in the local DB. The dangerous half is the migration: an old array-shaped baseline diffed against the new map shape reports the entire tenant as drifted. Plan is a real, versioned migration path rather than a silent reshape — a `config_version` column on `drift_baseline_snapshots`, a per-spec baseline shape migration run on read (so Staging/restores are covered before any manual SQL lands), positional `drift_events` setting paths rewritten to their real policy ids, and a manual SQL migration doing the same conversion in bulk.
