# #4169 — TestModeDiagnosticsPanel.xaml raw Typography/Color literal remediation

- **Status:** ⏳ IN FLIGHT 2026-09-15T01:52:34Z
- **Scope:** buildconsole
- **Started:** 2026-09-15

## Log
- 2026-09-15T01:52:34Z ⏳ IN FLIGHT — Pulled #4169 body + its one comment (the BUILD dispatch), and read sibling #4161/#4162 for established conventions. Read `Themes/Typography.xaml` (BodyFontSize=14, SubHeaderFontSize=16, HeaderFontSize=18, MonospaceFontSize=13, CaptionFontSize=11, DefaultFontFamily, MonospaceFontFamily) and `Themes/Colors.xaml`/`Themes/DarkTheme.xaml` for the real brush keys. Grep-confirmed the file's 75 raw FontSize / 34 raw FontFamily / 17 raw-hex-color lines match the issue's audit counts exactly. Mapped every raw `FontSize` by role: `14` → `{StaticResource BodyFontSize}` (rail/API-Helper icon glyphs, already matched Body exactly), everything else (`8.5`/`9`/`9.5`/`10`/`10.5`/`11`) → `{StaticResource CaptionFontSize}` (labels/pills/badges/section headers/buttons/icon glyphs — this file is entirely compact diagnostics-sidebar UI, matching #4161's precedent of mapping icon glyphs and compact text to CaptionFontSize), except the 6 genuinely monospace/code elements (tag name, CSS selector, console command box, console history cmd+result, opacity readout) which got `{StaticResource MonospaceFontSize}` + `{StaticResource MonospaceFontFamily}` instead, matching #4161's "monospace fields → Monospace resources" rule. `FontFamily="Segoe UI"` (2 occurrences) → `{StaticResource DefaultFontFamily}`. Left all 26 `FontFamily="Segoe MDL2 Assets"` icon-glyph literals untouched per #4169's own explicit carve-out (separate, already-tracked finding under #4162's audit). Replaced solid raw hex with exact/semantic resource matches: `#DC8A8A`→`StatusErrorBrush`, `#D4A56C`→`StatusWarningBrush`, `#79B3C8`→`StatusRunningBrush`, `#8FC496`→`StatusSuccessBrush`, `#0A0D12`→`AppBackgroundBrush` (same near-identical-to-AppBackgroundColor mapping #4161 already used), `#10B981`→`StatusSuccessBrush` (semantic "active" status dot, same role as the adjacent `TxtDomA11y` "a11y verified" text which already used `StatusSuccessBrush` two lines below in-file — approximate by role, hue not exact). Left the 6 genuinely un-matched wash-tier 8-digit hex pairs (`#1ADC8A8A`/`#40DC8A8A`, `#1AD4A56C`/`#40D4A56C`, `#1A79B3C8`/`#4079B3C8`, `#1A8B949E`/`#408B949E`×2) untouched — no StatusError/Warning/Running/Neutral **wash** brush exists in `Colors.xaml`/`DarkTheme.xaml` (only `AccentWash*`/`StatusSuccessWash*` do), and #4169 explicitly says to flag a genuinely-unmatched color as its own finding rather than invent one — filing that now as a sibling sub-issue of #1202.
