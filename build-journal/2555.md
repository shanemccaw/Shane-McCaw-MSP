# #2555 — Batter Up tab: search box + sort-by-state, fix detail pane chat vs SQL panel

- **Status:** ✅ DONE 2026-09-03T15:45:22Z
- **Scope:** buildconsole
- **Started:** 2026-09-03
- **Commit(s):** b5b855386 (Batter Up tab: search box, sort-by-state, fix chat vs SQL panel)

## Log
- 2026-09-03T15:40:24Z ⏳ IN FLIGHT — starting: (1) search box filter on RowsList cards, (2) sort-by-state grouping using existing BatterUpRow signals, (3) wire DetailPane.ShowChatInsteadOfActions = true in BatterUpPanel.xaml.cs to match AiBatterUpPanel.xaml.cs.
- 2026-09-03T15:45:22Z ✅ DONE — All three real, scoped fixes landed in `Controls/BatterUpPanel.xaml` + `.xaml.cs`:
  1. **Search box** — new `FilterBoxHost`/`TxtFilter` column in the header Grid (between TxtCount and the Free-flow toggle, same overlay-placeholder pattern as AiBatterUpPanel's #1863 filter box), filtering `_allRows` live by issue number or title substring (case-insensitive) via `RenderFilteredRows`. Hidden when there's nothing to filter.
  2. **Sort-by-state** — new `BtnSortByState` QuietTile toggle next to Free-flow. `StatePriority` groups purely client-side over the already-fetched `Services.BatterUpRow` signals (`HasBuildComment`, `IsBlocked`, `TrackedTerminalStatus`): Ready to launch → Blocked → Failed/Canceled → No BUILD: comment yet, stable-sorted (board order preserved within each group). No new backend query.
  3. **Chat vs SQL panel bug** — `BatterUpPanel()` constructor now sets `DetailPane.ShowChatInsteadOfActions = true`, matching `AiBatterUpPanel.xaml.cs`'s sibling wiring exactly, so the right-hand `IssueDetailView` shows the linked-chat column instead of the near-always-empty SQL Migrations/test-manifest Actions panel.
  - Both TxtFilter and BtnSortByState feed a single shared `RenderFilteredRows()` used by `RefreshAsync`'s first render too, so filtering/sorting apply consistently without a refetch.
  - `dotnet build BuildConsole.csproj -c Debug` — **Build succeeded, 0 Warning(s), 0 Error(s)**.
  - Out of scope for this WPF-app-only build per CLAUDE.md: no test manifest (test-manifests/ coverage is scoped to `artifacts/msp-portal/` and `artifacts/shane-mccaw-consulting/`, not `desktop/BuildConsole`).
  - `node scripts/dev-server/verify-branch-merged.mjs` — OK, `agent/2555-q1394` (b5b85538) merged into `origin/main`. `git status --porcelain` clean.
  - No genuine new findings surfaced during this build — all three items were exactly as scoped in the issue body.
