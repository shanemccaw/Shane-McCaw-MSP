# #3394 — MCP tool: close_issue with enforced NOT_PLANNED comment rule

- **Status:** 🛑 BLOCKED 2026-09-10T00:57:29Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** none (no code changed)

## Log
- 2026-09-10T00:57:29Z 🛑 BLOCKED — Dispatched with `--blocked-by 3390`. Checked
  `gh issue view 3390`: state is still **OPEN** ("MCP server scaffold + bearer auth
  + PAT held server-side only", sub-issue of #3377 Feature: GitHub MCP Server). The
  `close_issue` tool this issue asks for cannot be built without the MCP server
  scaffold #3390 provides — no server, no tool to register `close_issue` on.
  Confirmed the real `blocked_by` dependency edge from #3394 → #3390 already exists
  (`gh api .../issues/3394/dependencies/blocked_by` returns `[3390]`) — no edge
  changes needed. Applied `blocked` label, removed `in-flight`. Posted an issue
  comment stating the blocker plainly. No code changed; stopping here per the
  blocked-work protocol rather than building against a non-existent scaffold.
