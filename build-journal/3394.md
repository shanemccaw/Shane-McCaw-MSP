# #3394 — MCP tool: close_issue with enforced NOT_PLANNED comment rule

- **Status:** ✅ DONE 2026-09-10T17:42:00Z
- **Scope:** platform
- **Started:** 2026-09-10
- **Commit(s):** 2dfaab7d4 (close_issue tool)

## Log
- 2026-09-10T17:31:01Z ⏳ IN FLIGHT — Resuming after unblock. #3390 (MCP server
  scaffold) is now CLOSED/complete. Removed the stale `blocked_by` edge to #3390,
  applied `in-flight` label. Building `close_issue` tool per issue body: real
  `close_issue(number, state_reason)` restricted to `completed` | `not_planned`;
  `not_planned` requires a non-empty `comment` and must post it BEFORE closing.
- 2026-09-10T17:42:00Z ✅ DONE — Added `githubRepo()` to `env.ts`, `closeIssue()`/
  `postIssueComment()` to `github.ts`, and `src/tools/close-issue.ts` registered
  in `tools/index.ts`. `state_reason=not_planned` with no `comment` is rejected
  BEFORE any GitHub call (Git #2167); with a comment, the comment posts first,
  then the issue closes. Typecheck clean (`npm run typecheck`). Existing scaffold
  smoke test still passes (`npm run smoke`). Live end-to-end verification: created
  scratch issue #3534, confirmed `not_planned` w/o comment threw pre-flight with
  no GitHub call, then confirmed `not_planned` with a comment posted the comment
  (issuecomment-5622810524) and closed #3534 with `state_reason=not_planned`.
  Committed as `2dfaab7d4`, rebased onto and pushed to `origin/main`
  (`99da1a1e4`). `verify-branch-merged.mjs` confirms ancestor of main. Working
  tree clean.
