# #1418 — Cap secondary-account builds to Sonnet Medium, overflow heavier builds to "Sonnet+ Overflow" milestone

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-27
- **Commit(s):** 6048d3beb

## Log
- 2026-08-27 ⏳ IN FLIGHT — read #1418, #1416 (landed multi-account routing), #1202 (parent epic). Starting investigation of QueueBuildAsync / QueueWatcherService / account+model+effort fields to implement the Sonnet Medium cap + Sonnet+ Overflow milestone + bulk-resume action.
- 2026-08-27 ✅ DONE — Implemented the full cap: `Services/AccountCapPolicy.cs` centralizes "exceeds Sonnet Medium" (any Opus model, or High/xhigh effort). `QueueWatcherService.LaunchItem` checks this FIRST for `account == "secondary"` builds, before any worktree provisioning, and if it exceeds the cap calls `HoldForSonnetOverflowAsync` instead of launching: marks the DB row `status='held'` (`BuildQueuePostgresClient.MarkHeldForOverflowAsync` — new status value, no CHECK constraint on `bt_build_queue.status` so no migration needed, confirmed via `\d bt_build_queue`), and for issue-linked builds lazily creates/reuses the real GitHub "Sonnet+ Overflow" milestone (`GitHubApiClient.GetOrCreateMilestoneAsync` + `SetIssueMilestoneAsync`, both new) and reassigns the issue to it. `GetNextAsync`'s existing `WHERE status = 'queued'` means a held row is never re-claimed automatically. Builds at/below Sonnet Medium on secondary are completely unaffected (existing #1416 routing untouched). Held items stay visible under BuildQueuePanel's "Active" filter with a distinct "⏫ SONNET+ OVERFLOW" status pill, plus a new bulk "Resume All on Primary" banner (`BtnResumeSonnetOverflow_Click` → `BuildQueuePostgresClient.ResumeHeldOverflowAsync`, flips every held row to `account=NULL`/`status='queued'` in one statement and clears the milestone off each resumed issue) — matches Shane's "so I can kick them off quick" ask. Nice-to-have reminder (#978 reset-time parsing): `MainWindow.CheckSonnetOverflowReminderAsync` fires a one-shot toast the first usage-meter poll after `WeeklyResetTarget` has passed, if any items are still held.
  Verification (real): `dotnet build -c Debug` → 0 errors, same pre-existing warning count (no new warnings introduced). Verified the real hold/list/resume SQL directly against the local PostgreSQL 18 `shanemccawmsp` database in a rolled-back transaction (insert secondary+opus queued row → held → visible via the held-status SELECT → resumed to queued/account=NULL) — all three statements round-tripped correctly, no schema changes needed. No live end-to-end BuildConsole run (WPF app launch) was done in this session; the DB/GitHub API call shapes were verified against the real schema and against the existing, already-proven call patterns elsewhere in this file (CreateIssueAsync/UpdateIssueAsync/CloseMilestoneAsync), not exercised through a live app instance.
