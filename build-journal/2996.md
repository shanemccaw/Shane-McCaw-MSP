# #2996 — Account Security: wire data rights (v2)

- **Status:** ✅ DONE 2026-09-06T18:58:00Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** a30324372fd0be616c7206a5ea3c9940a89d6fce, 860f1a25bb5f0e607bd24499404bbacce22e8d66, 7fdec677 (merge main), pushed to origin/main

## Log
- 2026-09-06T18:40:18Z ⏳ IN FLIGHT — Account Security page does not exist yet in
  `artifacts/portal` (retired portal-v2 build never carried over; sibling issues
  #1602/#1603/#1675 for MFA/sessions/change-password are still open/unbuilt too).
  Building the page shell per `Design/portal/design_handoff_full_site/screens/Account
  Security.dc.html` + `docs/account-security-contract-pack.md`, wiring identity, MFA
  read, sessions read+revoke, last-sign-in (all real "CURRENT" endpoints per the pack,
  needed just to render the page honestly) plus this issue's actual scope: real
  `GET /api/portal/data-export` (file download) and `POST /api/portal/deletion-request`
  (type-to-confirm gate, retention notice shown pre-confirm). Change-password / MFA
  enrollment CTAs left inert per the design's own "Not wired yet" copy — those are
  #1675 / MFA-enrollment sibling issues' scope, not this one's.
- 2026-09-06T18:58:00Z ✅ DONE — Built `artifacts/portal/src/pages/account-security.tsx`
  + `components/account-security/useAccountSecurityLive.ts`, wired the route into
  `App.tsx`, and pointed `UserMenu`'s "Account security" row at the real
  `/account-security` route instead of `comingSoonHref`. Real endpoints wired:
  `GET /api/auth/mfa/enrollments`, `GET /api/auth/sessions` (+ `DELETE .../:id`,
  `POST .../revoke-others`), `GET /api/auth/login-history`, and this issue's own
  scope — `GET /api/portal/data-export` (real file download via blob) and
  `POST /api/portal/deletion-request` (type-to-confirm "DELETE MY ACCOUNT" gate,
  7-year retention notice shown before submit, per the issue body). Change-password
  and MFA self-service enrollment left inert, matching the design's own "Not wired
  yet" copy and #1675/the MFA-enrollment sibling issue's scope.
  Verification: `tsc --noEmit` clean, `vite build` succeeds. Added
  `test-manifests/portal/account-security-data-rights.json` (apiTests for all
  reads/auth-gates; uiSteps limited to the honest unauthenticated RequireAuth
  state — no portal login-gated UI run was possible, same structural gap #2921's
  manifest documents), registered in `_regression-suite.json`.
  `shaneapp://runTest` was attempted for the new manifest and returned "manifest
  not found" — this branch wasn't merged into the dev-server checkout at the time
  of the attempt; documented honestly in the manifest's own notes rather than
  retried in a loop.
  Merge conflict in `artifacts/portal/src/App.tsx` against `origin/main` (a
  concurrent session landed #2991's real login page + auth-core routes in the
  interim) resolved by hand — combined both route sets, both typecheck and build
  re-verified clean after the merge. Pushed directly to `origin/main`
  (`7fdec677`); `verify-branch-merged.mjs` confirms merged. `git status --porcelain`
  clean at completion.
  No genuine new finding surfaced beyond what the contract pack already
  documented (change-password/MFA-enrollment/login-history-surface gaps are
  already tracked on their own sibling issues #1675/#1602-successor/#1603) — no
  new issue filed.
