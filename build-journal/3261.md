# #3261 — MCP tool to let Claude actually see captured photo content (Shane's Life)

- **Status:** ✅ DONE 2026-09-08T19:15:00Z
- **Scope:** platform (web/shanes-life, standalone app — not the MSP pnpm workspace)
- **Started:** 2026-09-08
- **Commit(s):** 9a1977ff9acb2505dce3edd09d1f9f3afdc96520

## Log
- 2026-09-08T00:00:00Z ⏳ IN FLIGHT — read #3261 + #3220, confirmed real gap: list_captures
  returns capture metadata but no image bytes; GET /api/media/:id and core readMedia() already
  exist. Local `finances` DB (from ShanesSurvival settings.json) has no photo captures right now
  (the referenced captureId c5f0c66e-... isn't in the local DB — it's presumably on the live
  Replit deploy), so verification will be a self-created real capture through the app's own
  upload path, not that specific row. Plan: new `get_capture_photo` MCP tool (captureId or
  mediaId in, real base64 image content out), plus a small protocol.mjs change so a tool handler
  can hand back real MCP image content instead of always JSON-text.
- 2026-09-08T19:15:00Z ✅ DONE — commit 9a1977ff. Added `get_capture_photo` (captureId or mediaId
  in) to `web/shanes-life/src/mcp/tools.mjs`, reading real bytes via the existing `media.readMedia`
  core function (same real per-user auth every other tool already trusts, no new auth surface).
  `web/shanes-life/src/mcp/protocol.mjs`'s `tools/call` dispatch now recognises a handler result
  shaped `{ __mcpContent: [...], ...metadata }` and returns `__mcpContent` verbatim as the real MCP
  content array (an `image` block) instead of always JSON-stringifying to text — every other tool
  is unaffected (confirmed live, see below). Self-enforced 5MB inline-size limit
  (`MAX_INLINE_IMAGE_BYTES`) since this app deliberately has no image-resize dependency (single
  runtime dependency `pg`, per the app's own README); over that limit the tool returns a real,
  honest error naming the actual byte size rather than truncating or faking a resize. Filed the gap
  as #3262 (parented under #3220, the real Feature-tier parent; board status set to AI Batter Up).
  **Verification (real, live, against the local `finances` Postgres + a real running local server
  on :5057, not simulated):** the specific captureId named in #3261's body
  (c5f0c66e-4a95-4cc9-91b4-450fc8c6ab5b) is not present in the local dev database — it's presumably
  on the live Replit deploy, out of reach here — so I created a disposable real test account
  (`sl-test-3261@shanes.life`), a real 1x1 PNG media row + photo capture through the app's own real
  `storeMedia`/`createCapture` core functions, minted a real MCP token via
  `bin/issue-mcp-token.mjs`, and called the real `/mcp` JSON-RPC HTTP endpoint end-to-end:
  - `get_capture_photo` by `captureId` → real `content[0]` = `{type:"image", mimeType:"image/png",
    data:<real base64>}`, `isError:false`, `structuredContent` carries real mediaId/byteSize.
  - Same by `mediaId` directly → same real result.
  - No args → real `isError:true`, "Provide either captureId or mediaId".
  - Unknown captureId → real `isError:true`, "Capture not found".
  - A real seeded 6MB JPEG capture → real `isError:true`, "Photo is 6.0MB, over the 5MB limit...".
  - `list_captures` (an unrelated, pre-existing text-returning tool) still returns plain
    `content[0].type === "text"`, `isError:false` — confirms the protocol.mjs change doesn't
    disturb any of the other ~50 existing tools.
  Cleaned up: deleted the disposable test user/media/captures/token rows, removed the scratch
  `.env` and scratch seed/cleanup scripts, killed the local dev server process. `git status
  --porcelain` clean before this commit; HEAD verified an ancestor of `origin/main` (pushed
  directly, ff, no branch divergence). Comment posted on #3261, labels flipped
  in-flight→complete.
