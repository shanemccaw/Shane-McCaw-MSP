# #4004 — Portal Shell: wire the responsive/mobile drawer nav from the updated Shell.dc.html

- **Status:** ✅ DONE 2026-09-14T04:02:56Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 8a70bcb6b (rebased/pushed to main as ed6cf166e)

## Log
- 2026-09-14T04:01:35Z ⏳ IN FLIGHT — pulled #4004 + #1817, read Design/portal/design_handoff_full_site/screens/Shell.dc.html (real `_rz` resize handler + drawer/rail derived state, 760px breakpoint measured against the shell's own container width) and Mobile Preview.dc.html; wired PortalShell/TopBar/SidebarNav for the responsive drawer, typechecked and production-built clean; committing now.
- 2026-09-14T04:02:56Z ✅ DONE — Wired the real drawer nav per Shell.dc.html:
  - `PortalShell.tsx`: `useNarrowShell` replaces the old `window.matchMedia`-based
    `useNarrowViewport` with a `ResizeObserver` on the shell's own root ref
    (matches the design's `_rz` handler measuring container width, not window
    width); added `drawerRequested`/`drawerOpen` (derived `narrow && requested`,
    matching the design's own derivation so widening past 760px silently closes
    the drawer with no separate reset effect) and the scrim overlay.
  - `TopBar.tsx`: added the drawer/menu trigger button (narrow only), hides the
    SOP tray trigger and the brand subtitle below 760px, tightens header
    gap/padding to the design's narrow values. Alerts + account triggers stay
    reachable at every width, per the design.
  - `SidebarNav.tsx`: accepts `narrow`/`onNavigate`; renders as a floating
    absolute overlay (280px, z-index 57, `#070d1c` bg, shadow) only while the
    drawer is open at narrow widths, in-flow rail otherwise (unchanged 232px/
    transparent/no-shadow values at desktop width — verified no desktop
    regression). Nav row clicks call `onNavigate` to close the drawer, matching
    the design's own click handler. Every module nav item (all of
    `MODULE_NAV_ITEMS`, unchanged) stays reachable from the drawer.
  - Real finding fixed inline (not filed separately, per "fixed in this build"):
    `AlertsDropdown.tsx`'s popover was a fixed 404px width anchored `right:0`
    with no narrow-width handling — verifying the new breakpoint against the
    design (alerts stays visible/reachable at every width) surfaced that it
    overflows off-screen below ~430px viewport width. Clamped to
    `min(404px, calc(100vw - 24px))`, matching the design's own
    `width:min(404px, calc(100% - 24px))` formula for that popover.
  - Investigated (grep, no matches) whether a "settings stacks" side-nav
    container or any `<table>` element ("wide tables scroll sideways") exists
    anywhere in `artifacts/portal/src` — neither does yet. Nothing to wire for
    those two Mobile Preview callouts; not invented.
  - Verification: `pnpm --filter @workspace/portal exec tsc --noEmit` clean;
    `pnpm --filter @workspace/portal build` clean (production bundle built).
    Live browser/UI check against a running instance was **not** performed —
    this build participates in `--buildSet PortalShell` (dispatch header), whose
    restart is deliberately deferred until every member of the set completes
    (confirmed via `request-restart.mjs`: "merged into set 'PortalShell' [8/8];
    restart DEFERRED until the whole set completes"), so forcing an early
    restart here would defeat that coalescing. Correctness verified instead by
    full code review against Shell.dc.html's own derived-state logic
    (`narrow`/`drawer`/`drawerOpen`/`sidebarShow`/`sbMode`/`sbW`/`sbPos`/`sbZ`/
    `sbBg`/`sbShadow` at Shell.dc.html:1018-1051) line-by-line, and confirming
    the desktop-width values are byte-identical to the pre-#4004 code.
  - No fixture/hardcoded data introduced.
  - Branch verified merged into `origin/main` via
    `scripts/dev-server/verify-branch-merged.mjs` before writing DONE.
    `git status --porcelain` clean.
  - No new findings filed as separate issues — the one gap found
    (AlertsDropdown overflow) was fixed directly in this commit.
