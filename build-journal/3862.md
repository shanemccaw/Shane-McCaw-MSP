# #3862 — GitDoctorView run-log real success/failure coloring

- **Status:** ✅ DONE 2026-09-13T00:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** f08d7d2fcbd12bc8def356317982a7db283f8ad6

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Fix hardcoded GreenBrush on GitDoctorView run-log lines; use real StatusSuccessBrush/StatusErrorBrush based on the command's actual exit result.
- 2026-09-13T00:10:00Z ✅ DONE — `_gdLog` tuple gained an `IsSuccess` bool threaded from `result.Success` in `RunGitDoctorStepsAsync`; `RenderGitDoctorLog` now colors each non-head line `StatusSuccessBrush` when `IsSuccess` else `StatusErrorBrush` (the same resources #2126/#2127 established), replacing the previous unconditional `GreenBrush`. Head/"Finished" lines default success=true (informational, not a command outcome). Verified via `grep` that no `GreenBrush` reference remains in the run-log render path. Branch verified merged into origin/main (`verify-branch-merged.mjs` OK). Working tree clean.
