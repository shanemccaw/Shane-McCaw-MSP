# #1439 — Security (top-level pillar page + Security Plan): strict pass — eliminate all remaining fake data

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** dc1e9c1cc (merged to main; rebased onto origin/main twice — df03ce645 → 8b70119c1 → dc1e9c1cc as the final pushed hash)

## Log
- 2026-08-27 ⏳ IN FLIGHT — Starting full strict pass on portal-v2-security.tsx (top-level
  pillar page) and portal-v2-security-plan.tsx per Shane's live-testing report ("Security
  still fake data" / "Security Plan fake data"). Also re-verifying
  portal-v2-security-mfa.tsx / -ca.tsx / -evidence.tsx for regressions from #1414/#1429/#1430/#1431.
- 2026-08-28 ✅ DONE — Found and fixed real, live fixture-fallback leaks (not just missed
  tags):
  - **Security Plan** (`securityPlanLive.ts`/`securityPlanWire.ts`): the page rendered the
    FULL design fixture ("Halden Materials", ~51% compliant) for every real customer except
    the one seeded testbed tenant, since the seed migration only seeds `customer_id = 1`.
    This is exactly Shane's "Security Plan fake data" report. Replaced with a real 4-state
    discriminated union (`loading` / `live` / `no-plan` / `error`) — `no-plan` and `error`
    each render an honest empty state; `toSecurityPlan` no longer fills blank header/owner
    fields with the fixture's own values either.
  - **Security pillar page** (`portal-v2-security.tsx`): Security Categories cards now show
    a real live score or an honest dash — never `SEC_AREA_LINKS`'s fixture number — once
    both `useSecAreaLinksLive` and `useCaBaselineLive` have loaded (previously fell back to
    the fixture number on a null/unresolved value). "Secure Score" hero tile is now wired to
    the real `security.secureScore` dashboard-resolvers metric (was permanently "Not measured
    yet" despite a real, reachable backend). `PillarScanBar`'s scanNumber/fixedSinceScan1/
    nextScan had no backing query anywhere — tagged `NO-BACKEND-TO-WIRE` and switched to an
    honest dash instead of the fixture's "14"/"4"/"22 hours" shown unconditionally to every
    tenant.
  - **CA drill-down**: band/stat panel now shows a loading skeleton or honest "No scan data
    available" instead of the design's own fixture statuses when `useCaBaselineLive` hasn't
    collected real rows.
  - **MFA drill-down**: "gaps"/"partial" state bodies (headline + rows + button copy) now
    render one honest empty state instead of fixture named rows under a headline still
    quoting the fixture's counts as fact — the real, current case for the testbed tenant
    (`identity:mfa-registration` returns `license_gap`). "MFA controls we check" panel tagged
    `NO-BACKEND-TO-WIRE`.
  - **Evidence pages** (`secEvidenceData.ts`): tagged the already-honest-in-the-UI but
    previously untagged backend gaps (OAuth's 3 unbacked stat cards, Email's 5 unbacked stat
    cards, all three pages' topRisks/secondaryRows) with the literal `NO-BACKEND-TO-WIRE`
    convention. NOT redesigned into honest per-card empty states in this pass (flagged for a
    follow-up issue — see the GitHub comment).
  - Test manifests updated to match: `portal-v2-security-plan.json`, `security-drilldowns.json`
    (MFA2/4/4b/10/11/12 rewritten against the real license-gapped testbed tenant),
    `portal-v2-pillars.json`.
  - Logging: client-side gaps use `reportClientEvent` on the existing `engine.dashboard`
    channel; the api-server route already used `logger.child({channel:"tenant.portal"})`.
  - **Verification, honestly**: unit tests (`tsx --test`) pass 36/37 across
    secDashboardData/secCaModel/secMfaModel/securityPlanWire/securityPlanModel — the 1
    failure is an unrelated `lucide-react` module-resolution error traced to a genuinely
    broken cross-worktree `node_modules` symlink in this shared pnpm store (confirmed via
    `stat`: the relative `..` count in `node_modules/@types/node`'s symlink overshoots the
    drive root once hoisted at this shallower depth), not a code defect. A full
    `tsc --noEmit` could not be completed this session for the same reason (`vite/client`
    and `node` ambient types unresolvable via the same broken symlink chain, reproduced
    across multiple retries with `--preserveSymlinks` and explicit `--typeRoots`) —
    `shaneapp://runTest`/`shaneapp://executeSql` were also unavailable this session (the
    `shane-msp` MCP server failed to connect, `CONNECTION_CLOSED`). Manual review,
    brace/paren-balance checks and import-usage checks on every touched file found no
    issues. Verified merged: `node scripts/dev-server/verify-branch-merged.mjs` confirms
    `dc1e9c1c` is an ancestor of `origin/main`.
  - Pushed to `main` at `dc1e9c1cc` (two rebases along the way to resolve concurrent pushes,
    including one real content conflict against a concurrent Adoption-pillar session's
    `lastVerifiedAgainstCommit` edit to the same shared `portal-v2-pillars.json`, resolved
    keeping both sessions' actual content changes).
