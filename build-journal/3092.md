# Build Journal — Issue #3092

- **Status:** ✅ DONE 2026-09-12T00:10:00Z
- **Scope:** platform

## Log
- 2026-09-12T00:00:00Z — Started. `dev-all.mjs`'s per-service log append (`appendFileSync(svcLog, text)`) had no size cap/rotation, unlike the merged `dev-all.log` which rotates at `MAX_LOG_BYTES`. Adding matching rotation for per-service logs.
- 2026-09-12T00:10:00Z — ✅ DONE — Added a `svcLogBytes` Map tracking bytes written per service in `writeLog()`; when a service's running total exceeds `MAX_LOG_BYTES`, it rotates `<service>.log` to `<service>.prev.log` via the existing `rotateFile()` helper and resets the counter, matching the merged log's own rotation behavior exactly. Verified with `node --check`. Commit d3de88360, rebased onto and pushed to `origin/main`. `verify-branch-merged.mjs` confirms merged. Working tree clean.
