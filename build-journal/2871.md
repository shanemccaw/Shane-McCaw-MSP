# #2871 — DLP role-group provisioning: re-run after manual SP→OrganizationManagement grant

- **Status:** ✅ DONE 2026-09-07T11:35:00-04:00
- **Scope:** platform
- **Started:** 2026-09-07
- **Commit(s):** 4cdd5516098412af45a76613d74c88135a43a672

## Log
- 2026-09-07T15:10:14Z ⏳ IN FLIGHT — re-run provisionDlpRoleGroupForTenant(tenantId, 1, "admin_manual_retrigger") against testbed tenant now that Shane has manually added SP 1c640fe8-1c23-4510-8235-9ee6938d8f8b to OrganizationManagement.
- First live re-run (worktree-local `.env.local` was actually stale — predated #2834's
  `PURVIEW_DLP_ROLE_GROUP_NAME` fix; re-set it) still failed, but with a NEW error:
  generic `auth_failed: Could not establish a Security & Compliance session for the
  target tenant.` — even for a plain read-only `Get-RoleGroup` call, i.e. Connect-IPPSSession
  itself was failing, not just the role-group cmdlet. That ruled out a simple
  "not enough propagation time yet" read.
- Pulled real container logs (`az containerapp logs show -n ca-ps-execution-dev -g
  rg-smccaw-2184`, read-only, DEV container — no SSH, no private key) since the
  api-server's generic `auth_failed` classification swallows the real PowerShell
  error text. Real cause: `"Organization cannot be a Guid, please enter the name of
  the tenant instead."` — `provisionDlpRoleGroupForTenant` passes the raw M365 tenant
  GUID as `-Organization`, but Connect-IPPSSession requires the tenant's real domain.
  This is a genuine, separate code bug (unrelated to Shane's manual role grant) that
  was masking whether the grant had actually taken effect.
- Fixed: step 3 now resolves the tenant's real domain via `getInitialDomainForTenant()`
  (`graph.ts`'s existing #238 helper, built for this exact constraint) before calling
  `add-role-group-member`, falling back to the raw tenantId only if that Graph lookup
  itself fails. Re-ran — Connect-IPPSSession now succeeds (confirmed in container logs),
  but hit a second real issue: `Add-RoleGroupMember`'s `-Identity` couldn't resolve
  `PURVIEW_DLP_ROLE_GROUP_NAME`'s value "Compliance Data Administrator" (the role
  group's **DisplayName**) on `FfoRecipientSession` — `ManagementObjectNotFoundException`.
  Live `Get-RoleGroup` confirmed the real internal `Name` is `ComplianceDataAdministrator`
  (no spaces); corrected the local (gitignored) env var to that value.
- **Final live re-run: `overallStatus: "provisioned"`.** `addRoleGroupMember` step
  `status: "succeeded"`. Re-ran a second time to confirm idempotency —
  `addRoleGroupMember` still `succeeded`, all other steps `already_done`. **Shane's
  manual OrganizationManagement grant is confirmed working through the automated
  app-only path** — the only reason it didn't show up as fixed on the first re-run was
  the pre-existing GUID-vs-domain bug above, now fixed in code, not a propagation delay.
- Added regression coverage to `dlp-role-group-provisioning.test.ts`: mocked
  `getInitialDomainForTenant` and asserted the resolved domain (not the raw tenant GUID)
  is what's actually passed to `add-role-group-member`. `vitest run
  src/lib/dlp-role-group-provisioning.test.ts` → 5/5 passed. `tsc -p tsconfig.json
  --noEmit` → clean, no errors.
- No new GitHub issue filed — the bug found was the direct, in-scope blocker to this
  issue's own task and was fixed in the same session, not a separate/tangential finding.
- ✅ DONE — pushed `4cdd5516098412af45a76613d74c88135a43a672` to `origin/main` (rebased
  twice past concurrent pushes). `verify-branch-merged.mjs` confirms ancestor of main.
  `git status --porcelain` clean (all scratch investigation scripts under
  `src/scripts/*-2871.mjs` deleted before commit; only the `.env.local`
  `PURVIEW_DLP_ROLE_GROUP_NAME` value change is local/gitignored, no commit needed for it).
