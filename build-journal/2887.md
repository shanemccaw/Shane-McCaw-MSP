# #2887 — Feature: MCP Server (ShanesSurvival)

- **Status:** ✅ DONE 2026-09-04T19:45:00Z
- **Scope:** platform (desktop/ShanesSurvival — Shane's personal project, not MSP product)
- **Started:** 2026-09-04
- **Commit(s):** 90375434b

## Log
- 2026-09-04T19:05:00Z ⏳ IN FLIGHT — building the real local MCP server (stdio) for
  ShanesSurvival: extracting Settings/Dashboard logic into a shared `ShanesSurvival.Core`
  class library so the MCP tools reuse the exact real GATE shortfall/bleed queries already
  built in #2885, then wiring `ShanesSurvival.Mcp` (official `ModelContextProtocol` NuGet SDK,
  stdio transport) with real read-only tools against the same local Postgres DB.
- 2026-09-04T19:45:00Z ✅ DONE — commit `90375434b` on `main`. Extracted Settings/Dashboard/
  Account logic into a new `ShanesSurvival.Core` (net8.0) class library; added
  `TransactionRepository` there for bounded recent-transaction reads. Added
  `ShanesSurvival.Mcp` (official `ModelContextProtocol` .NET SDK, stdio transport) exposing 4
  real read-only tools — `gate_status`, `bill_status`, `spend_bleed`, `recent_transactions` —
  all reading the real local Postgres DB via the same `%AppData%\ShanesSurvival\settings.json`
  connection string the WPF app uses. `dotnet build ShanesSurvival.sln` clean (3 projects).
  Live-verified for real: drove the built `.exe`'s actual stdin/stdout pipes through a real
  `initialize` → `tools/list` → `tools/call` MCP session — all 4 tools registered with correct
  schemas and executed against the real local DB, returning honest "no role assigned yet"
  results (the real current state of that DB). Caught and fixed a real bug during that
  verification: `InvariantGlobalization` in the Mcp csproj made
  `CultureInfo.GetCultureInfo("en-US")` throw at runtime, surfaced as a generic MCP tool error;
  removed, re-verified clean. README updated with the tool list and real Claude Desktop
  `claude_desktop_config.json` setup instructions. No fixture data anywhere — every tool reads
  live from Postgres. `verify-branch-merged.mjs` confirmed merged into `origin/main`;
  `git status --porcelain` clean at finish.
