# Build Journal — #4314

- **Status:** ✅ DONE 2026-09-16T01:10:00Z
- **Scope:** platform

## Log
- 2026-09-16T00:57:28Z — Starting: add 5 missing FK-column indexes (wf_runs.retriggered_from_run_id, wf_run_node_logs.run_id, wf_run_node_outputs.run_id, wf_trigger_events.run_id, wf_trigger_events.trigger_id) per issue recommendation. Additive migration, run against local DATABASE_URL, verify via EXPLAIN.
- 2026-09-16T01:10:00Z — ✅ DONE — Added Drizzle `index()` defs in `lib/db/src/schema/index.ts` for all 5 FK columns; wrote `lib/db/migrations/manual/4314-wf-runs-fk-indexes.sql` and ran it myself against local `DATABASE_URL` (5x `CREATE INDEX`, all succeeded). Verified with `EXPLAIN` + `SET enable_seqscan = off` that all 5 queries (on `run_id`/`trigger_id`/`retriggered_from_run_id`) now resolve via their new index scans instead of a sequential scan. `tsc --noEmit -p lib/db/tsconfig.json` clean. Commit `933d39133` (merged to main as `df92a45cd`). No findings filed — the issue's own recommendation was exact and complete; the duplicate FK constraint on `wf_runs.retriggered_from_run_id` (`wf_runs_retriggered_from_run_id_fkey` + `wf_runs_retriggered_from_run_id_wf_runs_id_fk`) noticed in passing is cosmetic drizzle-generated redundancy, not a real bug worth a separate issue.
