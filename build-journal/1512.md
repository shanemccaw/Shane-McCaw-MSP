# #1512 — Risk Register: signed RBD document render and signature capture

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 647dede36

## Log
- 2026-08-31 ⏳ IN FLIGHT — building the render + signature-capture layer on top of
  #1508's `msp_rbd_versions` (versioning/supersession, already DONE at
  `c1eaf649c8c37363b561f04444ece46439869f7b`). Per the issue's settled architecture,
  repointing existing machinery rather than building a second stack:
  `msp_report_definitions`/`msp_report_runs` (htmlContent/pdfBase64) for the render,
  and the SOW flow's signature shape (signerName, signatureData, signedAt, signedIp,
  shareToken) for capture — `msp_rbd_versions` already carries signerName≈signedBy.name,
  signedAt, signedIp≈signedBy.ipAddress from #1508; adding the two genuinely missing
  pieces (signatureData, shareToken/expiry). SCOPE STOP honored — no `artifacts/portal`
  UI, wire contract only.
- 2026-08-31 ✅ DONE — shipped at `647dede36` (rebased onto and pushed directly to
  `origin/main`, ancestry confirmed via `verify-branch-merged.mjs`):
  - `lib/db/src/schema/msp.ts` — `msp_rbd_versions.signatureData`/`shareToken`/
    `shareTokenExpiresAt` (real drawn signature + unauthenticated review/sign link,
    the two fields genuinely missing versus `msp_sows`' signature capture);
    `msp_report_runs.rbdVersionUid` (links a rendered "risk_decision_document" run
    back to the version it rendered, no FK by design); `REPORT_DOC_TYPES` gains
    `risk_decision_document`.
  - `lib/db/migrations/manual/2026-08-31-rbd-signature-capture-1512.sql` — additive,
    run against local `DATABASE_URL`, verified via `information_schema.columns` and
    the `simulator_migration_runs` self-marking row. Filed on #1630.
  - `artifacts/api-server/src/lib/rbd-document-render.ts` — deterministic template
    render of a version's own `content`/`signed`/`signedBy`/`signatureData` columns
    (never AI generation, never a re-read of live rows), reusing `insight-pdf.ts`'s
    Chromium `buildHtmlDoc`/`htmlToPdf` pipeline; persists one `msp_report_runs` row
    per version, updated in place on re-render so a post-signature render reflects
    the version's current columns rather than a stale pre-signature snapshot.
  - `artifacts/api-server/src/lib/rbd-versioning.ts` — `signRbdVersion` now also
    takes optional `signatureData`; added `getRbdVersionByUid`,
    `generateRbdShareLink` (30-day expiry, current+unsigned only), and
    `getRbdVersionByShareToken`.
  - `artifacts/api-server/src/routes/msp-rbd-versions.ts` (MSP-side) —
    `POST .../document` (render/persist), `POST .../share` (generate share link);
    `PATCH .../sign` now accepts optional `signatureData`.
  - `artifacts/api-server/src/routes/portal-rbd-document.ts` (new, CustomerUser
    floor, tenant-scoped like `portal-risk-register.ts`) — version
    history/current/document read, and the whole-document customer signature —
    distinct from that file's existing per-line-item accept flow.
  - `artifacts/api-server/src/routes/public-rbd-document.ts` (new) —
    unauthenticated read + rate-limited sign via share link, mirroring
    `msp-sow.ts`'s own public flow.
  - Verification: `tsc -b lib/db` clean (isolated, composite project). Full
    business-logic path — createRbdVersion → sign → supersede → re-sign-rejected →
    sign-superseded-rejected → generateRbdShareLink → getRbdVersionByShareToken →
    renderRbdVersionHtml (byte-identical on repeat, differs correctly between two
    versions with different real content, differs correctly before/after signing) →
    getPersistedRbdVersionDocument — run against real local Postgres inside a
    throwaway `rbdId` seeded from the one real `msp_risk_decisions` row
    (RBD-2026-575, Contoso Corporation), then fully cleaned up; all 22 assertions
    passed. **Honest gap**: `renderAndPersistRbdVersionDocument`'s actual
    `htmlToPdf()` Chromium call was NOT exercised — this Windows dev box has no
    Chromium/Nix (the same pre-existing constraint every other `insight-pdf.ts`
    consumer in this codebase already has locally, not introduced here); the DB
    round-trip it depends on was verified directly instead (see above).
  - **Filed two new issues, both parented and set to AI Batter Up**: #2089
    (worktree isolation — cross-package TS changes are unverifiable pre-merge via
    `tsc --noEmit` by the isolation design itself, AND a `@workspace/db` dependency
    junction was observed repointed mid-session by a concurrent process to an
    unrelated worktree, `C:\wt\1518-q936` — restored before finishing; parented to
    #1202).
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed before
    writing this line.
