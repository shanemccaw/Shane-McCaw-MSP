# #2631 — Data Rights and Privacy (MSP Console): regenerate contract pack from finished backend

- **Status:** ✅ DONE 2026-09-03T18:02:14Z
- **Scope:** platform
- **Started:** 2026-09-03
- **Commit(s):** b9e104cb4 (rebased/pushed as 44326e435, then 18b313e18 on main)

## Log
- 2026-09-03T17:58:40Z ⏳ IN FLIGHT — extracting the #1642-pattern contract pack for
  `msp-data-rights.ts`'s three real routes (GET /msp/data-rights, GET
  /msp/data-rights/customers/:customerId/users, POST
  .../deletion-request), read-only, no product code changes.
- 2026-09-03T18:02:14Z ✅ DONE — wrote
  `docs/data-rights-and-privacy-msp-console-contract-pack.md` (#1642 pattern):
  full wire contract for all 3 routes cited to file:line, CURRENT/DECIDED
  table, real enum unions (`audit_logs.action_type`/`actor_role`), honest-empty
  contract, cross-surface link to the shared `lib/data-rights.ts` helper and
  the customer-facing pack (#2549/#1652), forbidden list, orphaned-endpoint
  table (all 3 routes have zero live callers — no `artifacts/msp-console`
  frontend exists yet, confirmed via `ls artifacts/`). Real local-DB state
  checked via `psql` (1 real `data_export_downloaded` audit_logs row, 0
  `deletion_request_submitted`, 0 `msp_staff_customer_scopes` rows) and cited
  in the pack. No schema/code changes — read-only extraction session, no
  new findings warranted filing (the only two candidates — the missing
  `data-subject-rights.md` runbook, and the shared logging channel — are
  either already filed under #1652 or noted in-pack as a non-blocking
  observation, per §7). `git status --porcelain` clean;
  `verify-branch-merged.mjs` confirms `agent/2631-q1412` (18b313e18) is
  merged into `origin/main`.
