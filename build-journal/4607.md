# #4607 — tenant-write-preconditions.live-verify.ts: 3 #4528 assertions fail — #4522's CA-enforcement rule now intercepts before license

- **Status:** ✅ DONE 2026-09-18T05:04:45Z
- **Scope:** platform
- **Started:** 2026-09-18
- **Commit(s):** 42ea073ae (test fix, rebased to `31217b8f8` on `main`)

## Log
- 2026-09-18T05:00:06Z ⏳ IN FLIGHT — investigated real DB state; confirmed the CA-policy write shapes in `microrem.enforce-ca-policy` (`PATCH .../policies/{{policyId}} {"state":"enabled"}`) and SOP-SEED-IAM-03 step 3 (`PATCH .../policies/{id} {state: enabled}`) are static template/SOP content, not transient DB test debris — direction (a) applies, not (b). Proceeding to update the 3 assertions and add a license-only test case using `action.create-named-location` (license-gated, endpoint outside `/identity/conditionalAccess/policies`, so rule 0 never fires for it).
- 2026-09-18T05:04:45Z ✅ DONE — updated the 3 `#4528` assertions to expect `ca_enforcement_requires_promotion` (422), dropped their now-unneeded `lacksP1` skip guards (that refusal fires unconditionally — execute_action/SOP runs never pass `caEnforcementMode: "immediate"`), and added a new case proving the license rule still fires in isolation via `action.create-named-location`. Pushed to `main` at `31217b8f8`. Full opt-in live-verify suite re-run for real against the real local Postgres and the real testbed tenant (`mccawsoft2` / tenant row id 2080) both before and after the rebase onto `origin/main` — **all 8 tests pass** both times (7 existing + 1 new). `git status --porcelain` clean; `verify-branch-merged.mjs` confirms `agent/4607-q3234` is an ancestor of `origin/main`.

## Changes Made
- `artifacts/api-server/src/lib/tenant-write-preconditions.live-verify.ts` — updated 3 pre-existing assertions and the file's header doc comment; added 1 new test case. See `build-journal/4607-plan.md` for the full root-cause writeup and real GitHub blob permalinks.

## Verification Results
```
node --env-file=.env.local node_modules/vitest/vitest.mjs run \
  --config vitest.live-verify.config.ts \
  src/lib/tenant-write-preconditions.live-verify.ts

 Test Files  1 passed (1)
      Tests  8 passed (8)
```
Ran twice for real (pre-rebase and post-rebase onto `origin/main`), both against the
real local Postgres and the real testbed tenant (`c4c814d4-3afe-441e-9145-62461d0a4fd3`).
`npx tsc --noEmit` shows no errors for the touched file.
