# #3491 — Feature: clicking a Build Queue item highlights/selects the matching build in Build Watch, if running

- **Status:** ✅ DONE 2026-09-10T05:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-10
- **Commit(s):** 06f17026b (rebased to 2371f7386)

## Log
- 2026-09-10T05:28:02Z ⏳ IN FLIGHT — investigated BuildWatchWindow's slot/QueueItemId tracking and BuildQueuePanel's existing SelectNode item-click handler; implementing `BuildWatchWindow.SelectSlotForQueueItem(int)` + wiring it from MainWindow's existing `BuildQueuePanel_TaskSelected` (TaskSelected already fires QueueItemId + Status on every card click). UX decision (per issue's explicit flag): do NOT force-open Build Watch on a queue-item click — only sync/highlight it if it's already open, matching the existing Log Viewer sync's same "sync if open, don't force open" shape right above it in the same handler.
- 2026-09-10T05:40:00Z ✅ DONE — added `BuildWatchWindow.SelectSlotForQueueItem(int queueItemId)`: finds the occupied slot with that `QueueItemId` (no-op if not found — not open, not running there, or evicted), calls `BringIntoView()` (defensive — the 8-slot UniformGrid has no scrolling today, all slots always visible) and applies a real, visible highlight via a new `FlashSlotSelection` helper: BlueBrush border (2.5px, same accent BuildQueuePanel already uses for its own selected-card border) + a brighter/wider DropShadowEffect halo for 1.8s, then reverts to whatever `ApplySlotGlow`/border the slot's *current* real state calls for (handles the slot changing state or clearing mid-flash). Wired from `MainWindow.BuildQueuePanel_TaskSelected` (fires on every `SelectNode` card click via the existing `TaskSelected` event, which already carries `QueueItemId`+`Status`): calls `_buildWatch?.SelectSlotForQueueItem(e.QueueItemId)` only when `e.Status == "running"`. Real, honest scope check per issue's own item 3: a non-running click never even attempts the lookup. Build Watch is genuinely never force-opened by this — matches the Log Viewer sync immediately above it in the same handler (`existing?.ScopeToBuild(...)`, no `OpenLogViewerTab()` unless `e.OpenLogPanel`); Shane's stated ask was purely "if that build is currently running there" (i.e. only when Build Watch is already open and tracking it), so this reads as the intended default, not an unstated assumption. `dotnet build desktop/BuildConsole/BuildConsole.csproj -c Debug` → Build succeeded, 0 warnings, 0 errors. No manifest/DB/schema work — pure WPF UI wiring, out of test-manifest scope per CLAUDE.md. `git status --porcelain` clean; branch verified an ancestor of origin/main before this line was written.
