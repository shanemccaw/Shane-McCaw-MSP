# #1401 — My Architect: remaining fixture leaks after #1398 (architect, billing, rolled/remaining date strings, heading)

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-27
- **Commit(s):** c1ce2bf90 (fix), 25e936933 (test-manifest hash pin)

## Log
- 2026-08-27 ⏳ IN FLIGHT — investigating each of the 5 leaks per the issue: architect name, billing line, rolledExpiry, remainingTail, heading. Found real backend sources for architect (retainer_settings.architect_name) and billing rate (retainer_settings.hourly_rate_cents) already returned by GET /api/portal/retainer as `settings`, just not consumed by useRetainerLive.ts. rolledExpiry/remainingTail are genuinely derivable from the bucket's real `period` ("YYYY-MM", also already in the wire payload) via the documented rollover model (rolled hours expire at period end). heading's month/year is derivable from the real current date directly (independent of retainer enrollment). Implementing: extend useRetainerLive to expose settings.architectName/hourlyRateCents + bucket.period; add pure derivations (periodEndDate/rolledExpiryLabel/daysLeftInPeriodLabel/currentHeadingLabel) to retainerModel.ts; wire the page to real values when configured, honest no-data when not.
- 2026-08-27 ✅ DONE — All 5 fields are now real, no backend/schema change needed (the data already existed, just wasn't consumed by the frontend hook):
  - **architect** — `retainer_settings.architect_name` (nullable text, already in the API's `settings` payload). Shows the real name when set; "No architect assigned yet" when the row exists but has none; hidden entirely (not "No architect assigned") when there's no retainer row at all, since the honest-empty state below already covers that case.
  - **billing** — real dollar figure computed from `bucket.retainedHours * settings.hourlyRateCents / 100`. Dropped the fake "next status report Friday" schedule clause entirely — no real report-schedule field exists anywhere, and per the HARD RULE a real number does not license inventing a fake day next to it.
  - **rolledExpiry** / **remainingTail** — derived from the bucket's real `period` ("YYYY-MM", already in the wire payload) per the documented rollover model in `retainer-hours.ts` (rolled hours expire at the end of their own period, never roll twice): new `periodEndDate`/`rolledExpiryLabel`/`daysLeftInPeriodLabel` in `retainerModel.ts`. Also renamed the adjacent "Rolled from July" MicroStat label to "Rolled from last month" — a hardcoded prior-month name sitting next to a now-genuinely-computed date would have been the exact same leak class in the next field over.
  - **heading** — `currentHeadingLabel()` states the real current calendar month/year, independent of retainer enrollment (so it's correct even for an unconfigured account, unlike the other four which need a real retainer row).
  Removed the five now-dead fixture keys from `RET_COPY` in `retainerData.ts` so a stale value can't leak back in later. Added 8 new unit tests in `retainerModel.test.ts` (periodEndDate/rolledExpiryLabel/daysLeftInPeriodLabel/currentHeadingLabel, including a leap-year and a stale-read-past-period-end case) — full suite (31/31, this file + NoScanDataState.test.ts) passes. `tsc --noEmit` on `artifacts/msp-portal` is clean except the same 2 pre-existing unrelated errors #1398 already flagged (SowScreen.tsx, PortalV2Shell.tsx — untouched by this session). Updated `test-manifests/portal/portal-v2-retainer.json`: added RET15 (architect/billing block renders without throwing for the unenrolled test account) and a new note documenting the fix and this harness's real limit — it has no negative-text assertion capability, so it cannot mechanically prove a fictional name/dollar figure is *absent*; that was verified by direct code review instead. **Could not execute the manifest via `shaneapp://runTest` this session** — same as #1398, the `shane-msp` MCP server carrying the `shaneapp://` protocol failed to connect (CONNECTION_CLOSED) for the whole session; a real, statable connection failure, not a scope gap. Pushed straight to `origin/main` (c1ce2bf90, 25e936933) per this repo's commit-to-main convention.
