# #1744 — Microsoft Changes: wire the portal UI to the real endpoints

- **Status:** ✅ DONE 2026-09-06T19:18:20Z
- **Scope:** platform
- **Started:** 2026-09-06
- **Commit(s):** ccc4b7d8a (merged directly to `main`)

## Log
- 2026-09-06T19:01:13Z ⏳ IN FLIGHT — Real Design export landed
  (`Design/portal/design_handoff_full_site/screens/Microsoft Changes.dc.html`) plus
  `docs/microsoft-changes-contract-pack.md` (§0-§7, real, extracted from finished
  backend). Building the real page in `artifacts/portal`: stat cards, day-one honest
  banner, triage (needs-a-decision) section with real decline action, horizon wave
  grid + service density + in-this-wave detail, date-unclear column. Found one real
  gap while wiring: the customer wire (`GET /api/portal/message-center`) has no field
  at all connecting a post to its routed Change Request (code/status/intake) even
  though `m365_change_routings` + `msp_change_requests` carry that link server-side —
  needed for the triage card's "Decline" action (which POSTs to
  `/api/portal/change-control/:code/decline`) and the "needs a decision" framing.
  This is a missing wire field, not a product decision (the routing engine and CR
  linkage already exist per contract pack §1d/§4) — adding a `routing` field to the
  route per the HARD RULE ("BUILD IT") rather than stopping at an empty state.
- 2026-09-06T19:18:20Z ✅ DONE — Shipped `ccc4b7d8a`. Backend:
  `artifacts/api-server/src/routes/portal-message-center.ts` now joins
  `m365_change_routings` + `msp_change_requests` (scoped by the same
  `(mspId, customerId)` predicate as the existing analysis join) and adds a
  `routing: WireRouting | null` field to every post — decision, reason, intake,
  `changeRequestCode`/`changeRequestStatus` (only when the CR row actually still
  resolves — hand-checked against local `DATABASE_URL`: `m365_change_routings` id=3
  points at `change_request_id=83`, which does not exist in the local, currently-empty
  `msp_change_requests` table; the code deliberately keys presence off the JOINED row's
  own `id`, not the routing row's soft FK, so this dangling case correctly yields no
  code rather than a code for a CR that would 404 on decline), and `declined`. No
  schema change needed — both tables already existed.
  Frontend: `artifacts/portal/src/components/microsoft-changes/{types.ts,
  useMicrosoftChangesLive.ts}` (data layer) + `artifacts/portal/src/pages/
  microsoft-changes.tsx` (the real page, route `/microsoft-changes`, wired into
  `App.tsx` and `moduleNav.ts`) — stat cards, day-one honest-empty banner, TRIAGE
  section (real posts whose routing is `auto_created`+`approval` intake, still open,
  undeclined — empty right now on the local tenant since the one real routing present
  is `informed` intake, which correctly means no decision needed) with a real Decline
  dialog wired to `POST /api/portal/change-control/:code/decline`, HORIZON wave grid
  (real 5-band axis from `buckets[].wave`, not the design's mismatched 4-band mock —
  the design's own "Next 30/31-90/Beyond 90" labels don't match what `buildBuckets`
  actually produces, so the real band structure is used instead) + per-workload
  density grid + "in this wave" detail + a real date-unclear column. No fixture data
  anywhere — every number comes from `GET /api/portal/message-center`.
  Verification: `tsc --noEmit` clean on both `artifacts/api-server` and
  `artifacts/portal`; `vite build` clean; the existing 57-test
  `portal-message-center.test.ts` suite still passes unchanged (only the route, not
  the lib, was touched). Wrote and registered
  `test-manifests/portal/microsoft-changes.json`.
  **`shaneapp://runTest` NOT run this session** — blocked on the same, already-filed,
  still-open **#3054** (dev-server checkout 93+ commits behind `origin/main`,
  merge-back coordinator fails on a spurious add/add conflict) that #2992's own
  bookend documented; not re-filed as a duplicate. `git merge-base --is-ancestor` /
  `verify-branch-merged.mjs` confirms `ccc4b7d8a` is a real ancestor of `origin/main`;
  `git status --porcelain` clean.
  No new GitHub issues filed — the one real gap found (the missing `routing` wire
  field) was fixed in this same build, which CLAUDE.md's finding rules exclude from
  "file it as a new issue."
