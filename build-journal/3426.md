# Git #3426 — Remove Hub Storage nav entry from Admin Panel

- **Status:** ✅ DONE 2026-09-10T00:10:00Z
- **Scope:** platform

## Log
- 2026-09-10T00:00:00Z — Investigated. Issue premise ("no real backing page exists behind del-hub-storage / /delivery/hub-storage") is FALSE. Found:
  - `artifacts/admin-panel/src/pages/SharePoint.tsx` (324 lines) — full, real UI: hub config, file/folder browsing, breadcrumbs.
  - `artifacts/admin-panel/src/pages/workspaces/DeliveryWorkspace.tsx:35` — `case "hub-storage": return <SharePointPage />;`
  - `artifacts/api-server/src/routes/admin-sharepoint.ts:27,40` — real `GET`/`POST /api/admin/sharepoint/hub-config` endpoints.
  - `artifacts/admin-panel/src/App.tsx:350` — `/sharepoint` redirects to `/delivery/hub-storage`.
  - This is a live, wired feature, not a dead nav entry. Removing it would delete working product surface based on a stale/incorrect audit.
  - Stopping per the "real product decision" exception — this conflicts with the issue's own stated premise and needs Shane's call, not a deletion.
  - Filed finding issue for Shane's decision (see below); no code changed.
- 2026-09-10T00:10:00Z — ✅ DONE — No code change (nothing to remove; audit premise disproven). Filed #3548 (parented under #1095) documenting the real evidence and asking Shane to decide: keep Hub Storage as-is, or remove the whole feature (nav + page + backend + consent flow) if the original intent still stands. Commented on #3426 with the same evidence. Labels: in-flight→complete applied on #3426.
