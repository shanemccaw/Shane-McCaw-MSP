# #4451 — Remove "What this page deliberately does not do" internal disclosure component from customer view (Portal)

- **Status:** ✅ DONE 2026-09-17T02:50:09Z
- **Scope:** platform
- **Started:** 2026-09-17
- **Commit(s):** 11fe078b8 (removal across 22 pages + 5 manifests), c80e9507a (lastVerifiedAgainstCommit follow-up)

## Log
- 2026-09-17T02:24:20Z ⏳ IN FLIGHT — Investigated: confirmed this is NOT one shared imported React component. It is the same UI shape ("What this page deliberately does not do" / "What this page does not do" collapsible ledger box, §N.M spec citations) hand-duplicated inline across ~22 portal page files, each with its own `ledgerOpen` (or equivalent) state and a module-level `LEDGER`-style const array. Confirmed pages beyond Shane's 8: billing, break-glass-status, customer-diagnostics, customer-documents, customer-offers, customer-requests, customer-team, data-rights-and-privacy, email-auth-setup, my-architect, notification-preferences, offboarding, ownership, poams, policy-decisions, project-detail, remediation-tracking, runbooks, scope-and-sla, security-plan, status-reports, webhooks. Decision: removing outright from all of them (not moving to MSP Console) — this is internal engineering scope/architecture-decision commentary (contract-pack §N.M citations, build-session rationale) with no operational value to an MSP operator (no customer data, no actionable state); it isn't the kind of content the MSP Console surfaces. Three non-page files (`lib/ownership-api.ts`, `lib/remediation-checklist-types.ts`, `api-server/src/routes/portal-change-catalog.ts`) only reference the pattern in code *comments* (not customer-facing UI) — left untouched, out of scope.
- 2026-09-17T02:50:09Z ✅ DONE — Removed the disclosure box (JSX block/component, toggle state, LEDGER-style data array) from all 22 confirmed pages. `support.tsx`'s distinct "What ShaneBot will not do" card (different heading, real customer-relevant AI-behavior transparency, not internal spec-citation commentary) was deliberately left alone — out of scope. Fixed 4 stale doc-comments that dangled a "see the ledger card below" pointer after removal (billing.tsx, customer-offers.tsx, my-architect.tsx, offboarding.tsx, data-rights-and-privacy.tsx). Updated 5 test manifests whose uiSteps asserted the removed component's presence (test-manifests/portal/ownership.json OWN8, poams.json UI7, scope-and-sla.json SS6, sops-runbooks.json U12 — all removed since textContains can't assert absence; diagnostics-and-scripts.json UI3 flipped to assert `state: absent` on the surviving testid) and set `lastVerifiedAgainstCommit` on all 5 to the real hash. Cleaned up two now-unused imports my own removal caused (Card/CardContent in poams.tsx, useState in scope-and-sla.tsx) — confirmed via a real unused-import scan against every touched file; a handful of pre-existing unused `type` imports in customer-documents.tsx/email-auth-setup.tsx/ownership.tsx/runbooks.tsx/security-plan.tsx were confirmed pre-existing (git diff shows my edits never touched those lines) and left alone as out-of-scope debt, not filed as a finding (cosmetic dead-code, not a real bug). Verification: `pnpm run typecheck` (tsc --noEmit) and `pnpm run build` (vite build) both clean on artifacts/portal after every edit. `shaneapp://runTest` was not invoked — no runTest tool/script is reachable from this session's toolset (confirmed via ToolSearch), and the standing worktree-merge-gap precedent (documented in several of the touched manifests' own notes) means it couldn't reach an isolated worktree's changes anyway; this is the Git #2160 "genuinely unreachable" case, not an assumed default. No new findings filed as sibling sub-issues of #4451 — the only real discovery (the disclosure box was NOT one shared component, contrary to the issue's own suggestion, but 22 independently hand-duplicated copies with inconsistent implementations) is investigation result for *this* issue, not a new bug for a sibling. Branch verified merged into origin/main (`verify-branch-merged.mjs` exit 0); working tree clean.
