# #3217 — Feature: Money — Cars real charging cost + mileage tracking (Shane's Life)

- **Status:** 🛑 BLOCKED 2026-09-08T18:35:00Z
- **Scope:** platform
- **Started:** 2026-09-08
- **Commit(s):** none — genuinely blocked before any code change

## Log
- 2026-09-08T18:35:00Z 🛑 BLOCKED — Pulled #3217's real scope (real odometer sync +
  real charging-cost tracking off Tesla's Fleet API, feeding #3149's maintenance
  logic). Confirmed the standing `blocked_by` edge to #3158 (Tesla OAuth) is still
  accurate, not stale: #3158 is `state: open`, labeled `complete` + `Shane To-Do` —
  the Tesla integration code shipped and was hands-on verified against the local DB
  (webhook → nudge pipeline, dedupe), but the actual OAuth round-trip to
  `auth.tesla.com` is unexercised because `TESLA_CLIENT_ID`/`TESLA_CLIENT_SECRET`/
  `TESLA_REDIRECT_URI` aren't set locally/Replit yet — that's explicitly `Shane
  To-Do` on #3158, not something an agent can complete. Without a real connected
  Tesla account there is no live vehicle_state odometer data or Charging Endpoints
  data to build #3217's real sync against, and this issue's own body says "Real,
  confirmed available via Tesla's Fleet API given Shane's broad real scope
  selection at app registration" — contingent on that connection existing.
  Left the existing `blocked_by 3158` edge in place (correct, not stale) and did
  not touch #3199 (deploy) since that's #3158's own dependency, not #3217's direct
  blocker. No code changed. Labels/comment updated on #3217 per standard blocked
  workflow; ending the session here rather than guessing forward on Fleet API
  scope/shape while the underlying connection doesn't exist yet.
