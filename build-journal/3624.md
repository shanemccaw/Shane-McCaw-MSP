# #3624 — Build Queue: a card with real open blockers should show "Blocked" + a ghost card, never a generic status

- **Status:** ⏳ IN FLIGHT 2026-09-11T03:40:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-11T03:26:00Z ⏳ IN FLIGHT — claimed dispatch, pulled #3624's full body + comment, pulled #3623's full body + comment per the dispatch instruction ("wait for and read #3623's real findings first").
- 2026-09-11T03:30:00Z 🛑 BLOCKED — confirmed via `gh api .../issues/3624/dependencies/blocked_by` that the real edge to #3623 is already wired (state: open), and confirmed #3623 has no `build-journal/3623.md` yet and is still labeled `in-flight` with zero comments beyond the dispatch — its investigation has not produced findings yet. #3624's own dispatch body is explicit: "Wait for and read #3623's real findings first (this may already be fixed by the claim-logic correction, or a genuine separate display gap may remain — don't guess in isolation)." Per CLAUDE.md's bounded-wait rule, a session must not spin/poll indefinitely on an unresolved dependency — self-blocking now (label + real edge, which already exists) rather than guessing at a fix that #3623's own findings may make moot or reshape. Applied `blocked` label, removed `in-flight`, left explanatory comment on #3624. No code touched; working tree clean.
- 2026-09-11T03:40:00Z ⏳ IN FLIGHT — Shane re-dispatched and unblocked directly: removed the real `blocked_by` edge to #3623 and confirmed in a comment ("RE-DISPATCH, UNBLOCKED") that this issue's fix is a pure display fix independent of #3623's separate claim-logic root-cause work — 16 real items affected right now. Confirmed `dependencies/blocked_by` is now empty. Removed `blocked` label, restored `in-flight`. Resuming real implementation now.
