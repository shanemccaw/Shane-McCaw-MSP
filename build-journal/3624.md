# #3624 — Build Queue: a card with real open blockers should show "Blocked" + a ghost card, never a generic status

- **Status:** ✅ DONE 2026-09-11T03:55:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-11
- **Commit(s):** cf01f76fb (real fix), plus own bookend/label commits ab666f971, 918cf5130

## Log
- 2026-09-11T03:26:00Z ⏳ IN FLIGHT — claimed dispatch, pulled #3624's full body + comment, pulled #3623's full body + comment per the dispatch instruction ("wait for and read #3623's real findings first").
- 2026-09-11T03:30:00Z 🛑 BLOCKED — confirmed via `gh api .../issues/3624/dependencies/blocked_by` that the real edge to #3623 is already wired (state: open), and confirmed #3623 has no `build-journal/3623.md` yet and is still labeled `in-flight` with zero comments beyond the dispatch — its investigation has not produced findings yet. #3624's own dispatch body is explicit: "Wait for and read #3623's real findings first (this may already be fixed by the claim-logic correction, or a genuine separate display gap may remain — don't guess in isolation)." Per CLAUDE.md's bounded-wait rule, a session must not spin/poll indefinitely on an unresolved dependency — self-blocking now (label + real edge, which already exists) rather than guessing at a fix that #3623's own findings may make moot or reshape. Applied `blocked` label, removed `in-flight`, left explanatory comment on #3624. No code touched; working tree clean.
- 2026-09-11T03:40:00Z ⏳ IN FLIGHT — Shane re-dispatched and unblocked directly: removed the real `blocked_by` edge to #3623 and confirmed in a comment ("RE-DISPATCH, UNBLOCKED") that this issue's fix is a pure display fix independent of #3623's separate claim-logic root-cause work — 16 real items affected right now. Confirmed `dependencies/blocked_by` is now empty. Removed `blocked` label, restored `in-flight`. Resuming real implementation now.
- 2026-09-11T03:55:00Z ✅ DONE — Root cause confirmed by tracing the real code: `IsGenuinelyBlocked` (desktop/BuildConsole/Controls/BuildQueuePanel.xaml.cs) bailed out with `if (item.Status != "queued") return false;` before ever consulting the live open-issue set — so `node.IsBlocked` was hard-wired `false` for any row not in raw `queued` status, no matter how genuinely open its declared blocker was. `GhostStatusLabel` already checked `IsBlocked` before Verifying/canceled (correct priority), but the flag feeding it was never true outside `queued` — confirming point 3 of this issue's own body almost exactly: the priority order was fine, the flag computation was the gap.
  - Fix: extended `IsGenuinelyBlocked` (new shared `IsBlockedEligibleStatus` helper) to also cover `Verifying` (the #3585 incident class — claimed/ran with a real open blocker) and a supervisory self-cancel pending re-dispatch (`canceled` + `ExitCode == 0`, i.e. `IsWaitingSelfBlocked`) — the exact three statuses this issue's body names ("queued, canceled+WAITING, or even a Verifying row"). Other statuses (done, failed, genuine abandoned cancel, superseded, parked, capped, external, limit-paused) deliberately left alone — terminal, or a distinct staging state with its own label that shouldn't be overridden.
  - `BuildQueueCard`'s headline status pill gets a new `isBlocked` branch (same priority GhostStatusLabel already used) so the card itself now renders "🔒 BLOCKED" for any eligible status, not just queued.
  - The ghost-card render gate (`BuildBlockerGhostCard`) now shares the same `IsBlockedEligibleStatus` check, so a genuinely-blocked Verifying/WAITING card also gets the real, clickable ghost card(s) naming the blocker(s) — not just the headline label.
  - `CreateGraphNodeDot` (mini-map DAG dot) already read `node.IsBlocked` directly — picks up the fix automatically, no code change needed there.
  - `ComputeQueueStatusCounts` (header Blocked/UpNext counts) verified UNaffected: it `continue`s past Verifying/canceled rows before ever calling `IsGenuinelyBlocked`, so the header numbers keep their existing queued-only semantics — out of this issue's scope.
  - Verification: `dotnet build BuildConsole.csproj` — 0 errors (pre-existing unrelated warnings only). Per established policy (Git #2156 memory), did NOT launch BuildConsole.exe or drive its UI — build + full code-path tracing (every real consumer of `node.IsBlocked` checked) is this app's sanctioned verification. Cross-checked current live queue state via direct `DATABASE_URL` query + `gh issue list --state open`: no live Verifying row currently carries an open declared blocker (#3585's own `blocked_by_numbers` is now empty in the DB — the data-population root cause is #3623's own scope, not this issue's), so the fix is presently latent on live data but will fire the instant a Verifying/WAITING row does carry a real open blocker; the existing `queued` path (#3421→#3425's still-queued blocked chain) continues to render correctly, confirming no regression.
  - No new finding filed: the one adjacent gap found (#3585's `blocked_by_numbers` being empty) is already the exact subject #3623 is investigating (its own body's point 2) — noted in the issue comment rather than duplicated as a new issue.
  - `git status --porcelain` clean; `HEAD` confirmed ancestor of `origin/main` via `git merge-base --is-ancestor`.
