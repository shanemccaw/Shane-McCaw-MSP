# #2814 — msp-sla-scope-creep.test.ts mock gaps

- **Status:** ✅ DONE 2026-09-04T15:25:00Z
- **Scope:** platform

## Log
- 2026-09-04T15:20:46Z — Started. Issue body states the fix (logger.child mock, drizzle-orm `eq`,
  `mspStaffCustomerScopesTable`/`tenantsTable` stubs, `db.select().from().where()` chain) was
  already applied as part of #2726's own commit, same file.
- 2026-09-04T15:25:00Z — ✅ DONE — Verified: the mock-gap fix described in the issue body was
  already committed at `f49524294` (part of #2726's "Fix MSP scope-creep evaluate cross-customer
  scoping bug"), confirmed still present in `artifacts/api-server/src/routes/msp-sla-scope-creep.test.ts`
  (`logger.child` mock, `eq: vi.fn(...)`, `mspStaffCustomerScopesTable` stub all in place). Ran
  `npx vitest run msp-sla-scope-creep` from `artifacts/api-server/` — all 26 tests pass. No code
  change needed this build beyond this bookend. Commit `a8b726051` (IN FLIGHT bookend) rebased and
  pushed to origin/main as `7f8141d05`.
