# #3990 — Exiting Test Mode never runs the DOM Inspector's stop script

- **Status:** ✅ DONE 2026-09-13T00:30:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-13
- **Commit(s):** 74c697667 (fix), 2c235afa0 (post-rebase HEAD on origin/main)

## Log
- 2026-09-13T00:00:00Z ⏳ IN FLIGHT — Fixing `TestModeDiagnosticsPanel.ClearActiveTab()` and `AttachWebView()` to run `DisableDomInspectorAsync`/`DisableDomMutationObserverAsync` (the real in-page stop script) against the outgoing WebView2 before detaching, so DOM Inspector's unthrottled mousemove/click listeners don't keep running on the tab after Test Mode exits or after a tab switch mid-session.
- 2026-09-13T00:30:00Z ✅ DONE — `ClearActiveTab()` now captures the outgoing WebView2 reference, clears C#-side wiring synchronously (unchanged behavior), then awaits `DisableDomInspectorAsync` + `DisableDomMutationObserverAsync` against that captured reference — the real fix for Test Mode exit. `AttachWebView()` got the same treatment for the tab-switch path called from `MainWindow.xaml.cs` via `UpdateTestModeActiveTab()` (lines 2668, 7095) — it now stops the previous tab's DOM inspector/mutation observer before attaching the new tab, guarded so same-tab reattachment doesn't fire the stop script unnecessarily. Both methods stayed `async void` (fire-and-forget, matching this file's existing pattern for `EnableDomInspectorAsync` etc.) so no call-site changes were needed at `MainWindow.TestMode.cs`'s `ExitTestMode()`/`UpdateTestModeActiveTab()`. Verified: `dotnet build BuildConsole.csproj -c Debug` — Build succeeded, 0 warnings, 0 errors. `verify-branch-merged.mjs` confirms `agent/3990-q2670` (2c235afa) merged into `origin/main`. `git status --porcelain` clean. No live WebView2/BuildConsole runner was launched for this session (HARD RULE: never launch BuildConsole.exe to verify) — verification is compile-clean + code-path trace matching the issue's exact repro description; the stop script itself (`DomInspectorStopScript`/`DomMutationStopScript`) is idempotent (guards on `window.__vttDomInspectorCleanUp` existence), so calling it even when the inspector was never engaged is harmless.
