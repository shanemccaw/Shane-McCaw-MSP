# #2401 — Issues with no Feature parent — new RepoHealthRule needed

- **Status:** ✅ DONE 2026-09-03T01:05:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-02
- **Commit(s):** 885fd46e8 (rebased/pushed to main as 71a2eae9d)

## Log
- 2026-09-02T19:33:00Z ⏳ IN FLIGHT — Read #2401 body + comment (Posted 2026-09-02T19:28:08Z), #2400 (Feature parent), and `desktop/ShaneBuilder/ShaneBuilder-FullFeature.md` cross-cutting Repo Health item 5. Adding `RepoHealthRule.NoFeatureParent` to the existing Depth/Naming/Stale/Orphan set in `desktop/ShaneBuilder/Services/RepoHealthService.cs` + wiring `MainWindow.xaml.cs`/`.xaml`. Worktree at `C:\wt\2401-q1234` was not auto-provisioned by BuildConsole (empty dir, no `.git` — same class as Git #2118); self-provisioned via `git worktree add` + manual `registerWorktree`/`linkDeps` per the documented recovery recipe.
- 2026-09-03T01:05:00Z ✅ DONE — Added `RepoHealthRule.NoFeatureParent`: a real leaf issue (no sub-issues, not itself an Epic/GATE/Feature container) whose full fetched parent chain carries no `Feature:`-prefixed ancestor. Wired into `RunScanAsync`, `FixableDirectly` (re-parentable like Naming), `RuleLabel`, and the MainWindow.xaml.cs tile grid / rule-description panel / send-to-chat flow (bumped both `UniformGrid` tile rows from 4→5 columns in `MainWindow.xaml`). Verified with `dotnet build ShaneBuilder.csproj -c Debug` — 0 errors. Sanity-checked the rule's real-world output with a live `gh api graphql` sample of open issues (same shape `RepoHealthService.cs` queries): correctly excluded empty `Feature:`-titled containers (#2494, #2474) and correctly flagged #2455 as a genuine no-Feature-parent leaf. That #2455 finding was real (references Feature #1696 in body prose only, no actual `sub_issues` edge) — filed as #2495, parented as a sibling sub-issue of #2400 (not the Epic), labeled `bug`, board status set to "AI Batter Up". `verify-branch-merged.mjs` confirms 71a2eae9d is an ancestor of `origin/main`. `git status --porcelain` clean.
