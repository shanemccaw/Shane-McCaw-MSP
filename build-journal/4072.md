# #4072 — Read-boundary audit coverage: remaining admin-*.ts files (#4046 follow-up)

- **Status:** ✅ DONE 2026-09-14T18:44:43Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** 70a04c1e3 (merged as 37bfadf14 on origin/main)

## Log
- 2026-09-14T17:57:18Z ⏳ IN FLIGHT — pulled issue #4072 body + dispatch comment, reviewed #4046's established audit pattern (`auditPrivilegedRead`/`resolveAuditActorRole`/`createAuditLog` in `lib/audit.ts`, and the wired call sites in `admin-active-directory.ts` / `admin-document-downloads.ts`). Enumerated all 93 non-test `admin-*.ts` route files. Next: triage each for a PlatformAdmin GET reading one specific customer's/MSP's/user's data, then wire the missing ones and resolve the 2 judgment calls (`live-document-shares.ts`, `msp-sow.ts`).
- 2026-09-14T18:44:43Z ✅ DONE — Triaged all 93 non-test `admin-*.ts` files (5 parallel research passes) and found 61 real gap routes across 35 files (full list in the issue comment); wired `auditPrivilegedRead`/`createAuditLog` into every one, matching the #4046 pattern exactly (hardcoded `actorRole: "platform_admin"` under plain `requireAdmin`; `if (req.user)` guard on the 2 routes gated by `requireAdminOrIngestToken` since that middleware also accepts an unattended bearer-token caller with no session; query-param-scoped routes only audited on the branch where the id was actually provided). Resolved both #4046 judgment calls with an explicit no-audit decision, documented in the issue comment. Verification: `tsc --noEmit` clean across the whole api-server package; full `pnpm run test` (vitest + node test runner) — found and fixed 6 real regressions the new `req.user` reads exposed in test mocks (admin-retainer, admin-write-actions, admin-public-chat, admin-monitor-check-runs x2, admin-m365-run's module-mock export list); remaining test failures (pcc.test.ts, `*.live-db.test.ts`, several `lib/*.test.ts`) confirmed pre-existing via `git stash` against a clean baseline, unrelated to this work. Filed the one confirmed-unrelated pre-existing regression (pcc.test.ts, 5 failures since #3551) as issue #4105, parented to #1946 (Feature parent), board status AI Batter Up. `git status --porcelain` clean; branch verified merged into origin/main via `verify-branch-merged.mjs`.
