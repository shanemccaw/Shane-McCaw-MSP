# #3708 — shanes-git: raise list-tool caps to 100 + enforce Epic/Feature/Issue hierarchy

- **Status:** ✅ DONE 2026-09-12T00:29:55Z
- **Scope:** platform
- **Started:** 2026-09-12
- **Commit(s):** 86732d3fe (real work, rebased/pushed as ae73279c5 on main), d9396116e (IN FLIGHT bookend, rebased as e9d5ce336)

## Log
- 2026-09-12T00:25:00Z ⏳ IN FLIGHT — Confirmed blocker #3697 is CLOSED/complete (landed as
  c8b901888 per its own bookend). Investigated `artifacts/github-mcp-server`: `listSubIssues` /
  `listBlockedBy` in `src/github.ts` had no `per_page` param at all (GitHub's own 30-row default),
  confirmed root cause of ask 1. No existing Epic/Feature title-classification logic anywhere in
  the server (ask 2 confirmed net-new). GitHub's real `GET /issues/{number}/parent` endpoint
  confirmed via docs for ask 3's "same Epic" lookup. Implementing: `per_page=100` fix, new
  `classifyIssueTitle`/`getParentIssue`/`SUB_ISSUE_HARD_CAP` in `github.ts`, new `src/hierarchy.ts`
  (`enforceHierarchyOrThrow`, `resolveOverflowParent`), wired into `add_sub_issue`. Typecheck green
  so far.
- 2026-09-12T00:29:55Z ✅ DONE — Shipped `ae73279c5` on `main`:
  - Ask 1: `listSubIssues`/`listBlockedBy` in `src/github.ts` now request `?per_page=100`.
  - Ask 2: new `classifyIssueTitle`/`getParentIssue`/`SUB_ISSUE_HARD_CAP` in `github.ts` + new
    `src/hierarchy.ts` (`enforceHierarchyOrThrow`) wired into `add_sub_issue` — a non-Feature
    child under a real Epic is rejected with `HierarchyViolationError` before any GitHub write.
  - Ask 3: `resolveOverflowParent` in `src/hierarchy.ts` — a Feature at/near the real 100-cap is
    redirected onto an existing `"<Feature> — Part N"` sibling under the same Epic (or a newly
    created one) before a raw 422 can happen; `add_sub_issue`'s response now reports
    `parentNumber`/`requestedParentNumber`/`redirected`/`redirectReason`.
  - New `scripts/verify-hierarchy-and-caps.mjs` (`npm run verify-hierarchy`) — real, live
    verification against GitHub (real #1096 has 72 sub-issues, proving the per_page=100 fix; real
    rejection of a non-Feature child under real Epic #1202; overflow-title helpers reproduce the
    real, already-live `#1788 → #3706` title exactly). Deliberately performs no real
    add/create writes — the genuinely-at-cap redirect/create branch is exercised by direct code
    review of the same functions the no-redirect path already proves are wired correctly, since a
    live run of that branch would create a real overflow issue on every verification run.
  - `npm run typecheck`, `npm run verify-hierarchy`, and `npm run verify-contents` (regression, no
    new failures) all green.
  - `git status --porcelain` clean; `verify-branch-merged.mjs` confirmed `agent/3708-q2372`
    (ae73279c5) is an ancestor of `origin/main`.
  - No findings filed — no genuine bug/gap discovered outside this issue's own scope during the
    build.
