# #4526 — Build agents' psql calls hang silently, get auto-backgrounded

- **Status:** ✅ DONE 2026-09-17T17:35:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** 3ee3ce85b (rebased onto main as 27dccbd61)

## Log
- 2026-09-17T17:10:00Z ⏳ IN FLIGHT — Reproduced root cause in a scratch shell: this Bash tool's env has `DATABASE_URL`/`PGPASSWORD` genuinely unset (confirmed via `echo`), even though `.env.local` defines `DATABASE_URL` — the shell never sources it (and per CLAUDE.md, never should — sourcing `.env.local` leaks the MT cert PEM key). `psql "$DATABASE_URL" -c "select 1"` with `$DATABASE_URL` empty falls back to psql's default connection params (localhost/current-user), which requires a password; psql prompts for it directly against the controlling terminal (bypassing stdout/stderr redirection), and in this non-interactive tool that read blocks forever with zero captured output — exactly the empty-then-`[killed]` shape in the issue. Verified `timeout 5 psql "$DATABASE_URL" -c "select 1"` → empty output, exit 124 (timeout kill). Verified fix direction: `psql -w` (never prompt) fails fast instead — `psql: error: ... fe_sendauth: no password supplied`, exit 2, real error text captured normally.
- 2026-09-17T17:35:00Z ✅ DONE — Shipped `scripts/db/psql.mjs`: loads `DATABASE_URL` safely via `find-tenant-scoped-tables.mjs`'s existing `loadDatabaseUrl()` (regex out of `.env.local`, never a full `source`), builds a password-free conninfo + `PGPASSWORD` env via `pg-cli.mjs`'s existing `pgCli()`, and always passes `-w` (never prompt) plus a bounded `PGCONNECT_TIMEOUT`. Updated the CLAUDE.md Database recipe and `scripts/db/README.md` to point agents at this wrapper instead of the bare `psql "$DATABASE_URL"` call that caused the hang. Verified live against the real local DB: `node scripts/db/psql.mjs -c "select 1"` and `-c "\dt"` both return real results with `$DATABASE_URL` unset in the shell; a deliberately bad `DATABASE_URL` now fails fast with a real `password authentication failed` error (exit 2) instead of hanging. Commit `3ee3ce85b`, rebased and pushed to `origin/main` as `27dccbd61`; `verify-branch-merged.mjs` confirms merged. `git status --porcelain` clean.
