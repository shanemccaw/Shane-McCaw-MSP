# #4526 — Build agents' psql calls hang silently, get auto-backgrounded

- **Status:** ⏳ IN FLIGHT 2026-09-17T17:10:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-17
- **Commit(s):** (fill at DONE)

## Log
- 2026-09-17T17:10:00Z ⏳ IN FLIGHT — Reproduced root cause in a scratch shell: this Bash tool's env has `DATABASE_URL`/`PGPASSWORD` genuinely unset (confirmed via `echo`), even though `.env.local` defines `DATABASE_URL` — the shell never sources it (and per CLAUDE.md, never should — sourcing `.env.local` leaks the MT cert PEM key). `psql "$DATABASE_URL" -c "select 1"` with `$DATABASE_URL` empty falls back to psql's default connection params (localhost/current-user), which requires a password; psql prompts for it directly against the controlling terminal (bypassing stdout/stderr redirection), and in this non-interactive tool that read blocks forever with zero captured output — exactly the empty-then-`[killed]` shape in the issue. Verified `timeout 5 psql "$DATABASE_URL" -c "select 1"` → empty output, exit 124 (timeout kill). Verified fix direction: `psql -w` (never prompt) fails fast instead — `psql: error: ... fe_sendauth: no password supplied`, exit 2, real error text captured normally.
