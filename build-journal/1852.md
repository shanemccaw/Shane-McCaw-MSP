# #1852 — ps-execution: per-cmdlet wall-clock guard so one hanging cmdlet no longer destroys every result in its request

- **Status:** ✅ DONE 2026-09-04T07:58:16Z
- **Scope:** platform
- **Started:** 2026-09-04
- **Commit(s):** 95404aa5c (implementation), bdedba516 (merge to main)

## Log
- 2026-09-04T07:51:47Z ⏳ IN FLIGHT — investigated #1852 body + both comments. Scope confirmed:
  per-cmdlet wall-clock guard only, inside `services/ps-execution/survey.ps1`'s batch probe loop
  (`Invoke-SurveyProbe` → `Invoke-SurveyCommandProbe`), the actual place a single child process
  runs many cmdlets in one request. `ps_capability_survey_results.status` is a plain TEXT column
  with no CHECK constraint (by design, see its own migration comment), so a new `timeout` status
  value is safe to introduce without a migration. Implementing next: a reusable
  `Invoke-CmdletWithWallClockTimeout` helper in `common.ps1` (isolated Runspace + async
  BeginInvoke/EndInvoke with a real timeout, `BeginStop` on timeout so the guard itself can never
  block past its own ceiling), wired into `Invoke-SurveyCommandProbe` behind a new
  `PS_EXECUTION_CMDLET_TIMEOUT_SECONDS` env var, clamped below `PS_EXECUTION_CHILD_TIMEOUT_SECONDS`.
- 2026-09-04T07:58:16Z ✅ DONE — implemented `Invoke-CmdletWithWallClockTimeout` in
  `services/ps-execution/common.ps1` (isolated Runspace, `BeginInvoke`/`EndInvoke` polled
  with a real wall-clock wait, `BeginStop` — never `Stop` — on timeout so the guard itself can
  never block past its own ceiling; imports the same already-loaded session module into the
  isolated Runspace by real file `.Path`, not bare name, since the dynamically generated
  `tmpEXO_*` module is never on `$env:PSModulePath`). Wired into
  `services/ps-execution/survey.ps1`'s `Invoke-SurveyCommandProbe`/`Invoke-SurveyProbe` behind
  new `PS_EXECUTION_CMDLET_TIMEOUT_SECONDS` (default 45s, clamped below
  `PS_EXECUTION_CHILD_TIMEOUT_SECONDS` with a 15s margin). A per-cmdlet timeout now records that
  one cmdlet as a new `timeout` status row (plain TEXT column in `ps_capability_survey_results`,
  no migration needed — schema already documents "must be able to record a new real outcome
  without a migration") and the batch continues; every other cmdlet's real result in the same
  request survives. Documented the new status in `docs/powershell-capability-survey.md`'s
  outcome vocabulary table.

  **Verification (real, in-session — pwsh 7 is available in this sandbox):** both edited files
  parse clean via `[System.Management.Automation.Language.Parser]::ParseFile`. Ran a full
  end-to-end `Invoke-SurveyProbe` batch against three throwaway fake cmdlets
  (`Get-FastThing`/`Get-ErroringThing`/`Get-HangingThing`, a 30s `Start-Sleep`) in one batch: the
  hanging one was abandoned at its clamped ~5s per-cmdlet ceiling (not the full 30s) and recorded
  `status=timeout`, while the OTHER TWO cmdlets' real `ok`/`access_denied` results landed intact
  in the same `Rows` array — the exact #1793 survey run #2 failure mode this issue reports,
  reproduced and fixed locally. Also verified: back-to-back calls after a timeout don't corrupt
  state (each call gets a fresh Runspace); the unguarded `-TimeoutSeconds 0` fallback path is
  unchanged; error messages unwrap `EndInvoke`'s `MethodInvocationException` wrapper to the real
  underlying exception/type so `Get-SurveyFailureStatus`'s classification still works correctly.
  **Not live-verified against a real EXO/Purview app-only session** — no Azure/Key-Vault
  reachability in this sandbox, the same standing limitation `entrypoint.ps1`'s own header
  already documents. Scope: per-cmdlet guard only, per #1852's dispatch — the
  incremental-result-protocol alternative it also names is explicitly out of scope here.

  Merged cleanly onto `origin/main` at `bdedba516` (fast-forward push, no conflicts against a
  concurrent `#1854` Test-* cmdlet judgment-pass commit already on main touching the same file).
  `verify-branch-merged.mjs` confirms ancestry; working tree clean.
