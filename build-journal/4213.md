# #4213 — Launch Control: resolveCustomerMonitoringTier reads earliest active client_services row of ANY service type

- **Status:** ✅ DONE 2026-09-15T00:45:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 228af1a88

## Log
- 2026-09-15T00:00:00Z ⏳ IN FLIGHT — replacing resolveCustomerMonitoringTier's unfiltered query in msp-launch-control.ts with a call to resolveCustomerTierEntitlement (portal-tier-features.ts, added by #4192), the correctly service_type-filtered shared resolver. Live-verifying with a synthetic customer whose earliest active client_services row is a non-monitoring service.
- 2026-09-15T00:45:00Z ✅ DONE — `resolveCustomerMonitoringTier` in `msp-launch-control.ts` now delegates to `resolveCustomerTierEntitlement` (portal-tier-features.ts); removed the now-unused `resolveCustomerUserIds`/`clientServicesTable`/`servicesTable` imports it had used directly. Live-verified with a synthetic customer (temp `tenants`/`users`/`client_services` rows, cleaned up after) whose earliest active `client_services` row was a non-monitoring `assessment` service followed by a real `premier` `monitoring_tier` row: old inline query returned NULL tier, `resolveCustomerTierEntitlement` correctly returned `currentTier: "premier"`. `tsc --noEmit` on api-server showed zero errors touching msp-launch-control.ts. Commit 228af1a88 pushed and confirmed merged into origin/main via verify-branch-merged.mjs. Working tree clean.
