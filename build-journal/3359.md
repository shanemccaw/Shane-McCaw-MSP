# #3359 — Home dashboard's GitHubIssueTimeSeriesService never migrated to the local mirror

- **Status:** ✅ DONE 2026-09-10T00:12:00Z
- **Scope:** buildconsole
- **Started:** 2026-09-09
- **Feature parent:** #1789 (Feature: Dev Server & Deploy Infrastructure)
- **Follows:** #3358 (Git Board tree → bt_issue_mirror; reused its mirror read helpers + columns)
- **Commit(s):** 4baf50a11 (code + migration), IN FLIGHT bookend earlier
- **Migration:** `lib/db/migrations/manual/2026-09-09-bt-issue-mirror-closed-backfill-3359.sql` (ran locally; appended to #1630)

## Log
- 2026-09-10T00:05:00Z ⏳ IN FLIGHT — Migrating `GitHubIssueTimeSeriesService.GetAllIssuesAsync`
  (Home dashboard's burndown/velocity/ETA charts) off its own live
  `ListBoardIssuesAsync(GitHubIssueState.All)` per-5-min walk onto the local mirror.

  **Verified the closed-history question the issue body flagged (query, don't assume):**
  `bt_issue_mirror` holds 773 rows — 500 open, only 273 closed, and of those closed only
  31 carry a real `closed_at` (242 are NULL, marked-closed by the full walk without a
  timestamp). `min(created_at)=2026-07-28`, `min/max(closed_at)` both `2026-09-09` — i.e.
  the mirror has essentially NO historical closed-issue data (real repo has thousands of
  closed issues; v1.1 alone is ~947/1430). `bt_milestone_mirror` is empty (0 rows) so
  #3365-comment option (b) isn't even available, and milestone header counts can't
  reconstruct a per-day burndown anyway (no per-issue timestamps). **A like-for-like
  migration would produce a badly-wrong series — worse than today's fail-closed.**

  **Fix (option a, the closed-inclusive mirror — real backend, not a fixture):** the mirror
  already has every column the time series needs (created_at, closed_at, milestone_number,
  parent_number, sub_issue_count, child_issue_numbers) and `TryGetBoardIssuesAsync(openOnly:false)`
  already reconstructs the full `GitBoardIssue` shape. The only missing piece is *populating*
  the closed set with real timestamps. Adding a rate-limit-conscious closed-issue backfill
  (one `ListBoardIssuesAsync(All)` walk, gated to run ~once/24h inside the full sync, NOT
  per-tick — replacing Home's own per-5-min All walk), plus a `HasClosedBackfillAsync` gate so
  the time series reads live until the closed set is genuinely present. Then
  `GetAllIssuesAsync` reads mirror-first, falls back to live.
- 2026-09-10T00:12:00Z ✅ DONE — Shipped in `4baf50a11` (on origin/main; branch verified merged, exit 0).

  **What shipped:**
  - **Migration** `2026-09-09-bt-issue-mirror-closed-backfill-3359.sql` — adds
    `bt_issue_mirror_sync_state.last_closed_backfill_at` (backfill gating + reader gate). NO new
    `bt_issue_mirror` columns needed (created_at/closed_at/milestone/parent/sub-issue all present
    from #3358). Ran against local `DATABASE_URL` in-session; verified column present. Appended to
    release-gate #1630 for Replit/staging.
  - **`GitHubIssueMirror.MaybeBackfillClosedIssuesAsync`** — one `ListBoardIssuesAsync(All)` walk,
    keeps only closed issues, upserts them with real created/closed timestamps + Git Board display
    fields; deliberately preserves board_status/blocked_by/blocking (full walk owns those) and never
    touches open rows (Git Board #3358 openOnly reads unaffected). Gated to ~once/24h
    (`ClosedBackfillInterval`), circuit-open aware, fail-closed (skips/aborts without recording on any
    failure). Wired into `MaybeSyncAsync` after a SUCCESSFUL full sync, inside the existing
    single-flight guard. Plus `HasClosedBackfillAsync`/`GetClosedBackfillAtAsync` + `RecordClosedBackfillAsync`.
  - **`GitHubIssueTimeSeriesService.GetAllIssuesAsync`** — reads open+closed from the mirror
    (`TryGetBoardIssuesAsync(openOnly:false)`) gated on `HasUsableDataAsync && HasClosedBackfillAsync`;
    any miss/error falls through to the existing live ALL walk; forceRefresh (manual refresh) still
    goes live. Retires Home's per-5-min live ALL walk on the common hit.

  **Verification (BuildConsole is out of scope for test manifests — clean build + real SQL, per CLAUDE.md):**
  - `dotnet build BuildConsole.csproj -c Debug` → **Build succeeded, 0 Warning(s), 0 Error(s)**.
  - Backfill INSERT…ON CONFLICT (exact column list) + the openOnly:false read query validated against
    the real local schema inside a **rolled-back transaction** — column/type alignment correct, read
    back correctly, **0 rows after ROLLBACK** (no data pollution).
  - `last_closed_backfill_at` confirmed NULL → `HasClosedBackfillAsync` false → the time series stays
    on live until the first real backfill lands (fail-closed, as designed).
  - Did NOT launch BuildConsole.exe (single-instance mutex can courier into Shane's live session —
    memory `never-launch-buildconsole-exe-for-verification`). Live circuit-open behavior is covered by
    the same fail-closed structure #3358 uses.

  **Behavior note (honest):** between 24h backfills, a just-closed issue is captured within ~5 min by
  the incremental `since=` pass (which sets its real closed_at); an issue the full-walk mark-closed
  pass flips before incremental catches it is briefly `closed` with `closed_at` NULL, which BuildSeries
  treats as still-open until corrected — a minor, self-correcting transient, far better than the prior
  full fail-closed.

  **Findings filed:** none new — the closed-history gap this issue names was already tracked and is
  what this build fixed. #3365 (the ALL-states rollup finding filed by #3358) is materially addressed by
  this same closed-inclusive mirror.

  **Working tree:** clean (`git status --porcelain` empty apart from this bookend). Only my 3 files touched.
