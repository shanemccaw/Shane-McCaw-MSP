# #1673 — Portal scaffolding: create artifacts/portal/ and retire artifacts/msp-portal

- **Status:** ✅ DONE
- **Scope:** both
- **Started:** 2026-08-29
- **Commit(s):** 539386aae (scaffold + retire), 13a0bd443 (scrub stale comments), rebased onto main as of push a7cd49c1b

## Log
- 2026-08-29 ⏳ IN FLIGHT — Verified archive tag `portal-archive-2026-08-29` (157 page files) and confirmed `@workspace/msp-portal` is a leaf (only its own package.json matches). About to delete `artifacts/msp-portal` and scaffold `artifacts/portal` (`@workspace/portal`), port the live hooks, and update dev-server + BuildConsole config touchpoints.
- 2026-08-29 ✅ DONE — Deleted `artifacts/msp-portal` wholesale (`git rm -r`, ~940 files; preserved at the tag). Scaffolded `artifacts/portal` = `@workspace/portal`: Vite + React 19 + Tailwind v4 + shadcn/ui (new-york) + lucide-react + wouter. New `package.json`, `vite.config.ts`, `tsconfig.json` (`@/*` alias), `index.html`, `src/main.tsx`, `src/App.tsx` (QueryClient + Auth + Theme + Slug providers, an auth guard, a layout, and only a placeholder index + not-found routed — nothing else), `src/components/layout.tsx`, `src/pages/index.tsx`, `src/pages/not-found.tsx`, `components.json`, `.replit-artifact/artifact.toml`, `src/index.css`.

### Verification
- `vite build` (via a temporary node_modules junction from main's msp-portal deps — see the pnpm note below): **PASS**, 1742 modules transformed, dist/public emitted. Junction removed afterward; main's deps confirmed intact.
- `grep -rn 'Data"' artifacts/portal/src/` → **zero** fixture imports.
- `grep msp-portal` across `scripts/`, `desktop/BuildConsole/*.cs`, and `artifacts/portal/` → clean (all live config repointed; comments scrubbed). Remaining `msp-portal` hits are the unrelated MSP-portal API feature (api-server routes/links), dead portal-v2 test-manifests (owned by the page-rebuild issues), and `pnpm-lock.yaml` — all historical/out-of-scope.
- `verify-branch-merged.mjs` → OK, `agent/1673-q821` merged into origin/main.

### Step 5 — live hooks ported (17 hooks + 5 support files, all fixture-free)
Ported flat into `src/components/`: billingLive (+billingWire), subscriptionsLive (+subscriptionsWire), govOversharingRunbooksLive, govOversharingSitesLive, oversharingItemsLive, remediationLive, useCaBaselineLive, useHltDriftLive, useHltObjectsLive, useMfaRegistrationLive, useSecAreaLinksLive, useSecEvidenceEmailLive, useSecEvidenceLegacyAuthLive, useSecEvidenceOauthLive, holds/useHoldBadge, holds/useRunbooks, shell/useSelectionAsk. Support (kept at `@/`-alias paths): health-suite/useTopicHealthLive, assessment-test/types, copilot-journey/useRemediationTracker. Lib infra: auth-context, report-client-event, utils, slug-context, theme-context. `remediationLive`'s `../copilot-journey/` import rewritten to `@/components/...` after flattening.

**Hooks deliberately LEFT OUT** (preserved at the tag; each will be re-homed with its real data layer during the per-page rebuilds under #1648–1671). They cannot be carried without dragging a `*Data` fixture or the retired `copilot-scan-scene` lib:
- Direct `*Data` import: alertPrefsLive, complianceObligationsLive, projectsLive, riskRegisterLive, securityPlanLive, shell/useShaneBot, useAccountSecurityLive, useAdpWorkloadsLive, useChangeControl, useGovAreaLinksLive, useMessageCenter, useRetainerLive, useSops, webhooksLive.
- Transitive contamination: receiptLive (receiptModel/receiptWire → Data), useRemediationPillarScores (remediationScores → remediationData), piiGovernanceLive + usePiiExposedBadge (piiGovernanceWire → riskRegisterWire → Data), usePortalV2Pillars + useLivePillarHero (portalV2Model → copilot-journey/journeyTokens → retired `@workspace/copilot-scan-scene`).

### Step 6 — workspace lib deps
Kept only `@workspace/api-client-react`. Dropped `copilot-scan-scene` (retired, #1674), `dashboard-canvas`, `debug-console`, `json-viewer` — no ported hook imports any of them. Also removed the `@source "../../../lib/json-viewer/src"` line from index.css and the @fluentui / react-grid-layout / react-resizable npm deps that only backed retired surfaces.

### Step 7 — config touchpoints repointed msp-portal → portal
`scripts/dev-all.mjs`, `scripts/dev-server/refresh-main-server.mjs`, `scripts/dev-server/service-targeting.mjs` (name + `artifacts/portal/`), `scripts/dev-server/server-process.mjs`, `scripts/dev-server/selftest.mjs`, `scripts/dev-server/README.md`, `scripts/audit-portal-fidelity.mts` (path strings only — script is otherwise dead, see below). BuildConsole C#: `DevServicesManager.cs`, `DevServiceRouting.cs`, `MainWindow.xaml.cs` (×3).

### Follow-ups / honest gaps
- **BuildConsole must be recompiled by Shane.** The C# strings are updated but the running WPF app hardcodes the old values until rebuilt — dev-server integration for the portal service is stale until then.
- **`pnpm-lock.yaml` still names `@workspace/msp-portal` and lacks `@workspace/portal`.** A `pnpm install` at the **main** repo root (a real TTY) is needed to relink and regenerate the lock. It was NOT run here: the worktree's root `node_modules` is a junction to the main checkout, and pnpm wanted to purge the modules dir — which would have destroyed main's shared deps. The build was validated via a temporary junction instead.
- `scripts/audit-portal-fidelity.mts` is now dead (its `SRC` and the retired `Design/design_handoff_customer_portal/` inputs are gone); path strings were repointed only to keep the grep clean. Deleting it was blocked by the tool classifier; recommend removal in a follow-up.
- Dead `test-manifests/portal/portal-v2-*.json` still describe the retired pages — out of #1673 scope; the page-rebuild issues (#1648–1671, discover-before-create) own them.
