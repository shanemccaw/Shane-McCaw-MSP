# #1565 — Security Plan: Generated artifacts carry a filter footprint

- **Status:** ✅ DONE
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** none — no code changed this session (bookend commits only; see log)

## Log
- 2026-08-31 ⏳ IN FLIGHT — Pulled #1565 body + comments and the full architecture chain
  (#1561 foundation, #1562 RBD pattern, #1508 precedent, #1563 scope constraint).
  Investigating whether the filter footprint requirement is already satisfied by #1561's
  assembled-view commit (`security-plan-assembly.ts` / `msp.ts`'s `SecurityPlanFilterFootprint`)
  and #1563's follow-up (`dbb20eefb`), or whether a real gap remains.
- 2026-08-31 ✅ DONE — No code changed. Investigation confirmed #1565's requirement was
  already fully built, across three prior commits on this same issue chain:
  - `46ff73e92` (#1561) added `SecurityPlanFilterFootprint` (`lib/db/src/schema/msp.ts`) —
    `scope` (filters applied), `excludedByModule` (what was excluded, per module) and
    `totalExcluded` (count) — computed by `applyScopeAndFootprint`
    (`security-plan-assembly.ts`) for every assembly, honest or scoped, and baked into
    `SecurityPlanContent` as a non-optional field, so no wire response (live `GET
    .../assembled` or sealed `POST .../versions`) can omit it — structurally
    un-suppressible by any future filter UI.
  - `dbb20eefb` (#1563) closed the one real gap found in that area: a scoped seal could
    go through with no human `scope.statement`. Added `scopeMissingRequiredStatement`,
    enforced as a 400 on `POST /api/msp/security-plan/:customerId/versions`.
  - `4ae74aa27` (#1564) closed the follow-on gap: an unscoped/honest seal's
    `footprint.scope.statement` could be blank. Added `synthesizeScopeStatement`, always
    producing a bounded, non-empty statement.
  - Re-verified myself rather than trusting the prior sessions' claims: read
    `security-plan-assembly.ts`, `msp-security-plan.ts` and the `msp.ts` type definitions
    end to end; confirmed `msp_security_plan_versions` exists on the local DB with the
    exact columns Drizzle declares (`psql`); reran the real test suite —
    `npx vitest run src/lib/security-plan-assembly.test.ts src/lib/security-plan-drift.test.ts`
    → 18/18 pass. No `artifacts/portal` page or `Design/portal/` export exists for this
    module (confirmed by grep) — SCOPE STOP correctly has nothing to wire.
  - No finding filed: no gap found. `git status --porcelain` clean throughout;
    `node scripts/dev-server/verify-branch-merged.mjs` not needed — commit `e714f6f28`
    (the bookend) was pushed straight to `origin/main` via rebase, confirmed by
    `git log origin/main` after push.
  - Not closing #1565 myself, per standing convention.
