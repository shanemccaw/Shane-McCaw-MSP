# Build Journal — #4267

- **Status:** ✅ DONE 2026-09-15T00:45:00Z
- **Scope:** platform
- **Log:**
  - 2026-09-15T00:05:00Z — Started. Investigating azure-automation.ts usages in workflow-executor.ts and client-script-sequence.ts per #4267.
  - 2026-09-15T00:45:00Z — ✅ DONE — Removed the dead `isAzureConfigured()`/`resolveScriptById()` Azure Automation branches from `workflow-executor.ts` (`execute_runbook`, `update_m365_profile`, `validate_m365_permissions`) and rewrote `client-script-sequence.ts` (confirmed zero callers anywhere in the codebase). All replaced with an explicit "has no server-side execution binding" failure, matching #4262's 422 message pattern, instead of the old dead-Azure-branch behavior. Deleted `azure-automation.ts` and its 5 now-unused test mocks (`workflow-executor-{comms,content,core,integrations,generate-script}.test.ts`). Added 8 new live tests in `workflow-executor-integrations.test.ts` asserting the new explicit error messages for all three node types. `resolveScriptById` confirmed to have had exactly one caller (`resolveExecuteRunbookId`), itself only called from the now-removed `execute_runbook` single-runbook path — so it was already unreachable-but-present dead code, not a live regression risk. Verification: `tsc --noEmit` clean (exit 0); full workflow-executor test suite (286 tests across 5 files, including the 8 new ones) passes. Commit `c38dfef69`, rebased and pushed to `origin/main` as `26b4dc709`. Working tree clean; `verify-branch-merged.mjs` confirms merged. No findings filed — this was a clean, contained decommission with no surprises.
