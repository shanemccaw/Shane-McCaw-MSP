# #1567 — Security Plan: Divergence flagging — what the platform can and cannot detect

- **Status:** ✅ DONE 2026-09-01T00:02:10Z
- **Scope:** platform
- **Started:** 2026-08-31
- **Commit(s):** 6c68c136ae20af92ac6e9a8416728f7fb6a13ab9 (real post-push hash on origin/main, verified via `verify-branch-merged.mjs`)

## Log
- 2026-08-31T23:42:08Z ⏳ IN FLIGHT — Pulled #1567's body + comments and the full architecture chain (#1561, #1562, #1563, #1565, #1508). Investigating whether #1562's live-vs-last-signed drift build already satisfies #1567's requirement (flag data drift mechanically; never assert authored prose is stale/accurate), or whether a real gap remains.
- 2026-09-01T00:02:10Z ✅ DONE — Investigation found #1562's `computeSecurityPlanDrift` already satisfies #1567's requirement structurally: it only ever reads `SecurityPlanContent.modules` (real source-table rows), never `.prose` — the diff is mechanical data drift only, and nothing in the codebase asserts prose is stale, current or accurate. No schema/migration/route change was needed.

  Closed the gap of that guarantee having no test lock and no explicit doc reference to #1567: added a regression test (`security-plan-drift.test.ts` — "prose differing between live and last-signed produces no drift") proving prose changes alone, with every module's data identical, never produce a drift entry; added doc comments to `security-plan-drift.ts`'s header and the `/drift` route in `msp-security-plan.ts` making the #1567 guarantee explicit for future readers.

  While verifying with a worktree-local `@workspace/db` alias override (#2121 pattern — this worktree's `node_modules`/`lib/*/dist` junction to the main checkout, whose working copy is stale at `46ff73e92`/#1561, so a plain `tsc`/`vitest` run resolves `@workspace/db` to that stale schema instead of this worktree's own), found and fixed one real unrelated bug: the drift test's `content()` helper built a `SecurityPlanFilterFootprint.scope` missing the `statement` field #1564 made required — invisible under the stale resolution, real under the correct one. Fixed in the same commit.

  Rebased twice over concurrent landings (#1565 DONE, #1566 DONE — the latter adding `security-plan-prose.test.ts`) with one real merge conflict in the shared `content()` test helper (resolved by hand, keeping the merged formatting and my `prose` parameter).

  **Verification (all observed this session):** `npx vitest run` on `security-plan-drift.test.ts` + `security-plan-assembly.test.ts` — 19/19 pass under plain resolution. Under a worktree-local `@workspace/db` alias override (both `tsconfig` for `tsc` and `vitest.config` for `vitest`, both scratch/uncommitted, deleted after use): `tsc --noEmit` zero errors on every Security Plan file (66 pre-existing errors are the known #1765/#1772/#1774/#1777/#1778/#1781 class, all in unrelated files — `onedrive-sharing.test.ts`, `ai-cost-capture.test.ts`, etc., none touching `security-plan-*`); `vitest run` across all three Security Plan test files (drift + assembly + prose) — 28/28 pass, confirming `security-plan-prose.test.ts`'s 9 failures under plain resolution are the same known staleness artifact, not a real #1566 bug. `psql` against local `DATABASE_URL`: `msp_security_plan_versions` at 0 rows (honest — nothing sealed locally yet, matches #1561/#1562/#1563/#1564's own reports). `git status --porcelain` clean throughout; no scratch files left behind.

  No findings filed — the gap closed was #1567's own stated scope (locking an existing guarantee with a test + docs) plus a small, directly-adjacent test-helper fix, not an out-of-scope discovery.
