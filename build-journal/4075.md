# #4075 — Build Queue card: highlight border when item.Note is set

- **Status:** ✅ DONE 2026-09-14T15:32:00Z
- **Scope:** buildconsole

## Log
- 2026-09-14T15:23:33Z — Investigated `BuildQueueCard` in `Controls/BuildQueuePanel.xaml.cs` (~line 5050). Confirmed no `hasNote` branch exists in the `cardBorderColor` cascade; the 📝 badge (Git #3742, ~line 5699) uses amber `#F9E2AF`. Added `bool hasNote` and inserted it into the cascade right after `isBlocked` and before `VerifyingStatus`, using muted gold `#C9A06B` (same hue family as the badge, lower saturation so it doesn't compete with `isWaitingForInput`'s vivid amber). Deliberately scoped to `cardBorderColor` only — `cardBgColor` and `BorderThickness` untouched per the issue's literal ask.
- 2026-09-14T15:32:00Z — ✅ DONE — Commit `9466753c1` (code) pushed to `origin/main` as `f6033eca8` after rebase. `dotnet build desktop/BuildConsole/BuildConsole.csproj` succeeded clean (0 warnings, 0 errors). `verify-branch-merged.mjs` confirms `agent/4075-q2750` is an ancestor of `origin/main`. `git status --porcelain` clean. Color choice: muted gold `#C9A06B` for the `hasNote` border, my judgment call within the issue's "low-tone/muted, not vivid" guidance.
