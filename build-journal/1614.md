# #1614 — Key Vault cert `mt-app-cert` / app registration sync — verify only

- **Status:** ✅ DONE — verified live, no code/config changes made (read-only)
- **Scope:** platform
- **Started:** 2026-08-29
- **Commit(s):** 8dcc1b784 (bookend only; this is a verification-only session, no app
  registration or Key Vault writes were made)

## Log
- 2026-08-29 ⏳ IN FLIGHT — Shane reports the cert sync was done by an agent after he
  granted permission, but nothing was recorded on #1614 (no comment, no bookend, no
  commit). Read-only verification pass: check current Key Vault `mt-app-cert` thumbprint
  vs app `9ea2e409-d1b9-422a-8451-02fa0b98d1c3` registered thumbprint(s), then run live
  `shaneapp://executeCmdlet` against both EXO session types (compliance/get-connection-info,
  exchange/get-transport-rules) and report the real envelope. No app registration or Key
  Vault writes will be made by this session.
- 2026-08-29 ✅ DONE — Confirmed live via `az`: Key Vault secret `mt-app-cert` is
  unchanged at `251BDCD5895FB127F44648F29999F3B1820CA2D3` (`CN=ShaneMcCawMSP-MT-App`,
  SHA1 verified directly from the fetched PEM). App `9ea2e409-d1b9-422a-8451-02fa0b98d1c3`
  now has **both** thumbprints registered — the original `BC8F5C26BA1FA641C15264278EDCDA8F0F481906`
  (`CN=ShaneMcCawMSP-MT-App-2026`) plus `251BDCD5895FB127F44648F29999F3B1820CA2D3`
  (`CN=ShaneMcCawMSP-MT-App`, `startDateTime: 2026-08-29T03:57:31Z`) — the additive
  Option B path the issue described (`az ad app credential reset --append`), matching
  #1483's own log of applying the identical fix for the same shared app-only cert.
  Live `shaneapp://executeCmdlet` against testbed tenant
  `c4c814d4-3afe-441e-9145-62461d0a4fd3` (`mccawsoft2.onmicrosoft.com`):
  - `compliance` / `get-connection-info` (`Connect-IPPSSession`) → **`ok:true`**,
    1 item, real `State: "Connected"`, `CertificateAuthentication: true`,
    `AppId: 9ea2e409-...`, 29.7s (cold connect).
  - `exchange` / `get-transport-rules` (`Connect-ExchangeOnline` + `Get-TransportRule`)
    → **`ok:true`**, 7 items, real transport-rule objects with full properties
    (`Priority`, `State`, `Conditions`, `Actions`, `Description`, etc.), 7.2s.
  Both were HTTP 502 `auth_failed` before the sync per the issue; both now return real
  connected sessions with real data, not just a bare successful connect. AADSTS700027
  is resolved. #1481's real `extracted_properties` shape is now capturable — the
  `get-transport-rules` run above is a real, non-fabricated sample of it.
