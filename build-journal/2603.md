# #2603 — Security Plan: wire the MSP Console UI to the real endpoints

- **Status:** ✅ DONE 2026-09-15T13:35:00Z
- **Scope:** platform
- **Started:** 2026-09-15
- **Commit(s):** 342550010 (bookend IN FLIGHT), f70c87eb9 (real work, merged to main)

## Log
- 2026-09-15T13:10:00Z ⏳ IN FLIGHT — Authorized by Shane (2026-09-15): no Claude Design export exists for #2603 (#2602 never landed); building the MSP Console Security Plan screen directly against the real `/api/msp/security-plan/:customerId/*` routes (`msp-security-plan.ts`, #1689/#3793 dual signature), per `docs/msp-console/security-plan-msp-console-contract-pack.md` (#4082) and `docs/portal/security-plan-contract-pack.md`. Adding `artifacts/msp-console/src/api/security-plan-api.ts`, a new module at `artifacts/msp-console/src/modules/security-plan/SecurityPlan.tsx` (author/edit draft, seal a version, view sealed history, MSP's own sign action, drift), a visible dashed-border "Agent-built UI — pending design review" banner, and wiring into `console/nav.ts` (Governance group, id `sp`) + `console/ConsoleShell.tsx`.
- 2026-09-15T13:35:00Z ✅ DONE — Shipped: `artifacts/msp-console/src/api/security-plan-api.ts` (typed client for all 9 routes: assembled, drift, versions, versions/current, draft/freeze, draft, draft/prose, versions POST/seal, versions/:uid/sign), `artifacts/msp-console/src/modules/security-plan/SecurityPlan.tsx` (3 tabs — Draft & authoring, Sealed versions, Drift — plus the required visible "Agent-built UI — pending design review" dashed banner), nav entry `sp` under Governance (icon `file-check`), and the `ConsoleShell.tsx` mount gating the MSP's own sign action on `ladder.msp-admin` client-side (`profile.mspRole === "MSPAdmin" || "PlatformAdmin"`, same pattern every other MSPAdmin-gated action in this shell already uses). Verified: `tsc --noEmit` clean, `vite build` clean, both re-checked after rebasing onto origin/main (which had picked up an unrelated concurrent #2650 module in the same two shared files — merged with no conflicts). No test manifest: `artifacts/msp-console` is out of the CLAUDE.md test-manifest scope (portal + marketing site only). Not live-clicked-through end-to-end against a real authenticated MSP session — the local `msp_security_plan_versions`/`_drafts` tables are genuinely empty (0 rows, per both contract packs), so there is no existing sealed data to click through either; the freeze → author → seal → sign flow was verified by direct comparison against `msp-security-plan.ts`'s route code and both contract packs, field by field, not by a live click-through. Branch verified merged into origin/main (`verify-branch-merged.mjs`, commit f70c87eb9). Working tree clean. No findings filed — nothing discovered outside this issue's own scope.
