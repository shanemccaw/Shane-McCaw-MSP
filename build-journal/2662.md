# #2662 — Wire Staff Roster into artifacts/msp-console

- **Status:** ✅ DONE 2026-09-14T04:45:00Z
- **Scope:** platform
- **Started:** 2026-09-14
- **Commit(s):** ca7c4ab80 (IN FLIGHT bookend), a6c2faf59 (Staff Roster implementation, post-rebase tip)

## Log
- 2026-09-14T04:05:00Z ⏳ IN FLIGHT — Design export confirmed present (`Design/MSP_Console/design_handoff_msp_console/Staff Roster.dc.html`, README screen 65). Read pack + `docs/msp-console/msp-staff-roles-and-onboarding-contract-pack.md` in full. Building the real page against `msp-settings.ts`'s 14 staff/invite routes + reusing shared session/status hooks from `account-security-api.ts`. Mounting under Operations (`nav.ts` `MSP_PAGES`, id `staff`), rendered from `ConsoleShell.tsx`.
- 2026-09-14T04:45:00Z ✅ DONE — Shipped `artifacts/msp-console/src/api/staff-roster-api.ts` (new) and `.../console/modules/StaffRoster.tsx` (new), wired into `nav.ts` (`MSP_PAGES` id `staff`) and `ConsoleShell.tsx` (`/ops/staff`). Widened `account-security-api.ts`'s `AccountSecurityUser` type with `canApprovePurchases`/`approvePurchasesGrantable`/`assignedCustomersCount`, which the live roster route already returns (confirmed by reading the current route code — the #2460/#3570 purchase-approval migration moved it off a plain column onto a grant-role read, still same wire field names) but no prior consumer typed. All 19 routes from the contract pack cross-checked line-by-line against the current `msp-settings.ts` (auth is now `requireCapability("ladder.msp-admin")`, superseding the pack's `requireRole` citation — same effective gate). No fixture/mock data anywhere. Verification: `pnpm run typecheck` and `pnpm run build` both clean in `artifacts/msp-console`; minted a local JWT for the real MSPAdmin account `shanemccaw+mspadmin@outlook.com` (user 2760, MSP 1) and round-tripped every route live against the local dev api-server + database — roster (with the real `canApprovePurchases`/`assignedCustomersCount` fields), customer-scopes GET, sessions GET, and a full invite POST → GET → DELETE cycle, all matching the UI's exact request/response shapes. `shaneapp://runTest` is not exposed as a callable tool in this session, so no in-app WebView2 screenshot was taken; the live API round-trip above is the real substitute. msp-console is not in the test-manifest scope (CLAUDE.md restricts that to `artifacts/msp-portal`/`artifacts/shane-mccaw-consulting`), so no manifest was written. Branch verified merged into `origin/main` (`node scripts/dev-server/verify-branch-merged.mjs`); working tree clean. No new findings filed — every route matched the pack with only the expected `requireRole`→`requireCapability` and purchase-approval-grant-model drift, neither of which is a defect.
