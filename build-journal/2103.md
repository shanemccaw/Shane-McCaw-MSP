# #2103 — Track dispatch count per issue; Windows toast warning on re-dispatch (threshold 2, before launch)

- **Status:** ✅ DONE
- **Scope:** buildconsole
- **Started:** 2026-08-31
- **Commit(s):** 72968dcd6 (feature), c44860739 (IN FLIGHT bookend)

## Log
- 2026-08-31 ⏳ IN FLIGHT — starting real audit: queue launcher call site, project-board column-transition history source, existing ToastEngine/ToastHostWindow mechanism.
- 2026-08-31 ✅ DONE — Real audit confirmed all three unknowns before writing code:
  1. **Dispatch call site:** `QueueWatcherService.LaunchItem` — the actual `RedirectedProcessLauncher.Launch` process spawn, not `QueueBuildAsync` (which only adds/dedups a `bt_build_queue` row).
  2. **Board column-transition history:** GitHub's real `issue.timelineItems(itemTypes: [PROJECT_V2_ITEM_STATUS_CHANGED_EVENT, CLOSED_EVENT])` — confirmed live via `gh api graphql` introspection and a real query against #2103 and #1867 (the pain-point issue named in the body) before writing any C#. No such history read existed anywhere in the app.
  3. **Toast mechanism:** reused `ToastEngine.Warning` as-is — no new toast system.

  Shipped: new `build_dispatch_log` table (`issue_number`, `queue_item_id` FK → `bt_build_queue(id)`, `dispatched_at`, `session_id`, `outcome`) — Drizzle schema (`lib/db/src/schema/index.ts`) + manual migration `lib/db/migrations/manual/2026-08-31-build-dispatch-log-2103.sql`, run myself against local `DATABASE_URL` (confirmed: `CREATE TABLE`, `CREATE INDEX`, self-mark insert all succeeded; `\d build_dispatch_log` confirms real FK). Recorded on #1630's standing migration checklist.

  `BuildQueuePostgresClient`: `LogDispatchAsync` (INSERT at dispatch), `CountDispatchesSinceAsync` (count since a boundary timestamp), and `MarkCompleteAsync` extended to mirror the terminal status (`done`/`verifying`/`failed`) back onto the dispatch row via `queue_item_id` — no fragile in-memory correlation map, survives an app restart mid-build.

  `GitHubIssuesService.GetLastLeftBatterUpFamilyAtAsync`: walks the real timeline to find the most recent time the issue left `{Batter Up, AI Batter Up}` into `{Backlog, Park}`, or was closed — exactly the boundary the issue body specifies ("last closed OR moved to Backlog/Park"), not a raw all-time count. A properly closed-and-reopened issue correctly does not carry its old count forward.

  `QueueWatcherService.LaunchItem`: right before the process spawns (never for a Reply/`--resume` continuation — that picks up the same session, not a fresh dispatch), calls the new `TrackDispatchAsync`, which checks the count and fires `ToastEngine.Warning("Re-dispatching #N", "{ordinal} time — no progress since last run.")` BEFORE launch when the count is ≥1, then writes the dispatch row. Fails open (a GitHub/DB hiccup logs and lets the build launch — this is a heads-up toast, not a launch gate).

  **Verification:** `dotnet build BuildConsole.csproj` — 0 warnings, 0 errors. `tsc --noEmit -p lib/db/tsconfig.json` — clean. Live simulation against the real local DB + real GitHub data: seeded a prior dispatch row for #2103 (currently sitting in "Batter Up" since 2026-08-31T15:45:02Z, never left the family) and confirmed the exact `CountDispatchesSinceAsync` SQL returns 1 (count-with-NULL-boundary correctly counts all-time). Separately, using #1867's REAL board history (moved to Backlog at 2026-08-31T15:38:00Z), seeded one dispatch row before that boundary and one after, and confirmed the exact SQL only counts the post-boundary row (1, not 2) — proving the "since it last left the family" logic is genuinely boundary-aware, not a raw count. Verification rows deleted afterward (table left empty of test data). `git status --porcelain` clean; branch verified merged into `origin/main` via `verify-branch-merged.mjs` before writing this DONE.

  **Scope note:** the completion write-back only covers the direct-Postgres path (`_db`, the app's primary/default mode per its own doc comment — "the Neon Postgres server is always on"). The legacy HTTP-fallback path (`_api`, only used when `_db` is unavailable) is not extended to update `build_dispatch_log`'s outcome column — not filed as a separate finding since it's a minor scoping call on my own new feature (dispatch tracking still works fully in that mode; only the outcome/session_id backfill on completion would be skipped), not a pre-existing bug.
