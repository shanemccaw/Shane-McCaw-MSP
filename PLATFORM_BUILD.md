# Platform Build Log

Append-only log of work sessions. Each session adds exactly one row, in two edits:

1. **At session start** (before any real work): append a row with status `⏳ IN FLIGHT — {step name}`, commit it immediately as its own standalone commit.
2. **At session end** (after the real work is done, tested, and typechecked): update that same row to `✅ DONE — {step name}`, filling in the real commit hash of the work, then commit the update (either folded into the final commit or immediately after it).

If a session dies, crashes, or is abandoned mid-way, its row stays `⏳ IN FLIGHT`, which is itself the record that an attempt was made and didn't finish.

See [CLAUDE.md](CLAUDE.md) for the exact instructions.

History before 2026-08-12 lives in [PLATFORM_BUILD_ARCHIVE_2026-08-12.md](PLATFORM_BUILD_ARCHIVE_2026-08-12.md) (Git #948). Pull that file for prior sessions' landed implementations, gotchas, and decisions.

| Date | Status | Step | Commit |
|------|--------|------|--------|
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — `UiTestExecutor` captureResponse content-read timing: kick off the response body read IMMEDIATELY inside the `WebResourceResponseReceived` handler (store the in-flight read `Task`, not the raw event args) rather than deferring it until after the outer capture `Task.WhenAny` resolves, and log the real elapsed time between the event firing and when the read is actually attempted. Follow-on to #1011's already-landed content-read timeout guard (`0b1fe5e9`). VERIFICATION GATE (per task): checked against real Microsoft WebView2 docs before committing to the fix shape — the "must read synchronously in-handler" theory is NOT a documented constraint (MS Learn `GetContentAsync` Remarks explicitly permit deferred reads; the documented hang cause is blocking stream reads awaiting data that never arrives, already guarded by #1011), so this is framed as a low-risk DEFENSIVE + DIAGNOSTIC change, not a confirmed root-cause fix. IMPLEMENTATION: the capture handler now guards on first-match and starts `ReadResponseBodyAsync(args, firedSw, stepNumber)` synchronously the instant the matching response arrives (so `GetContentAsync()` is invoked inside the event), storing the in-flight read `Task` on a new `CapturedResponse` payload (Args + ReadTask + event-fired `Stopwatch`) on the TCS instead of the raw event args; the awaiting code simply `await`s `captured.ReadTask`. LOGGING (all on `testing.ui-executor`): handler logs "starting content read immediately in-handler (deferral eliminated)"; `ReadResponseBodyAsync` logs "entered Nms after the WebResourceResponseReceived event fired" (the real event→read-attempt elapsed, ~0ms on this path); the consume site logs "main flow resumed Nms after the event fired". #1011's `GetContentAsync`/`ReadToEndAsync` `Task.WhenAny` timeout guards are preserved untouched. Built clean in-place (`dotnet build -t:Rebuild`, 0 errors; only the 2 pre-existing unrelated CS0067 `Start/StopRecordingRequested` warnings — no new warnings from the change). No DB. No test manifest (desktop/BuildConsole is outside the test-coverage scope). Epic #803 is the umbrella (never flipped complete) → no per-issue label sync; referenced #1011 is already CLOSED and left untouched. Not GUI-live-verified (can't launch Shane's running BuildConsole here) — on the next uiSteps run with a captureResponse, confirm a "starting content read immediately in-handler" + "entered ~0ms after the event fired" log pair appears; if a body-read hang ever recurs, that logged elapsed number is the real data for the next investigation. | `55428a6c` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — `UiTestExecutor.ReadResponseBodyAsync` content-stream read hang guard. Symptom class: after a step's captureResponse wait resolves with a matched response, the body is read via `args.Response.GetContentAsync()` then `new StreamReader(stream).ReadToEndAsync()` — both `await`ed UNGUARDED, so a WebView2 content stream that opens but never finishes delivering (or `GetContentAsync` that never returns) deadlocks the run with no further log output, exactly like the #803 screenshot/captureResponse hangs. **Checked the follow-up the task called out: yes, `ReadToEndAsync()` (the line right after `GetContentAsync`) was equally unguarded — so BOTH awaits are now wrapped**, not just the first. Each is guarded with the same `Task.WhenAny(work, Task.Delay(ResponseBodyReadTimeoutMs))` shape navigation (15s)/captureResponse (5s)/screenshot (#995, 8s) already use; new `ResponseBodyReadTimeoutMs = 5000` (scaled to captureResponse's 5s since it reads that same captured response, inside the task's 5-8s range). On either timeout the method returns a clear `(null, "GetContentAsync timed out after 5000ms")` / `(null, "ReadToEndAsync timed out after 5000ms")` result — the caller (`_ , bodyReadError) = await ReadResponseBodyAsync(...)` at the post-capture path) surfaces it as a body-read failure and the step completes instead of the run awaiting indefinitely. Orphaned-task safety: on the GetContentAsync-timeout path a late/never stream is disposed + its fault observed in a `ContinueWith`; on the ReadToEndAsync-timeout path the in-flight read owns reader+stream disposal via a `ContinueWith` (never torn out from under a pending read), so no leak and no unobserved-task exception. Diagnostic logging is the deliverable, not optional — stage-specific `ActivityLog.Log(Channel="testing.ui-executor", …)` on GetContentAsync-resolved (real elapsed ms), null-stream, each timeout (stating "Task.Delay won the race at Xms" — which outcome won), body-read-complete (elapsed + char count), and catch, matching #995's stopwatch/which-outcome-won pattern. No DB. No test manifest (desktop/BuildConsole is outside the test-coverage scope). Epic #803 is the umbrella (never flipped complete) → no per-issue label sync. Built clean (`dotnet build -t:Rebuild`, 0 errors; only the 2 pre-existing unrelated `CS0067` `Start/StopRecordingRequested` warnings) in an isolated short-path worktree off fresh `origin/main` — the primary tree had a concurrent session's uncommitted `BuildConsoleSettings.cs`/`QueueWatcherService.cs` edits left completely untouched. Not GUI-live-verified (can't launch Shane's running BuildConsole here) — on the next uiSteps run against a stalling response, confirm the run now completes with a `testing.ui-executor` "…timed out after 5000ms" line instead of hanging. | `0b1fe5e9` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Git #1003) — VERIFIED-ALREADY-LANDED (no new code from this session). This exact fix was implemented and pushed concurrently by a sibling session as commit `3a4ff8a7` (its own DONE row directly below) while this session was mid-implementation in the shared checkout. I had independently arrived at the identical design — a static `UiTestExecutor.StepCompleted` fired once per uiStep from the `RunAsync` loop, subscribed as the fifth executor in `TestRunnerWindow` (unsubscribed on `Closed`), and the `RunUiTestAsync` `onTelemetry` `UiStepDonePattern` converted from step-advance to skip-terminal-card so the shared `_stepCursor` is not double-advanced (`OnStepCompleted` becomes the single advance path) — then, on `git pull --rebase`, found `3a4ff8a7` already on `origin/main`, so I DISCARDED my duplicate working-tree edits (`git checkout --`) rather than push a conflicting second declaration of the same event/helper. Verified the LANDED code rather than duplicating it: `dotnet build` clean (0 errors; only the 2 pre-existing unrelated `CS0067` `Start/StopRecordingRequested` warnings), and traced `hello-world-ui.json`'s real path against `3a4ff8a7` — both uiSteps fire `StepCompleted` (step 1 `goto /`+captureResponse fires even in the timeout/null-capture `capturedArgs==null` WARN case that was invisible tonight; step 2 `expect` fires normally; 2 uiStep rows ↔ 2 firings, single advance, no double-count), and confirmed the double-advance is correctly handled (TestRunnerWindow L273 `if (UiStepDonePattern.IsMatch(e.Label)) return;` with the telemetry advance removed). My own IN FLIGHT bookend (`aebd2e60`) is left as the record; the #803 sibling IN FLIGHT/DONE rows are untouched. `#1003` labels synced to `complete`. | `3a4ff8a7` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — `UiTestExecutor` static `StepCompleted` event: wired uiSteps into the same live per-step `StepCompleted` telemetry spine the api/graph/zoho/powershell executors already fire. Added `public static event Action<TestStepResult>? StepCompleted` (exact signature/pattern of the other four) and fire it once per completed uiStep from the `RunAsync` loop via a new `BuildStepCompletedResult(UiStepResult)` helper carrying the step's real label/passed/detail/duration (+Expected/Actual/Context/ScreenshotPath). Deliberately ONE combined entry per step (matching the one-row-per-uiStep step list), `Passed` = the COMBINED verdict (`UiStepResult.Passed` = action AND captureResponse AND extract/duration), with the captureResponse outcome folded into Detail so a headless consumer seeing only this stream still gets the full story. `TestRunnerWindow` now subscribes `UiTestExecutor.StepCompleted` alongside the existing four (unsubscribed on `Closed`); the fragile UI-only `UiStepDonePattern` telemetry-advance in `RunUiTestAsync` is REMOVED so the shared step cursor isn't double-advanced (`OnStepCompleted` → `AddStepResult` + `AdvanceStep` is now the single advance path), and the raw "STEP N PASS/WARN" telemetry card is suppressed since StepCompleted's structured card supersedes it (all other telemetry lines still card). Traced `hello-world-ui.json`'s two steps against the real path: step 1 `goto /`+captureResponse fires `StepCompleted` even in the timeout/null-capture case (`capturedArgs==null` → `overallPassed=false` → a `Passed=false` firing, the case invisible tonight), step 2 `expect` fires normally; 2 uiStep rows ↔ 2 firings, no double/gap. No DB. No test manifest (desktop/BuildConsole is outside the test-coverage scope). Epic #803 is the umbrella (never flipped complete) → no per-issue label sync. Built clean in an isolated worktree off fresh `origin/main` (`dotnet build`, 0 errors; only the 2 pre-existing unrelated `CS0067` `Start/StopRecordingRequested` warnings) — the primary tree had a concurrent session's uncommitted `UiTestExecutor.cs` captureResponse-logging WIP (the line-75 IN FLIGHT row), left completely untouched; my edits are at non-overlapping lines so their later commit auto-merges. Not GUI-live-verified (can't launch Shane's running BuildConsole here) — on the next uiSteps run, confirm each uiStep now streams a `[UI] PASS/WARN` structured card and advances its step-list row exactly once, including a captureResponse-timeout step showing WARN instead of silently vanishing. | `3a4ff8a7` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Git #980) — Build Watch panel: `SlotGrid.Rows/Columns` are now computed from the real current count of active (occupied) slots and re-applied on every occupancy change instead of the fixed 2x4. New `ReflowSlotGrid()` (called from the constructor, `OccupySlot`, and `ClearSlot`) maps the occupied-slot count → grid dims: 1→1x1 (a single build fills the whole window), 2→1x2, 3-4→2x2, 5-6→2x3, 7-8→2x4 (the true max); 0 collapses to 1x1 (full-window empty state) and any overflow clamps to 2x4. Because a `UniformGrid` lays children row-major across exactly Rows×Columns cells and a slot can be freed at any index (dismiss / auto-evict / issue-close), `ReflowSlotGrid` also reorders the child collection to pack occupied slots to the front in stable `_slots` order — so every live build stays on-screen regardless of its physical slot index while the empty slots fall outside the shrunk grid. Idempotent (only mutates the child collection when the desired order actually differs). No logging change needed (pure UI layout; slot occupancy already logs to the `build-watch` channel). No DB. No test manifest (desktop/BuildConsole is outside the test-coverage scope). Compiled clean (`dotnet build`, 0 C# errors; only the 2 pre-existing unrelated `CS0067` warnings) — the final exe-copy step was blocked only because Shane had BuildConsole running. Not GUI-live-verified (can't launch Shane's running instance here) — on the next multi-build watch, confirm the grid shrinks/grows with the live-build count. | `a5b5a6be` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — `UiTestExecutor` post-capture-timeout hang fix. Symptom: a uiSteps manifest (e.g. #958 `hello-world-ui.json`) navigates fine (`NAV OK` logged), the `/api/public/hero-headlines` captureResponse hits its own 5s `Task.WhenAny` timeout with `capturedArgs == null` (an EXPECTED valid case), then the run hangs for minutes with no further log output. Root cause: the only `await` remaining in that null path is the always-on #977 screenshot capture — `CaptureScreenshotAsync` → `CoreWebView2.CapturePreviewAsync` — which is `await`ed UNGUARDED. When the WebView2 isn't in a renderable/composited state (run triggered while BuildConsole is minimized/occluded/on a locked session), `CapturePreviewAsync` never completes; the method's `try/catch` only catches exceptions, and a never-completing Task is not an exception, so the whole run deadlocks. Fix: wrap the capture in a `Task.WhenAny(capture, Task.Delay(ScreenshotTimeoutMs))` guard (same pattern navigation/captureResponse already use) so best-effort screenshot telemetry can NEVER hang a run — on timeout it logs, skips, and lets the step complete with the real captureResponse WARN — plus explicit `ActivityLog.Log` stage tracing across the entire post-capture path (capture-wait resolution, body read, screenshot invoke, step return) so any future recurrence is diagnosable from the log alone. No DB. No test manifest (desktop/BuildConsole is outside the test-coverage scope). Epic #803 is the umbrella (never flipped complete) → no per-issue label sync. Built clean (`dotnet build`, 0 errors; only the 2 pre-existing unrelated CS0067 `Start/StopRecordingRequested` warnings) in an isolated worktree off fresh `origin/main` — the primary tree had a concurrent session's uncommitted Home-screen WIP left untouched. On rebase, reconciled a real content conflict with the concurrent #995 `shotSw` screenshot-timing landing by keeping BOTH sides (the timer AND the timeout guard; capture is now `await`ed to completion before `shotSw.Stop()`, so #995's reported duration stays the true capture time). Not GUI-live-verified (can't launch Shane's BuildConsole here) — on the next uiSteps run against a minimized/occluded window, confirm the run now completes with a `testing.screenshot` "timed out after 8000ms — skipping" line instead of hanging. | `afbd08d8` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — screenshot-baseline re-prompt diagnosis + `testing.baseline` diagnostic logging. Traced `TestManifest.SourcePath` → `SubjectFromManifest` (area/slug) → `repoRoot` through `LoadFromFile` / #989 Play (`BtnPlayTest_Click`→`RunManifestAsync`) / `ScreenshotReviewService.ReviewIfNeededAsync`, and confirmed `ScreenshotBaselineStore.Promote`'s write path == `Compare`'s read path (same `repoRoot`/`area`/`slug`/`stepIndex` construction on both sides). **FINDING: not a path/promote bug.** On-disk proof: `test-results/baselines/smoke/hello-world-ui/{1,2}.png` are byte-identical (MD5) to the run they were promoted from (`958-…134437`), so `Promote` genuinely wrote real files and `Compare` reads that exact same location — an identical re-capture yields 0% diff and would NOT re-prompt. The recurring "approve" prompt is genuine run-to-run visual variance of the captured page: these are on-failure screenshots of the live homepage (animated typewriter hero + randomly-ordered/live-editable headlines), so a later run (`…134556`) has different pixels that exceed the 6% mean-diff tolerance — a real content diff, not a lost baseline. Added explicit `ActivityLog.Log("testing.baseline", …)` on both the Compare (read: repoRoot/area/slug/step + resolved path + exists + diff%) and Promote (write: repoRoot/area/slug/dir + per-file dest + previously-silent missing-source skip) sides, plus a `SubjectFromManifest` trace of `SourcePath → area/slug`, so any real future drift is diagnosable from the log alone. Build-verified (0 errors). Shane's call on the follow-up: baselining on-failure animated-page shots will keep re-prompting — consider tolerating animated regions or only baselining `screenshot:true`/passing states. | `d1cc6651` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — `GalleryOverlay` (#966's on-demand screenshot browser) was a plain in-`TestRunnerWindow` `Grid`, so it rendered BEHIND the "Live UI Test View" WebView2 (the WPF+WebView2 airspace problem, unfixable by Z-order on a hosted control). **DECISION: option (b) — kept it, the two are genuinely distinct, so converted the overlay into its own real top-level Window.** Why NOT redundant with #975's `ScreenshotReviewWindow`: #975 is a *conditional* baseline-diff **approval gate** — auto-shown by `ScreenshotReviewService.ReviewIfNeededAsync` ONLY when a baseline is missing or a real visual diff is found, with baseline-vs-this-run side-by-side + `Approve→promote-baseline+close-issue` / `Report issue→send-to-chat` semantics (and it needs a subject + baseline comparisons + those two delegates, none of which the `BtnScreenshots` button has). #966's gallery is the *always-available on-demand* browse of every captured screenshot for ANY run — including a clean run whose shots all match baseline, which the review gate deliberately shows nothing for. Retiring it / pointing `BtnScreenshots` at `ScreenshotReviewWindow` would remove the only way to eyeball a clean run's shots and would misuse the issue-closing Approve gate as a casual viewer — so they're kept separate. New `ScreenshotGalleryWindow.xaml(.cs)` holds the click-through (same header/image/caption/nav as the old overlay); as its own HWND it renders ABOVE the center WebView2. `BtnScreenshots_Click` now opens it non-modally (`Owner = this`, `.Show()`) so the Test Runner stays live behind it; the overlay `Grid` + its code-behind (`ShowGalleryImage`/prev/next/close + `_galleryIndex`) removed from `TestRunnerWindow` (`_galleryShots` kept only to enable/count the button). No logging change needed (pure UI relocation). `dotnet build -t:Rebuild` clean in an isolated worktree off fresh `origin/main` (0 errors; only the 2 pre-existing unrelated `CS0067` `Start/StopRecordingRequested` warnings) — the primary tree had a concurrent session's uncommitted Home-screen WIP left untouched. Not live-verified in a running GUI (can't launch Shane's BuildConsole here) — on the next UI-manifest run, click `📷 Screenshots` and confirm the gallery window now floats above the Live UI Test View instead of hiding behind it. No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope. Epic #803 is the umbrella (never flipped complete) → no per-issue label sync. | `7f8c9dae` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF) — Home screen: remove the hardcoded non-closable `claude.ai` `TabItem` and replace it with a native closable "Home" view as the default first tab (`ClaudeWebView` relocated to a parked 1×1 hidden host so the ~19 `GetActiveWebView` callers + builder-button/#942 chat-status infra keep working). Session persistence of open chat tabs (BoardChat identity + per-pane layout) via `BuildConsoleSettings`. Three-section ADHD roll-up matching #874's visual language — "Where you left off" (persisted last-launch chat tabs), "Running now" (live queue via the shared `BuildTrackerApiClient`), "Done, waiting for you" (done builds whose GitHub issue is still open, via `GitHubIssuesService.GetOpenIssueNumbersAsync`) — every entry click-through to the originating chat tab through the existing `OpenChatTab`/`FindChatForIssue` path. `ActivityLog.Log("home-screen", …)` on open / section render / click-through. | `806a0d55` |
| 2026-08-13 | ✅ DONE | Test manifests (Epic #803): reactive DLQ Postgres schema-error scan (`observability/dlq-schema-error-scan.json` — scans `msp_dlq_store` last-48h for `column/relation/type "..." does not exist`, extracts the bad identifier, asserts rowCount 0) + proactive TS↔live-DB column-drift guard (`observability/schema-drift-guard.json` — 44-column operational spine `msp_dlq_store`/`msp_job_queue`/`outbound_webhooks` vs `information_schema.columns`, missing==0 + non-vacuous present==44). Both routed through #896's `POST /admin/deploy/sql-test` (the endpoint the headless runner can auth to via ingest token; `GET /admin/dlq` is `requireAdmin`-only with no time-window filter — documented in-manifest). SELECT-first VALUES form (sql-test rejects leading `WITH`). Full 259-table sweep left as standalone hand-run diagnostic with regeneration recipe: `lib/db/migrations/manual/archive/diagnostics/2026-08-13-schema-drift-ts-vs-live.sql`. Registered in `_regression-suite.json`. Not live-verified (no DB access here). | `89d98679` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Epic #803) — Automation sidebar "Play" button now runs the loaded manifest through the real `RunManifestAsync` pipeline instead of the dead recording-only playback path. `BtnPlayTest_Click` used to fire `PlayTestRequested` with `RecordedSteps`, which #963 rendered permanently empty (Record button removed → nothing ever calls `RecordAction` → `RecordedSteps` stays `[]`), so Play silently ran zero steps. Reroute Play to source the loaded manifest from `_lastLoadedManifest` and run it through the same `RunManifestAsync` path every other trigger (Menu > Run Tests, regression sweep, #898 remote trigger) already uses — full apiTests/graphTests/postGraphApiTests/zohoTests/uiSteps/powerShellVerify pipeline with live TestRunnerWindow telemetry, not a uiSteps-only replay. | `7abbb25f` |
| 2026-08-12 | ✅ DONE | BuildConsole (WPF) repo hygiene: removed accidentally-committed temp `wpftmp.csproj`. Shane: "Oh no there are multiple .csproj files." A stray MSBuild-generated temp project file (auto-created and normally auto-deleted, but left behind when a build gets killed mid-flight — happened during tonight's #935 crash-diagnosis rebuild cycle) had been accidentally swept into an unrelated concurrent session's #937 bookend commit, causing "multiple project files, specify which one" ambiguity for any plain `dotnet build` in the directory (confirmed: it was genuinely `git ls-files`-tracked, not just a local leftover, so it would recur on every pull). Untracked + deleted via `git rm --cached`, added a `*_wpftmp.csproj` rule to `desktop/BuildConsole/.gitignore` so a future one can't get committed again. Verified: plain `dotnet build` (no explicit project arg) now resolves to the single real `BuildConsole.csproj` with no ambiguity error. Flagged separately, not fixed here (not mine to touch - another session's active in-progress work): `BuildConsole.csproj` currently pins `ICSharpCode.AvalonEdit` to `Version="6.3.0.90"`, which doesn't exist on nuget.org (`NU1102`, nearest real version found is `4.3.0-alpha-00000000`) - the build still won't fully succeed until that session corrects its own version pin. | `677da270` |
| 2026-08-12 | ✅ DONE | Git #949 - Epic: #773 Admin View of the Portal. Fixed `ViewAsSwitcher.tsx`'s `handleSelect()` Assessment/CustomerUser branch, which sent impersonation to `/crm/portal` (a dead artifact — no code there consumes `impersonation_token`, near-certain blank/404) instead of `/portal` (the real msp-portal artifact whose boot logic in `auth-context.tsx` consumes the token and routes to the tenant). Now matches the already-correct `MSPAdmin` branch exactly: `/portal/?impersonation_token=...&target_slug=${account.mspSlug}`, using `mspSlug` already present on every row from `GET /admin/view-as/accounts` but previously unused in this branch. Guarded the nullable `mspSlug` with `?? ""` for `encodeURIComponent` (tsc caught `string \| null` mismatch). No backend or msp-portal boot-effect changes — client-side URL fix only, per #949/#774's scoping. Typecheck clean (`tsc --noEmit`). Root-caused in #774's investigation comment; filed as #949. | `f983547a` |
| 2026-08-12 | ✅ DONE | BuildConsole (WPF) fix broken AvalonEdit NuGet reference blocking all builds. Shane: "can you build the project" — still failing with `NU1102` (previously flagged, not yet fixed, since it was another concurrent session's #939 in-progress work). Root cause: package id `ICSharpCode.AvalonEdit` at `Version="6.3.0.90"` doesn't exist on nuget.org at all under that id (confirmed via the NuGet search API: the real, currently-published package id is just `AvalonEdit`, latest stable `6.3.1.120` — the assembly/namespace ships as `ICSharpCode.AvalonEdit.*` regardless, so #939's actual C# `using`/XAML `xmlns` references were unaffected). Corrected the single `PackageReference` line only — no other changes to `BuildConsole.csproj` or any of #939's own `SqlRunnerView` code. Verified: `dotnet build` clean, 0 errors, 0 warnings. | `41631c3a` |
| 2026-08-12 | ✅ DONE | Git #927 — bookend backfill for `test-manifests/927-chat-escalation-push.json` (public-chat escalation -> Zoho Desk ticket -> admin push manifest), which a prior session committed (`97c49725`) without ever adding its PLATFORM_BUILD.md row or syncing issue labels. Verified against current code: `public-chat.ts` L341-343's `escalateThisTurn && contactEmail` precondition and `zoho-desk.ts` L337-342's `sendWebPushToAdmins({ title: "New chat escalation", body, linkPath: PUBLIC_CHAT_REVIEW_LINK_PATH })` (`PUBLIC_CHAT_REVIEW_LINK_PATH = "/pipeline/chat-queue"`, zoho-desk.ts L264) both match the manifest's apiTests exactly. Manifest's own honest limit stands: no `/zoho/desk/*` lookup route and no subscribed test device exist, so push delivery itself can't be asserted end-to-end — only the real trigger path (live-LLM purchase-intent escalation over `POST /api/public-chat`) and the server-side `sendWebPushToAdmins` call shape are covered; a Vitest unit-test follow-up for the call-shape assertion remains unshipped, tracked here not as a new issue. No code changes this session — bookend/label sync only. | `97c49725` |
| 2026-08-12 | ✅ DONE | Git #901 — bookend backfill for `test-manifests/baseline-actions-powershell-verify.json` (independently verify all 10 real `baseline_action_templates` write actions via #900's `powerShellVerify`), which two prior sessions on this same issue committed the real work for (`83af155d`) but never flipped their own PLATFORM_BUILD.md row to DONE — both stale `⏳ IN FLIGHT` rows are frozen in `PLATFORM_BUILD_ARCHIVE_2026-08-12.md` and left untouched per policy. Re-verified this session against current code before backfilling, per the task's own instructions: pulled #901's full issue body + all 3 comments (the blocked comment, the resumed-unblocked comment, and the Done comment — later comments supersede the original body, and the Done comment matches `83af155d` exactly), read the landed `PowerShellTestExecutor`/#900 and remote-trigger-over-HTTP/#898 mechanisms, and confirmed all 4 of #901's real deliverables exist and are correct: (1) the read-only enumeration SQL at `lib/db/migrations/manual/archive/diagnostics/2026-08-12-baseline-action-templates-enumerate-901.sql`, (2) `test-manifests/baseline-actions-powershell-verify.json` with one apiTests-write + graphTests-app-read + powerShellVerify-ground-truth triad per real row (all 10 from the phase3 migration, "remove" actions using the documented presence-probe trick since `powerShellVerify` has no `containsNone`), (3) registration in `_regression-suite.json`, and (4) the matching `requireAdminOrIngestToken()` scope fix on `admin-baseline-templates.ts`'s testbed-customers/:templateId/test routes so a headless #898-triggered run can authenticate. Attempted the remote-trigger-over-HTTP step myself this session as instructed: no `DEPLOY_URL`/`BUILD_TRACKER_INGEST_TOKEN` env reachable here, and both `localhost:3000` and `localhost:5000` time out — same headless-environment limitation #900 and the original #901 landing both already documented; could not live-trigger or read results back. No code changes this session — bookend backfill + re-verification only. Issue #901 already correctly carries `complete` + `Shane To-Do` labels from the original landing; no label change needed. | `83af155d` |
| 2026-08-12 | ✅ DONE | Git #551 (final phase) — `appgov:stale-app-registrations` fix: replaced the hardcoded `countIfLastSignInOlderThan(180)` transform (reads `signInActivity.lastSignInDateTime`, which `/applications` never carries, so it fires stale on 11/11 live app registrations regardless of the argument) with a real age-based `countWhere('{{createdDateTime}} olderThanDays N')` clause at 180/365-day cutoffs, confirmed against the actual `applyMapping`/`evalClause` transform vocabulary in `monitor-executor.ts` (`countIfOlderThan` does not exist — `olderThanDays` inside `countWhere` is the real primitive, same one #541 used) rather than assumed from the issue text, per Shane's Option 1 scoping comment (age-based, no `/beta` dependency, since `/applications` has no usage/last-sign-in signal at all and `graphFetchPaginated` hardcodes v1.0). Renamed off "stale" framing to "Aging App Registrations"; added real `severity_rules` (was `[]`) — critical on any registration over a year old, warning over 180 days. Per-item "affected" tagging: confirmed the `FanOutItemNormalizer`/`FAN_OUT_ITEM_NORMALIZERS` mechanism #357 used for EEEU is wired only into `runFanOutCheck` and this check has no fan-out, so no normalizer hook exists to hang a stored flag on without new plumbing (out of scope per Shane's own "no new fan-out plumbing required"); the raw `tenant_check_item_details` rows already carry `createdDateTime` per app (already-collecting, confirmed in Phase 1's 2026-08-08 addendum), so the affected subset is recoverable downstream with the identical predicate the mapping itself uses — documented as an explicit design decision, not silently assumed. Wired into `assess:copilot-readiness` via idempotent migration PART D (was enhanced-monitoring + detail:full-item-collection only, confirmed absent from copilot-readiness in Phase 1's package table). Migration: `lib/db/migrations/manual/2026-08-12-stale-app-registrations-age-based-551.sql` (Shane runs it — no DATABASE_URL here; PART C is the live confirmation query). 13 new tests (`stale-app-registrations-age-based-551.test.ts`): defect reproduction (11/11 stale, inert argument), fix verification (real per-age counts, no signInActivity dependency), severity bands, package-membership assertion against the migration's own SQL text. Regression: 257 tests green across 6 related monitor/mapping suites. `tsc --noEmit`: 71 errors, byte-identical count against a stashed pre-change baseline — zero in changed files. This closes out #551 (global-admin-count, unreviewed-consents, risky-permission-grants, cert-secret-expiration were already confirmed fixed in prior phases). | `6ad4db44` |
| 2026-08-13 | ✅ DONE | Git #901 (bookend correction) — the earlier IN FLIGHT backfill row for this issue was never flipped to DONE. Verified independently: real commit `83af155da4` contains a substantial, well-documented manifest (`test-manifests/baseline-actions-powershell-verify.json`) triggering all 10 real baseline_action_templates rows via `POST /admin/baseline-templates/:templateId/test`, diffing the app's own post-write Graph read against #900's independent PowerShell verification. Also includes a genuine auth-scope fix in admin-baseline-templates.ts (`requireAdminOrIngestToken()` on the two routes needed for headless #898-triggered runs). Real, documented preconditions remain for Shane before this manifest can actually execute (testbed tenant flags, GRAPH_TEST_TENANT_ID, real seed object ids, one-time interactive `Connect-MgGraph`). | `83af155da4` |
| 2026-08-13 | ✅ DONE | Git #921 (bookend correction) — the real DONE bookend (commit `922b01cf`, referencing work commit `883bcf8b`) was lost from PLATFORM_BUILD.md, almost certainly clobbered by #948's archive-and-reset landing at nearly the same time (883bcf8b: 2026-08-13T02:22:46Z). Verified independently via direct code inspection: `Controls/GitDetailView.cs` (LoadMilestone/LoadEpic/LoadIssue, 3 render modes), `GitMilestone.GithubNumber` (`LeftSidebar.xaml.cs`), and `MainWindow.GitDetailTabs.cs` (multi-pane tab integration, dedup via git-detail:{kind}:{key}) all genuinely exist and match #921's full spec - milestone/epic/issue detail tabs, Shane To-Do carve-out, click opens-or-focuses, additive to the existing side panel. | `883bcf8b`, `922b01cf` |
| 2026-08-13 | ✅ DONE | Git #950 — Build Queue (BuildConsole WPF): `RenderQueue` now sorts via new `SortForDisplay` helper — numbered rows first, biggest `GithubNumber` first; un-numbered rows sort after by `Id` descending (QueueItem has no CreatedAt, and Id is the monotonic autoincrement PK — the reasonable "newest first" fallback, also the tiebreak for duplicate rows sharing a number), applied to both `topLevel` and each `childrenOf` nested blocked-by group. `BuildQueueTreeItem`'s `IsExpanded` flipped `true`→`false` so nested children start collapsed. New `QueueSearchBox` TextBox above `QueueTree` (grey overlay-placeholder, no native WPF placeholder) with `QueueSearchBox_TextChanged` → live substring filter on `GithubNumber.ToString()` (e.g. "94" matches #940/#946/#947); the filter is applied inside `RenderQueue` reading `ApplyFilter(_lastItems)`, so it layers on top of the current filter chip rather than bypassing it, re-applies on the 15s poll, and clears to the full sorted list when emptied. Handler no-ops under the Tests chip (disk-based manifest tree, no queue rows) and guards `QueueTree == null` (the #935 mid-BAML-parse lesson). `dotnet build` clean: 0 warnings, 0 errors. No test manifest — desktop/BuildConsole is out of the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `a238ac6c` |
| 2026-08-13 | ✅ DONE | Git #952 (Epic #803) — UI Automation sidebar (BuildConsole WPF `Controls/LeftSidebar.xaml`): move API/Graph badges above the Available Tests list, remove `ManifestFilesList`'s `MaxHeight="140"` so it fills all available vertical height (URL box, Record/Play/Clear, badges fixed at top; test list takes the rest), and add a flyout Popup (Web-Tools-popout pattern from #864) that opens on manifest click showing that manifest's apiTests/graphTests/uiSteps/zohoTests/powerShellVerify steps in a readable list — keeping "Recorded Test Actions" dedicated to the live Record workflow. `dotnet build` clean (0 warn, 0 err). No test manifest — desktop/BuildConsole is out of the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `58068ffb` |
| 2026-08-13 | ✅ DONE | Git #953 (Epic #803) — Test Environment Variables store + general config-placeholder resolver. `BuildConsoleSettings` gains a `TestEnvVar {Name,Value}` type + `TestEnvironmentVariables` list (same `%AppData%\BuildConsole\settings.json` round-trip as WebTools; empty-list field initializer so a pre-#953 settings.json still deserializes). New Settings section in `LeftSidebar.xaml`/`.xaml.cs` — an add/edit/remove key/value editor mirroring the #864 Web Tools pattern (values masked in the list since they're often secrets like `TEST_PORTAL_PASSWORD`; a repeat NAME edits in place instead of duplicating). `TestRunVariables` gains a `SeedConfigVariables()` config layer checked in `Resolve()` BEFORE the per-run extracted values — the same "resolved first" precedence `{{DEPLOY_URL}}`/`{{SECRET_KEY}}` already have — logging every resolve/miss to the new `testing.config-vars` channel; the `VariableNotResolvedException` message now points at Settings. `RunManifestAsync` seeds the layer from Settings before any executor runs, so every `{{NAME}}` (`TEST_*`/`GRAPH_TEST_*`) resolves against the store first. `{{DEPLOY_URL}}`/`{{SECRET_KEY}}` left exactly as-is — still resolved by `HttpTestExecutor.ResolvePlaceholders` from `build-queue-watcher.config.json` before `TestRunVariables` runs, so they never reach the store. `dotnet build` clean (0 warn, 0 err). No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `8b3c9daa` |
| 2026-08-13 | ✅ DONE | Git #955 (Epic #803) — Git Board `IssuesTree` (BuildConsole WPF `Controls/LeftSidebar.xaml`): added missing `SelectedItemChanged="IssuesTree_SelectedItemChanged"` to the `IssuesTree` tag, alongside the existing `SizeChanged="IssuesTree_SizeChanged"`. Confirmed the C# handler already exists and is correct — only the XAML wiring was missing, so clicking a milestone/epic/issue did nothing. `dotnet build` clean (0 warn, 0 err). No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `fc1a3d3a` |
| 2026-08-13 | ✅ DONE | Git #956: BuildConsole (WPF) Build Queue panel item flickers away then reappears after clicking chat Queue button. Shane: "when I click on Queue in the chat... it shows the item in the queue in the right Build Queue panel. Then it disappears. Then about 20 or so seconds later it reappears." Root cause: `BuildQueuePanel.RefreshAsync()` had no protection against overlapping calls. Clicking the chat's injected "📋 Queue" button posts `BT_QUEUE_BUILD`, and `MainWindow.xaml.cs`'s handler calls `RefreshAsync()` immediately after the POST succeeds — completely independent of the panel's own 15s `_pollTimer`. If a regularly-scheduled poll happened to already be in flight (started slightly before the click, fetching the queue without the new item yet), the two live fetches could complete out of order: the click's own fetch (requested later, faster round trip) finishes first and correctly shows the new item, then the earlier-started poll finishes second with its stale pre-click snapshot and overwrites it — last-writer-wins, not last-requested-wins. The item then reappeared on the next poll tick (~15s later), matching "~20 or so seconds" exactly. Fixed with a monotonic generation counter (`_refreshGeneration`), incremented synchronously the instant a call starts (before the await) — a call's result is only applied if it's still the most-recently-started call by the time its own fetch completes, so whichever call was requested last always wins regardless of completion order. Caught and corrected a real issue-number collision mid-fix: a blanket `sed` meant to correct my own guessed number (#950 → real #956) accidentally overwrote 6 pre-existing, unrelated `Git #950` comments from another concurrent session's real work (search box + biggest-to-smallest sort + collapsed-by-default) already in this same file — caught via `git diff` before committing and reverted those 6 lines back to #950, keeping only my own 2 new additions as #956. No DB work needed/attempted. Verified: `dotnet build` clean, 0 errors, 0 warnings; diff confirmed purely additive (35 insertions, 0 deletions) once the collision was fixed. Not live-verified against an actual click-vs-poll race here — Shane should confirm clicking "Queue" in a chat no longer causes the item to flicker away and come back. | `e7317c2a` |
| 2026-08-13 | ✅ DONE | Git #954 (Epic #803) — Settings: full native WPF tab with six-category navigation, replacing the cramped sidebar panel. New `Controls/SettingsTabView.xaml`(.cs) hosts every setting in one scrollable tab split into six clearly-separated sections — General (Claude endpoint, Build Tracker read-only display, Theme), Credentials (GitHub PAT #834, Zoho token #880), Test Environment (#953 vars editor), Chat Integration (New Chat Project URL #922), Web Tools (#864 list editor), Replit Watcher (#902 interval/selector/two URLs). Every field's `x:Name`, Click handler, and `BuildConsoleSettings` persistence moved verbatim out of LeftSidebar's old in-sidebar `SettingsView` StackPanel — a layout/navigation change only, no functional change to any setting (verified: build clean, all save/add/edit/remove handlers intact). Opened/focused via new `MainWindow.SettingsTab.cs` (`OpenSettingsTab`), reusing the #893/#894/#921 multi-pane editor-tab infra: opens-or-focuses, dedup on `Tag="settings:main"`, same header/close/AttachTabContextMenu/AttachTabDragHandlers recipe as the Git detail tabs. LeftSidebar's `SettingsView` is now just a six-item category nav list; a click raises `SettingsCategoryRequested` → MainWindow opens/focuses the tab and scrolls to that section (`SettingsTabView.ScrollToSection`, deferred to DispatcherPriority.Loaded so layout has extents). Both the ActivityBar cog (`ActivityBar_ActiveViewChanged`, view=="Settings") and File > Settings (`MenuSettings_Click`) now open the tab instead of routing to the old in-sidebar content. Replit "re-apply on save" hook (`ReplitWatcherSettingsChanged`) moved off LeftSidebar onto SettingsTabView, rewired per tab instance in `OpenSettingsTab`. `ActivityLog.Log("settings.tab", ...)` on open and on category navigation. `dotnet build` clean (0 warn, 0 err). No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `e09e5945` |
| 2026-08-13 | ✅ DONE | Git #959 (Epic #803) — CLAUDE.md amendment to "Test Coverage — Standing Practice" (Shared File Write Discipline section untouched): added discover-before-create (search test-manifests/ by route/feature, not issue number, before writing a new manifest — update the existing file in place if found), a top-level `lastVerifiedAgainstCommit` field required on every manifest, and the new `test-manifests/{area}/{feature-slug}.json` naming convention (e.g. `copilot-readiness/checkout.json`) replacing the old flat `{issue}-{feature-slug}.json` convention for new manifests going forward. Pulled #959's full issue body (no comments) confirming the exact three asks and Shane's own quotes before writing. No DB work, no code changes outside CLAUDE.md. | `6d7bc6dd` |
| 2026-08-13 | ✅ DONE | Git #957 (Epic #803) — SQL "hello world": `test-manifests/hello-world-sql.json`, one `apiTests` entry POSTing `{ "query": "SELECT 1 AS ping" }` to #896's `POST /admin/deploy/sql-test`, asserting `expect: { status: 200, jsonPath: "$.rows[0].ping", value: 1 }` — the real returned row value, not just success/no-error (confirmed against `HttpTestExecutor.EvaluateExpectation`'s jsonPath/value comparison, the same mechanism `725-chat-escalation.json`'s apiTests already use). Registered in `_regression-suite.json`. Read #896's landed endpoint (`admin-deploy-console.ts`) directly: its own `expect` shape only supports `rowCount`/`columns`, not a value check — so the value assertion has to live in the *outer* apiTest's jsonPath/value, not the sql-test call's own body, which is what this manifest does. Could NOT complete the live-trigger step: attempted `POST /admin/deploy/test-run` per #898's documented sequence, but no `DEPLOY_URL`/`BUILD_TRACKER_INGEST_TOKEN` env is reachable in this headless session and `localhost:3000`/`:5000` both refuse the connection — the identical environment limitation #900/#901 already documented. The manifest and registration are real and ready; Shane (or a session with the dev server actually running) still needs to trigger it once to get the first genuine pass/fail read back. | `5a9cfa6e` |
| 2026-08-13 | ✅ DONE | Git #958 (Epic #803) — UI "hello world", sibling of #957's SQL one: `test-manifests/hello-world-ui.json`, one `uiSteps` entry re-navigating to `/` with a `captureResponse` on `GET /api/public/hero-headlines` (the real fetch `useHeroHeadlines.ts`'s `useTypewriterHeadline()` makes on mount) reusing #892's `containsAny`/`containsNone`, scoped via `jsonPath: "$[0].leadText"` so the response's legitimate `seasonalLabel:null` can't false-fail a `containsNone: ["null"]` check; `containsAny` matches one of the three real seed-migration headlines (`lib/db/migrations/manual/2026-07-19-hero-headlines.sql`). Second step asserts the header CTA (`[data-testid="get-real-score-cta"]`) is visible. Registered in `_regression-suite.json` (also backfilled #957's own manifest, which was real but never registered). Unlike #957's session, found the live trigger reachable: `scripts/build-queue-watcher.config.json` (present on disk, not an env var) has the real `apiBaseUrl`/`ingestToken` — POSTing to `/admin/deploy/test-run` 404'd until realizing `app.ts` mounts `routes/index` under `app.use("/api", router)`, so the actually-live path is `/api/admin/deploy/test-run` (the route file's own literal string is relative, not absolute-from-host). Triggered twice, polled `GET /api/admin/deploy/test-run/:runId`, and read back a genuine `status:"done"` `ManifestRunResult` from this headless session both times — the core #898 proof this issue asks for. Both runs came back `AllPassed:false` on step 1 (`navigate {{DEPLOY_URL}}`, `❌ TEST FAILED (Exception)`), which surfaced a real, previously-latent bug: `MainWindow.RunManifestAsync` passed `manifest.BaseUrl` straight into `UiTestExecutor.RunAsync` with no `{{DEPLOY_URL}}` placeholder resolution (unlike `HttpTestExecutor`, which already resolves it for every apiTest) — `CoreWebView2.Navigate("{{DEPLOY_URL}}")` throws, failing every uiSteps manifest's first step. Fixed: `HttpTestExecutor.ResolvePlaceholders` made `internal` and called on `manifest.BaseUrl` before handing it to `RunUiTestAsync`. `dotnet build` clean (0 warn, 0 err). The live BuildConsole instance that claimed both runs is still on the pre-fix binary (I can't restart Shane's own running GUI app from here) — restarting it to pick up the fix and confirm a genuine pass is a Shane To-Do. `.gitignore`: added `test-results/` (BuildConsole's local per-run JSON dump, never meant to be committed — surfaced only because live-triggering wrote two files into this working tree). | `3a8564f1` |
| 2026-08-13 | ✅ DONE | Git #960 (Epic #803) — Migrated all 19 flat `test-manifests/*.json` into the `test-manifests/{area}/{feature-slug}.json` structure #959 landed, via history-preserving `git mv` (renames, not add/delete). `{area}` derived from each manifest's REAL routes/targets, not its filename: `auth/` (437 verification-code-flow, 656 password-reset-guard — `/api/auth/*` + `/api/public/flow/*`), `chat/` (725 escalation, 927 escalation-push — `/api/public-chat`), `crm/` (883 zoho-lead-and-invoice-sync, 929 zoho-lead-field-verification, 926 engagebay-end-to-end — funnel `/api/quiz/home-lead-capture` → Zoho/EngageBay), `assessment/` (886 home-quiz — home `/` quiz → assessment-flow handoff), `copilot-readiness/` (887 consent-scan-trigger, 888 verdict-pillars, 889 documents-screen, 890 sow-proposal, 891 checkout, 792 remediation-tracker-e2e — all literal `/copilot-readiness/*` routes), `admin/` (928 tenant-switcher-floaty, 901 baseline-actions-powershell-verify, 900 powershell-verify-example — admin/deploy surfaces), `smoke/` (957 hello-world-sql, 958 hello-world-ui — pipeline self-tests). Added a top-level `lastVerifiedAgainstCommit` to every manifest set to the real `git log -1` last-touch commit of each file (looked up, not guessed); hello-world-ui's stale value (`6d7bc6dd`, an ancestor) corrected to its real last-touch `3a8564f1`. Rewrote `_regression-suite.json` to the new paths, grouped by area, and DEDUPED the pre-existing doubled `hello-world-sql` entry (18 unique; 900 stays intentionally unregistered per its own EXAMPLE note). Updated live cross-references to new paths in CLAUDE.md (the `chat/escalation.json` example pointer), `smoke/hello-world-ui.json` (its `→ copilot-readiness/documents-screen.json` note) and `admin/baseline-actions-powershell-verify.json` (its `→ admin/powershell-verify-example.json` note). Left historical provenance references untouched by design: PLATFORM_BUILD.md / ARCHIVE log rows and two applied-migration comments (`2026-08-12-baseline-action-templates-enumerate-901.sql`, `2026-08-12-test-checkout-session-verification-code-879.sql`) — frozen records, not live pointers. Verified no orphans: 19 files → 19 new homes, all 18 registry entries resolve, root holds only `_regression-suite.json` + the 7 area dirs, every migrated file re-parses as valid JSON. Concurrent-session note: #958 committed its hello-world-ui.json + registration (`3a8564f1`) mid-session, so those were folded into the migration cleanly rather than clobbered. No DB work; no code changes outside test-manifests + the one CLAUDE.md line. | `95da7cda` |
| 2026-08-13 | ✅ DONE | Git #964 (Epic #803) — PowerShell "hello world", third sibling of #957 (SQL)/#958 (UI): `test-manifests/smoke/hello-world-powershell.json`, one `powerShellVerify` step with `afterStep: "GRAPH_TEST_TENANT_ID"` (a bare config-var name, resolved through #953/#961's Test Environment Variables layer — no apiTests/graphTests step needed) diffed against `(Get-MgContext).TenantId`, `compareField.matchType: "exact"`. Covers both #964 asks with no special-casing, confirmed by reading `PowerShellTestExecutor.cs` directly: (1) connection-succeeds — the executor's own `BuildScript` always runs delegated `Connect-MgGraph -UseDeviceCode -NoWelcome` first and hard-checks `AuthType -eq 'Delegated'`, detecting a device-code prompt on the child process's output and aborting within `timeoutMs` with the "run Connect-MgGraph interactively once" message rather than hanging; (2) correct-tenant — the exact-match diff against the real `GRAPH_TEST_TENANT_ID` fails this step outright if a stale cached session points at the wrong tenant, not just "connected to something". Registered in `_regression-suite.json`. Pulled #964's full issue body (no comments) plus #900's landed `PowerShellTestExecutor`, #953/#961's Test Environment Variables config-var resolver, and `TestRunVariables.Resolve`'s config-vars-before-extracted-values precedence before writing anything. Live-triggered via #898's remote-trigger-over-HTTP path (`POST /api/admin/deploy/test-run` → poll), same as #957/#958 — this surfaced a real regression #960's manifest migration introduced: `MainWindow.xaml.cs`'s `TestTriggerTickAsync` did a flat `Path.Combine(repoRoot, "test-manifests", manifestFile)`, which 404'd for every manifest once they all moved into `{area}/` subdirs (confirmed: reproduced live, `"Manifest not found or unparseable"` for `hello-world-powershell.json`) — this broke the remote-trigger pipeline for ALL manifests, not just this one. Fixed: recursive `Directory.EnumerateFiles(testManifestsRoot, manifestFile, SearchOption.AllDirectories)` lookup, still safely scoped under `test-manifests/` since `admin-test-trigger.ts`'s `MANIFEST_FILE_RE` already rejects any path separator in `manifestFile`. `dotnet build` clean (0 new warnings, 0 errors — pre-existing 2 unrelated `CS0067` warnings only). Could not get a live pass/fail read-back this session: the live BuildConsole GUI instance that claims remote-trigger runs is still on the pre-fix binary (same "can't restart Shane's own running GUI app from here" limitation #958 documented for its own bug) — restarting it to pick up the fix and get a genuine first pass/fail on this manifest is a Shane To-Do, same as #958 left for its own fix. | `ad0fabda` |
| 2026-08-13 | ✅ DONE | Git #961 (Epic #803) — Test Environment Variables auto-discovery, on top of #953's blank editor. New `Services/TestManifestVariableScanner.cs` walks every real manifest under `test-manifests/{area}/{feature-slug}.json` (post-#960 nested), recursively regex-extracts every `{{NAME}}` placeholder from all string values, and auto-adds any not already stored with a `<unset>` default + `NeedsReview` flag on the `TestEnvVar` model. Two placeholder classes excluded: `DEPLOY_URL`/`SECRET_KEY` (resolved separately from build-queue-watcher.config.json) and — PER MANIFEST, subtracted before the cross-file union so a name extracted in one manifest but referenced un-extracted in another still surfaces — any name that manifest's own `extract.as` block populates (cross-step values, #877's `TestRunVariables`, not config). Fills gaps only: a value Shane already set is never overwritten or re-flagged. Verified the discovery against the real corpus — correctly excludes `adminToken`/`authToken`/`extractedCode`/`testbedCustomerId` (all extract-`as`) and `DEPLOY_URL`/`SECRET_KEY`, surfacing the genuine `TEST_*`/`ENGAGEBAY_WEBHOOK_SECRET` set. UI (`Controls/SettingsTabView.xaml`/`.cs`): `NeedsReview` rows render an orange "NEEDS REVIEW" tag (PeachBrush, matching #874's warning-color convention) with the actual current default shown in full plus a plain-language "set X properly — current default is `<unset>`" line so Shane fixes it in place; the flag auto-clears the moment he edits the value away from the default. Scan runs automatically on section open (constructor + `ScrollToSection("TestEnvironment")`) and via a new manual "Rescan Manifests" button with a one-line result status; a scan exception is caught so it can never take the Settings tab down. No GitHub issue filed per variable (kept local to BuildConsole, per the task). Logs a one-line found/added summary to the dedicated `testing.config-vars.scan` channel each run. `dotnet build` clean (0 warn, 0 err). No DB work. Committed via an isolated worktree (`C:\Source\wt-961`) after a concurrent session clobbered all four of this session's uncommitted files in the shared primary tree — re-applied, build-verified, and pathspec-committed only these four files from the clean worktree. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `52b57e6b` |
| 2026-08-13 | ✅ DONE | Git #967 (Epic #803) — Scheduled recurring regression-suite runs + on-failure push alerting (BuildConsole WPF). New `Services/RegressionScheduleService.cs` — a background `DispatcherTimer` scheduler that mirrors `ReplitWatcherService` verbatim (nullable-guarded timer + `_busy` re-entrancy flag + `ApplyConfig()` reading Settings and starting/stopping live), reusing the app's one established polling pattern rather than inventing a new mechanism (per #967's explicit "reuse #863/#902's DispatcherTimer pattern" ask). It sweeps the FULL `test-manifests/_regression-suite.json` every N hours and, ONLY on failure, fires an admin web-push alert naming the first failing manifest/step (`Feature (#Issue): step '…' failed — <detail>` + `(+N more failing)`) with a `/adminv2/git` deep link; a fully passing scheduled run is silent (no push), satisfying #967 requirement 4. Deliberately does NOT kick an immediate run on `ApplyConfig()` (unlike the Replit watcher) — a full-suite sweep pops the shared WebView2 runner and would be disruptive on every app-start/settings-save; first run lands one interval out. The service stays free of UI/HTTP specifics: MainWindow injects two delegates — (1) `RunRegressionSuiteCollectAsync`, the existing `RunRegressionSuite_Click` body factored out unchanged (same parse→`TestManifest.LoadFromFile`→`RunManifestAsync` path) now returning each manifest's `ManifestRunResult`; the menu item ignores the list, the scheduler inspects `!AllPassed`; and (2) `BuildTrackerApiClient.SendTestAlertAsync` → `POST /api/admin/deploy/test-alert`. Settings: `BuildConsoleSettings` gains `ScheduledRegressionEnabled` (false), `ScheduledRegressionIntervalHours` (24), `PushOnRegressionFailure` (true) — same field-initializer-as-default round-trip as the Replit block, so a pre-#967 settings.json still deserializes. `SettingsTabView`: a new "Scheduled runs" group UNDER the existing Test Environment category (#954 tab / #953 vars section) — enable checkbox, interval-hours box, push-on-failure checkbox, Save — with a `ScheduleSettingsChanged` event wired per tab instance in `MainWindow.SettingsTab.cs` for live arm/disarm on save (mirrors #902's `ReplitWatcherSettingsChanged`). Server: new `POST /admin/deploy/test-alert` in `admin-test-trigger.ts`, gated by the same `requireAdminOrIngestToken()` the app already uses for `/test-run` (no new auth surface), calling `sendWebPushToAdmins({title, body, linkPath, playSound})` — the exact #727 pattern (identical to `admin-observability.ts`/`admin-callback-tokens.ts`); the WPF app can't call `sendWebPushToAdmins` directly since VAPID keys + push-subscription rows are server-side. `ActivityLog.Log("testing.scheduled-run", …)` on arm/disarm, run start, run finish (pass-count or fail-summary), and alert sent/suppressed. Verified: BuildConsole `dotnet build` clean (0 errors; only the 2 pre-existing #963 `CS0067` warnings), incl. a full rebuild after rebasing onto #973's concurrent LinkedIn-composer edits to the same Settings files (ctor prefill + save-handler conflicts resolved keeping BOTH). api-server change typechecked clean against the fully-installed toolchain (throwaway-copy `tsc -p` — no new diagnostics referencing the route or web-push.ts). Done in an isolated detached worktree off origin/main (the main tree had several concurrent sessions' uncommitted WIP across MainWindow/SettingsTabView/BuildConsoleSettings), so my commit swept none of theirs. No DB work. No test manifest — desktop/BuildConsole + api-server internal tooling are outside the msp-portal/shane-mccaw-consulting test-coverage scope. NOT live-verified end-to-end (no dev server + subscribed push device reachable from this headless session); Shane restarting his running BuildConsole picks up the scheduler, and a real failing scheduled run is the first genuine push test. | `c6a7419f` |
| 2026-08-13 | ✅ DONE | Git #963 (Epic #803) — UI Automation panel: removed `BtnRecordTest`/`RecordingBadge`, `BtnClearSteps`, and the `AutomationTargetUrl` text box from `AutomationView` (`Controls/LeftSidebar.xaml`) — Shane's own words: "I'll never record myself... Just need to clean up that UI to what is real." Retargeted `BtnPlayTest_Click`: it used to read `AutomationTargetUrl.Text`; now it resolves the target from the currently-selected manifest's own `baseUrl` (tracked in a new `_lastLoadedManifest` field, set in `ManifestFilesList_SelectionChanged`) through `HttpTestExecutor.ResolvePlaceholders` — the same `{{DEPLOY_URL}}` resolution `RunManifestAsync` already applies (per #958) — so Play never fires against an empty/stale URL; guards with a MessageBox if no manifest is selected yet. Removed the now-dead `_isRecording` field and the `BtnRecordTest_Click`/`BtnClearSteps_Click` handlers (both referenced the removed controls, so deleting them was required for a compiling build, not optional cleanup). Kept `ManifestFilesList`, `ManifestBadgesRow`, `BtnPlayTest`, and the "Recorded Test Actions" list/`RecordedSteps` plumbing (`RecordAction`/`AddRecordedStep`, `StartRecordingRequested`/`StopRecordingRequested` events still wired in `MainWindow.xaml.cs`) exactly as-is — untouched per the issue's explicit removal list, even though the two recording events are now unreachable from `LeftSidebar` itself (2 harmless `CS0067` "event never used" warnings; not touching `MainWindow.xaml.cs`'s recording plumbing was a deliberate scope decision, since that file had unrelated concurrent edits in flight from other sessions during this one). #952 had already landed (`58068ffb`) before this session started — confirmed its real landed XAML/badges-flyout state before editing, no conflict. `dotnet build`: 0 errors (2 pre-existing-unrelated-cause warnings noted above; verified by temporarily sidelining other sessions' uncommitted WIP in the same files to get a clean isolated build, then restoring it — none of it touched or committed). No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `01559f27` |
| 2026-08-13 | ✅ DONE | Git #965 (Epic #803) — Enforce isTestbed=true gate on graphTests (#808) and powerShellVerify (#900). Confirmed the real gap from #965's body (no comments): the server-side write-action path (`admin-baseline-templates.ts` `:templateId/test`, line 357 `if (!customer.isTestbed)`) already refuses non-testbed targets, but NEITHER WPF executor did — both relied purely on convention (whoever set `GRAPH_TEST_TENANT_ID`/connected as the right person), with zero runtime proof. New shared `desktop/BuildConsole/Services/TestbedGate.cs` checks a tenant ID against the server's OWN authoritative list — the existing `GET /admin/baseline-templates/testbed-customers` (which already returns exactly `isTestbed=true AND status='active'` customers with a connected tenant) — rather than baking a second copy of the rule into the app; authenticated with the same `apiBaseUrl`/`ingestToken` `BuildTrackerApiClient` uses (the route accepts `BUILD_TRACKER_INGEST_TOKEN` via `requireAdminOrIngestToken`, so it works headless/#898-triggered). FAIL-CLOSED throughout: server unreachable, config missing, empty list, or tenant absent → REFUSE; 30s cache so a manifest full of graphTests doesn't re-fetch per step. `GraphTestExecutor`: gate runs inside the shared `AuthorizeAsync` preflight, right after the existing `GRAPH_TEST_TENANT_ID` equality guard and BEFORE any token is minted or Graph HTTP call made (covers ordinary graph steps AND #878 mail-poll steps, which share `AuthorizeAsync`). `PowerShellTestExecutor`: fetches the GUID-sanitized testbed allowlist first (fail-closed — no fetch, no cmdlet) and injects it into the pwsh script; after `Connect-MgGraph` succeeds the connected `Get-MgContext.TenantId` is emitted (new `__PSVERIFY_TENANT__` sentinel) and checked against the allowlist (new `__PSVERIFY_TESTBED_REFUSED__` sentinel + `PsOutcomeKind.TestbedRefused`), refusing BEFORE the verification cmdlet runs — the deeper check #964 alone couldn't make (not just "TenantId == GRAPH_TEST_TENANT_ID" but "that tenant is genuinely testbed"). Every check (pass OR refuse) logs loudly on the new `testing.testbed-gate` channel via `ActivityLog` — a refusal is never a silent skip. `dotnet build` clean (0 errors; only the 2 pre-existing unrelated CS0067 recording-event warnings from #963). No test manifest (desktop/BuildConsole is outside the test-coverage scope) and no SQL/Shane To-Do (reuses the existing endpoint — no schema change). Landed via isolated worktree at origin/main: three concurrent sessions (#966/#969/#970) were actively editing GraphTestExecutor.cs/PowerShellTestExecutor.cs on the shared checkout and repeatedly clobbered/deleted my edits (incl. their in-flight `HttpTestExecutor.CheckMaxDuration` that doesn't compile yet), so I applied, built, and committed my 3 files in a clean detached worktree instead. | `50580907` |
| 2026-08-13 | ✅ DONE | Git #970 (Epic #803) — Mobile/responsive viewport coverage for uiSteps: optional `"viewport": { "width": N, "height": N }` field (or a `"mobile"`/`"tablet"` preset name string) at the manifest (`TestManifest.ViewportJson`) or per-uiStep level (`ManifestUiStep.ViewportJson` → `AutomationAction.Viewport`), parsed by new `UiTestExecutor.ViewportSpec.Parse`. `UiTestExecutor.RunAsync` resizes `_webView`'s WPF `Width`/`Height` before each step (step override falls back to the manifest-wide default), no-ops when already at the requested size, and restores the default auto-stretch desktop size in a `finally` block covering every exit path (pass, incomplete, nav failure, crash) so a later run or the manual "Play" path sharing the same WebView2 never inherits a stale resize. Wired `ActivityLog.Log("testing.viewport", ...)` + a `Telemetry` "VIEWPORT" card on both resize and restore. Read #970's full issue body (no comments) and `UiTestExecutor.cs`'s real WebView2 setup/navigation code before writing anything, per the task's own instructions. Infrastructure only — no existing manifest retrofitted, no mobile-coverage priority decided (that stays Shane's call). Isolated `dotnet build` clean (0 errors, 2 pre-existing unrelated `CS0067` warnings) — verified by temporarily sidelining an unrelated concurrent session's in-progress, non-compiling `#965` testbed-gate WIP in `GraphTestExecutor.cs`/`PowerShellTestExecutor.cs` (same #963-precedent technique), then restoring it untouched. No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `d61a0613` |
| 2026-08-13 | ✅ DONE | Git #966 (Epic #803) — Screenshot capture during uiSteps runs + review gallery in Test Runner window. `UiTestExecutor.RunAsync` gained a `screenshotDir` param and a best-effort `CaptureScreenshotAsync` helper that uses WebView2's native `CoreWebView2.CapturePreviewAsync(Png, …)` to write `{stepIndex}.png` under `test-results/{issue}-{timestamp}/screenshots/` — captured always on any step failure (DOM/JS action fail, unresolved `{{var}}`, or a failed initial navigation captured as step 0), plus on any uiStep that declares `"screenshot": true` (new `AutomationAction.Screenshot`/`ManifestUiStep.Screenshot`, parsed in `TestManifest`). Capture never throws (a failure just logs and skips), and runs after the per-step `sw.Stop()` so it never inflates `DurationMs`. Each capture is logged to the new `testing.screenshot` channel via `ActivityLog.Log`, recorded on `UiStepResult.ScreenshotPath` (absolute) → carried onto `TestStepResult.ScreenshotPath` and rewritten **repo-relative** (forward slashes) by `RunManifestAsync` before the results JSON is written, so each screenshot is associated with the exact step/run that produced it. `UiTestRunResult.Screenshots` feeds a new click-through review gallery in `TestRunnerWindow` (`GalleryOverlay` full-window overlay + "📷 Screenshots (N)" header button, Prev/Next, counter, caption, path; images loaded with `BitmapCacheOption.OnLoad` so files aren't locked) — deliberately capture + human review only, **no** automated pixel-diffing (fragile against font rendering / anti-aliasing / live data). Manual "Play" path also captures (falls back to a `manual-{timestamp}` folder) and populates the gallery. `TestRunResult` gained a shared `RunFolderName` stem so the screenshots dir and results JSON always share one run-named folder. Built in an isolated git worktree (concurrent sessions #965/#968/#969/#970 were live-editing the same executor/manifest files); `dotnet build` clean (0 errors; 4 pre-existing CS0067 warnings only). Rebased cleanly onto #965/#968/#969/#970 as they landed — reconciled the shared `RunUiTestAsync`/`RunAsync` signature so #970's `defaultViewport` and this issue's `screenshotDir` coexist. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). No DB work. | `177031c4` |
| 2026-08-13 | ✅ DONE | Git #986 (Epic #803) — Environment-gated testbed reset + Graph rollback teardown endpoints. Two dev-only routes under `/api/admin/testbed`, structurally 403-dead outside the dev environment via a `requireDevOrigin` middleware that reuses stripe.ts's EXACT `REPLIT_DOMAINS`/`.replit.dev` check — extracted to a new exported `isReplitDevEnvironment()` that `getStripeKey`/`getStripePublishableKey` now also call, so there is ONE dev/prod mechanism, not two (per #986: "do not invent a second dev/prod detection mechanism"). Per Shane's scope decision the environment gate IS the safety boundary — no per-row isTestbed filtering ("any tenant in the dev environment... is a test tenant"). Both routes are `requireAdminOrIngestToken` AFTER the gate so the headless #898 BuildConsole harness reaches them with `BUILD_TRACKER_INGEST_TOKEN`. (1) `POST /admin/testbed/reset` takes an explicit `customerId` (tenants.id) — never a blind full-DB wipe — and in ONE transaction clears that customer's `tenant_engine_overrides` (injected sim Graph overrides) + `baseline_action_template_audit_log` rows (write-action history; matched by `afterSnapshot->>'customerId'` as TEXT, cast-safe so a stray non-numeric value can't throw the txn); the reset SQL lives in the endpoint (drizzle delete + `.returning()` counts), never run in this session. (2) `POST /admin/testbed/teardown-graph-writes` reverses real M365 Graph writes by an explicit `auditLogIds` list (each `/execute` returns one), each undone via Launch Control's own `rollbackExecution()` reverse-template design (self-pair boolean-invert / Teams-membership special case) — no second undo mechanism; per-item results, one failure never aborts the rest. Every reset/teardown call + every gate decision (pass AND block) logs loudly on the new `admin.testbed` channel with what/from-where (host, ip, x-forwarded-for, user, REPLIT_DOMAINS). Unblocks #987 (real blocked_by dependency). `tsc --noEmit`: 71 errors, byte-identical to the pre-change baseline — zero in changed files. NOT live-verified from this headless session (no running dev server reachable; routes go live once the dev server pulls+restarts). No manual SQL/schema change and no new secret — reuses existing `REPLIT_DOMAINS` + `BUILD_TRACKER_INGEST_TOKEN`. No test manifest — api-server is outside the msp-portal/shane-mccaw-consulting test-coverage scope. | `bf5f5b43` |
| 2026-08-13 | ✅ DONE | Git #968 (Epic #803) — Test result history + trend tracking. Pulled #968's full body (no comments) plus #803's umbrella epic and the real current test-results output shape from #812/#874 first: `ManifestRunResult.WriteToFile` (`Services/TestRunResult.cs`) is the single choke point every trigger path already funnels through (menu Run, regression sweep, #898 remote-trigger poll) — one call site, so hooking history there covers all of them with no per-caller wiring. New `Services/TestHistoryStore.cs` appends one JSON line per run to `test-results/_history.jsonl` (issue, feature, mode, startedAt, pass/fail counts, overall result, result file name) — newline-delimited on purpose, not a JSON array rewritten whole each run, per CLAUDE.md's Shared File Write Discipline section and the issue's own explicit ask to avoid that collision shape. New `TestHistoryWindow` (sibling of `TestRunnerWindow`, opened via new Run > "Test History…" menu item) shows a chronological "Runs (newest first)" list plus a "Per-Manifest Streaks" panel (e.g. "failed the last 2 runs"), both computed fresh from the full history on each Refresh rather than a second persisted shape that could drift from the raw entries. No DB work needed/attempted. Verified: `dotnet build` clean, 0 errors, 0 new warnings (2 pre-existing unrelated `CS0067` warnings only). Not live-verified — no history entries exist yet to populate the window against (this lands the first thing that starts writing them); Shane should confirm Run > Test History… opens and a manifest run appends a visible row. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `cd0869ef` |
| 2026-08-13 | ✅ DONE | Git #969 (Epic #803) — Performance/timing thresholds on manifest steps: optional `"maxDurationMs"` field on any step type (apiTests/graphTests/postGraphApiTests/zohoTests/uiSteps/powerShellVerify). Pulled #969's full issue body (no comments; also read parent Epic #803's issue body + its one comment) and the real per-step timing already tracked in `HttpTestExecutor.cs`/`GraphTestExecutor.cs`/`UiTestExecutor.cs`/`ZohoTestExecutor.cs`/`PowerShellTestExecutor.cs` (each already runs a `Stopwatch` per step for logging/`TestStepResult.DurationMs`) before writing anything. New shared `HttpTestExecutor.CheckMaxDuration` (two overloads: raw `JsonElement` step for apiTests/graphTests/zohoTests/powerShellVerify, and `long?` for uiSteps) is the single evaluator every executor calls against its own already-tracked elapsed ms — same sharing pattern as the existing `EvaluateExpectation`/`EvaluateContentAssertions`. A step exceeding its threshold is marked FAILED (this codebase's `TestStepResult.Passed` is a bool, no tri-state WARN, so functional-but-slow steps fail rather than soft-warn) with the real actual-vs-threshold ms folded into `Detail`, even when its functional assertions all passed. `uiSteps` thread `maxDurationMs` through `ManifestUiStep` → `Controls.AutomationAction` (that step type isn't raw JSON like the others) into `UiTestExecutor.ExecuteStepAsync`'s existing `overallPassed`/`stepDetail` computation. Built and pushed from an isolated worktree (`C:\Source\ShaneMcCawConsulting\wt-969`): three other concurrent sessions (#961, #965, #966/#970) were actively editing these exact same executor files in the primary tree, and a first attempt there had its `HttpTestExecutor.cs` edit silently clobbered — redid the work cleanly against fresh `origin/main` in the worktree instead, resolving two genuine (non-overlapping, same-insertion-point) rebase conflicts against #970's viewport work in `TestManifest.cs`/`LeftSidebar.xaml.cs`/`MainWindow.xaml.cs` by keeping both additions. `dotnet build` clean both before and after the final rebase (0 errors; only the 2 pre-existing unrelated `CS0067` warnings). No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `2461495b` |
| 2026-08-13 | ✅ DONE | Git #973 — LinkedIn post pre-fill via WebView2 (BuildConsole WPF, not Epic #803). New Settings field "LinkedIn Post Composer Selector" (#902 pattern), a #937-shaped always-on-top compose floaty with a "Send to LinkedIn" action that opens/focuses a LinkedIn WebView2 tab and injects the post text into the real compose box via the #871 native-setter + #922 dispatch-events technique (never clicks Schedule/Post — Shane completes that himself), and a clear inline recalibrate message when the selector finds no element. Log linkedin.prefill. | `acc4a684` |
| 2026-08-13 | ✅ DONE | Editor Tab "Pinned" state (BuildConsole WPF, Epic #803) — pin a #893/#894 editor tab so it drops out of the normal tab row into a small always-visible strip item (icon + short label) while its WebView2 session stays genuinely alive in the background. Keep-alive follows the exact HwndHost rule the #902 ReplitWatcherWebView comment already documents: a WebView2's `Opacity` is read-only and `Visibility=Hidden/Collapsed` (or a zero size) tears down / never creates the CoreWebView2 controller — so a pinned tab's live content is NOT hidden, it's parked at full size in a new overlay `PinnedHostCanvas` (added in `MainWindow.xaml`, overlapping the panes) and slid far OFF-screen (`Canvas.Left = -20000`, still laid out, still rendering, session never suspended). One click on the strip item (or its `Open (Full View)` context item) slides it back to `0,0` over the panes and flips the canvas hit-testable; collapse slides it back off-screen. Nothing is reparented while pinned, so there is zero WebView2 churn/reflow between collapse↔expand — the session just keeps running. `Unpin` reparents the still-live content back into a fresh normal tab in the active pane (rebuilt header + close + `AttachTabContextMenu`/`AttachTabDragHandlers`). Pin entry point is a new "Pin Tab" item in the existing `AttachTabContextMenu`. All logic lives in a new isolated partial-class file `MainWindow.PinnedTabs.cs` (kept out of the concurrently-edited `MainWindow.xaml.cs`, #921 pattern); the only edits to shared files are the two XAML additions (strip bar + overlay canvas), one context-menu item, and one startup seed call. LinkedIn is the first real use: `SeedPinnedLinkedIn()` (deferred to `DispatcherPriority.Loaded` so the canvas has layout extents) builds a self-contained nav-toolbar+WebView2 panel via `CreatePinnedWebContent`, sharing the app's WebView2 environment (cookies/session) so Shane stays logged in, and registers it as a collapsed pinned entry — always one click away, never taking a tab slot. `ActivityLog.Log("tabs.pinned", ...)` fires on pin / unpin / collapse / expand (and the LinkedIn seed). Isolated `dotnet build -t:Rebuild` clean: 0 errors, only the 2 pre-existing unrelated `CS0067` warnings (`StartRecordingRequested`/`StopRecordingRequested`, counted twice across the real + `_wpftmp` project). Built/committed/pushed from an isolated worktree (`C:\Source\ShaneMcCawConsulting\_wt-pinned-tabs`) off fresh `origin/main` because the primary tree had uncommitted #965/#966/#970 WIP in `MainWindow.xaml.cs` from concurrent sessions; my code hunks did not overlap theirs (rebase applied cleanly; only PLATFORM_BUILD.md needed an append-conflict resolve). Not live-verified in the running GUI — can't launch/restart Shane's own BuildConsole from here; Shane should confirm the 📌 PINNED strip shows a LinkedIn item on launch, that clicking it opens LinkedIn full-view logged in, and that right-click → Pin Tab on any web tab collapses it while keeping its session running. No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `6a0c0452` |
| 2026-08-13 | ✅ DONE | Git #972 (revised scope) — pinned/persistent web-page chips (icon + short label) filling the previously-empty middle of MainWindow.xaml's "EDITOR PANES" header bar (the DockPanel above EditorGridContainer), between the "EDITOR PANES" label (now docked left) and the Single/Side-by-Side/Top-Bottom/4-Squares layout buttons (docked right). Two chips, `💼 LinkedIn` and `🖥 Replit` (BtnChipLinkedIn/BtnChipReplit), centered in the remaining fill space. Clicking a chip opens/focuses that persistent WebView2 session — kept **genuinely alive off-screen** in #972's landed `PinnedHostCanvas` keep-alive overlay at full size (HwndHost rule: never suspended/torn down, same as ReplitWatcherWebView #902) — without consuming a tab slot or touching the current pane layout; clicking an already-open chip tucks it back off-screen (session stays running). Reuses #972's landed pinned-session machinery rather than duplicating it: `RegisterPinnedEntry` gained an `addToStrip` flag so these entries surface via the header bar instead of the 📌 PINNED strip (no double-up), `CreatePinnedWebContent` builds the live nav-toolbar+WebView2 panel (shared WebView2 env → stays logged in), `ExpandPinned`/`CollapsePinned` drive show/hide; `PinnedTabEntry` gained a nullable `ChipButton` so the active chip is highlighted on expand/collapse (StripButton uses null-guards throughout now). Old `SeedPinnedLinkedIn()` replaced by `SeedPinnedWebChips()` → `SeedPinnedWebChip(url,label,glyph,chip)`, seeding **both** LinkedIn and Replit Workspace at startup (`DispatcherPriority.Loaded`, so the canvas has layout extents and the x:Named chip buttons exist) so both sessions are warm from launch — this is the "relocate LinkedIn's entry point to the bar + add Replit" the revised issue asked for. `FocusPinnedWebChip` handles the chip clicks (lazily recreates if an entry was unpinned/torn down). `ActivityLog.Log("tabs.pinned", ...)` on chip seed and open/focus. Isolated `dotnet build -t:Rebuild` clean: 0 errors, only the 4 pre-existing unrelated CS0067 recording-event warnings (StartRecordingRequested/StopRecordingRequested, counted across real + _wpftmp). Built/committed/pushed from an isolated worktree (`C:\Source\SMC-wt-972chips`) off fresh `origin/main` — the primary tree had two unrelated concurrent-session dirty files (`GraphTestExecutor.cs`/`PowerShellTestExecutor.cs`, left untouched); my three files (MainWindow.PinnedTabs.cs / .xaml / .xaml.cs) don't overlap #972's original hunks (built on top of its landed 6a0c0452). Not live-verified in the running GUI (can't launch/restart Shane's own BuildConsole from here) — Shane should confirm the 💼 LinkedIn + 🖥 Replit chips appear in the EDITOR PANES bar and that clicking each opens its live session over the panes. No DB work. No test manifest — desktop/BuildConsole is outside the msp-portal/shane-mccaw-consulting test-coverage scope. | `8591f171` |
| 2026-08-13 | ✅ DONE | Screenshot baseline storage + visual-diff detection + review/approval presentation dialog (BuildConsole WPF, Epic #803) — extends #966's capture pipeline. On a manifest run's first approval, promote its screenshots to a persistent baseline at `test-results/baselines/{manifest-area}/{feature-slug}/{stepIndex}.png` (source of truth, only overwritten by explicit re-approval). Every run diffs each captured screenshot against its baseline using a downscaled similarity threshold (not exact-pixel; robust to AA/font noise); no baseline yet = always needs review. New `ScreenshotReviewWindow` shown automatically ONLY when a baseline is missing or a meaningful diff is found (never on a clean matching run) — walks screenshots one at a time with narration (step, what was verified, pass/fail, diff %) and two end actions: "Report issue" (reuses #937's `SendTextToActiveClaudeChatAsync` compose+send, pre-seeded with context) and "Approve" (promotes the run's screenshots to baseline, sends a landed-and-verified note to the active chat, closes the manifest's associated GitHub issue via `GitHubApiClient.SetIssueStateAsync`). Built as a general `ScreenshotReviewService` keyed on (area, slug, issue) so ALL screenshot-producing runs going forward (including a future mailer audit) reuse it — wired into the one existing producer (`RunManifestAsync`'s uiSteps run). Logs `testing.screenshot-review` (`ScreenshotReviewService`) + `testing.baseline` (`ScreenshotBaselineStore`). Pulled #966's real landed capture pipeline (`UiTestExecutor.CaptureScreenshotAsync` → `UiScreenshotCapture` → `RunFolderName`/`test-results/{run}/screenshots/{stepIndex}.png`) and #937's real send-to-active-chat mechanism (`MainWindow.SendTextToActiveClaudeChatAsync` → ProseMirror composer inject, never presses Enter) and reused both directly — not reimplemented. Image compare is 100% in-box WPF (`RenderTargetBitmap` downscale both PNGs to a fixed 64×64 grid → mean per-channel diff, 6% tolerance = a similarity threshold, deliberately NOT the exact-pixel match #966 rejected as fragile) so nothing new has to restore from NuGet; baselines live under the `.gitignore`'d `test-results/baselines/{area}/{slug}/` — Shane's local source of truth, only ever written on an explicit Approve, never swept into a commit. Manifest→issue association is traced from the manifest's own on-disk `{area}/{feature-slug}` path + `Issue` field (not guessed). Baselines require captured screenshots, and #966 only captures on a step failure or a `"screenshot": true` opt-in — so to baseline a passing flow, mark its steps `"screenshot": true`. Confirmed repo-wide there is no desktop "mailer audit" (or any non-uiStep) screenshot producer yet — the general `ScreenshotReviewService.ReviewIfNeededAsync(subject, shots, sendToChat, interactive)` covers "all screenshot-producing runs going forward incl. the mailer audit" by construction the instant one is added; the single existing producer (`RunManifestAsync`'s uiSteps run) is wired now (3 additive hunks). Non-interactive runs (scheduled #967 sweep + #898 headless remote-trigger, gated `!isRegression && !_testTriggerBusy`) compare + log but suppress the modal so a background/3am run can never hang. Built + committed + pushed from an isolated worktree (`C:\Source\ShaneMcCawConsulting\wt-966-screenshot-review`) off fresh `origin/main` because the primary tree had a concurrent session's uncommitted #965 WIP and #966's own work wasn't even on local `main` (13 behind); `dotnet build` clean after rebasing onto current `origin/main` (0 errors; only the 2 pre-existing unrelated `CS0067` warnings — `Start/StopRecordingRequested`). Not live-verified in the running GUI (can't launch/restart Shane's BuildConsole from here) — Shane should run a manifest whose uiStep declares `"screenshot": true` (or one that fails a step), confirm the review window pops on the first run (no baseline) and on a later visual change but NOT on a clean matching re-run, and that Approve sets the baseline + drops a note into the active Claude chat + closes the linked issue. No Git # is tied to this task → no issue label sync (the Approve button closing an issue is a runtime action on the manifest's OWN issue, Shane's deliberate click). No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `60852be7` |
| 2026-08-13 | ✅ DONE | Claude usage meter status-bar indicator (BuildConsole WPF, Epic #803) — always-visible live Claude usage percentage in the status bar. New `Services/ClaudeUsageMeterService.cs` runs a 10-minute `DispatcherTimer` poll (same shape as #902's Replit watcher) that navigates its OWN dedicated hidden 1x1 `UsageMeterWebView` (a second background WebView2 — never fights the Replit one over navigation — sharing the app's WebView2 environment/cookies via `EnsureWebViewInitializedAsync` so it reads the page inside Shane's claude.ai login) to `claude.ai/settings/usage`, then extracts the real `aria-valuenow`/`aria-valuemax`/`aria-valuetext` off every `[role="meter"]` plus the first "Resets …" label in one JS pass, via the #832 `Navigate()`-not-`.Source` + #864/UiTestExecutor double-JSON-unwrap `ExecuteScriptAsync` pattern. Percentage is derived in C# (`ComputePercent`: prefers an explicit "NN%" in valuetext, else valuenow/valuemax, defaulting max=100; picks the most-constraining meter when several exist). New StatusBar item (`UsageDot` + `UsageStatusText`, its own two grid columns before the clock) shows `Claude: N% used`; on ANY extraction failure — not logged in, page structure changed, meter/label absent, or navigation failure — it drops the stale number and shows a muted `Claude: --` (grey dot) rather than a wrong number or a crash (every path is guarded; the poll never lets ticks stack and never throws out). The "Resets Sun 9:00 PM" label is parsed (`ParseResetTarget`) into the real next-occurrence LOCAL datetime — weekday+time rolls to next week when this week's slot is already past; bare time rolls to tomorrow when today's is past; unparseable → no countdown. Only at usage ≥85% is a `resets in Xd Xh Xm` countdown (`FormatCountdown`, omits leading-zero units) appended; it's ticked SMOOTHLY every 30s by a second display timer computing off the already-known reset target, so only the underlying percentage waits on the real 10-min poll. `ActivityLog.Log("usage-meter", …)` on every poll attempt/success/failure. Pure helpers (`ParseResetTarget`/`ComputePercent`/`FormatCountdown`) verified against a throwaway net7 console copying them verbatim — all weekday/bare-time/roll-forward, percent-derivation, and countdown-format cases correct. Isolated `dotnet build -t:Rebuild` clean: 0 errors, only the 4 pre-existing unrelated `CS0067` recording-event warnings (counted across real + `_wpftmp`). Built/committed/pushed from an isolated worktree (`C:\Source\wt-usagemeter`) off fresh `origin/main` because the primary tree had two concurrent-session dirty files (`GraphTestExecutor.cs`/`PowerShellTestExecutor.cs`, #965 WIP — left untouched). Not live-verified in the running GUI (can't launch/restart Shane's own BuildConsole from here, and it depends on a live claude.ai login + the real current DOM of /settings/usage) — Shane should confirm the `Claude: N% used` indicator appears (or a muted `Claude: --` when logged out) and, near a limit, the countdown ticks. If the selectors need calibration against the live page, the extraction JS lives in `ClaudeUsageMeterService.ReadMeterAsync`. No DB work. No test manifest — desktop/BuildConsole is outside the msp-portal/shane-mccaw-consulting test-coverage scope. | `7b12ccf6` |
| 2026-08-13 | ✅ DONE | Git #977 (Epic #803) — always-on screenshot capture for every uiSteps run. Changed only the default capture *trigger* in `UiTestExecutor.ExecuteStepAsync` (not the capture mechanism itself — reused #966's `CaptureScreenshotAsync` → `CoreWebView2.CapturePreviewAsync(Png)` → `UiScreenshotCapture` pipeline as-is): the old `if (!overallPassed || step.Screenshot)` gate became an unconditional `CaptureScreenshotAsync(...)` on every step, so a screenshot is now taken at each UI test step by default, pass or fail, for every existing and future UI-driven manifest in the regression suite. This is what gives #975's baseline/diff/approval workflow (`ScreenshotReviewService`) something to compare on a *passing* run — a step can pass all DOM/text assertions while drifting visually, so drift is only catchable suite-wide with unconditional capture. Capture stays best-effort (never throws; no-ops when `screenshotDir` is null / capture disabled), so making it unconditional can't fail a run, and it still runs after the #969 `sw.Stop()` so it never inflates `DurationMs`. #966's per-step `"screenshot": true` flag is now redundant for uiSteps (always-on supersedes it) and retained only to colour the capture reason ("explicit" vs "step" vs "step-failed"); the unresolved-`{{var}}` failure path already captured and is unchanged. Doc comments on `RunAsync`'s `screenshotDir` param and `UiTestRunResult.Screenshots` updated to state always-on. Built in an isolated `git worktree` off fresh `origin/main` (local `main` ~13 behind; #966/#975 only landed on `origin/main`, and the primary tree held a concurrent session's uncommitted #965 WIP left untouched); `dotnet build` clean — 0 errors, only the 2 pre-existing unrelated `CS0067` warnings (`Start/StopRecordingRequested`). Not live-verified in the running GUI (can't launch Shane's BuildConsole from here) — on the next run of any existing UI manifest, expect `ScreenshotReviewService` to pop the review dialog because no baseline exists yet for the now-captured passing steps; that's the intended first-baseline flow, Shane approves to seed. No DB work. No test manifest — desktop/BuildConsole is outside the test-coverage scope (msp-portal / shane-mccaw-consulting only). | `751a6b9f` |
| 2026-08-13 | ✅ DONE | Git #971: BuildConsole (WPF) Sessions tile hang risk fixed + In-Flight/Sessions layout parity with Completed/To-Do. Shane: "The Build Queue needs to show even when I am building using 'Send to Builder' since that opens a different process... I need a way to know it's running. I think this is what Session is for? But it's always empty. So maybe a bug is there? Also, In-Flight and Sessions should not be a 2 column layout, they should work the same as Completed & To-Do." Confirmed "Send to Builder" (`ChatButtonInjector.cs`) launches via a `mybuilder://` URI handled by `scripts/run-claude.ps1`, which runs `claude.exe --name <title>` directly — a real process outside the queue, exactly as Shane described — and that `ClaudeAgentsService.ListActiveSessionsAsync()` (the Sessions tile's data source) IS the right mechanism, reading the real claude CLI's own agent list machine-wide. Found a real bug in it: redirected both stdout and stderr but only ever read stdout, sequentially, before waiting for exit — a textbook .NET Process deadlock if the CLI ever writes enough to stderr to fill the OS pipe buffer (an update notice, a deprecation warning), since the child blocks writing while this call sits blocked reading stdout. That reads as "Sessions is always empty" with no error, not a crash. Fixed by reading both streams concurrently via `Task.WhenAll`, and added `ActivityLog` logging on every non-empty failure path (exit code, stderr, parse failure) so a future failure is actually diagnosable. Also replaced the old 2-column `Grid` for In-Flight/Sessions with the same full-width single-column `StackPanel` shape #941 established for Completed/To-Do: same mutually-exclusive accordion (expanding one force-collapses the other), same removed `MaxHeight="220"` cap so an expanded list uses its full natural height, same `CharacterEllipsis` + explicit `MaxWidth` title trimming (the #932/#941 lesson — trimming never engages without a real numeric `MaxWidth`). Survived heavy concurrent-session churn to land: caught this same content getting wiped by other sessions' `git reset --hard` events (visible in reflog) and a cosmetic issue-number correction getting swept away twice before it stuck — the functional commit itself was never at risk, verified safely present in history throughout. No DB work needed/attempted. Verified: `dotnet build` clean, 0 errors, 0 warnings (only pre-existing unrelated warnings from other in-progress work in `LeftSidebar.xaml.cs`). Not live-verified against an actual stderr-fill hang (couldn't reproduce non-empty stderr from the real CLI in this environment) — Shane should confirm Sessions now reliably shows real entries including a live Send-to-Builder launch, and that In-Flight/Sessions now look and behave like Completed/To-Do. | `278a13b2` |
| 2026-08-13 | ✅ DONE | Git #942 (Epic #803) follow-up — ChatButtonInjector both-bars live-status fix (BuildConsole WPF). Root cause: `__btTagQueued` used a singular `querySelector` matching only the clicked button's correlation token, so the sibling bar's Queue button (top vs bottom, whichever wasn't clicked) never received `data-bt-queue-id` and stayed stuck on "📋 Queue" regardless of real build status. Fix: `scan`/`buildButtonBar` stamp a shared per-block id on BOTH bars' Queue buttons; `__btTagQueued` finds the clicked button, then tags every Queue button sharing that block id with the same `data-bt-queue-id`. Then in `__btApplyStatus` — which already updates every button by shared queue id — also hide each bar's sibling "Send to Builder" button while queued/in-progress/done, restoring it when the build fails (mode === "failed", matching the retry-able state), uniformly across both bars now that both are reachable. No existing shared identifier fit (checked: `referencedNumber` isn't stamped on the buttons, is null for non-issue prompts, and is non-unique across blocks; `btOrigLabel` is identical on all) — so `scan` now mints one per-block `data-bt-block` id and stamps it on both bars' Queue buttons. Changes are entirely inside `ChatButtonInjector.cs`'s injected JS raw-string (no C# structural change); `dotnet build` clean (0 errors, only the 2 pre-existing unrelated `CS0067` recording-event warnings). Not live-verified in the running GUI (can't restart Shane's BuildConsole from here) — Shane should confirm a Queue click now flips BOTH the top and bottom bars to live status and hides each bar's "Send to Builder" until/unless the build fails. No DB work. No test manifest — desktop/BuildConsole is outside the msp-portal/shane-mccaw-consulting test-coverage scope. | `c47831f0` |
| 2026-08-13 | ✅ DONE | Git #976 (Epic #803) — Audit all 27 real DB `email_templates` slugs: trace each slug's REAL send code path and classify it (DB-row-used-and-wrapped-in-branded-layout / DB-row-used-but-NOT-wrapped / still-calling-old-hardcoded-mailer.ts-function / no-real-trigger-path-found). Confirm each fetches the `email_templates` DB row (via `sendEmailFromTemplate`/`getEmailTemplateOrFallback`) and that `sendEmail`/`sendEmailForMsp` wraps it in `branded-layout`'s `{{body}}` substitution (except `branded-layout`/`tenant-health-block` themselves). Screenshot-gate rendered composed emails via #975's baseline/diff/approval workflow. Register manifests under `test-manifests/mailer/` + `_regression-suite.json`. Per-template breakdown is the deliverable. **RESULT** (traced vs `555278b4`): **20 DB-row-used-and-correctly-wrapped** (all via `sendEmailFromTemplate`/`getEmailTemplateOrFallback`; every path wraps in `branded-layout` — the 2 PDF-attachment paths `quiz-report-email`/`service-overview-email` explicitly `await brandedEmail()` before `sendEmailWithAttachment`), **0 DB-used-but-NOT-wrapped**, **1 still-calling-old-hardcoded-function** (`purchase-confirmation`: `public-assessment-payment.ts:865` sends the hardcoded `purchaseConfirmationEmail()` and never reads the DB row — real drift; still branded, but Admin-Panel edits to the row do nothing), **4 no-real-trigger-path** (`client-thread-reply` + orphan rows `kanban-script-exhausted`/`kanban-document-exhausted`/`manual-script-escalation`, the latter 3 not even code-seeded), **2 wrapper/snippet exempt** (`branded-layout`, `tenant-health-block`). Deliverables: `test-manifests/mailer/email-template-branding-audit.json` (#892 apiTests assert DB truth + the `{{body}}` wrapping contract; uiSteps drive the Admin Panel email-template preview across all 27, screenshot-gated per #975 w/ #977 always-on capture, each step named with its verdict) + `README.md` full findings table; scoped data-testids on `EmailTemplates.tsx`; read-only diagnostic SQL at `lib/db/migrations/manual/archive/diagnostics/2026-08-13-email-template-audit-976.sql`. Audit-only — no send-code changes (purchase-confirmation fix + orphan-row cleanup are Shane's calls, see `Shane To-Do`). Not executed here (no BuildConsole/DB/mail access) — run is Shane's. | `3beaa574` |
| 2026-08-13 | ✅ DONE | Git #980 — Floaty "Build Watch" panel for BuildConsole (WPF): a new independent, draggable-to-any-monitor, resizable top-level window (not docked in MainWindow, position/size persisted in BuildConsoleSettings) with an 8-slot grid. Each occupied slot streams a live build's output by reusing the existing #802/#825 per-item log-tail mechanism (BuildLogPaths.ForQueueItem tail). On completion a green overlay/border is applied (red for a genuine failure), with a per-slot Dismiss button. A new running build takes an empty slot, else auto-evicts the OLDEST completed slot; when all 8 are still running it waits for a slot rather than force-evicting a running build (documented edge case). Also auto-removes a slot when its build's GitHub issue closes (reusing GitHubIssuesService.GetOpenIssueNumbersAsync). Completion detection uses the #943-informed reliable signal — it does NOT trust the queue Status alone: a failed/canceled row carrying the orphan-sweep sentinel exitCode -2 is treated as still-running until a real terminal result lands, since #943 proved -2 can be a false-failure on a still-live process. Logs slot occupied/completed/dismissed/auto-evicted/issue-closed-removal on the build-watch channel. Build-verified (`dotnet build` clean, 0 errors; only pre-existing unrelated CS0067 warnings); not live-GUI-verified (can't launch Shane's BuildConsole from here) — Shane should confirm the window opens, streams a live build, greens a completed slot, and auto-clears on issue close. No DB work. Outside the msp-portal/shane-mccaw-consulting test-coverage scope, so no manifest. | `b7ee346c` |
| 2026-08-13 | ✅ DONE | Git #981 — Web Tools popout: add Git repo as a default entry. Not part of Epic #803; small follow-up to #864. Added the real GitHub repo URL (https://github.com/shanemccaw/Shane-McCaw-MSP) as a fourth default Web Tools entry alongside LinkedIn/Google Analytics/Microsoft Clarity (`BuildConsoleSettings.WebTools` field initializer), plus a one-time migration in `BuildConsoleSettings.Load()` that backfills it into Shane's existing settings.json (whose saved WebTools list already overrides the field-initializer defaults, so it wouldn't have picked up the new default on its own) and re-saves. `dotnet build` clean, 0 errors, 0 warnings. Not live-verified in the running GUI. No DB work. No test manifest — desktop/BuildConsole is outside the msp-portal/shane-mccaw-consulting test-coverage scope. | `0650b88c` |
| 2026-08-13 | ✅ DONE | Git #975 (bookend restoration) — real DONE bookend commit `778f393b` was lost to a shared-file collision (same recurring pattern as #901/#921). Verified independently: `Services/ScreenshotBaselineStore.cs`, `Services/ScreenshotReviewService.cs`, `ScreenshotReviewWindow.xaml(.cs)` all real and present. Baseline/diff/review workflow built on top of #966's real capture pipeline and #937's real send-to-active-chat mechanism, per real commits `36ee8f6c` (IN FLIGHT) → `60852be7` (work) → `778f393b` (DONE). Confirmed honest caveat: no mailer-audit screenshot producer exists yet in the desktop app (#976 still blocked/not built) - the review service is general and will be used by construction once one exists. Not live-verified in a running GUI session - Shane should confirm the review window pops on first run and on a real visual change, but not on a clean matching re-run. | `36ee8f6c`, `60852be7`, `778f393b`|
| 2026-08-13 | ✅ DONE | Epic #803 follow-up — fix `PopulateManifestsList()` (BuildConsole `LeftSidebar.xaml.cs`) for #960's nested `test-manifests/{area}/{feature-slug}.json` structure: switched to recursive `Directory.GetFiles(..., SearchOption.AllDirectories)`, list entries as `Path.GetRelativePath`-relative paths instead of bare filenames (existing `Path.Combine` load logic in `ManifestFilesList_SelectionChanged` keeps working unchanged against a relative path), excludes `_regression-suite.json` (the index file, not a runnable test). No wiring changes needed — existing `testing.manifest-list` log line now reports the real recursive count. `dotnet build` clean, 0 errors (2 pre-existing unrelated warnings). No DB work. Outside msp-portal/shane-mccaw-consulting scope — no test manifest. | `c41852b2` |
| 2026-08-13 | ✅ DONE | Git #972 (follow-up bug fix) — collapsed pinned WebView2 (LinkedIn/Replit EDITOR PANES header chip) visually covered the real document tabs/content instead of being genuinely out of view. Root cause: a WebView2 is an HwndHost whose hosted surface only reliably repositions on a SIZE change — `ExpandPinned` resizes (`SizePinnedBorder`) then moves to 0,0 (worked), but `CollapsePinned` only did `Canvas.SetLeft(-20000)` with no size change, so the live surface stayed painted at 0,0 over the panes (z-order can't occlude an HwndHost — it always paints over WPF content, so off-screen is the only lever). Fix (`MainWindow.PinnedTabs.cs` only): new `ParkOffscreen(host)` shrinks a collapsed host to a 1x1 footprint AND moves it off-screen in both axes (`PinnedOffscreenX/Y = -20000`, `PinnedCollapsedSize = 1`) — the size change is what forces the WebView2 to actually update its rendering bounds and leave view; 1x1 is nonzero so CoreWebView2 stays alive and the session stays logged in / never reloads (the exact keep-alive trick ReplitWatcherWebView/UsageMeterWebView #902 use). `CollapsePinned` + registration seed + `PinnedHostCanvas_SizeChanged` all route through it; only the currently-expanded host now tracks canvas size (collapsed ones stay 1x1 so they can't resurrect the covering risk). `ExpandPinned` unchanged (already grows to full + moves to 0,0). Keep-alive design from #972 preserved — nothing reparented while pinned, expand/collapse only reflows the page. `ActivityLog.Log("tabs.pinned", ...)` on collapse now includes the computed off-screen position + size for diagnosis if it recurs. `dotnet build -t:Rebuild` clean: 0 errors, only the 4 pre-existing unrelated CS0067/CS8619 LeftSidebar warnings (counted across real + _wpftmp). Not live-verified in a running GUI (can't launch Shane's BuildConsole from here) — Shane should confirm: click a chip to open LinkedIn over the panes, click again to collapse, and the editor is fully visible again (no ghost WebView2), then reopen to confirm it's still logged in / didn't reload. No DB work. No test manifest — desktop/BuildConsole is outside the msp-portal/shane-mccaw-consulting test scope. | `17e9ea50` |
| 2026-08-13 | ✅ DONE | Git #984 (Epic #803) — convert `ManifestFilesList` (BuildConsole `LeftSidebar.xaml`) from a flat `ListBox` to a collapsed-by-default, color-coded-by-area `TreeView`. Top-level nodes = area (first path segment) rendered with a small palette color chip + the area name in that accent + a real count badge (e.g. `copilot-readiness (6)`); areas alphabetical as a stable top-level index, `IsExpanded=false` by default. Leaf nodes = bare manifest filenames, each carrying #983's exact relative path in `Tag` (unchanged load key — `ManifestFilesTree_SelectedItemChanged` still does `Path.Combine(manifestsDir, tag)` and opens #952's steps flyout, so click-to-load behavior is byte-for-byte preserved; area headers carry no Tag so clicking one only expands/collapses). Each area gets a stable existing-palette accent (deterministic hash of the area name over Blue/Mauve/Green/Peach/Yellow/Red brushes, so an area keeps its color regardless of which others are present) — header text + chip colored, leaves get a thin left color bar (text stays `TextBrush` so a red area never reads as an "error"). Search box above the tree reuses the Build Queue #950 `QueueSearchBox` overlay-placeholder pattern: filters by filename OR area (case-insensitive substring); a matching area shows all its children, matching areas auto-expand, non-matching areas/leaves drop out, clearing restores the collapsed default. Leaves sort by real `File.GetCreationTimeUtc`, most recent first (tie-break filename). `_manifestEntries` caches the disk scan so search re-renders without re-hitting disk; existing `testing.manifest-list` log line unchanged (still reports real recursive count). Builds on #983 (recursive relative-path scan) + #952 (full-height + flyout) — replaces neither. `dotnet build -t:Rebuild` clean: 0 errors, only the 2 pre-existing unrelated CS0067 (`Start/StopRecordingRequested`) warnings (×2 real+_wpftmp = 4). Not live-verified in a running GUI (can't launch Shane's BuildConsole from here) — Shane should confirm the tree groups by area, collapses by default, colors distinctly, search filters + auto-expands, and a leaf click still flies out the steps. No DB work. Outside msp-portal/shane-mccaw-consulting scope — no test manifest. Note: found an unrelated uncommitted `Services/ClaudeAgentsService.cs` change in the working tree (Sessions debug logging, another session's WIP) — left it untouched, not swept into this commit. | `e814848c` |
| 2026-08-13 | ✅ DONE | Git #988 — UI Automation flyout: fix `ManifestStepsPopup` (`Controls/LeftSidebar.xaml`) `StaysOpen="False"` race causing show-then-instant-close. Root cause confirmed by event-timing audit: the popup was opened synchronously inside `ManifestFilesTree_SelectedItemChanged`, itself fired mid-dispatch of the tree's own mouse-up — WPF's `StaysOpen="False"` auto-close listens for exactly that kind of "outside click" and read the still-in-flight triggering click as one, closing the popup the instant it opened. Fix: `StaysOpen="True"`, explicit dismissal wired via new `Opened`/`Closed` handlers. On `Opened`, the fix hooks the owning Window's `PreviewMouseDown`/`PreviewKeyDown` via `Dispatcher.BeginInvoke(DispatcherPriority.Input)` — deferred to run only after the current mouse-up has fully finished dispatching, so the triggering click can never self-trigger the new close path either. Because the Popup has `AllowsTransparency="True"` it renders in its own top-level layered window (confirmed: WPF popups with `AllowsTransparency` get a real separate HWND), so clicks anywhere inside it — including the existing `BtnCloseStepsFlyout` close button — never route through the owning Window's handlers at all; any `PreviewMouseDown` that reaches the Window is therefore genuinely outside the popup, no extra hit-testing needed. Escape closes it the same way. `Closed` unhooks both handlers so nothing leaks onto the next open. `dotnet build -t:Rebuild` clean: 0 errors, only the 2 pre-existing unrelated CS0067 (`Start/StopRecordingRequested`) warnings (×2 real+_wpftmp = 4). Not live-verified in a running GUI (can't launch Shane's BuildConsole from here) — reasoning above stands in per the issue's own instruction; Shane should confirm a manifest click now opens the flyout and it stays open until a real outside click, Escape, or the ✕ button. No DB work. Outside msp-portal/shane-mccaw-consulting scope — no test manifest (pure WPF popup timing fix). | `6fdf83a9` |
| 2026-08-13 | ✅ DONE | Git #991: BuildConsole (WPF) Sessions - Get-Process fallback with real window titles. Shane, after #984's real Sessions fix landed: "I have like 4 goins now" against a Sessions panel that correctly reflected `claude agents --json` but still didn't match reality. Confirmed via direct side-by-side check (`Get-Process -Name claude` vs `claude agents --json`, same moment): the CLI's own "agents" registry doesn't discover every real running claude.exe process - specifically, sessions launched via the Send-to-Builder path (`run-claude.ps1` -> `Start-Process -NoNewWindow -Wait`, non-interactive `--permission-mode auto`) were real and running but completely invisible to `claude agents --json`, a genuine limitation in the CLI itself, not something fixable in BuildConsole's own code. Shane: "fall back using Get-Process." Then, seeing the fallback entries labeled generically: "Can you not get the window title and use those? instead of claude.exe." `ClaudeAgentsService.ListActiveSessionsWithFallbackAsync()` merges the CLI's own list with a raw `Process.GetProcessesByName("claude")` scan, adding a fallback entry (`Kind="untracked"`) for every real PID the CLI's list doesn't account for. claude.exe itself owns no window (`MainWindowHandle` always 0, confirmed live - attached to its parent's console per `-NoNewWindow`), but the real live-updated title (`run-claude.ps1`'s own `$Host.UI.RawUI.WindowTitle`, further updated by claude.exe with status glyphs like "◐ 986") lives on an ancestor process - confirmed live as a `cmd.exe` window titled exactly that. `Process` has no parent-PID API, so `FindAncestorWindowTitle()` walks the chain via WMI's `Win32_Process` (new `System.Management` package reference, verified against a real published version on nuget.org first - learned that lesson from the earlier #939 AvalonEdit incident), checking each ancestor's own `MainWindowTitle` up to 4 levels up. `BuildQueuePanel`'s render already preferred `Name` first, so the real resolved title shows automatically; a distinct ❔ icon and "(untracked)" tooltip mark these as CLI-invisible-but-real. No DB work needed/attempted. Verified live end-to-end: rebuilt, relaunched, read a temporary merged debug-log line directly - 2 of 3 real fallback sessions resolved their actual window titles (`◐ 986`, `◐ 990`); the third (mid-transition at poll time) fell back to the generic "claude.exe (untracked)" label as designed. `dotnet build` clean, 0 errors, 0 warnings. Not visually confirmed in the live UI (no screenshot capability) - Shane should confirm the Sessions tile itself now shows all real sessions with meaningful titles. | `3971d61b` |
| 2026-08-13 | ✅ DONE | Git #989 (bookend restoration) — real commit `7abbb25fa5` ("Play button runs loaded manifest through RunManifestAsync") confirmed: `BtnPlayTest_Click` now fires `PlayTestRequested?.Invoke(this, _lastLoadedManifest)`, passing the real loaded manifest directly instead of the permanently-empty `RecordedSteps` (dead since #963 removed the Record button). Bookend was lost to the same shared-file collision pattern as #901/#921/#924/#975. | `7abbb25fa5` |
| 2026-08-13 | ✅ DONE | Git #997 (Epic #803) — Manifest steps flyout: `DescribeUiStep` (`LeftSidebar.xaml.cs`) previously appended only a bare `⟳capture` glyph when a uiStep carried a `captureResponse` block, giving zero indication of what URL it was actually watching for (Shane: "the popout doesnt say anything about it either"). New `DescribeCaptureResponse` helper parses the step's raw `captureResponse` JSON and renders `⟳capture: {urlPattern}` (e.g. `⟳capture: /api/public/hero-headlines`), plus a short parenthetical summary of the `expect` block's real assertions when present (`status`, `jsonPath`, `containsAny×N`, `containsNone×N`) — matches real manifest shapes confirmed against `test-manifests/admin/tenant-switcher-floaty.json` and `test-manifests/chat/escalation.json`. Falls back to the bare `⟳capture` glyph on malformed JSON so a parse failure can't blank the row. #996 (the companion live-log issue, same detail level for the runtime log instead of this preview) was still open/unlanded as of this session, so this was implemented standalone per the issue's own fallback instruction, not ported from it. Display-only — no wire logging needed. `dotnet build` verified clean in an isolated worktree off this session's own HEAD (0 errors, only the 2 pre-existing unrelated CS0067 `Start/StopRecordingRequested` warnings) — the primary tree had concurrent sessions' uncommitted Home-screen/other WIP left untouched. Not live-verified in a running GUI (can't launch Shane's BuildConsole from here) — Shane should confirm a manifest step with a `captureResponse` block now shows its real urlPattern (and assertion summary where present) in the steps flyout. No DB work. Outside msp-portal/shane-mccaw-consulting scope — no test manifest (pure WPF preview-text fix). | `f9353cb3` |
| 2026-08-13 | ✅ DONE | Git #995 follow-up — `UiTestExecutor.cs`/`PowerShellTestExecutor.cs` step-logging: make every "invisible" step feature (captureResponse arm + resolve, #877 extract, #977 screenshot capture, powerShellVerify auth/connection state) log its own armed/resolved state with real target/pattern/duration, so the exact #995 hang scenario is diagnosable straight from the log next time instead of requiring a manual trace. `dotnet build` (verify output dir, since a live BuildConsole.exe held bin/ locked) clean, 0 errors. Outside msp-portal/shane-mccaw-consulting scope — no test manifest. Not live-verified in a running GUI (can't launch Shane's BuildConsole here) — Shane should confirm on the next UI-manifest/powerShellVerify run that the new log lines (step-start armed note, capture match/timeout+duration, extract resolve+duration, screenshot duration, PS auth/connection resolve+duration) appear as designed. | `c26a7ca4` |
| 2026-08-13 | ✅ DONE | Epic #803 — `HttpTestExecutor.cs`/`GraphTestExecutor.cs`/`ZohoTestExecutor.cs` live PASS/FAIL/ERROR log line: changed the live telemetry log line to use the real resolved request URL (host and all) instead of the relative manifest path, so a failure is diagnosable straight from the log without cross-referencing which `baseUrl`/environment the run was against. `HttpTestExecutor`/`ZohoTestExecutor` both had the bare relative-path gap; `GraphTestExecutor`'s manifest paths are normally already full Graph URLs, but its log line could still show an unresolved `{{variable}}` placeholder instead of the value actually substituted in, so it now logs the post-interpolation resolved URL too — added a `logLabel` override on its shared `Finish()` helper so the log line and the `TestStepResult.Label` field can diverge without duplicating the log call at every site. Pure logging/telemetry change (wire logging IS this issue's task, per #803). No DB work. Outside msp-portal/shane-mccaw-consulting scope — no test manifest (pure WPF telemetry fix). `dotnet build` clean, 0 errors (only the pre-existing unrelated CS0067 warnings) — verified via a rebase-and-resolve against a fast-moving shared checkout (several concurrent sessions landed commits mid-session; my own uncommitted edits were wiped once by a concurrent `git pull --rebase` before I'd committed and had to be reapplied — landed clean on the second attempt). Not live-verified in a running GUI (can't launch Shane's BuildConsole here) — Shane should confirm the next apiTests/graphTests/zohoTests run's live log shows the full host+path on PASS/FAIL/ERROR lines. | `660b3064` |
| 2026-08-13 | ✅ DONE | Epic #803 — fix bare `/admin/...` paths (missing `/api` prefix) across `test-manifests/smoke/hello-world-sql.json`, `test-manifests/admin/baseline-actions-powershell-verify.json`, `test-manifests/observability/schema-drift-guard.json`, `test-manifests/observability/dlq-schema-error-scan.json` (15 occurrences total: 1 + 11 + 2 + 1). `app.ts` mounts the whole router at `app.use("/api", router)`, so every `apiTests[].path` in these 4 files was silently 404ing against the real deployed app. Absolute `https://graph.microsoft.com/...` graphTests paths confirmed unaffected/untouched. Re-swept all of `test-manifests/` afterward for the same bare `/admin/...` pattern — these 4 files were the only matches. Also added a small proactive check in `HttpTestExecutor.RunOneAsync`: warns in the live log the moment a manifest path starts with `/admin/` instead of `/api/admin/`, so this class of mistake surfaces immediately instead of as a confusing FAIL. `dotnet build` verified clean (0 errors, only the 2 pre-existing unrelated `CS0067` warnings) via an alternate `-o` output dir since the live BuildConsole.exe held `bin/` locked. No DB work. | `b0709a59` |
| 2026-08-13 | ✅ DONE | Git #1001: BuildConsole (WPF) Sessions - click-to-focus window, right-click close, and a real WMI hang fixed. Shane: "What'd be really cool now is if I clicked on them they brought that window into focus." then "Next thing... Give me a right click context menu to close them." Click-to-focus: added `ClaudeAgentSession.WindowHandle` (resolved for every session) and `ClaudeAgentsService.BringToForeground()` (`SetForegroundWindow` + `ShowWindow(SW_RESTORE)` if minimized, called directly from the click event, which is exactly the case Windows' foreground-lock exception allows); `ActiveSessionsList_SelectionChanged` wires it, clearing selection after so re-clicking the same row still fires. Right-click close: a `ContextMenu` with "✕ Close Session" killing the real process tree (`Process.Kill(entireProcessTree: true)`), same immediate-no-confirmation shape as this app's existing "⏹ Stop". The real bug hunt: the merge step's own diagnostic line never appeared, poll after poll - first suspected an overlapping-poll race (added a re-entrancy guard, didn't fix it), then suspected classic WMI (`ManagementObjectSearcher`, DCOM-based) deadlocking on the WPF UI thread (moved the whole merge phase off the UI thread via `Task.Run`, still didn't fix it), then confirmed via isolated repeated live testing that classic WMI was genuinely hanging indefinitely on this machine - not slow, not erroring, just never returning, even off the UI thread - a real machine-specific WMI/DCOM issue distinct from the newer CIM/WinRM path PowerShell's own cmdlets use (explaining why earlier manual `Get-CimInstance` testing looked fine). Fixed by replacing `ManagementObjectSearcher` with `CreateToolhelp32Snapshot` (pure kernel32, zero COM/DCOM/WMI) for walking the parent-process chain; removed the now-unused `System.Management` package reference. Survived several rounds of concurrent-session file reverts mid-debugging (redone from scratch twice) before landing. No DB work needed/attempted. Verified live end-to-end multiple times in isolation (single BuildConsole instance, `%USERPROFILE%\buildconsole_sessions_debug.log` mirror): the previously-silently-hanging merge step now completes and resolves real window handles for the majority of sessions. `dotnet build` clean, 0 errors, 0 warnings. One minor pre-existing cosmetic issue noticed (not fixed, unrelated scope): some Send-to-Builder session titles show a mangled em-dash, a UTF-8/Latin-1 encoding mismatch somewhere upstream in the `mybuilder://` URL round trip, only now visible because titles finally resolve at all. Shane should confirm clicking a session brings its real window forward and right-click → Close actually ends it. | `92a04cef` |
| 2026-08-13 | ✅ DONE | BuildConsole (WPF, Git #1002) — open maximized by default. Shane: "Can the window always open maximized." Set `WindowState="Maximized"` on `MainWindow.xaml`. The existing Git #894 `WM_GETMINMAXINFO` hook (`ClampMaximizedBoundsToWorkArea`) already clamps maximized bounds to the real work area, so this doesn't cover the taskbar or overflow the screen — no additional logic needed. Also added `deploy-shanesbuild.cmd` (`cb9f9b7f`, separate ask, no dedicated issue — small tooling utility): builds Release and deploys to `bin\ShanesBuild\`, stopping/relaunching only the path-scoped `ShanesBuild` instance, never a blanket kill by process name — verified live end-to-end (real relaunch, new PID confirmed running from `ShanesBuild\`). `dotnet build` clean, 0 errors (only the pre-existing unrelated `CS0067` warnings). Not live-verified against a running GUI here — Shane should confirm the window opens maximized on next launch. | `5a02dba0` |
| 2026-08-13 | ✅ DONE | Epic #803 — `UiTestExecutor.cs` `captureResponse` logging: include the real current page URL (`_webView.Source`) alongside the `urlPattern` in the armed-note, matched/timed-out resolution, and final PASS/WARN log lines, since a `urlPattern` is a partial match pattern rather than one resolved URL like `HttpTestExecutor`/`GraphTestExecutor`/`ZohoTestExecutor` already got in `660b3064`. `dotnet build` clean, 0 errors (only the pre-existing unrelated `CS0067` warnings). Outside msp-portal/shane-mccaw-consulting scope — no test manifest (pure WPF telemetry fix). No DB work. Not live-verified in a running GUI (can't launch Shane's BuildConsole here) — Shane should confirm the next uiSteps run with a captureResponse shows the current page URL alongside the pattern in the log. | `72e93315` |
| 2026-08-13 | ✅ DONE | Git #987 (Epic #803) — consolidated the 6 money-path manifests (#886–891) into ONE self-contained create → use → verify → teardown lifecycle: new `test-manifests/copilot-readiness/money-path-e2e.json` (17 apiTests + 2 postGraphApiTests + 45 uiSteps), deleted the 6 source files, swapped their 6 `_regression-suite.json` entries for the one. Phase→bucket mapping around the executor's FIXED order (apiTests → graphTests → postGraphApiTests → zohoTests → uiSteps → powerShellVerify, one shared #877 var store): SETUP+CREATE+EXECUTE+VERIFY in `apiTests`, TEARDOWN in `postGraphApiTests` (runs after VERIFY, before uiSteps — the only structural slot for an HTTP teardown; safe because #986 reset is conservative and deletes nothing the uiSteps read), preserved UI assertions in `uiSteps`; every step name is phase-prefixed so a failure names its phase on the `testing.api-executor`/`testing.ui-executor` channels. SETUP: dynamic testbed customerId via `GET /api/admin/baseline-templates/testbed-customers` (Bearer `{{SECRET_KEY}}`, the proven #901 lookup) → `POST /api/admin/testbed/reset` (#986) → testbed Assessment login → extract `{{authToken}}`. CREATE: real `POST /api/public/checkout-session` (productSlug `copilot-readiness-assessment`, Zod-verified body) mints a genuinely NEW checkout_sessions row + funnel lead each run → extract `{{sessionId}}` → assert its consent-status resolves. EXECUTE: every #887/#889/#890 API honesty assertion (consentStatus granted; 202 scan trigger + persisted active run + checksTotal; copilotGate; sow ready/allWorkstreams/selectedWorkstreamTitles; payment-options ready/selfServe=false/phases). VERIFY: fresh `POST /api/auth/login` credential round-trip. TEARDOWN: `teardown-graph-writes` dev-gate/auth/envelope contract check + final #986 reset. uiSteps preserve #886 quiz → #888 verdict/pillars → #889 documents → #890 SOW → #891 checkout no-signed-scope guardrail. Repointed `smoke/hello-world-ui.json`'s live textContains cross-ref off the deleted `documents-screen.json` to the new file; bumped both touched manifests' `lastVerifiedAgainstCommit`. **Honest scope (documented at length in the manifest notes):** the credentialed account is minted only inside the Microsoft admin-consent OAuth callback (`provisionProspectAccount`) + a real Stripe payment (`set-password` gate), NEITHER drivable by the WebView2/HTTP harness, and there is no client-side unique-email generator — so VERIFY logs in with the testbed Assessment account's creds, not a harness-minted password; a fully-automated 'brand-new credentialed account each run' is architecturally impossible here and was NOT faked (CREATE still genuinely creates a new funnel record each run). The deep `debug-reset-session` wipe is deliberately NOT invoked (it would destroy the maintained completed-scan state the #888/#889 UI assertions need, and the async re-scan can't be awaited) — documented for a future sync-scan variant. The money path makes no real M365 Graph WRITE actions (read-only scan; Launch Control writes are the separate remediation flow), so teardown-graph-writes is a reachability/gate check with an out-of-range id, not a fabricated write. #887's live-scan overlay and #891's signed-scope happy path are point-in-time states mutually exclusive with the completed-verdict journey, preserved at the API level / as documented specs. No DB work; manifests only (no code changes). All touched JSON re-parses valid; committed from an isolated worktree off `origin/main` to avoid a concurrent BuildConsole session's unpushed commit + dirty tree. Not live-run — no WebView2/BuildConsole runner or live dev tenant reachable here. **Shane To-Do:** (1) BuildConsole → Settings → Test Environment Variables: set/confirm DEPLOY_URL, SECRET_KEY (= BUILD_TRACKER_INGEST_TOKEN), TEST_PORTAL_EMAIL/TEST_PORTAL_PASSWORD (a testbed Assessment account kept scanned: completed verdict, ≥2 documents, resolvable SOW), TEST_PORTAL_SLUG; (2) run against the `.replit.dev` dev origin (the #986 endpoints 403 off-dev by design); (3) confirm SETUP/CREATE/EXECUTE/VERIFY pass + TEARDOWN resets clean. | `144f8468` |
