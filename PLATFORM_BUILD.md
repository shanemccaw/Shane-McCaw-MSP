# Platform Build Log

Append-only log of work sessions. Each session adds exactly one row, in two edits:

1. **At session start** (before any real work): append a row with status `⏳ IN FLIGHT — {step name}`, commit it immediately as its own standalone commit.
2. **At session end** (after the real work is done, tested, and typechecked): update that same row to `✅ DONE — {step name}`, filling in the real commit hash of the work, then commit the update (either folded into the final commit or immediately after it).

If a session dies, crashes, or is abandoned mid-way, its row stays `⏳ IN FLIGHT`, which is itself the record that an attempt was made and didn't finish.

See [CLAUDE.md](CLAUDE.md) for the exact instructions.

| Date | Status | Step | Commit |
|------|--------|------|--------|
| 2026-07-19 | ⏳ IN FLIGHT | Cost Engine — Real SKU Pricing and License Waste Dollars | — |
| 2026-07-19 | ⏳ IN FLIGHT | Real Emergency MFA Bypass Code | — |
| 2026-07-19 | ✅ DONE | Reorganize Customer Dashboard Into 10 Category Tabs — the 10 categories are now REAL customer dashboard tabs, wired to live registry metrics, reusing the EXISTING monitoring_package multi-tab mechanism (GET /api/dashboard/resolved-list + <DashboardTabs>). Pure manual SQL + page deletion — ZERO schema/endpoint/renderer code change. TWO decisions were genuinely the user's (spec had an internal contradiction + a wrong premise) and were asked, not guessed: (Q1) the 17 auto-* widgets a prior task appended to customer_default are RELOCATED off it onto their correct category tab (not duplicated); (Q2) tabs surfaced via the SQL-only monitoring_package path — user knowingly accepted the sales-bundle GATING (category tabs only appear once a customer has an active assignment of the new "Customer Dashboard Category Tabs" bundle; NOT auto-assigned — Part E of the migration is a commented per-customer opt-in). Each category = 1 monitoring_packages row (tab label, no checks attached = no-op execution) + 1 monitoring_package dashboard_templates row (widgets), created for every MSP that already has a customer_default. 104 widgets across 10 templates, validated: JSON parses, every metricKey is real, 0 stubs wired, 0 duplicate ids, every rendererType shape-compatible per getValidRenderersForMetric (ScoreRing only where a denominator exists, Smart only where smartEligible). Verification limits (no DATABASE_URL here): scoping proven by code path — category tabs resolve through the same resolveTemplate(scopeId=req.user.customerId) + resolveMonitoringPackageKeys(user.mspId,user.customerId) the existing monitoring_package tabs use, so no cross-tenant leakage and no query-param override; msp-portal typechecks clean. Known minor limitation: resolved-list has no ORDER BY on package templates, so tab display order isn't guaranteed to match ord 1..10 (inherent to the unchanged endpoint). D1 PER-CATEGORY AUDIT (registry = 168 metrics; only status="available" and status="needs_aggregation"-with-implemented-transform wired; the 3 not_collected stubs NEVER wired): (1) Executive — FULLY COVERED: engine.healthScore/securityScore/complianceScore/adoptionScore (Gauge), engine.pillarSnapshot (Radar), engine.scoreTrend + health.clientHealthHistory (Trend), benchmark.scoreVsIndustry (Bar, needs_aggregation). (2) Identity & Access — FULLY COVERED: mfaRegistered/passwordless/stale/impossibleTravel (Smart), legacyAuth/riskyUser/highRiskSignin/failedSignin (Trend — these 4 are smartEligible but trend-shaped so Smart is correctly NOT offered), signinActivity (Heatmap, needs_aggregation), provisioning + privilegedRoleChange (Timeline). Gap: no PIM eligible-vs-active, no MFA enforcement (registration only). (3) Security Posture — WELL COVERED (leans on needs_aggregation): engine.securityScore + security.secureScore (Gauge), highSeverityAlert/lowScoreControl (Smart), activeAlert (Stat), alertsBySeverity/riskDetection/diagnostics.findingsBySeverity (Distribution), secureScoreControls (Bar), malware/phishing (Trend, relocated). Gap: no CVE/vuln metric, no patch-compliance (only device-side intune.outdated). secureScore is Tier-2. (4) Compliance & Governance — FULLY COVERED: engine.compliance/governanceScore (Gauge), oversharedSiteCount (ScoreRing — only metric in the whole registry with a denominator), weakDlp/missingLabel/missingRetention/orphanedSite/orphanedTeam/overdueAccessReview/orphanedAccessPackage (Smart), dlpIncident/guestUser/externalInvite/oneDriveExternal (Trend), entitlementPolicyDrift (Timeline). (5) Collaboration & Sharing — FULLY COVERED: forwardingMailbox/sharedMailboxSignin/copilot.overshareExposure (Smart, relocated), mailbox/teamsChannel/delegation (Stat), 4x active-user trends, fileActivity (Heatmap, needs_aggregation). (6) Licensing & Cost — PARTIALLY COVERED: skuBreakdown/copilotLicenseBreakdown (Distribution), wasteEstimateBreakdown (Bar), duplicate/inactive (Smart). REAL GAP left unwired: licensing.costTrend is a not_collected stub — no $ cost time-series exists (waste is count/distribution only). (7) Configuration Drift — FULLY COVERED: 10 drift.* event feeds + 2 dynamics.* (all Timeline). (8) Intune & Devices — FULLY COVERED (the most complete category, all Tier-1): 6x device Smart tiles + configDrift (Stat) + highThreat (Trend) — all 8 relocated intune.* metrics. (9) Usage & Adoption — WELL COVERED: 8x usage.* workload trends, adoption/copilot engine scores (Gauge), powerPlatform.app/flow (Trend, relocated — inventory only, still no Shadow-IT ownership/risk signal), meetingsOrganized (needs_aggregation). REAL GAP left unwired: copilot.usagePerUser is a not_collected stub. (10) Operational Maturity — COVERED: sla.compliancePercent (Gauge), sla.activeBreach (Smart), scopeCreepStatus/packages.active/offers.active (Stat), alertVolume (Trend), findingsBySeverity (Distribution), recentAlerts/recentScans/remediationOffers (Timeline). REAL GAPS left unwired: serviceHealth.uptimeStatus (not_collected stub); projects.* customer-scope resolve to not_available (user-keyed, no customer→user bridge); workflow-success/automation metrics are scope="msp" (platform-operational, not customer-facing). Manual SQL: 2026-07-19-customer-dashboard-category-tabs.sql. | 84c3bb8b |
| 2026-07-19 | ✅ DONE | msp-users-user-id-fk-constraint | Discovered already done: schema `.references()` on `mspUsersTable.userId` + manual migration `2026-07-18-msp-users-user-id-fk.sql` (orphan-check SELECT, commented DELETE, ALTER TABLE ADD CONSTRAINT) both landed yesterday in 8f24e76d, marked DONE in this file at 92e3f64d. No code/schema change made this session. Verified per task's remaining asks: no raw-SQL/non-ORM deletion path against `users` exists anywhere (only `db.delete(usersTable)` at portal.ts:6928); the client-delete route (portal.ts) already deletes the `msp_users` row (line 6927) immediately before deleting `users` (line 6928), so it does not violate the new FK. | 8f24e76d (pre-existing) |
| 2026-07-19 | ✅ DONE | AI Chat — Trigger Real Remediation — the AI support chat can now propose a genuinely-eligible one-click remediation and, on explicit human confirmation, fire the existing testbed-gated POST /portal/mission-control/remediate for real. D1: the chat's grounded context did NOT surface remediable-finding data, so extended it — new exported listRemediableOffers() in portal-mission-control.ts is the shared source of truth for what the /remediate endpoint would accept (testbed tenant + sent offer + service→instant-pack), mirroring the same eligibility the endpoint enforces (purely additive, 0 deletions). D2: support-chat.ts injects those into the CustomerUser system prompt; the model may emit [PROPOSE_REMEDIATION:<offerId>] ONLY to *offer* (never act). The server re-validates the id against listRemediableOffers before returning proposedRemediation, so a hallucinated/ineligible id (or ANY marker from a non-testbed tenant, whose list is empty) yields no actionable proposal; marker is stripped from the visible reply. support-chat.tsx renders an explicit Confirm & run / Not now card — only a human click calls the real endpoint; its 403 for non-testbed is surfaced honestly (no silent fail, no false success). Greeting/disclaimer copy updated (was "I can't take actions"). The AI can NEVER fire directly — no action runs without an explicit confirm click on a server-validated offer, and the endpoint's testbed guard is untouched. Verified: 14/14 support-chat tests pass (incl. valid-marker→proposal, ineligible-id→dropped, empty-tenant→dropped); api-server + msp-portal typecheck clean for the changed files. Pre-existing unrelated failures left alone: health-engine.test.ts (TS2698) and portal-mission-control.test.ts "derives severities from real engine output fields" (health risk-score threshold vs test expectation mismatch — my change to that file is additive-only and does not touch the engines route). | ae33e6a6 |
| 2026-07-19 | ✅ DONE | Dashboard PPT Export — no PPT-generation library existed anywhere in this monorepo's dependency tree (checked every artifacts/*, lib/* package.json), so chose pptxgenjs (actively maintained, native chart support, Node ESM compatible) over building a custom OOXML writer. dashboard-ppt.ts is a new, independent consumer of dashboard-snapshot.ts's exported resolution helpers (resolveCallerScope/findDefaultTemplate/resolveTemplate/resolveMetric) — dashboard-snapshot.ts itself untouched. Each widget shape renders to a real slide element: scalar -> giant stat text, trend -> native pptxgenjs line chart (real chart XML, not a screenshot), distribution -> native bar chart, heatmap -> table, timeline -> bulleted list. New GET /portal/dashboard/ppt mirrors the existing /portal/dashboard/pdf route exactly (same auth floor, same DashboardSnapshotError handling). Verified for real: ran the exact addSlide/addChart/addTable/addText calls against representative MetricResult-shaped sample data (no live DB access in this environment), wrote a real .pptx, unzipped it, and confirmed real slide XML + 2 real chart parts (chart1.xml/chart2.xml) with the actual data values present (87.5%, 72/78/85, Critical/Warning/Info, Windows/macOS, bullet event text) — not just a 200 response. api-server and msp-portal both typecheck clean (health-engine.test.ts pre-existing debt excluded, same as prior sessions). | d522bdf7 |
| 2026-07-19 | ✅ DONE | Executive Mode — Simplified Leadership View — new fenced-off /executive-mode page (msp-portal), 5 tiles, zero new backend endpoints: (1) Score — GET /api/portal/mission-control/engines, health.score inverted via the same toGoodnessPercent MissionControl.tsx already uses; (2) Risk list — GET /api/portal/mission-control/overview, already severity+recency sorted findings, sliced to top 5; (3) Next Best Action — derived client-side from the SAME overview findings' existing per-finding `action` field (no new AI call); (4) AI Summary — <ExecutiveSummaryCard> reused verbatim; (5) Quarterly Summary — reuses the EXISTING POST /api/portal/dashboard/share (dashboard-export.ts) snapshot+share-link flow verbatim, just relabeled, no new document-generation path built. Investigated ai-next-best-actions.ts fully: NOT reusable for this tile — requireAdmin-only, built on the legacy CRM schema (usersTable/projectsTable/opportunitiesTable, no mspId/customerId tenant scoping), prompts explicitly frame "what should Shane do today" across the whole client portfolio with admin-panel link paths; its callers (ClientDetail.tsx, MarketingCommandCenter.tsx, Overview.tsx) all live in admin-panel, not the customer portal. Verified all 5 tiles scope strictly off req.user.customerId (JWT claim) with no query-param override path on this page — no cross-tenant leakage. msp-portal typechecks clean. Does not touch Mission Control, the full dashboard, or per-widget chart-type-choice. | 2a76cd90 |
| 2026-07-19 | ✅ DONE | Assessment Comparison Mode — scope-vs-scope SOW version comparison built real (GET /api/portal/assessment/sow/versions returns every archived+active consolidated_sow version with structured sowPricingLines, scoped via req.user.id; new /assessment/compare page renders two versions side by side with a diff summary, reusing AssessmentSowSelector's existing pricing-line rendering). Free-tier-scan vs. paid-upgrade comparison investigated and NOT built — msp_diagnostic_findings and sowPricingLines share no common key/joinable field, so that comparison is a genuine data-model mismatch today, not fabricated. | dc34cdb2 |
| 2026-07-19 | ✅ DONE | Executive Mode — Per-Widget Chart Type Choice | 221706e4 |
| 2026-07-19 | ✅ DONE | Customer Tenant Timeline — GET /api/portal/customer/timeline aggregates 5 event types into one chronologically-merged, cursor-paginated (limit+before, 30/page default, 100 cap) feed: completed/failed diagnostic scans (msp_diagnostic_runs), warning/critical findings only (msp_diagnostic_findings — ok/info excluded as routine), engine score changes with \|delta\| >= 5 pts (tenant_engine_snapshots — arbitrary-but-reasoned significance bar), delivered/approved documents (insights_generated_documents), and sent/accepted/rejected/expired sales offers (sales_offers). Notification Center rows deliberately excluded as a source to avoid double-counting the same offer/document events notification-center.ts already fans out to the bell. No workflow-run-history table exists in this codebase (only step/template tables) — that source was dropped rather than fabricated. Renders via existing TimelineList.tsx unmodified. Note: App.tsx/app-shell.tsx/routes/index.ts route+nav wiring landed inside concurrent commit 7ce5fd93 (that session staged those files whole) — flagged, not silently absorbed; verified present in HEAD. | 6edf6af7 |
| 2026-07-19 | ✅ DONE | Customer Notification Preferences UI — per-category on/off + email opt-in (customer_notification_preferences table, opt-in-in-app/opt-out-email default), gated inside notification-center.ts's createNotification (direct db.insert bypass sites in portal.ts remain ungated, pre-existing debt not this task's scope). Email via existing Graph sendMessage. Webhook (v1 Teams/Slack) reuses existing outbound_webhooks/webhook-delivery.ts fan-out via event-bus.ts dispatchEvent — no new delivery infra built. Does not touch policy_rules severity/cooldown/escalation; recurring quiet hours NOT built (flagged as real follow-up needing new scheduling infra). Needs manual SQL: 2026-07-19-customer-notification-preferences.sql. | 7ce5fd93 |
| 2026-07-19 | ✅ DONE | In-Portal Marketplace — Shared Across Roles — Authenticated, shared-across-roles marketplace (Assessment + CustomerUser) in the customer portal; one page, RBAC controls catalog scope + which sections render (matching the Sharing/Account-Basics/GDPR shared-page pattern). D1 (role scope): reuses the EXISTING serviceType + visibility="public" catalog convention — no new schema, no per-product role check. Assessment sees serviceType {assessment, monitoring_tier}; CustomerUser+ sees {assessment, monitoring_tier, micro_offer, retainer}. D2: new read-only endpoint GET /api/portal/marketplace/catalog (routes/portal-marketplace.ts, requireRole("Assessment") floor so both roles reach it, role narrows results server-side) + new page src/pages/marketplace.tsx (browse-by-category, real pricing, customer-safe fields only). D3 (stop-and-report gap, NOT worked around): the two authenticated checkouts are object-bound — portal-checkout.ts needs a *sent offer* id, portal-assessment.ts derives from the caller's active *consolidated SOW*; neither takes an arbitrary catalog service id, and the only service-id→Stripe path is the unauthenticated guest onboarding flow. So arbitrary browse-item self-serve checkout has no correct authenticated endpoint today; wired purchase path is Recommended→/customer-offers, browse items route to the real "contact provider" surfaces. D4 DECISION: personalized "Recommended for your tenant" section powered by the REAL Sales Offer Engine (GET /api/portal/offers, CustomerUser-floored, sent-state offers only) — chosen over a generic browse-only pass because live, signal-grounded offers already exist and match the product's "targeted, not generic upsell" philosophy; Assessment-tier omits the section (that endpoint is above their floor). NOTE: the marketplace commit 93e4003d was built from a stale base and clobbered the timeline + notification-preferences route/nav/router wiring from already-DONE tasks 6edf6af7/7ce5fd93; repaired in bc52d7d6 (both features + marketplace now coexist in HEAD). msp-portal typechecks clean; api-server typecheck errors are pre-existing unrelated debt in lib/health-engine.test.ts. | 93e4003d (marketplace) + bc52d7d6 (wiring repair) |
| 2026-07-19 | ✅ DONE | Public Website Rebuild: Stage 1 Shell/Core | a06f8185 |
| 2026-07-19 | ✅ DONE | engagement offer real-time trigger on qualifying events | 52f8f6b5 |
| 2026-07-19 | ✅ DONE | quiz submit wires Lead Offer Engine synchronously | 4b1cdb6a |
| 2026-07-19 | ✅ DONE | engagement offer evaluation seeded workflow | 991fac42 |
| 2026-07-19 | ✅ DONE | critical workflow manual-trigger lock — allowManualTrigger metadata flag on system workflow seeds; POST .../run and .../test-run in admin-workflows.ts now 403 when a definition's metadata.allowManualTrigger === false; .../runs/:id/rerun untouched. Applied to Engine Score Retention & Rollup. Note: the seed-system-workflows.ts portion (interface field, upsert query, Retention & Rollup entry) landed inside a concurrent session's commit 63512110 — that session's commit swept up this file's uncommitted edits before they could be committed standalone here. Flagged, not silently absorbed; verified present/correct in HEAD. | 8ae46023 |
| 2026-07-19 | ✅ DONE | evaluate_engagement_offers workflow node | 5da9a1e1 |
| 2026-07-19 | ✅ DONE | engagement-offer-engine evaluator — standalone evaluateEngagementOfferForLead/evaluateAllEngagementOffers, local ENGAGEMENT_INTENT_POINTS map (not imported from lead-intent.ts), platform-only rules (mspId IS NULL), cooldown dedup mirroring policy-engine.ts's raw-SQL style. Not wired into any route/workflow yet. | 6f693bea |
| 2026-07-19 | ✅ DONE | lead-offer-engine AI pricing — AI-determined opportunistic pricing replacing rule-based pricing adjustment, hard-capped server-side (maxDiscountPct from lead_offer_pricing_config, clamped regardless of AI output); computeLeadOfferEngine restructured into two phases so AI is only called for candidates surviving scoring/filtering/cap, not every eligible candidate. Falls back to base price (no discount) on AI failure. | 4f4fd222 |
| 2026-07-19 | ✅ DONE | Assessment Document Generation Workflow — Service-Mapped, Sequenced SOW — visible seeded workflow (two triggers: diagnostics.run_completed + portal.first_login) replacing the hidden assessment-doc-trigger.ts/autoFireAllDocumentCards path; new assessment_doc_gate node ports the two-sided wait-for-both gate (login gate RESTORED after a single-trigger regression was caught); find_object "service" case + services.associated_documents jsonb (manual SQL) + admin ServiceEditor UI; sequential foreach doc gen → consolidated SOW after (grounds on them) → build_presentation (customer-visible + SOW only); run-ID SSE progress + honest failure/completion screens (polling = source of truth). Needs manual SQL: 2026-07-19-services-associated-documents.sql. | 63512110 |
| 2026-07-19 | ✅ DONE | engagement-offer-engine schema — engagement_offer_rules + engagement_offer_firings tables, schema only, no evaluator logic. Note: landed inside 4f4fd222 because a concurrent session's commit swept up this session's separately-staged patch before it could be committed on its own — flagged, not silently absorbed. | 4f4fd222 |
| 2026-07-19 | ✅ DONE | lead-scoring-engine refactor | efdcead6 |
| 2026-07-19 | ✅ DONE | analytics event type taxonomy extension | a2fb86b0 |
| 2026-07-19 | ✅ DONE | lead-offer-pricing-config schema table | 96e1b3d3 |
| 2026-07-19 | ✅ DONE | Wire Real Dashboard Modules — Sharing, Device Health, Power Platform, Copilot — wired real customer_default widgets (Sharing/Data Exposure, Device Health, Copilot overshare) plus email security/threat trends via idempotent manual SQL (canvas_layout append, no DB access here). Power Platform wired as placeholder-quality only (appCount/flowCount) — real "Shadow IT" needs a new ownership/risk engine signal, flagged as follow-up, not fabricated. Workflow activity/automation logs confirmed a real gap (workflow.* metrics are msp-scope platform-operational, not customer-facing) — not wired. Also built AI Executive Summary tile (new dashboard_executive_summaries table + manual SQL, dashboard-executive-summary.ts reusing OMG-card AI conventions, 24h cache + manual refresh, GET /api/dashboard/executive-summary, ExecutiveSummaryCard on customer-dashboard.tsx). Needs manual SQL: 2026-07-19-dashboard-modules-widgets.sql, 2026-07-19-dashboard-executive-summaries.sql. | 336ae813 |
| 2026-07-19 | ✅ DONE | lead-offer-engine core logic — standalone LeadOfferCandidate/LeadOfferEngineOutput types, inferSignalsFromQuizScores (confidence-weighted quiz-score inference, highest-confidence-wins on key collision), leadGroupFires, computeLeadOfferEngine mirroring computeSalesOfferEngine's structure with zero imports from sales-offer-engine.ts (fresh priceToCents/buildIdempotencyKey/rationale helpers, "lead:" idempotency-key salt). Not wired into any route yet. | 55caa176 |
| 2026-07-19 | ✅ DONE | Assessment Payment Plan Screen — pay-in-full built real (PAY-TODAY coupon + hosted Stripe Checkout + signature + dedicated webhook, Assessment role floor). Phased BLOCKED (per-phase auto-invoicing is bound to CRM quick_win_presentations/projects space; can't resolve an Assessment consolidated_sow) → captured as signed provider-handoff, reported not worked around. portal-checkout.ts untouched. Needs manual SQL: assessment_sow_agreements. | e9c12356 |
| 2026-07-19 | ✅ DONE | Assessment Flow as Visible Workflows — safe slice only (packageKey added to diagnostics.run_completed; Deliverable 1 presentation SSE endpoint confirmed already-existing, no build). First Login + Document Generation workflows (D3/D4) NOT built — deferred pending redesign: prompt's build_presentation/packageKey-doc-mapping/one-shot-wait-for-both assumptions did not hold against the codebase (stop-and-report). Corrected direction captured in memory. | fd9a7511 |
| 2026-07-19 | ✅ DONE | Extend Document Sharing + View Tracking Beyond SOWs | 208af156 |
| 2026-07-19 | ✅ DONE | Interactive SOW Scope Selector | e825be49 |
| 2026-07-19 | ✅ DONE | TEMP DEBUG — Testbed-Only Assessment Scan Trigger Button (temporary debug code, not a permanent feature — awaiting a backlog removal ticket) | 967551d2 |
| 2026-07-19 | ✅ DONE | Assessment Document Generation Trigger — Telemetry + First Login | 639e6ba4 |
| 2026-07-18 | ✅ DONE | policy-engine escalation lifecycle | 0a6c5c38 |
| 2026-07-18 | ✅ DONE | Admin Dashboard Designer (Step 4b) | dfcb9f47 |
| 2026-07-18 | ✅ DONE | #1 Engine Development - policy-rule-suppressions | ab10e2bc |
| 2026-07-18 | ✅ DONE | #1 Engine Development: policy-engine suppression gate | 9c941ef0 |
| 2026-07-18 | ✅ DONE | Customer/MSP Dashboard Editor | 56ec3a77 |
| 2026-07-18 | ✅ DONE | Events tree node | 9d77b61d |
| 2026-07-18 | ✅ DONE | Engines tree node | 9d65f853 |
| 2026-07-18 | ✅ DONE | policy-engine category tagging | aa493fd0 |
| 2026-07-18 | ✅ DONE | #2 Smart Widget State Logic | a1089508 |
| 2026-07-18 | ✅ DONE | Signal Rules UI v1 | eefafc6e |
| 2026-07-18 | ✅ DONE | #4 customer-connect-dropdown-action | dbe72b1b |
| 2026-07-18 | ✅ DONE | #6 Audit Log Metadata Display Formatting | 4b090d38 |
| 2026-07-18 | ✅ DONE | #8 DLQ Help Text and Action Labels | 813a96f1 |
| 2026-07-18 | ✅ DONE | Dashboard Designer Access + Create Button | 2cbce684 |
| 2026-07-18 | ✅ DONE | #6 Portal 404 Redesign + Audit Logging | ffe0f36d |
| 2026-07-18 | ✅ DONE | #B Portal Favicon Fix | fbc3a616 |
| 2026-07-18 | ✅ DONE | Portal Dashboard Designer | 7c5215ba |
| 2026-07-19 | ✅ DONE | Trim Dashboard Extras — Remove Quick Actions/Projects/Services, Move Help Card to Bottom | f20672ee |
| 2026-07-18 | ✅ DONE | Guard Portfolio Risk Route Against resolveMspId Swap | 06dee9bd |
| 2026-07-18 | ✅ DONE | Factory Floor Data Audit | a4841a53 |
| 2026-07-18 | ✅ DONE | sales-offers list + fulfillment-queue mspId path migration | 47679db4 | |
| 2026-07-18 | ✅ DONE | resolve-slug tenant restriction + settings ownership checks + sales-offers dead-route cleanup | ec4d68a3 |
| 2026-07-18 | ✅ DONE | Wire Remediation Offers dashboard metric to sales_offers | 65ab285b |
| 2026-07-18 | ✅ DONE | Portal Foundation Redesign | 5f555414 |
| 2026-07-18 | ✅ DONE | engine-snapshot-customer-resolution-fix | a8cb2558 |
| 2026-07-18 | ✅ DONE | Engine Score Explain Dialog | 60b9f673 |
| 2026-07-18 | ✅ DONE | signal-decay-rate-fractional-migration | 6208028f |
| 2026-07-19 | ✅ DONE | Fix Mission Control Layout Proportions | 7c7a9643 |
| 2026-07-18 | ✅ DONE | Portal Shell Rebuild — CustomerUser | ab0e1f5f |
| 2026-07-18 | ✅ DONE | msp-users-user-id-fk-constraint | 8f24e76d |
| 2026-07-18 | ✅ DONE | stripe-metadata-service-id-casing-fix | 4fcbc9da |
| 2026-07-18 | ✅ DONE | tenant-profile-resolution-consolidation | 16908195 |
| 2026-07-19 | ✅ DONE | Severity-Grouped Findings Boxes With Modal Drill-In | fe7ba9b9 |
| 2026-07-18 | ✅ DONE | api-server-typecheck-fixes | 7d375bfa |
| 2026-07-18 | ✅ DONE | Portal Multi-Template Dashboard Resolve + Tabs (project/assessment excluded, pending projectsTable->mspId/customerId backlog fix) | 40e24e67 |
| 2026-07-18 | ✅ DONE | Mission Control Canvas — CustomerUser | 68dca058 |
| 2026-07-18 | ✅ DONE | Portfolio Risk Tenant-Scoping Audit (read-only; verdict LEAK NOT CONFIRMED) | 43c24840 |
| 2026-07-18 | ✅ DONE | MRR Canonical Source Audit | 8b5109a4 |
| 2026-07-18 | ✅ DONE | Fix Monitoring MRR Date Scoping | b8598440 |
| 2026-07-18 | ✅ DONE | MRR Metric Separation Audit | b9dcb15f |
| 2026-07-18 | ✅ DONE | Fix Global Toast Crash on Structured API Errors | 2c830cef |
| 2026-07-18 | ✅ DONE | fix-fetchActiveTenants-id-space-regression | 4520fe58 |
| 2026-07-18 | ✅ DONE | Merge Customer Home Content Into Mission Control + Flip Landing Route | ca751fe9 |
| 2026-07-19 | ✅ DONE | Wire Client Errors Into Platform Observability | 62945841 |
| 2026-07-19 | ✅ DONE | Move ErrorBoundary Above AuthProvider | 655090ea |
| 2026-07-19 | ✅ DONE | Three.js Integration Spike | a9377160 |
| 2026-07-19 | ✅ DONE | Build Overlord Total and Colony Scoring | c9c2c544 |
| 2026-07-19 | ✅ DONE | Fix Inverted Health Score Display + Hero Polish | 1c594a12 |
| 2026-07-19 | ✅ DONE | Restyle CustomerDashboardExtras to Match Fluent Design System | f9140197 |
| 2026-07-19 | ✅ DONE | Wire Live Colony Data Into 3D Scene | 3acbcb95 |
| 2026-07-19 | ✅ DONE | Add Factory Floor Nav Link and Wire HQ to Overlord Total | 5b223044 |
| 2026-07-19 | ✅ DONE | Merge Health Breakdown Into Hero | f3ed188e |
| 2026-07-19 | ✅ DONE | Auto-Trigger Sales Offer Generation On Diagnostics Completion | 128ba852 |
| 2026-07-19 | ✅ DONE | Add Animated Revenue Belts to HQ | dc72ff38 |
| 2026-07-19 | ✅ DONE | PowerShell Script Download for Requires-Script Findings | e453afb3 |
| 2026-07-19 | ✅ DONE | Move Welcome Header Above Mission Control | c8fdb18b |
| 2026-07-19 | ✅ DONE | Restyle DashboardTabs to Underline Pattern | a92bf210 |
| 2026-07-19 | ✅ DONE | Resource Category Data Audit | a7cf1c55 |
| 2026-07-19 | ✅ DONE | Reject cross-MSP tenant consent conflicts | c6d764de |
| 2026-07-19 | ✅ DONE | Fix Missing Diagnostics Runs List Endpoint + Malformed SSE URL | 16c3aa0a |
| 2026-07-19 | ✅ DONE | Build 5-Way Resource Category Split | 2ebb9b2f |
| 2026-07-19 | ✅ DONE | Assessment RBAC Role — Foundation | bd63fee9 |
| 2026-07-19 | ✅ DONE | Assessment Wizard — Scan Trigger, Progress Flow, First-Login Provisioning | 52e10b2a |
| 2026-07-19 | ✅ DONE | Assessment Results Viewer + OMG Cards | bdd4308d |
| 2026-07-19 | ✅ DONE | Account/Session Basics — Password, Login History, Real Sessions | 948f2bec |
| 2026-07-19 | ✅ DONE | GDPR Export/Deletion — Bridge to Current Customer Schema (export + deletion-request enrichment shipped; admin destructive erasure deferred via stop-and-report) | 69811f66 |
| 2026-07-19 | ✅ DONE | lead-offer-engine schema tables | e0e05be8 |
| 2026-07-19 | ✅ DONE | lead-scoring-engine schema tables | 6fe1273e |
| 2026-07-19 | ✅ DONE | quizLeadsTable lead offer result column | bf242f27 |
| 2026-07-19 | ✅ DONE | Dashboard Export + Share Link — D1 DECISION: both features render a frozen point-in-time snapshot, not a live view, matching the precedent set by the existing document-sharing feature (its public view also serves htmlContent captured at generation time, not a live re-render) — an unauthenticated *live* dashboard view would mean exposing the resolver data path with no auth, a materially bigger security surface, for a "share this" feature that doesn't need it. Server-side dashboard-snapshot.ts resolves the caller's customer_default template+override (via newly-exported resolveCallerScope/findDefaultTemplate/resolveTemplate from dashboard-overrides.ts) then calls resolveMetric per widget and renders each shape (scalar/trend/distribution/heatmap/timeline) to plain HTML/inline-SVG cards, ordered by grid position. PDF export (GET /portal/dashboard/pdf) reuses insight-pdf.ts's buildHtmlDoc+htmlToPdf Chromium pipeline unmodified, same as insights-documents. Share link (POST /portal/dashboard/share) persists the snapshot as a real insights_generated_documents row (docType "dashboard_snapshot") and creates a quick_win_result_shares row via the EXISTING token/30-day-expiry pattern — the already-existing /public/documents/:shareToken view + view-tracking routes need zero changes. PPT export not built (real follow-up, not scope-creep — chart-quality PPT export is meaningfully more work than the simple card-based PDF here, not "trivially cheap alongside it"). Dashboard collections not built (separate, later work per non-goals). | c45a5fb8 |
| 2026-07-19 | ✅ DONE | Public Website Rebuild: Stage 2 Pages — replaced Stage 1's StubPage placeholders with real, routed pages on the Stage 1 design system: 8 Solutions/Topic pages (single data-driven template) + hub, Products (catalog-driven via real /api/services, no hardcoded prices), Retainer (wires in the pre-existing but previously unrouted RetainersOverview + 3 Architect tier detail pages), Trust & Security, Quiz hub, Login (reuses the real checkout-gate email→portal-URL lookup, no new backend), and MSP Partner ToS + DPA legal pages (stub content). Home, Assessment, Monitoring, Resources, MSP, About, Contact, and Legal Terms/Privacy were already real pre-Stage-1 pages, not stubs — left untouched this stage. Personalization, real copy, and analytics visualization stay out of scope (Stage 3/4). All 21 routes verified rendering with zero console errors; typecheck clean. | a1a1b40c |
| 2026-07-19 | ✅ DONE | Fix Broken Team Invite Endpoint — customer-team.tsx's handleInvite called POST /api/portal/team/invite, which didn't exist server-side (confirmed via full route-file search). Built it in portal.ts reusing the EXISTING accountSetupTokensTable + ensureClientSetupToken + buildAccountSetupUrl + sendEmailFromTemplate("account-setup") convention (same one /admin/clients uses), not a new invite mechanism — real email via Exchange Online/Graph, never Resend. New teammate always created mspRole="CustomerUser" with customerId/mspId pinned to the INVITER's own JWT claims, never client-supplied, so cross-tenant planting is not possible; confirmed getMspClaims() (auth.ts) resolves the new user's JWT claims straight from this msp_users row at setup-password completion, so RBAC scoping is correct end-to-end. Added msp_users.department/job_title columns (manual SQL) since the invite form already sent those fields with nowhere to persist. Existing handleRevokeSessions/DELETE .../sessions route (earlier session's fix, same file) verified untouched. Traced the full flow by code (form -> route -> users+msp_users rows -> token -> email -> account-setup.tsx -> /auth/setup-password -> JWT) — not live-DB-tested per this repo's no-DB-access rule. Note: GET /portal/team and the file's other team-management handlers (status/reset-password/temp-password/reset-mfa) remain unbuilt/mocked — pre-existing gap, out of this task's scope. api-server typechecks clean (pre-existing health-engine.test.ts errors excluded, same as prior sessions' notes). | 1ac31497 |
| 2026-07-19 | ✅ DONE | Customer-Facing Cross-Domain Search — extended the real Cmd+K command-palette.tsx (unchanged for MSP staff) to branch by mspRole: CustomerUser/Assessment sessions now hit a new GET /api/portal/customer/search (routes/portal-customer-search.ts, requireRole("Assessment") floor matching portal-marketplace.ts) instead of the staff /api/msp/customers lookup. Sources wired: msp_diagnostic_findings (scoped by JWT customerId — msp_customers.id space), insights_generated_documents delivered/approved only + sales_offers sent/accepted/rejected/expired only (both scoped by JWT userId — usersTable.id space, same dual-id-space convention portal-customer-timeline.ts already uses), and services (role-scoped public catalog, same allow-set as portal-marketplace.ts — Assessment narrower than CustomerUser+). All four via plain ILIKE title/description substring match, no semantic/AI search (flagged as a real future layer). Results render grouped by source (Findings/Documents/Offers/Marketplace) in the same dialog shell/debounce/keyboard-nav. Static nav-target search (Billing, Team, Settings, etc.) added as a client-side-only customer nav list — no backend round-trip needed for those. Deferred, not built: full-text search across generated document HTML content (no index to lean on; titles carry enough way-finding signal — real follow-up needs pg_trgm/tsvector). Verification: no live DB access in this environment, so traced via automated test (portal-customer-search.test.ts, added to vitest.config.ts's include list) — a shared search term ("security") surfaces both a mocked finding and a mocked document, each correctly scoped to the caller's own customerId/userId (never client-supplied, so no cross-tenant leakage path exists). api-server and msp-portal both typecheck clean (health-engine.test.ts pre-existing debt excluded, same as prior sessions). | b13a171f |
| 2026-07-19 | ✅ DONE | Fix Remaining Mocked Team-Management Handlers — audited the 5 handlers flagged unbuilt by the invite-fix session: GET /portal/team (route never existed), PATCH .../status (never existed), POST .../reset-password (never existed), POST .../temp-password (never existed), POST .../reset-mfa (never existed) — all five were missing routes entirely, not mock-data routes; the only prior code for equivalents was the MSP-internal-staff stub at msp-settings.ts (audit-log-only, no real token/hash/table writes) which was NOT reused. Built all five real, in portal.ts, ownership-scoped via assertCustomerAccess against the TARGET's own customerId (same pattern as the existing .../sessions route), never a client-supplied customerId. GET /portal/team: real roster from mspUsersTable+usersTable scoped to the requester's own customerId, with real MFA status (mfaEnrollmentsTable + webauthnCredentialsTable, same read the admin mfa-status route uses) and real session count/last-login derived from user_sessions (mspUsersTable.lastLoginAt confirmed still dead — never written anywhere). Status: real isActive persisted to mspUsersTable, self-suspend blocked, sessions revoked on suspend. reset-password: reuses the REAL /auth/forgot-password convention (passwordResetTokensTable, 1hr token, "password-reset" email via Exchange/Graph — never Resend), not the msp-settings.ts stub. temp-password: generates + bcrypt-hashes (cost 12, matching /auth/reset-password) a real password directly into usersTable.passwordHash, returned once in the response for the existing show-once credential modal. reset-mfa: real delete across mfaEnrollmentsTable/mfaChallengesTable/webauthnCredentialsTable/webauthnChallengesTable, mirroring /admin/clients/:id/mfa-reset exactly, plus the same "mfa-reset" email template. Confirmed invite (portal.ts ~611-680) and session-revoke (~575-601) untouched (diff shows only additive routes after invite, plus two widened import lines). Known remaining gaps, explicitly NOT built (flagged, not silently left): PATCH .../mfa-enforcement has no backing column anywhere in the schema (would need a new mspUsersTable.mfaEnforced boolean + manual SQL — out of this task's 5-handler scope) so it still 404s; account-lockout (isLockedOut/Unlock button) and the emergency-MFA-bypass-code button remain client-local-only fiction — no lockout tracking exists anywhere in the schema and neither was in this task's deliverable list. api-server typechecks clean (pre-existing health-engine.test.ts spread-type errors excluded, consistent with prior sessions); msp-portal typechecks clean, no frontend changes were needed since customer-team.tsx's expected response shape already matched. Not live-DB-tested per this repo's no-DB-access rule; traced by code end-to-end (form/button → route → table writes/reads → email/audit). | 0303ffcc |
| 2026-07-19 | ✅ DONE | Public Website Rebuild: Stage 2b Home/Assessment/Monitoring — re-skinned Home.tsx, Assessments.tsx, AssessmentDetail.tsx, and Monitoring.tsx (the four pages Stage 2 left untouched as "already real") onto the actual Stage 1 charcoal/glass design system (GlassPanel/GradientText/StatPanel, Layout wrapper) — previously generic Tailwind slate classes (Home/Assessments/Monitoring) or hardcoded legacy navy #0A2540/#0078D4/#00B4D8 literals (AssessmentDetail). Guardrail fixes scoped to these four files only: removed AssessmentDetail's ICP FedRAMP/ITAR/CMMC/Gov-Contractor/Defense language (platform isn't marketed for government/FedRAMP-scoped tenants); rewrote Monitoring's "NASA Copilot Deployment Standard / distributed federal government-wide" banner to personal-bio framing; removed the fabricated "15 min Telemetry Cadence" claim (never verified) in favor of the real admin-configurable hourly/daily frequency enum (admin-monitor-checks.ts); removed the "Six specialized signal engines" total-count claim (real engine-registry.ts ENGINE_DEFS count is 12, several internal/non-tenant-facing) in favor of count-derived-from-what's-shown labeling. Grepped all four files for other NASA/FedRAMP/compliance-badge language and corrected remaining instances to personal-bio-only framing; factual "Shane did X at NASA" history left as-is. Monitoring.tsx pricing: replaced the always-"Custom" flat basePrice display with the real seat calculator (pricePerUserMonth × max(actualSeats, seatCountFloor) + flatMonthlySurcharge from typeAttributes) — same calculation Home.tsx's calculator already used, now shared across both. Home.tsx scoped to cold-visitor state only (Monitoring hero + Assessment free-proof-first CTA); personalization/recognized-visitor states are Stage 4, not this task. No functional logic touched — consent flow, catalog fetching, checkout routing untouched, presentation + copy only. Typecheck clean; all four routes verified rendering with zero console errors via a live dev server + headless Chromium pass (screenshots confirmed charcoal/glass rendering); no backend/DB in this environment, so AssessmentDetail's real catalog fetch correctly falls back to its existing NotFound state without a live slug — confirmed no crash. | 07ffee26 |
| 2026-07-19 | ✅ DONE | Password Reset Frontend — wired real backend endpoints (POST /auth/forgot-password, POST /auth/reset-password) to real UI. "Forgot password?" link on login.tsx opens an inline ForgotPasswordCard (same MFA-challenge-card toggle pattern) that posts the email; endpoint always returns ok:true (no enumeration) so UI shows a generic "check your email" state. New public /reset-password route + page reads the `token` query param matching the URL the real reset email sends, reuses account-setup.tsx's exact 8-char zod password schema/UI, posts {token, password} to /auth/reset-password, surfaces the backend's invalid/expired-token error message inline, and redirects to /login on success (no auto-login — reset-password issues no tokens, unlike setup-password). Backend logic untouched. msp-portal typechecks clean. | 3d1c40a9 |
| 2026-07-19 | ✅ DONE | Team MFA Enforcement Toggle — real `mfa_enforced` boolean on `msp_users` (default false, manual SQL), PATCH /portal/team/:userId/mfa-enforcement ownership-scoped via assertCustomerAccess mirroring the .../status handler; GET /portal/team's hardcoded `mfaEnforced: false` replaced with the real column. Enforced live at /auth/login: if mfaEnforced=true and the user has zero active MFA enrollments, login is blocked (`mfaSetupRequired: true`, no tokens issued) instead of silently letting them in; already-enrolled users are unaffected either way. login.tsx shows a real blocking "contact your administrator" card rather than dead-ending. Flagged, not built: no pre-auth self-service MFA enrollment flow exists anywhere in this codebase (every /auth/mfa/*/setup route requires a full authenticated session) — a locked-out user has no self-serve path to enroll today; the only escape hatch is an MSPAdmin/customer flipping the toggle back off via the same real endpoint. api-server and msp-portal typecheck clean (health-engine.test.ts pre-existing debt excluded, consistent with prior sessions). Not live-DB-tested per this repo's no-DB-access rule; verified by full code trace (toggle → column write → login query → block/pass branches). | 12714353 |
| 2026-07-19 | ✅ DONE | Fix Nav Dropdown Legibility — Header.tsx's Solutions/Platform/Company dropdowns used the shared .glass-panel class (6% white fill, tuned for the hero stat-panel signature element over a flat charcoal background); floated over arbitrary hero content, bright/large hero text visibly bled through. Grepped all .glass-panel usage across the app first — the only other floating-popover-style usage was these three Header.tsx dropdowns (all other callers are inline eyebrow badges / in-flow tab bars, not absolutely-positioned popovers, so left untouched). Added a new .menu-panel class in index.css — same border/blur as .glass-panel but a near-opaque charcoal-1 background (97%) — and scoped only the three dropdown panels to it, rather than raising --glass-fill globally (which would also change the stat-panel's look). Verified via a headless-Chromium pass against the dev server: all three dropdowns are fully opaque/legible even against a worst-case background (giant bold hero heading directly behind the panel). | e7852c48 |
| 2026-07-19 | ✅ DONE | Public Website Rebuild: Stage 3 Content — fixed guardrail violations left over from pre-rebuild content that Stage 2/2b explicitly left untouched (About, Contact, MSP/Partners, Resources, Retainer detail pages, and all 8 topic quiz pages): removed FedRAMP/GCC/ITAR/CMMC government-contractor ICP marketing and NASA-platform-capability framing (e.g. "NASA-Grade", "Federal-grade accountability" as a marketed feature) in favor of personal-bio-only NASA framing, consistent with the pattern Stage 2b established on Home/Assessment/Monitoring. Removed a stray ITAR mention and an unverified fixed "Hourly" cadence claim from the Solutions/Topic data (real check frequency is admin-configurable per admin-monitor-checks.ts). Fixed broken funnel CTAs found during the sweep: 12 quiz links on Resources.tsx pointed at nonexistent routes (e.g. /migration-readiness-quiz instead of the real /migration-quiz); all quick-win package recommendations in QuickWinsSelectorQuiz.tsx and CopilotQuiz.tsx's tier-based service upsells pointed at /quick-wins/* and /services/* paths not in the router — repointed to the real /products and /solutions/:slug pages; retainer breadcrumbs pointed at the killed /pricing page — repointed to /retainers and /monitoring. Removed a hardcoded "$1,500" fallback price from ArchitectEssentials.tsx (no-hardcoding rule) in favor of a real "Contact for pricing" state when the catalog returns no price. Expanded MSP Partner ToS and DPA from Stage 2 stub content to real, complete legal provisions (onboarding, white-label use, sub-processors, security measures referencing the real Trust & Security proof points, breach notification, retention/deletion, international transfers) — flagged for actual legal review before being relied on as binding. Verified unchanged: Solutions hub, Trust & Security (already matches the confirmed proof-points list exactly), Products/Monitoring pricing (already catalog-driven), QuizHub, and Home/Assessment/Monitoring (Stage 2b's corrected state confirmed still accurate). Not fixed, flagged as a known gap: Pricing.tsx, Projects.tsx, Services.tsx, and quick-wins/MicroOfferDetail.tsx remain live-routed in App.tsx despite being marked "killed/superseded" in the reference doc, and still carry heavy FedRAMP/ITAR/GCC-High marketing and NASA-platform-capability violations — unreachable from current nav but still resolve if linked directly; recommend a follow-up task to redirect or remove these routes. Individual quiz pages' og:url meta tags still use pre-rebuild URL slugs (cosmetic SEO mismatch only). shane-mccaw-consulting typechecks clean. Note: this session's staged changes were swept into a concurrent session's commit before this session could commit them standalone (git commit -a-style sweep, not silently absorbed — flagged here per this file's established convention); the 20 files are verified present and correct in HEAD. | c2978f28 (concurrent sweep, not this session's own commit) |
| 2026-07-19 | ✅ DONE | Real Account Lockout — customer-team.tsx's isLockedOut/handleUnlock was pure client-side state with zero backend and no schema concept of lockout at all. Added real failed-login tracking on msp_users (failedLoginAttempts, lastFailedLoginAt, lockedUntil — manual SQL, not yet applied) since usersTable has no per-tenant team concept and legacy platform-admin users (no msp_users row) intentionally stay lockout-exempt, matching the existing mfaEnforced precedent. Threshold: 5 failed passwords within a 15-minute window locks the account for 15 minutes (ACCOUNT_LOCKOUT_THRESHOLD/_WINDOW_MINUTES/_DURATION_MINUTES env-overridable, hardcoded defaults otherwise) — chosen as a standard, conservative anti-brute-force default; deliberately looser than the existing per-IP loginLimiter (10/15min) since this is a second, per-account layer, not a replacement. /auth/login now checks lockedUntil before the bcrypt compare (blocks with 423 + accountLocked:true, no password-validity signal leaked) and increments/locks on each bad password (423 the moment the threshold is crossed) — mirrors the mfaEnforced pattern of returning before token issuance, both now sharing one consolidated mspUsersTable lookup instead of two. A correct password always resets the counter/lock, so 1-2 mistyped attempts followed by success never trips lockout. Real unlock: POST /portal/team/:userId/unlock (portal.ts), ownership-scoped via assertCustomerAccess against the target's own customerId exactly like the sibling status/mfa-enforcement/reset-mfa handlers, resets failedLoginAttempts/lastFailedLoginAt/lockedUntil and audit-logs team_member_unlocked. GET /portal/team's hardcoded isLockedOut:false replaced with the real column-derived value. customer-team.tsx's handleUnlock now calls the real endpoint (fire-and-forget on failure, same pattern as reset-mfa) before its existing optimistic local-state update. Verified by full code trace, not live DB (no DATABASE_URL in this environment): repeated bad passwords -> counter increments -> 5th returns 423 and blocks token issuance -> admin unlock clears the row -> next login proceeds normally through the untouched MFA/token-issuance path. api-server and msp-portal both typecheck clean (health-engine.test.ts pre-existing TS2698 debt excluded, consistent with prior sessions). | 83270c98 |
| 2026-07-19 | ✅ DONE | api-server-typecheck-fixes — re-verified the task brief's claimed 11 errors across workflow-executor.ts (missing `evaluate_signal_policies` union member, raw-SQL `decayRate` string-vs-number cast) and admin-insights.ts (7 `.warn`/`.info` call sites). All already fixed by an earlier same-day session (7d375bfa, 2026-07-18: added the union member, added `coerceDecayRate()` at the two cast sites, and renamed a local closure that was shadowing the module-level structured logger in admin-insights.ts's executeAutomation — the real bug: those 7 call sites were correct against the real `logger.child()` API, they were just hitting the shadowed local instead). No code changes made this session. Ran `pnpm run typecheck:libs` (clean) and `pnpm run typecheck` (all 8 artifacts/**+scripts packages with a typecheck script) — only remaining failure is the pre-existing, out-of-scope health-engine.test.ts TS2698 spread-type debt (vi.mock importOriginal() typed as unknown), consistent with every prior session's notes. | 7d375bfa (pre-existing) |
| 2026-07-19 | ✅ DONE | Remove Killed Legacy Routes: Pricing/Services/Projects — removed /pricing, /services, /projects routes and their page components (Pricing.tsx, Services.tsx, Projects.tsx) from artifacts/shane-mccaw-consulting, killed per website-rebuild-reference-v2.md's sitemap despite remaining live-routed in App.tsx as flagged by the Stage 3 session (c2978f28). quick-wins/MicroOfferDetail.tsx checked via the same live-routed test but is already unrouted in App.tsx — left untouched, not in scope. Fixed the 9 dangling internal links this broke: Checkout.tsx and not-found.tsx's "view all services"/"explore services" cards repointed to /products; ArticleAssessmentCTA.tsx's 7 per-topic "Take the Free X Assessment" CTAs repointed from /services#anchor fragments to the matching real /solutions/:slug topic pages (a better content match than /products for assessment CTAs specifically). Updated vite.config.ts's STATIC_PAGES sitemap-generator array to drop the killed routes' entries (including stale /services/* sub-path entries that predated the Stage 2 /solutions rebuild and were already dead); public/sitemap.xml regenerated automatically by the vite dev server's sitemap plugin, confirming the generator change took effect. Header.tsx/Footer.tsx already had no references (clean). shane-mccaw-consulting typechecks clean. | 5e0211f5 |
| 2026-07-19 | ✅ DONE | Database-Driven Hero Headlines with Seasonal Scheduling — replaced Home.tsx's hardcoded hero headline with admin-authored, runtime-editable content (same philosophy as Monitor Checks/Signal Rules): new hero_headlines table (leadText/gradientText/active/seasonalLabel/startDate/endDate/sortOrder), seeded with the 3 requested evergreen headlines in the manual SQL migration itself. Public GET /api/public/hero-headlines (no auth, active + evergreen-or-in-seasonal-window, ordered by sortOrder) in a new public-hero.ts; admin CRUD (GET/POST/PATCH/DELETE) added to admin-marketing.ts under /api/admin/marketing/hero-headlines, mirroring the existing outreach-templates CRUD block exactly (requireAdmin, inline req.body casts, flat try/catch, no logging — confirmed that's the real convention: `log`/`req.log` calls in that file are reserved for AI-generation failure paths, plain CRUD never logs). New admin-panel screen HeroHeadlines.tsx (list/create/edit/delete/toggle-active, modeled on Coupons.tsx's hand-rolled table+form pattern — no shared DataTable/Modal exists in this codebase), wired into Content & Offers > Publishing nav. Home.tsx hero now fetches on load via new useTypewriterHeadline() hook (artifacts/shane-mccaw-consulting/src/hooks/useHeroHeadlines.ts) and types/pauses/deletes/loops through the returned set inside a single <h1> (SEO: one element, JS-only text cycling); falls back to a single hardcoded headline ("Your tenant has problems. We find them before your CEO does.") if the API returns zero rows or fails — this is the actual path exercised in this environment (no DATABASE_URL, so api-server can't run against a real DB) and was verified live via a headless-Chromium pass against the vite dev server (PowerShell required for PORT/BASE_PATH env — Git Bash mangles a leading "/" BASE_PATH via MSYS path conversion into "/Program Files/Git/", same class of gotcha as the admin-panel-local-vite-windows precedent): typewriter renders mid-type on the fallback headline, gradient styling correct, zero console errors. Rest of the copy pass landed: new subhead, credibility-panel copy, Engine Overview subhead. Engine Overview header deliberately renders `{ENGINES.length} Engines. Zero Guesswork.` (dynamically 6) instead of the task brief's literal "12 Engines" — a prior session's own guardrail comment on that exact ENGINES array explicitly warns against labeling this list with the platform's full internal engine count (12 includes non-tenant-facing business-ops engines), the same violation Stage 2b already fixed once for "Six specialized signal engines"; flagging here rather than reintroducing it silently. Catalog/CTA sections reviewed for tone per the task brief and left unchanged — already blunt/professional, no unverifiable claims to fix. Three other sessions (Session Replay, Heatmap Visualization) were concurrently editing the same shared files (routes/index.ts, workspaceNav.tsx, admin-marketing.ts) uncommitted throughout this session; committed via targeted `git add` of only this task's files/hunks after confirming each diff was cleanly isolated, their in-progress work left untouched. api-server, admin-panel, and shane-mccaw-consulting all typecheck clean (pre-existing health-engine.test.ts TS2698 debt excluded, consistent with prior sessions); lib/db declaration output rebuilt via `tsc -b lib/db` after the schema change (composite project, emitDeclarationOnly — referencing packages don't pick up new exports until rebuilt). Needs manual SQL: 2026-07-19-hero-headlines.sql (creates hero_headlines, seeds the 3 evergreen headlines) — headlines will not appear on the live site until Shane runs this. | 831422fc |
| 2026-07-19 | ✅ DONE | Admin Panel: Heatmap Visualization — new admin-analytics-heatmap.ts route (not an extension of admin-marketing.ts or routes/analytics.ts) aggregates the click/scroll-position capture already live on the public site (confirmed the real analytics_site_events schema — sessionId/page/eventType/elementLabel/elementHref/metadata jsonb — and analytics_sessions/analytics_pageviews via direct read before building, plus the rebuilt frontend tracker artifacts/shane-mccaw-consulting/src/lib/analytics.ts, which confirmed click/cta_click/nav_click/outbound_click/rage_click/dead_click all carry {x,y} pageX/pageY in metadata; there is no continuous mousemove capture, only click coordinates, and viewport width is never captured — flagged in the route's own comment and surfaced to the admin in-UI, not hidden). GET .../heatmap/pages (page picker, ranked by positioned-event count) and GET .../heatmap (page + date range + device-class filter) return a server-aggregated 24x32 grid — click cells and friction (rage/dead-click) cells bucketed separately, x normalized to a per-device-type reference width (desktop/tablet/mobile — the only granularity analytics_sessions stores), y normalized to the 99th-percentile observed click y as an estimated page height — plus a real continuous scroll-depth curve from analytics_pageviews.max_scroll_pct (not the coarser 25/50/75/100% milestone events). growth.website-analytics channel, mirrors routes/analytics.ts's execRows/resolveRange conventions; used IN(...)-with-hardcoded-literal instead of = ANY(array-param) since no ANY-array precedent existed in this codebase to confirm driver serialization. New admin-panel page AnalyticsHeatmap.tsx: page/range/device filter row, stat tiles, a sequential-blue click-density grid (dataviz skill's sequential-ramp formula, one hue light→dark) with destructive-red friction rings, per-cell hover tooltips, a "Top Hotspots" plain-table accessible fallback (color is never the only channel), and a scroll-depth bar chart. Wired into Command workspace nav (cmd-heatmap, Flame icon, next to the concurrent session's Session Replay entry) and CommandWorkspace.tsx's section switch — no existing nav restructuring. Does not touch the website frontend or build session replay (parallel task, separate file set, confirmed no route-path overlap). Verified live via a throwaway Playwright harness (harness.html + stubbed fetch/EventSource mounting the real <App/> at /command/heatmap, deleted after use, per the admin-panel-local-vite-windows verification recipe) — zero console errors, page picker/range/device filter/hover tooltips/scroll-depth all render correctly against mocked data; not live-DB-tested per this repo's no-DB-access rule. Two other sessions (Hero Headlines, Session Replay) were concurrently editing the same shared files (routes/index.ts, workspaceNav.tsx, CommandWorkspace.tsx) uncommitted throughout this session; this commit was built via targeted `git add -p` patches containing only this task's hunks so their in-progress uncommitted work was left untouched, not swept in. api-server and admin-panel both typecheck clean for the touched files (pre-existing health-engine.test.ts TS2698 debt and the concurrent Hero Headlines session's not-yet-exported heroHeadlinesTable errors excluded, neither caused by or fixed by this session). No schema/migration changes needed (existing tables only). | d728e0bd |
| 2026-07-19 | ✅ DONE | Admin Panel: Session Replay Player — recorded, reviewable-after-the-fact reconstruction of a visitor's session (page views, clicks, scroll milestones, in order), explicitly NOT a live viewer per website-rebuild-reference-v2.md §4's locked distinction (that section also confirms the existing `/admin/analytics/live` + live-stream SSE endpoints are a separate, already-existing feature, not this one). Confirmed the real analytics_sessions/analytics_pageviews/analytics_site_events schema and the rebuilt frontend tracker (artifacts/shane-mccaw-consulting/src/lib/analytics.ts) via direct read before building — no field names assumed. New route file admin-analytics-session-replay.ts (not an extension of admin-marketing.ts or routes/analytics.ts): GET .../sessions (filterable by lead email + date range, paginated) and GET .../sessions/:sessionId (session metadata + pageviews + a merged, chronologically-sorted timeline of pageview enter/exit and site-event rows) — read-only, growth.website-analytics channel, mirrors routes/analytics.ts's existing execRows/resolveRange conventions. New admin-panel page SessionReplay.tsx: session list/search view + a scrubbable playback view (timeline bar with page segments + event tick marks, play/pause, 1x/4x/8x speed, click-to-seek, auto-scrolling event log, scroll-depth gauge derived from the nearest preceding scroll_milestone). Wired into Command workspace nav (cmd-session-replay, next to Analytics) and CommandWorkspace.tsx's section switch — no existing nav restructuring. Does not touch the website frontend, build any live-viewing capability, or build heatmap visualization (parallel task, separate file set — confirmed no overlap). Two other sessions (Hero Headlines, Heatmap Visualization) were concurrently editing shared files (routes/index.ts, workspaceNav.tsx, CommandWorkspace.tsx) uncommitted throughout this session; this commit was built via targeted patches containing only this task's hunks so their in-progress uncommitted work was left untouched, not swept in. api-server and admin-panel both typecheck clean for the touched files (pre-existing health-engine.test.ts TS2698 debt and the concurrent Hero Headlines session's not-yet-exported heroHeadlinesTable errors excluded, neither caused by or fixed by this session). Not live-DB-tested per this repo's no-DB-access rule; no schema/migration changes were needed (existing tables only). | 8e41c4df |
| 2026-07-19 | ✅ DONE | Public Website Rebuild: Stage 4a Personalization Foundation — shared plumbing only (website-rebuild-reference-v2.md §3), no page wiring (Stage 4b). Confirmed all three real mechanisms via code read before building: (1) auth-session detection for the "assessment" tier reuses the EXISTING POST /api/auth/refresh (httpOnly refresh-token cookie) + Authorization Bearer pattern LandingPage.tsx's gate-status check already uses from this same public site — not a new mechanism; a real session is one where the refreshed JWT payload carries a customerId (Assessment always requires an account, no exception, per §3). (2) quiz-lead lookup for the "quiz" tier reuses the durable smc_sid cookie from Stage 1's analytics.ts (now exported via getAnalyticsSessionId(), same read-or-create logic, not a second id scheme) — no endpoint previously resolved session→lead, so built one: new public GET /api/public/personalization/state (public-personalization.ts) resolves sessionId → analytics_sessions.identified_email (set by the existing POST /api/analytics/identify) → most recent quiz_leads row for that email; deliberately queries quizLeadsTable directly, not leadsTable/findLeadByEmail (lead-intent.ts) — confirmed those are a separate CRM/engagement-offer lead system, zero shared code with the Lead Offer Engine per §3's own note. (3) Assessment-results data source confirmed as the real GET /api/portal/assessment-results/:serviceSlug (msp-diagnostics.ts, requireAuth) — the actual data msp-portal's Assessment Results Viewer (assessment-dashboard.tsx) reads; ruled out a same-named decoy, /api/portal/assessment-results with no slug (portal-customer-engines.ts, requireRole("CustomerUser")) which returns only `enabledModules`, an unrelated shape. usePersonalizationState() (context + provider, mounted in App.tsx) resolves every visitor to cold\|quiz\|assessment, assessment tier checked first. Typed fetchers: useQuizOfferData() calls the real GET /api/quiz/results/:leadId (quiz.ts) using a resend token the new endpoint mints server-side — that route is HMAC-token-gated (verifyResendToken) and the token is only computable server-side (JWT_SECRET), so makeResendToken was exported from quiz.ts rather than duplicating the results payload shape; leadOffer === null is handled explicitly as a valid state, not an error. useAssessmentResultsData(serviceSlug) calls the real assessment-results route with the Bearer token, typed to its actual response shape (confirmed by direct read — msp-portal's own AssessmentResultsPayload TS interface in module-registry.ts does NOT match what the backend actually returns; flagged as pre-existing drift, not fixed, out of this task's scope). <PersonalizedContent cold quiz assessment> structurally enforces the confidence-tier tone rule — quiz-tier content always renders inside a visible "Based on what you told us" frame; assessment-tier renders as-is (stated as fact, no hedging); cold renders while the tier is still resolving, never a blank page. growth.website-analytics channel on the new endpoint (same subsystem analytics.ts/quiz.ts already log under). api-server and shane-mccaw-consulting both typecheck clean (pre-existing health-engine.test.ts TS2698 debt excluded, consistent with prior sessions); verified live against the vite dev server (no api-server/DB in this environment) — all 5 new/changed frontend modules transform through Vite with zero errors, App.tsx renders with PersonalizationProvider mounted. NOTE: this session's staged-but-uncommitted changes were swept into a concurrent session's commit (8aec0b17, "Investigate Real Licensing Cost Trend Data") before this session could commit them standalone — flagged on both sides per this file's established convention (see that row's own note); all files verified present and correct in HEAD. | 8aec0b17 (concurrent sweep, not this session's own commit) |
| 2026-07-19 | ✅ DONE | portal-favicon-logo-swap — replaced msp-portal's flat navy favicon.svg (fbc3a616) with the new master-logo PNG icon set (16/32/48 favicons + 180x180 apple-touch-icon + 512x512 for future PWA use), all placed in artifacts/msp-portal/public/ and verified at their exact pixel dimensions before wiring. index.html's `<head>` updated to the 4-tag PNG set; old favicon.svg deleted after confirming zero references remained in artifacts/msp-portal/src. No manifest.json/webmanifest exists for msp-portal, so step 4 (icon-512 into a manifest) was skipped rather than fabricating one. Grepped admin-panel, shane-mccaw-consulting, and crm — each has its own separate `/favicon.svg` reference (own app, own public/favicon.svg), untouched, out of scope. Pure static-asset + markup swap, no logic/schema change, no typecheck-relevant surface. | 3121ab24 |
| 2026-07-19 | ✅ DONE | admin-panel-favicon-logo-swap | bacbf7ee |
| 2026-07-19 | ✅ DONE | Investigate Real Licensing Cost Trend Data — pure investigation, no code changes. FINDING 1 (Graph scope): current enforced consent scope is `REQUIRED_MT_SCOPES` (artifacts/api-server/src/lib/graph.ts:23-36) — Directory.Read.All, SecurityEvents.Read.All, Exchange.ManageAsApp, Sites.Read.All, Reports.Read.All, Policy.Read.All, DeviceManagementConfiguration.Read.All, AuditLog.Read.All, ActivityFeed.Read, IdentityRiskyUser.Read.All, AccessReview.Read.All, TeamSettings.Read.All — none of these carry cost/pricing data, and even Organization.Read.All (needed for /subscribedSkus) isn't in the enforced list (a broader doc/marketing-copy list exists separately in artifacts/crm/src/lib/requiredPermissions.ts, not the real consent set). FINDING 2 (real cost API is a different auth surface entirely): Microsoft Cost Management data lives behind Azure Resource Manager (management.azure.com) under Azure RBAC (Cost Management Reader/Billing Reader on the customer's Azure subscription) — a completely separate grant from Microsoft Graph delegated/admin-consent scopes. This platform does have pre-existing ARM auth plumbing (artifacts/api-server/src/lib/azure-credentials.ts:30-43 validates ClientSecretCredential tokens against management.azure.com/.default), but it's used only to test customer App Registration credentials, and the only real ARM feature built on it (Azure Automation runbook execution, azure-automation.ts) is explicitly retired/no-op. No ARM Cost Management calls exist anywhere. Building this for real would mean requesting a net-new Azure subscription-level RBAC grant from every customer tenant — a re-consent/product decision explicitly out of this task's scope per its own non-goals, so not requested. FINDING 3 (checked and ruled out Shane's recollection of an existing SKU price table): no per-SKU dollar price table exists anywhere in the repo (grepped for typical M365 list-price numbers + SKU_PRICES/licensePricing/skuCost-style names — zero matches). licensing.wasteEstimateBreakdown (lib/dashboard-registry/src/metrics.ts:774-783, "License Waste by SKU") is honestly labeled as a categorical distribution (not currency) and its transform, groupByCount (artifacts/api-server/src/lib/monitor-executor.ts:318-331, wired via lib/db/drizzle/0191_update_monitor_checks_transforms.sql), is a per-SKU seat COUNT — no multiplication by any price constant anywhere. Only licensing.costTrend (metrics.ts:822-831) is typed currency, and its own sourceKey is literally "not_collected:license-cost" / status "not_collected" — the registry already self-documents the gap correctly. RECOMMENDATION (given to Shane, declined this session): the only realistically buildable real-$ path without new Azure/Graph consent is a simple admin-entered "monthly per-SKU cost" table (manually maintained by Shane/admin, not live-collected) feeding licensing.costTrend honestly labeled as admin-entered, not automated. Shane chose NOT to build this now. No further action taken; gap remains explicitly unfilled and correctly marked not_collected in the registry. NOTE: this session's commit (8aec0b17) unintentionally swept in another concurrent session's already-staged, uncommitted Stage 4a Personalization Foundation work (public-personalization.ts, PersonalizedContent.tsx, usePersonalizationData.ts, usePersonalizationState.tsx, analytics.ts additions, App.tsx/routes/index.ts/quiz.ts wiring) — those files were already staged in the index before this session ran `git add`, not authored by this session. Flagged per this file's established convention rather than silently absorbed; that work is real and present in HEAD, just mis-attributed to this commit's message. | (investigation only) + 8aec0b17 (commit also carries a swept-in concurrent session's Stage 4a Personalization work, not this task's own)
