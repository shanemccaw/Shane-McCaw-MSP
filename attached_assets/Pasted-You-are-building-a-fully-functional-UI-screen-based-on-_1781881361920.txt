You are building a fully functional UI screen based on the following design system. 
Use modern HTML, CSS (Tailwind or clean utility classes), and minimal JavaScript.

Your job:
1. Convert the entire design system into a real, functional screen.
2. Implement the layout, spacing, typography, colors, and components exactly as described.
3. Use a clean, responsive grid (12-col desktop, 8-col tablet, 4-col mobile).
4. Build a dashboard-style screen with cards, sections, and structure that matches the design language.

-------------------------------------
DESIGN SYSTEM (IMPLEMENT EXACTLY)
-------------------------------------
[PASTE THE CONTENTS OF DESIGN.MD HERE]

-------------------------------------
REQUIRED OUTPUT
-------------------------------------

Create a complete, production-ready screen that includes:

### 1. Global Layout
- A centered container with max-width: 1280px
- 24px gutters on desktop, 16px on mobile
- Background using `background` and `surface` tokens
- Use tonal layering for depth (Level 0–3)

### 2. Header Section
- Large headline using `headline-lg`
- Subheader using `body-md`
- Use primary color for emphasis

### 3. Card Grid
Create a 3-column responsive grid of cards:
- Each card uses Level 1 elevation
- Rounded corners according to the design system
- Internal padding based on spacing rules
- Title (headline-md)
- Metadata row using label-md
- Body text using body-sm

### 4. Progress Bar Component
Implement the ultra-thin progress bar:
- 4px height
- Light gray track
- Teal gradient fill
- Optional percentage label above

### 5. Status Badge Component
- Small pill shape (full radius)
- Muted teal background
- Dark teal text
- Use label-md typography

### 6. Kanban Board Section
- 4 columns
- Transparent background
- Dashed 1px border
- Cards using Level 1 elevation
- Drag indicator using teal accent

### 7. Input Field Component
- 4px radius
- 1px border (#E2E8F0)
- On focus: primary blue border + light teal glow
- Optional left icon (20px, 1.5px stroke)

### 8. Responsive Behavior
- Collapse grid to 1-column on mobile
- Maintain spacing rules (8px internal, 64px between sections)

### 9. Code Requirements
- Use semantic HTML
- Use Tailwind classes OR generate a custom CSS file using the design tokens
- Keep code clean, modular, and readable
- Include comments explaining component structure

### 10. Deliverables
Output:
- Full HTML
- Full CSS (or Tailwind classes)
- Any minimal JS needed for interactions (drag/drop, hover states)
- No placeholder lorem ipsum — use meaningful labels and sample data

-------------------------------------
GOAL
-------------------------------------
Produce a polished, professional, functional UI screen that visually matches the design system and demonstrates the brand personality: “The Trusted Advisor” — calm, organized, efficient, and precise.

