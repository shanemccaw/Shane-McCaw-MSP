# Archive: superseded explorations
These are the option sheets (Shanes Life 00 … 17) that led to the prototype, kept for history. Every screen on them was redesigned afterwards in `../Shanes Life - First Slice Prototype.dc.html`, which is the only design to build from. Do not implement from these pages, and do not reconcile the prototype against them.

Reference only (not screens): `Shanes Life 13 - Architecture` (system diagram), `Shanes Life 14 - Critters` (drawing spec; the SVG symbols themselves are the `<defs>` at the top of the prototype).

`screenshots/` here are pre-v4 renders of the old skin (single-page Lists, Dates without the calendar, six-tab Money).
