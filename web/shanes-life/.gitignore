node_modules/
.env
.env.local
*.log

# Real pg_dump backups taken by bin/migrate-financial-core-data.mjs before it writes to TARGET
# (Git #3295) -- these hold Shane's real financial data verbatim and must never be committed.
backups/
