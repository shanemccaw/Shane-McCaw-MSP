{
  "issue": 1113,
  "feature": "Security Intelligence Overview (/security-overview) Top Security Risks widget now renders a non-functional 'QUICK FIX' chip (data-testid='finding-quick-fix-chip') next to findings whose title/checkLabel text matches a known write-action domain keyword (users/sign-in, MFA, licensing, groups, teams, SharePoint/OneDrive). Prep for Launch Control write-action integration (#1068-1075/#1077, part of epic #1045) — purely visual, no click handler, not a real per-finding link to a write_action_catalog row (schema investigation in the issue's own comment found no clean join key exists today).",
  "baseUrl": "{{DEPLOY_URL}}",
  "loginAs": "Customer — RBAC test",
  "lastVerifiedAgainstCommit": "0231b1938",

  "notes": [
    "The chip is a client-side, presentational-only text heuristic (TopSecurityRisks.tsx's hasLikelyWriteAction()) — NOT driven by server data, no guaranteed match against any specific tenant's real finding content. This manifest only asserts the structural/always-true parts, not a guaranteed chip.",
    "ACCOUNT-TIER GAP -- RESOLVED (#4091): this note previously recorded that /api/portal/mission-control/overview (requireRole('Customer')) 403'd the single shared testbed login because it resolved to the pre-provisioned 'Assessment' role, explicitly below Customer (requireAuth.ts ROLE_ORDER). #4091 migrated this manifest onto #4089's real, dedicated Customer-tier RBAC test profile ({{TEST_RBAC_CUSTOMER_EMAIL}}/{{TEST_RBAC_CUSTOMER_PASSWORD}}, AccountTier=Customer), so D1/D2/D3 below have been flipped back to critical (this note's own prior instruction) -- re-run to confirm they now clear the Customer floor."
  ],

  "apiTests": [
    {
      "name": "SETUP — log in as the designated TESTBED Assessment account; extract the bearer.",
      "method": "POST",
      "path": "/api/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "email": "{{TEST_RBAC_CUSTOMER_EMAIL}}", "password": "{{TEST_RBAC_CUSTOMER_PASSWORD}}" },
      "expect": { "status": 200, "jsonPath": "$.accessToken", "exists": true },
      "extract": { "as": "authToken", "jsonPath": "$.accessToken" }
    },
    {
      "name": "D1 — GET /api/portal/mission-control/overview returns the real findings array the Top Security Risks widget renders from.",
      "method": "GET",
      "path": "/api/portal/mission-control/overview",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.findings", "exists": true },
      "critical": true
    }
  ],

  "uiSteps": [
    {
      "name": "goto the real Security Intelligence Overview page (branded, slug-scoped route).",
      "action": "goto",
      "target": "/portal/shane-mccaw-consulting/security-overview"
    },
    {
      "name": "D2 — Top Security Risks widget genuinely renders (structural — real findings list, not the empty state), proving the chip logic runs against real data.",
      "action": "expect",
      "selector": "[data-testid='top-security-risks-widget']",
      "state": "visible",
      "critical": true
    },
    {
      "name": "D3 — present-if-any: when a QUICK FIX chip renders at all, it carries the expected label. Expected-empty for any tenant whose current findings don't match the keyword heuristic, so this stays non-critical for that reason alone.",
      "action": "expect",
      "selector": "[data-testid='finding-quick-fix-chip']",
      "state": "present",
      "textContains": "QUICK FIX",
      "critical": false
    }
  ]
}
