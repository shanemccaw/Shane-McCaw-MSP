{
  "issue": 1132,
  "feature": "Enhanced-tier findings whose check hit a real Microsoft Graph license gap (checkStatus='license_gap') now render a locked-state card (Lock icon, plain-language explanation, data-testid='license-gap-locked-card') instead of a blank/error-looking finding, reusing #1099's Copilot-readiness locked-card visual pattern (LicenseGapUpsellCard.tsx). Two real CTAs per Shane's confirmed hybrid (issue comment 2026-08-18): a self-serve link straight to Microsoft's own SKU pricing page (license-gap-purchase-links.ts, data-testid='license-gap-selfserve-link', never a resale), and a sales-assisted path (data-testid='license-gap-chat-cta') that opens ShaneBot with the migration intent pre-sent via the portal's existing useSupportChat()/sendMessage() mechanism, which always resolves the shanebot_paid instance server-side (route-keyed, no client instance selection needed). Wired into every findings render surface: TopicFindings.tsx (6 topic pages), IntelligenceSignals.tsx + RiskDetailDrawer.tsx (security-overview), TopicRemediationModal.tsx (governance/compliance/etc.), and MissionControl.tsx (dashboard home) — plus a compact Lock badge (in place of the severity chip) on the two Top-5 rank-list widgets (TopSecurityRisks.tsx, TopGovernanceRisks.tsx) whose detail drawer/modal shows the full card. portal-mission-control.ts's GET /overview mapper now resolves each license-gapped finding's tenant-tiered purchase recommendation (buildLicenseGapPurchase/recommendationForCheckKey) into a licenseGap field.",
  "baseUrl": "{{DEPLOY_URL}}",
  "loginAs": "Customer — RBAC test",
  "lastVerifiedAgainstCommit": "0231b1938",

  "notes": [
    "REAL LIVE DATA CONFIRMED (shaneapp://executeSql, 2026-08-18): customer_id=1 (the TEST_RBAC_CUSTOMER_EMAIL testbed tenant) has genuine checkStatus='license_gap' findings across all 3 license-gap-purchase-links.ts categories (identity:pim-eligible-roles, security:antiphishing-coverage, governance:retention-label-adoption, among ~18 other gapped check keys, 44-58 finding rows each across historical runs) — a real tier-3/consolidated-E7 recommendation case, not a hypothetical. Titles read e.g. 'Not checked — requires Microsoft Defender for Office 365' / 'requires Microsoft Entra ID P2 or Microsoft Entra ID Governance' / 'requires Microsoft Purview retention/eDiscovery capability'.",
    "ACCOUNT-TIER GAP -- RESOLVED (#4091): this note previously recorded that GET /api/portal/mission-control/overview (requireRole('Customer')) 403'd the single shared testbed login because it resolved to the pre-provisioned 'Assessment' role, explicitly below Customer (requireAuth.ts ROLE_ORDER) -- despite customer_id=1 genuinely holding the license-gap findings above. #4091 migrated this manifest onto #4089's real, dedicated Customer-tier RBAC test profile ({{TEST_RBAC_CUSTOMER_EMAIL}}/{{TEST_RBAC_CUSTOMER_PASSWORD}}, AccountTier=Customer), so D1/D2/D3/D4 below have been flipped back to critical (their own prior instruction: 'provision a Customer-tier test account... then flip these back to critical') -- re-run to confirm they now clear the Customer floor.",
    "Server-side field shape (portal-mission-control.ts) and the client LicenseGapUpsellCard component were verified by direct code read + a clean tsc --noEmit on every touched file (both api-server and msp-portal packages) — not a live UI assertion for the locked-card's exact rendered content, documented honestly rather than faked given the account-tier gap above."
  ],

  "apiTests": [
    {
      "name": "SETUP — log in as the designated TESTBED Assessment account; extract the bearer.",
      "method": "POST",
      "path": "/api/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "email": "{{TEST_RBAC_CUSTOMER_EMAIL}}", "password": "{{TEST_RBAC_CUSTOMER_PASSWORD}}" },
      "expect": { "status": 200, "jsonPath": "$.accessToken", "exists": true },
      "extract": { "as": "authToken", "jsonPath": "$.accessToken" }
    },
    {
      "name": "D1 — GET /api/portal/mission-control/overview returns findings carrying the new licenseGap field.",
      "method": "GET",
      "path": "/api/portal/mission-control/overview",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.findings", "exists": true },
      "critical": true
    }
  ],

  "uiSteps": [
    {
      "name": "goto the real Security Intelligence Overview page (branded, slug-scoped route) — customer_id=1's real license-gap findings should surface here via IntelligenceSignals.tsx.",
      "action": "goto",
      "target": "/portal/shane-mccaw-consulting/security-overview"
    },
    {
      "name": "D2 — a license-gap locked card renders with the Lock icon + explanation, instead of a blank/error finding — real license-gap findings genuinely exist for this tenant (confirmed via SQL).",
      "action": "expect",
      "selector": "[data-testid='license-gap-locked-card']",
      "state": "present",
      "critical": true
    },
    {
      "name": "D3 — the self-serve Microsoft pricing-page link renders alongside the sales-assisted ShaneBot CTA (the confirmed hybrid).",
      "action": "expect",
      "selector": "[data-testid='license-gap-selfserve-link']",
      "state": "present",
      "critical": true
    },
    {
      "name": "D4 — the sales-assisted CTA renders.",
      "action": "expect",
      "selector": "[data-testid='license-gap-chat-cta']",
      "state": "present",
      "textContains": "Talk to us about migrating",
      "critical": true
    }
  ]
}
