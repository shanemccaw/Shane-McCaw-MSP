{
  "feature": "CopilotAssessmentLanding reachable at /LP/copilot-assessment; / renders the restored multi-service Home.tsx (Git #1089)",
  "baseUrl": "{{DEPLOY_URL}}",
  "lastVerifiedAgainstCommit": "TBD",
  "notes": [
    "Git #1087 originally mounted CopilotAssessmentLanding.tsx at BOTH / and /LP/copilot-assessment.",
    "Git #1089 restored the pre-7/27 multi-service Home.tsx (Monitoring calculator, paid/free",
    "assessment split, retainers) to / from commit 8224d2e7a, wired against the reconciled Product",
    "Catalog/useCatalog shapes confirmed in #1086/#1090/#1091. /LP/copilot-assessment keeps rendering",
    "CopilotAssessmentLanding.tsx completely unchanged — App.tsx's route registration for it was not",
    "touched. This manifest now asserts the SPLIT: / shows the restored Home's own real markers",
    "(not the quiz), and /LP/copilot-assessment still shows the quiz markers exercised in more depth",
    "by copilot-readiness/home-quiz-scoring.json (which moved its own goto targets to",
    "/LP/copilot-assessment in this same #1089 pass) — no duplicate scoring coverage added here."
  ],
  "uiSteps": [
    { "action": "goto", "target": "/" },
    { "action": "expect", "selector": "[data-testid='home-hero-badge']", "state": "visible", "textContains": "Built by a NASA M365 Architect" },
    { "action": "expect", "selector": "#seat-count", "state": "visible" },
    { "action": "expect", "selector": "#catalog", "state": "present" },
    { "action": "expect", "selector": "[data-testid='quiz-option-A']", "state": "absent" },

    { "action": "goto", "target": "/LP/copilot-assessment" },
    { "action": "expect", "selector": "[data-testid='hero-headline']", "state": "visible" },
    { "action": "expect", "selector": "[data-testid='quiz-option-A']", "state": "visible" },
    { "action": "expect", "selector": "[data-testid='get-real-score-cta']", "state": "visible" }
  ]
}
