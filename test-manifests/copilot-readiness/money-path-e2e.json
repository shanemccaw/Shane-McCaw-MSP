{
  "issue": 987,
  "feature": "Money-path E2E (consolidates #886–891) — setup → create/execute → verify login → teardown, one self-contained account-lifecycle flow",
  "baseUrl": "{{DEPLOY_URL}}",
  "lastVerifiedAgainstCommit": "6cb87e0f",

  "notes": [
    "GIT #987 (Epic #803). This single manifest CONSOLIDATES the six money-path manifests — #886 assessment/home-quiz.json, #887 copilot-readiness/consent-scan-trigger.json, #888 copilot-readiness/verdict-pillars.json, #889 copilot-readiness/documents-screen.json, #890 copilot-readiness/sow-proposal.json, #891 copilot-readiness/checkout.json — into ONE create → use → verify → teardown lifecycle, restructuring the account lifecycle around every content/theme/honesty assertion those six already proved (none discarded; each is preserved and labelled with its origin issue below).",

    "PHASE → EXECUTOR-BUCKET MAPPING (MainWindow.RunManifestAsync runs phases in a FIXED order: apiTests → graphTests → postGraphApiTests → zohoTests → uiSteps → powerShellVerify, all sharing ONE #877 TestRunVariables store so an extracted value flows forward into every later phase). Setup + Create + Execute(API) + Verify all live in `apiTests` (they must be strictly ordered and precede teardown). Teardown lives in `postGraphApiTests` — because graphTests is empty it runs immediately after apiTests, i.e. AFTER verify. The preserved UI content assertions live in `uiSteps`. Every step's `name` is prefixed with its phase (SETUP/CREATE/EXECUTE/VERIFY/TEARDOWN) so a failure names its phase straight from the live log: apiTests/postGraphApiTests log on the `testing.api-executor` channel, uiSteps on `testing.ui-executor`.",

    "WHY TEARDOWN IS in postGraphApiTests, NOT after uiSteps: the executor has NO HTTP bucket after uiSteps (only powerShellVerify, which runs pwsh Get-* cmdlets, not arbitrary POSTs). So an HTTP teardown structurally cannot run after the UI assertions. This is safe here because the teardown is the CONSERVATIVE #986 reset (clears tenant_engine_overrides + the write-action audit log only) plus the rollback-endpoint contract check — it deletes NO account, scan run, verdict, document, or SOW that the uiSteps observe.",

    "*** HONESTY CAVEAT — 'CREATE ITS OWN ACCOUNT' (the load-bearing constraint) ***: the platform mints the credentialed account (`users` row) ONLY inside the Microsoft admin-consent OAuth callback (`provisionProspectAccount`, direct-tenant-provisioning.ts), and only unlocks its password via `POST /api/public/flow/set-password` AFTER a real Stripe payment flips the checkout session to `paid`. Neither the interactive Microsoft admin-consent OAuth redirect nor a genuinely-confirmed Stripe PaymentIntent is drivable by BuildConsole's WebView2 + HTTP harness, and the harness has NO client-side unique-value generator (TestRunVariables only holds Settings config vars + values extracted from real responses — see TestRunVariables.cs). So a fully harness-minted, unique-per-run login credential is architecturally impossible past that consent+payment wall. What this flow DOES do, honestly: (a) CREATE — it drives the REAL public intake `POST /api/public/checkout-session` (the exact call the quiz→details step fires), minting a genuinely NEW checkout_sessions row + funnel lead every run, and asserts the new session resolves; (b) VERIFY — it performs a real `POST /api/auth/login` credential round-trip. The credentials it logs in with are the designated testbed Assessment account's (config vars), NOT a freshly-minted password — because minting one is impossible here. This is the honest maximum; fabricating an end-to-end account creation was explicitly NOT done. See PLATFORM_BUILD.md #987 row / the issue for Shane's sign-off on this boundary.",

    "SCAN IS ASYNC + MAINTAINED-STATE ASSUMPTION: `POST /api/portal/assessment/debug-trigger-scan` returns 202 `{ runId, status:'pending' }` and runs `runDiagnostics` in the background; the executor cannot await its completion. The EXECUTE apiTests fire it to prove the real trigger contract (202 + a persisted active run + a backend-computed checksTotal — #887's honesty checks), but the verdict/pillars/documents/SOW assertions (API and UI) read the testbed Assessment account's MAINTAINED, already-completed scan — exactly as #888/#889/#890 already assumed. The deep `POST /api/portal/assessment/debug-reset-session` wipe (quiz + diagnostic runs + doc-gen + documents) is therefore DELIBERATELY NOT invoked inside this flow: wiping + async-re-triggering could not restore a completed verdict before the uiSteps run, and would leave every #888/#889 UI assertion asserting the unavailable state. Its contract is noted here for a future synchronous-scan-capable variant. Precondition: keep the testbed Assessment account in a scanned state (a completed scan with a verdict, ≥2 generated documents, and a resolvable SOW), same precondition #888–#890 already require.",

    "GRAPH-WRITE TEARDOWN (#986 teardown-graph-writes): the money path performs NO M365 Graph WRITE actions — the live scan is read-only diagnostics; Launch Control write-actions/remediation are a SEPARATE flow (covered by copilot-readiness/remediation-tracker-e2e.json). So there is nothing real to roll back here. Rather than fabricate a write, the TEARDOWN step exercises the rollback endpoint's dev-origin gate + admin/ingest auth + independent-failure envelope with a deliberately OUT-OF-RANGE auditLogId (proving the path is reachable and gated, returning `toreDown:true` with that id recorded as failed, since rollbackExecution never aborts the batch). A future variant that adds a real write-action captures that write's `/execute` auditLogId and passes it here NEWEST-FIRST (LIFO), per #986's contract.",

    "STRIPE / CHECKOUT (from #891): the journey-checkout HAPPY path (Stripe's own hosted card page) and the signed-scope precondition (`cj_signed_scope` in sessionStorage, set only by Screen 3's drawn-canvas signature) are NOT executor-drivable (WebView2+JS-injection cannot seed sessionStorage, draw the signature, or wait for the cross-origin Stripe redirect). Preserved here is #891's fully-runnable NEGATIVE honesty guardrail: on the checkout route with no signed scope, the 'no signed scope' panel is visible and the pay button is absent (the 2401cbe7 guardrail). Because this session never signs a scope, that negative state is the natural, real observation. Stripe is in TEST mode for any .replit.dev origin (stripe.ts getStripeKey → sk_test_), so nothing here can ever touch a live key.",

    "MUTUALLY-EXCLUSIVE POINT-IN-TIME UI STATES (why some #887/#891 UI selectors are represented at the API level instead of as uiSteps): a single linear WebView2 session holds ONE point in time. #887's live-scan OVERLAY (cj-scan-overlay / cj-scan-streaming-label / cj-scan-checks-done) is only visible DURING an in-progress scan, which is mutually exclusive with the COMPLETED-verdict state #888/#889/#890 need — so #887's Scene-0 honesty contract is preserved via its API assertions in EXECUTE (consentStatus granted, 202 trigger, active.runId, active.checksTotal) and its overlay selectors stay covered by re-running a mid-scan snapshot. Likewise #891's signed-scope happy path is mutually exclusive with its own no-signed-scope negative case; the negative case is what runs here.",

    "ENV VARS required in BuildConsole → Settings → Test Environment Variables: DEPLOY_URL (deployed origin serving both the marketing quiz at '/' and the portal at /login + /copilot-readiness — the same single origin all six source manifests targeted), SECRET_KEY (= BUILD_TRACKER_INGEST_TOKEN; the Bearer that satisfies requireAdminOrIngestToken on the #986 testbed endpoints and the #901 testbed-customers lookup, per requireAuth.ts), TEST_PORTAL_EMAIL / TEST_PORTAL_PASSWORD (the designated testbed Assessment account), TEST_PORTAL_SLUG (portal branding slug for the login route). The testbed customerId (tenants.id) is NOT a config var — it is read live from the DB via GET /api/admin/baseline-templates/testbed-customers (the same proven #901 bootstrap), so the reset always targets a real testbed customer, never a guessed literal.",

    "The #986 testbed endpoints are hard-gated to a real .replit.dev dev origin (requireDevOrigin, reusing isReplitDevEnvironment from stripe.ts); against a real deployment they 403 by design. Run this manifest against the .replit.dev dev origin. No raw DB access is used anywhere here — every clean/reset/rollback goes through the gated endpoints, per #987.",

    "CONSENT GROUND-TRUTH (this session, extends #900/#901 dual-verification): the EXECUTE consentStatus step only proves the PLATFORM'S OWN flag reads 'granted' — a scan-status DB value. The powerShellVerify step 'VERIFY (Graph ground truth) — write-app consent genuinely landed' independently confirms that claim against Microsoft Graph itself. Signed in as Shane's DELEGATED identity (PowerShellTestExecutor runs its OWN Connect-MgGraph -UseDeviceCode -ContextScope CurrentUser before every cmdlet — so the manifest cmdlet must NOT embed its own Connect / -TenantId / -Scopes; the executor owns the connection AND the #965 testbed gate), it FIRST asserts the delegated session is connected to the cross-tenant WRITE-TARGET tenant ({{GRAPH_PROD_TEST_TENANT_ID}} — Shane's dedicated, fully-clean test tenant the cross-tenant write app writes INTO, which is DISTINCT from the master {{GRAPH_TEST_TENANT_ID}} the app is registered/consented FROM), then resolves the write app's service principal by appId ({{MT_APP_WRITE_CLIENT_ID}}) IN THAT TARGET TENANT and asserts it carries real appRoleAssignments or oauth2PermissionGrants: it returns 'granted' ONLY when connected to the write-target tenant AND the SP genuinely exists there WITH grants, else 'WRONG-TENANT'/'NOT-FOUND'/'NO-GRANTS' which fail the exact-match against the platform's extracted {{appConsentStatus}}='granted'. WHY THE WRITE-TARGET TENANT, NOT THE MASTER (this session's correction on top of #84/6cb87e0f): the write app's architecture is genuinely cross-tenant — an admin-consent granted from the master tenant provisions the write app's SP + grants in a SEPARATE target directory, so checking the master/connected tenant (#84's original assumption) reads the wrong directory; the consent that actually authorizes the write lands in GRAPH_PROD_TEST_TENANT_ID. Before the cmdlet runs, the executor auto-gates the connected tenant against the server's isTestbed=true allowlist (#965), so this can never read a real customer tenant. Precondition (Shane, once): Connect-MgGraph interactively as yourself INTO the write-target tenant (GRAPH_PROD_TEST_TENANT_ID) with Application.Read.All (+ a directory read able to enumerate appRoleAssignments/oauth2 grants) so the cached delegated token is valid, and set BOTH MT_APP_WRITE_CLIENT_ID and the new GRAPH_PROD_TEST_TENANT_ID in Settings > Test Environment Variables (the #961 scan flags each needsReview with an <unset> default until you do) — and confirm GRAPH_PROD_TEST_TENANT_ID is flagged isTestbed=true on the server so the #965 gate admits it. On a headless run with no cached delegated token this step fails clearly ('run Connect-MgGraph interactively once'), never hangs — the honest maximum, mirroring smoke/hello-world-powershell.json.",

    "TEARDOWN OF THE WRITE-APP SP GRANTS — DELIBERATELY NOT ADDED HERE (honest reconciliation): the paired ask was to also REMOVE the write app's service-principal appRoleAssignments/oauth2 grants from the tenant BEFORE a consent-revocation teardown. It is NOT committed into this regression flow, for four reasons found by reading the executors directly, not assumed: (1) powerShellVerify is VERIFY-only — every entry REQUIRES an afterStep and its cmdlet is wrapped in `... | ConvertTo-Json` (a read), with no write/teardown semantics; (2) it is the LAST phase (apiTests->graphTests->postGraphApiTests->zohoTests->uiSteps->powerShellVerify), so a PowerShell teardown structurally cannot run BEFORE any other step in the flow; (3) there is NO consent-revocation step in this flow for it to precede — the platform's own PATCH /api/admin/consent/:tenantId/revoke merely flips a DB consent flag and never removes SP grants from Entra, and it is requireAdmin (unreachable with the ingest token) anyway; (4) MOST IMPORTANTLY, this money path performs NO in-run write-app consent GRANT (see the GRAPH-WRITE TEARDOWN note above — the scan is read-only), so the write app's appRoleAssignments/oauth2 grants in the testbed tenant are STANDING SHARED STATE that baseline-actions-powershell-verify.json (#901) and every write-action test depend on. Removing them here would strip the app-only WRITE consent those tests need and this flow never re-establishes it. A grant-teardown is only correct in a variant that FIRST performs a real in-run admin-consent grant (so cleanup removes ONLY what that run added). When that variant exists, the removal belongs in a real teardown/write phase (or a gated HTTP endpoint) placed BEFORE its revocation — e.g. for each Get-MgServicePrincipalAppRoleAssignment / Get-MgOauth2PermissionGrant on THAT run's SP, call Remove-MgServicePrincipalAppRoleAssignment / Remove-MgOauth2PermissionGrant, #965-gated exactly as the verify step here is, AND scoped to the cross-tenant WRITE-TARGET tenant (GRAPH_PROD_TEST_TENANT_ID) where the write app's grants actually live — NOT the master GRAPH_TEST_TENANT_ID (same cross-tenant correction this session applied to the verify step: the SP + grants exist in the target directory the write app writes into, so both the verify read and any future grant-teardown must run connected to GRAPH_PROD_TEST_TENANT_ID). Committing a blind 'remove all grants for the write app SP' into the green regression suite would corrupt the shared testbed — so it is documented, not shipped."
  ],

  "apiTests": [
    {
      "name": "SETUP 1/3 — bootstrap: resolve the real testbed customer id (tenants.id) from the DB so the reset is scoped to a real testbed customer, not a guessed literal (same #901 lookup)",
      "method": "GET",
      "path": "/api/admin/baseline-templates/testbed-customers",
      "headers": { "Authorization": "Bearer {{SECRET_KEY}}" },
      "expect": { "status": 200, "jsonPath": "$.customers[0].id", "exists": true },
      "extract": { "as": "testbedCustomerId", "jsonPath": "$.customers[0].id" }
    },
    {
      "name": "SETUP 2/3 — #986 testbed reset to a known-clean baseline: clear this testbed customer's tenant_engine_overrides + write-action audit log (dev-origin gated; never raw SQL). This is the 'clean database' step, via the endpoint #986 built for exactly this.",
      "method": "POST",
      "path": "/api/admin/testbed/reset",
      "headers": { "Authorization": "Bearer {{SECRET_KEY}}", "Content-Type": "application/json" },
      "body": { "customerId": "{{testbedCustomerId}}" },
      "expect": { "status": 200, "jsonPath": "$.reset", "value": true }
    },
    {
      "name": "SETUP 3/3 — log in as the designated testbed Assessment account to obtain a real bearer for the Assessment-role EXECUTE endpoints below; extract it as {{authToken}}",
      "method": "POST",
      "path": "/api/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "email": "{{TEST_PORTAL_EMAIL}}", "password": "{{TEST_PORTAL_PASSWORD}}" },
      "expect": { "status": 200, "jsonPath": "$.accessToken", "exists": true },
      "extract": { "as": "authToken", "jsonPath": "$.accessToken" }
    },

    {
      "name": "CREATE — genuine public intake: POST /api/public/checkout-session (the exact call the quiz→details step fires) mints a brand-NEW checkout_sessions row + funnel lead for the assessment product, from scratch this run; extract the new sessionId. This is the 'go through the purchase flow / create' step the harness CAN genuinely drive (see the CREATE honesty caveat in notes for the consent+payment wall past this point). Deliberately NOT {{TEST_PORTAL_EMAIL}} (Git #634 changed checkout-session to 409 already_has_account for that email — see the dedicated 409 test below) — checkout_sessions.email has no uniqueness constraint, so a fixed non-account probe address is safe to reuse every run.",
      "method": "POST",
      "path": "/api/public/checkout-session",
      "headers": { "Content-Type": "application/json" },
      "body": {
        "productSlug": "copilot-readiness-assessment",
        "fullName": "Regression Probe",
        "email": "money-path-e2e-probe@shanemccawconsulting.test",
        "company": "Money-Path E2E Testbed",
        "industry": "Technology",
        "seats": 1
      },
      "expect": { "status": 200, "jsonPath": "$.sessionId", "exists": true },
      "extract": { "as": "sessionId", "jsonPath": "$.sessionId" }
    },
    {
      "name": "CREATE (honesty) — the just-created session is a real, resolvable new funnel record: consent-status for {{sessionId}} returns a real sessionStatus, proving a genuinely new session was created this run (not a reused static one)",
      "method": "GET",
      "path": "/api/public/flow/consent-status?sessionId={{sessionId}}",
      "headers": { "Content-Type": "application/json" },
      "expect": { "status": 200, "jsonPath": "$.sessionStatus", "exists": true }
    },
    {
      "name": "CREATE (Git #634 negative path) — checkout-session now catches a returning customer at the FIRST step instead of only at set-password after payment: submitting the testbed Assessment account's own email (a real users row with a real passwordHash, proven by SETUP 3/3's login above) returns 409 already_has_account with a real portalUrl, and does NOT mint a new checkout session",
      "method": "POST",
      "path": "/api/public/checkout-session",
      "headers": { "Content-Type": "application/json" },
      "body": {
        "productSlug": "copilot-readiness-assessment",
        "fullName": "Regression Probe",
        "email": "{{TEST_PORTAL_EMAIL}}",
        "company": "Money-Path E2E Testbed",
        "industry": "Technology",
        "seats": 1
      },
      "expect": { "status": 409, "jsonPath": "$.error", "value": "already_has_account" }
    },

    {
      "name": "EXECUTE (#887 Scene-0 honesty) — the testbed tenant carries a real GRANTED Microsoft admin consent; the reveal's honesty contract requires a genuinely consented tenant, never the design's stand-in fixture. Extracts the platform's OWN consentStatus flag as {{appConsentStatus}} so the powerShellVerify step below can diff that claim against real Microsoft Graph ground truth (the write app's service-principal grants actually present in the tenant), per #900/#901's dual-verification doctrine — 'the platform says granted' is only trusted once an independent delegated Graph read agrees.",
      "method": "GET",
      "path": "/api/portal/scan-status",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.consentStatus", "value": "granted" },
      "extract": { "as": "appConsentStatus", "jsonPath": "$.consentStatus" }
    },
    {
      "name": "EXECUTE (#887 live-scan trigger) — trigger the real tenant scan the way Scene 0's auto-trigger effect does; hard-gated server-side to testbed tenants, so a 202 here also proves the account is genuinely testbed-flagged, not just asserted client-side; extract the run id",
      "method": "POST",
      "path": "/api/portal/assessment/debug-trigger-scan",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 202, "jsonPath": "$.runId", "exists": true },
      "extract": { "as": "scanRunId", "jsonPath": "$.runId" }
    },
    {
      "name": "EXECUTE (#887) — scan-status now reports a real ACTIVE run backed by the trigger above (the run row genuinely persisted, not a static/mocked response)",
      "method": "GET",
      "path": "/api/portal/scan-status",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.active.runId", "exists": true }
    },
    {
      "name": "EXECUTE (#887) — the active run carries a real backend-computed checksTotal (the count Scene 0's radar wedges + centre counter render from), not an empty placeholder",
      "method": "GET",
      "path": "/api/portal/scan-status",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.active.checksTotal", "exists": true }
    },
    {
      "name": "EXECUTE (#889 documents gate) — assessment status is the real payload the Documents screen's data hook (useCopilotJourney) polls; copilotGate present on the wire",
      "method": "GET",
      "path": "/api/portal/assessment/status",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.copilotGate", "exists": true }
    },
    {
      "name": "EXECUTE (#890 SOW) — sow resolves for the testbed tenant with a real `ready` flag on the wire",
      "method": "GET",
      "path": "/api/portal/assessment/sow",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.ready", "exists": true }
    },
    {
      "name": "EXECUTE (#890 SOW) — sow's toggleable workstreams come off the wire as a real array, never a hardcoded fixture",
      "method": "GET",
      "path": "/api/portal/assessment/sow",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.allWorkstreams", "isArray": true }
    },
    {
      "name": "EXECUTE (#890 SOW) — sow states the tenant's own stored selection, so a returning customer's real choices are read back rather than silently re-defaulted to everything-on",
      "method": "GET",
      "path": "/api/portal/assessment/sow",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.selectedWorkstreamTitles", "isArray": true }
    },
    {
      "name": "EXECUTE (#890 payment-options) — payment-options resolves with a real `ready` flag for the same active document",
      "method": "GET",
      "path": "/api/portal/assessment/sow/payment-options",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.ready", "exists": true }
    },
    {
      "name": "EXECUTE (#890 payment-options honesty) — the phased plan states the real platform fact: milestone billing is provider-arranged, selfServe is false, never an invented deposit charge (PLATFORM_BUILD 2401cbe7/2571ed56)",
      "method": "GET",
      "path": "/api/portal/assessment/sow/payment-options",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.phased.selfServe", "value": false }
    },
    {
      "name": "EXECUTE (#890 payment-options) — the per-phase breakdown is a real array, the same shape CheckoutPaymentChoice's indicative schedule reads",
      "method": "GET",
      "path": "/api/portal/assessment/sow/payment-options",
      "headers": { "Authorization": "Bearer {{authToken}}" },
      "expect": { "status": 200, "jsonPath": "$.phased.phases", "isArray": true }
    },

    {
      "name": "VERIFY — the money path's own credential round-trip (Shane: 'login with what you created is the test'): a fresh, unauthenticated POST /api/auth/login with the account credentials returns a real accessToken, proving the login money-path works end to end AFTER the whole execute flow. Honest scope: these are the testbed Assessment account's credentials — a fully harness-minted password is impossible past the consent+payment wall (see the CREATE honesty caveat).",
      "method": "POST",
      "path": "/api/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "email": "{{TEST_PORTAL_EMAIL}}", "password": "{{TEST_PORTAL_PASSWORD}}" },
      "expect": { "status": 200, "jsonPath": "$.accessToken", "exists": true }
    }
  ],

  "postGraphApiTests": [
    {
      "name": "TEARDOWN 1/2 — Graph-write rollback (#986 teardown-graph-writes): exercise the endpoint's dev-origin gate + admin/ingest auth + independent-failure envelope. The money path makes NO real M365 Graph WRITE actions (read-only scan; Launch Control writes are the separate remediation flow), so this is driven with a deliberately out-of-range auditLogId to prove the rollback path is reachable and gated without fabricating a write; the batch records it as failed and still returns toreDown:true. A future write-adding variant passes that write's real /execute auditLogId here, newest-first.",
      "method": "POST",
      "path": "/api/admin/testbed/teardown-graph-writes",
      "headers": { "Authorization": "Bearer {{SECRET_KEY}}", "Content-Type": "application/json" },
      "body": { "auditLogIds": [2000000000] },
      "expect": { "status": 200, "jsonPath": "$.toreDown", "value": true }
    },
    {
      "name": "TEARDOWN 2/2 — final #986 reset: clear this testbed customer's tenant_engine_overrides + write-action audit log again so the next run starts fresh with no accumulated sim-drift (conservative — does NOT delete the account, scan, verdict, documents, or SOW the uiSteps below still observe)",
      "method": "POST",
      "path": "/api/admin/testbed/reset",
      "headers": { "Authorization": "Bearer {{SECRET_KEY}}", "Content-Type": "application/json" },
      "body": { "customerId": "{{testbedCustomerId}}" },
      "expect": { "status": 200, "jsonPath": "$.reset", "value": true }
    }
  ],

  "uiSteps": [
    { "action": "goto", "target": "/" },
    { "action": "expect", "selector": "[data-testid='quiz-option-A']", "state": "visible" },

    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },
    { "action": "click", "selector": "[data-testid='quiz-option-A']" },

    { "action": "expect", "selector": "[data-testid='quiz-result']", "state": "visible" },
    { "action": "click", "selector": "[data-testid='get-real-score-cta']" },
    { "action": "expect", "selector": "[data-testid='assessment-flow-details-step']", "state": "visible" },

    { "action": "goto", "target": "/portal/shane-mccaw-consulting/login" },
    {
      "name": "Wait for login form to render before typing",
      "action": "expect",
      "selector": "#email",
      "state": "visible",
      "critical": true
    },
    { "action": "input", "selector": "#email", "value": "{{TEST_PORTAL_EMAIL}}", "critical": true },
    { "action": "input", "selector": "#password", "value": "{{TEST_PORTAL_PASSWORD}}", "critical": true },
    {
      "name": "Portal login — CRITICAL: authenticate before the copilot-readiness verdict/pillars/documents/proposal/checkout assertions below. A failed login makes every one of those downstream steps meaningless, so the run ABORTS here immediately on failure rather than cascading them all as failures that are really this one root cause.",
      "action": "click",
      "selector": "[data-testid='login-submit']",
      "critical": true,
      "captureResponse": {
        "urlPattern": "/api/auth/login",
        "expect": { "status": 200 }
      }
    },

    { "action": "goto", "target": "/portal/shane-mccaw-consulting/copilot-readiness" },

    {
      "name": "Wait for live scan / Scene 0 overlay to finish and reveal Scene 1 verdict",
      "action": "expect",
      "selector": "[data-cj-scene='1']",
      "state": "visible",
      "timeoutMs": 90000
    },
    { "action": "expect", "selector": "[data-testid='preview-badge']", "state": "hidden" },

    { "action": "expect", "selector": "[data-testid='verdict-score']", "state": "visible" },
    { "action": "expect", "selector": "[data-testid='verdict-score-unavailable']", "state": "hidden" },

    { "action": "expect", "selector": "[data-cj-scene='2'] [data-testid='pillar-headline']", "state": "visible" },
    { "action": "expect", "selector": "[data-cj-scene='2'] [data-testid='pillar-headline-unavailable']", "state": "hidden" },
    { "action": "expect", "selector": "[data-cj-scene='2'] [data-testid='pillar-score']", "state": "visible" },
    { "action": "expect", "selector": "[data-cj-scene='2'] [data-testid='pillar-score-unavailable']", "state": "hidden" },

    { "action": "expect", "selector": "[data-cj-scene='3'] [data-testid='pillar-headline']", "state": "visible" },
    { "action": "expect", "selector": "[data-cj-scene='3'] [data-testid='pillar-headline-unavailable']", "state": "hidden" },
    { "action": "expect", "selector": "[data-cj-scene='3'] [data-testid='pillar-score']", "state": "visible" },
    { "action": "expect", "selector": "[data-cj-scene='3'] [data-testid='pillar-score-unavailable']", "state": "hidden" },

    { "action": "goto", "target": "/portal/shane-mccaw-consulting/copilot-readiness/documents" },

    { "action": "expect", "selector": "[data-testid='document-reading-pane']", "state": "visible" },
    { "action": "expect", "selector": "[data-testid='active-document-title']", "state": "visible" },

    {
      "name": "Navigate to Statement of Work via persistent header CTA button",
      "action": "click",
      "selector": "[data-testid='sow-cta-button']"
    },
    {
      "name": "Verify Statement of Work document loaded and sales offer engine responded",
      "action": "expect",
      "selector": "[data-testid='sow-document']",
      "state": "visible",
      "timeoutMs": 30000
    },
    {
      "name": "Phase cards with toggles are rendered",
      "action": "expect",
      "selector": "[data-testid='sow-phase-toggle']",
      "state": "visible",
      "timeoutMs": 15000
    },
    {
      "name": "Price calculation net fee is displayed",
      "action": "expect",
      "selector": "[data-testid='sow-net-fee']",
      "state": "visible"
    },
    {
      "name": "Toggle an unlocked phase in the SOW to test price recalculation",
      "action": "click",
      "selector": "[data-testid='sow-phase-toggle']"
    },
    {
      "name": "Updated price remains visible after toggling scope",
      "action": "expect",
      "selector": "[data-testid='sow-net-fee']",
      "state": "visible"
    },
    {
      "name": "Signature and inline payment section is visible",
      "action": "expect",
      "selector": "[data-testid='sow-signature-section']",
      "state": "visible"
    },
    {
      "name": "Pay in Full payment option is available",
      "action": "expect",
      "selector": "[data-testid='sow-plan-pay-in-full']",
      "state": "visible"
    },
    {
      "name": "Pay by Phase payment option is available",
      "action": "expect",
      "selector": "[data-testid='sow-plan-pay-by-phase']",
      "state": "visible"
    },
    {
      "name": "Switch payment plan to Pay by Phase",
      "action": "click",
      "selector": "[data-testid='sow-plan-pay-by-phase']"
    },
    {
      "name": "Pay by Phase option remains active",
      "action": "expect",
      "selector": "[data-testid='sow-plan-pay-by-phase']",
      "state": "visible"
    }
  ],

  "powerShellVerify": [
    {
      "name": "VERIFY (Graph ground truth) — write-app consent genuinely landed in the CROSS-TENANT WRITE-TARGET tenant (GRAPH_PROD_TEST_TENANT_ID), not the master: the cross-tenant write app is registered/consented FROM the master tenant (GRAPH_TEST_TENANT_ID) but writes INTO a different, dedicated clean test tenant, so its service principal + admin-consent grants land in THAT target tenant. This step first asserts the delegated Connect-MgGraph session is actually connected to {{GRAPH_PROD_TEST_TENANT_ID}} (else 'WRONG-TENANT'), then confirms the write app's service principal (appId {{MT_APP_WRITE_CLIENT_ID}}) exists there AND carries real appRoleAssignments or oauth2 permission grants — the actual Entra state behind the platform's {{appConsentStatus}}='granted' flag, read as Shane's delegated identity (NOT the app's own client-credentials). Executor owns the delegated Connect-MgGraph + the #965 isTestbed gate; this cmdlet is a pure Graph READ that returns 'granted' only when connected to the write-target tenant AND the SP truly exists there with grants, else 'WRONG-TENANT'/'NOT-FOUND'/'NO-GRANTS' (which fail the exact-match, flagging that the platform claimed consent the write-target tenant does not actually reflect).",
      "afterStep": "appConsentStatus",
      "cmdlet": "$( $__tid = [string](Get-MgContext).TenantId; if ($__tid -ne '{{GRAPH_PROD_TEST_TENANT_ID}}') { 'WRONG-TENANT' } else { $__sp = Get-MgServicePrincipal -Filter 'appId eq ''{{MT_APP_WRITE_CLIENT_ID}}''' -All -ErrorAction SilentlyContinue | Select-Object -First 1; if (-not $__sp) { 'NOT-FOUND' } else { $__ra = @(Get-MgServicePrincipalAppRoleAssignment -ServicePrincipalId $__sp.Id -All -ErrorAction SilentlyContinue); $__gr = @(Get-MgOauth2PermissionGrant -All -ErrorAction SilentlyContinue | Where-Object { $_.ClientId -eq $__sp.Id }); if ($__ra.Count -gt 0 -or $__gr.Count -gt 0) { 'granted' } else { 'NO-GRANTS' } } } )",
      "compareField": { "matchType": "exact" },
      "timeoutMs": 90000
    }
  ]
}
