{
  "feature": "Customer Portal v2 -- MY ARCHITECT / the retainer page (/portal-v2/retainer), recreated from 'Customer Portal Shell.dc.html' markup 1981-2196 and logic class 15852-15984. A NEW page: the retainer carried 19% of its design copy (all of it built in the logic class, invisible to the page) and had no route before this. Eight retained hours a month shown as a working log at two zoom levels -- the month rollup (hours used, findings closed, what is next) and a week you select in place, which swaps the report card without moving the month.",
  "baseUrl": "{{DEPLOY_URL}}",
  "lastVerifiedAgainstCommit": "ea364b50",
  "notes": [
    "THE LOGIN BLOCK IS NOT OPTIONAL. /portal-v2/* sits behind ProtectedRoute.",
    "UI ROUTE SHAPE: /portal/{slug}/portal-v2/retainer. App.tsx declares '/portal-v2/retainer' ABOVE the '/portal-v2/:pillar' insertion marker so the param route does not swallow it.",
    "shaneapp://runTest hits the DEPLOYED build, not the local worktree -- these brand-new routes assert green only after the pushed commit is deployed.",
    "THE HOUR FIGURES ARE DERIVED, NOT STORED (retainerModel.ts). RET_HOURS is { retained: 8, rolled: 2, used: 5.5 }; available = 10.0, remaining = 4.5, used% = 55. RET1/RET2 read them off the DOM, which is what proves the arithmetic happens rather than being typed in. retainerModel.test.ts pins the same numbers at the unit level.",
    "THE WEEK SELECTOR IS REAL LOCAL STATE. RET6 clicks the W33 tab and RET7 asserts the report card swapped to that week's summary -- proof the selection re-renders in place rather than navigating. The month rollup above it does not move.",
    "AN EMPTY WEEK OMITS ITS BLOCKS. W31 logged no hours, so its report has no Work log / Produced / asks blocks (buildWeekView's has* flags). Not asserted here beyond the switch, because a negative 'absent' assertion is awkward in this harness; retainerModel.test.ts covers it.",
    "THERE IS NO NEGATIVE TEXT ASSERTION IN THIS HARNESS, and an array in `textContains` is ANY-of, not all-of. Every all-of assertion here is its own step.",
    "NO `select` STEPS. UiTestExecutor.cs cannot drive a native <select>; this page has none in the asserted flow.",
    "UI ONLY: the ask box, the Open-as-PDF / Open / Book time / Billing buttons are visual, to be wired in a later pass. The weekly report content (summary, per-line work log, deliverables, asks), RET_OUTCOMES, RET_TERMS, RET_DOCS and RET_COPY have no backend source and stay design fixture.",
    "LIVE WIRING (#1285/#1293): the month bucket (retained/rolled/used) and the 'Where the hours went' work log now read GET /api/portal/retainer (useRetainerLive.ts) -- real once the customer has an active retainer row (AdminV2 Retainer Hours, admin-retainer.ts). RET14 below reads the hidden pv2-ret-source marker; it asserts 'fixture' because the harness's TEST_PORTAL_EMAIL account is not enrolled on a retainer, so RET1-RET5's fixture numbers stay valid. A future manifest pass against an enrolled test account should assert 'live' and the real bucket/work-log numbers instead.",
    "ENV VARS required: DEPLOY_URL, TEST_PORTAL_EMAIL / TEST_PORTAL_PASSWORD.",
    "LOADING-STATE FIXTURE FLASH FIX (Git #1365) -- the month rollup cards and the 'Where the hours went' work log now render PortalV2LoadingState while GET /api/portal/retainer is in flight, instead of the design fixture, matching the pattern #1343 established for Ownership/Change Control. Not asserted with a new uiStep here: the loading window is typically sub-200ms against the dev/test API, so gating a step on it would be a flaky race rather than reliable coverage. RET1-RET5/RET14's settled fixture assertions above are unchanged."
  ],
  "uiSteps": [
    { "name": "goto the real login URL.", "action": "goto", "target": "/portal/login" },
    { "name": "WAIT for the login form to hydrate.", "action": "expect", "selector": "#email", "state": "visible" },
    { "action": "input", "selector": "#email", "value": "{{TEST_PORTAL_EMAIL}}" },
    { "action": "input", "selector": "#password", "value": "{{TEST_PORTAL_PASSWORD}}" },
    { "name": "Primary login.", "action": "click", "selector": "[data-testid='login-submit']", "critical": true },

    { "name": "goto My Architect. It must resolve rather than fall through to the param pillar route or NotFound.", "action": "goto", "target": "/portal/shane-mccaw-consulting/portal-v2/retainer" },
    { "name": "RET-title -- the page resolved and names itself.", "action": "expect", "selector": "[data-testid='pv2-page-title']", "textContains": "My Architect · August 2026", "critical": true },

    { "name": "RET1 -- the time card shows the DERIVED total, not a stored one: 5.5 of 10.0.", "action": "expect", "selector": "[data-testid='pv2-ret-time-card']", "textContains": "of 10.0 hours used", "critical": true },
    { "name": "RET2 -- ...and the derived used percentage.", "action": "expect", "selector": "[data-testid='pv2-ret-time-card']", "textContains": "55%" },
    { "name": "RET3 -- ...and the rolled-forward line with its expiry.", "action": "expect", "selector": "[data-testid='pv2-ret-time-card']", "textContains": "2.0 hours · expire 31 Aug" },

    { "name": "RET4 -- the work log names the first item in full, with its outcome.", "action": "expect", "selector": "[data-testid='pv2-ret-work']", "textContains": "Entra Connect: cleared 11 of 14 sync errors, staged the upgrade", "critical": true },
    { "name": "RET5 -- ...and a closed item shows the finding it belongs to.", "action": "expect", "selector": "[data-testid='pv2-ret-work']", "textContains": "CMP-01" },
    { "name": "RET14 -- the hidden source marker states this account is NOT enrolled on a real retainer, so RET1-RET5 read the design fixture rather than a live (empty) bucket.", "action": "expect", "selector": "[data-testid='pv2-ret-source']", "textContains": "fixture", "sessionTag": "1285-live-wiring" },

    { "name": "RET6a -- the current week (W34) is selected by default; its report leads with the sync-server summary.", "action": "expect", "selector": "[data-testid='pv2-ret-week-report']", "textContains": "Most of this week went into the sync server", "critical": true },
    { "name": "RET6 -- switch the week in place by clicking the W33 tab.", "action": "click", "selector": "[data-testid='pv2-ret-week-tab-W33']", "critical": true },
    { "name": "RET7 -- the report card swapped to W33's summary WITHOUT navigating.", "action": "expect", "selector": "[data-testid='pv2-ret-week-report']", "textContains": "Two findings closed this week", "critical": true },
    { "name": "RET8 -- ...and W33's header states its logged total.", "action": "expect", "selector": "[data-testid='pv2-ret-week-report']", "textContains": "2.5 h logged" },

    { "name": "RET9 -- the 'what the retainer has produced' rail carries the since-June outcome.", "action": "expect", "selector": "[data-testid='pv2-ret-produced']", "textContains": "6 findings closed and verified by re-scan", "critical": true },
    { "name": "RET10 -- the documents rail lists the week's status report.", "action": "expect", "selector": "[data-testid='pv2-ret-docs']", "textContains": "Weekly status report · W33" },
    { "name": "RET11 -- the terms block states the retained-hours price.", "action": "expect", "selector": "[data-testid='pv2-ret-terms']", "textContains": "8 hours a month, $2,400" },

    { "name": "RET12 -- Back to Overview is a real link to a real route.", "action": "click", "selector": "[data-testid='pv2-ret-back']", "critical": true },
    { "name": "RET13 -- ...and it lands on the Overview, not a 404.", "action": "expect", "selector": "[data-testid='pv2-main']", "state": "visible" }
  ]
}
