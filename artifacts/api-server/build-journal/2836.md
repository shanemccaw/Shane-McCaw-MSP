# #2836 — CRLF row-boundary regex fix in compliance-surface-coverage-2762.test.ts

- **Status:** ✅ DONE 2026-09-04T14:35:00Z
- **Scope:** platform

## Log
- ⏳ IN FLIGHT 2026-09-04T14:30:00Z — Investigated `readShippedConfig()` in
  `artifacts/api-server/src/lib/__tests__/compliance-surface-coverage-2762.test.ts`.
  Found the CRLF normalization fix (`.replace(/\r\n/g, "\n")`) already present at line 62,
  committed in `70e89493ad` ("api-server tests: fix remaining 26 stale-fixture failures (#1772)"),
  authored 2026-09-04 04:57:02 -0400 — after #2836's issue body was written but before this
  build was dispatched. That commit is already an ancestor of this worktree's HEAD and of
  `origin/main`.
- ✅ DONE 2026-09-04T14:35:00Z — Ran the test file fresh in this worktree
  (`cd artifacts/api-server && npx vitest run src/lib/__tests__/compliance-surface-coverage-2762.test.ts`):
  26/26 passed. Confirmed via `git blame` that the fix line is `.replace(/\r\n/g, "\n")` before
  the row-boundary string search, same pattern #2763 applied to its own copy. No code change
  needed this session — the fix was already landed on `main` by a prior session/commit before
  this build was dispatched. Working tree clean (`git status --porcelain` empty). No commit
  made by this session.
