// Re-export from the shared context so any legacy import still works
export { useQuickAccess, DEFAULT_ITEMS } from "@/contexts/QuickAccessContext";
export type { QuickAccessItem } from "@/contexts/QuickAccessContext";
